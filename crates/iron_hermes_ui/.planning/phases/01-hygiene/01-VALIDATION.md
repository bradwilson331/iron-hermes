---
phase: 1
slug: hygiene
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-02
---

# Phase 1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None configured (out-of-scope for v1 per REQUIREMENTS.md TEST-01..03) |
| **Config file** | none |
| **Quick run command** | `cargo build --features web` |
| **Full suite command** | `cargo build --features web && cargo build --features desktop && cargo build --features mobile` |
| **Estimated runtime** | ~30–60 seconds (clean), ~3–10 seconds (incremental) |

---

## Sampling Rate

- **After every task commit:** Run `cargo build --features web`
- **After every plan wave:** Run full suite (all three platforms)
- **Before `/gsd-verify-work`:** Full suite green AND `Cargo.lock` is staged
- **Max feedback latency:** ≤60 seconds (cold), ≤10s (incremental)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 1-01-01 | 01 | 1 | HYG-01 | — | N/A | smoke | `grep -q 'version = "=0.7.1"' Cargo.toml && grep -q 'web = \["dioxus/web"\]' Cargo.toml && grep -q 'desktop = \["dioxus/desktop"\]' Cargo.toml && grep -q 'mobile = \["dioxus/mobile"\]' Cargo.toml` | ✅ Cargo.toml | ⬜ pending |
| 1-02-01 | 02 | 1 | HYG-02 | — | N/A | smoke | `test -f Cargo.lock && git ls-files --error-unmatch Cargo.lock` | ❌ W0 | ⬜ pending |
| 1-03-01 | 03 | 1 | HYG-03 | — | N/A | smoke | `grep -q 'tailwind_input = "tailwind.css"' Dioxus.toml && grep -q 'tailwind_output = "assets/tailwind.css"' Dioxus.toml` | ✅ Dioxus.toml | ⬜ pending |
| 1-04-01 | 04 | 2 | HYG-04 | — | N/A | compile | `cargo build --features web` | ❌ W0 | ⬜ pending |
| 1-04-02 | 04 | 2 | HYG-04 | — | N/A | smoke | `test -f src/app.rs && test -f src/components/mod.rs && test -f src/state.rs && test -f src/platform/mod.rs` | ❌ W0 | ⬜ pending |
| 1-05-01 | 05 | 1 | HYG-05 | — | N/A | smoke | `grep -q '\*\*/\.DS_Store' .gitignore && grep -q 'warp2ironhermes-handoff\.zip' .gitignore` | ✅ .gitignore | ⬜ pending |
| 1-PHASE-GATE | gate | 3 | HYG-01..05 | — | N/A | compile | `cargo build --features web && cargo build --features desktop && cargo build --features mobile` | gate | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `Cargo.lock` — generated by `cargo build`, then staged with `git add Cargo.lock` (HYG-02)
- [ ] `src/app.rs` — extracted `App` component from `main.rs` (HYG-04)
- [ ] `src/components/mod.rs` — components module root with `pub mod hero;` and `pub use hero::Hero;` (HYG-04)
- [ ] `src/components/hero.rs` — `Hero` component moved from `main.rs` (HYG-04)
- [ ] `src/state.rs` — empty stub with `// reserved for shared signal state` (HYG-04)
- [ ] `src/platform/mod.rs` — empty stub with `// reserved for #[cfg(feature = "...")]` modules (HYG-04)

No external test framework install needed — Phase 1 success is verified via `cargo build` + `grep` smoke checks. The first phase that needs `cargo test` is post-v1.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `dx serve` regenerates `assets/tailwind.css` automatically on edit of `tailwind.css` | HYG-03 | Requires running dev server and observing hot-reload — cannot be unit-tested | 1. `dx serve` 2. Touch `tailwind.css` 3. Confirm `assets/tailwind.css` mtime updates and contains compiled output |
| All three platform features compile cleanly from a fresh `cargo clean` | HYG-01, HYG-04 | Cold build is slow (~60s) and disk-bound; gate-only check | `cargo clean && cargo build --features web && cargo build --features desktop && cargo build --features mobile` |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (`Cargo.lock`, new `src/` files)
- [ ] No watch-mode flags (`dx serve` is manual gate only)
- [ ] Feedback latency < 60s (cold) / 10s (incremental)
- [ ] `nyquist_compliant: true` set in frontmatter once planner has populated all task IDs

**Approval:** pending
