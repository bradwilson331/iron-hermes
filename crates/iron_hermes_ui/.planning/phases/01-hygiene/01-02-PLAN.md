---
phase: 01-hygiene
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01-PLAN.md
files_modified:
  - src/main.rs
  - src/app.rs
  - src/components/mod.rs
  - src/components/hero.rs
  - src/state.rs
  - src/platform/mod.rs
autonomous: true
requirements:
  - HYG-04

must_haves:
  truths:
    - "src/ is no longer a single-file monolith — it has at least 6 source files across a module hierarchy"
    - "The project compiles with cargo build --features web after the split"
    - "App component is in src/app.rs, Hero component is in src/components/hero.rs"
    - "src/state.rs and src/platform/mod.rs exist as stubs for later phases"
  artifacts:
    - path: "src/main.rs"
      provides: "Entry point with mod declarations and fn main()"
      contains: "mod app"
    - path: "src/app.rs"
      provides: "App root component"
      exports: ["App"]
    - path: "src/components/mod.rs"
      provides: "Components module re-exports"
      contains: "pub use hero::Hero"
    - path: "src/components/hero.rs"
      provides: "Hero scaffold component"
      exports: ["Hero"]
    - path: "src/state.rs"
      provides: "Phase 4 placeholder stub"
      contains: "// Phase placeholder"
    - path: "src/platform/mod.rs"
      provides: "Phase 6 placeholder stub"
      contains: "// Phase placeholder"
  key_links:
    - from: "src/main.rs"
      to: "src/app.rs"
      via: "mod app; use app::App;"
      pattern: "mod app"
    - from: "src/app.rs"
      to: "src/components/hero.rs"
      via: "use crate::components::Hero;"
      pattern: "crate::components::Hero"
    - from: "src/components/mod.rs"
      to: "src/components/hero.rs"
      via: "mod hero; pub use hero::Hero;"
      pattern: "pub use hero::Hero"
---

<objective>
Split the monolithic `src/main.rs` into a proper Rust module hierarchy: entry point, App component, Hero component, and two placeholder stubs for state and platform modules.

Purpose: Establishes the module structure that every subsequent phase will build into. Without this split, Phases 2–6 would be adding more code to an ever-growing single file, which is unworkable. The `src/components/`, `src/state.rs`, and `src/platform/` boundaries mirror the prototype's component architecture and the project's multi-platform constraint.

Output: 5 new files created (`src/app.rs`, `src/components/mod.rs`, `src/components/hero.rs`, `src/state.rs`, `src/platform/mod.rs`), `src/main.rs` slimmed down to entry point + mod declarations. Project must compile cleanly after the split.
</objective>

<execution_context>
@/Users/twilson/code/iron_hermes_ui/AGENTS.md
</execution_context>

<context>
@/Users/twilson/code/iron_hermes_ui/.planning/PROJECT.md
@/Users/twilson/code/iron_hermes_ui/.planning/ROADMAP.md
@/Users/twilson/code/iron_hermes_ui/.planning/phases/01-hygiene/01-RESEARCH.md
@/Users/twilson/code/iron_hermes_ui/.planning/phases/01-hygiene/01-PATTERNS.md

<interfaces>
<!-- Source of truth: current src/main.rs (read before editing). -->
<!-- All new files are derived from or split out of this file. -->

Current `src/main.rs` (full file — 38 lines):
```rust
use dioxus::prelude::*;

const FAVICON: Asset = asset!("/assets/favicon.ico");
const MAIN_CSS: Asset = asset!("/assets/main.css");
const HEADER_SVG: Asset = asset!("/assets/header.svg");
const TAILWIND_CSS: Asset = asset!("/assets/tailwind.css");

fn main() {
    dioxus::launch(App);
}

#[component]
fn App() -> Element {
    rsx! {
        document::Link { rel: "icon", href: FAVICON }
        document::Link { rel: "stylesheet", href: MAIN_CSS } document::Link { rel: "stylesheet", href: TAILWIND_CSS }
        Hero {}

    }
}

#[component]
pub fn Hero() -> Element {
    rsx! {
        div {
            id: "hero",
            img { src: HEADER_SVG, id: "header" }
            div { id: "links",
                a { href: "https://dioxuslabs.com/learn/0.7/", "📚 Learn Dioxus" }
                a { href: "https://dioxuslabs.com/awesome", "🚀 Awesome Dioxus" }
                a { href: "https://github.com/dioxus-community/", "📡 Community Libraries" }
                a { href: "https://github.com/DioxusLabs/sdk", "⚙️ Dioxus Development Kit" }
                a { href: "https://marketplace.visualstudio.com/items?itemName=DioxusLabs.dioxus", "💫 VSCode Extension" }
                a { href: "https://discord.gg/XgGxMSkvUM", "👋 Community Discord" }
            }
        }
    }
}
```

Target file contents are specified verbatim in task actions below.
Do not deviate from the specified content — these are exact transcriptions
from PATTERNS.md with visibility and import corrections applied.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1-04-01: Create new module files (src/app.rs, src/components/, src/state.rs, src/platform/mod.rs)</name>
  <files>src/app.rs, src/components/mod.rs, src/components/hero.rs, src/state.rs, src/platform/mod.rs</files>
  <read_first>
    - /Users/twilson/code/iron_hermes_ui/src/main.rs — read the full file; App (lines 12–20) and Hero (lines 22–38) are the source content to move
    - /Users/twilson/code/iron_hermes_ui/.planning/phases/01-hygiene/01-PATTERNS.md — sections for src/app.rs, src/components/mod.rs, src/components/hero.rs contain exact target content with all import corrections already applied
    - /Users/twilson/code/iron_hermes_ui/AGENTS.md — Dioxus 0.7 component rules (#[component], PascalCase, returns Element, no cx/Scope)
  </read_first>
  <action>
Create five files with the exact content specified below. Do NOT copy-paste from `src/main.rs` blindly — the imports, visibility modifiers (`pub`), and asset constant placements differ from the source.

**Create `src/app.rs`:**
```rust
use dioxus::prelude::*;
use crate::components::Hero;

const FAVICON: Asset = asset!("/assets/favicon.ico");
const MAIN_CSS: Asset = asset!("/assets/main.css");
const TAILWIND_CSS: Asset = asset!("/assets/tailwind.css");

#[component]
pub fn App() -> Element {
    rsx! {
        document::Link { rel: "icon", href: FAVICON }
        document::Link { rel: "stylesheet", href: MAIN_CSS }
        document::Link { rel: "stylesheet", href: TAILWIND_CSS }
        Hero {}
    }
}
```
Key deltas from `main.rs`: add `pub`, add `use crate::components::Hero;`, declare FAVICON/MAIN_CSS/TAILWIND_CSS here (not HEADER_SVG — that belongs in hero.rs), fix the two-Link-on-one-line formatting bug from main.rs.

**Create `src/components/mod.rs`:**
```rust
mod hero;

pub use hero::Hero;
```
`mod hero;` is private (standard Rust). `pub use hero::Hero` re-exports Hero so callers can write `use crate::components::Hero;`.

**Create `src/components/hero.rs`:**
```rust
use dioxus::prelude::*;

const HEADER_SVG: Asset = asset!("/assets/header.svg");

#[component]
pub fn Hero() -> Element {
    rsx! {
        div {
            id: "hero",
            img { src: HEADER_SVG, id: "header" }
            div { id: "links",
                a { href: "https://dioxuslabs.com/learn/0.7/", "📚 Learn Dioxus" }
                a { href: "https://dioxuslabs.com/awesome", "🚀 Awesome Dioxus" }
                a { href: "https://github.com/dioxus-community/", "📡 Community Libraries" }
                a { href: "https://github.com/DioxusLabs/sdk", "⚙️ Dioxus Development Kit" }
                a { href: "https://marketplace.visualstudio.com/items?itemName=DioxusLabs.dioxus", "💫 VSCode Extension" }
                a { href: "https://discord.gg/XgGxMSkvUM", "👋 Community Discord" }
            }
        }
    }
}
```
This is the exact Hero from main.rs, with HEADER_SVG asset constant brought into this file. Hero was already `pub` in main.rs — keep it `pub`.

**Create `src/state.rs`:**
```rust
// Phase placeholder — implementation begins in Phase 4 (Data Layer & Interactions).
```

**Create `src/platform/mod.rs`:**
```rust
// Phase placeholder — implementation begins in Phase 6 (Mobile Shell).
// Platform-specific behavior will be gated via #[cfg(feature = "web")],
// #[cfg(feature = "desktop")], and #[cfg(feature = "mobile")] in this module.
```

Note: `src/platform/` directory must be created before writing `src/platform/mod.rs`. The Write tool will create parent directories automatically, but verify the directory exists after writing.
  </action>
  <verify>
    <automated>test -f /Users/twilson/code/iron_hermes_ui/src/app.rs && test -f /Users/twilson/code/iron_hermes_ui/src/components/mod.rs && test -f /Users/twilson/code/iron_hermes_ui/src/components/hero.rs && test -f /Users/twilson/code/iron_hermes_ui/src/state.rs && test -f /Users/twilson/code/iron_hermes_ui/src/platform/mod.rs && echo "PASS: all 5 new files exist" || echo "FAIL: one or more files missing"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/app.rs` exits 0
    - `test -f src/components/mod.rs` exits 0
    - `test -f src/components/hero.rs` exits 0
    - `test -f src/state.rs` exits 0
    - `test -f src/platform/mod.rs` exits 0
    - `grep -q 'pub fn App' src/app.rs` exits 0
    - `grep -q 'use crate::components::Hero' src/app.rs` exits 0
    - `grep -q 'pub use hero::Hero' src/components/mod.rs` exits 0
    - `grep -q 'pub fn Hero' src/components/hero.rs` exits 0
    - `grep -q 'HEADER_SVG' src/components/hero.rs` exits 0
    - `grep -q 'Phase placeholder' src/state.rs` exits 0
    - `grep -q 'Phase placeholder' src/platform/mod.rs` exits 0
  </acceptance_criteria>
  <done>All 5 new files exist with correct content: App in app.rs, Hero in components/hero.rs, re-export in components/mod.rs, placeholder stubs in state.rs and platform/mod.rs</done>
</task>

<task type="auto">
  <name>Task 1-04-02: Slim down src/main.rs to entry point + mod declarations, then verify compile</name>
  <files>src/main.rs</files>
  <read_first>
    - /Users/twilson/code/iron_hermes_ui/src/main.rs — read current content before overwriting; confirm App and Hero components are still there (they were not yet removed)
    - /Users/twilson/code/iron_hermes_ui/src/app.rs — confirm it exists and has pub fn App before removing App from main.rs
    - /Users/twilson/code/iron_hermes_ui/src/components/hero.rs — confirm Hero is there before removing it from main.rs
  </read_first>
  <action>
Replace `src/main.rs` with the slimmed-down entry point. The App and Hero components have already been moved to their new modules in the previous task, so this file now only needs: the module declarations, the App import, and `fn main()`.

**New content for `src/main.rs`:**
```rust
use dioxus::prelude::*;

mod app;
mod components;
mod state;
mod platform;

use app::App;

fn main() {
    dioxus::launch(App);
}
```

Key points:
- All four asset constants (FAVICON, MAIN_CSS, HEADER_SVG, TAILWIND_CSS) are removed from main.rs — they now live in the modules that use them (FAVICON/MAIN_CSS/TAILWIND_CSS in app.rs, HEADER_SVG in hero.rs)
- `mod app;` declares the `src/app.rs` module
- `mod components;` declares the `src/components/` module (Rust finds `src/components/mod.rs` automatically)
- `mod state;` declares `src/state.rs`
- `mod platform;` declares `src/platform/` (Rust finds `src/platform/mod.rs` automatically)
- `use app::App;` brings App into scope so `dioxus::launch(App)` compiles
- No `pub use` needed here — nothing else imports from main

After writing the file, run the compile check:
```bash
cd /Users/twilson/code/iron_hermes_ui && cargo build --features web
```

If the build fails, read the compiler error carefully:
- `error[E0603]: function App is private` → check that `pub fn App` is in src/app.rs
- `error[E0603]: function Hero is private` → check that `pub fn Hero` is in src/components/hero.rs and `pub use hero::Hero` is in components/mod.rs
- `error[E0425]: cannot find function App in module app` → check that `mod app;` is present and src/app.rs exists
- `error: unresolved import crate::components::Hero` → check components/mod.rs has `pub use hero::Hero`
  </action>
  <verify>
    <automated>cd /Users/twilson/code/iron_hermes_ui && cargo build --features web 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'mod app' src/main.rs` exits 0
    - `grep -q 'mod components' src/main.rs` exits 0
    - `grep -q 'mod state' src/main.rs` exits 0
    - `grep -q 'mod platform' src/main.rs` exits 0
    - `grep -q 'use app::App' src/main.rs` exits 0
    - `grep -q 'fn main' src/main.rs` exits 0
    - `grep -q 'fn App' src/main.rs` exits 1 — App is gone from main.rs
    - `grep -q 'fn Hero' src/main.rs` exits 1 — Hero is gone from main.rs
    - `cargo build --features web` exits 0 with no errors
  </acceptance_criteria>
  <done>src/main.rs contains only mod declarations + fn main(), App and Hero are fully moved to their new modules, and `cargo build --features web` exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan performs a pure Rust source reorganization — file moves and module wiring. No data crosses any trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | — | src/ module tree | accept | No security attack surface introduced by this plan. Changes are a mechanical Rust module split with no new logic, no new I/O, no new dependencies, no user input, no network calls, and no secrets. The `src/state.rs` and `src/platform/mod.rs` stubs are empty comment-only files with zero executable code. ASVS V2–V6 do not apply. |
</threat_model>

<verification>
After both tasks complete:

```bash
# File structure check
test -f src/main.rs && \
test -f src/app.rs && \
test -f src/components/mod.rs && \
test -f src/components/hero.rs && \
test -f src/state.rs && \
test -f src/platform/mod.rs && \
echo "PASS: all files present"

# Content checks
grep -q 'mod app' src/main.rs && echo "main.rs: mod app OK"
grep -q 'pub fn App' src/app.rs && echo "app.rs: pub fn App OK"
grep -q 'pub use hero::Hero' src/components/mod.rs && echo "components/mod.rs: pub use OK"
grep -q 'pub fn Hero' src/components/hero.rs && echo "hero.rs: pub fn Hero OK"

# Compile check
cargo build --features web && echo "PASS: web build clean"
```
</verification>

<success_criteria>
- `test -f src/app.rs && test -f src/components/mod.rs && test -f src/components/hero.rs && test -f src/state.rs && test -f src/platform/mod.rs` exits 0
- `cargo build --features web` exits 0
- `src/main.rs` contains `mod app`, `mod components`, `mod state`, `mod platform`, and `fn main()` — no component definitions
</success_criteria>

<output>
After completion, create `/Users/twilson/code/iron_hermes_ui/.planning/phases/01-hygiene/01-02-SUMMARY.md`
</output>
