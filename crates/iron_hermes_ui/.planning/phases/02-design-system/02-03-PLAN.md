---
phase: 02-design-system
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/wordmark.svg
  - assets/ih-shield.png
  - assets/header.svg
autonomous: true
requirements:
  - DS-04

must_haves:
  truths:
    - "assets/wordmark.svg exists and is byte-identical to the prototype source"
    - "assets/ih-shield.png exists and is byte-identical to the prototype source"
    - "assets/header.svg has been deleted (scaffold no longer ships)"
  artifacts:
    - path: "assets/wordmark.svg"
      provides: "IronHermes wordmark SVG (replaces scaffold header.svg in Hero stub)"
    - path: "assets/ih-shield.png"
      provides: "IronHermes shield PNG (paired with wordmark in Hero stub)"
  key_links:
    - from: "src/components/hero.rs"
      to: "assets/wordmark.svg, assets/ih-shield.png"
      via: "asset!() at module top + img { src: ASSET } in rsx! (wired in 02-04, not this plan)"
      pattern: "asset!\\(\"/assets/(wordmark\\.svg|ih-shield\\.png)\""
---

<objective>
Land the IronHermes brand assets and remove the Dioxus scaffold image. Byte-copy `wordmark.svg` and `ih-shield.png` from `warp2ironhermes/project/ironhermes/assets/` into `assets/`, then delete `assets/header.svg`. Implements the file half of DS-04 (the Rust-side `asset!()` declarations and `<img>` wiring + `main.css` rewrite live in 02-04). Wave 1 — runs in parallel with 02-01 and 02-02; no Rust source edits.

Purpose: Make the brand-correct images available to the asset pipeline before Wave 2 wires them. Removing `header.svg` here (rather than deferring to 02-04) makes the deletion atomic with its replacement and avoids a transient state where both old and new image files coexist.

**Important:** Plan 02-04 will delete the `HEADER_SVG` Rust constant from `src/components/hero.rs` BEFORE the build runs against this deleted file. Wave 1 finishes (all three plans land) before Wave 2 begins, so the build is never invoked between this plan's `rm` and 02-04's Rust edit.

Output: `assets/wordmark.svg` (new), `assets/ih-shield.png` (new), `assets/header.svg` (deleted).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system/02-CONTEXT.md
@.planning/phases/02-design-system/02-RESEARCH.md
@.planning/phases/02-design-system/02-PATTERNS.md
@.planning/phases/02-design-system/02-VALIDATION.md
</context>

<deviations>
None.
</deviations>

<deferred>
- **`assets/scanner.svg`** is intentionally NOT copied. Per CONTEXT.md `<deferred>` and RESEARCH.md Open Question 1, scanner.svg belongs to Phase 3 (status-bar scanner implementation). Despite being co-located with the brand assets in `warp2ironhermes/project/ironhermes/assets/`, it is excluded from this plan to keep Phase 2 scope tight to the four success criteria.
- **Scaffold `assets/main.css` rewrite** lives in 02-04. This plan deletes only `assets/header.svg`; `assets/main.css` rewrite is paired with the Rust edits that consume the new tokens.
</deferred>

<tasks>

<task type="auto">
  <name>Task 1: Copy wordmark.svg and ih-shield.png; delete header.svg</name>
  <files>assets/wordmark.svg, assets/ih-shield.png, assets/header.svg</files>
  <read_first>
    - warp2ironhermes/project/ironhermes/assets/wordmark.svg (source)
    - warp2ironhermes/project/ironhermes/assets/ih-shield.png (source)
    - assets/header.svg (the scaffold file being deleted — confirm it exists before rm)
    - .planning/phases/02-design-system/02-CONTEXT.md (D-03 — Hero stub renders these two; canonical_refs lists scanner.svg as deferred)
    - .planning/phases/02-design-system/02-PATTERNS.md (`assets/wordmark.svg` and `assets/ih-shield.png` sections — single-binary-copy pattern; `assets/header.svg` deletion section)
    - .planning/phases/02-design-system/02-RESEARCH.md (Pattern 3 — `<img>` rendering of SVG and PNG via `asset!()`)
  </read_first>
  <action>
Run from project root, in this order:

```bash
cp warp2ironhermes/project/ironhermes/assets/wordmark.svg  assets/wordmark.svg
cp warp2ironhermes/project/ironhermes/assets/ih-shield.png assets/ih-shield.png
rm assets/header.svg
```

**Do NOT** copy `scanner.svg`. It is deferred to Phase 3 per CONTEXT.md `<deferred>`.

**Do NOT** copy any other file from `warp2ironhermes/project/ironhermes/assets/`. The full directory listing shows three files (`ih-shield.png`, `scanner.svg`, `wordmark.svg`) — only the first and third are in scope.

**Do NOT** rename or modify the files. The Rust `asset!()` constants in 02-04 will reference the exact lowercase names `wordmark.svg` and `ih-shield.png` (matching `WORDMARK_SVG` / `IH_SHIELD_PNG` constant names per PATTERNS.md `hero.rs` after-state).

**Do NOT** edit `src/components/hero.rs` in this plan. The `HEADER_SVG` constant there will reference a deleted file after this plan lands — that's fine, because 02-04 (Wave 2) will rewrite `hero.rs` BEFORE any subsequent `cargo build` is invoked. Wave 1 plans never invoke `cargo build` between each other; the next build runs as part of 02-04.

If `cargo build` IS run between Wave 1 and Wave 2 (e.g., for unrelated debugging), it will fail with `path must point to a file: /assets/header.svg` — this is expected and intentional, and is fixed the moment 02-04 runs. Do NOT pre-emptively edit `hero.rs` to "fix" this — it is the correct sequencing.
  </action>
  <verify>
    <automated>test -f assets/wordmark.svg && test -f assets/ih-shield.png && ! test -f assets/header.svg</automated>
  </verify>
  <acceptance_criteria>
    - `test -f assets/wordmark.svg` exits 0
    - `test -f assets/ih-shield.png` exits 0
    - `! test -f assets/header.svg` exits 0 (i.e., `assets/header.svg` no longer exists)
    - `cmp assets/wordmark.svg warp2ironhermes/project/ironhermes/assets/wordmark.svg` exits 0 (byte identity)
    - `cmp assets/ih-shield.png warp2ironhermes/project/ironhermes/assets/ih-shield.png` exits 0 (byte identity)
    - SVG file is non-empty: `test -s assets/wordmark.svg` exits 0
    - PNG file is non-empty: `test -s assets/ih-shield.png` exits 0
    - PNG magic bytes correct: `head -c 8 assets/ih-shield.png | od -An -tx1 | tr -d ' '` outputs `89504e470d0a1a0a`
    - SVG starts with the XML/SVG marker: `head -c 5 assets/wordmark.svg | grep -E -q '^(<\?xml|<svg)'` exits 0
    - Source files unchanged: `cmp warp2ironhermes/project/ironhermes/assets/wordmark.svg warp2ironhermes/project/ironhermes/assets/wordmark.svg` is trivially true; verify the source still exists with `test -f warp2ironhermes/project/ironhermes/assets/wordmark.svg && test -f warp2ironhermes/project/ironhermes/assets/ih-shield.png` exit 0
    - `assets/scanner.svg` was NOT copied (deferred): `! test -f assets/scanner.svg` exits 0
    - `git status` shows three changes: `assets/wordmark.svg` (new), `assets/ih-shield.png` (new), `assets/header.svg` (deleted)
  </acceptance_criteria>
  <done>`assets/wordmark.svg` and `assets/ih-shield.png` exist and are byte-identical to their prototype sources; `assets/header.svg` has been deleted; `assets/scanner.svg` is NOT present; `warp2ironhermes/` is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Same-origin static image fetch | Browser fetches `/assets/wordmark.svg` and `/assets/ih-shield.png` from the same origin. No third-party image hosting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T (Tampering) | Vendored brand images | accept | Same-origin only; no remote image links. Per RESEARCH.md "Security Domain" — ASVS V2-V6 do not apply. |
| T-02-05 | I (Information disclosure via SVG `<script>`) | `assets/wordmark.svg` | accept | Image is rendered via `<img src=...>` (Pattern 3), NOT inline `dangerouslySetInnerHTML`-equivalent. The browser renders SVGs from `<img>` in a sandboxed mode where `<script>` tags are inert. The SVG is from the trusted prototype handoff. |

No additional mitigations needed. The Hero in 02-04 uses `<img src: WORDMARK_SVG />` (sandboxed rendering), not inline-SVG injection.
</threat_model>

<verification>
**Smoke checks (automated):**
```bash
test -f assets/wordmark.svg                                                              # expect: exit 0
test -f assets/ih-shield.png                                                             # expect: exit 0
! test -f assets/header.svg                                                              # expect: exit 0
cmp assets/wordmark.svg warp2ironhermes/project/ironhermes/assets/wordmark.svg          # expect: exit 0
cmp assets/ih-shield.png warp2ironhermes/project/ironhermes/assets/ih-shield.png        # expect: exit 0
! test -f assets/scanner.svg                                                              # expect: exit 0 (deferred to Phase 3)
```

**Compile check (intentionally NOT run in this plan):** Running `cargo build` between this plan and 02-04 will fail because `src/components/hero.rs` still references the now-deleted `/assets/header.svg`. This is the intended Wave 1→Wave 2 sequencing. The compile gate runs only inside 02-04 after `hero.rs` is rewritten.
</verification>

<success_criteria>
- `assets/wordmark.svg` and `assets/ih-shield.png` exist and are byte-identical to their `warp2ironhermes/` sources
- `assets/header.svg` has been deleted
- `assets/scanner.svg` is NOT present (deferred to Phase 3)
- `warp2ironhermes/` source tree is untouched
- Git diff shows two file additions and one file deletion (no other changes from this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system/02-03-SUMMARY.md` recording:
- `cmp` exit codes for both byte-identity checks (must be 0 / 0)
- Confirmation that `assets/header.svg` is gone
- Confirmation that `assets/scanner.svg` is NOT present (deferred to Phase 3)
- Note that DS-04 file half is complete; the Rust-side wiring + `main.css` rewrite live in 02-04
- Note for the orchestrator: `cargo build` is intentionally NOT run after this plan (would fail until 02-04 lands the `hero.rs` rewrite)
</output>
