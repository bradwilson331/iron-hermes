---
phase: 02-design-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/warp-ih.css
autonomous: true
requirements:
  - DS-03

must_haves:
  truths:
    - "assets/warp-ih.css is byte-identical to warp2ironhermes/project/styles/warp-ih.css modulo a 4-line attribution header"
    - "warp-ih.css contains at least 50 .wh-* selector declarations"
    - "warp-ih.css contains [data-theme=...], [data-density=...], [data-block=...], [data-agent=...] override blocks (carried forward for Phase 5)"
  artifacts:
    - path: "assets/warp-ih.css"
      provides: "Warp shell layout — .wh-app, .wh-titlebar, .wh-block family, status bar, scanner cells, palette overlay, density/theme/block data-attribute overrides"
      min_lines: 510
      contains: ".wh-app"
  key_links:
    - from: "assets/warp-ih.css"
      to: "assets/design-tokens.css"
      via: "var(--accent-primary), var(--brand), var(--font-mono), var(--w-bg-*) consumption — last-wins cascade"
      pattern: "var\\(--"
---

<objective>
Mechanical verbatim port of the Warp shell layout CSS (Pattern 1 from RESEARCH.md). Byte-copy `warp2ironhermes/project/styles/warp-ih.css` to `assets/warp-ih.css` with a 4-line attribution header prepended. Implements DS-03 in full. Wave 1 — runs in parallel with 02-01 and 02-03; touches a single file (`assets/warp-ih.css`) and modifies no Rust source.

Purpose: Land the Warp shell layout layer (`.wh-app`, `.wh-titlebar`, `.wh-block` family, status bar, scanner cell rules, palette overlay, density/theme/block data-attribute overrides) byte-identical to the prototype source-of-truth so Phase 3 can render against it without any CSS authoring. The data-attribute overrides (`[data-theme="cyan"]` etc.) ride along even though runtime switching is Phase 5 — pure CSS, no runtime cost.

Output: `assets/warp-ih.css` (~511 lines = 4 attribution + 507 source). No source code changes. No build wiring (deferred to 02-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system/02-CONTEXT.md
@.planning/phases/02-design-system/02-RESEARCH.md
@.planning/phases/02-design-system/02-PATTERNS.md
@.planning/phases/02-design-system/02-VALIDATION.md
@warp2ironhermes/project/styles/warp-ih.css
</context>

<deviations>
None for this plan. Cascade ordering against Tailwind preflight is handled in 02-04. This plan only writes a static asset file.
</deviations>

<deferred>
Nothing deferred from this plan's scope. Runtime data-attribute switching (THEME-01..05) lives in Phase 5 — but the CSS rules backing it are ported now (per CONTEXT.md `<decisions>` Discretion item).
</deferred>

<tasks>

<task type="auto">
  <name>Task 1: Verbatim port warp-ih.css → assets/warp-ih.css with 4-line attribution header</name>
  <files>assets/warp-ih.css</files>
  <read_first>
    - warp2ironhermes/project/styles/warp-ih.css (the verbatim source — 507 lines, UTF-8, LF, no BOM)
    - .planning/phases/02-design-system/02-RESEARCH.md (Pattern 1 — exact bash recipe; Pitfall 3 — encoding drift; DS-03 row in Validation Architecture)
    - .planning/phases/02-design-system/02-PATTERNS.md (`assets/warp-ih.css` section — full bash recipe with verification command)
    - .planning/phases/02-design-system/02-CONTEXT.md (D-02 — no @import; Discretion — theme data-attribute overrides ported now)
  </read_first>
  <action>
Run the EXACT bash recipe from RESEARCH.md Pattern 1 (preserves bytes, line endings, encoding) from project root:

```bash
{
  printf '/* Source: %s\n * Copied: %s (Phase 2)\n * DO NOT EDIT — re-sync from source if upstream changes.\n */\n' \
    "warp2ironhermes/project/styles/warp-ih.css" "$(date -u +%F)"
  cat warp2ironhermes/project/styles/warp-ih.css
} > assets/warp-ih.css
```

**Required result:** the first 4 lines of `assets/warp-ih.css` are exactly:

```
/* Source: warp2ironhermes/project/styles/warp-ih.css
 * Copied: 2026-05-02 (Phase 2)
 * DO NOT EDIT — re-sync from source if upstream changes.
 */
```

(Date string is whatever `$(date -u +%F)` resolves to at execution time; acceptance criteria match a permissive `YYYY-MM-DD` regex.)

Lines 5..end are the verbatim 507-line `warp-ih.css` content — including its `:root { --w-bg-0..4 ... }` Warp surface scale block, all `.wh-app`, `.wh-titlebar`, `.wh-block` family rules, status-bar pill rules, scanner cell rules, palette overlay rules, and the `[data-density="compact"]`, `[data-theme="cyan"|"magenta"|"green"|"amber"]`, `[data-block="framed"|"flat"|"minimal"]`, `[data-agent="right"|"bottom"|"hidden"]` override blocks.

**Do NOT** edit any byte inside the source content. **Do NOT** strip the data-attribute override blocks even though Phase 5 wires the runtime switches — they are pure CSS and ride along (per CONTEXT.md Discretion). **Do NOT** merge with `design-tokens.css` (D-02). **Do NOT** add `@import` directives (the source has none and adding them would violate D-02).

If the file already exists from an earlier attempt, the redirect (`>`) overwrites it cleanly.
  </action>
  <verify>
    <automated>tail -n +5 assets/warp-ih.css | cmp - warp2ironhermes/project/styles/warp-ih.css && grep -c '\.wh-' assets/warp-ih.css</automated>
  </verify>
  <acceptance_criteria>
    - `test -f assets/warp-ih.css` exits 0
    - Byte-identity past the header: `tail -n +5 assets/warp-ih.css | cmp - warp2ironhermes/project/styles/warp-ih.css` exits 0
    - At least 50 `.wh-*` selector occurrences: `grep -c '\.wh-' assets/warp-ih.css` outputs an integer ≥ 50 (RESEARCH.md DS-03 row notes ~57 in source)
    - The canonical root selector exists: `grep -c '\.wh-app' assets/warp-ih.css` outputs an integer ≥ 1
    - All four data-attribute override families are present:
      - `grep -c '\[data-theme=' assets/warp-ih.css` outputs an integer ≥ 1
      - `grep -c '\[data-density=' assets/warp-ih.css` outputs an integer ≥ 1
      - `grep -c '\[data-block=' assets/warp-ih.css` outputs an integer ≥ 1
      - `grep -c '\[data-agent=' assets/warp-ih.css` outputs an integer ≥ 1
    - Total line count ≥ 510: `wc -l < assets/warp-ih.css` outputs an integer ≥ 510 (4 + 507 = 511; off-by-one tolerated)
    - Attribution header line 1 is correct: `sed -n '1p' assets/warp-ih.css` outputs exactly `/* Source: warp2ironhermes/project/styles/warp-ih.css`
    - Attribution header line 2 matches: `sed -n '2p' assets/warp-ih.css` matches regex ` \* Copied: [0-9]{4}-[0-9]{2}-[0-9]{2} \(Phase 2\)`
    - Attribution header line 3: `sed -n '3p' assets/warp-ih.css` outputs exactly ` * DO NOT EDIT — re-sync from source if upstream changes.`
    - Attribution header line 4: `sed -n '4p' assets/warp-ih.css` outputs exactly ` */`
    - No `@import` directives introduced: `grep -c '^@import' assets/warp-ih.css` outputs `0`
    - No `url(...)` references introduced (RESEARCH.md verified source has zero): `grep -c 'url(' assets/warp-ih.css` outputs `0`
    - No UTF-8 BOM: `head -c 3 assets/warp-ih.css | od -An -tx1 | tr -d ' '` does NOT output `efbbbf`
    - `warp2ironhermes/project/styles/warp-ih.css` is unchanged: `wc -l < warp2ironhermes/project/styles/warp-ih.css` still outputs `507`
  </acceptance_criteria>
  <done>`assets/warp-ih.css` exists with the 4-line attribution header followed by the 507-line verbatim source; ≥ 50 `.wh-*` rules present; all four data-attribute override families present; byte-identity confirmed via `cmp`; source untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Same-origin static stylesheet fetch | Browser fetches `/assets/warp-ih.css` from the same origin as the app HTML. No third-party stylesheet hosting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | T (Tampering) | Vendored layout CSS | accept | Same-origin only; no remote stylesheet links. Per RESEARCH.md "Security Domain" — ASVS V2-V6 do not apply to a static-asset-only phase. Repository review covers supply-chain. |

No mitigations beyond standard browser CSS sandboxing required. No selectors target `iframe`, `script`, or any executable boundary; pure layout/typography rules.
</threat_model>

<verification>
**Smoke checks (automated):**
```bash
test -f assets/warp-ih.css                                                                # expect: exit 0
tail -n +5 assets/warp-ih.css | cmp - warp2ironhermes/project/styles/warp-ih.css         # expect: exit 0
grep -c '\.wh-' assets/warp-ih.css                                                        # expect: ≥ 50
grep -c '\.wh-app' assets/warp-ih.css                                                     # expect: ≥ 1
grep -c '\[data-theme=' assets/warp-ih.css                                                # expect: ≥ 1
grep -c '\[data-density=' assets/warp-ih.css                                              # expect: ≥ 1
grep -c '\[data-block=' assets/warp-ih.css                                                # expect: ≥ 1
grep -c '\[data-agent=' assets/warp-ih.css                                                # expect: ≥ 1
grep -c '^@import' assets/warp-ih.css                                                     # expect: 0
grep -c 'url(' assets/warp-ih.css                                                         # expect: 0
```

**Compile check:**
```bash
cargo build --features web
```
Baseline must remain green; this plan adds a static asset only and does not touch Rust source.
</verification>

<success_criteria>
- `assets/warp-ih.css` exists and is `cmp`-identical to `warp2ironhermes/project/styles/warp-ih.css` past the 4-line attribution header
- ≥ 50 `.wh-*` selector occurrences present (DS-03 expectation per RESEARCH.md)
- All four data-attribute override families (`theme`, `density`, `block`, `agent`) present in the file
- No `@import` and no `url(...)` references introduced
- `cargo build --features web` still succeeds
- `warp2ironhermes/` source tree is untouched
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system/02-02-SUMMARY.md` recording:
- `cmp` exit code (must be 0) for byte-identity past line 4
- `.wh-*` selector count
- Counts for each of the four data-attribute override families
- Total line count
- The exact UTC date that landed in the attribution header line 2
- Note that DS-03 is file-complete; runtime verification (`.wh-app` selector reachable from `document.styleSheets`) lives in 02-04's manual UAT
</output>
