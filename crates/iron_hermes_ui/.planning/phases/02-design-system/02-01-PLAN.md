---
phase: 02-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/fonts/IoskeleyMono-Black.woff2
  - assets/fonts/IoskeleyMono-Bold.woff2
  - assets/fonts/IoskeleyMono-BoldItalic.woff2
  - assets/fonts/IoskeleyMono-Condensed.woff2
  - assets/fonts/IoskeleyMono-CondensedBold.woff2
  - assets/fonts/IoskeleyMono-CondensedMedium.woff2
  - assets/fonts/IoskeleyMono-ExtraBold.woff2
  - assets/fonts/IoskeleyMono-ExtraLight.woff2
  - assets/fonts/IoskeleyMono-Italic.woff2
  - assets/fonts/IoskeleyMono-Light.woff2
  - assets/fonts/IoskeleyMono-Medium.woff2
  - assets/fonts/IoskeleyMono-Regular.woff2
  - assets/fonts/IoskeleyMono-SemiBold.woff2
  - assets/fonts/IoskeleyMono-SemiCondensed.woff2
  - assets/fonts/IoskeleyMono-SemiLight.woff2
  - assets/fonts/IoskeleyMono-Thin.woff2
  - assets/design-tokens.css
autonomous: true
requirements:
  - DS-01
  - DS-02

must_haves:
  truths:
    - "All 16 IoskeleyMono .woff2 files exist under assets/fonts/"
    - "assets/design-tokens.css is byte-identical to colors_and_type.css modulo a 4-line attribution header"
    - "design-tokens.css contains exactly 16 @font-face declarations"
    - "Font URLs in design-tokens.css remain in their relative form url(\"fonts/IoskeleyMono-*.woff2\") (no asset!() wrapping per D-06)"
  artifacts:
    - path: "assets/fonts/"
      provides: "16 woff2 files served as static assets at /assets/fonts/*.woff2"
    - path: "assets/design-tokens.css"
      provides: "ANSI palette CSS variables, type scale, brand color, scanner constants, 16 @font-face blocks"
      min_lines: 245
      contains: "@font-face"
  key_links:
    - from: "assets/design-tokens.css"
      to: "assets/fonts/IoskeleyMono-*.woff2"
      via: "CSS @font-face src: url(\"fonts/...\") relative resolution"
      pattern: "url\\(\"fonts/IoskeleyMono-"
---

<objective>
Mechanical verbatim port of the IronHermes design tokens (Pattern 1 from RESEARCH.md). Copy 16 Ioskeley Mono `.woff2` files into `assets/fonts/`, then byte-copy `warp2ironhermes/project/ironhermes/colors_and_type.css` to `assets/design-tokens.css` with a 4-line attribution header prepended. Implements DS-01 (file half) and DS-02 in full. Wave 1 — runs in parallel with 02-02 and 02-03; no Rust source edits, no other plan touches these files.

Purpose: Land the design-token CSS layer (ANSI palette, font stacks, radii, brand color, 16 @font-face declarations) byte-identical to the prototype source-of-truth. Future re-syncs are a single `cat src > dst` re-run, not a diff-merge (per D-02).

Output: `assets/fonts/` directory with 16 `.woff2` files; `assets/design-tokens.css` (~245 lines = 4 attribution + 241 source). No source code changes. No build wiring (deferred to 02-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system/02-CONTEXT.md
@.planning/phases/02-design-system/02-RESEARCH.md
@.planning/phases/02-design-system/02-PATTERNS.md
@.planning/phases/02-design-system/02-VALIDATION.md
@warp2ironhermes/project/ironhermes/colors_and_type.css
</context>

<deviations>
None for this plan. The cascade-order deviation from CONTEXT.md D-01 (Tailwind first vs. last) is handled in plan 02-04 where the `<link>` tags are emitted in `src/app.rs`. This plan only writes static asset files; cascade ordering is not yet visible.
</deviations>

<deferred>
- **`assets/scanner.svg`** is intentionally NOT copied here. It belongs to Phase 3 (status-bar scanner implementation) per CONTEXT.md `<deferred>` and RESEARCH.md Open Question 1. Plan 02-03 also explicitly excludes it.
</deferred>

<tasks>

<task type="auto">
  <name>Task 1: Copy 16 Ioskeley Mono woff2 files into assets/fonts/</name>
  <files>
    assets/fonts/IoskeleyMono-Black.woff2,
    assets/fonts/IoskeleyMono-Bold.woff2,
    assets/fonts/IoskeleyMono-BoldItalic.woff2,
    assets/fonts/IoskeleyMono-Condensed.woff2,
    assets/fonts/IoskeleyMono-CondensedBold.woff2,
    assets/fonts/IoskeleyMono-CondensedMedium.woff2,
    assets/fonts/IoskeleyMono-ExtraBold.woff2,
    assets/fonts/IoskeleyMono-ExtraLight.woff2,
    assets/fonts/IoskeleyMono-Italic.woff2,
    assets/fonts/IoskeleyMono-Light.woff2,
    assets/fonts/IoskeleyMono-Medium.woff2,
    assets/fonts/IoskeleyMono-Regular.woff2,
    assets/fonts/IoskeleyMono-SemiBold.woff2,
    assets/fonts/IoskeleyMono-SemiCondensed.woff2,
    assets/fonts/IoskeleyMono-SemiLight.woff2,
    assets/fonts/IoskeleyMono-Thin.woff2
  </files>
  <read_first>
    - .planning/phases/02-design-system/02-CONTEXT.md (D-05, D-06 — all 16 fonts copied; relative URLs preserved)
    - .planning/phases/02-design-system/02-PATTERNS.md (`assets/fonts/` section — bulk copy + asset-pipeline note re: Assumption A2)
    - .planning/phases/02-design-system/02-RESEARCH.md (Pitfall 2 — relative-URL anchoring; Assumption A2 — un-declared static-file serving)
    - List source dir: `ls -1 warp2ironhermes/project/ironhermes/fonts/IoskeleyMono-*.woff2`
  </read_first>
  <action>
Run from project root:

```bash
mkdir -p assets/fonts
cp warp2ironhermes/project/ironhermes/fonts/IoskeleyMono-*.woff2 assets/fonts/
```

The 16 expected files (verified in CONTEXT.md `<canonical_refs>` and RESEARCH.md):
Black, Bold, BoldItalic, Condensed, CondensedBold, CondensedMedium, ExtraBold, ExtraLight, Italic, Light, Medium, Regular, SemiBold, SemiCondensed, SemiLight, Thin (each prefixed `IoskeleyMono-` and suffixed `.woff2`).

**Do NOT** wrap any of these files in `asset!()` from Rust. They are served as un-declared static files at `/assets/fonts/...` so that the verbatim `url("fonts/IoskeleyMono-*.woff2")` references inside `design-tokens.css` resolve correctly. This honors D-06 and avoids breaking the verbatim-port invariant from D-02.

**Do NOT** copy any other file from `warp2ironhermes/project/ironhermes/fonts/` (no `.ttf`, no `.otf`, no `licence.txt` — none exist there in any case; the glob is woff2-only).

**Do NOT** delete `warp2ironhermes/` files. That tree is read-only per PROJECT.md "Constraints".
  </action>
  <verify>
    <automated>ls -1 assets/fonts/IoskeleyMono-*.woff2 | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `ls -1 assets/fonts/IoskeleyMono-*.woff2 | wc -l` outputs `16`
    - `test -f assets/fonts/IoskeleyMono-Regular.woff2` exits 0
    - `test -f assets/fonts/IoskeleyMono-Bold.woff2` exits 0
    - `test -f assets/fonts/IoskeleyMono-Thin.woff2` exits 0
    - `test -f assets/fonts/IoskeleyMono-CondensedMedium.woff2` exits 0
    - `cmp assets/fonts/IoskeleyMono-Regular.woff2 warp2ironhermes/project/ironhermes/fonts/IoskeleyMono-Regular.woff2` exits 0 (byte identity)
    - No non-woff2 files were created in `assets/fonts/`: `ls assets/fonts/ | grep -vc '\.woff2$'` outputs `0`
    - `warp2ironhermes/` is unchanged: `ls warp2ironhermes/project/ironhermes/fonts/IoskeleyMono-*.woff2 | wc -l` still outputs `16`
  </acceptance_criteria>
  <done>16 woff2 files exist in `assets/fonts/`, byte-identical to the prototype sources; `warp2ironhermes/` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Verbatim port colors_and_type.css → assets/design-tokens.css with 4-line attribution header</name>
  <files>assets/design-tokens.css</files>
  <read_first>
    - warp2ironhermes/project/ironhermes/colors_and_type.css (the verbatim source — 241 lines, UTF-8, LF, no BOM)
    - .planning/phases/02-design-system/02-RESEARCH.md (Pattern 1 — exact bash recipe; Pitfall 3 — encoding drift)
    - .planning/phases/02-design-system/02-PATTERNS.md (`assets/design-tokens.css` section — full bash recipe with verification command)
    - .planning/phases/02-design-system/02-CONTEXT.md (D-02 — no @import; D-05 — all 16 @font-face preserved; D-06 — relative font URLs)
  </read_first>
  <action>
Run the EXACT bash recipe from RESEARCH.md Pattern 1 (preserves bytes, line endings, encoding) from project root:

```bash
{
  printf '/* Source: %s\n * Copied: %s (Phase 2)\n * DO NOT EDIT — re-sync from source if upstream changes.\n */\n' \
    "warp2ironhermes/project/ironhermes/colors_and_type.css" "$(date -u +%F)"
  cat warp2ironhermes/project/ironhermes/colors_and_type.css
} > assets/design-tokens.css
```

**Required result:** the first 4 lines of `assets/design-tokens.css` are exactly:

```
/* Source: warp2ironhermes/project/ironhermes/colors_and_type.css
 * Copied: 2026-05-02 (Phase 2)
 * DO NOT EDIT — re-sync from source if upstream changes.
 */
```

(The date string `2026-05-02` is what `$(date -u +%F)` resolves to in the planner's session — if the executor runs this on a different UTC day, accept whatever today's UTC date is in `YYYY-MM-DD` form. The acceptance criteria below match the exact-day grep PLUS a permissive YYYY-MM-DD regex fallback.)

Lines 5..end (i.e., 241 more lines) are the verbatim contents of `colors_and_type.css` — including its leading `/* IronHermes Design System — colors & type */` comment block, all 16 `@font-face { ... }` declarations, the `:root { ... }` ANSI palette block, all `html, body { ... }` base rules, and all utility classes.

**Do NOT** edit any byte inside the source content. **Do NOT** rewrite font URLs to `asset!()`. **Do NOT** strip or merge `@font-face` blocks. **Do NOT** add a final newline beyond what `cat` already produces. **Do NOT** use a tool that auto-adds a UTF-8 BOM (e.g., do not edit through editors that default-write BOM on save).

If the file already exists from an earlier attempt, the redirect (`>`) overwrites it cleanly. Do NOT `rm` first; the redirect is sufficient.
  </action>
  <verify>
    <automated>tail -n +5 assets/design-tokens.css | cmp - warp2ironhermes/project/ironhermes/colors_and_type.css && grep -c '@font-face' assets/design-tokens.css</automated>
  </verify>
  <acceptance_criteria>
    - `test -f assets/design-tokens.css` exits 0
    - Byte-identity past the header: `tail -n +5 assets/design-tokens.css | cmp - warp2ironhermes/project/ironhermes/colors_and_type.css` exits 0
    - Exactly 16 `@font-face` declarations: `grep -c '@font-face' assets/design-tokens.css` outputs `16`
    - Total line count ≥ 245: `wc -l < assets/design-tokens.css` outputs an integer ≥ 245 (4 header + 241 source = 245; equality if source ends with a final newline, off-by-one tolerated)
    - Attribution header line 1 is correct: `sed -n '1p' assets/design-tokens.css` outputs exactly `/* Source: warp2ironhermes/project/ironhermes/colors_and_type.css`
    - Attribution header line 2 matches date pattern: `sed -n '2p' assets/design-tokens.css` matches regex ` \* Copied: [0-9]{4}-[0-9]{2}-[0-9]{2} \(Phase 2\)`
    - Attribution header line 3: `sed -n '3p' assets/design-tokens.css` outputs exactly ` * DO NOT EDIT — re-sync from source if upstream changes.`
    - Attribution header line 4: `sed -n '4p' assets/design-tokens.css` outputs exactly ` */`
    - No `@import` directives introduced: `grep -c '^@import' assets/design-tokens.css` outputs `0`
    - Font URLs remain relative (no asset!() wrapping): `grep -c 'url("fonts/IoskeleyMono-' assets/design-tokens.css` outputs `16`
    - No UTF-8 BOM on the file: `head -c 3 assets/design-tokens.css | od -An -tx1 | tr -d ' '` does NOT output `efbbbf`
    - `warp2ironhermes/project/ironhermes/colors_and_type.css` is unchanged: `wc -l < warp2ironhermes/project/ironhermes/colors_and_type.css` still outputs `241`
  </acceptance_criteria>
  <done>`assets/design-tokens.css` exists with the 4-line attribution header followed by the 241-line verbatim source; 16 `@font-face` blocks present; byte-identity confirmed via `cmp`; source file untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Same-origin static asset fetch | Browser fetches `/assets/fonts/*.woff2` and `/assets/design-tokens.css` from the same origin as the app HTML. No third-party CDN. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T (Tampering) | Vendored woff2 files in `assets/fonts/` | accept | Files vendored from prototype handoff; same-origin only. No third-party font CDN reduces tampering risk to repository-supply-chain (covered by Cargo/git review). Per RESEARCH.md "Security Domain" — ASVS V2-V6 do not apply to a static-asset-only phase. |
| T-02-02 | I (Information disclosure) | `design-tokens.css` and font files | accept | Public design tokens; no secrets. CSS is intentionally world-readable in any deployed web app. |

No mitigations beyond standard browser font-loader sandboxing are required. No new auth, no input handling, no network egress, no secrets.
</threat_model>

<verification>
**Smoke checks (automated, run after both tasks):**
```bash
ls -1 assets/fonts/IoskeleyMono-*.woff2 | wc -l   # expect: 16
grep -c '@font-face' assets/design-tokens.css     # expect: 16
tail -n +5 assets/design-tokens.css | cmp - warp2ironhermes/project/ironhermes/colors_and_type.css   # expect: exit 0
grep -c '^@import' assets/design-tokens.css       # expect: 0
grep -c 'url("fonts/IoskeleyMono-' assets/design-tokens.css   # expect: 16
```

**Compile check (no Rust source edits in this plan, but the file system change must not break the existing build):**
```bash
cargo build --features web
```
This must still succeed; new files are not yet referenced by any `asset!()` call (that wiring lives in 02-04), so the existing 4 `Asset` constants and 3 `document::Link` calls are unaffected.
</verification>

<success_criteria>
- All 16 woff2 files exist under `assets/fonts/` and are byte-identical to their `warp2ironhermes/` counterparts
- `assets/design-tokens.css` is `cmp`-identical to `colors_and_type.css` past the 4-line attribution header
- `assets/design-tokens.css` contains exactly 16 `@font-face` declarations
- No `@import` chains introduced (D-02 honored)
- `cargo build --features web` still succeeds (no Rust changes; baseline preserved)
- `warp2ironhermes/` source tree is untouched
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system/02-01-SUMMARY.md` recording:
- Confirmed 16 woff2 file count and one byte-identity sample (`IoskeleyMono-Regular.woff2 cmp` result)
- Confirmed `design-tokens.css` byte identity past line 4 (`cmp` exit code)
- Confirmed 16 `@font-face` declaration count
- Total line count of `design-tokens.css`
- The exact UTC date that landed in the attribution header line 2
- Note that DS-01 (file half) and DS-02 are complete at the file level; runtime verification (font actually rendering) lives in 02-04's manual UAT
</output>
