---
phase: 03-desktop-shell
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified: []
autonomous: false
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-04, SHELL-05, SHELL-06, SHELL-07, SHELL-08, SHELL-09, SHELL-10]
must_haves:
  truths:
    - "CONTEXT.md decisions verified through manual UAT: D-01 (component decomposition mirrors shell.jsx 1:1 — all primitives present in rendered shell), D-02 (block_stream container renders the scrollback list), D-03 (hero stub gone — only WarpHermes renders), D-04 (state.rs types render correctly through demo fixtures), D-05 (asset constants resolved — wordmark/shield/scanner-anim load), D-06 (pure-static — no console errors from missing handlers; visuals only), D-07 (plain owned props correctly populate UI), D-08 (scanner @keyframes animates continuously), D-09 (input textarea renders with prototype CSS), D-10 (all 6 Block enum variants render with correct stripes), D-11 (Block dispatcher composes CommandLine and ToolCall correctly), D-12 (CommandLine bin/arg/flag tokens render in distinct prototype colors), D-13 (4 ToolCall status states render distinctly — Pending/Running/Done/Failed), D-14 (BlockStream renders Vec<Block> in scrollable container), D-15 (hover affordances reveal via CSS :hover only), D-16 (is-ai content renders as plain text per Approach A), D-17 (demo_blocks() seeds the visible scrollback), D-18 (~8–10 fixture blocks visible across one screen scroll), D-19 (palette overlay open by default — visible without keyboard input), D-20 (title-bar tabs and status-bar pills match app.jsx prototype defaults verbatim)"
    - "User has run `dx serve --features web` and the dev server reports OK"
    - "User has opened both `localhost:8080` (the Dioxus app) and `warp2ironhermes/project/Warp × IronHermes.html` (the prototype) in side-by-side browser tabs"
    - "User has verified all 10 SHELL-XX requirements pass via the per-criterion checklist below"
    - "User has typed `approved` in chat OR has reported specific failures"
  artifacts: []
  key_links: []
---

<objective>
Manual UAT checkpoint for Phase 3. Plan 03-04 landed the green three-platform compile gate; this plan verifies the rendered shell visually matches the prototype across all 10 SHELL-XX requirements via a side-by-side browser comparison. Per VALIDATION.md "Manual UAT" row and project STATE.md todo ("After Phase 3: side-by-side visual review of desktop shell against `warp2ironhermes/project/Warp × IronHermes.html`"), this is the canonical Phase 3 user-facing acceptance gate.

Purpose: Visual fidelity is the project's primary failure mode (PROJECT.md core value). Headless visual regression testing is Out of Scope for v1, so manual UAT is the only verification method that can detect pixel drift.

Output: User feedback (`approved` or list of failures). On `approved`, append a "Manual UAT Outcome" block to `.planning/phases/03-desktop-shell/03-05-SUMMARY.md`; next step is `/gsd-verify-work` to close Phase 3. On failure, the orchestrator routes to `/gsd-plan-phase 3 --gaps`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-desktop-shell/03-CONTEXT.md
@.planning/phases/03-desktop-shell/03-UI-SPEC.md
@.planning/phases/03-desktop-shell/03-VALIDATION.md
@.planning/phases/03-desktop-shell/03-01-SUMMARY.md
@.planning/phases/03-desktop-shell/03-02-SUMMARY.md
@.planning/phases/03-desktop-shell/03-03-SUMMARY.md
@.planning/phases/03-desktop-shell/03-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual UAT — side-by-side visual review against prototype HTML</name>
  <files>(verification-only — no source changes)</files>
  <action>
**Setup:** Run `dx serve --features web` from project root. Wait for `serving at http://localhost:8080`. Open TWO browser tabs side-by-side:

1. Tab A — Dioxus build: `http://localhost:8080`
2. Tab B — Prototype reference: open `warp2ironhermes/project/Warp × IronHermes.html` directly via the file browser

Walk the reviewer through the SHELL-01..SHELL-10 checklist below. For each criterion, perform the visual check and either tick it off mentally OR capture the deviation. At the end, the reviewer types `approved` in chat OR lists the failing criteria.

**This task is verification-only. There is no source code to write. The plan's "automation" already happened in Plans 03-01..03-04; this plan is the human gate that confirms pixel fidelity.**

What was built (recap of Plans 03-01..03-04):

- **State module (`src/state.rs`)** — Block enum (6 variants), CommandLine, Token, ToolCall, ToolStatus, Mode, PaletteItem, Tab, Message, TokenBudget; demo_blocks() (10), demo_messages() (5), demo_palette_items() (10), demo_tabs() (3)
- **Scanner CSS (`assets/scanner-anim.css`)** — `@keyframes wh-scanner-tick` + 10 staggered `:nth-child` `animation-delay` rules
- **Leaf primitives (5)** — Sigil, Scanner, CommandLine, ToolCall, StatusBar
- **Composite primitives (6)** — TitleBar, Block, BlockStream, InputBox, AgentPanel, CommandPalette
- **Composer (`src/components/warp_hermes.rs`)** — wires all 11 primitives under `.wh-app` with hardcoded `data-theme=cyan / data-density=comfy / data-block=framed / data-agent=right`
- **`src/app.rs`** — swapped `Hero {}` → `WarpHermes {}`; added scanner-anim.css link after warp-ih.css
- **Deleted `src/components/hero.rs`** — assets migrated to title_bar.rs with `#[allow(dead_code)]`

The three-platform compile gate (`cargo build --features web` / `--features desktop` / `--features mobile`) and `cargo clippy --features web -- -D warnings` are all green per Plan 03-04 Task 4.
  </action>
  <how-to-verify>
**Reviewer checklist — walk through each SHELL-XX criterion side-by-side:**

**SHELL-01 — Title bar:**
- [ ] Three macOS traffic-light dots top-left (red `#ff5f57`, amber `#febc2e`, green `#28c840`), 12×12 px each, ~8px gap
- [ ] "IH" sigil (cyan on dark, 18×18) + "IronHermes" text (cyan, weight 700, ~12px)
- [ ] 3 tabs: `ironhermes chat` (active, top stripe), `cargo watch` (live green dot), `agent · scratch` (dim dot)
- [ ] First tab shows the active stripe (top 2px accent line)
- [ ] Each tab shows its `×` close glyph in disabled-grey
- [ ] After tabs: `+` new-tab button in dim grey
- [ ] Top-right: `⌘K` shortcut label, ~11px

**SHELL-02 — Block stream + 5 stripe types:**
- [ ] At least 8 blocks visible/scrollable in the central column
- [ ] B1 (`is-cmd`, cyan stripe): `~/projects/ironhermes ❯ ironhermes doctor` with bin/arg colored differently; time `0.4s` on right
- [ ] B2 (`is-out`, dim stripe): "IronHermes Doctor" + 40-char rule + 8 doctor lines
- [ ] B3 (`is-cmd`, cyan stripe): `git diff --stat` with `--stat` in magenta (flag color)
- [ ] B4 (`is-ok`, green stripe): 4-line diff stat with `[OK]` chip in green
- [ ] B5 (`is-ai`, magenta stripe): one-paragraph AI text mentioning `concise` and `pill rotation`
- [ ] B6 (`is-err`, red stripe): `error[E0282]: type annotations needed` with `exit 1` chip in red
- [ ] Each stripe is exactly 2px wide on the left edge
- [ ] Stripe colors match the prototype side-by-side

**SHELL-03 — Hover affordances:**
- [ ] Hover any non-Cmd block: copy (⎘), rerun (↻), share (↗) buttons fade in (top-right, 24×24)
- [ ] Hover an `is-cmd` block: only copy (⎘) and rerun (↻) appear (no share, per UI-SPEC line 351)
- [ ] Move cursor away: buttons fade back to invisible

**SHELL-04 — CommandLine token coloring:**
- [ ] In B3 (`git diff --stat`), bin token `git` renders fg-strong + bold weight 700
- [ ] arg token `diff` renders default fg
- [ ] flag token `--stat` renders magenta (`--accent-secondary`)
- [ ] Tokens space-separated visually

**SHELL-05 — Tool-call blocks (4 status states):**
- [ ] B7 (Tool, Pending): "Tool: read_file" with `pending…` in yellow on right
- [ ] B8 (Tool, Running): "Tool: edit_file" with `running…` in yellow
- [ ] B9 (Tool, Done): "Tool: search" with `[OK]` in green
- [ ] B10 (Tool, Failed): "Tool: compile" with `failed` in red
- [ ] All four show the yellow `is-tool` stripe
- [ ] Args summary visible below the tool name in dim 11px text

**SHELL-06 — Input box mode glyph + focus ring:**
- [ ] Bottom of central column: input wrap visible
- [ ] Mode pill says `Shell` (uppercase via CSS)
- [ ] Left of textarea: prompt glyph `❯` in cyan
- [ ] Textarea placeholder: "Type a command, or `/` for commands"
- [ ] Right of textarea: 3 buttons — `@` (attach), `●` (voice), `↵` (run, in cyan)
- [ ] Below mode pill: "⌥+M to switch" left, "↵ run · ⇧↵ newline · ⌃C cancel" right
- [ ] **Focus ring:** input wrap has cyan border + 3px cyan glow (`is-focus` is hardcoded in Phase 3 per planner-handoff #4)
- [ ] Click into textarea: cyan caret; typing inserts characters (browser default — no Rust state captures the text in Phase 3)

**SHELL-07 — Agent side panel:**
- [ ] Right side of viewport: 360px-wide column visible
- [ ] Side panel head: "IH" sigil (20px) + "HERMES" title in cyan + `/default` personality pill
- [ ] 5 message cards:
  - M1 (user, 00:14:42): "Pull request feedback on the personality refactor — did I miss anything?"
  - M2 (hermes, 00:14:43): tool call `read_file` with `[OK]` (green)
  - M3 (hermes, 00:14:46): multi-paragraph text mentioning `personality.rs:84` and `PROMPT_CACHE`
  - M4 (user, 00:14:50): "Good catch. Patch it."
  - M5 (hermes, 00:14:51): tool call `edit_file` with `running…` (yellow)
- [ ] User vs hermes messages styled differently (`is-user` / `is-hermes`)

**SHELL-08 — Status bar dot-pills:**
- [ ] Bottom of central column (height 24px): 5 dot-pills in this order
  - Pill 0 (cyan): `Chat`
  - Pill 1 (magenta): `claude-sonnet-4`
  - Pill 2 (green): `anthropic`
  - Pill 3 (yellow): `12.3K/128K (10%)`
  - Scanner cells (10 unicode glyphs) — see SHELL-09
- [ ] Each pill separated by dim middot (`·`)
- [ ] Right side: hint `/help · ⌃C cancel · ⌘K palette` in dim grey

**SHELL-09 — Scanner animation:**
- [ ] In status bar between tokens pill and hint: 10 cells with `░` glyphs
- [ ] Cells animate continuously — bright cyan "lit" glyph appears to bounce across the row
- [ ] Motion smooth (10 fps / 100ms tick); runs on page load (no user input needed)
- [ ] Adjacent cells show t1 (bright cyan) and t2 (dim cyan-mix) trailing the lit cell
- [ ] CSS-driven, NOT signal-driven — DevTools Performance panel shows no JS scripting; animation is GPU-composited

**SHELL-10 — Command palette overlay:**
- [ ] On page load, overlay covers the lower-mid portion (`open: true` per CONTEXT D-19)
- [ ] Overlay top: search row with `⌘K` accent badge + "Search commands, files, recent…" placeholder + `esc` chip on right
- [ ] Section header `Slash commands` (uppercase, dim)
- [ ] 6 slash rows with `/` glyph:
  - `/help` — Show available commands — `?`
  - `/status` — IronHermes status — `⌘` `I`
  - `/doctor` — Run doctor checks — (none)
  - `/personality` — Change personality preset — (none)
  - `/clear` — Clear scrollback — `⌘` `K`
  - `/quit` — Exit chat — `⌘` `Q`
- [ ] **First row (`/help`) has `is-active` highlight** (`--w-bg-3` background per UI-SPEC line 301)
- [ ] Section header `Workflows` (uppercase, dim)
- [ ] 4 workflow rows with `▸` glyph:
  - Git: working tree status — `git status`
  - Cargo: build workspace — `cargo build`
  - Start chat session — `ironhermes chat`
  - Run config doctor — `ironhermes doctor`

**Pixel-perfect side-by-side cross-check (UI-SPEC dimensions):**
- [ ] Title bar height ≈ 32px
- [ ] Status bar height ≈ 24px
- [ ] Side panel width ≈ 360px
- [ ] Block stripes are 2px wide
- [ ] Body font is monospace and renders Ioskeley Mono (DevTools → Computed → font-family)
- [ ] Accent color is cyan `#4ec9b0`

**Reply format:**

If everything passes: type `approved`.

If anything fails: list failing criteria, e.g.:
```
SHELL-09 fails: scanner cells render but no animation visible
SHELL-02 fails: is-out stripe color appears black, not dim grey
```
The orchestrator routes failures into a `--gaps` Plan 03-06 if needed.
  </how-to-verify>
  <verify>
Verification is the user's reply to chat. There is no automated check — this is a checkpoint:human-verify task. The "verify" condition is: user typed `approved` (pass) or listed specific SHELL-XX failures (route to gap-closure planning). No CLI command can replace the side-by-side visual comparison against the prototype HTML.
  </verify>
  <done>
User replied `approved` (Phase 3 ready for `/gsd-verify-work`) OR user listed failing SHELL-XX criteria with descriptions (orchestrator spawns `/gsd-plan-phase 3 --gaps`).
  </done>
  <resume-signal>Type "approved" to close Phase 3 manual UAT, or list failing SHELL-XX criteria with brief descriptions.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev server → user browser | `dx serve` exposes localhost:8080 — local-only by default; no network exposure. |
| prototype HTML reference | Read-only file at `warp2ironhermes/project/Warp × IronHermes.html`; opened in user's browser tab as the source of truth, not modified by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Spoofing | UAT reviewer identity | accept | Solo-developer workflow per PROJECT.md "Solo Developer + Claude Workflow"; only the user reviews. |
| T-03-17 | Tampering | prototype HTML | accept | `warp2ironhermes/` is read-only per CLAUDE.md; UAT compares against on-disk file unchanged from initial handoff. |
</threat_model>

<verification>
**Plan-level checks:**
- This plan has no automated verification (it is a manual UAT checkpoint).
- The blocking gate is the user's reply: `approved` (proceed to `/gsd-verify-work`) or failure list (route to `--gaps` planning).
</verification>

<success_criteria>
- User runs `dx serve --features web` successfully
- User opens both browser tabs (Dioxus build + prototype HTML)
- User walks through SHELL-01..SHELL-10 checklist
- User replies `approved` (Phase 3 closes) OR lists specific failures (gap-closure planning is triggered)
</success_criteria>

<output>
After user replies `approved`, append a "Manual UAT Outcome" block to `.planning/phases/03-desktop-shell/03-05-SUMMARY.md` capturing:
- UAT date
- Reviewer (user)
- Per-SHELL-XX pass/fail status (all 10 should be PASS)
- Reviewer comments on cross-cut findings (cursor styles, micro-spacing, etc.)
- Confirmation: Phase 3 ready for `/gsd-verify-work`

If user reports failures, do NOT write the SUMMARY; instead inform the orchestrator to spawn `/gsd-plan-phase 3 --gaps` with the failure list.
</output>
