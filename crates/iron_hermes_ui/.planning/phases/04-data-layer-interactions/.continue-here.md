---
context: phase
phase: 04-data-layer-interactions
task: complete
total_tasks: 6
status: paused
last_updated: 2026-05-03T16:16:46.172Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Subagent returns mid-iteration without committing wrap-up | Both 04-01 and 04-03 executor agents returned to the orchestrator with SUMMARY.md / STATE.md / ROADMAP.md changes uncommitted (or untracked) — the agent's planned final commit never landed. Orchestrator had to recover by inspecting `git status` and crafting the missing commit. | advisory | Orchestrator MUST always run `git status --short` after each subagent return, and commit any wrap-up files the agent left behind before declaring the wave complete. Treat subagent text output as informational only — verify state on disk. |

<current_state>
Phase 4 Wave 3 just completed and was committed cleanly:

- HEAD = `a364314 docs(04-03): summary + tracking after Wave 3 (mocks module tree)`
- All 04-03 task commits + wrap-up landed
- Working tree clean (only pre-existing untracked files: `.omc/`, `AGENTS.md`, `README.md`, `assets/favicon.ico`, `assets/tailwind.css`, `clippy.toml`, `tailwind.css`, `warp2ironhermes/` — none of these are Phase 4 artifacts)
- 3 of 6 plans complete (04-01, 04-02, 04-03)
- 3 plans remaining (04-04a, 04-04b, 04-05)
- Phase 4 still **In Progress** in ROADMAP.md (3/6)
- STATE.md `progress.completed_plans: 12` was the snapshot before Phase 4 began; it advances as Wave 4/5 land
</current_state>

<completed_work>

Completed Plans:
- **04-01** Cargo + cfg foundation (Wave 1) — deps + `platform::timer::sleep(u32)` cfg-gated; three-platform compile gate green; `tokio` dev-dep proven via `#[tokio::test]`. Commits: `166f538`, `1cb06e1`, `0da48ef`, `9489fc9`.
- **04-02** state.rs extensions (Wave 2) — `Personality`, `BlockEntry`, `PaletteState`, `ShellSettings`, `now_time` helper; `demo_blocks` → `demo_block_entries` rename. Commits: `ab9451a`, `621b0f1`, `0bafd00`.
- **04-03** Mocks module tree (Wave 3) — `src/mocks/{personalities,shell_outputs,agent_steps,mod}.rs`; `warp_hermes.rs` transient shim; module-level `#![allow(dead_code)]` + scoped allows on Wave-1 carry-forward symbols; borrow-then-await discipline first becomes binding. Commits: `c9a9cba`, `811e147`, `ffaec5e`, `983e8db`, `6ef651d`, `a364314`.
</completed_work>

<remaining_work>

Plans remaining (3):
- **04-04a** (Wave 4) — BlockEntry-id propagation through `src/components/shell/markdown.rs`, `mod.rs`, `block.rs`, `block_stream.rs`, plus shim adjustments in `warp_hermes.rs`. Autonomous. Files modified: 5.
- **04-04b** (Wave 4) — Prop-shape refactor for `input_box.rs`, `command_palette.rs`, `status_bar.rs`, `agent_panel.rs`, plus shim adjustments in `warp_hermes.rs`. Autonomous. Files modified: 5.
  - **MUST RUN AFTER 04-04a** — both plans modify `src/components/warp_hermes.rs`. Despite being assigned wave 4 together, the execute-phase workflow's intra-wave files_modified overlap detection forces sequential execution.
- **04-05** (Wave 5) — Replaces the Wave 3 expanded shim with the full WarpHermes integration. Files modified: 1 (`src/components/warp_hermes.rs`). **autonomous: false** — contains at least one checkpoint task requiring user interaction.

After all plans:
- code_review_gate (`/gsd-code-review` skill, advisory)
- regression_gate (`cargo test`)
- verify_phase_goal via `gsd-verifier` agent → produces `04-VERIFICATION.md`
</remaining_work>

<decisions_made>

- **Wave 4 plans serialize.** 04-04a and 04-04b both modify `src/components/warp_hermes.rs` → intra-wave files_modified overlap → orchestrator runs them one at a time on main working tree.
- **Worktrees disabled.** `workflow.use_worktrees=false` → all subagents run sequentially on main; the executor agent itself updates `STATE.md` and `ROADMAP.md` (no orchestrator-side merge step).
- **Wave 3 shim is intentional.** `warp_hermes.rs` collects `Vec<BlockEntry>` into `Vec<Block>` so the Phase 3 BlockStream prop type-checks. Wave 4 retires this shim entirely — do not patch around it; let 04-04a/04-04b do the prop-shape work.
- **Borrow-then-await discipline (D-06) is now binding.** Clippy `await-holding-invalid-types` rule fires on every `.await` because `clippy.toml` lists `GenerationalRef`, `GenerationalRefMut`, and `dioxus_signals::WriteLock`. Pattern: `{ let id = sig(); sig.set(id+1); id }` evaluates to the new id while keeping all signal locks scoped. RESEARCH Pattern 2 GOOD example.
- **Dead-code suppressions name their consumer.** Module-level `#![allow(dead_code)]` in `src/mocks/*` and scoped `#[allow(dead_code)] // consumed by Wave 4 (Plan 04-04a/04-04b)` on `Personality::label`, `Personality::ALL`, `PaletteState`, `ShellSettings`. Wave 4 should REMOVE these allows as it wires the symbols.
</decisions_made>

<blockers>

- None for Phase 4. Wave 4 and Wave 5 can resume immediately.
- **Pre-existing (non-blocking for Phase 4)**: Phase 2 Plan 02-04 manual UAT still open (checkpoint:human-verify). Documented in STATE.md from prior session; does not gate Phase 4.
- **Wave 5 user-interaction reminder**: Plan 04-05 has `autonomous: false` and at least one checkpoint task. Resuming run will pause for user input when Wave 5 reaches the checkpoint.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured machine-readable handoff (canonical state).
2. `.planning/phases/04-data-layer-interactions/04-CONTEXT.md` — locked decisions for the phase (D-01..D-37).
3. `.planning/phases/04-data-layer-interactions/04-RESEARCH.md` — pitfalls and patterns (Borrow-then-await, web-sys feature flags, etc.).
4. `.planning/phases/04-data-layer-interactions/04-PATTERNS.md` — existing-code analogs for the new files.
5. `.planning/phases/04-data-layer-interactions/04-01-SUMMARY.md`, `04-02-SUMMARY.md`, `04-03-SUMMARY.md` — what's already built and what each subsequent wave consumes.
6. `.planning/phases/04-data-layer-interactions/04-04a-PLAN.md` and `04-04b-PLAN.md` — next two plans, in execution order.
7. `./CLAUDE.md` and `./AGENTS.md` — project instructions and Dioxus 0.7 API reference.

## Critical Anti-Patterns (do NOT repeat these)

- **ANTI-PATTERN: Trust subagent's "PLAN COMPLETE" text without spot-check.** → After each subagent return, run `git status --short` and `git log --oneline -10`. If `STATE.md`/`ROADMAP.md`/`SUMMARY.md` are uncommitted/untracked, the agent's wrap-up commit didn't land — craft and apply the missing commit before declaring the wave done.
- **ANTI-PATTERN: Patch around the Wave 3 warp_hermes.rs shim.** → The shim is intentional. Wave 4 retires it. Do not "fix" it incrementally.
- **ANTI-PATTERN: Hold a Signal write across `.await`.** → Clippy will fail the build. Use the inline-id-block pattern: `{ let id = sig(); sig.set(id+1); id }` then `.await`.

## Infrastructure State

- Branch: `main`. Branching strategy `none` for this phase.
- Worktrees: disabled.
- Submodules: none.
- Build: `cargo build` clean for default features. Three-platform check command (canonical): `for f in web desktop mobile; do cargo check --no-default-features --features $f || exit 1; done`.
- Test: `cargo test` runnable; the only Phase 4 test so far is the desktop tokio time-driver smoke test added by 04-01.
- Pre-existing untracked files (NOT Phase 4 artifacts; leave alone): `.omc/`, `AGENTS.md`, `README.md`, `assets/favicon.ico`, `assets/tailwind.css`, `clippy.toml`, `tailwind.css`, `warp2ironhermes/`.

<context>
We were three plans into Phase 4 when the user paused. Wave 1, 2, and 3 are committed and clean. The orchestrator was about to spawn the Wave 4 agent (04-04a) when the user invoked /gsd-pause-work — a previous attempt to "resume" a mid-thought 04-03 agent via a fresh subagent was rejected (correctly: the original agent had already finished landing its commits, so the resume was redundant; we just needed to commit the wrap-up files it had written but not yet committed).

Mental model going in to resume:
- Wave 4 is mostly mechanical prop-shape changes — but the warp_hermes.rs overlap means strict serialization. Run 04-04a, verify it builds clean on all three platforms, THEN run 04-04b.
- Wave 5 is the integration step that retires the shim. It has at least one checkpoint task (autonomous=false). Be ready for the executor agent to return mid-plan with a checkpoint payload that needs user input.
- After Wave 5 completes: run code-review skill, regression gate (`cargo test`), then `gsd-verifier` to produce VERIFICATION.md and close the phase.
</context>

<next_action>
Run `/gsd-execute-phase 4`. The orchestrator will:
1. Re-discover plans via `gsd-sdk query phase-plan-index 04` — should report 3 incomplete (04-04a, 04-04b, 04-05).
2. Detect the `src/components/warp_hermes.rs` overlap in Wave 4 and serialize 04-04a → 04-04b.
3. After every subagent return, run `git status --short` to verify the wrap-up commit landed before declaring the wave complete (see anti-pattern table).
4. Pause at Wave 5 checkpoint task; surface to user; spawn continuation agent with user response.
5. Run code-review → regression gate → gsd-verifier → mark phase complete.
</next_action>
