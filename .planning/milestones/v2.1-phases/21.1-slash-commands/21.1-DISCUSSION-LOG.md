# Phase 21.1: Slash Commands - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-17
**Phase:** 21.1-slash-commands
**Areas discussed:** Router location & crate, Command set & platform rules, Alias & prefix matching, Integration pattern

---

## Router Location & Crate

| Option | Description | Selected |
|--------|-------------|----------|
| ironhermes-core (Recommended) | Leaf crate — accessible from CLI, gateway, agent, and ACP. No circular deps. | ✓ |
| ironhermes-agent | Next to AgentLoop. But gateway would need to depend on agent. | |
| New ironhermes-commands crate | Dedicated crate. Clean separation but adds a workspace crate. | |

**User's choice:** ironhermes-core
**Notes:** None — recommended default accepted.

---

## Command Set & Platform Rules

| Option | Description | Selected |
|--------|-------------|----------|
| Router + SKILL-13 core set (Recommended) | Build router + 10 SKILL-13 commands. Additional commands added incrementally. | |
| Router + full hermes-agent parity | Implement all 30+ commands from the reference. Complete parity in one phase. | ✓ |
| Router only, no new commands | Infrastructure only. Migrate existing commands into router. | |

**User's choice:** Router + full hermes-agent parity
**Notes:** User wants complete command parity, not incremental. Commands must be wired to functions or explicitly marked TODO.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Platform tags on commands (Recommended) | Each command declares platform availability. Router filters by context. | ✓ |
| All commands everywhere | Register all commands on all platforms. Return 'not available' for wrong platform. | |
| Separate command sets per platform | Each platform registers its own set. Shared commands duplicated. | |

**User's choice:** Platform tags on commands
**Notes:** None — recommended default accepted.

---

## Alias & Prefix Matching

| Option | Description | Selected |
|--------|-------------|----------|
| Shortest unique prefix (Recommended) | Unambiguous prefix resolves. Ambiguous returns error listing candidates. | ✓ |
| Explicit aliases only | No prefix matching. Only exact matches or declared aliases. | |
| Both prefix + explicit aliases | Aliases checked first, then prefix fallback. | |

**User's choice:** Shortest unique prefix
**Notes:** None — recommended default accepted.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, aliases + prefix (Recommended) | Commands declare aliases. Resolution: exact → alias → prefix. | ✓ |
| Prefix only, no aliases | Prefix matching sufficient. Skip alias mechanism. | |

**User's choice:** Yes, aliases + prefix
**Notes:** None — recommended default accepted.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Built-in commands first, then skills (Recommended) | resolve_command() checks built-ins first. Unmatched treated as skill invocation. | ✓ |
| Unified namespace | Skills and built-ins share one namespace. Prefix applies to both. | |

**User's choice:** Built-in commands first, then skills
**Notes:** None — recommended default accepted.

---

## Integration Pattern

| Option | Description | Selected |
|--------|-------------|----------|
| Replace both, single router (Recommended) | Delete hardcoded CLI and gateway handlers. Both call unified router. Extension chain stays. | ✓ |
| Wrap existing implementations | Router delegates to existing handlers as backends. | |
| Gradual migration | New router + old handlers coexist temporarily. | |

**User's choice:** Replace both, single router
**Notes:** None — recommended default accepted.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Before AgentLoop, in caller (Recommended) | CLI REPL and gateway handler check '/' prefix, call router before AgentLoop. | ✓ |
| Inside AgentLoop | AgentLoop checks for slash commands at turn start. Mixes concerns. | |
| Middleware layer | CommandMiddleware wraps AgentLoop. Clean separation but new layer. | |

**User's choice:** Before AgentLoop, in caller
**Notes:** None — recommended default accepted.

---

| Option | Description | Selected |
|--------|-------------|----------|
| CommandContext struct (Recommended) | Commands receive context with shared state refs. Platforms construct their own. | ✓ |
| Closure-based handlers | Each command is a closure capturing deps. Flexible but harder to extend. | |
| Trait-based command handlers | Each command implements SlashCommand trait. Structured but verbose. | |

**User's choice:** CommandContext struct
**Notes:** None — recommended default accepted.

---

## User-Provided Context

The user provided two comprehensive reference documents during discussion:
1. **hermes-agent CLI Commands Reference** — Full list of terminal shell commands (hermes chat, hermes model, etc.)
2. **hermes-agent Slash Commands Reference** — Complete list of in-chat slash commands with platform availability

Key user directive: "The commands must be wired up to a function, use the /Users/twilson/code/hermes-agent source code in research to identify if the slash command can be used or mark it a TODO if it can't be wired up."

## Claude's Discretion

- CommandContext fields and Optional vs required deps
- Command handler implementation (functions vs trait objects)
- Internal module structure in ironhermes-core
- Flag parsing for commands like /model --global
- Running-agent guard mechanism

## Deferred Ideas

- Voice/TTS (/voice) — no infrastructure
- Background sessions (/background) — needs session manager
- Filesystem checkpoints (/rollback, /snapshot) — no checkpoint system
- Analytics (/insights, /usage cost) — needs tracking infrastructure
- Self-update (/update) — compiled binary, different mechanism
