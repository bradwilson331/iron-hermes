---
phase: 21.6-port-deployment-setup-files-from-hermes-agent
plan: 01
subsystem: infra
tags: [deployment, env-template, yaml-config, dockerignore, first-run-scaffolding]

# Dependency graph
requires:
  - phase: 12-provider-resolver
    provides: Config struct with all provider/subsystem sections
provides:
  - ".env.example template with all provider and tool environment variables"
  - "cli-config.yaml.example full YAML config mirroring every Config struct section"
  - ".dockerignore for Docker build exclusions"
  - "docker/SOUL.md default agent identity seed"
  - "ensure_home_dirs() first-run directory scaffolding in main.rs"
affects: [21.6-02-install-entrypoint-scripts, 21.6-03-dockerfile-compose]

# Tech tracking
tech-stack:
  added: []
  patterns: ["belt-and-suspenders directory creation on every startup (D-21)", "commented-out .env template pattern (D-03)"]

key-files:
  created:
    - .env.example
    - cli-config.yaml.example
    - .dockerignore
    - docker/SOUL.md
  modified:
    - crates/ironhermes-cli/src/main.rs

key-decisions:
  - "Rust 2024 edition requires unsafe blocks for env var mutation in tests -- used unsafe with SAFETY comments and --test-threads=1 constraint"

patterns-established:
  - "Deployment template files at repo root with commented-out defaults matching Config struct"
  - "ensure_home_dirs() called between dotenvy load and Cli::parse() for idempotent directory creation"

requirements-completed: [D-01, D-02, D-03, D-04, D-05, D-06, D-20, D-21, D-22, D-23, D-24]

# Metrics
duration: 5min
completed: 2026-04-21
---

# Phase 21.6 Plan 01: Deployment Setup Files Summary

**Deployment template files (.env.example, cli-config.yaml.example, .dockerignore, docker/SOUL.md) and ensure_home_dirs() first-run scaffolding creating 7 subdirectories under IRONHERMES_HOME**

## Performance

- **Duration:** 5 min
- **Started:** 2026-04-21T21:25:49Z
- **Completed:** 2026-04-21T21:31:38Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- .env.example with all 11 LLM provider sections, tool API keys, sandbox config, gateway platforms, and debug flags -- all lines commented-out per D-03
- cli-config.yaml.example mirroring all 16 Config struct sections (model, agent, terminal, web, exec, gateway, cron, memory, compression, skills, subagent, rate_limit, batch, security, providers, custom_providers) with inline documentation
- .dockerignore excluding target/, .planning/, .git/, .claude/, .omc/, worktrees/ while preserving Cargo.lock
- docker/SOUL.md default agent identity seed containing no secrets (T-21.6-02 verified)
- ensure_home_dirs() creating 7 directories (cron, sessions, logs, hooks, memories, skills, workspace) idempotently on every startup

## Task Commits

Each task was committed atomically:

1. **Task 1: Create .env.example, cli-config.yaml.example, .dockerignore, and docker/SOUL.md** - `87e0d07` (feat)
2. **Task 2: Add ensure_home_dirs() first-run scaffolding to main.rs** - `16d9541` (feat)

## Files Created/Modified
- `.env.example` - Environment variable template for all providers, tools, sandbox, gateway, debug flags
- `cli-config.yaml.example` - Full YAML config template matching every Config struct section with defaults
- `.dockerignore` - Docker build exclusions (target, .planning, .git, .claude, .omc, worktrees)
- `docker/SOUL.md` - Default agent identity seed for Docker deployments
- `crates/ironhermes-cli/src/main.rs` - Added ensure_home_dirs() function, call site, and unit test

## Decisions Made
- Rust 2024 edition requires `unsafe` blocks for `std::env::set_var`/`remove_var` in tests -- wrapped with SAFETY comments noting `--test-threads=1` constraint

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Unsafe blocks for env var mutation in Rust 2024 edition**
- **Found during:** Task 2 (unit test compilation)
- **Issue:** Rust 2024 edition makes `std::env::set_var` and `std::env::remove_var` unsafe functions; test code failed to compile
- **Fix:** Wrapped both calls in `unsafe {}` blocks with SAFETY comments documenting the `--test-threads=1` constraint
- **Files modified:** crates/ironhermes-cli/src/main.rs
- **Verification:** `cargo test -p ironhermes-cli --bin ironhermes -- ensure_home_dirs --test-threads=1` passes
- **Committed in:** 16d9541 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug fix)
**Impact on plan:** Necessary for Rust 2024 edition compliance. No scope creep.

## Issues Encountered
None beyond the Rust 2024 unsafe requirement documented above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Template files ready for Plan 02 (install/entrypoint scripts) to reference
- docker/SOUL.md ready for Plan 03 (Dockerfile/compose) to COPY into image
- ensure_home_dirs() provides belt-and-suspenders guarantee that Plan 02 install.sh can rely on

## Self-Check: PASSED

All 6 files verified present. Both commit hashes (87e0d07, 16d9541) confirmed in git log.

---
*Phase: 21.6-port-deployment-setup-files-from-hermes-agent*
*Completed: 2026-04-21*
