---
phase: 32.2-subagent-delegation-parity
plan: "02"
subsystem: tools
tags: [delegate_task, subagent, rust, anyhow, schema, max_iterations]

requires:
  - phase: 32.2-01
    provides: renamed SubagentConfig fields (max_concurrent_children, child_timeout_seconds) and delegation: config namespace

provides:
  - clarify and send_message structurally excluded from child registries in both delegate_task.rs copies
  - batch oversize returns Err with descriptive message citing delegation.max_concurrent_children config key
  - max_iterations exposed in JSON schema at top-level and tasks.items.properties levels
  - effective_max_iterations and per_task_max_iterations resolvers wire per-call args into run_child
  - five new unit tests proving D-05 exclusions, D-06 oversize bail, D-08 schema and resolver

affects:
  - 32.2-03 (nested orchestration plan — will extend build_child_registry signature with depth/role params)
  - any code path that calls execute_batch with more tasks than max_concurrent_children

tech-stack:
  added: []
  patterns:
    - "Structural exclusion match arms for tool names in build_child_registry (silent-skip no-op arms)"
    - "anyhow::bail! for early batch-oversize Err before any tokio::spawn"
    - "Per-call override resolver pattern: args.get(key).and_then(as_u64).map(as usize).unwrap_or(config.field)"

key-files:
  created: []
  modified:
    - crates/ironhermes-tools/src/delegate_task.rs
    - crates/ironagent-tools-api/src/delegate_task.rs

key-decisions:
  - "D-05: clarify and send_message silently skipped in build_child_registry for ALL children (no role exception)"
  - "D-06: execute_batch returns Err immediately when tasks.len() > max_concurrent_children; no silent truncation"
  - "D-08: max_iterations per-call override wired in both single-task and batch paths; no upper cap (PROV-09 iteration budget already throttles)"
  - "ironagent-tools-api stale fields (timeout_secs, max_subagents) fixed to match Plan 01 renames in lockstep with Plan 02 changes"

patterns-established:
  - "Batch oversize guard fires before tokio::spawn so zero tasks are spawned on error"
  - "Per-task resolver declared outside tokio::spawn block to avoid re-parsing JSON inside async move"

requirements-completed:
  - DEL-05
  - DEL-06
  - DEL-08

duration: 18min
completed: 2026-05-17
---

# Phase 32.2 Plan 02: Add clarify/send_message exclusions, batch oversize bail, max_iterations schema

**`clarify` and `send_message` structurally excluded from child registries; batch oversize returns Err citing `delegation.max_concurrent_children`; `max_iterations` per-call override wired in schema and both execute paths — applied byte-identically to both `delegate_task.rs` copies.**

## Performance

- **Duration:** ~18 min
- **Started:** 2026-05-17T04:26:37Z
- **Completed:** 2026-05-17T04:44:00Z
- **Tasks:** 2 (executed as one atomic commit per plan protocol)
- **Files modified:** 2

## Accomplishments

- D-05: added `"clarify" => { /* no clarify_callback channel in children */ }` and `"send_message" => { /* no platform session in children */ }` match arms to `build_child_registry` in both crate copies, after the existing `"cronjob"` arm
- D-06: replaced `tasks_array.iter().take(max_batch) + tracing::warn!` truncation block with `anyhow::bail!` that fires before any `tokio::spawn`; promoted `let tasks: Vec<_> = tasks_array.iter().collect()` to run unconditionally
- D-08: added `"max_iterations"` property to schema at top-level and in `tasks[].items.properties`; wired `effective_max_iterations` in single-task execute path and `per_task_max_iterations` in batch execute path; both replace the former hardcoded `self.config.max_iterations` / `config.max_iterations` arguments to `run_child`
- Renamed `test_batch_truncates_to_max_subagents` → `test_batch_oversize_returns_err` with rewritten body asserting `result.is_err()` and both message substrings
- Added 4 new unit tests: `test_build_child_registry_strips_clarify`, `test_build_child_registry_strips_send_message`, `test_delegate_task_schema_has_max_iterations`, `test_per_call_max_iterations_overrides_config`
- Fixed stale `timeout_secs` and `max_subagents` field names in `ironagent-tools-api` tests (Plan 01 renames had not been applied there)

## Task Commits

Both tasks executed as one atomic commit:

1. **Tasks 1+2: D-05 exclusions + D-06 bail + D-08 schema/resolver** - `7bcd93b7` (feat)

## Exact Oversize Bail Message

```
Batch size {} exceeds max_concurrent_children limit of {}. Increase delegation.max_concurrent_children in config.yaml to allow larger batches.
```

(With `{}` substituted with `tasks_array.len()` and `max_batch` at runtime.)

## Exact Schema Description Strings for max_iterations

**Top-level (single-task call):**
```json
"max_iterations": {
    "type": "integer",
    "minimum": 1,
    "description": "Per-call max LLM iterations override. Defaults to delegation.max_iterations from config."
}
```

**Per-task in batch (`tasks[].items.properties`):**
```json
"max_iterations": {
    "type": "integer",
    "minimum": 1,
    "description": "Per-task max iterations override. Defaults to delegation.max_iterations from config."
}
```

## Byte-Identical Parity Confirmation

Diff of key changed sections between both crate copies (all empty = byte-identical):

```
$ diff <(grep -A1 '"clarify" =>' crates/ironhermes-tools/src/delegate_task.rs) \
       <(grep -A1 '"clarify" =>' crates/ironagent-tools-api/src/delegate_task.rs)
(no output)

$ diff <(grep -A1 '"send_message" =>' crates/ironhermes-tools/src/delegate_task.rs) \
       <(grep -A1 '"send_message" =>' crates/ironagent-tools-api/src/delegate_task.rs)
(no output)

$ diff <(grep 'Batch size' crates/ironhermes-tools/src/delegate_task.rs) \
       <(grep 'Batch size' crates/ironagent-tools-api/src/delegate_task.rs)
(no output)

$ diff <(grep 'effective_max_iterations' crates/ironhermes-tools/src/delegate_task.rs) \
       <(grep 'effective_max_iterations' crates/ironagent-tools-api/src/delegate_task.rs)
(no output)
```

## Files Created/Modified

- `crates/ironhermes-tools/src/delegate_task.rs` — D-05 match arms, D-06 bail, D-08 schema + resolvers, renamed test, 4 new tests
- `crates/ironagent-tools-api/src/delegate_task.rs` — byte-identical mirror plus fix of stale Plan 01 field names (timeout_secs→child_timeout_seconds, max_subagents→max_concurrent_children)

## Decisions Made

- Fixed stale `timeout_secs: 1` and `max_subagents: 2` field names in `ironagent-tools-api` test bodies (Rule 1 auto-fix — those fields no longer exist after Plan 01 renames and would fail to compile once ironagent-tools-api is brought into the workspace). Applied inline as part of the same commit.
- `ironagent-tools-api` batch path also had stale `config.timeout_secs` (not `config.child_timeout_seconds`) — fixed in the same pass.
- No upper cap added on `max_iterations` per-call override (T-32.2-06 accepted: existing PROV-09 iteration budget throttles runaway iterations).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed stale SubagentConfig field names in ironagent-tools-api**
- **Found during:** Task 1 (applying changes to ironagent-tools-api)
- **Issue:** `ironagent-tools-api` still used `timeout_secs` (in `per_task_timeout_secs` resolver) and `max_subagents` (in test body) — Plan 01 renamed these to `child_timeout_seconds` and `max_concurrent_children` respectively. These would cause compilation failure once the crate is brought into the workspace.
- **Fix:** Updated `config.timeout_secs` → `config.child_timeout_seconds` in batch resolver; `timeout_secs: 1` → `child_timeout_seconds: 1` and `max_subagents: 2` → `max_concurrent_children: 2` in test bodies.
- **Files modified:** `crates/ironagent-tools-api/src/delegate_task.rs`
- **Committed in:** `7bcd93b7`

---

**Total deviations:** 1 auto-fixed (Rule 1 — stale field names from Plan 01 renames not applied to ironagent-tools-api)
**Impact on plan:** Necessary for compilation correctness. No scope creep.

## Issues Encountered

- `test_batch_oversize_returns_err` was defined twice: once replacing the old test body, and once in the new tests block added during TDD RED phase. Removed the duplicate from the new-tests block, keeping only the renamed existing test. Discovered at compile time; fixed immediately.

## Verification Results

```
cargo test -p ironhermes-tools --lib delegate_task::tests
test result: ok. 45 passed; 0 failed; 0 ignored; 0 measured; 307 filtered out

Specific new tests:
  test_build_child_registry_strips_clarify ... ok
  test_build_child_registry_strips_send_message ... ok
  test_batch_oversize_returns_err ... ok
  test_delegate_task_schema_has_max_iterations ... ok
  test_per_call_max_iterations_overrides_config ... ok

cargo check --workspace: Finished (warnings are pre-existing, unrelated to this plan)
```

## Next Phase Readiness

- Plan 32.2-03 can now extend `build_child_registry` signature with `role`, `child_depth`, and `config` parameters — the match block is in the correct shape with all known exclusions already present
- `max_iterations` is schema-exposed and resolver-wired; Plan 03 does not need to touch those paths

## Known Stubs

None — all new functionality is fully implemented and tested.

## Threat Flags

No new threat surface introduced. T-32.2-04 (clarify/send_message elevation) and T-32.2-05 (batch oversize DoS) are now mitigated as planned. T-32.2-06 (max_iterations escalation) is accepted per plan.

## Self-Check: PASSED

- `7bcd93b7` exists in git log ✓
- `crates/ironhermes-tools/src/delegate_task.rs` modified ✓
- `crates/ironagent-tools-api/src/delegate_task.rs` modified ✓
- 45 tests pass in ironhermes-tools ✓
- 0 occurrences of `test_batch_truncates_to_max_subagents` in either file ✓
- 0 occurrences of `tasks_array.iter().take(` in either file ✓
- `anyhow::bail!` count ≥ 2 in both files (4 each) ✓

---
*Phase: 32.2-subagent-delegation-parity*
*Completed: 2026-05-17*
