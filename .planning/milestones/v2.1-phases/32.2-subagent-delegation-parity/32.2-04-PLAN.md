---
phase: 32.2-subagent-delegation-parity
plan: 04
type: execute
wave: 4
depends_on:
  - 32.2-03
files_modified:
  - crates/ironhermes-agent/src/subagent_registry.rs
  - crates/ironhermes-agent/src/subagent_runner.rs
  - crates/ironhermes-core/src/commands/context.rs
autonomous: true
requirements:
  - DEL-10
  - DEL-11
must_haves:
  truths:
    - "`SubagentRegistry::build_tree()` returns a Vec<SubagentTreeNode> grouped by parent_id with stable ordering by started_at"
    - "Root nodes are those with `parent_id == None`; children nest underneath via recursive lookup"
    - "`SubagentInfo.parent_id` is populated from `AgentSubagentRunner.caller_subagent_id` (no longer hardcoded to None)"
    - "`SubagentListSnapshot::tree_summary()` exists as a trait method with a flat default implementation so existing test fakes compile unchanged"
    - "`SubagentRegistryHandle` overrides `tree_summary()` with a real tree-walking impl using block_in_place + block_on"
    - "`SubagentTreeEntry { id, task_summary, uptime, status, parent_id, depth }` is a new public type in commands/context.rs (the `status` field carries the per-node status pill required by D-12 tui_rata spec)"
  artifacts:
    - path: "crates/ironhermes-agent/src/subagent_registry.rs"
      provides: "SubagentTreeNode struct, SubagentRegistry::build_tree(), SubagentRegistry::children_of(), SubagentRegistryHandle::tree_summary() override"
      contains: "pub fn build_tree"
    - path: "crates/ironhermes-agent/src/subagent_runner.rs"
      provides: "SubagentInfo.parent_id populated from self.caller_subagent_id"
      contains: "parent_id: self.caller_subagent_id.clone()"
    - path: "crates/ironhermes-core/src/commands/context.rs"
      provides: "SubagentTreeEntry type, SubagentListSnapshot::tree_summary() default method"
      contains: "pub struct SubagentTreeEntry"
  key_links:
    - from: "AgentSubagentRunner.caller_subagent_id"
      to: "SubagentInfo.parent_id"
      via: "subagent_runner.rs line ~182"
      pattern: "parent_id: self\\.caller_subagent_id\\.clone\\(\\)"
    - from: "SubagentRegistry.active HashMap"
      to: "SubagentTreeNode tree"
      via: "build_tree recursion"
      pattern: "fn build_tree"
    - from: "SubagentListSnapshot trait"
      to: "SubagentRegistryHandle impl"
      via: "tree_summary method"
      pattern: "fn tree_summary"
---

<objective>
Populate the `SubagentInfo.parent_id` field (replacing the `// v2.1+` TODO) using the `caller_subagent_id` field Plan 03 added to `AgentSubagentRunner`, build the pure data-transformation `build_tree()` method on `SubagentRegistry`, and extend the `SubagentListSnapshot` trait with a `tree_summary()` method that has a default flat implementation (so existing test fakes compile without changes) and a real recursive impl on `SubagentRegistryHandle`.

Purpose: This plan delivers the data layer for the `/agents` tree view. Plan 05 consumes `tree_summary()` to render the indented ASCII tree in `cmd_agents` and to JSON-serialize for the iron_hermes_ui server. The default-impl approach (per RESEARCH Open Question 1 recommendation) preserves the existing `FakeSubagentList` in `commands/context.rs` tests at line 564 without requiring any changes to those tests.

Output: `cargo test -p ironhermes-agent` proves parent_id population and build_tree recursion; `cargo test -p ironhermes-core` proves the trait default impl compiles for all existing impls; full workspace tests stay green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32.2-subagent-delegation-parity/32.2-CONTEXT.md
@.planning/phases/32.2-subagent-delegation-parity/32.2-RESEARCH.md
@.planning/phases/32.2-subagent-delegation-parity/32.2-PATTERNS.md
@.planning/phases/32.2-subagent-delegation-parity/32.2-03-SUMMARY.md

@crates/ironhermes-agent/src/subagent_registry.rs
@crates/ironhermes-agent/src/subagent_runner.rs
@crates/ironhermes-core/src/commands/context.rs

<interfaces>
<!-- Existing SubagentInfo (subagent_registry.rs lines 13-21) — parent_id field is ALREADY DECLARED but always None -->
pub struct SubagentInfo {
    pub id: SubagentId,
    pub task_summary: String,
    pub parent_id: Option<SubagentId>,
    pub started_at: Instant,
    pub cancel: CancellationToken,
    pub transcript_path: PathBuf,
}

<!-- Existing SubagentRegistry (subagent_registry.rs ~lines 22-70) -->
pub struct SubagentRegistry {
    active: HashMap<SubagentId, SubagentInfo>,
}
impl SubagentRegistry {
    pub fn register(&mut self, info: SubagentInfo)
    pub fn unregister(&mut self, id: &str) -> Option<SubagentInfo>
    pub fn list(&self) -> Vec<SubagentInfo>    // flat snapshot
    pub fn get(&self, id: &str) -> Option<&SubagentInfo>
    // ADD: pub fn build_tree(&self) -> Vec<SubagentTreeNode>
    // ADD: fn children_of(&self, parent_id: &str) -> Vec<SubagentTreeNode>
}

<!-- Existing SubagentRegistryHandle (subagent_registry.rs ~lines 75-115) — implements SubagentListSnapshot -->
impl SubagentListSnapshot for SubagentRegistryHandle {
    fn active_count(&self) -> usize { ... }
    fn list_summary(&self) -> Vec<(String, String, Duration)> {
        tokio::task::block_in_place(|| {
            tokio::runtime::Handle::current().block_on(async {
                let guard = self.0.read().await;
                guard.list().into_iter().map(...).collect()
            })
        })
    }
    fn kill(&self, id: &str) -> bool { ... }
    fn transcript_path(&self, id: &str) -> Option<PathBuf> { ... }
    // ADD: fn tree_summary(&self) -> Vec<SubagentTreeEntry> { ... }  // override of default impl
}

<!-- Existing SubagentListSnapshot trait (commands/context.rs lines 61-70) -->
pub trait SubagentListSnapshot: Send + Sync {
    fn active_count(&self) -> usize;
    fn list_summary(&self) -> Vec<(String, String, Duration)>;
    fn kill(&self, id: &str) -> bool;
    fn transcript_path(&self, id: &str) -> Option<PathBuf>;
    // ADD with default impl returning flat tree:
    // fn tree_summary(&self) -> Vec<SubagentTreeEntry> { /* default: flatten list_summary */ }
}

<!-- New types to add to commands/context.rs -->
pub struct SubagentTreeEntry {
    pub id: String,
    pub task_summary: String,
    pub uptime: std::time::Duration,
    pub status: String,                // D-12 tui_rata status pill ("running", "killed", "completed", ...)
    pub parent_id: Option<String>,
    pub depth: usize,
}

<!-- New type to add to subagent_registry.rs -->
pub struct SubagentTreeNode {
    pub info: SubagentInfo,
    pub children: Vec<SubagentTreeNode>,
}

<!-- Subagent_runner.rs line ~182 (target of parent_id wiring) -->
parent_id: None, // Plan 04 wires self.caller_subagent_id    -- comment from Plan 03
// → becomes:
parent_id: self.caller_subagent_id.clone(),
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SubagentTreeEntry + tree_summary() default method on SubagentListSnapshot trait in commands/context.rs</name>
  <files>crates/ironhermes-core/src/commands/context.rs</files>
  <read_first>
    - crates/ironhermes-core/src/commands/context.rs (full file — trait at lines 61-70, FakeSubagentList test impl at 564-590, existing test pattern shows what needs to keep compiling)
    - .planning/phases/32.2-subagent-delegation-parity/32.2-PATTERNS.md (section 8 — SubagentTreeEntry shape, default-impl pattern, RESEARCH Open Question 1 recommendation)
    - .planning/phases/32.2-subagent-delegation-parity/32.2-CONTEXT.md (D-11)
  </read_first>
  <behavior>
    - New public struct `SubagentTreeEntry` defined in commands/context.rs (place near the SubagentListSnapshot trait at line 61): `pub struct SubagentTreeEntry { pub id: String, pub task_summary: String, pub uptime: std::time::Duration, pub status: String, pub parent_id: Option<String>, pub depth: usize }`. Derive `Debug, Clone`. The `status` field carries the per-node status pill required by D-12 tui_rata spec — default to the literal string `"running"` for the trait default implementation (since `list_summary()` does not carry status; tree-aware impls populate it from `SubagentInfo`).
    - Extend `SubagentListSnapshot` trait with `fn tree_summary(&self) -> Vec<SubagentTreeEntry>` that has a default implementation: maps `self.list_summary()` into `SubagentTreeEntry` records with `status: "running".to_string()`, `parent_id: None`, and `depth: 0` for every entry (flat default). This ensures `FakeSubagentList` in the same file's tests (line 566) does not need to implement the new method.
    - New unit test in commands/context.rs tests mod: `test_default_tree_summary_flattens_list_summary` — construct a `FakeSubagentList`, call `.tree_summary()`, assert the result has the same length as `.list_summary()` and every entry has `parent_id == None`, `depth == 0`, and `status == "running"` (the trait default).
    - The `FakeSubagentList` test stub (lines 564-590) MUST NOT be modified — its job is to prove the default impl works.
  </behavior>
  <action>
    Edit `crates/ironhermes-core/src/commands/context.rs`. Add the `pub struct SubagentTreeEntry` (with derive Debug, Clone) above the `SubagentListSnapshot` trait declaration at line 61. Extend the trait with the new `tree_summary(&self) -> Vec<SubagentTreeEntry>` method WITH a default implementation that maps `self.list_summary()` element-by-element into `SubagentTreeEntry` (`status: "running".to_string(), parent_id: None, depth: 0`). Add the unit test `test_default_tree_summary_flattens_list_summary` in the existing tests mod (where `FakeSubagentList` is defined). DO NOT modify the `FakeSubagentList` impl block — proving it still compiles without overriding `tree_summary()` is part of the acceptance.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-core --lib commands::context::tests::test_default_tree_summary_flattens_list_summary 2>&1 | tail -15 && cargo check -p ironhermes-core 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ironhermes-core --lib commands::context::tests::test_default_tree_summary_flattens_list_summary 2>&1` exits 0
    - `cargo check -p ironhermes-core 2>&1` exits 0
    - `grep -c "pub struct SubagentTreeEntry" crates/ironhermes-core/src/commands/context.rs` equals 1
    - `grep -c "pub status: String" crates/ironhermes-core/src/commands/context.rs` is greater than or equal to 1 (status field on SubagentTreeEntry — D-12 tui_rata spec)
    - `grep -c "fn tree_summary" crates/ironhermes-core/src/commands/context.rs` is greater than or equal to 1 (declaration with default impl)
    - `grep -A 20 "impl SubagentListSnapshot for FakeSubagentList" crates/ironhermes-core/src/commands/context.rs | grep -c "fn tree_summary"` equals 0 (FakeSubagentList does NOT override the new method — proves the default impl path)
    - All previously passing context.rs tests still pass: `cargo test -p ironhermes-core --lib commands::context::tests 2>&1 | grep "test result: ok"` shows ok line
  </acceptance_criteria>
  <done>SubagentTreeEntry exists; SubagentListSnapshot has a tree_summary() default method; FakeSubagentList compiles unchanged; the default-impl flattening test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SubagentTreeNode + build_tree() on SubagentRegistry; override tree_summary() on SubagentRegistryHandle; populate SubagentInfo.parent_id from AgentSubagentRunner.caller_subagent_id</name>
  <files>crates/ironhermes-agent/src/subagent_registry.rs, crates/ironhermes-agent/src/subagent_runner.rs</files>
  <read_first>
    - crates/ironhermes-agent/src/subagent_registry.rs (full file — SubagentInfo at 13-21, SubagentRegistry at 22-70, SubagentRegistryHandle impl SubagentListSnapshot block at 75-115 with block_in_place pattern)
    - crates/ironhermes-agent/src/subagent_runner.rs (full file — `SubagentInfo` construction at lines 179-187, `caller_subagent_id` field from Plan 03)
    - .planning/phases/32.2-subagent-delegation-parity/32.2-PATTERNS.md (sections 6 and 7 — build_tree recursive pattern, block_in_place bridge)
    - .planning/phases/32.2-subagent-delegation-parity/32.2-CONTEXT.md (D-10, D-11)
    - .planning/phases/32.2-subagent-delegation-parity/32.2-03-SUMMARY.md (confirms caller_subagent_id is on AgentSubagentRunner)
  </read_first>
  <behavior>
    - New public struct in subagent_registry.rs: `pub struct SubagentTreeNode { pub info: SubagentInfo, pub children: Vec<SubagentTreeNode> }`. Derive `Debug, Clone` (SubagentInfo is already Clone — verify).
    - New method on `SubagentRegistry`: `pub fn build_tree(&self) -> Vec<SubagentTreeNode>`. Collect entries where `parent_id.is_none()` as roots; for each root, recursively call `children_of(&info.id)` to populate children. Sort roots by `started_at`; sort each level's children by `started_at` as well for stable ordering.
    - New helper method on `SubagentRegistry`: `fn children_of(&self, parent_id: &str) -> Vec<SubagentTreeNode>` — recursive lookup matching `info.parent_id.as_deref() == Some(parent_id)`.
    - Override `tree_summary()` on `SubagentRegistryHandle`: flatten the `build_tree()` output into a depth-tracked Vec<SubagentTreeEntry>. Use `tokio::task::block_in_place` + `Handle::current().block_on(async { ... })` matching the existing `list_summary()` pattern at lines 96-111. Implement a free function `flatten_tree(nodes: &[SubagentTreeNode], depth: usize, out: &mut Vec<SubagentTreeEntry>)` that walks depth-first, emitting one SubagentTreeEntry per node with `parent_id` from the SubagentInfo, `depth` from the recursion depth, and `status` derived from the SubagentInfo (D-12 status pill). Status derivation: if `info.cancel.is_cancelled()` → `"killed"`, else → `"running"`. Future extensions (e.g. `"completed"`) follow the same pattern but only `running` and `killed` are observable today; if `SubagentInfo` later grows a richer status enum, swap the derivation logic without changing the SubagentTreeEntry shape.
    - Subagent_runner.rs line ~182: replace `parent_id: None, // Plan 04 wires self.caller_subagent_id` with `parent_id: self.caller_subagent_id.clone(),`. The clone is on `Option<String>` so it is cheap.
    - New unit tests in subagent_registry.rs tests mod:
      - `test_build_tree_flat_when_no_parents` — register three SubagentInfo with `parent_id = None`, call `build_tree()`, assert length == 3 and each node has empty children.
      - `test_build_tree_groups_by_parent` — register a parent (id="root", parent_id=None) and two children (parent_id=Some("root".into())). Call `build_tree()`. Assert length == 1; the single root node has `children.len() == 2` and both children have `info.parent_id == Some("root")`.
      - `test_build_tree_three_levels` — register root → mid → leaf chain. Assert tree depth is 3 (root.children[0].children.len() == 1).
      - `test_tree_summary_handle_flattens_with_depth` — using SubagentRegistryHandle, register a 2-level tree, call `tree_summary()`, assert the flattened entries have correct `depth` values (0 for root, 1 for children), correct `parent_id` references, and `status == "running"` for non-cancelled nodes plus `status == "killed"` for a node whose cancel token has been triggered.
    - New unit test in subagent_runner.rs tests mod: `test_run_child_propagates_caller_id_to_subagent_info` — construct an AgentSubagentRunner with `.with_caller_id("parent_sub_abc".to_string())`, drive a `run_child` call (using whatever test scaffolding the file has — likely a mock AgentLoop or a constructor-only path), and inspect the resulting SubagentInfo in the registry to confirm `parent_id == Some("parent_sub_abc")`. If `run_child` requires too much scaffolding to drive in a unit test, EXTRACT a pure helper `fn build_subagent_info(subagent_id: String, task_summary: String, caller_subagent_id: Option<String>, ...) -> SubagentInfo` that contains the construction logic, call it directly in the test, and have the production `run_child` call this helper. Refactor only as needed to make the construction unit-testable.
  </behavior>
  <action>
    Edit `crates/ironhermes-agent/src/subagent_registry.rs`: add `pub struct SubagentTreeNode` with derive(Debug, Clone), implement `build_tree()` and the private `children_of()` helper following the PATTERNS.md section 7 code (sort by started_at). Override `tree_summary()` on `SubagentRegistryHandle` using the block_in_place pattern from `list_summary()` at lines 96-111; implement the `flatten_tree` recursive walker that produces depth-tagged `SubagentTreeEntry` records (import the type via `use ironhermes_core::commands::context::SubagentTreeEntry`). Populate `status` per node via the derivation rule in `<behavior>`: cancelled → `"killed"`, otherwise → `"running"`. Add the four new unit tests described in `<behavior>`. Edit `crates/ironhermes-agent/src/subagent_runner.rs`: change line ~182 from `parent_id: None` to `parent_id: self.caller_subagent_id.clone()` (the comment from Plan 03 is removed since the TODO is now closed). If the construction logic at line 179 is hard to drive in a unit test, extract it into a `fn build_subagent_info(...)` helper module-private function and call the helper from the test for the `test_run_child_propagates_caller_id_to_subagent_info` assertion. Update existing comments in subagent_runner.rs that reference `v2.1+ will thread parent id` to instead reference Plan 32.2-04.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-agent --lib subagent_registry::tests::test_build_tree_flat_when_no_parents subagent_registry::tests::test_build_tree_groups_by_parent subagent_registry::tests::test_build_tree_three_levels subagent_registry::tests::test_tree_summary_handle_flattens_with_depth 2>&1 | tail -20 && cargo test -p ironhermes-agent --lib subagent_runner::tests::test_run_child_propagates_caller_id_to_subagent_info 2>&1 | tail -15 && cargo build --workspace 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All five new tests pass (`test_build_tree_flat_when_no_parents`, `test_build_tree_groups_by_parent`, `test_build_tree_three_levels`, `test_tree_summary_handle_flattens_with_depth`, `test_run_child_propagates_caller_id_to_subagent_info`)
    - `cargo build --workspace 2>&1` exits 0
    - `grep -c "pub struct SubagentTreeNode" crates/ironhermes-agent/src/subagent_registry.rs` equals 1
    - `grep -c "pub fn build_tree" crates/ironhermes-agent/src/subagent_registry.rs` equals 1
    - `grep -c "fn children_of" crates/ironhermes-agent/src/subagent_registry.rs` equals 1
    - `grep -c "fn tree_summary" crates/ironhermes-agent/src/subagent_registry.rs` equals 1
    - `grep -c "parent_id: self\\.caller_subagent_id\\.clone()" crates/ironhermes-agent/src/subagent_runner.rs` equals 1
    - `grep -c "parent_id: None,.*v2\\.1" crates/ironhermes-agent/src/subagent_runner.rs` equals 0 (TODO removed)
    - `cargo test --workspace 2>&1 | tail -5` shows no test regressions
  </acceptance_criteria>
  <done>SubagentTreeNode exists; SubagentRegistry::build_tree returns nested trees sorted by started_at; SubagentRegistryHandle.tree_summary produces depth-tagged flat entries; SubagentInfo.parent_id is populated from the runner's caller_subagent_id; all new tests pass; workspace builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SubagentRegistry HashMap → external readers | tree_summary clones SubagentInfo data; cloned struct passes through trait boundary to consumers (handlers.rs, future iron_hermes_ui JSON serializer) |
| caller_subagent_id field write → SubagentInfo.parent_id | In-process trust — both ends are inside ironhermes-agent crate |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-32.2-13 | Tampering | low | parent_id values from arbitrary strings | mitigate | `caller_subagent_id` is produced internally by `random_subagent_id()` (uuid v4) — never user input. SubagentInfo.parent_id is therefore always a valid internal ID or None. |
| T-32.2-14 | Denial of Service | low | Cycle in parent_id graph (parent_id loop) | mitigate | `caller_subagent_id` is set ONCE at runner clone time and is immutable; the parent always exists before the child (registration order). build_tree's recursion has no cycle-detection because by construction the graph is a DAG (rooted tree). If a future refactor introduces dynamic parent_id reassignment, add a visited-set guard. |
| T-32.2-15 | Information Disclosure | low | tree_summary exposes task_summary across subtrees | accept | `task_summary` is already exposed via `list_summary` — no new disclosure surface. |
</threat_model>

<verification>
After both tasks complete:
- `cargo build --workspace 2>&1` exits 0
- `cargo test --workspace 2>&1 | tail -10` shows no regressions
- `cargo test -p ironhermes-agent --lib subagent_registry 2>&1` shows ALL tests passing (existing + 4 new)
- `cargo test -p ironhermes-agent --lib subagent_runner 2>&1` shows ALL tests passing (existing + new caller_id test)
- `cargo test -p ironhermes-core --lib commands::context 2>&1` shows ALL tests passing (existing + 1 new default-impl test)
</verification>

<success_criteria>
- SubagentTreeEntry + SubagentTreeNode types exist in their respective crates
- SubagentListSnapshot::tree_summary has a default flat impl that lets existing fakes compile unchanged
- SubagentRegistry::build_tree returns a stable, sorted-by-started_at recursive tree
- SubagentRegistryHandle overrides tree_summary with a depth-tagged flat impl
- SubagentInfo.parent_id is populated from AgentSubagentRunner.caller_subagent_id (no more None hardcode)
- All new tests pass; full workspace tests stay green
</success_criteria>

<output>
Create `.planning/phases/32.2-subagent-delegation-parity/32.2-04-SUMMARY.md` when done. Include:
- Whether the build_subagent_info helper extraction was required (and the resulting file diff)
- Stable ordering guarantee for trees (which field sorts, and what happens on ties)
- A small example: register a 3-node tree (root + 2 children), call tree_summary, paste the resulting Vec<SubagentTreeEntry>
</output>
