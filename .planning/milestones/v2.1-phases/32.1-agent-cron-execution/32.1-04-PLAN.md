---
phase: 32.1-agent-cron-execution
plan: 04
type: execute
wave: 2
depends_on: [32.1-01]
files_modified:
  - crates/ironhermes-cron/src/delivery.rs
autonomous: true
requirements:
  - CRON-DELIVERY-MULTI-TARGET
  - CRON-DELIVERY-ALLOWLIST
  - CRON-DELIVERY-HOME-CHANNEL
  - CRON-DELIVERY-LEGACY-ENV
  - CRON-DELIVERY-THREAD-ENV
  - CRON-DELIVERY-ORIGIN-FALLBACK
  - CRON-DELIVERY-FSYNC-CHMOD
tags: [cron, delivery, multi-target, allowlist]

must_haves:
  truths:
    - "`pub const KNOWN_DELIVERY_PLATFORMS: &[&str]` lists `telegram`, `discord`, `slack`, `matrix`, `whatsapp`, `webhook`, `qq` — every other platform token is rejected (no env-var lookup)"
    - "`resolve_delivery_targets(job) -> Vec<DeliveryTarget>` returns all targets implied by the job's `deliver` field — comma-split, `all` expansion, platform-without-chat_id home-channel lookup, `origin` fallback, deduplication"
    - "The existing `resolve_delivery_target(job) -> Option<DeliveryTarget>` (singular) is preserved (deprecated) for transitional compatibility; documented to delegate to `resolve_delivery_targets(job).into_iter().next()`"
    - "`deliver = \"all\"` expands to every `KNOWN_DELIVERY_PLATFORMS` entry that has a non-empty `<PLATFORM>_HOME_CHANNEL` env var (or legacy fallback) configured; platforms without a configured home channel are silently skipped with a `tracing::warn!`"
    - "`deliver = \"qq\"` falls back to `QQBOT_HOME_CHANNEL` if `QQ_HOME_CHANNEL` is unset (one legacy alias documented in Python parity)"
    - "`deliver = \"origin\"` with no `job.origin` falls back to the first configured `<PLATFORM>_HOME_CHANNEL` from `KNOWN_DELIVERY_PLATFORMS` iteration order, matching Python's `_resolve_single_delivery_target`"
    - "Each `DeliveryTarget` produced via a home-channel lookup populates its `thread_id` from `<PLATFORM>_HOME_CHANNEL_THREAD_ID` when present"
    - "Tokens not in `KNOWN_DELIVERY_PLATFORMS` (e.g. `\"reddit\"`, `\"../etc/passwd\"`) NEVER trigger any env-var read; they produce no target and emit a single `tracing::warn!` per token"
    - "`save_job_output(...)` calls `f.sync_all()?` after `f.flush()?` and applies `chmod 0600` (Unix) to the resulting `.md` file"
  artifacts:
    - path: "crates/ironhermes-cron/src/delivery.rs"
      provides: "`KNOWN_DELIVERY_PLATFORMS`, `resolve_delivery_targets`, env-var tables, fsync/chmod in `save_job_output`"
      contains: "pub fn resolve_delivery_targets"
  key_links:
    - from: "crates/ironhermes-cron/src/delivery.rs::resolve_delivery_targets"
      to: "crates/ironhermes-cron/src/job.rs::CronJob"
      via: "reads job.deliver and job.origin"
      pattern: "job\\.deliver"
    - from: "crates/ironhermes-cron/src/delivery.rs::expand_routing_token"
      to: "process env vars (TELEGRAM_HOME_CHANNEL etc.)"
      via: "std::env::var on allowlisted platform tokens only"
      pattern: "std::env::var"
---

<objective>
Close the PARITY.md §11 incidental gaps that live in the cron crate's
delivery module: multi-target routing (#8), `all` token + comma-split (#9),
platform-without-chat_id home-channel lookup (#10), origin-fallback (#11),
allowlist (#12), legacy env var fallback (#13), thread-id env var (#14),
plus the `save_job_output` fsync (#18 — same-file extension of Plan 03's
work) and `chmod 0600` (#15).

The new `resolve_delivery_targets` (plural) returns `Vec<DeliveryTarget>`;
the old singular `resolve_delivery_target` is preserved for any remaining
caller during transition. The cron-runner crate (Plan 05–06) will be the
primary consumer of the new plural form. The CLI cron-status command may
continue to call the singular form until a later cleanup.

Purpose: every delivery decision in the new runner (Plan 06) reads from
this module. Landing the routing primitives in Wave 2 (parallel with
Plan 03's storage parity) keeps the runner crate's dependency edges to
a single wave below.

Output: an extended `delivery.rs` with the allowlist constant, the plural
resolver, env-var lookup helpers, and hardened `save_job_output`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md
@.planning/phases/32.1-agent-cron-execution/32.1-RESEARCH.md
@.planning/phases/32.1-agent-cron-execution/32.1-PATTERNS.md
@.planning/phases/32.1-agent-cron-execution/32.1-01-SUMMARY.md
@crates/ironhermes-cron/src/delivery.rs
@crates/ironhermes-cron/src/job.rs
@crates/ironhermes-cron/PARITY.md

<interfaces>
<!-- Existing delivery.rs API (from grep) -->
- pub struct DeliveryTarget { platform: String, chat_id: String, thread_id: Option<String>, ... } at line 15
- pub fn resolve_delivery_target(job: &CronJob) -> Option<DeliveryTarget> at line 31
- pub fn is_silent(output: &str) -> bool at line 62
- pub fn save_job_output(job_id: &str, output: &str) -> Result<PathBuf> at line 73
- pub fn format_delivery_message(job_name: &str, output: &str) -> String at line 117

<!-- New surface this plan adds -->
- pub const KNOWN_DELIVERY_PLATFORMS: &[&str] = &["telegram", "discord", "slack", "matrix", "whatsapp", "webhook", "qq"];
- pub fn resolve_delivery_targets(job: &CronJob) -> Vec<DeliveryTarget>
- fn expand_routing_token(token: &str, job: &CronJob) -> Vec<DeliveryTarget>  (private)
- fn home_channel_env_var(platform: &str) -> Option<&'static str>             (private)
- fn legacy_home_channel_env_var(platform: &str) -> Option<&'static str>      (private)
- fn home_channel_thread_env_var(platform: &str) -> Option<&'static str>      (private)
- fn lookup_home_channel(platform: &str) -> Option<DeliveryTarget>            (private)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add allowlist, env-var tables, helpers, and resolve_delivery_targets plural API</name>
  <files>crates/ironhermes-cron/src/delivery.rs</files>
  <read_first>
    - crates/ironhermes-cron/src/delivery.rs (entire file — DeliveryTarget struct, resolve_delivery_target, is_silent, save_job_output, format_delivery_message, the make_job test fixture at lines 144–168)
    - crates/ironhermes-cron/src/job.rs (CronJob.deliver: String, CronJob.origin: Option<JobOrigin>, JobOrigin shape — Plan 01 added the lenient origin deserializer)
    - crates/ironhermes-cron/PARITY.md §7 (delivery routing table — single source of truth for the env var names, legacy fallback, and `all` expansion semantics)
    - hermes-agent/cron/scheduler.py (`_resolve_delivery_target`, `_resolve_delivery_targets`, `_resolve_single_delivery_target`, `_expand_routing_tokens`, `_HOME_TARGET_ENV_VARS`, `_LEGACY_HOME_TARGET_ENV_VARS`, `_KNOWN_DELIVERY_PLATFORMS`)
    - .planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md (§Delivery routing — locks the seven rules)
    - .planning/phases/32.1-agent-cron-execution/32.1-PATTERNS.md (§ironhermes-cron/src/delivery.rs Multi-target loop + KNOWN_DELIVERY_PLATFORMS lines 451–471)
  </read_first>
  <behavior>
    - Test 1 (local): `resolve_delivery_targets(&job_with_deliver("local"))` returns `Vec::new()`.
    - Test 2 (single platform with chat_id): `deliver = "telegram:123"` returns one `DeliveryTarget { platform: "telegram", chat_id: "123", thread_id: None }`.
    - Test 3 (multi-target comma split): `deliver = "telegram:123,discord:abc"` returns two targets, one per platform, with the correct chat_ids; order is preserved.
    - Test 4 (`all` token, env vars set): Set `TELEGRAM_HOME_CHANNEL=tg1` and `DISCORD_HOME_CHANNEL=ds1`; `deliver = "all"` returns at least two targets, one for `telegram` (chat_id=`tg1`) and one for `discord` (chat_id=`ds1`); platforms WITHOUT a home channel set are silently absent. Use `std::env::set_var` via the project's existing test-env-mutex pattern from `crates/ironhermes-core/src/ssrf.rs` or wherever the project's test env-lock idiom lives (Phase 21.6 used `OnceLock<Mutex<()>>`).
    - Test 5 (platform without colon → home channel): With `TELEGRAM_HOME_CHANNEL=tg-home`, `deliver = "telegram"` returns one target `{platform:"telegram", chat_id:"tg-home", thread_id: None}`.
    - Test 6 (platform without colon, home unset): With no `TELEGRAM_HOME_CHANNEL`, `deliver = "telegram"` returns `Vec::new()` AND emits a `tracing::warn!` containing `telegram`.
    - Test 7 (legacy env var, qq): With `QQBOT_HOME_CHANNEL=qq-legacy` and `QQ_HOME_CHANNEL` unset, `deliver = "qq"` returns one target with `chat_id == "qq-legacy"`.
    - Test 8 (legacy env var, qq, precedence): With BOTH `QQ_HOME_CHANNEL=qq-new` AND `QQBOT_HOME_CHANNEL=qq-legacy` set, `deliver = "qq"` returns one target with `chat_id == "qq-new"` (primary wins).
    - Test 9 (thread id env var): With `TELEGRAM_HOME_CHANNEL=tg1` and `TELEGRAM_HOME_CHANNEL_THREAD_ID=42`, `deliver = "telegram"` returns one target with `thread_id == Some("42".into())`.
    - Test 10 (`deliver = origin` with origin): A job with `deliver="origin"` and `origin = Some(JobOrigin{platform:"telegram", chat_id:"orig-1", ...})` returns one target matching origin.
    - Test 11 (`deliver = origin` without origin, fallback): A job with `deliver="origin"` and `origin = None`, but with `TELEGRAM_HOME_CHANNEL=tg1` set, returns one target with `platform="telegram"`, `chat_id="tg1"`.
    - Test 12 (`deliver = origin` without origin, no fallback): Same as Test 11 but with no platform env vars set. Returns `Vec::new()` and emits a single `tracing::warn!`.
    - Test 13 (allowlist gate): `deliver = "reddit"` (not in `KNOWN_DELIVERY_PLATFORMS`) returns `Vec::new()`. NO env-var lookup occurs — verified by setting a fake `REDDIT_HOME_CHANNEL=should-not-be-read` and asserting the returned vec is empty (proves the allowlist short-circuits before any `std::env::var` call).
    - Test 14 (allowlist with traversal-shaped token): `deliver = "../etc/passwd"` returns `Vec::new()` with no panic and no env-var read.
    - Test 15 (deduplication): `deliver = "telegram:123,telegram:123"` returns exactly ONE target (duplicates suppressed). Implementation can use an `IndexSet`-like manual dedup loop or `Vec::dedup_by` after a stable sort.
    - Test 16 (singular delegates to plural): `resolve_delivery_target(&job)` returns the FIRST element of `resolve_delivery_targets(&job)`, or `None` if the plural form returned empty — verified on a comma-split job that yields ≥ 2 targets.
  </behavior>
  <action>
    In `crates/ironhermes-cron/src/delivery.rs`, near the top of the file
    (just below the existing `use` statements and `DeliveryTarget` struct):

    (a) Add the public allowlist constant exactly as locked in CONTEXT.md:

        pub const KNOWN_DELIVERY_PLATFORMS: &[&str] =
            &["telegram", "discord", "slack", "matrix", "whatsapp", "webhook", "qq"];

    (b) Add three private env-var-mapping helpers:

        fn home_channel_env_var(platform: &str) -> Option<&'static str> {
            match platform {
                "telegram"  => Some("TELEGRAM_HOME_CHANNEL"),
                "discord"   => Some("DISCORD_HOME_CHANNEL"),
                "slack"     => Some("SLACK_HOME_CHANNEL"),
                "matrix"    => Some("MATRIX_HOME_CHANNEL"),
                "whatsapp"  => Some("WHATSAPP_HOME_CHANNEL"),
                "webhook"   => Some("WEBHOOK_HOME_CHANNEL"),
                "qq"        => Some("QQ_HOME_CHANNEL"),
                _           => None,
            }
        }

        fn legacy_home_channel_env_var(platform: &str) -> Option<&'static str> {
            match platform {
                "qq" => Some("QQBOT_HOME_CHANNEL"),
                _    => None,
            }
        }

        fn home_channel_thread_env_var(platform: &str) -> Option<&'static str> {
            match platform {
                "telegram"  => Some("TELEGRAM_HOME_CHANNEL_THREAD_ID"),
                "discord"   => Some("DISCORD_HOME_CHANNEL_THREAD_ID"),
                "slack"     => Some("SLACK_HOME_CHANNEL_THREAD_ID"),
                "matrix"    => Some("MATRIX_HOME_CHANNEL_THREAD_ID"),
                "whatsapp"  => Some("WHATSAPP_HOME_CHANNEL_THREAD_ID"),
                "webhook"   => Some("WEBHOOK_HOME_CHANNEL_THREAD_ID"),
                "qq"        => Some("QQ_HOME_CHANNEL_THREAD_ID"),
                _           => None,
            }
        }

    (c) Add a `lookup_home_channel` helper that returns `Option<DeliveryTarget>`
    by combining the primary env var, legacy fallback, and thread-id env var:

        fn lookup_home_channel(platform: &str) -> Option<DeliveryTarget> {
            // Allowlist gate FIRST — no env var read for unknown platforms
            if !KNOWN_DELIVERY_PLATFORMS.contains(&platform) {
                return None;
            }
            let chat_id = home_channel_env_var(platform)
                .and_then(|v| std::env::var(v).ok())
                .filter(|s| !s.is_empty())
                .or_else(|| {
                    legacy_home_channel_env_var(platform)
                        .and_then(|v| std::env::var(v).ok())
                        .filter(|s| !s.is_empty())
                })?;
            let thread_id = home_channel_thread_env_var(platform)
                .and_then(|v| std::env::var(v).ok())
                .filter(|s| !s.is_empty());
            Some(DeliveryTarget {
                platform: platform.to_string(),
                chat_id,
                thread_id,
                ..Default::default()  // if other fields exist on DeliveryTarget; use struct init otherwise
            })
        }

    NOTE: `DeliveryTarget` may not derive `Default` today. Verify via
    `grep -nE "^#\\[derive.*DeliveryTarget\\]|^pub struct DeliveryTarget"
    crates/ironhermes-cron/src/delivery.rs`. If no Default derive, construct
    the struct explicitly with every field (probably 3 fields per current
    grep). Do NOT add a Default derive in this plan — that's an API change
    outside scope.

    (d) Add `expand_routing_token(token: &str, job: &CronJob) -> Vec<DeliveryTarget>`:

        fn expand_routing_token(token: &str, job: &CronJob) -> Vec<DeliveryTarget> {
            let token = token.trim();
            if token.is_empty() { return Vec::new(); }

            // `all` expands to every configured home channel
            if token.eq_ignore_ascii_case("all") {
                return KNOWN_DELIVERY_PLATFORMS
                    .iter()
                    .filter_map(|p| lookup_home_channel(p))
                    .collect();
            }

            // `origin` resolves to job.origin, or falls back to any configured home channel
            if token.eq_ignore_ascii_case("origin") {
                if let Some(origin) = &job.origin {
                    if KNOWN_DELIVERY_PLATFORMS.contains(&origin.platform.as_str()) {
                        return vec![DeliveryTarget {
                            platform: origin.platform.clone(),
                            chat_id: origin.chat_id.clone(),
                            thread_id: origin.thread_id.clone(),
                        }];
                    } else {
                        tracing::warn!(platform=%origin.platform, "origin platform not in KNOWN_DELIVERY_PLATFORMS — skipping");
                        return Vec::new();
                    }
                }
                // No origin — fallback to first configured home channel
                for p in KNOWN_DELIVERY_PLATFORMS {
                    if let Some(target) = lookup_home_channel(p) {
                        return vec![target];
                    }
                }
                tracing::warn!("deliver=origin has no origin and no home channel configured");
                return Vec::new();
            }

            // `platform:chat_id` form
            if let Some((platform, chat_id)) = token.split_once(':') {
                let platform = platform.trim();
                let chat_id  = chat_id.trim();
                if !KNOWN_DELIVERY_PLATFORMS.contains(&platform) {
                    tracing::warn!(platform=%platform, "deliver platform not in allowlist — skipping");
                    return Vec::new();
                }
                let thread_id = home_channel_thread_env_var(platform)
                    .and_then(|v| std::env::var(v).ok())
                    .filter(|s| !s.is_empty());
                return vec![DeliveryTarget {
                    platform: platform.to_string(),
                    chat_id: chat_id.to_string(),
                    thread_id,
                }];
            }

            // Bare `platform` without colon → home channel
            if !KNOWN_DELIVERY_PLATFORMS.contains(&token) {
                tracing::warn!(token=%token, "deliver token not in allowlist — skipping");
                return Vec::new();
            }
            match lookup_home_channel(token) {
                Some(t) => vec![t],
                None => {
                    tracing::warn!(platform=%token, "deliver={token} has no home channel configured");
                    Vec::new()
                }
            }
        }

    (e) Add the public plural API:

        pub fn resolve_delivery_targets(job: &CronJob) -> Vec<DeliveryTarget> {
            let deliver = job.deliver.trim();
            if deliver.is_empty() || deliver.eq_ignore_ascii_case("local") {
                return Vec::new();
            }

            let mut out: Vec<DeliveryTarget> = Vec::new();
            for part in deliver.split(',') {
                for target in expand_routing_token(part, job) {
                    // dedup against already-seen targets
                    let already = out.iter().any(|t|
                        t.platform == target.platform &&
                        t.chat_id  == target.chat_id  &&
                        t.thread_id == target.thread_id
                    );
                    if !already {
                        out.push(target);
                    }
                }
            }
            out
        }

    (f) Update the existing `resolve_delivery_target` (singular) at line 31
    to delegate to the plural form (mark with a doc comment that singular is
    deprecated in favor of plural):

        /// **Deprecated**: use [`resolve_delivery_targets`] (plural) for full
        /// multi-target parity (comma-split, `all`, home-channel lookup).
        /// This single-target form returns the first plural result.
        pub fn resolve_delivery_target(job: &CronJob) -> Option<DeliveryTarget> {
            resolve_delivery_targets(job).into_iter().next()
        }

    Existing callers (tick.rs `complete_job_run`, gateway runner.rs
    `dispatch_delivery`) continue to compile — only the implementation
    changed, not the signature. They now get whichever target comes first
    in the plural list. Plan 06 / Plan 07 will migrate them to the plural
    form explicitly.

    (g) Tests: extend the existing test module in delivery.rs (or add
    `mod multi_target_tests { ... }`) covering all 16 behaviors. Use the
    existing `make_job` fixture (Plan 01 updated it for the new
    `CronJob` fields). Env-var mutating tests MUST acquire a process-wide
    `OnceLock<Mutex<()>>` lock to avoid racing other tests in the crate
    that read process env. Pattern: copy from any existing env-mutating
    test in `crates/ironhermes-core` (search via
    `grep -rn "OnceLock<Mutex<()>>" crates/`).
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cron --lib multi_target_tests -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `cargo test -p ironhermes-cron --lib multi_target_tests -- --test-threads=1` exits 0 (single-threaded to keep env mutations deterministic)
    - source assertion: `grep -n "pub const KNOWN_DELIVERY_PLATFORMS" crates/ironhermes-cron/src/delivery.rs` matches exactly 1 line
    - source assertion: the allowlist contains exactly seven entries — verified by `grep -oE "\\\"[a-z]+\\\"" crates/ironhermes-cron/src/delivery.rs | head -7` returning telegram, discord, slack, matrix, whatsapp, webhook, qq (or use a stronger pattern: `awk '/KNOWN_DELIVERY_PLATFORMS/,/];/' crates/ironhermes-cron/src/delivery.rs | grep -oE "\\\"[a-z]+\\\"" | wc -l` returns 7)
    - source assertion: `grep -n "pub fn resolve_delivery_targets" crates/ironhermes-cron/src/delivery.rs` matches exactly 1 line
    - source assertion: `grep -n "pub fn resolve_delivery_target\\(" crates/ironhermes-cron/src/delivery.rs` matches exactly 1 line (singular still present)
    - source assertion: `grep -n "deprecated" crates/ironhermes-cron/src/delivery.rs` matches at least 1 line (singular form documented as deprecated)
    - source assertion: `grep -nE "fn (home_channel_env_var|legacy_home_channel_env_var|home_channel_thread_env_var|lookup_home_channel|expand_routing_token)" crates/ironhermes-cron/src/delivery.rs` returns 5 lines
    - source assertion: `grep -n "QQBOT_HOME_CHANNEL" crates/ironhermes-cron/src/delivery.rs` matches at least 1 line (legacy fallback)
    - source assertion: `grep -nE "QQBOT_HOME_CHANNEL|TELEGRAM_HOME_CHANNEL|DISCORD_HOME_CHANNEL|SLACK_HOME_CHANNEL|MATRIX_HOME_CHANNEL|WHATSAPP_HOME_CHANNEL|WEBHOOK_HOME_CHANNEL|QQ_HOME_CHANNEL" crates/ironhermes-cron/src/delivery.rs | wc -l` is at least 8
    - source assertion: the allowlist gate runs BEFORE any `std::env::var` call inside `expand_routing_token` — verified by `awk '/fn expand_routing_token/,/^}/' crates/ironhermes-cron/src/delivery.rs | grep -nE "KNOWN_DELIVERY_PLATFORMS|std::env::var" | head -1` containing `KNOWN_DELIVERY_PLATFORMS` (not `std::env::var`)
    - test-command: `cargo test -p ironhermes-cron -- --test-threads=1` exits 0 (full crate, env-mutating tests respected)
    - test-command: `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>The cron crate's delivery module exposes a 7-entry allowlist constant, a multi-target plural resolver with comma/`all`/`origin`/bare-platform expansion, dedupe, legacy env-var fallback for `qq`, and thread-id env var support. The singular resolver delegates to the plural form.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden save_job_output with fsync + Unix chmod 0600</name>
  <files>crates/ironhermes-cron/src/delivery.rs</files>
  <read_first>
    - crates/ironhermes-cron/src/delivery.rs lines 73–115 (the `save_job_output` body)
    - 32.1-03-SUMMARY.md (the exact pattern Plan 03 used for `JobStore::save` fsync + chmod — copy verbatim for symmetry)
    - crates/ironhermes-cron/src/lib.rs lines 139–145 (`#[cfg(unix)]` style reference)
  </read_first>
  <behavior>
    - Test 1 (fsync — source-grep): `grep -nE "f\\.sync_all\\(\\)" crates/ironhermes-cron/src/delivery.rs` matches at least 1 line within the `save_job_output` body.
    - Test 2 (Unix chmod 0600): After calling `save_job_output("test-id", "hello")` on a tempdir-backed `HERMES_HOME`, the returned `.md` file has mode `0o600` (Unix only). Test gated by `#[cfg(unix)]`. Use `HERMES_HOME` env var override pattern (already used by other env-mutating tests in the crate) to keep the test hermetic. Acquire the same OnceLock<Mutex<()>> env lock used in Task 1.
    - Test 3 (output dir chmod 0700): The parent directory `<HERMES_HOME>/cron/output/<job_id>/` has mode `0o700` after the same call (Unix only).
    - Test 4 (regression — atomic rename still works): The function still returns `Ok(path)` and `path` exists with the correct content.
  </behavior>
  <action>
    In `save_job_output`:

    (a) Locate the existing `f.flush()?` call at approximately line 94 (verify
    via `grep -n flush crates/ironhermes-cron/src/delivery.rs`). Add a
    `f.sync_all()?;` immediately after it (the same pattern Plan 03 used in
    `JobStore::save`).

    (b) After the `fs::create_dir_all(&job_dir)?` (or equivalent) that creates
    the per-job output directory, add a `#[cfg(unix)]` block setting that
    directory's mode to `0o700`. Silent on error.

    (c) After the atomic `fs::rename(&tmp, &final_path)?`, add a
    `#[cfg(unix)]` block setting `final_path`'s mode to `0o600`. Silent on
    error.

    (d) Add `#[cfg(test)] mod save_job_output_tests_phase_32_1` covering the
    four behaviors. The test sets `HERMES_HOME` to a `tempfile::TempDir`,
    acquires the env lock, calls `save_job_output`, then reads back the
    file permissions and contents. On non-Unix the chmod tests are
    automatically skipped via `#[cfg(unix)]`.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cron --lib save_job_output_tests_phase_32_1 -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `cargo test -p ironhermes-cron --lib save_job_output_tests_phase_32_1 -- --test-threads=1` exits 0
    - source assertion: `awk '/pub fn save_job_output/,/^}/' crates/ironhermes-cron/src/delivery.rs | grep -c "sync_all"` returns at least 1
    - source assertion: `awk '/pub fn save_job_output/,/^}/' crates/ironhermes-cron/src/delivery.rs | grep -c "from_mode(0o600)"` returns at least 1
    - source assertion: `awk '/pub fn save_job_output/,/^}/' crates/ironhermes-cron/src/delivery.rs | grep -c "from_mode(0o700)"` returns at least 1
    - test-command: `cargo test -p ironhermes-cron -- --test-threads=1` exits 0
  </acceptance_criteria>
  <done>`save_job_output` flushes-and-syncs before atomic rename and applies `chmod 0700` to the per-job output dir and `chmod 0600` to the resulting `.md` file on Unix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `job.deliver` field on disk → routing decision | An attacker who can edit `jobs.json` could craft `deliver` values designed to enumerate env vars (e.g., `deliver = "stripe_secret"`) — the allowlist closes this |
| Output files in `~/.hermes/cron/output/` on Unix | Other users on the same host should not be able to read job outputs — chmod 0600 closes this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32.1.04-01 | Information Disclosure | Env-var enumeration via crafted `deliver` token | mitigate | `KNOWN_DELIVERY_PLATFORMS` allowlist runs as the FIRST check in every routing branch (`expand_routing_token` and `lookup_home_channel`). No env-var read occurs for unknown tokens. Verified by Test 13 (sets `REDDIT_HOME_CHANNEL` and confirms it is never read). |
| T-32.1.04-02 | Information Disclosure | Other Unix users reading `~/.hermes/cron/output/*.md` | mitigate | `chmod 0700` on output dir, `chmod 0600` on each `.md` file. Best-effort on Windows. |
| T-32.1.04-03 | Denial of Service | Pathological `deliver` value like 10000 comma-separated tokens | accept | Each token is bounded constant-work via the allowlist; the dedupe loop is O(n²) but n is bounded by `KNOWN_DELIVERY_PLATFORMS.len() × tokens` and in practice is tiny. No mitigation needed. |
| T-32.1.04-04 | Tampering | A malicious `JobOrigin.platform` not in allowlist | mitigate | `expand_routing_token`'s `origin` branch validates `origin.platform` against `KNOWN_DELIVERY_PLATFORMS` before constructing the target; non-allowlisted origin platforms are skipped with a warn. |
| T-32.1.04-05 | Denial of Service | Crash window between flush and rename on `save_job_output` | mitigate | `f.sync_all()` between flush and rename closes the durability gap (same as PARITY.md §11.18). |
| T-32.1.04-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new crates. All work uses existing `tracing`, `serde`, `std::env`, `std::os::unix::fs::PermissionsExt`. |
</threat_model>

<verification>
- `cargo test -p ironhermes-cron -- --test-threads=1` is green
- `cargo build --workspace` is green
- PARITY.md §11 gaps #8, #9, #10, #11, #12, #13, #14, #15 (output chmod portion), #18 (delivery.rs portion) closed
- The singular `resolve_delivery_target` still exists, delegates to plural, marked deprecated in source comments
</verification>

<success_criteria>
- 7-entry allowlist hardcoded; tokens outside it never trigger env reads
- Multi-target routing supports comma split, `all`, `origin` (with fallback), bare platform (home channel), platform:chat_id
- Legacy `QQBOT_HOME_CHANNEL` falls back when `QQ_HOME_CHANNEL` is unset; primary takes precedence when both set
- Thread-id env vars populate `DeliveryTarget.thread_id` on home-channel lookups
- `save_job_output` calls `sync_all` and applies Unix 0600 / 0700 permissions
- 16+ unit tests cover routing matrix; env mutation tests serialized with a single-threaded test runner flag
</success_criteria>

<output>
Create `.planning/phases/32.1-agent-cron-execution/32.1-04-SUMMARY.md` when done. Record:
- The exact env-mutex pattern used (which crate it copied from)
- Any platform-table additions beyond the seven CONTEXT.md-locked entries (none expected; flag if any)
- Whether `DeliveryTarget` already had `Default` or whether explicit struct init was needed
- Plan 06 hand-off: confirm that `resolve_delivery_targets` is the canonical entry point for the runner crate's per-target dispatch loop
</output>
</content>
</invoke>