---
phase: 32.1-agent-cron-execution
plan: 09
type: execute
wave: 6
depends_on: []
files_modified:
  - crates/ironhermes-cron/src/delivery.rs
autonomous: true
gap_closure: true
requirements:
  - GAP-32.1-TG-WHITELIST-FALLBACK
tags: [cron, gap-closure, delivery, telegram, regression]

must_haves:
  truths:
    - "A cron job with `deliver = \"telegram\"` (bare token, no chat_id) resolves to the single entry of `~/.ironhermes/config.yaml::gateway.platforms.telegram.whitelist` when `TELEGRAM_HOME_CHANNEL` is unset AND the whitelist has exactly one authorized chat"
    - "When `TELEGRAM_HOME_CHANNEL` IS set, the env var ALWAYS wins — the config.yaml fallback is never consulted (env var preserves existing pre-32.1 precedence and the documented Plan 04 contract)"
    - "When the whitelist has 0 or >1 entries, behavior is unchanged: bare `deliver=telegram` with no env var returns `Vec::new()` and emits the existing `tracing::warn!` (consistent with the OriginDecision::Multi / OriginDecision::None branches in `Config::telegram_default_origin`)"
    - "The config fallback applies ONLY to the `telegram` platform — other platforms (`discord`, `slack`, etc.) still use env-var-only home-channel resolution because `Config::gateway.platforms` only carries Telegram-shaped whitelist semantics today"
    - "The fallback runs INSIDE `lookup_home_channel` (the existing function) and is gated by the `KNOWN_DELIVERY_PLATFORMS` allowlist check that already runs first — no behavior change for unknown platform tokens"
    - "All existing `ironhermes-cron` library tests (currently 105+) continue to pass; the new fallback adds new tests but breaks none"
    - "The `cron create --deliver` CLI help text comment in `crates/ironhermes-cli/src/cron.rs` lines 36-39 (`'defaults to origin routing to the configured Telegram chat if the gateway has exactly one authorized chat in config.yaml's whitelist'`) is once again accurate — observable by inspection of the runtime behavior matching the docstring"
  artifacts:
    - path: "crates/ironhermes-cron/src/delivery.rs"
      provides: "lookup_home_channel now falls back to Config::gateway.platforms.telegram.whitelist when platform == \"telegram\" and the env var lookup yields nothing"
      contains: "fn lookup_telegram_whitelist_fallback"
  key_links:
    - from: "crates/ironhermes-cron/src/delivery.rs::lookup_home_channel"
      to: "crates/ironhermes-core/src/config.rs::Config::load"
      via: "Config::load() inside the telegram-only fallback branch"
      pattern: "Config::load"
    - from: "crates/ironhermes-cron/src/delivery.rs::lookup_home_channel"
      to: "Config::gateway.platforms.get(\"telegram\").whitelist"
      via: "reads single-entry whitelist when env var is unset"
      pattern: "gateway\\.platforms\\.get"
---

<objective>
Close the behavior regression recorded in `32.1-HUMAN-UAT.md` §Gaps Test 5: `resolve_delivery_targets` (Plan 04, `crates/ironhermes-cron/src/delivery.rs`) lost the pre-32.1 gateway behavior of using `config.yaml::gateway.platforms.telegram.whitelist`'s single entry as the implicit home channel for `deliver=telegram` when `TELEGRAM_HOME_CHANNEL` is unset.

The `cron create --deliver` CLI help text still documents this fallback ("defaults to 'origin' routing to the configured Telegram chat if the gateway has exactly one authorized chat in config.yaml's whitelist"), and `Config::telegram_default_origin` (used by `resolve_cron_deliver` at job-creation time) still honors it — but the *runtime delivery resolution* lost it during the Plan 04 multi-target refactor.

UAT observed: `deliver=telegram` with config.yaml whitelist configured but `TELEGRAM_HOME_CHANNEL` unset emits `WARN deliver=telegram has no home channel configured platform=telegram` and no delivery occurs. Workaround: `export TELEGRAM_HOME_CHANNEL=<id>`.

Fix: in `lookup_home_channel` (the existing private helper at `delivery.rs:77-98`), after the env-var lookup returns `None` for the telegram platform specifically, fall back to `Config::load()?.gateway.platforms.get("telegram").whitelist` and use the single whitelist entry when `whitelist.len() == 1`.

Purpose: restore pre-32.1 parity for the most common operator setup — a single-user Telegram bot configured exclusively via `config.yaml` (no env vars). The env-var precedence is preserved, so any operator who DID set `TELEGRAM_HOME_CHANNEL` continues to get that value.

Output: extended `lookup_home_channel` with a telegram-only config fallback, new unit tests covering both fallback paths and the env-var-precedence path, all existing tests still green.

**Dependency note: NO new crate dep.** `ironhermes-cron/Cargo.toml` already lists `ironhermes-core = { path = "../ironhermes-core" }` (used today via `ironhermes_core::get_hermes_home` in `delivery.rs:253` and `store.rs:3` and `lib.rs:2`). `Config` is re-exported from `ironhermes-core::config`. No cycle risk — verified by inspection of the dep graph.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md
@.planning/phases/32.1-agent-cron-execution/32.1-HUMAN-UAT.md
@.planning/phases/32.1-agent-cron-execution/32.1-04-SUMMARY.md
@crates/ironhermes-cron/src/delivery.rs
@crates/ironhermes-core/src/config.rs
@crates/ironhermes-cron/Cargo.toml

<interfaces>
<!-- Existing API in delivery.rs that we are extending -->
fn lookup_home_channel(platform: &str) -> Option<DeliveryTarget>   // private, delivery.rs:77
pub fn resolve_delivery_targets(job: &CronJob) -> Vec<DeliveryTarget>   // delivery.rs:190
pub const KNOWN_DELIVERY_PLATFORMS: &[&str] = &["telegram", "discord", "slack", "matrix", "whatsapp", "webhook", "qq"];   // delivery.rs:30

<!-- The Config surface that already exists in ironhermes-core -->
// crates/ironhermes-core/src/config.rs:1018-1035
pub fn telegram_default_origin(&self) -> OriginDecision {
    let Some(tg) = self.gateway.platforms.get("telegram") else { return OriginDecision::None; };
    if !tg.enabled { return OriginDecision::None; }
    match tg.whitelist.len() {
        0 => OriginDecision::None,
        1 => OriginDecision::Single { platform: "telegram".to_string(), chat_id: tg.whitelist[0].to_string() },
        _ => OriginDecision::Multi { whitelist: tg.whitelist.iter().map(|id| id.to_string()).collect() },
    }
}

<!-- Construction precedence — env var ALWAYS wins -->
// Plan 04 lookup_home_channel: env var primary, legacy env var secondary, no config fallback.
// This plan inserts a tertiary fallback (telegram-only) AFTER the legacy env var branch.

<!-- ironhermes-cron already depends on ironhermes-core — verified -->
// crates/ironhermes-cron/Cargo.toml:
//   ironhermes-core = { path = "../ironhermes-core" }
// No new dep needed; no cycle (ironhermes-core does NOT depend on ironhermes-cron).
</interfaces>

<grep_audit>
Pre-implementation audit:
```
grep -rn "ironhermes-cron" crates/ironhermes-core/Cargo.toml   # MUST return 0 matches (no reverse dep → no cycle)
grep -n "ironhermes-core" crates/ironhermes-cron/Cargo.toml    # MUST return ≥ 1 match (existing dep)
grep -n "gateway.platforms.get" crates/ironhermes-core/src/config.rs   # confirm the accessor shape
```

Post-implementation audit:
```
grep -n "telegram_whitelist_fallback\|whitelist\[0\]\|whitelist.len() == 1" crates/ironhermes-cron/src/delivery.rs
   # MUST match ≥ 1 line — the fallback is implemented
grep -c "Config::load" crates/ironhermes-cron/src/delivery.rs
   # MUST be ≥ 1 (the fallback calls Config::load)
```
</grep_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add telegram-only config.yaml whitelist fallback in lookup_home_channel</name>
  <files>crates/ironhermes-cron/src/delivery.rs</files>
  <read_first>
    - crates/ironhermes-cron/src/delivery.rs (entire file — focus on lines 30 (KNOWN_DELIVERY_PLATFORMS), 37-68 (env-var tables), 77-98 (lookup_home_channel — the function being extended), 104-172 (expand_routing_token), 190-211 (resolve_delivery_targets), 326-908 (existing test modules — especially `multi_target_tests` for the env-mutex pattern and fixture shape)
    - crates/ironhermes-core/src/config.rs lines 928-1035 (OriginDecision enum + telegram_default_origin method — the canonical shape of the same access pattern used elsewhere in the workspace)
    - crates/ironhermes-cron/Cargo.toml (confirm `ironhermes-core` dep already present — no Cargo.toml edit required)
    - .planning/phases/32.1-agent-cron-execution/32.1-HUMAN-UAT.md §Gaps "Regression: telegram home-channel resolution lost the config.yaml whitelist fallback" (the canonical gap definition)
    - .planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md §Delivery routing (the seven locked rules — none of them forbid a config fallback, but the rules say env var is the primary lookup; this plan adds a tertiary fallback after legacy env var)
  </read_first>
  <behavior>
    Twelve test cases. The env-mutating tests MUST acquire the same `OnceLock<Mutex<()>>` env lock used by the existing `multi_target_tests` and `save_job_output_tests_phase_32_1` modules in `delivery.rs`. Place the new tests in a new sibling `#[cfg(test)] mod telegram_whitelist_fallback_tests` to keep the gap-closure additions self-contained for review.

    - Test 1 (env var wins — single whitelist entry): With `TELEGRAM_HOME_CHANNEL=env-value` set AND `config.yaml` containing a single-entry telegram whitelist `["config-value"]` (via `IRONHERMES_HOME` pointing at a tempdir with a fixture `config.yaml`), `resolve_delivery_targets(&job_with_deliver("telegram"))` returns one target with `chat_id == "env-value"`. PROVES env var precedence is preserved.
    - Test 2 (config fallback — single whitelist entry, env unset): With `TELEGRAM_HOME_CHANNEL` removed AND `config.yaml` containing a single-entry telegram whitelist `["12345"]`, `resolve_delivery_targets(&job_with_deliver("telegram"))` returns one target with `platform == "telegram"`, `chat_id == "12345"`, `thread_id == None`. PROVES the fallback fires.
    - Test 3 (config fallback — zero whitelist entries, env unset): With both env unset AND the telegram whitelist EMPTY (`whitelist: []`), `resolve_delivery_targets(&job_with_deliver("telegram"))` returns `Vec::new()` (no target, existing warn). PROVES behavior unchanged for the zero-entry case.
    - Test 4 (config fallback — multiple whitelist entries, env unset): With env unset AND the telegram whitelist `["a", "b"]` (two entries), `resolve_delivery_targets(&job_with_deliver("telegram"))` returns `Vec::new()` and emits a `tracing::warn!`. PROVES the fallback intentionally refuses ambiguous multi-entry whitelists (matches `OriginDecision::Multi` semantics for create-time defaulting).
    - Test 5 (config fallback — telegram section missing entirely): With env unset AND `config.yaml` containing NO `gateway.platforms.telegram` section at all (or no config.yaml present), `resolve_delivery_targets(&job_with_deliver("telegram"))` returns `Vec::new()`. PROVES no panic, no false-positive routing.
    - Test 6 (config fallback — telegram section present but disabled): With env unset AND a telegram section that has `enabled: false` AND a single whitelist entry, `resolve_delivery_targets(&job_with_deliver("telegram"))` returns `Vec::new()`. PROVES the `enabled: false` gate is honored (matches Config::telegram_default_origin's disabled branch).
    - Test 7 (no fallback for non-telegram platforms — discord): With env `DISCORD_HOME_CHANNEL` unset AND a (hypothetical) `gateway.platforms.discord.whitelist = ["x"]` set, `resolve_delivery_targets(&job_with_deliver("discord"))` returns `Vec::new()`. The discord platform does NOT consult any config fallback. PROVES the fallback is telegram-scoped only.
    - Test 8 (no fallback for `qq` either, env unset, no QQBOT_HOME_CHANNEL): existing test7/test8 in `multi_target_tests` already cover the legacy env-var fallback for qq. This new test reaffirms there's NO config.yaml fallback for qq even if a (nonexistent) whitelist were present. Returns `Vec::new()`.
    - Test 9 (thread_id env still wins): With env `TELEGRAM_HOME_CHANNEL=env-tg` AND `TELEGRAM_HOME_CHANNEL_THREAD_ID=42` set, `resolve_delivery_targets(&job_with_deliver("telegram"))` returns one target with `chat_id == "env-tg"` AND `thread_id == Some("42")`. PROVES adding the config fallback didn't disturb the existing thread_id env wiring.
    - Test 10 (config fallback yields no thread_id): With env unset AND a single-entry telegram whitelist `["12345"]`, the returned target has `thread_id == None`. The config.yaml accessor doesn't carry thread-id semantics (Telegram bot platform topics are env-only by the Plan 04 contract). PROVES the fallback doesn't fabricate a thread_id.
    - Test 11 (`deliver=telegram:explicit-chat-id` is unaffected by config): With env unset AND `config.yaml` whitelist `["should-not-be-used"]`, `resolve_delivery_targets(&job_with_deliver("telegram:caller-supplied"))` returns one target with `chat_id == "caller-supplied"`. PROVES the explicit `platform:chat_id` path bypasses both env lookup and config fallback (existing contract is preserved).
    - Test 12 (`deliver=origin` with origin set is unaffected by config): With env unset, `config.yaml` whitelist `["wl-1"]`, AND a job whose `origin = Some(JobOrigin{platform:"telegram", chat_id:"origin-1", ..})`, `resolve_delivery_targets(&job_with_deliver_and_origin("origin", ...))` returns one target with `chat_id == "origin-1"`. PROVES the `origin` routing branch still wins over the new config fallback for telegram.
  </behavior>
  <action>
    Step 1 — Confirm the dep graph allows the import.

    Run the pre-implementation grep audit (above). Confirm:
    - `ironhermes-core` does NOT depend on `ironhermes-cron` (no reverse edge — no cycle).
    - `ironhermes-cron` already depends on `ironhermes-core` (the dep edge exists today).

    No `Cargo.toml` edit is required.

    Step 2 — Add a new private helper in `crates/ironhermes-cron/src/delivery.rs` directly ABOVE `lookup_home_channel` (insertion point: just before line 70 / 71 — the `// lookup_home_channel` section header).

        // ---------------------------------------------------------------------------
        // Telegram config.yaml whitelist fallback (gap-closure 32.1-09)
        // ---------------------------------------------------------------------------
        //
        // Restores pre-32.1 gateway parity: when TELEGRAM_HOME_CHANNEL is unset,
        // resolve to the single entry of `gateway.platforms.telegram.whitelist`
        // from `config.yaml` if and only if the whitelist has exactly one entry.
        // Zero or multiple entries return None (matching Config::telegram_default_origin's
        // OriginDecision::None / OriginDecision::Multi semantics).
        //
        // SCOPE: telegram only. Other platforms (discord/slack/matrix/etc.) do
        // not currently carry whitelist semantics in `Config::gateway.platforms`
        // shaped like Telegram, so the fallback is intentionally telegram-scoped.
        fn lookup_telegram_whitelist_fallback() -> Option<DeliveryTarget> {
            // Config::load reads ${IRONHERMES_HOME}/config.yaml (or defaults if missing).
            // Failures are non-fatal — fallback simply returns None.
            let config = match ironhermes_core::config::Config::load() {
                Ok(c)  => c,
                Err(e) => {
                    tracing::debug!(error = %e, "telegram fallback: Config::load failed — skipping");
                    return None;
                }
            };
            let tg = config.gateway.platforms.get("telegram")?;
            if !tg.enabled {
                return None;
            }
            match tg.whitelist.len() {
                1 => {
                    let chat_id = tg.whitelist[0].to_string();
                    if chat_id.is_empty() {
                        return None;
                    }
                    Some(DeliveryTarget {
                        platform: "telegram".to_string(),
                        chat_id,
                        thread_id: None,
                    })
                }
                _ => None,
            }
        }

    Step 3 — Extend `lookup_home_channel` (the function at `delivery.rs:77-98`) with a telegram-only fallback AFTER the legacy env var branch and BEFORE the thread_id env-var read.

    Replace the existing body of `lookup_home_channel` with:

        fn lookup_home_channel(platform: &str) -> Option<DeliveryTarget> {
            // Allowlist gate FIRST — no env var read for unknown platforms
            if !KNOWN_DELIVERY_PLATFORMS.contains(&platform) {
                return None;
            }
            let chat_id = home_channel_env_var(platform)
                .and_then(|v| std::env::var(v).ok())
                .filter(|s| !s.is_empty())
                .or_else(|| {
                    legacy_home_channel_env_var(platform)
                        .and_then(|v| std::env::var(v).ok())
                        .filter(|s| !s.is_empty())
                });

            // Gap-closure 32.1-09: telegram-only config.yaml whitelist fallback.
            // Runs ONLY when both the primary and legacy env vars yielded nothing,
            // AND the platform is telegram. Other platforms remain env-only.
            if chat_id.is_none() && platform == "telegram" {
                if let Some(target) = lookup_telegram_whitelist_fallback() {
                    return Some(target);
                }
            }

            let chat_id = chat_id?;

            let thread_id = home_channel_thread_env_var(platform)
                .and_then(|v| std::env::var(v).ok())
                .filter(|s| !s.is_empty());
            Some(DeliveryTarget {
                platform: platform.to_string(),
                chat_id,
                thread_id,
            })
        }

    Notes on the diff shape:
    - The env-var lookup is unchanged in behavior — it produces `Some(chat_id)` exactly when it did before.
    - The new `if chat_id.is_none() && platform == "telegram"` branch is the entirety of the fallback. It returns EARLY with the fallback target if present; otherwise control falls through to the `chat_id?` short-circuit and `lookup_home_channel` returns `None` as before.
    - The fallback path does NOT populate `thread_id` from env vars (because we're already in the "env var unset" branch). The fallback target's `thread_id` is always `None` — Test 10 locks this.

    Step 4 — Add a `tests` sub-module `telegram_whitelist_fallback_tests` at the bottom of `crates/ironhermes-cron/src/delivery.rs` (alongside the existing `multi_target_tests` and `save_job_output_tests_phase_32_1`).

    Test infrastructure needed:
    - Re-use the `env_lock()` pattern from the existing test modules (`OnceLock<Mutex<()>>`).
    - Each test sets `IRONHERMES_HOME` to a `tempfile::TempDir` and writes a `config.yaml` fixture there (with the desired `gateway.platforms.telegram.whitelist` and `enabled` flags). The on-disk fixture is what `Config::load` reads.
    - The `config.yaml` shape mirrors the test fixtures at `crates/ironhermes-core/src/config.rs:1656-1730` (`test_telegram_default_origin_*`). Copy that YAML shape verbatim.
    - Be sure to `remove_var` any TELEGRAM_HOME_CHANNEL/TELEGRAM_HOME_CHANNEL_THREAD_ID at test entry so a stale env from another test doesn't leak in.
    - At test exit, `remove_var` everything you set and restore `IRONHERMES_HOME` to its prior value (mirror the `tick_observes_external_job_writes` pattern at `tick.rs:200-269`).

    Each test fixture's `config.yaml` looks like:

        gateway:
          platforms:
            telegram:
              enabled: true
              whitelist:
                - "12345"

    (Adjust whitelist content / enabled flag per test.)

    Implement all 12 behaviors listed above. Use the existing `make_job` fixture from `multi_target_tests` (copy-paste it into the new module; it's a local test helper, not a public API).

    Step 5 — Update the existing `multi_target_tests::test6_bare_platform_home_unset_returns_empty` test (`delivery.rs:644-653`) IF it would now spuriously fail due to the new fallback. Specifically: that test sets `IRONHERMES_HOME` implicitly to whatever is in the environment. If the developer's machine has a `~/.ironhermes/config.yaml` with a single telegram whitelist entry, the test would now resolve a target instead of returning empty.

    Fix by isolating the test's `IRONHERMES_HOME` to a tempdir (matching the pattern used by `tick_observes_external_job_writes`). Add the same fix to any other env-mutating test in `delivery.rs` that asserts an empty Vec for `deliver = "telegram"` and could be affected by the new fallback (e.g. `test12_origin_no_origin_no_home_channel_returns_empty` at `delivery.rs:751-766`).

    The audit pattern:
    ```
    grep -nE 'make_job\("telegram"|deliver = "telegram"|deliver: "telegram' crates/ironhermes-cron/src/delivery.rs
    ```
    Inspect each match; if the test asserts an empty Vec without isolating `IRONHERMES_HOME`, add a tempdir + empty `config.yaml` fixture (or no config.yaml at all → `Config::load` returns defaults with empty platforms map → fallback returns None → behavior preserved).

    Defensive minimum: the simplest fix is to set `IRONHERMES_HOME` to a tempdir in EVERY env-mutating test that asserts the empty-Vec result for telegram. The tempdir has no config.yaml so `Config::load` returns `Config::default()`, whose `gateway.platforms` map does not contain a "telegram" entry, so the fallback returns `None` — preserving the original test's intent.

    Step 6 — Run the full test suite to confirm no regression:
    ```
    cargo test -p ironhermes-cron -- --test-threads=1
    cargo build --workspace
    ```
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cron --lib telegram_whitelist_fallback_tests -- --test-threads=1 &amp;&amp; cargo test -p ironhermes-cron --lib -- --test-threads=1 &amp;&amp; cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `cargo test -p ironhermes-cron --lib telegram_whitelist_fallback_tests -- --test-threads=1` exits 0 and reports at least 12 tests run
    - test-command: `cargo test -p ironhermes-cron --lib -- --test-threads=1` exits 0 with NO test failures (regression check — the full crate test count must be ≥ what it was before this plan)
    - test-command: `cargo build --workspace` exits 0
    - source assertion: `grep -n "fn lookup_telegram_whitelist_fallback" crates/ironhermes-cron/src/delivery.rs` matches exactly 1 line
    - source assertion: `awk '/fn lookup_home_channel/,/^}/' crates/ironhermes-cron/src/delivery.rs | grep -c 'platform == "telegram"'` returns at least 1 (the new gating check)
    - source assertion: `awk '/fn lookup_home_channel/,/^}/' crates/ironhermes-cron/src/delivery.rs | grep -c 'lookup_telegram_whitelist_fallback'` returns at least 1
    - source assertion: `grep -c "Config::load" crates/ironhermes-cron/src/delivery.rs` returns at least 1
    - source assertion: `grep -c "gateway.platforms.get" crates/ironhermes-cron/src/delivery.rs` returns at least 1
    - source assertion: `grep -nE 'ironhermes_core::config::Config|ironhermes_core::Config' crates/ironhermes-cron/src/delivery.rs` matches at least 1 line
    - source assertion: Cargo.toml unchanged (no new deps added): `git diff crates/ironhermes-cron/Cargo.toml` is empty
    - precedence assertion: the env-var lookup runs BEFORE the config fallback. Verified by `awk '/fn lookup_home_channel/,/^}/' crates/ironhermes-cron/src/delivery.rs | grep -nE 'std::env::var|lookup_telegram_whitelist_fallback'` — the first match must be a `std::env::var` line, the `lookup_telegram_whitelist_fallback` match must come later in the function body
    - scope assertion: the fallback fires ONLY when `platform == "telegram"`. Verified by inspecting the `if chat_id.is_none() && platform == "telegram"` guard. `awk '/fn lookup_home_channel/,/^}/' crates/ironhermes-cron/src/delivery.rs | grep -c '"telegram"'` returns at least 1.
  </acceptance_criteria>
  <done>A cron job with `deliver = "telegram"` and `TELEGRAM_HOME_CHANNEL` unset successfully resolves to the single entry of `config.yaml::gateway.platforms.telegram.whitelist` when that whitelist has exactly one entry; the env var still wins when set; zero/multi entry whitelists preserve the existing empty-Vec + warn behavior; only the telegram platform uses the fallback; the full `ironhermes-cron` test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config.yaml` on disk → routing decision | An attacker who can write `~/.ironhermes/config.yaml` could redirect cron deliveries to an arbitrary Telegram chat ID. This is the same trust boundary `Config::telegram_default_origin` already crosses today (used by `resolve_cron_deliver` in the CLI). The fallback inherits — not widens — this boundary. |
| `TELEGRAM_HOME_CHANNEL` env var → routing decision | Unchanged; env var precedence is preserved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32.1.09-01 | Tampering | Hostile `config.yaml` redirects cron deliveries | accept | Same trust boundary already crossed by `Config::telegram_default_origin` (job-creation default) and by `Config::load` workspace-wide. `config.yaml` is operator-owned (writable only by the same user that runs the cron daemon under default 0600/0700 perms). Mitigating this would require sandboxing the config loader — out of scope for a regression-closure plan. |
| T-32.1.09-02 | Information Disclosure | Env-var enumeration via the fallback | mitigate | The fallback runs ONLY for `platform == "telegram"` AFTER the existing `KNOWN_DELIVERY_PLATFORMS` allowlist check has already passed. No new env vars are read by the fallback. `Config::load` reads exactly one file (`${IRONHERMES_HOME}/config.yaml`) and does not enumerate environment. |
| T-32.1.09-03 | Spoofing | A non-telegram platform spoofing into the telegram fallback via a crafted `Config::gateway.platforms` entry | accept | The fallback hardcodes the string `"telegram"` and reads only `gateway.platforms.get("telegram")`. A crafted config entry like `gateway.platforms.discord` cannot influence the telegram fallback path. |
| T-32.1.09-04 | Denial of Service | `Config::load` IO failure during every tick | mitigate | The fallback wraps `Config::load` in a `match` and treats `Err` as "fall back to None" with a `tracing::debug!`. A persistent disk error doesn't crash the tick loop. Config is also re-read only when env var lookup yields `None` (rare in production where operators typically set the env var) — so this is not a per-tick I/O cost in the common case. |
| T-32.1.09-05 | Repudiation | Operator can't tell from logs whether delivery used env or config fallback | mitigate | Plan 04's existing `tracing::warn!` "deliver={platform} has no home channel configured" stays in place for the zero-fallback case. Add no new log on the success path — the fallback is invisible because that IS the documented behavior. If operators need diagnostic visibility, a future plan can add a `tracing::info!` to the fallback success branch; out of scope here. |
| T-32.1.09-06 | Tampering | Race between `config.yaml` reload and tick execution | accept | `Config::load` is read-only and synchronous. A concurrent operator edit can yield a partial-read on systems with non-atomic file writes, but `serde_yaml::from_str` will then return `Err` and the fallback returns `None` — the tick proceeds without crashing. Atomic config writes are an existing project concern, not introduced here. |
| T-32.1.09-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new crates. The dep edge `ironhermes-cron → ironhermes-core` already exists (verified by `grep -n "ironhermes-core" crates/ironhermes-cron/Cargo.toml`). `Cargo.toml` is unchanged. |
</threat_model>

<verification>
- `cargo test -p ironhermes-cron --lib -- --test-threads=1` is green with all new and existing tests passing
- `cargo build --workspace` is green with zero new warnings introduced by this plan
- `grep -rn "ironhermes-cron" crates/ironhermes-core/Cargo.toml` returns 0 matches (no cycle)
- The `cron create --deliver` CLI help text at `crates/ironhermes-cli/src/cron.rs:36-39` once again accurately describes runtime behavior
- A repeat of UAT Test 5 (deliver=telegram, env unset, single-entry whitelist) now succeeds without the `export TELEGRAM_HOME_CHANNEL=...` workaround
</verification>

<success_criteria>
- `deliver = "telegram"` with env unset AND a single-entry whitelist routes to that entry
- `deliver = "telegram"` with env set routes to the env value (env wins)
- `deliver = "telegram"` with env unset AND a 0-entry or >1-entry whitelist returns empty (existing warn preserved)
- Other platforms (discord, slack, etc.) are unaffected — no config fallback for non-telegram
- `Cargo.toml` is byte-for-byte unchanged (no new dependency)
- All ≥ 12 new tests pass; all 105+ existing tests still pass; full workspace builds clean
- One PLAN.md, one SUMMARY.md, one (or two) atomic commits
</success_criteria>

<output>
Create `.planning/phases/32.1-agent-cron-execution/32.1-09-SUMMARY.md` when done. Record:
- The exact line range in `delivery.rs` where the new helper was inserted
- The exact line range in `delivery.rs::lookup_home_channel` where the fallback branch was added
- Confirmation that `Cargo.toml` was NOT modified (`git diff crates/ironhermes-cron/Cargo.toml` empty)
- Test counts: how many new tests added, total `ironhermes-cron --lib` test count before vs after, `cargo test` summary line
- Whether any existing test in `multi_target_tests` required `IRONHERMES_HOME` isolation to keep passing (list which ones — for forward-compat documentation)
- A note on whether to repeat UAT Test 5 (operator action — not blocking this plan's completion, but the gap closure isn't fully validated until that re-run succeeds)
</output>
</content>
