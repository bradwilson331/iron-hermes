# Phase 27.1.4.1: gateway-fallback-gap - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-12
**Phase:** 27.1.4.1-gateway-fallback-gap
**Areas discussed:** D-12 revisit, Fallback scope, Failure escalation, Fallback provider source, Config files and documentation, Refactor helper, Test coverage

---

## D-12 Revisit

| Option | Description | Selected |
|--------|-------------|----------|
| Oversight — should be fixed | The main path and UI path both have fallback. Cron being the only exception was likely just never revisited. Wire it the same way. | ✓ |
| Was intentional, context changed | Cron was originally simple/isolated, but now that it runs real agent workloads with skills attached, it deserves the same resilience. | |

**User's choice:** Oversight — should be fixed
**Notes:** Phase name "gateway fallback gap" and urgency status ("Urgent work - to be planned") confirm this is a bug, not a feature request.

---

## Fallback Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Just cron — the only gap | Wire fallback into runner.rs run_cron_job() only. | |
| Also check subagent delegation | Gateway can spawn subagents — audit that path too. | |
| Broader audit first | Grep all AgentLoop::new call sites across the whole workspace before deciding scope. | ✓ |

**User's choice:** Broader audit first — AND added: "the Gateway does not fallback and the iron_hermes_ui must fallback"
**Notes:** Full workspace audit found 3 production gaps: runner.rs (cron), subagent_runner.rs (subagent delegation), batch/runner.rs (CLI batch). The existing handler.rs and iron_hermes_ui sites DO have fallback code but use a silent triple-nested if-let with no warning when the provider isn't configured. All 3 gaps + the silent sites are in scope.

---

## Failure Escalation

| Option | Description | Selected |
|--------|-------------|----------|
| Deliver failure message to cron target | Cron's configured delivery target receives an error summary. Consistent with existing complete_job_run error handling. | ✓ |
| Warn + log only | tracing::warn! + JSONL event. No Telegram notification. | |
| You decide | Match whatever complete_job_run does today. | |

**User's choice:** Deliver failure message to cron target

---

## Silent Fallback Warn!

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — add tracing::warn! when fallback skipped | Emit warn! at each site when fallback provider can't be resolved/built. | ✓ |
| No — silent is fine | Fallback is optional; nested if-let silently skips it. | |

**User's choice:** Yes — add tracing::warn! when fallback skipped

---

## Fallback Provider Source

| Option | Description | Selected |
|--------|-------------|----------|
| Same as main path — main_endpoint.fallback_providers | Reuse resolver.resolve_for_main().fallback_providers list. | ✓ |
| Cron-specific fallback config | Add cron.fallback_providers field for independent routing. | |

**User's choice:** Same as main path

---

## Other Gaps Scope

| Option | Description | Selected |
|--------|-------------|----------|
| All 3 gaps in this phase | Fix cron + subagent_runner + CLI batch in one phase. | ✓ |
| Cron only — defer subagent + batch | Fix gateway cron gap now, file tickets for the others. | |
| Cron + subagent, defer batch | CLI batch is out of scope for a gateway phase. | |

**User's choice:** All 3 gaps in this phase

---

## Warn! Scope for Existing Sites

| Option | Description | Selected |
|--------|-------------|----------|
| Fix all sites in this phase | Add tracing::warn! to the existing handler.rs and iron_hermes_ui fallback blocks too. | ✓ |
| New sites only, defer existing | Only add warn! to the 3 new sites. Leave existing blocks untouched. | |

**User's choice:** Fix all sites in this phase

---

## Config CLI Gap

| Option | Description | Selected |
|--------|-------------|----------|
| Gateway needs hermes config subcommands | Expose config_cli.rs logic from gateway entrypoint. | |
| Gateway config loading should match CLI | The gap is in what gets validated at startup. | |
| UI /api/config endpoint needs set/get too | iron_hermes_ui needs POST/PATCH config endpoints. | |

**User's choice:** "The gateway and iron_hermes_ui need to follow the existing config.yaml configuration used by the cli and TUI. If any changes are made - it must occur to all fallback paths, config, config examples, docs and commands."
**Notes:** This is a consistency rule: any config change must be reflected across ALL paths (CLI + gateway + UI + yaml.example + README). Not a "build new endpoints" requirement.

---

## YAML Example Gaps

| Option | Description | Selected |
|--------|-------------|----------|
| Missing learning: section | setup_wizard.rs writes learning.* keys not in the example. | |
| Missing tools: section | The yaml.example doesn't document the tools: toolsets map. | |
| Both — add learning: and tools: sections | Add both. | |
| I'm not sure — diff them | Let researcher figure out what's missing. | |

**User's choice:** Options 3 and 4 (both sections needed + researcher diff)
**Notes:** User also noted "line 1 default model does not appear to be used: model: default: gemma4:latest base_url: http://localhost:11434/v1 provider: ollama" — the model section in yaml.example needs to document both cloud and local Ollama configurations. User runs Ollama locally (gemma4:latest) as their actual fallback provider.

---

## Refactor Helper

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — extract a shared helper | wire_fallback_if_configured(agent, &resolver) -> AgentLoop. | ✓ |
| No — inline at each site | Keep it explicit at each site. Pattern is short. | |

**User's choice:** Yes — extract a shared helper

---

## Test Coverage

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — add invariant tests for all 3 new sites | Static assert tests that source files contain .with_fallback(. | ✓ |
| Unit tests only | Write unit/integration tests instead of static asserts. | |
| No new tests | Existing coverage is enough. | |

**User's choice:** Yes — add invariant tests for all 3 new sites (modeled on invariants_22_4.rs)

---

## Claude's Discretion

- Whether `wire_fallback_if_configured` lives in `any_client.rs` or a new `fallback.rs` module
- Whether `batch/runner.rs` needs its own `ProviderResolver` construction or can receive one
- Exact `tracing::warn!` message wording — follow existing patterns in `any_client.rs`

## Deferred Ideas

- **Cron-specific fallback_providers config** — user chose to reuse main_endpoint.fallback_providers; add cron.fallback_provider in a future phase if needed
- **Per-subagent fallback config** — subagents inherit main resolver's fallback for now
- **CLI batch per-worker provider isolation** — finer-grained routing is out of scope
