---
phase: 27.1.4.1
slug: gateway-fallback-gap
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-12
---

# Phase 27.1.4.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (Rust built-in) |
| **Config file** | none — uses Cargo workspace test discovery |
| **Quick run command** | `cargo test -p ironhermes-cli --test invariants_27_1_4_1` (plus per-crate invariants files once they exist) |
| **Full suite command** | `cargo test --workspace` |
| **Estimated runtime** | ~30–120 seconds (full workspace; invariants alone ~5s) |

---

## Sampling Rate

- **After every task commit:** Run the relevant crate's invariants test (`cargo test -p <crate> --test invariants_27_1_4_1`) plus `cargo build`
- **After every plan wave:** Run `cargo test --workspace`
- **Before `/gsd-verify-work`:** Full suite must be green; `cargo clippy --workspace` clean
- **Max feedback latency:** ~120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-T1 | 27.1.4.1-01 | 1 (Wave 0 dep) | PROV-07 | T-27.1.4.1-01 | 3 static invariant files exist; assert `.with_fallback(` present in runner/subagent/batch source | static-invariant | `cargo build --tests --workspace` | ❌ → created by this task | ⬜ pending |
| 01-T2 | 27.1.4.1-01 | 1 | PROV-07 | T-27.1.4.1-01, T-27.1.4.1-02 | `wire_fallback_if_configured` fails safe: never panics, two `tracing::warn!` branches, only `fb_name` interpolated (no creds) | build + clippy (unit optional) | `cargo build -p ironhermes-agent && cargo clippy -p ironhermes-agent -- -D warnings` | ✅ (any_client.rs / lib.rs exist) | ⬜ pending |
| 01-T3 | 27.1.4.1-01 | 1 | PROV-07 | T-27.1.4.1-01, T-27.1.4.1-04 | Fallback wired at all 5 AgentLoop sites via shared helper; D-12 "NO fallback" comment removed | static-invariant + build + clippy | `cargo test -p ironhermes-gateway -p ironhermes-agent -p ironhermes-cli --test invariants_27_1_4_1 && cargo build --workspace && cargo clippy --workspace -- -D warnings` | ✅ (3 invariants files from 01-T1) | ⬜ pending |
| 02-T1 | 27.1.4.1-02 | 1 | PROV-07 (D-08/D-09/D-10) | T-27.1.4.1-05, T-27.1.4.1-07 | `cli-config.yaml.example` documents learning: (5 keys) + tools.toolsets:; no real API keys | grep assertion | `grep -q skill_generation_enabled cli-config.yaml.example && grep -q toolsets: cli-config.yaml.example` | ✅ (file exists) | ⬜ pending |
| 02-T2 | 27.1.4.1-02 | 1 | PROV-07 (D-11) | T-27.1.4.1-05 | README Provider fallback section: `hermes config set` + Ollama gemma4:latest example, placeholder creds only | grep assertion | `grep -q fallback_providers README.md && grep -q gemma4:latest README.md && grep -q 'hermes config set' README.md` | ✅ (file exists) | ⬜ pending |
| 02-T3 | 27.1.4.1-02 | 1 | PROV-07 (D-08) | — | ROADMAP entry finalized (PROV-07, 2 plans); D-08 cross-check: no CLI/gateway/UI-read config key missing from example | grep assertion + manual cross-check | `grep -A6 'Phase 27.1.4.1: gateway fallback gap' .planning/ROADMAP.md | grep -q PROV-07` | ✅ (ROADMAP exists) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

> Planner: populate one row per task. The 3 new `*-PLAN.md` files must each map their wiring task to a `cargo test ... --test invariants_27_1_4_1` assertion (`SOURCE.contains(".with_fallback(")`).

---

## Wave 0 Requirements

- [ ] `crates/ironhermes-gateway/tests/invariants_27_1_4_1.rs` — `include_str!` assertion that `runner.rs` contains `.with_fallback(`
- [ ] `crates/ironhermes-agent/tests/invariants_27_1_4_1.rs` — `include_str!` assertion that `subagent_runner.rs` contains `.with_fallback(`
- [ ] `crates/ironhermes-cli/tests/invariants_27_1_4_1.rs` — `include_str!` assertion that `batch/runner.rs` contains `.with_fallback(`

*(These are new test files; no `cargo test` framework install needed — Rust built-in.)*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Cron job actually fails over to the local Ollama fallback when the cloud provider is down | PROV-07 | Requires a misconfigured/offline primary provider + a running Ollama instance; not reproducible in CI | Configure `providers.<cloud>.fallback_providers[0]` to a local Ollama custom provider, stop/break the cloud provider, trigger a cron job, confirm it completes via the fallback and a `tracing::warn!` is NOT emitted (fallback resolved fine) |
| `tracing::warn!` is emitted when the configured fallback provider name is bogus | PROV-07 | Log-output assertion across a real AgentLoop construction; covered structurally by the helper but the emitted log line is observed manually | Set `fallback_providers[0]` to a name that does not exist, run any agent path, confirm the warn! line appears in logs |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (01-T1 creates the 3 new invariants test files)
- [x] No watch-mode flags
- [x] Feedback latency < 120s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planned — pending execution
