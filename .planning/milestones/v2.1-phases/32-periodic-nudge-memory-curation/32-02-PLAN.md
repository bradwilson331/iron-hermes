---
phase: "32"
plan: "02"
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - crates/ironhermes-gateway/src/handler.rs
  - crates/ironhermes-agent/src/nudge.rs
autonomous: true
requirements: [LEARN-01]

must_haves:
  truths:
    - "Gateway handler tracks turns_since_nudge per session in a HashMap<SessionKey, u32> on GatewayHandler"
    - "Nudge fires after agent.run() completes in handle_with_multimodal, wrapped in tokio::spawn"
    - "Counter resets to 0 after firing; per-session counter persists across messages for the same session"
    - "nudge_interval=0 disables the nudge in gateway just as in run_chat"
    - "Unit tests verify counter logic: fires_at_interval, disabled_when_zero, counter_resets_after_fire"
  artifacts:
    - path: "crates/ironhermes-gateway/src/handler.rs"
      provides: "nudge_turns field on GatewayHandler + fire site in handle_with_multimodal"
      contains: "nudge_turns"
    - path: "crates/ironhermes-agent/src/nudge.rs"
      provides: "should_nudge helper + 3 counter-logic unit tests"
      contains: "fires_at_interval"
  key_links:
    - from: "crates/ironhermes-gateway/src/handler.rs handle_with_multimodal"
      to: "crates/ironhermes-agent/src/nudge.rs spawn_nudge_review"
      via: "tokio::spawn after agent.run() returns Ok"
      pattern: "nudge_turns"
---

<objective>
Wire the periodic nudge into the Telegram gateway handler and add counter-logic unit tests that
prove LEARN-01 behavioural correctness independent of AgentLoop or MemoryManager.

Purpose: Completes LEARN-01 for the gateway path. The gateway has per-session state (messages
persist across turns via SessionStore) so the nudge counter is per-session, stored in a
HashMap<SessionKey, u32> using the same Arc<std::sync::Mutex<HashMap>> interior-mutability
pattern as skill_overlays.

Output: Gateway handler with per-session nudge counter, nudge.rs with should_nudge helper and
3 counter-logic tests (total 6 tests in nudge::tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-periodic-nudge-memory-curation/32-RESEARCH.md
@.planning/phases/32-periodic-nudge-memory-curation/32-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From crates/ironhermes-gateway/src/handler.rs (GatewayHandler struct pattern):
  // Interior mutability pattern used by skill_overlays (handle_message takes &self):
  skill_overlays: Arc<std::sync::Mutex<std::collections::HashMap<SessionKey, Vec<(String, String)>>>>,
  active_personality_overlay: Arc<std::sync::Mutex<std::collections::HashMap<SessionKey, String>>>,
  // nudge_turns follows the same pattern:
  nudge_turns: Arc<std::sync::Mutex<std::collections::HashMap<SessionKey, u32>>>,

From handle_with_multimodal (lines ~999-1045):
  // agent.run(messages).await at line ~999
  // Ok(result) arm starts at ~1011
  // info!("Agent completed...") at ~1013
  // session store update at ~1040
  // Nudge fire site goes BETWEEN ~1013 and ~1040

Counter logic for the fire site (std::sync::Mutex guard must NOT cross await):
  let nudge_interval = self.config.memory.nudge_interval;
  if nudge_interval > 0 && self.config.memory.memory_enabled {
      let should_fire = {
          let mut map = self.nudge_turns.lock().unwrap_or_else(|e| e.into_inner());
          let count = map.entry(session_key.clone()).or_insert(0);
          *count += 1;
          if *count >= nudge_interval {
              *count = 0;
              true
          } else {
              false
          }
      }; // guard dropped here — before any await
      if should_fire {
          if let Some(ref mgr) = self.memory_manager {
              let mgr_clone = Arc::clone(mgr);
              let client_clone = client.clone();  // AnyClient built earlier in handle_with_multimodal
              let messages_snapshot = messages.clone();
              let config_clone = self.config.clone();
              tokio::spawn(async move {
                  ironhermes_agent::nudge::spawn_nudge_review(
                      messages_snapshot,
                      mgr_clone,
                      client_clone,
                      &config_clone,
                  ).await;
              });
          }
      }
  }

From crates/ironhermes-agent/src/nudge.rs (created Plan 32-01):
  pub async fn spawn_nudge_review(
      messages_snapshot: Vec<ChatMessage>,
      memory_manager: Arc<tokio::sync::Mutex<MemoryManager>>,
      client: AnyClient,
      config: &Config,
  )
  // Existing tests: prompt_contains_tier_guidance, prompt_contains_cap_info,
  //                 prompt_contains_nothing_to_save_signal
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add should_nudge helper and counter-logic tests to nudge.rs</name>
  <files>crates/ironhermes-agent/src/nudge.rs</files>
  <read_first>
    - crates/ironhermes-agent/src/nudge.rs (full file — created by Plan 32-01; find the #[cfg(test)] block and the existing 3 tests to know where to append)
  </read_first>
  <behavior>
    - Test fires_at_interval: interval=3, counter starts at 0; should_nudge returns false/false/true on turns 1/2/3; counter resets to 0 after firing
    - Test disabled_when_zero: interval=0; should_nudge returns false on 20 consecutive calls; counter stays at 0
    - Test counter_resets_after_fire: after firing at turn 2 (interval=2), counter=0; fires again at turn 2 of next cycle
  </behavior>
  <action>
    Add a private helper function to nudge.rs (above or below spawn_nudge_review):

      /// Returns true when the nudge should fire. Increments *counter; resets to 0 on fire.
      /// Returns false immediately when interval == 0 (disabled).
      /// Used by both run_chat and gateway callers; extracted for testability.
      pub(crate) fn should_nudge(interval: u32, counter: &mut u32) -> bool {
          if interval == 0 {
              return false;
          }
          *counter += 1;
          if *counter >= interval {
              *counter = 0;
              true
          } else {
              false
          }
      }

    Optionally update spawn_nudge_review callers in run_chat (main.rs) and handler.rs to use
    should_nudge instead of inline logic — but only if it simplifies the code. The helper is
    pub(crate) so it is available within ironhermes-agent; callers outside the crate (main.rs,
    handler.rs) inline the logic as shown in the interfaces section.

    Add three tests to the existing #[cfg(test)] mod tests block:

      #[test]
      fn fires_at_interval() {
          let mut c = 0u32;
          assert!(!should_nudge(3, &mut c));  // turn 1: c=1
          assert!(!should_nudge(3, &mut c));  // turn 2: c=2
          assert!(should_nudge(3, &mut c));   // turn 3: fires, c=0
          assert_eq!(c, 0);
          assert!(!should_nudge(3, &mut c));  // turn 1 of next cycle
      }

      #[test]
      fn disabled_when_zero() {
          let mut c = 0u32;
          for _ in 0..20 {
              assert!(!should_nudge(0, &mut c));
          }
          assert_eq!(c, 0);
      }

      #[test]
      fn counter_resets_after_fire() {
          let mut c = 0u32;
          assert!(!should_nudge(2, &mut c)); // turn 1: c=1
          assert!(should_nudge(2, &mut c));  // turn 2: fires, c=0
          assert_eq!(c, 0);
          assert!(!should_nudge(2, &mut c)); // turn 1 of cycle 2: c=1
          assert!(should_nudge(2, &mut c));  // turn 2: fires again, c=0
      }
  </action>
  <verify>
    <automated>cargo test -p ironhermes-agent nudge::tests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p ironhermes-agent nudge::tests exits 0 with 6 tests ok (3 from Plan 32-01 + 3 new)
    - grep "fires_at_interval" crates/ironhermes-agent/src/nudge.rs returns a match
    - grep "disabled_when_zero" crates/ironhermes-agent/src/nudge.rs returns a match
    - grep "counter_resets_after_fire" crates/ironhermes-agent/src/nudge.rs returns a match
    - grep "pub(crate) fn should_nudge" crates/ironhermes-agent/src/nudge.rs returns a match
    - cargo build -p ironhermes-agent exits 0
  </acceptance_criteria>
  <done>6 nudge tests pass (3 prompt-content + 3 counter-logic); should_nudge helper present</done>
</task>

<task type="auto">
  <name>Task 2: Wire per-session nudge counter into GatewayHandler</name>
  <files>crates/ironhermes-gateway/src/handler.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/handler.rs (GatewayHandler struct definition; GatewayHandler::new() constructor;
      handle_with_multimodal — specifically the agent.run() call and Ok(result) arm; find the
      client variable name as built at the AgentLoop::new call site; verify messages variable
      name passed to agent.run(); confirm session_key is SessionKey not String)
  </read_first>
  <action>
    Three changes to handler.rs:

    Change 1 — Add nudge_turns field to GatewayHandler struct (after active_personality_overlay):
      /// Per-session nudge turn counter. Arc<Mutex<HashMap>> mirrors skill_overlays pattern.
      /// Interior mutability required: handle_with_multimodal takes &self. (Phase 32 LEARN-01)
      nudge_turns: Arc<std::sync::Mutex<std::collections::HashMap<SessionKey, u32>>>,

    Change 2 — Initialize nudge_turns in GatewayHandler::new() struct literal (parallel to
    the skill_overlays and active_personality_overlay initializations):
      nudge_turns: Arc::new(std::sync::Mutex::new(std::collections::HashMap::new())),

    Change 3 — Add nudge fire site in handle_with_multimodal inside the Ok(result) arm.
    Placement: AFTER the info!("Agent completed...") log line, BEFORE the session store
    update block that writes result.appended. Use the counter logic from the <interfaces>
    section. Key constraints:
      - Lock the nudge_turns HashMap in a synchronous block; assign the bool to should_fire
      - Drop the lock before any await or tokio::spawn call
      - Use unwrap_or_else(|e| e.into_inner()) for poisoned-lock resilience (same as skill_overlays pattern)
      - client variable: read the exact name from the function — it is the AnyClient built for
        AgentLoop::new; clone it before the spawn
      - messages variable: clone the Vec<ChatMessage> passed to agent.run() for the snapshot
      - session_key: use the SessionKey already in scope (do not call .to_string_key() here)
  </action>
  <verify>
    <automated>cargo build -p ironhermes-gateway 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "nudge_turns" crates/ironhermes-gateway/src/handler.rs returns >= 4 (field decl, new init, lock call, entry call)
    - grep "spawn_nudge_review" crates/ironhermes-gateway/src/handler.rs returns 1 match
    - grep "ironhermes_agent::nudge::spawn_nudge_review" crates/ironhermes-gateway/src/handler.rs returns 1 match
    - cargo build -p ironhermes-gateway exits 0 with no new errors
    - cargo test --workspace exits 0 (full regression gate)
  </acceptance_criteria>
  <done>Gateway handler tracks nudge turns per session via nudge_turns HashMap; fires spawn_nudge_review post-agent-turn; full workspace green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway nudge tokio::spawn -> MemoryManager | Nudge writes flow through MemoryTool -> MemoryStore scan_content; no bypass |
| std::sync::Mutex guard scope | Guard released before tokio::spawn; Mutex never held across await |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Denial of Service | per-session HashMap growth | accept | One u32 per active session; sessions bounded by Telegram user count; entry removed if session is cleared via /new or session_store eviction |
| T-32-07 | Availability | std::sync::Mutex held across await | mitigate | Counter mutation done in synchronous block; should_fire bool extracted before tokio::spawn — enforced by borrow scope structure |
| T-32-08 | Denial of Service | gateway nudge recursion | accept | nudge_turns HashMap lives on GatewayHandler; nudge's inner AgentLoop does not reference GatewayHandler — structurally impossible to recurse |
| T-32-SC | Tampering | cargo installs | accept | No new external packages; pure workspace changes |
</threat_model>

<verification>
```bash
# Counter-logic tests (6 total)
cargo test -p ironhermes-agent nudge::tests

# Gateway crate build
cargo build -p ironhermes-gateway

# Full regression suite
cargo test --workspace
```

Expected: 6 nudge tests pass, gateway builds clean, full workspace test suite green.
</verification>

<success_criteria>
1. cargo test -p ironhermes-agent nudge::tests — 6 tests pass (3 prompt + 3 counter)
2. grep -c "nudge_turns" crates/ironhermes-gateway/src/handler.rs — >= 4 matches
3. grep "ironhermes_agent::nudge::spawn_nudge_review" crates/ironhermes-gateway/src/handler.rs — 1 match
4. cargo build --workspace exits 0
5. cargo test --workspace exits 0
</success_criteria>

<output>
Create `.planning/phases/32-periodic-nudge-memory-curation/32-02-SUMMARY.md` when done.
</output>
