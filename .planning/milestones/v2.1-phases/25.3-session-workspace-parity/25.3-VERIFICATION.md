---
phase: 25.3-session-workspace-parity
verified: 2026-05-03T20:28:36Z
status: verified
score: 28/28 must-haves verified
overrides_applied: 0
re_verification: true
prior_verifications:
  - 2026-05-03T18:03:21Z   # initial verification — flagged 1 BLOCKER (must-have #28)
  - 2026-05-03T19:56:11Z   # gap-closure re-verification (Plans 25.3-12..16 + 17 doc close-out)
  - 2026-05-03T20:28:36Z   # this re-verification (post-gap-closure independent re-check)
gaps_closed:
  - "Truth #28: gateway SessionStore::get_or_create persisted workspace_root=NULL (BLOCKER) — closed by Plan 25.3-14 (SessionStore.workspace field + setter; GatewayRunner::set_workspace propagation; canonical_root_string encoding)"
  - "Code review CR-01: ratatui REPL skipped state.create_session — closed by Plan 25.3-13"
  - "Code review CR-02: gateway trajectory at process-wide gateway-<uuid> path — closed by Plan 25.3-15"
  - "Code review CR-03: workspace_root encoding mismatch (to_str vs display) — closed by Plan 25.3-16 (canonical_root_string)"
  - "Code review CR-04: ratatui REPL never called PromptBuilder::with_workspace_root — closed by Plan 25.3-13"
  - "Warning WR-01: cmd_sessions filter encoding mismatch (subset of CR-03) — closed by Plan 25.3-16"
  - "Warning WR-02: SessionStore hardcoded workspace_root=None (subset of BLOCKER) — closed by Plan 25.3-14"
  - "Info IN-03: empty-string workspace filter footgun — closed by Plan 25.3-16 (filter normalization)"
gaps_remaining: []
regressions: []
human_verification:
  - test: "Visual: launch `hermes chat` in a workspace-rooted directory and confirm `[Workspace: <root>]` appears in the system prompt (cache-stable, frozen at session start) — Manual-Only per VALIDATION.md (cache hit rate verification requires live Anthropic API)."
    expected: "[Workspace: <root>] line present in Identity slot of system prompt; cache_read_input_tokens > cache_creation_input_tokens after 5+ turns."
    why_human: "Requires live Anthropic API and multi-turn session; cache stats cannot be mocked deterministically."
  - test: "Bulk export: create 3+ sessions across multiple dates, run `hermes session export-all --since YYYY-MM-DD`, verify directory layout under ~/.ironhermes/sessions/<id>/ for each matching session."
    expected: "Directory count matches the --since filter; each <id>/ contains messages.json + metadata.json + context.json + (trajectories.jsonl if present)."
    why_human: "VALIDATION.md flags this as Manual-Only — integration test would need to seed thousands of session rows."
  - test: "Workspace prompt absence: from a directory with no .hermes/.ironhermes ancestor, run `hermes chat --debug` and confirm no [Workspace:] line appears."
    expected: "No '[Workspace:' line in the printed system prompt when cwd has no workspace marker; the global ~/.ironhermes/ fallback does not synthesize a Workspace prompt line."
    why_human: "Verifying absence in global mode requires running outside any workspace marker — context-dependent."
---

# Phase 25.3: session-workspace parity Verification Report

**Phase Goal:** Achieve session/workspace parity — wire `Workspace` newtype + `TrajectoryWriter` through every CommandContext construction site (cli main.rs, ratatui REPL, Telegram gateway), record one TrajectoryEntry per tool call (with `redact_args()` applied to sensitive args), persist `workspace_root` on the sessions table, and expose `hermes session export[-all]` + `/export-session` slash to operators. Phase 25.4 Curator must be unblocked (curator role in `RESERVED_ROLE_NAMES`, `TrajectoryReader` API, `SessionDirectoryExport`, `Workspace.skills_dir`).

**Verified:** 2026-05-03T20:28:36Z
**Status:** verified
**Re-verification:** Yes — independent post-gap-closure re-check after Plans 25.3-12 through 25.3-17

## Goal Achievement

### Observable Truths (from CONTEXT-locked decisions D-T-1..4, D-F-1, D-F-2, D-W-1, D-W-2, D-P0-1)

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | RESERVED_ROLE_NAMES extended 6→7 with "curator" (D-P0-1) | VERIFIED | `crates/ironhermes-core/src/config.rs:54-63` — array literal contains `"curator"` at line 61; doc comment references "seven roles" at line 67; tests `reserved_role_names_contains_all_seven_roles_with_curator` + `validate_role_name_accepts_all_reserved_roles` pass |
| 2 | Schema migration v8 adds workspace_root TEXT NULL (D-W-1) | VERIFIED | `crates/ironhermes-state/src/lib.rs:46 SCHEMA_VERSION=8`; line 363 `ALTER TABLE sessions ADD COLUMN workspace_root TEXT`; tests `schema_version_constant_is_8`, `fresh_install_has_workspace_root_column`, `v7_db_upgrades_to_v8_preserving_rows` all GREEN (12/12 ironhermes-state lib tests pass) |
| 3 | Session struct has pub workspace_root: Option<String> (D-W-1) | VERIFIED | `crates/ironhermes-state/src/lib.rs` Session struct exposes `workspace_root` field; integration test `session_workspace_root` reads it back |
| 4 | StateStore::create_session accepts workspace_root parameter (D-W-1) | VERIFIED | 6-arg signature in lib.rs; `create_session_with_workspace_round_trips` test passes |
| 5 | ironhermes-trajectory crate exists, builds, is workspace member (D-T-2) | VERIFIED | crate in `Cargo.toml workspace.members`; `cargo test -p ironhermes-trajectory --lib` 18/18 GREEN; roundtrip test 1/1 GREEN |
| 6 | TrajectoryEntry serializes with all 8 D-T-1 fields | VERIFIED | format.rs golden tests pass (impact_level snake_case, entry roundtrip) |
| 7 | ImpactLevel enum (Read=0, Write=5, SystemChange=10) snake_case (D-T-1) | VERIFIED | `impact_level_serializes_as_snake_case` test passes |
| 8 | TrajectoryWriter fsync per line + Drop sync_data (D-T-2, D-T-4) | VERIFIED | tests `append_persists_to_disk_after_each_call` + `drop_flushes_final_entry` GREEN |
| 9 | TrajectoryReader minimal API for Phase 25.4 consumption (D-T-2) | VERIFIED | reader.rs `read_all`; reader tests GREEN within 18-test lib suite |
| 10 | Workspace newtype with 6 fields (root, soul_path, agents_chain, memory_dir, skills_dir, tools_config) (D-W-1) | VERIFIED | `crates/ironhermes-core/src/workspace.rs`; 21 workspace lib tests pass (was 19; +2 from Plan 25.3-16 canonical_root_string ASCII + non-UTF-8 lossy) |
| 11 | resolve_from_cwd walks up looking for .hermes/.ironhermes markers (D-W-1) | VERIFIED | tests `resolve_walks_up_one_parent`, `resolve_walks_up_multiple_parents`, `resolve_returns_none_when_no_marker_anywhere` GREEN |
| 12 | Workspace.skills_dir exposed for Phase 25.4 Curator emission (D-W-2 forward-ref) | VERIFIED | workspace.rs field present; `skills_dir_is_top_level_not_under_marker` test GREEN |
| 13 | Tool::redact_args trait default returns raw clone (Discretion D-2) | VERIFIED | `crates/ironhermes-tools/src/registry.rs:66`; default-returns-input test GREEN |
| 14 | WebExtractTool::redact_args overrides + delegates to redact_secrets_in_url (Discretion D-2) | VERIFIED | `crates/ironhermes-tools/src/web_extract.rs:123`; 4 dedicated redact tests pass |
| 15 | CommandContext gains workspace + trajectory_writer fields with builder methods (D-W-2, D-T-3) | VERIFIED | `commands/context.rs`: TrajectoryWriterHandle trait at L150; fields + with_workspace + with_trajectory_writer builders present |
| 16 | TrajectoryWriterHandle trait defined in ironhermes-core for cycle-break (D-T-3) | VERIFIED | trait declared `pub trait TrajectoryWriterHandle: Send + Sync` in commands/context.rs |
| 17 | ironhermes-core remains a strict leaf crate (no path deps on other ironhermes-* crates) | VERIFIED | core Cargo.toml has no `ironhermes-*` path-deps |
| 18 | TrajectoryWriterHandleImpl in ironhermes-trajectory wraps Arc<Mutex<TrajectoryWriter>> (D-T-3) | VERIFIED | handle.rs implements the trait; trajectory crate depends on core (correct cycle-break direction) |
| 19 | PromptBuilder::with_workspace_root appends [Workspace: <root>] to Identity slot (D-W-2) | VERIFIED | `crates/ironhermes-agent/src/prompt_builder.rs:204` — `format!("{}", root.display())` agrees by construction with `canonical_root_string` (lossy U+FFFD); 4 dedicated tests pass per VALIDATION inventory |
| 20 | All 4 CommandContext-construction sites attach .with_workspace + .with_trajectory_writer | VERIFIED | parity-guard `workspace_wired_in_all_commandcontext_construction_sites` + `trajectory_writer_wired_in_all_commandcontext_construction_sites` GREEN; 13/13 invariants_25_3 tests GREEN with 0 ignored |
| 21 | GatewayRunner exposes set_workspace + set_trajectory_root setters (D-W-2, D-T-3) | VERIFIED | `crates/ironhermes-gateway/src/runner.rs:142 set_workspace`, `:176 set_trajectory_root`; both propagate via `try_write` into SessionStore; INV-25.3-06 widened to accept either set_trajectory_writer OR set_trajectory_root and is GREEN |
| 22 | AgentLoop appends per-tool TrajectoryEntry post-redact (D-T-1, D-T-3) | VERIFIED | `crates/ironhermes-agent/src/agent_loop.rs` with_trajectory_writer builder + per-tool append site + classify_impact_level classifier; tests GREEN |
| 23 | TrajectoryWriter path is workspace-scoped when workspace exists, else ~/.ironhermes/ fallback | VERIFIED | main.rs: `let trajectory_root: PathBuf = match &workspace { Some(ws) => ws.root.join(".ironhermes/sessions"), None => get_hermes_home().join("sessions") }` at line 2318; per-session subdirs lazily opened by SessionStore |
| 24 | SessionDirectoryExport produces 4-file layout messages.json + metadata.json + context.json + trajectories.jsonl (D-F-1) | VERIFIED | `crates/ironhermes-state/src/session_export.rs`; e2e tests `session_export_writes_4_file_layout`, `session_export_all_with_no_sessions_exits_zero`, `session_export_all_since_filter_excludes_old_sessions` 3/3 GREEN |
| 25 | StateStore::list_sessions_filtered with workspace_root_filter param (D-W-2) | VERIFIED | `list_sessions_filtered_by_workspace` integration test GREEN |
| 26 | /sessions --workspace slash command parses flag + dispatches to filtered method (D-W-2) | VERIFIED | `crates/ironhermes-core/src/commands/handlers.rs` `cmd_sessions` uses `ws.canonical_root_string()` post-Plan-25.3-16; empty-string filter normalized to None at handler boundary |
| 27 | hermes session export <id> + export-all CLI subcommands wired (D-F-1, D-F-2) | VERIFIED | `cli_args.rs` SessionSubcommand::{Export, ExportAll}; `session_cmd.rs` dispatcher; main.rs Commands::Session arm; 3/3 e2e tests GREEN |
| 28 | Resolved Workspace passed to StateStore::create_session on EVERY surface (D-W-1) | VERIFIED | **GAP CLOSED.** Plan 25.3-14: `crates/ironhermes-gateway/src/session.rs:95` `workspace: Option<Arc<Workspace>>` field; line 125 `pub fn set_workspace`; line 184-198 `get_or_create` reads `self.workspace.as_ref().map(\|ws\| ws.canonical_root_string())` and threads into `state.create_session`. `Plan 0 placeholder` literal removed (grep returns 0). Round-trip locked by `crates/ironhermes-gateway/tests/session_workspace_root.rs` (2/2 GREEN). INV-25.3-09 + INV-25.3-11 GREEN. |

**Score:** 28/28 truths verified

### Required Artifacts (load-bearing files for the must-haves)

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `crates/ironhermes-trajectory/{Cargo.toml, src/lib.rs, src/format.rs, src/writer.rs, src/reader.rs, src/handle.rs}` | new crate, builds, exports TrajectoryEntry/ImpactLevel/Writer/Reader/HandleImpl | VERIFIED | All files present; lib tests 18/18 + 1 roundtrip integration GREEN |
| `crates/ironhermes-trajectory/tests/{format.rs, roundtrip.rs}` | golden JSONL + roundtrip tests | VERIFIED | Both pass |
| `crates/ironhermes-core/src/workspace.rs` | Workspace newtype + resolve_from_cwd + canonical_root_string | VERIFIED | 21 lib tests GREEN (was 19; +2 from Plan 25.3-16) |
| `crates/ironhermes-core/src/commands/context.rs` (workspace + trajectory_writer fields, TrajectoryWriterHandle trait) | trait + 2 fields + 2 builders | VERIFIED | parity-guard GREEN |
| `crates/ironhermes-state/src/session_export.rs` | SessionDirectoryExport (4-file writer) | VERIFIED | session_export tests + e2e 3/3 GREEN |
| `crates/ironhermes-state/src/lib.rs` (schema v8, list_sessions_filtered, Session.workspace_root) | migration v8 + filter + struct field | VERIFIED | 12/12 ironhermes-state lib tests GREEN |
| `crates/ironhermes-cli/tests/invariants_25_3.rs` (parity-guard) | 13 cross-crate grep tests | VERIFIED | 13 passed; 0 failed; 0 ignored — all 5 gap-closure INV-25.3-07..11 + 25.3-12 are GREEN with no `#[ignore]` remaining |
| `crates/ironhermes-cli/tests/session_export_e2e.rs` | subprocess test of `hermes session export[-all]` | VERIFIED | 3/3 GREEN |
| `crates/ironhermes-cli/src/main.rs` (4-site wireup; canonical_root_string at create_session) | run_chat + run_single + run_gateway resolve workspace, open trajectory writer (or root for gateway), persist workspace_root | VERIFIED | run_chat ~620 + run_single ~1099 use `workspace.as_ref().map(\|ws\| ws.canonical_root_string())`; run_gateway at 2302/2318 builds `trajectory_root: PathBuf` and calls `runner.set_trajectory_root(...)` at line 2571; no `gateway-{}` literal (grep returns 0); no `gateway_trajectory_id` (grep returns 0) |
| `crates/ironhermes-cli/src/tui_rata/{app.rs, event_loop.rs, commands.rs}` | AppDeps fields + workspace resolution + ctx attach + create_session + with_workspace_root | VERIFIED | event_loop.rs L412 calls `s.create_session(&session_id, "cli-repl", ...)` ; L451 PromptBuilder source="cli-repl"; L456 calls `with_workspace_root(&ws.root)`; L525 seeds `system_message` into AppDeps; canonical_root_string used at create_session |
| `crates/ironhermes-gateway/src/runner.rs` (set_workspace + set_trajectory_root setters) | parity with set_toolset_session | VERIFIED | L142 set_workspace; L176 set_trajectory_root; both `try_write` into SessionStore; INV-25.3-06 widened-and-GREEN |
| `crates/ironhermes-gateway/src/handler.rs` (per-message ctx attach + per-session TrajectoryWriter from SessionStore) | ws.clone() + per-session writer | VERIFIED | L324 handler.set_workspace(ws.clone()); L682 reads `canonical_session_id`; L692 calls `store.get_or_create_trajectory_writer(&canonical_session_id)`; L695 `agent.with_trajectory_writer(tw)` |
| `crates/ironhermes-gateway/src/session.rs` (SessionStore propagates workspace + per-session trajectory writers) | workspace + trajectory_root + lazy-open writer cache | VERIFIED | L95 `workspace: Option<Arc<Workspace>>`; L102 `trajectory_root: Option<PathBuf>`; L107 `trajectory_writers: HashMap`; L125 `set_workspace`; L134 `set_trajectory_root`; L146 `get_or_create_trajectory_writer` lazy-open + cache; L184-198 get_or_create reads `self.workspace.as_ref().map(\|ws\| ws.canonical_root_string())` and passes via `state.create_session(...)` |
| `crates/ironhermes-agent/src/agent_loop.rs` (with_trajectory_writer + append site + classify_impact_level) | builder + per-tool-call append | VERIFIED | tests GREEN; tool.redact_args called before TrajectoryEntry constructed |
| `crates/ironhermes-agent/src/prompt_builder.rs` (with_workspace_root at slot 1) | append [Workspace: <root>] to Identity | VERIFIED | dedicated tests per VALIDATION inventory |
| `crates/ironhermes-tools/src/{registry.rs, web_extract.rs}` (Tool::redact_args) | trait default + WebExtractTool override | VERIFIED | both tests GREEN |
| `crates/ironhermes-gateway/tests/session_workspace_root.rs` (gap-closure regression) | SQLite round-trip for workspace_root on gateway sessions | VERIFIED | 2/2 GREEN: `gateway_session_with_workspace_persists_workspace_root` + `gateway_session_without_workspace_persists_null` |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| validate_role_name | RESERVED_ROLE_NAMES | iter | WIRED | config.rs:72 |
| StateStore::init_schema | run_migrations v8 | version gate `if current < 8` | WIRED | lib.rs:355-365 |
| ironhermes-trajectory lib | format/writer/reader/handle modules | pub mod + pub use | WIRED | lib.rs |
| Cargo.toml workspace | crates/ironhermes-trajectory | workspace.members | WIRED | confirmed |
| TrajectoryWriter::append | self.file.sync_data | fsync per line | WIRED | writer.rs |
| Drop for TrajectoryWriter | sync_data().ok() | final flush | WIRED | writer.rs |
| ironhermes-trajectory | ironhermes-core | path dep (cycle-break direction) | WIRED | trajectory has core dep; core has no trajectory dep |
| Tool::redact_args | AgentLoop trajectory append | per-tool-call call | WIRED | agent_loop.rs invokes `tool.redact_args(&raw)` before constructing TrajectoryEntry |
| WebExtractTool::redact_args | redact_secrets_in_url | delegation | WIRED | web_extract.rs:123 |
| PromptBuilder::with_workspace_root | PromptSlot::Identity | set_slot | WIRED | prompt_builder.rs |
| main.rs::run_* | resolve_from_cwd | workspace resolution | WIRED | 3 sites resolve workspace at L620, L1099, L2302 |
| build_cmd_ctx + build_command_context + handler | ctx.with_workspace + ctx.with_trajectory_writer | 4-site builder chain | WIRED | parity-guard 13/13 GREEN |
| GatewayRunner.set_workspace | SessionStore.workspace via try_write | propagation | WIRED | runner.rs:153 forwards Arc; SessionStore.set_workspace at session.rs:125 |
| GatewayRunner.set_trajectory_root | SessionStore.trajectory_root via try_write | propagation | WIRED | runner.rs:179; SessionStore.set_trajectory_root at session.rs:134 |
| GatewayMessageHandler::run_agent | per-session TrajectoryWriter | canonical_session_id lookup + lazy-open | WIRED | handler.rs:682-695 |
| AgentLoop trajectory_writer | append_json_line via TrajectoryWriterHandle | trait dispatch | WIRED | agent_loop.rs |
| SessionDirectoryExport::write | trajectories.jsonl source | std::fs::copy | WIRED | session_export.rs |
| Commands::Session(SessionSubcommand) | session_cmd::handle_session_command | dispatch arm | WIRED | main.rs Commands::Session |
| /export-session | cmd_export_session | CommandRouter | WIRED | handlers.rs |
| GatewayRunner.workspace | SessionStore.workspace → SessionStore::get_or_create → StateStore::create_session(workspace_root) | propagates via canonical_root_string | WIRED | **NEWLY WIRED in gap-closure.** Plans 25.3-14 + 25.3-16 chained the propagation; round-trip locked by `tests/session_workspace_root.rs` (2/2 GREEN). |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| Session row workspace_root (CLI run_chat / run_single) | `workspace_root_canon = workspace.as_ref().map(\|ws\| ws.canonical_root_string())` | `resolve_from_cwd(&cwd)` at session start | Yes — real path string (lossy U+FFFD for non-UTF-8) | FLOWING |
| Session row workspace_root (REPL build_app_deps) | same canonical encoding | resolved Workspace | Yes | FLOWING |
| Session row workspace_root (Gateway SessionStore::get_or_create) | `self.workspace.as_ref().map(\|ws\| ws.canonical_root_string())` | GatewayRunner.set_workspace → SessionStore.set_workspace | Yes — round-trip locked by integration test | FLOWING |
| TrajectoryEntry.args | `tool.redact_args(&raw)` then JSON-encoded | LLM ToolCall.args | Yes — post-redact | FLOWING |
| TrajectoryEntry.impact_level | `classify_impact_level(name)` | static name table | Yes — derived | FLOWING |
| `[Workspace: <root>]` Identity slot | `with_workspace_root(&ws.root)` (root.display() lossy = canonical_root_string) | resolved Workspace | Yes — when ws is Some; correctly omits when None | FLOWING |
| /sessions --workspace filter | `ws.canonical_root_string()` from ctx.workspace; empty-string normalized to None | CommandContext.workspace | Yes | FLOWING |
| /export-session output | StateStoreHandle::export_session_text | StateStore SQLite query | Yes | FLOWING |
| `hermes session export` 4-file layout | SessionDirectoryExport::write reads SessionExport from StateStore | StateStore | Yes — e2e tests verify on-disk artifacts | FLOWING |
| Gateway per-session trajectory file | `<trajectory_root>/<canonical_session_id>/trajectories.jsonl` | SessionStore.get_or_create_trajectory_writer (lazy-open keyed by canonical SQLite UUID) | Yes — reachable from `hermes session export <gw-session-id>` | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Workspace builds clean | `cargo build --workspace` (run via dependent test gates) | exit 0 | PASS |
| Trajectory crate green | `cargo test -p ironhermes-trajectory --lib` | 18 passed; 0 failed | PASS |
| Trajectory roundtrip | `cargo test -p ironhermes-trajectory` (integration) | 1 passed; 0 failed | PASS |
| ironhermes-state lib (schema v8 + workspace_root) | `cargo test -p ironhermes-state --lib` | 12 passed; 0 failed (incl. schema_migration_v8_tests::*, list_sessions_filtered_by_workspace, session_export::tests::*) | PASS |
| Workspace resolution + canonical_root_string | `cargo test -p ironhermes-core --lib workspace` | 21 passed; 0 failed (was 19; +2 from Plan 25.3-16) | PASS |
| Parity-guard 4-site wireup + gap-closure locks | `cargo test -p ironhermes-cli --test invariants_25_3` | 13 passed; 0 failed; **0 ignored** | PASS |
| Gateway workspace_root regression | `cargo test -p ironhermes-gateway --test session_workspace_root` | 2 passed; 0 failed | PASS |
| End-to-end session export | `cargo test -p ironhermes-cli --test session_export_e2e` | 3 passed; 0 failed | PASS |

### Requirements Coverage

Phase 25.3 uses CONTEXT-locked decision IDs (D-T-1..4, D-W-1..2, D-F-1..2, D-P0-1) per CONTEXT.md and the ROADMAP entry. No top-level `REQUIREMENTS.md` tags are pre-mapped to this phase (REQUIREMENTS.md uses `MEM-*`, `SESS-*`, `PRMT-*`, etc. categories). The decision-ID set is the authoritative requirement set for verification.

| Requirement | Source Plan(s) | Description | Status | Evidence |
| ----------- | -------------- | ----------- | ------ | -------- |
| D-T-1 | 01, 05, 09 | Per-tool-call TrajectoryEntry with 8 fields, post-redact args | SATISFIED | TrajectoryEntry struct, redact_args trait + override, AgentLoop append site all VERIFIED |
| D-T-2 | 01, 04, 06 | New ironhermes-trajectory crate; TrajectoryWriter (fsync) + TrajectoryReader | SATISFIED | crate exists; writer fsync per line + Drop sync_data; reader read_all; 18 lib tests GREEN |
| D-T-3 | 03, 06, 08, 09, 15 | All four surfaces write trajectories | SATISFIED | parity-guard 13/13 GREEN; gateway uses per-session canonical-UUID-keyed writers post-Plan-25.3-15 |
| D-T-4 | 04 | Append-only, no automatic eviction in v1 | SATISFIED | TrajectoryWriter has no eviction logic |
| D-F-1 | 10, 11 | Hybrid SQLite-canonical + on-demand 4-file export via `hermes session export <id>` + /export-session | SATISFIED | SessionDirectoryExport writes 4 files; CLI subcommand + slash both wired; e2e GREEN |
| D-F-2 | 11 | `hermes session export-all [--since YYYY-MM-DD]` bulk export | SATISFIED | session_cmd ExportAll arm; --since filter; e2e `session_export_all_since_filter_excludes_old_sessions` GREEN |
| D-W-1 | 00, 02, 06, 08, 13, 14, 16 | Workspace newtype + cwd walk-up + frozen-snapshot + sessions.workspace_root TEXT NULL + value-persisted on every surface | SATISFIED | Newtype, resolve_from_cwd, schema migration v8, struct field, CLI persistence — all VERIFIED. Plans 25.3-13 (REPL), 25.3-14 (gateway SessionStore), and 25.3-16 (canonical_root_string single-source-of-truth encoding) close the persistence-everywhere gap. Round-trip locked by `tests/session_workspace_root.rs` (2/2 GREEN). |
| D-W-2 | 02, 06, 07, 08, 10, 13, 15 | Workspace consumed by /sessions filter, prompt slot, trajectory dir scoping; Curator output destination wired | SATISFIED | All four consumers verified: /sessions --workspace filter (canonical_root_string + empty-string defense IN-03); [Workspace:] Identity-slot prompt line on all chat surfaces (REPL added by Plan 25.3-13); workspace-scoped trajectory paths in all 3 surfaces (gateway uses per-session canonical UUID post-Plan-25.3-15); skills_dir field exposed |
| D-P0-1 | 00 | RESERVED_ROLE_NAMES extended 6→7 with "curator"; validation test updated | SATISFIED | Array literal contains "curator" at config.rs:61; renamed test passes; doc comment says "seven reserved helper-task roles" |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `crates/ironhermes-tools/tests/browser_integration.rs` | 90 | Pre-existing `register_browser_tools_with_vision` arg-count mismatch | Info | Pre-existing per Plan 11 SUMMARY; out of phase scope; unrelated to 25.3 |
| `crates/ironhermes-cli/src/main.rs` | various | 27 pre-existing warnings (`finish_prompt` etc.) | Info | Compiler `dead_code`/`unused_imports` warnings; pre-existing; not introduced by Phase 25.3 |
| `crates/ironhermes-core/src/commands/handlers.rs` | 1662 | `dispatch_all_todo_stubs_return_not_yet_available` lists `"cron"` despite cron promotion in `96b19c1` (Phase 22.4.2.1) | Info | Documented and explicitly deferred in Plan 25.3-17 SUMMARY; predates Phase 25.3; not in any 25.3-12..16 modified surface |

The original `gateway/session.rs:113` "Plan 0 placeholder" hardcoded-None entry was closed by Plan 25.3-14 (commit `0b2394c`); `grep -c 'Plan 0 placeholder' crates/ironhermes-gateway/src/session.rs` returns **0**. The original `main.rs::run_gateway` `gateway-{}` literal + `gateway_trajectory_id` were closed by Plan 25.3-15 (commit `c29377e`); both greps return **0**.

### Human Verification Required

See `human_verification:` block in frontmatter — three Manual-Only items per VALIDATION.md (cache hit rate, bulk-export at scale, prompt absence in global mode). Each is explicitly classified as Manual-Only in VALIDATION.md and is not a gap. They are auditor-grade checks that require live infrastructure (Anthropic API for cache stats; populated SQLite at scale; running outside any workspace marker).

### Gaps Summary

The phase achieves **28/28 must-haves**. The original verifier-flagged BLOCKER (Telegram gateway sessions persisted `workspace_root=NULL`) was closed by Plan 25.3-14; the four CR-XX critical findings from `25.3-REVIEW.md` were closed by Plans 25.3-13 (CR-01 + CR-04), 25.3-15 (CR-02), and 25.3-16 (CR-03 + WR-01 + IN-03); six new INV-25.3-XX parity-guards (07..12) lock the closures against re-introduction.

This independent re-verification (the third overall) re-ran the entire validation harness and confirms:

- 13/13 invariants_25_3 parity-guards GREEN (0 ignored)
- 21/21 workspace lib tests GREEN (was 19; +2 from canonical_root_string)
- 18/18 trajectory lib + 1/1 roundtrip GREEN
- 12/12 ironhermes-state lib tests GREEN (schema_migration_v8 + list_sessions_filtered_by_workspace + session_export)
- 3/3 session_export_e2e GREEN
- 2/2 gateway session_workspace_root regression GREEN

No outstanding gaps. Residual warnings (WR-03..06 + IN-01..02) are explicitly deferred per the table below — none block the phase contract.

## Gap Closure (2026-05-03 — Plans 25.3-12..17)

Phase 25.3 originally verified 27/28 must-haves with a single phase-blocking gap (must-have #28 — Telegram gateway sessions persisted `workspace_root=NULL`). The follow-on code review (`25.3-REVIEW.md`) flagged 4 CRITICAL findings that were folded into the gap-closure batch:

| Plan | Wave | Closes | Description |
|------|------|--------|-------------|
| 25.3-12 | 7 | (RED gates) | Add INV-25.3-07..11 to lock the gap-closure contract before code changes land (`#[ignore]`-gated RED tests) |
| 25.3-13 | 8 | CR-01 + CR-04 | ratatui REPL: state.create_session + PromptBuilder.with_workspace_root |
| 25.3-14 | 8 | VERIFIER BLOCKER + WR-02 | Gateway SessionStore: thread workspace_root into get_or_create + 2-test SQLite regression test |
| 25.3-15 | 9 | CR-02 | Gateway trajectory canonical UUID: per-session writers cached in SessionStore (was process-wide `gateway-<uuid>`) |
| 25.3-16 | 10 | CR-03 + WR-01 + IN-03 | `Workspace::canonical_root_string` single-source-of-truth encoding + empty-string filter normalization |
| 25.3-17 | 11 | (re-verify + docs) | Re-run harness, regenerate VERIFICATION.md to 28/28, update VALIDATION.md inventory |

New parity-guards: 6 (INV-25.3-07 through INV-25.3-12). Original 7 + new 6 = **13 total** (all GREEN, 0 ignored).
New integration tests: 2 (gateway/tests/session_workspace_root.rs).
New unit tests: 2 (workspace::tests::canonical_root_string_returns_path_for_ascii + canonical_root_string_lossy_subs_non_utf8_unix).

Residual warnings explicitly DEFERRED (not blocking phase verification):

| ID | Source | Title | Defer-to | Rationale |
|----|--------|-------|----------|-----------|
| WR-03 | 25.3-REVIEW.md | `run_export_all` exits 0 even if every session failed | Phase 25.4 / operator-CLI polish | Operator-UX issue, not a correctness gap; per-session warnings are still emitted to stderr. Does not affect D-F-1/D-F-2 contract. |
| WR-04 | 25.3-REVIEW.md | TrajectoryWriter creates ghost empty dirs for tool-less turns | Phase 25.4 retention policy | D-T-4 explicitly states "no automatic eviction in v1"; Plan 25.3-15's lazy-open per-session model already reduces this surface. Will revisit when Curator retention/pruning policy lands in 25.4. |
| WR-05 | 25.3-REVIEW.md | `cmd_export_session` slash command not workspace-aware | Phase 25.4 | Curator owns workspace-scoped export flow in 25.4; CLI subcommand `hermes session export <id> --output <dir>` already supports operator overrides as a workaround. |
| WR-06 | 25.3-REVIEW.md | `from_root_and_marker` TOCTOU on SOUL.md move | Document only | Benign per the review (SOUL.md doesn't move during normal operation); resolution is frozen-snapshot per session, so the window is bounded. |
| IN-01 | 25.3-REVIEW.md | TrajectoryEntry `ts` constructor vs. test-literal divergence | Document only | Benign drift surface; constructor uses `Utc::now().to_rfc3339()` consistently in production code. |
| IN-02 | 25.3-REVIEW.md | `cmd_sessions` duplicate `--workspace` last-wins | UX polish | Documented behavior; operator-typo case. |

This re-verification confirms the gap closure: 2026-05-03T20:28:36Z. Status: **verified**.

**Phase 25.4 readiness:** All Curator prerequisites are now CLEAN — `RESERVED_ROLE_NAMES["curator"]`, `TrajectoryReader` API, `SessionDirectoryExport`, `Workspace.skills_dir`, per-workspace session metadata on EVERY surface (CLI + REPL + gateway), per-session trajectories at canonical UUIDs on the gateway, REPL session lifecycle parity with classic-TUI.

---

*Initial verification: 2026-05-03T18:03:21Z (initial — prior status had a single phase-blocking gap, score 27/28)*
*Gap-closure re-verification: 2026-05-03T19:56:11Z (Plan 25.3-17 docs close-out — verified at 28/28)*
*Independent re-verification: 2026-05-03T20:28:36Z (this update — gap-closure independently re-verified at 28/28)*
*Verifier: Claude (gsd-verifier)*
