---
phase: 25.3-session-workspace-parity
plan: 17
type: execute
wave: 11
depends_on: [13, 14, 15, 16]
files_modified:
  - .planning/phases/25.3-session-workspace-parity/25.3-VERIFICATION.md
  - .planning/phases/25.3-session-workspace-parity/25.3-VALIDATION.md
autonomous: true
gap_closure: true
requirements:
  - D-T-1
  - D-T-2
  - D-T-3
  - D-W-1
  - D-W-2
user_setup: []

must_haves:
  truths:
    - "Full workspace test suite runs and passes (cargo build + cargo test --workspace)"
    - "All 12 INV-25.3-* parity-guards are GREEN"
    - "VERIFICATION.md is regenerated showing 28/28 must-haves verified (was 27/28)"
    - "VALIDATION.md is updated to reflect the 5 new gap-closure parity-guards as Automated tests"
    - "Gap-closure SUMMARY documents which CR-XX was closed by which plan and the residual warnings (WR-03..06 + IN-01..02) explicitly deferred"
  artifacts:
    - path: ".planning/phases/25.3-session-workspace-parity/25.3-VERIFICATION.md"
      provides: "Updated verification status: 28/28, status=verified"
      contains: "28/28"
    - path: ".planning/phases/25.3-session-workspace-parity/25.3-VALIDATION.md"
      provides: "Updated test inventory listing INV-25.3-07..12"
      contains: "INV-25.3-12"
  key_links:
    - from: "All gap-closure plans (25.3-12..16)"
      to: "Updated VERIFICATION.md must-have status table"
      via: "Re-running grep + cargo test gates and updating the report"
      pattern: "score: 28/28"
---

<objective>
Final gap-closure plan: re-run the verifier-class harness over the closed gaps, regenerate `25.3-VERIFICATION.md` with the corrected score (28/28 must-haves), update `25.3-VALIDATION.md` to inventory the 5 new gap-closure parity-guards as Automated tests, and produce a gap-closure SUMMARY documenting:
- Which CR-XX from 25.3-REVIEW.md was closed by which plan
- Which WR-XX / IN-XX residual warnings remain explicitly deferred (and to where)
- The full test count delta (parity-guards + workspace unit tests)

Purpose: Re-verify Phase 25.3 to a 28/28 must-have score so `/gsd-execute-phase 25.3 --gaps-only` can advance the milestone state machine. Without this update, STATE.md still shows Phase 25.3 as `gaps_found` and milestone progress is blocked.

Output: Three documentation updates + one SUMMARY file. NO source code changes (this plan is documentation-only; all source-code work is owned by Plans 25.3-13..16).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25.3-session-workspace-parity/25.3-CONTEXT.md
@.planning/phases/25.3-session-workspace-parity/25.3-VERIFICATION.md
@.planning/phases/25.3-session-workspace-parity/25.3-VALIDATION.md
@.planning/phases/25.3-session-workspace-parity/25.3-REVIEW.md
@.planning/phases/25.3-session-workspace-parity/25.3-12-SUMMARY.md
@.planning/phases/25.3-session-workspace-parity/25.3-13-SUMMARY.md
@.planning/phases/25.3-session-workspace-parity/25.3-14-SUMMARY.md
@.planning/phases/25.3-session-workspace-parity/25.3-15-SUMMARY.md
@.planning/phases/25.3-session-workspace-parity/25.3-16-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the verifier-class harness and capture results</name>
  <files>tmp/25.3-gap-closure-harness.log (transient — not committed)</files>
  <read_first>
    - .planning/phases/25.3-session-workspace-parity/25.3-VERIFICATION.md (the existing report — read the must-have table to understand which 27 are already GREEN and which one (#28) was the BLOCKER)
    - .planning/phases/25.3-session-workspace-parity/25.3-12-SUMMARY.md, 13-SUMMARY.md, 14-SUMMARY.md, 15-SUMMARY.md, 16-SUMMARY.md (gap-closure plan SUMMARYs to confirm what each plan reports as DONE)
  </read_first>
  <action>
    Run the full gap-closure harness (single shell pipeline), capture to a transient log
    file, and inspect for any FAILED markers:
    
    ```bash
    set -e
    LOG=tmp/25.3-gap-closure-harness.log
    mkdir -p tmp
    
    {
        echo "===== cargo build --workspace ====="
        cargo build --workspace
    
        echo
        echo "===== cargo test --workspace --lib ====="
        cargo test --workspace --lib
    
        echo
        echo "===== cargo test -p ironhermes-trajectory ====="
        cargo test -p ironhermes-trajectory
    
        echo
        echo "===== cargo test -p ironhermes-state ====="
        cargo test -p ironhermes-state
    
        echo
        echo "===== cargo test -p ironhermes-core --lib workspace ====="
        cargo test -p ironhermes-core --lib workspace
    
        echo
        echo "===== cargo test -p ironhermes-cli --test invariants_25_3 ====="
        cargo test -p ironhermes-cli --test invariants_25_3
    
        echo
        echo "===== cargo test -p ironhermes-cli --test session_export_e2e ====="
        cargo test -p ironhermes-cli --test session_export_e2e
    
        echo
        echo "===== cargo test -p ironhermes-gateway --test session_workspace_root ====="
        cargo test -p ironhermes-gateway --test session_workspace_root
    } 2>&1 | tee "$LOG"
    
    # Count FAILED lines.
    if grep -E "^test result.*[1-9][0-9]* failed" "$LOG" > /dev/null; then
        echo "FAIL: at least one cargo test reported failures"
        grep -E "^test result.*failed" "$LOG"
        exit 1
    fi
    
    # Count INV-25.3 tests run.
    INV_COUNT=$(grep -cE "test invariant_25_3_(0[1-9]|1[0-9])" "$LOG" || true)
    echo "INV-25.3-* tests observed: $INV_COUNT"
    ```
    
    Confirm:
    - All cargo test commands return exit 0 (no FAILED).
    - At least 12 distinct INV-25.3-XX tests run and all pass.
    - The new `session_workspace_root` integration test runs and passes (2/2).
    - The trajectory crate tests pass (>= 18).
    - The state crate schema migration tests pass.
    
    If any failure surfaces, STOP and report the specific test → plan-owner mapping
    rather than re-running. The plan dependency chain (depends_on: [13, 14, 15, 16])
    means a failure here indicates one of those plans regressed before this plan
    started.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&amp;1 | tail -5 && cargo test -p ironhermes-cli --test invariants_25_3 2>&amp;1 | grep -E 'test result'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` exits 0
    - `cargo test -p ironhermes-cli --test invariants_25_3` reports `0 failed` and `>= 12 passed`
    - `cargo test -p ironhermes-gateway --test session_workspace_root` reports `0 failed` and `2 passed`
    - `cargo test -p ironhermes-trajectory` reports `0 failed`
    - `cargo test -p ironhermes-state` reports `0 failed`
    - tmp/25.3-gap-closure-harness.log captured
  </acceptance_criteria>
  <done>
    Harness output ready for Task 2 to consume.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update VERIFICATION.md to 28/28 + regenerate the must-have table</name>
  <files>.planning/phases/25.3-session-workspace-parity/25.3-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/25.3-session-workspace-parity/25.3-VERIFICATION.md (entire file — preserve format; only change the gap section + score + status)
    - tmp/25.3-gap-closure-harness.log (from Task 1)
  </read_first>
  <action>
    Update `25.3-VERIFICATION.md`:
    
    1. Frontmatter:
       - `verified: <today's ISO timestamp>` (use `date -u +%Y-%m-%dT%H:%M:%SZ`)
       - `status: verified` (was `gaps_found`)
       - `score: 28/28 must-haves verified` (was `27/28`)
       - `re_verification: true` (or `re_verified: <prior date>` — match existing field name; see grep)
       - DELETE the entire `gaps:` block (the BLOCKER is closed by Plan 25.3-14)
       - KEEP `human_verification:` block (the 3 manual items remain Manual-Only per VALIDATION.md)
    
    2. In the `## Goal Achievement → Observable Truths` table, row #28: change Status from `**FAILED**` to `VERIFIED` and replace the Evidence column with:
    
       ```
       Plan 25.3-14 closed the verifier blocker: `crates/ironhermes-gateway/src/session.rs::SessionStore`
       gained an `Option<Arc<Workspace>>` field + `set_workspace` setter; `GatewayRunner::set_workspace`
       propagates to the inner SessionStore; `get_or_create` reads `self.workspace` and threads
       `canonical_root_string` (Plan 25.3-16) into `state.create_session(...)`.
       Regression test `crates/ironhermes-gateway/tests/session_workspace_root.rs` (2/2 GREEN)
       proves the round-trip into SQLite. INV-25.3-09 + INV-25.3-11 GREEN.
       ```
    
    3. In `## Required Artifacts`, row for `crates/ironhermes-gateway/src/session.rs`:
       change Status from `**STUB**` to `VERIFIED` and update Details column to reference Plan 25.3-14.
    
    4. In `## Key Link Verification`, row `GatewayRunner.workspace → SessionStore::get_or_create`:
       change Status from `**NOT_WIRED**` to `WIRED` and update the Details column.
    
    5. In `## Data-Flow Trace (Level 4)`, row `Session row workspace_root (Gateway surface)`:
       change `(no source — hardcoded)` to `self.workspace.as_ref().map(|ws| ws.canonical_root_string())`,
       change Status from `DISCONNECTED` to `FLOWING`.
    
    6. In `## Anti-Patterns Found`, REMOVE the `crates/ironhermes-gateway/src/session.rs:113`
       hardcoded-None entry (the literal "Plan 0 placeholder" comment was deleted by Plan 25.3-14).
       KEEP the unrelated browser_integration entry.
    
    7. In `## Requirements Coverage`, row for `D-W-1`: change Status from `**PARTIAL**` to `SATISFIED`
       and update Evidence to reference Plan 25.3-14 closing the gateway gap.
    
    8. APPEND a new section after `## Gaps Summary`:
    
       ```markdown
       ## Gap Closure (2026-05-03 — Plans 25.3-12..16)
       
       Phase 25.3 originally verified 27/28 must-haves with a single phase-blocking
       gap (must-have #28 — Telegram gateway sessions persisted `workspace_root=NULL`).
       The follow-on code review (`25.3-REVIEW.md`) flagged 4 CRITICAL findings that
       were folded into the gap-closure batch:
       
       | Plan | Closes | Description |
       |------|--------|-------------|
       | 25.3-12 | (RED gates) | Add INV-25.3-07..11 to lock the gap-closure contract before code changes land |
       | 25.3-13 | CR-01 + CR-04 | ratatui REPL: state.create_session + PromptBuilder.with_workspace_root |
       | 25.3-14 | VERIFIER BLOCKER + WR-02 | Gateway SessionStore: thread workspace_root into get_or_create + regression test |
       | 25.3-15 | CR-02 | Gateway trajectory canonical UUID: per-session writers cached in SessionStore (was `gateway-<uuid>`) |
       | 25.3-16 | CR-03 + WR-01 + IN-03 | Workspace::canonical_root_string single-source-of-truth encoding |
       
       New parity-guards: 6 (INV-25.3-07 through INV-25.3-12). Original 7 + new 6 = 13 total.
       
       Residual warnings explicitly DEFERRED (not blocking phase verification):
       - WR-03 (export-all exits 0 even if every session failed) — defer to Phase 25.4 or operator-CLI polish phase.
       - WR-04 (TrajectoryWriter ghost empty dirs for tool-less turns) — defer; D-T-4 says "no eviction in v1".
       - WR-05 (cmd_export_session not workspace-aware) — defer to Phase 25.4 (Curator owns workspace-scoped export).
       - WR-06 (from_root_and_marker TOCTOU on SOUL.md move) — benign; document only.
       - IN-01 (TrajectoryEntry ts test/runtime divergence) — defer; benign drift surface.
       - IN-02 (cmd_sessions duplicate --workspace last-wins) — defer; UX polish.
       
       Full re-verification: 2026-05-03 (this update). Status: verified.
       ```
  </action>
  <verify>
    <automated>grep -c '28/28' .planning/phases/25.3-session-workspace-parity/25.3-VERIFICATION.md && grep -c 'status: verified' .planning/phases/25.3-session-workspace-parity/25.3-VERIFICATION.md && grep -c 'Gap Closure (2026-05-03' .planning/phases/25.3-session-workspace-parity/25.3-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '28/28' .planning/phases/25.3-session-workspace-parity/25.3-VERIFICATION.md` returns >= 1
    - `grep -c 'status: verified' .planning/phases/25.3-session-workspace-parity/25.3-VERIFICATION.md` returns >= 1
    - `grep -c 'gaps_found' .planning/phases/25.3-session-workspace-parity/25.3-VERIFICATION.md` returns 0
    - The `gaps:` frontmatter block is removed
    - Must-have #28 row marked VERIFIED
    - The `## Gap Closure (2026-05-03` section is appended with the plan→CR mapping table
    - Each WR-XX deferral has an explicit rationale (not just listed)
  </acceptance_criteria>
  <done>
    VERIFICATION.md reflects 28/28 verified status. Phase 25.3 milestone state can advance.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update VALIDATION.md test inventory + write 25.3-17-SUMMARY.md gap-closure summary</name>
  <files>.planning/phases/25.3-session-workspace-parity/25.3-VALIDATION.md, .planning/phases/25.3-session-workspace-parity/25.3-17-SUMMARY.md</files>
  <read_first>
    - .planning/phases/25.3-session-workspace-parity/25.3-VALIDATION.md (entire file — understand the existing test inventory format and how Manual-Only items are listed)
  </read_first>
  <action>
    **Edit 1 — VALIDATION.md:** APPEND a new subsection under the parity-guard test inventory:
    
    ```markdown
    ### Phase 25.3 Gap-Closure Parity-Guards (Plans 25.3-12..16)
    
    Six new INV-25.3-XX tests were added during gap closure to lock the contract against
    re-introduction of the regressions found by 25.3-VERIFICATION.md (BLOCKER must-have #28)
    and 25.3-REVIEW.md (CR-01..CR-04). All Automated.
    
    | Test | Locks | Plan |
    |------|-------|------|
    | INV-25.3-07 | REPL build_app_deps calls state.create_session | 25.3-13 |
    | INV-25.3-08 | with_workspace_root in main.rs (>=2) + event_loop.rs + handler.rs | 25.3-13, 25.3-15 (handler line 543 pre-existing) |
    | INV-25.3-09 | gateway/session.rs no `Plan 0 placeholder`; references `workspace` >= 2x | 25.3-14 |
    | INV-25.3-10 | main.rs no `gateway-{}` literal, no `gateway_trajectory_id` | 25.3-15 |
    | INV-25.3-11 | gateway/session.rs references `workspace_root` >= 1 (impl signature/field/pass-through) | 25.3-14 |
    | INV-25.3-12 | canonical_root_string used at all 4 persistence + 1 filter site | 25.3-16 |
    
    Run command: `cargo test -p ironhermes-cli --test invariants_25_3`
    Expected: 13 passed, 0 failed (was 7 originally; +6 from gap closure).
    
    ### New Phase 25.3 Gap-Closure Integration Test
    
    | Test File | Tests | Locks | Plan |
    |-----------|-------|-------|------|
    | `crates/ironhermes-gateway/tests/session_workspace_root.rs` | 2 (with-workspace + without-workspace) | gateway sessions round-trip workspace_root into SQLite | 25.3-14 |
    
    Run command: `cargo test -p ironhermes-gateway --test session_workspace_root`
    Expected: 2 passed, 0 failed.
    ```
    
    **Edit 2 — write 25.3-17-SUMMARY.md** as the gap-closure summary file. Use the
    `templates/summary.md` shape. Required content:
    
    ```markdown
    ---
    phase: 25.3-session-workspace-parity
    plan: 17
    completed: <today's ISO timestamp>
    gap_closure: true
    closes:
      - VERIFICATION.md must-have #28 (BLOCKER): Telegram gateway workspace_root persistence
      - REVIEW.md CR-01: ratatui REPL state.create_session
      - REVIEW.md CR-02: Gateway trajectory canonical session UUID
      - REVIEW.md CR-03: Workspace path encoding canonicalization
      - REVIEW.md CR-04: ratatui REPL prompt_builder.with_workspace_root
      - REVIEW.md WR-01: filter encoding mismatch (subset of CR-03)
      - REVIEW.md WR-02: SessionStore hardcoded None (subset of #28)
      - REVIEW.md IN-03: empty-string workspace filter normalization
    deferred:
      - WR-03: bulk-export error reporting (operator-CLI polish phase)
      - WR-04: TrajectoryWriter ghost empty dirs (Phase 25.4 retention policy)
      - WR-05: workspace-aware /export-session output dir (Phase 25.4)
      - WR-06: from_root_and_marker TOCTOU (benign; document only)
      - IN-01: TrajectoryEntry ts test divergence (benign)
      - IN-02: cmd_sessions duplicate --workspace last-wins (UX polish)
    ---
    
    # Phase 25.3 Gap Closure: Final Summary
    
    Phase 25.3 shipped the trajectory crate, hybrid flat-file export, Workspace newtype,
    and Plan 0 RESERVED_ROLE_NAMES["curator"] extension across 12 plans (25.3-00..11).
    Verifier returned 27/28 must-haves with a single phase-blocking gap on the Telegram
    gateway surface. Code reviewer found 4 CRITICAL issues that would have shipped if
    treated as Warning. Plans 25.3-12..16 closed all 5 issues; this plan re-verifies
    and updates documentation.
    
    ## Plan-by-plan close-out
    
    | Plan | Wave | Files Modified | Tests Added | Issue Closed |
    |------|------|----------------|-------------|--------------|
    | 25.3-12 | 7 | 1 (test only) | 5 RED parity-guards | (locks contract for 25.3-13..16) |
    | 25.3-13 | 8 | 2 (event_loop + app) | INV-25.3-07, -08 turn GREEN | CR-01 + CR-04 |
    | 25.3-14 | 8 | 3 (session + runner + new test) | session_workspace_root.rs (2 tests); INV-25.3-09, -11 GREEN | VERIFIER BLOCKER + WR-02 |
    | 25.3-15 | 8 | 4 (main + runner + handler + session) | INV-25.3-10 GREEN | CR-02 |
    | 25.3-16 | 9 | 6 (workspace + 4 callers + invariants) | 2 workspace unit tests + INV-25.3-12 | CR-03 + WR-01 + IN-03 |
    | 25.3-17 | 10 | 2 (VERIFICATION + VALIDATION docs) | (re-verify only) | Documentation close-out |
    
    ## Test count delta
    
    - Parity-guards: 7 → 13 (+6 new INV-25.3-XX)
    - Workspace unit tests: 19 → 21 (+2: ASCII + non-UTF-8 lossy)
    - Gateway integration tests: 0 → 2 (new file: session_workspace_root.rs)
    - Total new tests: +10
    
    ## Verification Re-run
    
    All gates GREEN:
    - `cargo build --workspace` exit 0
    - `cargo test --workspace --lib` exit 0 (no failures)
    - `cargo test -p ironhermes-cli --test invariants_25_3`: 13 passed, 0 failed
    - `cargo test -p ironhermes-gateway --test session_workspace_root`: 2 passed, 0 failed
    - `cargo test -p ironhermes-trajectory`: >= 18 passed, 0 failed
    - `cargo test -p ironhermes-state`: schema-migration-v8 + workspace-row tests pass
    - `cargo test -p ironhermes-core --lib workspace`: 21 passed, 0 failed
    
    ## Residual Warnings (Explicitly Deferred)
    
    See VERIFICATION.md "Gap Closure (2026-05-03)" section for the deferred-warning
    rationale. Each WR-XX is mapped to either Phase 25.4 (Curator) or to a future
    operator-CLI polish phase.
    
    ## Phase 25.4 Readiness
    
    All Phase 25.4 Curator prerequisites are now CLEAN:
    - RESERVED_ROLE_NAMES["curator"] (Plan 25.3-00)
    - TrajectoryReader API (Plan 25.3-04)
    - SessionDirectoryExport 4-file layout (Plan 25.3-10)
    - Workspace.skills_dir field (Plan 25.3-02)
    - **Per-workspace session metadata on EVERY surface** (this gap closure — Plan 25.3-14 + 25.3-16)
    - **Per-session trajectories at canonical UUIDs on the gateway** (this gap closure — Plan 25.3-15)
    - **REPL session lifecycle parity with classic-TUI** (this gap closure — Plan 25.3-13)
    
    Phase 25.3 is unblocked. Milestone state machine can advance.
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/25.3-session-workspace-parity/25.3-17-SUMMARY.md && grep -c 'VERIFIER BLOCKER' .planning/phases/25.3-session-workspace-parity/25.3-17-SUMMARY.md && grep -c 'INV-25.3-12' .planning/phases/25.3-session-workspace-parity/25.3-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 25.3-17-SUMMARY.md exists with frontmatter listing the 5 closed issues + 6 deferred warnings
    - VALIDATION.md contains the new "Phase 25.3 Gap-Closure Parity-Guards" section
    - VALIDATION.md inventories INV-25.3-07 through INV-25.3-12
    - VALIDATION.md inventories the new session_workspace_root.rs file
    - 25.3-17-SUMMARY.md documents the test count delta (+10)
    - 25.3-17-SUMMARY.md confirms all gates GREEN
  </acceptance_criteria>
  <done>
    Documentation is consistent across VERIFICATION.md, VALIDATION.md, and the gap-closure SUMMARY. The phase can be marked complete in the milestone state machine.
  </done>
</task>

</tasks>

<verification>
- VERIFICATION.md frontmatter shows `status: verified` + `score: 28/28`
- VERIFICATION.md `gaps:` block is removed
- VERIFICATION.md `## Gap Closure (2026-05-03)` section is appended with the 5-plan mapping
- VALIDATION.md inventories the 6 new INV-25.3-XX tests + the new session_workspace_root.rs file
- 25.3-17-SUMMARY.md documents the test delta (+10), the 5 closed issues, and the 6 deferred warnings
</verification>

<success_criteria>
- Phase 25.3 verifier-class score: 28/28 must-haves verified
- All 4 CRITICAL code-review findings closed (CR-01..CR-04)
- Phase 25.4 prerequisites are clean and ready
- Milestone state machine can advance past Phase 25.3
</success_criteria>

<output>
The 25.3-17-SUMMARY.md IS the gap-closure SUMMARY this plan produces. No additional summary file is needed beyond what Task 3 writes.
</output>
