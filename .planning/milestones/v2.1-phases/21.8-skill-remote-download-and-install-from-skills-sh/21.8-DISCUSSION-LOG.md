# Phase 21.8: Skill Remote Download and Install from skills.sh — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `21.8-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-04-22
**Phase:** 21.8-skill-remote-download-and-install-from-skills-sh
**Areas discussed:** Phase intent, 19.1 fate, Crate layout, CLI surface, Auth strategy, Install flow, Lock file, Security primitives, Install UX, Phase scope, Post-install, Error handling, Agent identity

---

## Phase intent (multiSelect)

| Option | Description | Selected |
|--------|-------------|----------|
| Verify end-to-end works | Hardening/UAT phase. Run a real install against skills.sh, fix breakage. | ✓ |
| Registry schema mismatch | Hardcoded `RegistryEntry` may not match what skills.sh serves. | ✓ |
| Install UX polish | Download progress, error messages, verbose flag. | ✓ |
| Post-install activation | Installed skill usable without restart. | ✓ |

**User's choice:** All four selected.
**Notes:** User pointed to `.planning/21.8-handoff.md` — "the current install does not work and needs to be updated with this." Handoff revealed 19.1 `SkillsShSource` is architecturally wrong (assumes nonexistent `/registry.json`); real skills.sh uses a blob API at `/api/download`. Scope re-interpreted as a rewrite of skills.sh support based on the reference TypeScript implementation at `/Users/twilson/code/skills`.

## Selected gray areas (multiSelect)

| Option | Selected |
|--------|----------|
| Install UX + progress | ✓ |
| Registry contract + robustness | ✓ |
| Default command shape | ✓ |
| Post-install session behavior | ✓ |

**User's choice:** All four selected.

---

## 19.1 skills.sh adapter fate

| Option | Description | Selected |
|--------|-------------|----------|
| Rip out + replace | Delete `SkillsShSource`; new blob-API adapter. `GitHubSource` + `WellKnownSkillSource` stay. | ✓ |
| Rewrite in place | Keep `SkillsShSource` name, swap implementation to blob API. | |
| Build parallel + deprecate | New `SkillsShBlobSource` alongside broken one. | |

**User's choice:** Rip out + replace (recommended).
**Notes:** Locked as D-01 in CONTEXT.md.

## Crate layout

| Option | Description | Selected |
|--------|-------------|----------|
| Extend `ironhermes-hub` | Add `blob.rs` + `lock.rs` + `sanitize.rs` modules to existing crate. | ✓ |
| New `ironhermes-skills-hub` crate | Dedicated crate. | |
| Extend `ironhermes-core` | Colocate with `SkillRegistry`. | |

**User's choice:** Extend `ironhermes-hub` (recommended).
**Notes:** Locked as D-02.

## CLI surface

| Option | Description | Selected |
|--------|-------------|----------|
| `hermes skills install <name>` | Keep plural; bare names already route to skills-sh. | ✓ |
| `hermes skill install <name>` | Match reference singular verb. | |
| Both aliases | Accept singular + plural. | |

**User's choice:** Plural `hermes skills install <name>` (recommended).
**Notes:** Locked as D-03. D-04 adds verb rename: `uninstall` → `remove` with `uninstall` kept as hidden alias.

## Auth strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse 19.1 `GitHubAuth::resolve()` | Anonymous → env token → `gh auth token` fallback. | ✓ |
| Anonymous only | Hits 60/hr unauthenticated rate limit. | |
| Require token | Hard fail without `GITHUB_TOKEN`. | |

**User's choice:** Reuse 19.1 `GitHubAuth::resolve()` (recommended).
**Notes:** Locked as D-05.

## Install flow

| Option | Description | Selected |
|--------|-------------|----------|
| Mirror reference exactly | Trees API → raw.githubusercontent → skills.sh/api/download. | ✓ |
| Skip Trees API, blob-first | Direct `/api/download` by name. | |
| Git clone fallback | Fall back to simple-git. | |

**User's choice:** Mirror reference exactly (recommended).
**Notes:** Locked as D-06. D-07 adds `SKILLS_DOWNLOAD_URL` env override; D-08 sets 10s per-call timeout; D-09 hard-rejects git clone fallback.

## Lock file

| Option | Description | Selected |
|--------|-------------|----------|
| Port lock file exactly | `~/.ironhermes/skills-lock.json` with `computedHash` + `snapshotHash`. | ✓ |
| Extend existing `HubManifest` | Fold into 19.1 per-skill manifest format. | |
| Skip lock file for v1 | Manifest only, no hash bookkeeping. | |

**User's choice:** Port lock file exactly (recommended).
**Notes:** Locked as D-10..D-14. D-15 adds one-way migration from 19.1 `.hub/*.json` manifests to the new lock file (retire `HubManifest`).

## Security primitives (multiSelect)

| Option | Description | Selected |
|--------|-------------|----------|
| Terminal escape stripping | Strip CSI/OSC/DCS/PM/APC/ESC/C1/control chars from server strings. | ✓ |
| YAML-only frontmatter guard | Reject `---js` and non-YAML delimiters. | ✓ |
| Path traversal guards | `sanitizeName`/`sanitizeSubpath`/`isPathSafe`/`isContainedIn`. | ✓ |
| Pre-install audit endpoint | Query `add-skill.vercel.sh/audit` pre-install. | ✓ |

**User's choice:** All four selected.
**Notes:** Locked as D-16 (terminal escapes), D-17 (YAML-only), D-18 (path traversal), D-19 (audit endpoint with `--skip-audit` bypass). D-20 added for temp-dir containment as a derived security invariant.

## Install UX

| Option | Description | Selected |
|--------|-------------|----------|
| Progress lines + summary | Multi-line headlines + `tracing::info!` detail. | ✓ |
| Silent + final line | Quiet by default, `--verbose` opt-in. | |
| Clack-style spinner | Unicode spinner via `indicatif`. | |

**User's choice:** Progress lines + summary (recommended).
**Notes:** Locked as D-21. Five-line default shape: Resolving/Discovering/Downloading/Scanning/Installed.

## Phase scope (CLI verbs)

| Option | Description | Selected |
|--------|-------------|----------|
| install + list + remove + update | Full v1 surface. | ✓ |
| install only | Minimal; leaves remove/update inconsistent with lock file. | |
| install + list only | remove/update deferred to 21.9. | |

**User's choice:** Full v1 surface (recommended).
**Notes:** Informs D-15 migration need — all four verbs share the lock file.

## Post-install session behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Next session only | Load at agent startup; no mid-session mutation. | ✓ |
| Hot-reload in current session | Mutate live `SkillRegistry`, invalidates cached prompt. | |
| Refuse install while agent active | Block install; require separate terminal. | |

**User's choice:** Next session only (recommended).
**Notes:** Locked as D-23. Derives from Phase 15 frozen-snapshot contract + PRMT-02 cache-safety.

## Error handling

| Option | Description | Selected |
|--------|-------------|----------|
| Fail-fast with actionable errors | One retry on transient, typed `HubError` messages. | ✓ |
| Aggressive retry + cache | 3x exponential backoff, local blob cache. | |
| Offline mode flag | `--offline` short-circuits network, uses local cache. | |

**User's choice:** Fail-fast (recommended).
**Notes:** Locked as D-24. D-25 adds atomic pipeline contract (tmp + rename for lock file writes).

## Agent identity

| Option | Description | Selected |
|--------|-------------|----------|
| Behave as `openclaw` for v1 | User-Agent: `ironhermes/<ver> (via openclaw)`. | ✓ |
| Send `ironhermes` User-Agent, no agent type | Skip agent-type metadata. | |
| Register `ironhermes` agent type upstream now | Open PR against skills CLI. | |

**User's choice:** Behave as `openclaw` for v1 (recommended).
**Notes:** Locked as D-22. Upstream PR deferred.

---

## Done

| Option | Selected |
|--------|----------|
| Write context | ✓ |
| Revisit an area | |

## Claude's Discretion

- Rust type shapes for `ParsedSource`, `BlobSkill`, `SkillSnapshotFile`, `TreeEntry`, `RepoTree` (planner-drafted)
- Tar extraction composition (`tempfile::tempdir` + `flate2::read::GzDecoder` + `tar::Archive`)
- Module decomposition within `ironhermes-hub`
- `toSkillSlug` port (golden-test vector against reference)
- Test harness layout (reuse `wiremock` + `tempfile`)
- `sanitize.rs` as `pub` module vs crate-private

## Deferred Ideas (preserved for future phases)

- Publish flows (skills.sh / GitHub push)
- Upstream PR adding `ironhermes` to `AgentType` union
- GitHub / GitLab / arbitrary-git source types
- `.well-known/agent-skills` provider for arbitrary hosts
- Telemetry endpoint (`add-skill.vercel.sh/t`) opt-out
- Hot-reload into live agent session (explicitly rejected)
- Spinner / progress-bar UX (`indicatif`)
- Lockfile version migration beyond v1
- `hub_install` agent-initiated tool action (explicitly rejected)
- Multi-agent symlink fan-out (N/A for ironhermes)
- Scheduled / background update check
- Lockfile pinning beyond `snapshotHash`
