---
phase: 22.4-ratatui-backed-repl-tmon-architecture
plan: "09"
subsystem: tests
tags: [rust, tests, static-grep, invariants, regression-lock]

# Dependency graph
requires:
  - phase: 22.4-08
    provides: tui_rata/ source files (main.rs arm, event_loop.rs, app.rs, commands.rs, ui.rs, status_line.rs, history.rs, stream_events.rs, keybindings.rs, mod.rs)
provides:
  - "25 compile-time regression gates INV-22.4-00..24 in crates/ironhermes-cli/tests/invariants_22_4.rs"
  - "Registration-order lock via .find() position checks (WARNING-NEW-03)"
  - "commands.rs Task-4 completeness sentinel via byte-count (WARNING-NEW-04)"
  - "Enter-arm slash precheck lock in app.rs (BLOCKER-NEW-03)"
affects:
  - "Any future refactor of tui_rata/ that renames/removes locked identifiers will fail these tests"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "include_str! compile-time source coupling — same pattern as INV-21.7 / INV-22.3"
    - ".find() position comparison for registration-order assertions (INV-22.4-24)"
    - "Byte-count sentinel for impl completeness guard (INV-22.4-00)"

key-files:
  created:
    - crates/ironhermes-cli/tests/invariants_22_4.rs
  modified: []

key-decisions:
  - "INV-22.4-04 adapted from plan spec: plan used `run_agent_turn` but actual impl uses `agent.run(messages_snapshot)` inside spawn_turn with per-turn AgentLoop::new. Updated grep to `agent.run || run_agent_turn` to correctly capture streaming contract (Rule 1 auto-fix)."
  - "TUI_RATA_KB included as unused const — mirrors plan spec; suppressed by dead_code warning. No functionality impact."

# Metrics
duration: 12min
completed: 2026-04-24
---

# Phase 22.4 Plan 09: Invariants 22.4 Summary

**25 static-grep regression gates locking Phase 22.4 tui_rata structural + ordering contracts — INV-22.4-00..24 with WARNING-NEW-03 registration-order lock, WARNING-NEW-04 commands.rs sentinel, and BLOCKER-NEW-03 Enter-arm precheck**

## Performance

- **Duration:** ~12 min
- **Completed:** 2026-04-24
- **Tasks:** 1 (auto)
- **Files created:** 1

## Accomplishments

- Created `crates/ironhermes-cli/tests/invariants_22_4.rs` with exactly 25 `#[test]` functions (INV-22.4-00..24)
- All 25 tests pass: `cargo test -p ironhermes-cli --test invariants_22_4`
- All 14 D-18 parity items locked by at least one INV
- Existing INV suites (invariants_22_3 + invariants_22_3_streaming) remain green
- Zero new dev-deps — pure `include_str!` + `assert!` / `.find()` pattern

## Task Commits

1. **Task 1: Create invariants_22_4.rs with 25 static-grep regression tests** — `7f05457` (feat)

## Files Created/Modified

- `crates/ironhermes-cli/tests/invariants_22_4.rs` — 25 INV-22.4-* tests; include_str! for 10 source files

## §Invariant Roster

| INV ID | Category | Description |
|--------|----------|-------------|
| INV-22.4-00 | WARNING-NEW-04 | commands.rs byte sentinel — len > 1500 (stub ≈500 bytes; real impl ≈8000 bytes). Task-4 completeness gate. |
| INV-22.4-01 | Structural | tui_rata/mod.rs declares all 10 sub-modules + re-exports run_chat_ratatui |
| INV-22.4-02 | D-03/D-04 | main.rs contains classic_tui field + IRONHERMES_CLASSIC_TUI env var |
| INV-22.4-03 | D-04 | IsTerminal gate on BOTH stdin() and stdout() in main.rs |
| INV-22.4-04 | D-18 item 1 | agent.run() + StreamEvent::Delta in event_loop.rs (streaming wired) |
| INV-22.4-05 | D-18 item 2 | HookRegistry::new + add_listener in event_loop.rs |
| INV-22.4-06 | D-18 item 3 | build_mcp_manager in event_loop.rs |
| INV-22.4-07 | D-18 item 4 | build_memory_manager + register_memory_tool in event_loop.rs |
| INV-22.4-08 | D-18 item 5 | SubagentRegistry::new in event_loop.rs |
| INV-22.4-09 | D-18 item 6 | ProcessRegistry::new_for_session in event_loop.rs |
| INV-22.4-10 | D-18 item 7 | CommandRouter or build_command_registry in event_loop.rs |
| INV-22.4-11 | WARNING-08 + BLOCKER-NEW-03 | suggest_typo + "Did you mean" in commands.rs; dispatch_slash + starts_with in app.rs |
| INV-22.4-12 | D-18 item 9 | BlocklistGuardrail in event_loop.rs |
| INV-22.4-13 | D-18 item 10 | register_cronjob_tool + register_skills_tool + register_execute_code_tool |
| INV-22.4-14 | D-18 item 11 | print_yolo_banner_to_stderr in main.rs |
| INV-22.4-15 | WARNING-03 | .child_token() count >= 2 across event_loop.rs + app.rs |
| INV-22.4-16 | D-10..D-14 | DoubleCtrlCState::new + CtrlCDecision::CancelTurn in app.rs |
| INV-22.4-17 | D-10 | StatusLineState in status_line.rs + knight_rider::frame in ui.rs |
| INV-22.4-18 | D-15 | ratatui::init() + ratatui::restore() paired in event_loop.rs |
| INV-22.4-19 | Pitfall 10 | EventStream::new() in event_loop.rs; NOT on App |
| INV-22.4-20 | T-22.4-05-01 | KeyEventKind::Press filter in app.rs |
| INV-22.4-21 | D-17 | pub enum StreamEvent in stream_events.rs + UnboundedSender in event_loop.rs |
| INV-22.4-22 | History codec | Unit-separator \u{1F} in history.rs |
| INV-22.4-23 | D-01/D-14 | EnableMouseCapture + DisableMouseCapture paired in event_loop.rs |
| INV-22.4-24 | WARNING-NEW-03 | Registration order: HookRegistry/MemoryManager/McpManager/ProcessRegistry/SubagentRegistry all < AgentLoop::new; StatusLineState < App::new — via .find() position checks |

## §Failures Encountered

None — all 25 tests passed on first run after the INV-22.4-04 grep adaptation.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] INV-22.4-04: `run_agent_turn` not present in event_loop.rs**
- **Found during:** Task 1 (source code review before writing test)
- **Issue:** Plan spec called for `TUI_RATA_EVLOOP.contains("run_agent_turn")`. The actual implementation in event_loop.rs uses a per-turn pattern: `AgentLoop::new(...)` is constructed inside `spawn_turn()` and the turn runs via `agent.run(messages_snapshot)`. The string `run_agent_turn` does not appear.
- **Fix:** Updated INV-22.4-04 assert to `TUI_RATA_EVLOOP.contains("agent.run") || TUI_RATA_EVLOOP.contains("run_agent_turn")`. Both the first-form (actual) and fallback (spec) are checked, making the test future-proof if the API name is aligned.
- **Files modified:** `crates/ironhermes-cli/tests/invariants_22_4.rs`
- **Commit:** `7f05457`

## Known Stubs

None — this plan only creates test infrastructure (static greps). No data flow or UI rendering involved.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. Test file only uses `include_str!` at compile time and `assert!` at test time.

## Self-Check: PASSED

- `crates/ironhermes-cli/tests/invariants_22_4.rs` — FOUND
- Commit `7f05457` — confirmed (output from `git commit` above)
- 25 tests pass — confirmed (`test result: ok. 25 passed; 0 failed`)
- Existing INV suites green — confirmed (invariants_22_3: 6 passed, invariants_22_3_streaming: 3 passed)
