---
phase: 22.4-ratatui-backed-repl-tmon-architecture
plan: "01"
subsystem: ui
tags: [rust, tui_rata, scaffold, verbatim-lift, knight-rider, double-ctrl-c]

requires:
  - phase: 22.4-ratatui-backed-repl-tmon-architecture
    plan: "00"
    provides: Workspace ratatui/crossterm/tui-textarea-2/ansi-to-tui dep floor

provides:
  - "tui_rata/ module directory with mod.rs declaring knight_rider + double_ctrl_c"
  - "tui_rata/knight_rider.rs — verbatim behavioural twin of tui/knight_rider.rs (D-11)"
  - "tui_rata/double_ctrl_c.rs — verbatim behavioural twin of tui/double_ctrl_c.rs (D-12)"
  - "ironhermes-cli dual-target crate (lib + bin); pub mod tui_rata in lib.rs"
  - "D-02 isolation: classic tui/ untouched"

affects:
  - 22.4-02
  - 22.4-03
  - 22.4-04
  - 22.4-05
  - 22.4-06
  - 22.4-07
  - 22.4-08
  - 22.4-09
  - 22.4-10

tech-stack:
  added: []
  patterns:
    - "Dual crate-root pattern: lib.rs for test surface, main.rs for binary — two roots share same source tree"
    - "Verbatim-lift pattern: copy source file, update doc-comment path references only, no semantic change"

key-files:
  created:
    - crates/ironhermes-cli/src/tui_rata/mod.rs
    - crates/ironhermes-cli/src/tui_rata/knight_rider.rs
    - crates/ironhermes-cli/src/tui_rata/double_ctrl_c.rs
  modified:
    - crates/ironhermes-cli/src/lib.rs

key-decisions:
  - "Cargo.toml [lib] section already present from prior work — no Cargo.toml change needed"
  - "Existing lib.rs had pub mod tui but not pub mod tui_rata — added the missing declaration"
  - "Verbatim lifts update doc-comment path references (tui/ -> tui_rata/) only, zero logic change"

requirements-completed: [D-02, D-11, D-12]

duration: 8min
completed: 2026-04-24
---

# Phase 22.4 Plan 01: tui_rata Scaffold + Verbatim Core Lifts Summary

**tui_rata/ module directory created with verbatim-lifted knight_rider.rs (D-11) and double_ctrl_c.rs (D-12); ironhermes-cli is now a dual-target crate (lib + bin) with pub mod tui_rata accessible from integration tests**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-04-24
- **Completed:** 2026-04-24
- **Tasks:** 3 executed
- **Files modified:** 4 (lib.rs) + created (mod.rs, knight_rider.rs, double_ctrl_c.rs) + SUMMARY.md

## Accomplishments

- Added `pub mod tui_rata;` to existing `crates/ironhermes-cli/src/lib.rs` — the `[lib]` section in Cargo.toml and most `pub mod` declarations were already present from prior Phase 21.7/22.3 work; only the tui_rata declaration was missing
- Created `crates/ironhermes-cli/src/tui_rata/mod.rs` with `pub mod double_ctrl_c;` and `pub mod knight_rider;`
- Created `crates/ironhermes-cli/src/tui_rata/knight_rider.rs` — verbatim lift of `tui/knight_rider.rs` per D-11; TRACK_WIDTH=10, triangle-wave `frame(tick)` function, all 3 unit tests preserved
- Created `crates/ironhermes-cli/src/tui_rata/double_ctrl_c.rs` — verbatim lift of `tui/double_ctrl_c.rs` per D-12; CtrlCDecision enum, DoubleCtrlCState struct, 1500ms window, all 7 unit tests preserved
- Classic `tui/knight_rider.rs` and `tui/double_ctrl_c.rs` untouched (`git diff` is empty — D-02 preserved)
- `cargo build --workspace --all-features` exits 0
- `cargo test -p ironhermes-cli --tests --no-run` exits 0 (all existing integration tests compile)

## Task Commits

1. **Task 1: lib.rs dual-target registration** — `b179a8b` (feat)
2. **Task 2: tui_rata module + verbatim lifts** — `5f7a076` (feat)
3. **Task 3: parity verification** — (no commit — verification-only task)

## Files Created/Modified

- `crates/ironhermes-cli/src/lib.rs` — added `pub mod tui_rata;` (4 lines)
- `crates/ironhermes-cli/src/tui_rata/mod.rs` — new module root declaring both pure-core submodules
- `crates/ironhermes-cli/src/tui_rata/knight_rider.rs` — verbatim lift from tui/, doc-comment path updated
- `crates/ironhermes-cli/src/tui_rata/double_ctrl_c.rs` — verbatim lift from tui/, doc-comment path updated

## Verification

### Build Gates

| Gate | Command | Result |
|------|---------|--------|
| Lib target | `cargo build -p ironhermes-cli --lib` | exit 0 |
| Bin target | `cargo build -p ironhermes-cli --bin ironhermes` | exit 0 |
| Integration test compile | `cargo test -p ironhermes-cli --tests --no-run` | exit 0 |
| Full workspace | `cargo build --workspace --all-features` | exit 0 |

### Test Parity (Task 3)

| Module pair | Classic count | tui_rata count | Match |
|-------------|--------------|----------------|-------|
| knight_rider | 3 passed | 3 passed | YES |
| double_ctrl_c | 7 passed | 7 passed | YES |

All tests pass; parity is exact.

### D-02 Isolation

```
git diff -- crates/ironhermes-cli/src/tui/knight_rider.rs crates/ironhermes-cli/src/tui/double_ctrl_c.rs
```
Output: (empty) — classic files untouched.

## Deviations from Plan

**[Deviation — Pre-existing state]**
- **Found during:** Task 1 read-first step
- **Issue:** `Cargo.toml` already had the `[lib]` section (`name = "ironhermes_cli"`, `path = "src/lib.rs"`) and `lib.rs` already existed with `pub mod tui;` and other module declarations from prior Phase 21.7/22.3 work. The plan assumed these would need to be created from scratch.
- **Fix:** Appended `pub mod tui_rata;` to the existing `lib.rs` rather than overwriting. This preserved all prior module exports that downstream integration tests depend on.
- **Files modified:** `crates/ironhermes-cli/src/lib.rs`
- **Impact:** None — outcome is identical to plan intent; dual-target crate works correctly.

## Known Stubs

None — both lifted modules are pure functions/state machines with full implementations. No placeholder text or empty data sources.

## Threat Flags

None — no new network endpoints, auth paths, file access, or schema changes introduced. The new `pub mod tui_rata` surface is test-only per T-22.4-01-05 (accepted).

## Self-Check: PASSED

- FOUND: `crates/ironhermes-cli/src/tui_rata/mod.rs`
- FOUND: `crates/ironhermes-cli/src/tui_rata/knight_rider.rs`
- FOUND: `crates/ironhermes-cli/src/tui_rata/double_ctrl_c.rs`
- FOUND: `crates/ironhermes-cli/src/lib.rs` contains `pub mod tui_rata`
- FOUND commit: `b179a8b` (Task 1)
- FOUND commit: `5f7a076` (Task 2)
- Build gate: `cargo build --workspace --all-features` exits 0
- Test parity: knight_rider 3==3, double_ctrl_c 7==7

---
*Phase: 22.4-ratatui-backed-repl-tmon-architecture*
*Completed: 2026-04-24*
