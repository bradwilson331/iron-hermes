---
phase: 22.4-ratatui-backed-repl-tmon-architecture
plan: 12
status: complete
shipped_at: 2026-04-24
gap_closure: true
addresses:
  - D-17
  - CR-02
---

# Plan 22.4-12: Tool Progress Wiring (D-17 / CR-02 Gap Closure)

## Outcome

D-17 ("all 8 D-17 canonical StreamEvent variants live in production") gap closed.
ToolCall / ToolProgress / ToolResult variants are now reachable from the
production `spawn_turn` path, not just from snapshot tests that direct-injected
them.

## Execution mode

This plan was executed **inline by the orchestrator** (not via a parallel
worktree subagent) because the executor agent's bash permission requests were
denied repeatedly during this session. Tasks 1 and 2 shipped through orchestrator
Edits + commits. Task 3 was deferred mid-execution when the user filed UAT
issues against the running ratatui REPL; it was rolled into the gap-closure
phase as Plan 22.4-13 to land alongside the unrelated UAT fixes.

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| 1 | `8a39eed` | feat(22.4-12): add ToolResultCallback + with_tool_result builder + 6 callback sites |
| 2 | `8a9125b` | feat(22.4-12): wire with_tool_progress + with_tool_result in spawn_turn |
| 3 | (deferred → Plan 22.4-13) | INV-22.4-26 + INV-22.4-27 grep-locks |

## What shipped (Tasks 1+2)

**`crates/ironhermes-agent/src/agent_loop.rs`** (+38 lines):
- New type: `pub type ToolResultCallback = Box<dyn Fn(&str, bool) + Send + Sync>`
- New struct field: `tool_result_callback: Option<ToolResultCallback>` (line 105)
- New initializer: `tool_result_callback: None` in `::new()` (line 168)
- New builder: `pub fn with_tool_result(callback) -> Self` (lines 299-308)
- 6 callback firings (one block per `fire_hook(HookEventKind::ToolCompleted)` site
  at lines 895, 926, 987, 996, 1014, 1042). Each firing fires AFTER the hook so
  hooks remain authoritative; the UI is a pure side observer.

**`crates/ironhermes-cli/src/tui_rata/event_loop.rs`** (+36/-4 lines):
- Two new closures inside `spawn_turn`:
  - `tool_progress_cb` forwards `(name, args_preview)` → paired
    `StreamEvent::ToolCall { name }` + `StreamEvent::ToolProgress { name, phase }`
  - `tool_result_cb` forwards `(name, ok)` → `StreamEvent::ToolResult { name, ok }`
- Builder chain extended to `.with_tool_progress(tool_progress_cb).with_tool_result(tool_result_cb)`
- Stale doc comment updated to enumerate all 8 D-17 variants

## What deferred (Task 3 → Plan 22.4-13)

Two grep-lock invariants were planned but not written before the UAT detour:
- **INV-22.4-26** — `tui_rata/event_loop.rs` must contain `with_tool_progress(`
  AND `with_tool_result(` so future refactors cannot silently drop the wiring.
- **INV-22.4-27** — `tui_rata/event_loop.rs` must construct
  `StreamEvent::ToolCall`, `StreamEvent::ToolProgress`, AND `StreamEvent::ToolResult`
  in a sender closure (handler arms in `app.rs` alone do not count).

These two tests are now Plan 22.4-13's payload. The wiring they lock is already
live; the tests just have to be appended to `crates/ironhermes-cli/tests/invariants_22_4.rs`.

## Verification

After Tasks 1+2 shipped:
- `cargo build -p ironhermes-agent` → exit 0, 12 existing warnings, no new
- `cargo build -p ironhermes-cli` → exit 0, 21 existing warnings, no new
- Existing snapshot suite (`tui_rata_snapshots`) stayed green: 8/8 passed
  (snapshots use direct `App::handle_stream_event` injection so adding a new
  production sender path does not change rendered bytes)
- Acceptance criteria spot-checks (Task 1):
  - `grep -c "pub type ToolResultCallback"` → 1 ✓
  - `grep -c "pub fn with_tool_result"` → 1 ✓
  - `grep -c "tool_result_callback: None"` → 1 ✓
  - `grep -c "if let Some(ref cb) = self.tool_result_callback"` → 6 ✓

## Key files touched

- `crates/ironhermes-agent/src/agent_loop.rs` — production tool-result side channel
- `crates/ironhermes-cli/src/tui_rata/event_loop.rs` — UI sender wiring
