---
phase: 22.4-ratatui-backed-repl-tmon-architecture
plan: 06
type: execute
wave: 6
depends_on: ["22.4-05"]
files_modified:
  - crates/ironhermes-cli/src/tui_rata/mod.rs
  - crates/ironhermes-cli/src/tui_rata/ui.rs
autonomous: true
requirements: [D-02, D-05, D-10, D-11]
tags: [rust, tui_rata, ui, ratatui, frame-render]

must_haves:
  truths:
    - "`tui_rata/ui.rs` exports `pub fn ui(frame: &mut Frame, app: &App)` — pure render function taking &App (no mutation)"
    - "Layout is 4-chunk vertical: transcript Min(5), knight-rider Length(1), status pills Length(1), input Length(3) — per CONTEXT §specifics"
    - "Transcript chunk renders `app.transcript_text()` inside a bordered Block with scroll((transcript_scroll, 0)) + wrap(Wrap { trim: false })"
    - "Transcript widget is `Paragraph::new(...)` (RESEARCH Open Question §4 commitment — Paragraph baseline, locked by acceptance grep per WARNING-07)"
    - "Block title shows `\"Chat [{scroll_indicator}]\"` — `live` or `scroll N/M`"
    - "Knight-rider chunk renders `app.knight_rider_tick` via `knight_rider::frame` piped through `ansi_to_tui::IntoText`"
    - "Knight-rider only rendered when `pending_rx.is_some()` (in-flight); else blank"
    - "Status pills chunk renders `status_line::render_status_line_ratatui(&app.status)` inside a `Paragraph`"
    - "Input chunk renders `&app.textarea` (tui-textarea widget)"
    - "Cursor positioned via `frame.set_cursor_position(Position::new(...))` using `textarea.cursor()` + border offset"
    - "`ui` is exported publicly so plan 22.4-10 snapshot tests can `use ironhermes_cli::tui_rata::ui`"
  artifacts:
    - path: "crates/ironhermes-cli/src/tui_rata/ui.rs"
      provides: "Pure frame render function for the ratatui REPL"
      contains: "pub fn ui"
    - path: "crates/ironhermes-cli/src/tui_rata/mod.rs"
      provides: "Module root registers ui"
      contains: "pub mod ui"
  key_links:
    - from: "tui_rata/event_loop.rs (plan 22.4-07) terminal.draw"
      to: "tui_rata/ui.rs"
      via: "terminal.draw(|f| ui(f, &app))"
      pattern: "|f| ui\\(f,"
    - from: "tui_rata/ui.rs"
      to: "tui_rata/app.rs"
      via: "app.transcript_text(), app.transcript_scroll, app.textarea, app.status, app.knight_rider_tick, app.scroll_indicator(area), app.pending_rx"
      pattern: "app\\.(transcript_text|transcript_scroll|textarea|status|knight_rider_tick)"
---

<objective>
Land the pure `ui(frame, app)` render function. Per D-16 + RESEARCH §Render frame (lines ~1196–1243) + PATTERNS.md §`tui_rata/ui.rs` (lines ~488–559), the template is tmon's `ui()` (tmon `main.rs` lines 564–624) adapted to the IronHermes 4-chunk layout from CONTEXT §specifics:

```
Constraint::Min(5)     // transcript
Constraint::Length(1)  // knight-rider row (only visible during in-flight)
Constraint::Length(1)  // status pills row
Constraint::Length(3)  // tui-textarea input
```

The function is PURE — takes `&App` (not `&mut App`), produces frame draw calls, returns unit. This purity is mandatory for plan 22.4-10's `TestBackend` snapshot tests.

**REVISION NOTE (WARNING-07):** Transcript uses `Paragraph::new(...)` per RESEARCH Open Question §4 commitment ("Ship `Paragraph` in v1"). Acceptance grep locks this choice so a future refactor cannot silently swap to tui-scrollview without updating the invariant.

Purpose: Sole renderer for the ratatui frame.
Output: One new source file; event_loop.rs drives it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-CONTEXT.md
@.planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-RESEARCH.md
@.planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-PATTERNS.md
@/Users/twilson/code/tmon/src/main.rs
@crates/ironhermes-cli/src/tui_rata/app.rs
@crates/ironhermes-cli/src/tui_rata/status_line.rs
@crates/ironhermes-cli/src/tui_rata/knight_rider.rs
@crates/ironhermes-cli/src/tui_rata/mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create `tui_rata/ui.rs` with 4-chunk layout + all widget renders + Paragraph lock</name>
  <files>crates/ironhermes-cli/src/tui_rata/ui.rs, crates/ironhermes-cli/src/tui_rata/mod.rs, crates/ironhermes-cli/Cargo.toml</files>
  <read_first>
    - /Users/twilson/code/tmon/src/main.rs lines 564–624 (tmon `ui()` reference)
    - .planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-PATTERNS.md §`tui_rata/ui.rs` (lines ~488–559)
    - .planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-RESEARCH.md §Render frame (lines ~1196–1243)
    - .planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-CONTEXT.md §specifics (lines ~225–227)
    - .planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-RESEARCH.md §Cursor positioning (lines ~308–319)
    - .planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-RESEARCH.md §ansi-to-tui (lines ~340–356)
    - .planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-RESEARCH.md §Open Questions §4 (lines ~1317–1321) — Paragraph commitment
    - crates/ironhermes-cli/src/tui_rata/app.rs (`transcript_text`, `scroll_indicator`, `pending_rx`)
    - crates/ironhermes-cli/src/tui_rata/mod.rs (post-22.4-05 state: 7 submodules)
  </read_first>
  <behavior>
    - `ui(frame, app)` splits `frame.area()` into 4 vertical chunks.
    - Transcript (chunks[0]): bordered Block titled `Chat [{scroll_indicator}]`; `Paragraph::new(app.transcript_text())` with `wrap(Wrap { trim: false })` and `scroll((app.transcript_scroll, 0))`.
    - Knight-rider (chunks[1]): when `app.pending_rx.is_some()`, `Paragraph::new(knight_rider::frame(tick) via ansi_to_tui::IntoText)`. Idle: empty Paragraph.
    - Status pills (chunks[2]): `Paragraph::new(render_status_line_ratatui(&app.status))`.
    - Input (chunks[3]): `frame.render_widget(&app.textarea, chunks[3])`.
    - Cursor: `frame.set_cursor_position(Position::new(chunks[3].x + col, chunks[3].y + row))`.
    - No side effects; no `println!`; no `app` mutation.
  </behavior>
  <action>
    (a) Create `crates/ironhermes-cli/src/tui_rata/ui.rs`:

    ```rust
    //! Pure frame-render function for the tui_rata REPL (Phase 22.4).
    //!
    //! Template: /Users/twilson/code/tmon/src/main.rs `ui()` fn (lines 564–624).
    //! 4-chunk vertical layout per CONTEXT §specifics:
    //! - Min(5) transcript (Paragraph — per RESEARCH Open Question §4)
    //! - Length(1) knight-rider row (rendered only when in-flight)
    //! - Length(1) status pills row (D-10)
    //! - Length(3) tui-textarea input (D-05)
    //!
    //! Takes `&App` (not `&mut`) so plan 22.4-10's TestBackend snapshot tests
    //! render deterministically.

    use ansi_to_tui::IntoText;
    use ratatui::{
        layout::{Constraint, Direction, Layout, Position, Rect},
        widgets::{Block, Borders, Paragraph, Wrap},
        Frame,
    };

    use crate::tui_rata::app::App;
    use crate::tui_rata::knight_rider;
    use crate::tui_rata::status_line::render_status_line_ratatui;

    pub fn ui(frame: &mut Frame, app: &App) {
        let chunks = Layout::default()
            .direction(Direction::Vertical)
            .constraints([
                Constraint::Min(5),    // transcript
                Constraint::Length(1), // knight-rider (blank when idle)
                Constraint::Length(1), // status pills
                Constraint::Length(3), // tui-textarea input
            ])
            .split(frame.area());

        render_transcript(frame, app, chunks[0]);
        render_knight_rider(frame, app, chunks[1]);
        render_status(frame, app, chunks[2]);
        render_input(frame, app, chunks[3]);
        render_cursor(frame, app, chunks[3]);
    }

    fn render_transcript(frame: &mut Frame, app: &App, area: Rect) {
        let title = format!("Chat [{}]", app.scroll_indicator(area));
        let block = Block::default().borders(Borders::ALL).title(title);
        let text = app.transcript_text();
        // RESEARCH Open Question §4 commits to Paragraph for v1. If UAT
        // surfaces lag on >1000-line transcripts, follow-up phase can swap
        // to tui-scrollview. INV-22.4-style acceptance grep locks this choice.
        let paragraph = Paragraph::new(text)
            .block(block)
            .wrap(Wrap { trim: false })
            .scroll((app.transcript_scroll, 0));
        frame.render_widget(paragraph, area);
    }

    fn render_knight_rider(frame: &mut Frame, app: &App, area: Rect) {
        if app.pending_rx.is_none() {
            frame.render_widget(Paragraph::new(""), area);
            return;
        }
        let ansi_string = knight_rider::frame(app.knight_rider_tick);
        let text = ansi_string.as_bytes().into_text().unwrap_or_default();
        frame.render_widget(Paragraph::new(text), area);
    }

    fn render_status(frame: &mut Frame, app: &App, area: Rect) {
        let line = render_status_line_ratatui(&app.status);
        frame.render_widget(Paragraph::new(line), area);
    }

    fn render_input(frame: &mut Frame, app: &App, area: Rect) {
        frame.render_widget(&app.textarea, area);
    }

    fn render_cursor(frame: &mut Frame, app: &App, area: Rect) {
        let (row, col) = app.textarea.cursor();
        // Plan 22.4-05 Task 1 constructs textarea without a Block — no +1 offset.
        // If a block is later added (e.g., ta.set_block(...)), update this and
        // re-snapshot plan 22.4-10.
        let cursor_x = area.x.saturating_add(col as u16);
        let cursor_y = area.y.saturating_add(row as u16);
        frame.set_cursor_position(Position::new(cursor_x, cursor_y));
    }

    #[cfg(all(test, feature = "test-support"))]
    mod tests {
        use super::*;
        use ratatui::{backend::TestBackend, Terminal};

        #[test]
        fn ui_renders_four_chunks_in_80x24() {
            let app = App::new_test_empty();
            let backend = TestBackend::new(80, 24);
            let mut terminal = Terminal::new(backend).unwrap();
            terminal.draw(|f| ui(f, &app)).unwrap();
        }

        #[test]
        fn ui_idle_knight_rider_chunk_is_blank() {
            let app = App::new_test_empty();
            assert!(app.pending_rx.is_none());
            let backend = TestBackend::new(80, 24);
            let mut terminal = Terminal::new(backend).unwrap();
            terminal.draw(|f| ui(f, &app)).unwrap();
        }
    }
    ```

    (b) Edit `crates/ironhermes-cli/src/tui_rata/mod.rs` (post-22.4-05 state) — add `pub mod ui;` alphabetically (end):

    ```rust
    pub mod app;
    pub mod double_ctrl_c;
    pub mod history;
    pub mod keybindings;
    pub mod knight_rider;
    pub mod status_line;
    pub mod stream_events;
    pub mod ui;
    ```

    (c) Confirm `test-support` feature is defined in `crates/ironhermes-cli/Cargo.toml`. Plan 22.4-05 Task 1 adds it. If somehow missing:

    ```toml
    [features]
    test-support = []
    ```

    Run `cargo build -p ironhermes-cli` — MUST exit 0. Run `cargo test -p ironhermes-cli --features test-support tui_rata::ui` — at least 1 test MUST pass.
  </action>
  <verify>
    <automated>test -f crates/ironhermes-cli/src/tui_rata/ui.rs &amp;&amp; grep -q 'pub fn ui' crates/ironhermes-cli/src/tui_rata/ui.rs &amp;&amp; grep -q 'Constraint::Min(5)' crates/ironhermes-cli/src/tui_rata/ui.rs &amp;&amp; grep -q 'Constraint::Length(1)' crates/ironhermes-cli/src/tui_rata/ui.rs &amp;&amp; grep -q 'Constraint::Length(3)' crates/ironhermes-cli/src/tui_rata/ui.rs &amp;&amp; grep -q 'knight_rider::frame' crates/ironhermes-cli/src/tui_rata/ui.rs &amp;&amp; grep -q 'render_status_line_ratatui' crates/ironhermes-cli/src/tui_rata/ui.rs &amp;&amp; grep -q 'app.transcript_text' crates/ironhermes-cli/src/tui_rata/ui.rs &amp;&amp; grep -q 'ansi_to_tui::IntoText\|into_text' crates/ironhermes-cli/src/tui_rata/ui.rs &amp;&amp; grep -q 'set_cursor_position' crates/ironhermes-cli/src/tui_rata/ui.rs &amp;&amp; grep -q 'scroll((app.transcript_scroll, 0))' crates/ironhermes-cli/src/tui_rata/ui.rs &amp;&amp; grep -q 'Wrap { trim: false }' crates/ironhermes-cli/src/tui_rata/ui.rs &amp;&amp; grep -q 'Paragraph::new' crates/ironhermes-cli/src/tui_rata/ui.rs &amp;&amp; grep -q 'pub mod ui' crates/ironhermes-cli/src/tui_rata/mod.rs &amp;&amp; cargo build -p ironhermes-cli 2&gt;&amp;1 | tail -5 &amp;&amp; cargo test -p ironhermes-cli --features test-support tui_rata::ui 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/ironhermes-cli/src/tui_rata/ui.rs` exists
    - Contains literal strings: `pub fn ui`, `Constraint::Min(5)`, `Constraint::Length(1)` (≥ 2 occurrences), `Constraint::Length(3)`, `knight_rider::frame`, `render_status_line_ratatui`, `app.transcript_text`, `into_text`, `set_cursor_position`, `Wrap { trim: false }`, `scroll((app.transcript_scroll, 0))`
    - **Transcript widget lock (WARNING-07):** `grep -q 'Paragraph::new' crates/ironhermes-cli/src/tui_rata/ui.rs` passes — RESEARCH Open Question §4 Paragraph commitment regression-locked
    - Function signature: `pub fn ui(frame: &mut Frame, app: &App)` (&App not &mut App)
    - `crates/ironhermes-cli/src/tui_rata/mod.rs` contains `pub mod ui;`
    - `crates/ironhermes-cli/Cargo.toml` contains `test-support = []` in `[features]`
    - `cargo build -p ironhermes-cli` exits 0
    - `cargo test -p ironhermes-cli --features test-support tui_rata::ui` exits 0 (at least 1 test passes)
    - No `println!` / `eprintln!` / `print!` in ui.rs: `grep -cE '(^|\s)(print|println|eprintln)!' crates/ironhermes-cli/src/tui_rata/ui.rs` returns 0
  </acceptance_criteria>
  <done>ui.rs renders the full 4-chunk ratatui frame with Paragraph transcript widget locked; TestBackend drives it; zero stdout side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `knight_rider::frame` ANSI bytes → `ansi_to_tui::IntoText` → ratatui Paragraph | Non-SGR ANSI escapes stripped by ansi-to-tui (RESEARCH §Threat 2) |
| `&App` pure read → `frame.render_widget` + `frame.set_cursor_position` | No mutation; thread-safe |
| App state → transcript render | User-submitted content is plain Line::from(String); ANSI bytes in user input not interpreted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22.4-06-01 | Tampering | Non-SGR ANSI escapes bleed terminal state | mitigate | `ansi_to_tui::IntoText` strips non-SGR. |
| T-22.4-06-02 | Denial of service | `frame.render_widget(&app.textarea, ...)` panic on malformed state | mitigate | tui-textarea is well-tested; App::new always initializes via TextArea::default(). |
| T-22.4-06-03 | Information disclosure | `println!` / `eprintln!` would bleed into alt-screen | mitigate | Acceptance: grep for print/println/eprintln returns 0. |
| T-22.4-06-04 | Denial of service | Layout Constraint under-specified on narrow terminals | accept | `Min(5)` absorbs extra height; fixed heights sum to 5. |
| T-22.4-06-05 | Tampering | Cursor offset misconfigured vs textarea block | mitigate | Task 1 documents trade-off; plan 22.4-10 snapshots catch misplacement. |
| T-22.4-06-06 | Tampering | Silent swap from Paragraph to tui-scrollview breaks RESEARCH Open Question §4 commitment (WARNING-07) | mitigate | Acceptance grep for `Paragraph::new` locks the choice. |
</threat_model>

<verification>
- `cargo build -p ironhermes-cli` exits 0
- `cargo test -p ironhermes-cli --features test-support tui_rata::ui` passes ≥ 1 test
- `pub fn ui(frame: &mut Frame, app: &App)` signature present
- 4-chunk layout constraints present
- `Paragraph::new` present (WARNING-07 lock)
- No println/eprintln/print in ui.rs
- mod.rs declares `pub mod ui;`
</verification>

<success_criteria>
- Pure `ui(frame, &app)` renders 4-chunk ratatui layout
- Transcript via Paragraph (Open Question §4 commitment regression-locked)
- Knight-rider, status, input, cursor all rendered
- TestBackend drives deterministically
- Zero stdout side effects (D-02 alt-screen hygiene)
</success_criteria>

<output>
After completion, create `.planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-06-SUMMARY.md` including §Cursor Offset Decision (textarea block + matching +0/+1 offset).
</output>
