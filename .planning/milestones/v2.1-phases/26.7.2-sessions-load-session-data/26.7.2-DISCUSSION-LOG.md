# Phase 26.7.2: Sessions load session data - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-19
**Phase:** 26.7.2-sessions-load-session-data
**Areas discussed:** History display in Chat, Session row enrichment, Load trigger + navigation, Continuation semantics

---

## History display in Chat

| Option | Description | Selected |
|--------|-------------|----------|
| Same ChatBubble components | Reuse existing chat-msg.user / chat-msg.assistant RSX. History looks identical to a live session. | ✓ |
| Distinct read-only style | Historical bubbles get a visual dim or border-left accent change to indicate past session. | |
| You decide | Leave rendering style to Claude's discretion | |

**User's choice:** Same ChatBubble components
**Notes:** No visual differentiation between historical and live bubbles.

---

| Option | Description | Selected |
|--------|-------------|----------|
| No separator | History flows seamlessly into live session | |
| Divider row between history and new input | Thin rule or label separates loaded history from live session | ✓ |
| You decide | Claude's discretion | |

**User's choice:** Divider row between history and new input
**Notes:** A divider element (e.g. "─── Earlier ───") inserted at the history/live boundary. Exact visual treatment is Claude's discretion.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Clear on /clear command — same as live bubbles | History prepopulated into Signal<Vec<ChatBubble>>; /clear wipes whole signal | ✓ |
| History persists on /clear | Only new-session bubbles cleared; historical bubbles stay | |

**User's choice:** Clear on /clear — same as live bubbles

---

## Session row enrichment

| Option | Description | Selected |
|--------|-------------|----------|
| Add last message preview snippet | New `last_message: Option<String>` field in SessionInfo, truncated ~80 chars server-side | ✓ |
| Keep current data, improve timestamp | No preview; format created_at as relative time only | |
| No changes to row data | Row enrichment out of scope | |

**User's choice:** Add last message preview snippet
**Notes:** SessionInfo gains `last_message` field. Shown in SessionRow sub-text.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Relative time — "2 days ago" | Format created_at as relative time client-side | ✓ |
| Absolute date — "May 17, 2026" | Format as readable date, client-side | |
| Leave as-is | Keep existing id_tail + message count | |

**User's choice:** Relative time — "2 days ago"
**Notes:** Client-side formatting via JS Date. No server changes needed for timestamp.

---

## Load trigger + navigation

| Option | Description | Selected |
|--------|-------------|----------|
| use_effect on session_id change in ScreenChat | use_effect fires on session_id change; navigates first then loads async | ✓ |
| Server fn call before navigation in sessions.rs | on_select awaits messages fetch BEFORE setting active_screen | |

**User's choice:** use_effect on session_id change in ScreenChat
**Notes:** Navigation happens first, then async history load. Matches Dioxus reactivity idioms.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Empty chat stream — same as a fresh session | .chat-stream shows nothing until Signal<Vec<ChatBubble>> is populated | ✓ |
| Loading indicator row | "Loading history…" bubble or spinner while fetch is in flight | |

**User's choice:** Empty chat stream — same as a fresh session
**Notes:** Consistent with 26.7 D-13 (no custom loading UI).

---

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — session_id change clears + reloads | use_effect fires for every change; clears signal then fetches | ✓ |
| Only clear on /clear command | History persists until explicit /clear | |

**User's choice:** Yes — session_id change clears + reloads

---

## Continuation semantics

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — fully continuable | Input stays active; sending a message continues the session server-side | ✓ |
| Read-only history view | Historical sessions shown but input disabled/hidden | |

**User's choice:** Fully continuable
**Notes:** state.rs::build_messages_for_turn already uses session_id; history load is purely a UI-side prepopulation.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Last N messages — e.g. 50 | Cap at 50; constant defined in server fn | ✓ |
| All messages | Load full history | |
| You decide | Leave cap to Claude's discretion | |

**User's choice:** Last N messages (50)
**Notes:** Display-only cap. Server-side `build_messages_for_turn` has its own context-window management.

---

## Claude's Discretion

- Exact divider element shape (hr, div.chat-divider, ChatBubble variant)
- Whether `last_message` shows role prefix or strips it
- Field name / URL form for the new GET endpoint (parameterized path vs query param)
- Relative timestamp implementation (js_sys::Date, helper fn, or inline web_sys call)

## Deferred Ideas

- Server-side session deletion — own follow-up phase
- Paginated history / scroll-up to load earlier messages — own phase
- `+ NEW SESSION` button wiring — own phase
- Session search / filter (⊞ FILTER button) — own phase
- Per-session title editing — own phase
