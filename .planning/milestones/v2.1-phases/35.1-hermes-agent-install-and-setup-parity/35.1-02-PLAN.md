---
phase: 35.1
plan: 02
type: execute
wave: 2
depends_on:
  - 35.1-00
  - 35.1-01
files_modified:
  - crates/ironhermes-cli/src/setup.rs
  - crates/ironhermes-cli/tests/setup_wizard.rs
autonomous: true
requirements:
  - CFG-01
tags:
  - cli
  - setup-wizard
  - doctor
  - rust
must_haves:
  truths:
    - "When `ironhermes setup` is invoked with no section argument, the wizard first asks 'Full setup (all sections)?' (D-11)"
    - "If user picks quick path, only run_minimum_viable_flow runs (D-11 fast-path)"
    - "If user picks full path, run_minimum_viable_flow runs THEN run_skills_section, run_terminal_section, run_tools_section, run_memory_section, run_gateway_section, run_agent_section run in order"
    - "After ALL wizard sections (quick OR full) complete, doctor::run_doctor_check() is called automatically before the function returns (D-03)"
    - "After doctor runs, a completion summary prints showing configured provider, model, enabled platforms, and a next-step hint (D-12)"
    - "Existing per-section dispatch (`ironhermes setup model`, `setup memory`, etc.) still works unchanged — D-03/D-11/D-12 only fire when section is None"
  artifacts:
    - path: "crates/ironhermes-cli/src/setup.rs"
      provides: "D-11 choice prompt, D-03 doctor call, D-12 print_setup_completion_summary"
      contains: "print_setup_completion_summary"
    - path: "crates/ironhermes-cli/tests/setup_wizard.rs"
      provides: "Un-ignored D-03, D-11, D-12 source-text invariant tests passing"
      contains: "d11_fast_full_choice_prompt_present_in_setup_rs"
  key_links:
    - from: "crates/ironhermes-cli/src/setup.rs run_setup None arm"
      to: "doctor::run_doctor_check()"
      via: "in-process call after sections complete"
      pattern: "doctor::run_doctor_check"
    - from: "crates/ironhermes-cli/src/setup.rs run_setup None arm"
      to: "print_setup_completion_summary"
      via: "called after doctor, before returning Ok(())"
      pattern: "print_setup_completion_summary"
---

<objective>
Wave 2 (parallel with Plan 03) — close three of the four remaining gaps from RESEARCH § "Key Architecture Insight": D-11 fast/full choice at wizard start, D-03 doctor call at wizard exit, D-12 completion summary. All three changes land in `setup.rs::run_setup` (the `None` section arm) and in a new helper `print_setup_completion_summary`. Source-text invariant tests in `tests/setup_wizard.rs` (created in Plan 00) get un-ignored and filled in.

This plan does NOT modify per-section dispatch (`Some("model") => ...` etc.) — those routes remain section-isolated. D-03/D-11/D-12 only fire when the user runs `ironhermes setup` bare (section is `None`), matching the hermes-agent reference behavior where these are wizard-entrypoint UX, not per-section overhead.

Purpose: With Plan 01's skills+terminal sections + this plan's D-11/D-03/D-12, the bare `ironhermes setup` invocation produces the full hermes-agent parity experience: choice → sections → doctor → summary.

Output: Updated run_setup None arm, new print_setup_completion_summary helper, three passing source-text invariant tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-CONTEXT.md
@.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-RESEARCH.md
@.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-PATTERNS.md
@.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-VALIDATION.md
@.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-00-SUMMARY.md
@.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-01-SUMMARY.md
@crates/ironhermes-cli/src/setup.rs
@crates/ironhermes-cli/src/doctor.rs
@crates/ironhermes-core/src/config.rs

<interfaces>
<!-- Symbols available in setup.rs after Plans 00 and 01: -->
<!--   - mod doctor; (from Plan 00) — call as `doctor::run_doctor_check()` inside setup.rs without prefix since both are in ironhermes-cli binary crate -->
<!--   - pub async fn run_skills_section(rl, hermes_home) -> Result<()>           (added in Plan 01) -->
<!--   - async fn run_terminal_section(config, hermes_home, rl) -> Result<()>     (added in Plan 01) -->
<!--   - existing: run_minimum_viable_flow, run_model_section, run_memory_section, run_gateway_section, run_tools_section, run_agent_section -->
<!--   - existing: fn prompt_yes_no(rl: &mut DefaultEditor, prompt: &str, default: bool) -> Result<bool>   (do NOT redefine) -->

<!-- Config struct shape (from ironhermes-core/src/config.rs, verified): -->
<!--   pub struct Config { pub model: ModelConfig, pub gateway: GatewayConfig, pub terminal: TerminalConfig, ... } -->
<!--   pub struct ModelConfig { pub provider: String, pub default: String, pub base_url: Option<String>, ... } -->
<!--   pub struct GatewayConfig { pub platforms: HashMap<String, ...>, ... } -->

<!-- Doctor module (from Plan 00): -->
<!--   pub fn run_doctor_check() -> Result<()>     (in src/doctor.rs) -->

<!-- Current run_setup dispatch (setup.rs lines 115-128 after Plan 01 changes): -->
<!--   match section { -->
<!--       None => run_minimum_viable_flow(&mut config, &hermes_home, &mut rl, mode).await?, -->
<!--       Some("model") => run_model_section(&mut config, &mut rl).await?, -->
<!--       Some("memory") => run_memory_section(&mut config, &hermes_home, &mut rl).await?, -->
<!--       Some("gateway") => run_gateway_section(&mut config, &mut rl).await?, -->
<!--       Some("tools") => run_tools_section(&mut rl, &hermes_home).await?, -->
<!--       Some("agent") => run_agent_section(&mut config, &mut rl).await?, -->
<!--       Some("skills") => run_skills_section(&mut rl, &hermes_home).await?,      // Plan 01 -->
<!--       Some("terminal") => run_terminal_section(&mut config, &hermes_home, &mut rl).await?,    // Plan 01 -->
<!--       Some(other) => bail!("unknown setup section: {} (valid: model, memory, gateway, tools, skills, terminal)", other), -->
<!--   } -->

<!-- The three Wave 0 stubs to un-ignore in tests/setup_wizard.rs: -->
<!--   d11_fast_full_choice_prompt_present_in_setup_rs -->
<!--   d03_doctor_call_present_at_wizard_exit -->
<!--   d12_completion_summary_present_in_setup_rs -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire D-11 fast/full choice + D-03 doctor call into run_setup None arm</name>
  <files>crates/ironhermes-cli/src/setup.rs, crates/ironhermes-cli/tests/setup_wizard.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/setup.rs (full file — locate run_setup function, current None match arm, the prompt_yes_no helper to confirm its signature, and the existing source-text invariant tests around lines 779-799 for analog pattern)
    - crates/ironhermes-cli/src/doctor.rs (created in Plan 00 — confirm `pub fn run_doctor_check() -> Result<()>` signature is callable synchronously)
    - .planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-PATTERNS.md (§ "D-11 fast/full choice insertion"; § "D-03 doctor call")
    - .planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-CONTEXT.md (D-11 default is quick — full setup is opt-in via "y"; D-03 doctor fires after ALL sections complete)
    - .planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-RESEARCH.md (§ "Pattern 4: Doctor call at wizard exit"; § "Open Questions" item 3 — default to quick)
    - crates/ironhermes-cli/tests/setup_wizard.rs (the two D-11 and D-03 stubs)
  </read_first>
  <behavior>
    - Test: `d11_fast_full_choice_prompt_present_in_setup_rs` — `include_str!("../src/setup.rs")` contains the literal "Full setup" prompt text (or a clearly identifiable variant) AND contains both `run_minimum_viable_flow` call AND a conditional invocation of `run_skills_section` AND `run_terminal_section` AND the rest of the full-path sections within the `None =>` arm. Asserts the source contains the contract — no rustyline I/O simulation needed.
    - Test: `d03_doctor_call_present_at_wizard_exit` — `include_str!("../src/setup.rs")` contains `run_doctor_check` AND the `run_doctor_check()` call appears AFTER (greater file offset than) the `run_minimum_viable_flow` call inside the `None =>` arm body — the doctor must fire AFTER sections, not before. Use `source.find(...)` offsets just like setup.rs:779-799 analog.
    - Behavioral contract (manual): bare `ironhermes setup` prompts "Full setup (all sections)?", default N → runs minimum_viable_flow → runs doctor → prints summary (D-12 lands in Task 2). When user answers `y`, also runs skills/terminal/tools/memory/gateway/agent between minimum_viable and doctor.
  </behavior>
  <action>
    Modify the `None` arm of the `match section` block in `setup.rs::run_setup` (currently the single-line dispatch `run_minimum_viable_flow(...).await?`). Replace it with a block that:

    1. **D-11 choice prompt FIRST.** Use existing `prompt_yes_no(&mut rl, "Full setup (all sections)? [y = full, N = quick provider+model only]", false)?` and bind to `let full_setup = ...`. Default is `false` per RESEARCH Open Question 3 (quick is default; full is opt-in by typing "y"). Do NOT redefine prompt_yes_no.

    2. **Always run minimum viable flow.** Both quick and full paths invoke `run_minimum_viable_flow(&mut config, &hermes_home, &mut rl, mode).await?;` exactly once. This is unconditional — provider + model are mandatory.

    3. **Full path additional sections.** Wrap the rest in `if full_setup { ... }`. Inside, in order: `run_skills_section(&mut rl, &hermes_home).await?;` then `run_terminal_section(&mut config, &hermes_home, &mut rl).await?;` then `run_tools_section(&mut rl, &hermes_home).await?;` then `run_memory_section(&mut config, &hermes_home, &mut rl).await?;` then `run_gateway_section(&mut config, &mut rl).await?;` then `run_agent_section(&mut config, &mut rl).await?;`. Order matters: skills+terminal+tools first (data setup), then memory+gateway+agent (subsystem config). This mirrors the hermes-agent reference order per CONTEXT § canonical_refs.

    4. **D-03 doctor call AFTER the if block.** After the `if full_setup { ... }` block, unconditionally call `doctor::run_doctor_check()?;` (synchronous, no `.await`). This runs for BOTH quick and full paths, as the wizard exit gate. Prefix a one-line header like `println!("\nRunning doctor check...\n");` before the call to match the wizard's section-header style.

    5. **D-12 placeholder.** Insert `print_setup_completion_summary(&config, &hermes_home);` immediately after the doctor call. The actual `print_setup_completion_summary` function definition lives in Task 2 of this plan — Task 1 only adds the call site so the dispatch flow is complete in a single source diff and the un-ignored tests can locate both names in the same compile.

    Do NOT change any other match arm (per-section invocations stay isolated — D-03/D-11/D-12 are wizard-entrypoint behaviors only, per RESEARCH § Architecture Patterns flow diagram). Do NOT call doctor or summary inside per-section arms.

    In `crates/ironhermes-cli/tests/setup_wizard.rs`, un-ignore the two stubs `d11_fast_full_choice_prompt_present_in_setup_rs` and `d03_doctor_call_present_at_wizard_exit` and replace each body with the assertions described under <behavior>. Use the same `include_str!("../src/setup.rs")` pattern as setup.rs internal tests at lines 779-799 (PATTERNS § "Source-text invariant test pattern").
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cli -- --test-threads=1 d11_fast_full_choice_prompt_present_in_setup_rs d03_doctor_call_present_at_wizard_exit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Full setup" crates/ironhermes-cli/src/setup.rs` returns at least 1 (D-11 prompt string)
    - `grep -c "doctor::run_doctor_check" crates/ironhermes-cli/src/setup.rs` returns at least 1 (D-03 call site)
    - `grep -c "print_setup_completion_summary" crates/ironhermes-cli/src/setup.rs` returns at least 1 (D-12 call site placeholder — definition lands in Task 2)
    - `grep -c "if full_setup" crates/ironhermes-cli/src/setup.rs` returns at least 1 (D-11 branching)
    - Ordering invariant: in setup.rs, the byte offset of `run_doctor_check` is GREATER than the byte offset of `run_minimum_viable_flow` within the file (the d03 test asserts this)
    - Per-section dispatch arms unchanged: `grep -c 'Some("model") => run_model_section' crates/ironhermes-cli/src/setup.rs` returns 1; same for memory, gateway, tools, agent, skills, terminal
    - `cargo test -p ironhermes-cli -- --test-threads=1 d11_fast_full_choice_prompt_present_in_setup_rs` exits 0 with one pass
    - `cargo test -p ironhermes-cli -- --test-threads=1 d03_doctor_call_present_at_wizard_exit` exits 0 with one pass
    - `cargo build -p ironhermes-cli` exits 0 (note: print_setup_completion_summary will be unresolved unless Task 2 lands in the same commit OR a stub is added — see Task 2 action; preferred: implement Task 2 stub first then real body in same task, or land both tasks in one commit)
  </acceptance_criteria>
  <done>
    D-11 choice prompt is wired. D-03 doctor call is wired in the None arm. Two source-text invariant tests pass. Per-section dispatch arms are untouched. The print_setup_completion_summary call site exists (definition added in Task 2).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement print_setup_completion_summary helper (D-12) + un-ignore test</name>
  <files>crates/ironhermes-cli/src/setup.rs, crates/ironhermes-cli/tests/setup_wizard.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/setup.rs (full file — locate the Task 1 call site for print_setup_completion_summary; the helper definition goes in this same file at module scope)
    - crates/ironhermes-core/src/config.rs (Config + ModelConfig + GatewayConfig shapes; specifically how gateway.platforms is typed — needed to read "telegram enabled" status)
    - .planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-PATTERNS.md (§ "Pattern 6: Completion Summary (D-12)")
    - .planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-CONTEXT.md (D-12: configured provider, model, which platforms are enabled, then "Run `ironhermes` to start chatting" OR "Run `ironhermes gateway` to start the Telegram bot")
    - .planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-RESEARCH.md (§ "Pattern 6"; § "Open Questions" item 2 on gateway.platforms type uncertainty — recommendation: use `config_setter::config_get(hermes_home, "gateway.platforms.telegram.enabled")` if the typed access path is opaque)
    - crates/ironhermes-cli/tests/setup_wizard.rs (the d12 stub)
  </read_first>
  <behavior>
    - Test: `d12_completion_summary_present_in_setup_rs` — `include_str!("../src/setup.rs")` contains the literal strings "Setup complete" AND ("Run \`ironhermes\`" OR "start chatting") AND ("ironhermes gateway" OR "Telegram bot"). Source-text invariant pattern — no runtime simulation.
    - Behavioral contract (manual): after doctor runs, the summary prints (1) "Setup complete!" header, (2) configured provider name, (3) configured model name, (4) which platforms are enabled (telegram for now — others are no-ops if the platform list is empty), (5) a final next-step hint — `Run \`ironhermes gateway\` to start the Telegram bot.` if telegram is enabled, otherwise `Run \`ironhermes\` to start chatting.`
  </behavior>
  <action>
    Add a private helper to `crates/ironhermes-cli/src/setup.rs`:

    `fn print_setup_completion_summary(config: &Config, hermes_home: &Path)` — signature mirrors PATTERNS § "Pattern 6". Body:

    1. Print a blank line then a bold-green header line `"Setup complete!"`. Use `colored::Colorize` (already in setup.rs's import surface via other sections — confirm by grepping for `use colored` in setup.rs; if absent, add `use colored::Colorize;` to the imports block).
    2. Print `"  Provider: {}"` with `config.model.provider`.
    3. Print `"  Model:    {}"` with `config.model.default`.
    4. Determine telegram-enabled status. Use the safest path that compiles against the current `GatewayConfig.platforms` shape:
       - First, try `ironhermes_core::config_setter::config_get(hermes_home, "gateway.platforms.telegram.enabled")` per RESEARCH Open Question 2 fallback recommendation — returns `Option<String>`; treat `"true"` (case-insensitive) as enabled.
       - This path is independent of the runtime type of `GatewayConfig.platforms` (raw YAML splice), so it compiles regardless of whether platforms is `HashMap<String, GatewayPlatformConfig>` or `HashMap<String, serde_yaml::Value>`.
    5. If telegram enabled: print `"  Telegram: enabled"` and finally `"  Run \`ironhermes gateway\` to start the Telegram bot."`. Otherwise: print only the final `"  Run \`ironhermes\` to start chatting."`. Both final hints use the backtick spelling to match CONTEXT D-12 verbatim.

    Function returns `()` (no Result) — pure stdout printing, no I/O errors propagated. If `config_get` returns Err, treat as "telegram not enabled" (best-effort summary; doctor already covered failures).

    Hold on visual styling: bold/green for the header is enough — keep the body lines plain to match the doctor output style.

    Un-ignore the `d12_completion_summary_present_in_setup_rs` stub in `crates/ironhermes-cli/tests/setup_wizard.rs` and fill its body per the <behavior> block. Use the same `include_str!` pattern as Task 1's tests.

    Land Task 2 in the same commit as Task 1 (or land Task 1's print_setup_completion_summary call site at the same time as this function definition) so the crate compiles between commits. Recommended: single squashed commit for the entire D-11/D-03/D-12 trio since they form one logical change.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cli -- --test-threads=1 d12_completion_summary_present_in_setup_rs 2>&1 | tail -20 && cargo test -p ironhermes-cli -- --test-threads=1 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn print_setup_completion_summary" crates/ironhermes-cli/src/setup.rs` returns 1
    - `grep -c "Setup complete" crates/ironhermes-cli/src/setup.rs` returns at least 1
    - `grep -c "start chatting" crates/ironhermes-cli/src/setup.rs` returns at least 1 OR `grep -c "Run \`ironhermes\`" crates/ironhermes-cli/src/setup.rs` returns at least 1
    - `grep -c "Telegram bot" crates/ironhermes-cli/src/setup.rs` returns at least 1
    - `cargo build -p ironhermes-cli` exits 0
    - `cargo test -p ironhermes-cli -- --test-threads=1 d12_completion_summary_present_in_setup_rs` exits 0 with one pass
    - `cargo test -p ironhermes-cli -- --test-threads=1` (full crate suite) exits 0 — no regressions across all wizard, config, doctor, and other existing tests
    - Manual smoke: running `ironhermes setup` with a configured provider produces the summary block at the end including the next-step hint
  </acceptance_criteria>
  <done>
    print_setup_completion_summary is implemented. D-12 test passes. Full ironhermes-cli test suite is green. The complete D-03/D-11/D-12 trio of run_setup-None-arm changes is operational. CFG-01 is satisfied for the wizard-entrypoint UX gap (the remaining preflight first-run detection lands in Plan 03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard ↔ prompt_yes_no | Untrusted; bounded to y/n answer; no injection surface |
| config.yaml ↔ print_setup_completion_summary | Read-only; user-controlled but display-only; no execution of values |
| stdout ↔ end user | Display-only; values printed are non-secret (provider name, model name, "Telegram: enabled" boolean) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35.1-summary-disclosure | Information Disclosure | print_setup_completion_summary could echo a secret if model.provider or model.default contained a credential | accept | `config.model.provider` and `config.model.default` are non-secret by design (provider names like "openrouter", model names like "openrouter/gpt-4o-mini"). Secrets live in .env (api keys), never in config.yaml. No new disclosure path introduced. |
| T-35.1-doctor-side-effects | Tampering | doctor::run_doctor_check could mutate state during wizard exit | accept | Plan 00 extracted cmd_doctor verbatim — its body is purely read-only filesystem probes (config exists, .env exists, gateway PID alive). No writes. Existing tests profile_doctor verify this. |
| T-35.1-prompt-yes-no-bypass | Tampering | A non-tty stdin could bypass the D-11 choice prompt | accept | `prompt_yes_no` uses rustyline; non-tty invocations either error out or fall through to the default (`false` = quick path). Quick path runs minimum_viable_flow which already errors cleanly if non-interactive — established behavior across all existing wizard sections. |
| T-35.1-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies in Wave 2 Plan 02; existing workspace crates only (verified in RESEARCH.md § Package Legitimacy Audit). |
</threat_model>

<verification>
- `cargo build -p ironhermes-cli` exits 0
- `cargo test -p ironhermes-cli -- --test-threads=1` exits 0 with no regressions
- All three source-text invariant tests (d11, d03, d12) pass
- `ironhermes setup` (manual smoke with a real configured provider): bare invocation prompts for fast/full → minimum_viable → (optionally other sections if full) → doctor output → summary
- `ironhermes setup model` (and other per-section invocations): behavior unchanged — does NOT prompt for fast/full, does NOT run doctor, does NOT print summary
</verification>

<success_criteria>
- D-11 choice wired with default=quick
- D-03 doctor call wired post-sections, in-process (not subprocess)
- D-12 summary helper implemented and called
- Per-section dispatch unchanged
- Three Wave 0 stubs un-ignored and passing
- Full crate test suite green
</success_criteria>

<output>
Create `.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-02-SUMMARY.md` when done. Include: exact run_setup None-arm diff, signature of print_setup_completion_summary, how gateway.platforms.telegram.enabled is read, and which Wave 0 stubs are now passing.
</output>
