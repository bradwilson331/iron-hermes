# Phase 35.1: hermes-agent install and setup parity - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-22
**Phase:** 35.1-hermes-agent-install-and-setup-parity
**Areas discussed:** Wizard coverage gaps, Config commands shape, First-run / install flow, Setup end state

---

## Wizard coverage gaps

### Skills wizard section

| Option | Description | Selected |
|--------|-------------|----------|
| Configure skill prereqs | For each installed skill that declares config requirements, prompt for API keys/env vars — mirrors run_tools_section | ✓ |
| List and enable/disable skills | Show installed skills, let user toggle per platform | |
| Both prereqs + enable/disable | Full parity with skills config CLI | |
| You decide | Match whatever hermes-agent setup.py's skills section does | |

**User's choice:** Configure skill prereqs (option 1)
**Notes:** Mirrors existing run_tools_section pattern exactly — no new UX needed.

---

### Terminal section

| Option | Description | Selected |
|--------|-------------|----------|
| Skip it | IronHermes defaults are fine; no backend choice needed | |
| Add minimal terminal config | Prompt for cwd default only | ✓ |
| You decide | Match hermes-agent's terminal section scope | |

**User's choice:** Add minimal terminal config
**Notes:** Just the cwd default — maps to TerminalConfig.cwd in config.rs.

---

### Wizard exit

| Option | Description | Selected |
|--------|-------------|----------|
| Just write config + print summary | No live API call; wizard writes files, prints You're ready | |
| API key validation | Quick LLM API ping after config write | |
| Full preflight check | Run ironhermes doctor automatically at end | ✓ |

**User's choice:** Full preflight check (ironhermes doctor)
**Notes:** Doctor output serves as the final verification before declaring setup complete.

---

## Config commands shape

### Config set/get key format

| Option | Description | Selected |
|--------|-------------|----------|
| Dot-notation | model.default, agent.max_iterations — matches Python hermes | ✓ (implicitly) |
| Flat env-var style | MODEL_DEFAULT — mirrors .env file keys | |
| You decide | Match Python hermes config CLI exactly | |

**User's choice:** "the config is already set - use the existing cli-config.yaml.example"
**Notes:** Config structure is fully defined in cli-config.yaml.example — commands work with those existing keys using dot-notation.

---

### Config show

| Option | Description | Selected |
|--------|-------------|----------|
| Full config as YAML | Merged config (file + env overrides) as formatted YAML to stdout | ✓ |
| Pretty table per section | Labeled table per config section in terminal | |
| You decide | Match hermes config show output | |

**User's choice:** Full config as YAML

---

### Config migrate

| Option | Description | Selected |
|--------|-------------|----------|
| Match hermes: scan skills for unconfigured prereqs | Reuse prereq machinery from setup.rs | ✓ |
| Broader: scan skills + tools | More comprehensive than reference | |
| Skip config migrate for now | Defer CFG-03 to later | |

**User's choice:** Match hermes reference exactly

---

## First-run / install flow

### First-run auto-launch

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-launch wizard on first run | Missing config → auto-launch setup before chat | ✓ |
| Print hint only | Print setup suggestion and exit | |
| Silent scaffold only | Create directories silently, start normally | |

**User's choice:** Auto-launch wizard on first run

---

### install.sh behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Rely on binary first-run detection | install.sh installs binary only; binary handles first-run | ✓ |
| install.sh prompts: run setup now? | Post-install optional setup prompt in script | |
| You decide | Match setup-hermes.sh exactly | |

**User's choice:** Rely on binary first-run detection

---

### First-run detection signal

| Option | Description | Selected |
|--------|-------------|----------|
| Missing API key in .env | Practical 'not runnable' signal | ✓ (with modification) |
| Missing config.yaml | Simpler but too conservative | |
| Missing ~/.ironhermes/ entirely | Very conservative; won't catch partial configs | |

**User's choice:** Option 1 — missing API key — but Ollama must be treated as valid too
**Notes:** Detection: no API key in .env AND no local endpoint (Ollama/localhost base_url) in config.yaml. Either satisfies "runnable" — both must be absent to trigger setup.

---

## Setup end state

### Minimum completion bar

| Option | Description | Selected |
|--------|-------------|----------|
| LLM provider only | Working API key or Ollama endpoint — agent can chat | ✓ |
| LLM + at least one platform | Must configure Telegram or CLI too | |
| Whatever doctor passes | Doctor defines the bar | |

**User's choice:** LLM provider only

---

### Wizard path

| Option | Description | Selected |
|--------|-------------|----------|
| Minimum viable fast-path + full option | Quick (just LLM) or full (all sections) choice at wizard start | ✓ |
| Always walk all sections | No fast-path; each section individually skippable | |
| You decide | Keep whatever run_minimum_viable_flow already does | |

**User's choice:** Minimum viable fast-path + full option

---

### Completion UX

| Option | Description | Selected |
|--------|-------------|----------|
| Summary + next command | Provider, model, platforms enabled + "Run: ironhermes" | ✓ |
| Just 'Setup complete' | Minimal confirmation | |
| Doctor output only | Let doctor output serve as summary | |

**User's choice:** Summary + next command

---

## Claude's Discretion

None — all areas had explicit user decisions.

## Deferred Ideas

- Full profile lifecycle CLI (PROF-01..N) — CFG-04 complete; full profile management is future phase
- Skin/theme engine — own phase
- SlashCommandCompleter / tab autocomplete — own phase
- ACP adapter + CLI feature parity — tracked separately in cli-feature-parity.md todo
