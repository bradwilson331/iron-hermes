---
phase: 35.1
plan: 01
type: execute
wave: 1
depends_on:
  - 35.1-00
files_modified:
  - crates/ironhermes-cli/src/setup.rs
  - crates/ironhermes-cli/tests/setup_wizard.rs
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
tags:
  - cli
  - setup-wizard
  - skills
  - terminal
  - rust
must_haves:
  truths:
    - "Running setup wizard's skills section scans installed skills and prompts for unconfigured env-var prereqs and config-field prereqs"
    - "Running setup wizard's terminal section prompts for cwd default and writes it to TerminalConfig.cwd"
    - "Skills section masks API key input via is_secret_prereq_name path (no echo to stdout for *_KEY/_TOKEN/_SECRET/_PASSWORD names)"
    - "Skills section with empty skills_dir returns Ok(()) without panic"
    - "Setup wizard's `Some(\"skills\")` arm no longer bails with 'deferred to Phase 28'"
    - "Setup wizard supports new `Some(\"terminal\")` arm"
  artifacts:
    - path: "crates/ironhermes-cli/src/setup.rs"
      provides: "run_skills_section, run_terminal_section, apply_skills_prereq_answers testability seam"
      contains: "pub async fn run_skills_section"
    - path: "crates/ironhermes-cli/tests/setup_wizard.rs"
      provides: "Un-ignored D-01 and D-02 unit tests now passing"
      contains: "d01_run_skills_section_no_skills_dir_returns_ok"
  key_links:
    - from: "crates/ironhermes-cli/src/setup.rs run_skills_section"
      to: "ironhermes_core::SkillRegistry::load_with_paths"
      via: "single-path scan of hermes_home/skills"
      pattern: "SkillRegistry::load_with_paths"
    - from: "crates/ironhermes-cli/src/setup.rs run_skills_section"
      to: "write_env_var_to_dotenv (env_var prereqs) AND ironhermes_core::config_setter::config_set (config_field prereqs)"
      via: "two-write-path contract per RESEARCH Pitfall 7"
      pattern: "write_env_var_to_dotenv|config_setter::config_set"
    - from: "crates/ironhermes-cli/src/setup.rs run_terminal_section"
      to: "TerminalConfig.cwd"
      via: "config.terminal.cwd = answer; config.save_to(...)"
      pattern: "config\\.terminal\\.cwd"
---

<objective>
Wave 1 — unblock the two missing wizard sections in `setup.rs`. Replace the `bail!("section deferred to Phase 28")` at line 122 with a real `run_skills_section`, and add a brand-new `run_terminal_section`. Both must mirror existing wizard section patterns exactly (RESEARCH § "Pattern 1" and § "Pattern 2"; PATTERNS § "run_tools_section" analog).

**Requirements coverage:**
- **CFG-01 (active):** This plan ships the two missing wizard sections that complete the first-run interactive setup experience.
- **CFG-02 (already satisfied):** This plan **does not touch** config_cli.rs. `ironhermes config set/get/show` already ship and are tested per RESEARCH § "Key Architecture Insight". The requirement is included here for traceability — no implementation change is required.
- **CFG-03 (already satisfied):** Likewise, `ironhermes config migrate` already ships in `cmd_config_migrate`. No implementation change is required. The new `run_skills_section` is the wizard-time twin of `config migrate` but is a separate code path (interactive vs subcommand) — implementing it does NOT modify `cmd_config_migrate`.

Purpose: Closes 2 of the 6 actual gaps (D-01, D-02 from CONTEXT) — Wave 2 closes the remaining 4 (D-03, D-07/D-08, D-11, D-12).

Output: Two new pub async fns in setup.rs, one testability seam, two passing tests in setup_wizard.rs (D-01 skills cases and D-02 terminal case).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-CONTEXT.md
@.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-RESEARCH.md
@.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-PATTERNS.md
@.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-VALIDATION.md
@.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-00-SUMMARY.md
@crates/ironhermes-cli/src/setup.rs
@crates/ironhermes-cli/src/config_cli.rs
@crates/ironhermes-core/src/skills.rs

<interfaces>
<!-- Existing reusable symbols already in setup.rs (do NOT redefine): -->
<!--   - pub(crate) fn write_env_var_to_dotenv(hermes_home: &Path, name: &str, value: &str) -> Result<()>   (lines ~510-566) -->
<!--   - fn prompt_with_default(rl, prompt: &str, default: &str) -> Result<String>                          (lines ~60-79) -->
<!--   - fn prompt_yes_no(rl, prompt: &str, default: bool) -> Result<bool>                                  (lines ~402-418) -->
<!--   - pub(crate) fn is_secret_prereq_name(name: &str) -> bool                                            (lines ~384-390) -->
<!--   - fn prompt_for_prereq_value(rl, tool_name, prereq) -> Result<PrereqAnswer>                          (lines ~430-489) -->
<!--   - fn apply_prereq_value(hermes_home, prereq, value) -> Result<()>                                    (lines ~492-506) -->
<!--   - fn apply_skip_prompts(hermes_home, tool_name) -> Result<()>                                        (line ~570) -->
<!--   - pub fn apply_tool_prereq_answers(hermes_home, answers) -> Result<()>                               (lines ~622-630) -->
<!--   - enum PrereqAnswer { Value(String), Skip, Defer }                                                   (used by prompt_for_prereq_value) -->

<!-- Existing reusable symbols in sibling modules: -->
<!--   - From ironhermes_core::config_setter:  pub fn config_get(hermes_home: &Path, key: &str) -> Result<Option<String>> -->
<!--                                          pub fn config_set(hermes_home: &Path, key: &str, value: &str) -> Result<()> -->
<!--   - From ironhermes_core (re-exported):    SkillRegistry with .load_with_paths(&[PathBuf]) -> Self and .list() -> impl Iterator<Item = &Skill> -->
<!--   - From ironhermes_core::skills:          HermesMetadata { required_environment_variables: Vec<EnvVarEntry>, config: Vec<SkillConfigField>, ... } -->
<!--                                            EnvVarEntry { name: String, prompt: Option<String>, help: Option<String>, required_for: Option<String> } -->
<!--                                            SkillConfigField { key: String, default: Option<serde_yaml::Value>, description: Option<String>, field_type: Option<String> } -->
<!--   - From crates/ironhermes-cli/src/config_cli.rs (currently fn, not pub): env_var_exists_in_dotenv(env_path: &Path, var: &str) -> Result<bool>  (lines ~250-258) -->

<!-- Existing config.rs (verified): -->
<!--   pub struct TerminalConfig { pub backend: String /* default "local", NOT prompted */, pub cwd: String /* default ".", THIS is the prompt */ } -->

<!-- Existing dispatch site (setup.rs lines 115-128): -->
<!--   match section { None => run_minimum_viable_flow(...), Some("model") => ..., Some("memory") => ..., Some("gateway") => ..., Some("tools") => ..., Some("agent") => ..., Some("skills") => bail!("section deferred to Phase 28"), Some(other) => bail!("unknown setup section: ... valid: model, memory, gateway, tools"), } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement run_skills_section + apply_skills_prereq_answers seam (D-01)</name>
  <files>crates/ironhermes-cli/src/setup.rs, crates/ironhermes-cli/tests/setup_wizard.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/setup.rs (entire file; the new fn is appended; the bail line at ~122 must be replaced with the function call. Identify the exact line range of run_tools_section since run_skills_section mirrors it.)
    - crates/ironhermes-cli/src/config_cli.rs (lines 162-194 = cmd_config_migrate SkillRegistry scan loop, the data-source pattern to mirror; lines 250-258 = env_var_exists_in_dotenv to make pub(crate) for reuse)
    - crates/ironhermes-core/src/skills.rs (HermesMetadata, EnvVarEntry, SkillConfigField struct shapes — verify field names match what RESEARCH listed)
    - .planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-PATTERNS.md (§ "Core pattern — run_tools_section" template; § "SkillRegistry scan pattern" template; § "Shared Patterns — Prereq write routing")
    - .planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-RESEARCH.md (Pitfall 5 single-path scan; Pitfall 7 two-write-path contract)
    - crates/ironhermes-cli/tests/setup_wizard.rs (find the two D-01 stubs created in Wave 0; un-ignore them in this task)
  </read_first>
  <behavior>
    - Test: `d01_run_skills_section_no_skills_dir_returns_ok` — given a temp HERMES_HOME with no `skills/` subdirectory, the runner returns `Ok(())` and prints "No skills installed — nothing to configure." (or equivalent) to stdout. No panic, no error.
    - Test: `d01_run_skills_section_prompts_for_each_unconfigured_skill_prereq` — uses the new `apply_skills_prereq_answers` testability seam (no rustyline). Given a HERMES_HOME with one skill that declares `required_environment_variables: [{name: "FAKE_KEY"}]` and `config: [{key: "skills.fake.timeout"}]`, calling `apply_skills_prereq_answers(hermes_home, &[("fake-skill", "env_var", "FAKE_KEY", "secret123"), ("fake-skill", "config_field", "skills.fake.timeout", "30")])` writes FAKE_KEY=secret123 to .env (mode 0600) and writes skills.fake.timeout: 30 to config.yaml via config_setter::config_set. Verify by reading both files post-call.
    - Test: `is_secret_prereq_name("FAKE_KEY") == true` and the masked-input path is used for that prompt (covered by existing setup.rs unit tests for is_secret_prereq_name; no additional masking test needed in this plan).
  </behavior>
  <action>
    Add to `crates/ironhermes-cli/src/setup.rs`:

    1. **Make `env_var_exists_in_dotenv` reusable.** Either change its visibility in `config_cli.rs` from `fn` to `pub(crate) fn`, OR copy the 6-line helper into `setup.rs` as `fn env_var_exists_in_dotenv(env_path: &Path, var: &str) -> Result<bool>` (RESEARCH § "Code Examples" shows exact body). Prefer the pub(crate) widening in config_cli.rs to avoid duplication — both files are in the same binary crate.

    2. **Add `pub async fn run_skills_section(rl: &mut rustyline::DefaultEditor, hermes_home: &Path) -> Result<()>`** mirroring `run_tools_section` (PATTERNS § "Core pattern"). Body uses `ironhermes_core::SkillRegistry::load_with_paths(&[skills_dir])` exactly as `cmd_config_migrate` does at config_cli.rs:175 (single-path scan per RESEARCH Pitfall 5; do NOT scan optional-skills/ — Phase 25.7 territory). For each skill, iterate `skill.hermes_metadata.required_environment_variables` and `skill.hermes_metadata.config`. For each unconfigured env-var entry, call `prompt_for_prereq_value` (build a `Prerequisite` with `kind: "env_var"`, `name: entry.name.clone()`, `description: entry.help.clone()`, optional, treat as required if `entry.required_for.is_some()`) and route the answer through `apply_prereq_value` — secret masking is automatic via `is_secret_prereq_name`. For each unconfigured config-field entry, call `prompt_for_prereq_value` with `kind: "config_field"`, `name: cfg_field.key.clone()`, and route through `apply_prereq_value` (which dispatches to `config_setter::config_set` per RESEARCH Pitfall 7 two-write-path contract).

    3. **Add `pub fn apply_skills_prereq_answers(hermes_home: &Path, answers: &[(&str, &str, &str, &str)]) -> Result<()>`** as the testability seam mirroring `apply_tool_prereq_answers` at setup.rs:622-630. Tuple shape is `(skill_name, kind, prereq_name, value)` where `kind` is `"env_var"` or `"config_field"`. For `"env_var"` answers, call `write_env_var_to_dotenv`. For `"config_field"` answers, call `ironhermes_core::config_setter::config_set`. Unknown kind returns `anyhow::bail!("unknown skills prereq kind '{}'")`.

    4. **Replace the bail at setup.rs line 122.** Change `Some("skills") => anyhow::bail!("section deferred to Phase 28"),` to `Some("skills") => run_skills_section(&mut rl, &hermes_home).await?,`. Also update the unknown-section error message a few lines below to include "skills" in the valid list: change the bail string from `"unknown setup section: {} (valid: model, memory, gateway, tools)"` to `"unknown setup section: {} (valid: model, memory, gateway, tools, skills, terminal)"` — the "terminal" addition is from Task 2 and lands in this same task to avoid bouncing the error string between commits. Do not widen any other dispatch arm.

    Un-ignore the two D-01 stubs in `crates/ironhermes-cli/tests/setup_wizard.rs` (remove `#[ignore = "..."]` attribute) and replace each one-line body with a real assertion per the <behavior> block above. Use `TempDir` + `env_lock` for test isolation per PATTERNS § "env_lock for test serialization". Per Phase 21.6 precedent, env-mutating tests use `--test-threads=1`.

    Do NOT touch `config_cli.rs::cmd_config_migrate` — it stays as-is (CFG-02/CFG-03 already shipped). Do NOT write to the deprecated `model.api_key` field (RESEARCH § Anti-Patterns). Do NOT scan multiple skill paths (RESEARCH Pitfall 5).
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cli -- --test-threads=1 d01_run_skills_section 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub async fn run_skills_section" crates/ironhermes-cli/src/setup.rs` returns 1
    - `grep -c "pub fn apply_skills_prereq_answers" crates/ironhermes-cli/src/setup.rs` returns 1
    - `grep -v '^[[:space:]]*//' crates/ironhermes-cli/src/setup.rs | grep -c "deferred to Phase 28"` returns 0 (the bail string is gone from non-comment code)
    - `grep -c "Some(\"skills\") => run_skills_section" crates/ironhermes-cli/src/setup.rs` returns 1
    - `grep -c "SkillRegistry::load_with_paths" crates/ironhermes-cli/src/setup.rs` returns at least 1
    - `grep -v '^[[:space:]]*//' crates/ironhermes-cli/tests/setup_wizard.rs | grep -c "#\\[ignore" returns at most 4` (two D-01 stubs un-ignored; D-02 + three D-03/D-11/D-12 still ignored from Wave 0 and Task 2 of this plan)
    - `cargo test -p ironhermes-cli -- --test-threads=1 d01_run_skills_section_no_skills_dir_returns_ok` exits 0 with one pass
    - `cargo test -p ironhermes-cli -- --test-threads=1 d01_run_skills_section_prompts_for_each_unconfigured_skill_prereq` exits 0 with one pass
    - Behavior: with a real `~/.ironhermes/skills/` directory containing a skill with `required_environment_variables`, running `ironhermes setup skills` invokes the new section (manual verify outside of automated tests)
  </acceptance_criteria>
  <done>
    Skills section is operational. Bail line is gone. Two D-01 tests pass. Section can be invoked via `ironhermes setup skills`. CFG-01 partial coverage for the skills wizard gap closed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement run_terminal_section + dispatch arm (D-02)</name>
  <files>crates/ironhermes-cli/src/setup.rs, crates/ironhermes-cli/tests/setup_wizard.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/setup.rs (full file — the new fn appends after run_skills_section from Task 1; dispatch match is the same one Task 1 updated for the unknown-section error string)
    - crates/ironhermes-core/src/config.rs (TerminalConfig struct definition: pub backend: String, pub cwd: String, defaults "local" and "." respectively — verify by direct read)
    - .planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-PATTERNS.md (§ "Terminal section pattern — run_memory_section signature"; § "prompt_with_default helper")
    - .planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-CONTEXT.md (D-02: prompt for cwd only; backend stays at default "local")
    - crates/ironhermes-cli/tests/setup_wizard.rs (find the d02 stub; un-ignore it)
  </read_first>
  <behavior>
    - Test: `d02_run_terminal_section_prompts_for_cwd_and_writes_config` — using a unit seam (NOT rustyline), confirm that setting `config.terminal.cwd = "/some/dir".to_string()` and calling `config.save_to(hermes_home.join("config.yaml"))` round-trips through `Config::load` and reads back `terminal.cwd == "/some/dir"`. The seam pattern mirrors how Task 1 tested via `apply_skills_prereq_answers` — direct write path, no rustyline. The actual interactive prompt is exercised manually only.
    - Behavior contract: `run_terminal_section` prompts the user once for a working directory with the current value (or ".") as the default. Empty input keeps default ".". `config.terminal.backend` is NOT prompted and remains untouched. After collecting input, `config.save_to(hermes_home.join("config.yaml"))` is called exactly once.
  </behavior>
  <action>
    Add to `crates/ironhermes-cli/src/setup.rs`:

    1. **Add `async fn run_terminal_section(config: &mut Config, hermes_home: &Path, rl: &mut rustyline::DefaultEditor) -> Result<()>`** with signature mirroring `run_memory_section` per PATTERNS § "Terminal section pattern". Body:
       - Print a section header (e.g., `println!("\nTerminal configuration.\n");`) matching the existing wizard sections' header style.
       - Use `prompt_with_default(rl, "Default working directory for agent", current_cwd)?` where `current_cwd` is `&config.terminal.cwd` (fall back to `"."` if empty).
       - Assign `config.terminal.cwd = if answer.trim().is_empty() { ".".to_string() } else { answer.trim().to_string() };`.
       - Save with `config.save_to(&hermes_home.join("config.yaml")).context("writing config.yaml")?;`.
       - Do NOT touch `config.terminal.backend` (D-02: cwd only).

    2. **Add dispatch arm at setup.rs line ~127.** Insert a new arm BEFORE the existing `Some(other) => bail!(...)` catch-all: `Some("terminal") => run_terminal_section(&mut config, &hermes_home, &mut rl).await?,`. (Task 1 already updated the unknown-section error string to include "terminal" — no further change to that bail.)

    3. **Add a testability path.** Either expose `run_terminal_section` itself as `pub` (matches Task 1's `pub async fn run_skills_section`) OR add a `pub fn apply_terminal_answer(hermes_home: &Path, cwd: &str) -> Result<()>` seam that loads config, sets terminal.cwd, saves config — analogous to `apply_skills_prereq_answers`. Prefer the seam since it avoids invoking rustyline in tests. Wire the test to call the seam, not the interactive fn.

    Un-ignore the d02 stub in `crates/ironhermes-cli/tests/setup_wizard.rs` and fill its body per the <behavior> block.

    Do NOT add a new field to TerminalConfig (the struct already has `cwd`). Do NOT prompt for `backend` (out of scope per D-02). Do NOT call save_to more than once per section invocation. Do NOT serde-rename or restructure the existing TerminalConfig.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cli -- --test-threads=1 d02_run_terminal_section 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async fn run_terminal_section" crates/ironhermes-cli/src/setup.rs` returns 1
    - `grep -c "Some(\"terminal\") => run_terminal_section" crates/ironhermes-cli/src/setup.rs` returns 1
    - `grep -c "config\\.terminal\\.cwd" crates/ironhermes-cli/src/setup.rs` returns at least 1 (in run_terminal_section body)
    - `grep -c "config\\.terminal\\.backend" crates/ironhermes-cli/src/setup.rs` returns 0 (D-02 says cwd only)
    - `cargo test -p ironhermes-cli -- --test-threads=1 d02_run_terminal_section_prompts_for_cwd_and_writes_config` exits 0 with one pass
    - `cargo test -p ironhermes-cli -- --test-threads=1` (full crate) exits 0 with no regressions
    - Existing wizard sections (model/memory/gateway/tools/agent) still dispatch correctly — `ironhermes setup model` etc. unchanged
  </acceptance_criteria>
  <done>
    Terminal section is operational. Dispatch arm wired. D-02 test passes. config.terminal.cwd is the only field touched. No other section's behavior changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (rustyline) ↔ setup.rs | Untrusted user input crosses here; secret-name detection masks display |
| setup.rs ↔ ~/.ironhermes/.env | Secret values written to disk — must be 0600 |
| setup.rs ↔ ~/.ironhermes/config.yaml | Config values written to disk — non-secret data only per CONTEXT design |
| skill SKILL.md frontmatter ↔ HermesMetadata struct | Skill author input crosses here; already scanned by SkillRegistry (SKILL-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35.1-02 | Information Disclosure | API key echo to stdout during skills prereq prompting | mitigate | `is_secret_prereq_name(&entry.name)` returns true for `*_KEY/_TOKEN/_SECRET/_PASSWORD` names; `prompt_for_prereq_value` already uses masked readline for those — `run_skills_section` reuses these symbols verbatim (no new prompting code). Acceptance: secret skill prereqs do not echo to stdout (covered by existing setup.rs `is_secret_prereq_name` unit tests). |
| T-35.1-03 | Information Disclosure | .env written world-readable | mitigate | `write_env_var_to_dotenv` (setup.rs lines 510-566) already sets `0600` permissions atomically via tempfile+rename; `run_skills_section` and `apply_skills_prereq_answers` route env-var prereqs through that exact function — no new disk writes introduced. Acceptance: post-call `.env` has mode `0600` (assertable via `std::fs::metadata(&env_path).permissions().mode() & 0o777 == 0o600` in tests). |
| T-35.1-skill-injection | Tampering | Skill SKILL.md frontmatter could contain malicious prompt text in `prompt:` or `help:` fields | mitigate | `SkillRegistry::load_with_paths` runs `scan_skill_content` per SKILL-07 (security scan) before any frontmatter is exposed to user-facing prompts; `run_skills_section` only consumes already-validated `HermesMetadata`. No new input pathway. |
| T-35.1-config-injection | Tampering | Config field value written to config.yaml via config_setter::config_set could break YAML structure | mitigate | `config_setter::config_set` uses raw-YAML splice that preserves unknown keys (per Phase 19 precedent §20-04 D-25); user input is treated as a YAML scalar value, never as structured YAML — same code path config_cli.rs cmd_config_migrate has used in production. No new code surface. |
| T-35.1-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies in Wave 1; existing workspace crates only (verified in RESEARCH.md § Package Legitimacy Audit). |
</threat_model>

<verification>
- `cargo build -p ironhermes-cli` exits 0
- `cargo test -p ironhermes-cli -- --test-threads=1` exits 0 with no regressions and the three new D-01/D-02 tests passing
- `grep` acceptance gates above all return their expected counts
- `ironhermes setup skills` (manual smoke) invokes run_skills_section instead of bailing with "deferred to Phase 28"
- `ironhermes setup terminal` (manual smoke) invokes run_terminal_section and prompts for cwd
- config_cli.rs is unmodified (CFG-02/CFG-03 already shipped — `git diff crates/ironhermes-cli/src/config_cli.rs` shows zero changes except possibly the pub(crate) visibility widening of env_var_exists_in_dotenv from Task 1)
</verification>

<success_criteria>
- run_skills_section implemented, wired into dispatch, tested
- run_terminal_section implemented, wired into dispatch, tested
- D-01 bail line removed
- Two D-01 tests + one D-02 test pass
- No regression in other wizard sections or in config_cli.rs subcommands
- CFG-01 closer to satisfaction (Wave 2 finishes it with D-03/D-11/D-12; D-07/D-08)
</success_criteria>

<output>
Create `.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-01-SUMMARY.md` when done. Include: which symbol was made pub(crate) (env_var_exists_in_dotenv) or whether the helper was inlined; the actual signature shapes of run_skills_section, run_terminal_section, apply_skills_prereq_answers; the dispatch arm diff; any deviations.
</output>
