---
phase: 35.1
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/ironhermes-cli/src/doctor.rs
  - crates/ironhermes-cli/src/main.rs
  - crates/ironhermes-cli/src/lib.rs
  - crates/ironhermes-cli/tests/setup_wizard.rs
  - crates/ironhermes-cli/tests/doctor_integration.rs
autonomous: true
requirements:
  - CFG-01
tags:
  - cli
  - setup-wizard
  - rust
must_haves:
  truths:
    - "tests/setup_wizard.rs file exists and compiles with #[ignore] stub tests for D-01, D-02, D-11, D-03, D-12"
    - "tests/doctor_integration.rs contains a new #[ignore] stub for D-07/D-08 first-run LLM detection"
    - "src/doctor.rs module exists and exports pub fn run_doctor_check() -> Result<()>"
    - "main.rs dispatch at Commands::Doctor calls doctor::run_doctor_check() (no longer the local fn cmd_doctor)"
    - "`cargo build -p ironhermes-cli` succeeds (no compile errors after extraction)"
    - "`cargo test -p ironhermes-cli --no-run` succeeds (Wave 0 tests compile but are ignored)"
  artifacts:
    - path: "crates/ironhermes-cli/src/doctor.rs"
      provides: "Extracted doctor command logic callable in-process from setup.rs"
      contains: "pub fn run_doctor_check"
    - path: "crates/ironhermes-cli/tests/setup_wizard.rs"
      provides: "Wave 0 test scaffolds for D-01/D-02/D-03/D-11/D-12 source-text invariants and unit seams"
      contains: "#[ignore]"
    - path: "crates/ironhermes-cli/tests/doctor_integration.rs"
      provides: "Extended Wave 0 stub for D-07/D-08 detection"
      contains: "d07_d08"
  key_links:
    - from: "crates/ironhermes-cli/src/main.rs"
      to: "crates/ironhermes-cli/src/doctor.rs"
      via: "mod doctor + doctor::run_doctor_check()"
      pattern: "mod doctor"
    - from: "crates/ironhermes-cli/tests/setup_wizard.rs"
      to: "crates/ironhermes-cli/src/setup.rs"
      via: "include_str! source-text invariants"
      pattern: "include_str!\\(\"\\.\\./src/setup\\.rs\"\\)"
---

<objective>
Wave 0 — foundational scaffolding required by every subsequent wave. Two deliverables:

1. **Extract `cmd_doctor` to `src/doctor.rs`** as `pub fn run_doctor_check() -> Result<()>` so Wave 2's `setup.rs` D-03 can call it in-process without circular dependency or subprocess shelling.
2. **Create Wave 0 test scaffolds** (`tests/setup_wizard.rs` new file; `tests/doctor_integration.rs` extended) with `#[ignore]` stubs for D-01, D-02, D-03, D-07/D-08, D-11, D-12. Each stub names the invariant being checked so Wave 1/2 tasks know exactly which `#[ignore]` lines to remove and which assertions to fill in. Per VALIDATION.md Nyquist requirements: every behaviour-adding task in later waves must point to a pre-existing test name.

Purpose: Unblocks subsequent waves by making doctor callable from setup.rs and by giving every later `<verify><automated>` line a real test target.

Output: New module `doctor.rs`, new test file `setup_wizard.rs`, extended `doctor_integration.rs`, working `cargo build -p ironhermes-cli`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-CONTEXT.md
@.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-RESEARCH.md
@.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-PATTERNS.md
@.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-VALIDATION.md
@crates/ironhermes-cli/src/main.rs
@crates/ironhermes-cli/src/cron.rs
@crates/ironhermes-cli/src/config_cli.rs
@crates/ironhermes-cli/tests/config_migrate_discovery.rs
@crates/ironhermes-cli/tests/doctor_integration.rs

<interfaces>
<!-- Existing fn cmd_doctor lives in main.rs lines 579-636 (RESEARCH.md confirms). The signature is `fn cmd_doctor() -> Result<()>`. The local helper `fn print_check(name: &str, ok: bool)` is also in main.rs and must move with it. -->
<!-- Existing module declarations live in main.rs lines 30-41 (mod batch; mod config_cli; mod cron; ...). Add `mod doctor;` to this list. -->
<!-- The dispatch arm at main.rs line 367 is `Some(Commands::Doctor) => cmd_doctor(),` — change to `Some(Commands::Doctor) => doctor::run_doctor_check(),` -->
<!-- Existing analog: crates/ironhermes-cli/src/cron.rs is the module-extraction pattern (own file, pub handler fn called from main dispatch). config_cli.rs is the second analog. -->
<!-- Tests/setup_wizard.rs does NOT exist yet; tests/doctor_integration.rs exists at ~67 lines with profile_doctor and profile_first_use functions per PATTERNS.md. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract cmd_doctor to src/doctor.rs module</name>
  <files>crates/ironhermes-cli/src/doctor.rs, crates/ironhermes-cli/src/main.rs, crates/ironhermes-cli/src/lib.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/main.rs (read entire file to locate `fn cmd_doctor`, the `fn print_check` helper, the existing `mod` declarations, and the Commands::Doctor dispatch arm at ~line 367)
    - crates/ironhermes-cli/src/cron.rs (analog: module-extraction pattern with pub handler fn)
    - crates/ironhermes-cli/src/config_cli.rs (second analog: pub fn handle_X exported from src module)
    - crates/ironhermes-cli/src/lib.rs (to confirm whether modules are re-exported there; many sibling modules are; if so, add `pub mod doctor;` too)
    - .planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-PATTERNS.md (§ "crates/ironhermes-cli/src/doctor.rs — NEW file")
    - .planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-RESEARCH.md (§ "Pitfall 4: cmd_doctor is not pub")
  </read_first>
  <action>
    Create new file `crates/ironhermes-cli/src/doctor.rs`. Move the entire body of `fn cmd_doctor` from `main.rs` into this new module as `pub fn run_doctor_check() -> Result<()>`. Also move the local `fn print_check` helper into the new module as a private fn. Imports inside doctor.rs must include `anyhow::Result`, `colored::Colorize`, `ironhermes_core::config::Config`, and any other types currently referenced inside cmd_doctor's body (verify by reading main.rs cmd_doctor body — references include `ironhermes_cli::status_cmd::current_profile()`, `ironhermes_core::get_hermes_home()`, `ironhermes_gateway::pid::read_gateway_pid`, `ironhermes_gateway::pid::is_pid_alive`, `ironhermes_gateway::pid::PidLiveness`).

    Add `mod doctor;` to the module list near the top of `main.rs` (alongside `mod cron;`, `mod config_cli;`). In `lib.rs`, if other crate-internal modules are re-exported via `pub mod X;` (such as `pub mod setup;`), add `pub mod doctor;` in the same style so integration tests can reach it; if lib.rs only re-exports a subset, follow whatever shape `cron.rs` uses for tests/cron_integration.rs.

    In `main.rs` at line ~367, replace `Some(Commands::Doctor) => cmd_doctor(),` with `Some(Commands::Doctor) => doctor::run_doctor_check(),`. Delete the now-orphaned `fn cmd_doctor` and `fn print_check` from main.rs.

    Add a module-level doc comment header to doctor.rs matching PATTERNS.md §"File header pattern": `//! \`hermes doctor\` — configuration and dependency health check.` followed by `//!` and `//! Extracted from main.rs cmd_doctor (Phase 35.1 D-03).` followed by `//!` and `//! Called from \`setup::run_setup\` at wizard exit and from \`main\` dispatch.`.

    Do NOT change the doctor command's behaviour, output text, check order, or return shape. Pure code motion + visibility widening. (per CFG-01 / D-03 enablement)
  </action>
  <verify>
    <automated>cargo build -p ironhermes-cli 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/ironhermes-cli/src/doctor.rs` exists
    - `grep -c "pub fn run_doctor_check" crates/ironhermes-cli/src/doctor.rs` returns 1
    - `grep -v '^[[:space:]]*//' crates/ironhermes-cli/src/main.rs | grep -c "fn cmd_doctor"` returns 0 (function removed; comments stripped before count)
    - `grep -c "^mod doctor;" crates/ironhermes-cli/src/main.rs` returns 1
    - `grep -c "doctor::run_doctor_check()" crates/ironhermes-cli/src/main.rs` returns at least 1 (dispatch arm)
    - `cargo build -p ironhermes-cli` exits 0
    - Running `target/debug/ironhermes doctor` (or `cargo run -p ironhermes-cli -- doctor`) prints "IronHermes Doctor" header (behavior preserved)
  </acceptance_criteria>
  <done>
    cmd_doctor is callable as doctor::run_doctor_check() from any module in the crate (including setup.rs in Wave 2). Build is green. No behavioral change to ironhermes doctor command output.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Wave 0 test scaffolds for setup wizard + first-run detection</name>
  <files>crates/ironhermes-cli/tests/setup_wizard.rs, crates/ironhermes-cli/tests/doctor_integration.rs</files>
  <read_first>
    - crates/ironhermes-cli/tests/config_migrate_discovery.rs (analog: env_lock pattern, TempDir, assert_cmd usage)
    - crates/ironhermes-cli/tests/doctor_integration.rs (read entire file to know what tests exist before extending; the d07_d08 stub must not collide with existing test names)
    - crates/ironhermes-cli/src/setup.rs (lines 1-50 for imports/structure context; lines 779-799 for the existing source-text invariant test analog cited in PATTERNS.md)
    - .planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-PATTERNS.md (§ "crates/ironhermes-cli/tests/setup_wizard.rs — NEW test file")
    - .planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-VALIDATION.md (§ "Per-Task Verification Map" — every Wave 0 row names a test the executor of later waves will un-ignore)
  </read_first>
  <action>
    Create new file `crates/ironhermes-cli/tests/setup_wizard.rs` with the following six `#[ignore]` stub tests. Each must compile but assert nothing yet — Wave 1 (Plan 01) and Wave 2 (Plans 02 and 03) tasks will remove the `#[ignore]` and fill in real assertions against the named invariant.

    Stub test names (mandatory exact names — later waves grep these):
    1. `d01_run_skills_section_no_skills_dir_returns_ok` — Wave 1 / Plan 01 / Task 1
    2. `d01_run_skills_section_prompts_for_each_unconfigured_skill_prereq` — Wave 1 / Plan 01 / Task 1
    3. `d02_run_terminal_section_prompts_for_cwd_and_writes_config` — Wave 1 / Plan 01 / Task 2
    4. `d11_fast_full_choice_prompt_present_in_setup_rs` — Wave 2 / Plan 02 (source-text invariant via `include_str!("../src/setup.rs")`)
    5. `d03_doctor_call_present_at_wizard_exit` — Wave 2 / Plan 02 (source-text invariant; asserts `run_doctor_check` appears in setup.rs)
    6. `d12_completion_summary_present_in_setup_rs` — Wave 2 / Plan 02 (source-text invariant; asserts "Run `ironhermes`" or "start chatting" appears)

    Include the env_lock helper (PATTERNS.md §"env_lock for test serialization") at top of file. Include `use tempfile::TempDir;` and `use std::sync::OnceLock;`. Each stub body MUST be exactly one line: `// Wave 0 stub — Wave N Task M removes #[ignore] and replaces body.` with NO assertions. The `#[ignore = "Wave 0 stub — pending Phase 35.1 Plan NN-TT"]` attribute is mandatory on each.

    Extend `crates/ironhermes-cli/tests/doctor_integration.rs` by appending a single new `#[ignore]` stub:
    7. `d07_d08_first_run_triggers_setup_when_no_api_key_and_no_local_endpoint` — Wave 2 / Plan 03

    Use the same `#[ignore = "Wave 0 stub — pending Phase 35.1 Plan 03-01"]` attribute. Body is one line: the same Wave 0 stub comment. Do NOT modify any existing test in doctor_integration.rs.

    The file `tests/setup_wizard.rs` must compile under `cargo test -p ironhermes-cli --no-run`. Use `#[allow(dead_code)]` on the env_lock helper if necessary to silence warnings while the stubs are ignored. (per VALIDATION.md Wave 0 Requirements)
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cli --no-run 2>&1 | tail -10 && cargo test -p ironhermes-cli -- --list 2>&1 | grep -E "d01_run_skills|d02_run_terminal|d11_fast_full|d03_doctor_call|d12_completion|d07_d08_first_run" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/ironhermes-cli/tests/setup_wizard.rs` exists
    - `grep -c "^#\[ignore" crates/ironhermes-cli/tests/setup_wizard.rs` returns 6
    - `grep -c "fn d01_run_skills_section_no_skills_dir_returns_ok" crates/ironhermes-cli/tests/setup_wizard.rs` returns 1
    - `grep -c "fn d01_run_skills_section_prompts_for_each_unconfigured_skill_prereq" crates/ironhermes-cli/tests/setup_wizard.rs` returns 1
    - `grep -c "fn d02_run_terminal_section_prompts_for_cwd_and_writes_config" crates/ironhermes-cli/tests/setup_wizard.rs` returns 1
    - `grep -c "fn d11_fast_full_choice_prompt_present_in_setup_rs" crates/ironhermes-cli/tests/setup_wizard.rs` returns 1
    - `grep -c "fn d03_doctor_call_present_at_wizard_exit" crates/ironhermes-cli/tests/setup_wizard.rs` returns 1
    - `grep -c "fn d12_completion_summary_present_in_setup_rs" crates/ironhermes-cli/tests/setup_wizard.rs` returns 1
    - `grep -c "fn d07_d08_first_run_triggers_setup_when_no_api_key_and_no_local_endpoint" crates/ironhermes-cli/tests/doctor_integration.rs` returns 1
    - `cargo test -p ironhermes-cli --no-run` exits 0
    - `cargo test -p ironhermes-cli -- --list 2>&1 | grep -E "d01_run_skills|d02_run_terminal|d11_fast_full|d03_doctor_call|d12_completion|d07_d08_first_run" | wc -l` returns 7
  </acceptance_criteria>
  <done>
    All seven Wave 0 test names exist as `#[ignore]` stubs and are listed by `cargo test -- --list`. Wave 1/2 plans can reference these exact names in their `<automated>` verify lines.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer ↔ binary | Pure code motion; no new attack surface introduced by Wave 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35.1-W0-01 | Tampering | doctor.rs module visibility | accept | doctor::run_doctor_check is intentionally `pub` so setup.rs can invoke it; same trust boundary as before (in-process binary) — no widening of external surface |
| T-35.1-W0-02 | Information Disclosure | doctor output (API key existence echoed) | accept | Behavior preserved verbatim from main.rs cmd_doctor; existing test profile_doctor already covers the output shape; doctor only prints `OK`/`MISSING` boolean per key, never the key value |
| T-35.1-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies in Wave 0; existing workspace crates only (verified in RESEARCH.md § Package Legitimacy Audit: "No new packages") |
</threat_model>

<verification>
- `cargo build -p ironhermes-cli` exits 0
- `cargo test -p ironhermes-cli --no-run` exits 0
- All seven Wave 0 test names appear in `cargo test -- --list` output
- `ironhermes doctor` (built binary) still prints "IronHermes Doctor" header line — behavior preserved
- main.rs no longer contains `fn cmd_doctor` definition (only the dispatch arm referencing `doctor::run_doctor_check()`)
</verification>

<success_criteria>
- doctor.rs module created with pub fn run_doctor_check
- main.rs uses doctor::run_doctor_check() in dispatch (no more cmd_doctor)
- tests/setup_wizard.rs created with 6 #[ignore] stubs by exact names
- tests/doctor_integration.rs extended with the d07_d08 stub
- Workspace builds and test list contains all 7 stubs
</success_criteria>

<output>
Create `.planning/phases/35.1-hermes-agent-install-and-setup-parity/35.1-00-SUMMARY.md` when done. Include: files modified, public symbols introduced (`doctor::run_doctor_check`), test names created, and any deviations from the plan.
</output>
