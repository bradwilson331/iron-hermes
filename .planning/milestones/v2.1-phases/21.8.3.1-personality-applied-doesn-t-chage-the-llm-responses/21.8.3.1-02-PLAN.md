---
phase: 21.8.3.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-gateway/src/handler.rs
  - crates/ironhermes-core/tests/handlers_personality.rs
autonomous: true
requirements: [D-01, D-05, D-07, D-08, D-09]
tags: [gateway, telegram, personality, overlay, prompt-builder]

must_haves:
  truths:
    - "D-01: Scope is TUI + gateway only — no web UI changes in this phase."
    - "D-07: GatewayMessageHandler struct owns `active_personality_overlay: Option<String>` initialized to None in the constructor."
    - "D-08: After /personality <name> succeeds in Telegram, the gateway stores the overlay text in self.active_personality_overlay and replies with a confirmation (not the raw overlay)."
    - "D-05: After /personality clear in Telegram, self.active_personality_overlay is None and the bot replies 'Personality cleared.' without invoking core dispatch."
    - "D-09: On every subsequent turn, prompt_builder.set_overlay(text.clone()) is called when the field is Some, before build_system_message() — the LLM sees personality on every turn until cleared."
    - "Core handler cmd_personality still returns Error('Unknown personality: clear') for the bare 'clear' argument — the contract that 'clear' MUST be intercepted at the surface layer is locked by a regression test."
  artifacts:
    - path: "crates/ironhermes-gateway/src/handler.rs"
      provides: "active_personality_overlay field + dispatch detection + clear pre-check + per-turn set_overlay call"
      contains: "active_personality_overlay"
    - path: "crates/ironhermes-core/tests/handlers_personality.rs"
      provides: "Regression test documenting that core has no 'clear' case"
      contains: "personality_clear_at_core_returns_unknown_error"
  key_links:
    - from: "crates/ironhermes-gateway/src/handler.rs (Output arm)"
      to: "crates/ironhermes-gateway/src/handler.rs (PromptBuilder per-turn setup)"
      via: "self.active_personality_overlay field"
      pattern: "self\\.active_personality_overlay"
    - from: "crates/ironhermes-gateway/src/handler.rs"
      to: "crates/ironhermes-agent/src/prompt_builder.rs"
      via: "prompt_builder.set_overlay(text.clone())"
      pattern: "prompt_builder\\.set_overlay"
---

<objective>
Fix the Telegram gateway side of the personality-overlay bug. Add active_personality_overlay: Option<String> to GatewayMessageHandler (D-07), detect personality-apply Output in the command-dispatch match and store the text (D-08), add a /personality clear pre-dispatch check (CONTEXT D-05 gateway analog, RESEARCH §Open Question 1 Option B), and call prompt_builder.set_overlay(text.clone()) between load_skills() and build_system_message() on every turn (D-09).

Add one regression test in handlers_personality.rs that locks the contract: core dispatch for /personality clear returns CommandResult::Error — surface handlers (TUI post-router AND gateway pre-dispatch) MUST intercept "clear" before it reaches core.

Purpose: The gateway never had a personality field at all. The /personality <name> Output text was sent verbatim to the Telegram chat as a useless echo and discarded. After this plan, the LLM in Telegram sessions receives the overlay every turn until cleared.

Output: One modified gateway file, one extended test file, gateway compiles, all gateway and core tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/21.8.3.1-personality-applied-doesn-t-chage-the-llm-responses/21.8.3.1-CONTEXT.md
@.planning/phases/21.8.3.1-personality-applied-doesn-t-chage-the-llm-responses/21.8.3.1-RESEARCH.md
@.planning/phases/21.8.3.1-personality-applied-doesn-t-chage-the-llm-responses/21.8.3.1-PATTERNS.md

<interfaces>
Key contracts the executor must respect. Extracted from codebase.

`GatewayMessageHandler` field block ends at handler.rs:149 (closing brace of struct).
The `skill_overlays` field at handler.rs:85 is the structural analog —
session-persistent state initialized to empty in the constructor.

`GatewayMessageHandler::new` constructor body — the `Self { ... }` block ends near
handler.rs:172 with `skill_overlays: Arc::new(...)`. The new initializer line
must land inside this `Self { ... }` block, before the closing `}`.

Command dispatch match in `handle_slash_command` (or equivalent — the method
containing the `match self.command_router.resolve(...)` block at handler.rs:398):

  let parts: Vec<&str> = command_input.split_whitespace().collect();         // line 391
  let args: Vec<&str> = if parts.len() > 1 { parts[1..].to_vec() } else { vec![] }; // line 392-396

  match self.command_router.resolve(command_input, platform) {                // line 398
      ResolveResult::Exact(def) | ResolveResult::PrefixMatch(def) => {        // line 399
          let core_result = ironhermes_core::commands::handlers::dispatch(    // line 400
              def, &args, &ctx, &self.command_router,
          );
          match core_result {
              CoreCommandResult::Output(text) => {                            // line 407
                  with_rate_limit_retry(|| adapter.send_message(&event.chat_id, &text, None)).await?;
              }
              // ... other arms ...
          }
      }
      // ResolveResult::None / Ambiguous arms ...
  }

- `def.name` is in scope inside `CoreCommandResult::Output` arm (def is bound at the outer ResolveResult arm at line 399).
- `args` is in scope at line 391 onward — visible at the pre-dispatch check site.

Per-turn PromptBuilder setup at handler.rs:759-789:

  let mut prompt_builder = PromptBuilder::new(&model, "telegram") ...         // line 759
      .with_workspace_root(&ws.root) ... ;                                     // line 767
  prompt_builder.set_memory_manager(mgr.clone());                              // line 770
  prompt_builder.set_skill_registry(registry);                                 // line 776
  prompt_builder.load_memory().await;                                          // line 778
  prompt_builder.load_skills();                                                // line 779
  if let Ok(overlays) = self.skill_overlays.lock() {                           // line 782
      if let Some(session_overlays) = overlays.get(&key) {
          for (name, body) in session_overlays {
              prompt_builder.activate_skill(name, body);                       // line 785
          }
      }
  }
  let system_msg = prompt_builder.build_system_message();                      // line 789

`PromptBuilder::set_overlay` from prompt_builder.rs:165:

  pub fn set_overlay(&mut self, text: String) { self.active_overlay = Some(text); }

Takes `String` by value. Use `.clone()` since `self.active_personality_overlay`
retains ownership.

`with_rate_limit_retry` and `adapter.send_message` are already imported at top
of handler.rs and used throughout (e.g., line 408). No new imports required.

Existing tests in handlers_personality.rs (122-144 — `personality_apply_unknown_returns_error`)
are the EXACT fixture pattern to copy:

  fn make_test_ctx_with_personality() -> CommandContext { ... }    // line 53-66
  fn make_router() -> CommandRouter { ... }                         // line 68-73
  let result = dispatch(def, &["nonexistent_preset_xyz"], &ctx, &router); // line 134
  match result { CommandResult::Error(msg) => { ... } ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add active_personality_overlay field to GatewayMessageHandler (struct + constructor)</name>
  <files>crates/ironhermes-gateway/src/handler.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/handler.rs (lines 80-200 — struct definition and constructor)
    - .planning/phases/21.8.3.1-personality-applied-doesn-t-chage-the-llm-responses/21.8.3.1-CONTEXT.md (D-07)
    - .planning/phases/21.8.3.1-personality-applied-doesn-t-chage-the-llm-responses/21.8.3.1-PATTERNS.md (lines 173-218 — exact insertion text)
  </read_first>
  <action>
    Apply D-07 in `crates/ironhermes-gateway/src/handler.rs`:

    1. STRUCT FIELD ADDITION:
       Locate the closing `}` of the `GatewayMessageHandler` struct (around line 149 — immediately after the `workspace: Option<...>` field at line ~146 and the trailing `// Phase 25.3-15 CR-02 close-out:` comment).

       Immediately BEFORE the struct's closing `}`, insert this EXACT block (4-space indentation matching sibling fields):

           /// Phase 21.8.3.1 D-07: active personality overlay for this session.
           /// Set by command dispatch when /personality <name> succeeds (D-08).
           /// Cleared by /personality clear (CONTEXT D-05 gateway analog, pre-dispatch).
           /// Re-applied to PromptBuilder slot 8 every turn (D-09).
           /// None = default identity (no overlay injected).
           active_personality_overlay: Option<String>,

    2. CONSTRUCTOR INITIALIZER:
       Locate `GatewayMessageHandler::new` (starts at handler.rs:151). The constructor body ends with a `Self { ... }` block (around line 172). The last existing initializer line in that block is `workspace: None,` (with a trailing comment).

       Immediately BEFORE the closing `}` of the `Self { ... }` block, insert this EXACT line (preserving indentation matching sibling initializers — typically 12 spaces inside `Self { ... }`):

               // Phase 21.8.3.1 D-07: no personality active until /personality <name> sets it.
               active_personality_overlay: None,

    Constraints:
    - Do NOT change any other field or initializer.
    - Do NOT add new dependencies (no `use` imports needed — `Option<String>` is in prelude).
    - Field type MUST be `Option<String>` (NOT `Arc<...>` or `RwLock<...>`).
    - Field visibility MUST be private (no `pub` — only the impl block reads/writes it).
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; grep -c "active_personality_overlay: Option&lt;String&gt;" crates/ironhermes-gateway/src/handler.rs | awk '{ if ($1 == 1) exit 0; else exit 1 }' &amp;&amp; grep -c "active_personality_overlay: None" crates/ironhermes-gateway/src/handler.rs | awk '{ if ($1 == 1) exit 0; else exit 1 }' &amp;&amp; cargo check -p ironhermes-gateway 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "active_personality_overlay: Option<String>" crates/ironhermes-gateway/src/handler.rs` returns `1` (struct field present, exact type)
    - `grep -c "active_personality_overlay: None" crates/ironhermes-gateway/src/handler.rs` returns `1` (constructor initializer present)
    - `grep -c "pub active_personality_overlay" crates/ironhermes-gateway/src/handler.rs` returns `0` (field is NOT public)
    - `cargo check -p ironhermes-gateway 2>&1 | tail -1` ends without `error[E` lines (struct still compiles; subsequent tasks add the readers/writers)
    - The struct field appears BEFORE the closing brace of `GatewayMessageHandler` and the initializer appears inside the `Self { ... }` block — verify via `awk` ordering: `awk '/^pub struct GatewayMessageHandler/{a=NR} /active_personality_overlay: Option<String>/{b=NR} /^}/ && b>0 && c==0 {c=NR} END {exit (a<b && b<c)?0:1}' crates/ironhermes-gateway/src/handler.rs` exits 0
  </acceptance_criteria>
  <done>
    Struct has new private field `active_personality_overlay: Option<String>` with doc comment referencing D-07/D-08/D-09. Constructor initializes it to `None`. Gateway crate compiles with `cargo check`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire dispatch detection + clear pre-check + per-turn set_overlay (handler.rs)</name>
  <files>crates/ironhermes-gateway/src/handler.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/handler.rs (lines 380-480 — args computation, command_router.resolve match, CoreCommandResult::Output arm)
    - crates/ironhermes-gateway/src/handler.rs (lines 750-800 — per-turn PromptBuilder setup)
    - .planning/phases/21.8.3.1-personality-applied-doesn-t-chage-the-llm-responses/21.8.3.1-CONTEXT.md (D-08, D-09 + Claude's Discretion on gateway clear)
    - .planning/phases/21.8.3.1-personality-applied-doesn-t-chage-the-llm-responses/21.8.3.1-RESEARCH.md (Open Question 1 — Option B locked)
    - .planning/phases/21.8.3.1-personality-applied-doesn-t-chage-the-llm-responses/21.8.3.1-PATTERNS.md (lines 220-302 — exact replacement blocks)
    - crates/ironhermes-agent/src/prompt_builder.rs (lines 160-175 — confirm set_overlay signature takes String by value)
  </read_first>
  <action>
    Apply D-05 (gateway analog), D-08, D-09 in `crates/ironhermes-gateway/src/handler.rs`. Three insertions:

    --- INSERTION A: Pre-dispatch "clear" check (D-05 gateway analog, Option B per RESEARCH) ---

    Locate the line `match self.command_router.resolve(command_input, platform) {` at line 398. Find the inner `ResolveResult::Exact(def) | ResolveResult::PrefixMatch(def) =>` arm at line 399 and its body where `dispatch(...)` is called (line 400-405).

    Immediately AFTER the line `ResolveResult::Exact(def) | ResolveResult::PrefixMatch(def) => {` (line 399) and BEFORE the line `let core_result = ironhermes_core::commands::handlers::dispatch(` (line 400), insert this EXACT block (preserving indentation matching the sibling `let core_result = ...` line):

                // Phase 21.8.3.1 D-05 gateway analog (RESEARCH Open Question 1, Option B):
                // Intercept /personality clear BEFORE core dispatch. Core's cmd_personality
                // has no "clear" case — it would return Error("Unknown personality: clear")
                // and we'd send that confusing error to the user. Mirrors TUI handle_subsystem_mutator.
                if def.name == "personality" && args.first() == Some(&"clear") {
                    self.active_personality_overlay = None;
                    with_rate_limit_retry(|| {
                        adapter.send_message(&event.chat_id, "Personality cleared.", None)
                    })
                    .await?;
                    return Ok(());
                }

    --- INSERTION B: Detection in CoreCommandResult::Output arm (D-08) ---

    Locate the `CoreCommandResult::Output(text) => {` arm at line 407. Replace the ENTIRE existing arm body (the three lines: line 408-410, currently the unconditional `with_rate_limit_retry(|| adapter.send_message(&event.chat_id, &text, None)).await?;` plus closing brace) with this EXACT block (preserving indentation matching sibling arms like `CoreCommandResult::NewSession { .. } => {` at line 411):

                    CoreCommandResult::Output(text) => {
                        // Phase 21.8.3.1 D-08: detect personality apply (same heuristic as
                        // TUI handle_subsystem_mutator). Heuristic: command name is "personality"
                        // AND the Output text is NOT one of the three list/no-config prefixes.
                        // On match, store the overlay text and send a confirmation (don't echo the raw overlay).
                        let is_personality_apply = def.name == "personality"
                            && !text.starts_with("Available")
                            && !text.starts_with("Personality registry")
                            && !text.starts_with("No personalities");

                        if is_personality_apply {
                            self.active_personality_overlay = Some(text.clone());
                            let confirm = format!(
                                "Personality applied ({} chars). Active for this session.",
                                text.len()
                            );
                            with_rate_limit_retry(|| {
                                adapter.send_message(&event.chat_id, &confirm, None)
                            })
                            .await?;
                        } else {
                            with_rate_limit_retry(|| {
                                adapter.send_message(&event.chat_id, &text, None)
                            })
                            .await?;
                        }
                    }

    --- INSERTION C: Per-turn PromptBuilder overlay injection (D-09) ---

    Locate the existing skill-overlays block at handler.rs:782-788:

            if let Ok(overlays) = self.skill_overlays.lock() {
                if let Some(session_overlays) = overlays.get(&key) {
                    for (name, body) in session_overlays {
                        prompt_builder.activate_skill(name, body);
                    }
                }
            }
            let system_msg = prompt_builder.build_system_message();

    Immediately AFTER the closing `}` of the `if let Ok(overlays) = self.skill_overlays.lock() { ... }` block (the line that closes BEFORE `let system_msg = prompt_builder.build_system_message();`) and BEFORE the `let system_msg = ...` line, insert this EXACT block (matching the surrounding 8-space indentation):

            // Phase 21.8.3.1 D-09: inject active personality overlay into PromptBuilder slot 8
            // (SessionOverlay, ephemeral). Re-applied every turn from self.active_personality_overlay;
            // never explicitly cleared between turns (field holds None when no personality is active).
            // Order: AFTER load_skills + skill_overlays loop, BEFORE build_system_message.
            if let Some(ref overlay_text) = self.active_personality_overlay {
                prompt_builder.set_overlay(overlay_text.clone());
            }

    Constraints (apply to all three insertions):
    - The `&self` -vs- `&mut self` distinction: the method containing the dispatch match MUST already be `&mut self` because we mutate `self.active_personality_overlay`. If the method takes `&self`, the executor MUST report this as a blocker — DO NOT silently change the signature without filing a deviation note. (RESEARCH Pitfall 6 / Pre-existing handler signature is expected to be `&mut self` — verify when reading the file at line ~380.)
    - Do NOT remove or modify any other arm of the `match core_result { ... }` block (NewSession, ClearSession, Error, Handled, Quit, PassThrough, McpReload, ResetTerminal).
    - Do NOT touch the `skill_overlays` block — D-09 inserts AFTER it.
    - Do NOT add `prompt_builder.clear_overlay()` anywhere (D-04: never explicitly cleared between turns).
    - Confirmation message text MUST be exactly `"Personality applied ({} chars). Active for this session."` (uses "Active for this session." per RESEARCH §Open Question 2 recommendation; intentionally differs from TUI's "Active next turn." because gateway sessions persist longer).
    - "Personality cleared." text MUST match the TUI's exact string from Plan 01 Task 3.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; grep -F -c 'def.name == "personality" &amp;&amp; args.first() == Some(&amp;"clear")' crates/ironhermes-gateway/src/handler.rs | awk '{ if ($1 == 1) exit 0; else exit 1 }' &amp;&amp; grep -c "is_personality_apply" crates/ironhermes-gateway/src/handler.rs | awk '{ if ($1 &gt;= 2) exit 0; else exit 1 }' &amp;&amp; grep -c "self.active_personality_overlay = Some" crates/ironhermes-gateway/src/handler.rs | awk '{ if ($1 == 1) exit 0; else exit 1 }' &amp;&amp; grep -c "self.active_personality_overlay = None" crates/ironhermes-gateway/src/handler.rs | awk '{ if ($1 == 1) exit 0; else exit 1 }' &amp;&amp; grep -F -c "prompt_builder.set_overlay(overlay_text.clone())" crates/ironhermes-gateway/src/handler.rs | awk '{ if ($1 == 1) exit 0; else exit 1 }' &amp;&amp; grep -F -c "Personality cleared." crates/ironhermes-gateway/src/handler.rs | awk '{ if ($1 == 1) exit 0; else exit 1 }' &amp;&amp; grep -F -c "Active for this session." crates/ironhermes-gateway/src/handler.rs | awk '{ if ($1 == 1) exit 0; else exit 1 }' &amp;&amp; cargo build -p ironhermes-gateway 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F -c 'def.name == "personality" && args.first() == Some(&"clear")' crates/ironhermes-gateway/src/handler.rs` returns `1` (clear pre-check present)
    - `grep -c "self\.active_personality_overlay = None" crates/ironhermes-gateway/src/handler.rs` returns `1` (clear path writes None)
    - `grep -F -c "Personality cleared." crates/ironhermes-gateway/src/handler.rs` returns `1` (clear confirmation string present)
    - `grep -c "is_personality_apply" crates/ironhermes-gateway/src/handler.rs` returns at least `2` (one binding, one use in `if`)
    - `grep -c "self\.active_personality_overlay = Some" crates/ironhermes-gateway/src/handler.rs` returns `1` (apply path writes Some)
    - `grep -F -c "Active for this session." crates/ironhermes-gateway/src/handler.rs` returns `1` (gateway-specific confirmation wording per RESEARCH §Open Question 2)
    - `grep -F -c "prompt_builder.set_overlay(overlay_text.clone())" crates/ironhermes-gateway/src/handler.rs` returns `1` (per-turn overlay injection landed exactly once — D-09 is single-call-site)
    - `grep -c "prompt_builder\.clear_overlay" crates/ironhermes-gateway/src/handler.rs` returns `0` (D-04: never explicitly cleared)
    - Ordering: clear pre-check appears BEFORE `dispatch(`: `awk '/Some\(&"clear"\)/{a=NR} /handlers::dispatch\(/{b=NR; exit} END {exit (a<b)?0:1}' crates/ironhermes-gateway/src/handler.rs` exits 0
    - Ordering: D-09 set_overlay call appears AFTER skill_overlays block AND BEFORE build_system_message: `awk '/skill_overlays\.lock\(\)/{a=NR} /set_overlay\(overlay_text\.clone\(\)\)/{b=NR} /build_system_message\(\)/{c=NR; exit} END {exit (a<b && b<c)?0:1}' crates/ironhermes-gateway/src/handler.rs` exits 0
    - `cargo build -p ironhermes-gateway 2>&1 | tail -1` ends with no `error[E` lines
    - `cargo test -p ironhermes-gateway 2>&1 | tail -3` shows `test result: ok` (existing gateway tests not regressed)
  </acceptance_criteria>
  <done>
    Three insertions landed: (A) pre-dispatch `/personality clear` short-circuit returns confirmation without invoking core; (B) `CoreCommandResult::Output` arm detects personality-apply via name + 3-prefix exclusion, stores text in `self.active_personality_overlay`, replies with confirmation; (C) per-turn PromptBuilder setup calls `set_overlay(overlay_text.clone())` between skill_overlays loop and `build_system_message()`. Gateway compiles and existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add core regression test locking the "clear is not handled at core" contract</name>
  <files>crates/ironhermes-core/tests/handlers_personality.rs</files>
  <read_first>
    - crates/ironhermes-core/tests/handlers_personality.rs (full file, 166 lines — fixture helpers at 53-73 and existing analog test at 124-144)
    - crates/ironhermes-core/src/commands/handlers.rs (lines 581-611 — cmd_personality returns Error for unknown preset names)
    - .planning/phases/21.8.3.1-personality-applied-doesn-t-chage-the-llm-responses/21.8.3.1-CONTEXT.md (D-05 — surface-only intercept)
    - .planning/phases/21.8.3.1-personality-applied-doesn-t-chage-the-llm-responses/21.8.3.1-PATTERNS.md (lines 305-378 — exact test text)
  </read_first>
  <action>
    Append a new test to `crates/ironhermes-core/tests/handlers_personality.rs`. The test documents the contract that core's `cmd_personality` does NOT recognize "clear" — surface handlers (TUI post-router from Plan 01 Task 3, gateway pre-dispatch from Plan 02 Task 2) MUST intercept "clear" before it reaches core dispatch.

    1. Locate the end of file (after the last test `personality_no_registry_returns_not_configured` ending at line 166 with the closing `}`).

    2. Append this EXACT block at end of file (preserving Rust formatting; one blank line before the doc comment):

    /// Phase 21.8.3.1: /personality clear dispatched to core returns Error("Unknown personality: clear").
    /// Documents the contract: "clear" is NOT handled at core level.
    /// Surface-level handlers (TUI handle_subsystem_mutator pre-check; gateway pre-dispatch
    /// in handler.rs) intercept "clear" before it reaches cmd_personality.
    /// This test guards against future refactors that might add "clear" to core
    /// without coordinating with both surface-level pre-checks.
    #[test]
    fn personality_clear_at_core_returns_unknown_error() {
        let ctx = make_test_ctx_with_personality();
        let router = make_router();
        let def = router
            .commands
            .iter()
            .find(|c| c.name == "personality")
            .unwrap();
        let result = dispatch(def, &["clear"], &ctx, &router);
        match result {
            CommandResult::Error(msg) => {
                assert!(
                    msg.contains("Unknown personality") || msg.contains("clear"),
                    "core must not know about 'clear'; expected error mentioning 'Unknown personality' or 'clear', got: {msg}"
                );
            }
            other => panic!(
                "expected Error when 'clear' reaches core (surface handlers must intercept first), got {other:?}"
            ),
        }
    }

    Constraints:
    - Use `make_test_ctx_with_personality()` and `make_router()` helpers that already exist at lines 53-73 — do NOT redefine them.
    - Do NOT modify any existing test in the file.
    - Do NOT add `use` imports — `dispatch`, `CommandResult` are already in scope at the top of the test file.
    - Do NOT change `cmd_personality` in `crates/ironhermes-core/src/commands/handlers.rs` — the contract is "core does NOT know clear", not "add clear to core".
    - The test MUST pass on the current codebase (no implementation change required) — it's a contract regression guard.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; grep -c "personality_clear_at_core_returns_unknown_error" crates/ironhermes-core/tests/handlers_personality.rs | awk '{ if ($1 == 1) exit 0; else exit 1 }' &amp;&amp; grep -F -c 'dispatch(def, &amp;["clear"]' crates/ironhermes-core/tests/handlers_personality.rs | awk '{ if ($1 == 1) exit 0; else exit 1 }' &amp;&amp; cargo test -p ironhermes-core --test handlers_personality 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "personality_clear_at_core_returns_unknown_error" crates/ironhermes-core/tests/handlers_personality.rs` returns `1` (test function present)
    - `grep -F -c 'dispatch(def, &["clear"]' crates/ironhermes-core/tests/handlers_personality.rs` returns `1` (test dispatches "clear" to core)
    - `grep -c "Unknown personality" crates/ironhermes-core/tests/handlers_personality.rs` returns at least `2` (existing unknown test + new clear test both check this string)
    - `cargo test -p ironhermes-core --test handlers_personality 2>&1 | tail -3` ends with `test result: ok` and the count includes `personality_clear_at_core_returns_unknown_error` running and passing — verify with: `cargo test -p ironhermes-core --test handlers_personality personality_clear_at_core 2>&1 | grep -c "test personality_clear_at_core_returns_unknown_error ... ok"` returns `1`
    - `crates/ironhermes-core/src/commands/handlers.rs` is NOT modified by this task (core handler stays untouched): `git diff --name-only crates/ironhermes-core/src/commands/handlers.rs` reports no changes
  </acceptance_criteria>
  <done>
    Test `personality_clear_at_core_returns_unknown_error` exists in `crates/ironhermes-core/tests/handlers_personality.rs`, calls `dispatch(def, &["clear"], &ctx, &router)`, asserts `CommandResult::Error` with message containing "Unknown personality" or "clear", and PASSES on the current codebase. The core handler `cmd_personality` is unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram user → Gateway | User sends `/personality <name>` or `/personality clear` from a Telegram chat; routed through `GatewayMessageHandler` |
| Gateway state → PromptBuilder → LLM | Per-turn `PromptBuilder` reads `self.active_personality_overlay` and injects into system message |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.8.3.1-G1 | Tampering (system prompt injection via overlay) | `handler.rs` D-09 set_overlay call | accept | Overlay text originates from `PersonalityRegistry` which already runs `scan_context_content` on HERMES_HOME .md files (`personality.rs:108-115`). No new attack surface — overlay text was already pre-scanned at registry load. |
| T-21.8.3.1-G2 | Tampering (gateway echoes raw overlay text into chat history before D-08) | `handler.rs:407` original Output arm | mitigate | D-08 detection block intercepts the personality apply case and replies with a confirmation message, NOT the raw overlay text. Acceptance criterion: `is_personality_apply` branch sends `confirm` (formatted "Personality applied (N chars)..." string) instead of `&text`. |
| T-21.8.3.1-G3 | Spoofing ("clear" as user-supplied preset name) | `handler.rs` pre-dispatch check | mitigate | "clear" is not among the 14 built-in presets (`personality.rs`). User cannot register a preset named "clear" because pre-dispatch check intercepts BEFORE registry lookup. Pre-check ordering is locked by acceptance grep `awk` ordering test. |
| T-21.8.3.1-G4 | Information Disclosure (overlay leaks via Telegram message log) | Telegram chat history | accept | The previous (broken) behavior already echoed overlay text to the chat as a `CoreCommandResult::Output` reply. D-08 REDUCES disclosure: post-fix, only `"Personality applied (N chars). Active for this session."` is sent — overlay content stays server-side. Net security improvement. |
| T-21.8.3.1-G5 | Denial of Service (per-turn overlay clone allocation) | `handler.rs` D-09 `overlay_text.clone()` | accept | `String::clone()` per turn is bounded by overlay size (small — preset .md files are <1 KB typical). Re-cloning every turn is the same cost as PromptBuilder's existing per-turn build_system_message work. No new amplification. |
| T-21.8.3.1-G6 | Repudiation (silent gateway state mutation by /personality clear) | `handler.rs` pre-dispatch clear arm | mitigate | The clear path sends `"Personality cleared."` confirmation via `with_rate_limit_retry(...send_message...)`. User receives positive ACK; mutation is not silent. Acceptance criterion grep checks `Personality cleared.` string is present and `with_rate_limit_retry` is called inside the clear arm. |

All threats have explicit dispositions. No high-severity threats remain unmitigated. Per phase research, this surface introduces no new attack surface — overlay text originates from pre-scanned PersonalityRegistry content.
</threat_model>

<verification>
After all three tasks complete:

1. Compile: `cargo build -p ironhermes-gateway -p ironhermes-core 2>&1 | tail -3` succeeds with no errors.
2. Gateway tests: `cargo test -p ironhermes-gateway 2>&1 | tail -3` shows `test result: ok` (no regression).
3. Core handlers tests: `cargo test -p ironhermes-core --test handlers_personality 2>&1 | tail -3` shows `test result: ok` and includes the new test.
4. Field is wired end-to-end:
   - `grep -c "active_personality_overlay" crates/ironhermes-gateway/src/handler.rs` returns at least `5` (struct field, constructor init, clear-path write, apply-path write, per-turn read).
5. Confirmation strings landed:
   - `grep -F -c "Personality cleared." crates/ironhermes-gateway/src/handler.rs` returns `1`.
   - `grep -F -c "Active for this session." crates/ironhermes-gateway/src/handler.rs` returns `1`.
6. Cross-plan invariant (with Plan 01): no surface MUST omit the clear pre-check. Verify via combined grep:
   - `grep -F -l "Personality cleared." crates/ironhermes-cli/src/tui_rata/commands.rs crates/ironhermes-gateway/src/handler.rs | wc -l` returns `2`.
</verification>

<success_criteria>
- [ ] `GatewayMessageHandler` has private field `active_personality_overlay: Option<String>` and constructor sets it to `None`.
- [ ] Pre-dispatch `def.name == "personality" && args.first() == Some(&"clear")` check sets field to `None`, sends `"Personality cleared."` via Telegram, and `return Ok(())` BEFORE invoking `dispatch(...)`.
- [ ] `CoreCommandResult::Output` arm computes `is_personality_apply` (name == "personality" AND text NOT prefixed with the three list/no-config strings) and on match: writes `self.active_personality_overlay = Some(text.clone())`, sends `"Personality applied ({} chars). Active for this session."` confirmation.
- [ ] Per-turn `PromptBuilder` setup calls `prompt_builder.set_overlay(overlay_text.clone())` after the `skill_overlays` block and before `build_system_message()` whenever `self.active_personality_overlay.is_some()`.
- [ ] `prompt_builder.clear_overlay()` is NOT called anywhere in the gateway (D-04).
- [ ] New test `personality_clear_at_core_returns_unknown_error` in `handlers_personality.rs` PASSES on current codebase, locking the contract that "clear" is NOT a core-level case.
- [ ] `cargo build -p ironhermes-gateway` succeeds.
- [ ] `cargo test -p ironhermes-gateway` passes (no regressions).
- [ ] `cargo test -p ironhermes-core --test handlers_personality` passes including the new test.
</success_criteria>

<output>
After completion, create `.planning/phases/21.8.3.1-personality-applied-doesn-t-chage-the-llm-responses/21.8.3.1-02-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Capture:
- D-07 field addition (struct + constructor)
- D-08 detection heuristic (3-prefix exclusion + name guard)
- D-05 gateway analog (Option B from RESEARCH §Open Question 1) — pre-dispatch clear short-circuit
- D-09 PromptBuilder per-turn `set_overlay()` call site
- The new core regression test and its rationale (locks "clear is surface-only" contract for any future ACP / new-surface phase)
- Final test results: `cargo test -p ironhermes-gateway` and `cargo test -p ironhermes-core --test handlers_personality` last lines.
</output>
