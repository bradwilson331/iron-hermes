---
phase: 27.1.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-tools/src/hexapod_tcp.rs
autonomous: true
requirements:
  - HXP-NAV-01
  - HXP-NAV-03
  - HXP-NAV-04

must_haves:
  truths:
    - "Agent can call rotate with positive degrees and the tool sends CMD_MOVE#1#0#0#5#10 followed by a stop"
    - "Agent can call rotate with negative degrees and the tool sends CMD_MOVE#1#0#0#5#-10 followed by a stop"
    - "Agent can call head_pan with any angle and the tool sends CMD_HEAD#0#<clamped_angle>"
    - "Agent can call head_tilt with any angle and the tool sends CMD_HEAD#1#<clamped_angle>"
    - "Agent can call buzzer_on and the tool sends CMD_BUZZER#1"
    - "Agent can call buzzer_off and the tool sends CMD_BUZZER#0"
    - "All 5 new actions pass the outer allowlist — they do not return a blocked-command string"
    - "The tool schema lists all 10 actions in the enum and declares degrees and angle properties"
    - "All 20 unit tests pass with --test-threads=1"
  artifacts:
    - path: "crates/ironhermes-tools/src/hexapod_tcp.rs"
      provides: "Extended hexapod tool with 10 actions and 20 tests"
      contains: "build_rotate_wire"
  key_links:
    - from: "execute() outer match arm"
      to: "10-action allowlist"
      via: "exhaustive match string"
      pattern: "\"rotate\" | \"head_pan\" | \"head_tilt\" | \"buzzer_on\" | \"buzzer_off\""
    - from: "build_rotate_wire"
      to: "CMD_MOVE wire string"
      via: "pure fn → format!()"
      pattern: "CMD_MOVE#1#0#0#5#"
    - from: "test_new_actions_not_blocked"
      to: "outer allowlist arm"
      via: "async execute() call without HEXAPOD_IP set"
      pattern: "HEXAPOD_IP env var not set"
---

<objective>
Extend crates/ironhermes-tools/src/hexapod_tcp.rs with five new robot actions
(rotate, head_pan, head_tilt, buzzer_on, buzzer_off) plus seven new unit tests
(tests 14–20) following the exact patterns established in Phase 27.1.1.

Purpose: Delivers HXP-NAV-01 (rotate), HXP-NAV-03 (head pan/tilt), HXP-NAV-04
(buzzer) in a single atomic change to one file. No new crates, no new files,
no Cargo.toml changes.

Output: hexapod_tcp.rs with 10 actions, 5 new constants, 1 new helper function,
updated schema and DESCRIPTION, and 20 passing unit tests.
</objective>

<execution_context>
@/Users/twilson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/twilson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/twilson/code/ironhermes/.planning/PROJECT.md
@/Users/twilson/code/ironhermes/.planning/ROADMAP.md
@/Users/twilson/code/ironhermes/.planning/STATE.md
@/Users/twilson/code/ironhermes/.planning/phases/27.1.2-navigation-inserted/27.1.2-CONTEXT.md
@/Users/twilson/code/ironhermes/.planning/phases/27.1.2-navigation-inserted/27.1.2-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from hexapod_tcp.rs. -->
<!-- All additions extend this file — no codebase exploration needed beyond reading it. -->

From crates/ironhermes-tools/src/hexapod_tcp.rs (existing):

```rust
// Existing constants block (add new constants here):
pub(crate) const STOP_CMD: &str = "CMD_MOVE#0#0#0#0#0\n";
pub(crate) const BATTERY_LOW_V1: f32 = 5.5;
pub(crate) const BATTERY_LOW_V2: f32 = 6.0;

// Existing pure helper (model build_rotate_wire on this):
pub(crate) fn build_walk_wire(direction: &str, speed: i64) -> String { ... }

// Existing fire-and-forget TCP helper (reuse directly for all 5 new actions):
async fn send_fire_and_forget(addr: &str, cmd: &str) -> anyhow::Result<()> { ... }

// Outer match allowlist (currently 5 actions — extend to 10):
match action {
    "walk" | "stop" | "read_battery" | "read_distance" | "relax_servos" => { ... }
    other => Ok(format!("Action '{other}' is blocked ..."))
}

// Inner match (add 5 new arms before the unreachable!() arm):
match action {
    "walk" => { ... }
    "stop" => { ... }
    "relax_servos" => { ... }
    "read_battery" => { ... }
    "read_distance" => { ... }
    _ => unreachable!(...)
}

// Test pattern (ENV_LOCK + tokio::test for async, plain #[test] for pure fns):
static ENV_LOCK: Mutex<()> = Mutex::new(());
// Tests 1–13 already exist; add 14–20 in the same mod tests { } block
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 5 new constants, build_rotate_wire helper, and 5 new execute() arms</name>
  <files>crates/ironhermes-tools/src/hexapod_tcp.rs</files>

  <read_first>
    crates/ironhermes-tools/src/hexapod_tcp.rs — read the entire file before touching
    anything. Understand the constants block layout, build_walk_wire signature and doc
    comment style, the two-level match structure in execute(), and the existing test
    numbering (tests 1–13 currently in mod tests).
  </read_first>

  <behavior>
    - build_rotate_wire(90) returns "CMD_MOVE#1#0#0#5#10\n"
    - build_rotate_wire(-45) returns "CMD_MOVE#1#0#0#5#-10\n"
    - build_rotate_wire(0) returns "CMD_MOVE#1#0#0#5#10\n" (zero is non-negative)
    - BUZZER_ON_CMD == "CMD_BUZZER#1\n"
    - BUZZER_OFF_CMD == "CMD_BUZZER#0\n"
    - execute({action: "rotate", degrees: 90}) with no HEXAPOD_IP → "Error: HEXAPOD_IP env var not set"
    - execute({action: "head_pan", angle: 180}) with HEXAPOD_IP set → attempts TCP with wire "CMD_HEAD#0#90\n" (clamped)
    - execute({action: "buzzer_on"}) with no HEXAPOD_IP → "Error: HEXAPOD_IP env var not set"
  </behavior>

  <action>
    Read the file first. Then make the following additions in order:

    1. CONSTANTS BLOCK — append after the existing BATTERY_LOW_V2 constant line
       (keep within the "Wire-protocol constants" section):
       - pub(crate) const ROTATE_MS_PER_DEGREE: u64 = 20;
         doc: "Calibration placeholder — tune on real robot after live testing. Per D-04."
       - pub(crate) const HEAD_PAN_MAX: i64 = 90;
         doc: "Protocol maximum for head pan servo (§6 of Freenove protocol). Per D-08."
       - pub(crate) const HEAD_TILT_MAX: i64 = 90;
         doc: "Protocol maximum for head tilt servo (§6 of Freenove protocol). Per D-08."
       - pub(crate) const BUZZER_ON_CMD: &str = "CMD_BUZZER#1\n";
         doc: "Buzzer on command. Per D-10."
       - pub(crate) const BUZZER_OFF_CMD: &str = "CMD_BUZZER#0\n";
         doc: "Buzzer off command. Per D-11."

    2. DESCRIPTION CONST — replace the existing DESCRIPTION value with one that lists
       all 10 actions: walk, stop, read_battery, read_distance, relax_servos, rotate,
       head_pan, head_tilt, buzzer_on, buzzer_off. Clarify that degrees (rotate only)
       and angle (head_pan/head_tilt only) are action-specific params. Per D-15.

    3. PURE HELPER — add build_rotate_wire after build_walk_wire, before parse_battery_response:
       ```
       pub(crate) fn build_rotate_wire(degrees: i64) -> String {
           let angle = if degrees >= 0 { 10i64 } else { -10i64 };
           format!("CMD_MOVE#1#0#0#5#{angle}\n")
       }
       ```
       Doc comment: "Build the CMD_MOVE wire string for rotate-in-place (D-03).
       angle=+10 clockwise, angle=-10 counterclockwise. x=0, y=0 → in-place. Speed fixed at 5 per D-02."

    4. OUTER ALLOWLIST ARM — in execute(), change the outer match arm from:
         "walk" | "stop" | "read_battery" | "read_distance" | "relax_servos"
       to:
         "walk" | "stop" | "read_battery" | "read_distance" | "relax_servos"
         | "rotate" | "head_pan" | "head_tilt" | "buzzer_on" | "buzzer_off"
       Per D-20 (inherited): both the outer allowlist arm AND inner dispatch arm must be updated.

    5. INNER MATCH ARMS — add 5 new arms inside the inner match, before the unreachable!() arm.
       Place them after "read_distance" and before the unreachable catch-all.

       "rotate" arm (per D-03, D-04, D-05):
         - let degrees = args["degrees"].as_i64().unwrap_or(0);
         - let wire = build_rotate_wire(degrees);
         - let duration = Duration::from_millis(degrees.unsigned_abs() * ROTATE_MS_PER_DEGREE);
         - On send_fire_and_forget error → return D-17 error string
         - On success: tokio::time::sleep(duration).await; then best-effort stop:
             let _ = send_fire_and_forget(&addr, STOP_CMD).await;
         - Return Ok("OK".to_string())

       "head_pan" arm (per D-06, D-08, D-09):
         - let raw = args["angle"].as_i64().unwrap_or(0);
         - let angle = raw.clamp(-HEAD_PAN_MAX, HEAD_PAN_MAX);
         - let wire = format!("CMD_HEAD#0#{angle}\n");
         - send_fire_and_forget, return Ok("OK") or D-17 error string.

       "head_tilt" arm (per D-07, D-08, D-09):
         - let raw = args["angle"].as_i64().unwrap_or(0);
         - let angle = raw.clamp(-HEAD_TILT_MAX, HEAD_TILT_MAX);
         - let wire = format!("CMD_HEAD#1#{angle}\n");
         - send_fire_and_forget, return Ok("OK") or D-17 error string.

       "buzzer_on" arm (per D-10):
         - send_fire_and_forget(&addr, BUZZER_ON_CMD)
         - return Ok("OK") or D-17 error string.

       "buzzer_off" arm (per D-11):
         - send_fire_and_forget(&addr, BUZZER_OFF_CMD)
         - return Ok("OK") or D-17 error string.

       The unreachable!() arm comment must be updated to reference "10 allowed actions".

    6. SCHEMA — in schema(), update:
       - "action" enum to: ["walk","stop","read_battery","read_distance","relax_servos",
           "rotate","head_pan","head_tilt","buzzer_on","buzzer_off"]
       - Add "degrees" property: type integer, description "Rotation in degrees. Positive=clockwise
           (right), negative=counterclockwise (left). Used only for the rotate action."
       - Add "angle" property: type integer, minimum -90, maximum 90, description
           "Head servo angle in degrees (-90 to 90). Used only for head_pan and head_tilt actions."
       Per D-13, D-14.

    CRITICAL: Use tokio::time::sleep — NOT std::thread::sleep. The tokio crate already
    imports Duration from tokio::time at the top of the file. Do not add any new use imports;
    call tokio::time::sleep(...) with a Duration value.

    CRITICAL: The outer allowlist match arm and inner dispatch arm are SEPARATE match blocks
    in the existing code. Both must list all 10 actions. The compiler does NOT catch the outer
    arm omission — only test 20 (added in Task 2) will catch it.

    Do not add fenced code blocks in doc comments. Use plain prose doc comments only.
  </action>

  <verify>
    <automated>cargo test -p ironhermes-tools hexapod_tcp -- --test-threads=1</automated>
  </verify>

  <acceptance_criteria>
    1. cargo test -p ironhermes-tools hexapod_tcp -- --test-threads=1 exits 0 (all 13 existing tests pass; new code does not break anything)
    2. grep 'ROTATE_MS_PER_DEGREE' crates/ironhermes-tools/src/hexapod_tcp.rs outputs at least one match in the constants block
    3. grep 'build_rotate_wire' crates/ironhermes-tools/src/hexapod_tcp.rs outputs matches for both the fn definition and call site in the rotate arm
    4. grep '"rotate" | "head_pan" | "head_tilt" | "buzzer_on" | "buzzer_off"' crates/ironhermes-tools/src/hexapod_tcp.rs confirms the outer allowlist arm contains all 5 new actions (or equivalent multi-line form)
    5. grep '"degrees"' crates/ironhermes-tools/src/hexapod_tcp.rs confirms the schema has a degrees property
    6. grep '"angle"' crates/ironhermes-tools/src/hexapod_tcp.rs confirms the schema has an angle property
    7. grep 'tokio::time::sleep' crates/ironhermes-tools/src/hexapod_tcp.rs confirms async sleep is used in the rotate arm
    8. grep -v 'thread::sleep' crates/ironhermes-tools/src/hexapod_tcp.rs has no std::thread::sleep anywhere in the file
    9. cargo clippy -p ironhermes-tools -- -D warnings exits 0 (no new warnings introduced)
  </acceptance_criteria>

  <done>
    File compiles cleanly. All 13 existing tests pass. The 5 new action arms are present
    in both the outer allowlist and inner dispatch match. Schema has 10-value enum plus
    degrees and angle properties. build_rotate_wire helper exists as a pure function.
    DESCRIPTION updated to list all 10 actions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests 14–20 (7 unit tests for Phase 27.1.2 actions)</name>
  <files>crates/ironhermes-tools/src/hexapod_tcp.rs</files>

  <read_first>
    crates/ironhermes-tools/src/hexapod_tcp.rs — read the mod tests block to see the
    existing test 1–13 structure (numbering, doc comment header style, ENV_LOCK usage
    pattern) before adding the new tests. The new tests must follow the exact same style.
  </read_first>

  <action>
    Append 7 new tests inside the existing #[cfg(test)] mod tests { } block, after test 13
    (test_read_timeout_branch). Follow the existing header comment style:
    "// -----------------------------------------------------------------------"
    "// Test N: <one-line description>"
    "// -----------------------------------------------------------------------"

    TEST 14 — test_rotate_wire_positive_degrees (plain #[test], no async, no ENV_LOCK):
      assert_eq!(build_rotate_wire(90), "CMD_MOVE#1#0#0#5#10\n");
      Description: "Positive degrees → angle=+10 in wire (D-03, clockwise)"

    TEST 15 — test_rotate_wire_negative_degrees (plain #[test]):
      assert_eq!(build_rotate_wire(-45), "CMD_MOVE#1#0#0#5#-10\n");
      Description: "Negative degrees → angle=-10 in wire (D-03, counterclockwise)"

    TEST 16 — test_rotate_wire_zero_degrees (plain #[test]):
      assert_eq!(build_rotate_wire(0), "CMD_MOVE#1#0#0#5#10\n");
      Description: "Zero degrees is non-negative → angle=+10 (boundary: positive branch)"

    TEST 17 — test_head_pan_clamps_high (plain #[test]):
      let clamped = 180i64.clamp(-HEAD_PAN_MAX, HEAD_PAN_MAX);
      assert_eq!(clamped, 90);
      let wire = format!("CMD_HEAD#0#{clamped}\n");
      assert_eq!(wire, "CMD_HEAD#0#90\n");
      Description: "head_pan angle=180 clamped to HEAD_PAN_MAX=90 before wire format (D-06, D-08)"

    TEST 18 — test_head_tilt_clamps_low (plain #[test]):
      let clamped = (-180i64).clamp(-HEAD_TILT_MAX, HEAD_TILT_MAX);
      assert_eq!(clamped, -90);
      let wire = format!("CMD_HEAD#1#{clamped}\n");
      assert_eq!(wire, "CMD_HEAD#1#-90\n");
      Description: "head_tilt angle=-180 clamped to -HEAD_TILT_MAX=-90 before wire format (D-07, D-08)"

    TEST 19 — test_buzzer_wire_strings (plain #[test]):
      assert_eq!(BUZZER_ON_CMD, "CMD_BUZZER#1\n");
      assert_eq!(BUZZER_OFF_CMD, "CMD_BUZZER#0\n");
      Description: "Buzzer wire constant strings match protocol §5 (D-10, D-11)"

    TEST 20 — test_new_actions_not_blocked (#[tokio::test], async, uses ENV_LOCK):
      Acquire ENV_LOCK. Remove HEXAPOD_IP. For each of the 5 new action names
      ["rotate", "head_pan", "head_tilt", "buzzer_on", "buzzer_off"], call
      tool.execute(json!({"action": <name>, "degrees": 0, "angle": 0})) and assert
      the result does NOT start with "Action '" (i.e., it is NOT the blocked-command
      string). The expected response is "Error: HEXAPOD_IP env var not set" since no
      HEXAPOD_IP is set. This proves all 5 new actions pass the outer allowlist arm
      and reach the env-var read instead of hitting the catch-all.
      Description: "New Phase 2 actions pass outer allowlist (D-20) — return env-var error, not blocked string"

    After all 7 tests are added, the test module closes with the existing closing brace.
  </action>

  <verify>
    <automated>cargo test -p ironhermes-tools hexapod_tcp -- --test-threads=1 2>&1</automated>
  </verify>

  <acceptance_criteria>
    1. cargo test -p ironhermes-tools hexapod_tcp -- --test-threads=1 exits 0
    2. Output contains "test tests::test_rotate_wire_positive_degrees ... ok"
    3. Output contains "test tests::test_rotate_wire_negative_degrees ... ok"
    4. Output contains "test tests::test_rotate_wire_zero_degrees ... ok"
    5. Output contains "test tests::test_head_pan_clamps_high ... ok"
    6. Output contains "test tests::test_head_tilt_clamps_low ... ok"
    7. Output contains "test tests::test_buzzer_wire_strings ... ok"
    8. Output contains "test tests::test_new_actions_not_blocked ... ok"
    9. Output shows exactly 20 tests pass (0 failures)
    10. cargo test -p ironhermes-tools -- --test-threads=1 exits 0 (full crate suite still green)
  </acceptance_criteria>

  <done>
    All 20 tests pass. Tests 14–16 verify build_rotate_wire outputs. Tests 17–18 verify
    head servo angle clamping. Test 19 verifies buzzer wire constants. Test 20 verifies
    all 5 new actions pass the outer allowlist and are not incorrectly blocked.
    Full crate test suite is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → execute() | LLM-provided action string and integer params cross into the tool layer |
| execute() → TCP socket | Wire bytes sent to robot; robot trusts all bytes unconditionally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.1.2-01 | Spoofing | execute() outer match arm | mitigate | D-20 compile-time exhaustive match; new actions added to outer allowlist arm; test 20 asserts no new action accidentally blocked |
| T-27.1.2-02 | Tampering | head_pan/head_tilt angle param | mitigate | i64::clamp(-HEAD_PAN_MAX, HEAD_PAN_MAX) before wire format; out-of-range angle silently clamped; tests 17–18 assert clamp behavior |
| T-27.1.2-03 | Tampering | rotate degrees param | accept | degrees.unsigned_abs() * ROTATE_MS_PER_DEGREE is safe for all realistic i64 inputs (u64 overflow requires >9×10^17 degrees); no additional mitigation needed |
| T-27.1.2-04 | DoS | rotate action blocking tokio executor | mitigate | tokio::time::sleep (non-blocking) used exclusively; std::thread::sleep forbidden; acceptance criterion 8 grepping for thread::sleep |
| T-27.1.2-05 | DoS | rotate stop command after sleep | accept | Stop is best-effort after sleep; if TCP connection fails, robot continues rotating until physical stop or power cycle — acceptable for local-network robot with human present |
</threat_model>

<verification>
Full phase verification gate (run before /gsd-verify-work):

  cargo test -p ironhermes-tools -- --test-threads=1

Must output: 20 tests pass, 0 failed.

Secondary check — confirm no std::thread::sleep in file:
  grep 'thread::sleep' crates/ironhermes-tools/src/hexapod_tcp.rs
Must return no matches.

Confirm outer allowlist includes all 10 actions (inline check):
  grep -c '"rotate"' crates/ironhermes-tools/src/hexapod_tcp.rs
Must return >= 2 (outer arm + inner arm + at minimum test 20).
</verification>

<success_criteria>
- hexapod_tcp.rs compiles without warnings (cargo clippy -p ironhermes-tools -- -D warnings exits 0)
- All 20 unit tests pass with --test-threads=1
- 5 new constants present: ROTATE_MS_PER_DEGREE, HEAD_PAN_MAX, HEAD_TILT_MAX, BUZZER_ON_CMD, BUZZER_OFF_CMD
- build_rotate_wire pure helper present and tested (tests 14–16)
- DESCRIPTION const lists all 10 action names
- Schema enum has 10 values; degrees and angle properties declared
- Outer allowlist match arm covers all 10 actions
- tokio::time::sleep used for rotate; no std::thread::sleep anywhere
</success_criteria>

<output>
After completion, create .planning/phases/27.1.2-navigation-inserted/27.1.2-01-SUMMARY.md
following the template at ~/.claude/get-shit-done/templates/summary.md
</output>
