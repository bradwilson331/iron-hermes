---
phase: 33
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-core/src/skills.rs
  - crates/ironhermes-agent/src/prompt_builder.rs
autonomous: true
requirements: [LEARN-03, LEARN-04]

must_haves:
  truths:
    - "SkillSource::SelfCreated variant compiles in all crates that import SkillSource"
    - "Scan enforcement match in try_register_skill_from_dir handles SelfCreated with WARN-BUT-LOAD (not hard-reject)"
    - "validate_skill_name is pub so ironhermes-tools can call it cross-crate"
    - "Trigger guidance block appears in system prompt when skill_manage is registered and skill_creation_guidance = true"
  artifacts:
    - path: "crates/ironhermes-core/src/skills.rs"
      provides: "SelfCreated variant in SkillSource enum + pub validate_skill_name"
      contains: "SelfCreated"
    - path: "crates/ironhermes-agent/src/prompt_builder.rs"
      provides: "Skill creation trigger guidance injected into system prompt"
      contains: "skill_manage"
  key_links:
    - from: "crates/ironhermes-tools/src/skill_manage.rs"
      to: "ironhermes_core::skills::validate_skill_name"
      via: "pub fn call from external crate"
      pattern: "pub fn validate_skill_name"
    - from: "crates/ironhermes-core/src/skills.rs:try_register_skill_from_dir"
      to: "SkillSource::SelfCreated"
      via: "exhaustive match arm"
      pattern: "SelfCreated"
---

<objective>
Add SkillSource::SelfCreated to the ironhermes-core skills enum, expose validate_skill_name as pub, and inject the skill-creation trigger guidance block into the system prompt.

Purpose: This plan provides the core data-model changes and behavioral guidance that Plans 02 and 03 build on. Without SelfCreated in the enum, Plan 02's tool cannot assign the trust tier. Without pub validate_skill_name, Plan 02 cannot validate slugs cross-crate. Without the guidance block, the agent has no instruction to call skill_manage post-task.

Output: Modified skills.rs with SelfCreated variant + pub validate_skill_name; modified prompt_builder.rs with trigger guidance injection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-autonomous-skill-creation/33-RESEARCH.md
</context>

<interfaces>
From crates/ironhermes-core/src/skills.rs (lines ~118-128):
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub enum SkillSource {
    Builtin,
    Official,
    Trusted,
    Community,
    // ADD: SelfCreated with #[serde(rename = "Self-created")]
}
impl Default for SkillSource {
    fn default() -> Self { SkillSource::Builtin }
}
```

Scan enforcement match in try_register_skill_from_dir (skills.rs ~line 578-596):
```rust
if scan_result.starts_with("[BLOCKED:") {
    match source {
        SkillSource::Community => { warn!(...); return; }  // hard-reject
        SkillSource::Builtin | SkillSource::Official | SkillSource::Trusted => {
            warn!("WARN-BUT-LOAD ...");  // proceed
        }
        // Phase 33 adds: SkillSource::SelfCreated => WARN-BUT-LOAD arm
    }
}
```

validate_skill_name currently private (line ~39):
```rust
fn validate_skill_name(name: &str) -> Result<(), &'static str> {
```

scan_skill_content signature (context_scanner.rs line ~133):
```rust
pub fn scan_skill_content(content: &str, filename: &str) -> String;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SkillSource::SelfCreated + pub validate_skill_name</name>
  <files>crates/ironhermes-core/src/skills.rs</files>
  <read_first>
    - crates/ironhermes-core/src/skills.rs — read the full file. Extract: (a) SkillSource enum body and all serde derives, (b) validate_skill_name definition line, (c) try_register_skill_from_dir function including the scan enforcement match block, (d) resolve_source function, (e) ALL sites with "SkillSource::" to find every exhaustive match that will need a new arm.
  </read_first>
  <behavior>
    - Test: SkillSource::SelfCreated serializes to the string "Self-created" (hyphen, not camelCase)
    - Test: validate_skill_name("git-workflow") returns Ok(()) when called from ironhermes-core test
    - Test: validate_skill_name("bad name!") returns Err with a message
    - Test: cargo build --workspace exits 0 (compiler catches any missed match arm)
  </behavior>
  <action>
    Step 1 — Add SelfCreated to SkillSource enum after Community. Add #[serde(rename = "Self-created")] attribute on the variant so it serializes/deserializes as "Self-created" (with hyphen) per RESEARCH.md Pattern 3 frontmatter example and the agentskills.io spec. The enum already derives Serialize/Deserialize so a variant-level rename attribute is the correct approach.

    Step 2 — Update ALL exhaustive match arms on SkillSource in this file. Before writing, search for every "SkillSource::" occurrence to find all match sites. Critical sites include: (a) the scan enforcement match in try_register_skill_from_dir — add SelfCreated to the WARN-BUT-LOAD arm alongside Builtin | Official | Trusted with comment "// Phase 33: SelfCreated — WARN-BUT-LOAD (agent-authored, not untrusted external)"; (b) resolve_source return-value logic if it has a match; (c) any Display or FromStr impl; (d) any test that matches on SkillSource. Do NOT put SelfCreated in the hard-reject Community arm.

    Step 3 — Change validate_skill_name from `fn` to `pub fn`. Add doc comment above it: "/// Validates a skill slug against agentskills.io name rules. Returns Err with a human-readable message if invalid. Exposed as pub in Phase 33 for cross-crate use by SkillManageTool."

    Step 4 — Do NOT change any other behavior. All existing logic, all existing tests, all existing function signatures remain byte-for-byte identical except the three targeted changes above.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-core 2>&amp;1 | grep "^error" | wc -l | tr -d ' '</automated>
    <automated>cargo test -p ironhermes-core 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "SelfCreated" crates/ironhermes-core/src/skills.rs returns >= 3 (enum body + WARN-BUT-LOAD arm + at least one other match)
    - grep "pub fn validate_skill_name" crates/ironhermes-core/src/skills.rs returns exactly 1 line
    - grep "Self-created" crates/ironhermes-core/src/skills.rs returns the serde rename line
    - cargo build -p ironhermes-core exits 0 with zero "^error" lines
    - cargo test -p ironhermes-core exits 0 (all pre-existing skill tests still pass)
  </acceptance_criteria>
  <done>SelfCreated compiles in WARN-BUT-LOAD arm; validate_skill_name is pub; cargo test -p ironhermes-core exits 0</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject skill-creation trigger guidance into system prompt</name>
  <files>crates/ironhermes-agent/src/prompt_builder.rs</files>
  <read_first>
    - crates/ironhermes-agent/src/prompt_builder.rs — read fully. Find: the build() method or equivalent entry point, where Phase 32 periodic nudge guidance is injected (search for "nudge" or "learning"), how the tool registry or tool schema list is accessed at prompt-build time, what config struct/fields are read.
    - The config file that defines LearningConfig (likely crates/ironhermes-core/src/config.rs) — grep for "LearningConfig\|learning\|periodic_nudge" to find the struct definition and add skill_creation_guidance field.
    - cli-config.yaml.example — find the learning: block to understand the existing shape.
  </read_first>
  <behavior>
    - Test: When a tool list containing a tool named "skill_manage" is passed, the assembled system prompt string contains "Skill Creation" section header and "skill_manage"
    - Test: When skill_creation_guidance = false in LearningConfig, the guidance block is absent from the system prompt
    - Test: Guidance text contains all four trigger conditions: "5 or more tool calls", "tool error", "corrected", "non-obvious workflow"
  </behavior>
  <action>
    Step 1 — If LearningConfig does not have a skill_creation_guidance: bool field, add it with #[serde(default = "default_skill_creation_guidance")] and a private fn default_skill_creation_guidance() -> bool { true }. Place it adjacent to periodic_nudge_interval_seconds in the struct. Keep all other fields unchanged.

    Step 2 — In prompt_builder.rs, find the injection site where the system prompt is assembled (the method that appends behavioral guidance sections). Add the following logic: if the tool registry/schema list contains a tool named "skill_manage" AND config.learning.skill_creation_guidance == true, append the guidance block.

    Step 3 — The guidance block to inject as a new section (use the exact text from RESEARCH.md Pattern 6):

    Section header: "## Skill Creation (Learning Loop)"

    Body: "After completing a task, evaluate whether the approach is worth documenting. Write a SKILL.md if ANY of these conditions is true:\n- You made 5 or more tool calls to complete the task\n- You recovered from a tool error or unexpected result during the task\n- The user corrected your approach mid-task\n- You discovered a non-obvious workflow that worked well\n\nWhen creating a skill, call skill_manage(action=\"create\", ...). For subsequent improvements to an existing skill, prefer skill_manage(action=\"patch\", ...) — pass only the changed substring, not the full file. Full rewrites use action=\"edit\".\n\nSelf-created skills appear in your skill index next session. Choose a descriptive category (e.g. \"development\", \"automation\", \"data\", \"research\") and a kebab-case name."

    Step 4 — Do NOT add a code-side tool-call counter in agent_loop.rs. The guidance is the trigger mechanism per RESEARCH.md Pattern 6 and the Architectural Responsibility Map. The agent judges whether to call skill_manage; code does not force the call.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-agent 2>&amp;1 | grep "^error" | wc -l | tr -d ' '</automated>
    <automated>cargo test -p ironhermes-agent -- prompt 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "skill_manage" crates/ironhermes-agent/src/prompt_builder.rs returns >= 1 line (the injection condition check)
    - grep -n "Skill Creation" crates/ironhermes-agent/src/prompt_builder.rs returns the section header line
    - grep -n "skill_creation_guidance" crates/ironhermes-core/src/config.rs (or config module) returns >= 1 line
    - grep "5 or more tool calls" crates/ironhermes-agent/src/prompt_builder.rs returns the trigger condition line
    - cargo build -p ironhermes-agent exits 0
  </acceptance_criteria>
  <done>Trigger guidance present in system prompt output when skill_manage registered + skill_creation_guidance=true; cargo build -p ironhermes-agent exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output -> SkillSource assignment | Agent labels skills SelfCreated; code enforces trust tier at registry load scan |
| System prompt injection | Guidance text is a static string literal — not derived from user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01-A | Elevation of Privilege | SkillSource::SelfCreated in scan enforcement match | mitigate | SelfCreated placed in WARN-BUT-LOAD arm (not Community hard-reject, not scan-skip); scan_skill_content still runs at registry load |
| T-33-01-B | Tampering | prompt_builder.rs trigger guidance text | accept | Guidance is a hardcoded string literal; no user-controlled content enters the injection site |
| T-33-01-SC | Tampering | cargo workspace dependencies | accept | Zero new packages installed in this plan; no legitimacy gate needed |
</threat_model>

<verification>
cargo build --workspace exits 0
cargo test -p ironhermes-core exits 0
grep -c "SelfCreated" crates/ironhermes-core/src/skills.rs returns >= 3
grep "pub fn validate_skill_name" crates/ironhermes-core/src/skills.rs returns 1 line
grep "skill_manage" crates/ironhermes-agent/src/prompt_builder.rs returns >= 1 line
</verification>

<success_criteria>
- SkillSource::SelfCreated compiles across the workspace with no non-exhaustive pattern errors
- validate_skill_name is pub and callable cross-crate
- SelfCreated is in the WARN-BUT-LOAD arm of the scan enforcement match (not hard-reject)
- System prompt includes skill-creation guidance when skill_manage is registered and skill_creation_guidance=true
- All pre-existing ironhermes-core skill tests still pass
</success_criteria>

<output>
Create `.planning/phases/33-autonomous-skill-creation/33-01-SUMMARY.md` when done
</output>
