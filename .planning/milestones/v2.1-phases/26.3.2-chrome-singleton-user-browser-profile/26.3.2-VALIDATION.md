---
phase: 26.3.2
slug: chrome-singleton-user-browser-profile
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-12
---

# Phase 26.3.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `26.3.2-RESEARCH.md` § Validation Architecture; Per-Task map populated by the planner (Plan 26.3.2-01).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in `#[test]` (sync) |
| **Config file** | none — workspace-level `cargo test` |
| **Quick run command** | `cargo test -p ironhermes-core --lib -- browser_profile` |
| **Full suite command** | `cargo test -p ironhermes-core -p ironhermes-tools -p ironagent-tools-api` |
| **Estimated runtime** | ~10 seconds (quick) / ~60–120 s (three-crate) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-core --lib -- browser_profile`
- **After every plan wave:** Run `cargo test -p ironhermes-core -p ironhermes-tools -p ironagent-tools-api`
- **Before `/gsd-verify-work`:** `cargo test --workspace` must be green
- **Max feedback latency:** ~10 seconds

---

## Per-Task Verification Map

> All work is in Plan 26.3.2-01 (single plan, single wave). Requirement IDs are the D-NN decisions from `26.3.2-CONTEXT.md` (no REQUIREMENTS.md entries this phase). Test IDs SL-01..SL-08 map to the 7 unit tests in `browser_profile.rs` (SL-01..SL-07) plus the both-crates-compile check (SL-08).

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| Task 1 | 26.3.2-01 | 1 | D-01 (SL-01) | — | no lock present → `UseProfile`, nothing deleted | unit | `cargo test -p ironhermes-core --lib -- browser_profile::tests::no_lock_returns_use_profile` | ❌ (created by Task 1) | ⬜ pending |
| Task 1 | 26.3.2-01 | 1 | D-01 (SL-02) | T-26.3.2-02 | unparseable lock (regular file) → `UseProfile`, NOT deleted | unit | `cargo test -p ironhermes-core --lib -- browser_profile::tests::unparseable_regular_file_returns_use_profile_without_deletion` | ❌ (created by Task 1) | ⬜ pending |
| Task 1 | 26.3.2-01 | 1 | D-01 (SL-03) | T-26.3.2-02 | unparseable symlink target (no `-<digits>`) → `UseProfile`, NOT deleted | unit | `cargo test -p ironhermes-core --lib -- browser_profile::tests::unparseable_bad_target_returns_use_profile_without_deletion` | ❌ (created by Task 1) | ⬜ pending |
| Task 1 | 26.3.2-01 | 1 | D-02 (SL-04) | T-26.3.2-01 | stale lock (dead pid `i32::MAX as u32`) → `UseProfile` + `SingletonLock` removed | unit | `cargo test -p ironhermes-core --lib -- browser_profile::tests::stale_lock_returns_use_profile_and_removes_sentinel` | ❌ (created by Task 1) | ⬜ pending |
| Task 1 | 26.3.2-01 | 1 | D-04 (SL-05) | — | stale lock → all 3 sentinels (`SingletonLock`/`SingletonSocket`/`SingletonCookie`) removed | unit | `cargo test -p ironhermes-core --lib -- browser_profile::tests::stale_lock_also_removes_socket_and_cookie` | ❌ (created by Task 1) | ⬜ pending |
| Task 1 | 26.3.2-01 | 1 | D-05/D-06 (SL-06) | T-26.3.2-03 | live lock (pid = `std::process::id()`) → `UseEphemeral` + `SingletonLock` NOT deleted | unit | `cargo test -p ironhermes-core --lib -- browser_profile::tests::live_lock_returns_use_ephemeral_and_preserves_sentinel` | ❌ (created by Task 1) | ⬜ pending |
| Task 1 | 26.3.2-01 | 1 | D-01/D-03 (SL-07) | — | target parser splits on LAST `-` (regression: `Brads-MacBook-Pro.local-20704`) | unit | `cargo test -p ironhermes-core --lib -- browser_profile::tests::parse_singleton_target_handles_hostname_with_dashes` | ❌ (created by Task 1) | ⬜ pending |
| Task 1 | 26.3.2-01 | 1 | D-07/D-08/D-09/D-10 | — | `pub mod browser_profile;` registered; `#[cfg(not(unix))]` returns `UseProfile` (no panic); no `println!`/`eprintln!`; no `nix` dep | compile + grep | `cargo test -p ironhermes-core --lib -- browser_profile` (compiles) ; `! grep -n 'nix' crates/ironhermes-core/Cargo.toml` | ❌ (created by Task 1) | ⬜ pending |
| Task 2 | 26.3.2-01 | 1 | D-05/D-07/D-08 (SL-08) | — | both `browser_session.rs` `spawn()` call sites compile against the shared helper; `reconcile_singleton_lock` precedes `builder.user_data_dir(...)` in each | compile | `cargo build -p ironhermes-tools -p ironagent-tools-api` | ❌ (edited by Task 2) | ⬜ pending |
| Task 2 | 26.3.2-01 | 1 | D-07 (regression) | — | three-crate test set green after wiring | unit | `cargo test -p ironhermes-core -p ironhermes-tools -p ironagent-tools-api` | ❌ (depends on Task 1+2) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

### Nyquist sampling continuity

Both tasks in Plan 26.3.2-01 have `<automated>` verify commands (Task 1: `cargo test -p ironhermes-core --lib -- browser_profile`; Task 2: `cargo build -p ironhermes-tools -p ironagent-tools-api && cargo test -p ironhermes-core -p ironhermes-tools -p ironagent-tools-api`). No run of ≥3 consecutive tasks without an automated verify. Compliant.

---

## Wave 0 Requirements

- [ ] `crates/ironhermes-core/src/browser_profile.rs` — new file: the `reconcile_singleton_lock(&Path) -> SingletonOutcome` helper + `#[cfg(unix)]` pid-liveness/hostname helpers + the `#[cfg(test)] mod tests` with fixtures (stale / live / unparseable symlinks via `std::os::unix::fs::symlink` in a `tempfile::TempDir`) and tests SL-01..SL-07 — **Task 1 of Plan 26.3.2-01**
- [ ] `crates/ironhermes-core/src/lib.rs` — add `pub mod browser_profile;` (+ `pub use browser_profile::{SingletonOutcome, reconcile_singleton_lock};`) — **Task 1 of Plan 26.3.2-01**
- [ ] Both `crates/ironhermes-tools/src/browser_session.rs` and `crates/ironagent-tools-api/src/browser_session.rs` — call the helper before the `.user_data_dir(...)` decision; branch on `UseProfile` vs `UseEphemeral` (SL-08 = these compile) — **Task 2 of Plan 26.3.2-01**

*No existing test files are modified; no new test files are created (all tests live in `browser_profile.rs`).*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| End-to-end: with a real stale `SingletonLock` left in `$HERMES_HOME/browser-profile`, a `browser_navigate` tool call no longer aborts with `Failed to create a ProcessSingleton` | D-01/D-02/D-04 | Requires a real Chromium binary + a real `$HERMES_HOME`; not exercised in CI | 1. `kill -9` a running hermes browser session (or `ln -s "$(hostname)-99999999" ~/.ironhermes/browser-profile/SingletonLock`). 2. Run an agent turn that calls `browser_navigate`. 3. Confirm it succeeds and a `warn!` line reports the stale-lock cleanup. |
| Live-lock ephemeral fallback: with the operator's own Chrome running on the profile dir, `browser_navigate` still works (degraded, no persistence) and logs a `warn!` | D-05 | Requires two real Chromium processes contending for one profile dir | 1. Launch Chrome with `--user-data-dir=~/.ironhermes/browser-profile`. 2. Run an agent turn that calls `browser_navigate`. 3. Confirm it succeeds and a `warn!` line says the session is ephemeral / not persisted. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (`browser_profile.rs` + lib.rs registration + both call sites)
- [x] No watch-mode flags
- [x] Feedback latency < 10 s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planned (Plan 26.3.2-01 created 2026-05-12)
