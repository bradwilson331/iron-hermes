---
phase: 21.8.2-skills-hot-reload-command
verified: 2026-05-09T00:00:00Z
re_verified: 2026-05-09T22:30:00Z
status: passed
score: 16/16 must-haves verified (initial) + 9/9 G-01 gap-closure must-haves verified
overrides_applied: 0
re_verification:
  previous_status: passed
  previous_score: 16/16
  gaps_closed:
    - "G-01: System-role messages from /skills reload and /<skill-name> SKILL-13 fallback don't auto-scroll the rata-TUI viewport"
  gaps_remaining: []
  regressions: []
---

# Phase 21.8.2: Skills Hot-Reload Command — Verification Report

**Phase Goal:** Two parts: (1) Skills with Title Case names load via in-memory normalization (D-10/D-11/D-12). (2) Users can run `/skills reload` (CLI REPL, TUI, gateway/Telegram) or `hermes skills reload` (CLI subcommand) to pick up newly-installed skills without restart, plus a SKILL-13 fallback so typing `/<skill-name>` activates the SKILL.md body into the next agent turn.

**Verified:** 2026-05-09
**Status:** GOAL ACHIEVED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Title Case skill names (e.g. `Command Development`) load as normalized kebab-case (`command-development`) — not rejected by validate_skill_name | VERIFIED | `skills.rs:218` — normalization block `to_lowercase().replace(' ', "-").split('-').filter(!empty).join("-")` inserted before `validate_skill_name` call; 5 unit tests pass |
| 2 | Double-space names (e.g. `Code  Review`) normalize to `code-review` (no consecutive hyphens, Pitfall 4) | VERIFIED | `skills.rs:218` — split/filter/join pattern collapses consecutive hyphens; `normalize_no_double_hyphen` test passes |
| 3 | Mixed hyphen+case (e.g. `MCP-Integration`) normalizes to `mcp-integration` | VERIFIED | `normalize_preserves_pre_existing_hyphen_with_titlecase` test passes |
| 4 | Already-valid names (`ascii-art`) are unchanged with no normalization warning | VERIFIED | `normalize_no_op_for_valid_names` test passes; D-12 warn only fires when raw_name != frontmatter.name |
| 5 | Normalization emits `tracing::warn!` with original name, normalized name, and SKILL.md path (D-12) | VERIFIED | `skills.rs:560` — `warn!("SkillRegistry: normalized skill name {:?} -> {:?} at path {:?}", ...)` in `try_register_skill_from_dir`; `extract_raw_name_from_yaml` helper at line 504; `try_register_emits_normalization_warn_with_path` test passes |
| 6 | On-disk directory is never renamed (D-11 invariant) | VERIFIED | `grep -c 'fs::rename' skills.rs` = 0 |
| 7 | Names that fail validation AFTER normalization are still rejected | VERIFIED | Normalization precedes `validate_skill_name` call (not bypasses it); existing rejection path unchanged |
| 8 | `CommandResult::SkillsReload` and `CommandResult::SkillActivated { name, body }` are public variants | VERIFIED | `commands/mod.rs:163,170`; both derive PartialEq; 2 variant existence tests pass |
| 9 | `/skills reload` in CLI REPL loads new registry, prints D-04 diff, swaps Arc, reflects new contents | VERIFIED | `main.rs:1648` — full SkillsReload arm: `load_with_config`, `HashSet::difference`, diff text, D-05 invalid_skipped clause, `skill_registry = new_registry.clone()`, `prompt_builder.set_skill_registry`; `let mut skill_registry` at line 1342 |
| 10 | `hermes skills reload` CLI subcommand loads registry, prints skills, reports invalid_skipped, exits 0 | VERIFIED | `skills_cmd.rs:823` — `cmd_reload()` calls `load_with_config`, prints `Skills reloaded: N loaded, M invalid skipped.`, lists names; `SkillsAction::Reload` variant at line 77; dispatch at line 879; 5 static-grep tests pass |
| 11 | Typing `/<skill-name>` (no registered command) activates SKILL-13 fallback — returns `SkillActivated`, REPL injects body via `prompt_builder.activate_skill` | VERIFIED | `tui/commands.rs:94-107` — SKILL-13 at `ResolveResult::NotFound`; `main.rs:1724` — `SkillActivated` arm calls `prompt_builder.activate_skill(&name, &body)`; `activate_skill` at `prompt_builder.rs:199` pushes to `skill_overlays`; `build_split()` prepends overlays at line 598 |
| 12 | Typing `/help` (registered command) when skill `help` exists — registered command wins, SKILL-13 never fires (D-08) | VERIFIED | D-08 is structurally enforced: 3-stage resolution runs first; SKILL-13 only executes in `ResolveResult::NotFound` branch; registered commands cannot reach NotFound |
| 13 | Gateway handles `/skills reload` with atomic inner-Arc swap (`Arc<Mutex<Option<Arc<SkillRegistry>>>>`) and sends diff to chat | VERIFIED | `handler.rs:76` — `skill_registry: Arc<std::sync::Mutex<Option<Arc<SkillRegistry>>>>>`; `handler.rs:494-593` — full SkillsReload arm with lock/load/diff/swap/send_message |
| 14 | TUI (tui_rata) handles `/skills reload` via `SlashOutcome::SkillsReload`, appends diff to transcript via `history.push(Role::System)` | VERIFIED | `tui_rata/commands.rs:376` — `SlashOutcome::SkillActivated`; SKILL-13 at line 366-382; 14 static-grep invariants in `skill_registry_wiring.rs` all pass including `tui_rata_apply_slash_outcome_uses_history_push_for_skills` and `tui_rata_skillsreload_handled` |
| 15 | All 4 CommandContext construction sites wire `with_skill_registry` | VERIFIED | `main.rs:1010` — `build_cmd_ctx` helper covers run_chat + run_single; `tui_rata/commands.rs:617`; `handler.rs:377`; 4 static-grep invariant tests pass |
| 16 | `prompt_builder.activate_skill` stores body in `skill_overlays`, which `build_split()` prepends to system prompt for next turn | VERIFIED | `prompt_builder.rs:87,199,598`; 3 unit tests (`activate_skill_appends_to_overlays`, `activate_skill_multiple_concatenate`, `clear_skill_overlays_removes_bodies`) pass |

**Score:** 16/16 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/ironhermes-core/src/skills.rs` | Normalization block + extract_raw_name_from_yaml + D-12 warn + 9 unit tests | VERIFIED | Normalization at line 218; helper at 504; warn at 560; 9 tests all pass |
| `crates/ironhermes-core/src/commands/mod.rs` | `SkillsReload` + `SkillActivated { name, body }` variants | VERIFIED | Lines 163, 170 |
| `crates/ironhermes-core/src/commands/handlers.rs` | `cmd_skills(args, ctx)` with reload arm | VERIFIED | Line 732; dispatch at line 69 |
| `crates/ironhermes-cli/src/main.rs` | SkillsReload/SkillActivated arms, `let mut skill_registry`, SKILL-13 (via tui/commands.rs) | VERIFIED | Lines 1342, 1648, 1724 |
| `crates/ironhermes-cli/src/skills_cmd.rs` | `SkillsAction::Reload` + `cmd_reload` + dispatch | VERIFIED | Lines 77, 823, 879 |
| `crates/ironhermes-cli/src/tui_rata/app.rs` | `pub skill_registry: Option<Arc<...>>` on App + AppDeps | VERIFIED | Lines 147, 268 |
| `crates/ironhermes-cli/src/tui_rata/commands.rs` | SKILL-13 at NotFound + SlashOutcome arms + with_skill_registry wiring | VERIFIED | Lines 366-382, 617 |
| `crates/ironhermes-gateway/src/handler.rs` | `Arc<Mutex<Option<Arc<SkillRegistry>>>>` + skill_overlays + SkillsReload arm + SKILL-13 | VERIFIED | Lines 76, 85, 377, 494, 628 |
| `crates/ironhermes-agent/src/prompt_builder.rs` | `activate_skill` + `skill_overlays` + `build_split` integration | VERIFIED | Lines 87, 199, 598 |
| `crates/ironhermes-cli/tests/skill_registry_wiring.rs` | 14 static-grep invariant tests | VERIFIED | All 14 pass |
| `crates/ironhermes-cli/tests/skills_reload_cli.rs` | 5 static-grep smoke tests | VERIFIED | All 5 pass |
| `crates/ironhermes-gateway/tests/skill_registry_wiring.rs` | 1 static-grep invariant | VERIFIED | Passes |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `skills.rs` normalization block | `validate_skill_name` call | Executes before validation | WIRED | Line 218 block precedes line ~228 validation call |
| `try_register_skill_from_dir` | `tracing::warn!` with path | `extract_raw_name_from_yaml` comparison | WIRED | Line 560 after parse_skill_md call |
| `handlers.rs` dispatch table | `cmd_skills(args, ctx)` | `"skills" => cmd_skills(args, ctx)` | WIRED | Line 69 |
| `main.rs` SkillsReload arm | `SkillRegistry::load_with_config` | `CommandResult::SkillsReload =>` branch | WIRED | Line 1652-1656 |
| `main.rs` SKILL-13 fallback | `registry.find` + `registry.read_content` | `tui/commands.rs ResolveResult::NotFound` | WIRED | Lines 98-106 in tui/commands.rs; consumed at main.rs line 1724 |
| `skills_cmd.rs` SkillsAction::Reload | `load_with_config` + print | `cmd_reload` dispatch arm | WIRED | Lines 823, 879 |
| `handler.rs` SkillsReload arm | `skill_registry.lock()` + `load_with_config` + diff + swap | Mutex-guarded atomic swap | WIRED | Lines 494-593 |
| `handler.rs` SKILL-13 NotFound | `skill_registry.lock()` + `registry.find` + `skill_overlays.push` | Per-session overlay store | WIRED | Lines 628-666 |
| `prompt_builder.activate_skill` | `build_split()` prepending overlays | `skill_overlays` Vec field | WIRED | Lines 199, 598 |

---

## New Test Pass/Fail Summary

| Test Group | Package | Count | Result |
|------------|---------|-------|--------|
| `normalize_*` (Plan 01 normalization) | ironhermes-core lib | 5 | ALL PASS |
| `extract_raw_name_from_yaml_*` + `try_register_*` (Plan 01 D-12) | ironhermes-core lib | 4 | ALL PASS |
| `command_result_skill*` + `cmd_skills_*` (Plan 02 variants + cmd_skills) | ironhermes-core lib | 6 | ALL PASS |
| `skill_registry_wiring` static-grep invariants (Plan 02/03 CLI) | ironhermes-cli test | 14 | ALL PASS |
| `skill_registry_wiring` static-grep invariant (Plan 02 gateway) | ironhermes-gateway test | 1 | ALL PASS |
| `skills_reload_cli` static-grep smoke tests (Plan 03) | ironhermes-cli test | 5 | ALL PASS |
| `activate_skill_*` + `clear_skill_overlays_*` (Plan 03 prompt_builder) | ironhermes-agent lib | 3 | ALL PASS |
| **Total new tests** | | **38** | **ALL PASS** |

---

## Build Status

**Workspace build:** CLEAN — `cargo build --workspace` exits 0, warnings only, no errors.

**Pre-existing test failures (NOT introduced by Phase 21.8.2):**

- `commands::handlers::tests::dispatch_all_todo_stubs_return_not_yet_available` — FAILS. This test asserts that `cron` returns a "not yet available" stub, but `cron` now has a real handler (pre-dating Phase 21.8.2). The test was already failing before this phase (documented in Plan 01 SUMMARY, confirmed by the test existing in commit `0b76a02a` which is the last commit before Phase 21.8.2 started). This is a test-maintenance gap predating this phase.

- `ironhermes-tools-api` integration tests — 27+ E0433 compile errors (`use of unresolved module ironhermes_tools`). Pre-existing; entirely unrelated to skills or commands changes.

---

## Anti-Patterns Found

No blockers. The following observations are informational:

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `main.rs:2023-2031` (mid-turn dispatch) | `SkillsReload` arm prints a dimmed notice and returns; `SkillActivated` arm is similarly a no-op | INFO | Intentional design: mid-turn reload/activation is documented as deferred per Plan 02 SUMMARY. Not a stub — behavior is defined and user-facing. |
| `skills_cmd.rs` output format | Prints `Skills reloaded: N loaded, M invalid skipped.` + per-skill lines rather than the plan's originally stated `Skills reloaded. K skills: name1, name2, ...` | INFO | D-Plan03-04 explicitly states CLI subcommand does not compute a diff (no in-memory "old" registry). The actual output is functional and user-meaningful. Deviation documented in Plan 03 SUMMARY as auto-fixed. |

---

## Human Verification Required

None. All truths are verifiable from source code and passing tests. The phase is scoped to CLI + gateway + TUI (web platform explicitly deferred per Plan 03 `<scope>` section).

---

## Deferred Items

| Item | Addressed In | Evidence |
|------|-------------|----------|
| Web platform (`iron_hermes_ui`) `/skills reload` handler | Future phase (21.8.3 or later) | Plan 03 `<scope>`: "USER EXPLICITLY SCOPED OUT WEB. Narrow D-09 to: CLI + gateway + TUI only." The variants and core dispatch are already in place for incremental addition. |

---

## Gaps Summary

No gaps. All 16 observable truths are VERIFIED. The single failing test (`dispatch_all_todo_stubs_return_not_yet_available`) is a pre-existing maintenance issue predating this phase — it is not caused by any change in Phase 21.8.2 and its pre-existence was documented during execution.

---

## Final Verdict: GOAL ACHIEVED

Both phase goals are fully implemented and verified:

1. **Title Case normalization (Plan 01):** Skills with Title Case, multi-word, or mixed hyphen+case names load correctly. The `extract_raw_name_from_yaml` + D-12 warn infrastructure fires at the correct call site with path. 9 unit tests covering all normalization cases pass. No on-disk rename was introduced.

2. **Hot-reload command (Plans 02 + 03):** `/skills reload` works in CLI REPL (diff output, Arc swap, prompt_builder sync), TUI (SlashOutcome dispatch, history push), and gateway (Mutex-guarded atomic inner-Arc swap, per-session overlay store). `hermes skills reload` CLI subcommand works. SKILL-13 dynamic fallback is wired at all three `ResolveResult::NotFound` surfaces (tui/commands.rs, tui_rata/commands.rs, handler.rs). `prompt_builder.activate_skill` correctly injects skill bodies into the next turn's system prompt. 38 new tests all pass; workspace builds clean.

---

_Verified: 2026-05-09_
_Verifier: Claude (gsd-verifier)_

---

## G-01 Gap Closure Verification

**Re-verified:** 2026-05-09T22:30:00Z
**Trigger:** UAT G-01 (low-severity UX gap reported in test 4 of 21.8.2-UAT.md) prompted gap-closure plan `21.8.2-04-PLAN.md`. This section verifies plan 04 was actually executed and the gap is closed in the codebase.

### Gap recap

> Slash-outcome System messages (`/skills reload` diff line, SKILL-13 fallback `Skill '<name>' activated for this turn.`) appear in `App.history` but the rata-TUI viewport does not snap to the new line on the same render tick.

### Plan 04 must-have truths — verified against codebase

| # | Plan-04 must_have | Status | Evidence |
|---|-------------------|--------|----------|
| 1 | After `/skills reload` in the rata TUI, the new System-role line is visible on the SAME render tick (no follow-up agent turn required). | VERIFIED | `apply_slash_outcome` SkillsReload arm at app.rs:582-587 calls `self.scroll_to_bottom()` (line 586) which sets `auto_follow=true` so `reconcile_scroll` clamps `transcript_scroll` to `max` next render tick. Behaviour locked by `apply_slash_outcome_skills_reload_re_engages_auto_follow` test (passes). |
| 2 | After `/<skill-name>` SKILL-13 fallback, the activation System line is visible immediately. | VERIFIED | `apply_slash_outcome` SkillActivated arm at app.rs:588-595 calls `self.scroll_to_bottom()` (line 594). Behaviour locked by `apply_slash_outcome_skill_activated_re_engages_auto_follow` test (passes). |
| 3 | `apply_slash_outcome` SkillsReload arm calls `scroll_to_bottom()` before returning. | VERIFIED | app.rs:586 — `self.scroll_to_bottom();` is the last statement in the SkillsReload arm. |
| 4 | `apply_slash_outcome` SkillActivated arm calls `scroll_to_bottom()` before returning. | VERIFIED | app.rs:594 — `self.scroll_to_bottom();` is the last statement in the SkillActivated arm. |
| 5 | `apply_slash_outcome` Handled, ClearSession, Unknown, and Error arms also call `scroll_to_bottom()` (one-pass fix — they share the same bug). | VERIFIED | Handled arm at app.rs:574; ClearSession at app.rs:602; Unknown at app.rs:609; Error at app.rs:617. All four call `self.scroll_to_bottom()` as the last statement. Total 6 arms wired (Handled, SkillsReload, SkillActivated, ClearSession, Unknown, Error). Silent/Quit/ResetTerminal/McpReload arms intentionally untouched (no `history.push`). |
| 6 | `scroll_to_bottom()` helper exists on `App`, sets `self.auto_follow = true` and `self.transcript_scroll = 0` (mirroring `submit()` semantics around app.rs:718). | VERIFIED | app.rs:392-395 — `pub fn scroll_to_bottom(&mut self)` body sets `self.auto_follow = true;` (line 393) and `self.transcript_scroll = 0;` (line 394). Placed between `scroll_to_top` (line 377) and `scroll_indicator` (line 398) as symmetric counterpart. |
| 7 | `scroll_to_bottom()` name appears verbatim in every affected arm of `apply_slash_outcome` and in the helper definition. | VERIFIED | `grep -n "scroll_to_bottom" app.rs` returns the helper definition (line 392) plus 6 invocations inside `apply_slash_outcome` (lines 574, 586, 594, 602, 609, 617). |
| 8 | `apply_slash_outcome`'s visibility widened from private `fn` to `pub(super) fn` so unit tests in `mod scroll_tests` can drive each arm directly. | VERIFIED | app.rs:567 — `pub(super) fn apply_slash_outcome(&mut self, outcome: crate::tui_rata::commands::SlashOutcome)` with explanatory doc comment at lines 565-566 (`Visibility widened to pub(super) for unit-test access from mod scroll_tests — Phase 21.8.2 G-01 closure. Still crate-private.`). |
| 9 | Two new unit tests live in `mod scroll_tests` (gated on `#[cfg(all(test, feature = "test-support"))]`) and assert that `apply_slash_outcome` with `SkillsReload` and with `SkillActivated` re-engage `auto_follow=true` + `transcript_scroll=0` after a prior `scroll_up` disabled auto-follow. | VERIFIED | `mod scroll_tests` opens at app.rs:1026 (gating attribute at line 1025). Test 1: `fn apply_slash_outcome_skills_reload_re_engages_auto_follow()` at line 1280 — asserts `auto_follow`, `transcript_scroll == 0`, `history.len() == prev_len + 1`, last entry's `role == Role::System`. Test 2: `fn apply_slash_outcome_skill_activated_re_engages_auto_follow()` at line 1319 — same assertions plus `pending_skill_overlays.len() == 1`. |

**Score:** 9/9 plan-04 must_haves verified.

### Targeted test execution

```bash
cargo test -p ironhermes-cli --features test-support --lib tui_rata::app::scroll_tests
```

Result: **20 passed; 0 failed; 0 ignored; 0 measured; 206 filtered out; finished in 0.03s**

Both new tests appear in the pass list:
- `tui_rata::app::scroll_tests::apply_slash_outcome_skills_reload_re_engages_auto_follow ... ok`
- `tui_rata::app::scroll_tests::apply_slash_outcome_skill_activated_re_engages_auto_follow ... ok`

The mod was 18 tests pre-plan-04; it is 20 tests post-plan-04 (matches the 04-SUMMARY.md claim of +2).

### Build sanity check

```bash
cargo build -p ironhermes-cli
```

Result: `Finished dev profile [unoptimized + debuginfo] target(s) in 9.24s` — exit 0. Only pre-existing warnings (`NoOpExt` never constructed, `AgentLoopAdapter` field never read, `render_help_router` never used, `finish_prompt` never used) — none introduced by plan 04.

### Commits

| Commit | Type | Subject |
|--------|------|---------|
| `88ca68a5` | test | RED — apply_slash_outcome SkillsReload + SkillActivated must re-engage auto_follow |
| `a0702161` | fix  | GREEN — apply_slash_outcome arms re-engage auto_follow via scroll_to_bottom() |

Both commits modify only `crates/ironhermes-cli/src/tui_rata/app.rs`. No out-of-scope files (`main.rs`, `handler.rs`, `tui/commands.rs`, `tui/extension.rs`) were touched, matching plan 04's `<scope>` constraints.

### Cross-impact on other UAT items

| UAT test | Result before | Result after plan 04 | Notes |
|----------|---------------|----------------------|-------|
| 1. Cold start smoke | pass | pass (unaffected) | Plan 04 does not alter startup. |
| 2. `/skills` returns catalog | pass | pass (unaffected) | Plan 04 does not alter listing. |
| 3. Title Case skills load | pass | pass (unaffected) | Plan 04 does not alter normalization. |
| 4. `/skills reload` in CLI REPL picks up new skill | issue (G-01) | **closed** | Functional reload was already passing; the rata-TUI scroll-snap is now fixed. |
| 5. `hermes skills reload` shell subcommand | pass | pass (unaffected) | Plan 04 does not touch `skills_cmd.rs`. |
| 6. SKILL-13 dynamic fallback | pass | pass + improved UX | Activation confirmation now snaps into view immediately (was off-screen). |
| 7. Registered command beats same-name skill | pass | pass (unaffected) | Plan 04 does not touch `CommandRouter` resolution. |
| 8. Title Case normalization warn in logs | pass | pass (unaffected) | Plan 04 does not touch `tracing::warn!` site. |
| 9. (optional) TUI `/skills reload` updates transcript | pass with scroll bug | **closed** | Same fix as test 4. The tui_rata surface was the primary target. |
| 10. (optional) Gateway/Telegram `/skills reload` | pass | pass (unaffected) | Telegram was never affected by G-01 (no scroll concept). |

Side-benefit upgrades from the one-pass fix (NOT regression-tested but flagged for awareness): `Handled` (e.g. `/help` output), `ClearSession` (e.g. `/clear`), `Unknown { hint }` (typo-suggester), and `Error(err)` arms now also auto-snap the rata-TUI viewport. These were not part of UAT G-01 but share the same bug class — the fix scope was correctly widened in plan 04.

### Final verdict on G-01

**G-01: CLOSED.** All nine plan-04 must_haves are verified in the source at the claimed line numbers; `mod scroll_tests` runs 20 passing tests including the two new regression-locked tests for `SkillsReload` and `SkillActivated`; `cargo build -p ironhermes-cli` is clean; both required commits (`88ca68a5` RED + `a0702161` GREEN) are present on `develop`. No regressions detected in tests 1-10 of `21.8.2-UAT.md`. Phase 21.8.2 remains in `GOAL ACHIEVED` state, now with G-01 resolved.

### Files touched by this verification pass

- `.planning/phases/21.8.2-skills-hot-reload-command/21.8.2-VERIFICATION.md` — appended this `## G-01 Gap Closure Verification` section; preserved the original initial-verification report above.

No source files modified during verification.

---

_Re-verified: 2026-05-09T22:30:00Z_
_Re-verifier: Claude (gsd-verifier) — gap-closure pass for G-01 after `21.8.2-04` execution_
