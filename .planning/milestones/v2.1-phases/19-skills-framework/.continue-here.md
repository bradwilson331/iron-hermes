---
context: phase
phase: 19-skills-framework
stage: discuss-phase
task: 3
total_tasks: 8
status: in_progress
last_updated: 2026-04-14T22:49:37.487Z
---

<current_state>
Mid-`/gsd-discuss-phase 19` (Skills Framework). Initialization, prior-context load, codebase scout, and gray-area identification are complete. Gray areas were presented to the user in chat with code-context and prior-decision annotations. Paused before the user picked which areas to discuss — context window was ~81% on the paused session, so a fresh session is preferable for the multi-turn question/answer loop.

`19-CONTEXT.md` and `19-DISCUSSION-LOG.md` have NOT been written yet. The `.planning/phases/19-skills-framework/` directory exists but is empty.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Phase init via `gsd-tools init phase-op 19` — phase_found=true, phase_name="Skills Framework", padded_phase=19. Loaded PROJECT.md, REQUIREMENTS.md (SKILL-01..SKILL-11 are Phase 19 scope; SKILL-12/13/14 belong to Phase 20), STATE.md, and prior CONTEXT.md from Phases 14, 15, 17.
- Task 2: Codebase scout. Existing baseline confirmed: `crates/ironhermes-core/src/skills.rs` (SkillRegistry, parse_skill_md, platform filter, name validation, opaque `serde_yaml::Value` metadata), `crates/ironhermes-tools/src/skills_tool.rs` (list/view/activate/deactivate via Tool trait), `crates/ironhermes-core/src/config.rs` SkillsConfig (enabled flag + extra_paths). Phases 07, 07.1, 07.2 are the prior baseline; Phase 07.1 GAP-REPORT and 07.2 CONTEXT/DISCUSSION-LOG are required reading.
- Task 3: Identified 7 gray areas, presented to user with code-context + prior-decision annotations. (See section below.)
</completed_work>

<remaining_work>

- Task 4: User picks which gray areas to discuss (or replies "all" / "skip 5"). The Skills Hub gray area is the biggest scope risk — ~2,691 LOC in Python reference (`skills_hub.py`) and may warrant splitting into its own phase.
- Task 5: Discuss each selected area — default flow is 4 AskUserQuestion turns per area, with code-context annotations and prior-decision flags. Honor scope_guardrail (capture creep into Deferred Ideas, never silently expand).
- Task 6: Write `.planning/phases/19-skills-framework/19-CONTEXT.md` using the standard 6-section template (domain, decisions, canonical_refs MANDATORY, code_context, specifics, deferred).
- Task 7: Write `.planning/phases/19-skills-framework/19-DISCUSSION-LOG.md` audit trail.
- Task 8: `gsd-tools commit` for both files; `gsd-tools state record-session`; commit STATE.md; auto-advance/confirm per workflow.
</remaining_work>

<gray_areas_presented>

These 7 gray areas were presented in chat. The user must pick which to discuss next.

1. **Conditional activation mechanism (SKILL-03)** — Where the `requires_toolsets`/`requires_tools`/`fallback_for_*` filter runs (registry-load vs catalog-render), typed-vs-opaque metadata extraction, mid-session toolset toggling.
2. **Env var / credential setup UX (SKILL-04, SKILL-06, SKILL-11)** — How missing requirements surface (wizard, greyed-catalog, hidden, activate-error envelope), and how declared env vars flow into Phase 8's exec sandbox env-stripping.
3. **Skill settings namespace (SKILL-05)** — Typed schema vs free-form blob; how agent reads values (skill body injection / env vars / config tool); Phase 19 vs Phase 23 boundary on `hermes config migrate`.
4. **Security scanning (SKILL-07)** — Reuse `scan_context_content()` vs skill-specific patterns; body-only vs full file; hard-reject (MEM-05 style) vs WARN-BUT-LOAD (Phase 7.2 D-13 style); scan timing.
5. **Skills Hub architecture (SKILL-08, SKILL-09)** — Source adapters (GitHub / skills.sh / well-known HTTP), trust enforcement (signature vs origin label), install lifecycle (vendor vs cached/symlinked), update strategy, CLI vs tool surface.
6. **Credential mounting into sandboxes (SKILL-06, SKILL-11)** — Storage location, sandbox visibility mechanism (env path / bind mount / copy), interaction with Phase 8 env stripping.
7. **Hermes metadata extraction strategy** — Move from opaque `serde_yaml::Value` (Phase 7.2 D-09) to typed `HermesMetadata` struct, or keep opaque + add accessor helpers? Underpins gray areas 1–3.
</gray_areas_presented>

<decisions_made>

- No phase-19 implementation decisions captured yet. Discussion is at the gray-area-selection step.
- Carrying forward (locked from earlier phases):
  - Frozen-snapshot pattern (Phases 11/12/14/15) — skill catalog frozen into prompt slot 4 at session start.
  - WARN-BUT-LOAD divergence rule (Phase 07.1 D-09, 07.2 D-13) — parse leniently; hard-reject only on safety-critical violations like name validation.
  - Skills metadata stored as opaque `serde_yaml::Value` (Phase 07.2 D-09) — Phase 19 may revisit (gray area 7).
  - Hermes-agent reference is authoritative — port faithfully (tools/skills_tool.py, skills_hub.py, skills_guard.py, skills_sync.py, skill_manager_tool.py).
  - Single-binary, single-operator, Rust 2024 / tokio / SQLite / OpenAI-compatible (PROJECT.md constraints).
</decisions_made>

<blockers>
- Context window pressure (~81% on paused session). Workaround: resume in a fresh session.
- Open scope question: does Skills Hub (gray area 5) stay in Phase 19 or split into its own phase given ~2,691 LOC of Python reference? User input required.
</blockers>

## Required Reading (in order)

1. `.planning/ROADMAP.md` §"Phase 19: Skills Framework" — goal, success criteria, requirement mapping (SKILL-01..SKILL-11).
2. `.planning/REQUIREMENTS.md` §Skills (lines 70-85) — exact requirement text for SKILL-01..SKILL-11.
3. `.planning/PROJECT.md` — vendor constraints, "port hermes-agent faithfully" rule.
4. `.planning/STATE.md` — current milestone progress, prior decisions log.
5. `.planning/phases/15-10-layer-prompt-assembly/15-CONTEXT.md` — slot 4 (Skills) ordering, frozen-snapshot, durable/ephemeral split.
6. `.planning/phases/14-context-files-soul-md/14-CONTEXT.md` — `scan_context_content()` reuse pattern, security-scan policy.
7. `.planning/phases/17-memory-tools-external-providers/17-CONTEXT.md` — structured-error-envelope pattern (D-15) likely reusable for skill setup-error responses.
8. `.planning/milestones/v1.1-phases/07-skills-system/07-CONTEXT.md` — original skills baseline decisions.
9. `.planning/milestones/v1.1-phases/07.1-*/07.1-GAP-REPORT.md` — explicit gap list against hermes-agent reference; rows tagged "v2" map to Phase 19 work.
10. `.planning/milestones/v1.1-phases/07.2-*/07.2-CONTEXT.md` — opaque-metadata decision (D-09), WARN-BUT-LOAD rule (D-13), platform-filter implementation (already complete = SKILL-10).
11. `crates/ironhermes-core/src/skills.rs` — current SkillRegistry / SkillFrontmatter / SkillRecord / parse_skill_md.
12. `crates/ironhermes-tools/src/skills_tool.rs` — current SkillsTool actions and active_skills tracking.
13. `crates/ironhermes-core/src/config.rs` (lines 70-80, 347-370, 525-680 for tests) — SkillsConfig + round-trip pattern; new `skills.config` namespace will live here.
14. `crates/ironhermes-agent/src/prompt_builder.rs` — where `SkillRegistry::catalog_text()` is consumed (slot 4).
15. (If reachable) `~/code/hermes-agent/tools/skills_tool.py`, `skills_hub.py`, `skills_guard.py`, `skills_sync.py`, `skill_manager_tool.py` — canonical reference implementations.

## Critical Anti-Patterns (do NOT repeat these)

- **Re-asking decided questions**: Phase 7.2 already locked the opaque-metadata representation, platform-filter behavior, and WARN-BUT-LOAD rule. Don't re-litigate unless gray area 7 explicitly opens it.
- **Scope creep into Phase 20**: Slash commands (SKILL-12/13/14), tool registry (TOOL-01..05), and `is_available()` plumbing are Phase 20 — capture as Deferred Ideas if they come up.
- **Silent metadata schema drift**: If you decide to typed-extract `metadata.hermes.*`, document the migration explicitly so existing skills with extra fields don't break. WARN-BUT-LOAD remains the parsing rule.
- **Skipping canonical_refs section**: CONTEXT.md `<canonical_refs>` is MANDATORY per template. List every spec/ADR/feature doc with full relative paths grouped by topic.
- **Skipping ROADMAP-derived "Canonical refs"**: Phase 19 has no `Canonical refs:` line in ROADMAP.md (most v2.0 phases don't). Source canonical refs from REQUIREMENTS.md (SKILL-01..11), prior CONTEXT.md, the existing code paths, and any docs the user references during discussion.

## Infrastructure State

- No running services tied to this discussion phase.
- Working directory `/Users/twilson/code/ironhermes`, branch `develop`. The session-start git status had many unrelated `.omc/` and `.claude/` untracked files — none are relevant to Phase 19 and should not be staged here.
- `.planning/phases/19-skills-framework/` exists but contains no files yet.

## Pre-Execution Critique Required

Not applicable — paused before any plan exists. Standard `/gsd-plan-phase 19` will follow once CONTEXT.md is written.

<context>
Mental model: Phase 19 is largely additive on top of a working Phase 7 baseline. The Phase 7 series (7, 7.1, 7.2) already shipped SKILL.md parsing, registry scanning across 3 directories + extras, the `skills` tool, name validation, platform filter, and opaque metadata storage. Phase 19's job is to wire up the metadata fields that Phase 7.2 deliberately left opaque — `requires_toolsets`, `fallback_for_toolsets`, `requires_tools`, `fallback_for_tools`, `required_environment_variables`, `required_credential_files`, and `metadata.hermes.config` — plus add security scanning to skill content (consistent with how SOUL.md and MEMORY.md are scanned), env-var pass-through to the Phase 8 exec sandbox, and the Skills Hub for publish/install with trust levels.

The Skills Hub is the elephant. In hermes-agent it's `skills_hub.py` (2,691 LOC) plus `skills_sync.py` (295 LOC) plus `skill_manager_tool.py` (701 LOC). Even with Rust being more concise, that's likely 2-3 plans on its own. A reasonable reshape: Phase 19 covers SKILL-01..SKILL-07 + SKILL-10 + SKILL-11 (the local skills framework — frontmatter extensions, conditional activation, env/credential gating, security, sandbox pass-through), and a separate phase (or 19.1) covers SKILL-08 + SKILL-09 (the Hub). User must decide.

Decision-strategy preferences inferred from prior phases: user picks decisive, opinionated implementations and asks Claude to "port faithfully" from hermes-agent. Expect the same here. Frozen-snapshot pattern, structured-error envelopes (Phase 17 D-15 style), and Phase 7.2's WARN-BUT-LOAD parsing should all carry forward unless user signals otherwise.
</context>

<next_action>
1. Re-read this file and the Required Reading list in order (don't try to read all 15 in parallel — context budget matters).
2. Re-display the 7 gray areas verbatim from the `<gray_areas_presented>` section above.
3. Ask the user which areas to discuss (multiSelect via AskUserQuestion). Offer "all" and "skip Skills Hub (split to its own phase)" as explicit options.
4. Resume the discuss-phase workflow at the `discuss_areas` step (`@$HOME/.claude/get-shit-done/workflows/discuss-phase.md`, line ~590).
5. Honor `<scope_guardrail>` — anything beyond SKILL-01..SKILL-11 goes in Deferred Ideas.
6. After all areas are discussed, write `19-CONTEXT.md` (6-section template, canonical_refs MANDATORY) and `19-DISCUSSION-LOG.md`, then commit and update STATE.md.
</next_action>
