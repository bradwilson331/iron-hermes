# Phase 26.7: Wire Up Web to Real Services - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-17
**Phase:** 26.7-wire-up-web-to-real-services
**Areas discussed:** Scope, Data sources, Agent write operations, Memory write operations, Type shapes, Registry access

---

## Scope

| Option | Description | Selected |
|--------|-------------|----------|
| All 10 screens | Wire everything in one pass — clears the entire mock layer | |
| Cheap wins only (Tools + Agents) | Only wire the 2 screens where server fns already exist | |
| High-value subset | Tools, Agents, Memory, Skills, Models — the screens users interact with | ✓ |

**User's choice:** High-value subset (5 screens)
**Notes:** Office, Gateway, Soul, Schedules, Providers remain on stub_data. Gateway explicitly deferred to the Discord/Slack phase — user noted "Gateway will have Discord and Slack added, add new server functions in that phase."

---

## Data Sources

| Option | Description | Selected |
|--------|-------------|----------|
| All 8 screens (Models, Memory, Skills, Providers, Schedules, Soul, Gateway, Office) | Researcher verifies all data sources | |
| Skip Office and Gateway | Wire the 6 with obvious data sources | ✓ |

**User's choice:** Skip Office and Gateway
**Notes:** User confirmed Gateway deferred to future Discord/Slack phase.

---

## Agent Write Operations

| Option | Description | Selected |
|--------|-------------|----------|
| Wire all agent ops: status + kill + interrupt + prune | Server fns already exist — cheap to wire | ✓ |
| Status only (read-only) | Leave write ops for a follow-up phase | |

**User's choice:** Wire all agent ops
**Notes:** kill/interrupt/prune server fns already exist in api.rs — wiring them is essentially free.

---

## Memory Write Operations

| Option | Description | Selected |
|--------|-------------|----------|
| Read-only first | Just display MEMORY.md and USER.md contents | ✓ |
| Include add/delete | Wire the full memory tool CRUD | |

**User's choice:** Read-only first
**Notes:** Write ops (add/delete) are a separate concern with form UI implications — cleaner scope for this phase.

---

## Type Shapes

| Option | Description | Selected |
|--------|-------------|----------|
| New owned-String types per screen in api.rs | Same pattern as SessionInfo/ConfigSummary/ToolInfo | ✓ |
| Reuse stub types, change &'static str to String | Modifies existing shared types | |
| You decide | Leave to researcher/planner | |

**User's choice:** New owned-String types per screen in api.rs
**Notes:** Stub types in stub_data.rs stay for the deferred screens. New types: MemoryInfo, SkillInfo, ModelInfo.

---

## Registry Access

| Option | Description | Selected |
|--------|-------------|----------|
| Add to AppState if missing | Same pattern as runtime_bundle, memory_manager, subagent_registry | ✓ |
| Leave to researcher/planner | Researcher picks the access path | |

**User's choice:** Add to AppState if missing
**Notes:** If SkillRegistry or ModelRegistry aren't on AppState, this phase adds them — constructed in AppState::init(), stored as Arc<T>.

---

## Claude's Discretion

- Exact field set for `MemoryInfo` — what fields from MemoryManager to expose (key, value, store name, capacity)
- Exact field set for `SkillInfo` — name, description, category, enabled state
- Exact field set for `ModelInfo` — name, provider, context_length; Phase 21.3 is the reference
- Whether ModelRegistry and SkillRegistry are async or sync (affects server fn `.await` usage)
- Ordering of sub-items within each screen's RSX

## Deferred Ideas

- **Gateway screen** — deferred to Discord/Slack phase (Phase 34+)
- **Office, Soul, Schedules, Providers screens** — remain on stub_data; future phases
- **Memory write ops** — read-only in this phase; web-based memory editing is future scope
- **Stub/mock layer cleanup** — `mocks/agent_steps`, `mocks/run_shell`, `mocks/personalities` are dead code; remove in a dedicated cleanup phase
- **Delete session server fn** — noted as deferred in sessions.rs; out of scope for 26.7
