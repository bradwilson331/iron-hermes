---
phase: 21.4-persistent-memory-gap-analysis-verification
plan: "01"
subsystem: memory
tags: [gap-analysis, audit, memory, lifecycle-hooks, config]
dependency_graph:
  requires: []
  provides:
    - GAP-ANALYSIS.md (verification baseline for Plans 02 and 03)
    - 6 gaps catalogued with severity, evidence, and fix assignment
  affects:
    - Plan 02 (closes GAP-1, GAP-2, GAP-3, GAP-4)
    - Plan 03 (closes GAP-5, GAP-6)
tech_stack:
  added: []
  patterns:
    - Code audit pattern: grep + read to verify wiring at file:line precision
    - Hook wiring matrix: 11-column table covering all MemoryProvider trait hooks
key_files:
  created:
    - .planning/phases/21.4-persistent-memory-gap-analysis-verification/GAP-ANALYSIS.md
  modified: []
decisions:
  - "GAP-2 fix must apply with_memory_manager() inside build_context_engine before Arc::new() — method is on concrete types only, not ContextEngine trait"
  - "GAP-4 fix uses Option<Arc<...>> return from build_memory_manager (not no-op sentinel) — all consumers already guard on if let Some"
  - "GAP-6 on_session_end: natural-end path of run_single is tractable; ctrl-c path intentionally deferred (async context unsuitable)"
  - "MEM-06 is VERIFIED correct — frozen snapshot pattern implemented and tested in all 3 entry points"
metrics:
  duration: "3 min"
  completed: "2026-04-20"
  tasks_completed: 1
  files_changed: 1
---

# Phase 21.4 Plan 01: Gap Analysis Audit Report Summary

**One-liner:** Structured 6-gap audit of IronHermes memory provider lifecycle wiring against hermes-agent reference, with 11-hook wiring matrix and MEM-06 verification.

## What Was Built

A comprehensive `GAP-ANALYSIS.md` audit report at `.planning/phases/21.4-persistent-memory-gap-analysis-verification/GAP-ANALYSIS.md`. This is a **documentation-only plan** — no code was changed.

The report was produced by direct code inspection of all 14 source files listed in the plan's `<read_first>` block, cross-referenced against `REFERENCE-hermes-agent-memory.md` and the 6 hermes-agent automatic provider behaviors.

## Findings

### Already Correct (no action needed)

- **MEM-06 frozen snapshot** — VERIFIED. `MemoryStore.snapshot` captured once by `load_from_disk()`. `format_for_system_prompt` reads from `snapshot` (not `entries`). `load_memory` called exactly once in `run_single` (main.rs:416), `run_chat` (main.rs:692), and `run_gateway` (runner.rs:~737). `test_snapshot_frozen_after_load` test pins this contract.
- **system_prompt_block wiring** — WIRED. `prompt_builder.rs:load_memory()` calls `system_prompt_block()` and appends non-empty blocks.
- **on_memory_write mirror fanout** — WIRED. `MemoryManager.handle_tool_call` dispatches to mirror with 5s timeout (Phase 20).
- **sync_turn** — WIRED. `memory_flush_handler.rs` fires via ContextPreCompress hook (Phase 18).
- **get_tool_schemas** — WIRED. `MemoryManager.get_tool_schemas()` registered at all entry points.
- **hermes memory setup** — IMPLEMENTED. `MemorySubcommand::Setup` exists.
- All store operations (add/replace/remove, capacity, duplicates, security scanning) — IMPLEMENTED.

### Gaps Found (6 total)

| Gap ID | Severity | Description | Fix Plan |
|--------|----------|-------------|----------|
| GAP-1 | Critical | `queue_prefetch` never fires in CLI chat mode — `run_agent_turn` builds `AgentLoop` without `.with_memory_manager()` | Plan 02 |
| GAP-2 | Critical | `on_pre_compress` never fires anywhere — `build_context_engine` / `attach_context_engine` have no `memory_manager` param; `with_memory_manager()` is on concrete types only | Plan 02 |
| GAP-3 | Critical | `queue_prefetch` never fires in gateway mode — `handler.rs:469` builds `AgentLoop` without `.with_memory_manager()` despite `self.memory_manager` being in scope | Plan 02 |
| GAP-4 | Major | `memory_enabled` + `user_profile_enabled` config toggles absent from `MemoryConfig` (config.rs:136-153) | Plan 02 |
| GAP-5 | Major | `hermes memory status` + `hermes memory off` subcommands missing from `MemorySubcommand` (main.rs:113-116) | Plan 03 |
| GAP-6 | Minor | `on_session_end` not called in any clean exit path | Plan 03 |

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — this plan produces a documentation artifact only.

## Threat Flags

None — documentation artifact in `.planning/` directory, not shipped to production.

## Self-Check: PASSED

- [x] GAP-ANALYSIS.md exists at `.planning/phases/21.4-persistent-memory-gap-analysis-verification/GAP-ANALYSIS.md`
- [x] File contains GAP-1 through GAP-6 identifiers (37 occurrences of "GAP-")
- [x] Feature comparison table with correct column headers present
- [x] MEM-06 documented as VERIFIED
- [x] Critical appears 16 times (>= 3 required)
- [x] Major appears 11 times (>= 2 required)
- [x] queue_prefetch in GAP-1 and GAP-3 descriptions
- [x] on_pre_compress in GAP-2 description
- [x] memory_enabled and user_profile_enabled in GAP-4 description
- [x] hermes memory status and hermes memory off in GAP-5 description
- [x] on_session_end in GAP-6 description
- [x] Hook wiring matrix covers all 11 MemoryProvider trait hooks
- [x] Commit 598bda0 exists
