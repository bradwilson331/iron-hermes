---
phase: 22.4.2
plan: 01
type: execute
wave: 2
depends_on: ["22.4.2-00"]
files_modified:
  - crates/ironhermes-core/src/commands/handlers.rs
  - crates/ironhermes-cli/src/tui_rata/commands.rs
  - crates/ironhermes-cli/tests/invariants_22_4.rs
  - crates/ironhermes-cli/tests/handlers_state_store.rs
autonomous: true
requirements: [D-01, D-03, D-05, D-06, D-10]

must_haves:
  truths:
    - "tui_rata invoke_handler is collapsed from 30-arm match to one-line core::handlers::dispatch delegation"
    - "/sessions returns formatted list of recent sessions from StateStore (or `Session storage not configured.` when None)"
    - "/resume <id-or-title> looks up a session via StateStore and returns load-confirmation text"
    - "/save <name> exports the current session via StateStore.export_session to a sanitized path under hermes_home/exports"
    - "/history shows the current session message log read from ctx.history snapshot (truncated for V8.1 disclosure mitigation)"
    - "/title (no args) returns current title; /title <new-title> persists via StateStore.update_session_title"
    - "All 5 handlers honor the ctx.<field>.is_some() guard pattern (D-05)"
    - "INV-22.4-31 and INV-22.4-33 (inverted in Plan 00 with #[ignore]) become active and PASS — #[ignore] removed"
    - "5 new per-command INVs (37-41) added and pass: stub-marker absent + cmd_<name> present"
    - "1 new behavioral test file at crates/ironhermes-cli/tests/handlers_state_store.rs (per OQ-7 BLOCKER — NOT in core/tests due to circular dep)"
  artifacts:
    - path: "crates/ironhermes-core/src/commands/handlers.rs"
      provides: "5 cmd_* functions for sessions/resume/save/history/title"
      contains: "fn cmd_sessions"
    - path: "crates/ironhermes-cli/src/tui_rata/commands.rs"
      provides: "Collapsed invoke_handler"
      contains: "ironhermes_core::commands::handlers::dispatch"
    - path: "crates/ironhermes-cli/tests/handlers_state_store.rs"
      provides: "Behavioral tests for the 5 StateStore commands"
      contains: "make_test_ctx_with_state_store"
    - path: "crates/ironhermes-cli/tests/invariants_22_4.rs"
      provides: "INV-37..41 added; #[ignore] removed from INV-31/33"
      contains: "INV-22.4-37"
  key_links:
    - from: "crates/ironhermes-core/src/commands/handlers.rs"
      to: "ctx.state_store: Option<Arc<std::sync::Mutex<StateStore>>>"
      via: "is_some() guard + sync Mutex lock inside dispatch_slash_blocking's outer block_in_place"
      pattern: "ctx\\.state_store"
    - from: "crates/ironhermes-cli/src/tui_rata/commands.rs::invoke_handler"
      to: "ironhermes_core::commands::handlers::dispatch"
      via: "one-line delegation per D-01"
      pattern: "ironhermes_core::commands::handlers::dispatch\\("
---

<objective>
Wire up StateStore-backed slash commands. Write real cmd_* bodies in core::handlers for /sessions /resume /save /history /title. Collapse tui_rata::invoke_handler from a 30-arm match to a one-line delegation per D-01. Activate the inverted INV-22.4-31 and INV-22.4-33 (Plan 00 marked them #[ignore]). Add 5 per-command static-grep INVs (37-41). Add 1 behavioral test file at crates/ironhermes-cli/tests/handlers_state_store.rs (placed in -cli/tests/ per RESEARCH.md OQ-7 BLOCKER — ironhermes-state imports ironhermes-core, so adding it as dev-dep of ironhermes-core would be circular).

Purpose: First of three parallel-safe Wave 2 plans (01/02/03 all build on Plan 00). Owning StateStore in isolation gates 5 of the 28 wired commands and proves out the dispatch-collapse pattern Wave 2 plans rely on.

Output: 5 working slash commands on tui_rata + gateway (free win per RESEARCH.md P5), 5 new per-command INVs, 1 new behavioral test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22.4.2-wire-up-slash-commands/22.4.2-CONTEXT.md
@.planning/phases/22.4.2-wire-up-slash-commands/22.4.2-RESEARCH.md
@.planning/phases/22.4.2-wire-up-slash-commands/22.4.2-PATTERNS.md
@.planning/phases/22.4.2-wire-up-slash-commands/22.4.2-00-PLAN.md
@.continue-here.md

<interfaces>
StateStore public API (verified RESEARCH.md P0.1, source: crates/ironhermes-state/src/lib.rs):
- pub fn open_default() -> Result&lt;Self&gt;
- pub fn list_sessions(&amp;self, source: Option&lt;&amp;str&gt;, limit: usize, offset: usize) -&gt; Result&lt;Vec&lt;Session&gt;&gt;
- pub fn get_session(&amp;self, id: &amp;str) -&gt; Result&lt;Option&lt;Session&gt;&gt;
- pub fn get_session_by_title(&amp;self, title: &amp;str) -&gt; Result&lt;Option&lt;Session&gt;&gt;
- pub fn get_messages(&amp;self, session_id: &amp;str) -&gt; Result&lt;Vec&lt;StoredMessage&gt;&gt;
- pub fn update_session_title(&amp;mut self, id: &amp;str, title: &amp;str) -&gt; Result&lt;()&gt;
- pub fn export_session(&amp;self, session_id: &amp;str) -&gt; Result&lt;SessionExport&gt;

Wrapper at all existing call sites: Arc&lt;std::sync::Mutex&lt;StateStore&gt;&gt; (NOT RwLock). Verified: agent_loop.rs:286, gateway handler.rs:722.

Dispatch entry (handlers.rs:12) — sync fn, args is &amp;[&amp;str].

Existing reference handler shapes (verbatim copy targets):
- cmd_skills (handlers.rs:421-426) — None-guard returning informational text
- cmd_stop (handlers.rs:105-136) — Arc clone + None-guard + block_in_place async bridge
- cmd_models_refresh (handlers.rs:515-560) — multi-line text output
- cmd_title (handlers.rs:~368) — EXISTS already as basic informational stub; this plan REPLACES its body

Existing invoke_handler (commands.rs:203-391, 188 LOC, 30 arms) — DELETE entire body, replace with one-line dispatch delegation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write 5 cmd_* StateStore handler bodies in core::handlers</name>
  <files>crates/ironhermes-core/src/commands/handlers.rs</files>
  <read_first>
    - crates/ironhermes-core/src/commands/handlers.rs (FULL FILE — dispatch at line 12, cmd_stop 105-136, cmd_skills 421-426, cmd_reload_mcp 643-649, cmd_models_refresh 515-560, existing cmd_title at ~368, todo_stub at 655, make_ctx test fixture at 704)
    - crates/ironhermes-state/src/lib.rs (StateStore impl block — confirm Session/StoredMessage/SessionExport struct field names)
    - crates/ironhermes-core/src/types.rs (or wherever ChatMessage lives — confirm role/content field names for cmd_history)
    - .planning/phases/22.4.2-wire-up-slash-commands/22.4.2-PATTERNS.md (Category 1C: None-guard + sync Mutex lock pattern)
    - .planning/phases/22.4.2-wire-up-slash-commands/22.4.2-RESEARCH.md (P0.1 + P2 — async bridge analysis: StateStore methods are sync; outer block_in_place from dispatch_slash_blocking covers Mutex lock; no spawn_blocking needed)
    - .continue-here.md (Plan 00 BLOCKING async-bridge entry)
  </read_first>
  <behavior>
    - cmd_sessions(&amp;[], &amp;ctx) with ctx.state_store == None returns CommandResult::Output("Session storage not configured.")
    - cmd_sessions(&amp;[], &amp;ctx) with populated StateStore returns Output containing "session" and a row count line
    - cmd_resume(&amp;["abc123"], &amp;ctx) with no matching session returns Output containing "not found"
    - cmd_resume(&amp;["abc123"], &amp;ctx) with matching session returns Output containing the id and message count
    - cmd_save(&amp;["mysession"], &amp;ctx) with populated StateStore writes to hermes_home/exports/mysession.json and returns Output("Saved session to ...")
    - cmd_save(&amp;[], &amp;ctx) returns Output("Usage: /save &lt;name&gt;") (no panic)
    - cmd_save(&amp;["../etc/passwd"], &amp;ctx) returns Error (V12.4 path-traversal mitigation)
    - cmd_history(&amp;[], &amp;ctx) with ctx.history populated returns Output containing message count + truncated previews (max 80 chars per line, V8.1)
    - cmd_history(&amp;[], &amp;ctx) with ctx.history == None returns Output("History not available.")
    - cmd_title(&amp;[], &amp;ctx) returns CURRENT title via get_session(ctx.session_id)
    - cmd_title(&amp;["My", "new", "title"], &amp;ctx) calls update_session_title(ctx.session_id, "My new title") and returns Output("Title updated to: My new title")
  </behavior>
  <action>
Write 5 sync `fn cmd_*(args: &amp;[&amp;str], ctx: &amp;CommandContext) -&gt; CommandResult` functions following PATTERNS.md Category 1C verbatim shape. Add corresponding match arms to dispatch() at handlers.rs:12. Reuse the existing cmd_title slot (~handlers.rs:368) — replace its body but keep the function name; if signature differs, update both fn and dispatch arm.

Each function MUST follow this skeleton (None-guard from cmd_skills + sync Mutex lock from RESEARCH.md P2):

1. Match &amp;ctx.state_store: Some(s) =&gt; s.clone(), None =&gt; return Output("Session storage not configured.")
2. Validate args (Output("Usage: ...") for empty when required; sanitize for /save name per V12.4)
3. Lock store via store.lock() (graceful handling of poisoned mutex via .into_inner())
4. Call sync StateStore method
5. Format result as CommandResult::Output(multi-line text) or CommandResult::Error on failure

Specific bodies:

cmd_sessions: list_sessions(None, limit_arg_or_20, 0). Format header "Recent sessions (N):" + per-row "  &lt;id&gt; — &lt;title-or-(untitled)&gt; (N msgs)". Return Output("No sessions found.") for empty result.

cmd_resume: required &lt;target&gt; arg. Try get_session(target), then get_session_by_title(target). Return Output("Session `&lt;t&gt;` not found.") on miss; on hit return Output("Session &lt;id&gt; loaded (&lt;n&gt; messages). Use post-router hook to swap into App."). Note in SUMMARY: actual App.history swap belongs to Plan 04's handle_session_control branch — Plan 01 only returns load-confirmation text.

cmd_save: required &lt;name&gt; arg. SECURITY V12.4: reject if name contains '/', '..', or starts with '.' — return Error with message. Build path = crate::get_hermes_home().join("exports").join(format!("{name}.json")). create_dir_all(&amp;dir). serde_json::to_string_pretty(&amp;export). std::fs::write(&amp;path, json). Return Output(format!("Saved session to {}", path.display())). Verify get_hermes_home() exists in core (it does — RESEARCH.md OQ-7 confirms ironhermes-state imports it from core).

cmd_history: read from ctx.history (NOT app — App-side mutations stay tui_rata-side). Match Some(h) / None pattern. Lock h.read() (RwLock from Plan 00 ctx field type). Return "No messages in history yet." for empty. Otherwise build multi-line: header "Showing last K of N messages:" + per-row "  [i] &lt;Role&gt;: &lt;preview&gt;" where preview = msg.content.lines().next().unwrap_or("").chars().take(80).collect::&lt;String&gt;() (V8.1: bounded disclosure).

cmd_title: empty args =&gt; read mode (get_session(ctx.session_id), Output current title). Non-empty =&gt; write mode (args.join(" ") =&gt; new_title; update_session_title(ctx.session_id, &amp;new_title); Output("Title updated to: ...")). Lock as &amp;mut for the write path.

NO async fn (dispatch is sync per handlers.rs:12). NO spawn_blocking (StateStore methods are sync rusqlite — outer dispatch_slash_blocking block_in_place covers them per RESEARCH.md P2). NO block_on (none of these methods are async).

Verify each Session/StoredMessage/SessionExport field name against the actual struct (read crates/ironhermes-state/src/lib.rs first). If field is `display_title` instead of `title`, adjust accordingly.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-core 2&gt;&amp;1 | tail -10 &amp;&amp; grep -E "fn cmd_(sessions|resume|save|history|title)\(" crates/ironhermes-core/src/commands/handlers.rs | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p ironhermes-core` exits 0
    - `grep -E "fn cmd_(sessions|resume|save|history|title)\(" crates/ironhermes-core/src/commands/handlers.rs | wc -l` returns at least 5
    - `grep "ctx.state_store" crates/ironhermes-core/src/commands/handlers.rs | wc -l` returns at least 4 (sessions/resume/save/title; history reads ctx.history)
    - `grep "ctx.history" crates/ironhermes-core/src/commands/handlers.rs | wc -l` returns at least 1 (cmd_history)
    - `grep "Session storage not configured" crates/ironhermes-core/src/commands/handlers.rs | wc -l` returns at least 4 (None-guard text in 4 of 5 handlers)
    - `grep "async fn cmd_" crates/ironhermes-core/src/commands/handlers.rs` returns ZERO matches (sync invariant)
    - `grep "spawn_blocking" crates/ironhermes-core/src/commands/handlers.rs | grep -E "(sessions|resume|save|history|title)"` returns ZERO matches
    - All 5 dispatch arms route through cmd_&lt;name&gt;: `grep -E '"(sessions|resume|save|history|title)" =&gt;' crates/ironhermes-core/src/commands/handlers.rs | wc -l` returns at least 5
    - `cargo test -p ironhermes-core --lib 2&gt;&amp;1 | grep "FAILED" | wc -l` returns 0
  </acceptance_criteria>
  <done>5 sync cmd_* functions exist with state_store-backed bodies, None-guards, sync Mutex locks, V12.4 sanitization on /save, V8.1 truncation in /history; dispatch routes to them; core builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Collapse tui_rata invoke_handler to one-line dispatch delegation</name>
  <files>crates/ironhermes-cli/src/tui_rata/commands.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/tui_rata/commands.rs (FULL FILE — invoke_handler 203-391; dispatch_slash 60-95; build_command_context 170-181 [extended in Plan 00]; map_core_to_slash_outcome via grep)
    - crates/ironhermes-cli/src/tui_rata/event_loop.rs (lines 60-130 — locate dispatch_slash invocation site; signature change must not break caller)
    - crates/ironhermes-core/src/commands/handlers.rs (dispatch at line 12 — confirm whether quit/exit/help/reset are handled there or need passthrough)
    - .planning/phases/22.4.2-wire-up-slash-commands/22.4.2-PATTERNS.md (Category 3A: invoke_handler collapse — verbatim target)
    - .planning/phases/22.4.2-wire-up-slash-commands/22.4.2-RESEARCH.md (A3, OQ-8 — INV-31/33 collateral)
  </read_first>
  <behavior>
    - cargo build -p ironhermes-cli succeeds after the collapse
    - invoke_handler body contains exactly one statement that calls ironhermes_core::commands::handlers::dispatch(...)
    - The literal `Phase 22.4.1 stub:` appears AT MOST 0 times in commands.rs (since stubs lived in invoke_handler arms which are now deleted; /voice and /prompt's stubs lived in those same arms and disappear from tui_rata too — they remain referenced only via INV-22.4-33's allow-list which targets handlers.rs OR a different surface; if /voice and /prompt need to KEEP their stubs in commands.rs per D-07, this is impossible after the collapse — see action below)
    - cargo test --features test-support -p ironhermes-cli --lib regression-clean
    - All Tier A commands (/skills /agents /status /clear /quit) still work via core::handlers::dispatch
  </behavior>
  <action>
DELETE the entire 30-arm match body inside invoke_handler (commands.rs lines 203-391). REPLACE with the D-01 collapsed shape (PATTERNS.md Cat-3A). Add `args: &amp;[&amp;str]` parameter to the signature.

New body (verbatim — adjust signature only if existing differs):

    async fn invoke_handler(
        name: &amp;str,
        ctx: &amp;CommandContext,
        router: &amp;CommandRouter,
        args: &amp;[&amp;str],
    ) -&gt; Result&lt;CommandResult, anyhow::Error&gt; {
        let def = router
            .find(name)
            .ok_or_else(|| anyhow::anyhow!("unknown command: {name}"))?;
        Ok(ironhermes_core::commands::handlers::dispatch(def, args, ctx, router))
    }

Update the SOLE caller (dispatch_slash, ~line 74) to extract args and pass them:

    let args_str = input.strip_prefix(&amp;format!("/{}", def.name)).unwrap_or("").trim();
    let args: Vec&lt;&amp;str&gt; = args_str.split_whitespace().collect();
    let result = invoke_handler(def.name, &amp;ctx, router, &amp;args).await?;

(D-11 from 22.4.1 reaffirmed: def.name interpolation, NOT literal command names.)

Verify no other caller of invoke_handler exists: `grep -rn "invoke_handler" crates/ironhermes-cli`. If the only caller is dispatch_slash, the signature change is contained.

PRESERVE handle_mouse_slash (commands.rs:128-163) — Plan 03 generalizes it; Plan 01 must not delete.

CRITICAL — INV-22.4-33 inverted assertion expects /voice and /prompt stubs to STILL be present in commands.rs. After this collapse, those stubs disappear from commands.rs (they were in the deleted match arms). Resolution: INV-22.4-33's allow-list assertion (Plan 00 wrote it to check `Phase 22.4.1 stub: /voice` and `.../prompt` PRESENT in commands.rs) MUST be UPDATED to check that the markers are present in the registry definitions OR in handlers.rs::todo_stub fallback OR removed entirely. Cleaner: update INV-22.4-33's "still present" half to instead assert that the registry still has /voice and /prompt entries (`grep "name: \"voice\"" crates/ironhermes-core/src/commands/registry.rs`) — this is the structurally honest check post-collapse. UPDATE INV-22.4-33 accordingly in this task (Plan 01 owns the activation of the inverted assertion).

If quit/exit/help/reset are NOT handled by core::handlers::dispatch, keep a small special-case prefix in invoke_handler before the dispatch call:

    let result = match name {
        "exit" | "quit" | "q" =&gt; CommandResult::Quit,
        _ =&gt; {
            let def = router.find(name).ok_or_else(|| anyhow::anyhow!("unknown command: {name}"))?;
            ironhermes_core::commands::handlers::dispatch(def, args, ctx, router)
        }
    };
    Ok(result)

Verify by reading handlers.rs::dispatch arms first — if it has cmd_quit / cmd_exit / cmd_help, the prefix is REDUNDANT and the cleaner one-liner is preferred.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-cli 2&gt;&amp;1 | tail -10 &amp;&amp; grep -c "ironhermes_core::commands::handlers::dispatch(" crates/ironhermes-cli/src/tui_rata/commands.rs &amp;&amp; grep -c "Phase 22.4.1 stub:" crates/ironhermes-cli/src/tui_rata/commands.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p ironhermes-cli` exits 0
    - `grep -c "ironhermes_core::commands::handlers::dispatch(" crates/ironhermes-cli/src/tui_rata/commands.rs` returns at least 1
    - `grep -c "Phase 22.4.1 stub:" crates/ironhermes-cli/src/tui_rata/commands.rs` returns 0
    - invoke_handler is at most ~15 LOC (versus 188 pre-collapse): `awk '/async fn invoke_handler/,/^}$/' crates/ironhermes-cli/src/tui_rata/commands.rs | wc -l` returns less than 20
    - `grep "fn handle_mouse_slash" crates/ironhermes-cli/src/tui_rata/commands.rs` returns 1 (preservation)
    - `cargo test --features test-support -p ironhermes-cli --lib 2&gt;&amp;1 | grep "FAILED" | wc -l` returns 0
    - INV-22.4-33's "still present /voice /prompt" half updated to grep registry.rs (or removed); test passes
  </acceptance_criteria>
  <done>invoke_handler is a one-line dispatch delegation; stub markers gone from commands.rs; handle_mouse_slash preserved; INV-22.4-33 allow-list pivots to registry-resident /voice and /prompt entries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add 5 per-command INVs + remove #[ignore] from INV-31/33</name>
  <files>crates/ironhermes-cli/tests/invariants_22_4.rs</files>
  <read_first>
    - crates/ironhermes-cli/tests/invariants_22_4.rs (FULL FILE — INV-22.4-31 inverted+ignored at ~624 by Plan 00; INV-22.4-33 inverted+ignored by Plan 00; INV-22.4-34 negation pattern at 744-760; INV-22.4-01 multi-item loop at 40-61)
    - .planning/phases/22.4.2-wire-up-slash-commands/22.4.2-PATTERNS.md (Category 6A &amp; 6B — stub-marker absence + per-command handler-existence patterns)
    - crates/ironhermes-core/src/commands/handlers.rs (post-Task-1 — confirm fn cmd_sessions/resume/save/history/title presence)
    - crates/ironhermes-cli/src/tui_rata/commands.rs (post-Task-2 — confirm stub markers gone)
  </read_first>
  <behavior>
    - INV-22.4-31 (inverted) #[ignore] removed; test active and passes
    - INV-22.4-33 (inverted, pivoted /voice /prompt assertion to registry) #[ignore] removed; test active and passes
    - 5 new per-command INVs (37-41) added — one per StateStore command — and pass
    - All `cargo test -p ironhermes-cli --test invariants_22_4` tests pass
  </behavior>
  <action>
(A) REMOVE the `#[ignore]` attribute from INV-22.4-31 and INV-22.4-33 (added by Plan 00). Verify both tests pass post-Task-1+Task-2.

(B) ADD INV-22.4-37 through INV-22.4-41, ONE PER COMMAND in {sessions, resume, save, history, title}. Each INV has TWO assertions per PATTERNS.md Cat-6B:

    const TUI_RATA_COMMANDS: &amp;str = include_str!("../src/tui_rata/commands.rs");          // already exists
    const CORE_HANDLERS: &amp;str = include_str!("../../ironhermes-core/src/commands/handlers.rs");  // add if missing

    #[test]
    fn invariant_22_4_37_sessions_wired() {
        // (a) stub marker absent for /sessions
        assert!(
            !TUI_RATA_COMMANDS.contains("Phase 22.4.1 stub: /sessions"),
            "INV-22.4-37 (a): stub marker for /sessions must be absent in commands.rs"
        );
        // (b) real handler present in core
        assert!(
            CORE_HANDLERS.contains("fn cmd_sessions("),
            "INV-22.4-37 (b): fn cmd_sessions must exist in core::commands::handlers"
        );
    }

Repeat for resume (INV-38), save (INV-39), history (INV-40), title (INV-41). Each INV is its own #[test] fn with the two-assert structure. INV numbers MUST be sequential 37-41 (Plans 02/03/04 continue from 42 onward).

(C) Verify all tests pass:
    cargo test -p ironhermes-cli --test invariants_22_4 -- --test-threads=1
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cli --test invariants_22_4 2&gt;&amp;1 | tail -10 &amp;&amp; grep -E "INV-22.4-(37|38|39|40|41)" crates/ironhermes-cli/tests/invariants_22_4.rs | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ironhermes-cli --test invariants_22_4` all tests pass; no FAILED, no ignored (or only ignored tests are pre-existing ones not added by this phase)
    - `grep -c "fn invariant_22_4_(37|38|39|40|41)" crates/ironhermes-cli/tests/invariants_22_4.rs` returns 5
    - `grep -B1 "fn invariant_22_4_31\|fn invariant_22_4_33" crates/ironhermes-cli/tests/invariants_22_4.rs | grep -c "#\[ignore\]"` returns 0 (ignore removed)
    - `grep "fn cmd_sessions(\|fn cmd_resume(\|fn cmd_save(\|fn cmd_history(\|fn cmd_title(" crates/ironhermes-core/src/commands/handlers.rs | wc -l` returns at least 5 (proves Task 1 landed)
  </acceptance_criteria>
  <done>5 new per-command INVs added and pass; INV-31/33 #[ignore] removed and tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Behavioral test file at crates/ironhermes-cli/tests/handlers_state_store.rs</name>
  <files>crates/ironhermes-cli/tests/handlers_state_store.rs</files>
  <read_first>
    - crates/ironhermes-state/tests/state_store.rs (lines 5-10 — temp_store fixture template)
    - crates/ironhermes-core/src/commands/handlers.rs (lines 704-720 — make_ctx test fixture template)
    - crates/ironhermes-core/src/commands/context.rs (post-Plan-00 — confirm with_state_store builder + with_history builder signatures)
    - crates/ironhermes-cli/Cargo.toml (confirm ironhermes-state and tempfile are already dev-deps; add if missing — both are workspace crates and tempfile is already used elsewhere)
    - .planning/phases/22.4.2-wire-up-slash-commands/22.4.2-PATTERNS.md (Category 5A: make_test_ctx_with_&lt;subsystem&gt; helper)
    - .planning/phases/22.4.2-wire-up-slash-commands/22.4.2-RESEARCH.md (P3 + OQ-7 — fixture inventory + circular-dep BLOCKER)
  </read_first>
  <behavior>
    - 1 fixture helper: make_test_ctx_with_state_store() returns (CommandContext, NamedTempFile) where NamedTempFile keeps the temp DB alive for the test duration
    - At least 5 tests, one per command: sessions_lists_seeded_rows, resume_finds_seeded_session, save_writes_export_to_disk, history_returns_truncated_preview, title_round_trip
    - Tests use the public dispatch(def, args, ctx, router) entry rather than calling cmd_* fns directly (mirrors the actual production call path)
  </behavior>
  <action>
Create new file `crates/ironhermes-cli/tests/handlers_state_store.rs`. File MUST live in -cli/tests/ NOT -core/tests/ per RESEARCH.md OQ-7 (ironhermes-state imports ironhermes_core; placing this test in -core/tests/ would require ironhermes-state as dev-dep of ironhermes-core which creates circular dep).

Structure:

    use std::sync::{Arc, Mutex};
    use std::sync::atomic::AtomicBool;
    use tempfile::NamedTempFile;
    use ironhermes_state::StateStore;
    use ironhermes_core::commands::{CommandContext, CommandRouter};
    use ironhermes_core::commands::handlers::dispatch;
    use ironhermes_core::commands::registry::build_registry;
    use ironhermes_core::types::Platform;

    fn make_test_ctx_with_state_store() -&gt; (CommandContext, NamedTempFile) {
        let f = NamedTempFile::new().expect("tempfile");
        let store = StateStore::new(f.path()).expect("StateStore::new");
        let arc_store = Arc::new(Mutex::new(store));
        let ctx = CommandContext::new(
            Platform::Local,
            "test-session-id".to_string(),
            Arc::new(AtomicBool::new(false)),
        )
        .with_state_store(arc_store);
        (ctx, f)
    }

    fn make_router() -&gt; CommandRouter {
        CommandRouter::new(build_registry())
    }

    #[test]
    fn sessions_with_no_storage_returns_not_configured() {
        let ctx = CommandContext::new(Platform::Local, "s".into(), Arc::new(AtomicBool::new(false)));
        let router = make_router();
        let def = router.find("sessions").expect("registry has /sessions");
        let result = dispatch(def, &amp;[], &amp;ctx, &amp;router);
        match result {
            ironhermes_core::commands::CommandResult::Output(text) =&gt; {
                assert!(text.contains("not configured"), "got: {text}");
            }
            other =&gt; panic!("expected Output, got {other:?}"),
        }
    }

    #[test]
    fn sessions_lists_seeded_rows() {
        let (ctx, _f) = make_test_ctx_with_state_store();
        // Seed: create session via store directly, then dispatch.
        if let Some(arc) = &amp;ctx.state_store {
            let mut g = arc.lock().unwrap();
            g.create_session(/* args from actual API */).ok();
        }
        let router = make_router();
        let def = router.find("sessions").unwrap();
        let result = dispatch(def, &amp;[], &amp;ctx, &amp;router);
        // Assert Output text contains "session"
    }

Add 4 more tests for resume, save, history, title — each follows the pattern: (1) build ctx with StateStore (and ctx.history Arc&lt;RwLock&lt;Vec&gt;&gt; for history test), (2) seed if needed, (3) call dispatch with constructed args, (4) match CommandResult::Output and assert on text contents.

For the save test: assert the exported JSON file exists at hermes_home/exports/&lt;name&gt;.json — set HERMES_HOME to a tempdir for the test (use std::env::set_var inside an unsafe block per Rust 2024 edition; document with SAFETY comment per Phase 21.6 precedent; gate with --test-threads=1 in the verify command).

For the history test: build ctx with both with_state_store AND with_history(Arc::new(RwLock::new(vec![ChatMessage::user("hello"), ChatMessage::assistant("world")]))). Assert Output contains "Showing last" and the message previews.

Verify Cargo.toml has ironhermes-state and tempfile as dev-deps; add if missing.

If the actual StateStore API requires session creation via a different method than `create_session`, adjust to whatever the real signature is — read crates/ironhermes-state/src/lib.rs first.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cli --test handlers_state_store -- --test-threads=1 2&gt;&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/ironhermes-cli/tests/handlers_state_store.rs` exists
    - `cargo test -p ironhermes-cli --test handlers_state_store -- --test-threads=1` reports at least 5 tests passed, 0 failed
    - `grep "fn make_test_ctx_with_state_store" crates/ironhermes-cli/tests/handlers_state_store.rs` returns 1 match
    - `grep "ironhermes_core::commands::handlers::dispatch\|use ironhermes_core::commands::handlers::dispatch" crates/ironhermes-cli/tests/handlers_state_store.rs | wc -l` returns at least 1 (tests use the production dispatch entry, not direct cmd_* calls)
    - `grep "with_state_store" crates/ironhermes-cli/tests/handlers_state_store.rs | wc -l` returns at least 1
  </acceptance_criteria>
  <done>handlers_state_store.rs landed in -cli/tests/ with at least 5 passing behavioral tests covering /sessions /resume /save /history /title via the production dispatch entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → /save &lt;name&gt; → filesystem path | Untrusted name string crosses into std::fs::write target path |
| User input → /title &lt;text&gt; → SQLite | Untrusted title string crosses into rusqlite parameterized statement (SQL injection mitigated by prepared statement; content disclosure later via /sessions output) |
| ctx.history snapshot → /history Output | Snapshot of conversation messages disclosed via Output (V8.1: bounded by 80-char preview + line limit) |
| ctx.state_store → /resume Output | Session id/title disclosed in Output for confirmed loads (V8.1: minimal — no message bodies in /resume output) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22.4.2-01-01 | Tampering | /save name → filesystem path | mitigate | V12.4: cmd_save rejects names containing '/' or '..' or starting with '.'; export path resolved under hermes_home/exports/&lt;name&gt;.json (no traversal escape); returns CommandResult::Error on invalid name |
| T-22.4.2-01-02 | Information Disclosure | /history Output revealing message bodies | mitigate | V8.1: cmd_history truncates each row to first line + 80 chars; default limit of 10 messages; user can opt-in to higher limit via /history N arg (audit-surface preserved) |
| T-22.4.2-01-03 | Information Disclosure | /sessions Output revealing titles | accept | V8.1: titles are user-set metadata, not message content; 20-row default limit; same disclosure as the legacy classic-tui /sessions handler — no new exposure |
| T-22.4.2-01-04 | Denial of Service | sync std::sync::Mutex&lt;StateStore&gt; held during list_sessions | accept | Single-process REPL; one slash command at a time per dispatch_slash_blocking gate; rusqlite list_sessions completes in &lt;100ms for typical session counts (existing benchmark) |
| T-22.4.2-01-05 | Repudiation | /title overwrites without audit log | accept | Title changes are user-initiated UX edits; no compliance/audit obligation; rusqlite WAL retains prior value momentarily but no explicit audit log required (matches existing classic-tui behavior) |
| T-22.4.2-01-06 | Spoofing | /resume by title finds session not owned by current user | accept | Single-user CLI, no auth boundary within the CLI process; if multi-user shared host emerges later, StateStore-level access control is a separate phase |
| T-22.4.2-01-07 | Elevation of Privilege | gateway path gains unintended StateStore access | mitigate | Gateway's CommandContext::new(...) does NOT call .with_state_store(...); ctx.state_store is None on gateway path; cmd_sessions/etc. None-guard returns informational text per D-05 — gateway behavior unchanged unless gateway bootstrap is later extended to thread state_store |
| T-22.4.2-01-08 | Tampering | poisoned std::sync::Mutex on panic recovery | mitigate | Each lock acquire uses match/.into_inner() pattern — graceful degradation, no propagated panic; subsequent commands continue to function |
</threat_model>

<verification>
- `cargo build --workspace` exits 0
- `cargo test --workspace --features test-support 2&gt;&amp;1 | grep "FAILED" | wc -l` returns 0
- `cargo test -p ironhermes-cli --test invariants_22_4` all tests pass (INV-31/33 inverted+active; INV-37..41 added+pass)
- `cargo test -p ironhermes-cli --test handlers_state_store -- --test-threads=1` 5+ tests pass
- INV-22.4-33's allow-list assertion (/voice /prompt) — verify whichever surface (registry.rs OR todo_stub fallback) the assertion was pivoted to is correctly grepped
- `cargo build -p ironhermes-cli 2&gt;&amp;1 | grep "warning\|error" | head -5` shows no NEW warnings/errors introduced (pre-existing OK)
</verification>

<success_criteria>
- 4 tasks complete with green acceptance criteria
- 4 files modified: handlers.rs (5 new bodies + 5 dispatch arm rewrites), commands.rs (invoke_handler collapsed), invariants_22_4.rs (5 new INVs + #[ignore] removed from 2)
- 1 file created: handlers_state_store.rs (5+ behavioral tests)
- 5 of 28 commands wired (sessions, resume, save, history, title)
- INV ledger 36 → 41 (5 added)
- Zero stub markers remain in tui_rata/commands.rs
- Gateway gains 5 commands as a free win (None-guard returns informational text where state_store not threaded)
- Wave 2 unblocked for Plans 02 + 03 (parallel-safe — different regions of handlers.rs)
</success_criteria>

<output>
After completion, create `.planning/phases/22.4.2-wire-up-slash-commands/22.4.2-01-SUMMARY.md`
</output>
