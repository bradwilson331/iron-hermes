---
phase: 22.4.2
plan: 02
type: execute
wave: 2
depends_on: ["22.4.2-00"]
files_modified:
  - crates/ironhermes-core/src/commands/handlers.rs
  - crates/ironhermes-core/tests/handlers_provider.rs
  - crates/ironhermes-cli/tests/invariants_22_4.rs
autonomous: true
requirements: [D-02, D-03, D-05, D-06, D-10]

must_haves:
  truths:
    - "/model (no args) lists available models from ProviderResolver.model_registry()"
    - "/model <name> validates the new model exists; core handler returns Output text confirming model selection AND records the selection in ctx.provider_resolver lookup; tui_rata post-router AnyClient rebuild is owned by Plan 03 (handle_subsystem_mutator) — Plan 02 does NOT touch commands.rs"
    - "/provider returns current provider/model/endpoint status from ProviderResolver.resolve_for_main() — V8.1 mitigation: api_key field excluded from Output"
    - "/fast (no args) returns Output describing role-resolution result from ProviderResolver.resolve_role(\"fast\") (informational text only — App-side fast_enabled atomic toggle + AnyClient rebuild are owned by Plan 03's handle_subsystem_mutator)"
    - "All 3 handlers honor the ctx.provider_resolver.is_some() guard pattern (D-05)"
    - "3 new per-command INVs (42-44) added and pass: stub-marker absent + cmd_<name> present"
    - "1 new behavioral test file at crates/ironhermes-core/tests/handlers_provider.rs (no circular dep — placement OK in core/tests)"
    - "Plan 02 is parallel-safe with Plans 01 and 03 — files touched are disjoint regions of handlers.rs (cmd_model/provider/fast block) + new test file + INV append-only"
  artifacts:
    - path: "crates/ironhermes-core/src/commands/handlers.rs"
      provides: "3 new cmd_* functions for model/provider/fast"
      contains: "fn cmd_model"
    - path: "crates/ironhermes-core/tests/handlers_provider.rs"
      provides: "Behavioral tests for /model /provider /fast via dispatch entry"
      contains: "make_test_ctx_with_provider_resolver"
    - path: "crates/ironhermes-cli/tests/invariants_22_4.rs"
      provides: "INV-42 through INV-44 added"
      contains: "INV-22.4-42"
  key_links:
    - from: "crates/ironhermes-core/src/commands/handlers.rs"
      to: "ctx.provider_resolver: Option<Arc<ProviderResolver>>"
      via: "is_some() guard + sync method calls (resolve_for_main, model_registry, resolve_role)"
      pattern: "ctx\\.provider_resolver"
    - from: "crates/ironhermes-core/src/commands/handlers.rs cmd_model/cmd_fast"
      to: "Plan 03's handle_subsystem_mutator (post-router hook)"
      via: "Output text returned by core; Plan 03 reads core_result and rebuilds AnyClient on App"
      pattern: "Selected model:|Fast role"
---

<objective>
Wire up ProviderResolver-backed slash commands AT THE CORE LAYER ONLY. Write 3 new cmd_* bodies in core::handlers for /model /provider /fast. Add 3 per-command INVs (42-44). Add 1 behavioral test file at crates/ironhermes-core/tests/handlers_provider.rs.

CRITICAL — REVISION ITERATION 1 (Option B): Plan 02 does NOT touch crates/ironhermes-cli/src/tui_rata/commands.rs. The post-router hook expansion (including /model and /fast App-side AnyClient rebuild) is owned ENTIRELY by Plan 03. This makes Plans 01, 02, 03 truly parallel-safe — different files / different regions / no write conflict on commands.rs.

Purpose: Second of three parallel-safe Wave 2 plans. Owns ProviderResolver in isolation: 3 commands. The /model write path requires App-side AnyClient rebuild — that work is owned by Plan 03's handle_subsystem_mutator helper, which expands the post-router hook to cover {model, fast, personality, compress}. Plan 02 ships only the core handlers; their Output text becomes the input to Plan 03's hook.

Output: 3 working slash commands at the core dispatch layer, 3 new per-command INVs, 1 new behavioral test file. (Full App-side rebuild reachability lands when Plan 03 ships its handle_subsystem_mutator extension.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22.4.2-wire-up-slash-commands/22.4.2-CONTEXT.md
@.planning/phases/22.4.2-wire-up-slash-commands/22.4.2-RESEARCH.md
@.planning/phases/22.4.2-wire-up-slash-commands/22.4.2-PATTERNS.md
@.planning/phases/22.4.2-wire-up-slash-commands/22.4.2-00-PLAN.md
@.continue-here.md

<interfaces>
ProviderResolver public API (verified RESEARCH.md P0.2, source: crates/ironhermes-core/src/provider.rs):
- pub fn build(config: &Config) -> Result<Self>        // construction
- pub fn resolve_for_main(&self) -> &ResolvedEndpoint   // current main endpoint
- pub fn main_provider(&self) -> &str                  // current provider name
- pub fn resolve(&self, provider: &str) -> Option<&ResolvedEndpoint>
- pub fn resolve_role(&self, role: &str) -> Option<ResolvedEndpoint>     // role-based, e.g. "fast"
- pub fn model_registry(&self) -> &ModelRegistry

ProviderResolver derives Clone — no Arc wrapper needed for thread-safety. Stored on App (post-Plan-00) as plain `pub resolver: ProviderResolver`. ctx field is `Option<Arc<ProviderResolver>>` populated via `with_provider_resolver(Arc::new(app.resolver.clone()))` in build_command_context.

ResolvedEndpoint shape (read provider.rs to confirm exact field names): typically has model: String, provider: String, base_url: String, api_key: String — verify before use.

App-side AnyClient: held on App as built by build_app_deps. Mid-session model switch requires rebuilding AnyClient from the new ResolvedEndpoint. This is App-side mutation and is OWNED BY PLAN 03 — Plan 02 does NOT touch the post-router hook or commands.rs.

Existing analog handler shapes:
- cmd_skills (handlers.rs:421-426) — None-guard returning informational text
- cmd_models_refresh (handlers.rs:515-560) — multi-line text output (model listing reference)
- cmd_provider basic stub (handlers.rs ~line 408 — currently returns hardcoded "Provider: ..." text per D-discretion in CONTEXT — REPLACE)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write cmd_model + cmd_provider + cmd_fast bodies in core::handlers</name>
  <files>crates/ironhermes-core/src/commands/handlers.rs</files>
  <read_first>
    - crates/ironhermes-core/src/commands/handlers.rs (FULL FILE — dispatch arm at line 12; existing cmd_provider stub if present; cmd_models_refresh 515-560 as multi-line analog; cmd_skills 421-426 as None-guard analog)
    - crates/ironhermes-core/src/provider.rs (ProviderResolver impl block — confirm method signatures + ResolvedEndpoint field names)
    - .planning/phases/22.4.2-wire-up-slash-commands/22.4.2-PATTERNS.md (Category 1A: None-guard + sync read pattern, verbatim)
    - .planning/phases/22.4.2-wire-up-slash-commands/22.4.2-RESEARCH.md (P0.2 — public API; P2 — async bridge: NOT needed since all ProviderResolver methods are sync)
    - .continue-here.md (Plan 00 BLOCKING async-bridge entry)
  </read_first>
  <behavior>
    - cmd_model(&[], &ctx) with provider_resolver populated returns Output containing "Available models:" + per-model entries from model_registry()
    - cmd_model(&["gpt-4o"], &ctx) returns Output("Selected model: gpt-4o (post-router hook will rebuild client)") if model exists in registry
    - cmd_model(&["nonexistent"], &ctx) returns Error("Unknown model: nonexistent")
    - cmd_model(&[], &ctx) with provider_resolver == None returns Output("Provider resolver not configured.")
    - cmd_provider(&[], &ctx) returns Output containing current provider name + main model + endpoint URL (truncated for V8.1 — no api_key in output)
    - cmd_fast(&[], &ctx) returns Output("Fast mode: model swap to <fast-model-name>") if resolve_role("fast") returns Some
    - cmd_fast(&[], &ctx) returns Output("Fast role not configured (no fast preset in config).") if resolve_role returns None
  </behavior>
  <action>
Write 3 sync `fn cmd_*(args: &[&str], ctx: &CommandContext) -> CommandResult` functions following PATTERNS.md Category 1A verbatim shape. Add corresponding match arms to dispatch().

cmd_model:
- Match &ctx.provider_resolver: Some(r) => r.clone() (Arc::clone), None => return Output("Provider resolver not configured.")
- If args.is_empty(): list mode. Iterate r.model_registry() (read provider.rs to confirm iter API — likely .iter() or .all() returning &ModelInfo). Build multi-line Output: header "Available models:" + per-row "  - <name> (<provider>, ctx=<context_length>)". Limit to first 20 rows or all rows; if >20, append "  ... (use /models for full list)".
- If args has >= 1: validate mode. let target = args[0]; check if r.model_registry().contains(target) or equivalent membership method. If exists: Output(format!("Selected model: {target} (post-router hook will rebuild client)")). If not: Error(format!("Unknown model: {target}")).

cmd_provider:
- Match &ctx.provider_resolver pattern.
- Read r.resolve_for_main() and r.main_provider().
- Format Output: "Current provider: <name>\nModel: <model>\nEndpoint: <base_url>\nContext length: <ctx_length>". V8.1: do NOT include api_key in output. Verify ResolvedEndpoint field names and adjust.

cmd_fast:
- Match &ctx.provider_resolver pattern.
- Call r.resolve_role("fast"). If Some(endpoint): Output(format!("Fast mode: model swap to {} (post-router hook will rebuild client).", endpoint.model)). If None: Output("Fast role not configured (no fast preset in config).").
- Note: cmd_fast in core returns informational text only. The actual Arc<AtomicBool> toggle on app.fast_enabled + AnyClient rebuild is owned by PLAN 03's handle_subsystem_mutator (post-router hook). Plan 02 does NOT touch commands.rs.

REPLACE the existing cmd_provider stub if one exists at handlers.rs ~408 (per CONTEXT D-discretion: "current cmd_provider returns hardcoded text"). Update dispatch arm if signature changes.

Add 3 dispatch arms (or update existing):
    "model" => cmd_model(args, ctx),
    "provider" => cmd_provider(args, ctx),
    "fast" => cmd_fast(args, ctx),

NO async fn. NO block_on (all ProviderResolver methods sync per RESEARCH.md P2 + verified provider.rs).

Verify ResolvedEndpoint and ModelRegistry field/method names against actual source — adjust any field reference (model, provider, base_url, context_length) if the real names differ.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-core 2>&amp;1 | tail -10 ; grep -E "fn cmd_(model|provider|fast)\(" crates/ironhermes-core/src/commands/handlers.rs | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p ironhermes-core` exits 0
    - `grep -E "fn cmd_(model|provider|fast)\(" crates/ironhermes-core/src/commands/handlers.rs | wc -l` returns at least 3
    - `grep "ctx.provider_resolver" crates/ironhermes-core/src/commands/handlers.rs | wc -l` returns at least 3 (one per handler)
    - `grep "Provider resolver not configured" crates/ironhermes-core/src/commands/handlers.rs | wc -l` returns at least 3 (None-guard text)
    - `grep "async fn cmd_" crates/ironhermes-core/src/commands/handlers.rs` returns ZERO matches (sync invariant)
    - All 3 dispatch arms present: `grep -E '"(model|provider|fast)" =>' crates/ironhermes-core/src/commands/handlers.rs | wc -l` returns at least 3
    - V8.1 — `grep "api_key" crates/ironhermes-core/src/commands/handlers.rs` does NOT appear in cmd_provider's Output formatting (no key disclosure)
    - `cargo test -p ironhermes-core --lib 2>&amp;1 | grep "FAILED" | wc -l` returns 0
  </acceptance_criteria>
  <done>3 cmd_* functions exist with provider_resolver-backed bodies, None-guards, sync method calls, no api_key disclosure; dispatch arms route correctly; core builds clean. NO commands.rs touch — Plan 03 owns the post-router hook.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add INV-42..44 + behavioral tests at handlers_provider.rs</name>
  <files>crates/ironhermes-cli/tests/invariants_22_4.rs, crates/ironhermes-core/tests/handlers_provider.rs</files>
  <read_first>
    - crates/ironhermes-cli/tests/invariants_22_4.rs (post-Plan-01 — INV-37..41 added; CORE_HANDLERS const; pattern from PATTERNS.md Cat-6B)
    - crates/ironhermes-core/src/commands/handlers.rs (post-Task-1 — confirm cmd_model/provider/fast presence)
    - crates/ironhermes-core/src/commands/context.rs (with_provider_resolver builder from Plan 00)
    - crates/ironhermes-core/src/provider.rs (ProviderResolver::build signature, Config::default behavior)
    - crates/ironhermes-core/Cargo.toml (confirm test-relevant dev-deps; tempfile is workspace dep)
    - .planning/phases/22.4.2-wire-up-slash-commands/22.4.2-PATTERNS.md (Category 5A: make_test_ctx_with_<subsystem>)
    - .planning/phases/22.4.2-wire-up-slash-commands/22.4.2-RESEARCH.md (P3 — test fixture inventory; A2 — ProviderResolver::build(&Default::default()) risk)
  </read_first>
  <behavior>
    - 3 new INVs (42-44) added to invariants_22_4.rs and pass
    - 3+ behavioral tests in handlers_provider.rs covering /model /provider /fast via dispatch entry; all pass
  </behavior>
  <action>
(A) ADD INV-22.4-42 (model), INV-22.4-43 (provider), INV-22.4-44 (fast) to invariants_22_4.rs. Each follows the Plan-01 INV-37..41 template:

    #[test]
    fn invariant_22_4_42_model_wired() {
        assert!(
            !TUI_RATA_COMMANDS.contains("Phase 22.4.1 stub: /model"),
            "INV-22.4-42 (a): stub marker for /model must be absent"
        );
        assert!(
            CORE_HANDLERS.contains("fn cmd_model("),
            "INV-22.4-42 (b): fn cmd_model must exist in core::commands::handlers"
        );
    }

Repeat verbatim shape for INV-43 (/provider) and INV-44 (/fast). INV numbers MUST be 42, 43, 44 (sequential after Plan-01's 41).

(B) CREATE `crates/ironhermes-core/tests/handlers_provider.rs` (no circular dep — provider lives in core).

Structure:

    use std::sync::{Arc, atomic::AtomicBool};
    use ironhermes_core::commands::{CommandContext, CommandRouter};
    use ironhermes_core::commands::handlers::dispatch;
    use ironhermes_core::commands::registry::build_registry;
    use ironhermes_core::types::Platform;
    use ironhermes_core::ProviderResolver;
    use ironhermes_core::config::Config;

    fn make_test_ctx_with_provider_resolver() -> CommandContext {
        // ProviderResolver::build(&Config::default()) per RESEARCH.md A2 — verify at impl time
        let cfg = Config::default();
        let resolver = ProviderResolver::build(&cfg).expect("ProviderResolver::build");
        CommandContext::new(
            Platform::Local,
            "test-session".to_string(),
            Arc::new(AtomicBool::new(false)),
        )
        .with_provider_resolver(Arc::new(resolver))
    }

    fn make_router() -> CommandRouter {
        CommandRouter::new(build_registry())
    }

    #[test]
    fn provider_with_no_resolver_returns_not_configured() {
        let ctx = CommandContext::new(Platform::Local, "s".into(), Arc::new(AtomicBool::new(false)));
        let router = make_router();
        let def = router.find("provider").expect("registry has /provider");
        let result = dispatch(def, &[], &ctx, &router);
        match result {
            ironhermes_core::commands::CommandResult::Output(text) => {
                assert!(text.contains("not configured"), "got: {text}");
            }
            other => panic!("expected Output, got {other:?}"),
        }
    }

    #[test]
    fn provider_returns_current_status() {
        let ctx = make_test_ctx_with_provider_resolver();
        let router = make_router();
        let def = router.find("provider").unwrap();
        let result = dispatch(def, &[], &ctx, &router);
        match result {
            ironhermes_core::commands::CommandResult::Output(text) => {
                assert!(text.contains("provider") || text.contains("Provider"), "got: {text}");
                assert!(!text.contains("api_key") && !text.contains("API_KEY"), "V8.1: api_key leak in: {text}");
            }
            other => panic!("expected Output, got {other:?}"),
        }
    }

    #[test]
    fn model_lists_available_models_when_no_args() {
        let ctx = make_test_ctx_with_provider_resolver();
        let router = make_router();
        let def = router.find("model").unwrap();
        let result = dispatch(def, &[], &ctx, &router);
        // Assert Output text contains "models" or model entries
    }

    #[test]
    fn fast_returns_role_resolution_text() {
        let ctx = make_test_ctx_with_provider_resolver();
        let router = make_router();
        let def = router.find("fast").unwrap();
        let result = dispatch(def, &[], &ctx, &router);
        // Assert Output mentions "fast" — result depends on whether Config::default has a fast role
    }

If `ProviderResolver::build(&Config::default())` panics (RESEARCH.md A2 medium risk), fall back to a hand-built minimal `Config { providers: hashmap![ "openrouter" => ProviderConfig { base_url: "https://openrouter.ai/api/v1".into(), api_key: "test".into(), .. } ], .. }`. Verify the actual Config struct shape by reading crates/ironhermes-core/src/config.rs.

If Config::default produces a valid resolver, prefer that for simplicity.

Verify Cargo.toml dev-deps; add tempfile if missing (likely already present via workspace).
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cli --test invariants_22_4 invariant_22_4_42 2>&amp;1 | tail -5 ; cargo test -p ironhermes-cli --test invariants_22_4 invariant_22_4_43 2>&amp;1 | tail -5 ; cargo test -p ironhermes-cli --test invariants_22_4 invariant_22_4_44 2>&amp;1 | tail -5 ; cargo test -p ironhermes-core --test handlers_provider 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ironhermes-cli --test invariants_22_4 invariant_22_4_42` PASS
    - `cargo test -p ironhermes-cli --test invariants_22_4 invariant_22_4_43` PASS
    - `cargo test -p ironhermes-cli --test invariants_22_4 invariant_22_4_44` PASS
    - File `crates/ironhermes-core/tests/handlers_provider.rs` exists
    - `cargo test -p ironhermes-core --test handlers_provider` reports at least 3 tests passed, 0 failed
    - `grep "fn make_test_ctx_with_provider_resolver" crates/ironhermes-core/tests/handlers_provider.rs` returns 1 match
    - V8.1 assertion present: `grep "api_key" crates/ironhermes-core/tests/handlers_provider.rs` returns at least 1 (the negative assertion in provider_returns_current_status)
  </acceptance_criteria>
  <done>3 new INVs added and pass; behavioral test file added with at least 3 passing tests; V8.1 api_key non-disclosure asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → /model <name> → ProviderResolver lookup → core handler validation | Untrusted model name string crosses into endpoint resolution; if resolver lookup fails, Error returned (no rebuild — App-side rebuild is Plan 03's responsibility) |
| ctx.provider_resolver.resolve_for_main() → /provider Output | Endpoint metadata (provider name, model name, base_url) crosses into Output text; api_key MUST NOT be included (V8.1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22.4.2-02-01 | Information Disclosure | /provider Output revealing api_key | mitigate | V8.1: cmd_provider Output composes only model/provider/base_url/context_length fields — explicitly excludes api_key; behavioral test asserts "api_key" string absent in Output |
| T-22.4.2-02-02 | Tampering | /model accepting arbitrary string | mitigate | V5.1: cmd_model validates target against r.model_registry() membership before returning success Output; unknown model returns Error; Plan 03's post-router hook double-checks before rebuild |
| T-22.4.2-02-03 | Denial of Service | /model rebuild on every keystroke from a stuck input loop | accept | Single-process REPL; one slash command per dispatch_slash_blocking gate; rebuild is App-side (Plan 03) and cheap (no HTTP — TLS connector reuse); user-initiated only |
| T-22.4.2-02-04 | Elevation of Privilege | gateway path gains unintended /model rebuild | mitigate | Gateway's post-router hook does NOT exist — gateway uses different handler stack (handler.rs:218); ctx.provider_resolver may be None on gateway, in which case core handler's None-guard returns informational text only — no rebuild |
| T-22.4.2-02-05 | Repudiation | Mid-session model switch with no audit trail | accept | UX preference; not audit-critical; tracing log line at INFO level on model switch is sufficient (existing tracing infrastructure) |
| T-22.4.2-02-06 | Information Disclosure | /model list mode revealing all configured models including private endpoints | accept | List output reveals model names + provider names that are already in the user's local config.yaml — same disclosure level as `cat ~/.ironhermes/config.yaml` would provide locally |
</threat_model>

<verification>
- `cargo build --workspace` exits 0
- `cargo test --workspace --features test-support 2>&1 | grep "FAILED" | wc -l` returns 0
- All 3 new INVs (42-44) PASS
- handlers_provider.rs has at least 3 passing behavioral tests
- V8.1 negative assertion (api_key absence) holds in cmd_provider Output
- Plan 03 unblocked — Plan 03 owns the post-router hook expansion (including /model and /fast App-side rebuild)
</verification>

<success_criteria>
- 2 tasks complete with green acceptance criteria
- 3 files modified/created: handlers.rs (3 new bodies), invariants_22_4.rs (INV-42/43/44), handlers_provider.rs (new test file in core/tests/)
- 3 of 28 commands wired AT THE CORE LAYER (model, provider, fast) — App-side reachability lands when Plan 03 ships handle_subsystem_mutator
- INV ledger 41 → 44 (3 added)
- V8.1 mitigation in place (api_key non-disclosure)
- ProviderResolver remains read-only (no new mutation methods on resolver itself)
- Wave 2 progress: Plan 02 + Plan 01 ship 8 of 28 core handlers; Plan 03 covers another 9 + the full post-router hook
- NO write conflict on commands.rs (Plan 02 does not touch it)
</success_criteria>

<output>
After completion, create `.planning/phases/22.4.2-wire-up-slash-commands/22.4.2-02-SUMMARY.md`
</output>
