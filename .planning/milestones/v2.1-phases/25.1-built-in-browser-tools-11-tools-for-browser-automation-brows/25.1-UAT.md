---
status: partial
phase: 25.1-built-in-browser-tools-11-tools-for-browser-automation-brows
source: [25.1-01-SUMMARY.md, 25.1-02-SUMMARY.md, 25.1-03-SUMMARY.md, 25.1-04-SUMMARY.md, 25.1-05-SUMMARY.md, 25.1-06-SUMMARY.md, 25.1-07-SUMMARY.md, 25.1-08-SUMMARY.md, 25.1-09-SUMMARY.md, 25.1-10-SUMMARY.md, 25.1-11-SUMMARY.md, 25.1-13-SUMMARY.md, 25.1-14-SUMMARY.md, 25.1-15-SUMMARY.md]
started: 2026-04-30T20:05:00Z
updated: 2026-05-01T15:00:00Z
---

## Current Test

Re-test cycle 3 — GAP-6 + GAP-7 closures landed (plans 25.1-16, 25.1-17, 25.1-18); operator-confirmed live 4-turn browser flow. Re-running blocked tests in this order:

  4 → 7 → 8 → 10

Currently presenting Test 4 (GAP-6 closure verification). Tests 7/8/10 will follow once 4 confirms GAP-6 and the gateway persistence path is exercised against the allowlist + console-eval + close-idempotency contracts.

## Tests

### 1. Cold Start Smoke Test
expected: Build hermes; run with --help; binary starts cleanly, prints usage, exits 0. No panics or stack traces.
result: pass

### 2. Browser toolset is default-disabled (D-04)
expected: Run `hermes toolset list` (or equivalent toolset-status command). The `browser` toolset appears in the list and shows enabled=false. The 11 browser_* tools do NOT appear in the active tool schema (no LLM call needed — this is the registry view).
result: pass
prior_run: |
  First run (2026-04-30T20:11Z) reported the browser toolset absent from `hermes toolset list`
  (only 6 toolsets shown). Tracked as GAP-1; fix landed in commit 8af9261 (plan 25.1-12).
  Re-run on 2026-04-30T20:26Z: pass.

### 3. Cache-break banner on enable (D-19)
expected: Run `hermes toolset enable browser`. Output displays the cache-break banner — same format as Phase 25 toolsets — indicating the schema cache will rebuild on the next LLM call. After enable, `hermes toolset list` shows browser as enabled=true.
result: pass
prior_run: |
  First run (2026-04-30T20:14Z) blocked by GAP-1 — `hermes toolset enable browser` returned
  `unknown toolset 'browser'`. Fix landed in commit 8af9261. Re-run on 2026-05-01T03:35Z: pass.
  Post-enable list shows `browser    enabled    11      11/11` with all 11 tool names available.
note: |
  Side-observation worth investigating separately: post-enable, memory/session/agent/skills
  show 'disabled' even though they were 'enabled' before. Either (a) hermes toolset enable
  replaced rather than appended (regression), (b) user manually disabled them, or (c) config
  reset. Not blocking this test result; flagged for follow-up.

### 4. Chromium binary discovery (D-05)
expected: With chromium installed (or BROWSER_PATH set to a chromium binary), all 11 browser_* tools are available in a session. Without chromium and BROWSER_PATH unset (or set to /dev/null/x), no browser_* tools appear in get_definitions output. Test by running a session with and without chromium present.
result: pass
resolved_via: 25.1-16 (GAP-6 closure)
re_test_cycle_3: |
  Re-test on 2026-05-01 after plans 25.1-16 (commits 829f2be / 405bc6a / c733139)
  landed: with BROWSER_PATH=/dev/null/x → toolset list browser row 0/11 + show browser
  reports all 11 members unavailable with prereq chromium; with BROWSER_PATH unset and
  system chromium present → both list and show report 11/11 ✓. List/show divergence
  structurally impossible because both call the shared `browser_chromium_unavailable()`
  helper. Operator confirmed.
prior_reported: |
  Re-test on 2026-05-01T12:15Z (after plan 13 fix landed and binary rebuilt at 07:16Z):
  With BROWSER_PATH=/dev/null/x exported, `ironhermes toolset list` STILL shows browser
  row as `11/11` with all tools ✓. Plan 13's fix to find_chromium_binary IS in the binary
  but is_available() is never queried for browser tools from the toolset-list code path.
severity: major (closed)
prior_run: |
  First run blocked by GAP-1. Second run (2026-05-01) opened GAP-2 for find_chromium_binary
  fall-through. Plan 13 closed GAP-2 in browser_session.rs:159-172 with strong unit tests.
  This re-test surfaces GAP-6 — a separate, upstream bug: register_defaults() in
  registry.rs:454-467 only registers 7 default tools (terminal, read_file, write_file,
  patch_file, search_files, web_search, web_read). Browser tools require Arc<Config>
  (from plan 14) and are only registered via register_browser_tools_with_vision, which
  cmd_toolset_list does not call. So registry.list_unavailable() returns no browser_*
  names → build_toolset_rows always counts 11/11 regardless of chromium discovery.
diagnosis_notes: |
  Plan 13's manual UAT step ("browser row shows 0/11") was incorrect — it assumed the
  list path queries is_available() per browser tool. It doesn't. The unit test
  find_chromium_binary_returns_none_when_browser_path_set_to_absent_file_strict is
  green (correctly) but proves only the leaf function. The CLI display path bypasses it.

### 5. browser_navigate + browser_snapshot end-to-end
expected: Start a hermes chat session with browser toolset enabled. Tell the agent to "navigate to <some URL with a button> and take a snapshot." Agent's tool calls succeed; the snapshot output contains a numeric `[N] button "..."` line for the page's interactive button. Refs are stable for that one snapshot call.
result: pass
notes: |
  User asked agent: "navigate to hackernews and take a snapshot of that page".
  Agent (openai/gpt-4o-mini via openrouter) successfully called browser_navigate then
  browser_snapshot, then returned a markdown summary of the Hacker News homepage
  with article links, top stories with point counts, etc. — proves snapshot returned
  rich page content the model could reason about. Raw `[N] role "name"` format in the
  underlying tool output (not user-visible — model summarized it for the chat reply).

### 6. browser_click consumes ref + stale-ref handling (D-10/D-11)
expected: After a snapshot, ask the agent to "click the button with ref N." The click executes (page state changes) without error. Then ask the agent to navigate to a different page, and click the SAME ref (stale). The response contains a structured `element_stale` error envelope rather than a Rust panic; the agent recovers by re-snapshotting.
result: pass

### 7. URL allowlist enforcement (D-15, T-25.1-01)
expected: Set `browser.allowed_domains: ["example.com"]` in config. Ask the agent to navigate to `https://wikipedia.org/`. The tool returns a structured `domain_blocked` error envelope (not a successful nav, no exception). Then nav to `https://example.com/` — succeeds.
result: issue
re_test_cycle_3: |
  Re-test on 2026-05-01 in the local `ironhermes chat` REPL (not gateway/Telegram).
  Agent for BOTH navigate calls (wikipedia.org AND example.com) replied with the
  canned "I can't browse the web or navigate to external websites directly" — i.e.
  the model does not see browser_* tools in its tool schema at all. This is
  distinct from GAP-3 (allowlist not enforced — would have shown a successful
  navigate to wikipedia). It is also distinct from GAP-7 (streaming 400). The
  allowlist contract cannot be exercised because the tools are not present.
  Tracked as GAP-8.
severity: major
prior_reported_blocked_by: gap-7
reason: |
  Re-test on 2026-05-01 (gateway/Telegram mode): the FIRST browser_navigate call
  succeeds (200), agent completes naturally. The SECOND user message in the same
  chat — when message history now contains a prior assistant tool_calls + tool
  result — triggers an immediate 400 Bad Request from the streaming endpoint, all
  3 retries 400. Confirmed on BOTH openai direct (gpt-4o) AND openrouter
  (gpt-4o-mini, the original UAT provider). Same chat completions endpoint with
  a non-streaming request and 9 turns of chained web_read tool calls (history
  grows 2→18 msgs) succeeds without any failures — proving the bug is specific
  to streaming + prior-tool-call history. See GAP-7 for full diagnosis.
severity: major (original test); blocker (current re-test)
hypothesized_root_cause: |
  Three candidates: (a) browser_navigate is not calling
  `BrowserSession::validate_navigation_url` before page.goto; (b) the BrowserConfig is
  loaded with an empty allowed_domains regardless of config (config-passthrough bug);
  (c) the allowlist check exists but matches host substrings/patterns incorrectly.
  Plan 02 implemented validate_navigation_url with exact-match host checks (5 unit
  tests passed against it); Plan 04 wired browser_navigate to call it. Worth checking
  that the BrowserConfig actually reaches BrowserSession::spawn from runtime config,
  not from BrowserConfig::default().

### 8. browser_console eval gating (D-13, T-25.1-02)
expected: With yolo OFF, ask the agent to evaluate JS via `browser_console mode:"eval" expression:"document.title"`. The tool returns an `approval_needed` envelope showing the JS expression — does NOT execute. With yolo ON, the same call evaluates and returns the result. browser_console mode:"log" is NEVER gated.
result: blocked
blocked_by: gap-8 (chat-REPL path); gateway/Telegram path unblocked by GAP-7 closure
reason: |
  Re-test cannot proceed: gateway+browser path crashes the streaming LLM call before
  any browser tool can execute (see GAP-7). browser_console mode:eval cannot be
  exercised end-to-end until GAP-7 is closed.
severity: major (original test)
hypothesized_root_cause: |
  Same architectural pattern as GAP-3 (config-passthrough). Three candidates:
  (a) BrowserConsoleTool::execute() does not branch on mode, evaluating JS unconditionally.
  (b) The branch exists but reads yolo from a default Config (always considered yolo=true).
  (c) `should_prompt_for_approval(yolo)` is called but its return value is ignored/inverted.
  Plan 07's unit tests passed (7/7) — most likely (b): the runtime Config with the actual
  autonomous.yolo value is not reaching the tool.

### 9. browser_vision routes through auxiliary cascade (D-07)
expected: With `auxiliary.vision: { provider: openai, model: gpt-4o-mini }` (or any multimodal model) configured, ask the agent to "use browser_vision to describe what's on the page." The vision call hits the configured aux provider, NOT the main provider. (Verify by inspecting provider logs or by seeing the model name in the response.) Without auxiliary.vision configured AND with a vision-capable main provider, falls back to main. Without either, browser_vision is excluded from get_definitions.
result: pass
notes: |
  AnyClientVisionHandle (Plan 11) wired correctly through resolve_role("vision") cascade.
  Phase 26 SC-2 ("vision wired") is genuinely closed end-to-end — not just in unit tests.
  This validates the most architecturally novel piece of the phase (Phase 26's resolver
  cascade getting its first real consumer).

### 10. browser_close idempotency + session re-spawn (D-03)
expected: Ask the agent to navigate, then close the browser. Then ask it to navigate again. The second navigate spawns a fresh BrowserSession (lazy re-init). Calling close twice doesn't error.
result: blocked
blocked_by: gap-8 (chat-REPL path); gateway/Telegram path unblocked by GAP-7 closure
reason: |
  Re-test cannot proceed: gateway+browser path crashes the streaming LLM call before
  any browser tool can execute (see GAP-7). Description-rewrite fix (plan 15)
  for browser_close natural-language mapping cannot be verified end-to-end until
  GAP-7 is closed.
severity: major (original test)
hypothesized_root_cause: |
  Three candidates:
  (a) browser_close's tool schema description does not surface to the model in a way that
      maps "close the browser" → browser_close. The description may be too narrow or use
      jargon (e.g., "Close session, free resources") rather than user-facing language.
  (b) The Tool::schema() output for browser_close is malformed and the LLM provider strips
      it from the active tool list, even though `toolset list` lies about availability.
  (c) Some registry filter (Tool::is_available, prerequisites) is excluding browser_close
      at request time but not at toolset-list time — divergent state between the two paths.
  
  Worth checking the tool description string in browser_close.rs and comparing against the
  other 10 tools that the model DOES invoke fluently.

## Summary

total: 10
passed: 6
issues: 1            # test 4 still failing post plan-13 (now diagnosed as GAP-6, distinct upstream bug)
blocked: 3           # tests 7, 8, 10 blocked by GAP-7 (gateway+browser streaming crash)
pending: 0
skipped: 0
re_test_cycle_2:
  date: 2026-05-01
  triggered_by: "plans 25.1-13, 25.1-14, 25.1-15 (gap-closure)"
  outcome: "GAP-3/4/5 closure UNVERIFIED (blocked by GAP-7); GAP-2 closure incomplete (GAP-6 surfaced)"
  open_gaps: [GAP-6, GAP-7]
  resolved_via_re_test: []
  unverified_due_to_blocker: [GAP-3, GAP-4, GAP-5]

## Gaps

# GAP-1 (RESOLVED — 2026-04-30T20:24Z)
- truth: "browser toolset appears in `hermes toolset list` and is enable-able via `hermes toolset enable browser` (D-04 toolset enumeration extended from 6 to 7)"
  status: resolved
  reason: |
    User reported two consistent failures during first UAT run:
    (1) Test 2 — `hermes toolset list` output shows only 6 toolsets. `browser` absent.
    (2) Test 3 — `hermes toolset enable browser` returns: `Error: unknown toolset 'browser' — known toolsets: agent, code, memory, session, skills, web`.
  severity: major
  test: 2
  cascading_blocked_tests: [3, 4, 5, 6, 7, 8, 9, 10]
  root_cause: |
    `crates/ironhermes-cli/src/toolset_cmd.rs:15` — `KNOWN_TOOLSETS: &[&str]` was a 6-entry slice
    that backed both the `check_known_toolset()` validator (gates enable/disable/show) and
    `build_toolset_rows()` (controls list output). Phase 25.1 D-04 extended the enum to 7 in
    `ToolsConfig::default()` (Plan 02) and registered the 11 browser_* tools in the registry
    (Plan 09), but the CLI's hardcoded known-set was not extended.
  artifacts:
    - path: "crates/ironhermes-cli/src/toolset_cmd.rs"
      issue: "KNOWN_TOOLSETS missing 'browser' entry; toolset_members_map() missing 'browser' key"
  missing:
    - "Add 'browser' to KNOWN_TOOLSETS slice (6 → 7 entries)"
    - "Add 'browser' → 11 browser_* tool names mapping in toolset_members_map()"
    - "Regression tests browser_in_known_set + cmd_toolset_enable_accepts_browser"
  debug_session: ""
  fix_plan: "25.1-12"
  fix_commits: ["8af9261", "ea439cc"]

# GAP-2
- truth: "find_chromium_binary returns None when BROWSER_PATH is set but does not point to an executable file (D-05 step 1 authority)"
  status: failed
  reason: |
    User set `BROWSER_PATH=/dev/null/x` (nonexistent path) and ran `ironhermes toolset list`.
    The browser row still showed all 11 tools as available (11/11 ✓). Expected: when BROWSER_PATH
    is set, the function returns None without falling through to subsequent discovery sources.
    Actual: function appears to fall through from BROWSER_PATH (invalid) to step 5 (platform-specific
    paths) and find the system Chrome at /Applications/Google Chrome.app/...
  severity: major
  test: 4
  hypothesized_root_cause: |
    `find_chromium_binary` in crates/ironhermes-tools/src/browser_session.rs likely uses
    `let candidate = env::var("BROWSER_PATH").ok();` followed by `if let Some(p) = candidate { if p.exists() { return Some(p) } }` — but missing the early-return None when the env var is set but the path is invalid. Spec D-05 step 1 says "if set, must point to an executable file" — implying authoritative when set.
  artifacts: []
  missing:
    - "If BROWSER_PATH is set: validate executable. If valid → use. If invalid → return None (do NOT fall through)."
    - "Same logic for CHROMIUM_PATH (step 2) — if set explicitly, must be honored."
    - "Add unit test: BROWSER_PATH=/dev/null/x → find_chromium_binary() returns None"
  debug_session: ""

# GAP-3
- truth: "browser_navigate enforces D-15 allowed_domains allowlist (host exact-match), returning domain_blocked envelope when host is not in the configured allowlist"
  status: failed
  reason: |
    User set `browser.allowed_domains: ["example.com"]` in config and asked the agent
    to navigate to https://wikipedia.org/. The navigation SUCCEEDED (Hermes responded
    "I've accessed the Wikipedia homepage"). Expected: structured domain_blocked error
    envelope. T-25.1-01 (SSRF mitigation) is not actually enforced.
  severity: major
  test: 7
  threat: T-25.1-01
  hypothesized_root_cause: |
    Three candidates:
    (a) browser_navigate is not calling validate_navigation_url before page.goto.
    (b) BrowserConfig is loaded as default() (empty allowed_domains) regardless of user config — config-passthrough bug between hermes config load and BrowserSession::spawn.
    (c) Allowlist exists but the host-extraction or matching logic has a bug that lets non-listed hosts through.
    Plan 02 unit tests for validate_navigation_url passed (5/5), so the function works
    in isolation — most likely (b) — config not reaching the function.
  artifacts:
    - path: "crates/ironhermes-tools/src/browser_navigate.rs"
      issue: "verify call to validate_navigation_url(allowed_domains, url) exists in execute path"
    - path: "crates/ironhermes-tools/src/browser_session.rs"
      issue: "verify BrowserSession holds the configured BrowserConfig (allowed_domains, allowed_schemes)"
    - path: "crates/ironhermes-cli/src/main.rs"
      issue: "verify BrowserConfig is loaded from Config and passed to BrowserSession::spawn — not BrowserConfig::default()"
  missing:
    - "Wire BrowserConfig from main config through to BrowserSession::spawn in all 3 entry points"
    - "Add integration test: config with allowed_domains=['example.com'] → navigate('https://wikipedia.org') returns domain_blocked"
  debug_session: ""

# GAP-4
- truth: "browser_console mode:eval is gated through yolo+approval (returns approval_needed envelope when yolo=false, executes only when yolo=true). mode:log is never gated."
  status: failed
  reason: |
    User (verbatim): "fail, each time the title was examined". JS eval (`document.title`)
    returned the page title each time without an approval prompt, with yolo presumed OFF
    (default config). T-25.1-02 (arbitrary JS exec mitigation) is bypassed.
  severity: major
  test: 8
  threat: T-25.1-02
  hypothesized_root_cause: |
    Same architectural pattern as GAP-3 (config-passthrough). Three candidates:
    (a) BrowserConsoleTool::execute() does not actually branch on mode — evaluates JS unconditionally.
    (b) Branch exists but reads yolo from a default Config (always treats as yolo=true).
    (c) should_prompt_for_approval(yolo) is called but its return value is ignored/inverted.
    Plan 07 unit tests (7/7) passed — most likely (b): the runtime Config with the actual
    autonomous.yolo value is not reaching the tool. Worth checking how Plan 07 wired
    yolo into BrowserConsoleTool — likely Config::load() is called inside execute() and
    if the config file is missing/incomplete, defaults to yolo=true.
  artifacts:
    - path: "crates/ironhermes-tools/src/browser_console.rs"
      issue: "verify mode='eval' branch calls should_prompt_for_approval(yolo) and short-circuits on true"
    - path: "crates/ironhermes-tools/src/approval.rs"
      issue: "verify should_prompt_for_approval(yolo) returns true when yolo=false (i.e., the prompt is needed)"
  missing:
    - "Wire runtime Config (with autonomous.yolo) into BrowserConsoleTool — same fix as GAP-3 BrowserConfig threading"
    - "Add integration test: yolo=false + browser_console mode=eval → approval_needed envelope, no execution"
    - "Add integration test: yolo=true + browser_console mode=eval → executes, returns Value"
    - "Add integration test: yolo=false + browser_console mode=log → executes (never gated)"
  debug_session: ""

# GAP-5
- truth: "browser_close is invoke-able by the model when the user asks to close the browser"
  status: failed
  reason: |
    User asked agent "close the browser". Model responded "I don't have the capability to
    open or close browsers". Subsequently `browser_navigate` to hackernews.com worked fine
    in the same session — so the toolset is enabled, the registry has the tool (test 3
    showed 11/11), but `browser_close` is not effectively reaching the model OR its schema
    is too obscure for the model to map natural-language close requests to it.
  severity: major
  test: 10
  hypothesized_root_cause: |
    Three candidates:
    (a) browser_close's tool description string is too narrow ("Close session, free resources")
        and the model declines to map "close the browser" → browser_close. Fix: rewrite the
        description to use user-facing language explicitly mentioning "close" / "browser".
    (b) Tool::schema() output for browser_close is malformed and the LLM provider strips it
        from the active tool list at request time. Fix: dump the actual JSON schema sent to
        the provider and verify browser_close is present and well-formed.
    (c) A registry filter (Tool::is_available, prerequisites) excludes browser_close at request
        time but not at toolset-list time — divergent state. Fix: trace why is_available may
        return false for browser_close specifically (the other 10 tools work).
  artifacts:
    - path: "crates/ironhermes-tools/src/browser_close.rs"
      issue: "verify Tool::description and Tool::schema produce model-friendly output; verify is_available behavior matches the other 10 browser_* tools"
    - path: "crates/ironhermes-tools/src/registry.rs"
      issue: "verify get_definitions() includes browser_close (not just register_browser_tools listing)"
  missing:
    - "Diagnose path-divergence between toolset list (shows ✓) and request-time tool schema (model says no capability)"
    - "Improve browser_close description to explicit user-facing intent: 'Close the browser session and release Chromium resources'"
    - "Add manual UAT after fix: confirm 'close the browser' triggers browser_close tool call"
  debug_session: ""
  fix_plan: "25.1-15"
  fix_commits: ["90f29e8", "c0c3e55"]
  status_after_fix: "pending re-test (UAT 2026-05-01)"

# GAP-6 (RESOLVED — 2026-05-01 via plan 25.1-16)
- truth: "`ironhermes toolset list` reflects browser tool availability based on chromium discovery — when find_chromium_binary returns None, browser row shows 0/11 (or omits the toolset). The display path actually queries Tool::is_available() for browser_* tools."
  status: resolved
  fix_plan: "25.1-16"
  fix_commits: ["829f2be", "405bc6a", "c733139"]
  resolution: |
    Plan 25.1-16 implemented option (a): a shared `browser_chromium_unavailable()`
    helper in toolset_cmd.rs that consults `find_chromium_binary(None).is_none()`,
    consumed by BOTH `build_toolset_rows` (the list path) AND `build_toolset_show_view`
    (the show path). List/show divergence is now structurally impossible. 3 unit
    tests lock the contract: `browser_row_reports_zero_when_chromium_unavailable`,
    `browser_show_reports_zero_when_chromium_unavailable`,
    `browser_row_reports_full_when_chromium_available`. Manual UAT cycle 3 to confirm
    `BROWSER_PATH=/dev/null/x ironhermes toolset list` shows browser row 0/11 ✗.
  reason: |
    Re-test of test 4 after plan 13's GAP-2 fix landed: with BROWSER_PATH=/dev/null/x
    exported, `toolset list` still shows browser as 11/11. Plan 13's fix to
    find_chromium_binary IS in the binary (confirmed: target/release/ironhermes mtime
    2026-05-01T07:16Z, after merge commit 0142a57). Unit tests for find_chromium_binary
    are green (8/8). The bug is in the registry-population path used by toolset list,
    not in chromium discovery itself.
  severity: major
  test: 4
  related: GAP-2
  hypothesized_root_cause: |
    `cmd_toolset_list` (crates/ironhermes-cli/src/toolset_cmd.rs:115-133) constructs
    a fresh ToolRegistry and calls `registry.register_defaults()`. `register_defaults`
    (crates/ironhermes-tools/src/registry.rs:454-467) registers ONLY 7 tools (terminal,
    read_file, write_file, patch_file, search_files, web_search, web_read) — not the
    11 browser_* tools, because browser tools require Arc<Config> (added in plan 14).
    Consequently `registry.list_unavailable()` never reports any browser_* names →
    `build_toolset_rows` (toolset_cmd.rs:248-319) computes `available_count = 11` for
    every browser_* member regardless of actual chromium discovery state.
    Plan 13's UAT prediction ("browser row shows 0/11") was wrong because it assumed
    is_available() runs in the list path. It doesn't.
  artifacts:
    - path: "crates/ironhermes-cli/src/toolset_cmd.rs"
      issue: "cmd_toolset_list uses register_defaults() which omits browser tools — list count never reflects chromium availability"
    - path: "crates/ironhermes-tools/src/registry.rs"
      issue: "register_defaults() does not register browser_* tools (they need Arc<Config> per plan 14)"
  missing: |
    Fix options (pick one):
    (a) Special-case in build_toolset_rows: when computing available_count for the
        'browser' toolset, call find_chromium_binary(None) directly; if None, mark
        all 11 browser_* members unavailable with prereq "chromium-not-found".
    (b) Have cmd_toolset_list load full Config and call register_browser_tools_with_vision
        with a dummy session/resolver/vision_client. Most consistent with the existing
        is_available()-driven prereq machinery, but pulls a lot of runtime infra into
        the list path.
    (c) Move chromium-not-found into a per-tool prereq that does NOT require Config —
        registered alongside the existing 7 tools in register_defaults but as a synthetic
        unavailability hint when is_available()=false. Smaller surface than (b).
    Add regression test: BROWSER_PATH=/dev/null/x → `toolset list` browser row shows 0/11.
  debug_session: ""

# GAP-7 (RESOLVED — 2026-05-01 via plan 25.1-17 in-core + plan 25.1-18 gateway-handler; live 4-turn UAT confirmed by operator)
- truth: "When browser toolset is enabled in gateway/Telegram mode and the user asks the agent to use a browser tool, the streaming LLM call succeeds (no provider rejection); the agent invokes browser_navigate (or other browser_* tool) and the response is delivered to the user."
  status: resolved
  fix_plan: "25.1-17, 25.1-18"
  fix_commits: ["0f88b34", "b509c1f", "2339067", "2779fe4", "dea7de7"]
  resolution: |
    Two-stage closure:
    
    Stage 1 — In-core invariant + pre-send guards (plan 25.1-17, commits 0f88b34 + b509c1f).
    Added `pub fn validate_tool_call_pairing` in ironhermes-core (strict pending_ids walk,
    8 unit tests). Refactored `tool_pair::check_orphan_invariant` as a thin delegate.
    Pre-send invariant guards at TWO call sites in client.rs (chat_completion line 66,
    chat_completion_stream line 131). Switched `StateStore::get_messages` from
    `ORDER BY timestamp ASC` to `ORDER BY id ASC` for the query path.
    
    Stage 2 — Gateway-handler tool-message persistence (plan 25.1-18, commit 2779fe4).
    Plan 17's deterministic regression suites bypassed the gateway hot path entirely
    (state_store tests + wiremock client tests, neither exercised the full gateway
    persistence path). The actual orphan-creation site was a Role filter at
    `crates/ironhermes-gateway/src/handler.rs:642-646` that dropped Role::Tool messages
    on persist:
    
        let new_messages = result.messages.into_iter()
            .filter(|m| m.role == Role::Assistant)   // ← dropped Role::Tool
            .collect();
    
    Per D-02, gateway sessions live in `GatewaySession::messages` (in-memory) and never
    re-hydrate from SQLite, so the SQL ORDER BY fix from plan 17 was inert for the
    gateway. Plan 18 added `pub appended: Vec<ChatMessage>` to AgentResult tracking
    the round-trip output of THIS run only (compression-safe — pre_chat_compress
    cannot perturb it), and replaced the gateway filter with `for msg in result.appended
    { store.add_message_to_session(...) }`. Two new tests in
    `crates/ironhermes-agent/tests/agent_loop_appended.rs` exercise the full
    `agent.run()` path with a wiremock 2-turn flow and prove appended preserves the
    assistant↔tool pair AND that reconstructed `[sys, user] + appended + new_user`
    history passes `validate_tool_call_pairing`.
    
    Live UAT: operator confirmed 4-turn browser-tool conversation completes cleanly
    post-rebuild, multi-browser conversations work end-to-end, no
    `tool-call pairing invariant violated` WARN lines in logs.
  reason: |
    User logs (verbatim, gateway mode, 2026-05-01T12:30:13–12:33:01Z):

    First trial — "navigate to hackernews":
      WARN LLM call failed, retrying: Streaming LLM call failed retry=1
      WARN LLM call failed, retrying: Streaming LLM call failed retry=2
      WARN LLM call failed, retrying: Streaming LLM call failed retry=3
      ERROR Agent error: Streaming LLM call failed

    Second trial after restart — "navigate to hackernews": completed in 2 turns,
    likely without invoking browser tools (response from training data / web tool).

    Third trial — "use browser to navigate to hacker news":
      Same 3-retry cascade, immediate failure.

    Retries fire ~0.85s and ~1.4s apart — these are immediate provider rejections
    (4xx-style), not network timeouts. Original UAT tests 5 and 6 passed in
    chat mode with openai/gpt-4o-mini via openrouter on 2026-04-30 (before plans
    13/14/15). Regression in either gateway-mode tool serialization OR
    plan-14/plan-15-induced schema changes that fail validation against current
    provider model.
  severity: blocker
  test: 7
  blocks: [7, 8, 10]
  hypothesized_root_cause: |
    CONCLUSIVE NARROWING (after option-B re-test on openrouter at 2026-05-01T12:50-12:51Z):

    The bug is SPECIFIC to the streaming code path's handling of message history
    that contains prior tool_calls. Provider strictness, schema rejection, tool-count
    cap, plan-14/15 regressions, and openrouter-vs-direct-OpenAI are all RULED OUT.

    Evidence ladder:
    1. Streaming + first browser_navigate (history=2 msgs, no prior tool_calls):
         openrouter gpt-4o-mini → 200 OK, executes successfully (12:50:11Z)
    2. Streaming + new user message (history=5 msgs WITH prior tool_calls):
         SAME openrouter gpt-4o-mini → 400 Bad Request, all 3 retries 400 (12:50:47Z)
    3. Non-streaming chat completion + 9 turns of chained web_read calls
         (history grows 2→18 msgs, MANY prior tool_calls):
         SAME openrouter gpt-4o-mini → all 200 OK, no failures (12:51:02-19Z)

    → Failure surface = (streaming endpoint) ∩ (history contains prior tool_calls).
    Non-streaming with the same shape of history works perfectly.

    LIKELY ROOT CAUSE LOCATION:
      crates/ironhermes-agent/src/client.rs:247-289 (assemble_tool_calls_from_stream)
      and/or how the resulting ToolCall is serialized into the next streaming
      request body at client.rs:117-126 (chat_completion_stream).

    `assemble_tool_calls_from_stream` correctly builds `arguments` as String via
    delta concatenation. So the assembled SHAPE looks correct. However:
      - The id field starts as String::new() and is overwritten only if a delta
        carries a non-empty id (lines 255-258). If openrouter or OpenAI streaming
        omits the id in subsequent chunks AND the model's id arrived in a chunk
        that was not received cleanly, the resulting ToolCall has empty id.
      - When this empty-id ToolCall is then included in the next streaming
        request's messages history, the assistant.tool_calls[0].id is "" but the
        following tool message's tool_call_id is also "" — provider rejects.
      - OR: provider streaming-mode tool-call ID format differs from non-streaming
        (e.g., short toolu_01... in streaming vs longer call_xxx... in non-streaming);
        when echoing the streaming-mode id back in a request, server rejects.

    PATH TO PROOF (single-line diagnostic):
      The streaming client at client.rs:143-145 ALREADY emits the response body
      when status != 2xx:
        anyhow::bail!("Streaming chat completion failed ({}): {}", status, body);
      But agent_loop.rs:793 logs only `{err}` (top-level context). Change `{err}`
      to `{err:#}` (or iterate err.chain()) and the next run's WARN line will
      contain the provider's exact error.message — which names the offending
      field deterministically. One-line diagnostic; zero risk.

    DEFINITIVE ROOT CAUSE (after applying {err:#} diagnostic, 2026-05-01T13:01:23Z):

    Provider error body verbatim:
      "An assistant message with 'tool_calls' must be followed by tool messages
       responding to each 'tool_call_id'. The following tool_call_ids did not
       have response messages: call_3PlsiYtJbAmwCtvtxjcIhI3G"
      param: "messages.[3].role"

    THIS IS NOT A STREAMING-SERIALIZATION BUG. It is a SESSION-HISTORY PERSISTENCE
    BUG that surfaces specifically through streaming + multi-turn tool calls.

    Evidence:
      - Session 1 (13:00:59): 3 turns, browser_navigate then browser_snapshot,
        completed naturally. In-memory message count after 2 tool round-trips +
        final reply = 7 (system, user, asst_A, tool_A, asst_B, tool_B, asst_final).
      - Session 2 (13:01:23) STARTS AT messages=6 — exactly one short of the 7
        expected. One message is missing from the persisted history.
      - The provider names the missing one's id: call_3PlsiYtJbAmwCtvtxjcIhI3G.
        This is the tool_call_id of an assistant.tool_calls entry whose tool
        result message was either never persisted or stripped on restore.

    Why streaming + browser surfaces it (and not non-streaming web_read):
      Browser tool results (especially browser_snapshot) emit larger payloads
      than web_read. The non-streaming chat-completion test at 13:51:02-19Z
      hit history sizes 2→18 with 9 tool round-trips and never failed because
      web_read results stayed small enough that the persistence layer didn't
      truncate. Browser tools cross the threshold where the bug manifests.

    LIKELY FAULT SITES (audit order):
      1. crates/ironhermes-state/ — session/conversation message persistence.
         Look for "save", "persist", "store_message", or any path that writes
         a Vec<ChatMessage> to disk. Check whether tool-role messages are
         saved alongside the assistant messages that triggered them, or whether
         pairing is by user/assistant turn (which would orphan tool results).
      2. crates/ironhermes-agent/ — context compression. The DEBUG log shows
         "context compression check ratio=0.022 threshold=0.5" — well below
         threshold, so compression did NOT fire here. But the code path that
         WOULD compress (over threshold) might also be the path that drops
         tool messages without preserving asst.tool_calls ↔ tool.tool_call_id
         pairing invariant.
      3. The agent_loop history truncation/budgeting that runs BEFORE building
         the streaming request. If a budget enforces "drop oldest" by message
         count without checking the tool-pair invariant, an orphaned
         assistant.tool_calls remains while its tool result is dropped.

    The fix MUST preserve the OpenAI invariant:
      "Every assistant.tool_calls[i].id appears as a subsequent tool message's
       tool_call_id, AND every tool message's tool_call_id appears as a prior
       assistant.tool_calls[i].id."
    Add a invariant-check function `validate_tool_call_pairing(messages: &[ChatMessage]) -> Result<()>`
    and call it in chat_completion_stream::pre-send AND in any session restore
    path. Fail loudly with the orphaned id rather than letting it reach the
    provider as a 400.

    Provider observed: api.openai.com/v1/chat/completions, model=gpt-4o (DIRECT
    OpenAI, NOT openrouter as in the original 2026-04-30 UAT).

    Behavior pattern:
      Turn 1 (messages=2): browser_navigate → status 200, executes successfully
      Turn 2 (messages=4): final response → status 200, agent completes naturally
      ---- new user message in same chat ----
      Turn 1 of next loop (messages=5, history includes prior tool_calls):
        → status 400 Bad Request, all 3 retries also 400

    The first browser tool call works in isolation. The 400 fires only AFTER the
    persisted message history contains a prior assistant `tool_calls` message and
    its `tool` role result. This rules out the prior schema-rejection / tool-count
    / plan-14 / plan-15 hypotheses (those would 400 on the first call too).

    Hypothesis A1 (most likely): OpenAI direct API stricter than openrouter on
    historical tool_calls serialization. Common offenders:
      - Assistant `tool_calls[].function.arguments` sent as JSON object instead
        of JSON string. (OpenAI requires string per tools API spec.)
      - Missing `name` field on `tool` role messages.
      - `tool_call_id` mismatch between assistant.tool_calls[i].id and the
        following tool message's tool_call_id.
      - Tool result content empty/null when OpenAI requires a string.
    Original UAT passed on openrouter because openrouter normalizes lenient
    formats to the underlying provider. Direct OpenAI on gpt-4o enforces strictly.

    Hypothesis A2: tool-message `content` may be a non-string (e.g., serde_json
    Value or Object) for browser tool results — OpenAI requires content as
    string for tool role.

    To confirm: capture the request body in agent_loop.rs:916 streaming path
    OR enable wire-level logging via reqwest middleware. The 400 response body
    from OpenAI names the exact offending field — that single error string
    should pinpoint the fix in <5 minutes.
  artifacts:
    - path: "crates/ironhermes-agent/src/agent_loop.rs"
      issue: "line 916 — Streaming LLM call failed; need raw provider error body to diagnose"
    - path: "crates/ironhermes-tools/src/browser_close.rs"
      issue: "schema: properties={} (empty); may fail strict-mode validation"
    - path: "crates/ironhermes-tools/src/browser_back.rs"
      issue: "schema: properties={} (empty); may fail strict-mode validation"
    - path: "crates/ironhermes-tools/src/browser_press.rs"
      issue: "schema: properties={} (empty); may fail strict-mode validation"
    - path: "crates/ironhermes-tools/src/browser_get_images.rs"
      issue: "schema: properties={} (empty); may fail strict-mode validation"
  missing:
    - "Step 1 DONE (2026-05-01): {err:#} diagnostic applied at agent_loop.rs:793 + handler.rs:656. Provider body now visible in WARN logs. Definitive root cause identified above (session-history persistence drops a tool message)."
    - "Step 2: audit crates/ironhermes-state/ for session/conversation persistence. Find the path that writes ChatMessage Vec to disk and the path that restores it. Verify tool-role messages are stored AND restored in correct order alongside the assistant.tool_calls that triggered them."
    - "Step 3: add validate_tool_call_pairing(messages: &[ChatMessage]) -> Result<()>. Run pre-send in chat_completion_stream and chat_completion (both paths). On failure, log the orphan id and either repair (drop the assistant.tool_calls), reconstruct (synthesize an empty tool result), or fail loudly with explicit guidance."
    - "Step 4: fix the root persistence bug — store tool messages, restore them in order, preserve asst↔tool pairing across compression/truncation."
    - "Step 5: regression tests — (a) round-trip a session with browser tool calls through save/load and assert no orphaned ids; (b) trigger compression with tool calls in history and assert pairing is preserved; (c) end-to-end test that 2 sequential browser tool turns followed by a 3rd user message does NOT 400."
    - "Step 6: re-test UAT 7/8/10 to confirm GAP-3/4/5 closure end-to-end. Also re-test 4 to confirm GAP-6 (toolset list display) is closed."
  debug_session: ""

# GAP-8 (NEW — surfaced during re-test cycle 3 of test 7 on 2026-05-01; blocks tests 7/8/10 on the chat-REPL path)
- truth: "`ironhermes chat` (the local rata-based REPL) registers the 11 browser_* tools the same way `ironhermes <prompt>` (run_single) and the gateway (run_gateway) do — when browser toolset is enabled and chromium is discoverable, the model sees browser_navigate/snapshot/click/etc. in its tool schema and can invoke them."
  status: failed
  reason: |
    Re-test on 2026-05-01 of test 7 in `ironhermes chat` (live REPL — see screenshot
    in conversation): user asked the agent to "navigate to https://wikipedia.org/" and
    "navigate to https://example.com/". For BOTH calls the model replied with the
    canned "I can't browse the web or navigate to external websites directly" — i.e.
    the model does not see browser_* tools in its tool schema. Distinct from GAP-3
    (allowlist not enforced — would have shown a successful navigate to wikipedia)
    and from GAP-7 (streaming 400). The allowlist contract cannot be exercised
    because the tools are simply not present in the chat-REPL path.
  severity: major
  test: 7
  cascading_blocked_tests: [7, 8, 10]
  root_cause: |
    `Commands::Chat` dispatches to `ironhermes_cli::tui_rata::run_chat_ratatui`
    (`crates/ironhermes-cli/src/main.rs:355` and `:437`). Browser-tool wiring lives
    in three places — `run_single` (main.rs:677), `run_chat` (main.rs:1178), and
    `run_gateway` (main.rs:2139). `grep -rn 'register_browser_tools\|with_browser_session\|browser_session' crates/ironhermes-cli/src/tui_rata/`
    returns ZERO matches. The rata REPL builds a ToolRegistry without
    `register_browser_tools_with_vision` and an AgentLoop without
    `with_browser_session(...)`, so the chat path's tool schema does not include
    the 11 browser_* entries regardless of toolset enable/disable state. Test 5's
    prior pass was on a non-rata path; it was never re-verified after
    `run_chat_ratatui` became the default `Commands::Chat` handler (Phase 22.4.4).
  artifacts:
    - path: "crates/ironhermes-cli/src/main.rs"
      issue: "Commands::Chat → run_chat_ratatui dispatch (lines 355 + 437) routes to a path that omits browser-tool wiring."
    - path: "crates/ironhermes-cli/src/tui_rata/"
      issue: "no register_browser_tools_with_vision or with_browser_session call anywhere under this directory — entire directory missing the wiring that exists in run_chat (line 1178) and run_gateway (line 2139)."
  missing:
    - "In tui_rata's session/agent setup: build `browser_session = Arc<Mutex<Option<BrowserSession>>>`, call `registry.register_browser_tools_with_vision(...)` with the same args run_chat uses, pass `browser_session` into the AgentLoop builder via `with_browser_session()`."
    - "Mirror the Arc<Config>-passthrough pattern from plan 25.1-14 so allowlist/yolo/aux-vision reach the rata REPL's browser tools."
    - "Regression test: a unit test in tui_rata asserts the AgentLoop built for chat has the 11 browser_* tools registered when ToolsConfig has `browser=enabled` AND chromium is discoverable; AND zero of them registered when `browser=disabled` (D-04 must hold)."
  debug_session: ""
