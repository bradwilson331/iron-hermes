---
phase: 27.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-02: REQUIREMENTS.md contains a new `### Hexapod Integration` section with 16 HXP-prefixed requirement entries (HXP-TOOL-01..06, HXP-LOCO-01..05, HXP-NAV-01..04, HXP-DOC-01)."
    - "D-02: ROADMAP.md no longer contains the `[Urgent work - to be planned]` Phase 27.1 stub at lines 800–809; it has been replaced with a real meta-phase entry plus three sub-phase entries 27.1.1 / 27.1.2 / 27.1.3."
    - "D-02b: ROADMAP.md sub-phase entries reference paths like `crates/ironhermes-tools/src/hexapod_tcp.rs` exactly as written in the source repo (no path rewriting)."
    - "D-03: Every imported requirement ID carries the `HXP-` prefix (TOOL-NN/LOCO-NN/NAV-NN/DOC-NN are not introduced bare in either file by this plan)."
    - "D-03a (surface 1+2): The HXP- rename has propagated atomically to REQUIREMENTS.md (definitions + Active scope table row + 16 Traceability rows) AND ROADMAP.md (the 27.1.1/.2/.3 `**Requirements:**` lines)."
    - "D-03b: The existing IronHermes `### Tool Registry` TOOL-01..05 entries (Phase 25 toolset management) remain unmodified — their bullets, traceability rows, and Active scope row are byte-identical to pre-import state."
    - "D-01: Phase 27.1 (meta-phase) keeps `**Depends on:** Phase 27`, 27.1.1 → `Phase 27.1`, 27.1.2 → `Phase 27.1.1`, 27.1.3 → `Phase 27.1.2` — dependency chain re-anchored from source 1/2/3 numbering."
    - "D-01a: Phase 27 (Prompt Caching, PRMT-08/09) entry above the 27.1 block remains byte-identical to pre-import state — only lines 800–809 are replaced."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "16 HXP-prefixed requirement definitions, Active-scope row, 16 Traceability rows"
      contains: "### Hexapod Integration"
    - path: ".planning/ROADMAP.md"
      provides: "Real Phase 27.1 meta-phase entry + Phase 27.1.1/.2/.3 sub-phase entries replacing the placeholder stub"
      contains: "### Phase 27.1.1: Safe Foundation (INSERTED)"
  key_links:
    - from: ".planning/ROADMAP.md (Phase 27.1.1 Requirements line)"
      to: ".planning/REQUIREMENTS.md (### Hexapod Integration section)"
      via: "HXP-TOOL-01..06, HXP-LOCO-01..05 IDs"
      pattern: "HXP-TOOL-0[1-6]|HXP-LOCO-0[1-5]"
    - from: ".planning/REQUIREMENTS.md Traceability table (16 new rows)"
      to: ".planning/ROADMAP.md Phase 27.1.x entries"
      via: "Phase 27.1.1 / 27.1.2 / 27.1.3 phase tags in Status column"
      pattern: "HXP-.*Phase 27\\.1\\.[123]"
---

<objective>
Land the two foundational planning-surface writes that define the HXP- ID namespace and re-anchor the ROADMAP for the hexapod sub-phases.

**Purpose:** Per RESEARCH §Order-of-Operations, REQUIREMENTS.md must be written before ROADMAP.md so the `**Requirements:**` lines in the new sub-phase entries reference IDs that already exist in the requirements catalogue. Both writes happen in this plan because they are the only two writes in Wave 1 — they must serialize (both touch `.planning/`) but neither depends on anything outside this plan.

**Output:** REQUIREMENTS.md gains a `### Hexapod Integration` section, an Active scope row, and 16 Traceability rows. ROADMAP.md lines 800–809 are replaced with a real Phase 27.1 meta-phase block followed by three sub-phase blocks 27.1.1 / 27.1.2 / 27.1.3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-CONTEXT.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-RESEARCH.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert `### Hexapod Integration` section into REQUIREMENTS.md (16 HXP- reqs + Active-scope row + 16 Traceability rows)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — read current state of Active-scope table at lines ~10–22, the `### Learning Loop` section ending at line ~180, the `## Future Requirements` heading at line ~182, the Traceability table at lines ~256–361, and the `**Coverage:**` summary at lines ~363–370)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-PATTERNS.md (lines 23–104 — exact section heading + checkbox + Active-scope row + Traceability row patterns)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-RESEARCH.md (lines 154–190 — the 16 source IDs and their HXP- mappings)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-CONTEXT.md `<decisions>` section (D-02, D-03, D-03a — the HXP- rename contract; D-03b — DO NOT rename existing TOOL-01..05)
  </read_first>
  <action>
Apply three edits to `.planning/REQUIREMENTS.md` per D-02 + D-03 + D-03a (surface 1) + Pitfall 1 of RESEARCH.md (no global find-replace — write new content with HXP- baked in):

**EDIT 1 — Update Active scope summary on line 10.**
Change: `**Active scope (34 reqs across 8 categories):**`
To:     `**Active scope (50 reqs across 9 categories):**`

**EDIT 2 — Append a new row to the Active-scope table (immediately after line 21, the `**Learning Loop (NEW for v2.1)**` row, before the closing blank line at line 22).**
Insert this single row verbatim:

```
| Hexapod Integration | HXP-TOOL-01..06, HXP-LOCO-01..05, HXP-NAV-01..04, HXP-DOC-01 |
```

**EDIT 3 — Insert a new `### Hexapod Integration` section between `### Learning Loop` (ends ~line 180) and `## Future Requirements` (line 182).**
Place this entire block on a fresh line BEFORE the `## Future Requirements` H2 heading. Preserve a blank line on either side of the new section to match the spacing pattern of `### Learning Loop`:

```markdown

### Hexapod Integration

- [ ] **HXP-TOOL-01**: `hexapod_tcp` tool registered in IronHermes ToolRegistry that sends commands to the robot and returns a result
- [ ] **HXP-TOOL-02**: Allowlist + per-command argument bounds validation (compile-time exhaustive match; blocked commands return Ok("Action '...' is blocked"))
- [ ] **HXP-TOOL-03**: Fresh TCP connection to `HEXAPOD_IP:5002` per call (stateless; no persistent socket)
- [ ] **HXP-TOOL-04**: Semantic JSON schema → wire protocol translation in Rust (action enum → CMD_* strings with correct field layout)
- [ ] **HXP-TOOL-05**: Parsed human-readable response for sensor commands (CMD_POWER, CMD_SONIC) with 3s read timeout
- [ ] **HXP-TOOL-06**: Send stop + relax on IronHermes session end via `on_session_end` Tool trait hook
- [ ] **HXP-LOCO-01**: Walk forward/backward/left/right at configurable speed 2–10
- [ ] **HXP-LOCO-02**: Stop and return to neutral stance (CMD_MOVE mode=0)
- [ ] **HXP-LOCO-03**: Read battery voltage with low-battery alert (< 5.5V or < 6.0V threshold)
- [ ] **HXP-LOCO-04**: Read ultrasonic distance in centimeters
- [ ] **HXP-LOCO-05**: Relax servos for safe idle (CMD_RELAX)
- [ ] **HXP-NAV-01**: Rotate in place by degrees (positive = right, negative = left)
- [ ] **HXP-NAV-02**: Set all LEDs to RGB color (0–255 per channel) or turn off
- [ ] **HXP-NAV-03**: Control head pan (50–180°) and tilt (0–180°) independently
- [ ] **HXP-NAV-04**: Buzzer on/off
- [ ] **HXP-DOC-01**: Skill doc at `skills/hexapod/DESCRIPTION.md` with full protocol reference (all CMD_* strings, wire format, action list, blocked commands)
```

**EDIT 4 — Append 16 Traceability rows immediately after the last existing row (line ~361, the `LEARN-05 | Phase 33 | Pending` row), and before the blank line preceding `**Coverage:**`.**
Insert these rows verbatim, in this exact order (matches PATTERNS.md lines 86–102):

```
| HXP-TOOL-01 | Phase 27.1.1 | Pending |
| HXP-TOOL-02 | Phase 27.1.1 | Pending |
| HXP-TOOL-03 | Phase 27.1.1 | Pending |
| HXP-TOOL-04 | Phase 27.1.1 | Pending |
| HXP-TOOL-05 | Phase 27.1.1 | Pending |
| HXP-TOOL-06 | Phase 27.1.1 | Pending |
| HXP-LOCO-01 | Phase 27.1.1 | Pending |
| HXP-LOCO-02 | Phase 27.1.1 | Pending |
| HXP-LOCO-03 | Phase 27.1.1 | Pending |
| HXP-LOCO-04 | Phase 27.1.1 | Pending |
| HXP-LOCO-05 | Phase 27.1.1 | Pending |
| HXP-NAV-01 | Phase 27.1.2 | Pending |
| HXP-NAV-02 | Phase 27.1.3 | Pending |
| HXP-NAV-03 | Phase 27.1.2 | Pending |
| HXP-NAV-04 | Phase 27.1.2 | Pending |
| HXP-DOC-01 | Phase 27.1.3 | Pending |
```

**EDIT 5 — Update the `*Last updated:*` footer line (currently line ~374) to reflect today's import. Change the date to `2026-05-10` and append a note. Replace the existing line:**

```
*Last updated: 2026-04-27 — v2.1 expanded scope: + 5 Learning Loop reqs (LEARN-01..05) + 2 phases (32 + 33). Total: 34 reqs across 11 phases.*
```

with:

```
*Last updated: 2026-05-10 — Phase 27.1 import: + 16 Hexapod Integration reqs (HXP-TOOL-01..06, HXP-LOCO-01..05, HXP-NAV-01..04, HXP-DOC-01) across Phases 27.1.1/.2/.3. Total active: 50 reqs across 9 categories.*
```

**HARD CONSTRAINT (D-03b + Pitfall 1):** DO NOT modify any existing line that mentions un-prefixed `TOOL-01`, `TOOL-02`, ..., `TOOL-05`, `LOCO-NN`, `NAV-NN`, or `DOC-NN`. The existing `### Tool Registry` section (lines ~121–125) and the existing Traceability rows for TOOL-01..05 (lines ~319–323) are Phase 25 "Toolset management" content and MUST remain byte-identical. Use the Edit tool with precise old_string/new_string targeting; do NOT use Bash sed or any global find-replace.
  </action>
  <verify>
    <automated>bash -c '
set -e
cd /Users/twilson/code/ironhermes
# 1) Section heading exists exactly once
test "$(grep -c "^### Hexapod Integration$" .planning/REQUIREMENTS.md)" = "1"
# 2) Exactly 16 HXP- definition lines (filter comment-style # if any; here lines start with -)
test "$(grep -cE "^- \[ \] \*\*HXP-(TOOL|LOCO|NAV|DOC)-[0-9]{2}\*\*:" .planning/REQUIREMENTS.md)" = "16"
# 3) Active-scope row present
grep -qF "| Hexapod Integration | HXP-TOOL-01..06, HXP-LOCO-01..05, HXP-NAV-01..04, HXP-DOC-01 |" .planning/REQUIREMENTS.md
# 4) Active-scope summary updated to 50 / 9
grep -qF "**Active scope (50 reqs across 9 categories):**" .planning/REQUIREMENTS.md
# 5) Exactly 16 HXP- traceability rows
test "$(grep -cE "^\| HXP-(TOOL|LOCO|NAV|DOC)-[0-9]{2} \| Phase 27\.1\.[123] \| Pending \|$" .planning/REQUIREMENTS.md)" = "16"
# 6) Each HXP-ID appears at least twice (once in section, once in traceability)
for id in HXP-TOOL-01 HXP-TOOL-02 HXP-TOOL-03 HXP-TOOL-04 HXP-TOOL-05 HXP-TOOL-06 HXP-LOCO-01 HXP-LOCO-02 HXP-LOCO-03 HXP-LOCO-04 HXP-LOCO-05 HXP-NAV-01 HXP-NAV-02 HXP-NAV-03 HXP-NAV-04 HXP-DOC-01; do
  count=$(grep -cF "$id" .planning/REQUIREMENTS.md)
  test "$count" -ge 2 || { echo "FAIL: $id appears $count times (expect >= 2)"; exit 1; }
done
# 7) PROHIBITED: existing TOOL-01..05 not renamed (Phase 25 Toolset Management lines must survive byte-identical)
test "$(grep -cE "^- \[ \] \*\*TOOL-0[1-5]\*\*:" .planning/REQUIREMENTS.md)" = "5"
# 8) PROHIBITED: existing TOOL-01..05 traceability rows not renamed
test "$(grep -cE "^\| TOOL-0[1-5] \| Phase 25 \| Pending \|$" .planning/REQUIREMENTS.md)" = "5"
echo "REQUIREMENTS.md edits OK"
'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### Hexapod Integration$" .planning/REQUIREMENTS.md` returns exactly 1.
    - `grep -cE "^- \[ \] \*\*HXP-(TOOL|LOCO|NAV|DOC)-[0-9]{2}\*\*:" .planning/REQUIREMENTS.md` returns exactly 16.
    - `grep -qF "| Hexapod Integration | HXP-TOOL-01..06, HXP-LOCO-01..05, HXP-NAV-01..04, HXP-DOC-01 |" .planning/REQUIREMENTS.md` exits 0.
    - `grep -qF "**Active scope (50 reqs across 9 categories):**" .planning/REQUIREMENTS.md` exits 0.
    - `grep -cE "^\| HXP-(TOOL|LOCO|NAV|DOC)-[0-9]{2} \| Phase 27\.1\.[123] \| Pending \|$" .planning/REQUIREMENTS.md` returns exactly 16.
    - Each of the 16 HXP-IDs appears at least twice in the file (definition + traceability).
    - `grep -cE "^- \[ \] \*\*TOOL-0[1-5]\*\*:" .planning/REQUIREMENTS.md` returns exactly 5 (existing Phase 25 toolset management TOOL-01..05 unmodified).
    - `grep -cE "^\| TOOL-0[1-5] \| Phase 25 \| Pending \|$" .planning/REQUIREMENTS.md` returns exactly 5 (existing traceability rows for Phase 25 TOOL-01..05 unmodified).
    - `grep -qF "*Last updated: 2026-05-10 — Phase 27.1 import:" .planning/REQUIREMENTS.md` exits 0.
  </acceptance_criteria>
  <done>REQUIREMENTS.md contains the new `### Hexapod Integration` section with 16 HXP- definitions, the Active-scope table has the new row + updated summary count, the Traceability table has 16 new rows, and the existing Phase 25 TOOL-01..05 content is byte-identical to pre-edit.</done>
</task>

<task type="auto">
  <name>Task 2: Replace ROADMAP.md Phase 27.1 stub (lines 800–809) with real meta-phase entry + 3 sub-phase entries (27.1.1/.2/.3)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 795–815 — confirm exact pre-edit state of Phase 27 entry footer + the 27.1 stub at 800–809 + the Phase 28 heading at 810; line numbers may shift slightly across revisions, so anchor the replacement on the headings, not the line numbers)
    - .planning/ROADMAP.md (lines 246–323 — Phase 21.8 / 21.8.1 / 21.8.2 / 21.8.3 / 21.8.3.1 sub-phase analog block to mirror the format)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-PATTERNS.md (lines 109–204 — exact replacement block specification)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-RESEARCH.md (lines 90–149 — Insertion Point Precision; lines 191–211 — Goal/Depends/Success Criteria for each sub-phase)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-CONTEXT.md `<decisions>` section (D-01 — sub-phase numbering; D-02b — paths already correct; Pitfall 3 — dependency chain)
    - .planning/REQUIREMENTS.md (just-modified, to confirm HXP-IDs the new ROADMAP entries cite are now defined)
  </read_first>
  <action>
Replace ROADMAP.md lines 800–809 (the entire current Phase 27.1 stub, ending with the blank line before `### Phase 28:`) with the block below. Use the Edit tool: target the precise 10-line span starting with `### Phase 27.1: Import Free_Hexapod gsd planning (INSERTED)` and ending immediately before `### Phase 28: Skills Trust Tiers`.

Do NOT touch line 798 (`**Phase directory:** \`.planning/phases/27-prompt-caching/\``) or any line above. Do NOT touch line 810 (`### Phase 28: Skills Trust Tiers`) or any line below.

**Replacement block** (everything between the BEGIN and END markers — markers are NOT part of the output):

```
========= BEGIN REPLACEMENT =========
### Phase 27.1: Import Free_Hexapod gsd planning (INSERTED)

**Goal:** Import GSD planning artifacts from `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/` into IronHermes' canonical `.planning/` so the IronHermes × Hexapod Robot Integration work can be planned and executed under this project's GSD tracking. Delivers: (1) 16 HXP-prefixed requirements in REQUIREMENTS.md under a new `### Hexapod Integration` section, (2) three sub-phase ROADMAP entries (27.1.1/.2/.3), (3) verbatim copy of the locked Phase 1 CONTEXT.md to `phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md` with HXP- ID rewrites applied, (4) FROZEN.md redirect in the source repo. Meta-phase only — no code written.
**Requirements:** (none — D-01..D-04 from 27.1-CONTEXT.md are the requirements set)
**Depends on:** Phase 27
**Plans:** TBD

Plans:
- [ ] TBD (run /gsd-plan-phase 27.1 to break down)

**Phase directory:** `.planning/phases/27.1-import-free-hexapod-gsd-planning/`

### Phase 27.1.1: Safe Foundation (INSERTED)

**Goal:** Agent can issue walk, stop, read_battery, read_distance, and relax_servos commands to the Freenove hexapod robot over TCP — with every unsafe command blocked before any bytes reach the hardware.
**Requirements:** HXP-TOOL-01, HXP-TOOL-02, HXP-TOOL-03, HXP-TOOL-04, HXP-TOOL-05, HXP-TOOL-06, HXP-LOCO-01, HXP-LOCO-02, HXP-LOCO-03, HXP-LOCO-04, HXP-LOCO-05
**Depends on:** Phase 27.1
**Success Criteria** (what must be TRUE):
  1. Hexapod physically walks forward, backward, left, right on command
  2. Agent can halt the robot and return it to neutral stance
  3. Battery voltage string returned (e.g., "Battery: 7.2V / 8.1V (OK)")
  4. Ultrasonic distance value returned in centimeters
  5. Robot automatically halts and relaxes servos on IronHermes session end
**Plans:** TBD

Plans:
- [ ] TBD (run /gsd-plan-phase 27.1.1 to break down)

**Phase directory:** `.planning/phases/27.1.1-safe-foundation/`

### Phase 27.1.2: Navigation (INSERTED)

**Goal:** Agent can rotate in place, aim the head, and turn the buzzer on/off.
**Requirements:** HXP-NAV-01, HXP-NAV-03, HXP-NAV-04
**Depends on:** Phase 27.1.1
**Success Criteria** (what must be TRUE):
  1. Robot rotates clockwise and counterclockwise by specified degrees
  2. Head pan and tilt move independently within safe servo ranges
  3. Buzzer turns on and off on command (audible confirmation)
**Plans:** TBD

Plans:
- [ ] TBD (run /gsd-plan-phase 27.1.2 to break down)

**Phase directory:** `.planning/phases/27.1.2-navigation/`

### Phase 27.1.3: Expression + Skill Doc (INSERTED)

**Goal:** Agent can set LED colors for visual signaling; complete skill doc gives the agent full protocol knowledge without extra instructions.
**Requirements:** HXP-NAV-02, HXP-DOC-01
**Depends on:** Phase 27.1.2
**Success Criteria** (what must be TRUE):
  1. All LEDs change to specified RGB color on command; turn off on command
  2. `skills/hexapod/DESCRIPTION.md` is present and contains all CMD_* wire formats, action list, and blocked commands
  3. Agent can invoke any Phase 1–3 protocol action using the skill doc alone (no inline prompt guidance needed)
**Plans:** TBD

Plans:
- [ ] TBD (run /gsd-plan-phase 27.1.3 to break down)

**Phase directory:** `.planning/phases/27.1.3-expression-skill-doc/`

========= END REPLACEMENT =========
```

After the END marker is the existing `### Phase 28: Skills Trust Tiers` line which MUST remain unchanged.

**HARD CONSTRAINTS:**
- Use Edit tool with the entire 10-line stub as `old_string` (the literal lines 800–809 plus the trailing blank line that precedes `### Phase 28`). Do NOT use sed, awk, or any global rewrite.
- The four new entries are H3 (`### `) — never H4 (PATTERNS.md §"ROADMAP entry heading levels").
- Each entry has the `(INSERTED)` tag (PATTERNS.md §`(INSERTED)` tag convention).
- Each entry has its `**Phase directory:**` line (PATTERNS.md §"Phase directory line").
- Dependency chain: 27.1 → Phase 27, 27.1.1 → Phase 27.1, 27.1.2 → Phase 27.1.1, 27.1.3 → Phase 27.1.2 (Pitfall 3).
- Phase 27 (line ~787, Prompt Caching) and Phase 28 (post-replacement line, Skills Trust Tiers) are NOT modified (D-01a).
  </action>
  <verify>
    <automated>bash -c '
set -e
cd /Users/twilson/code/ironhermes
# 1) Phase 27.1 stub goal text gone
! grep -qF "[Urgent work - to be planned]" .planning/ROADMAP.md
# 2) Exactly 4 INSERTED phase headings for 27.1 family (parent + 3 children)
test "$(grep -cE "^### Phase 27\.1(\.[123])?: .* \(INSERTED\)$" .planning/ROADMAP.md)" = "4"
# 3) Each of the three children present with its full title
grep -qF "### Phase 27.1.1: Safe Foundation (INSERTED)" .planning/ROADMAP.md
grep -qF "### Phase 27.1.2: Navigation (INSERTED)" .planning/ROADMAP.md
grep -qF "### Phase 27.1.3: Expression + Skill Doc (INSERTED)" .planning/ROADMAP.md
# 4) Dependency chain anchored correctly
grep -A2 "^### Phase 27\.1: " .planning/ROADMAP.md | grep -qF "**Depends on:** Phase 27" && true
grep -A4 "^### Phase 27\.1\.1: " .planning/ROADMAP.md | grep -qF "**Depends on:** Phase 27.1"
grep -A4 "^### Phase 27\.1\.2: " .planning/ROADMAP.md | grep -qF "**Depends on:** Phase 27.1.1"
grep -A4 "^### Phase 27\.1\.3: " .planning/ROADMAP.md | grep -qF "**Depends on:** Phase 27.1.2"
# 5) Each child cites HXP- requirement IDs
grep -A2 "^### Phase 27\.1\.1: " .planning/ROADMAP.md | grep -qF "HXP-TOOL-01, HXP-TOOL-02, HXP-TOOL-03, HXP-TOOL-04, HXP-TOOL-05, HXP-TOOL-06, HXP-LOCO-01, HXP-LOCO-02, HXP-LOCO-03, HXP-LOCO-04, HXP-LOCO-05"
grep -A2 "^### Phase 27\.1\.2: " .planning/ROADMAP.md | grep -qF "HXP-NAV-01, HXP-NAV-03, HXP-NAV-04"
grep -A2 "^### Phase 27\.1\.3: " .planning/ROADMAP.md | grep -qF "HXP-NAV-02, HXP-DOC-01"
# 6) Phase directories declared for all four
grep -qF "**Phase directory:** \`.planning/phases/27.1-import-free-hexapod-gsd-planning/\`" .planning/ROADMAP.md
grep -qF "**Phase directory:** \`.planning/phases/27.1.1-safe-foundation/\`" .planning/ROADMAP.md
grep -qF "**Phase directory:** \`.planning/phases/27.1.2-navigation/\`" .planning/ROADMAP.md
grep -qF "**Phase directory:** \`.planning/phases/27.1.3-expression-skill-doc/\`" .planning/ROADMAP.md
# 7) Phase 27 + Phase 28 untouched (heading bytes survive)
grep -qF "### Phase 27: Prompt Caching" .planning/ROADMAP.md
grep -qF "### Phase 28: Skills Trust Tiers" .planning/ROADMAP.md
# 8) No bare TOOL-NN/LOCO-NN/NAV-NN/DOC-NN cited in the new sub-phase Requirements lines (D-03)
# Extract the Requirements line of each new sub-phase and confirm zero un-prefixed IDs there.
for phase in 27.1.1 27.1.2 27.1.3; do
  line=$(grep -A2 "^### Phase ${phase}: " .planning/ROADMAP.md | grep "^\*\*Requirements:\*\*" || true)
  echo "$line" | grep -qE "(^|[^-])\b(TOOL|LOCO|NAV|DOC)-[0-9]+" && {
    echo "FAIL: bare ID in Requirements line for ${phase}: $line"; exit 1;
  } || true
done
echo "ROADMAP.md edits OK"
'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF "[Urgent work - to be planned]" .planning/ROADMAP.md` exits NON-ZERO (the placeholder is gone).
    - `grep -cE "^### Phase 27\.1(\.[123])?: .* \(INSERTED\)$" .planning/ROADMAP.md` returns exactly 4.
    - All three sub-phase headings present verbatim: `### Phase 27.1.1: Safe Foundation (INSERTED)`, `### Phase 27.1.2: Navigation (INSERTED)`, `### Phase 27.1.3: Expression + Skill Doc (INSERTED)`.
    - Dependency chain (verified by 2-line lookahead from each heading): 27.1 → Phase 27, 27.1.1 → Phase 27.1, 27.1.2 → Phase 27.1.1, 27.1.3 → Phase 27.1.2.
    - Each child sub-phase's `**Requirements:**` line contains only HXP-prefixed IDs (no un-prefixed `TOOL-NN`, `LOCO-NN`, `NAV-NN`, `DOC-NN`).
    - All four `**Phase directory:**` lines present pointing to expected directories (one currently exists: `.planning/phases/27.1-import-free-hexapod-gsd-planning/`; three will be created lazily on `/gsd-plan-phase 27.1.x`).
    - `### Phase 27: Prompt Caching` and `### Phase 28: Skills Trust Tiers` headings remain present (Phase 27/28 byte-stable).
  </acceptance_criteria>
  <done>ROADMAP.md no longer has the `[Urgent work - to be planned]` stub. Four `(INSERTED)` Phase 27.1 family entries (parent + 3 children) are present with correct dependency chain, HXP- requirement citations, and phase directory declarations. Phase 27 and Phase 28 entries are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner → Markdown writer | Planning-time author embeds exact ID strings the executor must write verbatim — no untrusted input crosses |
| IronHermes `.planning/` ↔ Hexapod source repo | This plan touches ONLY IronHermes `.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md`; source repo is read-only here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.1-01 | Tampering | REQUIREMENTS.md `### Tool Registry` section (Phase 25 TOOL-01..05) | mitigate | Acceptance criteria #7 + #8 grep-assert that exactly 5 un-prefixed `TOOL-0[1-5]` definitions and 5 `Phase 25` traceability rows survive byte-identical. Action text forbids global find-replace and uses Edit tool with explicit old_string/new_string targeting. |
| T-27.1-02 | Tampering | ROADMAP.md Phase 27 (Prompt Caching) entry | mitigate | Action restricts replacement scope to lines 800–809; Phase 27 ends at line 798 (`**Phase directory:**`). Acceptance criterion grep-asserts `### Phase 27: Prompt Caching` heading survives. |
| T-27.1-03 | (none) | All other code/config | accept | Markdown-only writes; no executable code, no secrets, no network, no auth surface. Severity: none. |
</threat_model>

<verification>
After both tasks complete, run a combined sanity sweep:

```bash
cd /Users/twilson/code/ironhermes
# REQUIREMENTS surface
grep -c "^### Hexapod Integration$" .planning/REQUIREMENTS.md      # 1
grep -cE "^- \[ \] \*\*HXP-" .planning/REQUIREMENTS.md              # 16
grep -cE "^\| HXP-" .planning/REQUIREMENTS.md                       # 16 (traceability rows)
# ROADMAP surface
grep -cE "^### Phase 27\.1(\.[123])?:" .planning/ROADMAP.md         # 4
grep -cF "HXP-" .planning/ROADMAP.md                                # 17 (1 in 27.1 narrative + 11 in 27.1.1 + 3 in 27.1.2 + 2 in 27.1.3 — at minimum >= 16)
# Negative — no bare IDs newly introduced
! grep -E "^\*\*Requirements:\*\* (TOOL|LOCO|NAV|DOC)-" .planning/ROADMAP.md
```
</verification>

<success_criteria>
1. REQUIREMENTS.md `### Hexapod Integration` section exists with all 16 HXP- definitions (T-01..06, L-01..05, N-01..04, D-01).
2. REQUIREMENTS.md Active scope table has the new Hexapod Integration row and the summary line shows `(50 reqs across 9 categories)`.
3. REQUIREMENTS.md Traceability table has 16 new HXP- rows mapping to Phases 27.1.1/.2/.3 with `Pending` status.
4. REQUIREMENTS.md existing `### Tool Registry` (Phase 25 TOOL-01..05) section is byte-identical to pre-edit state.
5. ROADMAP.md Phase 27.1 stub at lines 800–809 is replaced with one meta-phase entry plus three sub-phase entries (27.1.1, 27.1.2, 27.1.3).
6. ROADMAP.md sub-phase entries have correct dependency chain (27.1.1 → 27.1, 27.1.2 → 27.1.1, 27.1.3 → 27.1.2).
7. ROADMAP.md Phase 27 (Prompt Caching) and Phase 28 (Skills Trust Tiers) entries are byte-identical to pre-edit state.
8. No bare TOOL-NN/LOCO-NN/NAV-NN/DOC-NN IDs are introduced anywhere in either file by this plan (HXP- prefix on every imported ID).
</success_criteria>

<output>
After completion, create `.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-01-SUMMARY.md` describing:
- The two files modified (REQUIREMENTS.md, ROADMAP.md)
- The 5 edits to REQUIREMENTS.md (active-scope summary, active-scope row, section, traceability rows, footer date)
- The 1 replacement to ROADMAP.md (10-line stub → 4 INSERTED entries)
- Confirmation that Phase 25 TOOL-01..05 content is unmodified
- Confirmation that all sub-phase Requirements lines reference HXP- IDs only
</output>
