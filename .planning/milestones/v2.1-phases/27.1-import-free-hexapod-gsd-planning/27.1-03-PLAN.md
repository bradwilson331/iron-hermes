---
phase: 27.1
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - ~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/FROZEN.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-04: A new file `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/FROZEN.md` exists, marking the source repo's `.planning/` as frozen with a redirect notice to IronHermes."
    - "D-04 (content shape): FROZEN.md contains all five required elements per CONTEXT.md `<specifics>` (D-04 + Claude's Discretion bullet 1): import date `2026-05-10`, target repo path `~/code/ironhermes`, target phase reference `Phase 27.1.x` (or its sub-phase names), explicit do-not-edit warning, brief explanation of why both repos exist."
    - "D-04 (non-deletion guard): No source repo file under `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/` is deleted by this plan; only the new FROZEN.md is added."
    - "D-04 (no-modify guard): The source repo's PROJECT.md, ROADMAP.md, REQUIREMENTS.md, and any phase CONTEXT.md are NOT modified by this plan (D-04 explicit prohibition)."
    - "Claude's Discretion bullet 2 (recommend YES): FROZEN.md is committed inside the Hexapod repo so the freeze is git-recorded in that repo's history (manual checkpoint task)."
  artifacts:
    - path: "~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/FROZEN.md"
      provides: "Redirect notice from the now-frozen source repo to IronHermes' canonical Phase 27.1.x planning surface"
      contains: "Target repository:** `~/code/ironhermes`"
  key_links:
    - from: "~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/FROZEN.md"
      to: ".planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md (in IronHermes — written by Plan 02)"
      via: "Explicit pointer paragraph at end of FROZEN.md naming the IronHermes location of the locked Phase 1 decisions"
      pattern: "27\\.1\\.1-safe-foundation/27\\.1\\.1-CONTEXT\\.md"
---

<objective>
Close the import by writing FROZEN.md inside the source Hexapod repo, then have the operator git-commit it inside that repo (the only manual step in this phase).

**Purpose:** Per D-04 the source `.planning/` is left intact but marked frozen with a redirect notice; the freeze is recorded in the source repo's git history (Claude's Discretion bullet 2: recommend commit). Plans 01 and 02 must finish first because the FROZEN.md notice points downstream at the now-existing IronHermes phase tree.

**Output:**
- New file in source repo: `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/FROZEN.md`
- One commit in the source repo recording the freeze (manual checkpoint — touches a separate git repo)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-CONTEXT.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-RESEARCH.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-PATTERNS.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-01-SUMMARY.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write FROZEN.md inside the source Hexapod repo's .planning/ directory</name>
  <files>~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/FROZEN.md</files>
  <read_first>
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-PATTERNS.md (lines 296–333 — the synthesized FROZEN.md template + commit recommendation)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-RESEARCH.md (lines 292–312 — recommended FROZEN.md content shape)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-CONTEXT.md `<decisions>` (D-04 + D-04a + Claude's Discretion bullet 1 — exact wording requirements)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-CONTEXT.md `<specifics>` (the 5 required content elements: date, target repo, target phase, do-not-edit warning, why-both-repos-exist explanation)
  </read_first>
  <action>
**Step A — Confirm the parent directory exists** (the Hexapod repo's `.planning/` already exists):

```bash
test -d ~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning || {
  echo "FAIL: source repo .planning/ missing — abort"; exit 1;
}
```

**Step B — Write the file** using the Write tool at the absolute path `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/FROZEN.md` (resolve `~` to `/Users/twilson` if the Write tool requires absolute paths). The exact contents to write:

```markdown
# FROZEN — Planning Artifacts Imported to IronHermes

**Import date:** 2026-05-10
**Target repository:** `~/code/ironhermes`
**Target phase:** Phase 27.1.x (27.1.1 Safe Foundation / 27.1.2 Navigation / 27.1.3 Expression + Skill Doc)

## Do Not Edit

This `.planning/` directory is frozen as of 2026-05-10. All planning and implementation
work for the IronHermes × Hexapod Robot Integration continues in
`~/code/ironhermes/.planning/` under Phases 27.1.1, 27.1.2, and 27.1.3.

**Do not create or modify any planning files in this directory.**

This repository (`Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi`) remains the
read-only reference for the robot's hardware protocol and the Pi-side server code
(`Code/Server/`). It is not the canonical home for IronHermes agent planning.
The IronHermes agent is the software that controls this robot; its planning lives
in the IronHermes repo.

For the locked implementation decisions for Phase 1 (Safe Foundation), see:
`~/code/ironhermes/.planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md`
```

**Five required content elements** (per CONTEXT.md `<specifics>`) — confirm each is in the body above:
1. Import date: `2026-05-10` ✓
2. Target repo path: `~/code/ironhermes` ✓
3. Target phase reference: `Phase 27.1.x` (with sub-phase names) ✓
4. Explicit do-not-edit warning: `**Do not create or modify any planning files in this directory.**` ✓
5. Brief explanation of why both repos exist: the third paragraph ("This repository ... remains the read-only reference ... agent's planning lives in the IronHermes repo") ✓

**HARD CONSTRAINTS (D-04 + D-04a):**
- Do NOT delete any existing file under `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/`.
- Do NOT modify the source repo's PROJECT.md, ROADMAP.md, REQUIREMENTS.md, or any phase CONTEXT.md.
- Do NOT touch `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/CLAUDE.md` or any file outside `.planning/`.
- The only file added to the Hexapod repo is `.planning/FROZEN.md`.
  </action>
  <verify>
    <automated>bash -c '
set -e
F=~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/FROZEN.md
# 1) File exists
test -f "$F"
# 2) All 5 required content elements
grep -qF "**Import date:** 2026-05-10" "$F"
grep -qF "**Target repository:** \`~/code/ironhermes\`" "$F"
grep -qF "Phase 27.1.x" "$F"
grep -qF "**Do not create or modify any planning files in this directory.**" "$F"
grep -qF "read-only reference" "$F"
# 3) Heading present
grep -qF "# FROZEN — Planning Artifacts Imported to IronHermes" "$F"
# 4) Pointer to IronHermes phase tree
grep -qF "~/code/ironhermes/.planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md" "$F"
# 5) NEGATIVE: source repo PROJECT.md / ROADMAP.md / REQUIREMENTS.md UNTOUCHED
# Use git in the source repo to confirm only FROZEN.md is in the working-tree diff (no other modifications).
cd ~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi
modified=$(git status --porcelain -- .planning/ | awk "{ print \$2 }" | sort)
expected=".planning/FROZEN.md"
if [ "$modified" != "$expected" ]; then
  echo "FAIL: source repo working tree has unexpected modifications:"
  git status --porcelain -- .planning/
  exit 1
fi
echo "FROZEN.md write OK + source repo planning/ otherwise clean"
'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f ~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/FROZEN.md` exits 0.
    - File contains all five required content elements (import date `2026-05-10`, target repo `~/code/ironhermes`, target phase `Phase 27.1.x`, do-not-edit warning, why-both-repos explanation).
    - File contains pointer line `~/code/ironhermes/.planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md`.
    - `cd ~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi && git status --porcelain -- .planning/` lists ONLY `.planning/FROZEN.md` as modified — no other source repo file is touched.
    - PROJECT.md, ROADMAP.md, REQUIREMENTS.md, and all CONTEXT.md files inside the source repo are byte-identical to pre-edit (verified by absence from `git status` output).
  </acceptance_criteria>
  <done>FROZEN.md exists in the source repo with all five required content elements, points at the IronHermes phase tree, and is the only file added or modified in the source repo's `.planning/` working tree.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator commits FROZEN.md inside the Hexapod source repo (manual — separate git repo)</name>
  <what-built>
    Task 1 wrote `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/FROZEN.md` and verified the source repo working tree is otherwise clean. Per CONTEXT.md Claude's Discretion bullet 2 ("recommend commit so the freeze is git-recorded in that repo's history"), the freeze should now be committed inside the Hexapod repo. This is a checkpoint — not autonomous — because the Hexapod repo is a separate git repository outside the IronHermes CI/automation surface (per VALIDATION.md `## Manual-Only Verifications` — "Touches a separate git repo outside this repo's CI surface").
  </what-built>
  <how-to-verify>
    Run these commands in a terminal (the executor agent SHOULD attempt the commit autonomously since `git` is a CLI Claude can use; this checkpoint exists to let the operator confirm the commit landed in the right repo and to authorize any auth prompts that surface):

    ```bash
    cd ~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi
    git status -- .planning/
    # Expect: only .planning/FROZEN.md as untracked (or staged)
    git add .planning/FROZEN.md
    git commit -m "freeze: planning artifacts imported to ironhermes phase 27.1.x

    All Phase 27.1.x planning continues in ~/code/ironhermes/.planning/.
    See ~/code/ironhermes/.planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md
    for the locked implementation decisions for Phase 1 (Safe Foundation)."
    git log --oneline -1 -- .planning/FROZEN.md
    # Expect: a single commit hash + message containing "freeze: planning artifacts imported to ironhermes phase 27.1.x"
    ```

    **Verification steps:**
    1. `git log --oneline -1 -- .planning/FROZEN.md` returns a non-empty result.
    2. The commit message includes `freeze: planning artifacts imported to ironhermes phase 27.1.x`.
    3. `git status -- .planning/` is clean (no uncommitted changes) afterward.
    4. Operator visually confirms no IronHermes-side files were touched (this commit is inside the Hexapod repo only).

    **Do NOT push** the commit unless the operator explicitly requests it — the freeze is local-history.
  </how-to-verify>
  <resume-signal>
    Reply `committed` when the FROZEN.md commit is recorded in the Hexapod repo's local history. Reply `skip` if the operator chose not to commit (the file remains on disk; phase still completes, but the manual-verify row in VALIDATION.md `## Manual-Only Verifications` is unmet). Reply `failed: <reason>` if the commit could not be made (e.g., separate git author identity needed in that repo).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IronHermes execution context → Hexapod source repo file system | This plan writes one file (`FROZEN.md`) into a separate git repo; D-04 forbids deleting or modifying any other file there |
| IronHermes execution context → Hexapod source repo git history | The optional commit in Task 2 records a single freeze entry in the source repo's history; no push to remote is implied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.1-01 | Tampering | Source repo files OTHER than FROZEN.md (PROJECT.md, ROADMAP.md, REQUIREMENTS.md, CONTEXT.md, HANDOFF.json, STATE.md, research/, codebase/) | mitigate | Acceptance criterion #5 runs `git status --porcelain -- .planning/` in the source repo and asserts the working-tree change set equals exactly `.planning/FROZEN.md` (one file). Any other modification is a hard fail. Action text explicitly forbids touching CLAUDE.md or anything outside `.planning/`. |
| T-27.1-02 | Tampering | Source repo git history (commit scope) | mitigate | Task 2's commit message scopes the change explicitly to `freeze:` semantics; `git add` targets only `.planning/FROZEN.md` (not `git add -A`). Operator confirmation gate ensures no accidental cross-repo commit. |
| T-27.1-03 | (none) | Network / auth surface | accept | No network operations; no remote push; no secrets. Severity: none. |
</threat_model>

<verification>
After both tasks complete:

```bash
F=~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/FROZEN.md
test -f "$F"                                                                # 1
grep -qF "Phase 27.1.x" "$F"                                                # 1
cd ~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi
git log --oneline -1 -- .planning/FROZEN.md                                 # one commit (after Task 2)
git status --porcelain -- .planning/                                        # empty (clean working tree)
```
</verification>

<success_criteria>
1. `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/FROZEN.md` exists with all five required content elements.
2. The source repo's working tree shows ONLY FROZEN.md as a change (no other source-repo file is modified or deleted by this plan).
3. The commit recording the freeze is present in the Hexapod repo's local git history (Task 2 — manual checkpoint).
4. IronHermes-side artifacts from Plans 01 and 02 are not modified by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-03-SUMMARY.md` describing:
- The new file written in the source repo and its five content elements
- The commit (or skip rationale) in the Hexapod repo's local history
- Confirmation that no other source-repo file was modified
</output>
