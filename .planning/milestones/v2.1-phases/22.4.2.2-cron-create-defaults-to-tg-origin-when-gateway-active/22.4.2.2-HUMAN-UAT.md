---
status: approved
phase: 22.4.2.2-cron-create-defaults-to-tg-origin-when-gateway-active
source: [22.4.2.2-VERIFICATION.md]
started: 2026-04-26T18:50:00Z
updated: 2026-04-27T00:00:00Z
---

## Current Test

[approved by operator — test 1 confirmed live; tests 2–4 deferred]

## Tests

### 1. CLI single-whitelist smoke test
expected: `~/.ironhermes/config.yaml` has `gateway.platforms.telegram.enabled: true` and a single-entry `whitelist`. Run `hermes cron create --name 'tg-routed-test' --schedule '*/5 * * * *' --prompt 'echo hi'` (no `--deliver` flag). The new entry in `~/.ironhermes/cron/jobs.json` MUST have `deliver: "origin"` and `origin: { platform: "telegram", chat_id: "<your-whitelist-entry>", chat_name: null, thread_id: null }`.
result: passed (operator confirmed: asked the agent to create a cron job; it was created and delivered to Telegram)

### 2. CLI multi-whitelist fallback
expected: Temporarily add a second entry to `gateway.platforms.telegram.whitelist`. Run the same `hermes cron create` (no `--deliver`). Stderr MUST contain the literal substring `multiple authorized chats` and the actual whitelist values. The new job MUST have `deliver: "local"` and `origin: null`.
result: blocked (deferred by operator — phase approved without this test running live)

### 3. CLI explicit --deliver bypass
expected: With config still eligible for auto-routing (single-entry whitelist, gateway enabled), run `hermes cron create --deliver local --name 'explicit-local' --schedule '*/5 * * * *' --prompt 'echo hi'`. The new job MUST have `deliver: "local"` and `origin: null` — the explicit flag overrides auto-routing per D-04.
result: blocked (deferred by operator — phase approved without this test running live)

### 4. LLM tool path (cronjob tool create action)
expected: With single-entry whitelist + gateway enabled, start `hermes chat` and ask the agent: "Create a cron job that runs every hour and prints hello." When the agent fires the `cronjob` tool's `create` action without supplying `deliver`, the resulting job in `~/.ironhermes/cron/jobs.json` MUST have `deliver: "origin"` and a populated `origin` field matching the whitelist entry. Multi-chat fallback variant: temporarily widen the whitelist, observe `tracing::warn` in the gateway log (NOT in the chat transcript), verify the created job has `deliver: "local"` and `origin: null`.
result: blocked (deferred by operator — phase approved without this test running live)

## Summary

total: 4
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 3

## Gaps
