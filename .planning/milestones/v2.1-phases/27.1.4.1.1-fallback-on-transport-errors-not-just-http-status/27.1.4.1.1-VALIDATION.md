---
phase: 27.1.4.1.1
slug: fallback-on-transport-errors-not-just-http-status
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-12
---

# Phase 27.1.4.1.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in `#[test]` (cargo test) |
| **Config file** | none — standard `cargo test` |
| **Quick run command** | `cargo test -p ironhermes-agent test_classify` |
| **Full suite command** | `cargo test -p ironhermes-agent` |
| **Estimated runtime** | ~10–30 seconds (quick); full crate suite longer |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-agent test_classify`
- **After every plan wave:** Run `cargo test -p ironhermes-agent`
- **Before `/gsd-verify-work`:** Full suite must be green (incl. `invariants_27_1_4_1_1`)
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | 01 | 1 | PROV-07 | — / — | N/A | unit | `cargo test -p ironhermes-agent test_classify_transport` (connection refused → `(true,true)`) | ❌ W0 (new) | ⬜ pending |
| TBD | 01 | 1 | PROV-07 | — / — | N/A | unit | `cargo test -p ironhermes-agent test_classify_transport` (DNS failure → `(true,true)`) | ❌ W0 (new) | ⬜ pending |
| TBD | 01 | 1 | PROV-07 | — / — | N/A | unit | `cargo test -p ironhermes-agent test_classify_transport` (connect timeout → `(true,true)`) | ❌ W0 (new) | ⬜ pending |
| TBD | 01 | 1 | PROV-07 | — / — | N/A | unit | `cargo test -p ironhermes-agent test_classify_transport` (connection reset → `(true,true)`) | ❌ W0 (new) | ⬜ pending |
| TBD | 01 | 1 | PROV-07 | — / — | N/A | unit | `cargo test -p ironhermes-agent test_classify_transport` (`error sending request for url` → `(true,true)`) | ❌ W0 (new) | ⬜ pending |
| TBD | 01 | 1 | PROV-07 | — / — | N/A | unit | `cargo test -p ironhermes-agent test_classify` (generic non-transport error → `(true,false)`; repurposed `test_classify_other_error`) | ✅ exists (modified) | ⬜ pending |
| TBD | 01 | 1 | PROV-07 | — / — | N/A | static | `cargo test -p ironhermes-agent --test invariants_27_1_4_1_1` (helper fn name present in agent_loop.rs) | ❌ W0 (new) | ⬜ pending |
| TBD | 01 | 1 | PROV-07 | — / — | N/A | static | `cargo test -p ironhermes-agent --test invariants_27_1_4_1_1` (`error sending request for url` literal present) | ❌ W0 (new) | ⬜ pending |
| TBD | 01 | 1 | PROV-07 | — / — | N/A | unit | `cargo test -p ironhermes-agent test_classify` (existing `test_classify_429_error` / `_401_error` / `_500_error` / `_walks_anyhow_context_chain` / `_production_error_format` still pass) | ✅ exists | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] New transport-failure unit tests inside `crates/ironhermes-agent/src/agent_loop.rs` `mod tests` (co-located with `test_classify_*_error`)
- [ ] `crates/ironhermes-agent/tests/invariants_27_1_4_1_1.rs` — static-grep lock for PROV-07 (helper fn name + `error sending request for url` literal), modeled on `invariants_27_1_4_1.rs`
- [ ] Repurpose existing `test_classify_other_error` to feed a non-transport error string (must happen in the same commit as adding the transport helper, or that test goes red)

*No framework install needed — `cargo test` is already in use.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| End-to-end: local-Ollama primary (not running) + OpenRouter fallback → request switches to OpenRouter instead of erroring after 3 retries | PROV-07 | Requires a live OpenRouter key + a configured fallback chain + Ollama stopped; not reproducible as a pure unit test | 1. Configure primary = Ollama (`http://localhost:11434`), fallback = OpenRouter. 2. Ensure Ollama is NOT running. 3. Issue an agent request. 4. Confirm logs show fallback activation and a successful OpenRouter completion (not `MAX_RETRIES` exhaustion + error). |

*All unit-testable behaviors have automated verification; only the live multi-provider switch is manual.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
