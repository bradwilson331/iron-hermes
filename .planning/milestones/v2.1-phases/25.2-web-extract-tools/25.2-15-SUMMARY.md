---
phase: 25.2
plan: 15
subsystem: tools-toolset-session-handle-wireup
tags: [gap-closure, toolset-session-handle, repl, telegram, wireup, uat-issue-2]
gap_closure: true
depends_on: [25.2-14]
requires:
  - "Phase 25 Plan 04 ToolsetSessionHandle trait at crates/ironhermes-core/src/commands/context.rs:116-132"
  - "Phase 25 Plan 04 CommandContext::with_toolset_session builder at crates/ironhermes-core/src/commands/context.rs:430-433"
  - "Phase 25.2 register_web_extract_tool wiring (Plan 14 — already in run_chat/run_single/run_gateway)"
  - "tokio multi-thread runtime (already present via workspace tokio = full)"
provides:
  - "Production impl ToolsetSessionHandle for RegistryToolsetSession in ironhermes-tools"
  - "Live attachment of ToolsetSessionHandle to CommandContext at run_chat / run_single / run_gateway"
  - "build_cmd_ctx widened APPEND-ONLY with toolset_session: Option<Arc<dyn ToolsetSessionHandle>>"
  - "CLI toolset_members_map web entry now lists web_extract (3 members)"
  - "Two parity-guard regression tests (with_toolset_session_wired_in_all_three_sites + toolset_members_map_agrees_with_registry_toolset_session)"
affects:
  - "/toolset slash command (REPL + Telegram) — now returns populated table instead of 'toolset session handle not configured'"
  - "/toolset enable|disable — now mutates the live in-session ToolsConfig and pushes via set_toolset_config"
  - "hermes toolset show web — now lists 3 members (web_search, web_read, web_extract)"
tech-stack:
  added: []
  patterns:
    - "tokio::task::block_in_place + Handle::current().block_on for sync-trait → async-runtime bridge (mirrors Phase 22.4.2 MemoryManagerHandle.status_text)"
    - "Arc<TokioRwLock<ToolRegistry>> shared between mcp_manager + AgentLoop + RegistryToolsetSession (no second Arc layer)"
    - "Local copy of toolset_members_map in tools crate to avoid CLI→tools dep cycle (Option B chosen over moving the helper)"
    - "APPEND-ONLY signature widening on shared helpers — every existing parameter preserved verbatim, single new trailing parameter added"
key-files:
  created:
    - crates/ironhermes-tools/src/toolset_session.rs
    - .planning/phases/25.2-web-extract-tools/25.2-15-SUMMARY.md
  modified:
    - crates/ironhermes-tools/src/lib.rs
    - crates/ironhermes-cli/src/main.rs
    - crates/ironhermes-cli/src/toolset_cmd.rs
decisions:
  - "Option B over Option A: implement ToolsetSessionHandle directly in ironhermes-tools using ironhermes-core primitives + a local copy of toolset_members_map; do NOT move build_toolset_rows / build_toolset_show_view down from ironhermes-cli. Smaller diff, no test churn in toolset_cmd.rs (those tests still cover the CLI path's pure logic). Cross-crate parity locked by toolset_members_map_agrees_with_registry_toolset_session regression test."
  - "APPEND-ONLY widening of build_cmd_ctx: one new trailing parameter toolset_session: Option<Arc<dyn ToolsetSessionHandle>>, one new conditional .with_toolset_session(handle) chain at end of builder. Every existing .with_* call preserved verbatim — INV-21.7-08 lock-step parity preserved at both prompt-time and mid-turn dispatch sites."
  - "Reuse the existing Arc<RwLock<ToolRegistry>> from main.rs:737/1246/2257 — DO NOT introduce a second Arc layer. mcp_manager, AgentLoop, and the new RegistryToolsetSession all share the same registry instance."
  - "Local members_map copy in tools crate (toolset_session.rs) and CLI map (toolset_cmd.rs:242) BOTH list web_extract — kept in sync by toolset_members_map_agrees_with_registry_toolset_session structural test."
  - "Symptom 2 (LLM truncation at 'Here are█') flagged as out-of-scope — independent of handle wireup per the diagnoser's Eliminated section. Logged as a follow-up requiring runtime trace of finish_reason + delta.tool_calls in a separate /gsd-debug session."
metrics:
  duration_minutes: 5
  completed_date: 2026-05-02
  tasks_completed: 3
  files_changed: 4
  tests_added: 7
  commits: 3
---

# Phase 25.2 Plan 15: Toolset Session Handle Wireup (UAT Issue 2 / Symptom 1) Summary

Production wireup of `ToolsetSessionHandle` onto `CommandContext` at every live `AgentLoop` site (REPL / Telegram / single-shot) — closes the deferred Phase 25 Plan 04 gap that was triggering `"/toolset: toolset session handle not configured."` in UAT Test 2.

## Tasks Completed

| Task | Name                                                                                                  | Commit  | Files                                                                                            |
| ---- | ----------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------ |
| 1    | Implement RegistryToolsetSession + re-export from ironhermes-tools                                    | 37afb6e | crates/ironhermes-tools/src/toolset_session.rs (new), crates/ironhermes-tools/src/lib.rs         |
| 2    | Wire RegistryToolsetSession into build_cmd_ctx + all three CLI construction sites                     | 22b8098 | crates/ironhermes-cli/src/main.rs                                                                |
| 3    | Add web_extract to CLI toolset_members_map + cross-crate parity test                                  | 3dda29d | crates/ironhermes-cli/src/toolset_cmd.rs                                                         |

## Verification

**Automated:**
- `cargo build --workspace` — exits 0 (1m15s, 28 pre-existing warnings carried)
- `cargo check -p ironhermes-tools` — exits 0
- `cargo test -p ironhermes-tools --lib toolset_session::tests` — 5/5 pass:
  - `enable_then_disable_round_trip`
  - `render_list_returns_non_empty`
  - `render_show_reports_web_extract_in_web_toolset`
  - `validate_name_rejects_uppercase_and_dots`
  - `check_known_rejects_unknown_toolset`
- `cargo build -p ironhermes-cli` — exits 0
- `cargo test -p ironhermes-cli --bin ironhermes with_toolset_session_wired_in_all_three_sites` — 1/1 pass
- `cargo test -p ironhermes-cli --bin ironhermes register_web_extract_tool_wired_in_all_three_sites` — still passes (no regression)
- `cargo test -p ironhermes-cli --bin ironhermes toolset_cmd::tests` — 12/12 pass (10 pre-existing + 2 new)
  - new: `toolset_members_map_web_includes_web_extract`
  - new: `toolset_members_map_agrees_with_registry_toolset_session`

**Static-grep gates (post-edit, against non-comment source of main.rs):**
- `RegistryToolsetSession::new(` count: **4** (≥3 required — 3 production sites + 1 in test)
- `Some(toolset_session.clone())` count: **4** (≥2 required — 2 build_cmd_ctx call sites + 2 in test)
- `toolset_session: Option<Arc<dyn ironhermes_core::commands::context::ToolsetSessionHandle>>` parameter present at line 937
- `m.insert("web", &["web_search", "web_read", "web_extract"]);` present at toolset_cmd.rs:242

**Manual UAT replay path** (documented per plan; not executed in this autonomous run — the orchestrator's verification ceremony will cover REPL + Telegram round-trips):
1. `cargo run --bin ironhermes -- chat`
2. `/toolset` → expect populated table (NOT the fallback string), with `web` row showing 3 members
3. `/toolset show web` → expect web_search, web_read, web_extract listed
4. `/toolset enable browser` → expect "/toolset: enabled browser for this session." plus the schema-cache banner
5. Same flow over Telegram from the whitelisted chat

## Deviations from Plan

None — the plan was followed verbatim. Two minor execution notes:

- **B-3 audit (Cargo.toml rt-multi-thread fallback):** NOT triggered. Workspace declares `tokio = { version = "1", features = ["full"] }` and the tools crate uses `tokio = { workspace = true }`, so `block_in_place` resolves cleanly. No `Cargo.toml` change needed.
- **Pre-existing baseline carry-over:** `cargo clippy -p ironhermes-tools --lib -- -D warnings` reports 10 pre-existing errors in `ironhermes-core` (memory_store.rs `manual_is_multiple_of`, skills.rs `derivable_impls`, etc.). Confirmed identical errors exist on the pristine 165bffa baseline (verified via `git stash`). These are out-of-scope per the deviation rules (scope boundary — not introduced by this plan, not in the files this plan touches). Logged for a future workspace-wide clippy cleanup.

## Threat Model Coverage

| Threat ID            | Disposition | Mitigation Status                                                                                                                                                                                          |
| -------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-25.2-15-01         | mitigate    | `validate_name` enforces `^[a-z][a-z0-9_]{0,31}$`; `check_known` rejects names not in the local D-01 allow-list. Both gates run BEFORE any registry mutation. Tests `validate_name_rejects_uppercase_and_dots` + `check_known_rejects_unknown_toolset` pass. |
| T-25.2-15-02         | mitigate    | `RegistryToolsetSession` mutates ONLY the in-memory `Arc<Mutex<ToolsConfig>>` and pushes via `set_toolset_config`. Verified by grep: `grep -n 'config_setter\|config.yaml' crates/ironhermes-tools/src/toolset_session.rs` returns ZERO matches. |
| T-25.2-15-03         | accept      | Production REPL/Telegram run on multi-thread tokio runtime; tests use `flavor = "multi_thread"` explicitly. If a future caller switches to single-thread, `block_in_place` panics with a clear message — acceptable failure mode. |
| T-25.2-15-04         | accept      | `render_list` / `render_show` output reveals enabled/disabled toolset state (same surface as the existing CLI `hermes toolset list`). No api keys / secrets in the rendered output. |
| T-25.2-15-05         | mitigate    | `toolset_members_map_web_includes_web_extract` and `toolset_members_map_agrees_with_registry_toolset_session` regression tests lock structural agreement between the CLI map and the slash UI map. Future drift breaks the build. |
| T-25.2-15-symptom2   | transfer    | Symptom 2 (LLM truncation at "Here are█") is INDEPENDENT — confirming requires runtime trace of `finish_reason` + `delta.tool_calls`. Out of scope for this plan; logged in Next Phase Readiness below. |

## Next Phase Readiness

- **UAT Issue 2 / Symptom 1 (this plan): CLOSED** — `/toolset` will return a populated table in REPL + Telegram on next session. Manual UAT replay path documented above.
- **UAT Issue 2 / Symptom 2 (LLM truncation at "Here are█") follow-up:** independent of handle wireup. Run `/gsd-debug 'LLM truncation at Here are█ — capture finish_reason + delta.tool_calls trace'` to open a new diagnose-only session that adds runtime tracing to the streaming client, captures a real model response that truncates, and confirms the hypothesis (model emitting a tool_call mid-sentence; visible glyph is the Knight-Rider activity scanner from `crates/ironhermes-cli/src/tui/render.rs:917`, not a streaming caret).
- **UAT Issue 9 (secret-leak in agent's pre-emptive safety warning):** out of scope for this plan (Plan 16 territory). The fix lives at agent-prompt level (system prompt or tool-input pre-filter), not in the web_extract tool — Plan 04 sanitize pass never runs because the LLM intercepts the URL prompt-side and refuses without calling the tool.
- **CommandContext is now fully wired for /toolset slash UI** — no other live AgentLoop site is missing the handle. If a new entry point is added in the future (e.g., a stdio JSON-RPC adapter), the parity-guard test `with_toolset_session_wired_in_all_three_sites` will need to be tightened (≥3 → ≥4) along with adding the construction line.

## Self-Check: PASSED

- File `crates/ironhermes-tools/src/toolset_session.rs`: FOUND
- File `.planning/phases/25.2-web-extract-tools/25.2-15-SUMMARY.md`: FOUND (this file)
- Commit `37afb6e`: FOUND (`feat(25.2-15): add RegistryToolsetSession production ToolsetSessionHandle impl`)
- Commit `22b8098`: FOUND (`feat(25.2-15): wire RegistryToolsetSession into all three CLI entry points`)
- Commit `3dda29d`: FOUND (`fix(25.2-15): add web_extract to CLI toolset_members_map web entry`)
- All automated `<verify>` blocks: PASSED (cargo check, cargo build, cargo test, plan-required grep gates)
- 5 unit tests in `toolset_session::tests`: PASSED
- Parity guard `with_toolset_session_wired_in_all_three_sites`: PASSED
- Pre-existing parity guard `register_web_extract_tool_wired_in_all_three_sites`: STILL PASSES (no regression)
- 2 new toolset_cmd::tests regression tests: PASSED
