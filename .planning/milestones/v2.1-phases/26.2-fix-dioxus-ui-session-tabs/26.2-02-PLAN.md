---
phase: 26.2
plan: 02
type: execute
wave: 2
depends_on: [26.2-01]
files_modified:
  - crates/iron_hermes_ui/src/state.rs
  - crates/iron_hermes_ui/src/components/shell/title_bar.rs
  - crates/iron_hermes_ui/src/components/warp_hermes.rs
  - crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
tags:
  - dioxus
  - tab-strip
  - session-management
  - ui-interactivity
must_haves:
  truths:
    - "D-01: Clicking a tab sets active_tab to the clicked index, writes that tab's session_id into the session_id signal, and clears blocks + messages signals"
    - "D-02: Tab clicks are no-ops when scanner_active is true; the .wh-tabs container shows pointer-events: none and opacity: 0.5 during streaming"
    - "D-03: Clicking + calls create_session() via spawn(async), pushes a Tab { label: 'New Session', live: true, session_id: <id> }, sets active_tab to the new last index, sets session_id, clears blocks + messages"
    - "D-04: New tab placeholder label is exactly 'New Session' — no UUID display, no prompt"
    - "D-05: Clicking the close (×) button calls evt.stop_propagation() before invoking on_tab_close — the parent tab's onclick does NOT fire; session removed from UI only, persists in StateStore"
    - "D-06: Closing a tab removes it from the tabs signal; if the closed tab was active, active_tab adjusts; session_id updated to the new active tab's session_id; blocks + messages cleared; closing last tab triggers create_session()"
    - "D-07: Tabs live in Signal<Vec<Tab>> seeded by a use_effect watching sessions(); direct signal mutation — no server re-fetch for create/close"
    - "D-08: Mount use_effect is extended so the create_session() success arm pushes a Tab for the newly created session and sets active_tab; guarded by already_present check to prevent double-seed"
    - "D-09: TitleBar component prop signature has exactly: tabs, active_tab, show_traffic_lights, disabled, on_tab_click, on_tab_close, on_tab_new (4 data + 3 EventHandler props)"
    - "Tab struct in state.rs has session_id: String field; demo_tabs() fixtures provide demo-0/demo-1/demo-2 session_ids; the inline `let tabs: Vec<Tab>` derivation in warp_hermes.rs is replaced with a Signal<Vec<Tab>> seeded by use_effect on sessions()"
  artifacts:
    - path: "crates/iron_hermes_ui/src/state.rs"
      provides: "Tab.session_id field; updated demo_tabs() fixture"
      contains: "pub session_id: String"
    - path: "crates/iron_hermes_ui/src/components/shell/title_bar.rs"
      provides: "TitleBar with EventHandler props, stop_propagation close button, disabled state"
      contains: "on_tab_click: EventHandler<usize>"
    - path: "crates/iron_hermes_ui/src/components/warp_hermes.rs"
      provides: "tabs signal, on_tab_click/new/close closures, mount-effect with D-08 push, updated TitleBar call site"
      contains: "let mut tabs = use_signal(Vec::<Tab>::new);"
    - path: "crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs"
      provides: "3 new source-text tests covering tab callbacks and stop_propagation"
      contains: "fn tab_close_uses_stop_propagation"
  key_links:
    - from: "TitleBar"
      to: "WarpHermes"
      via: "EventHandler<usize> / EventHandler<()> props"
      pattern: "on_tab_click:|on_tab_close:|on_tab_new:"
    - from: "warp_hermes.rs on_tab_new closure"
      to: "server::api::create_session()"
      via: "spawn(async move { ... .await })"
      pattern: "create_session\\(\\)\\.await"
    - from: "warp_hermes.rs Signal<Vec<Tab>>"
      to: "use_server_future(list_sessions)"
      via: "use_effect watching sessions() — seeds tabs once on resolve"
      pattern: "tabs\\.write\\(\\)\\.push"
    - from: "title_bar.rs close button"
      to: "parent tab div onclick"
      via: "evt.stop_propagation() prevents bubble"
      pattern: "evt\\.stop_propagation\\(\\)"
---

<objective>
Make the TitleBar session tab strip fully interactive: click switches the active session, "+" creates a new session, "×" closes a tab (auto-creating a replacement when the last is closed), and the tab list stays in sync with session lifecycle. Also extend the Tab struct with a session_id field so click handlers can resolve which session to switch to.

Purpose: Phase 26.1 closed the WebSocket lifecycle. Phase 26.2 closes the UI gap so users can actually use multiple sessions through the Dioxus UI. Without this, the tab strip is a static decoration.

Output: D-01..D-09 implemented across `state.rs`, `title_bar.rs`, `warp_hermes.rs`; 3 new source-text regression tests in `websocket_lifecycle_parity.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.2-fix-dioxus-ui-session-tabs/26.2-CONTEXT.md
@.planning/phases/26.2-fix-dioxus-ui-session-tabs/26.2-RESEARCH.md
@.planning/phases/26.2-fix-dioxus-ui-session-tabs/26.2-PATTERNS.md
@.planning/phases/26.2-fix-dioxus-ui-session-tabs/26.2-UI-SPEC.md
@.planning/phases/26.2-fix-dioxus-ui-session-tabs/26.2-VALIDATION.md
@.planning/phases/26.2-fix-dioxus-ui-session-tabs/26.2-01-SUMMARY.md

## Verified contracts

### Tab struct (state.rs lines 148-152)
- Currently: `pub struct Tab { pub label: String, pub live: bool }` with derives `Clone, PartialEq, Debug`
- demo_tabs() at lines 526-541 — 3 entries with only label + live

### TitleBar (title_bar.rs — 67 lines total, current signature)
- Current: `pub fn TitleBar(tabs: Vec<Tab>, active_tab: usize, show_traffic_lights: bool) -> Element`
- Renders `.wh-titlebar`, `.wh-tabs` container, per-tab `div` with key, `wh-tab-dot` span, label, and `×` span; `+` button with no onclick
- Imports: `use super::sigil::Sigil; use crate::state::Tab; use dioxus::prelude::*;`

### WarpHermes (warp_hermes.rs — verified lines)
- Line 50-52: `let mut input/blocks/messages = use_signal(...)`
- Line 53-55: TWO `#[allow(unused_mut)]` (one removed by Plan 01 Task 1)
- Line 61: `let active_tab = use_signal(|| 0_usize);` — needs `let mut`
- Line 69: `let mut session_id = use_signal(|| "pending".to_string());`
- Lines 72-84: existing `use_effect` calling `create_session()` and only setting `session_id` — must extend to push Tab (D-08)
- Line 338: `let sessions = use_server_future(move || crate::server::api::list_sessions())?;`
- Lines 354-366: inline `let tabs: Vec<Tab> = match sessions() { ... }` — REMOVE; replace with Signal seeded by use_effect
- Line 689: `TitleBar { tabs: tabs, active_tab: active_tab(), show_traffic_lights: true }` — REPLACE with 7-prop call
- Line 87: `let mut streaming_block_id = use_signal(|| Option::<u64>::None);`
- Line 59: `let mut scanner_active = use_signal(|| false);` — read in on_tab_click for D-02 gate

### Server API (server/api.rs — used as-is, no changes)
- `pub async fn create_session() -> Result<String, ServerFnError>` — String is the session key
- `pub async fn list_sessions() -> Result<Vec<SessionInfo>, ServerFnError>`
- SessionInfo: `pub id: String`, `pub title: Option<String>`

### EventHandler analog patterns (existing components)
- block_stream.rs:18-19 — single `on_rerun: EventHandler<u64>`
- input_box.rs:20-26 — multi-prop with `on_submit: EventHandler<()>`
- command_palette.rs:22-29 — `on_pick: EventHandler<PaletteItem>`

### clippy.toml rule (enforced)
- NEVER hold `Signal::read()` / `Signal::write()` / `GenerationalRef` / `WriteLock` across `.await` points
- Always extract values into owned locals before any `.await` or before reading a different signal that might be borrowed
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Tab struct + rewrite TitleBar with EventHandler props and stop_propagation close button</name>
  <files>
    - crates/iron_hermes_ui/src/state.rs
    - crates/iron_hermes_ui/src/components/shell/title_bar.rs
  </files>
  <read_first>
    - crates/iron_hermes_ui/src/state.rs (current Tab at lines 148-152, demo_tabs at lines 526-541)
    - crates/iron_hermes_ui/src/components/shell/title_bar.rs (full 67-line file)
    - crates/iron_hermes_ui/src/components/shell/block_stream.rs (lines 18-19 — EventHandler analog)
    - crates/iron_hermes_ui/src/components/shell/input_box.rs (lines 20-26 — multi-prop EventHandler analog)
    - .planning/phases/26.2-fix-dioxus-ui-session-tabs/26.2-PATTERNS.md (state.rs + title_bar.rs sections)
    - .planning/phases/26.2-fix-dioxus-ui-session-tabs/26.2-UI-SPEC.md (Interaction Contract + CSS Rules)
    - .planning/phases/26.2-fix-dioxus-ui-session-tabs/26.2-CONTEXT.md (D-09 prop signature)
  </read_first>
  <behavior>
    - `Tab` struct gains `pub session_id: String` field; derives unchanged (Clone, PartialEq, Debug)
    - `demo_tabs()` returns 3 tabs each with `session_id` "demo-0", "demo-1", "demo-2"
    - `TitleBar` prop signature is exactly `(tabs: Vec<Tab>, active_tab: usize, show_traffic_lights: bool, disabled: bool, on_tab_click: EventHandler<usize>, on_tab_close: EventHandler<usize>, on_tab_new: EventHandler<()>)` in this order
    - `.wh-tabs` container has `style: if disabled { "pointer-events: none; opacity: 0.5;" } else { "" }`
    - Each tab `div` has `onclick: move |_| on_tab_click.call(i)` (i from for-loop)
    - The `×` close element is a `button` (not span); onclick calls `evt.stop_propagation()` then `on_tab_close.call(i)`; style preserves existing color/margin/font but adds `background: none; border: none; cursor: pointer;`
    - The `+` button gains `onclick: move |_| on_tab_new.call(())` — preserve existing style verbatim
    - Module compiles standalone; warp_hermes.rs call-site mismatch is expected and is fixed in Tasks 2 and 3 (same commit batch)
  </behavior>
  <action>
Step A — In `crates/iron_hermes_ui/src/state.rs` lines 148-152, locate the existing `pub struct Tab` definition and add a third public field `session_id: String` while preserving the `#[derive(Clone, PartialEq, Debug)]` and the doc comment. The final struct must contain exactly these three fields in this order: `label: String`, `live: bool`, `session_id: String`. Add a doc comment on `session_id` noting it is used by `WarpHermes::on_tab_click` to switch the active session per Phase 26.2 D-09.

Step B — In the same file, update `demo_tabs()` (lines 526-541). The function currently builds a `vec!` of 3 `Tab` values, each missing the new field. For each entry, add `session_id: "demo-N".to_string()` where N is the entry's zero-based index (0 for the first entry, 1 for the second, 2 for the third). Preserve all existing `label` and `live` values verbatim — do not change them.

Step C — REPLACE `crates/iron_hermes_ui/src/components/shell/title_bar.rs` ENTIRELY. The new file must:
  1. Keep the existing 3 imports (`use super::sigil::Sigil; use crate::state::Tab; use dioxus::prelude::*;`)
  2. Keep both `#[allow(dead_code)] const` declarations for WORDMARK_SVG and IH_SHIELD_PNG verbatim
  3. Keep the existing doc comment on TitleBar but APPEND a new paragraph: "Phase 26.2 D-09: gains three EventHandler callbacks for interactive tabs (click to switch, close to remove, new to create) and a `disabled` prop that greys out the tab strip during streaming (D-02)."
  4. Replace the prop signature with the multi-line 7-arg form (see &lt;behavior&gt; for exact prop names and types). Indentation: 4-space, opening paren on same line as `fn TitleBar(`, each prop on its own line, closing `) -> Element {` on its own line.
  5. Inside the rsx block, the `.wh-titlebar` div, traffic-light cluster, brand block, and `.wh-titlebar-actions` div remain BYTE-EQUIVALENT to the current source (do not change traffic-light hex codes, gap values, or `⌘K` text)
  6. The `.wh-tabs` container div must add a second attribute after `class: "wh-tabs"`: `style: if disabled { "pointer-events: none; opacity: 0.5;" } else { "" }`
  7. Inside the `for (i, t) in tabs.iter().enumerate()` block, the per-tab `div` must add `onclick: move |_| on_tab_click.call(i),` immediately after the two `class:` attributes
  8. The wh-tab-dot span and `"{t.label}"` text node remain unchanged
  9. The previously-bare `span { ... "×" }` close element must be REPLACED with a `button` element. The button's onclick handler is `move |evt| { evt.stop_propagation(); on_tab_close.call(i); }`. The button's style attribute must be the literal string `"color: var(--fg-disabled); margin-left: 4px; font-size: 11px; background: none; border: none; cursor: pointer;"` (preserves existing visual + adds button-reset declarations). The button's child is the literal `"×"` (Unicode U+00D7, NOT ASCII x).
  10. The trailing `+` `button` must add `onclick: move |_| on_tab_new.call(()),` as its first attribute, preserving the existing style attribute verbatim (`"padding: 0 10px; color: var(--fg-dim); font-size: 14px; font-weight: 700; background: none; border: none; cursor: default;"`) and the literal child `"+"`.

No new imports are needed — `EventHandler`, `Asset`, `asset!`, `Element`, `rsx!` all come from `dioxus::prelude::*`.

After this task, `cargo build -p iron_hermes_ui` will fail with a prop-count mismatch on warp_hermes.rs:689 (3 props vs 7-prop component). That mismatch is fixed in Tasks 2 + 3 — keep the three tasks in a single commit chain so the workspace returns to green at the end of the wave.
  </action>
  <verify>
    <automated>cargo check -p iron_hermes_ui --lib 2>&amp;1 | tail -25 ; grep -c 'pub session_id: String' crates/iron_hermes_ui/src/state.rs ; grep -c 'on_tab_click: EventHandler' crates/iron_hermes_ui/src/components/shell/title_bar.rs ; grep -c 'evt.stop_propagation' crates/iron_hermes_ui/src/components/shell/title_bar.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub session_id: String' crates/iron_hermes_ui/src/state.rs` returns 1
    - `grep -c 'session_id: "demo-' crates/iron_hermes_ui/src/state.rs` returns 3 (one per demo tab entry)
    - `grep -c 'on_tab_click: EventHandler<usize>' crates/iron_hermes_ui/src/components/shell/title_bar.rs` returns 1
    - `grep -c 'on_tab_close: EventHandler<usize>' crates/iron_hermes_ui/src/components/shell/title_bar.rs` returns 1
    - `grep -c 'on_tab_new: EventHandler<()>' crates/iron_hermes_ui/src/components/shell/title_bar.rs` returns 1
    - `grep -c 'disabled: bool' crates/iron_hermes_ui/src/components/shell/title_bar.rs` returns 1
    - `grep -c 'evt.stop_propagation()' crates/iron_hermes_ui/src/components/shell/title_bar.rs` returns 1
    - `grep -c 'pointer-events: none; opacity: 0.5' crates/iron_hermes_ui/src/components/shell/title_bar.rs` returns 1
    - `grep -c 'on_tab_new.call(())' crates/iron_hermes_ui/src/components/shell/title_bar.rs` returns 1
    - `grep -c 'on_tab_click.call(i)' crates/iron_hermes_ui/src/components/shell/title_bar.rs` returns 1
    - `grep -c 'on_tab_close.call(i)' crates/iron_hermes_ui/src/components/shell/title_bar.rs` returns 1
    - The `×` element is a `button` (not span): `awk '/"×"/{found=1; for(i=1;i<=NR;i++);} END{exit !found}' crates/iron_hermes_ui/src/components/shell/title_bar.rs` and `grep -B5 '"×"' crates/iron_hermes_ui/src/components/shell/title_bar.rs | grep -c 'button {'` returns at least 1
    - Cargo check on the lib MAY error only at warp_hermes.rs:689 (prop count). Other compile errors are blocking.
  </acceptance_criteria>
  <done>Tab struct extended; demo_tabs updated; TitleBar rewritten with 7 props and stop_propagation close button. The only expected compile error is the warp_hermes.rs:689 call-site mismatch — fixed in Tasks 2/3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace inline tabs derivation with Signal<Vec<Tab>>; add 3 callback closures and use_effect seeders in WarpHermes</name>
  <files>crates/iron_hermes_ui/src/components/warp_hermes.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/components/warp_hermes.rs (lines 50-90, 258-340, 354-366, 472-503, 506-590, 600-700)
    - crates/iron_hermes_ui/src/state.rs (post-Task-1 — Tab now has session_id)
    - crates/iron_hermes_ui/src/server/api.rs (create_session + list_sessions signatures)
    - .planning/phases/26.2-fix-dioxus-ui-session-tabs/26.2-PATTERNS.md (warp_hermes.rs section — closures + WR-01 site already fixed by Plan 01)
    - .planning/phases/26.2-fix-dioxus-ui-session-tabs/26.2-CONTEXT.md (D-01..D-08)
    - .planning/phases/26.2-fix-dioxus-ui-session-tabs/26.2-UI-SPEC.md (Interaction Contract section)
    - .planning/phases/26.2-fix-dioxus-ui-session-tabs/26.2-RESEARCH.md (Pitfalls 1-4 — signal borrow safety, last-tab guard, index clamp, double-seed)
  </read_first>
  <behavior>
    - `let active_tab = use_signal(|| 0_usize);` at line 61 becomes `let mut active_tab = use_signal(|| 0_usize);`
    - A new signal `let mut tabs = use_signal(Vec::<Tab>::new);` is declared immediately after the session_id signal at line 69
    - The existing inline `let tabs: Vec<Tab> = match sessions() { ... }` block at lines 354-366 is REMOVED entirely (the local `tabs` shadowing-binding is gone — `tabs` now refers exclusively to the `Signal<Vec<Tab>>`)
    - A `use_effect` watching `sessions()` seeds the `tabs` signal once when `list_sessions()` resolves (D-07): on `Some(Ok(list))`, push each `Tab { label: s.title.unwrap_or(s.id.clone()), live: true, session_id: s.id }` ONLY IF the session_id is not already present in `tabs.read()`
    - The existing mount `use_effect` (lines 72-84) is EXTENDED so the success arm pushes a `Tab { label: "New Session".to_string(), live: true, session_id: sid.clone() }` to `tabs` (D-08), guarded by `already_present` check, then sets `active_tab` to the new last index, and sets `session_id` (preserving existing behavior). The error arm is unchanged.
    - Three closures are defined inside `WarpHermes`, AFTER the existing `on_rerun` handler (~line 503) and BEFORE the rsx block:
      * `on_tab_click: |idx: usize| -> ()`: early-return if `scanner_active()` is true (D-02); else extract session_id from `tabs.read().get(idx)` into an owned String, drop the read guard, set active_tab, set session_id, `blocks.set(Vec::new())`, `messages.write().clear()`
      * `on_tab_new: |_: ()| -> ()`: spawn(async move { ... }) calling `create_session().await`. On success: build new Tab, push, drop write guard, read len for new_idx, set active_tab, set session_id, clear blocks, clear messages. On error: log via `web_sys::console::log_1` under `#[cfg(target_arch = "wasm32")]` and `let _ = e;`
      * `on_tab_close: |idx: usize| -> ()`: `tabs.write().remove(idx)` (drop write guard), then check `tabs.read().is_empty()`. If empty, invoke the same logic as `on_tab_new` (spawn create_session and append). If not empty, compute the new active index per UI-SPEC: if `idx < cur` then `cur - 1`; if `idx == cur` then `cur.saturating_sub(1).min(tabs.read().len() - 1)`; if `idx > cur` then `cur` (unchanged but clamped). Set active_tab; extract session_id from `tabs.read().get(new_idx)`; set session_id; clear blocks; clear messages.
    - The TitleBar call site at line 689 changes from 3 props to a 7-prop call: `tabs: tabs()`, `active_tab: active_tab()`, `show_traffic_lights: true`, `disabled: scanner_active()`, `on_tab_click: move |idx| on_tab_click(idx)`, `on_tab_close: move |idx| on_tab_close(idx)`, `on_tab_new: move |_| on_tab_new(())`
    - All signal borrow safety rules from clippy.toml are honored: no `.read()` or `.write()` guard held across an `.await`, no `.read()` on the same signal as a live `.write()` guard
  </behavior>
  <action>
Apply the changes below in order. After each step, the next step's preconditions must hold (e.g., the `tabs` signal must exist before the seeder use_effect references it).

Step A — Mutability fix at line 61. Change `let active_tab = use_signal(|| 0_usize);` to `let mut active_tab = use_signal(|| 0_usize);`.

Step B — Add the new tabs signal. Immediately after line 69 (`let mut session_id = use_signal(|| "pending".to_string());`), insert: `let mut tabs = use_signal(Vec::<Tab>::new);` (this becomes the new line 70). Confirm `Tab` is in scope by checking the existing `use crate::state::Tab;` import at the top of the file — if absent, add it to the imports block alongside other state imports.

Step C — Extend the existing mount use_effect at lines 72-84 (after Step B insertion this is now lines 73-85). The current `Ok(sid) => session_id.set(sid)` arm becomes a multi-statement block that:
  1. Clones sid into `session_id.set(sid.clone())`
  2. Reads `tabs.read()` to compute `let already_present = tabs.read().iter().any(|t| t.session_id == sid);` — drop the read guard before the next step
  3. If `!already_present`: `tabs.write().push(Tab { label: "New Session".to_string(), live: true, session_id: sid });` then drop the write guard
  4. After the push: read `tabs.read().len() - 1` into `let new_idx = tabs.read().len() - 1;` and call `active_tab.set(new_idx);`

Critical signal borrow rule: `already_present` must be computed in a separate scope (use a block `{ tabs.read().iter().any(...) }`) so the read guard drops before the subsequent write. The `len()` read after the push is also in its own scope. See RESEARCH.md Pitfall 1.

The error arm of the mount effect remains unchanged.

Step D — Add a NEW use_effect AFTER the mount effect (insert immediately following the closing `});` of the mount effect block) that seeds `tabs` from `sessions()` per D-07. Body: read `sessions()`; if `Some(Ok(list))`, iterate each `SessionInfo s`; for each, check if `s.id` is already in `tabs.read()` (separate scope); if not, push `Tab { label: s.title.clone().unwrap_or_else(|| s.id.clone()), live: true, session_id: s.id.clone() }` (use a separate scope for the write). Skip if `sessions()` returns None or Err.

Step E — DELETE the inline `let tabs: Vec<Tab> = match sessions() { ... }` block at original lines 354-366 entirely. After this deletion, the local `tabs` name will resolve to the Signal declared in Step B (no shadowing). The `palette_items` derivation immediately above (lines 341-352) and the `(model_name, provider_name)` derivation immediately below (lines 368-371) are unchanged.

Step F — Add three closure bindings after the existing `on_rerun` closure (~line 503) and before the rsx block. Each closure captures `tabs`, `active_tab`, `session_id`, `blocks`, `messages`, and (for on_tab_click) `scanner_active` — all signals are `Copy`, so capture-by-move works. The closures must be declared in this order:

  1. `let on_tab_click = move |idx: usize| { ... };` — D-01 + D-02. Body:
     - `if scanner_active() { return; }`
     - Extract: `let sid = { let ts = tabs.read(); ts.get(idx).map(|t| t.session_id.clone()) };`
     - `if let Some(sid) = sid { active_tab.set(idx); session_id.set(sid); blocks.set(Vec::new()); messages.write().clear(); }`

  2. `let on_tab_new = move |_: ()| { ... };` — D-03 + D-04. Body wraps `spawn(async move { ... })`. Inside the async block:
     - `match crate::server::api::create_session().await {`
     - `  Ok(sid) => {`
     - `    let new_tab = Tab { label: "New Session".to_string(), live: true, session_id: sid.clone() };`
     - `    tabs.write().push(new_tab);`
     - `    let new_idx = tabs.read().len() - 1;`
     - `    active_tab.set(new_idx);`
     - `    session_id.set(sid);`
     - `    blocks.set(Vec::new());`
     - `    messages.write().clear();`
     - `  }`
     - `  Err(e) => {`
     - `    #[cfg(target_arch = "wasm32")]`
     - `    web_sys::console::log_1(&format!("New tab session creation failed: {e}").into());`
     - `    let _ = e;`
     - `  }`
     - `}`

  3. `let on_tab_close = move |idx: usize| { ... };` — D-05 + D-06. Body:
     - `tabs.write().remove(idx);`
     - `let now_empty = tabs.read().is_empty();`
     - `if now_empty {`
     - `  spawn(async move { /* same body as on_tab_new success arm */ });` — copy the create_session-and-push logic verbatim. Do not call `on_tab_new` directly (closures cannot easily call sibling closures without FnMut juggling); inline the body.
     - `} else {`
     - `  let cur = active_tab();`
     - `  let len = tabs.read().len();`
     - `  let new_idx = if idx < cur { cur - 1 } else if idx == cur { cur.saturating_sub(1).min(len - 1) } else { cur.min(len - 1) };`
     - `  active_tab.set(new_idx);`
     - `  let sid = { let ts = tabs.read(); ts.get(new_idx).map(|t| t.session_id.clone()) };`
     - `  if let Some(sid) = sid { session_id.set(sid); }`
     - `  blocks.set(Vec::new());`
     - `  messages.write().clear();`
     - `}`

For all three closures: every `tabs.read()` block must be in its own `{ ... }` scope so the read guard drops before any subsequent `tabs.write()` or sibling-signal mutation.

Step G — Update the TitleBar call site at the original line 689. Replace `TitleBar { tabs: tabs, active_tab: active_tab(), show_traffic_lights: true }` with the 7-prop multi-line form:

  - Line 1: `TitleBar {`
  - Line 2: `    tabs: tabs(),`
  - Line 3: `    active_tab: active_tab(),`
  - Line 4: `    show_traffic_lights: true,`
  - Line 5: `    disabled: scanner_active(),`
  - Line 6: `    on_tab_click: move |idx| on_tab_click(idx),`
  - Line 7: `    on_tab_close: move |idx| on_tab_close(idx),`
  - Line 8: `    on_tab_new: move |_| on_tab_new(()),`
  - Line 9: `}`

Indentation matches the surrounding rsx block.

Step H — If web_sys is not already imported at the file top, the existing `#[cfg(target_arch = "wasm32")] web_sys::console::log_1(...)` calls at lines 78-81 prove it is in scope. Reuse without adding new imports.

Do NOT modify the Plan 01 WR-01 / IN-01 fixes already landed in this file. Do NOT add new use statements unless `Tab` is somehow not in scope (verify before adding).
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui 2>&amp;1 | tail -25 ; cargo clippy -p iron_hermes_ui --lib --no-deps 2>&amp;1 | tail -20 ; grep -c 'let mut active_tab' crates/iron_hermes_ui/src/components/warp_hermes.rs ; grep -c 'let mut tabs = use_signal' crates/iron_hermes_ui/src/components/warp_hermes.rs ; grep -c 'on_tab_click' crates/iron_hermes_ui/src/components/warp_hermes.rs ; grep -c 'on_tab_new' crates/iron_hermes_ui/src/components/warp_hermes.rs ; grep -c 'on_tab_close' crates/iron_hermes_ui/src/components/warp_hermes.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'let mut active_tab = use_signal' crates/iron_hermes_ui/src/components/warp_hermes.rs` returns 1
    - `grep -c 'let mut tabs = use_signal(Vec::<Tab>::new);' crates/iron_hermes_ui/src/components/warp_hermes.rs` returns 1
    - `grep -c 'let tabs: Vec<Tab> = match sessions()' crates/iron_hermes_ui/src/components/warp_hermes.rs` returns 0 (old inline derivation removed)
    - `grep -c 'let on_tab_click = move |idx: usize|' crates/iron_hermes_ui/src/components/warp_hermes.rs` returns 1
    - `grep -c 'let on_tab_new = move |_: ()|' crates/iron_hermes_ui/src/components/warp_hermes.rs` returns 1
    - `grep -c 'let on_tab_close = move |idx: usize|' crates/iron_hermes_ui/src/components/warp_hermes.rs` returns 1
    - `grep -c 'create_session().await' crates/iron_hermes_ui/src/components/warp_hermes.rs` returns at least 2 (mount effect + on_tab_new + last-tab close branch — at least 2 distinct call sites)
    - `grep -c 'scanner_active()' crates/iron_hermes_ui/src/components/warp_hermes.rs` returns at least 2 (existing read sites + on_tab_click guard + TitleBar disabled prop)
    - `grep -c '"New Session".to_string()' crates/iron_hermes_ui/src/components/warp_hermes.rs` returns at least 2 (mount D-08 push + on_tab_new push + last-tab close push)
    - `grep -c 'show_traffic_lights: true,' crates/iron_hermes_ui/src/components/warp_hermes.rs` returns 1 (TitleBar call site updated)
    - `grep -c 'disabled: scanner_active()' crates/iron_hermes_ui/src/components/warp_hermes.rs` returns 1
    - `cargo build -p iron_hermes_ui` exits 0 (workspace returns to green after Tasks 1-2 land together)
    - `cargo clippy -p iron_hermes_ui --lib --no-deps -- -D warnings` exits 0 (no signal-borrow-across-await violations)
  </acceptance_criteria>
  <done>WarpHermes has the tabs Signal, all 3 callbacks, the D-07/D-08 seeders, and the updated TitleBar call site. Workspace builds cleanly. Clippy is silent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add 3 source-text regression tests for tab callbacks and stop_propagation</name>
  <files>crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs</files>
  <read_first>
    - crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs (post-Plan-01 state — already has 8 tests including busy_gate_opportunistically_clears_finished_turn)
    - crates/iron_hermes_ui/src/components/warp_hermes.rs (post-Task-2 state — to confirm the source strings the new tests will assert on)
    - crates/iron_hermes_ui/src/components/shell/title_bar.rs (post-Task-1 state — to confirm stop_propagation site)
    - .planning/phases/26.2-fix-dioxus-ui-session-tabs/26.2-PATTERNS.md (websocket_lifecycle_parity.rs section — 3 new tests blueprint)
    - .planning/phases/26.2-fix-dioxus-ui-session-tabs/26.2-VALIDATION.md (Wave 0 requirements list)
  </read_first>
  <behavior>
    - Three new test functions are appended to `tests/websocket_lifecycle_parity.rs`, each using the existing `read()` helper
    - `tab_click_clears_blocks_and_switches_session_id` reads `src/components/warp_hermes.rs` and asserts: `on_tab_click` closure exists, `scanner_active()` + `return;` D-02 guard pattern is present, `blocks.set(Vec::new())` is called somewhere in the file, `messages.write().clear()` is called
    - `tab_new_calls_create_session_and_appends_tab` reads warp_hermes.rs and asserts: `on_tab_new` closure exists, `create_session().await` appears at least once, `"New Session".to_string()` literal appears at least once, `tabs.write().push` appears at least once
    - `tab_close_uses_stop_propagation` reads `src/components/shell/title_bar.rs` and asserts: `evt.stop_propagation()` appears, all 3 EventHandler prop names appear (`on_tab_click`, `on_tab_close`, `on_tab_new`), `disabled: bool` prop appears, the `pointer-events: none; opacity: 0.5` style appears
    - All 8 pre-existing tests still pass; total test count is now 11
  </behavior>
  <action>
APPEND the three test functions below to the END of `crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs`. Place them after the last existing test (`busy_gate_opportunistically_clears_finished_turn` from Plan 01 Task 3), separated by one blank line each. Use the existing `read()` helper — do not add new helpers or imports.

Test 1:
```
#[test]
fn tab_click_clears_blocks_and_switches_session_id() {
    let ui = read("src/components/warp_hermes.rs");
    assert!(
        ui.contains("let on_tab_click = move |idx: usize|"),
        "WarpHermes must define on_tab_click closure with usize idx (D-09)"
    );
    assert!(
        ui.contains("scanner_active()") && ui.contains("return;"),
        "on_tab_click must guard against tab switch during streaming (D-02): expect scanner_active() check + early return"
    );
    assert!(
        ui.contains("blocks.set(Vec::new())"),
        "tab switch must clear blocks signal (D-01)"
    );
    assert!(
        ui.contains("messages.write().clear()"),
        "tab switch must clear messages signal (D-01)"
    );
}
```

Test 2:
```
#[test]
fn tab_new_calls_create_session_and_appends_tab() {
    let ui = read("src/components/warp_hermes.rs");
    assert!(
        ui.contains("let on_tab_new = move |_: ()|"),
        "WarpHermes must define on_tab_new closure (D-09)"
    );
    assert!(
        ui.contains("create_session().await"),
        "on_tab_new must call the create_session server function (D-03)"
    );
    assert!(
        ui.contains("\"New Session\".to_string()"),
        "new tab must use \"New Session\" placeholder label (D-04)"
    );
    assert!(
        ui.contains("tabs.write().push"),
        "on_tab_new must push the new Tab onto the tabs signal (D-03)"
    );
}
```

Test 3:
```
#[test]
fn tab_close_uses_stop_propagation() {
    let tb = read("src/components/shell/title_bar.rs");
    assert!(
        tb.contains("evt.stop_propagation()"),
        "close button must call evt.stop_propagation() to prevent tab click bubbling (CONTEXT Specifics + UI-SPEC Interaction Contract)"
    );
    assert!(
        tb.contains("on_tab_click: EventHandler<usize>"),
        "TitleBar must declare on_tab_click EventHandler<usize> prop (D-09)"
    );
    assert!(
        tb.contains("on_tab_close: EventHandler<usize>"),
        "TitleBar must declare on_tab_close EventHandler<usize> prop (D-09)"
    );
    assert!(
        tb.contains("on_tab_new: EventHandler<()>"),
        "TitleBar must declare on_tab_new EventHandler<()> prop (D-09)"
    );
    assert!(
        tb.contains("disabled: bool"),
        "TitleBar must accept a disabled: bool prop for D-02 streaming gate"
    );
    assert!(
        tb.contains("pointer-events: none; opacity: 0.5"),
        "disabled state must apply pointer-events: none + opacity: 0.5 to .wh-tabs (UI-SPEC Disabled State)"
    );
}
```

Do NOT modify any existing tests or helpers. The new tests use only the existing `read()` helper from the top of the file.
  </action>
  <verify>
    <automated>cargo test -p iron_hermes_ui --test websocket_lifecycle_parity 2>&amp;1 | tail -25 ; cargo test -p iron_hermes_ui 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fn tab_click_clears_blocks_and_switches_session_id' crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs` returns 1
    - `grep -c 'fn tab_new_calls_create_session_and_appends_tab' crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs` returns 1
    - `grep -c 'fn tab_close_uses_stop_propagation' crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs` returns 1
    - `cargo test -p iron_hermes_ui --test websocket_lifecycle_parity 2>&amp;1 | grep -E 'test result:.*ok\\. [0-9]+ passed'` shows count >= 11 (8 from Plan 01 wave + 3 from this task)
    - `cargo test -p iron_hermes_ui --test websocket_lifecycle_parity 2>&amp;1 | grep -c ' FAILED'` returns 0
    - `cargo test -p iron_hermes_ui` exits 0 (full crate suite green)
  </acceptance_criteria>
  <done>3 new regression tests added, all pass. Total parity test count is 11. Full iron_hermes_ui test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser WASM ↔ create_session/list_sessions | Server functions own session ID generation (uuid v4) and StateStore writes; client only consumes returned strings |
| Tab close button ↔ parent tab onclick | DOM event bubbling boundary; stop_propagation is the contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.2-07 | Spoofing | session_id signal | accept | session_id values flow only from server (create_session / list_sessions); client never authors session IDs. Tab.session_id is populated from server-returned strings — see RESEARCH.md Security Domain section ("Session IDs generated server-side via uuid::Uuid::new_v4()") |
| T-26.2-08 | Tampering | tab strip state | accept | Tab signal is local UI state with no persistence beyond the WASM session; closing the browser discards it. No cross-user influence possible — single-tenant Dioxus app |
| T-26.2-09 | Repudiation | n/a | accept | No audit trail required for tab interactions — purely UI affordance |
| T-26.2-10 | Information Disclosure | tab labels | accept | "New Session" placeholder is constant; session.title from list_sessions is server-controlled and already exposed in the API (Phase 25.5 D-04) |
| T-26.2-11 | Denial of Service | rapid tab churn | mitigate | D-02 disables tab clicks during streaming so a user cannot trigger create_session loops while a turn is in flight; the ws.rs busy-gate from Plan 01 Task 2 protects the underlying request channel |
| T-26.2-12 | Elevation of Privilege | n/a | accept | No role/permission model added; tab strip respects the same single-operator scope as the rest of the Dioxus shell |
| T-26.2-13 | Tampering (regression) | tab callback wiring | mitigate | 3 new source-text tests in Task 3 lock the closure shapes, EventHandler prop signatures, and stop_propagation site against silent regression |
</threat_model>

<verification>
After Plan 02 completes:
- `cargo build -p iron_hermes_ui` exits 0
- `cargo clippy -p iron_hermes_ui --lib --no-deps -- -D warnings` exits 0
- `cargo test -p iron_hermes_ui --test websocket_lifecycle_parity` exits 0 with at least 11 passing tests
- `cargo test -p iron_hermes_ui` exits 0
- All MUST-HAVE truths observable in source via grep gates documented per task
- Manual verification (per VALIDATION.md Manual-Only Verifications) deferred to `/gsd-verify-work` UAT step:
  * Tab click visually switches active highlight
  * Streaming greys the tab strip at 0.5 opacity
  * "+" creates a new tab labeled "New Session"
  * "×" removes a tab and auto-switches to adjacent tab
</verification>

<success_criteria>
- Tab struct has session_id field; demo_tabs() updated; no demo regressions
- TitleBar prop signature has 4 data props + 3 EventHandler props (4+3=7 total)
- WarpHermes has tabs Signal seeded by both list_sessions effect (D-07) and mount create_session effect (D-08, idempotent via already_present check)
- 3 callback closures wired into TitleBar call site; signal borrow safety preserved
- Closing the last tab triggers create_session — tabs vec is never empty in the steady state
- 3 new regression tests pass; total parity test count is 11; full iron_hermes_ui suite green
</success_criteria>

<output>
After completion, create `.planning/phases/26.2-fix-dioxus-ui-session-tabs/26.2-02-SUMMARY.md`.
</output>
