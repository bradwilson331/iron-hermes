---
phase: 26.4
slug: web-ui-side-tabs-panel
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-05
---

# Phase 26.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | No Dioxus UI component test framework — cargo test covers server layer only |
| **Config file** | none |
| **Quick run command** | `cargo test -p iron_hermes_ui` |
| **Full suite command** | `cargo test -p iron_hermes_ui` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p iron_hermes_ui`
- **After every plan wave:** Run `cargo test -p iron_hermes_ui` + visual check via `dx serve`
- **Before `/gsd-verify-work`:** Full cargo test suite must be green + visual inspection of tab strip
- **Max feedback latency:** 30 seconds (compile + test)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 26.4-01-01 | 01 | 1 | D-05 | — | N/A | manual | `dx serve` — verify `.wh-side-tabs` strip renders between `.wh-side-head` and content | ✅ | ⬜ pending |
| 26.4-01-02 | 01 | 1 | D-01/D-02/D-03 | — | N/A | unit + manual | `cargo test -p iron_hermes_ui` compiles clean; visual: AGENT tab shows messages, INFO tab shows cards | ✅ | ⬜ pending |
| 26.4-01-03 | 01 | 1 | D-06 | — | N/A | manual | `dx serve` — active tab has 2px bottom `var(--accent-primary)` border; inactive tab has none | ✅ | ⬜ pending |
| 26.4-01-04 | 01 | 1 | D-09 | — | N/A | manual | Switch TitleBar session tab → side tab selection preserved (does not reset to 0) | ✅ | ⬜ pending |
| 26.4-01-05 | 01 | 1 | D-07 | — | N/A | manual | INFO tab Config card shows real model/provider/context values from live config | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. No new test files needed — this is a pure UI phase (RSX + CSS) with no testable server-side logic beyond what's already covered by existing integration tests.

*No Wave 0 file creation required.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Tab strip renders (AGENT + INFO) below `.wh-side-head` | D-01, D-04 | No Dioxus component test framework | `dx serve` → open web UI → verify two-tab strip below the HERMES header |
| Clicking INFO tab switches content area | D-02, D-03 | Requires browser rendering | Click INFO → verify Session + Config cards appear; click AGENT → verify message list returns |
| Active tab indicator (2px bottom border, cyan) | D-06 | Visual CSS verification | Inspect active tab — bottom border present; no top border (TitleBar uses top, side uses bottom) |
| `active_side_tab` persists across session tab switches | D-09 | Integration behavior requiring live UI | Switch TitleBar session → side panel tab selection unchanged |
| INFO card values populated from live signals | D-07 | Requires live session state | Start a chat turn → verify token counts update in INFO tab |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
