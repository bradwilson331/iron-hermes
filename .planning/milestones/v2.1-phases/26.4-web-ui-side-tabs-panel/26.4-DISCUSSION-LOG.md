# Phase 26.4: web-ui-side-tabs-panel - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-05
**Phase:** 26.4-web-ui-side-tabs-panel
**Areas discussed:** What views the tabs show, Tab strip placement & style, Non-Agent tab content, Tab state scope

---

## What Views the Tabs Show

| Option | Description | Selected |
|--------|-------------|----------|
| Agent + Memory + Context (3 tabs) | Agent, Memory (MEMORY.md), Context (context files) | |
| Agent + Info (2 tabs, minimal) | Agent + Info showing session metadata and config | ✓ |
| Agent + Sessions + Memory (3 tabs) | Agent, Sessions, Memory | |
| You decide | Claude's discretion | |

**User's choice:** Agent + Info (2 tabs, minimal)

### Info tab content follow-up

| Option | Description | Selected |
|--------|-------------|----------|
| Session stats only | id, start time, message count, token used/max | |
| Model + provider + config snapshot | Model name, provider, context window, config flags | |
| Both session stats + config snapshot | Full picture: session lifecycle + agent config | ✓ |
| You decide | Claude's discretion | |

**User's choice:** Both — Session card + Config card

---

## Tab Strip Placement & Style

| Option | Description | Selected |
|--------|-------------|----------|
| Replace .wh-side-head with tab row | Header replaced by Agent/Info strip | |
| Tabs below the existing header | Keep header, add tab strip below it | ✓ |
| Match TitleBar tab style exactly | Reuse .wh-tab / .wh-tabs CSS | |
| You decide | Claude's discretion | |

**User's choice:** Tabs below existing header (Sigil + HERMES + personality preserved)

### CSS approach follow-up

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse .wh-tab / .wh-tabs (Phase 26.2) | Visual consistency, no new CSS | |
| New .wh-side-tab CSS classes | Tuned for side panel width | ✓ |
| You decide | Claude's discretion | |

**User's choice:** New .wh-side-tab CSS classes

### Active tab indicator follow-up

| Option | Description | Selected |
|--------|-------------|----------|
| Bottom border accent (var(--accent-primary)) | 2px cyan bottom border | ✓ |
| Background fill (var(--w-bg-2)) | Lighter background on active tab | |
| You decide | Claude's discretion | |

**User's choice:** 2px bottom border in var(--accent-primary)

---

## Non-Agent Tab Content

| Option | Description | Selected |
|--------|-------------|----------|
| Props from WarpHermes only (no new server functions) | Use existing signals | ✓ |
| New get_session_info() server function | Richer server-side data | |
| You decide | Claude's discretion | |

**User's choice:** Props from WarpHermes only

### Info tab render follow-up

| Option | Description | Selected |
|--------|-------------|----------|
| Key-value rows (label: value) | Monospace grid | |
| Styled info cards (grouped sections) | Two card sections: Session + Config | ✓ |
| You decide | Claude's discretion | |

**User's choice:** Styled info cards (grouped sections)

---

## Tab State Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Global (one active_side_tab signal) | App-wide, TitleBar session tabs don't affect it | ✓ |
| Per-session (each session remembers its tab) | More complex, requires per-session map | |

**User's choice:** Global — one `active_side_tab: Signal<usize>` in WarpHermes

---

## Claude's Discretion

- Exact `.wh-side-tab` CSS values (padding, font-size, height)
- Tab label casing ("Agent"/"Info" vs "AGENT"/"INFO")
- Info card section header styling
- Whether personality pill stays in `.wh-side-head` (recommended: yes, keep it there)
- `AgentPanel` prop signature — which new props to add

## Deferred Ideas

- Memory tab (MEMORY.md / USER.md contents) — would need new server function
- Sessions history tab — would need list_sessions in side panel
- Per-session side tab state
- Context files tab
