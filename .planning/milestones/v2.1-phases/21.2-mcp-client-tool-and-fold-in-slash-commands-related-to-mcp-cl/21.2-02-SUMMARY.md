---
phase: 21.2-mcp-client-tool-and-fold-in-slash-commands-related-to-mcp-cl
plan: 02
subsystem: tool-registry
tags: [mcp, rwlock, tool-registry, refactor, d-10]
dependency_graph:
  requires: [21.2-01]
  provides: [Arc<RwLock<ToolRegistry>> migration, register_dynamic, unregister_by_prefix]
  affects: [ironhermes-tools, ironhermes-agent, ironhermes-gateway, ironhermes-cli]
tech_stack:
  added: [tokio::sync::RwLock across workspace]
  patterns: [Arc<RwLock<T>> shared mutable state, read guard per-call dispatch]
key_files:
  created: []
  modified:
    - crates/ironhermes-tools/src/registry.rs
    - crates/ironhermes-agent/src/agent_loop.rs
    - crates/ironhermes-agent/src/agent_wiring.rs
    - crates/ironhermes-agent/src/subagent_runner.rs
    - crates/ironhermes-gateway/src/runner.rs
    - crates/ironhermes-gateway/src/handler.rs
    - crates/ironhermes-cli/src/main.rs
    - crates/ironhermes-cli/src/batch/runner.rs
    - crates/ironhermes-tools/src/delegate_task.rs
decisions:
  - "rpc_registry in execute_code.rs preserved as Arc<ToolRegistry> (no RwLock) — read-only safe subset per RESEARCH.md Pitfall 3 and D-10"
  - "guardrail_error_detail() returns &ErrorDetailLevel reference; cloned before dropping read guard to avoid holding guard across await"
  - "batch/runner.rs filters::run_filters takes &ToolRegistry — acquire read guard, deref to inner, drop before next await"
  - "tokio::sync::RwLock used throughout (not std::sync) for async compatibility"
metrics:
  duration: "8 minutes"
  completed: "2026-04-22"
  tasks: 2
  files: 9
---

# Phase 21.2 Plan 02: ToolRegistry RwLock Migration Summary

ToolRegistry mutable API added (register_dynamic, unregister_by_prefix) and all Arc<ToolRegistry> callsites migrated to Arc<RwLock<ToolRegistry>> in a single atomic wave, enabling MCP reload without deadlocks.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add register_dynamic and unregister_by_prefix to ToolRegistry | 068a5b9 | registry.rs |
| 2 | Migrate Arc<ToolRegistry> to Arc<RwLock<ToolRegistry>> | 753aa17 | 8 files |

## What Was Built

**Task 1 (TDD):** Added two new methods to `ToolRegistry` in `ironhermes-tools/src/registry.rs`:
- `register_dynamic(&mut self, tool: Box<dyn Tool>)` — runtime MCP tool registration (semantic alias for register())
- `unregister_by_prefix(&mut self, server_name: &str) -> usize` — removes all tools named `{server}__*`, returns count
- 7 unit tests covering: insert, overwrite, prefix removal, count, empty registry, no-match case

**Task 2 (Migration):** Atomic cross-workspace migration of `Arc<ToolRegistry>` to `Arc<RwLock<ToolRegistry>>`:
- `agent_loop.rs`: field + `new()` + 4 call sites (`get_definitions`, `check_guardrails`, `guardrail_error_detail`, `execute_tool`) all use `.read().await`
- `subagent_runner.rs`: `SubagentRunner` trait impl `run_child` parameter
- `gateway/handler.rs`: struct field + constructor + `Arc::clone` at AgentLoop construction
- `gateway/runner.rs`: struct field + constructor + `execute_cron_job` function signature
- `cli/main.rs`: `RwLock` import + 3x `Arc::new(RwLock::new(registry))` in `run_single`, `run_chat`, `run_gateway` + `run_agent_turn` parameter
- `cli/batch/runner.rs`: registry construction + read guard for `filters::run_filters(&ToolRegistry)`
- `tools/delegate_task.rs`: `SubagentRunner` trait definition + all mock impls + 2x `Arc::new(RwLock::new(child_registry))`
- `agent_wiring.rs`: test helper

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking Issue] agent_wiring.rs test helper missed in initial grep**
- **Found during:** Task 2 cargo test run (first attempt)
- **Issue:** `crates/ironhermes-agent/src/agent_wiring.rs` test helper `bare_agent()` used `Arc::new(ToolRegistry::new())` directly without wrapping in RwLock
- **Fix:** Added `RwLock` import and wrapped registry in `Arc::new(RwLock::new(...))`
- **Files modified:** `crates/ironhermes-agent/src/agent_wiring.rs`
- **Commit:** 753aa17

**2. [Rule 3 - Blocking Issue] batch/runner.rs missed in initial grep**
- **Found during:** Task 2 first cargo check
- **Issue:** `crates/ironhermes-cli/src/batch/runner.rs` constructed `Arc::new(ToolRegistry::new())` and passed it to `AgentLoop::new()`; also passed `&registry` to `filters::run_filters(&ToolRegistry)` which expects the concrete type
- **Fix:** Added `RwLock` to imports; wrapped registry construction; acquire `.read().await` guard before calling `run_filters`
- **Files modified:** `crates/ironhermes-cli/src/batch/runner.rs`
- **Commit:** 753aa17

**3. [Rule 3 - Blocking Issue] runner.rs test at line 1505 used qualified path without RwLock**
- **Found during:** Task 2 test run
- **Issue:** `Arc::new(ironhermes_tools::ToolRegistry::new())` at test line 1505 in runner.rs wasn't caught by `replace_all` due to qualified path
- **Fix:** Replaced with `Arc::new(RwLock::new(ironhermes_tools::ToolRegistry::new()))`
- **Files modified:** `crates/ironhermes-gateway/src/runner.rs`
- **Commit:** 753aa17

## Pre-existing Failures (Not Introduced)

2 tests in `ironhermes-core` were already failing before this plan:
- `commands::handlers::tests::cmd_models_info_known_model` — missing context_length for claude-sonnet-4 in static model table
- `provider::tests::test_provider_resolver_populates_model_metadata` — related model metadata issue

Verified by git stash + re-run of baseline: both fail without any of this plan's changes.

## Security Review

Per T-21.2-04: write guards are only acquired inside `register_dynamic`/`unregister_by_prefix` (not yet called from production code — plan 03 wires McpManager). No write guard is held across any `.await` point.

Per T-21.2-05: `rpc_registry` in `execute_code.rs` remains `Arc<ToolRegistry>` (no RwLock). It is constructed from a separate fresh ToolRegistry with only D-07 safe tools. Structurally isolated — cannot receive MCP tools.

## Known Stubs

None. This is a pure type migration + new API methods. No placeholder data or TODO stubs introduced.

## Threat Flags

None. No new network endpoints, auth paths, or schema changes introduced. The RwLock wrapping is internal to the existing ToolRegistry boundary.

## Self-Check: PASSED

- registry.rs contains `pub fn register_dynamic` — FOUND
- registry.rs contains `pub fn unregister_by_prefix` — FOUND
- agent_loop.rs field is `Arc<RwLock<ToolRegistry>>` — FOUND (line 63)
- main.rs has 3x `RwLock::new(registry)` — FOUND (lines 402, 699, 1210)
- execute_code.rs still has `Arc<ToolRegistry>` for rpc_registry — FOUND (5 occurrences)
- Commit 068a5b9 exists — FOUND
- Commit 753aa17 exists — FOUND
