# Phase 25.6: replicate-cli-web-wiring - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves alternatives considered.

**Date:** 2026-05-04
**Phase:** 25.6-replicate-cli-web-wiring
**Areas discussed:** Factory boundary, Factory placement, Lifecycle scope, Hook/guardrail behavior, Prompt/session parity, Scope handling for proxy/backend idea

---

## Factory Boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Shared in-process factory | Extract reusable Rust factory consumed by CLI + web, preserving direct in-process wiring | ✓ |
| Keep local + small helpers | Keep wiring mostly inline and only factor tiny helper chunks | |
| Localhost proxy backend | Route web through separate local backend service | |

**User's choice:** Shared in-process factory
**Notes:** User proposed a localhost proxy idea during discussion; that idea was scoped as deferred and not selected for Phase 25.6.

---

## Factory Placement

| Option | Description | Selected |
|--------|-------------|----------|
| `ironhermes-agent` | Shared by CLI + web without dependency inversion | ✓ |
| `ironhermes-tools` | Tool-centric placement with broader wiring concerns mixed in | |
| `iron_hermes_ui` then lift later | Web-first implementation with later extraction | |

**User's choice:** `ironhermes-agent`
**Notes:** User initially suggested a future `ironagent-tools-api` crate, then explicitly deferred that crate for this phase.

---

## Lifecycle Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Hybrid scope | App-scoped shared infra + per-session/per-turn state | ✓ |
| Everything app-scoped | Simplest wiring, weaker session isolation | |
| Everything per-session | Strong isolation, higher startup overhead | |

**User's choice:** Hybrid scope
**Notes:** App-scoped includes shared registries/managers; per-session covers session id, persistence, and cancellation/runtime turn state.

---

## Hook/Guardrail Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Mirror CLI exactly | Keep hook/guardrail behavior parity with CLI wiring | ✓ |
| Mirror, but no webhooks | Keep most parity but suppress webhook listeners | |
| Guardrails only, no hooks | Minimal behavior for now | |

**User's choice:** Mirror CLI exactly
**Notes:** Includes `HooksConfig`, `BlocklistGuardrail`, `error_detail`, JSONL listener, webhook listeners when configured, and retry-queue drain.

---

## Prompt/Session Parity

| Option | Description | Selected |
|--------|-------------|----------|
| Full parity for turns | Use PromptBuilder path + session/message persistence parity behavior | ✓ |
| Tool parity only | Keep prompt/session simpler in web | |
| Custom web prompt flow | Diverge from CLI setup | |

**User's choice:** Full parity for turns
**Notes:** Keep web session namespace separate (`Platform::Web`) while matching CLI turn setup path.

---

## Scope Handling (Proxy/Backend Idea)

| Option | Description | Selected |
|--------|-------------|----------|
| Defer to backlog | Record as deferred future-phase capability | ✓ |
| Drop for now | Do not track as deferred idea | |

**User's choice:** Defer to backlog
**Notes:** Idea captured as deferred: `/api` proxy to local backend service (`127.0.0.1:9119`).

---

## the agent's Discretion

- Internal helper naming and exact factory return types.
- Exact test placement and assertion style for parity lock checks.

## Deferred Ideas

- Localhost proxy backend architecture for web (`/api` -> `http://127.0.0.1:9119`).
- New `ironagent-tools-api` crate (explicitly deferred for this phase).
