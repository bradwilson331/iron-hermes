---
phase: 25.6-replicate-cli-web-wiring
plan: 02
type: execute
wave: 2
depends_on: [25.6-01]
files_modified:
  - crates/ironhermes-cli/src/main.rs
  - crates/ironhermes-cli/src/tui_rata/event_loop.rs
  - crates/ironhermes-cli/tests/cli_web_runtime_parity.rs
autonomous: true
requirements: [D-03, D-05, D-08, D-09, D-10, D-14]
must_haves:
  truths:
    - "CLI entry points consume the shared factory instead of maintaining divergent wiring blocks."
    - "CLI behavior for hooks, guardrails, and tool surface remains unchanged after migration."
    - "Ratatui bootstrap (`build_app_deps`) uses the same app-scoped runtime construction path."
  artifacts:
    - path: "crates/ironhermes-cli/src/main.rs"
      provides: "Factory-backed runtime wiring for run_single/run_chat/run_gateway"
    - path: "crates/ironhermes-cli/src/tui_rata/event_loop.rs"
      provides: "Factory-backed runtime wiring for ratatui startup"
    - path: "crates/ironhermes-cli/tests/cli_web_runtime_parity.rs"
      provides: "Regression tests locking factory usage and parity"
  key_links:
    - from: "CLI entry points"
      to: "ironhermes-agent app runtime factory"
      via: "bundle construction call"
      pattern: "build_app_runtime_bundle"
    - from: "runtime bundle"
      to: "AgentLoop construction"
      via: "registry/hook/mcp handles passed through"
      pattern: "with_hook_registry|attach_context_engine|build_mcp_manager|set_mcp_manager"
---

<objective>
Migrate CLI wiring sites to the shared runtime factory while preserving existing behavior and ordering contracts.

Purpose: enforce D-03 at the source-of-truth surface that web parity depends on.
Output: CLI + ratatui callsites use one shared app-scoped factory path with parity tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25.6-replicate-cli-web-wiring/25.6-CONTEXT.md
@.planning/phases/25.6-replicate-cli-web-wiring/25.6-01-PLAN.md
@crates/ironhermes-cli/src/main.rs
@crates/ironhermes-cli/src/tui_rata/event_loop.rs

<interfaces>
From `crates/ironhermes-cli/src/main.rs`:
```rust
async fn run_single(...)
async fn run_chat(...)
async fn run_gateway(...)
fn build_registry_with_process_registry(...) -> ToolRegistry
```

From `crates/ironhermes-cli/src/tui_rata/event_loop.rs`:
```rust
async fn build_app_deps(cli: &Cli, yolo: bool) -> Result<AppDeps>
```

From Plan 25.6-01 output:
```rust
pub async fn build_app_runtime_bundle(...) -> anyhow::Result<AppRuntimeBundle>
pub struct AppRuntimeBundle { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace duplicated CLI wiring blocks with factory-backed bundle usage</name>
  <files>crates/ironhermes-cli/src/main.rs</files>
  <read_first>crates/ironhermes-cli/src/main.rs, crates/ironhermes-agent/src/app_runtime_factory.rs</read_first>
  <behavior>
    - Test 1: run_single/run_chat/run_gateway all invoke the shared factory path.
    - Test 2: Existing hooks/guardrails behavior remains active (HookRegistry wiring, BlocklistGuardrail, retry drain).
    - Test 3: Tool surface parity is retained (browser + web_extract + execute_code + skills + delegate_task + cronjob + memory).
  </behavior>
  <action>Refactor `run_single`, `run_chat`, and `run_gateway` to consume the app-scoped runtime bundle from Plan 25.6-01 instead of hand-building the same wiring chain. Preserve CLI runtime behavior and order semantics per D-03/D-08/D-09/D-10/D-14. Keep per-session/per-turn values (session_id, persistence writes, cancellation tokens, PromptBuilder turn setup) in main.rs — do not move these into app-scoped factory logic.</action>
  <acceptance_criteria>
    - `main.rs` contains factory invocation in all three entry points.
    - Existing callsites still wire `with_hook_registry` and `attach_context_engine(..., Some(hook_registry), ...)`.
    - Registration of browser/web_extract/execute_code/delegate_task/cronjob/skills/memory remains present through factory bundle usage.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p ironhermes-cli --test cli_tool_parity -- --nocapture</automated>
  </verify>
  <done>All three CLI entry points share one app-scoped runtime construction path without feature regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate ratatui build_app_deps to shared runtime bundle consumption</name>
  <files>crates/ironhermes-cli/src/tui_rata/event_loop.rs</files>
  <read_first>crates/ironhermes-cli/src/tui_rata/event_loop.rs, crates/ironhermes-agent/src/app_runtime_factory.rs</read_first>
  <behavior>
    - Test 1: `build_app_deps` uses factory output for app-scoped runtime initialization.
    - Test 2: Existing ratatui browser and hook wiring invariants still pass.
  </behavior>
  <action>Update `build_app_deps` to consume the same shared app runtime factory used by main.rs entry points, preserving D-03 parity and existing ratatui contracts (including browser-session and hook wiring behavior). Keep ratatui-specific UI state and event-loop concerns local to event_loop.rs.</action>
  <acceptance_criteria>
    - `build_app_deps` references shared runtime bundle API.
    - Existing ratatui invariants around browser session and hook usage continue to compile/pass.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p ironhermes-cli --test invariants_22_4 -- --nocapture</automated>
  </verify>
  <done>Ratatui startup consumes the same app-scoped wiring path as other CLI surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add CLI regression test locks for shared-factory parity</name>
  <files>crates/ironhermes-cli/tests/cli_web_runtime_parity.rs</files>
  <read_first>crates/ironhermes-cli/tests/cli_tool_parity.rs, crates/ironhermes-cli/src/main.rs, crates/ironhermes-cli/src/tui_rata/event_loop.rs</read_first>
  <behavior>
    - Test 1: Static assertions enforce that CLI callsites use the shared factory.
    - Test 2: Parity assertions prevent accidental removal of key tool/hook wiring contracts.
  </behavior>
  <action>Create `cli_web_runtime_parity.rs` with static-grep style tests that lock D-03 parity assumptions: shared factory usage in run_single/run_chat/run_gateway/build_app_deps, and preserved key behavior anchors (hook wiring, browser/web_extract tool exposure, retry-drain path). Keep tests deterministic and dependency-free, matching existing invariant test style.</action>
  <acceptance_criteria>
    - New test file exists and runs under `cargo test -p ironhermes-cli --test cli_web_runtime_parity`.
    - Tests fail when factory callsites are removed or key parity wiring markers disappear.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p ironhermes-cli --test cli_web_runtime_parity -- --nocapture</automated>
  </verify>
  <done>CLI parity migration is protected by explicit regression tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime bundle → CLI agent execution | Wiring mistakes can expose/disable capabilities unexpectedly |
| hooks config → CLI listeners | Malformed config can alter monitoring/guardrail behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25.6-02-01 | T | main.rs wiring refactor | mitigate | Add explicit factory-usage tests and preserve anchor invariants from existing cli_tool_parity tests. |
| T-25.6-02-02 | E | guardrail/hook propagation | mitigate | Keep hook_registry threading into `with_hook_registry` and `attach_context_engine(...Some(hook_registry)...)` with regression checks. |
| T-25.6-02-03 | D | ratatui startup | accept | Preserve async/background startup patterns; avoid introducing blocking work on render/event loop path. |
</threat_model>

<verification>
- `cargo test -p ironhermes-cli --test cli_web_runtime_parity -- --nocapture`
- `cargo test -p ironhermes-cli --test cli_tool_parity -- --nocapture`
- `cargo check -p ironhermes-cli`
</verification>

<success_criteria>
- CLI and ratatui use the shared runtime factory.
- No regression in hook/guardrail/tool wiring behavior.
- New parity tests lock factory usage and critical contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/25.6-replicate-cli-web-wiring/25.6-02-SUMMARY.md`
</output>
