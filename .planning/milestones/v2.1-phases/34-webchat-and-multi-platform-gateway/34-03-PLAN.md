---
phase: 34-webchat-and-multi-platform-gateway
plan: 03
type: execute
wave: 2
depends_on: [34-02]
files_modified:
  - crates/ironhermes-gateway/src/discord.rs
  - crates/ironhermes-gateway/src/lib.rs
autonomous: true
requirements: [LEARN-01, LEARN-02, LEARN-03, LEARN-04, LEARN-05]

must_haves:
  truths:
    - "A Discord message received by run_discord_adapter is converted to MessageEvent with Platform::Discord and routed through GatewayMessageHandler.handle_with_multimodal"
    - "DiscordAdapter implements all 7 PlatformAdapter methods; platform() returns Platform::Discord"
    - "Sender allowlist (whitelist) is checked before routing — empty whitelist denies all messages (matches Telegram canonical semantics at runner.rs:608-611 + config.rs:731 'Empty = deny all (D-12)')"
    - "run_discord_adapter shuts down cleanly on CancellationToken.cancelled() (no infinite hang)"
    - "Empty msg.content (MESSAGE_CONTENT intent missing or denied) is detected and rejected before dispatching to handler (T-34-03 mitigation)"
    - "INV-34-01 (Plan 01 Task 2) flips from RED to GREEN"
    - "D-10: DiscordAdapter implements PlatformAdapter and routes through GatewayHandler.handle_with_multimodal — delivered by this entire plan (Tasks 1+2+3)"
  artifacts:
    - path: "crates/ironhermes-gateway/src/discord.rs"
      provides: "DiscordAdapter + DiscordEventHandler + run_discord_adapter + discord_message_to_event"
      contains: "impl PlatformAdapter for DiscordAdapter"
      exports: ["DiscordAdapter", "run_discord_adapter"]
    - path: "crates/ironhermes-gateway/src/lib.rs"
      provides: "pub mod discord; re-exports"
      contains: "pub mod discord"
  key_links:
    - from: "crates/ironhermes-gateway/src/discord.rs"
      to: "crates/ironhermes-gateway/src/handler.rs"
      via: "Arc<GatewayMessageHandler>.handle_with_multimodal(&event, adapter, cancel, processed)"
      pattern: "handle_with_multimodal"
    - from: "crates/ironhermes-gateway/src/discord.rs"
      to: "crates/ironhermes-core/src/types.rs (Platform::Discord)"
      via: "Platform::Discord enum variant"
      pattern: "Platform::Discord"
---

<objective>
Implement DiscordAdapter using serenity 0.12.5: (1) implement PlatformAdapter trait so the adapter can send / edit / delete Discord messages; (2) implement serenity EventHandler so incoming Discord messages are converted to MessageEvent and routed through GatewayMessageHandler.handle_with_multimodal (Learning Loop inherited structurally per D-12); (3) expose `run_discord_adapter(token, handler, cancel) -> Result<()>` startup function for the Wave 3 GatewayRunner wiring.

Purpose: D-10 — first of two new platform adapters. Discord/Slack inherit nudge + skill-create automatically via handle_with_multimodal — no Learning Loop code in this file.

Output: crates/ironhermes-gateway/src/discord.rs (new), crates/ironhermes-gateway/src/lib.rs (modified with pub mod discord + pub use). Plan 01's INV-34-01 grep test flips RED → GREEN.

## Canonical Whitelist Semantics (verified during revision review)

`crates/ironhermes-core/src/config.rs:731` — `/// Telegram user IDs allowed to interact with the bot. Empty = deny all (D-12).`

`crates/ironhermes-gateway/src/runner.rs:601-611` — runtime gate:
```
// Whitelist check (D-10/D-11/D-12)
if !whitelist.is_empty() {
    let sender_id: i64 = event.sender_id.parse().unwrap_or(0);
    if !whitelist.contains(&sender_id) {
        warn!(sender_id = sender_id, "Sender not in whitelist, ignoring");
        continue;
    }
} else {
    warn!("Whitelist is empty — denying all messages (D-12)");
    continue;
}
```

Conclusion: **empty whitelist = silent-warn-then-deny-all**. Discord/Slack adapters MIRROR this exact branch structure verbatim. Do NOT silently invert to allow-all.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-webchat-and-multi-platform-gateway/34-CONTEXT.md
@.planning/phases/34-webchat-and-multi-platform-gateway/34-RESEARCH.md
@.planning/phases/34-webchat-and-multi-platform-gateway/34-PATTERNS.md

<interfaces>
<!-- Contracts the executor must satisfy verbatim -->

From crates/ironhermes-gateway/src/adapter.rs (PlatformAdapter trait — 7 methods):
```rust
#[async_trait]
pub trait PlatformAdapter: Send + Sync {
    fn platform(&self) -> Platform;
    async fn send_message(&self, chat_id: &str, content: &str, thread_id: Option<&str>) -> Result<MessageResponse>;
    async fn edit_message(&self, chat_id: &str, message_id: &str, content: &str) -> Result<()>;
    async fn edit_message_markdown(&self, chat_id: &str, message_id: &str, content: &str) -> Result<()>;
    async fn delete_message(&self, chat_id: &str, message_id: &str) -> Result<()>;
    async fn add_reaction(&self, _: &str, _: &str, _: &str) -> Result<()> { Ok(()) }
    async fn send_chat_action(&self, _: &str, _: &str) -> Result<()> { Ok(()) }
    fn is_running(&self) -> bool;
}
```

From crates/ironhermes-gateway/src/handler.rs line 769 (GatewayMessageHandler — routing entry point):
```rust
pub async fn handle_with_multimodal(
    &self,
    event: &MessageEvent,
    adapter: Arc<dyn PlatformAdapter>,
    cancel: CancellationToken,
    processed: ProcessedAttachments,
) -> Result<()>;
```

From crates/ironhermes-core (Platform enum):
```rust
pub enum Platform { Cli, Telegram, Web, Discord, Slack, ... }
```

From crates/ironhermes-gateway/src/multimodal.rs:
```rust
pub struct ProcessedAttachments { /* default = empty */ }
```

From crates/ironhermes-core (MessageEvent — see telegram.rs:378-431 for full field list):
```rust
pub struct MessageEvent {
    pub platform: Platform,
    pub message_id: String,
    pub chat_id: String,
    pub sender_id: String,
    pub content: String,
    pub attachments: Vec<Attachment>,
    pub thread_id: Option<String>,
    pub chat_type: String,
    pub chat_name: Option<String>,
    pub sender_name: Option<String>,
    pub replied_to_id: Option<String>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create discord.rs — DiscordAdapter (PlatformAdapter impl) + discord_message_to_event</name>
  <files>crates/ironhermes-gateway/src/discord.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/telegram.rs lines 1-9 (imports), lines 68-81 (struct + new), lines 213-298 (PlatformAdapter impl — 7 methods), lines 378-431 (tg_message_to_event analog)
    - crates/ironhermes-gateway/src/adapter.rs (full file — trait signatures)
    - .planning/phases/34-webchat-and-multi-platform-gateway/34-PATTERNS.md (lines 27-181 — DiscordAdapter analog mapping)
    - .planning/phases/34-webchat-and-multi-platform-gateway/34-RESEARCH.md (Pitfall 1 — MESSAGE_CONTENT intent; Pitfall 6 — edit rate limit)
    - serenity 0.12.5 docs.rs entries for `ChannelId::say`, `MessageId::edit`, `EditMessage`, `ChannelId::delete_message` (use Context7 MCP via `resolve-library-id` then `query-docs` if exact API signatures differ from the patterns below)
  </read_first>
  <behavior>
    - Module exposes `pub struct DiscordAdapter { ctx: serenity::prelude::Context }` and `pub fn discord_message_to_event(msg: &serenity::model::channel::Message) -> ironhermes_core::MessageEvent`.
    - `discord_message_to_event` returns a MessageEvent with `platform: Platform::Discord`, `chat_id = msg.channel_id.to_string()`, `sender_id = msg.author.id.to_string()`, `content = msg.content.clone()`, `sender_name = Some(msg.author.name.clone())`, `chat_type` derived from channel kind ("dm" for DM, "group" for guild text channels), other fields per the MessageEvent contract.
    - `impl PlatformAdapter for DiscordAdapter` implements all 7 methods; `platform()` returns `Platform::Discord`; `is_running()` returns `false` (lifecycle managed by GatewayRunner).
    - `send_message` calls `ChannelId::new(chat_id.parse::<u64>()?).say(&self.ctx.http, content).await` and returns a MessageResponse with the new message_id, the chat_id, and `Platform::Discord`.
    - `edit_message` and `edit_message_markdown` both call `ChannelId::new(chat_id.parse()?).edit_message(&self.ctx.http, MessageId::new(message_id.parse()?), serenity::builder::EditMessage::new().content(content)).await` (Discord renders Markdown natively — no separate code path).
    - `delete_message` calls `ChannelId::new(chat_id.parse()?).delete_message(&self.ctx.http, MessageId::new(message_id.parse()?)).await`.
    - `add_reaction` and `send_chat_action` use the trait default no-op (DO NOT override — Discord lacks chat_action; reactions can be added in a future phase if needed).
  </behavior>
  <action>
    Create crates/ironhermes-gateway/src/discord.rs containing:

    (1) Imports per PATTERNS.md lines 42-53 (use serenity::prelude::Context as SerenityContext, etc.).

    (2) `pub struct DiscordAdapter { ctx: serenity::prelude::Context }` — single field; the serenity Context owns the HTTP client.

    (3) `#[async_trait] impl PlatformAdapter for DiscordAdapter { ... }` — all 7 methods. Use `chat_id.parse::<u64>().map_err(|e| anyhow::anyhow!("invalid Discord channel_id {chat_id}: {e}"))?` and the same shape for message_id. For `send_message`, the returned `serenity::model::channel::Message` has `.id.to_string()` → use that as `MessageResponse.message_id`. For `edit_message_markdown`, delegate to `self.edit_message(...).await` since Discord auto-renders Markdown (one-line impl).

    (4) `pub fn discord_message_to_event(msg: &serenity::model::channel::Message) -> ironhermes_core::MessageEvent` mirroring telegram.rs `tg_message_to_event` (lines 378-431). Field mapping:
        - platform: ironhermes_core::Platform::Discord
        - message_id: msg.id.to_string()
        - chat_id: msg.channel_id.to_string()
        - sender_id: msg.author.id.to_string()
        - content: msg.content.clone()
        - attachments: vec![] (Phase 34 is text-only; attachments deferred — same as PATTERNS.md line 459)
        - thread_id: msg.thread.as_ref().map(|t| t.id.to_string()) — if the serenity 0.12.5 Message API differs (e.g. uses `msg.thread_id`), use whichever field actually exists; do not fabricate.
        - chat_type: derive from `msg.guild_id` — `None` → "dm".to_string(); `Some(_)` → "group".to_string() (Discord DMs have no guild_id)
        - chat_name: None for DMs; could be derived from channel name in future — leave None for Phase 34
        - sender_name: Some(msg.author.name.clone())
        - replied_to_id: msg.referenced_message.as_ref().map(|m| m.id.to_string())

    (5) Unit test module `#[cfg(test)] mod tests { ... }` with at least one test: `discord_message_to_event_returns_platform_discord` constructing a minimal stand-in (use a manual struct literal where serenity types permit, or skip the construction and test only that the module compiles + module-level helpers like `parse_chat_type_from_guild_id(guild_id: Option<...>) -> &'static str` return "dm" for None and "group" for Some). If the serenity types are not constructable from outside, extract the chat_type logic to `fn classify_chat_type(guild_id: Option<serenity::model::id::GuildId>) -> &'static str` and unit-test that.

    Per CLAUDE.md "no error handling for impossible scenarios" — do NOT wrap successful API calls in extra Option/Result layers; let `?` propagate.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-gateway 2>&amp;1 | tail -20 &amp;&amp; cargo test -p ironhermes-gateway --lib discord 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/ironhermes-gateway/src/discord.rs` exists
    - `grep -c "impl PlatformAdapter for DiscordAdapter" crates/ironhermes-gateway/src/discord.rs` returns 1
    - `grep -c "Platform::Discord" crates/ironhermes-gateway/src/discord.rs` returns >= 2 (platform() return + discord_message_to_event)
    - `grep -c "pub fn discord_message_to_event" crates/ironhermes-gateway/src/discord.rs` returns 1
    - `cargo build -p ironhermes-gateway` exits 0
    - `cargo test -p ironhermes-gateway --lib discord` exits 0 (unit tests pass)
  </acceptance_criteria>
  <done>DiscordAdapter struct + PlatformAdapter impl + discord_message_to_event helper compile and unit-test. Module not yet exposed via lib.rs — Task 3 wires that.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add DiscordEventHandler + run_discord_adapter (routing + cancellation + intent gates + canonical whitelist semantics)</name>
  <files>crates/ironhermes-gateway/src/discord.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/discord.rs (file from Task 1 — extend it)
    - crates/ironhermes-gateway/src/handler.rs line 769 (handle_with_multimodal signature)
    - crates/ironhermes-gateway/src/runner.rs lines 403-505 (Telegram startup pattern — getMe / set_my_commands / poll spawn for parallel reference)
    - **crates/ironhermes-gateway/src/runner.rs lines 601-611 (CANONICAL whitelist semantics — empty whitelist = deny all with warn-and-continue; D-12)**
    - **crates/ironhermes-core/src/config.rs:731 ('Telegram user IDs allowed to interact with the bot. Empty = deny all (D-12).' — canonical doc)**
    - .planning/phases/34-webchat-and-multi-platform-gateway/34-RESEARCH.md (Pattern 1, Pitfalls 1, 3, 5 — MESSAGE_CONTENT intent, mutex-across-await, client.start() shutdown)
    - .planning/phases/34-webchat-and-multi-platform-gateway/34-PATTERNS.md (lines 135-178 — EventHandler + run_discord_adapter sketch)
    - serenity 0.12.5 docs.rs: GatewayIntents bitmask, EventHandler trait, Client::builder, shard_manager().shutdown_all() (Context7: `resolve-library-id serenity` then `query-docs <id> "Client::builder shard_manager shutdown_all 0.12"`)
  </read_first>
  <behavior>
    - `DiscordEventHandler.message(ctx, msg)` skips bot messages (`msg.author.bot`), rejects empty content (`msg.content.is_empty()` — T-34-03 MESSAGE_CONTENT mitigation), checks the configured sender allowlist (T-34-02 — passed via a new field; empty whitelist denies all, matching Telegram runner.rs:608-611), wraps the serenity Context in a DiscordAdapter, calls `discord_message_to_event`, and forwards to `handler.handle_with_multimodal(&event, adapter, cancel.child_token(), ProcessedAttachments::default()).await`. Result is logged via `tracing` on error; not propagated (EventHandler::message returns ()).
    - `pub async fn run_discord_adapter(token: &str, whitelist: Vec<u64>, handler: Arc<GatewayMessageHandler>, cancel: CancellationToken) -> Result<()>` constructs the serenity Client with `GatewayIntents::GUILD_MESSAGES | GatewayIntents::DIRECT_MESSAGES | GatewayIntents::MESSAGE_CONTENT`, registers `DiscordEventHandler { handler, whitelist, cancel: cancel.clone() }`, and wraps `client.start()` in `tokio::select!` against `cancel.cancelled()`. The cancel arm calls `client.shard_manager.shutdown_all().await` and returns Ok(()) (Pitfall 5).
    - Token string is NEVER passed to `tracing::*!` (T-34-01); log only the bot's `Ready.user.tag` after the Ready event fires.
    - No std::sync::Mutex guard is held across any `.await` inside the EventHandler closures (Pitfall 3 — the adapter itself doesn't touch nudge_turns, but the discipline must be visible).
  </behavior>
  <action>
    Append to crates/ironhermes-gateway/src/discord.rs:

    (a) `pub struct DiscordEventHandler { handler: Arc<crate::GatewayMessageHandler>, whitelist: Vec<u64>, cancel: CancellationToken }`. Use Vec<u64> (Discord user IDs are u64 snowflakes); convert from the existing PlatformGatewayConfig.whitelist Vec<i64> at the call site via `.iter().map(|&v| v as u64).collect()` in run_discord_adapter (Telegram uses i64; Discord IDs fit in u64 — same int width, different sign convention; cast is lossless for Discord IDs since they're always positive).

    (b) `#[serenity::async_trait] impl serenity::prelude::EventHandler for DiscordEventHandler` with `async fn message(&self, ctx: SerenityContext, msg: SerenityMessage)`. The whitelist branch MUST mirror Telegram's verified semantics at runner.rs:601-611 verbatim:

        ```
        // CANONICAL: matches runner.rs:601-611 + config.rs:731 (D-12 empty = deny all)
        if !self.whitelist.is_empty() {
            if !self.whitelist.contains(&msg.author.id.get()) {
                tracing::warn!(sender_id = %msg.author.id, "Sender not in whitelist, ignoring");
                return;
            }
        } else {
            tracing::warn!("Whitelist is empty — denying all messages (D-12)");
            return;
        }
        ```

        Other guards in `message`:
        - early return if `msg.author.bot`
        - early return + `tracing::warn!("Discord MESSAGE_CONTENT intent appears missing or denied — empty content from non-bot author {}", msg.author.id)` if `msg.content.is_empty()` (T-34-03 explicit guard, not just structural)
        - else: build adapter, build event, call `self.handler.handle_with_multimodal(...)`, log Err via `tracing::error!("Discord handler error: {e:#}")`

    (c) Optional `async fn ready(&self, _ctx: SerenityContext, ready: serenity::model::gateway::Ready)` logging `tracing::info!(bot = %ready.user.tag(), "Discord adapter connected")` — NEVER log the token.

    (d) `pub async fn run_discord_adapter(token: &str, whitelist: Vec<u64>, handler: Arc<crate::GatewayMessageHandler>, cancel: CancellationToken) -> Result<()>` per PATTERNS.md lines 158-176. Build intents bitmask with all three flags (GUILD_MESSAGES, DIRECT_MESSAGES, MESSAGE_CONTENT). Use `tokio::select!` with shutdown_all on the cancel arm.

    (e) Extend the unit-test module with `cancellation_token_check_compiles` — a smoke test that constructs `CancellationToken::new()`, calls `.child_token()`, drops both. Goal: lock that the CancellationToken import is correct (compile-time gate) without requiring a live Discord token. The actual end-to-end Discord round-trip is in the Plan 02 user_setup checkpoint, not here.

    Per CLAUDE.md, no emojis in code/docs.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-gateway 2>&amp;1 | tail -20 &amp;&amp; cargo clippy -p ironhermes-gateway --lib -- -D clippy::await_holding_lock 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GatewayIntents::MESSAGE_CONTENT" crates/ironhermes-gateway/src/discord.rs` returns >= 1
    - `grep -c "shard_manager" crates/ironhermes-gateway/src/discord.rs` returns >= 1 (cancel arm cleanup)
    - `grep -c "msg.content.is_empty" crates/ironhermes-gateway/src/discord.rs` returns >= 1 (T-34-03 guard)
    - `grep -c "whitelist" crates/ironhermes-gateway/src/discord.rs` returns >= 2 (struct field + check)
    - `grep -c "denying all messages (D-12)" crates/ironhermes-gateway/src/discord.rs` returns >= 1 (canonical empty-whitelist deny log — matches Telegram runner.rs:609)
    - `grep -c "pub async fn run_discord_adapter" crates/ironhermes-gateway/src/discord.rs` returns 1
    - `grep -c "handle_with_multimodal" crates/ironhermes-gateway/src/discord.rs` returns >= 1 (THE routing call — required to flip INV-34-01 GREEN)
    - `cargo build -p ironhermes-gateway` exits 0
    - `cargo clippy -p ironhermes-gateway --lib -- -D clippy::await_holding_lock` exits 0 (Pitfall 3 gate)
    - Token string `token` never appears as an argument to `tracing::*!` macros: `grep -E 'tracing::(info|warn|debug|error|trace)!.*token' crates/ironhermes-gateway/src/discord.rs` returns 0 lines
  </acceptance_criteria>
  <done>run_discord_adapter compiles, clippy clean, security guards in place (intent + canonical-whitelist-deny + no token logging), cancellation wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire discord module in lib.rs; INV-34-01 flips GREEN</name>
  <files>crates/ironhermes-gateway/src/lib.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/lib.rs (entire file — current pub mod block lines 1-30)
    - .planning/phases/34-webchat-and-multi-platform-gateway/34-PATTERNS.md (lines 314-339 — lib.rs additions)
  </read_first>
  <behavior>
    - `pub mod discord;` declared alongside existing `pub mod telegram;` declaration.
    - `pub use discord::{DiscordAdapter, run_discord_adapter};` re-export (callers in Plan 05 will reference both).
    - INV-34-01 (`inv_34_01_discord_routes_through_handle_with_multimodal` from Plan 01 Task 2) compiles and PASSES — `include_str!("../src/discord.rs")` now resolves to the file from Tasks 1+2, and the file contains `handle_with_multimodal`.
  </behavior>
  <action>
    In crates/ironhermes-gateway/src/lib.rs:
    (a) Add `pub mod discord;` in alphabetical position within the `pub mod` block (between `pub mod backoff;` and `pub mod handler;`, or wherever `d` falls alphabetically — match existing ordering convention).
    (b) Add `pub use discord::{DiscordAdapter, run_discord_adapter};` directly after the `pub use adapter::{MessageHandler, PlatformAdapter};` line (alphabetical or grouped by domain — match existing convention).
  </action>
  <verify>
    <automated>cargo build -p ironhermes-gateway 2>&amp;1 | tail -5 &amp;&amp; cargo test -p ironhermes-gateway --test invariants_34 inv_34_01_discord_routes_through_handle_with_multimodal 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub mod discord" crates/ironhermes-gateway/src/lib.rs` returns 1
    - `grep -c "discord::DiscordAdapter\|discord::run_discord_adapter" crates/ironhermes-gateway/src/lib.rs` returns >= 1
    - `cargo build -p ironhermes-gateway` exits 0
    - `cargo test -p ironhermes-gateway --test invariants_34 inv_34_01_discord_routes_through_handle_with_multimodal` exits 0 (Plan 01 Task 2 RED gate flips GREEN)
    - INV-34-02 (Slack) remains RED until Plan 04 lands slack.rs — this is expected; Slack is a separate plan
  </acceptance_criteria>
  <done>Discord adapter publicly exposed; INV-34-01 green. Ready for Plan 05 GatewayRunner wiring once Slack lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord Gateway WebSocket ↔ DiscordEventHandler | Untrusted user-controlled message text crosses here |
| DiscordEventHandler ↔ GatewayMessageHandler | Authenticated, allowlisted MessageEvent crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Information Disclosure | DiscordEventHandler.ready/message logging | mitigate | Token string `token` never appears as a tracing macro argument (grep-asserted in Task 2 acceptance). Only `ready.user.tag()` logged after connection. |
| T-34-02 | Elevation of Privilege | Unauthorized Discord sender | mitigate | DiscordEventHandler.message mirrors Telegram canonical semantics from runner.rs:601-611: empty whitelist warns and denies all (config.rs:731 D-12); non-empty whitelist rejects senders absent from list. Grep-asserted by literal "denying all messages (D-12)" log string. |
| T-34-03 | Tampering | Empty msg.content (MESSAGE_CONTENT intent missing or denied) | mitigate | Explicit `msg.content.is_empty()` guard with `tracing::warn!` before dispatch — fail closed rather than send empty turn to agent. |
| T-34-05 | Spoofing | Session key collision across platforms | mitigate | discord_message_to_event sets Platform::Discord; downstream SessionKey(Platform::Discord, chat_id) prevents collision with Telegram chat_id "12345". |
| T-34-PITFALL-3 | Tampering | Mutex guard across .await in adapter callbacks | mitigate | Adapter itself never touches nudge_turns; clippy `-D await_holding_lock` gate enforced in Task 2 acceptance. |
| T-34-PITFALL-5 | DoS | client.start() blocks forever on Ctrl+C | mitigate | tokio::select! with cancel.cancelled() arm + shard_manager.shutdown_all() in Task 2; grep-asserted. |
</threat_model>

<verification>
- `cargo build -p ironhermes-gateway` clean
- `cargo clippy -p ironhermes-gateway --lib -- -D clippy::await_holding_lock` clean
- `cargo test -p ironhermes-gateway --test invariants_34 inv_34_01_*` green (INV-34-02 still red — Plan 04 closes)
- `cargo test -p ironhermes-gateway --lib discord` green (unit tests in discord.rs)
</verification>

<success_criteria>
- discord.rs exists with DiscordAdapter + EventHandler + run_discord_adapter (3 public surfaces)
- All 7 PlatformAdapter methods implemented; platform() returns Platform::Discord
- run_discord_adapter wraps client.start() with cancellation; shard_manager.shutdown_all() called on cancel
- MESSAGE_CONTENT intent in GatewayIntents bitmask
- Sender allowlist enforced with empty-whitelist deny-all (matches Telegram canonical at runner.rs:608-611); tokens never logged (T-34-01/02/03 mitigations all live)
- INV-34-01 flips RED → GREEN
- Plan 03 SUMMARY.md records Task 2's clippy-await-holding-lock baseline so future plans can preserve it
</success_criteria>

<output>
Create `.planning/phases/34-webchat-and-multi-platform-gateway/34-03-SUMMARY.md` when done.
</output>
