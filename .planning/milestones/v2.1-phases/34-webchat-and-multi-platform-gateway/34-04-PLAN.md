---
phase: 34-webchat-and-multi-platform-gateway
plan: 04
type: execute
wave: 2
depends_on: [34-02, 34-03]
files_modified:
  - crates/ironhermes-gateway/src/slack.rs
  - crates/ironhermes-gateway/src/lib.rs
autonomous: true
requirements: [LEARN-01, LEARN-02, LEARN-03, LEARN-04, LEARN-05]

must_haves:
  truths:
    - "A Slack message received by run_slack_adapter (Socket Mode WebSocket) is converted to MessageEvent with Platform::Slack and routed through GatewayMessageHandler.handle_with_multimodal"
    - "SlackAdapter implements all 7 PlatformAdapter methods; platform() returns Platform::Slack"
    - "Slack Socket Mode requires TWO tokens (app_token xapp-, bot_token xoxb-) and run_slack_adapter accepts both"
    - "Sender allowlist (whitelist) is checked before routing — empty whitelist denies all (matches Telegram canonical semantics at runner.rs:608-611 + config.rs:731 D-12)"
    - "Socket Mode callback ACKs within 3s by dispatching work to tokio::spawn rather than blocking (T-34-04 mitigation)"
    - "INV-34-02 (Plan 01 Task 2) flips from RED to GREEN"
    - "D-11: SlackAdapter implements PlatformAdapter and routes Slack Socket Mode events through GatewayHandler.handle_with_multimodal — delivered by this entire plan (Tasks 1+2+3)"
  artifacts:
    - path: "crates/ironhermes-gateway/src/slack.rs"
      provides: "SlackAdapter + run_slack_adapter + slack_event_to_message_event"
      contains: "impl PlatformAdapter for SlackAdapter"
      exports: ["SlackAdapter", "run_slack_adapter"]
    - path: "crates/ironhermes-gateway/src/lib.rs"
      provides: "pub mod slack; re-exports"
      contains: "pub mod slack"
  key_links:
    - from: "crates/ironhermes-gateway/src/slack.rs"
      to: "crates/ironhermes-gateway/src/handler.rs"
      via: "handler.handle_with_multimodal(&event, adapter, cancel, processed)"
      pattern: "handle_with_multimodal"
    - from: "crates/ironhermes-gateway/src/slack.rs"
      to: "crates/ironhermes-core/src/types.rs (Platform::Slack)"
      via: "Platform::Slack enum variant"
      pattern: "Platform::Slack"
---

<objective>
Implement SlackAdapter using slack-morphism 2.22.0 Socket Mode: (1) implement PlatformAdapter trait so the adapter can send / edit / delete Slack messages via the Web API; (2) implement Socket Mode push-event callbacks that convert Slack `SlackMessageEvent` to MessageEvent and route through GatewayMessageHandler.handle_with_multimodal; (3) expose `run_slack_adapter(app_token, bot_token, whitelist, handler, cancel) -> Result<()>` startup function for Wave 3 GatewayRunner wiring.

Purpose: D-11 — second of two new platform adapters. Slack inherits Learning Loop structurally (D-12). Two-token shape (Pitfall 2): `xapp-` for Socket Mode WebSocket, `xoxb-` for chat.postMessage / chat.update / chat.delete.

Output: crates/ironhermes-gateway/src/slack.rs (new), crates/ironhermes-gateway/src/lib.rs (modified). Plan 01's INV-34-02 grep test flips RED → GREEN.

## Dependency note (revised per BLOCKER 3)

Plan 04 was previously listed as parallel-with-Plan-03 in Wave 2, but both plans modify the same `crates/ironhermes-gateway/src/lib.rs` Task 3. Per planner's same-wave-no-file-overlap rule, this plan now **depends_on: [34-02, 34-03]** — Plan 04 starts after Plan 03's lib.rs edit lands so its own lib.rs edit applies cleanly without a merge collision. Plan 03 runs at Wave 2 start; Plan 04 queues after Plan 03 completes (still within Wave 2's serial extension). Plan 05 (Wave 3) waits on both.

## Canonical Whitelist Semantics (verified during revision review — see Plan 03 for full citation)

Mirror the verified Telegram pattern at `runner.rs:601-611` + `config.rs:731`: empty whitelist = warn + deny all (D-12). Plan 04 implements this verbatim for Slack push-event callbacks (string user IDs instead of u64, but the branch structure is identical).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-webchat-and-multi-platform-gateway/34-CONTEXT.md
@.planning/phases/34-webchat-and-multi-platform-gateway/34-RESEARCH.md
@.planning/phases/34-webchat-and-multi-platform-gateway/34-PATTERNS.md

<interfaces>
<!-- Same PlatformAdapter trait + MessageEvent contract as Plan 03; see Plan 03 <interfaces>. Slack-specific contracts below. -->

slack-morphism 2.22 Socket Mode key types (from docs.rs/slack-morphism — pin docs link below for executor):
**Pinned docs source:** https://docs.rs/slack-morphism/2.22.0/slack_morphism/ — section `socket_mode` for `SlackClientSocketModeListener`, `SlackSocketModeListenerCallbacks::with_push_events`, `SlackPushEvent::EventCallback`, `SlackEventCallbackBody::Message`, `SlackMessageEvent`. If docs.rs is unreachable, the executor MAY pull the same info via Context7 (`resolve-library-id slack-morphism` then `query-docs <id> "Socket Mode chat_post_message SlackMessageEvent 2.22"`) — if BOTH paths fail, escalate per the Task 1 human-verify checkpoint that Plan 02 already established (operator confirms package legitimacy + feature names before install). WARNING 4 closure: the supply-chain risk on the closure shape is covered by the Plan 02 blocking human-verify gate; this is the documented backstop.

```rust
SlackClient<SlackClientHyperConnector>            // top-level client
SlackApiToken / SlackApiTokenValue                 // token wrapper (both xapp- and xoxb- wrapped in this type)
SlackClientSocketModeListener                      // the Socket Mode WebSocket listener
SlackSocketModeListenerCallbacks                   // builder with .with_push_events(...) callback registration
SlackPushEventCallback / SlackPushEvent::EventCallback(SlackEventCallbackBody::Message(...))
SlackMessageEvent { sender, origin, content, ... }  // exact field path varies — confirm via pinned docs above
```

Slack Web API methods (called via `SlackClientSession`):
- `chat_post_message` — send_message
- `chat_update` — edit_message / edit_message_markdown
- `chat_delete` — delete_message
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create slack.rs — SlackAdapter (PlatformAdapter impl) + slack_event_to_message_event</name>
  <files>crates/ironhermes-gateway/src/slack.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/telegram.rs lines 1-9 (imports), lines 68-81 (struct + new), lines 213-298 (PlatformAdapter impl — 7 methods), lines 378-431 (tg_message_to_event analog)
    - crates/ironhermes-gateway/src/adapter.rs (full file — trait signatures)
    - crates/ironhermes-gateway/src/discord.rs (Plan 03 Task 1 — companion adapter for cross-reference; same architectural shape)
    - .planning/phases/34-webchat-and-multi-platform-gateway/34-PATTERNS.md (lines 185-234 — SlackAdapter analog mapping)
    - .planning/phases/34-webchat-and-multi-platform-gateway/34-RESEARCH.md (Pattern 2; Pitfall 2 — two tokens; Pitfall 4 — Socket Mode ACK timing)
    - **Pinned docs source for SlackMessageEvent + SlackPushEventCallback field paths:** https://docs.rs/slack-morphism/2.22.0/slack_morphism/ (specifically the `socket_mode` and `prelude` modules). If unreachable, fall back to Context7 query (`resolve-library-id slack-morphism` + `query-docs`). Both failure modes escalate to the Plan 02 Task 1 human-verify gate — do not improvise field names.
  </read_first>
  <behavior>
    - Module exposes `pub struct SlackAdapter { client: Arc<slack_morphism::SlackClient<slack_morphism::SlackClientHyperConnector>>, bot_token: slack_morphism::SlackApiToken }` and `pub fn slack_event_to_message_event(...) -> ironhermes_core::MessageEvent`.
    - `slack_event_to_message_event` returns MessageEvent with `platform: Platform::Slack`, `chat_id = channel_id (Slack: C0123... or D0123...)`, `sender_id = user_id`, `content = text`, `chat_type = "dm" if channel starts with "D" else "group"` (Slack channel-ID convention), other fields default per MessageEvent.
    - `impl PlatformAdapter for SlackAdapter` implements all 7 methods; `platform()` returns `Platform::Slack`; `is_running()` returns `false`.
    - `send_message` opens a session with bot_token and calls `chat_post_message(channel: chat_id, text: content)`; returns MessageResponse with the returned message timestamp as `message_id`.
    - `edit_message` and `edit_message_markdown` both call `chat_update(channel, ts, text)`. Slack uses Markdown-like "mrkdwn" formatting natively, same code path as Discord (Plan 03).
    - `delete_message` calls `chat_delete(channel, ts)`.
    - `add_reaction` and `send_chat_action` use trait default no-op (Slack supports reactions and typing indicators, but neither is required for Phase 34 Learning Loop parity — deferred).
  </behavior>
  <action>
    Create crates/ironhermes-gateway/src/slack.rs containing:

    (1) Imports per PATTERNS.md lines 190-198:
        ```
        use crate::adapter::PlatformAdapter;
        use anyhow::Result;
        use async_trait::async_trait;
        use ironhermes_core::{MessageEvent, MessageResponse, Platform};
        use slack_morphism::prelude::*;
        use std::sync::Arc;
        use tokio_util::sync::CancellationToken;
        ```

    (2) `pub struct SlackAdapter { client: Arc<SlackClient<SlackClientHyperConnector>>, bot_token: SlackApiToken }` — two fields. The client is reusable across requests; bot_token authorizes send/edit/delete API calls.

    (3) `impl SlackAdapter { pub fn new(client: Arc<SlackClient<SlackClientHyperConnector>>, bot_token: SlackApiToken) -> Self { Self { client, bot_token } } }`.

    (4) `#[async_trait] impl PlatformAdapter for SlackAdapter { ... }` — all 7 methods:
        - `platform() -> Platform::Slack`
        - `send_message` — call `self.client.open_session(&self.bot_token).chat_post_message(&SlackApiChatPostMessageRequest::new(SlackChannelId(chat_id.into()), SlackMessageContent::new().with_text(content.into()))).await?`. The returned `SlackApiChatPostMessageResponse` has a `ts: SlackTs` field → use `ts.to_string()` for MessageResponse.message_id. Confirm exact request/response type names via the pinned docs URL above (slack-morphism 2.22 may use `SlackApiChatPostMessageRequest::new(...)` or a builder; use whichever the published crate exposes).
        - `edit_message` — call `chat_update` with `SlackApiChatUpdateRequest::new(SlackChannelId(chat_id.into()), SlackMessageContent::new().with_text(content.into()), SlackTs(message_id.into()))`.
        - `edit_message_markdown` — delegate to `self.edit_message(chat_id, message_id, content).await` (Slack auto-formats mrkdwn).
        - `delete_message` — call `chat_delete` with channel + ts.
        - `add_reaction`, `send_chat_action` — DO NOT override; use trait defaults.
        - `is_running() -> false`.

    (5) `pub fn slack_event_to_message_event(event: &SlackMessageEvent) -> MessageEvent`. Field mapping (confirm Slack types via pinned docs):
        - platform: Platform::Slack
        - message_id: event.origin.ts.to_string() (Slack message timestamp)
        - chat_id: event.origin.channel.to_string() (Slack channel ID e.g. "C0123ABC" or "D0123ABC")
        - sender_id: event.sender.user.as_ref().map(|u| u.to_string()).unwrap_or_default()
        - content: event.content.as_ref().and_then(|c| c.text.clone()).unwrap_or_default() — Slack message text. If exact field path differs in 2.22, mirror whatever the pinned docs show.
        - attachments: vec![] (text-only Phase 34)
        - thread_id: event.origin.thread_ts.as_ref().map(|t| t.to_string())
        - chat_type: derived helper `classify_slack_channel_type(channel: &str) -> &'static str` — returns "dm" if channel starts with 'D', else "group". Slack channel-ID convention: D=DM, C=public channel, G=private channel/group DM. Extract as `pub(crate) fn` so it is unit-testable without constructing slack-morphism types.
        - chat_name: None (channel name lookup is a separate Web API call — deferred)
        - sender_name: None (user name lookup is a separate Web API call — deferred; sender_id is sufficient for whitelist check)
        - replied_to_id: None (thread parent already captured in thread_id)

    (6) Unit test module `#[cfg(test)] mod tests`:
        - `classify_slack_channel_type_dm_starts_with_d` asserts `classify_slack_channel_type("D0123ABC") == "dm"`
        - `classify_slack_channel_type_channel_starts_with_c` asserts `classify_slack_channel_type("C0123ABC") == "group"`
        - `classify_slack_channel_type_group_starts_with_g` asserts `classify_slack_channel_type("G0123ABC") == "group"`

    Per CLAUDE.md "no error handling for impossible scenarios" — propagate `?` directly without wrapping. Do NOT log tokens (T-34-01).
  </action>
  <verify>
    <automated>cargo build -p ironhermes-gateway 2>&amp;1 | tail -20 &amp;&amp; cargo test -p ironhermes-gateway --lib slack 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/ironhermes-gateway/src/slack.rs` exists
    - `grep -c "impl PlatformAdapter for SlackAdapter" crates/ironhermes-gateway/src/slack.rs` returns 1
    - `grep -c "Platform::Slack" crates/ironhermes-gateway/src/slack.rs` returns >= 2
    - `grep -c "pub fn slack_event_to_message_event\|pub fn classify_slack_channel_type" crates/ironhermes-gateway/src/slack.rs` returns >= 1
    - `cargo build -p ironhermes-gateway` exits 0
    - `cargo test -p ironhermes-gateway --lib slack` exits 0 (>= 3 unit tests pass)
  </acceptance_criteria>
  <done>SlackAdapter struct + impl + event converter compile and unit-test. Channel-type classifier locked. Module not yet exposed via lib.rs — Task 3 wires that.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add run_slack_adapter (Socket Mode listener + cancellation + canonical whitelist + non-blocking ACK with proper move semantics)</name>
  <files>crates/ironhermes-gateway/src/slack.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/slack.rs (file from Task 1 — extend it)
    - crates/ironhermes-gateway/src/handler.rs line 769 (handle_with_multimodal signature)
    - crates/ironhermes-gateway/src/discord.rs (Plan 03 Task 2 — parallel structure: whitelist + cancellation + intent guard; mirrors canonical Telegram empty-whitelist semantics)
    - **crates/ironhermes-gateway/src/runner.rs lines 601-611 (CANONICAL whitelist semantics for cross-platform consistency)**
    - **crates/ironhermes-core/src/config.rs:731 ('Empty = deny all (D-12)' — canonical doc)**
    - .planning/phases/34-webchat-and-multi-platform-gateway/34-RESEARCH.md (Pattern 2; Pitfalls 2 + 4 — two tokens, 3s ACK timing)
    - .planning/phases/34-webchat-and-multi-platform-gateway/34-PATTERNS.md (lines 211-228 — Socket Mode startup)
    - **Pinned docs:** https://docs.rs/slack-morphism/2.22.0/slack_morphism/socket_mode/ — `SlackClientSocketModeListener::new`, `SlackSocketModeListenerCallbacks::with_push_events`, `SlackClientEventsListenerEnvironment`, `listen_for(token)` signatures. The 2.22 callback closure signature is `Fn(SlackPushEventCallback, Arc<SlackHyperClient>, SlackClientEventsUserState) -> impl Future<Output = AnyStdResult<()>>` per crate docs; if the doc page is unavailable, escalate to the Plan 02 Task 1 human-verify gate.
  </read_first>
  <behavior>
    - `pub async fn run_slack_adapter(app_token: &str, bot_token: &str, whitelist: Vec<String>, handler: Arc<GatewayMessageHandler>, cancel: CancellationToken) -> Result<()>`.
    - Constructs `SlackClient::new(SlackClientHyperConnector::new()?)` wrapped in Arc; both tokens converted to SlackApiToken via SlackApiTokenValue.
    - Socket Mode push-events callback: extract the `SlackMessageEvent` from `SlackPushEventCallback`; skip bot messages (Slack tags via `event.sender.bot_id.is_some()` or similar); apply canonical whitelist (empty = deny all per runner.rs:608-611); if allowed, BUILD the MessageEvent + ProcessedAttachments::default() in the callback scope, then MOVE them into `tokio::spawn(async move { ... })` so the callback returns immediately and slack-morphism's Socket Mode ACK fires within 3s (Pitfall 4).
    - `tokio::select!` wraps `socket_mode_listener.listen_for(&app_token_obj)` against `cancel.cancelled()`. Cancel arm returns Ok(()) — listener cleanup is handled by slack-morphism when the listener is dropped.
    - Tokens NEVER passed to tracing macros (T-34-01).
  </behavior>
  <action>
    Append to crates/ironhermes-gateway/src/slack.rs:

    (a) `pub async fn run_slack_adapter(app_token: &str, bot_token: &str, whitelist: Vec<String>, handler: Arc<crate::GatewayMessageHandler>, cancel: CancellationToken) -> Result<()>` body per PATTERNS.md lines 211-228 plus the security additions below.

    (b) Inside the function:
        1. Build `client = Arc::new(SlackClient::new(SlackClientHyperConnector::new()?))`.
        2. Convert tokens: `let app_token_obj = SlackApiToken::new(app_token.into())` and same for `bot_token_obj`.
        3. Create the `SlackAdapter { client: client.clone(), bot_token: bot_token_obj.clone() }` once and wrap in `Arc::new(...)` — reused across all push events.
        4. Build callbacks via `SlackSocketModeListenerCallbacks::new().with_push_events(...)`. The closure body MUST:
            - Pattern match the event to extract `SlackMessageEvent` (skip non-message events with early Ok return — Socket Mode delivers many event types)
            - Skip bot messages via the appropriate sender check (`event.sender.bot_id.is_some()` — verify exact field via pinned docs)
            - Canonical whitelist gate (mirrors Telegram runner.rs:601-611 verbatim, but with String IDs):
              ```
              if !whitelist.is_empty() {
                  if !whitelist.contains(&sender_id_string) {
                      tracing::warn!(sender_id = %sender_id_string, "Sender not in whitelist, ignoring");
                      return Ok(());
                  }
              } else {
                  tracing::warn!("Whitelist is empty — denying all messages (D-12)");
                  return Ok(());
              }
              ```
            - Convert via `slack_event_to_message_event(&event)`.
            - Spawn handler dispatch with MOVE semantics — construct the event BEFORE spawn and move it into the `async move` block (do NOT borrow `&event` into the spawned future; `event` may not outlive the spawn). Example structure:
              ```
              let msg_event = slack_event_to_message_event(&event);
              let h = handler.clone();
              let a: Arc<dyn PlatformAdapter> = adapter.clone();
              let c = cancel.child_token();
              tokio::spawn(async move {
                  if let Err(e) = h.handle_with_multimodal(&msg_event, a, c, ProcessedAttachments::default()).await {
                      tracing::error!("Slack handler error: {e:#}");
                  }
              });
              ```
              The `msg_event` binding is created in callback scope, then moved into the async block. `h`, `a`, `c` are all owned values (Arc clones + child token). NOTHING is borrowed across the spawn boundary — the borrow checker is satisfied because every captured value implements `Send + 'static`.
            - Closure returns `Ok(())` immediately after spawn.
        5. Build listener environment via `SlackClientEventsListenerEnvironment::new(client.clone()).with_error_handler(|err, _, _| { tracing::warn!("Slack socket mode error: {err}"); false })`.
        6. Build listener via `SlackClientSocketModeListener::new(&SlackClientSocketModeConfig::new(), env, callbacks)`.
        7. `tokio::select! { result = listener.listen_for(&app_token_obj) => result.map_err(|e| anyhow::anyhow!("Slack listener: {e}")), _ = cancel.cancelled() => Ok(()) }`.

    (c) Log a single startup line: `tracing::info!("Slack Socket Mode adapter starting (app_token: xapp-...redacted, bot_token: xoxb-...redacted)");` — NEVER include the actual token strings.

    Per the pinned docs at https://docs.rs/slack-morphism/2.22.0/slack_morphism/socket_mode/, the exact slack-morphism 2.22 callback closure signature is `Fn(SlackPushEventCallback, Arc<SlackHyperClient>, SlackClientEventsUserState) -> impl Future<Output = AnyStdResult<()>>`. If at implementation time the published 2.22.0 API differs from these docs, escalate via the Plan 02 Task 1 supply-chain checkpoint (WARNING 4 closure backstop) rather than guessing the closure shape.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-gateway 2>&amp;1 | tail -20 &amp;&amp; cargo clippy -p ironhermes-gateway --lib -- -D clippy::await_holding_lock 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub async fn run_slack_adapter" crates/ironhermes-gateway/src/slack.rs` returns 1
    - `grep -c "listen_for" crates/ironhermes-gateway/src/slack.rs` returns >= 1 (Socket Mode startup)
    - `grep -c "tokio::spawn" crates/ironhermes-gateway/src/slack.rs` returns >= 1 (non-blocking ACK)
    - `grep -c "async move" crates/ironhermes-gateway/src/slack.rs` returns >= 1 (proper move semantics — borrow-checker safety)
    - `grep -c "cancel.cancelled" crates/ironhermes-gateway/src/slack.rs` returns >= 1 (shutdown)
    - `grep -c "whitelist" crates/ironhermes-gateway/src/slack.rs` returns >= 2 (param + check)
    - `grep -c "denying all messages (D-12)" crates/ironhermes-gateway/src/slack.rs` returns >= 1 (canonical empty-whitelist deny log)
    - `grep -c "handle_with_multimodal" crates/ironhermes-gateway/src/slack.rs` returns >= 1 (THE routing call — required to flip INV-34-02 GREEN)
    - Token strings never passed to tracing: `grep -E 'tracing::(info|warn|debug|error|trace)!.*\b(app_token|bot_token)\b' crates/ironhermes-gateway/src/slack.rs` returns 0 lines (parameter names allowed in comments, NOT in macro args)
    - `cargo build -p ironhermes-gateway` exits 0
    - `cargo clippy -p ironhermes-gateway --lib -- -D clippy::await_holding_lock` exits 0
  </acceptance_criteria>
  <done>run_slack_adapter compiles, clippy clean, Socket Mode ACK timing protected by tokio::spawn dispatch with proper async-move semantics, canonical-whitelist deny-all enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire slack module in lib.rs; INV-34-02 flips GREEN (after Plan 03's lib.rs edit lands)</name>
  <files>crates/ironhermes-gateway/src/lib.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/lib.rs (current state — Plan 03's `pub mod discord;` edit has already landed per depends_on: [34-02, 34-03])
    - .planning/phases/34-webchat-and-multi-platform-gateway/34-PATTERNS.md (lines 314-339)
  </read_first>
  <behavior>
    - `pub mod slack;` declared alongside `pub mod discord;` (already present from Plan 03 — depends_on serializes the edits).
    - `pub use slack::{SlackAdapter, run_slack_adapter};` re-export.
    - INV-34-02 (`inv_34_02_slack_routes_through_handle_with_multimodal` from Plan 01 Task 2) compiles and PASSES.
  </behavior>
  <action>
    In crates/ironhermes-gateway/src/lib.rs:
    (a) Add `pub mod slack;` in alphabetical position (after `pub mod runner;`, before/around `pub mod session;` — match existing ordering).
    (b) Add `pub use slack::{SlackAdapter, run_slack_adapter};` near the existing re-exports, alphabetical.

    Plan 03's `pub mod discord;` is already in place per the depends_on serialization. Add only the slack delta — leave Plan 03's lines untouched.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-gateway 2>&amp;1 | tail -5 &amp;&amp; cargo test -p ironhermes-gateway --test invariants_34 inv_34_02_slack_routes_through_handle_with_multimodal 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub mod slack" crates/ironhermes-gateway/src/lib.rs` returns 1
    - `grep -c "slack::SlackAdapter\|slack::run_slack_adapter" crates/ironhermes-gateway/src/lib.rs` returns >= 1
    - `cargo build -p ironhermes-gateway` exits 0
    - `cargo test -p ironhermes-gateway --test invariants_34 inv_34_02_slack_routes_through_handle_with_multimodal` exits 0
    - Once BOTH Plan 03 and Plan 04 are complete: `cargo test -p ironhermes-gateway --test invariants_34` runs both inv_34_01 AND inv_34_02 GREEN (Wave 2 exit gate)
  </acceptance_criteria>
  <done>Slack adapter publicly exposed; INV-34-02 green. Wave 2 fully green when paired with Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slack Socket Mode WebSocket ↔ run_slack_adapter callback | Untrusted user-controlled message text crosses here |
| Socket Mode callback ↔ tokio::spawn → GatewayMessageHandler | Authenticated, allowlisted MessageEvent crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Information Disclosure | Two-token logging (app_token + bot_token) | mitigate | Both tokens redacted in startup log; grep gate in Task 2 acceptance asserts no tracing macro contains `app_token`/`bot_token` identifiers |
| T-34-02 | Elevation of Privilege | Unauthorized Slack sender | mitigate | Push-event callback applies canonical Telegram semantics from runner.rs:601-611: empty whitelist = warn + deny all (D-12); non-empty whitelist drops senders absent from list. Grep-asserted by literal "denying all messages (D-12)" log string |
| T-34-04 | Spoofing | Slack event replay / ACK timeout | mitigate | Callback dispatches handler call via `tokio::spawn(async move { ... })` (msg_event constructed before spawn and moved in — borrow-checker safe) and returns Ok(()) immediately, ensuring slack-morphism's internal ACK fires < 3s. Grep gate on `tokio::spawn` and `async move` counts >= 1 in Task 2 acceptance |
| T-34-05 | Spoofing | Session key collision across platforms | mitigate | slack_event_to_message_event sets Platform::Slack; downstream SessionKey(Platform::Slack, chat_id) discriminates from Discord/Telegram chat IDs |
| T-34-PITFALL-2 | Authentication | Single-token Slack config | mitigate | run_slack_adapter requires TWO &str args (app_token, bot_token); PlatformGatewayConfig.app_token added in Plan 02; Plan 05 wires both fields. Wrong-token-count = compile error, not runtime failure |
| T-34-PITFALL-3 | Tampering | Mutex guard across .await | mitigate | Adapter holds no Mutex; clippy `-D await_holding_lock` gate in Task 2 acceptance |
| T-34-SUPPLY | Tampering | slack-morphism 2.22 closure signature drift | mitigate | Pinned docs URL in <read_first>; Context7 fallback; Plan 02 Task 1 blocking human-verify gate as final backstop (WARNING 4 closure) |
</threat_model>

<verification>
- `cargo build -p ironhermes-gateway` clean
- `cargo clippy -p ironhermes-gateway --lib -- -D clippy::await_holding_lock` clean
- `cargo test -p ironhermes-gateway --test invariants_34 inv_34_02_*` green
- `cargo test -p ironhermes-gateway --lib slack` green (>= 3 unit tests on classify_slack_channel_type)
- Wave 2 exit gate: `cargo test -p ironhermes-gateway --test invariants_34` runs both 34-01 and 34-02 green
</verification>

<success_criteria>
- slack.rs exists with SlackAdapter + run_slack_adapter (Socket Mode listener + tokio::spawn ACK pattern with proper move semantics)
- All 7 PlatformAdapter methods implemented; platform() returns Platform::Slack
- Two-token shape enforced at function signature (app_token + bot_token)
- Canonical-whitelist deny-all enforced (mirrors Telegram runner.rs:608-611); tokens never logged (T-34-01/02/04 mitigations live)
- INV-34-02 flips RED → GREEN
- Wave 2 exit: both INV-34-01 (Plan 03) and INV-34-02 (Plan 04) green
- Plan 04 SUMMARY.md notes any deltas from PATTERNS.md slack-morphism sketch (likely: exact callback closure signature confirmed against pinned docs)
</success_criteria>

<output>
Create `.planning/phases/34-webchat-and-multi-platform-gateway/34-04-SUMMARY.md` when done.
</output>
