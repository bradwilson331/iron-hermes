---
phase: 26.1-fix-websocket-error-for-chat
plan: 02
subsystem: ui
tags: [dioxus, websocket, feature-gating, protocol, parity-tests]
requires:
  - phase: 26.1-01
    provides: websocket lifecycle behavior (D-01..D-15) for server/client chat flow
provides:
  - Feature-gated websocket server runtime boundaries for default web builds
  - Client websocket protocol linkage to crate::protocol type definitions
  - Regression parity assertions for feature boundary and lifecycle invariants
affects: [manual-uat, websocket-chat, server-runtime-parity]
tech-stack:
  added: []
  patterns:
    - cfg(feature = "server") gates around server-only websocket runtime dependencies
    - client protocol type usage anchored to crate::protocol instead of server internals
key-files:
  created:
    - .planning/phases/26.1-fix-websocket-error-for-chat/26.1-02-SUMMARY.md
  modified:
    - crates/iron_hermes_ui/src/server/ws.rs
    - crates/iron_hermes_ui/src/components/warp_hermes.rs
    - crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs
key-decisions:
  - "Keep ws_chat available in both build modes while isolating runtime-only execution inside cfg(feature = \"server\") branches."
  - "Make crate::protocol the only client-side source for ChatRequest/ChatStreamEvent to prevent cross-feature coupling."
patterns-established:
  - "Websocket route boundary pattern: cfg-gate imports + execution path, keep non-server branch symbol-safe."
  - "Parity tests must lock both positive protocol paths and negative anti-coupling checks."
requirements-completed: []
duration: 3min
completed: 2026-05-04
---

# Phase 26.1 Plan 02: Websocket Feature Boundary Hardening Summary

**Websocket server runtime is now feature-gated for default web builds while client chat serialization is pinned to shared `crate::protocol` types and locked by parity tests.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-05-04T14:56:05Z
- **Completed:** 2026-05-04T14:58:51Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Added strict `#[cfg(feature = "server")]` guards for server-only websocket runtime imports and execution branches in `ws_chat`.
- Replaced client websocket protocol references from `crate::server::ws::*` to `crate::protocol::*` in `warp_hermes.rs`.
- Extended `websocket_lifecycle_parity.rs` with boundary-lock assertions for cfg anchors and protocol-path anti-regressions.

## Task Commits

Each task was committed atomically:

1. **Task 1: Isolate server-only websocket runtime from default web build** - `18d7ac6` (fix)
2. **Task 2: Lock feature-boundary fix and lifecycle invariants with parity tests** - `b07ce42` (test)

## Files Created/Modified
- `crates/iron_hermes_ui/src/server/ws.rs` - feature-gated server-only websocket runtime path with non-server-safe fallback branch.
- `crates/iron_hermes_ui/src/components/warp_hermes.rs` - client websocket request/event handling now bound to `crate::protocol` types.
- `crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs` - static source assertions for cfg boundaries, protocol linkage, and lifecycle anchors.

## Decisions Made
- Preserved `/api/ws/chat` route availability across build modes while keeping server runtime symbols inaccessible in non-server compilation paths.
- Kept lifecycle behavior (reconnect reset and in-flight forwarding) unchanged and encoded boundary expectations as deterministic source-inspection tests.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Default and server-feature build checks pass for `iron_hermes_ui`.
- Websocket boundary and lifecycle invariants are regression-locked and ready for manual UAT rerun.

## Self-Check: PASSED

- FOUND: `.planning/phases/26.1-fix-websocket-error-for-chat/26.1-02-SUMMARY.md`
- FOUND commit: `18d7ac6`
- FOUND commit: `b07ce42`
