---
phase: 26.1-fix-websocket-error-for-chat
plan: 01
subsystem: websocket
tags: [websocket, dioxus, lifecycle, reconnect, streaming]
requires:
  - phase: 25.6-replicate-cli-web-wiring
    provides: startup-scoped AppState and web chat websocket baseline
provides:
  - Concurrent server-side websocket streaming while a turn is in flight
  - One in-flight request gate with clean abort-on-disconnect behavior
  - Reconnect-resilient Dioxus receiver loop with transient UI-state reset
  - Source-parity regression tests for server/client websocket lifecycle invariants
affects: [chat-transport, web-ui-streaming, websocket-reliability]
tech-stack:
  added: [tracing (workspace) in iron_hermes_ui]
  patterns: [spawned turn task + forwarded mpsc stream, reconnect receive lifecycle loop]
key-files:
  created:
    - crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs
  modified:
    - crates/iron_hermes_ui/src/server/ws.rs
    - crates/iron_hermes_ui/src/components/warp_hermes.rs
    - crates/iron_hermes_ui/Cargo.toml
key-decisions:
  - Keep terminal websocket event ownership in the spawned server turn task and forward events concurrently via channel drain.
  - Treat client disconnect boundaries as recoverable: preserve partial transcript, reset transient stream pointers, and retry connect/receive lifecycle.
patterns-established:
  - "Websocket turn lifecycle: explicit local in-flight gate in ws_chat with abort on send/recv failure"
  - "UI reconnect handling: reset scanner_active + streaming_block_id on disconnect without clearing already-rendered blocks"
requirements-completed: []
duration: 4m
completed: 2026-05-04
---

# Phase 26.1 Plan 01: Websocket Lifecycle Stabilization Summary

**Concurrent server turn streaming plus reconnect-resilient Dioxus receive lifecycle for `/api/ws/chat` with parity tests locking lifecycle invariants.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-04T10:29:32-04:00
- **Completed:** 2026-05-04T10:33:28-04:00
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- Refactored `ws_chat` to run turns in spawned tasks and forward websocket events while the turn is executing.
- Added one-in-flight socket gate, malformed-frame recovery continuation, and abort-on-send-failure cleanup with warn telemetry.
- Hardened client websocket lifecycle with reconnect loop, disconnect state reset, and transcript-visible generic disconnect error block.
- Added lifecycle parity tests that lock server/client websocket invariants.

## Task Commits

Each task was committed atomically:

1. **Task 1: Refactor server websocket loop to stream while turn is executing**
   - `53f26c9` (test) RED: failing server lifecycle parity assertions
   - `d42e09e` (feat) GREEN: concurrent spawned turn execution + forwarding + abort handling
2. **Task 2: Make Dioxus websocket receiver lifecycle reconnect-resilient**
   - `9606126` (test) RED: failing reconnect lifecycle assertions
   - `bd91232` (feat) GREEN: reconnect receive loop + transient state reset + disconnect UI notice
3. **Task 3: Add websocket lifecycle parity regression tests**
   - `868b876` (test) finalized parity invariants for route, server forwarding, and client reconnect anchors

## Files Created/Modified

- `crates/iron_hermes_ui/src/server/ws.rs` - concurrent turn lifecycle, in-flight gate, send/recv failure abort behavior
- `crates/iron_hermes_ui/src/components/warp_hermes.rs` - reconnect-resilient receiver lifecycle with disconnect state reset
- `crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs` - static source parity tests for websocket lifecycle invariants
- `crates/iron_hermes_ui/Cargo.toml` - added `tracing` workspace dependency used by websocket telemetry

## Decisions Made

- Kept protocol and route contracts unchanged (`ChatRequest`, `ChatStreamEvent`, `#[get("/api/ws/chat")]`) while changing only lifecycle ordering and error handling.
- Preserved partial streamed AI text across disconnect boundaries by only resetting transient stream pointers.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added missing telemetry dependency for server warn logging**
- **Found during:** Task 1
- **Issue:** `tracing::warn!` usage in `ws.rs` failed to compile because `iron_hermes_ui` did not depend on `tracing`.
- **Fix:** Added `tracing = { workspace = true }` to `crates/iron_hermes_ui/Cargo.toml`.
- **Files modified:** `crates/iron_hermes_ui/Cargo.toml`
- **Verification:** `cargo test -p iron_hermes_ui --features server --test websocket_lifecycle_parity -- --nocapture`
- **Committed in:** `d42e09e`

---

**Total deviations:** 1 auto-fixed (Rule 3: blocking)
**Impact on plan:** Required for compile correctness; no scope creep.

## Issues Encountered

- Initial websocket parity anchor expected old forwarding loop text; adjusted source parity anchors to match new `tokio::select!` lifecycle shape.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Web chat websocket transport now streams concurrently and survives reconnect boundaries.
- Lifecycle regressions are guarded by deterministic source-parity tests.

## Self-Check: PASSED
