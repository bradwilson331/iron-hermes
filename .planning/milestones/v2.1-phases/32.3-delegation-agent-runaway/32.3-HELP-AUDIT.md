---
phase: 32.3
audit_type: help_discoverability
audit_date: 2026-05-17
fixes_in_phase: false
b3_carve_out: "/agents interrupt|prune|status help entries added by Plan 03 (build_registry)"
followup_destination: future_doc_polish_phase
plan: "05"
surfaces_audited: [tui_rata, iron_hermes_ui, gateway]
commands_audited:
  - "/agents (bare — list/tree)"
  - "/agents kill <id>"
  - "/agents interrupt <id>"
  - "/agents prune"
  - "/agents status <id>"
related_summaries:
  - 32.3-01-SUMMARY.md  # RegistrationGuard RAII
  - 32.3-02-SUMMARY.md  # Stale detection + tracker wiring
  - 32.3-03-SUMMARY.md  # ShrikeService + B3 build_registry additions
---

# Phase 32.3 Plan 05 — /help Discoverability Audit

## Audit Scope

This audit inspects how `/help` discoverability of the `/agents` family renders across the three operator-facing surfaces — `tui_rata` (in-process ratatui REPL), `iron_hermes_ui` (web), and `gateway/Telegram` — after Phase 32.3 Plan 03's B3 carve-out added three new sibling `CommandDef` entries to `build_registry()` (`agents interrupt`, `agents prune`, `agents status`).

Commands audited: bare `/agents` (list/tree), `/agents kill <id>`, `/agents interrupt <id>`, `/agents prune`, `/agents status <id>` — five commands × three surfaces = **15 audit rows**.

Per D-11 / D-12 / B3: this is an audit-only deliverable. Gaps are recorded as TODO-tagged follow-up items for a future doc-polish phase. The ONLY help-output fixes that landed in Phase 32.3 are the three new-subcommand `CommandDef` entries added by Plan 03 to `crates/ironhermes-core/src/commands/registry.rs` (lines 50–69); those are implementation work delivered in Plan 03, not audit work delivered here.

## Methodology

- Read `crates/ironhermes-core/src/commands/registry.rs` `build_registry()` (the single source of truth for all `/help` output across surfaces).
- Read `crates/ironhermes-core/src/commands/handlers.rs` `cmd_help` (lines 1113–1143) — confirms `/help` output is generated identically for every surface from the shared `CommandRouter` filtered by `Platform`.
- Read each surface adapter for surface-specific overrides:
  - `crates/ironhermes-cli/src/tui_rata/commands.rs` (line 662 doc-comment confirms: "new CommandDef added to `build_registry()` automatically surfaces in /help" — no TUI-side overrides).
  - `crates/iron_hermes_ui/src/server/state.rs` (uses `Platform::Web` — no separate `/help` text; routes through the shared `CommandRouter`).
  - `crates/ironhermes-gateway/src/handler.rs` (uses `Platform::Telegram`; explicit regression test `handler_does_not_have_hardcoded_help_text` at line 1454 locks the no-hardcoded-help invariant).
- For each subcommand on each surface, record:
  - **listed?** — does `/help` mention the command/subcommand?
  - **accurate?** — does the listed description / args_hint match real Plan 01–04 behavior?
  - **gap_severity** — `none` (listed + accurate), `minor` (listed but description stale or missing a Phase 32.3 nuance), `major` (not listed at all).
- Do NOT modify any `/help` source. The B3 carve-out is verified-only: re-confirm Plan 03's CommandDef additions are in `registry.rs`.

## Gap Summary

| Severity | Count | Description |
|----------|-------|-------------|
| none     | 9     | All three surfaces × {bare `/agents`, `/agents interrupt`, `/agents status`} render with discoverable + accurate descriptions. |
| minor    | 6     | All three surfaces × {`/agents kill`, `/agents prune`} — `kill` output text ("Cancelled subagent {}.") drifts from the description verb ("kill"); `prune` /help row carries no `[confirm]` hint for Telegram. Telegram's D-09 confirm-token requirement (Plan 04 scope) is not yet surfaced in `args_hint` on any surface. |
| major    | 0     | No subcommands are completely unlisted on any surface — Plan 03's B3 carve-out closed every "not listed" gap. |

**Total: 15 audit rows. 9 clean, 6 minor follow-up todos, 0 major gaps.**

## Full Audit Table

| Surface         | Command                  | Listed? | Accurate? | Gap Severity | Notes / Follow-up Todo |
|-----------------|--------------------------|---------|-----------|--------------|-------------------------|
| tui_rata        | /agents (bare)           | yes     | yes       | none         | bare `/agents` lists/trees subagents — description "List, kill, interrupt, prune, or inspect active subagents" is accurate; args_hint `[list\|kill <id>\|interrupt <id>\|prune\|status <id>\|logs <id>]` enumerates the full surface. |
| tui_rata        | /agents kill <id>        | yes     | partial   | minor        | FUTURE: Cmd_agents kill arm at handlers.rs:193 emits "Cancelled subagent {}." but the registry description says "kill" — operator looking for the word "Killed" in output may be confused. Consider aligning output verb with command verb, or update help description to say "Kill (cancel) a subagent". |
| tui_rata        | /agents interrupt <id>   | yes     | yes       | none         | Plan 03 B3: registry.rs:50–56 lists with description "Soft-cancel a subagent (finalizes current iteration)" and args_hint "<id>" — matches handler arm at handlers.rs:262 emitting "Interrupted {} — finalizing...". |
| tui_rata        | /agents prune            | yes     | partial   | minor        | FUTURE: Plan 03 B3 lists with description "Sweep registry entries idle longer than the stale threshold" — accurate, but no mention that the threshold defaults to 120s (`PRUNE_DEFAULT_STALE_SECS` per 32.3-03-SUMMARY) and is currently NOT operator-tunable per-call (Plan 03 Deviation D-2). Consider extending description to: "Sweep registry entries idle > 120s (configured by delegation.stale_warn_seconds)". |
| tui_rata        | /agents status <id>      | yes     | yes       | none         | Plan 03 B3: description "Show diagnostic info for one active subagent", args_hint "<id>" — matches the render_status_kv key/value block at handlers.rs:349. |
| iron_hermes_ui  | /agents (bare)           | yes     | yes       | none         | Same `Platform::Web` filter passes Universal entries; web surface also routes /help through the shared CommandRouter (state.rs has no /help override). Description and args_hint identical to tui_rata. |
| iron_hermes_ui  | /agents kill <id>        | yes     | partial   | minor        | FUTURE: same "Cancelled subagent" vs "kill" verb mismatch as tui_rata. Web surface additionally calls `ShrikeService::kill` directly via `SubagentRegistryHandle` trait override (32.3-03-SUMMARY W3) so output text matches tui_rata. |
| iron_hermes_ui  | /agents interrupt <id>   | yes     | yes       | none         | Plan 03 B3 entry listed; web routes through identical handler arm. |
| iron_hermes_ui  | /agents prune            | yes     | partial   | minor        | FUTURE: same threshold-not-tunable note as tui_rata. Web surface could also benefit from a future REST endpoint variant (per 32.3-RESEARCH §Pattern 5) — but that is Plan 04 + future doc-polish scope, not this audit. |
| iron_hermes_ui  | /agents status <id>      | yes     | yes       | none         | Plan 03 B3 entry listed; web surface routes through identical handler arm. JSON shape for web rendering is reserved on `SubagentStatusInfo` (serde::Serialize derive per 32.3-03-SUMMARY Task 1b) — already wired for future `/api/agents/status` endpoint. |
| gateway         | /agents (bare)           | yes     | yes       | none         | Telegram /help filters Universal entries; bare /agents shows the same description and args_hint as TUI. Regression test `handler_does_not_have_hardcoded_help_text` (handler.rs:1454) locks no-hardcoded-help invariant. |
| gateway         | /agents kill <id>        | yes     | partial   | minor        | FUTURE: D-09 confirm-token contract (Plan 04 scope) requires `/agents kill <id> confirm` on Telegram, but `args_hint` reads `[...\|kill <id>\|...]` — no `[confirm]` suffix for the Telegram filter. Consider Telegram-specific args_hint variant or appending `[confirm]` (gateway-conditional). Also inherits the "Cancelled" vs "kill" verb mismatch. |
| gateway         | /agents interrupt <id>   | yes     | yes       | none         | Plan 03 B3 entry; D-09 does NOT require confirm for interrupt (non-destructive), so args_hint `<id>` is accurate on Telegram. |
| gateway         | /agents prune            | yes     | partial   | minor        | FUTURE: D-09 confirm-token contract requires `/agents prune confirm` on Telegram, but the Plan 03 B3 CommandDef carries no args_hint at all. Consider Telegram-conditional args_hint of `[confirm]`. Inherits the threshold-not-tunable note as well. |
| gateway         | /agents status <id>      | yes     | yes       | none         | Plan 03 B3 entry listed; D-09 does NOT require confirm for status (non-destructive). args_hint "<id>" is accurate on Telegram. |

## B3 Verification

Plan 03 (`32.3-03-SUMMARY.md`) reports adding three new sibling `CommandDef` entries to `build_registry()` for the new `/agents` subcommands as the D-12 carve-out (B3). Verifying:

```text
$ grep -c '"agents interrupt"\|"agents prune"\|"agents status"' \
    crates/ironhermes-core/src/commands/registry.rs
3
```

**Result: 3 CommandDef entries present — B3 carve-out landed correctly. No Plan 03 regression.** The three entries are at registry.rs lines 50–56 (`agents interrupt`), 57–62 (`agents prune`), and 63–69 (`agents status`). All three carry `PlatformFilter::Universal` so they surface on TUI, web, and Telegram /help output uniformly.

The bare `agents` entry (lines 39–45) was also updated by Plan 03 to extend its `args_hint` to `[list|kill <id>|interrupt <id>|prune|status <id>|logs <id>]` — verified.

## Follow-up Todos

These TODO-tagged items are the audit output a future doc-polish phase will pick up. Each row above with `gap_severity >= minor` produces one entry below.

- **FUTURE-32.3-AUDIT-01** (`/agents kill` verb mismatch, all surfaces): `cmd_agents` kill arm at `handlers.rs:193` emits `"Cancelled subagent {}."` but the registry description starts with "List, **kill**, ..." (and `KillResult::Display` from `shrike.rs` says `Killed sub_xxxx (ran NNNs, NN turns)`). The handler should either route through `ShrikeService::kill -> KillResult::Display` to render `Killed sub_xxx (ran NNNs, NN turns)`, or update the registry description to explicitly say "Cancel a subagent". Surfaces affected: tui_rata, iron_hermes_ui, gateway.
- **FUTURE-32.3-AUDIT-02** (`/agents prune` threshold not surfaced, all surfaces): Plan 03 Deviation D-2 hard-coded `PRUNE_DEFAULT_STALE_SECS = 120` because `CommandContext` has no `delegation_stale_warn_seconds()` accessor yet. The /help description should mention the default (e.g. "Sweep entries idle > 120s") so operators know the threshold without reading source. Bonus: surface a `--stale-secs <N>` per-call override once a CommandContext accessor lands.
- **FUTURE-32.3-AUDIT-03** (`/agents kill <id>` Telegram confirm-token absent from `args_hint`): D-09 requires the literal `confirm` token as the LAST argument on Telegram for destructive ops. The bare `agents` `args_hint` enumerates `kill <id>` without the `[confirm]` suffix. Consider a Telegram-conditional `args_hint` (e.g. `kill <id> [confirm — Telegram only]`) so Telegram operators see the confirm requirement in /help. Documented in `docs/DELEGATION.md` Gateway Confirm Token section (Plan 05 Task 1), but /help does not surface it. Surface affected: gateway.
- **FUTURE-32.3-AUDIT-04** (`/agents prune` Telegram confirm-token absent from `args_hint`): The Plan 03 B3 `CommandDef` for `agents prune` has no `args_hint`. On Telegram, D-09 requires `/agents prune confirm`. Consider Telegram-conditional `args_hint` of `[confirm — Telegram only]` so operators discover the confirm requirement without reading docs. Surface affected: gateway.
- **FUTURE-32.3-AUDIT-05** (web surface alternative — REST endpoints): Per 32.3-RESEARCH §Pattern 5 Question 2, iron_hermes_ui's /help inherits the slash registry, but REST endpoint variants (e.g. `/api/agents/kill`, `/api/agents/prune`) would also be discoverable to web-only operators. This is forward-looking — Plan 04 Wave 4 (running in parallel) wires the surface adapters but does not add REST endpoint discoverability to /help. Track for future iron_hermes_ui doc-polish.
- **FUTURE-32.3-AUDIT-06** (web surface /agents kill verb mismatch — same as 01): Web inherits the same `"Cancelled subagent {}."` output as tui_rata via the shared handler. Closing FUTURE-32.3-AUDIT-01 also closes this. Listed separately so a future planner does not miss the surface count.

## References

- `.planning/phases/32.3-delegation-agent-runaway/32.3-CONTEXT.md` — D-11 audit scope, D-12 audit-only mandate (gaps recorded, not fixed in 32.3), D-09 gateway confirm-token contract.
- `.planning/phases/32.3-delegation-agent-runaway/32.3-RESEARCH.md` — Pattern 6 (cmd_agents Extension), Open Questions §2 (web REST vs slash dispatch).
- `.planning/phases/32.3-delegation-agent-runaway/32.3-03-PLAN.md` — B3 implementation source for the three new `/agents` subcommand CommandDef entries.
- `.planning/phases/32.3-delegation-agent-runaway/32.3-03-SUMMARY.md` — Plan 03 records B3 verification at registry.rs lines 50–69; Deviation D-2 records the `PRUNE_DEFAULT_STALE_SECS = 120` hard-coding.
- `docs/DELEGATION.md` — Phase 32.3 Plan 05 Task 1 extension; Gateway Confirm Token subsection documents D-09; Shrike Service section documents the four-subcommand surface; Stale-Child Detection section documents stale_warn_seconds semantics; The 6.7-Hour Ghost Bug section documents the RAII guard fix.
- `crates/ironhermes-core/src/commands/registry.rs` lines 39–69 — the canonical /help text source for the `/agents` family.
- `crates/ironhermes-core/src/commands/handlers.rs` lines 168–347 — `cmd_agents` handler arms emitting actual output strings.
- `crates/ironhermes-gateway/src/handler.rs` line 1454 — `handler_does_not_have_hardcoded_help_text` regression test that locks no-hardcoded-help invariant.
