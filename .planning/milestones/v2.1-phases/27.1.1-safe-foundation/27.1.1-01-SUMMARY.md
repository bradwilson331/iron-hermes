---
phase: 27.1.1-safe-foundation
plan: "01"
subsystem: ironhermes-tools
tags: [tool-trait, session-lifecycle, hexapod, safety, registry]
dependency_graph:
  requires: []
  provides: [Tool::on_session_end, ToolRegistry::call_session_end_hooks]
  affects: [crates/ironhermes-tools/src/registry.rs]
tech_stack:
  added: []
  patterns: [default-trait-method, sync-fire-and-forget-hook]
key_files:
  created: []
  modified:
    - crates/ironhermes-tools/src/registry.rs
decisions:
  - "on_session_end is synchronous fn (not async fn) so Box<dyn Tool> object-safety is preserved; overrides use tokio::spawn internally (D-14)"
  - "call_session_end_hooks iterates self.tools (not intercepts) — intercept handlers are not Tools and don't need lifecycle callbacks"
metrics:
  duration: "3 minutes"
  completed: "2026-05-10"
  tasks: 1
  files: 1
---

# Phase 27.1.1 Plan 01: Tool Session-End Hook Foundation Summary

Two surgical splices in `crates/ironhermes-tools/src/registry.rs` add a synchronous `fn on_session_end(&self) {}` default to the Tool trait (D-13) and a `pub fn call_session_end_hooks(&self)` helper to ToolRegistry (D-15) — the complete API contract that Plans 02 and 04 consume for hexapod safety halt.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Splice `on_session_end` default into Tool trait + add `call_session_end_hooks` to ToolRegistry | 44e67400 | crates/ironhermes-tools/src/registry.rs |

## Verification Results

- `cargo check -p ironhermes-tools`: clean (1 pre-existing dead_code warning, unchanged)
- `cargo test -p ironhermes-tools --lib`: 304 passed, 0 failed
- `cargo check --workspace`: clean (pre-existing warnings only, zero new)
- AC1 — `fn on_session_end(&self) {}` non-comment count: **1** (pass)
- AC2 — `pub fn call_session_end_hooks` non-comment count: **1** (pass)
- AC3 — `for tool in self.tools.values()` non-comment count: **1** (pass)
- AC4 — `async fn execute` still present: **15 matches** (pass)
- AC5 — `async fn on_session_end` count: **0** (pass — stays sync)

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None. Both methods have real implementations: the trait default is a correct no-op body; the registry helper correctly iterates all registered tools.

## Threat Flags

None. This change adds no network endpoints, auth paths, file access patterns, or schema changes at trust boundaries.

## Self-Check: PASSED

- SUMMARY.md: FOUND
- Commit 44e67400: FOUND
- on_session_end in registry.rs: FOUND
- call_session_end_hooks in registry.rs: FOUND
