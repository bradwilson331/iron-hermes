---
phase: 27.1.1-safe-foundation
plan: "04"
subsystem: ironhermes-tools
tags: [hexapod, tcp, registry, tool-registration, hxp-tool-01]
dependency_graph:
  requires: [27.1.1-02, 27.1.1-03]
  provides: [HexapodTcpTool registered in ToolRegistry]
  affects:
    - crates/ironhermes-tools/src/registry.rs
    - crates/ironhermes-tools/src/lib.rs
tech_stack:
  added: []
  patterns: [register-defaults-unconditional, is_available-gating, unit-struct-registration]
key_files:
  created: []
  modified:
    - crates/ironhermes-tools/src/registry.rs
decisions:
  - "HexapodTcpTool registered in register_defaults() unconditionally — is_available() returns false when HEXAPOD_IP is unset, so the tool is hidden from the LLM schema without extra wiring"
  - "pub mod hexapod_tcp already present in lib.rs from Plan 02 Rule 3 deviation — Task 1 verified and skipped the splice (idempotent)"
  - "Browser integration test failures (3 tests) are pre-existing chromium SingletonLock infrastructure issues unrelated to this plan"
metrics:
  duration: "6 minutes"
  completed: "2026-05-10"
  tasks: 2
  files: 1
---

# Phase 27.1.1 Plan 04: Register HexapodTcpTool in ToolRegistry Summary

Two-line registry wiring that makes HexapodTcpTool discoverable by every IronHermes invocation path — `use crate::hexapod_tcp::HexapodTcpTool;` import plus `self.register(Box::new(HexapodTcpTool));` call inside `register_defaults()`, gated by `is_available()` when `HEXAPOD_IP` is unset.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Verify `pub mod hexapod_tcp;` in lib.rs (already present from Plan 02) | — (idempotent, no commit needed) | crates/ironhermes-tools/src/lib.rs (verified only) |
| 2 | Register HexapodTcpTool inside ToolRegistry::register_defaults() | 0210c245 | crates/ironhermes-tools/src/registry.rs |

## Verification Results

- `grep -c 'pub mod hexapod_tcp' crates/ironhermes-tools/src/lib.rs`: **1** (pass — alphabetically between `file_tools` and `memory_manager_handle`)
- `grep -c 'pub use.*HexapodTcpTool' crates/ironhermes-tools/src/lib.rs`: **0** (pass — no pub-use re-export per crate convention)
- `grep -c 'use crate::hexapod_tcp::HexapodTcpTool' crates/ironhermes-tools/src/registry.rs`: **1** (pass)
- `grep -c 'Box::new(HexapodTcpTool)' crates/ironhermes-tools/src/registry.rs`: **1** (pass)
- `cargo check -p ironhermes-tools`: **clean** (1 pre-existing dead_code warning)
- `cargo test -p ironhermes-tools --lib`: **318 passed, 0 failed**
- `cargo test -p ironhermes-tools --lib hexapod`: **14 passed, 0 failed**
- `cargo check --workspace`: **clean** (pre-existing warnings only, zero new)

## Notable Deviations

### Task 1 — lib.rs splice skipped (idempotent)

**Plan 02 Rule 3 deviation:** During Plan 02's TDD RED phase, `pub mod hexapod_tcp;` was added to `crates/ironhermes-tools/src/lib.rs` as a blocking Rule 3 fix — without it, `cargo test -p ironhermes-tools hexapod` compiled 0 tests (the inline `#[cfg(test)]` block was never compiled into the test binary).

**Plan 04 Task 1 action:** Verified the line is present exactly once, in alphabetical position (line 18, between `pub mod file_tools;` at line 17 and `pub mod memory_manager_handle;` at line 19). No splice was performed. This is the correct idempotent behavior — the prerequisite was already satisfied.

**Documented in Plan 02 SUMMARY.md** under "Deviations from Plan — Auto-fixed Issues — [Rule 3 - Blocking] Added `pub mod hexapod_tcp;` to lib.rs".

### Browser integration tests (pre-existing, out of scope)

Three browser integration tests (`browser_click_with_stale_ref_returns_structured_error`, `browser_navigate_then_snapshot_returns_refs`, `browser_vision_routes_to_auxiliary_vision_role`) fail with chromium SingletonLock errors — Chromium cannot acquire a process singleton in this environment. These failures are pre-existing and unrelated to Plan 04's changes (registry.rs only). Lib tests (318) and hexapod tests (14) are fully green.

## Acceptance Criteria Results

| Criterion | Result |
|-----------|--------|
| `pub mod hexapod_tcp` in lib.rs exactly once, alphabetical | pass |
| No `pub use HexapodTcpTool` re-export in lib.rs | pass |
| `use crate::hexapod_tcp::HexapodTcpTool` inside register_defaults | pass |
| `self.register(Box::new(HexapodTcpTool))` inside register_defaults | pass |
| HXP-TOOL-01 comment above registration call | pass |
| `cargo check -p ironhermes-tools` exits 0 | pass |
| `cargo test -p ironhermes-tools --lib` exits 0 (318 tests) | pass |
| All 14 hexapod unit tests pass | pass |
| `cargo check --workspace` exits 0 | pass |
| When HEXAPOD_IP unset: is_available() returns false (hides from LLM) | pass (by code inspection — prerequisite walk returns Err when env var absent) |

## Known Stubs

None.

## Threat Flags

None. This plan adds only internal Rust registry wiring (no new network endpoints, auth paths, file access patterns, or schema changes at trust boundaries). The `HexapodTcpTool` TCP threat surface was documented in Plan 02's SUMMARY.

## Self-Check: PASSED

- SUMMARY.md: created at .planning/phases/27.1.1-safe-foundation/27.1.1-04-SUMMARY.md
- Commit 0210c245: registry.rs feat — FOUND
- registry.rs modifications: VERIFIED (use import + register call inside register_defaults)
- lib.rs pub mod hexapod_tcp: VERIFIED present (Plan 02 commit, not Plan 04)
- 318 lib tests passing: CONFIRMED
- 14 hexapod tests passing: CONFIRMED
- workspace check clean: CONFIRMED
