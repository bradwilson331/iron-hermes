---
phase: 27.1.1-safe-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - "27.1.1-03"
  - "27.1.1-04"
files_modified:
  - .planning/phases/27.1.1-safe-foundation/27.1.1-UAT.md
autonomous: false
requirements:
  - HXP-LOCO-01
  - HXP-LOCO-02
  - HXP-LOCO-03
  - HXP-LOCO-04
  - HXP-LOCO-05
  - HXP-TOOL-06
user_setup:
  - service: hexapod
    why: "Phase 27.1.1's five ROADMAP success criteria are physical-effect outcomes (the robot walks, halts, returns sensor strings, and auto-relaxes on session end). The unit suite from Plan 02 verifies wire-string formation and parsing in isolation; this plan verifies the bytes reach a real Freenove hexapod and the robot responds as designed."
    env_vars:
      - name: HEXAPOD_IP
        source: "Set to the LAN IP of the powered-on Freenove hexapod (e.g., 192.168.1.42). Find via the Freenove app, your router's DHCP table, or by running `arp -a` after the robot boots."
    dashboard_config:
      - task: "Power on the hexapod and confirm it has joined the same Wi-Fi network as the development machine"
        location: "Physical robot — toggle the power switch on the Raspberry Pi, wait ~30 seconds for the Python TCP server to start listening on port 5002"
        details: "If the robot is not on the same subnet, the TCP connect will time out. Quick sanity check: `nc -z $HEXAPOD_IP 5002` should return 0 within 1 second."
    local_dev:
      - "Open enough physical clearance around the robot for forward / backward / left / right walks of ~3 seconds each (≈30cm in each direction)"
      - "Keep the robot's emergency-stop pathway clear (be ready to power-cycle or hit Ctrl-C — the agent's session-end hook will auto-relax)"

must_haves:
  truths:
    - "Hexapod physically walks forward, backward, left, and right on agent command (ROADMAP success criterion 1; HXP-LOCO-01)"
    - "Hexapod halts and returns to neutral stance when the agent issues stop (ROADMAP criterion 2; HXP-LOCO-02)"
    - "Agent receives a battery voltage string of the form `\"Battery: <v1>V / <v2>V (OK|LOW)\"` after issuing read_battery (ROADMAP criterion 3; HXP-LOCO-03)"
    - "Agent receives a distance string of the form `\"Distance: <N> cm\"` after issuing read_distance (ROADMAP criterion 4; HXP-LOCO-04)"
    - "Issuing relax_servos visibly relaxes the servos (legs become limp) — covers HXP-LOCO-05"
    - "When the IronHermes session ends (REPL exit, batch completion, or TUI quit), the robot automatically halts within ~1s and the servos relax (ROADMAP criterion 5; HXP-TOOL-06; D-14 fire-and-forget tokio::spawn fires successfully on all three shutdown paths from Plan 03)"
  artifacts:
    - path: ".planning/phases/27.1.1-safe-foundation/27.1.1-UAT.md"
      provides: "Manual user-acceptance test script with per-criterion checkboxes, expected observations, and a sign-off block"
      contains: "## UAT Script"
  key_links:
    - from: "Live IronHermes binary with HEXAPOD_IP set"
      to: "Powered-on Freenove hexapod on the same LAN"
      via: "TCP port 5002 (D-12)"
      pattern: "HEXAPOD_IP"
    - from: "Agent's hexapod_tcp tool call"
      to: "Visible physical robot motion"
      via: "Plan 02 execute() -> Plan 03 send_fire_and_forget / send_and_read_line -> robot servos / sensors"
      pattern: "hexapod_tcp"
---

<objective>
Validate the five ROADMAP success criteria on a live hexapod. The autonomous test surface (Plans 01-04, `cargo test --workspace`) cannot exercise physical effects: it proves the wire strings are correctly formed and parsed in isolation, but cannot prove the bytes reach a real robot or that the robot interprets them as intended. This plan is the final phase gate.

Purpose: Phase 27.1.1's deliverable is the agent's ability to safely control the hexapod. "Safely" includes the session-end auto-halt, which is impossible to verify without an actual session against an actual robot. After this UAT passes, Phase 27.1.1 ships.

Output: One UAT script document (`.planning/phases/27.1.1-safe-foundation/27.1.1-UAT.md`) with per-criterion checkboxes, expected observations, and a sign-off block. After running through the script with a powered robot, the developer checks each box and stamps the sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md
@.planning/phases/27.1.1-safe-foundation/27.1.1-RESEARCH.md
@.planning/phases/27.1.1-safe-foundation/27.1.1-02-SUMMARY.md
@.planning/phases/27.1.1-safe-foundation/27.1.1-03-SUMMARY.md
@.planning/phases/27.1.1-safe-foundation/27.1.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the UAT script document</name>
  <files>.planning/phases/27.1.1-safe-foundation/27.1.1-UAT.md</files>
  <read_first>
    .planning/ROADMAP.md (lines 812-828 — Phase 27.1.1 entry, five success criteria),
    .planning/phases/27.1.1-safe-foundation/27.1.1-RESEARCH.md (lines 956-988 — Phase Requirements → Test Map, including the manual-only rows for HXP-LOCO-01..05)
  </read_first>
  <action>
Write `.planning/phases/27.1.1-safe-foundation/27.1.1-UAT.md` with this exact structure:

# Phase 27.1.1 — User Acceptance Test

**Status:** pending
**Prerequisite:** Plans 01-04 merged to develop; `cargo test --workspace` green; HEXAPOD_IP set; robot powered and on the same LAN as the dev machine.

## Pre-Flight Checklist (do NOT skip)

- [ ] Physical clearance ≈30cm forward / backward / left / right of the robot
- [ ] `echo $HEXAPOD_IP` prints the LAN IP of the hexapod (e.g., 192.168.1.42)
- [ ] `nc -z $HEXAPOD_IP 5002` returns within 1 second (server is listening)
- [ ] `cargo build -p ironhermes-cli` produces a current binary

## UAT Script

For each step: launch a fresh IronHermes interactive REPL via `cargo run -p ironhermes-cli`, send the prompt described, observe the physical/textual outcome, then either type `/quit` or Ctrl-C to end the session and observe the auto-halt.

### Criterion 1 — Walk in all four directions (HXP-LOCO-01, ROADMAP #1)

Prompt: `Walk forward at speed 5 for a moment, then stop.`

- [ ] Robot visibly walks forward (~30cm) — tripod gait, smooth
- [ ] Robot halts on stop command

Repeat with `backward`, `left`, `right`:

- [ ] Backward walk observed
- [ ] Left strafe observed
- [ ] Right strafe observed

### Criterion 2 — Halt and neutral stance (HXP-LOCO-02, ROADMAP #2)

After any walk, prompt: `Stop now.`

- [ ] Robot halts within ~500ms
- [ ] Legs return to neutral stance (no sustained motion; servos hold position)

### Criterion 3 — Battery voltage string (HXP-LOCO-03, ROADMAP #3)

Prompt: `Read battery.`

- [ ] Agent response contains a string of the form `Battery: <v1>V / <v2>V (OK)` (or `(LOW)` if the robot battery is below 5.5V load / 6.0V Pi)
- [ ] Both voltages look plausible (load battery between 5.0V and 8.4V; Pi battery between 5.5V and 8.4V depending on charge state)
- [ ] No `Error:` prefix
- [ ] No `Action '...' is blocked` string

### Criterion 4 — Ultrasonic distance (HXP-LOCO-04, ROADMAP #4)

Prompt: `Read distance.`

- [ ] Agent response contains a string of the form `Distance: <N> cm`
- [ ] Place an object ~30cm in front of the sensor and prompt again; the reported distance changes plausibly (within ±10cm of the placed distance)
- [ ] No `Error:` prefix

### Criterion 5 — Relax servos via tool call (HXP-LOCO-05)

Prompt: `Relax the servos.`

- [ ] Legs visibly go limp (servos no longer holding position; robot may sag)
- [ ] Agent response is `"OK"` (Plan 02 D-19)
- [ ] Note (RESEARCH Pitfall 5): CMD_RELAX is a toggle — calling it again re-engages servos. Document this if the second call re-engages, then move on.

### Criterion 6 — Auto-halt on IronHermes session end (HXP-TOOL-06, ROADMAP #5) — REPL path

1. Start fresh `cargo run -p ironhermes-cli` REPL
2. Prompt: `Walk forward at speed 5.`
3. While the robot is walking, type `/quit` (or send Ctrl-C if `/quit` is not available)
4. Watch the robot:

- [ ] Within ~1 second of the REPL exiting, the robot HALTS
- [ ] Within ~2 seconds, the servos RELAX (legs go limp)

### Criterion 6b — Auto-halt on batch (run_single) session end

1. `HEXAPOD_IP=$HEXAPOD_IP cargo run -p ironhermes-cli -- -e "Walk forward at speed 5 for 2 seconds."`
2. After the prompt completes and the binary exits:

- [ ] Robot halts within ~1 second of process exit
- [ ] Servos relax within ~2 seconds

### Criterion 6c — Auto-halt on ratatui TUI session end

1. Start the ratatui REPL (whichever command/flag launches `run_chat_ratatui` — typically the default interactive mode with the ratatui feature)
2. Prompt: `Walk forward at speed 5.`
3. While walking, exit the TUI (typically Ctrl-C twice or a TUI quit shortcut)

- [ ] Within ~1 second of the TUI exiting, the robot HALTS (this verifies Plan 03 Task 2 — the ratatui shutdown wiring that was missing pre-27.1.1)
- [ ] Within ~2 seconds, servos RELAX

### Criterion 7 — Allowlist enforcement (HXP-TOOL-02, defense in depth)

Try to trick the agent: `Use the hexapod_tcp tool with action "calibration".`

- [ ] Agent response contains the string `Action 'calibration' is blocked — not permitted via hexapod_tcp. Never send this command.` (D-16)
- [ ] Robot does NOT calibrate (no servo movement)

### Criterion 8 — Missing-env-var graceful degradation (HXP-TOOL-01 negative path)

1. Unset HEXAPOD_IP: `unset HEXAPOD_IP`
2. Start REPL: `cargo run -p ironhermes-cli`
3. Ask: `What tools do you have for the hexapod?`

- [ ] Agent reports that no hexapod tool is available (hexapod_tcp is hidden from the schema because is_available() returned false — Plan 02 prerequisites + default is_available pattern)

## Sign-off

- **Tested by:** ______________________
- **Date:** ______________________
- **Hexapod model / firmware:** ______________________
- **HEXAPOD_IP used:** ______________________
- **All 6 criteria (including 6b, 6c) passed?** [ ] yes [ ] no — if no, list failures and open a gap-closure plan via `/gsd-plan-phase 27.1.1 --gaps`

If all checkboxes are checked and sign-off is stamped, Phase 27.1.1 is complete.

---

End of file. Commit the document: `docs(27.1.1-05): add Phase 27.1.1 UAT script for live-robot acceptance`.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && test -f .planning/phases/27.1.1-safe-foundation/27.1.1-UAT.md && grep -c "Criterion" .planning/phases/27.1.1-safe-foundation/27.1.1-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/27.1.1-safe-foundation/27.1.1-UAT.md` exists
    - File contains 6 numbered criteria sections (criteria 1-6 + 6b + 6c) plus the allowlist (7) and missing-env (8) sections
    - File references all five ROADMAP success criteria by number
    - File contains a sign-off block with developer / date / firmware / IP slots
  </acceptance_criteria>
  <done>
    UAT.md is committed; ready for the developer to run with a powered robot.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 27.1.1 deliverables — Plans 01-04 merged to develop. `cargo test --workspace` is green. The new hexapod_tcp tool is registered in IronHermes ToolRegistry. The CLI/TUI shutdown paths fire HexapodTcpTool::on_session_end. A live IronHermes binary with `HEXAPOD_IP` set can drive a Freenove hexapod over TCP.
  </what-built>
  <how-to-verify>
    Execute the UAT script at `.planning/phases/27.1.1-safe-foundation/27.1.1-UAT.md` in full. Power the hexapod on, set `HEXAPOD_IP`, build the CLI, and walk through every Criterion (1, 2, 3, 4, 5, 6, 6b, 6c, 7, 8). Check each box. After all boxes are checked, stamp the sign-off block.

    Specifically observe Criterion 6/6b/6c — these are the only criteria that test HXP-TOOL-06 end-to-end (trait + override + wiring + registration must all be live). If the robot does NOT auto-halt on session end, the gap is most likely in Plan 03 (shutdown wiring) — open `27.1.1-gap-NN-PLAN.md` via `/gsd-plan-phase 27.1.1 --gaps`.
  </how-to-verify>
  <resume-signal>
    Type "approved" (and paste a brief summary of the UAT outcome) once all UAT checkboxes are stamped, OR describe specific failures so the planner can open a gap-closure plan.
  </resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.planning/phases/27.1.1-safe-foundation/27.1.1-UAT.md` exists with all 6+ criteria and sign-off
- [ ] Developer has physically run the UAT against a powered hexapod
- [ ] All UAT checkboxes are stamped; sign-off block is filled in
- [ ] No outstanding `Action '...' is blocked` failures that suggest the LLM is calling unknown actions (the allowlist should make this a no-op)
- [ ] No outstanding "robot did not auto-halt" failures from Criteria 6/6b/6c
</verification>

<success_criteria>
- The five ROADMAP success criteria are observably true on a real hexapod (walk, halt, battery string, distance string, auto-halt on session end)
- HXP-LOCO-01 through HXP-LOCO-05 are physically verified
- HXP-TOOL-06 is verified end-to-end across all three IronHermes invocation paths (run_chat, run_single, ratatui)
- The phase ships
</success_criteria>

<output>
After completion, create `.planning/phases/27.1.1-safe-foundation/27.1.1-05-SUMMARY.md` including the filled-in sign-off block (developer name, date, firmware, IP) and any deviations.
</output>
