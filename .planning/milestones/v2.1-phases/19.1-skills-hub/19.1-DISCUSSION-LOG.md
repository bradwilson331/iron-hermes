# Phase 19.1 — Skills Hub — Discussion Log

**Date:** 2026-04-15
**Participants:** User (Brad Wilson), Claude
**Mode:** `/gsd-discuss-phase 19.1` (default interactive)

---

## Prior context loaded

- `.planning/PROJECT.md`, `REQUIREMENTS.md` (§Skills SKILL-08, SKILL-09), `STATE.md`, `ROADMAP.md` §19.1.
- Phase 19 CONTEXT.md — D-15 WARN-BUT-LOAD / hard-reject-community, D-16 scan-at-registry-load, SkillSource enum shape, "Hub split to 19.1" boundary.
- hermes-agent reference skim: `skills_hub.py` (2,691 LOC), `skills_sync.py` (295 LOC), `skill_manager_tool.py` (701 LOC).

## Gray areas identified (4)

1. Source adapters
2. Trust assignment
3. Install lifecycle
4. Surface scope (publish + CLI + tool)

All four selected for discussion.

---

## Area 1: Source Adapters

| Q | Answer | Locked as |
|---|--------|-----------|
| Which adapters in v1? | Port all three (GitHub + well-known + skills.sh) | D-01 |
| GitHub default taps? | Port reference taps as-is | D-02 |
| GitHub auth model? | Anonymous + PAT + gh-cli fallback (no GitHub App) | D-03 |
| Where does the trusted-repo allowlist live? | `config.yaml` under `hub.trusted_repos`, empty default | D-04 |

## Area 2: Trust Assignment

| Q | Answer | Locked as |
|---|--------|-----------|
| What is Official? | `optional-skills/` compile-time bundled dir | D-05 |
| Trust scope? | Per-source only; no per-skill override | D-06 |
| Well-known trust ceiling? | Always Community | D-07 |
| Recompute or freeze trust? | Recompute on every registry-load from config | D-08 |

## Area 3: Install Lifecycle

| Q | Answer | Locked as |
|---|--------|-----------|
| Install layout? | Flat by category + `.hub/` manifest dir (reference shape) | D-09 |
| Update strategy? | Manual only: `hermes skills update [name]`, no lockfile v1 | D-10 |

## Area 4: Surface Scope

| Q | Answer | Locked as |
|---|--------|-----------|
| Publish in v1? | Deferred — install/search/update/uninstall only | D-12 |
| Tool-surface additions? | Read-only: `hub_search` only; installs CLI-only | D-13 |

(D-11 and D-14 synthesized from the above — install pipeline shape and CLI command layout respectively.)

---

## Scope creep captured

- None raised during discussion. Publish was actively deferred (D-12), not pulled in.

## Ambiguity resolved

- "Is skills.sh worth the complexity?" → Yes, port all three, because reference already does and skills.sh is a thin wrapper on GitHub.
- "Does `hub_install` on the tool surface let the agent mutate install state?" → No (D-13); keeps user-gated per ROADMAP criterion 4.
- "What is Official if Hub installs can't reach it?" → `optional-skills/` bundled dir; Hub stops at Trusted (D-05 + D-06).

## Next step

`/gsd-plan-phase 19.1` — planner consumes this CONTEXT.md to produce wave-grouped plan files.
