---
context: phase
phase: 19.1-skills-hub
task: 0
total_tasks: 12
status: ready_to_execute
last_updated: 2026-04-15T10:52:01.604Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Re-litigating CONTEXT.md decisions | Phase 19.1 has 14 user-locked decisions (D-01..D-14). Downstream agents (executor, fixers) must port faithfully, not re-design. | advisory | Plans cite D-XX inline; CONTEXT.md is required-read for every task via `<read_first>`. |
| Treating warning thresholds as blockers | Plans 01 (10 files) and 02 (11 files) tripped scope-sanity warnings; both were accepted as scaffolding + parallel-adapters. | advisory | Only split if execution context actually overruns — do not preemptively re-plan. |

<current_state>
Phase 19.1 (Skills Hub) just completed full discuss → research → plan → check pipeline. All 5 PLANs are committed (b18ca28). Plan-checker found 1 blocker (RESEARCH.md missing `(RESOLVED)` suffix on Open Questions section) — fixed in c290c9c. Ready to execute Wave 1.

**Paused because:** Context window hit 87% during the planning + verification cycle. Fresh `/clear` strongly recommended before execution to give Wave 1 (Plan 19.1-01: scaffold + Trusted enum variant) a clean window.
</current_state>

<completed_work>

- CONTEXT.md (commit 6e0d853) — 14 decisions locked
- RESEARCH.md (commit af89ce1, fix c290c9c) — Rust crates, adapter design, Validation Architecture, Open Questions RESOLVED
- VALIDATION.md (commit 6d476d6) — Per-Task Verification Map with 12 cargo-test rows, all `<automated>` verified
- 5 PLAN.md files (commit b18ca28) — 12 tasks across 4 waves, threat models on every plan
</completed_work>

<remaining_work>

**Wave 1** — Plan 19.1-01 (3 tasks)
- T1: Add `SkillSource::Trusted` variant to `crates/ironhermes-core/src/skills.rs` (unblocks everything downstream)
- T2: Add `HubConfig { trusted_repos, github_token_env, extra_taps, well_known_origins }` to SkillsConfig with backward-compat serde defaults
- T3: Scaffold `crates/ironhermes-hub/` crate (Cargo.toml + lib.rs + source/auth/paths/error/manifest modules) with reqwest/tar/flate2/wiremock deps

**Wave 2** — Plan 19.1-02 (3 tasks, parallel adapter ports)
- T1: GitHubSource (Trees API + tarball fetch + path-traversal guard, MAX_EXTRACTED_BYTES=50MB / MAX_ENTRIES=1000)
- T2: WellKnownSkillSource (HTTPS-only + SSRF guard, hardcoded Community per D-07)
- T3: SkillsShSource (resolve via skills.sh registry, delegate to GitHubSource)

**Wave 3 (parallel)** — Plans 19.1-03 + 19.1-04
- 03 T1: Install pipeline (fetch → quarantine → scan → atomic move → manifest)
- 03 T2: update() (hash-drift detection, backup-rename-restore rollback) + uninstall()
- 04 T1: `resolve_source()` in core/skills.rs (D-08 recomputation, D-05 optional-skills, D-07 well-known override)
- 04 T2: End-to-end trust resolution integration test

**Wave 4** — Plan 19.1-05 (2 tasks)
- T1: `hermes skills` CLI subcommand tree (install/search/update/uninstall/list/trust)
- T2: Read-only `hub_search` tool action in SkillsTool (D-13 — NO hub_install)
</remaining_work>

<decisions_made>

- Skip publish flows (D-12 → defer to Phase 19.2 or Phase 20 with skill_manage)
- Read-only `hub_search` tool only (D-13) — install mutations stay on CLI
- New crate `ironhermes-hub` (not module in ironhermes-tools) — isolates reqwest/tar/flate2 from core
- Wave 0 absorbed into Plan 01 — no separate Wave 0 plan
- wiremock 0.6 over mockito for HTTP mocking — async-native
- Plans 01 (10 files) and 02 (11 files) at warning threshold — accepted as-is
</decisions_made>

<blockers>
- Context window at 87% on paused session — fresh /clear strongly recommended before execution.
</blockers>

## Required Reading (in order)

1. `.planning/phases/19.1-skills-hub/19.1-CONTEXT.md` — 14 locked decisions D-01..D-14 (DO NOT re-litigate)
2. `.planning/phases/19.1-skills-hub/19.1-RESEARCH.md` — Rust crate choices + adapter details + Validation Architecture
3. `.planning/phases/19.1-skills-hub/19.1-VALIDATION.md` — Per-task cargo test commands
4. `.planning/phases/19.1-skills-hub/19.1-01-PLAN.md` — Wave 1 (start here)
5. `~/code/hermes-agent/tools/skills_hub.py` — Authoritative Python reference (2,691 LOC) for line-by-line port

## Critical Anti-Patterns (do NOT repeat these)

- ANTI-PATTERN: "Port the Python logic" without specifying exact Rust signatures → every `<action>` in the plans already includes concrete trait sigs, struct fields, URL patterns. Use them verbatim.
- ANTI-PATTERN: Adding hub_install tool action → explicitly forbidden by D-13. Plan 05 T2 has a grep-negative-assertion to enforce.
- ANTI-PATTERN: Encoding trust in the manifest → trust is recomputed at registry-load from config.hub.trusted_repos (D-08). Manifest stores source + identifier only.

## Infrastructure State

- Workspace: clean (all phase artifacts committed through c290c9c)
- New crate `ironhermes-hub`: not yet created (Wave 1 Plan 01 T3 creates it)
- Test infra: wiremock will be added as dev-dep in Plan 01 T3

<context>
Just finished planning + verification cycle for Phase 19.1 in a single session. The plan-checker raised one blocker (Dim-11 Open Questions header missing `(RESOLVED)` marker) — trivial format fix applied directly. All other dimensions passed first try. Plans are well-structured: each task has concrete `<action>` text, `<read_first>` includes the Python reference being ported, and `<acceptance_criteria>` cites grep-verifiable conditions and `cargo test` commands.

The execution should be largely autonomous — every plan has `autonomous: yes`. No human-in-the-loop interventions queued. Run-of-the-mill port work where the reference implementation is the source of truth.
</context>

<next_action>
Run `/clear`, then `/gsd-execute-phase 19.1`. Wave 1 (Plan 19.1-01) starts with adding the `SkillSource::Trusted` enum variant — that's the single change that unblocks everything downstream.
</next_action>
