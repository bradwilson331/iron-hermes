---
phase: 07.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md
autonomous: true
requirements: []
requirements_addressed: []
# Note: phase 07.1 CREATES new requirement IDs as its deliverable (SKILL-05+).
# Per planning_context, this phase has no pre-existing requirement IDs to map.
# Plan 02 will mint and append the new IDs to REQUIREMENTS.md.

must_haves:
  truths:
    - "07.1-GAP-REPORT.md exists in the phase directory"
    - "Report covers all 14 capability areas named in the phase title"
    - "Every capability row has a status (present/partial/absent), a severity, an effort, a milestone, and a recommended REQ-ID"
    - "Every gap dossier cites at least one file:line from the current ironhermes codebase OR explicitly says 'absent'"
    - "Every gap dossier cites a specific Python hermes-agent source file (with line range where possible)"
    - "Report reuses and cites .planning/research/SKILLS.md per D-18"
    - "Report treats Phase 7 (SKILL-01..SKILL-04) as authoritative and does not re-audit it per D-17"
  artifacts:
    - path: ".planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md"
      provides: "Capability matrix, per-gap dossiers, summary appendix"
      contains: "## Capability Matrix"
      min_lines: 200
  key_links:
    - from: "07.1-GAP-REPORT.md"
      to: "crates/ironhermes-core/src/skills.rs"
      via: "file:line citation in Current state"
      pattern: "crates/ironhermes-core/src/skills\\.rs"
    - from: "07.1-GAP-REPORT.md"
      to: "hermes-agent/tools/skills_guard.py"
      via: "Spec quote / Python reference"
      pattern: "skills_guard\\.py"
    - from: "07.1-GAP-REPORT.md"
      to: ".planning/research/SKILLS.md"
      via: "D-18 reuse citation"
      pattern: "research/SKILLS\\.md"
---

<objective>
Produce `07.1-GAP-REPORT.md` — a comprehensive gap analysis comparing the ironhermes Phase 7 skills system against the full Hermes Skills System spec (agentskills.io + Python hermes-agent reference + REQUIREMENTS.md v2 self-improvement items).

Purpose: Give the project a definitive, evidence-backed map of what Phase 7 shipped vs. what the broader skills ecosystem covers, so Plan 02 can mint concrete SKILL-05+ requirement IDs and future phases can pick them up.

Output: `07.1-GAP-REPORT.md` with capability matrix + per-gap dossiers + summary appendix per D-13.

**Phase character:** AUDIT-ONLY. No Rust source files are modified. The audit READS `crates/**/*.rs` and `~/code/hermes-agent/tools/*.py` to produce evidence. The only file this plan writes is `07.1-GAP-REPORT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-CONTEXT.md
@.planning/research/SKILLS.md
@.planning/phases/07-skills-system/07-CONTEXT.md
@.planning/phases/07-skills-system/07-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit capability groups A+B (Spec compliance basics + Configuration & env vars) and seed GAP-REPORT.md skeleton</name>
  <files>.planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md</files>
  <read_first>
    - .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-CONTEXT.md (source of truth for audit spec — D-03..D-14)
    - .planning/research/SKILLS.md (prior agentskills.io spec research — D-18; reuse its spec summary)
    - .planning/phases/07-skills-system/07-VERIFICATION.md (authoritative for what Phase 7 shipped — D-17)
    - crates/ironhermes-core/src/skills.rs (SkillFrontmatter, SkillRecord, parse_skill_md, SkillRegistry)
    - crates/ironhermes-core/src/config.rs (candidate home for SkillsConfig block)
    - crates/ironhermes-core/src/constants.rs (get_hermes_home)
    - crates/ironhermes-tools/src/skills_tool.rs (SkillsTool list/view/activate)
    - crates/ironhermes-cli/src/main.rs (skill registry construction)
    - ~/code/hermes-agent/tools/skills_tool.py (Python tool surface — for spec compliance basics)
    - ~/code/hermes-agent/skills/hermes-agent/SKILL.md (extended frontmatter example: platforms, metadata.hermes.config, requires_toolsets, fallback_for_toolsets)
    - ~/code/hermes-agent/skills/creative/ascii-art/SKILL.md (second frontmatter example)
  </read_first>
  <action>
**Objective:** Seed `07.1-GAP-REPORT.md` with the header, capability matrix skeleton, and per-gap dossiers for the first two capability groups (D-03 Spec compliance basics, D-04 Configuration & env vars).

**Step 1 — Create the report skeleton.**

Write `07.1-GAP-REPORT.md` with this structure (XML tag sections per code_context convention):

```markdown
<!-- frontmatter-begin -->
phase: 07.1-skill-system-gap-analysis
audited: <today's date>
status: in_progress
parity_bar: "agentskills.io open standard + Python hermes-agent reference + REQUIREMENTS.md v2 SELF-08/SELF-10"
divergence_rule: "open standard wins; hermes-agent extensions parsed leniently (D-09)"
<!-- frontmatter-end -->

# Phase 07.1 — Skills System Gap Report

<summary>
Counts (fill in as audit progresses):
- Capabilities audited: _TBD_
- Present: _TBD_
- Partial: _TBD_
- Absent: _TBD_
- BLOCKER: _TBD_
- HIGH: _TBD_
- MEDIUM: _TBD_
- LOW: _TBD_

Phase 7 authoritative baseline (per D-17): SKILL-01..SKILL-04 shipped — SkillRegistry (3-path scan, first-path-wins dedup), parse_skill_md (serde_yaml), SkillsTool (list/view/activate), catalog injection in PromptBuilder section 5.5, cron tick skill resolution. 27 unit tests across skills.rs / skills_tool.rs / runner.rs. See .planning/phases/07-skills-system/07-VERIFICATION.md.

This report audits what is OUTSIDE that scope.
</summary>

<capability_matrix>
## Capability Matrix

| # | Capability | Group | Status | Severity | Effort | Milestone | Recommended REQ-ID | Recommended Phase |
|---|------------|-------|--------|----------|--------|-----------|---------------------|-------------------|
| 1 | Progressive disclosure | A | present | — | — | — | — | Phase 7 (done) |
| 2 | SKILL.md format (name, description) | A | present | — | — | — | — | Phase 7 (done) |
| ... | (populated by audit tasks) | | | | | | | |
</capability_matrix>

<dossiers>
## Per-Gap Dossiers

(populated below)
</dossiers>

<summary_appendix>
## Summary Appendix: Recommended Roadmap Additions

(populated after all dossiers)
</summary_appendix>
```

**Step 2 — Audit Capability Group A (D-03: Spec compliance basics).**

Copy of D-03 verbatim (implement EACH item):
> Audit coverage of: `platforms` filter, extended frontmatter fields (`compatibility`, `allowed-tools`, `metadata`, `metadata.hermes.*`), SKILL.md validation rules (name regex, description length 1–1024, directory-matches-name), directory depth limits, name-collision precedence handling.

For EACH of these 8 sub-capabilities, perform the following evidence procedure:

Sub-capabilities:
  A1. `platforms` filter (e.g., `platforms: [macos, linux]` skip-on-mismatch)
  A2. `compatibility` frontmatter field
  A3. `allowed-tools` frontmatter field
  A4. `metadata` (arbitrary key-value map) frontmatter field
  A5. `metadata.hermes.*` extensions (tags, category, requires_toolsets, fallback_for_toolsets, related_skills)
  A6. Name regex validation (1-64 chars, lowercase alphanumeric + hyphens, no leading/trailing/consecutive hyphens)
  A7. Description length validation (1-1024 chars)
  A8. Directory-matches-name validation
  A9. Directory depth limits (spec recommends max depth 4, skip .git/node_modules)
  A10. Name-collision precedence handling beyond "first-path wins dedup"

Evidence procedure for each sub-capability (run these EXACT commands):
  1. `grep -n "platforms\|compatibility\|allowed.tools\|allowed_tools\|metadata\|name\|description" crates/ironhermes-core/src/skills.rs` — find the SkillFrontmatter struct and see which fields exist.
  2. `grep -n "depth\|MAX_DEPTH\|max_depth\|walkdir" crates/ironhermes-core/src/skills.rs` — check for depth limits.
  3. `grep -rn "platforms\|allowed.tools\|compatibility" ~/code/hermes-agent/tools/skills_tool.py ~/code/hermes-agent/tools/skills_hub.py | head -60` — find Python reference.
  4. `grep -c '#\[test\]' crates/ironhermes-core/src/skills.rs` — count tests (should be ~14 per VERIFICATION.md).
  5. Record result as: status (present/partial/absent), file:line for current state (or "absent"), Python reference file:line, test coverage.

For each sub-capability, write a dossier section in the `<dossiers>` block using this exact template:

```markdown
### Gap: <Capability name> [A<N>]

**Group:** Spec compliance basics
**Status:** <present | partial | absent>
**Severity:** <BLOCKER | HIGH | MEDIUM | LOW>
**Effort:** <S | M | L | XL> (Python LOC proxy: <N> LOC in <file>)
**Milestone:** <v1.1 | v2 | backlog>

**Spec quote:** "<exact text from .planning/research/SKILLS.md or agentskills.io>" (or pointer: `~/code/hermes-agent/tools/<file>:<lines>`)

**Current state:** <file:line in ironhermes OR "absent — no field in SkillFrontmatter struct">

**Test coverage:** <which tests exercise it, or "none">

**Delta:** <what's missing, precisely — one or two sentences>

**Recommended scope:**
- Proposed REQ-ID: SKILL-<NN> (TBD in Plan 02)
- Proposed phase shape: <brief — e.g., "extend SkillFrontmatter; 1 plan, ~2 tasks">
- Rough LOC estimate (Rust): <N> lines based on Python <N> LOC sanity-checked
```

Severity rubric (from D-14):
- BLOCKER = breaks agentskills.io compliance or hermes-agent backward compat (e.g., missing `allowed-tools` parse path → valid skills fail to load)
- HIGH = user-visible capability gap with workflow impact
- MEDIUM = nice-to-have parity feature
- LOW = cosmetic or deep internal

Also add ONE row per sub-capability to the `## Capability Matrix` table.

**Step 3 — Audit Capability Group B (D-04: Configuration & env vars).**

Copy of D-04 verbatim (implement EACH item):
> Audit coverage of: `SkillsConfig` block in `config.yaml` for custom scan paths, external/configurable skill directories (beyond the three hard-coded paths from D-02 of Phase 7), secure env-var setup flow for skills that need API keys, `metadata.hermes.config` prompt mechanism (skill declares required config keys, hermes prompts user at first use).

Sub-capabilities:
  B1. `SkillsConfig` block in `config.yaml` for custom scan paths
  B2. External/configurable skill directories (beyond the hard-coded 3 paths)
  B3. Secure env-var setup flow for skills that need API keys (how the user supplies e.g. FIRECRAWL_API_KEY to a skill at first use)
  B4. `metadata.hermes.config` prompt mechanism (skill declares required config keys with prompt strings; hermes asks user at first use and persists)

Evidence procedure for each:
  1. `grep -n "SkillsConfig\|skills_config\|skills:" crates/ironhermes-core/src/config.rs` — check for SkillsConfig block.
  2. `grep -rn "scan_paths\|skill.*path\|external.*skill" crates/ironhermes-core/src/` — check for configurability.
  3. `grep -rn "metadata.hermes.config\|config.*prompt\|env_var\|setup_prompt" ~/code/hermes-agent/tools/skills_tool.py ~/code/hermes-agent/tools/skill_manager_tool.py | head -40` — find Python reference.
  4. `grep -rn "metadata:" ~/code/hermes-agent/skills/hermes-agent/SKILL.md` — inspect the extended frontmatter example for `metadata.hermes.config`.
  5. `grep -rn "platforms\|metadata" crates/ironhermes-core/src/skills.rs` — cross-check ironhermes parser.
  6. Record: status, file:line, Python reference file:line, test coverage, delta, recommended scope.

Write one dossier section per sub-capability using the same template from Step 2.

**Step 4 — Update the summary counts.**

After the dossiers are written for Groups A+B, update the `<summary>` block with running counts for capabilities audited so far (the remainder is filled in by Task 2/3).

**Important:**
- Every dossier MUST cite a concrete file:line (or explicitly write "absent — no such construct in crates/ironhermes-core/src/skills.rs").
- Every spec quote MUST come from `.planning/research/SKILLS.md`, `agentskills.io`, or a specific Python file:line in `~/code/hermes-agent/tools/`. Never fabricate quotes.
- If a capability is already covered by Phase 7 (progressive disclosure, basic SKILL.md format, basic name+description validation), record it in the matrix as `present` with reference to 07-VERIFICATION.md and skip the dossier (per D-17).
- For divergence cases (agentskills.io says X, hermes-agent does Y), record the gap against the agentskills.io version and flag the hermes extension as "extension, parse leniently" (per D-09).
- If you cannot back a claim with a citation after grepping/reading, mark the gap as "unverified — needs investigation" (per D-12).

**Do NOT touch any Rust source files. Read-only.**
  </action>
  <verify>
    <automated>test -f .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md && grep -q '## Capability Matrix' .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md && grep -q '## Per-Gap Dossiers' .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md && [ "$(grep -c '### Gap:' .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md)" -ge 8 ] && grep -q 'crates/ironhermes-core/src/skills.rs' .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md && grep -q 'skills_tool.py\|skill_manager_tool.py' .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md` exists
    - File contains `## Capability Matrix` section header
    - File contains `## Per-Gap Dossiers` section header
    - File contains `<summary>` block with counts placeholders
    - `grep -c '### Gap:' 07.1-GAP-REPORT.md` returns >= 8 (at least one per Group A+B sub-capability that is partial/absent)
    - `grep 'crates/ironhermes-core/src/skills.rs' 07.1-GAP-REPORT.md` returns >= 1 (citation to current code)
    - `grep 'crates/ironhermes-core/src/config.rs' 07.1-GAP-REPORT.md` returns >= 1 (Group B cites config.rs)
    - `grep -E 'skills_tool\.py|skill_manager_tool\.py|skills_hub\.py' 07.1-GAP-REPORT.md` returns >= 1 (Python reference citation)
    - `grep 'Severity:' 07.1-GAP-REPORT.md` returns >= 8 (one per gap dossier)
    - `grep 'Status:' 07.1-GAP-REPORT.md` returns >= 8
    - `grep 'Effort:' 07.1-GAP-REPORT.md` returns >= 8
    - `grep 'Delta:' 07.1-GAP-REPORT.md` returns >= 8
    - `grep 'research/SKILLS.md' 07.1-GAP-REPORT.md` returns >= 1 (D-18 reuse)
    - No Rust source files modified (verify via `git status crates/` shows clean for any files not already modified)
  </acceptance_criteria>
  <done>GAP-REPORT.md exists with header, capability matrix skeleton (with rows for Groups A+B), and per-gap dossiers for every partial/absent sub-capability in Groups A+B. Every dossier has spec quote, current state citation, Python reference, delta, and recommended scope. No Rust files modified.</done>
</task>

<task type="auto">
  <name>Task 2: Audit capability groups C+D (Skills Hub & lifecycle + Security & agent-managed skills) and extend GAP-REPORT.md</name>
  <files>.planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md</files>
  <read_first>
    - .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-CONTEXT.md (D-05, D-06, D-07, D-13, D-14)
    - .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md (the report this task extends)
    - .planning/research/SKILLS.md (for spec cross-reference)
    - .planning/REQUIREMENTS.md (for SELF-08, SELF-10 v2 text — D-07 parity items)
    - .planning/research/self-improvement.md (SELF-08, SELF-10 research — cited in CONTEXT.md canonical_refs)
    - crates/ironhermes-core/src/skills.rs (current implementation — verify absence of hub/sync/guard code)
    - crates/ironhermes-tools/src/skills_tool.rs (current tool — verify no skill_manage action exists)
    - crates/ironhermes-tools/src/registry.rs (tool registration — verify no register_skill_manage)
    - crates/ironhermes-hooks/src/event.rs (SkillActivated hook — check if more events needed)
    - ~/code/hermes-agent/tools/skills_hub.py (2,691 LOC — multi-source hub)
    - ~/code/hermes-agent/tools/skills_sync.py (295 LOC — update lifecycle)
    - ~/code/hermes-agent/tools/skills_guard.py (1,105 LOC — security scanner)
    - ~/code/hermes-agent/tools/skill_manager_tool.py (701 LOC — skill_manage tool)
    - ~/code/hermes-agent/tools/skills_tool.py (1,344 LOC — slash command integration hints)
  </read_first>
  <action>
**Objective:** Extend `07.1-GAP-REPORT.md` with dossiers for the two remaining capability groups (D-05 Skills Hub & lifecycle, D-06 Security & agent-managed skills), plus the SELF-08/SELF-10 parity items from D-07.

**Step 1 — Audit Capability Group C (D-05: Skills Hub & lifecycle).**

Copy of D-05 verbatim:
> Audit coverage of: multi-source hub (git repos, local paths, remote tarballs), update lifecycle (fetch / refresh / diff / install / remove — Python `skills_sync.py`, `skills_hub.py`), trust levels (verified / community / local), hub state tracking (`.hub/` directory equivalent).

Sub-capabilities:
  C1. Multi-source hub — git repos as skill source
  C2. Multi-source hub — local paths as skill source
  C3. Multi-source hub — remote tarballs as skill source
  C4. Update lifecycle: fetch (download from source)
  C5. Update lifecycle: refresh (re-fetch and compare)
  C6. Update lifecycle: diff (show what changed)
  C7. Update lifecycle: install (apply new/updated skills)
  C8. Update lifecycle: remove (uninstall skill)
  C9. Trust levels: verified / community / local classification
  C10. Hub state tracking (`.hub/` directory or equivalent JSON state file)

Evidence procedure for each:
  1. `grep -rn "hub\|Hub\|HUB" crates/ironhermes-core/src/ crates/ironhermes-tools/src/` — check for any hub-related code in ironhermes.
  2. `grep -rn "git clone\|tarball\|download\|fetch.*skill" crates/ironhermes-core/src/ crates/ironhermes-tools/src/` — check for source adapters.
  3. `grep -rn "trust\|verified\|community" crates/ironhermes-core/src/skills.rs crates/ironhermes-tools/src/skills_tool.rs` — check for trust levels.
  4. `wc -l ~/code/hermes-agent/tools/skills_hub.py ~/code/hermes-agent/tools/skills_sync.py` — get authoritative LOC for effort estimation.
  5. `grep -n "class\|def " ~/code/hermes-agent/tools/skills_hub.py | head -60` — enumerate Python public surface (SkillMeta, SkillBundle, SourceAdapter, etc.).
  6. `grep -n "class\|def " ~/code/hermes-agent/tools/skills_sync.py | head -40` — enumerate sync lifecycle surface.
  7. `grep -n "\.hub\|hub_state\|HubState" ~/code/hermes-agent/tools/skills_hub.py | head -20` — find hub state schema.

Record each as a dossier section using the template from Task 1.

Expected finding: ALL C1-C10 are **absent** in ironhermes. Phase 7 shipped zero hub/sync/lifecycle code per VERIFICATION.md. Effort for each should reference the Python LOC (skills_hub.py 2,691 LOC → XL; skills_sync.py 295 LOC → M). Severity is likely HIGH for a few (install/remove/refresh — user-visible workflow) and MEDIUM for others.

**Step 2 — Audit Capability Group D (D-06 + D-07: Security & agent-managed skills + SELF parity).**

Copy of D-06 verbatim:
> Audit coverage of: security scanning of skill content (Python `skills_guard.py` — prompt injection detection, malicious pattern detection, allow/deny lists), `skill_manage` tool (agent creates/edits/deletes its own skills — SELF-08/SELF-10 territory), slash-command integration (skills invoked via `/skill-name` in chat, not just tool calls).

Copy of D-07 verbatim:
> Self-improvement capabilities from REQUIREMENTS.md v2 are in scope as part of the parity bar: SELF-08 (procedural knowledge stored in SKILL.md directories) and SELF-10 (agent saves multi-step procedures as reusable skills after 5+ tool calls).

Sub-capabilities:
  D1. Security scanning — prompt injection detection on skill content load
  D2. Security scanning — malicious pattern detection (shell escapes, exfil, etc.)
  D3. Security scanning — allow/deny lists for skill sources or content
  D4. `skill_manage` tool — create action
  D5. `skill_manage` tool — patch/edit action
  D6. `skill_manage` tool — delete action
  D7. `skill_manage` tool — write_file / remove_file (bundled resource CRUD)
  D8. Slash-command integration (`/skill-name` in chat dispatches to skill)
  D9. SELF-08 parity — procedural knowledge stored in SKILL.md directories (does ironhermes support this semantically? is it covered by Phase 7 or is there a gap?)
  D10. SELF-10 parity — agent saves multi-step procedures as reusable skills after 5+ tool calls (auto-skill-creation heuristic)

Evidence procedure for each:
  1. `grep -rn "skills_guard\|SkillsGuard\|skill.*scan\|prompt.*inject" crates/ironhermes-core/src/ crates/ironhermes-tools/src/` — check for any guard code.
  2. `grep -rn "skill_manage\|SkillManage\|skill_manager" crates/ironhermes-tools/src/ crates/ironhermes-core/src/` — check for skill_manage tool.
  3. `grep -rn "slash\|/skill\|slash_command" crates/ironhermes-gateway/src/ crates/ironhermes-tools/src/` — check for slash-command dispatch.
  4. `grep -n "action\|dispatch" crates/ironhermes-tools/src/skills_tool.rs` — enumerate current SkillsTool actions (expect: list, view, activate ONLY).
  5. `wc -l ~/code/hermes-agent/tools/skills_guard.py ~/code/hermes-agent/tools/skill_manager_tool.py` — effort LOC.
  6. `grep -n "class\|def " ~/code/hermes-agent/tools/skills_guard.py | head -40` — enumerate guard surface (pattern lists, scanners).
  7. `grep -n "class\|def " ~/code/hermes-agent/tools/skill_manager_tool.py | head -40` — enumerate skill_manage surface.
  8. Cross-check SELF-08 and SELF-10 in `.planning/REQUIREMENTS.md` v2 section — does Phase 7 satisfy them (partially)? Record as "partial" with note that Phase 7's read-only skill loading is the foundation but auto-creation (SELF-10) and agent editing (D4-D7) are absent.

Write one dossier section per sub-capability.

**Step 3 — Re-check Phase 7 hook surface for D-dependent features.**

`grep -n "SkillActivated\|SkillCreated\|SkillUpdated\|SkillDeleted\|SkillDenied" crates/ironhermes-hooks/src/event.rs`

Phase 7 shipped `SkillActivated` only. The Skills Hub + skill_manage features would need additional hook variants (SkillInstalled, SkillRemoved, SkillManaged, SkillDenied). If these are needed, add a short note at the top of Group C or D dossiers flagging "hook surface extension required".

**Step 4 — Add any cross-group duplicate notes.**

Per D's Claude Discretion point: if the same underlying feature shows up under two capability groups (e.g., `metadata.hermes.config` is partly A5 and partly B4), mention it once in the primary group and cross-reference from the other with a single line: `See also [A5] above.` Do not duplicate the full dossier.

**Step 5 — Update `<summary>` counts.**

After Groups C+D are written, update the `<summary>` block with running counts: total capabilities audited, breakdown by status, breakdown by severity.

**Also update the `## Capability Matrix` table** with all rows from Groups C+D (matrix should now have ~30 rows spanning all 4 groups).

**Do NOT touch any Rust source files. Read-only.**
  </action>
  <verify>
    <automated>grep -q 'Skills Hub' .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md && grep -q 'Security' .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md && [ "$(grep -c '### Gap:' .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md)" -ge 20 ] && grep -q 'skills_hub\.py' .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md && grep -q 'skills_guard\.py' .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md && grep -q 'skill_manager_tool\.py' .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md && grep -q 'SELF-08\|SELF-10' .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '### Gap:' 07.1-GAP-REPORT.md` returns >= 20 (Groups A+B from Task 1 yielded >=8; Groups C+D add >=12 more)
    - `grep 'skills_hub.py' 07.1-GAP-REPORT.md` returns >= 1
    - `grep 'skills_sync.py' 07.1-GAP-REPORT.md` returns >= 1
    - `grep 'skills_guard.py' 07.1-GAP-REPORT.md` returns >= 1
    - `grep 'skill_manager_tool.py' 07.1-GAP-REPORT.md` returns >= 1
    - `grep -E 'SELF-08|SELF-10' 07.1-GAP-REPORT.md` returns >= 2 (one reference per parity item)
    - `grep 'slash' 07.1-GAP-REPORT.md` returns >= 1 (slash-command integration gap)
    - `grep 'skill_manage' 07.1-GAP-REPORT.md` returns >= 1
    - `grep 'trust' 07.1-GAP-REPORT.md` returns >= 1 (trust levels)
    - `grep 'hub state\|\.hub' 07.1-GAP-REPORT.md` returns >= 1
    - `grep 'Severity: BLOCKER\|Severity: HIGH\|Severity: MEDIUM\|Severity: LOW' 07.1-GAP-REPORT.md` count equals total dossier count
    - `grep 'absent' 07.1-GAP-REPORT.md` returns >= 10 (hub/guard/skill_manage features are all absent)
    - No Rust source files modified
  </acceptance_criteria>
  <done>GAP-REPORT.md has dossiers for all partial/absent sub-capabilities in Groups C+D plus SELF-08/SELF-10 parity items. Every Python reference file is cited at least once. Capability matrix rows added for all C+D sub-capabilities. Summary counts updated.</done>
</task>

<task type="auto">
  <name>Task 3: Write summary appendix, self-check evidence, finalize GAP-REPORT.md</name>
  <files>.planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md</files>
  <read_first>
    - .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md (entire file produced by Tasks 1+2)
    - .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-CONTEXT.md (D-13, D-14, D-15 — for appendix structure and REQ-ID ranges)
    - .planning/REQUIREMENTS.md (to see where SKILL-05+ will land in Plan 02)
  </read_first>
  <action>
**Objective:** Finalize `07.1-GAP-REPORT.md` by writing the summary appendix, cross-checking internal consistency, and flipping status from `in_progress` to `complete`.

**Step 1 — Write the summary appendix (D-13 structure item 4).**

Below the `<dossiers>` section, fill in the `<summary_appendix>` block with this structure:

```markdown
<summary_appendix>
## Summary Appendix: Recommended Roadmap Additions

### v1.1 Additions (BLOCKER + HIGH severity)

These gaps should be folded into the active v1.1 milestone. Each will receive a `SKILL-05+` REQ-ID in Plan 02.

| Gap | Severity | Effort | Proposed REQ-ID | Proposed Phase Shape |
|-----|----------|--------|------------------|----------------------|
| <gap name> | <S> | <E> | SKILL-05 | <brief> |
| ... | | | | |

### v2 Additions (Self-improvement parity)

These gaps extend SELF-08 / SELF-10 in the v2 Self-Improvement section.

| Gap | Severity | Effort | Proposed REQ-ID | Proposed Phase Shape |
|-----|----------|--------|------------------|----------------------|
| <SELF-08 extension> | <S> | <E> | SELF-11 (or SKILL-NN) | <brief> |
| <SELF-10 extension> | <S> | <E> | SELF-12 (or SKILL-NN) | <brief> |

### Backlog (MEDIUM / LOW severity)

| Gap | Severity | Effort | Proposed REQ-ID | Notes |
|-----|----------|--------|------------------|-------|
| ... | | | | |

### Quick Wins (S-effort + HIGH-severity)

Per the Claude's Discretion point in D-14, surface gaps that are small-effort but high-impact:

- <gap name> — <why it's a quick win>

### Migration Risk Callouts

Per the specifics block, flag any current Phase 7 code that would need to **change** (not just be extended) to support a gap:

- <file:line> — <why it needs migration>

### Coverage Summary

- Total capabilities audited: <N>
- Phase 7 already covered: <N>
- New gaps identified: <N>
- Proposed REQ-IDs: <list — SKILL-05 through SKILL-NN>
- Proposed for v1.1: <N>
- Proposed for v2: <N>
- Proposed for backlog: <N>
</summary_appendix>
```

Populate all tables from the dossiers written in Tasks 1+2. Assign tentative REQ-IDs (SKILL-05, SKILL-06, ...) in the order the dossiers appear. Plan 02 will finalize IDs when it appends to REQUIREMENTS.md — this is the draft mapping.

**Step 2 — Self-check pass (encoding the D-11/D-12 evidence standard).**

Run these self-check commands and confirm counts match:
  1. `grep -c '### Gap:' 07.1-GAP-REPORT.md` — note the total.
  2. `grep -c 'Spec quote:' 07.1-GAP-REPORT.md` — must equal the gap count.
  3. `grep -c 'Current state:' 07.1-GAP-REPORT.md` — must equal the gap count.
  4. `grep -c 'Delta:' 07.1-GAP-REPORT.md` — must equal the gap count.
  5. `grep -c 'Severity:' 07.1-GAP-REPORT.md` — must equal the gap count.
  6. `grep -c 'Effort:' 07.1-GAP-REPORT.md` — must equal the gap count.
  7. `grep -c 'Milestone:' 07.1-GAP-REPORT.md` — must equal the gap count.
  8. `grep -c 'Recommended REQ-ID:' 07.1-GAP-REPORT.md` — must equal the gap count.
  9. `grep -c 'Proposed REQ-ID:' 07.1-GAP-REPORT.md` — must equal the gap count.

If any count mismatch: fix the missing dossiers until all counts align.

**Step 3 — Verify no Rust files were modified.**

Run `git status crates/` — if any `crates/**/*.rs` file appears in the diff, revert it immediately. This phase is documentation-only per D-01.

**Step 4 — Flip status to complete.**

Update the frontmatter `status:` from `in_progress` to `complete`. Fill in final counts in the `<summary>` block at the top.

**Step 5 — Add a navigation index at the very top (under the title).**

Below `# Phase 07.1 — Skills System Gap Report` add a one-liner table of contents:

```markdown
**Contents:** [Summary](#summary) · [Capability Matrix](#capability-matrix) · [Gap Dossiers](#per-gap-dossiers) · [Summary Appendix](#summary-appendix)
```

**Do NOT touch any Rust source files. Read-only.**
  </action>
  <verify>
    <automated>bash -c 'R=.planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md; G=$(grep -c "### Gap:" "$R"); SQ=$(grep -c "Spec quote:" "$R"); CS=$(grep -c "Current state:" "$R"); DL=$(grep -c "Delta:" "$R"); SV=$(grep -c "Severity:" "$R"); [ "$G" -ge 15 ] && [ "$SQ" -ge "$G" ] && [ "$CS" -ge "$G" ] && [ "$DL" -ge "$G" ] && [ "$SV" -ge "$G" ] && grep -q "Summary Appendix" "$R" && grep -q "Quick Wins" "$R" && grep -q "status: complete" "$R" && [ -z "$(git status --porcelain crates/ 2>/dev/null | grep -v "^??")" ]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '### Gap:' 07.1-GAP-REPORT.md` returns >= 15 (realistic minimum after Groups A-D)
    - `grep -c 'Spec quote:' 07.1-GAP-REPORT.md` equals the Gap count (every dossier has a spec quote)
    - `grep -c 'Current state:' 07.1-GAP-REPORT.md` equals the Gap count
    - `grep -c 'Delta:' 07.1-GAP-REPORT.md` equals the Gap count
    - `grep -c 'Severity:' 07.1-GAP-REPORT.md` equals the Gap count
    - `grep -c 'Effort:' 07.1-GAP-REPORT.md` equals the Gap count
    - `grep 'Summary Appendix' 07.1-GAP-REPORT.md` returns >= 1
    - `grep 'Quick Wins' 07.1-GAP-REPORT.md` returns >= 1
    - `grep 'Migration Risk' 07.1-GAP-REPORT.md` returns >= 1
    - `grep 'status: complete' 07.1-GAP-REPORT.md` returns 1 (frontmatter finalized)
    - `grep 'SKILL-05' 07.1-GAP-REPORT.md` returns >= 1 (at least one REQ-ID draft proposed)
    - `git status --porcelain crates/` shows no modified Rust files (only untracked OK)
    - `git status --porcelain` shows `07.1-GAP-REPORT.md` as new/modified file
  </acceptance_criteria>
  <done>07.1-GAP-REPORT.md is complete: header, summary, capability matrix, per-gap dossiers (Groups A-D), summary appendix with v1.1/v2/backlog buckets, quick wins, migration risk callouts, and draft SKILL-05+ proposals. Status flipped to complete. No Rust files modified. Report is ready for Plan 02 to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — this is an audit-only phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-01 | N/A | Documentation-only deliverable | accept | Audit produces a single Markdown report (`07.1-GAP-REPORT.md`). No code, services, endpoints, or data handling are introduced. No new attack surface. Per D-01, no Rust source files are modified. The security gate is satisfied by the phase's nature as pure documentation. |
</threat_model>

<verification>
**Per-task verification is encoded in each `<verify>` block.**

**Phase-level verification:**
- `07.1-GAP-REPORT.md` exists, has frontmatter with `status: complete`, and contains Capability Matrix + Per-Gap Dossiers + Summary Appendix sections
- Every `### Gap:` section has all required fields (Spec quote, Current state, Test coverage, Delta, Recommended scope, Severity, Effort, Milestone)
- Every claim is backed by a file:line citation OR explicitly marked as "absent"
- Python hermes-agent reference files are cited (skills_tool.py, skills_hub.py, skills_guard.py, skills_sync.py, skill_manager_tool.py)
- `.planning/research/SKILLS.md` is cited at least once (D-18)
- Phase 7 is treated as authoritative — no re-audit of SKILL-01..SKILL-04 (D-17)
- No `crates/**/*.rs` files are modified (D-01)
- No changes to `.planning/ROADMAP.md` (D-16)
- No changes to `.planning/REQUIREMENTS.md` in this plan (Plan 02's job)
</verification>

<success_criteria>
1. `07.1-GAP-REPORT.md` is a complete, evidence-backed gap analysis across all 14 capability areas named in the phase title
2. Every gap dossier cites specific file:line in both current ironhermes code (or "absent") and Python hermes-agent reference
3. Capability matrix has a status, severity, effort, milestone, and proposed REQ-ID for every audited row
4. Summary appendix groups gaps into v1.1 / v2 / backlog with proposed REQ-IDs SKILL-05..SKILL-NN ready for Plan 02 to mint
5. No Rust source files modified (audit-only phase character preserved)
6. Plan 02 can execute without re-reading Phase 7 code — the GAP-REPORT.md is self-contained evidence for REQUIREMENTS.md mutation
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-01-SUMMARY.md` following the standard summary template:
- What was audited (the 14 capability areas)
- Total gaps identified with severity breakdown
- Key findings (top 3 surprises, if any)
- Path to GAP-REPORT.md
- Handoff notes for Plan 02 (which dossiers → which REQ-ID range)
</output>
