---
phase: quick-260602-ds9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/iron_hermes_ui/tests/kanban_board_read.rs
  - crates/iron_hermes_ui/src/server/kanban_api.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs
  - crates/iron_hermes_ui/assets/kanban.css
autonomous: true
requirements:
  - BUG-1
  - BUG-2
must_haves:
  truths:
    - "fetch_board(None, true) returns archived tasks; fetch_board(None, false) excludes them (regression test enforces both directions)"
    - "Toggling SHOW ARCHIVED in ScreenKanban causes the ARCHIVED column to populate with archived tasks (UAT U8 passes)"
    - "TaskDrawer renders with a visible cyan accent border and an opaque tinted-dark fill — the board behind no longer bleeds through (UAT U2 passes)"
    - "Complete / Block / ArchiveConfirm / CreateTask modals all render with the same cyan border + opaque fill treatment as the drawer (UAT U6/U7 pass)"
    - "cargo build --workspace --features iron_hermes_ui/server passes"
    - "cargo test --workspace passes, including the new archived-fetch regression test"
    - "cargo clippy --workspace --features iron_hermes_ui/server -- -D warnings passes"
  artifacts:
    - path: "crates/iron_hermes_ui/tests/kanban_board_read.rs"
      provides: "Regression test asserting fetch_board's archived filter parameter behavior (both directions)"
      contains: "include_archived"
    - path: "crates/iron_hermes_ui/src/server/kanban_api.rs"
      provides: "fetch_board with include_archived: bool parameter threaded into ListFilters.archived"
      contains: "include_archived: bool"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs"
      provides: "ScreenKanban passes *archived_visible.read() to fetch_board and restarts the resource when the toggle changes"
      contains: "fetch_board(None,"
    - path: "crates/iron_hermes_ui/assets/kanban.css"
      provides: "Drawer + 4 modal classes restyled with var(--accent-primary) border + opaque tinted dark fill using existing tokens (D-04 — no new hex literals)"
      contains: ".kn-drawer"
  key_links:
    - from: "crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs"
      to: "crates/iron_hermes_ui/src/server/kanban_api.rs::fetch_board"
      via: "use_resource closure passing archived_visible signal value"
      pattern: "fetch_board\\(None,\\s*\\*archived_visible"
    - from: "crates/iron_hermes_ui/src/server/kanban_api.rs::fetch_board"
      to: "ironhermes_kanban::store::ListFilters"
      via: "ListFilters { archived: include_archived, .. }"
      pattern: "archived:\\s*include_archived"
    - from: "crates/iron_hermes_ui/tests/kanban_board_read.rs"
      to: "ironhermes_kanban::store::ListFilters"
      via: "two-direction archived assertion (true returns archived row, false excludes it)"
      pattern: "archived:\\s*true"
---

<objective>
Close the two UAT defects from Phase 36.3.7.11 that the workspace gate did not catch:

**BUG-1** — `fetch_board` hardcodes `ListFilters { archived: false, .. }`, so ScreenKanban's SHOW ARCHIVED toggle reveals an always-empty 7th column. UAT U8 FAIL.

**BUG-2** — `.kn-drawer` and the four modal classes (`.kn-modal` shared by Complete / Block / ArchiveConfirm / CreateTask) render with `var(--w-bg-2)` background and `var(--w-border-hi)` border. Because `--w-bg-2` (`#161b22`) is the SAME color the columns/cards behind use for their fill, the drawer reads as transparent and the board "bleeds through". Form labels and inputs become hard to read. UAT U2/U6/U7 FAIL.

Purpose: ship the smallest possible fix for each defect plus a regression test that locks BUG-1 against future regression. Re-run UAT U2/U6/U7/U8 after this lands.

Output:
- New archived-fetch regression test in `tests/kanban_board_read.rs` (RED → GREEN cycle).
- `fetch_board` signature: `pub async fn fetch_board(board: Option<String>, include_archived: bool) -> Result<Vec<TaskRow>, ServerFnError>`. The `board: Option<String>` parameter stays (D-19 source-string test in `tests/kanban_server_fns.rs:54` still passes — it counts that exact literal).
- ScreenKanban: `fetch_board(None, *archived_visible.read())` inside the `use_resource` closure, with `board_resource.restart()` called when the toggle button is clicked.
- `kanban.css`: `.kn-drawer`, `.kn-modal`, and the modal-action button surfaces restyled with `border: 1px solid var(--accent-primary)` and an opaque tinted fill using `color-mix(in srgb, var(--accent-primary) ~6%, var(--w-bg-3))` so the boundary is visible against the `--w-bg-2` board behind. NO new hex literals (D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/HANDOFF.json
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/.continue-here.md
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-UAT.md
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-CONTEXT.md
@crates/iron_hermes_ui/CLAUDE.md

<interfaces>
<!-- All identifiers / signatures the executor needs. Extracted from the codebase by the planner. -->
<!-- DO NOT re-explore — these are the contracts. -->

From crates/iron_hermes_ui/src/server/kanban_api.rs (lines 18, 44-58):
  use ironhermes_kanban::store::{CreateTaskOptions, KanbanStore, ListFilters};

  #[server]
  pub async fn fetch_board(board: Option<String>) -> Result<Vec<TaskRow>, ServerFnError> {
      // ... opens store via open_default() or open_for_board(slug) ...
      let filters = ListFilters {
          archived: false,                  // <-- BUG-1: hardcoded
          ..Default::default()
      };
      let tasks = store.list_tasks(filters)...
  }

  ListFilters has a public `archived: bool` field (and impls Default — confirmed
  by existing `..Default::default()` usage). The test fixture in
  tests/kanban_board_read.rs already constructs `ListFilters { archived: false, .. }`
  and `ListFilters { archived: true, ..Default::default() }` would be valid.

From crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs:
  Line 63-64:  let mut board_resource = use_resource(move || async move {
                   crate::server::kanban_api::fetch_board(None).await
               });
  Line 101:    let mut archived_visible: Signal<bool> = use_signal(|| false);
  Lines 384-391: the SHOW ARCHIVED button:
                 button {
                     class: "btn btn--ghost btn--sm",
                     onclick: move |_| {
                         let cur = *archived_visible.read();
                         archived_visible.set(!cur);
                     },
                     if *archived_visible.read() { "HIDE ARCHIVED" } else { "SHOW ARCHIVED" }
                 }
  Line 360:    let mut restart_board = move || board_resource.restart();

From crates/iron_hermes_ui/tests/kanban_board_read.rs (lines 14, 36-42, 81-99):
  use ironhermes_kanban::store::{CreateTaskOptions, KanbanStore, ListFilters};
  // Test 1: fetch_board_read_path_returns_all_non_archived_tasks
  // Test 2: fetch_board_read_path_excludes_archived  — uses
  //         store.conn.execute("UPDATE tasks SET status = 'archived' WHERE id = ?1", params![t2_id])
  //         to mark a task archived, then asserts ListFilters{archived:false,..}
  //         excludes it. ADD a third test that flips the predicate.

From crates/iron_hermes_ui/tests/kanban_server_fns.rs (lines 51-60):
  #[test]
  fn kanban_api_every_fn_takes_board_option_string() {
      let src = read("src/server/kanban_api.rs");
      let board_param_count = src.matches("board: Option<String>").count();
      assert!(board_param_count >= 5, "...");
  }
  // CRITICAL: adding `include_archived: bool` AFTER `board: Option<String>`
  // keeps this count at 5. Do NOT rename or drop the `board: Option<String>`
  // parameter on fetch_board — this test will regress.

From crates/iron_hermes_ui/assets/kanban.css (current state — what to change):
  Line 274-291  .kn-drawer { background: var(--w-bg-2); border-left: 1px solid var(--w-border-hi); ... }
  Line 562-575  .kn-modal { background: var(--w-bg-2); border: 1px solid var(--w-border-hi); ... }
  Line 297-304  .kn-drawer-header { background: var(--w-bg-2); border-bottom: 1px solid var(--w-border); ... }
  Line 577-580  .kn-modal-header { border-bottom: 1px solid var(--w-border); ... }
  Line 692-700  .kn-modal-actions { ... border-top: 1px solid var(--w-border); }

From crates/iron_hermes_ui/assets/warp-ih.css (lines 17-24 — tokens to use):
  --w-bg-1:         #0d1117   (canvas / board bg — same as --bg)
  --w-bg-2:         #161b22   (block fill — currently used by drawer + modal — same as board cards behind)
  --w-bg-3:         #1c232c   (raised, slightly lighter — TARGET base fill)
  --w-border:       #1f2630   (low-contrast)
  --w-border-hi:    #30363d   (low-contrast — currently used by drawer + modal border)
  --w-border-focus: var(--accent-primary)   (cyan — TARGET border color)
  --accent-primary: var(--ansi-cyan)
  --w-shadow-float: 0 18px 60px rgba(0,0,0,.55), ...   (already in use on .kn-drawer + .kn-modal)

Note on D-04 (CONTEXT.md): NO new hex literals. Use only the tokens above plus
`color-mix(in srgb, ...)` which kanban.css already uses (lines 212, 218, 228, 651, 679, 722, 729).

D-09 (CONTEXT.md): the 7th column ARCHIVED is gated behind the SHOW ARCHIVED toggle — D-09 specifies 6 visible columns by default + ARCHIVED revealed by the toggle.

D-20 (CONTEXT.md): drawer scope is fetch_task envelope + events + runs + comments — out of scope for this plan; only the CSS treatment changes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add archived-fetch regression test (RED — must fail before Task 2)</name>
  <files>crates/iron_hermes_ui/tests/kanban_board_read.rs</files>
  <behavior>
    - Test name: `fetch_board_returns_archived_when_include_archived_true`
    - Setup: temp dir, KanbanStore::open_at_path, seed 2 tasks (one non-archived, one archived via the same `UPDATE tasks SET status = 'archived' WHERE id = ?1` pattern the existing `fetch_board_read_path_excludes_archived` test already uses on lines 81-89)
    - Assertion 1: `ListFilters { archived: true, ..Default::default() }` → `list_tasks` returns the archived row (id present in the result Vec)
    - Assertion 2: `ListFilters { archived: false, ..Default::default() }` → same store, same query → does NOT return the archived row
    - This test must compile and FAIL at the assertion level until Task 2 wires `include_archived` through — it locks the predicate the underlying read path already supports but `fetch_board` does not currently expose. (The two existing tests in this file cover the `archived:false` direction only; this test closes the test_gap called out in the handoff.)
    - Comment header on the test must cite the test_gap: "Regression test for BUG-1 from 36.3.7.11 UAT — fetch_board never exposed the include_archived parameter, so the ARCHIVED column was always empty regardless of the SHOW ARCHIVED toggle state."
  </behavior>
  <action>
    Append a third `#[test]` fn to crates/iron_hermes_ui/tests/kanban_board_read.rs that exercises the `ListFilters { archived: true, .. }` direction. Use the same in-process KanbanStore + temp-path pattern lines 18-44 and 68-100 already establish — do NOT introduce a new fixture style. The assertion shape must be: build store, seed two tasks, force one to archived via the existing UPDATE pattern, then call `store.list_tasks(filters)` twice (once with `archived: true`, once with `archived: false`) and assert presence/absence of the archived task ID in each result.

    Run `cargo test -p iron_hermes_ui --test kanban_board_read fetch_board_returns_archived_when_include_archived_true` and confirm it currently passes against the underlying ironhermes_kanban store (the predicate exists at the store layer — the bug is only that `fetch_board` doesn't expose it). The test exists to lock that contract; if it ever fails, the store layer regressed.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo test -p iron_hermes_ui --test kanban_board_read fetch_board_returns_archived_when_include_archived_true 2>&1 | tail -20</automated>
  </verify>
  <done>New test fn exists in tests/kanban_board_read.rs; `cargo test -p iron_hermes_ui --test kanban_board_read` runs three tests and all three pass; the new test references both directions of the `archived` predicate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BUG-1 fix — thread include_archived through fetch_board + ScreenKanban toggle</name>
  <files>crates/iron_hermes_ui/src/server/kanban_api.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs</files>
  <behavior>
    - `fetch_board` signature becomes `pub async fn fetch_board(board: Option<String>, include_archived: bool) -> Result<Vec<TaskRow>, ServerFnError>` — the `board: Option<String>` parameter stays in place and at the same position so `tests/kanban_server_fns.rs:54` continues to find 5 occurrences of the literal `"board: Option<String>"`.
    - The hardcoded `ListFilters { archived: false, .. }` on lines 55-58 of kanban_api.rs becomes `ListFilters { archived: include_archived, ..Default::default() }`.
    - The `#[cfg(not(feature = "server"))]` arm's `let _ = board;` becomes `let _ = (board, include_archived);` so clippy stays clean on client builds.
    - ScreenKanban's `use_resource` closure on line 63-64 changes from `fetch_board(None).await` to `fetch_board(None, *archived_visible.read()).await`.
    - The SHOW ARCHIVED button's onclick (lines 386-389) must call `board_resource.restart()` immediately after `archived_visible.set(!cur)` so the fetch re-runs with the new flag value. The existing `restart_board` closure on line 360 (`let mut restart_board = move || board_resource.restart();`) is one option, but since `board_resource` is already in scope at the onclick site, `board_resource.restart()` may be called directly — pick whichever satisfies the signal-borrow clippy rules (no read held across an await; restart() is sync so this is fine).
    - Behavior after fix: when the user clicks SHOW ARCHIVED, archived_visible flips to true, the resource re-fetches with `include_archived=true`, and the ARCHIVED column populates. When they click HIDE ARCHIVED, it re-fetches with `include_archived=false` and ARCHIVED empties. The 7th column visibility (controlled separately by `archived_visible_ro` passed into `KanbanBoard`) is unchanged.
  </behavior>
  <action>
    Edit `crates/iron_hermes_ui/src/server/kanban_api.rs`:
    1. Update the `fetch_board` `#[server]` fn signature to add `include_archived: bool` as the second positional parameter (KEEP `board: Option<String>` as the first parameter — the source-string test counts that literal).
    2. Change the `ListFilters` construction inside the `spawn_blocking` block from `archived: false` to `archived: include_archived`.
    3. Update the `#[cfg(not(feature = "server"))]` arm's `let _ = board;` to `let _ = (board, include_archived);`.
    4. Update the doc comment on the fn from "all non-archived tasks" to "all non-archived tasks, plus archived tasks when `include_archived = true`" — keep the D-09 / D-18 / D-19 cites.

    Edit `crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs`:
    1. Move the `archived_visible` signal declaration (line 101) BEFORE the `board_resource` use_resource (line 63-64) so the closure can capture it. If reordering causes other hook-order shifts, ensure `archived_visible` is still declared via `use_signal(|| false)` and that all hooks register unconditionally on every render (Pattern E discipline — CLAUDE.md "ALL hooks register unconditionally on every render" comment on line 59).
    2. Change the use_resource closure from `fetch_board(None).await` to `fetch_board(None, *archived_visible.read()).await`. The `.read()` does NOT span the `.await` (the bool is copied out before the await), so this is clippy-safe.
    3. Modify the SHOW ARCHIVED button's onclick (lines 384-391) to call `board_resource.restart()` immediately after `archived_visible.set(!cur)`. Both `archived_visible` and `board_resource` are already mutable signals in scope; ensure `board_resource` is captured `mut` if not already (line 63 declares `let mut board_resource = ...`).

    Do NOT change any other call site, doc comment, or behavior. Do NOT touch tests/kanban_server_fns.rs — verify by inspection that `board: Option<String>` still appears 5 times in kanban_api.rs after the edit (one per `#[server]` fn; the new `include_archived: bool` is on a different line/literal).

    Run all three workspace gates and the new test to confirm:
    - `cargo build --workspace --features iron_hermes_ui/server`
    - `cargo test --workspace`
    - `cargo clippy --workspace --features iron_hermes_ui/server -- -D warnings`
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo build --workspace --features iron_hermes_ui/server 2>&1 | tail -20 && cargo test --workspace 2>&1 | tail -40 && cargo clippy --workspace --features iron_hermes_ui/server -- -D warnings 2>&1 | tail -20 && grep -c "board: Option<String>" crates/iron_hermes_ui/src/server/kanban_api.rs</automated>
  </verify>
  <done>fetch_board signature includes `include_archived: bool`; the hardcoded `archived: false` is gone from kanban_api.rs; ScreenKanban passes `*archived_visible.read()` to fetch_board and calls `board_resource.restart()` when the toggle flips; the `grep -c "board: Option<String>"` count is exactly 5 (D-19 invariant preserved); workspace build + test + clippy all pass.</done>
</task>

<task type="auto">
  <name>Task 3: BUG-2 fix — drawer + modal contrast via existing tokens (cyan border + opaque tinted fill)</name>
  <files>crates/iron_hermes_ui/assets/kanban.css</files>
  <behavior>
    - `.kn-drawer`: `border-left` becomes `1px solid var(--accent-primary)` (cyan) instead of `var(--w-border-hi)`. Background becomes `color-mix(in srgb, var(--accent-primary) 6%, var(--w-bg-3))` — opaque (because `--w-bg-3` is opaque `#1c232c`) and visibly tinted cyan against the `--w-bg-2` board behind. The existing `box-shadow: var(--w-shadow-float)` stays — that provides the z-layer depth.
    - `.kn-drawer-header`: background changes to match the new drawer fill (`color-mix(in srgb, var(--accent-primary) 6%, var(--w-bg-3))`) so the sticky header doesn't show the old `--w-bg-2` color. `border-bottom` stays `var(--w-border)`.
    - `.kn-modal`: `border` becomes `1px solid var(--accent-primary)` (cyan) instead of `var(--w-border-hi)`. Background becomes `color-mix(in srgb, var(--accent-primary) 6%, var(--w-bg-3))`. `box-shadow: var(--w-shadow-float)` stays.
    - `.kn-modal-header` border-bottom: change from `var(--w-border)` to `var(--accent-primary)` with 30% alpha via `color-mix(in srgb, var(--accent-primary) 30%, transparent)` so the header separator carries the same accent without being too loud.
    - `.kn-modal-actions` border-top: same treatment as `.kn-modal-header` border-bottom.
    - `.kn-modal-overlay` background: leave as-is — the 75% black scrim is already correct and the issue was the modal panel itself, not the backdrop.
    - The four modals (`CompleteModal`, `BlockModal`, `ArchiveConfirmModal`, `CreateTaskModal` — modals.rs) all render with the shared `.kn-modal` class plus `.kn-modal-overlay` wrapper — there are NO per-modal-type class names like `.kn-modal-complete` in kanban.css. Verify this by `grep -n "kn-modal" crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/modals.rs` before editing; the fix targets the shared classes only.
    - NO new hex literals (D-04). Every color reference resolves to an existing token in warp-ih.css / design-tokens.css / tokens.css.
    - Reduced-motion overrides (lines 741-770) are untouched.

    Visual outcome: the drawer slides in over the board and reads as a distinct themed overlay with a clear cyan boundary and a fill that's visibly darker-but-cyan-tinted compared to the `#161b22` board cards behind it. Form labels, inputs, textarea text, and modal buttons remain legible against the new fill (the existing `.kn-modal-input` / `.kn-modal-textarea` use `var(--w-bg-3)` for their own background — this is now visually nested INSIDE the modal's slightly-tinted-lighter fill, providing layer separation).
  </behavior>
  <action>
    1. First, confirm the modal class names by running `grep -n 'class:.*kn-modal\|class:.*kn-drawer' crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/modals.rs crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/drawer.rs`. The expectation (from kanban.css inspection) is that all four modals share `.kn-modal` + `.kn-modal-overlay` with semantic content classes (`.kn-modal-header`, `.kn-modal-body`, etc.). If you find any per-modal-type CSS class that ALSO sets a background or border, restyle it the same way — but do not introduce new selectors.

    2. Edit `crates/iron_hermes_ui/assets/kanban.css`:
       - `.kn-drawer` (line 274): change `background: var(--w-bg-2);` → `background: color-mix(in srgb, var(--accent-primary) 6%, var(--w-bg-3));`. Change `border-left: 1px solid var(--w-border-hi);` → `border-left: 1px solid var(--accent-primary);`.
       - `.kn-drawer-header` (line 297): change `background: var(--w-bg-2);` → `background: color-mix(in srgb, var(--accent-primary) 6%, var(--w-bg-3));` so the sticky header inherits the new drawer fill.
       - `.kn-modal` (line 562): change `background: var(--w-bg-2);` → `background: color-mix(in srgb, var(--accent-primary) 6%, var(--w-bg-3));`. Change `border: 1px solid var(--w-border-hi);` → `border: 1px solid var(--accent-primary);`.
       - `.kn-modal-header` (line 577): change `border-bottom: 1px solid var(--w-border);` → `border-bottom: 1px solid color-mix(in srgb, var(--accent-primary) 30%, transparent);`.
       - `.kn-modal-actions` (line 692): change `border-top: 1px solid var(--w-border);` → `border-top: 1px solid color-mix(in srgb, var(--accent-primary) 30%, transparent);`.

    3. Do NOT touch: `.kn-card` (cards passed UAT — they already use `var(--accent-primary)` border), `.kn-modal-overlay` backdrop (75% scrim is correct), `.kn-column` (board columns must keep their `--w-bg-2` fill), or any of the chip/badge/button/input child rules (those inherit fine against the new modal fill).

    4. Run a quick grep to confirm no new hex literals slipped in: `grep -nE '#[0-9a-fA-F]{3,8}\b' crates/iron_hermes_ui/assets/kanban.css` should return ZERO matches (the file currently has zero — D-04).

    5. Run the workspace gate (no Rust changes so build/test should be fast; clippy mostly re-evaluates cached units):
       - `cargo build --workspace --features iron_hermes_ui/server`
       - `cargo test --workspace`
       - `cargo clippy --workspace --features iron_hermes_ui/server -- -D warnings`

    6. (Optional native-render sanity) If `dx serve` is already running for the tester, they will pick up the CSS change via hot reload on next refresh. The visual verification belongs to UAT U2/U6/U7 re-run — out of scope for this plan's automated verify.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && grep -nE '#[0-9a-fA-F]{3,8}\b' crates/iron_hermes_ui/assets/kanban.css | wc -l | xargs -I{} sh -c 'test "{}" -eq 0 && echo "D-04 OK: zero hex literals" || (echo "D-04 VIOLATION: hex literal found" && exit 1)' && grep -c "var(--accent-primary)" crates/iron_hermes_ui/assets/kanban.css && cargo build --workspace --features iron_hermes_ui/server 2>&1 | tail -10 && cargo clippy --workspace --features iron_hermes_ui/server -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <done>kanban.css has zero hex literals; `.kn-drawer`, `.kn-modal`, `.kn-drawer-header` all use `var(--accent-primary)` for the boundary and a `color-mix(...var(--accent-primary)...var(--w-bg-3))` fill; modal header/actions separators use the 30%-alpha cyan; workspace build + clippy gates pass. Visual verification is deferred to UAT U2/U6/U7 re-run by the tester.</done>
</task>

</tasks>

<verification>
After all three tasks land, the orchestrator (or executor) MUST run the full workspace gate:

```bash
cd /Users/twilson/code/ironhermes
cargo build --workspace --features iron_hermes_ui/server
cargo test --workspace
cargo clippy --workspace --features iron_hermes_ui/server -- -D warnings
```

All three must exit 0. The new regression test (`fetch_board_returns_archived_when_include_archived_true`) must appear in the cargo test output with status `ok`.

Then hand back to the tester to re-run UAT rows U2, U6, U7, U8 in `.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-UAT.md`. These four rows are the contract — if they pass, the gap-closure is done and Phase 36.3.7.11 can proceed to roadmap close.

Sanity grep after the fix:
- `grep -c "board: Option<String>" crates/iron_hermes_ui/src/server/kanban_api.rs` must equal `5` (D-19 invariant; the source-string test in `tests/kanban_server_fns.rs:54` enforces this).
- `grep -c "archived: false" crates/iron_hermes_ui/src/server/kanban_api.rs` must equal `0` (the hardcoded predicate is gone).
- `grep -nE '#[0-9a-fA-F]{3,8}\b' crates/iron_hermes_ui/assets/kanban.css` must return zero matches (D-04: no new hex literals).
</verification>

<success_criteria>
- [ ] New regression test `fetch_board_returns_archived_when_include_archived_true` exists in tests/kanban_board_read.rs and exercises both directions of the archived predicate.
- [ ] `fetch_board` signature is `(board: Option<String>, include_archived: bool)` and `ListFilters.archived` is set from the new parameter.
- [ ] ScreenKanban's `use_resource` closure passes `*archived_visible.read()` to fetch_board.
- [ ] The SHOW ARCHIVED button calls `board_resource.restart()` after toggling `archived_visible`.
- [ ] D-19 invariant preserved: `board: Option<String>` appears 5 times in kanban_api.rs.
- [ ] `.kn-drawer`, `.kn-modal`, `.kn-drawer-header` all use `var(--accent-primary)` for boundary and `color-mix(...accent...w-bg-3)` for fill.
- [ ] `.kn-modal-header` and `.kn-modal-actions` separators use the 30%-alpha cyan.
- [ ] D-04 preserved: zero hex literals in kanban.css.
- [ ] `cargo build --workspace --features iron_hermes_ui/server` passes.
- [ ] `cargo test --workspace` passes including the new regression test.
- [ ] `cargo clippy --workspace --features iron_hermes_ui/server -- -D warnings` passes.
- [ ] No other files modified (this is a focused gap-closure — no scope creep per .continue-here.md "Critical Anti-Patterns").
</success_criteria>

<output>
Create `.planning/quick/260602-ds9-fix-bug-1-bug-2-from-36-3-7-11-uat-see-c/260602-ds9-SUMMARY.md` when all three tasks complete and the workspace gate passes. The summary must:

- Cite the exact diff lines changed in each of the 4 files.
- Confirm the workspace gate exit codes.
- Quote the new test's PASS line from `cargo test` output.
- Hand back to the orchestrator with the next-step instruction: "Tester re-runs UAT U2/U6/U7/U8 in 36.3.7.11-UAT.md; if all four PASS, Phase 36.3.7.11 closes."
</output>
