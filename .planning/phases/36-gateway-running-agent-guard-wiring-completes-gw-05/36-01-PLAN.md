---
phase: 36-gateway-running-agent-guard-wiring-completes-gw-05
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-gateway/tests/running_agent_guard_tests.rs
  - crates/ironhermes-gateway/Cargo.toml
autonomous: true
requirements:
  - GW-05
tags:
  - gateway
  - testing
  - scaffolding

must_haves:
  truths:
    - "Test file crates/ironhermes-gateway/tests/running_agent_guard_tests.rs exists"
    - "All 11 GW-05 sub-behavior test stubs and the scaffold-compile sentinel exist as #[ignore] functions"
    - "Test helpers (build_test_session_store, RecordingPlatformAdapter, build_test_handler) compile and are reachable"
    - "cargo test -p ironhermes-gateway --tests --no-run succeeds (harness compiles)"
    - "cargo test -p ironhermes-gateway running_agent_guard returns 0 passed (all 11 ignored)"
  artifacts:
    - path: "crates/ironhermes-gateway/tests/running_agent_guard_tests.rs"
      provides: "Integration test harness for GW-05 guard behavior"
      contains:
        - "test_session_isolation"
        - "test_model_rejected_when_running"
        - "test_stop_bypasses_guard"
        - "test_new_bypasses_guard"
        - "test_status_bypasses_guard"
        - "test_queue_bypasses_guard"
        - "test_guard_clears_on_success"
        - "test_guard_clears_on_error"
        - "test_alias_bypasses_guard"
        - "test_freetext_rejected_when_running"
        - "test_stop_reads_real_flag"
        - "#[ignore]"
  key_links:
    - from: "crates/ironhermes-gateway/tests/running_agent_guard_tests.rs"
      to: "crates/ironhermes-gateway/src/session.rs"
      via: "use ironhermes_gateway::session::{SessionStore, SessionKey}"
      pattern: "use ironhermes_gateway::"
    - from: "crates/ironhermes-gateway/tests/running_agent_guard_tests.rs"
      to: "crates/ironhermes-gateway/src/adapter.rs"
      via: "RecordingPlatformAdapter implements PlatformAdapter trait"
      pattern: "impl PlatformAdapter for RecordingPlatformAdapter"

notes:
  - "Wave 0 (Nyquist) scaffold per 36-VALIDATION.md. All 11 stubs land #[ignore]'d so subsequent plans flip them on as behavior is implemented."
  - "Test file uses tests/ integration directory (Cargo conventions) so it links against the public surface — drives Plan 02 to expose enough to be testable."
  - "Out-of-scope follow-ups carried forward: web UI never intercepts slash commands; per-turn LLM cancel still missing on gateway (handler.rs:1032); CLI/TUI/gateway state model unification."
---

<objective>
Create the Wave 0 test scaffold for Phase 36: an integration test file at
`crates/ironhermes-gateway/tests/running_agent_guard_tests.rs` containing all 11
GW-05 sub-behavior tests from 36-VALIDATION.md as `#[ignore]` stubs, plus the
test helpers (mock SessionStore builder, recording PlatformAdapter, handler
builder) that Plan 02 will need to make the tests pass.

Purpose: Establish the validation contract before implementation, so Plan 02 has
a concrete acceptance bar to drive against. Each `#[ignore]` stub names the
exact behavior the implementation must satisfy; un-ignoring is the Plan 03 gate.

Output: One new test file plus optional dev-dependency additions to
`crates/ironhermes-gateway/Cargo.toml` if `async-trait` / `tokio-test` /
`mockall`-style helpers are required.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-CONTEXT.md
@.planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-RESEARCH.md
@.planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-VALIDATION.md
@.planning/phases/21.1-slash-commands/21.1-REVIEWS.md
@crates/ironhermes-gateway/src/handler.rs
@crates/ironhermes-gateway/src/session.rs
@crates/ironhermes-gateway/src/adapter.rs
@crates/ironhermes-core/src/commands/context.rs

<interfaces>
<!-- Extracted from codebase via direct file reads. Executor should use these
     verbatim — no codebase exploration needed for these signatures. -->

From crates/ironhermes-gateway/src/session.rs (lines 11-50, 87-95):
- pub struct SessionKey { pub platform: Platform, pub chat_id: String, pub user_id: Option<String> }
- SessionKey::new(platform: Platform, chat_id: impl Into<String>) -> Self
- SessionKey::with_user(self, user_id: impl Into<String>) -> Self
- SessionKey::to_string_key(&self) -> String
- pub struct GatewaySession { pub key, pub session_id, pub messages, pub created_at, pub updated_at, pub model: String, /* PLAN 02 will add: pub running: Arc<AtomicBool> */ }
- pub struct SessionStore { state: Arc<Mutex<StateStore>>, sessions: HashMap<String, GatewaySession>, ... }
- SessionStore::new(state: Arc<Mutex<StateStore>>) -> Self
- SessionStore::get_or_create(&mut self, key: SessionKey, model: &str, source: &str) -> &mut GatewaySession
- SessionStore::get(&self, key: &SessionKey) -> Option<&GatewaySession>

From crates/ironhermes-gateway/src/adapter.rs (line 9-onwards):
- #[async_trait::async_trait] pub trait MessageHandler: Send + Sync
- async fn handle(&self, event: &MessageEvent, adapter: Arc<dyn PlatformAdapter>, cancel: CancellationToken) -> Result<()>
- pub trait PlatformAdapter — has send_message(&self, chat_id: &str, text: &str, reply_to: Option<...>) -> Result<SentMessage>

From crates/ironhermes-gateway/src/handler.rs:
- line 1255: existing tests/make_handler() helper for GatewayMessageHandler — model on Wave 0 helper after this pattern
- line 1261: Arc::new(RwLock::new(SessionStore::new(state_store))) — pattern for building test SessionStore

From crates/ironhermes-core/src/commands/context.rs:368:
- pub agent_running: Arc<AtomicBool>  -- already wired since Phase 21.1

VALIDATION.md test names (canonical — match these exactly):
- test_session_isolation
- test_model_rejected_when_running
- test_stop_bypasses_guard
- test_new_bypasses_guard
- test_status_bypasses_guard
- test_queue_bypasses_guard
- test_guard_clears_on_success
- test_guard_clears_on_error
- test_alias_bypasses_guard
- test_freetext_rejected_when_running
- test_stop_reads_real_flag
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add running_agent_guard_tests.rs scaffold with helpers and 11 #[ignore] stubs</name>
  <files>crates/ironhermes-gateway/tests/running_agent_guard_tests.rs, crates/ironhermes-gateway/Cargo.toml</files>
  <read_first>
    - crates/ironhermes-gateway/tests/ (list directory — confirm Cargo test layout; if no `tests/` dir, this plan creates it)
    - crates/ironhermes-gateway/src/handler.rs (lines 1248-1300 — existing #[cfg(test)] mod tests + make_handler helper, used as the construction template)
    - crates/ironhermes-gateway/src/handler.rs (lines 62-100 — GatewayMessageHandler field layout for the test builder)
    - crates/ironhermes-gateway/src/session.rs (lines 11-50 — SessionKey + GatewaySession; lines 87-130 — SessionStore::new)
    - crates/ironhermes-gateway/src/adapter.rs (full file — MessageHandler + PlatformAdapter trait surface)
    - crates/ironhermes-gateway/Cargo.toml (existing [dev-dependencies] block — confirm tokio, async-trait presence before adding anything)
    - .planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-VALIDATION.md (lines 38-58 — canonical test-name list)
    - crates/ironhermes-state/src/lib.rs (StateStore::new signature for in-memory test instance — needed by SessionStore::new helper)
  </read_first>
  <action>
Create new integration test file `crates/ironhermes-gateway/tests/running_agent_guard_tests.rs`. The file must contain, in order:

1. Module-level doc comment naming this as the Phase 36 Wave-0 scaffold (per VALIDATION.md), citing GW-05 + the 11 sub-behaviors.

2. A `mod helpers { ... }` submodule that exposes:
   - `pub fn build_test_session_store() -> std::sync::Arc<tokio::sync::RwLock<ironhermes_gateway::session::SessionStore>>` — constructs a SessionStore over an in-memory StateStore (mirror the pattern at `handler.rs:1261`). Helper signature is final: callers in later tasks accept this exact return type.
   - `pub struct RecordingPlatformAdapter` — wraps a `tokio::sync::Mutex<Vec<(String, String)>>` (chat_id, text) capture log; implement the full `PlatformAdapter` trait from `crates/ironhermes-gateway/src/adapter.rs` such that `send_message` pushes `(chat_id.to_string(), text.to_string())` into the log and returns a stub `SentMessage`. Every other trait method may return `Ok(Default::default())` / `Ok(())` as appropriate but MUST NOT panic — Plan 02 needs the harness to compile and run end-to-end. Add `pub fn messages(&self) -> Vec<(String, String)>` accessor returning a clone of the log.
   - `pub fn build_test_handler(store: Arc<RwLock<SessionStore>>) -> ironhermes_gateway::handler::GatewayMessageHandler` — mirrors the existing `make_handler()` helper at `handler.rs:1255`. If the existing helper is `pub(crate)` and not reachable from `tests/`, replicate its construction inline in this helper (do NOT change the production `make_handler` visibility — keep test-only scaffolding scoped to this file).
   - `pub fn test_session_key(chat_id: &str) -> ironhermes_gateway::session::SessionKey` — `SessionKey::new(Platform::Telegram, chat_id).with_user("u1")`.
   - `pub fn d02_error_message() -> &'static str` — returns the EXACT string `"Agent is running. Use /stop to interrupt or /queue to send after this turn."` (locked verbatim per D-02). Plan 02's source string MUST equal this constant; Plan 03's assertions reference this helper.

3. Exactly 11 `#[tokio::test]` functions, each preceded by `#[ignore = "Wave 0 scaffold — implementation lands in Plan 36-02"]`. Each test name must match the VALIDATION.md table verbatim:
   - `test_session_isolation`
   - `test_model_rejected_when_running`
   - `test_stop_bypasses_guard`
   - `test_new_bypasses_guard`
   - `test_status_bypasses_guard`
   - `test_queue_bypasses_guard`
   - `test_guard_clears_on_success`
   - `test_guard_clears_on_error`
   - `test_alias_bypasses_guard`
   - `test_freetext_rejected_when_running`
   - `test_stop_reads_real_flag`

   Body of each stub: a doc-comment describing the locked behavior (cite D-01/D-02/D-04/D-06 as appropriate), an unimplemented!()-marker that documents what Plan 02 must satisfy. Do NOT write `panic!("not implemented")` — use a `todo!("Plan 36-02: <behavior>")` so a future accidental un-ignore produces an actionable diagnostic.

4. Update `crates/ironhermes-gateway/Cargo.toml` ONLY if any of `tokio`, `async-trait`, `tokio-util` are missing from `[dev-dependencies]`. Inspect first; do not add duplicates. Add `chrono` and `uuid` to dev-deps only if helpers need them and they are not already present (likely already are — they're prod deps).

Critical constraints (do NOT violate):
- The D-02 string in `d02_error_message()` is byte-for-byte locked. Plan 02's emitted error string is compared character-by-character against this helper.
- Do NOT modify any production source file in this plan. The test file is the only new code; Cargo.toml gets at most dev-dep additions.
- Do NOT remove the comment block at `handler.rs:377-380` here — Plan 03 owns the stale-comment cleanup.
- All 11 stubs share one common shape: build_test_session_store → grab a SessionKey → (eventually) flip the running flag → dispatch a synthetic MessageEvent → assert against RecordingPlatformAdapter.messages(). The scaffold lays the call shape via TODOs even though the body is `todo!()` — saves Plan 02 context.
- `#[ignore = "..."]` attributes are mandatory on every test — running `cargo test -p ironhermes-gateway` in this plan must report `running 11 tests ... test result: ok. 0 passed; 0 failed; 11 ignored;`.

If during reading you discover the existing `tests/` directory does not exist for `ironhermes-gateway`, create it. If you discover the production `make_handler` is not pub-callable from an integration test, do NOT touch its visibility — replicate its construction logic inside `build_test_handler` and add an inline comment pointing at handler.rs:1255 as the source pattern.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p ironhermes-gateway --tests --no-run 2>&amp;1 | tail -20</automated>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p ironhermes-gateway --test running_agent_guard_tests 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source: `crates/ironhermes-gateway/tests/running_agent_guard_tests.rs` exists
    - Source: file contains exactly 11 `#[tokio::test]` annotations
    - Source: file contains exactly 11 `#[ignore` attributes (one per test)
    - Source: file contains the string `"Agent is running. Use /stop to interrupt or /queue to send after this turn."` (D-02 verbatim)
    - Source: file contains `pub struct RecordingPlatformAdapter` and `impl PlatformAdapter for RecordingPlatformAdapter`
    - Source: file contains `pub fn build_test_session_store` and `pub fn build_test_handler`
    - Source: file does NOT modify `crates/ironhermes-gateway/src/handler.rs` or `crates/ironhermes-gateway/src/session.rs` (these belong to Plan 02)
    - Source: every test function name from the 11-name list appears verbatim in the file
    - Behavior: `cargo test -p ironhermes-gateway --tests --no-run` exits 0 (compiles)
    - Behavior: `cargo test -p ironhermes-gateway --test running_agent_guard_tests` reports `0 passed; 0 failed; 11 ignored`
    - Behavior: `cargo build -p ironhermes-gateway` is byte-for-byte unchanged in stdout/stderr (no production source touched)
  </acceptance_criteria>
  <done>
Test file lands with all 11 `#[ignore]`-d stubs, all helpers compile, and the gateway test binary builds. Plan 02 inherits a known-failing acceptance bar.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test crate → production crate | Integration tests link against `ironhermes_gateway` lib; test helpers must not weaken production visibility |
| Test mocks → production traits | `RecordingPlatformAdapter` implements `PlatformAdapter` in test code only; must not leak into production binary |

## STRIDE Threat Register

(Ported from 36-RESEARCH.md Security Domain; this plan only adds test scaffolding so threats are largely structural / hygiene.)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.1-05 | Tampering | Per-session state isolation (test surface) | mitigate | Test scaffold `test_session_isolation` stub forces Plan 02 to expose APIs that make per-session isolation observable (no global flag) |
| T-21.1-08 | Elevation of Privilege | Non-slash bypass via free-text (test surface) | mitigate | Test scaffold `test_freetext_rejected_when_running` stub forces Plan 02 to guard the non-slash path |
| T-21.1-09 | Tampering | Model swap mid-turn TOCTOU (test surface) | mitigate | Test scaffold `test_model_rejected_when_running` stub forces D-04 enforcement |
| T-36-SC | Tampering | Test-only mock visibility | accept | Scaffold lives in `tests/` directory; no risk of production binary shipping mocks |
</threat_model>

<verification>
After this plan completes:
1. `cargo test -p ironhermes-gateway --tests --no-run` exits 0
2. `cargo test -p ironhermes-gateway running_agent_guard` reports 11 ignored / 0 failed / 0 passed
3. `cargo build -p ironhermes-gateway` succeeds (production code unchanged)
4. Source-grep for `"Agent is running. Use /stop to interrupt or /queue to send after this turn."` returns exactly one hit (the test helper)
</verification>

<success_criteria>
- Test file exists with all 11 named `#[ignore]` stubs
- Helpers (`build_test_session_store`, `RecordingPlatformAdapter`, `build_test_handler`, `test_session_key`, `d02_error_message`) compile and are publicly reachable within the test crate
- `cargo test -p ironhermes-gateway running_agent_guard` exits 0 with 11 ignored
- No production source file modified
- D-02 string captured verbatim in `d02_error_message()` helper
</success_criteria>

<output>
Create `.planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-01-SUMMARY.md` when done, documenting:
- Confirmed line of `handle_slash_command` / `run_agent` / `GatewaySession` from the read_first pass
- Whether the existing `make_handler()` was reachable or had to be replicated
- Any dev-dep additions to `Cargo.toml`
- The exact 11 test names committed (proof against silent renames)
</output>
</content>
</invoke>