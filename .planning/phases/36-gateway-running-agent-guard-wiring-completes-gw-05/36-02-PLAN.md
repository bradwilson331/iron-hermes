---
phase: 36-gateway-running-agent-guard-wiring-completes-gw-05
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - crates/ironhermes-gateway/src/session.rs
  - crates/ironhermes-gateway/src/handler.rs
  - crates/ironhermes-gateway/tests/running_agent_guard_tests.rs
autonomous: true
requirements:
  - GW-05
tags:
  - gateway
  - concurrency
  - security
  - guard

must_haves:
  truths:
    - "GatewaySession holds an Arc<AtomicBool> running flag initialized to false on construction"
    - "RunningAgentGuard RAII type sets flag=true on new() and flag=false on Drop"
    - "run_agent constructs RunningAgentGuard at function entry (single call site covers all 5 invocations)"
    - "handle_slash_command retrieves the per-session running flag from SessionStore and populates CommandContext.agent_running with that exact handle"
    - "When flag==true and resolved command name is NOT in {stop, new, status, queue}, handle_slash_command rejects with the exact D-02 error string and returns Ok(())"
    - "When flag==true and the non-slash path in MessageHandler::handle is taken, the gateway rejects with the exact D-02 error string"
    - "Bypass list checks resolved_def.name (post-alias-resolution canonical name) so /reset -> new bypasses"
    - "Per-session isolation: session A running does not block session B"
    - "Flag clears on run_agent Ok and Err exit (RAII guarantee)"
    - "Old shim at handler.rs:377-380 (Arc::new(AtomicBool::new(false))) is gone — replaced by the real per-session flag retrieval"
    - "All 11 tests from Plan 01 are un-ignored and passing"
  artifacts:
    - path: "crates/ironhermes-gateway/src/session.rs"
      provides: "GatewaySession with `pub running: Arc<AtomicBool>` field; SessionStore::get_running_flag(&SessionKey) accessor"
      contains: "pub running: Arc<AtomicBool>"
    - path: "crates/ironhermes-gateway/src/handler.rs"
      provides: "RunningAgentGuard RAII type; is_bypass(name) predicate; guard checks at both dispatch sites; RunningAgentGuard::new at top of run_agent body"
      contains:
        - "struct RunningAgentGuard"
        - "impl Drop for RunningAgentGuard"
        - "fn is_bypass"
        - "Agent is running. Use /stop to interrupt or /queue to send after this turn."
    - path: "crates/ironhermes-gateway/tests/running_agent_guard_tests.rs"
      provides: "All 11 Wave-0 tests now un-ignored and exercising real behavior"
      contains: "#[tokio::test]"
  key_links:
    - from: "crates/ironhermes-gateway/src/handler.rs (handle_slash_command)"
      to: "GatewaySession.running"
      via: "SessionStore read lock → get(&session_key) → s.running.clone()"
      pattern: "running.clone\\(\\)|get_running_flag"
    - from: "crates/ironhermes-gateway/src/handler.rs (run_agent)"
      to: "RunningAgentGuard::new(flag)"
      via: "first-statement RAII binding"
      pattern: "RunningAgentGuard::new"
    - from: "crates/ironhermes-gateway/src/handler.rs (MessageHandler::handle non-slash arm)"
      to: "D-02 reject path"
      via: "flag.load(SeqCst) check before run_agent"
      pattern: "Agent is running\\. Use /stop"

notes:
  - "Pitfall 1 (non-slash bypass) IS in scope this plan — non-slash MessageHandler::handle path gets the same guard as handle_slash_command."
  - "is_bypass is local to handler.rs in this phase. Promotion to ironhermes-core (so CLI can share) is deferred — captured in 36-03 follow-ups."
  - "Stale comment cleanup at handler.rs:377-380 is left to Plan 03 (so Plan 02's diff is purely additive on that range — minimizes merge risk)."
  - "Per D-05: NO state added to AgentRuntime. State lives in SessionStore/GatewaySession only."
  - "Out-of-scope follow-ups: per-turn LLM cancel (handler.rs:1032); web UI slash interception; CLI/gateway unified mechanism. Captured in Plan 03 backlog."
---

<objective>
Land the running-agent guard for Phase 36. Three independent edits, all required
together to make the 11 Wave-0 tests pass:

1. Add `pub running: Arc<AtomicBool>` to `GatewaySession` (D-03/D-05), initialized
   false on construction. Add a `SessionStore::get_running_flag(&SessionKey) ->
   Arc<AtomicBool>` accessor that returns an existing flag or a fresh false-Arc
   if the session has not been created yet (creation happens inside `run_agent`).

2. Add `RunningAgentGuard` RAII type + `is_bypass(name)` predicate to
   `handler.rs`. Wire `RunningAgentGuard::new(flag)` as the FIRST statement of
   `run_agent`'s body (covers all 5 call sites: 523, 589, 772, 797, 1244).

3. Wire the flag through `handle_slash_command` (replacing the shim at
   handler.rs:377-380) AND through the non-slash arm of `MessageHandler::handle`
   (Pitfall 1). On flag==true + non-bypass: reject with the D-02 verbatim error
   via `adapter.send_message` and return `Ok(())`.

Then un-ignore all 11 tests from Plan 01.

Purpose: Close GW-05. Eliminate the codex HIGH-1 (guard never fires) and HIGH-2
(model-swap TOCTOU) findings from the Phase 21.1 cross-AI review.

Output: GW-05 marked Complete in REQUIREMENTS.md is a Plan 03 follow-up;
behavior delivery is this plan's bar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-CONTEXT.md
@.planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-RESEARCH.md
@.planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-VALIDATION.md
@.planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-01-SUMMARY.md
@.planning/phases/21.1-slash-commands/21.1-REVIEWS.md
@crates/ironhermes-gateway/src/session.rs
@crates/ironhermes-gateway/src/handler.rs
@crates/ironhermes-gateway/src/adapter.rs
@crates/ironhermes-core/src/commands/context.rs
@crates/ironhermes-core/src/commands/handlers.rs
@crates/ironhermes-core/src/commands/registry.rs
@crates/ironhermes-gateway/tests/running_agent_guard_tests.rs

<interfaces>
<!-- Critical existing types/functions consumed by this plan. -->

GatewaySession (session.rs:41-48):
  pub struct GatewaySession {
      pub key: SessionKey,
      pub session_id: String,
      pub messages: Vec<ChatMessage>,
      pub created_at: DateTime<Utc>,
      pub updated_at: DateTime<Utc>,
      pub model: String,
      // PLAN 02 ADDS: pub running: Arc<AtomicBool>,
  }

GatewaySession::new (session.rs:50-61):
  Currently initializes 6 fields. Plan 02 adds:
  running: Arc::new(AtomicBool::new(false)),

SessionStore (session.rs:87): pub struct SessionStore with `sessions: HashMap<String, GatewaySession>`
SessionStore::get (session.rs:236): pub fn get(&self, key: &SessionKey) -> Option<&GatewaySession>

CommandContext (ironhermes-core/src/commands/context.rs:368):
  pub agent_running: Arc<AtomicBool>  -- the field this plan finally populates with a real per-session handle

CommandContext::new (context.rs):
  pub fn new(platform: Platform, session_id: String, agent_running: Arc<AtomicBool>) -> Self

CommandRouter::resolve (registry.rs):
  Returns ResolveResult::Exact(def) | ResolveResult::PrefixMatch(def) | ResolveResult::NotFound
  def.name is the CANONICAL command name (post-alias resolution)
  e.g., user types "/reset" → router resolves to def.name == "new"

Bypass list (D-01, locked):
  fn is_bypass(name: &str) -> bool { matches!(name, "stop" | "new" | "status" | "queue") }
  (Excludes "approve" / "deny" intentionally — D-01.)

D-02 error string (LOCKED VERBATIM):
  "Agent is running. Use /stop to interrupt or /queue to send after this turn."

run_agent (handler.rs:801):
  async fn run_agent(&self, event: &MessageEvent, adapter: Arc<dyn PlatformAdapter>, cancel: CancellationToken, processed: ProcessedAttachments) -> Result<()>
  Called from 5 sites in handler.rs (lines 523, 589, 772, 797, 1244).
  Guard goes INSIDE run_agent at the top — single site covers all 5 invocations.

MessageHandler::handle (handler.rs:1221-1245):
  Slash path → handle_slash_command (already has flag retrieval point at line 377-380, this plan replaces the shim)
  Non-slash path → directly calls run_agent at line 1244 — PITFALL 1: needs a guard check here BEFORE the run_agent call

handle_with_multimodal (handler.rs:780-798):
  Same shape as MessageHandler::handle but on the multimodal path
  Line 792 branches on slash prefix; non-slash path at line 797 calls run_agent unconditionally
  PITFALL 1 ALSO APPLIES HERE — both non-slash entry sites need the guard

with_rate_limit_retry (handler.rs:34):
  Existing helper that wraps adapter.send_message calls (visible at line 450, 474, 495, etc.)
  Use this pattern for the D-02 error send

SessionKey (session.rs:11): pub struct with platform, chat_id, user_id (Option)
SessionKey::to_string_key (session.rs:31): pub fn → String
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add `running` field to GatewaySession + SessionStore accessor</name>
  <files>crates/ironhermes-gateway/src/session.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/session.rs (full file — confirm GatewaySession at line 41, GatewaySession::new at line 50, SessionStore at line 87, SessionStore::get at line 236)
    - .planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-CONTEXT.md (D-03, D-05, D-06)
    - .planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-RESEARCH.md (Pattern 3, Pitfall 6)
  </read_first>
  <action>
Edit `crates/ironhermes-gateway/src/session.rs`:

1. Add import: `use std::sync::atomic::AtomicBool;` (place near the existing `use std::sync::{Arc, Mutex};` at line 1).

2. Add a public field `running: Arc<AtomicBool>` to the `GatewaySession` struct (line 41 region). Per D-03/D-05, this is the canonical per-session running flag. Place it as the LAST field of the struct so the diff against the current 6-field layout is purely additive (minimizes merge surface).

3. In `GatewaySession::new` (line 50-61), initialize `running: Arc::new(AtomicBool::new(false))`. The flag is false at session birth; `RunningAgentGuard` flips it true and back. Add a brief doc comment on the field referencing D-03 + Phase 36.

4. Add a public method on `SessionStore`:
   ```
   pub fn get_running_flag(&self, key: &SessionKey) -> Arc<AtomicBool>
   ```
   Behavior: if `self.sessions.get(&key.to_string_key())` is `Some(session)`, return `session.running.clone()`. If `None` (session not yet created — first message of a chat), return `Arc::new(AtomicBool::new(false))` (a fresh false-Arc that the caller can hand to CommandContext; on the first message the flag is correctly false since no agent turn is in flight yet). Place this method right after `pub fn get` at line 236.

5. Update the struct doc comment for `GatewaySession` to mention "Phase 36: `running` flag drives per-session agent guard (D-03/D-05/D-06)." — one extra line on the existing `/// An active gateway conversation session.` block.

Critical constraints:
- Use `std::sync::atomic::AtomicBool` (NOT `tokio::sync` — D-03 says AtomicBool, the type is `Send + Sync` and lock-free).
- The field MUST be `pub`. Plan 02 Task 3 (handler) constructs a CommandContext using `session.running.clone()` from a read-locked SessionStore; the field needs visibility to that call site.
- Do NOT add a `set_running` or `clear_running` method. The flag is set/cleared exclusively via `RunningAgentGuard` (D-06). Exposing setters invites the "manual set/clear forgot a branch" bug class.
- Do NOT change `GatewaySession::new`'s signature — only add to the body.
- Do NOT change SessionStore.sessions visibility. The new `get_running_flag` accessor is the only API additions; sessions stays as-is.

The `Arc::new(AtomicBool::new(false))` fallback path in `get_running_flag` is correct because: (a) first message of a chat → no agent turn in flight → flag should be false; (b) once `run_agent` runs, it calls `get_or_create` which creates the GatewaySession and the SECOND read of the flag in subsequent dispatch will see the real session's flag; (c) the only risk is a parallel dispatch racing with first-message creation — acceptable because that race ends in flag==false either way (no agent turn yet).
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-gateway 2>&amp;1 | tail -15</automated>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; grep -c "pub running: Arc<AtomicBool>" crates/ironhermes-gateway/src/session.rs</automated>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; grep -c "pub fn get_running_flag" crates/ironhermes-gateway/src/session.rs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `crates/ironhermes-gateway/src/session.rs` contains `pub running: Arc<AtomicBool>` (exactly one occurrence in struct definition)
    - Source: `crates/ironhermes-gateway/src/session.rs` contains `running: Arc::new(AtomicBool::new(false))` inside GatewaySession::new body
    - Source: `crates/ironhermes-gateway/src/session.rs` contains `pub fn get_running_flag(&amp;self, key: &amp;SessionKey) -> Arc<AtomicBool>`
    - Source: `crates/ironhermes-gateway/src/session.rs` contains `use std::sync::atomic::AtomicBool;`
    - Source: NO occurrence of `pub fn set_running` or `pub fn clear_running` in session.rs (RAII-only contract)
    - Behavior: `cargo build -p ironhermes-gateway` exits 0
    - Behavior: `cargo test -p ironhermes-gateway --tests --no-run` exits 0 (test binary still compiles — helpers will need a re-compile but no source changes required from Plan 01 yet)
  </acceptance_criteria>
  <done>
GatewaySession carries the per-session flag, SessionStore exposes the read accessor, and the workspace still builds. Tests remain ignored (Plan 02 Task 3 un-ignores them).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement RunningAgentGuard + is_bypass in handler.rs; wire RAII guard inside run_agent</name>
  <files>crates/ironhermes-gateway/src/handler.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/handler.rs (lines 1-40 — imports + with_rate_limit_retry helper)
    - crates/ironhermes-gateway/src/handler.rs (lines 60-100 — GatewayMessageHandler struct definition)
    - crates/ironhermes-gateway/src/handler.rs (lines 360-450 — handle_slash_command resolve+dispatch flow, the shim at 377-380)
    - crates/ironhermes-gateway/src/handler.rs (lines 780-810 — handle_with_multimodal slash/non-slash split + run_agent header)
    - crates/ironhermes-gateway/src/handler.rs (lines 800-870 — run_agent function body, first ~70 lines so the guard insertion point is exact)
    - crates/ironhermes-gateway/src/handler.rs (lines 1220-1246 — MessageHandler::handle slash/non-slash split)
    - crates/ironhermes-core/src/commands/context.rs (CommandContext::new signature)
    - crates/ironhermes-core/src/commands/registry.rs (ResolveResult variants — confirm Exact/PrefixMatch carry CommandDef with .name field)
    - .planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-RESEARCH.md (Patterns 1, 2, 4; Pitfalls 1-5)
  </read_first>
  <action>
Edit `crates/ironhermes-gateway/src/handler.rs`. Five sub-edits, all in this single file:

A. **Add `RunningAgentGuard` RAII type** (D-06). Place it as a top-level private item near the existing `with_rate_limit_retry` helper at line 34 (so guard types live with their consumers; no new module). Implementation:
   - Tuple struct wrapping `Arc<std::sync::atomic::AtomicBool>`.
   - `fn new(flag: Arc<AtomicBool>) -> Self` calls `flag.store(true, Ordering::SeqCst)` THEN constructs `Self(flag)` — the store-on-construct ordering is mandatory per D-06.
   - `impl Drop` calls `self.0.store(false, Ordering::SeqCst)`.
   - Use `Ordering::SeqCst` for BOTH store sites (Pitfall 3 — Relaxed risks stale reads on weakly-ordered archs).
   - Add a doc comment citing D-06 + the Phase 36 RAII discipline; explicitly note "Drop fires on `?` early return, panic, and future cancellation — covers the 'forgot a cleanup branch' bug class.".

B. **Add `is_bypass(name: &str) -> bool` predicate** (D-01). Place it as a top-level private free function right after `RunningAgentGuard`. Body: `matches!(name, "stop" | "new" | "status" | "queue")`. Add a `// TODO(D-01): add "approve" | "deny" when approval queue lands` comment INSIDE the function body (per Pitfall 7 — leaves a trail for the next person extending the list).

C. **Insert the RAII guard at the top of `run_agent` body** (handler.rs:801). The guard takes the per-session flag, which must be retrieved BEFORE run_agent does its other work. Two construction sub-steps:
   1. As the FIRST statement of the function body (before the existing "Fire MessageReceived hook" block at line 808-820), retrieve the flag:
      `let _running_flag = { let session_key = SessionKey::new(event.platform.clone(), &event.chat_id).with_user(&event.sender_id); self.session_store.read().await.get_running_flag(&session_key) };`
   2. Immediately after retrieval, bind the RAII guard:
      `let _agent_guard = RunningAgentGuard::new(_running_flag);`
   The guard's Drop fires when `run_agent` returns, regardless of exit path (Ok / Err / panic).
   This single placement covers all 5 call sites of `run_agent` (lines 523, 589, 772, 797, 1244 — verify with grep before editing; if count differs from research, adapt but still keep ONE guard in the function body, not 5 at call sites — Pitfall 2).
   Use a leading underscore on `_agent_guard` so the unused-variable lint does not fire; the binding name is the only thing keeping the guard alive across run_agent's body.

D. **Replace the shim in `handle_slash_command`** (handler.rs:377-380). Today:
   ```
   let agent_running = Arc::new(std::sync::atomic::AtomicBool::new(false));
   let ctx = CommandContext::new(platform.clone(), session_key.to_string_key(), agent_running);
   ```
   After this plan, the construction reads the real per-session flag from SessionStore:
   `let agent_running = self.session_store.read().await.get_running_flag(&session_key);`
   `let ctx = CommandContext::new(platform.clone(), session_key.to_string_key(), agent_running.clone());`
   (Clone so the ctx and the upcoming guard check both reference the same Arc<AtomicBool>.)
   Keep the existing `let ctx = if let Some(...)` builder chain that adds toolset/workspace/skill_registry/subagent_registry (lines 381-423) byte-for-byte unchanged — only the FIRST two lines (377-380) of the construction change.
   IMPORTANT: Do NOT delete the old "agent_running always false for gateway slash commands..." comment block here. That cleanup lives in Plan 03 — keeping the diff additive on this range minimizes merge risk. Update the comment text to "Phase 36: per-session running flag wired from SessionStore (D-03/D-05/D-06)." in place; do not delete-and-rewrite as separate operations.

E. **Add guard check between resolve() and dispatch() in `handle_slash_command`** (after handler.rs:441). After the `match self.command_router.resolve(...)` arm matches `Exact(def) | PrefixMatch(def)`, BEFORE the existing dispatch path (currently at line 442 onwards), insert:
   - If `agent_running.load(Ordering::SeqCst)` AND `!is_bypass(&def.name)`:
     - Call `with_rate_limit_retry(|| adapter.send_message(&event.chat_id, "Agent is running. Use /stop to interrupt or /queue to send after this turn.", None)).await?;`
     - Return `Ok(())`.
   - Else: fall through to the existing dispatch logic untouched.
   The check uses `def.name` (post-resolution canonical name, NOT raw user input) so `/reset` correctly resolves to `"new"` and bypasses (Pitfall 4).
   Place this BEFORE the Phase 21.8.3.1 D-05 personality interception comment block (line ~442-444) — guard must fire first; the personality interception is a downstream dispatch-time concern.

F. **Add guard check in `MessageHandler::handle` non-slash arm** (handler.rs:1228-1244, Pitfall 1) AND in `handle_with_multimodal` non-slash arm (handler.rs:792-797). For each of these two non-slash branches:
   - Before the `self.run_agent(event, adapter, cancel, ...).await` call:
   - Retrieve the flag: `let session_key = SessionKey::new(event.platform.clone(), &event.chat_id).with_user(&event.sender_id); let agent_running = self.session_store.read().await.get_running_flag(&session_key);`
   - If `agent_running.load(Ordering::SeqCst)`:
     - `with_rate_limit_retry(|| adapter.send_message(&event.chat_id, "Agent is running. Use /stop to interrupt or /queue to send after this turn.", None)).await?;`
     - Return `Ok(())`.
   - Else: fall through to the existing `self.run_agent(...).await` call untouched.
   The D-02 string MUST be byte-for-byte identical at all three rejection sites (one in handle_slash_command, one in MessageHandler::handle, one in handle_with_multimodal). Suggested: define a private `const AGENT_RUNNING_REJECT_MSG: &str = "..."` at the top of handler.rs near `RunningAgentGuard` so all three sites reference one source of truth. Plan 01's `d02_error_message()` helper is the canonical comparison point for tests.

Critical constraints:
- The D-02 string is locked verbatim — do NOT rephrase, do NOT abbreviate, do NOT add a trailing newline. The exact characters are: `Agent is running. Use /stop to interrupt or /queue to send after this turn.` (no leading/trailing whitespace, single ASCII period at end).
- `Ordering::SeqCst` everywhere (both stores and loads). Pitfall 3.
- The guard check uses `def.name`, never the raw user input. Pitfall 4.
- Only ONE `RunningAgentGuard::new` call site per `run_agent` body. Pitfall 2.
- Do NOT introduce a global `AtomicBool`. Per-session only. Pitfall (codex HIGH-2).
- Do NOT add `running: Arc<AtomicBool>` parameter to `run_agent`'s signature — the function fetches it itself via `self.session_store` lookup at the top. Rationale: keeps the 5 call sites unchanged, no signature ripple.
- The `let session_key = SessionKey::new(...).with_user(...)` retrieval in `run_agent` mirrors the construction at handler.rs:374-375 byte-for-byte. Verify match before committing.
- Do NOT modify the comment block at handler.rs:1032 ("gateway has no per-turn cancel today") — out of scope per D-02.
- Do NOT touch AgentRuntime — D-05 lock.

After all 5 edits, do NOT yet un-ignore tests. Task 3 owns the test flip.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-gateway 2>&amp;1 | tail -15</automated>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; grep -c "struct RunningAgentGuard" crates/ironhermes-gateway/src/handler.rs</automated>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; grep -c "impl Drop for RunningAgentGuard" crates/ironhermes-gateway/src/handler.rs</automated>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; grep -c "fn is_bypass" crates/ironhermes-gateway/src/handler.rs</automated>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; grep -c "Agent is running. Use /stop to interrupt or /queue to send after this turn." crates/ironhermes-gateway/src/handler.rs</automated>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; grep -v '^\s*//' crates/ironhermes-gateway/src/handler.rs | grep -c "Arc::new(std::sync::atomic::AtomicBool::new(false))"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `crates/ironhermes-gateway/src/handler.rs` contains `struct RunningAgentGuard` (exactly 1 occurrence)
    - Source: `crates/ironhermes-gateway/src/handler.rs` contains `impl Drop for RunningAgentGuard` (exactly 1 occurrence)
    - Source: `crates/ironhermes-gateway/src/handler.rs` contains `fn is_bypass`
    - Source: `crates/ironhermes-gateway/src/handler.rs` contains the bypass match arm `matches!(name, "stop" | "new" | "status" | "queue")` (D-01 verbatim)
    - Source: `crates/ironhermes-gateway/src/handler.rs` contains `"Agent is running. Use /stop to interrupt or /queue to send after this turn."` (at least 1 occurrence; ideally a single const referenced from 3 sites)
    - Source: `crates/ironhermes-gateway/src/handler.rs` does NOT contain `Arc::new(std::sync::atomic::AtomicBool::new(false))` outside of comments (the shim is replaced — `grep -v '^\s*//' | grep -c` returns 0)
    - Source: `crates/ironhermes-gateway/src/handler.rs` contains `RunningAgentGuard::new(` exactly 1 occurrence in production code (single guard site inside run_agent body)
    - Source: `crates/ironhermes-gateway/src/handler.rs` contains `Ordering::SeqCst` at the load sites (at least 2: handle_slash_command guard check + non-slash guard check) and at the store sites inside RunningAgentGuard (2: new + Drop)
    - Source: `crates/ironhermes-gateway/src/handler.rs` contains `get_running_flag` calls at exactly 3 sites: top of run_agent, top of handle_slash_command (replacing shim), and the non-slash MessageHandler::handle / handle_with_multimodal sites (so at least 3 `get_running_flag` references — could be 3 if handle_with_multimodal shares with MessageHandler::handle via factored helper, or 4 if each site fetches independently)
    - Source: `crates/ironhermes-gateway/src/handler.rs` does NOT contain `pub fn run_agent` with `running_flag: Arc<AtomicBool>` parameter (signature unchanged)
    - Behavior: `cargo build -p ironhermes-gateway` exits 0
    - Behavior: `cargo build --workspace` exits 0 (no downstream breakage)
  </acceptance_criteria>
  <done>
RunningAgentGuard, is_bypass, and all three guard sites (slash dispatch, non-slash MessageHandler::handle, non-slash handle_with_multimodal) are wired. Production code compiles. Tests still ignored awaiting Task 3.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Un-ignore all 11 tests; implement bodies; verify entire suite green</name>
  <files>crates/ironhermes-gateway/tests/running_agent_guard_tests.rs</files>
  <read_first>
    - crates/ironhermes-gateway/tests/running_agent_guard_tests.rs (full file from Plan 01 — all 11 stubs + helpers)
    - crates/ironhermes-gateway/src/handler.rs (post-Task-2 — confirm RunningAgentGuard, is_bypass, AGENT_RUNNING_REJECT_MSG const if defined, guard sites)
    - crates/ironhermes-gateway/src/session.rs (post-Task-1 — confirm get_running_flag signature, GatewaySession.running field)
    - crates/ironhermes-core/src/commands/handlers.rs (cmd_stop body around line 127 — confirms it reads ctx.agent_running.load(SeqCst); test_stop_reads_real_flag asserts this on gateway)
    - .planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-VALIDATION.md (test name list — verify no rename happened in Plan 01)
  </read_first>
  <action>
Edit `crates/ironhermes-gateway/tests/running_agent_guard_tests.rs`. Three sub-edits:

A. **Remove the `#[ignore = "..."]` attribute from all 11 tests.** They become live `#[tokio::test]` functions. Do NOT remove the `#[tokio::test]` annotation itself.

B. **Replace each `todo!(...)` body with a real test implementation.** Each test follows the same template:
   1. Build a SessionStore + handler via helpers from Plan 01.
   2. Force-create the relevant session(s) via `session_store.write().await.get_or_create(...)`. Without this, `get_running_flag` returns a fresh false-Arc and tests cannot observe state transitions.
   3. For tests that need the flag set true (i.e., simulate a turn in flight): construct a `RunningAgentGuard` via the test-only path. Two options for the executor:
      a. Expose `RunningAgentGuard` as `pub(crate)` and have the test import it. (Preferred — keeps the RAII discipline visible in tests.)
      b. If exposing the guard is undesirable, add a `pub(crate) fn force_set_running(&mut self, key: &SessionKey, value: bool)` helper on SessionStore gated by `#[cfg(test)]` or `pub(crate)` visibility. (Acceptable fallback.)
      The executor picks. The test does NOT call `flag.store(true)` directly on a clone — tests must use the same primitive as production to avoid divergence.
   4. Dispatch a synthetic `MessageEvent` through `MessageHandler::handle` (the public trait method). Use `Platform::Telegram` as platform, chat_ids "s1"/"s2" for isolation tests.
   5. Assert against `RecordingPlatformAdapter::messages()` — for rejection tests, the captured message log MUST contain exactly the D-02 verbatim string from `helpers::d02_error_message()`. For bypass tests, the log MUST NOT contain the D-02 string (it dispatches normally — exact dispatch behavior may vary; assert via absence-of-rejection).

   Per-test specifics:

   - **test_session_isolation**: create two sessions (s1, s2). Set s1.running=true. Dispatch `/model claude` to s2 → MUST succeed (no D-02 message). Dispatch `/model claude` to s1 → MUST receive D-02. Proves per-session keying (codex HIGH-2 closed).
   - **test_model_rejected_when_running**: single session, set running=true, dispatch `/model gpt-4` → assert D-02 in log. Closes D-04.
   - **test_stop_bypasses_guard**: session running=true, dispatch `/stop` → MUST NOT receive D-02 (some `/stop`-handler response is fine; check absence of the reject string).
   - **test_new_bypasses_guard**: same shape with `/new`.
   - **test_status_bypasses_guard**: same shape with `/status`.
   - **test_queue_bypasses_guard**: same shape with `/queue`.
   - **test_guard_clears_on_success**: drop a `RunningAgentGuard` (call it in an inner scope, then exit). Verify the flag reads false after scope exit via `session.running.load(SeqCst)`.
   - **test_guard_clears_on_error**: same as above but the inner scope returns an `Err` via `?`. Verify flag still false. (Use a helper closure that returns `Result<(), Error>` and is `?`-chained inside a block to exercise the early-return path through Drop.)
   - **test_alias_bypasses_guard**: session running=true, dispatch `/reset` → CommandRouter resolves to canonical name "new" → bypass list includes "new" → MUST NOT receive D-02. Proves Pitfall 4 mitigation. (If "reset" alias for "new" is not registered in the command registry in the current codebase, substitute a known alias that resolves to a bypass-list name; verify via `command_router.resolve("/reset", &Platform::Telegram)` returning Exact(def) with def.name == "new" before relying on this test path.)
   - **test_freetext_rejected_when_running**: session running=true, dispatch a plain (non-slash) message "hello world" → MUST receive D-02 (Pitfall 1 mitigation — non-slash path guard).
   - **test_stop_reads_real_flag**: integration-style. Create session, set running=true. Dispatch `/stop`. The `cmd_stop` handler in `ironhermes-core/src/commands/handlers.rs:127` reads `ctx.agent_running.load(SeqCst)` — verify the response indicates the flag was true (e.g., the reply text differs from the "no agent running" branch). The exact assertion mode: capture the response sent via RecordingPlatformAdapter and assert it is NOT byte-for-byte equal to the "no agent running" message that the handler emits when the flag is false. (Read handlers.rs:127 for the two branches before writing the assertion.)

C. **Run the full suite and confirm 11 passing.**

Critical constraints:
- Tests MUST exercise the same RunningAgentGuard primitive as production — no parallel `flag.store(true)` in tests that bypasses the guard's discipline.
- The D-02 string comparison MUST go through `helpers::d02_error_message()` (single source of truth). If a test fails because production string differs, fix production — do NOT change the helper.
- Do not introduce `sleep` waits; the AtomicBool transitions are synchronous, no async scheduling issues to wait through.
- If a test cannot be made deterministic (e.g., RecordingPlatformAdapter has a race), prefer adding a tokio sync primitive (oneshot or Notify) inside the adapter rather than a sleep.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p ironhermes-gateway --test running_agent_guard_tests 2>&amp;1 | tail -20</automated>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; grep -c "#\[ignore" crates/ironhermes-gateway/tests/running_agent_guard_tests.rs</automated>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; grep -c "todo!" crates/ironhermes-gateway/tests/running_agent_guard_tests.rs</automated>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p ironhermes-gateway 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source: `crates/ironhermes-gateway/tests/running_agent_guard_tests.rs` contains 0 occurrences of `#[ignore` (all 11 tests un-ignored)
    - Source: `crates/ironhermes-gateway/tests/running_agent_guard_tests.rs` contains 0 occurrences of `todo!(` (all stubs replaced)
    - Source: every test name from the canonical 11-name list (Plan 01 frozen list) still appears verbatim — no rename
    - Behavior: `cargo test -p ironhermes-gateway --test running_agent_guard_tests` exits 0 with `running 11 tests ... test result: ok. 11 passed; 0 failed`
    - Behavior: `cargo test -p ironhermes-gateway` (full gateway suite) exits 0 — no regression in any other test
    - Behavior: `cargo test --workspace` exits 0 (cross-crate regression check — handler.rs is consumed by ironhermes-cli through trait paths; new behavior must not break CLI/TUI/web tests)
  </acceptance_criteria>
  <done>
All 11 GW-05 sub-behavior tests pass against the Plan 02 Task 1+2 implementation. Workspace builds clean. Phase 36's behavior contract is satisfied; Plan 03 finalizes docs/STATE updates and captures backlog items.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → handle_slash_command | Untrusted user-supplied command text crosses into the dispatch boundary; canonical-name resolution is the input-validation point |
| User input → MessageHandler::handle (non-slash) | Free-text user input that bypasses slash-command interception by omitting "/"; second guard surface (Pitfall 1) |
| Session A ↔ Session B | Cross-session state isolation; per-session `Arc<AtomicBool>` keyed by SessionKey |
| run_agent → drop → flag-clear | RAII Drop is the trust anchor for "flag eventually clears" — `?`/panic/cancellation all funnel here |

## STRIDE Threat Register

(Ported from 36-RESEARCH.md Security Domain table; ASVS L1; block on HIGH.)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.1-05 | Tampering | Per-session state isolation | mitigate | `pub running: Arc<AtomicBool>` lives on GatewaySession (per-session). SessionStore keys by SessionKey { platform, chat_id, user_id }. `test_session_isolation` proves no cross-session bleed. |
| T-21.1-08 | Elevation of Privilege | Non-slash bypass via free-text | mitigate | Pitfall 1 mitigation: `MessageHandler::handle` and `handle_with_multimodal` non-slash arms both retrieve the running flag and reject with D-02 before reaching `run_agent`. `test_freetext_rejected_when_running` enforces. |
| T-21.1-09 | Tampering | Model swap mid-turn TOCTOU (codex HIGH-2) | mitigate | D-04: `/model` is NOT on the bypass list; guard check in `handle_slash_command` rejects with D-02 string. No credential swap window during in-flight LLM call. `test_model_rejected_when_running` enforces. |
| T-36-01 | Elevation of Privilege | Bypass via alias resolution | mitigate | Guard checks `def.name` (canonical post-resolution name), never raw user input. `/reset` → `new` correctly bypasses; `/m` → `model` correctly rejects. `test_alias_bypasses_guard` enforces. Pitfall 4 mitigation. |
| T-36-02 | Tampering | Stale-flag memory ordering (weakly-ordered archs) | mitigate | `Ordering::SeqCst` on BOTH store and load sites. Pitfall 3 mitigation. (Performance impact negligible at per-message rates.) |
| T-36-03 | Denial of Service | Flag stuck true after run_agent error | mitigate | `RunningAgentGuard` Drop fires on `?` early return; D-06 RAII discipline. `test_guard_clears_on_error` enforces. Pitfall 5 mitigation. |
| T-36-SC | Tampering | Package supply chain | accept | No new packages introduced this phase. All types (`AtomicBool`, `Arc`, `Ordering`) are std. |
</threat_model>

<verification>
After all three tasks complete:
1. `cargo test -p ironhermes-gateway --test running_agent_guard_tests` → 11 passed / 0 failed / 0 ignored
2. `cargo test -p ironhermes-gateway` → full gateway suite green
3. `cargo test --workspace` → no cross-crate regressions
4. Source grep `Arc::new(std::sync::atomic::AtomicBool::new(false))` filtered through `grep -v '^\s*//'` → 0 hits in handler.rs (the shim is gone from production code paths)
5. Source grep `"Agent is running. Use /stop to interrupt or /queue to send after this turn."` → ≥ 2 hits (production source + test helper); ideally production uses a const so the grep hits exactly the const definition + test helper
6. The Drop impl on RunningAgentGuard is present (source grep)
7. is_bypass body matches D-01 verbatim
8. `cargo clippy -p ironhermes-gateway -- -D warnings` exits 0 (no new clippy regressions introduced)
</verification>

<success_criteria>
- GatewaySession carries `pub running: Arc<AtomicBool>` initialized false
- SessionStore::get_running_flag returns the per-session handle (or a fresh false-Arc on miss)
- RunningAgentGuard RAII type with SeqCst store-on-new / store-on-drop is present in handler.rs
- is_bypass predicate matches D-01 canonical names verbatim
- handle_slash_command's shim (Arc::new(AtomicBool::new(false))) is replaced with the per-session flag retrieval
- Guard check fires AFTER resolve(), BEFORE dispatch(), on `def.name` (post-alias)
- Non-slash path in both MessageHandler::handle AND handle_with_multimodal carries the same guard check
- D-02 string is byte-for-byte verbatim at all rejection sites
- All 11 GW-05 sub-behavior tests pass
- No production code modified outside the two named files (session.rs, handler.rs)
- No new external dependencies
- No changes to AgentRuntime (D-05 honored)
</success_criteria>

<output>
Create `.planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-02-SUMMARY.md` when done, documenting:
- Exact line numbers where RunningAgentGuard, is_bypass, AGENT_RUNNING_REJECT_MSG const (if defined), and each of the 3+ guard-check sites live in handler.rs (post-edit)
- Whether RunningAgentGuard was exposed as `pub(crate)` for tests or whether a `force_set_running` test helper was added instead
- Confirmation that the 5 run_agent call sites at lines 523, 589, 772, 797, 1244 are still present and reach the in-function guard correctly (or list the actual line numbers if they shifted)
- Pass/fail count from `cargo test -p ironhermes-gateway running_agent_guard`
- Any deviation from this plan (with rationale)
</output>
</content>
</invoke>