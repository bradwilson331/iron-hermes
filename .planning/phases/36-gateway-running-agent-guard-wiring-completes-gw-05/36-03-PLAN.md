---
phase: 36-gateway-running-agent-guard-wiring-completes-gw-05
plan: 03
type: execute
wave: 3
depends_on:
  - 36-02
files_modified:
  - crates/ironhermes-gateway/src/handler.rs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-BACKLOG.md
autonomous: false
requirements:
  - GW-05
tags:
  - cleanup
  - documentation
  - backlog
  - uat

must_haves:
  truths:
    - "The stale comment block at handler.rs:377-380 referencing 'running-agent guard is a future enhancement' is deleted or rewritten to point at the now-shipped mechanism"
    - "REQUIREMENTS.md GW-05 status flipped from 'Partial — guard pending' to 'Complete' (or equivalent)"
    - "ROADMAP.md Phase 36 entry checkboxes reflect plan completion"
    - "36-BACKLOG.md captures the three locked out-of-scope items (web UI slash interception, per-turn LLM cancel, CLI/gateway unified mechanism) with traceable REQ-IDs or phase pointers"
    - "Human UAT checkpoint records observed Telegram behavior matches D-02 / D-04 contract"
  artifacts:
    - path: ".planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-BACKLOG.md"
      provides: "Phase-level backlog of intentionally deferred follow-up work"
      contains:
        - "web UI"
        - "per-turn LLM cancel"
        - "handler.rs:1032"
        - "CLI parity unification"
    - path: ".planning/REQUIREMENTS.md"
      provides: "GW-05 traceability row updated to Complete"
      contains: "Phase 36"
  key_links:
    - from: ".planning/REQUIREMENTS.md GW-05 row"
      to: ".planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/"
      via: "phase pointer 'Phase 21.1 (dispatch only) + Phase 36 (guard) — Complete'"
      pattern: "Phase 36"
    - from: ".planning/ROADMAP.md Phase 36 section"
      to: "Plans completed"
      via: "checkbox flips on plan list"
      pattern: "\\[x\\] 36-(01|02|03)-PLAN.md"

notes:
  - "Human UAT checkpoint is gated on a working Telegram bot. If the executor cannot easily run a live UAT, the checkpoint reduces to a structured user-action request (manual verification noted) — see checkpoint:human-action task."
  - "BACKLOG.md is a phase-local document; it does not require modifying the global PROJECT.md / ROADMAP.md beyond the GW-05 status flip."
---

<objective>
Close Phase 36: remove the stale comment that Plan 02 intentionally left intact,
update REQUIREMENTS.md + ROADMAP.md to reflect GW-05 completion, capture the
three intentionally-deferred follow-up items in a phase-local backlog file, and
run the Real-Telegram UAT checkpoints from 36-VALIDATION.md §Manual-Only.

Purpose: Make the GW-05 close visible at the project level (so /gsd:verify-work
sees the right status) and ensure the backlog of deferred items is traceable
(so they are not silently forgotten).

Output: A clean handler.rs with no stale shim language, a REQUIREMENTS.md row
that traces GW-05 to Phase 36, a ROADMAP.md with Phase 36 plans marked
complete, and a 36-BACKLOG.md capturing deferred work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-CONTEXT.md
@.planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-VALIDATION.md
@.planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-02-SUMMARY.md
@crates/ironhermes-gateway/src/handler.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete stale comment at handler.rs:377-380; verify production source clean</name>
  <files>crates/ironhermes-gateway/src/handler.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/handler.rs (lines 370-410 — confirm post-Plan-02 state of the construction block; verify Plan 02 already rewrote the comment text or only the code)
    - .planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-02-SUMMARY.md (Plan 02 may have already done the comment swap — if so, this task is a no-op confirmation; if not, this task lands it)
  </read_first>
  <action>
Edit `crates/ironhermes-gateway/src/handler.rs`:

1. Verify the state of the construction block at lines ~377-380 (post-Plan-02 line numbers may have shifted slightly). The block should now construct `agent_running` from `self.session_store.read().await.get_running_flag(&session_key)`.

2. If Plan 02 left the OLD comment language in place ("agent_running always false for gateway slash commands — the running-agent guard is a future enhancement using per-session state"), DELETE the comment entirely and replace with:
   `// Phase 36 / GW-05: per-session running flag retrieved from SessionStore (D-03/D-05/D-06).`
   `// Construction here is the single source of truth — handle_with_multimodal and`
   `// MessageHandler::handle non-slash arms also call get_running_flag for their own guard check.`

3. If Plan 02 already updated the comment, verify the phrasing matches the GW-05 closed state (no "future enhancement" / "TODO" language). Adjust if stale phrasing remains.

4. Final pass: `grep -i "future enhancement\|TODO.*agent.running\|running-agent guard is a future" crates/ironhermes-gateway/src/handler.rs` — must return 0 hits.

Critical constraints:
- Production code (RunningAgentGuard, is_bypass, guard-check call sites) must not change behavior — this is purely a comment update.
- Run `cargo build -p ironhermes-gateway` after the edit to confirm no incidental token break.
- Run `cargo test -p ironhermes-gateway running_agent_guard` to confirm all 11 tests still pass.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; grep -ic "future enhancement\|running-agent guard is a future" crates/ironhermes-gateway/src/handler.rs</automated>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-gateway 2>&amp;1 | tail -5</automated>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p ironhermes-gateway --test running_agent_guard_tests 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -ic "future enhancement\|running-agent guard is a future" crates/ironhermes-gateway/src/handler.rs` returns 0
    - Source: handler.rs contains `Phase 36` and `GW-05` and `D-03/D-05/D-06` near the construction block at the post-edit line of `agent_running = self.session_store.read().await.get_running_flag(&session_key)`
    - Behavior: `cargo build -p ironhermes-gateway` exits 0
    - Behavior: `cargo test -p ironhermes-gateway --test running_agent_guard_tests` reports 11 passed / 0 failed
    - Behavior: `cargo clippy -p ironhermes-gateway -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
Stale comment language eliminated. Production source reflects shipped state. Tests still pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update REQUIREMENTS.md (GW-05 status), ROADMAP.md (Phase 36 checkboxes), create 36-BACKLOG.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-BACKLOG.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (find the GW-05 active-list row around line 147 — current text "partially shipped in Phase 21.1..."; and the traceability table row around line 358 "Phase 21.1 (dispatch only) + follow-up (guard) | Partial — guard pending")
    - .planning/ROADMAP.md (Phase 36 section around line 81-89 — current "Plans: 0 plans" and "TBD" entry)
    - .planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-CONTEXT.md (Deferred Ideas section — source of the three backlog items)
    - .planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-RESEARCH.md (Cross-Interface Wiring Survey — confirms web UI gap is real and out of scope)
  </read_first>
  <action>
Three independent edits, all in this task:

A. **Update `.planning/REQUIREMENTS.md`:**

   1. In the GW-05 active-list row (around line 147), update from:
      `[ ] **GW-05**: Gateway slash command dispatch via resolve_command() with running-agent guard (...) — *partially shipped in Phase 21.1...*`
      to:
      `[x] **GW-05**: Gateway slash command dispatch via resolve_command() with running-agent guard (blocks /model while agent active, bypasses /stop /approve /deny) — *Phase 21.1 shipped dispatch; Phase 36 completes the per-session guard wiring (codex HIGH-1/HIGH-2 closed). Note: /approve /deny remain off the bypass list pending approval-queue implementation per D-01.*`

   2. In the traceability table (around line 358), update from:
      `| GW-05 | Phase 21.1 (dispatch only) + follow-up (guard) | Partial — guard pending |`
      to:
      `| GW-05 | Phase 21.1 (dispatch) + Phase 36 (guard) | Complete |`

   3. Update the footer date stamp on REQUIREMENTS.md to today (2026-05-24) with a one-line entry mentioning the GW-05 close.

B. **Update `.planning/ROADMAP.md` Phase 36 section** (around lines 81-89):

   1. Change `**Plans:** 0 plans` to `**Plans:** 3 plans`
   2. Replace the placeholder `- [ ] TBD (run /gsd-plan-phase 36 to break down)` with the actual plan list:
      ```
      Plans:
      - [x] 36-01-PLAN.md — Wave 0 test scaffold: running_agent_guard_tests.rs with 11 #[ignore] stubs + helpers
      - [x] 36-02-PLAN.md — Core implementation: GatewaySession.running field, RunningAgentGuard RAII, is_bypass, three guard-check sites, un-ignore tests
      - [x] 36-03-PLAN.md — Cleanup: stale comment removal, REQUIREMENTS.md + ROADMAP.md updates, deferred-work backlog, UAT
      ```
      (Use `[x]` only on plans actually merged at time of writing — Plan 03 itself is mid-execution so leave that one as `[ ]` until /gsd:execute-phase flips it.)
   3. Do NOT change the Phase 36 goal text — it was authored explicitly and remains the canonical phase description.

C. **Create `.planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-BACKLOG.md`** (new file) with this content:

   ```
   # Phase 36 — Deferred Work Backlog

   The following items surfaced during Phase 36 research/planning but were
   intentionally scoped OUT of this phase. Each is tracked here for the next
   roadmap planning pass so they are not silently forgotten.

   ## 1. Web UI never intercepts slash commands

   Source: 36-RESEARCH.md "Cross-Interface Wiring Survey"
   Evidence: crates/iron_hermes_ui/src/server/state.rs:208 — run_web_turn() goes
   directly to runtime.run_turn() without slash-command interception. The
   AppState.command_router is held but only used by the list_slash_commands()
   REST endpoint, never as part of message routing.
   Impact: Slash commands typed in the web UI fall through to the agent as
   plain text instead of being dispatched to handlers. /stop, /model, /new,
   /memory, etc. silently no-op or get echoed.
   Effort estimate: medium — needs interception logic in run_web_turn() +
   CommandContext construction symmetric to handle_slash_command.
   Status: candidate for a future Phase 36.x or a dedicated web-UI parity
   phase. NOT in current ROADMAP.

   ## 2. Per-turn LLM cancellation still missing on gateway

   Source: 36-RESEARCH.md, locked at handler.rs:1032 ("gateway has no per-turn
   cancel today")
   Impact: /stop on gateway today only kills subagent processes via
   process_registry; it does not interrupt an in-flight LLM call on the
   parent turn. The user can wait, but Ctrl-C-equivalent semantics are not
   available.
   Effort estimate: high — requires runtime support for cancelable LLM
   requests across all providers + per-session cancellation token plumbing.
   Status: separate phase. Blocks any future move from D-03's two-state
   AtomicBool to the richer enum AgentState { Idle, Running, Cancelling,
   Queued } that 36-CONTEXT.md Deferred Ideas references.

   ## 3. CLI / TUI / gateway running-agent state model not unified

   Source: 36-RESEARCH.md "Cross-Interface Wiring Survey"
   Evidence: CLI uses a process-local agent_running flag set at
   main.rs:1707/2024; TUI derives from app.pending_rx.is_some() at
   tui_rata/commands.rs:537; gateway uses per-session
   GatewaySession.running (this phase). Three different mechanisms.
   Impact: Future maintenance hazard. A behavior change to the guard
   semantics has to land in three places.
   Effort estimate: medium — extract a shared RunningAgentRegistry trait
   in ironhermes-core that all three interfaces implement. Mirrors the
   MemoryManagerHandle / McpReloader trait patterns from Phase 20 / 21.2.
   Status: cleanup phase candidate.

   ## 4. /approve and /deny bypass list addition (deferred per D-01)

   Source: 36-CONTEXT.md D-01
   Trigger: when the real approval queue is implemented (separate phase),
   add "approve" and "deny" to the is_bypass() match arm in
   crates/ironhermes-gateway/src/handler.rs and update test coverage to
   include test_approve_bypasses_guard / test_deny_bypasses_guard.
   Tracker: TODO comment is already in the is_bypass() body per Plan 02
   Task 2 spec.

   ---

   Phase 36 closed: 2026-05-24 (planning) — GW-05 status flipped in
   REQUIREMENTS.md upon Plan 03 merge.
   ```

Critical constraints:
- Do NOT mark Plan 03 itself as `[x]` until it actually completes — execute-phase / verify-work hands flip the checkbox.
- Do NOT alter unrelated rows in REQUIREMENTS.md or ROADMAP.md.
- BACKLOG.md is a phase-local artifact; it does not replace future REQUIREMENTS.md row creation when these items get scheduled.
- Verify the GW-05 row in the active list (around line 147) AND the traceability table (around line 358) are BOTH updated — they are two separate places.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; grep -c "\[x\] \*\*GW-05\*\*" .planning/REQUIREMENTS.md</automated>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; grep -c "Phase 21.1 (dispatch) + Phase 36 (guard) | Complete" .planning/REQUIREMENTS.md</automated>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; grep -c "36-01-PLAN.md\|36-02-PLAN.md\|36-03-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; ls -la .planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-BACKLOG.md</automated>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; grep -c "web UI\|per-turn LLM cancel\|CLI parity unification\|approve\|deny" .planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-BACKLOG.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `.planning/REQUIREMENTS.md` line containing `GW-05` in active list is checked `[x]`
    - Source: `.planning/REQUIREMENTS.md` traceability table row for GW-05 reads `Phase 21.1 (dispatch) + Phase 36 (guard) | Complete`
    - Source: `.planning/REQUIREMENTS.md` footer date updated to 2026-05-24 with GW-05 close note
    - Source: `.planning/ROADMAP.md` Phase 36 section reads `**Plans:** 3 plans`
    - Source: `.planning/ROADMAP.md` Phase 36 plan list contains all three plan filenames (36-01-PLAN.md, 36-02-PLAN.md, 36-03-PLAN.md)
    - Source: `.planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-BACKLOG.md` exists
    - Source: 36-BACKLOG.md contains the strings "web UI", "per-turn LLM cancel", "handler.rs:1032", "CLI", "approve", "deny"
    - Behavior: no Rust/Cargo verification needed — these are .md edits only
  </acceptance_criteria>
  <done>
Project-level state files reflect GW-05 complete; deferred items are durably captured.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT — Real-Telegram verification of D-02 and D-04 behavior</name>
  <what-built>
Plan 02 wired the running-agent guard end-to-end on the gateway. Plan 03 Task 1
cleaned the comment. Plan 03 Task 2 updated state files. This checkpoint
exercises the guard against a live Telegram bot to confirm the contract holds
in production, not just in unit tests.
  </what-built>
  <how-to-verify>
**Prerequisites:**
1. A configured Telegram bot reachable from the gateway (set TELEGRAM_BOT_TOKEN in your .env; configure an allowed chat).
2. The gateway is built and runnable: `cargo build -p ironhermes-gateway -p ironhermes-cli` succeeds.
3. Start the gateway: `cargo run -p ironhermes-cli -- gateway run` (or your normal start command).

**Verification Steps:**

A. **D-02 + D-04 rejection (codex HIGH-2 close):**
   1. From your allowed Telegram chat, send a prompt that triggers a long agent turn — e.g., a deep web-search workflow:
      `Please research the history of the Rust async ecosystem and write a 5-paragraph summary.`
   2. While the agent is mid-turn (the "..." indicator is active, you can see streaming output, but the turn has not finished):
      - Send `/model anthropic:claude-opus-4-7`
   3. **Expected:** The bot replies with EXACTLY:
      `Agent is running. Use /stop to interrupt or /queue to send after this turn.`
   4. **Expected:** The original agent turn continues uninterrupted and eventually delivers its response normally.

B. **Bypass — `/stop` works mid-turn (D-01):**
   1. Send another prompt that triggers a subagent delegation (e.g., `Use delegate_task to spawn a child agent that lists the files in this directory.`).
   2. While the subagent is running, send `/stop`.
   3. **Expected:** The bot replies with a "Stopped N background process(es)." style message (NOT the D-02 rejection). The exact response is whatever `cmd_stop` currently emits on success.

C. **Bypass — free-text rejected (Pitfall 1):**
   1. Send another long-running prompt.
   2. While the agent is mid-turn, send a plain-text message (no slash) like `hello`.
   3. **Expected:** The bot replies with the D-02 rejection message. The original turn continues.

D. **Per-session isolation (codex HIGH-2 close):**
   - This requires two distinct chats with the gateway (e.g., two different users, or two different group chats).
   - In Chat A, trigger a long agent turn.
   - In Chat B (different chat_id), immediately send `/model anthropic:claude-opus-4-7`.
   - **Expected:** Chat B's `/model` command dispatches normally (no D-02 rejection) because Chat B's session is Idle. Chat A's turn continues.

If any step deviates from expected behavior, capture:
- The exact bot reply text
- The relevant gateway log lines (RUST_LOG=ironhermes_gateway=debug)
- The session_key for the affected chat (visible in logs)
Report the deviation to the user before approving.
  </how-to-verify>
  <resume-signal>Type "approved" if all four UAT steps match expected behavior; otherwise describe the observed deviation (text + logs).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live Telegram → gateway | Real user input crosses into the dispatch boundary; UAT validates the production guard fires |
| Documentation accuracy | REQUIREMENTS.md / ROADMAP.md drive downstream consumers (`/gsd:verify-work`, future planning); inaccurate status flips risk silent regressions |

## STRIDE Threat Register

(This plan adds no new attack surface; threats covered by Plan 02. UAT validates Plan 02's mitigations in production.)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.1-05 | Tampering | Live per-session isolation | mitigate | UAT step D exercises two-chat isolation against a live bot |
| T-21.1-09 | Tampering | Live TOCTOU on /model mid-turn | mitigate | UAT step A exercises the D-04 rejection against a live bot |
| T-36-DOC | Tampering | Status-flip accuracy | accept | REQUIREMENTS/ROADMAP grep-verifiable in acceptance criteria; deviation visible in PR diff |
</threat_model>

<verification>
After all three tasks complete:
1. Source: `grep -ic "future enhancement\|running-agent guard is a future" crates/ironhermes-gateway/src/handler.rs` → 0
2. Source: `grep -c "\[x\] \*\*GW-05\*\*" .planning/REQUIREMENTS.md` → 1
3. Source: ROADMAP.md Phase 36 lists 3 plans (not "TBD")
4. Source: 36-BACKLOG.md exists with 4 backlog sections
5. Behavior: Telegram UAT all 4 steps observed as expected (human attestation)
6. Behavior: `cargo test -p ironhermes-gateway` still green
</verification>

<success_criteria>
- Stale "future enhancement" comment removed from handler.rs
- REQUIREMENTS.md GW-05 row marked complete + traceability row updated to Phase 21.1 + Phase 36
- ROADMAP.md Phase 36 section lists 3 plans
- 36-BACKLOG.md created with the four deferred items (web UI, per-turn cancel, CLI unification, /approve|/deny bypass)
- Human UAT confirms D-01 (bypass), D-02 (reject + exact string), D-04 (/model reject), and per-session isolation against a live Telegram bot
- Gateway tests still green
</success_criteria>

<output>
Create `.planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-03-SUMMARY.md` when done, documenting:
- Confirmation of stale-comment removal (with the new comment text)
- The exact diff applied to REQUIREMENTS.md (the two updated rows verbatim)
- The exact diff applied to ROADMAP.md (Phase 36 section before/after)
- BACKLOG.md contents acknowledged
- UAT result: PASS / FAIL with any observed deviations and follow-up actions
</output>
</content>
</invoke>