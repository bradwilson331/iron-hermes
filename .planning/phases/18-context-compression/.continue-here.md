---
context: phase
phase: 18-context-compression
task: live-uat-rerun
total_tasks: 9
status: awaiting_live_uat_rerun
last_updated: 2026-04-13T10:42:25.645Z
---

<current_state>
Phase 18 plan 18-10 (root-cause fix for `apply_adaptive_shift` orphaned-tool-pair defect) is fully implemented and code-verified: 164/164 tests pass, clippy clean, 7 commits on `develop` (42eb073 → e20a7c2). Includes a post-ship front-straddle fix after the first live UAT re-pass (2026-04-13T05:18) revealed the original single-direction guard collapsed `prune_end=2 <= prune_start=3` when a pair's assistant message sat inside `protect_first_n`.

**Paused before:** second live UAT re-pass to confirm compression now succeeds end-to-end.
</current_state>

<completed_work>
- 18-10 RED tests — 42eb073 (all 5 UAT token shapes: 488/3055/3511/7111/9467)
- 18-10 GREEN — 4eade57 (consume apply_adaptive_shift return + pair-atomicity guard)
- 18-10 regression matrix — a45e511
- 18-10 SUMMARY — 15adbd1
- Post-ship RED: front-straddle repro — b123179
- Post-ship fix: two-direction guard — 28caf61
- SUMMARY post-ship addendum — e20a7c2
</completed_work>

<remaining_work>
- **Blocking:** Live UAT re-run (Test 5) with 2026-04-13 config — confirm `outcome="compressed"`, zero `logic_stall` warns, one `[CONTEXT HISTORY]` system message.
- Unblock UAT Tests 6 (single pinned history), 7 (aux-model fallback), 8 (memory flush ordering).
- Test 4: separate pass at `threshold=0.50` (pressure band skipped at 0.001).
- Test 3: Telegram gateway UAT.
</remaining_work>

<decisions_made>
- Guard policy: keep pair live (safety > recovery).
- `pair_atomicity_collapsed_range` at `tracing::warn!` with "logic stall" wording.
- Two-direction guard: back-straddle pulls `prune_end` back to `asst_idx`; front-straddle pushes `prune_start` forward past `max(tool_result_idx) + 1`.
- Monotone invariant: `prune_start` only grows, `prune_end` only shrinks.
</decisions_made>

<blockers>
- Live UAT re-run is human-driven (requires real Anthropic API + `web_read` session).
</blockers>

## Required Reading (in order)
1. `.planning/phases/18-context-compression/18-10-SUMMARY.md` — shipped scope including post-ship section
2. `.planning/phases/18-context-compression/18-UAT.md` — Tests 5–8 gated on this re-run
3. `crates/ironhermes-agent/src/summarizing_engine.rs` lines 300–372 — guard loop

## Infrastructure State
- Branch: `develop`, clean working tree
- Binaries: last built pre-commit `e20a7c2` — fresh `cargo build --workspace` recommended before UAT
- UAT config: `compression_threshold=0.001`, `protect_last_tokens=100`, aux role `claude-haiku-4-5`

<context>
Two rounds of fixes. First shipped a single-branch guard which live UAT immediately proved mishandled front-straddles. Second round split the guard into two directions and added explicit front-straddle unit tests matching the live log shape. Confidence in the second UAT is high.
</context>

<next_action>
1. `cargo build --workspace` (incorporate `e20a7c2`).
2. Start agent with 2026-04-13 UAT config.
3. Drive 4–6 `web_read` turns until compression fires repeatedly.
4. Confirm: at least one `outcome = "compressed"`; zero `pair_atomicity_collapsed_range` warns; exactly one `[CONTEXT HISTORY]` system message.
5. On success: flip Tests 5/6/7/8 to `pass` in `18-UAT.md`; close the gap entry at line 99.
6. On failure: capture full `SummarizingEngine` log block (before_tokens, message indices) and spawn plan 18-11.
</next_action>
