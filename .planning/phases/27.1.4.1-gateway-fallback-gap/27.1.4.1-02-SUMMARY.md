---
phase: 27.1.4.1-gateway-fallback-gap
plan: "02"
subsystem: docs/config
tags: [config, docs, learning, tools, fallback, provider]
dependency_graph:
  requires: []
  provides: [cli-config.yaml.example-learning-section, cli-config.yaml.example-tools-section, README-fallback-docs]
  affects: [operator-config-experience, D-08-consistency-invariant]
tech_stack:
  added: []
  patterns: [commented-out-defaults-yaml, hermes-config-set-cli-pattern]
key_files:
  created: []
  modified:
    - cli-config.yaml.example
    - README.md
decisions:
  - "Both new sections in cli-config.yaml.example are fully commented-out (# key: value) per the file's established convention for optional/default settings"
  - "tools: section uses the nested enabled: bool form (matching ToolsConfig struct fields) rather than flat bool — consistent with serde round-trip behavior"
  - "README Ollama example uses local-ollama (hyphenated, matching the api_key_env: empty pattern) rather than local-llama to match the gemma4:latest canonical use case"
metrics:
  duration: "~15 min"
  completed: "2026-05-12"
  tasks_completed: 3
  files_changed: 2
---

# Phase 27.1.4.1 Plan 02: Config/Docs Consistency Summary

**One-liner:** `cli-config.yaml.example` gains `learning:` (5 wizard-written keys) and `tools.toolsets:` sections; README Provider fallback section gains `hermes config set` command and canonical cloud-primary + Ollama `gemma4:latest` example with placeholder credentials only.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add learning: and tools: sections to cli-config.yaml.example | 13c98ea5 | cli-config.yaml.example |
| 2 | Extend the README Provider fallback section | 7e90b687 | README.md |
| 3 | Finalize the ROADMAP entry and run D-08 config-consistency check | (in SUMMARY) | — |

## Deviations from Plan

### ROADMAP.md already finalized

**Found during:** Task 3 pre-read

The `### Phase 27.1.4.1: gateway fallback gap (INSERTED)` block in `.planning/ROADMAP.md` was
already finalized during the planning session (lines 890-901). It already contains:
- Real goal (no `[Urgent work - to be planned]`)
- `**Requirements:** PROV-07`
- `**Plans:** 2 plans`
- Both plan lines (`27.1.4.1-01-PLAN.md` and `27.1.4.1-02-PLAN.md`)
- `**Phase directory:**` line

Per worktree-mode instructions, ROADMAP.md is a shared orchestrator artifact and cannot be
edited by parallel executor agents. The ROADMAP entry text is included below under
"## ROADMAP entry to apply" for the orchestrator to verify/apply as needed.

No other deviations — plan executed exactly as written for Tasks 1 and 2.

## D-08 Config-Consistency Cross-Check

**Result: PASS**

Keys read by CLI / gateway / UI src trees vs. what is now documented in `cli-config.yaml.example`:

### learning.* keys

| Key | Read by | Documented in example |
|-----|---------|----------------------|
| `learning.skill_generation_enabled` | `ironhermes-cli/src/config_cli.rs:136` | YES (Task 1) |
| `learning.*` (all 5) | `ironhermes-cli/src/setup.rs:185,301` (via format!) | YES (Task 1) |
| (no learning.* reads) | `ironhermes-gateway/src/` | n/a |
| (no learning.* reads) | `iron_hermes_ui/src/` | n/a |

All 5 wizard-written `learning.*` keys are now documented in `cli-config.yaml.example`.

### tools.toolsets keys

| Usage | Read by | Documented in example |
|-------|---------|----------------------|
| `tools.toolsets.{name}.enabled` | `ironhermes-cli/src/toolset_cmd.rs:81,100` | YES (Task 1) |
| `cfg.toolsets` map iteration | `ironhermes-cli/src/toolset_cmd.rs:521,574,635` | YES (Task 1) |
| `enabled_toolset_names()` | `ironhermes-cli/src/main.rs:801,1387` | YES (Task 1) |
| `enabled_toolset_names()` | `ironhermes-cli/src/tui_rata/event_loop.rs:527` | YES (Task 1) |
| `enabled_toolset_names()` | `iron_hermes_ui/src/server/state.rs:220` | YES (Task 1) |

The `tools.toolsets` map with all 7 keys (memory/session/agent/skills/web/code/browser) is now
documented in `cli-config.yaml.example` with their correct defaults from `ToolsConfig::default()`.

No `learning.*` or `tools.*` config key read by CLI/gateway/UI is missing from the example file.
**D-08 invariant holds.**

## Known Stubs

None.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced.
Both modified files are documentation/example artifacts only (T-27.1.4.1-05: mitigated — all
example credentials use `api_key_env` env-var names or empty strings, no inline real API keys).

## ROADMAP entry to apply

The orchestrator should verify `.planning/ROADMAP.md` contains the following block at the
`### Phase 27.1.4.1: gateway fallback gap (INSERTED)` entry. It was already written during
planning; no change is needed unless it was modified between planning and this wave.

```markdown
### Phase 27.1.4.1: gateway fallback gap (INSERTED)

**Goal:** Provider fallback wired at every AgentLoop construction site (gateway cron, agent subagent, CLI batch) via one shared `wire_fallback_if_configured` helper that emits `tracing::warn!` on misconfiguration; the two existing inline-if-let fallback sites refactored onto the same helper; config/docs consistent — `cli-config.yaml.example` gains the `learning:` and `tools:` sections, README documents `fallback_providers`.
**Requirements:** PROV-07
**Depends on:** Phase 27.1.4
**Plans:** 2 plans

Plans:
- [ ] 27.1.4.1-01-PLAN.md — Shared wire_fallback_if_configured helper + lib.rs re-export + wire 5 AgentLoop sites + 3 PROV-07 invariant test files
- [ ] 27.1.4.1-02-PLAN.md — Add learning: and tools: sections to cli-config.yaml.example; extend README Provider fallback section; finalize ROADMAP entry

**Phase directory:** `.planning/phases/27.1.4.1-gateway-fallback-gap/`
```

## Self-Check: PASSED

- [x] `cli-config.yaml.example` contains all 5 learning keys and toolsets: map — verified via grep
- [x] `README.md` contains `gemma4:latest`, `hermes config set`, `ollama`, `fallback_providers`, `localhost:11434`, and 429/5xx/401 prose
- [x] No inline real API keys in either file
- [x] Task 1 committed: 13c98ea5
- [x] Task 2 committed: 7e90b687
- [x] D-08 cross-check completed and recorded above
