---
phase: 27.1.4.1-gateway-fallback-gap
verified: 2026-05-12T00:00:00Z
status: passed
score: 12/12
overrides_applied: 0
re_verification: false
---

# Phase 27.1.4.1: gateway fallback gap — Verification Report

**Phase Goal:** Provider fallback wired at every AgentLoop construction site (gateway cron, agent subagent, CLI batch) via one shared `wire_fallback_if_configured` helper that emits `tracing::warn!` on misconfiguration; the two existing inline-if-let fallback sites refactored onto the same helper; config/docs consistent — `cli-config.yaml.example` gains the `learning:` and `tools:` sections, README documents `fallback_providers`.
**Verified:** 2026-05-12
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | D-01: Cron path gets fallback resilience; "D-12: cron uses fixed provider, NO fallback" comment removed | VERIFIED | `runner.rs:1144` calls `wire_fallback_if_configured(agent, &resolver)`. Grep for "NO fallback" in runner.rs returns empty. |
| 2  | D-02: All 3 previously-uncovered sites wired (subagent_runner.rs, batch/runner.rs, runner.rs) | VERIFIED | `subagent_runner.rs:210`, `batch/runner.rs:258`, `runner.rs:1144` all call the helper. |
| 3  | D-03: handler.rs and state.rs inline if-let blocks replaced with shared helper (adds warn!, removes duplication) | VERIFIED | `handler.rs:931` and `state.rs:179` both call `wire_fallback_if_configured`. `build_client as build_provider_client` alias removed from both files (grep confirms no remaining uses). |
| 4  | D-04/D-05: Helper reads `resolve_for_main().fallback_providers.first()`, builds client, chains `.with_fallback()` on success, emits `tracing::warn!` on each failure branch (no panic, no credential leak) | VERIFIED | `any_client.rs:173-200`: two distinct `tracing::warn!` call sites (provider-not-found at :193 and client-build-failed at :185). Only `fb_name` interpolated — no api_key/token. Uses `fb_endpoint.default_model` (not primary's model, per Pitfall 2). No unwrap/expect/panic inside the helper. |
| 5  | D-06: Cron failure delivers via existing `complete_job_run` error path — no new escalation behavior | VERIFIED | `runner.rs:997,1003,1013` show `complete_job_run` is the error delivery mechanism. `wire_fallback_if_configured` call at :1144 is before the execution block; on both-providers-fail the agent runs without fallback and the normal error path fires. |
| 6  | D-07: Static invariant tests assert fallback wiring present in runner.rs, subagent_runner.rs, batch/runner.rs | VERIFIED | All 3 invariant test files exist (`include_str!`, PROV-07 and 27.1.4.1 in failure messages). Tests pass green (3/3). Note: tests assert `wire_fallback_if_configured(agent` rather than `.with_fallback(` — see Anti-Patterns/Info note below. |
| 7  | All 5 AgentLoop construction sites wire fallback through one shared helper | VERIFIED | `grep -rn "wire_fallback_if_configured(agent" crates/` returns exactly 5 call sites: runner.rs, subagent_runner.rs, batch/runner.rs, handler.rs, state.rs. Definition in any_client.rs is the 6th match. |
| 8  | D-09/D-10: `cli-config.yaml.example` gains `learning:` section with all 5 keys | VERIFIED | All 5 keys present (commented-out per file convention): `skill_generation_enabled`, `periodic_nudge_interval_seconds`, `reflection_depth`, `skill_eval`, `max_skills`. Comment points to wizard.rs source. |
| 9  | D-09: `cli-config.yaml.example` gains `tools.toolsets:` map matching `ToolsConfig::default()` | VERIFIED | `tools: toolsets:` section present with all 7 keys: memory/session/agent/skills (enabled: true), web/code/browser (enabled: false). |
| 10 | D-11: README documents `fallback_providers` with `hermes config set` command and cloud-primary + local-Ollama `gemma4:latest` example (placeholder credentials only) | VERIFIED | README lines 57-110: contains `fallback_providers`, `hermes config set providers.openrouter.fallback_providers[0] local-ollama`, `gemma4:latest`, `localhost:11434`, 429/5xx/401 prose. Uses `api_key_env: ""` (no real secret). |
| 11 | D-08: Config-consistency cross-check passes (D-08 invariant) | VERIFIED | SUMMARY-02 records cross-check: all `learning.*` keys read by CLI/gateway/UI are now in example file; all `tools.toolsets` keys documented. D-08 invariant holds. |
| 12 | ROADMAP.md phase 27.1.4.1 entry finalized (real goal, PROV-07, 2 plans, no TBD) | VERIFIED | ROADMAP.md block contains real goal, `PROV-07`, `2/2 plans complete`, both plan lines, no "TBD" or "[Urgent work - to be planned]". |

**Score:** 12/12 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/ironhermes-agent/src/any_client.rs` | `pub fn wire_fallback_if_configured` | VERIFIED | Line 173; dual warn! branches; uses `fb_endpoint.default_model`; no panic; no credential leak |
| `crates/ironhermes-agent/src/lib.rs` | re-exports `wire_fallback_if_configured` | VERIFIED | Line 30: in `pub use any_client::{...}` block |
| `crates/ironhermes-gateway/src/runner.rs` | cron AgentLoop with fallback wired; D-12 "NO fallback" removed | VERIFIED | Line 1144 call + marker comment; "NO fallback" absent |
| `crates/ironhermes-agent/src/subagent_runner.rs` | subagent AgentLoop with fallback wired | VERIFIED | Line 210 call + marker comment |
| `crates/ironhermes-cli/src/batch/runner.rs` | batch worker AgentLoop with fallback wired | VERIFIED | Line 258 call inside spawn closure using `resolver_clone` (cloned at line 246) |
| `crates/ironhermes-gateway/src/handler.rs` | inline if-let replaced with helper; `build_provider_client` alias removed | VERIFIED | Line 931 helper call; alias grep returns empty |
| `crates/iron_hermes_ui/src/server/state.rs` | inline if-let replaced with helper; `build_provider_client` alias removed | VERIFIED | Line 179 helper call; alias grep returns empty |
| `crates/ironhermes-gateway/tests/invariants_27_1_4_1.rs` | static invariant; `include_str!`; PROV-07 + 27.1.4.1 in failure message | VERIFIED | File exists; `include_str!("../src/runner.rs")`; test passes green |
| `crates/ironhermes-agent/tests/invariants_27_1_4_1.rs` | static invariant; `include_str!`; PROV-07 + 27.1.4.1 in failure message | VERIFIED | File exists; `include_str!("../src/subagent_runner.rs")`; test passes green |
| `crates/ironhermes-cli/tests/invariants_27_1_4_1.rs` | static invariant; `include_str!`; PROV-07 + 27.1.4.1 in failure message | VERIFIED | File exists; `include_str!("../src/batch/runner.rs")`; test passes green |
| `cli-config.yaml.example` | learning: (5 keys) + tools.toolsets: sections | VERIFIED | All 5 keys and 7 toolset keys present; commented-out per file convention |
| `README.md` | Provider fallback section extended | VERIFIED | gemma4:latest, hermes config set, localhost:11434, 429/5xx/401, fallback_providers all present |
| `.planning/ROADMAP.md` | finalized phase 27.1.4.1 entry | VERIFIED | Real goal, PROV-07, 2/2 plans complete, no TBD |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `runner.rs` | `ironhermes_agent::wire_fallback_if_configured` | `use` import line 7 + call line 1144 | WIRED | `use ironhermes_agent::{..., wire_fallback_if_configured}` confirmed |
| `subagent_runner.rs` | `crate::any_client::wire_fallback_if_configured` | `use crate::any_client::{AnyClient, wire_fallback_if_configured}` line 19 + call line 210 | WIRED | Import and call confirmed |
| `batch/runner.rs` | `ironhermes_agent::wire_fallback_if_configured` | `use ironhermes_agent::{..., wire_fallback_if_configured}` line 4 + call line 258 inside spawn closure with `resolver_clone` | WIRED | Import and call confirmed; resolver_clone cloned at line 246 |
| `handler.rs` | `ironhermes_agent::wire_fallback_if_configured` | `use ironhermes_agent::{..., wire_fallback_if_configured}` line 14 + call line 931 | WIRED | Old triple-nested if-let replaced; import confirmed |
| `state.rs` | `ironhermes_agent::wire_fallback_if_configured` | `use ironhermes_agent::{..., wire_fallback_if_configured}` line 10 + call line 179 | WIRED | Old triple-nested if-let replaced; import confirmed |
| `cli-config.yaml.example learning: section` | `wizard.rs::apply_learning_loop_answer` | documents same 5 keys the wizard writes | WIRED | Comment in example file cites wizard.rs source |
| `cli-config.yaml.example tools: section` | `ToolsConfig::default()` | documents same toolsets map | WIRED | Comment in example file cites config.rs source |
| `README.md Provider fallback section` | `providers.<name>.fallback_providers` config field | `hermes config set providers.openrouter.fallback_providers[0] local-ollama` | WIRED | Command and YAML example both present |

---

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Gateway invariant test passes | `cargo test -p ironhermes-gateway --test invariants_27_1_4_1` | 1 passed; 0 failed | PASS |
| Agent invariant test passes | `cargo test -p ironhermes-agent --test invariants_27_1_4_1` | 1 passed; 0 failed | PASS |
| CLI invariant test passes | `cargo test -p ironhermes-cli --test invariants_27_1_4_1` | 1 passed; 0 failed | PASS |
| Helper definition is public and re-exported | `grep "wire_fallback_if_configured" crates/ironhermes-agent/src/lib.rs` | Found in `pub use any_client::{...}` block | PASS |
| Exactly 5 call sites (no extras, no missing) | `grep -rn "wire_fallback_if_configured(agent" crates/` | 5 matches in production code | PASS |
| "NO fallback" comment removed | `grep -n "NO fallback" crates/ironhermes-gateway/src/runner.rs` | No matches | PASS |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| PROV-07 | 01-PLAN.md, 02-PLAN.md | Fallback model switching: on primary failure (429/5xx/401), try configured fallback_providers in order | SATISFIED (gap closed) | PROV-07 was previously Complete (Phase 12) for the main conversation path. This phase extends fallback coverage to cron, subagent, and batch paths which were missing. 5 call sites confirmed, invariant tests green, docs updated. |

Note: PROV-07 remains `[x] Complete` in REQUIREMENTS.md (traceability status unchanged, per task instructions). The phase closes a coverage gap in the previously-complete requirement.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `any_client.rs` | 23, 44, 58, 77 | "not yet implemented" (CodexResponses) | INFO | Pre-existing; CodexResponses is explicitly documented as unimplemented. Unrelated to this phase. |
| `runner.rs` | 1323-1348 | "placeholder" (D-17 test about a different invariant) | INFO | Inside a test function asserting a D-17 Phase 07.3 invariant. Not a stub in production code. |
| `handler.rs` | 754-758 | "placeholder" (Telegram streaming initial message) | INFO | This is the Telegram streaming "typing indicator" initial message ID, not a code stub. Pre-existing behavior. |
| Invariant tests assert `wire_fallback_if_configured(agent` not `.with_fallback(` | all 3 invariant files | Divergence from D-07 stated intent | INFO | D-07 in PLAN said tests should assert `.with_fallback(` is present. Actual tests assert the helper call pattern. However: (1) the marker comments `// chains .with_fallback() via the shared helper — PROV-07` ensure `.with_fallback(` IS literally present in all 3 source files; (2) the tests are functionally stronger (they verify the correct call pattern, not just a substring from a comment). All 3 tests pass. The invariant intent is fully preserved. |

No blockers. No warnings that affect goal achievement.

---

## Human Verification Required

None. All must-haves are verifiable programmatically and confirmed.

---

## Gaps Summary

No gaps. All 12 must-have truths are VERIFIED. The phase goal is achieved.

The PROV-07 fallback coverage gap is closed: `wire_fallback_if_configured` exists as a public shared helper, is wired at all 5 AgentLoop construction sites that have a `ProviderResolver` in scope, emits `tracing::warn!` on both misconfiguration branches without panicking or leaking credentials, and three static invariant tests lock the wiring at the three newly-covered sites. Config and docs are consistent.

---

_Verified: 2026-05-12_
_Verifier: Claude (gsd-verifier)_
