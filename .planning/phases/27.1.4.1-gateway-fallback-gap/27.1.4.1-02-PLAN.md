---
phase: 27.1.4.1-gateway-fallback-gap
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cli-config.yaml.example
  - README.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [PROV-07]

must_haves:
  truths:
    - "An operator reading cli-config.yaml.example sees the learning: section with all 5 keys the setup wizard writes (skill_generation_enabled, periodic_nudge_interval_seconds, reflection_depth, skill_eval, max_skills) with their defaults"
    - "An operator reading cli-config.yaml.example sees the tools.toolsets: map matching ToolsConfig::default() (memory/session/agent/skills on, web/code/browser off)"
    - "An operator reading README.md learns how to configure fallback_providers — including a `hermes config set` command and a cloud-primary + local-Ollama (gemma4:latest) custom-provider example with placeholder credentials only"
    - "The config-consistency rule (D-08) holds: the learning:/tools: documentation in cli-config.yaml.example reflects what ironhermes-cli/ironhermes-gateway/iron_hermes_ui actually read, and README matches"
    - "ROADMAP.md phase 27.1.4.1 entry no longer says 'Requirements: TBD' or 'Goal: [Urgent work - to be planned]'"
  artifacts:
    - path: "cli-config.yaml.example"
      provides: "documented learning: section (5 keys) and tools.toolsets: section"
      contains: "skill_generation_enabled"
    - path: "README.md"
      provides: "extended Provider fallback section with hermes config set command + Ollama gemma4:latest example"
      contains: "fallback_providers"
    - path: ".planning/ROADMAP.md"
      provides: "finalized phase 27.1.4.1 entry (goal, requirements PROV-07, 2 plans listed)"
      contains: "PROV-07"
  key_links:
    - from: "cli-config.yaml.example learning: section"
      to: "crates/ironhermes-core/src/wizard.rs apply_learning_loop_answer"
      via: "documents the same 5 keys the wizard writes via config_setter"
      pattern: "skill_generation_enabled"
    - from: "cli-config.yaml.example tools: section"
      to: "crates/ironhermes-core/src/config.rs ToolsConfig::default()"
      via: "documents the same toolsets map"
      pattern: "toolsets:"
    - from: "README.md Provider fallback section"
      to: "providers.<name>.fallback_providers config field"
      via: "hermes config set providers.openrouter.fallback_providers[0] local-ollama"
      pattern: "fallback_providers"
---

<objective>
Close the config/docs consistency gap that rides along with the PROV-07 fallback fix. `cli-config.yaml.example` is missing a `learning:` section (the 5 keys the setup wizard writes via raw-YAML splice — unknown to serde) and a `tools:` section (`ToolsConfig::default()` has a non-empty `toolsets` map). `README.md`'s Provider fallback section exists but lacks a `hermes config set` command and the canonical cloud-primary + local-Ollama (`gemma4:latest`) use case. Also finalize the ROADMAP entry for this phase.

Purpose: D-08 phase-wide invariant — any config change must be reflected across CLI / gateway / UI / yaml.example / README; surface the wizard-written `learning.*` keys so operators can see and edit them; document the real-world fallback use case.
Output: updated `cli-config.yaml.example`, `README.md`, `.planning/ROADMAP.md`. No code changes — runs in parallel with Plan 01 (no file overlap, no symbol dependency).

Implements: D-08, D-09, D-10, D-11. (D-01..D-07 are Plan 01.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27.1.4.1-gateway-fallback-gap/27.1.4.1-CONTEXT.md
@.planning/phases/27.1.4.1-gateway-fallback-gap/27.1.4.1-RESEARCH.md
@.planning/phases/27.1.4.1-gateway-fallback-gap/27.1.4.1-PATTERNS.md

<interfaces>
<!-- The source-of-truth values to document. Verified in RESEARCH D-10 audit. -->

learning.* keys written by `crates/ironhermes-core/src/wizard.rs::apply_learning_loop_answer` (defaults when enabled=true):
  skill_generation_enabled: true
  periodic_nudge_interval_seconds: 300
  reflection_depth: "standard"
  skill_eval: true
  max_skills: 500
(when enabled=false: skill_generation_enabled=false and skill_eval=false; the other three keep the same defaults.)

tools.toolsets map from `crates/ironhermes-core/src/config.rs ToolsConfig::default()`:
  memory: true
  session: true
  agent: true
  skills: true
  web: false
  code: false
  browser: false

providers.<name>.fallback_providers field — `Vec<String>`, list of custom-provider names; fallback fires on 429/5xx/401 from the primary and uses the *fallback* provider's own default_model (not the primary's).

ROADMAP phase entry to finalize (currently at .planning/ROADMAP.md around line 890):
  ### Phase 27.1.4.1: gateway fallback gap (INSERTED)
  **Goal:** [Urgent work - to be planned]
  **Requirements**: TBD
  **Depends on:** Phase 27.1.4
  **Plans:** 0 plans
  Plans:
  - [ ] TBD (run /gsd-plan-phase 27.1.4.1 to break down)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add learning: and tools: sections to cli-config.yaml.example</name>
  <files>cli-config.yaml.example</files>
  <read_first>
    - cli-config.yaml.example (the whole file — observe the existing section comment style and indentation; find a sensible insertion point near the other top-level sections; note the ~line 255-257 comment about the fallback provider's own default_model)
    - crates/ironhermes-core/src/wizard.rs (the `apply_learning_loop_answer` fn ~lines 69-101 — confirms the 5 `learning.*` keys and their default values, including the enabled=false variant)
    - crates/ironhermes-core/src/config.rs (the `ToolsConfig` struct + its `Default` impl — confirms the `toolsets` map keys and bool defaults)
    - .planning/phases/27.1.4.1-gateway-fallback-gap/27.1.4.1-PATTERNS.md section "cli-config.yaml.example — add learning: and tools: sections" (the exact YAML to add)
    - .planning/phases/27.1.4.1-gateway-fallback-gap/27.1.4.1-RESEARCH.md section "D-10: Config Gap Audit" (the verified gap list — exactly 2 sections to add: learning: and tools:; auxiliary:/extract:/browser: are out of scope)
  </read_first>
  <action>
    Add two new sections to `cli-config.yaml.example`, matching the file's existing comment-and-indentation style, each preceded by a `#`-comment explaining the source. Use placeholder values only — no real API keys anywhere in the file.

    1. `learning:` section — document all 5 keys with their enabled=true defaults: `skill_generation_enabled: true`, `periodic_nudge_interval_seconds: 300`, `reflection_depth: "standard"`, `skill_eval: true`, `max_skills: 500`. Add a `#`-comment noting these are written by the setup wizard (`apply_learning_loop_answer` in `ironhermes-core/src/wizard.rs`) via raw-YAML splice and are not part of the typed `Config` struct, and that when the learning loop is disabled `skill_generation_enabled` and `skill_eval` become `false` (the other three keep the same values). Match the surrounding file's convention for whether keys are shown commented-out-with-defaults or live-with-defaults; prefer commented-out-with-defaults if the file is inconsistent.

    2. `tools:` section with a nested `toolsets:` map mirroring `ToolsConfig::default()`: `memory: true`, `session: true`, `agent: true`, `skills: true`, `web: false`, `code: false`, `browser: false`. Add a `#`-comment noting this mirrors `ToolsConfig::default()` in `ironhermes-core/src/config.rs` and that a disabled toolset keeps its tools out of the LLM tool list.

    Do NOT add `auxiliary:`, `extract:`, or `browser:` top-level sections — RESEARCH explicitly scopes those OUT of this phase. Do NOT alter the existing provider/model/fallback example content beyond adding these two sections (the README task handles the fallback tutorial).
  </action>
  <verify>
    <automated>grep -q "skill_generation_enabled" cli-config.yaml.example && grep -q "periodic_nudge_interval_seconds" cli-config.yaml.example && grep -q "reflection_depth" cli-config.yaml.example && grep -q "skill_eval" cli-config.yaml.example && grep -q "max_skills" cli-config.yaml.example && grep -q "toolsets:" cli-config.yaml.example && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cli-config.yaml.example` contains `learning:` and all 5 keys: `skill_generation_enabled`, `periodic_nudge_interval_seconds`, `reflection_depth`, `skill_eval`, `max_skills`
    - `cli-config.yaml.example` contains `tools:` with a nested `toolsets:` map listing `memory`, `session`, `agent`, `skills`, `web`, `code`, `browser`
    - `cli-config.yaml.example` does NOT add `auxiliary:` / `extract:` top-level sections (out of scope)
    - `cli-config.yaml.example` contains no real API key values (placeholder `<...>` / empty-string / `*_env` only)
    - `git diff -- cli-config.yaml.example` shows only additions of the two new sections + their comments (no removals of existing provider/model content)
  </acceptance_criteria>
  <done>`cli-config.yaml.example` documents the `learning:` (5 keys) and `tools.toolsets:` sections with their real defaults and source-pointing comments; no other sections changed; no real credentials.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the README Provider fallback section</name>
  <files>README.md</files>
  <read_first>
    - README.md (the "### Provider fallback" section around lines 55-75 — observe the existing `local-llama` `custom_providers` example and the prose style)
    - .planning/phases/27.1.4.1-gateway-fallback-gap/27.1.4.1-PATTERNS.md section "README.md — extend Provider fallback section (docs)" (the exact additions: the `hermes config set` command, the Ollama `gemma4:latest` block, the 429/5xx/401 explanation)
    - .planning/phases/27.1.4.1-gateway-fallback-gap/27.1.4.1-RESEARCH.md section "D-11: README Documentation Gap" (the 3 verified missing pieces)
  </read_first>
  <action>
    Extend the existing `### Provider fallback` section in `README.md` (do not replace it — keep the current `local-llama` example or fold it into the new canonical one). Add:

    1. A `hermes config set` command example for setting a fallback provider, e.g. `hermes config set providers.openrouter.fallback_providers[0] local-ollama`.
    2. A cloud-primary → local-Ollama `custom_providers` YAML block using `gemma4:latest` as the fallback model: a `custom_providers.local-ollama` entry with `base_url: "http://localhost:11434/v1"`, `api_key_env: ""` (Ollama needs no key), `default_model: "gemma4:latest"`, `api_mode: chat_completions`; a `model.default` set to a cloud model (e.g. `"anthropic/claude-sonnet-4"`); and `providers.openrouter.fallback_providers: [local-ollama]`. Use placeholder/example values only — no real API keys; show the cloud provider's key via `api_key_env` (env-var name), never an inline key.
    3. One or two sentences explaining: the fallback fires when the primary returns 429 / 5xx / 401, it tries the configured `fallback_providers` in order, and the fallback request uses the *fallback* provider's own `default_model` (not the primary's model name) — so the local endpoint must actually have that model.

    Do not document `learning:` / `tools:` in the README beyond what already exists — those are out of scope for the README portion of this phase (the yaml.example file is their reference).
  </action>
  <verify>
    <automated>grep -q "fallback_providers" README.md && grep -q "gemma4:latest" README.md && grep -q "hermes config set" README.md && grep -qi "ollama" README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `gemma4:latest` and `localhost:11434` and `fallback_providers`
    - `README.md` contains a `hermes config set` command line that references `fallback_providers`
    - `README.md` contains prose mentioning 429 / 5xx / 401 (or "rate limit / server error / auth failure") as the fallback trigger
    - `README.md` contains no inline real API key values (only `api_key_env` env-var names or empty strings)
    - `git diff -- README.md` shows only additions/edits inside the Provider fallback section
  </acceptance_criteria>
  <done>The README Provider fallback section documents the `hermes config set` workflow, the cloud-primary + local-Ollama `gemma4:latest` example with placeholder credentials, and when/how fallback fires.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize the ROADMAP entry and run the config-consistency check</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (the `### Phase 27.1.4.1: gateway fallback gap (INSERTED)` block around line 890 — currently has `**Goal:** [Urgent work - to be planned]`, `**Requirements**: TBD`, `**Plans:** 0 plans`, and a `- [ ] TBD ...` plan line)
    - .planning/phases/27.1.4.1-gateway-fallback-gap/27.1.4.1-01-PLAN.md (this phase's plan 01 objective — for the plan-list line)
    - .planning/phases/27.1.4.1-gateway-fallback-gap/27.1.4.1-CONTEXT.md (the `<domain>` section — source of the one-line goal)
  </read_first>
  <action>
    Update the `### Phase 27.1.4.1: gateway fallback gap (INSERTED)` entry in `.planning/ROADMAP.md`:
    - Replace `**Goal:** [Urgent work - to be planned]` with a real one-line goal derived from CONTEXT.md `<domain>`, e.g. "Provider fallback wired at every AgentLoop construction site (cron / subagent / CLI batch) via a shared helper that warns on misconfiguration; config/docs consistent for the learning:/tools: sections and fallback_providers."
    - Replace `**Requirements**: TBD` with `**Requirements:** PROV-07` (and normalize the key style to match other entries, e.g. `**Requirements:**`).
    - Replace `**Plans:** 0 plans` with `**Plans:** 2 plans`.
    - Replace the `- [ ] TBD (run /gsd-plan-phase 27.1.4.1 to break down)` line with two plan lines:
      `- [ ] 27.1.4.1-01-PLAN.md — Shared wire_fallback_if_configured helper + lib.rs re-export + wire 5 AgentLoop sites + 3 PROV-07 invariant tests`
      `- [ ] 27.1.4.1-02-PLAN.md — Add learning: and tools: sections to cli-config.yaml.example; extend README Provider fallback section; finalize ROADMAP entry`
    - Add a `**Phase directory:** \`.planning/phases/27.1.4.1-gateway-fallback-gap/\`` line if other recent phase entries carry one.

    Then run the D-08 config-consistency cross-check (read-only — no edits beyond what Tasks 1-2 already did, but if a discrepancy is found, fix it in this task): confirm the `learning:` keys documented in `cli-config.yaml.example` match `crates/ironhermes-core/src/wizard.rs::apply_learning_loop_answer`; confirm the `tools.toolsets` keys match `ToolsConfig::default()` in `crates/ironhermes-core/src/config.rs`; confirm `crates/ironhermes-cli/src/`, `crates/ironhermes-gateway/src/`, and `crates/iron_hermes_ui/src/` do not read any `learning.*` / `tools.*` config key that the example file omits (grep for `learning.` and `tools.` / `toolsets` usage in those three src trees). Record the result in the plan SUMMARY.
  </action>
  <verify>
    <automated>grep -A6 "Phase 27.1.4.1: gateway fallback gap" .planning/ROADMAP.md | grep -q "PROV-07" && grep -A8 "Phase 27.1.4.1: gateway fallback gap" .planning/ROADMAP.md | grep -q "27.1.4.1-01-PLAN.md" && grep -A8 "Phase 27.1.4.1: gateway fallback gap" .planning/ROADMAP.md | grep -q "27.1.4.1-02-PLAN.md" && ! (grep -A6 "Phase 27.1.4.1: gateway fallback gap" .planning/ROADMAP.md | grep -q "TBD") && echo OK; grep -rn "learning\.\|toolsets" crates/ironhermes-cli/src crates/ironhermes-gateway/src crates/iron_hermes_ui/src 2>/dev/null | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` phase 27.1.4.1 block contains `PROV-07`, `2 plans`, `27.1.4.1-01-PLAN.md`, `27.1.4.1-02-PLAN.md`
    - `.planning/ROADMAP.md` phase 27.1.4.1 block contains NO `TBD` and NO `[Urgent work - to be planned]`
    - Config-consistency check completed and recorded in the SUMMARY: every `learning.*` / `tools.*` key read by CLI/gateway/UI src is documented in `cli-config.yaml.example` (or the discrepancy is fixed in this task)
  </acceptance_criteria>
  <done>The ROADMAP entry for phase 27.1.4.1 is finalized with the real goal, `PROV-07`, and both plan lines; the D-08 config-consistency cross-check passes (or any gap found is fixed) and is recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → cli-config.yaml.example / README.md | Documentation artifacts read by the operator; no runtime input parsing. The only risk is shipping real secrets in example files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.1.4.1-05 | Information Disclosure | `cli-config.yaml.example` / `README.md` example blocks | mitigate | All examples use placeholder values: cloud provider credentials shown only via `api_key_env` (env-var name), Ollama uses `api_key_env: ""`. Acceptance criteria assert no inline real API keys in either file. |
| T-27.1.4.1-06 | Tampering | Documenting `tools.toolsets` defaults | accept | Documenting existing `ToolsConfig::default()` values does not change behavior; it makes the already-active defaults visible. No new surface. |
| T-27.1.4.1-07 | Information Disclosure | `learning.*` keys are wizard-spliced raw YAML unknown to serde | accept | These keys already exist in deployed configs; documenting them in the example file is purely informational. The values are operator-controlled, non-secret config knobs. |
</threat_model>

<verification>
- `grep -q "skill_generation_enabled" cli-config.yaml.example` and the other 4 learning keys present; `toolsets:` present.
- `grep -q "gemma4:latest" README.md` and `grep -q "hermes config set" README.md` and `grep -qi "ollama" README.md`.
- `grep -A6 "Phase 27.1.4.1: gateway fallback gap" .planning/ROADMAP.md | grep -q "PROV-07"` and no `TBD` in that block.
- No inline real API keys in `cli-config.yaml.example` or `README.md` (manual scan of the diff).
- D-08 cross-check result recorded in the plan SUMMARY.
</verification>

<success_criteria>
- `cli-config.yaml.example` documents the `learning:` section (5 keys) and `tools.toolsets:` section with their real defaults and source-pointing comments.
- `README.md` Provider fallback section documents the `hermes config set` workflow, the cloud-primary + local-Ollama `gemma4:latest` example (placeholder credentials only), and the 429/5xx/401 fallback trigger.
- `.planning/ROADMAP.md` phase 27.1.4.1 entry is finalized (real goal, `PROV-07`, 2 plans listed).
- D-08 config-consistency cross-check passes (no CLI/gateway/UI-read config key missing from the example file) and is recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/27.1.4.1-gateway-fallback-gap/27.1.4.1-02-SUMMARY.md`
</output>
