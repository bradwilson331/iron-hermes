---
phase: "22.3"
plan: 1
subsystem: commands
tags: [rust, repl-ux, typo-suggester, levenshtein, pure-function]
dependency_graph:
  requires: []
  provides: [ironhermes_core::commands::typo::suggest_typo]
  affects: []
tech_stack:
  added: []
  patterns: [pure-function module, cfg-test inline tests, hand-rolled DP matrix]
key_files:
  created:
    - crates/ironhermes-core/src/commands/typo.rs
  modified:
    - crates/ironhermes-core/src/commands/mod.rs
decisions:
  - "suggest_typo placed in commands/typo.rs (not inline in handlers.rs) for testability per CONTEXT Claude's Discretion"
  - "levenshtein operates on chars() not bytes — correct Unicode scalar handling"
  - "match arms use _ => None for >3 case (equivalent to 4.. pattern but more rustfmt-friendly)"
metrics:
  duration: "2 min"
  completed: "2026-04-23"
  tasks_completed: 1
  files_changed: 2
---

# Phase 22.3 Plan 1: Typo Suggester Module Summary

**One-liner:** Hand-rolled Levenshtein-distance-2 typo suggester `suggest_typo()` as pure module function in `ironhermes_core::commands::typo`, no new crate dep.

## What Was Built

Created `crates/ironhermes-core/src/commands/typo.rs` containing:

- `pub fn suggest_typo(input: &str, candidates: &[&str]) -> Option<String>` — UI-SPEC TYPO-2 format strings, distance ≤ 2 threshold, suppresses >3 matches, case-insensitive
- `fn levenshtein(a: &str, b: &str) -> usize` — standard O(n*m) DP matrix over `Vec<char>` (Unicode-correct)
- `#[cfg(test)] mod tests` with 10 unit tests covering all UI-SPEC TYPO-1..6 behaviors

Added `pub mod typo;` to `crates/ironhermes-core/src/commands/mod.rs` after the existing three `pub mod` declarations.

## Acceptance Criteria Results

| Check | Result |
|-------|--------|
| `typo.rs` exists | PASS |
| `pub fn suggest_typo(input: &str, candidates: &[&str]) -> Option<String>` present | PASS |
| `fn levenshtein(a: &str, b: &str) -> usize` present | PASS |
| `^pub mod typo;` in `mod.rs` | PASS |
| `cargo build -p ironhermes-core` exits 0 | PASS |
| `cargo test -p ironhermes-core --lib commands::typo::` — 10 passed | PASS |
| Em-dash format string `— did you mean /` present | PASS |
| No `Cargo.toml` changes | PASS (`git diff` empty) |

## Test Count

10 unit tests, all passing:

1. `typo_single_candidate_logs` — `"log"` → `Some("— did you mean /logs?")`
2. `typo_no_candidate` — `"zzz"` → `None`
3. `typo_two_candidates_sorted_by_distance` — `"agen"` → `/agent, /agents` sorted by distance
4. `typo_three_candidates_returns_some` — 3 matches within threshold → `Some`
5. `typo_more_than_three_returns_none` — 4 matches → `None` (UI-SPEC TYPO-2)
6. `typo_case_insensitive` — `"LOG"` matches `"logs"` (TYPO-1)
7. `typo_distance_three_excluded` — distance 3 → `None`
8. `typo_exact_match_returns_some_one` — distance 0 → `Some` (single match)
9. `levenshtein_basic` — `"kitten"/"sitting"` = 3 (canonical example)
10. `levenshtein_empty_strings` — `("","")=0`, `("abc","")=3`, `("","abc")=3`

## No Cargo.toml Change

Confirmed via `git diff crates/ironhermes-core/Cargo.toml` — empty diff. No `strsim` or any other dep added. Phase 21 D-18 constraint satisfied.

## Output String Compliance

Format strings match UI-SPEC TYPO-2 verbatim:
- 1 match: `"— did you mean /{X}?"` (em-dash U+2014)
- 2-3 matches: `"— did you mean one of: /{A}, /{B}?"` (sorted ascending by distance)

No deviation from locked output strings.

## Commits

| Task | Commit | Files |
|------|--------|-------|
| Task 1: Create typo.rs + declare in mod.rs | `6db94ca` | `commands/typo.rs` (created), `commands/mod.rs` (+1 line) |

## Deviations from Plan

None — plan executed exactly as written. The implementation matches the plan's `<action>` block verbatim. `match` arm `_ => None` used instead of `4..` pattern (equivalent; rustfmt preference) but functionally identical.

## Wave Role Confirmation

This plan is Wave 1. The module is importable as:
- `use crate::commands::typo::suggest_typo;` from sibling modules in `ironhermes-core`
- `use ironhermes_core::commands::typo::suggest_typo;` from other crates

Consumer wiring at `cmd_agents` `Some(other)` arm and `tui::commands::dispatch_command` `ResolveResult::NotFound` arm is deferred to Plan 22.3-05 (Wave 2) as specified.

## Threat Flags

None — this is a pure function module with no network endpoints, auth paths, file access, or schema changes.

## Known Stubs

None — `suggest_typo` is fully implemented and all tests pass. No hardcoded empty values or placeholder text.

## Self-Check: PASS

- `crates/ironhermes-core/src/commands/typo.rs` — FOUND
- `crates/ironhermes-core/src/commands/mod.rs` contains `pub mod typo;` — FOUND
- Commit `6db94ca` — FOUND (git log confirmed)
- 10 tests passing — CONFIRMED (`cargo test` output: `test result: ok. 10 passed`)
