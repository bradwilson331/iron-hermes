---
phase: 22.3-repl-ux-hardening-visual-stability-reset-unified-history
verified: 2026-04-24T00:00:00Z
status: human_needed
score: 11/11 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Run the 3-concurrent-subagent LoRA research scenario from 22.3-UAT-EVIDENCE.md on a live TTY"
    expected: "None of D-1, D-2, D-3, D-4, D-6, D-7 reproduce"
    why_human: "Terminal visual behavior (scrollback erase, DECSTBM re-anchor, banner bleed, prompt re-anchor) cannot be verified programmatically — requires live TTY per CONTEXT D-04"
  - test: "/clear produces scrollback wipe + DECSTBM re-anchor + prompt re-anchor with no messages.truncate side-effect (D-6)"
    expected: "Terminal visually resets; /list still shows previous conversation items; next You: prompt appears at reset position"
    why_human: "ANSI escape sequence output to stderr cannot be asserted by cargo test; requires visual inspection on a live terminal"
  - test: "Banner appears exactly once at startup, no bleed during /clear cycle (D-4)"
    expected: "No banner fragment mid-session after running /clear one or more times"
    why_human: "Structural invariant INV-22.3-02 is locked, but whether the terminal scrollback actually suppresses the bleed requires live TTY observation"
  - test: "Slash commands (e.g., /help, /clear) appear in up-arrow history alongside chat prompts (D-7)"
    expected: "Up-arrow recalls both /help and prior chat prompts; duplicates suppressed; history survives ironhermes restart"
    why_human: "rustyline readline interaction cannot be exercised in cargo test; requires live terminal session"
  - test: "/agents logs <alias> immediately after subagent register no longer fails with 'No such file or directory' (D-3)"
    expected: "Command succeeds or shows an empty transcript, never an OS file-not-found error"
    why_human: "Requires spawning live subagents and exercising the alias-to-transcript path end-to-end"
---

# Phase 22.3: REPL UX Hardening — Verification Report

**Phase Goal:** Close six concrete TTY-UX defects (D-1 ticker/output clobber, D-2 typo suggestions, D-3 alias-transcript race, D-4 banner bleed, D-6 `/clear` visual reset, D-7 unified persistent history) captured verbatim in 22.3-UAT-EVIDENCE.md, and re-pass the UAT scenario on a live TTY.
**Verified:** 2026-04-24
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `commands::typo::suggest_typo` exists as pure function (22.3-01) | VERIFIED | `crates/ironhermes-core/src/commands/typo.rs:23` — `pub fn suggest_typo(input: &str, candidates: &[&str]) -> Option<String>`; `pub mod typo;` at `mod.rs:8`; 10 unit tests pass |
| 2 | `TranscriptWriter::touch()` exists + called before registry register (22.3-02) | VERIFIED | `transcript.rs:142` — `pub fn touch(&self)`; `subagent_runner.rs` ordering confirmed: open=154 < touch=160 < register=183; 2 integration tests pass |
| 3 | rustyline 15 API (`set_history_ignore_dups`, NOT `HistoryDuplicates::Prev`) in repl_input.rs (22.3-03) | VERIFIED | `repl_input.rs:255` — `rl.set_history_ignore_dups(true)`; no `set_history_duplicates` in executable code; no `HistoryDuplicates::Prev` in executable code |
| 4 | `ReplInputChannel::spawn(Some(hermes_home.join("repl_history")))` in run_chat only (22.3-03) | VERIFIED | `main.rs:814` — `ironhermes_cli::ReplInputChannel::spawn(Some(`; grep count = 1 production call |
| 5 | `CommandResult::ResetTerminal` variant in BOTH core and TUI enums with mapper arm (22.3-04) | VERIFIED | `commands/mod.rs:148` unit variant; `tui/extension.rs:128` unit variant; `tui/commands.rs:140` — `CoreCommandResult::ResetTerminal => CommandResult::ResetTerminal` |
| 6 | `cmd_clear` returns `ResetTerminal`, `cmd_new` unchanged (22.3-04) | VERIFIED | `handlers.rs:85` — `CommandResult::ResetTerminal`; `cmd_new` at lines 71-77 still returns `NewSession { message }` |
| 7 | `reset_terminal_visual` helper in `tui/render.rs` with DECSTBM-aware scrollback wipe (22.3-05) | VERIFIED | `render.rs:407` — `pub fn reset_terminal_visual(reserved: u16)`; contains `\x1b[3J\x1b[H\x1b[2J` (CLR-1); DECSTBM re-anchor with `scroll_end > 0` guard (Pitfall 6); `prompt_position_ansi` re-anchor present |
| 8 | ResetTerminal arms in prompt-time + mid-turn matches in main.rs (22.3-05) | VERIFIED | `main.rs:1170` prompt-time arm calls `reset_terminal_visual(tui.reserved_row_count())`; `main.rs:1499` mid-turn arm prints dimmed notice; no `messages.truncate` in either arm (INV-22.3-03 confirmed) |
| 9 | Slash-side `add_history(&input)` at prompt-time site; mid-turn has none (INV-22.3-06) (22.3-05) | VERIFIED | `main.rs:1136` add_history call; ordering: add_history_line=1136 < dispatch_line=1156; total `repl_input.add_history(` count = 2 exactly |
| 10 | `suggest_typo` wired at `cmd_agents Some(other)` AND `dispatch_command NotFound` (22.3-05) | VERIFIED | `handlers.rs:4` import + `handlers.rs:262` call with `&["list", "kill", "logs"]`; `tui/commands.rs:16` import + `tui/commands.rs:110` call with `commands_for_platform` candidates |
| 11 | Six static-grep invariants in `crates/ironhermes-cli/tests/invariants_22_3.rs` all pass (22.3-06) | VERIFIED | `cargo test -p ironhermes-cli --test invariants_22_3` — `test result: ok. 6 passed`; file has 6 `#[test]` functions, 6 named functions matching convention, 3 `include_str!` constants |

**Score:** 11/11 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/ironhermes-core/src/commands/typo.rs` | `pub fn suggest_typo` + levenshtein + 10 unit tests | VERIFIED | Exists, substantive (267 lines), wired via `pub mod typo;` declaration |
| `crates/ironhermes-agent/src/transcript.rs` | `pub fn touch(&self)` method | VERIFIED | touch() at line 142 using sync `std::fs::OpenOptions::new().create(true).append(true)` |
| `crates/ironhermes-agent/src/subagent_runner.rs` | `writer.touch()` before register | VERIFIED | open=154, touch=160, register=183 — correct ordering |
| `crates/ironhermes-agent/tests/transcript_touch.rs` | 2 integration tests | VERIFIED | `transcript_file_exists_after_touch` + `touch_is_idempotent_does_not_truncate` both pass |
| `crates/ironhermes-cli/src/repl_input.rs` | History wired with correct rustyline 15 API | VERIFIED | `set_history_ignore_dups(true)`, `load_history`, `save_history`, NotFound-silent; drop-stub gone |
| `crates/ironhermes-core/src/commands/mod.rs` | `ResetTerminal` variant + `pub mod typo;` | VERIFIED | Both present; unit variant at line 148; `pub mod typo;` at line 8 |
| `crates/ironhermes-cli/src/tui/extension.rs` | `ResetTerminal` unit variant in TUI enum | VERIFIED | Line 128, no payload |
| `crates/ironhermes-cli/src/tui/commands.rs` | Mapper arm + `suggest_typo` in NotFound | VERIFIED | Mapper line 140; suggest_typo import line 16, call line 110 |
| `crates/ironhermes-core/src/commands/handlers.rs` | `cmd_clear` → ResetTerminal + suggest_typo in cmd_agents | VERIFIED | cmd_clear line 85; suggest_typo import line 4, call line 262 |
| `crates/ironhermes-cli/src/tui/render.rs` | `reset_terminal_visual(reserved: u16)` helper | VERIFIED | Line 407; DECSTBM-aware; placed between `prompt_position_ansi` (373) and `prepare_prompt_with_reserve` (431) |
| `crates/ironhermes-cli/src/main.rs` | ResetTerminal arms (×2) + slash add_history + spawn(Some) | VERIFIED | Lines 1170, 1499 (ResetTerminal); line 1136 (add_history); line 814 (spawn(Some)) |
| `crates/ironhermes-cli/tests/invariants_22_3.rs` | 6 static-grep invariants, all pass | VERIFIED | 6 tests, 6 named functions, 3 include_str! constants; all 6 pass |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `commands/typo.rs` | `commands/mod.rs` | `pub mod typo;` at mod.rs:8 | WIRED | Declaration present; module importable as `crate::commands::typo::suggest_typo` |
| `subagent_runner.rs` | `transcript.rs` | `writer.touch()` at line 160 | WIRED | Call exists; ordering open < touch < register confirmed |
| `main.rs` (spawn site) | `repl_input.rs` | `ReplInputChannel::spawn(Some(hermes_home.join("repl_history")))` at line 814 | WIRED | Exactly 1 production call; run_chat only (D-15) |
| `handlers.rs` | `commands/typo.rs` | `use crate::commands::typo::suggest_typo` + call at line 262 | WIRED | Import line 4; call in `cmd_agents Some(other)` arm |
| `tui/commands.rs` | `commands/typo.rs` | `use ironhermes_core::commands::typo::suggest_typo` + call at line 110 | WIRED | Import line 16; call in `ResolveResult::NotFound` arm with `commands_for_platform` candidates |
| `main.rs` | `tui/render.rs` | `reset_terminal_visual(tui.reserved_row_count())` at line 1175 | WIRED | Import via `use crate::tui::reset_terminal_visual`; re-exported through `tui/mod.rs` |
| `invariants_22_3.rs` | `main.rs` + `repl_input.rs` + `handlers.rs` | `include_str!` constants + string assertions | WIRED | 3 include_str! constants; cross-crate path for handlers.rs confirmed |

### Data-Flow Trace (Level 4)

These are structural/behavioral fixups producing observable terminal/command output, not components that render DB-backed dynamic data. Data-flow at the relevant function level:

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `suggest_typo` | `matches: Vec<(&str, usize)>` | Levenshtein distance computation over `candidates` slice | Yes — pure function, no empty return | FLOWING |
| `reset_terminal_visual` | ANSI bytes to stderr | `crossterm::terminal::size()` + `prompt_position_ansi` | Yes — real terminal geometry; is_tty guard prevents no-op on pipes | FLOWING |
| `TranscriptWriter::touch()` | File on disk | `std::fs::OpenOptions::new().create(true).append(true).open(&self.path)` | Yes — sync syscall creates real file | FLOWING |
| `repl_input.rs` history | rustyline ring buffer | `rl.load_history(path)` on init; `rl.add_history_entry` on each input | Yes — reads/writes real `$HERMES_HOME/repl_history` file | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| invariants_22_3 — 6 static-grep gates | `cargo test -p ironhermes-cli --test invariants_22_3` | `test result: ok. 6 passed` | PASS |
| transcript_touch — 2 runtime tests | `cargo test -p ironhermes-agent --test transcript_touch` | `test result: ok. 2 passed` | PASS |
| Live-TTY visual reset (/clear) | Requires live TTY | Cannot automate | SKIP (human required) |
| rustyline up-arrow history recall | Requires live TTY | Cannot automate | SKIP (human required) |

### Requirements Coverage

This is a fix-up phase — no REQ-IDs map to Phase 22.3. The UI-SPEC contract (22.3-UI-SPEC.md) and UAT scenario (22.3-UAT-EVIDENCE.md) serve as the requirements set per ROADMAP. All six defects D-1..D-7 (minus D-5, already addressed in Phase 21.7) have automated structural coverage; D-4 (banner bleed) is addressed structurally by INV-22.3-02. Live-TTY re-run (CONTEXT D-04) is the final gate.

| Defect | Plan | Automated Coverage | Status |
|--------|------|--------------------|--------|
| D-2 typo suggestions | 22.3-01 + 22.3-05 | suggest_typo unit tests (10); suggest_typo wired at both sites | SATISFIED (automated) |
| D-3 alias-transcript race | 22.3-02 | transcript_touch integration tests (2); touch < register ordering | SATISFIED (automated) |
| D-4 banner bleed | 22.3-06 INV-22.3-02 | INV-22.3-02: print_banner(); count=1 and strictly before new_with_extensions | SATISFIED (structural) |
| D-6 /clear visual reset | 22.3-04 + 22.3-05 | INV-22.3-01/03: ResetTerminal arms exist, no messages.truncate | NEEDS HUMAN (visual) |
| D-7 unified history | 22.3-03 + 22.3-05 | INV-22.3-04/05/06: API correct, add_history count=2, drop-stub gone | NEEDS HUMAN (TTY behavior) |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `handlers.rs` | 61, 652 | `// TODO stubs — everything without backing infrastructure` | Info | Pre-existing from Phase 21.1 (confirmed present in commit `4790bb3` before any Phase 22.3 edits); not introduced by this phase |
| `handlers.rs` | 454 | `// Keybindings section placeholder — actual keybindings injected by CLI adapter` | Info | Pre-existing from Phase 21 (comment only; keybindings are injected by CLI adapter as documented); not introduced by this phase |

No blockers or warnings introduced by Phase 22.3. All flagged items are pre-existing informational comments from prior phases.

### Human Verification Required

#### 1. Full D-1..D-7 UAT Scenario Re-Run

**Test:** Run the exact 3-concurrent-subagent LoRA research scenario from `22.3-UAT-EVIDENCE.md` on a live TTY. Suggested checklist per 22.3-06-SUMMARY.md:
1. `/clear` produces visual scrollback wipe, no `messages.truncate` side-effect (D-6, INV-22.3-01/03)
2. Banner appears exactly once at startup, no bleed during `/clear` cycle (D-4, INV-22.3-02)
3. `/agents list`, `/agents kill <alias>`, `/agents logs <alias>` all work during an active turn (D-3)
4. Unknown slash command shows typo suggestion: `/agnet` → `— did you mean /agents?` (D-2)
5. Unknown `/agents` subcommand shows typo suggestion: `/agents log` → `— did you mean /logs?` (D-2)
6. History persists across `ironhermes` restarts via `$HERMES_HOME/repl_history` (D-7)
7. Slash commands (e.g., `/help`) appear in up-arrow history alongside chat prompts (D-7, INV-22.3-04)
8. Duplicate consecutive history entries suppressed (INV-22.3-05)
9. Mid-turn `/clear` shows "ignored mid-turn" message, does NOT add to history (INV-22.3-06)

**Expected:** Each of D-1..D-7 (minus D-5) no longer reproduces. All items above pass.
**Why human:** Terminal visual behavior, scrollback state, banner bleed, readline interaction, and live-subagent file-system race cannot be asserted by cargo test. CONTEXT D-04 explicitly designates this as an operator task.

#### 2. /clear TTY Visual Reset

**Test:** At an ironhermes REPL with several exchanges visible, type `/clear` and observe the terminal.
**Expected:** Scrollback erased, screen cleared, DECSTBM re-established, `You: ` prompt appears at reset position. Previous conversation items are still present in `/list` output (no `messages.truncate` occurred).
**Why human:** ANSI escape sequences written to stderr cannot be inspected by automated tests; visual confirmation required.

#### 3. History Persistence Across Restart

**Test:** Type `/help`, then `hello world`, then exit with `/quit`. Restart ironhermes. Press up-arrow.
**Expected:** `hello world` and `/help` both appear in history in reverse-entry order.
**Why human:** rustyline readline interaction requires a live PTY; cargo test cannot simulate up-arrow key events.

### Gaps Summary

No gaps found. All 11 automated must-haves are verified in the codebase. The phase is structurally complete; the only remaining work is the live-TTY human UAT re-run mandated by CONTEXT D-04. This produces `human_needed` status as expected for this phase.

---

_Verified: 2026-04-24_
_Verifier: Claude (gsd-verifier)_
