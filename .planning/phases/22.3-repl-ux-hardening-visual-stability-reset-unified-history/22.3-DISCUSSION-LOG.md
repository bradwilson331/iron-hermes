# Phase 22.3: REPL UX hardening - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-23
**Phase:** 22.3-repl-ux-hardening-visual-stability-reset-unified-history
**Areas discussed:** Plan structure, Banner-bleed investigation depth, Test strategy, UAT re-verification scenario

---

## Plan structure

| Option | Description | Selected |
|--------|-------------|----------|
| Two-wave: foundations → integration | Wave 1 parallel: typo helper (pure fn), TranscriptWriter touch, history activation, ResetTerminal variant addition. Wave 2: run_chat/run_single integration (slash add_history, ResetTerminal handler, banner-once invariant, copy fixes). 4-5 plans total. Maximum parallelism, atomic landings. | ✓ |
| One plan per defect (D-1..D-7) | 6-7 narrow plans, one per defect. Easier to review, slower to land (more wave dependencies on shared files like main.rs and handlers.rs). | |
| Single mega-plan | One plan covering all six defects atomically. Simplest history, no wave coordination, but larger blast radius if execution fails partway. | |

**User's choice:** Two-wave: foundations → integration (Recommended)
**Notes:** Captured as D-01 in CONTEXT.md. Justification: UI-SPEC components are pure-function-shaped at the boundary, only the integration site (main.rs around lines 1142-1213) needs sequential edits.

---

## Banner-bleed investigation depth

| Option | Description | Selected |
|--------|-------------|----------|
| Trust the spec | Implement INV-22.3-02 static-grep, apply DECSTBM hardening, re-run live UAT. If banner bleed reappears, open a follow-up gap. | |
| Spawn a research probe first | Spawn a debugger/researcher to reproduce D-4 deterministically and isolate the actual writer before planning. Adds time but de-risks. | ✓ |
| Add a runtime guard too | In addition to static-grep, add an AtomicBool BANNER_PRINTED guard with a tracing::warn on violators. Defensive belt-and-braces. | |

**User's choice:** Spawn a research probe first
**Notes:** Captured as D-02 in CONTEXT.md. The user explicitly preferred investigation over trust — landing INV-22.3-02 (single call site) without understanding the actual writer risks the bleed reappearing in re-UAT and forcing another phase. Probe deliverable: 1-2 page findings note (`22.3-BANNER-PROBE.md` or appended to UAT-EVIDENCE) identifying the writer + recommended fix BEFORE the planner finalises the integration wave. Probe does NOT block Wave 1 plans (typo / transcript-touch / history activation can run in parallel with the probe).

---

## Test strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Static-grep invariants only | 6 INV-22.3-* invariants as static-grep regression tests (matches existing INV-21.7/22.1 pattern). Visual stability proven by manual UAT re-run. Zero new dev-deps. | ✓ |
| Add vt100 pty integration test | Spawn `hermes` in a vt100/expectrl pty harness, drive a scripted concurrent-subagent scenario, snapshot the rendered terminal grid. Adds dev-deps, conflicts with D-18 unless dev-deps-only is acceptable. | |
| Both | Static-grep invariants in Wave 1, then a follow-up plan adds vt100 harness as opt-in `--features tty-snapshot`. | |

**User's choice:** Static-grep invariants only (Recommended)
**Notes:** Captured as D-03 in CONTEXT.md. Matches Phase 21 D-18 (no new crate deps). Deferred vt100 harness logged in CONTEXT.md `<deferred>` for future regression demand.

---

## UAT re-verification scenario

| Option | Description | Selected |
|--------|-------------|----------|
| Same as 22.3-UAT-EVIDENCE | Re-run the exact 3-concurrent-subagent LoRA-research scenario from the captured evidence. Apples-to-apples comparison against the recorded baseline. | ✓ |
| Scripted multi-defect checklist | Build a deterministic checklist exercising each defect class. More thorough but longer to execute. | |
| Operator's choice | Defer to the user during VERIFICATION step — accept the LoRA scenario or any equivalent multi-subagent workload. | |

**User's choice:** Same as 22.3-UAT-EVIDENCE (Recommended)
**Notes:** Captured as D-04 in CONTEXT.md. Acceptance gate: each of D-1..D-7 (minus D-5) no longer reproduces. Transcript belongs in `22.3-HUMAN-UAT.md`.

---

## Final check

| Option | Description | Selected |
|--------|-------------|----------|
| I'm ready for context | UI-SPEC locks user-visible behavior; the four discussed areas cover the meaningful planner choices. | ✓ |
| Surface deferred enhancements | Re-open UI-SPEC's deferred items: Ctrl-L keybinding, config-file key for history size, history redaction policy. | |
| Discuss research-probe scope | Drill into what the banner-bleed probe should look at before planning. | |

**User's choice:** I'm ready for context (Recommended)

---

## Claude's Discretion

- Exact placement of `reset_terminal_visual` helper (render.rs vs new file)
- Whether `suggest_typo` lives in `commands/typo.rs` or inline in `handlers.rs`
- Exact regex anchors for the six INV-22.3-* static-grep tests
- Whether to use the optional `print_slash_block` helper macro or inline the pattern
- Exact text format of the `22.3-BANNER-PROBE.md` findings note

## Deferred Ideas

- Ctrl-L keybinding for `/clear` (requires rustyline config + Phase 22.1 KeybindingRegistry plumbing)
- Config-file key for max history size (hardcoded 1000 in this phase)
- Sensitive-arg history redaction list
- vt100 / pty integration test harness as `--features tty-snapshot`
- Scripted multi-defect UAT checklist
- Runtime `BANNER_PRINTED` AtomicBool guard
- Mute-ticker-during-slash-dispatch buffer (UI-SPEC permanently rejects)
