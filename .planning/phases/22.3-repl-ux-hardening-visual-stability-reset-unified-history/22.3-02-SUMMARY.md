---
phase: "22.3"
plan: 2
subsystem: ironhermes-agent
tags: [rust, repl-ux, transcript, subagent-runner, alias-race]
dependency_graph:
  requires: []
  provides: [TranscriptWriter::touch, transcript-touch-before-register]
  affects: [subagent_runner.rs, transcript.rs]
tech_stack:
  added: []
  patterns: [sync-std-fs-in-async-context, touch-before-register-ordering]
key_files:
  created:
    - crates/ironhermes-agent/tests/transcript_touch.rs
  modified:
    - crates/ironhermes-agent/src/transcript.rs  (lines 127-149: touch() method added)
    - crates/ironhermes-agent/src/subagent_runner.rs  (lines 150-167: writer binding + touch call)
decisions:
  - "tempfile variant A chosen — tempfile = 3 is already a dev-dep in ironhermes-agent/Cargo.toml; no new dep required per Phase 21 D-18"
  - "touch() fires BEFORE SubagentRegistry::register(info) at line 183 — corrects CONTEXT D-07 inversion per RESEARCH §Wiring Sites §1"
  - "touch() uses sync std::fs::OpenOptions matching open()'s create_dir_all sync style; safe in async context (single short syscall)"
metrics:
  duration: "2 minutes"
  completed: "2026-04-24"
  tasks_completed: 3
  files_modified: 3
---

# Phase 22.3 Plan 2: Transcript Touch on Registration Summary

TranscriptWriter::touch() sync method + call site wiring in subagent_runner.rs + 2-test integration suite proving file-on-disk before register.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Add TranscriptWriter::touch() method | a0cc145 | crates/ironhermes-agent/src/transcript.rs |
| 2 | Insert writer.touch() in subagent_runner.rs | ca2f1fe | crates/ironhermes-agent/src/subagent_runner.rs |
| 3 | Add integration test transcript_touch.rs | b811de8 | crates/ironhermes-agent/tests/transcript_touch.rs |

## What Was Built

- `TranscriptWriter::touch()` — a sync `std::fs::OpenOptions::new().create(true).append(true).open(&self.path)` method placed between `path()` and `append()` in `impl TranscriptWriter`. Errors silently dropped per Pitfall 3 / E-08 (no double-report). Lines 127–149 of `transcript.rs`.

- Call site in `subagent_runner.rs` — The original `Some(TranscriptWriter::open(path))` one-liner is split into `let writer = TranscriptWriter::open(path); writer.touch(); Some(writer)`. Touch fires at line 160, before `reg.write().await.register(info)` at line 183. Source ordering: open=154 < touch=160 < register=183.

- Integration test `crates/ironhermes-agent/tests/transcript_touch.rs`:
  - `transcript_file_exists_after_touch` — opens TempDir, calls open()+touch(), asserts `fs::metadata(&path).is_ok()`. INV-22.3-05 assertion message present.
  - `touch_is_idempotent_does_not_truncate` — writes content, calls touch() again, asserts content preserved (append semantics, not truncate).
  - Both tests pass: `test result: ok. 2 passed`

## Key Facts

- **Confirmed file paths and line ranges:**
  - `transcript.rs`: touch() at lines 127–149 (between path() at 123 and append() at 150+)
  - `subagent_runner.rs`: writer binding line 154, touch() line 160, register() line 183
  - `tests/transcript_touch.rs`: new file, 35 lines

- **Test count:** 2 (both pass)

- **Touch fires BEFORE register:** confirmed via awk source-order check (open=154 < touch=160 < register=183). This is the RESEARCH-corrected ordering — CONTEXT D-07 originally described "after register" which was inverted.

- **tempfile variant chosen:** Variant A (tempfile dev-dep). `tempfile = "3"` was already present in `[dev-dependencies]` in `crates/ironhermes-agent/Cargo.toml`. No new dep added.

- **No Cargo.toml change:** `git diff crates/ironhermes-agent/Cargo.toml` is empty. Phase 21 D-18 satisfied.

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns beyond the existing transcript dir, or schema changes at trust boundaries.

## Self-Check: PASSED

- `crates/ironhermes-agent/src/transcript.rs` — FOUND (modified)
- `crates/ironhermes-agent/src/subagent_runner.rs` — FOUND (modified)
- `crates/ironhermes-agent/tests/transcript_touch.rs` — FOUND (created)
- Commit a0cc145 — FOUND
- Commit ca2f1fe — FOUND
- Commit b811de8 — FOUND
