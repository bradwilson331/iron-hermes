# Phase 32.2: subagent-delegation-parity - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-16
**Phase:** 32.2-subagent-delegation-parity
**Areas discussed:** Phase scope, Nested orchestration, Blocked tools, Config rename, TUI tree view

---

## Phase Scope

| Option | Description | Selected |
|--------|-------------|----------|
| PRMT-05 skip_context_files | SOUL.md not loaded for children | |
| parent_id threading | Subagent registry parent_id=None TODO | |
| MemoryProvider delegation hooks | on_memory_write not firing during subagent turns | |
| Hermes-agent docs parity (full list) | User pasted canonical hermes-agent delegation docs | ✓ |

**User's choice:** User pasted the full hermes-agent delegation documentation and noted it was discovered while writing cron job documentation — the Rust implementation was missing several features described in the canonical docs.

---

## Capability Areas (multiselect)

| Option | Selected |
|--------|----------|
| Nested orchestration (role + max_spawn_depth) | ✓ |
| Complete blocked-tools list (clarify + send_message) | ✓ |
| Batch oversize → tool error | ✓ |
| Config key rename + per-call max_iterations | ✓ |
| /agents TUI tree view (all surfaces) | ✓ — user added: must wire in all TUI/gateway/web |

---

## Nested Orchestration

| Option | Description | Selected |
|--------|-------------|----------|
| delegate_task + same toolsets as leaf | Orchestrator gets delegation tool back plus specified toolsets | ✓ |
| delegate_task only (pure dispatcher) | Orchestrators only spawn, no file/web/terminal work | |

**User's choice:** Orchestrator gets delegate_task + same toolsets as leaf — mirrors Python.

| Option | Description | Selected |
|--------|-------------|----------|
| Default 1 (flat); raise via config only | max_spawn_depth: 1, role="orchestrator" is no-op at defaults | ✓ |
| Default 2 (one orchestrator level) | Allow one level of orchestration by default | |

**User's choice:** Default 1 (flat) — raise via config only. Matches Python hermes-agent defaults.

---

## Blocked Tools

**User's choice:** `clarify` and `send_message` are blocked for ALL children (leaf + orchestrator) — they have no user-interaction channel or platform session. User confirmed by checking hermes-agent source: `_NEVER_PARALLEL_TOOLS = frozenset({"clarify"})` and `send_message` is platform-dependent in toolsets.py.

---

## Config Key Rename

| Option | Description | Selected |
|--------|-------------|----------|
| Support both keys (backward compat) | delegation: wins if both present | |
| Hard rename — subagent: is dead | Only delegation: works; users update config.yaml | ✓ |

**User's choice:** Hard rename. No backward-compat alias.

---

## /agents Tree View

| Option | Description | Selected |
|--------|-------------|----------|
| Thread parent_id (existing TODO) | SubagentInfo.parent_id from subagent_runner.rs line 185 TODO | ✓ + all surfaces |
| Separate orchestrator session tree | New SessionTree type in registry | |

**User's choice:** Thread parent_id (already a TODO). Wire in ALL surfaces: tui_rata, gateway, web.

---

## Claude's Discretion

- Exact `child_depth` threading approach (field vs parameter)
- Per-branch cost/token rollup in tree view (stub zeroes if instrumentation is heavy)
- Diagnostic timeout dump (implement if falls naturally out of timeout path, otherwise defer)

## Deferred Ideas

- Diagnostic timeout dump — `~/.ironhermes/logs/subagent-timeout-<session>-<ts>.log` on zero-API-call timeout. Left to Claude's discretion; defer if non-trivial.
- MemoryProvider delegation hooks — explicitly deferred to Phase 34b+
- Per-subtree pause controls in /agents overlay — kill is in scope; pause is deferred
- PRMT-05 skip_context_files wiring — deferred (not part of delegation parity scope)
