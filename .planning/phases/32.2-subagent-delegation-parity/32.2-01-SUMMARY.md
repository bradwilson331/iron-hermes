# Plan 32.2-01 Summary: Config Rename subagent→delegation

**Status:** COMPLETE  
**Commits:** `72b155a0`, `2c8a49f3`

## What Was Done

Hard-renamed the `subagent:` config namespace to `delegation:` across the entire workspace, added two new fields to `SubagentConfig`, and wired a load-time deprecation gate.

## Grep Counts (post-rename)

| Check | Count |
|-------|-------|
| `pub delegation: SubagentConfig` in config.rs | 1 |
| `pub subagent: SubagentConfig` in config.rs | 0 |
| `pub max_spawn_depth: u32` in config.rs | 1 |
| `pub orchestrator_enabled: bool` in config.rs | 1 |
| `pub max_concurrent_children: usize` in config.rs | 1 |
| `pub child_timeout_seconds: u64` in config.rs | 1 |
| `fn detect_legacy_subagent_key` in config.rs | 1 |
| `pub max_concurrent_children: Option<usize>` in context.rs | 1 |
| `pub fn with_max_concurrent_children` in context.rs | 1 |
| `config.subagent.` in main.rs | 0 |

## Deprecation Message Text

```
Config key 'subagent:' is deprecated and no longer supported. Rename it to 'delegation:' in your config.yaml.
```

## Unexpected Call Sites Found

Beyond the four files listed in the plan, these additional files required migration:

- `crates/ironhermes-cli/src/status_cmd.rs:442` — `config.subagent.max_subagents` (SubagentStatus construction)
- `crates/ironhermes-cli/src/tui_rata/event_loop.rs:252,274,290` — 3 `config.subagent.*` accesses (semaphore + DelegateTaskWiring)
- `crates/ironhermes-agent/src/app_runtime_factory.rs:401` — `config.subagent.clone()` (DelegateTaskWiring)
- `crates/iron_hermes_ui/src/server/state.rs:101-102` — `config.subagent.max_subagents` + `config.subagent.clone()`
- `crates/ironhermes-tools/src/delegate_task.rs:987,1379` — SubagentConfig literal fields in test bodies (`timeout_secs: 1`, `max_subagents: 2`)

Note: `crates/ironhermes-cli/src/main.rs` lines 1202 and 2404 use `max_subagents:` as a field of `SubagentStatus` (a UI display struct in `tui/status_line.rs` / `tui_rata/status_line.rs`), **not** SubagentConfig — these are intentionally unchanged since `SubagentStatus.max_subagents` is a separate struct not covered by the D-07 rename.

Note: `crates/ironagent-tools-api/` is excluded from the workspace (stale duplicate per Cargo.toml comment). Its test file was updated in-place but cannot be compiled via cargo.

## Verification

- `cargo check --workspace` exits 0 ✓
- `cargo test -p ironhermes-core --lib config::` — 77 passed, 0 failed ✓
- `test_config_parses_delegation_key` ✓
- `test_subagent_config_defaults_include_new_fields` ✓
- `test_legacy_subagent_key_detected` ✓
- `test_delegation_key_not_flagged` ✓
- `test_subagent_substring_in_value_not_flagged` ✓
