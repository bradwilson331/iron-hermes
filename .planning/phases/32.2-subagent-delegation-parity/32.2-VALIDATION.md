---
phase: 32.2
slug: subagent-delegation-parity
status: ready
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-16
updated: 2026-05-17
---

# Phase 32.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | cargo test |
| **Config file** | Cargo.toml (workspace) |
| **Quick run command** | `cargo test -p ironhermes-tools -p ironhermes-agent -p ironagent-tools-api -p ironhermes-core` |
| **Full suite command** | `cargo test --workspace` |
| **Estimated runtime** | ~30 seconds (quick); ~90 seconds (full) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-tools -p ironhermes-agent -p ironagent-tools-api -p ironhermes-core`
- **After every plan wave:** Run `cargo test --workspace`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

> Every task in Phase 32.2 has `<automated>` verify per Nyquist rule. `wave_0_complete: false` reflects that Wave 0 scaffolding test files (where listed) are created as part of plan execution, not pre-execution.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 32.2-01-01 | 01 | 1 | DEL-07, DEL-08, DEL-09 | T-32.2-01, T-32.2-02 | Config rename + legacy `subagent:` key triggers exit(1); default `max_iterations = 50` (D-08) | unit | `cargo test -p ironhermes-core --lib config:: 2>&1 \| tail -40` | ✅ (modifies existing config.rs tests) | ⬜ pending |
| 32.2-01-02 | 01 | 1 | DEL-07 | T-32.2-03 | Rename propagates atomically across workspace; no legacy identifiers remain | unit + compile | `cargo check --workspace 2>&1 \| tail -30` | ✅ (modifies existing context.rs / main.rs / fixtures) | ⬜ pending |
| 32.2-02-01 | 02 | 2 | DEL-05, DEL-06 | T-32.2-04, T-32.2-05 | `clarify` + `send_message` stripped from child registry; batch oversize returns `Err` | unit | `cargo test -p ironhermes-tools -p ironagent-tools-api 2>&1 \| tail -30` | ✅ (modifies existing delegate_task tests; adds new tests in same files) | ⬜ pending |
| 32.2-02-02 | 02 | 2 | DEL-08 | T-32.2-06 | `max_iterations` per-call schema override (single + batch) | unit | `cargo test -p ironhermes-tools -p ironagent-tools-api --lib delegate_task 2>&1 \| tail -30` | ✅ (modifies existing delegate_task schema tests) | ⬜ pending |
| 32.2-03-01 | 03 | 3 | DEL-01, DEL-02, DEL-03, DEL-04 | T-32.2-07, T-32.2-08, T-32.2-09 | `ChildRole` + depth/kill-switch guard; orchestrator re-adds `delegate_task` only when allowed | unit | `cargo test -p ironhermes-tools -p ironagent-tools-api --lib delegate_task 2>&1 \| tail -40` | ✅ (modifies existing delegate_task; adds new tests in same file) | ⬜ pending |
| 32.2-03-02 | 03 | 3 | DEL-04, DEL-10 | T-32.2-10, T-32.2-11, T-32.2-12 | `current_depth` + `caller_subagent_id` threaded through `AgentSubagentRunner` to child spawn | unit | `cargo test -p ironhermes-agent --lib subagent_runner:: 2>&1 \| tail -30` | ✅ (modifies existing subagent_runner tests) | ⬜ pending |
| 32.2-04-01 | 04 | 4 | DEL-11 | T-32.2-13 | `SubagentTreeEntry` + `tree_summary()` default impl on trait; FakeSubagentList unchanged | unit | `cargo test -p ironhermes-core --lib commands::context::tests::test_default_tree_summary_flattens_list_summary 2>&1 \| tail -15 && cargo check -p ironhermes-core 2>&1 \| tail -10` | ✅ (adds new test in existing context.rs tests mod) | ⬜ pending |
| 32.2-04-02 | 04 | 4 | DEL-10, DEL-11 | T-32.2-13, T-32.2-14, T-32.2-15 | `build_tree()` recursion sorted by `started_at`; `SubagentInfo.parent_id` populated from `caller_subagent_id`; `flatten_tree` populates depth + status | unit | `cargo test -p ironhermes-agent --lib subagent_registry:: 2>&1 \| tail -20 && cargo test -p ironhermes-agent --lib subagent_runner::tests::test_run_child_propagates_caller_id_to_subagent_info 2>&1 \| tail -15 && cargo build --workspace 2>&1 \| tail -5` | ✅ (adds new tests in existing subagent_registry / subagent_runner tests mods) | ⬜ pending |
| 32.2-05-01 | 05 | 5 | DEL-12 | T-32.2-16, T-32.2-17 | `cmd_agents` renders indented ASCII tree with 8-char id prefix + 80-char summary clip + status pill; flat case preserves legacy output | unit | `cargo test -p ironhermes-core --lib commands::handlers::tests::test_cmd_agents_empty_registry commands::handlers::tests::test_cmd_agents_flat_list_unchanged commands::handlers::tests::test_cmd_agents_renders_tree_when_nested commands::handlers::tests::test_render_agent_tree_three_levels 2>&1 \| tail -20` | ✅ (adds new tests in existing handlers.rs tests mod) | ⬜ pending |
| 32.2-05-02 | 05 | 5 | DEL-12 | T-32.2-18 | iron_hermes_ui `subagent_tree_json` adapter returns nested JSON; docs/DELEGATION.md fully updated | unit + grep | `cargo test -p iron_hermes_ui --lib server::state::tests::test_subagent_tree_json_serializes_nested_tree 2>&1 \| tail -15 \|\| cargo test -p iron_hermes_ui 2>&1 \| grep -E "test_subagent_tree_json\|test result" \| tail -10 ; grep -c "delegation:" docs/DELEGATION.md ; grep -c "there is no.*role parameter" docs/DELEGATION.md` | ✅ (adds new test in existing/new state.rs tests mod; edits docs/DELEGATION.md) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Plans 01-05 all extend existing test mods rather than creating standalone Wave 0 files. The `wave_0_complete: false` flag stays false because the per-task tests are authored as part of the TDD task itself (RED step), not pre-execution. Each plan task carries `tdd="true"` and a `<behavior>` block enumerating the failing tests to write first.

The original Wave 0 file proposals are subsumed into per-plan task work:
- ~~`crates/ironhermes-core/tests/config_rename.rs`~~ → handled inline in Plan 01 Task 1 (config.rs lib tests mod)
- ~~`crates/ironhermes-tools/tests/delegate_task_blocked_tools.rs`~~ → handled inline in Plan 02 Task 1 (delegate_task.rs lib tests mod, both copies)
- ~~`crates/ironhermes-tools/tests/delegate_task_oversize.rs`~~ → handled inline in Plan 02 Task 1 (replaces existing `test_batch_truncates_to_max_subagents` in delegate_task.rs)

No external Wave 0 file scaffolding is required for this phase.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `/agents` TUI tree view indentation in real ratatui surface | D-12 (tui_rata) | Requires running TUI interactively; ratatui rendering can't be unit-tested at pixel level | Launch `cargo run --bin ironhermes -- tui` (or current entrypoint), spawn a delegated task with `role=orchestrator`, type `/agents`, verify indented ASCII tree with 8-char id prefix + `[running]` pill |
| Startup deprecation exit(1) for `subagent:` key | D-07 | Requires config file on disk + process-exit observation | Create `~/.ironhermes/config.yaml` with literal `subagent:` key, run `ironhermes`, verify stderr contains the deprecation message AND exit code is 1 (`echo $?`) |

> The pure-helper `detect_legacy_subagent_key` IS unit-tested in Plan 01 Task 1 — the manual verification above only covers the `process::exit(1)` call site itself (the part that cannot run in a test binary).

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — 10/10 tasks carry `<automated>` blocks
- [x] Sampling continuity: no 3 consecutive tasks without automated verify — every task has automated verify
- [x] Wave 0 covers all MISSING references — no MISSING markers; Wave 0 work folded into per-task TDD
- [x] No watch-mode flags — all verify commands are one-shot
- [x] Feedback latency < 30s — quick command targets ~30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved (revision iteration 1)
