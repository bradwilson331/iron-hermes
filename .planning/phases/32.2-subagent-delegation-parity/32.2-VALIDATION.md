---
phase: 32.2
slug: subagent-delegation-parity
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-16
---

# Phase 32.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | cargo test |
| **Config file** | Cargo.toml (workspace) |
| **Quick run command** | `cargo test -p ironhermes-tools -p ironhermes-agent -p ironagent-tools-api` |
| **Full suite command** | `cargo test --workspace` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-tools -p ironhermes-agent -p ironagent-tools-api`
- **After every plan wave:** Run `cargo test --workspace`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 32.2-01-01 | 01 | 1 | TBD | — | Config rename detected at startup | unit | `cargo test -p ironhermes-core` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/ironhermes-core/tests/config_rename.rs` — startup warning when old `subagent:` key detected
- [ ] `crates/ironhermes-tools/tests/delegate_task_blocked_tools.rs` — stubs for blocked-tool assertions (clarify, send_message)
- [ ] `crates/ironhermes-tools/tests/delegate_task_oversize.rs` — batch oversize → tool error assertion

*If none: "Existing infrastructure covers all phase requirements."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `/agents` TUI tree view indentation | D-12 | Requires running TUI interactively | Launch tui_rata, spawn a delegated task with role=orchestrator, type `/agents` and verify indented ASCII tree |
| Startup deprecation warning for `subagent:` key | D-07 | Requires config file on disk with old key | Create `~/.ironhermes/config.yaml` with `subagent:` key, run `ironhermes`, verify ⚠ warning is printed |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
