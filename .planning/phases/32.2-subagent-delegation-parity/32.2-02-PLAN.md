---
phase: 32.2-subagent-delegation-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 32.2-01
files_modified:
  - crates/ironhermes-tools/src/delegate_task.rs
  - crates/ironagent-tools-api/src/delegate_task.rs
autonomous: true
requirements:
  - DEL-05
  - DEL-06
  - DEL-08
must_haves:
  truths:
    - "`clarify` and `send_message` are silently excluded from the child registry for ALL children regardless of role"
    - "`execute_batch` returns Err (not silent truncation) when `tasks.len() > config.max_concurrent_children`, with a message naming `delegation.max_concurrent_children`"
    - "`delegate_task` JSON schema exposes a per-call `max_iterations` integer override at both single-task and per-task-in-batch levels"
    - "Per-call `max_iterations` from args overrides config; falls back to `config.max_iterations` when absent"
    - "Both crate copies (`ironhermes-tools` and `ironagent-tools-api`) carry byte-identical changes"
  artifacts:
    - path: "crates/ironhermes-tools/src/delegate_task.rs"
      provides: "Two new match arms (`clarify`, `send_message`); replaced batch-oversize block; new `effective_max_iterations` resolver in single + batch paths; schema additions"
      contains: "anyhow::bail!"
    - path: "crates/ironagent-tools-api/src/delegate_task.rs"
      provides: "Byte-identical mirror of the ironhermes-tools changes"
      contains: "anyhow::bail!"
  key_links:
    - from: "DelegateTaskTool::execute_batch (both copies)"
      to: "SubagentConfig.max_concurrent_children"
      via: "config field access"
      pattern: "self\\.config\\.max_concurrent_children"
    - from: "delegate_task schema args"
      to: "runner.run_child max_iterations parameter"
      via: "effective_max_iterations resolver"
      pattern: "effective_max_iterations"
---

<objective>
Apply three independent, additive changes to `DelegateTaskTool` in both `delegate_task.rs` copies: (1) D-05 add `clarify` and `send_message` to the structural-exclusion match in `build_child_registry`, (2) D-06 replace the silent batch-oversize truncation with an early `anyhow::bail!`, and (3) D-08 expose per-call `max_iterations` in the JSON schema at single and per-task-in-batch levels.

Purpose: These changes are independent of nested orchestration (Plan 03) and use the renamed `max_concurrent_children` field landed by Plan 01. Bundling them in one plan minimizes touches to the two parallel delegate_task.rs copies and keeps the diffs reviewable. Per RESEARCH Pitfall 2, both crate copies must be updated in lockstep.

Output: Both delegate_task.rs files compile and pass updated tests; the legacy `test_batch_truncates_to_max_subagents` is replaced by `test_batch_oversize_returns_err`; new unit tests prove clarify/send_message exclusion and max_iterations schema exposure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32.2-subagent-delegation-parity/32.2-CONTEXT.md
@.planning/phases/32.2-subagent-delegation-parity/32.2-RESEARCH.md
@.planning/phases/32.2-subagent-delegation-parity/32.2-PATTERNS.md
@.planning/phases/32.2-subagent-delegation-parity/32.2-01-SUMMARY.md

@crates/ironhermes-tools/src/delegate_task.rs
@crates/ironagent-tools-api/src/delegate_task.rs

<interfaces>
<!-- Current build_child_registry signature (delegate_task.rs ~line 368-418 in both copies) -->
pub fn build_child_registry(
    allowed_tools: &[String],
    memory_manager: Option<SharedMemoryManager>,
    child_cwd: &Path,
) -> anyhow::Result<ToolRegistry>

<!-- Plan 02 does NOT change the signature — Plan 03 does. Plan 02 only adds two new match arms. -->

<!-- Existing exclusion arms (delegate_task.rs lines ~376-381) -->
match tool_name.as_str() {
    "delegate_task" => { /* AGENT-05 */ }
    "skills" => { /* D-05 */ }
    "execute_code" => { /* not available */ }
    "cronjob" => { /* not available */ }
    // ADD: "clarify" => { /* no clarify_callback channel */ }
    // ADD: "send_message" => { /* no platform session */ }
    ...
}

<!-- Existing batch oversize block (delegate_task.rs ~lines 170-179) being replaced -->
let max_batch = self.config.max_subagents;   // Plan 01 already renamed this field
let tasks: Vec<_> = tasks_array.iter().take(max_batch).collect();
if tasks_array.len() > max_batch {
    tracing::warn!("Batch truncated from {} to {} tasks (max_subagents limit)", ...);
}

<!-- Existing per-call timeout_seconds resolver (delegate_task.rs lines 628-633) — pattern to mirror for max_iterations -->
let effective_timeout_secs = args
    .get("timeout_seconds")
    .and_then(|v| v.as_u64())
    .unwrap_or(self.config.child_timeout_seconds);  // Plan 01 renamed timeout_secs

<!-- Same per-task resolver appears in execute_batch ~lines 237-244 -->
let per_task_timeout_secs = task_obj.get("timeout_seconds")...unwrap_or(config.child_timeout_seconds);

<!-- Existing schema JSON timeout_seconds entry (~lines 476-479) — pattern for max_iterations entry -->
"timeout_seconds": { "type": "integer", "minimum": 1, "description": "..." }

<!-- Test to replace: test_batch_truncates_to_max_subagents at line 1372 in both copies -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add clarify + send_message exclusions and replace batch-oversize truncation with bail in both delegate_task.rs copies</name>
  <files>crates/ironhermes-tools/src/delegate_task.rs, crates/ironagent-tools-api/src/delegate_task.rs</files>
  <read_first>
    - crates/ironhermes-tools/src/delegate_task.rs (full file — covers build_child_registry at ~368, execute_batch at ~150-220, existing tests at 700-1100, test_batch_truncates_to_max_subagents at 1372)
    - crates/ironagent-tools-api/src/delegate_task.rs (full file — byte-identical structure)
    - .planning/phases/32.2-subagent-delegation-parity/32.2-PATTERNS.md (sections 2 and 3 — exact code patterns)
    - .planning/phases/32.2-subagent-delegation-parity/32.2-CONTEXT.md (D-05, D-06)
    - .planning/phases/32.2-subagent-delegation-parity/32.2-01-SUMMARY.md (confirms renamed field names in scope)
  </read_first>
  <behavior>
    - D-05 match arms: add `"clarify" => { /* no clarify_callback channel in children (Python: NEVER_PARALLEL_TOOLS) */ }` and `"send_message" => { /* no platform session in children (Python: toolsets.py) */ }` AFTER the existing `"cronjob"` arm and BEFORE the first tool-registration arm in both copies.
    - D-06 oversize: replace the `take(max_batch) + tracing::warn!` block in `execute_batch` with `anyhow::bail!("Batch size {} exceeds max_concurrent_children limit of {}. Increase delegation.max_concurrent_children in config.yaml to allow larger batches.", tasks_array.len(), max_batch);`. Move the `let tasks: Vec<_> = tasks_array.iter().collect();` line outside the if-block (since no truncation now). The bail must fire BEFORE any tokio task spawn.
    - The doc comment at line 159 (`/// Tasks are truncated to config.max_subagents (D-06), spawned as tokio tasks`) is rewritten to reflect the new behavior: `/// When tasks.len() > config.max_concurrent_children, returns an Err immediately (D-06 Phase 32.2). Otherwise spawns all tasks as parallel tokio tasks ...`.
    - Existing test `test_batch_truncates_to_max_subagents` (line 1372 in both copies) is renamed to `test_batch_oversize_returns_err` and its body rewritten: assert `result.is_err()`, assert error message contains `"max_concurrent_children"` and `"delegation.max_concurrent_children"`.
    - New tests in both copies: `test_build_child_registry_strips_clarify` (provides `["clarify"]`, asserts the resulting registry does NOT register a `clarify` tool), `test_build_child_registry_strips_send_message` (parallel test for send_message). Reuse the existing `MockRunner` test fixture pattern from lines 828-843.
  </behavior>
  <action>
    Edit both `crates/ironhermes-tools/src/delegate_task.rs` and `crates/ironagent-tools-api/src/delegate_task.rs` with the same diff. In `build_child_registry`'s match block: insert two new arms (`"clarify"`, `"send_message"`) immediately after the `"cronjob"` arm, each as a silent no-op `{ }` with the explanatory comment specified in `<behavior>`. In `execute_batch`: replace the truncation block (lines ~170-179) with a single `anyhow::bail!(...)` statement matching the message text specified in `<behavior>`; remove `tracing::warn!` from this path and the `.take(max_batch)` chain; promote the `let tasks: Vec<_> = tasks_array.iter().collect();` to run unconditionally. Update the doc comment above `execute_batch` to describe the new D-06 contract. Replace the test body of `test_batch_truncates_to_max_subagents` with the new `test_batch_oversize_returns_err` shape from PATTERNS.md section 3 (rename the function as well). Add the two new `test_build_child_registry_strips_*` unit tests by following the existing exclusion test pattern at `test_build_child_registry_strips_skills` (line 738 in tools, line 738 in the api copy). Use the same `MockRunner` and `Path::new("/tmp")` fixture. Both copies MUST receive byte-identical changes (modulo the file path); after editing, run `diff` between the build_child_registry function bodies and the execute_batch oversize blocks to confirm parity.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-tools --lib delegate_task:: 2>&1 | tail -30 && cargo test -p ironagent-tools-api --lib delegate_task:: 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ironhermes-tools --lib delegate_task::tests::test_build_child_registry_strips_clarify 2>&1` exits 0
    - `cargo test -p ironhermes-tools --lib delegate_task::tests::test_build_child_registry_strips_send_message 2>&1` exits 0
    - `cargo test -p ironhermes-tools --lib delegate_task::tests::test_batch_oversize_returns_err 2>&1` exits 0
    - `cargo test -p ironagent-tools-api --lib delegate_task::tests::test_build_child_registry_strips_clarify 2>&1` exits 0
    - `cargo test -p ironagent-tools-api --lib delegate_task::tests::test_build_child_registry_strips_send_message 2>&1` exits 0
    - `cargo test -p ironagent-tools-api --lib delegate_task::tests::test_batch_oversize_returns_err 2>&1` exits 0
    - `grep -c "test_batch_truncates_to_max_subagents" crates/ironhermes-tools/src/delegate_task.rs` equals 0
    - `grep -c "test_batch_truncates_to_max_subagents" crates/ironagent-tools-api/src/delegate_task.rs` equals 0
    - `grep -c '"clarify" =>' crates/ironhermes-tools/src/delegate_task.rs` equals 1
    - `grep -c '"send_message" =>' crates/ironhermes-tools/src/delegate_task.rs` equals 1
    - `grep -c '"clarify" =>' crates/ironagent-tools-api/src/delegate_task.rs` equals 1
    - `grep -c '"send_message" =>' crates/ironagent-tools-api/src/delegate_task.rs` equals 1
    - `grep -c "tasks_array.iter().take(" crates/ironhermes-tools/src/delegate_task.rs` equals 0
    - `grep -c "tasks_array.iter().take(" crates/ironagent-tools-api/src/delegate_task.rs` equals 0
    - `grep -c "anyhow::bail!" crates/ironhermes-tools/src/delegate_task.rs` is greater than or equal to 2 (existing Unknown tool bail + new oversize bail)
  </acceptance_criteria>
  <done>Both copies of delegate_task.rs structurally exclude `clarify` and `send_message`, return Err on batch oversize, and the test `test_batch_oversize_returns_err` (and the two new strip tests) pass on both crates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose per-call max_iterations in the delegate_task JSON schema (single + batch) and wire effective_max_iterations into both copies</name>
  <files>crates/ironhermes-tools/src/delegate_task.rs, crates/ironagent-tools-api/src/delegate_task.rs</files>
  <read_first>
    - crates/ironhermes-tools/src/delegate_task.rs (full file — covers schema() at ~440-520, execute() effective_timeout_secs resolver at lines 628-633, execute_batch per-task resolver at 237-244, run_child call sites at 295 and 645)
    - crates/ironagent-tools-api/src/delegate_task.rs (same structure)
    - .planning/phases/32.2-subagent-delegation-parity/32.2-PATTERNS.md (section 4 — exact resolver and schema patterns)
    - .planning/phases/32.2-subagent-delegation-parity/32.2-CONTEXT.md (D-08)
  </read_first>
  <behavior>
    - Schema additions in both copies: in the `schema()` method's JSON blob, add a top-level `"max_iterations"` property (type integer, minimum 1, description: "Per-call max LLM iterations override. Defaults to delegation.max_iterations from config.") at the same nesting level as `"timeout_seconds"`. In the `tasks[].items.properties` blob, add the same `"max_iterations"` property with description "Per-task max iterations override. Defaults to delegation.max_iterations from config."
    - Single-task execute path: add `let effective_max_iterations = args.get("max_iterations").and_then(|v| v.as_u64()).map(|v| v as usize).unwrap_or(self.config.max_iterations);` immediately after the existing `effective_timeout_secs` resolver at ~line 633. Replace the `max_iterations` argument in the subsequent `runner.run_child(...)` call (line 645) with `effective_max_iterations`.
    - Batch execute path: inside the per-task loop in `execute_batch`, add the parallel resolver `let per_task_max_iterations = task_obj.get("max_iterations").and_then(|v| v.as_u64()).map(|v| v as usize).unwrap_or(config.max_iterations);` next to the existing `per_task_timeout_secs` resolver. Replace `config.max_iterations` in the `run_child(...)` call (~line 295) with `per_task_max_iterations`.
    - New tests in both copies: `test_delegate_task_schema_has_max_iterations` — invokes `DelegateTaskTool::schema()`, parses the returned JSON, asserts `schema["function"]["parameters"]["properties"]["max_iterations"]["type"] == "integer"` AND asserts the same property exists inside `tasks.items.properties.max_iterations`. `test_per_call_max_iterations_overrides_config` — constructs a DelegateTaskTool with `config.max_iterations = 99`, calls execute with `{"task": "...", "max_iterations": 3}`, captures the value forwarded to `runner.run_child` via a capture-runner (extend the existing `PromptCapture` pattern at line 1176 to also record `max_iterations`), and asserts the captured value equals 3 (not 99).
  </behavior>
  <action>
    Edit both delegate_task.rs copies to add the schema field and the resolver in both call paths. In the `schema()` JSON: add the `max_iterations` property to the single-task properties block and to the `tasks.items.properties` block. In `execute()`: introduce `effective_max_iterations` right after `effective_timeout_secs` and use it in the `run_child(...)` call instead of `self.config.max_iterations`. In `execute_batch`: introduce `per_task_max_iterations` next to `per_task_timeout_secs` and use it in the `run_child(...)` call. Add the two new unit tests `test_delegate_task_schema_has_max_iterations` and `test_per_call_max_iterations_overrides_config` to both files. For the override test, extend the existing `PromptCapture` test struct to add a `last_max_iterations: Arc<Mutex<Option<usize>>>` field (mirror the existing capture pattern) and assert on it after `execute()` returns. Keep the diff byte-identical between the two copies.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-tools --lib delegate_task::tests::test_delegate_task_schema_has_max_iterations 2>&1 | tail -20 && cargo test -p ironhermes-tools --lib delegate_task::tests::test_per_call_max_iterations_overrides_config 2>&1 | tail -20 && cargo test -p ironagent-tools-api --lib delegate_task::tests::test_delegate_task_schema_has_max_iterations 2>&1 | tail -20 && cargo test -p ironagent-tools-api --lib delegate_task::tests::test_per_call_max_iterations_overrides_config 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ironhermes-tools --lib delegate_task::tests::test_delegate_task_schema_has_max_iterations 2>&1` exits 0
    - `cargo test -p ironhermes-tools --lib delegate_task::tests::test_per_call_max_iterations_overrides_config 2>&1` exits 0
    - `cargo test -p ironagent-tools-api --lib delegate_task::tests::test_delegate_task_schema_has_max_iterations 2>&1` exits 0
    - `cargo test -p ironagent-tools-api --lib delegate_task::tests::test_per_call_max_iterations_overrides_config 2>&1` exits 0
    - `grep -c "effective_max_iterations" crates/ironhermes-tools/src/delegate_task.rs` is greater than or equal to 2 (declaration + usage)
    - `grep -c "per_task_max_iterations" crates/ironhermes-tools/src/delegate_task.rs` is greater than or equal to 2
    - `grep -c "effective_max_iterations" crates/ironagent-tools-api/src/delegate_task.rs` is greater than or equal to 2
    - `grep -c "per_task_max_iterations" crates/ironagent-tools-api/src/delegate_task.rs` is greater than or equal to 2
    - `grep -c '"max_iterations":' crates/ironhermes-tools/src/delegate_task.rs` is greater than or equal to 2 (top-level + tasks.items)
    - `grep -c '"max_iterations":' crates/ironagent-tools-api/src/delegate_task.rs` is greater than or equal to 2
    - All existing delegate_task tests in both crates still pass: `cargo test -p ironhermes-tools --lib delegate_task:: 2>&1 | grep -c "test result: ok"` is greater than or equal to 1
  </acceptance_criteria>
  <done>Both delegate_task.rs copies expose `max_iterations` in the schema at single and per-task levels, the resolver threading wires args → run_child, the override test captures the per-call value, and all existing tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → DelegateTaskTool args | Untrusted JSON crosses the boundary; new fields (`max_iterations`, batch oversize behavior) must validate types and respect config ceilings |
| build_child_registry tool_name iteration | Match must be exhaustive for known tools; unknown tools already bail (line 414) |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-32.2-04 | Elevation of Privilege | medium | clarify / send_message available to children | mitigate | D-05 structurally excludes both tools in `build_child_registry` for ALL children. Verified by `test_build_child_registry_strips_clarify` and `test_build_child_registry_strips_send_message` on both crate copies. |
| T-32.2-05 | Denial of Service | medium | Batch oversize via large tasks array | mitigate | D-06 returns Err immediately before any spawn — no tokio tasks created when oversized. Test `test_batch_oversize_returns_err` asserts error path on both copies. |
| T-32.2-06 | Resource Exhaustion | low | max_iterations per-call escalation | mitigate | Per-call override is bounded only by the LLM's argument; no upper cap is added in this plan because the existing iteration-budget pressure (PROV-09) already throttles runaway iterations. Documented in schema description. |
| T-32.2-07 | Information Disclosure | low | Error message contains config key name | accept | `delegation.max_concurrent_children` is a documented config key, not a secret — surfacing it in the error is the intended behavior to guide users. |
</threat_model>

<verification>
After both tasks complete:
- `cargo test -p ironhermes-tools --lib delegate_task 2>&1` exits 0
- `cargo test -p ironagent-tools-api --lib delegate_task 2>&1` exits 0
- The two crates have byte-identical match arms for `"clarify"` and `"send_message"` (verify via `diff <(grep -A1 '"clarify" =>' crates/ironhermes-tools/src/delegate_task.rs) <(grep -A1 '"clarify" =>' crates/ironagent-tools-api/src/delegate_task.rs)` returning no output)
- No `tracing::warn!` related to batch truncation remains in either copy
</verification>

<success_criteria>
- `clarify` and `send_message` excluded from child registries in both crate copies
- Batch oversize returns Err (with config key name in message) in both copies
- `max_iterations` exposed in JSON schema at single + per-task levels in both copies
- `effective_max_iterations` / `per_task_max_iterations` resolvers wire args → run_child in both copies
- `test_batch_truncates_to_max_subagents` no longer exists in either copy
- All new tests pass on both crates
</success_criteria>

<output>
Create `.planning/phases/32.2-subagent-delegation-parity/32.2-02-SUMMARY.md` when done. Include: the exact text of the oversize bail message, the exact JSON schema description strings used for `max_iterations`, confirmation that both crate copies received byte-identical diffs (with a diff command output snippet).
</output>
