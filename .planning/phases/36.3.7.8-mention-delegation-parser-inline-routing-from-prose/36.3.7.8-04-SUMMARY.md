---
phase: "36.3.7.8"
plan: "04"
subsystem: ironhermes-cli/kanban
tags:
  - kanban
  - cli
  - mention
  - clap-derive
  - deferred-subverb
  - cli-parity-tests

dependency_graph:
  requires:
    - "crates/ironhermes-kanban/src/mention/parser.rs::parse_mentions (Plan 01)"
    - "crates/ironhermes-kanban/src/mention/resolver.rs::resolve_mention + FallbackPolicy + ResolverCtx (Plan 01)"
    - "crates/ironhermes-kanban/src/store.rs::create_mention_children + MentionPlan + MentionChildSpec (Plan 02)"
    - "crates/ironhermes-cli/src/kanban/commands.rs::open_store + profile_from_env (existing)"
  provides:
    - "crates/ironhermes-cli/src/kanban/mod.rs::KanbanCommands::Mention variant"
    - "crates/ironhermes-cli/src/kanban/mod.rs::handle_kanban_command::KanbanCommands::Mention arm"
    - "crates/ironhermes-cli/src/kanban/commands.rs::cmd_mention async fn"
    - "crates/ironhermes-cli/tests/mention_cli.rs (4 CLI parity tests)"
    - "crates/ironhermes-core/src/commands/handlers.rs::DEFERRED_KANBAN_SUBVERBS[\"mention\"]"
  affects:
    - "gateway /kanban mention routes to CLI-redirect message via DEFERRED_KANBAN_SUBVERBS"

tech_stack:
  added: []
  patterns:
    - "Inline parser+resolver+store path (NOT via LLM tool registry — PATTERNS.md Landmine 7)"
    - "SELECT DISTINCT assignee FROM tasks via params![] for known-assignees HashSet"
    - "parent.skills JSON decode (Option<String> → Option<Vec<String>>) before MentionPlan construction"
    - "FallbackPolicy::Error + SkipReason::UnknownHandle combined check for hard-error bail"
    - "clap-derive match+panic!() test pattern (no {:?} on KanbanCommands — Landmine 7)"

key_files:
  created:
    - "crates/ironhermes-cli/tests/mention_cli.rs"
  modified:
    - "crates/ironhermes-cli/src/kanban/mod.rs"
    - "crates/ironhermes-cli/src/kanban/commands.rs"
    - "crates/ironhermes-core/src/commands/handlers.rs"

decisions:
  - "cmd_mention runs parser+resolver INLINE (not via LLM tool registry) per PATTERNS.md Landmine 7 — CLI is a separate surface from the LLM tool path"
  - "Known-assignees HashSet built via inline SELECT DISTINCT (no list_known_assignees method on store yet) using params![] for SQL safety"
  - "parent.skills decoded from JSON string (Option<String>) to Option<Vec<String>> because Task struct stores skills as JSON-encoded string"
  - "Temporary todo!() stub used in Task 1 to allow cargo build to pass before Task 2 implemented the real function — stub removed in Task 2 commit"

metrics:
  duration: "~7 minutes"
  completed: "2026-05-31"
  tasks_completed: 2
  files_created: 1
  files_modified: 3
---

# Phase 36.3.7.8 Plan 04: CLI Parity Surface for @mention Delegation Summary

**One-liner:** `KanbanCommands::Mention` clap variant with inline parser+resolver+store dispatch, 4 CLI parity tests, and `"mention"` in `DEFERRED_KANBAN_SUBVERBS` for gateway routing.

## What Was Built

### Task 1 — KanbanCommands::Mention variant + dispatch arm + deferred-subverb entry (`96cebd89`)

Three coordinated additive edits:

**`crates/ironhermes-cli/src/kanban/mod.rs`:**
- Added `Mention { task_id: Option<String>, fallback_policy: String, idempotency_key: Option<String>, body_override: Option<String>, json: bool }` variant with `#[command(name = "mention")]` and `#[arg(long, default_value = "skip")]` on `fallback_policy`
- Added dispatch arm in `handle_kanban_command` routing `KanbanCommands::Mention { ... }` to `commands::cmd_mention(...).await`
- Phase comment: `// Phase 36.3.7.8 — kanban_mention (@mention delegation parser, inline routing).`

**`crates/ironhermes-core/src/commands/handlers.rs`:**
- Appended `"mention",` to `DEFERRED_KANBAN_SUBVERBS` after `"swarm"` (additive only — zero deleted lines)

**`crates/ironhermes-cli/src/kanban/commands.rs`:**
- Added temporary `todo!()` stub for `cmd_mention` with matching signature to allow Task 1 to compile cleanly

### Task 2 — cmd_mention handler + 4 CLI parity tests (`c675fdd8`)

**`crates/ironhermes-cli/src/kanban/commands.rs`** — replaced todo!() stub with full implementation:

1. `task_id` resolution from arg or `$HERMES_KANBAN_TASK` env
2. `open_store()?` — sync handle, single-process CLI
3. `store.get_task(&task_id)` — fetch parent card
4. `body_override.unwrap_or_else(|| parent.body.clone().unwrap_or_default())` — body selection
5. `parse_mentions(&body)` — pure-function parser (no IO)
6. `SELECT DISTINCT assignee FROM tasks` via `params![]` — builds `HashSet<String>` of known assignees
7. `fallback_policy.parse::<FallbackPolicy>()` — validated enum parse
8. `ResolverCtx { parent_task_id, parent_assignee, known_assignees: &known, fallback_policy: policy }` + per-span `resolve_mention` loop
9. `FallbackPolicy::Error + SkipReason::UnknownHandle` combined check → `bail!("unknown_handle: ...")`
10. `parent.skills` decoded from JSON `Option<String>` → `Option<Vec<String>>`
11. `MentionPlan` construction + `store.create_mention_children(plan)?`
12. JSON (`serde_json::to_string_pretty`) or human (`"Created N mention children for {task_id}"`) output

**`crates/ironhermes-cli/tests/mention_cli.rs`** (150 lines, 4 tests):

| Test | Verifies |
|------|----------|
| `mention_verb_parses_minimal` | Positional task_id + default fallback_policy="skip" |
| `mention_verb_parses_fallback_policy_flag` | `--fallback-policy pending` overrides default |
| `mention_verb_parses_idempotency_key_flag` | `--idempotency-key my-key` captured correctly |
| `mention_verb_parses_dispatch_happy_path` | All flags together — no clap conflict errors |

All tests use `match` + `panic!("expected Kanban::Mention variant")` (no `{:?}` on `KanbanCommands` — PATTERNS Landmine 7).

## Verification Results

```
cargo build -p ironhermes-cli -p ironhermes-core    → 0 errors (Finished)
cargo test -p ironhermes-cli --test mention_cli     → 4 passed, 0 failed

grep -c 'Mention {' mod.rs                          → 2 (variant + dispatch)
grep -c 'KanbanCommands::Mention' mod.rs            → 1
grep -c '#[command(name = "mention")]' mod.rs       → 1
grep -c '"mention",' handlers.rs                    → 1
git diff handlers.rs | grep '^-[^-]' | wc -l       → 0 (additive only)
grep -c 'pub async fn cmd_mention' commands.rs      → 1
grep -c 'fn mention_verb_parses' mention_cli.rs     → 4
grep -c 'todo!()' commands.rs                       → 0
grep -c '{:?}' mention_cli.rs                       → 0
```

## Deviations from Plan

### Auto-applied deviations

**1. [Rule 1 - Bug] parent.skills type mismatch**
- **Found during:** Task 2 implementation
- **Issue:** `MentionPlan.skills: Option<Vec<String>>` but `Task.skills: Option<String>` (JSON-encoded). Directly assigning `parent.skills` to `plan.skills` would be a type error.
- **Fix:** Added `serde_json::from_str::<Vec<String>>(s).ok()` decode step before constructing MentionPlan. Matches the pattern used elsewhere (skills are stored as JSON-encoded strings in the DB).
- **Files modified:** `crates/ironhermes-cli/src/kanban/commands.rs`
- **Commit:** `c675fdd8`

**2. [Rule 2 — Auto-add] profile_from_env() returns String not Option<String>**
- **Found during:** Task 2 — plan outline said `created_by: profile_from_env()` and `MentionPlan.created_by: Option<String>`
- **Issue:** In `commands.rs`, `profile_from_env()` returns `String` (not `Option<String>`) with "operator" fallback. The plan's body outline showed `created_by: profile_from_env()` without wrapping in `Some(...)`.
- **Fix:** Wrapped in `Some(profile_from_env())` to satisfy `MentionPlan.created_by: Option<String>`. This matches Plan 02 (store layer) semantics exactly.
- **Files modified:** `crates/ironhermes-cli/src/kanban/commands.rs`
- **Commit:** `c675fdd8`

No other deviations — plan executed as written.

## Known Stubs

None — all public functions are fully implemented and the 4 CLI parity tests pass. Plan 05 receiver tests will exercise `cmd_mention` end-to-end via `tools_smoke.rs`.

## Threat Flags

No new network endpoints, auth paths, file access patterns, or schema changes. All DB operations confirmed to use `params![]` (T-36.3.7.8-04-02 mitigated). `profile_from_env()` returns `Some(...)` for `created_by` preserving audit trail (T-36.3.7.8-04-05 mitigated).

## Self-Check: PASSED

| Item | Result |
|------|--------|
| `crates/ironhermes-cli/src/kanban/mod.rs` (Mention variant + dispatch) | FOUND |
| `crates/ironhermes-cli/src/kanban/commands.rs` (cmd_mention) | FOUND |
| `crates/ironhermes-cli/tests/mention_cli.rs` (4 tests) | FOUND |
| `crates/ironhermes-core/src/commands/handlers.rs` ("mention" in DEFERRED) | FOUND |
| Commit `96cebd89` (Task 1) | FOUND |
| Commit `c675fdd8` (Task 2) | FOUND |
| `cargo test -p ironhermes-cli --test mention_cli` | 4 passed, 0 failed |
