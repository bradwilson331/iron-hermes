---
phase: "36.3.7.8"
plan: "05"
type: execute
wave: 3
depends_on:
  - "36.3.7.8-01"
  - "36.3.7.8-02"
  - "36.3.7.8-03"
  - "36.3.7.8-04"
files_modified:
  - crates/ironhermes-kanban/tests/tools_smoke.rs
  - crates/ironhermes-core/tests/deferred_subverbs.rs
  - docs/kanban/reference.md
  - .planning/REQUIREMENTS.md
autonomous: true
nyquist_compliant: true
wave_0_complete: false
requirements:
  - REQ-36.3.7.8-01
  - REQ-36.3.7.8-02
  - REQ-36.3.7.8-03
  - REQ-36.3.7.8-04
  - REQ-36.3.7.8-05
  - REQ-36.3.7.8-06
  - REQ-36.3.7.8-08
  - REQ-36.3.7.8-09
  - REQ-36.3.7.8-10
  - REQ-36.3.7.8-11
  - REQ-36.3.7.8-12
  - REQ-36.3.7.8-13
  - REQ-36.3.7.8-15
  - REQ-36.3.7.8-16
tags:
  - kanban
  - mention
  - receiver-tests
  - tools-smoke
  - docs-reconciliation
  - requirements-anchor

must_haves:
  truths:
    - "`crates/ironhermes-kanban/tests/tools_smoke.rs` has a new `// kanban_mention tests` section with ≥ 15 receiver-end tests covering REQ-01..REQ-12 plus REQ-13 aggregate."
    - "Self-mention (REQ-09 layer 1 + layer 2) and A→B→A 4-hop cycle (REQ-10) are exercised by named integration tests inside `tools_smoke.rs`."
    - "Atomic rollback on per-child failure (REQ-11) is verified by an assignee-validation-fail test that asserts zero rows in tasks/task_links/task_events after the failed call."
    - "Idempotency replay (REQ-12) verified by a test that re-invokes `create_mention_children` with the same key and asserts identical `MentionResult.children` and no new row count."
    - "`crates/ironhermes-core/tests/deferred_subverbs.rs` exists (or is extended) with a test that asserts `\"mention\"` is in `DEFERRED_KANBAN_SUBVERBS` (REQ-08)."
    - "`docs/kanban/reference.md` §14 v1-NOTE narrative bumps `\"10 LLM tools\" → \"11 LLM tools\"` and inline-mentions `kanban_mention`; §200 table grows by one row (`kanban_mention`); §741 row `*(deferred to Phase 36.3.7.8 ...)*` is REPLACED with `*Shipped in Phase 36.3.7.8.*` (REQ-15)."
    - "REQ-16 grep gates: `grep -c 'deferred to Phase 36.3.7.8' docs/kanban/reference.md` returns 0; `grep -c 'all 11 LLM tools' docs/kanban/reference.md` returns ≥ 1; `grep -c 'kanban_mention' docs/kanban/reference.md` returns ≥ 2."
    - "`.planning/REQUIREMENTS.md` has a new section `## Phase 36.3.7.8 - @mention delegation parser` with 16 requirements REQ-36.3.7.8-01..16 anchored to author-delegated decisions D-01..D-04 from CONTEXT.md plus the registry/table additions from RESEARCH.md."
  artifacts:
    - path: "crates/ironhermes-kanban/tests/tools_smoke.rs"
      provides: "// kanban_mention tests section (≥ 15 tests covering REQ-01..REQ-13)"
      contains: "mention_happy_path_single_mention_creates_one_child, mention_inside_fenced_block_skipped, mention_self_reference_skipped, mention_invalid_assignee_rolls_back_whole_batch, mention_idempotency_key_replays_return_same_children"
      min_lines: 600
    - path: "crates/ironhermes-core/tests/deferred_subverbs.rs"
      provides: "Test asserting `mention` is in DEFERRED_KANBAN_SUBVERBS (REQ-08)"
      contains: "deferred_subverbs_includes_mention"
    - path: "docs/kanban/reference.md"
      provides: "§14, §200, §741 reconciliation: 10→11 tools, kanban_mention row, Shipped annotation"
      contains: "all 11 LLM tools, kanban_mention, Shipped in Phase 36.3.7.8"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-36.3.7.8-01..16 anchored to D-01..D-04 + RESEARCH §Validation Architecture"
      contains: "REQ-36.3.7.8-01, REQ-36.3.7.8-16"
  key_links:
    - from: "crates/ironhermes-kanban/tests/tools_smoke.rs"
      to: "crates/ironhermes-kanban/src/store.rs::create_mention_children"
      via: "store.create_mention_children(plan).unwrap()  (in test bodies)"
      pattern: "create_mention_children\\("
    - from: "docs/kanban/reference.md §200"
      to: "kanban_mention tool registration in src/tools/mod.rs"
      via: "table row referencing the 11th tool"
      pattern: "kanban_mention"
    - from: ".planning/REQUIREMENTS.md REQ-36.3.7.8-XX"
      to: "CONTEXT.md decisions D-01..D-04 + RESEARCH.md §Validation Architecture"
      via: "anchor lines `Anchors: D-XX, ...`"
      pattern: "REQ-36.3.7.8-"
---

<objective>
Close the phase: receiver-end test coverage in `tools_smoke.rs` (≥ 15 integration tests over the kanban_mention surface), a `DEFERRED_KANBAN_SUBVERBS` membership test in `ironhermes-core`, three line-group edits to `docs/kanban/reference.md` that reconcile §14 / §200 / §741, and a new `## Phase 36.3.7.8` section in `.planning/REQUIREMENTS.md` with all 16 REQs anchored to author-delegated D-01..D-04 decisions.

Purpose: closes REQ-13 (≥ 15 receiver tests), REQ-15 (docs reconciliation), REQ-16 (grep gates), REQ-08 receiver portion, and aggregates verification of REQ-01..REQ-12 from earlier plans. Locks the phase REQ surface into `.planning/REQUIREMENTS.md` so downstream verifier / closer tooling sees the canonical IDs.

Output: ≥ 600 LOC added to `crates/ironhermes-kanban/tests/tools_smoke.rs`; new (or extended) `crates/ironhermes-core/tests/deferred_subverbs.rs`; 3 line-group edits to `docs/kanban/reference.md`; new section in `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36.3.7.8-mention-delegation-parser-inline-routing-from-prose/36.3.7.8-CONTEXT.md
@.planning/phases/36.3.7.8-mention-delegation-parser-inline-routing-from-prose/36.3.7.8-RESEARCH.md
@.planning/phases/36.3.7.8-mention-delegation-parser-inline-routing-from-prose/36.3.7.8-PATTERNS.md
@.planning/phases/36.3.7.8-mention-delegation-parser-inline-routing-from-prose/36.3.7.8-VALIDATION.md
@.planning/phases/36.3.7.8-mention-delegation-parser-inline-routing-from-prose/36.3.7.8-01-SUMMARY.md
@.planning/phases/36.3.7.8-mention-delegation-parser-inline-routing-from-prose/36.3.7.8-02-SUMMARY.md
@.planning/phases/36.3.7.8-mention-delegation-parser-inline-routing-from-prose/36.3.7.8-03-SUMMARY.md
@.planning/phases/36.3.7.8-mention-delegation-parser-inline-routing-from-prose/36.3.7.8-04-SUMMARY.md
@.planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-01-SUMMARY.md
@docs/kanban/reference.md
@CLAUDE.md

<interfaces>
<!-- Pattern lifted from PATTERNS.md tools_smoke.rs section + VALIDATION.md REQ-13 test names + sibling phase 36.3.7.7. -->

CANONICAL ANALOG: crates/ironhermes-kanban/tests/tools_smoke.rs lines 874-1425 (`// kanban_swarm tests` section)

NEW IMPORTS (mirror existing `use` block at the top of tools_smoke.rs):

  use ironhermes_kanban::tools::{
      KanbanBlockTool, KanbanCommentTool, KanbanCompleteTool, KanbanCreateTool, KanbanHeartbeatTool,
      KanbanLinkTool, KanbanListTool, KanbanMentionTool,    // <-- new
      KanbanShowTool, KanbanSwarmTool, KanbanUnblockTool,
  };
  use ironhermes_kanban::store::{MentionChildSpec, MentionPlan, MentionResult};
  use ironhermes_kanban::mention::{FallbackPolicy, MAX_MENTION_CHAIN_DEPTH};

SECTION HEADER (mirror swarm-section header at lines 874-881):

  // kanban_mention tests (Phase 36.3.7.8 — @mention delegation parser)
  // ---------------------------------------------------------------------------
  //
  // Receiver-end tests for the new KanbanStore::create_mention_children sibling
  // primitive + the KanbanMentionTool LLM tool registered as the 11th tool.
  // Most tests drive store.create_mention_children(plan) directly to assert
  // DB-level shape (rows in tasks/task_links/task_events) and atomic rollback.

REUSED HELPERS (do NOT redefine):
  - fn make_store() -> Arc<TokioMutex<KanbanStore>>   (lines 42-48)
  - static ENV_LOCK: std::sync::Mutex<()> = ...        (line 36-ish)

NEW FIXTURE HELPER (per PATTERNS.md tools_smoke.rs section):

  /// Seeds a parent task with the given body and assignee; returns (task_id, assignee).
  fn seed_mention_parent(s: &mut KanbanStore, body: &str, assignee: &str) -> (String, String) {
      let opts = CreateTaskOptions { body: Some(body.to_string()), ..Default::default() };
      let t = s.create_task("parent task", assignee, opts).unwrap();
      (t.id, assignee.to_string())
  }

15 REQUIRED TEST NAMES (per VALIDATION.md REQ-13 + PATTERNS.md tools_smoke.rs section):

   1. mention_happy_path_single_mention_creates_one_child
   2. mention_multi_mention_body_creates_n_children
   3. mention_inside_fenced_block_skipped
   4. mention_inside_inline_code_skipped
   5. mention_inside_html_comment_skipped
   6. mention_mixed_prose_and_fence_only_prose_extracted
   7. mention_unknown_handle_skip_policy
   8. mention_unknown_handle_pending_policy
   9. mention_unknown_handle_error_policy
  10. mention_self_reference_skipped
  11. mention_ancestor_cycle_skipped              (REQ-10 layer 2 — chain depth 4 trigger)
  12. mention_idempotency_key_replays_return_same_children
  13. mention_invalid_task_id_rejected            (tool-level smoke via .execute)
  14. mention_task_id_defaults_to_hermes_kanban_task_env
  15. mention_case_insensitive_handle_resolution
  16. (BONUS) mention_invalid_assignee_rolls_back_whole_batch   (REQ-11)
  17. (BONUS) mention_registered_eleventh_tool                   (REQ-06 tool-count assertion)

  Total ≥ 15 — bonuses are encouraged to bump coverage but not strictly required.

ENV_LOCK USAGE (mirror swarm tests):
  Any test that sets HERMES_KANBAN_TASK or HERMES_PROFILE MUST take ENV_LOCK:
    let _env_guard = ENV_LOCK.lock().unwrap_or_else(|e| e.into_inner());
    std::env::set_var("HERMES_KANBAN_TASK", &parent_id);
    // ... test body ...
    std::env::remove_var("HERMES_KANBAN_TASK");

DB-LEVEL ASSERTIONS (model from swarm test at lines 948-954):

  // Count rows for atomic-rollback verification:
  let task_count: i64 = s.conn.query_row(
      "SELECT count(*) FROM tasks WHERE id LIKE 't_%'",
      params![], |r| r.get(0),
  ).unwrap();
  let link_count: i64 = s.conn.query_row(
      "SELECT count(*) FROM task_links WHERE parent_id = ?1",
      params![&parent_id], |r| r.get(0),
  ).unwrap();
  let event_count: i64 = s.conn.query_row(
      "SELECT count(*) FROM task_events WHERE task_id IN (SELECT child_id FROM task_links WHERE parent_id = ?1)",
      params![&parent_id], |r| r.get(0),
  ).unwrap();

ANCESTOR-CHAIN CYCLE TEST SETUP (REQ-10 layer 2 — depth 4):

  // Construct chain: t1(alice) → t2(bob) → t3(alice) → t4(bob)
  // Then call create_mention_children on t4 with @alice — should be Skipped(Cycle)
  // because alice appears in the ancestor chain within 4 hops.
  //
  // Use s.create_task + s.insert_link directly to build the chain;
  // then call s.create_mention_children with a child whose assignee == "alice".

REQ-08 RECEIVER TEST (new file or extension):

  Path:  crates/ironhermes-core/tests/deferred_subverbs.rs
  Test:  fn deferred_subverbs_includes_mention()
  Body:  Assert that the public constant or accessor for DEFERRED_KANBAN_SUBVERBS includes "mention".
         If the constant is private, expose it via a `pub(crate) fn` test helper or test via
         the behavior — invoke the dispatch with subverb="mention" and assert the deferred-CLI
         message is produced (preferred for stability if the helper doesn't exist).

DOCS RECONCILIATION (REQ-15 — three edits to docs/kanban/reference.md):

  §14 V1-NOTE narrative bump:
    Before:  "all 10 LLM tools"            (added in 36.3.7.7)
    After:   "all 11 LLM tools"
    Also add the literal token `kanban_mention` to the inline tool list at §14.

  §200 table — add a new row for kanban_mention:
    | kanban_mention | @mention delegation parser inline routing | Phase 36.3.7.8 |
    Match the column shape of the existing table (which has rows for kanban_create,
    kanban_show, kanban_swarm, etc.). Read the surrounding rows to identify the exact
    column header layout before inserting.

  §741 P6 row — replace deferral with shipped annotation:
    Before:  "P6 @mention: inline routing from prose, e.g. `@reviewer look at this`
              *(deferred to Phase 36.3.7.8 — @mention delegation parser not implemented in v1)*"
    After:   "P6 @mention: inline routing from prose, e.g. `@reviewer look at this`
              *Shipped in Phase 36.3.7.8.*"

REQ-16 GREP GATES — final phase verifier (run from repo root):

  grep -c 'deferred to Phase 36.3.7.8' docs/kanban/reference.md       # == 0
  grep -c 'all 11 LLM tools' docs/kanban/reference.md                  # >= 1
  grep -c 'kanban_mention' docs/kanban/reference.md                    # >= 2

REQUIREMENTS.md SECTION (model from REQUIREMENTS.md:210-221 — 36.3.7.7 section):

  Add a new section `## Phase 36.3.7.8 - @mention delegation parser` containing
  exactly 16 lines of the form:

  - [ ] **REQ-36.3.7.8-01**: {behavior from VALIDATION.md row 41}. Anchors: D-02.
  - [ ] **REQ-36.3.7.8-02**: {behavior from row 42}. Anchors: D-02.
  ...
  - [ ] **REQ-36.3.7.8-16**: {grep-gate behavior from row 56}. Anchors: D-04, RESEARCH §Validation.

  Use VALIDATION.md rows 41-56 as the verbatim source of REQ wording (NOT paraphrased).
  Each REQ MUST reference at least one D-XX decision from CONTEXT.md (D-01 LLM tool + CLI;
  D-02 regex + fence machine; D-03 three-stage resolver; D-04 two-layer cycle).

LANDMINES (PATTERNS.md):
  L1 NOT APPLICABLE — this plan does not touch cmd_create or cmd_mention's parent flag.
  L2: idempotency key suffix is `:mention:{i}` — assert in the idempotency test that the
      child row's `idempotency_key` matches `format!("{base}:mention:{}", mention_index)`.
  L7: KanbanCommands NoDebug — only matters in CLI tests; tools_smoke.rs has no clap parse.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 — receiver tests): Append ≥ 15 mention tests to tools_smoke.rs + add deferred-subverb membership test</name>
  <files>
    crates/ironhermes-kanban/tests/tools_smoke.rs,
    crates/ironhermes-core/tests/deferred_subverbs.rs
  </files>
  <read_first>
    - .planning/phases/36.3.7.8-mention-delegation-parser-inline-routing-from-prose/36.3.7.8-PATTERNS.md (tools_smoke.rs section + Shared Patterns ENV_LOCK + Landmines 2, 3, 5)
    - .planning/phases/36.3.7.8-mention-delegation-parser-inline-routing-from-prose/36.3.7.8-VALIDATION.md (rows 41-56 for REQ behavior phrasing)
    - crates/ironhermes-kanban/tests/tools_smoke.rs lines 874-1425 (`// kanban_swarm tests` section — copy structure)
    - crates/ironhermes-kanban/tests/tools_smoke.rs lines 14-48 (top-level imports + make_store + ENV_LOCK helpers)
    - crates/ironhermes-kanban/src/store.rs (after Plans 01/02 land — verify the new MentionPlan/MentionChildSpec/MentionResult are exported)
    - crates/ironhermes-core/tests/* (check whether a deferred_subverbs.rs test file already exists; if so, extend it instead of creating a new one)
  </read_first>
  <behavior>
    - `cargo test -p ironhermes-kanban --test tools_smoke -- mention -- --test-threads=1` runs ≥ 15 tests, ALL pass, 0 failed.
    - `cargo test -p ironhermes-core -- deferred_subverbs_includes_mention -- --test-threads=1` passes (or, if the test name is namespaced differently because the file already exists, the named test passes).
    - REQ-11 atomic-rollback test asserts post-failure row counts in `tasks`, `task_links`, `task_events` are all unchanged from pre-call counts.
    - REQ-12 idempotency test asserts:
      (1) two invocations with the same `idempotency_key` produce identical `MentionResult.children`;
      (2) the per-child `idempotency_key` column on the first child equals `format!("{base}:mention:0")`;
      (3) total tasks-row count is unchanged between invocations.
    - REQ-10 ancestor-cycle test builds a 4-hop chain (a→b→a→b) and asserts that a 5th-hop mention of `a` from `b` is skipped (or rejected by store).
  </behavior>
  <action>
    **(a) `crates/ironhermes-kanban/tests/tools_smoke.rs` — extend with the kanban_mention section:**

    1. Add `KanbanMentionTool` to the top-level `use ironhermes_kanban::tools::{...}` block (alphabetical insertion; PATTERNS.md tools_smoke.rs section #2 references this).

    2. Add `use ironhermes_kanban::store::{MentionChildSpec, MentionPlan, MentionResult};` and `use ironhermes_kanban::mention::{FallbackPolicy, MAX_MENTION_CHAIN_DEPTH};` to the import block.

    3. Locate the END of the `// kanban_swarm tests` section (just before the next section header, or at the end of file). Append a new section header block per <interfaces> "SECTION HEADER".

    4. Define the `seed_mention_parent` helper (per <interfaces>) ONCE near the top of the new section.

    5. Implement the 15 named tests from <interfaces> "15 REQUIRED TEST NAMES". For each test, follow this structure:
       ```
       #[tokio::test]
       async fn mention_<name>() {
           let _env_guard = ENV_LOCK.lock().unwrap_or_else(|e| e.into_inner());  // if env touched
           let store = make_store();
           let mut s = store.lock().await;

           let (parent_id, parent_assignee) = seed_mention_parent(&mut s, "@reviewer body", "alice");
           // ... create known assignees if needed (a row with assignee="reviewer") ...

           let plan = MentionPlan {
               parent_id: parent_id.clone(),
               children: vec![MentionChildSpec {
                   handle: "reviewer".to_string(),
                   assignee: "reviewer".to_string(),
                   mention_index: 0,
                   title: "@reviewer mention".to_string(),
                   body: None,
               }],
               idempotency_key: None,
               workspace: None, skills: None, tenant: None,
               created_by: Some("alice".to_string()),
           };
           let result = s.create_mention_children(plan).unwrap();
           assert_eq!(result.children.len(), 1);
           // ... DB-level assertions ...
       }
       ```
       For each REQ-specific test, replace the plan/setup to exercise the targeted behavior. Use the helper patterns from <interfaces> "DB-LEVEL ASSERTIONS" for row-count checks.

    6. The REQ-13 (`mention_invalid_task_id_rejected`) and `mention_task_id_defaults_to_hermes_kanban_task_env` tests MUST exercise the `KanbanMentionTool::execute` path (not just `store.create_mention_children`) — they verify the tool layer. Use the existing per-tool `.execute(args)` invocation pattern from the swarm tests as a model.

    7. The REQ-10 (`mention_ancestor_cycle_skipped`) test builds the 4-hop chain manually via `store.create_task` + `store.insert_link` (NOT via mention itself — otherwise the test is circular). Then call `create_mention_children` on the deepest task with a child whose assignee matches an ancestor — assert that the result either returns a `Skipped(Cycle)` (if the resolver layer 1 fires) OR `Err(KanbanError::Other(...))` with `"mention_cycle"` in the message (if the store layer 2 fires). EITHER is acceptable — the test asserts ONE of the two paths blocks the cycle.

    8. The REQ-11 atomic-rollback test feeds a `MentionPlan` with TWO children where the second has `assignee="UPPERCASE_FAIL"` (which fails `validate_profile_name`). Pre-flight validation should reject the WHOLE plan before any tx opens. Assert `create_mention_children(plan).is_err()` AND post-call row counts equal pre-call counts (i.e. ZERO new tasks/links/events).

    9. The REQ-12 idempotency test calls `create_mention_children` twice with the same `idempotency_key = Some("k1".to_string())`. After call 1, snapshot `tasks_count_after_1`. After call 2, assert `tasks_count_after_2 == tasks_count_after_1` (no new rows). Assert `result_1.children == result_2.children` (modulo ordering — sort if needed). Inspect the first child's `idempotency_key` column via raw SQL and assert it equals `"k1:mention:0"` (Landmine 2 — exact suffix scheme).

    **(b) `crates/ironhermes-core/tests/deferred_subverbs.rs` — REQ-08 receiver test:**

    1. Pre-flight grep `find crates/ironhermes-core/tests -name 'deferred*'` to see if a file already exists.
    2. If exists, append a new `#[test] fn deferred_subverbs_includes_mention() { ... }`.
    3. If not, create the file with the test. The test body should either:
       (a) reference the public constant `DEFERRED_KANBAN_SUBVERBS` if it's pub or pub(crate)-accessible (preferred), OR
       (b) invoke the slash-dispatch entry point with subverb=`"mention"` and assert the response matches the deferred-CLI message string (behavior-based test).
       Pick (a) if accessible; (b) if not. Document the choice in a `//` comment above the test.

    Run `cargo test -p ironhermes-kanban --test tools_smoke -- mention -- --test-threads=1` and confirm ≥ 15 passed. Run `cargo test -p ironhermes-core -- deferred_subverbs_includes_mention` and confirm pass.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-kanban --test tools_smoke -- mention -- --test-threads=1 2>&1 | tail -3 ; cargo test -p ironhermes-core -- deferred_subverbs_includes_mention -- --test-threads=1 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ironhermes-kanban --test tools_smoke -- mention -- --test-threads=1` shows ≥ 15 tests, 0 failed.
    - `cargo test -p ironhermes-core -- deferred_subverbs_includes_mention -- --test-threads=1` shows ≥ 1 passed, 0 failed.
    - `grep -c '^async fn mention_\|^fn mention_\|^#\[tokio::test\]' crates/ironhermes-kanban/tests/tools_smoke.rs | head -1` returns ≥ 15 mention-related test fn declarations (filter by `mention_` prefix).
    - `grep -c 'create_mention_children' crates/ironhermes-kanban/tests/tools_smoke.rs` returns ≥ 10 (most tests call it).
    - `grep -c ':mention:0' crates/ironhermes-kanban/tests/tools_smoke.rs` returns ≥ 1 (idempotency key suffix asserted).
    - `grep -c 'KanbanMentionTool' crates/ironhermes-kanban/tests/tools_smoke.rs` returns ≥ 1 (tool-layer test).
    - The full ironhermes-kanban suite still passes: `cargo test -p ironhermes-kanban -- --test-threads=1` returns 0 failed.
  </acceptance_criteria>
  <done>≥ 15 mention tests added to tools_smoke.rs; deferred-subverbs membership test passes; no regressions in pre-existing suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 0 — docs reconciliation + REQUIREMENTS.md): Edit docs/kanban/reference.md §14/§200/§741 and append REQ-36.3.7.8-01..16 to REQUIREMENTS.md</name>
  <files>
    docs/kanban/reference.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/phases/36.3.7.8-mention-delegation-parser-inline-routing-from-prose/36.3.7.8-VALIDATION.md (rows 41-56 — REQ behavior phrasing source)
    - .planning/phases/36.3.7.8-mention-delegation-parser-inline-routing-from-prose/36.3.7.8-CONTEXT.md (D-01..D-04 decision anchors)
    - .planning/phases/36.3.7.8-mention-delegation-parser-inline-routing-from-prose/36.3.7.8-RESEARCH.md §Phase Requirements (rows REQ-36.3.7.8-01..16)
    - docs/kanban/reference.md §14 (current "all 10 LLM tools" narrative — post-36.3.7.7)
    - docs/kanban/reference.md §200 (tool table — column shape)
    - docs/kanban/reference.md §741 (P6 row — current "deferred to Phase 36.3.7.8" marker)
    - .planning/REQUIREMENTS.md lines 210-221 (36.3.7.7 section as format model)
  </read_first>
  <behavior>
    - `grep -c 'deferred to Phase 36.3.7.8' docs/kanban/reference.md` returns 0 (REQ-16).
    - `grep -c 'all 11 LLM tools' docs/kanban/reference.md` returns ≥ 1 (REQ-16).
    - `grep -c 'kanban_mention' docs/kanban/reference.md` returns ≥ 2 (REQ-16).
    - `grep -c 'Shipped in Phase 36.3.7.8' docs/kanban/reference.md` returns ≥ 1.
    - `.planning/REQUIREMENTS.md` has a new section `## Phase 36.3.7.8 - @mention delegation parser` with 16 REQ entries.
    - Each REQ in REQUIREMENTS.md references at least one D-XX anchor.
    - No prior REQUIREMENTS.md sections are modified.
  </behavior>
  <action>
    **(a) `docs/kanban/reference.md` — three edits:**

    1. **§14 V1-NOTE bump:** find the line containing `all 10 LLM tools` (added by 36.3.7.7). Replace `all 10 LLM tools` with `all 11 LLM tools`. Inside the surrounding inline tool list (which currently includes `kanban_swarm`), append `kanban_mention` in alphabetical or chronological order — match the existing convention.

    2. **§200 tool table — add one row:** find the existing table that has a row for `kanban_swarm`. Read 2-3 rows around it to identify the exact column layout (the table likely has headers like `| Tool | Description | Phase |`). Add a new row:
       ```
       | kanban_mention | @mention delegation parser inline routing | Phase 36.3.7.8 |
       ```
       Adjust column count to match the actual table — if the table has 4 or 5 columns, fill the additional ones with content consistent with the swarm row (e.g., "Yes" / "No" for is_available, etc.).

    3. **§741 P6 row reconciliation:** find the line `*(deferred to Phase 36.3.7.8 — @mention delegation parser not implemented in v1)*`. Replace it with `*Shipped in Phase 36.3.7.8.*`. Keep the rest of the P6 row text intact.

    4. Verify via grep gates per REQ-16:
       ```
       grep -c 'deferred to Phase 36.3.7.8' docs/kanban/reference.md   # expect 0
       grep -c 'all 11 LLM tools' docs/kanban/reference.md              # expect >= 1
       grep -c 'kanban_mention' docs/kanban/reference.md                # expect >= 2
       grep -c 'Shipped in Phase 36.3.7.8' docs/kanban/reference.md     # expect >= 1
       ```

    **(b) `.planning/REQUIREMENTS.md` — append a new section:**

    1. Locate the END of the existing 36.3.7.7 section (around line 221 — find via `grep -n '## Phase 36.3.7.7' .planning/REQUIREMENTS.md`).
    2. After the last REQ-36.3.7.7-12 line, add a blank line and then a new section:
       ```
       ## Phase 36.3.7.8 - @mention delegation parser

       - [ ] **REQ-36.3.7.8-01**: Pure `parse_mentions(body: &str) -> Vec<MentionSpan>` function exists in `crates/ironhermes-kanban/src/mention/parser.rs` and returns one `MentionSpan { handle, byte_offset }` per literal `@<word>` occurrence in Markdown prose, skipping fenced code blocks, inline code, and HTML comments. Anchors: D-02.
       - [ ] **REQ-36.3.7.8-02**: `parse_mentions` follows CommonMark §6.5 + §6.6 fence detection rules (fence openers at column 0 with up to 3 leading spaces, matching-count backtick runs for inline). Indented 4-space code blocks are NOT treated as fences in v1 (documented limitation). Anchors: D-02.
       - [ ] **REQ-36.3.7.8-03**: `resolve_mention(handle, &ctx) -> Resolution` exists in `mention/resolver.rs` returning `Resolution::Resolved(String)`, `Resolution::Fallback(String, FallbackReason)`, or `Resolution::Skipped(SkipReason)`. Anchors: D-03.
       - [ ] **REQ-36.3.7.8-04**: Handle resolution is case-insensitive — `@Reviewer` and `@reviewer` collapse to lowercase `reviewer` BEFORE `validate_profile_name`. Anchors: D-03.
       - [ ] **REQ-36.3.7.8-05**: Three fallback policies — `skip` (default, drop silently), `pending` (materialize child with assignee="pending"), `error` (whole-batch reject). Anchors: D-03.
       - [ ] **REQ-36.3.7.8-06**: `kanban_mention` LLM tool registered as the 11th tool in `crates/ironhermes-kanban/src/tools/mod.rs::register_kanban_tools`. Returns `{task_id, mentions_parsed, children_created, skipped}` JSON. Anchors: D-01.
       - [ ] **REQ-36.3.7.8-07**: `hermes kanban mention <task_id>` CLI verb exists; dispatch arm in `handle_kanban_command` calls `cmd_mention(...)`. Flags: `--fallback-policy`, `--idempotency-key`, `--body-override`, `--json`. Anchors: D-01.
       - [ ] **REQ-36.3.7.8-08**: `"mention"` added to `DEFERRED_KANBAN_SUBVERBS` at `crates/ironhermes-core/src/commands/handlers.rs:1143-1148`. Gateway `/kanban mention ...` routes to the deferred-CLI message. Anchors: D-01.
       - [ ] **REQ-36.3.7.8-09**: Self-mention rejected at resolver layer — a handle resolving to the parent task's assignee returns `Resolution::Skipped(SkipReason::SelfReference)`. Anchors: D-04 layer 1.
       - [ ] **REQ-36.3.7.8-10**: Ancestor-chain cycle detection — `WITH RECURSIVE ancestors` walk capped at `MAX_MENTION_CHAIN_DEPTH = 4` skips children whose assignee appears in the chain. Anchors: D-04 layer 2.
       - [ ] **REQ-36.3.7.8-11**: Atomic transaction — `parse → resolve → insert N children + N links + N events` runs in one `BEGIN IMMEDIATE` transaction. Per-child failure rolls back the entire batch (zero rows in tasks/task_links/task_events). Anchors: D-04, INV-36.3.7-atomic.
       - [ ] **REQ-36.3.7.8-12**: Idempotency replay — per-child keys are `"{base}:mention:{i}"` (0-indexed by original parse order). Re-invocation short-circuits via `find_mentions_by_idempotency_key` and returns identical `MentionResult.children`. Anchors: D-01.
       - [ ] **REQ-36.3.7.8-13**: Receiver-end tests in `crates/ironhermes-kanban/tests/tools_smoke.rs` mention section cover REQ-01..REQ-12 — ≥ 15 tests. Anchors: D-01, D-02, D-03, D-04.
       - [ ] **REQ-36.3.7.8-14**: CLI parity tests in `crates/ironhermes-cli/tests/mention_cli.rs` — ≥ 4 tests covering clap parse + flag handling. Anchors: D-01.
       - [ ] **REQ-36.3.7.8-15**: `docs/kanban/reference.md` §741 row replaced with `*Shipped in Phase 36.3.7.8.*`; §14 V1-NOTE bumped from "10 LLM tools" to "11 LLM tools" with `kanban_mention` in the inline list; §200 table grows by one row for `kanban_mention`. Anchors: RESEARCH §Validation Architecture.
       - [ ] **REQ-36.3.7.8-16**: Verifier grep gates pass — `grep -c 'deferred to Phase 36.3.7.8' docs/kanban/reference.md` returns 0; `grep -c 'all 11 LLM tools' docs/kanban/reference.md` returns ≥ 1; `grep -c 'kanban_mention' docs/kanban/reference.md` returns ≥ 2. Anchors: D-04, RESEARCH §Validation Architecture.
       ```

    3. Verify: `grep -c 'REQ-36.3.7.8-' .planning/REQUIREMENTS.md` returns ≥ 16. Verify NO prior REQ-36.3.7.X- lines were rewritten: `git diff .planning/REQUIREMENTS.md | grep -E '^-[^-]' | grep -v 'REQ-36.3.7.8-' | wc -l` returns 0.

    4. Also update the `Phase` column in the REQUIREMENTS.md tracking table (around lines 423-425 — `| REQ-36.3.7.7-01 | Phase 36.3.7.7 | Pending |`) by appending 16 new rows for REQ-36.3.7.8-01..16 marked `Pending`. Match the existing table column format byte-for-byte.
  </action>
  <verify>
    <automated>grep -c 'deferred to Phase 36.3.7.8' docs/kanban/reference.md ; grep -c 'all 11 LLM tools' docs/kanban/reference.md ; grep -c 'kanban_mention' docs/kanban/reference.md ; grep -c 'REQ-36.3.7.8-' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'deferred to Phase 36.3.7.8' docs/kanban/reference.md` returns 0.
    - `grep -c 'all 11 LLM tools' docs/kanban/reference.md` returns ≥ 1.
    - `grep -c 'all 10 LLM tools' docs/kanban/reference.md` returns 0 (replaced).
    - `grep -c 'kanban_mention' docs/kanban/reference.md` returns ≥ 2.
    - `grep -c 'Shipped in Phase 36.3.7.8' docs/kanban/reference.md` returns ≥ 1.
    - `grep -c 'REQ-36.3.7.8-' .planning/REQUIREMENTS.md` returns ≥ 16.
    - `grep -c '## Phase 36.3.7.8' .planning/REQUIREMENTS.md` returns 1.
    - `git diff .planning/REQUIREMENTS.md | grep -E '^-[^-]' | grep -v 'REQ-36.3.7.8-' | wc -l` returns 0 (additive only).
    - Every new REQ line in REQUIREMENTS.md contains `Anchors: D-` (anchor reference present).
  </acceptance_criteria>
  <done>Docs reconciliation grep gates pass; REQUIREMENTS.md has all 16 REQs with D-XX anchors; no prior content rewritten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test inputs → store primitive | Tests author the inputs; trust boundary is internal. |
| Docs edits → consumers | Documentation is read by humans + downstream automation (verifier grep gates). Wording is locked by REQ-15/REQ-16. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.8-05-01 | Tampering | Stale grep gate from incomplete docs edit | mitigate | REQ-16 grep gates run as the final verifier — `grep -c 'deferred to Phase 36.3.7.8'` MUST return 0. The acceptance criteria of Task 2 enforce all four grep counts before marking complete. |
| T-36.3.7.8-05-02 | Repudiation | REQ added without anchor decision | mitigate | Every REQ-36.3.7.8-XX in REQUIREMENTS.md ends with `Anchors: D-NN[, ...]`. Acceptance criteria grep `Anchors: D-` confirms presence on every new line. |
| T-36.3.7.8-05-03 | Information Disclosure | Test output leaks body content via panic message | accept | Test bodies are authored by the executor; assertion failure messages may include task body strings. Acceptable in test output — these never ship to production. |
| T-36.3.7.8-05-04 | Denial of Service | Tools_smoke.rs runtime grows past CI budget | mitigate | Each new test uses `tempfile::tempdir()` via `make_store()` — isolated SQLite file per test. Test count grows from ~9 (post-36.3.7.7) to ~24-25 with mention section. Per VALIDATION.md, the full suite runs in ~3-4 min — well within budget. |
| T-36.3.7.8-05-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies. |
</threat_model>

<verification>
After both tasks complete:

```bash
# REQ-13 — receiver tests
cargo test -p ironhermes-kanban --test tools_smoke -- mention -- --test-threads=1 2>&1 | tail -3   # >= 15 passed

# REQ-08 — receiver test
cargo test -p ironhermes-core -- deferred_subverbs_includes_mention -- --test-threads=1 2>&1 | tail -3

# Full ironhermes-kanban suite (no regressions)
cargo test -p ironhermes-kanban -- --test-threads=1 2>&1 | tail -3

# REQ-15 + REQ-16 — docs reconciliation grep gates
grep -c 'deferred to Phase 36.3.7.8' docs/kanban/reference.md          # = 0
grep -c 'all 11 LLM tools' docs/kanban/reference.md                    # >= 1
grep -c 'all 10 LLM tools' docs/kanban/reference.md                    # = 0
grep -c 'kanban_mention' docs/kanban/reference.md                      # >= 2
grep -c 'Shipped in Phase 36.3.7.8' docs/kanban/reference.md           # >= 1

# Requirements.md anchored
grep -c 'REQ-36.3.7.8-' .planning/REQUIREMENTS.md                      # >= 16

# Full workspace gate (sibling-phase precedent)
cargo build --workspace 2>&1 | tail -3
cargo test --workspace --no-fail-fast -- --test-threads=1 2>&1 | tail -10
```
</verification>

<success_criteria>
- ≥ 15 mention tests in tools_smoke.rs, all passing.
- DEFERRED_KANBAN_SUBVERBS membership test passes.
- docs/kanban/reference.md §14/§200/§741 reconciled — REQ-16 grep gates pass.
- .planning/REQUIREMENTS.md contains 16 anchored REQ-36.3.7.8-XX entries + tracking-table rows.
- Full workspace `cargo test --workspace --no-fail-fast -- --test-threads=1` shows 0 failed across all crates.
- No regression in any pre-existing test in any crate.
</success_criteria>

<output>
Create `.planning/phases/36.3.7.8-mention-delegation-parser-inline-routing-from-prose/36.3.7.8-05-SUMMARY.md` when done.
</output>
