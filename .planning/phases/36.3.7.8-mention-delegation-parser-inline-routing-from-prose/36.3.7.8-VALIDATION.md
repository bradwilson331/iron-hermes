---
phase: 36.3.7.8
slug: mention-delegation-parser-inline-routing-from-prose
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-30
---

# Phase 36.3.7.8 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Sourced from `36.3.7.8-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` + `#[tokio::test]` (async via tokio test harness) |
| **Config file** | `crates/ironhermes-kanban/Cargo.toml` + workspace `Cargo.toml` |
| **Quick run command** | `cargo test -p ironhermes-kanban --test tools_smoke -- mention` |
| **Full suite command** | `cargo test --workspace --no-fail-fast -- --test-threads=1` |
| **Estimated runtime** | ~30s quick / ~3-4 min full |

---

## Sampling Rate

- **After every task commit:** `cargo test -p ironhermes-kanban --test tools_smoke -- mention`
- **After every plan wave:** `cargo test -p ironhermes-kanban && cargo test -p ironhermes-cli --test mention_cli && cargo test -p ironhermes-core`
- **Before `/gsd:verify-work`:** `cargo build --workspace && cargo test --workspace --no-fail-fast -- --test-threads=1` (full workspace gate — same as 36.3.7.6 / 36.3.7.7 closures)
- **Max feedback latency:** 60s for per-task verify; 240s for full-suite gate

---

## Per-Task Verification Map

| Req ID | Behavior | Test Type | Automated Command | File Exists | Status |
|--------|----------|-----------|-------------------|-------------|--------|
| REQ-01 | `parse_mentions` emits `MentionSpan` for each prose `@handle` | unit | `cargo test -p ironhermes-kanban mention::parser::tests::extracts_simple_handle` | ❌ W0 | ⬜ pending |
| REQ-02 | Parser skips fenced code, inline code, HTML comments | unit | `cargo test -p ironhermes-kanban mention::parser::tests::skips_fenced_inline_html_comment` | ❌ W0 | ⬜ pending |
| REQ-03 | `resolve_mention` returns Resolved / Fallback / Skipped | unit | `cargo test -p ironhermes-kanban mention::resolver::tests::three_resolution_variants` | ❌ W0 | ⬜ pending |
| REQ-04 | Handle is case-insensitive (`@Reviewer` → `reviewer`) | unit | `cargo test -p ironhermes-kanban mention::resolver::tests::case_insensitive_handle` | ❌ W0 | ⬜ pending |
| REQ-05 | Three fallback policies: `skip` / `pending` / `error` | integration | `cargo test -p ironhermes-kanban --test tools_smoke -- mention_fallback_policy` | ❌ W0 | ⬜ pending |
| REQ-06 | `kanban_mention` LLM tool registered (11th slot) | smoke | `cargo test -p ironhermes-kanban --test tools_smoke -- mention_registered_eleventh_tool` | ❌ W0 | ⬜ pending |
| REQ-07 | `hermes kanban mention` CLI verb parses + dispatches | integration | `cargo test -p ironhermes-cli --test mention_cli` | ❌ W0 | ⬜ pending |
| REQ-08 | `"mention"` in `DEFERRED_KANBAN_SUBVERBS` | unit | `cargo test -p ironhermes-core -- deferred_subverbs_includes_mention` | ❌ W0 | ⬜ pending |
| REQ-09 | Self-mention skipped | integration | `cargo test -p ironhermes-kanban --test tools_smoke -- mention_self_reference_skipped` | ❌ W0 | ⬜ pending |
| REQ-10 | A→B→A cycle blocked at chain depth 4 | integration | `cargo test -p ironhermes-kanban --test tools_smoke -- mention_cycle_A_B_A_4_hops_blocked` | ❌ W0 | ⬜ pending |
| REQ-11 | Atomic rollback on per-child failure | integration | `cargo test -p ironhermes-kanban --test tools_smoke -- mention_invalid_assignee_rolls_back_whole_batch` | ❌ W0 | ⬜ pending |
| REQ-12 | Idempotency replay returns same children | integration | `cargo test -p ironhermes-kanban --test tools_smoke -- mention_idempotency_key_replays_return_same_children` | ❌ W0 | ⬜ pending |
| REQ-13 | ≥15 receiver tests in `tools_smoke.rs` | aggregate | `cargo test -p ironhermes-kanban --test tools_smoke -- mention_` | ❌ W0 | ⬜ pending |
| REQ-14 | ≥4 CLI parity tests | aggregate | `cargo test -p ironhermes-cli --test mention_cli` | ❌ W0 | ⬜ pending |
| REQ-15 | `docs/kanban/reference.md` §741 reconciled | grep | `grep -c 'deferred to Phase 36.3.7.8' docs/kanban/reference.md` (expect 0) | ❌ W0 | ⬜ pending |
| REQ-16 | Grep gates pass (3 commands per Research §Validation) | grep | three `grep -c` commands per RESEARCH.md REQ-16 | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/ironhermes-kanban/src/mention/mod.rs` — module bootstrap + re-exports
- [ ] `crates/ironhermes-kanban/src/mention/parser.rs` — `parse_mentions` + fence state machine + unit tests
- [ ] `crates/ironhermes-kanban/src/mention/resolver.rs` — `resolve_mention` + `ResolverCtx` + `MAX_MENTION_CHAIN_DEPTH` + unit tests
- [ ] `crates/ironhermes-kanban/src/tools/mention.rs` — `KanbanMentionTool` impl + `is_available` gating test
- [ ] `crates/ironhermes-kanban/src/store.rs` — add `create_mention_children` sibling method (BEGIN IMMEDIATE tx)
- [ ] `crates/ironhermes-kanban/tests/tools_smoke.rs` — extend with `// kanban_mention tests` section (≥15 tests covering REQ-01..REQ-12)
- [ ] `crates/ironhermes-cli/src/kanban/mod.rs` — `KanbanCommands::Mention` variant + dispatch arm
- [ ] `crates/ironhermes-cli/src/kanban/commands.rs` — `cmd_mention` handler
- [ ] `crates/ironhermes-cli/tests/mention_cli.rs` — ≥4 clap parity tests
- [ ] `crates/ironhermes-core/src/commands/handlers.rs` — add `"mention"` to `DEFERRED_KANBAN_SUBVERBS`
- [ ] `docs/kanban/reference.md` — §14 tool-count bump, §200 add `kanban_mention` row, §741 reconcile P6 row
- [ ] No framework install needed — `cargo test` is the existing test runner.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| LLM agent calls `kanban_mention` mid-conversation and child tasks appear in the worker's claim queue | REQ-06 (live LLM exercise) | Requires a live model + active worker session; cannot stub deterministically | (a) Set `HERMES_KANBAN_TASK=<parent>`; (b) prompt model with body containing `@reviewer ...`; (c) confirm `hermes kanban tail` shows the child + correct parent_id |

*All other phase behaviors have automated verification.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s for per-task
- [ ] `nyquist_compliant: true` set in frontmatter (after Wave 0 closes)

**Approval:** pending
