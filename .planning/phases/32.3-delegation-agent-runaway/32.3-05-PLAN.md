---
phase: 32.3-delegation-agent-runaway
plan: "05"
type: execute
wave: 4
depends_on:
  - 32.3-03
files_modified:
  - docs/DELEGATION.md
  - .planning/phases/32.3-delegation-agent-runaway/32.3-HELP-AUDIT.md
autonomous: true
requirements:
  - D-11
  - D-12
  - D-04
  - D-05
  - D-08
  - D-09
tags: [docs, help, audit, delegation, discoverability, shrike-service]
must_haves:
  truths:
    - "docs/DELEGATION.md contains sections describing stale_warn_seconds (D-05), Stale status pill (D-06), the four shrike subcommands (D-08), and the gateway confirm token (D-09)"
    - "A `Help Discoverability Gaps` table exists in 32.3-HELP-AUDIT.md (or as a section appended to 32.3-SUMMARY.md before commit) covering tui_rata + iron_hermes_ui + gateway with surface | command | listed? | accurate? | gap_severity columns"
    - "Audit identifies gaps but does NOT fix /help output (D-12 — fixes are follow-up todos, with the carve-out that NEW 32.3 subcommands are added to build_registry by Plan 03 itself per B3)"
    - "Audit findings exist as a structured markdown section a future doc-polish phase can grep, not as scattered prose"
  artifacts:
    - path: "docs/DELEGATION.md"
      provides: "Shrike service reference section, stale_warn_seconds field doc, /agents subcommand examples for all three surfaces, gateway confirm token doc"
      contains: "Shrike Service\\|stale_warn_seconds\\|/agents kill\\|/agents interrupt\\|/agents prune\\|/agents status"
    - path: ".planning/phases/32.3-delegation-agent-runaway/32.3-HELP-AUDIT.md"
      provides: "Three-surface /help discoverability audit table with structured rows"
      contains: "surface | command | listed?"
  key_links:
    - from: "docs/DELEGATION.md"
      to: "32.3-HELP-AUDIT.md"
      via: "Reference link in DELEGATION.md mentioning audit-only status of /help fixes (D-12)"
      pattern: "32.3-HELP-AUDIT"
---

<objective>
Document the shrike service in `docs/DELEGATION.md` and audit `/help` discoverability across all three surfaces (TUI / web / gateway). Per D-11 and D-12, this is an AUDIT-ONLY plan — gaps are recorded as follow-up todos, NOT fixed in 32.3, with the carve-out that the THREE NEW /agents subcommands added in 32.3 (interrupt / prune / status) DO get help entries; that B3 work lives in Plan 03 (build_registry edits), not here.

Purpose: D-04, D-05, D-08, D-09 land observable user-facing features. Operators discover them via `/help` and via `docs/DELEGATION.md`. Without doc + help coverage, the shrike service exists but is invisible. D-12 explicitly defers the broader `/help` rewrite to a future doc-polish phase to keep this phase focused on the runaway-fix; the new-subcommand help entries themselves are implementation work (Plan 03).

**Wave 4 sequencing (B1 fix):** Plan 05 runs in parallel with Plan 04 (Wave 4) because it has no code dependency on the surface adapters and zero file overlap with Plan 04. Both Wave 4 plans depend on Plan 03 (Wave 3).

Output:
- `docs/DELEGATION.md` extended with four new sections (or four extensions to existing sections): Stale-Child Detection (D-04/D-05/D-06), Shrike Service (D-08), Gateway Confirm Token (D-09), Migration note for the 6.7-hour ghost fix (D-01/D-02 reference)
- `.planning/phases/32.3-delegation-agent-runaway/32.3-HELP-AUDIT.md` with a structured table covering 9 rows (3 surfaces × 3 destructive/diagnostic command groups OR 3 surfaces × 4 subcommands = 12 rows; choose the granularity that matches actual /help output)
- Gap rows written as TODO-tagged entries the next doc-polish phase can pick up
</objective>

<execution_context>
@/Users/twilson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/twilson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32.3-delegation-agent-runaway/32.3-CONTEXT.md
@.planning/phases/32.3-delegation-agent-runaway/32.3-RESEARCH.md
@.planning/phases/32.3-delegation-agent-runaway/32.3-03-PLAN.md
@docs/DELEGATION.md

<interfaces>
<!-- Existing doc structure + audit format guidance -->

From docs/DELEGATION.md (HEAD after 32.2-05):
- Section "Monitoring Active Subagents" with /agents tree view subsection — EXTEND with stale pill + new subcommands
- Section "Per-Call Schema Fields" (max_iterations, role) — EXTEND with stale_warn_seconds
- Section "Migration from `subagent:` to `delegation:`" — EXTEND with stale_warn_seconds config field addition
- Existing config YAML block with delegation: key — EXTEND with stale_warn_seconds: 120

From D-11 in CONTEXT.md:
- Audit table columns: surface | command | listed? | accurate? | gap_severity
- Surfaces: tui_rata, iron_hermes_ui, gateway/Telegram
- Commands to audit: /agents (the family), specifically /agents kill, /agents interrupt, /agents prune, /agents status — and the bare /agents (list/tree)

From D-12 in CONTEXT.md:
- Gaps NOT fixed in 32.3 — recorded only
- Output destination: 32.3-SUMMARY.md "Help Discoverability Gaps" section (research recommendation §3 in RESEARCH.md Open Questions) OR a dedicated 32.3-HELP-AUDIT.md file referenced from SUMMARY
- **Carve-out:** New 32.3 subcommands (interrupt/prune/status) DO need help entries — those are Plan 03 implementation work, not audit work. The audit verifies Plan 03 landed them correctly but does not author them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend docs/DELEGATION.md with stale-detection, shrike service, gateway confirm-token, and 6.7-hour ghost fix sections</name>
  <files>docs/DELEGATION.md</files>

  <read_first>
    - docs/DELEGATION.md (full file — find sections "Monitoring Active Subagents", "Per-Call Schema Fields", "Migration from `subagent:` to `delegation:`" and the YAML config block)
    - .planning/phases/32.3-delegation-agent-runaway/32.3-CONTEXT.md (D-01..D-12 — operator-facing contract authoritative source)
    - .planning/phases/32.3-delegation-agent-runaway/32.3-RESEARCH.md (operator semantics for stale detection + shrike + confirm)
    - .planning/phases/32.2-subagent-delegation-parity/32.2-05-SUMMARY.md (32.2-05 added the tree view; preserve and extend without overwriting)
  </read_first>

  <behavior>
    - DELEGATION.md gains the following content (place as discrete subsections under existing top-level headings; do NOT renumber prose):
      1. Under "Per-Call Schema Fields" — add `stale_warn_seconds: Option<u64>` row with description "Seconds of inactivity before this subagent is flagged Stale (per-call override). Falls back to `delegation.stale_warn_seconds` (default 120s)." Show single-task + per-task-in-batch JSON example.
      2. Under the YAML config block — add `stale_warn_seconds: 120` next to existing `child_timeout_seconds: 300` line. Inline comment: `# Stale-warn threshold; soft signal only. Hard kill ceiling is child_timeout_seconds.`
      3. New top-level section "Stale-Child Detection" — explains: tracker bumps on each LLM call / streaming token / tool dispatch (D-04 — Phase 32.1 ActivityTracker); when `now - last_activity > stale_warn_seconds`, the tree view renders `[stale]`; `tracing::warn!` emits once per child. Note: stale is a soft signal — operator decides whether to /agents kill.
      4. New top-level section "Shrike Service — Operator Termination Surface" — narrative for each of the four subcommands (kill / interrupt / prune / status). For each: what it does, when to use it, example output. Document that kill calls AgentLoop::interrupt + JoinHandle::abort + guard-deregister; interrupt calls AgentLoop::interrupt only (soft cancel); prune sweeps stale entries; status returns the diagnostic key/value block.
      5. New subsection under Shrike Service: "Gateway Confirm Token (Telegram only)" — D-09 explanation. Document that `/agents kill <id>` and `/agents prune` require the literal token `confirm` as the LAST argument when invoked over Telegram. TUI and iron_hermes_ui do NOT require confirm. Show example: `/agents kill sub_xxxxxxxx confirm`.
      6. New subsection: "The 6.7-Hour Ghost Bug (Phase 32.3 Fix Reference)" — brief operator-facing note explaining: "If you saw `/agents` reporting a subagent as Active for hours after its output files were written, this was a pre-32.3 registry-leak bug. Phase 32.3 fixes it with a RAII RegistrationGuard — every exit path (natural / timeout / cancel / panic / abort) now deregisters atomically." Link to 32.3-CONTEXT.md / 32.3-RESEARCH.md for engineering detail.
      7. Update "Migration from `subagent:` to `delegation:`" section to add a row: `stale_warn_seconds` is a new field; absent in pre-32.3 configs is treated as 120 via serde default.
    - All content in operator voice (not engineering voice). Engineering details link back to research/context docs.
  </behavior>

  <action>
    Read docs/DELEGATION.md to find:
    1. The YAML config block (search for `delegation:` followed by indented `max_concurrent_children:` etc.)
    2. The "Per-Call Schema Fields" section heading
    3. The "Monitoring Active Subagents" section
    4. The "Migration from `subagent:` to `delegation:`" section

    Edit docs/DELEGATION.md to add the seven content items listed in <behavior>. Use markdown ## / ### headings consistent with surrounding style. Use fenced code blocks for YAML config snippets and example /agents output (those ARE permitted in markdown docs — the prohibition on fenced blocks applies to PLAN action prose, not to the artifact being documented).

    For example sections, mirror the formatting of the existing "Monitoring Active Subagents" tree-view section (from 32.2-05) — same code-block style, same column widths in tables.

    Acceptance: every new section heading is a real markdown H2 or H3; every new field reference uses fixed-width `code` formatting; every new subcommand example is in a fenced block labeled `text` or `bash`.
  </action>

  <acceptance_criteria>
    - `grep -c "stale_warn_seconds" docs/DELEGATION.md` >= 3 (config YAML + per-call schema + migration table)
    - `grep -c "Stale-Child Detection\|Stale Child Detection\|stale-child-detection" docs/DELEGATION.md` >= 1
    - `grep -c "Shrike Service\|shrike service" docs/DELEGATION.md` >= 1
    - `grep -c "/agents kill\|/agents interrupt\|/agents prune\|/agents status" docs/DELEGATION.md` >= 4
    - `grep -c "confirm" docs/DELEGATION.md` >= 2 (gateway section + example)
    - `grep -c "6.7\|RegistrationGuard\|RAII" docs/DELEGATION.md` >= 1 (ghost bug reference)
    - `grep -c "32.3\|Phase 32.3" docs/DELEGATION.md` >= 1
    - File still well-formed markdown: `head -30 docs/DELEGATION.md` shows preserved front matter and original H1
    - No existing sections deleted: `grep -c "delegation:\|max_concurrent_children\|child_timeout_seconds\|max_spawn_depth" docs/DELEGATION.md` matches or exceeds 32.2-05 baseline (delegation: ×6, max_concurrent_children ×6, child_timeout_seconds ×4, max_spawn_depth ×6 per 32.2-05 SUMMARY)
  </acceptance_criteria>

  <verify>
    <automated>grep -c "stale_warn_seconds" docs/DELEGATION.md && grep -c "Shrike\|shrike" docs/DELEGATION.md && grep -c "/agents kill\|/agents interrupt\|/agents prune\|/agents status" docs/DELEGATION.md && grep -c "confirm" docs/DELEGATION.md && grep -c "32.3" docs/DELEGATION.md</automated>
  </verify>

  <done>DELEGATION.md has stale_warn_seconds doc, Shrike Service section, four subcommand examples, gateway confirm token note, and 6.7-hour ghost reference. Counts above thresholds.</done>
</task>

<task type="auto">
  <name>Task 2: Audit /help discoverability across three surfaces; record findings in 32.3-HELP-AUDIT.md as a structured table (NO /help fixes per D-12, with B3 carve-out for the three new subcommands handled by Plan 03)</name>
  <files>.planning/phases/32.3-delegation-agent-runaway/32.3-HELP-AUDIT.md</files>

  <read_first>
    - .planning/phases/32.3-delegation-agent-runaway/32.3-CONTEXT.md (D-11 + D-12 audit-only mandate)
    - .planning/phases/32.3-delegation-agent-runaway/32.3-03-PLAN.md (B3 — Plan 03 already added /agents interrupt|prune|status to build_registry as part of implementation; audit verifies presence, doesn't author them)
    - crates/ironhermes-core/src/commands/registry.rs (build_registry — find `/help` definition and how it lists commands; Plan 03 should have added 3 new CommandDef entries)
    - crates/ironhermes-core/src/commands/handlers.rs (cmd_help — confirm where `/help` output is generated, whether it includes subcommand descriptions)
    - crates/ironhermes-cli/src/tui_rata/commands.rs (any TUI-specific /help override or banner)
    - crates/ironhermes-gateway/src/handler.rs (gateway /help handling — search for "help" string emission)
    - crates/iron_hermes_ui/src/server/state.rs (web UI /help — likely renders the same registry list)
  </read_first>

  <behavior>
    - Inspect (read source code, do not run binaries) how each surface generates `/help` output:
      - tui_rata: confirm the help text comes from `build_registry` + per-command help strings.
      - iron_hermes_ui: confirm whether the web surface even has a /help concept (it may render command list via a different mechanism).
      - gateway: check whether Telegram /help iterates the same registry.
    - For each surface, determine for each of the FIVE /agents states (bare `/agents`, `/agents kill`, `/agents interrupt`, `/agents prune`, `/agents status`):
      - **listed?** — is the subcommand discoverable from /help output? (yes/no/partial) — interrupt/prune/status should be `yes` everywhere because Plan 03 added them to build_registry (B3 implementation)
      - **accurate?** — does the listed description match the actual behavior in Plans 01-04? (yes/no/n/a if not listed)
      - **gap_severity** — none / minor / major (none = listed + accurate; minor = listed but description stale or missing one of D-09 confirm gate / argument shape; major = not listed at all)
    - Write findings to `.planning/phases/32.3-delegation-agent-runaway/32.3-HELP-AUDIT.md` as one markdown table with 15 rows (3 surfaces × 5 commands), columns exactly: `Surface | Command | Listed? | Accurate? | Gap Severity | Notes / Follow-up Todo`.
    - For each row with gap_severity >= minor, write a one-line Notes column entry phrased as a follow-up todo: e.g. "FUTURE: add `/agents interrupt <id>` to tui_rata /help with one-line description 'cancel cooperatively (soft interrupt)'". These todos become a follow-up doc-polish phase's input per D-12.
    - Do NOT modify any /help source — D-12 is explicit: audit-only. (The B3 carve-out lives in Plan 03; verify it landed but do not duplicate.)
    - Add a summary table at the top counting gaps by severity.
  </behavior>

  <action>
    Create `.planning/phases/32.3-delegation-agent-runaway/32.3-HELP-AUDIT.md` with the following sections:

    1. **Frontmatter** (yaml fence at top): `phase: 32.3`, `audit_type: help_discoverability`, `audit_date: <today>`, `fixes_in_phase: false`, `b3_carve_out: "/agents interrupt|prune|status help entries added by Plan 03 (build_registry)"`, `followup_destination: future_doc_polish_phase`.
    2. **Section: Audit Scope** — single paragraph stating: tui_rata + iron_hermes_ui + gateway/Telegram; commands audited = bare /agents, /agents kill, /agents interrupt, /agents prune, /agents status; per D-11/D-12 audit-only with the B3 carve-out for new-subcommand help entries.
    3. **Section: Methodology** — bullet list: (a) read build_registry + cmd_help sources, (b) trace each subcommand discoverability, (c) record listed/accurate/gap_severity, (d) do NOT modify /help output, (e) verify B3 (Plan 03's build_registry additions for interrupt/prune/status) is present.
    4. **Section: Gap Summary** — small table: `Severity | Count` (none / minor / major counts).
    5. **Section: Full Audit Table** — the 15-row table described in <behavior>.
    6. **Section: B3 Verification** — confirm `grep -c '"agents interrupt"\|"agents prune"\|"agents status"' crates/ironhermes-core/src/commands/registry.rs` == 3 (the Plan 03 CommandDef additions). If count < 3, escalate as a Plan 03 regression.
    7. **Section: Follow-up Todos** — bulleted list extracting only the rows with severity >= minor, phrased as discrete TODO entries a future doc-polish planner can adopt as plan tasks.
    8. **Section: References** — links back to 32.3-CONTEXT.md (D-11/D-12), 32.3-RESEARCH.md (Pattern 6 cmd_agents Extension), docs/DELEGATION.md (Task 1 of this plan), 32.3-03-PLAN.md (B3 implementation source of /help entries).

    Do not write code. Read source files, record findings.

    No execution of binaries. Read-only filesystem inspection.
  </action>

  <acceptance_criteria>
    - `test -f .planning/phases/32.3-delegation-agent-runaway/32.3-HELP-AUDIT.md && echo OK` prints `OK`
    - `grep -c "^| " .planning/phases/32.3-delegation-agent-runaway/32.3-HELP-AUDIT.md` >= 15 (table rows — at minimum 15 audit rows)
    - `grep -cE "^\| (tui_rata|iron_hermes_ui|gateway)" .planning/phases/32.3-delegation-agent-runaway/32.3-HELP-AUDIT.md` >= 15
    - `grep -cE "/agents (kill|interrupt|prune|status)" .planning/phases/32.3-delegation-agent-runaway/32.3-HELP-AUDIT.md` >= 12 (4 subcommands × 3 surfaces minimum)
    - `grep -ci "gap_severity\|gap severity" .planning/phases/32.3-delegation-agent-runaway/32.3-HELP-AUDIT.md` >= 1 (column header present)
    - `grep -ci "B3 Verification\|b3.verification" .planning/phases/32.3-delegation-agent-runaway/32.3-HELP-AUDIT.md` >= 1 (Plan 03 cross-check section present)
    - `grep -c "FUTURE:\|TODO" .planning/phases/32.3-delegation-agent-runaway/32.3-HELP-AUDIT.md` >= 1 (at least one follow-up; if zero gaps found, document that explicitly in Gap Summary instead)
    - No source files in `crates/` modified during this task: `git status --short crates/ | wc -l` returns same count as before this task (verify via SUMMARY)
  </acceptance_criteria>

  <verify>
    <automated>test -f .planning/phases/32.3-delegation-agent-runaway/32.3-HELP-AUDIT.md && grep -c "^| " .planning/phases/32.3-delegation-agent-runaway/32.3-HELP-AUDIT.md && grep -cE "^\| (tui_rata|iron_hermes_ui|gateway)" .planning/phases/32.3-delegation-agent-runaway/32.3-HELP-AUDIT.md && echo done</automated>
  </verify>

  <done>32.3-HELP-AUDIT.md exists with 15+ rows covering 3 surfaces × 5 commands; gap counts tallied; B3 verification section confirms Plan 03 build_registry additions present; follow-up todos enumerated; zero source-code modifications.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown file edits → docs/ tree | non-executable, no runtime trust impact |
| Audit findings → future planner | structured-data contract for downstream phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32.3-17 | Information Disclosure | DELEGATION.md publishes internal SubagentId format hints to operators | accept | ids are opaque nonces; documenting them does not reveal session secrets |
| T-32.3-18 | Tampering | audit table is structurally normalized so future planners can parse without ambiguity | mitigate | strict column count enforced via acceptance grep; gap_severity drawn from fixed set (none/minor/major) |
| T-32.3-01 | Tampering | (cross-reference Plan 04 — Telegram replay) — confirm token doc must be PROMINENT in operator-facing docs | mitigate | Task 1 acceptance grep requires `confirm` ≥ 2 mentions; not buried |
</threat_model>

<verification>
- `grep -c "stale_warn_seconds\|Shrike\|/agents kill\|/agents interrupt\|/agents prune\|/agents status\|confirm\|32.3" docs/DELEGATION.md` — all thresholds met per Task 1 acceptance
- `wc -l .planning/phases/32.3-delegation-agent-runaway/32.3-HELP-AUDIT.md` returns >= 30 lines (frontmatter + 7 sections + 15+ table rows)
- `git diff --stat docs/DELEGATION.md` shows only additions (no destructive deletions of 32.2-05 content)
- `git status --short crates/` shows no unintended source modifications from the audit task
- `grep -c '"agents interrupt"\|"agents prune"\|"agents status"' crates/ironhermes-core/src/commands/registry.rs` == 3 (B3 carve-out from Plan 03 landed)
</verification>

<success_criteria>
- DELEGATION.md is the canonical operator reference for the shrike service post-32.3
- 32.3-HELP-AUDIT.md captures discoverability gaps with structured rows a follow-up planner can parse
- D-12's audit-only mandate is honored (with B3 carve-out for the three new-subcommand help entries delegated to Plan 03)
- Cross-references between DELEGATION.md and 32.3-HELP-AUDIT.md let operators trace operator-doc ↔ engineering-doc
- Phase 32.3 closes with operator-facing visibility for D-04, D-05, D-06, D-08, D-09 changes
</success_criteria>

<output>
Create `.planning/phases/32.3-delegation-agent-runaway/32.3-05-SUMMARY.md` when done. Record:
- Section-by-section diff summary for DELEGATION.md (which subsections added, which extended)
- Final audit table line count + gap counts by severity
- All FUTURE: todos extracted (verbatim) so /gsd:verify-work can confirm they're enumerated
- Confirmation that no `crates/` files were modified during the audit task (clean source tree)
- B3 verification result (count of `"agents interrupt"|"agents prune"|"agents status"` in commands/registry.rs)
- Cross-link to 32.3-SUMMARY.md noting that this plan's audit findings are the "Help Discoverability Gaps" section the phase exit checklist references
</output>
</content>
</invoke>