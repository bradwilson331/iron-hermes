---
phase: "36.3.7.7"
plan: "01"
type: execute
wave: 1
depends_on:
  - "36.3.7.6-01"
files_modified:
  - crates/ironhermes-kanban/src/types.rs
  - crates/ironhermes-kanban/src/store.rs
  - crates/ironhermes-kanban/src/tools/swarm.rs
  - crates/ironhermes-kanban/src/tools/mod.rs
  - crates/ironhermes-kanban/src/tools/create.rs
  - crates/ironhermes-kanban/src/lib.rs
  - crates/ironhermes-kanban/src/kanban_guidance.rs
  - crates/ironhermes-kanban/tests/tools_smoke.rs
  - crates/ironhermes-cli/src/kanban/mod.rs
  - crates/ironhermes-cli/src/kanban/commands.rs
  - crates/ironhermes-cli/tests/swarm_cli.rs
  - crates/ironhermes-core/src/commands/handlers.rs
  - docs/kanban/reference.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-01-SUMMARY.md
autonomous: true
nyquist_compliant: true
wave_0_complete: false
requirements:
  - REQ-36.3.7.7-01
  - REQ-36.3.7.7-02
  - REQ-36.3.7.7-03
  - REQ-36.3.7.7-04
  - REQ-36.3.7.7-05
  - REQ-36.3.7.7-06
  - REQ-36.3.7.7-07
  - REQ-36.3.7.7-08
  - REQ-36.3.7.7-09
  - REQ-36.3.7.7-10
  - REQ-36.3.7.7-11
  - REQ-36.3.7.7-12
tags:
  - kanban
  - llm-tools
  - swarm
  - fan-out
  - atomic-transaction
  - cli-parity
  - docs-reconciliation
  - bilateral-tracing
  - BUG-36.3.7.7-01

must_haves:
  truths:
    - "An orchestrator can run `hermes kanban swarm \"Design a multi-region failover plan\" --workers researcher,architect,sre --verifier reviewer --synthesizer writer` and observe one root card (status=done, ended_at set), three worker cards (status=todo, parents=[root]), one verifier card (status=todo, parents=[w1,w2,w3]), and one synthesizer card (status=todo, parents=[verifier]) — all created in a single atomic transaction."
    - "An LLM in orchestrator mode can call the registered `kanban_swarm` tool with `{goal, workers, verifier?, synthesizer?, blackboard?, ...}` and receive `{root_id, worker_ids, verifier_id?, synthesizer_id?, blackboard_event_id?}` JSON, or a structured rejection envelope on failure."
    - "All 4 topology shapes (P1 fan-out / fan-out+verifier / full 4-tier §664 / P3 quorum) succeed and produce the exact DAG specified by D-topology-shapes; an invalid assignee on ANY card rolls back the whole graph (zero rows in tasks/task_links/task_events/task_comments)."
    - "A blackboard JSON arg, when present, is persisted as exactly one `task_comments` row on the root card with `author='swarm'` and `body=serde_json::to_string(&blackboard)?`; re-invocation with the same `idempotency_key` returns the existing `SwarmGraphIds` without inserting any new rows."
    - "`/kanban swarm <anything>` from any gateway slash surface routes to the deferred-subverb message instead of the unknown-subverb typo-suggest (the string `\"swarm\",` appears in DEFERRED_KANBAN_SUBVERBS)."
    - "`docs/kanban/reference.md` §14, §200, and §664 are reconciled — the 9→10 LLM tool narrative is updated, the §200 table has a `kanban_swarm` row, and the §664 `*(deferred to Phase 36.3.7.7)*` v1-NOTE is replaced with `Shipped in Phase 36.3.7.7.`."
    - "`crates/ironhermes-kanban/src/store.rs::create_task` and `crates/ironhermes-kanban/src/worker_spawn.rs::build_kanban_worker_env` are byte-unchanged — the swarm primitive sibling-additions never modify the existing single-card create path or the 9-env spawn contract."
  artifacts:
    - path: "crates/ironhermes-kanban/src/types.rs"
      provides: "Public structs SwarmGraphSpec, SwarmGraphIds, KanbanWorkerSpec"
      contains: "pub struct SwarmGraphSpec, pub struct SwarmGraphIds, pub struct KanbanWorkerSpec"
    - path: "crates/ironhermes-kanban/src/store.rs"
      provides: "KanbanStore::create_swarm sibling primitive with BEGIN IMMEDIATE atomic transaction + idempotency-replay reconstruction"
      contains: "pub fn create_swarm"
    - path: "crates/ironhermes-kanban/src/tools/swarm.rs"
      provides: "KanbanSwarmTool LLM tool with nested-oneOf workers schema + shared-field handling + rejection JSON shapes"
      contains: "pub struct KanbanSwarmTool, kanban_swarm"
      min_lines: 200
    - path: "crates/ironhermes-kanban/src/tools/mod.rs"
      provides: "10th tool registration via `registry.register(Box::new(KanbanSwarmTool::new(store.clone(), explicit_enable)))`"
      contains: "pub mod swarm;, pub use swarm::KanbanSwarmTool;, KanbanSwarmTool::new"
    - path: "crates/ironhermes-cli/src/kanban/mod.rs"
      provides: "KanbanCommands::Swarm variant with --workers / --workers-json mutually-exclusive flags + dispatch arm"
      contains: "Swarm {, KanbanCommands::Swarm"
    - path: "crates/ironhermes-cli/src/kanban/commands.rs"
      provides: "cmd_swarm async fn that resolves workers/blackboard union types and calls store.create_swarm"
      contains: "pub async fn cmd_swarm"
    - path: "crates/ironhermes-cli/tests/swarm_cli.rs"
      provides: "≥3 CLI parse-shape parity tests including the reference.md §664 verbatim example"
      contains: "swarm_verb_parses_flat_workers, swarm_verb_parses_reference_md_664_example"
    - path: "crates/ironhermes-kanban/tests/tools_smoke.rs"
      provides: "≥9 receiver-end swarm tests covering all 4 topologies + blackboard + idempotency + rollback + worker-form variants + root.ended_at invariant"
      contains: "// kanban_swarm tests, swarm_fan_out_only_creates_root_and_workers, swarm_full_4_tier_matches_reference_md_664"
    - path: "crates/ironhermes-core/src/commands/handlers.rs"
      provides: "DEFERRED_KANBAN_SUBVERBS contains \"swarm\" — gateway slash arm routes to deferred-subverb message"
      contains: "\"swarm\","
    - path: "docs/kanban/reference.md"
      provides: "Reconciled v1-NOTEs at §14 and §664 + new §200 table row for kanban_swarm"
      contains: "all 10 LLM tools, kanban_swarm, Shipped in Phase 36.3.7.7"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-36.3.7.7-01 through REQ-36.3.7.7-12 anchored to D-XX decisions from CONTEXT.md"
      contains: "REQ-36.3.7.7-01, REQ-36.3.7.7-12"
    - path: ".planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-01-SUMMARY.md"
      provides: "Phase SUMMARY with bilateral-tracing table + per-task verification outcomes + commits ledger"
      contains: "Phase 36.3.7.7"
  key_links:
    - from: "crates/ironhermes-kanban/src/tools/swarm.rs::execute"
      to: "crates/ironhermes-kanban/src/store.rs::create_swarm"
      via: "store.lock().await; store.create_swarm(spec)"
      pattern: "store\\.create_swarm\\("
    - from: "crates/ironhermes-cli/src/kanban/commands.rs::cmd_swarm"
      to: "crates/ironhermes-kanban/src/store.rs::create_swarm"
      via: "open_store().create_swarm(spec)"
      pattern: "store\\.create_swarm\\("
    - from: "crates/ironhermes-kanban/src/tools/mod.rs::register_kanban_tools"
      to: "crates/ironhermes-kanban/src/tools/swarm.rs::KanbanSwarmTool"
      via: "registry.register(Box::new(KanbanSwarmTool::new(store.clone(), explicit_enable)))"
      pattern: "KanbanSwarmTool::new"
    - from: "crates/ironhermes-core/src/commands/handlers.rs::DEFERRED_KANBAN_SUBVERBS"
      to: "store.deferred_subverb_message(\"swarm\")"
      via: "string-array membership check at handlers.rs:1285 routing"
      pattern: "\"swarm\","
---

<objective>
Ship `kanban_swarm` as the 10th LLM tool + `hermes kanban swarm` CLI verb + new `KanbanStore::create_swarm` sibling primitive — one atomic dispatch that materializes a multi-card kanban graph (root + N workers + optional verifier + optional synthesizer + optional blackboard comment) for orchestrator fan-out. Reconcile the `docs/kanban/reference.md` §664 v1-NOTE marker and grow the §14 / §200 surface narrative from 9 → 10 tools. Add `"swarm"` to `DEFERRED_KANBAN_SUBVERBS` so gateway slash invocations route to the CLI-redirect message instead of the unknown-subverb typo-suggest.

Purpose: closes the §664 v1-NOTE deferral; completes the orchestrator-mode tool surface promised in ROADMAP §622-631; mirrors the 36.3.7.6 sibling-method pattern (`insert_link_checked` alongside `insert_link`) to keep `create_task` untouched per D-no-create-task-reuse; preserves the 9-env-var worker spawn contract by routing root discovery through the existing `kanban_show` parents-walk (D-root-discovery).

Output: one new LLM tool file (`tools/swarm.rs`), one new CLI parity test file (`tests/swarm_cli.rs`), three new public types in `types.rs`, one new sibling method in `store.rs`, ≥9 new receiver tests appended to `tools_smoke.rs`, ≥3 new CLI parse tests in `swarm_cli.rs`, 1 line added to `DEFERRED_KANBAN_SUBVERBS`, 3 line-group edits to `docs/kanban/reference.md`, REQ-36.3.7.7-01..12 added to `.planning/REQUIREMENTS.md`, one SUMMARY.md, and one ROADMAP/STATE close-out commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-CONTEXT.md
@.planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-RESEARCH.md
@.planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-PATTERNS.md
@.planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-VALIDATION.md
@.planning/phases/36.3.7.6-kanban-llm-tool-surface-completion-heartbeat-link-unblock/36.3.7.6-01-SUMMARY.md
@.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/INV-36.3.7.md
@docs/kanban/reference.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from canonical refs + RESEARCH §A1-A8 + PATTERNS.md. -->
<!-- Use these directly — no codebase exploration needed for the parts already settled. -->

PUBLIC TYPES TO ADD in crates/ironhermes-kanban/src/types.rs:

  // Per-worker carrier — accepted from both flat (assignee-only) and rich (assignee/title/body) forms.
  pub struct KanbanWorkerSpec {
      pub assignee: String,
      pub title: Option<String>,   // None -> auto-generated "<goal> - worker {i+1} of {N}"
      pub body: Option<String>,    // None -> inherits tool-level shared body (which itself may be None)
  }

  // Input to KanbanStore::create_swarm. Mirrors CreateTaskOptions for shared fields.
  pub struct SwarmGraphSpec {
      pub goal: String,
      pub workers: Vec<KanbanWorkerSpec>,
      pub verifier: Option<String>,           // assignee
      pub synthesizer: Option<String>,        // assignee
      pub blackboard: Option<serde_json::Value>,  // opaque JSON; D-no-blackboard-validation
      pub workspace: Option<String>,
      pub skills: Option<Vec<String>>,
      pub tenant: Option<String>,
      pub priority: Option<i64>,
      pub max_runtime_seconds: Option<i64>,
      pub max_retries: Option<i64>,
      pub idempotency_key: Option<String>,
      pub created_by: Option<String>,         // populated from $HERMES_PROFILE
      pub body: Option<String>,               // shared body fallback for flat-form workers (Claude's discretion)
  }

  // Output of KanbanStore::create_swarm.
  pub struct SwarmGraphIds {
      pub root_id: String,
      pub worker_ids: Vec<String>,
      pub verifier_id: Option<String>,
      pub synthesizer_id: Option<String>,
      pub blackboard_event_id: Option<i64>,   // task_comments rowid; None when no blackboard arg
  }

SIGNATURE TO ADD in crates/ironhermes-kanban/src/store.rs:

  impl KanbanStore {
      pub fn create_swarm(&mut self, spec: SwarmGraphSpec) -> Result<SwarmGraphIds>;
  }

EXISTING SYMBOLS THE PLAN CONSUMES (do NOT modify):

  store.rs:181  Self::append_event_internal(conn: &Connection, task_id, run_id, kind, payload, now) -> Result<i64>
  store.rs:218  self.find_by_idempotency_key(key: &str) -> Result<Option<Task>>
  store.rs:243-267  CREATE_TASK INSERT shape (15 placeholders; ?13 = scheduled_at, ?14 = created_by, ?15 = created_at)
  store.rs:440  self.conn.transaction_with_behavior(TransactionBehavior::Immediate)
  ironhermes_core::profile::validate_profile_name(&str) -> Result<...>
  crate::paths::validate_dir_workspace(&str) -> Result<...>
  crate::tools::create::parse_max_runtime(&Value) -> Option<i64>   // already pub fn
  KanbanStore::new_id(prefix: &str) -> String  // produces "t_<hex8>" or "c_<hex8>"
  KanbanEventKind::Created  // existing variant reused for ALL swarm cards

LOCKED SCHEMA FOR `workers` PROPERTY (nested oneOf — Anthropic-compat per Phase 36.3.7.2):

  "workers": {
    "description": "Worker assignees. String array (flat - auto-title per card) or object array (rich, per-card title/body). minItems=1 for both branches.",
    "oneOf": [
      { "type": "array", "items": { "type": "string" }, "minItems": 1 },
      { "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "assignee": { "type": "string" },
            "title":    { "type": "string" },
            "body":     { "type": "string" }
          },
          "required": ["assignee"]
        },
        "minItems": 1
      }
    ]
  }

REJECTION JSON SHAPE (matches sibling 6+3-tool pattern):

  { "status": "rejected", "reason": "<slug>", "detail": "<message>" }

  Concrete reason slugs swarm uses:
    "invalid_assignee"       - validate_profile_name failed on any of root/workers/verifier/synthesizer
    "empty_workers"          - workers array empty after normalization
    "invalid_workers_shape"  - workers arg neither all-string nor all-object
    "invalid_blackboard"     - blackboard arg failed serde_json parse (CLI only, --blackboard string form)
    "missing_profile"        - $HERMES_PROFILE unset and no fallback (tool layer)
    "missing_workers"        - neither workers nor workers_json provided (CLI parse-time)
    "conflicting_workers"    - both workers and workers_json provided (CLI parse-time; handled by clap conflicts_with)

PER-CARD IDEMPOTENCY KEY SUFFIXES (when spec.idempotency_key = "k"):
    root          -> "k:root"
    worker i      -> "k:worker:{i}"  (0-indexed)
    verifier      -> "k:verifier"
    synthesizer   -> "k:synthesizer"

DEFERRED_KANBAN_SUBVERBS CURRENT STATE (handlers.rs:1143-1148, verified):

  const DEFERRED_KANBAN_SUBVERBS: &[&str] = &[
      "claim", "complete", "block", "unblock", "comment", "archive",
      "reclaim", "reassign", "assign", "link", "unlink",
      "init", "tail", "watch", "runs", "assignees", "dispatch",
      "stats", "log", "context", "gc", "daemon", "diagnostics",
  ];
  // 24 entries; "swarm" absent; this phase adds "swarm" on a new trailing line.

CLI TEST WRAPPER PATTERN (heartbeat_cli.rs:1-71 - DO NOT derive Debug):

  use clap::Parser;
  use ironhermes_cli::kanban::KanbanCommands;

  // NOTE: KanbanCommands does NOT derive Debug (carried from 36.3.7 baseline,
  // unchanged in this phase). Do NOT derive Debug on TestCli or TestSub.
  #[derive(Parser)]
  struct TestCli { #[command(subcommand)] cmd: TestSub, }

  #[derive(clap::Subcommand)]
  enum TestSub { Kanban { #[command(subcommand)] sub: KanbanCommands }, }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 — REQUIREMENTS): Add REQ-36.3.7.7-01..12 to .planning/REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current contents — mirror the existing convention exactly)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-VALIDATION.md (rows 42-53 — the 12 REQ IDs and their behaviors)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-CONTEXT.md (decisions section — every REQ must anchor to a D-XX decision)
  </read_first>
  <behavior>
    - Adding 12 REQs MUST NOT change any other content in REQUIREMENTS.md (no rewording of earlier phases).
    - Every REQ must reference at least one D-XX decision from CONTEXT.md, by ID.
    - REQ wording must be the verifiable phrasing from VALIDATION.md rows 42-53, not paraphrased.
  </behavior>
  <action>
    Append a new section `## Phase 36.3.7.7 - Kanban swarm helper` to `.planning/REQUIREMENTS.md` containing 12 requirements REQ-36.3.7.7-01 through REQ-36.3.7.7-12. For each REQ, write one row with: ID, behavior text from VALIDATION.md row N, anchoring D-XX decision IDs from CONTEXT.md. The 12 REQs are:
    - REQ-36.3.7.7-01: 4 topology shapes create correct DAGs (anchors: D-topology-shapes, D-atomic-transaction, D-root-status-done, D-root-ended-at).
    - REQ-36.3.7.7-02: Blackboard arg seeds task_comment on root with author="swarm" (anchors: D-blackboard-init, D-no-blackboard-validation).
    - REQ-36.3.7.7-03: Idempotency replay returns same SwarmGraphIds (anchors: D-idempotency-suffix-scheme, D-atomic-transaction).
    - REQ-36.3.7.7-04: Invalid assignee rolls back whole graph - zero rows in tasks/task_links/task_events/task_comments (anchors: D-atomic-transaction, D-no-create-task-reuse).
    - REQ-36.3.7.7-05: Rich worker form accepts per-card title/body (anchors: D-worker-shape, D-shared-vs-per-card-fields).
    - REQ-36.3.7.7-06: Flat worker form auto-titles each card as "<goal> - worker {i+1} of {N}" (anchors: D-worker-shape).
    - REQ-36.3.7.7-07: CLI flat-form and rich-form parse + mutually-exclusive errors (anchors: D-cli-verb-shape, D-cli-worker-flags).
    - REQ-36.3.7.7-08: `/kanban swarm` routes to deferred-subverb message - `"swarm"` present in DEFERRED_KANBAN_SUBVERBS (anchors: D-slash-deferred).
    - REQ-36.3.7.7-09: docs/kanban/reference.md grep gates pass - 0 "deferred to Phase 36.3.7.7", >=1 "all 10 LLM tools", >=2 "kanban_swarm" (anchors: D-docs-reconciliation).
    - REQ-36.3.7.7-10: spawn-contract file unchanged - `grep -c 'HERMES_KANBAN_SWARM_ROOT' crates/ironhermes-kanban/src/worker_spawn.rs` returns 0 (anchors: D-root-discovery, INV-36.3.7-07).
    - REQ-36.3.7.7-11: `store.create_task` byte-unchanged on the existing signature line (anchors: D-no-create-task-reuse).
    - REQ-36.3.7.7-12: Tool count is exactly 10 - `grep -c 'registry.register(Box::new(' crates/ironhermes-kanban/src/tools/mod.rs` returns 10 (anchors: D-tool-surface-mounts-store-directly, D-docs-reconciliation).
    Use markdown table OR bullet list matching the existing REQUIREMENTS.md convention. Do not invent rationale beyond the D-XX anchor.
  </action>
  <verify>
    <automated>grep -c 'REQ-36.3.7.7-' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'REQ-36.3.7.7-01' .planning/REQUIREMENTS.md` returns >= 1
    - `grep -c 'REQ-36.3.7.7-12' .planning/REQUIREMENTS.md` returns >= 1
    - `grep -cE 'REQ-36.3.7.7-(0[1-9]|1[0-2])' .planning/REQUIREMENTS.md` returns >= 12
    - `grep -c 'D-topology-shapes\|D-atomic-transaction\|D-idempotency-suffix-scheme\|D-blackboard-init\|D-worker-shape\|D-slash-deferred\|D-docs-reconciliation\|D-no-create-task-reuse\|D-root-discovery\|D-tool-surface-mounts-store-directly' .planning/REQUIREMENTS.md` returns >= 10 anchor mentions in the new section
    - Commit message: `docs(36.3.7.7-01): register REQ-36.3.7.7-01..12 in REQUIREMENTS.md`
  </acceptance_criteria>
  <done>The 12 phase REQs exist in REQUIREMENTS.md with D-XX anchors and the file is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 0 — test scaffolds): Scaffold new test files + section headers</name>
  <files>
    crates/ironhermes-cli/tests/swarm_cli.rs,
    crates/ironhermes-kanban/tests/tools_smoke.rs
  </files>
  <read_first>
    - crates/ironhermes-cli/tests/heartbeat_cli.rs (lines 1-71 — exact TestCli/TestSub shape, the no-Debug comment to copy verbatim)
    - crates/ironhermes-kanban/tests/tools_smoke.rs (lines 1-50 for imports, 35-47 for ENV_LOCK + make_store, 548-665 for the heartbeat+link section convention)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-PATTERNS.md (lines 292-388 — receiver-test layout convention; lines 626-720 — CLI parity test convention)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-RESEARCH.md (§A7 — make_store/ENV_LOCK/seed_plain_task verified shapes)
  </read_first>
  <behavior>
    - swarm_cli.rs has the no-Debug comment copied VERBATIM from heartbeat_cli.rs:13-18, then 3 `#[test]` stubs marked `#[ignore = "Wave 0 scaffold - implemented in Wave 5"]`.
    - tools_smoke.rs has the `// kanban_swarm tests` banner appended at end-of-file in the exact comment-frame style as line 548-550, followed by 9 `#[tokio::test]` stubs marked `#[ignore = "Wave 0 scaffold - implemented in Wave 6"]`.
    - Wave 0 scaffolds compile against the existing 9-tool surface; the scaffolds do NOT import `KanbanSwarmTool` yet (it does not exist until Wave 3) — Wave 6 inserts the import alongside test bodies.
  </behavior>
  <action>
    Create NEW file `crates/ironhermes-cli/tests/swarm_cli.rs` with: (1) the heartbeat_cli.rs:1-18 prelude verbatim including the `// NOTE: KanbanCommands does NOT derive Debug` comment block, (2) `TestCli`/`TestSub` declarations matching heartbeat_cli.rs:20-32, (3) three `#[test]` functions stubbed with `#[ignore = "Wave 0 scaffold - implemented in Wave 5"]` and `panic!("Wave 0 scaffold")` bodies. The three test names are exactly: `swarm_verb_parses_flat_workers`, `swarm_verb_parses_rich_workers_json`, `swarm_verb_parses_reference_md_664_example`. MODIFY `crates/ironhermes-kanban/tests/tools_smoke.rs` by appending at end-of-file a new section started with the exact 3-line comment-frame `// ---------------------------------------------------------------------------` / `// kanban_swarm tests (Phase 36.3.7.7 BUG-36.3.7.7-01)` / `// ---------------------------------------------------------------------------`, then add 9 `#[tokio::test] async fn` stubs marked `#[ignore = "Wave 0 scaffold - implemented in Wave 6"]` with `panic!("Wave 0 scaffold")` bodies. The exact 9 function names are: `swarm_fan_out_only_creates_root_and_workers`, `swarm_with_verifier_gates_on_all_workers`, `swarm_full_4_tier_matches_reference_md_664`, `swarm_with_synth_no_verifier_gates_synth_on_workers`, `swarm_blackboard_arg_seeds_initial_comment_on_root`, `swarm_idempotency_key_replays_return_same_graph`, `swarm_invalid_assignee_rolls_back_whole_graph`, `swarm_rich_worker_form_accepts_per_card_title_and_body`, `swarm_flat_worker_form_auto_titles_each_card`. Do NOT add `KanbanSwarmTool` to the `use` block yet — Wave 6 inserts the import.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cli --test swarm_cli -- --list 2>&1 | grep -c 'swarm_verb_parses' && cargo test -p ironhermes-kanban --test tools_smoke -- --list 2>&1 | grep -c 'swarm_'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ironhermes-cli --test swarm_cli -- --list` lists 3 tests with `swarm_verb_parses_` prefix
    - `cargo test -p ironhermes-kanban --test tools_smoke -- --list` lists at least 9 tests with `swarm_` prefix
    - `grep -c 'Wave 0 scaffold' crates/ironhermes-cli/tests/swarm_cli.rs` returns >= 3
    - `grep -c 'Wave 0 scaffold' crates/ironhermes-kanban/tests/tools_smoke.rs` returns >= 9
    - `grep -c 'KanbanCommands does NOT derive Debug' crates/ironhermes-cli/tests/swarm_cli.rs` returns 1
    - `grep -c '// kanban_swarm tests' crates/ironhermes-kanban/tests/tools_smoke.rs` returns 1
    - `cargo build --workspace` exits 0 (Wave 0 scaffolds compile against the existing 9-tool surface)
    - Commit message: `test(36.3.7.7-01): scaffold swarm_cli.rs + tools_smoke swarm section (Wave 0)`
  </acceptance_criteria>
  <done>Both test files have ignored stubs that compile under `cargo build --workspace`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (Wave 1 — types): Add SwarmGraphSpec, SwarmGraphIds, KanbanWorkerSpec to types.rs</name>
  <files>
    crates/ironhermes-kanban/src/types.rs,
    crates/ironhermes-kanban/src/lib.rs
  </files>
  <read_first>
    - crates/ironhermes-kanban/src/types.rs (full file - find the section where Task/TaskComment/TaskLink/TaskRun are declared; append after them; verify derive shapes - most are `#[derive(Debug, Clone, Serialize, Deserialize)]`)
    - crates/ironhermes-kanban/src/store.rs:31-100 (CreateTaskOptions shape - copy the `Default` impl pattern and per-field doc comments style for SwarmGraphSpec)
    - crates/ironhermes-kanban/src/lib.rs:68-73 (existing re-export lines - extend `pub use types::{...}` with the 3 new types)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-PATTERNS.md (lines 264-289 - re-export diff sketch; lines 95-97 - types placement rationale)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-CONTEXT.md (lines 23-26 - exact field set for each type; lines 170-176 - Claude's Discretion authorizing types.rs placement)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-RESEARCH.md (§Open Questions Q1 - `types.rs` is the recommended home)
  </read_first>
  <behavior>
    - `KanbanWorkerSpec` has 3 fields: `assignee: String` (required), `title: Option<String>`, `body: Option<String>`. Implements `Deserialize` so serde_json can parse the `--workers-json` rich form directly.
    - `SwarmGraphSpec` has the 14 fields listed in the interfaces block. `goal`/`workers` are required so consumers always set them.
    - `SwarmGraphIds` has the 5 fields listed in the interfaces block. Implements `Serialize` so the tool layer can return it directly via `json!({...})`.
    - All three types are `pub` and `#[derive(Debug, Clone)]` minimum. `SwarmGraphIds` adds `Serialize`; `KanbanWorkerSpec` adds `Deserialize, Serialize`.
    - lib.rs re-exports the 3 types so `ironhermes_kanban::SwarmGraphSpec`, `ironhermes_kanban::SwarmGraphIds`, `ironhermes_kanban::KanbanWorkerSpec` all resolve from external crates (CLI + tests).
  </behavior>
  <action>
    In `crates/ironhermes-kanban/src/types.rs`, append three new public structs at end-of-file: `KanbanWorkerSpec` (fields: `pub assignee: String`, `pub title: Option<String>`, `pub body: Option<String>`; derives `Debug, Clone, serde::Deserialize, serde::Serialize`), `SwarmGraphSpec` (fields listed in the interfaces block above: `goal`, `workers`, `verifier`, `synthesizer`, `blackboard`, `workspace`, `skills`, `tenant`, `priority`, `max_runtime_seconds`, `max_retries`, `idempotency_key`, `created_by`, `body` - all `pub`; derives `Debug, Clone`), and `SwarmGraphIds` (fields: `pub root_id: String`, `pub worker_ids: Vec<String>`, `pub verifier_id: Option<String>`, `pub synthesizer_id: Option<String>`, `pub blackboard_event_id: Option<i64>`; derives `Debug, Clone, serde::Serialize`). Add a doc comment on each struct that references the phase `(Phase 36.3.7.7 D-topology-shapes / D-worker-shape / D-blackboard-init)`. In `crates/ironhermes-kanban/src/lib.rs`, extend the `pub use types::{...}` re-export at line 71 to include `KanbanWorkerSpec, SwarmGraphIds, SwarmGraphSpec`.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-kanban 2>&1 | tail -5 && grep -cE 'pub struct (KanbanWorkerSpec|SwarmGraphSpec|SwarmGraphIds)' crates/ironhermes-kanban/src/types.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'pub struct (KanbanWorkerSpec|SwarmGraphSpec|SwarmGraphIds)' crates/ironhermes-kanban/src/types.rs` returns 3
    - The `pub use types::` line in lib.rs contains all of `KanbanWorkerSpec`, `SwarmGraphSpec`, `SwarmGraphIds`
    - `cargo build -p ironhermes-kanban` exits 0
    - `cargo build --workspace` exits 0
    - Commit message: `feat(36.3.7.7-01): add SwarmGraphSpec/SwarmGraphIds/KanbanWorkerSpec public types`
  </acceptance_criteria>
  <done>Three new public types compile in `ironhermes-kanban`, re-exported via `lib.rs`, workspace builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4 (Wave 2 — store primitive): Add KanbanStore::create_swarm sibling method</name>
  <files>crates/ironhermes-kanban/src/store.rs</files>
  <read_first>
    - crates/ironhermes-kanban/src/store.rs:202-284 (full create_task body - the INSERT INTO tasks shape this method inlines; line 243-267 column order)
    - crates/ironhermes-kanban/src/store.rs:180-196 (append_event_internal signature - takes `&Connection`, works on `&tx`)
    - crates/ironhermes-kanban/src/store.rs:218-223 (find_by_idempotency_key short-circuit - the replay pattern to mirror, returning early without entering the tx)
    - crates/ironhermes-kanban/src/store.rs:440-481 (insert_link_checked - sibling-method + BEGIN IMMEDIATE template; copy structure verbatim then replace inner body)
    - crates/ironhermes-kanban/src/store.rs:756-840 (complete_task - earlier BEGIN IMMEDIATE pattern that sets `ended_at`; relevant for root card)
    - crates/ironhermes-kanban/src/store.rs:350-369 (add_comment INSERT shape - used inline for blackboard comment)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-RESEARCH.md (§A1 - author column is free-text, no validate; §A4 - verbatim INSERT SQL with ?16 = ended_at for root; §Pitfall 1 - Risk 1 ended_at omission; §Pitfall 2 - borrow conflict on append_event_internal; §Pitfall 3 - replay reconstruction)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-PATTERNS.md (lines 28-105 - exact transaction-open + INSERT diff sketch)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-CONTEXT.md (lines 25-29 - D-atomic-transaction, D-no-create-task-reuse, D-idempotency-suffix-scheme; lines 60-79 - D-topology-shapes implementation rules; lines 137-143 - idempotency suffix table)
  </read_first>
  <behavior>
    - The new method must NEVER call `self.create_task(...)` or `self.insert_link_checked(...)`; both would re-enter `&mut self` and break the outer transaction. All writes use `tx.execute(...)` directly.
    - Idempotency replay short-circuits BEFORE opening the transaction (`find_by_idempotency_key(&format!("{}:root", key))?` returning `Some` triggers the replay branch).
    - Replay branch reads worker/verifier/synth IDs by looking up each per-card key with the locked suffix scheme: `find_by_idempotency_key("k:worker:0")`, `"k:worker:1"`, ..., `"k:verifier"`, `"k:synthesizer"`. Workers loop until `find_by_idempotency_key` returns None.
    - Root card: `status='done'`, `ended_at=now`, `assignee=spec.created_by.clone().ok_or_else(|| anyhow!("created_by required"))?` (caller provides $HERMES_PROFILE; tool layer validates non-empty before constructing spec).
    - Worker/verifier/synthesizer cards: `status='todo'`, `ended_at=NULL` (omit from INSERT column list so it defaults NULL).
    - `assignee` validation: call `ironhermes_core::profile::validate_profile_name(&card.assignee)?` for EVERY card BEFORE entering the transaction (matches `create_task:210-211`).
    - `workspace` validation: call `crate::paths::validate_dir_workspace(&w)?` once on the shared workspace before entering the tx (same as `create_task:215`).
    - Blackboard comment: when `spec.blackboard.is_some()`, after all card INSERTs but before commit, INSERT one `task_comments` row with `id = Self::new_id("c")`, `task_id = root_id`, `author = "swarm"` (string literal - free-text per A1), `body = serde_json::to_string(&blackboard_value)?`, `created_at = now`. Capture `tx.last_insert_rowid()` as `blackboard_event_id`.
    - Returns `Ok(SwarmGraphIds { root_id, worker_ids, verifier_id, synthesizer_id, blackboard_event_id })`. On any insert failure, `?` propagates and the transaction is dropped (automatic rollback by rusqlite).
    - Per-card idempotency keys: when `spec.idempotency_key = Some("k")`, root gets `Some("k:root")`, worker i gets `Some(format!("k:worker:{}", i))`, verifier gets `Some("k:verifier")`, synthesizer gets `Some("k:synthesizer")`. When `spec.idempotency_key` is None, all per-card keys are None.
  </behavior>
  <action>
    Add a new public method `pub fn create_swarm(&mut self, spec: SwarmGraphSpec) -> Result<SwarmGraphIds>` to `KanbanStore`, placed immediately after `insert_link_checked` (around store.rs:481) for thematic adjacency. Structure: (1) Reject empty workers with `KanbanError::Other(anyhow!("empty workers"))`. (2) Validate every assignee via `validate_profile_name` (root.created_by, each worker.assignee, optional verifier, optional synthesizer) BEFORE opening tx. (3) Validate workspace via `validate_dir_workspace` if `spec.workspace` is Some. (4) Compute `now = Self::now()` once. (5) Idempotency replay branch: if `spec.idempotency_key = Some(ref key)` and `self.find_by_idempotency_key(&format!("{}:root", key))?.is_some()`, reconstruct `SwarmGraphIds` by looking up each per-card key with the locked suffix scheme and return early WITHOUT opening a tx. (6) Open `let tx = self.conn.transaction_with_behavior(TransactionBehavior::Immediate)?`. (7) INSERT root card with verbatim 16-column INSERT (`?16 = ended_at = now`, `status = "done"`, idempotency_key per suffix scheme), call `Self::append_event_internal(&tx, &root_id, None, KanbanEventKind::Created, Some(&root_payload), now)?`. (8) For each worker (i, spec) in `spec.workers.iter().enumerate()`: compute title (from `spec.title` if Some else auto-generated `format!("{} - worker {} of {}", spec.goal, i+1, n)`), body (from `worker.body` if Some else `spec.body.clone()`), call the 15-column INSERT (no ended_at), append Created event, INSERT `task_links` row with `parent_id=root_id, child_id=worker_id`. (9) If verifier present: INSERT verifier card (15-col, status='todo'), append Created event, INSERT N task_links rows `(worker_i, verifier_id)`. (10) If synthesizer present: INSERT synth card (15-col, status='todo'), append Created event, INSERT task_links - if verifier present `(verifier_id, synth_id)` (1 row), else N `(worker_i, synth_id)` rows. (11) If `spec.blackboard.is_some()`: INSERT one `task_comments` row per the verbatim shape in `<read_first>` add_comment, capture `blackboard_event_id = Some(tx.last_insert_rowid())`. (12) `tx.commit()?`. (13) Return `Ok(SwarmGraphIds {...})`. All `task_links` INSERTs use `INSERT OR IGNORE INTO task_links (parent_id, child_id, created_at) VALUES (?1, ?2, ?3)` (plain - cycles impossible by construction for freshly-minted IDs per D-atomic-transaction scope fence). Call `Self::append_event_internal(&tx, ...)` - NEVER `self.append_event_internal(...)`. The payload for each Created event is `serde_json::json!({ "assignee": ..., "status": ..., "parents": parents_slice, "tenant": tenant_opt })` matching the existing `create_task:281` shape. Do NOT modify the existing `create_task` method body anywhere.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-kanban 2>&1 | tail -5 && grep -c 'pub fn create_swarm' crates/ironhermes-kanban/src/store.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub fn create_swarm' crates/ironhermes-kanban/src/store.rs` returns 1
    - `grep -c 'pub fn create_task' crates/ironhermes-kanban/src/store.rs` returns 1 (unchanged count)
    - `git diff develop -- crates/ironhermes-kanban/src/store.rs | grep -E '^[-+]\s*pub fn create_task'` returns empty (no lines added/removed touching create_task signature)
    - `grep -c 'transaction_with_behavior(TransactionBehavior::Immediate)' crates/ironhermes-kanban/src/store.rs` returns >= 3 (insert_link_checked + block_task + new create_swarm)
    - `grep -c 'INSERT INTO task_comments' crates/ironhermes-kanban/src/store.rs` returns >= 2 (add_comment + new blackboard insert in create_swarm)
    - `grep -c '"swarm"' crates/ironhermes-kanban/src/store.rs` returns >= 1 (the author literal)
    - `grep -c ':root\|:worker:\|:verifier\|:synthesizer' crates/ironhermes-kanban/src/store.rs` returns >= 4 (per-card idempotency suffix format strings)
    - `cargo build --workspace` exits 0
    - `cargo test -p ironhermes-kanban --lib` exits 0
    - Commit message: `feat(36.3.7.7-01): add KanbanStore::create_swarm sibling primitive (BUG-36.3.7.7-01 producer)`
  </acceptance_criteria>
  <done>`create_swarm` compiles, existing tests pass, `create_task` signature line is byte-stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5 (Wave 3 — LLM tool): Create crates/ironhermes-kanban/src/tools/swarm.rs</name>
  <files>crates/ironhermes-kanban/src/tools/swarm.rs</files>
  <read_first>
    - crates/ironhermes-kanban/src/tools/create.rs (entire file - structural template; pay close attention to lines 13-21 imports, 24-36 struct + new, 45-71 parse_max_runtime, 100-150 schema builder, 138-144 nested oneOf for max_runtime, 160-162 is_available gate, 178-183 validate_profile_name + rejection JSON shape, 240 created_by from env, 243-249 store lock + create call + return)
    - crates/ironhermes-kanban/src/types.rs (the 3 new types added in Task 3)
    - crates/ironhermes-kanban/src/store.rs (the new create_swarm signature added in Task 4)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-PATTERNS.md (lines 107-196 - exact swarm.rs analog mapping; includes struct, is_available, nested oneOf, validate_profile_name rejection, store lock call)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-CONTEXT.md (lines 23-29 - full tool input/output contract; lines 81-104 - D-worker-shape exact JSON schema; lines 105-108 - D-shared-vs-per-card-fields list)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-RESEARCH.md (§A2 - parse_max_runtime is already pub fn; §A8 - root-discovery via kanban_show parents-walk for the tool description)
  </read_first>
  <behavior>
    - File has exactly one `pub struct KanbanSwarmTool { store: Arc<TokioMutex<KanbanStore>>, explicit_enable: bool }` and `impl KanbanSwarmTool { pub fn new(...) }` matching `KanbanCreateTool` shape (PATTERNS lines 113-124).
    - `impl Tool for KanbanSwarmTool` exposes 6 methods: `name() -> "kanban_swarm"`, `toolset() -> "kanban"` (match create.rs:158 verbatim), `description()` (one paragraph referencing reference.md §740 patterns + §664 example + kanban_show parents-walk for root discovery), `schema()` returns ToolSchema with the locked `workers` nested-oneOf + all shared properties + optional verifier/synthesizer/blackboard/idempotency_key, `is_available()` mirrors create.rs:160-162 exactly, `execute(args: Value) -> Result<String>` does the normalization + spec construction + store call.
    - `execute()` flow: (1) Parse `goal: String` (reject if missing or non-string with `invalid_arg` reason). (2) Parse `workers: Value` - match on `Value::Array`: iterate; if all elements are `Value::String`, normalize to `Vec<KanbanWorkerSpec> { assignee: s, title: None, body: None }`; if all elements are `Value::Object`, parse each via `serde_json::from_value::<KanbanWorkerSpec>(elt.clone())?`; mixed -> reject `{"status":"rejected","reason":"invalid_workers_shape"}`; empty -> reject `{"status":"rejected","reason":"empty_workers"}`. (3) Parse optional `verifier`, `synthesizer`, `workspace`, `tenant`, `priority`, `max_retries`, `idempotency_key` as their respective Option<...> types. (4) Parse `max_runtime` via `args.get("max_runtime").and_then(parse_max_runtime)`. (5) Parse `skills` as `Option<Vec<String>>`. (6) Parse `blackboard` as `Option<serde_json::Value>` - never validate structure. (7) Determine `created_by` from `std::env::var("HERMES_PROFILE").ok()`; if None reject `{"status":"rejected","reason":"missing_profile"}`. (8) Validate each assignee via `validate_profile_name` BEFORE building the spec (each worker, verifier, synthesizer, created_by - same rejection JSON shape as create.rs:178-183 with `reason:"invalid_assignee"`). (9) Construct `SwarmGraphSpec { goal, workers: worker_specs, verifier, synthesizer, blackboard, workspace, skills, tenant, priority, max_runtime_seconds: parsed, max_retries, idempotency_key, created_by: Some(profile), body: None }`. (10) `let mut store = self.store.lock().await; let ids = store.create_swarm(spec).map_err(|e| anyhow::anyhow!(...))?;`. Map store errors to rejection JSON when possible: `KanbanError::Other` with "invalid assignee" -> `"invalid_assignee"` reason. (11) Return `Ok(serde_json::to_string_pretty(&json!({ "root_id": ids.root_id, "worker_ids": ids.worker_ids, "verifier_id": ids.verifier_id, "synthesizer_id": ids.synthesizer_id, "blackboard_event_id": ids.blackboard_event_id }))?)`.
    - Top of file imports: `use std::sync::Arc; use async_trait::async_trait; use ironhermes_core::ToolSchema; use ironhermes_tools::Tool; use serde_json::{Value, json}; use tokio::sync::Mutex as TokioMutex; use crate::store::KanbanStore; use crate::types::{KanbanWorkerSpec, SwarmGraphSpec}; use crate::tools::create::parse_max_runtime;`.
    - Schema: properties block includes `goal` (string, required), `workers` (locked nested-oneOf from the interfaces block above, required), `verifier` (string, optional), `synthesizer` (string, optional), `blackboard` (any-JSON - use `{ "description": "Opaque JSON seed comment on root card. ..." }` with no `type` constraint per D-no-blackboard-validation), `workspace` (string, optional), `skills` (array of string, optional), `tenant` (string, optional), `priority` (integer, optional), `max_runtime` (nested oneOf integer-or-string - copy create.rs:138-144 verbatim), `max_retries` (integer, optional), `idempotency_key` (string, optional). `required: ["goal", "workers"]`.
  </behavior>
  <action>
    Create NEW file `crates/ironhermes-kanban/src/tools/swarm.rs` ~280 lines mirroring `tools/create.rs` structure. Imports per the behavior block above. Define `KanbanSwarmTool` struct (2 fields) + `new` constructor. Implement `Tool` trait with: `name() -> "kanban_swarm"`, `toolset()` matching create.rs:158, `description()` one paragraph referencing reference.md §740 patterns + §664 example + kanban_show parents-walk, `schema()` returning `ToolSchema::new("kanban_swarm".to_string(), serde_json::json!({ "type": "object", "properties": { ...locked workers oneOf + all shared/optional props... }, "required": ["goal", "workers"] }))`, `is_available()` returning `std::env::var("HERMES_KANBAN_TASK").is_ok() || self.explicit_enable`, `execute(args)` implementing the 11-step flow in the behavior block. Use the rejection JSON literal shape `r#"{{"status":"rejected","reason":"<slug>","detail":"<detail>"}}"#` matching create.rs:178-183. ALL assignee `validate_profile_name` calls happen BEFORE constructing `SwarmGraphSpec`. The `workers` normalization MUST emit `"empty_workers"` for an empty array and `"invalid_workers_shape"` for a mixed string/object array. Do NOT impose any structural validation on the `blackboard` Value (D-no-blackboard-validation).
  </action>
  <verify>
    <automated>cargo build -p ironhermes-kanban 2>&1 | tail -5 && grep -c '"kanban_swarm"' crates/ironhermes-kanban/src/tools/swarm.rs</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/ironhermes-kanban/src/tools/swarm.rs` exists with `pub struct KanbanSwarmTool`
    - `grep -c 'fn name' crates/ironhermes-kanban/src/tools/swarm.rs` returns 1
    - `grep -c '"kanban_swarm"' crates/ironhermes-kanban/src/tools/swarm.rs` returns >= 1
    - `grep -c 'oneOf' crates/ironhermes-kanban/src/tools/swarm.rs` returns >= 2 (workers schema + max_runtime schema)
    - `grep -c 'validate_profile_name' crates/ironhermes-kanban/src/tools/swarm.rs` returns >= 1
    - `grep -c 'parse_max_runtime' crates/ironhermes-kanban/src/tools/swarm.rs` returns >= 1
    - `grep -c 'store.create_swarm' crates/ironhermes-kanban/src/tools/swarm.rs` returns >= 1
    - `grep -c 'invalid_workers_shape\|empty_workers\|invalid_assignee' crates/ironhermes-kanban/src/tools/swarm.rs` returns >= 3
    - `cargo build --workspace` exits 0 (the tool compiles but is not yet registered)
    - Commit message: `feat(36.3.7.7-01): add KanbanSwarmTool LLM tool (BUG-36.3.7.7-01 producer continues)`
  </acceptance_criteria>
  <done>swarm.rs compiles standalone; registration happens in Wave 4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6 (Wave 4 — registration): Wire KanbanSwarmTool into tools/mod.rs</name>
  <files>crates/ironhermes-kanban/src/tools/mod.rs, crates/ironhermes-kanban/src/lib.rs</files>
  <read_first>
    - crates/ironhermes-kanban/src/tools/mod.rs (full file - line 18-26 pub mod block, line 28-36 pub use block, line 43 doc comment, line 51-83 register_kanban_tools body with the 3 sibling-phase registrations)
    - crates/ironhermes-kanban/src/lib.rs (line 73 - the pub use tools::... line where KanbanSwarmTool re-export is added)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-PATTERNS.md (lines 198-260 - exact diff sketch for the 3 insertions; line 257-258 - doc-comment update from "all 6" or "all 9" to "all 10")
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-CONTEXT.md (lines 156-158 - D-tool-surface-mounts-store-directly)
  </read_first>
  <behavior>
    - Three additions in `tools/mod.rs`: (1) `pub mod swarm;` declaration in the `pub mod` block (line 22-23 area, alphabetical placement between `show` and `unblock`). (2) `pub use swarm::KanbanSwarmTool;` in the `pub use` block (alphabetical placement between `show` and `unblock`). (3) `registry.register(Box::new(KanbanSwarmTool::new(store.clone(), explicit_enable)));` at end of `register_kanban_tools` function body, preceded by a comment line `// Phase 36.3.7.7 BUG-36.3.7.7-01 - kanban_swarm (D-topology-shapes: atomic fan-out graph).`
    - The doc comment at line 43 (currently mentions a tool count from a prior phase) is updated to "all 10 kanban tools".
    - One addition in `lib.rs`: `KanbanSwarmTool` is added to the existing `pub use tools::...` re-export so external crates can resolve `ironhermes_kanban::KanbanSwarmTool`.
    - `grep -c 'registry.register(Box::new(' crates/ironhermes-kanban/src/tools/mod.rs` MUST equal exactly 10 after the change (the tool-count verifier gate REQ-36.3.7.7-12).
  </behavior>
  <action>
    In `crates/ironhermes-kanban/src/tools/mod.rs`: (1) Add `pub mod swarm;` in alphabetical order in the `pub mod` declaration block. (2) Add `pub use swarm::KanbanSwarmTool;` in alphabetical order in the `pub use` block. (3) Update the doc comment that currently mentions the tool count from the prior phase value to "all 10 kanban tools" so future readers see the correct count. (4) At the end of `register_kanban_tools`, append the new registration block, preceded by a comment line `// Phase 36.3.7.7 BUG-36.3.7.7-01 - kanban_swarm (D-topology-shapes: atomic fan-out graph).` followed by `registry.register(Box::new(KanbanSwarmTool::new(store.clone(), explicit_enable)));` formatted to match the existing 3-line wrapped style at lines 69-82 if that style is in use. In `crates/ironhermes-kanban/src/lib.rs`, add `KanbanSwarmTool` to the tools re-export so it is accessible as `ironhermes_kanban::KanbanSwarmTool` from external crates.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -5 && grep -c 'registry.register(Box::new(' crates/ironhermes-kanban/src/tools/mod.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'registry.register(Box::new(' crates/ironhermes-kanban/src/tools/mod.rs` returns exactly 10
    - `grep -c 'pub mod swarm;' crates/ironhermes-kanban/src/tools/mod.rs` returns 1
    - `grep -c 'pub use swarm::KanbanSwarmTool' crates/ironhermes-kanban/src/tools/mod.rs` returns 1
    - `grep -c 'KanbanSwarmTool::new' crates/ironhermes-kanban/src/tools/mod.rs` returns 1
    - `grep -c 'all 10 kanban tools\|all 10 LLM tools' crates/ironhermes-kanban/src/tools/mod.rs` returns >= 1
    - `grep -c 'KanbanSwarmTool' crates/ironhermes-kanban/src/lib.rs` returns >= 1
    - `cargo build --workspace` exits 0
    - Commit message: `feat(36.3.7.7-01): register KanbanSwarmTool as 10th LLM tool (BUG-36.3.7.7-01 producer wired)`
  </acceptance_criteria>
  <done>KanbanSwarmTool is registered, tool count is exactly 10, lib.rs re-exports the type.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7 (Wave 5 — CLI verb): Add KanbanCommands::Swarm + cmd_swarm + dispatch arm</name>
  <files>
    crates/ironhermes-cli/src/kanban/mod.rs,
    crates/ironhermes-cli/src/kanban/commands.rs
  </files>
  <read_first>
    - crates/ironhermes-cli/src/kanban/mod.rs (full file - lines 36-88 Create variant, lines 213-224 Heartbeat variant for the minimal-variant comment style, lines 490-500 area for the dispatch arms)
    - crates/ironhermes-cli/src/kanban/commands.rs (lines 81-142 cmd_create body - open_store, parse helpers, output formatting; lines 238-243 cmd_link minimal handler; locate `profile_from_env`/`open_store` helper functions for reuse)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-PATTERNS.md (lines 390-496 - exact KanbanCommands::Swarm field set + clap conflicts_with attributes + dispatch arm sketch; lines 498-622 - cmd_swarm body skeleton)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-CONTEXT.md (lines 24-25 - full CLI variant signature including all 15 fields; lines 105-108 - D-cli-worker-flags mutual-exclusion; lines 149-151 - D-cli-verb-shape pretty output format)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-RESEARCH.md (§Pitfall 5 - clap does NOT comma-split; §Pitfall 6 - resolve blackboard_file before SwarmGraphSpec)
  </read_first>
  <behavior>
    - New variant `Swarm` placed after `Heartbeat` (so it lives near the recent additions, not buried in the middle of the file). 15 fields per the interfaces block. `workers` and `workers_json` use clap `conflicts_with = "..."` (mutual exclusion at parse time); `blackboard` and `blackboard_file` likewise. At least one of `workers`/`workers_json` MUST be present - runtime check in `cmd_swarm` (Pitfall 5 framing).
    - Dispatch arm added in `handle_kanban_command` (around line 490-500) that calls `commands::cmd_swarm(...)` with all 15 destructured fields.
    - `cmd_swarm` signature accepts the 15 args, opens default store, normalizes workers and blackboard, builds `SwarmGraphSpec`, calls `store.create_swarm(spec)`, prints pretty or `--json` output.
    - Pretty output format (from CONTEXT.md D-cli-verb-shape): `Created swarm {root_id}: {N} workers[, verifier {vid}][, synth {sid}][ (+blackboard)]` - bracketed sections only when corresponding ID present.
    - `--json` output: full SwarmGraphIds via `serde_json::to_string_pretty(&ids)?`.
    - Worker normalization (Pitfall 5): `workers: Option<String>` is comma-split - `flat.split(',').map(str::trim).filter(|s| !s.is_empty())`. `workers_json: Option<String>` is `serde_json::from_str::<Vec<KanbanWorkerSpec>>(rich)?`.
    - Blackboard normalization (Pitfall 6): if `blackboard_file: Option<PathBuf>` is Some, read file via `std::fs::File::open(path)?` + `serde_json::from_reader(file)?`; if `blackboard: Option<String>` is Some, `serde_json::from_str(&s)?`. Both go into `SwarmGraphSpec.blackboard`.
    - `created_by` comes from `profile_from_env()` (already defined in commands.rs - reuse directly).
    - `max_runtime` is parsed via the existing CLI duration helper (`parse_duration_secs` - same helper `cmd_create` uses; commands.rs already has it).
  </behavior>
  <action>
    In `crates/ironhermes-cli/src/kanban/mod.rs`: append a new `Swarm` variant to `KanbanCommands` enum, placed after the `Heartbeat` variant. Use clap-derive attributes for the 15 fields per the PATTERNS.md lines 437-475 sketch. Mark `workers` with `#[arg(long, conflicts_with = "workers_json")]` and `workers_json` with `#[arg(long, conflicts_with = "workers")]`; mark `blackboard` and `blackboard_file` likewise. Add a top doc comment that points at `docs/kanban/reference.md` §664 (D-cli-help-cross-reference). Add the dispatch arm in `handle_kanban_command` after the existing `Heartbeat` arm, preceded by comment `// Phase 36.3.7.7 BUG-36.3.7.7-01 - kanban swarm (D-topology-shapes).`; call `commands::cmd_swarm(...)` with all 15 fields. In `crates/ironhermes-cli/src/kanban/commands.rs`: add the imports `use ironhermes_kanban::{KanbanWorkerSpec, SwarmGraphSpec, SwarmGraphIds};` at the top (or extend an existing `use ironhermes_kanban::{...}` line). Add `pub async fn cmd_swarm(...)` with the 15-argument signature listed in CONTEXT.md and PATTERNS.md. Body sequence: (1) `let mut store = open_store()?;`. (2) Worker normalization with clap conflicts_with handled (`(Some(flat), None)`, `(None, Some(rich))`, both None -> `anyhow::bail!("--workers or --workers-json is required")`). (3) Blackboard normalization with `(Some, None) / (None, Some) / (None, None)`. (4) `let max_runtime_seconds = max_runtime.as_deref().map(parse_duration_secs).transpose()?;`. (5) Build `SwarmGraphSpec { ... }` populating all fields including `created_by: Some(profile_from_env())` and `body: None`. (6) `let ids = store.create_swarm(spec).context("Failed to create swarm")?;`. (7) If `json`, print `serde_json::to_string_pretty(&ids)?`; else print the pretty format from D-cli-verb-shape with each optional section bracketed. (8) `Ok(0)`.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -5 && grep -c 'Swarm {' crates/ironhermes-cli/src/kanban/mod.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Swarm {' crates/ironhermes-cli/src/kanban/mod.rs` returns >= 2 (variant decl + dispatch destructure - matches the Heartbeat pattern of `Heartbeat {` = 2)
    - `grep -c 'KanbanCommands::Swarm' crates/ironhermes-cli/src/kanban/mod.rs` returns >= 1
    - `grep -c 'pub async fn cmd_swarm' crates/ironhermes-cli/src/kanban/commands.rs` returns 1
    - `grep -c 'store.create_swarm' crates/ironhermes-cli/src/kanban/commands.rs` returns 1
    - `grep -c 'conflicts_with' crates/ironhermes-cli/src/kanban/mod.rs` returns >= 4 (workers + workers_json + blackboard + blackboard_file)
    - `grep -c 'SwarmGraphSpec' crates/ironhermes-cli/src/kanban/commands.rs` returns >= 1
    - `cargo build --workspace` exits 0
    - `cargo build -p ironhermes-cli --bin ironhermes` exits 0
    - Commit message: `feat(36.3.7.7-01): add hermes kanban swarm CLI verb (BUG-36.3.7.7-01 CLI parity)`
  </acceptance_criteria>
  <done>CLI variant + dispatch arm + handler function all compile; `hermes kanban swarm --help` renders (verified in Wave 6 tests).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8 (Wave 6 — receiver tests): Fill 10 swarm tests in tools_smoke.rs</name>
  <files>crates/ironhermes-kanban/tests/tools_smoke.rs</files>
  <read_first>
    - crates/ironhermes-kanban/tests/tools_smoke.rs (the Wave 0 scaffolds added in Task 2 - replace each `panic!("Wave 0 scaffold")` body; also re-read lines 548-665 for the heartbeat+link reference convention)
    - crates/ironhermes-kanban/src/tools/swarm.rs (the tool created in Task 5 - get exact rejection slug strings)
    - crates/ironhermes-kanban/src/store.rs (the new create_swarm signature added in Task 4 - get exact field names for SwarmGraphSpec construction in tests)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-VALIDATION.md (rows 42-51 - exact REQ-to-test mapping; test names must satisfy the listed `cargo test -p ironhermes-kanban -- swarm_...` selectors)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-CONTEXT.md (lines 28-29 - exact 9 receiver test names; lines 60-79 - D-topology-shapes; lines 110-123 - D-blackboard-init expectations)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-RESEARCH.md (§A7 - make_store / ENV_LOCK / seed_plain_task helper shapes; §Pitfall 1 - root.ended_at invariant; §Risk 1 - swarm_root_card_has_ended_at_set test)
  </read_first>
  <behavior>
    - Add `KanbanSwarmTool` to the `use ironhermes_kanban::tools::{...}` import (or `use ironhermes_kanban::tools::swarm::KanbanSwarmTool`) at the top of the file so the swarm tests can construct the tool.
    - Add `KanbanWorkerSpec, SwarmGraphSpec` to the `use ironhermes_kanban::{...}` import (for tests that call `store.create_swarm(spec)` directly without the tool wrapper).
    - Every test starts with `let _env_guard = ENV_LOCK.lock().unwrap_or_else(|e| e.into_inner());`.
    - Tests that set/remove env vars use `unsafe { std::env::set_var(...) }` / `unsafe { std::env::remove_var(...) }`.
    - Add a helper `fn seed_swarm_graph(store: &mut KanbanStore, goal: &str, workers: &[&str]) -> SwarmGraphIds` declared inside the swarm test section (mirrors `seed_plain_task` at tools_smoke.rs:624-627) to avoid repeating spec-construction across the 9 tests.
    - All 10 tests are `#[tokio::test] async fn` (no longer `#[ignore]` - remove the Wave 0 `#[ignore]` annotation).
    - Behavior expectations per test (matching VALIDATION.md REQs):
      - `swarm_fan_out_only_creates_root_and_workers` (REQ-01 shape 1): no verifier, no synth; assert root status='done' + ended_at set + 3 workers status='todo' + 3 task_links rows + 4 Created events.
      - `swarm_with_verifier_gates_on_all_workers` (REQ-01 shape 2): assert verifier card status='todo', 3 worker->verifier task_links rows, no synth row.
      - `swarm_full_4_tier_matches_reference_md_664` (REQ-01 shape 3): exact §664 example - workers researcher/architect/sre, verifier reviewer, synthesizer writer; assert 5 cards total + 3 root-worker + 3 worker-verifier + 1 verifier-synth links.
      - `swarm_with_synth_no_verifier_gates_synth_on_workers` (REQ-01 shape 4): synth's parents are the N workers directly; assert 3 worker->synth links and no verifier row.
      - `swarm_blackboard_arg_seeds_initial_comment_on_root` (REQ-02): seed structured JSON `{"shared_context":"x","nested":{"a":1,"b":[1,2,3]}}`; assert exactly one `task_comments` row on root with `author='swarm'` and body round-trips via `serde_json::from_str::<Value>` equal to seeded value.
      - `swarm_idempotency_key_replays_return_same_graph` (REQ-03): call `create_swarm` twice with same `idempotency_key="k"`; assert SwarmGraphIds match (root_id + worker_ids slice equal); assert no duplicate rows in tasks/task_links/task_events.
      - `swarm_invalid_assignee_rolls_back_whole_graph` (REQ-04): pass worker with assignee containing invalid characters (e.g., `"bad@@user"`); assert error returned; assert `SELECT COUNT(*) FROM tasks` returned 0 new rows AND `SELECT COUNT(*) FROM task_links` returned 0 new rows AND `SELECT COUNT(*) FROM task_events` returned 0 new rows AND `SELECT COUNT(*) FROM task_comments` returned 0 new rows.
      - `swarm_rich_worker_form_accepts_per_card_title_and_body` (REQ-05): construct workers with explicit title/body; assert created cards have those exact titles/bodies (not auto-generated).
      - `swarm_flat_worker_form_auto_titles_each_card` (REQ-06): construct workers with title=None; assert created cards' titles equal `format!("{} - worker {} of {}", goal, i+1, n)`.
    - Add a 10th `#[tokio::test] async fn swarm_root_card_has_ended_at_set` (Risk 1 mitigation): assert `store.get_task(root_id)?.ended_at.is_some()` - surfaces the root.ended_at invariant explicitly.
  </behavior>
  <action>
    In `crates/ironhermes-kanban/tests/tools_smoke.rs`: (1) Update the top-of-file imports to include `KanbanSwarmTool`, `SwarmGraphSpec`, `KanbanWorkerSpec`, `SwarmGraphIds`. (2) In the `// kanban_swarm tests` section added in Task 2, replace each scaffold body with a real implementation per the behavior block expectations. Remove the `#[ignore = "Wave 0 scaffold ..."]` annotations on all 9 tests. (3) Inside the swarm section, declare `fn seed_swarm_graph(store: &mut KanbanStore, goal: &str, workers: &[&str]) -> SwarmGraphIds` that builds a flat-form `SwarmGraphSpec` (no verifier, no synth, no blackboard, `idempotency_key=None`, `created_by=Some("alice".to_string())`, `workspace=None`, etc.) and calls `store.create_swarm(spec).unwrap()`. (4) Add a 10th test `swarm_root_card_has_ended_at_set` per Risk 1 that asserts `store.get_task(&ids.root_id)?.ended_at.is_some()`. (5) Each test takes ENV_LOCK as its first line per the existing convention. (6) Tests that need `HERMES_PROFILE` set (any test that runs the LLM tool's execute() instead of calling `store.create_swarm` directly) use `unsafe { std::env::set_var("HERMES_PROFILE", "alice"); }` then `unsafe { std::env::remove_var("HERMES_PROFILE"); }` at the end. (7) For the `swarm_invalid_assignee_rolls_back_whole_graph` test, query `tasks`, `task_links`, `task_events`, `task_comments` counts BEFORE the failing call AND after; assert deltas are 0.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-kanban --test tools_smoke -- --test-threads=1 swarm_ 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ironhermes-kanban --test tools_smoke -- --test-threads=1 swarm_ 2>&1 | grep -c 'test result: ok'` returns 1
    - `cargo test -p ironhermes-kanban --test tools_smoke -- --test-threads=1 swarm_ 2>&1 | grep -cE '^test swarm_.* ok$'` returns >= 10
    - `grep -c 'Wave 0 scaffold' crates/ironhermes-kanban/tests/tools_smoke.rs` returns 0 (all scaffold markers removed)
    - `grep -c 'KanbanSwarmTool\|SwarmGraphSpec' crates/ironhermes-kanban/tests/tools_smoke.rs` returns >= 2
    - `cargo test --workspace --no-fail-fast -- --test-threads=1` exits 0 with zero new failures vs develop tip
    - Commit message: `test(36.3.7.7-01): implement 10 swarm receiver tests in tools_smoke (BUG-36.3.7.7-01 consumer)`
  </acceptance_criteria>
  <done>10 swarm receiver tests pass; rollback test confirms whole-graph atomicity; root.ended_at invariant verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 9 (Wave 6 — CLI parity tests): Fill 3 swarm_cli.rs parse tests</name>
  <files>crates/ironhermes-cli/tests/swarm_cli.rs</files>
  <read_first>
    - crates/ironhermes-cli/tests/swarm_cli.rs (the Wave 0 scaffold added in Task 2 - replace each `panic!("Wave 0 scaffold")` body)
    - crates/ironhermes-cli/tests/heartbeat_cli.rs (lines 33-71 - exact try_parse_from + match-arm pattern; replicate verbatim shape)
    - crates/ironhermes-cli/src/kanban/mod.rs (the `Swarm` variant added in Task 7 - get exact field names for destructuring)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-PATTERNS.md (lines 672-716 - three concrete test bodies)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-CONTEXT.md (lines 254-257 - the §664 verbatim command line that test 3 verifies)
  </read_first>
  <behavior>
    - Three tests, ALL `#[test]` (NOT `#[tokio::test]` - pure parse, no async I/O).
    - Test 1 `swarm_verb_parses_flat_workers`: parses `["hermes","kanban","swarm","my goal","--workers","a,b,c"]`; asserts `goal == "my goal"`, `workers == Some("a,b,c")`, `workers_json == None`.
    - Test 2 `swarm_verb_parses_rich_workers_json`: parses `["hermes","kanban","swarm","my goal","--workers-json","[{\"assignee\":\"a\"},{\"assignee\":\"b\",\"title\":\"T\"}]"]`; asserts `workers_json.as_deref().unwrap().starts_with("[")`, `workers == None`.
    - Test 3 `swarm_verb_parses_reference_md_664_example`: parses the verbatim §664 command line: `["hermes","kanban","swarm","Design a multi-region failover plan","--workers","researcher,architect,sre","--verifier","reviewer","--synthesizer","writer"]`; asserts `goal == "Design a multi-region failover plan"`, `workers == Some("researcher,architect,sre")`, `verifier == Some("reviewer")`, `synthesizer == Some("writer")`.
    - Removing the `#[ignore = "Wave 0 scaffold ..."]` annotation from each test.
    - No `Debug` derive on TestCli/TestSub (heartbeat_cli.rs:13 comment carried verbatim).
  </behavior>
  <action>
    In `crates/ironhermes-cli/tests/swarm_cli.rs`: replace each scaffold body with the test body sketched in PATTERNS.md lines 672-716. Remove the `#[ignore]` annotation from all three tests. Use `KanbanCommands::Swarm { goal, workers, workers_json, verifier, synthesizer, .. }` destructuring (with `..` to ignore the other 10 fields not under test). Match-arm fallback is a plain `_ => panic!("expected Swarm variant")` (no `{other:?}` because TestSub has no Debug).
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cli --test swarm_cli -- --test-threads=1 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ironhermes-cli --test swarm_cli -- --test-threads=1 2>&1 | grep -cE '^test swarm_verb_parses_.* ok$'` returns 3
    - `cargo test -p ironhermes-cli --test swarm_cli -- --test-threads=1 2>&1 | grep -c '3 passed'` returns 1
    - `grep -c 'Wave 0 scaffold' crates/ironhermes-cli/tests/swarm_cli.rs` returns 0
    - `grep -c '#\[ignore' crates/ironhermes-cli/tests/swarm_cli.rs` returns 0
    - `grep -c 'KanbanCommands::Swarm' crates/ironhermes-cli/tests/swarm_cli.rs` returns >= 3
    - `grep -c 'Design a multi-region failover plan' crates/ironhermes-cli/tests/swarm_cli.rs` returns 1 (the §664 example)
    - Commit message: `test(36.3.7.7-01): implement 3 CLI parity tests in swarm_cli (D-cli-verb-shape consumer)`
  </acceptance_criteria>
  <done>3 CLI parse tests pass including the verbatim §664 command line.</done>
</task>

<task type="auto">
  <name>Task 10 (Wave 7 — gateway slash): Add "swarm" to DEFERRED_KANBAN_SUBVERBS</name>
  <files>crates/ironhermes-core/src/commands/handlers.rs</files>
  <read_first>
    - crates/ironhermes-core/src/commands/handlers.rs (lines 1143-1148 - the array; lines 1285-1295 - the downstream routing that this entry triggers)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-CONTEXT.md (lines 125-132 - D-slash-deferred; line 127 - exact disposition matching the 36.3.7.6 link/unblock pattern)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-PATTERNS.md (lines 723-751 - exact 1-line diff sketch)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-RESEARCH.md (§A5 - verified placement; trailing-comma style; "heartbeat" intentionally absent)
  </read_first>
  <behavior>
    - Exactly 1 single line added: `    "swarm",` on a new line before the closing `];`.
    - No other changes to handlers.rs.
    - No new function added; no new dispatch arm - the existing routing at handlers.rs:1285 handles the new entry.
    - "heartbeat" remains absent (intentionally - it shipped as a real CLI verb in 36.3.7.6; do NOT add it).
  </behavior>
  <action>
    Modify `crates/ironhermes-core/src/commands/handlers.rs` line 1147 (or wherever the closing `];` of `DEFERRED_KANBAN_SUBVERBS` sits) by adding a new trailing line `    "swarm",` immediately before the `];`. The result is the existing 24-entry array becomes a 25-entry array. Do NOT touch any other line in the file. Do NOT add a comment or annotation (the upstream array has no per-entry comments).
  </action>
  <verify>
    <automated>grep -c '"swarm",' crates/ironhermes-core/src/commands/handlers.rs && cargo build --workspace 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"swarm",' crates/ironhermes-core/src/commands/handlers.rs` returns >= 1
    - `grep -c '"heartbeat",' crates/ironhermes-core/src/commands/handlers.rs` returns 0 (still intentionally absent - D-no-deferred-subverbs-change from 36.3.7.6 holds)
    - `cargo build --workspace` exits 0
    - `cargo test --workspace --no-fail-fast -- --test-threads=1 deferred 2>&1 | grep -c 'FAILED'` returns 0 (any deferred-subverb test that may exist still passes)
    - Commit message: `feat(36.3.7.7-01): add "swarm" to DEFERRED_KANBAN_SUBVERBS (D-slash-deferred)`
  </acceptance_criteria>
  <done>`"swarm",` is in the array; gateway slash `/kanban swarm` will route to the deferred-subverb message.</done>
</task>

<task type="auto">
  <name>Task 11 (Wave 8 — docs reconciliation): Update 3 line-groups in docs/kanban/reference.md + KANBAN_GUIDANCE</name>
  <files>
    docs/kanban/reference.md,
    crates/ironhermes-kanban/src/kanban_guidance.rs
  </files>
  <read_first>
    - docs/kanban/reference.md (line 14 - current v1-NOTE; lines 198-208 - full §200 tools table including exact column count of every existing row; lines 662-674 - §664 v1-NOTE block + the verbatim CLI example below it that MUST remain byte-stable)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-PATTERNS.md (lines 755-797 - exact before/after for each of 3 line-groups; note the 4-column-vs-3-column heartbeat/link row discrepancy at PATTERNS line 794-795)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-CONTEXT.md (lines 30-34 - D-docs-reconciliation; lines 159-168 - exact verifier-gate spec; lines 254-256 - §664 verbatim example to preserve)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-RESEARCH.md (§A3 - table is 3 columns; the heartbeat row's trailing `| Shipped in Phase 36.3.7.6. |` is a 4th column some rows acquired in 36.3.7.6 - planner replicates the trailing column style for kanban_swarm row to match siblings)
    - crates/ironhermes-kanban/src/kanban_guidance.rs (line 1 to end - locate the `pub const KANBAN_GUIDANCE: &str` literal; append the parents-walk paragraph BEFORE the closing `";`; do NOT use `format!` or `concat!` - INV-36.3.7-SG-10 forbids it)
    - .planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/INV-36.3.7.md (INV-36.3.7-SG-10 - KANBAN_GUIDANCE static-const invariant)
  </read_first>
  <behavior>
    - 3 docs/kanban/reference.md edits: (a) Line 14 change "all 9 LLM tools" -> "all 10 LLM tools" AND append `, kanban_swarm` to the inline list before the period. (b) Add a new row to the §200 table (insert after the `kanban_unblock` row at line ~208) matching the column shape of the heartbeat/link/unblock sibling rows: if they use the 4-column-with-shipping-note style observed in reference.md:204-208, write the new row as `| \`kanban_swarm\` | Create N parallel worker cards + optional verifier + optional synthesizer + blackboard root, in one atomic transaction. | \`goal\`, \`workers\` | Shipped in Phase 36.3.7.7. |`; if the sibling rows are 3-column, drop the trailing `| Shipped... |` cell. (c) Replace the line-664 `*(deferred to Phase 36.3.7.7 ...)*` v1-NOTE block (the entire blockquote `> **v1 NOTE:** ...`) with `> **v1 NOTE:** Shipped in Phase 36.3.7.7.` (single-line replacement). Preserve lines 666-672 (the CLI example fenced block) verbatim.
    - 1 KANBAN_GUIDANCE edit: append a new section to the existing `pub const KANBAN_GUIDANCE: &str = "..."` literal, BEFORE the closing `";`, using a string-concatenation-free approach: extend the existing literal with a continuation `\n\n## Swarm graph root discovery\n\nWorkers in a swarm graph discover the root card by calling kanban_show with their own task_id and reading the parent_handoffs field. The root card is the parent of each worker. Workers can then call kanban_show with the root_id to read the shared blackboard from the comments field.`. The literal MUST remain a single `&str` const without `format!`/`concat!`. Verifier: `grep -c 'format!(\|concat!(' crates/ironhermes-kanban/src/kanban_guidance.rs` returns 0.
    - Verifier gates (locked from CONTEXT.md D-docs-reconciliation):
      - `grep -c 'deferred to Phase 36.3.7.7' docs/kanban/reference.md` returns 0
      - `grep -c 'all 10 LLM tools' docs/kanban/reference.md` returns >= 1
      - `grep -c 'kanban_swarm' docs/kanban/reference.md` returns >= 2
      - `grep -c 'Shipped in Phase 36.3.7.7' docs/kanban/reference.md` returns >= 2 (§200 row + §664 NOTE)
  </behavior>
  <action>
    Modify `docs/kanban/reference.md`: (a) On line 14, change "all 9 LLM tools" to "all 10 LLM tools" and append `, kanban_swarm` immediately before the period at the end of the inline tool-name list. (b) After the existing `kanban_unblock` row at line 208, INSERT a new row that matches the column-count of the kanban_heartbeat/link/unblock sibling rows at lines 204-208. The kanban_swarm row content is: tool column `\`kanban_swarm\``, purpose column `Create N parallel worker cards + optional verifier + optional synthesizer + blackboard root, in one atomic transaction.`, required-params column `\`goal\`, \`workers\``, optional 4th shipping-note column `Shipped in Phase 36.3.7.7.` only if siblings have one. (c) Replace the line-664 blockquote `> **v1 NOTE:** \`kanban swarm\` is deferred to Phase 36.3.7.7 ...` (entire single-line blockquote) with `> **v1 NOTE:** Shipped in Phase 36.3.7.7.`. Preserve the CLI example fenced block at lines 666-672 byte-for-byte. Modify `crates/ironhermes-kanban/src/kanban_guidance.rs`: locate the `pub const KANBAN_GUIDANCE: &str = "..."` literal; insert before the closing `";` the new section text with leading `\n\n## Swarm graph root discovery\n\n` followed by the 3-sentence paragraph from the behavior block. The insertion is a single literal string continuation - NO `format!`, NO `concat!`, NO macro invocations.
  </action>
  <verify>
    <automated>bash -c "test $(grep -c 'deferred to Phase 36.3.7.7' docs/kanban/reference.md) -eq 0 && test $(grep -c 'all 10 LLM tools' docs/kanban/reference.md) -ge 1 && test $(grep -c 'kanban_swarm' docs/kanban/reference.md) -ge 2 && test $(grep -cE 'format!\\(|concat!\\(' crates/ironhermes-kanban/src/kanban_guidance.rs) -eq 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'deferred to Phase 36.3.7.7' docs/kanban/reference.md` returns 0
    - `grep -c 'all 10 LLM tools' docs/kanban/reference.md` returns >= 1
    - `grep -c 'kanban_swarm' docs/kanban/reference.md` returns >= 2
    - `grep -c 'Shipped in Phase 36.3.7.7' docs/kanban/reference.md` returns >= 2
    - `grep -cE 'format!\(|concat!\(' crates/ironhermes-kanban/src/kanban_guidance.rs` returns 0 (INV-36.3.7-SG-10 holds)
    - `grep -c 'Swarm graph root discovery' crates/ironhermes-kanban/src/kanban_guidance.rs` returns >= 1
    - `grep -c 'pub const KANBAN_GUIDANCE: &str' crates/ironhermes-kanban/src/kanban_guidance.rs` returns 1 (still a const)
    - `cargo test -p ironhermes-kanban --test invariants_36_3_7 -- kanban_guidance_is_static_const 2>&1 | grep -c 'ok'` returns >= 1 (INV-36.3.7-SG-10 static-grep gate still passes)
    - The verbatim CLI example block at reference.md:666-672 is byte-unchanged
    - Commit message: `docs(36.3.7.7-01): reconcile reference.md §14/§200/§664 v1-NOTE + KANBAN_GUIDANCE parents-walk note (BUG-36.3.7.7-01 docs)`
  </acceptance_criteria>
  <done>All 4 doc-reconciliation grep gates pass; INV-36.3.7-SG-10 invariant unaffected; verbatim §664 example preserved.</done>
</task>

<task type="auto">
  <name>Task 12 (Wave 9 — verifier gate): Run greppable phase-gates + workspace test sweep</name>
  <files></files>
  <read_first>
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-VALIDATION.md (full table - all 12 REQ acceptance gates)
    - .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-CONTEXT.md (§Docs reconciliation lines 159-168; §Out of scope lines 38-53)
    - .planning/phases/36.3.7.6-kanban-llm-tool-surface-completion-heartbeat-link-unblock/36.3.7.6-01-SUMMARY.md (lines 234-256 - Gate 10 + workspace-test pattern to mirror)
  </read_first>
  <behavior>
    - This is a verification-only task; it adds NO files and modifies NO code. It runs greppable gates + full-workspace tests + records outcomes for SUMMARY.md (Task 13).
    - Gates run in this order; ANY failure blocks the SUMMARY task and triggers iteration on the failing wave:
      1. `grep -c 'deferred to Phase 36.3.7.7' docs/kanban/reference.md` == 0
      2. `grep -c 'all 10 LLM tools' docs/kanban/reference.md` >= 1
      3. `grep -c 'kanban_swarm' docs/kanban/reference.md` >= 2
      4. `grep -c '"swarm",' crates/ironhermes-core/src/commands/handlers.rs` >= 1
      5. `grep -c 'HERMES_KANBAN_SWARM_ROOT' crates/ironhermes-kanban/src/worker_spawn.rs` == 0  (REQ-36.3.7.7-10 spawn-contract byte-stable)
      6. `grep -c 'registry.register(Box::new(' crates/ironhermes-kanban/src/tools/mod.rs` == 10  (REQ-36.3.7.7-12 tool count)
      7. `grep -c 'pub fn create_swarm' crates/ironhermes-kanban/src/store.rs` == 1
      8. `grep -c 'pub fn create_task' crates/ironhermes-kanban/src/store.rs` == 1  (REQ-36.3.7.7-11 byte-unchanged)
      9. `git diff develop -- crates/ironhermes-kanban/src/worker_spawn.rs | wc -l` == 0  (spawn contract byte-stable)
      10. `git diff develop -- crates/ironhermes-kanban/src/store.rs | grep -E '^[-+]\s*pub fn create_task'` returns empty  (create_task signature byte-unchanged)
      11. `cargo build --workspace` exits 0
      12. `cargo test --workspace --no-fail-fast -- --test-threads=1` exits 0 with zero NEW failures vs develop tip
      13. `cargo test -p ironhermes-kanban --test invariants_36_3_7 -- --test-threads=1` exits 0  (INV-36.3.7-SG-01..SG-10 all still pass)
      14. Crate-isolation fence: `grep -cE '^ironhermes-gateway\s*=' crates/ironhermes-kanban/Cargo.toml` == 0
  </behavior>
  <action>
    Run all 14 gates listed in the behavior block in sequence. Record outcomes (pass/fail + actual count for grep gates) in a scratch buffer for Task 13 to embed in SUMMARY.md. If ANY gate fails, do NOT proceed to Task 13 - investigate which prior wave introduced the regression, fix it with a new commit (e.g., `fix(36.3.7.7-01): <gate-N> regression - <fix description>`), and re-run gates 1-14. The whole gate sweep is iterated until all 14 pass.
  </action>
  <verify>
    <automated>bash -c "test $(grep -c 'deferred to Phase 36.3.7.7' docs/kanban/reference.md) -eq 0 && test $(grep -c 'registry.register(Box::new(' crates/ironhermes-kanban/src/tools/mod.rs) -eq 10 && test $(grep -c 'HERMES_KANBAN_SWARM_ROOT' crates/ironhermes-kanban/src/worker_spawn.rs) -eq 0 && cargo build --workspace 2>&1 | tail -3 | grep -q 'Finished\\|finished'"</automated>
  </verify>
  <acceptance_criteria>
    - All 14 gates listed in the behavior block return their expected values
    - `cargo build --workspace` exits 0
    - `cargo test --workspace --no-fail-fast -- --test-threads=1` exits 0 with zero NEW failures vs develop tip
    - `cargo test -p ironhermes-kanban --test invariants_36_3_7 -- --test-threads=1` exits 0
    - No fix-up commits required (or, if required, they exist with `fix(36.3.7.7-01): ...` prefixes and the gates pass after them)
    - No commit message for this task itself (verification only); fix-up commits are produced only if gates fail
  </acceptance_criteria>
  <done>All 14 phase gates pass; the workspace test sweep is green; INV-36.3.7-SG-01..10 still pass.</done>
</task>

<task type="auto">
  <name>Task 13 (Wave 10 — SUMMARY): Write 36.3.7.7-01-SUMMARY.md + close STATE.md + ROADMAP.md</name>
  <files>
    .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-01-SUMMARY.md,
    .planning/STATE.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/phases/36.3.7.6-kanban-llm-tool-surface-completion-heartbeat-link-unblock/36.3.7.6-01-SUMMARY.md (full file - the structural template; mirror the bilateral-tracing table, the verification-gates table, the locked-decisions table, the commits ledger, and the self-check section)
    - .planning/STATE.md (current contents - find the "stopped_at" / "last_updated" / "last_activity" / "progress" fields to update; locate the "Recent commits" section to extend)
    - .planning/ROADMAP.md (line 622-631 area - the Phase 36.3.7.7 entry; needs "Plans:" filled in + phase verdict appended after this phase finalizes)
    - $HOME/.claude/get-shit-done/templates/summary.md (template structure)
  </read_first>
  <behavior>
    - SUMMARY.md follows the 36.3.7.6 template structure: frontmatter (phase, plan, subsystem, tags, dependency_graph, tech_stack, key_files, decisions, metrics), title, bilateral-tracing table (BUG-36.3.7.7-01 producer + consumer at each site), schema/code changes excerpts (the verbatim INSERT shape for root card with `ended_at`; the new types; the LLM tool struct sig; the CLI verb shape), tests added table (10 receiver + 3 CLI), verification gates outcomes (14 gates from Task 12), locked CONTEXT decisions table (16 D-XX decisions from CONTEXT.md mapped to file:line where observable), crate-isolation audit, out-of-scope verified-untouched table, commits ledger, deviations from plan (if any from Task 12 fix-ups), self-check section.
    - STATE.md updates: bump `last_updated` to 2026-05-30 timestamp, set `stopped_at: "Phase 36.3.7.7 closed PASS"`, update `last_activity` to a one-paragraph summary mirroring the 36.3.7.6 style. Bump `progress.completed_phases` to 18 (was 17) and `progress.completed_plans` to 82 (was 81). Append a "Recent commits" line listing the 12-ish phase commits.
    - ROADMAP.md updates: replace the `**Requirements**: TBD ...` line at §622-631 with the 12 REQ IDs comma-separated; replace `**Plans:** TBD` with `**Plans:** 1 plan`; replace the `Plans:` bulleted list `- [ ] TBD ...` with `- [x] 36.3.7.7-01-PLAN.md - Kanban swarm helper (10th LLM tool + CLI verb + docs reconciliation)`; append a `**Phase verdict:** Phase 36.3.7.7 CLOSED PASS. 14/14 phase-level gates verified. ...` paragraph after the Plans block, mirroring the 36.3.7.6 verdict paragraph at line 620.
  </behavior>
  <action>
    Create `.planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-01-SUMMARY.md` using the 36.3.7.6 SUMMARY.md as a structural template. Frontmatter: phase "36.3.7.7", plan "01", subsystem "ironhermes-kanban + ironhermes-cli + ironhermes-core + docs", tags listed in this plan's frontmatter, dependency_graph.requires includes "phase 36.3.7.6" and the symbol set from RESEARCH.md (append_event_internal, find_by_idempotency_key, transaction_with_behavior, validate_profile_name, parse_max_runtime), dependency_graph.provides lists "kanban_swarm-llm-tool", "create_swarm-store-method", "hermes-kanban-swarm-cli-verb", "SwarmGraphSpec/SwarmGraphIds/KanbanWorkerSpec-types", "10-tool-llm-surface", dependency_graph.affects lists all 13 modified files, tech_stack.added = [], tech_stack.patterns lists the sibling-method + tool-mounts-store + bilateral-tracing patterns inherited from 36.3.7.6, key_files.created lists [tools/swarm.rs, tests/swarm_cli.rs, types.rs (sections), SUMMARY.md], key_files.modified lists the rest, decisions lists the 16 D-XX decisions from CONTEXT.md, metrics with duration/tasks/files/commits/tests-added counts. Title section: 1-paragraph product narrative matching 36.3.7.6's opening paragraph. Bilateral-tracing table: 5+ rows (BUG-36.3.7.7-01 producer/consumer for store + tool + cli + tests + docs). Code-changes section: excerpts for the new types (3 structs), the create_swarm signature + transaction-open + INSERT INTO tasks (16-col for root, 15-col for workers), the LLM tool's `name() -> "kanban_swarm"` line, the CLI Swarm variant signature, the 3 reference.md edits. Tests-added table: 10 tools_smoke rows + 3 swarm_cli rows with one-line behavior each. Verification-gates table: 14 rows (from Task 12). Locked-decisions table: 16 D-XX rows mapped to file:line where observable. Crate-isolation audit: `grep -cE '^ironhermes-gateway\s*=' crates/ironhermes-kanban/Cargo.toml` = 0. Out-of-scope verified-untouched table: store.create_task, worker_spawn::build_kanban_worker_env, KanbanStoreWriter trait, KanbanEventKind enum, task_links DDL, multi-board surfaces, @mention parser, slash arm, no new external deps. Commits ledger: 11-13 commits with SHAs (executor fills SHAs at write time). Deviations section: list any Task-12 fix-up commits or "No deviations". Self-check section: list all created/modified files + commit SHAs, confirming they exist. Then update `.planning/STATE.md` per the behavior block (timestamp, stopped_at, last_activity, progress counters, recent commits). Then update `.planning/ROADMAP.md` per the behavior block (Requirements line, Plans line, plan checklist, verdict paragraph). Commit all three files together as `docs(state,roadmap): close Phase 36.3.7.7`.
  </action>
  <verify>
    <automated>test -f .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-01-SUMMARY.md && grep -c 'Phase 36.3.7.7' .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-01-SUMMARY.md && grep -c 'CLOSED PASS' .planning/ROADMAP.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-01-SUMMARY.md` succeeds
    - `grep -c 'Phase 36.3.7.7' .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-01-SUMMARY.md` returns >= 5
    - `grep -c 'BUG-36.3.7.7-01' .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-01-SUMMARY.md` returns >= 3
    - `grep -c 'D-topology-shapes\|D-atomic-transaction\|D-blackboard-init\|D-no-create-task-reuse\|D-tool-surface-mounts-store-directly\|D-docs-reconciliation\|D-slash-deferred\|D-cli-verb-shape' .planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-01-SUMMARY.md` returns >= 8 distinct decisions cited
    - `grep -c 'Phase 36.3.7.7 CLOSED PASS' .planning/ROADMAP.md` returns >= 1
    - `grep -c '36.3.7.7-01-PLAN.md' .planning/ROADMAP.md` returns >= 1
    - `grep -c 'REQ-36.3.7.7-01' .planning/ROADMAP.md` returns >= 1 (Requirements line filled in)
    - STATE.md `stopped_at` reads "Phase 36.3.7.7 closed PASS"
    - STATE.md `progress.completed_phases` increased by 1 vs the pre-phase value
    - Final commit message: `docs(state,roadmap): close Phase 36.3.7.7`
  </acceptance_criteria>
  <done>SUMMARY.md committed with bilateral-tracing + 14-gate + locked-decisions tables; STATE.md + ROADMAP.md closed; phase verdict recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM model -> kanban_swarm tool | Untrusted JSON args cross here (workers union shape, blackboard opaque JSON, assignee strings) |
| CLI user -> cmd_swarm | Untrusted shell args cross here (--workers comma-separated strings, --workers-json arbitrary JSON, --blackboard-file path) |
| Gateway slash -> /kanban swarm | Untrusted slash text from any gateway platform crosses here — routed to deferred-subverb message, NOT to handler arm |
| store.create_swarm -> SQLite | Inside the BEGIN IMMEDIATE transaction; parameterized SQL only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.7-01 | Tampering | KanbanSwarmTool::execute (worker assignee strings) | mitigate | validate_profile_name called on every assignee BEFORE constructing SwarmGraphSpec; rejection JSON `{"reason":"invalid_assignee","detail":...}` per Task 5 behavior |
| T-36.3.7.7-02 | Tampering | KanbanStore::create_swarm INSERT statements | mitigate | rusqlite parameterized queries (`params![]`) for all 5 INSERT shapes; no string interpolation; matches existing create_task pattern at store.rs:243-267 |
| T-36.3.7.7-03 | Tampering | blackboard JSON arg | accept | Opaque per D-no-blackboard-validation; stored as serialized string in task_comments.body; workers parse it themselves — same threat surface as existing kanban_comment.body |
| T-36.3.7.7-04 | Elevation of Privilege | Partial graph creation on insert failure | mitigate | BEGIN IMMEDIATE transaction wraps all 1+N+0..2 task inserts + N+0..2 link inserts + 0..1 comment insert; `?` propagation triggers automatic rollback; receiver test `swarm_invalid_assignee_rolls_back_whole_graph` (Task 8) asserts zero residual rows |
| T-36.3.7.7-05 | Tampering | --blackboard-file path | accept | std::fs::File::open opens file at given path; same threat surface as existing kanban_create's body-from-file path (if any); user-owned shell process boundary |
| T-36.3.7.7-06 | Information Disclosure | Cross-tenant link injection (worker assignee belongs to different tenant) | mitigate | Existing TenantMismatch error variant propagated from in-transaction link-insert path; create_swarm honors the same tenant gate inlined inside the tx (per A4 SQL shape) |
| T-36.3.7.7-07 | Denial of Service | --workers-json with deeply-nested or huge JSON | accept | serde_json default depth limits apply; same surface as any existing JSON-accepting tool; not a new attack vector |
| T-36.3.7.7-SC | Tampering | npm/pip/cargo installs | n/a | No new external dependencies in this phase — all libraries are pre-existing workspace members (rusqlite, serde_json, async-trait, ironhermes-tools, clap, tempfile, ironhermes-core::profile). RESEARCH.md §Package Legitimacy Audit confirms zero new packages. No legitimacy checkpoint needed. |
</threat_model>

<verification>
## Wave-merge gate (Wave 9 entry condition)

After each wave's last task completes, run:

```bash
cargo build --workspace 2>&1 | tail -5
cargo test --workspace --no-fail-fast -- --test-threads=1 2>&1 | tail -10
```

Both must exit 0 before advancing to the next wave.

## Phase-close gate (Task 12 verifier sweep)

The 14 gates listed in Task 12 behavior block. Any failure blocks Task 13 (SUMMARY) and triggers iteration on the failing wave.

## Spawn-contract invariant (REQ-36.3.7.7-10)

```bash
grep -c 'HERMES_KANBAN_SWARM_ROOT' crates/ironhermes-kanban/src/worker_spawn.rs   # must == 0
git diff develop -- crates/ironhermes-kanban/src/worker_spawn.rs | wc -l           # must == 0 (byte-stable)
```

## INV-36.3.7-SG-10 invariant (KANBAN_GUIDANCE static-const)

```bash
cargo test -p ironhermes-kanban --test invariants_36_3_7 -- kanban_guidance_is_static_const  # must exit 0
grep -cE 'format!\(|concat!\(' crates/ironhermes-kanban/src/kanban_guidance.rs                # must == 0
```

## Docs reconciliation grep gates (REQ-36.3.7.7-09)

```bash
grep -c 'deferred to Phase 36.3.7.7' docs/kanban/reference.md   # must == 0
grep -c 'all 10 LLM tools' docs/kanban/reference.md             # must >= 1
grep -c 'kanban_swarm' docs/kanban/reference.md                 # must >= 2
grep -c 'Shipped in Phase 36.3.7.7' docs/kanban/reference.md    # must >= 2
```

## Tool-count invariant (REQ-36.3.7.7-12)

```bash
grep -c 'registry.register(Box::new(' crates/ironhermes-kanban/src/tools/mod.rs   # must == 10
```

## Slash-deferral invariant (REQ-36.3.7.7-08)

```bash
grep -c '"swarm",' crates/ironhermes-core/src/commands/handlers.rs                # must >= 1
grep -c '"heartbeat",' crates/ironhermes-core/src/commands/handlers.rs            # must == 0 (still absent — D-no-deferred-subverbs-change holds)
```

## create_task byte-unchanged invariant (REQ-36.3.7.7-11)

```bash
git diff develop -- crates/ironhermes-kanban/src/store.rs | grep -E '^[-+]\s*pub fn create_task'   # must be empty
```

## Crate-isolation fence (inherited from 36.3.7.5/6)

```bash
grep -cE '^ironhermes-gateway\s*=' crates/ironhermes-kanban/Cargo.toml   # must == 0
```
</verification>

<success_criteria>

Phase 36.3.7.7 ships when ALL of the following hold:

- [ ] REQ-36.3.7.7-01..12 registered in `.planning/REQUIREMENTS.md` with D-XX anchors
- [ ] `crates/ironhermes-kanban/src/types.rs` contains `pub struct KanbanWorkerSpec`, `pub struct SwarmGraphSpec`, `pub struct SwarmGraphIds`
- [ ] `crates/ironhermes-kanban/src/store.rs` contains exactly one `pub fn create_swarm` AND exactly one `pub fn create_task` (the latter byte-unchanged)
- [ ] `crates/ironhermes-kanban/src/tools/swarm.rs` exists with `pub struct KanbanSwarmTool` + `impl Tool for KanbanSwarmTool` + `name() -> "kanban_swarm"`
- [ ] `crates/ironhermes-kanban/src/tools/mod.rs` registers exactly 10 tools via `registry.register(Box::new(...))`
- [ ] `crates/ironhermes-cli/src/kanban/mod.rs` contains `KanbanCommands::Swarm` variant + dispatch arm
- [ ] `crates/ironhermes-cli/src/kanban/commands.rs` contains `pub async fn cmd_swarm`
- [ ] `crates/ironhermes-cli/tests/swarm_cli.rs` has 3 passing CLI parse tests including the verbatim §664 example
- [ ] `crates/ironhermes-kanban/tests/tools_smoke.rs` has 10 passing swarm tests covering all 4 topologies + blackboard + idempotency + rollback + worker-form variants + root.ended_at invariant
- [ ] `crates/ironhermes-core/src/commands/handlers.rs` `DEFERRED_KANBAN_SUBVERBS` contains `"swarm"`
- [ ] `docs/kanban/reference.md` reconciled at §14, §200, §664 with all 4 grep gates passing
- [ ] `crates/ironhermes-kanban/src/kanban_guidance.rs` extended with parents-walk note; INV-36.3.7-SG-10 invariant still passes
- [ ] `cargo build --workspace` exits 0
- [ ] `cargo test --workspace --no-fail-fast -- --test-threads=1` exits 0 with zero NEW failures vs develop tip
- [ ] `crates/ironhermes-kanban/src/worker_spawn.rs` byte-unchanged (REQ-36.3.7.7-10)
- [ ] `crates/ironhermes-kanban/Cargo.toml` does NOT add `ironhermes-gateway` dep (crate-isolation fence held)
- [ ] `.planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-01-SUMMARY.md` exists with bilateral-tracing + 14-gate + locked-decisions tables
- [ ] `.planning/STATE.md` updated with `stopped_at: "Phase 36.3.7.7 closed PASS"` and progress counters bumped
- [ ] `.planning/ROADMAP.md` §622-631 has `**Phase verdict:** Phase 36.3.7.7 CLOSED PASS.` paragraph appended
</success_criteria>

<output>
Create `.planning/phases/36.3.7.7-kanban-swarm-helper-multi-task-fan-out-for-orchestrators/36.3.7.7-01-SUMMARY.md` per Task 13 when all 14 phase-gates pass.

Per-task commit prefixes:
- Task 1: `docs(36.3.7.7-01): ...`
- Task 2: `test(36.3.7.7-01): ...`
- Task 3-7: `feat(36.3.7.7-01): ...`
- Task 8-9: `test(36.3.7.7-01): ...`
- Task 10: `feat(36.3.7.7-01): ...`
- Task 11: `docs(36.3.7.7-01): ...`
- Task 12: no commit (verification only); fix-ups (if any) use `fix(36.3.7.7-01): ...`
- Task 13: `docs(36.3.7.7-01): SUMMARY ...` for the SUMMARY commit, then `docs(state,roadmap): close Phase 36.3.7.7` for the final state/roadmap close-out commit
</output>
