---
phase: 36.3.7.9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-kanban/src/paths.rs
  - crates/ironhermes-kanban/src/board/mod.rs
  - crates/ironhermes-kanban/src/board/resolver.rs
  - crates/ironhermes-kanban/src/board/slug.rs
  - crates/ironhermes-kanban/src/lib.rs
autonomous: true
requirements: [D-01, D-02]
tags: [multi-board, kanban, rust, cli]

must_haves:
  truths:
    - "paths::board_db_path_for_slug(\"default\") returns the legacy kanban_db_path() (D-01 back-compat)"
    - "paths::board_db_path_for_slug(<any-other-slug>) returns ~/.ironhermes/kanban/boards/<slug>/kanban.db"
    - "paths::list_boards() returns named-board slugs only, skips entries starting with '_', sorted, omits 'default'"
    - "board::slug::validate_board_slug accepts ^[a-z0-9][a-z0-9_-]{0,63}$ and downcases uppercase input"
    - "board::slug::validate_board_slug rejects path-traversal inputs (/, \\, .., leading/trailing whitespace) BEFORE downcasing"
    - "board::resolve_board_context returns the 4-tier precedence: flag > HERMES_KANBAN_BOARD > current file > 'default'"
    - "board::resolve_board_context returns BoardContext { slug, db_path, source } where source ∈ {Flag, Env, File, Default}"
  artifacts:
    - path: "crates/ironhermes-kanban/src/paths.rs"
      provides: "boards_root, board_dir, board_db_path, board_db_path_for_slug, current_board_file, notifier_config_path, archive_dir, list_boards"
    - path: "crates/ironhermes-kanban/src/board/mod.rs"
      provides: "BoardContext struct, BoardSource enum, default_board_context() helper, module re-exports"
    - path: "crates/ironhermes-kanban/src/board/resolver.rs"
      provides: "resolve_board_context(cli_flag: Option<&str>) pure fn"
    - path: "crates/ironhermes-kanban/src/board/slug.rs"
      provides: "validate_board_slug(input: &str) -> Result<String, BoardSlugError> + BoardSlugError thiserror enum"
  key_links:
    - from: "crates/ironhermes-kanban/src/board/resolver.rs"
      to: "crates/ironhermes-kanban/src/board/slug.rs::validate_board_slug"
      via: "direct call before constructing BoardContext"
      pattern: "validate_board_slug\\(&raw_slug\\)"
    - from: "crates/ironhermes-kanban/src/board/resolver.rs"
      to: "crates/ironhermes-kanban/src/paths.rs::board_db_path_for_slug"
      via: "computes db_path field of BoardContext"
      pattern: "board_db_path_for_slug\\("
    - from: "crates/ironhermes-kanban/src/lib.rs"
      to: "crates/ironhermes-kanban/src/board/mod.rs"
      via: "pub mod board; + pub use board::{BoardContext, BoardSource, resolve_board_context}"
      pattern: "pub mod board"
---

<objective>
Establish the pure-function foundations of the multi-board surface: extend `paths.rs` with multi-board path helpers, introduce a new `board/` module with the slug validator (T-1 mitigation), the `BoardContext`/`BoardSource` types, and the 4-tier `resolve_board_context()` resolver. No IO outside `std::env`/`std::fs::read_to_string`; no DB code. All tests are in-file `#[cfg(test)] mod tests`.

Purpose: Plans 02-09 all depend on these foundations. Without `board_db_path_for_slug`, store.rs cannot honor D-01 back-compat; without `validate_board_slug`, the CLI cannot block path traversal (T-1) at the entry; without `resolve_board_context`, every downstream consumer (CLI, LLM tools, dispatcher, notifier) cannot share a single 4-tier precedence rule (D-02).

Output: Three new files (`board/mod.rs`, `board/resolver.rs`, `board/slug.rs`), one extended file (`paths.rs`), one re-export amendment (`lib.rs`). All unit tests inline. `cargo build --workspace` exit 0. `cargo test -p ironhermes-kanban board::` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-CONTEXT.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-RESEARCH.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-PATTERNS.md

# Existing analog files (read before touching the new files)
@crates/ironhermes-kanban/src/paths.rs
@crates/ironhermes-kanban/src/mention/resolver.rs
@crates/ironhermes-kanban/src/error.rs
@crates/ironhermes-kanban/src/lib.rs

<interfaces>
<!-- Extracted from existing codebase — executor uses these directly, no exploration needed -->

From crates/ironhermes-kanban/src/paths.rs (existing helper shape — copy this pattern):
```rust
pub fn kanban_db_path() -> PathBuf {
    ironhermes_core::get_hermes_home().join("kanban.db")
}
```

From crates/ironhermes-kanban/src/error.rs (KanbanError enum location — the new BoardSlugError lives in board/slug.rs as its own thiserror enum, NOT a variant of KanbanError — researcher pattern §3):
```rust
pub type Result<T> = std::result::Result<T, KanbanError>;
```

From crates/ironhermes-kanban/src/mention/resolver.rs:169-206 (resolver shape — copy the if-let-chain style, NOT Option::or_else):
```rust
pub fn resolve_mention(handle: &str, ctx: &ResolverCtx<'_>) -> Resolution { ... }
```

From crates/ironhermes-kanban/src/lib.rs (existing module + re-export shape):
```rust
pub mod mention;
pub mod paths;
pub use paths::{kanban_db_path, kanban_workspaces_root, ...};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend paths.rs with multi-board path helpers + list_boards()</name>
  <read_first>
    - crates/ironhermes-kanban/src/paths.rs (full file — copy the existing helper signature shape and the inline `#[cfg(test)] mod tests` block at lines 69-119)
    - .planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-PATTERNS.md (§1, "extend in-place" assignment for paths.rs)
    - .planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-RESEARCH.md (§6, lines 438-516 — exact helper signatures and `list_boards` algorithm; also Pitfalls 1, 2, 4 at §1134+)
  </read_first>
  <behavior>
    - boards_root() returns ironhermes_core::get_hermes_home().join("kanban").join("boards")
    - board_dir("alpha") returns boards_root().join("alpha")
    - board_db_path("alpha") returns board_dir("alpha").join("kanban.db")
    - board_db_path_for_slug("default") returns kanban_db_path() exactly (D-01 / Pitfall 1)
    - board_db_path_for_slug("alpha") returns board_db_path("alpha")
    - current_board_file() returns ironhermes_core::get_hermes_home().join("kanban").join("current")
    - notifier_config_path() returns ironhermes_core::get_hermes_home().join("kanban").join("notifier.toml")
    - archive_dir() returns boards_root().join("_archived")
    - list_boards() returns Ok(vec![]) when boards_root() does not exist
    - list_boards() skips entries whose file_name starts with '_' (Pitfall 4: filters _archived)
    - list_boards() skips non-directory entries
    - list_boards() sorts results alphabetically and DOES NOT include "default" (Pitfall 2: caller prepends "default" explicitly when needed)
    - All tests use path.ends_with("leaf") assertions (matching existing test style at paths.rs:69-119) — do NOT hard-code the home dir prefix
  </behavior>
  <action>
    Open crates/ironhermes-kanban/src/paths.rs. Append the eight new pub fns AFTER the existing `validate_dir_workspace` fn but BEFORE the `#[cfg(test)] mod tests` block. Use the exact signatures and bodies from RESEARCH.md §6 and PATTERNS.md §1. Function list to add (in this order): `boards_root`, `board_dir`, `board_db_path`, `board_db_path_for_slug`, `current_board_file`, `notifier_config_path`, `archive_dir`, `list_boards`.

    For `board_db_path_for_slug`: implement the D-01 special case verbatim — `if slug == "default" { kanban_db_path() } else { board_db_path(slug) }`. Do NOT extract this into a helper; the explicit branch is the documented invariant (Pitfall 1).

    For `list_boards`: signature is `pub fn list_boards() -> std::io::Result<Vec<String>>`. Algorithm: if `!boards_root().exists()` return Ok(vec![]); iterate `std::fs::read_dir(&root)?`; for each entry skip if name starts with '_'; skip if !file_type()?.is_dir(); push name; finally sort and return. Do NOT include "default" in the returned vec — callers (dispatcher, notifier sweep) prepend it explicitly (Pitfall 2). Use `entry.file_name().to_string_lossy().into_owned()` per PATTERNS.md §1.

    Inside the existing `#[cfg(test)] mod tests` block, add ≥10 new tests using the existing `path.ends_with("leaf")` style:
      - boards_root_ends_with_boards
      - board_dir_appends_slug
      - board_db_path_appends_kanban_db
      - board_db_path_for_slug_default_is_legacy (assert ends_with "kanban.db" and DOES NOT contain "/boards/")
      - board_db_path_for_slug_named_uses_boards_layout (assert contains "/boards/alpha/kanban.db")
      - current_board_file_ends_with_current
      - notifier_config_path_ends_with_notifier_toml
      - archive_dir_ends_with_archived (under "boards/_archived")
      - list_boards_returns_empty_when_root_absent (use tempfile + IRONHERMES_HOME env override OR call via dummy fs and assert Ok(vec![]) when path doesn't exist — pattern: serial test that creates a tempdir, sets IRONHERMES_HOME, runs, restores)
      - list_boards_skips_underscore_prefix_and_files (create tempdir with `boards/_archived/`, `boards/alpha/`, `boards/.hidden` (file), assert returns ["alpha"] sorted; lock the Pitfall 4 invariant)

    For tempdir-based tests use `tempfile = { workspace = true }` if already a dev-dep, otherwise add to the kanban crate's dev-deps in this task. Verify by running `cargo test -p ironhermes-kanban paths::tests`.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-kanban --lib paths::tests</automated>
  </verify>
  <acceptance_criteria>
    - Source: `crates/ironhermes-kanban/src/paths.rs contains pub fn boards_root() -> PathBuf`
    - Source: `crates/ironhermes-kanban/src/paths.rs contains pub fn board_db_path_for_slug(slug: &str) -> PathBuf`
    - Source: `crates/ironhermes-kanban/src/paths.rs contains pub fn list_boards() -> std::io::Result<Vec<String>>`
    - Behavior: `board_db_path_for_slug("default")` equals `kanban_db_path()` (D-01)
    - Behavior: `list_boards()` filters out entries starting with `_` (Pitfall 4)
    - Test: `cargo test -p ironhermes-kanban --lib paths::tests` exits 0 with ≥10 new tests added
    - Build: `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>
    paths.rs exports all eight new helpers; ≥10 new unit tests pass; `cargo build --workspace` clean; the D-01 special case is locked by `board_db_path_for_slug_default_is_legacy`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create board/ module — BoardContext types, slug validator (T-1 mitigation), 4-tier resolver</name>
  <read_first>
    - crates/ironhermes-kanban/src/mention/resolver.rs (full file — the resolver pattern at lines 169-206 and test scaffold at 212-238 are the template)
    - crates/ironhermes-kanban/src/paths.rs (after Task 1 — confirm `board_db_path_for_slug`, `current_board_file` exist)
    - crates/ironhermes-kanban/src/lib.rs (the `pub mod` + `pub use` re-export block at lines 23-76)
    - .planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-PATTERNS.md (§2 resolver pattern + §3 slug validator pattern, full code excerpts)
    - .planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-RESEARCH.md (§5 resolver lines 364-436; §12 slug validator lines 816-878; Pitfalls 1, 3 at lines 1136-1175)
  </read_first>
  <behavior>
    BoardSlugError variants (thiserror): Empty, TooLong, BadStart, InvalidChars, Reserved(String), PathTraversal.

    validate_board_slug acceptance (positive):
      - "alpha" → Ok("alpha")
      - "atm10-server" → Ok("atm10-server")
      - "my_board_2" → Ok("my_board_2") [underscore allowed — divergence from validate_profile_name]
      - "A" → Ok("a") [single-char accepted, downcased]
      - "X"*64 → Ok("x"*64) [boundary]
      - "Alpha-Beta" → Ok("alpha-beta") [downcase]
      - "1board" → Ok("1board") [leading digit accepted]

    validate_board_slug rejection (negative) — each MUST produce the named variant:
      - "" → Err(Empty)
      - "X"*65 → Err(TooLong)
      - "_underscore_start" → Err(BadStart)
      - "-dash-start" → Err(BadStart)
      - "alpha bravo" → Err(InvalidChars) [space]
      - "alpha.board" → Err(InvalidChars) [dot]
      - "alpha\x01" → Err(InvalidChars) [control char]
      - "a/b" → Err(PathTraversal)
      - "..\\evil" → Err(PathTraversal) [backslash + ..]
      - "../etc/passwd" → Err(PathTraversal)
      - " leading-space" → Err(PathTraversal) [trim mismatch detection BEFORE downcase per RESEARCH §12 line 859]
      - "_archived" → Err(Reserved("_archived"))

    validate_board_slug must NOT reject "default" — that's the built-in (Pitfall 3); the `boards create default` CLI command rejects it separately (Plan 03).

    resolve_board_context behavior:
      - resolve_board_context(Some("alpha")) → BoardContext { slug: "alpha", db_path: board_db_path_for_slug("alpha"), source: BoardSource::Flag }
      - resolve_board_context(None) with HERMES_KANBAN_BOARD="beta" set → source: BoardSource::Env, slug: "beta"
      - resolve_board_context(None) with env unset + current_board_file containing "gamma\n" → source: BoardSource::File, slug: "gamma" (trim whitespace)
      - resolve_board_context(None) with env unset + current file missing/empty → source: BoardSource::Default, slug: "default", db_path: kanban_db_path()
      - resolve_board_context(Some("BAD..PATH")) → Err(BoardSlugError::PathTraversal) [validate before constructing BoardContext]
      - resolve_board_context returns Ok with raw "default" slug WITHOUT running slug validation (Pitfall 3 special-case at PATTERNS §2 line 142-146)

    BoardSource::as_str() returns "flag"|"env"|"file"|"default" exactly (consumed by Plan 07 envelope serialization).
  </behavior>
  <action>
    Create directory `crates/ironhermes-kanban/src/board/` with three files.

    `board/mod.rs`: Declare `pub mod resolver;` and `pub mod slug;`. Define `pub struct BoardContext { pub slug: String, pub db_path: std::path::PathBuf, pub source: BoardSource }` (derive Debug, Clone). Define `pub enum BoardSource { Flag, Env, File, Default }` (derive Debug, Clone, PartialEq, Eq). Implement `pub fn as_str(&self) -> &'static str` on BoardSource matching all four arms to "flag"/"env"/"file"/"default". Re-export at the bottom: `pub use resolver::{resolve_board_context, default_board_context}; pub use slug::{validate_board_slug, BoardSlugError};`. Also add `pub fn default_board_context() -> BoardContext { BoardContext { slug: "default".to_string(), db_path: crate::paths::kanban_db_path(), source: BoardSource::Default } }` — Plan 07 tools use this in the `Err` fallback path.

    `board/slug.rs`: Implement the thiserror enum and `validate_board_slug` per PATTERNS.md §3 and RESEARCH.md §12. CRITICAL ordering inside the fn: (1) detect trim mismatch (`if s != input` or `if input.trim() != input`) → PathTraversal; (2) detect `/`, `\\`, `..` → PathTraversal; (3) downcase to local `let s = input.trim().to_lowercase()`; (4) empty → Empty; (5) length > 64 → TooLong; (6) first char !ascii_alphanumeric → BadStart; (7) any body char ∉ [a-z0-9_-] → InvalidChars; (8) RESERVED_BOARD_SLUGS.contains → Reserved(s). Returns `Ok(s)` (the downcased trimmed slug). Inline `#[cfg(test)] mod tests` with ≥18 tests covering every positive + negative case enumerated in `<behavior>` above.

    `board/resolver.rs`: Implement `pub fn resolve_board_context(cli_flag: Option<&str>) -> Result<BoardContext, BoardSlugError>` per PATTERNS.md §2 lines 131-158 — verbatim if-let-chain (NOT `Option::or_else`). For the File tier, call private helper `fn read_current_board_file_opt() -> Option<String>` that does `std::fs::read_to_string(crate::paths::current_board_file())` and returns Some(trimmed) only if non-empty after trim, None otherwise (NotFound → None silently per Pitfall 5 spirit). After determining raw_slug + source, apply the Pitfall 3 special case: `let slug = if raw_slug == "default" { raw_slug } else { crate::board::slug::validate_board_slug(&raw_slug)? };`. Compute `db_path = crate::paths::board_db_path_for_slug(&slug)`. Return `Ok(BoardContext { slug, db_path, source })`. Inline `#[cfg(test)] mod tests` with ≥8 tests: flag_overrides_env, env_overrides_file, file_overrides_default, fourth_tier_is_default, default_slug_skips_validation_and_uses_legacy_db_path (asserts db_path == kanban_db_path() not board_db_path("default")), invalid_flag_slug_returns_path_traversal_error, current_file_trims_trailing_newline, empty_current_file_falls_through_to_default. Tests MUST serialize env-var access — use a `static ENV_LOCK: std::sync::Mutex<()>` + per-test lock guard (pattern from `tools_smoke.rs` ENV_LOCK referenced in STATE.md commit `4208bc28`). Each test must save+restore HERMES_KANBAN_BOARD and IRONHERMES_HOME on entry/exit (tempdir for IRONHERMES_HOME so current_board_file isolation works).

    `lib.rs`: Add `pub mod board;` between `pub mod tools;` and `pub mod types;` (alphabetical placement). Add re-export line: `pub use board::{BoardContext, BoardSource, resolve_board_context};` near the existing `pub use` block at line 50-76.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-kanban --lib board::</automated>
  </verify>
  <acceptance_criteria>
    - Source: `crates/ironhermes-kanban/src/board/mod.rs contains pub struct BoardContext` AND `pub enum BoardSource`
    - Source: `crates/ironhermes-kanban/src/board/slug.rs contains pub fn validate_board_slug`
    - Source: `crates/ironhermes-kanban/src/board/resolver.rs contains pub fn resolve_board_context`
    - Source: `crates/ironhermes-kanban/src/lib.rs contains pub mod board` AND `pub use board::`
    - Behavior: `validate_board_slug("..")` returns `Err(BoardSlugError::PathTraversal)` (T-1)
    - Behavior: `validate_board_slug("_archived")` returns `Err(BoardSlugError::Reserved(...))` (Pitfall 4 reservation)
    - Behavior: `resolve_board_context(None)` with no env / no file returns `BoardSource::Default` and `db_path == kanban_db_path()` (D-01 + D-02 4th tier)
    - Behavior: `resolve_board_context(Some("alpha"))` returns `BoardSource::Flag` (D-02 tier 1)
    - Test: `cargo test -p ironhermes-kanban --lib board::` exits 0 with ≥26 tests added (≥18 slug + ≥8 resolver)
    - Build: `cargo build --workspace` exits 0
    - Grep: `grep -c 'validate_board_slug' crates/ironhermes-kanban/src/board/resolver.rs >= 1` (resolver invokes validator before path construction → T-1 mitigation)
  </acceptance_criteria>
  <done>
    The `board` module compiles, all 26+ tests pass, the four resolution tiers are demonstrably ordered, the slug validator rejects every T-1 path-traversal vector, and the D-01 default-board special case is enforced both in the validator skip (Pitfall 3) and in the db_path computation (Pitfall 1).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → slug validator | Untrusted user input enters via `--board <slug>` and `boards create/switch/rm <slug>` (Plans 03, 04, 07) |
| Env var → resolver | `HERMES_KANBAN_BOARD` set by the dispatcher (Plan 06); a malicious env override in a debug shell could attempt traversal |
| Filesystem → resolver | `~/.ironhermes/kanban/current` contents are user-writable; could contain `..` or path separators |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.9-01-01 | Tampering | board/slug.rs::validate_board_slug | mitigate | Reject `/`, `\\`, `..`, leading/trailing whitespace BEFORE downcasing (RESEARCH §12 line 858-861); locked by `validate_board_slug_rejects_dotdot` and `validate_board_slug_rejects_slash` tests in Task 2 |
| T-36.3.7.9-01-02 | Tampering | board/resolver.rs::resolve_board_context | mitigate | Resolver calls `validate_board_slug` on every non-"default" slug before constructing `db_path` (PATTERNS §2 line 142-146); locked by `invalid_flag_slug_returns_path_traversal_error` test |
| T-36.3.7.9-01-03 | Information Disclosure | board/slug.rs `_archived` reservation | mitigate | `_archived` added to `RESERVED_BOARD_SLUGS` to prevent shadowing the archive sibling dir; locked by `validate_board_slug_rejects_reserved_archived` test |
| T-36.3.7.9-01-SC | Supply Chain | new module compile surface | accept | No new external crate deps in this plan; `thiserror`/`anyhow`/std only |
</threat_model>

<verification>
- `cargo build --workspace` exits 0
- `cargo test -p ironhermes-kanban --lib paths::tests board::` exits 0
- `cargo clippy -p ironhermes-kanban -- -D warnings` exits 0
- grep -c 'pub fn board_db_path_for_slug' crates/ironhermes-kanban/src/paths.rs == 1
- grep -c 'pub fn resolve_board_context' crates/ironhermes-kanban/src/board/resolver.rs == 1
- grep -c 'pub fn validate_board_slug' crates/ironhermes-kanban/src/board/slug.rs == 1
- grep -c 'pub mod board' crates/ironhermes-kanban/src/lib.rs == 1
</verification>

<success_criteria>
All ≥36 new unit tests pass (Task 1 ≥10 + Task 2 ≥26). The four-tier resolver, the path-traversal slug rejection, and the D-01 default-board special case are each locked by a named test that would fail if a future refactor regressed the contract. `cargo build --workspace` clean.
</success_criteria>

<output>
Create `.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-01-SUMMARY.md` with the bilateral-tracing table (BUG/Feature → producer file:line + consumer test file:line) per LEARNINGS 2026-05-30 precedent. Include exit code of `cargo test -p ironhermes-kanban --lib paths::tests board::` and grep counts for each acceptance-criteria source assertion.
</output>
