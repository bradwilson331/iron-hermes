---
phase: 36.3.7.9-multi-board-cli-boards-and-board-flag
verified: 2026-05-31T20:27:27Z
status: passed
score: 12/12 must-haves verified
gate_count: 12
gate_results:
  D-01: VERIFIED
  D-02: VERIFIED
  D-03: VERIFIED
  D-04: VERIFIED
  D-05: VERIFIED
  D-06: VERIFIED
  D-07: VERIFIED
  D-08: VERIFIED
  T-1:  VERIFIED
  T-2:  VERIFIED
  CR-01: VERIFIED
  CR-04: VERIFIED
overrides_applied: 0
re_verification: false
---

# Phase 36.3.7.9: Multi-Board CLI + --board Flag Verification Report

**Phase Goal:** Ship the `boards` subcommand surface (`boards list/create/switch/show/rename/rm`) and the `--board <slug>` flag so every kanban CLI verb and LLM tool routes to the correct per-board SQLite database, governed by locked decisions D-01 through D-08.
**Verified:** 2026-05-31T20:27:27Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (Invariant Gates)

| # | Gate | Truth | Status | File:Line Evidence |
|---|------|-------|--------|--------------------|
| 1 | D-01 | `"default"` slug routes to legacy `~/.ironhermes/kanban.db`; named boards route to `boards/<slug>/kanban.db` | VERIFIED | `paths.rs:92-100` (`board_db_path_for_slug`); `store.rs:201-206` (`open_for_board` doc+impl) |
| 2 | D-02 | 4-tier resolution: `--board` flag > `HERMES_KANBAN_BOARD` env > `~/.ironhermes/kanban/current` file > `"default"` | VERIFIED | `board/resolver.rs:25-57` (full if-let chain, source tracking, pitfall-3 skip) |
| 3 | D-03 | `notifier.toml` with `subscribe_boards = ["*"]` default; `Explicit` codepath ships inert in v1 | VERIFIED | `notifier_config.rs:1-81` (`SubscribeBoardsConfig::All/Explicit`; `default_subscribe_boards` returns `["*"]`); `notifier.rs:98` (`subscribe_boards` field) |
| 4 | D-04 | One dispatcher sweeps all boards per tick; per-board open failure logs warn+skips; board slug propagated to worker env | VERIFIED | `dispatcher.rs:158-215` (`run_dispatch_tick` list_boards sweep, open_for_board, warn+skip); `worker_spawn.rs:98,113-117` (`build_kanban_worker_env` with `board_slug`, sets `HERMES_KANBAN_BOARD` and `HERMES_KANBAN_DB`) |
| 5 | D-05 | Cycle detection unchanged: `WITH RECURSIVE` SQL runs per-board; cross-board links fail at TaskNotFound | VERIFIED | `store.rs:598,676-688` (`WITH RECURSIVE descendants` unchanged); `cycle_detection_per_board.rs:1-128` (3 tests including cross-board TaskNotFound) |
| 6 | D-06 | Auto-migrate with stderr banner `[kanban] migrated board '<slug>' from v<old> to v<new>` inside rusqlite RAII transaction | VERIFIED | `store.rs:249-297` (`format_migration_banner` at :249-256; banner emitted at :290-292; `self.conn.transaction()` RAII at :291) |
| 7 | D-07 | `boards rm` archives by default to `_archived/<slug>-<ts>/`; `--delete` requires zero open tasks | VERIFIED | `boards.rs:270-378` (`cmd_boards_rm`: `symlink_metadata` :283, `kanban.db.rm-lock` :308, `LockGuard` RAII :330-338, `open_tasks_count()` guard :342-353, `archive_dir()` :370, `_archived/<slug>-<ts>` dest :371) |
| 8 | D-08 | Every LLM tool response envelope (success AND rejection) contains `"board"` and `"board_source"` fields | VERIFIED | `tools/common.rs:54-125` (`ok_with_board`, `inject_board_fields`, `reject_with_board` all inject `board`+`board_source`); `tools/list.rs:98-153` (representative tool: resolve at :99, reject_with_board at :101, ok_with_board at :153) |
| 9 | T-1 | Path-traversal mitigation: `validate_board_slug` rejects `..`, `/`, whitespace BEFORE downcasing | VERIFIED | `board/slug.rs:53-96` (steps 1-2 fire before step 3 downcase; `PathTraversal` error on `/`, `\\`, `..`, trim-mismatch) |
| 10 | T-2 | SQL injection mitigation: no slug literals in SQL `format!()` strings | VERIFIED | `tests/sql_no_slug_literals.rs:41-107` (grep audit test); `store.rs:9-10` (doc comment: "no `format!`-interpolated SQL") |
| 11 | CR-01 | `validate_board_slug` called at CLI boundary in `open_store_for_board` before `open_for_board`; zero bare `open_store()` callsites remain | VERIFIED | `commands.rs:39-45` (`open_store_for_board` calls `validate_board_slug` at :43 then `open_for_board` at :45); zero `open_store\b` matches outside `open_store_for_board` |
| 12 | CR-04 | Migration UPDATE + banner wrapped in `rusqlite Connection::transaction()` RAII; no raw BEGIN/COMMIT/ROLLBACK in migration path | VERIFIED | `store.rs:280-297` (`self.conn.transaction()` at :291; `tx.execute` at :293; `tx.commit()` at :297; comment at :280-288 explains CR-04 fix) |

**Score:** 12/12 gates verified

---

## Required Artifacts

| Artifact | Purpose | Status | Key Evidence |
|----------|---------|--------|--------------|
| `crates/ironhermes-kanban/src/paths.rs` | D-01: 8 path helpers | VERIFIED | `boards_root` :74, `board_dir` :81, `board_db_path` :86, `board_db_path_for_slug` :96-100, `current_board_file` :105, `archive_dir` :119-120, `list_boards` :128 |
| `crates/ironhermes-kanban/src/board/mod.rs` | `BoardContext`, `BoardSource` types | VERIFIED | File exists; used by resolver.rs and all tools |
| `crates/ironhermes-kanban/src/board/resolver.rs` | D-02: 4-tier resolution | VERIFIED | :25-57 full if-let chain |
| `crates/ironhermes-kanban/src/board/slug.rs` | T-1: path-traversal defence | VERIFIED | :53-96 with correct 8-step security ordering |
| `crates/ironhermes-kanban/src/store.rs` | D-01 routing, D-06 banner, CR-04 RAII tx | VERIFIED | `open_for_board` :203-206; `format_migration_banner` :249-256; RAII tx :291-297 |
| `crates/ironhermes-kanban/src/notifier_config.rs` | D-03: subscribe_boards config | VERIFIED | `SubscribeBoardsConfig::All/Explicit` :75-81; `default_subscribe_boards` returns `["*"]` :39-41 |
| `crates/ironhermes-kanban/src/notifier.rs` | D-03/D-04: multi-board notifier sweep | VERIFIED | `subscribe_boards` field :98; `board_watermarks` :96; `run_notifier_tick` :242; `open_for_board` :262; warn+skip :272 |
| `crates/ironhermes-kanban/src/dispatcher.rs` | D-04: per-tick board sweep | VERIFIED | `SpawnFn` with board_slug :66; `list_boards()` sweep :167; `open_for_board` :181; warn+skip :184; `run_dispatch_tick_for_board` :216 |
| `crates/ironhermes-kanban/src/worker_spawn.rs` | D-04: board slug to worker env | VERIFIED | `build_kanban_worker_env(board_slug)` :98; `HERMES_KANBAN_BOARD` :117; `HERMES_KANBAN_DB` via `board_db_path_for_slug` :114; `spawn_worker_for_board` :179 |
| `crates/ironhermes-kanban/src/tools/common.rs` | D-08: shared envelope helpers | VERIFIED | `resolve_board_context_from_args` :36-48; `ok_with_board` :54; `inject_board_fields` :70-79; `reject_with_board` :100-113 |
| `crates/ironhermes-cli/src/kanban/boards.rs` | D-07: boards subcommand impl; CR-02/CR-03 fixes | VERIFIED | `cmd_boards_rm` :270-378; `LockGuard` RAII :330-338; `BoardToml`/`toml::from_str` :414-427; `symlink_metadata` :283; `kanban.db.rm-lock` :308 |
| `crates/ironhermes-cli/src/kanban/commands.rs` | CR-01: `open_store_for_board` boundary | VERIFIED | :39-45; zero bare `open_store()` callsites |
| `crates/ironhermes-cli/src/kanban/mod.rs` | `KanbanCommands::Boards` variant; `--board` flag dispatch | VERIFIED | `Boards` variant :309; `handle_kanban_command(cmd, board: Option<String>)` :526; dispatch arms :648-658 |
| `crates/ironhermes-core/src/commands/handlers.rs` | Plan 08: `"boards"` in `DEFERRED_KANBAN_SUBVERBS` | VERIFIED | `DEFERRED_KANBAN_SUBVERBS` :1143; `"boards"` entry :1150 |
| `docs/kanban/reference.md` | Phase 36.3.7.9 documentation annotation | VERIFIED | Line 71: `Shipped in Phase 36.3.7.9` with D-01+D-02 described verbatim |

---

## Test File Existence

All 10 phase-specific test files confirmed present on disk:

| Test File | Tests | Gate |
|-----------|-------|------|
| `crates/ironhermes-kanban/tests/multi_board_open.rs` | 9 | D-01, D-06 |
| `crates/ironhermes-kanban/tests/notifier_multi_board.rs` | 6 | D-03 |
| `crates/ironhermes-kanban/tests/dispatcher_multi_board.rs` | 4 | D-04 |
| `crates/ironhermes-kanban/tests/cycle_detection_per_board.rs` | 3 | D-05 |
| `crates/ironhermes-kanban/tests/tools_board_envelope.rs` | ~22 | D-08, T-5 |
| `crates/ironhermes-kanban/tests/sql_no_slug_literals.rs` | 1 | T-2 |
| `crates/ironhermes-cli/tests/boards_clap_parity.rs` | 11 | D-07 CLI surface |
| `crates/ironhermes-cli/tests/boards_lifecycle.rs` | 13 | D-07, T-3, T-6, T-7 |
| `crates/ironhermes-cli/tests/board_flag_threading.rs` | 4 | D-02 CLI |
| `crates/ironhermes-cli/tests/boards_e2e.rs` | 3 | D-02, D-07, D-08 |
| `crates/ironhermes-core/tests/handlers_deferred_subverbs.rs` | 6 | Plan 08 DEFERRED routing |

---

## Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| `--board <slug>` CLI flag | `open_store_for_board` | `commands.rs:39-45` calls `validate_board_slug` then `open_for_board` | WIRED |
| `open_for_board(slug)` | `board_db_path_for_slug(slug)` | `store.rs:204` | WIRED |
| `board_db_path_for_slug("default")` | `kanban_db_path()` (legacy path) | `paths.rs:97-99` | WIRED |
| `board_db_path_for_slug(named)` | `board_db_path(slug)` = `boards/<slug>/kanban.db` | `paths.rs:100` | WIRED |
| `resolve_board_context(cli_flag)` | `validate_board_slug` | `resolver.rs:50` | WIRED |
| `run_dispatch_tick` | `list_boards()` + `open_for_board` per named board | `dispatcher.rs:167-184` | WIRED |
| `spawn_worker_for_board(board_slug)` | `build_kanban_worker_env(board_slug)` | `worker_spawn.rs:235` | WIRED |
| `build_kanban_worker_env` | `HERMES_KANBAN_BOARD = board_slug` | `worker_spawn.rs:117` | WIRED |
| `build_kanban_worker_env` | `HERMES_KANBAN_DB = board_db_path_for_slug(slug)` | `worker_spawn.rs:113-114` | WIRED |
| `init_schema` migration branch | `self.conn.transaction()` RAII | `store.rs:291` | WIRED |
| `tool.execute()` | `resolve_board_context_from_args` + `ok_with_board`/`reject_with_board` | `tools/list.rs:99-153` (representative) | WIRED |
| `KanbanCommands::Boards` | `boards::cmd_boards_*` dispatch | `mod.rs:648-658` | WIRED |
| `"boards"` in `DEFERRED_KANBAN_SUBVERBS` | gateway `/kanban boards <verb>` → deferred-CLI message | `handlers.rs:1150,1288` | WIRED |

---

## Data-Flow Trace (Level 4)

| Component | Data Variable | Source | Produces Real Data | Status |
|-----------|--------------|--------|--------------------|--------|
| `open_for_board(slug)` | `board_db_path_for_slug(slug)` | `paths.rs:96-100` — conditional branch on `slug == "default"` | Yes — live filesystem path computation | FLOWING |
| `run_dispatch_tick` board slugs | `list_boards()` result | `paths.rs:128-140` — reads `boards_root()` directory entries from disk | Yes — real directory listing | FLOWING |
| `build_kanban_worker_env` `HERMES_KANBAN_DB` | `board_db_path_for_slug(board_slug)` | `worker_spawn.rs:114` — computes live path per slug | Yes — routed to correct board DB | FLOWING |
| `init_schema` migration banner | `format_migration_banner(slug_hint, v, SCHEMA_VERSION)` | `store.rs:290` — reads actual `schema_version` row from DB | Yes — `v` comes from real DB query at :262-267 | FLOWING |

---

## Anti-Patterns Scan

Files modified/created by this phase were scanned for debt markers and stubs.

| File | Pattern | Classification | Notes |
|------|---------|---------------|-------|
| `store.rs:600,806,1256,1303` | `BEGIN IMMEDIATE` in comments | INFO | These are doc comments on write methods — not migration code. The migration path at :291 uses `self.conn.transaction()` (rusqlite RAII). Not a debt marker. |
| `worker_spawn.rs` | `#[allow(dead_code)]` on `store` field in tool structs | INFO | Documented forward-compat retention (Plan 07 SUMMARY); not a blocking stub. |
| `boards.rs:410-441` | `BoardToml` / `toml::from_str` | INFO | CR-02 fix — replaces prior hand-rolled parser. Clean implementation. |

No `TBD`, `FIXME`, or `XXX` markers found in phase-modified files. No unreferenced debt markers.

---

## Behavioral Spot-Checks

Step 7b: Code-level checks (no runnable entry points tested without build).

| Behavior | Verification Method | Result |
|----------|-------------------|--------|
| `validate_board_slug("../etc/passwd")` returns `PathTraversal` | `slug.rs:60` fires before downcase at :65 | PASS |
| `resolve_board_context(None)` with no env/file returns `BoardSource::Default` | `resolver.rs:42-44` | PASS |
| `board_db_path_for_slug("default")` returns `kanban_db_path()` | `paths.rs:97-99` branch | PASS |
| `"boards"` in `DEFERRED_KANBAN_SUBVERBS` routes to deferred-CLI message | `handlers.rs:1150,1288` | PASS |
| `open_store_for_board(Some("../evil"))` calls `validate_board_slug` and gets `PathTraversal` before any DB open | `commands.rs:43-44` | PASS |
| `format_migration_banner(Some("alpha"), 1, 2)` == `"[kanban] migrated board 'alpha' from v1 to v2"` | `store.rs:249-256` | PASS |

---

## Requirements Coverage

Phase 36.3.7.9 uses locked CONTEXT decisions D-01..D-08 as requirement IDs (no REQ-IDs in REQUIREMENTS.md for this phase).

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| D-01 | Default board legacy path; named boards at `boards/<slug>/kanban.db` | SATISFIED | `paths.rs:92-100`; `store.rs:201-206` |
| D-02 | 4-tier resolution chain | SATISFIED | `board/resolver.rs:25-57` |
| D-03 | `subscribe_boards = ["*"]` default; `Explicit` inert in v1 | SATISFIED | `notifier_config.rs:39-81`; `notifier.rs:242-272` |
| D-04 | Dispatcher sweeps all boards per tick; slug propagated to workers | SATISFIED | `dispatcher.rs:158-215`; `worker_spawn.rs:98-235` |
| D-05 | Cycle detection per-board unchanged; cross-board blocked at TaskNotFound | SATISFIED | `store.rs:676-688`; `cycle_detection_per_board.rs:1-128` |
| D-06 | Auto-migrate with stderr banner inside RAII transaction | SATISFIED | `store.rs:249-297` |
| D-07 | `boards rm` archives by default; `--delete` requires zero open tasks | SATISFIED | `boards.rs:270-378` |
| D-08 | Every LLM tool envelope carries `board` + `board_source` | SATISFIED | `tools/common.rs:54-125`; `tools_board_envelope.rs` (22 tests) |

CR findings (post-code-review fixes):

| Finding | Fix Location | Status |
|---------|-------------|--------|
| CR-01: slug validation at CLI boundary | `commands.rs:43-45` | SATISFIED |
| CR-02: `BoardToml`/`toml::from_str` replaces hand-rolled parser | `boards.rs:410-441` | SATISFIED |
| CR-03: `LockGuard` RAII for `kanban.db.rm-lock` | `boards.rs:330-338` | SATISFIED |
| CR-04: `Connection::transaction()` RAII replaces raw BEGIN/COMMIT | `store.rs:291-297` | SATISFIED |

---

## Human Verification Required

None. All observable truths are verifiable programmatically via grep and file-level inspection.

---

## Gaps Summary

No gaps. All 12 gates are VERIFIED with concrete file:line anchors. All four critical code-review findings (CR-01 through CR-04) are confirmed fixed in the committed source. All 10 phase-specific test files exist on disk. The docs/kanban/reference.md annotation is present at line 71.

---

_Verified: 2026-05-31T20:27:27Z_
_Verifier: Claude (gsd-verifier) — goal-backward, adversarial stance_
