---
phase: 36.3.7.9
plan: 09
type: execute
wave: 5
depends_on: [36.3.7.9-01, 36.3.7.9-02, 36.3.7.9-03, 36.3.7.9-04, 36.3.7.9-05, 36.3.7.9-06, 36.3.7.9-07, 36.3.7.9-08]
files_modified:
  - crates/ironhermes-kanban/tests/boards_e2e.rs
  - crates/ironhermes-kanban/tests/cycle_detection_per_board.rs
  - crates/ironhermes-kanban/tests/sql_no_slug_literals.rs
  - docs/kanban/reference.md
autonomous: true
requirements: [D-05, D-08]
tags: [multi-board, e2e, integration, kanban, docs]

must_haves:
  truths:
    - "End-to-end test demonstrates: create board alpha → switch → create task → list → tool envelope carries board=alpha,board_source=file"
    - "End-to-end test demonstrates: --board flag overrides current file and HERMES_KANBAN_BOARD env"
    - "Cycle detection works per-board unchanged: tasks on board alpha cannot link to tasks on board beta; ancestor-walk runs against the per-board DB only (D-05 no-code-change assertion)"
    - "T-2 SQL injection check: no SQL string in the kanban crate interpolates a slug as a literal — every store call uses parametrized queries OR uses the slug only as a filesystem path component"
    - "docs/kanban/reference.md §71 v1-NOTE block is replaced with `Shipped in Phase 36.3.7.9` annotation (same pattern as 36.3.7.5 docs reconciliation)"
  artifacts:
    - path: "crates/ironhermes-kanban/tests/boards_e2e.rs"
      provides: "End-to-end integration test for boards create/switch/show/list/rm + --board flag override + tool envelope assertion"
    - path: "crates/ironhermes-kanban/tests/cycle_detection_per_board.rs"
      provides: "D-05 invariant: cycle detection runs per-board and rejects cross-board link attempts cleanly"
    - path: "crates/ironhermes-kanban/tests/sql_no_slug_literals.rs"
      provides: "T-2 mitigation proof: static-grep audit that no SQL string in the kanban crate contains a slug literal"
    - path: "docs/kanban/reference.md"
      provides: "§71 v1-NOTE → Shipped-in-36.3.7.9 reconciliation"
  key_links:
    - from: "crates/ironhermes-kanban/tests/boards_e2e.rs"
      to: "every Wave 1-4 producer"
      via: "exercises the full pipeline: paths + resolver + store + CLI + tools + dispatcher"
      pattern: "open_for_board\\|resolve_board_context\\|inject_board_fields"
    - from: "crates/ironhermes-kanban/tests/cycle_detection_per_board.rs"
      to: "crates/ironhermes-kanban/src/store.rs::insert_link_checked (Phase 36.3.7.6 WITH RECURSIVE)"
      via: "asserts cycle SQL is per-board scoped"
      pattern: "WITH RECURSIVE"
---

<objective>
Close the phase with three integration test files and one docs reconciliation. (1) `boards_e2e.rs` exercises the full multi-board pipeline end-to-end — Plans 03+04+06+07 in a single user-shaped flow. (2) `cycle_detection_per_board.rs` asserts the D-05 no-code-change invariant: cycle detection still works correctly under multi-board because cross-board links are forbidden by construction (each board's DB has its own task_id space). (3) `sql_no_slug_literals.rs` is a static-grep audit proving T-2 (SQL injection via slug) is mitigated — no SQL string in the kanban crate accepts a slug as a literal. (4) `docs/kanban/reference.md` §71 gets its v1-NOTE reconciled, matching the Phase 36.3.7.5 docs-reconciliation pattern.

Purpose: After Plans 01-08 ship, the individual components are unit + integration tested at their seams. This plan closes the verifier-PASS-readiness gap by exercising the full user-shaped pipeline (D-05) and proving the T-2 boundary stays clean.

Output: Three new test files + one docs edit. No production code changes. `cargo test --workspace` exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-CONTEXT.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-RESEARCH.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-PATTERNS.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-VALIDATION.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-01-SUMMARY.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-02-SUMMARY.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-03-SUMMARY.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-04-SUMMARY.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-05-SUMMARY.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-06-SUMMARY.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-07-SUMMARY.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-08-SUMMARY.md

# Existing files referenced
@docs/kanban/reference.md
@crates/ironhermes-kanban/src/store.rs

<interfaces>
<!-- All from Plans 01-08; this plan is consumer-only -->

From crates/ironhermes-kanban/src/board/:
```rust
pub fn resolve_board_context(cli_flag: Option<&str>) -> Result<BoardContext, BoardSlugError>;
```

From crates/ironhermes-kanban/src/store.rs (Plan 02 + 03):
```rust
impl KanbanStore {
    pub fn open_for_board(slug: &str) -> Result<Self>;
    pub fn open_tasks_count(&self) -> Result<i64>;  // from Plan 03 Task 2
}
```

From crates/ironhermes-cli/src/kanban/boards.rs (Plan 03):
```rust
pub async fn cmd_boards_create(slug: String, name: Option<String>, description: Option<String>, switch: bool, json: bool) -> Result<i32>;
pub async fn cmd_boards_switch(slug: String) -> Result<i32>;
pub async fn cmd_boards_show(json: bool) -> Result<i32>;
pub async fn cmd_boards_list(json: bool) -> Result<i32>;
pub async fn cmd_boards_rm(slug: String, delete: bool) -> Result<i32>;
```

From crates/ironhermes-kanban/src/tools/* (Plan 07):
```rust
// every tool's execute(args) returns a JSON string containing "board" + "board_source"
```

<!-- Phase 36.3.7.6 producer (D-05 reference) -->

From crates/ironhermes-kanban/src/store.rs::insert_link_checked (Phase 36.3.7.6):
```rust
// WITH RECURSIVE ancestor walk against the active connection only
pub fn insert_link_checked(...) -> Result<...>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create end-to-end integration test exercising the full multi-board pipeline</name>
  <read_first>
    - crates/ironhermes-kanban/tests/tools_smoke.rs (existing receiver-end tests — pattern reference)
    - crates/ironhermes-cli/tests/boards_lifecycle.rs (Plan 03 Task 2 output)
    - crates/ironhermes-kanban/tests/boards_lifecycle.rs (if Plan 03 used kanban-crate tests location)
    - .planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-VALIDATION.md (per-task verification map)
  </read_first>
  <behavior>
    boards_e2e.rs exercises ≥3 user-shaped scenarios:

    Scenario A — "name a new board, switch to it, create a task, list, verify tool envelope":
      1. set IRONHERMES_HOME = tempdir.
      2. call cmd_boards_create("alpha", None, None, switch=true, json=false). Assert exit 0.
      3. assert paths::board_dir("alpha").exists() AND paths::current_board_file() contents == "alpha".
      4. call cmd_boards_show(json=true). Capture stdout. Assert JSON contains "slug":"alpha", "source":"file".
      5. via direct KanbanStore::open_for_board("alpha") → CreateTaskOptions API, insert one task. Assert no row appears in `kanban_db_path()` (default DB) — proves isolation.
      6. invoke KanbanListTool::execute({"board": null}). Assert envelope contains "board":"alpha","board_source":"file" AND lists the one new task.
      7. invoke KanbanListTool::execute({"board": "default"}). Assert envelope contains "board":"default","board_source":"flag" AND DOES NOT list the alpha task.

    Scenario B — "--board flag overrides env and file":
      1. set IRONHERMES_HOME = tempdir, current file = "alpha", HERMES_KANBAN_BOARD = "beta", create boards alpha + beta + gamma.
      2. invoke KanbanShowTool::execute({"board": "gamma", "task_id": <gamma-task-id>}). Assert envelope contains "board":"gamma","board_source":"flag".
      3. invoke KanbanShowTool::execute({"task_id": <alpha-task-id>}) — no board arg. Assert envelope contains "board":"beta","board_source":"env" (env tier wins over file). Note: lookup fails since the task lives on alpha not beta; assert rejection envelope STILL contains "board":"beta","board_source":"env" (T-5).

    Scenario C — "archive then verify board.dir is gone and _archived/alpha-<ts>/ exists":
      1. create alpha board with one task.
      2. cmd_boards_rm("alpha", delete=false). Assert exit 0.
      3. assert !paths::board_dir("alpha").exists() AND archive_dir().read_dir() contains exactly one entry matching `alpha-\d+`.
      4. assert the archived kanban.db is intact (open via KanbanStore::open(archived_path); read the task).

    All scenarios use the env_lock pattern for serialization. Tests are `#[tokio::test]` because cmd_* fns are async.
  </behavior>
  <action>
    Create `crates/ironhermes-kanban/tests/boards_e2e.rs` with three `#[tokio::test]` scenarios per the behavior block. Each scenario:
      - Acquires env_lock at the top.
      - Sets IRONHERMES_HOME to a tempdir.
      - Builds the scenario inputs (boards, tasks, env vars, current file).
      - Invokes the producer functions across crates (cmd_boards_*, KanbanStore::*, tool.execute).
      - Asserts the named invariants.
      - Restores env_lock on scope exit.

    For tools whose execute takes args via JSON Value: build args inline via `serde_json::json!({"board": "alpha", ...})`.

    For the boards CLI cmd_* calls from the kanban-crate integration test: this means the kanban crate's test depends on ironhermes-cli's cmd_boards_* fns. If ironhermes-cli is not yet a dev-dep of ironhermes-kanban, add it: `[dev-dependencies] ironhermes-cli = { workspace = true }`. (Precedent: Phase 36.3.7.5 Plan 04 added ironhermes-kanban as a dev-dep of ironhermes-core for the keystone e2e test.) If the dependency direction is awkward, MOVE the test file to `crates/ironhermes-cli/tests/boards_e2e.rs` and make ironhermes-cli call the kanban tools directly — pick whichever side has the cheaper dev-dep delta.

    The scenarios MUST be runnable in CI without manual operator action. Document the scenario count in the SUMMARY.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-kanban --test boards_e2e</automated>
  </verify>
  <acceptance_criteria>
    - Source: `crates/ironhermes-kanban/tests/boards_e2e.rs contains #[tokio::test]` (or `crates/ironhermes-cli/tests/boards_e2e.rs` if moved)
    - Source: contains assertion strings for each of the 3 scenarios A/B/C
    - Behavior: Scenario A locks the boards-create+switch+tool-envelope user-shape (D-08 file-source tier)
    - Behavior: Scenario B locks the 4-tier precedence (D-02 flag > env > file > default)
    - Behavior: Scenario C locks the D-07 archive semantics + recoverability
    - Test: `cargo test -p ironhermes-kanban --test boards_e2e` (or cli location) exits 0 with ≥3 scenarios
    - Build: `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>
    The full pipeline is exercised end-to-end in a CI-runnable test; the three locked scenarios prove D-02, D-07, D-08 hold under realistic user flows.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert D-05 cycle-detection no-change invariant + T-2 SQL injection mitigation proof</name>
  <read_first>
    - crates/ironhermes-kanban/src/store.rs::insert_link_checked (Phase 36.3.7.6 producer of WITH RECURSIVE ancestor walk)
    - crates/ironhermes-kanban/tests/tools_smoke.rs (existing `kanban_link_rejects_cycle` from Phase 36.3.7.6 — verify it still passes after Plan 07 envelope changes)
    - .planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-CONTEXT.md (D-05 lines 66-69)
  </read_first>
  <behavior>
    cycle_detection_per_board.rs has ≥3 tests:
      - `cycle_detection_works_unchanged_on_default_board`: seeds a 3-task A→B→C chain on default; attempts C→A link; asserts LinkCycle rejection. Re-locks the Phase 36.3.7.6 invariant under multi-board (D-05).
      - `cycle_detection_works_independently_on_named_board`: same A→B→C+C→A scenario but seeded entirely on alpha board; asserts LinkCycle rejection. Proves the ancestor-walk SQL runs per-board correctly.
      - `cross_board_link_is_blocked_by_construction_not_by_cycle_sql`: creates task X on default and task Y on alpha; attempts insert_link_checked(parent=X, child=Y) against the alpha connection — assert TaskNotFound rejection (because X does not exist in alpha's DB). This demonstrates the D-05 rationale: cross-board links are blocked at the foreign-key/lookup layer, NOT at the cycle-detection layer; the cycle SQL only sees same-board task_ids.

    sql_no_slug_literals.rs has ≥1 test:
      - `no_sql_string_contains_a_slug_literal_in_kanban_crate`: a static grep test using `include_str!` on every .rs file in `crates/ironhermes-kanban/src/store.rs` and the schema/migration files. Searches for patterns that look like SQL string + literal slug embedding (e.g., `format!("... {slug}", ...)` where the context is rusqlite execute) — practical heuristic: count occurrences of `format!(` AND `execute(`/`query_row(` on the SAME line OR within 3 lines. Simpler approach: assert that the kanban crate's source contains ZERO instances of the pattern `format!(.*{slug}.*WHERE\b` or `format!(.*WHERE.*{slug}` — these would be the SQL-injection-risk patterns.

      Approach the executor MUST use: do a focused grep — `grep -rE 'format!\([^)]*WHERE' crates/ironhermes-kanban/src/` should return zero matches (or only matches that are entirely in test files or comments). Embed the audit as a `#[test]` that runs `Command::new("grep")` and asserts exit code != 0 OR an empty match set. Alternative: build a list of files via `walkdir` (a dev-dep, common) and read each + assert no occurrence of `format!.*WHERE` after stripping comments and string-literal contents. PREFER the grep-based test — simplest, no new dev-dep.

      Document the test's exact mechanic in a top-of-file doc comment so future PRs understand what it locks (T-2 mitigation: every SQL statement uses `?N` parametrized binds or static SQL; the slug never appears as a literal inside a SQL string).
  </behavior>
  <action>
    Create `crates/ironhermes-kanban/tests/cycle_detection_per_board.rs`. Use env_lock + tempdir IRONHERMES_HOME. Three `#[test]` functions per the behavior block above. Each test:
      - Opens the appropriate per-board store via KanbanStore::open_for_board.
      - Seeds tasks via the existing CreateTaskOptions API.
      - Attempts the link via store.insert_link_checked or equivalent.
      - Asserts the expected error variant (LinkCycle, TaskNotFound).

    Create `crates/ironhermes-kanban/tests/sql_no_slug_literals.rs`. Implement the static-grep audit per the behavior block. Run `Command::new("grep").args(["-rE", "format!\\([^)]*WHERE", "crates/ironhermes-kanban/src/"]).output()` and assert that the captured stdout contains zero lines OR lines only in test files (`/tests/` substring). Document the heuristic and its limitations in a top-of-file comment block.

    Verify both test files compile and pass via the verify command below. If the grep heuristic produces false positives (e.g., a legitimate format! that builds a non-SQL string with "WHERE" elsewhere in its body), refine the regex by including the rusqlite call context (`format!.*WHERE.*\.execute\(` or similar), OR enumerate the false-positive lines and assert against a known whitelist.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-kanban --test cycle_detection_per_board && cargo test -p ironhermes-kanban --test sql_no_slug_literals</automated>
  </verify>
  <acceptance_criteria>
    - Source: `crates/ironhermes-kanban/tests/cycle_detection_per_board.rs contains LinkCycle`
    - Source: `crates/ironhermes-kanban/tests/cycle_detection_per_board.rs contains TaskNotFound` (cross-board test)
    - Source: `crates/ironhermes-kanban/tests/sql_no_slug_literals.rs contains grep` or `Command::new`
    - Behavior: cycle detection on alpha board independently catches an alpha-only A→B→C+C→A cycle (D-05)
    - Behavior: cross-board insert_link_checked returns TaskNotFound (NOT LinkCycle), proving the multi-board isolation is at the lookup layer
    - Behavior: SQL-audit test passes — no `format!(...)` SQL strings contain a slug literal in the kanban crate source
    - Test: `cargo test -p ironhermes-kanban --test cycle_detection_per_board` exits 0 (≥3 tests)
    - Test: `cargo test -p ironhermes-kanban --test sql_no_slug_literals` exits 0 (≥1 test)
    - Build: `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>
    D-05 no-code-change invariant locked by a passing test; T-2 SQL injection mitigation proved by a static audit that fails on regression.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile docs/kanban/reference.md §71 v1-NOTE blocks to "Shipped in Phase 36.3.7.9"</name>
  <read_first>
    - docs/kanban/reference.md (find §71-§110 and grep for "deferred to Phase 36.3.7.9" or any v1-NOTE referencing the multi-board CLI)
    - .planning/phases/36.3.7.5-* SUMMARYs (Phase 36.3.7.5 Plan 04 docs-reconciliation pattern — search for "Shipped in Phase 36.3.7.5" — same pattern applies here)
  </read_first>
  <behavior>
    - Every v1-NOTE block in docs/kanban/reference.md that references "deferred to Phase 36.3.7.9" (or the equivalent forward-pointer naming the multi-board CLI) is replaced with `Shipped in Phase 36.3.7.9` annotations describing the shipped behavior.
    - Replacement annotations name the specific D-XX decisions that materialized (e.g., "Per D-01 the default board stays at ~/.ironhermes/kanban.db; named boards live at ~/.ironhermes/kanban/boards/<slug>/. Per D-06 schema migrations auto-run with stderr banner.").
    - Format mirrors Phase 36.3.7.5 docs-reconciliation pattern from STATE.md.
    - Existing prose using `~/.hermes/` (the doc-only inaccuracy from RESEARCH.md lines 71-77) is corrected to `~/.ironhermes/` in the same edit pass — only within the changed sections; leave unrelated prose untouched.
    - The verifier gate `grep -c 'deferred to Phase 36.3.7.9' docs/kanban/reference.md == 0` passes after this task.
  </behavior>
  <action>
    Open `docs/kanban/reference.md`. Grep for `deferred to Phase 36.3.7.9` and `deferred to Phase 36.3.7.3` (the original placeholder mentioned in ROADMAP — the docs may use either). Also grep for `multi-board` and `boards list/create/switch` in case the v1-NOTE uses different phrasing.

    For each match: replace the v1-NOTE block with a `Shipped in Phase 36.3.7.9` annotation. The annotation should be a 2-4 line block summarizing the materialized decisions for that doc section. Examples:
      - For §71 (on-disk layout): "Shipped in Phase 36.3.7.9. Per D-01 the default board stays at `~/.ironhermes/kanban.db` (no migration); named boards live at `~/.ironhermes/kanban/boards/<slug>/kanban.db`."
      - For §95-110 (CLI resolution chain): "Shipped in Phase 36.3.7.9. Per D-02 the 4-tier precedence is `--board` flag > `HERMES_KANBAN_BOARD` env > `~/.ironhermes/kanban/current` file > `default`."
      - For §104 (rename semantics): "Shipped in Phase 36.3.7.9. Per Pitfall 7 `boards rename` changes the display name (stored in `<board_dir>/board.toml`); the slug is immutable."

    Within the modified sections only, replace any `~/.hermes/` reference with `~/.ironhermes/` (RESEARCH.md path root disambiguation). Do NOT do a global sweep — only the touched lines.

    The verifier gate `grep -c 'deferred to Phase 36.3.7.9' docs/kanban/reference.md == 0` must pass. Run this grep manually after the edits to confirm.

    No new tests for docs. The verification step is the grep.
  </action>
  <verify>
    <automated>test "$(grep -c 'deferred to Phase 36.3.7.9' docs/kanban/reference.md)" = "0" && test "$(grep -c 'Shipped in Phase 36.3.7.9' docs/kanban/reference.md)" -ge "1"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'deferred to Phase 36.3.7.9' docs/kanban/reference.md == 0` (all forward-pointers replaced)
    - Source: `grep -c 'Shipped in Phase 36.3.7.9' docs/kanban/reference.md >= 1` (at least one materialized annotation present)
    - Behavior: every modified section names the specific D-XX decision(s) materializing the doc claim
    - Build: docs change does not affect cargo; `cargo build --workspace` exits 0 (unchanged)
  </acceptance_criteria>
  <done>
    The docs no longer carry stale forward-pointers to Phase 36.3.7.9; the materialized behavior is annotated with D-XX traceability matching the Phase 36.3.7.5 precedent.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Multi-plan integration surface | E2E test exercises the union of Plans 01-08 producers and asserts each link works in combination |
| Static SQL audit | If a future PR introduces a `format!(...)` SQL string with a slug literal, the audit fails |
| Cycle-detection invariant | If a future PR moves cycle detection to a cross-board surface, the D-05 test fails |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.9-09-01 (=T-2 from planning_context) | Tampering / Injection | SQL slug literal | mitigate | Static-grep audit in `sql_no_slug_literals.rs` fails on any `format!(...)WHERE` pattern in production SQL; locks T-2 mitigation by regression test |
| T-36.3.7.9-09-02 | Repudiation | docs/code drift | mitigate | docs/kanban/reference.md §71 reconciled to name the shipped decisions; verifier grep gate enforces the rename |
| T-36.3.7.9-09-03 | Spoofing | cross-board task link via cycle SQL exploitation | mitigate | `cross_board_link_is_blocked_by_construction_not_by_cycle_sql` test proves cross-board links fail at the foreign-key layer (TaskNotFound), not at the cycle layer — D-05 rationale locked |
| T-36.3.7.9-09-SC | Supply Chain | tests only | accept | This plan adds zero production code; tests only |
</threat_model>

<verification>
- `cargo build --workspace` exits 0
- `cargo test -p ironhermes-kanban --test boards_e2e` (or cli location) exits 0
- `cargo test -p ironhermes-kanban --test cycle_detection_per_board` exits 0
- `cargo test -p ironhermes-kanban --test sql_no_slug_literals` exits 0
- `cargo test --workspace` exits 0 (full suite, zero regressions across the whole phase)
- `cargo clippy --workspace -- -D warnings` exits 0
- `grep -c 'deferred to Phase 36.3.7.9' docs/kanban/reference.md == 0`
- `grep -c 'Shipped in Phase 36.3.7.9' docs/kanban/reference.md >= 1`
- `grep -c 'WITH RECURSIVE' crates/ironhermes-kanban/src/store.rs >= 1` (Phase 36.3.7.6 cycle SQL preserved — D-05 no-code-change confirmation)
</verification>

<success_criteria>
The phase ships verifier-ready: the full pipeline is exercised end-to-end; D-05 no-code-change is proved by a passing test; T-2 SQL injection mitigation is locked by a static audit; the docs are reconciled to name the shipped behavior. `cargo test --workspace` is green across all 9 plans.
</success_criteria>

<output>
Create `.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-09-SUMMARY.md` with:
- The bilateral-tracing table covering Tasks 1-3.
- The full-workspace `cargo test --workspace` exit code.
- The docs reconciliation grep counts (deferred → 0; Shipped → ≥1).
- The D-05 + T-2 mitigation proof (named tests + assertion lines).
- Suggested follow-ups for any deferred work surfaced during execution (e.g., Arc-per-board dispatcher caching).
</output>
