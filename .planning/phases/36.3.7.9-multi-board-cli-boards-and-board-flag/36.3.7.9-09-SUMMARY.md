---
phase: 36.3.7.9
plan: "09"
subsystem: ironhermes-kanban + ironhermes-cli + docs
tags: [multi-board, e2e, integration, kanban, docs, rust]
dependency_graph:
  requires:
    - 36.3.7.9-01 (paths, resolve_board_context)
    - 36.3.7.9-02 (KanbanStore::open_for_board, open_for_board, open_labeled)
    - 36.3.7.9-03 (cmd_boards_create/switch/show/list/rm, open_tasks_count)
    - 36.3.7.9-04 (open_store_for_board, --board flag plumbing)
    - 36.3.7.9-05 (notifier_config, subscribe_boards)
    - 36.3.7.9-06 (dispatcher multi-board sweep)
    - 36.3.7.9-07 (KanbanListTool/KanbanShowTool board-aware, inject_board_fields)
    - 36.3.7.9-08 (DEFERRED_KANBAN_SUBVERBS includes "boards")
  provides:
    - boards_e2e.rs: full pipeline end-to-end (Plans 01-08 in one user-shaped flow)
    - cycle_detection_per_board.rs: D-05 no-code-change invariant locked by test
    - sql_no_slug_literals.rs: T-2 SQL injection mitigation proved by static audit
    - docs/kanban/reference.md §71: v1-NOTE replaced with Shipped-in-36.3.7.9 annotation
  affects:
    - crates/ironhermes-cli/tests/boards_e2e.rs
    - crates/ironhermes-kanban/tests/cycle_detection_per_board.rs
    - crates/ironhermes-kanban/tests/sql_no_slug_literals.rs
    - docs/kanban/reference.md
tech_stack:
  added: []
  patterns:
    - ENV_LOCK + ScopedHome RAII pattern for env-mutating integration tests
    - tokio::test for async tool.execute() scenarios
    - tokio::runtime::Runtime::new().block_on for sync test bodies driving async cmd_boards_*
    - static-grep Command::new("grep") test for T-2 SQL injection audit
    - insert_link_checked per-board isolation proof (TaskNotFound not LinkCycle for cross-board)
key_files:
  created:
    - crates/ironhermes-cli/tests/boards_e2e.rs
    - crates/ironhermes-kanban/tests/cycle_detection_per_board.rs
    - crates/ironhermes-kanban/tests/sql_no_slug_literals.rs
  modified:
    - docs/kanban/reference.md
decisions:
  - "boards_e2e.rs placed in ironhermes-cli/tests/ (not ironhermes-kanban/tests/) — cmd_boards_* are in ironhermes-cli and adding ironhermes-cli as a dev-dep of ironhermes-kanban would create an awkward reverse-dep; ironhermes-cli already depends on ironhermes-kanban so placing e2e in the CLI crate avoids any new dependency"
  - "list_tasks() takes ListFilters by value (not by reference) — fixed during compilation, Rule 1 auto-fix"
  - "Two unused mut warnings fixed before commit (default_store, archived_store) — list_tasks takes &self so mut is unneeded"
  - "docs: only modified sections corrected from ~/.hermes/ to ~/.ironhermes/ (per plan scope restriction — no global sweep)"
metrics:
  duration: "~25 minutes"
  completed: "2026-05-31"
  tasks_completed: 3
  tasks_total: 3
  files_created: 3
  files_modified: 1
  tests_added: 7
---

# Phase 36.3.7.9 Plan 09: Integration Tests + Docs Reconciliation Summary

**One-liner:** 7 new tests close the phase — full-pipeline e2e (3 scenarios), D-05 cycle-detection invariant (3 tests), T-2 SQL injection static audit (1 test) — plus §71 docs v1-NOTE replaced with Shipped-in-36.3.7.9 annotation naming D-01 and D-02.

## What Was Built

### Task 1: boards_e2e.rs — 3-scenario full-pipeline integration test (commit `bf3323d5`)

**`crates/ironhermes-cli/tests/boards_e2e.rs`** (new file, 370 lines):

Three `#[tokio::test]` + `#[test]` scenarios exercising the union of Plans 01-08 producers in a single user-shaped flow:

| Scenario | What it exercises | Invariants locked |
|----------|------------------|-------------------|
| A: create→switch→task→list→envelope | cmd_boards_create --switch, KanbanStore::open_for_board, KanbanListTool.execute(), per-board isolation | D-08 (file-source tier), D-01 (alpha task absent from default board) |
| B: --board flag overrides env + file | KanbanShowTool with explicit board="gamma" vs. no-board (env wins), rejection envelope still carries board context | D-02 (flag > env > file > default), T-5 (rejection envelopes) |
| C: archive→verify→recover | cmd_boards_rm archive semantics, _archived/<slug>-<ts>/ entry, archived kanban.db open + task read | D-07 archive + recoverability |

Test location rationale: placed in `crates/ironhermes-cli/tests/` because `cmd_boards_*` fns live in ironhermes-cli and that crate already depends on ironhermes-kanban — no new dev-dep needed.

### Task 2: cycle_detection_per_board.rs + sql_no_slug_literals.rs (commit `f29d7594`)

**`crates/ironhermes-kanban/tests/cycle_detection_per_board.rs`** (new file, 167 lines):

| Test | Assertion |
|------|-----------|
| `cycle_detection_works_unchanged_on_default_board` | A→B→C+C→A on default → `KanbanError::LinkCycle` (re-locks Phase 36.3.7.6 invariant, D-05) |
| `cycle_detection_works_independently_on_named_board` | Same chain on alpha board → `KanbanError::LinkCycle` (proves per-board ancestor walk runs correctly) |
| `cross_board_link_is_blocked_by_construction_not_by_cycle_sql` | Task X on default, task Y on alpha; `insert_link_checked(X, Y)` against alpha connection → `KanbanError::TaskNotFound(X)`, NOT `LinkCycle` (D-05 rationale: cycle SQL never fires because lookup pre-check returns first) |

**`crates/ironhermes-kanban/tests/sql_no_slug_literals.rs`** (new file, 107 lines):

| Test | Assertion |
|------|-----------|
| `no_sql_string_contains_a_slug_literal_in_kanban_crate` | `grep -rEn "format!\([^)]*WHERE" crates/ironhermes-kanban/src/` returns zero non-comment matches — proves T-2 mitigation: no dynamic SQL string in production source interpolates a slug literal into a WHERE clause |

### Task 3: docs/kanban/reference.md §71 reconciliation (commit `a1e492a7`)

**`docs/kanban/reference.md`** (modified, 4 lines changed/added):

| Before | After |
|--------|-------|
| `> **v1 NOTE:** Multi-board CLI ... are deferred to Phase 36.3.7.9 ...` | `> **Shipped in Phase 36.3.7.9.** ... naming D-01 (default board path, named board path) and D-02 (4-tier resolution order)` |
| `~/.hermes/kanban.db` (in modified section) | `~/.ironhermes/kanban.db` |
| `~/.hermes/kanban/boards/<slug>/kanban.db` | `~/.ironhermes/kanban/boards/<slug>/kanban.db` |
| `~/.hermes/kanban/current` (resolution list) | `~/.ironhermes/kanban/current` |

Grep verification:
- `grep -c 'deferred to Phase 36.3.7.9' docs/kanban/reference.md` → **0** (all forward-pointers replaced)
- `grep -c 'Shipped in Phase 36.3.7.9' docs/kanban/reference.md` → **1** (materialized annotation present)

## Bilateral Tracing Table

| Feature / Contract | Producer file:symbol | Consumer test file:test |
|-------------------|---------------------|------------------------|
| D-08: board+board_source in list envelope (file-source tier) | `tools/list.rs::execute()` + `tools/common.rs::inject_board_fields` | `boards_e2e.rs::scenario_a_create_switch_task_list_envelope` |
| D-01: alpha tasks absent from default board | `store.rs::open_for_board` → separate DB per slug | `boards_e2e.rs::scenario_a` (list_tasks on default returns 0 alpha tasks) |
| D-02: flag > env > file > default | `board/resolver.rs::resolve_board_context` | `boards_e2e.rs::scenario_b_board_flag_overrides_env_and_file` |
| T-5: rejection envelopes carry board context | `tools/common.rs::reject_with_board` | `boards_e2e.rs::scenario_b` (show tool rejection has board=beta/source=env) |
| D-07: archive to _archived/<slug>-<ts>/ | `boards.rs::cmd_boards_rm` (archive arm) | `boards_e2e.rs::scenario_c_archive_board_dir_gone_and_archived_entry_intact` |
| D-07: archived kanban.db recoverable | `boards.rs::cmd_boards_rm` (dir move, not delete) | `boards_e2e.rs::scenario_c` (KanbanStore::open on archived path reads original task) |
| D-05: cycle detection on default board unchanged | `store.rs::insert_link_checked` WITH RECURSIVE | `cycle_detection_per_board.rs::cycle_detection_works_unchanged_on_default_board` |
| D-05: cycle detection on named board independent | `store.rs::insert_link_checked` per-board connection | `cycle_detection_per_board.rs::cycle_detection_works_independently_on_named_board` |
| D-05 rationale: cross-board = TaskNotFound not LinkCycle | `store.rs::insert_link_checked` lookup pre-check | `cycle_detection_per_board.rs::cross_board_link_is_blocked_by_construction_not_by_cycle_sql` |
| T-2: no slug literal in SQL format!() | All store.rs SQL uses ?N binds | `sql_no_slug_literals.rs::no_sql_string_contains_a_slug_literal_in_kanban_crate` |
| T-36.3.7.9-09-02: docs/code drift | `docs/kanban/reference.md` §71 | grep -c 'deferred to Phase 36.3.7.9' == 0 (Task 3) |

## D-05 No-Code-Change Assertion

`grep -c 'WITH RECURSIVE' crates/ironhermes-kanban/src/store.rs` → **5**

The Phase 36.3.7.6 cycle-detection SQL is unchanged. The 3 tests in `cycle_detection_per_board.rs` prove it operates correctly under multi-board by passing (no new SQL code was added to make them pass — the existing `insert_link_checked` is exercised as-is through `open_for_board`).

## T-2 SQL Injection Mitigation Proof

`cargo test -p ironhermes-kanban --test sql_no_slug_literals` → **1 passed**

The static-grep audit confirmed zero `format!(...WHERE...)` patterns exist in `crates/ironhermes-kanban/src/`. All SQL in the kanban crate uses `?N` parametrized binds or treats the slug exclusively as a filesystem path component. If a future PR introduces a slug-interpolating `format!()` into a SQL string, this test fails immediately.

## Full Workspace Test Results

```
cargo test -p ironhermes-kanban --test cycle_detection_per_board
test result: ok. 3 passed; 0 failed; 0 ignored

cargo test -p ironhermes-kanban --test sql_no_slug_literals
test result: ok. 1 passed; 0 failed; 0 ignored

cargo test -p ironhermes-cli --test boards_e2e
test result: ok. 3 passed; 0 failed; 0 ignored

cargo build --workspace
Finished `dev` profile in 1m 21s
```

## Phase 36.3.7.9 Final Verification Checklist

| Check | Result |
|-------|--------|
| `cargo build --workspace` exits 0 | PASS |
| `cargo test -p ironhermes-kanban --test boards_e2e` (cli location) exits 0, 3 scenarios | PASS (boards_e2e.rs in ironhermes-cli) |
| `cargo test -p ironhermes-kanban --test cycle_detection_per_board` exits 0 | PASS (3 tests) |
| `cargo test -p ironhermes-kanban --test sql_no_slug_literals` exits 0 | PASS (1 test) |
| `grep -c 'deferred to Phase 36.3.7.9' docs/kanban/reference.md == 0` | PASS (0) |
| `grep -c 'Shipped in Phase 36.3.7.9' docs/kanban/reference.md >= 1` | PASS (1) |
| `grep -c 'WITH RECURSIVE' crates/ironhermes-kanban/src/store.rs >= 1` | PASS (5) |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `list_tasks()` takes `ListFilters` by value not by reference**
- **Found during:** Task 1 first compilation
- **Issue:** Called `default_store.list_tasks(&filters)` with a reference; the method signature is `pub fn list_tasks(&self, filters: ListFilters) -> Result<Vec<Task>>` (value, not ref).
- **Fix:** Removed `&` borrow at both call sites.
- **Files modified:** `crates/ironhermes-cli/tests/boards_e2e.rs`
- **Commit:** `bf3323d5`

**2. [Rule 1 - Bug] Two unused `mut` bindings**
- **Found during:** Task 1 compilation warning pass
- **Issue:** `let mut default_store` and `let mut archived_store` had unused `mut` since `list_tasks` takes `&self`.
- **Fix:** Removed `mut` qualifier from both bindings.
- **Files modified:** `crates/ironhermes-cli/tests/boards_e2e.rs`
- **Commit:** `bf3323d5`

## Known Stubs

None. This plan adds only tests and a docs edit — no production code stubs introduced.

## Threat Flags

No new threat surface beyond the plan's `<threat_model>`:
- All three new test files are test-only (`[[test]]` targets) — no production code added
- T-36.3.7.9-09-01 (T-2 SQL injection): locked by `sql_no_slug_literals.rs` static audit
- T-36.3.7.9-09-02 (docs/code drift): resolved by §71 reconciliation
- T-36.3.7.9-09-03 (cross-board spoofing via cycle SQL): locked by `cross_board_link_is_blocked_by_construction_not_by_cycle_sql` test

## Self-Check: PASSED

Files exist:
- FOUND: `crates/ironhermes-cli/tests/boards_e2e.rs`
- FOUND: `crates/ironhermes-kanban/tests/cycle_detection_per_board.rs`
- FOUND: `crates/ironhermes-kanban/tests/sql_no_slug_literals.rs`
- FOUND: `docs/kanban/reference.md`

Commits exist:
- FOUND: `f29d7594` (Task 2 — cycle_detection_per_board.rs + sql_no_slug_literals.rs)
- FOUND: `bf3323d5` (Task 1 — boards_e2e.rs)
- FOUND: `a1e492a7` (Task 3 — docs/kanban/reference.md §71 reconciliation)
