---
phase: 36.3.7.9
plan: 08
type: execute
wave: 3
depends_on: [36.3.7.9-03]
files_modified:
  - crates/ironhermes-core/src/commands/handlers.rs
autonomous: true
requirements: [D-04]
tags: [multi-board, gateway, deferred-subverbs, kanban, rust]

must_haves:
  truths:
    - "DEFERRED_KANBAN_SUBVERBS contains the literal string \"boards\""
    - "Gateway `/kanban boards <verb>` slash dispatch routes to the deferred-CLI message verbatim (same as `swarm` and `mention` precedent)"
    - "No new gateway-side slash arm is wired — `boards` is a CLI-only surface in this phase (same scope-fence pattern as 36.3.7.6 link/unblock and 36.3.7.7 swarm)"
    - "Existing DEFERRED_KANBAN_SUBVERBS entries are byte-stable (no removals, no reorderings)"
  artifacts:
    - path: "crates/ironhermes-core/src/commands/handlers.rs"
      provides: "DEFERRED_KANBAN_SUBVERBS array gains exactly one new entry: \"boards\""
  key_links:
    - from: "gateway slash dispatch `/kanban boards list`"
      to: "crates/ironhermes-core/src/commands/handlers.rs::cmd_kanban router"
      via: "args.first() == Some(\"boards\") matches the deferred-subverb list"
      pattern: "DEFERRED_KANBAN_SUBVERBS"
---

<objective>
Add the single literal `"boards"` to `DEFERRED_KANBAN_SUBVERBS` so gateway `/kanban boards <verb>` slash dispatch routes to the existing "deferred to CLI" message verbatim — the same precedent established by `"swarm"` (Phase 36.3.7.7) and `"mention"` (Phase 36.3.7.8). No new gateway-side slash dispatch arm is wired; the `boards` surface is CLI-only in this phase.

Purpose: Without this single-entry addition, a gateway slash like `/kanban boards list` would either fall through to an unknown-subverb error path OR (worse) collide with the `kanban list` arm's args interpretation. The single-entry pattern matches the exhaustive routing precedent at `handlers.rs:1143-1150`. This is a tiny scope-fenced plan (single file, single line, single test) decoupled from Plan 03's BoardsCommands enum work — both can run in parallel within Wave 3.

Output: One literal added to one array; one regression test locking the array's membership of `"boards"`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-CONTEXT.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-RESEARCH.md
@.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-PATTERNS.md

# File this plan modifies
@crates/ironhermes-core/src/commands/handlers.rs

<interfaces>
<!-- Existing DEFERRED_KANBAN_SUBVERBS — handlers.rs:1143-1150 verbatim -->

```rust
const DEFERRED_KANBAN_SUBVERBS: &[&str] = &[
    "claim", "complete", "block", "unblock", "comment", "archive",
    "reclaim", "reassign", "assign", "link", "unlink",
    "init", "tail", "watch", "runs", "assignees", "dispatch",
    "stats", "log", "context", "gc", "daemon", "diagnostics",
    "swarm",
    "mention",
];
```

<!-- Router shape — handlers.rs:1161-1167 (per RESEARCH §11) -->

```rust
fn cmd_kanban(args: &[&str], ctx: &CommandContext) -> CommandResult {
    match args.first().copied() {
        None | Some("list") => CommandResult::Output(store.list_text()),
        Some("show") => { ... }
        Some("create") => { ... } // Phase 36.3.7.5
        Some(other) if DEFERRED_KANBAN_SUBVERBS.contains(&other) => {
            CommandResult::Output(format!("'{other}' is deferred to the CLI..."))
        }
        Some(other) => { ... unknown subverb ... }
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append "boards" to DEFERRED_KANBAN_SUBVERBS + lock with regression test</name>
  <read_first>
    - crates/ironhermes-core/src/commands/handlers.rs (lines 1140-1170 — read the array AND the router match block)
    - .planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-PATTERNS.md (§12 lines 798-829)
    - .planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-RESEARCH.md (§11 lines 770-814 — single-entry vs per-leaf decision)
  </read_first>
  <behavior>
    - The DEFERRED_KANBAN_SUBVERBS array gains exactly ONE new entry: `"boards"` (single string, lowercase, matches the `args.first()` token).
    - The new entry is placed on its own line BELOW `"mention",` (matching the Phase 36.3.7.6 swarm + 36.3.7.8 mention placement — one entry per line for the new additions).
    - No existing entries are removed, renamed, or reordered.
    - A gateway slash `/kanban boards list` flows through `cmd_kanban` → `args = ["boards", "list"]` → `args.first() == Some("boards")` → matches `DEFERRED_KANBAN_SUBVERBS.contains(&"boards")` → returns the "deferred to CLI" output. (The downstream `list` token is not consumed; the deferred message is the same whether the user typed `/kanban boards` or `/kanban boards list`.)
    - The pre-existing test coverage on the DEFERRED_KANBAN_SUBVERBS array (if any) continues to pass byte-stably.
  </behavior>
  <action>
    Open `crates/ironhermes-core/src/commands/handlers.rs`. Locate the `DEFERRED_KANBAN_SUBVERBS` constant at lines 1143-1150 (per RESEARCH §11). Append a single new line `    "boards",   // Phase 36.3.7.9` AFTER the existing `"mention",` entry and BEFORE the closing `];`. Match the indentation and quoting style of the existing entries exactly.

    Add a regression test to a suitable test file. Options:
      (a) Add to an existing handlers test file if one exists (e.g., `crates/ironhermes-core/tests/handlers_kanban_notify.rs` from Phase 36.3.7.5 Plan 04).
      (b) Create a new tiny test file `crates/ironhermes-core/tests/handlers_deferred_subverbs.rs`.
    PREFER (b) for scope isolation.

    Test (≥2):
      - `deferred_kanban_subverbs_contains_boards`: imports `ironhermes_core::commands::handlers::DEFERRED_KANBAN_SUBVERBS` (or via a pub re-export — if the const is private, add `pub(crate)` and re-export via `crates/ironhermes-core/src/commands/mod.rs` as `pub use handlers::DEFERRED_KANBAN_SUBVERBS;` ONLY IF needed for the test; alternatively, use the existing `cmd_kanban` dispatch with an args slice and assert the deferred-CLI message text contains the substring `"boards"`). PREFER the dispatch-based test — black-box behavior assertion, no API surface change.
      - `deferred_kanban_subverbs_existing_entries_byte_stable`: assert that calling cmd_kanban with each of `["swarm"]`, `["mention"]`, `["link"]`, `["unblock"]`, `["dispatch"]` still routes to the deferred-CLI message (locks "no removals" invariant).

    Dispatch-based test mechanics: invoke `cmd_kanban(&["boards", "list"], &ctx)` (or whatever the actual dispatch function name is — read the file to confirm) and assert the returned CommandResult::Output's text contains a known substring of the deferred-CLI message (e.g., "deferred to the CLI" — confirm exact wording from the existing handlers.rs body).
  </action>
  <verify>
    <automated>cargo test -p ironhermes-core --test handlers_deferred_subverbs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `crates/ironhermes-core/src/commands/handlers.rs contains "boards",` (the new entry literal)
    - Source: `grep -c '"boards"' crates/ironhermes-core/src/commands/handlers.rs >= 1`
    - Source: `grep -c '"swarm"\|"mention"' crates/ironhermes-core/src/commands/handlers.rs == 2` (byte-stability check on existing entries)
    - Behavior: `cmd_kanban(&["boards", "list"], ...)` routes to the deferred-CLI output (asserted by test)
    - Behavior: `cmd_kanban(&["swarm"], ...)` still routes to the same deferred-CLI output (existing entry unchanged)
    - Test: `cargo test -p ironhermes-core --test handlers_deferred_subverbs` exits 0 (≥2 tests)
    - Build: `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>
    The `"boards"` entry is in the array; gateway `/kanban boards <verb>` slash dispatch routes to the deferred-CLI message verbatim; existing entries are byte-stable; the regression test locks both invariants.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway slash text → cmd_kanban router | User-typed `/kanban boards rm <slug>` could attempt destructive action via slash if a gateway dispatch arm existed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.9-08-01 | Elevation of Privilege | gateway slash escalation to destructive boards verbs | mitigate | Adding `"boards"` to DEFERRED_KANBAN_SUBVERBS routes the slash to a benign text message; NO gateway-side write path is wired in this phase. Locked by `deferred_kanban_subverbs_contains_boards` test. |
| T-36.3.7.9-08-02 | Tampering | array order regression | mitigate | Existing-entries byte-stability test asserts swarm + mention + link + unblock + dispatch all still defer; a future PR that removes an entry would fail the test |
| T-36.3.7.9-08-SC | Supply Chain | no new deps | accept | Single-string edit; zero crates added |
</threat_model>

<verification>
- `cargo build --workspace` exits 0
- `cargo test -p ironhermes-core --test handlers_deferred_subverbs` exits 0
- `cargo test -p ironhermes-core` exits 0 (zero regressions in existing core tests)
- `cargo clippy -p ironhermes-core -- -D warnings` exits 0
- grep -c '"boards",' crates/ironhermes-core/src/commands/handlers.rs >= 1
- grep -c 'DEFERRED_KANBAN_SUBVERBS' crates/ironhermes-core/src/commands/handlers.rs >= 2 (declaration + router check)
</verification>

<success_criteria>
The single-entry pattern is preserved verbatim from the 36.3.7.6/7/8 precedents; gateway slash dispatch routes correctly; the regression test locks both the new entry AND the byte-stability of existing entries.
</success_criteria>

<output>
Create `.planning/phases/36.3.7.9-multi-board-cli-boards-and-board-flag/36.3.7.9-08-SUMMARY.md` with the bilateral-tracing entry (producer = handlers.rs single-line addition; consumer = handlers_deferred_subverbs.rs ≥2 tests), the existing-entries byte-stability proof, and the cargo test exit code.
</output>
