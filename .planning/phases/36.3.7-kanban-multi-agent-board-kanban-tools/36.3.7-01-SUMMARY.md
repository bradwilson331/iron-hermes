---
phase: 36.3.7
plan: "01"
subsystem: ironhermes-kanban
status: halted-at-checkpoint
checkpoint: Task 0 — sysinfo legitimacy gate
halted_at: 2026-05-29T09:35:27Z
tags: [kanban, crate-scaffold, workspace, types, paths, events, config, cli-flag]
---

# Phase 36.3.7 Plan 01: Crate Scaffold + Types + CLI Flag Summary

**Status: HALTED — awaiting human decision at Task 0 checkpoint**

**One-liner:** Scaffold `ironhermes-kanban` crate with full public type/path/event/config surface, workspace registration, `chat -q` flag, and invariants test scaffold.

---

## Halted at Checkpoint: Task 0

**Reason:** Plan 01 has `autonomous: false` and Task 0 is `type="checkpoint:human-verify" gate="blocking-human"`.

The `<human_verify_checkpoint_protocol>` requires halting before any implementation work and obtaining explicit human approval on the sysinfo/nix/libc choice.

**Key finding discovered during pre-execution analysis:**

Both candidate packages are **already in the workspace** as pre-existing, vetted dependencies:

- `sysinfo = "0.30"` — already in `[workspace.dependencies]` (Cargo.toml line 125), used by `ironhermes-cli` (Phase 21.7 Plan 10) and `ironhermes-exec`
- `nix = { version = "0.29", features = ["process"] }` — already in `[workspace.dependencies]` (Cargo.toml line 126), used by `ironhermes-gateway` with `features = ["signal"]`

The RESEARCH.md `[ASSUMED]` tag was written before the researcher noticed these packages were already in the workspace from prior phases. No new `cargo add` install is required — `ironhermes-kanban/Cargo.toml` will reference them via `{ workspace = true }`.

**This means the supply-chain risk (T-36.3.7-01-SC) is already accepted** — the packages have been in the workspace since at least Phase 21.7. The user decision still required is which to use for the PID-liveness primitive:

- `sysinfo { workspace = true }` — already used for PID/process work in ironhermes-cli (cascade-cancel scenarios)
- `nix { workspace = true, features = ["signal"] }` — already used in ironhermes-gateway for Unix signal handling
- `libc` via `#[cfg(unix)]` — no new dep, pure unsafe

---

## Completed Tasks

None — execution halted before Task 1.

---

## Pending Tasks (awaiting resume)

- Task 1: Create ironhermes-kanban crate skeleton + workspace registration
- Task 2: Define types, paths, events, and config modules
- Task 3: Add chat -q/--query flag to Chat subcommand + invariants test scaffold

---

## Deviations from Plan

None yet — halted before implementation.

---

## Self-Check: N/A

No tasks executed yet.
