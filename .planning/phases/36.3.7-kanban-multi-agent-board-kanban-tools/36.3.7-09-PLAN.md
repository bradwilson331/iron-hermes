---
phase: 36.3.7
plan: 09
type: execute
wave: 6
depends_on: [36.3.7-02, 36.3.7-03, 36.3.7-04, 36.3.7-05, 36.3.7-06, 36.3.7-07, 36.3.7-08]
files_modified:
  - crates/ironhermes-kanban/tests/invariants_36_3_7.rs
  - crates/ironhermes-kanban/tests/end_to_end.rs
  - crates/ironhermes-kanban/tests/protocol_violation.rs
  - docs/kanban/reference.md
  - .planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/INV-36.3.7.md
autonomous: false  # ends with two checkpoint:human-verify gates (live worker spawn + live /kanban from gateway)
requirements: []  # closes the phase; full coverage for VALIDATION.md 10 critical invariants
suggested_model: opus  # final coverage decisions + end-to-end test design + INV ledger composition

must_haves:
  truths:
    - "All static-grep invariants in invariants_36_3_7.rs have NO #[ignore] attributes and pass"
    - "End-to-end test (full lifecycle via stub spawn_fn) succeeds: init -> create -> dispatcher tick -> 'worker' completes via tool layer -> final state has status=done + completed task_runs row + completed event"
    - "Protocol-violation test: simulates a worker that exited 0 without calling kanban_complete -> next dispatcher tick emits protocol_violation event AND auto-blocks the task"
    - "Idempotency_key dedup verified at the CLI surface (not just store): two `ironhermes kanban create ... --idempotency-key K` calls return the same task_id"
    - "Cross-tenant link rejection verified at the CLI surface: `ironhermes kanban link <parent> <child>` between tasks of different tenants exits non-zero"
    - "INV-36.3.7.md ledger documents all 10 critical invariants with test name + source file + a one-line justification"
    - "docs/kanban/reference.md has a v1-scope notice marking deferred features (kanban_heartbeat/link/unblock tools, multi-board, dashboard plugin, gateway notifier, swarm helper, specify/decompose, @mention parser, portable profile artifacts, external CLI lanes)"
    - "User confirmed manually: a real worker subprocess spawn end-to-end (dispatcher claims a real task and an ironhermes child process completes it)"
    - "User confirmed manually: /kanban list works from a real gateway session (TG or local CLI) mid-turn without blocking the running agent"
  artifacts:
    - path: "crates/ironhermes-kanban/tests/end_to_end.rs"
      provides: "full_lifecycle_via_tools_layer test — init -> create -> dispatcher claim -> stub spawn_fn (which calls kanban_complete via the tool layer) -> assert final state"
    - path: "crates/ironhermes-kanban/tests/protocol_violation.rs"
      provides: "Worker exits 0 with task running -> protocol_violation event + auto-block"
    - path: ".planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/INV-36.3.7.md"
      provides: "Phase INV ledger (matches Phase 22.4.2.2 / 27.1.4.x precedent)"
  key_links:
    - from: "VALIDATION.md 10 critical invariants"
      to: "Test names in invariants_36_3_7.rs / cas_concurrency.rs / dispatcher_logic.rs / tools_smoke.rs / store_smoke.rs / end_to_end.rs / protocol_violation.rs"
      via: "INV ledger mapping table"
      pattern: "INV-36.3.7-\\d+"
    - from: "Manual checkpoint"
      to: "Real worker subprocess + live /kanban"
      via: "blocking checkpoint:human-verify"
      pattern: "kanban create.*default\\|kanban dispatch\\|/kanban list"
---

<objective>
Close Phase 36.3.7 with cross-plan integration tests, the protocol-violation coverage that was deferred from plan 03, the INV ledger documenting all 10 critical invariants, doc reconciliation flagging deferred features in `docs/kanban/reference.md`, and two manual human-verify checkpoints (live worker spawn + live `/kanban` from gateway). Without this plan, the phase ships untested as an integrated whole — every prior plan tested its layer in isolation; this plan proves the layers compose correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-CONTEXT.md
@.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-VALIDATION.md
@.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-02-SUMMARY.md
@.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-03-SUMMARY.md
@.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-04-SUMMARY.md
@.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-05-SUMMARY.md
@.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-06-SUMMARY.md
@.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-07-SUMMARY.md
@.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-08-SUMMARY.md
@docs/kanban/reference.md
@crates/ironhermes-kanban/tests/invariants_36_3_7.rs
@crates/ironhermes-kanban/tests/dispatcher_logic.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end lifecycle test + protocol-violation test + invariants finalization</name>
  <files>crates/ironhermes-kanban/tests/end_to_end.rs, crates/ironhermes-kanban/tests/protocol_violation.rs, crates/ironhermes-kanban/tests/invariants_36_3_7.rs</files>
  <read_first>
    - .planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-CONTEXT.md (D-27 protocol violation rule)
    - .planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-03-SUMMARY.md (confirms which protocol-violation detection mechanism shipped: v1 heuristic OR dispatcher_state.json reconciliation)
    - .planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-VALIDATION.md (lines 71-84 — 10 critical invariants)
    - crates/ironhermes-kanban/tests/dispatcher_logic.rs (existing dispatcher tests; end_to_end extends similar patterns + adds the tool-layer roundtrip)
    - crates/ironhermes-kanban/tests/tools_smoke.rs (existing tool tests; the stub spawn_fn in end_to_end calls KanbanCompleteTool directly)
    - crates/ironhermes-kanban/tests/invariants_36_3_7.rs (verify each #[ignore] is gone)
  </read_first>
  <behavior>
    - end_to_end.rs/full_lifecycle_via_tools_layer:
      1. tempdir as IRONHERMES_HOME -> KanbanStore::open_default() succeeds; confirms 5 tables.
      2. store.create_task("test task", "default", CreateTaskOptions{tenant=Some("t-test"), workspace=Some("scratch"), ...}) -> task_id.
      3. Construct DispatcherContext::with_spawn_fn(store, config, custom_spawn) where custom_spawn is a closure that:
         a. Sets HERMES_KANBAN_TASK=task_id, HERMES_KANBAN_RUN_ID=run.id, HERMES_KANBAN_CLAIM_LOCK=run.claim_lock, HERMES_PROFILE="default" via std::env::set_var (test-scope; document race risk in test comment).
         b. Constructs KanbanCompleteTool with the same store Arc, calls .execute({"summary": "done", "metadata": {"verified": true}}).await.
         c. Returns Ok(std::process::id()) (self PID == "alive" by definition).
      4. dispatcher::run_dispatch_tick(&ctx).await.
      5. Assertions:
         - get_task(id).status == "done"
         - SELECT COUNT(*) FROM task_runs WHERE task_id=? AND outcome='completed' returns 1
         - SELECT COUNT(*) FROM task_events WHERE task_id=? AND kind='claimed' returns 1
         - SELECT COUNT(*) FROM task_events WHERE task_id=? AND kind='completed' returns 1
         - Optionally: payload of the completed event contains the first-line summary.

    - protocol_violation.rs: tests for whichever mechanism was implemented in plan 03 task 2.
      v1 HEURISTIC path: 
        - Seed running task with claim_pid = some integer (use 99999998 — definitely dead but not the same as the crash-detection sentinel 99999999 if any tests use it).
        - First tick -> detect_crashed step fires (PID dead), emits crashed event, increments consecutive_failures.
        - REVISE: protocol violation distinguished from crashed: per plan 03's v1 heuristic, the heuristic looks for "PID was previously spawned AND is now dead AND task is still 'running' AND no spawn_failed/crashed was emitted THIS tick". To set this up the test must simulate a worker that successfully spawned (PID recorded), then exited cleanly without calling kanban_complete. Use:
          - Seed task in status=running, claim_pid=99999998 (dead), with a prior spawned event already in task_events for this run.
          - First tick: detect_crashed identifies dead PID -> emits crashed event (NOT protocol_violation per v1 heuristic — crashed and protocol_violation are mutually exclusive in the v1 split).
          - The v1 heuristic per plan 03: if `child.try_wait()` recorded exit_code=Some(0) (which the dispatcher CANNOT determine cross-tick without a state file), it would fire protocol_violation; without that signal, we fall back to crashed. So the v1 path is observationally indistinguishable from crashed in unit tests UNLESS the dispatcher_state.json reconciliation was implemented.

      RECONCILIATION-FILE path (if plan 03 SUMMARY confirmed it):
        - Seed running task; write dispatcher_state.json mapping the dead PID to this task_id+run_id+spawned_at.
        - Tick: reconciliation reads the state file, sees pid 99999998 is dead, task is still running -> emits protocol_violation AND calls block_task. Assert events + status.

      FINAL DECISION: if plan 03 SUMMARY says v1 heuristic was chosen AND cannot distinguish from crashed in a pure-unit test, document the limitation in this test file (`#[test] #[ignore] fn protocol_violation_distinguished_from_crashed_requires_state_file()` with a #[doc] comment explaining), AND add a tagged manual-only invariant to the INV ledger ("INV-36.3.7-10 verified manually — requires live worker subprocess exiting normally without kanban_complete"). The manual checkpoint at the end of THIS plan will cover it.

    - invariants_36_3_7.rs final sweep:
      - Confirm zero #[ignore] attributes remain on the 5 original static-grep tests.
      - Add INVARIANT 6: `kanban_subcommand_registered_in_main` — `include_str!("../../ironhermes-cli/src/main.rs")` source contains `Commands::Kanban`. Proves plan 06 wiring cannot regress.
      - Add INVARIANT 7: `kanban_commanddef_universal` — `include_str!("../../ironhermes-core/src/commands/registry.rs")` source contains `CommandDef::new("kanban"` AND `Universal` within 200 chars of each other (search for both literals; if neither linewise check is feasible, simpler: assert the source contains the literal substring `CommandDef::new("kanban", "Manage kanban board`).
      - Add INVARIANT 8: `kanban_dispatcher_spawned_in_gateway` — `include_str!("../../ironhermes-gateway/src/runner.rs")` source contains `ironhermes_kanban::run_dispatch_loop`. Proves plan 08 wiring cannot regress.
      - Add INVARIANT 9: `chat_subcommand_has_q_flag` — `include_str!("../../ironhermes-cli/src/main.rs")` source contains `long = "query"` AND `short = 'q'`. Proves D-15 worker spawn shape preserved.
      - Add INVARIANT 10: `kanban_guidance_is_static_const` — `include_str!("../src/kanban_guidance.rs")` source contains `pub const KANBAN_GUIDANCE: &str` AND does NOT contain `format!` / `concat!` / `${`. Proves D-26 cache-stability cannot regress.
  </behavior>
  <action>
    Create `tests/end_to_end.rs` per behavior section. Use `tempfile::tempdir()` for IRONHERMES_HOME; set via std::env::set_var (with cleanup via Drop guard pattern to prevent cross-test pollution). Use `tokio::test` for async. Construct DispatcherContext via `with_spawn_fn` (added in plan 03 task 2 for testability); if missing, the executor adds it now as a minor plan 03 patch.

    Create `tests/protocol_violation.rs` per the decision branch matching plan 03 SUMMARY. Document the v1 limitation in test comments. Add at least one ACTIVE test that asserts crashed-and-protocol-violation are distinct event kinds in the KanbanEventKind enum (plan 01 task 2 created both — assert both serialize to different strings: `KanbanEventKind::Crashed.as_str() == "crashed"` and `KanbanEventKind::ProtocolViolation.as_str() == "protocol_violation"`).

    Update `tests/invariants_36_3_7.rs`: sweep for #[ignore] attrs (should be zero), add INVARIANT 6-10 per behavior section.

    Sizing: 2 new test files (~300 LOC) + 1 modified (~80 LOC of new invariants), ~20% context.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-kanban --test invariants_36_3_7 2>&1 | tail -20 ; cargo test -p ironhermes-kanban --test end_to_end 2>&1 | tail -10 ; cargo test -p ironhermes-kanban --test protocol_violation 2>&1 | tail -10 ; grep -c "#\\[ignore\\]" crates/ironhermes-kanban/tests/invariants_36_3_7.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ironhermes-kanban --test invariants_36_3_7` exits 0; ≥ 10 active tests (5 original + 5 new INV 6-10) pass.
    - `cargo test -p ironhermes-kanban --test end_to_end` exits 0; full_lifecycle test passes.
    - `cargo test -p ironhermes-kanban --test protocol_violation` exits 0 (active tests pass; manual-only test is #[ignore]'d intentionally if v1 heuristic was chosen).
    - `grep -c "#\\[ignore\\]" crates/ironhermes-kanban/tests/invariants_36_3_7.rs` returns 0 (no remaining ignores on the static-grep gates).
    - `cargo test --workspace` exits 0.
  </acceptance_criteria>
  <done>Cross-plan integration verified by end-to-end test; protocol-violation behavior covered (in tests OR explicitly documented as manual-only); all 10 invariants in invariants_36_3_7.rs active and passing.</done>
</task>

<task type="auto">
  <name>Task 2: INV-36.3.7.md ledger + docs/kanban/reference.md v1-scope reconciliation</name>
  <files>.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/INV-36.3.7.md, docs/kanban/reference.md</files>
  <read_first>
    - .planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-VALIDATION.md (10 critical invariants — exact phrasing)
    - .planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-CONTEXT.md (`<deferred>` section — list of explicitly deferred features for the v1-scope notice)
    - docs/kanban/reference.md (the entire 867-line file — but use Grep to find sections that document deferred features; only EDIT the sections that need a "(v1 NOTE: deferred to Phase 36.3.7.x)" annotation; do NOT rewrite the doc)
    - Look at any prior phase's INV ledger if one exists (likely under .planning/phases/22.4.2.2/ or 27.1.4.x/) for the canonical format
  </read_first>
  <action>
    Step A — Create the INV ledger at `.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/INV-36.3.7.md`:

    ```
    # INV-36.3.7 — Kanban Kernel Invariant Ledger

    **Phase:** 36.3.7 — Kanban / multi-agent board (kanban_* tools)
    **Created:** <date>
    **Precedent:** Phase 22.4.2.2, 27.1.4.x INV ledgers

    ## Critical invariants from VALIDATION.md

    | # | Invariant | Source plan | Test file | Test name |
    |---|-----------|-------------|-----------|-----------|
    | INV-36.3.7-01 | Atomic claim race: only one winner | 02 | cas_concurrency.rs | atomic_claim_race_one_winner |
    | INV-36.3.7-02 | expected_run_id rejection of stale writes | 02, 04 | tools_smoke.rs | kanban_complete_rejects_stale_run_id |
    | INV-36.3.7-03 | created_cards phantom-id rejection (permanent event) | 02, 04 | tools_smoke.rs | kanban_complete_rejects_phantom_created_cards |
    | INV-36.3.7-04 | Dispatcher respawn-guard (blocker_auth/recent_success/active_pr) | 03 | dispatcher_logic.rs | respawn_guard_blocker_auth, respawn_guard_recent_success, respawn_guard_active_pr |
    | INV-36.3.7-05 | Live-PID extension vs dead-PID reclaim | 03 | dispatcher_logic.rs | live_pid_extends_when_alive, dead_pid_triggers_reclaim |
    | INV-36.3.7-06 | Stranded-task diagnostic + severity escalation | 03, 06 | (manual via `ironhermes kanban diagnostics`) |
    | INV-36.3.7-07 | Env scrub: no inherited shell secrets in worker env | 03 | invariants_36_3_7.rs | worker_spawn_calls_env_clear; worker_spawn.rs unit test build_kanban_worker_env_scrubs_secrets |
    | INV-36.3.7-08 | Skills sync idempotency / preserve-user-edits | 07 | skills_sync.rs | second_run_preserves_user_edits |
    | INV-36.3.7-09 | claim_lock-gated worker writes no-op + emit claim_expired | 02 | cas_concurrency.rs | claim_lock_gates_writes |
    | INV-36.3.7-10 | Protocol violation auto-block (worker exits 0 with task running) | 03, 09 | protocol_violation.rs (active OR manual-only — see plan 03/09 SUMMARY) |

    ## Static-grep regression gates (invariants_36_3_7.rs)

    | # | Gate | Asserts |
    |---|------|---------|
    | INV-36.3.7-SG-01 | atomic_claim_uses_begin_immediate | cas.rs contains "Immediate" |
    | INV-36.3.7-SG-02 | cas_inserts_task_run_in_same_transaction | cas.rs contains "task_runs" |
    | INV-36.3.7-SG-03 | dispatcher_calls_build_kanban_worker_env | worker_spawn.rs contains "build_kanban_worker_env" |
    | INV-36.3.7-SG-04 | worker_spawn_calls_env_clear | worker_spawn.rs contains "env_clear" |
    | INV-36.3.7-SG-05 | kanban_is_in_bypass_list | running_agent.rs contains "kanban" |
    | INV-36.3.7-SG-06 | kanban_subcommand_registered_in_main | main.rs contains "Commands::Kanban" |
    | INV-36.3.7-SG-07 | kanban_commanddef_universal | registry.rs contains kanban CommandDef + Universal |
    | INV-36.3.7-SG-08 | kanban_dispatcher_spawned_in_gateway | gateway runner.rs contains "ironhermes_kanban::run_dispatch_loop" |
    | INV-36.3.7-SG-09 | chat_subcommand_has_q_flag | main.rs contains `long = "query"` AND `short = 'q'` |
    | INV-36.3.7-SG-10 | kanban_guidance_is_static_const | kanban_guidance.rs contains `pub const KANBAN_GUIDANCE` AND no format!/concat!/${ |
    ```

    Adjust the table contents to match the actual test names shipped in each plan (read each plan's SUMMARY before filling out).

    Step B — docs/kanban/reference.md v1-scope reconciliation:

    Use Grep to locate the sections that describe deferred features. For each, INSERT (do NOT remove the existing text — just annotate):

    Locations to annotate (use Edit tool, not Write — minimize doc churn):
    1. "## Two surfaces" lists 9 LLM tools — annotate that v1 ships 6 (show/list/complete/block/comment/create) and the remaining 3 (heartbeat/link/unblock) are deferred to Phase 36.3.7.1.
    2. "## Boards (multi-project)" section — annotate at the top: "v1 NOTE: only the default board is exercised; multi-board CLI (boards list/create/switch/show/rename/rm) and `--board <slug>` flag are deferred to Phase 36.3.7.3."
    3. "## Dashboard (GUI)" section — annotate at the top: "v1 NOTE: the dashboard plugin is deferred to Phase 36.3.7.4. The slash command + CLI surface in this doc work today; the dashboard SPA / REST / WS does not exist yet."
    4. "### Auto vs Manual orchestration" — annotate: "v1 NOTE: auto_decompose / triage decomposer / specifier are deferred to Phase 36.3.7.2. Triage column exists as a parking lot; transitions out are operator-only."
    5. "### Kanban Swarm topology helper" — annotate: "v1 NOTE: `kanban swarm` is deferred to Phase 36.3.7.6."
    6. "### Auto-subscribe on /kanban create (gateway only)" + "## Gateway notifications" — annotate: "v1 NOTE: the gateway notifier (notify-subscribe/notify-list/notify-unsubscribe + auto-subscribe) is deferred to Phase 36.3.7.5. The `notification_sources` config key is RESERVED so the deferred phase needs no migration."
    7. "## Collaboration patterns" table — annotate P6 (@mention) row: "(deferred to 36.3.7.7)" and P9 (Triage specifier) row: "(deferred to 36.3.7.2)".
    8. ANY section mentioning `kanban heartbeat` CLI verb or `kanban_heartbeat` tool — annotate as "(deferred — placeholder only in v1)".

    Aim for ~10-15 small Edit calls, each adding a one-line "v1 NOTE: deferred to Phase 36.3.7.x" annotation. Do NOT delete content. Future deferred-phase plans will remove these annotations.

    Sizing: 1 ledger file (~70 LOC) + ~15 small doc edits (~30 LOC of additions total), ~10% context.
  </action>
  <verify>
    <automated>ls .planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/INV-36.3.7.md ; grep -c "INV-36.3.7-" .planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/INV-36.3.7.md ; grep -c "v1 NOTE\|deferred to Phase 36.3.7" docs/kanban/reference.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/INV-36.3.7.md` succeeds.
    - `grep -c "INV-36.3.7-" .planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/INV-36.3.7.md` returns ≥ 20 (10 critical invariants + 10 static-grep gates).
    - `grep -c "v1 NOTE\|deferred to Phase 36.3.7" docs/kanban/reference.md` returns ≥ 8 (≥ 8 deferred-feature annotations).
    - The ledger contains a row for each of the 10 VALIDATION.md critical invariants with a non-empty test name (or "manual" annotation).
  </acceptance_criteria>
  <done>INV ledger composed; reference.md annotated for v1-scope; deferred features visible to readers without removing forward-looking content.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live worker subprocess spawn — end-to-end manual smoke</name>
  <read_first>
    - .planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/INV-36.3.7.md (lists which invariants are manual-only)
    - docs/kanban/reference.md ## Quick start (commands the user runs)
  </read_first>
  <what-built>
    The full kanban kernel: schema/store/dispatcher/worker-spawn/6 tools/CLI/skills/gateway-embedded-dispatch. A live worker subprocess spawn end-to-end requires:
    - A real `ironhermes` binary (just-built workspace).
    - A real worker profile (e.g., `default` — already exists for the user).
    - The gateway running (which hosts the dispatcher per D-09).
    OR
    - A one-shot dispatcher pass via `ironhermes kanban dispatch` (simpler — no gateway needed for the smoke).
  </what-built>
  <how-to-verify>
    Follow this exact sequence (substitute YOUR_PROFILE with an existing profile name):

    1. `cargo build --release --workspace` succeeds (or `--debug` — release recommended for realistic spawn timing).
    2. `IRONHERMES_HOME=$(mktemp -d) ; export IRONHERMES_HOME ; echo "Using IRONHERMES_HOME=$IRONHERMES_HOME"` — isolated home so smoke does not collide with your real `~/.ironhermes/`.
    3. `./target/debug/ironhermes kanban init` — confirm `ls $IRONHERMES_HOME/kanban.db` succeeds and `ls $IRONHERMES_HOME/skills/kanban-worker/SKILL.md` succeeds (proves plan 07 sync ran).
    4. `./target/debug/ironhermes kanban create "smoke-test task — say hello and call kanban_complete with a one-line summary" --assignee YOUR_PROFILE --json` — capture the returned task_id.
    5. `./target/debug/ironhermes kanban list` — confirm the task appears with status=ready.
    6. `./target/debug/ironhermes kanban dispatch --max 1` — one-shot dispatch pass. Watch tracing output. Expect to see `Kanban dispatch tick` span, `claimed` event, `spawned` event with a PID.
    7. `ps -p <PID>` should show a running `ironhermes --profile YOUR_PROFILE --skills kanban-worker chat -q "work kanban task <id>"` process.
    8. Wait for the worker process to exit (or `kill -TERM <PID>` if it hangs; the worker may take a few minutes if the model thinks hard).
    9. `./target/debug/ironhermes kanban show <task_id>` — confirm status=done (or status=blocked if the worker block-instead-of-complete'd — also acceptable for the smoke).
    10. `./target/debug/ironhermes kanban runs <task_id>` — confirm at least one run row with outcome='completed' (or 'blocked').
    11. `cat $IRONHERMES_HOME/logs/kanban/<task_id>.stdout.log` — confirm worker stdout was captured.

    Decide:
    - (a) Smoke passes — type "smoke OK" — phase proceeds to Task 4.
    - (b) Smoke fails at step N — type "smoke FAILED at step N: <description>" — phase WILL NOT advance; the executor must triage which plan owns the failure and likely a fix-forward sub-phase 36.3.7.0 is needed.
    - (c) Smoke partial — worker spawned but exited with status=blocked due to a missing-profile or auth issue — type "smoke PARTIAL: <description>" — phase advances IF the dispatcher behaved correctly (spawned + recorded outcome); the worker-side issue is operator-config not a kernel bug.
  </how-to-verify>
  <resume-signal>Type "smoke OK", "smoke PARTIAL: <description>", or "smoke FAILED at step N: <description>"</resume-signal>
  <acceptance_criteria>
    - User has typed one of the three approved tokens.
    - If OK or PARTIAL: phase advances to Task 4.
    - If FAILED: phase halts; gap-closure plan needed.
  </acceptance_criteria>
  <done>User has confirmed (or explicitly failed) the live end-to-end worker spawn.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live /kanban from gateway session — mid-run bypass smoke</name>
  <read_first>
    - docs/kanban/reference.md ## /kanban slash command ## Mid-run usage section
    - .planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-CONTEXT.md (D-36 mid-run bypass rule)
  </read_first>
  <what-built>
    The /kanban slash command is registered universally (plan 06 CommandDef + plan 06 is_bypass extension), so it propagates to TG/Discord/Slack/iron_hermes_ui by construction. The mid-run bypass means: even while an agent is in the middle of a turn, `/kanban list` (or any other kanban verb) should fire immediately without being queued behind the running agent.
  </what-built>
  <how-to-verify>
    Test path A — local CLI (simplest):
    1. `./target/debug/ironhermes chat` — interactive REPL.
    2. At the prompt: type a slow-thinking prompt like `Count to 10 slowly, explaining each number in detail.`
    3. WHILE THE AGENT IS THINKING, immediately type `/kanban list` and press Enter.
    4. Confirm the kanban list output appears INTERLEAVED with the agent's response (or in the queue display, BEFORE the agent finishes — the exact UX depends on how the CLI surfaces bypassed commands).
    5. Confirm the agent's turn was NOT interrupted (the slow-thinking response still completes).

    Test path B — gateway + Telegram (if configured):
    1. Start the gateway: `IRONHERMES_HOME=<your real home> ironhermes gateway start` (use real home so TG creds work).
    2. In Telegram, send a slow-thinking prompt to the bot.
    3. While the bot is "typing", send `/kanban list` from Telegram.
    4. Confirm the bot responds to `/kanban list` immediately (not queued).
    5. Confirm the original slow-thinking response still arrives.

    Test path C — iron_hermes_ui (if running):
    1. Start: `cargo run -p iron_hermes_ui` (or however the UI is invoked).
    2. Open the UI; start a slow-thinking prompt.
    3. While agent is thinking, send `/kanban list` via the UI's slash-command surface.
    4. Same expectation as path A.

    Decide:
    - (a) Bypass works — type "bypass OK" — phase complete.
    - (b) Bypass blocks the running agent or queues /kanban — type "bypass FAILED: <observed behavior>" — likely plan 06's is_bypass extension or plan 06's UI test did not actually verify the runtime path; gap-closure plan needed.
    - (c) Unable to test (no gateway/UI configured, CLI REPL doesn't support concurrent input) — type "bypass NOT TESTABLE: <reason>" — phase advances on the strength of the static-grep invariants (INV-36.3.7-SG-05 kanban_is_in_bypass_list) and plan 06's iron_hermes_ui regression test; document in SUMMARY that runtime path was not manually verified.
  </how-to-verify>
  <resume-signal>Type "bypass OK", "bypass FAILED: <description>", or "bypass NOT TESTABLE: <reason>"</resume-signal>
  <acceptance_criteria>
    - User has typed one of the three approved tokens.
    - OK or NOT TESTABLE: phase complete.
    - FAILED: phase halts; gap-closure plan needed for plan 06's bypass wiring.
  </acceptance_criteria>
  <done>User has confirmed (or explicitly failed/skipped) the mid-run /kanban bypass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual checkpoint output → phase advancement | Operator judgment trusted |
| Real worker spawn → live kanban.db writes | Same as production trust model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7-09-01 | Tampering | Tests un-ignored that don't actually assert correct behavior | mitigate | INV ledger maps each VALIDATION.md invariant to a named test; reviewer cross-checks ledger entries against test bodies |
| T-36.3.7-09-02 | Information Disclosure | Manual smoke logs leak real worker stdout content | accept | Tmp dir isolation; user runs in their own environment |
| T-36.3.7-09-03 | DoS | End-to-end test takes >60s and blocks CI | mitigate | end_to_end test uses stub spawn_fn (no real subprocess); should complete in <5s |
</threat_model>

<verification>
After this plan:
1. `cargo test --workspace` exits 0.
2. `cargo test -p ironhermes-kanban --test invariants_36_3_7` runs ≥ 10 active tests, all pass.
3. INV-36.3.7.md ledger exists with all 10 + 10 invariants mapped to tests.
4. docs/kanban/reference.md annotates ≥ 8 deferred-feature sections.
5. User has signed off on live worker spawn smoke (or explicitly flagged failure).
6. User has signed off on live /kanban bypass smoke (or explicitly flagged not-testable).
</verification>

<success_criteria>
- All 10 VALIDATION.md critical invariants mapped to passing automated tests OR explicitly documented as manual-only (with the manual checkpoint covering them).
- 10 static-grep invariants in invariants_36_3_7.rs all active and passing.
- End-to-end test exercises store + dispatcher + tool layer in one flow.
- Protocol-violation behavior covered (in tests OR manual-only ledger entry).
- INV-36.3.7.md ledger composed (matches prior-phase precedent).
- docs/kanban/reference.md v1-scope notices added without rewriting forward-looking content.
- Two manual checkpoints completed.
- `cargo test --workspace && cargo clippy --workspace -- -D warnings && cargo build --workspace` all exit 0 — phase-end gate.
</success_criteria>

<output>
Create `.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-09-SUMMARY.md` recording:
- Final automated test count and which invariants are manual-only (with checkpoint reference).
- INV-36.3.7.md location and row count.
- Number of deferred-feature annotations added to reference.md.
- Outcome of Task 3 manual smoke (smoke OK / PARTIAL / FAILED + notes).
- Outcome of Task 4 bypass smoke (OK / FAILED / NOT TESTABLE + notes).
- Any follow-up phase recommendations (e.g., 36.3.7.0 fix-forward if a smoke failed).
- Overall phase status: COMPLETE / BLOCKED / NEEDS-FOLLOWUP.
</output>
