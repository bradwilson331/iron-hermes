# Phase 36.3.7: Kanban / multi-agent board — kanban_* tools - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-29
**Phase:** 36.3.7-kanban-multi-agent-board-kanban-tools
**Areas discussed:** v1 scope cut, Multi-tenant, Workspace kinds, DB strategy, Crate home, Dispatcher tick, Worker spawn, Worker context handoff, chat -q gap, Reclaim idempotency, Concurrency, Env scrub, Worker logs, Tool surface, Tool shape, Worker skill content, (re-opened) Tool surface, (re-opened) Dispatcher home, (re-opened) Phase scope, (re-opened) Worker skills, Protocol items, Operator UX, Spawn cmd, Runtime gates, Lane plugins

---

## Discussion structure

The discussion began with the standard scope/DB/worker-spawn/tool-surface frame, but the user pasted **two large canonical reference docs mid-discussion** — the Hermes Kanban product reference (~67KB) and the Hermes Worker Lanes spec (~10KB). These superseded the original PDF design spec on multiple architectural choices and forced **re-opening four earlier locked decisions** with reference-grounded options. The audit table below reflects the full sequence including the contradicted-then-revised answers.

---

## v1 scope cut

| Option | Description | Selected |
|---|---|---|
| Full kernel (PRs 1–7) | Scaffold + single-profile path + deps + atomic claim + dispatcher + gateway + worker skill | ✓ |
| Minimal slice (PRs 1–4) | Board+claims only, dispatcher in 36.3.7.1 | |
| Kernel + orchestrator profile/skill bundle (PRs 1–7 + §6) | Full kernel + orchestrator template + 6 starter specialists | |
| Full v1 (PRs 1–8) | Kernel + portable profile artifacts | |

**User's choice:** Full kernel (PRs 1–7).
**Notes:** Defers orchestrator template profile + portable profile artifacts + dashboard to follow-up. Scope re-opened after canonical reference doc reveal — see "(re-opened) Phase scope" below.

---

## Multi-tenant

| Option | Description | Selected |
|---|---|---|
| Include in v1 — ship tenant column as PDF prescribes | Nullable column + HERMES_TENANT propagation + workspace path expansion | ✓ |
| Defer to 36.3.7.1 | Keep v1 kernel single-tenant; risk of post-hoc migrations | |
| Include schema, skip propagation | Column + filter only; no env var or workspace expansion | |

**User's choice:** Include in v1 — ship tenant column as PDF prescribes.

---

## Workspace kinds

| Option | Description | Selected |
|---|---|---|
| All three (scratch, dir, worktree) — PDF v1 | Covers Stories 1–4 day one | ✓ |
| scratch + dir only | Defer worktree (coding pipeline Story 4 deferred) | |
| scratch only | Minimum viable; probably too small to be useful | |

**User's choice:** All three (scratch, dir, worktree) — PDF v1.

---

## DB strategy

| Option | Description | Selected |
|---|---|---|
| Separate kanban.db at ~/.ironhermes/kanban.db (PDF prescription) | Two SQLite files, two WALs, clean isolation | ✓ |
| Unified inside ironhermes-state | Single state.db owns sessions+FTS5+kanban; risk of lock contention | |
| Separate file but co-located via shared SqliteOpenOptions | Same crate, two DBs — compromise | |

**User's choice:** Separate kanban.db at ~/.ironhermes/kanban.db (PDF prescription).

---

## Crate home

| Option | Description | Selected |
|---|---|---|
| New crate: ironhermes-kanban (recommended) | Mirrors ironhermes-cron / ironhermes-state separation | ✓ |
| Module inside ironhermes-state (state::kanban) | Fewer crates; mixes concerns | |
| Module inside ironhermes-cron (work-queue cousin) | Couples time-triggered cron with dependency-triggered kanban; PDF §16.3 separates them | |

**User's choice:** New crate ironhermes-kanban (recommended).

---

## Dispatcher tick (initial answer)

| Option | Description | Selected |
|---|---|---|
| Register as a built-in ironhermes-cron-runner job | Reuses cron infra | |
| Standalone `ironhermes kanban dispatch` subcommand fired by OS cron | PDF verbatim; independent of cron-runner | ✓ |
| In-process tokio task in gateway/UI | No installation overhead; tied to gateway lifecycle | |

**User's choice:** Standalone `ironhermes kanban dispatch` subcommand fired by OS cron.
**Notes:** **CONTRADICTED later** — canonical reference doc states gateway-embedded dispatcher is the default; standalone is deprecated. See "(re-opened) Dispatcher home" below.

---

## Worker spawn mechanism

| Option | Description | Selected |
|---|---|---|
| `ironhermes --profile <P> chat -q "work kanban task <id>"` subprocess (PDF verbatim) | Reuses existing CLI; profile isolation per Phase 24 | ✓ |
| In-process tokio task running AgentLoop directly | Re-introduces NanoClaw failure pattern; PDF rejects this | |
| Subprocess via new headless worker binary `ironhermes-kanban-worker` | Dedicated thin binary; smaller worker surface | |

**User's choice:** Subprocess via ironhermes CLI.

---

## Worker context handoff

| Option | Description | Selected |
|---|---|---|
| Worker reads kanban.db directly at startup (knows task_id from argv) | Self-serve; late comments visible | |
| Dispatcher prepares prompt string and passes via stdin/file path | Worker is dumb; no DB access | |
| Hybrid: dispatcher passes task_id; worker reads DB, calls hermes kanban comment to post results | Cleanest; mirrors PDF §11 "no out-of-band state" | ✓ |

**User's choice:** Hybrid (task_id-only handoff via env; worker reads kanban.db; worker writes via 6 LLM tools).
**Notes:** The "calls hermes kanban comment" phrasing in the option was tightened by the reference doc — workers call `kanban_*` tools, NOT shell out to the CLI. The hybrid model holds; the write mechanism becomes "tool calls into kanban_db" not "exec ironhermes kanban comment".

---

## chat -q gap

| Option | Description | Selected |
|---|---|---|
| I'll confirm by checking the CLI | Grep for `chat -q` / quiet-mode / non-interactive flag | ✓ |
| Assume it exists — plan as if quiet-mode is ready | Risk of mid-implementation discovery | |
| Assume gap — add `chat -q --profile` as explicit prerequisite plan | Bake gap in unconditionally | |

**User's choice:** Confirm by checking. **Result:** IronHermes has `-e/--execute` at top-level + `--profile` global, but NO `chat -q` shape. Per re-opened "Spawn cmd" question below, the canonical lanes-doc spawn shape `chat -q` is added as a new flag on the `Chat` subcommand.

---

## Reclaim idempotency

| Option | Description | Selected |
|---|---|---|
| Worker tags every comment/complete call with its claim_lock value; DB rejects writes from mismatched lock | Clean DB-layer idempotency | ✓ |
| Idempotency keys on commit — worker generates UUID per turn, complete is a no-op if seen | More moving parts; less natural for transitions | |
| Best-effort — last writer wins, log loud warnings | Simpler; rare-race tolerance | |

**User's choice:** claim_lock-asserted writes (DB rejects mismatched-lock writes).
**Notes:** Composes with `expected_run_id` from lanes-doc for defense in depth (CONTEXT.md D-41).

---

## Concurrency

| Option | Description | Selected |
|---|---|---|
| Configurable cap via `kanban.dispatcher.max_concurrent` (default 8) | Tracks running count per tick; stops claiming at cap | ✓ |
| Unbounded (PDF default) | "Parallelism limited only by host resources"; surprise-OOM risk | |
| Per-profile concurrency cap (default 4 per profile, 16 total) | Two-level bound; more complex | |

**User's choice:** Configurable cap default 8.
**Notes:** Field is `kanban.max_in_progress` (reference's name) — same semantics.

---

## Env scrub

| Option | Description | Selected |
|---|---|---|
| Reuse the existing `build_safe_env()` allowlist from ironhermes-exec (Phase 22 pattern) | Same env-strip used for code execution sandbox | ✓ |
| Pass-through (inherit all env) | Simpler; leaks parent env | |
| Profile-scoped: source ~/.ironhermes/profiles/<P>/.env explicitly | Strongest isolation; may break tools expecting inherited env | |

**User's choice:** Reuse `build_safe_env()` allowlist.

---

## Worker logs

| Option | Description | Selected |
|---|---|---|
| Capture to `~/.ironhermes/logs/kanban/<task-id>.{stdout,stderr}.log` | Per-task log files | ✓ |
| Append to existing ironhermes log file | Single stream; concurrent worker mingling | |
| Drop stdout/stderr (rely on worker tracing) | Simpler; loses early-failure visibility | |

**User's choice:** Per-task log files under `~/.ironhermes/logs/kanban/`.

---

## Tool surface (initial answer)

| Option | Description | Selected |
|---|---|---|
| CLI + skill ONLY — follow PDF §16.3 recommendation | No LLM-callable tools; workers shell out | |
| Single `kanban_tool` with verb dispatch (mirrors memory_tool/cronjob_tool pattern) | One tool, action enum | |
| Granular `kanban_create`, `kanban_comment`, etc. tools | Per-verb gating; cleanest schema | |
| Hybrid: CLI+skill default, opt-in kanban_tool exposed via toolset enable | Most flexible; largest surface to test | ✓ |

**User's choice:** Hybrid.
**Notes:** **CONTRADICTED later** by reference doc — workers use 9 granular tools via auto-enabled toolset; CLI is for humans only. See "(re-opened) Tool surface" below.

---

## Tool shape

| Option | Description | Selected |
|---|---|---|
| Single `kanban_tool` with `action` parameter | Mirrors memory_tool | ✓ |
| Granular tools (6 individual registrations) | Per-verb gating | |
| Tier-2 split: kanban_view + kanban_work | Two tools, one per access level | |

**User's choice:** Single kanban_tool with action parameter.
**Notes:** **CONTRADICTED later** — reference uses 9 granular tools. User confirmed reference is source of truth and selected "ship core 6 granular in v1" on re-opening.

---

## Worker skill content

| Option | Description | Selected |
|---|---|---|
| Behavioral guidance only | SKILL.md prescriptive text; profile needs shell or kanban_tool | |
| Behavioral + declares kanban_tool toolset dependency | Auto-enables toolset on spawn | |
| Two skills: kanban-worker + kanban-orchestrator bundled in PR7 | Ship both skills | |
| **Other (freeform)** | User pasted the full Hermes Kanban product reference (~67KB) | ✓ |

**User's choice:** Freeform — pasted the canonical Hermes Kanban product reference page.
**Notes:** This was the inflection point. The pasted reference revealed that 4 prior locked answers contradicted the canonical product surface and forced a re-opening pass. User confirmed reference is the source of truth (overrides PDF where they conflict) and that the doc lives at `docs/kanban/reference.md` + `.planning/phases/36.3.7-.../36.3.7-KANBAN-REFERENCE.md`.

---

## (re-opened) Tool surface

| Option | Description | Selected |
|---|---|---|
| Yes — ship 9 granular kanban_* tools per reference | All 9: show/list/complete/block/heartbeat/comment/create/link/unblock | |
| Yes, but defer some — ship core 6 in v1, defer kanban_heartbeat/link/unblock | Core 6: show/list/complete/block/comment/create | ✓ |
| Reverse course — keep single kanban_tool dispatch | Against reference; not recommended | |

**User's choice:** Core 6 in v1; defer heartbeat/link/unblock to follow-up.
**Notes:** User additionally cited two upstream canonical SKILL.md URLs as the authoritative skill contract:
- `https://github.com/NousResearch/hermes-agent/blob/main/skills/devops/kanban-worker/SKILL.md`
- `https://github.com/NousResearch/hermes-agent/blob/main/skills/devops/kanban-orchestrator/SKILL.md`

Both were fetched via `gh api` and saved to the phase dir; their content validated the "defer 3 tools" choice (heartbeat is advisory in the skill protocol, link is replaced by `parents=[...]` on create, unblock is operator-side only).

---

## (re-opened) Dispatcher home

| Option | Description | Selected |
|---|---|---|
| Yes — embed dispatcher in IronHermes gateway | Default per reference | ✓ |
| Yes, but also embed in iron_hermes_ui server | Either backend hosts it; DB-level lock prevents double-spawn | |
| Yes, gateway-only — UI server users must run gateway alongside | Strict reference parity | |

**User's choice:** Gateway-embedded.
**Notes:** Supersedes the initial "Standalone subcommand fired by OS cron" answer.

---

## (re-opened) Phase scope

| Option | Description | Selected |
|---|---|---|
| Kernel v1 only — board + 9 tools + gateway dispatcher + runs + heartbeat + circuit breaker + single board + 3 workspace kinds + `/kanban` + CLI verbs | Worker protocol end-to-end | ✓ |
| Kernel + triage/specify/decompose | Add triage parking + auto-decompose tick + LLM helpers | |
| Kernel + dashboard plugin | Add iron_hermes_ui visual board | |
| Full reference — ship everything | Mega-phase | |

**User's choice:** Kernel v1 only.
**Notes:** Subsequent re-opening of the tool-surface area dropped 3 of 9 tools (heartbeat/link/unblock), narrowing the kernel further to the 6-tool surface. The user-pasted Hermes Worker Lanes spec (saved as `docs/kanban/worker-lanes.md`) then revealed additional protocol primitives — KANBAN_GUIDANCE prompt injection, `created_cards` validation gate, `expected_run_id` run-fencing — which are also explicitly in v1 scope.

---

## (re-opened) Worker skills

| Option | Description | Selected |
|---|---|---|
| Yes — both bundled; ship in this phase as `skills/kanban-worker/` and `skills/kanban-orchestrator/` | Skills manifest in repo; install/update flow copies into each profile | ✓ |
| Yes bundled, but ship only kanban-worker in 36.3.7 (defer kanban-orchestrator) | Worker protocol ships; orchestrator defers | |
| Hub-installed (override the reference) | User runs `skills install kanban-worker` post-upgrade | |

**User's choice:** Both bundled in 36.3.7.
**Notes:** Skill content sourced byte-for-byte from upstream v2.0.0/v3.0.0 with `hermes -p` → `ironhermes --profile` and `~/.hermes/` → `~/.ironhermes/` substitutions.

---

## Protocol items (KANBAN_GUIDANCE + created_cards)

| Option | Description | Selected |
|---|---|---|
| Yes — both in v1: KANBAN_GUIDANCE injection + created_cards validation gate | KANBAN_GUIDANCE hooks into 10-layer prompt builder when HERMES_KANBAN_TASK env is set; created_cards rejects phantom ids with permanent event log | ✓ |
| KANBAN_GUIDANCE in v1, defer created_cards validation | Violates protocol contract | |
| Both deferred | Breaks worker protocol promises | |

**User's choice:** Both in v1.
**Notes:** User also pasted the Kanban Worker Lanes reference page mid-answer, which is the third major canonical doc — saved as `docs/kanban/worker-lanes.md`.

---

## Operator UX

(Multi-select question — user selected all 4 surfaced items.)

| Option | Description | Selected |
|---|---|---|
| `hermes kanban reclaim <id>` CLI verb | Operator recovery: abort running worker + reset to ready | ✓ |
| `hermes kanban reassign <id> <new-profile> --reclaim` CLI verb | Switch to different profile + reclaim | ✓ |
| `notification_sources` config key (gateway cross-profile allowance) | Default profile-isolation; reserved config key | ✓ |
| `review-required:` block-reason convention (soft, doc-only) | Documented convention in worker skill + reference | ✓ |

**User's choice:** All four.

---

## Spawn cmd

| Option | Description | Selected |
|---|---|---|
| Add `chat -q <prompt>` parity to IronHermes CLI — ship the canonical shape | New `-q/--query <PROMPT>` flag on `Chat` subcommand | ✓ |
| Reuse existing `-e` flag — spawn `ironhermes --profile <P> -e "work kanban task <id>"` | Zero CLI changes; document deviation | |
| Both: ship `chat -q` as new path AND keep `-e` as alias | Belt-and-braces | |

**User's choice:** Add `chat -q` parity to IronHermes CLI.

---

## Runtime gates

(Multi-select question — user selected all 4 surfaced items.)

| Option | Description | Selected |
|---|---|---|
| Live-PID detection (extend claim if PID alive, only reclaim dead PIDs) | Critical for long LLM calls; emits `claim_extended` | ✓ |
| `expected_run_id` parameter on `kanban_complete` / `kanban_block` | Run-level fencing; rejects superseded-run writes | ✓ |
| `task.max_runtime_seconds` hard wall-clock cap + SIGTERM→SIGKILL with 5s grace | Catches deadlocked workers | ✓ |
| Stranded-task diagnostic (surfaces `stranded_in_ready` after 30 min) | Catches typo'd assignees / deleted profiles | ✓ |

**User's choice:** All four.

---

## Lane plugins

| Option | Description | Selected |
|---|---|---|
| Defer entirely — 36.3.7 ships only the default Hermes profile lane | No spawn_fn pluggability; aligns with [[project_plugin_loader_rejected]] | ✓ |
| Defer external lanes, but ship spawn_fn as internal Rust trait/closure | Future hook without registration API | |
| Skip the entire pluggability discussion — hard-wire the Hermes profile spawn | Cheapest now, more invasive later | |

**User's choice:** Defer entirely.

---

## Claude's Discretion

Areas where the user delegated to Claude's judgment, captured in CONTEXT.md `<decisions>` § "Claude's Discretion":

- Schema migration mechanism (extend ironhermes-state's `SCHEMA_VERSION` pattern in a fresh module, or new approach).
- Exact event-payload JSON shapes (match upstream where the reference table specifies; choose field names for advisory events).
- Tracing instrumentation strategy (`tracing::info_span!` selection for dispatch tick, spawn, claim).
- Test layout — in-crate `#[cfg(test)]` unit tests vs. workspace integration tests under `crates/ironhermes-kanban/tests/`; precedent says integration for cross-process invariants.
- Whether to emit a Phase 36.3.7 INV ledger (recommended yes for protocol-correctness invariants).

---

## Deferred Ideas

The full deferred list lives in CONTEXT.md `<deferred>`. Headline buckets:

1. **Tool surface follow-up** (likely 36.3.7.1) — `kanban_heartbeat`, `kanban_link`, `kanban_unblock` LLM tools.
2. **Triage + decomposer flow** (likely 36.3.7.2) — auto-decompose tick, `specify`/`decompose` CLI verbs, auxiliary LLM slots, profile descriptions.
3. **Multi-board** (likely 36.3.7.3) — `boards` subcommand surface, per-board layout, `--board` flag.
4. **Dashboard plugin** (likely 36.3.7.4 — large) — React SPA, REST surface, WebSocket event tailing, drag-drop columns, recovery drawer.
5. **Gateway notifier** (likely 36.3.7.5) — `notify-subscribe`/`list`/`unsubscribe`, auto-subscribe on `/kanban create`, terminal-event delivery.
6. **Topology helpers** (likely 36.3.7.6) — `kanban swarm` builder.
7. **@mention parser (P6)** (likely 36.3.7.7) — gateway-side `@profile-name` recognition.
8. **Portable profile artifacts** (likely 36.3.7.8) — `profile export`/`install`.
9. **External CLI worker lanes** (much later) — Codex / Claude Code / OpenCode wrappers via plugin `spawn_fn`.
