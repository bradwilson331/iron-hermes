# Phase 36.3.7.4 — VERIFY

**Verified:** 2026-05-29
**Verdict:** PASS (all 7 phase-level gates)

## Outcome

Phase 36.3.7.4 closed the latent bilateral doc/impl gap flagged by Phase 36.3.7.1
Plan 01 (the `reclaim_stale_claims` "reclaimed event" doc-vs-impl mismatch).

**The producer fix (BUG-36.3.7.4-01) was a NO-OP**, exactly as PLAN.md Task 2's
conditional gate anticipated. The receiver-end test (BUG-36.3.7.4-02) ships as a
regression lock.

## Task 1 verification (read-only)

Read `crates/ironhermes-kanban/src/cas.rs::release_claim` (lines 114-145) and
confirmed:

```rust
// cas.rs:136-140
if affected == 1 {
    let payload = serde_json::json!({
        "stale_lock": claim_lock,
        "reason": reason,
    });
    tx.execute(
        "INSERT INTO task_events (task_id, run_id, kind, payload, created_at) \
         VALUES (?1, NULL, 'reclaimed', ?2, ?3)",
        params![task_id, payload.to_string(), now],
    )?;
}
```

`reclaim_stale_claims` at `dispatcher.rs:459` calls
`release_claim(..., "ttl_expired")`, which IS the producer of the `reclaimed`
event row for the TTL-expiry path. The dispatcher.rs:19 doc-comment is
factually correct ("append `reclaimed` event") — the implementation path is
just indirect through `release_claim` rather than a direct
`store.append_event` call in `reclaim_stale_claims` itself.

**Per CONTEXT alternative (a) and PLAN Task 2's conditional gate, this makes
the producer fix a no-op.** Only the receiver test ships.

## Task 3 receiver test result

`crates/ironhermes-kanban/tests/dispatcher_logic.rs:1351-1466`:

```
running 1 test
test reclaim_stale_claims_appends_reclaimed_event_to_task_events ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 16 filtered out
```

The test asserts:
1. After `run_dispatch_tick(&ctx)` with a seeded NULL-pid stale-claim task,
   `events.iter().filter(|e| e.kind == "reclaimed").count() >= 1`
2. The `reclaimed` event's payload contains `"ttl_expired"`
3. The payload carries the seeded `stale_lock` string

## Gate results

| # | Gate | Result |
|---|------|--------|
| 1 | New receiver test PASSES (`count >= 1` for `kind='reclaimed'`) | ✓ PASS — 1/1 |
| 2 | 36.3.7.0-03, 36.3.7.1-01, 36.3.7.1-02 breaker tests still PASS | ✓ PASS — full kanban suite green |
| 3 | `cargo build -p ironhermes-kanban` exit 0 | ✓ PASS |
| 4 | `cargo test --workspace --no-fail-fast` no new failures vs. develop tip | ✓ PASS (background gate completed) |
| 5 | `git diff` on dispatcher.rs limited to producer block + comment (OR empty if no-op) | ✓ PASS — empty (no-op branch taken) |
| 6 | `git diff` on dispatcher_logic.rs limited to single new `#[tokio::test]` tail-append | ✓ PASS — 116 insertions, one tail-appended `async fn` |
| 7 | SUMMARY cites both bilateral ends + Task 1 verification outcome | ✓ PASS — see SUMMARY.md |

## Sentinel checks

- `grep 'KanbanEventKind::Reclaimed' crates/ironhermes-kanban/src/dispatcher.rs`
  → 0 hits (confirms no-op: producer fix not applied because pre-existing
  `release_claim` path was identified)
- `archive_running_emits_reclaimed_event` (store_smoke.rs) — PASS
  - Pre-existing receiver lock on the same emission path via a different
    caller; corroborates the cas.rs:136-140 INSERT is exercised at runtime
- Full kanban suite: dispatcher_logic + store_smoke + tools_smoke + all
  others — exit 0, zero failures

## Commit landed

- `1161fc7d test(36.3.7.4): receiver-end test asserting Reclaimed event row append (BUG-36.3.7.4-02)`

Task 2 (producer fix) intentionally produced no commit per the conditional gate.

## Bilateral-tracing closure

Closed via Phase 36.3.7.4-SUMMARY.md.

## Forward note

Phase 36.3.7.5 (gateway notifier) can now safely depend on `task_events`
containing `reclaimed` rows for the TTL-expiry reclaim path — both the
canonical emission point (`cas::release_claim` lines 136-140) and the
end-to-end dispatcher tick path are regression-locked.
