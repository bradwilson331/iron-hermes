# Phase 36.3.7.4 — SUMMARY

**Closed:** 2026-05-29
**Verdict:** PASS — 7/7 gates green (see VERIFY.md)
**Commits:** 1 (`1161fc7d`) + this docs commit
**Producer fix:** NO-OP (pre-existing emission path confirmed)
**Receiver test:** SHIPPED (regression lock)

## What this phase did

Closed the latent bilateral doc/impl gap flagged by Phase 36.3.7.1 Plan 01:
the dispatcher.rs:19 doc-comment promised `reclaim_stale_claims` would
"reset to `ready`, append `reclaimed` event", but no direct
`store.append_event(KanbanEventKind::Reclaimed)` call exists in that function.

The Task 1 read-only verification (PLAN.md Task 2's conditional gate)
revealed the `reclaimed` event row IS already written — by `cas::release_claim`
at `crates/ironhermes-kanban/src/cas.rs:136-140` via direct SQL INSERT into
`task_events`, called from `dispatcher.rs:459` with `reason="ttl_expired"`.
The doc-comment is factually correct; the implementation path is indirect.

**Outcome:** BUG-36.3.7.4-01 (producer fix) was correctly a NO-OP.
BUG-36.3.7.4-02 (receiver test) shipped as a regression lock that holds
regardless of whether a future refactor moves the emission inline.

## Bilateral-tracing closure

| End | Path | Mechanism | Locked by |
|---|---|---|---|
| **PRODUCER (pre-existing)** | `dispatcher::reclaim_stale_claims` → `cas::release_claim` → SQL `INSERT INTO task_events (..., kind='reclaimed', ...)` | Indirect via `release_claim(..., "ttl_expired")` at dispatcher.rs:459; SQL emission at cas.rs:136-140 | Pre-existing test `archive_running_emits_reclaimed_event` (store_smoke.rs) + new dispatcher_logic test |
| **CONSUMER (this phase)** | `reclaim_stale_claims_appends_reclaimed_event_to_task_events` test | Seeds NULL-pid stale claim, runs full dispatch tick, asserts `task_events` row + payload shape | dispatcher_logic.rs:1351-1466 (committed `1161fc7d`) |
| **DOWNSTREAM (next phase)** | Phase 36.3.7.5 gateway notifier polling loop | Will subscribe to `task_events` and deliver `reclaimed` notifications to gateway chats | Out of scope here; receiver test guarantees the rows exist for the notifier to consume |

## Doc-vs-impl reconciliation

The dispatcher.rs:19 doc-comment is **NOT** wrong:
> 3. **reclaim_stale_claims** — expired claim with dead or null PID →
>    reset to `ready`, append `reclaimed` event.

The "append `reclaimed` event" promise is honored by the call chain:

```
dispatcher::reclaim_stale_claims  (dispatcher.rs:399-475)
  ↓
  store.release_claim(task_id, claim_lock, "ttl_expired")  (dispatcher.rs:459)
  ↓
  cas::release_claim transaction  (cas.rs:114-145)
  ↓
  tx.execute("INSERT INTO task_events ... kind='reclaimed' ...")  (cas.rs:136-140)
```

No source change required. The receiver test now locks this chain
regression-tight.

## Files changed

- `crates/ironhermes-kanban/tests/dispatcher_logic.rs` — +116 lines
  (one new `#[tokio::test]` tail-appended; no other diff in test file)
- `crates/ironhermes-kanban/src/dispatcher.rs` — **unchanged** (no-op branch)

## Commits

| SHA | Message |
|---|---|
| `1161fc7d` | `test(36.3.7.4): receiver-end test asserting Reclaimed event row append (BUG-36.3.7.4-02)` |
| (this) | `docs(36.3.7.4): SUMMARY + VERIFY — bilateral-tracing closure on dispatcher Reclaimed event parity` |

## Forward note

Phase 36.3.7.5 (gateway notifier) — now ready to execute. The notifier can
subscribe to `task_events` and trust that `reclaimed` rows ARE persisted for
the TTL-expiry reclaim path. Both bilateral ends are locked:

- **Producer:** `cas::release_claim` emission (covered by
  `archive_running_emits_reclaimed_event` + new dispatcher_logic test)
- **End-to-end dispatcher tick:** locked by this phase's new test

## Bilateral-tracing meta-rule reaffirmation

This phase is a textbook application of the
**bilateral-tracing meta-rule** (LEARNINGS 2026-05-29): every wire-up
has producer + consumer ends, and both must be locked by tests in the same
phase (or earlier) so the contract cannot silently regress.

The producer here was emitted by code authored before the meta-rule existed
(release_claim's INSERT). The receiver-end lock retroactively closes the
contract by adding the test that the original implementation lacked — even
though the producer is correct, the doc-comment's promise now has explicit
regression-test backing.

Phase 36.3.7.4 took the absolute minimum effort to close the gap (1 commit,
1 test, no source change) — exactly the kind of low-cost contract-lock that
keeps the bilateral-tracing discipline cheap to apply across future phases.
