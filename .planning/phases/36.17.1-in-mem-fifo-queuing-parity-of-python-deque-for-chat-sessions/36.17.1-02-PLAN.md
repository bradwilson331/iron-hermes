---
phase: 36.17.1
plan: 02
type: execute
wave: 2
depends_on: [36.17.1-01]
files_modified:
  - crates/ironhermes-gateway/src/runner.rs
  - crates/ironhermes-gateway/src/handler.rs
autonomous: true
requirements: TBD
must_haves:
  truths:
    - "GatewayRunner owns an Arc<SessionQueue> initialized in new()"
    - "GatewayRunner exposes try_enqueue/dequeue/queue_len/clear_queue/retain_queue public methods that delegate to SessionQueue"
    - "GatewayMessageHandler holds an Arc<SessionQueue> threaded via build_gateway_handler — NOT an Arc<GatewayRunner> (no circular reference)"
    - "When agent_running is true at handler.rs:840-854 the message is enqueued instead of rejected; on CapacityReached the D-13 Telegram UX fires (❌ reaction + chat reply)"
    - "After run_agent completes, the per-chat worker drains the queue by popping and re-invoking run_agent in arrival order until pop returns None"
    - "The drain loop is exposed as `pub(crate) async fn drain_pending(&self, key: &SessionKey, handler: &GatewayMessageHandler, adapter: Arc<dyn PlatformAdapter>, cancel: CancellationToken) -> Result<()>` on GatewayRunner — unconditionally, so integration tests can exercise the actual worker drain behavior (not a substitute pop-sequence unit test)"
    - "D-01: Delivers part (b) of D-01 scope — GatewayRunner integration (field + 5 public API methods + busy/idle dispatch branch + post-turn drain loop)"
    - "D-13: Telegram cap-hit UX (❌ reaction + chat reply) wired into the busy-branch CapacityReached arm"
    - "D-14: SessionQueue lives as Arc<SessionQueue> field on GatewayRunner (private)"
    - "D-15: Thin public API on GatewayRunner — try_enqueue/dequeue/queue_len/clear_queue/retain_queue; raw SessionQueue type not exported"
    - "D-17: Runner methods delegating to SessionQueue remain synchronous"
  artifacts:
    - path: "crates/ironhermes-gateway/src/runner.rs"
      provides: "session_queue field + 5 public API methods + handler threading + pub(crate) drain_pending helper"
      contains: "session_queue: Arc<SessionQueue>"
    - path: "crates/ironhermes-gateway/src/handler.rs"
      provides: "session_queue field on GatewayMessageHandler + set_session_queue + busy-branch enqueue + cap-hit UX"
      contains: "session_queue: Arc<SessionQueue>"
  key_links:
    - from: "GatewayRunner::new"
      to: "SessionQueue::new"
      via: "Arc::new(SessionQueue::new()) initialization"
      pattern: "Arc::new\\(SessionQueue::new\\(\\)\\)"
    - from: "GatewayRunner::build_gateway_handler"
      to: "GatewayMessageHandler::set_session_queue"
      via: "handler.set_session_queue(self.session_queue.clone())"
      pattern: "set_session_queue"
    - from: "handler.rs handle_with_multimodal (line ~840-854)"
      to: "self.session_queue.try_push"
      via: "agent_running.load(SeqCst) == true branch"
      pattern: "self\\.session_queue\\.try_push"
    - from: "per-chat worker post-turn block"
      to: "GatewayRunner::drain_pending"
      via: "runner.drain_pending(&session_key, &handler, adapter.clone(), cancel.clone()).await"
      pattern: "drain_pending"
---

<objective>
Thread `Arc<SessionQueue>` from Plan 01 onto `GatewayRunner` and `GatewayMessageHandler`. Replace the busy-agent reject branch in `handle_with_multimodal` with a queue-or-cap-hit flow. Add a post-turn drain loop in the per-chat worker that dequeues and re-invokes `run_agent` until the queue is empty for that session. The drain loop is exposed as `pub(crate) async fn drain_pending(...)` on `GatewayRunner` unconditionally — it is a phase deliverable per D-01(b), not best-effort.

Purpose: Wire the data structure into the live request path so messages arriving during an active turn are queued in arrival order and replayed one full turn per item (no merging, per D-01).
Output: Modified `runner.rs` (field + 5 public methods + drain loop + pub(crate) drain_pending) and `handler.rs` (field + setter + busy branch + cap-hit UX).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-CONTEXT.md
@.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-RESEARCH.md
@.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-PATTERNS.md
@.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-01-PLAN.md
@crates/ironhermes-gateway/src/session_queue.rs
@crates/ironhermes-gateway/src/runner.rs
@crates/ironhermes-gateway/src/handler.rs
@crates/ironhermes-gateway/src/session.rs
@crates/ironhermes-gateway/src/adapter.rs

<interfaces>
<!-- Plan-01 outputs the executor will use directly: -->

From crates/ironhermes-gateway/src/session_queue.rs (Plan 01):
- `pub struct SessionQueue { ... }`
- `pub fn new() -> Self`
- `pub fn try_push(&self, key: &SessionKey, event: MessageEvent) -> Result<(), QueueError>`
- `pub fn pop(&self, key: &SessionKey) -> Option<MessageEvent>`
- `pub fn len(&self, key: &SessionKey) -> usize`
- `pub fn clear(&self, key: &SessionKey)`
- `pub fn retain<F: Fn(&MessageEvent) -> bool>(&self, key: &SessionKey, predicate: F)`
- `pub const MAX_QUEUE_DEPTH: usize = 128;`
- `pub const WARN_QUEUE_DEPTH: usize = 96;`
- `pub enum QueueError { CapacityReached { session_key: String, max: usize } }`

From crates/ironhermes-gateway/src/runner.rs:
- `GatewayRunner::new(...)` body at lines 109-130 — Self {} struct literal; this is where the new field initializes
- `build_gateway_handler(&self)` at lines 292-395 — where Arc fields are cloned and threaded to handler; the new `handler.set_session_queue(self.session_queue.clone())` call lands here
- `pub async fn start(&self)` at line 398 — takes `&self`; the new public API methods follow the same `&self` signature
- Per-chat worker dispatch loop at lines 734-772 — this is where the post-turn drain call (`runner.drain_pending(...).await`) must be added (per RESEARCH §"Open Questions (RESOLVED)" Q2: dequeue at the worker, not inside run_agent)

From crates/ironhermes-gateway/src/handler.rs:
- `GatewayMessageHandler::new(config, resolver, session_store, tool_registry)` constructor signature
- Existing busy-guard branch at lines 840-854 (the `agent_running.load(SeqCst)` block — exact injection point)
- `SessionKey::new(event.platform.clone(), &event.chat_id).with_user(&event.sender_id)` construction pattern at lines 843-844
- `with_rate_limit_retry(|| adapter.send_message(...))` adapter call wrapper at lines 847-850
- `run_agent(&self, event, adapter, cancel, processed)` signature — the post-turn drain re-entry target
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add session_queue field + 5 public API methods to GatewayRunner</name>
  <files>crates/ironhermes-gateway/src/runner.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/runner.rs lines 1-150 (imports + GatewayRunner struct + new() body — the field declaration and initialization both land here)
    - crates/ironhermes-gateway/src/runner.rs lines 292-395 (build_gateway_handler — Arc threading pattern for handler construction)
    - crates/ironhermes-gateway/src/session_queue.rs (Plan 01 — types and public surface)
    - .planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-PATTERNS.md §"runner.rs (modified — additive field + public API methods + is_draining)"
  </read_first>
  <behavior>
    - GatewayRunner compiles with the new field and 5 public methods
    - Each public method delegates to the corresponding SessionQueue method with the right signature and Ordering::SeqCst is NOT touched here (drain-mode flag lands in Plan 04)
    - cargo check -p ironhermes-gateway green; no clippy warnings
    - Existing Phase 36 gateway tests still pass (no regression in handler/session behavior)
  </behavior>
  <action>
    1. In `crates/ironhermes-gateway/src/runner.rs`, add the import `use crate::session_queue::{SessionQueue, QueueError};` near the existing `use crate::session::SessionKey;` import block.
    2. Add a field to the `GatewayRunner` struct definition: `session_queue: Arc<SessionQueue>,` immediately after the existing `session_store: Arc<RwLock<SessionStore>>,` field. Doc-comment: `/// Phase 36.17.1: per-session FIFO queue (D-06, D-14). Always initialized.`.
    3. In `GatewayRunner::new(...)` Self {} struct literal at lines 109-130, add the initializer line: `session_queue: Arc::new(SessionQueue::new()),`. Place adjacent to `session_store` initialization for readability.
    4. Add 5 new public methods on `impl GatewayRunner` near where other delegation helpers live (search for a `pub fn` that returns `&self.session_store` or similar to find the right neighborhood; if none, place immediately after `pub fn new(...)`):
       - `pub fn try_enqueue(&self, key: &SessionKey, event: MessageEvent) -> Result<(), QueueError> { self.session_queue.try_push(key, event) }`
       - `pub fn dequeue(&self, key: &SessionKey) -> Option<MessageEvent> { self.session_queue.pop(key) }`
       - `pub fn queue_len(&self, key: &SessionKey) -> usize { self.session_queue.len(key) }`
       - `pub fn clear_queue(&self, key: &SessionKey) { self.session_queue.clear(key) }`
       - `pub fn retain_queue<F: Fn(&MessageEvent) -> bool>(&self, key: &SessionKey, predicate: F) { self.session_queue.retain(key, predicate) }`
       Each method gets a Rustdoc line referencing D-15 ("Adapters access the queue via thin public API on GatewayRunner") and the underlying SessionQueue method.
    5. Add a crate-private accessor for internal threading: `pub(crate) fn session_queue(&self) -> Arc<SessionQueue> { self.session_queue.clone() }`. Plan-02 Task 2 uses this to thread into the handler; Plan 04 will reuse the same accessor for drain-mode.
    6. Verify imports: `Arc` should already be in scope from `use std::sync::{Arc, Mutex};` at runner.rs:14. `MessageEvent` should already be in scope; if not, add `use ironhermes_core::MessageEvent;`.

    Constraints: Do NOT add `is_draining: Arc<AtomicBool>` yet — that's Plan 04. Do NOT modify the shutdown sequence (lines 870-943) yet. Do NOT delete the existing `cmd_queue` stub — Plan 03 owns that file. Keep this task purely additive.
  </action>
  <verify>
    <automated>cargo check -p ironhermes-gateway 2>&1 | tail -10 ; cargo test -p ironhermes-gateway --lib -- 2>&1 | tail -5 ; cargo clippy -p ironhermes-gateway --lib -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'session_queue: Arc<SessionQueue>' crates/ironhermes-gateway/src/runner.rs` >= 1 (field declaration)
    - `grep -c 'session_queue: Arc::new(SessionQueue::new())' crates/ironhermes-gateway/src/runner.rs` == 1 (initializer)
    - `grep -c 'pub fn try_enqueue' crates/ironhermes-gateway/src/runner.rs` == 1
    - `grep -c 'pub fn dequeue' crates/ironhermes-gateway/src/runner.rs` == 1
    - `grep -c 'pub fn queue_len' crates/ironhermes-gateway/src/runner.rs` == 1
    - `grep -c 'pub fn clear_queue' crates/ironhermes-gateway/src/runner.rs` == 1
    - `grep -c 'pub fn retain_queue' crates/ironhermes-gateway/src/runner.rs` == 1
    - `grep -c 'pub(crate) fn session_queue' crates/ironhermes-gateway/src/runner.rs` == 1
    - `cargo check -p ironhermes-gateway` exits 0
    - `cargo test -p ironhermes-gateway --lib` exits 0 (no regressions in Phase 36 / 36.1 suites)
    - `cargo clippy -p ironhermes-gateway --lib -- -D warnings` exits 0
  </acceptance_criteria>
  <done>GatewayRunner owns SessionQueue; 5 public delegation methods + 1 crate-private accessor in place; no regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread Arc<SessionQueue> into GatewayMessageHandler; replace busy-reject with try_enqueue + D-13 cap-hit UX</name>
  <files>crates/ironhermes-gateway/src/handler.rs, crates/ironhermes-gateway/src/runner.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/handler.rs lines 1-50 (imports + GatewayMessageHandler struct declaration)
    - crates/ironhermes-gateway/src/handler.rs lines 95-170 (constructor + setter pattern for optional shared state, e.g. set_memory_manager, set_process_registry)
    - crates/ironhermes-gateway/src/handler.rs lines 840-854 (the exact busy-guard branch being replaced — currently calls `with_rate_limit_retry(|| adapter.send_message(..., AGENT_RUNNING_REJECT_MSG, ...))` then `return Ok(())`)
    - crates/ironhermes-gateway/src/runner.rs lines 292-395 (build_gateway_handler — the threading site for `handler.set_session_queue(self.session_queue.clone())`)
    - crates/ironhermes-gateway/src/session_queue.rs (Plan 01 — try_push signature + QueueError variants)
    - crates/ironhermes-gateway/src/user_queue.rs lines 66-75 (add_reaction call pattern — for cap-hit ❌ reaction reference)
    - .planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-PATTERNS.md §"telegram.rs (modified — cap-hit UX on Err(QueueError::CapacityReached))"
    - .planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-RESEARCH.md §"Busy/Idle Decision Point" + §"Common Pitfalls" Pitfall 2 (guard across await)
  </read_first>
  <behavior>
    - When agent_running.load(SeqCst) == true at handle_with_multimodal: the event is cloned and passed to self.session_queue.try_push(&session_key, event.clone()). The previous reject-with-AGENT_RUNNING_REJECT_MSG message is no longer sent.
    - On Ok(()): a tracing::debug! line records the enqueue; no chat reply (per D-13 — free-text enqueues are silent; UserQueueManager's 👁 transport-layer reaction stays as the visible signal).
    - On Err(QueueError::CapacityReached { .. }): adapter.add_reaction(&event.chat_id, &event.message_id, "❌").await fires, then with_rate_limit_retry(|| adapter.send_message(&event.chat_id, "⏳ Queue is full (128 messages). Wait for the agent to drain before sending more.", None)).await fires. A tracing::warn! line records the drop. Function then returns Ok(()).
    - GatewayMessageHandler.session_queue is Option<Arc<SessionQueue>> initialized None; set via set_session_queue called from build_gateway_handler. If unset (None) at the busy branch, the handler falls back to the previous reject-with-AGENT_RUNNING_REJECT_MSG behavior (covers non-runner-built test handlers and preserves backward-compat for existing tests).
    - Existing Phase 36/36.1 GW-05 reject tests still pass when session_queue is None (fallback path).
    - New integration test demonstrates enqueue-when-busy (depth grows on second message during agent run).
  </behavior>
  <action>
    PART A — handler.rs additive plumbing:
    1. Add `use crate::session_queue::{SessionQueue, QueueError};` near the existing `use crate::session::SessionKey;` import.
    2. Add a field to `GatewayMessageHandler`: `session_queue: Option<Arc<SessionQueue>>` (Option preserves backward-compat for handlers built outside `build_gateway_handler`).
    3. Initialize the field to `None` in `GatewayMessageHandler::new(...)`.
    4. Add `pub fn set_session_queue(&mut self, queue: Arc<SessionQueue>) { self.session_queue = Some(queue); }` immediately after `set_memory_manager` or the equivalent setter.

    PART B — handler.rs busy-branch replacement (lines 840-854):
    5. In `handle_with_multimodal`, find the existing block where `agent_running.load(Ordering::SeqCst)` returns true. The current body looks like `with_rate_limit_retry(|| adapter.send_message(&event.chat_id, AGENT_RUNNING_REJECT_MSG, None)).await?; return Ok(());`. Replace ONLY this rejection body with:
       - If `self.session_queue.is_none()`: keep the existing AGENT_RUNNING_REJECT_MSG path (backward-compat for tests).
       - If `self.session_queue.is_some()`: call `match self.session_queue.as_ref().unwrap().try_push(&session_key, event.clone()) { Ok(()) => { tracing::debug!(session = %session_key.to_string_key(), depth = self.session_queue.as_ref().unwrap().len(&session_key), "SessionQueue: enqueued event while agent busy (Phase 36.17.1)"); } Err(QueueError::CapacityReached { .. }) => { /* D-13 UX */ } }`
       - Inside the `CapacityReached` arm: drop any active MutexGuard reference (we are not holding one — try_push is sync and returns before this point), then `adapter.add_reaction(&event.chat_id, &event.message_id, "❌").await.ok();` (use `.ok()` so a reaction failure does not poison the chat reply), then `with_rate_limit_retry(|| adapter.send_message(&event.chat_id, "⏳ Queue is full (128 messages). Wait for the agent to drain before sending more.", None)).await?;`, then `tracing::warn!(session = %session_key.to_string_key(), "SessionQueue: capacity reached, message dropped (Phase 36.17.1)");`.
       - After the match, `return Ok(());` regardless of branch.
    6. CRITICAL: ensure `event.clone()` is used at the try_push site so the original `event` is still available if the cap-hit path needs `event.chat_id` and `event.message_id`. Alternative: extract those fields into locals BEFORE the try_push call.

    PART C — runner.rs threading site (single line):
    7. In `crates/ironhermes-gateway/src/runner.rs`, inside `build_gateway_handler` (between lines 292-395, after the base `handler = GatewayMessageHandler::new(...)` and after existing optional setters like `set_memory_manager`), add: `handler.set_session_queue(self.session_queue.clone());`. Phase 36.17.1 comment.

    Constraints: NEVER hold a MutexGuard across `.await` — `session_queue.try_push` is sync, completes and drops the guard before any adapter call (Pitfall 2). Do NOT touch the post-turn drain loop in runner.rs yet (Task 3 owns that). Do NOT change `AGENT_RUNNING_REJECT_MSG` or the slash-command path (Plan 03 owns the slash path). Use `Ordering::SeqCst` for consistency with Phase 36's existing AtomicBool reads (handler.rs:844-845).
  </action>
  <verify>
    <automated>cargo build -p ironhermes-gateway 2>&1 | tail -10 ; cargo test -p ironhermes-gateway --lib -- 2>&1 | tail -10 ; cargo clippy -p ironhermes-gateway -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'session_queue: Option<Arc<SessionQueue>>' crates/ironhermes-gateway/src/handler.rs` == 1 (field decl)
    - `grep -c 'pub fn set_session_queue' crates/ironhermes-gateway/src/handler.rs` == 1
    - `grep -c 'handler.set_session_queue(self.session_queue.clone())' crates/ironhermes-gateway/src/runner.rs` == 1
    - `grep -c 'self.session_queue.as_ref().unwrap().try_push' crates/ironhermes-gateway/src/handler.rs` >= 1 (busy-branch enqueue OR equivalent if-let-Some form)
    - `grep -c '⏳ Queue is full (128 messages)' crates/ironhermes-gateway/src/handler.rs` == 1 (D-13 chat reply literal)
    - `grep -c 'add_reaction(&event.chat_id, &event.message_id, "❌")' crates/ironhermes-gateway/src/handler.rs` == 1 (D-13 reaction)
    - `grep -c 'SessionQueue: capacity reached' crates/ironhermes-gateway/src/handler.rs` == 1 (cap-hit warn line)
    - `grep -c 'tokio::sync::Mutex' crates/ironhermes-gateway/src/handler.rs` does not increase from baseline (no async-mutex leak from this plan)
    - `cargo build -p ironhermes-gateway` exits 0
    - `cargo test -p ironhermes-gateway --lib` exits 0 (Phase 36/36.1 reject-message tests still pass via Option=None fallback)
    - `cargo clippy -p ironhermes-gateway -- -D warnings` exits 0
  </acceptance_criteria>
  <done>Handler holds Arc<SessionQueue>, busy branch enqueues instead of rejects, cap-hit UX implemented, runner threads the queue. Existing reject tests still pass via the None-fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Post-turn drain loop in per-chat worker + busy-enqueue integration test</name>
  <files>crates/ironhermes-gateway/src/runner.rs, crates/ironhermes-gateway/tests/session_queue_integration.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/runner.rs lines 650-780 (the per-chat worker dispatch — where messages are routed from UserQueueManager to GatewayMessageHandler.handle_with_multimodal; the drain loop sits in this region)
    - crates/ironhermes-gateway/src/handler.rs run_agent signature and call sites (the drain loop calls run_agent directly per RESEARCH Open Question 2 — bypasses handle_with_multimodal so the busy guard is not re-evaluated on dequeued events)
    - .planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-RESEARCH.md §"Common Pitfalls" Pitfall 4 (re-entering run_agent — RAII guard re-sets AtomicBool inside run_agent so re-entry is safe)
    - .planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-VALIDATION.md (test infrastructure + sampling)
    - Existing Phase 36 integration tests under `crates/ironhermes-gateway/tests/` for the RecordingPlatformAdapter pattern
  </read_first>
  <behavior>
    - The drain loop is implemented as `pub(crate) async fn drain_pending(&self, key: &SessionKey, handler: &GatewayMessageHandler, adapter: Arc<dyn PlatformAdapter>, cancel: CancellationToken) -> Result<()>` on `GatewayRunner`. The function pops events from `self.session_queue` in FIFO order and calls `handler.run_agent(&event, adapter.clone(), cancel.clone(), no_attachments).await` for each, until pop returns None.
    - The per-chat worker post-turn block calls `self.drain_pending(&session_key, &handler, adapter.clone(), cancel.clone()).await` after each `handle_with_multimodal` returns. The worker captures `Arc<GatewayRunner>` (or `Arc<SessionQueue>` + a closure-capturable handler reference — see Action step 3) so it can invoke the drainer.
    - The RAII RunningAgentGuard inside run_agent sets AtomicBool true on entry and drops it on exit; the drain loop sees AtomicBool == false between iterations so no double-guard issue.
    - Integration test `test_busy_agent_enqueues_event`: set up a GatewayMessageHandler with a SessionQueue and set agent_running=true for a session_key; dispatch a free-text MessageEvent through handle_with_multimodal; assert queue_len == 1 and RecordingPlatformAdapter recorded zero send_message calls (silent enqueue per D-13).
    - Integration test `test_cap_hit_emits_reaction`: pre-fill the SessionQueue to 128 for a session_key, set agent_running=true, dispatch a 129th event through handle_with_multimodal; assert add_reaction("❌") was recorded with the 129th event's message_id and send_message recorded "Queue is full (128 messages)"; assert queue_len == 128 (cap held).
    - Integration test `test_drain_after_turn`: exercise `GatewayRunner::drain_pending` directly. Pre-load the SessionQueue with 3 events {"A", "B", "C"} for a session_key; construct a `GatewayMessageHandler` with a stub run_agent (or use a `RecordingPlatformAdapter` + an instrumented handler that records each `run_agent` invocation order); call `runner.drain_pending(&session_key, &handler, adapter.clone(), cancel.clone()).await?`; assert handler observed exactly 3 run_agent invocations in arrival order ["A", "B", "C"] and `queue_len == 0` afterwards. This test exercises the real drain loop, not a substitute pop-sequence unit test.
  </behavior>
  <action>
    PART A — runner.rs drain loop and `pub(crate) drain_pending` helper:
    1. Locate the per-chat worker spawn in `start()` or the dispatch loop near lines 734-772 where `GatewayMessageHandler.handle_with_multimodal(...)` (or `run_agent`) is invoked. The worker pattern is: receive an event from the per-chat mpsc::Receiver, call the handler, repeat.
    2. Add a new method on `impl GatewayRunner` (place adjacent to the other queue accessors from Task 1):
       ```rust
       /// Phase 36.17.1: post-turn FIFO drain (D-01(b)).
       /// Pops events from the session queue and re-invokes run_agent in arrival order
       /// until the queue is empty. Called by the per-chat worker after each handler turn.
       /// Bypasses handle_with_multimodal's busy-guard per RESEARCH Pitfall 4.
       pub(crate) async fn drain_pending(
           &self,
           key: &SessionKey,
           handler: &GatewayMessageHandler,
           adapter: Arc<dyn PlatformAdapter>,
           cancel: CancellationToken,
       ) -> anyhow::Result<()> {
           while let Some(next_event) = self.session_queue.pop(key) {
               tracing::debug!(
                   session = %key.to_string_key(),
                   remaining = self.session_queue.len(key),
                   "SessionQueue: draining next queued event (Phase 36.17.1)"
               );
               // Empty attachments slice matches handler.run_agent expectation for queue replay.
               handler.run_agent(&next_event, adapter.clone(), cancel.clone(), &[]).await?;
           }
           Ok(())
       }
       ```
       Adjust the exact signature (return type, `processed` arg, adapter Arc'd vs &) to match `run_agent`'s real signature read from handler.rs. `drain_pending` MUST be exposed as `pub(crate)` so the integration test in PART B can invoke it directly.
    3. In the per-chat worker block (the region near runner.rs:734-772 where `handle_with_multimodal` is called), after the handler call returns, add the drain call: `self.drain_pending(&session_key, &handler, adapter.clone(), cancel.clone()).await.map_err(|e| tracing::error!(?e, "drain_pending failed"))?;` (or equivalent — match the worker's existing error-handling style). Capture `Arc<GatewayRunner>` into the spawn closure if the worker does not already hold `&self`; the existing pattern for `Arc<RwLock<SessionStore>>` is the template.
    4. The drain helper must NOT re-enter `handle_with_multimodal` — it calls `run_agent` directly (Pitfall 4). The RAII `RunningAgentGuard` inside `run_agent` sets/clears the AtomicBool around each iteration, so any concurrent enqueue arriving during a drained turn lands in the same session_queue and is picked up on the next pop.

    PART B — integration tests in NEW file `crates/ironhermes-gateway/tests/session_queue_integration.rs`:
    5. Create the test file. Module structure: `use ironhermes_gateway::*;`. Build a `RecordingPlatformAdapter` (or reuse the one from existing Phase 36 GW-05 tests at `crates/ironhermes-gateway/tests/running_agent_guard_tests.rs` — extract or duplicate based on visibility). The recording adapter captures `send_message` and `add_reaction` calls (with their args) in a `Mutex<Vec<RecordedCall>>`. RecordingPlatformAdapter must capture `message_id` on `add_reaction` so the cap-hit test can verify the reaction targets the dropped 129th message.
    6. Write `test_busy_agent_enqueues_event`:
       - Construct a `GatewayMessageHandler` with `set_session_queue(Arc::new(SessionQueue::new()))` and set `agent_running` AtomicBool to true for the target session_key.
       - Send an event through `handle_with_multimodal`; assert `queue_len(&session_key) == 1` afterwards.
       - Assert RecordingPlatformAdapter recorded ZERO send_message calls from the busy-branch (free-text enqueue is silent per D-13).
    7. Write `test_cap_hit_emits_reaction`:
       - Same setup; pre-fill the queue to 128 via direct `session_queue.try_push` calls.
       - Send the 129th event through `handle_with_multimodal`. Assert:
         - RecordingPlatformAdapter recorded exactly one `add_reaction` call with emoji "❌" matching the event's chat_id and message_id.
         - RecordingPlatformAdapter recorded exactly one `send_message` call whose content contains the literal string "Queue is full (128 messages)".
         - `session_queue.len(&session_key) == 128` after the 129th push (i.e. capacity not exceeded; new event dropped — T-36.17.1-01 evidence).
    8. Write `test_drain_after_turn` (REQUIRED — this is the post-turn drain deliverable per D-01(b); a substitute pop-sequence-only unit test is NOT acceptable):
       - Build an `Arc<GatewayRunner>` (or the minimal subset needed to call `drain_pending`) with a `SessionQueue` and a `GatewayMessageHandler` whose `run_agent` is instrumented to record (in a `Mutex<Vec<String>>`) the `content` of each `MessageEvent` it receives. The instrumentation may use a stub `run_agent` shim wrapper or a dedicated test-only handler subtype — pick the simplest path that exercises the real `drain_pending` code.
       - Pre-load the SessionQueue with 3 events for the target session_key, with content fields "A", "B", "C" in arrival order.
       - Assert `agent_running == false` before the call (drain runs with the busy flag cleared, per RESEARCH §"Common Pitfalls" Pitfall 4).
       - Invoke `runner.drain_pending(&session_key, &handler, adapter.clone(), cancel.clone()).await?` directly. The test MUST call the real `drain_pending` — not a hand-rolled pop loop in the test body.
       - Assert: the recorded run_agent invocation list equals `["A", "B", "C"]` in that exact order; `queue_len(&session_key) == 0`; no enqueue-during-drain race occurred (depth went 3 -> 2 -> 1 -> 0).
  </action>
  <verify>
    <automated>cargo test -p ironhermes-gateway --test session_queue_integration 2>&1 | tail -15 ; cargo test -p ironhermes-gateway 2>&1 | tail -5 ; cargo clippy -p ironhermes-gateway --tests -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `[ -f crates/ironhermes-gateway/tests/session_queue_integration.rs ]` returns 0 (file exists)
    - `grep -c 'pub(crate) async fn drain_pending' crates/ironhermes-gateway/src/runner.rs` == 1 (drain helper exposed unconditionally — phase deliverable per D-01(b))
    - `grep -c 'self.drain_pending(' crates/ironhermes-gateway/src/runner.rs` >= 1 (per-chat worker calls the drain helper after each turn)
    - `grep -c 'session_queue.pop(' crates/ironhermes-gateway/src/runner.rs` >= 1 OR `grep -c 'self.session_queue.pop' crates/ironhermes-gateway/src/runner.rs` >= 1 (drain loop pop call site)
    - `grep -c 'SessionQueue: draining next queued event' crates/ironhermes-gateway/src/runner.rs` == 1
    - `cargo test -p ironhermes-gateway --test session_queue_integration test_busy_agent_enqueues_event` passes
    - `cargo test -p ironhermes-gateway --test session_queue_integration test_cap_hit_emits_reaction` passes
    - `cargo test -p ironhermes-gateway --test session_queue_integration test_drain_after_turn` passes (the test must invoke `runner.drain_pending(...)` directly — a substitute pop-sequence-only test is not acceptable)
    - `grep -c 'runner.drain_pending(' crates/ironhermes-gateway/tests/session_queue_integration.rs` >= 1 (the drain test exercises the real helper, not an inlined pop loop)
    - `cargo test -p ironhermes-gateway` exits 0 (full crate test suite green — no regressions)
    - `cargo clippy -p ironhermes-gateway --tests -- -D warnings` exits 0
  </acceptance_criteria>
  <done>Post-turn drain loop exposed as `pub(crate) async fn drain_pending` on GatewayRunner; per-chat worker calls it after each turn; busy-enqueue, cap-hit, and drain-after-turn integration tests green; full crate test suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram long-poll → handler.rs:handle_with_multimodal | Untrusted user content + Telegram message_id reach the per-session enqueue site. |
| Per-chat worker → run_agent re-entry | Dequeued events re-invoke run_agent without passing through the busy-guard. RAII guard inside run_agent must hold for the duration of each iteration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.17.1-01 | Denial of Service | handler.rs busy branch try_push | mitigate | Plan 01 enforces MAX_QUEUE_DEPTH=128 at the SessionQueue layer; Plan 02 calls try_push exactly once per busy-arrival and respects the Err return without retry. Integration test `test_cap_hit_emits_reaction` verifies the 129th push does not exceed depth=128. |
| T-36.17.1-02 | Information Disclosure | session_key construction | mitigate | All call sites construct `SessionKey::new(event.platform.clone(), &event.chat_id).with_user(&event.sender_id)` — same pattern as handler.rs:843-844, covering platform+chat_id+user_id triple (Pitfall 7). No cross-session bleed. |
| T-36.17.1-03 | Tampering (race) | drain loop after run_agent | mitigate | Drain loop calls run_agent directly (bypassing handle_with_multimodal) per Pitfall 4. The RAII RunningAgentGuard inside run_agent re-sets AtomicBool true for the duration of each drained turn, so any push arriving mid-drained-turn enqueues to the same key and is picked up on the next pop iteration. Order is preserved by VecDeque FIFO semantics from Plan 01. |
| T-36.17.1-04 | DoS via /queue flood | (deferred to Plan 03) | accept | At ASVS L1, single-user /queue flooding their own bucket to 128 is documented-but-accepted. Telegram protocol layer rate-limits message arrival. Higher ASVS would add per-user rate limiting. No mitigation in this plan. |
</threat_model>

<verification>
- `cargo build -p ironhermes-gateway` succeeds
- `cargo test -p ironhermes-gateway` full suite green (lib + integration)
- `cargo clippy -p ironhermes-gateway -- -D warnings` exits 0
- Phase 36 / 36.1 GW-05 reject-message tests still pass (session_queue=None fallback)
</verification>

<success_criteria>
GatewayRunner owns Arc<SessionQueue>. GatewayMessageHandler holds Option<Arc<SessionQueue>>, threaded via build_gateway_handler. When agent_running is true at the non-slash entry point, the event is enqueued; cap-hit triggers ❌ reaction + chat reply. After each run_agent completion, the worker calls `runner.drain_pending(...)` which pops queued events in FIFO order and re-invokes run_agent directly. Drain helper is exposed as `pub(crate) async fn drain_pending` and the integration test invokes it directly (not a substitute pop-sequence test). No regressions in Phase 36/36.1 tests.
</success_criteria>

<output>
Create `.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-02-SUMMARY.md` when done.
</output>
