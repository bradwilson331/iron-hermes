---
phase: 36.17.1
plan: 04
type: execute
wave: 3
depends_on: [36.17.1-02]
files_modified:
  - crates/ironhermes-gateway/src/runner.rs
autonomous: true
requirements: TBD
must_haves:
  truths:
    - "GatewayRunner has a new field is_draining: Arc<AtomicBool> initialized to false"
    - "is_draining is set to true at runner.rs:~902 BEFORE self.cancel.cancel() during shutdown (Python parity: _queue_during_drain_enabled)"
    - "GatewayRunner exposes a drain_for_restart() method that the shutdown path calls"
    - "When is_draining is true, try_enqueue continues to ACCEPT new pushes (in-process drain mode preserves arrival order — D-03)"
    - "Queue contents survive the in-process drain window (between is_draining.store(true) and process exit) — verified by unit test"
    - "D-03: Queue-during-drain mode — drain_for_restart sets is_draining=true BEFORE cancel fires; drain mode does NOT relax MAX_QUEUE_DEPTH=128"
  artifacts:
    - path: "crates/ironhermes-gateway/src/runner.rs"
      provides: "is_draining flag + drain_for_restart() + shutdown sequence injection"
      contains: "is_draining"
  key_links:
    - from: "GatewayRunner shutdown sequence (~runner.rs:902)"
      to: "self.is_draining.store(true, Ordering::SeqCst)"
      via: "set BEFORE self.cancel.cancel()"
      pattern: "is_draining.store\\(true.*\\n.*self.cancel.cancel"
    - from: "GatewayRunner::drain_for_restart"
      to: "is_draining flag flip + cancel"
      via: "consolidated method"
      pattern: "drain_for_restart"
---

<objective>
Add the drain-mode flag (`is_draining: Arc<AtomicBool>`) to `GatewayRunner`, set it to `true` BEFORE `self.cancel.cancel()` fires in the shutdown sequence (`runner.rs:~902`), expose a public `is_draining()` getter and a `drain_for_restart()` method, and document the contract: drain mode preserves queue contents AND continues to accept new enqueues during the in-process drain window so messages arriving mid-shutdown are not lost (D-03).

Purpose: Close T-36.17.1-03 (lost-update during drain-mode transition) by locking the contract: drain mode preserves AND accepts. Verify queue contents survive the drain window via a unit test.
Output: Modified runner.rs with the new field, accessor, drain method, shutdown injection, and unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-CONTEXT.md
@.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-RESEARCH.md
@.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-PATTERNS.md
@.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-02-PLAN.md
@crates/ironhermes-gateway/src/runner.rs
@crates/ironhermes-gateway/src/session_queue.rs

<interfaces>
<!-- From Plan 02 (live in tree): -->
- GatewayRunner.session_queue: Arc<SessionQueue>
- GatewayRunner::try_enqueue / dequeue / queue_len / clear_queue / retain_queue (5 public methods)
- GatewayRunner::session_queue() -> Arc<SessionQueue> crate-private accessor

<!-- Python source (drain-mode semantics): -->
From /Users/twilson/code/hermes-agent/gateway/run.py:2298-2302:
```
def _queue_during_drain_enabled(self) -> bool:
    return self._restart_requested and self._busy_input_mode in {"queue", "steer"}
```
The Rust analog: `pub fn is_draining(&self) -> bool` returns the AtomicBool value. During drain mode, the gateway CONTINUES to accept enqueues — the contract preserves arrival order across the brief in-process window.

<!-- Shutdown sequence (existing, runner.rs:870-943): -->
1. tokio::select! fires on ctrl+c / cancel token (runner.rs:874)
2. MCP servers shut down (runner.rs:895-898)
3. self.cancel.cancel() fires (runner.rs:902) — INJECTION POINT: is_draining.store(true) IMMEDIATELY BEFORE this
4. Per-chat workers drained with 5s timeout (runner.rs:904-933)
5. drop(msg_tx) (runner.rs:937)
6. join_set drained (runner.rs:940)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add is_draining field + accessor + drain_for_restart method + shutdown injection</name>
  <files>crates/ironhermes-gateway/src/runner.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/runner.rs lines 1-30 (imports — verify Arc, AtomicBool, Ordering availability; std::sync::atomic may need new import)
    - crates/ironhermes-gateway/src/runner.rs lines 90-150 (GatewayRunner struct + new() body — the field declaration and initializer land here, adjacent to session_queue from Plan 02)
    - crates/ironhermes-gateway/src/runner.rs lines 870-943 (full shutdown sequence — find the exact line where `self.cancel.cancel()` is called and inject is_draining.store(true) immediately before)
    - crates/ironhermes-gateway/src/session_queue.rs (Plan 01 — verify try_push has no is_draining gate; the contract is "drain mode preserves AND accepts")
    - .planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-PATTERNS.md §"Shutdown sequence — drain-mode injection point"
    - .planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-RESEARCH.md §"§2298-2302 — _queue_during_drain_enabled"
  </read_first>
  <behavior>
    - GatewayRunner.is_draining: Arc<AtomicBool> exists and is initialized to AtomicBool::new(false) in new()
    - pub fn is_draining(&self) -> bool { self.is_draining.load(Ordering::SeqCst) } accessor compiles and returns false by default
    - pub fn drain_for_restart(&self) consolidates the drain-mode entry: `self.is_draining.store(true, Ordering::SeqCst); self.cancel.cancel();`
    - In the shutdown sequence near runner.rs:902, BEFORE the existing self.cancel.cancel() line, the new sequence calls self.is_draining.store(true, Ordering::SeqCst). The simplest realization: replace the existing `self.cancel.cancel();` with `self.drain_for_restart();` so the ordering invariant is encapsulated.
    - SessionQueue.try_push does NOT consult is_draining — drain mode is signal-only; the queue continues to accept pushes (D-03 + T-36.17.1-03 contract: preserve AND accept).
    - Unit test test_drain_mode_preserves_queue: build a GatewayRunner (or a runner stub that exposes the relevant fields), push 3 events into the queue, call drain_for_restart, assert queue_len == 3 (events preserved) and is_draining() == true.
    - Unit test test_drain_mode_accepts_new_pushes: in drain mode (is_draining=true), call try_enqueue with a fresh event; assert Ok(()) and queue_len increments.
    - Existing Phase 36 / 36.1 shutdown / cancel tests still pass (the drain_for_restart helper preserves the original cancel semantics).
  </behavior>
  <action>
    1. In crates/ironhermes-gateway/src/runner.rs imports (lines 1-30), add `use std::sync::atomic::{AtomicBool, Ordering};` if not already present. (Phase 36 added AtomicBool import to session.rs and handler.rs — verify runner.rs's status with grep before adding.)

    2. In the GatewayRunner struct declaration, add a new field immediately after session_queue (Plan 02): `is_draining: Arc<AtomicBool>,`. Doc comment: `/// Phase 36.17.1 D-03: drain-mode flag — set true BEFORE cancel.cancel() during shutdown so late-arriving messages stay in the queue and reach the next agent turn (in-process only).`

    3. In GatewayRunner::new(...) Self {} literal (around lines 109-130), initialize the field adjacent to session_queue: `is_draining: Arc::new(AtomicBool::new(false)),`.

    4. Add two new methods on impl GatewayRunner near the Plan-02 queue API methods:
       - `pub fn is_draining(&self) -> bool { self.is_draining.load(Ordering::SeqCst) }`
       - `pub fn drain_for_restart(&self) { self.is_draining.store(true, Ordering::SeqCst); self.cancel.cancel(); }`
       Each gets a Rustdoc line citing D-03 and Python parity (gateway/run.py:2298-2302).

    5. In the shutdown sequence (lines 870-943), find the existing `self.cancel.cancel();` line (~line 902). Replace it with `self.drain_for_restart();`. Add an inline comment: `// Phase 36.17.1 D-03: set is_draining BEFORE cancel so the queue keeps accepting late arrivals`.

       Verify no other call sites in the workspace call `self.cancel.cancel()` on GatewayRunner — if there are additional shutdown paths, decide per-site whether to switch them to drain_for_restart() (preferred) or leave cancel.cancel() in place (acceptable when drain-mode semantics are NOT desired, e.g. forced abort). Default: preserve drain semantics by using drain_for_restart everywhere cancel.cancel was called from a graceful path.

    6. SessionQueue.try_push contract: confirm Plan 01's try_push does NOT consult is_draining. The drain flag is a SIGNAL, not a gate. If during code review the executor finds an erroneous is_draining check inside try_push, REMOVE it — the contract per D-03 is "preserve AND accept".

    7. Add unit tests inside a `#[cfg(test)] mod drain_mode_tests` block in runner.rs (or a new tests/drain_mode.rs integration file if runner.rs internals aren't accessible from tests):
       - test_is_draining_starts_false: new runner -> is_draining() == false
       - test_drain_for_restart_flips_flag: call drain_for_restart -> is_draining() == true AND cancel token is cancelled (use CancellationToken::is_cancelled() to verify)
       - test_drain_mode_preserves_queue: push 3 events into runner.session_queue via runner.try_enqueue; call drain_for_restart; assert runner.queue_len(&key) == 3 (events preserved across the flag flip)
       - test_drain_mode_accepts_new_pushes: call drain_for_restart first (is_draining=true), then runner.try_enqueue(&key, event) returns Ok(()), and queue_len increments

       If GatewayRunner cannot be cheaply constructed in a unit test, construct a minimal fixture: a helper `fn test_runner() -> GatewayRunner` building a runner with default Config + stub state. If that's too heavy, factor the drain-mode logic into a small struct DrainState that GatewayRunner composes, test that in isolation, and assert at the integration level that GatewayRunner's shutdown path invokes it.

    Constraints:
    - DO NOT add an is_draining gate inside try_push (contract: drain mode preserves AND accepts).
    - DO NOT change the existing 5s drain-timeout behavior in lines 904-933 — that's the worker-task drain, separate from the queue drain-mode flag.
    - Use Ordering::SeqCst for symmetry with Phase 36 AtomicBool usage (session.rs, handler.rs).
    - Drain-mode is in-process only — the unit test asserts the queue's len within the same process; cross-process queue persistence is explicitly out of scope (RESEARCH §"§2298-2302" + Phase title "in-mem").
  </action>
  <verify>
    <automated>cargo build -p ironhermes-gateway 2>&1 | tail -10 ; cargo test -p ironhermes-gateway --lib runner 2>&1 | tail -10 ; cargo test -p ironhermes-gateway 2>&1 | tail -10 ; cargo clippy -p ironhermes-gateway -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'is_draining: Arc<AtomicBool>' crates/ironhermes-gateway/src/runner.rs` >= 1 (field decl)
    - `grep -c 'is_draining: Arc::new(AtomicBool::new(false))' crates/ironhermes-gateway/src/runner.rs` == 1 (initializer)
    - `grep -c 'pub fn is_draining(&self) -> bool' crates/ironhermes-gateway/src/runner.rs` == 1
    - `grep -c 'pub fn drain_for_restart' crates/ironhermes-gateway/src/runner.rs` == 1
    - `grep -c 'self.is_draining.store(true' crates/ironhermes-gateway/src/runner.rs` >= 1
    - In the shutdown region around line 902: the existing `self.cancel.cancel();` line is replaced with `self.drain_for_restart();` — verify with `awk '/self.drain_for_restart\(\);/{found=1} END {print found}' crates/ironhermes-gateway/src/runner.rs` returns 1
    - Drain ordering invariant: `is_draining.store(true` precedes `self.cancel.cancel()` in source order. Since drain_for_restart encapsulates the pair, this is enforced by the method body; verify with: `awk '/pub fn drain_for_restart/,/^    }/' crates/ironhermes-gateway/src/runner.rs | grep -E 'is_draining\.store|cancel\.cancel' | head -2` shows is_draining.store FIRST and cancel.cancel SECOND
    - `cargo test -p ironhermes-gateway --lib runner::drain_mode_tests::test_is_draining_starts_false` passes (or whichever module name the executor chose)
    - `cargo test -p ironhermes-gateway --lib runner::drain_mode_tests::test_drain_for_restart_flips_flag` passes
    - `cargo test -p ironhermes-gateway --lib runner::drain_mode_tests::test_drain_mode_preserves_queue` passes
    - `cargo test -p ironhermes-gateway --lib runner::drain_mode_tests::test_drain_mode_accepts_new_pushes` passes
    - `grep -c 'is_draining' crates/ironhermes-gateway/src/session_queue.rs` == 0 (contract: try_push does NOT consult is_draining)
    - `cargo test -p ironhermes-gateway` exits 0 (no regressions in Phase 36 / 36.1 shutdown tests)
    - `cargo clippy -p ironhermes-gateway -- -D warnings` exits 0
  </acceptance_criteria>
  <done>is_draining flag added, drain_for_restart consolidates the flag-flip-then-cancel ordering, shutdown sequence uses drain_for_restart, queue accepts new pushes during drain, 4 unit tests prove the contract, no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tokio shutdown signal → GatewayRunner.drain_for_restart | The shutdown signal must transition the runner into drain mode BEFORE workers exit so any in-flight enqueue request observes is_draining=true and is NOT dropped. |
| External producer → try_push during drain | Pushes arriving in the brief window between drain_for_restart() and process exit must be accepted; rejection would lose user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.17.1-01 | DoS | drain_for_restart loop | mitigate | Same MAX_QUEUE_DEPTH=128 cap applies during drain. Drain mode does not relax bounds. |
| T-36.17.1-03 | Lost-update (Tampering) | drain-mode transition window | mitigate | drain_for_restart() encapsulates `is_draining.store(true, SeqCst)` THEN `cancel.cancel()` — atomic in source order, enforced by the acceptance criterion's awk check on method body ordering. SessionQueue.try_push has NO is_draining gate (contract: preserve AND accept). Concurrent push during the millisecond between store(true) and cancel.cancel sees is_draining=true and succeeds (Ok). Concurrent push between cancel.cancel and process exit may still race the cancel-token's downstream effects, but the push itself succeeds — queue contents are preserved in memory until process exit. Cross-process persistence is explicitly out of scope (RESEARCH Assumption A3). |
| T-36.17.1-SC | Tampering | new dependency | n/a | No new dependencies introduced in this plan. |
</threat_model>

<verification>
- cargo build -p ironhermes-gateway exits 0
- cargo test -p ironhermes-gateway full suite green (drain tests + Phase 36 / 36.1 shutdown tests)
- cargo clippy -p ironhermes-gateway -- -D warnings exits 0
- Source-order audit: is_draining.store(true) PRECEDES cancel.cancel() in drain_for_restart body
</verification>

<success_criteria>
GatewayRunner owns Arc<AtomicBool> is_draining initialized false. drain_for_restart() flips it to true THEN cancels, in that order. Shutdown sequence uses drain_for_restart(). SessionQueue.try_push has no is_draining gate (preserve-AND-accept contract). Four unit tests cover: default false, flip-flag, preserve-queue, accept-new-pushes. T-36.17.1-03 closed.
</success_criteria>

<output>
Create `.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-04-SUMMARY.md` when done.
</output>
