# Phase 36.17.1 — UAT Test Prompts (129 total)

**Phase:** 36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions
**Created:** 2026-05-28
**Status:** Draft

> Locked decisions exercised here:
> - D-01 — full /queue feature parity (queue type + gateway wiring + /queue + busy-agent enqueue + /new+/reset clearing + drain-mode).
> - D-02 — Telegram is the only wired platform in this phase. Discord, Slack, and iron_hermes_ui web wiring are deferred to follow-up phases.
> - D-03 — drain-mode preservation is in-process only. Cross-process restart preservation is out of scope.
> - D-13 — Telegram cap-hit UX: ❌ reaction via PlatformAdapter::add_reaction, then a chat reply ⏳ Queue is full (128 messages). Wait for the agent to drain before sending more.

---

## Category 1: SessionQueue Core Data Structure (Prompts 1–30)

| # | Prompt | Decision | Plan |
|---|--------|----------|------|
| 1 | Push a single message event into an empty SessionQueue and verify `len()` returns 1. | D-06 | 01 |
| 2 | Push event A then event B; pop returns A first (FIFO order). | D-06 | 01 |
| 3 | Push A, B, C; pop three times and verify A → B → C order with no reordering. | D-06 | 01 |
| 4 | Pop from a session key that was never pushed returns `None` without panicking. | D-08 | 01 |
| 5 | Pop from an empty SessionQueue (no keys ever added) returns `None`. | D-08 | 01 |
| 6 | Push 1 event, pop it, then pop again — second pop returns `None`. | D-08 | 01 |
| 7 | Push 128 events (the MAX_QUEUE_DEPTH limit); all 128 succeed with `Ok(())`. | D-09 | 01 |
| 8 | Push 128 events then attempt a 129th — returns `Err(QueueError::CapacityReached)`. | D-09, D-10 | 01 |
| 9 | Verify `QueueError::CapacityReached` contains `session_key` and `max: 128` fields. | D-11 | 01 |
| 10 | Push 128 events, pop one (depth → 127), push again — succeeds (re-entry below cap). | D-09 | 01 |
| 11 | Push 95 events — no `tracing::warn!` emitted (below WARN_QUEUE_DEPTH=96 threshold). | D-11 | 01 |
| 12 | Push the 96th event — `tracing::warn!` fires with session key and depth. | D-11 | 01 |
| 13 | Push events 97 through 128 — each triggers the soft warning (all are ≥96). | D-11 | 01 |
| 14 | Verify `MAX_QUEUE_DEPTH` constant equals 128. | D-09 | 01 |
| 15 | Verify `WARN_QUEUE_DEPTH` constant equals 96 (75% of 128). | D-11 | 01 |
| 16 | Call `clear()` on a session with 5 queued events — `len()` returns 0 afterwards. | D-08 | 01 |
| 17 | Call `clear()` on a session key that was never pushed — no panic, no error. | D-08 | 01 |
| 18 | Call `clear()` twice on the same key — idempotent, no panic. | D-08 | 01 |
| 19 | After `clear()`, `pop()` returns `None`. | D-08 | 01 |
| 20 | Push 5 events with alternating content ("a","b","a","b","a"); `retain(\|e\| e.content == "a")` keeps 3 events. | D-08 | 01 |
| 21 | After `retain`, popped events are only those matching the predicate, in original FIFO order. | D-08 | 01 |
| 22 | `retain` on an empty/missing key — no panic, no error. | D-08 | 01 |
| 23 | `retain` that keeps nothing → removes the key entirely; `len()` returns 0. | D-08 | 01 |
| 24 | Push events for session key A and session key B — popping A returns only A's events. | D-09 | 01 |
| 25 | Push events for key A and key B — `clear(A)` does not affect key B's queue. | D-09 | 01 |
| 26 | SessionKeys differing only in `user_id` (Some("u1") vs Some("u2")) produce independent queues. | T-36.17.1-02 | 01 |
| 27 | SessionKeys differing only in `platform` (Telegram vs Discord) produce independent queues. | T-36.17.1-02 | 01 |
| 28 | SessionKeys differing only in `chat_id` produce independent queues. | T-36.17.1-02 | 01 |
| 29 | `len()` on a never-pushed key returns 0 (not an error). | D-08 | 01 |
| 30 | After popping all events from a key, the internal map entry is removed (no empty-VecDeque accretion) — verified by `len()` returning 0. | D-06 | 01 |

---

## Category 2: SplitSlotQueue Parity Mirror (Prompts 31–40)

| # | Prompt | Decision | Plan |
|---|--------|----------|------|
| 31 | `SplitSlotQueue` push A then B then C; pop returns A, B, C in order (smoke test). | D-07 | 01 |
| 32 | `SplitSlotQueue` push 128 events; 129th returns `Err(CapacityReached)`. | D-07 | 01 |
| 33 | `SplitSlotQueue` pop from empty returns `None`. | D-07 | 01 |
| 34 | `SplitSlotQueue` len after 3 pushes returns 3; after 1 pop returns 2. | D-07 | 01 |
| 35 | `SplitSlotQueue` clear removes all events; len returns 0. | D-07 | 01 |
| 36 | `proptest_equivalence`: for 1024 random push/pop/len/clear sequences, `SessionQueue` and `SplitSlotQueue` produce identical observable results. | D-07 | 01 |
| 37 | `proptest_cap_invariant`: across 1024 push-only sequences, `SessionQueue::len` never exceeds 128. | D-07, T-36.17.1-01 | 01 |
| 38 | `SplitSlotQueue` mirrors Python's pending_slot + overflow_list two-layer semantics exactly — slot occupied → overflow append; slot free → slot insert. | D-07 | 01 |
| 39 | `SplitSlotQueue` promote logic: after pop clears slot, overflow head moves to slot (mirrors `_promote_queued_event`). | D-07 | 01 |
| 40 | Parity module is `#[cfg(test)]` only — `SplitSlotQueue` does not appear in release binary (`cargo build --release` references zero parity types). | D-07 | 01 |

---

## Category 3: GatewayRunner Integration (Prompts 41–55)

| # | Prompt | Decision | Plan |
|---|--------|----------|------|
| 41 | `GatewayRunner::new()` initializes `session_queue` field as `Arc<SessionQueue>`. | D-14 | 02 |
| 42 | `runner.try_enqueue(key, event)` delegates to `SessionQueue::try_push` and returns the same result. | D-15 | 02 |
| 43 | `runner.dequeue(key)` delegates to `SessionQueue::pop`. | D-15 | 02 |
| 44 | `runner.queue_len(key)` delegates to `SessionQueue::len`. | D-15 | 02 |
| 45 | `runner.clear_queue(key)` delegates to `SessionQueue::clear`. | D-15 | 02 |
| 46 | `runner.retain_queue(key, predicate)` delegates to `SessionQueue::retain`. | D-15 | 02 |
| 47 | `runner.session_queue()` returns a cloned `Arc<SessionQueue>` for internal threading. | D-15 | 02 |
| 48 | `GatewayMessageHandler` struct has a `session_queue: Option<Arc<SessionQueue>>` field. | D-15 | 02 |
| 49 | `handler.set_session_queue(arc)` stores the queue reference. | D-15 | 02 |
| 50 | `build_gateway_handler` calls `handler.set_session_queue(self.session_queue.clone())`. | D-15 | 02 |
| 51 | Handler with `session_queue = None` falls back to the old `AGENT_RUNNING_REJECT_MSG` behavior. | D-15 | 02 |
| 52 | Existing Phase 36/36.1 GW-05 reject-message tests still pass (None fallback exercised). | — | 02 |
| 53 | No `tokio::sync::Mutex` introduced in `session_queue.rs`. | D-17 | 01 |
| 54 | No `parking_lot::Mutex` introduced in `session_queue.rs`. | D-16 | 01 |
| 55 | All `SessionQueue` methods are synchronous — no `.await` in any critical section. | D-17 | 01 |

---

## Category 4: Busy-Agent Enqueue Path (Prompts 56–70)

| # | Prompt | Decision | Plan |
|---|--------|----------|------|
| 56 | When `agent_running == true` and `session_queue.is_some()`, a free-text message is enqueued (not rejected). | D-01(b) | 02 |
| 57 | The enqueued event uses `event.clone()` — original event fields (chat_id, message_id) remain available. | D-01(b) | 02 |
| 58 | On successful enqueue, a `tracing::debug!` line records session key and depth. | D-01(b) | 02 |
| 59 | On successful enqueue of free text, **no** chat reply is sent to the user (silent enqueue). | D-13 | 02 |
| 60 | The existing `UserQueueManager` 👁 transport-layer reaction is unaffected by the new session-level queue. | — | 02 |
| 61 | When `agent_running == true` and queue is at capacity (128), `try_push` returns `CapacityReached`. | D-09 | 02 |
| 62 | On `CapacityReached`, `adapter.add_reaction(chat_id, message_id, "❌")` fires. | D-13 | 02 |
| 63 | On `CapacityReached`, `adapter.send_message` sends "⏳ Queue is full (128 messages). Wait for the agent to drain before sending more." | D-13 | 02 |
| 64 | On `CapacityReached`, a `tracing::warn!` records the drop with session key. | D-13 | 02 |
| 65 | The `add_reaction` call uses `.ok()` so a reaction failure does not poison the chat reply. | D-13 | 02 |
| 66 | After cap-hit UX, the function returns `Ok(())` — no error propagated. | D-13 | 02 |
| 67 | Queue depth remains 128 after a cap-hit rejection (129th not appended). | D-10, T-36.17.1-01 | 02 |
| 68 | SessionKey is constructed with the full triple: `platform + chat_id + user_id` (Pitfall 7). | T-36.17.1-02 | 02 |
| 69 | The `std::sync::MutexGuard` is dropped before any `.await` call (Pitfall 2 — compiler enforces this). | D-17 | 02 |
| 70 | When `agent_running == false`, the message goes to `run_agent` directly (no enqueue). | D-01(b) | 02 |

---

## Category 5: Post-Turn Drain Loop (Prompts 71–82)

| # | Prompt | Decision | Plan |
|---|--------|----------|------|
| 71 | `drain_pending` is exposed as `pub(crate) async fn` on `GatewayRunner`. | D-01(b) | 02 |
| 72 | `drain_pending` pops events from `SessionQueue` in FIFO order. | D-01 | 02 |
| 73 | `drain_pending` calls `handler.run_agent` for each popped event (not `handle_with_multimodal`). | Pitfall 4 | 02 |
| 74 | Drain loop with 3 queued events A, B, C produces run_agent invocations in order A → B → C. | D-01 | 02 |
| 75 | After drain completes, `queue_len(key)` is 0. | D-01 | 02 |
| 76 | Drain loop with an empty queue (0 events) completes immediately with no run_agent calls. | D-01 | 02 |
| 77 | The RAII `RunningAgentGuard` inside `run_agent` sets AtomicBool true for each drained turn. | Pitfall 4 | 02 |
| 78 | Events enqueued during a drained turn (mid-drain arrival) land in the queue and are picked up on the next pop iteration. | D-01, T-36.17.1-03 | 02 |
| 79 | Per-chat worker calls `drain_pending` after each `handle_with_multimodal` return. | D-01(b) | 02 |
| 80 | `drain_pending` logs a `tracing::debug!` for each drained event with session key and remaining count. | D-01(b) | 02 |
| 81 | Drain loop does not merge events — each queued item gets one full agent turn. | D-01 | 02 |
| 82 | Drain loop bypasses the busy-guard check in `handle_with_multimodal` (Pitfall 4 — direct `run_agent` call). | Pitfall 4 | 02 |

---

## Category 6: /queue Slash Command (Prompts 83–97)

| # | Prompt | Decision | Plan |
|---|--------|----------|------|
| 83 | `CommandResult` enum has a `Queued { message: String }` variant. | D-08 | 03 |
| 84 | `cmd_queue` with non-empty args returns `CommandResult::Queued { message: args.join(" ") }`. | D-01(c) | 03 |
| 85 | `cmd_queue` with empty args returns `CommandResult::Output` containing "Usage: /queue". | D-01(c) | 03 |
| 86 | `cmd_queue` does not reference `ctx.agent_loop` (Pitfall 3 gate removed). | Pitfall 3 | 03 |
| 87 | `cmd_queue` is side-effect free — no Mutex, no I/O. | D-08 | 03 |
| 88 | `/queue` is in the running-agent bypass list (`is_bypass("queue")` returns true). | D-01(c) | 03 |
| 89 | Gateway handler intercepts `CommandResult::Queued` and synthesizes a `MessageEvent`. | D-01(c) | 03 |
| 90 | Synthesized `MessageEvent` inherits `platform`, `chat_id`, `sender_id`, `message_id` from the triggering event. | T-36.17.1-02 | 03 |
| 91 | Synthesized `MessageEvent`'s `content` field is the `/queue` argument text. | D-01(c) | 03 |
| 92 | On successful enqueue via `/queue`, bot replies "Queued for the next turn." when depth ≤ 1. | D-01(c) | 03 |
| 93 | On successful enqueue via `/queue`, bot replies "Queued for the next turn. ({depth} queued)" when depth > 1. | D-01(c) | 03 |
| 94 | `/queue` on a full queue (128) triggers the same D-13 cap-hit UX (❌ reaction + chat reply). | D-13 | 03 |
| 95 | When `session_queue` is `None`, the Queued arm sends a degraded "Queued for the next turn." reply anyway. | D-15 | 03 |
| 96 | `/queue` works while the agent is actively running a turn (bypass list ensures it reaches the command handler). | D-01(c) | 03 |
| 97 | The `"queue"` dispatch wiring in `handlers.rs` is unchanged — only the function body is rewritten. | D-01(c) | 03 |

---

## Category 7: /new and /reset Clearing Hooks (Prompts 98–106)

| # | Prompt | Decision | Plan |
|---|--------|----------|------|
| 98 | `/new` calls `session_queue.clear(&session_key)` BEFORE `session_store.remove(&session_key)` (Pitfall 5). | D-01(e), Pitfall 5 | 03 |
| 99 | `/reset` is aliased to `/new` at the router level — a single injection covers both. | D-01(e) | 03 |
| 100 | After `/new`, `queue_len(key)` is 0. | D-01(e) | 03 |
| 101 | After `/new`, previously queued events do not replay. | D-01(e) | 03 |
| 102 | After `/new`, `session_store` no longer contains the session key. | D-01(e) | 03 |
| 103 | The `clear_queue` call fires even if the session had no queued events (idempotent). | D-01(e) | 03 |
| 104 | The `clear_queue` guard drops before the `session_store.write().await` acquires (no cross-lock deadlock). | Pitfall 2 | 03 |
| 105 | A freshly created session after `/new` starts with an empty queue. | D-01(e) | 03 |
| 106 | If `session_queue` is `None`, `/new` skips the clear call and proceeds with the existing store.remove behavior. | D-15 | 03 |

---

## Category 8: Drain-Mode Flag (Prompts 107–117)

| # | Prompt | Decision | Plan |
|---|--------|----------|------|
| 107 | `GatewayRunner` has an `is_draining: Arc<AtomicBool>` field initialized to `false`. | D-03 | 04 |
| 108 | `runner.is_draining()` returns `false` by default. | D-03 | 04 |
| 109 | `drain_for_restart()` sets `is_draining` to `true`. | D-03 | 04 |
| 110 | `drain_for_restart()` calls `self.cancel.cancel()` after setting the flag. | D-03 | 04 |
| 111 | `is_draining.store(true)` precedes `cancel.cancel()` in source order within `drain_for_restart`. | D-03, T-36.17.1-03 | 04 |
| 112 | The shutdown sequence uses `self.drain_for_restart()` instead of bare `self.cancel.cancel()`. | D-03 | 04 |
| 113 | After `drain_for_restart()`, `CancellationToken::is_cancelled()` returns true. | D-03 | 04 |
| 114 | `try_enqueue` succeeds during drain mode (is_draining=true) — queue accepts new pushes. | D-03 | 04 |
| 115 | Queue contents are preserved after `drain_for_restart` — `queue_len` remains unchanged. | D-03 | 04 |
| 116 | `SessionQueue::try_push` has NO `is_draining` gate — the drain flag is signal-only. | D-03 | 04 |
| 117 | `MAX_QUEUE_DEPTH=128` still applies during drain mode (no relaxation). | D-03, T-36.17.1-01 | 04 |

---

## Category 9: Telegram End-to-End (Prompts 118–129)

| # | Prompt | Decision | Plan |
|---|--------|----------|------|
| 118 | A Telegram `MessageEvent` arriving while `agent_running=true` enqueues into `SessionQueue` (not rejected). | D-01(b), D-02 | 05 |
| 119 | The enqueue is silent — `RecordingPlatformAdapter` records zero `send_message` calls from the busy branch. | D-13 | 05 |
| 120 | Pre-fill queue to 128 for a Telegram session; dispatch a 129th — `add_reaction("❌")` fires with the 129th event's `chat_id` and `message_id`. | D-13 | 05 |
| 121 | The 129th cap-hit sends exactly one `send_message` containing "Queue is full (128 messages)". | D-13 | 05 |
| 122 | After cap-hit, `session_queue.len(key) == 128` (cap held, 129th was dropped). | T-36.17.1-01 | 05 |
| 123 | Telegram `getUpdates` offset advances unconditionally before dispatch (Pitfall 6) — cap-hit cannot cause infinite re-delivery. | Pitfall 6 | 05 |
| 124 | `drain_pending` with 3 Telegram events A, B, C replays them in order A → B → C via `run_agent`. | D-01 | 05 |
| 125 | After drain, `session_queue.len(key) == 0`. | D-01 | 05 |
| 126 | UAT Scenario 1 (busy-enqueue silent path): send message during active turn → 👁 reaction visible, second message replays after first turn. | D-01, D-02 | 05 |
| 127 | UAT Scenario 2 (/queue command path): `/queue do this next` while busy → bot replies "Queued for the next turn." → queued message replays after current turn. | D-01(c), D-02 | 05 |
| 128 | UAT Scenario 3 (cap-hit UX): 128 queued + 129th → ❌ reaction + "⏳ Queue is full" reply; no re-delivery of 129th. | D-13, Pitfall 6 | 05 |
| 129 | UAT Scenario 4 (/new clears + drain-mode): `/new` clears queued events; ctrl+c during busy turn logs `is_draining=true` before cancel fires; late arrivals in the drain window are preserved in-process. | D-01(e), D-03 | 05 |

---

## Coverage Summary

| Category | Prompts | Count | Plans Covered |
|----------|---------|-------|---------------|
| SessionQueue Core Data Structure | 1–30 | 30 | 01 |
| SplitSlotQueue Parity Mirror | 31–40 | 10 | 01 |
| GatewayRunner Integration | 41–55 | 15 | 01, 02 |
| Busy-Agent Enqueue Path | 56–70 | 15 | 02 |
| Post-Turn Drain Loop | 71–82 | 12 | 02 |
| /queue Slash Command | 83–97 | 15 | 03 |
| /new and /reset Clearing Hooks | 98–106 | 9 | 03 |
| Drain-Mode Flag | 107–117 | 11 | 04 |
| Telegram End-to-End | 118–129 | 12 | 05 |
| **Total** | **1–129** | **129** | **01–05** |

## Decision Coverage

| Decision | Prompts Exercising It |
|----------|-----------------------|
| D-01 (full /queue parity) | 56–59, 70–82, 83–97, 98–106, 118, 124–129 |
| D-02 (Telegram only) | 118–129 |
| D-03 (drain-mode in-process) | 107–117, 129 |
| D-06 (SessionQueue shape) | 1–3, 30, 41 |
| D-07 (#[cfg(test)] parity) | 31–40 |
| D-08 (idiomatic Rust API) | 4–6, 16–23, 29, 83, 87 |
| D-09 (128 per-session cap) | 7–8, 10, 14, 24–25, 61, 67 |
| D-10 (drop-newest) | 8, 67 |
| D-11 (soft warn at 96) | 9, 11–13, 15 |
| D-13 (Telegram cap-hit UX) | 59, 62–66, 94, 119–122, 128 |
| D-14 (private field on runner) | 41 |
| D-15 (thin public API) | 42–50, 95, 106 |
| D-16 (std::sync::Mutex) | 54 |
| D-17 (sync methods only) | 53, 55, 69 |

## Threat Coverage

| Threat | Prompts Exercising It |
|--------|-----------------------|
| T-36.17.1-01 (DoS / memory cap) | 8, 37, 67, 117, 122 |
| T-36.17.1-02 (cross-session bleed) | 26–28, 68, 90 |
| T-36.17.1-03 (drain-mode lost-update) | 78, 111 |
| T-36.17.1-04 (DoS via /queue flood) | 94, 128 |

## Pitfall Coverage

| Pitfall | Prompts Exercising It |
|---------|-----------------------|
| Pitfall 2 (MutexGuard across await) | 69, 104 |
| Pitfall 3 (agent_loop gate) | 86 |
| Pitfall 4 (double guard on drain re-entry) | 73, 77, 82 |
| Pitfall 5 (clear before remove ordering) | 98 |
| Pitfall 6 (Telegram offset advance) | 123, 128 |
| Pitfall 7 (SessionKey full triple) | 68 |
