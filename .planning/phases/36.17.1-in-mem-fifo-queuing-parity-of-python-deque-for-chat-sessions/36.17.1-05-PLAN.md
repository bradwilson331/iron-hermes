---
phase: 36.17.1
plan: 05
type: execute
wave: 4
depends_on: [36.17.1-02, 36.17.1-03, 36.17.1-04]
files_modified:
  - crates/ironhermes-gateway/src/telegram.rs
  - crates/ironhermes-gateway/tests/session_queue_integration.rs
  - crates/ironhermes-gateway/tests/session_queue_telegram_uat.md
autonomous: false
requirements: TBD
must_haves:
  truths:
    - "The Telegram adapter exercises the busy-enqueue and cap-hit paths end-to-end with a real TelegramAdapter (or a tightly-coupled mock that replicates its add_reaction + send_message + ack-offset shape)"
    - "End-to-end test proves: while agent is busy, an incoming Telegram update enqueues; when capacity hits 128 the 129th update triggers the cap-hit reaction + chat reply + warn-log, AND Telegram's getUpdates offset advances past the dropped update (no infinite retry loop)"
    - "After agent turn completes, the per-chat worker calls GatewayRunner::drain_pending which drains queued events one full turn at a time, in arrival order, no merging"
    - "Manual UAT script documents the live-Telegram steps for D-03 drain-mode preservation (cross-restart preservation is out of scope per RESEARCH; drain UAT verifies in-process drain only)"
    - "D-02: Telegram-only platform — Discord/Slack/web wiring NOT exercised in tests"
    - "D-03: Drain-mode flag verified in UAT Scenario 4b (in-process only; cross-process out of scope)"
    - "D-13: Telegram cap-hit UX — ❌ reaction via PlatformAdapter::add_reaction + 'Queue is full' chat reply verified end-to-end"
    - "D-12: HTTP 429 + Retry-After:5 — design-only this phase (webhook adapter not yet shipped); documented in UAT runbook but NOT verified in automated tests"
  artifacts:
    - path: "crates/ironhermes-gateway/src/telegram.rs"
      provides: "No new logic — all behavior already lives in handler.rs (Plans 02 + 03). This plan VERIFIES the Telegram adapter's existing call sites flow through correctly."
      contains: "PlatformAdapter for TelegramAdapter"
    - path: "crates/ironhermes-gateway/tests/session_queue_integration.rs"
      provides: "Three new end-to-end tests: enqueue-on-busy, cap-hit UX, post-turn drain in arrival order"
      contains: "test_telegram_busy_path_enqueues"
    - path: "crates/ironhermes-gateway/tests/session_queue_telegram_uat.md"
      provides: "Manual UAT runbook for live Telegram cap-hit + drain"
      contains: "Manual UAT: Phase 36.17.1"
  key_links:
    - from: "TelegramAdapter long-poll loop"
      to: "GatewayMessageHandler.handle_with_multimodal"
      via: "existing message dispatch (no change)"
      pattern: "handle_with_multimodal"
    - from: "tests/session_queue_integration.rs"
      to: "RecordingPlatformAdapter (from Plan 02 Task 3)"
      via: "shared test fixture"
      pattern: "RecordingPlatformAdapter"
    - from: "test_telegram_post_turn_drain_in_arrival_order"
      to: "GatewayRunner::drain_pending"
      via: "direct call into the pub(crate) drain helper from Plan 02 Task 3"
      pattern: "drain_pending"
---

<objective>
Verify the Telegram-specific end-to-end paths land correctly: (a) during a busy agent turn, Telegram messages enqueue silently with the existing UserQueueManager 👁 reaction as the visible signal; (b) on cap-hit, the ❌ reaction + "Queue is full" chat reply fire AND Telegram's getUpdates offset still advances (no infinite retry — per Pitfall 6, offset advance happens at runner.rs:513-519 unconditionally before dispatch); (c) post-turn drain via `GatewayRunner::drain_pending` replays queued events one full turn at a time in arrival order, no merging; (d) provide a manual UAT runbook for live-Telegram verification.

Purpose: Lock the Telegram-only ship requirement (D-02). Verify Plans 01-04 work end-to-end on the only platform wired in this phase, including Plan 04's drain-mode flag. Provide a checkpoint where the user can verify on live Telegram.
Output: New end-to-end integration tests in session_queue_integration.rs, a UAT runbook, and a blocking human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-CONTEXT.md
@.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-RESEARCH.md
@.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-PATTERNS.md
@.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-02-PLAN.md
@.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-03-PLAN.md
@.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-04-PLAN.md
@crates/ironhermes-gateway/src/telegram.rs
@crates/ironhermes-gateway/src/adapter.rs
@crates/ironhermes-gateway/src/handler.rs
@crates/ironhermes-gateway/src/runner.rs
@crates/ironhermes-gateway/src/user_queue.rs
@crates/ironhermes-gateway/tests/session_queue_integration.rs

<interfaces>
<!-- From Plans 02 + 03 + 04 (live in tree before this plan executes): -->
- GatewayMessageHandler with Option<Arc<SessionQueue>> + busy-branch enqueue + cap-hit UX (Plan 02)
- GatewayRunner::drain_pending exposed as `pub(crate) async fn` (Plan 02 Task 3) — direct entry point for the post-turn drain test
- CommandResult::Queued interception + clear_queue ordering (Plan 03)
- GatewayRunner.is_draining + drain_for_restart (Plan 04)
- crates/ironhermes-gateway/tests/session_queue_integration.rs already exists with RecordingPlatformAdapter and 6+ tests from Plans 02/03

<!-- TelegramAdapter (unchanged in this plan, but referenced for correctness): -->

From crates/ironhermes-gateway/src/telegram.rs:
- impl PlatformAdapter for TelegramAdapter with override of add_reaction (uses Bot API setMessageReaction)
- send_message override (uses Bot API sendMessage)
- The long-poll loop is in runner.rs:~515-680 — Telegram getUpdates offset is advanced UNCONDITIONALLY at lines 513-519 BEFORE dispatch to UserQueueManager (Pitfall 6: cap-hit UX cannot cause an infinite retry because the offset has already advanced)

From crates/ironhermes-gateway/src/adapter.rs:22-60:
- async fn add_reaction(&self, chat_id, message_id, emoji) -> Result<()>
- async fn send_message(&self, chat_id, content, thread_id) -> Result<MessageResponse>
- Default no-op impls; TelegramAdapter overrides both

From .planning/phases/36.17.1-...-RESEARCH.md §"Common Pitfalls" Pitfall 6:
"D-13 explicitly states 'ack the Telegram getUpdates offset' — the offset advance already happens unconditionally in the poll loop (runner.rs:515-519) before the message is dispatched. The cap-hit UX fires AFTER offset advance, so this is safe by construction."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end Telegram-flow integration tests (busy enqueue, cap-hit UX, post-turn drain)</name>
  <files>crates/ironhermes-gateway/tests/session_queue_integration.rs</files>
  <read_first>
    - crates/ironhermes-gateway/tests/session_queue_integration.rs (the existing test file from Plan 02 Task 3 — review the RecordingPlatformAdapter shape + existing helpers, and note that `runner.drain_pending(...)` is exposed as `pub(crate)` and already exercised by `test_drain_after_turn`)
    - crates/ironhermes-gateway/src/runner.rs lines 510-540 (Telegram poll-loop offset advance — verify it precedes dispatch unconditionally)
    - crates/ironhermes-gateway/src/runner.rs (the `pub(crate) async fn drain_pending` helper from Plan 02 Task 3 — this is the direct entry point the post-turn drain test calls)
    - crates/ironhermes-gateway/src/handler.rs lines 840-854 (busy-branch implementation from Plan 02)
    - crates/ironhermes-gateway/src/handler.rs Plan 03 Task 2 Queued intercept arm
    - crates/ironhermes-gateway/src/user_queue.rs lines 50-80 (👁 reaction at transport layer — clarify that 👁 fires for ALL queued-at-transport-layer messages, separate from SessionQueue)
    - .planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-VALIDATION.md §"Per-Task Verification Map" + §"Manual-Only Verifications"
  </read_first>
  <behavior>
    - test_telegram_busy_path_enqueues: simulate a Telegram MessageEvent arriving via the handler path while agent_running=true. The event enqueues into SessionQueue. RecordingPlatformAdapter records: ZERO send_message calls from the Queued/busy branch (free-text enqueue is silent per D-13); the existing UserQueueManager 👁 transport-layer reaction is NOT under test here (separate layer).
    - test_telegram_cap_hit_full_ux: pre-fill SessionQueue to 128. Dispatch a 129th event via the handler. Assertions:
      * RecordingPlatformAdapter.add_reaction calls == 1 with emoji "❌" matching the 129th event's chat_id + message_id
      * RecordingPlatformAdapter.send_message calls == 1 with content containing "Queue is full (128 messages)"
      * SessionQueue.len(&key) == 128 after the attempt (cap held, T-36.17.1-01 evidence)
      * The poll-loop offset advance is OUTSIDE handler scope (runner.rs:513-519); a NOTE comment in the test references Pitfall 6 — "the cap-hit UX cannot cause infinite Telegram retry because offset advance precedes dispatch unconditionally". The test does not (and cannot) verify offset advance from handler-level tests; the live UAT in Task 2 covers it.
    - test_telegram_post_turn_drain_in_arrival_order: enqueue 3 events with distinct content "A", "B", "C" in that order while agent_running=true. Release the busy flag and invoke `runner.drain_pending(&session_key, &handler, adapter.clone(), cancel.clone()).await?` directly — this exercises the same `pub(crate) async fn drain_pending` exposed by Plan 02 Task 3. Assert run_agent (instrumented to record event content) is called 3 times in order A, B, C — no merging (D-01 explicit: one full agent turn per queued item). A substitute pop-sequence-only test is NOT acceptable; the test must call `drain_pending` directly.
    - All tests pass under cargo test -p ironhermes-gateway --test session_queue_integration.
  </behavior>
  <action>
    1. Open crates/ironhermes-gateway/tests/session_queue_integration.rs (created in Plan 02 Task 3). Append three new tests.

    2. test_telegram_busy_path_enqueues:
       - Build a GatewayMessageHandler with set_session_queue(Arc::new(SessionQueue::new())).
       - Construct a SessionKey using Platform::Telegram + chat_id "test_chat" + sender_id "test_user".
       - Set agent_running flag for that session to true via SessionStore.get_running_flag(&key).store(true, SeqCst).
       - Build a MessageEvent with platform=Telegram, chat_id="test_chat", sender_id="test_user", content="hello".
       - Call handler.handle_with_multimodal(event, recording_adapter, cancel_token, no_attachments).await.
       - Assert: session_queue.len(&key) == 1; recording_adapter.send_message_calls() is empty; recording_adapter.add_reaction_calls() is empty (from the handler's Queued/busy branch — UserQueueManager's transport-layer 👁 is a separate layer not exercised here).

    3. test_telegram_cap_hit_full_ux:
       - Same setup. Pre-fill the queue by calling session_queue.try_push(&key, ev) 128 times with distinct content "msg_001".."msg_128".
       - Dispatch a 129th event with content "msg_129", chat_id="test_chat", message_id="msg_id_129" via handler.handle_with_multimodal.
       - Assert: recording_adapter.add_reaction_calls() has exactly 1 entry with emoji=="❌", chat_id=="test_chat", message_id=="msg_id_129".
       - Assert: recording_adapter.send_message_calls() has exactly 1 entry whose content contains "Queue is full (128 messages)".
       - Assert: session_queue.len(&key) == 128 (T-36.17.1-01 cap held; the 129th was dropped, not appended).
       - Comment block at top of test: "// Pitfall 6: Telegram getUpdates offset advance happens at runner.rs:513-519 BEFORE dispatch. Handler-level test cannot verify offset advance — see session_queue_telegram_uat.md for the live verification step."

    4. test_telegram_post_turn_drain_in_arrival_order:
       - Build an `Arc<GatewayRunner>` (or the minimal subset needed to call `drain_pending`) with a SessionQueue and a `GatewayMessageHandler` whose `run_agent` is instrumented to record (in a `Mutex<Vec<String>>`) the `content` of each `MessageEvent` it receives. Reuse the instrumentation pattern from Plan 02 Task 3's `test_drain_after_turn`.
       - Set agent_running=true for the session_key, enqueue 3 events with content "A", "B", "C" in that order via session_queue.try_push, then release the busy flag (store false).
       - Invoke `runner.drain_pending(&session_key, &handler, adapter.clone(), cancel.clone()).await?` directly. This is the same `pub(crate) async fn drain_pending` exposed by Plan 02 Task 3 — do NOT inline a hand-rolled pop loop in the test.
       - Assert: the recorded run_agent invocation list equals `["A", "B", "C"]` in that exact order; `session_queue.len(&session_key) == 0`; depth went 3 -> 2 -> 1 -> 0 (FIFO arrival order, no merging).

    5. Helpers / fixtures:
       - If the existing session_queue_integration.rs already has helpers like `fn make_handler() -> GatewayMessageHandler`, `fn make_event(chat: &str, content: &str) -> MessageEvent`, and `fn make_runner() -> Arc<GatewayRunner>`, reuse them. Otherwise factor them now so subsequent phases (Discord/Slack/Web /queue) can share.
       - The drain helper is already exposed as `pub(crate) async fn drain_pending` per Plan 02 Task 3 — no additional visibility change is needed in this plan.

    Constraints:
    - Do NOT introduce a real Telegram network call — all tests are in-process with RecordingPlatformAdapter.
    - Do NOT modify telegram.rs's behavior — all queue logic lives in handler.rs per Plans 02 + 03.
    - Cap-hit assertions MUST exercise the EXACT D-13 strings ("⏳ Queue is full (128 messages). ...") and emoji ("❌"). Test should fail loudly if anyone re-words these later.
    - If RecordingPlatformAdapter does not currently capture message_id on add_reaction (Plan 02 might have used a coarser signature), extend it in this task — the cap-hit test must verify the reaction targets the correct dropped message.
    - The post-turn drain test MUST invoke `runner.drain_pending(...)` directly. A pop-sequence-only test is not acceptable; the deliverable is end-to-end exercise of the real drain helper.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-gateway --test session_queue_integration test_telegram_busy_path_enqueues 2>&1 | tail -5 ; cargo test -p ironhermes-gateway --test session_queue_integration test_telegram_cap_hit_full_ux 2>&1 | tail -5 ; cargo test -p ironhermes-gateway --test session_queue_integration test_telegram_post_turn_drain_in_arrival_order 2>&1 | tail -5 ; cargo test -p ironhermes-gateway 2>&1 | tail -10 ; cargo clippy -p ironhermes-gateway --tests -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ironhermes-gateway --test session_queue_integration test_telegram_busy_path_enqueues` passes
    - `cargo test -p ironhermes-gateway --test session_queue_integration test_telegram_cap_hit_full_ux` passes
    - `cargo test -p ironhermes-gateway --test session_queue_integration test_telegram_post_turn_drain_in_arrival_order` passes
    - `grep -c 'test_telegram_busy_path_enqueues' crates/ironhermes-gateway/tests/session_queue_integration.rs` >= 1
    - `grep -c 'test_telegram_cap_hit_full_ux' crates/ironhermes-gateway/tests/session_queue_integration.rs` >= 1
    - `grep -c 'test_telegram_post_turn_drain_in_arrival_order' crates/ironhermes-gateway/tests/session_queue_integration.rs` >= 1
    - `grep -c 'runner.drain_pending(' crates/ironhermes-gateway/tests/session_queue_integration.rs` >= 2 (test_drain_after_turn from Plan 02 + the new test_telegram_post_turn_drain_in_arrival_order — both exercise the real drain helper)
    - `grep -c '"❌"' crates/ironhermes-gateway/tests/session_queue_integration.rs` >= 1 (D-13 reaction emoji literal under test)
    - `grep -c 'Queue is full (128 messages)' crates/ironhermes-gateway/tests/session_queue_integration.rs` >= 1 (D-13 chat reply literal under test)
    - `grep -c 'Pitfall 6' crates/ironhermes-gateway/tests/session_queue_integration.rs` >= 1 (offset-advance comment present)
    - `cargo test -p ironhermes-gateway` full suite green
    - `cargo clippy -p ironhermes-gateway --tests -- -D warnings` exits 0
  </acceptance_criteria>
  <done>Three end-to-end Telegram-flow tests prove busy-enqueue silence, cap-hit UX, and FIFO post-turn drain via `runner.drain_pending`. Full crate test suite stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Write the manual UAT runbook for live Telegram verification</name>
  <files>crates/ironhermes-gateway/tests/session_queue_telegram_uat.md</files>
  <read_first>
    - .planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-VALIDATION.md §"Manual-Only Verifications" (the two manual verifications: cap-hit UX on real Telegram + drain preservation across in-process drain)
    - .planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-CONTEXT.md D-13 (cap-hit UX strings) + D-03 (drain mode scope: in-process only)
    - .planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-04-PLAN.md (drain-mode flag behavior — Scenario 4 of the UAT documents the in-process drain path delivered by Plan 04)
    - scripts/uat/phase-36.17-web-logging.sh (Phase 36.17 UAT script — pattern reference for runbook conventions: mktemp IRONHERMES_HOME, start server, curl, assert)
  </read_first>
  <behavior>
    - File exists at crates/ironhermes-gateway/tests/session_queue_telegram_uat.md
    - Documents 4 UAT scenarios with explicit step lists and expected outcomes:
      1. Busy-enqueue silent path: send a message, while agent is generating send a second; observe the existing 👁 reaction (transport layer, UserQueueManager); after first turn finishes, the second runs automatically.
      2. /queue command path: while agent is busy, run /queue do this next; bot replies "Queued for the next turn." (or with depth count if >1). After current turn completes, the queued message replays.
      3. Cap-hit UX: stress-send 128 messages while agent is busy; send the 129th; observe ❌ reaction on the 129th AND a "⏳ Queue is full (128 messages). Wait for the agent to drain before sending more." chat reply. Verify Telegram does NOT re-deliver the dropped 129th (no infinite retry from offset-advance ordering, Pitfall 6).
      4. /new clears queue + Plan 04 drain-mode preservation: enqueue 3 messages via /queue; run /new; observe "Conversation cleared. Starting fresh."; subsequent messages are fresh (the 3 queued ones must NOT replay — they were cleared). Then exercise Plan 04's in-process drain-mode flag: while agent is busy, trigger a graceful shutdown (ctrl+c or SIGTERM) and verify the runner sets `is_draining=true` before cancel fires; any in-flight messages already in `msg_rx` are pushed to the session_queue rather than dropped (cross-process restart is out of scope; verify only the in-process window).
    - Cross-process drain preservation (the second manual UAT in VALIDATION.md) is documented as OUT OF SCOPE per RESEARCH §"§2298-2302" + Assumption A3 — the runbook explicitly states "cross-process restart preservation is not in this phase; D-03 covers in-process drain only".
  </behavior>
  <action>
    Create crates/ironhermes-gateway/tests/session_queue_telegram_uat.md with the following structure:

    1. Header: "# Manual UAT: Phase 36.17.1 — in-mem FIFO Queuing (Telegram)". Brief intro paragraph referencing CONTEXT.md D-02 (Telegram-only), D-13 (cap-hit UX), D-01 (full scope), D-03 (drain mode in-process only).
    2. Prerequisites section: live Telegram bot token in $TELEGRAM_BOT_TOKEN, $IRONHERMES_HOME pointed at a test config, a chat where the bot is admin'd, the agent configured to respond to free-text messages.
    3. Scenario 1: "Busy-enqueue silent path" — 5 numbered steps + an "Expected" subsection listing observable outcomes (👁 reaction visible, second message replays after first turn ends, no extra reply text for the enqueued message).
    4. Scenario 2: "/queue command path" — 4 steps + Expected (reply text contains "Queued for the next turn.", depth counter increments on subsequent /queue calls).
    5. Scenario 3: "Cap-hit UX" — explicit steps:
       a. Send a long-running prompt (e.g. "research X exhaustively") to start a slow turn.
       b. Run `for i in $(seq 1 128); do telegram-cli send "/queue msg $i"; done` (or equivalent — note that exact tooling depends on the user's setup; the runbook documents intent, not a fixed command).
       c. Send the 129th: `/queue msg 129`.
       d. Expected: 129th message receives ❌ reaction AND a chat reply with "⏳ Queue is full (128 messages). Wait for the agent to drain before sending more." Bot continues responding to currently-active turn normally. After all 128 queued items drain (slow — may take minutes), no extras appear.
       e. Verify Telegram does NOT re-deliver the dropped 129th by checking the bot logs for any duplicate processing of msg_id 129.
    6. Scenario 4: "/new clears queue + Plan 04 drain-mode preservation" — split into two subsections:
       a. "/new clears queue" (5 steps + Expected): after /new, none of the 3 queued messages replay; agent starts fresh.
       b. "Plan 04 in-process drain-mode" (4 steps + Expected): while agent is busy with queued items, ctrl+c the gateway; verify the runner's shutdown sequence sets is_draining=true BEFORE self.cancel.cancel() (visible in logs as a tracing::info!/warn! line); verify messages arriving in the brief drain window are pushed to session_queue rather than dropped (in-process only — process exit still loses queue contents).
    7. Out-of-scope section: explicit "Cross-process drain preservation" header noting that D-03's preservation is in-process only — `kill -9 ironhermes-gateway` LOSES queue contents by design; this is documented in RESEARCH.md §"§2298-2302" and Assumption A3.
    8. Sign-off block: a checklist with [ ] for each scenario, dated by the verifier.

    Constraints:
    - Do NOT write a bash automation script — D-13 cap-hit verification requires visual inspection of Telegram reactions and message rendering, which cannot be reliably automated from the gateway side without a Telegram MTProto client. The runbook is intentionally manual.
    - Use plain Markdown (no special directives) so the runbook is human-readable.
  </action>
  <verify>
    <automated>test -f crates/ironhermes-gateway/tests/session_queue_telegram_uat.md && grep -c '## Scenario' crates/ironhermes-gateway/tests/session_queue_telegram_uat.md && grep -c 'Queue is full (128 messages)' crates/ironhermes-gateway/tests/session_queue_telegram_uat.md && grep -ci 'cross-process' crates/ironhermes-gateway/tests/session_queue_telegram_uat.md && grep -c 'is_draining' crates/ironhermes-gateway/tests/session_queue_telegram_uat.md</automated>
  </verify>
  <acceptance_criteria>
    - `[ -f crates/ironhermes-gateway/tests/session_queue_telegram_uat.md ]` returns 0
    - `grep -c '## Scenario' crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 4 (all four scenarios present)
    - `grep -c '❌' crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 1 (D-13 emoji documented)
    - `grep -c 'Queue is full (128 messages)' crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 1 (D-13 chat reply literal documented)
    - `grep -ci 'cross-process' crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 1 (out-of-scope note present)
    - `grep -c 'Pitfall 6' crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 1 (offset-advance note documented in Scenario 3)
    - `grep -c 'is_draining' crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 1 (Plan 04 drain-mode flag documented in Scenario 4)
  </acceptance_criteria>
  <done>UAT runbook exists with all 4 scenarios, exact D-13 strings, Pitfall-6 offset-advance note, Plan 04 is_draining drain-mode subsection, and explicit cross-process out-of-scope marker.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN UAT — live Telegram verification of all 4 scenarios</name>
  <files>(none — human-only verification against live Telegram bot)</files>
  <action>Pause execution and present the runbook below to the user. Do not proceed until the user types "approved" (or returns a structured failure with screenshots + log excerpts).</action>
  <verify>Human approval via resume-signal — there is no automated check that can substitute for visually verifying Telegram message-rendering and reaction-rendering on a live bot.</verify>
  <done>User types "approved" in chat AND all four UAT scenarios from session_queue_telegram_uat.md are visually confirmed (D-13 ❌ + chat reply on cap-hit, depth-aware /queue reply, /new clears queue, Plan 04 is_draining drain-mode preservation, Pitfall-6 no-re-delivery).</done>
  <what-built>
    Phase 36.17.1 ships:
    - SessionQueue type with 128-message per-session FIFO cap (Plan 01)
    - GatewayRunner wiring + busy-branch enqueue + post-turn drain helper exposed as `pub(crate) async fn drain_pending` (Plan 02)
    - /queue slash command + /new and /reset clear hooks (Plan 03)
    - Drain-mode flag `is_draining` on GatewayRunner with shutdown-sequence integration (Plan 04)
    - End-to-end integration tests with RecordingPlatformAdapter, including direct exercise of `runner.drain_pending` (this plan, Task 1)
    - UAT runbook at crates/ironhermes-gateway/tests/session_queue_telegram_uat.md (this plan, Task 2)

    All automated tests pass: cargo test -p ironhermes-gateway, including proptest_equivalence (1024 cases), test_telegram_cap_hit_full_ux, test_telegram_post_turn_drain_in_arrival_order, drain-mode unit tests.

    What requires live verification: D-13's Telegram-specific UX (❌ reaction rendering + chat-reply visibility + getUpdates offset advance with no Telegram re-delivery) AND Plan 04's in-process drain-mode flag behavior on a real ctrl+c shutdown. Automated tests cannot verify reaction rendering or Telegram's re-delivery behavior — only a real Telegram bot can.
  </what-built>
  <how-to-verify>
    Follow the runbook at crates/ironhermes-gateway/tests/session_queue_telegram_uat.md.

    Quick path (minimum sufficient for sign-off):
    1. Start the gateway: `cargo run --release --bin ironhermes -- gateway` (with $TELEGRAM_BOT_TOKEN and $IRONHERMES_HOME set).
    2. In your test Telegram chat with the bot:
       a. Send a slow prompt: "Research the history of FIFO queues in detail and write 2000 words."
       b. While the agent is still generating (you should see the 👁 reaction on follow-ups), run Scenario 3 from the runbook: send /queue 128 times rapidly, then send a 129th /queue.
       c. Verify on the 129th: ❌ reaction appears AND a chat reply "⏳ Queue is full (128 messages). Wait for the agent to drain before sending more." appears.
       d. Wait ~30 seconds and verify the 129th message is NOT re-delivered (no duplicate ❌ reactions or processing).
    3. Run Scenario 4a (/new clears queue): enqueue 3 via /queue, run /new, verify "Conversation cleared. Starting fresh." appears AND the 3 queued messages do NOT replay.
    4. Run Scenario 4b (Plan 04 drain-mode): with the agent still busy and a few items queued, ctrl+c the gateway; tail the logs and confirm an is_draining=true line appears BEFORE the cancel-fired line.
    5. If all scenarios PASS visually: approve.
    6. If any scenario FAILS: document the failure (screenshot the chat + paste relevant log lines from $IRONHERMES_HOME/logs/web.log if available, or stdout) and reject.

    Tip: if you cannot generate 128 messages by hand, use the bot's /queue with a script: `for i in $(seq 1 129); do telegram-cli msg @your_test_bot "/queue test $i"; sleep 0.05; done` (adapt to your Telegram client tooling).
  </how-to-verify>
  <resume-signal>Type "approved" if all UAT scenarios pass, or describe the failure (with screenshots and logs) to reject.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real Telegram producer → ironhermes gateway | Live Telegram UAT exercises real network, real Bot API, real user identity. Cap-hit UX must render correctly and not cause infinite re-delivery. |
| Live IRONHERMES_HOME → process logs | UAT verification reads from logs to confirm tracing::warn lines fire on cap-hit and is_draining=true fires on shutdown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.17.1-01 | DoS | live Telegram cap-hit | mitigate | UAT Scenario 3 verifies cap is held at 128 even under live load; test_telegram_cap_hit_full_ux verifies the same at unit level. |
| T-36.17.1-02 | Information Disclosure | live multi-user UAT | accept | UAT runs in a single chat with the verifier's own user_id. Cross-user isolation is proven by test_session_isolation (Plan 01) + test_session_key_full_triple_used; a multi-user live test is not required for this phase's UAT. |
| T-36.17.1-04 | DoS via /queue flood | UAT Scenario 3 | mitigate | The scenario IS the flood — verifies the cap holds. Telegram-side rate limits (the protocol's natural ~20/sec/chat) protect the gateway from infinite flood velocity. |
| T-36.17.1-06 | Tampering | Telegram offset advance | mitigate | Runner.rs:513-519 advances offset BEFORE dispatch (Pitfall 6). UAT Scenario 3 step "verify 129th is not re-delivered" is the live evidence. If this regresses, the UAT will catch it before sign-off. |
| T-36.17.1-07 | Loss of in-flight events | Plan 04 drain-mode flag | mitigate | UAT Scenario 4b verifies the runner sets is_draining=true before cancel fires and that messages arriving in the drain window are pushed to session_queue rather than dropped. |
</threat_model>

<verification>
- cargo test -p ironhermes-gateway --test session_queue_integration green for the 3 new Telegram tests
- cargo test -p ironhermes-gateway full suite green
- UAT runbook exists with 4 scenarios + D-13 literals + Pitfall-6 + Plan-04 is_draining subsection + cross-process out-of-scope note
- Human verifier approves all 4 UAT scenarios on live Telegram
</verification>

<success_criteria>
End-to-end integration tests cover busy-enqueue silence, cap-hit UX (❌ reaction + chat reply, cap held at 128), and FIFO post-turn drain via `runner.drain_pending` (no merging, arrival order preserved). Manual UAT runbook documents 4 live-Telegram scenarios including Plan 04's in-process drain-mode flag. Human verifier signs off after exercising the runbook on a live Telegram bot.
</success_criteria>

<output>
Create `.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-05-SUMMARY.md` when done.
</output>
