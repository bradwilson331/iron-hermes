---
phase: 36.17.1
slug: in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-27
---

# Phase 36.17.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `36.17.1-RESEARCH.md` §"Validation Architecture".

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` + `cargo clippy` (Rust workspace); `proptest 1.x` for parity equivalence |
| **Config file** | `crates/ironhermes-gateway/Cargo.toml` (proptest added under `[dev-dependencies]` in Wave 0) |
| **Quick run command** | `cargo test -p ironhermes-gateway --lib session_queue::` |
| **Full suite command** | `cargo test -p ironhermes-gateway && cargo clippy -p ironhermes-gateway -- -D warnings` |
| **Estimated runtime** | ~30 seconds (unit + proptest defaults); ~60 seconds with `PROPTEST_CASES=1024` |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-gateway --lib session_queue::`
- **After every plan wave:** Run `cargo test -p ironhermes-gateway && cargo clippy -p ironhermes-gateway -- -D warnings`
- **Before `/gsd:verify-work`:** Full workspace `cargo test` must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

> Populated by `gsd-planner` in Step 8. Each task in each PLAN.md must reference a row here.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| (TBD by planner) | — | — | — | — | — | — | — | — | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Add `proptest = "1"` to `crates/ironhermes-gateway/Cargo.toml` `[dev-dependencies]` (RESEARCH §7 — not yet present in any Cargo.toml)
- [ ] Create `crates/ironhermes-gateway/src/session_queue.rs` stub with module declaration in `lib.rs` so `cargo test -p ironhermes-gateway --lib session_queue::` resolves before tasks run
- [ ] Create `crates/ironhermes-gateway/src/session_queue/parity_tests.rs` (or co-located `#[cfg(test)] mod parity` block) stub for `SplitSlotQueue` mirror + proptest scaffold

*If existing infrastructure covers some of these, the planner should remove the corresponding bullet.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Telegram cap-hit UX (`❌` reaction + chat reply) on real Telegram bot | D-13 | Requires a live Telegram bot token + chat; `PlatformAdapter::add_reaction` mocks cannot prove the reaction renders correctly in a real client | 1. Send 128 messages while agent is busy. 2. Send the 129th. 3. Verify the 129th gets a `❌` reaction and the bot replies with the "Queue is full" message. 4. Verify `getUpdates` offset advances past the dropped message. |
| Queue-during-drain preservation across a real gateway restart | D-03 | Drain mode hooks `Arc<AtomicBool>` + shutdown signal; in-process unit tests can prove the flag flips but cannot prove the next-spawned process picks up the queued events without a real restart | 1. Start gateway, send agent into long-running turn. 2. Send 3 messages while agent is busy (they queue). 3. SIGTERM gateway. 4. Restart gateway. 5. Verify the 3 queued messages replay in arrival order. (Note: D-03 explicitly preserves only across in-process drain, not cross-process — clarify expectation in test setup.) |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (proptest dep, session_queue.rs stub)
- [ ] No watch-mode flags (no `cargo watch`, no `--ignore-fail-fast` masking)
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter once planner populates rows

**Approval:** pending
