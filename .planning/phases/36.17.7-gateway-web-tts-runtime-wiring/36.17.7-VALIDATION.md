---
phase: 36.17.7
slug: gateway-web-tts-runtime-wiring
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-04
---

# Phase 36.17.7 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Lifted from `36.17.7-RESEARCH.md` §Validation Architecture (lines 985-1044).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in test (`#[test]`, `#[tokio::test]`) |
| **Config file** | `Cargo.toml` per-crate (no external test config) |
| **Quick run command** | `cargo test -p ironhermes-agent --test invariants_36_17_7` |
| **Full suite command** | `cargo test -p ironhermes-agent -p ironhermes-tools -p iron_hermes_ui -p ironhermes-gateway -p ironhermes-cli` |
| **Estimated runtime** | ~120 seconds full; <5 seconds invariant quick run |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-agent --test invariants_36_17_7`
- **After every plan wave:** Run `cargo test -p ironhermes-agent -p ironhermes-tools -p iron_hermes_ui -p ironhermes-gateway -p ironhermes-cli`
- **Before `/gsd:verify-work`:** Full suite must be green AND operator UAT-T, UAT-W, UAT-TUI, UAT-R confirmed
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

> Populated by planner; rows below are seeded from research §Validation Architecture and will be re-keyed to `{plan_id}-{task_id}` once plans are written.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | 1 | D-01 | — | `register_tts_tools` fires only when `tts_wiring: Some(...)` | unit | `cargo test -p ironhermes-agent -- tts_wiring_registers_tools` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | D-01 | — | `register_tts_tools` NOT fired when `tts_wiring: None` | unit | `cargo test -p ironhermes-agent -- tts_wiring_none_no_register` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | D-02-a | — | `ChatStreamEvent::AudioOut` serializes to external-tagged JSON | unit | `cargo test -p iron_hermes_ui -- test_audio_out_json_shape` | ❌ W0 | ⬜ pending |
| TBD | TBD | 2 | D-02-a | — | `WebAudioDispatcher::send_audio_file` emits `AudioOut` event on tx | unit | `cargo test -p iron_hermes_ui -- test_web_audio_dispatcher_emits` | ❌ W0 | ⬜ pending |
| TBD | TBD | 2 | D-02-c | T-path-traversal | `GET /audio/:uuid` returns 200 + mp3 bytes for valid uuid | integration | `cargo test -p iron_hermes_ui -- test_audio_route_200` | ❌ W0 | ⬜ pending |
| TBD | TBD | 2 | D-02-c | T-path-traversal | `GET /audio/:uuid` returns 404 for unknown uuid | integration | `cargo test -p iron_hermes_ui -- test_audio_route_404` | ❌ W0 | ⬜ pending |
| TBD | TBD | 2 | D-02-d | T-unbounded-disk | GC sweep deletes files older than `max_age_days` | unit | `cargo test -p iron_hermes_ui -- test_gc_sweep_removes_old_files` | ❌ W0 | ⬜ pending |
| TBD | TBD | 2 | D-02-d | T-unbounded-disk | GC sweep does NOT delete files newer than `max_age_days` | unit | `cargo test -p iron_hermes_ui -- test_gc_sweep_keeps_new_files` | ❌ W0 | ⬜ pending |
| TBD | TBD | 2 | D-02-d | — | GC sweep error does not panic when audio_cache dir missing | unit | `cargo test -p iron_hermes_ui -- test_gc_sweep_missing_dir_no_panic` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | D-03-b | — | `NotSupportedAudioDispatcher::send_audio_file` returns `Err` | unit | `cargo test -p ironhermes-tools -- test_not_supported_dispatcher_errors` | ❌ W0 | ⬜ pending |
| TBD | TBD | 3 | D-05 | — | `agent_runtime.rs` source no longer contains `session_key: None,` | source-grep | `cargo test -p ironhermes-agent --test invariants_36_17_7 -- agent_runtime_session_key_none_removed` | ❌ W0 | ⬜ pending |
| TBD | TBD | 3 | D-05 | — | gateway handler source contains `session_key: Some(` | source-grep | `cargo test -p ironhermes-agent --test invariants_36_17_7 -- gateway_handler_wires_session_key_some` | ❌ W0 | ⬜ pending |
| TBD | TBD | 3 | D-05 | — | `iron_hermes_ui/src/server/ws.rs` source contains `session_key: Some(` | source-grep | `cargo test -p ironhermes-agent --test invariants_36_17_7 -- web_ws_handler_wires_session_key_some` | ❌ W0 | ⬜ pending |
| TBD | TBD | 3 | D-05 | — | `tui_rata/event_loop.rs` source contains `session_key: Some(` | source-grep | `cargo test -p ironhermes-agent --test invariants_36_17_7 -- tui_event_loop_wires_session_key_some` | ❌ W0 | ⬜ pending |
| TBD | TBD | 3 | D-05 | — | `register_tts_tools(` call count ≥ 4 across production source | source-grep | `cargo test -p ironhermes-agent --test invariants_36_17_7 -- register_tts_tools_called_at_minimum_four_sites` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

All test files are new — none exist yet:

- [ ] `crates/ironhermes-agent/tests/invariants_36_17_7.rs` — D-05 source-grep guards (5 tests)
- [ ] `crates/ironhermes-agent/tests/tts_per_turn_wiring.rs` — D-01 unit tests for `TurnRequest::tts_wiring` → `register_tts_tools` invocation
- [ ] `crates/ironhermes-tools/tests/not_supported_dispatcher.rs` — D-03-b error return test
- [ ] `crates/iron_hermes_ui/tests/audio_out_protocol.rs` — `AudioOut` JSON shape lock test
- [ ] `crates/iron_hermes_ui/tests/web_audio_dispatcher.rs` — `WebAudioDispatcher` emit test
- [ ] `crates/iron_hermes_ui/tests/audio_route.rs` — HTTP replay route 200/404 + path-traversal tests
- [ ] `crates/iron_hermes_ui/tests/audio_gc.rs` — GC sweep mtime tests

*Framework already installed (Rust built-in test) — no install step required.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Telegram delivers audible voice message after `text_to_speech` + `send_audio` | A3 | Requires live Telegram bot + chat | UAT-T: `cd $WORKTREE && cargo run --bin hermes -- gateway start --telegram` → ask agent "summarize HN as a podcast in a female voice" → expect voice message in chat, NOT `say` fallback |
| iron_hermes_ui inline `<audio controls>` renders + plays | A4 | Requires browser + WS connection | UAT-W: start iron_hermes_ui server → open browser → same prompt → expect inline audio control in transcript, click play → audible playback |
| TUI session audible rodio playback on host | A4 (TUI variant) | Requires host audio | UAT-TUI: `cargo run --bin hermes -- chat` → same prompt → expect audible playback through host speakers |
| Page-reload replay of cached audio via `GET /audio/:uuid` | A4 + D-02-c | Requires browser refresh after UAT-W | UAT-R: after UAT-W, refresh browser, expand message → audio plays back from `/audio/:uuid` HTTP route, NOT WS bytes |
| `toolset list` shows `voice` as `enabled` AND `Registered: Live` for an active web session | A7 | Requires live session running while CLI inspects | UAT-Reg: start iron_hermes_ui session → in second terminal `cargo run --bin hermes -- toolset list` → expect new `Registered` column showing `Live` for `voice` |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (7 new test files seeded above)
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter (after planner re-keys Task IDs)

**Approval:** pending
