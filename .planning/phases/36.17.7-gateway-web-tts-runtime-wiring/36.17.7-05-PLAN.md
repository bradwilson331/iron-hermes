---
phase: 36.17.7
slug: gateway-web-tts-runtime-wiring
plan: 05
type: execute
wave: 3
depends_on:
  - 36.17.7-02
  - 36.17.7-03
  - 36.17.7-04
files_modified:
  - crates/ironhermes-core/src/config.rs
  - crates/iron_hermes_ui/src/server/audio_cache.rs
  - crates/iron_hermes_ui/src/server/audio_route.rs
  - crates/iron_hermes_ui/src/server/mod.rs
  - crates/iron_hermes_ui/src/server/state.rs
  - crates/ironhermes-tools/src/registry.rs
  - crates/ironhermes-core/src/commands/context.rs
  - crates/ironhermes-core/src/commands/handlers.rs
  - crates/ironhermes-cli/src/toolset_cmd.rs
  - crates/ironhermes-agent/tests/invariants_36_17_7.rs
  - crates/iron_hermes_ui/tests/audio_route.rs
  - crates/iron_hermes_ui/tests/audio_gc.rs
autonomous: false
requirements: [A5, A6, A7]
user_setup:
  - service: browser
    why: "UAT-R confirms page-reload replay via /audio/:uuid HTTP route after a prior UAT-W audio block was created"
    env_vars:
      - name: IRONHERMES_HOME
        source: "Operator-chosen path; same as Plan 04 UAT-W (recommend /tmp/uat-36.17.7-home)"
must_haves:
  truths:
    - "GET /audio/:uuid returns 200 + audio/mpeg bytes for a valid uuid file in $IRONHERMES_HOME/audio_cache/"
    - "GET /audio/:uuid returns 400/404 for malformed uuids and 404 for unknown uuids"
    - "Path traversal attempts are rejected by uuid format validation + canonicalize+starts_with"
    - "GC sweep on AppState init deletes files older than audio_cache.max_age_days"
    - "Periodic GC tokio task sweeps at audio_cache.sweep_interval_secs cadence"
    - "AudioCacheConfig field exists on Config with serde defaults max_age_days=7, sweep_interval_secs=86400"
    - "toolset list shows a Registered column with values Live / Inspection / — (REVISION BLOCKER 2: Path B implemented — Live state is reachable via the in-session /toolset slash dispatch threading through CommandContext.is_tts_registered_live)"
    - "ToolRegistry::is_tts_registered_live() accessor returns true iff a non-inspection session has registered text_to_speech + send_audio (REVISION BLOCKER 2)"
    - "cargo test -p ironhermes-agent --test invariants_36_17_7 passes 5/5 source-grep guards (D-05 / A5)"
    - "Operator UAT-R confirms audio replays after browser reload via /audio/:uuid"
    - "Operator UAT-Reg confirms BOTH the Inspection state (CLI) and the Live state (in-session /toolset list when an active web session is running)"
    - "No regression: hermes tts test/play still works; toolset list/show voice still shows 2/2 (A6)"
  artifacts:
    - path: "crates/ironhermes-core/src/config.rs"
      provides: "AudioCacheConfig { max_age_days: u32, sweep_interval_secs: u64 } + Config.audio_cache field"
      contains: "audio_cache: AudioCacheConfig"
    - path: "crates/iron_hermes_ui/src/server/audio_cache.rs"
      provides: "gc_sweep_audio_cache + run_audio_cache_gc_loop"
      exports: ["gc_sweep_audio_cache", "run_audio_cache_gc_loop"]
    - path: "crates/iron_hermes_ui/src/server/audio_route.rs"
      provides: "GET /audio/:uuid handler with T-path-traversal mitigations"
      exports: ["serve_audio"]
    - path: "crates/iron_hermes_ui/src/server/state.rs"
      provides: "AppState::init startup GC sweep + spawned periodic GC loop"
      contains: "gc_sweep_audio_cache"
    - path: "crates/ironhermes-tools/src/registry.rs"
      provides: "ToolRegistry::is_tts_registered_live() accessor (REVISION BLOCKER 2)"
      contains: "is_tts_registered_live"
    - path: "crates/ironhermes-core/src/commands/context.rs"
      provides: "CommandContext.tts_registration_status: Option<TtsRegistrationStatus> field threaded from in-session /toolset dispatch (REVISION BLOCKER 2)"
      contains: "tts_registration_status"
    - path: "crates/ironhermes-cli/src/toolset_cmd.rs"
      provides: "Registered column with Live/Inspection/— values; CLI inspection path shows Inspection; /toolset slash dispatch via CommandContext shows Live when a real session_key registered TTS"
      contains: "Registered"
    - path: "crates/ironhermes-agent/tests/invariants_36_17_7.rs"
      provides: "5 D-05 source-grep guards across agent_runtime.rs, handler.rs, ws.rs, event_loop.rs (A5)"
      contains: "session_key: None"
  key_links:
    - from: "GET /audio/:uuid"
      to: "$IRONHERMES_HOME/audio_cache/{uuid}.mp3"
      via: "uuid::Uuid::parse_str + audio_cache_dir.join + canonicalize + starts_with"
      pattern: "canonicalize"
    - from: "AppState::init"
      to: "gc_sweep_audio_cache + run_audio_cache_gc_loop"
      via: "tokio::spawn periodic loop with CancellationToken"
      pattern: "gc_sweep_audio_cache"
    - from: "/toolset slash dispatch via CommandContext"
      to: "ToolRegistry::is_tts_registered_live"
      via: "CommandContext.tts_registration_status field populated before dispatch (BLOCKER 2 Path B)"
      pattern: "is_tts_registered_live"
    - from: "invariants_36_17_7.rs"
      to: "production source files"
      via: "include_str! cross-crate source-grep"
      pattern: "include_str!\\(\"\\.\\./\\.\\./"
---

<objective>
Land the four remaining D-decisions, the A5 invariant, and the closing regression sweep:
- **D-02-c**: `GET /audio/:uuid` HTTP replay route (Task 2) — enables UAT-R browser reload replay.
- **D-02-d**: `AudioCacheConfig` + `gc_sweep_audio_cache` + periodic loop + state.rs init wiring (Tasks 1, 3, 4).
- **D-06 (REVISION BLOCKER 2 — Path B FULL implementation)**: `Registered` column showing Live / Inspection / —. Includes `ToolRegistry::is_tts_registered_live()` accessor + threading through `CommandContext` so the in-session `/toolset list` slash dispatch shows `Live` when a real session_key registered TTS (Task 5). The CLI inspection path (`hermes toolset list`) shows `Inspection`. Both states observable per CONTEXT D-06 — the operator-toggled enablement vs `register_tts_tools` actually fired distinction is preserved end-to-end.
- **D-05 / A5 (REVISION HIGH 5)**: `invariants_36_17_7.rs` 5 source-grep guards. Plan 01's `..Default::default()` rewrite of `agent_runtime.rs` makes Test 1 trivially GREEN (no narrowing needed).
- **A6**: Regression sweep — `hermes tts test/play` + `hermes toolset list voice 2/2` still pass.

REVISION fixes locked in frontmatter `requirements: [A5, A6, A7]` (HIGH 5 fix — A5 is the D-05 invariant whose owning test lives in Task 6 of this plan).

Output: 3 new server files (audio_cache.rs, audio_route.rs, both `#[cfg(feature = "server")]`), 1 new test file (invariants_36_17_7.rs), 1 modified state.rs (GC spawn), 1 modified config.rs (AudioCacheConfig), 1 modified registry.rs (BLOCKER 2 accessor), 2 modified core/commands files (BLOCKER 2 CommandContext thread), 1 modified toolset_cmd.rs (D-06 column), 2 Wave 0 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-CONTEXT.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-VALIDATION.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-02-SUMMARY.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-03-SUMMARY.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-04-SUMMARY.md
@crates/ironhermes-core/src/config.rs
@crates/iron_hermes_ui/src/server/state.rs
@crates/iron_hermes_ui/src/server/mod.rs
@crates/iron_hermes_ui/src/server/kanban_ws.rs
@crates/iron_hermes_ui/src/server/ws.rs
@crates/ironhermes-tools/src/registry.rs
@crates/ironhermes-core/src/commands/context.rs
@crates/ironhermes-core/src/commands/handlers.rs
@crates/ironhermes-cli/src/toolset_cmd.rs
@crates/ironhermes-agent/tests/invariants_36_17_5.rs
@crates/ironhermes-agent/src/agent_runtime.rs
@crates/ironhermes-gateway/src/handler.rs
@crates/ironhermes-cli/src/tui_rata/event_loop.rs

<interfaces>
<!-- Critical source-line citations and signatures. -->

Config struct pattern (config.rs:334-340):
```rust
#[serde(default)] pub autonomous: AutonomousConfig,
#[serde(default)] pub tools: ToolsConfig,
// ADD: #[serde(default)] pub audio_cache: AudioCacheConfig,
```

Kanban tail spawn pattern (state.rs:194-207):
```rust
let kanban_tail_cancel = tokio_util::sync::CancellationToken::new();
tokio::spawn(async move { run_kanban_tail_loop(tail_tx, tail_cancel, tail_interval_ms).await; });
```

Post-Plan-01 agent_runtime.rs (the literal `session_key: None,` no longer appears — Plan 01 rewrote `from_config` to use `..Default::default()`):
```rust
let input = AppRuntimeFactoryInput {
    config: config.clone(),
    resolver, cwd, process_registry, memory_manager,
    delegate_task, hooks_config, emit_mcp_startup_logs,
    ..Default::default()
};
```
This means D-05 Test 1 (`!SRC.contains("session_key: None,")` on `agent_runtime.rs`) is trivially GREEN — no narrowing needed.

ToolRegistry::is_tts_registered_live signature (REVISION BLOCKER 2 — Task 5 ships this):
```rust
// In crates/ironhermes-tools/src/registry.rs
impl ToolRegistry {
    /// Phase 36.17.7 D-06 / REVISION BLOCKER 2: returns true iff text_to_speech
    /// AND send_audio are currently registered AND the registration session_key is
    /// NOT the sentinel inspection key (Platform::Local, "inspect").
    pub fn is_tts_registered_live(&self) -> bool { /* see Task 5 */ }
}
```

CommandContext threading (REVISION BLOCKER 2):
```rust
// crates/ironhermes-core/src/commands/context.rs — add:
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum TtsRegistrationStatus { Live, Inspection, NotRegistered }

pub struct CommandContext {
    // ... existing fields ...
    pub tts_registration_status: Option<TtsRegistrationStatus>, // populated by the in-session /toolset dispatch site
}

impl CommandContext {
    pub fn with_tts_registration_status(mut self, status: TtsRegistrationStatus) -> Self { ... }
}
```

In-session `/toolset` dispatch site (REVISION BLOCKER 2 wire-up — likely in crates/ironhermes-core/src/commands/handlers.rs):
```rust
// Before dispatching to cmd_toolset_*, the in-session caller (agent loop, gateway handler,
// ws handler, tui event_loop) attaches the status by querying its own AgentRuntime's registry:
let status = if runtime.registry.read().await.is_tts_registered_live() {
    TtsRegistrationStatus::Live
} else if runtime.registry.read().await.get_definition("text_to_speech").is_some() {
    TtsRegistrationStatus::Inspection
} else {
    TtsRegistrationStatus::NotRegistered
};
let ctx = ctx.with_tts_registration_status(status);
```

Toolset display pattern (toolset_cmd.rs current columns):
```
voice  disabled  2  2/2 (text_to_speech ✓, send_audio ✓)
NAME   ENABLED   TOOL_COUNT  AVAILABLE
// Plan 05 BLOCKER 2 Path B: add Registered column → Live / Inspection / —
// CLI inspection path: always "Inspection" (sentinel key registers tools)
// In-session /toolset dispatch: "Live" when ctx.tts_registration_status == Some(Live)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AudioCacheConfig to Config + Wave 0 test scaffolding for audio_route.rs and audio_gc.rs</name>
  <files>crates/ironhermes-core/src/config.rs, crates/iron_hermes_ui/tests/audio_route.rs, crates/iron_hermes_ui/tests/audio_gc.rs</files>
  <read_first>
    - crates/ironhermes-core/src/config.rs (lines 301-380 — existing `pub tts: TtsConfig`, `pub tools: ToolsConfig`, `pub autonomous: AutonomousConfig`)
    - crates/iron_hermes_ui/tests/web_queue_protocol.rs (canonical source-grep test pattern)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/ironhermes-core/src/config.rs`" + §test_file_patterns)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 6)
  </read_first>
  <behavior>
    Config after this task:
      - New struct `pub struct AudioCacheConfig { pub max_age_days: u32, pub sweep_interval_secs: u64 }` adjacent to `ToolsConfig` / `AutonomousConfig`.
      - `#[derive(Debug, Clone, Serialize, Deserialize)]` and `#[serde(default)]` on the struct.
      - `impl Default for AudioCacheConfig { fn default() -> Self { Self { max_age_days: 7, sweep_interval_secs: 86400 } } }`.
      - `Config` gains `#[serde(default)] pub audio_cache: AudioCacheConfig,` adjacent to `pub tools: ToolsConfig`.
      - `cargo build -p ironhermes-core` exits 0.
      - Pre-existing config YAMLs (no `audio_cache:` section) parse cleanly via serde defaults.
    Wave 0 test files:
      - `crates/iron_hermes_ui/tests/audio_route.rs` contains:
        - `const SOURCE: &str = include_str!("../src/server/audio_route.rs");`
        - `#[test] audio_route_declared_as_get_handler`: `SOURCE.contains("/audio/:uuid")`.
        - `#[test] audio_route_validates_uuid_format`: `SOURCE.contains("Uuid::parse_str")` OR `SOURCE.contains("Uuid::from_str")`.
        - `#[test] audio_route_canonicalizes_and_checks_containment`: `SOURCE.contains("canonicalize")` AND `SOURCE.contains("starts_with")`.
        - `#[test] audio_route_returns_audio_mpeg_content_type`: `SOURCE.contains("audio/mpeg")`.
        - `#[test] audio_route_no_unwrap_or_panic`: `!SOURCE.contains(".unwrap()") && !SOURCE.contains("panic!")`.
      - `crates/iron_hermes_ui/tests/audio_gc.rs` contains:
        - `const SOURCE: &str = include_str!("../src/server/audio_cache.rs");` and `const STATE: &str = include_str!("../src/server/state.rs");`
        - `#[test] audio_gc_sync_sweep_fn_exists`: `SOURCE.contains("fn gc_sweep_audio_cache")`.
        - `#[test] audio_gc_async_loop_fn_exists`: `SOURCE.contains("async fn run_audio_cache_gc_loop")`.
        - `#[test] audio_gc_uses_cancellation_token`: `SOURCE.contains("cancel.cancelled()")` AND `SOURCE.contains("CancellationToken")`.
        - `#[test] audio_gc_never_panics`: `!SOURCE.contains(".unwrap()") && !SOURCE.contains("panic!")`.
        - `#[test] audio_gc_logs_errors_via_tracing`: `SOURCE.contains("tracing::warn!")`.
        - `#[test] audio_gc_startup_sweep_called_from_state_init`: `STATE.contains("gc_sweep_audio_cache")`.
        - `#[tokio::test] audio_gc_removes_files_older_than_max_age`: functional test using tempdir + mtime aging via `filetime` if available, else downgrade to source-grep with note in commit body.
        - `#[tokio::test] audio_gc_missing_dir_does_not_panic`: calls `gc_sweep_audio_cache(Path::new("/nonexistent/path/q9z"), 1)` — must not panic.
      - Both test files gated `#![cfg(feature = "server")]` matching Plan 04 Task 1 gating.
  </behavior>
  <action>
    Edit `crates/ironhermes-core/src/config.rs`:
      Step 1 — Locate `Config` struct (~line 301). Identify `pub tools: ToolsConfig`. Add directly below: `#[serde(default)] pub audio_cache: AudioCacheConfig,` with doc comment `/// Phase 36.17.7 D-02-d: audio cache lifecycle policy.`
      Step 2 — Declare `AudioCacheConfig` struct + `Default` impl alongside `ToolsConfig` / `AutonomousConfig`.
      Step 3 — If `default_config_matches_expectations` or similar tests assert specific defaults, append `audio_cache.max_age_days = 7` + `audio_cache.sweep_interval_secs = 86400`.

    Create `crates/iron_hermes_ui/tests/audio_route.rs`. Mirror `web_queue_protocol.rs`. All source-grep — depends on Task 2 module existence to compile.
    Create `crates/iron_hermes_ui/tests/audio_gc.rs`. Same pattern. Functional `#[tokio::test]` requires `audio_cache::gc_sweep_audio_cache` from Task 3. Imports: `use iron_hermes_ui::server::audio_cache::gc_sweep_audio_cache;` `use std::path::Path;` `use tempfile::TempDir;`. If `filetime` is required and absent from workspace, drop mtime-aging test and source-grep only.
    No new ironhermes-core or iron_hermes_ui external deps without justification in commit body.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-core 2>&amp;1 | tail -10 ; cargo test -p ironhermes-core --lib 2>&amp;1 | tail -10 ; cargo build -p iron_hermes_ui --tests --features server --no-run 2>&amp;1 | tail -20 ; grep -c "pub audio_cache: AudioCacheConfig" crates/ironhermes-core/src/config.rs ; grep -c "pub struct AudioCacheConfig" crates/ironhermes-core/src/config.rs ; grep -c "max_age_days: 7" crates/ironhermes-core/src/config.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub audio_cache: AudioCacheConfig" crates/ironhermes-core/src/config.rs` returns 1.
    - `grep -c "pub struct AudioCacheConfig" crates/ironhermes-core/src/config.rs` returns 1.
    - `grep -c "impl Default for AudioCacheConfig" crates/ironhermes-core/src/config.rs` returns 1.
    - `grep -c "max_age_days: 7" crates/ironhermes-core/src/config.rs` returns 1.
    - `grep -c "sweep_interval_secs: 86400" crates/ironhermes-core/src/config.rs` returns 1.
    - `grep -c "#\[test\]\|#\[tokio::test\]" crates/iron_hermes_ui/tests/audio_route.rs` returns ≥ 5.
    - `grep -c "#\[test\]\|#\[tokio::test\]" crates/iron_hermes_ui/tests/audio_gc.rs` returns ≥ 5.
    - `cargo build -p ironhermes-core` exits 0.
    - `cargo test -p ironhermes-core --lib` exits 0.
    - iron_hermes_ui tests may compile-fail only on missing `audio_route` / `audio_cache` modules.
  </acceptance_criteria>
  <done>Config gains AudioCacheConfig with serde defaults; Wave 0 tests scaffolded; ironhermes-core builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement audio_route.rs — GET /audio/:uuid with T-path-traversal mitigations</name>
  <files>crates/iron_hermes_ui/src/server/audio_route.rs, crates/iron_hermes_ui/src/server/mod.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/ws.rs (lines 88-91 — `#[get("/api/ws/chat")]` Dioxus fullstack handler)
    - crates/iron_hermes_ui/src/server/kanban_ws.rs (lines 61-65 — sibling `#[get]` pattern)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/iron_hermes_ui/src/server/audio_route.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 4, §Pitfall 4)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-CONTEXT.md (§Known Threat Patterns — T-path-traversal HIGH)
  </read_first>
  <behavior>
    After this task:
      - `crates/iron_hermes_ui/src/server/audio_route.rs` exists with `pub async fn serve_audio(uuid: String) -> impl IntoResponse`.
      - Server-feature gated.
      - Handler:
        - Validates `uuid` parses as `uuid::Uuid` (else `BAD_REQUEST`).
        - Resolves `audio_cache_dir = get_hermes_home().join("audio_cache")`.
        - Builds candidate path; `.canonicalize()` on the candidate. On `Err`, `NOT_FOUND`.
        - On `Ok(canon)`, `canon.starts_with(&audio_cache_dir)` check; if not, `FORBIDDEN`.
        - Reads bytes via `tokio::fs::read(&canon).await`; on Err returns `NOT_FOUND`.
        - On success returns `([(header::CONTENT_TYPE, "audio/mpeg")], bytes).into_response()`.
      - `mod.rs` declares `pub mod audio_route;`.
      - `cargo build -p iron_hermes_ui --features server` exits 0.
      - Wave 0 `audio_route.rs` tests 5/5 GREEN.
  </behavior>
  <action>
    Create `crates/iron_hermes_ui/src/server/audio_route.rs` per PATTERNS verbatim:
      - Imports: `use dioxus::prelude::*;` `#[cfg(feature = "server")] use axum::extract::Path;` `#[cfg(feature = "server")] use axum::response::{IntoResponse, Response};` `#[cfg(feature = "server")] use ironhermes_core::constants::get_hermes_home;` `use uuid::Uuid;`.
      - `#[get("/audio/:uuid")]` macro on `pub async fn serve_audio(uuid: String) -> impl IntoResponse`.
      - Body per PATTERNS: UUID parse → BAD_REQUEST; audio_cache_dir derivation; canonicalize → NOT_FOUND; starts_with guard → FORBIDDEN; tokio::fs::read → NOT_FOUND; Success: `([(axum::http::header::CONTENT_TYPE, "audio/mpeg")], bytes).into_response()`.
      - `#[cfg(not(feature = "server"))]` arm: `(axum::http::StatusCode::NOT_FOUND, "server feature required").into_response()`.
    Edit `crates/iron_hermes_ui/src/server/mod.rs`: add `pub mod audio_route;`.
    Threat mitigation literals must appear: `Uuid::parse_str` OR `Uuid::from_str`, `canonicalize`, `starts_with`, `audio/mpeg`.
    Do NOT add streaming Body (files < 1MB). Do NOT add explicit auth gate. Document auth-deferred decision in a comment.
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --features server 2>&amp;1 | tail -10 ; cargo test -p iron_hermes_ui --test audio_route --features server 2>&amp;1 | tail -20 ; grep -c "Uuid::parse_str\|Uuid::from_str" crates/iron_hermes_ui/src/server/audio_route.rs ; grep -c "canonicalize" crates/iron_hermes_ui/src/server/audio_route.rs ; grep -c "starts_with" crates/iron_hermes_ui/src/server/audio_route.rs ; grep -c "audio/mpeg" crates/iron_hermes_ui/src/server/audio_route.rs ; grep -c "pub mod audio_route" crates/iron_hermes_ui/src/server/mod.rs</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/iron_hermes_ui/src/server/audio_route.rs` exists.
    - `grep -c "pub async fn serve_audio" crates/iron_hermes_ui/src/server/audio_route.rs` returns 1.
    - `grep -c "Uuid::parse_str\|Uuid::from_str" crates/iron_hermes_ui/src/server/audio_route.rs` returns ≥ 1.
    - `grep -c "canonicalize" crates/iron_hermes_ui/src/server/audio_route.rs` returns ≥ 1.
    - `grep -c "starts_with" crates/iron_hermes_ui/src/server/audio_route.rs` returns ≥ 1.
    - `grep -c "audio/mpeg" crates/iron_hermes_ui/src/server/audio_route.rs` returns ≥ 1.
    - `grep -c ".unwrap()\|panic!" crates/iron_hermes_ui/src/server/audio_route.rs` returns 0.
    - `grep -c "pub mod audio_route" crates/iron_hermes_ui/src/server/mod.rs` returns 1.
    - `cargo build -p iron_hermes_ui --features server` exits 0.
    - `cargo test -p iron_hermes_ui --test audio_route --features server` exits 0; 5/5 GREEN.
    - `cargo clippy -p iron_hermes_ui --features server -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>GET /audio/:uuid handler ships with full T-path-traversal mitigations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement audio_cache.rs — gc_sweep_audio_cache + run_audio_cache_gc_loop</name>
  <files>crates/iron_hermes_ui/src/server/audio_cache.rs, crates/iron_hermes_ui/src/server/mod.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/kanban_ws.rs (lines 228-323 — `run_kanban_tail_loop` with `tokio::select!` + `CancellationToken`)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/iron_hermes_ui/src/server/audio_cache.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 5)
  </read_first>
  <behavior>
    After this task:
      - `crates/iron_hermes_ui/src/server/audio_cache.rs` exists with:
        - `pub fn gc_sweep_audio_cache(dir: &std::path::Path, max_age_days: u32)` — sync sweep; mtime check; remove old files via `std::fs::remove_file`. NotFound = no-op. Other read errors → `tracing::warn!`. Per-file remove errors → `tracing::warn!` and continue.
        - `pub async fn run_audio_cache_gc_loop(dir: PathBuf, max_age_days: u32, sweep_interval_secs: u64, cancel: CancellationToken)` — `tokio::select!` loop; sleep + sweep; exit on cancel.
      - All errors via `tracing::warn!` — NO unwrap, NO panic.
      - Server-feature gated.
      - `mod.rs` declares `pub mod audio_cache;`.
      - Wave 0 `audio_gc.rs` tests source-grep + functional GREEN.
  </behavior>
  <action>
    Create per PATTERNS verbatim. Imports: `std::fs`, `std::path::{Path, PathBuf}`, `std::time::{Duration, SystemTime}`, `tokio::time::sleep`, `tokio_util::sync::CancellationToken`.
    `gc_sweep_audio_cache` body:
      - `let cutoff = SystemTime::now() - Duration::from_secs(max_age_days as u64 * 86400);`
      - Match on `std::fs::read_dir(dir)`: Ok → iterate entries, read metadata + mtime, remove if old; remove Err → `tracing::warn!`. NotFound → no-op. Other Err → `tracing::warn!`.
    `run_audio_cache_gc_loop` body: `let interval = Duration::from_secs(sweep_interval_secs); loop { tokio::select! { _ = tokio::time::sleep(interval) => { gc_sweep_audio_cache(&dir, max_age_days); } _ = cancel.cancelled() => { tracing::info!("audio_cache GC loop cancelled"); break; } } }`
    Edit `mod.rs` add `pub mod audio_cache;`.
    Do NOT use `tokio::time::interval` or broadcast. Do NOT use async file I/O for sweep.
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --features server 2>&amp;1 | tail -10 ; cargo test -p iron_hermes_ui --test audio_gc --features server 2>&amp;1 | tail -20 ; grep -c "fn gc_sweep_audio_cache" crates/iron_hermes_ui/src/server/audio_cache.rs ; grep -c "async fn run_audio_cache_gc_loop" crates/iron_hermes_ui/src/server/audio_cache.rs ; grep -c "\.unwrap()\|panic!" crates/iron_hermes_ui/src/server/audio_cache.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn gc_sweep_audio_cache" crates/iron_hermes_ui/src/server/audio_cache.rs` returns 1.
    - `grep -c "async fn run_audio_cache_gc_loop" crates/iron_hermes_ui/src/server/audio_cache.rs` returns 1.
    - `grep -c "cancel.cancelled" crates/iron_hermes_ui/src/server/audio_cache.rs` returns ≥ 1.
    - `grep -c "tracing::warn!" crates/iron_hermes_ui/src/server/audio_cache.rs` returns ≥ 1.
    - `grep -c "\.unwrap()\|panic!" crates/iron_hermes_ui/src/server/audio_cache.rs` returns 0.
    - `grep -c "pub mod audio_cache" crates/iron_hermes_ui/src/server/mod.rs` returns 1.
    - `cargo build -p iron_hermes_ui --features server` exits 0.
    - `cargo test -p iron_hermes_ui --test audio_gc --features server` exits 0.
    - `cargo clippy -p iron_hermes_ui --features server -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>GC sweep functions ship; unit-tested. Ready for state.rs wire-up.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire startup GC sweep + periodic GC tokio task in AppState::init</name>
  <files>crates/iron_hermes_ui/src/server/state.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/state.rs (lines 172-220 — `AppState::init` and existing kanban tail spawn at 194-207)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/iron_hermes_ui/src/server/state.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 5)
  </read_first>
  <behavior>
    After this task:
      - `AppState` gains `pub audio_gc_cancel: tokio_util::sync::CancellationToken` adjacent to `kanban_tail_cancel`.
      - `AppState::init` calls sync startup sweep `gc_sweep_audio_cache(&audio_cache_dir, max_age_days);` before async work.
      - Then `tokio::spawn(async move { run_audio_cache_gc_loop(gc_dir, gc_max_age, gc_interval, gc_cancel_clone).await; });`.
      - Final `AppState { ... }` literal includes `audio_gc_cancel,`.
      - `cargo build -p iron_hermes_ui --features server` exits 0.
      - Source-grep `state.rs` contains `gc_sweep_audio_cache` (Wave 0 test GREEN).
  </behavior>
  <action>
    Edit `crates/iron_hermes_ui/src/server/state.rs`:
      Step 1 — Locate `AppState::init`. Note existing kanban tail spawn at 194-207.
      Step 2 — Add field `pub audio_gc_cancel: tokio_util::sync::CancellationToken,` to AppState adjacent to `kanban_tail_cancel`. Doc comment per PATTERNS.
      Step 3 — In init, after kanban spawn block: derive `audio_cache_dir` + `gc_max_age` + `gc_interval` from config. Call sync `gc_sweep_audio_cache(&audio_cache_dir, gc_max_age);`. Create `audio_gc_cancel + gc_cancel_clone + gc_dir`. `tokio::spawn(...)` the loop.
      Step 4 — In the final `AppState { ... }` struct literal, add `audio_gc_cancel,`.
    Do NOT add broadcast. Do NOT block.
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --features server 2>&amp;1 | tail -10 ; cargo test -p iron_hermes_ui --features server 2>&amp;1 | tail -10 ; grep -c "gc_sweep_audio_cache" crates/iron_hermes_ui/src/server/state.rs ; grep -c "run_audio_cache_gc_loop" crates/iron_hermes_ui/src/server/state.rs ; grep -c "audio_gc_cancel" crates/iron_hermes_ui/src/server/state.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gc_sweep_audio_cache" crates/iron_hermes_ui/src/server/state.rs` returns ≥ 1.
    - `grep -c "run_audio_cache_gc_loop" crates/iron_hermes_ui/src/server/state.rs` returns 1.
    - `grep -c "audio_gc_cancel" crates/iron_hermes_ui/src/server/state.rs` returns ≥ 2 (field + struct-literal init).
    - `grep -c "tokio::spawn" crates/iron_hermes_ui/src/server/state.rs` returns ≥ 2 (existing kanban + new GC).
    - `cargo build -p iron_hermes_ui --features server` exits 0.
    - `cargo test -p iron_hermes_ui --features server` exits 0.
    - `cargo test -p iron_hermes_ui --test audio_gc --features server -- audio_gc_startup_sweep_called_from_state_init` exits 0.
    - `cargo clippy -p iron_hermes_ui --features server -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>Startup sweep + periodic GC wired into iron_hermes_ui server boot path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: D-06 Path B FULL implementation (REVISION BLOCKER 2) — ToolRegistry::is_tts_registered_live accessor + CommandContext.tts_registration_status field + dispatch-site threading + toolset_cmd Registered column with Live/Inspection/— values</name>
  <files>crates/ironhermes-tools/src/registry.rs, crates/ironhermes-core/src/commands/context.rs, crates/ironhermes-core/src/commands/handlers.rs, crates/ironhermes-cli/src/toolset_cmd.rs</files>
  <read_first>
    - crates/ironhermes-tools/src/registry.rs (full file — the ToolRegistry struct, register_tts_tools, get_definition; understand the internal tools map structure)
    - crates/ironhermes-core/src/commands/context.rs (full file — CommandContext struct + existing builder methods; confirm field-add and builder-add idiom)
    - crates/ironhermes-core/src/commands/handlers.rs (full file — `handle_toolset_command` or `cmd_toolset_list` / `cmd_toolset_show` dispatch site; the in-session entrypoint that fires via `/toolset` slash; this is where the wire-up Lives → Inspection logic plugs in)
    - crates/ironhermes-cli/src/toolset_cmd.rs (full file — sentinel `register_tts_for_inspection` at lines 407-416, the toolset list display rows logic)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/ironhermes-cli/src/toolset_cmd.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 9)
    - .planning/phases/36.17.6-toolset-cli-tts-wiring/36.17.6-CONTEXT.md (inspection sentinel-key pattern — Path B must NOT collapse the two)
  </read_first>
  <behavior>
    After this task — three layers of changes:

    **Layer 1 (REVISION BLOCKER 2 — registry accessor) — `crates/ironhermes-tools/src/registry.rs`**:
      - `impl ToolRegistry` gains `pub fn is_tts_registered_live(&self) -> bool` returning true iff:
        1. `self.tools.contains_key("text_to_speech")` AND `self.tools.contains_key("send_audio")`, AND
        2. The current `SendAudioTool`'s embedded `session_key` is NOT the sentinel (`Platform::Local, "inspect"`).
      - The check on (2) requires `SendAudioTool` to expose its `session_key()` accessor (read-only borrow). If absent, add a `pub fn session_key(&self) -> &SessionKey` method to `SendAudioTool` in `crates/ironhermes-tools/src/send_audio_tool.rs`. Document in commit body.
      - Sentinel comparison: `session_key.platform == Platform::Local && session_key.chat_id == "inspect"`.
      - Add a `pub fn tts_registration_status(&self) -> TtsRegistrationStatus` helper that returns `Live` / `Inspection` / `NotRegistered` based on the same logic — useful for the CommandContext wire-up below.
      - Re-export `TtsRegistrationStatus` from `ironhermes-tools` crate root (`pub use registry::TtsRegistrationStatus;` in lib.rs) so downstream crates can reach it without a separate enum import. OR define the enum in `ironhermes-core` (Layer 2) and import it into `ironhermes-tools` — executor picks the cleaner crate-graph; default is `ironhermes-core::commands::context::TtsRegistrationStatus` declared in Layer 2, with `ironhermes-tools::registry` importing it for the accessor return type.
      - Add a unit test in `registry.rs`'s `mod tests` (or in a separate `tests/tts_registration_status.rs`) that:
        - Builds a fresh `ToolRegistry`, calls `register_tts_tools(SessionKey::new(Platform::Local, "inspect"), None, Arc::new(Config::default()))`, asserts `is_tts_registered_live() == false` AND `tts_registration_status() == Inspection`.
        - Builds another `ToolRegistry`, calls `register_tts_tools(SessionKey::new(Platform::Web, "session-uuid"), None, ...)`, asserts `is_tts_registered_live() == true` AND `tts_registration_status() == Live`.
        - Builds an empty `ToolRegistry`, asserts `tts_registration_status() == NotRegistered`.

    **Layer 2 (REVISION BLOCKER 2 — CommandContext threading) — `crates/ironhermes-core/src/commands/context.rs`**:
      - Declare `pub enum TtsRegistrationStatus { Live, Inspection, NotRegistered }` with `#[derive(Debug, Clone, Copy, PartialEq, Eq)]`.
      - Add `pub tts_registration_status: Option<TtsRegistrationStatus>` field to `CommandContext` (additive — Option keeps existing callers working).
      - Add builder method `pub fn with_tts_registration_status(mut self, status: TtsRegistrationStatus) -> Self { self.tts_registration_status = Some(status); self }`.
      - Update any test fixtures that build a `CommandContext` exhaustively — only if compile breaks; the Option means most build clean.

    **Layer 3 (REVISION BLOCKER 2 — dispatch-site wire-up) — `crates/ironhermes-core/src/commands/handlers.rs`**:
      - In `handle_toolset_command` (or whichever handler dispatches `/toolset list` / `/toolset show`), if a runtime is available (the in-session path), populate the status by querying the runtime's registry: `let status = runtime.bundle.registry.read().await.tts_registration_status(); let ctx = ctx.with_tts_registration_status(status);` before calling into `cmd_toolset_*`.
      - The CLI-inspection path (in `toolset_cmd.rs` cmd_toolset_list) does NOT attach a runtime — it constructs its own one-off registry via `register_tts_for_inspection`. The Layer 4 logic detects this absence and defaults to `Inspection`.

    **Layer 4 (REVISION BLOCKER 2 — toolset_cmd column) — `crates/ironhermes-cli/src/toolset_cmd.rs`**:
      - Add a `Registered` column to the display rows in `cmd_toolset_list` + `cmd_toolset_show`.
      - Value computation:
        - For the CLI-inspection path: ALWAYS `Inspection` (because `register_tts_for_inspection` always fires before display).
        - For the in-session `/toolset list` slash dispatch: the value comes from `ctx.tts_registration_status` — `Live`, `Inspection`, or `—` (NotRegistered).
        - For non-`voice` toolsets: `—`.
      - Update the column-formatting code so the new column aligns with existing `NAME / ENABLED / TOOL_COUNT / AVAILABLE`.
      - Add a unit test in toolset_cmd.rs's existing `#[cfg(test)] mod tests`: `registered_column_shows_inspection_for_voice_after_inspection_register`. Builds the inspection registry + retrieves the voice row + asserts `row.registered == "Inspection"`.
      - Add a second unit test: `registered_column_shows_live_when_command_context_carries_live_status`. Constructs a fake row-builder with `tts_registration_status: Some(Live)` and asserts `row.registered == "Live"`.

    `cargo build -p ironhermes-tools -p ironhermes-core -p ironhermes-cli` exits 0.
    `cargo test -p ironhermes-tools -p ironhermes-core -p ironhermes-cli` exits 0.
    Pre-existing 36.17.6 tests (`browser_in_known_set` length 9, `toolset_members_map_voice_entry`) still pass.
  </behavior>
  <action>
    Layer 1 — Edit `crates/ironhermes-tools/src/registry.rs`:

      **REVISION pass 2 MED-1 fix — Layer 1 pre-flight (read before Step 1).** The original Layer 1 design downcasts the tool entry via `Box<dyn Tool>` → `&SendAudioTool` using `dyn Any`, which requires `Tool: Any` as a supertrait. Adding `Any` to `Tool` propagates to every existing `impl Tool` in the workspace. Before committing to that path, run `grep -rn "impl.*Tool for\|impl Tool for" crates/ironhermes-tools/src crates/ironhermes-core/src crates/iron_hermes_ui/src crates/ironhermes-gateway/src crates/ironhermes-cli/src` and count implementors. Then choose:

      - **Path A (downcast via Any) — choose IF:** Tool already extends Any OR implementors count ≤ 3 and each can be cheaply updated with `as_any(&self) -> &dyn Any { self }`. Proceed with the original Steps 1-4.
      - **Path B (separate SessionKey slot — RECOMMENDED if Tool does NOT extend Any and implementors > 3):** Skip the downcast entirely. Add a new field `tts_session_key: Option<ironhermes_core::SessionKey>` on `ToolRegistry` directly. In `register_tts_tools`, set `self.tts_session_key = Some(session_key.clone())` alongside the existing `self.tools.insert(...)` calls. In `is_tts_registered_live` and `tts_registration_status`, the check becomes pure key-lookup: `self.tools.contains_key("text_to_speech") && self.tools.contains_key("send_audio") && self.tts_session_key.as_ref().is_some_and(|k| !(k.platform == Platform::Local && k.chat_id == "inspect"))`. Path B does NOT touch `SendAudioTool`, the `Tool` trait, or any other implementor — `files_modified` stays scoped to `registry.rs` only.

      Document Path A vs Path B choice in commit body. The 3 unit tests in Step 4 work identically for both paths — the assertions are about `is_tts_registered_live` / `tts_registration_status` return values, not about the internal mechanism.

      Step 1 — Locate `impl ToolRegistry`. Add `is_tts_registered_live` + `tts_registration_status` methods. Use the enum from Layer 2 (`ironhermes_core::commands::context::TtsRegistrationStatus`) — IF the crate-graph permits (verify `ironhermes-tools` already depends on `ironhermes-core` for `SessionKey` — it does per 36.17.5 wiring). If not, declare the enum in `ironhermes-tools/src/registry.rs` and have Layer 2 re-import.
      Step 2 — **Path A only:** If `SendAudioTool` doesn't expose `session_key()`, add a `pub fn session_key(&self) -> &SessionKey` accessor in `crates/ironhermes-tools/src/send_audio_tool.rs`. The `SendAudioTool` struct already holds the session_key per 36.17.5 Plan 03; no constructor change needed. **Path B:** skip this step — add `pub tts_session_key: Option<ironhermes_core::SessionKey>` field to `ToolRegistry` and a corresponding `Default::default()` init (`None`).
      Step 3 — **Path A only:** In `is_tts_registered_live`: cast the relevant tool entry to `&SendAudioTool` via the registry's internal type-erased dispatch. If the registry stores `Box<dyn Tool>`, expose a `pub fn get_send_audio_tool(&self) -> Option<&SendAudioTool>` accessor that downcasts via `dyn Any` (requires `Tool: Any` — if the trait doesn't already extend Any, add `Tool: std::any::Any` to the trait bound AND `as_any(&self) -> &dyn Any { self }` to every existing `impl Tool` enumerated in the pre-flight grep). **Path B:** in `register_tts_tools`, add a single line `self.tts_session_key = Some(session_key.clone());` adjacent to the existing tool-insertion calls. `is_tts_registered_live` then reads `self.tts_session_key` directly — no downcast.
      Step 4 — Add unit tests in `registry.rs` or a new `tests/tts_registration_status.rs`. Tests are path-agnostic.

    Layer 2 — Edit `crates/ironhermes-core/src/commands/context.rs`:
      Step 5 — Declare `TtsRegistrationStatus` enum with derives. Add `pub tts_registration_status: Option<TtsRegistrationStatus>` to CommandContext. Add `with_tts_registration_status` builder. Re-export `TtsRegistrationStatus` from `ironhermes_core::commands::*` and `ironhermes_core::*` if `commands` is re-exported at the crate root.

    Layer 3 — Edit `crates/ironhermes-core/src/commands/handlers.rs`:
      Step 6 — Locate the `/toolset` dispatch (likely a `handle_toolset_command` fn or a match arm in `handle_slash_command`). Before dispatching to `cmd_toolset_*`, if `ctx` carries a reference to the running `AgentRuntime` (or a similar accessor), populate `ctx = ctx.with_tts_registration_status(runtime.bundle.registry.read().await.tts_registration_status())`.
      Step 7 — If the runtime isn't reachable from this handler scope, document the integration gap in commit body and the executor identifies the per-platform wire-up site (gateway handler.rs, ws.rs, tui event_loop.rs each have access to their runtime). Plan 02/03/04 may need micro-edits to populate the status before delegating to `handle_toolset_command`; if so, identify those edits and apply them in this task as additional file modifications. Default scope is `commands/handlers.rs`; if cross-crate wire-up is needed, add `crates/ironhermes-gateway/src/handler.rs` + `crates/iron_hermes_ui/src/server/ws.rs` + `crates/ironhermes-cli/src/tui_rata/event_loop.rs` to this plan's `files_modified` via a SUMMARY annotation.

    Layer 4 — Edit `crates/ironhermes-cli/src/toolset_cmd.rs`:
      Step 8 — DO NOT modify `register_tts_for_inspection` at lines 407-416 (it must remain).
      Step 9 — Locate row-building logic. Add `registered: &'static str` field to `ToolsetRow` (or compute inline in the display loop).
      Step 10 — Computation: for `voice` row: `Live` if `ctx.tts_registration_status == Some(Live)`; `Inspection` if `ctx.tts_registration_status == Some(Inspection)` OR `ctx.tts_registration_status == None` AND `register_tts_for_inspection` fired (the CLI-inspection path); `—` if `ctx.tts_registration_status == Some(NotRegistered)`. For non-voice rows: `—`.
      Step 11 — Update the display formatter (println! / table format string) to include the new column. Verify 36.17.6 tests still pass.
      Step 12 — Add the 2 unit tests in &lt;behavior&gt;.

    Do NOT modify `register_tts_for_inspection` (sentinel key must remain). Do NOT change meaning of `enabled` / `disabled` columns.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-tools -p ironhermes-core -p ironhermes-cli 2>&amp;1 | tail -10 ; cargo test -p ironhermes-tools -p ironhermes-core -p ironhermes-cli --lib 2>&amp;1 | tail -30 ; grep -c "is_tts_registered_live" crates/ironhermes-tools/src/registry.rs ; grep -c "tts_registration_status" crates/ironhermes-core/src/commands/context.rs ; grep -c "TtsRegistrationStatus" crates/ironhermes-core/src/commands/context.rs ; grep -c "Registered" crates/ironhermes-cli/src/toolset_cmd.rs ; grep -c "Live\|Inspection" crates/ironhermes-cli/src/toolset_cmd.rs</automated>
  </verify>
  <acceptance_criteria>
    - **REVISION BLOCKER 2 lock (Layer 1): `grep -c "is_tts_registered_live" crates/ironhermes-tools/src/registry.rs` returns ≥ 1.**
    - **REVISION BLOCKER 2 lock (Layer 2): `grep -c "TtsRegistrationStatus" crates/ironhermes-core/src/commands/context.rs` returns ≥ 1.**
    - **REVISION BLOCKER 2 lock (Layer 2): `grep -c "tts_registration_status" crates/ironhermes-core/src/commands/context.rs` returns ≥ 1.**
    - **REVISION BLOCKER 2 lock (Layer 2): `grep -c "with_tts_registration_status" crates/ironhermes-core/src/commands/context.rs` returns 1.**
    - **REVISION BLOCKER 2 lock (Layer 3): `grep -c "tts_registration_status\|with_tts_registration_status" crates/ironhermes-core/src/commands/handlers.rs` returns ≥ 1.**
    - **REVISION BLOCKER 2 lock (Layer 4): `grep -c "Registered" crates/ironhermes-cli/src/toolset_cmd.rs` returns ≥ 1.**
    - **REVISION BLOCKER 2 lock (Layer 4): `grep -c "Inspection" crates/ironhermes-cli/src/toolset_cmd.rs` returns ≥ 1.**
    - **REVISION BLOCKER 2 lock (Layer 4): `grep -c "Live" crates/ironhermes-cli/src/toolset_cmd.rs` returns ≥ 1.**
    - `grep -c "registered_column_shows_inspection_for_voice" crates/ironhermes-cli/src/toolset_cmd.rs` returns 1.
    - `grep -c "registered_column_shows_live_when_command_context" crates/ironhermes-cli/src/toolset_cmd.rs` returns 1.
    - `cargo build -p ironhermes-tools -p ironhermes-core -p ironhermes-cli` exits 0.
    - `cargo test -p ironhermes-tools -p ironhermes-core -p ironhermes-cli` exits 0 — including the new BLOCKER 2 unit tests.
    - Pre-existing 36.17.6 tests pass.
    - `cargo clippy -p ironhermes-tools -p ironhermes-core -p ironhermes-cli -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>REVISION BLOCKER 2 Path B is FULLY implemented end-to-end: registry accessor + CommandContext field + dispatch-site threading + display column. Both `Inspection` (CLI) and `Live` (in-session) states are observable per D-06 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Author invariants_36_17_7.rs cross-crate source-grep test (D-05 / A5)</name>
  <files>crates/ironhermes-agent/tests/invariants_36_17_7.rs</files>
  <read_first>
    - crates/ironhermes-agent/tests/invariants_36_17_5.rs (canonical pattern)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/ironhermes-agent/tests/invariants_36_17_7.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 8 verified test bodies)
    - crates/ironhermes-agent/src/agent_runtime.rs (POST-Plan-01 — `session_key: None,` literal no longer appears thanks to `..Default::default()` rewrite)
    - crates/ironhermes-gateway/src/handler.rs (post-Plan-02 — `session_key: Some(` literal present)
    - crates/iron_hermes_ui/src/server/ws.rs (post-Plan-04 — `session_key: Some(` literal present)
    - crates/ironhermes-cli/src/tui_rata/event_loop.rs (post-Plan-03 — `session_key: Some(` literal present)
    - crates/ironhermes-agent/src/app_runtime_factory.rs (the `register_tts_tools(` call site)
  </read_first>
  <behavior>
    After this task:
      - `crates/ironhermes-agent/tests/invariants_36_17_7.rs` exists with the 5 tests per VALIDATION.md and PATTERNS:
        1. `agent_runtime_session_key_none_removed`: `assert!(!SRC.contains("session_key: None,"))` — TRIVIALLY GREEN per Plan 01 rewrite (no narrowing needed — REVISION BLOCKER 1 is fixed at Plan 01).
        2. `gateway_handler_wires_session_key_some`: `SRC.contains("session_key: Some(")` on `handler.rs`.
        3. `web_ws_handler_wires_session_key_some`: `SRC.contains("session_key: Some(")` on `ws.rs`.
        4. `tui_event_loop_wires_session_key_some`: `SRC.contains("session_key: Some(")` on `event_loop.rs`.
        5. `register_tts_tools_called_at_minimum_four_sites`: sum of `.matches("register_tts_tools(").count()` across 5 source files ≥ 4.
      - `cargo test -p ironhermes-agent --test invariants_36_17_7` exits 0; 5/5 GREEN.
  </behavior>
  <action>
    Create `crates/ironhermes-agent/tests/invariants_36_17_7.rs` per RESEARCH §Pattern 8 verbatim. Because Plan 01 rewrote `agent_runtime.rs::from_config` to use `..Default::default()`, Test 1 is straightforward: `assert!(!SRC.contains("session_key: None,"))`. No Path A/B reconciliation needed (the reconciliation was applied at Plan 01).

    Step 1 — Test 1: `const SRC: &str = include_str!("../src/agent_runtime.rs"); assert!(!SRC.contains("session_key: None,"), "Phase 36.17.7 D-05 Test 1: agent_runtime.rs must not contain the literal `session_key: None,` — Plan 01's ..Default::default() rewrite of from_config should have eliminated it.")`.
    Step 2 — Test 2: `const SRC: &str = include_str!("../../ironhermes-gateway/src/handler.rs"); assert!(SRC.contains("session_key: Some("), "...")`.
    Step 3 — Test 3: `const SRC: &str = include_str!("../../iron_hermes_ui/src/server/ws.rs"); assert!(SRC.contains("session_key: Some("), "...")`.
    Step 4 — Test 4: `const SRC: &str = include_str!("../../ironhermes-cli/src/tui_rata/event_loop.rs"); assert!(SRC.contains("session_key: Some("), "...")`.
    Step 5 — Test 5: build `&[(&str, &str)]` of `(name, include_str!(...))` for the 5 source files; sum `.matches("register_tts_tools(").count()`; assert `count >= 4`.
    Step 6 — Add module docstring explaining D-05 contract.

    Do NOT target `app_runtime_factory.rs` with the negative assert (Pitfall 7). Do NOT modify production sources.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-agent --test invariants_36_17_7 2>&amp;1 | tail -30 ; grep -c "include_str!" crates/ironhermes-agent/tests/invariants_36_17_7.rs ; grep -c "../../ironhermes-gateway/src/handler.rs\|../../iron_hermes_ui/src/server/ws.rs\|../../ironhermes-cli/src/tui_rata/event_loop.rs" crates/ironhermes-agent/tests/invariants_36_17_7.rs</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/ironhermes-agent/tests/invariants_36_17_7.rs` exists.
    - `grep -c "#\[test\]" crates/ironhermes-agent/tests/invariants_36_17_7.rs` returns ≥ 5.
    - `grep -c "include_str!" crates/ironhermes-agent/tests/invariants_36_17_7.rs` returns ≥ 5.
    - `grep -c "../../ironhermes-gateway/src/handler.rs" crates/ironhermes-agent/tests/invariants_36_17_7.rs` returns ≥ 1.
    - `grep -c "../../iron_hermes_ui/src/server/ws.rs" crates/ironhermes-agent/tests/invariants_36_17_7.rs` returns ≥ 1.
    - `grep -c "../../ironhermes-cli/src/tui_rata/event_loop.rs" crates/ironhermes-agent/tests/invariants_36_17_7.rs` returns ≥ 1.
    - `cargo test -p ironhermes-agent --test invariants_36_17_7` exits 0; 5/5 GREEN.
    - `cargo test -p ironhermes-agent --test invariants_36_17_5` exits 0 (no regression).
  </acceptance_criteria>
  <done>D-05 invariant guard live (A5 satisfied). The phase's cross-crate source-grep contract is locked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Operator UAT-R + UAT-Reg (BOTH Inspection AND Live per REVISION BLOCKER 2) + A6 regression sweep</name>
  <what-built>
    Plan 05 Tasks 1-6 completed:
    - `Config.audio_cache` + AudioCacheConfig defaults (Task 1).
    - `GET /audio/:uuid` with T-path-traversal mitigations (Task 2).
    - `gc_sweep_audio_cache` + `run_audio_cache_gc_loop` (Task 3).
    - AppState::init startup sweep + periodic GC task (Task 4).
    - **REVISION BLOCKER 2 Path B FULL**: `ToolRegistry::is_tts_registered_live` accessor + `CommandContext.tts_registration_status` field + dispatch-site threading + `toolset list` Registered column with Live/Inspection/— (Task 5).
    - `invariants_36_17_7.rs` 5 D-05 source-grep guards GREEN (Task 6 — REVISION BLOCKER 1 is locked at Plan 01).
  </what-built>
  <how-to-verify>
    Three sub-UATs run in sequence; checkpoint approval requires ALL three.

    Pre-flight:
      1. Ensure `$IRONHERMES_HOME` set (same as Plans 02-04 UATs).
      2. Run `cargo test --workspace 2>&1 | tail -30` and confirm `0 failed` outside the 2 pre-existing kanban end_to_end failures per STATE.md.

    UAT-R — page-reload audio replay:
      1. Start iron_hermes_ui server: `cargo run -p iron_hermes_ui --features server`.
      2. Re-run Plan 04 UAT-W: ask agent to synthesize HN podcast audio. Confirm inline `<audio controls>` plays audibly via Blob URL (per REVISION HIGH 4).
      3. Refresh the browser tab. Wait for transcript to reload.
      4. Locate previous audio block. The `<audio>` src now resolves to `/audio/{uuid}` (Blob URL is session-scoped).
      5. Click play on the reloaded `<audio>`. Confirm audible replay.
      6. Confirm via browser devtools network tab: `GET /audio/{uuid}` returned 200 + `Content-Type: audio/mpeg`.
      7. Path-traversal sanity: attempt `GET /audio/..%2F..%2Fetc%2Fpasswd` — must return 400 (invalid uuid). Document the exact response status.

    UAT-Reg (REVISION BLOCKER 2 — BOTH states observable):
      Sub-step A (Inspection state):
        1. Run `cargo run -p ironhermes-cli --bin hermes -- toolset list`.
        2. Confirm `Registered` column header appears.
        3. For `voice` row, value reads `Inspection` (no active session — CLI inspection sentinel).
        4. Run `cargo run -p ironhermes-cli --bin hermes -- toolset show voice`. Confirm detail view also shows Registered.

      Sub-step B (Live state — REVISION BLOCKER 2 contract):
        5. Start an iron_hermes_ui session in a browser (server still running from UAT-R). Confirm session is active (agent responded to at least one message).
        6. In the same browser session OR a parallel session, send the slash command `/toolset list` AS the agent (via chat input). The reply renders inside the chat transcript.
        7. For `voice` row in the in-session output, value reads `Live` (the WebAudioDispatcher session_key (Web, ...) is NOT the sentinel).
        8. If the in-session `/toolset list` is not yet wired to render rows in the chat transcript (executor's discretion choice), alternative: open a TUI session via `cargo run -- chat`, type `/toolset list` inside TUI, confirm `voice` row shows `Live`.
        9. If neither (b) nor (c) is reachable in this phase budget, document the gap in resume-signal and mark UAT-Reg as "Inspection observed; Live observation deferred to next phase" — coordinator may accept this partial closure but BLOCKER 2 Path B contract is then only structurally complete (registry + context + display column ship but operator hasn't witnessed Live).

    A6 regression sweep:
      1. `cargo run -p ironhermes-cli --bin hermes -- tts test`. Confirm 36.17.5 CLI TTS test passes.
      2. `cargo run -p ironhermes-cli --bin hermes -- tts play 'Hello from regression check'`. Confirm host playback (rodio).
      3. Confirm `toolset list` voice row reads `2 tools 2/2 (text_to_speech ✓, send_audio ✓)` — 36.17.6 invariant.

    Failure modes:
      - UAT-R Step 5 silent → `/audio/{uuid}` 404 — check audio_cache_dir resolution.
      - UAT-R Step 7 returns 200 for path traversal → HIGH SEVERITY — fix before proceeding.
      - UAT-Reg Sub-step A `Inspection` missing → Layer 4 display formatter bug.
      - UAT-Reg Sub-step B `Live` missing despite active session → Layer 3 dispatch-site wire-up bug; CommandContext not being populated before slash dispatch.
      - A6 Step 1 fails → unrelated 36.17.5 regression; investigate.
  </how-to-verify>
  <resume-signal>Type "approved" if UAT-R Steps 5+6+7 GREEN, UAT-Reg shows BOTH `Inspection` (sub-A) AND `Live` (sub-B) per REVISION BLOCKER 2 contract, AND A6 regression sweep clean. If `Live` observation is deferred per UAT-Reg sub-step 9, type "approved-partial-blocker2-live-deferred". Else describe failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /audio/:uuid | Untrusted client-supplied uuid crosses here. Path traversal is the canonical HIGH risk. |
| AppState::init → audio_cache disk | Startup sweep + periodic GC do mass deletes bounded to `$IRONHERMES_HOME/audio_cache/`. |
| toolset_cmd CLI inspection registry → operator stdout | Display data only. |
| ToolRegistry::is_tts_registered_live (downcast through Tool trait Any) | Internal API; no user input crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-path-traversal | Tampering | audio_route.rs (HIGH) | mitigate | Three-layer defense: (a) `uuid::Uuid::parse_str`, (b) `canonicalize`, (c) `starts_with(audio_cache_dir)`. UAT-R Step 7 verifies. |
| T-unbounded-disk | Denial of Service | audio_cache (MEDIUM) | mitigate | Task 3 GC + Task 4 startup+periodic wire-up. Errors → `tracing::warn!`. |
| T-info-disclosure-via-uuid | Information Disclosure | uuid v4 guessability (LOW) | accept | 122 bits of randomness; single-operator deployment. |
| T-36.17.7-05-01 | Tampering | Tool trait Any downcast (REVISION BLOCKER 2) | mitigate | If `Tool: Any` is added to enable downcast, the downcast site is restricted to in-crate (`registry.rs`) — no external caller can use this hook to substitute a tool. |
| T-36.17.7-05-SC | Tampering | cargo deps | mitigate | Zero new external deps. All workspace-existing. |
</threat_model>

<verify_plan>
1. `cargo build -p ironhermes-core -p iron_hermes_ui -p ironhermes-cli -p ironhermes-tools -p ironhermes-agent --features server` exits 0.
2. `cargo test --workspace 2>&1 | tail -20` shows 0 NEW failures.
3. `cargo test -p ironhermes-agent --test invariants_36_17_7` exits 0; 5/5 GREEN.
4. `cargo test -p ironhermes-agent --test invariants_36_17_5` exits 0.
5. `cargo test -p iron_hermes_ui --test audio_route --features server` exits 0; 5/5 GREEN.
6. `cargo test -p iron_hermes_ui --test audio_gc --features server` exits 0.
7. `cargo clippy --workspace --features server -- -D warnings` exits 0.
8. `grep -c "audio_cache: AudioCacheConfig" crates/ironhermes-core/src/config.rs` returns 1.
9. `grep -c "Uuid::parse_str\|Uuid::from_str" crates/iron_hermes_ui/src/server/audio_route.rs` returns ≥ 1.
10. `grep -c "canonicalize" crates/iron_hermes_ui/src/server/audio_route.rs` returns ≥ 1.
11. `grep -c "starts_with" crates/iron_hermes_ui/src/server/audio_route.rs` returns ≥ 1.
12. `grep -c "fn gc_sweep_audio_cache\|async fn run_audio_cache_gc_loop" crates/iron_hermes_ui/src/server/audio_cache.rs` returns 2.
13. **REVISION BLOCKER 2 lock: `grep -c "is_tts_registered_live" crates/ironhermes-tools/src/registry.rs` returns ≥ 1.**
14. **REVISION BLOCKER 2 lock: `grep -c "TtsRegistrationStatus" crates/ironhermes-core/src/commands/context.rs` returns ≥ 1.**
15. **REVISION BLOCKER 2 lock: `grep -c "with_tts_registration_status\|tts_registration_status" crates/ironhermes-core/src/commands/handlers.rs` returns ≥ 1.**
16. **REVISION BLOCKER 2 lock: `grep -c "Registered" crates/ironhermes-cli/src/toolset_cmd.rs` returns ≥ 1.**
17. UAT-R + UAT-Reg (BOTH Inspection AND Live) + A6 regression sweep approved by operator.
</verify_plan>

<success_criteria>
- D-02-c HTTP replay route ships with full T-path-traversal mitigations.
- D-02-d audio cache GC sweeps on startup and periodically.
- **REVISION BLOCKER 2 Path B FULL**: `ToolRegistry::is_tts_registered_live` + `CommandContext.tts_registration_status` + dispatch threading + `Registered` column show BOTH Inspection (CLI) and Live (in-session) states per D-06 contract.
- D-05 / A5: `invariants_36_17_7.rs` GREEN.
- A6 no regression: 36.17.5 TTS CLI + 36.17.6 toolset 2/2.
- A7 toolset list shows new Registered column.
- Operator-confirmed page-reload audio replay (UAT-R).
- Zero new external dependencies.
</success_criteria>

<output>
Create `.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-05-SUMMARY.md` documenting:
- 11 files modified/created (incl. Wave 0 tests + invariants test + BLOCKER 2 registry/context/handlers wire-up)
- REVISION BLOCKER 2 verification: Path B implementation status (which layers landed cleanly vs which deferred); UAT-Reg observation of both Inspection and Live states
- REVISION HIGH 5 confirmation: A5 covered by Task 6 invariants_36_17_7.rs + Plan 01 prep; frontmatter `requirements: [A5, A6, A7]` correct
- D-05 Test 1 verification: trivially GREEN per Plan 01 `..Default::default()` rewrite
- Source-grep gate counts from &lt;verify_plan&gt;
- UAT-R transcript + path-traversal 400 evidence
- UAT-Reg transcript: Inspection (sub-A) + Live (sub-B) observations
- A6 regression sweep results
- A1..A7 coverage confirmation across all 5 phase plans
- must_haves achieved: all 12 truths; all 8 artifacts; all 4 key_links
</output>
