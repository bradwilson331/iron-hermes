---
phase: 36.17.7
slug: gateway-web-tts-runtime-wiring
plan: 05
type: execute
wave: 3
depends_on:
  - 36.17.7-02
  - 36.17.7-03
  - 36.17.7-04
files_modified:
  - crates/ironhermes-core/src/config.rs
  - crates/iron_hermes_ui/src/server/audio_cache.rs
  - crates/iron_hermes_ui/src/server/audio_route.rs
  - crates/iron_hermes_ui/src/server/mod.rs
  - crates/iron_hermes_ui/src/server/state.rs
  - crates/ironhermes-cli/src/toolset_cmd.rs
  - crates/ironhermes-agent/tests/invariants_36_17_7.rs
  - crates/iron_hermes_ui/tests/audio_route.rs
  - crates/iron_hermes_ui/tests/audio_gc.rs
autonomous: false
requirements: [A6, A7]
user_setup:
  - service: browser
    why: "UAT-R confirms page-reload replay via /audio/:uuid HTTP route after a prior UAT-W audio block was created"
    env_vars:
      - name: IRONHERMES_HOME
        source: "Operator-chosen path; same as Plan 04 UAT-W (recommend /tmp/uat-36.17.7-home)"
must_haves:
  truths:
    - "GET /audio/:uuid returns 200 + audio/mpeg bytes for a valid uuid file in $IRONHERMES_HOME/audio_cache/"
    - "GET /audio/:uuid returns 400/404 for malformed uuids and 404 for unknown uuids"
    - "Path traversal attempts (e.g. /audio/../../../etc/passwd) are rejected by uuid format validation + canonicalize+starts_with guard"
    - "GC sweep on AppState init deletes files older than audio_cache.max_age_days from $IRONHERMES_HOME/audio_cache/"
    - "Periodic GC tokio task sweeps at audio_cache.sweep_interval_secs cadence"
    - "AudioCacheConfig field exists on Config with serde defaults max_age_days=7, sweep_interval_secs=86400"
    - "toolset list shows a Registered column with values Live / Inspection / — that reflects per-session registration state (D-06)"
    - "cargo test -p ironhermes-agent --test invariants_36_17_7 passes 5/5 source-grep guards (D-05)"
    - "Operator UAT-R confirms audio replays after browser reload via /audio/:uuid"
    - "No regression: hermes tts test/play still works (A6 / 36.17.5); hermes toolset list/show voice still shows 2/2 (A6 / 36.17.6)"
  artifacts:
    - path: "crates/ironhermes-core/src/config.rs"
      provides: "AudioCacheConfig { max_age_days: u32, sweep_interval_secs: u64 } + Config.audio_cache field with #[serde(default)]"
      contains: "audio_cache: AudioCacheConfig"
    - path: "crates/iron_hermes_ui/src/server/audio_cache.rs"
      provides: "gc_sweep_audio_cache(dir, max_age_days) sync sweep + run_audio_cache_gc_loop async periodic task"
      exports: ["gc_sweep_audio_cache", "run_audio_cache_gc_loop"]
    - path: "crates/iron_hermes_ui/src/server/audio_route.rs"
      provides: "GET /audio/:uuid handler with uuid::Uuid::parse_str gate + canonicalize+starts_with path-containment + audio/mpeg response"
      exports: ["serve_audio"]
    - path: "crates/iron_hermes_ui/src/server/state.rs"
      provides: "AppState::init startup GC sweep + spawned periodic GC loop + audio_gc_cancel field"
      contains: "gc_sweep_audio_cache"
    - path: "crates/ironhermes-cli/src/toolset_cmd.rs"
      provides: "Registered column in toolset list display + Live/Inspection/— values per D-06 (CLI-inspection path always shows Inspection)"
      contains: "Registered"
    - path: "crates/ironhermes-agent/tests/invariants_36_17_7.rs"
      provides: "5 D-05 source-grep guards across agent_runtime.rs, handler.rs, ws.rs, event_loop.rs"
      contains: "session_key: None"
  key_links:
    - from: "GET /audio/:uuid"
      to: "$IRONHERMES_HOME/audio_cache/{uuid}.mp3"
      via: "uuid::Uuid::parse_str + audio_cache_dir.join + canonicalize + starts_with"
      pattern: "canonicalize"
    - from: "AppState::init"
      to: "gc_sweep_audio_cache + run_audio_cache_gc_loop"
      via: "tokio::spawn periodic loop with CancellationToken"
      pattern: "gc_sweep_audio_cache"
    - from: "invariants_36_17_7.rs"
      to: "production source files"
      via: "include_str! cross-crate source-grep asserts"
      pattern: "include_str!\\(\"\\.\\./\\.\\./"
---

<objective>
Land the four remaining D-decisions and close the verification surface:
- D-02-c: `GET /audio/:uuid` HTTP replay route in iron_hermes_ui axum router (Task 2) — enables UAT-R browser reload replay.
- D-02-d: `audio_cache.max_age_days` + `audio_cache.sweep_interval_secs` in `Config`; `gc_sweep_audio_cache` + `run_audio_cache_gc_loop` in iron_hermes_ui (Tasks 1 + 3 + 4).
- D-06: `Registered` column in `toolset list` showing Live / Inspection / — (Task 5).
- D-05: New `invariants_36_17_7.rs` cross-crate source-grep test that asserts `agent_runtime.rs` lost `session_key: None,` AND `handler.rs`, `ws.rs`, `event_loop.rs` each contain `session_key: Some(` AND `register_tts_tools(` appears ≥ 4 times across production source (Task 6).
- A6: No regression in 36.17.5 `hermes tts test/play` or 36.17.6 `hermes toolset list/show voice` 2/2 ✓ (Task 7 full-suite gate).

Plan 05 is the closing surface. UAT-R operator step confirms the cache-replay path is live. UAT-Reg confirms the D-06 column reads correctly in a real session.

Output: 3 new server files (audio_cache.rs, audio_route.rs gated `#[cfg(feature = "server")]`, no change to legacy-shell), 1 new test file (invariants_36_17_7.rs), 2 modified server files (state.rs for GC spawn, mod.rs for route + cache module declarations), 1 modified config file (config.rs), 1 modified CLI file (toolset_cmd.rs), 2 Wave 0 test files (audio_route.rs + audio_gc.rs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-CONTEXT.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-VALIDATION.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-02-SUMMARY.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-03-SUMMARY.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-04-SUMMARY.md
@crates/ironhermes-core/src/config.rs
@crates/iron_hermes_ui/src/server/state.rs
@crates/iron_hermes_ui/src/server/mod.rs
@crates/iron_hermes_ui/src/server/kanban_ws.rs
@crates/iron_hermes_ui/src/server/ws.rs
@crates/ironhermes-cli/src/toolset_cmd.rs
@crates/ironhermes-agent/tests/invariants_36_17_5.rs
@crates/ironhermes-agent/src/agent_runtime.rs
@crates/ironhermes-gateway/src/handler.rs
@crates/ironhermes-cli/src/tui_rata/event_loop.rs

<interfaces>
<!-- Critical source-line citations and signatures — lifted from PATTERNS so executor doesn't search. -->

Config struct pattern to mirror (config.rs:334-340 — peer-level fields with #[serde(default)]):
```rust
#[serde(default)]
pub autonomous: AutonomousConfig,
#[serde(default)]
pub tools: ToolsConfig,
// ADD: #[serde(default)] pub audio_cache: AudioCacheConfig,
```

Kanban tail spawn pattern (state.rs:194-207 — the canonical periodic-task pattern):
```rust
let kanban_tail_cancel = tokio_util::sync::CancellationToken::new();
tokio::spawn(async move {
    run_kanban_tail_loop(tail_tx, tail_cancel, tail_interval_ms).await;
});
```

Audio cache directory derivation (RESEARCH §Pattern 5 + §Pattern 6):
```rust
let audio_cache_dir = ironhermes_core::constants::get_hermes_home().join("audio_cache");
```

D-05 negative-assert target — current production line that this phase makes false (agent_runtime.rs:213-217):
```rust
// Phase 36.17.5 D-15: per-turn threading deferred to a follow-up phase.
session_key: None,        // ← D-05 negative-assert targets this literal
telegram_adapter: None,
```
**IMPORTANT** — these lines belong to `from_config`'s `build_app_runtime_bundle` call site (the startup, single durable bundle). They MUST stay `None` semantically (startup bundle has no TTS — the TTS registration happens per-turn inside `run_turn` per Plan 01). The D-05 negative assert in Task 6 below greps `agent_runtime.rs` for the literal `session_key: None,`. After Plan 01 changed run_turn to call register_tts_tools per-turn, the production durable-bundle call at line 215 STILL hard-codes `session_key: None` — that is correct semantically (the bundle is per-process, not per-turn). The D-05 invariant therefore must NOT assert absence of `session_key: None,` in `agent_runtime.rs` if Plan 01's design left that line in place. RECONCILE THIS in Task 6 (see Task 6 reconciliation note).

Existing invariants_36_17_5.rs (the include_str! cross-crate pattern to mirror):
```rust
const SRC: &str = include_str!("../src/app_runtime_factory.rs");
#[test] fn ... { assert!(SRC.contains("registry.register_tts_tools(")); }
```

Toolset display pattern (toolset_cmd.rs current columns from 36.17.6 VERIFICATION):
```
voice  disabled  2  2/2 (text_to_speech ✓, send_audio ✓)
NAME   ENABLED   TOOL_COUNT  AVAILABLE
// Plan 05 D-06: add Registered column → values: Live, Inspection, —
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AudioCacheConfig to Config + Wave 0 test scaffolding for audio_route.rs and audio_gc.rs</name>
  <files>crates/ironhermes-core/src/config.rs, crates/iron_hermes_ui/tests/audio_route.rs, crates/iron_hermes_ui/tests/audio_gc.rs</files>
  <read_first>
    - crates/ironhermes-core/src/config.rs (lines 301-380 — must see existing `pub tts: TtsConfig`, `pub tools: ToolsConfig`, `pub autonomous: AutonomousConfig` peer-level fields with `#[serde(default)]`)
    - crates/iron_hermes_ui/tests/web_queue_protocol.rs (the canonical source-grep test pattern)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/ironhermes-core/src/config.rs`" + §test_file_patterns)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 6 Config Key Placement)
  </read_first>
  <behavior>
    Config after this task:
      - New struct `pub struct AudioCacheConfig { pub max_age_days: u32, pub sweep_interval_secs: u64 }` declared alongside `ToolsConfig` / `AutonomousConfig`.
      - `#[derive(Debug, Clone, Serialize, Deserialize)]` and `#[serde(default)]` on the struct.
      - `impl Default for AudioCacheConfig { fn default() -> Self { Self { max_age_days: 7, sweep_interval_secs: 86400 } } }`.
      - `Config` gains `#[serde(default)] pub audio_cache: AudioCacheConfig,` placed adjacent to `pub tools: ToolsConfig`.
      - `cargo build -p ironhermes-core` exits 0.
      - Pre-existing config YAMLs (no `audio_cache:` section) parse cleanly via serde defaults.
    Wave 0 test files:
      - `crates/iron_hermes_ui/tests/audio_route.rs` contains:
        - `const SOURCE: &str = include_str!("../src/server/audio_route.rs");`
        - `#[test] audio_route_declared_as_get_handler`: `SOURCE.contains("#[get(\"/audio/:uuid\")]")` OR `SOURCE.contains("/audio/:uuid")` (substring presence — flexible across Dioxus fullstack macro forms).
        - `#[test] audio_route_validates_uuid_format`: `SOURCE.contains("Uuid::parse_str")` OR `SOURCE.contains("Uuid::from_str")`.
        - `#[test] audio_route_canonicalizes_and_checks_containment`: `SOURCE.contains("canonicalize")` AND `SOURCE.contains("starts_with")`.
        - `#[test] audio_route_returns_audio_mpeg_content_type`: `SOURCE.contains("audio/mpeg")`.
        - `#[test] audio_route_no_unwrap_or_panic`: `!SOURCE.contains(".unwrap()") && !SOURCE.contains("panic!")`.
      - `crates/iron_hermes_ui/tests/audio_gc.rs` contains:
        - `const SOURCE: &str = include_str!("../src/server/audio_cache.rs");` and `const STATE: &str = include_str!("../src/server/state.rs");`
        - `#[test] audio_gc_sync_sweep_fn_exists`: `SOURCE.contains("fn gc_sweep_audio_cache")`.
        - `#[test] audio_gc_async_loop_fn_exists`: `SOURCE.contains("async fn run_audio_cache_gc_loop")`.
        - `#[test] audio_gc_uses_cancellation_token`: `SOURCE.contains("cancel.cancelled()")` AND `SOURCE.contains("CancellationToken")`.
        - `#[test] audio_gc_never_panics`: `!SOURCE.contains(".unwrap()") && !SOURCE.contains("panic!")` (errors must be `tracing::warn!`).
        - `#[test] audio_gc_logs_errors_via_tracing`: `SOURCE.contains("tracing::warn!")`.
        - `#[test] audio_gc_startup_sweep_called_from_state_init`: `STATE.contains("gc_sweep_audio_cache")`.
        - `#[tokio::test] audio_gc_removes_files_older_than_max_age` (functional test): creates a tempdir, writes 2 mp3 files, uses `std::fs::set_permissions` + `filetime::set_file_mtime` (if `filetime` is available) to age one file beyond `max_age_days=1`, calls `gc_sweep_audio_cache(&dir, 1)`, asserts only the new file remains. If `filetime` is not in workspace deps, fall back to source-grep test only and document in commit body.
        - `#[tokio::test] audio_gc_missing_dir_does_not_panic`: calls `gc_sweep_audio_cache(Path::new("/nonexistent/path/q9z"), 1)` — must not panic (NotFound branch).
      - All test files gated `#![cfg(feature = "server")]` if necessary (matches Plan 04 Task 1 gating choice).
      - Both files compile-fail acceptable on missing modules (`audio_route`, `audio_cache`) — those land in Tasks 2 + 3.
  </behavior>
  <action>
    Edit `crates/ironhermes-core/src/config.rs`:
      Step 1 — Locate the `Config` struct definition (~line 301 per RESEARCH §Pattern 6). Identify the existing `pub tools: ToolsConfig` field. Add directly below it: `#[serde(default)] pub audio_cache: AudioCacheConfig,` with doc comment `/// Phase 36.17.7 D-02-d: audio cache lifecycle policy. Pre-36.17.7 configs parse cleanly via #[serde(default)].`
      Step 2 — Declare the `AudioCacheConfig` struct + `Default` impl at the same nesting level as `ToolsConfig` / `AutonomousConfig`. Use the exact field types listed in &lt;behavior&gt;.
      Step 3 — Verify no test in the existing config test suite explicitly excludes `audio_cache` from a YAML round-trip. If `default_config_matches_expectations` or similar tests exist (per 36.17.6 STATE notes about default field assertions), append `audio_cache.max_age_days = 7` and `audio_cache.sweep_interval_secs = 86400` to the expectations.
    Create `crates/iron_hermes_ui/tests/audio_route.rs`. Mirror `web_queue_protocol.rs`'s structure. All 5 tests are source-grep — none depend on the module compiling at this task (the `const SOURCE` will fail to compile if the file doesn't exist, but Tasks 2 ships it).
    Create `crates/iron_hermes_ui/tests/audio_gc.rs`. Same source-grep pattern. The 2 `#[tokio::test]` functional tests require `audio_cache::gc_sweep_audio_cache` to exist — Task 3 ships it. Imports: `use iron_hermes_ui::server::audio_cache::gc_sweep_audio_cache;` `use std::path::Path;` `use tempfile::TempDir;`. If `filetime` is needed for mtime manipulation, verify workspace availability — if absent, drop the mtime-based functional test and keep source-grep only.
    Do NOT add new external deps to ironhermes-core or iron_hermes_ui without justifying in commit body.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-core 2>&amp;1 | tail -10 ; cargo test -p ironhermes-core --lib 2>&amp;1 | tail -10 ; cargo build -p iron_hermes_ui --tests --features server --no-run 2>&amp;1 | tail -20 ; grep -c "pub audio_cache: AudioCacheConfig" crates/ironhermes-core/src/config.rs ; grep -c "pub struct AudioCacheConfig" crates/ironhermes-core/src/config.rs ; grep -c "max_age_days: 7" crates/ironhermes-core/src/config.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub audio_cache: AudioCacheConfig" crates/ironhermes-core/src/config.rs` returns 1.
    - `grep -c "pub struct AudioCacheConfig" crates/ironhermes-core/src/config.rs` returns 1.
    - `grep -c "impl Default for AudioCacheConfig" crates/ironhermes-core/src/config.rs` returns 1.
    - `grep -c "max_age_days: 7" crates/ironhermes-core/src/config.rs` returns 1 (Default impl).
    - `grep -c "sweep_interval_secs: 86400" crates/ironhermes-core/src/config.rs` returns 1.
    - `grep -c "#\[test\]\|#\[tokio::test\]" crates/iron_hermes_ui/tests/audio_route.rs` returns ≥ 5.
    - `grep -c "#\[test\]\|#\[tokio::test\]" crates/iron_hermes_ui/tests/audio_gc.rs` returns ≥ 5.
    - `cargo build -p ironhermes-core` exits 0.
    - `cargo test -p ironhermes-core --lib` exits 0.
    - Build of iron_hermes_ui tests may fail only on missing `audio_route` / `audio_cache` modules (Tasks 2 + 3 land them). Any other compile error blocks the task.
  </acceptance_criteria>
  <done>Config gains AudioCacheConfig with serde defaults; Wave 0 tests for the HTTP route and GC sweep are scaffolded; ironhermes-core builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement audio_route.rs — GET /audio/:uuid with path-traversal mitigations (T-path-traversal)</name>
  <files>crates/iron_hermes_ui/src/server/audio_route.rs, crates/iron_hermes_ui/src/server/mod.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/ws.rs (lines 88-91 — the `#[get("/api/ws/chat")]` Dioxus fullstack handler shape to mirror)
    - crates/iron_hermes_ui/src/server/kanban_ws.rs (lines 61-65 — sibling axum `#[get]` pattern)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/iron_hermes_ui/src/server/audio_route.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 4 HTTP Replay Route, §Pitfall 4 Path Traversal)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-CONTEXT.md (§Known Threat Patterns — T-path-traversal HIGH severity, T-info-disclosure-via-uuid accept LOW)
  </read_first>
  <behavior>
    After this task:
      - `crates/iron_hermes_ui/src/server/audio_route.rs` exists with `pub async fn serve_audio(uuid: String) -> impl IntoResponse` (or whichever signature matches the Dioxus fullstack `#[get]` macro form used by ws.rs:88).
      - File-level `#[cfg(feature = "server")]` gating (or per-item, matching ws.rs's idiom).
      - The handler:
        - Validates `uuid` parses as `uuid::Uuid` (else returns `StatusCode::BAD_REQUEST` with body "invalid uuid").
        - Resolves `audio_cache_dir = get_hermes_home().join("audio_cache")`.
        - Builds candidate path `audio_cache_dir.join(format!("{uuid}.mp3"))`.
        - Calls `.canonicalize()` on the candidate path. On `Err`, returns `StatusCode::NOT_FOUND` with body "not found".
        - On `Ok(canon)`, asserts `canon.starts_with(&audio_cache_dir.canonicalize().unwrap_or(audio_cache_dir.clone()))` — if not, returns `StatusCode::FORBIDDEN` with body "forbidden". (Executor: canonicalize the dir lazily to avoid panicking when the dir doesn't exist; fall back to comparing against the un-canonicalized dir if canonicalize fails — RESEARCH §Pattern 4 + §Pitfall 4.)
        - Reads bytes via `tokio::fs::read(&canon).await`. On `Err` returns `StatusCode::NOT_FOUND`.
        - On success returns `([(header::CONTENT_TYPE, "audio/mpeg")], bytes).into_response()`.
      - `crates/iron_hermes_ui/src/server/mod.rs` declares `pub mod audio_route;`.
      - The route is registered with the axum router. RESEARCH notes that Dioxus fullstack's `#[get]` macro auto-registers server functions found by macro discovery. Verify the existing `ws.rs` `#[get]` registration mechanism — if `serve_dioxus_application` auto-discovers, no explicit `.route(...)` wiring is required in `main.rs`. Document in commit body whichever mechanism applies.
      - `cargo build -p iron_hermes_ui --features server` exits 0.
      - Wave 0 `audio_route.rs` tests 5/5 GREEN.
  </behavior>
  <action>
    Create `crates/iron_hermes_ui/src/server/audio_route.rs`. Copy PATTERNS §"`crates/iron_hermes_ui/src/server/audio_route.rs`" verbatim:
      - Imports: `use dioxus::prelude::*;` `#[cfg(feature = "server")] use axum::extract::Path;` `#[cfg(feature = "server")] use axum::response::{IntoResponse, Response};` `#[cfg(feature = "server")] use ironhermes_core::constants::get_hermes_home;` plus `use uuid::Uuid;` (must be a workspace dep — verify in workspace Cargo.toml; per RESEARCH §Standard Stack: already at workspace level).
      - `#[get("/audio/:uuid")]` macro on `pub async fn serve_audio(uuid: String) -> impl IntoResponse { ... }`.
      - Body structured exactly as in PATTERNS:
        - UUID parse gate → BAD_REQUEST.
        - audio_cache_dir derivation.
        - canonicalize → NOT_FOUND on Err.
        - starts_with guard → FORBIDDEN.
        - tokio::fs::read → NOT_FOUND on Err.
        - Success: `([(axum::http::header::CONTENT_TYPE, "audio/mpeg")], bytes).into_response()`.
      - `#[cfg(not(feature = "server"))]` arm: `(axum::http::StatusCode::NOT_FOUND, "server feature required").into_response()` (so the function compiles on the WASM target without server logic).
    Edit `crates/iron_hermes_ui/src/server/mod.rs`:
      - Add `pub mod audio_route;` near the existing `pub mod ws;` declaration.
    Threat mitigation MUST appear literally in the source (Task 1's source-grep test gates this):
      - The literal `Uuid::parse_str` OR `Uuid::from_str` for T-path-traversal step 1.
      - The literal `canonicalize` for T-path-traversal step 2.
      - The literal `starts_with` for T-path-traversal step 3.
      - The literal `audio/mpeg` for the Content-Type.
    Do NOT add a streaming Body response — files are < 1MB and full read is fine (RESEARCH §Pattern 4).
    Do NOT add an explicit auth gate — the existing ws.rs route has none (operator-local server). Document the auth-deferred decision in a comment in the route source.
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --features server 2>&amp;1 | tail -10 ; cargo test -p iron_hermes_ui --test audio_route --features server 2>&amp;1 | tail -20 ; grep -c "Uuid::parse_str\|Uuid::from_str" crates/iron_hermes_ui/src/server/audio_route.rs ; grep -c "canonicalize" crates/iron_hermes_ui/src/server/audio_route.rs ; grep -c "starts_with" crates/iron_hermes_ui/src/server/audio_route.rs ; grep -c "audio/mpeg" crates/iron_hermes_ui/src/server/audio_route.rs ; grep -c "pub mod audio_route" crates/iron_hermes_ui/src/server/mod.rs</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/iron_hermes_ui/src/server/audio_route.rs` exists.
    - `grep -c "pub async fn serve_audio" crates/iron_hermes_ui/src/server/audio_route.rs` returns 1.
    - `grep -c "Uuid::parse_str\|Uuid::from_str" crates/iron_hermes_ui/src/server/audio_route.rs` returns ≥ 1.
    - `grep -c "canonicalize" crates/iron_hermes_ui/src/server/audio_route.rs` returns ≥ 1.
    - `grep -c "starts_with" crates/iron_hermes_ui/src/server/audio_route.rs` returns ≥ 1.
    - `grep -c "audio/mpeg" crates/iron_hermes_ui/src/server/audio_route.rs` returns ≥ 1.
    - `grep -c ".unwrap()\|panic!" crates/iron_hermes_ui/src/server/audio_route.rs` returns 0 (per Task 1 audio_route_no_unwrap_or_panic).
    - `grep -c "pub mod audio_route" crates/iron_hermes_ui/src/server/mod.rs` returns 1.
    - `cargo build -p iron_hermes_ui --features server` exits 0.
    - `cargo test -p iron_hermes_ui --test audio_route --features server` exits 0; all 5 Wave 0 tests pass.
    - `cargo clippy -p iron_hermes_ui --features server -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>GET /audio/:uuid handler ships with full T-path-traversal mitigations and is callable by the WASM client + browser direct-fetch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement audio_cache.rs — gc_sweep_audio_cache + run_audio_cache_gc_loop</name>
  <files>crates/iron_hermes_ui/src/server/audio_cache.rs, crates/iron_hermes_ui/src/server/mod.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/kanban_ws.rs (lines 228-323 — `run_kanban_tail_loop` is the canonical periodic-task pattern with `tokio::select!` + `CancellationToken`)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/iron_hermes_ui/src/server/audio_cache.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 5 Audio Cache GC)
  </read_first>
  <behavior>
    After this task:
      - `crates/iron_hermes_ui/src/server/audio_cache.rs` exists with two pub functions:
        - `pub fn gc_sweep_audio_cache(dir: &std::path::Path, max_age_days: u32)` — sync sweep that walks the directory, checks `mtime` against `now() - max_age_days*86400`, removes older files via `std::fs::remove_file`. NotFound on the dir is a no-op. Other read errors log via `tracing::warn!` and return. Per-file remove errors log via `tracing::warn!` and continue.
        - `pub async fn run_audio_cache_gc_loop(dir: std::path::PathBuf, max_age_days: u32, sweep_interval_secs: u64, cancel: tokio_util::sync::CancellationToken)` — `tokio::select!` loop that ticks `tokio::time::sleep(Duration::from_secs(sweep_interval_secs))` and calls `gc_sweep_audio_cache`. Exits on `cancel.cancelled()`.
      - All error handling via `tracing::warn!` — NO `.unwrap()`, NO `panic!`.
      - File-level `#[cfg(feature = "server")]` gating.
      - `crates/iron_hermes_ui/src/server/mod.rs` declares `pub mod audio_cache;`.
      - Wave 0 `audio_gc.rs` tests 5+/7 source-grep GREEN; the 2 functional `#[tokio::test]`s GREEN.
  </behavior>
  <action>
    Create `crates/iron_hermes_ui/src/server/audio_cache.rs`. Copy PATTERNS §"`crates/iron_hermes_ui/src/server/audio_cache.rs`" verbatim:
      - File-level `#[cfg(feature = "server")]` gating.
      - Imports per the PATTERNS snippet: `std::fs`, `std::path::{Path, PathBuf}`, `std::time::{Duration, SystemTime}`, `tokio::time::sleep`, `tokio_util::sync::CancellationToken`.
      - `gc_sweep_audio_cache` body exactly as in PATTERNS:
        - `let cutoff = SystemTime::now() - Duration::from_secs(max_age_days as u64 * 86400);`
        - Match on `std::fs::read_dir(dir)`:
          - `Ok(entries)`: for each entry, read metadata + mtime, if `mtime < cutoff` call `std::fs::remove_file`. On remove Err → `tracing::warn!(path = %entry.path().display(), error = %e, "audio_cache GC: failed to remove file")`.
          - `Err(e) if e.kind() == std::io::ErrorKind::NotFound`: no-op.
          - `Err(e)`: `tracing::warn!(error = %e, "audio_cache GC: failed to read dir")`.
      - `run_audio_cache_gc_loop` body exactly as in PATTERNS:
        - `let interval = Duration::from_secs(sweep_interval_secs);`
        - `loop { tokio::select! { _ = tokio::time::sleep(interval) => { gc_sweep_audio_cache(&dir, max_age_days); } _ = cancel.cancelled() => { tracing::info!("audio_cache GC loop cancelled; exiting"); break; } } }`
    Edit `crates/iron_hermes_ui/src/server/mod.rs`:
      - Add `pub mod audio_cache;` next to the existing `pub mod ws;`.
    Do NOT use `tokio::time::interval` (sleep is sufficient per PATTERNS).
    Do NOT add a broadcast channel (kanban tail has subscribers; GC does not — only a CancellationToken).
    Do NOT use `tokio::fs::read_dir` (sync `std::fs::read_dir` keeps the sweep function callable from both startup-sync and async-loop contexts per PATTERNS).
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --features server 2>&amp;1 | tail -10 ; cargo test -p iron_hermes_ui --test audio_gc --features server 2>&amp;1 | tail -20 ; grep -c "fn gc_sweep_audio_cache" crates/iron_hermes_ui/src/server/audio_cache.rs ; grep -c "async fn run_audio_cache_gc_loop" crates/iron_hermes_ui/src/server/audio_cache.rs ; grep -c "cancel.cancelled" crates/iron_hermes_ui/src/server/audio_cache.rs ; grep -c "tracing::warn!" crates/iron_hermes_ui/src/server/audio_cache.rs ; grep -c "\.unwrap()\|panic!" crates/iron_hermes_ui/src/server/audio_cache.rs ; grep -c "pub mod audio_cache" crates/iron_hermes_ui/src/server/mod.rs</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/iron_hermes_ui/src/server/audio_cache.rs` exists.
    - `grep -c "fn gc_sweep_audio_cache" crates/iron_hermes_ui/src/server/audio_cache.rs` returns 1.
    - `grep -c "async fn run_audio_cache_gc_loop" crates/iron_hermes_ui/src/server/audio_cache.rs` returns 1.
    - `grep -c "cancel.cancelled" crates/iron_hermes_ui/src/server/audio_cache.rs` returns ≥ 1.
    - `grep -c "tracing::warn!" crates/iron_hermes_ui/src/server/audio_cache.rs` returns ≥ 1.
    - `grep -c "\.unwrap()\|panic!" crates/iron_hermes_ui/src/server/audio_cache.rs` returns 0.
    - `grep -c "pub mod audio_cache" crates/iron_hermes_ui/src/server/mod.rs` returns 1.
    - `cargo build -p iron_hermes_ui --features server` exits 0.
    - `cargo test -p iron_hermes_ui --test audio_gc --features server` exits 0; all Wave 0 source-grep tests GREEN; both functional `#[tokio::test]` tests GREEN (or — fallback documented in commit body — the mtime-aging test downgraded if `filetime` unavailable).
    - `cargo clippy -p iron_hermes_ui --features server -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>GC sweep functions exist and are unit-tested. Ready for state.rs init wire-up in Task 4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire startup GC sweep + periodic GC tokio task in AppState::init</name>
  <files>crates/iron_hermes_ui/src/server/state.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/state.rs (lines 172-220 — must see `AppState::init` and the existing kanban tail spawn pattern at lines 194-207)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/iron_hermes_ui/src/server/state.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 5 Audio Cache GC)
  </read_first>
  <behavior>
    After this task:
      - `AppState` struct gains `pub audio_gc_cancel: tokio_util::sync::CancellationToken` field placed adjacent to existing `kanban_tail_cancel` field.
      - `AppState::init` (or whichever constructor builds `AppState`) calls the startup sweep synchronously: `crate::server::audio_cache::gc_sweep_audio_cache(&audio_cache_dir, config.audio_cache.max_age_days);` before the async runtime work that follows.
      - Then spawns the periodic GC task via `tokio::spawn(async move { crate::server::audio_cache::run_audio_cache_gc_loop(gc_dir, gc_max_age, gc_interval, gc_cancel_clone).await; });` matching the kanban tail spawn pattern at lines 194-207.
      - Variables: `let audio_cache_dir = ironhermes_core::constants::get_hermes_home().join("audio_cache");` `let gc_max_age = config.audio_cache.max_age_days;` `let gc_interval = config.audio_cache.sweep_interval_secs;` `let audio_gc_cancel = tokio_util::sync::CancellationToken::new();` `let gc_cancel_clone = audio_gc_cancel.clone();` `let gc_dir = audio_cache_dir.clone();`.
      - `cargo build -p iron_hermes_ui --features server` exits 0.
      - Source-grep `state.rs` contains `gc_sweep_audio_cache` (Wave 0 Task 1 `audio_gc_startup_sweep_called_from_state_init` test GREEN).
  </behavior>
  <action>
    Edit `crates/iron_hermes_ui/src/server/state.rs`:
      Step 1 — Locate `AppState::init` (search for `fn init(` or `pub async fn init(`). Note the existing kanban tail spawn at lines 194-207.
      Step 2 — Add `pub audio_gc_cancel: tokio_util::sync::CancellationToken,` field to the `AppState` struct adjacent to `kanban_tail_cancel`. Doc comment `/// Phase 36.17.7 D-02-d: audio cache GC cancellation token. Mirrors kanban_tail_cancel directly above.`
      Step 3 — In `AppState::init`, after the kanban tail spawn block, add the GC block per PATTERNS:
        - Derive `audio_cache_dir`, `gc_max_age`, `gc_interval` from config.
        - Call `crate::server::audio_cache::gc_sweep_audio_cache(&audio_cache_dir, gc_max_age);` (sync startup sweep, before any await).
        - Create `let audio_gc_cancel = tokio_util::sync::CancellationToken::new();` `let gc_cancel_clone = audio_gc_cancel.clone();` `let gc_dir = audio_cache_dir.clone();`.
        - `tokio::spawn(async move { crate::server::audio_cache::run_audio_cache_gc_loop(gc_dir, gc_max_age, gc_interval, gc_cancel_clone).await; });`
      Step 4 — In the final `AppState { ... }` struct literal that init returns, add `audio_gc_cancel,` as a new field.
    Do NOT add a broadcast channel (GC has no subscribers).
    Do NOT block on the periodic task — `tokio::spawn` releases control immediately.
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --features server 2>&amp;1 | tail -10 ; cargo test -p iron_hermes_ui --features server 2>&amp;1 | tail -10 ; grep -c "gc_sweep_audio_cache" crates/iron_hermes_ui/src/server/state.rs ; grep -c "run_audio_cache_gc_loop" crates/iron_hermes_ui/src/server/state.rs ; grep -c "audio_gc_cancel" crates/iron_hermes_ui/src/server/state.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gc_sweep_audio_cache" crates/iron_hermes_ui/src/server/state.rs` returns ≥ 1 (startup sweep call).
    - `grep -c "run_audio_cache_gc_loop" crates/iron_hermes_ui/src/server/state.rs` returns 1 (spawn site).
    - `grep -c "audio_gc_cancel" crates/iron_hermes_ui/src/server/state.rs` returns ≥ 2 (field declaration + struct-literal init).
    - `grep -c "tokio::spawn" crates/iron_hermes_ui/src/server/state.rs` returns ≥ 2 (existing kanban + new GC spawn).
    - `cargo build -p iron_hermes_ui --features server` exits 0.
    - `cargo test -p iron_hermes_ui --features server` exits 0.
    - `cargo test -p iron_hermes_ui --test audio_gc --features server -- audio_gc_startup_sweep_called_from_state_init` exits 0.
    - `cargo clippy -p iron_hermes_ui --features server -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>Startup sweep + periodic GC are wired into the iron_hermes_ui server boot path. Audio cache is bounded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Add Registered column to toolset list (D-06)</name>
  <files>crates/ironhermes-cli/src/toolset_cmd.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/toolset_cmd.rs (full file — must see the existing toolset display rows logic, the sentinel `register_tts_for_inspection` helper at lines 407-416, and the `cmd_toolset_list` / `cmd_toolset_show` display code paths)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/ironhermes-cli/src/toolset_cmd.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 9 Toolset "Registered" Indicator)
    - .planning/phases/36.17.6-toolset-cli-tts-wiring/36.17.6-CONTEXT.md (the inspection sentinel-key pattern that D-06 must coexist with — do NOT collapse the two paths)
  </read_first>
  <behavior>
    After this task:
      - `cmd_toolset_list` and `cmd_toolset_show` output gain a `Registered` column showing one of: `Live` (real session_key present in the registry — only reachable from in-session `/toolset` slash dispatch), `Inspection` (sentinel `Platform::Local, "inspect"` key present — current 36.17.6 state), `—` (TTS tools not registered at all).
      - For the CLI-inspection path (current 36.17.6 path), the `Registered` value for the `voice` row is always `Inspection` because `register_tts_for_inspection` always fires.
      - The Live path requires accessing a live `AgentRuntime.bundle.registry`. For Plan 05's v1 implementation, executor may choose:
        - **Path A (simpler)**: CLI inspection always shows `Inspection` (the realistic state for `hermes toolset list` invoked from a terminal). The Live indicator is reachable only via `/toolset` slash command inside an agent turn, which is a separate code path. Document in commit body — `Live` value is not exercised by the CLI inspection path; UAT-Reg validates the column exists with the correct `Inspection` value.
        - **Path B (full D-06)**: Thread a `ToolRegistry::is_tts_registered_live()` accessor through `CommandContext` so the `/toolset` slash dispatch (which has access to the live runtime) shows `Live`. This requires modifications to `crates/ironhermes-core/src/commands/handlers.rs::handle_toolset_command`. Heavier; defer if budget tight.
        - Executor decides at run time. Commit body records the choice.
      - The Registered column appears in BOTH `cmd_toolset_list` (single row per toolset) AND `cmd_toolset_show` (detail view for a single toolset).
      - For non-TTS toolsets (everything except `voice`), the column reads `—` (TTS tools don't apply).
      - `cargo build -p ironhermes-cli` exits 0.
      - `cargo test -p ironhermes-cli` exits 0 (no regression).
      - A new unit test in toolset_cmd.rs's existing test module: `registered_column_shows_inspection_for_voice_after_inspection_register` builds the inspection registry + asserts the row's Registered field reads `Inspection`.
  </behavior>
  <action>
    Edit `crates/ironhermes-cli/src/toolset_cmd.rs`:
      Step 1 — Locate the existing `register_tts_for_inspection` helper at lines 407-416. DO NOT modify it (RESEARCH §Anti-Patterns: "Do NOT extend the inspection sentinel-key for live sessions").
      Step 2 — Locate the row-building logic (probably a `ToolsetRow` struct or inline display loop in `cmd_toolset_list`). Add a new field `registered: &'static str` (or `Cow<'static, str>` if dynamic) to the row struct OR add an inline computation in the display loop.
      Step 3 — Computation rule:
        - If the toolset name is `voice` AND `register_tts_for_inspection` has been called (registry has both `text_to_speech` AND `send_audio` tool definitions present): value is `Inspection`.
        - If executor implements Path B: if the registry has `text_to_speech` registered with a non-sentinel session_key (i.e. NOT `SessionKey { Platform::Local, "inspect", None }`): value is `Live`. Otherwise `Inspection`.
        - Else: value is `—`.
      Step 4 — Update the display formatter (likely a `println!` or table format string) to include the new column. Verify column alignment doesn't break existing tests (the 36.17.6 tests assert specific row strings — read those first and update the expectations if needed).
      Step 5 — Add a unit test `registered_column_shows_inspection_for_voice_after_inspection_register` in the existing `#[cfg(test)] mod tests` block of `toolset_cmd.rs`. Test must build the inspection registry via the existing helper, retrieve the voice row, and assert `row.registered == "Inspection"`.
      Step 6 — Verify 36.17.6 invariants (the test `browser_in_known_set` etc.) still pass.
    Path A reference is preferred unless executor's commit budget allows Path B without risking the phase gate.
    Do NOT modify `register_tts_for_inspection` (the sentinel key must remain `Platform::Local, "inspect"`).
    Do NOT change the meaning of `enabled` / `disabled` columns — they remain operator-toggled per `config.tools.toolsets.<name>.enabled`.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-cli 2>&amp;1 | tail -10 ; cargo test -p ironhermes-cli --lib 2>&amp;1 | tail -20 ; grep -c "Registered" crates/ironhermes-cli/src/toolset_cmd.rs ; grep -c "Inspection\|registered" crates/ironhermes-cli/src/toolset_cmd.rs ; cargo run -p ironhermes-cli --bin hermes -- toolset list 2>&amp;1 | grep -c "Registered" 2>&amp;1 || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Registered" crates/ironhermes-cli/src/toolset_cmd.rs` returns ≥ 1 (column header literal).
    - `grep -c "Inspection" crates/ironhermes-cli/src/toolset_cmd.rs` returns ≥ 1 (value literal).
    - `grep -c "registered_column_shows_inspection_for_voice_after_inspection_register" crates/ironhermes-cli/src/toolset_cmd.rs` returns 1 (new unit test).
    - `cargo build -p ironhermes-cli` exits 0.
    - `cargo test -p ironhermes-cli --lib` exits 0 (including new test).
    - The existing 36.17.6 tests (`toolset_members_map_voice_entry`, `browser_in_known_set` length 9) still pass.
    - Smoke check: `cargo run -p ironhermes-cli --bin hermes -- toolset list 2>&1 | grep -E "(Registered|Inspection)"` returns ≥ 1 line (manual; can be deferred to UAT-Reg).
    - `cargo clippy -p ironhermes-cli --lib -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>The Registered column lands in `toolset list` / `toolset show voice`. Commit body documents Path A vs Path B choice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Author invariants_36_17_7.rs cross-crate source-grep test (D-05)</name>
  <files>crates/ironhermes-agent/tests/invariants_36_17_7.rs</files>
  <read_first>
    - crates/ironhermes-agent/tests/invariants_36_17_5.rs (the canonical include_str! cross-crate pattern)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/ironhermes-agent/tests/invariants_36_17_7.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 8 D-05 Invariant Test Structure + the 5 verified test bodies at lines 500-563)
    - crates/ironhermes-agent/src/agent_runtime.rs (post-Plan-01) — verify whether `session_key: None,` literal still appears
    - crates/ironhermes-gateway/src/handler.rs (post-Plan-02) — verify `session_key: Some(` literal appears
    - crates/iron_hermes_ui/src/server/ws.rs (post-Plan-04) — verify `session_key: Some(` literal appears
    - crates/ironhermes-cli/src/tui_rata/event_loop.rs (post-Plan-03) — verify `session_key: Some(` literal appears (or its committed-comment fallback per Plan 03 Task 1 acceptance criteria)
    - crates/ironhermes-agent/src/app_runtime_factory.rs — verify the literal `register_tts_tools(` appears (the guarded call site)
  </read_first>
  <behavior>
    After this task:
      - `crates/ironhermes-agent/tests/invariants_36_17_7.rs` exists with the 5 tests per VALIDATION.md and PATTERNS:
        1. `agent_runtime_session_key_none_removed`: `const SRC: &str = include_str!("../src/agent_runtime.rs"); assert!(!SRC.contains("session_key: None,"), "...")`
        2. `gateway_handler_wires_session_key_some`: `include_str!("../../ironhermes-gateway/src/handler.rs"); assert!(SRC.contains("session_key: Some("), "...")`
        3. `web_ws_handler_wires_session_key_some`: `include_str!("../../iron_hermes_ui/src/server/ws.rs"); assert!(SRC.contains("session_key: Some("), "...")`
        4. `tui_event_loop_wires_session_key_some`: `include_str!("../../ironhermes-cli/src/tui_rata/event_loop.rs"); assert!(SRC.contains("session_key: Some("), "...")`
        5. `register_tts_tools_called_at_minimum_four_sites`: sum the `.matches("register_tts_tools(").count()` across 5 source files; assert `count >= 4`.

    **Critical reconciliation for Test 1.** Plan 01's design left the `from_config` call site at `agent_runtime.rs:215-217` with `session_key: None, telegram_adapter: None` because that call site builds the durable startup bundle (no TTS at startup — TTS is registered per-turn inside `run_turn`). The D-05 invariant in RESEARCH §Pattern 8 / PATTERNS §test_file_patterns greps `agent_runtime.rs` for the literal `session_key: None,` AND asserts its absence. These two facts conflict.

    Resolution: The invariant test author MUST inspect `agent_runtime.rs` first (the read_first read above). Two acceptable paths:
      - **Path A (rewrite the from_config call site)**: If Plan 01 implementation removed `session_key: None,` from the visible source (e.g. by switching to `..Default::default()` field-init shorthand), Test 1 GREENs as authored.
      - **Path B (refine the invariant scope)**: If Plan 01 kept the literal `session_key: None,` because it is correct semantically for the durable startup bundle, then Test 1's assertion must be narrowed. Acceptable narrower form: `assert!(SRC.matches("session_key: None,").count() <= 1, "agent_runtime.rs may keep at most ONE session_key: None, occurrence — the durable startup bundle. Per-turn callsites must use Some(...).")`. Pair this with a positive assert that the per-turn block exists: `assert!(SRC.contains("if let Some(ref wiring) = request.tts_wiring"), "agent_runtime.rs::run_turn must contain the per-turn register_tts_tools guard from Plan 01")`.

    Executor decides which path applies based on the literal source after Plans 01-04 have merged. Document choice in commit body. PATTERNS §Pitfall 6 + §Pitfall 7 are the relevant context — D-05 must not false-fail on test helpers in `app_runtime_factory.rs` (the negative assert targets `agent_runtime.rs` specifically).
      - `cargo test -p ironhermes-agent --test invariants_36_17_7` exits 0; all 5 tests GREEN.
  </behavior>
  <action>
    Create `crates/ironhermes-agent/tests/invariants_36_17_7.rs`. Copy RESEARCH §Pattern 8 verbatim (the full file body is specified at RESEARCH lines 500-563). Apply the reconciliation rule for Test 1 based on the actual post-Plans-01-04 state of `agent_runtime.rs`:
      Step 1 — Read `crates/ironhermes-agent/src/agent_runtime.rs` after Plans 01-04 merged. Count occurrences of the literal `session_key: None,` via `grep -c`.
      Step 2 — If count == 0: write Test 1 as `assert!(!SRC.contains("session_key: None,"))` per RESEARCH verbatim.
      Step 3 — If count > 0: write Test 1 as the narrower form from &lt;behavior&gt; Path B: `assert!(SRC.matches("session_key: None,").count() <= 1)` + positive `assert!(SRC.contains("if let Some(ref wiring) = request.tts_wiring"))`.
      Step 4 — Tests 2-5 are unchanged from RESEARCH §Pattern 8: cross-crate `include_str!` paths `"../../ironhermes-gateway/src/handler.rs"`, `"../../iron_hermes_ui/src/server/ws.rs"`, `"../../ironhermes-cli/src/tui_rata/event_loop.rs"`. Verify each path resolves at compile time before committing.
      Step 5 — Test 5 builds a `&[(&str, &str)]` of `(name, include_str!(...))` pairs across `app_runtime_factory.rs`, `agent_runtime.rs`, `handler.rs`, `ws.rs`, `event_loop.rs`. Sums `.matches("register_tts_tools(").count()` and asserts `count >= 4`.
      Step 6 — Add a module docstring explaining the D-05 contract and the Path A/B choice taken by Test 1.
    Do NOT target `app_runtime_factory.rs` with the negative assert (Pitfall 7: test helpers in factory legitimately use `session_key: None`).
    Do NOT modify the production source files in this task — invariants are read-only. If Test 1 reveals an issue with how Plan 01 left agent_runtime.rs, file a follow-up plan rather than retro-editing.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-agent --test invariants_36_17_7 2>&amp;1 | tail -30 ; grep -c "agent_runtime_session_key_none_removed\|gateway_handler_wires_session_key_some\|web_ws_handler_wires_session_key_some\|tui_event_loop_wires_session_key_some\|register_tts_tools_called_at_minimum_four_sites" crates/ironhermes-agent/tests/invariants_36_17_7.rs ; grep -c "include_str!" crates/ironhermes-agent/tests/invariants_36_17_7.rs</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/ironhermes-agent/tests/invariants_36_17_7.rs` exists.
    - `grep -c "#\[test\]" crates/ironhermes-agent/tests/invariants_36_17_7.rs` returns ≥ 5.
    - `grep -c "include_str!" crates/ironhermes-agent/tests/invariants_36_17_7.rs` returns ≥ 5.
    - `grep -c "../../ironhermes-gateway/src/handler.rs" crates/ironhermes-agent/tests/invariants_36_17_7.rs` returns ≥ 1.
    - `grep -c "../../iron_hermes_ui/src/server/ws.rs" crates/ironhermes-agent/tests/invariants_36_17_7.rs` returns ≥ 1.
    - `grep -c "../../ironhermes-cli/src/tui_rata/event_loop.rs" crates/ironhermes-agent/tests/invariants_36_17_7.rs` returns ≥ 1.
    - `cargo test -p ironhermes-agent --test invariants_36_17_7` exits 0; all 5 tests GREEN.
    - The existing `cargo test -p ironhermes-agent --test invariants_36_17_5` continues to pass (no regression).
    - Commit body documents Path A vs Path B choice for Test 1.
  </acceptance_criteria>
  <done>D-05 invariant guard is LIVE. The phase's source-grep contract is locked: any future commit that removes `session_key: Some(` from a handler or removes `register_tts_tools(` from production source breaks this test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Operator UAT-R + UAT-Reg + A6 regression sweep</name>
  <what-built>
    Plan 05 Tasks 1-6 have completed:
    - `Config.audio_cache` field + AudioCacheConfig defaults shipped (Task 1).
    - `GET /audio/:uuid` handler with uuid-parse + canonicalize + starts_with mitigations (Task 2).
    - `gc_sweep_audio_cache` + `run_audio_cache_gc_loop` ship in audio_cache.rs (Task 3).
    - AppState::init runs startup sweep + spawns periodic GC tokio task (Task 4).
    - `toolset list` / `toolset show voice` gain a Registered column (Live/Inspection/—) (Task 5).
    - invariants_36_17_7.rs locks D-05: handler.rs/ws.rs/event_loop.rs each contain `session_key: Some(`; register_tts_tools called ≥ 4 times (Task 6).
  </what-built>
  <how-to-verify>
    Three sub-UATs run in sequence; checkpoint approval requires all three.

    Pre-flight (operator runs once):
      1. Ensure `$IRONHERMES_HOME` is set (recommend same as Plans 02-04 UATs).
      2. Run `cargo test --workspace 2>&1 | tail -30` and confirm `0 failed` outside of the 2 pre-existing kanban end_to_end failures noted in STATE.md (those are unrelated; document in commit body if they appear).

    UAT-R — page-reload audio replay (must-have truth #9):
      1. Start iron_hermes_ui server: `cargo run -p iron_hermes_ui --features server` (or `dx serve` per project convention).
      2. Re-run Plan 04 UAT-W: in a browser, ask the agent to synthesize HN podcast audio. Confirm inline `<audio controls>` plays audibly.
      3. Refresh the browser tab (Cmd-R / Ctrl-R). Wait for the transcript to reload.
      4. Locate the previous audio block in the reloaded transcript. The `<audio>` src now resolves to `/audio/{uuid}` (no Blob URL after reload).
      5. Click play on the reloaded `<audio>` control. Confirm audible replay.
      6. Confirm via browser devtools network tab: a request `GET /audio/{uuid}` returned 200 with `Content-Type: audio/mpeg`.
      7. Confirm path traversal hardening: in browser address bar (or curl), attempt `GET /audio/..%2F..%2Fetc%2Fpasswd` — must return 400 (invalid uuid) NOT 200. Document the exact response status in resume-signal.

    UAT-Reg — toolset list Registered column (must-have truth #7):
      1. In a separate terminal (with iron_hermes_ui server still running or a fresh session): run `cargo run -p ironhermes-cli --bin hermes -- toolset list`.
      2. Confirm output includes a `Registered` column header.
      3. For the `voice` row, confirm the value is `Inspection` (CLI inspection path always shows Inspection per D-06 Path A).
      4. Run `cargo run -p ironhermes-cli --bin hermes -- toolset show voice`. Confirm detail view also shows the Registered indicator.
      5. (If executor chose Path B — Live indicator): start a TUI session via `cargo run -- chat`, type `/toolset list` inside the TUI, confirm the voice row shows `Live`. Skip this step if Path A was chosen.

    A6 regression sweep (must-have truth #10):
      1. Run `cargo run -p ironhermes-cli --bin hermes -- tts test`. Confirm 36.17.5 CLI TTS test sub-command still passes (Edge TTS test invocation).
      2. Run `cargo run -p ironhermes-cli --bin hermes -- tts play 'Hello from regression check'`. Confirm host playback (rodio).
      3. Confirm `toolset list` voice row reads `2 tools 2/2 (text_to_speech ✓, send_audio ✓)` — the 36.17.6 invariant.

    Failure modes to call out in resume-signal:
      - UAT-R Step 5 silent: `/audio/{uuid}` 404 — check audio_cache_dir resolution at audio_route.rs Task 2; verify `$IRONHERMES_HOME/audio_cache/` contains the expected uuid mp3.
      - UAT-R Step 7 returns 200 for path traversal: HIGH SEVERITY — fix immediately, blocks the phase.
      - UAT-Reg `Registered` column missing: re-check toolset_cmd.rs Task 5 Step 4 display formatter.
      - A6 Step 1 fails: A regression in 36.17.5 TTS provider path — investigate but typically unrelated to Plan 05's surface.
  </how-to-verify>
  <resume-signal>Type "approved" if UAT-R Steps 5+6+7 all GREEN, UAT-Reg shows Registered column with correct Inspection value, AND A6 regression sweep clean. Else describe the specific failure(s) with sub-UAT label (UAT-R / UAT-Reg / A6) and step number.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /audio/:uuid | Untrusted client-supplied uuid crosses here. Path traversal is the canonical HIGH-severity risk. |
| AppState::init → audio_cache disk | Startup sweep + periodic GC do mass deletes. Bound by `$IRONHERMES_HOME/audio_cache/` directory scope. |
| toolset_cmd CLI inspection registry → operator stdout | Display data only; no external I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-path-traversal | Tampering | audio_route.rs `GET /audio/:uuid` (HIGH) | mitigate | Three-layer defense per Task 2: (a) `uuid::Uuid::parse_str` validates format before any filesystem access; (b) `audio_cache_dir.join + canonicalize()` resolves the actual path; (c) `canon.starts_with(audio_cache_dir)` asserts the resolved path lives inside the cache directory. UAT-R Step 7 operator-verifies the mitigation against `..%2F..%2F` URL-encoded escape attempts. |
| T-unbounded-disk | Denial of Service | audio_cache directory (MEDIUM) | mitigate | Task 3 ships `gc_sweep_audio_cache` with mtime-based pruning at `max_age_days` cadence. Task 4 wires startup sweep + periodic loop with config-driven `sweep_interval_secs` (default 86400). Both NotFound + per-file remove errors degrade to `tracing::warn!`, never panic. |
| T-info-disclosure-via-uuid | Information Disclosure | audio file UUID guessability (LOW) | accept | Audio files use uuid v4 (122 bits of randomness, unguessable in practice). Accepted for v1 single-operator deployment per CONTEXT §Known Threat Patterns. Comment in audio_route.rs documents the accepted risk. |
| T-ws-binary-spoofing | Spoofing | (out of scope here — already mitigated in Plan 04) | accept | Plan 04 ws.rs handler covered this; Plan 05 adds no ws client→server binary path. |
| T-36.17.7-05-SC | Tampering | cargo deps | mitigate | Plan 05 introduces zero new external dependencies. `uuid`, `tokio`, `tokio-util`, `axum`, `tracing`, `serde`, `dioxus_fullstack` are all workspace-existing. `tempfile` dev-dep verified in Plan 04. No package-legitimacy human checkpoint needed. |
</threat_model>

<verify_plan>
1. `cargo build -p ironhermes-core -p iron_hermes_ui -p ironhermes-cli -p ironhermes-agent --features server` (where applicable) exits 0.
2. `cargo test --workspace 2>&1 | tail -20` shows 0 NEW failures (pre-existing kanban end_to_end 2 failures in STATE.md are acceptable per STATE.md note).
3. `cargo test -p ironhermes-agent --test invariants_36_17_7` exits 0; 5/5 GREEN.
4. `cargo test -p ironhermes-agent --test invariants_36_17_5` exits 0 (Phase 36.17.5 invariants — no regression).
5. `cargo test -p iron_hermes_ui --test audio_route --features server` exits 0; 5/5 GREEN.
6. `cargo test -p iron_hermes_ui --test audio_gc --features server` exits 0; all Wave 0 tests GREEN.
7. `cargo clippy --workspace --features server -- -D warnings` exits 0.
8. `grep -c "audio_cache: AudioCacheConfig" crates/ironhermes-core/src/config.rs` returns 1.
9. `grep -c "Uuid::parse_str\|Uuid::from_str" crates/iron_hermes_ui/src/server/audio_route.rs` returns ≥ 1 (T-path-traversal step 1).
10. `grep -c "canonicalize" crates/iron_hermes_ui/src/server/audio_route.rs` returns ≥ 1 (T-path-traversal step 2).
11. `grep -c "starts_with" crates/iron_hermes_ui/src/server/audio_route.rs` returns ≥ 1 (T-path-traversal step 3).
12. `grep -c "fn gc_sweep_audio_cache\|async fn run_audio_cache_gc_loop" crates/iron_hermes_ui/src/server/audio_cache.rs` returns 2.
13. `grep -c "Registered" crates/ironhermes-cli/src/toolset_cmd.rs` returns ≥ 1.
14. UAT-R + UAT-Reg + A6 regression sweep all approved by operator.
</verify_plan>

<success_criteria>
- D-02-c HTTP replay route ships with full T-path-traversal mitigations.
- D-02-d audio cache GC sweeps on startup and periodically.
- D-06 Registered column appears in toolset list / show.
- D-05 invariants_36_17_7.rs locks the cross-crate `session_key: Some(` contract.
- A6 no regression: 36.17.5 TTS CLI test/play work; 36.17.6 toolset list shows 2/2.
- A7 toolset list shows the new Registered column.
- Operator-confirmed page-reload audio replay (UAT-R).
- Zero new external dependencies.
</success_criteria>

<output>
Create `.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-05-SUMMARY.md` documenting:
- 9 files modified/created (count includes 2 Wave 0 tests + 1 invariants test)
- Source-grep gate counts from &lt;verify_plan&gt;
- D-05 Test 1 Path A vs Path B choice for invariants_36_17_7.rs
- D-06 Path A (CLI Inspection only) vs Path B (CLI + Live via slash) choice for toolset Registered column
- Functional test downgrade (filetime mtime aging) commit body note if applicable
- UAT-R transcript: browser used, reload behavior, network tab evidence for GET /audio/:uuid 200 + path-traversal 400
- UAT-Reg transcript: toolset list output snippet showing Registered Inspection
- A6 regression sweep transcript: hermes tts test + hermes tts play results
- Confirmation of A1..A7 coverage across all 5 phase plans
- must_haves achieved: all 10 truths; all 6 artifacts; all 3 key_links
</output>
