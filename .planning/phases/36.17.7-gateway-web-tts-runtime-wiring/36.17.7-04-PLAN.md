---
phase: 36.17.7
slug: gateway-web-tts-runtime-wiring
plan: 04
type: execute
wave: 2
depends_on:
  - 36.17.7-01
files_modified:
  - crates/iron_hermes_ui/src/protocol.rs
  - crates/iron_hermes_ui/src/state.rs
  - crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs
  - crates/iron_hermes_ui/src/server/ws.rs
  - crates/iron_hermes_ui/src/server/mod.rs
  - crates/iron_hermes_ui/src/components/HermesApp/mod.rs
  - crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs
  - crates/iron_hermes_ui/assets/main.css
  - crates/iron_hermes_ui/tests/audio_out_protocol.rs
  - crates/iron_hermes_ui/tests/web_audio_dispatcher.rs
autonomous: false
requirements: [A2, A4]
user_setup:
  - service: browser
    why: "UAT-W needs a desktop browser supporting native <audio controls> with mp3 playback (Chrome / Firefox / Safari)"
    env_vars:
      - name: IRONHERMES_HOME
        source: "Operator-chosen path; recommend /tmp/uat-36.17.7-home"
    dashboard_config:
      - task: "Start iron_hermes_ui server (cargo run -p iron_hermes_ui or dx serve, per project convention) and note the URL"
        location: "Terminal output of cargo run"
must_haves:
  truths:
    - "text_to_speech + send_audio appear in the LLM tool-schema list for every iron_hermes_ui web turn"
    - "ChatStreamEvent::AudioOut serializes to external-tagged JSON {\"AudioOut\":{\"mime\",\"uuid\",\"bytes\"}}"
    - "WebAudioDispatcher reads $IRONHERMES_HOME/audio_cache/{uuid}.mp3, packages mp3 bytes into AudioOut, sends via ws tx as Message::Binary"
    - "Browser renders an inline <audio controls> in the chat transcript at the stream position of each send_audio call; clicking play produces audible playback"
    - "Plan 04 Wave 0 tests (audio_out_protocol.rs + web_audio_dispatcher.rs) GREEN after Task 4 lands"
  artifacts:
    - path: "crates/iron_hermes_ui/src/protocol.rs"
      provides: "ChatStreamEvent::AudioOut { mime, uuid, bytes } variant + inline test_audio_out_json_shape"
      contains: "AudioOut"
    - path: "crates/iron_hermes_ui/src/state.rs"
      provides: "Block::Audio { uuid, mime } variant for inline transcript rendering"
      contains: "Block::Audio"
    - path: "crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs"
      provides: "WebAudioDispatcher struct + impl AudioDispatcher (reads mp3 from path, sends AudioOut via ws tx)"
      exports: ["WebAudioDispatcher"]
    - path: "crates/iron_hermes_ui/src/server/ws.rs"
      provides: "Per-turn WebAudioDispatcher construction + tts_wiring threading + Message::Binary send arm for AudioOut"
      contains: "session_key: Some("
    - path: "crates/iron_hermes_ui/src/components/HermesApp/mod.rs"
      provides: "Client-side AudioOut recv arm (creates Blob URL or routes to Block::Audio) + Message::Binary handler"
      contains: "AudioOut"
    - path: "crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs"
      provides: "Block::Audio match arm rendering <audio controls> per UI-SPEC.md"
      contains: "Block::Audio"
    - path: "crates/iron_hermes_ui/assets/main.css"
      provides: ".wh-audio-body + audio:focus-visible CSS rules per UI-SPEC.md"
      contains: ".wh-audio-body"
  key_links:
    - from: "iron_hermes_ui ws recv loop tx"
      to: "WebAudioDispatcher::send_audio_file"
      via: "Arc<dyn AudioDispatcher> mounted on TurnRequest.tts_wiring"
      pattern: "WebAudioDispatcher::new\\("
    - from: "ChatStreamEvent::AudioOut on ws tx"
      to: "browser <audio controls>"
      via: "Message::Binary frame → wasm client → Blob URL → Block::Audio render"
      pattern: "Message::Binary"
---

<objective>
Build the web runtime TTS surface end to end: add `ChatStreamEvent::AudioOut` (D-02-a), ship `WebAudioDispatcher` (D-02-a impl), wire it per-turn in the ws handler with a `session_key: Some(SessionKey { Web, ... })` + `Message::Binary` send arm for AudioOut frames, add the `Block::Audio` UI variant + RSX render per UI-SPEC.md, and confirm an audible inline `<audio controls>` plays in the browser transcript (D-02-b/f). HTTP replay route + GC are deferred to Plan 05 — Plan 04 ships first-play via the binary WS frame only.

Purpose: Closes A2 (tools appear in LLM schema on web) and A4-web (operator hears synthesized audio in browser). This is the largest plan in the phase because it spans 3 layers — protocol, server runtime, client RSX — plus CSS, plus 2 Wave 0 test files.

Output:
- Wave 0 tests `audio_out_protocol.rs` + `web_audio_dispatcher.rs` (Task 1).
- `ChatStreamEvent::AudioOut` variant + inline shape test (Task 2).
- `Block::Audio` state variant (Task 2).
- `WebAudioDispatcher` impl in `iron_hermes_ui::server::web_audio_dispatcher` + `mod.rs` re-export (Task 3).
- Per-turn wiring + `Message::Binary` AudioOut send arm in `ws.rs` (Task 4).
- Client-side `AudioOut` recv arm in HermesApp + `Block::Audio` RSX render + CSS rules (Task 5).
- Operator UAT-W blocking checkpoint confirms audible inline browser playback (Task 6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-CONTEXT.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-UI-SPEC.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-01-SUMMARY.md
@crates/iron_hermes_ui/src/protocol.rs
@crates/iron_hermes_ui/src/state.rs
@crates/iron_hermes_ui/src/server/ws.rs
@crates/iron_hermes_ui/src/server/state.rs
@crates/iron_hermes_ui/src/server/mod.rs
@crates/iron_hermes_ui/src/components/HermesApp/mod.rs
@crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs
@crates/iron_hermes_ui/src/components/warp_hermes.rs
@crates/iron_hermes_ui/assets/main.css
@crates/iron_hermes_ui/assets/warp-ih.css
@crates/iron_hermes_ui/assets/design-tokens.css
@crates/iron_hermes_ui/tests/web_queue_protocol.rs
@crates/ironhermes-agent/src/agent_runtime.rs
@crates/ironhermes-tools/src/send_audio_tool.rs
@crates/ironhermes-gateway/src/telegram.rs

<interfaces>
<!-- Critical signatures and call sites — extracted from PATTERNS so executor doesn't search. -->

Post-Plan-01 TurnRequest + TtsPerTurnWiring:
```rust
pub struct TtsPerTurnWiring {
    pub session_key: ironhermes_core::SessionKey,
    pub audio_dispatcher: Option<std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>>,
}
```

Current ChatStreamEvent enum (crates/iron_hermes_ui/src/protocol.rs:18-44) — extend with one variant:
```rust
#[derive(Serialize, Deserialize, Debug, Clone)]
pub enum ChatStreamEvent {
    Delta { text: String },
    ToolCallStart { name: String, args: String },
    ToolCallEnd { name: String, success: bool },
    Finished { total_tokens: u32 },
    Error { message: String },
    SubagentEvent {},
    QueueUpdated { depth: u32, paused: bool },
    // ADD: AudioOut { mime: String, uuid: String, bytes: Vec<u8> },
}
```

Existing web_key helper (ws.rs:113-119):
```rust
fn web_key(session_id: &str) -> ironhermes_core::session::SessionKey {
    ironhermes_core::session::SessionKey {
        platform: ironhermes_core::types::Platform::Web,
        chat_id: session_id.to_string(),
        user_id: Some("web".into()),
    }
}
```

Existing per-turn channel + binary-frame precedent (warp_hermes.rs:181 — `Message::Binary` is server-to-client already in the protocol):
```rust
// warp_hermes.rs:181 — client receives Message::Binary by converting bytes to UTF-8 then parsing as JSON.
// Add the same arm to HermesApp's ws handler.
```

AudioDispatcher trait (the impl target — defined in ironhermes-tools/src/send_audio_tool.rs:24-40):
```rust
#[async_trait]
pub trait AudioDispatcher: Send + Sync {
    async fn send_voice_file(&self, chat_id: &str, path: &PathBuf, thread_id: Option<&str>) -> anyhow::Result<()>;
    async fn send_audio_file(&self, chat_id: &str, path: &PathBuf, thread_id: Option<&str>) -> anyhow::Result<()>;
}
```

UI-SPEC.md inline audio block RSX shape:
```rust
div { class: "wh-block is-ai",
    div { class: "wh-block-head", span { class: "wh-author", "Hermes" } }
    div { class: "wh-block-body wh-audio-body",
        audio {
            controls: true,
            preload: "none",
            "aria-label": "Synthesized audio",
            title: "{uuid}",
            src: "/audio/{uuid}",
        }
    }
}
```
Note: `src: "/audio/{uuid}"` uses the HTTP replay route Plan 05 will ship. For Plan 04, the first-play path is via Blob URL constructed from the binary WS frame; the `/audio/:uuid` fallback path lights up after Plan 05. Both can coexist via a client-side Blob-URL precedence (first-play uses Blob, page reload uses HTTP route).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — author audio_out_protocol.rs + web_audio_dispatcher.rs test files</name>
  <files>crates/iron_hermes_ui/tests/audio_out_protocol.rs, crates/iron_hermes_ui/tests/web_audio_dispatcher.rs</files>
  <read_first>
    - crates/iron_hermes_ui/tests/web_queue_protocol.rs (the canonical protocol-shape lock test pattern — mirror the include_str! + assert structure)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/iron_hermes_ui/tests/audio_out_protocol.rs`" and §"`crates/iron_hermes_ui/tests/web_audio_dispatcher.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-VALIDATION.md (rows for D-02-a)
  </read_first>
  <behavior>
    audio_out_protocol.rs MUST contain:
      - `const PROTOCOL_SOURCE: &str = include_str!("../src/protocol.rs");`
      - `#[test] protocol_rs_declares_audio_out_variant`: asserts `PROTOCOL_SOURCE.contains("AudioOut")`.
      - `#[test] protocol_rs_audio_out_has_mime_uuid_bytes_fields`: asserts `PROTOCOL_SOURCE.contains("mime: String")`, `contains("uuid: String")`, `contains("bytes: Vec<u8>")`.
      - `#[test] protocol_rs_audio_out_shape_test_exists`: asserts `PROTOCOL_SOURCE.contains("test_audio_out_json_shape")` (the inline serde shape test lives next to the variant).
      - `#[test] mod_rs_handles_audio_out_in_recv_loop`: `const MOD_RS: &str = include_str!("../src/components/HermesApp/mod.rs");` + asserts `MOD_RS.contains("AudioOut")` (proves Task 5's client arm landed).
      - `#[test] audio_out_serializes_to_external_tagged_json`: builds `ChatStreamEvent::AudioOut { mime: "audio/mpeg".into(), uuid: "x".into(), bytes: vec![0xFF, 0xFB] }`, calls `serde_json::to_string(&ev).unwrap()`, asserts the JSON starts with `{"AudioOut":` (external tagging) and contains `"mime":"audio/mpeg"` and `"uuid":"x"`. Round-trips via `serde_json::from_str` and asserts the result `matches!` `ChatStreamEvent::AudioOut { .. }`.
    web_audio_dispatcher.rs MUST contain:
      - `const SOURCE: &str = include_str!("../src/server/web_audio_dispatcher.rs");`
      - `#[test] dispatcher_impl_for_audio_dispatcher_present`: asserts `SOURCE.contains("impl ironhermes_tools::AudioDispatcher for WebAudioDispatcher")` OR `SOURCE.contains("impl") && SOURCE.contains("AudioDispatcher for WebAudioDispatcher")` (substring presence).
      - `#[test] dispatcher_struct_holds_tx_and_audio_cache_dir`: asserts `SOURCE.contains("tx:") && SOURCE.contains("UnboundedSender")` AND `SOURCE.contains("audio_cache_dir:")`.
      - `#[test] dispatcher_emits_audio_out_event`: asserts `SOURCE.contains("ChatStreamEvent::AudioOut")`.
      - `#[test] dispatcher_does_not_re_persist`: asserts `!SOURCE.contains("write_all") && !SOURCE.contains("File::create")` (TextToSpeechTool already wrote the file; the dispatcher only reads).
      - `#[tokio::test] dispatcher_sends_audio_out_on_send_audio_file`: builds `(tx, rx) = tokio::sync::mpsc::unbounded_channel::<ChatStreamEvent>();` + a temp dir containing `{uuid}.mp3` with known bytes; constructs `WebAudioDispatcher::new(tx, temp_dir.path().into())`; calls `.send_audio_file("session", &temp_dir.path().join("00000000-0000-0000-0000-000000000000.mp3"), None).await`; asserts the result is `Ok(())` AND `rx.recv()` yields a `ChatStreamEvent::AudioOut { mime: "audio/mpeg", uuid: "00000000-0000-0000-0000-000000000000", bytes }` with `bytes` matching the file contents.
    Compile-fail on missing APIs (`ChatStreamEvent::AudioOut`, `WebAudioDispatcher::new`) is the expected RED state until Tasks 2-3 land.
  </behavior>
  <action>
    Create both test files in `crates/iron_hermes_ui/tests/`. Mirror the `web_queue_protocol.rs` source-grep test pattern (PATTERNS §test_file_patterns).
    Imports needed for `audio_out_protocol.rs`: `use iron_hermes_ui::protocol::ChatStreamEvent;`. The crate-name slug is `iron_hermes_ui` per the Cargo manifest — verify by reading `crates/iron_hermes_ui/Cargo.toml` first.
    Imports for `web_audio_dispatcher.rs`: `use iron_hermes_ui::server::web_audio_dispatcher::WebAudioDispatcher;` `use iron_hermes_ui::protocol::ChatStreamEvent;` `use ironhermes_tools::AudioDispatcher;` `use std::path::PathBuf;` `use tempfile::TempDir;` `use tokio::sync::mpsc;`.
    Cargo dev-dep check: `tempfile` is likely present (used by other iron_hermes_ui tests). If absent, add `tempfile = { workspace = true }` to `[dev-dependencies]` of `crates/iron_hermes_ui/Cargo.toml`. Same check for `tokio` test-util feature.
    The dispatcher fn signature requires `path: &PathBuf` per the trait — pass `&temp_dir.path().join("...")` directly (PathBuf via `.join`).
    Server-feature gating: `WebAudioDispatcher` lives behind `#[cfg(feature = "server")]` in iron_hermes_ui (per PATTERNS §"Shared Patterns" — `#[cfg(feature = "server")]` gating). The test file must add a top-level `#![cfg(feature = "server")]` so cargo only builds it when the `server` feature is enabled. Alternatively, gate individual tests with `#[cfg(feature = "server")]` if the file structure requires.
    Commit body notes RED state.
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --tests --features server --no-run 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/iron_hermes_ui/tests/audio_out_protocol.rs` exists, contains the 5 named tests.
    - File `crates/iron_hermes_ui/tests/web_audio_dispatcher.rs` exists, contains the 5 named tests.
    - `grep -c "#\[test\]\|#\[tokio::test\]" crates/iron_hermes_ui/tests/audio_out_protocol.rs` returns ≥ 5.
    - `grep -c "#\[test\]\|#\[tokio::test\]" crates/iron_hermes_ui/tests/web_audio_dispatcher.rs` returns ≥ 5.
    - `grep -c "include_str!" crates/iron_hermes_ui/tests/audio_out_protocol.rs` returns ≥ 2 (protocol.rs + mod.rs).
    - `grep -c "include_str!" crates/iron_hermes_ui/tests/web_audio_dispatcher.rs` returns ≥ 1.
    - Compile failure is acceptable IF and ONLY IF the failure is on missing `ChatStreamEvent::AudioOut`, `WebAudioDispatcher`, or `web_audio_dispatcher` module — i.e. the APIs Tasks 2 + 3 will add. Any syntax / import error blocks the task.
  </acceptance_criteria>
  <done>Both test files exist with named tests and compile-fail only on the planned-missing APIs. Commit body notes RED state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ChatStreamEvent::AudioOut variant + inline shape test + Block::Audio variant</name>
  <files>crates/iron_hermes_ui/src/protocol.rs, crates/iron_hermes_ui/src/state.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/protocol.rs (full file — must see the existing `QueueUpdated` variant + the inline `test_queue_updated_json_shape` test to mirror)
    - crates/iron_hermes_ui/src/state.rs (must see the existing `Block` enum + `BlockEntry` to know where to slot `Block::Audio`)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/iron_hermes_ui/src/protocol.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-UI-SPEC.md (lines 109-149 — Block::Audio recommended decision)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 3 ChatStreamEvent::AudioOut Encoding)
  </read_first>
  <behavior>
    protocol.rs after this task:
      - `ChatStreamEvent` enum has a new variant `AudioOut { mime: String, uuid: String, bytes: Vec<u8> }` declared AFTER `QueueUpdated`.
      - Variant has a doc comment explaining the wire shape: `/// Phase 36.17.7 D-02-a: synthesized audio delivery to the web client. JSON wire shape (external tagging): {"AudioOut":{"mime":"audio/mpeg","uuid":"<uuidv4>","bytes":[<u8>...]}}. Transmitted as Message::Binary per D-02-a. Client wraps bytes as Blob → URL.createObjectURL → <audio src=blobURL>.`
      - Tests mod gains `test_audio_out_json_shape` mirroring `test_queue_updated_json_shape`: builds `AudioOut { mime, uuid, bytes }`, serializes via `serde_json::to_string`, asserts the JSON starts with `{"AudioOut":`, contains `"mime":"audio/mpeg"` + `"uuid":"test-uuid"`, then round-trips via `serde_json::from_str`.
      - No `#[serde(...)]` attribute on the AudioOut variant (default external tagging matches all other ChatStreamEvent variants).
    state.rs after this task:
      - `Block` enum gains a new variant `Audio { uuid: String, mime: String }`. Position adjacent to existing variants like `Block::Text`, `Block::Tool`, etc. — read the file first to confirm the exact existing variant family.
      - The `BlockEntry` infrastructure can match-render `Block::Audio` (Task 5 wires the RSX; Task 2 only declares the variant).
    Both files build clean. Plan 04 Wave 0 tests `audio_out_protocol.rs` go from compile-fail → 3 of 5 GREEN (the variant-grep tests; the dispatcher-side tests stay RED until Task 3).
  </behavior>
  <action>
    Edit `crates/iron_hermes_ui/src/protocol.rs`:
      Step 1 — Locate the `ChatStreamEvent` enum (line ~18-44). After the `QueueUpdated { depth: u32, paused: bool },` variant, add the `AudioOut { mime: String, uuid: String, bytes: Vec<u8> },` variant with the doc comment listed in &lt;behavior&gt;.
      Step 2 — Locate the inline tests mod (search for `#[cfg(test)]` or `mod tests {`). After the existing `test_queue_updated_json_shape` function, add `test_audio_out_json_shape` mirroring it. Specific assertions: `assert!(json.starts_with(r#"{"AudioOut":"#))`, `assert!(json.contains("\"mime\":\"audio/mpeg\""))`, `assert!(json.contains("\"uuid\":\"test-uuid\""))`, round-trip `assert!(matches!(parsed, ChatStreamEvent::AudioOut { .. }))`.
      Step 3 — Serde-bytes decision. `Vec<u8>` serializes to a JSON array of integers by default. For binary WS frame transport (Message::Binary), the array form is fine because the entire JSON is sent as raw bytes. Plan 04 uses the array form to avoid adding `serde_bytes`. Document the choice in a comment on the variant. If executor finds `serde_bytes` already in `Cargo.toml`, may switch to `#[serde(with = "serde_bytes")]` and update the shape test accordingly — but the default is "no new dep, array form."
    Edit `crates/iron_hermes_ui/src/state.rs`:
      Step 4 — Locate the `Block` enum. Read its current variants. After the most appropriate existing variant (place near `Block::Tool` or wherever Hermes-output blocks live — read first), add `Audio { uuid: String, mime: String },` with doc comment `/// Phase 36.17.7 D-02-a/f: inline audio control rendered in the chat transcript at the stream position of each send_audio emit.`
      Step 5 — If `Block` is referenced exhaustively elsewhere in `state.rs` or sibling files (search for `match block {` or `match entry.block {`), find each exhaustive match site. For each, add a placeholder `Block::Audio { .. } => { /* rendered by block_stream.rs Task 5 */ },` arm OR an explicit `Block::Audio { .. } => unreachable!()` in test/scratch code paths. If the file is internally consistent, the Rust compiler will guide. Document fixed sites in the commit body.
    Do NOT modify `mod.rs`, `block_stream.rs`, `ws.rs`, or any server files in this task.
    Do NOT add new deps.
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui 2>&amp;1 | tail -10 ; cargo test -p iron_hermes_ui --lib -- protocol::tests::test_audio_out_json_shape 2>&amp;1 | tail -10 ; cargo test -p iron_hermes_ui --test audio_out_protocol -- protocol_rs_declares_audio_out_variant protocol_rs_audio_out_has_mime_uuid_bytes_fields protocol_rs_audio_out_shape_test_exists 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AudioOut {" crates/iron_hermes_ui/src/protocol.rs` returns ≥ 1 (variant declaration).
    - `grep -c "mime: String" crates/iron_hermes_ui/src/protocol.rs` returns ≥ 1.
    - `grep -c "uuid: String" crates/iron_hermes_ui/src/protocol.rs` returns ≥ 1.
    - `grep -c "bytes: Vec<u8>" crates/iron_hermes_ui/src/protocol.rs` returns ≥ 1.
    - `grep -c "test_audio_out_json_shape" crates/iron_hermes_ui/src/protocol.rs` returns ≥ 1.
    - `grep -c "Audio { uuid: String, mime: String }\|Audio {" crates/iron_hermes_ui/src/state.rs` returns ≥ 1.
    - `cargo build -p iron_hermes_ui` exits 0.
    - `cargo test -p iron_hermes_ui --lib -- protocol::tests::test_audio_out_json_shape` exits 0.
    - The 3 Wave-0 protocol.rs source-grep tests from Task 1 (`protocol_rs_declares_audio_out_variant`, `protocol_rs_audio_out_has_mime_uuid_bytes_fields`, `protocol_rs_audio_out_shape_test_exists`) pass.
    - `cargo clippy -p iron_hermes_ui --lib -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>`ChatStreamEvent::AudioOut` and `Block::Audio` are declared; inline shape test GREEN. Plan 04 Wave 0 protocol tests 3/5 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship WebAudioDispatcher impl in iron_hermes_ui::server::web_audio_dispatcher</name>
  <files>crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs, crates/iron_hermes_ui/src/server/mod.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/mod.rs (find the existing `pub mod ws;` / `pub mod state;` declarations; mirror the placement)
    - crates/iron_hermes_ui/src/server/ws.rs (lines 1-30 — see the existing `#[cfg(feature = "server")]` gating idiom for server-only files)
    - crates/ironhermes-gateway/src/telegram.rs (the existing `impl AudioDispatcher for TelegramAdapter` — confirms the orphan-rule pattern works: trait in `ironhermes-tools`, impl in consuming crate)
    - crates/ironhermes-tools/src/send_audio_tool.rs (AudioDispatcher trait at lines 24-40 — signatures to copy exactly)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 2 WebAudioDispatcher, §Pitfall 2)
  </read_first>
  <behavior>
    After this task:
      - File `crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs` exists, contains `pub struct WebAudioDispatcher { tx: UnboundedSender<ChatStreamEvent>, audio_cache_dir: PathBuf }` and `impl ironhermes_tools::AudioDispatcher for WebAudioDispatcher`.
      - `send_voice_file` and `send_audio_file` both:
        - Read mp3 bytes from `path` via `tokio::fs::read`.
        - Extract uuid from `path.file_stem()` (the filename minus `.mp3`).
        - Build `ChatStreamEvent::AudioOut { mime: "audio/mpeg", uuid, bytes }` and `tx.send(event)`.
        - Return `Ok(())` on success; map any error to `anyhow::anyhow!`.
        - Do NOT write any new file (TextToSpeechTool already persisted).
      - `crates/iron_hermes_ui/src/server/mod.rs` declares `pub mod web_audio_dispatcher;` so external imports work and tests compile.
      - The entire file is gated `#[cfg(feature = "server")]` (PATTERNS §"Shared Patterns").
      - `cargo build -p iron_hermes_ui --features server` exits 0.
      - Plan 04 Wave 0 tests `web_audio_dispatcher.rs` go from compile-fail → all 5 GREEN.
  </behavior>
  <action>
    Create `crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs`. Copy from PATTERNS §"`crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs`" verbatim (the file shape is fully specified there). Key elements (executor MUST match exactly, do not paraphrase identifiers):
      - File-level: `#![cfg(feature = "server")]` OR per-item gating. Match the gating idiom used by `state.rs` / `ws.rs`; whichever is consistent.
      - Imports: `use async_trait::async_trait;` `use std::path::PathBuf;` `use std::sync::Arc;` `use tokio::sync::mpsc::UnboundedSender;` `use crate::protocol::ChatStreamEvent;`
      - `pub struct WebAudioDispatcher { tx: UnboundedSender<ChatStreamEvent>, audio_cache_dir: PathBuf }` with `pub fn new(tx: UnboundedSender<ChatStreamEvent>, audio_cache_dir: PathBuf) -> Self`.
      - `#[async_trait] impl ironhermes_tools::AudioDispatcher for WebAudioDispatcher` — both methods. Method signatures must match the trait EXACTLY: `chat_id: &str, path: &PathBuf, thread_id: Option<&str>` (underscore-prefix unused params).
      - `send_voice_file` body: forward to `self.send_audio_file(...)`. For web, voice and audio are both raw mp3 (D-02-e). Single helper method.
      - `send_audio_file` body:
        - `let bytes = tokio::fs::read(path).await.map_err(|e| anyhow::anyhow!("read audio file: {e}"))?;`
        - `let uuid = path.file_stem().and_then(|s| s.to_str()).unwrap_or("unknown").to_string();`
        - `let event = ChatStreamEvent::AudioOut { mime: "audio/mpeg".to_string(), uuid, bytes };`
        - `self.tx.send(event).map_err(|e| anyhow::anyhow!("ws send failed: {e}"))?;`
        - `Ok(())`
      - Doc comments per PATTERNS §"`crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs`".
    Edit `crates/iron_hermes_ui/src/server/mod.rs`:
      - Add `pub mod web_audio_dispatcher;` next to the existing `pub mod ws;` (or wherever sibling module declarations live — match the alphabetic / grouped order already used).
    Do NOT place this file in `ironhermes-tools` (RESEARCH §Pitfall 2: circular dep `tools → ui → tools` if so).
    Do NOT re-persist audio bytes — TextToSpeechTool wrote the file; this dispatcher only reads.
    Do NOT generate a new UUID; use the filename stem.
    Do NOT add new dependencies. `async-trait`, `tokio`, `anyhow`, `ironhermes_tools` all exist as deps of `iron_hermes_ui` per existing usage in the crate.
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --features server 2>&amp;1 | tail -10 ; cargo test -p iron_hermes_ui --test web_audio_dispatcher --features server 2>&amp;1 | tail -20 ; grep -c "impl ironhermes_tools::AudioDispatcher for WebAudioDispatcher\|AudioDispatcher for WebAudioDispatcher" crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs ; grep -c "pub mod web_audio_dispatcher" crates/iron_hermes_ui/src/server/mod.rs</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs` exists.
    - `grep -c "pub struct WebAudioDispatcher" crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs` returns 1.
    - `grep -c "AudioDispatcher for WebAudioDispatcher" crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs` returns 1.
    - `grep -c "ChatStreamEvent::AudioOut" crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs` returns ≥ 1.
    - `grep -c "tokio::fs::read" crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs` returns ≥ 1.
    - `grep -cE "write_all|File::create" crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs` returns 0 (no re-persistence).
    - `grep -c "pub mod web_audio_dispatcher" crates/iron_hermes_ui/src/server/mod.rs` returns 1.
    - `cargo build -p iron_hermes_ui --features server` exits 0.
    - `cargo test -p iron_hermes_ui --test web_audio_dispatcher --features server` exits 0; all 5 tests pass.
    - `cargo test -p iron_hermes_ui --test audio_out_protocol --features server` still passes (3 protocol-side tests from Task 2, plus the 4th if the round-trip test was authored).
    - `cargo clippy -p iron_hermes_ui --features server -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>`WebAudioDispatcher` is callable from `ws.rs` (next task). Plan 04 Wave 0 `web_audio_dispatcher.rs` tests 5/5 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire WebAudioDispatcher per-turn in ws.rs + add Message::Binary send arm for AudioOut</name>
  <files>crates/iron_hermes_ui/src/server/ws.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/ws.rs (full file — must see lines 113-119 `web_key` helper, lines ~216 per-turn `(tx, rx)` channel construction, the existing recv-loop that forwards `ChatStreamEvent` to the socket via `Message::Text`, and the call site of `run_web_turn` / `runtime.run_turn`)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/iron_hermes_ui/src/server/ws.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Web Runtime Constructor Detail, §Pitfall 3 Message::Binary)
  </read_first>
  <behavior>
    After this task:
      - `ws.rs` per-turn block contains (between channel construction at ~216 and the `run_web_turn` / `run_turn` call):
        - `let audio_tx = tx.clone();`
        - `let audio_cache_dir = ironhermes_core::constants::get_hermes_home().join("audio_cache");`
        - `let web_audio_dispatcher = std::sync::Arc::new(crate::server::web_audio_dispatcher::WebAudioDispatcher::new(audio_tx, audio_cache_dir));`
        - `let tts_wiring = Some(ironhermes_agent::TtsPerTurnWiring { session_key: web_key(&session_id), audio_dispatcher: Some(web_audio_dispatcher as std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>) });`
      - `tts_wiring` is plumbed into the `TurnRequest` that `runtime.run_turn` receives. Depending on whether `run_web_turn` builds `TurnRequest` internally or `ws.rs` builds it directly, executor chooses the cleanest path:
        - Option A: If `ws.rs` builds the `TurnRequest`, add `tts_wiring,` as the final field.
        - Option B: If `run_web_turn` builds the `TurnRequest` internally, add a `tts_wiring: Option<TtsPerTurnWiring>` parameter to `run_web_turn` and pass `tts_wiring` from ws.rs through. Update any other callers of `run_web_turn` to pass `None` (RESEARCH Open Q2: likely only ws.rs calls it; verify via grep).
      - The recv loop that forwards `ChatStreamEvent` to the socket gains a `Message::Binary` arm specifically for `ChatStreamEvent::AudioOut`. Pattern (executor uses concrete identifiers):
        - `match &ev { ChatStreamEvent::AudioOut { .. } => { let bytes = serde_json::to_vec(&ev).unwrap_or_default(); let _ = socket.send_raw(Message::Binary(bytes.into())).await; } _ => { let json = serde_json::to_string(&ev).unwrap_or_default(); let _ = socket.send_raw(Message::Text(json)).await; } }`
        - Non-AudioOut events stay `Message::Text` (no change to existing behavior — RESEARCH §Pitfall 3).
      - The `session_key: Some(web_key(&session_id))` form satisfies D-05 (Plan 05 invariant searches for the literal `session_key: Some(`).
      - `cargo build -p iron_hermes_ui --features server` exits 0.
      - `cargo test -p iron_hermes_ui --features server` exits 0.
      - Source-grep: ws.rs contains `WebAudioDispatcher::new`, `session_key: Some(`, `Message::Binary`, `ironhermes_agent::TtsPerTurnWiring`.
  </behavior>
  <action>
    Edit `crates/iron_hermes_ui/src/server/ws.rs`:
      Step 1 — Verify `web_key` helper exists at lines 113-119 (per RESEARCH §Verified web_key helper). If missing, port from the verified shape in &lt;interfaces&gt;.
      Step 2 — Locate the per-turn `let (tx, rx) = mpsc::unbounded_channel::<ChatStreamEvent>();` at ~ws.rs:216. Confirm the variable names (`tx`, `rx`, `session_id`).
      Step 3 — Immediately after the channel construction, add the dispatcher construction block from &lt;behavior&gt;. Use fully-qualified paths if no shorter aliases exist (`std::sync::Arc`, `crate::server::web_audio_dispatcher::WebAudioDispatcher`, `ironhermes_agent::TtsPerTurnWiring`, `ironhermes_tools::AudioDispatcher`).
      Step 4 — Thread `tts_wiring` into `TurnRequest`. First grep `ws.rs` for `run_web_turn` and `runtime.run_turn` to find the actual TurnRequest construction site.
        - If TurnRequest is built in `ws.rs`: add `tts_wiring,` as a final field to the struct literal.
        - If TurnRequest is built inside `run_web_turn` (in `state.rs` or similar): change `run_web_turn`'s signature to accept `tts_wiring: Option<ironhermes_agent::TtsPerTurnWiring>` and pass `tts_wiring` from ws.rs. Update any other callers (RESEARCH Open Q2 — grep `cargo run -- ` callers; should be ws.rs only).
        - Document the chosen path in the commit body.
      Step 5 — Add the `Message::Binary` send arm. Locate the recv loop where `rx.recv()` events are forwarded to `socket.send_raw(Message::Text(...))`. Replace the single send line with the match block from &lt;behavior&gt;. Ensure imports include `dioxus_fullstack::Message` (or whichever module exposes `Message::Binary` — verify by reading existing `Message::Text(...)` usage in the same file).
      Step 6 — Verify that `bytes.into()` produces the type `Message::Binary` expects. If `Message::Binary` takes `Vec<u8>` directly, drop `.into()`. If it takes `Bytes`, keep `.into()` (the `bytes::Bytes::from(Vec<u8>)` conversion).
    Do NOT switch non-AudioOut events to `Message::Binary` (RESEARCH §Pitfall 3: text path is hot, do not change).
    Do NOT add the `Message::Binary` arm to the recv-from-client side — AudioOut is server-to-client only (CONTEXT §threat T-ws-binary-spoofing).
    Do NOT add HTTP route or GC code — Plan 05 owns those.
    Do NOT modify state.rs's `AppState` struct unless run_web_turn signature change requires it (in which case minimize the diff — only the parameter addition).
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --features server 2>&amp;1 | tail -10 ; cargo test -p iron_hermes_ui --features server 2>&amp;1 | tail -10 ; grep -c "WebAudioDispatcher::new" crates/iron_hermes_ui/src/server/ws.rs ; grep -c "session_key: Some(" crates/iron_hermes_ui/src/server/ws.rs ; grep -c "Message::Binary" crates/iron_hermes_ui/src/server/ws.rs ; grep -c "ironhermes_agent::TtsPerTurnWiring" crates/iron_hermes_ui/src/server/ws.rs ; grep -c "ChatStreamEvent::AudioOut" crates/iron_hermes_ui/src/server/ws.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WebAudioDispatcher::new" crates/iron_hermes_ui/src/server/ws.rs` returns 1.
    - `grep -c "session_key: Some(" crates/iron_hermes_ui/src/server/ws.rs` returns ≥ 1 (D-05 prep).
    - `grep -c "Message::Binary" crates/iron_hermes_ui/src/server/ws.rs` returns ≥ 1.
    - `grep -c "ironhermes_agent::TtsPerTurnWiring" crates/iron_hermes_ui/src/server/ws.rs` returns ≥ 1.
    - `grep -c "ChatStreamEvent::AudioOut" crates/iron_hermes_ui/src/server/ws.rs` returns ≥ 1 (in the Binary-arm match).
    - `grep -c "audio_cache" crates/iron_hermes_ui/src/server/ws.rs` returns ≥ 1.
    - `cargo build -p iron_hermes_ui --features server` exits 0.
    - `cargo test -p iron_hermes_ui --features server` exits 0 (no regression).
    - `cargo clippy -p iron_hermes_ui --features server -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>The web ws layer constructs WebAudioDispatcher per turn, plumbs tts_wiring into TurnRequest, and sends AudioOut events as Message::Binary. text_to_speech + send_audio appear in the LLM schema for every web turn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Client-side AudioOut recv arm in HermesApp + Block::Audio RSX render + CSS rules</name>
  <files>crates/iron_hermes_ui/src/components/HermesApp/mod.rs, crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs, crates/iron_hermes_ui/assets/main.css</files>
  <read_first>
    - crates/iron_hermes_ui/src/components/HermesApp/mod.rs (full file — must see the ws recv loop, the existing `ChatStreamEvent` match arms — especially `QueueUpdated` and `Delta` for the pattern to mirror, and the `Message::Binary` handling site if any)
    - crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs (full file — see how `Block::Text` / `Block::Tool` / etc. are rendered; mirror that match-arm pattern for `Block::Audio`)
    - crates/iron_hermes_ui/src/components/warp_hermes.rs (line ~181 — the existing `Message::Binary` UTF-8 conversion in the legacy-shell handler. HermesApp may need an equivalent arm; verify mod.rs's ws handler first)
    - crates/iron_hermes_ui/assets/main.css (see existing `.wh-block-body` rule and where to insert the `.wh-audio-body` + `audio:focus-visible` rules per UI-SPEC.md)
    - crates/iron_hermes_ui/assets/warp-ih.css (read-only — confirm `--accent-primary` and `--space-2` tokens exist; do NOT modify)
    - crates/iron_hermes_ui/assets/design-tokens.css (read-only — confirm token availability)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-UI-SPEC.md (full file — the design contract)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/iron_hermes_ui/src/components/HermesApp/*`")
  </read_first>
  <behavior>
    After this task:
      - HermesApp's ws recv loop has a `ChatStreamEvent::AudioOut { mime, uuid, bytes }` match arm that:
        - Pushes a new `BlockEntry { id: <stream-id>, block: Block::Audio { uuid: uuid.clone(), mime: mime.clone() } }` into the transcript stream signal.
        - Optionally caches a Blob URL keyed by uuid into a `use_signal` map so first-play uses the in-memory bytes (vs the `/audio/{uuid}` HTTP route Plan 05 will ship). For Plan 04 first-play: the `<audio src=...>` may point at a `blob:` URL constructed via `URL.createObjectURL(new Blob([bytes], { type: mime }))` on the client. The HTTP route fallback `src="/audio/{uuid}"` is the page-reload path Plan 05 wires.
        - If implementing Blob URL caching is non-trivial in WASM (web-sys / gloo APIs), executor may opt for the simpler "always use `/audio/{uuid}`" form — but then UAT-W will FAIL until Plan 05 ships. The cleaner Plan 04 path is the Blob URL form using `web-sys` (verify availability in `iron_hermes_ui/Cargo.toml`).
        - Decision recorded in commit body: "Blob URL first-play" vs "HTTP route only (UAT-W blocks on Plan 05)".
      - If HermesApp's ws handler does NOT already have a `Message::Binary` arm (RESEARCH Open Q3), add one that calls `String::from_utf8_lossy(&bytes)` then JSON-parses as a `ChatStreamEvent` and falls into the same match block as Text frames. Mirror `warp_hermes.rs:181`.
      - `block_stream.rs`'s `Block` match block gains a `Block::Audio { uuid, mime: _ }` arm that emits the RSX shape from UI-SPEC.md §Component Inventory:
        - `div { class: "wh-block is-ai", div { class: "wh-block-head", span { class: "wh-author", "Hermes" } }, div { class: "wh-block-body wh-audio-body", audio { controls: true, preload: "none", "aria-label": "Synthesized audio", title: "{uuid}", src: "{audio_src}" } } }` where `audio_src` is either the cached blob URL or `format!("/audio/{}", uuid)`.
        - No `autoplay` attribute (D-02-b).
        - No `<button>` wrapper around `<audio>` — the native control is the play surface.
      - `main.css` gains:
        - `.wh-audio-body { padding-top: var(--space-2); }`
        - `.wh-audio-body audio { width: 100%; display: block; }`
        - `audio:focus-visible { outline: 2px solid var(--accent-primary); outline-offset: 2px; }`
        - All declarations use existing tokens (no hardcoded hex per UI-SPEC.md "CSS loading constraint").
      - `cargo build -p iron_hermes_ui --no-default-features --features web` (or whichever feature set builds the WASM target) exits 0. If iron_hermes_ui builds with a single default feature set, the server feature build is sufficient.
      - Plan 04 Wave 0 test `audio_out_protocol.rs::mod_rs_handles_audio_out_in_recv_loop` flips GREEN (the source-grep finds `AudioOut` in `mod.rs`).
  </behavior>
  <action>
    Edit `crates/iron_hermes_ui/src/components/HermesApp/mod.rs`:
      Step 1 — Locate the ws recv loop and the existing `ChatStreamEvent` match (likely an arm for `Delta`, `Finished`, `QueueUpdated`).
      Step 2 — Add a new arm `ChatStreamEvent::AudioOut { mime, uuid, bytes }` that:
        - Constructs a `Block::Audio { uuid: uuid.clone(), mime: mime.clone() }`.
        - Pushes a `BlockEntry` into the transcript stream signal (mirror how `Delta` builds and appends a `Block::Text` BlockEntry — read the existing arm first).
        - Optionally caches a Blob URL via `web_sys::Url::create_object_url_with_blob(...)` in a signal map keyed by uuid for first-play. Or — simpler — store the raw `bytes` in a signal map; the `block_stream.rs` render path can look up the bytes and construct the Blob inline.
        - If Blob handling is too complex for one task slice, fall back to `src="/audio/{uuid}"` and document in commit body that UAT-W depends on Plan 05's HTTP route landing. Either path satisfies acceptance (UAT-W is gated by Task 6 which can be re-run after Plan 05 in fallback mode).
      Step 3 — Search `mod.rs` for `Message::Binary` recv arm. If absent, add one that converts the binary frame to UTF-8 and parses as JSON, falling through to the same `ChatStreamEvent` match (mirror `warp_hermes.rs:181`). Comment: `// Phase 36.17.7 D-02-a: binary frames carry AudioOut JSON; convert to text and reuse the existing match.`
    Edit `crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs`:
      Step 4 — Locate the `match block { ... }` in the existing block-render fn. After the most recent variant arm, add the `Block::Audio { uuid, mime: _ }` arm that produces the RSX shape from &lt;behavior&gt;. The `src` attribute resolution: `format!("/audio/{}", uuid)` if Blob URL caching is deferred, otherwise the cached `blob:` URL from the signal map (via `use_context`).
      Step 5 — Confirm no `Block::Audio` arm is left as `unreachable!()` from Task 2's Step 5 — overwrite with the real implementation.
    Edit `crates/iron_hermes_ui/assets/main.css`:
      Step 6 — Append the 3 rules from &lt;behavior&gt; at the end of the file (or in a logical grouping with other `.wh-*` rules). Use only existing CSS custom properties: `var(--space-2)` for the padding and `var(--accent-primary)` for the focus ring. No hardcoded hex values.
    Verification: After this task, the operator can run the server, open the browser, and a `send_audio` LLM call produces an inline `<audio controls>` element in the transcript. Audio plays on click (Task 6 confirms audibly).
    Do NOT touch warp-ih.css or design-tokens.css.
    Do NOT add a `legacy-shell` cfg gate (UI-SPEC.md: "design-tokens.css and warp-ih.css MUST load unconditionally"; HermesApp is the active root per memory note `project_warp_vs_hermes_app_active_root`).
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui 2>&amp;1 | tail -10 ; cargo build -p iron_hermes_ui --features server 2>&amp;1 | tail -10 ; cargo test -p iron_hermes_ui --test audio_out_protocol --features server -- mod_rs_handles_audio_out_in_recv_loop 2>&amp;1 | tail -10 ; grep -c "ChatStreamEvent::AudioOut" crates/iron_hermes_ui/src/components/HermesApp/mod.rs ; grep -c "Block::Audio" crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs ; grep -c "wh-audio-body" crates/iron_hermes_ui/assets/main.css ; grep -c "audio:focus-visible" crates/iron_hermes_ui/assets/main.css ; grep -c "var(--accent-primary)" crates/iron_hermes_ui/assets/main.css ; grep -cE "#[0-9a-fA-F]{6}" crates/iron_hermes_ui/assets/main.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ChatStreamEvent::AudioOut" crates/iron_hermes_ui/src/components/HermesApp/mod.rs` returns ≥ 1.
    - `grep -c "Block::Audio" crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs` returns ≥ 1.
    - `grep -c "Block::Audio" crates/iron_hermes_ui/src/components/HermesApp/mod.rs` returns ≥ 1 (the recv arm pushes Block::Audio).
    - `grep -c "controls: true" crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs` returns ≥ 1 (UI-SPEC mandates native `<audio controls>`).
    - `grep -cE "autoplay" crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs` returns 0 (D-02-b: no autoplay).
    - `grep -c "aria-label" crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs` returns ≥ 1 (accessibility).
    - `grep -c "wh-audio-body" crates/iron_hermes_ui/assets/main.css` returns ≥ 1.
    - `grep -c "audio:focus-visible" crates/iron_hermes_ui/assets/main.css` returns ≥ 1.
    - `grep -c "var(--accent-primary)" crates/iron_hermes_ui/assets/main.css` returns ≥ 1.
    - The new CSS block uses ONLY tokens (no hardcoded hex): the executor's added lines may NOT contain a `#XXXXXX` literal. Test: `git diff crates/iron_hermes_ui/assets/main.css | grep -v '^#' | grep -E '^\+' | grep -cE "#[0-9a-fA-F]{3,8}"` returns 0.
    - `cargo build -p iron_hermes_ui` exits 0.
    - `cargo build -p iron_hermes_ui --features server` exits 0.
    - `cargo test -p iron_hermes_ui --test audio_out_protocol --features server -- mod_rs_handles_audio_out_in_recv_loop` exits 0 (the test from Task 1 that grepped mod.rs for AudioOut now passes).
    - `cargo clippy -p iron_hermes_ui -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>HermesApp handles `AudioOut` ws events and renders inline `<audio controls>` elements in the transcript. CSS rules apply UI-SPEC.md tokens. First-play path (Blob URL or `/audio/:uuid`) is documented in commit body — UAT-W in Task 6 will determine if Plan 05 is required to make audio audibly play.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Operator UAT-W — browser inline audio control playback (A4 web variant + A2 acceptance gate)</name>
  <what-built>
    Plan 04 Tasks 1-5 have completed:
    - Wave 0 tests `audio_out_protocol.rs` (5 tests) and `web_audio_dispatcher.rs` (5 tests) all GREEN.
    - `ChatStreamEvent::AudioOut { mime, uuid, bytes }` variant + inline shape test in protocol.rs.
    - `Block::Audio { uuid, mime }` variant in state.rs.
    - `WebAudioDispatcher` impl in iron_hermes_ui::server::web_audio_dispatcher reads mp3 from path, builds AudioOut, sends via ws tx.
    - ws.rs constructs WebAudioDispatcher per turn, plumbs `tts_wiring` into TurnRequest with `session_key: Some(web_key(session_id))`, sends AudioOut events as `Message::Binary`.
    - HermesApp's ws recv loop handles `AudioOut` and pushes `Block::Audio` into the transcript stream; `block_stream.rs` renders inline `<audio controls>` per UI-SPEC.md; CSS rules in main.css apply existing tokens for focus ring + body padding.
  </what-built>
  <how-to-verify>
    Pre-flight:
      1. `echo $IRONHERMES_HOME` (recommend `/tmp/uat-36.17.7-home`).
      2. Confirm `voice` toolset enabled in `$IRONHERMES_HOME/config.yaml`.
      3. Confirm host audio output unmuted (browser audio path uses the OS default output).
      4. Build + start the server: per project convention, run `cargo run -p iron_hermes_ui --features server` OR `dx serve` if the Dioxus dev runner is configured. Note the URL.

    UAT-W Steps:
      1. Open the iron_hermes_ui URL in a desktop browser (Chrome / Firefox / Safari).
      2. In the chat input, send: `Summarize the top three Hacker News stories right now as a 30-second podcast hosted by a calm female voice. Use text_to_speech then send_audio.`
      3. Observe the transcript:
         - Agent text streams in normally (Delta events).
         - At the moment `send_audio` fires, an inline `<audio controls>` element appears in the transcript with the play button visible.
         - No `autoplay` (the audio should NOT start playing on its own — D-02-b).
      4. Click the play button on the audio control. Confirm audible speech, female voice, summary of HN stories.
      5. Optional (UAT-R preview — Plan 05 will lock this): refresh the browser. The transcript reloads. Locate the previous audio block. If the implementation chose the Blob URL form for first-play, the reloaded `<audio>` will show 404 (Blob URLs are session-scoped) UNTIL Plan 05 ships the `/audio/:uuid` HTTP route. If the implementation chose `src="/audio/:uuid"` from the start, expect 404 here too (Plan 05 lights this up). Plan 05's UAT-R is the canonical reload test.

    Failure modes to call out in resume-signal:
      - No `<audio>` element appears in transcript: Block::Audio render arm bug — re-check block_stream.rs Task 5 Step 4.
      - `<audio>` appears but no play button: CSS/RSX bug — confirm `controls: true` is in the RSX (PATTERNS §UI-SPEC).
      - Click play, nothing audible: Browser console may show "failed to load" if the src URL is wrong. If using Blob URL, may indicate the byte handling is broken. If using `/audio/:uuid`, expected to fail until Plan 05.
      - `<audio>` plays but agent text mentions `say` or `hyperframes-media`: tools-not-registered — re-check ws.rs Task 4 Step 4 (tts_wiring plumb).
      - Visual: audio control width unreadable / overflows the block — CSS regression in main.css; revert to UI-SPEC.md `width: 100%`.

    If first-play succeeds (Steps 1-4 pass): mark UAT-W approved even if Step 5 reload fails — Plan 05 owns the reload path.
  </how-to-verify>
  <resume-signal>Type "approved" if browser inline audio playback occurred in Step 4, OR describe the specific failure (which step + observed-vs-expected + browser-console errors if any).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → iron_hermes_ui ws | Client-controlled input crosses the existing ws boundary (CONTEXT §threat T-ws-binary-spoofing: client-to-server binary path is NOT added — server-to-client only). |
| iron_hermes_ui ws → browser via Message::Binary | Server sends AudioOut JSON-encoded as bytes. JSON is well-formed by `serde_json::to_vec`; no injection surface. |
| TextToSpeechTool → audio_cache disk | Existing 36.17.5 T-output-path mitigation (canonicalize + starts_with). Plan 04 only reads from this directory in WebAudioDispatcher; no new write path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ws-binary-spoofing | Spoofing | ws.rs Message::Binary handler | accept | Server-to-client only. CONTEXT documents this explicitly. No client-to-server binary handler added; the recv path (client → server) continues to accept Message::Text only. |
| T-36.17.7-04-01 | Information Disclosure | WebAudioDispatcher reads from audio_cache | mitigate | path arg comes from `TextToSpeechTool`'s in-process construction (well-known canonicalized form). The dispatcher does NOT accept arbitrary client-supplied paths. |
| T-36.17.7-04-02 | Tampering | AudioOut JSON shape | mitigate | External-tagged serde matches all other ChatStreamEvent variants. Inline `test_audio_out_json_shape` locks the wire format against accidental field rename. |
| T-36.17.7-04-SC | Tampering | cargo deps | mitigate | Plan 04 introduces zero new external dependencies. `async-trait`, `tokio`, `dioxus_fullstack`, `serde`, `anyhow`, `ironhermes_core`, `ironhermes_tools`, `ironhermes_agent` are all existing deps of `iron_hermes_ui`. `tempfile` is a dev-dep verified in Task 1. No package-legitimacy human checkpoint needed. |
</threat_model>

<verify_plan>
1. `cargo build -p iron_hermes_ui --features server` exits 0.
2. `cargo build -p iron_hermes_ui` (default features) exits 0.
3. `cargo test -p iron_hermes_ui --test audio_out_protocol --features server` exits 0 (5/5 GREEN).
4. `cargo test -p iron_hermes_ui --test web_audio_dispatcher --features server` exits 0 (5/5 GREEN).
5. `cargo test -p iron_hermes_ui --features server` exits 0 (full crate, no regression).
6. `cargo clippy -p iron_hermes_ui --features server -- -D warnings` exits 0.
7. `grep -c "session_key: Some(" crates/iron_hermes_ui/src/server/ws.rs` returns ≥ 1.
8. `grep -c "Message::Binary" crates/iron_hermes_ui/src/server/ws.rs` returns ≥ 1.
9. `grep -c "AudioOut" crates/iron_hermes_ui/src/protocol.rs` returns ≥ 2 (variant + inline test).
10. `grep -c "Block::Audio" crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs` returns ≥ 1.
11. UAT-W checkpoint approved by operator (Tasks 1-5 GREEN + Step 4 audible).
12. No regression in Phase 36.17.5 invariants: `cargo test -p ironhermes-agent --test invariants_36_17_5` exits 0.
</verify_plan>

<success_criteria>
- ChatStreamEvent gains AudioOut; Block gains Audio; both have inline shape tests.
- WebAudioDispatcher impl ships and is wired per-turn in ws.rs with `Message::Binary` send arm.
- HermesApp renders inline `<audio controls>` per UI-SPEC.md.
- CSS rules in main.css use existing tokens (no new hex).
- Operator UAT-W confirms audible browser playback (A4 web variant + A2 transitive proof).
- Zero regression in `iron_hermes_ui` test suite or downstream Phase 36.17.5 invariants.
- No new external dependencies.
</success_criteria>

<output>
Create `.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-04-SUMMARY.md` documenting:
- 10 files modified (count includes Wave 0 tests)
- Source-grep gate counts from &lt;verify_plan&gt;
- Wave 0 test counts: audio_out_protocol.rs 5/5 GREEN, web_audio_dispatcher.rs 5/5 GREEN
- First-play strategy chosen (Blob URL vs `/audio/:uuid` placeholder); note whether UAT-W "approved" reflects audible play or deferred pending Plan 05
- UAT-W transcript: browser used, observed control appearance, audible result, any console errors
- D-05 prep: confirms `session_key: Some(` literal in ws.rs
- run_web_turn signature decision (Option A or B from Task 4 Step 4)
- must_haves achieved: all 5 truths; all 7 artifacts; both key_links
</output>
