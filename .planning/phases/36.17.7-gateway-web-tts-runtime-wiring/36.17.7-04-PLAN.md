---
phase: 36.17.7
slug: gateway-web-tts-runtime-wiring
plan: 04
type: execute
wave: 2
depends_on:
  - 36.17.7-01
files_modified:
  - crates/iron_hermes_ui/Cargo.toml
  - crates/iron_hermes_ui/src/protocol.rs
  - crates/iron_hermes_ui/src/state.rs
  - crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs
  - crates/iron_hermes_ui/src/server/ws.rs
  - crates/iron_hermes_ui/src/server/mod.rs
  - crates/iron_hermes_ui/src/components/HermesApp/mod.rs
  - crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs
  - crates/iron_hermes_ui/assets/main.css
  - crates/iron_hermes_ui/tests/audio_out_protocol.rs
  - crates/iron_hermes_ui/tests/web_audio_dispatcher.rs
autonomous: false
requirements: [A2, A4]
user_setup:
  - service: browser
    why: "UAT-W needs a desktop browser supporting native <audio controls> with mp3 playback (Chrome / Firefox / Safari)"
    env_vars:
      - name: IRONHERMES_HOME
        source: "Operator-chosen path; recommend /tmp/uat-36.17.7-home"
    dashboard_config:
      - task: "Start iron_hermes_ui server (cargo run -p iron_hermes_ui or dx serve, per project convention) and note the URL"
        location: "Terminal output of cargo run"
must_haves:
  truths:
    - "text_to_speech + send_audio appear in the LLM tool-schema list for every iron_hermes_ui web turn"
    - "ChatStreamEvent::AudioOut serializes to external-tagged JSON {\"AudioOut\":{\"mime\",\"uuid\",\"bytes\"}}"
    - "WebAudioDispatcher reads $IRONHERMES_HOME/audio_cache/{uuid}.mp3, packages mp3 bytes into AudioOut, sends via ws tx as Message::Binary"
    - "Browser renders an inline <audio controls> in the chat transcript at the stream position of each send_audio call; clicking play produces audible playback from a Blob URL (REVISION HIGH 4 fix — first-play is ALWAYS Blob URL, never depends on Plan 05's HTTP route)"
    - "HermesApp ws handler has an UNCONDITIONAL Message::Binary recv arm that deserializes binary frames as ChatStreamEvent and dispatches AudioOut to the block stream (REVISION HIGH 7 fix)"
    - "Plan 04 Wave 0 tests (audio_out_protocol.rs + web_audio_dispatcher.rs) GREEN after Task 4 lands"
  artifacts:
    - path: "crates/iron_hermes_ui/Cargo.toml"
      provides: "wasm-bindgen + web-sys (Blob, Url, HtmlAudioElement features) + js-sys deps for Blob URL first-play (REVISION HIGH 4 fix)"
      contains: "web-sys"
    - path: "crates/iron_hermes_ui/src/protocol.rs"
      provides: "ChatStreamEvent::AudioOut { mime, uuid, bytes } variant + inline test_audio_out_json_shape"
      contains: "AudioOut"
    - path: "crates/iron_hermes_ui/src/state.rs"
      provides: "Block::Audio { uuid, mime } variant for inline transcript rendering"
      contains: "Block::Audio"
    - path: "crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs"
      provides: "WebAudioDispatcher struct + impl AudioDispatcher (reads mp3 from path, sends AudioOut via ws tx)"
      exports: ["WebAudioDispatcher"]
    - path: "crates/iron_hermes_ui/src/server/ws.rs"
      provides: "Per-turn WebAudioDispatcher construction + tts_wiring threading + Message::Binary send arm for AudioOut"
      contains: "session_key: Some("
    - path: "crates/iron_hermes_ui/src/components/HermesApp/mod.rs"
      provides: "Unconditional Message::Binary recv arm + AudioOut handler that constructs Blob URL via web_sys::Url::create_object_url_with_blob and stores in context (HIGH 4 + HIGH 7)"
      contains: "Message::Binary"
    - path: "crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs"
      provides: "Block::Audio match arm rendering <audio controls> with Blob-URL-or-fallback src per UI-SPEC.md"
      contains: "Block::Audio"
    - path: "crates/iron_hermes_ui/assets/main.css"
      provides: ".wh-audio-body + audio:focus-visible CSS rules per UI-SPEC.md"
      contains: ".wh-audio-body"
  key_links:
    - from: "iron_hermes_ui ws recv loop tx"
      to: "WebAudioDispatcher::send_audio_file"
      via: "Arc<dyn AudioDispatcher> mounted on TurnRequest.tts_wiring"
      pattern: "WebAudioDispatcher::new\\("
    - from: "ChatStreamEvent::AudioOut on ws tx"
      to: "browser <audio controls>"
      via: "Message::Binary frame → wasm Message::Binary arm → JSON parse → web_sys::Blob → Url::create_object_url_with_blob → Block::Audio render"
      pattern: "create_object_url_with_blob"
---

<objective>
Build the web runtime TTS surface end to end: add `ChatStreamEvent::AudioOut` (D-02-a), ship `WebAudioDispatcher` (D-02-a impl), wire it per-turn in the ws handler with a `session_key: Some(SessionKey { Web, ... })` + `Message::Binary` send arm for AudioOut frames, add the `Block::Audio` UI variant + RSX render per UI-SPEC.md, and confirm an audible inline `<audio controls>` plays in the browser transcript via Blob URL first-play (D-02-b/f).

**REVISION HIGH 4 fix:** First-play is MANDATED to be Blob URL via `web_sys::Url::create_object_url_with_blob`. The "/audio/{uuid}" form is RESERVED for Plan 05 UAT-R page-reload replay only. Plan 04's UAT-W must produce audible play WITHOUT requiring Plan 05 to ship — the two paths are decoupled.

**REVISION HIGH 7 fix:** HermesApp's `Message::Binary` recv arm is UNCONDITIONAL — Task 5 ships it even if HermesApp already has a binary handler (the existing handler may not route binary frames into the ChatStreamEvent match).

**REVISION MEDIUM 10 fix:** Task 5 includes an RSX interpolation clarification note in `<action>` so the executor doesn't conflate `{uuid}` template syntax with literal-string usage.

Purpose: Closes A2 (tools appear in LLM schema on web) and A4-web (operator hears synthesized audio in browser). Plan 04 is decoupled from Plan 05's HTTP route — UAT-W can pass at end of Wave 2 without Plan 05 having shipped.

Output:
- Wave 0 tests `audio_out_protocol.rs` + `web_audio_dispatcher.rs` (Task 1).
- `ChatStreamEvent::AudioOut` variant + inline shape test (Task 2).
- `Block::Audio` state variant (Task 2).
- `WebAudioDispatcher` impl in `iron_hermes_ui::server::web_audio_dispatcher` + `mod.rs` re-export (Task 3).
- Per-turn wiring + `Message::Binary` AudioOut send arm in `ws.rs` (Task 4).
- WASM deps (wasm-bindgen, web-sys, js-sys) in `Cargo.toml` + Blob-URL-first-play client + unconditional Message::Binary recv arm + `Block::Audio` RSX + CSS (Task 5).
- Operator UAT-W blocking checkpoint confirms audible inline browser playback (Task 6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-CONTEXT.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-UI-SPEC.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-01-SUMMARY.md
@crates/iron_hermes_ui/Cargo.toml
@crates/iron_hermes_ui/src/protocol.rs
@crates/iron_hermes_ui/src/state.rs
@crates/iron_hermes_ui/src/server/ws.rs
@crates/iron_hermes_ui/src/server/state.rs
@crates/iron_hermes_ui/src/server/mod.rs
@crates/iron_hermes_ui/src/components/HermesApp/mod.rs
@crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs
@crates/iron_hermes_ui/src/components/warp_hermes.rs
@crates/iron_hermes_ui/assets/main.css
@crates/iron_hermes_ui/assets/warp-ih.css
@crates/iron_hermes_ui/assets/design-tokens.css
@crates/iron_hermes_ui/tests/web_queue_protocol.rs
@crates/ironhermes-agent/src/agent_runtime.rs
@crates/ironhermes-tools/src/send_audio_tool.rs
@crates/ironhermes-gateway/src/telegram.rs

<interfaces>
<!-- Critical signatures and call sites — extracted from PATTERNS so executor doesn't search. -->

Post-Plan-01 TurnRequest + TtsPerTurnWiring:
```rust
pub struct TtsPerTurnWiring {
    pub session_key: ironhermes_core::SessionKey,
    pub audio_dispatcher: Option<std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>>,
}
```

Current ChatStreamEvent enum (crates/iron_hermes_ui/src/protocol.rs:18-44) — extend with one variant:
```rust
#[derive(Serialize, Deserialize, Debug, Clone)]
pub enum ChatStreamEvent {
    Delta { text: String },
    ToolCallStart { name: String, args: String },
    ToolCallEnd { name: String, success: bool },
    Finished { total_tokens: u32 },
    Error { message: String },
    SubagentEvent {},
    QueueUpdated { depth: u32, paused: bool },
    // ADD: AudioOut { mime: String, uuid: String, bytes: Vec<u8> },
}
```

Existing web_key helper (ws.rs:113-119):
```rust
fn web_key(session_id: &str) -> ironhermes_core::session::SessionKey {
    ironhermes_core::session::SessionKey {
        platform: ironhermes_core::types::Platform::Web,
        chat_id: session_id.to_string(),
        user_id: Some("web".into()),
    }
}
```

Existing per-turn channel + binary-frame precedent (warp_hermes.rs:181 — `Message::Binary` server-to-client):
```rust
// warp_hermes.rs:181 — legacy-shell client converts binary frames to UTF-8 then JSON-parses.
// HermesApp's mod.rs needs the same arm UNCONDITIONALLY (REVISION HIGH 7).
```

AudioDispatcher trait (impl target — defined in ironhermes-tools/src/send_audio_tool.rs:24-40):
```rust
#[async_trait]
pub trait AudioDispatcher: Send + Sync {
    async fn send_voice_file(&self, chat_id: &str, path: &PathBuf, thread_id: Option<&str>) -> anyhow::Result<()>;
    async fn send_audio_file(&self, chat_id: &str, path: &PathBuf, thread_id: Option<&str>) -> anyhow::Result<()>;
}
```

WASM Blob URL APIs (REVISION HIGH 4 mandate — required imports for Task 5):
```rust
// In HermesApp/mod.rs AudioOut handler:
use js_sys::Uint8Array;
use wasm_bindgen::JsCast;
use web_sys::{Blob, BlobPropertyBag, Url};

// Construct Blob URL from raw mp3 bytes:
let arr = Uint8Array::from(bytes.as_slice());
let sequence = js_sys::Array::new();
sequence.push(&arr.buffer());
let mut bag = BlobPropertyBag::new();
bag.type_(&mime);
let blob = Blob::new_with_u8_array_sequence_and_options(&sequence, &bag).unwrap();
let blob_url = Url::create_object_url_with_blob(&blob).unwrap();
// Store blob_url keyed by uuid in a Signal map → block_stream.rs reads via use_context.
```

UI-SPEC.md inline audio block RSX shape (note RSX brace interpolation — REVISION MEDIUM 10):
```rust
// In Dioxus RSX, `{uuid}` and `{audio_src}` are template-variable interpolations
// of in-scope let bindings. They are NOT the literal strings "{uuid}" / "{audio_src}".
// Equivalent in non-RSX code: format!("/audio/{}", uuid).
div { class: "wh-block is-ai",
    div { class: "wh-block-head", span { class: "wh-author", "Hermes" } }
    div { class: "wh-block-body wh-audio-body",
        audio {
            controls: true,
            preload: "none",
            "aria-label": "Synthesized audio",
            title: "{uuid}",
            src: "{audio_src}",
        }
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — author audio_out_protocol.rs + web_audio_dispatcher.rs test files</name>
  <files>crates/iron_hermes_ui/tests/audio_out_protocol.rs, crates/iron_hermes_ui/tests/web_audio_dispatcher.rs</files>
  <read_first>
    - crates/iron_hermes_ui/tests/web_queue_protocol.rs (canonical protocol-shape lock test pattern)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/iron_hermes_ui/tests/audio_out_protocol.rs`" + §"`crates/iron_hermes_ui/tests/web_audio_dispatcher.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-VALIDATION.md (rows for D-02-a)
  </read_first>
  <behavior>
    audio_out_protocol.rs MUST contain:
      - `const PROTOCOL_SOURCE: &str = include_str!("../src/protocol.rs");`
      - `#[test] protocol_rs_declares_audio_out_variant`: asserts `PROTOCOL_SOURCE.contains("AudioOut")`.
      - `#[test] protocol_rs_audio_out_has_mime_uuid_bytes_fields`: asserts `PROTOCOL_SOURCE.contains("mime: String")`, `contains("uuid: String")`, `contains("bytes: Vec<u8>")`.
      - `#[test] protocol_rs_audio_out_shape_test_exists`: asserts `PROTOCOL_SOURCE.contains("test_audio_out_json_shape")`.
      - `#[test] mod_rs_handles_audio_out_in_recv_loop`: `const MOD_RS: &str = include_str!("../src/components/HermesApp/mod.rs");` + asserts `MOD_RS.contains("AudioOut")` AND `MOD_RS.contains("Message::Binary")` AND `MOD_RS.contains("create_object_url_with_blob")` (REVISION HIGH 4 + HIGH 7 markers — proves Task 5 landed both the unconditional Binary arm AND the Blob URL first-play).
      - `#[test] audio_out_serializes_to_external_tagged_json`: builds `ChatStreamEvent::AudioOut { mime: "audio/mpeg".into(), uuid: "x".into(), bytes: vec![0xFF, 0xFB] }`, `serde_json::to_string(&ev).unwrap()`, asserts JSON starts with `{"AudioOut":` and contains `"mime":"audio/mpeg"` + `"uuid":"x"`. Round-trips via `serde_json::from_str` + asserts `matches!` `ChatStreamEvent::AudioOut { .. }`.
    web_audio_dispatcher.rs MUST contain:
      - `const SOURCE: &str = include_str!("../src/server/web_audio_dispatcher.rs");`
      - `#[test] dispatcher_impl_for_audio_dispatcher_present`: asserts `SOURCE.contains("AudioDispatcher for WebAudioDispatcher")`.
      - `#[test] dispatcher_struct_holds_tx_and_audio_cache_dir`: asserts `SOURCE.contains("tx:") && SOURCE.contains("UnboundedSender")` AND `SOURCE.contains("audio_cache_dir:")`.
      - `#[test] dispatcher_emits_audio_out_event`: asserts `SOURCE.contains("ChatStreamEvent::AudioOut")`.
      - `#[test] dispatcher_does_not_re_persist`: asserts `!SOURCE.contains("write_all") && !SOURCE.contains("File::create")`.
      - `#[tokio::test] dispatcher_sends_audio_out_on_send_audio_file`: builds `(tx, rx) = tokio::sync::mpsc::unbounded_channel::<ChatStreamEvent>();` + a temp dir containing `{uuid}.mp3` with known bytes; constructs `WebAudioDispatcher::new(tx, temp_dir.path().into())`; calls `.send_audio_file("session", &temp_dir.path().join("00000000-0000-0000-0000-000000000000.mp3"), None).await`; asserts result is `Ok(())` AND `rx.recv()` yields `ChatStreamEvent::AudioOut { mime: "audio/mpeg", uuid: "00000000-0000-0000-0000-000000000000", bytes }` matching file contents.
    Compile-fail acceptable on missing APIs (`ChatStreamEvent::AudioOut`, `WebAudioDispatcher::new`).
  </behavior>
  <action>
    Create both test files in `crates/iron_hermes_ui/tests/`. Mirror the `web_queue_protocol.rs` source-grep test pattern.
    Imports for `audio_out_protocol.rs`: `use iron_hermes_ui::protocol::ChatStreamEvent;`. Verify the crate slug by reading `Cargo.toml`.
    Imports for `web_audio_dispatcher.rs`: `use iron_hermes_ui::server::web_audio_dispatcher::WebAudioDispatcher;` `use iron_hermes_ui::protocol::ChatStreamEvent;` `use ironhermes_tools::AudioDispatcher;` `use std::path::PathBuf;` `use tempfile::TempDir;` `use tokio::sync::mpsc;`.
    Cargo dev-dep check: `tempfile` and `tokio` test-util feature — verify presence; if absent, add to `[dev-dependencies]` of `iron_hermes_ui/Cargo.toml`.
    Server-feature gating: Top-level `#![cfg(feature = "server")]` for `web_audio_dispatcher.rs` (the impl is server-gated); audio_out_protocol.rs is shared protocol — no gating required UNLESS the mod.rs read_first reveals that HermesApp's binary recv arm is server-only (typically not — wasm-target builds reach mod.rs too).
    Commit body notes RED state and the rationale for the Message::Binary + create_object_url_with_blob asserts (these only GREEN after Task 5 lands).
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --tests --features server --no-run 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Files `crates/iron_hermes_ui/tests/audio_out_protocol.rs` and `crates/iron_hermes_ui/tests/web_audio_dispatcher.rs` exist.
    - `grep -c "#\[test\]\|#\[tokio::test\]" crates/iron_hermes_ui/tests/audio_out_protocol.rs` returns ≥ 5.
    - `grep -c "#\[test\]\|#\[tokio::test\]" crates/iron_hermes_ui/tests/web_audio_dispatcher.rs` returns ≥ 5.
    - `grep -c "include_str!" crates/iron_hermes_ui/tests/audio_out_protocol.rs` returns ≥ 2.
    - `grep -c "create_object_url_with_blob" crates/iron_hermes_ui/tests/audio_out_protocol.rs` returns ≥ 1 (REVISION HIGH 4 marker).
    - `grep -c "Message::Binary" crates/iron_hermes_ui/tests/audio_out_protocol.rs` returns ≥ 1 (REVISION HIGH 7 marker).
    - Compile failure acceptable ONLY on missing `ChatStreamEvent::AudioOut`, `WebAudioDispatcher`, or `web_audio_dispatcher` module.
  </acceptance_criteria>
  <done>Both Wave 0 test files exist with named tests including REVISION HIGH 4 + HIGH 7 markers. Commit body notes RED state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ChatStreamEvent::AudioOut variant + inline shape test + Block::Audio variant</name>
  <files>crates/iron_hermes_ui/src/protocol.rs, crates/iron_hermes_ui/src/state.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/protocol.rs (full file — existing `QueueUpdated` + inline `test_queue_updated_json_shape`)
    - crates/iron_hermes_ui/src/state.rs (full file — existing `Block` enum + `BlockEntry`)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/iron_hermes_ui/src/protocol.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-UI-SPEC.md (lines 109-149 — Block::Audio)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 3)
  </read_first>
  <behavior>
    protocol.rs after this task:
      - `ChatStreamEvent` gains `AudioOut { mime: String, uuid: String, bytes: Vec<u8> }` declared after `QueueUpdated`.
      - Variant doc-commented per UI-SPEC.md / D-02-a.
      - Tests mod gains `test_audio_out_json_shape` mirroring `test_queue_updated_json_shape`.
      - No `#[serde(...)]` attribute on AudioOut (default external tagging).
    state.rs after this task:
      - `Block` enum gains `Audio { uuid: String, mime: String }` variant. Position adjacent to existing variants.
    Both files build clean. Wave 0 audio_out_protocol.rs tests for `protocol_rs_declares_audio_out_variant` + `protocol_rs_audio_out_has_mime_uuid_bytes_fields` + `protocol_rs_audio_out_shape_test_exists` + `audio_out_serializes_to_external_tagged_json` GREEN. The remaining 1 test (`mod_rs_handles_audio_out_in_recv_loop`) stays RED until Task 5 (it asserts on `Message::Binary` + `create_object_url_with_blob` in mod.rs).
  </behavior>
  <action>
    Edit `crates/iron_hermes_ui/src/protocol.rs`:
      Step 1 — Locate `ChatStreamEvent` enum (line ~18-44). After `QueueUpdated { depth: u32, paused: bool },` add `AudioOut { mime: String, uuid: String, bytes: Vec<u8> },` with doc comment `/// Phase 36.17.7 D-02-a: synthesized audio delivery to the web client. JSON wire shape (external tagging): {"AudioOut":{"mime":"audio/mpeg","uuid":"<uuidv4>","bytes":[<u8>...]}}. Transmitted as Message::Binary per D-02-a.`
      Step 2 — Locate inline tests mod. After `test_queue_updated_json_shape` add `test_audio_out_json_shape`. Assertions: `assert!(json.starts_with(r#"{"AudioOut":"#))`, `assert!(json.contains("\"mime\":\"audio/mpeg\""))`, `assert!(json.contains("\"uuid\":\"test-uuid\""))`, round-trip `assert!(matches!(parsed, ChatStreamEvent::AudioOut { .. }))`.
      Step 3 — Serde-bytes default: `Vec<u8>` serializes as JSON array. Plan 04 uses array form (no `serde_bytes` dep).

    Edit `crates/iron_hermes_ui/src/state.rs`:
      Step 4 — Locate `Block` enum. Add `Audio { uuid: String, mime: String },` adjacent to existing variants. Doc comment `/// Phase 36.17.7 D-02-a/f: inline audio control rendered in chat transcript at the stream position of each send_audio emit.`
      Step 5 — If `Block` is matched exhaustively elsewhere, add a placeholder `Block::Audio { .. } => { /* rendered by block_stream.rs Task 5 */ }` arm. Document fixed sites in commit body.

    Do NOT modify mod.rs, block_stream.rs, ws.rs, server files in this task. No new deps.
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui 2>&amp;1 | tail -10 ; cargo test -p iron_hermes_ui --lib -- protocol::tests::test_audio_out_json_shape 2>&amp;1 | tail -10 ; cargo test -p iron_hermes_ui --test audio_out_protocol --features server -- protocol_rs_declares_audio_out_variant protocol_rs_audio_out_has_mime_uuid_bytes_fields protocol_rs_audio_out_shape_test_exists audio_out_serializes_to_external_tagged_json 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AudioOut {" crates/iron_hermes_ui/src/protocol.rs` returns ≥ 1.
    - `grep -c "mime: String" crates/iron_hermes_ui/src/protocol.rs` returns ≥ 1.
    - `grep -c "uuid: String" crates/iron_hermes_ui/src/protocol.rs` returns ≥ 1.
    - `grep -c "bytes: Vec<u8>" crates/iron_hermes_ui/src/protocol.rs` returns ≥ 1.
    - `grep -c "test_audio_out_json_shape" crates/iron_hermes_ui/src/protocol.rs` returns ≥ 1.
    - `grep -c "Audio {" crates/iron_hermes_ui/src/state.rs` returns ≥ 1.
    - `cargo build -p iron_hermes_ui` exits 0.
    - `cargo test -p iron_hermes_ui --lib -- protocol::tests::test_audio_out_json_shape` exits 0.
    - The 4 Wave 0 protocol.rs tests pass.
    - `cargo clippy -p iron_hermes_ui --lib -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>`ChatStreamEvent::AudioOut` and `Block::Audio` declared; inline shape test GREEN. Plan 04 Wave 0 protocol tests 4/5 GREEN (5th passes after Task 5).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship WebAudioDispatcher impl in iron_hermes_ui::server::web_audio_dispatcher</name>
  <files>crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs, crates/iron_hermes_ui/src/server/mod.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/mod.rs (existing `pub mod ws;` / `pub mod state;`)
    - crates/iron_hermes_ui/src/server/ws.rs (lines 1-30 — `#[cfg(feature = "server")]` gating idiom)
    - crates/ironhermes-gateway/src/telegram.rs (existing `impl AudioDispatcher for TelegramAdapter` — confirms orphan-rule pattern)
    - crates/ironhermes-tools/src/send_audio_tool.rs (AudioDispatcher trait at lines 24-40)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 2, §Pitfall 2)
  </read_first>
  <behavior>
    After this task:
      - `crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs` exists, contains `pub struct WebAudioDispatcher { tx: UnboundedSender<ChatStreamEvent>, audio_cache_dir: PathBuf }` and `impl ironhermes_tools::AudioDispatcher for WebAudioDispatcher`.
      - Both methods read mp3 bytes via `tokio::fs::read(path).await`, extract uuid from `path.file_stem()`, build `ChatStreamEvent::AudioOut { mime: "audio/mpeg".to_string(), uuid, bytes }`, send via `self.tx.send(event)`.
      - Do NOT write any new file (TextToSpeechTool already persisted).
      - `crates/iron_hermes_ui/src/server/mod.rs` declares `pub mod web_audio_dispatcher;`.
      - File gated `#[cfg(feature = "server")]`.
      - `cargo build -p iron_hermes_ui --features server` exits 0.
      - Plan 04 Wave 0 web_audio_dispatcher.rs tests 5/5 GREEN.
  </behavior>
  <action>
    Create `crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs` per PATTERNS verbatim:
      - Gating: `#![cfg(feature = "server")]` or per-item — match `state.rs` / `ws.rs`.
      - Imports: `use async_trait::async_trait;` `use std::path::PathBuf;` `use std::sync::Arc;` `use tokio::sync::mpsc::UnboundedSender;` `use crate::protocol::ChatStreamEvent;`
      - Struct + `pub fn new(tx, audio_cache_dir) -> Self`.
      - `#[async_trait] impl ironhermes_tools::AudioDispatcher for WebAudioDispatcher`.
      - `send_voice_file` forwards to `send_audio_file` (D-02-e: raw mp3 for both).
      - `send_audio_file` body:
        - `let bytes = tokio::fs::read(path).await.map_err(|e| anyhow::anyhow!("read audio file: {e}"))?;`
        - `let uuid = path.file_stem().and_then(|s| s.to_str()).unwrap_or("unknown").to_string();`
        - `let event = ChatStreamEvent::AudioOut { mime: "audio/mpeg".to_string(), uuid, bytes };`
        - `self.tx.send(event).map_err(|e| anyhow::anyhow!("ws send failed: {e}"))?;`
        - `Ok(())`
      - Doc comments per PATTERNS.
    Edit `crates/iron_hermes_ui/src/server/mod.rs`:
      - Add `pub mod web_audio_dispatcher;`.
    Do NOT place in `ironhermes-tools` (Pitfall 2). Do NOT re-persist. Do NOT generate new UUID. No new deps.
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --features server 2>&amp;1 | tail -10 ; cargo test -p iron_hermes_ui --test web_audio_dispatcher --features server 2>&amp;1 | tail -20 ; grep -c "AudioDispatcher for WebAudioDispatcher" crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs ; grep -c "pub mod web_audio_dispatcher" crates/iron_hermes_ui/src/server/mod.rs</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs` exists.
    - `grep -c "pub struct WebAudioDispatcher" crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs` returns 1.
    - `grep -c "AudioDispatcher for WebAudioDispatcher" crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs` returns 1.
    - `grep -c "ChatStreamEvent::AudioOut" crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs` returns ≥ 1.
    - `grep -c "tokio::fs::read" crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs` returns ≥ 1.
    - `grep -cE "write_all|File::create" crates/iron_hermes_ui/src/server/web_audio_dispatcher.rs` returns 0.
    - `grep -c "pub mod web_audio_dispatcher" crates/iron_hermes_ui/src/server/mod.rs` returns 1.
    - `cargo build -p iron_hermes_ui --features server` exits 0.
    - `cargo test -p iron_hermes_ui --test web_audio_dispatcher --features server` exits 0; 5/5 GREEN.
    - `cargo clippy -p iron_hermes_ui --features server -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>WebAudioDispatcher callable from ws.rs (next task). Wave 0 web_audio_dispatcher.rs tests 5/5 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire WebAudioDispatcher per-turn in ws.rs + add Message::Binary send arm for AudioOut</name>
  <files>crates/iron_hermes_ui/src/server/ws.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/ws.rs (full file — lines 113-119 `web_key`, lines ~216 per-turn `(tx, rx)`, recv-loop sending `ChatStreamEvent` via `Message::Text`, `run_web_turn` / `runtime.run_turn` call site)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/iron_hermes_ui/src/server/ws.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Web Runtime Constructor Detail, §Pitfall 3)
  </read_first>
  <behavior>
    After this task:
      - `ws.rs` per-turn block (between channel construction at ~216 and `run_web_turn` call) contains:
        - `let audio_tx = tx.clone();`
        - `let audio_cache_dir = ironhermes_core::constants::get_hermes_home().join("audio_cache");`
        - `let web_audio_dispatcher = std::sync::Arc::new(crate::server::web_audio_dispatcher::WebAudioDispatcher::new(audio_tx, audio_cache_dir));`
        - `let tts_wiring = Some(ironhermes_agent::TtsPerTurnWiring { session_key: web_key(&session_id), audio_dispatcher: Some(web_audio_dispatcher as std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>) });`
      - `tts_wiring` plumbed into `TurnRequest`. Option A (ws.rs builds TurnRequest directly) preferred; Option B (signature change to `run_web_turn`) if needed.
      - Recv loop gains a `Message::Binary` arm for `ChatStreamEvent::AudioOut`:
        ```
        match &ev {
            ChatStreamEvent::AudioOut { .. } => {
                let bytes = serde_json::to_vec(&ev).unwrap_or_default();
                let _ = socket.send_raw(Message::Binary(bytes.into())).await;
            }
            _ => {
                let json = serde_json::to_string(&ev).unwrap_or_default();
                let _ = socket.send_raw(Message::Text(json)).await;
            }
        }
        ```
      - Non-AudioOut events stay `Message::Text` (Pitfall 3).
      - `cargo build -p iron_hermes_ui --features server` exits 0; `cargo test` exits 0.
      - Source-grep: ws.rs contains `WebAudioDispatcher::new`, `session_key: Some(`, `Message::Binary`, `ironhermes_agent::TtsPerTurnWiring`.
  </behavior>
  <action>
    Edit `crates/iron_hermes_ui/src/server/ws.rs`:
      Step 1 — Verify `web_key` helper at lines 113-119. If missing, port from &lt;interfaces&gt;.
      Step 2 — Locate `let (tx, rx) = mpsc::unbounded_channel::<ChatStreamEvent>();` at ~ws.rs:216.
      Step 3 — Insert dispatcher construction block from &lt;behavior&gt; with fully-qualified paths.
      Step 4 — Thread `tts_wiring` into `TurnRequest`. Grep `ws.rs` for `run_web_turn` / `runtime.run_turn`. Choose Option A (extend TurnRequest literal here) or Option B (signature change to `run_web_turn`). Document choice in commit body.
      Step 5 — Add `Message::Binary` send arm. Locate the existing `socket.send_raw(Message::Text(...))` site. Replace with the match block from &lt;behavior&gt;. Ensure imports include `Message::Binary` from `dioxus_fullstack` (verify by reading existing `Message::Text` import).
      Step 6 — Verify `bytes.into()` produces what `Message::Binary` expects. If `Vec<u8>` direct, drop `.into()`; if `Bytes`, keep.
    Do NOT switch non-AudioOut events to Binary (Pitfall 3). Do NOT add a client→server binary handler (T-ws-binary-spoofing). Do NOT add HTTP route or GC code (Plan 05).
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --features server 2>&amp;1 | tail -10 ; cargo test -p iron_hermes_ui --features server 2>&amp;1 | tail -10 ; grep -c "WebAudioDispatcher::new" crates/iron_hermes_ui/src/server/ws.rs ; grep -c "session_key: Some(" crates/iron_hermes_ui/src/server/ws.rs ; grep -c "Message::Binary" crates/iron_hermes_ui/src/server/ws.rs ; grep -c "ironhermes_agent::TtsPerTurnWiring" crates/iron_hermes_ui/src/server/ws.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WebAudioDispatcher::new" crates/iron_hermes_ui/src/server/ws.rs` returns 1.
    - `grep -c "session_key: Some(" crates/iron_hermes_ui/src/server/ws.rs` returns ≥ 1.
    - `grep -c "Message::Binary" crates/iron_hermes_ui/src/server/ws.rs` returns ≥ 1.
    - `grep -c "ironhermes_agent::TtsPerTurnWiring" crates/iron_hermes_ui/src/server/ws.rs` returns ≥ 1.
    - `grep -c "ChatStreamEvent::AudioOut" crates/iron_hermes_ui/src/server/ws.rs` returns ≥ 1.
    - `grep -c "audio_cache" crates/iron_hermes_ui/src/server/ws.rs` returns ≥ 1.
    - `cargo build -p iron_hermes_ui --features server` exits 0.
    - `cargo test -p iron_hermes_ui --features server` exits 0.
    - `cargo clippy -p iron_hermes_ui --features server -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>ws.rs constructs WebAudioDispatcher per turn, plumbs tts_wiring into TurnRequest, sends AudioOut as Message::Binary. text_to_speech + send_audio appear in LLM schema for every web turn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Add WASM deps (Cargo.toml); UNCONDITIONAL Message::Binary recv arm in HermesApp (HIGH 7); MANDATORY Blob URL first-play via web_sys::Url::create_object_url_with_blob (HIGH 4); Block::Audio RSX render with RSX interpolation note (MEDIUM 10); CSS rules</name>
  <files>crates/iron_hermes_ui/Cargo.toml, crates/iron_hermes_ui/src/components/HermesApp/mod.rs, crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs, crates/iron_hermes_ui/assets/main.css</files>
  <read_first>
    - crates/iron_hermes_ui/Cargo.toml (full file — verify which of wasm-bindgen / web-sys / js-sys / gloo-* are already declared; if any are absent, this task adds them)
    - crates/iron_hermes_ui/src/components/HermesApp/mod.rs (full file — ws recv loop, existing `ChatStreamEvent` match arms, ANY existing `Message::Binary` handling site)
    - crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs (full file — how `Block::Text` / `Block::Tool` are rendered; mirror for `Block::Audio`)
    - crates/iron_hermes_ui/src/components/warp_hermes.rs (line ~181 — legacy-shell `Message::Binary` UTF-8 conversion reference)
    - crates/iron_hermes_ui/assets/main.css (existing `.wh-block-body` rule + token usage)
    - crates/iron_hermes_ui/assets/warp-ih.css (read-only — confirm `--accent-primary` and `--space-2` tokens)
    - crates/iron_hermes_ui/assets/design-tokens.css (read-only — token availability)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-UI-SPEC.md (full file — design contract)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/iron_hermes_ui/src/components/HermesApp/*`")
  </read_first>
  <behavior>
    After this task:

    **REVISION HIGH 4 — Blob URL first-play (MANDATORY, no fallback)**:
      - `Cargo.toml` has `wasm-bindgen`, `web-sys` (with `Blob`, `Url`, `HtmlAudioElement`, `BlobPropertyBag` features), `js-sys` declared as dependencies (use workspace versions if already declared at workspace level; otherwise specify minimum `0.3` for web-sys, `0.2` for wasm-bindgen and js-sys). If already present at the workspace level, just add `{ workspace = true, features = [...extra features needed...] }` for web-sys.
      - HermesApp's ws recv loop's `ChatStreamEvent::AudioOut { mime, uuid, bytes }` match arm:
        - Constructs a Blob from `bytes`: builds `js_sys::Uint8Array::from(bytes.as_slice())`, wraps in `js_sys::Array`, calls `web_sys::Blob::new_with_u8_array_sequence_and_options(&arr, &BlobPropertyBag::new().type_(&mime))`.
        - Generates Blob URL: `web_sys::Url::create_object_url_with_blob(&blob).expect("blob url")`.
        - Stores `blob_url` keyed by `uuid` in a `use_context::<Signal<HashMap<String, String>>>` map so block_stream.rs can look it up at render time.
        - Pushes `BlockEntry { id: <stream-id>, block: Block::Audio { uuid: uuid.clone(), mime: mime.clone() } }` into the transcript stream signal.
      - First-play does NOT depend on Plan 05's HTTP route. The Blob URL is generated client-side from the binary WS frame bytes — the audio is playable immediately, before Plan 05 has shipped anything.

    **REVISION HIGH 7 — UNCONDITIONAL Message::Binary recv arm**:
      - HermesApp's ws message handler has a `Message::Binary(bytes)` arm that:
        1. Converts bytes to UTF-8 via `String::from_utf8_lossy(&bytes).to_string()` (mirrors warp_hermes.rs:181 pattern).
        2. Deserializes the JSON as `ChatStreamEvent` via `serde_json::from_str`.
        3. Dispatches into the SAME `ChatStreamEvent` match used by `Message::Text` frames.
      - This arm is added EVEN IF an existing binary arm exists in mod.rs — the existing arm may not route binary frames into the ChatStreamEvent dispatch (legacy-shell handles binary differently). The new arm ensures AudioOut frames reach the AudioOut handler.

    **REVISION MEDIUM 10 — RSX interpolation clarification**:
      - block_stream.rs's `Block::Audio { uuid, mime: _ }` match arm uses Dioxus RSX brace-interpolation: `audio { controls: true, src: "{audio_src}" }` where `audio_src` is an in-scope `let audio_src: String = ...;` binding. The braces ARE NOT the literal string "{audio_src}" — they are Dioxus template syntax that interpolates the variable's value. Equivalent in non-RSX: `format!("/audio/{}", uuid)`. The `audio_src` binding is computed: `let audio_src = blob_url_map.read().get(uuid).cloned().unwrap_or_else(|| format!("/audio/{}", uuid));` — Blob URL takes precedence; the `/audio/:uuid` fallback is the Plan 05 page-reload path.

    **Block::Audio RSX render**:
      - Emits per UI-SPEC.md §Component Inventory: `div { class: "wh-block is-ai", div { class: "wh-block-head", span { class: "wh-author", "Hermes" } }, div { class: "wh-block-body wh-audio-body", audio { controls: true, preload: "none", "aria-label": "Synthesized audio", title: "{uuid}", src: "{audio_src}" } } }`.
      - NO `autoplay` attribute (D-02-b).
      - NO `<button>` wrapper.

    **CSS rules in main.css**:
      - `.wh-audio-body { padding-top: var(--space-2); }`
      - `.wh-audio-body audio { width: 100%; display: block; }`
      - `audio:focus-visible { outline: 2px solid var(--accent-primary); outline-offset: 2px; }`
      - All declarations use existing tokens (no hardcoded hex per UI-SPEC.md).

    Final build/test outcomes:
      - `cargo build -p iron_hermes_ui` exits 0 (WASM + server feature sets).
      - `cargo build -p iron_hermes_ui --features server` exits 0.
      - Wave 0 `audio_out_protocol.rs::mod_rs_handles_audio_out_in_recv_loop` GREEN (the AudioOut + Message::Binary + create_object_url_with_blob source-greps all pass).
  </behavior>
  <action>
    Edit `crates/iron_hermes_ui/Cargo.toml` (REVISION HIGH 4 dep additions):
      Step 0 — Read full file. Check `[dependencies]` for existing `wasm-bindgen`, `web-sys`, `js-sys`.
        - If absent at crate level AND absent at workspace level: add at crate level `wasm-bindgen = "0.2"`, `js-sys = "0.3"`, `web-sys = { version = "0.3", features = ["Blob", "BlobPropertyBag", "Url", "HtmlAudioElement"] }`.
        - If present at workspace level: add `wasm-bindgen = { workspace = true }`, `js-sys = { workspace = true }`, `web-sys = { workspace = true, features = ["Blob", "BlobPropertyBag", "Url", "HtmlAudioElement"] }` (the workspace dep may already include features — verify and merge to avoid feature regression).
        - Document Cargo.toml diff in commit body.
        - Run `cargo build -p iron_hermes_ui --target wasm32-unknown-unknown` (if wasm target is set up) OR `cargo check -p iron_hermes_ui` to confirm the deps resolve.

    Edit `crates/iron_hermes_ui/src/components/HermesApp/mod.rs`:
      Step 1 — Locate the ws recv loop + existing `ChatStreamEvent` match (likely arms for `Delta`, `Finished`, `QueueUpdated`). Locate ALSO the `Message::Text` / `Message::Binary` dispatch at the outer match.

      Step 2 (REVISION HIGH 7 — UNCONDITIONAL) — Add (or replace) the `Message::Binary(bytes)` arm:
        ```
        Message::Binary(bytes) => {
            // Phase 36.17.7 D-02-a / HIGH 7: binary frames carry AudioOut JSON.
            // Convert to UTF-8 and dispatch through the existing ChatStreamEvent match.
            let text = String::from_utf8_lossy(&bytes).to_string();
            if let Ok(ev) = serde_json::from_str::<ChatStreamEvent>(&text) {
                // ... dispatch into the same handler used by Message::Text ...
                handle_chat_stream_event(ev);
            }
        }
        ```
        If the existing match handles Binary differently (e.g. legacy-shell passes raw bytes elsewhere), the new arm coexists for HermesApp specifically — verify the route in mod.rs takes precedence over warp_hermes.rs for HermesApp's WS surface. Refactor `handle_chat_stream_event` into a helper if the inline dispatch is duplicated.

      Step 3 (REVISION HIGH 4 — Blob URL first-play) — Add the `ChatStreamEvent::AudioOut { mime, uuid, bytes }` arm:
        ```
        ChatStreamEvent::AudioOut { mime, uuid, bytes } => {
            // Phase 36.17.7 D-02-a + REVISION HIGH 4: construct Blob URL for first-play.
            use js_sys::{Array, Uint8Array};
            use wasm_bindgen::JsCast;
            use web_sys::{Blob, BlobPropertyBag, Url};

            let arr = Uint8Array::from(bytes.as_slice());
            let sequence = Array::new();
            sequence.push(&arr.buffer());
            let mut bag = BlobPropertyBag::new();
            bag.type_(&mime);
            let blob = Blob::new_with_u8_array_sequence_and_options(&sequence, &bag)
                .expect("Phase 36.17.7 D-02-a: failed to construct Blob from AudioOut bytes");
            let blob_url = Url::create_object_url_with_blob(&blob)
                .expect("Phase 36.17.7 D-02-a: failed to create object URL for AudioOut Blob");

            // Store blob_url keyed by uuid into the shared Signal<HashMap<String, String>>
            // (use_context::<Signal<HashMap<String, String>>> at HermesApp root).
            blob_url_map.write().insert(uuid.clone(), blob_url);

            // Push Block::Audio into the transcript stream signal.
            transcript_stream.write().push(BlockEntry {
                id: next_stream_id(),
                block: Block::Audio { uuid: uuid.clone(), mime: mime.clone() },
            });
        }
        ```
        Adjust identifier names to match what mod.rs actually uses for the transcript stream signal (`block_entries`, `transcript_signal`, etc.). The `blob_url_map` is a NEW signal — at HermesApp root, add `let blob_url_map = use_signal(|| HashMap::<String, String>::new()); use_context_provider(|| blob_url_map);` so block_stream.rs can read via `let blob_url_map = use_context::<Signal<HashMap<String, String>>>();`.

    Edit `crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs`:
      Step 4 (REVISION MEDIUM 10 — RSX interpolation note) — In the `match block { ... }`, add the `Block::Audio { uuid, mime: _ }` arm:
        ```
        Block::Audio { uuid, mime: _ } => {
            // Phase 36.17.7 D-02-a/f: inline audio control per send_audio emit.
            // Lookup blob URL from the HighestApp-scope signal; fall back to /audio/:uuid
            // (the fallback path resolves once Plan 05 ships the HTTP replay route).
            let blob_url_map = use_context::<Signal<std::collections::HashMap<String, String>>>();
            let audio_src: String = blob_url_map
                .read()
                .get(uuid)
                .cloned()
                .unwrap_or_else(|| format!("/audio/{}", uuid));
            // RSX BRACE INTERPOLATION NOTE (REVISION MEDIUM 10):
            // In Dioxus RSX, `{uuid}` and `{audio_src}` are template-variable
            // interpolations of the in-scope let bindings — NOT literal strings.
            // Equivalent in non-RSX: format!("/audio/{}", uuid).
            rsx! {
                div { class: "wh-block is-ai",
                    div { class: "wh-block-head",
                        span { class: "wh-author", "Hermes" }
                    }
                    div { class: "wh-block-body wh-audio-body",
                        audio {
                            controls: true,
                            preload: "none",
                            "aria-label": "Synthesized audio",
                            title: "{uuid}",
                            src: "{audio_src}",
                        }
                    }
                }
            }
        }
        ```
      Step 5 — Confirm no `Block::Audio` arm is left as `unreachable!()` from Task 2's Step 5 — overwrite with the real implementation. Confirm no `autoplay` attribute appears.

    Edit `crates/iron_hermes_ui/assets/main.css`:
      Step 6 — Append the 3 rules from &lt;behavior&gt; at end of file. Use ONLY tokens (`var(--space-2)`, `var(--accent-primary)`). No hardcoded hex.

    Do NOT touch warp-ih.css or design-tokens.css. Do NOT gate behind `legacy-shell` (UI-SPEC.md).
    Do NOT fall back to "src='/audio/{uuid}' only" — Blob URL is mandatory per REVISION HIGH 4.
    Do NOT make the Message::Binary arm conditional — REVISION HIGH 7 requires unconditional addition.
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui 2>&amp;1 | tail -10 ; cargo build -p iron_hermes_ui --features server 2>&amp;1 | tail -10 ; cargo test -p iron_hermes_ui --test audio_out_protocol --features server -- mod_rs_handles_audio_out_in_recv_loop 2>&amp;1 | tail -10 ; grep -c "ChatStreamEvent::AudioOut" crates/iron_hermes_ui/src/components/HermesApp/mod.rs ; grep -c "Message::Binary" crates/iron_hermes_ui/src/components/HermesApp/mod.rs ; grep -c "create_object_url_with_blob" crates/iron_hermes_ui/src/components/HermesApp/mod.rs ; grep -c "Block::Audio" crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs ; grep -c "wh-audio-body" crates/iron_hermes_ui/assets/main.css ; grep -c "audio:focus-visible" crates/iron_hermes_ui/assets/main.css ; grep -c "var(--accent-primary)" crates/iron_hermes_ui/assets/main.css ; grep -cE "web-sys" crates/iron_hermes_ui/Cargo.toml ; grep -cE "wasm-bindgen" crates/iron_hermes_ui/Cargo.toml ; grep -cE "js-sys" crates/iron_hermes_ui/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - **REVISION HIGH 4 lock: `grep -c "create_object_url_with_blob" crates/iron_hermes_ui/src/components/HermesApp/mod.rs` returns ≥ 1.**
    - **REVISION HIGH 4 lock: `grep -cE "web-sys" crates/iron_hermes_ui/Cargo.toml` returns ≥ 1.**
    - **REVISION HIGH 4 lock: `grep -cE "wasm-bindgen" crates/iron_hermes_ui/Cargo.toml` returns ≥ 1.**
    - **REVISION HIGH 4 lock: `grep -cE "js-sys" crates/iron_hermes_ui/Cargo.toml` returns ≥ 1.**
    - **REVISION HIGH 7 lock: `grep -c "Message::Binary" crates/iron_hermes_ui/src/components/HermesApp/mod.rs` returns ≥ 1.**
    - **REVISION HIGH 7 lock: `grep -c "ChatStreamEvent::AudioOut" crates/iron_hermes_ui/src/components/HermesApp/mod.rs` returns ≥ 1.**
    - **REVISION MEDIUM 10 marker: `grep -c "RSX BRACE INTERPOLATION\|RSX interpolation\|template-variable interpolation" crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs` returns ≥ 1.**
    - `grep -c "Block::Audio" crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs` returns ≥ 1.
    - `grep -c "Block::Audio" crates/iron_hermes_ui/src/components/HermesApp/mod.rs` returns ≥ 1.
    - `grep -c "controls: true" crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs` returns ≥ 1.
    - `grep -cE "autoplay" crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs` returns 0.
    - `grep -c "aria-label" crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs` returns ≥ 1.
    - `grep -c "wh-audio-body" crates/iron_hermes_ui/assets/main.css` returns ≥ 1.
    - `grep -c "audio:focus-visible" crates/iron_hermes_ui/assets/main.css` returns ≥ 1.
    - `grep -c "var(--accent-primary)" crates/iron_hermes_ui/assets/main.css` returns ≥ 1.
    - New CSS contains no hex literals: `git diff crates/iron_hermes_ui/assets/main.css | grep -v '^#' | grep -E '^\+' | grep -cE "#[0-9a-fA-F]{3,8}"` returns 0.
    - `cargo build -p iron_hermes_ui` exits 0.
    - `cargo build -p iron_hermes_ui --features server` exits 0.
    - `cargo test -p iron_hermes_ui --test audio_out_protocol --features server -- mod_rs_handles_audio_out_in_recv_loop` exits 0.
    - `cargo clippy -p iron_hermes_ui -- -D warnings` exits 0.
    - Commit body documents: (a) WASM dep additions/upgrades in Cargo.toml, (b) the `handle_chat_stream_event` helper extraction if it was needed.
  </acceptance_criteria>
  <done>HermesApp handles AudioOut via Blob URL first-play (REVISION HIGH 4); UNCONDITIONAL Message::Binary recv arm shipped (REVISION HIGH 7); Block::Audio RSX render with interpolation note (REVISION MEDIUM 10); CSS rules per UI-SPEC.md. UAT-W first-play is now decoupled from Plan 05.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Operator UAT-W — browser inline audio control playback via Blob URL first-play (A4 web + A2 acceptance gate)</name>
  <what-built>
    Plan 04 Tasks 1-5 have completed:
    - Wave 0 tests `audio_out_protocol.rs` + `web_audio_dispatcher.rs` GREEN.
    - `ChatStreamEvent::AudioOut` + `Block::Audio` variants (Task 2).
    - `WebAudioDispatcher` impl in iron_hermes_ui::server::web_audio_dispatcher (Task 3).
    - ws.rs per-turn `WebAudioDispatcher` construction + `Message::Binary` send arm for AudioOut frames (Task 4).
    - **REVISION HIGH 4**: HermesApp client constructs Blob URL via `web_sys::Url::create_object_url_with_blob` for first-play — UAT-W audible play does NOT require Plan 05's HTTP route.
    - **REVISION HIGH 7**: UNCONDITIONAL `Message::Binary` recv arm in HermesApp routes binary frames through ChatStreamEvent dispatch.
    - **REVISION MEDIUM 10**: `block_stream.rs` Block::Audio arm uses RSX brace interpolation (`src: "{audio_src}"`) with a clarifying comment.
    - CSS rules in main.css apply existing tokens.
  </what-built>
  <how-to-verify>
    Pre-flight:
      1. `echo $IRONHERMES_HOME` (recommend `/tmp/uat-36.17.7-home`).
      2. Confirm `voice` toolset enabled in `$IRONHERMES_HOME/config.yaml`.
      3. Confirm host audio output unmuted.
      4. Build + start the server: `cargo run -p iron_hermes_ui --features server` OR `dx serve` per project convention. Note URL.

    UAT-W Steps:
      1. Open the iron_hermes_ui URL in a desktop browser (Chrome / Firefox / Safari).
      2. Open browser devtools → Console + Network tabs.
      3. In the chat input, send: `Summarize the top three Hacker News stories right now as a 30-second podcast hosted by a calm female voice. Use text_to_speech then send_audio.`
      4. Observe the transcript:
         - Agent text streams normally (Delta events).
         - At the moment `send_audio` fires, an inline `<audio controls>` element appears in the transcript with play button visible.
         - In devtools console, NO error like "failed to create blob URL" or "audio src 404".
         - Inspect the `<audio>` element's `src` attribute via devtools — it should start with `blob:` (NOT `/audio/:uuid` — that's the page-reload path).
         - No `autoplay` (audio should NOT start on its own — D-02-b).
      5. Click the play button on the audio control. Confirm audible speech, female voice, summary of HN stories.
      6. Devtools Network tab: confirm the audio bytes arrived via the WebSocket connection (binary frame visible in the WS message log), NOT via a separate `GET /audio/{uuid}` HTTP request. (Plan 05 ships the HTTP route — UAT-W proves it's not required for first-play.)
      7. (Optional UAT-R preview — Plan 05 owns the canonical test): refresh the browser. The transcript reloads. Locate the previous audio block. The `<audio>` src now points at `/audio/{uuid}` (Blob URL is session-scoped). Click play — expect 404 until Plan 05 ships. This is EXPECTED; UAT-W passes without it.

    Failure modes to call out in resume-signal:
      - No `<audio>` element appears: Block::Audio render arm bug — re-check block_stream.rs Task 5 Step 4.
      - `<audio>` appears but src is `/audio/{uuid}` (not `blob:`): Blob URL construction failed — check console for Url::create_object_url errors; re-check mod.rs Task 5 Step 3.
      - `<audio>` plays but agent text mentions `say` or `hyperframes-media`: tools-not-registered — re-check ws.rs Task 4 Step 3-4.
      - Visual: audio control overflows the block — CSS regression; revert to UI-SPEC.md `width: 100%`.
      - Network tab shows GET /audio/{uuid} for first-play (NOT WebSocket binary): the AudioOut payload didn't make it through; check ws.rs Message::Binary send arm.
  </how-to-verify>
  <resume-signal>Type "approved" if Step 5 produced audible browser playback via Blob URL (Step 4 confirmed `src` starts with `blob:`), OR describe the failure (which step + observed-vs-expected + browser-console / network-tab evidence).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → iron_hermes_ui ws | Client-controlled input crosses existing ws boundary (T-ws-binary-spoofing: server→client only — Plan 04 does NOT add a client→server binary handler). |
| iron_hermes_ui ws → browser via Message::Binary | Server sends AudioOut JSON-encoded as bytes. JSON is well-formed by `serde_json::to_vec`; no injection. |
| TextToSpeechTool → audio_cache disk | Existing 36.17.5 T-output-path mitigation. Plan 04 reads only. |
| WebAudioDispatcher → ws tx | In-process channel only. |
| HermesApp Blob URL construction | Browser-side URL.createObjectURL produces session-scoped blob URLs. No external network call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ws-binary-spoofing | Spoofing | ws.rs Message::Binary handler | accept | Server-to-client only. Recv path (client → server) accepts Message::Text only. |
| T-36.17.7-04-01 | Information Disclosure | WebAudioDispatcher reads from audio_cache | mitigate | path arg from in-process TextToSpeechTool construction (well-known canonicalized form). |
| T-36.17.7-04-02 | Tampering | AudioOut JSON shape | mitigate | External-tagged serde matches all other ChatStreamEvent variants. Inline `test_audio_out_json_shape` locks wire format. |
| T-36.17.7-04-03 | Information Disclosure | Blob URL leakage via HTML inspect | accept | Blob URLs are session-scoped (revoked on tab close). Single-operator deployment. |
| T-36.17.7-04-SC | Tampering | cargo deps (wasm-bindgen, web-sys, js-sys NEW) | mitigate | REVISION HIGH 4 introduces 3 WASM deps. All three are well-established Rust WASM ecosystem crates owned by `rustwasm` GitHub org (`wasm-bindgen`: 1B+ monthly downloads, `web-sys`: 800M+, `js-sys`: 800M+). No [SLOP] or [SUS] verdict. Task 5 commit body documents version-pin or workspace-dep verdict. |
</threat_model>

<verify_plan>
1. `cargo build -p iron_hermes_ui --features server` exits 0.
2. `cargo build -p iron_hermes_ui` exits 0.
3. `cargo test -p iron_hermes_ui --test audio_out_protocol --features server` exits 0 (5/5).
4. `cargo test -p iron_hermes_ui --test web_audio_dispatcher --features server` exits 0 (5/5).
5. `cargo test -p iron_hermes_ui --features server` exits 0 (full crate).
6. `cargo clippy -p iron_hermes_ui --features server -- -D warnings` exits 0.
7. `grep -c "session_key: Some(" crates/iron_hermes_ui/src/server/ws.rs` returns ≥ 1.
8. `grep -c "Message::Binary" crates/iron_hermes_ui/src/server/ws.rs` returns ≥ 1.
9. `grep -c "AudioOut" crates/iron_hermes_ui/src/protocol.rs` returns ≥ 2.
10. `grep -c "Block::Audio" crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs` returns ≥ 1.
11. **REVISION HIGH 4 lock: `grep -c "create_object_url_with_blob" crates/iron_hermes_ui/src/components/HermesApp/mod.rs` returns ≥ 1.**
12. **REVISION HIGH 4 lock: `grep -cE "web-sys|wasm-bindgen|js-sys" crates/iron_hermes_ui/Cargo.toml` returns ≥ 3.**
13. **REVISION HIGH 7 lock: `grep -c "Message::Binary" crates/iron_hermes_ui/src/components/HermesApp/mod.rs` returns ≥ 1.**
14. **REVISION MEDIUM 10 marker: `grep -c "template-variable interpolation\|RSX BRACE INTERPOLATION\|RSX interpolation" crates/iron_hermes_ui/src/components/HermesApp/block_stream.rs` returns ≥ 1.**
15. UAT-W checkpoint approved by operator with `src` starts-with `blob:` (REVISION HIGH 4 Step 4 verification).
16. No regression in 36.17.5 invariants.
</verify_plan>

<success_criteria>
- ChatStreamEvent::AudioOut + Block::Audio variants with inline shape tests.
- WebAudioDispatcher impl ships and is wired per-turn in ws.rs with Message::Binary send arm.
- WASM deps in Cargo.toml.
- HermesApp constructs Blob URL via web_sys::Url::create_object_url_with_blob for first-play (REVISION HIGH 4).
- HermesApp has UNCONDITIONAL Message::Binary recv arm (REVISION HIGH 7).
- Block::Audio RSX render uses brace interpolation with clarifying comment (REVISION MEDIUM 10).
- CSS rules use existing tokens (no new hex).
- Operator UAT-W confirms audible browser playback via Blob URL — INDEPENDENT of Plan 05.
- Zero regression in iron_hermes_ui test suite or 36.17.5 invariants.
- 3 new WASM deps approved (wasm-bindgen, web-sys, js-sys).
</success_criteria>

<output>
Create `.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-04-SUMMARY.md` documenting:
- 11 files modified (Wave 0 tests + Cargo.toml + protocol + state + 3 server files + 2 HermesApp files + CSS)
- Source-grep gate counts from &lt;verify_plan&gt;
- Wave 0 test counts: 5/5 + 5/5 GREEN
- REVISION HIGH 4 verification: `src` starts with `blob:` observation from UAT-W Step 4 + network-tab evidence
- REVISION HIGH 7 verification: Message::Binary recv arm count + whether handle_chat_stream_event helper was extracted
- REVISION MEDIUM 10 verification: RSX interpolation comment text + location
- Cargo.toml dep changes (workspace vs crate-level resolution for wasm-bindgen, web-sys, js-sys)
- UAT-W transcript: browser, blob URL form, audible result, console errors
- D-05 prep: `session_key: Some(` literal count in ws.rs
- run_web_turn signature decision (Option A or B from Task 4)
- must_haves achieved: all 6 truths; all 8 artifacts; both key_links
</output>
