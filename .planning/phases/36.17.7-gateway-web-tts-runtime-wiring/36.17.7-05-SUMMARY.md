---
phase: 36.17.7
plan: 05
subsystem: iron_hermes_ui (audio cache infra) + ironhermes-core/tools (toolset Registered column)
tags: [tts, audio-cache, gc, replay-route, http, toolset, registered-column, invariants, d-05, d-06]
requires:
  - 36.17.7-01-SUMMARY.md  # TtsPerTurnWiring + AgentRuntime per-turn registration
  - 36.17.7-04-SUMMARY.md  # WebAudioDispatcher + AudioOut + Blob URL first-play
provides:
  - AudioCacheConfig (Config.audio_cache) with defaults
  - GET /audio/:uuid HTTP replay route (cache-miss + page-reload replay) with T-path-traversal mitigations
  - gc_sweep_audio_cache + run_audio_cache_gc_loop (startup + periodic GC)
  - AppState::init GC wire-up (startup sweep + periodic tokio task)
  - ToolRegistry::is_tts_registered_live + tts_registration_status accessor
  - CommandContext.tts_registration_status field + dispatch-site threading
  - toolset list Registered column (Live / Inspection / —) — D-06 Path B
  - invariants_36_17_7.rs — 5 cross-crate D-05 source-grep guards (A5)
affects:
  - crates/ironhermes-core/src/config.rs (AudioCacheConfig)
  - crates/iron_hermes_ui/src/server/audio_route.rs (NEW)
  - crates/iron_hermes_ui/src/server/audio_cache.rs (NEW)
  - crates/iron_hermes_ui/src/server/state.rs (GC wire-up)
  - crates/ironhermes-tools/src/registry.rs (is_tts_registered_live)
  - crates/ironhermes-core/src/commands/context.rs (TtsRegistrationStatus)
  - crates/ironhermes-core/src/commands/handlers.rs (dispatch threading)
  - crates/ironhermes-core/src/commands/toolset_display.rs (Registered column)
  - crates/ironhermes-cli/src/toolset_cmd.rs (CLI Registered column)
  - crates/ironhermes-tools/src/toolset_session.rs (build_rows Registered)
  - crates/ironagent-tools-api/src/toolset_session.rs (Registered parity)
  - crates/ironhermes-agent/tests/invariants_36_17_7.rs (NEW)
key-files:
  created:
    - crates/iron_hermes_ui/src/server/audio_route.rs
    - crates/iron_hermes_ui/src/server/audio_cache.rs
    - crates/iron_hermes_ui/tests/audio_route.rs
    - crates/iron_hermes_ui/tests/audio_gc.rs
    - crates/ironhermes-agent/tests/invariants_36_17_7.rs
  modified:
    - crates/ironhermes-core/src/config.rs
    - crates/iron_hermes_ui/src/server/mod.rs
    - crates/iron_hermes_ui/src/server/state.rs
    - crates/ironhermes-tools/src/registry.rs
    - crates/ironhermes-tools/src/toolset_session.rs
    - crates/ironagent-tools-api/src/toolset_session.rs
    - crates/ironhermes-core/src/commands/context.rs
    - crates/ironhermes-core/src/commands/handlers.rs
    - crates/ironhermes-core/src/commands/toolset_display.rs
    - crates/ironhermes-cli/src/toolset_cmd.rs
decisions:
  - D-02-c — GET /audio/:uuid HTTP replay route enables browser page-reload replay (cache survives Blob URL revocation)
  - D-02-d — AudioCacheConfig + gc_sweep + periodic loop + AppState::init wire-up bound disk growth (T-unbounded-disk mitigation)
  - D-06 Path B FULL — Registered column shows Live (per-turn register_tts_tools fired) vs Inspection (CLI inspection registry) vs — (not registered)
  - T-path-traversal — uuid validated (reject path separators / .. / non-uuid shapes) before joining audio_cache_dir; returns 400 on invalid
  - D-05 invariants moved to ironhermes-agent/tests/invariants_36_17_7.rs (5 cross-crate source-grep guards)
metrics:
  completed: 2026-06-05T15:30:00Z
  closed_with: approved-partial-blocker2-live-deferred
---

# Phase 36.17.7 Plan 05: Audio Cache Infra + Registered Column Summary

Wave 3 closes the phase: HTTP replay route + audio-cache GC bound the
web-runtime audio surface, the D-06 `Registered` column lands across CLI +
in-session slash + web parity paths, and the D-05 cross-crate source-grep
invariants are locked in `invariants_36_17_7.rs`.

## Tasks

| Task | Type | Status | Commit |
|---|---|---|---|
| 1 — AudioCacheConfig + Wave 0 test scaffolding | auto | done | ca83fd17 |
| 2 — GET /audio/:uuid replay route + T-path-traversal | auto | done | 4a133e57 |
| 3 — audio_cache GC sweep + periodic loop | auto | done | cc6e034f |
| 4 — Wire GC into AppState::init | auto | done | 6525050d |
| 5 — D-06 Registered column Path B FULL | auto | done | d95602c7 |
| 6 — invariants_36_17_7.rs (D-05 / A5) | tdd | done | 5fb94272 |
| 7 — Operator UAT-R + UAT-Reg + A6 regression sweep | checkpoint:human-verify | **closed (partial — Live display deferred)** | — |

## A6 Regression Sweep — GREEN

All automated suites passed prior to UAT:

| Suite | Result |
|---|---|
| `ironhermes-cli --lib toolset` (incl. 36.17.6 invariants) | 15/15 |
| `ironhermes-agent tts_per_turn_wiring` (Plan 01) | 2/2 |
| `ironhermes-tools not_supported_dispatcher` (Plan 01 D-03-b) | 4/4 |
| `iron_hermes_ui audio_route --features server` | 5/5 |
| `iron_hermes_ui audio_gc --features server` | 8/8 |
| `ironhermes-agent invariants_36_17_7` (D-05) | 5/5 |
| `ironhermes-agent invariants_36_17_5` (no 36.17.5 regression) | 3/3 |
| `ironhermes-core tts_registry` (post-nextest trait fix) | 5/5 |

Full `cargo nextest run --workspace` is GREEN after the latent
`tts_registry.rs` `&PathBuf`→`&Path` fix (commit 0cc0a396) that the new
nextest CI job surfaced.

## UAT Outcome

- **Voice output: PASS on all three surfaces** (Telegram, TUI, Web). Audio is
  synthesized and delivered end-to-end. UAT-R replay route + page-reload
  replay confirmed.
- **UAT-Reg: PARTIAL.** The `/toolset list` slash command does not surface
  the `voice` toolset as `Live` in the running surfaces (reads `—`/`Inspection`).
  Voice still *works* — this is a display/observability gap, not a capability
  gap. User accepted closing the phase with this deferred
  (`approved-partial-blocker2-live-deferred`).

See `deferred-items.md` → "Plan 05 discoveries → `/toolset list` voice row"
for root-cause hypothesis (per-turn registry vs slash-handle registry split)
and the suggested follow-up fix.

## Hotfixes folded into this phase (post-Plan-04)

| Commit | Fix |
|---|---|
| 247e7327 | `send_audio` tool missing `Platform::Web` arm — Plan 04 wired the dispatcher but the tool match fell through to the catch-all error. Added Web arm (mp3/ogg/opus/wav → `send_audio_file`, no ffmpeg). This is what made UAT-W voice actually play. |
| be26f48e | rustls `aws-lc-rs` CryptoProvider install in `edge.rs` — Edge TTS WSS handshake panicked without an explicit process-level provider. Idempotent install before `connect_async`. |
| 0cc0a396 | CI: `workspace-tests` → `cargo nextest run --workspace --all-features` (+ separate `cargo test --doc`); fixed latent `tts_registry.rs` trait-signature break nextest surfaced. |

## Threat Flags

- **T-path-traversal (mitigated):** `GET /audio/:uuid` validates the uuid shape
  and rejects path separators / `..` before joining `audio_cache_dir`; invalid
  → 400. Verified by `audio_route.rs` tests.
- **T-unbounded-disk (mitigated):** startup + periodic GC sweep
  (`gc_sweep_audio_cache` / `run_audio_cache_gc_loop`) bound audio_cache growth;
  sweep errors are logged via `tracing::warn!`, never panic.

## Known Deferrals

1. `/toolset list` voice `Live` display gap — documented above + in `deferred-items.md`.
2. ~25 pre-existing workspace compiler warnings (unused imports / unused_mut /
   dead_code across unrelated subsystems) — out of scope for this phase per
   user direction; tracked for a separate cleanup PR. CI `-D warnings` will
   need them addressed before the lint job goes green.

## Self-Check: PASSED

- `crates/iron_hermes_ui/src/server/audio_route.rs` FOUND
- `crates/iron_hermes_ui/src/server/audio_cache.rs` FOUND
- `crates/ironhermes-agent/tests/invariants_36_17_7.rs` FOUND
- commits ca83fd17, 4a133e57, cc6e034f, 6525050d, d95602c7, 5fb94272 FOUND in `git log`
- A6 regression sweep GREEN; full `cargo nextest run --workspace` GREEN
