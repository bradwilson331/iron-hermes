---
phase: 36.17.7
slug: gateway-web-tts-runtime-wiring
plan: 02
type: execute
wave: 2
depends_on:
  - 36.17.7-01
files_modified:
  - crates/ironhermes-gateway/src/handler.rs
  - crates/ironhermes-gateway/src/runner.rs
autonomous: false
requirements: [A1, A3]
user_setup:
  - service: telegram-bot
    why: "UAT-T requires a live Telegram bot to verify voice-message delivery (A3)"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "@BotFather → /newbot or existing bot's token"
      - name: IRONHERMES_HOME
        source: "Operator-chosen path; recommend /tmp/uat-36.17.7-home"
    dashboard_config:
      - task: "Add test chat ID to bot's allowlist in iron_hermes config.yaml"
        location: "$IRONHERMES_HOME/config.yaml gateway.platform_allowlists.telegram"
must_haves:
  truths:
    - "text_to_speech + send_audio appear in the LLM tool-schema list for every Telegram turn"
    - "Telegram operator UAT-T produces an audible voice message in chat (not a `say` fallback)"
    - "Discord and Slack start paths mount NotSupportedAudioDispatcher so tools register but send_audio returns clean Err (D-03-b)"
  artifacts:
    - path: "crates/ironhermes-gateway/src/handler.rs"
      provides: "GatewayHandler.telegram_audio_dispatcher field + setter + per-turn TtsPerTurnWiring threading in run_agent's TurnRequest"
      contains: "session_key: Some("
    - path: "crates/ironhermes-gateway/src/runner.rs"
      provides: "TelegramAdapter clone-cast at Telegram start path; NotSupportedAudioDispatcher::new('discord') + ('slack') at respective start paths"
      contains: "NotSupportedAudioDispatcher::new"
  key_links:
    - from: "runner.rs Arc<TelegramAdapter>"
      to: "GatewayHandler.telegram_audio_dispatcher"
      via: "Arc::clone + clone-cast as Arc<dyn AudioDispatcher>"
      pattern: "handler.set_telegram_audio_dispatcher\\("
    - from: "handler.rs run_agent TurnRequest"
      to: "AgentRuntime::run_turn register_tts_tools"
      via: "TurnRequest.tts_wiring = Some(TtsPerTurnWiring { session_key, audio_dispatcher })"
      pattern: "tts_wiring: Some\\(ironhermes_agent::TtsPerTurnWiring"
    - from: "runner.rs Discord/Slack start paths"
      to: "GatewayHandler.telegram_audio_dispatcher (sic — the field name is platform-agnostic by convention; D-03-b stub mount)"
      via: "Arc::new(NotSupportedAudioDispatcher::new(platform_name)) as Arc<dyn AudioDispatcher>"
      pattern: "NotSupportedAudioDispatcher::new\\(\"(discord|slack)\"\\)"
---

<objective>
Thread per-turn `TtsPerTurnWiring` through the gateway's Telegram handler so `register_tts_tools` fires for every Telegram turn with the real `SessionKey` and the existing `TelegramAdapter` cast as the `AudioDispatcher`. Mirrors the runner's `set_media_sender` clone-cast pattern exactly. ALSO mount `NotSupportedAudioDispatcher::new("discord")` and `NotSupportedAudioDispatcher::new("slack")` on those platforms' gateway start paths (REVISION fix HIGH 6 — closes D-03-b so Discord/Slack sessions register the TTS tools but `send_audio` returns a clean Err).

Purpose: Closes A1 (tools appear in LLM schema on Telegram) and A3 (operator hears voice message in Telegram chat). HIGH 6 fix ensures D-03-b is realized — without it, Discord/Slack sessions would silently get `audio_dispatcher: None` and tools would not register at all because Plan 01's `if let Some(ref wiring)` guard requires a `TtsPerTurnWiring` value.

Output:
- `GatewayHandler` gains `telegram_audio_dispatcher: Option<Arc<dyn AudioDispatcher>>` + `set_telegram_audio_dispatcher` setter (field name is historical — the same field also holds the NotSupportedAudioDispatcher stub on Discord/Slack start paths).
- `runner.rs` Telegram start path clones-casts `Arc<TelegramAdapter>` into the setter.
- `runner.rs` Discord and Slack start paths construct `NotSupportedAudioDispatcher::new(...)` and call the same setter.
- `run_agent`'s `TurnRequest` construction populates `tts_wiring` per turn.
- Operator UAT-T blocking checkpoint confirms audible voice message delivery (A3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-CONTEXT.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-01-SUMMARY.md
@crates/ironhermes-gateway/src/handler.rs
@crates/ironhermes-gateway/src/runner.rs
@crates/ironhermes-gateway/src/telegram.rs
@crates/ironhermes-gateway/src/discord.rs
@crates/ironhermes-gateway/src/slack.rs
@crates/ironhermes-agent/src/agent_runtime.rs
@crates/ironhermes-tools/src/not_supported_dispatcher.rs

<interfaces>
<!-- Critical signatures and call sites — extracted from PATTERNS so executor doesn't search. -->

Post-Plan-01 TurnRequest + TtsPerTurnWiring + NotSupportedAudioDispatcher (all shipped in Plan 01):
```rust
// In ironhermes_agent:
pub struct TtsPerTurnWiring {
    pub session_key: ironhermes_core::SessionKey,
    pub audio_dispatcher: Option<std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>>,
}
#[derive(Default)]
pub struct TurnRequest {
    // ... existing fields ...
    pub compression_count: usize,
    pub tts_wiring: Option<TtsPerTurnWiring>,
}

// In ironhermes_tools:
pub struct NotSupportedAudioDispatcher { /* platform_name: &'static str */ }
impl NotSupportedAudioDispatcher {
    pub fn new(platform_name: &'static str) -> Self { /* ... */ }
}
// impl AudioDispatcher: both methods return anyhow::bail!("audio dispatch not supported on {}", self.platform_name)
```

Existing SessionKey construction (REUSE per Pitfall 5; do NOT rebuild) at handler.rs:434:
```rust
let session_key = SessionKey::new(event.platform.clone(), &event.chat_id)
    .with_user(&event.sender_id);
```

Canonical clone-cast pattern to mirror at runner.rs:715-726:
```rust
handler.set_media_sender(adapter.clone() as Arc<dyn crate::adapter::MediaSender>);
// Phase 36.17.7 Telegram path:
handler.set_telegram_audio_dispatcher(
    adapter.clone() as Arc<dyn ironhermes_tools::AudioDispatcher>,
);
```

NotSupportedAudioDispatcher mount pattern for Discord/Slack (REVISION HIGH 6 fix — new in this plan):
```rust
// runner.rs — in the Discord start path, after the discord-equivalent of media_sender setup:
handler.set_telegram_audio_dispatcher(
    std::sync::Arc::new(ironhermes_tools::NotSupportedAudioDispatcher::new("discord"))
        as std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>,
);

// runner.rs — in the Slack start path:
handler.set_telegram_audio_dispatcher(
    std::sync::Arc::new(ironhermes_tools::NotSupportedAudioDispatcher::new("slack"))
        as std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>,
);
```

Naming note: the field is named `telegram_audio_dispatcher` for historical / pattern-mirror reasons (it sits next to `media_sender` which is Telegram-shaped in 36.17.2.2). The field is platform-agnostic in type — `Option<Arc<dyn AudioDispatcher>>` — so the Discord/Slack `NotSupportedAudioDispatcher` mounts cleanly into the same slot. Executor MAY rename the field to `audio_dispatcher` if budget allows; either name is acceptable as long as both Telegram-real and Discord/Slack-stub paths mount through the same setter.

Existing TurnRequest build site at handler.rs:1471-1487 (extend with one new field):
```rust
let request = TurnRequest {
    messages,
    session_id: turn_session_id,
    cancel_token: None,
    stream: Some(stream_callback),
    tool_progress: Some(tool_callback),
    tool_result: None,
    trajectory_writer,
    pressure_tracker: None,
    state_store: Some(state_store_for_turn),
    compression_count: 0,
    // ADD: tts_wiring,
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add telegram_audio_dispatcher field + setter to GatewayHandler + thread tts_wiring into run_agent's TurnRequest</name>
  <files>crates/ironhermes-gateway/src/handler.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/handler.rs (full file — GatewayHandler struct, media_sender field + set_media_sender setter, SessionKey at line ~434, TurnRequest construction at ~1471-1487)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/ironhermes-gateway/src/handler.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Gateway Wiring Detail, §Common Pitfall 5)
    - crates/ironhermes-agent/src/agent_runtime.rs (post-Plan-01 TurnRequest + TtsPerTurnWiring shape)
    - crates/ironhermes-gateway/Cargo.toml (confirm ironhermes-agent + ironhermes-tools are already deps)
  </read_first>
  <behavior>
    After this task:
      - `GatewayHandler` has a new field `telegram_audio_dispatcher: Option<Arc<dyn ironhermes_tools::AudioDispatcher>>` placed adjacent to existing `media_sender` field.
      - `GatewayHandler` exposes `pub fn set_telegram_audio_dispatcher(&mut self, dispatcher: Arc<dyn ironhermes_tools::AudioDispatcher>)` placed adjacent to `set_media_sender`. Constructor signature unchanged (additive).
      - Constructor / Default / build_gateway_handler initializes `telegram_audio_dispatcher: None`.
      - In `run_agent`, the `TurnRequest` struct literal includes `tts_wiring` populated from `self.telegram_audio_dispatcher.as_ref().map(|disp| TtsPerTurnWiring { session_key: session_key.clone(), audio_dispatcher: Some(disp.clone()) })`.
      - Existing `session_key` variable at handler.rs:434 is REUSED — no new SessionKey constructed (Pitfall 5).
      - `cargo build -p ironhermes-gateway` exits 0.
      - `cargo test -p ironhermes-gateway` exits 0.
      - Source-grep gates: handler.rs contains `session_key: Some(`, `set_telegram_audio_dispatcher`, `telegram_audio_dispatcher: Option<`.
  </behavior>
  <action>
    Edit `crates/ironhermes-gateway/src/handler.rs`:
      Step 1 — Field declaration. Find `media_sender: Option<Arc<dyn crate::adapter::MediaSender>>` in the `GatewayHandler` struct. Add directly below: `pub telegram_audio_dispatcher: Option<std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>>,`. Match visibility to `media_sender`. Doc comment `/// Phase 36.17.7 D-01: per-turn AudioDispatcher slot. Telegram start path mounts TelegramAdapter clone-cast; Discord/Slack start paths mount NotSupportedAudioDispatcher per D-03-b. Mirrors media_sender directly above.`
      Step 2 — Constructor init. Find every struct-literal initialization of `GatewayHandler` (typically `pub fn new(...)` or `build_gateway_handler`). At each site, add `telegram_audio_dispatcher: None,` immediately after `media_sender: None,`.
      Step 3 — Setter. In `impl GatewayHandler` directly below `set_media_sender` add a setter `pub fn set_telegram_audio_dispatcher(&mut self, dispatcher: std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>)` that assigns `self.telegram_audio_dispatcher = Some(dispatcher);`. Doc comment: `/// Phase 36.17.7 D-01: install an AudioDispatcher for per-turn TTS wiring. Telegram mounts TelegramAdapter; Discord/Slack mount NotSupportedAudioDispatcher.` Match the `Arc` path style to whatever `set_media_sender` uses.
      Step 4 — Per-turn wiring at TurnRequest site. In `run_agent` at the TurnRequest construction (lines ~1471-1487 per RESEARCH §Verified `run_turn` call site), IMMEDIATELY before `let request = TurnRequest { ... };` insert: `let tts_wiring = self.telegram_audio_dispatcher.as_ref().map(|disp| ironhermes_agent::TtsPerTurnWiring { session_key: session_key.clone(), audio_dispatcher: Some(disp.clone()) });` with comment `// Phase 36.17.7 D-01: per-turn TTS wiring. session_key reused per Pitfall 5; do NOT build a new SessionKey here.`. If `session_key` is not in scope (shadowed earlier), capture into a longer-lived binding earlier. Verify type is `ironhermes_core::SessionKey`.
      Step 5 — Add field to TurnRequest literal. Append `tts_wiring,` (field-init shorthand) as the final field after `compression_count: 0,`.
      Step 6 — Import path. If `ironhermes_agent::TtsPerTurnWiring` is not already used in handler.rs, the fully-qualified path keeps you self-contained. Alternatively add `use ironhermes_agent::TtsPerTurnWiring;` next to existing `ironhermes_agent::*` imports.

    Do NOT modify `runner.rs`, `agent_runtime.rs`, or any other crate's file in this task. Do NOT add Cargo dependencies.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-gateway 2>&amp;1 | tail -20 ; grep -c "session_key: Some(" crates/ironhermes-gateway/src/handler.rs ; grep -c "set_telegram_audio_dispatcher" crates/ironhermes-gateway/src/handler.rs ; grep -c "telegram_audio_dispatcher: Option" crates/ironhermes-gateway/src/handler.rs ; grep -c "tts_wiring" crates/ironhermes-gateway/src/handler.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "telegram_audio_dispatcher: Option" crates/ironhermes-gateway/src/handler.rs` returns ≥ 1.
    - `grep -c "set_telegram_audio_dispatcher" crates/ironhermes-gateway/src/handler.rs` returns ≥ 2 (declaration + self-reference inside setter body).
    - `grep -c "telegram_audio_dispatcher: None" crates/ironhermes-gateway/src/handler.rs` returns ≥ 1 (constructor init).
    - `grep -c "session_key: Some(" crates/ironhermes-gateway/src/handler.rs` returns ≥ 1 (D-05 prep).
    - `grep -c "tts_wiring" crates/ironhermes-gateway/src/handler.rs` returns ≥ 2.
    - `grep -c "ironhermes_agent::TtsPerTurnWiring" crates/ironhermes-gateway/src/handler.rs` returns ≥ 1.
    - No new `SessionKey::new(` call inserted: `git diff crates/ironhermes-gateway/src/handler.rs | grep -v '^#' | grep -c '^+.*SessionKey::new('` returns 0.
    - `cargo build -p ironhermes-gateway` exits 0.
    - `cargo test -p ironhermes-gateway --lib` exits 0.
    - `cargo clippy -p ironhermes-gateway --lib -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>`GatewayHandler` has the `telegram_audio_dispatcher` field + setter; `run_agent` populates `tts_wiring` per turn using the existing `session_key`. Gateway crate builds clean. Runner-side wire-up (Telegram + Discord + Slack) is Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire set_telegram_audio_dispatcher on ALL three gateway start paths (Telegram clone-cast + Discord/Slack NotSupportedAudioDispatcher stub mount — REVISION HIGH 6 fix)</name>
  <files>crates/ironhermes-gateway/src/runner.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/runner.rs (full file — Arc&lt;TelegramAdapter&gt; construction at ~645, existing `set_media_sender` call at ~726, AND the Discord start path + Slack start path with their respective handler-build sites)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/ironhermes-gateway/src/runner.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Gateway Wiring Detail)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-CONTEXT.md (§Discord & Slack scope D-03 + D-03-b)
    - crates/ironhermes-gateway/src/discord.rs (verify the Discord adapter start path location)
    - crates/ironhermes-gateway/src/slack.rs (verify the Slack adapter start path location)
  </read_first>
  <behavior>
    After this task — three wire-up sites in runner.rs:
      Site 1 (Telegram, real dispatcher):
        - `runner.rs` contains a new `handler.set_telegram_audio_dispatcher(adapter.clone() as std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>);` line immediately after `handler.set_media_sender(adapter.clone() as Arc<dyn crate::adapter::MediaSender>);` in the Telegram start path.
      Site 2 (Discord, stub dispatcher):
        - `runner.rs` Discord start path contains `handler.set_telegram_audio_dispatcher(std::sync::Arc::new(ironhermes_tools::NotSupportedAudioDispatcher::new("discord")) as std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>);` placed adjacent to whatever existing media_sender / handler-build calls live in the Discord block. If the Discord block does not yet exist in runner.rs (i.e. Discord is started elsewhere), document in commit body and skip this site — but Discord MUST mount the stub somewhere in its start sequence for D-03-b compliance.
      Site 3 (Slack, stub dispatcher):
        - Same pattern with `NotSupportedAudioDispatcher::new("slack")`.
      - `cargo build -p ironhermes-gateway` exits 0.
      - `cargo test -p ironhermes-gateway` exits 0.
      - Source-grep: `grep -c "handler.set_telegram_audio_dispatcher" crates/ironhermes-gateway/src/runner.rs` returns 3 (Telegram + Discord + Slack).
      - Source-grep: `grep -c "NotSupportedAudioDispatcher::new" crates/ironhermes-gateway/src/runner.rs` returns ≥ 2 (one for Discord, one for Slack — REVISION HIGH 6 fix marker).
      - Discord/Slack agent sessions REGISTER text_to_speech + send_audio in the LLM schema (transitively via Plan 01's per-turn block); `send_audio` invocations Err with "audio dispatch not supported on discord" / "...slack" per Plan 01 Task 3 NotSupportedAudioDispatcher impl.
  </behavior>
  <action>
    Edit `crates/ironhermes-gateway/src/runner.rs`:
      Step 1 — Locate `let adapter: Arc<TelegramAdapter> = Arc::new(TelegramAdapter::new(&token));` at ~runner.rs:645. Confirm variable name.
      Step 2 — Locate `handler.set_media_sender(adapter.clone() as Arc<dyn crate::adapter::MediaSender>);` at ~runner.rs:726 (Telegram start path).
      Step 3 (Telegram, Site 1) — Add immediately after the `set_media_sender` line: a comment `// Phase 36.17.7 D-01: TelegramAdapter doubles as AudioDispatcher for TTS. Mirror set_media_sender above.` then `handler.set_telegram_audio_dispatcher(adapter.clone() as std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>);`. Match `Arc` import style.
      Step 4 (Discord, Site 2 — REVISION HIGH 6 fix) — Grep `runner.rs` for the Discord start path. Search hints: `start_discord_runner`, `DiscordAdapter::new`, `discord` module imports. Locate the handler-build site (where the Discord runner constructs its GatewayHandler or attaches platform-specific adapters). After whatever Discord-side equivalent of `set_media_sender` exists (or after the handler is built if no media_sender wiring exists for Discord), insert:
        ```
        // Phase 36.17.7 D-03-b: Discord lacks audio delivery; mount stub so tools still register
        // but send_audio returns a clean Err. Deletion target when Discord gets a real impl.
        handler.set_telegram_audio_dispatcher(
            std::sync::Arc::new(ironhermes_tools::NotSupportedAudioDispatcher::new("discord"))
                as std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>,
        );
        ```
        If the Discord runner's GatewayHandler is constructed inside a spawned tokio task (per `STATE.md` note on Discord/Slack being inside their own spawned tasks), wire the stub INSIDE that task at handler-construction time. Document the chosen insertion site (file:line) in commit body.
      Step 5 (Slack, Site 3 — REVISION HIGH 6 fix) — Same as Step 4 with `"slack"`. Slack start path hints: `start_slack_runner`, `SlackAdapter::new`, `slack` module imports.
      Step 6 — If Discord or Slack runner does NOT exist in `runner.rs` (handlers built in `discord.rs` / `slack.rs` directly), insert the stub mount in those files instead. The acceptance criterion grep target shifts to the appropriate file; document in commit body. The REVISION HIGH 6 contract requires the stub to be mounted somewhere reachable by Discord/Slack agent sessions; the location is executor's discretion based on the actual code layout.

    Do NOT modify Telegram's set_media_sender line. Do NOT upcast `Arc<dyn PlatformAdapter>` → `Arc<dyn AudioDispatcher>` directly (stable-Rust constraint per runner.rs:715 comment).
    Do NOT add new Cargo deps — `ironhermes_tools::NotSupportedAudioDispatcher` is exported by Plan 01 Task 3 and `ironhermes_tools` is already a dep of `ironhermes-gateway`.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-gateway 2>&amp;1 | tail -10 ; cargo test -p ironhermes-gateway --lib 2>&amp;1 | tail -10 ; grep -c "handler.set_telegram_audio_dispatcher" crates/ironhermes-gateway/src/runner.rs ; grep -c "NotSupportedAudioDispatcher::new" crates/ironhermes-gateway/src/runner.rs ; grep -cE 'NotSupportedAudioDispatcher::new\("(discord|slack)"\)' crates/ironhermes-gateway/src/ ; grep -c "ironhermes_tools::AudioDispatcher" crates/ironhermes-gateway/src/runner.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handler.set_telegram_audio_dispatcher" crates/ironhermes-gateway/src/runner.rs` returns 3 (Telegram + Discord + Slack). If Discord/Slack mounts live in `discord.rs` / `slack.rs` instead, the union across those files MUST sum to 3.
    - **REVISION HIGH 6 fix verification: `grep -rcE "NotSupportedAudioDispatcher::new\\(\"(discord|slack)\"\\)" crates/ironhermes-gateway/src/` returns ≥ 2 (one for Discord, one for Slack — separate lines).**
    - `grep -c "ironhermes_tools::AudioDispatcher" crates/ironhermes-gateway/src/runner.rs` returns ≥ 1 (Telegram path always lives here).
    - `grep -c "adapter.clone() as" crates/ironhermes-gateway/src/runner.rs` returns ≥ 2 (existing media_sender clone-cast + new Telegram audio dispatcher clone-cast).
    - `cargo build -p ironhermes-gateway` exits 0.
    - `cargo test -p ironhermes-gateway --lib` exits 0 (no regression).
    - `cargo test -p ironhermes-gateway` exits 0 (full gateway suite).
    - `cargo clippy -p ironhermes-gateway --lib -- -D warnings` exits 0.
    - Commit body documents the insertion file:line for each of the 3 wire-up sites.
  </acceptance_criteria>
  <done>The runner wires the TelegramAdapter into Telegram's GatewayHandler as both MediaSender and AudioDispatcher. Discord and Slack handlers mount `NotSupportedAudioDispatcher` so their TTS tools register (Plan 01's per-turn block guards on `Some(_)`, never `None`) but `send_audio` returns clean Err. D-03-b is realized end-to-end (REVISION HIGH 6 fix complete).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator UAT-T — Telegram TTS voice-message delivery (A3 acceptance gate)</name>
  <what-built>
    Plan 02 Tasks 1 + 2 have completed:
    - `GatewayHandler.telegram_audio_dispatcher` field + setter added (handler.rs).
    - `run_agent` now populates `TurnRequest.tts_wiring` per turn with the reused `session_key` and the `TelegramAdapter` cast as `AudioDispatcher`.
    - `runner.rs` (or sibling files) wires the TelegramAdapter into the Telegram start path AND mounts `NotSupportedAudioDispatcher::new("discord")` + `NotSupportedAudioDispatcher::new("slack")` on the respective platforms' start paths (REVISION HIGH 6 fix).
    Plan 01 Task 2 already made `AgentRuntime::run_turn` call `register_tts_tools(...)` when `tts_wiring` is `Some(_)`. The full Telegram path is now end-to-end: real session_key + dispatcher flows from runner → handler → TurnRequest → run_turn → ToolRegistry → LLM schema → tool invocation → TelegramAdapter::send_voice_file / send_audio_file → Telegram chat. Discord/Slack sessions also register the tools (transitively) but Err on send_audio per D-03-b.
  </what-built>
  <how-to-verify>
    Pre-flight (operator runs once):
      1. Verify env: `echo $TELEGRAM_BOT_TOKEN` (must be non-empty), `echo $IRONHERMES_HOME` (recommend `/tmp/uat-36.17.7-home`).
      2. Ensure `$IRONHERMES_HOME/config.yaml` enables `voice` toolset (empty `voice:` entry or `voice: {enabled: true}` is fine — default-on per `with_default_toolsets_merged`).
      3. Ensure test chat ID is in `gateway.platform_allowlists.telegram` in the same config.
      4. Start the bot: `cargo run --bin hermes -- gateway start --telegram` (separate terminal; leave running).

    UAT-T Steps:
      1. From a Telegram client with the bot added, send: `Summarize the top three Hacker News stories right now as a 30-second podcast hosted by a calm female voice. Use text_to_speech then send_audio.`
      2. Observe bot stdout: confirm registration runs (look for an info-level log if present, or trust the schema-registration is happening per Plan 01 + Plan 02 wiring).
      3. Within ~30 seconds, bot should reply with a voice message (mp3/ogg) in the Telegram chat — NOT a text fallback saying "I used `say` instead" or "I synthesized but can't deliver".
      4. Tap to play the voice message. Confirm audible speech, female voice.
      5. Confirm no `hyperframes-media` or `say` text appears in the bot's stdout error/warn logs for this turn.

    Failure modes to call out in the resume-signal if seen:
      - Tools-not-in-schema: agent text says "I don't have a TTS tool" or never attempts `send_audio`. Likely cause: `tts_wiring` plumbing bug — re-check handler.rs Step 4/5.
      - Schema-registered-but-silent: agent calls `send_audio`, no voice message arrives. Likely cause: dispatcher slot is `None` — re-check runner.rs Telegram Site 1.
      - Voice arrives but audio is corrupt: out-of-scope (Edge TTS provider issue), still PASS for A3 if voice is audibly present.
      - Permission/allowlist deny: bot logs `unauthorized`, no agent turn fires — fix allowlist, retry.
  </how-to-verify>
  <resume-signal>Type "approved" if voice message audibly played, OR describe the specific failure (which step + observed-vs-expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram client → bot | User-controlled message text crosses here; existing 36.17.2.2 / 36.17.5 mitigations apply (no new surface from Plan 02). |
| handler.run_agent → AgentRuntime::run_turn | `TurnRequest.tts_wiring` carries in-process Arc clones only; no untrusted data crosses. |
| TelegramAdapter → Telegram Bot API | Outbound voice/audio file upload uses existing 36.17.5 D-13 / D-14 / D-15 mitigations (T-INPUT-MEDIA-PATH canonicalization, T-INPUT-MEDIA-URL scheme gate, size pre-check, ogg/opus conversion for voice). |
| Discord/Slack agent loop → NotSupportedAudioDispatcher | LLM-invoked send_audio returns Err with a static platform_name. No user input interpolated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.17.7-02-01 | Information Disclosure | Logging of session_key clone | accept | `session_key` clone is in-process only; not serialized to logs. Accept. |
| T-36.17.7-02-02 | Tampering | Arc clone-cast in runner.rs | mitigate | Mirror existing `set_media_sender` clone-cast pattern in production since 36.17.2.2. `Arc<TelegramAdapter>` is process-lifetime, env-controlled token. No new tamper surface. |
| T-36.17.7-02-03 | Spoofing | LLM-invoked send_audio on Telegram | accept | Path traversal + URL scheme gating already mitigated in 36.17.5 D-15. Plan 02 makes the existing tool reachable via LLM schema; no new code path. Accept inherited mitigations. |
| T-36.17.7-02-04 | Repudiation | NotSupportedAudioDispatcher Err on Discord/Slack | accept | `&'static str` platform name is compile-time controlled; no log injection. Plain-prose `anyhow::bail!`. Low-risk single-operator deployment. |
| T-36.17.7-02-SC | Tampering | cargo deps | mitigate | Zero new external dependencies. `ironhermes-agent` + `ironhermes-tools` already in `ironhermes-gateway`'s `[dependencies]`. |
</threat_model>

<verify_plan>
1. `cargo build -p ironhermes-gateway` exits 0.
2. `cargo test -p ironhermes-gateway` exits 0 (full gateway crate).
3. `cargo clippy -p ironhermes-gateway -- -D warnings` exits 0.
4. `grep -c "session_key: Some(" crates/ironhermes-gateway/src/handler.rs` returns ≥ 1.
5. `grep -c "handler.set_telegram_audio_dispatcher" crates/ironhermes-gateway/src/runner.rs` returns 3 (or sum across runner.rs + discord.rs + slack.rs = 3).
6. **REVISION HIGH 6 lock: `grep -rcE "NotSupportedAudioDispatcher::new\\(\"(discord|slack)\"\\)" crates/ironhermes-gateway/src/` returns ≥ 2.**
7. `grep -c "telegram_audio_dispatcher: Option" crates/ironhermes-gateway/src/handler.rs` returns ≥ 1.
8. UAT-T checkpoint approved by operator (must-have truth #2).
9. No regression in Phase 36.17.5 invariants: `cargo test -p ironhermes-agent --test invariants_36_17_5` exits 0.
</verify_plan>

<success_criteria>
- `GatewayHandler` carries an AudioDispatcher slot and threads it into per-turn TurnRequest.
- Runner (or sibling files) wires TelegramAdapter (real), Discord (stub), Slack (stub) at startup.
- Telegram operator UAT-T audibly plays a synthesized voice message (A3).
- LLM tool-schema list for Telegram turns includes `text_to_speech` and `send_audio` (A1 — proved transitively by UAT-T audible result).
- Discord/Slack sessions also register tools; `send_audio` invocations Err with platform name (D-03-b).
- Zero regression in any other gateway path.
- No new external dependencies.
</success_criteria>

<output>
Create `.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-02-SUMMARY.md` documenting:
- 2 files modified (handler.rs, runner.rs — plus discord.rs / slack.rs if stub mounts live there)
- Exact source-grep gate counts from &lt;verify_plan&gt;
- REVISION HIGH 6 fix: location (file:line) of the 3 set_telegram_audio_dispatcher wire-ups
- UAT-T transcript: operator's prompt, observed voice message duration, voice character, any anomalies
- Full source-grep proof that `session_key: Some(` appears in handler.rs (Plan 05 D-05 invariant prep)
- must_haves achieved: all 3 truths; both artifacts; all 3 key_links
</output>
