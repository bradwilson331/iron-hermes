---
phase: 36.17.7
slug: gateway-web-tts-runtime-wiring
plan: 02
type: execute
wave: 2
depends_on:
  - 36.17.7-01
files_modified:
  - crates/ironhermes-gateway/src/handler.rs
  - crates/ironhermes-gateway/src/runner.rs
autonomous: false
requirements: [A1, A3]
user_setup:
  - service: telegram-bot
    why: "UAT-T requires a live Telegram bot to verify voice-message delivery (A3)"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "@BotFather → /newbot or existing bot's token"
      - name: IRONHERMES_HOME
        source: "Operator-chosen path; recommend /tmp/uat-36.17.7-home"
    dashboard_config:
      - task: "Add test chat ID to bot's allowlist in iron_hermes config.yaml"
        location: "$IRONHERMES_HOME/config.yaml gateway.platform_allowlists.telegram"
must_haves:
  truths:
    - "text_to_speech + send_audio appear in the LLM tool-schema list for every Telegram turn"
    - "Telegram operator UAT-T produces an audible voice message in chat (not a `say` fallback)"
  artifacts:
    - path: "crates/ironhermes-gateway/src/handler.rs"
      provides: "GatewayHandler.telegram_audio_dispatcher field + setter + per-turn TtsPerTurnWiring threading in run_agent's TurnRequest"
      contains: "session_key: Some("
    - path: "crates/ironhermes-gateway/src/runner.rs"
      provides: "handler.set_telegram_audio_dispatcher clone-cast wire-up adjacent to set_media_sender"
      contains: "set_telegram_audio_dispatcher"
  key_links:
    - from: "runner.rs Arc<TelegramAdapter>"
      to: "GatewayHandler.telegram_audio_dispatcher"
      via: "Arc::clone + clone-cast as Arc<dyn AudioDispatcher>"
      pattern: "handler.set_telegram_audio_dispatcher\\("
    - from: "handler.rs run_agent TurnRequest"
      to: "AgentRuntime::run_turn register_tts_tools"
      via: "TurnRequest.tts_wiring = Some(TtsPerTurnWiring { session_key, audio_dispatcher })"
      pattern: "tts_wiring: Some\\(ironhermes_agent::TtsPerTurnWiring"
---

<objective>
Thread per-turn `TtsPerTurnWiring` through the gateway's Telegram handler so `register_tts_tools` fires for every Telegram turn with the real `SessionKey` and the existing `TelegramAdapter` cast as the `AudioDispatcher`. Mirrors the runner's `set_media_sender` clone-cast pattern exactly.

Purpose: Closes A1 (tools appear in LLM schema on Telegram) and A3 (operator hears voice message in Telegram chat). The Telegram surface is the canonical proof-of-life because `TelegramAdapter::AudioDispatcher` already shipped in 36.17.5 — this plan only wires it through.

Output:
- `GatewayHandler` gains `telegram_audio_dispatcher: Option<Arc<dyn AudioDispatcher>>` + `set_telegram_audio_dispatcher` setter.
- `runner.rs` clones-casts `Arc<TelegramAdapter>` into the setter at the same site as `set_media_sender`.
- `run_agent`'s `TurnRequest` construction populates `tts_wiring` per turn.
- Operator UAT-T blocking checkpoint confirms audible voice message delivery (A3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-CONTEXT.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-01-SUMMARY.md
@crates/ironhermes-gateway/src/handler.rs
@crates/ironhermes-gateway/src/runner.rs
@crates/ironhermes-gateway/src/telegram.rs
@crates/ironhermes-agent/src/agent_runtime.rs

<interfaces>
<!-- Critical signatures and call sites — extracted from PATTERNS so executor does not search. -->

Post-Plan-01 TurnRequest + TtsPerTurnWiring (from crates/ironhermes-agent/src/agent_runtime.rs):
```rust
pub struct TtsPerTurnWiring {
    pub session_key: ironhermes_core::SessionKey,
    pub audio_dispatcher: Option<std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>>,
}
#[derive(Default)]
pub struct TurnRequest {
    // ... existing fields ...
    pub compression_count: usize,
    pub tts_wiring: Option<TtsPerTurnWiring>,
}
```

Existing SessionKey construction (REUSE per Pitfall 5; do NOT rebuild) at handler.rs:434:
```rust
let session_key = SessionKey::new(event.platform.clone(), &event.chat_id)
    .with_user(&event.sender_id);
```

Canonical clone-cast pattern to mirror at runner.rs:715-726:
```rust
handler.set_media_sender(adapter.clone() as Arc<dyn crate::adapter::MediaSender>);
// Phase 36.17.7: add immediately after, same adapter, parallel pattern:
handler.set_telegram_audio_dispatcher(
    adapter.clone() as Arc<dyn ironhermes_tools::AudioDispatcher>,
);
```

Existing TurnRequest build site at handler.rs:1471-1487 (extend with one new field):
```rust
let request = TurnRequest {
    messages,
    session_id: turn_session_id,
    cancel_token: None,
    stream: Some(stream_callback),
    tool_progress: Some(tool_callback),
    tool_result: None,
    trajectory_writer,
    pressure_tracker: None,
    state_store: Some(state_store_for_turn),
    compression_count: 0,
    // ADD next: tts_wiring,
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add telegram_audio_dispatcher field + setter to GatewayHandler + thread tts_wiring into run_agent's TurnRequest</name>
  <files>crates/ironhermes-gateway/src/handler.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/handler.rs (full file — must see GatewayHandler struct definition, existing media_sender field + set_media_sender setter, SessionKey at line ~434, TurnRequest construction at ~1471-1487)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/ironhermes-gateway/src/handler.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Gateway Wiring Detail, §Common Pitfall 5)
    - crates/ironhermes-agent/src/agent_runtime.rs (post-Plan-01 TurnRequest + TtsPerTurnWiring shape)
    - crates/ironhermes-gateway/Cargo.toml (confirm ironhermes-agent + ironhermes-tools are already deps)
  </read_first>
  <behavior>
    After this task:
      - `GatewayHandler` has a new field `telegram_audio_dispatcher: Option<Arc<dyn ironhermes_tools::AudioDispatcher>>` placed adjacent to existing `media_sender` field.
      - `GatewayHandler` exposes `pub fn set_telegram_audio_dispatcher(&mut self, dispatcher: Arc<dyn ironhermes_tools::AudioDispatcher>)` placed adjacent to existing `set_media_sender`. Constructor signature unchanged (additive — call sites unbroken).
      - Constructor / Default / explicit struct literal in `build_gateway_handler` initializes `telegram_audio_dispatcher: None`.
      - In `run_agent`, the existing `TurnRequest` struct literal includes `tts_wiring` populated from `self.telegram_audio_dispatcher.as_ref().map(|disp| TtsPerTurnWiring { session_key: session_key.clone(), audio_dispatcher: Some(disp.clone()) })`.
      - Existing `session_key` variable at handler.rs:434 is REUSED — no new SessionKey constructed (Pitfall 5).
      - `cargo build -p ironhermes-gateway` exits 0.
      - `cargo test -p ironhermes-gateway` exits 0 (no regression).
      - Source-grep gates: handler.rs contains literal `session_key: Some(` (D-05 prep), contains `set_telegram_audio_dispatcher`, contains `telegram_audio_dispatcher: Option<`.
  </behavior>
  <action>
    Edit `crates/ironhermes-gateway/src/handler.rs`:
      Step 1 — Field declaration. Find `media_sender: Option<Arc<dyn crate::adapter::MediaSender>>` in the `GatewayHandler` struct. Add directly below: `pub telegram_audio_dispatcher: Option<std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>>,`. Match visibility (`pub` or private) to `media_sender`. Add doc comment `/// Phase 36.17.7 D-01: per-turn TelegramAdapter cast as AudioDispatcher. Mirrors media_sender directly above.`
      Step 2 — Constructor init. Find every struct-literal initialization of `GatewayHandler` (typically `pub fn new(...)` or inside `build_gateway_handler`). At each site, add `telegram_audio_dispatcher: None,` immediately after the existing `media_sender: None,` line.
      Step 3 — Setter. In `impl GatewayHandler` locate `pub fn set_media_sender(&mut self, sender: Arc<dyn ... MediaSender>) { self.media_sender = Some(sender); }`. Directly below add a setter named `set_telegram_audio_dispatcher` that takes `dispatcher: std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>` and assigns `self.telegram_audio_dispatcher = Some(dispatcher);`. Doc comment: `/// Phase 36.17.7 D-01: install the Telegram AudioDispatcher for per-turn TTS wiring. Mirrors set_media_sender directly above.` Match the `std::sync::Arc` path style to whatever `set_media_sender` uses.
      Step 4 — Per-turn wiring at TurnRequest site. In `run_agent` at the TurnRequest construction (lines ~1471-1487 per RESEARCH §Verified `run_turn` call site), IMMEDIATELY before `let request = TurnRequest { ... };` insert a `let tts_wiring = self.telegram_audio_dispatcher.as_ref().map(|disp| ironhermes_agent::TtsPerTurnWiring { session_key: session_key.clone(), audio_dispatcher: Some(disp.clone()) });` binding. Add the comment `// Phase 36.17.7 D-01: per-turn TTS wiring. session_key reused from earlier in run_agent per Pitfall 5; do NOT build a new SessionKey here.` If `session_key` is not in scope at this exact line (shadowed earlier), search upward for the nearest `let session_key = SessionKey::new(...)` and either move the wiring construction below it, or clone `session_key` into a longer-lived binding earlier. Verify type is `ironhermes_core::SessionKey` (not a wrapper).
      Step 5 — Add field to TurnRequest literal. In the existing `let request = TurnRequest { ... compression_count: 0, };` block, append `tts_wiring,` (field-init shorthand) as the final field after `compression_count: 0,`.
      Step 6 — Import path verification. If `ironhermes_agent::TtsPerTurnWiring` is not already used in handler.rs, the fully-qualified path in Step 4 keeps you from needing a `use` line. If preferred, add `use ironhermes_agent::TtsPerTurnWiring;` next to existing `ironhermes_agent::*` imports.

    Do NOT modify `runner.rs`, `agent_runtime.rs`, or any other crate's file in this task. Do NOT add Cargo dependencies (executor must verify `ironhermes-agent` and `ironhermes-tools` are already in `crates/ironhermes-gateway/Cargo.toml` — read-only check). Both should be present per 36.17.5 wiring.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-gateway 2>&amp;1 | tail -20 ; grep -c "session_key: Some(" crates/ironhermes-gateway/src/handler.rs ; grep -c "set_telegram_audio_dispatcher" crates/ironhermes-gateway/src/handler.rs ; grep -c "telegram_audio_dispatcher: Option" crates/ironhermes-gateway/src/handler.rs ; grep -c "tts_wiring" crates/ironhermes-gateway/src/handler.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "telegram_audio_dispatcher: Option" crates/ironhermes-gateway/src/handler.rs` returns ≥ 1.
    - `grep -c "set_telegram_audio_dispatcher" crates/ironhermes-gateway/src/handler.rs` returns ≥ 2 (declaration + self-reference inside setter body).
    - `grep -c "telegram_audio_dispatcher: None" crates/ironhermes-gateway/src/handler.rs` returns ≥ 1 (constructor init).
    - `grep -c "session_key: Some(" crates/ironhermes-gateway/src/handler.rs` returns ≥ 1 (D-05 prep).
    - `grep -c "tts_wiring" crates/ironhermes-gateway/src/handler.rs` returns ≥ 2 (local binding + TurnRequest field).
    - `grep -c "ironhermes_agent::TtsPerTurnWiring" crates/ironhermes-gateway/src/handler.rs` returns ≥ 1.
    - No new `SessionKey::new(` call inserted: `git diff crates/ironhermes-gateway/src/handler.rs | grep -v '^#' | grep -c '^+.*SessionKey::new('` returns 0.
    - `cargo build -p ironhermes-gateway` exits 0.
    - `cargo test -p ironhermes-gateway --lib` exits 0.
    - `cargo clippy -p ironhermes-gateway --lib -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>`GatewayHandler` has the `telegram_audio_dispatcher` field + setter; `run_agent` populates `tts_wiring` per turn using the existing `session_key`. Gateway crate builds clean. Runner-side wire-up is Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire set_telegram_audio_dispatcher clone-cast in runner.rs alongside set_media_sender</name>
  <files>crates/ironhermes-gateway/src/runner.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/runner.rs (lines 635-727 — Arc&lt;TelegramAdapter&gt; construction at ~645 and existing `set_media_sender` call at ~726)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/ironhermes-gateway/src/runner.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Gateway Wiring Detail)
  </read_first>
  <behavior>
    After this task:
      - `runner.rs` contains a new `handler.set_telegram_audio_dispatcher(adapter.clone() as std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>);` line immediately after the existing `handler.set_media_sender(adapter.clone() as Arc<dyn crate::adapter::MediaSender>);` line in the Telegram start path.
      - Discord and Slack start paths do NOT call `set_telegram_audio_dispatcher` (those mount `NotSupportedAudioDispatcher` in a future phase per D-03-b; not part of runner.rs surface here).
      - `cargo build -p ironhermes-gateway` exits 0.
      - `cargo test -p ironhermes-gateway` exits 0.
      - Full TTS schema-registration path for Telegram end-to-end: runner installs dispatcher → handler.run_agent builds TtsPerTurnWiring → AgentRuntime::run_turn calls register_tts_tools → `text_to_speech` + `send_audio` appear in LLM schema.
  </behavior>
  <action>
    Edit `crates/ironhermes-gateway/src/runner.rs`:
      Step 1 — Locate `let adapter: Arc<TelegramAdapter> = Arc::new(TelegramAdapter::new(&token));` at ~runner.rs:645. Confirm variable name (`adapter`).
      Step 2 — Locate existing `handler.set_media_sender(adapter.clone() as Arc<dyn crate::adapter::MediaSender>);` at ~runner.rs:726. Confirm it is inside the Telegram start path (not Discord, not Slack).
      Step 3 — Add immediately after the `set_media_sender` line: a comment block `// Phase 36.17.7 D-01: TelegramAdapter doubles as AudioDispatcher for TTS. Mirror the set_media_sender clone-cast above. Same adapter, parallel trait.` then `handler.set_telegram_audio_dispatcher(adapter.clone() as std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>);`. Match the `Arc` import style to whatever the file already uses — if `use std::sync::Arc;` is in scope at the top, the snippet may drop `std::sync::` from the type ascription.
      Step 4 — Verify NO insertion in Discord or Slack start paths. Grep for `handler.set_media_sender` in runner.rs; if there are Discord/Slack analogs, leave them alone.
    Do NOT modify any other file. Do NOT change the existing `set_media_sender` line. Do NOT upcast `Arc<dyn PlatformAdapter>` → `Arc<dyn AudioDispatcher>` directly — use the concrete `Arc<TelegramAdapter>` for the clone-cast (runner.rs:715 comment documents the unstable-on-stable Rust constraint).
  </action>
  <verify>
    <automated>cargo build -p ironhermes-gateway 2>&amp;1 | tail -10 ; cargo test -p ironhermes-gateway --lib 2>&amp;1 | tail -10 ; grep -c "handler.set_telegram_audio_dispatcher" crates/ironhermes-gateway/src/runner.rs ; grep -c "ironhermes_tools::AudioDispatcher" crates/ironhermes-gateway/src/runner.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handler.set_telegram_audio_dispatcher" crates/ironhermes-gateway/src/runner.rs` returns 1 (exactly one wire-up site — Telegram only).
    - `grep -c "ironhermes_tools::AudioDispatcher" crates/ironhermes-gateway/src/runner.rs` returns ≥ 1.
    - `grep -c "adapter.clone() as" crates/ironhermes-gateway/src/runner.rs` returns ≥ 2 (existing media_sender clone-cast + the new audio dispatcher clone-cast).
    - `cargo build -p ironhermes-gateway` exits 0.
    - `cargo test -p ironhermes-gateway --lib` exits 0.
    - `cargo test -p ironhermes-gateway` exits 0 (full gateway suite — no integration test regression).
    - `cargo clippy -p ironhermes-gateway --lib -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>The runner wires the TelegramAdapter into the GatewayHandler as both MediaSender and AudioDispatcher. Telegram TTS schema is live end-to-end (text_to_speech + send_audio appear in the LLM tool list for every Telegram turn).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator UAT-T — Telegram TTS voice-message delivery (A3 acceptance gate)</name>
  <what-built>
    Plan 02 Tasks 1 + 2 have completed:
    - `GatewayHandler.telegram_audio_dispatcher` field + setter added (handler.rs).
    - `run_agent` now populates `TurnRequest.tts_wiring` per turn with the reused `session_key` and the `TelegramAdapter` cast as `AudioDispatcher`.
    - `runner.rs` clones-and-casts `Arc<TelegramAdapter>` into the new setter alongside the existing `set_media_sender` wire-up.
    Plan 01 Task 2 already made `AgentRuntime::run_turn` call `register_tts_tools(...)` when `tts_wiring` is `Some(_)`. The full Telegram path is now end-to-end: real session_key + dispatcher flows from runner → handler → TurnRequest → run_turn → ToolRegistry → LLM schema → tool invocation → TelegramAdapter::send_voice_file / send_audio_file → Telegram chat.
  </what-built>
  <how-to-verify>
    Pre-flight (operator runs once):
      1. Verify env: `echo $TELEGRAM_BOT_TOKEN` (must be non-empty), `echo $IRONHERMES_HOME` (recommend `/tmp/uat-36.17.7-home`).
      2. Ensure `$IRONHERMES_HOME/config.yaml` enables `voice` toolset (empty `voice:` entry or `voice: {enabled: true}` is fine — default-on per `with_default_toolsets_merged`).
      3. Ensure test chat ID is in `gateway.platform_allowlists.telegram` in the same config.
      4. Start the bot: `cargo run --bin hermes -- gateway start --telegram` (separate terminal; leave running).

    UAT-T Steps:
      1. From a Telegram client with the bot added, send: `Summarize the top three Hacker News stories right now as a 30-second podcast hosted by a calm female voice. Use text_to_speech then send_audio.`
      2. Observe bot stdout: confirm registration runs (look for an info-level log if present, or trust the schema-registration is happening per Plan 01 + Plan 02 wiring).
      3. Within ~30 seconds, bot should reply with a voice message (mp3/ogg) in the Telegram chat — NOT a text fallback saying "I used `say` instead" or "I synthesized but can't deliver".
      4. Tap to play the voice message. Confirm audible speech, female voice.
      5. Confirm no `hyperframes-media` or `say` text appears in the bot's stdout error/warn logs for this turn.

    Failure modes to call out in the resume-signal if seen:
      - Tools-not-in-schema: agent text says "I don't have a TTS tool" or never attempts `send_audio`. Likely cause: `tts_wiring` plumbing bug — re-check handler.rs Step 4/5.
      - Schema-registered-but-silent: agent calls `send_audio`, no voice message arrives. Likely cause: dispatcher slot is `None` — re-check runner.rs Step 3.
      - Voice arrives but audio is corrupt: out-of-scope (Edge TTS provider issue), still PASS for A3 if voice is audibly present.
      - Permission/allowlist deny: bot logs `unauthorized`, no agent turn fires — fix allowlist, retry.
  </how-to-verify>
  <resume-signal>Type "approved" if voice message audibly played, OR describe the specific failure (which step + observed-vs-expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram client → bot | User-controlled message text crosses here; existing 36.17.2.2 / 36.17.5 mitigations apply (no new surface from Plan 02). |
| handler.run_agent → AgentRuntime::run_turn | `TurnRequest.tts_wiring` carries in-process Arc clones only; no untrusted data crosses. |
| TelegramAdapter → Telegram Bot API | Outbound voice/audio file upload uses existing 36.17.5 D-13 / D-14 / D-15 mitigations (T-INPUT-MEDIA-PATH canonicalization, T-INPUT-MEDIA-URL scheme gate, size pre-check, ogg/opus conversion for voice). Plan 02 does not add a new outbound path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.17.7-02-01 | Information Disclosure | Logging of session_key clone | accept | `session_key` clone is in-process only; not serialized to logs. Existing handler.rs logging does not include SessionKey fields in info traces. Accept. |
| T-36.17.7-02-02 | Tampering | Arc clone-cast in runner.rs | mitigate | Mirror the existing `set_media_sender` clone-cast pattern in production since 36.17.2.2. `Arc<TelegramAdapter>` is process-lifetime, constructed from `&token` (env-controlled). No new tamper surface. |
| T-36.17.7-02-03 | Spoofing | LLM-invoked send_audio on Telegram | accept | Path traversal + URL scheme gating already mitigated in 36.17.5 D-15 / 36.17.2.2 D-15 (`canonicalize + starts_with`, scheme allowlist). Plan 02 does not introduce a new send_audio code path — it makes the existing tool reachable via the LLM schema. Accept inherited mitigations. |
| T-36.17.7-02-SC | Tampering | cargo deps | mitigate | Plan 02 introduces zero new external dependencies. Both `ironhermes-agent` and `ironhermes-tools` are already in `ironhermes-gateway`'s `[dependencies]` (verified by 36.17.5 Plan 03 SUMMARY's wiring). No package-legitimacy human checkpoint needed. |
</threat_model>

<verify_plan>
1. `cargo build -p ironhermes-gateway` exits 0.
2. `cargo test -p ironhermes-gateway` exits 0 (full gateway crate — including integration tests).
3. `cargo clippy -p ironhermes-gateway -- -D warnings` exits 0.
4. `grep -c "session_key: Some(" crates/ironhermes-gateway/src/handler.rs` returns ≥ 1.
5. `grep -c "handler.set_telegram_audio_dispatcher" crates/ironhermes-gateway/src/runner.rs` returns 1.
6. `grep -c "telegram_audio_dispatcher: Option" crates/ironhermes-gateway/src/handler.rs` returns ≥ 1.
7. UAT-T checkpoint approved by operator (must-have truth #2).
8. No regression in Phase 36.17.5 invariants: `cargo test -p ironhermes-agent --test invariants_36_17_5` exits 0.
</verify_plan>

<success_criteria>
- `GatewayHandler` carries the Telegram AudioDispatcher and threads it into per-turn TurnRequest.
- Runner wires the TelegramAdapter clone-cast at startup.
- Telegram operator UAT-T audibly plays a synthesized voice message in chat (A3).
- LLM tool-schema list for Telegram turns includes `text_to_speech` and `send_audio` (A1 — proved transitively by UAT-T audible result).
- Zero regression in any other gateway path (Discord/Slack still build, run, no behavior change).
- No new external dependencies.
</success_criteria>

<output>
Create `.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-02-SUMMARY.md` documenting:
- 2 files modified (handler.rs, runner.rs)
- Exact source-grep gate counts (the 6 grep-Cs in &lt;verify_plan&gt;)
- UAT-T transcript: operator's prompt, observed voice message duration, voice character, any anomalies
- Full source-grep proof that `session_key: Some(` now appears in `handler.rs` (Plan 05 invariant prep)
- must_haves achieved: truths #1, #2; artifacts handler.rs + runner.rs; key_links runner → handler → run_turn
</output>
