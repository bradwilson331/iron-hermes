---
phase: 36.17.7
slug: gateway-web-tts-runtime-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-agent/src/agent_runtime.rs
  - crates/ironhermes-tools/src/lib.rs
  - crates/ironhermes-tools/src/not_supported_dispatcher.rs
  - crates/ironhermes-agent/tests/tts_per_turn_wiring.rs
  - crates/ironhermes-tools/tests/not_supported_dispatcher.rs
autonomous: true
requirements: [A5]
must_haves:
  truths:
    - "TurnRequest carries an optional TtsPerTurnWiring slot that downstream surfaces can populate"
    - "AgentRuntime::run_turn registers text_to_speech + send_audio on bundle.registry when TtsPerTurnWiring is Some, before agent loop construction"
    - "NotSupportedAudioDispatcher exists in ironhermes-tools and returns an Err naming the platform for both send_voice_file and send_audio_file"
  artifacts:
    - path: "crates/ironhermes-agent/src/agent_runtime.rs"
      provides: "TurnRequest::tts_wiring field + TtsPerTurnWiring struct + per-turn register_tts_tools call in run_turn"
      contains: "pub tts_wiring: Option<TtsPerTurnWiring>"
    - path: "crates/ironhermes-tools/src/not_supported_dispatcher.rs"
      provides: "NotSupportedAudioDispatcher stub for Discord/Slack platforms"
      exports: ["NotSupportedAudioDispatcher"]
    - path: "crates/ironhermes-agent/tests/tts_per_turn_wiring.rs"
      provides: "Unit + source-grep tests for D-01 wiring"
    - path: "crates/ironhermes-tools/tests/not_supported_dispatcher.rs"
      provides: "Unit + source-grep tests for D-03-b error behavior"
  key_links:
    - from: "TurnRequest::tts_wiring"
      to: "ToolRegistry::register_tts_tools"
      via: "AgentRuntime::run_turn registration block"
      pattern: "self.bundle.registry.*register_tts_tools"
    - from: "ironhermes-tools::NotSupportedAudioDispatcher"
      to: "AudioDispatcher trait"
      via: "#[async_trait] impl in ironhermes-tools"
      pattern: "impl.*AudioDispatcher for NotSupportedAudioDispatcher"
---

<plan_layout_rationale>
Phase 36.17.7 has 3 platform surfaces (gateway/Telegram, web, TUI) × multiple sub-deliverables. The decomposition is:

- **Plan 01 (this plan, Wave 1):** Foundations. Owns the `agent_runtime.rs` struct extension + per-turn registry call, plus the `NotSupportedAudioDispatcher` stub. Every downstream plan depends on the new `TurnRequest::tts_wiring` field and the `TtsPerTurnWiring` struct. Plan 01 is autonomous and must merge before Waves 2–3 can begin.
- **Plan 02 (Wave 2):** Gateway/Telegram surface. Wires per-turn `TtsPerTurnWiring` in `handler.rs::run_agent` using the existing `Arc<TelegramAdapter>` lifted from `runner.rs:726`. Covers A1 + A3.
- **Plan 03 (Wave 2):** TUI surface. Wires per-turn `TtsPerTurnWiring` in `tui_rata/event_loop.rs::spawn_turn` with `Platform::Local` + `audio_dispatcher: None` (rodio dispatch lives inside SendAudioTool). Covers A4-TUI.
- **Plan 04 (Wave 2):** Web runtime surface. The biggest plan — adds `ChatStreamEvent::AudioOut` variant, `WebAudioDispatcher` impl, `Message::Binary` send arm in ws.rs, per-turn wiring of `WebAudioDispatcher` into `TurnRequest`, AND the `Block::Audio` UI control per the UI-SPEC. Covers A2 + A4-web.
- **Plan 05 (Wave 3):** Web replay + GC + config + toolset `Registered` column + D-05 invariants. Owns `audio_route.rs`, `audio_cache.rs`, `AudioCacheConfig`, the toolset_cmd column addition, and the cross-crate `invariants_36_17_7.rs` source-grep test. Depends on Plans 02/03/04 so the invariant test can grep for `session_key: Some(` in all three handler sites at once. Covers A6, A7, D-02-c, D-02-d, D-05.

Wave 0 test scaffolds are folded into the plan that owns the corresponding source file (D-05 invariant test in Plan 05 because it cross-references all three handlers).
</plan_layout_rationale>

<objective>
Establish the foundation for per-turn TTS registration across all three runtime surfaces. Add `TtsPerTurnWiring` + `TurnRequest::tts_wiring` to `crates/ironhermes-agent/src/agent_runtime.rs`, wire `register_tts_tools` into `AgentRuntime::run_turn` at the start of each turn when the slot is `Some(_)`, and ship `NotSupportedAudioDispatcher` in `ironhermes-tools` so future Discord/Slack plans can mount it without code changes here.

Purpose: Every Wave 2 surface plan (gateway, TUI, web) needs the `TurnRequest::tts_wiring` field to exist before it can wire its own dispatcher. The `NotSupportedAudioDispatcher` stub closes D-03-b without adding Discord/Slack runtime code.

Output:
- Extended `TurnRequest` struct + new `TtsPerTurnWiring` struct in `agent_runtime.rs`.
- Per-turn registration block in `AgentRuntime::run_turn`.
- `crates/ironhermes-tools/src/not_supported_dispatcher.rs` (new file) + `pub use` re-export from `lib.rs`.
- Wave 0 test files for D-01 wiring and D-03-b error behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-CONTEXT.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md
@crates/ironhermes-agent/src/agent_runtime.rs
@crates/ironhermes-agent/src/app_runtime_factory.rs
@crates/ironhermes-tools/src/registry.rs
@crates/ironhermes-tools/src/send_audio_tool.rs
@crates/ironhermes-tools/src/lib.rs
@crates/ironhermes-agent/tests/invariants_36_17_5.rs

<interfaces>
<!-- Critical types and signatures the executor needs from existing code. Extracted from CONTEXT/RESEARCH/PATTERNS. -->

From crates/ironhermes-tools/src/send_audio_tool.rs:24-40 (AudioDispatcher trait):
```rust
#[async_trait]
pub trait AudioDispatcher: Send + Sync {
    async fn send_voice_file(
        &self,
        chat_id: &str,
        path: &PathBuf,
        thread_id: Option<&str>,
    ) -> anyhow::Result<()>;

    async fn send_audio_file(
        &self,
        chat_id: &str,
        path: &PathBuf,
        thread_id: Option<&str>,
    ) -> anyhow::Result<()>;
}
```

From crates/ironhermes-tools/src/registry.rs:646-658 (register_tts_tools signature):
```rust
pub fn register_tts_tools(
    &mut self,
    session_key: ironhermes_core::SessionKey,
    dispatcher: Option<Arc<dyn crate::AudioDispatcher>>,
    config: Arc<ironhermes_core::Config>,
)
```

From crates/ironhermes-agent/src/agent_runtime.rs:93-110 (current TurnRequest shape — extend, do not rebuild):
```rust
#[derive(Default)]
pub struct TurnRequest {
    pub messages: Vec<ChatMessage>,
    pub session_id: String,
    pub cancel_token: Option<CancellationToken>,
    pub stream: Option<StreamCallback>,
    pub tool_progress: Option<ToolProgressCallback>,
    pub tool_result: Option<ToolResultCallback>,
    pub trajectory_writer: Option<Arc<dyn TrajectoryWriterHandle>>,
    pub pressure_tracker: Option<Arc<PressureTracker>>,
    pub state_store: Option<Arc<std::sync::Mutex<StateStore>>>,
    pub compression_count: usize,
    // ADD: pub tts_wiring: Option<TtsPerTurnWiring>,
}
```

From crates/ironhermes-agent/src/agent_runtime.rs:213-217 (current production code — note for downstream plans, not to be modified here):
```rust
// Phase 36.17.5 D-15: per-turn threading deferred to a follow-up phase.
session_key: None,
telegram_adapter: None,
```
This plan does NOT remove those `None` lines — Plan 05 ships the D-05 invariant that asserts their removal in production handler code paths. Plan 01 only adds the new field and the per-turn `register_tts_tools` call inside `run_turn`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — author tts_per_turn_wiring.rs + not_supported_dispatcher.rs test files</name>
  <files>crates/ironhermes-agent/tests/tts_per_turn_wiring.rs, crates/ironhermes-tools/tests/not_supported_dispatcher.rs</files>
  <read_first>
    - crates/ironhermes-agent/tests/invariants_36_17_5.rs (the canonical `include_str!` source-grep pattern to mirror)
    - crates/ironhermes-tools/tests/tts_tools.rs (Rust async unit-test patterns used in this crate)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/ironhermes-agent/tests/tts_per_turn_wiring.rs`" + §"`crates/ironhermes-tools/tests/not_supported_dispatcher.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-VALIDATION.md (rows for D-01 and D-03-b)
  </read_first>
  <behavior>
    tts_per_turn_wiring.rs MUST contain these `#[test]` (or `#[tokio::test]`) functions:
      - `turn_request_default_has_no_tts_wiring`: builds `TurnRequest::default()` and asserts `req.tts_wiring.is_none()` (regression guard so existing call sites using `..Default::default()` keep compiling).
      - `tts_per_turn_wiring_constructs_with_no_dispatcher`: builds `TtsPerTurnWiring { session_key: SessionKey::new(Platform::Local, "test"), audio_dispatcher: None }` and asserts it compiles + the dispatcher slot is `None` (TUI case).
      - `tts_per_turn_wiring_constructs_with_some_dispatcher`: builds the same struct with `audio_dispatcher: Some(Arc::new(NotSupportedAudioDispatcher::new("discord")) as Arc<dyn AudioDispatcher>)` and asserts the dispatcher slot is `Some` (Discord/Slack case + non-Telegram non-None proof).
      - `agent_runtime_source_declares_tts_wiring_field`: uses `include_str!("../src/agent_runtime.rs")` and asserts `SRC.contains("pub tts_wiring: Option<TtsPerTurnWiring>")` (locks the field type).
      - `agent_runtime_source_calls_register_tts_tools_in_run_turn`: uses the same `include_str!` and asserts `SRC.contains("register_tts_tools(")` AND `SRC.contains("if let Some(ref")` (proves the guarded per-turn call landed).
    not_supported_dispatcher.rs MUST contain:
      - `source_uses_anyhow_bail_not_panic`: `include_str!("../src/not_supported_dispatcher.rs")` + asserts `SRC.contains("anyhow::bail!")` AND `!SRC.contains("panic!")` AND `!SRC.contains(".unwrap()")`.
      - `source_declares_async_trait_impl`: asserts `SRC.contains("#[async_trait]")` AND `SRC.contains("impl") && SRC.contains("AudioDispatcher for NotSupportedAudioDispatcher")` (substring presence — exact whitespace not asserted).
      - `discord_send_audio_returns_err_with_discord_in_message`: `#[tokio::test]` that calls `NotSupportedAudioDispatcher::new("discord").send_audio_file("chat1", &PathBuf::from("/tmp/x.mp3"), None).await` and asserts the result is `Err` with `"discord"` in the error message via `format!("{}", err)`.
      - `slack_send_voice_returns_err_with_slack_in_message`: same pattern with `"slack"` + `send_voice_file`.
  </behavior>
  <action>
    Create the two test files in their respective `tests/` directories. Both files compile against APIs that do not yet exist at this task — that is intentional (RED phase of TDD). They will GREEN as soon as Tasks 2 + 3 land.
    Mirror exactly the `invariants_36_17_5.rs` structure: top-of-file `const SRC: &str = include_str!(...);` at the start of each source-grep test. Use `#[tokio::test]` for the async `send_audio_file` / `send_voice_file` calls (the crate already brings tokio test macros via dev-deps — verify by reading `Cargo.toml` of `ironhermes-tools` first).
    Imports for `tts_per_turn_wiring.rs`: `use ironhermes_agent::{TurnRequest, TtsPerTurnWiring};` `use ironhermes_core::{SessionKey, types::Platform};` `use ironhermes_tools::{AudioDispatcher, NotSupportedAudioDispatcher};` `use std::sync::Arc;`.
    Imports for `not_supported_dispatcher.rs`: `use ironhermes_tools::{AudioDispatcher, NotSupportedAudioDispatcher};` `use std::path::PathBuf;`.
    Do NOT add any `#[ignore]` attributes — all tests run by default. The RED state is expected to compile-fail until Tasks 2 + 3 land; this is the Wave 0 contract. Commit must include a comment in the commit body noting the RED state.
    Do not add any new dev-deps unless a `Cargo.toml` read confirms `tokio` is missing from `[dev-dependencies]` of the respective crate. If present, reuse; if missing, add `tokio = { workspace = true, features = ["test-util", "macros"] }` to `[dev-dependencies]` of the affected crate only.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-agent --tests --no-run 2>&amp;1 | tail -20; cargo build -p ironhermes-tools --tests --no-run 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/ironhermes-agent/tests/tts_per_turn_wiring.rs` exists, contains the 5 named tests listed in &lt;behavior&gt;.
    - File `crates/ironhermes-tools/tests/not_supported_dispatcher.rs` exists, contains the 4 named tests listed in &lt;behavior&gt;.
    - `grep -c "#\[test\]" crates/ironhermes-agent/tests/tts_per_turn_wiring.rs` ≥ 3.
    - `grep -c "#\[tokio::test\]" crates/ironhermes-tools/tests/not_supported_dispatcher.rs` ≥ 2.
    - `grep -c "include_str!" crates/ironhermes-agent/tests/tts_per_turn_wiring.rs` ≥ 2.
    - `grep -c "include_str!" crates/ironhermes-tools/tests/not_supported_dispatcher.rs` ≥ 1.
    - Compile failure at this task is acceptable IF and ONLY IF the failure is `ironhermes_agent::TurnRequest has no field `tts_wiring`` OR `ironhermes_tools::NotSupportedAudioDispatcher not found` — i.e. the APIs Tasks 2+3 will add. Any other compile error (syntax, missing import) blocks the task.
  </acceptance_criteria>
  <done>Both test files exist, contain the named tests, and either compile cleanly or fail only on the two missing APIs (`TurnRequest::tts_wiring` and `NotSupportedAudioDispatcher`). The commit body notes RED state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TtsPerTurnWiring struct + TurnRequest::tts_wiring field + per-turn register_tts_tools call in AgentRuntime::run_turn</name>
  <files>crates/ironhermes-agent/src/agent_runtime.rs</files>
  <read_first>
    - crates/ironhermes-agent/src/agent_runtime.rs (full file — the executor must see TurnRequest, AgentRuntime::run_turn body, and the `bundle.registry` access pattern)
    - crates/ironhermes-agent/src/app_runtime_factory.rs:97-103 (existing `if let Some(ref session_key) = input.session_key { ... register_tts_tools(...) }` block — Task 2's per-turn call mirrors this pattern verbatim)
    - crates/ironhermes-tools/src/registry.rs:638-658 (register_tts_tools signature — confirm field names: session_key, dispatcher, config)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/ironhermes-agent/src/agent_runtime.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 1: Per-Turn TTS Registration, §Common Pitfall 1)
  </read_first>
  <behavior>
    After this task:
      - `crates/ironhermes-agent/src/agent_runtime.rs` declares `pub struct TtsPerTurnWiring { pub session_key: ironhermes_core::SessionKey, pub audio_dispatcher: Option<std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>> }`.
      - `TurnRequest` gains a `pub tts_wiring: Option<TtsPerTurnWiring>` field positioned AFTER `compression_count`, with `#[derive(Default)]` on `TurnRequest` preserved.
      - `AgentRuntime::run_turn` calls `self.bundle.registry.write().await.register_tts_tools(wiring.session_key.clone(), wiring.audio_dispatcher.clone(), self.config.clone())` when `request.tts_wiring.is_some()`, before the agent loop is constructed for that turn.
      - The `pub use` surface of `crates/ironhermes-agent/src/lib.rs` (or `mod.rs` re-exports) exposes `TtsPerTurnWiring` so downstream plans can write `ironhermes_agent::TtsPerTurnWiring`.
      - `cargo test -p ironhermes-agent --test tts_per_turn_wiring` GREENs the 3 Task-1 tests in `tts_per_turn_wiring.rs` that target this surface (`turn_request_default_has_no_tts_wiring`, `tts_per_turn_wiring_constructs_with_no_dispatcher`, `agent_runtime_source_declares_tts_wiring_field`, `agent_runtime_source_calls_register_tts_tools_in_run_turn`).
  </behavior>
  <action>
    Edit `crates/ironhermes-agent/src/agent_runtime.rs`:
      1. Add the `TtsPerTurnWiring` struct definition adjacent to (immediately above) the existing `TurnRequest` struct. Use the exact field types listed in &lt;behavior&gt;. Add a `///` doc comment "Phase 36.17.7 D-01: per-turn TTS session descriptor."
      2. Add `pub tts_wiring: Option<TtsPerTurnWiring>` as the last field of `TurnRequest`. Preserve the existing `#[derive(Default)]`. Because the field is `Option<_>`, default is `None` automatically.
      3. Inside `AgentRuntime::run_turn`, at the very top of the function body — after budget/cancel-token setup but BEFORE the agent loop is constructed for this turn — add the per-turn registration block. Pattern (Rust pseudo-action; reuse identifiers exactly as written here):
         - `if let Some(ref wiring) = request.tts_wiring { let mut reg = self.bundle.registry.write().await; reg.register_tts_tools(wiring.session_key.clone(), wiring.audio_dispatcher.clone(), self.config.clone()); drop(reg); }`
         - Place the block before any line that reads `self.bundle.registry` for tool-schema list construction.
      4. Re-export `TtsPerTurnWiring` from the crate root: in `crates/ironhermes-agent/src/lib.rs` add `pub use agent_runtime::TtsPerTurnWiring;` next to the existing `pub use agent_runtime::TurnRequest;` (or equivalent re-export pattern — read lib.rs first to confirm the existing pattern; mirror exactly).
    Do NOT remove the `session_key: None,` / `telegram_adapter: None,` lines at agent_runtime.rs:215-217 — those are the call-site for `build_app_runtime_bundle` from `from_config` (the durable startup bundle), and they must stay `None` (the startup bundle never has TTS). Plan 05 covers the D-05 invariant that asserts handler-level call sites (gateway/ws/tui) DO contain `session_key: Some(`.
    Pitfall mitigation (RESEARCH Pitfall 1): inspect `ToolRegistry::register` in `crates/ironhermes-tools/src/registry.rs` first. If `register` appends rather than upserts (i.e. would create duplicate `text_to_speech` entries on subsequent turns), insert two `reg.unregister("text_to_speech");` `reg.unregister("send_audio");` calls BEFORE the `reg.register_tts_tools(...)` call. If `register` upserts, no unregister needed — leave the simpler block. Record the verdict in the commit body ("ToolRegistry::register UPSERTS — no unregister needed" or "ToolRegistry::register APPENDS — added unregister calls"). If `unregister` does not exist as a method, add it as a sibling of `register` on `ToolRegistry` with signature `pub fn unregister(&mut self, name: &str)` that removes the entry from the internal map; record in commit body and add `unregister` to PATTERNS context for downstream plans.
    Tokio runtime note: `self.bundle.registry` is `Arc<tokio::sync::RwLock<ToolRegistry>>` per existing pattern. The `.write().await` call is correct inside an `async fn`. Verify `run_turn` is async (it is, per RESEARCH §Verified `run_turn` call site).
    Do NOT add new dependencies. `ironhermes_core` and `ironhermes_tools` are already in `ironhermes-agent`'s `Cargo.toml` deps.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-agent --test tts_per_turn_wiring -- turn_request_default_has_no_tts_wiring agent_runtime_source_declares_tts_wiring_field agent_runtime_source_calls_register_tts_tools_in_run_turn 2>&amp;1 | tail -30; cargo build -p ironhermes-agent 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub struct TtsPerTurnWiring" crates/ironhermes-agent/src/agent_runtime.rs` returns 1.
    - `grep -c "pub tts_wiring: Option<TtsPerTurnWiring>" crates/ironhermes-agent/src/agent_runtime.rs` returns 1.
    - `grep -c "register_tts_tools(" crates/ironhermes-agent/src/agent_runtime.rs` returns ≥ 1 (the new per-turn call).
    - `grep -c "if let Some(ref wiring) = request.tts_wiring" crates/ironhermes-agent/src/agent_runtime.rs` returns 1.
    - `grep -c "pub use agent_runtime::TtsPerTurnWiring" crates/ironhermes-agent/src/lib.rs` returns 1.
    - `cargo build -p ironhermes-agent` exits 0.
    - `cargo test -p ironhermes-agent --test tts_per_turn_wiring -- turn_request_default_has_no_tts_wiring` exits 0.
    - `cargo test -p ironhermes-agent --test tts_per_turn_wiring -- agent_runtime_source_declares_tts_wiring_field` exits 0.
    - `cargo test -p ironhermes-agent --test tts_per_turn_wiring -- agent_runtime_source_calls_register_tts_tools_in_run_turn` exits 0.
    - The existing `crates/ironhermes-agent/tests/invariants_36_17_5.rs` continues to pass (`cargo test -p ironhermes-agent --test invariants_36_17_5` exits 0) — D-15 negative asserts there only cover `app_runtime_factory.rs`, not `agent_runtime.rs`, so this change is scope-compatible.
    - Commit body explicitly records whether `ToolRegistry::register` upserts or appends, and whether `unregister` calls were inserted.
  </acceptance_criteria>
  <done>`TurnRequest::tts_wiring` field exists, `TtsPerTurnWiring` struct is declared and re-exported, `AgentRuntime::run_turn` calls `register_tts_tools` when the slot is `Some`, and 3 of the 5 Task-1 `tts_per_turn_wiring.rs` tests are GREEN (the 2 dispatcher-construction tests stay RED until Task 3 lands `NotSupportedAudioDispatcher`).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship NotSupportedAudioDispatcher in ironhermes-tools (D-03-b)</name>
  <files>crates/ironhermes-tools/src/not_supported_dispatcher.rs, crates/ironhermes-tools/src/lib.rs</files>
  <read_first>
    - crates/ironhermes-tools/src/send_audio_tool.rs (lines 24-40 — the `AudioDispatcher` trait the new struct implements; copy field signatures exactly)
    - crates/ironhermes-tools/src/lib.rs (the existing `pub use` pattern — mirror for the new module)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/ironhermes-tools/src/not_supported_dispatcher.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 7: NotSupportedAudioDispatcher)
  </read_first>
  <behavior>
    After this task:
      - `crates/ironhermes-tools/src/not_supported_dispatcher.rs` declares `pub struct NotSupportedAudioDispatcher { platform_name: &'static str }` with a `pub fn new(platform_name: &'static str) -> Self` constructor.
      - The file implements `#[async_trait] impl crate::AudioDispatcher for NotSupportedAudioDispatcher` with both methods returning `anyhow::bail!("audio dispatch not supported on {}", self.platform_name)`.
      - `crates/ironhermes-tools/src/lib.rs` adds `pub mod not_supported_dispatcher;` AND `pub use not_supported_dispatcher::NotSupportedAudioDispatcher;` (sibling of the existing `AudioDispatcher` re-export — read lib.rs first to confirm exact form).
      - `cargo test -p ironhermes-tools --test not_supported_dispatcher` GREENs all 4 tests authored in Task 1.
      - `cargo test -p ironhermes-agent --test tts_per_turn_wiring -- tts_per_turn_wiring_constructs_with_some_dispatcher` GREENs (it imports `NotSupportedAudioDispatcher` from `ironhermes_tools`).
  </behavior>
  <action>
    Create `crates/ironhermes-tools/src/not_supported_dispatcher.rs`. Mirror the impl shape from PATTERNS §"`crates/ironhermes-tools/src/not_supported_dispatcher.rs`". Concrete identifiers (copy verbatim, do not paraphrase):
      - `use async_trait::async_trait;` `use std::path::PathBuf;`
      - `pub struct NotSupportedAudioDispatcher { platform_name: &'static str }`
      - `impl NotSupportedAudioDispatcher { pub fn new(platform_name: &'static str) -> Self { Self { platform_name } } }`
      - `#[async_trait] impl crate::AudioDispatcher for NotSupportedAudioDispatcher { async fn send_voice_file(...) -> anyhow::Result<()> { anyhow::bail!("audio dispatch not supported on {}", self.platform_name) } async fn send_audio_file(...) -> anyhow::Result<()> { anyhow::bail!("audio dispatch not supported on {}", self.platform_name) } }`
      - Method signatures (exact types — must match the trait): `_chat_id: &str, _path: &PathBuf, _thread_id: Option<&str>`. Underscore-prefix all unused params.
      - File-level doc comment: `//! Phase 36.17.7 D-03-b: stub dispatcher for platforms without audio delivery support (Discord, Slack). Tools still register so the LLM tool-schema list is consistent across platforms; send_audio_file / send_voice_file return a clean Err so the agent can narrate the limitation.`
    Edit `crates/ironhermes-tools/src/lib.rs`:
      - Add `pub mod not_supported_dispatcher;` near the existing `pub mod send_audio_tool;` (or wherever the AudioDispatcher's module is declared — mirror the location and the alphabetical-or-grouped order already used).
      - Add `pub use not_supported_dispatcher::NotSupportedAudioDispatcher;` next to the existing `pub use send_audio_tool::AudioDispatcher;` re-export.
    Do NOT add any new dependencies. `async-trait` and `anyhow` are already workspace deps used by `ironhermes-tools` (verified in RESEARCH §Standard Stack).
    Do NOT touch `send_audio_tool.rs` or any other file beyond the two listed above.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-tools --test not_supported_dispatcher 2>&amp;1 | tail -20; cargo test -p ironhermes-agent --test tts_per_turn_wiring 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/ironhermes-tools/src/not_supported_dispatcher.rs` exists.
    - `grep -c "pub struct NotSupportedAudioDispatcher" crates/ironhermes-tools/src/not_supported_dispatcher.rs` returns 1.
    - `grep -c "impl crate::AudioDispatcher for NotSupportedAudioDispatcher" crates/ironhermes-tools/src/not_supported_dispatcher.rs` returns 1.
    - `grep -c "anyhow::bail!" crates/ironhermes-tools/src/not_supported_dispatcher.rs` returns 2 (one per method).
    - `grep -c "panic!\|\.unwrap()" crates/ironhermes-tools/src/not_supported_dispatcher.rs` returns 0.
    - `grep -c "pub mod not_supported_dispatcher" crates/ironhermes-tools/src/lib.rs` returns 1.
    - `grep -c "pub use not_supported_dispatcher::NotSupportedAudioDispatcher" crates/ironhermes-tools/src/lib.rs` returns 1.
    - `cargo build -p ironhermes-tools` exits 0.
    - `cargo test -p ironhermes-tools --test not_supported_dispatcher` exits 0; all 4 tests pass.
    - `cargo test -p ironhermes-agent --test tts_per_turn_wiring` exits 0; all 5 tests pass (this is the Plan 01 GREEN state).
    - `cargo clippy -p ironhermes-tools --tests -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>`NotSupportedAudioDispatcher` is callable from any crate that depends on `ironhermes-tools`. All Plan 01 Wave 0 tests are GREEN. `ironhermes-agent` and `ironhermes-tools` both build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → AgentRuntime::run_turn | Caller-provided `request.tts_wiring` carries a `SessionKey` and an `Arc<dyn AudioDispatcher>`. Both are constructed by downstream plans (gateway / ws / tui handlers); Plan 01 only defines the slot. |
| LLM → NotSupportedAudioDispatcher | When mounted on Discord/Slack sessions (future plan), the LLM may invoke `send_audio` and receive an `Err`. The error message contains the static `platform_name` literal — no user-controlled data crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.17.7-01-01 | Tampering | `register_tts_tools` per-turn call | mitigate | Per-turn call sites only provide `Arc<dyn AudioDispatcher>` from in-process trusted constructors. The trait itself enforces `Send + Sync` so cross-thread tampering via `&mut self` aliasing is impossible. The `bundle.registry.write().await` lock guarantees exclusive access during registration. |
| T-36.17.7-01-02 | Repudiation | NotSupportedAudioDispatcher Err message | accept | The `&'static str` platform name is compile-time controlled; no log injection. The `anyhow::bail!` message is plain prose; no user input is interpolated. Accept — low risk single-operator deployment. |
| T-36.17.7-01-03 | Denial of Service | Per-turn `register_tts_tools` upsert | mitigate | Pitfall 1 mitigation in Task 2: if `ToolRegistry::register` appends, executor must add `unregister` calls before re-registering to prevent unbounded growth of the tool-schema list across turns. Verdict recorded in commit body. |
| T-36.17.7-01-SC | Tampering | cargo deps (no new packages) | mitigate | Plan 01 introduces zero new external dependencies (`async-trait`, `anyhow`, `ironhermes_core`, `ironhermes_tools` are all workspace-existing). Per RESEARCH §Package Legitimacy Audit: "No new packages are introduced by this phase." No package-legitimacy human checkpoint needed for Plan 01. |
</threat_model>

<verify_plan>
1. `cargo build -p ironhermes-agent -p ironhermes-tools` exits 0.
2. `cargo test -p ironhermes-agent --test tts_per_turn_wiring` exits 0 (all 5 tests GREEN).
3. `cargo test -p ironhermes-tools --test not_supported_dispatcher` exits 0 (all 4 tests GREEN).
4. `cargo test -p ironhermes-agent --test invariants_36_17_5` exits 0 (no regression on Phase 36.17.5 invariants).
5. `cargo clippy -p ironhermes-agent -p ironhermes-tools --tests -- -D warnings` exits 0.
6. `grep -c "pub tts_wiring: Option<TtsPerTurnWiring>" crates/ironhermes-agent/src/agent_runtime.rs` returns 1.
7. `grep -c "pub use not_supported_dispatcher::NotSupportedAudioDispatcher" crates/ironhermes-tools/src/lib.rs` returns 1.
</verify_plan>

<success_criteria>
- `TurnRequest` has a `tts_wiring: Option<TtsPerTurnWiring>` slot and `AgentRuntime::run_turn` registers TTS tools when the slot is `Some(_)`.
- `NotSupportedAudioDispatcher` is callable from `ironhermes-tools` and returns an `Err` whose message names the platform.
- All 9 Plan-01 Wave 0 tests are GREEN.
- Zero regression: Phase 36.17.5 invariants test still passes, `ironhermes-agent` builds, `ironhermes-tools` builds.
- No new external dependencies introduced.
</success_criteria>

<output>
Create `.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-01-SUMMARY.md` documenting:
- The 3 files created/modified
- The ToolRegistry::register upsert-vs-append verdict (and whether `unregister` was added)
- Plan-01 must_haves achieved (truth #1, #3 + artifacts + key_links #1, #2)
- Test counts (9 tests added; 5 cross-crate dependencies on `TurnRequest::tts_wiring` and `NotSupportedAudioDispatcher` are now satisfied for Plans 02-05)
</output>
