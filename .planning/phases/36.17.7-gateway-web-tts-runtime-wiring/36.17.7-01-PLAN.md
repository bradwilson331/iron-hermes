---
phase: 36.17.7
slug: gateway-web-tts-runtime-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-agent/src/agent_runtime.rs
  - crates/ironhermes-agent/src/app_runtime_factory.rs
  - crates/ironhermes-agent/src/lib.rs
  - crates/ironhermes-tools/src/lib.rs
  - crates/ironhermes-tools/src/not_supported_dispatcher.rs
  - crates/ironhermes-agent/tests/tts_per_turn_wiring.rs
  - crates/ironhermes-tools/tests/not_supported_dispatcher.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "TurnRequest carries an optional TtsPerTurnWiring slot that downstream surfaces can populate"
    - "AgentRuntime::run_turn registers text_to_speech + send_audio on bundle.registry when TtsPerTurnWiring is Some, before agent loop construction"
    - "AppRuntimeFactoryInput derives Default; agent_runtime.rs's from_config builds the startup bundle via ..Default::default() — the literal `session_key: None,` no longer appears in agent_runtime.rs (D-05 prep)"
    - "NotSupportedAudioDispatcher exists in ironhermes-tools and returns an Err naming the platform for both send_voice_file and send_audio_file"
    - "A behavioral test (#[tokio::test] tts_wiring_registers_tools_in_run_turn) proves that run_turn registers text_to_speech + send_audio when tts_wiring is Some"
  artifacts:
    - path: "crates/ironhermes-agent/src/agent_runtime.rs"
      provides: "TurnRequest::tts_wiring field + TtsPerTurnWiring struct + per-turn register_tts_tools call in run_turn + from_config rewrite using ..Default::default()"
      contains: "pub tts_wiring: Option<TtsPerTurnWiring>"
    - path: "crates/ironhermes-agent/src/app_runtime_factory.rs"
      provides: "#[derive(Default)] on AppRuntimeFactoryInput so callers can use ..Default::default()"
      contains: "#[derive(Default)]"
    - path: "crates/ironhermes-tools/src/not_supported_dispatcher.rs"
      provides: "NotSupportedAudioDispatcher stub for Discord/Slack platforms"
      exports: ["NotSupportedAudioDispatcher"]
    - path: "crates/ironhermes-agent/tests/tts_per_turn_wiring.rs"
      provides: "Unit + source-grep + behavioral tests for D-01 wiring (incl. the tts_wiring_registers_tools_in_run_turn behavioral test)"
    - path: "crates/ironhermes-tools/tests/not_supported_dispatcher.rs"
      provides: "Unit + source-grep tests for D-03-b error behavior"
  key_links:
    - from: "TurnRequest::tts_wiring"
      to: "ToolRegistry::register_tts_tools"
      via: "AgentRuntime::run_turn registration block"
      pattern: "self.bundle.registry.*register_tts_tools"
    - from: "ironhermes-tools::NotSupportedAudioDispatcher"
      to: "AudioDispatcher trait"
      via: "#[async_trait] impl in ironhermes-tools"
      pattern: "impl.*AudioDispatcher for NotSupportedAudioDispatcher"
---

<plan_layout_rationale>
Phase 36.17.7 has 3 platform surfaces (gateway/Telegram, web, TUI) × multiple sub-deliverables. The decomposition:

- **Plan 01 (this plan, Wave 1):** Foundations. Owns the `agent_runtime.rs` struct extension + per-turn registry call, plus the `NotSupportedAudioDispatcher` stub, plus the `AppRuntimeFactoryInput` `Default` derive + `from_config` `..Default::default()` rewrite that satisfies D-05 negative-assert (REVISION fix BLOCKER 1). Every downstream plan depends on the new `TurnRequest::tts_wiring` field and the `TtsPerTurnWiring` struct. Plan 01 is autonomous and must merge before Waves 2–3 can begin. Frontmatter `requirements: []` (REVISION fix HIGH 5: A5 is owned by Plan 05 Task 6 source-grep invariants_36_17_7.rs which depends on Plan 01's rewrites).
- **Plan 02 (Wave 2):** Gateway/Telegram surface. Wires per-turn `TtsPerTurnWiring` in `handler.rs::run_agent` + Discord/Slack `NotSupportedAudioDispatcher` mounting (REVISION fix HIGH 6). Covers A1 + A3.
- **Plan 03 (Wave 2):** TUI surface. Covers A4-TUI.
- **Plan 04 (Wave 2):** Web runtime surface. Mandates Blob URL first-play (REVISION fix HIGH 4) + unconditional Binary recv arm (REVISION fix HIGH 7) + RSX interpolation note (REVISION fix MEDIUM 10). Covers A2 + A4-web.
- **Plan 05 (Wave 3):** Replay route + GC + config + toolset Registered column (Path B, REVISION fix BLOCKER 2) + D-05 invariants. Covers A5, A6, A7.
</plan_layout_rationale>

<objective>
Establish the foundation for per-turn TTS registration across all three runtime surfaces:
1. Add `TtsPerTurnWiring` + `TurnRequest::tts_wiring` to `crates/ironhermes-agent/src/agent_runtime.rs`.
2. Derive `Default` on `AppRuntimeFactoryInput` (`crates/ironhermes-agent/src/app_runtime_factory.rs`) so callers can omit per-field initialization.
3. Rewrite `AgentRuntime::from_config`'s `AppRuntimeFactoryInput { ... session_key: None, telegram_adapter: None }` literal at `agent_runtime.rs:213-217` to use `..Default::default()`, eliminating the `session_key: None,` literal that the D-05 invariant test asserts absent (BLOCKER 1 fix — Plan 05 Task 6's invariants_36_17_7.rs Test 1 stays exactly as designed: `!SRC.contains("session_key: None,")`).
4. Wire `register_tts_tools` into `AgentRuntime::run_turn` at the start of each turn when `tts_wiring` is `Some(_)`.
5. Ship `NotSupportedAudioDispatcher` in `ironhermes-tools` so Plan 02 can mount it on Discord/Slack start paths (HIGH 6 prep).

Purpose: Every Wave 2 surface plan needs the `TurnRequest.tts_wiring` field AND the `Default`-derived `AppRuntimeFactoryInput` AND the `NotSupportedAudioDispatcher` to compile. Plan 01 ships all three.

Output:
- `TurnRequest` extended; `TtsPerTurnWiring` struct declared; per-turn registration call in `run_turn`.
- `AppRuntimeFactoryInput` derives `Default`; `from_config` uses `..Default::default()`.
- `NotSupportedAudioDispatcher` shipped + re-exported from `ironhermes-tools` lib.rs.
- Wave 0 test files including a behavioral `#[tokio::test] tts_wiring_registers_tools_in_run_turn` (MEDIUM 8 fix).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-CONTEXT.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md
@.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md
@crates/ironhermes-agent/src/agent_runtime.rs
@crates/ironhermes-agent/src/app_runtime_factory.rs
@crates/ironhermes-agent/src/lib.rs
@crates/ironhermes-tools/src/registry.rs
@crates/ironhermes-tools/src/send_audio_tool.rs
@crates/ironhermes-tools/src/lib.rs
@crates/ironhermes-agent/tests/invariants_36_17_5.rs

<interfaces>
<!-- Critical signatures the executor needs. Extracted from CONTEXT/RESEARCH/PATTERNS. -->

From crates/ironhermes-tools/src/send_audio_tool.rs:24-40 (AudioDispatcher trait):
```rust
#[async_trait]
pub trait AudioDispatcher: Send + Sync {
    async fn send_voice_file(&self, chat_id: &str, path: &PathBuf, thread_id: Option<&str>) -> anyhow::Result<()>;
    async fn send_audio_file(&self, chat_id: &str, path: &PathBuf, thread_id: Option<&str>) -> anyhow::Result<()>;
}
```

From crates/ironhermes-tools/src/registry.rs:646-658 (register_tts_tools signature):
```rust
pub fn register_tts_tools(
    &mut self,
    session_key: ironhermes_core::SessionKey,
    dispatcher: Option<Arc<dyn crate::AudioDispatcher>>,
    config: Arc<ironhermes_core::Config>,
)
```

From crates/ironhermes-agent/src/agent_runtime.rs:93-110 (current TurnRequest — extend):
```rust
#[derive(Default)]
pub struct TurnRequest {
    pub messages: Vec<ChatMessage>,
    pub session_id: String,
    pub cancel_token: Option<CancellationToken>,
    pub stream: Option<StreamCallback>,
    pub tool_progress: Option<ToolProgressCallback>,
    pub tool_result: Option<ToolResultCallback>,
    pub trajectory_writer: Option<Arc<dyn TrajectoryWriterHandle>>,
    pub pressure_tracker: Option<Arc<PressureTracker>>,
    pub state_store: Option<Arc<std::sync::Mutex<StateStore>>>,
    pub compression_count: usize,
    // ADD: pub tts_wiring: Option<TtsPerTurnWiring>,
}
```

From crates/ironhermes-agent/src/agent_runtime.rs:213-217 (CURRENT — Plan 01 REWRITES this block):
```rust
// Phase 36.17.5 D-15: per-turn threading deferred to a follow-up phase.
let input = AppRuntimeFactoryInput {
    config: config.clone(),
    resolver,
    cwd,
    process_registry,
    memory_manager,
    delegate_task,
    hooks_config,
    emit_mcp_startup_logs,
    session_key: None,       // ← Plan 01 REMOVES this literal via ..Default::default() rewrite
    telegram_adapter: None,  // ← Plan 01 REMOVES this literal via ..Default::default() rewrite
};
```

After Plan 01 (the literal post-rewrite shape):
```rust
// Phase 36.17.7 Plan 01: startup bundle has no TTS — per-turn TTS now lives in TurnRequest.tts_wiring.
// Using ..Default::default() eliminates `session_key: None,` from the source (D-05 prep).
let input = AppRuntimeFactoryInput {
    config: config.clone(),
    resolver,
    cwd,
    process_registry,
    memory_manager,
    delegate_task,
    hooks_config,
    emit_mcp_startup_logs,
    ..Default::default()
};
```

From crates/ironhermes-agent/src/app_runtime_factory.rs:35-67 (current AppRuntimeFactoryInput — add `#[derive(Default)]`):
```rust
// CURRENT — no derive on the struct:
pub struct AppRuntimeFactoryInput {
    pub config: Arc<Config>,
    pub resolver: Arc<ProviderResolver>,
    pub cwd: PathBuf,
    pub process_registry: Arc<RwLock<ProcessRegistry>>,
    pub memory_manager: Option<SharedMemoryManager>,
    pub delegate_task: Option<DelegateTaskWiring>,
    pub hooks_config: HooksConfig,
    pub emit_mcp_startup_logs: bool,
    pub session_key: Option<ironhermes_core::SessionKey>,
    pub telegram_adapter: Option<Arc<dyn ironhermes_tools::AudioDispatcher>>,
}

// AFTER Plan 01 Task 2 Step 0:
#[derive(Default)]
pub struct AppRuntimeFactoryInput { ... unchanged fields ... }
```

Default-derivation analysis for `AppRuntimeFactoryInput`:
- `Arc<Config>`, `Arc<ProviderResolver>`, `Arc<RwLock<ProcessRegistry>>` — `Arc<T>` requires `T: Default`. `Config: Default` (verified in 36.17.6 via `default_config_matches_expectations` test). `ProviderResolver`: must verify; if no `Default`, executor adds `#[derive(Default)]` to it OR (preferred) wraps the relevant Arcs in `Option<...>` so they default to `None`. Read `provider_resolver.rs` / `process_registry.rs` first to confirm; document choice in commit body.
- `PathBuf: Default = PathBuf::new()` ✓
- `Option<...>`, `bool`, `HooksConfig` — verify `HooksConfig: Default` (likely true from existing usage). If not, derive it.

If any non-Option field lacks Default and cannot be cheaply made Default, fallback Path: convert `config`, `resolver`, `process_registry`, `cwd`, `hooks_config` to `Option<...>` slots in `AppRuntimeFactoryInput` only at this struct level (the `build_app_runtime_bundle` consumer unwraps via `.expect("startup bundle requires config")`). Document Path A (all fields default) vs Path B (Option-wrap missing-Default fields) in commit body. Path A preferred if cheap.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — author tts_per_turn_wiring.rs (incl. behavioral test) + not_supported_dispatcher.rs test files</name>
  <files>crates/ironhermes-agent/tests/tts_per_turn_wiring.rs, crates/ironhermes-tools/tests/not_supported_dispatcher.rs</files>
  <read_first>
    - crates/ironhermes-agent/tests/invariants_36_17_5.rs (canonical `include_str!` source-grep pattern)
    - crates/ironhermes-tools/tests/tts_tools.rs (Rust async unit-test patterns)
    - crates/ironhermes-agent/src/agent_runtime.rs (full file — verify whether `AgentRuntime::for_tests()` exists or how an `AgentRuntime` is buildable in tests; the behavioral test below depends on a constructor reachable from tests)
    - crates/ironhermes-tools/src/registry.rs (read ToolRegistry::get_definition / contains-by-name / debug-list method — the behavioral test asserts presence of `text_to_speech` and `send_audio` in the registry after `run_turn`)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/ironhermes-agent/tests/tts_per_turn_wiring.rs`" + §"`crates/ironhermes-tools/tests/not_supported_dispatcher.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-VALIDATION.md (rows D-01 and D-03-b)
  </read_first>
  <behavior>
    tts_per_turn_wiring.rs MUST contain these `#[test]` / `#[tokio::test]` functions:
      1. `turn_request_default_has_no_tts_wiring`: builds `TurnRequest::default()` and asserts `req.tts_wiring.is_none()` (regression guard for `..Default::default()` callers).
      2. `tts_per_turn_wiring_constructs_with_no_dispatcher`: builds `TtsPerTurnWiring { session_key: SessionKey::new(Platform::Local, "test"), audio_dispatcher: None }` and asserts the dispatcher slot is `None` (TUI case).
      3. `tts_per_turn_wiring_constructs_with_some_dispatcher`: builds the same struct with `audio_dispatcher: Some(Arc::new(NotSupportedAudioDispatcher::new("discord")) as Arc<dyn AudioDispatcher>)` and asserts `Some` (Discord/Slack case).
      4. `agent_runtime_source_declares_tts_wiring_field`: `include_str!("../src/agent_runtime.rs")` + asserts `SRC.contains("pub tts_wiring: Option<TtsPerTurnWiring>")`.
      5. `agent_runtime_source_calls_register_tts_tools_in_run_turn`: same include + asserts `SRC.contains("register_tts_tools(")` AND `SRC.contains("if let Some(ref")`.
      6. `agent_runtime_source_uses_default_default_in_from_config` (REVISION BLOCKER 1 prep): asserts `SRC.contains("..Default::default()")` AND `!SRC.contains("session_key: None,")` AND `!SRC.contains("telegram_adapter: None,")` — locks the D-05 negative-assert.
      7. `app_runtime_factory_input_derives_default`: `include_str!("../src/app_runtime_factory.rs")` + asserts `SRC.contains("#[derive(Default)]")` AND `SRC.contains("pub struct AppRuntimeFactoryInput")`.
      8. **`#[tokio::test] async fn tts_wiring_registers_tools_in_run_turn` (REVISION MEDIUM 8 behavioral test)**: builds a minimal `AgentRuntime` via `AgentRuntime::for_tests()` (or the analogous test-feature constructor — read `agent_runtime.rs` first; if missing, this test is gated `#[cfg(feature = "test-support")]` and Task 2 ships the constructor). Builds a `TurnRequest::default()` with `tts_wiring: Some(TtsPerTurnWiring { session_key: SessionKey::new(Platform::Local, "test"), audio_dispatcher: None })`. Calls `runtime.run_turn(request).await` — the call may legitimately Err on missing LLM (the test does NOT need a successful turn; it only needs the registry mutation to have happened). After the call, the test reads `runtime.bundle.registry.read().await` and asserts `registry.get_definition("text_to_speech").is_some()` AND `registry.get_definition("send_audio").is_some()`. If `get_definition` isn't the accessor name, use whatever the registry exposes (`tools.contains_key`, `definitions().iter().any(...)`, etc.) — pick from registry.rs read. If the test cannot construct a real AgentRuntime in unit-test scope, fall back to a direct integration on the production registry: instantiate the ToolRegistry, call `register_tts_tools(SessionKey::new(Platform::Local, "test"), None, Arc::new(Config::default()))` directly, and assert both tools registered. Document the chosen path in commit body. This test fails RED until Task 2 lands the per-turn registration block.

    not_supported_dispatcher.rs MUST contain:
      1. `source_uses_anyhow_bail_not_panic`: `include_str!("../src/not_supported_dispatcher.rs")` + `SRC.contains("anyhow::bail!")` AND `!SRC.contains("panic!")` AND `!SRC.contains(".unwrap()")`.
      2. `source_declares_async_trait_impl`: `SRC.contains("#[async_trait]")` AND `SRC.contains("AudioDispatcher for NotSupportedAudioDispatcher")`.
      3. `#[tokio::test] discord_send_audio_returns_err_with_discord_in_message`: calls `NotSupportedAudioDispatcher::new("discord").send_audio_file("chat1", &PathBuf::from("/tmp/x.mp3"), None).await` and asserts result is Err with `"discord"` in `format!("{}", err)`.
      4. `#[tokio::test] slack_send_voice_returns_err_with_slack_in_message`: same pattern with `"slack"` + `send_voice_file`.
  </behavior>
  <action>
    Create both test files. RED-state compile failures on missing APIs (`TurnRequest::tts_wiring`, `NotSupportedAudioDispatcher`, `AppRuntimeFactoryInput: Default`, the behavioral test's registry access path) are expected and intentional — Tasks 2 + 3 GREEN them.

    Imports for `tts_per_turn_wiring.rs`: `use ironhermes_agent::{TurnRequest, TtsPerTurnWiring};` `use ironhermes_core::{SessionKey, types::Platform, Config};` `use ironhermes_tools::{AudioDispatcher, NotSupportedAudioDispatcher, ToolRegistry};` `use std::sync::Arc;`. The behavioral test additionally imports whatever AgentRuntime construction path exists (`use ironhermes_agent::AgentRuntime;` if for_tests is public; else fall through to direct ToolRegistry path).

    Imports for `not_supported_dispatcher.rs`: `use ironhermes_tools::{AudioDispatcher, NotSupportedAudioDispatcher};` `use std::path::PathBuf;`.

    Confirm `tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }` in `[dev-dependencies]` of both crates. If absent in either, add. Commit body documents the dev-dep additions.

    Commit body notes RED state, expected failures, and the behavioral test path chosen (real AgentRuntime vs direct ToolRegistry).
  </action>
  <verify>
    <automated>cargo build -p ironhermes-agent --tests --no-run 2>&amp;1 | tail -20; cargo build -p ironhermes-tools --tests --no-run 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/ironhermes-agent/tests/tts_per_turn_wiring.rs` exists with all 8 named tests.
    - File `crates/ironhermes-tools/tests/not_supported_dispatcher.rs` exists with all 4 named tests.
    - `grep -c "#\[test\]\|#\[tokio::test\]" crates/ironhermes-agent/tests/tts_per_turn_wiring.rs` returns ≥ 8.
    - `grep -c "#\[test\]\|#\[tokio::test\]" crates/ironhermes-tools/tests/not_supported_dispatcher.rs` returns ≥ 4.
    - `grep -c "tts_wiring_registers_tools_in_run_turn" crates/ironhermes-agent/tests/tts_per_turn_wiring.rs` returns 1 (REVISION MEDIUM 8 marker).
    - `grep -c "include_str!" crates/ironhermes-agent/tests/tts_per_turn_wiring.rs` returns ≥ 2 (`agent_runtime.rs` + `app_runtime_factory.rs`).
    - Compile failure acceptable IF AND ONLY IF failures are on the planned-missing APIs: `TurnRequest::tts_wiring` field, `NotSupportedAudioDispatcher`, `AppRuntimeFactoryInput: Default`, or `AgentRuntime::for_tests` if that's the chosen behavioral path. Any other compile/syntax error blocks the task.
  </acceptance_criteria>
  <done>Both Wave 0 test files exist with all named tests including the new behavioral `tts_wiring_registers_tools_in_run_turn`. Commit body notes RED state + behavioral-test path choice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive Default on AppRuntimeFactoryInput; add TtsPerTurnWiring + TurnRequest.tts_wiring; rewrite from_config to use ..Default::default(); add per-turn register_tts_tools call in run_turn</name>
  <files>crates/ironhermes-agent/src/agent_runtime.rs, crates/ironhermes-agent/src/app_runtime_factory.rs, crates/ironhermes-agent/src/lib.rs</files>
  <read_first>
    - crates/ironhermes-agent/src/agent_runtime.rs (full file — TurnRequest at ~93-110, AgentRuntime::run_turn body, AgentRuntime::from_config at ~150-246 incl. the AppRuntimeFactoryInput literal at 213-217, lib.rs re-exports)
    - crates/ironhermes-agent/src/app_runtime_factory.rs (full file — AppRuntimeFactoryInput struct at 35-67, plus the guard at 97-103 and `default_input()` test helper at ~347 — verify NOT renaming or removing that helper)
    - crates/ironhermes-tools/src/registry.rs (ToolRegistry::register semantics — read body to determine upsert vs append per Pitfall 1)
    - crates/ironhermes-core/src/config.rs (verify Config: Default exists)
    - The provider_resolver module + process_registry module (verify Default availability for `Arc<ProviderResolver>` + `Arc<RwLock<ProcessRegistry>>`)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/ironhermes-agent/src/agent_runtime.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 1, §Common Pitfall 1, §Pitfall 7)
  </read_first>
  <behavior>
    After this task:
    - `crates/ironhermes-agent/src/app_runtime_factory.rs`'s `AppRuntimeFactoryInput` struct carries `#[derive(Default)]`. All field types are `Default`-compatible OR Path B is applied (Option-wrap missing-Default fields).
    - `crates/ironhermes-agent/src/agent_runtime.rs` declares `pub struct TtsPerTurnWiring { pub session_key: ironhermes_core::SessionKey, pub audio_dispatcher: Option<std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>> }`.
    - `TurnRequest` gains `pub tts_wiring: Option<TtsPerTurnWiring>` as the final field. `#[derive(Default)]` preserved.
    - `AgentRuntime::from_config` builds `AppRuntimeFactoryInput` using `..Default::default()` for the last fields — the literal strings `session_key: None,` and `telegram_adapter: None,` no longer appear in `agent_runtime.rs` (BLOCKER 1 fix locking the D-05 negative-assert).
    - `AgentRuntime::run_turn` body, at the top before agent-loop construction, calls `register_tts_tools` on `self.bundle.registry.write().await` when `request.tts_wiring.is_some()`. The block is exactly: `if let Some(ref wiring) = request.tts_wiring { let mut reg = self.bundle.registry.write().await; reg.register_tts_tools(wiring.session_key.clone(), wiring.audio_dispatcher.clone(), self.config.clone()); drop(reg); }`. (Pitfall 1 unregister calls precede this block if executor's registry.rs read shows `register` appends rather than upserts.)
    - `crates/ironhermes-agent/src/lib.rs` re-exports `TtsPerTurnWiring`: `pub use agent_runtime::TtsPerTurnWiring;`.
    - **If chosen behavioral path in Task 1 was `AgentRuntime::for_tests`**: this task ships the `for_tests` constructor under `#[cfg(feature = "test-support")]` returning a minimal `AgentRuntime` with a default Config + ToolRegistry. Otherwise no test-feature changes needed.
    - `cargo test -p ironhermes-agent --test tts_per_turn_wiring` exits 0 for 7 of the 8 tests (the 8th — `tts_wiring_registers_tools_in_run_turn` — also passes IF the behavioral path can complete; if registry write happens before any LLM call, the test passes even when run_turn ultimately Errs on missing provider). If the behavioral test can't be made green from a fake AgentRuntime in this task, document in commit body and rely on the production behavioral coverage via Plan 02 + UAT-T audible result.
  </behavior>
  <action>
    Edit `crates/ironhermes-agent/src/app_runtime_factory.rs`:
      Step 0 (BLOCKER 1 prep) — Add `#[derive(Default)]` to `AppRuntimeFactoryInput`. If any field type is not `Default`:
        - **Path A (preferred):** Add `#[derive(Default)]` to that type if it's project-owned (e.g. `ProviderResolver`, `ProcessRegistry`, `HooksConfig`).
        - **Path B (fallback):** Wrap the non-Default field in `Option<...>` and update `build_app_runtime_bundle` to `.expect("startup bundle requires <field>")` at consumption.
        Record choice + per-field verdict in commit body.

    Edit `crates/ironhermes-agent/src/agent_runtime.rs`:
      Step 1 — Add `pub struct TtsPerTurnWiring { pub session_key: ironhermes_core::SessionKey, pub audio_dispatcher: Option<std::sync::Arc<dyn ironhermes_tools::AudioDispatcher>> }` directly above `TurnRequest`. Doc comment: `/// Phase 36.17.7 D-01: per-turn TTS session descriptor.`
      Step 2 — Add `pub tts_wiring: Option<TtsPerTurnWiring>` as the last field of `TurnRequest`. Preserve `#[derive(Default)]`.
      Step 3 (BLOCKER 1 fix) — Locate `AgentRuntime::from_config`'s `AppRuntimeFactoryInput { ... session_key: None, telegram_adapter: None };` literal (~lines 213-217 per RESEARCH §Verified agent_runtime.rs Lines That Must Change). Rewrite the literal to use `..Default::default()` for the last fields:
        ```
        // Phase 36.17.7 Plan 01 (BLOCKER 1 fix for D-05): startup bundle has no TTS.
        // Per-turn TTS lives in TurnRequest.tts_wiring → AgentRuntime::run_turn.
        // Using ..Default::default() eliminates `session_key: None,` from the source
        // so the D-05 invariant in invariants_36_17_7.rs Test 1 GREENs.
        let input = AppRuntimeFactoryInput {
            config: config.clone(),
            resolver,
            cwd,
            process_registry,
            memory_manager,
            delegate_task,
            hooks_config,
            emit_mcp_startup_logs,
            ..Default::default()
        };
        ```
        Verify no other line in `agent_runtime.rs` contains the literal `session_key: None,` (search before committing). The `default_input()` test helper in `app_runtime_factory.rs` MAY still contain it — that's fine (invariants_36_17_7.rs Test 1 targets `agent_runtime.rs` only per Pitfall 7).
      Step 4 — Inside `AgentRuntime::run_turn`, at the very top of the function body, add the per-turn registration block:
        ```
        // Phase 36.17.7 D-01: register TTS tools for this turn's session.
        if let Some(ref wiring) = request.tts_wiring {
            let mut reg = self.bundle.registry.write().await;
            reg.register_tts_tools(
                wiring.session_key.clone(),
                wiring.audio_dispatcher.clone(),
                self.config.clone(),
            );
            drop(reg);
        }
        ```
        If `ToolRegistry::register` appends (Pitfall 1), prepend `reg.unregister("text_to_speech"); reg.unregister("send_audio");` before the `reg.register_tts_tools(...)` call. If `unregister` doesn't exist, add it to `crates/ironhermes-tools/src/registry.rs` as a sibling method `pub fn unregister(&mut self, name: &str) { self.tools.remove(name); /* ... wherever the internal map field is */ }`. Document verdict + whether `unregister` was added in commit body.
      Step 5 — In `crates/ironhermes-agent/src/lib.rs`, add `pub use agent_runtime::TtsPerTurnWiring;` adjacent to the existing `pub use agent_runtime::TurnRequest;`.
      Step 6 (conditional) — If Task 1's behavioral test chose the `AgentRuntime::for_tests` path AND the constructor doesn't already exist, ship it under `#[cfg(feature = "test-support")]` returning a minimal `AgentRuntime` (Config::default, empty ToolRegistry, no provider). The constructor's purpose is solely to enable per-turn registration testing without a live provider — `run_turn` may legitimately Err on the LLM call, but the registry mutation already happened before that. If `for_tests` exists with adequate shape, reuse it.

    Do NOT modify the `default_input()` test helper in `app_runtime_factory.rs:347` (Pitfall 7).
    Do NOT add new dependencies.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-agent --test tts_per_turn_wiring 2>&amp;1 | tail -30; cargo build -p ironhermes-agent 2>&amp;1 | tail -10; grep -c "session_key: None," crates/ironhermes-agent/src/agent_runtime.rs; grep -c "..Default::default()" crates/ironhermes-agent/src/agent_runtime.rs; grep -c "#\[derive(Default)\]" crates/ironhermes-agent/src/app_runtime_factory.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub struct TtsPerTurnWiring" crates/ironhermes-agent/src/agent_runtime.rs` returns 1.
    - `grep -c "pub tts_wiring: Option<TtsPerTurnWiring>" crates/ironhermes-agent/src/agent_runtime.rs` returns 1.
    - `grep -c "if let Some(ref wiring) = request.tts_wiring" crates/ironhermes-agent/src/agent_runtime.rs` returns 1.
    - `grep -c "register_tts_tools(" crates/ironhermes-agent/src/agent_runtime.rs` returns ≥ 1.
    - **BLOCKER 1 fix verification: `grep -c "session_key: None," crates/ironhermes-agent/src/agent_runtime.rs` returns 0.**
    - **BLOCKER 1 fix verification: `grep -c "telegram_adapter: None," crates/ironhermes-agent/src/agent_runtime.rs` returns 0.**
    - **BLOCKER 1 fix verification: `grep -c "..Default::default()" crates/ironhermes-agent/src/agent_runtime.rs` returns ≥ 1.**
    - `grep -c "#\[derive(Default)\]" crates/ironhermes-agent/src/app_runtime_factory.rs` returns ≥ 1 (the new derive on AppRuntimeFactoryInput).
    - `grep -c "pub struct AppRuntimeFactoryInput" crates/ironhermes-agent/src/app_runtime_factory.rs` returns 1 (no rename).
    - `grep -c "pub use agent_runtime::TtsPerTurnWiring" crates/ironhermes-agent/src/lib.rs` returns 1.
    - `cargo build -p ironhermes-agent` exits 0.
    - `cargo test -p ironhermes-agent --test tts_per_turn_wiring -- agent_runtime_source_uses_default_default_in_from_config` exits 0 (the BLOCKER 1 lock test).
    - `cargo test -p ironhermes-agent --test tts_per_turn_wiring -- app_runtime_factory_input_derives_default` exits 0.
    - `cargo test -p ironhermes-agent --test tts_per_turn_wiring -- turn_request_default_has_no_tts_wiring agent_runtime_source_declares_tts_wiring_field agent_runtime_source_calls_register_tts_tools_in_run_turn` exits 0.
    - `cargo test -p ironhermes-agent --test invariants_36_17_5` exits 0 (no regression).
    - Commit body records: (a) ToolRegistry::register upsert-vs-append verdict, (b) Path A vs Path B for Default derivation, (c) behavioral-test path resolution.
  </acceptance_criteria>
  <done>`AppRuntimeFactoryInput` derives Default; `from_config` uses `..Default::default()`; `TurnRequest.tts_wiring` and per-turn register block live in `agent_runtime.rs`. The literal `session_key: None,` no longer appears in `agent_runtime.rs`. 6 of 8 Task-1 tests GREEN (the 2 dispatcher-Some tests + the behavioral test stay RED until Task 3 lands NotSupportedAudioDispatcher).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship NotSupportedAudioDispatcher in ironhermes-tools (D-03-b)</name>
  <files>crates/ironhermes-tools/src/not_supported_dispatcher.rs, crates/ironhermes-tools/src/lib.rs</files>
  <read_first>
    - crates/ironhermes-tools/src/send_audio_tool.rs (lines 24-40 — AudioDispatcher trait)
    - crates/ironhermes-tools/src/lib.rs (existing pub use pattern to mirror)
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-PATTERNS.md (§"`crates/ironhermes-tools/src/not_supported_dispatcher.rs`")
    - .planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-RESEARCH.md (§Pattern 7)
  </read_first>
  <behavior>
    After this task:
      - `crates/ironhermes-tools/src/not_supported_dispatcher.rs` declares `pub struct NotSupportedAudioDispatcher { platform_name: &'static str }` with `pub fn new(platform_name: &'static str) -> Self`.
      - The file implements `#[async_trait] impl crate::AudioDispatcher for NotSupportedAudioDispatcher` with both methods returning `anyhow::bail!("audio dispatch not supported on {}", self.platform_name)`.
      - `crates/ironhermes-tools/src/lib.rs` adds `pub mod not_supported_dispatcher;` AND `pub use not_supported_dispatcher::NotSupportedAudioDispatcher;`.
      - `cargo test -p ironhermes-tools --test not_supported_dispatcher` GREENs all 4 tests.
      - `cargo test -p ironhermes-agent --test tts_per_turn_wiring` exits 0 for all 8 tests (including the behavioral test from Task 1 if the chosen path is reachable in this task; otherwise the behavioral test stays RED and its coverage is replaced by Plan 02 UAT-T's audible result — document in commit body).
  </behavior>
  <action>
    Create `crates/ironhermes-tools/src/not_supported_dispatcher.rs` per PATTERNS:
      - `use async_trait::async_trait;` `use std::path::PathBuf;`
      - `pub struct NotSupportedAudioDispatcher { platform_name: &'static str }`
      - `impl NotSupportedAudioDispatcher { pub fn new(platform_name: &'static str) -> Self { Self { platform_name } } }`
      - `#[async_trait] impl crate::AudioDispatcher for NotSupportedAudioDispatcher { async fn send_voice_file(&self, _chat_id: &str, _path: &PathBuf, _thread_id: Option<&str>) -> anyhow::Result<()> { anyhow::bail!("audio dispatch not supported on {}", self.platform_name) } async fn send_audio_file(&self, _chat_id: &str, _path: &PathBuf, _thread_id: Option<&str>) -> anyhow::Result<()> { anyhow::bail!("audio dispatch not supported on {}", self.platform_name) } }`
      - File-level doc comment per PATTERNS.

    Edit `crates/ironhermes-tools/src/lib.rs`:
      - Add `pub mod not_supported_dispatcher;` near existing `pub mod send_audio_tool;`.
      - Add `pub use not_supported_dispatcher::NotSupportedAudioDispatcher;` next to existing `pub use send_audio_tool::AudioDispatcher;`.

    Do NOT add new dependencies. Do NOT touch `send_audio_tool.rs` or other files.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-tools --test not_supported_dispatcher 2>&amp;1 | tail -20; cargo test -p ironhermes-agent --test tts_per_turn_wiring 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/ironhermes-tools/src/not_supported_dispatcher.rs` exists.
    - `grep -c "pub struct NotSupportedAudioDispatcher" crates/ironhermes-tools/src/not_supported_dispatcher.rs` returns 1.
    - `grep -c "impl crate::AudioDispatcher for NotSupportedAudioDispatcher" crates/ironhermes-tools/src/not_supported_dispatcher.rs` returns 1.
    - `grep -c "anyhow::bail!" crates/ironhermes-tools/src/not_supported_dispatcher.rs` returns 2.
    - `grep -c "panic!\|\.unwrap()" crates/ironhermes-tools/src/not_supported_dispatcher.rs` returns 0.
    - `grep -c "pub mod not_supported_dispatcher" crates/ironhermes-tools/src/lib.rs` returns 1.
    - `grep -c "pub use not_supported_dispatcher::NotSupportedAudioDispatcher" crates/ironhermes-tools/src/lib.rs` returns 1.
    - `cargo build -p ironhermes-tools` exits 0.
    - `cargo test -p ironhermes-tools --test not_supported_dispatcher` exits 0; 4/4 GREEN.
    - `cargo test -p ironhermes-agent --test tts_per_turn_wiring` exits 0 for all source-grep + struct-construction tests. The behavioral test (`tts_wiring_registers_tools_in_run_turn`) may either GREEN (if Task 2 shipped a working test-feature AgentRuntime) or stay RED (documented in commit body, deferred to Plan 02 UAT-T audible result for end-to-end behavioral coverage).
    - `cargo clippy -p ironhermes-tools --tests -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>`NotSupportedAudioDispatcher` callable from any crate depending on `ironhermes-tools`. All Plan 01 Wave 0 source-grep + struct tests GREEN. ironhermes-agent + ironhermes-tools build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → AgentRuntime::run_turn | Caller-provided `request.tts_wiring` carries a `SessionKey` and an `Arc<dyn AudioDispatcher>`. Both constructed by in-process trusted handlers (gateway / ws / tui). |
| LLM → NotSupportedAudioDispatcher | LLM may invoke `send_audio` on Discord/Slack sessions and receive an Err with the `&'static str` platform name. No user-controlled data crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.17.7-01-01 | Tampering | `register_tts_tools` per-turn call | mitigate | Send + Sync trait + `bundle.registry.write().await` exclusive lock guarantees no cross-thread aliasing during registration. |
| T-36.17.7-01-02 | Repudiation | NotSupportedAudioDispatcher Err message | accept | `&'static str` platform name is compile-time; no user input interpolated; plain-prose `anyhow::bail!` message. Low-risk single-operator deployment. |
| T-36.17.7-01-03 | Denial of Service | Per-turn registration upsert | mitigate | Pitfall 1 mitigation: if `ToolRegistry::register` appends, unregister calls precede registration to prevent unbounded growth. Verdict in commit body. |
| T-36.17.7-01-SC | Tampering | cargo deps (no new packages) | mitigate | Zero new external deps. `async-trait`, `anyhow`, `ironhermes_core`, `ironhermes_tools` all workspace-existing. |
</threat_model>

<verify_plan>
1. `cargo build -p ironhermes-agent -p ironhermes-tools` exits 0.
2. `cargo test -p ironhermes-agent --test tts_per_turn_wiring` exits 0 for all source-grep + struct-construction tests (7 of 8 minimum; 8/8 if behavioral path was reachable).
3. `cargo test -p ironhermes-tools --test not_supported_dispatcher` exits 0 (4/4).
4. `cargo test -p ironhermes-agent --test invariants_36_17_5` exits 0 (no regression).
5. `cargo clippy -p ironhermes-agent -p ironhermes-tools --tests -- -D warnings` exits 0.
6. **BLOCKER 1 lock: `grep -c "session_key: None," crates/ironhermes-agent/src/agent_runtime.rs` returns 0.**
7. `grep -c "..Default::default()" crates/ironhermes-agent/src/agent_runtime.rs` returns ≥ 1.
8. `grep -c "#\[derive(Default)\]" crates/ironhermes-agent/src/app_runtime_factory.rs` returns ≥ 1.
9. `grep -c "pub tts_wiring: Option<TtsPerTurnWiring>" crates/ironhermes-agent/src/agent_runtime.rs` returns 1.
10. `grep -c "pub use not_supported_dispatcher::NotSupportedAudioDispatcher" crates/ironhermes-tools/src/lib.rs` returns 1.
</verify_plan>

<success_criteria>
- `AppRuntimeFactoryInput` derives `Default`; `from_config` uses `..Default::default()`.
- The literal `session_key: None,` no longer appears in `agent_runtime.rs` (BLOCKER 1 fix).
- `TurnRequest.tts_wiring` slot exists; `AgentRuntime::run_turn` registers TTS tools per-turn.
- `NotSupportedAudioDispatcher` ready for Plan 02 to mount on Discord/Slack (HIGH 6 prep).
- Behavioral test `tts_wiring_registers_tools_in_run_turn` covers the registration mutation (MEDIUM 8 fix); end-to-end audible coverage in Plan 02 UAT-T if behavioral path is fake-AgentRuntime-only.
- Zero new external dependencies.
- Zero regression in 36.17.5 invariants.
</success_criteria>

<output>
Create `.planning/phases/36.17.7-gateway-web-tts-runtime-wiring/36.17.7-01-SUMMARY.md` documenting:
- 7 files created/modified (incl. app_runtime_factory.rs Default derive + lib.rs re-export + 2 test files)
- BLOCKER 1 fix verification: post-rewrite source-grep counts for `session_key: None,` (= 0), `..Default::default()` (≥ 1), `#[derive(Default)]` (≥ 1).
- HIGH 5 frontmatter status: `requirements: []` (A5 co-owned with Plan 05 Task 6).
- MEDIUM 8 fix: behavioral test name + Path resolution (real AgentRuntime via test-support feature vs direct ToolRegistry call).
- Path A vs Path B for AppRuntimeFactoryInput Default derivation; per-field verdict.
- ToolRegistry::register upsert-vs-append verdict + whether `unregister` was added.
- Plan-01 must_haves achieved: all 5 truths; all 5 artifacts; both key_links.
- Cross-crate dependency status: TurnRequest::tts_wiring + TtsPerTurnWiring + NotSupportedAudioDispatcher + AppRuntimeFactoryInput: Default all available for Plans 02-05.
</output>
