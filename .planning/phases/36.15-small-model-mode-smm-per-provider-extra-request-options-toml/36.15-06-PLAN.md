---
phase: 36.15
plan: 06
type: execute
wave: 3
depends_on:
  - 36.15-04
files_modified:
  - crates/ironhermes-agent/tests/invariants_36_15.rs
autonomous: true
requirements:
  - PROV-14

must_haves:
  truths:
    - "`tests/invariants_36_15.rs` exists and statically asserts that `agent_loop.rs` no longer passes literal `None` as the final argument to `chat_completion` or `chat_completion_stream` — comment lines stripped before grep"
    - "The same file statically asserts `agent_loop.rs` passes `self.resolved_extras.clone()` as the final argument to both `chat_completion` and `chat_completion_stream` at least twice combined"
    - "The same file statically asserts that `any_client.rs` and `anthropic_client.rs` source text is unchanged at the D-06 out-of-scope markers (`build_openrouter_chat_request_full` signature still present, `_extra: Option<HashMap` still present in AnthropicClient)"
    - "The PROV-07 invariants from `tests/invariants_36_14.rs` still pass — running `cargo test -p ironhermes-agent --test invariants_36_14` reports the same passes as before Plan 04 landed"
    - "Full workspace builds clean: `cargo build --release --workspace` succeeds"
  artifacts:
    - path: "crates/ironhermes-agent/tests/invariants_36_15.rs"
      provides: "Static-grep regression gates for Phase 36.15: no None-pass for extra; resolved_extras.clone present; D-06 out-of-scope markers unchanged; floor preserved"
      contains: "PROV-11"
  key_links:
    - from: "invariants_36_15.rs"
      to: "agent_loop.rs source text"
      via: "include_str!(\"../src/agent_loop.rs\")"
      pattern: "include_str!"
    - from: "invariants_36_15.rs"
      to: "any_client.rs + anthropic_client.rs + client.rs source text"
      via: "include_str! invariants"
      pattern: "include_str!"
---

<objective>
Lock Phase 36.15's wiring decisions with static-grep invariants in a new `crates/ironhermes-agent/tests/invariants_36_15.rs` file — mirroring the proven `tests/invariants_36_14.rs` pattern (which itself mirrors `invariants_22_4.rs`, `invariants_27_1_4_1.rs`). The file uses `include_str!` to pull source files into the test binary and asserts source-text invariants at test-run time. No new dev-deps; pure string assertions.

The invariants:
1. **No literal `None` passes for `extra` in agent_loop.rs.** The substring `"chat_completion(messages, tools, None, None, None, None)"` must NOT exist in `agent_loop.rs` source after comment-line stripping. Same for `chat_completion_stream`. (PROV-11 wiring regression gate.)
2. **`self.resolved_extras.clone()` is present at the call sites.** Count of `"self.resolved_extras.clone()"` occurrences in `agent_loop.rs` ≥ 2 (one per method).
3. **D-06 out-of-scope markers preserved.** `any_client.rs` still contains the signature fragment `build_openrouter_chat_request_full`. `anthropic_client.rs` still contains the fragment `_extra: Option<HashMap` (the leading underscore marks intentional non-wiring per D-06).
4. **`client.rs:236` floor preserved.** `client.rs` still contains the substring `or_insert_with` AND `stream_options` AND `include_usage` near each other — the floor invariant.
5. **PROV-07 fallback chain from Phase 36.14 unchanged.** Running `cargo test -p ironhermes-agent --test invariants_36_14` (Phase 36.14's regression gate file) reports the same pass count as before this phase landed. This is enforced indirectly by running the existing test and checking exit code in the verify step — NOT by duplicating its assertions in this file.

Output: A new `crates/ironhermes-agent/tests/invariants_36_15.rs` with 4-5 static-grep tests. Plus a full-workspace release build to confirm no regression.

This plan is Wave 3 (depends on Plan 04 — needs Plan 04's source changes to be present so the grep gates can ASSERT them). Parallel-safe with Plan 05 (different file, no overlap in `files_modified`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@crates/ironhermes-agent/tests/invariants_36_14.rs
@crates/ironhermes-agent/src/agent_loop.rs
@crates/ironhermes-agent/src/any_client.rs
@crates/ironhermes-agent/src/anthropic_client.rs
@crates/ironhermes-agent/src/client.rs
@.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-CONTEXT.md
@.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-RESEARCH.md
@.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-PATTERNS.md
@.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-04-SUMMARY.md
@.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-05-SUMMARY.md

<interfaces>
<!-- Existing pattern from invariants_36_14.rs lines 1-63. -->

From crates/ironhermes-agent/tests/invariants_36_14.rs (analog file — entire shape):
- Top of file: `//! Phase 36.14 static-grep regression gates. //! Locks SSE error propagation wiring in client.rs and agent_loop.rs (PROV-07 extension). //! Follows include_str! pattern from invariants_22_4.rs / invariants_27_1_4_1.rs. No dev-deps.`
- Then: `const CLIENT_SOURCE: &str = include_str!("../src/client.rs");`
- Then: `const AGENT_LOOP_SOURCE: &str = include_str!("../src/agent_loop.rs");`
- Tests look like: `#[test] fn name() { assert!(CLIENT_SOURCE.contains("substring"), "message explaining the invariant"); }`
- Negative asserts use `assert!(!CLIENT_SOURCE.contains("forbidden_substring"), "...")`.

From .planning/phases/.../36.15-PATTERNS.md §invariants_36_15.rs:
- Use `include_str!("../src/agent_loop.rs")` and `include_str!("../src/agent_runtime.rs")` plus `include_str!("../src/any_client.rs")` and `include_str!("../src/anthropic_client.rs")` and `include_str!("../src/client.rs")`.
- Negative example: `assert!(!AGENT_LOOP_SOURCE.contains(".chat_completion(messages, tools, None, None, None, None)"), "PROV-11 (phase 36.15): agent_loop.rs call_llm must pass resolved_extras not None for the extra parameter. The literal None-pass was replaced in 36.15.")`.

From .planning/get-shit-done planner guidance (this prompt):
- "Grep gate hygiene: `grep -c` counts comments, so header prose can be self-invalidating. Use `grep -v '^#' | grep -c token`. Bare `== 0` gates on unfiltered files are forbidden."
- For Rust source, comments start with `//`. Strip them before substring counting if the substring might appear in a header doc-comment. The tests in this file are written in Rust, so comment-stripping is done with a one-liner: `let stripped: String = source.lines().filter(|l| !l.trim_start().starts_with("//")).collect::<Vec<_>>().join("\n");`. Use a small inline helper `fn strip_comments(s: &str) -> String { ... }` in the test file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/invariants_36_15.rs with static-grep gates covering PROV-11 wiring, D-06 untouched paths, and client.rs floor</name>
  <read_first>
    - crates/ironhermes-agent/tests/invariants_36_14.rs (the entire file — the exact analog pattern: include_str! consts at the top, #[test] functions below with assert!(SOURCE.contains(...)) and negative asserts)
    - crates/ironhermes-agent/src/agent_loop.rs (post-Plan-04 state — confirm the substrings the gates assert on actually exist or DON'T exist as expected)
    - crates/ironhermes-agent/src/any_client.rs (confirm `build_openrouter_chat_request_full` substring still present)
    - crates/ironhermes-agent/src/anthropic_client.rs (confirm `_extra: Option<HashMap` substring still present — leading underscore)
    - crates/ironhermes-agent/src/client.rs (confirm `or_insert_with` + `stream_options` + `include_usage` substrings still present)
    - .planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-PATTERNS.md §invariants_36_15 example
  </read_first>
  <behavior>
    All tests live in `crates/ironhermes-agent/tests/invariants_36_15.rs`. Each test is a synchronous `#[test]` (no tokio). Use a `strip_comments` helper to filter out Rust line comments before substring counting where the assertion uses a count (planner grep-gate-hygiene rule).

    - Test 1 `prov_11_agent_loop_no_longer_passes_literal_none_for_extra`: Assert that the stripped `agent_loop.rs` source does NOT contain the substring `"chat_completion(messages, tools, None, None, None, None)"` AND does NOT contain `"chat_completion_stream(messages, tools, None, None, None, None)"`. Message: "PROV-11 (phase 36.15): agent_loop.rs call_llm / call_llm_streaming must pass self.resolved_extras.clone() — not literal None — for the extra parameter. If this test fails, Plan 04's substitution at agent_loop.rs:1757 and :1790 has regressed."
    - Test 2 `prov_11_agent_loop_passes_resolved_extras_clone`: Count occurrences of `"self.resolved_extras.clone()"` in the stripped `agent_loop.rs` source. Assert count ≥ 2 (at least one per method).
    - Test 3 `d_06_openrouter_claude_path_still_present_and_untouched`: Assert stripped `any_client.rs` source contains the substring `"fn build_openrouter_chat_request_full"` — the D-06 out-of-scope OpenRouter Claude cache builder is still present and unwired (since wiring would add `resolved_extras` plumbing to this function; absence of any phase-36.15-specific markers near this function is the invariant). Message: "D-06: build_openrouter_chat_request_full is intentionally out-of-scope in phase 36.15. If this signature is missing, the function was removed or renamed — verify before continuing."
    - Test 4 `d_06_anthropic_client_underscore_extra_still_ignored`: Assert stripped `anthropic_client.rs` source contains `"_extra: Option<HashMap"` (leading underscore marks intentional non-wiring per D-06). Message: "D-06: AnthropicClient _extra is intentionally ignored in phase 36.15 (leading underscore signals 'unused'). If the underscore is gone, the extras flow has been accidentally wired into the native Anthropic path — revert and defer."
    - Test 5 `client_rs_stream_options_include_usage_floor_preserved`: Assert stripped `client.rs` source contains all three substrings: `"or_insert_with"`, `"stream_options"`, `"include_usage"`. Each must appear at least once. Message: "D-09 invariant: client.rs:236 `or_insert_with` floor for stream_options.include_usage MUST remain. If this test fails, Plan 04 unintentionally removed the floor — restore from git."
  </behavior>
  <files>crates/ironhermes-agent/tests/invariants_36_15.rs</files>
  <action>
    Create `crates/ironhermes-agent/tests/invariants_36_15.rs` from scratch following the exact shape of `crates/ironhermes-agent/tests/invariants_36_14.rs`.

    File header doc-comments: three slash-slash comment lines describing the file's purpose — "Phase 36.15 static-grep regression gates. Locks PROV-11 wiring (resolved_extras.clone replaces literal None at agent_loop.rs:1757 + :1790) and D-06 out-of-scope markers (any_client.rs::build_openrouter_chat_request_full untouched; anthropic_client.rs::_extra still underscore-ignored). Follows include_str! pattern from invariants_36_14.rs / invariants_27_1_4_1.rs. No dev-deps."

    After the doc comments, declare five `const` items via `include_str!`:
    - `const AGENT_LOOP_SOURCE: &str = include_str!("../src/agent_loop.rs");`
    - `const AGENT_RUNTIME_SOURCE: &str = include_str!("../src/agent_runtime.rs");`
    - `const ANY_CLIENT_SOURCE: &str = include_str!("../src/any_client.rs");`
    - `const ANTHROPIC_CLIENT_SOURCE: &str = include_str!("../src/anthropic_client.rs");`
    - `const CLIENT_SOURCE: &str = include_str!("../src/client.rs");`

    Add a private helper `fn strip_comments(s: &str) -> String` that returns a new owned String containing only the lines of `s` whose trimmed-leading content does NOT start with `//`. Use iterator chain: split lines, filter `!line.trim_start().starts_with("//")`, collect with newline separator.

    Implement the five `#[test]` functions from `<behavior>`. Each function:
    1. Stores the stripped source in a `let stripped = strip_comments(&SOURCE_CONST);` local at the top.
    2. Calls `stripped.contains(substring)` or `stripped.matches(substring).count() >= 2` as appropriate.
    3. Uses `assert!` with the descriptive message text from `<behavior>`.

    The substring patterns must match EXACTLY what `agent_loop.rs` looks like post-Plan-04. If Plan 04's executor chose a slightly different spelling (e.g. extra whitespace inside the parens, or breaking the call across lines), this plan's executor will need to adapt — read the actual post-Plan-04 `agent_loop.rs` and pick robust substrings. Recommended robust substring for negative assertion: just check `"None, None, None, None)"` does NOT appear at the END of a `chat_completion` or `chat_completion_stream` call — but the test needs that combined context to avoid false positives matching other six-arg calls. Solution: use a slightly longer substring that includes the surrounding context, but tolerant to formatter variance.

    Example robust positive-pattern: `stripped.matches("self.resolved_extras.clone()").count() >= 2`.

    Example robust negative-pattern: `assert!(!stripped.contains(", None, None, None, None)\n            .await") || !stripped.contains(", None, None, None, None)\n            .await")` — but this is brittle. Simpler robust shape: assert the IF-AND-ONLY-IF positive form (Test 2 already does this with count ≥ 2). Drop Test 1's negative phrasing OR keep it tolerant by accepting either the precise substring OR a 2+ count of the positive substring. Recommended: keep Test 1 specifically targeting the OLD literal pattern `chat_completion(messages, tools, None, None, None, None)` because if Plan 04 was done correctly, this substring is GONE — verify against the actual file.

    For Test 5 (floor preserved), use three independent `assert!(stripped.contains(...))` calls — one per substring. Substrings can co-exist on different lines; the test is not asserting they're adjacent, only that all three are present somewhere in `client.rs`.

    DO NOT add any other test. DO NOT include the PROV-07 invariants directly — those live in `invariants_36_14.rs` and the verify command for this plan asserts they still pass.

    DO NOT add any dev-deps to `Cargo.toml` — `include_str!` is built-in.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p ironhermes-agent --test invariants_36_15 2>&amp;1 | tail -15 | grep -qE "test result: ok\. [5-9] passed" &amp;&amp; cargo test -p ironhermes-agent --test invariants_36_14 2>&amp;1 | tail -10 | grep -qE "test result: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `crates/ironhermes-agent/tests/invariants_36_15.rs` exists.
    - `grep -c "^#\[test\]" crates/ironhermes-agent/tests/invariants_36_15.rs` returns 5.
    - `cargo test -p ironhermes-agent --test invariants_36_15` reports 5 passes.
    - `cargo test -p ironhermes-agent --test invariants_36_14` still passes (PROV-07 fallback chain regression-protected).
    - The file contains five `include_str!` consts mapping to the five source files under inspection.
    - Each test has a descriptive failure message that points back to the specific phase / decision (D-06, D-09, PROV-11, etc.).
  </acceptance_criteria>
  <done>invariants_36_15.rs ships with 5 static-grep regression gates; Phase 36.14 invariants still green.</done>
</task>

<task type="auto">
  <name>Task 2: Full-workspace release build + workspace test gate</name>
  <read_first>
    - .planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-VALIDATION.md §Sampling Rate (the phase-gate criteria)
  </read_first>
  <files>(no file edits — this task is a verification gate)</files>
  <action>
    Run the following in order (sequentially; each must succeed before the next):

    Command 1: `cd /Users/twilson/code/ironhermes && cargo build --release --workspace 2>&1 | tail -10` — confirm zero compile errors across the entire workspace. Should end with `Finished \`release\` profile` or similar success marker.

    Command 2: `cd /Users/twilson/code/ironhermes && cargo test -p ironhermes-core 2>&1 | tail -10` — confirm config_extras + extras_canary + schema tests all green.

    Command 3: `cd /Users/twilson/code/ironhermes && cargo test -p ironhermes-agent 2>&1 | tail -20` — confirm agent crate full test suite green: extra_request_options (7 tests from Plan 04 + Plan 05), invariants_36_15 (5 tests), invariants_36_14 (existing, unchanged), all other existing tests.

    Command 4: `cd /Users/twilson/code/ironhermes && cargo clippy --workspace --all-targets 2>&1 | tail -20` — confirm no new clippy warnings. Existing warnings are fine; the diff should not introduce new ones (especially `dead_code` on the new `resolved_extras` field — it's used in two methods + builder + tests, so it shouldn't trip dead_code).

    If any command fails, surface the failure verbatim in the SUMMARY (NOT silenced; NOT retried via sleep loop). Diagnose the root cause and either patch in-place (small fixes) or escalate to the parent planner (substantive failures require redesign).
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build --release --workspace 2>&amp;1 | tail -3 | grep -qE "Finished" &amp;&amp; cargo test -p ironhermes-core 2>&amp;1 | tail -3 | grep -qE "test result: ok" &amp;&amp; cargo test -p ironhermes-agent 2>&amp;1 | tail -3 | grep -qE "test result: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --release --workspace` finishes successfully (zero errors).
    - `cargo test -p ironhermes-core` all green.
    - `cargo test -p ironhermes-agent` all green — explicitly includes the 7 `extra_request_options` tests and 5 `invariants_36_15` tests and unchanged `invariants_36_14` tests.
    - `cargo clippy --workspace --all-targets` introduces no new warnings vs pre-phase baseline. Specifically, no `dead_code` warning on `resolved_extras`.
  </acceptance_criteria>
  <done>Full workspace builds release-clean; all tests pass across both crates; clippy unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invariants_36_15.rs → source text via include_str! | Tests read raw source at compile time; no runtime trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.15-14 | Tampering | Future edits to agent_loop.rs accidentally remove `resolved_extras.clone()` | mitigate | Test 2 in this plan asserts count ≥ 2; will fail loudly if a refactor removes it. |
| T-36.15-15 | Tampering | Future edits accidentally wire extras into any_client.rs::build_openrouter_chat_request_full (D-06 violation) | accept | Static-grep cannot detect the addition of new code; Test 3 only confirms the function STILL EXISTS. Active wiring would need to be caught in code review. |
| T-36.15-SC | Tampering | npm/pip/cargo install of new packages | accept | Plan 06 installs ZERO new packages — `include_str!` is built-in. |
</threat_model>

<verification>
- `cargo test -p ironhermes-agent --test invariants_36_15 2>&1 | tail -10` — 5 tests pass.
- `cargo test -p ironhermes-agent --test invariants_36_14 2>&1 | tail -10` — Phase 36.14 invariants still green.
- `cargo build --release --workspace 2>&1 | tail -5` — release build finishes successfully.
- `cargo test -p ironhermes-core 2>&1 | tail -5` — core crate all green.
- `cargo test -p ironhermes-agent 2>&1 | tail -5` — agent crate all green.
</verification>

<success_criteria>
1. `tests/invariants_36_15.rs` exists with 5 static-grep tests.
2. All 5 tests pass.
3. `invariants_36_14.rs` tests still pass (PROV-07 regression-protected).
4. `cargo build --release --workspace` is clean.
5. `cargo test` is clean across both crates.
6. `cargo clippy` introduces no new warnings.
</success_criteria>

<output>
Create `.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-06-SUMMARY.md` when done, documenting:
- The 5 invariants and their exact failure messages
- `cargo build --release --workspace` final output line (e.g. "Finished `release` profile [optimized] target(s) in 4m 12s")
- Full-workspace test pass counts: ironhermes-core: N1, ironhermes-agent: N2 (where N1, N2 ≥ pre-phase baselines)
- Clippy delta: zero new warnings (or list any that appeared with rationale)
- Phase 36.15 is now ready for `/gsd:verify-work`
</output>
