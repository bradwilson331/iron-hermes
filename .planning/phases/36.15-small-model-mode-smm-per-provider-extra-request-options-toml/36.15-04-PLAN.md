---
phase: 36.15
plan: 04
type: execute
wave: 2
depends_on:
  - 36.15-02
files_modified:
  - crates/ironhermes-agent/src/agent_loop.rs
  - crates/ironhermes-agent/src/agent_runtime.rs
  - crates/ironhermes-agent/tests/extra_request_options.rs
autonomous: true
requirements:
  - PROV-11

must_haves:
  truths:
    - "AgentLoop has a new field `resolved_extras: Option<HashMap<String, serde_json::Value>>` and a builder method `with_resolved_extras` mirroring `with_provider_name`"
    - "AgentLoop::call_llm (agent_loop.rs:1757 area) and AgentLoop::call_llm_streaming (agent_loop.rs:1790 area) pass `self.resolved_extras.clone()` to chat_completion / chat_completion_stream instead of literal `None`"
    - "AgentRuntime::run_turn resolves extras per-turn via `ironhermes_core::config_extras::resolve_extras(&self.config.providers, provider_name, model_name)` and wires the result into AgentLoop via `with_resolved_extras`"
    - "Wiremock-backed integration tests confirm Ollama `num_ctx`, vLLM `top_k`, and OpenRouter `provider.order` appear in the actual POST request body to /chat/completions"
    - "build_openrouter_chat_request_full (any_client.rs:546) and AnthropicClient (anthropic_client.rs:877) remain UNCHANGED — D-06 out-of-scope paths are untouched"
    - "client.rs:236 `or_insert_with` floor for `stream_options.include_usage` is byte-identical to pre-task"
  artifacts:
    - path: "crates/ironhermes-agent/src/agent_loop.rs"
      provides: "AgentLoop with resolved_extras field + with_resolved_extras builder; call_llm + call_llm_streaming now pass self.resolved_extras.clone() instead of None"
      contains: "with_resolved_extras"
    - path: "crates/ironhermes-agent/src/agent_runtime.rs"
      provides: "Per-turn extras resolution in run_turn"
      contains: "config_extras::resolve_extras"
    - path: "crates/ironhermes-agent/tests/extra_request_options.rs"
      provides: "Wiremock integration tests for Ollama num_ctx, vLLM top_k, OpenRouter provider.order on the wire"
      contains: "ollama_num_ctx_appears_in_request_body"
  key_links:
    - from: "AgentRuntime::run_turn"
      to: "config_extras::resolve_extras"
      via: "function call passing &self.config.providers, provider_name, model_name"
      pattern: "config_extras::resolve_extras"
    - from: "AgentLoop::call_llm"
      to: "LlmClient::chat_completion"
      via: "self.resolved_extras.clone() as the extra parameter (no longer None)"
      pattern: "self\\.resolved_extras\\.clone"
---

<objective>
Wire the typed config layer (Plans 01-03) into the actual LLM call path per CONTEXT.md D-05/D-10. Add `resolved_extras` as an `Option<HashMap<String, serde_json::Value>>` field on `AgentLoop` via a `with_resolved_extras` builder (mirroring the existing `with_provider_name` pattern per PATTERNS.md). Modify `call_llm` (`agent_loop.rs:1757`) and `call_llm_streaming` (`agent_loop.rs:1790`) to pass `self.resolved_extras.clone()` to `LlmClient::chat_completion` / `chat_completion_stream` instead of the literal `None`. In `AgentRuntime::run_turn`, resolve extras per-turn by calling `ironhermes_core::config_extras::resolve_extras` against `(self.config.providers, self.resolver.main_provider(), self.resolver.resolve_for_main().default_model)` and wire the result via `.with_resolved_extras(...)` into the AgentLoop builder chain.

Ship three wiremock-backed integration tests in a NEW `crates/ironhermes-agent/tests/extra_request_options.rs` proving the three canonical knobs land on the wire: Ollama `num_ctx = 32768`; vLLM `top_k = 40`; OpenRouter `provider.order = ["anthropic", "openai"]`. These tests use `LlmClient::chat_completion(..., Some(extra))` direct entry point — they verify the LAST hop (HashMap-on-AgentLoop to wire). D-09 caller-wins, D-10 mid-session switch, and reserved-key collision tests ship in Plan 05.

Out-of-scope (D-06) — DO NOT MODIFY: `any_client.rs` (OpenRouter Claude cache builder), `anthropic_client.rs` (`_extra` ignored), `client.rs:236` floor. The signature at `client.rs:144`/`:208` already accepts `extra` — no edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@crates/ironhermes-agent/src/agent_loop.rs
@crates/ironhermes-agent/src/agent_runtime.rs
@crates/ironhermes-agent/src/client.rs
@crates/ironhermes-core/src/config_extras.rs
@crates/ironhermes-agent/tests/streaming_usage_capture.rs
@.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-CONTEXT.md
@.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-RESEARCH.md
@.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-PATTERNS.md

<interfaces>
<!-- Existing shapes the executor needs. From PATTERNS.md + RESEARCH.md. -->

From crates/ironhermes-agent/src/agent_loop.rs (CURRENT):
- `pub struct AgentLoop` declared around line 136; field cluster includes `client: Arc<LlmClient>`, `provider_name: String`, plus other builder-set fields.
- Existing builder method `pub fn with_provider_name(mut self, provider: impl Into<String>) -> Self` at lines ~541-549. New `with_resolved_extras` mirrors this.
- `async fn call_llm(&self, messages: &[ChatMessage], tools: Option<&[ToolSchema]>) -> Result<...>` at line ~1757. Body currently calls `self.client.chat_completion(messages, tools, None, None, None, None)`. The final `None` is the `extra` parameter.
- `async fn call_llm_streaming(&self, ...) -> Result<...>` at line ~1790. Body currently calls `self.client.chat_completion_stream(messages, tools, None, None, None, None)`.
- Internal call sites of `call_llm`/`call_llm_streaming` exist inside `run()` at lines 1563, 1584, 1587. The builder-field approach avoids touching them — Pitfall 6 mitigation.

From crates/ironhermes-agent/src/agent_runtime.rs (CURRENT):
- `pub struct AgentRuntime` carries `config: Arc<Config>` and `resolver: Arc<ProviderResolver>` (RESEARCH §Sources lines 117-143).
- `run_turn` at line ~249-504 constructs an `AgentLoop` via a builder chain ending around line 340.
- Existing accessor patterns inside run_turn (PATTERNS.md §Resolver accessor chain):
  - `self.resolver.main_provider()` returns the provider name (a `&str` or `String` — the providers HashMap key).
  - `self.resolver.resolve_for_main().default_model` returns the wire model name (the same value LlmClient uses when None is passed for the model parameter).
  - `self.resolver.resolve_for_main().context_length()` is already called at line ~253 — extras resolution goes right after this.

From crates/ironhermes-agent/src/client.rs (NO EDITS — referenced only):
- `pub async fn chat_completion(&self, messages, tools, model: Option<&str>, json_schema: Option<&JsonSchema>, temperature: Option<f32>, extra: Option<HashMap<String, serde_json::Value>>) -> Result<ChatResponse>` at line 144.
- `pub async fn chat_completion_stream(...)` at line 208 — same signature.
- The `or_insert_with` floor at line 235-238 inside `chat_completion_stream` PRESERVES `stream_options.include_usage: true` as the floor — DO NOT modify.

From crates/ironhermes-agent/tests/streaming_usage_capture.rs (analog for wiremock harness, lines 19-119):
- Imports `ironhermes_agent::client::{LlmClient, StreamEvent}`, `ironhermes_core::ChatMessage`, `wiremock::matchers::any`, `wiremock::{Mock, MockServer, ResponseTemplate}`.
- Pattern: `let server = MockServer::start().await;` then `Mock::given(any()).respond_with(ResponseTemplate::new(200).set_body_raw(body, "text/event-stream")).mount(&server).await;` then `let client = LlmClient::new(server.uri(), "test-key", "model-name");` then call `client.chat_completion_stream(...)`.
- For wire-body assertion: use `Mock::given(any()).mount_as_scoped(&server).await` to keep the mock handle, then after the request call inspect `server.received_requests().await.expect(...)` to get the captured request body bytes. Parse with `serde_json::from_slice::<serde_json::Value>(&req.body)` and assert field presence.

LlmClient::new signature confirmed (streaming_usage_capture.rs:110-ish):
- `LlmClient::new(base_url: impl Into<String>, api_key: impl Into<String>, model: impl Into<String>) -> Self`.

From .planning/phases/.../36.15-CONTEXT.md §decisions D-06:
- DO NOT modify `crates/ironhermes-agent/src/any_client.rs` line 546 (`build_openrouter_chat_request_full`).
- DO NOT modify `crates/ironhermes-agent/src/anthropic_client.rs` line 877 (`_extra` is intentionally ignored).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add resolved_extras field + with_resolved_extras builder to AgentLoop and substitute self.resolved_extras.clone() in call_llm + call_llm_streaming</name>
  <read_first>
    - crates/ironhermes-agent/src/agent_loop.rs (the entire `pub struct AgentLoop` declaration block at ~line 136, the `impl AgentLoop` block containing `with_provider_name` at lines 541-549, the `call_llm` method at line 1757, and the `call_llm_streaming` method at line 1790)
    - crates/ironhermes-agent/src/agent_runtime.rs (the AgentLoop construction site inside run_turn at line ~340 area — needed because adding a struct field requires updating the struct-literal initializer there too)
    - .planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-PATTERNS.md §agent_loop.rs MOD section (the exact substitution pattern and builder method shape)
  </read_first>
  <behavior>
    - Test 1 `agent_loop_default_resolved_extras_is_none`: When AgentLoop is constructed without calling `.with_resolved_extras(...)`, the `resolved_extras` field equals `None`. This is the backward-compat default.
    - Test 2 `agent_loop_with_resolved_extras_stores_the_map`: After `.with_resolved_extras(Some(map))` where `map` contains `{"num_ctx": json!(8192)}`, the AgentLoop's `resolved_extras` field equals that map. Verified either via a `#[cfg(test)] pub(crate) fn resolved_extras_for_test(&self) -> Option<&HashMap<String, serde_json::Value>>` accessor added in this task, or via direct field read if tests live in the same module.
    - Test 3 (regression, asserted statically by grep): `call_llm` and `call_llm_streaming` method bodies no longer contain the literal substring `"None, None, None, None)"` for the `chat_completion`/`chat_completion_stream` calls — the final `None` is replaced.
  </behavior>
  <files>crates/ironhermes-agent/src/agent_loop.rs, crates/ironhermes-agent/src/agent_runtime.rs</files>
  <action>
    Edit `crates/ironhermes-agent/src/agent_loop.rs` first:

    Step 1 — Add the field declaration to `pub struct AgentLoop` (~line 136 area). Insert immediately after the `provider_name: String` field. The type is `Option<std::collections::HashMap<String, serde_json::Value>>`. Use the fully-qualified path or the short form depending on whether `HashMap` is already imported via `use std::collections::HashMap;` at the top of the file — match existing style. Add the field name `resolved_extras` followed by its type.

    Step 2 — Update the struct-literal initializer wherever `AgentLoop { ... }` is constructed. From RESEARCH there is a primary construction site inside `agent_runtime.rs` run_turn; if `agent_loop.rs` also has a `new()`/`Default` constructor, update that too. Add `resolved_extras: None,` to every struct-literal initializer of `AgentLoop`. Use `grep -n "AgentLoop {" crates/ironhermes-agent/src/` to find all sites and update each. Default value is `None` (no extras when not wired).

    Step 3 — Add the builder method to the existing `impl AgentLoop` block right after `with_provider_name` (line ~549). Signature: `pub fn with_resolved_extras(mut self, extras: Option<std::collections::HashMap<String, serde_json::Value>>) -> Self`. Body: assigns `self.resolved_extras = extras;` then returns `self`. Formatting matches `with_provider_name` exactly (three lines plus brace).

    Step 4 — Modify the `call_llm` body at line ~1757. Locate the existing call to `self.client.chat_completion(messages, tools, None, None, None, None)` and replace ONLY the final `None` with the expression `self.resolved_extras.clone()`. The first three `None`s (model, json_schema, temperature) stay unchanged. Use Edit tool with a precise unique substring match.

    Step 5 — Modify the `call_llm_streaming` body at line ~1790. Same substitution applied to the `chat_completion_stream` call: only the final `None` becomes `self.resolved_extras.clone()`.

    Step 6 — Add two unit tests to the existing `#[cfg(test)] mod tests` block at the bottom of `agent_loop.rs` (or create one if absent). Tests are `agent_loop_default_resolved_extras_is_none` and `agent_loop_with_resolved_extras_stores_the_map`. If AgentLoop construction in tests requires non-trivial setup, add a `#[cfg(test)] pub(crate) fn for_test() -> AgentLoop` helper that builds a minimal AgentLoop with stub LlmClient — reuse whatever test helpers already exist in this file's test module (search for existing `AgentLoop::` constructions in tests).

    Then update `crates/ironhermes-agent/src/agent_runtime.rs` IF the run_turn site uses a struct-literal `AgentLoop { ... }` (Step 2). If it uses a builder chain via a `new()` constructor, no edit needed here — the field default cascades. Use grep to confirm.

    DO NOT modify the internal call sites of `call_llm` / `call_llm_streaming` inside `run()` (lines 1563, 1584, 1587). DO NOT modify any file under `any_client.rs`, `anthropic_client.rs`, or `client.rs`. DO NOT change the visibility of existing fields.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-agent 2>&amp;1 | tail -5 &amp;&amp; ! cargo build -p ironhermes-agent 2>&amp;1 | grep -qE "^error" &amp;&amp; grep -c "self\.resolved_extras\.clone()" crates/ironhermes-agent/src/agent_loop.rs | grep -qE "^[2-9]$" &amp;&amp; grep -c "self\.client\.chat_completion(messages, tools, None, None, None, None)" crates/ironhermes-agent/src/agent_loop.rs | grep -q "^0$" &amp;&amp; grep -c "self\.client\.chat_completion_stream(messages, tools, None, None, None, None)" crates/ironhermes-agent/src/agent_loop.rs | grep -q "^0$" &amp;&amp; cargo test -p ironhermes-agent --lib agent_loop 2>&amp;1 | tail -10 | grep -qE "test result: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "resolved_extras: Option<.*HashMap<String, serde_json::Value>>" crates/ironhermes-agent/src/agent_loop.rs` returns success (field declared).
    - `grep -q "pub fn with_resolved_extras" crates/ironhermes-agent/src/agent_loop.rs` returns success (builder method present).
    - `grep -c "self\.resolved_extras\.clone()" crates/ironhermes-agent/src/agent_loop.rs` returns 2 or more (used in call_llm and call_llm_streaming, possibly more if helpers exist).
    - `grep -c "self\.client\.chat_completion(messages, tools, None, None, None, None)" crates/ironhermes-agent/src/agent_loop.rs` returns 0 (old literal-None gone).
    - `grep -c "self\.client\.chat_completion_stream(messages, tools, None, None, None, None)" crates/ironhermes-agent/src/agent_loop.rs` returns 0.
    - `cargo build -p ironhermes-agent` succeeds with no new warnings about unused fields.
    - `cargo test -p ironhermes-agent --lib agent_loop` pass count is ≥ pre-task baseline and includes both new unit tests.
    - `git diff crates/ironhermes-agent/src/any_client.rs crates/ironhermes-agent/src/anthropic_client.rs crates/ironhermes-agent/src/client.rs` shows zero changes (D-06 + floor preservation).
  </acceptance_criteria>
  <done>AgentLoop carries resolved_extras as a builder-set field; both call_llm and call_llm_streaming pass it instead of literal None; OpenRouter Claude + AnthropicClient + client.rs floor untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire AgentRuntime::run_turn to resolve extras per-turn and call .with_resolved_extras on the AgentLoop builder</name>
  <read_first>
    - crates/ironhermes-agent/src/agent_runtime.rs (the entire `run_turn` method, lines 249-504; pay attention to AgentLoop construction at line ~340 and resolver accessor patterns at 253, 403-404, 469-471)
    - crates/ironhermes-core/src/config_extras.rs (the `resolve_extras` function signature created by Plan 02)
    - crates/ironhermes-core/src/lib.rs (to confirm `config_extras` is reachable as `ironhermes_core::config_extras::resolve_extras`)
    - .planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-PATTERNS.md §agent_runtime.rs MOD section (exact insertion block and builder-chain extension)
  </read_first>
  <behavior>
    - Test `agent_runtime_resolved_extras_for_test_turn_returns_provider_extras`: A test-only `pub(crate)` helper `resolved_extras_for_test_turn(&self) -> Option<HashMap<String, serde_json::Value>>` (added in this task) — when called against an AgentRuntime whose Config has `providers.test_provider.extra_request_options` with Ollama `num_ctx = 4096` — returns `Some(map)` where `map["num_ctx"] == json!(4096)`.
    - If extracting this helper proves structurally impractical (AgentRuntime construction in tests requires heavy mocks), the assertion is deferred to Task 3 wiremock integration coverage — document the deferral in the SUMMARY.
  </behavior>
  <files>crates/ironhermes-agent/src/agent_runtime.rs</files>
  <action>
    Edit `crates/ironhermes-agent/src/agent_runtime.rs`:

    Step 1 — Inside `run_turn` at line ~249-504, insert a new extras-resolution block IMMEDIATELY AFTER the existing call to `self.resolver.resolve_for_main().context_length()` (line ~253) and BEFORE the first AgentLoop construction. The block declares three locals: a provider name (clone of `self.resolver.main_provider()` as owned String), a model name (clone of `self.resolver.resolve_for_main().default_model` as owned String), and a `resolved_extras_map` from calling `ironhermes_core::config_extras::resolve_extras(&self.config.providers, &provider_name, &model_name)`. Then bind `resolved_extras: Option<HashMap<String, serde_json::Value>>` to `Some(map)` if non-empty, `None` otherwise — to avoid wasting an allocation pass through chat_completion when extras are empty. Use ordinary local-binding syntax (no fenced code in this prose).

    Step 2 — Find the AgentLoop construction site at line ~340 area. If it uses a builder chain (e.g. `AgentLoop::new(...).with_X(...).with_Y(...)`), append `.with_resolved_extras(resolved_extras)` to the chain — place it after `.with_provider_name(...)` for adjacency convention. If it uses a struct literal `AgentLoop { ... }`, add `resolved_extras,` to the field list (Plan 04 Task 1 already requires updating struct-literal initializers, so the field name is already in scope).

    Step 3 — Add the test-only helper `pub(crate) fn resolved_extras_for_test_turn(&self) -> Option<std::collections::HashMap<String, serde_json::Value>>` to the impl block of AgentRuntime, guarded by `#[cfg(test)]`. Body re-derives provider/model and calls `ironhermes_core::config_extras::resolve_extras`, returning the same `Option<HashMap>` shape used in run_turn. This lets unit tests assert the resolution result without running an async LLM turn.

    Step 4 — Add the unit test from `<behavior>` to a `#[cfg(test)] mod tests` block at the bottom of `agent_runtime.rs` (or append if one exists). The test constructs an AgentRuntime with a minimal Config containing the test_provider entry, calls the helper, and asserts the map contains `num_ctx == json!(4096)`. If a test constructor doesn't exist, use a `#[cfg(test)] pub(crate) fn for_test_with_config(config: Config, resolver: ProviderResolver) -> Self` helper or any existing test-construction path in this file.

    DO NOT cache resolved_extras across turns — D-10 mandates per-turn resolution. DO NOT modify any run_turn body section outside the new block + the single builder-chain addition. DO NOT modify any other method.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-agent 2>&amp;1 | tail -5 &amp;&amp; ! cargo build -p ironhermes-agent 2>&amp;1 | grep -qE "^error" &amp;&amp; grep -q "ironhermes_core::config_extras::resolve_extras" crates/ironhermes-agent/src/agent_runtime.rs &amp;&amp; grep -q "with_resolved_extras" crates/ironhermes-agent/src/agent_runtime.rs &amp;&amp; cargo test -p ironhermes-agent --lib agent_runtime 2>&amp;1 | tail -10 | grep -qE "test result: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "ironhermes_core::config_extras::resolve_extras" crates/ironhermes-agent/src/agent_runtime.rs` returns success.
    - `grep -q "with_resolved_extras" crates/ironhermes-agent/src/agent_runtime.rs` returns success.
    - `cargo build -p ironhermes-agent` is clean.
    - `cargo test -p ironhermes-agent --lib agent_runtime` pass count ≥ pre-task baseline; the new helper test passes (or is documented as deferred to Task 3).
    - No edits outside `run_turn` body, the impl block accessor addition, and the test module.
  </acceptance_criteria>
  <done>AgentRuntime resolves extras per-turn via config_extras::resolve_extras and wires them into AgentLoop via with_resolved_extras; helper test confirms resolution path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/extra_request_options.rs with three wiremock wire-body integration tests for Ollama / vLLM / OpenRouter</name>
  <read_first>
    - crates/ironhermes-agent/tests/streaming_usage_capture.rs (the entire file — analog wiremock harness, especially the MockServer/LlmClient setup and the `mount_as_scoped` + `received_requests` extraction pattern)
    - crates/ironhermes-agent/src/client.rs (the `chat_completion` signature at line 144 — confirm parameter order: `messages, tools, model, json_schema, temperature, extra`)
    - .planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-PATTERNS.md §tests/extra_request_options.rs (wiremock harness pattern + `minimal_chat_response` helper)
    - .planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-RESEARCH.md §Code Examples (wire-body assertion pattern)
  </read_first>
  <behavior>
    - Test 1 `ollama_num_ctx_appears_in_request_body`: An `LlmClient::new(server.uri(), "test-key", "llama3.1:8b").chat_completion(&messages, None, None, None, None, Some(extra_with_num_ctx_32768))` call results in a POST to `/chat/completions` whose JSON body contains `num_ctx: 32768` as a top-level field (flattened from `ChatRequest.extra`).
    - Test 2 `vllm_top_k_appears_in_request_body`: Same shape with `model = "meta-llama/Llama-3-8B"`, `extra = {"top_k": json!(40)}` — POST body contains `top_k: 40`.
    - Test 3 `openrouter_provider_order_nested_in_request_body`: `model = "anthropic/claude-3.5-sonnet"` (or any non-Claude OR route per D-07), `extra = {"provider": json!({"order": ["anthropic", "openai"]})}` — POST body contains `provider: { order: ["anthropic", "openai"] }` as a nested object.
    - All three tests confirm the wire-body shape via `server.received_requests().await.expect(...)` after the call returns — parse the body bytes as `serde_json::Value` and assert with `pointer` or direct key access.
  </behavior>
  <files>crates/ironhermes-agent/tests/extra_request_options.rs</files>
  <action>
    Create `crates/ironhermes-agent/tests/extra_request_options.rs` from scratch. The file is a standalone Rust integration test crate — it lives under `crates/ironhermes-agent/tests/` and is picked up automatically by cargo without manifest edits (Cargo.toml dev-deps `wiremock`, `tokio`, `serde_json` are already in scope per RESEARCH §Standard Stack).

    Imports needed at the top: `use ironhermes_agent::client::LlmClient;`, `use ironhermes_core::ChatMessage;`, `use std::collections::HashMap;`, `use wiremock::matchers::any;`, `use wiremock::{Mock, MockServer, ResponseTemplate};`. Match the import shape from `streaming_usage_capture.rs` lines 19-22.

    Add a private helper function `fn minimal_chat_response() -> serde_json::Value` returning a minimal valid ChatResponse JSON: object with `id="test"`, `object="chat.completion"`, `created=1`, `model="test"`, `choices=[{index:0, message:{role:"assistant", content:"ok"}, finish_reason:"stop"}]`, `usage={prompt_tokens:5, completion_tokens:2, total_tokens:7}`. Use `serde_json::json!({...})` syntax. This is the stub response so the call returns Ok and we can inspect the captured request.

    Add another private helper `async fn assert_request_body_contains(server: &MockServer, key_path: &str, expected: serde_json::Value)`. Body: call `server.received_requests().await.expect("requests captured")`, take the last (or only) request, deserialize `req.body` as `serde_json::Value`, use `serde_json::Value::pointer(key_path)` to walk a JSON Pointer (e.g. `/num_ctx`, `/provider/order`), and assert equality with `expected`. Panic with a descriptive message on mismatch.

    Implement the three `#[tokio::test]` functions per `<behavior>`. Each test:
    1. Starts `MockServer::start().await`.
    2. Mounts a `Mock::given(any()).respond_with(ResponseTemplate::new(200).set_body_json(minimal_chat_response())).mount(&server).await`.
    3. Constructs `LlmClient::new(server.uri(), "test-key", model_name)`.
    4. Builds `let messages = vec![ChatMessage::user("hi")];`.
    5. Builds the `extra: HashMap<String, serde_json::Value>` with the test's specific key(s).
    6. Calls `client.chat_completion(&messages, None, None, None, None, Some(extra)).await.expect("request should succeed");`.
    7. Calls the helper to assert the wire body contains the expected key/value pair at the right JSON Pointer.

    For Test 3 (OpenRouter provider.order), the JSON Pointer is `/provider/order` and the expected value is `serde_json::json!(["anthropic", "openai"])`.

    DO NOT use `wiremock::matchers::body_json_includes` or any matcher whose existence isn't confirmed by `streaming_usage_capture.rs` — version drift would silently fail. The `received_requests().await` + JSON parse pattern is verified-available (RESEARCH §Sources).

    DO NOT add any other test in this file — Plan 05 appends D-09 / D-10 / collision tests to this same file in Wave 3.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p ironhermes-agent --test extra_request_options 2>&amp;1 | tail -20 | grep -qE "test result: ok\. [3-9] passed"</automated>
  </verify>
  <acceptance_criteria>
    - `crates/ironhermes-agent/tests/extra_request_options.rs` exists.
    - `grep -c "^#\[tokio::test\]" crates/ironhermes-agent/tests/extra_request_options.rs` returns 3 (three tests).
    - `grep -q "ollama_num_ctx_appears_in_request_body" crates/ironhermes-agent/tests/extra_request_options.rs` succeeds.
    - `grep -q "vllm_top_k_appears_in_request_body" crates/ironhermes-agent/tests/extra_request_options.rs` succeeds.
    - `grep -q "openrouter_provider_order_nested_in_request_body" crates/ironhermes-agent/tests/extra_request_options.rs` succeeds.
    - `cargo test -p ironhermes-agent --test extra_request_options` reports `test result: ok. 3 passed` (or higher if helpers contribute).
    - The Plan 03 schema tests and Plan 02 config_extras tests still pass — full-workspace regression unaffected.
  </acceptance_criteria>
  <done>Three wiremock integration tests prove Ollama num_ctx, vLLM top_k, and OpenRouter provider.order land in the actual POST body to /chat/completions; the file is set up for Plan 05 to append D-09/D-10/collision tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AgentRuntime → AgentLoop | resolved_extras flows from config through resolution helper into the LLM call boundary. |
| LlmClient → HTTP POST body | `#[serde(flatten)]` on `ChatRequest.extra` puts each key at the top level of the request body. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.15-09 | Tampering | An extras key named `model`, `messages`, `temperature`, etc. could shadow the named ChatRequest field via serde flatten | mitigate | Reserved-key collision test ships in Plan 05. RESEARCH §Security Domain confirms named fields win over flattened extras in serde serialization order (ASSUMED — verified by the Plan 05 collision test). |
| T-36.15-10 | Tampering | Extras leak through to D-06 out-of-scope paths (`build_openrouter_chat_request_full`, AnthropicClient) | mitigate | This plan touches NEITHER `any_client.rs` NOR `anthropic_client.rs`. Acceptance criteria assert `git diff` shows zero changes to those files. |
| T-36.15-11 | Information Disclosure | Test keys leak the literal `test-key` API string into logs | accept | `test-key` is a hardcoded test sentinel, not a real credential. |
| T-36.15-SC | Tampering | npm/pip/cargo install of new packages | accept | Plan 04 installs ZERO new packages (RESEARCH §Standard Stack confirmed). |
</threat_model>

<verification>
- `cargo build -p ironhermes-agent 2>&1 | tail -5` — clean build, no new warnings.
- `cargo test -p ironhermes-agent --lib 2>&1 | tail -10` — pass count ≥ pre-task baseline.
- `cargo test -p ironhermes-agent --test extra_request_options 2>&1 | tail -10` — 3 tests pass.
- `git diff --stat crates/ironhermes-agent/src/any_client.rs crates/ironhermes-agent/src/anthropic_client.rs crates/ironhermes-agent/src/client.rs` shows zero changes.
- `grep -c "self\.resolved_extras\.clone()" crates/ironhermes-agent/src/agent_loop.rs` returns ≥ 2.
</verification>

<success_criteria>
1. AgentLoop has `resolved_extras` field + `with_resolved_extras` builder.
2. call_llm and call_llm_streaming pass `self.resolved_extras.clone()` instead of literal `None`.
3. AgentRuntime::run_turn calls `ironhermes_core::config_extras::resolve_extras` per-turn and wires the result via `.with_resolved_extras(...)`.
4. New `tests/extra_request_options.rs` ships three wiremock integration tests proving the three canonical knobs land on the wire.
5. D-06 out-of-scope files (`any_client.rs`, `anthropic_client.rs`, `client.rs`) are byte-identical.
6. `client.rs:236` `or_insert_with` floor is preserved.
</success_criteria>

<output>
Create `.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-04-SUMMARY.md` when done, documenting:
- AgentLoop wiring approach taken (builder-field per RESEARCH Open Question 2 recommendation; no signature changes to call_llm/call_llm_streaming internal sites).
- Result of `grep -c "AgentLoop {" crates/ironhermes-agent/src/` (struct-literal sites updated count).
- Test counts: AgentLoop unit (+2), AgentRuntime unit (+1 or deferred), wiremock integration (+3).
- Confirmation that D-06 files (`any_client.rs`, `anthropic_client.rs`) and `client.rs:236` floor are byte-identical.
- Hand-off note for Plan 05: `tests/extra_request_options.rs` is open for D-09 caller-wins, D-10 mid-session-switch, and reserved-key-collision tests.
</output>
