---
phase: 36.15
plan: "06"
subsystem: ironhermes-agent / test invariants
tags: [static-grep, regression-gates, prov-11, d-06, d-09]
dependency_graph:
  requires: [36.15-04]
  provides: [PROV-11 regression gate, D-06 marker gates, D-09 floor gate]
  affects: [crates/ironhermes-agent/tests/invariants_36_15.rs]
tech_stack:
  added: []
  patterns: [include_str! static-grep, strip_comments helper, #[allow(dead_code)] on anchor const]
key_files:
  created:
    - crates/ironhermes-agent/tests/invariants_36_15.rs
  modified: []
decisions:
  - "Used #[allow(dead_code)] on AGENT_RUNTIME_SOURCE const rather than adding a 6th test (plan specifies exactly 5 tests); agent_runtime.rs resolve_extras is already gated by its own inline test run_turn_calls_resolve_extras"
  - "strip_comments helper filters // line-comments before substring matching to prevent false positives from doc-comments containing the asserted pattern"
  - "Pre-existing clippy errors in context_engine.rs (assert!(true) tautologies) confirmed pre-date this plan via git show on base commit — not introduced by plan 06"
metrics:
  duration: "~8 minutes"
  completed: "2026-05-26"
  tasks_completed: 2
  files_created: 1
---

# Phase 36.15 Plan 06: Static-Grep Invariants (invariants_36_15.rs) Summary

One-liner: 5 static-grep tests locking PROV-11 resolved_extras wiring + D-06 untouched-path markers + D-09 stream_options floor via include_str! pattern (no dev-deps).

## Tasks Completed

### Task 1: Create tests/invariants_36_15.rs with static-grep gates

Created `crates/ironhermes-agent/tests/invariants_36_15.rs` following the exact shape of `invariants_36_14.rs`.

**5 invariants implemented:**

1. **`prov_11_agent_loop_no_longer_passes_literal_none_for_extra`**
   Asserts (negative) that stripped `agent_loop.rs` does NOT contain
   `"chat_completion(messages, tools, None, None, None, None)"` or
   `"chat_completion_stream(messages, tools, None, None, None, None)"`.
   Failure message: "PROV-11 (phase 36.15): agent_loop.rs call_llm / call_llm_streaming must pass self.resolved_extras.clone() — not literal None — for the extra parameter. If this test fails, Plan 04's substitution at the call_llm site has regressed."

2. **`prov_11_agent_loop_passes_resolved_extras_clone`**
   Asserts `stripped.matches("self.resolved_extras.clone()").count() >= 2`.
   Failure message: "PROV-11 (phase 36.15): agent_loop.rs must contain `self.resolved_extras.clone()` at least twice — once in call_llm and once in call_llm_streaming."

3. **`d_06_openrouter_claude_path_still_present_and_untouched`**
   Asserts stripped `any_client.rs` contains `"fn build_openrouter_chat_request_full"`.
   Failure message: "D-06: build_openrouter_chat_request_full is intentionally out-of-scope in phase 36.15. If this signature is missing, the function was removed or renamed — verify before continuing."

4. **`d_06_anthropic_client_underscore_extra_still_ignored`**
   Asserts stripped `anthropic_client.rs` contains `"_extra: Option<HashMap"`.
   Failure message: "D-06: AnthropicClient _extra is intentionally ignored in phase 36.15 (leading underscore signals 'unused'). If the underscore is gone, the extras flow has been accidentally wired into the native Anthropic path — revert and defer."

5. **`client_rs_stream_options_include_usage_floor_preserved`**
   Three independent asserts: `or_insert_with`, `stream_options`, `include_usage` all present in stripped `client.rs`.
   Failure message: "D-09 invariant: client.rs `or_insert_with` floor for stream_options.include_usage MUST remain."

**Commit:** `ad25f341`

### Task 2: Full-workspace release build + workspace test gate

- `cargo build --release --workspace`: `Finished 'release' profile [optimized] target(s) in 3m 34s` — clean.
- `cargo test -p ironhermes-core`: 16 passed, 0 failed.
- `cargo test -p ironhermes-agent`: All test result lines "ok". 400+ tests in the lib harness; 5 invariants_36_15 tests; 4 invariants_36_14 tests; 7 extra_request_options tests — all green.
- `cargo test -p ironhermes-agent --test invariants_36_14`: 4 passed — PROV-07 fallback chain unchanged.
- `cargo clippy --workspace --all-targets`: 2 pre-existing errors in `context_engine.rs` (tautological boolean assertions at lines 448 and 560) confirmed to pre-date this plan via `git show a7ce5ace` — not introduced by plan 06. Zero new warnings from invariants_36_15.rs (the `#[allow(dead_code)]` on `AGENT_RUNTIME_SOURCE` suppresses the expected warning).

## Deviations from Plan

### Auto-fixed Issues

None — plan executed exactly as written, with one minor structural decision:

**Structural: AGENT_RUNTIME_SOURCE const with #[allow(dead_code)]**
- **Found during:** Task 1 — the plan includes `AGENT_RUNTIME_SOURCE` as one of 5 `include_str!` consts but specifies exactly 5 `#[test]` functions, none of which use it directly.
- **Fix:** Applied `#[allow(dead_code)]` to the const. The const serves as a compile-time check that `agent_runtime.rs` is reachable; `resolve_extras` wiring is already gated by agent_runtime.rs's own inline test `run_turn_calls_resolve_extras`.
- **Files modified:** `invariants_36_15.rs` (the new file itself)

## Known Stubs

None.

## Threat Flags

None — this plan creates only a test file with no runtime trust boundaries.

## Self-Check: PASSED

- `crates/ironhermes-agent/tests/invariants_36_15.rs` exists: FOUND
- `grep -c "^#\[test\]"` returns 5: CONFIRMED
- Commit `ad25f341` exists: CONFIRMED
- `cargo test -p ironhermes-agent --test invariants_36_15`: 5 passed
- `cargo test -p ironhermes-agent --test invariants_36_14`: 4 passed (PROV-07 unchanged)
- `cargo build --release --workspace`: Finished successfully
- STATE.md: NOT modified (per parallel executor contract)
- ROADMAP.md: NOT modified (per parallel executor contract)
