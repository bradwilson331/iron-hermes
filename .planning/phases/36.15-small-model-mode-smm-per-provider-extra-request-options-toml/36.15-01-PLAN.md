---
phase: 36.15
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - crates/ironhermes-core/src/config.rs
autonomous: true
requirements:
  - PROV-11
  - PROV-12
  - PROV-13
  - PROV-14

must_haves:
  truths:
    - "REQUIREMENTS.md lists PROV-11, PROV-12, PROV-13, PROV-14 with Phase 36.15 traceability"
    - "ROADMAP.md Phase 36.15 entry has a real Goal and a real Requirements line (no [TBD])"
    - "A canary test exists that round-trips the locked TOML/YAML extras shape (provider-level num_ctx + per-model num_ctx override) through serde_yaml and FAILS today because the struct fields don't exist yet"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "PROV-11..PROV-14 entries + traceability rows"
      contains: "PROV-11"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 36.15 finalized Goal + Requirements line"
      contains: "PROV-11"
    - path: "crates/ironhermes-core/src/config.rs"
      provides: "Wave 0 canary test module that round-trips the locked YAML extras shape"
      contains: "mod extras_canary"
  key_links:
    - from: "REQUIREMENTS.md PROV-11..PROV-14 rows"
      to: "Phase 36.15 traceability table"
      via: "Phase column = 'Phase 36.15'"
      pattern: "PROV-1[1-4].*Phase 36.15"
    - from: "config.rs::extras_canary test"
      to: "ProviderConfig deserialization path"
      via: "serde_yaml::from_str + Config::providers HashMap access"
      pattern: "serde_yaml::from_str"
---

<objective>
Land Wave 0 scaffolding: add new requirement IDs **PROV-11..PROV-14** to `REQUIREMENTS.md`, finalize the `ROADMAP.md` Phase 36.15 entry, and write a failing **YAML round-trip canary test** that locks the exact locked TOML/YAML shape from CONTEXT.md D-03 (provider-level `num_ctx = 8192` + per-model `"llama3.1:8b"` override `num_ctx = 32768`). This canary test is the highest-risk gate from PATTERNS.md Pitfall 1 (untagged-enum-with-all-Option-fields). Plan 02 makes it pass by adding the struct fields; if Plan 02's untagged enum mis-deserializes, this test fails loudly. No struct additions in this plan — only the canary, requirements, and roadmap.

Purpose: Establish PROV-11..PROV-14 as durable requirement IDs and force the typed-shape decision through a failing test before any production code is written. Removes ambiguity later when Plan 02 picks between the untagged-enum path (D-01 strict) and the `HashMap<String, serde_json::Value>` fallback (D-01 deviation, documented in plan).

Output: Updated `REQUIREMENTS.md` + `ROADMAP.md` + a `#[cfg(test)] mod extras_canary` block at the bottom of `crates/ironhermes-core/src/config.rs` whose test functions reference fields that don't yet exist (`provider.extra_request_options`, `provider.models`), causing `cargo build -p ironhermes-core --tests` to fail at compile time. This compile-time failure IS the Wave 0 "RED" — Plan 02 makes it "GREEN".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-CONTEXT.md
@.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-RESEARCH.md
@.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-PATTERNS.md

<interfaces>
<!-- Key locations the executor needs. Extracted from codebase per PATTERNS.md / RESEARCH.md. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From crates/ironhermes-core/src/config.rs (CURRENT shape — what Wave 0 starts against):
- `pub struct Config { pub providers: HashMap<String, ProviderConfig>, ... }` — top-level YAML root
- `pub struct ProviderConfig { base_url, api_key, api_key_env, api_mode, default_model, fallback_providers, disabled }` at line 196 area — NO `extra_request_options`, NO `models` field yet. The canary test references these missing fields and MUST fail to compile until Plan 02 lands them.
- `#[serde(default)]` is on `ProviderConfig` (line ~194) — meaning today's configs without `extra_request_options` parse cleanly.
- Project uses `serde_yaml::from_str` for YAML config deserialization (NOT `toml::from_str`) — CONTEXT.md uses TOML syntax illustratively; the actual file is `config.yaml`.

From .planning/REQUIREMENTS.md (CURRENT state — PROV-10 is highest-assigned PROV ID; PROV-11..14 are free):
- "Provider Resolution" section ends at PROV-10 (line 139 area).
- Traceability table ends with `PROV-10 | Phase 21.7 (was 12) | Complete` (line 353 area).
- Last-updated footer is at the bottom (`*Last updated: 2026-05-24 ...*`).

From .planning/ROADMAP.md (CURRENT Phase 36.15 entry, line 123 area):
- `**Goal:** [Urgent work - to be planned]`
- `**Requirements**: TBD`
- `**Plans:** 0 plans` and `- [ ] TBD (run /gsd-plan-phase 36.15 to break down)`
- The Plan list and counts will be updated by the planner-orchestrator post-plan write; this plan's task only finalizes Goal + Requirements line.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append PROV-11..PROV-14 to REQUIREMENTS.md + traceability rows</name>
  <read_first>
    - .planning/REQUIREMENTS.md (the entire Provider Resolution section + the Traceability table)
    - .planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-RESEARCH.md §Phase Requirements (PROV-11..PROV-14 descriptions)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Edit `.planning/REQUIREMENTS.md` to add four new requirement IDs under the existing "Provider Resolution" category, immediately AFTER the existing PROV-10 bullet:

    - **PROV-11**: Per-provider `extra_request_options` TOML/YAML knob wired through `AgentRuntime` into `ChatRequest.extra` on every OpenAI-compat LLM call (Ollama `num_ctx`, vLLM `top_k`, OpenRouter non-Claude `provider.order`).
    - **PROV-12**: Per-model override of `extra_request_options` under `providers.<name>.models.<model>` with shallow per-key merge (per-model wins; missing per-model keys fall back to provider default).
    - **PROV-13**: `extra_request_options` keys discoverable via `hermes config show`/`hermes config set` — entries appear in `config_schema.rs` static schema Vec.
    - **PROV-14**: Merge precedence is "caller wins per key" (D-09); the existing `stream_options.include_usage: true` floor at `client.rs:236` is preserved; mid-session `/model` switch picks up per-model extras on the next turn (D-10); PROV-07 SSE fallback chain from Phase 36.14 is unchanged.

    All four are checkbox-unchecked (`- [ ]`).

    Then append four rows to the Traceability table (after the existing PROV-10 row):

    ```
    | PROV-11 | Phase 36.15 | Pending |
    | PROV-12 | Phase 36.15 | Pending |
    | PROV-13 | Phase 36.15 | Pending |
    | PROV-14 | Phase 36.15 | Pending |
    ```

    Finally update the bottom footer date stamp: append a new line `*Last updated: 2026-05-26 — Phase 36.15 import: + 4 Provider Resolution reqs (PROV-11 extras flow, PROV-12 per-model override, PROV-13 schema discoverability, PROV-14 merge + D-10 + PROV-07 regression).*` immediately above the existing dated footer lines.

    DO NOT edit any other requirement description; PROV-01..PROV-10 stay byte-identical. DO NOT increment the `v2.1 active` count in the milestone header (these are out-of-milestone phase-local additions, consistent with how Phase 36 added GW-05 wiring without bumping the milestone count).
  </action>
  <verify>
    <automated>grep -c "^- \[ \] \*\*PROV-1[1-4]\*\*" .planning/REQUIREMENTS.md | grep -q "^4$" &amp;&amp; grep -E "^\| PROV-1[1-4] \| Phase 36\.15 \| Pending \|$" .planning/REQUIREMENTS.md | wc -l | grep -q "^[[:space:]]*4$"</automated>
  </verify>
  <acceptance_criteria>
    - PROV-11, PROV-12, PROV-13, PROV-14 each appear as unchecked bullets `- [ ] **PROV-1N**` in the Provider Resolution section.
    - Each of PROV-11..14 appears in the Traceability table mapped to `Phase 36.15 | Pending`.
    - A new dated footer line referencing Phase 36.15 import is present.
    - PROV-01..PROV-10 source text is byte-identical to pre-edit (verify by `git diff -- .planning/REQUIREMENTS.md` showing only additions, no deletions in the PROV-01..PROV-10 lines).
  </acceptance_criteria>
  <done>REQUIREMENTS.md contains four new PROV requirements + four traceability rows + footer line, all referencing Phase 36.15.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize ROADMAP.md Phase 36.15 Goal + Requirements line</name>
  <read_first>
    - .planning/ROADMAP.md (specifically the Phase 36.15 block around line 123)
    - .planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-CONTEXT.md §domain (phase boundary statement)
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    Edit the Phase 36.15 entry in `.planning/ROADMAP.md` (the section whose heading already contains "Phase 36.15: Small Model Mode (SMM) — per-provider extra_request_options TOML knob...").

    Replace the placeholder `**Goal:** [Urgent work - to be planned]` with a goal derived directly from CONTEXT.md §domain — paraphrased to read:

    `**Goal:** Add a per-provider (with optional per-model override) TOML/YAML configuration knob — `extra_request_options` — whose values flow through `AgentRuntime` into `ChatRequest.extra` on every OpenAI-compatible LLM call, so Ollama `num_ctx`, vLLM `top_k`, and OpenRouter `provider.order` (non-Claude routes) can be tuned without code changes. Scope is the knob and its wiring only; the full Small Model Mode architecture port (governor, router, escalation) is Phase 36.16. Closes the Ollama `exceed_context_size_error` fallback path by making `num_ctx` operator-configurable (static knob per D-08; no dynamic retry).`

    Replace the placeholder `**Requirements**: TBD` with:

    `**Requirements**: PROV-11, PROV-12, PROV-13, PROV-14 (added to REQUIREMENTS.md as part of Phase 36.15)`

    Leave the `**Depends on:** Phase 36` line unchanged. Do NOT update the plan count or plan list — the planner-orchestrator owns those mutations after the plan write completes.

    DO NOT modify Phase 36.16 or any other phase entry.
  </action>
  <verify>
    <automated>grep -A3 "^### Phase 36.15:" .planning/ROADMAP.md | grep -qE "PROV-11.*PROV-12.*PROV-13.*PROV-14" &amp;&amp; ! grep -A3 "^### Phase 36.15:" .planning/ROADMAP.md | grep -q "Urgent work - to be planned"</automated>
  </verify>
  <acceptance_criteria>
    - Phase 36.15 `**Goal:**` line no longer contains "[Urgent work - to be planned]" and DOES contain the phrase "extra_request_options" plus a reference to "Phase 36.16" as the deferred architecture-port successor.
    - Phase 36.15 `**Requirements**:` line lists all four IDs PROV-11, PROV-12, PROV-13, PROV-14.
    - Phase 36.16 section is byte-identical to pre-edit (verify via `git diff` shows zero changes outside the Phase 36.15 block).
  </acceptance_criteria>
  <done>ROADMAP.md Phase 36.15 entry has real Goal + Requirements; no other phase touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Wave 0 canary test module to config.rs locking the D-03 YAML round-trip shape</name>
  <read_first>
    - crates/ironhermes-core/src/config.rs (the ENTIRE file — needed to find the existing `#[cfg(test)] mod tests` block at the bottom and the `ProviderConfig` declaration around line 196)
    - .planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-CONTEXT.md §decisions D-01..D-03 (the locked YAML shape)
    - .planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-RESEARCH.md §Pitfall 1 (untagged enum risk) and §Code Examples (config roundtrip test pattern)
    - .planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-PATTERNS.md §Shared Patterns (`#[serde(default)]` + Default impl pattern)
  </read_first>
  <behavior>
    The canary test asserts the locked acceptance shape from CONTEXT.md D-03:
    - Test 1 `extras_canary_provider_level_num_ctx_roundtrips`: a YAML doc with `providers.ollama.extra_request_options.num_ctx = 8192` deserializes into `Config` such that `config.providers["ollama"].extra_request_options` is `Some(...)` with `num_ctx == 8192`.
    - Test 2 `extras_canary_per_model_override_wins`: a YAML doc with provider-level `num_ctx = 8192` AND `providers.ollama.models."llama3.1:8b".extra_request_options.num_ctx = 32768` deserializes such that the per-model entry's `num_ctx == 32768` AND the provider-level `num_ctx == 8192` are BOTH preserved (the merge happens in `resolve_extras`, not at deserialization).
    - Test 3 `extras_canary_quoted_yaml_key_with_colon_parses`: a YAML doc with the quoted key `"llama3.1:8b"` (colon-bearing) deserializes into `provider.models` successfully without serde error.
    - Test 4 `extras_canary_openrouter_provider_order_nested`: a YAML doc with `providers.openrouter.extra_request_options.provider.order = ["anthropic", "openai"]` deserializes such that the nested order list is preserved exactly.

    All four tests reference field paths (`provider.extra_request_options`, `provider.models`) that do not exist in the current `ProviderConfig` struct. The test MUST therefore fail to compile until Plan 02 lands those fields. This is the desired Wave 0 RED state.
  </behavior>
  <files>crates/ironhermes-core/src/config.rs</files>
  <action>
    At the bottom of `crates/ironhermes-core/src/config.rs`, append a new test module guarded by `#[cfg(test)]` (separate from any existing `mod tests` block — name this one `mod extras_canary`):

    - Module uses `super::*` to import the `Config` struct.
    - Module imports `std::collections::HashMap` (already in scope at module level, but re-import inside the test module to keep tests self-contained).
    - Each test is a plain `#[test]` (synchronous — `serde_yaml::from_str` is sync).
    - Each test calls `serde_yaml::from_str::<Config>(yaml)` and asserts on the resulting `Config::providers` HashMap.
    - Provider-level access pattern: `config.providers.get("ollama").expect("ollama provider").extra_request_options.as_ref().expect("extras present")` — the field path that DOES NOT YET EXIST.
    - Per-model access pattern: `config.providers.get("ollama").expect("ollama").models.get("llama3.1:8b").expect("model entry").extra_request_options.as_ref().expect("model extras present")`.
    - For `num_ctx` value extraction, match against the `ProviderExtraOptions::Ollama(OllamaExtraOptions)` variant produced by Plan 02 — use pattern matching: `if let Some(ProviderExtraOptions::Ollama(opts)) = &provider.extra_request_options { assert_eq!(opts.num_ctx, Some(8192)); } else { panic!("expected Ollama variant") }`.
    - For `OpenRouter` `provider.order`: pattern-match `ProviderExtraOptions::OpenRouter(or)` and assert `or.provider.as_ref().and_then(|p| p.order.as_ref())` equals `Some(&vec!["anthropic".to_string(), "openai".to_string()])`.
    - The YAML strings embedded in tests MUST use the exact D-03 locked shape (CONTEXT.md §decisions): top-level `providers:`, nested `extra_request_options:`, and quoted YAML key `"llama3.1:8b"`.

    Document at the top of the module with a doc comment: `//! Wave 0 (Phase 36.15) canary tests. These reference fields not yet present on ProviderConfig — compilation fails on purpose until Plan 02 lands the struct extension. If untagged-enum deserialization (D-01) proves brittle in serde_yaml when all variant fields are Option<T>, the planner falls back to Option<HashMap<String, serde_json::Value>> on ProviderConfig directly; these tests are updated accordingly at that decision point.`

    DO NOT add `extra_request_options` or `models` to the `ProviderConfig` struct in this task. DO NOT modify any other module. DO NOT touch the existing `mod tests` block.

    The `Cargo.toml` for `ironhermes-core` already has `serde_yaml` as a dependency (verified — it's used elsewhere in the same file). No Cargo.toml edits.
  </action>
  <verify>
    <automated>grep -q "^mod extras_canary {" crates/ironhermes-core/src/config.rs &amp;&amp; ! (cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-core --tests 2>&amp;1 | grep -q "error\[E0609\]\\|error\[E0599\]\\|error\[E0433\]" ; echo $? | grep -q "^0$")</automated>
  </verify>
  <acceptance_criteria>
    - `crates/ironhermes-core/src/config.rs` contains the literal text `mod extras_canary` (the new module).
    - The module contains four `#[test]` functions whose names match the four behavior IDs above (`extras_canary_provider_level_num_ctx_roundtrips`, `extras_canary_per_model_override_wins`, `extras_canary_quoted_yaml_key_with_colon_parses`, `extras_canary_openrouter_provider_order_nested`).
    - Running `cargo build -p ironhermes-core --tests` FAILS with a compile error citing the missing `extra_request_options` field or missing `models` field on `ProviderConfig` (E0609 unknown field), the missing `ProviderExtraOptions` type (E0433 unresolved), or the missing `OllamaExtraOptions` / `OpenRouterExtraOptions` types. This is the Wave 0 RED gate.
    - `cargo build -p ironhermes-core` (no --tests) still SUCCEEDS — production code is untouched.
    - The existing `mod tests` block in `config.rs` (if any) is byte-identical to pre-edit.
  </acceptance_criteria>
  <done>config.rs has a `mod extras_canary` block with four serde_yaml round-trip tests that fail to compile against current `ProviderConfig` (Wave 0 RED locked); production build still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → config.yaml | Operator-controlled YAML file; values flow to LLM request bodies. Operator self-trust. |
| .planning/ files → no runtime impact | REQUIREMENTS.md and ROADMAP.md are documentation; no code path consumes them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.15-01 | Tampering | REQUIREMENTS.md PROV-* numbering | accept | Phase 36.15 owns its IDs; no overlap risk because PROV-10 is highest existing. |
| T-36.15-02 | Information Disclosure | canary test module | accept | Tests embed example YAML only; no secrets, no key material. |
| T-36.15-03 | Tampering | ROADMAP.md placeholder replacement | mitigate | `git diff` review post-edit confirms only Phase 36.15 block changed. Acceptance criteria embed this check. |
</threat_model>

<verification>
- `grep -c "^- \[ \] \*\*PROV-1[1-4]\*\*" .planning/REQUIREMENTS.md` returns `4`.
- `grep -A3 "^### Phase 36.15:" .planning/ROADMAP.md` does not contain `[Urgent work - to be planned]`.
- `cd /Users/twilson/code/ironhermes && cargo build -p ironhermes-core --tests 2>&1 | tail -20` shows compile errors referencing `extra_request_options` or `models` field on `ProviderConfig` (Wave 0 RED gate).
- `cd /Users/twilson/code/ironhermes && cargo build -p ironhermes-core 2>&1 | tail -5` shows clean build (production code untouched).
</verification>

<success_criteria>
1. REQUIREMENTS.md lists PROV-11, PROV-12, PROV-13, PROV-14 with descriptions matching CONTEXT.md scope.
2. REQUIREMENTS.md traceability table maps all four to Phase 36.15.
3. ROADMAP.md Phase 36.15 entry has a real Goal (no `[TBD]`/`[Urgent work...]`) and a real Requirements list.
4. `config.rs` has a new `mod extras_canary` test module with four tests that round-trip the D-03 locked YAML shape.
5. `cargo build -p ironhermes-core --tests` FAILS (expected; Wave 0 RED).
6. `cargo build -p ironhermes-core` SUCCEEDS (production code untouched).
</success_criteria>

<output>
Create `.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-01-SUMMARY.md` when done, documenting:
- PROV-11..PROV-14 added with one-line descriptions
- Roadmap finalized
- Canary tests added and confirmed failing at compile (the diagnostic message text)
- Decision marker for Plan 02: if Plan 02's untagged-enum deserialization fails any of the four canary tests, fall back to `Option<HashMap<String, serde_json::Value>>` on ProviderConfig and document the D-01 deviation in 36.15-02-SUMMARY.md as an ADR-style note.
</output>
