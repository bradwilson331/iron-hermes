---
status: partial
phase: 36.15-small-model-mode-smm-per-provider-extra-request-options-toml
source: [36.15-VERIFICATION.md]
started: 2026-05-26T18:00:00Z
updated: 2026-05-26T18:00:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Ollama live end-to-end (closes phase goal — Ollama exceed_context_size_error fallback)
expected: With `providers.ollama.extra_request_options.num_ctx: 8192` (or per-model 32768) in config.yaml, Ollama receives `num_ctx` on the wire and loads the model with that context window. No `exceed_context_size_error` on a prompt that fills the extended window.
result: [pending]

### 2. `hermes config show` surfaces the new extras keys (PROV-13)
expected: `hermes config show` lists `providers.ollama.extra_request_options.num_ctx` (and the other five canonical keys: `num_predict`, `top_k` for Ollama+vLLM, `top_p` for vLLM, `provider.order` for OpenRouter). Each entry shows as non-secret, non-required, non-cache-breaking.
result: [pending]

### 3. `hermes config set` accepts a canonical extras key (PROV-13)
expected: `hermes config set providers.ollama.extra_request_options.num_ctx 32768` succeeds (exit 0), writes the value to `~/.ironhermes/config.yaml` under the correct YAML path, and does NOT emit an "unknown key" validation error.
result: [pending]

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
