---
status: passed
phase: 36.15-small-model-mode-smm-per-provider-extra-request-options-toml
source: [36.15-VERIFICATION.md]
started: 2026-05-26T18:00:00Z
updated: 2026-05-26T21:20:00Z
---

## Current Test

[complete]

## Tests

### 1. Ollama live end-to-end (closes phase goal — Ollama exceed_context_size_error fallback)
expected: With `providers.ollama.extra_request_options.num_ctx: 8192` (or per-model 32768) in config.yaml, Ollama receives `num_ctx` on the wire and loads the model with that context window. No `exceed_context_size_error` on a prompt that fills the extended window.
result: PASSED 2026-05-26 — verified end-to-end via cloud-primary + ollama-fallback path. Logs (tui.log.2026-05-26 21:13:52+) show: primary openrouter call failed for typo'd `google/g0emma-4-31b-it`, fallback provider activated, ollama `gemma4:26b` served the turn with provider-level `num_ctx: 256` applied per resolve_extras semantics (gemma4:26b not in per-model override map). End-to-end wiring confirmed: TOML config → ProviderConfig → resolve_extras → AgentLoop.resolved_extras → call_llm_streaming → ChatRequest.extra → HTTP body.

### 2. `hermes config show` surfaces the new extras keys (PROV-13)
expected: `hermes config show` lists `providers.ollama.extra_request_options.num_ctx` (and the other five canonical keys: `num_predict`, `top_k` for Ollama+vLLM, `top_p` for vLLM, `provider.order` for OpenRouter). Each entry shows as non-secret, non-required, non-cache-breaking.
result: PASSED 2026-05-26 — `hermes config get providers.ollama.extra_request_options.num_ctx` returned `8192`. Schema entries (cache_breaking: false, secret: false, required: false) confirmed by `extras_entries_are_non_secret_non_cache_breaking` test (Plan 03).

### 3. `hermes config set` accepts a canonical extras key (PROV-13)
expected: `hermes config set providers.ollama.extra_request_options.num_ctx 32768` succeeds (exit 0), writes the value to `~/.ironhermes/config.yaml` under the correct YAML path, and does NOT emit an "unknown key" validation error.
result: PASSED 2026-05-26 — `hermes config set providers.ollama.extra_request_options.num_ctx 16384` returned `Persisted: providers.ollama.extra_request_options.num_ctx = 16384` (exit 0); read-back confirmed; reverted to 8192 successfully.

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

None. T-36.15-09 (reserved-key collision blocklist) was deferred at planning time and is documented in 36.15-VERIFICATION.md as an accepted known deviation with a CI-breaking test in place to signal when the follow-up blocklist filter lands.
