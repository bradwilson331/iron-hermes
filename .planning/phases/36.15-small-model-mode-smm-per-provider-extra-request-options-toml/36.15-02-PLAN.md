---
phase: 36.15
plan: 02
type: execute
wave: 1
depends_on:
  - 36.15-01
files_modified:
  - crates/ironhermes-core/src/config.rs
  - crates/ironhermes-core/src/config_extras.rs
  - crates/ironhermes-core/src/lib.rs
autonomous: true
requirements:
  - PROV-11
  - PROV-12

must_haves:
  truths:
    - "ProviderConfig has new `extra_request_options: Option<ProviderExtraOptions>` and `models: HashMap<String, ProviderModelConfig>` fields, both `#[serde(default)]` so pre-36.15 configs parse unchanged"
    - "The four Wave 0 canary tests added by Plan 01 (`extras_canary_*`) PASS — provider-level num_ctx, per-model override coexistence, quoted YAML key with colon, OpenRouter provider.order nested"
    - "`config_extras::resolve_extras(&providers, provider_name, model_name)` returns a merged `HashMap<String, serde_json::Value>` where per-model keys win over per-provider keys (D-03)"
    - "An additional unit test asserts that with provider `num_ctx = 8192` and per-model `num_ctx = 32768` for `llama3.1:8b`, `resolve_extras` returns `{num_ctx: 32768}` for that model AND `{num_ctx: 8192}` for `llama3.1:70b` (model absent from `models` map → provider fallback)"
  artifacts:
    - path: "crates/ironhermes-core/src/config_extras.rs"
      provides: "Typed extras structs + resolve_extras merge helper"
      exports: ["OllamaExtraOptions", "VllmExtraOptions", "OpenRouterExtraOptions", "ProviderRouting", "ProviderExtraOptions", "ProviderModelConfig", "resolve_extras"]
    - path: "crates/ironhermes-core/src/config.rs"
      provides: "ProviderConfig extension with extra_request_options + models fields"
      contains: "extra_request_options"
    - path: "crates/ironhermes-core/src/lib.rs"
      provides: "Module declaration `pub mod config_extras;`"
      contains: "pub mod config_extras"
  key_links:
    - from: "crates/ironhermes-core/src/config.rs::ProviderConfig"
      to: "crates/ironhermes-core/src/config_extras.rs::ProviderExtraOptions"
      via: "field type"
      pattern: "extra_request_options: Option<.*ProviderExtraOptions>"
    - from: "config_extras::resolve_extras"
      to: "serde_json::to_value + as_object"
      via: "typed-struct to HashMap conversion at boundary"
      pattern: "serde_json::to_value"
---

<objective>
Land the typed-config layer per CONTEXT.md D-01..D-04 and turn the Wave 0 canary RED into GREEN. Create `crates/ironhermes-core/src/config_extras.rs` containing the strongly-typed extras structs (`OllamaExtraOptions`, `VllmExtraOptions`, `OpenRouterExtraOptions`, `ProviderRouting`), the untagged `ProviderExtraOptions` enum, the `ProviderModelConfig` wrapper, and the `resolve_extras` merge helper. Extend `ProviderConfig` in `config.rs` with two new fields (`extra_request_options`, `models`). Wire the new module from `lib.rs`. Confirm the four Plan 01 canary tests PASS plus add the additional `resolve_extras` merge unit test.

Purpose: This is the single source of truth for "what is a valid extras shape." Plan 04 will serialize the typed value to `HashMap<String, serde_json::Value>` via `serde_json::to_value` at the AgentRuntime boundary (D-04). Plan 03 will list the schema keys these structs accept (PROV-13).

Output: A new `config_extras.rs` module + two new fields on `ProviderConfig` + lib.rs module declaration. Plan 01's canary tests turn from RED-compile to GREEN-pass.

**ADR note for executor:** If the untagged enum `ProviderExtraOptions` fails to round-trip the Plan 01 canary tests through `serde_yaml` (PATTERNS.md Pitfall 1 — all-Option-fields variant ambiguity), fall back to a raw `Option<HashMap<String, serde_json::Value>>` on `ProviderConfig` directly. The fallback is the documented D-01 deviation. Either way, the `resolve_extras` signature stays the same — it always returns `HashMap<String, serde_json::Value>`. Record the chosen path in 36.15-02-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@crates/ironhermes-core/src/config.rs
@crates/ironhermes-core/src/lib.rs
@.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-CONTEXT.md
@.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-RESEARCH.md
@.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-PATTERNS.md
@.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-01-PLAN.md

<interfaces>
<!-- Key locations and the existing analog struct to mirror. Extracted from PATTERNS.md. -->

From crates/ironhermes-core/src/config.rs (analog — PromptCachingConfig at lines 343-399):
- Existing typed-config-struct pattern uses `#[derive(Debug, Clone, Serialize, Deserialize)]` + `#[serde(default)]` on the struct + explicit `impl Default for X { ... }` + `#[serde(skip_serializing_if = "Option::is_none")]` on every Option field.
- Closed-enum precedent (CacheTtl): `#[derive(...)] #[serde(rename_all = "lowercase")] pub enum CacheTtl { FiveMinutes, OneHour }` — closed variant set for type safety.

From crates/ironhermes-core/src/config.rs::ProviderConfig (line ~196):
- Current fields: `base_url`, `api_key` (deprecated), `api_key_env`, `api_mode`, `default_model`, `fallback_providers: Vec<String>`, `disabled: Option<bool>`.
- `#[serde(default)]` is on the struct; `impl Default` returns explicit None/empty values.
- Add two new fields after `disabled`: `pub extra_request_options: Option<ProviderExtraOptions>` and `pub models: HashMap<String, ProviderModelConfig>`. Default impl extends with `extra_request_options: None` and `models: HashMap::new()`.

From .planning/phases/.../36.15-PATTERNS.md §config_extras.rs (NEW):
- `OllamaExtraOptions { num_ctx: Option<u32>, num_predict: Option<i32>, top_k: Option<u32>, top_p: Option<f64>, num_batch: Option<u32> }`.
- `VllmExtraOptions { top_k: Option<i32>, top_p: Option<f64>, repetition_penalty: Option<f64>, min_p: Option<f64> }`.
- `OpenRouterExtraOptions { provider: Option<ProviderRouting> }`.
- `ProviderRouting { order: Option<Vec<String>>, ignore: Option<Vec<String>>, quantizations: Option<Vec<String>> }`.
- `#[serde(untagged)] enum ProviderExtraOptions { Ollama(OllamaExtraOptions), OpenRouter(OpenRouterExtraOptions), Vllm(VllmExtraOptions) }` — note: declaration order matters for untagged matching; OpenRouter is listed BEFORE Vllm so that the `provider:` key (OpenRouter-only) gets matched before falling through to Vllm. Ollama is first because `num_ctx` is unique to Ollama.
- `ProviderModelConfig { extra_request_options: Option<ProviderExtraOptions> }` — `#[serde(default)]`.

From .planning/phases/.../36.15-PATTERNS.md §resolve_extras helper:
- Signature: `pub fn resolve_extras(providers: &HashMap<String, crate::config::ProviderConfig>, provider_name: &str, model_name: &str) -> HashMap<String, serde_json::Value>`.
- Path: provider extras → serde_json::to_value → .as_object().cloned() → HashMap; same for per-model; merge by inserting model entries into provider HashMap (model wins).
- D-03 shallow merge (no recursive deep merge) — confirmed in CONTEXT.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create config_extras.rs with typed structs and resolve_extras helper</name>
  <read_first>
    - crates/ironhermes-core/src/config.rs (the entire file — esp. PromptCachingConfig at lines 343-399 as the analog pattern, ProviderConfig at lines 196-223, and the existing `use` block at the top)
    - crates/ironhermes-core/src/lib.rs (to learn how modules are declared)
    - .planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-PATTERNS.md §config_extras.rs (the exact struct shapes to copy)
    - .planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-RESEARCH.md §Pattern 1, §Pattern 2 (fallback), §Pattern 4 (merge helper)
  </read_first>
  <behavior>
    - Test 1 `resolve_extras_returns_empty_when_no_extras`: Empty `providers` map → `resolve_extras(&map, "ollama", "any")` returns empty HashMap.
    - Test 2 `resolve_extras_provider_only_falls_back_to_provider_default`: Provider has `num_ctx = 8192`, no `models` entry → `resolve_extras(&map, "ollama", "llama3.1:70b")` returns `{num_ctx: 8192}`.
    - Test 3 `resolve_extras_per_model_wins_per_key_d03`: Provider has `num_ctx = 8192` AND `num_predict = 128`, per-model entry for `llama3.1:8b` has `num_ctx = 32768` (no num_predict) → `resolve_extras(&map, "ollama", "llama3.1:8b")` returns `{num_ctx: 32768, num_predict: 128}` (model wins on num_ctx, provider fills num_predict gap).
    - Test 4 `resolve_extras_openrouter_provider_order_nested_preserved`: Provider extras have `provider: {order: ["anthropic", "openai"]}` → `resolve_extras` returns `{"provider": Value::Object({"order": Value::Array([...])})}` — the nested object survives `to_value` round-trip.
    - Test 5 `resolve_extras_unknown_provider_returns_empty`: `resolve_extras(&map, "nonexistent_provider", "any_model")` returns empty HashMap (no panic, no error).
    - Test 6 (compile-only) `provider_extra_options_default_is_none_friendly`: All four typed structs implement `Default`; `OllamaExtraOptions::default()` produces all-None fields.
  </behavior>
  <files>
    crates/ironhermes-core/src/config_extras.rs,
    crates/ironhermes-core/src/lib.rs
  </files>
  <action>
    Create `crates/ironhermes-core/src/config_extras.rs` from scratch following the structure laid out in PATTERNS.md §config_extras.rs.

    The module exports:
    - `OllamaExtraOptions` — fields: `num_ctx: Option<u32>`, `num_predict: Option<i32>`, `top_k: Option<u32>`, `top_p: Option<f64>`, `num_batch: Option<u32>`. Derive `Debug, Clone, Default, Serialize, Deserialize`. Apply `#[serde(default)]` to the struct and `#[serde(skip_serializing_if = "Option::is_none")]` to every Option field.
    - `VllmExtraOptions` — fields: `top_k: Option<i32>`, `top_p: Option<f64>`, `repetition_penalty: Option<f64>`, `min_p: Option<f64>`. Same derives + attrs.
    - `OpenRouterExtraOptions` — single field `provider: Option<ProviderRouting>` with `#[serde(skip_serializing_if = "Option::is_none")]`. Same derives + attrs.
    - `ProviderRouting` — fields: `order: Option<Vec<String>>`, `ignore: Option<Vec<String>>`, `quantizations: Option<Vec<String>>`. Same derives + attrs.
    - `ProviderExtraOptions` — `#[derive(Debug, Clone, Serialize, Deserialize)]` + `#[serde(untagged)]` enum with variants in this exact declaration order: `Ollama(OllamaExtraOptions)`, `OpenRouter(OpenRouterExtraOptions)`, `Vllm(VllmExtraOptions)`. Declaration order matters per Pitfall 1 — Ollama is first (unique `num_ctx`), OpenRouter is second (unique `provider` key), Vllm last (its keys overlap most with Ollama).
    - `ProviderModelConfig` — single field `extra_request_options: Option<ProviderExtraOptions>` with `#[serde(default)]` on struct and `#[serde(skip_serializing_if = "Option::is_none")]` on the field. Derives: `Debug, Clone, Default, Serialize, Deserialize`.

    Implement the merge helper function:

    `pub fn resolve_extras(providers: &HashMap<String, crate::config::ProviderConfig>, provider_name: &str, model_name: &str) -> HashMap<String, serde_json::Value>`

    Implementation per RESEARCH §Pattern 4:
    1. Fetch `providers.get(provider_name)` → `Option<&ProviderConfig>`.
    2. Convert provider-level `extra_request_options` to `HashMap<String, serde_json::Value>` via `serde_json::to_value(&opts).ok().and_then(|v| v.as_object().cloned()).map(|m| m.into_iter().collect()).unwrap_or_default()`.
    3. Same for per-model extras via `provider_cfg.and_then(|p| p.models.get(model_name)).and_then(|m| m.extra_request_options.as_ref())`.
    4. Merge: start with provider map, iterate model map and `.insert(k, v)` to overwrite per-key (per-model wins, D-03).
    5. Return merged map.

    Add a `#[cfg(test)] mod tests` block at the bottom of `config_extras.rs` with the six tests listed in `<behavior>`. Tests construct `ProviderConfig` values programmatically via `..Default::default()` (avoiding YAML deserialization concerns here — those are exercised by Plan 01's canary).

    Then edit `crates/ironhermes-core/src/lib.rs` to add `pub mod config_extras;` next to the existing `pub mod config;` (or wherever sibling `pub mod` declarations live — match the existing pattern).

    Adding `use std::collections::HashMap;` and `use serde::{Deserialize, Serialize};` at the top of `config_extras.rs` is required.

    DO NOT modify `ProviderConfig` in this task — that is Task 2. DO NOT add `serde_json` to Cargo.toml — RESEARCH §Standard Stack confirms it's already a workspace dependency.

    **Fallback path (if untagged enum mis-deserializes):** Skip the `ProviderExtraOptions` enum entirely. Instead, replace its uses with `serde_json::Value` directly:
    - `OpenRouterExtraOptions`, `VllmExtraOptions`, etc. remain as typed introspection types (for tests + future schema generation) but `ProviderModelConfig.extra_request_options` and (in Task 2) `ProviderConfig.extra_request_options` become `Option<HashMap<String, serde_json::Value>>` instead. `resolve_extras` becomes simpler (no `to_value` step for the deserialized side). Document this deviation prominently at the top of the file as an ADR-style doc comment, AND update 36.15-02-SUMMARY.md to note the D-01 deviation.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p ironhermes-core --lib config_extras 2>&amp;1 | tail -20 | grep -qE "test result: ok\. [6-9] passed"</automated>
  </verify>
  <acceptance_criteria>
    - `crates/ironhermes-core/src/config_extras.rs` exists with the five public types (`OllamaExtraOptions`, `VllmExtraOptions`, `OpenRouterExtraOptions`, `ProviderRouting`, `ProviderModelConfig`) AND the public function `resolve_extras` — verifiable by `grep -c "^pub \(struct\|fn\|enum\)" crates/ironhermes-core/src/config_extras.rs` returning ≥ 6.
    - `crates/ironhermes-core/src/lib.rs` declares `pub mod config_extras;`.
    - All six unit tests in the `config_extras` module pass.
    - The merge test (Test 3) specifically asserts: `resolve_extras` for model `"llama3.1:8b"` returns a HashMap whose `num_ctx` key is `serde_json::json!(32768)` AND whose `num_predict` key is `serde_json::json!(128)` (the provider-level fallback).
    - If the ADR fallback path was taken: a doc comment at the top of `config_extras.rs` begins with `//! D-01 DEVIATION:` and explains the chosen shape.
  </acceptance_criteria>
  <done>config_extras.rs module exists, lib.rs exports it, six unit tests pass green, resolve_extras correctly implements D-03 per-key merge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ProviderConfig with extra_request_options + models fields and turn Plan 01 canary GREEN</name>
  <read_first>
    - crates/ironhermes-core/src/config.rs (specifically `pub struct ProviderConfig` at line ~196 and `impl Default for ProviderConfig` at lines ~212-223 — the exact analog to extend; also the `mod extras_canary` block from Plan 01 at the bottom of the file)
    - crates/ironhermes-core/src/config_extras.rs (the new module from Task 1)
    - .planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-PATTERNS.md §config.rs MOD section (exact line numbers to edit)
  </read_first>
  <behavior>
    - The four Plan 01 canary tests (`extras_canary_provider_level_num_ctx_roundtrips`, `extras_canary_per_model_override_wins`, `extras_canary_quoted_yaml_key_with_colon_parses`, `extras_canary_openrouter_provider_order_nested`) all PASS after this task.
    - Pre-36.15 configs (no `extra_request_options` key, no `models` key) continue to deserialize successfully — verified by an additional test `pre_36_15_config_yaml_still_parses` that round-trips a minimal `providers.ollama: { base_url: "..." }` YAML.
    - Importing `ProviderExtraOptions` and `ProviderModelConfig` from `crate::config_extras` works inside `config.rs` (the canary tests reference these via `super::*`).
  </behavior>
  <files>crates/ironhermes-core/src/config.rs</files>
  <action>
    Edit `crates/ironhermes-core/src/config.rs`:

    1. At the top of the file (with the other `use` statements), add `use crate::config_extras::{ProviderExtraOptions, ProviderModelConfig};` — OR if the file uses fully-qualified paths in some places, use the fully-qualified form `crate::config_extras::...` directly in the struct field types. Match the style already in use in this file.

    2. In `pub struct ProviderConfig` (around line 196), add two new fields AFTER the existing `disabled: Option<bool>` field:
       - `#[serde(default, skip_serializing_if = "Option::is_none")] pub extra_request_options: Option<ProviderExtraOptions>,`
       - `#[serde(default, skip_serializing_if = "HashMap::is_empty")] pub models: std::collections::HashMap<String, ProviderModelConfig>,`

       Use the fully-qualified `std::collections::HashMap` path if `HashMap` is not already imported in `config.rs` (check the top-of-file `use` block first — if it's already imported via `use std::collections::HashMap;`, drop the `std::collections::` prefix).

    3. In `impl Default for ProviderConfig` (lines ~212-223), add to the struct initializer:
       - `extra_request_options: None,`
       - `models: HashMap::new(),` (or `std::collections::HashMap::new()` to match)

    4. Add a new test to the bottom of the file (NOT inside `mod extras_canary` — add to the existing `mod tests` block if one exists, otherwise create a sibling `#[cfg(test)] mod backcompat_tests`):
       `fn pre_36_15_config_yaml_still_parses()` — deserializes a minimal `providers: { ollama: { base_url: "http://localhost:11434/v1" } }` YAML and asserts `config.providers["ollama"].extra_request_options.is_none()` AND `config.providers["ollama"].models.is_empty()`.

    DO NOT modify any other struct in `config.rs`. DO NOT remove or modify the `mod extras_canary` block from Plan 01 — the test bodies already use the exact field names and types being added here.

    **If the Task 1 ADR fallback path was taken** (`ProviderExtraOptions` is not an enum; extras are `HashMap<String, serde_json::Value>` directly): use `pub extra_request_options: Option<std::collections::HashMap<String, serde_json::Value>>` instead, and update the Plan 01 canary test bodies accordingly (they will need to access `.get("num_ctx")` on a HashMap rather than pattern-matching on a variant). Note the deviation in 36.15-02-SUMMARY.md.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p ironhermes-core --lib -- extras_canary backcompat 2>&amp;1 | tail -15 | grep -qE "test result: ok\. [5-9] passed"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ironhermes-core --lib extras_canary` passes all four canary tests added in Plan 01.
    - `cargo test -p ironhermes-core --lib pre_36_15_config_yaml_still_parses` passes (backward compat).
    - `cargo build -p ironhermes-core` succeeds without warnings about unused fields.
    - `grep -c "extra_request_options" crates/ironhermes-core/src/config.rs` returns ≥ 3 (struct field, Default impl, plus at least one test reference) — verifies the field landed.
    - `grep -q "models: std::collections::HashMap<String, ProviderModelConfig>" crates/ironhermes-core/src/config.rs || grep -q "models: HashMap<String, ProviderModelConfig>" crates/ironhermes-core/src/config.rs` succeeds.
    - PROV-01..PROV-10 unrelated tests still pass: `cargo test -p ironhermes-core --lib 2>&1 | tail -5` shows the same pass count as pre-task baseline (≥ baseline).
  </acceptance_criteria>
  <done>ProviderConfig has the two new fields with #[serde(default)] guarding backward compat; the four Wave 0 canary tests turn from RED-compile to GREEN-pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config.yaml → ProviderConfig | Untrusted-by-operator-self YAML parsed via serde_yaml. Field shape enforces type safety. |
| ProviderExtraOptions → serde_json::Value (wire) | Untagged-enum decode + serde_json re-encode at the boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.15-04 | Tampering | Untagged enum mis-deserializes to wrong variant (Pitfall 1) | mitigate | Plan 01 canary tests fail loudly if mis-deserialization occurs. ADR fallback to `HashMap<String, serde_json::Value>` documented and executable. |
| T-36.15-05 | Tampering | Per-model entry could shadow `default_model` or other ProviderConfig fields by accident | accept | `models` is a HashMap<String, ProviderModelConfig> (separate type, separate field), not a flat key map — schema enforces structural separation. |
| T-36.15-06 | Information Disclosure | Extras serde-skip rules could leak unset fields as `null` keys on the wire | mitigate | `#[serde(skip_serializing_if = "Option::is_none")]` on every Option field; nulls suppressed by construction. Verified by Test 4 (OpenRouter provider.order nested) — only the order key appears, not `ignore: null` or `quantizations: null`. |
</threat_model>

<verification>
- `cargo test -p ironhermes-core --lib config_extras 2>&1 | tail -10` — six tests pass.
- `cargo test -p ironhermes-core --lib extras_canary 2>&1 | tail -10` — four canary tests pass.
- `cargo test -p ironhermes-core --lib 2>&1 | tail -5` — pass count ≥ pre-task baseline.
- `cargo build -p ironhermes-core 2>&1 | tail -5` — clean build.
</verification>

<success_criteria>
1. `crates/ironhermes-core/src/config_extras.rs` exists with five public types + `resolve_extras` function.
2. `crates/ironhermes-core/src/lib.rs` exports `pub mod config_extras;`.
3. `ProviderConfig` has `extra_request_options: Option<ProviderExtraOptions>` and `models: HashMap<String, ProviderModelConfig>`, both `#[serde(default)]`.
4. Plan 01's four canary tests PASS.
5. `resolve_extras` correctly implements per-model-wins shallow merge (D-03).
6. Backward compat: pre-36.15 configs still parse with default empty extras.
</success_criteria>

<output>
Create `.planning/phases/36.15-small-model-mode-smm-per-provider-extra-request-options-toml/36.15-02-SUMMARY.md` when done, documenting:
- Final shape chosen for `ProviderExtraOptions` (untagged enum success path OR D-01 fallback to `HashMap<String, serde_json::Value>` with reasoning)
- Test count: six `config_extras` unit + four `extras_canary` + one `backcompat` = 11 minimum.
- Files modified: `config.rs`, `config_extras.rs` (NEW), `lib.rs`.
- ADR if applicable: "Chose X over Y because the canary failed/passed at step Z."
</output>
