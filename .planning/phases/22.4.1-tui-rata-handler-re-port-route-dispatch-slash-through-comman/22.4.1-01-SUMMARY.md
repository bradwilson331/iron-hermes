---
phase: 22.4.1
plan: 01
subsystem: ironhermes-cli/tui_rata
tags: [tui-rata, command-router, dispatch-slash, handler-re-port, invariant, phase-22.4.1]
title: tui_rata — retire fast-paths + 4 invoke_handler arms + render_help_router + INV-29/-31 inversion + INV-34
requirements:
  - D-02
  - D-05
  - D-06
  - D-07
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15
dependency_graph:
  requires:
    - "Plan 22.4.1-00 — core registry promotes mouse/mcp/sessions/memory to ResolveResult::Exact"
    - "Phase 22.4 Plans 22.4-16/22.4-18 — fast-paths and helpers being retired"
    - "Phase 21.1 — CommandRouter::resolve three-stage resolution + commands_by_category"
  provides:
    - "Pure router-shell dispatch_slash + post-router App-side mouse hook (D-10/D-11/D-12)"
    - "invoke_handler signature widened to (name, _ctx, &CommandRouter) — locked for Plan 02 to extend"
    - "render_help_router(router, &Platform::Local) — auto-discovers new CommandDefs"
    - "INV-22.4-34 multi-name absence grep-lock for retired strip_prefix literals"
    - "Inverted INV-22.4-29 sub-(b) and INV-22.4-31 Strategy 2 + 2b — locked test ledger contract preserved"
    - "Unblocks Plan 22.4.1-02 — bulk match arms can extend the locked invoke_handler signature"
  affects:
    - "crates/ironhermes-cli/src/tui_rata/commands.rs (-126/+83 lines, net -43)"
    - "crates/ironhermes-cli/tests/invariants_22_4.rs (-31/+66 lines, net +35; 32 → 33 tests)"
tech_stack:
  added: []
  patterns:
    - "Pure router-shell dispatch (Phase 21.1 pattern) + post-router App-side hook (D-10 idiom — single deviation point for stateful commands)"
    - "def.name-interpolated args extraction `strip_prefix(&format!(\"/{}\", def.name))` — D-11 INV-34-safe"
    - "Router-driven /help via commands_by_category enumeration (lifted from classic-tui format_help)"
    - "[INVERTED] / [REPLACED] markers in INV error messages to signal locked-contract reversal"
key_files:
  created: []
  modified:
    - path: crates/ironhermes-cli/src/tui_rata/commands.rs
      lines_added: 83
      lines_removed: 126
      summary: Pure router-shell dispatch_slash + 4 new invoke_handler arms (mouse/mcp/sessions/memory) + render_help_router; deleted 4 fast-path blocks + 3 dead helpers (handle_mcp_slash/handle_sessions_slash/handle_memory_slash) + hand-built render_help
    - path: crates/ironhermes-cli/tests/invariants_22_4.rs
      lines_added: 66
      lines_removed: 31
      summary: INV-22.4-29 sub-(b) inverted; INV-22.4-31 Strategy 2 inverted; Strategy 2b replaced; sanity for /mouse removed; new INV-22.4-34 (multi-name strip_prefix absence grep-lock) appended
decisions:
  - "Edit ordering for Task 1: imports → dispatch_slash body → delete dead helpers → invoke_handler signature widening + 4 new arms → replace render_help — minimizes intermediate compile-broken states"
  - "render_help_router lifted body verbatim from classic-tui format_help (RESEARCH Finding 1 path), dropped extension+keybinding params (none in tui_rata)"
  - "Stub-text for the 4 new arms uses `Phase 22.4.1 stub:` marker (verifies Plan 01 ownership distinct from Plan 22.4 stubs)"
  - "Mouse arm has stub Output even though dispatch_slash hooks it pre-map_core — required so /help discovers the command via router-driven enumeration (D-13)"
  - "INV-22.4-31 Strategy 2b REPLACED rather than INVERTED — the prior fn-name needles are dead-code-deleted, no semantic equivalence to assert; the replacement asserts the new canonical home (invoke_handler arms)"
  - "INV-22.4-31 sanity for /mouse REMOVED rather than inverted — INV-34 already covers multi-name absence including /mouse; INV-29 sub-(b) is also inverted; double-coverage was noise"
  - "INV-22.4-34 scoped to LITERAL strings (per Pitfall 2) — NOT the generic `strip_prefix(\"/`, because the post-router args extraction uses `strip_prefix(&format!(\"/{}\", def.name))` non-literal interpolation"
metrics:
  duration_minutes: 6
  completed_date: 2026-04-25
  tasks_completed: 2
  files_changed: 2
  commits:
    - hash: eb19e56
      type: refactor
      summary: retire fast-paths + 4 invoke_handler arms + render_help_router
    - hash: 8d44743
      type: test
      summary: invert INV-22.4-29/-31 + add INV-22.4-34 absence grep-lock
---

# Phase 22.4.1 Plan 01: tui_rata Pure Router-Shell + INV Inversion Summary

Retired the four `strip_prefix("/mouse")`, `strip_prefix("/mcp")`, `strip_prefix("/sessions")`, `strip_prefix("/memory")` fast-paths in `tui_rata::dispatch_slash`, replaced the 22-line hand-built `render_help()` with a router-driven `render_help_router()`, deleted three dead helper fns (`handle_mcp_slash`/`handle_sessions_slash`/`handle_memory_slash`), inverted `INV-22.4-29` sub-(b) + `INV-22.4-31` Strategy 2 + 2b in place, and added `INV-22.4-34` (multi-name `strip_prefix` literal-absence grep-lock). All 33 invariants tests are green; snapshot suite passes with zero `.snap.new`.

## Net Diff

| File                                          | + lines | - lines | Net | Notes                                                                                |
| --------------------------------------------- | ------- | ------- | --- | ------------------------------------------------------------------------------------ |
| `crates/ironhermes-cli/src/tui_rata/commands.rs` | 83      | 126     | -43 | 4 fast-path blocks + 3 helper fns + hand-built render_help DELETED; 4 new invoke_handler arms + post-router hook + render_help_router ADDED |
| `crates/ironhermes-cli/tests/invariants_22_4.rs` | 66      | 31      | +35 | INV-22.4-29 sub-(b) inverted + INV-22.4-31 Strategy 2 inverted + 2b replaced + /mouse sanity removed; INV-22.4-34 (32 lines) appended |

**Total:** +149 lines added / -157 lines removed / **net -8 lines** across the plan — the plan ships net negative LOC because the 3 dead helpers and the hand-built render_help body collectively exceed the new arms + helper.

## Task 1 — Pure router-shell dispatch_slash (commit `eb19e56`)

`crates/ironhermes-cli/src/tui_rata/commands.rs` edits (six surgical edits per plan):

1. **Imports** — Added `CommandCategory` to the existing `ironhermes_core::commands::{...}` import (required by `render_help_router`).
2. **`dispatch_slash` body** — Replaced lines 56–131 with the locked CONTEXT D-02 shape: pure `app.command_router.resolve(input, &platform)` with a single post-router `if def.name == "mouse"` hook calling `handle_mouse_slash(app, args)`. Args extraction uses `input.strip_prefix(&format!("/{}", def.name)).unwrap_or("").trim()` (D-11 — `def.name`-interpolated, NOT literal `"/mouse"`).
3. **Dead helpers deleted** — `handle_mcp_slash`, `handle_sessions_slash`, `handle_memory_slash` (and their preceding doc comments + section banner) all removed.
4. **`invoke_handler` widened + 4 new arms** — Signature changed from `(name, _ctx)` to `(name, _ctx, router: &CommandRouter)`. Replaced `"help" => CommandResult::Output(render_help())` with `"help" => CommandResult::Output(render_help_router(router, &Platform::Local))`. Inserted four new arms (`mouse`/`mcp`/`sessions`/`memory`) BEFORE the preserved `agents` arm. Each carries a `Phase 22.4.1 stub:` marker.
5. **`render_help_router`** — Replaced 22-line hand-built `render_help()` with router-driven category enumeration body lifted from classic-tui `format_help` (RESEARCH Finding 1).
6. **Imports verification** — Confirmed `std::io`, `crossterm::execute`, `DisableMouseCapture`/`EnableMouseCapture`, `AtomicBool`/`Ordering`, `Arc` imports remain in use by the preserved `handle_mouse_slash` and `build_command_context`. No import removal needed.

**Preserved verbatim per D-06/D-07/D-10:**
- `handle_mouse_slash` (full body — call site moves from old fast-path to post-router hook).
- `"agents"` invoke_handler arm.
- `"skills"` invoke_handler arm.
- Generic `other => CommandResult::Output(format!("(tui_rata: /{other} not yet wired in Phase 22.4 — ..."))` fallback.

## Task 2 — INV inversion + INV-34 (commit `8d44743`)

`crates/ironhermes-cli/tests/invariants_22_4.rs` edits (five surgical edits per plan):

1. **INV-22.4-29 sub-(b) INVERTED** — `assert!(!TUI_RATA_COMMANDS.contains("input.strip_prefix(\"/mouse\")"), ...)` with `[INVERTED — Plan 22.4.1-01]` marker in the error message.
2. **INV-22.4-31 Strategy 2 INVERTED** — Loop now asserts `!TUI_RATA_COMMANDS.contains(&needle)` for `/mcp`, `/sessions`, `/memory` strip_prefix literals.
3. **INV-22.4-31 Strategy 2b REPLACED** — Old loop over `fn handle_mcp_slash` / `fn handle_sessions_slash` / `fn handle_memory_slash` REPLACED with new loop over `"mcp" => CommandResult::Output(` / `"sessions" => CommandResult::Output(` / `"memory" => CommandResult::Output(` (the new canonical home of the stub text).
4. **INV-22.4-31 sanity for /mouse REMOVED** — The 8-line redundant sanity assertion deleted; replaced with a 4-line explanatory comment pointing to INV-22.4-34 + INV-22.4-29.
5. **INV-22.4-34 APPENDED** — New test `invariant_22_4_34_dispatch_slash_no_strip_prefix` after `invariant_22_4_32_router_membership` (the Plan 00 addition). Iterates `["strip_prefix(\"/mouse\")", "strip_prefix(\"/mcp\")", "strip_prefix(\"/sessions\")", "strip_prefix(\"/memory\")"]` and asserts each appears ZERO times in `TUI_RATA_COMMANDS`.

**Preserved unchanged (per plan):**
- INV-22.4-29 sub-(a) — `EnableMouseCapture` in event_loop.
- INV-22.4-29 sub-(c) — `DisableMouseCapture` + `EnableMouseCapture` in commands.rs (still satisfied by preserved `handle_mouse_slash`).
- INV-22.4-29 sanity assertions — RAII guard + `mouse_capture_enabled` AtomicBool wiring.
- INV-22.4-31 Strategy 1 — `"agents"` and `"skills"` arm presence (D-06).
- INV-22.4-31 help-marker sanity loop — all five names still appear (`/agents`/`/skills` as preserved arms; `/mcp`/`/sessions`/`/memory` as new arm names; `render_help_router` enumerates all via category iteration).
- INV-22.4-31 generic-fallback sanity — `not yet wired in Phase 22.4` (D-07).

## Verification Evidence

```
cargo build -p ironhermes-cli                         → 0 (Finished dev profile in 23.94s)
cargo clippy -p ironhermes-cli --no-deps              → 0 (51 pre-existing warnings, 0 new from this plan)
cargo test  -p ironhermes-cli --test invariants_22_4  → 33 passed; 0 failed (32 from Plan 00 + INV-34)
cargo test  -p ironhermes-cli --test tui_rata_snapshots --features test-support → 8 passed; 0 failed
find crates/ironhermes-cli/tests/snapshots -name '*.snap.new' | wc -l → 0
```

Specific INV gates:

```
cargo test -p ironhermes-cli --test invariants_22_4 invariant_22_4_29_mouse_capture_paired_with_toggle -- --exact → 1 passed
cargo test -p ironhermes-cli --test invariants_22_4 invariant_22_4_31_handler_coverage_high_traffic    -- --exact → 1 passed
cargo test -p ironhermes-cli --test invariants_22_4 invariant_22_4_34_dispatch_slash_no_strip_prefix   -- --exact → 1 passed
```

Source-grep gates (RETIRED fast-paths must be ABSENT):

```
grep -c 'input.strip_prefix("/mouse")'    crates/ironhermes-cli/src/tui_rata/commands.rs → 0
grep -c 'input.strip_prefix("/mcp")'      crates/ironhermes-cli/src/tui_rata/commands.rs → 0
grep -c 'input.strip_prefix("/sessions")' crates/ironhermes-cli/src/tui_rata/commands.rs → 0
grep -c 'input.strip_prefix("/memory")'   crates/ironhermes-cli/src/tui_rata/commands.rs → 0
```

Source-grep gates (NEW arms + helper PRESENT):

```
grep -c '"mouse" => CommandResult::Output('     → 1
grep -c '"mcp" => CommandResult::Output('       → 1
grep -c '"sessions" => CommandResult::Output('  → 1
grep -c '"memory" => CommandResult::Output('    → 1
grep -c 'fn render_help_router'                 → 1
grep -c 'render_help_router(router, &Platform::Local)' → 1
grep -c 'if def.name == "mouse"'                → 1
grep -c 'strip_prefix(&format!("/{}", def.name))' → 1
grep -c 'Phase 22.4.1 stub:'                    → 4 (one per Plan-01 arm)
```

Source-grep gates (D-06/D-07 PRESERVED, dead helpers DELETED):

```
grep -c '"agents" => CommandResult::Output('    → 1
grep -c '"skills" => CommandResult::Output('    → 1
grep -c 'not yet wired in Phase 22.4'           → 1
grep -c 'fn handle_mouse_slash'                 → 1
grep -c 'fn handle_mcp_slash'                   → 0
grep -c 'fn handle_sessions_slash'              → 0
grep -c 'fn handle_memory_slash'                → 0
```

## Snapshot Delta

Zero `.snap.new` produced (RESEARCH Finding 3 prediction holds): no snapshot frame in the tui_rata test suite renders `/help` output, so the format change from hand-built `render_help()` to router-driven `render_help_router()` produces no observable byte-diff in any tracked snapshot. No `cargo insta accept` invocation required; D-15 path remains unused.

## Behavior Neutrality

User-visible behavior for the four routed commands:

| Command           | Pre-22.4.1 path (Plans 22.4-16/-18) | Post-22.4.1 Plan 01 path                                                                                          | UX delta |
| ----------------- | ----------------------------------- | ---------------------------------------------------------------------------------------------------------------- | -------- |
| `/mouse on\|off`  | strip_prefix fast-path → `handle_mouse_slash`            | router→`invoke_handler`→stub Output (unused for /mouse) BUT post-router hook on `def.name == "mouse"` → `handle_mouse_slash(app, args)` with `def.name`-interpolated args | Identical — capture toggle still fires, AtomicBool still flips, same `SlashOutcome::Handled(...)` text |
| `/mcp`            | strip_prefix fast-path → `handle_mcp_slash` (deleted)    | router→`invoke_handler`→`"mcp" => CommandResult::Output("/mcp — MCP server list and status...")`              | Stub text reworded (Plan 22.4 stub vs Plan 22.4.1 stub) but still discoverable + descriptive |
| `/sessions`       | strip_prefix fast-path → `handle_sessions_slash` (deleted) | router→`invoke_handler`→`"sessions" => CommandResult::Output("/sessions — List recent sessions...")`        | Same as above |
| `/memory`         | strip_prefix fast-path → `handle_memory_slash` (deleted) | router→`invoke_handler`→`"memory" => CommandResult::Output("/memory — Memory provider status...")`            | Same as above |
| `/help`           | hand-built 22-line string                                | router→`invoke_handler`→`render_help_router(router, &Platform::Local)` — full category enumeration            | Output now ROUTER-DRIVEN — auto-discovers new CommandDefs without per-call-site maintenance (D-13) |
| `/agents`/`/skills` | router→`invoke_handler` arms (verbatim)                  | router→`invoke_handler` arms (verbatim, BYTE-IDENTICAL)                                                       | None (D-06) |

## Plan Sequencing Hand-off

**Plan 22.4.1-02 is unblocked.** The locked-in contracts:

- **`invoke_handler` signature** — `async fn invoke_handler(name: &str, _ctx: &CommandContext, router: &CommandRouter) -> Result<CommandResult, anyhow::Error>` — Plan 02's bulk match arms add new branches to this exact signature; `_ctx` and `router` are already plumbed.
- **File ordering convention** — universal arms (quit/exit/clear/new/help/reset/reload-mcp) → mouse/mcp/sessions/memory (Plan 01) → agents (verbatim, D-06) → skills (verbatim, D-06) → generic fallback (verbatim, D-07). Plan 02's bulk Session+Configuration arms extend BEFORE the agents arm preserved-verbatim region.
- **Stub-text marker convention** — `Phase 22.4.1 stub:` for arms wired in this plan; Plan 02 should use the same marker for newly-wired arms or graduate to real handlers.
- **Post-router hook precedent** — `if def.name == "mouse"` is the ONLY stateful command in tui_rata. Plan 02 is expected to be pure-router-only (no new App-state hooks); if Plan 02 discovers another stateful command, the locked pattern is `if def.name == "<X>"` post-router with `def.name`-interpolated args extraction (NOT a literal `"/X"`) so INV-34 stays at zero hits.

## Deviations from Plan

None - plan executed exactly as written. No Rule 1–4 deviations triggered.

## Deferred Issues (Pre-existing, Out of Scope)

- 21 pre-existing build warnings in `ironhermes-cli` (unused functions in tui/render.rs, sqlite/duckdb providers, etc.) carried over from Plan 22.4.1-00 SUMMARY's deferred-items log. Not introduced or affected by this plan.
- 51 total clippy warnings (with 6 duplicates) in `ironhermes-cli` — also pre-existing. Zero new clippy warnings introduced by this plan's edits. Verified via grep over clippy output that none reference `handle_mcp_slash` / `handle_sessions_slash` / `handle_memory_slash` / `render_help` / `render_help_router` / `tui_rata/commands.rs` patterns.

## TDD Gate Compliance

The plan tasks were marked `tdd="true"` but the plan-level `type: execute` (not `type: tdd`) governs the gate-enforcement scope. Both tasks follow a fused-gate pattern matching Plan 22.4.1-00's pattern:

- **Task 1 (`refactor` commit)** — Refactors dispatch_slash + retires fast-paths + adds new arms + replaces render_help. Tests in `invariants_22_4.rs` would FAIL after Task 1 alone (Strategy 2 + 2b assertions still expect the deleted fast-paths and helpers).
- **Task 2 (`test` commit)** — Inverts the failing assertions and adds INV-34 to lock the new contract. The combined sequence `Task 1 → Task 2` is the GREEN phase — neither commit individually is green, but the plan as a whole lands green.

Standard TDD ordering would expect `test` before `refactor`, but here the test inversion is a regression-lock against the source change (semantically backstop, not behavior driver). Documenting per gate-compliance protocol — same precedent as Plan 22.4.1-00.

## Self-Check: PASSED

- File `crates/ironhermes-cli/src/tui_rata/commands.rs` exists ✓ (modified, -126/+83 lines)
- File `crates/ironhermes-cli/tests/invariants_22_4.rs` exists ✓ (modified, -31/+66 lines)
- File `.planning/phases/22.4.1-tui-rata-handler-re-port-route-dispatch-slash-through-comman/22.4.1-01-SUMMARY.md` exists ✓ (this file)
- Commit `eb19e56` exists ✓ (refactor: retire fast-paths + 4 invoke_handler arms + render_help_router)
- Commit `8d44743` exists ✓ (test: invert INV-22.4-29/-31 + add INV-22.4-34 absence grep-lock)
