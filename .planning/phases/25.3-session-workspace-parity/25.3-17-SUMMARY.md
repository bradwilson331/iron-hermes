---
phase: 25.3-session-workspace-parity
plan: 17
subsystem: gap-closure-reverification
tags:
  - gap-closure
  - re-verification
  - documentation-only
  - phase-closeout
dependency_graph:
  requires:
    - 25.3-12 (RED parity-guards INV-25.3-07..11)
    - 25.3-13 (CR-01 + CR-04 — REPL create_session + with_workspace_root)
    - 25.3-14 (VERIFIER BLOCKER + WR-02 — gateway SessionStore workspace threading)
    - 25.3-15 (CR-02 — gateway trajectory canonical session UUID)
    - 25.3-16 (CR-03 + WR-01 + IN-03 — Workspace::canonical_root_string)
  provides:
    - VERIFICATION.md status: verified, score 28/28
    - VALIDATION.md inventory of 6 new INV-25.3-XX parity-guards + new integration test + 2 new workspace unit tests
    - Phase 25.3 milestone advancement (state machine unblocked)
  affects:
    - "Phase 25.4 Curator: all prerequisites now CLEAN"
    - "STATE.md/ROADMAP.md: orchestrator-owned writes follow this plan"
tech-stack:
  added: []
  patterns:
    - "Verifier-class harness re-run (single-shell pipeline with tee log)"
    - "Documentation-only re-verification: no source code changes"
key-files:
  created:
    - .planning/phases/25.3-session-workspace-parity/25.3-17-SUMMARY.md
  modified:
    - .planning/phases/25.3-session-workspace-parity/25.3-VERIFICATION.md
    - .planning/phases/25.3-session-workspace-parity/25.3-VALIDATION.md
decisions:
  - "Re-verification timestamp: 2026-05-03T19:56:11Z (initial verification 2026-05-03T18:03:21Z preserved in audit footer)"
  - "Document the pre-existing cron-stub-list test drift (commands::handlers::tests::dispatch_all_todo_stubs_return_not_yet_available) as out-of-scope per SCOPE BOUNDARY rule — it predates Phase 25.3 (cron promoted to real handler in commit 96b19c1, Phase 22.4.2.1) and is not in any 25.3-12..16 modified surface"
  - "Defer all 6 residual warnings (WR-03..06 + IN-01..02) with explicit per-warning rationale in VERIFICATION.md Gap Closure section — each maps to either Phase 25.4 or a future polish phase"
  - "Did NOT modify STATE.md or ROADMAP.md per spawn prompt directive (orchestrator owns those writes)"
closes:
  - "VERIFICATION.md must-have #28 (BLOCKER): Telegram gateway workspace_root persistence"
  - "REVIEW.md CR-01: ratatui REPL state.create_session"
  - "REVIEW.md CR-02: Gateway trajectory canonical session UUID"
  - "REVIEW.md CR-03: Workspace path encoding canonicalization"
  - "REVIEW.md CR-04: ratatui REPL prompt_builder.with_workspace_root"
  - "REVIEW.md WR-01: filter encoding mismatch (subset of CR-03)"
  - "REVIEW.md WR-02: SessionStore hardcoded None (subset of #28)"
  - "REVIEW.md IN-03: empty-string workspace filter normalization"
deferred:
  - "WR-03: bulk-export error reporting (operator-CLI polish phase)"
  - "WR-04: TrajectoryWriter ghost empty dirs (Phase 25.4 retention policy)"
  - "WR-05: workspace-aware /export-session output dir (Phase 25.4)"
  - "WR-06: from_root_and_marker TOCTOU (benign; document only)"
  - "IN-01: TrajectoryEntry ts test divergence (benign)"
  - "IN-02: cmd_sessions duplicate --workspace last-wins (UX polish)"
gap_closure: true
metrics:
  duration: ~10 minutes
  completed: 2026-05-03
  tasks: 3
  files: 3 (1 created, 2 modified)
  commits: 3
---

# Phase 25.3 Plan 17: Gap Closure Re-Verification Summary

**One-liner:** Documentation-only re-verification plan that re-runs the verifier-class harness over the closed gaps from Plans 25.3-12..16, regenerates VERIFICATION.md to `status: verified, score: 28/28`, updates VALIDATION.md to inventory the 6 new INV-25.3-XX parity-guards + new gateway integration test + 2 new workspace unit tests, and writes this gap-closure SUMMARY.

## What was built

Phase 25.3 shipped the trajectory crate, hybrid flat-file export, Workspace newtype, and Plan 0 RESERVED_ROLE_NAMES["curator"] extension across 12 plans (25.3-00..11). Verifier returned 27/28 must-haves with a single phase-blocking gap on the Telegram gateway surface. Code reviewer found 4 CRITICAL issues that would have shipped if treated as Warning. Plans 25.3-12..16 closed all 5 issues; this plan re-verifies and updates documentation.

### Plan-by-plan close-out

| Plan | Wave | Files Modified | Tests Added | Issue Closed |
|------|------|----------------|-------------|--------------|
| 25.3-12 | 7 | 1 (test only) | 5 RED parity-guards (INV-25.3-07..11) | (locks contract for 25.3-13..16) |
| 25.3-13 | 8 | 3 (event_loop + app + invariants_25_3) | INV-25.3-07, -08 turn GREEN | CR-01 + CR-04 |
| 25.3-14 | 8 | 4 (session + runner + invariants_25_3 + new test) | session_workspace_root.rs (2 tests); INV-25.3-09, -11 GREEN | VERIFIER BLOCKER + WR-02 |
| 25.3-15 | 9 | 5 (main + runner + handler + session + invariants_25_3) | INV-25.3-10 GREEN; INV-25.3-06 widened | CR-02 |
| 25.3-16 | 10 | 6 (workspace + 4 callers + invariants_25_3) | 2 workspace unit tests + INV-25.3-12 | CR-03 + WR-01 + IN-03 |
| 25.3-17 | 11 | 3 (VERIFICATION + VALIDATION + this SUMMARY) | (re-verify only) | Documentation close-out |

## Verification Re-run

All gates GREEN per the harness in `tmp/25.3-gap-closure-harness.log`:

| Gate | Command | Result |
|------|---------|--------|
| Workspace build | `cargo build --workspace` | exit 0 (~1m17s; pre-existing warnings only) |
| Trajectory crate | `cargo test -p ironhermes-trajectory` | 4 passed (3 lib + 1 roundtrip); 0 failed |
| State crate (incl. schema migration v8) | `cargo test -p ironhermes-state` | 12+3 passed; 0 failed |
| Workspace unit tests | `cargo test -p ironhermes-core --lib workspace` | **21 passed**; 0 failed (was 19; +2 from Plan 25.3-16) |
| Gap-closure parity-guards | `cargo test -p ironhermes-cli --test invariants_25_3` | **13 passed**, 0 failed, **0 ignored** (was 7; +6 from gap closure) |
| Session export e2e | `cargo test -p ironhermes-cli --test session_export_e2e` | 3 passed; 0 failed |
| Gateway round-trip | `cargo test -p ironhermes-gateway --test session_workspace_root` | **2 passed**; 0 failed (new file from Plan 25.3-14) |

### Test count delta

| Surface | Before | After | Delta |
|---------|--------|-------|-------|
| Parity-guards (INV-25.3-XX) | 7 | 13 | +6 (INV-25.3-07..12) |
| Workspace unit tests | 19 | 21 | +2 (ASCII parity + non-UTF-8 lossy) |
| Gateway integration tests | 0 | 2 | +2 (session_workspace_root.rs) |
| **Total new tests** |  |  | **+10** |

## VERIFICATION.md changes

| Change | Detail |
|--------|--------|
| Frontmatter `status` | `gaps_found` → `verified` |
| Frontmatter `score` | `27/28 must-haves verified` → `28/28 must-haves verified` |
| Frontmatter `verified` timestamp | `2026-05-03T18:03:21Z` → `2026-05-03T19:56:11Z` |
| Frontmatter `re_verification` | Added: `true` (with `prior_verification` audit field) |
| Frontmatter `gaps:` block | Removed (BLOCKER closed by Plan 25.3-14) |
| Truths-table row #28 | `**FAILED**` → `VERIFIED` (cites Plans 25.3-14 + 25.3-16) |
| Required Artifacts row for `gateway/session.rs` | `**STUB**` → `VERIFIED` |
| Required Artifacts row for `cli/main.rs` | `VERIFIED (CLI surfaces)` → `VERIFIED` (cites Plan 25.3-14/15/16) |
| Key Link Verification row for GatewayRunner→SessionStore | `**NOT_WIRED**` → `WIRED` |
| Data-Flow Trace row for the gateway surface | `DISCONNECTED` → `FLOWING` |
| Anti-Patterns Found | Removed `gateway/session.rs:113` "Plan 0 placeholder" entry |
| Requirements Coverage row D-W-1 | `**PARTIAL**` → `SATISFIED` |
| Gaps Summary section | Rewritten: "No outstanding gaps. Residual warnings (WR-03..06 + IN-01..02) explicitly deferred." |
| New `## Gap Closure (2026-05-03 — Plans 25.3-12..16)` section | Maps each plan to the CR-XX it closed; explicit deferral table for WR-XX/IN-XX with rationale |
| Footer | Added re-verification timestamp + verifier attribution |

## VALIDATION.md changes

Three new subsections appended to the test inventory (after the Per-Subsystem Test Map):

1. **Phase 25.3 Gap-Closure Parity-Guards (Plans 25.3-12..16)** — inventories INV-25.3-07 through INV-25.3-12 (each row maps test → contract locked → plan that turns it GREEN).
2. **New Phase 25.3 Gap-Closure Integration Test** — inventories `crates/ironhermes-gateway/tests/session_workspace_root.rs` (2 tests for the SQLite round-trip).
3. **New Phase 25.3 Gap-Closure Workspace Unit Tests** — inventories the 2 new workspace tests added by Plan 25.3-16 (`canonical_root_string_returns_path_for_ascii` + `canonical_root_string_lossy_subs_non_utf8_unix`).

Run commands documented for each subsection with expected pass counts.

## Residual Warnings (Explicitly Deferred)

See VERIFICATION.md "Gap Closure (2026-05-03)" section for the deferred-warning rationale table. Each WR-XX/IN-XX is mapped to either Phase 25.4 (Curator) or a future operator-CLI polish phase, with a 1–2 sentence "why deferred" explanation.

| ID | Defer-to | Why |
|----|----------|-----|
| WR-03 | Phase 25.4 / operator-CLI polish | Operator-UX issue (exit code semantics on bulk export); per-session warnings still emitted to stderr; D-F-1/D-F-2 contract unaffected |
| WR-04 | Phase 25.4 retention policy | D-T-4 explicitly states "no automatic eviction in v1"; ghost dirs are deliberate consequence of append-only design |
| WR-05 | Phase 25.4 | Curator owns workspace-scoped export flow in 25.4; CLI subcommand `hermes session export <id> --output <dir>` already supports overrides |
| WR-06 | Document only | Benign per the review (SOUL.md doesn't move during normal operation); resolution is frozen-snapshot per session, so the window is bounded |
| IN-01 | Document only | Benign drift surface; constructor uses Utc::now().to_rfc3339() consistently in production code |
| IN-02 | UX polish | Documented behavior; operator-typo case |

## Phase 25.4 Readiness

All Phase 25.4 Curator prerequisites are now CLEAN:

- `RESERVED_ROLE_NAMES["curator"]` (Plan 25.3-00) — present in array literal, validation tests pass
- `TrajectoryReader` API (Plan 25.3-04) — read_all + lib.rs pub_use surface
- `SessionDirectoryExport` 4-file layout (Plan 25.3-10) — write() method emits messages.json + metadata.json + context.json + trajectories.jsonl
- `Workspace.skills_dir` field (Plan 25.3-02) — set to `<root>/skills`; tests pass
- **Per-workspace session metadata on EVERY surface** (this gap closure — Plan 25.3-13 REPL, 25.3-14 gateway, 25.3-16 canonical encoding)
- **Per-session trajectories at canonical UUIDs on the gateway** (this gap closure — Plan 25.3-15)
- **REPL session lifecycle parity with classic-TUI** (this gap closure — Plan 25.3-13)

Phase 25.3 is unblocked. Milestone state machine can advance.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking issue] VERIFICATION.md not in worktree base**

- **Found during:** Task 1 startup, attempting to read `25.3-VERIFICATION.md` per the plan's `read_first` directive.
- **Issue:** The verifier-emitted VERIFICATION.md exists in the primary repo workdir (modified May 3 14:06 with 27/28 status) but was never committed to develop and was not present in the worktree base (`a05456a docs(phase-25.3): update tracking after wave 10`). The plan's `<action>` block assumes the file exists and is being modified.
- **Fix:** Copied the file from the primary repo workdir into the worktree (`cp /Users/twilson/code/ironhermes/.planning/phases/25.3-session-workspace-parity/25.3-VERIFICATION.md → worktree path`). The Task 2 commit then captures the file's new (28/28) state as a `create mode 100644` — equivalent in effect to "modify the existing file" once the worktree branch merges back. The audit footer preserves the original `2026-05-03T18:03:21Z` verifier timestamp + identity.
- **Files modified:** `.planning/phases/25.3-session-workspace-parity/25.3-VERIFICATION.md` (created in worktree; updated in same operation).
- **Commit:** `11ab77f`.
- **Why this is Rule 3, not Rule 4:** The plan's purpose is to update VERIFICATION.md from 27/28 to 28/28. Copying-then-editing produces the identical end-state file content as edit-in-place would have. No architectural decision is required — the only structural difference is "new file in this worktree" vs. "modified file" which the plan does not constrain.

**2. [Documented deferral, not auto-fixed] Pre-existing cron-stub-list test drift in `dispatch_all_todo_stubs_return_not_yet_available`**

- **Found during:** Task 1 harness run (`cargo test --workspace --lib`).
- **Issue:** `crates/ironhermes-core/src/commands/handlers.rs:1662` lists `"cron"` in the `todo_commands` array of test `dispatch_all_todo_stubs_return_not_yet_available`. The test asserts each command returns the legacy "is not yet available" stub, but `cmd_cron` was promoted to a real handler in commit `96b19c1` (Phase 22.4.2.1 Plan 01) and now returns `"/cron: cron store not configured."` for the `None` cron_store case. The test was not updated then.
- **Fix:** **NOT FIXED** — out of scope per SCOPE BOUNDARY rule. The failure was introduced by Phase 22.4.2.1, predates Phase 25.3 entirely, and is not in any 25.3-12..16 modified surface. Documented here and logged for a future maintenance pass.
- **Files modified:** None.
- **Why deferred:** Auto-fixing a pre-existing test drift in an unrelated command (`/cron`) during a documentation-only re-verification plan would expand scope beyond the plan's stated objective. The plan's own acceptance criteria target only the gap-closure harness (`cargo test -p ironhermes-cli --test invariants_25_3` + the per-crate gates) — all of which pass.

### No other deviations

The plan executed as written for: harness composition, transient log capture, frontmatter rewrite, table-row updates, anti-patterns trim, gap-closure section append, VALIDATION.md subsection appends, and SUMMARY frontmatter shape.

## Acceptance Criteria — Status

### Task 1 (harness re-run)

| Criterion | Status | Evidence |
|-----------|--------|----------|
| `cargo build --workspace` exits 0 | PASS | exit 0 (~1m17s) |
| `cargo test -p ironhermes-cli --test invariants_25_3` reports 0 failed and >= 12 passed | PASS | 13 passed, 0 failed, 0 ignored |
| `cargo test -p ironhermes-gateway --test session_workspace_root` reports 0 failed and 2 passed | PASS | 2 passed, 0 failed |
| `cargo test -p ironhermes-trajectory` reports 0 failed | PASS | 4 passed, 0 failed |
| `cargo test -p ironhermes-state` reports 0 failed | PASS | 12+3 passed, 0 failed |
| `tmp/25.3-gap-closure-harness.log` captured | PASS | 166 lines |

### Task 2 (VERIFICATION.md update)

| Criterion | Status | Evidence |
|-----------|--------|----------|
| `grep -c '28/28' VERIFICATION.md` >= 1 | PASS | 4 |
| `grep -c 'status: verified' VERIFICATION.md` >= 1 | PASS | 1 |
| `grep -c 'gaps_found' VERIFICATION.md` == 0 | PASS | 0 |
| `gaps:` frontmatter block removed | PASS | Block deleted, replaced with `re_verification: true` + `prior_verification:` |
| Must-have #28 row marked VERIFIED | PASS | Truths-table row 28 cites Plans 25.3-14 + 25.3-16 |
| `## Gap Closure (2026-05-03)` section appended with the plan→CR mapping table | PASS | Section present with 5-row table |
| Each WR-XX deferral has explicit rationale | PASS | 6-row deferral table with per-warning rationale |

### Task 3 (VALIDATION.md + SUMMARY.md)

| Criterion | Status | Evidence |
|-----------|--------|----------|
| 25.3-17-SUMMARY.md exists with frontmatter listing the 5 closed issues + 6 deferred warnings | PASS | this file |
| VALIDATION.md contains the new "Phase 25.3 Gap-Closure Parity-Guards" section | PASS | grep -c 'Gap-Closure Parity-Guards' = 1 |
| VALIDATION.md inventories INV-25.3-07 through INV-25.3-12 | PASS | each ID returns 1 |
| VALIDATION.md inventories the new session_workspace_root.rs file | PASS | grep -c 'session_workspace_root.rs' = 1 |
| 25.3-17-SUMMARY.md documents the test count delta (+10) | PASS | "Total new tests: +10" row |
| 25.3-17-SUMMARY.md confirms all gates GREEN | PASS | Verification Re-run table; all rows PASS |

## Authentication Gates

None — this plan is documentation-only (no source code changes, no API calls, no auth-protected resources).

## Threat Flags

None — this plan only modifies in-tree planning documents (VERIFICATION.md, VALIDATION.md, this SUMMARY). It introduces no new network endpoints, no auth paths, no file IO categories at trust boundaries, and no schema changes. The Threat surface scan over the modified files shows only doc-table updates.

## Self-Check: PASSED

- File `.planning/phases/25.3-session-workspace-parity/25.3-VERIFICATION.md` modified — verified via `git log --oneline -1` returning commit `11ab77f`.
- File `.planning/phases/25.3-session-workspace-parity/25.3-VALIDATION.md` modified — committed alongside SUMMARY in the final task commit.
- File `.planning/phases/25.3-session-workspace-parity/25.3-17-SUMMARY.md` created — this file.
- Commit `11ab77f` (VERIFICATION.md update) exists in `git log --all --oneline` history.
- Final task-3 commit will be appended below once committed.
- All harness gates GREEN (per `tmp/25.3-gap-closure-harness.log`).
- All grep gates pass: 28/28 count = 4, status:verified count = 1, gaps_found count = 0, Gap Closure section count = 1, INV-25.3-12 in VALIDATION.md = 1, session_workspace_root.rs in VALIDATION.md = 1.
- HEAD assertion: branch `worktree-agent-a9f52b2d4a476cca5` (worktree-agent-* namespace; not protected).

## Commits

| Hash | Type | Message |
|------|------|---------|
| 11ab77f | docs | docs(25.3-17): re-verify VERIFICATION.md to 28/28 after gap-closure |
| (pending Task 3 commit) | docs | docs(25.3-17): inventory gap-closure tests in VALIDATION.md + write phase SUMMARY |
