---
phase: 25.3-session-workspace-parity
verified: 2026-05-03T19:56:11Z
status: verified
score: 28/28 must-haves verified
overrides_applied: 0
re_verification: true
prior_verification: 2026-05-03T18:03:21Z
human_verification:
  - test: "Visual: launch `hermes chat` in a workspace-rooted directory and confirm `[Workspace: <root>]` appears in the system prompt (cache-stable, frozen at session start) — Manual-Only per VALIDATION.md (cache hit rate verification requires live Anthropic API)."
    expected: "[Workspace: <root>] line present in Identity slot of system prompt; cache_read_input_tokens > cache_creation_input_tokens after 5+ turns."
    why_human: "Requires live Anthropic API and multi-turn session; cache stats cannot be mocked deterministically."
  - test: "Bulk export: create 3+ sessions across multiple dates, run `hermes session export-all --since YYYY-MM-DD`, verify directory layout under ~/.ironhermes/sessions/<id>/ for each matching session."
    expected: "Directory count matches the --since filter; each <id>/ contains messages.json + metadata.json + context.json + (trajectories.jsonl if present)."
    why_human: "VALIDATION.md flags this as Manual-Only — integration test would need to seed thousands of session rows."
  - test: "Workspace prompt absence: from a directory with no .hermes/.ironhermes ancestor, run `hermes chat --debug` and confirm no [Workspace:] line appears."
    expected: "No '[Workspace:' line in the printed system prompt when cwd has no workspace marker; the global ~/.ironhermes/ fallback does not synthesize a Workspace prompt line."
    why_human: "Verifying absence in global mode requires running outside any workspace marker — context-dependent."
---

# Phase 25.3: session-workspace parity Verification Report

**Phase Goal:** Achieve session/workspace parity — wire `Workspace` newtype + `TrajectoryWriter` through every CommandContext construction site (cli main.rs, ratatui REPL, Telegram gateway), record one TrajectoryEntry per tool call (with redact_args() applied to sensitive args), persist `workspace_root` on the sessions table, and expose `hermes session export[-all]` + `/export-session` slash to operators. Phase 25.4 Curator must be unblocked (curator role in RESERVED_ROLE_NAMES, TrajectoryReader API, SessionDirectoryExport).

**Verified:** 2026-05-03T19:56:11Z
**Status:** verified
**Re-verification:** Yes — gap-closure re-verification after Plans 25.3-12..16 (initial verification 2026-05-03T18:03:21Z)

## Goal Achievement

### Observable Truths (derived from CONTEXT-locked decisions D-T-1..4, D-F-1, D-F-2, D-W-1, D-W-2, D-P0-1)

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | RESERVED_ROLE_NAMES extended 6→7 with "curator" (D-P0-1) | VERIFIED | `crates/ironhermes-core/src/config.rs:54-63` array literal; test `reserved_role_names_contains_all_seven_roles_with_curator` passes; `validate_role_name_accepts_all_reserved_roles` passes |
| 2 | Schema migration v8 adds workspace_root TEXT NULL (D-W-1) | VERIFIED | `crates/ironhermes-state/src/lib.rs:46 SCHEMA_VERSION=8`, line 363 `ALTER TABLE sessions ADD COLUMN workspace_root TEXT`; tests `schema_version_constant_is_8`, `fresh_install_has_workspace_root_column`, `v7_db_upgrades_to_v8_preserving_rows` all pass |
| 3 | Session struct has pub workspace_root: Option<String> (D-W-1) | VERIFIED | `crates/ironhermes-state/src/lib.rs:140` |
| 4 | StateStore::create_session accepts workspace_root parameter (D-W-1) | VERIFIED | `crates/ironhermes-state/src/lib.rs:385` signature; `create_session_with_workspace_round_trips` test passes |
| 5 | ironhermes-trajectory crate exists, builds, is workspace member (D-T-2) | VERIFIED | `Cargo.toml workspace.members` contains `crates/ironhermes-trajectory`; `cargo build --workspace` clean; `cargo test -p ironhermes-trajectory` passes 18/18 |
| 6 | TrajectoryEntry serializes with all 8 D-T-1 fields (name, args, result_or_error, duration_ms, impact_level, turn_index, tool_call_id, ts) | VERIFIED | `crates/ironhermes-trajectory/src/format.rs:51-72`; golden test `entry_roundtrips` passes |
| 7 | ImpactLevel enum (Read=0, Write=5, SystemChange=10) serializes as snake_case (D-T-1) | VERIFIED | `format.rs:28-44`; `impact_level_serializes_as_snake_case` test passes |
| 8 | TrajectoryWriter fsync per line + Drop sync_data (D-T-2, D-T-4) | VERIFIED | `writer.rs:62 self.file.sync_data()`, `writer.rs:94-98 impl Drop`; tests `append_persists_to_disk_after_each_call`, `drop_flushes_final_entry` pass |
| 9 | TrajectoryReader minimal API for Phase 25.4 consumption (D-T-2) | VERIFIED | `reader.rs:18 pub struct TrajectoryReader`, `read_all()`; 5/5 reader tests pass |
| 10 | Workspace newtype with 6 fields (root, soul_path, agents_chain, memory_dir, skills_dir, tools_config) (D-W-1) | VERIFIED | `crates/ironhermes-core/src/workspace.rs:28-37`; 19/19 workspace tests pass |
| 11 | resolve_from_cwd walks up looking for .hermes/.ironhermes markers (D-W-1) | VERIFIED | `workspace.rs:85`; tests `resolve_walks_up_multiple_parents`, `walk_up_terminates_at_filesystem_root` pass |
| 12 | Workspace.skills_dir exposed for Phase 25.4 Curator emission (D-W-2 forward-ref) | VERIFIED | `workspace.rs:35 pub skills_dir: PathBuf`; line 59 sets it to `<root>/skills`; test `skills_dir_is_top_level_not_under_marker` passes |
| 13 | Tool::redact_args trait default returns raw clone (Discretion D-2) | VERIFIED | `crates/ironhermes-tools/src/registry.rs:66`; `tool_redact_args_default_returns_input_verbatim` test passes |
| 14 | WebExtractTool::redact_args overrides default + applies redact_secrets_in_url (Discretion D-2) | VERIFIED | `crates/ironhermes-tools/src/web_extract.rs:123`; 4 dedicated tests (`web_extract_redact_args_redacts_url_secrets`, etc.) pass |
| 15 | CommandContext gains workspace + trajectory_writer fields with builder methods (D-W-2, D-T-3) | VERIFIED | `crates/ironhermes-core/src/commands/context.rs:334-342, 504, 516`; init to None at 377-379; with_workspace + with_trajectory_writer tests pass |
| 16 | TrajectoryWriterHandle trait defined in ironhermes-core for cycle-break (D-T-3) | VERIFIED | `context.rs:150 pub trait TrajectoryWriterHandle: Send + Sync` with `append_json_line` |
| 17 | ironhermes-core remains a strict leaf crate (no path deps on other ironhermes-* crates) | VERIFIED | `crates/ironhermes-core/Cargo.toml` has no `ironhermes-*` deps |
| 18 | TrajectoryWriterHandleImpl in ironhermes-trajectory wraps Arc<Mutex<TrajectoryWriter>> (D-T-3) | VERIFIED | `crates/ironhermes-trajectory/src/handle.rs:17`; trajectory crate depends on core (correct cycle-break direction) |
| 19 | PromptBuilder::with_workspace_root appends [Workspace: <root>] to Identity slot (D-W-2) | VERIFIED | `crates/ironhermes-agent/src/prompt_builder.rs:204`; 4 dedicated tests pass (idempotent, lands in Identity slot, appends to default + SOUL) |
| 20 | All 4 CommandContext-construction sites attach .with_workspace + .with_trajectory_writer (load-bearing parity) | VERIFIED | main.rs:1048, 1055; tui_rata/commands.rs:593, 597; gateway/handler.rs:319, 325; parity-guard test file `invariants_25_3.rs` 7/7 GREEN with all `#[ignore]` removed |
| 21 | GatewayRunner exposes set_workspace + set_trajectory_writer setters (D-W-2, D-T-3) | VERIFIED | `crates/ironhermes-gateway/src/runner.rs:129, 137`; build_gateway_handler at 281, 286 clones into handler |
| 22 | AgentLoop calls trajectory.append_json_line after every tool execution + redacts args via tool.redact_args (D-T-1, D-T-3) | VERIFIED | `crates/ironhermes-agent/src/agent_loop.rs:1242-1331`; `with_trajectory_writer` builder at 272; `classify_impact_level` classifier at 1383; tests `classify_impact_level_known_categories`, `execute_tool_call_without_trajectory_writer_is_a_noop_for_ledger` pass |
| 23 | TrajectoryWriter path is workspace-scoped (<workspace>/.ironhermes/sessions/<id>/...) when workspace exists, else ~/.ironhermes/ fallback | VERIFIED | main.rs:640-648, 1117-1126, 2316; tui_rata/event_loop.rs:315-323; gateway run_gateway path (main.rs:2316) |
| 24 | SessionDirectoryExport produces 4-file layout messages.json + metadata.json + context.json + trajectories.jsonl (D-F-1) | VERIFIED | `crates/ironhermes-state/src/session_export.rs:20-78`; e2e test `session_export_writes_4_file_layout` passes (3/3 e2e tests pass) |
| 25 | StateStore::list_sessions_filtered with workspace_root_filter param (D-W-2) | VERIFIED | `lib.rs:512-540`; backwards-compat: `list_sessions` delegates with None |
| 26 | /sessions --workspace slash command parses flag + dispatches to filtered method (D-W-2) | VERIFIED | `crates/ironhermes-core/src/commands/handlers.rs:419-466`; tests `cmd_sessions_workspace_explicit_path_filters`, `cmd_sessions_bare_workspace_uses_ctx_workspace`, `cmd_sessions_no_args_uses_unfiltered_path` pass |
| 27 | hermes session export <id> + export-all CLI subcommands wired (D-F-1, D-F-2) | VERIFIED | `cli_args.rs:101-127 SessionSubcommand::{Export, ExportAll}`; `session_cmd.rs:25-46 handle_session_command`; `main.rs:416-418 Commands::Session dispatch`; e2e tests pass 3/3 including `--since` filter |
| 28 | Resolved Workspace passed to StateStore::create_session on EVERY surface (D-W-1 — value persistence) | VERIFIED | Plan 25.3-14 closed the verifier blocker: `crates/ironhermes-gateway/src/session.rs::SessionStore` gained an `Option<Arc<Workspace>>` field + `set_workspace` setter; `GatewayRunner::set_workspace` propagates to the inner SessionStore; `get_or_create` reads `self.workspace` and threads `canonical_root_string` (Plan 25.3-16) into `state.create_session(...)`. Regression test `crates/ironhermes-gateway/tests/session_workspace_root.rs` (2/2 GREEN) proves the round-trip into SQLite. INV-25.3-09 + INV-25.3-11 GREEN. |

**Score:** 28/28 truths verified

### Required Artifacts (load-bearing files for the must-haves)

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `crates/ironhermes-trajectory/{Cargo.toml, src/lib.rs, src/format.rs, src/writer.rs, src/reader.rs, src/handle.rs}` | new crate, builds, exports TrajectoryEntry/ImpactLevel/Writer/Reader/HandleImpl | VERIFIED | All files exist; `cargo build` clean; 18/18 unit tests pass |
| `crates/ironhermes-trajectory/tests/{format.rs, roundtrip.rs}` | golden JSONL + roundtrip tests | VERIFIED | Both tests exist; integration tests pass |
| `crates/ironhermes-core/src/workspace.rs` | Workspace newtype + resolve_from_cwd | VERIFIED | 198 lines; 19/19 tests pass |
| `crates/ironhermes-core/src/commands/context.rs` (workspace + trajectory_writer fields, TrajectoryWriterHandle trait) | trait + 2 fields + 2 builders | VERIFIED | trait at line 150; fields at 334, 342; builders at 504, 516; tests pass |
| `crates/ironhermes-state/src/session_export.rs` | SessionDirectoryExport (4-file writer) | VERIFIED | pub struct + write() method writes 4 files; integration tests pass |
| `crates/ironhermes-state/src/lib.rs` (schema v8, list_sessions_filtered, Session.workspace_root) | migration v8 + filter + struct field | VERIFIED | 4/4 schema_migration_v8 tests pass |
| `crates/ironhermes-cli/tests/invariants_25_3.rs` (parity-guard) | 6 grep tests + 1 toolset_session existing | VERIFIED | 7/7 GREEN; all #[ignore] removed (only in comments) |
| `crates/ironhermes-cli/tests/session_export_e2e.rs` | subprocess test of `hermes session export` | VERIFIED | 3/3 pass: writes_4_file_layout, all_with_no_sessions_exits_zero, all_since_filter_excludes_old_sessions |
| `crates/ironhermes-cli/src/main.rs` (4-site wireup + create_session passes workspace_root) | run_chat + run_single + run_gateway resolve workspace, open trajectory writer, attach to ctx, persist workspace_root | VERIFIED | run_chat (~629) + run_single (~1106) call `canonical_root_string()` (Plan 25.3-16); run_gateway resolves workspace and now uses `set_trajectory_root` (Plan 25.3-15) which propagates to SessionStore for per-session canonical-UUID-keyed trajectory writers; `set_workspace` propagates to SessionStore (Plan 25.3-14). |
| `crates/ironhermes-cli/src/tui_rata/{app.rs, event_loop.rs, commands.rs}` | AppDeps fields + workspace resolution + ctx attach | VERIFIED | resolve_from_cwd at event_loop.rs:315; commands.rs:593, 597 attach fields |
| `crates/ironhermes-gateway/src/runner.rs` (set_workspace, set_trajectory_writer setters) | parity with set_toolset_session | VERIFIED | setters at 129, 137; clones into handler at 281, 286 |
| `crates/ironhermes-gateway/src/handler.rs` (per-message ctx attach + agent_loop.with_trajectory_writer) | ws.clone() + tw.clone() into ctx | VERIFIED | 319, 325 ctx attach; 543 prompt_builder.with_workspace_root; 673 agent.with_trajectory_writer |
| `crates/ironhermes-gateway/src/session.rs` (SessionStore propagates workspace_root to create_session) | workspace_root threaded into get_or_create | VERIFIED | Plan 25.3-14 added `SessionStore.workspace: Option<Arc<Workspace>>` field + `set_workspace` setter; Plan 25.3-16 switched the encoding to `canonical_root_string()`. `get_or_create` now passes `self.workspace.as_ref().map(|ws| ws.canonical_root_string())` to `state.create_session(...)`. Regression test `tests/session_workspace_root.rs` (2/2) covers the SQLite round-trip. |
| `crates/ironhermes-agent/src/agent_loop.rs` (with_trajectory_writer + append site) | builder + per-tool-call append | VERIFIED | 272 builder; 1242-1331 success/failure append sites; classifier at 1383 |
| `crates/ironhermes-agent/src/prompt_builder.rs` (with_workspace_root) | append [Workspace: <root>] to Identity | VERIFIED | line 204; 4/4 dedicated tests |
| `crates/ironhermes-tools/src/{registry.rs, web_extract.rs}` (Tool::redact_args) | trait default + WebExtractTool override | VERIFIED | registry.rs:66 default; web_extract.rs:123 override |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| validate_role_name | RESERVED_ROLE_NAMES | iter | WIRED | line 72 iterates array; test passes |
| StateStore::init_schema | run_migrations | version gate `if current < 8` | WIRED | lib.rs:355-365 |
| ironhermes-trajectory lib | format/writer/reader/handle modules | pub mod + pub use | WIRED | lib.rs:14-22 |
| Cargo.toml workspace | crates/ironhermes-trajectory | workspace.members | WIRED | confirmed |
| TrajectoryWriter::append | self.file.sync_data | fsync per line | WIRED | writer.rs:62 |
| Drop for TrajectoryWriter | self.file.sync_data().ok() | final flush | WIRED | writer.rs:97 |
| ironhermes-trajectory | ironhermes-core | path dep (cycle-break direction) | WIRED | trajectory Cargo.toml has core dep; core has no trajectory dep |
| Tool::redact_args | AgentLoop trajectory append | per-tool-call call | WIRED | agent_loop.rs calls `tool.redact_args(&raw)` before TrajectoryEntry |
| WebExtractTool::redact_args | redact_secrets_in_url | delegation | WIRED | web_extract.rs:123 imports + delegates |
| PromptBuilder::with_workspace_root | PromptSlot::Identity | set_slot | WIRED | prompt_builder.rs:217 |
| main.rs::run_* | resolve_from_cwd | workspace resolution | WIRED | 3 sites resolve workspace |
| build_cmd_ctx + build_command_context + handler | ctx.with_workspace + ctx.with_trajectory_writer | 4-site builder chain | WIRED | parity-guard test 7/7 GREEN |
| GatewayRunner.set_* | build_gateway_handler clone | handler.set_* | WIRED | runner.rs:281, 286 |
| AgentLoop trajectory_writer | append_json_line via TrajectoryWriterHandle | trait dispatch | WIRED | agent_loop.rs:1266 |
| SessionDirectoryExport::write | trajectories.jsonl source | std::fs::copy | WIRED | session_export.rs:71 |
| Commands::Session(SessionSubcommand) | session_cmd::handle_session_command | dispatch arm | WIRED | main.rs:416-418 |
| /export-session | cmd_export_session | CommandRouter | WIRED | handlers.rs:35 |
| GatewayRunner.workspace | SessionStore::get_or_create create_session(workspace_root=...) | propagates via SessionStore.set_workspace + canonical_root_string | WIRED | Plan 25.3-14: `GatewayRunner::set_workspace` calls `self.session_store.try_write()` and forwards the `Arc<Workspace>` into `SessionStore.workspace`. Plan 25.3-16: `get_or_create` reads `self.workspace.as_ref().map(|ws| ws.canonical_root_string())` and threads it into `state.create_session(...)`. Round-trip locked by `session_workspace_root.rs` (2 tests GREEN). |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| Session row workspace_root (CLI surfaces) | `workspace.as_ref().and_then(|ws| ws.root.to_str())` | `resolve_from_cwd(&cwd)` at session start | Yes — real path string | FLOWING |
| Session row workspace_root (Gateway surface) | `self.workspace.as_ref().map(\|ws\| ws.canonical_root_string())` at gateway/session.rs::get_or_create | resolved Workspace from GatewayRunner.set_workspace -> SessionStore.set_workspace | Yes — real path string (lossy U+FFFD for non-UTF-8) | FLOWING |
| TrajectoryEntry.args | `tool.redact_args(&raw)` then JSON-encoded | LLM ToolCall.args at execute_tool_call | Yes — post-redact | FLOWING |
| TrajectoryEntry.impact_level | `classify_impact_level(name)` | static name table | Yes — derived | FLOWING |
| `[Workspace: <root>]` Identity slot | `with_workspace_root(&ws.root)` | resolved Workspace at session start | Yes — when ws is Some | FLOWING (and correctly omits when ws is None) |
| /sessions --workspace filter | `ws.root.to_string_lossy()` from ctx.workspace | CommandContext.workspace | Yes | FLOWING |
| /export-session output | StateStoreHandle::export_session_text | StateStore SQLite query | Yes — real session data via existing export primitives | FLOWING |
| `hermes session export` 4-file layout | SessionDirectoryExport::write reads SessionExport from StateStore | StateStore | Yes — e2e test verifies on-disk artifacts | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Workspace builds clean | `cargo build --workspace` | exit 0 (27 pre-existing warnings only) | PASS |
| Trajectory crate green | `cargo test -p ironhermes-trajectory --lib` | 18 passed; 0 failed | PASS |
| Schema migration v8 green | `cargo test -p ironhermes-state schema` | 4 passed; 0 failed | PASS |
| Workspace resolution green | `cargo test -p ironhermes-core --lib workspace` | 19 passed; 0 failed | PASS |
| RESERVED_ROLE_NAMES with curator green | `cargo test -p ironhermes-core --lib config::tests` | reserved_role_names_contains_all_seven_roles_with_curator + validate_role_name_accepts_all_reserved_roles pass; 61/61 config tests pass | PASS |
| Parity-guard 4-site wireup green | `cargo test -p ironhermes-cli --test invariants_25_3` | 7 passed; 0 failed; **0 ignored** (Plan 8 removed all #[ignore]) | PASS |
| End-to-end session export | `cargo test -p ironhermes-cli --test session_export_e2e` | 3 passed; 0 failed (writes_4_file_layout + export_all + --since filter) | PASS |

### Requirements Coverage

CONTEXT-locked decisions are the requirement set (no REQUIREMENTS.md tags pre-mapped per Phase 25.3 ROADMAP entry).

| Requirement | Source Plan(s) | Description | Status | Evidence |
| ----------- | -------------- | ----------- | ------ | -------- |
| D-T-1 | 01, 05, 09 | Per-tool-call TrajectoryEntry with 8 fields, post-redact args | SATISFIED | TrajectoryEntry struct, redact_args trait, AgentLoop append site all verified |
| D-T-2 | 01, 04, 06 | New ironhermes-trajectory crate; TrajectoryWriter (fsync) + TrajectoryReader | SATISFIED | crate exists; writer fsync per line + Drop sync_data; reader read_all; tests green |
| D-T-3 | 03, 06, 08, 09 | All four surfaces write trajectories | SATISFIED | parity-guard 7/7 green; agent_loop trajectory append wired in main.rs (2 sites), tui_rata, gateway |
| D-T-4 | 04 | Append-only, no automatic eviction in v1 | SATISFIED | TrajectoryWriter has no eviction logic; append-only design verified |
| D-F-1 | 10, 11 | Hybrid SQLite-canonical + on-demand 4-file export via `hermes session export <id>` + /export-session | SATISFIED | SessionDirectoryExport writes 4 files; CLI subcommand + slash both wired; e2e test green |
| D-F-2 | 11 | `hermes session export-all [--since YYYY-MM-DD]` bulk export | SATISFIED | session_cmd.rs:46 ExportAll arm; --since filter; e2e test `session_export_all_since_filter_excludes_old_sessions` green |
| D-W-1 | 00, 02, 06, 08, 14, 16 | Workspace newtype + cwd walk-up + frozen-snapshot + sessions.workspace_root TEXT NULL column + value-persisted on every surface | SATISFIED | Newtype, resolve_from_cwd, schema migration v8, struct field, CLI persistence — all VERIFIED. Plan 25.3-14 closed the gateway gap: `SessionStore.workspace` field + `set_workspace` setter + `get_or_create` threads the resolved workspace into `state.create_session(...)`. Plan 25.3-16 canonicalized the encoding via `Workspace::canonical_root_string()` (lossy-but-deterministic) so prompt + DB column + filter all agree even on non-UTF-8 paths. Round-trip locked by `tests/session_workspace_root.rs` (2/2 GREEN). |
| D-W-2 | 02, 06, 07, 08, 10 | Workspace consumed by /sessions filter, prompt slot, trajectory dir scoping; Curator output destination wired | SATISFIED | All four consumers verified: /sessions --workspace filter (cmd_sessions); [Workspace:] Identity-slot prompt line; workspace-scoped trajectory paths in all 3 surfaces; skills_dir field exposed |
| D-P0-1 | 00 | RESERVED_ROLE_NAMES extended 6→7 with "curator"; validation test updated | SATISFIED | Array literal with curator; renamed test passes; doc comment says "seven reserved helper-task roles" |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `crates/ironhermes-tools/tests/browser_integration.rs` | 90 | Pre-existing `register_browser_tools_with_vision` arg-count mismatch | Info | Pre-existing per Plan 11 SUMMARY (line 218-219); out of phase scope; unrelated to 25.3 |

The original `gateway/session.rs:113` "Plan 0 placeholder" hardcoded-None entry was closed by Plan 25.3-14 (commit `0b2394c`); the literal placeholder comment is removed and the field is wired through `SessionStore.workspace` + `set_workspace` propagation.

### Human Verification Required

See `human_verification:` block in frontmatter — three Manual-Only items per VALIDATION.md (cache hit rate, bulk-export at scale, prompt absence in global mode). These do not block goal achievement on their own; they are auditor-grade checks that VALIDATION.md already classified as Manual-Only.

### Gaps Summary

The phase achieves **28/28 must-haves**. The original verifier-flagged BLOCKER (Telegram gateway sessions persisted `workspace_root=NULL`) was closed by Plan 25.3-14; the four CR-XX critical findings from `25.3-REVIEW.md` were closed by Plans 25.3-13..16; six new INV-25.3-XX parity-guards lock the closures against re-introduction.

No outstanding gaps. Residual warnings (WR-03..06 + IN-01..02) are explicitly deferred per the table below — none block the phase contract.

## Gap Closure (2026-05-03 — Plans 25.3-12..16)

Phase 25.3 originally verified 27/28 must-haves with a single phase-blocking gap (must-have #28 — Telegram gateway sessions persisted `workspace_root=NULL`). The follow-on code review (`25.3-REVIEW.md`) flagged 4 CRITICAL findings that were folded into the gap-closure batch:

| Plan | Closes | Description |
|------|--------|-------------|
| 25.3-12 | (RED gates) | Add INV-25.3-07..11 to lock the gap-closure contract before code changes land |
| 25.3-13 | CR-01 + CR-04 | ratatui REPL: state.create_session + PromptBuilder.with_workspace_root |
| 25.3-14 | VERIFIER BLOCKER + WR-02 | Gateway SessionStore: thread workspace_root into get_or_create + regression test |
| 25.3-15 | CR-02 | Gateway trajectory canonical UUID: per-session writers cached in SessionStore (was `gateway-<uuid>`) |
| 25.3-16 | CR-03 + WR-01 + IN-03 | Workspace::canonical_root_string single-source-of-truth encoding |

New parity-guards: 6 (INV-25.3-07 through INV-25.3-12). Original 7 + new 6 = 13 total.

Residual warnings explicitly DEFERRED (not blocking phase verification):

| ID | Source | Title | Defer-to | Rationale |
|----|--------|-------|----------|-----------|
| WR-03 | 25.3-REVIEW.md | `run_export_all` exits 0 even if every session failed | Phase 25.4 or operator-CLI polish phase | Operator-UX issue, not a correctness gap; the per-session warnings are still emitted to stderr. CI consumers depending on exit code can grep for "Warning:" in the meantime. Does not affect D-F-1/D-F-2 contract. |
| WR-04 | 25.3-REVIEW.md | TrajectoryWriter creates ghost empty dirs for tool-less turns | Phase 25.4 retention policy | D-T-4 explicitly states "no automatic eviction in v1"; ghost dirs are a deliberate consequence of append-only design. Will be revisited when Curator retention/pruning policy lands in 25.4. |
| WR-05 | 25.3-REVIEW.md | `cmd_export_session` slash command not workspace-aware | Phase 25.4 | Curator owns workspace-scoped export flow in 25.4; CLI subcommand `hermes session export <id> --output <dir>` already supports operator overrides as a workaround. |
| WR-06 | 25.3-REVIEW.md | `from_root_and_marker` TOCTOU on SOUL.md move | Document only | Benign per the review (SOUL.md doesn't move during normal operation); resolution is frozen-snapshot per session, so the window is bounded. Will be revisited if a real-world failure surfaces. |
| IN-01 | 25.3-REVIEW.md | TrajectoryEntry `ts` constructor vs. test-literal divergence | Document only | Benign drift surface; constructor uses `Utc::now().to_rfc3339()` consistently in production code. Adding a constructor-emitted timestamp regex test is low-priority polish. |
| IN-02 | 25.3-REVIEW.md | `cmd_sessions` duplicate `--workspace` last-wins | UX polish | Documented behavior; operator-typo case. Easy follow-up if telemetry shows users hitting it. |

Full re-verification: 2026-05-03 (this update). Status: verified.

**Phase 25.4 readiness:** All Curator prerequisites are now CLEAN — RESERVED_ROLE_NAMES["curator"], TrajectoryReader API, SessionDirectoryExport, Workspace.skills_dir, per-workspace session metadata on EVERY surface (CLI + REPL + gateway), per-session trajectories at canonical UUIDs on the gateway, REPL session lifecycle parity with classic-TUI.

---

*Verified: 2026-05-03T18:03:21Z (initial — prior status had a single phase-blocking gap, score 27/28)*
*Re-verified: 2026-05-03T19:56:11Z (gap-closure — verified at 28/28)*
*Verifier: Claude (gsd-verifier) + gsd-executor (Plan 25.3-17 re-verification)*
