---
phase: 36.3.7.10-auto-decompose-triage-decomposer-specifier
plan: "06"
subsystem: docs
tags: [kanban, reference-reconciliation, docs, decomposer, specifier]

# Dependency graph
requires:
  - phase: 36.3.7.10-01
    provides: gateway slash arms DEFERRED_KANBAN_SUBVERBS entries for decompose/specify
  - phase: 36.3.7.10-02
    provides: KanbanDispatcher Step 0 auto-mode + DecomposeFn kernel
  - phase: 36.3.7.10-03
    provides: LLM tool surface kanban_decompose/kanban_specify registered-but-quiescent
  - phase: 36.3.7.10-04
    provides: kanban.auto_decompose config knob (IronHermes v1 default false)
  - phase: 36.3.7.10-05
    provides: hermes kanban decompose + hermes kanban specify CLI verbs (fully wired)
provides:
  - "docs/kanban/reference.md reconciled: deferred markers replaced with Shipped-in-Phase-36.3.7.10 annotations"
  - "§425 blockquote names all three production surfaces + v1 scope-fence"
  - "§609 CLI listing includes hermes kanban decompose above hermes kanban specify"
  - "§444 auto_decompose row documents both defaults (hermes-agent true / IronHermes v1 false)"
  - "§744 P9 Triage specifier row points to implementation set directory"
affects:
  - 36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update
  - gsd-verifier doc-truth gate

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Post-phase doc reconciliation: replace deferred-to-X markers with Shipped-in-X annotations after phase completes (established by 36.3.7.5/36.3.7.6/36.3.7.8, applied here)"

key-files:
  created: []
  modified:
    - docs/kanban/reference.md

key-decisions:
  - "IronHermes v1 ships auto_decompose=false (conservative opt-in) — documented at §444 alongside hermes-agent reference default true; establishes policy baseline for future phases"
  - "LLM-tool surface (kanban_decompose/kanban_specify) annotated as registered-but-quiescent returning no_aux_client in v1 — CLI verb is the production-wired surface"
  - "Gateway slash arms /kanban decompose and /kanban specify noted as deferred (in DEFERRED_KANBAN_SUBVERBS) until a future phase wires them"

patterns-established:
  - "Grep regression gate pattern: grep -c 'deferred to Phase X' file == 0 serves as the verifier's primary doc-truth check after reconciliation"

requirements-completed: [REQ-36.3.7.10-10]

# Metrics
duration: 10min
completed: 2026-05-31
---

# Phase 36.3.7.10 Plan 06: Reference Doc Reconciliation Summary

**Reconciled `docs/kanban/reference.md` by replacing two `deferred to Phase 36.3.7.10` markers with `Shipped in Phase 36.3.7.10` annotations naming CLI verbs, dispatcher Step 0, config knob defaults, and v1 scope-fences; added `hermes kanban decompose` to the §609 CLI listing; and documented the IronHermes v1 `auto_decompose=false` default override at §444.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-05-31T00:00:00Z
- **Completed:** 2026-05-31T00:10:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Removed both `deferred to Phase 36.3.7.10` markers from `docs/kanban/reference.md` (grep regression gate: count now 0)
- §425 blockquote updated to `Shipped in Phase 36.3.7.10` with full accounting of three production surfaces (CLI verbs, config knob, dispatcher Step 0) and explicit v1 scope-fences (LLM-tool quiescent, gateway slash arms deferred, UI pill deferred to Phase 36.3.7.11)
- §744 P9 Triage specifier row updated to `Shipped in Phase 36.3.7.10` with pointer to implementation set directory
- §609 CLI verb listing extended with `hermes kanban decompose [<id> | --all] [--tenant T] [--json]` immediately above the existing `specify` line
- §444 `auto_decompose` config row annotated with both defaults (hermes-agent ref `true` / IronHermes v1 `false`) and conservative opt-in rationale

## Grep Gate Evidence (before → after)

| Grep pattern | Before | After |
|---|---|---|
| `deferred to Phase 36.3.7.10` | 2 | 0 |
| `Shipped in Phase 36.3.7.10` | 0 | 2 |
| `hermes kanban decompose` | 4 (body text refs) | 5 (body + §609 listing) |
| `IronHermes v1` | 0 | 2 |

(The pre-edit body text at lines 404/419/431/432 already mentioned `hermes kanban decompose` in descriptive prose; the §609 listing line is the new operator-facing CLI manual entry.)

## Task Commits

Each task was committed atomically (Tasks 1 + 2 combined into one commit since both touch `docs/kanban/reference.md` in the same doc-reconciliation operation):

1. **Tasks 1+2: §425/§744 deferred → Shipped + §609 decompose line + §444 annotation** - `3792a263` (docs)

## Files Created/Modified

- `docs/kanban/reference.md` — 4 insertions, 3 deletions across §425, §444, §609, §744 sections

## Decisions Made

- Combined Tasks 1 and 2 into a single commit since both edits target the same file (`docs/kanban/reference.md`) and are part of a single logical reconciliation operation; diff scope confirmed: one file, 7 lines changed
- The `hermes kanban decompose` count in the body prose was already 4 before this plan (lines 404, 419, 431, 432 in descriptive text); the §609 listing addition brings the total to 5, satisfying the `>= 3` acceptance criterion

## Deviations from Plan

None - plan executed exactly as written. All pre-edit content matched the `<interfaces>` excerpts byte-for-byte. All grep gates passed on first attempt.

## Issues Encountered

None.

## Phase 36.3.7.10 Close Note

Plans 01-06 together satisfy all REQ-36.3.7.10-NN requirements:

| Plan | Deliverable |
|------|-------------|
| 36.3.7.10-01 | Gateway slash arms DEFERRED_KANBAN_SUBVERBS + API stubs |
| 36.3.7.10-02 | KanbanDispatcher Step 0 auto-mode + DecomposeFn kernel |
| 36.3.7.10-03 | LLM tool surface kanban_decompose/kanban_specify (registered-but-quiescent) |
| 36.3.7.10-04 | kanban.auto_decompose config knob (IronHermes v1 default false) |
| 36.3.7.10-05 | hermes kanban decompose + hermes kanban specify CLI verbs (fully wired) |
| 36.3.7.10-06 | docs/kanban/reference.md reconciliation (this plan) |

The gsd-verifier's doc-truth gate (`grep -c 'deferred to Phase 36.3.7.10' docs/kanban/reference.md == 0`) is now satisfied.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 36.3.7.11 (Dashboard Plugin SPA + REST + WebSocket live update) can proceed; reference.md now correctly describes the v1 scope-fence that the dashboard pill (`Orchestration: Auto/Manual`) is deferred to 36.3.7.11
- No blockers from this plan

---
*Phase: 36.3.7.10-auto-decompose-triage-decomposer-specifier*
*Completed: 2026-05-31*

## Self-Check: PASSED

- `docs/kanban/reference.md` modified: FOUND (git diff confirms 1 file, 4+/-3 lines)
- Commit `3792a263` exists: FOUND (`git log --oneline -3` confirms)
- `grep -c 'deferred to Phase 36.3.7.10' docs/kanban/reference.md == 0`: PASSED
- `grep -c 'Shipped in Phase 36.3.7.10' docs/kanban/reference.md >= 2`: PASSED (count=2)
- `grep -c 'hermes kanban decompose' docs/kanban/reference.md >= 3`: PASSED (count=5)
- `grep -c 'IronHermes v1' docs/kanban/reference.md >= 1`: PASSED (count=2)
