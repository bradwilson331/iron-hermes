---
phase: 36.3.7.10
slug: auto-decompose-triage-decomposer-specifier
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-31
---

# Phase 36.3.7.10 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust `#[test]` + `#[tokio::test]` (cargo workspace; no external test runner) |
| **Config file** | `Cargo.toml` workspace `[dev-dependencies]` (`tempfile`, `anyhow`, `serde_json`, `tokio` test-util already present) |
| **Quick run command** | `cargo test -p ironhermes-kanban --test decomposer_logic -- --test-threads=1` |
| **Full suite command** | `cargo test --workspace` |
| **Estimated runtime** | Quick: ~15s · Full: ~120s |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-kanban --test decomposer_logic -- --test-threads=1`
- **After every plan wave:** Run `cargo test --workspace`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 120 seconds (full suite)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 36.3.7.10-01-01 | 01 | 1 | REQ-36.3.7.10-04, REQ-36.3.7.10-06 | T-36.3.7.10-01-01 / T-36.3.7.10-01-02 | KanbanConfig.auto_decompose defaults to false (conservative opt-in); auto_decompose_per_tick caps per-tick LLM calls | unit | `cargo test -p ironhermes-kanban --lib -- config::tests::default_matches_context_md events::tests::v1_added_kinds_are_distinct` | ✅ (existing tests extended additively) | ⬜ pending |
| 36.3.7.10-01-02 | 01 | 1 | REQ-36.3.7.10-08, REQ-36.3.7.10-09 | T-36.3.7.10-01-03 / T-36.3.7.10-01-04 | kanban_decomposer added to RESERVED_ROLE_NAMES allowlist; DEFERRED_KANBAN_SUBVERBS extended with decompose/specify to return clean deferred response (no info disclosure beyond reference.md §609) | unit | `cargo test -p ironhermes-core --lib -- config::tests::reserved_role_names_contains_all_eight_roles_with_kanban_decomposer` | ✅ (existing test renamed + extended) | ⬜ pending |
| 36.3.7.10-02-01 | 02 | 2 | REQ-36.3.7.10-01, REQ-36.3.7.10-06 | T-36.3.7.10-02-02 / T-36.3.7.10-02-04 / T-36.3.7.10-02-SC | DecomposeFn injection boundary keeps kanban crate free of provider client; all SQL writes use rusqlite params! (no string interpolation); crate-isolation fence holds | build + grep | `cargo build -p ironhermes-kanban && grep -cE '^ironhermes-agent\s*=\|^reqwest\s*=' crates/ironhermes-kanban/Cargo.toml` (must exit 0 with grep output `0`) | ❌ W0 — create `crates/ironhermes-kanban/src/decomposer.rs` + `apply_specify`/`apply_decompose` in store.rs | ⬜ pending |
| 36.3.7.10-02-02 | 02 | 2 | REQ-36.3.7.10-01, REQ-36.3.7.10-06 | T-36.3.7.10-02-01 / T-36.3.7.10-02-05 | Status guard prevents double-decompose race; LLM failure retains task in triage + appends decompose_failed event; prompt injection contained to TEXT body (no exec path) | unit (×6 receiver tests) | `cargo test -p ironhermes-kanban --test decomposer_logic -- --test-threads=1` | ❌ W0 — create `crates/ironhermes-kanban/tests/decomposer_logic.rs` | ⬜ pending |
| 36.3.7.10-03-01 | 03 | 3 | REQ-36.3.7.10-07 | T-36.3.7.10-03-01 / T-36.3.7.10-03-04 | Tools route board context through validated resolve_board_context_from_args (Phase 36.3.7.9 D-08); is_available gates on HERMES_KANBAN_TASK env var; v1 returns no_aux_client envelope (no live LLM invocation surface) | unit | `cargo build -p ironhermes-kanban && cargo test -p ironhermes-kanban --lib -- tools::decompose tools::specify` | ❌ W0 — create `crates/ironhermes-kanban/src/tools/decompose.rs` + `crates/ironhermes-kanban/src/tools/specify.rs` | ⬜ pending |
| 36.3.7.10-03-02 | 03 | 3 | REQ-36.3.7.10-07 | T-36.3.7.10-03-SC | Tool-count regression test enforces exactly 13 tools at compile time (catches accidental tool removal or unregistered additions) | unit | `cargo test -p ironhermes-kanban --lib tools::tests::register_kanban_tools_registers_exactly_thirteen && cargo test -p ironhermes-kanban` | ✅ (existing test renamed 11→13) | ⬜ pending |
| 36.3.7.10-04-01 | 04 | 3 | REQ-36.3.7.10-05 | T-36.3.7.10-04-02 / T-36.3.7.10-04-SC | DispatcherContext.decompose_fn defaults to None — graceful degradation when gateway runner does not wire production fn (RESEARCH §Q1 rec b); per-board sweep clones the Option cheaply | build (cross-crate) | `cargo build -p ironhermes-kanban && cargo build -p ironhermes-gateway` | ✅ (existing tests cover constructor defaults) | ⬜ pending |
| 36.3.7.10-04-02 | 04 | 3 | REQ-36.3.7.10-05, REQ-36.3.7.10-06 | T-36.3.7.10-04-01 / T-36.3.7.10-04-03 / T-36.3.7.10-04-04 | Step 0 enforces auto_decompose_per_tick cap (DoS/cost mitigation); sequential iteration prevents burst LLM billing; per-task failures logged-and-continued (no cascade halt); double-tick race contained by status guard in apply_decompose | unit (×3 dispatcher receiver tests) | `cargo test -p ironhermes-kanban --test dispatcher_logic -- --test-threads=1` | ❌ W0 — extend existing `crates/ironhermes-kanban/tests/dispatcher_logic.rs` with 3 new auto_decompose_step_* tests | ⬜ pending |
| 36.3.7.10-05-01 | 05 | 4 | REQ-36.3.7.10-02, REQ-36.3.7.10-03 | T-36.3.7.10-05-01 / T-36.3.7.10-05-02 / T-36.3.7.10-05-03 | CLI verbs wire production DecomposeFn via three-tier cascade (kanban.decomposer_model > auxiliary.kanban_decomposer > main provider); absent provider config exits Ok(1) with stderr message naming config keys; --all sweep capped by auto_decompose_per_tick | build + smoke | `cargo build -p ironhermes-cli && cargo test -p ironhermes-cli --no-run && cargo run -p ironhermes-cli -- kanban decompose --help && cargo run -p ironhermes-cli -- kanban specify --help` | ❌ W0 — extend `crates/ironhermes-cli/src/kanban/{mod.rs,commands.rs}` with 2 variants + 2 cmd_ fns + build_runtime_decompose_fn | ⬜ pending |
| 36.3.7.10-05-02 | 05 | 4 | REQ-36.3.7.10-02, REQ-36.3.7.10-03 | T-36.3.7.10-05-04 / T-36.3.7.10-05-SC | Clap parse-shape locks: id/all mutually exclusive (conflicts_with enforcement), --author defaults to HERMES_PROFILE env, --board honors 4-tier resolution; tests use try_parse_from so failures return Err (no exit-on-error) | unit (×4+ clap parse tests) | `cargo test -p ironhermes-cli --test decompose_cli` | ❌ W0 — create `crates/ironhermes-cli/tests/decompose_cli.rs` | ⬜ pending |
| 36.3.7.10-06-01 | 06 | 5 | REQ-36.3.7.10-10 | T-36.3.7.10-06-01 / T-36.3.7.10-06-03 | §425 + §744 deferred annotations replaced with Shipped-in-Phase-36.3.7.10 wording; replacement only names surfaces evidenced as shipped by Plan 01-05 SUMMARYs; diff scope-fenced to one file | grep gate | `test $(grep -c 'deferred to Phase 36.3.7.10' docs/kanban/reference.md) -eq 0 && test $(grep -c 'Shipped in Phase 36.3.7.10' docs/kanban/reference.md) -ge 2` | ✅ (existing file edited in place) | ⬜ pending |
| 36.3.7.10-06-02 | 06 | 5 | REQ-36.3.7.10-10 | T-36.3.7.10-06-01 | §609 CLI listing gains `hermes kanban decompose` line; §444 config row annotated with IronHermes v1 default override (false) vs hermes-agent ref default (true) | grep gate | `test $(grep -c 'hermes kanban decompose' docs/kanban/reference.md) -ge 3 && test $(grep -c 'deferred to Phase 36.3.7.10' docs/kanban/reference.md) -eq 0` | ✅ (existing file edited in place) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements at the framework level (Rust `#[test]` + `#[tokio::test]` with `tempfile` already in workspace dev-dependencies; no new test runner / framework install needed). This phase adds tests by extending existing crates rather than introducing new test infra. The "❌ W0" cells in the Per-Task Verification Map above mark NEW test files / test fns that must be created as part of the implementing task itself (TDD discipline — write the test first within the same task that ships the production code).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| End-to-end `hermes kanban decompose <id>` against a live LLM provider | REQ-36.3.7.10-02 | Requires operator-configured provider API key (Anthropic / OpenRouter) — automated CI does not carry production credentials; the build_runtime_decompose_fn closure issues a real HTTP call that costs tokens | (1) Set `ANTHROPIC_API_KEY` or `OPENROUTER_API_KEY` in env; (2) ensure `config.yaml` has `model.default` or `auxiliary.kanban_decomposer` configured; (3) `hermes kanban create --title "build a hello-world rust crate" --status triage`; (4) capture the returned task ID `t_xxx`; (5) `hermes kanban decompose t_xxx`; (6) assert: stdout shows `{ok: true, task_id, child_ids: [...], new_title}`, the parent task is now `todo` (verify via `hermes kanban show t_xxx`), and 2-5 child tasks exist with non-empty assignees. |
| End-to-end `hermes kanban specify <id>` against a live LLM provider | REQ-36.3.7.10-03 | Same reason — live provider call | (1) Same env setup; (2) `hermes kanban create --title "add tests for foo" --status triage`; (3) capture task ID `t_yyy`; (4) `hermes kanban specify t_yyy --author operator`; (5) assert: stdout shows `{ok: true, task_id, new_title}`, the task is now `todo`, the body is rewritten with `## Goal` / `## Approach` / `## Acceptance Criteria` / `## Risk Register` / `## Suggested Skills` sections, and a `specified` event row exists with `author=operator`. |
| Dispatcher Step 0 auto-mode against a live provider (gateway runner DecomposeFn injection) | REQ-36.3.7.10-05 | The gateway runner ships with `decompose_fn=None` in v1 per RESEARCH §Q1 rec (b); operator must manually wire the production DecomposeFn via a follow-up phase OR by directly modifying gateway runner code for a one-off test | (1) Modify `crates/ironhermes-gateway/src/runner.rs` to construct DispatcherContext with `decompose_fn: Some(build_runtime_decompose_fn(...))`; (2) set `kanban.auto_decompose = true` in config.yaml; (3) start gateway; (4) `hermes kanban create --status triage` × 5; (5) wait one dispatcher tick (default 60s); (6) assert: 3 of the 5 triage tasks promoted to `todo` (per default `auto_decompose_per_tick=3`), remaining 2 still `triage`; (7) wait another tick; (8) assert: remaining 2 now `todo`. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (W0 dependencies are NEW test files that the implementing task itself creates per TDD discipline)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every task in the Per-Task Verification Map has an automated command)
- [x] Wave 0 covers all MISSING references (every ❌ W0 row names the exact file to create as part of that task)
- [x] No watch-mode flags (all cargo test commands are single-shot)
- [x] Feedback latency < 120s (full workspace suite)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-31
