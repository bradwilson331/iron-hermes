---
phase: 36.3.7.10
plan: 06
type: execute
wave: 5
depends_on: [36.3.7.10-01, 36.3.7.10-02, 36.3.7.10-03, 36.3.7.10-04, 36.3.7.10-05]
files_modified:
  - docs/kanban/reference.md
autonomous: true
requirements: [REQ-36.3.7.10-10]
tags: [docs, reference-reconciliation, kanban]

must_haves:
  truths:
    - "`docs/kanban/reference.md` no longer contains the v1-NOTE `deferred to Phase 36.3.7.10` block at §425"
    - "`docs/kanban/reference.md` no longer contains the `(deferred to Phase 36.3.7.10 — triage decomposer/specifier not implemented in v1...)` annotation in the §744 P9 Triage specifier row"
    - "Both replacement blocks read `Shipped in Phase 36.3.7.10` and cite the shipped surfaces: CLI verbs (`hermes kanban decompose` / `specify`), config knob (`kanban.auto_decompose` default `false` — note: ironhermes ships `false` for v1 per RESEARCH §Q7 contra reference.md's documented `true`), dispatcher Step 0 (auto-mode), LLM tool surface registered-but-quiescent (`kanban_decompose` / `kanban_specify` return `no_aux_client` in v1 — use CLI verb)"
    - "The §609 CLI verb listing already shows `hermes kanban specify` (per the grep we ran — present at v1 baseline); a `hermes kanban decompose [<id>|--all] [--tenant T]` line is added immediately above the existing specify line so both new verbs are documented in the manual surface"
    - "The §444 config table row for `auto_decompose` documents the IronHermes default `false` (conservative opt-in) alongside the hermes-agent reference default `true` — this anchors the policy difference established in RESEARCH §Q7"
    - "No other section of reference.md is altered; the doc edit is strictly scope-fenced to §425, §609, §744, and §444 (one specific row)"
    - "The grep gate `grep -c 'deferred to Phase 36.3.7.10' docs/kanban/reference.md == 0` becomes the verifier's regression check for this plan"
  artifacts:
    - path: "docs/kanban/reference.md"
      provides: "Reconciled reference.md with v1-NOTE deferred-to-36.3.7.10 blocks replaced by Shipped-in-Phase-36.3.7.10 annotations + new `hermes kanban decompose` CLI verb line at §609 + IronHermes-default override for auto_decompose at §444"
      contains: "Shipped in Phase 36.3.7.10"
  key_links:
    - from: "docs/kanban/reference.md §425"
      to: ".planning/phases/36.3.7.10-auto-decompose-triage-decomposer-specifier/"
      via: "Shipped-in-Phase-36.3.7.10 annotation block"
      pattern: "Shipped in Phase 36.3.7.10"
    - from: "docs/kanban/reference.md §609"
      to: "crates/ironhermes-cli/src/kanban/mod.rs::KanbanCommands::Decompose"
      via: "CLI verb listing"
      pattern: "hermes kanban decompose"
---

<objective>
Reconcile the source-of-truth doc `docs/kanban/reference.md` with the shipped Phase 36.3.7.10 behavior. Two `deferred to Phase 36.3.7.10` annotations at §425 (Auto-decompose / triage decomposer / specifier feature description) and §744 (P9 Triage specifier in the lifecycle persona table) are replaced with `Shipped in Phase 36.3.7.10` blocks that name the shipped surfaces and the v1 caveats. The §609 CLI verb listing is extended to include `hermes kanban decompose` (the spec line for `specify` already exists at v1 baseline). The §444 config table row for `auto_decompose` is annotated with the IronHermes-default override (`false` for v1 — contra the hermes-agent reference default of `true`).

Purpose: Phase 36.3.7.10 changes the source-of-truth behavior described by reference.md; future contributors (and the gsd-verifier's doc-truth gate) read reference.md as authoritative. Leaving the `deferred to Phase 36.3.7.10` markers in place would produce a doc-truth drift bug. The pattern of post-phase doc reconciliation was established by Phase 36.3.7.6 BUG-05 (5 line edits at §425/§427 referencing 36.3.7.1), Phase 36.3.7.5 §703/§761 reconciliation, and Phase 36.3.7.8 §14/§200/§741 reconciliation; this plan applies the same pattern verbatim to the two §425/§744 blocks.

Output: 1 modified file (`docs/kanban/reference.md`). ~10 line edits across 3 sections (§425 + §444 + §609 + §744). No code changes. The plan is the LAST wave; it depends on all prior plans having shipped so the "shipped" annotations are factually accurate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.3.7.10-auto-decompose-triage-decomposer-specifier/36.3.7.10-CONTEXT.md
@.planning/phases/36.3.7.10-auto-decompose-triage-decomposer-specifier/36.3.7.10-RESEARCH.md
@.planning/phases/36.3.7.10-auto-decompose-triage-decomposer-specifier/36.3.7.10-01-SUMMARY.md
@.planning/phases/36.3.7.10-auto-decompose-triage-decomposer-specifier/36.3.7.10-02-SUMMARY.md
@.planning/phases/36.3.7.10-auto-decompose-triage-decomposer-specifier/36.3.7.10-03-SUMMARY.md
@.planning/phases/36.3.7.10-auto-decompose-triage-decomposer-specifier/36.3.7.10-04-SUMMARY.md
@.planning/phases/36.3.7.10-auto-decompose-triage-decomposer-specifier/36.3.7.10-05-SUMMARY.md

# File this plan modifies
@docs/kanban/reference.md

<interfaces>
<!-- Exact pre-edit content + insertion points (verified by the planner's grep run). -->

Pre-edit content at docs/kanban/reference.md (verified via `grep -n 'deferred to Phase 36.3.7.10' docs/kanban/reference.md`):

LINE 427 (the §425-region v1-NOTE block):
```
> **v1 NOTE:** Auto-decompose / triage decomposer / specifier (`hermes kanban decompose`, `hermes kanban specify`, `kanban.auto_decompose`) are deferred to Phase 36.3.7.10 (re-homed 2026-05-30 from the defunct Phase 36.3.7.2 assignment, which shipped the delegate_task `oneOf` tool-schema fix instead). In v1, the `triage` column exists as a parking lot; transitions out of `triage` are operator-only (`hermes kanban assign` + manual status update). The **Orchestration: Auto/Manual** toggle does not exist yet.
```

LINE 746 (the §744 P9 Triage specifier row):
```
| P9 Triage specifier | rough idea → `triage` → `hermes kanban specify` expands body → `todo` | "turn this one-liner into a spec'd task" | *(deferred to Phase 36.3.7.10 — triage decomposer/specifier not implemented in v1; re-homed 2026-05-30 from the defunct 36.3.7.2 assignment which shipped the delegate_task `oneOf` tool-schema fix instead)* |
```

Pre-edit content at §444 (config table — the `auto_decompose` row):
```
| `auto_decompose` | `true` | Dispatcher auto-runs the decomposer every tick. |
```

Pre-edit content at §609 (CLI verb listing — partial):
```
hermes kanban specify [<id> | --all] [--tenant T]      # flesh out a triage-column idea
```
(no `decompose` line exists at v1 baseline per the grep we ran — must be ADDED above the specify line)

The §425/§427 block is a `> **v1 NOTE:**` blockquote; the replacement block is a `> **Shipped in Phase 36.3.7.10:**` blockquote with the same outer structure.

The §744 row is a Markdown table row with 4 columns: persona name, lifecycle arrow, quoted intent, status annotation. The replacement value for the last column is `*(Shipped in Phase 36.3.7.10 — see `.planning/phases/36.3.7.10-*` for the implementation set)*`.

The §444 row gets an annotation appended to the description column: `| \`auto_decompose\` | \`true\` (hermes-agent ref) / \`false\` (IronHermes v1 default) | Dispatcher auto-runs the decomposer every tick. **Phase 36.3.7.10:** IronHermes ships \`false\` as the conservative opt-in default; set to \`true\` to enable auto-mode. |`. The "Default" column gains both values; the description gains the Phase 36.3.7.10 annotation.

The §609 line ADDS a new line immediately ABOVE the existing specify line: `hermes kanban decompose [<id> | --all] [--tenant T] [--json]  # fan a triage task into a child swarm (Phase 36.3.7.10)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace §425 v1-NOTE block + §744 P9 row deferred-annotation with Shipped-in-Phase-36.3.7.10 wording</name>
  <read_first>
    - docs/kanban/reference.md (read lines 420-450 for the §425 region; read lines 740-755 for the §744 region; confirm pre-edit content matches the <interfaces> excerpts byte-for-byte)
    - .planning/phases/36.3.7.5-gateway-notifier-auto-subscribe-polling-loop/36.3.7.5-04-SUMMARY.md (look for the reconciliation evidence pattern Phase 36.3.7.5 used at §703/§761 — same shape as this plan)
    - .planning/phases/36.3.7.10-auto-decompose-triage-decomposer-specifier/36.3.7.10-02-SUMMARY.md (Plan 02 evidence — confirm the kernel shipped so the "Shipped" claim is true)
    - .planning/phases/36.3.7.10-auto-decompose-triage-decomposer-specifier/36.3.7.10-05-SUMMARY.md (Plan 05 evidence — confirm the CLI verbs shipped)
  </read_first>
  <behavior>
    - After the edit, `grep -c 'deferred to Phase 36.3.7.10' docs/kanban/reference.md == 0`.
    - After the edit, `grep -c 'Shipped in Phase 36.3.7.10' docs/kanban/reference.md >= 2` (one at §425, one at §744; the §609 + §444 edits don't necessarily use the "Shipped in" phrase but reference the phase).
    - The §425 blockquote replacement names: (a) the shipped CLI verbs `hermes kanban decompose` + `hermes kanban specify`; (b) the shipped config knob `kanban.auto_decompose` with the IronHermes v1 default `false` caveat; (c) the shipped dispatcher Step 0 (auto-mode); (d) the v1 scope-fence note that the LLM-tool surface (`kanban_decompose` / `kanban_specify`) and gateway slash arms (`/kanban decompose` / `/kanban specify`) return `no_aux_client` / deferred response respectively in v1 and the CLI verb is the production-wired surface.
    - The §744 row replacement reads: `*(Shipped in Phase 36.3.7.10 — see \`.planning/phases/36.3.7.10-auto-decompose-triage-decomposer-specifier/\` for the implementation set)*` in the status column. Persona name + arrow + intent columns are byte-stable.
    - No other section of reference.md changes.
  </behavior>
  <action>
    Open `docs/kanban/reference.md`. Read lines 420-450 to confirm the pre-edit §425 block matches the <interfaces> excerpt byte-for-byte. If it does, replace line 427 (the blockquote starting `> **v1 NOTE:** Auto-decompose...`) with a new blockquote:

      ```
      > **Shipped in Phase 36.3.7.10:** Auto-decompose / triage decomposer / specifier ship with three production surfaces: (1) CLI verbs `hermes kanban decompose [<id>|--all]` and `hermes kanban specify [<id>|--all] [--author NAME]` (operator-driven, fully wired); (2) config knob `kanban.auto_decompose` (default `false` in IronHermes v1 — conservative opt-in; contrast hermes-agent reference default `true`); (3) dispatcher Step 0 auto-mode (gated on `auto_decompose=true` AND a wired `DecomposeFn`; gateway runner ships with `decompose_fn=None` in v1 per scope-fence — production wiring is queued for a follow-up phase). The LLM-tool surface (`kanban_decompose` / `kanban_specify`) is registered-but-quiescent in v1: invocations return `no_aux_client` and direct the orchestrator to the CLI verb. Gateway slash arms `/kanban decompose` and `/kanban specify` return the standard deferred-subverb response (entries added to `DEFERRED_KANBAN_SUBVERBS` in 36.3.7.10-01) until a future phase wires them. The **Orchestration: Auto/Manual** UI pill is also deferred — toggle behavior via the config knob until the dashboard plugin ships (Phase 36.3.7.11).
      ```

    Read lines 740-755 to confirm the pre-edit §744 P9 row matches the <interfaces> excerpt. If it does, replace the status-column cell `*(deferred to Phase 36.3.7.10 — triage decomposer/specifier not implemented in v1; re-homed 2026-05-30 from the defunct 36.3.7.2 assignment which shipped the delegate_task \`oneOf\` tool-schema fix instead)*` with `*(Shipped in Phase 36.3.7.10 — see \`.planning/phases/36.3.7.10-auto-decompose-triage-decomposer-specifier/\` for the implementation set: CLI verbs + dispatcher Step 0 + LLM-tool registration + config knobs)*`. The persona name, arrow, and intent columns are byte-stable.

    After both edits, run the grep regression: `grep -c 'deferred to Phase 36.3.7.10' docs/kanban/reference.md` MUST return `0`. If it returns >0, find the residual and remove it (there may be a stray reference in another section the planner missed; the spec is that ALL such markers go away).

    Run a count check: `grep -c 'Shipped in Phase 36.3.7.10' docs/kanban/reference.md` MUST return `>=2` (one per replacement block).

    Do NOT touch any other section. The doc-edit is scope-fenced — `git diff --stat docs/kanban/reference.md` should show one file, ~10 changed lines.
  </action>
  <verify>
    <automated>test $(grep -c 'deferred to Phase 36.3.7.10' docs/kanban/reference.md) -eq 0 && test $(grep -c 'Shipped in Phase 36.3.7.10' docs/kanban/reference.md) -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - Grep gate (regression): `grep -c 'deferred to Phase 36.3.7.10' docs/kanban/reference.md == 0`
    - Grep gate (presence): `grep -c 'Shipped in Phase 36.3.7.10' docs/kanban/reference.md >= 2`
    - Source (§425 replacement names CLI verbs): `grep -c 'hermes kanban decompose' docs/kanban/reference.md >= 2` (one in the §425 block, one will be added in Task 2 at §609 — minimum `>= 1` after Task 1 alone)
    - Source (§425 replacement names v1 default override): `grep -c 'IronHermes v1' docs/kanban/reference.md >= 1` OR `grep -cE 'IronHermes.*default' docs/kanban/reference.md >= 1`
    - Source (§744 replacement preserves persona structure): `grep -c '| P9 Triage specifier |' docs/kanban/reference.md == 1` (row still exists, just with the status column updated)
    - Diff scope: `git diff --stat docs/kanban/reference.md` shows ONE file modified
  </acceptance_criteria>
  <done>
    Both v1-NOTE/deferred annotations are replaced with Shipped-in-Phase-36.3.7.10 wording that names the three production surfaces (CLI verbs, config knob, dispatcher Step 0) and the v1 scope-fence (LLM-tool surface + gateway slash arms quiescent). Grep regression gate passes. Doc-edit is scope-fenced to §425 + §744.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add `hermes kanban decompose` line to §609 CLI listing + annotate §444 config row for `auto_decompose` with the IronHermes v1 default override</name>
  <read_first>
    - docs/kanban/reference.md (read lines 600-615 for the §609 CLI listing region; read lines 438-455 for the §444 config table; confirm pre-edit content matches the <interfaces> excerpts)
    - .planning/phases/36.3.7.10-auto-decompose-triage-decomposer-specifier/36.3.7.10-RESEARCH.md (§Q6 — the exact `hermes kanban decompose` CLI surface to document; §Q7 — the IronHermes-default-`false` policy rationale)
  </read_first>
  <behavior>
    - After the edit, `grep -c 'hermes kanban decompose' docs/kanban/reference.md >= 3` (the §425 block from Task 1 + the new §609 listing line + at least one other mention from RESEARCH-derived text elsewhere).
    - The §609 listing has a new line ABOVE the existing `hermes kanban specify ...` line that reads `hermes kanban decompose [<id> | --all] [--tenant T] [--json]  # fan a triage task into a child swarm (Phase 36.3.7.10)`.
    - The §444 config row for `auto_decompose` is annotated to show both defaults (hermes-agent: `true`; IronHermes v1: `false`) and the rationale (conservative opt-in per Phase 36.3.7.10).
    - No other section of reference.md changes; the diff scope remains one file.
  </behavior>
  <action>
    Open `docs/kanban/reference.md`. Read lines 600-615 to confirm the §609 region matches the <interfaces> excerpt. Locate the line `hermes kanban specify [<id> | --all] [--tenant T]      # flesh out a triage-column idea`. Insert a new line IMMEDIATELY ABOVE it:

      ```
      hermes kanban decompose [<id> | --all] [--tenant T] [--json]   # fan a triage task into a child swarm (Phase 36.3.7.10)
      ```

    (Match the existing indentation + comment column alignment — use the same number of spaces between the closing `]` and the `#` as the surrounding lines; this is a code-block, alignment is human-readable.)

    Read lines 438-455 to confirm the §444 config table. Find the row `| \`auto_decompose\` | \`true\` | Dispatcher auto-runs the decomposer every tick. |` and replace with:

      ```
      | `auto_decompose` | `true` (hermes-agent ref) / `false` (IronHermes v1) | Dispatcher auto-runs the decomposer every tick. **Phase 36.3.7.10:** IronHermes ships `false` as the conservative opt-in default (per the gateway notifier + boards precedent); set to `true` in `config.yaml` to enable auto-mode. The gateway runner does NOT wire a production `DecomposeFn` in v1 — auto-mode is a no-op until a follow-up phase ships the wiring. |
      ```

    Run the count check: `grep -c 'hermes kanban decompose' docs/kanban/reference.md >= 3`. If less than 3, the §609 insertion did not land or a §425 grep miscounted — investigate before claiming done.

    Run the regression: `grep -c 'deferred to Phase 36.3.7.10' docs/kanban/reference.md == 0` (Task 1's gate must still hold).

    Final diff scope check: `git diff --stat docs/kanban/reference.md` still shows ONE file modified, ~10-15 lines total across Tasks 1 and 2.
  </action>
  <verify>
    <automated>test $(grep -c 'hermes kanban decompose' docs/kanban/reference.md) -ge 3 && test $(grep -c 'deferred to Phase 36.3.7.10' docs/kanban/reference.md) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Grep gate (CLI listing): `grep -c '^hermes kanban decompose ' docs/kanban/reference.md >= 1` (anchors on bareword start; the §609 listing is in a code block)
    - Grep gate (count includes §425 + §609): `grep -c 'hermes kanban decompose' docs/kanban/reference.md >= 3`
    - Grep gate (§444 annotation): `grep -c 'IronHermes v1' docs/kanban/reference.md >= 1`
    - Grep gate (Task 1 still holds): `grep -c 'deferred to Phase 36.3.7.10' docs/kanban/reference.md == 0`
    - Grep gate (Task 1 marker preserved): `grep -c 'Shipped in Phase 36.3.7.10' docs/kanban/reference.md >= 2`
    - Diff scope: `git diff --stat docs/kanban/reference.md` shows ONE file modified
    - File integrity: `wc -l docs/kanban/reference.md` differs from pre-edit count by <30 lines (additive only — the blockquote replacement may shrink slightly; the §609 addition adds 1 line; the §444 row may add 1 line if formatting wraps)
  </acceptance_criteria>
  <done>
    `hermes kanban decompose` is documented in the §609 CLI verb listing; the §444 config row records both the hermes-agent reference default (`true`) and the IronHermes v1 default (`false`) with the conservative-opt-in rationale; Task 1's grep gates still hold; the doc-edit scope is unchanged (one file).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc text → contributor onboarding | The reference.md is read by future contributors as source-of-truth; incorrect "Shipped" claims would mislead implementation decisions |
| Doc text → gsd-verifier doc-truth gate | The verifier greps reference.md to detect doc-truth drift; this plan retires the deferred markers so the gate stays clean |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.10-06-01 | Tampering | Replacement text incorrectly claims more shipped than was actually shipped | mitigate | Plan depends_on includes ALL prior plans (01-05); the SUMMARYs at .planning/phases/36.3.7.10-*/36.3.7.10-NN-SUMMARY.md are required reading; the replacement text only names surfaces that those SUMMARYs evidence as shipped. The v1 scope-fence (LLM-tool quiescent, gateway runner no-op, dashboard pill deferred) is explicitly named so the doc does not over-promise. |
| T-36.3.7.10-06-02 | Tampering | Replacement text leaks internal planning artifacts inappropriately | accept | The reference to `.planning/phases/36.3.7.10-*/` is a forward-pointer for future implementers; the planning dir is already in the repo and visible to anyone reading the docs |
| T-36.3.7.10-06-03 | Repudiation | Edit touches sections beyond §425/§444/§609/§744 | mitigate | Diff-scope acceptance criteria require `git diff --stat docs/kanban/reference.md` shows ONE file; verifier can additionally inspect the diff to confirm only the four scope-fenced regions changed |
| T-36.3.7.10-06-SC | Supply chain | No code change | accept | This plan is pure docs; no Cargo / runtime / test surface affected |
</threat_model>

<verification>
- `grep -c 'deferred to Phase 36.3.7.10' docs/kanban/reference.md == 0` (regression gate — primary success signal)
- `grep -c 'Shipped in Phase 36.3.7.10' docs/kanban/reference.md >= 2`
- `grep -c 'hermes kanban decompose' docs/kanban/reference.md >= 3`
- `grep -c 'IronHermes v1' docs/kanban/reference.md >= 1`
- `git diff --stat docs/kanban/reference.md` shows ONE file modified, additive line count <30
- `cargo build --workspace` exits 0 (sanity check — no accidental code change)
</verification>

<success_criteria>
`docs/kanban/reference.md` accurately reflects the Phase 36.3.7.10 shipped surfaces: §425 blockquote names CLI + config + dispatcher Step 0 surfaces with the v1 scope-fence; §609 lists the new `hermes kanban decompose` verb alongside the existing `specify`; §444 records both defaults (hermes-agent `true` / IronHermes v1 `false`) with rationale; §744 P9 Triage specifier row points to the implementation directory. The grep regression gate `grep -c 'deferred to Phase 36.3.7.10' docs/kanban/reference.md == 0` is the verifier's primary check.
</success_criteria>

<output>
Create `.planning/phases/36.3.7.10-auto-decompose-triage-decomposer-specifier/36.3.7.10-06-SUMMARY.md` with: (a) before/after grep counts for `deferred to Phase 36.3.7.10` (was 2, is 0) + `Shipped in Phase 36.3.7.10` (was 0, is >=2); (b) before/after grep counts for `hermes kanban decompose` (was 0 in user-facing surfaces, is >=3); (c) git diff stat showing one file modified; (d) phase-close note that all 10 REQ-36.3.7.10-NN requirements are satisfied across Plans 01-06.
</output>
