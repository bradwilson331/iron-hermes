# Plan Check: Phase 36.17.2.2 — IronHermes Telegram client delivers streaming final media messages

**Checker:** gsd-plan-checker (Revision Gate, Iteration 1 of 3)
**Date:** 2026-06-03
**Plans verified:** 7 (01–07)
**Verdict:** See end of document

---

## Dimension 1: Requirement Coverage

ROADMAP requirements: **D-01 through D-20** (all 20 decisions locked in CONTEXT.md).

| D-XX | Covering Plan(s) | Coverage Status |
|------|-----------------|-----------------|
| D-01 | 01, 04, 05, 07 | COVERED |
| D-02 | 04, 05, 07 | COVERED |
| D-03 | 05 | COVERED |
| D-04 | 01 | COVERED |
| D-05 | 02 | COVERED |
| D-06 | 02 | COVERED |
| D-07 | 06, 07 | COVERED |
| D-08 | 02, 06 | COVERED |
| D-09 | 02, 06, 07 | COVERED |
| D-10 | 06, 07 | COVERED |
| D-11 | 06, 07 | COVERED |
| D-12 | 04, 07 | COVERED |
| D-13 | 04 | COVERED |
| D-14 | 04, 07 | COVERED |
| D-15 | 04, 06, 07 | COVERED |
| D-16 | 02 | COVERED |
| D-17 | 03 | COVERED |
| D-18 | 03, 06 | COVERED |
| D-19 | 06 | COVERED |
| D-20 | 06, 07 | COVERED |

**Result: PASS.** All 20 D-XX decisions appear in at least one plan's `requirements` frontmatter field.

---

## Dimension 2: Task Completeness

| Plan | Tasks | Has Files | Has Action | Has Verify | Has Done | Status |
|------|-------|-----------|------------|------------|----------|--------|
| 01 | 3 | ✅ | ✅ | ✅ | ✅ | PASS |
| 02 | 3 | ✅ | ✅ | ✅ | ✅ | PASS |
| 03 | 2 | ✅ | ✅ | ✅ | ✅ | PASS |
| 04 | 4 | ✅ | ✅ | ✅ | ✅ | PASS |
| 05 | 2 | ✅ | ✅ | ✅ | ✅ | PASS |
| 06 | 5 | ✅ | ✅ | ✅ | ✅ | PASS — 5 tasks is borderline; see Dimension 5 |
| 07 | 2 (1 auto + 1 checkpoint) | ✅ | ✅ | ✅ | ✅ | PASS |

All tasks have concrete `<files>`, specific `<action>` descriptions with exact identifiers (method names, line numbers, regex patterns), runnable `<verify>` with `<automated>` commands, and measurable `<done>` criteria.

**Result: PASS.**

---

## Dimension 3: Dependency Correctness

Declared dependency graph from plan frontmatter:

```
01: depends_on: []          → Wave 0
02: depends_on: []          → Wave 0
03: depends_on: [02]        → Wave 0 (types from media_tag.rs)
04: depends_on: [01,02,03]  → Wave 0
05: depends_on: [01,04]     → Wave 1
06: depends_on: [02,03,04,05] → Wave 1
07: depends_on: [04,05,06]  → Wave 2
```

**Issue: Plan 03 is Wave 0 but depends on plan 02 (also Wave 0).** Within Wave 0, 01 and 02 can run parallel, but 03 must wait for 02 (needs `MediaRef`/`MediaSource`/`MediaKind` types). This is valid sequential dependency within the same wave label — the planner describes this as "Wave 0" for all four plans but with internal ordering. This is logically correct; 03 simply cannot execute until 02 completes even though they share the wave label. No cycle detected.

**Issue: Plan 04 lists `wave: 0` in frontmatter but depends on 01, 02, and 03.** Since 03 already depends on 02, and 04 depends on all three, 04 is actually Wave 1 in a strict wave model. However the planner's intent is that all four (01-04) are drafted in the same "structural reconciliation" group before Wave 1 (05, 06) begins. If execution is sequential per plan order, 01→02→03→04→05→06→07 satisfies all dependencies. No functional issue with execution.

No cycles. No missing plan references. All referenced plan IDs exist.

**Result: PASS** (with INFO note on wave labeling inconsistency for plans 03/04).

---

## Dimension 4: Key Links Planned

Traced critical wiring paths:

| Link | From | To | Via | Task | Status |
|------|------|----|-----|------|--------|
| escape fn → stream_consumer | markdown_v2.rs | stream_consumer.rs:100 | import + call in final-edit branch | Plan 05 Task 2 | COVERED |
| escape fn → telegram.rs | markdown_v2.rs | edit_message_markdown_v2 body | import + applied to content | Plan 04 Task 2 | COVERED |
| MediaTagExtractor → stream_callback | media_tag.rs | handler.rs run_agent | chained feed in closure | Plan 06 Task 2 | COVERED |
| MediaSender → TelegramAdapter | adapter.rs | telegram.rs | impl block | Plan 04 Task 4 | COVERED |
| set_media_sender → runner.rs | handler.rs | runner.rs | clone-cast wire-up | Plan 06 Task 4 | COVERED |
| D-19 dispatch → after consumer+typing await | extractor | handler.rs:1443–1445 | sequential await barriers | Plan 06 Task 3 | COVERED |
| send_file_multipart → MessageResponse | telegram.rs | adapter.rs | type refactor | Plan 04 Task 1 | COVERED |
| pub mod markdown_v2 → lib.rs | markdown_v2.rs | lib.rs | pub mod declaration | Plan 01 Task 3 | COVERED |
| pub mod media_tag → lib.rs | media_tag.rs | lib.rs | pub mod declaration | Plan 02 Task 3 | COVERED |
| MediaSender type re-export → adapter.rs | media_tag.rs | adapter.rs | pub use | Plan 03 Task 1 | COVERED |

All must_haves key_links entries have implementing tasks. The wiring chain from LLM output → scrubber → extractor → stream_consumer → MediaSender → Telegram is fully planned.

**Result: PASS.**

---

## Dimension 5: Scope Sanity

| Plan | Tasks | Files Modified | Estimated Complexity | Status |
|------|-------|----------------|---------------------|--------|
| 01 | 3 | 2 | Medium (TDD, state machine) | PASS |
| 02 | 3 | 2 | High (TDD, complex state machine) | PASS |
| 03 | 2 | 2 | Low | PASS |
| 04 | 4 | 6 | HIGH — structural reconciliation of 6 files | WARNING |
| 05 | 2 | 3 | Medium | PASS |
| 06 | 5 | 3 | HIGH — handler wiring + integration test | WARNING (borderline) |
| 07 | 2 | 1 | Low | PASS |

**Plan 04** touches 6 files across 4 tasks. The planner explicitly justifies this co-location (task 4 action: "These changes share telegram.rs lines plus the PlatformAdapter trait surface — splitting them creates merge conflicts and forces a sequential wave where Wave 0 should run parallel"). The rationale is sound; a split would cause cascading compile failures. This is a reasonable exception.

**Plan 06** has 5 tasks with 3 files. The task count is at the blocker threshold but: (a) handler.rs tasks 1–3 are logically sequential steps on the same function; (b) task 4 is a 1-line runner.rs add; (c) task 5 is a separate test file. Five tasks is the maximum; no further splitting is recommended without breaking the functional unity of the D-19 dispatch.

**Result: WARNING on Plan 04 (6 files, justified) and Plan 06 (5 tasks, borderline). Not blockers given justifications, but executor should be alert to context budget.**

---

## Dimension 6: Verification Derivation

All plans carry `must_haves` with truths, artifacts, and key_links:

- **Truths** are user/system-observable: "cargo test ... is green", "edit arrives before send_*", "tags inside fences pass through verbatim", "operator approved gate". Not implementation-focused.
- **Artifacts** map to truths with `contains` assertions for source-level verification.
- **Key_links** connect all critical wiring points with named `via` methods and `pattern` regexes.

Phase-level must-haves 1–10 are distributed across plan frontmatter:
- MH-1 (markdown_v2 test green) → Plan 01
- MH-2 (media_tag test green) → Plan 02
- MH-3 (telegram_media_delivery test green) → Plan 06
- MH-4 (workspace green) → Plan 06
- MH-5 (MediaSender trait 6 methods) → Plan 03
- MH-6 (impl MediaSender for TelegramAdapter) → Plan 04
- MH-7 (D-19 dispatch at corrected anchor) → Plan 06
- MH-8 (stream_consumer uses edit_message_markdown_v2) → Plan 05
- MH-9 (telegram_media_uat.md exists with 9 scenarios) → Plan 07
- MH-10 (no D-03 regression) → Plan 05

**Result: PASS.**

---

## Dimension 7: Context Compliance

CONTEXT.md has 20 locked decisions (D-01–D-20), Claude's Discretion items, and Deferred Ideas.

**Locked Decisions — all 20 implemented:**
- D-01 through D-20: each has one or more implementing tasks verified in Dimension 1.

**Deferred Ideas — none included in plans:**
- Checked: sendMediaGroup (not planned), sendSticker/sendAnimation (not planned), inbound voice/video (not planned), Discord/Slack MediaSender impls (not planned), per-tag caption opt-in (not planned). No scope creep detected.

**Claude's Discretion items:**
- `MediaTagExtractor` location → Plan 02 chose `crates/ironhermes-gateway/src/media_tag.rs` (CONTEXT.md recommended location). Valid.
- `MediaSource`/`MediaKind`/`MediaRef` location → Plan 02 chose gateway-only (CONTEXT.md recommended). Valid.
- `#[async_trait]` vs `Pin<Box<dyn Future>>` → Plans 03/04 use `#[async_trait]` (established pattern). Valid.
- Overflow-chunk MarkdownV2 path → Plan 05 implements `send_message_markdown_v2` for all overflow chunks (CONTEXT.md recommended). Valid.
- Reinsert-on-failure pattern → Plan 06 edits placeholder via `editMessageText` to append tag text (CONTEXT.md recommended). Valid.

**Result: PASS.**

---

## Dimension 7b: Scope Reduction Detection

Scanned all 7 plan `<action>` sections for scope-reduction language:

- No "v1", "simplified", "static for now", "hardcoded", "future enhancement", "placeholder", "stub", "not wired to", "will be wired later" language found in any action.
- Plan 04 Task 3 includes a deliberate intermediate state: "Do NOT apply `escape_outside_code_blocks` to `&remaining` here — that escape-application is plan 05's job." This is NOT scope reduction — it is explicit handoff between sequential plans that both execute, with the full delivery confirmed in plan 05.
- Plan 06 Task 3 contains one note: "The executor must locate the final-body source in scope... If a public accessor does not exist, add one as `pub fn final_body(&self) -> &str { &self.buffer }`." This is concrete instruction for an open question at plan-time, with a resolution path specified. Not scope reduction.

**Result: PASS.**

---

## Dimension 7c: Architectural Tier Compliance

RESEARCH.md does not contain an `## Architectural Responsibility Map` section.

**Result: SKIPPED (no responsibility map found).**

---

## Dimension 8: Nyquist Compliance

VALIDATION.md exists at `.planning/phases/36.17.2.2-.../36.17.2.2-VALIDATION.md`. Gate 8e: PASS.

### Check 8a — Automated Verify Presence

| Task | Plan | Wave | Automated Command | Status |
|------|------|------|-------------------|--------|
| Task 1 (golden tests RED) | 01 | 0 | `cargo test -p ironhermes-gateway --lib markdown_v2` | ✅ |
| Task 2 (implement GREEN) | 01 | 0 | `cargo test -p ironhermes-gateway --lib markdown_v2` | ✅ |
| Task 3 (wire lib.rs) | 01 | 0 | `cargo build -p ironhermes-gateway && cargo test ... markdown_v2` | ✅ |
| Task 1 (extractor RED) | 02 | 0 | `cargo build -p ironhermes-gateway --tests` | ✅ |
| Task 2 (implement GREEN) | 02 | 0 | `cargo test -p ironhermes-gateway --lib media_tag` | ✅ |
| Task 3 (wire lib.rs) | 02 | 0 | `cargo build -p ironhermes-gateway && cargo test ... media_tag && markdown_v2` | ✅ |
| Task 1 (MediaSender trait) | 03 | 0 | `cargo build -p ironhermes-gateway && grep count` | ✅ |
| Task 2 (prompt_builder) | 03 | 0 | `cargo build -p ironhermes-agent && grep check` | ✅ |
| Task 1 (send_file_multipart) | 04 | 0 | `cargo build -p ironhermes-gateway && cargo build -p ironhermes-cron` | ✅ |
| Task 2 (edit_message_markdown_v2) | 04 | 0 | `rg fn edit_message_markdown + rg fn edit_message_markdown_v2` | ✅ |
| Task 3 (stream_consumer rename) | 04 | 0 | `cargo build -p ironhermes-gateway --tests` | ✅ |
| Task 4 (impl MediaSender) | 04 | 0 | `cargo build -p ironhermes-gateway && grep count` | ✅ |
| Task 1 (send_message_markdown_v2) | 05 | 1 | `cargo build -p ironhermes-gateway --tests && rg count` | ✅ |
| Task 2 (escape wiring) | 05 | 1 | `cargo test -p ironhermes-gateway --lib stream_consumer` | ✅ |
| Task 1 (field + setter) | 06 | 1 | `cargo build -p ironhermes-gateway --tests` | ✅ |
| Task 2 (extractor wiring) | 06 | 1 | `cargo build -p ironhermes-gateway && grep counts` | ✅ |
| Task 3 (D-19 dispatch) | 06 | 1 | `cargo build -p ironhermes-gateway && grep counts` | ✅ |
| Task 4 (runner.rs wire) | 06 | 1 | `cargo build -p ironhermes-gateway && grep counts` | ✅ |
| Task 5 (integration test) | 06 | 1 | `cargo test -p ironhermes-gateway --test telegram_media_delivery` | ✅ |
| Task 1 (UAT runbook) | 07 | 2 | `test -f ... && grep counts` | ✅ |
| Task 2 (checkpoint:human-verify) | 07 | 2 | Manual — operator `approved` | ✅ (manual checkpoint, correct type) |

### Check 8b — Feedback Latency

All automated commands are one-shot `cargo test` or `cargo build` commands. No watch-mode flags. No E2E suite. Sub-second to ~30s per command.

**Result: PASS.**

### Check 8c — Sampling Continuity

No window of 3 consecutive tasks without `<automated>` verify exists.

**Result: PASS.**

### Check 8d — Wave 0 Completeness

VALIDATION.md explicitly lists all Wave 0 gaps:
- `markdown_v2.rs` skeleton → Plan 01 Task 1 (creates file with stubs + tests)
- `media_tag.rs` skeleton → Plan 02 Task 1 (creates file with stubs + tests)
- `tests/telegram_media_delivery.rs` → Plan 06 Task 5
- `tests/telegram_media_uat.md` → Plan 07 Task 1
- `send_file_multipart` refactor → Plan 04 Task 1
- `edit_message_markdown_v2` strategy → Plan 04 Task 2
- Overflow-chunk path → Plan 05
- `send_image_file` rename decision → Plan 04 Task 1 (explicitly defers rename, uses sibling method)
- Synthetic LLM injection → Plan 06 Task 5 (Open Q2 resolution path specified)

All `MISSING` Wave 0 references in VALIDATION.md have matching plans.

**Result: PASS.**

### Dimension 8 Overall: PASS

---

## Dimension 9: Cross-Plan Data Contracts

Key shared data flows:

1. **`escape_outside_code_blocks` output → stream_consumer AND telegram.rs**: Plan 01 creates the function. Plan 05 applies it at stream_consumer:100. Plan 04 applies it inside `edit_message_markdown_v2`. Both consumers call it on the same body type (`&str`, returns `String`). No conflict.

2. **`send_file_multipart` return type change**: Plan 04 changes `Result<()>` → `Result<MessageResponse>`. The 4 `TgSendApi` callers are updated in the same task (either changed or `.map(|_| ())`-wrapped). No downstream consumer left with wrong expectation.

3. **`edit_message_markdown` → `edit_message_markdown_v2` rename**: Plan 04 Task 2 renames the trait and ALL impls (TelegramAdapter, MockAdapter, RecordingFailingAdapter, all test files). Plan 04 Task 3 renames the only remaining production call site (stream_consumer:100). Compile-time enforcement guarantees completeness. No contract mismatch possible.

4. **`MediaRef.original_tag_text`**: Plan 02 defines this field. Plan 06 Task 3 uses `media_ref.original_tag_text.clone()` in the reinsert path. The field is defined as `pub original_tag_text: String` in the `<interfaces>` block of Plan 02 and is populated by the extractor's body-capture logic. Consistent.

5. **Chained scrubber→extractor in stream_callback**: Plan 06 Task 2 chains as `extractor.feed(&scrubber.feed(delta))`. Both take `&str`, return `String`. The output of scrubber's `feed` is fed into extractor's `feed`. Type-compatible. The dual-tail flush (Plan 06 Task 2 action item (c)) correctly feeds `scrubber_tail` through `extractor.feed()` before calling `extractor.flush_tail()`. No data contract conflict.

**Result: PASS.**

---

## Dimension 10: CLAUDE.md Compliance

CLAUDE.md specifies:
- Use cheapest model for subagents (not applicable to plans)
- No cross-crate spawning restrictions
- Preferred tools list (not applicable to Rust code plans)

No plan introduces forbidden patterns, wrong testing frameworks, or architectural violations per CLAUDE.md.

**Result: PASS.**

---

## Dimension 11: Research Resolution

RESEARCH.md has an `## Open Questions` section (lines 523–546). The section heading does NOT have `(RESOLVED)` suffix. Five questions are listed:

1. Verify `prompt_builder.rs:774` — RESEARCH says "verify-during-execute" but Plan 03 Task 2 explicitly addresses it with concrete acceptance criteria.
2. How are synthetic LLM streams injected? — Plan 06 Task 5 gives a resolution path (`rg -n 'impl.*Provider.*for' crates/` + `FakeStreamingProvider` if none exists).
3. D-17 divergence documentation — Plan 03 Task 1 puts it in a doc-comment.
4. MediaSender Arc construction — Plan 06 Task 4 uses the clone-cast-twice pattern.
5. MediaTagExtractor wire placement — Plan 06 Task 2 uses scrubber→extractor chain.

**The section heading lacks `(RESOLVED)` suffix, but every question has a concrete resolution path in the plans.** Per the dimension specification: "If section heading has `(RESOLVED)` suffix → PASS". The suffix is absent.

However, this dimension asks whether plans address the questions — and they do, with concrete task actions. The technical risk is that Open Q2 (synthetic LLM injection) says "investigate first, build FakeStreamingProvider if needed" which is a Wave 0 discovery task inside Plan 06 Task 5. This is an open implementation question that could become a blocker during execution if the Provider mock doesn't exist and building FakeStreamingProvider takes significantly more effort than estimated.

**This is a WARNING, not a BLOCKER.** The plans acknowledge the uncertainty and provide a clear resolution path. The RESEARCH.md section header missing `(RESOLVED)` is a documentation gap, not a plan gap.

---

## Dimension 12: Pattern Compliance

PATTERNS.md exists. All 12 file entries have analogs mapped:

| File | Analog | Plan References Analog | Status |
|------|--------|----------------------|--------|
| markdown_v2.rs | streaming_scrubber.rs (module/test layout) | Plan 01 read_first items + action | ✅ |
| media_tag.rs | StreamingContextScrubber (full mirror) | Plan 02 read_first items + action | ✅ |
| adapter.rs (MediaSender trait) | PlatformAdapter (same file) | Plan 03 read_first + interfaces | ✅ |
| telegram.rs (send_audio) | send_voice/send_image_file/send_video/send_document | Plan 04 Task 1 action | ✅ |
| telegram.rs (impl MediaSender) | impl PlatformAdapter + impl TgSendApi | Plan 04 Task 4 read_first | ✅ |
| telegram.rs (send_file_multipart refactor) | send_message projection pattern | Plan 04 Task 1 action | ✅ |
| telegram.rs (edit_message_markdown_v2) | edit_message_markdown (lines 250-265) | Plan 04 Task 2 read_first | ✅ |
| handler.rs (setter + dispatch) | set_session_queue / set_user_queue_manager | Plan 06 Task 1 read_first | ✅ |
| runner.rs (wire) | handler.set_session_queue / set_user_queue_manager | Plan 06 Task 4 read_first | ✅ |
| stream_consumer.rs (final-edit switchover) | Line 100 call (analog is the existing call) | Plan 05 Task 2 read_first | ✅ |
| tests/telegram_media_delivery.rs | uqm_session_queue_unification.rs | Plan 06 Task 5 read_first | ✅ |
| tests/telegram_media_uat.md | session_queue_telegram_uat.md | Plan 07 Task 1 read_first | ✅ |

Shared patterns applied:
- `#[async_trait]` → Plans 03, 04, 06 all reference it explicitly
- Setter-based handler wiring → Plan 06 Task 1
- Arc dyn-dispatch construction (avoid upcasting) → Plans 03, 06
- Log + visible-to-user signal → Plans 04 Task 4, 06 Task 3
- `Arc<Mutex<...>>` for per-turn state machines → Plan 06 Task 2
- Rust integration-test binary isolation → Plans 06 Task 5

**Result: PASS.**

---

## Critical Anchor Check: D-19 Dispatch Placement

**This is the single highest-stakes verification item.**

Plan 06 Task 3 `<action>` explicitly states:

> "Insert a new code block between handler.rs lines 1443 (`typing_handle.await.ok();`) and 1445 (`match agent_result {`)."

Plan 06 `must_haves` truths state:

> "After `consumer_handle.await.ok()` AND `typing_handle.await.ok()` complete (handler.rs:1439 + :1443) AND BEFORE the `match agent_result` arm at :1445, the D-19 dispatch loop runs"

Plan 06 `key_links` states:

> "insertion AFTER both await.ok() barriers AND BEFORE match agent_result" with pattern `consumer_handle\.await\.ok|typing_handle\.await\.ok|match agent_result`

Plan 06 Task 3 `acceptance_criteria` includes:

> `` `awk '/typing_handle\.await\.ok\(\);/{print NR" typing"; t=NR} /take_attachments\(\)/{print NR" take"; k=NR} /match agent_result/{print NR" match"; m=NR} END{exit !(t<k && k<m)}' crates/ironhermes-gateway/src/handler.rs` exits 0``

This `awk` ordering assertion is machine-verifiable and directly tests the corrected anchor. PATTERNS.md §8 and RESEARCH.md both confirm the stale CONTEXT.md anchor (`after stream_consumer.flush(true).await?`) does not exist as an inline call. The plan uses the corrected anchor throughout with zero ambiguity.

**CRITICAL ANCHOR CHECK: PASS.**

---

## Security Threat Model Check

Per the `<security_check>` specification:

### T-INPUT-MEDIA-PATH (HIGH)

Plan 04 Task 4 action explicitly implements:
- `Path::canonicalize()?` before multipart upload
- Allowed-roots check against `HERMES_HOME` and `/tmp`
- Returns typed `Err` on disallowed path
- Acceptance criteria: `grep -c '\.canonicalize()' crates/ironhermes-gateway/src/telegram.rs` returns at least `1`

Threat model in Plan 04 explicitly lists this as `mitigate`. **COVERED.**

### T-INPUT-MEDIA-URL (MEDIUM)

Plan 04 Task 4 action explicitly implements:
- Scheme gate: reject if not `http://` or `https://`
- Acceptance criteria: `grep -E 'starts_with\("https?://"\)' crates/ironhermes-gateway/src/telegram.rs | wc -l` returns at least `1`

Threat model in Plan 04 explicitly lists as `mitigate`. **COVERED.**

### T-MD-INJECTION (LOW)

Plans 01 (golden tests), 04/05 (D-02 fallback), and threat models in Plans 01, 04, 05. **COVERED.**

### T-LOG-LEAK (LOW)

Plan 04 Task 4 action: "log the FILENAME ONLY per the phase threat model (do not log the full path to avoid T-LOG-LEAK)"
Acceptance criteria: `grep -E 'file_name\(\)' crates/ironhermes-gateway/src/telegram.rs | wc -l` returns at least `1`
**COVERED.**

### T-UAT-MANUAL (informational)

Plan 07 Task 2 is `checkpoint:human-verify`. **COVERED.**

### Threat model `<threat_model>` presence per plan:

| Plan | Has `<threat_model>` block | STRIDE Register | Status |
|------|---------------------------|-----------------|--------|
| 01 | ✅ | T-MD-INJECTION, T-MD-PARSEFAIL | PASS |
| 02 | ✅ | T-EXTRACT-NONASCII-PANIC, T-EXTRACT-FENCE-BYPASS, T-EXTRACT-ESCAPED-TICK, T-EXTRACT-MEM-LEAK | PASS |
| 03 | ✅ | T-PROMPT-CACHE-INVALIDATION, T-TRAIT-UPCAST-MISUSE, T-INPUT-MEDIA-PATH, T-PROMPT-INJECTION | PASS |
| 04 | ✅ | T-INPUT-MEDIA-PATH (HIGH), T-INPUT-MEDIA-URL (MEDIUM), T-MD-INJECTION, T-LOG-LEAK, T-RIPPLE-BREAK, T-D02-INFINITE-RETRY | PASS |
| 05 | ✅ | T-MD-INJECTION-CALLSITE, T-D03-REGRESSION, T-OVERFLOW-INCONSISTENT, T-RIPPLE-MISS-IMPL | PASS |
| 06 | ✅ | T-D19-ANCHOR-MISPLACEMENT (HIGH), T-D19-MISSING-MEDIA-SENDER, T-D10-REINSERT-LOOP, T-INPUT-MEDIA-PATH, T-RUNNER-UPCAST-MISUSE, T-TEST-FAKE-PROVIDER-LEAK, T-D02-RETRY-NOT-OBSERVABLE | PASS |
| 07 | ✅ | T-UAT-MANUAL, T-UAT-PROD-CONFIG-LEAK, T-UAT-BOT-TOKEN-IN-LOGS, T-UAT-MEDIA-UPLOAD-LEAKS, T-UAT-SKIPPED-SCENARIO | PASS |

Security mitigations appear as `<acceptance_criteria>` source/behavior assertions (not just prose) in Plans 04 and 06.

**SECURITY THREAT MODEL CHECK: PASS.**

---

## Must-Haves Traceability (Phase-Level)

| Must-Have | Covered By | Verified Via |
|-----------|-----------|-------------|
| 1. `cargo test -p ironhermes-gateway markdown_v2` green | Plan 01 | Plan 01 Task 2/3 acceptance criteria |
| 2. `cargo test -p ironhermes-gateway media_tag` green | Plan 02 | Plan 02 Task 2/3 acceptance criteria |
| 3. `cargo test -p ironhermes-gateway --test telegram_media_delivery` green | Plan 06 | Plan 06 Task 5 acceptance criteria |
| 4. `cargo test --workspace` green | Plan 06 | Plan 06 Task 5 acceptance criteria (workspace assertion) |
| 5. MediaSender trait with 6 D-18 method signatures | Plan 03 | Plan 03 Task 1 acceptance criteria (≥6 async fn count) |
| 6. `impl MediaSender for TelegramAdapter` | Plan 04 | Plan 04 Task 4 acceptance criteria |
| 7. D-19 dispatch at CORRECTED anchor | Plan 06 | Plan 06 Task 3 awk ordering assertion |
| 8. stream_consumer uses edit_message_markdown_v2 | Plan 05 | Plan 05 Task 2 acceptance criteria |
| 9. `tests/telegram_media_uat.md` with 9 scenarios + `approved` gate | Plan 07 | Plan 07 Task 1/2 acceptance criteria |
| 10. No D-03 regression | Plan 05 | Plan 05 Task 2 intermediate-edit assertions; CURSOR count diff assertion |

**All 10 must-haves are traced to specific tasks with machine-verifiable acceptance criteria.**

---

## VALIDATION.md Completeness

VALIDATION.md has a per-task verification map with "TBD by planner" placeholders in all rows. The planner explicitly delegated population to the executor:

> "Per-Task Verification Map populated by planner (one row per task)" — listed as a pending sign-off item.

Plan 06 Task 5 action item (f) explicitly tasks the executor with populating those rows:

> "Populate the per-task verification map rows in VALIDATION.md — replace the TBD-by-planner rows with one row per test from this task."

Plan 06 Task 5 acceptance criteria includes:

> "`grep -c 'TBD by planner' .planning/.../36.17.2.2-VALIDATION.md` returns `0` after the executor populates the per-task verification map"

The planner's note in VALIDATION.md states "Planner-owned. This table is the canonical Nyquist sampling map. The planner MUST populate it during plan generation." This is a tension: the planner deferred the full population to the executor, but the table exists with partial structure and the executor is explicitly instructed to complete it.

Per plan-check instruction: "Acceptable per planner's note (executor populates during execution). Verify plan 06 (or wherever) explicitly tasks the executor with populating those rows." Plan 06 Task 5 (f) does exactly this.

**Result: WARNING (acceptable per the check specification — the delegation is explicit and the executor has a machine-verifiable acceptance criterion to confirm completion).**

---

## Cargo Command Reality Check

All `cargo test` commands in acceptance criteria:

- `cargo test -p ironhermes-gateway markdown_v2` — valid; filters by test name substring
- `cargo test -p ironhermes-gateway --lib markdown_v2` — valid; restricts to lib tests only
- `cargo test -p ironhermes-gateway --lib media_tag` — valid
- `cargo test -p ironhermes-gateway media_tag` — valid
- `cargo build -p ironhermes-gateway --tests` — valid; compiles test binaries
- `cargo build -p ironhermes-cron` — valid; separate crate build
- `cargo test -p ironhermes-gateway --test telegram_media_delivery` — valid; names the integration test binary
- `cargo test -p ironhermes-gateway` — valid; full gateway suite
- `cargo test --workspace` — valid; workspace-wide
- `cargo build -p ironhermes-agent` — valid

All commands use valid `cargo` syntax for this workspace. The `--test <name>` flag correctly references integration test binaries (separate files in `tests/`). The `-p ironhermes-gateway` package flag is consistent with the crate structure.

**Result: PASS.**

---

## Issues Summary

### Blockers (must fix before execution)

**None found.**

### Warnings (should fix, execution can proceed)

**W-01 [scope_sanity] Plan 04 touches 6 files in 4 tasks**
- Plan: 04
- Metrics: 4 tasks, 6 files (adapter.rs, telegram.rs, stream_consumer.rs, 3 test files)
- Planner justification is sound (co-location prevents merge conflicts and cascading compile failures in Wave 0)
- Fix: Executor should be aware of context budget; these changes are mechanical (search-replace renames) so risk is low.

**W-02 [scope_sanity] Plan 06 has 5 tasks**
- Plan: 06
- Metrics: 5 tasks, 3 files
- Tasks 1–4 are logically sequential sub-steps of handler.rs wiring; task 5 is the integration test in a separate file
- Fix: Executor should watch context budget. If task 5 (integration test) is complex due to FakeStreamingProvider, consider whether it should be split off.

**W-03 [research_resolution] RESEARCH.md `## Open Questions` section lacks `(RESOLVED)` suffix**
- File: 36.17.2.2-RESEARCH.md
- All questions have concrete resolution paths in plans; the RESOLVED marker is a documentation gap
- Fix: Add `(RESOLVED)` to the section heading or add inline RESOLVED markers per question before plan execution begins.

**W-04 [dependency_correctness] Wave labeling inconsistency for plans 03 and 04**
- Plans 03 (depends_on: [02]) and 04 (depends_on: [01,02,03]) are labeled `wave: 0` but have intra-wave dependencies
- Execution is unambiguous (sequential per plan number satisfies all deps), but the wave label could confuse a parallel executor
- Fix: Either accept sequential Wave 0 execution or label 03 as Wave 0b and 04 as Wave 0c, or simply execute them sequentially.

**W-05 [nyquist] VALIDATION.md per-task map deferred to executor**
- VALIDATION.md contains TBD-by-planner placeholders
- Plan 06 Task 5 explicitly tasks the executor with populating them with a machine-verifiable acceptance criterion
- Fix: Acceptable as-is per plan-check specification; executor must complete population before phase exit.

### Info (suggestions)

**I-01 [open_question] Synthetic LLM injection mechanism (Open Q2) is unresolved**
- Plan 06 Task 5 provides a discovery path (`rg -n 'impl.*Provider.*for' crates/`) and a fallback (build `FakeStreamingProvider`)
- If `FakeStreamingProvider` needs to be built, this adds unplanned work to Plan 06 Task 5 — potentially the largest hidden scope in the phase
- Recommendation: Executor run the `rg` discovery as Plan 06 Task 5's first micro-step and report back before proceeding if no mock Provider exists.

**I-02 [open_question] `StreamConsumer::final_body()` accessor needed for D-10 reinsert**
- Plan 06 Task 3 acknowledges this gap and provides two resolution paths (accessor or oneshot channel)
- Either path is implementable; the plan is explicit about the decision point
- Recommendation: Executor check `StreamConsumer` for an existing `buffer` accessor before adding one.

---

## Overall Verdict

All 13 specified checks passed or produced only warnings. No blocking issues found.

| Check | Result |
|-------|--------|
| 1. Goal alignment (10 must-haves traced) | PASS |
| 2. D-XX coverage (all 20 decisions in plan requirements) | PASS |
| 3. D-19 anchor correction (CRITICAL) | PASS — corrected anchor explicitly verified |
| 4. Wave dependency correctness | PASS (WARNING on wave labeling) |
| 5. TDD discipline (Plans 01/02 tests-first ordering) | PASS |
| 6. `<read_first>` completeness | PASS |
| 7. `<acceptance_criteria>` testability | PASS |
| 8. `<action>` concreteness | PASS |
| 9. `must_haves` field coverage | PASS |
| 10. No-regression check (Plan 05 D-03 preservation) | PASS |
| 11. Security threat_model | PASS |
| 12. VALIDATION.md update delegation | PASS (WARNING on TBD rows) |
| 13. Cargo command reality | PASS |

---

## PLAN CHECK PASS
