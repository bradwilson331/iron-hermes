---
phase: 36.17.2.2
plan: 06
type: execute
wave: 1
depends_on:
  - 36.17.2.2-02
  - 36.17.2.2-03
  - 36.17.2.2-04
  - 36.17.2.2-05
files_modified:
  - crates/ironhermes-gateway/src/handler.rs
  - crates/ironhermes-gateway/src/runner.rs
  - crates/ironhermes-gateway/tests/telegram_media_delivery.rs
autonomous: true
requirements:
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-15
  - D-18
  - D-19
  - D-20
user_setup: []

must_haves:
  truths:
    - "`GatewayMessageHandler` carries a `media_sender: Option<Arc<dyn MediaSender>>` field at handler.rs:170-ish, initialized `None` in `new()` at :222-ish."
    - "`GatewayMessageHandler::set_media_sender(&mut self, sender: Arc<dyn MediaSender>)` exists at handler.rs ~:358 (sibling of `set_session_queue` / `set_user_queue_manager`)."
    - "`MediaTagExtractor` is wired alongside `StreamingContextScrubber` inside `run_agent`; the stream callback chains them as `extractor.feed(&scrubber.feed(delta))` per Open Q5 / Assumption A10."
    - "After `consumer_handle.await.ok()` AND `typing_handle.await.ok()` complete (handler.rs:1439 + :1443) AND BEFORE the `match agent_result` arm at :1445, the D-19 dispatch loop runs: drain `extractor.take_attachments()`, iterate, call `media_sender.send_media(...)`. Anchor CORRECTED from stale CONTEXT.md per RESEARCH FLAGGED RISK / Pitfall 6."
    - "On `send_media` Err OR D-15-typed oversize Err, tag literals accumulate into `failed_tags: Vec<String>` and append via ONE combined `adapter.edit_message_markdown_v2(...)` call (one edit per turn, not one per failure) per D-10 + D-19."
    - "When `media_sender` is `None` (non-Telegram platforms) AND extractor produced tags, a single `warn!('media tags emitted on platform without MediaSender: {} refs dropped', n)` log fires; tags drop silently on user side (D-18)."
    - "`runner.rs` Telegram start path calls `handler.set_media_sender(telegram.clone() as Arc<dyn MediaSender>)` via the clone-cast pattern per RESEARCH Open Q4 / Assumption A7."
    - "Discord/Slack/web start paths do NOT call `set_media_sender` — field stays `None` (D-18 contract)."
    - "`crates/ironhermes-gateway/tests/telegram_media_delivery.rs` exists with `RecordingMediaAdapter` succeeding-adapter fixture and ≥6 named tests covering D-07/D-09/D-10/D-11/D-15."
    - "`cargo test -p ironhermes-gateway --test telegram_media_delivery` is green; `cargo test --workspace` is green."
  artifacts:
    - path: "crates/ironhermes-gateway/src/handler.rs"
      provides: "`media_sender` field + `set_media_sender` setter + extractor wired into stream callback + D-19 dispatch loop at corrected anchor."
      contains: "MediaTagExtractor"
    - path: "crates/ironhermes-gateway/src/runner.rs"
      provides: "Telegram start path clone-casts adapter to `Arc<dyn MediaSender>` and wires it via `set_media_sender`."
      contains: "set_media_sender"
    - path: "crates/ironhermes-gateway/tests/telegram_media_delivery.rs"
      provides: "Integration test surface covering D-07/D-09/D-10/D-11/D-15; `RecordingMediaAdapter` fixture; helpers copied per Rust integration-test binary isolation."
      contains: "RecordingMediaAdapter"
  key_links:
    - from: "crates/ironhermes-gateway/src/handler.rs D-19 dispatch block"
      to: "handler.rs lines 1439 + 1443 + 1445"
      via: "insertion AFTER both await.ok() barriers AND BEFORE match agent_result"
      pattern: "consumer_handle\\.await\\.ok|typing_handle\\.await\\.ok|match agent_result"
    - from: "crates/ironhermes-gateway/src/handler.rs extractor wiring"
      to: "handler.rs:1333-1343 scrubber wire-up"
      via: "chain extractor.feed(&scrubber.feed(delta)) inside stream_callback closure"
      pattern: "scrubber_gw_cb|extractor_cb"
    - from: "crates/ironhermes-gateway/src/runner.rs Telegram start"
      to: "Arc<TelegramAdapter> construction site"
      via: "clone-cast to Arc<dyn MediaSender>; NO trait upcasting from Arc<dyn PlatformAdapter>"
      pattern: "as Arc<dyn .*MediaSender>"
---

<objective>
Wire everything together inside `handler.rs::run_agent` and `runner.rs` Telegram start path; ship the integration test that verifies the D-07/D-09/D-10/D-11/D-15 end-to-end behavior. All prior plans produced the parts; this plan assembles them.

Purpose: D-19 is the central runtime change. RESEARCH flagged its anchor as HIGH-risk because the CONTEXT.md anchor (`after stream_consumer.flush(true).await?`) is wrong — there is no such inline call in `run_agent`. CORRECT anchor: AFTER both `consumer_handle.await.ok()` at :1439 AND `typing_handle.await.ok()` at :1443, BEFORE `match agent_result` at :1445. Plan 06 fixes the anchor and lands the dispatch + composition. The integration test layer (D-20 unit + integration leg) lands here; the manual UAT runbook (D-20 final leg) lands in plan 07.
Output: handler.rs orchestrates the media pipeline; runner.rs wires Telegram; new integration test asserts the end-to-end shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-CONTEXT.md
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-VALIDATION.md
@crates/ironhermes-gateway/src/handler.rs
@crates/ironhermes-gateway/src/runner.rs
@crates/ironhermes-gateway/src/adapter.rs
@crates/ironhermes-gateway/src/telegram.rs
@crates/ironhermes-gateway/src/media_tag.rs
@crates/ironhermes-gateway/src/markdown_v2.rs
@crates/ironhermes-gateway/src/stream_consumer.rs
@crates/ironhermes-gateway/tests/uqm_session_queue_unification.rs
@.planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-CONTEXT.md
</context>

<interfaces>
Post-plan-06 runtime contract:

- `GatewayMessageHandler` struct gains `media_sender: Option<Arc<dyn MediaSender>>` field + `set_media_sender(&mut self, sender: Arc<dyn MediaSender>)` setter.
- `run_agent` constructs per-turn `Arc<Mutex<MediaTagExtractor>>` next to the existing scrubber. The `stream_callback` chains them: `let scrubbed = scrubber_cb.lock().unwrap().feed(delta); let visible = extractor_cb.lock().unwrap().feed(&scrubbed); if !visible.is_empty() { let _ = stream_tx_clone.try_send(visible); }`.
- After stream end: both tails flush. Scrubber tail is also fed through extractor so MEDIA tags inside `<memory-context>` spans are consistently dropped: `let scrubber_tail = scrubber_gw.lock().unwrap().flush(); let extractor_pre = extractor_gw.lock().unwrap().feed(&scrubber_tail); let extractor_tail = extractor_gw.lock().unwrap().flush_tail(); let tail = format!("{extractor_pre}{extractor_tail}"); if !tail.is_empty() { let _ = stream_tx.try_send(tail); }`.
- D-19 dispatch block: AFTER `consumer_handle.await.ok()` AND `typing_handle.await.ok()`, BEFORE `match agent_result`. Drains `extractor.take_attachments()`. For each `MediaRef`: call `media_sender.send_media(&event.chat_id, &media_ref, None).await`. On Err, push `media_ref.original_tag_text.clone()` to `failed_tags`. After loop, if `failed_tags` non-empty, build `reinsert_body` by concatenating the placeholder's last-known body + `"\n\n"` + `failed_tags.join("\n")`, then one `adapter.edit_message_markdown_v2(&event.chat_id, &placeholder_id, &escape_outside_code_blocks(&reinsert_body)).await` (escape preserves any markdown structure in the original body; the appended tag literals are escaped too since they contain `<` `>` which are NOT reserved chars per the 18-char list, but the appended text may include path components with `.` which IS reserved — escape is correct).
- `runner.rs` Telegram start path: `let telegram_arc = Arc::new(TelegramAdapter::new(token)); ... handler.set_media_sender(telegram_arc.clone() as Arc<dyn MediaSender>); ...; let adapter_for_dispatch = telegram_arc as Arc<dyn PlatformAdapter>;` — clone-cast twice, never upcast.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add media_sender field + setter to GatewayMessageHandler</name>
  <read_first>
    - crates/ironhermes-gateway/src/handler.rs (anchors per RESEARCH §Constructor Change: struct at :172, `pub fn new` at :173-225 with `user_queue_manager: None` at :222 to mirror, existing setters `set_session_queue` at :345 and `set_user_queue_manager` at :349-358 — match their doc-comment style)
    - crates/ironhermes-gateway/src/adapter.rs (post-plans 03/04 — `MediaSender` trait is publicly declared)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md (§8 §Analog A setter pattern + §Field-add pattern — exact line shapes)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md (§Constructor Change `Concrete edit list for the planner` — 5 edits; Pitfall 3 silent-drop risk)
  </read_first>
  <action>Edit `crates/ironhermes-gateway/src/handler.rs`:
(a) Add `use crate::adapter::MediaSender;` near top imports.
(b) Add field `media_sender: Option<Arc<dyn MediaSender>>,` to the `GatewayMessageHandler` struct (line-170 area), adjacent to `user_queue_manager: Option<Arc<UserQueueManager>>`.
(c) Initialize `media_sender: None,` in `pub fn new` (around line 222), adjacent to `user_queue_manager: None`.
(d) Add the setter immediately after `set_user_queue_manager` at :349-358: `pub fn set_media_sender(&mut self, sender: Arc<dyn MediaSender>) { self.media_sender = Some(sender); }` with a doc-comment citing Phase 36.17.2.2 D-18: notes production wires this from runner.rs Telegram start path, notes Discord/Slack/web stay None so the D-19 dispatch loop falls through with a warn-log when extracted tags appear without a media_sender (Pitfall 3 mitigation).
(e) Do NOT change the `pub fn new` SIGNATURE — adding a constructor parameter would force every one of the 5 existing call sites to update. Setter pattern keeps them stable.</action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-gateway --tests 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'fn set_media_sender' crates/ironhermes-gateway/src/handler.rs` returns `1`.
    - Source: `grep -c 'media_sender: None' crates/ironhermes-gateway/src/handler.rs` returns at least `1`.
    - Source: `grep -c 'media_sender: Option<Arc<dyn MediaSender>>' crates/ironhermes-gateway/src/handler.rs` returns at least `1`.
    - Source: `grep -c 'use crate::adapter::MediaSender' crates/ironhermes-gateway/src/handler.rs` returns `1`.
    - Behavior: `cargo build -p ironhermes-gateway --tests` exits 0 — all 5 existing `GatewayMessageHandler::new` call sites continue to compile against the unchanged constructor signature (production runner.rs:498, internal handler.rs:~1694, plus 3 test fixtures: uqm_session_queue_unification.rs:175, session_queue_integration.rs:246, running_agent_guard_tests.rs:124 per RESEARCH inventory).
  </acceptance_criteria>
  <done>Field + setter present. Constructor signature unchanged. Gateway crate builds (5 existing call sites unbroken).</done>
</task>

<task type="auto">
  <name>Task 2: Wire MediaTagExtractor into the stream_callback chain alongside the existing scrubber</name>
  <read_first>
    - crates/ironhermes-gateway/src/handler.rs (anchor :1333-1343 — existing scrubber wire-up; :1416 `stream: Some(stream_callback)` insertion into TurnRequest; :1430 scrubber tail flush)
    - crates/ironhermes-gateway/src/media_tag.rs (post-plan-02 — `MediaTagExtractor::new()`, `feed(delta)`, `flush_tail()`, `take_attachments()`)
    - crates/ironhermes-agent/src/streaming_scrubber.rs (scrubber's `feed` + `flush` shape — confirms tail-flush order)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md (§Shared Patterns §`Arc<Mutex<...>>` for per-turn state machines — full code excerpt of the chained wire-up)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md (Open Q5 / Assumption A10 — chain order: scrubber first, extractor second; rationale: MEDIA tags inside `<memory-context>` spans drop consistently)
  </read_first>
  <action>Edit the `run_agent` body in `crates/ironhermes-gateway/src/handler.rs`:

(a) Immediately after the existing scrubber construction at handler.rs:1333-1336 (`let scrubber_gw = std::sync::Arc::new(std::sync::Mutex::new(ironhermes_agent::streaming_scrubber::StreamingContextScrubber::new(),));`), add:

    `let extractor_gw = std::sync::Arc::new(std::sync::Mutex::new(crate::media_tag::MediaTagExtractor::new()));`
    `let extractor_cb = std::sync::Arc::clone(&extractor_gw);`

(b) Change the existing `stream_callback` closure at :1338-1343 from:

    `let visible = scrubber_gw_cb.lock().unwrap().feed(delta); if !visible.is_empty() { let _ = stream_tx_clone.try_send(visible); }`

    to:

    `let scrubbed = scrubber_gw_cb.lock().unwrap().feed(delta); let visible = extractor_cb.lock().unwrap().feed(&scrubbed); if !visible.is_empty() { let _ = stream_tx_clone.try_send(visible); }`

(c) Replace the existing scrubber tail flush at handler.rs:1430 with the chained-tail flush:

    `let scrubber_tail = scrubber_gw.lock().unwrap().flush();`
    `let extractor_pre = if !scrubber_tail.is_empty() { extractor_gw.lock().unwrap().feed(&scrubber_tail) } else { String::new() };`
    `let extractor_tail = extractor_gw.lock().unwrap().flush_tail();`
    `let tail = format!("{extractor_pre}{extractor_tail}");`
    `if !tail.is_empty() { let _ = stream_tx.try_send(tail); }`

(d) Do NOT yet add the D-19 dispatch loop — task 3 owns that block (it MUST land AFTER both `consumer_handle.await.ok()` at :1439 AND `typing_handle.await.ok()` at :1443; task 2's scope ends at the tail flush at :1430).</action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-gateway 2>&amp;1 | tail -10 &amp;&amp; grep -c 'MediaTagExtractor::new' crates/ironhermes-gateway/src/handler.rs &amp;&amp; grep -c 'extractor_gw' crates/ironhermes-gateway/src/handler.rs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'MediaTagExtractor::new' crates/ironhermes-gateway/src/handler.rs` returns `1` (constructed once per turn).
    - Source: `grep -c 'extractor_cb' crates/ironhermes-gateway/src/handler.rs` returns at least `2` (clone for closure + use inside closure).
    - Source: `grep -c 'flush_tail' crates/ironhermes-gateway/src/handler.rs` returns at least `1` (tail flush emitted).
    - Source: chained order present — `grep -B 1 -A 3 'extractor_cb.lock' crates/ironhermes-gateway/src/handler.rs | grep -c 'scrubber_gw_cb.lock'` returns at least `1` (scrubber output is the input to extractor in the same closure body).
    - Source: existing scrubber wire-up still in place — `grep -c 'StreamingContextScrubber::new' crates/ironhermes-gateway/src/handler.rs` returns `1` (no regression).
    - Behavior: `cargo build -p ironhermes-gateway` exits 0.
    - Behavior: `cargo test -p ironhermes-gateway 2>&amp;1 | grep -E 'test result.*FAIL' | wc -l` returns `0` (existing tests pass; the extractor wiring is purely additive — no `<MEDIA: ...>` tags appear in existing test fixtures, so the extractor is a no-op pass-through).
  </acceptance_criteria>
  <done>Extractor wired alongside scrubber. Chain order is scrubber→extractor (Open Q5). Both tails flush in sequence. Existing tests unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Insert D-19 dispatch block at the CORRECTED anchor (after both await.ok barriers, before match agent_result)</name>
  <read_first>
    - crates/ironhermes-gateway/src/handler.rs (anchors per RESEARCH FLAGGED RISK + Pitfall 6 verified by direct read 2026-06-03: line 1439 `consumer_handle.await.ok();`, line 1443 `typing_handle.await.ok();`, line 1445 `match agent_result {`. D-19 block goes BETWEEN :1443 and :1445.)
    - crates/ironhermes-gateway/src/adapter.rs (`MediaSender::send_media` signature: `async fn send_media(&self, chat_id: &str, m: &MediaRef, thread_id: Option<&str>) -> Result<MessageResponse>` with default body that dispatches on kind)
    - crates/ironhermes-gateway/src/media_tag.rs (`take_attachments() -> Vec<MediaRef>`; `MediaRef.original_tag_text: String`)
    - crates/ironhermes-gateway/src/markdown_v2.rs (`escape_outside_code_blocks` for the reinsert body)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md (§8 §`D-19 dispatch block` excerpt — the full template per the CORRECTED anchor)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md (FLAGGED RISK D-19; Pitfall 6 anchor narrative; §Code Examples D-19 corrected insertion point)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-CONTEXT.md (D-07 ordering, D-10 reinsert-on-fail, D-11 multi-tag sequential, D-15 size cap, D-19 dispatch shape — note CONTEXT D-19 anchor is STALE per RESEARCH; use the corrected anchor)
  </read_first>
  <action>Insert a new code block between handler.rs lines 1443 (`typing_handle.await.ok();`) and 1445 (`match agent_result {`). The block:

    `let media_refs = extractor_gw.lock().unwrap().take_attachments();`
    `if !media_refs.is_empty() {`
    `    if let Some(media_sender) = self.media_sender.as_ref() {`
    `        let mut failed_tags: Vec<String> = Vec::new();`
    `        for media_ref in media_refs {`
    `            match media_sender.send_media(&event.chat_id, &media_ref, None).await {`
    `                Ok(_) => {}`
    `                Err(e) => {`
    `                    tracing::warn!(`
    `                        chat_id = %event.chat_id,`
    `                        kind = ?media_ref.kind,`
    `                        error = %e,`
    `                        "attachment failed, reinserting tag literal (D-10)"`
    `                    );`
    `                    failed_tags.push(media_ref.original_tag_text.clone());`
    `                }`
    `            }`
    `        }`
    `        if !failed_tags.is_empty() {`
    `            // D-10: one combined re-edit of the placeholder appending each failed tag literal on its own line.`
    `            // Use edit_message_markdown_v2 + escape so the placeholder's existing markdown body stays intact`
    `            // and the appended literals (which contain `.` and other reserved chars in paths) are also escaped.`
    `            let appended = failed_tags.join("\n");`
    `            // The placeholder's existing body is whatever was rendered by stream_consumer.flush(true). The`
    `            // executor must locate the final-body source in scope at this point: examine handler.rs around`
    `            // :1440 to find the StreamConsumer reference (it owns self.buffer per stream_consumer.rs:88-94`
    `            // which is the post-flush final-body string). If a public accessor does not exist, add one as`
    `            // pub fn final_body(&self) -> &str { &self.buffer } on StreamConsumer with a Phase 36.17.2.2 D-10`
    `            // doc-comment, and import the constant into scope here. Combine: format!("{}\n\n{}", final_body, appended).`
    `            let reinsert_body = format!("{}\n\n{}", stream_consumer_final_body, appended);`
    `            let escaped = crate::markdown_v2::escape_outside_code_blocks(&reinsert_body);`
    `            if let Err(e) = adapter.edit_message_markdown_v2(&event.chat_id, &placeholder_id, &escaped).await {`
    `                tracing::error!(`
    `                    chat_id = %event.chat_id,`
    `                    message_id = %placeholder_id,`
    `                    error = %e,`
    `                    "D-10 reinsert edit failed; placeholder retains its post-flush body without tag literals"`
    `                );`
    `            }`
    `        }`
    `    } else {`
    `        tracing::warn!(`
    `            chat_id = %event.chat_id,`
    `            ref_count = media_refs.len(),`
    `            "media tags emitted on platform without MediaSender — dropping refs (D-18)"`
    `        );`
    `    }`
    `}`

Resolution notes for the executor:
- `event` is in scope inside `run_agent` (the incoming `MessageEvent` parameter); `event.chat_id` is `String`. Pass `&event.chat_id`.
- `placeholder_id` — locate the variable name in scope at this point. The streaming pipeline established it before the spawn (probably named `placeholder_id` or `placeholder_msg_id` or similar — `rg -n 'placeholder' crates/ironhermes-gateway/src/handler.rs | head -10` to locate). Use the existing identifier verbatim.
- `adapter` — locate the `Arc<dyn PlatformAdapter>` parameter or in-scope variable; in `run_agent` it is typically a function parameter named `adapter`. `rg -n 'fn run_agent' crates/ironhermes-gateway/src/handler.rs` to verify the signature.
- `stream_consumer_final_body` — the final-flushed text. If `StreamConsumer` does not expose a `final_body()` accessor today, add one as `pub fn final_body(&self) -> &str { &self.buffer }` in `crates/ironhermes-gateway/src/stream_consumer.rs` next to the existing `pub fn flush(...)` method (around line 80-105) with a doc-comment citing Phase 36.17.2.2 D-10. The consumer task holds the `StreamConsumer` by mutex/ownership; locate the reference at this point in `run_agent` — if it has been dropped by `consumer_handle.await.ok()`, the body must be captured BEFORE the spawn and passed through; in that case have the spawned consumer task at handler.rs:1280 push the final-body via a `oneshot::channel<String>` whose receiver is awaited here. Read handler.rs:1275-1450 to determine which approach is least invasive. If the existing code already captures the final body via the StreamConsumer's mutex held by the parent task, prefer the accessor route.
- `thread_id`: D-19 dispatch passes `None` because `telegram.rs:222` ignores `_thread_id` today per RESEARCH §`D-19 dispatch — CORRECTED insertion point` note.

This block MUST come BEFORE `match agent_result` at :1445 — placing it AFTER `match agent_result` would make attachment dispatch run only on the Ok arm and miss media that was extracted before the agent errored out (acceptable behavior is "send any extracted attachments regardless of agent_result branch — the user's text and media are independent of whether the turn completed cleanly").</action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-gateway 2>&amp;1 | tail -15 &amp;&amp; grep -c 'take_attachments' crates/ironhermes-gateway/src/handler.rs &amp;&amp; grep -c 'send_media' crates/ironhermes-gateway/src/handler.rs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'take_attachments' crates/ironhermes-gateway/src/handler.rs` returns `1` (the D-19 dispatch drains the accumulator).
    - Source: `grep -c '\.send_media(' crates/ironhermes-gateway/src/handler.rs` returns `1` (single dispatch call site).
    - Source: `grep -c 'media tags emitted on platform without MediaSender' crates/ironhermes-gateway/src/handler.rs` returns `1` (Pitfall 3 warn log).
    - Source: `grep -c 'attachment failed, reinserting tag literal' crates/ironhermes-gateway/src/handler.rs` returns `1` (D-10 warn log).
    - Source: anchor correctness — `awk '/typing_handle\.await\.ok\(\);/{print NR" typing"; t=NR} /take_attachments\(\)/{print NR" take"; k=NR} /match agent_result/{print NR" match"; m=NR} END{exit !(t<k && k<m)}' crates/ironhermes-gateway/src/handler.rs` exits 0 (the take_attachments line MUST appear AFTER typing_handle.await.ok() AND BEFORE match agent_result — this is the CORRECTED anchor per RESEARCH Pitfall 6).
    - Source: `grep -c 'final_body' crates/ironhermes-gateway/src/stream_consumer.rs` returns at least `1` if the accessor approach was used (else verify the oneshot-channel approach is implemented with `tokio::sync::oneshot` import and a `let (body_tx, body_rx) = ...` pair near the consumer spawn).
    - Behavior: `cargo build -p ironhermes-gateway` exits 0.
    - Behavior: `cargo test -p ironhermes-gateway 2>&amp;1 | grep -E 'test result.*FAIL' | wc -l` returns `0` — existing tests still pass (none of them emit MEDIA tags, so the dispatch loop is a no-op).
  </acceptance_criteria>
  <done>D-19 dispatch block lives at the CORRECTED anchor (between :1443 and :1445). Failed tags reinsert via one combined edit. None-`media_sender` warns and drops. Existing tests unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Wire set_media_sender in runner.rs Telegram start path (clone-cast, never upcast)</name>
  <read_first>
    - crates/ironhermes-gateway/src/runner.rs (anchors per RESEARCH §`GatewayMessageHandler` Constructor Change: `handler.set_session_queue(...)` at :561 and `handler.set_user_queue_manager(...)` at :711 — the analog wiring points. The Telegram `TelegramAdapter` construction is in the Telegram start path; locate via `rg -n 'TelegramAdapter::new\|Arc::new(TelegramAdapter' crates/ironhermes-gateway/src/runner.rs`.)
    - crates/ironhermes-gateway/src/telegram.rs (post-plan-04 — `TelegramAdapter` implements both `PlatformAdapter` and `MediaSender`)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md (§9 — runner.rs wire-up + §Shared Patterns §Arc dyn-dispatch construction `avoid trait upcasting`)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md (Open Q4 / Assumption A7 — clone-cast separately; do NOT upcast `Arc<dyn PlatformAdapter>` → `Arc<dyn MediaSender>`)
  </read_first>
  <action>Edit `crates/ironhermes-gateway/src/runner.rs`:

(a) In the Telegram start path (where `TelegramAdapter` is constructed and the handler is wired), restructure the adapter construction to use the clone-cast-twice pattern. Find the existing line where the adapter is constructed and the line where it is passed as `Arc<dyn PlatformAdapter>` to the dispatch loop. Replace with:

    `let telegram_arc = Arc::new(TelegramAdapter::new(token));`
    `// Phase 36.17.2.2 D-18: install the MediaSender impl (Telegram only).`
    `// Construct as Arc<TelegramAdapter> then clone-cast separately for each trait.`
    `// Do NOT upcast Arc<dyn PlatformAdapter> -> Arc<dyn MediaSender> — that was unstable on stable Rust`
    `// at the time of writing (RESEARCH Assumption A7); use independent clones instead.`
    `handler.set_media_sender(telegram_arc.clone() as Arc<dyn crate::adapter::MediaSender>);`
    `let adapter_for_dispatch: Arc<dyn PlatformAdapter> = telegram_arc.clone();`

    Then proceed with the existing dispatch wire-up using `adapter_for_dispatch` (or whatever variable name the existing code uses — preserve the local name and only change how it is constructed).

(b) `handler` is mutable at this point because the existing `set_session_queue` (:561) and `set_user_queue_manager` (:711) calls already require `&mut handler`. The new setter call slots into the same window (mutable handle in scope before the `let handler = Arc::new(handler);` wrap that occurs slightly later — verify via `rg -n 'Arc::new(handler)' crates/ironhermes-gateway/src/runner.rs`). If the construction shape today is `let mut handler = self.build_gateway_handler(); handler.set_session_queue(...); handler.set_user_queue_manager(...); let handler = Arc::new(handler);`, insert the `handler.set_media_sender(...)` call BEFORE the `Arc::new(handler)` wrap, after `set_user_queue_manager`.

(c) Do NOT touch the Discord / Slack / web / cron start paths — `media_sender` stays `None` on those platforms per D-18.</action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-gateway 2>&amp;1 | tail -10 &amp;&amp; grep -c 'set_media_sender' crates/ironhermes-gateway/src/runner.rs &amp;&amp; grep -c 'as Arc<dyn .*MediaSender>' crates/ironhermes-gateway/src/runner.rs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'handler\.set_media_sender(' crates/ironhermes-gateway/src/runner.rs` returns `1`.
    - Source: `grep -c 'as Arc<dyn .*MediaSender>' crates/ironhermes-gateway/src/runner.rs` returns at least `1` (the clone-cast).
    - Source: no upcast attempt — `grep -E 'as Arc<dyn .*PlatformAdapter>.*MediaSender|as Arc<dyn .*MediaSender>.*PlatformAdapter' crates/ironhermes-gateway/src/runner.rs | wc -l` returns `0` (no chained-cast text suggesting upcasting was attempted).
    - Source: Discord / Slack / web start paths untouched — `git diff crates/ironhermes-gateway/src/runner.rs | grep -E '^[+-].*discord|^[+-].*slack|^[+-].*webhook' | wc -l` returns `0` (no edits to those platform paths' code lines).
    - Behavior: `cargo build -p ironhermes-gateway` exits 0.
    - Behavior: `cargo test -p ironhermes-gateway 2>&amp;1 | grep -E 'test result.*FAIL' | wc -l` returns `0` (no regression).
  </acceptance_criteria>
  <done>Telegram start path wires `set_media_sender` via clone-cast. Non-Telegram paths untouched. Gateway crate builds and existing tests green.</done>
</task>

<task type="auto">
  <name>Task 5: New integration test crates/ironhermes-gateway/tests/telegram_media_delivery.rs with RecordingMediaAdapter fixture</name>
  <read_first>
    - crates/ironhermes-gateway/tests/uqm_session_queue_unification.rs (full file — the template for `RecordingFailingAdapter`, `spawn_test_worker`, `build_test_session_store`, `build_test_handler_with_queue`, `make_event_full`; per RESEARCH §`RecordingPlatformAdapter` and PATTERNS §11 the helpers MUST be copied because Rust integration tests are isolated binaries)
    - crates/ironhermes-gateway/src/media_tag.rs (the types the new fixture's `media_log` field uses)
    - crates/ironhermes-gateway/src/adapter.rs (`MediaSender` trait — fixture impls both `PlatformAdapter` and `MediaSender`)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md (§11 — new test file pattern + the `RecordingMediaAdapter` sketch; §Shared Patterns §Rust integration-test binary isolation)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md (§`RecordingPlatformAdapter` Option A `new sibling fixture RecordingSucceedingAdapter` — the recommended approach; Open Q2 — synthetic-LLM-stream injection mechanism; D-20 §Integration assertions list)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-VALIDATION.md (per-task verification map rows for D-07/D-10/D-15 — populate now per planner role instruction)
  </read_first>
  <action>Create new file `crates/ironhermes-gateway/tests/telegram_media_delivery.rs`:

(a) Copy verbatim (per Rust integration-test binary isolation): the helpers `build_test_session_store`, `build_test_handler_with_queue`, `make_event_full`, `spawn_test_worker` from `crates/ironhermes-gateway/tests/uqm_session_queue_unification.rs`. Add the file-top doc comment crediting the source per the existing convention (PATTERNS §11 imports/module-doc pattern).

(b) Define `pub struct RecordingMediaAdapter`:

    `pub struct RecordingMediaAdapter {`
    `    pub send_log: StdMutex<Vec<(String, String)>>,                                          // (chat_id, content) for plain send`
    `    pub send_v2_log: StdMutex<Vec<(String, String)>>,                                        // (chat_id, content) for send_message_markdown_v2`
    `    pub edit_log: StdMutex<Vec<(String, String, String, bool)>>,                             // (chat_id, message_id, content, is_markdown_v2)`
    `    pub media_log: StdMutex<Vec<(String, MediaRef, Option<String>)>>,                       // (chat_id, ref, thread_id)`
    `    pub reactions: StdMutex<Vec<(String, String, String)>>,                                  // (chat, msg, emoji)`
    `    pub next_message_id: StdMutex<i64>,`
    `    pub photo_fail_paths: StdMutex<HashSet<String>>,                                         // simulate D-10 missing-path: send_photo returns Err if path string is in this set`
    `    pub parse_mode_fail_msg_ids: StdMutex<HashSet<String>>,                                   // simulate D-02 parse-mode 400 on edit_message_markdown_v2 for matched message_ids`
    `}`

    Implement `pub fn new() -> Arc<Self>`. Implement `#[async_trait] impl PlatformAdapter for RecordingMediaAdapter` — `send_message` returns `Ok(MessageResponse { message_id: synthetic_increment, ... })` (succeeds, NOT fails — unlike `RecordingFailingAdapter`); `edit_message` records into edit_log with `is_markdown_v2=false`; `edit_message_markdown_v2` checks `parse_mode_fail_msg_ids` — if matched, return `Err(anyhow::anyhow!("Bad Request: can't parse entities (test simulation)"))` to drive plan 04's D-02 fallback; otherwise record with `is_markdown_v2=true` and return Ok; `send_message_markdown_v2` records into send_v2_log and returns Ok; `add_reaction` records; `send_chat_action` returns Ok (no-op).

    Implement `#[async_trait] impl MediaSender for RecordingMediaAdapter` — each per-type method checks `photo_fail_paths` (for the Path variant only) — if the path string is in the failure set, return `Err(anyhow::anyhow!("simulated send failure"))`; otherwise push `(chat_id, MediaRef { source, kind: <method-specific>, original_tag_text: String::new() }, thread_id)` to `media_log` and return `Ok(MessageResponse { ... })`. The `send_media` default body in the trait will dispatch correctly.

(c) For the synthetic LLM stream: per RESEARCH Open Q2 / Wave 0 Gap, first run `rg -n 'pub trait Provider' crates/ironhermes-core/src/provider.rs` and `rg -n 'impl.*Provider' crates/ironhermes-agent/` to locate the chat-completion `Provider` trait. If a mock already exists (most likely candidates: `MockProvider`, `EchoProvider`, `TestProvider` in `crates/ironhermes-core/src/provider.rs` or `crates/ironhermes-agent/src/`), use it. If none exists, define a minimal `FakeStreamingProvider` inside this test file that implements the `Provider` trait by returning a closure-driven sequence of stream deltas (the test specifies the delta sequence per-scenario, the fake just replays them). The fake should be wired into `Config` / `ProviderResolver` such that the `AgentRuntime` consumed by `GatewayMessageHandler` calls it during `run_turn`. Inspect `crates/ironhermes-agent/src/agent_runtime.rs` to find the injection seam; the simplest approach is a feature-flagged test-only constructor on `AgentRuntime` or a `Provider` injection on `Config`. Document the chosen mechanism in a top-of-file comment.

(d) Implement 7 named `#[tokio::test]` functions:
    1. `text_only_v2_edit_renders_with_escape`: synthetic stream emits a single chunk `"Hello *world*. Visit https://example.com."` (contains a `.` reserved char). Wait for the worker to process. Assert exactly one entry in `edit_log` with `is_markdown_v2=true` AND content equal to `escape_outside_code_blocks("Hello *world*. Visit https://example.com.")` (compute the expected via the imported escape fn — single source of truth). Assert `media_log` is empty (D-07 — no attachments, only the V2 edit).
    2. `single_photo_text_then_attachment_order`: synthetic stream emits `"Here is the picture: <MEDIA: /tmp/photo.png>"`. Pre-create `/tmp/photo.png` as a 1-byte file so D-15 passes (`tokio::fs::write("/tmp/photo.png", b"x").await.unwrap()` in test setup). Worker processes. Assert: (a) `edit_log` has exactly one entry with `is_markdown_v2=true` containing only the visible text `"Here is the picture: "` (extractor stripped the tag); (b) `media_log` has exactly one entry with kind=Photo, source=Path("/tmp/photo.png"); (c) the `edit_log` entry's timestamp is BEFORE the `media_log` entry's timestamp (D-07 ordering — use a monotonic counter shared across both logs to verify order, OR assert by index that the edit happened before the media log push by checking that the recorded indices indicate ordering: snapshot the lengths just before the test waits for completion and verify both lengths reached their target in the expected order).
    3. `multi_tag_emits_in_stream_order`: synthetic stream emits `"<MEDIA: /tmp/a.png><MEDIA: /tmp/b.ogg><MEDIA: /tmp/c.pdf>"`. Pre-create all three files. Assert `media_log` has 3 entries in order Photo("/tmp/a.png"), Voice("/tmp/b.ogg"), Document("/tmp/c.pdf") (D-11).
    4. `missing_path_reinserts_tag`: synthetic stream emits `"intro <MEDIA: /tmp/does_not_exist_xxxxxx.png>"`. Add the path to `photo_fail_paths` so RecordingMediaAdapter's `send_photo` simulates the failure (D-10). Assert: `media_log` is empty (the failure means nothing was logged as a successful send). Assert `edit_log` has TWO entries: the initial post-flush edit AND the D-10 reinsert edit; the second edit's content contains the literal `<MEDIA: /tmp/does_not_exist_xxxxxx.png>` (the tag literal was appended).
    5. `oversize_path_reinserts_without_upload`: synthetic stream emits `"<MEDIA: /tmp/oversize.png>"`. Pre-create `/tmp/oversize.png` as a 21 MiB file via `tokio::fs::write("/tmp/oversize.png", vec![0u8; 21 * 1024 * 1024]).await.unwrap()`. The D-15 pre-check in plan 04's `impl MediaSender::send_photo` MUST trigger before any upload attempt — assert `media_log` is empty AND `edit_log` has a reinsert entry containing the tag literal (D-15 + D-10).
    6. `tag_inside_fence_passes_through_no_attachment`: synthetic stream emits a fenced code block containing `<MEDIA: /tmp/a.png>`. Pre-create the file (the fence skip should mean it is NEVER read). Assert `media_log` is empty AND the `edit_log` final body contains the literal `<MEDIA: /tmp/a.png>` text inside the fence (D-09 + D-08 extractor's fence-skip).
    7. `parse_mode_400_retries_as_plain_text`: synthetic stream emits a simple `"hello"`. Locate the placeholder message_id (synthesized by the fixture at the streaming-bridge moment) and add it to `parse_mode_fail_msg_ids` so the RecordingMediaAdapter's `edit_message_markdown_v2` returns `Err("Bad Request: can't parse entities (test simulation)")`. Plan 04's D-02 fallback retries — the retry should hit `edit_message` (plain text, no parse_mode); add a recording variant to capture that. Assert: `edit_log` shows two entries — the first with `is_markdown_v2=true` (the failed attempt; record-before-Err) and the second with `is_markdown_v2=false` (the plain-text retry).

    For test 7: if plan 04's D-02 implementation issues the retry via `api_call("editMessageText", &plain_params)` directly without routing through the trait's `edit_message` method, the RecordingMediaAdapter won't capture the retry. In that case the test asserts only that the first attempt is recorded AND that there is no error propagating up (the worker continues — D-02 is "silent fallback"); add a comment that the retry is NOT directly observable through the trait surface and is covered by the live UAT (plan 07 Scenario 8).

(e) Insert test-cleanup `tokio::fs::remove_file` calls for each tempfile created (use `tempfile::NamedTempFile` if `tempfile` is in dev-dependencies — `rg 'tempfile' crates/ironhermes-gateway/Cargo.toml`; if not, use `/tmp/` paths and clean up in test teardown).

(f) Populate the per-task verification map rows in `.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-VALIDATION.md` — replace the TBD-by-planner rows with one row per test from this task. Set `wave_0_complete: true` in the VALIDATION.md frontmatter once the integration tests are green (this happens after plan 06 runs; do not set it now during planning).</action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p ironhermes-gateway --test telegram_media_delivery 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File: `test -f crates/ironhermes-gateway/tests/telegram_media_delivery.rs` exits 0.
    - Source: `grep -c 'pub struct RecordingMediaAdapter' crates/ironhermes-gateway/tests/telegram_media_delivery.rs` returns `1`.
    - Source: both trait impls present — `grep -c 'impl PlatformAdapter for RecordingMediaAdapter' crates/ironhermes-gateway/tests/telegram_media_delivery.rs` returns `1` AND `grep -c 'impl MediaSender for RecordingMediaAdapter' crates/ironhermes-gateway/tests/telegram_media_delivery.rs` returns `1`.
    - Source: at least 7 named tests — `grep -E 'async fn (text_only_v2_edit_renders_with_escape|single_photo_text_then_attachment_order|multi_tag_emits_in_stream_order|missing_path_reinserts_tag|oversize_path_reinserts_without_upload|tag_inside_fence_passes_through_no_attachment|parse_mode_400_retries_as_plain_text)' crates/ironhermes-gateway/tests/telegram_media_delivery.rs | wc -l` returns `7`.
    - Behavior: `cargo test -p ironhermes-gateway --test telegram_media_delivery` exits 0 with `test result: ok. 7 passed` (or higher if executor added more cases).
    - Behavior: `cargo test --workspace 2>&amp;1 | grep -E 'test result.*FAIL' | wc -l` returns `0` — full workspace regression including this new test, the 36.17.2 D-21 UQM tests, the schema-v3 migration tests, and every other phase's suite.
    - VALIDATION.md: `grep -c 'TBD by planner' .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-VALIDATION.md` returns `0` after the executor populates the per-task verification map per task 5 (f).
  </acceptance_criteria>
  <done>Integration test file exists and green. RecordingMediaAdapter fixture implemented. All 7 scenarios covered. Workspace regression green. VALIDATION.md map populated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM stream → MediaTagExtractor → MediaSender dispatch → Telegram | The full extraction-to-attachment path; every link is model-controlled until the dispatch loop. |
| Failed-attachment path → reinsert via edit_message_markdown_v2 | The reinsert body is constructed in-handler; must respect MarkdownV2 escape (otherwise the reinsert itself fails with 400). |
| `media_sender == None` on non-Telegram platforms → silent attachment-drop | Pitfall 3 (silent drop) — mitigated by always-warn log. |
| Synthetic LLM provider in integration test → handler internals | Test-only; no production trust impact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-D19-ANCHOR-MISPLACEMENT (HIGH) | Tampering | handler.rs D-19 dispatch block placement | mitigate | Task 3 acceptance criterion uses `awk` to assert lexical order `typing_handle.await.ok < take_attachments < match agent_result`. RESEARCH FLAGGED RISK + Pitfall 6 + PATTERNS §8 all triangulate on this anchor. Misplacement is the highest-risk failure mode in the phase. |
| T-D19-MISSING-MEDIA-SENDER (Pitfall 3) | Information Disclosure | handler.rs when media_sender is None but tags emitted | mitigate | `else` branch warns and drops with explicit count + chat_id. Task 5 test `single_photo_text_then_attachment_order` covers the happy path (media_sender set); a future test could cover the missing-media_sender drop path on a synthetic non-Telegram event, but is not required for phase exit. |
| T-D10-REINSERT-LOOP | Denial of Service | handler.rs reinsert via edit_message_markdown_v2 | mitigate | The reinsert is ONE edit per turn (not one per failure). If the reinsert edit itself fails with parse-mode error, the D-02 fallback in plan 04's `edit_message_markdown_v2` impl retries as plain text — no recursion. If the plain-text retry fails too, log `error!` and continue (do NOT loop). |
| T-INPUT-MEDIA-PATH (HIGH, phase-level) | Information Disclosure | handler.rs D-19 dispatch → MediaSender::send_* | mitigate | Plan 04 task 4 enforces path canonicalization + allowed-roots check inside the MediaSender impl (T-INPUT-MEDIA-PATH mitigation lives at the adapter layer, not handler). Handler-side is purely orchestration. |
| T-RUNNER-UPCAST-MISUSE | Tampering | runner.rs Telegram start path | mitigate | Task 4 explicitly uses the clone-cast-twice pattern. Acceptance criterion asserts NO chained `as Arc<dyn ...>` text that would suggest an upcast attempt. |
| T-TEST-FAKE-PROVIDER-LEAK | Denial of Service | FakeStreamingProvider in test binary | accept | Test-only; isolated to the integration test binary. If executor cannot wire injection without invasive `Provider` trait surgery, fall back to feature-flagged test constructor on `AgentRuntime`. The risk of accidental production use is zero because the fake lives in `tests/` (not `src/`). |
| T-D02-RETRY-NOT-OBSERVABLE | Reliability | parse_mode_400_retries_as_plain_text test | accept | If plan 04's D-02 retry bypasses the trait surface (direct `api_call` to Telegram), the integration test cannot observe the plain-text retry. Live UAT Scenario 8 (plan 07) covers it. Test 7 still asserts the first-attempt edit was recorded AND no error propagates. |
</threat_model>

<verification>
- `cargo build -p ironhermes-gateway` exits 0 after each task.
- `cargo test -p ironhermes-gateway --test telegram_media_delivery` exits 0 with ≥ 7 passing tests.
- `cargo test --workspace` exits 0 — no FAIL across all crates.
- `grep -c 'fn set_media_sender' crates/ironhermes-gateway/src/handler.rs` returns 1.
- `grep -c 'take_attachments' crates/ironhermes-gateway/src/handler.rs` returns 1.
- `grep -c 'MediaTagExtractor::new' crates/ironhermes-gateway/src/handler.rs` returns 1.
- `grep -c 'handler.set_media_sender' crates/ironhermes-gateway/src/runner.rs` returns 1.
- `awk` ordering check confirms the D-19 dispatch lives between typing_handle.await.ok and match agent_result.
- `.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-VALIDATION.md` has all `TBD by planner` rows replaced.
</verification>

<success_criteria>
- All must_haves observable via the grep / build / test commands above.
- 5 commits land: `feat(36.17.2.2-06): GatewayMessageHandler.set_media_sender setter`, `feat(36.17.2.2-06): wire MediaTagExtractor in run_agent stream callback`, `feat(36.17.2.2-06): D-19 dispatch loop at corrected handler.rs anchor`, `feat(36.17.2.2-06): runner.rs Telegram start path wires set_media_sender`, `test(36.17.2.2-06): integration test telegram_media_delivery covers D-07/D-09/D-10/D-11/D-15`.
- Plan 07 inherits a fully-functional production path; its only remaining work is the live UAT runbook and the operator-`approved` sign-off gate.
</success_criteria>

<output>
Create `.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-06-SUMMARY.md` when done — record (1) the exact line numbers where the D-19 dispatch block was inserted (for future maintainers locating it after further drift), (2) the chosen synthetic-LLM-stream injection mechanism (existing mock vs new FakeStreamingProvider), (3) whether the `StreamConsumer::final_body()` accessor was added or the oneshot-channel approach was used for the reinsert-body propagation.
</output>
