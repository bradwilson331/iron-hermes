---
phase: 36.17.2.2
plan: 02
type: tdd
wave: 0
depends_on: []
files_modified:
  - crates/ironhermes-gateway/src/media_tag.rs
  - crates/ironhermes-gateway/src/lib.rs
autonomous: true
requirements:
  - D-05
  - D-06
  - D-08
  - D-09
  - D-16  # outbound-only (defines the type surface used here; no inbound code touched)
user_setup: []

must_haves:
  truths:
    - "`cargo test -p ironhermes-gateway media_tag` is green for every D-05/D-06/D-08/D-09 case in the test table below."
    - "`MediaTagExtractor::feed(delta)` strips well-formed `<MEDIA: ...>` tags from the visible stream and accumulates one `MediaRef` per tag (D-05/D-08)."
    - "Tags split across delta boundaries (`<MEDI` then `A: foo.png>`) accumulate to exactly one `MediaRef`; the partial prefix is held back and emits visible text only after disambiguation (D-08 partial-prefix buffering — RESEARCH §Pattern Audit)."
    - "Tags inside ```fenced``` code blocks AND `inline code` spans pass through to the visible stream unchanged, with no `MediaRef` extracted (D-09)."
    - "`<MEDIA: https://...>` parses as `MediaSource::Url`; anything else parses as `MediaSource::Path`; the file/URL extension drives `MediaKind` per the D-06 table (`.png` etc → Photo; `.ogg .opus` → Voice; `.mp3 .m4a .flac .wav` → Audio; `.mp4 .mov .webm` → Video; anything else → Document)."
    - "`take_attachments()` drains the accumulator — subsequent calls return `vec![]` until new tags are fed."
    - "Non-ASCII text adjacent to tags (e.g. `İİİ <MEDIA: foo.png> Привет`) does not panic and extracts the tag correctly (mirror scrubber regression `non_ascii_before_tag_does_not_panic`)."
    - "`flush_tail()` on an unterminated `<MEDIA: ...` open returns the held buffer as VISIBLE text (extractor policy diverges from scrubber: scrubber discards, extractor emits — per RESEARCH §Pattern Audit `flush_tail policy DIFFERS` and §Code Examples Open Q5 note `user trust requires tag-like text never disappears`)."
  artifacts:
    - path: "crates/ironhermes-gateway/src/media_tag.rs"
      provides: "`pub struct MediaTagExtractor`, `pub enum MediaSource { Path(PathBuf), Url(String) }`, `pub enum MediaKind { Photo, Voice, Audio, Video, Document }`, `pub struct MediaRef { pub source: MediaSource, pub kind: MediaKind }`, plus inline test module covering D-05/D-06/D-08/D-09."
      contains: "pub struct MediaTagExtractor"
    - path: "crates/ironhermes-gateway/src/lib.rs"
      provides: "`pub mod media_tag;` declaration so plans 03/04/06 can `use crate::media_tag::{MediaTagExtractor, MediaRef, MediaSource, MediaKind};`."
      contains: "pub mod media_tag"
  key_links:
    - from: "crates/ironhermes-gateway/src/media_tag.rs"
      to: "crates/ironhermes-agent/src/streaming_scrubber.rs"
      via: "pattern mirror — partial-prefix buffering algorithm copied per RESEARCH §Pattern Audit"
      pattern: "find_ascii_ci|max_partial_suffix"
---

<objective>
Land the `MediaTagExtractor` plus its supporting types (`MediaSource`, `MediaKind`, `MediaRef`) as a new gateway module. Mirror `StreamingContextScrubber` exactly for the delta-buffered state machine (partial-prefix held-back, ASCII byte search) and add the three D-09 / Pitfall 5 state booleans (`in_fenced_code`, `in_inline_code`, `prev_was_escape`) for tag suppression inside code spans. Pure unit work — no integration wiring; plans 04 (impl MediaSender) and 06 (handler.rs dispatch) consume these types later.

Purpose: D-08 requires extraction to happen DURING streaming (scrubber-style) so tags never reach the user's screen as visible text under the happy path. The partial-prefix buffering is the only correct algorithm; mirroring `StreamingContextScrubber` reuses an already-battle-tested approach (the scrubber survived the U+0130 non-ASCII regression CR-01). TDD ordering catches the high-risk state interactions (D-09 fence-skip, Pitfall 5 escaped backtick) before any consumer wires them up.
Output: `crates/ironhermes-gateway/src/media_tag.rs` with the four type exports and the inline `#[cfg(test)] mod tests` block; `lib.rs` declares `pub mod media_tag;`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-CONTEXT.md
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md
@crates/ironhermes-agent/src/streaming_scrubber.rs
@crates/ironhermes-gateway/src/lib.rs
</context>

<interfaces>
<!-- Types and methods plans 03/04/06 consume. -->

```rust
// crates/ironhermes-gateway/src/media_tag.rs (new)
use std::path::PathBuf;

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum MediaSource {
    Path(PathBuf),
    Url(String),
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum MediaKind {
    Photo,
    Voice,
    Audio,
    Video,
    Document,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct MediaRef {
    pub source: MediaSource,
    pub kind: MediaKind,
    /// Original tag literal as it appeared in the stream (e.g. `<MEDIA: /tmp/foo.png>`),
    /// used by handler.rs D-19 reinsert path when an attachment fails.
    pub original_tag_text: String,
}

pub struct MediaTagExtractor { /* private state */ }

impl MediaTagExtractor {
    pub fn new() -> Self;
    pub fn reset(&mut self);
    /// Feed a streaming delta; returns the visible portion to forward to the user.
    pub fn feed(&mut self, delta: &str) -> String;
    /// Flush any held-back partial prefix at end-of-stream — returns it as VISIBLE text
    /// (diverges from scrubber's discard semantics, per RESEARCH §Pattern Audit).
    pub fn flush_tail(&mut self) -> String;
    /// Drain accumulated tags; subsequent calls return `vec![]` until new tags are fed.
    pub fn take_attachments(&mut self) -> Vec<MediaRef>;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit test table (RED) — extractor + types</name>
  <read_first>
    - crates/ironhermes-agent/src/streaming_scrubber.rs (full file; the test block at the bottom is the analog template — case shape, naming convention, `non_ascii_before_tag_does_not_panic` is the regression to mirror)
    - crates/ironhermes-gateway/src/lib.rs (verify the `pub mod` declaration style for task 3)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-CONTEXT.md (decisions D-05/D-06/D-08/D-09 — the grammar + dispatch table + state-machine contracts these tests assert)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md (§Pattern Audit lists the 10 "test cases the extractor MUST cover" verbatim — copy each into a `#[test]` fn; Pitfall 5 escaped-backtick case is a MUST)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md (§1 entry — confirms `find_ascii_ci` and `max_partial_suffix` helper shape to mirror)
  </read_first>
  <behavior>
    Create `crates/ironhermes-gateway/src/media_tag.rs` with the four public types from `<interfaces>` (real type bodies, since they have no algorithm to stub) and a stubbed `MediaTagExtractor` whose `feed`, `flush_tail`, and `take_attachments` all return `unimplemented!()`. Add `#[cfg(test)] mod tests` block with these named cases (each test imports `super::*` and constructs a fresh `MediaTagExtractor::new()`):

    Grammar + happy path:
    - `full_tag_in_one_delta_produces_one_mediaref`: feed `<MEDIA: /tmp/foo.png>`; visible string is empty; `take_attachments()` returns one `MediaRef { source: MediaSource::Path(PathBuf::from("/tmp/foo.png")), kind: MediaKind::Photo, original_tag_text: "<MEDIA: /tmp/foo.png>".to_string() }`.
    - `tag_with_leading_whitespace_in_body`: `<MEDIA:   foo.png>` (per D-06 `<MEDIA:\s*([^>]+)>` — whitespace after the colon is consumed); body trims to `foo.png`; produces Photo MediaRef.

    Streaming partial-prefix buffering (mirrors scrubber):
    - `split_open_tag_across_two_deltas`: feed `<MEDIA: f`; visible is empty; feed `oo.png>`; visible is empty; `take_attachments()` returns one MediaRef.
    - `partial_prefix_disproved_emits_verbatim`: feed `<MEDI`; visible is empty (held back); feed `um is the message`; visible string contains `<MEDIum is the message` (the prefix was held, then disambiguated, then emitted verbatim); `take_attachments()` returns `vec![]`.
    - `tag_never_closes_flush_tail_emits_visible`: feed `prefix <MEDIA: foo`; stream ends; `flush_tail()` returns `<MEDIA: foo` (note: extractor policy diverges from scrubber — scrubber DISCARDS unterminated spans; extractor EMITS them per RESEARCH §Pattern Audit "flush_tail policy DIFFERS" and "user trust requires tag-like text never disappears"); `take_attachments()` returns `vec![]`.

    Multi-tag + ordering:
    - `multiple_tags_in_one_delta_preserve_stream_order`: feed `<MEDIA: a.png>middle<MEDIA: b.ogg>`; visible is `middle`; `take_attachments()` returns `[Photo("a.png"), Voice("b.ogg")]` in that exact order.
    - `take_attachments_drains_and_clears`: same input as above; first call returns 2 entries, second call returns `vec![]`.

    D-09 fence + inline-code skip:
    - `tag_inside_fenced_code_passes_through`: feed `here is a fence\n\`\`\`\n<MEDIA: foo.png>\n\`\`\`\ntail`; visible string contains the literal `<MEDIA: foo.png>` (pass-through inside fence); `take_attachments()` returns `vec![]`.
    - `tag_inside_inline_code_passes_through`: feed `text \`<MEDIA: foo.png>\` more`; visible string contains the literal `<MEDIA: foo.png>` between the backticks; `take_attachments()` returns `vec![]`.
    - `escaped_backtick_does_not_open_inline_code`: feed `text \\\`<MEDIA: foo.png>\\\` more` (literal `\`` escaped backticks, not inline-code openers); `take_attachments()` returns one Photo MediaRef (Pitfall 5 mitigation: the `\`` is escaped, so inline-code state stays false, so the tag DOES extract).
    - `inline_code_inside_fence_no_extraction_either_way`: feed a fence containing `\`<MEDIA: foo.png>\``; visible contains the literal; `take_attachments()` returns `vec![]` (fence state dominates inline-code state).

    URL vs path + MediaKind dispatch (D-06):
    - `url_form_https_produces_url_source`: feed `<MEDIA: https://example.com/image.jpg>`; MediaRef has `source: MediaSource::Url("https://example.com/image.jpg".to_string())` and `kind: MediaKind::Photo`.
    - `url_form_http_produces_url_source`: same with `http://`.
    - `path_with_relative_segments_produces_path_source`: `<MEDIA: ./relative/path.png>`; `source: MediaSource::Path(PathBuf::from("./relative/path.png"))`.
    - `extension_dispatch_table`: parameterized assertions for every D-06 extension — `.png`/`.jpg`/`.jpeg`/`.webp`/`.gif` → Photo; `.ogg`/`.opus` → Voice; `.mp3`/`.m4a`/`.flac`/`.wav` → Audio; `.mp4`/`.mov`/`.webm` → Video; `.pdf`/`.txt`/no-extension → Document. One assertion per extension is fine; do not loop.
    - `extension_case_insensitive`: `<MEDIA: FOO.PNG>` produces MediaKind::Photo.
    - `url_extension_in_path_segment`: `<MEDIA: https://example.com/a/b/foo.mp3?token=x>` produces Audio (extension is read from the last path segment, ignoring query string).

    Robustness regressions:
    - `non_ascii_text_around_tag_no_panic`: feed `İİİ <MEDIA: foo.png> Привет`; visible contains `İİİ` and `Привет`; `take_attachments()` returns one Photo MediaRef (mirror of scrubber's CR-01 regression case).
    - `empty_delta_returns_empty_string_no_state_change`: feed `""`; visible is `""`; `take_attachments()` returns `vec![]`.
    - `reset_clears_all_state`: feed `<MEDIA: foo.png>`; call `reset()`; `take_attachments()` returns `vec![]`; subsequent `feed(<MEDIA: bar.png>)` extracts only `bar.png`.

    Run `cargo test -p ironhermes-gateway media_tag` to confirm RED (all tests panic on `unimplemented!()`). Commit: `test(36.17.2.2-02): add failing tests for MediaTagExtractor`.
  </behavior>
  <action>Create `crates/ironhermes-gateway/src/media_tag.rs`. At module top, define `MediaSource`, `MediaKind`, `MediaRef` per `<interfaces>` (these are real type bodies — derive `Debug`, `Clone`, `PartialEq`, `Eq` on all three; `MediaKind` also derives `Copy`). Define `pub struct MediaTagExtractor { in_tag: bool, in_fenced_code: bool, in_inline_code: bool, prev_was_escape: bool, tag_body: String, buf: String, attachments: Vec<MediaRef> }` with `pub fn new()`, `pub fn reset(&mut self)`, and `pub fn feed`, `pub fn flush_tail`, `pub fn take_attachments` all stubbed `unimplemented!()`. Add the `#[cfg(test)] mod tests { use super::*; use std::path::PathBuf; ... }` block with the named test functions from `<behavior>` above. Do NOT add `pub mod media_tag;` to `lib.rs` yet — task 3 wires it post-GREEN. Per D-16 (outbound-only), DO NOT touch `crates/ironhermes-gateway/src/telegram.rs` `tg_message_to_event` (line 378-431) — these new types are outbound-only.</action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-gateway --tests 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'pub struct MediaTagExtractor' crates/ironhermes-gateway/src/media_tag.rs` returns `1`.
    - Source: `grep -c 'pub enum MediaSource' crates/ironhermes-gateway/src/media_tag.rs` returns `1`.
    - Source: `grep -c 'pub enum MediaKind' crates/ironhermes-gateway/src/media_tag.rs` returns `1`.
    - Source: `grep -c 'pub struct MediaRef' crates/ironhermes-gateway/src/media_tag.rs` returns `1`.
    - Source: `grep -E 'in_fenced_code|in_inline_code|prev_was_escape' crates/ironhermes-gateway/src/media_tag.rs | wc -l` returns at least `3` (the D-09 + Pitfall 5 state booleans declared as struct fields).
    - Source: at least 19 `#[test]` fns present (`grep -c '#\[test\]' crates/ironhermes-gateway/src/media_tag.rs` returns ≥ 19).
    - Source: named tests `tag_never_closes_flush_tail_emits_visible`, `escaped_backtick_does_not_open_inline_code`, `non_ascii_text_around_tag_no_panic`, `take_attachments_drains_and_clears`, `extension_dispatch_table`, `tag_inside_fenced_code_passes_through` all appear (`grep -E 'fn (tag_never_closes_flush_tail_emits_visible|escaped_backtick_does_not_open_inline_code|non_ascii_text_around_tag_no_panic|take_attachments_drains_and_clears|extension_dispatch_table|tag_inside_fenced_code_passes_through)' crates/ironhermes-gateway/src/media_tag.rs | wc -l` returns `6`).
    - Behavior: `cargo build -p ironhermes-gateway --tests` exits 0 (stubs compile even with `lib.rs` not yet declaring the module — Rust will reject task-3 wiring if the file does not compile alone, so this build step is the precondition for task 3).
    - Source assertion: `grep -c 'fn tg_message_to_event' crates/ironhermes-gateway/src/telegram.rs` is unchanged from baseline (single occurrence around line 378; the D-16 outbound-only guarantee is preserved — task did not touch the inbound path).
  </acceptance_criteria>
  <done>RED step complete. Stub types compile, test table exists, no implementation. TDD commit `test(36.17.2.2-02): add failing tests for MediaTagExtractor`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MediaTagExtractor (GREEN) — mirror StreamingContextScrubber + add D-09 / Pitfall 5 state</name>
  <read_first>
    - crates/ironhermes-gateway/src/media_tag.rs (the stubs + tests from task 1 — implementations replace `unimplemented!()`)
    - crates/ironhermes-agent/src/streaming_scrubber.rs (THE template — copy `find_ascii_ci`, `max_partial_suffix`, and the overall `feed()` loop shape verbatim; substitute `OPEN_TAG = "<MEDIA:"` and a single `>` close marker; add accumulator + fence/inline-code state)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md (§1 entry shows the exact code excerpts to mirror and the adaptation notes)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md (§Pattern Audit non-obvious pitfalls list — items 1-6 are the implementation traps; especially "ASCII byte search NOT `to_lowercase()`" and "max_partial_suffix returns at most tag.len()-1")
  </read_first>
  <behavior>
    Replace the four `unimplemented!()` stub bodies with real logic. Algorithm summary (port from `StreamingContextScrubber::feed` at streaming_scrubber.rs:56-122 unless otherwise noted):

    1. `feed(delta)` — prepend `self.buf` to `delta`. Then loop over the combined buffer:
       a. If `self.in_tag`: search for first `>` byte in the remaining slice. Found → append `&buf[..idx]` to `self.tag_body`, parse `self.tag_body` per D-06 (URL detect by `starts_with("http://")` or `starts_with("https://")`; trim leading whitespace; infer MediaKind via the last-segment extension table). Push to `self.attachments` (build `original_tag_text` as `format!("<MEDIA: {}>", self.tag_body)`). Clear `self.tag_body`. Advance past `>`. Set `self.in_tag = false`. Continue. Not found → append the entire remaining slice to `self.tag_body`, return accumulated visible output.
       b. Else if `self.in_fenced_code`: search forward byte-by-byte for the next ```` ``` ```` triple-backtick run (ASCII byte search, no `to_lowercase()`). If found, emit `&buf[..end_of_fence_close]` to visible output (the fence text including the closing backticks passes through verbatim), advance past it, toggle `self.in_fenced_code = false`, continue. If not found, emit the entire remaining slice to visible (and possibly hold a max_partial_suffix of ``` ``` ``` similar to scrubber's partial-prefix handling) and return.
       c. Else if `self.in_inline_code`: walk byte-by-byte; on the first unescaped `` ` ``, emit `&buf[..=idx_of_backtick]` to visible, toggle `self.in_inline_code = false`, advance, continue. (Track `prev_was_escape` per char — `\` followed by `` ` `` does NOT toggle state.) If not found, emit the rest verbatim and return.
       d. Else: search for the earliest of these openers in the remaining slice: ```` ``` ```` (fence open), `` ` `` (inline-code open — only if NOT followed by another ``` ``` `` ``` `` triple), `<MEDIA:` (case-insensitive ASCII via `find_ascii_ci`, mirror scrubber). Whichever appears first wins:
          - Fence: emit text before, push the three backticks to visible, set `self.in_fenced_code = true`, advance, continue.
          - Inline code: check `prev_was_escape` — if true (the backtick is escaped), emit it verbatim and DO NOT toggle. Else emit text before + the backtick to visible, set `self.in_inline_code = true`, continue.
          - `<MEDIA:`: emit text before to visible, advance past the opener, set `self.in_tag = true`, continue (clear `self.tag_body` first).
          - None found: emit the slice minus a `max_partial_suffix(buf, "<MEDIA:")` tail; the tail is held in `self.buf` for the next `feed()` call (mirror scrubber).
       Update `prev_was_escape` per char emitted.

    2. `flush_tail()` — extractor policy (DIFFERS from scrubber): if `self.in_tag` is true, return `format!("<MEDIA:{}{}", " ", self.tag_body)` so the user sees the unterminated tag literal (RESEARCH §Pattern Audit "flush_tail policy DIFFERS — user trust requires tag-like text never disappears"). If `self.in_tag` is false, return `std::mem::take(&mut self.buf)` (any held partial prefix turned out not to be a real tag — emit verbatim). Clear `self.in_tag`, `self.tag_body`, `self.buf` after producing the return string. Do NOT clear `self.attachments` (those are drained separately by `take_attachments`).

    3. `take_attachments()` — `std::mem::take(&mut self.attachments)`.

    4. `reset()` — clear ALL state (`in_tag`, `in_fenced_code`, `in_inline_code`, `prev_was_escape`, `tag_body`, `buf`, `attachments`).

    Helper fns to define inside the file (private): `fn find_ascii_ci(haystack: &str, needle: &str) -> Option<usize>` and `fn max_partial_suffix(buf: &str, tag: &str) -> usize` — copy verbatim from `streaming_scrubber.rs:148-173` (RESEARCH §Pattern Audit confirms the exact byte-level eq_ignore_ascii_case approach is the correct mitigation for the non-ASCII regression CR-01).

    Run `cargo test -p ironhermes-gateway media_tag` until all task-1 tests pass. TDD commit: `feat(36.17.2.2-02): implement MediaTagExtractor with D-09 fence/inline-code skip and Pitfall 5 backslash respect`.
  </behavior>
  <action>Edit `crates/ironhermes-gateway/src/media_tag.rs` and replace all four `unimplemented!()` bodies per the `<behavior>` algorithm. Copy `find_ascii_ci` and `max_partial_suffix` helper functions verbatim from `crates/ironhermes-agent/src/streaming_scrubber.rs:148-173` (substitute appropriate scope — these are private to the new module). The `OPEN_TAG: &str = "<MEDIA:"` constant goes at module top (mirror scrubber's const-at-top style). Do NOT depend on `regex` — RESEARCH §Don't Hand-Roll explicitly says reuse the scrubber's byte-level approach (regex would lose the partial-prefix behavior). Per Open Q5 / Assumption A10, this plan does NOT chain extractor + scrubber together — plan 06 does that wiring inside `handler.rs::run_agent`. This plan's job ends at producing a correct standalone extractor.</action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p ironhermes-gateway --lib media_tag 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `cargo test -p ironhermes-gateway --lib media_tag` exits 0 with `test result: ok` and the number of passed tests is ≥ 19 (task-1 count).
    - Source: `grep -c 'unimplemented!()' crates/ironhermes-gateway/src/media_tag.rs` returns `0`.
    - Source: `grep -c 'eq_ignore_ascii_case' crates/ironhermes-gateway/src/media_tag.rs` returns at least `2` (the ASCII byte search helpers — confirms the CR-01 mitigation pattern was copied, not replaced with `to_lowercase()`).
    - Source: `grep -c 'fn find_ascii_ci' crates/ironhermes-gateway/src/media_tag.rs` returns `1` and `grep -c 'fn max_partial_suffix' crates/ironhermes-gateway/src/media_tag.rs` returns `1`.
    - Source: `grep -c 'use regex' crates/ironhermes-gateway/src/media_tag.rs` returns `0` (no regex dependency added — partial-prefix buffering requires byte-level state).
    - Source: `grep -c 'OPEN_TAG' crates/ironhermes-gateway/src/media_tag.rs` returns at least `2` (constant declared and used).
  </acceptance_criteria>
  <done>GREEN: every D-05/D-06/D-08/D-09/Pitfall-5 case from task 1 passes. TDD commit `feat(36.17.2.2-02): ...` recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Wire pub mod media_tag in lib.rs and confirm gateway crate builds clean</name>
  <read_first>
    - crates/ironhermes-gateway/src/lib.rs (post-plan-01 state — should already declare `pub mod markdown_v2;`)
    - crates/ironhermes-gateway/src/media_tag.rs (the file the new `pub mod` declaration must point at)
  </read_first>
  <action>Add `pub mod media_tag;` to `crates/ironhermes-gateway/src/lib.rs` in the same `pub mod` group as `markdown_v2` and the other gateway modules (`adapter`, `handler`, `runner`, `stream_consumer`, `telegram`, `session_queue`, `user_queue`). Preserve the existing ordering convention (alphabetical if used). The types must be publicly importable as `crate::media_tag::{MediaTagExtractor, MediaRef, MediaSource, MediaKind}` so plans 03/04/06 can consume them.</action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-gateway 2>&amp;1 | tail -5 &amp;&amp; cargo test -p ironhermes-gateway media_tag 2>&amp;1 | tail -5 &amp;&amp; cargo test -p ironhermes-gateway markdown_v2 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c '^pub mod media_tag;' crates/ironhermes-gateway/src/lib.rs` returns `1`.
    - Source: both `pub mod markdown_v2;` and `pub mod media_tag;` are present (`grep -E '^pub mod (markdown_v2|media_tag);' crates/ironhermes-gateway/src/lib.rs | wc -l` returns `2`).
    - Behavior: `cargo build -p ironhermes-gateway` exits 0.
    - Behavior: `cargo test -p ironhermes-gateway media_tag` exits 0 (tests discoverable via crate-level selector, not just `--lib`).
    - Behavior: `cargo test -p ironhermes-gateway markdown_v2` still exits 0 (no regression to plan 01).
    - Behavior: `cargo test -p ironhermes-gateway 2>&amp;1 | grep -E 'test result.*FAIL|^error' | wc -l` returns `0` (no regression to existing gateway suite).
  </acceptance_criteria>
  <done>`pub mod media_tag;` declared. Types importable from sibling crate modules. Full gateway suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-streamed delta → MediaTagExtractor | Model-controlled text drives accumulator; later phases (plan 04/06) consume the resulting `MediaRef.source` path/URL. Path-validation happens in plan 04 (`send_file_multipart` callers) and plan 06 (D-15 oversize pre-check + path canonicalization). This plan does NOT validate paths — extractor's job is faithful extraction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.17.2.2-EXTRACT-NONASCII-PANIC | Denial of Service | MediaTagExtractor::feed (non-ASCII input) | mitigate | Mirror `StreamingContextScrubber`'s ASCII-byte `eq_ignore_ascii_case` approach (NEVER `to_lowercase()` per RESEARCH §Pattern Audit pitfall 1). Regression test `non_ascii_text_around_tag_no_panic` exercises the U+0130 + Cyrillic surface that triggered scrubber's CR-01. |
| T-36.17.2.2-EXTRACT-FENCE-BYPASS | Information Disclosure | MediaTagExtractor::feed (D-09 fence skip) | mitigate | D-09 contract: tags inside ```` ``` ```` fences or `` ` `` inline-code spans pass through unchanged with no extraction. Tests `tag_inside_fenced_code_passes_through`, `tag_inside_inline_code_passes_through`, `inline_code_inside_fence_no_extraction_either_way` enforce this. Prevents the model from "documenting the convention in a code block" while also triggering an unintended attachment. |
| T-36.17.2.2-EXTRACT-ESCAPED-TICK | Tampering | MediaTagExtractor inline-code state (Pitfall 5) | mitigate | `prev_was_escape` field tracks unescaped-vs-escaped backticks. Test `escaped_backtick_does_not_open_inline_code` asserts the extractor's correct interpretation. |
| T-36.17.2.2-EXTRACT-MEM-LEAK | Denial of Service | MediaTagExtractor.tag_body unbounded growth on unterminated tag across many deltas | accept | RESEARCH does not flag this and the gateway's existing per-turn timeout bounds the lifetime of an extractor instance. `take_attachments`/`reset`/end-of-turn destruction reclaim memory. Acceptable for the current threat model. If unbounded `tag_body` becomes a concern, a future patch can cap it (e.g. discard the tag and emit verbatim past 4 KB). |
</threat_model>

<verification>
- `cargo test -p ironhermes-gateway media_tag` green with all 19+ named tests passing.
- `cargo build -p ironhermes-gateway` exits 0; no regression to plan 01's `markdown_v2` test suite.
- No `unimplemented!()` literals remain.
- `pub mod media_tag;` declared in `lib.rs` so plans 04/06 can `use crate::media_tag::*;`.
- `tg_message_to_event` in `telegram.rs:378` byte-unchanged (D-16 outbound-only).
</verification>

<success_criteria>
- All must_haves observable via the test selector `cargo test -p ironhermes-gateway media_tag`.
- TDD commits land in order: `test(36.17.2.2-02): ...` (RED), `feat(36.17.2.2-02): ...` (GREEN), optional `chore(36.17.2.2-02): wire media_tag in lib.rs` (or fold into the GREEN commit).
- The four type exports + extractor are ready for plan 03 (trait declaration in `adapter.rs`), plan 04 (`impl MediaSender for TelegramAdapter` uses `MediaSource`/`MediaKind`), and plan 06 (handler.rs `MediaTagExtractor` wired alongside scrubber).
</success_criteria>

<output>
Create `.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-02-SUMMARY.md` when done — record final test count, any extension-table additions beyond D-06, and a one-line note on how `flush_tail` handled the unterminated-tag case (extractor's emit-as-visible policy diverges from scrubber's discard policy and that divergence belongs on record).
</output>
