---
phase: 36.17.2.2
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - crates/ironhermes-gateway/src/markdown_v2.rs
  - crates/ironhermes-gateway/src/lib.rs
autonomous: true
requirements:
  - D-01
  - D-04
user_setup: []

must_haves:
  truths:
    - "`cargo test -p ironhermes-gateway markdown_v2` is green for every cell in the golden table (D-04 unconditional escape AND smart `escape_outside_code_blocks` covering fence/inline-code/link-URL state)."
    - "Reserved chars inside ```fenced``` blocks survive unescaped (D-04 fence interior)."
    - "Reserved chars inside `inline code` spans survive unescaped (D-04 inline-code state)."
    - "Reserved chars inside `[label](url)` URL halves survive unescaped (D-04 link-URL state — Pitfall 1 mitigation)."
    - "Pre-escaped reserved chars (`\\.`) are not double-escaped (D-04 backslash respect)."
  artifacts:
    - path: "crates/ironhermes-gateway/src/markdown_v2.rs"
      provides: "`pub fn escape_markdown_v2(&str) -> String` AND `pub fn escape_outside_code_blocks(&str) -> String` plus inline `#[cfg(test)] mod tests` golden table."
      contains: "pub fn escape_markdown_v2"
    - path: "crates/ironhermes-gateway/src/lib.rs"
      provides: "`pub mod markdown_v2;` declaration so other gateway modules and tests can `use crate::markdown_v2::escape_outside_code_blocks;`."
      contains: "pub mod markdown_v2"
  key_links:
    - from: "crates/ironhermes-gateway/src/markdown_v2.rs"
      to: "crates/ironhermes-gateway/src/lib.rs"
      via: "pub mod declaration"
      pattern: "pub mod markdown_v2"
---

<objective>
Land the D-04 MarkdownV2 escape module — the foundation every other plan in this phase consumes. Two pure functions plus a golden test table sourced from the CONTEXT.md `<canonical_refs>` §Test fixtures list, written FIRST (TDD) so the three concurrent state machines (fence / inline-code / link-URL) are pinned down by test before any consumer wires them up.

Purpose: D-04 lists three concurrent state machines (fence / inline-code / link-URL) and RESEARCH.md flags this as the highest-risk piece of escape logic in the phase (Pitfall 1 — naive escape inside link URLs corrupts links). Writing the golden table first ensures every state transition is observably correct before the function is wired into `stream_consumer.rs:100` in plan 05.
Output: `crates/ironhermes-gateway/src/markdown_v2.rs` with two `pub fn` exports and an inline `#[cfg(test)] mod tests` block of golden cases; `crates/ironhermes-gateway/src/lib.rs` exposes the module so plans 04/05 can import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-CONTEXT.md
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md
@crates/ironhermes-agent/src/streaming_scrubber.rs
@crates/ironhermes-gateway/src/lib.rs
</context>

<interfaces>
<!-- Module exports the rest of the phase will consume (after plan 01 ships). -->

```rust
// crates/ironhermes-gateway/src/markdown_v2.rs (new)

/// Unconditional escape of all 18 Telegram MarkdownV2 reserved chars.
/// Use when the body is known to be plain literal text (no markdown syntax).
pub fn escape_markdown_v2(text: &str) -> String;

/// Smart escape that preserves real MarkdownV2 syntax — backslash-escapes the
/// 18 reserved chars OUTSIDE of fenced code blocks, inline-code spans, and
/// `[label](url)` link URLs; passes them through verbatim INSIDE those contexts.
pub fn escape_outside_code_blocks(text: &str) -> String;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write golden test table (RED) — escape_markdown_v2 + escape_outside_code_blocks</name>
  <read_first>
    - crates/ironhermes-gateway/src/lib.rs (verify module-declaration style for the `pub mod markdown_v2;` add in task 3)
    - crates/ironhermes-agent/src/streaming_scrubber.rs (the test layout template — `#[cfg(test)] mod tests { use super::*; #[test] fn ...` block shape; do NOT copy its scrubber logic)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-CONTEXT.md (D-04 + `<canonical_refs>` §Test fixtures bullet list — the canonical case enumeration)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md (Pitfall 1 link-URL state pitfall and the "FLAGGED RISK — HIGH D-04 escape-function complexity" item — test cases must include these regressions)
  </read_first>
  <behavior>
    Create `crates/ironhermes-gateway/src/markdown_v2.rs` with stub `pub fn escape_markdown_v2(_text: &str) -> String { unimplemented!() }` and stub `pub fn escape_outside_code_blocks(_text: &str) -> String { unimplemented!() }`, then a `#[cfg(test)] mod tests` block covering every case below. Each `#[test]` calls one or both functions and asserts the exact expected string. Tests MUST fail with `unimplemented!()` panics before task 2 — that is the RED step.

    Cases for `escape_markdown_v2` (unconditional escape — used by callers that know their body is plain literal text):
    - `all_18_reserved_chars_isolated`: input `_*[]()~\`>#+-=|{}.!`; each char gains a leading backslash; total length doubles.
    - `non_reserved_chars_pass_through`: ASCII letters/digits, spaces, and non-ASCII characters such as `İ` and `Привет` are returned verbatim (no escape, no panic).
    - `pre_escaped_reserved_not_double_escaped`: input `\\.` returns `\\.` (the existing escape is honored, no second backslash inserted).
    - `empty_input_returns_empty`.

    Cases for `escape_outside_code_blocks` (smart escape — the function `stream_consumer.rs:100` will call in plan 05):
    - `bold_star_pair_preserved`: input `*bold*` returns `*bold*` (the surrounding asterisks open/close bold syntax; they must NOT escape).
    - `double_star_bold_preserved`: input `**bold**` returns `**bold**`.
    - `single_underscore_italic_preserved`: input `_italic_` returns `_italic_`.
    - `double_underscore_italic_preserved`: input `__italic__` returns `__italic__`.
    - `inline_code_passes_reserved_through`: input `` `foo.bar(baz)!` `` returns `` `foo.bar(baz)!` `` unchanged (D-04 inline-code state — reserved chars inside backticks are preserved).
    - `fenced_code_passes_all_18_chars_through`: input is a triple-backtick fence containing every one of the 18 reserved chars on one line; output equals input exactly (D-04 fence interior).
    - `link_label_escaped_url_preserved`: input `[label.with.dots](https://example.com/foo.bar)` returns the link with the label's dots escaped (`[label\.with\.dots]`) BUT the URL inside parens UNCHANGED (`(https://example.com/foo.bar)`) — Pitfall 1 mitigation.
    - `link_url_with_inner_parens`: input `[label](url with (parens))` returns the label half escaped and the URL half — including the inner literal parens — unchanged.
    - `backslash_escaped_backtick_does_not_open_code`: input `` Use \` for inline code, then a literal . here `` returns the trailing `.` ESCAPED (the `\`` is a literal escaped backtick, NOT an inline-code opener — Pitfall 5 mitigation).
    - `inline_code_inside_fence_no_effect`: input is a fence containing `` `bar.baz` ``; output equals input exactly (inside a fence, the inner backtick does NOT toggle inline-code state — fenced state dominates).
    - `reserved_after_existing_escape_not_doubled`: input `already-\\.` returns `already\-\\.` (the unescaped `-` outside any context is escaped; the pre-escaped `\\.` is left alone).
    - `mixed_real_world`: input ``Hello *world*. Visit [docs](https://x.com/a.b) or run `cat file.txt` — done.`` returns the bold pair preserved, the `.` after `*world*` escaped, the link label escaped, the URL preserved, the inline-code body preserved, and the bare em-dash + final `.` escaped. Write the exact expected string out in the test.

    Run `cargo test -p ironhermes-gateway markdown_v2` and capture the failing output as evidence of RED (record commit message: `test(36.17.2.2-01): add failing golden table for markdown_v2 escape` per the TDD commit convention).
  </behavior>
  <action>Create the new file `crates/ironhermes-gateway/src/markdown_v2.rs` containing the stubbed `escape_markdown_v2` and `escape_outside_code_blocks` functions (both returning `unimplemented!()`) plus a `#[cfg(test)] mod tests { use super::*; ... }` block containing the 13 named test cases above. Use the const `RESERVED: &[char] = &['_', '*', '[', ']', '(', ')', '~', '`', '>', '#', '+', '-', '=', '|', '{', '}', '.', '!'];` defined at module top per RESEARCH.md §Code Examples — both the production code and the tests reference this slice for the "all 18" enumeration. Per D-04, name the unit module `markdown_v2` (matches the file name) so `cargo test -p ironhermes-gateway markdown_v2` is the discovery selector. Do NOT add `pub mod markdown_v2;` to `lib.rs` yet — task 3 wires it after GREEN.</action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p ironhermes-gateway --lib markdown_v2 2>&amp;1 | grep -E "test result|panicked|unimplemented" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'pub fn escape_markdown_v2' crates/ironhermes-gateway/src/markdown_v2.rs` returns `1`.
    - Source assertion: `grep -c 'pub fn escape_outside_code_blocks' crates/ironhermes-gateway/src/markdown_v2.rs` returns `1`.
    - Source assertion: `grep -c '#\[test\]' crates/ironhermes-gateway/src/markdown_v2.rs` returns at least `13`.
    - Behavior: `cargo build -p ironhermes-gateway --tests 2>&amp;1` succeeds (stubs compile under `#[cfg(test)]` discovery even though `lib.rs` does not yet declare the module — file is reachable via `--lib` test root only after task 3, so use `cargo build -p ironhermes-gateway --tests` here just to verify Rust syntax; the failing-test step happens in task 3 after wiring).
    - Source assertion: the named tests `all_18_reserved_chars_isolated`, `link_label_escaped_url_preserved`, `backslash_escaped_backtick_does_not_open_code`, `fenced_code_passes_all_18_chars_through`, `mixed_real_world` all literally appear in the test block (`grep -E 'fn (all_18_reserved_chars_isolated|link_label_escaped_url_preserved|backslash_escaped_backtick_does_not_open_code|fenced_code_passes_all_18_chars_through|mixed_real_world)' crates/ironhermes-gateway/src/markdown_v2.rs | wc -l` returns `5`).
  </acceptance_criteria>
  <done>RED step complete: golden test table exists, stubs return `unimplemented!()`, file compiles under `cargo build -p ironhermes-gateway --tests` once it is exposed (task 3 wires `pub mod`). Per TDD convention from `tdd_integration` section of the planner role, commit message `test(36.17.2.2-01): add failing golden table for markdown_v2 escape`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement escape functions (GREEN)</name>
  <read_first>
    - crates/ironhermes-gateway/src/markdown_v2.rs (the stubs + test table from task 1 — implementations replace `unimplemented!()`)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md (§Code Examples sketch plus Pitfall 1 link-URL pitfall AND Pitfall 5 backslash-backtick pitfall)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md (§2 entry — sketch confirms `const RESERVED` placement and module-level shape)
  </read_first>
  <behavior>
    Replace the two `unimplemented!()` stubs with real implementations so every test from task 1 passes.

    `escape_markdown_v2(text)` walks `text.chars()` once; for each char, if `RESERVED.contains(&c)` AND the previous emitted char was NOT a literal backslash (track via local `prev_was_backslash: bool`), push `\` then `c`; otherwise push `c`. This preserves the `\\.` → `\\.` invariant from task 1's `pre_escaped_reserved_not_double_escaped` case.

    `escape_outside_code_blocks(text)` is a single forward pass over `text.chars()` with three booleans: `in_fence: bool`, `in_inline_code: bool`, `in_link_url: bool` (plus `prev_was_backslash`). Transitions:
    - Detect ```` ``` ```` fence opener/closer by looking at the next three bytes of the remaining slice (use a `Peekable<Chars>` or simple index advance). On match, push the three backticks verbatim and toggle `in_fence`; advance the iterator past all three. While `in_fence`, every char passes through verbatim — no escape, no inner state toggles.
    - When NOT in a fence and the current char is `` ` `` AND `!prev_was_backslash`, toggle `in_inline_code` and push the backtick verbatim.
    - When NOT in a fence and NOT in inline-code, detect link grammar: a `[` opens a label (push verbatim); on `]` followed immediately by `(`, set `in_link_url = true` after pushing `](`; on the matching `)` that closes the URL (track depth to handle `url with (parens)` per the `link_url_with_inner_parens` case), clear `in_link_url` after pushing `)`.
    - When `in_fence`, `in_inline_code`, or `in_link_url` is true → push the current char verbatim regardless of whether it is in `RESERVED`.
    - Otherwise → if `RESERVED.contains(&c)` and `!prev_was_backslash`, push `\\` then `c`; else push `c`.
    - Always update `prev_was_backslash` to `c == '\\'` (taking into account that an escape we just inserted means the NEXT char's `prev_was_backslash` is true if we wrote `\\`).

    Run `cargo test -p ironhermes-gateway --lib markdown_v2` until every test from task 1 is green. Once green, commit `feat(36.17.2.2-01): implement MarkdownV2 escape with fence/inline-code/link-URL state` per the TDD GREEN commit convention.
  </behavior>
  <action>Edit `crates/ironhermes-gateway/src/markdown_v2.rs` and replace both `unimplemented!()` stub bodies with the implementations described in `<behavior>` above. Keep the `RESERVED` const slice as the single source of truth for the 18 chars. Do NOT introduce regex crate dependencies — RESEARCH.md §Don't Hand-Roll explicitly calls out hand-rolling the state machine (regex would be wrong here because the three contexts need single-pass tracking). If the link-URL paren-depth case is ambiguous on edge cases not covered by the test table, prefer the behavior that PRESERVES the URL (no escape) — Pitfall 1 says under-preserving the URL breaks user-visible links, while over-preserving (passing a reserved char through) at worst triggers D-02's plain-text retry fallback.</action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p ironhermes-gateway --lib markdown_v2 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `cargo test -p ironhermes-gateway --lib markdown_v2` exits 0 with `test result: ok. 13 passed; 0 failed` (count ≥ 13 — task 1 may have added more cases).
    - Source assertion: `grep -c 'unimplemented!()' crates/ironhermes-gateway/src/markdown_v2.rs` returns `0` (no stubs left).
    - Source assertion: `grep -E 'in_fence|in_inline_code|in_link_url' crates/ironhermes-gateway/src/markdown_v2.rs | wc -l` returns at least `3` (state machine present).
    - Source assertion: `grep -c 'use regex' crates/ironhermes-gateway/src/markdown_v2.rs` returns `0` (no regex dependency added per RESEARCH §Don't Hand-Roll).
  </acceptance_criteria>
  <done>GREEN: all golden cases pass. TDD commit `feat(36.17.2.2-01): implement MarkdownV2 escape with fence/inline-code/link-URL state` recorded. No `unimplemented!()` remains.</done>
</task>

<task type="auto">
  <name>Task 3: Wire module into lib.rs and confirm full gateway crate builds</name>
  <read_first>
    - crates/ironhermes-gateway/src/lib.rs (existing `pub mod` declarations — match the alphabetical/grouping convention used today)
    - crates/ironhermes-gateway/src/markdown_v2.rs (the file the new `pub mod` declaration must point at)
  </read_first>
  <action>Add `pub mod markdown_v2;` to `crates/ironhermes-gateway/src/lib.rs` in the same `pub mod` group that already declares `adapter`, `handler`, `runner`, `stream_consumer`, `telegram`, `session_queue`, `user_queue` (preserve the existing ordering style — alphabetical if that is what the file uses today). Per D-04, the module is `pub` so plans 05 (stream_consumer switchover) and 06 (handler.rs reinsert-body construction) can `use crate::markdown_v2::escape_outside_code_blocks;`. Do not change visibility on other modules.</action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-gateway 2>&amp;1 | tail -5 &amp;&amp; cargo test -p ironhermes-gateway markdown_v2 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '^pub mod markdown_v2;' crates/ironhermes-gateway/src/lib.rs` returns `1`.
    - Behavior: `cargo build -p ironhermes-gateway` exits 0 (no compile error from the new module).
    - Behavior: `cargo test -p ironhermes-gateway markdown_v2` exits 0 with all golden cases passing — including discovery via the `-p ... markdown_v2` selector (not just `--lib markdown_v2`).
    - Behavior: `cargo test -p ironhermes-gateway 2>&amp;1 | grep -E 'test result.*FAIL|^error' | wc -l` returns `0` (no regression to the wider gateway crate suite; pre-existing tests still pass — the prior-phase 36.17.2 D-03 cursor-strip regression is one of these).
  </acceptance_criteria>
  <done>`pub mod markdown_v2;` declared in lib.rs. Module is now importable as `crate::markdown_v2::{escape_markdown_v2, escape_outside_code_blocks}` from any sibling gateway module. Full gateway crate builds, all existing tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → Telegram MarkdownV2 parser | Model-controlled text is interpreted by Telegram as MarkdownV2; malformed escapes can break parsing or (more subtly) corrupt link URLs the user clicks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.17.2.2-MD-INJECTION | Tampering | escape_outside_code_blocks (markdown_v2.rs) | mitigate | Golden test `link_label_escaped_url_preserved` asserts `[label.with.dots](https://example.com/foo.bar)` keeps the URL intact (no escape inside parens). Golden test `backslash_escaped_backtick_does_not_open_code` asserts `\`` does not toggle inline-code state. Both regressions covered by task 1's test table. Phase-level D-02 single-retry-as-plain-text fallback (plan 04) absorbs any escape bug that slips through. |
| T-36.17.2.2-MD-PARSEFAIL | Denial of Service | edit_message_markdown_v2 call site (plan 04 wires it) | accept | If the escape function under-escapes and Telegram returns `400 can't parse entities`, D-02 retries as plain text — worst-case user-visible degradation is loss of markdown rendering on that turn. Acceptable per CONTEXT.md `<deferred>` "Markdown linter / pre-validation before final edit". |
</threat_model>

<verification>
- `cargo test -p ironhermes-gateway markdown_v2` green with all 13+ golden cases.
- `cargo build -p ironhermes-gateway` succeeds.
- No `unimplemented!()` literals remain in the file.
- `pub mod markdown_v2;` present in `lib.rs`.
- Module is importable from elsewhere in the crate (verified implicitly when plan 04 adds the import in `telegram.rs` for the `edit_message_markdown_v2` body and plan 05 adds it in `stream_consumer.rs` for the overflow-chunk path).
</verification>

<success_criteria>
- All must_haves truths observable via the test selector `cargo test -p ironhermes-gateway markdown_v2`.
- `crates/ironhermes-gateway/src/markdown_v2.rs` exists with both `pub fn` exports.
- `crates/ironhermes-gateway/src/lib.rs` declares `pub mod markdown_v2;`.
- Three TDD commits land in order: `test(36.17.2.2-01): ...` (RED), `feat(36.17.2.2-01): ...` (GREEN), `chore(36.17.2.2-01): wire markdown_v2 in lib.rs` (or fold the wiring into the GREEN commit if the executor prefers two commits over three).
</success_criteria>

<output>
Create `.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-01-SUMMARY.md` when done — record the exact test count, any cases added beyond the 13 planned, and a one-line note on whether the link-URL paren-depth tracker required a stack or just a depth counter (informs future maintainers).
</output>
