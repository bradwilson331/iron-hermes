---
phase: 36.17.2.2
plan: 07
type: execute
wave: 2
depends_on:
  - 36.17.2.2-04
  - 36.17.2.2-05
  - 36.17.2.2-06
files_modified:
  - crates/ironhermes-gateway/tests/telegram_media_uat.md
autonomous: false
requirements:
  - D-01
  - D-02
  - D-07
  - D-09
  - D-10
  - D-11
  - D-12
  - D-14
  - D-15
  - D-20
user_setup:
  - service: telegram
    why: "Live UAT requires a real Telegram bot + test chat to verify client-side rendering (MarkdownV2 bold/italic, voice-bubble vs music-player UI, photo render, etc.) — Telegram does not expose a 'rendered HTML' API endpoint."
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "Test bot token from @BotFather (separate from production)"
      - name: IRONHERMES_HOME
        source: "Test config dir — separate from production (e.g. /tmp/uat-36.17.2.2-home)"
    dashboard_config:
      - task: "Create test bot via @BotFather and grant it admin in a test chat where the operator can send messages."
        location: "Telegram → @BotFather → /newbot"

must_haves:
  truths:
    - "`crates/ironhermes-gateway/tests/telegram_media_uat.md` exists with 9 scenarios per D-20 mapped to the post-Wave-1 production behavior."
    - "The runbook structure mirrors `tests/session_queue_telegram_uat.md` (36.17.2 D-22 template) — same prerequisites table, same Scenario-N format, same `approved`-gated sign-off checklist."
    - "The 9 scenarios cover: (1) text-only MarkdownV2 final edit, (2) single photo, (3) voice .ogg, (4) audio .mp3, (5) multi-tag photo+voice+document, (6) URL form passthrough, (7) missing path reinsert, (8) markdown parse error → plain-text fallback, (9) tag inside fenced code block → verbatim pass-through."
    - "The Document History block at the bottom is initialized with one entry for Phase 36.17.2.2."
    - "Sign-off block reads `approved` per the parent-phase D-22 protocol (operator's literal reply gates phase exit)."
  artifacts:
    - path: "crates/ironhermes-gateway/tests/telegram_media_uat.md"
      provides: "Operator runbook for the 9 D-20 scenarios; blocking-human checkpoint at the end."
      contains: "Scenario 9"
  key_links:
    - from: "crates/ironhermes-gateway/tests/telegram_media_uat.md"
      to: "crates/ironhermes-gateway/tests/session_queue_telegram_uat.md"
      via: "structural template (Phase 36.17.2 D-22 protocol)"
      pattern: "Sign-off Checklist|Document History"
---

<objective>
Author the live UAT runbook for `<MEDIA: ...>` delivery + MarkdownV2 final-text rendering and gate phase exit on the operator typing `approved` after running all 9 scenarios. Mirrors the 36.17.2 D-22 protocol that closed the session-queue phase.

Purpose: Telegram client rendering (MarkdownV2 bold/italic visible to user, voice-bubble vs music-player UI, photo render order, etc.) cannot be observed via the Bot API — there is no "rendered HTML" endpoint. The integration test in plan 06 covers everything API-observable; this runbook covers everything else. Per D-20 + VALIDATION.md Manual-Only Verifications table, 9 behaviors require live operator verification.
Output: `crates/ironhermes-gateway/tests/telegram_media_uat.md` runbook; one human-verify checkpoint task at the end requesting operator `approved` reply.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-CONTEXT.md
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-VALIDATION.md
@crates/ironhermes-gateway/tests/session_queue_telegram_uat.md
@.planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author crates/ironhermes-gateway/tests/telegram_media_uat.md per D-20 (9 scenarios)</name>
  <read_first>
    - crates/ironhermes-gateway/tests/session_queue_telegram_uat.md (full file — the structural template per PATTERNS §12; header front-matter, prerequisites table, Scenario-N section format, sign-off checklist, document history footer)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-CONTEXT.md (D-20 §`Three-layer test plan` Live UAT sub-bullet — the 9 scenario titles)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-VALIDATION.md (Manual-Only Verifications table — each row maps to one Scenario)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md (§12 — adaptation notes for the new runbook)
    - .planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-CONTEXT.md (D-22 — the `approved`-gated sign-off protocol this phase inherits)
  </read_first>
  <action>Create new file `crates/ironhermes-gateway/tests/telegram_media_uat.md` mirroring `crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` line-for-line at the structural level. Required sections:

(a) Header block (top of file):

    `# Manual UAT: Telegram Media Delivery — Live UAT`
    `> **Phase reference:** Phase 36.17.2.2`
    `> ` .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/`
    `>`
    `> **Architecture (locked):** Final-edit rendered as MarkdownV2 with smart escape; streaming intermediate edits stay plain text per prior-phase 36.17.2 D-03; <MEDIA: path|url> tags extracted and dispatched as native Telegram attachments AFTER the final edit completes; failed attachments reinsert into the placeholder; tags inside fenced code pass through verbatim.`
    `>`
    `> **Locked decisions exercised here:**`
    `> - **D-01** — Final edit sends parse_mode: MarkdownV2 with smart escape.`
    `> - **D-02** — Single retry as plain text on parse-mode 400.`
    `> - **D-07** — Text-first, attachment-second; no caption on attachments.`
    `> - **D-09** — Tags inside ```...``` fences pass through verbatim, no extraction.`
    `> - **D-10** — Missing / unreadable / oversized media → warn + reinsert tag literal into placeholder.`
    `> - **D-11** — Multiple tags = sequential send_media calls in stream order.`
    `> - **D-12** — URL form passes through to Telegram unchanged (no local fetch).`
    `> - **D-14** — .ogg/.opus → sendVoice (inline bubble); .mp3/.m4a/.flac/.wav → sendAudio (music player).`
    `> - **D-15** — File size pre-check before multipart upload.`
    `> - **D-20** — Live Telegram UAT runbook gated as a checkpoint:human-verify task.`
    `>`
    `> **Inherited contracts (from 36.17.2 / 36.17.2.1 / 36.17.2.2):**`
    `> - D-03 (36.17.2) — Streaming intermediate edits stay plain text; cursor █ strip preserved.`
    `> - D-06 (36.17.2) — Per-chat worker pop-loop unchanged; media dispatch happens INSIDE handle_with_multimodal so the queue/worker shape is preserved.`
    `> - D-22 (36.17.2) — Live Telegram UAT runbook gated on operator approved reply.`
    `>`
    `> **Why this runbook exists:** the automated suite in tests/telegram_media_delivery.rs covers the in-process handler + adapter paths exhaustively. What automated tests cannot verify is Telegram client RENDERING — MarkdownV2 bold/italic visible, voice-bubble UI vs music-player UI, the user's actual perception of attachment order, missing-path reinsert appearance in the chat. This runbook exists to confirm those user-visible properties.`

(b) Prerequisites table (mirror session_queue_telegram_uat.md:41-57):

    | Item | Value | How to set |
    |------|-------|------------|
    | `TELEGRAM_BOT_TOKEN` | Your test bot's token from `@BotFather` | `export TELEGRAM_BOT_TOKEN=...` |
    | `IRONHERMES_HOME` | Test config dir — separate from production | `export IRONHERMES_HOME=/tmp/uat-36.17.2.2-home` |
    | Test chat | A Telegram chat where the bot is admin'd | Add bot, `/start`, send "hi" to confirm |
    | Agent config | Free-text messages must produce a real agent turn | Pick a model in cli-config.yaml; verify the bot responds |
    | Test media files | Pre-create local files for scenarios 2-5 | `/tmp/uat-photo.png` (any small PNG ≤ 1 MiB), `/tmp/uat-voice.ogg` (any small Opus ≤ 1 MiB), `/tmp/uat-music.mp3` (any small MP3 ≤ 5 MiB), `/tmp/uat-doc.pdf` (any small PDF ≤ 5 MiB), `/tmp/uat-oversize.png` (a 21 MiB file — `dd if=/dev/zero of=/tmp/uat-oversize.png bs=1M count=21`) |
    | Public URL | A reachable public-internet PNG URL for scenario 6 | e.g. `https://www.gstatic.com/webp/gallery/1.webp` or any operator-chosen stable URL |

    `Start the gateway in a terminal you can ctrl+c later:`
    ` `` ``bash`
    `cargo run --release --bin ironhermes -- gateway 2>&1 | tee /tmp/uat-36.17.2.2-gateway.log`
    ` `` `

(c) 9 Scenario sections — for each, include `What this verifies (D-NN)`, `Steps` (numbered), `Expected`, and `Regression signal`:

    Scenario 1 — Text-only MarkdownV2 final edit (D-01)
    - What: Final-edit body renders with bold + italic visible in the Telegram client.
    - Steps:
      1. Send to the test chat: `Reply with the literal text: **bold** and *italic* and `code` and a [link](https://example.com).`
      2. Wait for the agent's reply to finalize (cursor █ stops).
    - Expected: The reply renders `**bold**` as bold text, `*italic*` as italic text, `` `code` `` as a monospace span, and `[link](https://example.com)` as a clickable link. No raw asterisks or backticks visible.
    - Regression signal: If raw `**bold**` or `*italic*` text appears literally, MarkdownV2 escape is over-escaping; if 👀 reaction appears but no body, the V2 edit silently 400'd and D-02 fallback did not catch it.

    Scenario 2 — Single photo (D-07)
    - What: Text edit + sendPhoto arrive in the correct order.
    - Steps:
      1. Send: `Reply with the literal text "Here is the picture:" followed by <MEDIA: /tmp/uat-photo.png> on its own line.`
      2. Wait for the agent's reply.
    - Expected: The placeholder message renders as "Here is the picture:" (the tag stripped). A SECOND message appears immediately after with the photo as a native Telegram attachment.
    - Regression signal: Photo arrives BEFORE text → D-07 ordering broken; tag literal `<MEDIA: /tmp/uat-photo.png>` visible in the placeholder → extractor not wired; the photo never arrives → media_sender not set in runner.rs (Pitfall 3).

    Scenario 3 — Voice .ogg (D-14)
    - What: `.ogg` files dispatch to sendVoice and render as the inline round bubble.
    - Steps:
      1. Send: `Reply with <MEDIA: /tmp/uat-voice.ogg>.`
      2. Wait.
    - Expected: A Telegram voice message bubble (round play-button UI) appears, not a generic file attachment or audio music-player.
    - Regression signal: If a music-player UI appears, D-14 dispatch is routing `.ogg` to sendAudio instead of sendVoice; if a generic document UI appears, dispatch fell through to sendDocument (wrong extension table).

    Scenario 4 — Audio .mp3 (D-14)
    - What: `.mp3` files dispatch to sendAudio and render as the music-player UI.
    - Steps:
      1. Send: `Reply with <MEDIA: /tmp/uat-music.mp3>.`
      2. Wait.
    - Expected: A Telegram audio attachment with the music-player UI (waveform + play button + title metadata).
    - Regression signal: Voice-bubble appears instead → dispatch routed `.mp3` to sendVoice (wrong; Telegram rejects non-opus on sendVoice anyway, so the D-10 reinsert may fire showing the tag literal — a different failure signature).

    Scenario 5 — Multi-tag (D-11)
    - What: Three tags produce one text edit + 3 attachments in correct order.
    - Steps:
      1. Send: `Reply with three attachments in this exact order: photo, then voice, then document. The literal text body should say "Sending three things:". Use <MEDIA: /tmp/uat-photo.png>, <MEDIA: /tmp/uat-voice.ogg>, <MEDIA: /tmp/uat-doc.pdf>.`
      2. Wait until all 4 messages have settled.
    - Expected: 4 messages in order: (1) placeholder rendered as "Sending three things:" (or similar — tags stripped), (2) photo attachment, (3) voice bubble, (4) document attachment.
    - Regression signal: Out-of-order arrival → D-11 sequential await broken; missing one → D-15 size cap triggered (verify file sizes); single combined message instead of 4 → extractor not extracting individual tags.

    Scenario 6 — URL form (D-12)
    - What: A `<MEDIA: https://...>` tag passes through to Telegram which fetches and renders.
    - Steps:
      1. Send: `Reply with <MEDIA: https://www.gstatic.com/webp/gallery/1.webp>.` (or operator-chosen stable URL).
      2. Wait.
    - Expected: Photo renders in chat as a native attachment.
    - Regression signal: D-10 reinsert appears with the URL literal → Telegram's fetch failed (verify URL is publicly reachable from Telegram's servers, not just from your local machine); 404-style error in `/tmp/uat-36.17.2.2-gateway.log` → URL was rejected before passthrough.

    Scenario 7 — Missing path reinsert (D-10)
    - What: A nonexistent path triggers warn + reinsert of the tag literal in the placeholder.
    - Steps:
      1. Send: `Reply with literal text "Sending file:" followed by <MEDIA: /tmp/this-file-does-not-exist-xxx.png>.`
      2. Wait.
    - Expected: Placeholder updates to show "Sending file:" AND the tag literal `<MEDIA: /tmp/this-file-does-not-exist-xxx.png>` appended (visible as text). The gateway log contains a `warn!` line referencing the path's filename.
    - Regression signal: Tag literal does NOT appear in the placeholder → D-10 reinsert path broken; full path (not just filename) appears in the log → T-LOG-LEAK mitigation broken.

    Scenario 8 — Markdown parse error → plain-text fallback (D-02)
    - What: A malformed MarkdownV2 body (unclosed `**`) triggers the D-02 silent retry as plain text.
    - Steps:
      1. Send: `Reply with the literal text "**unclosed bold and `unclosed_code` and a trailing dot."` (Note the intentional dangling `**` and dangling backtick — invalid MarkdownV2.)
      2. Wait.
    - Expected: The reply text appears in the chat (as plain text, NOT bold-rendered). The gateway log contains a `warn!` line "MarkdownV2 parse failed; retrying as plain text (D-02 fallback)" with the message_id of the failed edit.
    - Regression signal: 👀 reaction but no body → D-02 second-failure path fired; the body shows bold-rendered text → escape function silently fixed the malformed input rather than letting D-02 catch it (unexpected, but not necessarily wrong — note the deviation).

    Scenario 9 — Tag inside fence passes through (D-09)
    - What: A `<MEDIA: ...>` tag inside a fenced code block is NOT extracted; the literal text reaches the user.
    - Steps:
      1. Send: `Reply with a fenced code block containing literally <MEDIA: /tmp/uat-photo.png> on its own line, with the surrounding text "Here is the tag syntax:" before the fence.`
      2. Wait.
    - Expected: Placeholder shows "Here is the tag syntax:" followed by a fenced code block containing the literal text `<MEDIA: /tmp/uat-photo.png>`. NO photo attachment arrives.
    - Regression signal: Photo attachment arrives → extractor's D-09 fence skip is broken; the tag is stripped from the visible body → same.

(d) Sign-off Checklist (mirror session_queue_telegram_uat.md:493-523):

    `## Sign-off Checklist`
    `Run each scenario once on a live Telegram bot. Tick when verified.`
    `- [ ] **Scenario 1** — Text-only MarkdownV2 renders.`
    `- [ ] **Scenario 2** — Single photo arrives AFTER the text edit.`
    `- [ ] **Scenario 3** — .ogg voice bubble appears.`
    `- [ ] **Scenario 4** — .mp3 music-player UI appears.`
    `- [ ] **Scenario 5** — Multi-tag arrives in correct order (photo, voice, document).`
    `- [ ] **Scenario 6** — URL form fetches and renders.`
    `- [ ] **Scenario 7** — Missing path triggers warn + reinsert.`
    `- [ ] **Scenario 8** — Malformed Markdown triggers D-02 plain-text fallback.`
    `- [ ] **Scenario 9** — Tag inside fence passes through; no attachment.`
    `Verifier (your name): ____________________`
    `Date (YYYY-MM-DD): ____________________`
    `Notes / failures (attach screenshots + log excerpts if any scenario fails):`
    ` `` ``  `
    `[paste here]`
    ` `` `

(e) Document History footer (mirror session_queue_telegram_uat.md:527-532):

    `## Document History`
    `- **2026-06-03 (Phase 36.17.2.2-07):** Initial runbook for `<MEDIA: ...>` media delivery and MarkdownV2 final-text rendering.`</action>
  <verify>
    <automated>test -f /Users/twilson/code/ironhermes/crates/ironhermes-gateway/tests/telegram_media_uat.md &amp;&amp; grep -c '## Scenario' /Users/twilson/code/ironhermes/crates/ironhermes-gateway/tests/telegram_media_uat.md &amp;&amp; grep -c 'Sign-off Checklist' /Users/twilson/code/ironhermes/crates/ironhermes-gateway/tests/telegram_media_uat.md</automated>
  </verify>
  <acceptance_criteria>
    - File: `test -f crates/ironhermes-gateway/tests/telegram_media_uat.md` exits 0.
    - Source: `grep -c '## Scenario' crates/ironhermes-gateway/tests/telegram_media_uat.md` returns at least `9`.
    - Source: `grep -c 'Sign-off Checklist' crates/ironhermes-gateway/tests/telegram_media_uat.md` returns at least `1`.
    - Source: `grep -c 'Document History' crates/ironhermes-gateway/tests/telegram_media_uat.md` returns at least `1`.
    - Source: each named decision appears at least once — `grep -cE 'D-01|D-02|D-07|D-09|D-10|D-11|D-12|D-14|D-15|D-20' crates/ironhermes-gateway/tests/telegram_media_uat.md` returns at least `10`.
    - Source: scenario titles reference the right surface — `grep -E '^### Scenario [1-9]\b' crates/ironhermes-gateway/tests/telegram_media_uat.md | wc -l` returns at least `9` (alternatively `^## Scenario` if the section level is one deeper; verify against the session_queue_telegram_uat.md template's level).
    - Source: phase reference present — `grep -c '36.17.2.2' crates/ironhermes-gateway/tests/telegram_media_uat.md` returns at least `2`.
  </acceptance_criteria>
  <done>Runbook file exists with all 9 scenarios, prerequisites, sign-off checklist, and document history footer. Mirrors the 36.17.2 D-22 template per PATTERNS §12.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs all 9 UAT scenarios on a live Telegram bot and replies `approved`</name>
  <what-built>
    The full Phase 36.17.2.2 production pipeline is in place: MarkdownV2 final edits with smart escape, `<MEDIA: ...>` tag extraction and native attachment dispatch (5 types), URL passthrough, missing-path / oversize reinsert, D-02 plain-text fallback, D-09 fenced-code pass-through. Plan 06's integration test covers everything API-observable. This checkpoint verifies the Telegram client-rendering surface that the Bot API cannot expose to automated tests.
  </what-built>
  <how-to-verify>
    1. Read `crates/ironhermes-gateway/tests/telegram_media_uat.md` (created by task 1 above).
    2. Set up the prerequisites table — separate test bot, separate `IRONHERMES_HOME`, test chat where the bot is admin, pre-created media files at `/tmp/uat-{photo.png,voice.ogg,music.mp3,doc.pdf,oversize.png}`, one reachable public PNG URL.
    3. Start the gateway: `cargo run --release --bin ironhermes -- gateway 2>&amp;1 | tee /tmp/uat-36.17.2.2-gateway.log` in a terminal you can `ctrl+c`.
    4. Run each of the 9 scenarios in order. For each: send the prompt to the test chat, wait for the agent reply to finalize, compare against the scenario's "Expected" section, tick the corresponding sign-off checkbox if matched.
    5. If any scenario fails, record the failure in the runbook's `Notes / failures` block (screenshots + the relevant `/tmp/uat-36.17.2.2-gateway.log` excerpt). Do NOT type `approved` in that case — instead reply with a description of what failed so the executor can open a gap-closure plan.
    6. If all 9 scenarios pass, fill in `Verifier` and `Date` in the runbook, commit the runbook file with the ticks, then reply `approved` in the chat to release the checkpoint.
  </how-to-verify>
  <resume-signal>Type "approved" once all 9 scenarios pass, OR describe any failure(s) for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → live Telegram bot → real Bot API | Live UAT touches real Telegram infrastructure with operator-controlled bot token. Operator is the trust authority for this checkpoint. |
| `IRONHERMES_HOME=/tmp/uat-...` → test config | Operator MUST use a separate `IRONHERMES_HOME` from production to avoid polluting production state. Documented in prerequisites table. |
| Test chat → real users | Operator MUST use a test chat with bot admin granted, not a chat with non-test users; the bot's responses during UAT include the literal teaching prompt of D-NN identifiers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-UAT-MANUAL (informational, phase-level) | — | Operator-driven verification | accept | Per parent-phase 36.17.2 D-22 protocol, blocking-human checkpoint with operator `approved` reply is the canonical phase-exit gate for surfaces the Bot API cannot expose to automated tests. Acceptable cost. |
| T-UAT-PROD-CONFIG-LEAK | Information Disclosure | If operator forgets to set IRONHERMES_HOME | mitigate | Prerequisites table makes the env var setup explicit. Runbook's first item enforces separation. |
| T-UAT-BOT-TOKEN-IN-LOGS | Information Disclosure | TELEGRAM_BOT_TOKEN possibly in gateway log | accept | Gateway log redaction is out of scope for this phase; operators are expected to inspect/redact `/tmp/uat-36.17.2.2-gateway.log` before sharing. |
| T-UAT-MEDIA-UPLOAD-LEAKS | Information Disclosure | Operator-created test media files | accept | Operator-supplied test files are non-sensitive by definition. The phase-level T-INPUT-MEDIA-PATH mitigation (plan 04 canonicalization + allowed roots) protects against the LLM emitting paths outside test fixtures. |
| T-UAT-SKIPPED-SCENARIO | Reliability | Operator skips a scenario | mitigate | Sign-off checklist requires explicit tick per scenario; `approved` reply gate ensures explicit acceptance. Skipped scenarios mean unchecked boxes — `approved` should only be typed after ALL 9 boxes are ticked. |
</threat_model>

<verification>
- `test -f crates/ironhermes-gateway/tests/telegram_media_uat.md` exits 0.
- Runbook contains 9 scenarios, each citing the D-NN decision it verifies.
- Sign-off checklist with 9 unchecked boxes + `Verifier` and `Date` placeholders.
- Operator reply `approved` after running all 9 scenarios on a live Telegram bot.
- Once `approved` arrives, set `nyquist_compliant: true` and `wave_0_complete: true` in `.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-VALIDATION.md` frontmatter; this is the executor's final step before phase exit.
</verification>

<success_criteria>
- All must_haves observable via file existence + grep.
- Operator-`approved` reply gates phase exit per the parent-phase D-22 protocol.
- 2 commits: `docs(36.17.2.2-07): live UAT runbook for media delivery + MarkdownV2 rendering` and (post-approval) `docs(36.17.2.2-07): UAT sign-off — Phase 36.17.2.2 approved by operator`.
- After approval, the must_haves of the whole phase are demonstrated: items 1-4 (`cargo test ...` greps from phase-level must_haves) green from plans 01-02-04-06; items 5-8 (source assertions) green from plans 03-04-06; item 9 (UAT existence) green from this plan; item 10 (no D-03 regression) green from plan 05's explicit non-modification of the intermediate-edit branch + the existing 36.17.2 test suite passing.
</success_criteria>

<output>
Create `.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-07-SUMMARY.md` when done — record the operator's `approved` timestamp, the verifier name, and any deviations recorded in the runbook's Notes / failures block.
</output>
