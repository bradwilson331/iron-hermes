---
phase: 36.17.2.2
plan: 07
subsystem: ironhermes-gateway
tags:
  - telegram
  - uat
  - runbook
  - markdown-v2
  - media-delivery
dependency-graph:
  requires:
    - 36.17.2.2-04
    - 36.17.2.2-05
    - 36.17.2.2-06
  provides:
    - "Operator UAT runbook for 9 D-20 live-Telegram scenarios"
  affects:
    - "Phase 36.17.2.2 exit gate (operator `approved` reply)"
tech-stack:
  added: []
  patterns:
    - "36.17.2 D-22 live-UAT runbook template (mirrored line-for-line)"
key-files:
  created:
    - crates/ironhermes-gateway/tests/telegram_media_uat.md
  modified: []
decisions:
  - "Mirrored session_queue_telegram_uat.md structure at `##` (h2) section depth — Sign-off Checklist + Document History footer match template line-for-line."
  - "Scenario headers carry the D-NN identifier inline (e.g. `## Scenario 1 — Text-only MarkdownV2 final edit (D-01)`) so grep-based acceptance for decision coverage succeeds at the header level."
metrics:
  duration: "~5 min"
  completed: "2026-06-03"
  tasks_completed: 1
  tasks_total: 2
  tasks_deferred: 1
---

# Phase 36.17.2.2 Plan 07: Live UAT runbook for media delivery + MarkdownV2 rendering — Summary

Authored `crates/ironhermes-gateway/tests/telegram_media_uat.md` — a 9-scenario operator runbook that mirrors the 36.17.2 D-22 protocol and gates Phase 36.17.2.2 exit on an `approved` reply after the operator runs all 9 scenarios on a live Telegram bot. Task 2 (the blocking-human-verify checkpoint where the operator actually runs the UAT) is **DEFERRED to a separate session**; this run only authored the runbook.

## What was built

A live UAT runbook at `crates/ironhermes-gateway/tests/telegram_media_uat.md` covering the 9 Telegram client-rendering surfaces that the Bot API cannot expose to automated tests:

| # | Scenario | Decision |
|---|----------|----------|
| 1 | Text-only MarkdownV2 final edit (bold/italic/code/link) | D-01 |
| 2 | Single photo — text edit + sendPhoto arrive in order | D-07 |
| 3 | Voice `.ogg` → round bubble UI | D-14 |
| 4 | Audio `.mp3` → music-player UI | D-14 |
| 5 | Multi-tag (photo + voice + document) in order | D-11 |
| 6 | URL form `<MEDIA: https://...>` passthrough | D-12 |
| 7 | Missing path → `warn!` + tag literal reinsert + filename-only log | D-10 / T-LOG-LEAK |
| 8 | Malformed Markdown → D-02 plain-text fallback | D-02 |
| 9 | Tag inside fenced code block → verbatim pass-through (no attachment) | D-09 |

The runbook also documents the inherited contracts from 36.17.2 (D-03 streaming-text invariant, D-06 worker-loop preservation, D-22 `approved`-gate protocol) and D-15 size-cap behavior implicitly covered by Scenario 5.

## Verification (acceptance criteria from plan)

| Check | Required | Actual |
|-------|----------|--------|
| `test -f crates/ironhermes-gateway/tests/telegram_media_uat.md` | exit 0 | PASS |
| `grep -c '## Scenario' …` | ≥ 9 | 9 |
| `grep -c 'Sign-off Checklist' …` | ≥ 1 | 1 |
| `grep -c 'Document History' …` | ≥ 1 | 1 |
| `grep -cE 'D-01\|D-02\|D-07\|D-09\|D-10\|D-11\|D-12\|D-14\|D-15\|D-20' …` | ≥ 10 | 39 |
| `grep -c '36.17.2.2' …` | ≥ 2 | 8 |
| Sign-off Checklist unchecked `- [ ]` boxes | exactly 9 | 9 |

All Task 1 acceptance gates pass.

## Deferred — Task 2 (operator-approved live UAT)

**Task 2 (operator-approved live UAT) DEFERRED — runbook authored only. Operator runs the 9 scenarios separately and replies `approved`.**

Task 2 is the `type="checkpoint:human-verify"` `gate="blocking"` step that requires:

1. Operator to set `TELEGRAM_BOT_TOKEN` + `IRONHERMES_HOME=/tmp/uat-36.17.2.2-home`
2. Operator to start `cargo run --release --bin ironhermes -- gateway`
3. Operator to pre-create `/tmp/uat-{photo.png,voice.ogg,music.mp3,doc.pdf,oversize.png}` (the oversize file via `dd if=/dev/zero of=/tmp/uat-oversize.png bs=1M count=21`)
4. Operator to run each of the 9 scenarios on a live test chat
5. Operator to tick the sign-off checklist, commit, and reply `approved` in chat

This run did NOT:
- Invoke `cargo run --bin ironhermes -- gateway`
- Set or use `TELEGRAM_BOT_TOKEN`
- Create UAT prerequisite files at `/tmp/uat-*`
- Modify `nyquist_compliant` / `wave_0_complete` in `VALIDATION.md` frontmatter
- Update `STATE.md` or `ROADMAP.md` (orchestrator owns those writes)

After the operator replies `approved` in a future session, the executor for the follow-up plan should:
1. Record the operator's `approved` timestamp + verifier name + any failure notes from the runbook
2. Flip `nyquist_compliant: true` and `wave_0_complete: true` in `36.17.2.2-VALIDATION.md` frontmatter
3. Commit `docs(36.17.2.2-07): UAT sign-off — Phase 36.17.2.2 approved by operator`

## Deviations from Plan

None — runbook authored exactly to the `<action>` block spec in the plan. Scenarios 1-9 use `##` (h2) headers per the template; the plan's acceptance criteria accept either `##` or `###` and the actual count check is `grep -c '## Scenario'` which passes at 9.

## Known Stubs

None. The runbook is authored content (no code stubs); the deferral of Task 2 is intentional and documented above.

## Threat Flags

None. The runbook introduces no new security surface — it documents existing surface for operator verification. T-UAT-* threats from the plan's `<threat_model>` are addressed within the runbook itself (separate `IRONHERMES_HOME` requirement; explicit sign-off checklist enforces no-skipped-scenario; bot-token-in-logs is operator-managed).

## Commits

- `[hash filled by commit]` — docs(36.17.2.2-07): live UAT runbook for media delivery + MarkdownV2 rendering

## Self-Check: PASSED

- File `crates/ironhermes-gateway/tests/telegram_media_uat.md`: FOUND
- All 7 acceptance-criteria greps: PASS (see Verification table above)
- SUMMARY.md created at `.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-07-SUMMARY.md`
- Task 2 explicitly documented as DEFERRED — operator owns the live UAT
- STATE.md / ROADMAP.md / VALIDATION.md frontmatter: NOT modified (per scope_restriction)
