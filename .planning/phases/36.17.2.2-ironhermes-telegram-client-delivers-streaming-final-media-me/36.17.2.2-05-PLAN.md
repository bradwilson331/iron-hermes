---
phase: 36.17.2.2
plan: 05
type: execute
wave: 1
depends_on:
  - 36.17.2.2-01   # escape_outside_code_blocks
  - 36.17.2.2-04   # edit_message_markdown_v2 rename + PlatformAdapter trait
files_modified:
  - crates/ironhermes-gateway/src/adapter.rs
  - crates/ironhermes-gateway/src/telegram.rs
  - crates/ironhermes-gateway/src/stream_consumer.rs
autonomous: true
requirements:
  - D-01
  - D-03
  - D-04
user_setup: []

must_haves:
  truths:
    - "The final-edit call site at `crates/ironhermes-gateway/src/stream_consumer.rs` final-edit branch passes `&escape_outside_code_blocks(&remaining)` (not raw `&remaining`) to `edit_message_markdown_v2` — D-01 + D-04 contract honored."
    - "The overflow-chunk send sites at stream_consumer.rs (currently lines 121-128 and 131-134 — `send_message(&self.chat_id, &chunk, None)` and `send_message(&self.chat_id, &rest, None)`) route through a new `send_message_markdown_v2` method per the CONTEXT.md D-Discretion recommendation `every overflow chunk uses send_message_markdown_v2 so the entire response renders consistently`."
    - "A new `PlatformAdapter::send_message_markdown_v2` method exists with TelegramAdapter + MockAdapter + RecordingFailingAdapter impls, mirroring the per-impl pattern from plan 04's `edit_message_markdown_v2` rename (default body MAY delegate to `send_message` for impls where MarkdownV2 distinction is moot — e.g., test fixtures — but `TelegramAdapter` MUST send `parse_mode: MarkdownV2` and apply the D-02 fallback)."
    - "Streaming intermediate edits at `stream_consumer.rs:114, 161, 176` STAY plain text — D-03 invariant from prior phase 36.17.2 preserved. The cursor `█` strip path at lines 154-178 (per RESEARCH §Line-Number Drift Report `cursor is OMITTED BY CONSTRUCTION on final flush`) is untouched."
    - "Existing flush tests at stream_consumer.rs:343-575 continue to pass — including the renamed `test_final_edit_uses_edit_message_markdown_v2`."
    - "`cargo test -p ironhermes-gateway` is green (no regression to the 36.17.2 D-21 UQM test surface, no regression to stream_consumer's own unit tests)."
  artifacts:
    - path: "crates/ironhermes-gateway/src/stream_consumer.rs"
      provides: "Final-edit body pre-escaped through `escape_outside_code_blocks`; overflow chunks routed through `send_message_markdown_v2`; intermediate edits unchanged (D-03)."
      contains: "escape_outside_code_blocks"
    - path: "crates/ironhermes-gateway/src/adapter.rs"
      provides: "`send_message_markdown_v2` trait method added alongside the existing `send_message`."
      contains: "send_message_markdown_v2"
    - path: "crates/ironhermes-gateway/src/telegram.rs"
      provides: "`TelegramAdapter` impl of `send_message_markdown_v2` — `parse_mode: MarkdownV2` + D-02 single-retry-as-plain-text fallback."
      contains: "send_message_markdown_v2"
  key_links:
    - from: "crates/ironhermes-gateway/src/stream_consumer.rs"
      to: "crate::markdown_v2::escape_outside_code_blocks"
      via: "import + applied to body before edit_message_markdown_v2 and send_message_markdown_v2 calls in the final-edit branch only"
      pattern: "use crate::markdown_v2::|escape_outside_code_blocks"
---

<objective>
Complete the D-01 final-text rendering pipeline by applying `escape_outside_code_blocks` at the call site AND extending the overflow-chunk path to also render as MarkdownV2. After this plan, every byte of the final-edit body (single chunk OR multi-chunk overflow) reaches Telegram with `parse_mode: MarkdownV2` and a correctly-escaped body; intermediate streaming edits stay plain text per the prior-phase D-03 invariant.

Purpose: Plan 04 wired the trait + impl + D-02 fallback. Plan 04 task 3 renamed the call site for build-greenness but left the body un-escaped. This plan applies the escape (D-04 module from plan 01) and ALSO adds the `send_message_markdown_v2` trait method + impls per the CONTEXT.md D-Discretion recommendation that every overflow chunk render consistently as MarkdownV2 — RESEARCH FLAGGED RISK explicitly calls this out as a decision the planner must resolve.
Output: stream_consumer.rs uses MarkdownV2 with correctly-escaped bodies for both single-chunk and overflow paths; new `send_message_markdown_v2` trait method exists with impls on TelegramAdapter, MockAdapter, RecordingFailingAdapter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-CONTEXT.md
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md
@crates/ironhermes-gateway/src/stream_consumer.rs
@crates/ironhermes-gateway/src/adapter.rs
@crates/ironhermes-gateway/src/telegram.rs
@crates/ironhermes-gateway/src/markdown_v2.rs
@.planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-CONTEXT.md
</context>

<interfaces>
Post-plan-05 surface:

- `PlatformAdapter::send_message_markdown_v2(chat_id, content, thread_id) -> Result<MessageResponse>` sibling of the existing `send_message`. Callers pre-escape `content` via `crate::markdown_v2::escape_outside_code_blocks`.
- `stream_consumer.rs` final-edit branch:
    - Single chunk: `.edit_message_markdown_v2(&self.chat_id, &self.current_message_id, &escape_outside_code_blocks(&remaining)).await?`
    - Overflow chunk #1 (was line 123 `send_message`): `.send_message_markdown_v2(&self.chat_id, &escape_outside_code_blocks(&chunk), None).await?`
    - Overflow chunk #2+ tail (was line 133/167 `send_message`): same `send_message_markdown_v2` with `escape_outside_code_blocks(&rest)` / `escape_outside_code_blocks(&remainder)`.
- Intermediate (non-final) branch at stream_consumer.rs:114, 161 — UNCHANGED. Cursor `█` path (lines 154-178) UNCHANGED. D-03 invariant preserved.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add send_message_markdown_v2 to PlatformAdapter trait + impls (TelegramAdapter with D-02 fallback, MockAdapter + RecordingFailingAdapter as records-only)</name>
  <read_first>
    - crates/ironhermes-gateway/src/adapter.rs (post-plan-04 state — trait has `send_message` and `edit_message_markdown_v2`; we add a parallel `send_message_markdown_v2`)
    - crates/ironhermes-gateway/src/telegram.rs (anchors: `send_message` at :218-236 — the analog for the new method's body; `edit_message_markdown_v2` from plan 04 — the analog for the D-02 fallback shape; `api_call` helper for the actual HTTP plumbing)
    - crates/ironhermes-gateway/src/stream_consumer.rs (anchors: MockAdapter at :261-326; calls-log enum at :240-272 area which records each adapter method call — needs a new variant for the new method)
    - crates/ironhermes-gateway/tests/uqm_session_queue_unification.rs (RecordingFailingAdapter — add impl of new method recording into `send_log`)
    - crates/ironhermes-gateway/tests/session_queue_integration.rs (search for any PlatformAdapter impl needing the new method)
    - crates/ironhermes-gateway/tests/running_agent_guard_tests.rs (same)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-CONTEXT.md (D-Discretion final bullet — recommendation: every overflow chunk uses `send_message_markdown_v2` so the entire response renders consistently)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md (FLAGGED RISK row 11 — `D-Discretion overflow-chunk question is concrete` — recommendation matches CONTEXT and ships the new trait method)
  </read_first>
  <action>(a) Add to the `PlatformAdapter` trait in `crates/ironhermes-gateway/src/adapter.rs` a new method `async fn send_message_markdown_v2(&self, chat_id: &str, content: &str, thread_id: Option<&str>) -> Result<MessageResponse>;` (no default body — every impl must provide one to make the parse-mode policy explicit). Add a doc-comment that callers must pre-escape `content` via `crate::markdown_v2::escape_outside_code_blocks` and that the method is used by the overflow-chunk path in `stream_consumer.rs`. Place the new method declaration immediately after the existing `send_message` for grouping consistency.

(b) Implement `send_message_markdown_v2` on `TelegramAdapter` in `crates/ironhermes-gateway/src/telegram.rs` (next to the existing `send_message` at :218-236 and the new `edit_message_markdown_v2` from plan 04). Body shape: copy the existing `send_message` body verbatim, but add `"parse_mode": "MarkdownV2"` to the `params` json. Wrap in the same D-02 substring-match fallback shape as `edit_message_markdown_v2` (from plan 04 task 2): on `Err`, check `format!("{e}")` against `"can't parse"`, `"parse entities"`, `"parse_mode"`; if any match, log `tracing::warn!(chat_id, error = %desc, "MarkdownV2 parse failed on send_message_markdown_v2; retrying as plain text (D-02 fallback)")` and re-issue with `parse_mode` omitted. On retry-Ok return `Ok(MessageResponse { message_id: result.message_id.to_string(), chat_id: chat_id.to_string(), platform: Platform::Telegram })`. On retry-Err `tracing::error!` and return the second `Err`. The projection on success path is identical to existing `send_message`.

(c) Implement on `MockAdapter` in `crates/ironhermes-gateway/src/stream_consumer.rs`: add a new variant `SendMessageMarkdownV2 { chat_id: String, content: String }` (or whatever naming convention the existing calls-log enum uses — match the pattern of `SendMessage` / `EditMessageMarkdownV2`) to the calls-log enum near :240-272. Implement the trait method to push the new variant into the calls log and return `Ok(MessageResponse { message_id: format!("mock-msg-{...}"), chat_id: chat_id.to_string(), platform: Platform::Telegram })` mirroring the existing `send_message` impl shape (increment whatever `next_message_id` counter the fixture already uses — RESEARCH §`RecordingPlatformAdapter` mentions `next_message_id: StdMutex<i64>` at stream_consumer.rs:262-272).

(d) Implement on `RecordingFailingAdapter` in `crates/ironhermes-gateway/tests/uqm_session_queue_unification.rs`: same shape as the existing `send_message` impl (records into `send_log`, returns `Err` to fast-exit `run_agent` — that's the fixture's whole purpose). Doc-comment on the impl noting this fixture intentionally fails to exit early; the plan 06 integration test introduces a NEW succeeding fixture that does not.

(e) Repeat (d) for `crates/ironhermes-gateway/tests/session_queue_integration.rs` and `crates/ironhermes-gateway/tests/running_agent_guard_tests.rs` if either declares a PlatformAdapter impl. Use `rg -n 'impl PlatformAdapter for' crates/ironhermes-gateway/tests/` to enumerate. Compile-time check enforces no impl is missed — `cargo build` will fail if any impl block is missing the new method.</action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-gateway --tests 2>&amp;1 | tail -10 &amp;&amp; rg -n 'fn send_message_markdown_v2' crates/ironhermes-gateway/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Source: `rg -n 'fn send_message_markdown_v2' crates/ironhermes-gateway/` returns at least `4` (PlatformAdapter trait declaration + TelegramAdapter + MockAdapter + RecordingFailingAdapter).
    - Source: `grep -c 'tracing::warn!' crates/ironhermes-gateway/src/telegram.rs` returns at least `2` (the existing edit_message_markdown_v2 warn AND the new send_message_markdown_v2 warn — D-02 applied to both).
    - Source: `grep -c '"parse_mode": "MarkdownV2"' crates/ironhermes-gateway/src/telegram.rs` returns at least `2` (one for `edit_message_markdown_v2`, one for the new `send_message_markdown_v2`).
    - Behavior: `cargo build -p ironhermes-gateway --tests` exits 0 (every PlatformAdapter impl in src/ AND tests/ has the new method).
    - Behavior: `cargo test -p ironhermes-gateway 2>&amp;1 | grep -E 'test result.*FAIL' | wc -l` returns `0` (existing tests unchanged — no overflow tests yet reference the new method; that's task 2).
  </acceptance_criteria>
  <done>New `send_message_markdown_v2` trait method declared and implemented across all PlatformAdapter impls. TelegramAdapter has D-02 fallback. Test fixtures record calls. Gateway crate builds.</done>
</task>

<task type="auto">
  <name>Task 2: Wire escape_outside_code_blocks into the final-edit branch — single chunk + overflow chunks — and verify D-03 intermediate-edit invariant preserved</name>
  <read_first>
    - crates/ironhermes-gateway/src/stream_consumer.rs (anchors per RESEARCH §Line-Number Drift Report: final-edit branch at :88-105 with call site at :100 — already renamed to `edit_message_markdown_v2` in plan 04 task 3 but body still raw; overflow paths at :121-128 and :131-134 inside the final branch — currently call `send_message` with plain text; intermediate-edit branch at :114, 161, 176 — MUST stay plain text per D-03; cursor strip at :154-178 — MUST stay untouched per D-03)
    - crates/ironhermes-gateway/src/markdown_v2.rs (post-plan-01 — confirm `pub fn escape_outside_code_blocks(&str) -> String` is the import target)
    - .planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-CONTEXT.md (D-03 — intermediate edits stay plain text; cursor `█` strip preserved at `stream_consumer.rs:159-175`; this is the prior-phase invariant we must not break)
  </read_first>
  <action>Edit `crates/ironhermes-gateway/src/stream_consumer.rs`:

(a) Add the import `use crate::markdown_v2::escape_outside_code_blocks;` near the existing `use` block at the top of the file. Confirm `crate::markdown_v2` is accessible (plan 01 task 3 added `pub mod markdown_v2;` to `lib.rs`).

(b) At the renamed call site in the final-edit branch (post-plan-04 task 3, this is the `.edit_message_markdown_v2(&self.chat_id, &self.current_message_id, &remaining).await?` near line 100), wrap `&remaining` in the escape: change the third argument from `&remaining` to `&escape_outside_code_blocks(&remaining)`. The variable `remaining` is the running String slice for the final chunk; the escape returns an owned `String`, and the trait method takes `&str`, so `&` the owned string. No clone needed beyond what `escape_outside_code_blocks` itself allocates.

(c) At the two overflow-chunk send sites (currently `send_message(&self.chat_id, &chunk, None)` at line 123 and `send_message(&self.chat_id, &rest, None)` at line 133), change BOTH to `send_message_markdown_v2(&self.chat_id, &escape_outside_code_blocks(&chunk), None)` and `send_message_markdown_v2(&self.chat_id, &escape_outside_code_blocks(&rest), None)` respectively. Also handle the third overflow site at line 167 (the `send_message(&self.chat_id, &remainder, None)` call per the line-drift report) IF it is inside the final-edit branch — if it is inside the NON-final (intermediate) branch per the cursor-strip context at :154-178, LEAVE IT plain (D-03 invariant). Determine which branch each site is in by reading the surrounding `if final_edit { ... } else { ... }` structure; the cursor at line 9 (`const CURSOR`) is appended at line 151 (non-final branch only per RESEARCH §Line-Number Drift Report `cursor is appended at line 151 (non-final branch only)`), so the line-167 site sits in the non-final branch and STAYS plain.

(d) Leave the intermediate-edit `edit_message` calls at :114 and :161 alone. They take no `parse_mode`, follow `edit_message` (not `edit_message_markdown_v2`), and that is the explicit D-03 contract from the prior phase. Likewise the cursor `█` append at :151 (per RESEARCH §Line-Number Drift Report `SEMANTIC DRIFT — cursor is OMITTED BY CONSTRUCTION on final flush`) stays exactly as is — do NOT introduce a strip step in the final branch (it would be a no-op because the cursor is never added there in the first place; gratuitous code with no observable effect is the wrong fix per RESEARCH).

(e) If any existing test in `crates/ironhermes-gateway/src/stream_consumer.rs:343-575` constructs a body that contains MarkdownV2 reserved chars and asserts the body reaches the MockAdapter unescaped, that test now needs updating to assert against the ESCAPED body. Use `rg -n 'EditMessageMarkdownV2|SendMessageMarkdownV2' crates/ironhermes-gateway/src/stream_consumer.rs` to find the asserting call sites, then for each MarkdownV2-routing test, replace the literal expected-string with `&escape_outside_code_blocks("<original expected literal>")` OR compute the expected escaped form inline. The existing test `test_final_edit_uses_edit_message_markdown_v2` (renamed in plan 04 task 2 from `test_final_edit_uses_edit_message_markdown`) at the line-545 area is the canary — if it had an assertion on a specific body string, update it. Tests that use plain-ASCII bodies with NO reserved chars need no change (escape is a no-op on input without `[`, `]`, etc.).</action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p ironhermes-gateway --lib stream_consumer 2>&amp;1 | tail -15 &amp;&amp; grep -c 'escape_outside_code_blocks' crates/ironhermes-gateway/src/stream_consumer.rs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'use crate::markdown_v2::escape_outside_code_blocks' crates/ironhermes-gateway/src/stream_consumer.rs` returns `1`.
    - Source: `grep -E 'escape_outside_code_blocks\(' crates/ironhermes-gateway/src/stream_consumer.rs | wc -l` returns at least `3` (one for the single-chunk final edit, two for the overflow-chunk sites — note these match-counts EXCLUDE comments via the `\b` regex hygiene rule).
    - Source: `grep -nE '\.send_message_markdown_v2\(' crates/ironhermes-gateway/src/stream_consumer.rs | wc -l` returns at least `2` (the two overflow-chunk sites).
    - Source: intermediate-edit branch preserved — `grep -nE '\.edit_message\(' crates/ironhermes-gateway/src/stream_consumer.rs | grep -v 'edit_message_markdown' | wc -l` returns at least `2` (the non-final `edit_message` calls at :114, :161 still present per D-03).
    - Source: cursor strip path untouched — `grep -c 'CURSOR' crates/ironhermes-gateway/src/stream_consumer.rs` returns the same count as on develop (verify by `git diff crates/ironhermes-gateway/src/stream_consumer.rs | grep -E '^[+-].*CURSOR' | wc -l` returning `0` after this task's commit).
    - Behavior: `cargo test -p ironhermes-gateway --lib stream_consumer` exits 0 with the renamed `test_final_edit_uses_edit_message_markdown_v2` passing.
    - Behavior: `cargo test -p ironhermes-gateway 2>&amp;1 | grep -E 'test result.*FAIL' | wc -l` returns `0` (full gateway suite green, including 36.17.2 D-21 UQM tests which exercise the now-updated RecordingFailingAdapter through the worker pop-loop).
  </acceptance_criteria>
  <done>D-01 escape applied at every final-edit call site (single chunk + 2 overflow chunks). D-03 intermediate-edit branch and cursor path preserved. All stream_consumer tests green. The MarkdownV2 path is end-to-end correct in production code (plan 06 wires the handler.rs piece + integration test).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-emitted final-edit body → `escape_outside_code_blocks` → Telegram MarkdownV2 parser | Model controls the body; the escape function is the parse-correctness gate. |
| LLM-emitted overflow chunks (final-edit only) → escape → Telegram | Same trust boundary applied to messages past `MAX_MESSAGE_LEN`. |
| Intermediate streaming deltas → `edit_message` (no parse_mode) | D-03 invariant — model output reaches user verbatim during streaming, no parse-mode risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-MD-INJECTION-CALLSITE | Tampering | stream_consumer.rs final-edit call sites | mitigate | Plan 01's golden table establishes correctness of `escape_outside_code_blocks`. This plan applies it at every call site that opts into MarkdownV2 (single + 2 overflow). D-02 fallback in TelegramAdapter (plan 04 + task 1 of this plan) absorbs any escape bug. |
| T-D03-REGRESSION | Tampering | stream_consumer.rs intermediate-edit branch | mitigate | Task 2 explicitly excludes `edit_message` calls at :114, :161 from the rewrite. Acceptance criterion `\.edit_message\(... | wc -l \geq 2` enforces. RESEARCH §Line-Number Drift Report `cursor is OMITTED BY CONSTRUCTION on final flush` confirms no strip step is needed. |
| T-OVERFLOW-INCONSISTENT | Tampering | overflow-chunk send sites at :123, :133 | mitigate | Task 1 introduces `send_message_markdown_v2`; task 2 wires both overflow sites to it. CONTEXT.md D-Discretion recommends this approach so "the entire response renders consistently". A user reading a long message would otherwise see one MarkdownV2-rendered chunk followed by plain-text chunks — visually broken. |
| T-RIPPLE-MISS-IMPL | Tampering | Test fixture PlatformAdapter impls missing the new method | mitigate | Compile-time check — Rust will reject any missing impl. Task 1 enumerates with `rg -n 'impl PlatformAdapter for' crates/ironhermes-gateway/tests/` so the executor knows the full surface. |
</threat_model>

<verification>
- `cargo build -p ironhermes-gateway --tests` exits 0 after task 1 (trait + every impl gains the new method).
- `cargo test -p ironhermes-gateway` exits 0 with no FAIL after task 2.
- `grep -c 'escape_outside_code_blocks' crates/ironhermes-gateway/src/stream_consumer.rs` returns ≥ 3 (one single-chunk + two overflow sites).
- `git diff crates/ironhermes-gateway/src/stream_consumer.rs | grep -E '^[+-].*\.edit_message\(' | wc -l` returns 0 (no intermediate-edit call mutated — D-03 invariant preserved).
- `git diff crates/ironhermes-gateway/src/stream_consumer.rs | grep -E '^[+-].*CURSOR' | wc -l` returns 0 (cursor path untouched).
- Three new `parse_mode: MarkdownV2` sites exist in telegram.rs (`grep -c '"parse_mode": "MarkdownV2"' crates/ironhermes-gateway/src/telegram.rs` returns ≥ 2 — one for `edit_message_markdown_v2` from plan 04 + one for the new `send_message_markdown_v2`).
</verification>

<success_criteria>
- All must_haves observable via the grep/build/test commands above.
- 2 commits land: `feat(36.17.2.2-05): add send_message_markdown_v2 trait method + D-02 fallback in TelegramAdapter` and `feat(36.17.2.2-05): apply escape_outside_code_blocks at stream_consumer final-edit sites + overflow chunks route through MarkdownV2`.
- Plan 06 inherits a fully-functional final-edit body path; its integration test can drive a synthetic LLM stream that emits literal markdown syntax + reserved chars, then assert the body reaches MockAdapter / RecordingMediaAdapter ESCAPED + `parse_mode: MarkdownV2`.
</success_criteria>

<output>
Create `.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-05-SUMMARY.md` when done — record the exact list of test files where `RecordingFailingAdapter` (or sibling impls) required the new method's manual implementation, and a one-line note on whether any existing stream_consumer test had to be updated for the new escaped-body assertion shape.
</output>
