---
phase: 36.17.2.2
plan: 04
type: execute
wave: 0
depends_on:
  - 36.17.2.2-01
  - 36.17.2.2-02
  - 36.17.2.2-03
files_modified:
  - crates/ironhermes-gateway/src/adapter.rs
  - crates/ironhermes-gateway/src/telegram.rs
  - crates/ironhermes-gateway/src/stream_consumer.rs
  - crates/ironhermes-gateway/tests/uqm_session_queue_unification.rs
  - crates/ironhermes-gateway/tests/session_queue_integration.rs
  - crates/ironhermes-gateway/tests/running_agent_guard_tests.rs
autonomous: true
requirements:
  - D-01
  - D-02
  - D-12
  - D-13
  - D-14
  - D-15
  - D-18
user_setup: []

must_haves:
  truths:
    - "`PlatformAdapter` trait has `edit_message_markdown_v2` (not `edit_message_markdown`); legacy method REMOVED entirely (RESEARCH FLAGGED RISK option `a` full replacement)."
    - "Every adapter impl in the codebase compiles against the new trait signature — `TelegramAdapter` (telegram.rs), `MockAdapter` (stream_consumer.rs:311), `RecordingFailingAdapter` (uqm_session_queue_unification.rs and any sibling test files)."
    - "`TelegramAdapter::edit_message_markdown_v2` sends `parse_mode: MarkdownV2` AND implements D-02 single-retry-as-plain-text fallback with `warn!` log on parse-mode 400 errors and `error!` log on the second failure."
    - "`crates/ironhermes-gateway/src/telegram.rs::send_file_multipart` returns `Result<MessageResponse>` (was `Result<()>`), parses `TelegramResponse<TgMessage>`, projects to `MessageResponse` per RESEARCH Pitfall 4."
    - "`TelegramAdapter::send_audio` exists as a one-line wrapper around `send_file_multipart(\"sendAudio\", \"audio\", ...)` per D-13."
    - "`impl MediaSender for TelegramAdapter` exists with all 5 per-type methods (D-13 + D-14 dispatch table), each handling both `MediaSource::Path` (multipart upload) and `MediaSource::Url` (URL-form passthrough per D-12)."
    - "Per-type D-15 size pre-check on the `Path` variant runs before multipart upload (≤20 MB for photo/voice/audio/video, ≤50 MB for document); on oversize, return `Err` with a typed message that the D-19 handler can detect for the reinsert path."
    - "`cargo test -p ironhermes-gateway` is green (no regression to 36.17.2 UQM/SessionQueue tests, no regression to stream_consumer's MockAdapter-driven flush tests at lines 343-575)."
  artifacts:
    - path: "crates/ironhermes-gateway/src/adapter.rs"
      provides: "Updated `PlatformAdapter` with `edit_message_markdown_v2` replacing `edit_message_markdown`."
      contains: "edit_message_markdown_v2"
    - path: "crates/ironhermes-gateway/src/telegram.rs"
      provides: "`send_file_multipart` -> Result<MessageResponse>, new `send_audio` helper, `edit_message_markdown_v2` with D-02 fallback, `impl MediaSender for TelegramAdapter`."
      contains: "impl MediaSender for TelegramAdapter"
  key_links:
    - from: "crates/ironhermes-gateway/src/telegram.rs::send_file_multipart"
      to: "ironhermes_core::MessageResponse"
      via: "TgMessage projection (mirror existing send_message at telegram.rs:218-236)"
      pattern: "Ok\\(MessageResponse"
    - from: "crates/ironhermes-gateway/src/telegram.rs::edit_message_markdown_v2"
      to: "warn-and-retry plain-text path"
      via: "anyhow Error description substring match on parse-mode failure"
      pattern: "can't parse|parse entities|parse_mode"
---

<objective>
Land the structural reconciliation that everything else in the phase rides on. Five mechanically related changes share `telegram.rs` and `adapter.rs` so they MUST run in one plan (file-ownership rule):

1. Refactor `send_file_multipart` from `Result<()>` to `Result<MessageResponse>` (RESEARCH Pitfall 4 / Wave 0 Gap).
2. Add `send_audio` helper on `TelegramAdapter` per D-13 (mechanical mirror of `send_voice`).
3. Replace `PlatformAdapter::edit_message_markdown` (legacy `parse_mode: Markdown`) with `edit_message_markdown_v2` (new `parse_mode: MarkdownV2`) — RESEARCH FLAGGED RISK option `a` full replacement; ripple to `TelegramAdapter`, `MockAdapter`, `RecordingFailingAdapter` and any sibling test adapter impls.
4. Implement D-02 single-retry-as-plain-text fallback inside the new `TelegramAdapter::edit_message_markdown_v2`.
5. Add `impl MediaSender for TelegramAdapter` per D-13 + D-14 dispatch + D-12 URL-form passthrough + D-15 path size pre-check.

Purpose: These changes share `telegram.rs` lines plus the `PlatformAdapter` trait surface — splitting them creates merge conflicts and forces a sequential wave where Wave 0 should run parallel. Doing them together keeps the trait-and-impl change atomic.
Output: `PlatformAdapter` trait switched to V2; `TelegramAdapter` implements both the refactored legacy contract and the new `MediaSender`; `MockAdapter` + `RecordingFailingAdapter` rippled; gateway crate suite still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-CONTEXT.md
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md
@.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md
@crates/ironhermes-gateway/src/adapter.rs
@crates/ironhermes-gateway/src/telegram.rs
@crates/ironhermes-gateway/src/stream_consumer.rs
@crates/ironhermes-gateway/src/media_tag.rs
@crates/ironhermes-gateway/src/markdown_v2.rs
@crates/ironhermes-gateway/tests/uqm_session_queue_unification.rs
@crates/ironhermes-gateway/tests/session_queue_integration.rs
@crates/ironhermes-gateway/tests/running_agent_guard_tests.rs
</context>

<interfaces>
Post-plan-04 surface plans 05/06 consume:

- `PlatformAdapter::edit_message_markdown_v2(chat_id, message_id, content) -> Result<()>` replaces the legacy method. Callers pre-escape `content` via `crate::markdown_v2::escape_outside_code_blocks`.
- `TelegramAdapter::send_audio(chat_id, path, thread_id) -> Result<MessageResponse>` wraps `send_file_multipart("sendAudio", "audio", ...)`.
- `impl MediaSender for TelegramAdapter` — 5 per-type async methods. `send_media` inherits the trait default body.
- `send_file_multipart` returns `Result<MessageResponse>` (was `Result<()>`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor send_file_multipart -> Result&lt;MessageResponse&gt; and add send_audio (D-13)</name>
  <read_first>
    - crates/ironhermes-gateway/src/telegram.rs (anchors: `send_voice` :25, `send_image_file` :34, `send_video` :43, `send_document` :52, `send_file_multipart` :157, `impl PlatformAdapter for TelegramAdapter` :213, `send_message` :218 returning `MessageResponse` — that projection is the template `send_file_multipart` learns)
    - crates/ironhermes-gateway/src/adapter.rs (current trait; `MessageResponse` in scope via `use ironhermes_core::{MessageEvent, MessageResponse, Platform};` at line 3)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md (§6 refactor return type; §4 `send_audio` mechanical mirror)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md (Pitfall 4; FLAGGED RISK D-05 method-name drift — `send_image_file` exists at telegram.rs:34, not `send_photo`)
  </read_first>
  <action>Edit `crates/ironhermes-gateway/src/telegram.rs`:

(a) Change `send_file_multipart` signature at line 157 from `Result<()>` to `Result<MessageResponse>`. Inside the body, change the deserialize generic from `TelegramResponse<serde_json::Value>` to `TelegramResponse<TgMessage>` (`TgMessage` is the existing struct at telegram.rs:319 area — verify with `rg -n 'struct TgMessage' crates/ironhermes-gateway/src/telegram.rs`; it deserializes `message_id: i64`). Replace the final `Ok(())` with the projection used by `send_message` at telegram.rs:231-235: `let result = body.result.context("Telegram returned no result")?; Ok(MessageResponse { message_id: result.message_id.to_string(), chat_id: chat_id.to_string(), platform: Platform::Telegram })`. If `TelegramResponse.result` is `Option<T>` use `.context`; if it is `T` directly, drop `?`. Inspect `TelegramResponse<T>` definition (telegram.rs:304-310 area per PATTERNS §6) to pick the right shape.

(b) Update the 4 internal callers at telegram.rs:31 (`send_voice`), :40 (`send_image_file`), :49 (`send_video`), :58 (`send_document`). Two options per Pitfall 4 / PATTERNS §6 adaptation note: (i) change THEIR return types to `Result<MessageResponse>` — best, keeps the projected `message_id` available; or (ii) suffix `.map(|_| ())` to each call to preserve `Result<()>` on the `TgSendApi` trait method. The `TgSendApi` trait lives in `ironhermes-cron` — `rg -n 'fn send_voice|fn send_image_file|fn send_video|fn send_document' crates/ironhermes-cron/src/` to check trait signatures. Use option (ii) `.map(|_| ())` if the trait returns `Result<()>` (avoids cross-crate API change per RESEARCH Assumption A8 "All 4 callers are internal").

(c) Add `send_audio` per D-13. The method lives inside the existing `impl TgSendApi for TelegramAdapter` block IF `TgSendApi` already declares `send_audio` (`rg -n 'fn send_audio' crates/ironhermes-cron/src/`); otherwise add it as an inherent method on `TelegramAdapter` in the `impl TelegramAdapter` block at telegram.rs:74-210 per PATTERNS §4 adaptation note. Signature: `async fn send_audio(&self, chat_id: &str, path: &Path, thread_id: Option<&str>) -> anyhow::Result<MessageResponse> { self.send_file_multipart("sendAudio", "audio", chat_id, path, thread_id).await }`. Bot API method string `"sendAudio"`, multipart field `"audio"` per D-13 + RESEARCH §Telegram Bot API Confirmations Assumption A2 (pattern symmetry with the existing four helpers).

Per RESEARCH FLAGGED RISK D-05: do NOT rename `send_image_file` to `send_photo` in this plan — that ripples to `ironhermes-cron` callers (`rg -n 'send_image_file' crates/ironhermes-cron/src/`). `MediaSender::send_photo` in task 4 is a sibling, not a rename. Document the name divergence in a doc-comment on the new `MediaSender::send_photo` impl.</action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-gateway 2>&amp;1 | tail -10 &amp;&amp; cargo build -p ironhermes-cron 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -A 6 'async fn send_file_multipart' crates/ironhermes-gateway/src/telegram.rs | grep -c 'Result&lt;MessageResponse&gt;'` returns at least `1`.
    - Source: `grep -c 'TelegramResponse&lt;TgMessage&gt;' crates/ironhermes-gateway/src/telegram.rs` returns at least `1` (was `serde_json::Value` before).
    - Source: `grep -c '"sendAudio"' crates/ironhermes-gateway/src/telegram.rs` returns at least `1`.
    - Source: `grep -E 'fn send_audio' crates/ironhermes-gateway/src/telegram.rs | wc -l` returns at least `1`.
    - Source: existing helpers still exist (`grep -E 'fn (send_voice|send_image_file|send_video|send_document)' crates/ironhermes-gateway/src/telegram.rs | wc -l` returns `4` — no renames in this task).
    - Behavior: `cargo build -p ironhermes-gateway` exits 0.
    - Behavior: `cargo build -p ironhermes-cron` exits 0 (`TgSendApi` contract preserved via option ii `.map(|_| ())` if applicable).
  </acceptance_criteria>
  <done>`send_file_multipart` returns `Result<MessageResponse>`. `send_audio` exists. Cron crate still builds. Structural piece 1 of 5 done.</done>
</task>

<task type="auto">
  <name>Task 2: Replace edit_message_markdown -> edit_message_markdown_v2 across the trait and all impls, with D-02 fallback in TelegramAdapter</name>
  <read_first>
    - crates/ironhermes-gateway/src/adapter.rs (existing `edit_message_markdown` at lines 38-43)
    - crates/ironhermes-gateway/src/telegram.rs:250-265 (legacy `edit_message_markdown` body with `parse_mode: "Markdown"`)
    - crates/ironhermes-gateway/src/stream_consumer.rs:261-326 (MockAdapter — `edit_message_markdown` impl at :311; calls-log enum at :240-272 area which likely has an `EditMessageMarkdown` variant to rename)
    - crates/ironhermes-gateway/src/stream_consumer.rs:540-580 (the test `test_final_edit_uses_edit_message_markdown` at line 545 needs renaming after the trait change; do NOT change the call site at line 100 — that is plan 05's job)
    - crates/ironhermes-gateway/tests/uqm_session_queue_unification.rs (search `edit_message_markdown`)
    - crates/ironhermes-gateway/tests/session_queue_integration.rs (search `edit_message_markdown`)
    - crates/ironhermes-gateway/tests/running_agent_guard_tests.rs (search `edit_message_markdown`)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md (§7 — sketch of the new impl plus the body-already-escaped contract)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md (FLAGGED RISK `PlatformAdapter::edit_message_markdown is REQUIRED` — option `a` full replacement; D-02 fallback per Telegram Bot API Confirmations row 8)
  </read_first>
  <action>Make these mechanical changes in this order to avoid intermediate uncompilable states:

(a) Rename the trait method at `crates/ironhermes-gateway/src/adapter.rs:38` from `edit_message_markdown` to `edit_message_markdown_v2`. Add a doc-comment that callers must pre-escape `content` via `crate::markdown_v2::escape_outside_code_blocks` (the trait surface does NOT auto-escape — the call site in `stream_consumer.rs` does the escape per plan 05).

(b) Rename the impl at `crates/ironhermes-gateway/src/telegram.rs:250` from `edit_message_markdown` to `edit_message_markdown_v2`. Change `params.parse_mode` from `"Markdown"` to `"MarkdownV2"`. Wrap the existing `api_call("editMessageText", &params).await?` in a `match` block: on `Ok`, return `Ok(())`. On `Err(e)`, substring-match `format!("{e}")` against `"can't parse"`, `"parse entities"`, `"parse_mode"` — if any match, log `tracing::warn!(chat_id, message_id, error = %desc, "MarkdownV2 parse failed; retrying as plain text (D-02 fallback)")` and re-issue the call with a `plain_params` json that OMITS the `parse_mode` field; on retry-Ok return `Ok(())`, on retry-Err log `tracing::error!` and return `Err(retry_err)`. If the original error does not substring-match parse-mode keywords, return it unchanged. Confirm `use tracing::{warn, error};` or `use tracing;` is already imported in telegram.rs (`rg -n 'use tracing' crates/ironhermes-gateway/src/telegram.rs`); if not, add the import. D-02 substring set comes from RESEARCH §Telegram Bot API Confirmations row 8 (exact phrasing varies across Bot API versions — substring is robust).

(c) Rename the MockAdapter impl at `crates/ironhermes-gateway/src/stream_consumer.rs:311` from `edit_message_markdown` to `edit_message_markdown_v2`. Body unchanged (records the call into the calls log — the test fixture does not care about parse_mode). If the calls-log enum has a variant `EditMessageMarkdown { ... }` (search the file at `:240-272` area), rename it to `EditMessageMarkdownV2 { ... }` and update all matches throughout `stream_consumer.rs`. Rename the existing test at `:545` from `test_final_edit_uses_edit_message_markdown` to `test_final_edit_uses_edit_message_markdown_v2` so the test name aligns with the method it verifies. DO NOT change `:100` — that call site update is plan 05's job (the build will still pass after this task because the call site change `.edit_message_markdown(...)` → `.edit_message_markdown_v2(...)` is the ONE remaining call-site update plan 05 owns; complete it as task 3 inside this plan instead to keep the build green between Wave 0 plans — see task 3).

(d) Rename the RecordingFailingAdapter impl at `crates/ironhermes-gateway/tests/uqm_session_queue_unification.rs` (`rg -n 'edit_message_markdown' crates/ironhermes-gateway/tests/uqm_session_queue_unification.rs`) — mechanical rename, body unchanged (it pushes to `edit_log`).

(e) Repeat (d) for `crates/ironhermes-gateway/tests/session_queue_integration.rs` and `crates/ironhermes-gateway/tests/running_agent_guard_tests.rs` if either declares a `PlatformAdapter` impl. `rg -n 'edit_message_markdown' crates/ironhermes-gateway/tests/` to enumerate every occurrence; every impl block needs the rename for the crate to build under the new trait.</action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; rg -n 'fn edit_message_markdown\b' crates/ironhermes-gateway/ 2>&amp;1 | grep -v 'edit_message_markdown_v2' | wc -l &amp;&amp; rg -n 'fn edit_message_markdown_v2' crates/ironhermes-gateway/ 2>&amp;1 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Source: `rg -nE 'fn edit_message_markdown\b' crates/ironhermes-gateway/ | grep -v '_v2' | wc -l` returns `0` (zero remaining legacy `edit_message_markdown` method definitions across src/ and tests/).
    - Source: `rg -n 'fn edit_message_markdown_v2' crates/ironhermes-gateway/ | wc -l` returns at least `4` (PlatformAdapter trait + TelegramAdapter impl + MockAdapter impl + RecordingFailingAdapter impl; additional rippled test adapters allowed).
    - Source: `grep -c '"MarkdownV2"' crates/ironhermes-gateway/src/telegram.rs` returns at least `1` AND `grep -c '"Markdown"' crates/ironhermes-gateway/src/telegram.rs | head -1` returns `0` for the legacy literal (`grep -c '"Markdown"$\|"Markdown",' crates/ironhermes-gateway/src/telegram.rs` should be `0` — legacy parse_mode value gone).
    - Source: D-02 fallback present (`grep -E "can't parse|parse entities|D-02" crates/ironhermes-gateway/src/telegram.rs | wc -l` returns at least `2` — the substring match strings AND the comment/log line referencing D-02).
    - Source: `grep -c 'tracing::warn!' crates/ironhermes-gateway/src/telegram.rs` returns at least `1` (the D-02 warn-and-retry log).
    - Behavior: `cargo build -p ironhermes-gateway --tests` exits 0 (task 3 below adds the stream_consumer.rs:100 call-site rename to keep this green).
  </acceptance_criteria>
  <done>Trait + 4 impls renamed. `parse_mode: MarkdownV2`. D-02 fallback implemented in TelegramAdapter only (test fixtures don't need it — they record calls, they don't talk to Telegram).</done>
</task>

<task type="auto">
  <name>Task 3: Update stream_consumer.rs:100 call site to edit_message_markdown_v2 (keeps Wave 0 build green; full body-escape switchover is plan 05)</name>
  <read_first>
    - crates/ironhermes-gateway/src/stream_consumer.rs (anchors: line 100 `edit_message_markdown` call inside the `final_edit` branch at lines 88-105; the surrounding context of `MAX_MESSAGE_LEN`-bounded chunking)
    - crates/ironhermes-gateway/src/adapter.rs (post-task-2 trait surface — method is now `edit_message_markdown_v2`)
  </read_first>
  <action>Mechanical rename of the call at `crates/ironhermes-gateway/src/stream_consumer.rs:100`: change `.edit_message_markdown(&self.chat_id, &self.current_message_id, &remaining)` to `.edit_message_markdown_v2(&self.chat_id, &self.current_message_id, &remaining)`. **Do NOT** apply `escape_outside_code_blocks` to `&remaining` here — that escape-application is plan 05's job (plan 05 owns both the escape application AND the D-Discretion decision about whether overflow chunks at lines 121-128 / 131-134 also become MarkdownV2). This task ONLY renames the method call so the crate compiles end-to-end after task 2. The `&remaining` body in this intermediate state will be sent through MarkdownV2 parse mode with NO escaping, which means Telegram will likely return 400 on any message containing reserved chars — but the D-02 fallback added in task 2 will silently retry as plain text, so user-visible degradation is bounded to "loses markdown rendering until plan 05 ships". This is acceptable since Wave 0 plans land together before any execution. Plan 05's task 1 properly applies the escape and that becomes the persistent steady state.</action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-gateway --tests 2>&amp;1 | tail -10 &amp;&amp; grep -n 'edit_message_markdown_v2' crates/ironhermes-gateway/src/stream_consumer.rs | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n '\.edit_message_markdown_v2(' crates/ironhermes-gateway/src/stream_consumer.rs | wc -l` returns at least `1` (the renamed call site at the post-rename line near 100).
    - Source: `grep -nE '\.edit_message_markdown\(' crates/ironhermes-gateway/src/stream_consumer.rs | wc -l` returns `0` (no legacy method calls remaining in this file).
    - Behavior: `cargo build -p ironhermes-gateway --tests` exits 0.
    - Behavior: `cargo test -p ironhermes-gateway --lib stream_consumer 2>&amp;1 | tail -10` runs without compile errors (some tests may FAIL because the MockAdapter calls-log enum was renamed and assertions reference the old variant name — those test-name failures are addressed by task 2's variant rename; if any unrelated assertion fails, it indicates the rename was incomplete).
  </acceptance_criteria>
  <done>`stream_consumer.rs:100` calls `edit_message_markdown_v2`. Full crate builds. Plan 05 owns the escape application.</done>
</task>

<task type="auto">
  <name>Task 4: Implement MediaSender for TelegramAdapter — D-13/D-14 dispatch + D-12 URL form + D-15 size pre-check</name>
  <read_first>
    - crates/ironhermes-gateway/src/telegram.rs (post-task-1 state — `send_file_multipart` returns `Result<MessageResponse>`; existing impl blocks at :11-60 `impl TgSendApi for TelegramAdapter`, :213-298 `impl PlatformAdapter for TelegramAdapter`; `api_call` helper for URL-form path)
    - crates/ironhermes-gateway/src/adapter.rs (post-plan-03 state — the `MediaSender` trait is declared; `MediaRef`, `MediaSource`, `MediaKind` re-exported from `crate::media_tag`)
    - crates/ironhermes-gateway/src/media_tag.rs (types — note `MediaSource::Path(PathBuf)` vs `MediaSource::Url(String)`)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-PATTERNS.md (§5 — sketch of the impl block with both `Path` and `Url` arms per method; §6 — `send_message`-style `TgMessage` → `MessageResponse` projection used for URL form)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-CONTEXT.md (D-12 URL passthrough, D-13 ship all 5 types, D-14 ogg/opus → voice and mp3/m4a/flac/wav → audio, D-15 size caps 20 MB / 50 MB; D-17 no captions)
    - .planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-RESEARCH.md (Pitfall 4 — projection contract; Security Domain T-INPUT-MEDIA-PATH; §`#[async_trait]` Verification)
  </read_first>
  <action>Append `#[async_trait] impl MediaSender for TelegramAdapter { ... }` to `crates/ironhermes-gateway/src/telegram.rs` (after the existing `impl PlatformAdapter for TelegramAdapter` block at lines 213-298). Implement all 5 per-type methods. Do NOT implement `send_media` — it inherits the trait's default body that dispatches on `MediaKind` (D-18). For each per-type method, the body matches on `&MediaRef.source` (caller passes `&MediaSource` per the trait signature):

(a) `MediaSource::Path(p)`: run the D-15 size pre-check first. Per-method size caps: `send_photo`/`send_voice`/`send_audio`/`send_video` = 20 MiB = `20 * 1024 * 1024`; `send_document` = 50 MiB = `50 * 1024 * 1024`. Use `tokio::fs::metadata(p).await?.len()` and compare to the cap. If `len > cap`, return `Err(anyhow::anyhow!("media exceeds size cap: {} bytes > {} bytes cap (path {})", len, cap, p.display()))` — the typed error message lets the D-19 dispatch loop in plan 06 detect the oversize signal via substring match if needed. Also enforce the D-19/T-INPUT-MEDIA-PATH path-canonicalization security gate inline: call `p.canonicalize()?` and reject the path if the canonicalized form is NOT under one of the allowed roots — for now `std::env::var("HERMES_HOME").ok()` (if set) plus `/tmp` (literal). On rejection return `Err(anyhow::anyhow!("media path outside allowed roots: {}", p.display()))` and log `tracing::warn!(filename = %p.file_name().and_then(|n| n.to_str()).unwrap_or("?"), "rejected media path outside allowed roots (T-INPUT-MEDIA-PATH)")` — log the FILENAME ONLY per the phase threat model (do not log the full path to avoid T-LOG-LEAK). If the size pre-check AND the canonicalization both pass, call `self.send_file_multipart("sendXxx", "xxx", chat_id, p, thread_id).await` (sendPhoto/photo, sendVoice/voice, sendAudio/audio, sendVideo/video, sendDocument/document per D-13/D-14).

(b) `MediaSource::Url(u)`: per D-12 NO local fetch, pass the URL straight to Telegram. Also enforce the T-INPUT-MEDIA-URL scheme gate: reject if `!u.starts_with("http://") && !u.starts_with("https://")` — return `Err(anyhow::anyhow!("media URL scheme not allowed: {}", u))`. On scheme-OK build `let params = serde_json::json!({ "chat_id": chat_id, "<field>": u });` where `<field>` is `"photo"`/`"voice"`/`"audio"`/`"video"`/`"document"` per method, optionally adding `"message_thread_id": tid` if `thread_id.is_some()`. Call `let result: TgMessage = self.api_call("sendXxx", &params).await?;` and project to `MessageResponse { message_id: result.message_id.to_string(), chat_id: chat_id.to_string(), platform: Platform::Telegram }` per PATTERNS §5. No captions (D-17).

The 5 methods are mechanically symmetric — author one (recommend `send_photo`), then copy-edit method/field strings for the others. Add a doc-comment on the impl block citing D-17 (no captions), D-12 (URL form), D-13 (5 types), D-14 (voice vs audio dispatch), and noting that `send_photo` is INTENTIONALLY a sibling — NOT a rename — of the existing `TgSendApi::send_image_file` method on this same struct (RESEARCH FLAGGED RISK D-05 — `send_image_file` has `ironhermes-cron` callers and renaming it is out of scope).</action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-gateway 2>&amp;1 | tail -10 &amp;&amp; grep -c '^impl MediaSender for TelegramAdapter' crates/ironhermes-gateway/src/telegram.rs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'impl MediaSender for TelegramAdapter' crates/ironhermes-gateway/src/telegram.rs` returns `1`.
    - Source: `grep -E 'async fn send_(photo|voice|audio|video|document)' crates/ironhermes-gateway/src/telegram.rs | wc -l` returns at least `9` (5 in the new `impl MediaSender` block + 4 in the existing `impl TgSendApi` block; `send_audio` from task 1 makes the TgSendApi side count 5 if it landed there, total 10; either ≥9 acceptable signaling all required methods exist).
    - Source: D-15 size caps present (`grep -E '20 \* 1024 \* 1024|50 \* 1024 \* 1024' crates/ironhermes-gateway/src/telegram.rs | wc -l` returns at least `2`).
    - Source: D-12 URL form present (`grep -E 'starts_with\("https?://"\)' crates/ironhermes-gateway/src/telegram.rs | wc -l` returns at least `1`).
    - Source: path canonicalization gate present (`grep -c '\.canonicalize()' crates/ironhermes-gateway/src/telegram.rs` returns at least `1`).
    - Source: filename-only logging per T-LOG-LEAK (`grep -E 'file_name\(\)' crates/ironhermes-gateway/src/telegram.rs | wc -l` returns at least `1`).
    - Behavior: `cargo build -p ironhermes-gateway` exits 0.
    - Behavior: `cargo test -p ironhermes-gateway 2>&amp;1 | grep -E 'test result.*FAIL' | wc -l` returns `0` (no regression to existing tests including 36.17.2 D-21 UQM tests and stream_consumer's MockAdapter-driven flush tests).
  </acceptance_criteria>
  <done>`impl MediaSender for TelegramAdapter` exists with all 5 per-type methods. D-12 URL passthrough, D-13 type count, D-14 audio-format dispatch, D-15 size pre-check, T-INPUT-MEDIA-PATH canonicalization, T-INPUT-MEDIA-URL scheme gate, T-LOG-LEAK filename-only logging all live in source. Full gateway crate builds and full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-emitted `MediaRef.source` → multipart upload OR URL passthrough | Model-controlled paths and URLs cross into Telegram's transport. Path traversal and SSRF are the live threats. |
| LLM-emitted final-edit body → Telegram MarkdownV2 parser | Parse failures must not surface as silent message drops. |
| Test fixtures (MockAdapter, RecordingFailingAdapter) → no production impact | These impls do not talk to Telegram; they record calls only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-INPUT-MEDIA-PATH (HIGH, phase-level) | Information Disclosure | `MediaSender::send_*` `MediaSource::Path` arm | mitigate | Task 4 enforces `Path::canonicalize` + allowed-roots check (`HERMES_HOME`, `/tmp`) before opening the file. Symlinks resolving outside allowed roots are rejected. D-15 size pre-check additionally bounds per-request bandwidth. Failure path returns typed `Err` for the D-19 reinsert flow (plan 06). |
| T-INPUT-MEDIA-URL (MEDIUM, phase-level) | Information Disclosure | `MediaSender::send_*` `MediaSource::Url` arm | mitigate | Task 4 rejects schemes other than `http://` and `https://` (no `file://`, `gopher://`, `ftp://`). D-12 delegates the fetch to Telegram so no client-side SSRF surface exists on our side. |
| T-MD-INJECTION (LOW, phase-level) | Tampering | `TelegramAdapter::edit_message_markdown_v2` | mitigate | D-02 single-retry-as-plain-text fallback absorbs malformed MarkdownV2 from the model. Plan 01's golden test table prevents most pathological escape outputs at the source. |
| T-LOG-LEAK (LOW, phase-level) | Information Disclosure | task-4 `warn!` on path-reject and D-15 oversize log | mitigate | Path log is `file_name()`-only (no parent dirs), reducing PII leakage if a user file path contains sensitive identifiers. |
| T-36.17.2.2-RIPPLE-BREAK | Tampering | MockAdapter / RecordingFailingAdapter rename | mitigate | Task 2 (d)/(e) enumerate every test file with `rg -n 'edit_message_markdown' crates/ironhermes-gateway/tests/` and rename all impl blocks. Compile-time guarantee that no impl is missed. |
| T-36.17.2.2-D02-INFINITE-RETRY | Denial of Service | D-02 retry-on-parse-error path | mitigate | The D-02 retry happens AT MOST ONCE. Second failure logs `error!` and returns the second `Err` — no recursion, no loop. |
</threat_model>

<verification>
- `cargo build -p ironhermes-gateway` exits 0 after each task.
- `cargo build -p ironhermes-cron` exits 0 (TgSendApi contract preserved).
- `cargo test -p ironhermes-gateway` exits 0 with no FAIL lines.
- `rg -n 'fn edit_message_markdown\b' crates/ironhermes-gateway/ | grep -v '_v2' | wc -l` returns 0 (no legacy method defs remain).
- `rg -n '\.edit_message_markdown\(' crates/ironhermes-gateway/ | wc -l` returns 0 (no legacy method CALLS remain — task 3 caught the only production caller; task 2 fixed the test fixtures).
- `grep -c 'impl MediaSender for TelegramAdapter' crates/ironhermes-gateway/src/telegram.rs` returns 1.
- `grep -c '\.canonicalize()' crates/ironhermes-gateway/src/telegram.rs` returns at least 1 (path-traversal mitigation).
</verification>

<success_criteria>
- All must_haves observable via the grep/build/test commands above.
- 4 commits land: `refactor(36.17.2.2-04): send_file_multipart returns Result<MessageResponse> + add send_audio`, `refactor(36.17.2.2-04): replace edit_message_markdown with edit_message_markdown_v2 + D-02 fallback`, `chore(36.17.2.2-04): rename stream_consumer call site to edit_message_markdown_v2`, `feat(36.17.2.2-04): impl MediaSender for TelegramAdapter`.
- Plan 05 can now call `.edit_message_markdown_v2(&self.chat_id, &self.current_message_id, &escape_outside_code_blocks(&remaining))` at stream_consumer.rs:100 (post-rename line will be ~100).
- Plan 06 can now construct `Arc<TelegramAdapter>` and clone-cast to `Arc<dyn MediaSender>` (RESEARCH Open Q4 / Assumption A7) and pass it through `GatewayMessageHandler::set_media_sender`.
</success_criteria>

<output>
Create `.planning/phases/36.17.2.2-ironhermes-telegram-client-delivers-streaming-final-media-me/36.17.2.2-04-SUMMARY.md` when done — record the exact set of files rippled by the rename (so plan 05 / 06 / 07 reviewers can confirm zero legacy `edit_message_markdown` references survive) and a one-line note on whether the cron-crate side (`TgSendApi`) needed any change.
</output>
