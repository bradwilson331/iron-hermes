---
context: phase
phase: 26.4.1-config-fix
task: null
total_tasks: 5
status: ready_to_execute
last_updated: 2026-05-07T10:25:33.649Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified — planning completed cleanly._

<current_state>
Phase 26.4.1 planning is **complete**. All 3 plans committed at ba612ec. Ready to execute.

**Position:** Pre-execution — no plans started yet.
**Wave 1 (parallel):** 26.4.1-01 (CFG-01, setup.rs) + 26.4.1-03 (CFG-03, main.rs)
**Wave 2:** 26.4.1-02 (CFG-02, setup.rs) — depends on plan 01
</current_state>

<completed_work>

Completed this session:
- Phase 26.4.1 goal clarified via user bug report (was "TBD" placeholder in ROADMAP)
- ROADMAP.md goal updated with full bug descriptions
- 26.4.1-CONTEXT.md created with locked implementation decisions (D-CFG-01, D-CFG-02, D-CFG-03)
- 3 PLAN.md files created by gsd-planner (opus)
- Plan checker found 1 warning (Plan 03 Task 1 verify referenced test file from Task 2) — fixed directly
- STATE.md updated (planning complete, 3 plans)
- All artifacts committed: ba612ec
</completed_work>

<remaining_work>

All execution — nothing started yet:
- 26.4.1-01: CFG-01 — rewrite `run_minimum_viable_flow` API-key persistence to `.env` + `providers:` map
- 26.4.1-03: CFG-03 — widen `run_preflight` gate in main.rs to include `Commands::Gateway`
- 26.4.1-02: CFG-02 — add `KNOWN_AUX_PROVIDERS` guard to auxiliary wizard stages (after 01)
</remaining_work>

<decisions_made>

- **CFG-01 approach:** Use existing `write_env_var_to_dotenv(hermes_home, env_var_name, &api_key)` + `config_setter::config_set("providers.{provider}.api_key_env", env_var_name)`. Do NOT call `apply_api_key_answer` in minimum-viable flow. Env var name: `{PROVIDER_UPPER}_API_KEY` (e.g. `OPENROUTER_API_KEY`).
- **CFG-02 approach:** Static const `KNOWN_AUX_PROVIDERS = ["openrouter", "anthropic", "openai", "google", "mistral", "groq"]`. Guard in both `run_minimum_viable_flow` and `run_agent_section`. Unknown → warn + skip (don't persist).
- **CFG-03 approach:** One-line change in main.rs. `is_interactive_repl` and `is_chat_or_bare` must NOT widen (gateway keeps `ironhermes=info` log filter). Locked by 3 invariant tests in Plan 03.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/26.4.1-config-fix/26.4.1-CONTEXT.md` — locked implementation decisions, exact file locations
2. `.planning/phases/26.4.1-config-fix/26.4.1-01-PLAN.md` — CFG-01 plan (Wave 1)
3. `.planning/phases/26.4.1-config-fix/26.4.1-03-PLAN.md` — CFG-03 plan (Wave 1, parallel with 01)
4. `.planning/phases/26.4.1-config-fix/26.4.1-02-PLAN.md` — CFG-02 plan (Wave 2)

## Infrastructure State
- Dev server: not running (no `dx serve` needed — this is CLI/config code)
- Build: `cargo build --workspace` baseline clean as of Phase 26.4

## User Bug Report (for context)
```
~ % ironhermes chat
Welcome to IronHermes. Let's get you configured.
Provider [openrouter]: openrouter
Error: interrupted

~ % ironhermes gateway
[provider:openrouter] using deprecated env var OPENROUTER_API_KEY — set providers.openrouter.api_key_env in config.yaml to silence this warning
Error: auxiliary.provider 'local' is not a known provider — define it in providers: first
```

<context>
Planning session completed cleanly. The phase was inserted as a placeholder with TBD goal. During this session, the user described 3 bugs via the AskUserQuestion gate. All bugs are highly localized:
- CFG-01 + CFG-02: both in `crates/ironhermes-cli/src/setup.rs` (wizard)
- CFG-03: one line in `crates/ironhermes-cli/src/main.rs`

No new files need to be created except the Plan 03 test file (`tests/invariants_26_4_1_cfg_03.rs`).
</context>

<next_action>
Start with: `/gsd-execute-phase 26.4.1`
Wave 1 plans (01 + 03) can run in parallel — they touch disjoint files.
</next_action>
