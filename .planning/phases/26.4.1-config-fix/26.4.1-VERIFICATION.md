---
phase: 26.4.1-config-fix
verified: 2026-05-07T00:00:00Z
status: passed
score: 12/12 must-haves verified
overrides_applied: 0
re_verification: false
---

# Phase 26.4.1: Config Fix Verification Report

**Phase Goal:** Fix three wizard/gateway config bugs: (1) first-run wizard writes API key to deprecated model.api_key instead of .env + providers map, (2) wizard accepts invalid auxiliary provider names that crash ProviderResolver::build() on next launch, (3) hermes gateway crashes with unhelpful error when config.yaml is missing instead of auto-launching the setup wizard.
**Verified:** 2026-05-07
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Requirements Coverage Note

The REQUIREMENTS.md descriptions for CFG-01, CFG-02, CFG-03 do not match what this phase implements. REQUIREMENTS.md defines:
- CFG-01: "Interactive setup wizard guides first-run configuration"
- CFG-02: "hermes config set/get/show for managing config.yaml values"
- CFG-03: "hermes config migrate scans skills for unconfigured settings"

The phase plans define distinct bug-fix scopes under the same IDs (API key .env persistence, auxiliary allow-list guard, gateway preflight widening). This is a pre-existing labeling collision in REQUIREMENTS.md — the traceability table maps CFG-01/02/03 to Phase 23 (Pending), and this phase 26.4.1 is a hotfix sub-phase claiming the same IDs for different concerns. All three bug fixes from the phase goal ARE fully implemented. The REQUIREMENTS.md mismatch is a documentation artifact, not a code gap; the phase goal is achieved as stated.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | After `hermes setup`, `$HERMES_HOME/.env` contains `{PROVIDER}_API_KEY=...` | VERIFIED | `write_env_var_to_dotenv(hermes_home, &env_var_name, &api_key)` called at setup.rs:155; test `cfg_01_writes_api_key_to_dotenv_and_providers_map` PASSES |
| 2 | After `hermes setup`, `config.yaml` contains `providers.{provider}.api_key_env: "{PROVIDER}_API_KEY"` | VERIFIED | `config_setter::config_set(hermes_home, &format!("providers.{}.api_key_env", provider), &env_var_name)` at setup.rs:156-160; test `cfg_01_writes_api_key_to_dotenv_and_providers_map` verifies this |
| 3 | After `hermes setup`, `config.yaml` does NOT contain `model.api_key:` (deprecated field) | VERIFIED | `apply_api_key_answer` is NOT called inside `run_minimum_viable_flow` body (awk extraction returns empty); test `cfg_01_does_not_write_legacy_model_api_key` PASSES; test `cfg_01_run_minimum_viable_flow_does_not_call_apply_api_key_answer` PASSES via `include_str!` source-text invariant |
| 4 | `run_model_section` (re-config) is untouched — still calls `apply_api_key_answer` for backward compat | VERIFIED | setup.rs:245 `apply_api_key_answer(config, &api_key)` present in `run_model_section` |
| 5 | `provider_env_var_name` helper exists and correctly derives env-var names (including hyphen handling) | VERIFIED | setup.rs:85-87; test `provider_env_var_name_uppercases_and_replaces_hyphens` PASSES |
| 6 | When operator types unknown provider at auxiliary prompt, wizard warns and skips (does NOT call `apply_auxiliary_answer`) | VERIFIED | Guard at setup.rs:212-217 (`!is_known_aux_provider` → `eprintln!` + skip) in `run_minimum_viable_flow` |
| 7 | Same allow-list guard is present in BOTH `run_minimum_viable_flow` AND `run_agent_section` | VERIFIED | setup.rs:212 (mvf) and setup.rs:350 (run_agent_section); test `cfg_02_both_aux_call_sites_have_guard` PASSES with `include_str!` ordering invariant |
| 8 | `KNOWN_AUX_PROVIDERS` const and `is_known_aux_provider` helper exist with correct 6-provider locked list | VERIFIED | setup.rs:28-46; `KNOWN_AUX_PROVIDERS = ["openrouter","anthropic","openai","google","mistral","groq"]`; tests `cfg_02_is_known_aux_provider_accepts_built_ins`, `cfg_02_is_known_aux_provider_rejects_unknown`, `cfg_02_known_aux_providers_const_locked` all PASS |
| 9 | `hermes gateway` invocation triggers `run_preflight` before gateway startup (missing config → wizard, not crash) | VERIFIED | main.rs:309-312: `let run_preflight = matches!(cli.command, Some(Commands::Chat { .. }) \| Some(Commands::Gateway { .. }) \| None) && cli.execute.is_none();` |
| 10 | `is_interactive_repl` and `is_chat_or_bare` sibling gates are NOT widened to Gateway (log filter preserved) | VERIFIED | main.rs:325-326 and 337-338 both match only `Some(Commands::Chat { .. }) \| None`; INV-02 test `sibling_gates_not_widened_to_gateway` PASSES |
| 11 | Phase 26.4.1 amendment doc comment present in main.rs near the widened gate | VERIFIED | main.rs:293-308 contains full CFG-03 amendment comment; INV-03 test `phase_amendment_doc_comment_present` PASSES |
| 12 | Integration regression test file `invariants_26_4_1_cfg_03.rs` exists and all 3 tests pass | VERIFIED | File exists at `crates/ironhermes-cli/tests/invariants_26_4_1_cfg_03.rs`; `cargo test --test invariants_26_4_1_cfg_03` → 3/3 PASS |

**Score:** 12/12 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/ironhermes-cli/src/setup.rs` | CFG-01: `write_env_var_to_dotenv` + `provider_env_var_name` + `config_setter::config_set` in `run_minimum_viable_flow`; CFG-02: `KNOWN_AUX_PROVIDERS` + `is_known_aux_provider` + guards at 2 call sites | VERIFIED | All constructs present at expected line numbers; 8/8 unit tests pass |
| `crates/ironhermes-cli/src/main.rs` | CFG-03: `run_preflight` gate widened to `Chat \| Gateway \| None`; sibling gates unchanged; amendment doc comment present | VERIFIED | Gate at lines 309-312 matches exactly; sibling gates at 325-326 and 337-338 unchanged |
| `crates/ironhermes-cli/tests/invariants_26_4_1_cfg_03.rs` | 3 source-text regression invariant tests for CFG-03 | VERIFIED | File created in commit d09a1eb; 3/3 tests pass |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `run_minimum_viable_flow` | `write_env_var_to_dotenv` | direct call with `env_var_name = provider_env_var_name(&provider)` | VERIFIED | setup.rs:154-155 |
| `run_minimum_viable_flow` | `config_setter::config_set` | dotted key `providers.{provider}.api_key_env` | VERIFIED | setup.rs:156-160 |
| `run_minimum_viable_flow` auxiliary stage | `is_known_aux_provider` | guard before `apply_auxiliary_answer` | VERIFIED | setup.rs:212; `is_known_aux_provider` precedes `apply_auxiliary_answer` at 221 |
| `run_agent_section` auxiliary stage | `is_known_aux_provider` | same guard before `apply_auxiliary_answer` | VERIFIED | setup.rs:350; `is_known_aux_provider` precedes `apply_auxiliary_answer` at 367 |
| `main()` | `preflight::run_preflight_check` | gate now matches `Chat \| Gateway \| None` | VERIFIED | main.rs:309-314 |

---

### Data-Flow Trace (Level 4)

Not applicable — the changed code paths are wizard I/O functions and a CLI dispatch gate, not components that render dynamic data from a data store.

---

### Behavioral Spot-Checks

| Behavior | Check | Result | Status |
|----------|-------|--------|--------|
| CFG-01: `run_minimum_viable_flow` body contains NO call to `apply_api_key_answer` | `awk` body extraction + grep | Empty output (no match) | PASS |
| CFG-01: `write_env_var_to_dotenv` called with derived env var name | `grep -n "write_env_var_to_dotenv(hermes_home, &env_var_name"` | Match at line 155 | PASS |
| CFG-02: `is_known_aux_provider` called at both auxiliary sites | `grep -c "is_known_aux_provider"` | 22 occurrences (>>5 required) | PASS |
| CFG-02: Warning message appears exactly 2 times (one per call site) | `grep -nE "Unknown provider"` | 2 matches | PASS |
| CFG-03: widened gate pattern appears exactly once in main.rs | INV-01 test | 1 occurrence bound to `run_preflight` | PASS |
| CFG-03: un-widened sibling gate pattern appears exactly twice | INV-02 test | 2 occurrences | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description (as defined in plan) | Status | Evidence |
|-------------|------------|----------------------------------|--------|----------|
| CFG-01 | Plan 01 | API key persisted to .env + providers map, not deprecated model.api_key | SATISFIED | 4/4 unit tests pass; code at setup.rs:152-164 |
| CFG-02 | Plan 02 | Auxiliary provider allow-list guard at both wizard call sites | SATISFIED | 4/4 unit tests pass; KNOWN_AUX_PROVIDERS const + is_known_aux_provider helper wired at setup.rs:208-222 and 348-375 |
| CFG-03 | Plan 03 | run_preflight gate widened to include Commands::Gateway | SATISFIED | Gate at main.rs:309-312; 3/3 invariant integration tests pass |

**Note:** The REQUIREMENTS.md descriptions for CFG-01/02/03 describe different features (wizard UX, config CLI, config migrate) than what this phase implements (API key bug fix, auxiliary allow-list, gateway preflight). This is a pre-existing ID collision in the requirements document. The traceability table maps CFG-01/02/03 to Phase 23 (still Pending). Phase 26.4.1 uses these IDs for hotfix sub-tasks. No action needed — the phase goal as stated is fully achieved; the REQUIREMENTS.md inconsistency predates this phase and is a documentation concern for a future housekeeping pass.

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| None | — | — | — |

No TODO/FIXME/HACK/PLACEHOLDER patterns found in `setup.rs` or `main.rs`. No stub implementations, empty return values, or disconnected state in any of the modified code. Pre-existing warnings (25 in ironhermes-cli) are unchanged; zero new warnings introduced.

---

### Human Verification Required

None — all must-haves are verifiable programmatically. Manual smoke-test paths (e.g. running `hermes setup` interactively and checking .env output) are confirmed by the unit tests that exercise the exact same call sequence.

---

## Gaps Summary

No gaps. All three bug fixes are fully implemented, wired, and test-locked.

- CFG-01: `run_minimum_viable_flow` writes API key to `.env` (0600 mode) and `providers.{provider}.api_key_env` in config.yaml. The deprecated `model.api_key` path is not touched. Four unit tests lock this including a source-text invariant.
- CFG-02: `KNOWN_AUX_PROVIDERS` allow-list with `is_known_aux_provider` guard is wired at both `run_minimum_viable_flow` and `run_agent_section` auxiliary stages. Unknown provider names (e.g. "local") emit a warning and skip save, preventing the `ProviderResolver::build()` panic. Four unit tests lock this.
- CFG-03: `run_preflight` gate in `main.rs` now covers `Commands::Gateway`. The two sibling gates (`is_interactive_repl`, `is_chat_or_bare`) are intentionally NOT widened to preserve the `ironhermes=info` log filter for the gateway daemon. Three integration invariant tests lock the gate shape.

---

_Verified: 2026-05-07_
_Verifier: Claude (gsd-verifier)_
