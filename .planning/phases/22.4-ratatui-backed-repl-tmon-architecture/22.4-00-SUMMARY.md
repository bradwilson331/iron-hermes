---
phase: 22.4-ratatui-backed-repl-tmon-architecture
plan: "00"
subsystem: ui
tags: [rust, cargo, workspace-deps, ratatui, crossterm, tui-textarea, tui-logger, ansi-to-tui]

requires:
  - phase: 22.3-repl-ux-hardening
    provides: Stable REPL base that the ratatui-backed REPL will replace/extend

provides:
  - "Workspace crossterm 0.29 with event-stream feature (D-13)"
  - "Workspace ratatui 0.30, tui-textarea 0.7, ansi-to-tui 8, tui-logger 0.18 dep floor (D-14)"
  - "ironhermes-cli wired to all four new ratatui-ecosystem crates via workspace = true"
  - "cargo-tree spike evidence: FAIL — two ratatui compile units (0.29.0 from tui-textarea, 0.30.0 direct)"

affects:
  - 22.4-01
  - 22.4-02
  - 22.4-03
  - 22.4-04
  - 22.4-05
  - 22.4-06
  - 22.4-07
  - 22.4-08
  - 22.4-09
  - 22.4-10

tech-stack:
  added:
    - "crossterm 0.29 (bumped from 0.28, event-stream feature enabled)"
    - "ratatui 0.30 (workspace dep)"
    - "tui-textarea 0.7 (workspace dep — Pitfall §1 materialized: resolves ratatui ^0.29 → v0.29.0 duplicate)"
    - "tui-logger 0.18 with tracing-support (workspace dep)"
    - "ansi-to-tui 8 (workspace dep)"
  patterns:
    - "All ratatui-ecosystem crates declared in [workspace.dependencies] and inherited via workspace = true"

key-files:
  created: []
  modified:
    - Cargo.toml
    - crates/ironhermes-cli/Cargo.toml

key-decisions:
  - "Task 3 FAIL: tui-textarea 0.7 declares ratatui ^0.29 which cargo resolves to v0.29.0 alongside workspace ratatui v0.30.0 — Pitfall §1 materialized"
  - "Task 4 checkpoint:decision returned to user — three options: proceed-task3-passed (N/A), fallback-tui-textarea-2=0.10.2, fallback-git-dep"

patterns-established:
  - "Workspace dep inheritance pattern: declare in [workspace.dependencies], consume via { workspace = true } in crate Cargo.toml"

requirements-completed: [D-13, D-14]

duration: 4min
completed: 2026-04-24
---

# Phase 22.4 Plan 00: Workspace Dep Floor + cargo-tree Spike Summary

**crossterm bumped to 0.29 (event-stream enabled) and ratatui ecosystem added to workspace — spike reveals Pitfall §1: tui-textarea 0.7 forces a duplicate ratatui 0.29.0 compile unit alongside the 0.30.0 floor dep, requiring user decision on fallback strategy**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-04-24T14:16:11Z
- **Completed:** 2026-04-24T14:19:36Z
- **Tasks:** 3 executed (Task 4 is a checkpoint:decision awaiting user input)
- **Files modified:** 2

## Accomplishments

- Bumped workspace `crossterm` from `"0.28"` to `{ version = "0.29", features = ["event-stream"] }` — all existing call sites API-compatible, workspace builds clean
- Added `ratatui = "0.30"`, `tui-textarea = "0.7"`, `ansi-to-tui = "8"`, `tui-logger = { version = "0.18", features = ["tracing-support"] }` to `[workspace.dependencies]`
- Wired all four into `crates/ironhermes-cli/Cargo.toml` via `{ workspace = true }` after existing `crossterm` line
- `cargo fetch` resolved and downloaded all new deps successfully from crates.io
- Ran cargo-tree spike per RESEARCH Open Question §1 — Pitfall §1 confirmed: TWO ratatui compile units present

## Task Commits

1. **Task 1: Bump workspace crossterm 0.28 → 0.29 with event-stream feature** - `42fede9` (chore)
2. **Task 2: Add ratatui ecosystem deps to workspace and wire into ironhermes-cli** - `ce9c786` (chore)
3. **Task 3: cargo-tree spike** - (no commit — spike produces evidence only; plan FAIL case documented here)

## Files Created/Modified

- `Cargo.toml` - crossterm bumped to 0.29 with event-stream; ratatui ecosystem workspace deps added
- `crates/ironhermes-cli/Cargo.toml` - ansi-to-tui, ratatui, tui-logger, tui-textarea wired via workspace = true

## Spike Results

**`cargo tree -i ratatui@0.29.0` output:**
```
ratatui v0.29.0
└── tui-textarea v0.7.0
    └── ironhermes-cli v0.1.0 (/Users/twilson/code/ironhermes/.claude/worktrees/agent-a5c6d7d6/crates/ironhermes-cli)
```

**`cargo tree -i ratatui@0.30.0` output:**
```
ratatui v0.30.0
├── ironhermes-cli v0.1.0 (/Users/twilson/code/ironhermes/.claude/worktrees/agent-a5c6d7d6/crates/ironhermes-cli)
└── tui-logger v0.18.2
    └── ironhermes-cli v0.1.0 (/Users/twilson/code/ironhermes/.claude/worktrees/agent-a5c6d7d6/crates/ironhermes-cli)
```

**`cargo tree -p ratatui` output (ambiguous — two packages):**
```
error: There are multiple `ratatui` packages in your project, and the specification `ratatui` is ambiguous.
Please re-run this command with one of the following specifications:
  ratatui@0.29.0
  ratatui@0.30.0
```

**Verdict: FAIL — Pitfall §1 materialized.**

- `tui-textarea v0.7.0` declares `ratatui = "^0.29"` in its own Cargo.toml, which cargo resolves to `v0.29.0`
- The workspace floor `ratatui = "0.30"` resolves separately to `v0.30.0`
- Both compile units are present; cargo allows this but downstream `tui_rata/ui.rs` code will fail to compile because a `ratatui 0.30 Frame` cannot be passed to `tui-textarea`'s `TextArea::render` which is bound to `ratatui 0.29`
- `cargo build --workspace --all-features` exits 0 today (no `.rs` code yet imports these crates), but will fail as soon as any downstream plan writes code using both

**Count of top-level ratatui nodes:** 2 (FAIL per acceptance criteria: "at most one")

## Decisions Made

- Task 3 FAIL case confirmed per RESEARCH Pitfall §1 — `tui-textarea 0.7` declares `ratatui ^0.29` which cargo resolves to `v0.29.0` (does NOT unify with `v0.30.0` direct dep)
- Per plan Task 4 instructions: NOT auto-picking fallback — decision routed to user as `checkpoint:decision` per RESEARCH guidance that this exceeds Claude's Discretion ceiling (D-14)

## Deviations from Plan

None — plan executed exactly as written. The FAIL case was expected and documented in RESEARCH Pitfall §1. The checkpoint:decision is the correct response per Task 4 plan logic.

## Issues Encountered

Pitfall §1 materialized as predicted in RESEARCH Open Question §1. `tui-textarea 0.7` pins `ratatui ^0.29` and cargo resolves it to `0.29.0` rather than unifying with the `0.30.0` floor. This prevents the plan from auto-proceeding.

## Next Phase Readiness

**BLOCKED on Task 4 checkpoint:decision.** User must select one of three options:

1. **proceed-task3-passed** — N/A (Task 3 FAILED; this option does not apply)
2. **fallback-tui-textarea-2 = "0.10.2"** — Replace `tui-textarea = "0.7"` with `tui-textarea-2 = "0.10.2"` in workspace deps; update ironhermes-cli to match; import path in all downstream `tui_rata/` code becomes `tui_textarea_2::TextArea`
3. **fallback-git-dep** — Use a git dependency on `tui-textarea` main branch; reproducibility concern per RESEARCH

All Wave 1+ Phase 22.4 plans are unblocked as soon as the user selects option 2 or 3 and the corresponding Cargo.toml change is made.

---
*Phase: 22.4-ratatui-backed-repl-tmon-architecture*
*Completed: 2026-04-24*
