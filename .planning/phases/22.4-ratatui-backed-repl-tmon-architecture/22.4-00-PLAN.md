---
phase: 22.4-ratatui-backed-repl-tmon-architecture
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - Cargo.toml
  - crates/ironhermes-cli/Cargo.toml
autonomous: false
requirements: [D-13, D-14]
tags: [rust, cargo, workspace-deps, ratatui, crossterm]

must_haves:
  truths:
    - "Workspace Cargo.toml declares crossterm 0.29 with event-stream feature"
    - "Workspace Cargo.toml declares ratatui 0.30, tui-textarea, ansi-to-tui, tui-logger"
    - "ironhermes-cli depends on the four new ratatui-ecosystem crates via workspace inheritance"
    - "Workspace compiles with the new dep floor (classic-tui path still builds cleanly under D-02)"
    - "`cargo tree -p ratatui` resolves to a single ratatui 0.30.x unit (Pitfall §1 spike)"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace dep pins for ratatui ecosystem"
      contains: "ratatui = \"0.30\""
    - path: "crates/ironhermes-cli/Cargo.toml"
      provides: "Crate-level ratatui-ecosystem dep wiring"
      contains: "ratatui = { workspace = true }"
  key_links:
    - from: "workspace Cargo.toml"
      to: "crates/ironhermes-cli/Cargo.toml"
      via: "workspace = true inheritance"
      pattern: "workspace = true"
    - from: "workspace Cargo.toml crossterm entry"
      to: "D-16 async event loop"
      via: "features = [\"event-stream\"]"
      pattern: "features = \\[\"event-stream\"\\]"
---

<objective>
Land the workspace dependency floor required by every downstream Phase 22.4 plan, and confirm (via a cargo-tree spike) that the tui-textarea 0.7 + ratatui 0.30 combination resolves to a single ratatui compile unit. Per D-13, bump workspace `crossterm` from 0.28 to 0.29 with the `event-stream` feature enabled. Per D-14, add `ratatui = "0.30"`, `tui-textarea = "0.7"`, `ansi-to-tui = "8"`, and `tui-logger = "0.18"` (with `tracing-support`) to `[workspace.dependencies]` and wire them into `crates/ironhermes-cli/Cargo.toml` via `workspace = true`. The classic-tui path (per D-02) must still compile and pass its existing tests under the new dep floor — this plan does NOT touch any `.rs` file. Per RESEARCH Open Question §1, the executor runs `cargo tree -p ratatui` as a live spike: if two ratatui compile units appear, the executor pauses at the checkpoint for the user to approve the `tui-textarea-2 = "0.10.2"` fallback.

Purpose: Unblocks every Phase 22.4 plan that imports ratatui/crossterm 0.29/tui-textarea/ansi-to-tui/tui-logger.
Output: Updated `Cargo.toml` (root + cli crate). Spike evidence recorded in task output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-CONTEXT.md
@.planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-RESEARCH.md
@.planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-PATTERNS.md
@Cargo.toml
@crates/ironhermes-cli/Cargo.toml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump workspace crossterm 0.28 → 0.29 with event-stream feature</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (workspace root — current crossterm line is `crossterm = "0.28"` at line 65 per `grep -n "crossterm" Cargo.toml`)
    - .planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-RESEARCH.md §crossterm 0.28 → 0.29 Migration (D-13) (lines ~211–242) — confirms the only breaking change is `KeyModifiers` Display impl, which IronHermes does not call
    - .planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-CONTEXT.md §D-13 (workspace crossterm bump) + §D-16 (async EventStream requires event-stream feature)
    - .planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-PATTERNS.md §`Cargo.toml` (workspace root) — target pin shape
    - /Users/twilson/code/tmon/Cargo.toml — proven-compatible pins (`crossterm = "0.29.0"`, `features = ["event-stream"]`)
  </read_first>
  <action>
    Edit the workspace root `Cargo.toml` under `[workspace.dependencies]` (create the section if missing). Replace the existing `crossterm = "0.28"` entry with `crossterm = { version = "0.29", features = ["event-stream"] }`. Do not change the `[workspace.package]` block or any other workspace field. Do not edit any `.rs` file.

    If no `[workspace.dependencies]` table exists and the existing `crossterm = "0.28"` lives in another section (e.g. `[workspace.dependencies]` already present), replace the line in place. Keep surrounding comments and whitespace. Per RESEARCH §crossterm 0.28 → 0.29 Migration, all existing IronHermes call sites (`tui/keybindings.rs:128,134`, `tui/extension.rs:223,240`, `tui/commands.rs:221,233,488`, `tui/render.rs` poll, `main.rs:1067` poll) are API-compatible with 0.29 and require NO source edits in this task.

    Run the workspace build after the edit: `cargo build --workspace --all-features` and `cargo test -p ironhermes-cli --tests --no-run`. Both MUST exit 0. If either fails because of a `KeyModifiers` Display-impl call site (the only documented 0.28→0.29 break), grep for `KeyModifiers` format usage (`grep -rn "KeyModifiers" crates/ | grep -E 'format!|to_string|write!'`); no such call sites are expected per RESEARCH §crossterm §Existing IronHermes call sites to audit.
  </action>
  <verify>
    <automated>grep -q 'crossterm = { version = "0.29", features = \["event-stream"\] }' Cargo.toml &amp;&amp; cargo build --workspace --all-features 2&gt;&amp;1 | tail -5 &amp;&amp; cargo test -p ironhermes-cli --tests --no-run 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml contains the literal string `crossterm = { version = "0.29", features = ["event-stream"] }`
    - `cargo build --workspace --all-features` exits 0
    - `cargo test -p ironhermes-cli --tests --no-run` exits 0
    - `grep -c 'crossterm = "0.28"' Cargo.toml` returns 0 (old pin fully removed)
    - No `.rs` files modified (`git diff --stat -- '*.rs' | wc -l` returns 0 after this task)
  </acceptance_criteria>
  <done>Workspace pins crossterm 0.29 with event-stream enabled; full workspace still compiles; all existing crossterm call sites remain source-unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add ratatui + tui-textarea + ansi-to-tui + tui-logger to workspace deps and wire into ironhermes-cli</name>
  <files>Cargo.toml, crates/ironhermes-cli/Cargo.toml</files>
  <read_first>
    - Cargo.toml (post-Task 1 state)
    - crates/ironhermes-cli/Cargo.toml (current `[dependencies]` already uses `crossterm = { workspace = true }` per `grep -n "crossterm" crates/ironhermes-cli/Cargo.toml` line 44)
    - .planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-RESEARCH.md §Standard Stack §Installation (lines ~142–162) — canonical version pins + feature flag list
    - .planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-RESEARCH.md §Dependency Version Matrix (lines ~935–953) — exact version pin notes
    - .planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-PATTERNS.md §`crates/ironhermes-cli/Cargo.toml` — target shape
    - .planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-CONTEXT.md §D-14 (floor deps + Claude's Discretion ceiling)
  </read_first>
  <action>
    (a) In workspace root `Cargo.toml` under `[workspace.dependencies]`, append these entries verbatim (preserve alphabetical ordering if the existing section is sorted; otherwise append at end):
    ```toml
    ansi-to-tui = "8"
    ratatui = "0.30"
    tui-logger = { version = "0.18", features = ["tracing-support"] }
    tui-textarea = "0.7"
    ```
    (b) In `crates/ironhermes-cli/Cargo.toml` under the existing `[dependencies]` block, append the following entries verbatim after the existing `crossterm = { workspace = true }` line:
    ```toml
    ansi-to-tui = { workspace = true }
    ratatui = { workspace = true }
    tui-logger = { workspace = true }
    tui-textarea = { workspace = true }
    ```
    Do NOT add these to `[dev-dependencies]` — they are runtime deps used by `tui_rata/`. Do NOT add them to any other crate this phase (`tui_rata/` lives only in `ironhermes-cli`). Keep the existing `insta = "1"` dev-dep entry unchanged (already present per RESEARCH §Supporting). Do not edit `[features]` or `[[bin]]` blocks.
  </action>
  <verify>
    <automated>grep -q 'ratatui = "0.30"' Cargo.toml &amp;&amp; grep -q 'tui-textarea = "0.7"' Cargo.toml &amp;&amp; grep -q 'ansi-to-tui = "8"' Cargo.toml &amp;&amp; grep -q 'tui-logger = { version = "0.18", features = \["tracing-support"\] }' Cargo.toml &amp;&amp; grep -q 'ratatui = { workspace = true }' crates/ironhermes-cli/Cargo.toml &amp;&amp; grep -q 'tui-textarea = { workspace = true }' crates/ironhermes-cli/Cargo.toml &amp;&amp; grep -q 'ansi-to-tui = { workspace = true }' crates/ironhermes-cli/Cargo.toml &amp;&amp; grep -q 'tui-logger = { workspace = true }' crates/ironhermes-cli/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml contains exact strings `ratatui = "0.30"`, `tui-textarea = "0.7"`, `ansi-to-tui = "8"`, `tui-logger = { version = "0.18", features = ["tracing-support"] }`
    - crates/ironhermes-cli/Cargo.toml contains all four `{ workspace = true }` lines for `ratatui`, `tui-textarea`, `ansi-to-tui`, `tui-logger`
    - `cargo metadata --format-version 1 --no-deps` exits 0 (manifests valid)
    - `cargo fetch --locked` OR `cargo fetch` exits 0 (all new deps resolve from crates.io)
    - No other `.toml` files modified (`git diff --stat -- '*.toml'` shows only root + cli Cargo.toml)
  </acceptance_criteria>
  <done>Workspace and crate manifests include the D-14 floor deps; metadata parses cleanly; all new deps fetch successfully from crates.io.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: cargo-tree spike — confirm single ratatui compile unit</name>
  <files>(none — spike produces evidence only)</files>
  <read_first>
    - .planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-RESEARCH.md §Pitfall §1 (tui-textarea 0.7 declares `ratatui ^0.29`) — lines ~1005–1018
    - .planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-RESEARCH.md §Open Questions §1 (lines ~1290–1296) — spike procedure
    - .planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-RESEARCH.md §Dependency Version Matrix fallback row (tui-textarea-2 = "0.10.2")
  </read_first>
  <action>
    Run the following commands in the workspace root (post-Task 2 state) and capture raw stdout:
    ```
    cargo build --workspace --all-features 2>&1 | tail -20
    cargo tree -p ratatui 2>&1
    cargo tree -i ratatui 2>&1 | head -50
    ```
    Inspect `cargo tree -i ratatui` output:
    - **PASS case:** only a single `ratatui v0.30.x` node appears in the inverted tree. Downstream plans proceed with `tui-textarea = "0.7"`.
    - **FAIL case:** TWO distinct `ratatui vX.Y.Z` nodes appear (e.g., `ratatui v0.29.0` and `ratatui v0.30.0`). This indicates Pitfall §1 materialised.

    If PASS: record evidence as plain text in the task output (paste the first 30 lines of `cargo tree -i ratatui`). Proceed.

    If FAIL: do NOT automatically switch to `tui-textarea-2`. Pause execution and surface the raw output to the user. The next task is a `checkpoint:decision` that asks the user to approve the `tui-textarea-2 = "0.10.2"` fallback before any further source work.
  </action>
  <verify>
    <automated>cargo build --workspace --all-features &amp;&amp; cargo tree -i ratatui 2&gt;&amp;1 | grep -c '^ratatui v' | awk '$1 &lt;= 1 { exit 0 } { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace --all-features` exits 0
    - `cargo tree -i ratatui` output contains at most one line starting with `ratatui v` at column 0 (i.e., at most one top-level ratatui compile unit). If 2+, task FAILS and the next checkpoint activates.
    - Evidence text (first 30 lines of `cargo tree -i ratatui`) captured in the plan SUMMARY.md under a §Spike Results section.
  </acceptance_criteria>
  <done>cargo-tree evidence confirms ratatui resolves to a single compile unit; or the failure is surfaced to the user via the next checkpoint task.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Checkpoint — tui-textarea fallback decision (skip if Task 3 passed)</name>
  <decision>If Task 3's `cargo tree -i ratatui` showed two distinct ratatui compile units (ratatui 0.29 from tui-textarea + ratatui 0.30 direct), approve the fallback.</decision>
  <context>
    RESEARCH Pitfall §1 and Open Question §1 flagged this risk: tui-textarea 0.7 declares `ratatui ^0.29` which may force a duplicate compile unit with the D-14 `ratatui = "0.30"` floor. If Task 3 hit the FAIL case, we cannot proceed — downstream `tui_rata/ui.rs` will pass a `ratatui 0.30` `Frame` to a `ratatui 0.29`-bound `TextArea::render`, failing compilation. RESEARCH-approved fallback: `tui-textarea-2 = "0.10.2"` (a maintained fork explicitly tracking ratatui head). This decision is routed to the user because it changes the dep surface beyond the D-14 floor and needs explicit approval (Claude's Discretion ceiling per D-14).

    If Task 3 PASSED (single ratatui compile unit), this checkpoint auto-resolves as "proceed" — no user action needed; record in task output that "Task 3 PASSED — checkpoint skipped".
  </context>
  <options>
    <option id="proceed-task3-passed">
      <name>Proceed — Task 3 passed</name>
      <pros>No dep churn; tui-textarea 0.7 works out of the box.</pros>
      <cons>None.</cons>
    </option>
    <option id="fallback-tui-textarea-2">
      <name>Fallback — swap to tui-textarea-2 = "0.10.2"</name>
      <pros>Maintained fork explicitly tracking ratatui head; eliminates the duplicate-ratatui compile-unit failure.</pros>
      <cons>Fork of a popular crate — adds a mild sustainability concern; import path becomes `tui_textarea_2::TextArea` in all downstream tui_rata/ code (trivial mechanical rename).</cons>
    </option>
    <option id="fallback-git-dep">
      <name>Fallback — use git dep on tui-textarea main branch</name>
      <pros>Tracks upstream fixes as they land.</pros>
      <cons>Reproducibility concern; RESEARCH Pitfall §1 advises against this path for production.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed-task3-passed, fallback-tui-textarea-2, or fallback-git-dep.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io → local cargo cache | Untrusted crate source code compiled + linked into ironhermes-cli |
| Cargo.toml edits → downstream plans | This plan's manifest edits gate every Wave 1+ plan's import statements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22.4-00-01 | Tampering | Untrusted ratatui-ecosystem crate from crates.io | accept | All four crates (ratatui 0.30, tui-textarea 0.7, ansi-to-tui 8, tui-logger 0.18) are widely used, maintained, and in the D-14 floor approved by user CONTEXT. Cargo.lock pins exact SHA-checked versions. |
| T-22.4-00-02 | Denial of service | Duplicate ratatui compile units (Pitfall §1) | mitigate | Task 3 `cargo tree -i ratatui` spike FAILS the plan if two units appear; Task 4 checkpoint routes fallback decision to the user before downstream work proceeds. |
| T-22.4-00-03 | Tampering | tui-logger `tracing-support` feature installs a global subscriber layer | accept | Subscriber registration happens in a later plan (22.4-10) gated on the D-03/D-04 backend branch per RESEARCH Pitfall §2. This plan only declares the dep. |
| T-22.4-00-04 | Denial of service | Workspace build regression on other crates from crossterm 0.29 bump | mitigate | Task 1 runs `cargo build --workspace --all-features` + `cargo test -p ironhermes-cli --tests --no-run` as gating verify commands. RESEARCH §crossterm §Existing IronHermes call sites confirms all 0.28 call sites are 0.29-compatible with zero source edits. |
</threat_model>

<verification>
- `cargo build --workspace --all-features` exits 0
- `cargo test -p ironhermes-cli --tests --no-run` exits 0 (classic-tui tests still compile under new dep floor per D-02)
- `cargo tree -i ratatui` shows exactly one top-level ratatui unit OR the user has approved the fallback path via Task 4
- No `.rs` files modified by this plan (only `Cargo.toml` + `crates/ironhermes-cli/Cargo.toml`)
</verification>

<success_criteria>
- Workspace Cargo.toml: `crossterm = { version = "0.29", features = ["event-stream"] }`, `ratatui = "0.30"`, `tui-textarea = "0.7"`, `ansi-to-tui = "8"`, `tui-logger = { version = "0.18", features = ["tracing-support"] }`
- crates/ironhermes-cli/Cargo.toml inherits all four new deps via `{ workspace = true }`
- Full workspace compiles; existing tests still build
- cargo-tree spike evidence captured; if FAIL case, user approved fallback before closing this plan
- Downstream plans can now `use ratatui::…`, `use crossterm::event::EventStream`, `use tui_textarea::TextArea`, `use ansi_to_tui::IntoText`, `use tui_logger::…`
</success_criteria>

<output>
After completion, create `.planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-00-SUMMARY.md` including §Spike Results with the `cargo tree -i ratatui` evidence block.
</output>
