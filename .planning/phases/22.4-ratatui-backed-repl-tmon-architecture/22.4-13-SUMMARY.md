---
phase: 22.4-ratatui-backed-repl-tmon-architecture
plan: 13
status: complete
shipped_at: 2026-04-24
gap_closure: true
addresses:
  - D-17
  - CR-02
carry_over_from: 22.4-12
---

# Plan 22.4-13: 22.4-12 Task 3 Carry-Over (D-17 / CR-02 Grep-Locks)

## Outcome

The two static-grep invariants reserved by Plan 22.4-12 Task 3 are now live in
`crates/ironhermes-cli/tests/invariants_22_4.rs`. The D-17 / CR-02 wiring shipped
inline by Plan 22.4-12 Tasks 1+2 (commits `8a39eed` and `8a9125b`) is now
locked against silent regression in the same way every other 22.4 invariant is.

## Pre-flight (Task 1)

All six grep targets satisfied at execution time:

| Check | Expected | Actual | Status |
|-------|----------|--------|--------|
| `fn invariant_22_4_*` count | ≥ 26 | 26 | ✓ |
| `with_tool_progress(` in event_loop.rs | ≥ 1 | 1 | ✓ |
| `with_tool_result(` in event_loop.rs | ≥ 1 | 1 | ✓ |
| `StreamEvent::ToolCall` in event_loop.rs | ≥ 1 | 1 | ✓ |
| `StreamEvent::ToolProgress` in event_loop.rs | ≥ 1 | 1 | ✓ |
| `StreamEvent::ToolResult` in event_loop.rs | ≥ 1 | 1 | ✓ |

Pre-flight passed → safe to append.

## What shipped (Task 2)

**`crates/ironhermes-cli/tests/invariants_22_4.rs`** (+50 lines, append-only at end):

- **INV-22.4-26 `invariant_22_4_26_tool_progress_wired`** — asserts both
  `with_tool_progress(` and `with_tool_result(` appear in `TUI_RATA_EVLOOP`.
  If a future refactor drops either chained call from `spawn_turn`, CI fails.

- **INV-22.4-27 `invariant_22_4_27_tool_variants_constructed`** — loop asserts
  all three previously-dead `StreamEvent::ToolCall` / `ToolProgress` / `ToolResult`
  variants are constructed in `event_loop.rs` (the production sender path), not
  just handled in `app.rs`. Closes the dead-handler/no-sender ambiguity.

## Verification

| Command | Expected | Actual |
|---------|----------|--------|
| `grep -c "fn invariant_22_4_" crates/ironhermes-cli/tests/invariants_22_4.rs` | 28 | 28 ✓ |
| `cargo test -p ironhermes-cli --test invariants_22_4` | 28 passed | 28 passed, 0 failed ✓ |
| `invariant_22_4_26_tool_progress_wired` | pass | pass ✓ |
| `invariant_22_4_27_tool_variants_constructed` | pass | pass ✓ |

## Scope respected

- Test-only delta — zero production source modified.
- Existing INVs 00–25 byte-identical (no renumbering, no relocation).
- `TUI_RATA_EVLOOP` constant at line 13 untouched.

## Wave-1 baseline established

Subsequent gap-closure plans (22.4-14, 22.4-15, 22.4-16) start from a clean
INV-22.4-27 floor. Plan 22.4-15 picks up at INV-22.4-28; Plan 22.4-16 replaces
INV-22.4-23 with INV-22.4-29 (per the locked mouse-policy decision).
