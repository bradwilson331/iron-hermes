---
status: partial
phase: 22.4-ratatui-backed-repl-tmon-architecture
source: [22.4-01-SUMMARY.md, 22.4-05-SUMMARY.md, 22.4-06-SUMMARY.md, 22.4-07-SUMMARY.md, 22.4-08-SUMMARY.md]
started: 2026-04-24T22:39:40Z
updated: 2026-04-24T22:39:40Z
---

## Current Test

[testing complete — user reported 3 issues directly]

## Tests

### 1. User Input Prompt Area
expected: |
  When the ratatui REPL launches via `hermes chat`, the bottom of the TUI
  shows a clearly defined input prompt area (textarea/input box) where the
  user can type a prompt to send to the agent. The area should be visually
  distinct (border, label, or contrasting background) so the user
  immediately knows where to type.
result: issue
reported: "there is no user input prompt area defined"
severity: blocker

### 2. web_search Tool Works
expected: |
  Issuing a chat prompt that triggers the `web_search` tool causes the
  agent to invoke the tool, fetch results, and stream the response back
  into the transcript. ToolCall/ToolProgress/ToolResult activity should be
  visible in the status pill or activity row while it runs.
result: issue
reported: "the web_search function does not work"
severity: blocker

### 3. Mouse / Click Inside Response Block
expected: |
  Clicking inside the response/transcript pane with the mouse selects
  text or focuses the area for scroll/copy operations. At minimum, the
  TUI should not eat the click without any visible response or fail to
  enable text selection in the rendered transcript.
result: issue
reported: "cannot click inside the response block"
severity: major

## Summary

total: 3
passed: 0
issues: 3
pending: 0
skipped: 0

## Gaps

- truth: "Bottom-of-TUI input prompt area is visually defined and labeled so the user knows where to type"
  status: failed
  reason: "User reported: there is no user input prompt area defined"
  severity: blocker
  test: 1
  artifacts: []
  missing: []

- truth: "web_search tool dispatches successfully from the ratatui REPL and surfaces ToolCall/ToolProgress/ToolResult to the UI"
  status: failed
  reason: "User reported: the web_search function does not work"
  severity: blocker
  test: 2
  artifacts: []
  missing: []

- truth: "Mouse clicks inside the response/transcript pane are usable (text selection or focus), not silently consumed"
  status: failed
  reason: "User reported: cannot click inside the response block"
  severity: major
  test: 3
  artifacts: []
  missing: []
