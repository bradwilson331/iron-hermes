---
phase: 22.4-ratatui-backed-repl-tmon-architecture
plan: 11
subsystem: tui
tags: [ratatui, banner, gap-closure, tui, cli]

requires:
  - phase: 22.4-ratatui-backed-repl-tmon-architecture
    provides: run_chat_ratatui dispatch sites in main.rs (Commands::Chat arm + bare-hermes arm)

provides:
  - print_banner() + stdout/stderr flush inserted in both ratatui dispatch arms before run_chat_ratatui
  - INV-22.4-25 static-grep invariant locking both new call sites against future regression
  - D-03 gap closure: banner now lands in scrollback pre-TUI for all ratatui entry paths

affects:
  - 22.4-VERIFICATION.md gap-1 (D-03 verification truth 3: FAILED -> VERIFIED)
  - future refactors of main.rs ratatui dispatch sites

tech-stack:
  added: []
  patterns:
    - "print_banner() + io::stdout().flush().ok() + io::stderr().flush().ok() before run_chat_ratatui (mirrors classic run_chat GAP-5 double-flush pattern)"
    - "static-grep invariant using MAIN_RS.matches().count() >= N for call-site count enforcement"
    - "first-occurrence ordering check (MAIN_RS.find()) to enforce pre-alt-screen banner ordering"

key-files:
  created:
    - .planning/phases/22.4-ratatui-backed-repl-tmon-architecture/22.4-11-SUMMARY.md
  modified:
    - crates/ironhermes-cli/src/main.rs
    - crates/ironhermes-cli/tests/invariants_22_4.rs

key-decisions:
  - "Inserted print_banner() at dispatch layer (not inside run_chat_ratatui) to keep both banners (yolo + hermes) at the same call site, matching INV-22.4-14 pattern"
  - "Double-flush (stdout + stderr) mirrors classic run_chat line 763-764 GAP-5 rationale verbatim"
  - "INV-22.4-25 uses >= 3 count assertion (not == 3) to be forward-compatible with any future additional dispatch sites"

patterns-established:
  - "D-03 pattern: any new ratatui dispatch site must call print_banner() + both flushes before run_chat_ratatui"
  - "Gap-closure invariant shape: count call sites, check first-occurrence ordering, grep-lock flush companions"

requirements-completed:
  - D-03

duration: 12min
completed: 2026-04-24
---

# Phase 22.4 Plan 11: print_banner() pre-ratatui gap closure (D-03) Summary

**print_banner() + GAP-5 double-flush inserted in both ratatui dispatch arms (Commands::Chat + bare-hermes) before run_chat_ratatui, locked by INV-22.4-25 static-grep invariant — closes D-03 Gap 1**

## Performance

- **Duration:** 12 min
- **Started:** 2026-04-24T16:00:00Z
- **Completed:** 2026-04-24T16:12:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Inserted `print_banner(); io::stdout().flush().ok(); io::stderr().flush().ok();` in the Commands::Chat ratatui arm of main.rs (after existing yolo-banner guard, before `run_chat_ratatui` call) — new call site at line 257
- Inserted the identical 3-line block in the bare-hermes ratatui arm of main.rs (after existing yolo-banner guard, before `run_chat_ratatui` call) — new call site at line 321
- Appended `invariant_22_4_25_print_banner_pre_ratatui` to invariants_22_4.rs, asserting: (a) >= 3 `print_banner();` call sites in main.rs, (b) first `print_banner()` precedes first `run_chat_ratatui(` in source, (c) both GAP-5 flush lines are present
- All 26 invariants pass; run_chat_invariants 11/11 pass; tui_rata_snapshots 8/8 pass; cargo build 0 new warnings

## Task Commits

Each task was committed atomically:

1. **Task 1: Insert print_banner() + stdout/stderr flush in both ratatui dispatch arms** - `f1aeb73` (feat)
2. **Task 2: Add INV-22.4-25 to lock print_banner() at ratatui dispatch sites** - `fc2f096` (test)

## Files Created/Modified

- `crates/ironhermes-cli/src/main.rs` - Two 3-line blocks inserted: print_banner() + stdout flush + stderr flush, one in Commands::Chat ratatui arm (line ~257 post-edit) and one in bare-hermes ratatui arm (line ~321 post-edit)
- `crates/ironhermes-cli/tests/invariants_22_4.rs` - INV-22.4-25 appended (48 lines); test count: 25 -> 26

## Decisions Made

- Kept `print_banner()` as a private fn in main.rs and inserted at the dispatch layer (not inside `run_chat_ratatui`). This keeps both banners (yolo-stderr + hermes-stdout) at the same call site, exactly matching the INV-22.4-14 pattern for the yolo banner.
- Used `>= 3` in the count assertion (not `== 3`) to be forward-compatible with any future additional dispatch sites.
- INV-22.4-25 uses first-occurrence ordering (`.find()`) from INV-22.4-24's established pattern, which is the minimal correct proof that at least one `print_banner()` call precedes the first `run_chat_ratatui(` call.

## Deviations from Plan

None - plan executed exactly as written. The `io` and `Write` imports were already in scope at line 12 (`use std::io::{self, Write};`), as predicted. No import additions were needed.

## Issues Encountered

- Initial verification grep ran against the main repo (not the worktree) because `cd /Users/twilson/code/ironhermes` was used. Corrected by running cargo test from the worktree cwd (`/Users/twilson/code/ironhermes/.claude/worktrees/agent-a8f23f35`). Edits were applied correctly to the worktree file throughout.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- D-03 verification truth 3 is now closable: `print_banner()` fires BEFORE `ratatui::init()` in all three entry paths (classic run_chat + Commands::Chat ratatui arm + bare-hermes ratatui arm)
- INV-22.4-25 locks both new call sites against future regression — any refactor that removes or moves them will fail CI
- Phase 22.4 gap-closure progress: D-03 Gap 1 resolved

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. The only change is a synchronous stdout/stderr write before ratatui::init() — consistent with the threat model accept disposition for T-22.4-11-02 (bounded-length write, no race with async boundary).

## Self-Check: PASSED

- `crates/ironhermes-cli/src/main.rs` — confirmed modified (3 print_banner(); call sites at lines 257, 321, 770)
- `crates/ironhermes-cli/tests/invariants_22_4.rs` — confirmed modified (26 fn invariant_22_4_ functions)
- Commit `f1aeb73` exists in git log
- Commit `fc2f096` exists in git log
- 26/26 invariants_22_4 tests pass
- 11/11 run_chat_invariants pass
- 8/8 tui_rata_snapshots pass
- cargo build: 21 warnings (pre-existing count, no new warnings), exit 0

---
*Phase: 22.4-ratatui-backed-repl-tmon-architecture*
*Completed: 2026-04-24*
