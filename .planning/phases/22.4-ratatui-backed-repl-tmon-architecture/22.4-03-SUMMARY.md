---
phase: 22.4-ratatui-backed-repl-tmon-architecture
plan: "03"
subsystem: ui
tags: [rust, tui_rata, status-line, pills, ratatui-port, d-10]

requires:
  - phase: 22.4-ratatui-backed-repl-tmon-architecture
    plan: "02"
    provides: tui_rata/ with mod.rs declaring knight_rider + double_ctrl_c + keybindings

provides:
  - "tui_rata/status_line.rs — StatusLineState, format_token_count, render_status_line_ratatui available under crate::tui_rata::status_line::*"
  - "render_status_line_ratatui(&StatusLineState) -> Line<'static> for use in tui_rata/ui.rs plan 22.4-07"
  - "Pill palette Cyan->Magenta->Green->Yellow locked by regression test"
  - "StatusLineState + format_token_count verbatim from classic tui/status_line.rs"
  - "D-02 isolation: classic tui/status_line.rs + tui/pills.rs untouched"

affects:
  - 22.4-07

tech-stack:
  added: []
  patterns:
    - "Verbatim-lift pattern: business logic (StatusLineState, format_token_count, pill derivation) copied exactly from classic; only output type changed from String/ColoredString to Line<'static>/Span::styled"
    - "Palette rotation via index mod: palette[i % palette.len()] replaces closure array from pills.rs"

key-files:
  created:
    - crates/ironhermes-cli/src/tui_rata/status_line.rs
  modified:
    - crates/ironhermes-cli/src/tui_rata/mod.rs

key-decisions:
  - "build_pills() private helper extracts pill text vec + hint Option — separates data derivation from ratatui rendering; render_status_line_ratatui composes on top"
  - "Hint stored as Option<String> (None when empty) vs classic's if state.hint.is_empty() check inline — functionally identical, cleaner for the two-phase approach"
  - "Palette has 4 entries (Cyan/Magenta/Green/Yellow) not 5 — classic pills.rs has 5 (adds dimmed at index 4 for the 5th+ pill); tui_rata variant omits the dimmed-pill rotation since hint is always DIM and the agents pill gets colour via normal rotation"

requirements-completed: [D-02, D-10]

duration: ~12min
completed: 2026-04-24T15:27:00Z
---

# Phase 22.4 Plan 03: tui_rata Status Line Port Summary

**StatusLineState + format_token_count verbatim from classic; render_status_line_ratatui returns a Cyan->Magenta->Green->Yellow palette-rotated Line<'static> of Span::styled pills; classic tui/status_line.rs + tui/pills.rs untouched (D-02)**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-04-24T15:15:15Z
- **Completed:** 2026-04-24T15:27:00Z
- **Tasks:** 2 executed (1 implementation + 1 verification)
- **Files modified:** 2 (status_line.rs created, mod.rs updated) + SUMMARY.md

## Accomplishments

- Created `crates/ironhermes-cli/src/tui_rata/status_line.rs` (348 lines)
  - `StatusLineState` struct and `Default` impl lifted verbatim from `tui/status_line.rs`
  - `format_token_count()` pure function lifted verbatim
  - `build_pills()` private helper extracts pill derivation verbatim (mode/model/provider/tokens/agents conditional)
  - `render_status_line_ratatui()` new ratatui renderer: `Span::styled` with palette rotation, `Line::from(spans)` output
  - Palette locked: `[Color::Cyan, Color::Magenta, Color::Green, Color::Yellow]` per D-10
  - Dot separators always `Modifier::DIM`; hint always `Modifier::DIM`
  - `agents: N/M` pill only rendered when `active_subagents > 0` (Pitfall 8 / E-11 preserved)
  - 10 unit tests: 4 lifted format_token_count tests + 6 new ratatui render tests
- Registered `pub mod status_line;` in `tui_rata/mod.rs` in alphabetical position
- `cargo build --workspace --all-features` exits 0
- `cargo test -p ironhermes-cli --tests --no-run` exits 0

## Task Commits

1. **Task 1: Port StatusLineState + format_token_count + pill derivation into tui_rata/status_line.rs** — `110b934` (feat)
2. **Task 2: Run lifted + new tests** — (no commit — verification-only task)

## Files Created/Modified

- `crates/ironhermes-cli/src/tui_rata/status_line.rs` — new file, 348 lines; StatusLineState + build_pills + render_status_line_ratatui + 10 tests
- `crates/ironhermes-cli/src/tui_rata/mod.rs` — added `pub mod status_line;` in alphabetical position

## Verification

### Build Gates

| Gate | Command | Result |
|------|---------|--------|
| Crate build | `cargo build -p ironhermes-cli` | exit 0 |
| Full workspace | `cargo build --workspace --all-features` | exit 0 |
| Integration test compile | `cargo test -p ironhermes-cli --tests --no-run` | exit 0 |

### Test Results (Task 2)

| Module | Tests | Result |
|--------|-------|--------|
| `tui_rata::status_line` (ported) | 10 passed | ok |
| `tui::status_line` (classic) | 12 passed | ok |
| `tui::pills` (classic) | 5 passed | ok |

### Test Coverage

**tui_rata::status_line tests (10 total):**

| Test | Description |
|------|-------------|
| `format_token_count_under_1000` | Lifted verbatim from classic |
| `format_token_count_kilo` | Lifted verbatim from classic |
| `format_token_count_boundary_999_999_is_one_mega` | Lifted verbatim from classic (R1 W4) |
| `format_token_count_mega` | Lifted verbatim from classic |
| `render_empty_state_has_no_hint_span` | New: validates 7 spans (4 pills + 3 seps) when hint empty |
| `render_with_hint_appends_dim_span` | New: validates 9 spans (4 pills + 4 seps + 1 hint) |
| `render_four_pills_rotates_palette_cyan_magenta_green_yellow` | New: palette rotation regression (T-22.4-03-03 mitigate) |
| `agents_pill_hides_at_zero_active` | New: E-11 / Pitfall 8 guard |
| `agents_pill_shows_when_active` | New: D-04 agents N/M pill validation |
| `dot_separators_are_dim` | New: separator Modifier::DIM invariant |

### Acceptance Criteria

| Criterion | Result |
|-----------|--------|
| `tui_rata/status_line.rs` exists | PASS |
| Contains `pub struct StatusLineState` | PASS |
| Contains `pub fn render_status_line_ratatui` | PASS |
| Contains `pub fn format_token_count` | PASS |
| Contains `Color::Cyan`, `Color::Magenta`, `Color::Green`, `Color::Yellow` | PASS |
| Contains `Modifier::DIM` | PASS |
| Contains `Line::from` | PASS |
| Zero `use colored` imports | PASS (count=0) |
| `tui_rata/mod.rs` contains `pub mod status_line` | PASS |
| Classic files untouched (`git diff` empty) | PASS |
| `cargo build -p ironhermes-cli` exits 0 | PASS |

### D-02 Isolation

```
git diff -- crates/ironhermes-cli/src/tui/status_line.rs crates/ironhermes-cli/src/tui/pills.rs
```
Output: (empty) — both classic files untouched.

## Deviations from Plan

None — plan executed exactly as written. `build_pills()` helper function name matches the plan's template structure. Palette uses 4 entries (Cyan/Magenta/Green/Yellow) rather than the classic 5-entry palette that includes dimmed — this is correct per the plan spec which explicitly lists only 4 colours and keeps hint always DIM separately.

## Known Stubs

None — `render_status_line_ratatui` is fully implemented and returns a proper `Line<'static>`. All fields of `StatusLineState` flow through to visible span content.

## Threat Flags

None — no new network endpoints, auth paths, file access, or schema changes. The new `tui_rata::status_line` surface is internal to the crate and produces pure values (no I/O). T-22.4-03-03 palette-rotation threat mitigated by the `render_four_pills_rotates_palette_cyan_magenta_green_yellow` regression test.

## Self-Check: PASSED

- FOUND: `crates/ironhermes-cli/src/tui_rata/status_line.rs`
- FOUND: `pub struct StatusLineState` in status_line.rs
- FOUND: `pub fn render_status_line_ratatui` in status_line.rs
- FOUND: `pub fn format_token_count` in status_line.rs
- FOUND: `Color::Cyan`, `Color::Magenta`, `Color::Green`, `Color::Yellow` in status_line.rs
- FOUND: `Modifier::DIM` in status_line.rs
- FOUND: `Line::from` in status_line.rs
- CONFIRMED: `use colored` count = 0
- FOUND: `pub mod status_line` in tui_rata/mod.rs
- FOUND commit: `110b934` (Task 1)
- Build gate: `cargo build --workspace --all-features` exits 0
- Test gate: `tui_rata::status_line` 10 passed, `tui::status_line` 12 passed, `tui::pills` 5 passed
- Classic files: `git diff` output = 0 bytes — D-02 preserved

---
*Phase: 22.4-ratatui-backed-repl-tmon-architecture*
*Completed: 2026-04-24*
