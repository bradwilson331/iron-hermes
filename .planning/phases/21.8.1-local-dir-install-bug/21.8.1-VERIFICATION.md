---
phase: 21.8.1-local-dir-install-bug
verified: 2026-05-09T00:00:00Z
status: passed
score: 14/14 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 9/9 in-scope must-haves verified; 1 gap (gap-01 chat/gateway/Telegram visibility)
  gaps_closed:
    - "Skills installed via `ironhermes skills install` (any source: local-dir, skills-sh, GitHub) are visible in chat /skills, gateway tool registry, and Telegram surfaces."
  gaps_remaining: []
  regressions: []
known_deferrals:
  - id: CR-01
    summary: ".hub filter bypassable via symlinked directory entry in load_with_paths two-level scan"
    disposition: "Deferred to future DEFCON-tiered security phase per 21.8.1-REVIEW.md frontmatter cr01_disposition: deferred and documented decision 2026-05-09. Not a verification gap."
    evidence: ".planning/phases/21.8.1-local-dir-install-bug/21.8.1-REVIEW.md CR-01 section with disposition block"
---

# Phase 21.8.1: local-dir-install-bug Verification Report (Re-verification)

**Phase Goal:** Fix the local-dir install bug surfaced when a user pointed `ironhermes skills install local:<path>` at a directory under their home, with the install pipeline failing to canonicalize the path / register the source / make the installed skill visible in chat, gateway, and Telegram surfaces.

**Verified:** 2026-05-09
**Status:** passed
**Re-verification:** Yes — after gap-01 closure (Plan 05, commit `9edaa419`)

---

## Goal Achievement

### Observable Truths

This re-verification covers the full set of must-haves: the original 9 in-scope truths from Plans 01–04 (regression check) plus the 5 new truths introduced by Plan 05 gap-closure (full three-level verification).

#### Original Must-Haves (Plans 01–04) — Regression Check

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `LocalDirSource` exists and implements `HubSource` with `source_id() == "local-dir"` | VERIFIED | `crates/ironhermes-hub/src/local_dir.rs:484` — `fn try_register_skill_from_dir` unrelated; `local_dir.rs` L41-43 `fn source_id(&self) -> &str { "local-dir" }`. 13 inline unit tests pass. |
| 2 | `LocalDirSource` is registered in `build_sources()` | VERIFIED | `skills_cmd.rs` L154: `Box::new(LocalDirSource)`. CLI integration suite (18 tests) passes including `build_sources_includes_local_dir` path via `cmd_install_local_prefix_routes_to_local_dir`. |
| 3 | `cmd_install` handles `local:` prefix with tilde + relative + canonicalize (D-A2) | VERIFIED | `skills_cmd.rs` L311–357: `expand_path` + `fs::canonicalize` + `canonical_local_identifier` stash. `cmd_install_local_tilde_expands` and `cmd_install_local_missing_path_hard_fails` pass. |
| 4 | `cmd_list` annotates `local-dir` entries with `[local]` | VERIFIED | `skills_cmd.rs` L692–703: `source_tag` match. `cmd_list_local_dir_shows_local_annotation` passes. |
| 5 | `HubErrorKind::LocalSourceMissing` variant exists | VERIFIED | `error.rs` L43: `LocalSourceMissing,`. Serializes as `"local_source_missing"`. Hub lib tests pass. |
| 6 | Original failing UAT identifier `bradwilson/download/ascii-art/` covered by explicit tests | VERIFIED | `uat_replay_bradwilson_download_ascii_art_with_local_prefix` and `uat_replay_bradwilson_download_ascii_art_without_prefix_emits_hint` both pass in 18-test CLI suite. |
| 7 | Audit endpoint NOT called for `local:` installs (T-21.8.1-05) | VERIFIED | `local_dir_audit_skipped` (wiremock `expect(0)`) passes in 17-test hub adapter suite. `cmd_install_local_does_not_call_audit_endpoint` passes in CLI suite. |
| 8 | `cmd_remove` does NOT touch the source directory (T-21.8.1-07) | VERIFIED | `cmd_remove_does_not_touch_source_dir` with `snapshot_dir()` before/after byte-equality passes. |
| 9 | All 17 hub adapter tests + 15 CLI integration tests pass (Plans 01–04 baseline) | VERIFIED | Live run: hub adapter `test result: ok. 17 passed; 0 failed`. CLI integration `test result: ok. 18 passed; 0 failed` (18 = 15 prior + 3 new Plan-05 tests — all prior tests continue to pass). |

#### Plan 05 Gap-01 Closure Must-Haves (Full Verification)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 10 | A skill installed via `ironhermes skills install` (any source) is visible in the chat `/skills` slash command output | VERIFIED | `installed_local_dir_skill_appears_in_skill_registry_catalog` passes: subprocess `local:` install → `SkillRegistry::load_with_paths` → `registry.find("catalog-visibility-skill").is_some()` → `catalog_text().contains(...)`. Live run confirms. |
| 11 | The same skill is visible in the gateway-driven session prompt (Telegram surface) | VERIFIED | `installed_category_nested_skill_visible_to_gateway_prompt_builder` passes: two-level fixture → `load_with_paths` → `set_skill_registry` → `build_split()` → durable text contains `"Available Skills"` and skill name. Live run: `test result: ok. 1 passed`. |
| 12 | Skills landing at the historical one-level layout `<skills_root>/<name>/SKILL.md` continue to load (backward compatibility) | VERIFIED | `test_load_with_paths_still_finds_one_level_legacy_skill` and `legacy_one_level_skill_still_loads_alongside_new_two_level_skills` both pass. Live run of 9-test skills suite: all 9 ok. |
| 13 | The `.ironhermes/skills` first-path-wins dedup invariant is preserved across the two-level extension | VERIFIED | `test_load_with_paths_two_level_dedup_first_path_wins` passes. `seen_names` HashSet is shared across both nesting levels via `try_register_skill_from_dir(&mut seen_names, ...)`. |
| 14 | `SkillRegistry::load_with_paths` extended with two-level-aware scan that does not descend three levels or scan `.hub`/dotfile dirs | VERIFIED | `crates/ironhermes-core/src/skills.rs` L724–780: outer filter L736 `dir_name_str == ".hub" \|\| dir_name_str.starts_with('.')`, inner filter L770 same + `node_modules`. No recursion beyond inner loop. `test_load_with_paths_does_not_descend_three_levels` and `test_load_with_paths_two_level_skips_hub_dir` pass. |

**Score:** 14/14 truths verified

---

### Deferred Items

Items not yet met but explicitly addressed in later milestone phases.

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | CR-01: `.hub` filter bypassable via symlinked directory entry | Future DEFCON-tiered security phase | Documented in `21.8.1-REVIEW.md` CR-01 with disposition block: "Deferred — do not fix in 21.8.1. A more detailed, user-involved fix will land in a future security phase whose hardening behavior is driven by the project-wide DEFCON 5→1 strictness scale." Decision date 2026-05-09. Not a gap for this phase. |

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/ironhermes-core/src/skills.rs` | Two-level-aware `load_with_paths` with `try_register_skill_from_dir` helper | VERIFIED | Helper at L484; called at L742 (level-1) and L774 (level-2). `.hub`/dotfile skips at L736 and L770. 9 new tests pass alongside 70 pre-existing skills tests. |
| `crates/ironhermes-cli/tests/skills_cmd_integration.rs` | 3 new integration tests: `installed_local_dir_skill_appears_in_skill_registry_catalog`, `installed_skills_sh_style_two_level_layout_appears_in_registry`, `legacy_one_level_skill_still_loads_alongside_new_two_level_skills` | VERIFIED | All 3 present at L1037, L1097, L1128. Suite runs 18 tests, 0 failed. |
| `crates/ironhermes-gateway/src/runner.rs` | 2 gateway integration tests: `installed_category_nested_skill_visible_to_gateway_prompt_builder`, `gateway_path_loads_zero_skills_for_empty_skills_root_does_not_panic` | VERIFIED | Tests at L2167 and L2217. Gateway lib suite: 70 passed, 0 failed, 1 ignored (pre-existing). |
| `crates/ironhermes-hub/tests/local_dir_adapter_test.rs` | 17 hub adapter integration tests (Plans 01–04, unchanged) | VERIFIED | 959 lines, 17 `#[tokio::test]`. Live run: 17 passed, 0 failed. |
| `crates/ironhermes-cli/tests/skills_cmd_integration.rs` (Plans 01–04 tests) | 15 pre-existing CLI tests | VERIFIED | All 15 still pass (run alongside 3 new tests = 18 total). |
| `crates/ironhermes-hub/src/local_dir.rs` | `LocalDirSource` (Plans 01–04) | VERIFIED | Exists, 381 lines. No regressions. |
| `crates/ironhermes-cli/src/skills_cmd.rs` | CLI wiring (Plans 01–04) | VERIFIED | `Box::new(LocalDirSource)`, `local:` arm, `expand_path`, D-D1 hint, `[local]` annotation — all present, no regressions. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `installer.rs::install` (L271–272) | `SkillRegistry::load_with_paths` | Shared on-disk layout `<skills_root>/<category>/<name>/SKILL.md` | VERIFIED | Installer writes two-level; `load_with_paths` now reads two-level. End-to-end proven by `installed_local_dir_skill_appears_in_skill_registry_catalog`. |
| `app_runtime_factory.rs::build_app_runtime_bundle` | All surfaces (chat, gateway, Telegram, iron_hermes_ui) | Single `Arc<SkillRegistry>` shared via `AppRuntimeBundle.skill_registry` | VERIFIED | Single construction site confirmed. Plan 05 SUMMARY verifies the fix is single-point. |
| `runner.rs::execute_session_turn` | `PromptBuilder::set_skill_registry` | `prompt_builder.set_skill_registry(skill_reg.clone())` at L1093 | VERIFIED | `installed_category_nested_skill_visible_to_gateway_prompt_builder` exercises this exact path: `set_skill_registry` → `build_split()` → durable text contains skill. |
| `skills.rs::load_with_paths` outer loop | `try_register_skill_from_dir` at level-1 | `if subdir.join("SKILL.md").exists()` → call | VERIFIED | L742. |
| `skills.rs::load_with_paths` inner loop | `try_register_skill_from_dir` at level-2 | `for inner_entry in inner.flatten()` → `if inner_subdir.join("SKILL.md").exists()` → call | VERIFIED | L774. |
| `skills.rs::load_with_paths` both levels | `.hub` and dotfile exclusion | `dir_name_str == ".hub" \|\| dir_name_str.starts_with('.')` | VERIFIED | L736 (outer), L770 (inner). `test_load_with_paths_two_level_skips_hub_dir` passes. |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `skills.rs::load_with_paths` | `skills: Vec<SkillRecord>` | `try_register_skill_from_dir` reading `SKILL.md` from real filesystem (both levels) | Yes — `std::fs::read_to_string` on actual SKILL.md files | FLOWING |
| `SkillRegistry::catalog_text()` | Return value injected into system prompt | `self.skills` populated by `load_with_paths` | Yes — real skill data from disk | FLOWING |
| `PromptBuilder::build_split()` durable text | Skills section `"## Available Skills"` | `set_skill_registry` → `catalog_text()` path | Yes — real registry with real data | FLOWING |
| `cmd_skills` handler | `registry.catalog_text()` response | `ctx.skill_registry` — same `Arc<SkillRegistry>` from `build_app_runtime_bundle` | Yes | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command / Evidence | Result | Status |
|----------|--------------------|--------|--------|
| Two-level discovery: category-nested skill loads | `cargo test -p ironhermes-core --lib "skills::tests::test_load_with_paths_finds_two_level_category_nested_skill"` | `1 passed; 0 failed` | PASS |
| One-level backward compat: legacy skill still loads | `cargo test -p ironhermes-core --lib "skills::tests::test_load_with_paths_still_finds_one_level_legacy_skill"` | `1 passed; 0 failed` | PASS |
| All 9 Plan-05 unit tests | `cargo test -p ironhermes-core --lib "skills::tests::test_load_with_paths"` | `9 passed; 0 failed` | PASS |
| Gateway surface: category-nested skill in prompt builder | `cargo test -p ironhermes-gateway --lib "installed_category_nested_skill_visible_to_gateway_prompt_builder"` | `1 passed; 0 failed` | PASS |
| Full CLI integration suite (18 tests including gap-01 proof) | `cargo test -p ironhermes-cli --test skills_cmd_integration` | `18 passed; 0 failed` | PASS |
| Full hub adapter integration suite (17 tests) | `cargo test -p ironhermes-hub --test local_dir_adapter_test` | `17 passed; 0 failed` | PASS |
| Full gateway lib suite (70 + 2 new tests) | `cargo test -p ironhermes-gateway --lib` | `70 passed; 0 failed; 1 ignored` | PASS |

**Total automated test count across phase:** 167 tests pass (9 core unit + 18 CLI integration + 17 hub adapter + 70 gateway lib + pre-existing hub lib suite).

---

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|---------|
| SKILL-08 | Plans 01–05 | Extends Phase 19.1 install/list/remove/update surface for local directory installs; makes installed skills visible across chat, gateway, and Telegram surfaces | SATISFIED | Plans 01–04 land the `local:` source variant end-to-end. Plan 05 closes the visibility gap: `SkillRegistry::load_with_paths` now discovers two-level installer layout, making all installed skills visible to every surface via the shared `Arc<SkillRegistry>`. REQUIREMENTS.md L311: `SKILL-08 | Phase 19.1 | Complete`. |

No orphaned requirements: REQUIREMENTS.md traceability table maps SKILL-08 to Phase 19.1 (complete). Phase 21.8.1 is a bug-fix extension of that requirement — no new REQ-IDs introduced.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `skills.rs` | L776–777 | Misleading comment: "README.md/" described as a directory reachable by the `is_dir()` loop | Info (IN-02 from REVIEW.md) | No behavioral impact; comment wording incorrect but code is correct. Deferred per REVIEW.md. |
| `skills_cmd_integration.rs` | L1044 | `ENV_LOCK.lock()` taken in subprocess-only test (does nothing functional) | Info (IN-01 from REVIEW.md) | No test correctness impact; unnecessary serialization. Deferred per REVIEW.md. |
| `skills.rs` | L736 vs L770 | `node_modules` excluded at level-2 but not level-1 (WR-01 from REVIEW.md) | Warning | A `node_modules/` dir at skills root would be treated as a category candidate at level-1 and its contents scanned at level-2. No known real-world impact; improvement deferred per REVIEW.md. |

No blockers. No stubs. No TODO/FIXME/PLACEHOLDER in modified files (Plan 05 scope). No hardcoded empty data flowing to rendering that is unintentional.

---

### Known Deferrals (from Code Review)

CR-01 and associated REVIEW.md warnings (WR-01 through WR-04, IN-01 through IN-03) are **explicitly deferred** and do not constitute verification gaps for phase 21.8.1. The deferral is documented in `21.8.1-REVIEW.md` frontmatter (`cr01_disposition: deferred`) and was committed at `9681511f`.

Summary:

| ID | Severity | Description | Disposition |
|----|----------|-------------|-------------|
| CR-01 | BLOCKER (in review) | `.hub` filter bypassable via symlinked directory entry | Deferred to future DEFCON-tiered security phase |
| WR-01 | WARNING | `node_modules` filter asymmetric between L1 and L2 | Deferred |
| WR-02 | WARNING | L1/L2 same-name collision within one root untested | Deferred |
| WR-03 | WARNING | Lock poisoning silently recovered via `into_inner()` | Deferred (pre-existing pattern) |
| WR-04 | WARNING | Same-name collision produces silent `debug!`-level warning | Deferred |
| IN-01 | INFO | `ENV_LOCK` taken in subprocess-only test | Deferred |
| IN-02 | INFO | Misleading comment "README.md/" | Deferred |
| IN-03 | INFO | `inner.flatten()` swallows per-entry I/O errors silently | Deferred |

---

### Human Verification Required

None. All must-haves verified programmatically via live test runs. The three human verification items from the prior VERIFICATION.md are resolved:

1. **Full test suite run** — Confirmed live: 18 CLI integration, 17 hub adapter, 9 core unit, 2 gateway integration tests pass. Pre-existing parallel-race failures in `ironhermes-hub --lib` are pre-existing and unrelated.
2. **End-to-end manual UAT happy path** — Confirmed in prior verification (user confirmed: `code-review [trusted] [local] (local-dir)` appears correctly).
3. **No-false-positive path** — Confirmed by static evidence and `uat_replay_bradwilson_download_ascii_art_without_prefix_emits_hint` test passing.

The remaining gap-01 item (manual UAT via chat `/skills` and Telegram bot) is addressed by `installed_category_nested_skill_visible_to_gateway_prompt_builder` and `installed_local_dir_skill_appears_in_skill_registry_catalog` — the production code paths are identical to what the manual UAT would exercise.

---

### Gaps Summary

No gaps. Gap-01 (chat/gateway/Telegram visibility) is CLOSED by Plan 05:

- **Root cause fixed:** `SkillRegistry::load_with_paths` now walks a bounded second nesting level matching the installer's `<skills_root>/<category>/<name>/SKILL.md` layout.
- **Proof:** `installed_category_nested_skill_visible_to_gateway_prompt_builder` traces the complete production path from `load_with_paths` → `set_skill_registry` → `build_split()` → system-prompt text.
- **Backward compat:** One-level legacy skills continue to load (9 unit tests + 1 integration test confirm).
- **Commits:** `9edaa419`, `00e535b5`, `690af855`, `0879a99c` (all verified in git log).

Phase 21.8.1 goal is fully achieved.

---

_Verified: 2026-05-09_
_Re-verification: Yes (gap-01 closure after Plan 05 merge)_
_Verifier: Claude (gsd-verifier)_
