---
phase: 21.8.1-local-dir-install-bug
verified: 2026-05-08T00:00:00Z
status: gaps_found
score: 9/9 must-haves verified (in scope) — 1 gap surfaced post-install (out of original scope, fold-in via gap-closure)
overrides_applied: 0
gaps:
  - id: gap-01-chat-tool-registry-visibility
    truth: "Skills installed via `ironhermes skills install` (any source: skills-sh, GitHub, local-dir) are visible in chat /tools, gateway tool registry, and Telegram chat surfaces."
    status: failed
    severity: warning
    surfaced_by: human_uat_2026-05-08
    evidence: "Manual UAT after Wave 4: `ironhermes skills list` shows 4 installed skills (advisory-skill, ascii-art, code-review [local], excalidraw). Chat /tools command lists 19 globally-installed `~/.claude/skills/` entries — none of the 4 ironhermes-managed skills appear. Pre-existing across all sources (excalidraw was installed before 21.8.1), so this is upstream of the local-dir feature, but blocks the practical use of local-dir installs."
    test_to_add: "Integration test that installs a skill via cmd_install and confirms it appears in the chat tool registry (and gateway/telegram surfaces). Plus an interactive UAT step in the project's UAT.md."
human_verification:
  - test: "Run cargo test -p ironhermes-hub --lib (with --test-threads=1 for parallel-race tests) && cargo test -p ironhermes-hub --test local_dir_adapter_test && cargo test -p ironhermes-cli --test skills_cmd_integration"
    expected: "Hub lib tests pass serially (4 known parallel-race failures are pre-existing). Hub adapter integration: 17 passed. CLI integration: 15 passed."
    result: pass
    note: "User confirmed test 1 — 4 hub-lib failures are the pre-existing parallel-test isolation race (passes with --test-threads=1; predates 21.8.1)."
  - test: "Run hermes skills install local:/tmp/manual-skill/ end-to-end"
    expected: "Stdout shows 'Resolving local:/tmp/...', 'Discovering', 'Downloading', 'Scanning', 'Installed ... [trusted]', 'Restart the agent'. Exit 0. skills-lock.json written with source: local-dir."
    result: pass
    note: "User confirmed via real run: lock entry shows `code-review [trusted] [local] (local-dir)`. Headline bug fixed. NOTE: chat /tools does not surface this skill — captured as gap-01."
  - test: "Run hermes skills install bradwilson/download/ascii-art/ from a CWD that does NOT contain bradwilson/"
    expected: "Exits 1 with error about GitHub (no hint fires — correct per D-D1 false-positive acceptance). No panic."
    result: blocked
    note: "Blocked by macOS TCC permissions (read_dir Operation not permitted on Downloads/) — cannot exercise the path-probe true case. Test stands as a static-evidence pass (looks_like_local_path_with_probe + has_known_prefix guard verified in code; covered by D-D1 pure-path unit tests in skills_cmd::tests)."
---

# Phase 21.8.1: local-dir-install-bug Verification Report

**Phase Goal:** Fix the bug surfaced in Phase 21.8 post-completion UAT — `hermes skills install <path>` rejects local directory identifiers because the dispatcher routes any identifier containing `/` to GitHubSource. Add a `local:<path>` source variant + `LocalDirSource` adapter (sibling to GitHubSource / WellKnownSkillSource / SkillsShBlobSource) and a pre-dispatch hint for path-shaped non-prefixed identifiers, so `hermes skills install local:~/Downloads/my-skill/` works end-to-end and the original failing identifier `bradwilson/download/ascii-art/` produces a clear "did you mean local:..." hint instead of a confusing GitHub tarball error.
**Verified:** 2026-05-08
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `LocalDirSource` exists and implements `HubSource` trait with `source_id() == "local-dir"` | VERIFIED | `crates/ironhermes-hub/src/local_dir.rs` L37: `pub struct LocalDirSource;`, L41-43: `fn source_id(&self) -> &str { "local-dir" }`. 13 inline unit tests including `local_dir_source_id_is_local_dir`. |
| 2 | `LocalDirSource` is registered in `build_sources()` in `crates/ironhermes-cli/src/skills_cmd.rs` | VERIFIED | `skills_cmd.rs` L154: `Box::new(LocalDirSource), // Phase 21.8.1: stateless unit struct, no config needed`. Returns Vec of 4 sources. |
| 3 | `cmd_install` handles the `local:` prefix arm with tilde + relative + canonicalize expansion | VERIFIED | `skills_cmd.rs` L316-357: strips `"local:"`, calls `expand_path(raw_path)`, `fs::canonicalize(&expanded)`, hard-fails on both with `strip_terminal_escapes`-wrapped errors, checks `canonical.is_dir()`, stashes `canonical_local_identifier`, passes canonical string to `hub_install`. |
| 4 | `cmd_list` annotates `local-dir` source entries | VERIFIED | `skills_cmd.rs` L692-703: `source_tag = match r["source"].as_str().unwrap_or("") { "local-dir" => " [local]", _ => "" }`. Appended to text format output. JSON output carries `"source":"local-dir"` automatically. |
| 5 | `HubErrorKind::LocalSourceMissing` variant exists | VERIFIED | `error.rs` L43: `LocalSourceMissing,` with D-C2 doc-comment. Serializes as `"local_source_missing"` (asserted in `new_variants_serialize_snake_case` test). 8 references in error.rs (grep confirms). |
| 6 | Original failing UAT identifier `bradwilson/download/ascii-art/` is covered by explicit tests | VERIFIED | `skills_cmd_integration.rs` contains both `uat_replay_bradwilson_download_ascii_art_with_local_prefix` (installs successfully via `local:` prefix) and `uat_replay_bradwilson_download_ascii_art_without_prefix_emits_hint` (exit 1, stderr contains "looks like a local path" and `local:bradwilson/download/ascii-art/`). grep confirms 9 occurrences of the verbatim identifier string. |
| 7 | Audit endpoint is NOT called for `local:` installs | VERIFIED | Hub-layer: `local_dir_adapter_test.rs::local_dir_audit_skipped` uses `Mock::given(any()).expect(0).mount(&mock_server).await` + asserts `received_requests().len() == 0`. CLI-layer: `skills_cmd_integration.rs::cmd_install_local_does_not_call_audit_endpoint` runs subprocess without `--skip-audit` and asserts `audit_server.received_requests().await.unwrap_or_default().len() == 0`. |
| 8 | `cmd_remove` does NOT touch the source directory | VERIFIED | `skills_cmd_integration.rs::cmd_remove_does_not_touch_source_dir` takes `snapshot_dir()` before install and after remove, asserts byte-equality with `assert_eq!(before_install, after_remove, "T-21.8.1-07: ...")`. `snapshot_dir()` walker produces `Vec<(String, String)>` of `(rel_path, sha256_hex)` pairs — not just existence but content equality. |
| 9 | All 17 hub adapter tests + 15 CLI integration tests pass | VERIFIED (per SUMMARY) | Plan 04 SUMMARY reports: hub `test result: ok. 17 passed; 0 failed; 0 ignored`, CLI `test result: ok. 15 passed; 0 failed; 0 ignored; finished in 4.47s`. Plan 01/02/03 SUMMARY reports confirm prior waves passed. SUMMARY evidence — cannot independently run without cargo. Flagged for human spot-check. |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/ironhermes-hub/src/local_dir.rs` | `LocalDirSource` implementing `HubSource` | VERIFIED | 381 lines. Has `pub struct LocalDirSource;`, full trait impl, `walk_source_dir`, `parse_skill_name_from_frontmatter`, `typed()` helper, 13 `#[cfg(test)]` tests. |
| `crates/ironhermes-hub/src/lib.rs` | `pub mod local_dir; pub use local_dir::LocalDirSource;` | VERIFIED | L13: `pub mod local_dir;`. L34: `pub use local_dir::LocalDirSource;`. No wildcard re-export. |
| `crates/ironhermes-hub/src/error.rs` | `HubErrorKind::LocalSourceMissing` variant | VERIFIED | L43 in enum. Serializes as `"local_source_missing"`. Tests extended in `tests_21_8` module. |
| `crates/ironhermes-cli/src/skills_cmd.rs` | `build_sources` adds LocalDirSource; `local:` dispatch arm; `cmd_list` annotation; `expand_path`; `looks_like_local_path_with_probe`; D-D1 hint | VERIFIED | L154: `Box::new(LocalDirSource)`. L231: `source == "local-dir"` in `recompute_trust_str`. L265-296: D-D1 hint block with `has_known_prefix` guard. L316-357: `local:` dispatch arm. L692-703: `[local]` annotation. L771-786: `expand_path`. L800-809: `looks_like_local_path_with_probe`. |
| `crates/ironhermes-hub/src/lock.rs` | `lock_entry_with_local_dir_source_round_trips` test | VERIFIED | L267-282: test asserts `source: "local-dir"` and `snapshotHash: ""` round-trip via serde_json. |
| `crates/ironhermes-hub/tests/local_dir_adapter_test.rs` | 17 hub integration tests | VERIFIED | 959 lines, 17 `#[tokio::test]` functions covering D-A2, D-B1, D-B2/D-15, D-C1 (2 tests), D-13, D-C2 (2 tests), T-21.8.1-01, T-21.8.1-02, T-21.8.1-05, and 4 failure modes. |
| `crates/ironhermes-cli/tests/skills_cmd_integration.rs` | 15 CLI integration tests including UAT replay + cmd_remove sacrosanctness | VERIFIED | 1019 lines, 15 tests. Includes `cmd_remove_does_not_touch_source_dir`, `uat_replay_bradwilson_download_ascii_art_with_local_prefix`, `uat_replay_bradwilson_download_ascii_art_without_prefix_emits_hint`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `skills_cmd.rs::build_sources` | `ironhermes_hub::LocalDirSource` | `Box::new(LocalDirSource)` in vec | WIRED | L154, 4th element of returned vec. Import confirmed at L15: `use ironhermes_hub::{..., LocalDirSource, ...}`. |
| `skills_cmd.rs::cmd_install dispatch block` | `LocalDirSource::fetch` | `local:` arm stashes canonical, `hub_install` receives it | WIRED | L316: `} else if identifier.starts_with("local:")`. L352: `canonical_local_identifier = Some(...)`. L396: `let install_identifier = canonical_local_identifier.as_deref().unwrap_or(identifier)`. L397: `hub_install(source, install_identifier, ...)`. |
| `skills_cmd.rs::cmd_install` | `fs::canonicalize + expand_path` | D-A2 in `local:` arm | WIRED | L319: `expand_path(raw_path)`. L326: `std::fs::canonicalize(&expanded)`. Both hard-fail with `strip_terminal_escapes`-wrapped errors. |
| `local_dir.rs::fetch` | `sanitize::strict_yaml_delimiter` | D-17 carry-forward | WIRED | `local_dir.rs` L89: `sanitize::strict_yaml_delimiter(&skill_md_content)?`. grep confirms 4 references to `strict_yaml_delimiter` in `local_dir.rs`. |
| `local_dir.rs::walk_source_dir` | symlink skip + `.git`/`node_modules`/`target` skip | `is_symlink()` + dir name check | WIRED | `local_dir.rs` L143-148: `if file_type.is_symlink() { continue; }` and `if name_str == ".git" || name_str == "node_modules" || name_str == "target" { continue; }`. Both grep-confirmed. |
| `lib.rs` | `ironhermes-cli` (via pub use) | `pub use local_dir::LocalDirSource;` | WIRED | `lib.rs` L34. `skills_cmd.rs` L15 imports it from `ironhermes_hub`. |
| `skills_cmd.rs::cmd_install D-D1 hint` | pre-dispatch early return | `has_known_prefix` guard + `looks_like_local_path_with_probe` | WIRED | L279-296. Guard includes `local:` to prevent hint firing on the `local:` dispatch arm. `looks_like_local_path_with_probe` closure uses `fs::metadata`. |
| `skills_cmd.rs::cmd_list_impl` | `[local]` annotation in text output | `source_tag` match on `"local-dir"` | WIRED | L692-703. `recompute_trust_str` returns `"trusted"` for `"local-dir"` (L231-232). JSON carries `"source":"local-dir"` automatically via serde. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `local_dir.rs::fetch` | `files: Vec<(String, Vec<u8>)>` | `walk_source_dir` reads real filesystem | Yes — `std::fs::read` on each file | FLOWING |
| `local_dir.rs::fetch` | `skill_md_content` | `files` vec entry for SKILL.md | Yes — bytes from real file | FLOWING |
| `local_dir.rs::fetch` | `SkillBundle { snapshot_hash: None }` | Hardcoded `None` | Intentional per D-C1 — no remote snapshot | FLOWING (by design) |
| `skills_cmd.rs::cmd_list_impl` | `items` / `lock.skills` | `SkillLock::load_or_default()` reads `skills-lock.json` | Yes — real DB equivalent (JSON file) | FLOWING |
| `skills_cmd.rs::cmd_install` | `canonical_local_identifier` | `fs::canonicalize` of user-supplied path | Yes — real filesystem resolution | FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED for automated checks — requires running `cargo build` and binary execution. Delegated to human verification item 1 above.

However, static verification of behavior shapes:

| Behavior | Evidence | Status |
|----------|----------|--------|
| `local:` identifier routes to `LocalDirSource` (not GitHub) | `starts_with("local:")` arm at L316 precedes `contains('/')` GitHub arm at L358. Dispatch order: well-known(L303) → skills-sh(L310) → local(L316) → contains('/')(L358) → bare. | PASS (static) |
| D-D1 hint exits 1 with correct message | `eprintln!("error: {} looks like a local path — did you mean 'hermes skills install local:{}'?", ...)` at L290-294. Returns `Ok(1)` at L295. | PASS (static) |
| D-21 line 1 suppressed for `local:` | `if !identifier.starts_with("local:")` guard at L265 around the generic "Resolving skills.sh/..." print. Local arm emits its own "Resolving local:..." at L347. | PASS (static) |
| Audit skip automatic for local installs | Canonical absolute path produces `""` from `extract_owner_repo` (verified in RESEARCH.md installer.rs:154-174). No CLI-layer audit call added for local arm. | PASS (static) |
| `cmd_remove` source dir untouched | `cmd_remove` at L620-638 calls only `hub_uninstall(name)` which deletes from `skills_root` and lock. No path from the lock `identifier` field is touched. | PASS (static) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| SKILL-08 | Plans 01-04 | Extends Phase 19.1 install/list/remove/update surface for local directory installs | SATISFIED | All 4 plan waves land the `local:` source variant: Plan 01 (error variant + hint + lock forward-compat), Plan 02 (LocalDirSource adapter), Plan 03 (CLI wiring), Plan 04 (integration test closure). |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `local_dir.rs` | L111 | `snapshot_hash: None` | Info | Intentional per D-C1 — local installs have no remote snapshot. Not a stub; the value is correct by design and tested. |
| `local_dir.rs` | L103 | `metadata: serde_json::json!({})` | Info | Empty metadata object. Intentional — local dirs have no hub metadata. Downstream consumers handle empty metadata gracefully. |

No blockers. No stubs. No TODO/FIXME/PLACEHOLDER found in modified files. No hardcoded empty data flowing to rendering that is unintentional.

### Human Verification Required

#### 1. Full test suite run

**Test:** From the repo root, run `cargo test -p ironhermes-hub --lib && cargo test -p ironhermes-hub --test local_dir_adapter_test && cargo test -p ironhermes-cli --test skills_cmd_integration`
**Expected:** Hub lib tests pass (includes 13 local_dir unit tests + 4 error tests + 1 lock test). Hub adapter integration: `test result: ok. 17 passed; 0 failed`. CLI integration: `test result: ok. 15 passed; 0 failed`. Pre-existing failures (inv_25_1_gap8, chat_memory_persistence, ironhermes-tools-api) do not expand in count.
**Why human:** Requires `cargo build` of the full workspace and binary compilation. Cannot run test harness programmatically from this context.

#### 2. End-to-end manual UAT — happy path

**Test:** `mkdir -p /tmp/manual-skill && echo $'---\nname: manual\n---' > /tmp/manual-skill/SKILL.md && cargo build --bin ironhermes && ./target/debug/ironhermes skills install local:/tmp/manual-skill/`
**Expected:** Stdout shows exactly `Resolving local:/tmp/manual-skill/...` (not `Resolving skills.sh/local:...`), then Discovering, Downloading, Scanning, `Installed 'manual' [trusted] — hash: ...`, `Restart the agent...`. Exit code 0. `$HERMES_HOME/skills-lock.json` written with `"source":"local-dir"`.
**Why human:** D-21 5-line progress shape and D-23 restart message can only be visually confirmed in a real terminal run.

#### 3. End-to-end manual UAT — no-false-positive path

**Test:** From a directory that does NOT contain a `bradwilson/` subdirectory, run `./target/debug/ironhermes skills install bradwilson/download/ascii-art/`
**Expected:** Exits with a GitHub-related error (not the D-D1 hint) — because the CWD has no `bradwilson/` dir, the path probe returns false, and the identifier falls through to the `contains('/')` GitHub arm. No panic.
**Why human:** Requires a specific CWD state and binary execution to confirm the false-positive guard works correctly in the full dispatch chain.

### Gaps Summary

**1 gap surfaced** by post-merge human UAT — promoted to status: gaps_found for follow-up:

- **gap-01: chat tool-registry visibility** — Skills installed via `ironhermes skills install` (any source) do not appear in chat `/tools`, gateway tool registry, or Telegram chat surfaces. Pre-existing across all sources (excalidraw, ascii-art, advisory-skill, code-review) so the gap is upstream of 21.8.1, but blocks practical end-to-end use of local-dir installs. Slated for gap-closure phase via `/gsd-plan-phase 21.8.1 --gaps`.

All 9 in-scope observable truths remain VERIFIED. The headline bug (local-dir install pipeline) is fixed and confirmed by user UAT. Test 3 (no-false-positive path) blocked by macOS TCC; static evidence from `skills_cmd.rs::looks_like_local_path_with_probe` + unit tests stands in lieu.

---

_Verified: 2026-05-08_
_Updated: 2026-05-09 (post-UAT, gap-01 added)_
_Verifier: Claude (gsd-verifier)_
