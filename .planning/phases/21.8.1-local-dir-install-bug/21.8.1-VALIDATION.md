---
phase: 21.8.1
slug: local-dir-install-bug
status: ready
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-08
---

# Phase 21.8.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `21.8.1-RESEARCH.md` § Validation Architecture (canonical sampling rate, aliasing risks, coverage matrix, failure-mode tests).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (Rust 2024) |
| **Config file** | `Cargo.toml` (workspace), per-crate `tests/` directories |
| **Quick run command** | `cargo test -p ironhermes-hub local_dir` |
| **Full suite command** | `cargo test --workspace` |
| **Estimated runtime** | ~30 s quick · ~3-5 min full |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-hub local_dir` (per RESEARCH.md sampling-rate floor: every prefix variant `/`, `./`, `../`, `~/`, plus a bare existing-dir path; that is the Nyquist floor for D-D1).
- **After every plan wave:** Run `cargo test --workspace`
- **Before `/gsd-verify-work`:** Full suite must be green AND `cargo clippy --workspace -- -D warnings` clean
- **Max feedback latency:** ~30 s for quick run

---

## Per-Task Verification Map

> Filled by Plan 04 executor after all 4 plans completed. Every task that introduces, modifies, or wires the local-dir source appears with its `<automated>` command. See coverage matrix in `21.8.1-RESEARCH.md` for the D-A1..D-D1 → test-row mapping.

| Task ID | Plan | Wave | Decision | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|----------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-T1 | 01 | 1 | (foundational) | (none — variant only) | LocalSourceMissing variant exists, serializes correctly, distinct from Io | unit | `cargo test -p ironhermes-hub --lib error::` | ✅ | ✅ |
| 01-T2 | 01 | 1 | D-D1, D-B2 (recompute_trust_str arm) | T-21.8.1-04, T-21.8.1-06 | Hint fires for 5 path shapes; recompute_trust_str returns "trusted" for local-dir | integration + unit | `cargo test -p ironhermes-cli recompute_trust_str_returns_trusted_for_local_dir local_dir_hint` | ✅ | ✅ |
| 01-T3 | 01 | 1 | D-C1 (forward-compat) | (none — schema test) | Lock entry with source: "local-dir" round-trips through serde | unit | `cargo test -p ironhermes-hub lock_entry_with_local_dir_source_round_trips` | ✅ | ✅ |
| 02-T1 | 02 | 2 | D-A2, D-B1, D-B2, D-C1, D-C2 (adapter) | T-21.8.1-01, T-21.8.1-02, T-21.8.1-03 | LocalDirSource adapter: walk, scan, frontmatter, error variants — 13 unit tests | unit | `cargo test -p ironhermes-hub --lib local_dir` | ✅ | ✅ |
| 02-T2 | 02 | 2 | (re-export plumbing) | (none) | LocalDirSource is publicly reachable via ironhermes_hub::LocalDirSource | build | `cargo build -p ironhermes-hub` | ✅ | ✅ |
| 03-T1 | 03 | 3 | D-A1, D-A2, D-B1, D-C1, D-C2 (CLI dispatch) | T-21.8.1-04, T-21.8.1-05 | cmd_install local: arm, expand_path, canonicalize, cmd_update generalization, audit-skip, cmd_list [local] annotation — 8 integration tests | integration | `cargo test -p ironhermes-cli --test skills_cmd_integration cmd_install_local cmd_update_local cmd_list_local` | ✅ | ✅ |
| 04-T1 | 04 | 4 | All adapter+pipeline decisions (D-A2, D-B1, D-B2, D-C1, D-C2) | T-21.8.1-01, T-21.8.1-02, T-21.8.1-03, T-21.8.1-05 | End-to-end pipeline: install → quarantine → scan → atomic_move → lock; 17 failure-mode integration tests | integration | `cargo test -p ironhermes-hub --test local_dir_adapter_test` | ✅ | ✅ |
| 04-T2 | 04 | 4 | D-D1 hint, cmd_list annotation, cmd_remove sacrosanctness, UAT replay | T-21.8.1-07 | cmd_remove cannot touch source dir (byte-snapshot equality); UAT identifier `bradwilson/download/ascii-art/` works with prefix, hint fires without prefix — 15 total CLI integration tests | integration | `cargo test -p ironhermes-cli --test skills_cmd_integration` | ✅ | ✅ |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Wave 0 landed pure-additive surface (error variant, trust mapping, hint, forward-compat test) before any callsite mutation. Per RESEARCH.md §Wave Ordering.

- [x] `crates/ironhermes-hub/src/error.rs` — `HubErrorKind::LocalSourceMissing` variant (Plan 01-T1)
- [x] `crates/ironhermes-cli/src/skills_cmd.rs::recompute_trust_str` — `"local-dir" => "trusted"` arm (Plan 01-T2)
- [x] `crates/ironhermes-cli/src/skills_cmd.rs::cmd_install` — pre-dispatch hint (D-D1) (Plan 01-T2)
- [x] `crates/ironhermes-hub/src/lock.rs` round-trip test for `source: "local-dir"` proving serde handles unknown source variant (Plan 01-T3)

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| End-to-end install of a real user-authored skill from a personal Downloads dir, then restart and confirm activation. | D-B1, D-23 (next-session activation carry-forward) | Cross-session activation requires process restart; not a unit-test surface. | (1) `mkdir -p /tmp/manual-skill && echo $'---\nname: manual\n---' > /tmp/manual-skill/SKILL.md`; (2) `hermes skills install local:/tmp/manual-skill/`; (3) verify D-21 5-line progress; (4) restart shell; (5) `hermes skills list` shows entry as `[trusted] [local]`; (6) drift check: edit `/tmp/manual-skill/SKILL.md`, run `hermes skills list` — expect "drifted"; (7) `hermes skills update manual` — drift clears. |
| Pre-dispatch hint UX on the original failing input. | D-D1 | Validates the headline bug fix matches the user-facing string contract. | (1) `hermes skills install bradwilson/download/ascii-art/` (the failing identifier from 21.8 UAT) — expect single hint line `error: looks like a local path — did you mean 'hermes skills install local:bradwilson/download/ascii-art/'?` and exit 1, NO GitHub fetch attempted. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 30 s for quick run
- [x] Coverage matrix from RESEARCH.md is fully reflected (every D-A1..D-D1 in at least one test row)
- [x] Failure-mode tests present: source dir missing at update, symlink-loop source, malicious SKILL.md (WARN posture asserted, not just "didn't reject"), source is regular file not dir, perms denied
- [x] `nyquist_compliant: true` set in frontmatter after planner & checker pass

**Approval:** planner-approved (Plan 04 executor verified all automated commands green)
