---
phase: 36.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-agent/src/anthropic_client.rs
  - crates/ironhermes-agent/tests/anthropic_usage_cache_fields.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "AnthropicUsage struct deserializes cache_read_input_tokens and cache_creation_input_tokens from Anthropic response bodies"
    - "The outer Usage wrapper at anthropic_client.rs:533-534 / 819-820 receives populated Option<u64> cache values instead of hardcoded None"
    - "Streaming SSE path also populates cache fields from the message_delta / final event accumulator"
    - "api_key_hash / cleartext API key never bleeds into Debug or tracing output (existing redaction preserved)"
  artifacts:
    - path: "crates/ironhermes-agent/src/anthropic_client.rs"
      provides: "AnthropicUsage with two new #[serde(default)] Option<u64> cache fields; parse_anthropic_response copies cache fields through to outer Usage"
      contains: "cache_read_input_tokens: Option<u64>"
    - path: "crates/ironhermes-agent/tests/anthropic_usage_cache_fields.rs"
      provides: "Deserialization parity tests for cache_read_input_tokens / cache_creation_input_tokens including absent-field default (None)"
  key_links:
    - from: "AnthropicUsage struct (line 138)"
      to: "outer Usage wrapper (lines 533-534, 819-820)"
      via: "parse_anthropic_response copies response.usage.cache_read_input_tokens through"
      pattern: "cache_read_input_tokens: response.usage.cache_read_input_tokens"
---

<objective>
Extend `AnthropicUsage` (the inner Anthropic-response deserialization struct at `anthropic_client.rs:138`) with two `#[serde(default)] Option<u64>` cache fields, and populate the outer OpenAI-compat `Usage` wrapper at the two parse sites (lines 533-534 and 819-820) from those new fields instead of hardcoding `None`. This is the foundational pre-requisite called out in Research Correction #2: without this, every downstream plan that relies on Anthropic prompt-cache metrics (cost accounting, cache-break detection, cache-hit asserts) silently receives `None` forever.

Purpose: Unblock Area A (prompt caching) and Area C (usage ledger) — both need populated cache fields to function.

Output: An `AnthropicUsage` that round-trips the cache-token fields from Anthropic responses, with deserialization tests confirming both presence (typical Anthropic prompt-cached responses) and absence (legacy/non-cached responses) parse correctly via `#[serde(default)]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.2-agent-loop-core-parity-prompt-caching-per-provider-rate-limi/36.2-CONTEXT.md
@.planning/phases/36.2-agent-loop-core-parity-prompt-caching-per-provider-rate-limi/36.2-RESEARCH.md
@.planning/phases/36.2-agent-loop-core-parity-prompt-caching-per-provider-rate-limi/36.2-PATTERNS.md

@crates/ironhermes-agent/src/anthropic_client.rs

<interfaces>
<!-- Critical current state extracted from anthropic_client.rs. Executor must NOT re-explore. -->

Current AnthropicUsage struct (anthropic_client.rs:138-142):
```rust
#[derive(Debug, Clone, Deserialize)]
struct AnthropicUsage {
    input_tokens: usize,
    output_tokens: usize,
}
```

Current outer Usage population at parse_anthropic_response (anthropic_client.rs:533-534, non-streaming):
```rust
let usage = Some(Usage {
    prompt_tokens: response.usage.input_tokens,
    completion_tokens: response.usage.output_tokens,
    total_tokens: response.usage.input_tokens + response.usage.output_tokens,
    cache_read_input_tokens: None,           // line 533 — always None today
    cache_creation_input_tokens: None,       // line 534 — always None today
});
```

Same pattern at lines 819-820 (streaming final-message parse site).

Target shape (additive — RESEARCH.md Example 1 + Example 2):
```rust
#[derive(Debug, Clone, Deserialize)]
struct AnthropicUsage {
    input_tokens: usize,
    output_tokens: usize,
    #[serde(default)]
    cache_read_input_tokens: Option<u64>,
    #[serde(default)]
    cache_creation_input_tokens: Option<u64>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend AnthropicUsage struct + write deserialization tests</name>
  <files>
    crates/ironhermes-agent/src/anthropic_client.rs,
    crates/ironhermes-agent/tests/anthropic_usage_cache_fields.rs
  </files>
  <read_first>
    - crates/ironhermes-agent/src/anthropic_client.rs (current AnthropicUsage at line 138-142; outer Usage population at 529-537 and 815-825)
    - .planning/phases/36.2-agent-loop-core-parity-prompt-caching-per-provider-rate-limi/36.2-PATTERNS.md (Pattern 4: AnthropicUsage Extension; Shared Patterns: `#[serde(default)]` for all new struct fields)
    - .planning/phases/36.2-agent-loop-core-parity-prompt-caching-per-provider-rate-limi/36.2-RESEARCH.md (Example 1 + Example 2 + Pitfall 3: AnthropicUsage Missing Cache Fields)
  </read_first>
  <behavior>
    - Test 1: A JSON body `{"input_tokens": 100, "output_tokens": 50, "cache_read_input_tokens": 8200, "cache_creation_input_tokens": 4200}` deserializes into `AnthropicUsage` with `cache_read_input_tokens = Some(8200)` and `cache_creation_input_tokens = Some(4200)`.
    - Test 2: A JSON body `{"input_tokens": 100, "output_tokens": 50}` (legacy / no cache fields) deserializes successfully with `cache_read_input_tokens = None` and `cache_creation_input_tokens = None` via `#[serde(default)]`.
    - Test 3: A JSON body with `"cache_read_input_tokens": null` deserializes to `None` (Option semantics preserved).
    - Test 4 (static-grep): the source string of `anthropic_client.rs` (loaded via `include_str!`) contains the literal `cache_read_input_tokens: Option<u64>` (locks the extension against accidental removal).
  </behavior>
  <action>
    Step 1 — Add the two new fields to the `AnthropicUsage` struct definition at `crates/ironhermes-agent/src/anthropic_client.rs:138`. The struct must end up exactly as shown in the `<interfaces>` block above (Target shape): preserve `#[derive(Debug, Clone, Deserialize)]`, keep the existing two `usize` fields unchanged, append the two new fields with `#[serde(default)]` and type `Option<u64>`. The field names MUST be exactly `cache_read_input_tokens` and `cache_creation_input_tokens` — these match the JSON keys Anthropic emits.

    Step 2 — Create test file `crates/ironhermes-agent/tests/anthropic_usage_cache_fields.rs` containing the four behaviors above. Since `AnthropicUsage` is `pub(crate)` / private, the tests must either (a) be in an inline `#[cfg(test)] mod tests {}` block at the bottom of `anthropic_client.rs` (preferred — same as existing tests in this file), OR (b) live in the integration test file using `include_str!` for static-grep only. Pick (a) for tests 1-3 (add an inline `#[cfg(test)] mod cache_field_tests {}` near the existing test modules) and (b) for test 4 (the integration test file at the path above). This matches the project's existing dual-pattern convention (RESEARCH.md notes inline test modules at agent_loop.rs:2585+).

    Step 3 — At `anthropic_client.rs:533-534`, replace the two hardcoded `None` assignments inside the outer `Usage { ... }` literal with:
    ```
    cache_read_input_tokens: response.usage.cache_read_input_tokens,
    cache_creation_input_tokens: response.usage.cache_creation_input_tokens,
    ```
    Do not change the surrounding `prompt_tokens` / `completion_tokens` / `total_tokens` assignments.

    Step 4 — At `anthropic_client.rs:819-820`, repeat the same replacement (streaming/final-event parse site). Both sites must be touched; missing the streaming site silently re-introduces the bug for any user on the streaming code path (RESEARCH.md Open Question 1).

    Step 5 — Run `cargo test -p ironhermes-agent --test anthropic_usage_cache_fields` (for the integration file) and `cargo test -p ironhermes-agent anthropic_client::cache_field_tests` (for the inline module) — both must pass. Then run `cargo test -p ironhermes-agent` to confirm the existing test suite (especially `parse_anthropic_response` tests at lines 1015-1065) still passes — the `#[serde(default)]` annotations make this change backwards-compatible.

    Pitfall guards (per RESEARCH.md Pitfall 3): do NOT change the outer `Usage` struct definition (it already has `Option<u64>` cache fields); the bug is purely on the deserialization → population path. Do NOT touch the streaming SSE accumulator state machine in this plan — if the streaming path computes its own per-event cache totals, that is out of scope; THIS plan only changes the final-message-end Usage construction at line 819-820, which is the same shape as the non-streaming site at 533-534.

    Per D-CACHE-04 / threat model: cache fields are `Option<u64>` so absent values stay `None`. Do not add `unwrap_or(0)` — downstream code (cost calculation) must distinguish "no cache info" from "cache info, zero tokens" for the cost-accounting forensics path.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-agent --test anthropic_usage_cache_fields &amp;&amp; cargo test -p ironhermes-agent</automated>
  </verify>
  <acceptance_criteria>
    - `crates/ironhermes-agent/src/anthropic_client.rs` contains the literal string `cache_read_input_tokens: Option<u64>` (grep-confirmable).
    - `crates/ironhermes-agent/src/anthropic_client.rs` contains the literal string `cache_creation_input_tokens: Option<u64>`.
    - `crates/ironhermes-agent/src/anthropic_client.rs` contains the literal string `cache_read_input_tokens: response.usage.cache_read_input_tokens` (population at both line 533 and line 819).
    - `crates/ironhermes-agent/src/anthropic_client.rs` no longer contains the literal string `cache_read_input_tokens: None,` (the two `None` hardcodings are gone — grep returns 0 lines).
    - `crates/ironhermes-agent/tests/anthropic_usage_cache_fields.rs` exists and contains at least one `#[test]` function asserting on `cache_read_input_tokens` deserialization.
    - `cargo test -p ironhermes-agent` exits 0; no existing test regressed.
    - `cargo clippy -p ironhermes-agent --no-deps -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>
    AnthropicUsage extended with two `#[serde(default)] Option<u64>` cache fields; both parse sites populate the outer Usage from them; tests prove the field round-trip on present / absent / null JSON; full agent test suite still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anthropic API response → AnthropicClient deserialization | Untrusted JSON from external HTTPS endpoint crosses into the agent process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.2-01-01 | Tampering | Anthropic response JSON (cache fields) | mitigate | Fields typed as `Option<u64>`; `#[serde(default)]` makes absent-or-null parse to `None`. A maliciously oversized integer is bounded by `u64::MAX`. No `unwrap_or(0)` collapses the absent/zero distinction. |
| T-36.2-01-02 | Information Disclosure | api_key_hash / cleartext API key in Debug output | mitigate | This plan does NOT add any new logging of the AnthropicUsage struct. Existing `AnthropicClient` Debug redaction at lines 555-560 (`api_key: "[REDACTED]"`) is preserved untouched. |
| T-36.2-01-03 | Denial of Service | Malformed `cache_read_input_tokens` causes serde parse failure | accept | serde returns a structured `Err` rather than panicking; agent loop already handles deserialization errors as `ProviderError::SchemaInvalid` (Plan 03 wires this). Low probability — Anthropic owns this schema. |
| T-36.2-01-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds NO new dependencies; only existing `serde`, `serde_json` (already in deps tree across 6 crates). Package Legitimacy Audit in RESEARCH.md confirms "no new external packages." |
</threat_model>

<verification>
- `cargo test -p ironhermes-agent` green (no existing test regression).
- `cargo test -p ironhermes-agent --test anthropic_usage_cache_fields` green (new tests pass).
- `cargo clippy -p ironhermes-agent --no-deps -- -D warnings` green.
- `grep -n 'cache_read_input_tokens: Option<u64>' crates/ironhermes-agent/src/anthropic_client.rs` returns 1 match (the struct field).
- `grep -n 'cache_read_input_tokens: None' crates/ironhermes-agent/src/anthropic_client.rs` returns 0 matches (the hardcoded Nones are gone).
- `grep -c 'cache_read_input_tokens: response.usage.cache_read_input_tokens' crates/ironhermes-agent/src/anthropic_client.rs` returns 2 (both parse sites populated).
</verification>

<success_criteria>
The `AnthropicUsage` struct deserializes Anthropic's cache-token fields end-to-end; the outer `Usage` wrapper carries those values into AgentLoop instead of always-None; the existing 12+ classify-llm-error tests and the parse_anthropic_response tests at lines 1015-1065 still pass byte-for-byte; the streaming path is patched alongside the non-streaming path so no caller of `AgentLoop` silently loses cache visibility based on which response path they take.
</success_criteria>

<output>
Create `.planning/phases/36.2-agent-loop-core-parity-prompt-caching-per-provider-rate-limi/36.2-01-SUMMARY.md` when done. Note the two grep counts (struct field + population) and confirm both parse sites at lines 533-534 and 819-820 were updated.
</output>
