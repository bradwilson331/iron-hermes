---
context: phase
phase: 36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update
task: 5
total_tasks: 5
status: phase-closed
last_updated: 2026-06-02T18:00:00.000Z
closure_note: Closed approved with carve-outs. UAT 9 PASS / 1 FAIL / 3 DEFERRED. Follow-on candidates filed in 36.3.7.11-05-SUMMARY.md UAT Execution Results section. This file kept as historical artifact only — do NOT resume against it; phase is complete.
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure
> during the Phase 36.3.7.11 UAT pass. Acknowledge each one explicitly before
> proceeding.

- [ ] CONSTRAINT: Plan 05 UAT must re-execute against the live `dx serve --features server` session AFTER both BUG-1 and BUG-2 fixes land — workspace test gate alone does not catch visual contrast or archived-fetch gaps.
- [ ] CONSTRAINT: BUG-1 fix MUST land an integration test that asserts toggling `archived_visible` causes an archived task to render in column 7. The existing test surface (`tests/kanban_board_read.rs` + `tests/kanban_full_suite.rs`) fixtured this out — that is the test_gap that allowed the bug to ship.
- [ ] CONSTRAINT: The orchestrator that ran Plan 05 wrote `~/.hermes/kanban.db` and `kanban promote` into the UAT.md — both nonexistent. Any future executor that authors a UAT script MUST cite the actual CLI surface via `cargo run -p ironhermes-cli -- kanban <verb> --help` before writing prereq blocks.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| executor-writes-nonexistent-CLI-verbs | Plan 05 executor authored a UAT.md referencing `kanban promote` (does not exist) and `~/.hermes/kanban.db` (actual is `~/.ironhermes/kanban.db`) and `--body` on `kanban comment` (positional). Tester surfaced each separately, three orchestrator fix commits required. | blocking | When any phase plan authors a script that invokes external CLIs, the executor MUST source verbs and flags from `<cli> <subcommand> --help` and quote them in the SUMMARY's `Verification` section — not from memory or context. |
| executor-bails-before-completion-signals | Plan 05 executor spawned `cargo build/test/clippy` as background tasks and returned control before the notifications arrived; no SUMMARY.md was written. Orchestrator had to run the gates manually and author SUMMARY.md in a follow-up commit. | advisory | The executor template's success_criteria should explicitly say "do NOT return until the gate scripts have completed and their exit codes are observed; SUMMARY.md must be written after the last gate exit code is known." |
| visual-defects-evade-workspace-gate | BUG-1 (archived fetch) and BUG-2 (drawer/modal transparency) both passed `cargo build && cargo test && cargo clippy -D warnings`. Plan 05's aggregating integration tests assert call-site shape, not rendered pixels. | advisory | Where a phase ships UI surfaces, the cross-plan aggregating test must include a fixture-based render assertion for at least one non-empty data path per visible column AND one screenshot/diff-snapshot of each modal/drawer state. Until that infra exists, manual UAT is the only check — the phase cannot close without it. |

<current_state>
All 5 plans for Phase 36.3.7.11 (Dashboard Kanban tab in iron_hermes_ui's HermesApp) landed on develop. Workspace gate green
(cargo build + test + clippy --workspace --features iron_hermes_ui/server -- -D warnings). HEAD: 99d96ff9 on develop. No
outstanding worktrees.

Manual UAT executed by tester through U8; two real defects surfaced:

1. BUG-1: ARCHIVED column is always empty even when "SHOW ARCHIVED" toggle is on, because `fetch_board` hardcodes
   `ListFilters { archived: false, .. }` and never accepts an `include_archived` parameter.
2. BUG-2: Detail drawer and 4 modals (Complete, Block, ArchiveConfirm, CreateTask) render with insufficient background
   contrast — underlying board bleeds through, form labels/inputs hard to read. Reference: Settings screen UI PREFERENCES
   card style (cyan border + opaque tinted dark fill) is the target visual.

Phase cannot close until both fixes land and UAT re-runs clean. Tester suggested gap-closure phase or per-plan reopen.
</current_state>

<completed_work>

Completed Plans:
- Plan 36.3.7.11-01: read-side `#[server]` fns + WS tail consumer + ScreenKanban board (merged at `1794758e` with 6-file
  conflict resolution against Plan 04 placeholder)
- Plan 36.3.7.11-02: drag-and-drop + 4 write `#[server]` fns + shared transition validator (merged at `7e5d47a6`)
- Plan 36.3.7.11-03: detail drawer + 4 modals + TRIAGE card decompose/specify buttons (merged at `73718f9f`)
- Plan 36.3.7.11-04: WheelWedge::Kanban 11-variant + Agents-page KANBAN BOARD → button (merged at `511485f0`)
- Plan 36.3.7.11-05 Task 1: aggregating integration tests `kanban_full_suite.rs` + `kanban_ws_lifecycle.rs` (`61696079`)
- Plan 36.3.7.11-05 Task 3: workspace gate verified (orchestrator-run)

UAT progress (8 rows attempted of 14):
- ✅ U1 — wheel + Agents navigation
- ❌ U2 — drawer transparency
- ✅ U3 — read path / no REST mount
- ✅ U4 — drag happy path
- ✅ U5 — drag-disallowed feedback
- ❌ U6 — blocked by U2 (Complete modal unreadable)
- ⏸ U7 — blocked by U2
- ❌ U8 — ARCHIVED column always empty
- ⏸ U9-U14 — not reached
</completed_work>

<remaining_work>

Code:
- BUG-1: `crates/iron_hermes_ui/src/server/kanban_api.rs:55-58` — add `include_archived: bool` parameter to fetch_board,
  thread it through ListFilters. Update ScreenKanban (`crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs`)
  to re-fetch when `archived_visible` toggles on.
- BUG-2: `crates/iron_hermes_ui/assets/kanban.css` drawer + modal rules — apply Settings card visual treatment
  (var(--accent-primary) border, opaque dark fill, z-index separation). Reference: `crates/iron_hermes_ui/src/components/hermes_app/screens/settings.rs`
  + the `.card` / `.panel` rules in `assets/main.css` (or wherever the canonical card CSS lives).

Tests:
- Add regression test to `tests/kanban_board_read.rs` asserting: archived task in DB + fetch_board(include_archived=true)
  returns it; fetch_board(include_archived=false) does not.
- Update `tests/kanban_full_suite.rs` to cover the toggle path end-to-end (or add a new `tests/kanban_archived_toggle.rs`).

Process:
- Decide remediation route: gap-closure phase 36.3.7.11.1 (recommended) vs. /gsd-execute-phase --gaps-only inline.
- Re-execute U1-U14 after fixes; confirm U2/U6/U7/U8 pass + no regression on U1/U3/U4/U5.
- `gsd-sdk query roadmap.update-plan-progress 36.3.7.11 36.3.7.11-05 complete` after UAT signs off.
- Final phase verification + roadmap close.
</remaining_work>

<decisions_made>

- Wave 2 ran sequentially (not parallel) because Plans 02 and 03 share screens/kanban.rs, card.rs, column.rs, kanban.css.
- Plan 04 shipped before Plan 01 in Wave 1 — Plan 04 introduces Screen::Kanban placeholder, Plan 01 replaces only the body.
- MODULE label set to 14 (not Plan 04's 11 or Plan 01's 10) — both originals collided with existing screens (office.rs=11, gateway.rs=10).
- UAT.md fix passes used `--idempotency-key` to prevent the seed prereqs from accumulating duplicate cards across iterations.
</decisions_made>

<blockers>
- BUG-1 (fetch_board archived filter): blocks U8 + ARCHIVED column visibility — required code fix
- BUG-2 (drawer/modal transparency): blocks U2/U6/U7 + visual validation of all modal flows — required CSS fix
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured machine-readable handoff state (mirrors this file).
2. `.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-CONTEXT.md` — locked D-01..D-23 decisions; the visual contract is in D-01/D-04 and the drawer scope in D-20.
3. `.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-UAT.md` — the 14-row UAT script (post three fix commits).
4. `.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-05-SUMMARY.md` — Plan 05 closure + the deviations table.
5. `crates/iron_hermes_ui/CLAUDE.md` — Dioxus 0.7.1 conventions; signal-borrow rules are clippy-enforced.

## Infrastructure State

- develop HEAD: `99d96ff9 docs(36.3.7.11-05): UAT seed uses --idempotency-key to prevent duplicate cards`
- No outstanding git worktrees (`git worktree list` shows only the primary).
- DB path: `~/.ironhermes/kanban.db` (NOT `~/.hermes/kanban.db` — UAT.md previously had this wrong; fixed in `a98bd242`).
- Tester DB has 12 task rows including 8 UAT-named cards from the debug iterations (`kanban list --json | jq -r '.[] | "\(.id) | \(.status) | \(.title)"'` to enumerate).
- OMC plugin state files (`.omc/project-memory.json`, `.omc/state/mission-state.json`) and a handful of `.DS_Store` are dirty in the working tree — non-blocking, OMC churn only.

## Pre-Execution Critique Required

NOT APPLICABLE — design is locked; the gap is implementation defects discovered at UAT.

<context>
Phase 36.3.7.11 delivers a Kanban tab inside HermesApp with read + write surfaces (D-05 `#[server]` fns only — no parallel
REST mount), HTML5 native drag-and-drop (D-06), optimistic local-signal update before spawn (D-07), WS tail consumer
independent of the gateway notifier (D-15/D-16), detail drawer with the exact `kanban_show` worker_context shape (D-20),
and four modals (Complete, Block, ArchiveConfirm, CreateTask). All 5 plans landed; the workspace -D warnings gate is
green; the integration tests pass. But the manual UAT surfaced two issues that the test surface didn't catch — one is a
backend predicate (archived filter), one is CSS contrast on overlays.

Recommended remediation route: **gap-closure phase 36.3.7.11.1**. It keeps the audit trail clean (Phase 36.3.7.11 closes
with all 5 plans in their original scope; the new phase scopes only the two defects + their regression tests). The
alternative — reopen Plans 01/03 in place — muddies the per-plan SUMMARY.md history and complicates roadmap rollups.
</context>

<next_action>
Decide remediation route with the tester, then dispatch. Recommended sequence:

1. `/gsd-phase add 36.3.7.11.1 --title "Kanban dashboard UAT gap closure (BUG-1 archived fetch + BUG-2 drawer contrast)"`
2. `/gsd-discuss-phase 36.3.7.11.1` — lock the two BUGs as the scope and confirm the regression-test contract.
3. `/gsd-plan-phase 36.3.7.11.1` — single-plan or two-plan; planner's call.
4. `/gsd-execute-phase 36.3.7.11.1`.
5. After UAT re-runs clean: close 36.3.7.11.1 then close 36.3.7.11 via roadmap.update-plan-progress.

Alternative shortest path if both defects feel small enough for a /gsd-quick:
1. `/gsd-quick "fix BUG-1 + BUG-2 from 36.3.7.11 UAT — see .continue-here.md"`.
2. Re-run UAT U2/U6/U7/U8.
</next_action>
