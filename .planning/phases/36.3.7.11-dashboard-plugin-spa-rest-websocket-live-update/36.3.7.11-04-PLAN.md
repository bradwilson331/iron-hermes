---
phase: 36.3.7.11
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/iron_hermes_ui/src/state.rs
  - crates/iron_hermes_ui/src/components/hermes_app/wheel.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs
  - crates/iron_hermes_ui/tests/kanban_wheel_nav.rs
autonomous: true
requirements:
  - D-02
  - D-03
must_haves:
  truths:
    - "D-02: WheelWedge gains an 11th variant Kanban (index 10); ALL six methods (index, from_index, label, sub, glyph, to_screen) updated atomically; from_index modulo changes from rem_euclid(10) to rem_euclid(11); the unreachable fallback arm comment is updated"
    - "D-02 wheel geometry: WHEEL_SECTION_COUNT constant in wheel.rs and all geometry that derives from a literal `10` updates to 11 (or to the constant if it already exists); wheel SVG renders 11 wedges with arc angle 360/11"
    - "D-03: Agents page toolbar gains a `KANBAN BOARD →` button immediately to the right of `PRUNE ENDED`; onclick sets active_screen to Screen::Kanban (the Screen enum already gained the variant in Plan 01)"
    - "Navigation round-trip: WheelWedge::Kanban.index() == 10 && WheelWedge::from_index(10) == WheelWedge::Kanban && WheelWedge::Kanban.to_screen() == Screen::Kanban"
    - "tests/kanban_wheel_nav.rs locks the round-trip behavior in a single test that calls all 11 from_index values and asserts the round-trip identity"
  artifacts:
    - path: "crates/iron_hermes_ui/src/state.rs"
      provides: "WheelWedge::Kanban variant + 6-method update"
      contains: "WheelWedge::Kanban"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/wheel.rs"
      provides: "11-wedge geometry"
      contains: "Kanban"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs"
      provides: "KANBAN BOARD → button"
      contains: "KANBAN BOARD"
  key_links:
    - from: "wheel.rs"
      to: "state.rs::WheelWedge"
      via: "from_index/to_screen called per wedge click"
      pattern: "WheelWedge::from_index"
    - from: "agents.rs toolbar"
      to: "state.rs::Screen::Kanban"
      via: "active_screen.set(Screen::Kanban) onclick"
      pattern: "Screen::Kanban"
---

<objective>
Land the navigation surface: add `Kanban` as the 11th `WheelWedge` variant (atomic 6-method update + geometry update — Risk 1), and add the `KANBAN BOARD →` button to the Agents page toolbar (D-03). This plan is independent of Plans 01/02/03 (depends_on=[]) and CAN ship first per RESEARCH.md "Recommended execution order: Plan 04 → Plan 01 → Plans 02 and 03 → Plan 05".

Plan 01 already adds `Screen::Kanban` variant. Plan 04 connects the wheel + Agents button to that screen. After Plan 04 ships in isolation, clicking the KANBAN wedge or the Agents toolbar button changes active_screen to `Screen::Kanban` and the screen renders whatever placeholder Plan 01 provides (or an empty screen if Plan 04 ships first).

Purpose: Discoverability. Two independent navigation paths (wheel + Agents button) per D-02 + D-03.

Output:
- `WheelWedge::Kanban` + 6-method update (label, sub, glyph, index, from_index modulo-11, to_screen).
- Wheel SVG geometry update for 11 wedges.
- `KANBAN BOARD →` button in `agents.rs` immediately right of `PRUNE ENDED`.
- `kanban_wheel_nav.rs` integration test locking the round-trip identity for all 11 wedges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-CONTEXT.md
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-RESEARCH.md
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-PATTERNS.md
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-UI-SPEC.md
@crates/iron_hermes_ui/CLAUDE.md
@crates/iron_hermes_ui/src/state.rs
@crates/iron_hermes_ui/src/components/hermes_app/wheel.rs
@crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Discovery — read wheel.rs geometry; report dynamic-vs-hardcoded and name the file:line locations to update</name>
  <files>(read-only discovery; produces inline notes in the PLAN summary file when complete)</files>
  <read_first>
    crates/iron_hermes_ui/src/components/hermes_app/wheel.rs (full file — geometry constants at lines 35-56, WheelWedge::from_index callsites at lines 305, 459, 466, 468, 502, the 10-wedge match at line 765, and any rendering loop that iterates 0..N wedges),
    crates/iron_hermes_ui/src/state.rs (WheelWedge enum + impl block — pattern from PATTERNS.md "state.rs MODIFY" section showing all 6 methods that must update),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-RESEARCH.md (Risk 1, Open Question Q2 wheel geometry),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-PATTERNS.md (state.rs section — all 6 methods enumerated)
  </read_first>
  <behavior>
    - Investigation report identifies whether `wheel.rs` derives its arc-angle math from a constant (e.g., `WHEEL_SECTION_COUNT = 10`) or hardcodes `10` / `360.0 / 10.0` literals.
    - Report enumerates every file:line where the literal `10` or `0..10` or `rem_euclid(10)` appears in wheel.rs and state.rs related to wedge count.
    - Report concludes with one of two findings:
      - DYNAMIC: a single named constant drives the geometry → bumping the constant from 10 to 11 is the only geometry change needed.
      - HARDCODED: geometry is inlined → enumerated list of file:lines must each update.
    - The findings are written into `.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-04-WHEEL-DISCOVERY.md` (a new artifact that Task 2 consumes).
  </behavior>
  <action>
    Run targeted greps to enumerate occurrences:
    - `grep -nE '(\\b10\\b|rem_euclid\\(10\\)|0\\.\\.10|360.*10)' crates/iron_hermes_ui/src/components/hermes_app/wheel.rs`
    - `grep -nE 'WHEEL_|SECTION_COUNT|WEDGE_COUNT|N_WEDGES' crates/iron_hermes_ui/src/components/hermes_app/wheel.rs`
    - `grep -n 'rem_euclid' crates/iron_hermes_ui/src/state.rs`

    Read `wheel.rs` lines 1-100 (geometry block) and lines 300-510 (the wedge interaction callsites grep already identified). Read state.rs around the `WheelWedge` impl block (PATTERNS.md guides to lines 617-737).

    Write the discovery report to `.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-04-WHEEL-DISCOVERY.md`. Contents:
    - Header: `# 36.3.7.11-04 Wheel Geometry Discovery`.
    - Conclusion line: `Geometry is: DYNAMIC` or `Geometry is: HARDCODED`.
    - Table of file:lines to update in Task 2 with the exact substitution required (e.g., `wheel.rs:765 — change "10 wedges per wheel-v2.js" comment + the match arm count`; `wheel.rs:42 — change `WHEEL_SECTION_COUNT: usize = 10` to `11``; `state.rs:653 — change `rem_euclid(10)` to `rem_euclid(11)`; etc.).

    Do NOT modify any production source file in this task. The discovery file is the only output.
  </action>
  <verify>
    <automated>test -f .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-04-WHEEL-DISCOVERY.md && grep -cE 'Geometry is: (DYNAMIC|HARDCODED)' .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-04-WHEEL-DISCOVERY.md | xargs test 1 -le</automated>
  </verify>
  <done>
    Discovery file exists and names a clear finding plus the file:lines Task 2 must touch.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atomic 6-method WheelWedge update + wheel.rs geometry update + Agents page KANBAN BOARD button + round-trip test</name>
  <files>crates/iron_hermes_ui/src/state.rs, crates/iron_hermes_ui/src/components/hermes_app/wheel.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs, crates/iron_hermes_ui/tests/kanban_wheel_nav.rs</files>
  <read_first>
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-04-WHEEL-DISCOVERY.md (Task 1 output — the file:line list),
    crates/iron_hermes_ui/src/state.rs (WheelWedge enum + the 6 methods: PATTERNS.md "state.rs MODIFY" lines 617-737 shows the analog shape),
    crates/iron_hermes_ui/src/components/hermes_app/wheel.rs (the file:lines from the discovery),
    crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs lines 343-362 (toolbar buttons "+ NEW AGENT" and "PRUNE ENDED" — the insertion point for the new button is immediately after PRUNE ENDED),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-CONTEXT.md (D-02 wheel-nav addition, D-03 toolbar button placement),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-UI-SPEC.md (§3.2 AgentsPageKanbanButton spec, §3.3 WheelNavKanbanModule spec, §7.1 label "KANBAN BOARD →"),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-RESEARCH.md (Risk 1 — modulo-10 wrap),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-PATTERNS.md (agents.rs MODIFY section — exact insertion point shown)
  </read_first>
  <behavior>
    - `WheelWedge` gains `Kanban` as the 11th variant (after `Settings` in the existing 10-variant enum).
    - `WheelWedge::index(WheelWedge::Kanban)` returns `10`.
    - `WheelWedge::from_index(10)` returns `WheelWedge::Kanban`.
    - `WheelWedge::from_index` uses `rem_euclid(11)` (not 10) — Risk 1 atomic fix.
    - `WheelWedge::Kanban.label()` returns `"KANBAN"` per UI-SPEC §3.3.
    - `WheelWedge::Kanban.sub()` returns `"TASK BOARD"` per UI-SPEC §3.3.
    - `WheelWedge::Kanban.glyph()` returns a glyph from the existing character set — `"▦"` or `"⬛"` per UI-SPEC §3.3 (no new asset).
    - `WheelWedge::Kanban.to_screen()` returns `Screen::Kanban` (the variant Plan 01 will/has added to the Screen enum; if Plan 04 ships first, this requires Plan 01's Screen enum addition to land OR Plan 04 also adds Screen::Kanban as an empty Screen variant — see Action Step A note below).
    - For every `i in 0..=10`, `WheelWedge::from_index(i).index() == i` (round-trip identity). The integration test enforces this.
    - Wheel SVG renders 11 wedges per the Task 1 discovery findings (geometry update applied per the discovery report's file:line list).
    - Agents page toolbar has a button labeled `"KANBAN BOARD →"` whose onclick sets the active screen Signal to `Screen::Kanban`. Button class matches existing buttons (UI-SPEC §3.2 — same style as `+ NEW AGENT` / `PRUNE ENDED`).
    - All 6 method updates land in a single commit / atomic edit (Risk 1 — failure to update any one of them causes silent miswiring).
  </behavior>
  <action>
    Step A — Screen enum coordination. CHECK FIRST: `grep -n 'Screen::Kanban' crates/iron_hermes_ui/src/state.rs`. If `Screen::Kanban` already exists (Plan 01 has merged), proceed to Step B. If NOT (Plan 04 is shipping first), add a minimal `Kanban` variant to the `Screen` enum here. Plan 01 will then layer on the ScreenKanban component without conflict. Also ensure the screen dispatch site in `crates/iron_hermes_ui/src/components/hermes_app/mod.rs` either already has a `Screen::Kanban => ...` arm (Plan 01 added it) or gets a minimal placeholder arm `Screen::Kanban => rsx! { div { "Kanban (coming soon)" } }` — clearly comment it as "Plan 04 placeholder; Plan 01 replaces this with ScreenKanban". The test in Task 2 step E will tolerate either form.

    Step B — `crates/iron_hermes_ui/src/state.rs` `WheelWedge` 6-method atomic update (PATTERNS.md lines 358-419 enumerates the analog shape):
    1. Add `Kanban` to the `WheelWedge` enum after the last existing variant (`Settings`).
    2. `index(self)` match: add arm `WheelWedge::Kanban => 10`.
    3. `from_index(i: usize)`: change `let n = ((i as i64).rem_euclid(10)) as usize;` to `let n = ((i as i64).rem_euclid(11)) as usize;`. Add arm `10 => WheelWedge::Kanban`. Update the fallback comment from `// unreachable after modulo-10` to `// unreachable after modulo-11`. Confirm the final unmatched arm still reads `_ => WheelWedge::Settings` (or whatever the existing fallback was — preserve semantics).
    4. `label(self)`: add arm `WheelWedge::Kanban => "KANBAN"`.
    5. `sub(self)`: add arm `WheelWedge::Kanban => "TASK BOARD"`.
    6. `glyph(self)`: add arm `WheelWedge::Kanban => "▦"` (executor may swap to `"⬛"` only if `"▦"` is not in the existing character set used elsewhere — verify by `grep` for the existing glyph strings).
    7. `to_screen(self)`: add arm `WheelWedge::Kanban => Screen::Kanban`.

    Step C — Wheel geometry update per Task 1 discovery. Apply the substitutions enumerated in `36.3.7.11-04-WHEEL-DISCOVERY.md`. Specifically:
    - If DYNAMIC: bump the named constant (e.g., `WHEEL_SECTION_COUNT: usize = 10` → `11`).
    - If HARDCODED: replace each enumerated literal `10` with `11` AND `360.0 / 10.0` with `360.0 / 11.0` AND any `0..10` iteration with `0..11`.
    - Update the comment at wheel.rs line 765 (`// CONTEXT D-10: 10 wedges per wheel-v2.js DEFAULT_SECTIONS.`) to note the 11-wedge addition for 36.3.7.11.

    Step D — `crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs`. Per PATTERNS.md "agents.rs (MODIFY)" exact insertion point (lines 343-362): inside the `div { class: "screen-actions" }`, immediately after the `button { ... "PRUNE ENDED" }` block, insert a new `button { class: "btn btn--ghost btn--sm", onclick: move |_| { active_screen.set(crate::state::Screen::Kanban); }, "KANBAN BOARD →" }`. The `active_screen` signal must be in scope; if it is not, look up how `agents.rs` accesses the screen signal (likely via `use_context::<Signal<Screen>>()` — read the file around line 33-62 to confirm and replicate the same access pattern).

    Step E — `crates/iron_hermes_ui/tests/kanban_wheel_nav.rs`. Integration test that asserts the round-trip identity for all 11 wedges:
    - For each `i in 0..=10`: assert `WheelWedge::from_index(i).index() == i`.
    - Assert `WheelWedge::from_index(10) == WheelWedge::Kanban`.
    - Assert `WheelWedge::Kanban.to_screen() == Screen::Kanban`.
    - Assert `WheelWedge::Kanban.label() == "KANBAN"` and `.sub() == "TASK BOARD"`.
    - Assert `WheelWedge::from_index(11)` (overflow) returns `WheelWedge::Chat` (modulo-11 wrap: 11 % 11 == 0 → Chat).
    - Assert NO existing wedge's `index()` changed (loop over all 10 pre-existing variants and check they map to 0..=9 unchanged).
    - Source-string assertion (mirrors `tests/websocket_lifecycle_parity.rs` pattern): read `agents.rs`, assert `"KANBAN BOARD →"` substring present. Read `wheel.rs`, assert `Kanban` substring present (proves geometry update at least references the new variant somewhere).
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui 2>&1 | tail -10 && cargo build -p iron_hermes_ui --features server 2>&1 | tail -10 && cargo test -p iron_hermes_ui --test kanban_wheel_nav 2>&1 | tail -10 && cargo clippy -p iron_hermes_ui --features server -- -D warnings 2>&1 | tail -20 && grep -c 'WheelWedge::Kanban' crates/iron_hermes_ui/src/state.rs | xargs -I {} test {} -ge 6 && grep -c 'rem_euclid(11)' crates/iron_hermes_ui/src/state.rs | xargs test 1 -le && grep -c 'KANBAN BOARD' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs | xargs test 1 -le</automated>
  </verify>
  <done>
    Round-trip test passes. Wheel SVG renders 11 wedges (manual UAT: open the app, count wedges or use DevTools to count `<path>` elements in the wheel SVG group). Agents page toolbar shows `KANBAN BOARD →` right of `PRUNE ENDED`. Clicking it or the wheel KANBAN wedge sets active_screen to Screen::Kanban.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard wheel navigation → Screen routing | Pure client-side; no untrusted input |
| Agents toolbar button → active_screen signal | Same — pure client-side state mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.11-04-T01 | Tampering | Missed match arm in any of the 6 WheelWedge methods | mitigate | Round-trip test asserts all 11 from_index values map correctly; exhaustive `match` over the enum is compile-time enforced by Rust (missing arms fail to build) |
| T-36.3.7.11-04-T02 | Tampering | `rem_euclid(10)` left unchanged → index 10 silently routes to Chat | mitigate | Test `from_index(10) == Kanban` and `from_index(11) == Chat` catches both directions |
| T-36.3.7.11-04-T03 | Tampering | Wheel SVG render count drifts from enum variant count | mitigate | If geometry is DYNAMIC, a single constant drives both; if HARDCODED, the discovery file enumerates every site; either way the manual UAT (count wedges) is the final check |
| T-36.3.7.11-04-T04 | Tampering | npm/pip/cargo package installs | not-applicable | No external registry packages |
</threat_model>

<verification>
- `cargo build -p iron_hermes_ui` succeeds (default + server features).
- `cargo test -p iron_hermes_ui --test kanban_wheel_nav` passes.
- `cargo clippy -p iron_hermes_ui -- -D warnings` clean.
- Manual UAT: launch dev server, count wheel wedges (expect 11), click KANBAN wedge → active_screen changes; navigate to Agents page, see `KANBAN BOARD →` button, click → active_screen changes.
</verification>

<success_criteria>
- D-02 (WheelWedge::Kanban + 6-method update + geometry), D-03 (Agents toolbar button).
- Round-trip test green; clippy clean; no Plan 01/02/03 dependencies required.
</success_criteria>

<output>
Create `.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-04-SUMMARY.md` when done.
</output>
