---
phase: 36.3.7.11
slug: dashboard-plugin-spa-rest-websocket-live-update
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-31
---

# Phase 36.3.7.11 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | cargo test (workspace) + cargo clippy (Dioxus 0.7.1 rules) |
| **Config file** | Cargo.toml workspace + crates/iron_hermes_ui/Cargo.toml |
| **Quick run command** | `cargo test -p iron_hermes_ui --features server kanban_` |
| **Full suite command** | `cargo build --workspace && cargo test --workspace && cargo clippy -p iron_hermes_ui --features server -- -D warnings` |
| **Estimated runtime** | ~120 seconds quick / ~480 seconds full |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p iron_hermes_ui --features server kanban_` (touched-area unit tests)
- **After every plan wave:** Run `cargo build --workspace && cargo test --workspace && cargo clippy --workspace -- -D warnings`
- **Before `/gsd:verify-work`:** Full suite must be green AND the manual UAT script in this phase dir must pass against a running `iron_hermes_ui` server.
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

> Populated by the planner during Step 8. Each task row maps to a `<test>` block in a PLAN.md task; rows marked ❌ W0 require a Wave 0 stub.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| {N}-01-01 | 01 | 1 | D-{NN} | T-{N}-01 / — | {expected secure behavior or "N/A"} | unit | `{command}` | ✅ / ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/iron_hermes_ui/tests/kanban_server_fns.rs` — stubs for D-13 write-fn signatures and D-05 read-fn signatures
- [ ] `crates/iron_hermes_ui/tests/kanban_ws_tail.rs` — stubs for WS tail-consumer contract and `last_event_id` cursor (D-08/D-15)
- [ ] `crates/iron_hermes_ui/tests/kanban_transition_validator.rs` — stubs for D-10 / D-11 / D-14 client+server transition validation (table-driven)
- [ ] `crates/ironhermes-kanban/src/store.rs` — add `list_all_events_after(since: i64) -> Vec<KanbanEvent>` (per RESEARCH.md — does not currently exist)
- [ ] `web-sys` Cargo feature additions — `DragEvent`, `DataTransfer` (per RESEARCH.md — currently absent; clean builds fail without them)

*Validation Architecture details live in `.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-RESEARCH.md` §"## Validation Architecture".*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Card visual parity with Models page reference image | D-01 | Pixel-level visual match cannot be automated against a screenshot | Run `iron_hermes_ui serve`, open Kanban tab, side-by-side compare to `Screenshot 2026-05-25 at 11.32.33 PM.png` |
| Drag-and-drop happy path (TRIAGE→TODO) | D-06 / D-10 | Browser-driven gesture; HTML5 drag events not reliably simulatable in unit tests | Manual UAT: drag a triage card to TODO column; expect optimistic move + persisted state after reload |
| Drag-DISALLOWED feedback (any → DONE) | D-11 | UX feedback (toast / cursor / column tint) is human-observable | Manual UAT: drag a TODO card onto DONE; expect rejection hint pointing to drawer `Complete…` button |
| Cross-process live update from CLI write | D-15 / D-16 | Two-process race against shared SQLite WAL | UAT: run `hermes kanban set <id> status=blocked` in a second shell with dashboard open; expect the card to move within ≤1s |
| Drawer refresh-on-event | D-21 | Requires open drawer + concurrent write to that task from another surface | UAT: open drawer, post comment via `kanban_comment_add` from CLI; expect comment thread to refresh |
| WS reconnect from `last_event_id` cursor | RESEARCH §"WS reconnect strategy" | Forced socket close + verify no missed deltas | UAT: kill WS frame mid-session (DevTools), expect reconnect within 5s and zero-loss replay from `last_event_id` |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (RESEARCH §"Risks & Landmines" — `list_all_events_after`, `DragEvent`/`DataTransfer` feature gate, `WheelWedge` modulo-10 update)
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
