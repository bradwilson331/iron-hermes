---
phase: 36.3.7.11
plan: 03
type: execute
wave: 3
depends_on:
  - 36.3.7.11-01
files_modified:
  - crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/mod.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/drawer.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/modals.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/card.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/column.rs
  - assets/kanban.css
  - crates/iron_hermes_ui/tests/kanban_drawer.rs
autonomous: true
requirements:
  - D-12
  - D-13
  - D-20
  - D-21
must_haves:
  truths:
    - "D-12: TRIAGE cards (in card layer) and TRIAGE drawer both expose ⚗ Decompose and ✨ Specify buttons that call run_decompose_or_specify"
    - "D-13: drawer wires Complete (PromptPayload::Complete), Block (PromptPayload::Block), Unblock (new_status=ready), Archive (new_status=archived + confirm), Comment (post_comment), Create (create_task)"
    - "D-20: drawer contains in order: DrawerHeader, StatusActionRow, TriageActionRow (conditional), WorkerContextBlock (parent_handoffs + prior_attempts), RunHistorySection, EventStreamSection (last 20), CommentSection (existing + compose)"
    - "D-21: per-task event counter increments on each TaskEventBatch row whose task_id matches the open task; drawer use_resource for comments/events/runs re-runs after a 200ms debounce when the counter changes (UI-SPEC §8.4)"
    - "Drawer focus management per UI-SPEC §3.9: on open focus moves to close button; on close focus returns to triggering card action button; Escape closes drawer"
    - "Modal focus trap per UI-SPEC §3.10: Tab cycles within modal only; Escape dismisses"
    - "NotWired branch from Plan 02: when run_decompose_or_specify returns DecomposeResult::NotWired, Decompose/Specify buttons render disabled with tooltip per UI-SPEC §4.3"
  artifacts:
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/drawer.rs"
      provides: "TaskDrawer + DrawerHeader + StatusActionRow + TriageActionRow + WorkerContextBlock + RunHistorySection + EventStreamSection + CommentSection"
      contains: "TaskDrawer"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/modals.rs"
      provides: "CompleteModal + BlockModal + ArchiveConfirmModal + CreateTaskModal"
      contains: "CompleteModal"
  key_links:
    - from: "drawer.rs::TaskDrawer"
      to: "src/server/kanban_api.rs::{fetch_task,fetch_task_events,fetch_task_runs,fetch_comments}"
      via: "use_resource per section"
      pattern: "use_resource"
    - from: "modals.rs::CompleteModal"
      to: "src/server/kanban_api.rs::patch_task_status"
      via: "spawn after form validate, PromptPayload::Complete"
      pattern: "PromptPayload::Complete"
    - from: "modals.rs::BlockModal"
      to: "src/server/kanban_api.rs::patch_task_status"
      via: "spawn after form validate, PromptPayload::Block"
      pattern: "PromptPayload::Block"
    - from: "kanban.rs WS message handler (Plan 01)"
      to: "drawer.rs::per-task event counter"
      via: "signal: per_task_event_counter[task_id] increments on each matching event"
      pattern: "per_task_event_counter"
---

<objective>
Land the detail drawer (mid-tier panel slide from right per UI-SPEC §3.9) with all sections from D-20 + the four modals (Complete, Block, ArchiveConfirm, CreateTask) per UI-SPEC §3.10. After this plan, clicking a card's `▶` action button opens the drawer; clicking `Complete…` opens the modal; submitting persists via the write fns from Plan 02. The drawer auto-refreshes per-section when a WS event for the open task arrives (D-21).

Purpose: Close the v1 write surface. The drawer is the only place where Complete/Block/Create happen (drag-and-drop cannot supply summary/reason — Risk 8). Decompose/Specify buttons (D-12) live both on TRIAGE cards (already stubbed in Plan 01 card.rs) and in the drawer's TriageActionRow.

Output:
- `drawer.rs` exporting `TaskDrawer` + all subsection components.
- `modals.rs` exporting four modals + shared `Modal` shell.
- Drawer state lifted to `ScreenKanban`: `open_drawer_task_id` is the trigger; sub-section resources re-fetch via the per-task event counter from D-21.
- TRIAGE card `⚗ Decompose` / `✨ Specify` buttons wired (Plan 01 left them as stubs).
- Drag-to-archive flow now opens `ArchiveConfirmModal` instead of persisting directly (replaces the Plan 02 TODO).
- `kanban_drawer.rs` integration test that asserts the drawer source contains the section markers from UI-SPEC §7.6 in order.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-CONTEXT.md
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-RESEARCH.md
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-PATTERNS.md
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-UI-SPEC.md
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-01-SUMMARY.md
@crates/iron_hermes_ui/CLAUDE.md
@crates/iron_hermes_ui/src/server/kanban_api.rs
@crates/iron_hermes_ui/src/protocol.rs
@crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs
@crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/card.rs
@crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/column.rs
@crates/ironhermes-kanban/src/tools/show.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 1): TaskDrawer + WorkerContextBlock + RunHistorySection + EventStreamSection + CommentSection + section labels + focus management</name>
  <files>crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/drawer.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/mod.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs, assets/kanban.css</files>
  <read_first>
    crates/iron_hermes_ui/CLAUDE.md (Dioxus 0.7.1; use_resource; signal discipline),
    crates/iron_hermes_ui/src/server/kanban_api.rs (Plan 01 read fns: fetch_task, fetch_task_events, fetch_task_runs, fetch_comments),
    crates/iron_hermes_ui/src/protocol.rs (Plan 01 types: WorkerContextEnvelope, TaskRunRow, CommentRow, KanbanEventRow),
    crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs (Plan 01 — extend with drawer rendering, per-task event counter signal, WS handler enrichment for D-21),
    crates/ironhermes-kanban/src/tools/show.rs lines 218-232 (canonical worker_context shape),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-CONTEXT.md (D-20, D-21),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-UI-SPEC.md (§3.9 TaskDrawer + focus mgmt + ARIA; §4.3 drawer states incl. NotWired tooltip; §5.2 drawer layout; §6.1 ARIA roles incl. role="complementary"; §6.2 keyboard nav incl. Escape closes; §7.6 section labels; §7.7 run outcome badge labels; §8.2 drawer open/close triggers; §8.4 drawer refresh debounce 200ms),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-PATTERNS.md (drawer.rs use_resource + re-fetch-on-event pattern; Pattern E hook discipline)
  </read_first>
  <behavior>
    - `TaskDrawer` is rendered when `open_drawer_task_id.read().is_some()` — slide-in from right per UI-SPEC §2.7 + §5.2, fixed position 480px wide, `role: "complementary", aria_label: "Task detail: {title}", aria_modal: "false"`.
    - Drawer body has SEVEN sections in order matching D-20: DrawerHeader (title + chips), StatusActionRow (Complete… / Block… / Unblock / Archive task — but the actual modal opens in Task 2), TriageActionRow conditional on status=Triage (⚗ Decompose / ✨ Specify), WorkerContextBlock (renders `worker_context` JSON from fetch_task — parent_handoffs + prior_attempts sub-sections per show.rs lines 218-232), RunHistorySection (renders fetch_task_runs result with outcome badges per UI-SPEC §7.7), EventStreamSection (last 20 task_events read-only), CommentSection (existing comments + compose box but the compose POST is wired in Task 2).
    - Each section uses `use_resource` to fetch its data (`fetch_task`, `fetch_task_runs`, `fetch_task_events`, `fetch_comments` — four separate resources). Resources re-run when `per_task_event_counter[task_id]` increments (D-21).
    - The screen (kanban.rs) maintains `per_task_event_counter: Signal<HashMap<String, u64>>`. The WS message handler from Plan 01 is extended: when `KanbanWsEvent::TaskEventBatch { events, .. }` arrives, iterate `events`; for each unique `task_id`, increment its entry in the counter map. The drawer's `use_resource` closures depend on the counter value for the open task — when it changes, Dioxus re-runs the resource. Apply a 200ms debounce per UI-SPEC §8.4: collect events into a buffer with a `gloo-timers`-driven 200ms timer; only emit counter increments after the buffer settles.
    - Keyboard / focus per UI-SPEC §6.2 and §3.9:
      - On open: focus moves to the close button (✕).
      - Tab cycles forward through StatusActionRow buttons → TriageActionRow buttons → CommentCompose textarea → CommentSubmit button → close button.
      - Shift+Tab reverses; the cycle does NOT escape the drawer (focus trap).
      - Escape closes the drawer (sets `open_drawer_task_id.set(None)`).
      - On close: focus returns to the card action button that opened the drawer. Implementation: the screen captures `last_focused_card_id: Signal<Option<String>>` when the open trigger fires; on close, calls a JS helper or Dioxus focus API to focus the card's action button — use `web_sys::HtmlElement::focus()` looked up by `aria-label="Open {title} detail"` or by a stable DOM id like `id: "kn-card-action-{task.id}"`.
    - Drawer states per UI-SPEC §4.3: Loading (skeleton), Stale data (silent sub-section re-fetch — no full-drawer flash), Error (block + Close), RUNNING task (action buttons disabled with tooltip), DONE task (Unblock/Complete/Block hidden), ARCHIVED task (all action buttons hidden), Decompose/Specify not wired (buttons disabled with tooltip per UI-SPEC §4.3 / §7.5).
    - Drawer is INDEPENDENT of any board-side card visual changes — it overlays the board (z-index 200 per UI-SPEC §2.6) and clicking outside does NOT dismiss (UI-SPEC §8.2 — design intent: avoid accidental dismissal during swarm supervision).
  </behavior>
  <action>
    Step A — Create `crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/drawer.rs`. Top-level `#[component] pub fn TaskDrawer(task_id: ReadOnlySignal<Option<String>>, per_task_event_counter: ReadOnlySignal<std::collections::HashMap<String, u64>>, on_close: EventHandler, on_open_complete_modal: EventHandler<String>, on_open_block_modal: EventHandler<String>, on_open_archive_modal: EventHandler<String>) -> Element`. The modal-open EventHandlers are filled in Task 2's modal wiring — drawer just emits the events.

    Inside the body, register the four `use_resource` calls BEFORE any conditional RSX. Each closure reads `task_id()` + `per_task_event_counter()` to register dependency tracking (per D-21 / UI-SPEC §8.4). Example pattern (no fenced code — see PATTERNS.md drawer section):

    Resource 1 (worker_context): `let task_detail = use_resource(move || async move { match task_id() { Some(id) => Some(crate::server::kanban_api::fetch_task(id, None).await), None => None } });` — Dioxus tracks the `task_id()` and `per_task_event_counter()` calls inside the async block so the resource restarts on change.

    Resource 2 (runs): `let runs = use_resource(...);` calling `fetch_task_runs`.

    Resource 3 (events): `let events = use_resource(...);` calling `fetch_task_events(id, None, 20)`.

    Resource 4 (comments): `let comments = use_resource(...);` calling `fetch_comments(id, None)`.

    Define small sub-components inside drawer.rs: `DrawerHeader`, `StatusActionRow`, `TriageActionRow`, `WorkerContextBlock`, `RunHistorySection`, `EventStreamSection`, `CommentSection`. Each takes the relevant resource output as `ReadOnlySignal<Option<Result<T, ServerFnError>>>` or similar.

    Section labels MUST match UI-SPEC §7.6 verbatim: `"WORKER CONTEXT"`, `"PARENT HANDOFFS"`, `"PRIOR ATTEMPTS"`, `"RUN HISTORY"`, `"EVENTS (last 20)"`, `"COMMENTS"`. Font: `--fs-11`, `--weight-bold` (per UI-SPEC §2.4 drawer section label row).

    StatusActionRow renders buttons per UI-SPEC §7.1: `Complete…` (onclick: `on_open_complete_modal.call(task_id.clone())`), `Block…` (onclick: `on_open_block_modal.call(task_id.clone())`), `Unblock task` (onclick: spawn `patch_task_status(task_id, None, KanbanStatus::Ready, None)`), `Archive task` (onclick: `on_open_archive_modal.call(task_id.clone())`). Conditional rendering per state matrix §4.3: hide Complete/Block when status=Done; hide Unblock when status≠Blocked; disable all (+tooltip) when status=InProgress.

    TriageActionRow renders `⚗ Decompose` / `✨ Specify` buttons. Each `onclick` spawns `run_decompose_or_specify(task_id, None, action).await`. On `Ok(DecomposeResult::NotWired { message })`, set the button's tooltip + disabled state per UI-SPEC §4.3.

    RunHistorySection renders one row per task_run with the outcome badge per UI-SPEC §7.7 (CSS class `kn-badge` + `data-outcome="{outcome}"` attribute; CSS will color-code).

    WorkerContextBlock renders the worker_context JSON 1:1 per Q5 — parent_handoffs as a list of `{parent_id, parent_title, parent_status, summary, metadata}`, prior_attempts as a list of `{outcome, summary, error, started_at, ended_at}`. Plain-text rendering only — no markdown.

    EventStreamSection renders the events list newest-first with `{kind} {created_at: human-readable}` per row.

    CommentSection renders existing comments (author, body, timestamp) + a compose box (`<textarea>` + `Post comment` button). The compose POST wiring is Task 2's responsibility — Task 1 just renders the form with `onsubmit` as a placeholder.

    Focus mgmt: on first render with `task_id.read().is_some()`, use `use_effect` (or equivalent) to focus the close button via `web_sys`. Add a `ondblclick`-blocking guard (clicking inside the drawer should NOT propagate to the board behind). Listen for Escape via a keydown handler on the drawer container that calls `on_close.call(())` when `event.key() == "Escape"`.

    ARIA: `role: "complementary"`, `aria_label: "Task detail: {title}"`, `aria_modal: "false"`.

    Step B — Update `crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/mod.rs` to declare `pub mod drawer;` and re-export `pub use drawer::TaskDrawer;`.

    Step C — Update `crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs`:
    1. Add `let per_task_event_counter: Signal<HashMap<String, u64>> = use_signal(|| HashMap::new());` to the screen's signal block (unconditional registration).
    2. Update the WS message handler installed in Plan 01: on `KanbanWsEvent::TaskEventBatch { events, .. }`, iterate the events, group by task_id, and (after 200ms debounce per UI-SPEC §8.4) increment the counter map for each affected task_id. Use a small `use_effect` + `gloo-timers::future::TimeoutFuture` pattern for the debounce.
    3. Render the drawer at the bottom of the screen RSX: `if open_drawer_task_id.read().is_some() { rsx! { TaskDrawer { task_id: open_drawer_task_id.into(), per_task_event_counter: per_task_event_counter.into(), on_close: move |_| open_drawer_task_id.set(None), on_open_complete_modal: ..., on_open_block_modal: ..., on_open_archive_modal: ... } } }`. The modal-open handlers set new `Signal<Option<String>>` fields that Task 2 introduces: `complete_modal_task: Signal<Option<String>>`, etc.

    Step D — Update `assets/kanban.css` with drawer CSS:
    - `.kn-drawer { position: fixed; right: 0; top: 0; bottom: 0; width: 480px; max-width: min(480px, calc(100vw - 80px)); background: var(--w-bg-2); border-left: 1px solid var(--w-border-hi); box-shadow: var(--w-shadow-float); z-index: 200; overflow-y: scroll; transform: translateX(0); transition: transform 200ms var(--ease-out, ease-out); }` per UI-SPEC §5.2 + §2.7.
    - `.kn-drawer[data-open="false"] { transform: translateX(100%); }`.
    - `.kn-drawer-header { padding: var(--sp-6); position: sticky; top: 0; background: var(--w-bg-2); }`.
    - `.kn-drawer-section { padding: var(--sp-4); }`.
    - `.kn-drawer-section-label { font-size: var(--fs-11); font-weight: var(--weight-bold, 700); color: var(--fg-dim); text-transform: uppercase; }`.
    - `.kn-badge { display: inline-block; padding: 2px 6px; border-radius: var(--w-radius-pill); font-size: var(--fs-11); }`.
    - `.kn-badge[data-outcome="completed"] { color: var(--success); border: 1px solid var(--success); }`.
    - `.kn-badge[data-outcome="blocked"], .kn-badge[data-outcome="crashed"], .kn-badge[data-outcome="gave_up"] { color: var(--danger); border: 1px solid var(--danger); }`.
    - `.kn-badge[data-outcome="timed_out"], .kn-badge[data-outcome="spawn_failed"] { color: var(--warn); border: 1px solid var(--warn); }`.
    - `.kn-badge[data-outcome="active"] { color: var(--accent-primary); border: 1px solid var(--accent-primary); }`.
    - At-rule: `@media (prefers-reduced-motion: reduce) { .kn-drawer { transition: none; } }`.
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --features server 2>&1 | tail -10 && cargo build -p iron_hermes_ui 2>&1 | tail -10 && cargo clippy -p iron_hermes_ui --features server -- -D warnings 2>&1 | tail -20 && grep -c '#\[component\]' crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/drawer.rs | xargs -I {} test {} -ge 1 && grep -c 'use_resource' crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/drawer.rs | xargs -I {} test {} -ge 4 && grep -vE '^[[:space:]]*(/\*|\*|//)' assets/kanban.css | grep -cE '#[0-9a-fA-F]{3,6}([^0-9a-fA-F]|$)' | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    Drawer renders all 7 sections in the D-20 order. Sub-section resources re-fetch on per-task event counter increment (manual UAT: open drawer, run `hermes kanban set <id> assignee=foo`, see drawer refresh within ~250-450ms). Clippy clean. No new hex.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 2): CompleteModal + BlockModal + ArchiveConfirmModal + CreateTaskModal + comment compose POST + drag-archive opens confirm modal + tests</name>
  <files>crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/modals.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/mod.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/drawer.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/column.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/card.rs, assets/kanban.css, crates/iron_hermes_ui/tests/kanban_drawer.rs</files>
  <read_first>
    crates/iron_hermes_ui/CLAUDE.md,
    crates/iron_hermes_ui/src/server/kanban_api.rs (Plan 02 write fns: patch_task_status, post_comment, create_task, run_decompose_or_specify),
    crates/iron_hermes_ui/src/protocol.rs (PromptPayload, CreateTaskPayload, DecomposeOrSpecify, DecomposeResult),
    crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/drawer.rs (Task 1 — modal-open EventHandlers consumed here),
    crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/column.rs (Plan 02 — drag-archive currently persists directly per the TODO comment),
    crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/card.rs (Plan 01/02 — TRIAGE card decompose/specify buttons stubbed),
    crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs (source-string assertion pattern),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-CONTEXT.md (D-12, D-13),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-UI-SPEC.md (§3.10 shared modal spec; §4.4 modal states; §7.1 button labels; §7.2 modal headers/bodies/CTAs incl. contextual dismiss labels; §7.5 toast copy)
  </read_first>
  <behavior>
    - `CompleteModal` renders per UI-SPEC §3.10 + §7.2: header `"Complete task"`, summary textarea (required), metadata textarea (JSON, optional), submit CTA `"Complete task"`, dismiss `"Keep editing"`. Submit disabled while summary is empty (error message `"A summary is required to complete a task."`). On submit: spawn `patch_task_status(task_id, None, KanbanStatus::Done, Some(PromptPayload::Complete { summary, metadata: parse_optional_json(metadata) }))`. On Ok: close modal + close drawer + toast `"Task completed"`. On Err: show inline error banner per UI-SPEC §4.4.
    - `BlockModal` per UI-SPEC §7.2: header `"Block task"`, reason textarea (required), submit `"Block task"`, dismiss `"Keep editing"`. Includes the prefix suggestion informational text. Submit calls `patch_task_status(task_id, None, KanbanStatus::Blocked, Some(PromptPayload::Block { reason }))`.
    - `ArchiveConfirmModal` per UI-SPEC §7.2: header `"Archive task?"`, body copy verbatim, confirm CTA `"Archive task"` (primary button color — NOT --danger; UI-SPEC §7.2 explicit), dismiss `"Keep task"`. Confirm spawns `patch_task_status(task_id, None, KanbanStatus::Archived, None)`.
    - `CreateTaskModal` per UI-SPEC §7.2: header `"Create task"`, fields per §7.2 (title required, assignee, priority segmented P0/P1/P2/P3 default P2, tenant optional, body optional, `Start in Triage` checkbox checked by default). Submit `"Create task"`, dismiss `"Discard task"`. Submit calls `create_task(None, CreateTaskPayload { ... })`.
    - All modals share: overlay `position: fixed; inset: 0; background: rgba(...0.75)`, dialog centered, `role: "dialog", aria_modal: "true", aria_labelledby: "{id}-title", aria_describedby: "{id}-desc"`. Focus trap. Escape dismisses (same as the contextual dismiss button).
    - Comment compose box in drawer (Task 1 left as stub): submit spawns `post_comment(task_id, None, body)`. Empty body returns inline error per UI-SPEC §4.3 "Comment error".
    - TRIAGE card `⚗`/`✨` button onclicks (Plan 01 stubs) spawn `run_decompose_or_specify(task_id, None, DecomposeOrSpecify::{Decompose|Specify})`. On `Ok(NotWired)` set the per-card disabled state + tooltip per UI-SPEC §4.3.
    - Drag-to-archive flow in `column.rs::ondrop` (Plan 02 left as TODO): replace the direct `patch_task_status` spawn for archive with a `archive_modal_task: Signal<Option<String>>` set; the modal then handles the actual mutation on confirm.
    - `tests/kanban_drawer.rs` asserts source-string presence of the 7 D-20 sections (UI-SPEC §7.6 labels), the 4 modal headers (UI-SPEC §7.2), and the 4 contextual dismiss labels (`"Keep editing"`, `"Keep task"`, `"Discard task"` — distinct labels prove no generic "Cancel" leak).
  </behavior>
  <action>
    Step A — Create `crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/modals.rs`. Define a shared `#[component] fn ModalShell(title: &str, on_dismiss: EventHandler, children: Element) -> Element` that renders the overlay + dialog with shared ARIA + focus trap (Tab cycling within the dialog: implement via a `keydown` handler on the dialog container that intercepts Tab/Shift+Tab when focus would leave the dialog and wraps it to the first/last focusable element).

    Define `#[component] pub fn CompleteModal(task_id: ReadOnlySignal<Option<String>>, on_dismiss: EventHandler, on_success: EventHandler) -> Element`. Inside: register hooks unconditionally (summary signal, metadata signal, error signal, submitting signal). Render the modal shell with the §7.2 copy. Submit button: disabled while summary is empty OR submitting is true. On submit: set submitting=true, spawn `patch_task_status(...)`, on Ok: call `on_success.call(())`; on Err: set error signal, set submitting=false.

    Define `BlockModal`, `ArchiveConfirmModal`, `CreateTaskModal` analogously. CreateTaskModal needs a segmented priority component; implement inline as four buttons that set a `Signal<i64>` for priority.

    Step B — Wire modals into kanban.rs. Add signals: `complete_modal_task: Signal<Option<String>>`, `block_modal_task: Signal<Option<String>>`, `archive_modal_task: Signal<Option<String>>`, `create_modal_open: Signal<bool>`. The drawer's `on_open_*_modal` EventHandlers from Task 1 set these signals. Render the modals at the bottom of the screen RSX (siblings of the drawer + toast).

    On `CreateTaskModal`'s success, refresh the board: `board_resource.restart();` AND `toast_msg.set(Some("Task '{title}' created"))` per UI-SPEC §6.5.

    Step C — Card TRIAGE button wiring. In `screens/kanban/card.rs`, find the Plan 01 stub `⚗ Decompose` / `✨ Specify` buttons (currently no-op or absent). For TRIAGE cards (status == KanbanStatus::Triage), render the two buttons in a row below the footer per UI-SPEC §3.7. Each `onclick` spawns the write fn. On `Ok(NotWired)`, set a per-card disabled signal AND toast `"{action} not configured. Run: hermes kanban {action-slug} {id}"` per UI-SPEC §7.5.

    Step D — Column archive drop wiring fix. In `screens/kanban/column.rs::ondrop`, replace the Plan 02 direct-archive code path with: if `column_status == KanbanStatus::Archived`, instead of calling `patch_task_status` directly, set `archive_modal_task.set(Some(tid_owned))`. (The signal handle for `archive_modal_task` must be propagated as a prop from `KanbanBoard` down to `KanbanColumn`.)

    Step E — `+ Add card` button per UI-SPEC §3.5. At the bottom of each `KanbanColumn`, render a button per UI-SPEC §7.1 row `+ Add card`. Onclick: `create_modal_open.set(true)`. Pre-populate the column's status as the initial selection (TRIAGE box pre-checked when from a TRIAGE column).

    Step F — Tests. `tests/kanban_drawer.rs` mirrors the source-string assertion pattern from `tests/websocket_lifecycle_parity.rs`. Read `src/components/hermes_app/screens/kanban/drawer.rs` and `modals.rs` as strings, assert ALL of:
    - Drawer source contains `"WORKER CONTEXT"`, `"PARENT HANDOFFS"`, `"PRIOR ATTEMPTS"`, `"RUN HISTORY"`, `"EVENTS (last 20)"`, `"COMMENTS"` (D-20 / UI-SPEC §7.6 labels in order).
    - Modals source contains `"Complete task"`, `"Block task"`, `"Archive task?"`, `"Create task"` (UI-SPEC §7.2 headers).
    - Modals source contains the contextual dismiss strings: `"Keep editing"`, `"Keep task"`, `"Discard task"` (no generic `"Cancel"` — verify NO occurrence of `"Cancel"` (case-insensitive, but allow the substring inside "cancellation" by using a word-boundary regex via `grep -E '\\bCancel\\b'`)).
    - Drawer source contains `aria_modal: "false"` (it's a panel, not a dialog).
    - Modal source contains `aria_modal: "true"` (it's a dialog).
    - Drawer source contains `"Close task detail"` (the close button aria-label per UI-SPEC §7.1).
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --features server 2>&1 | tail -10 && cargo build -p iron_hermes_ui 2>&1 | tail -10 && cargo clippy -p iron_hermes_ui --features server -- -D warnings 2>&1 | tail -20 && cargo test -p iron_hermes_ui --test kanban_drawer 2>&1 | tail -10 && ! grep -E '\bCancel\b' crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/modals.rs</automated>
  </verify>
  <done>
    Drawer renders; Complete/Block/Archive/Create modals open and persist via the Plan 02 write fns; comments post; Decompose/Specify buttons either persist or surface NotWired tooltip. Drag-to-archive opens confirm modal (not direct write). Card TRIAGE actions wired. Tests pass; no generic "Cancel" strings; clippy clean; no new hex.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST writes (Complete/Block/Comment/Create) | Untrusted form bodies — modal validates client-side (UX), `kanban_api` validates server-side (correctness) |
| Modal JSON metadata textarea → server | `metadata: Option<serde_json::Value>` — must parse client-side before send, server treats opaque |
| Comment body → drawer rendering | Plain-text only — no markdown render (RESEARCH §"Known Threat Patterns" row "Comment injection") |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.11-03-T01 | Tampering | CompleteModal submits with empty summary via JS bypass | mitigate | Server `patch_task_status` re-checks `prompt_payload.summary.is_empty()` (Plan 02 Task 2 behavior) |
| T-36.3.7.11-03-T02 | Tampering | Comment body injection via markdown / HTML | mitigate | EventStreamSection and CommentSection render plain text only — no `dangerous_inner_html`, no markdown parser; the body string is rendered as a Dioxus text node |
| T-36.3.7.11-03-T03 | Tampering | Forged task_id in modal submit | accept | Server validates task exists before mutation; worst case is a 404-style ServerFnError |
| T-36.3.7.11-03-I01 | Information Disclosure | Drawer fetches full worker_context for any visible card | accept | Same as Plan 01 — drawer-visible tasks are already on the board the user can see |
| T-36.3.7.11-03-D01 | DoS | Open-drawer + bursty WS updates → many re-fetches | mitigate | 200ms debounce on per-task event counter (UI-SPEC §8.4); at most one re-fetch per 200ms window per resource |
| T-36.3.7.11-03-T04 | Tampering | Decompose/Specify with no aux client → server crash | mitigate | Q9 branch (b): return `DecomposeResult::NotWired` rather than panicking; UI surfaces disabled state |
| T-36.3.7.11-03-T05 | Tampering | npm/pip/cargo package installs | not-applicable | No new external registry packages — all deps are workspace path-deps or were landed in Plan 01 |
</threat_model>

<verification>
- `cargo build -p iron_hermes_ui` (default web) succeeds.
- `cargo build -p iron_hermes_ui --features server` succeeds.
- `cargo test -p iron_hermes_ui --features server kanban_` runs the drawer test alongside Plan 01/02 tests.
- `cargo clippy -p iron_hermes_ui --features server -- -D warnings` clean.
- Manual UAT: click card `▶` → drawer opens, focus on close button; press Escape → drawer closes, focus returns to card action button; click `Complete…` → modal opens with focus in summary; submit empty summary → disabled CTA + error; submit valid → drawer closes, card moves to DONE.
</verification>

<success_criteria>
- D-12 (Triage cards + drawer Triage row both call run_decompose_or_specify), D-13 (drawer + modals wire all four write fns), D-20 (seven sections in order), D-21 (per-task event counter + 200ms debounce + sub-section re-fetch).
- All Plan 02 TODO comments (drag-archive opens modal) are closed.
- All tests pass.
</success_criteria>

<output>
Create `.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-03-SUMMARY.md` when done.
</output>
