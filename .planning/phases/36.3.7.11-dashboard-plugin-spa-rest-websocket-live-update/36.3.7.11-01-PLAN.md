---
phase: 36.3.7.11
plan: 01
type: execute
wave: 2
depends_on: []
files_modified:
  - crates/iron_hermes_ui/Cargo.toml
  - crates/iron_hermes_ui/src/protocol.rs
  - crates/iron_hermes_ui/src/server/mod.rs
  - crates/iron_hermes_ui/src/server/state.rs
  - crates/iron_hermes_ui/src/server/kanban_api.rs
  - crates/iron_hermes_ui/src/server/kanban_ws.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/mod.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/board.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/column.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/card.rs
  - crates/ironhermes-kanban/src/store.rs
  - crates/ironhermes-core/src/config.rs
  - crates/iron_hermes_ui/tests/kanban_server_fns.rs
  - crates/iron_hermes_ui/tests/kanban_ws_tail.rs
  - crates/iron_hermes_ui/tests/kanban_board_read.rs
autonomous: true
requirements:
  - D-01
  - D-02
  - D-04
  - D-05
  - D-08
  - D-09
  - D-15
  - D-16
  - D-17
  - D-18
  - D-19
  - D-22
  - D-23
must_haves:
  truths:
    - "D-01: kanban.css implements cyan-glow card border per UI-SPEC §2.2 using only existing tokens (--accent-primary, --w-radius-block, --w-shadow-block); pixel-level visual match to Kanban-main-view.png is verified in Plan 05 manual UAT U2"
    - "D-02: Screen::Kanban variant exists in src/state.rs; ScreenKanban renders inside HermesApp screen dispatch (visible when active_screen == Screen::Kanban) — wheel-nav wiring lands in Plan 04"
    - "D-04: kanban.css uses only existing tokens (--accent-primary, --w-bg-2, --w-radius-block); no new hex values introduced"
    - "D-05: NO REST mount — `grep -rn '/api/plugins/kanban' crates/iron_hermes_ui/src/server | grep -v test` returns 0; reads only via `#[server] fetch_board`, `fetch_task`, `fetch_task_events`, `fetch_task_runs`, `fetch_comments`"
    - "D-08: `/api/ws/kanban` route uses TypedWebsocket<String, String>; WS_KEEPALIVE_INTERVAL = Duration::from_secs(5); send_close_frame on teardown; RAII receiver drop on exit"
    - "D-09: Six visible columns rendered (TRIAGE, TODO, READY, IN PROGRESS, BLOCKED, DONE); status values exactly match KanbanStatus enum variants"
    - "D-15: kanban_ws::run_kanban_tail_loop polls list_all_events_after at config.dashboard.kanban.tail_interval_ms; does NOT import or reference ironhermes_kanban::notifier"
    - "D-16: KanbanStore::open_default opens ~/.hermes/kanban.db with WAL mode (existing PRAGMA); dashboard tail and gateway dispatcher both read the same file with no IPC/lock sharing"
    - "D-17: config.dashboard.kanban.tail_interval_ms reads default 250 via #[serde(default)]; missing YAML key does not fail Config::load"
    - "D-18: fetch_board with board=None resolves to KanbanStore::open_default; no board switcher UI rendered"
    - "D-19: every #[server] fn in kanban_api.rs takes `board: Option<String>` as a parameter"
    - "D-22: kanban #[server] fns and /api/ws/kanban inherit the same auth gate that protects /api/ws/chat — no kanban-specific token check"
    - "D-23: bind address comes from existing iron_hermes_ui serve flag; no new bind config introduced"
    - "WALKING SKELETON: with kanban.db containing N tasks, navigating active_screen to Screen::Kanban shows the 6-column board populated with N cards; a CLI write (`hermes kanban set <id> status=todo`) causes the dashboard to re-render the moved card within tail_interval_ms + WS RTT"
  artifacts:
    - path: "crates/iron_hermes_ui/src/server/kanban_api.rs"
      provides: "Read-side #[server] fns: fetch_board, fetch_task, fetch_task_events, fetch_task_runs, fetch_comments"
      contains: "#[server]"
    - path: "crates/iron_hermes_ui/src/server/kanban_ws.rs"
      provides: "WS handler at /api/ws/kanban + run_kanban_tail_loop"
      contains: "WS_KEEPALIVE_INTERVAL"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs"
      provides: "ScreenKanban component + per-screen Signal state"
      contains: "#[component]"
    - path: "crates/ironhermes-kanban/src/store.rs"
      provides: "list_all_events_after public method"
      contains: "pub fn list_all_events_after"
    - path: "crates/ironhermes-core/src/config.rs"
      provides: "DashboardConfig + DashboardKanbanConfig with tail_interval_ms"
      contains: "pub dashboard: DashboardConfig"
  key_links:
    - from: "crates/iron_hermes_ui/src/server/state.rs"
      to: "crates/iron_hermes_ui/src/server/kanban_ws.rs::run_kanban_tail_loop"
      via: "tokio::spawn at AppState::init"
      pattern: "run_kanban_tail_loop"
    - from: "crates/iron_hermes_ui/src/server/kanban_ws.rs"
      to: "crates/ironhermes-kanban/src/store.rs::list_all_events_after"
      via: "store.lock().await scope before broadcast send"
      pattern: "list_all_events_after"
    - from: "crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs"
      to: "crates/iron_hermes_ui/src/server/kanban_api.rs::fetch_board"
      via: "use_resource"
      pattern: "fetch_board"
---

<objective>
Land the walking skeleton: a Kanban tab rendered inside HermesApp that fetches real tasks from `~/.hermes/kanban.db` via `#[server]` fns, opens a WebSocket to `/api/ws/kanban`, and re-renders the board whenever the dashboard tail consumer pushes a `KanbanWsEvent::TaskEventBatch`. No drag-and-drop, no writes, no drawer in this plan — those are Plan 02 / Plan 03.

Purpose: Establish the data spine + WS infrastructure that Plans 02–04 build on. After this plan, the dashboard is visible (when active_screen is set to Screen::Kanban) and live-updates from any external write to kanban.db.

Output:
- `Screen::Kanban` variant on the screen enum (wheel-nav 11th-wedge wiring is Plan 04's responsibility — Plan 01 leaves the WheelWedge enum at 10 variants).
- `KanbanWsEvent`, `KanbanEventRow`, `PromptPayload` types in `protocol.rs` with wire-format tests.
- Five read-side `#[server]` fns in `kanban_api.rs` (signatures locked, all take `board: Option<String>` per D-19).
- `/api/ws/kanban` route + `run_kanban_tail_loop` polling `list_all_events_after` at 250ms default.
- `ScreenKanban` component rendering 6 columns of real tasks from `fetch_board`, with WS client that calls `board_resource.restart()` on any event.
- `list_all_events_after` added to `KanbanStore` (Wave 0 prerequisite).
- `DashboardConfig` added to `ironhermes-core::Config` with default 250ms.
- `web-sys` features `DragEvent` + `DataTransfer` added to `Cargo.toml` (early so Plan 02 builds cleanly).
- `ironhermes-kanban` added to `Cargo.toml` under `cfg(not(target_arch = "wasm32"))`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-CONTEXT.md
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-RESEARCH.md
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-PATTERNS.md
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-UI-SPEC.md
@crates/iron_hermes_ui/CLAUDE.md
@crates/iron_hermes_ui/Cargo.toml
@crates/iron_hermes_ui/src/server/ws.rs
@crates/iron_hermes_ui/src/server/api.rs
@crates/iron_hermes_ui/src/server/state.rs
@crates/iron_hermes_ui/src/server/mod.rs
@crates/iron_hermes_ui/src/protocol.rs
@crates/iron_hermes_ui/src/state.rs
@crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs
@crates/ironhermes-kanban/src/store.rs
@crates/ironhermes-kanban/src/types.rs
@crates/ironhermes-kanban/src/events.rs
@crates/ironhermes-kanban/src/notifier.rs
@crates/ironhermes-kanban/src/tools/show.rs
@crates/ironhermes-core/src/config.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): Add KanbanStore::list_all_events_after + DashboardConfig + Cargo.toml deps + protocol types + Wave-0 test stubs</name>
  <files>crates/ironhermes-kanban/src/store.rs, crates/ironhermes-core/src/config.rs, crates/iron_hermes_ui/Cargo.toml, crates/iron_hermes_ui/src/protocol.rs, crates/iron_hermes_ui/tests/kanban_server_fns.rs, crates/iron_hermes_ui/tests/kanban_ws_tail.rs</files>
  <read_first>
    crates/ironhermes-kanban/src/store.rs (analog: existing list_terminal_events_after at ~lines 2071-2095, max_event_id at ~lines 2096-2109; PATTERNS.md "Pattern G: TokioMutex<KanbanStore>"),
    crates/ironhermes-kanban/src/events.rs (KanbanEvent struct shape),
    crates/ironhermes-core/src/config.rs (existing `pub kanban: serde_yaml::Value` field at ~line 367 — sibling pattern),
    crates/iron_hermes_ui/Cargo.toml (web-sys features array at lines 24-38; cfg(not(target_arch = "wasm32")) dep block at lines 45-58),
    crates/iron_hermes_ui/src/protocol.rs (ChatStreamEvent at lines 18-44, test_subagent_event_json_shape at lines 47-107),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-CONTEXT.md (D-08, D-17, D-19),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-RESEARCH.md (Risk 3, Risk 6, Q2, Q6, Q7),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-PATTERNS.md (`crates/ironhermes-kanban/src/store.rs (MODIFY — add list_all_events_after)` section)
  </read_first>
  <behavior>
    - `KanbanStore::list_all_events_after(&self, watermark: i64) -> Result<Vec<crate::events::KanbanEvent>>` returns rows with id > watermark, ordered by id ASC, with NO `kind IN (...)` filter (returns ALL kinds).
    - With an empty `task_events` table, the call returns `Ok(vec![])`.
    - After inserting events with ids {1,2,3,4,5}, calling `list_all_events_after(2)` returns events with ids 3,4,5 (in id ASC order).
    - `Config::load` succeeds when YAML omits the `dashboard` key — `config.dashboard.kanban.tail_interval_ms == 250` via `#[serde(default)]` on `DashboardConfig` and `Default for DashboardKanbanConfig`.
    - YAML key `dashboard.kanban.tail_interval_ms: 500` parses to `config.dashboard.kanban.tail_interval_ms == 500`.
    - `protocol::KanbanWsEvent::TaskEventBatch { events: vec![], last_event_id: 42 }` serializes to JSON containing `"TaskEventBatch"` (external tag) and round-trips via `serde_json`.
    - `protocol::PromptPayload::Complete { summary, metadata }` and `PromptPayload::Block { reason }` round-trip via `serde_json`.
    - `crates/iron_hermes_ui/Cargo.toml` `web-sys` features array contains the string `"DragEvent"` AND `"DataTransfer"`.
    - `crates/iron_hermes_ui/Cargo.toml` `[target.'cfg(not(target_arch = "wasm32"))'.dependencies]` block contains a line for `ironhermes-kanban`.
  </behavior>
  <action>
    Step A — `crates/ironhermes-kanban/src/store.rs`: Add `pub fn list_all_events_after(&self, watermark: i64) -> Result<Vec<crate::events::KanbanEvent>>` immediately after the existing `list_terminal_events_after` method. Mirror its structure exactly (prepare statement → query_map → collect) but use the SQL `SELECT id, task_id, run_id, kind, payload, created_at FROM task_events WHERE id > ?1 ORDER BY id ASC` (no `kind IN (...)` filter). Use `rusqlite::params![watermark]`. Add a `#[cfg(test)] mod tests` inline test `list_all_events_after_returns_all_kinds` that opens a temp `KanbanStore` via `KanbanStore::open_for_board("default")` against a `tempfile::TempDir`, creates 3 tasks, asserts the resulting events stream from `list_all_events_after(0)` contains the non-terminal `created` events (proves the filter is not applied), and that `list_all_events_after(very_high_id)` returns empty.

    Step B — `crates/ironhermes-core/src/config.rs`: Add `#[derive(Deserialize, Serialize, Default, Clone, Debug)] #[serde(default)] pub struct DashboardConfig { pub kanban: DashboardKanbanConfig }`. Add `#[derive(Deserialize, Serialize, Clone, Debug)] pub struct DashboardKanbanConfig { pub tail_interval_ms: u64 }` with `impl Default for DashboardKanbanConfig { fn default() -> Self { Self { tail_interval_ms: 250 } } }`. Append `#[serde(default)] pub dashboard: DashboardConfig` to the existing `Config` struct (preserve field ordering — append at the end). Add inline tests: `dashboard_default_tail_interval_is_250` (deserializes empty YAML, asserts `cfg.dashboard.kanban.tail_interval_ms == 250`), and `dashboard_yaml_override` (deserializes `dashboard:\n  kanban:\n    tail_interval_ms: 500`, asserts 500). Implements D-17.

    Step C — `crates/iron_hermes_ui/Cargo.toml`: In the `web-sys` features array (currently at lines 24-38), append `"DragEvent"` and `"DataTransfer"` (Risk 3 — explicit before Plan 02 needs them). In `[target.'cfg(not(target_arch = "wasm32"))'.dependencies]` (lines 45-58), append `ironhermes-kanban = { path = "../ironhermes-kanban" }`. No other Cargo.toml edits — do not bump dioxus, do not change features.

    Step D — `crates/iron_hermes_ui/src/protocol.rs`: After the existing `ChatStreamEvent` enum and before the `#[cfg(test)]` block, add three types (all `#[derive(Serialize, Deserialize, Debug, Clone)]`):
    1. `pub enum KanbanWsEvent { TaskEventBatch { events: Vec<KanbanEventRow>, last_event_id: i64 }, Error { message: String }, Ping {} }`.
    2. `pub struct KanbanEventRow { pub id: i64, pub task_id: String, pub kind: String, pub payload: Option<String>, pub created_at: f64 }`.
    3. `pub enum PromptPayload { Complete { summary: String, metadata: Option<serde_json::Value> }, Block { reason: String } }`.
    Add wire-format tests inside the existing `#[cfg(test)] mod tests` (mirroring `test_subagent_event_json_shape`): `test_kanban_ws_event_task_event_batch_json_shape` asserts the JSON starts with `{"TaskEventBatch":`; `test_kanban_ws_event_ping_json_shape` asserts `{"Ping":{}}`; `test_prompt_payload_complete_round_trip` and `test_prompt_payload_block_round_trip` assert deserialize(serialize(x)) == x. Implements D-08 wire shape, D-13 transport wrapper (Q6 — PromptPayload in protocol, not in ironhermes-kanban).

    Step E — Create Wave-0 test stub files (header-only, no real assertions yet — Wave 1/2 tasks fill in):
    - `crates/iron_hermes_ui/tests/kanban_server_fns.rs` with a `// Plan 01 Wave 0 stub — see 36.3.7.11-VALIDATION.md` header comment and a placeholder `#[test] fn placeholder_will_be_replaced_in_wave_1() { /* TODO Plan 01 Task 2 */ }`.
    - `crates/iron_hermes_ui/tests/kanban_ws_tail.rs` with a header comment naming D-08 / D-15 and a placeholder `#[test] fn placeholder_will_be_replaced_in_wave_1() { /* TODO Plan 01 Task 3 */ }`.

    No fenced code blocks in this action body — the patterns are extracted in `<read_first>` PATTERNS.md cites above.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-kanban list_all_events_after_returns_all_kinds && cargo test -p ironhermes-core --lib -- dashboard_default_tail_interval_is_250 dashboard_yaml_override && cargo test -p iron_hermes_ui --lib -- protocol::tests::test_kanban_ws_event protocol::tests::test_prompt_payload && grep -c '"DragEvent"' crates/iron_hermes_ui/Cargo.toml | xargs test 1 -le && grep -c '"DataTransfer"' crates/iron_hermes_ui/Cargo.toml | xargs test 1 -le && grep -v '^#' crates/iron_hermes_ui/Cargo.toml | grep -c 'ironhermes-kanban' | xargs test 1 -le</automated>
  </verify>
  <done>
    `list_all_events_after` exists in store.rs and its inline test is green. `Config::load` parses YAML without a `dashboard` key (default 250) and with an override (500). `KanbanWsEvent`, `KanbanEventRow`, `PromptPayload` are defined in protocol.rs with round-trip tests green. `Cargo.toml` web-sys features array includes `DragEvent` and `DataTransfer`. `ironhermes-kanban` appears as a `cfg(not(target_arch = "wasm32"))` dep. Wave-0 stub test files exist.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1): kanban_api.rs read-side #[server] fns + AppState tail-broadcast + kanban_ws.rs WS handler + tail loop + server module registry</name>
  <files>crates/iron_hermes_ui/src/server/kanban_api.rs, crates/iron_hermes_ui/src/server/kanban_ws.rs, crates/iron_hermes_ui/src/server/state.rs, crates/iron_hermes_ui/src/server/mod.rs, crates/iron_hermes_ui/tests/kanban_server_fns.rs, crates/iron_hermes_ui/tests/kanban_ws_tail.rs, crates/iron_hermes_ui/tests/kanban_board_read.rs</files>
  <read_first>
    crates/iron_hermes_ui/src/server/api.rs (#[server] fn pattern, #[cfg(feature = "server")] gating at lines 7-9, global_app_state() access at lines 112-113, #[server] body pattern at lines 498-507, spawn_blocking pattern at lines 548-557),
    crates/iron_hermes_ui/src/server/ws.rs (full file — WS_KEEPALIVE_INTERVAL at line 66, send_close_frame at lines 74-86, route annotation at lines 88-92, select! loop at lines 121-127 + 936-964, RAII slot guard at lines 29-52),
    crates/iron_hermes_ui/src/server/state.rs (full file — AppState struct, OnceLock global pattern at lines 84-96, init pattern at lines 99-204),
    crates/iron_hermes_ui/src/server/mod.rs (module registry at lines 1-14),
    crates/ironhermes-kanban/src/store.rs (open_for_board, open_default, list_tasks, get_task, get_events, max_event_id signatures — pattern reuse only, no new method),
    crates/ironhermes-kanban/src/notifier.rs (lock-scope-before-send pattern at lines 302-320; watermark init pattern at lines 426-442 — STRUCTURAL REFERENCE ONLY, do NOT import from notifier per D-15),
    crates/ironhermes-kanban/src/tools/show.rs (worker_context shape at lines 218-232 — Q5 canonical contract),
    crates/iron_hermes_ui/tests/list_sessions_returns_platform_web.rs (analog for kanban_board_read.rs),
    crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs (source-read assertion pattern at lines 1-36),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-CONTEXT.md (D-05, D-08, D-15, D-16, D-19, D-22),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-RESEARCH.md (Q1, Q3, Q5, Q8, Risk 2),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-PATTERNS.md (Pattern A, Pattern C, Pattern G; kanban_api.rs section; kanban_ws.rs section; state.rs MODIFY section; mod.rs MODIFY section)
  </read_first>
  <behavior>
    - `kanban_api::fetch_board(board: Option<String>) -> Result<Vec<protocol::TaskRow>, ServerFnError>` returns all non-archived tasks grouped by status when called with `None`; with a kanban.db containing 3 tasks, the result contains exactly 3 entries.
    - `fetch_task(task_id: String, board: Option<String>)` returns the worker_context JSON shape per show.rs lines 218-232 (task_id, title, body, status, assignee, tenant, workspace, priority, parent_handoffs, prior_attempts, comments).
    - `fetch_task_events(task_id: String, board: Option<String>, limit: u32)` returns up to `limit` newest task_events for the given task (default 20 if limit==0).
    - `fetch_task_runs(task_id: String, board: Option<String>)` returns all task_runs rows for the task (read-only).
    - `fetch_comments(task_id: String, board: Option<String>)` returns all comment rows for the task in insertion order.
    - WS handler at `/api/ws/kanban` accepts a connection, runs a `tokio::select!` over `recv_raw()`, `broadcast_rx.recv()`, and `keepalive.tick()` with `WS_KEEPALIVE_INTERVAL = Duration::from_secs(5)`. On keepalive failure sends close-frame with `CloseCode::Away`.
    - `run_kanban_tail_loop(broadcast_tx, cancel_token, interval_ms)` polls `KanbanStore::list_all_events_after(watermark)` every `interval_ms` milliseconds; on new events: advances the watermark to max(id), serializes `KanbanWsEvent::TaskEventBatch { events, last_event_id }`, calls `broadcast_tx.send(json)` (discards `SendError` when receiver count is 0).
    - `AppState` gains two new fields `pub kanban_tail_broadcast: tokio::sync::broadcast::Sender<String>` and `pub kanban_tail_cancel: tokio_util::sync::CancellationToken`; `AppState::init` spawns `run_kanban_tail_loop` via `tokio::spawn` using `config.dashboard.kanban.tail_interval_ms`.
    - `kanban_ws.rs` source contains the literal string `list_all_events_after` (proves tail uses Wave-0 method) and contains `WS_KEEPALIVE_INTERVAL` and the route annotation `/api/ws/kanban`.
    - `kanban_ws.rs` source does NOT contain `use ironhermes_kanban::notifier` (proves D-15 independence — no shared primitive).
    - `kanban_api.rs` source contains five `#[server]` fns, all with `board: Option<String>` in their argument list (D-19 enforcement).
    - `server/mod.rs` registers both `kanban_api` and `kanban_ws` submodules.
  </behavior>
  <action>
    Step A — Create `crates/iron_hermes_ui/src/server/kanban_api.rs`. Follow `server/api.rs` PATTERNS.md "Pattern A" feature-gated imports: `#[cfg(feature = "server")] use ironhermes_kanban::store::KanbanStore;`, `#[cfg(feature = "server")] use ironhermes_kanban::types::{KanbanStatus, Task};` (and other types as the implementation requires). Define five `#[server]` fns with these exact signatures (the leading return-type wrappers below are the canonical form — implementations follow `api.rs::get_session_messages` lines 498-507 for the spawn_blocking + state.kanban_store lock pattern):

    - `pub async fn fetch_board(board: Option<String>) -> Result<Vec<protocol::TaskRow>, ServerFnError>` — calls `KanbanStore::open_default()` (or `open_for_board(&b)` when Some) inside `tokio::task::spawn_blocking`; reads via `KanbanStore::list_tasks` (or whatever read method already exists for "all non-archived tasks"); maps to a new `protocol::TaskRow` struct mirroring the `Task` shape (define `TaskRow` in `protocol.rs` if it does not exist — same shape as `ironhermes_kanban::types::Task` but plain Serde-derived). NOTE: define `protocol::TaskRow` in this task (extend protocol.rs from Task 1 if needed) so it is shared between client and server without making `ironhermes-kanban` a WASM dep.
    - `pub async fn fetch_task(task_id: String, board: Option<String>) -> Result<protocol::WorkerContextEnvelope, ServerFnError>` — opens store, calls `get_task` and joins `task_links` / `task_runs` / `task_comments` to produce the worker_context shape per show.rs lines 218-232. Define `protocol::WorkerContextEnvelope` in `protocol.rs` to encode that exact field set (Q5 canonical contract).
    - `pub async fn fetch_task_events(task_id: String, board: Option<String>, limit: u32) -> Result<Vec<protocol::KanbanEventRow>, ServerFnError>` — calls `get_events(task_id, limit)`. If `limit == 0`, treat as 20.
    - `pub async fn fetch_task_runs(task_id: String, board: Option<String>) -> Result<Vec<protocol::TaskRunRow>, ServerFnError>` — Define `protocol::TaskRunRow` to carry run_id, outcome, started_at, ended_at, elapsed_ms, summary, error, worker.
    - `pub async fn fetch_comments(task_id: String, board: Option<String>) -> Result<Vec<protocol::CommentRow>, ServerFnError>` — Define `protocol::CommentRow { author, body, created_at }`.

    All five fns acquire `crate::server::state::global_app_state()` and use `tokio::task::spawn_blocking` (Pattern from api.rs:548-557) when calling sync `KanbanStore` methods. Wrap rusqlite errors with `ServerFnError::new(format!(...))`. Implements D-05 (no REST routes), D-19 (board: Option<String>), D-22 (inherits existing session model).

    Step B — Create `crates/iron_hermes_ui/src/server/kanban_ws.rs`. Mirror `server/ws.rs` line-by-line for the WS lifecycle and add a tail-loop function:

    - Top of file: `use dioxus::prelude::*; use dioxus_fullstack::{WebSocketOptions, Websocket};` followed by feature-gated `#[cfg(feature = "server")]` imports for `dioxus_fullstack::{body::Bytes, CloseCode, Message, TypedWebsocket}`, `std::time::Duration`, `tokio::task::JoinHandle`, `tracing::{info, warn}`, `tokio::sync::broadcast`, `tokio_util::sync::CancellationToken`, `ironhermes_kanban::store::KanbanStore`.
    - Constant: `#[cfg(feature = "server")] const WS_KEEPALIVE_INTERVAL: Duration = Duration::from_secs(5);` (D-08 verbatim).
    - Helper `async fn send_close_frame(socket: &mut TypedWebsocket<String,String>, code: CloseCode, reason: &str)` cloned from ws.rs lines 74-86.
    - Route annotation `#[get("/api/ws/kanban")] pub async fn ws_kanban(ws: WebSocketOptions) -> Result<Websocket<String, String>>` mirroring ws.rs lines 88-92. Body subscribes via `let mut broadcast_rx = app_state.kanban_tail_broadcast.subscribe();` and runs the canonical `tokio::select!` over `socket.recv_raw()`, `broadcast_rx.recv()`, `keepalive.tick()` (PATTERNS.md "kanban_ws.rs" select! skeleton). On `broadcast_rx.recv()` success send the JSON string via `socket.send_raw(Message::Text(json))`. Lagged errors are silently dropped. Keepalive failure triggers `send_close_frame(&mut socket, CloseCode::Away, "keepalive failed")` then `break`.
    - Function `pub async fn run_kanban_tail_loop(tx: broadcast::Sender<String>, cancel: CancellationToken, interval_ms: u64)` that:
      1. Opens its own `KanbanStore` via `KanbanStore::open_default()` once at start (persistent connection per Q8) wrapped in `Arc<tokio::sync::Mutex<KanbanStore>>` (Risk 2).
      2. Initializes the watermark via the lock-scoped-before-anything-else pattern: `let mut watermark: i64 = { let s = store.lock().await; s.max_event_id()? };` (notifier.rs lines 426-442 pattern).
      3. Loop: `tokio::select!` over `tokio::time::sleep(Duration::from_millis(interval_ms))` and `cancel.cancelled()`. On tick:
         - Read events: `let events = { let s = store.lock().await; s.list_all_events_after(watermark)? };` — lock released BEFORE send (PATTERNS.md "Pattern G").
         - If non-empty: advance `watermark = events.last().unwrap().id;`; map each `KanbanEvent` to `protocol::KanbanEventRow`; build `KanbanWsEvent::TaskEventBatch { events: rows, last_event_id: watermark }`; serialize with `serde_json::to_string`; call `tx.send(json).ok();` (silently discard SendError when no subscribers, per Q1).
       - Use `tracing::warn!` on store read errors and continue the loop (do not crash the tail consumer).
    - Critically: do NOT add `use ironhermes_kanban::notifier::...` anywhere in this file (D-15 enforcement).

    Step C — Modify `crates/iron_hermes_ui/src/server/state.rs`. Append two fields to the `AppState` struct after `subagent_callback_slot`:
    - `pub kanban_tail_broadcast: tokio::sync::broadcast::Sender<String>`
    - `pub kanban_tail_cancel: tokio_util::sync::CancellationToken`
    In `AppState::init` (before the final `Ok(Self { ... })` block), build the channel and spawn the tail loop per PATTERNS.md "state.rs MODIFY" init pattern: `let (kanban_tail_broadcast, _) = tokio::sync::broadcast::channel::<String>(256); let kanban_tail_cancel = tokio_util::sync::CancellationToken::new(); let tail_tx = kanban_tail_broadcast.clone(); let tail_cancel = kanban_tail_cancel.clone(); let tail_interval_ms = config.dashboard.kanban.tail_interval_ms; tokio::spawn(async move { crate::server::kanban_ws::run_kanban_tail_loop(tail_tx, tail_cancel, tail_interval_ms).await; });` Add both fields to the final `Ok(Self { ... })` constructor. Implements D-15 (independent tail consumer), D-17 (config-driven interval).

    Step D — Modify `crates/iron_hermes_ui/src/server/mod.rs`. After the existing `pub mod ws;` line, append `pub mod kanban_api;` and `pub mod kanban_ws;` (follow the existing conditional-gating shape — `kanban_api` unconditional since `#[server]` macros handle the split; `kanban_ws` may need a `#[cfg(not(feature = "server"))]` no-op stub matching ws.rs's pattern — replicate that exactly).

    Step E — Wave-2 test file scaffolds:
    - `crates/iron_hermes_ui/tests/kanban_board_read.rs` — modeled on `tests/list_sessions_returns_platform_web.rs`. Open a temp `KanbanStore` via `tempfile::tempdir()`, create 3 tasks via `KanbanStore::create_task`, then directly call the store's `list_tasks` API (the `#[server]` fn cannot be tested without a full server runtime — so test the underlying read path). Assert all 3 tasks come back, including the worker_context JSON shape on one of them via a direct call to the equivalent show.rs path. This locks the contract that fetch_board's underlying reads are correct.
    - `crates/iron_hermes_ui/tests/kanban_server_fns.rs` — Replace the Task-1 placeholder with source-string assertions (mirroring tests/websocket_lifecycle_parity.rs lines 1-36): read `src/server/kanban_api.rs` as a string; assert `#[server]` appears at least 5 times; assert each of the five fn names (`fetch_board`, `fetch_task`, `fetch_task_events`, `fetch_task_runs`, `fetch_comments`) appears; assert `board: Option<String>` substring appears at least 5 times (D-19 enforcement); assert no `#[get(\"/api/plugins/kanban` substring (D-05 enforcement).
    - `crates/iron_hermes_ui/tests/kanban_ws_tail.rs` — Replace the Task-1 placeholder with source-string assertions: read `src/server/kanban_ws.rs`; assert `WS_KEEPALIVE_INTERVAL` present; assert `Duration::from_secs(5)` present; assert `tokio::time::interval(WS_KEEPALIVE_INTERVAL)` OR equivalent `keepalive` interval pattern present; assert `list_all_events_after` present (tail uses Wave-0 method); assert `\"/api/ws/kanban\"` route annotation present; assert `send_close_frame` present; assert NO `use ironhermes_kanban::notifier` (D-15 independence).
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --features server 2>&1 | tail -20 && cargo test -p iron_hermes_ui --features server --test kanban_board_read --test kanban_server_fns --test kanban_ws_tail -- --nocapture && cargo clippy -p iron_hermes_ui --features server -- -D warnings 2>&1 | tail -20</automated>
  </verify>
  <done>
    `kanban_api.rs` exports five `#[server]` fns with `board: Option<String>`. `kanban_ws.rs` exports `ws_kanban` route + `run_kanban_tail_loop`. `AppState` has two new fields and `init` spawns the tail loop. `mod.rs` registers both submodules. Three test files pass on a `cargo test --features server` run. Clippy clean (no signal-borrow-across-await diagnostics — none expected this task since no signals are introduced).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (Wave 2): ScreenKanban + KanbanBoard + KanbanColumn + KanbanCard components; WS client + board fetch wiring; Screen::Kanban variant</name>
  <files>crates/iron_hermes_ui/src/state.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/mod.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/board.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/column.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/card.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/mod.rs, crates/iron_hermes_ui/src/components/hermes_app/mod.rs, assets/kanban.css</files>
  <read_first>
    crates/iron_hermes_ui/CLAUDE.md (Dioxus 0.7.1 conventions, no cx/Scope/use_state, no signal borrow across .await),
    crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs (ScreenAgents scaffold at lines 33-62 + screen header at lines 329-362 + spawn-without-signal-across-await at lines 354-357),
    crates/iron_hermes_ui/src/components/hermes_app/screens/mod.rs (existing screen module registry),
    crates/iron_hermes_ui/src/components/hermes_app/mod.rs (screen dispatch — how active_screen Signal is matched to render a screen component),
    crates/iron_hermes_ui/src/state.rs (Screen enum location and shape — Plan 01 adds only Screen::Kanban variant; WheelWedge stays at 10 variants until Plan 04),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-UI-SPEC.md (sections 2.1, 2.2 cyan-glow CSS, 2.3 spacing, 2.4 typography, 2.5 radius, 2.7 motion, 3.5 KanbanBoard props, 3.6 KanbanColumn props, 3.7 KanbanCard props, 3.8 chips, 4.1 board states, 4.2 card states, 5.1 board layout, 6.1 ARIA, 6.4 focus order, 6.6 reduced motion, 7.3 empty state copy),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-CONTEXT.md (D-01, D-02, D-04, D-08, D-09),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-PATTERNS.md (kanban.rs screen scaffold section, KanbanBoard section, KanbanColumn section, KanbanCard section, Pattern E hook discipline),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-RESEARCH.md (Q11 walking skeleton — step 6 board_resource.restart on event)
  </read_first>
  <behavior>
    - `state::Screen::Kanban` variant exists; the `Screen` enum is matched exhaustively at the HermesApp screen dispatch site, and that match arm renders `ScreenKanban { is_active: active_screen() == Screen::Kanban }`.
    - When `kanban.db` contains tasks across all 6 status values, `ScreenKanban` renders six columns each containing a card per task in that status. Empty columns display the empty-state copy from UI-SPEC §7.3.
    - `KanbanCard` renders the title in the heading slot, a thin separator below, and a footer chip row containing STATUS + PRIORITY + ASSIGNEE + AGE per UI-SPEC §3.7. Card has CSS class `kn-card` (cyan-glow border via `kanban.css`).
    - Card has `aria-label` matching UI-SPEC §6.1 format and is keyboard-focusable (`tabindex="0"`).
    - WS client opens to `/api/ws/kanban` on screen mount via `use_websocket` (or equivalent Dioxus 0.7.1 client API); on any `KanbanWsEvent::TaskEventBatch` it calls `board_resource.restart()` (full re-fetch — delta apply is Plan 02 per Q11).
    - All `use_signal` / `use_resource` hooks register unconditionally before any conditional RSX (Pattern E — agents.rs hotfix pattern).
    - `assets/kanban.css` exists and is linked via `document::Link { rel: "stylesheet", href: KANBAN_CSS }` from `screens/kanban.rs`.
    - `cargo clippy -p iron_hermes_ui --features server -- -D warnings` reports zero `GenerationalRefMut`/`WriteLock`-across-await diagnostics.
    - `cargo build -p iron_hermes_ui` (default web feature, WASM target) compiles — this verifies `ironhermes-kanban` types are NOT pulled into the client build (PATTERNS.md Pattern A enforcement).
  </behavior>
  <action>
    Step A — `crates/iron_hermes_ui/src/state.rs`. Find the `pub enum Screen { ... }` declaration and add a `Kanban` variant (placement: after the last existing variant — preserve enum field ordering for any serde-derive). Do NOT touch `WheelWedge` in this plan — that is Plan 04's atomic 6-method update. Add an `impl Default for Screen` only if one already exists and the Default doesn't change. Any exhaustive `match` on Screen in the file that this task touches must gain a `Screen::Kanban => Screen::Kanban` arm or equivalent identity case — but do NOT update screen-dispatch matches outside this file; that wiring is part of Step E. Confirm `cargo check -p iron_hermes_ui --features server` builds (it may warn about non-exhaustive matches elsewhere — those are addressed in Step E).

    Step B — Create `crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/mod.rs`. Declare submodules `pub mod board;`, `pub mod column;`, `pub mod card;`. Re-export the component types: `pub use board::KanbanBoard; pub use column::KanbanColumn; pub use card::KanbanCard;`.

    Step C — Create `crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs` (ScreenKanban). Pattern after agents.rs lines 33-62 (ScreenAgents scaffold). Key elements:
    - Asset constant at module top: `const KANBAN_CSS: Asset = asset!("/assets/kanban.css");`.
    - `#[component] pub fn ScreenKanban(is_active: bool) -> Element` — ALL hooks registered unconditionally at the top:
      - `let mut board_resource = use_resource(move || async move { crate::server::kanban_api::fetch_board(None).await });` (D-18 board=None resolves default).
      - `let tasks: Signal<Vec<protocol::TaskRow>> = use_signal(|| Vec::new());` (Plan 02 will use this for optimistic updates; Plan 01 just mirrors board_resource).
      - `let mut dragged_task_id: Signal<Option<String>> = use_signal(|| None);` (reserved for Plan 02).
      - `let mut open_drawer_task_id: Signal<Option<String>> = use_signal(|| None);` (reserved for Plan 03).
      - `let mut archived_visible: Signal<bool> = use_signal(|| false);` (toolbar toggle — D-09).
      - `let mut ws_state: Signal<WsState> = use_signal(|| WsState::Connecting);` (define `WsState { Connecting, Connected, Reconnecting, Disconnected }` locally as a small enum at the top of kanban.rs).
      - `let mut toast_msg: Signal<Option<String>> = use_signal(|| None);` (reserved for Plan 02).
    - WS client: open a connection to `/api/ws/kanban` using the Dioxus fullstack client websocket API (mirror how `hermes_app/mod.rs` opens `/api/ws/chat` — that is the canonical pattern for this codebase; replicate it precisely). On each received `Message::Text` payload: deserialize into `KanbanWsEvent`; on `TaskEventBatch` call `board_resource.restart()` (D-08 minimal Plan 01 behavior — delta apply is Plan 02). Set `ws_state` based on connection lifecycle.
    - RSX: top-level `section { class: "screen", "data-screen-label": "// MODULE 10 — KANBAN", "is-active": if is_active { "true" } }`. Include `document::Link { rel: "stylesheet", href: KANBAN_CSS }`. Render a `KanbanToolbar`-style header div (just the WS status dot + archived toggle for Plan 01 — search/tenant/profile placeholders deferred to Plan 04 polish), then `KanbanBoard { tasks: tasks.into(), dragged_task_id, on_open_drawer: move |tid| { open_drawer_task_id.set(Some(tid)); } }`. Sync `board_resource`'s Ok value into `tasks` via a `use_effect`-equivalent (Dioxus 0.7.1 pattern: in the body of the resource match arm, set `tasks.set(rows)` — but do NOT hold a write across an await; assignment is fine since `set` does not await).

    Step D — Create the three child components.
    - `screens/kanban/board.rs`: `#[component] pub fn KanbanBoard(tasks: ReadOnlySignal<Vec<protocol::TaskRow>>, dragged_task_id: Signal<Option<String>>, on_open_drawer: EventHandler<String>) -> Element`. Renders a `div { class: "kn-board", role: "region", aria_label: "Kanban board", ... }` containing six `KanbanColumn` children — one per status `[Triage, Todo, Ready, InProgress, Blocked, Done]`. Pass the filtered `Vec<TaskRow>` for each column.
    - `screens/kanban/column.rs`: `#[component] pub fn KanbanColumn(status: protocol::KanbanStatus, tasks: ReadOnlySignal<Vec<protocol::TaskRow>>, dragged_task_id: ReadOnlySignal<Option<String>>) -> Element`. Renders the column header (label + count badge per UI-SPEC §3.6, font-weight 700, --fs-13), and either the cards or the empty-state copy (UI-SPEC §7.3 table) when `tasks.read().is_empty()`. Use `role: "list"`, `aria_label: "{STATUS} column, {N} tasks"`. Do NOT add ondragover/ondrop in this plan — that is Plan 02.
    - `screens/kanban/card.rs`: `#[component] pub fn KanbanCard(task: ReadOnlySignal<protocol::TaskRow>, on_open_drawer: EventHandler<String>) -> Element`. Renders the card per UI-SPEC §3.7 layout: heading (title), separator, footer chips (StatusChip, PriorityChip, AssigneeChip, AgeChip), action button glyph "▶" with `aria_label: "Open {task_title} detail"`. Attributes: `class: "kn-card"`, `role: "listitem"`, `tabindex: "0"`, `aria_label` per UI-SPEC §6.1, and the `draggable: "true"` + `ondragstart`/`ondragend` attributes are reserved-as-stubs (no-op handlers that Plan 02 fills in). NO ondrop or transition validation here. The action button onclick calls `on_open_drawer.call(task.read().id.clone())` — but the drawer itself ships in Plan 03; for Plan 01 this just sets the signal and renders nothing.

    Step E — Wire `Screen::Kanban` into the HermesApp screen dispatch. Read `crates/iron_hermes_ui/src/components/hermes_app/mod.rs` to find the active_screen match (likely around line 110 based on grep evidence). Add a `Screen::Kanban => rsx! { ScreenKanban { is_active: true } }` arm (or whatever shape the surrounding arms use). Update `screens/mod.rs` to export `pub mod kanban; pub use kanban::ScreenKanban;`.

    Step F — Create `assets/kanban.css`. Define `.kn-card`, `.kn-card:hover`, `.kn-card[data-dragging="true"]`, `.kn-card[data-pending="true"]`, `@keyframes kn-pending-pulse`, `.kn-board`, `.kn-column`, `.kn-column-header`, `.kn-chip`, `.kn-chip[data-kind="status"][data-status="…"]` rules — all using only existing CSS variables (`--accent-primary`, `--accent-primary-hi`, `--w-bg-2`, `--w-bg-3`, `--w-border`, `--w-border-hi`, `--w-radius-block`, `--w-radius-pill`, `--w-shadow-block`, `--w-shadow-float`, `--sp-2`, `--sp-4`, `--fs-11`, `--fs-13`, `--fs-14`, `--success`, `--warn`, `--danger`, `--brand`, `--fg`, `--fg-strong`, `--fg-dim`, `--ansi-yellow`, `--ansi-bright-cyan`). Source the cyan-glow declarations from UI-SPEC §2.2 verbatim. Include the `@media (prefers-reduced-motion: reduce)` block from UI-SPEC §6.6 with `.kn-card`, `.kn-drawer`, `.kn-modal`, `.kn-toast`, `.ih-status .dot::after` overrides. Do NOT introduce any new hex values (D-04 enforcement — `grep -E '#[0-9a-fA-F]{3,6}'` of kanban.css MUST return 0 hex literals).
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui 2>&1 | tail -10 && cargo build -p iron_hermes_ui --features server 2>&1 | tail -10 && cargo clippy -p iron_hermes_ui --features server -- -D warnings 2>&1 | tail -20 && grep -vE '^[[:space:]]*(/\*|\*|//)' assets/kanban.css | grep -cE '#[0-9a-fA-F]{3,6}([^0-9a-fA-F]|$)' | xargs -I {} test {} -eq 0 && grep -c 'Screen::Kanban' crates/iron_hermes_ui/src/state.rs | xargs test 1 -le && grep -c '#\[component\]' crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/card.rs | xargs test 1 -le && grep -c 'fetch_board' crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs | xargs test 1 -le</automated>
  </verify>
  <done>
    Setting `active_screen` to `Screen::Kanban` renders the 6-column board with real tasks from `~/.hermes/kanban.db`. WS client is opened to `/api/ws/kanban` and `board_resource.restart()` is called on `TaskEventBatch`. `cargo build` succeeds on both default (web/WASM) and `--features server` targets. Clippy is clean with `-D warnings`. `kanban.css` introduces zero new hex literals.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/ws/kanban | Untrusted WS messages from the browser; server controls what flows back |
| Browser → #[server] fns | Untrusted POST bodies; serde validation + KanbanStore validation |
| iron_hermes_ui process ↔ ~/.hermes/kanban.db | Cross-process WAL boundary (D-16); gateway also writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.11-01-T01 | Tampering | `#[server] fetch_*` fns | mitigate | All store reads use `rusqlite::params!` via existing `KanbanStore` methods; no `format!` SQL anywhere in `kanban_api.rs` |
| T-36.3.7.11-01-T02 | Tampering | Cross-process WAL access to `~/.hermes/kanban.db` | mitigate | SQLite WAL + `busy_timeout(5000ms)` already enforced by `KanbanStore::new_inner`; concurrent gateway-writer + dashboard-reader is the documented WAL contract (RESEARCH Q8) |
| T-36.3.7.11-01-T03 | Tampering | Forged `last_event_id` cursor from client | mitigate | Server controls watermark advancement in `run_kanban_tail_loop`; the client's cursor (when sent during reconnect — Plan 02 work) is advisory only |
| T-36.3.7.11-01-I01 | Information Disclosure | `fetch_task` returns full worker_context | accept | Same data is already exposed via `kanban_show` LLM tool + `hermes kanban show` CLI; auth gate is the existing iron_hermes_ui session model (D-22); localhost-bind threat model (D-23) |
| T-36.3.7.11-01-D01 | Denial of Service | Tail loop polls SQLite every 250ms | accept | ~50µs per query on local SSD (RESEARCH Q1); no rate-limiting added |
| T-36.3.7.11-01-T04 | Tampering | npm/pip/cargo package installs | not-applicable | This plan adds only the workspace path-dep `ironhermes-kanban` (already audited as part of the workspace); no external registry packages |
</threat_model>

<verification>
- `cargo build --workspace` succeeds.
- `cargo test -p iron_hermes_ui --features server kanban_` runs the three new test files and passes.
- `cargo test -p ironhermes-kanban list_all_events_after` passes.
- `cargo test -p ironhermes-core dashboard_default_tail_interval_is_250 dashboard_yaml_override` passes.
- `cargo clippy -p iron_hermes_ui --features server -- -D warnings` is clean.
- `cargo build -p iron_hermes_ui` (default web feature) succeeds — proves `ironhermes-kanban` is correctly gated out of the WASM client build.
- Manual smoke: with `~/.hermes/kanban.db` populated, navigating to Screen::Kanban shows real tasks; running `hermes kanban set <id> status=todo` causes the board to re-render within ~500ms.
</verification>

<success_criteria>
- All three tasks completed; verify commands return zero exit code.
- Decision Coverage: D-02 (Screen::Kanban variant + ScreenKanban component), D-04 (no new hex), D-05 (no REST mount), D-08 (WS lifecycle + 5s Ping + close frame + TypedWebsocket<String,String>), D-09 (six columns + KanbanStatus exact match), D-15 (tail consumer no notifier import), D-16 (WAL cross-process — same kanban.db file), D-17 (config key + default 250), D-18 (board=None default), D-19 (board: Option<String> on every #[server]), D-22 (auth inherited — no new gate), D-23 (no new bind config).
- `must_haves.truths` all observable in the running system.
</success_criteria>

<output>
Create `.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-01-SUMMARY.md` when done.
</output>
