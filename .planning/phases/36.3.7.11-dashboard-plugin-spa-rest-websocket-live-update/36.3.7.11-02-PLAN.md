---
phase: 36.3.7.11
plan: 02
type: execute
wave: 3
depends_on:
  - 36.3.7.11-01
files_modified:
  - crates/iron_hermes_ui/src/server/kanban_api.rs
  - crates/iron_hermes_ui/src/protocol.rs
  - crates/iron_hermes_ui/src/kanban/mod.rs
  - crates/iron_hermes_ui/src/kanban/transitions.rs
  - crates/iron_hermes_ui/src/lib.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/board.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/column.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/card.rs
  - assets/kanban.css
  - crates/iron_hermes_ui/tests/kanban_transitions.rs
  - crates/iron_hermes_ui/tests/kanban_write_fns.rs
autonomous: true
requirements:
  - D-06
  - D-07
  - D-10
  - D-11
  - D-13
  - D-14
  - D-19
must_haves:
  truths:
    - "D-06: ondragover handler in column.rs calls event.prevent_default() on the outer Event<DragData> wrapper; card.rs has draggable=\"true\" + ondragstart/ondragend updating dragged_task_id signal"
    - "D-07: tasks.write() guard scope ends BEFORE spawn(async move {...}); revert path re-acquires write lock inside the spawn body on Err; toast surfaces error via toast_msg signal"
    - "D-10 client-side: kanban::transitions::is_drag_allowed returns true only for (Triage→Todo), (Todo→Ready), (Blocked→Ready), (_→Archived); all other (from, to) pairs return false"
    - "D-10 server-side: patch_task_status rejects disallowed transitions with ServerFnError; Done and Blocked transitions only accepted when PromptPayload is Some (D-14 defense in depth)"
    - "D-11: column drop-invalid overlay renders the hint copy from UI-SPEC §7.4 (e.g., \"Completing a task needs a summary…\"); the same hint populates the live-region announcement"
    - "D-13: four write fns exist in kanban_api.rs with the exact signatures from CONTEXT.md — patch_task_status(task_id, board: Option<String>, new_status, prompt_payload: Option<PromptPayload>), post_comment(task_id, board: Option<String>, body), create_task(board: Option<String>, payload: CreateTaskPayload), run_decompose_or_specify(task_id, board: Option<String>, action: DecomposeOrSpecify)"
    - "D-14: patch_task_status calls kanban::transitions::is_drag_allowed server-side AND validates that summary/reason are non-empty when target is Done/Blocked; mirrors client-side validator exactly with a drift-warning comment"
    - "D-19: every new #[server] fn takes board: Option<String>; when None, opens default board"
    - "D-13 Q9 branch: run_decompose_or_specify either calls the decomposer kernel (branch a) OR returns DecomposeResult::NotWired{message} (branch b) per the Q9 decomposer.rs signature check; the executor MUST check the actual signature and pick the correct branch"
    - "Keyboard DnD alternative per UI-SPEC §6.3: Ctrl/Cmd+Right/Left on a focused card triggers the same is_drag_allowed validator; allowed transitions fire the same optimistic spawn; disallowed transitions announce the hint via live-region"
  artifacts:
    - path: "crates/iron_hermes_ui/src/kanban/transitions.rs"
      provides: "is_drag_allowed + drag_blocked_hint pure fns"
      contains: "is_drag_allowed"
    - path: "crates/iron_hermes_ui/src/server/kanban_api.rs"
      provides: "Four write #[server] fns wired"
      contains: "patch_task_status"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/column.rs"
      provides: "ondragover + ondrop with prevent_default + transition validation"
      contains: "prevent_default"
  key_links:
    - from: "column.rs::ondrop"
      to: "kanban_api::patch_task_status"
      via: "spawn after optimistic write guard drops"
      pattern: "patch_task_status"
    - from: "kanban_api::patch_task_status"
      to: "kanban::transitions::is_drag_allowed"
      via: "server-side validate before store mutation"
      pattern: "is_drag_allowed"
    - from: "card.rs::ondragstart"
      to: "ScreenKanban::dragged_task_id signal (Plan 01)"
      via: "*dragged_task_id.write() = Some(task_id)"
      pattern: "dragged_task_id"
---

<objective>
Land the drag-and-drop client UX + the four write-side `#[server]` fns + the shared transition validator (client copy + server-side enforcement). After this plan, dragging a TRIAGE card to TODO persists via `patch_task_status`; dragging to DONE shows the disallowed hint without firing the write; comment / create / decompose / specify endpoints exist (the drawer + modals in Plan 03 will USE them, but they MUST exist now because Plan 03 depends on Plan 01 only — and the actual write happens from the drawer modal).

Purpose: Close the v1 write path. D-13's four write fns are the canonical write surface. D-06/D-07/D-10/D-11/D-14 are the drag-and-drop interaction contract. The "Decompose/Specify wiring" Open Question (Q9) is resolved here by reading `decomposer.rs`'s actual signature.

Output:
- `src/kanban/transitions.rs` — pure-Rust client-side allowed-transition table + hint table (drift-risk comment per Q10).
- Four `#[server]` write fns in `kanban_api.rs` with the exact D-13 signatures.
- `protocol.rs` extended with `CreateTaskPayload`, `DecomposeOrSpecify`, `DecomposeResult`.
- `column.rs` drop handler: `ondragover` with `prevent_default()`, `ondrop` with optimistic-update + transition validation + clone-and-drop spawn pattern.
- `card.rs` drag source: `draggable="true"`, `ondragstart`, `ondragend`.
- Keyboard DnD alternative (UI-SPEC §6.3) — `Ctrl/Cmd+Arrow` on focused cards.
- `kanban_transitions.rs` test (table-driven, client + server alignment).
- `kanban_write_fns.rs` test (source-string assertion for D-13 signatures + D-19 board param).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-CONTEXT.md
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-RESEARCH.md
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-PATTERNS.md
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-UI-SPEC.md
@.planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-01-SUMMARY.md
@crates/iron_hermes_ui/CLAUDE.md
@crates/iron_hermes_ui/src/protocol.rs
@crates/iron_hermes_ui/src/server/kanban_api.rs
@crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs
@crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/board.rs
@crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/column.rs
@crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/card.rs
@crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs
@crates/ironhermes-kanban/src/store.rs
@crates/ironhermes-kanban/src/types.rs
@crates/ironhermes-kanban/src/decomposer.rs
@crates/ironhermes-kanban/src/specifier.rs
@crates/ironhermes-kanban/src/tools/decompose.rs
@crates/ironhermes-kanban/src/tools/specify.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): kanban::transitions module (client-side validator) + protocol types CreateTaskPayload/DecomposeOrSpecify/DecomposeResult + table-driven transitions test</name>
  <files>crates/iron_hermes_ui/src/kanban/mod.rs, crates/iron_hermes_ui/src/kanban/transitions.rs, crates/iron_hermes_ui/src/lib.rs, crates/iron_hermes_ui/src/protocol.rs, crates/iron_hermes_ui/tests/kanban_transitions.rs</files>
  <read_first>
    crates/iron_hermes_ui/src/lib.rs (top-level module registry — `pub mod kanban;` goes here),
    crates/iron_hermes_ui/src/protocol.rs (Plan 01 added KanbanWsEvent, KanbanEventRow, PromptPayload, TaskRow, WorkerContextEnvelope, TaskRunRow, CommentRow — extend with CreateTaskPayload, DecomposeOrSpecify, DecomposeResult),
    crates/ironhermes-kanban/src/types.rs (KanbanStatus variants — the client copy in protocol.rs MUST match these EXACTLY by name and JSON shape; that is the wire contract),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-CONTEXT.md (D-10 allowed transitions, D-11 blocked transitions, D-13 write fn signatures),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-RESEARCH.md (Q10 transition validator location, Risk 8 Complete/Block require structured input),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-PATTERNS.md (transitions.rs section — analog content from RESEARCH Q10),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-UI-SPEC.md (§7.4 drag-disallowed hint copy — verbatim strings)
  </read_first>
  <behavior>
    - `kanban::transitions::is_drag_allowed(from: KanbanStatus, to: KanbanStatus) -> bool` returns `true` only for: `(Triage, Todo)`, `(Todo, Ready)`, `(Blocked, Ready)`, `(_, Archived)`. All other pairs return `false`. Same-status (e.g., `(Triage, Triage)`) returns false (no-op moves are not drags).
    - `kanban::transitions::drag_blocked_hint(to: KanbanStatus) -> Option<&'static str>` returns the verbatim copy from UI-SPEC §7.4:
      - `Done` → `"Completing a task needs a summary — open the card and click Complete…"`
      - `Blocked` → `"Blocking a task needs a reason — open the card and click Block…"`
      - `Running` or `InProgress` → `"The dispatcher owns this transition — cannot assign running status from UI"`
      - Else: `None`. (Cross-skip and backwards-move hints are handled by the caller using extra context, not by `to` alone.)
    - `protocol::CreateTaskPayload` struct with fields `title: String, assignee: Option<String>, parents: Vec<String>, priority: i64, tenant: Option<String>, body: Option<String>, start_in_triage: bool` and round-trips via serde.
    - `protocol::DecomposeOrSpecify` enum `{ Decompose, Specify }` — external tagging.
    - `protocol::DecomposeResult` enum `{ Ok { children_count: u32, summary: String }, NotWired { message: String } }` — round-trips via serde.
    - `protocol::KanbanStatus` is either re-exported from `ironhermes-kanban` (when feature=server) OR is a wire-compatible local enum (variants exactly `Triage, Todo, Ready, InProgress, Blocked, Done, Archived` in lowercase Serde shape) so the WASM client can match on it.
    - Table-driven test in `tests/kanban_transitions.rs` asserts every (from, to) pair in a 7×7 grid against the expected allow/deny table.
  </behavior>
  <action>
    Step A — `crates/iron_hermes_ui/src/lib.rs`: add `pub mod kanban;` (alongside existing top-level modules). If `lib.rs` already declares a `kanban` module, skip; if iron_hermes_ui is a binary (no lib.rs), add the module to `main.rs` instead.

    Step B — `crates/iron_hermes_ui/src/kanban/mod.rs`: declare `pub mod transitions;`.

    Step C — `crates/iron_hermes_ui/src/kanban/transitions.rs`. Add file header comment naming D-10 + Q10 + the drift risk: "Client-side copy of D-10 allowed transitions. Server-side canonical: kanban_api::patch_task_status. KEEP IN SYNC." Define:
    - `use crate::protocol::KanbanStatus;` (or re-exported equivalent).
    - `pub fn is_drag_allowed(from: KanbanStatus, to: KanbanStatus) -> bool` — body uses a `matches!` macro per RESEARCH Q10 against the four allowed patterns. Exclude same-status (no-op) early.
    - `pub fn drag_blocked_hint(to: KanbanStatus) -> Option<&'static str>` — match on target status, return the UI-SPEC §7.4 hint string or `None`. The strings MUST be the EXACT copy from UI-SPEC §7.4 — not paraphrased.

    Step D — `crates/iron_hermes_ui/src/protocol.rs`: append three new types after the Plan 01 additions:
    - `#[derive(Serialize, Deserialize, Debug, Clone, Default)] pub struct CreateTaskPayload { pub title: String, pub assignee: Option<String>, pub parents: Vec<String>, pub priority: i64, pub tenant: Option<String>, pub body: Option<String>, pub start_in_triage: bool }`.
    - `#[derive(Serialize, Deserialize, Debug, Clone, Copy, PartialEq, Eq)] pub enum DecomposeOrSpecify { Decompose, Specify }`.
    - `#[derive(Serialize, Deserialize, Debug, Clone)] pub enum DecomposeResult { Ok { children_count: u32, summary: String }, NotWired { message: String } }`.

    Verify Plan 01's `KanbanStatus` (wire copy) variant set matches `ironhermes-kanban::types::KanbanStatus` by reading types.rs. If a wire-copy `KanbanStatus` is missing in protocol.rs (Plan 01 may have used `Task` shape with status as `String`), define it now:
    `#[derive(Serialize, Deserialize, Debug, Clone, Copy, PartialEq, Eq, Hash)] #[serde(rename_all = "lowercase")] pub enum KanbanStatus { Triage, Todo, Ready, InProgress, Blocked, Done, Archived }`. Round-trip serde test: `serde_json::to_string(&KanbanStatus::InProgress)` produces `"in_progress"` only if the canonical kanban status variant uses snake_case — confirm against types.rs and pick the matching `rename_all`. The variant set MUST match `KanbanStatus` in `ironhermes-kanban/src/types.rs`.

    Step E — `crates/iron_hermes_ui/tests/kanban_transitions.rs`: table-driven test.
    - Test 1 `is_drag_allowed_allows_the_four_transitions`: build a Vec of expected-true pairs `[(Triage, Todo), (Todo, Ready), (Blocked, Ready), (Triage, Archived), (Todo, Archived), (Ready, Archived), (InProgress, Archived), (Blocked, Archived), (Done, Archived)]`; assert `is_drag_allowed(from, to) == true` for each.
    - Test 2 `is_drag_allowed_denies_everything_else`: iterate the 7×7 grid of (from, to) and for each pair NOT in the allowed set, assert `is_drag_allowed(from, to) == false`. Include `(Triage, Triage)` and other same-status pairs in the denied set.
    - Test 3 `drag_blocked_hint_matches_ui_spec`: assert `drag_blocked_hint(Done) == Some("Completing a task needs a summary — open the card and click Complete…")` (exact byte-for-byte UI-SPEC §7.4 string). Assert for Blocked and InProgress. Assert `drag_blocked_hint(Todo) == None`.
    - Test 4 `protocol_round_trips`: serialize+deserialize each new type and assert equality.
  </action>
  <verify>
    <automated>cargo test -p iron_hermes_ui --test kanban_transitions 2>&1 | tail -20 && cargo build -p iron_hermes_ui 2>&1 | tail -10 && cargo build -p iron_hermes_ui --features server 2>&1 | tail -10</automated>
  </verify>
  <done>
    `kanban::transitions` module exists with both fns. Protocol gains three new types. Table-driven 7×7 transition test passes. Both default and server feature builds succeed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1): Four write-side #[server] fns in kanban_api.rs with server-side transition validation (D-14) + Q9 decomposer signature check + write-fn source-string test</name>
  <files>crates/iron_hermes_ui/src/server/kanban_api.rs, crates/iron_hermes_ui/tests/kanban_write_fns.rs</files>
  <read_first>
    crates/iron_hermes_ui/src/server/kanban_api.rs (Plan 01 — extend the existing module),
    crates/iron_hermes_ui/src/protocol.rs (Task 1 added CreateTaskPayload, DecomposeOrSpecify, DecomposeResult, KanbanStatus),
    crates/iron_hermes_ui/src/kanban/transitions.rs (Task 1 — server reuses is_drag_allowed),
    crates/ironhermes-kanban/src/store.rs (signatures to call: update_task_status, complete_task, block_task, add_comment, create_task),
    crates/ironhermes-kanban/src/decomposer.rs (Q9 critical — read the actual signature of `decompose_triage_task`; report whether it accepts an aux client and what trait/type is required),
    crates/ironhermes-kanban/src/specifier.rs (Q9 — same for `specify_triage_task`),
    crates/ironhermes-kanban/src/tools/decompose.rs (Q9 — the v1 no_aux_client rejection; confirms wiring gap),
    crates/iron_hermes_ui/src/server/api.rs lines 548-557 (spawn_blocking pattern),
    crates/iron_hermes_ui/src/server/state.rs (AppState — does it contain a `resolver` / `AgentRuntime` / `aux_client` field the kernel can consume?),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-CONTEXT.md (D-13, D-14, D-19),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-RESEARCH.md (Q9 decompose/specify wiring, Risk 8 PromptPayload required),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-PATTERNS.md (kanban_api.rs section)
  </read_first>
  <behavior>
    - `patch_task_status(task_id: String, board: Option<String>, new_status: KanbanStatus, prompt_payload: Option<PromptPayload>) -> Result<TaskRow, ServerFnError>`:
      - Open store (default if `board == None`).
      - Read current status via `get_task(task_id)`.
      - Server-side D-14 validation: if `is_drag_allowed(current, new_status)` is FALSE, return `Err(ServerFnError::new("Transition not allowed: {current:?} → {new_status:?}"))` — UNLESS the target is `Done` or `Blocked` with the required `PromptPayload`:
        - `(_, Done) with Some(PromptPayload::Complete { summary, .. })` where `!summary.is_empty()` → call `complete_task(task_id, summary, metadata)`.
        - `(_, Blocked) with Some(PromptPayload::Block { reason })` where `!reason.is_empty()` → call `block_task(task_id, reason)`.
        - `(_, Done) with None` or `(_, Blocked) with None` → return Err.
        - `(_, Done)` with `Some(Block{..})` or `(_, Blocked)` with `Some(Complete{..})` → return Err (wrong payload variant).
      - For allowed transitions (Triage→Todo, Todo→Ready, Blocked→Ready, *→Archived): call `update_task_status(task_id, new_status)`.
      - Return the updated `TaskRow` (re-fetched).
    - `post_comment(task_id: String, board: Option<String>, body: String) -> Result<CommentRow, ServerFnError>`:
      - Empty body returns Err with `"Comment body cannot be empty"`.
      - Calls `KanbanStore::add_comment(task_id, "ui", body)` (author = "ui" or whatever the existing UI session-attributed author string is — read api.rs to discover the convention; if there's no existing convention default to `"ui"`).
      - Returns the inserted `CommentRow`.
    - `create_task(board: Option<String>, payload: CreateTaskPayload) -> Result<TaskRow, ServerFnError>`:
      - Empty title returns Err `"Task title is required"`.
      - Builds the store's create-task call with `payload.title, payload.assignee.unwrap_or("unassigned"), payload.parents, payload.priority, payload.tenant`.
      - If `payload.start_in_triage == true` → call `KanbanStore::create_task` with status=Triage; else with status=Todo.
      - Returns the new `TaskRow`.
    - `run_decompose_or_specify(task_id: String, board: Option<String>, action: DecomposeOrSpecify) -> Result<DecomposeResult, ServerFnError>`:
      - Q9 BRANCH CHECK: read `crates/ironhermes-kanban/src/decomposer.rs::decompose_triage_task` and `specifier.rs::specify_triage_task` signatures. Determine whether `AppState`'s existing fields (resolver / AgentRuntime / provider client) satisfy the kernel's `AuxClient` (or whatever the kernel calls it) trait/argument.
        - **Branch (a)** — `AppState` does satisfy the kernel's aux client: call `decompose_triage_task(&app_state.resolver_or_runtime, task_id, board)` (or equivalent) inside `tokio::task::spawn_blocking` if the kernel is sync, or as a direct `.await` if async. Return `DecomposeResult::Ok { children_count: <from kernel>, summary: <from kernel> }`.
        - **Branch (b)** — `AppState` does NOT satisfy the kernel's aux client: return `Ok(DecomposeResult::NotWired { message: format!("Use: hermes kanban {} {}", action_slug, task_id) })` where `action_slug` is `"decompose"` or `"specify"`.
      - The executor MUST emit a comment at the call site naming the branch chosen and the file:line of the kernel signature consulted, so reviewers can verify the choice.
    - All four fns are gated `#[server]` and take `board: Option<String>` per D-19.
    - Sync `KanbanStore` calls are wrapped in `tokio::task::spawn_blocking` (or hold the existing `Arc<TokioMutex<KanbanStore>>` lock if Plan 01's tail consumer state introduced one; consult the Plan 01 summary).
  </behavior>
  <action>
    Step A — Append four `#[server]` fns to `crates/iron_hermes_ui/src/server/kanban_api.rs`. Use feature-gated imports for the kanban-side types (`#[cfg(feature = "server")] use ironhermes_kanban::store::KanbanStore;` etc.). Import `crate::kanban::transitions::is_drag_allowed` for the server-side D-14 validator. Mirror the spawn_blocking pattern from `api.rs::api_agents_kill` (lines 548-557).

    Write the `patch_task_status` body with explicit branching:
    1. Acquire `let state = crate::server::state::global_app_state();`.
    2. `spawn_blocking` wrapping: open store, get_task, branch on (current_status, new_status, prompt_payload):
       - If `prompt_payload` is None AND `is_drag_allowed(current, new_status)`: `update_task_status(task_id, new_status)`.
       - If `prompt_payload` is `Some(Complete{summary, metadata})` AND `new_status == Done` AND `!summary.is_empty()`: `complete_task(task_id, summary, metadata.map(|m| m.to_string()))`.
       - If `prompt_payload` is `Some(Block{reason})` AND `new_status == Blocked` AND `!reason.is_empty()`: `block_task(task_id, reason)`.
       - Else: return Err with a descriptive message naming the rejected pair.
    3. Re-fetch the updated task via `get_task` and return.

    `post_comment` body: validate non-empty, open store, `add_comment(task_id, "ui", body)`, return the new CommentRow.

    `create_task` body: validate non-empty title, open store, call the existing `KanbanStore::create_task` with the payload's fields and the start_in_triage-derived initial status.

    `run_decompose_or_specify` body — Q9 BRANCH:
    - Read `decomposer.rs::decompose_triage_task` and `specifier.rs::specify_triage_task`. Identify the trait/parameter for the aux client (likely a `&dyn AuxClient` or similar — search for the actual name).
    - Search `AppState` (`server/state.rs`) for a field that can satisfy that interface: look for `resolver`, `runtime`, `AgentRuntime`, `provider_resolver`, or similar.
    - If found: BRANCH (a). Wire the call. Insert source comment: `// Q9 BRANCH (a): wired against AppState.<field> — decomposer.rs:<line> takes <trait>`.
    - If NOT found: BRANCH (b). Return `DecomposeResult::NotWired { message: format!("Use: hermes kanban {} {}", slug, task_id) }`. Insert source comment: `// Q9 BRANCH (b): AppState lacks aux client; decomposer.rs:<line> requires <trait>. UI surfaces NotWired tooltip.`

    Step B — Tests in `crates/iron_hermes_ui/tests/kanban_write_fns.rs`. Source-string assertions (the runtime behavior is exercised in Plan 05 UAT):
    - Assert the file contains `fn patch_task_status`, `fn post_comment`, `fn create_task`, `fn run_decompose_or_specify` (each `#[server]`-annotated).
    - Assert each fn signature line contains `board: Option<String>` (D-19, 4 occurrences).
    - Assert `use crate::kanban::transitions::is_drag_allowed` import is present (D-14 server-side reuse).
    - Assert the `patch_task_status` body contains a reference to `is_drag_allowed` (server-side validation is called, not just imported).
    - Assert NO `format!` calls with `{task_id}` directly embedded in SQL (`grep -E 'format!\(.*SELECT|format!\(.*INSERT|format!\(.*UPDATE|format!\(.*DELETE'` returns 0 occurrences in kanban_api.rs — SQL injection mitigation).
    - Assert the `run_decompose_or_specify` fn body contains either `DecomposeResult::Ok` (branch a wired) OR `DecomposeResult::NotWired` (branch b explicit) — not both as the only return paths, since one of them is the chosen branch.
    - Assert the Q9-branch comment string appears in the file (`grep -c 'Q9 BRANCH'` ≥ 1).
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --features server 2>&1 | tail -10 && cargo test -p iron_hermes_ui --test kanban_write_fns 2>&1 | tail -20 && cargo clippy -p iron_hermes_ui --features server -- -D warnings 2>&1 | tail -20 && grep -E 'format!\(.*(SELECT|INSERT|UPDATE|DELETE)' crates/iron_hermes_ui/src/server/kanban_api.rs | grep -c '' | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    Four write fns present, signed exactly per D-13, all with board: Option<String> per D-19. D-14 server-side validation calls is_drag_allowed AND validates summary/reason. Q9 branch documented at the call site. No format! SQL anywhere in kanban_api.rs. Clippy clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (Wave 2): Drag-and-drop client wiring (card.rs + column.rs + board.rs) with optimistic update + keyboard DnD alternative + drop-invalid visual</name>
  <files>crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/board.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/column.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/card.rs, assets/kanban.css</files>
  <read_first>
    crates/iron_hermes_ui/CLAUDE.md (signal-write-then-drop-before-spawn rule; Q4 canonical pattern in PATTERNS.md "Pattern B"),
    crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs (Plan 01 — has dragged_task_id, tasks, pending_task_ids, toast_msg signals registered),
    crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/card.rs (Plan 01 — stub draggable + ondragstart/ondragend),
    crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/column.rs (Plan 01 — no drop handlers yet),
    crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs lines 350-357 (canonical spawn-without-signal-across-await pattern),
    crates/iron_hermes_ui/src/kanban/transitions.rs (Task 1 — is_drag_allowed + drag_blocked_hint),
    crates/iron_hermes_ui/src/server/kanban_api.rs (Task 2 — patch_task_status),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-CONTEXT.md (D-06 native DnD blueprint, D-07 optimistic+revert, D-10/D-11 transition tables),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-UI-SPEC.md (§3.7 card props + attributes, §3.6 column props, §4.1 board states drag-valid/drag-invalid, §4.2 card drag states, §6.3 keyboard DnD alternative, §6.5 live region announcements, §7.4 drag-disallowed hint copy, §7.5 toast copy, §8.1 drag-and-drop full spec),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-PATTERNS.md (Pattern B no-await-borrow, Pattern F prevent_default, column.rs blueprint, card.rs draggable attribute, board.rs optimistic update),
    .planning/phases/36.3.7.11-dashboard-plugin-spa-rest-websocket-live-update/36.3.7.11-RESEARCH.md (Risk 4 prevent_default, Risk 5 signal borrow, Q3 Dioxus event wiring)
  </read_first>
  <behavior>
    - `card.rs` (KanbanCard): `div { draggable: "true", data_dragging: if is_dragging() { "true" }, data_pending: if is_pending() { "true" }, role: "listitem", aria_label: "{title} — {status}, priority {priority}, assigned to {assignee}", tabindex: "0", id: "kn-card-action-{task.id}", ondragstart: move |_| { *dragged_task_id.write() = Some(task_id.clone()); }, ondragend: move |_| { *dragged_task_id.write() = None; }, onkeydown: move |evt: Event<KeyboardData>| { handle_keyboard_dnd(evt, ...) }, ... }`.
    - `column.rs` (KanbanColumn): `div { class: "kn-column", "data-status": status.as_str(), "data-drag-valid": if is_valid_target() { "true" }, "data-drag-invalid": if is_invalid_target() { "true" }, ondragover: move |event: Event<DragData>| { event.prevent_default(); /* compute valid/invalid target based on dragged_task_id + this column's status */ }, ondrop: move |_| { drop_handler() }, ... }`.
    - On `ondragover`: read `dragged_task_id`, look up the source task's status from `tasks` signal, call `is_drag_allowed(current, this_column_status)`. Set `is_valid_target` if true; set `is_invalid_target` if false and the dragged_task_id is Some. On disallowed target, render the hint copy from `drag_blocked_hint(this_column_status)` as a tooltip / overlay.
    - On `ondrop`:
      1. Read `dragged_task_id` clone; if None, return.
      2. Look up source task's current status from `tasks` signal.
      3. If `is_drag_allowed(current, target_status) == false`:
         - Do NOT mutate `tasks`. Do NOT call patch_task_status.
         - Append the hint copy to the live-region announcement.
         - Surface a brief toast `toast_msg.set(Some(<hint>))`.
         - Return.
      4. If target_status is Archived: open ArchiveConfirmModal (Plan 03 wires the modal; for Plan 02 set a placeholder signal `archive_modal_task: Signal<Option<String>> = Some(task_id)` — Plan 03 reads it).
      5. Otherwise:
         - Optimistic update — scoped write block:
           - `{ let mut w = tasks.write(); if let Some(t) = w.iter_mut().find(|t| t.id == tid) { t.status = target_status; } }` — write guard drops at the closing brace.
         - Insert `task_id` into `pending_task_ids` (separate scoped write).
         - Clone owned values.
         - `spawn(async move { ... })` calling `patch_task_status(tid_owned, None, target_status_owned, None).await`.
         - Inside the spawn body:
           - On Ok: scoped write to remove `task_id` from `pending_task_ids`.
           - On Err: re-acquire `tasks.write()` (new write lock — no outstanding borrow per Pattern B), revert the status in the matched task, remove from `pending_task_ids`, and set `toast_msg` to `format!("Move failed — \"{title}\" returned to {original_status}. {e}")` per UI-SPEC §7.5.
      6. Always: `dragged_task_id.set(None)` after the spawn dispatches (the `ondragend` handler also clears it; this is a safety net).
    - Keyboard DnD alternative (UI-SPEC §6.3): on `Ctrl+Right` / `Cmd+Right` / `Ctrl+Left` / `Cmd+Left` while a card has focus:
      1. Determine next column to the right/left in the visible column order.
      2. Run the SAME validator (`is_drag_allowed`).
      3. If allowed: fire the same optimistic spawn as ondrop. Announce via live region: "Task '{title}' moved to {TARGET STATUS}".
      4. If disallowed: announce via live region: `drag_blocked_hint(target).unwrap_or("This transition skips lifecycle steps — drag to TODO first")`.
      `Ctrl+Shift+Right/Left` is NOT used (browser zoom collision). `Alt+Right/Left` is the documented fallback to verify in browser testing.
    - `assets/kanban.css` additions:
      - `.kn-column[data-drag-valid="true"] { box-shadow: inset 0 0 0 1px var(--accent-primary); background: color-mix(in srgb, var(--accent-primary) 5%, transparent); }` per UI-SPEC §2.6.
      - `.kn-column[data-drag-invalid="true"] { box-shadow: inset 0 0 0 1px var(--danger); background: color-mix(in srgb, var(--danger) 5%, transparent); cursor: not-allowed; }` per UI-SPEC §2.6.
      - `.kn-column[data-drag-invalid="true"] .kn-drag-hint { display: block; }` (the hint copy renders when the column is the invalid target).
      - `.kn-drag-hint { display: none; padding: var(--sp-2); color: var(--danger); font-size: var(--fs-11); }`.
      - Body during drag: `body[data-kn-dragging="true"] { cursor: grabbing; }`. Toggle the body attribute via a small `use_effect` watching `dragged_task_id`.
      - All new CSS uses existing tokens — no new hex.
  </behavior>
  <action>
    Step A — `crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/card.rs`. Replace the Plan 01 stubs in the existing `KanbanCard` component:
    - Add the `draggable: "true"` attribute.
    - Add `id: "kn-card-action-{task.read().id}"` (used for focus restore from drawer in Plan 03).
    - `ondragstart: move |_| { *dragged_task_id.write() = Some(task.read().id.clone()); }`.
    - `ondragend: move |_| { *dragged_task_id.write() = None; }`.
    - `onkeydown: move |evt: Event<KeyboardData>|` — call helper `handle_card_keyboard_dnd(evt, &task, &tasks, &pending_task_ids, &toast_msg, &live_region_msg)` defined as a private fn in card.rs that:
      1. Checks `evt.modifiers().ctrl() || evt.modifiers().meta()` (Cmd on Mac).
      2. Checks `evt.key() == Key::ArrowRight || evt.key() == Key::ArrowLeft`.
      3. Looks up the current column index by mapping status to column position.
      4. Computes target_status (next column right/left).
      5. Calls the same validator + optimistic spawn path as ondrop. Reuse a shared helper `move_task_optimistic(...)` (define in board.rs or a kanban/utils.rs).
    - Add `data-pending: if pending_task_ids.read().contains(&task.read().id) { "true" }` attribute.
    - Card no longer renders the drawer-open click handler directly — it emits via `on_open_drawer: EventHandler<String>` prop (already in Plan 01 spec).

    Step B — `crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/column.rs`. Replace the Plan 01 stub component body:
    - Add `ondragover: move |event: Event<DragData>| { event.prevent_default(); /* compute valid/invalid */ }`. The `prevent_default()` call is on the outer `Event<DragData>` wrapper per Risk 4 — do NOT call it on the inner `DragData`.
    - Add `ondrop: move |_event|` calling the shared helper `move_task_optimistic(dragged_task_id, target_status=this_column_status, tasks, pending_task_ids, toast_msg, archive_modal_task)`.
    - Compute `is_valid_target` and `is_invalid_target` via `use_memo`: based on `dragged_task_id.read()`, look up the source task's status (from `tasks.read()`), and call `is_drag_allowed`. If `dragged_task_id` is None, both are false.
    - Render `data-drag-valid` / `data-drag-invalid` attributes per the memo.
    - When `is_invalid_target == true`, render a `div { class: "kn-drag-hint" }` containing `drag_blocked_hint(this_column_status).unwrap_or("This transition skips lifecycle steps — drag to TODO first")`.

    Step C — `crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/board.rs`. Define the shared helper `pub fn move_task_optimistic(...)` (or inline in column.rs; place it where reuse is most natural — board.rs is best because both keyboard nav and drop call it). The helper:
    1. Reads source task from `tasks`.
    2. If `is_drag_allowed == false`: announce hint via `live_region_msg.set(...)` and toast. Return.
    3. If target == Archived: `archive_modal_task.set(Some(task_id))`. Return.
    4. Otherwise: scoped write to mutate `tasks` (status update) — write guard drops at brace close.
    5. Scoped write to add `task_id` to `pending_task_ids`.
    6. Clone owned task_id, target_status, source_status.
    7. `spawn(async move { match patch_task_status(...).await { Ok(_) => { /* clear pending */ } Err(e) => { /* revert + toast */ } } })`.
    8. Outside the spawn (synchronous): `dragged_task_id.set(None)`.

    Step D — `crates/iron_hermes_ui/src/components/hermes_app/screens/kanban.rs`. Add `live_region_msg: Signal<Option<String>>` and `archive_modal_task: Signal<Option<String>>` to the unconditional hook block. Render a hidden `div { role: "log", aria_live: "polite", aria_label: "Board updates", style: "position: absolute; left: -10000px;", "{live_region_msg.read().clone().unwrap_or_default()}" }` per UI-SPEC §6.1 + §6.5. Add the body attribute toggle: `use_effect(move || { let dragging = dragged_task_id.read().is_some(); web_sys::window().and_then(|w| w.document()).and_then(|d| d.body()).map(|b| { if dragging { b.set_attribute("data-kn-dragging", "true").ok() } else { b.remove_attribute("data-kn-dragging").ok() } }); })`. Pass `archive_modal_task` and `live_region_msg` down through `KanbanBoard` → `KanbanColumn` props.

    Step E — `assets/kanban.css` additions per behavior table above.

    Step F — Extend `tests/kanban_write_fns.rs` (or create `tests/kanban_dnd_wiring.rs` if cleaner) with source-string assertions:
    - `card.rs` source contains `draggable: "true"`, `ondragstart`, `ondragend`.
    - `column.rs` source contains `ondragover`, `ondrop`, `prevent_default()`.
    - `column.rs` ondragover handler signature is `Event<DragData>` (assert substring `Event<DragData>`).
    - `column.rs` does NOT call `prevent_default()` on `DragData` directly — assert substring `event.prevent_default()` appears AND substring `.dyn_into::<DragData>().prevent_default` does NOT (or any similar inner-prevent_default pattern).
    - `column.rs` or `board.rs` source references `is_drag_allowed` (proves client-side validation is reachable from ondrop).
    - Card has `id: "kn-card-action-` substring (proves Plan 03 drawer-close focus restore is plumbed).
    - Live-region rendering is wired: `kanban.rs` source contains `role: "log"` and `aria_live: "polite"`.
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui 2>&1 | tail -10 && cargo build -p iron_hermes_ui --features server 2>&1 | tail -10 && cargo clippy -p iron_hermes_ui --features server -- -D warnings 2>&1 | tail -20 && cargo test -p iron_hermes_ui --test kanban_write_fns 2>&1 | tail -10 && grep -c 'event.prevent_default()' crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/column.rs | xargs test 1 -le && grep -c 'draggable: "true"' crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/card.rs | xargs test 1 -le && grep -c 'is_drag_allowed' crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/column.rs crates/iron_hermes_ui/src/components/hermes_app/screens/kanban/board.rs | grep -v ':0' | wc -l | xargs test 1 -le