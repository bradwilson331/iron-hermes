---
phase: 33
plan: 03
type: execute
wave: 3
depends_on: [33-02]
files_modified:
  - crates/ironhermes-cli/src/toolset_cmd.rs
  - crates/ironhermes-tools/src/toolset_session.rs
  - crates/ironhermes-core/src/constants.rs
  - crates/ironhermes-agent/src/app_runtime_factory.rs
  - crates/ironhermes-agent/tests/invariants_33.rs
autonomous: true
requirements: [LEARN-03, LEARN-04, LEARN-05]

must_haves:
  truths:
    - "hermes toolset list includes 'learning' as a known toolset after this plan"
    - "KNOWN_TOOLSETS has exactly 8 entries; count assertion updated to 8"
    - "toolset_members_map() in both toolset_cmd.rs and toolset_session.rs maps 'learning' to ['skill_manage']"
    - "DEFAULT_TOOLSETS in constants.rs includes 'learning' (no external prereqs)"
    - "app_runtime_factory calls register_skill_manage_tool when 'learning' toolset is active"
    - "All 6 INV-33-* static-grep invariant tests pass"
    - "CLI-session toolset agreement test still passes after adding 'learning' to both maps"
  artifacts:
    - path: "crates/ironhermes-cli/src/toolset_cmd.rs"
      provides: "'learning' in KNOWN_TOOLSETS + toolset_members_map + count assertion = 8"
      contains: "learning"
    - path: "crates/ironhermes-tools/src/toolset_session.rs"
      provides: "members_map 'learning' -> ['skill_manage']"
      contains: "learning"
    - path: "crates/ironhermes-core/src/constants.rs"
      provides: "DEFAULT_TOOLSETS includes 'learning'"
      contains: "learning"
    - path: "crates/ironhermes-agent/src/app_runtime_factory.rs"
      provides: "conditional register_skill_manage_tool call when 'learning' toolset enabled"
      contains: "register_skill_manage_tool"
    - path: "crates/ironhermes-agent/tests/invariants_33.rs"
      provides: "6 static-grep regression gates for Phase 33 wiring"
      contains: "INV-33"
  key_links:
    - from: "toolset_cmd.rs toolset_members_map"
      to: "toolset_session.rs members_map"
      via: "toolset_members_map_agrees_with_registry_toolset_session agreement test (~line 700)"
      pattern: "learning"
    - from: "app_runtime_factory.rs"
      to: "registry.register_skill_manage_tool"
      via: "conditional call when 'learning' toolset enabled"
      pattern: "register_skill_manage_tool"
---

<objective>
Complete Phase 33 by wiring the 'learning' toolset into all four registration surfaces (KNOWN_TOOLSETS, toolset_members_map, DEFAULT_TOOLSETS, app_runtime_factory), then lock every call site with static-grep invariant tests.

Purpose: skill_manage is dead code until it is discoverable via `hermes toolset enable learning`, present in the runtime members map, default-enabled on fresh install, and conditionally registered in the agent runtime factory. The invariant tests in invariants_33.rs are the Phase 33 equivalent of the Phase 22.4 INV-22.4-* regression suite — they prevent future refactors from silently breaking the wiring.

Output: Fully wired 'learning' toolset; 6 INV-33-* tests green; full workspace test suite clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-autonomous-skill-creation/33-RESEARCH.md
@.planning/phases/33-autonomous-skill-creation/33-01-SUMMARY.md
@.planning/phases/33-autonomous-skill-creation/33-02-SUMMARY.md
</context>

<interfaces>
From crates/ironhermes-cli/src/toolset_cmd.rs (lines 15-17):
  const KNOWN_TOOLSETS: &[&str] = &["web", "code", "memory", "agent", "skills", "session", "browser"];
  Currently 7 entries. Target: 8 entries with "learning" appended.

Count assertion (~line 479):
  assert_eq!(KNOWN_TOOLSETS.len(), 7, "KNOWN_TOOLSETS must have exactly 7 entries after Phase 25.1 extension")
  Update count to 8; update message to "...8 entries after Phase 33 addition of learning toolset".

toolset_members_map() function (~line 235 in toolset_cmd.rs):
  fn toolset_members_map() -> HashMap<&'static str, &'static [&'static str]>
  Pattern: m.insert("memory", &["memory"]); m.insert("skills", &["skills"]);
  Add: m.insert("learning", &["skill_manage"]);

From crates/ironhermes-tools/src/toolset_session.rs (~lines 50-70):
  fn members_map() -> HashMap<&'static str, &'static [&'static str]>
  Pattern: m.insert("memory", &["memory"]); m.insert("session", &["session_search"]);
  Add: m.insert("learning", &["skill_manage"]);

From crates/ironhermes-core/src/constants.rs (line 39):
  pub const DEFAULT_TOOLSETS: &[&str] = &["memory", "session", "agent", "skills", "robotics"];
  Add "learning". Rationale: no external prereqs (same as "memory", "session").

From crates/ironhermes-tools/src/registry.rs (added in Plan 02):
  pub fn register_skill_manage_tool(&mut self)

Existing agreement test in toolset_cmd.rs (~line 700):
  fn toolset_members_map_agrees_with_registry_toolset_session
  This test compares CLI map keys with RegistryToolsetSession map keys.
  It passes automatically once "learning" is added to both maps.

For invariants_33.rs: find an existing invariants_*.rs file under
crates/ironhermes-agent/tests/ to get the exact grep helper pattern (file path
read + string.matches(pattern).count() assertion shape).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Register 'learning' in KNOWN_TOOLSETS, toolset_members_map (both), and DEFAULT_TOOLSETS</name>
  <files>crates/ironhermes-cli/src/toolset_cmd.rs, crates/ironhermes-tools/src/toolset_session.rs, crates/ironhermes-core/src/constants.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/toolset_cmd.rs lines 15-30 — read KNOWN_TOOLSETS array (current 7 entries and their order)
    - crates/ironhermes-cli/src/toolset_cmd.rs lines 235-290 — read toolset_members_map() function body (find where to insert "learning" entry)
    - crates/ironhermes-cli/src/toolset_cmd.rs lines 465-490 — read the count assertion test (get exact current assertion message to update correctly)
    - crates/ironhermes-tools/src/toolset_session.rs lines 40-90 — read members_map() function body
    - crates/ironhermes-core/src/constants.rs lines 30-55 — read DEFAULT_TOOLSETS declaration and adjacent doc comment
  </read_first>
  <action>
    Five targeted edits across three files. Make no other changes.

    toolset_cmd.rs edit 1 — KNOWN_TOOLSETS array: append "learning" as the 8th entry after "browser".
    Result: &["web", "code", "memory", "agent", "skills", "session", "browser", "learning"]

    toolset_cmd.rs edit 2 — Count assertion: change 7 to 8 and update the message string to
    "KNOWN_TOOLSETS must have exactly 8 entries after Phase 33 addition of learning toolset".

    toolset_cmd.rs edit 3 — toolset_members_map() body: add
    m.insert("learning", &["skill_manage"]); // Phase 33
    after the m.insert("session", ...) line (or in the same alphabetical/logical group as memory/skills/session).

    toolset_session.rs edit — members_map() body: add
    m.insert("learning", &["skill_manage"]); // Phase 33
    after the m.insert("session", ...) line.

    constants.rs edit — DEFAULT_TOOLSETS: append "learning" to the slice.
    Result: &["memory", "session", "agent", "skills", "robotics", "learning"]
    Update the adjacent doc comment (line ~33) to mention "learning" is included by default.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cli -- toolset 2>&amp;1 | tail -15</automated>
    <automated>cargo test -p ironhermes-tools -- toolset 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '"learning"' crates/ironhermes-cli/src/toolset_cmd.rs returns >= 2 (array entry + members_map insert)
    - grep '"learning"' crates/ironhermes-tools/src/toolset_session.rs returns 1 line (members_map insert)
    - grep "DEFAULT_TOOLSETS" crates/ironhermes-core/src/constants.rs shows "learning" in the slice value
    - cargo test -p ironhermes-cli -- toolset exits 0 (count assertion passes at 8; agreement test passes)
    - cargo test -p ironhermes-tools -- toolset exits 0
    - cargo build --workspace exits 0
  </acceptance_criteria>
  <done>KNOWN_TOOLSETS.len() == 8; both members_maps contain "learning"; DEFAULT_TOOLSETS includes "learning"; all toolset tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Wire register_skill_manage_tool in app_runtime_factory + write invariants_33.rs</name>
  <files>crates/ironhermes-agent/src/app_runtime_factory.rs, crates/ironhermes-agent/tests/invariants_33.rs</files>
  <read_first>
    - crates/ironhermes-agent/src/app_runtime_factory.rs — read the full file. Find: (a) the toolset-gated conditional registration block (look for how register_memory_tool is called conditionally when "memory" toolset is active — the exact expression for checking active toolsets); (b) where prompt_builder config is assembled, specifically any block that reads config.learning fields (this is where skill_creation_guidance is wired if not already done in Plan 01's prompt_builder.rs change).
    - crates/ironhermes-agent/tests/ — run ls to list existing invariants_*.rs files, then read the first one (first 80 lines) to capture the exact include_str!/read_to_string helper pattern and assertion shape used in this project.
  </read_first>
  <action>
    app_runtime_factory.rs: find the section that checks active toolsets and conditionally calls register_memory_tool (or similar conditional registration). Add the analogous block immediately after the memory toolset block:
      if enabled_toolsets.contains("learning") {
          registry.register_skill_manage_tool();
      }
    Use whatever the actual toolset-check expression is (may be a HashSet contains, a slice any(), or a config field check — read the file first). Place it adjacent to the "memory" toolset block for logical grouping.

    invariants_33.rs: create the file. Copy the module-level structure and file-reading helper from the existing invariants file. Write 6 tests:

    INV-33-01: "register_skill_manage_tool called in app_runtime_factory" — reads crates/ironhermes-agent/src/app_runtime_factory.rs; asserts .matches("register_skill_manage_tool").count() >= 1

    INV-33-02: "skill_creation_guidance field wired in prompt_builder or config" — reads crates/ironhermes-agent/src/prompt_builder.rs; asserts .matches("skill_creation_guidance").count() >= 1

    INV-33-03: "SkillSource SelfCreated exists in skills.rs" — reads crates/ironhermes-core/src/skills.rs; asserts .matches("SelfCreated").count() >= 3

    INV-33-04: "validate_skill_name is pub in skills.rs" — reads crates/ironhermes-core/src/skills.rs; asserts .matches("pub fn validate_skill_name").count() >= 1

    INV-33-05: "skill_manage module declared in ironhermes-tools lib.rs" — reads crates/ironhermes-tools/src/lib.rs; asserts .matches("mod skill_manage").count() >= 1

    INV-33-06: "learning toolset in KNOWN_TOOLSETS in toolset_cmd.rs" — reads crates/ironhermes-cli/src/toolset_cmd.rs; asserts .matches("\"learning\"").count() >= 2

    Module-level comment: // Phase 33: static-grep regression gates for autonomous skill creation wiring.

    Use workspace-relative paths (e.g. "crates/ironhermes-core/src/skills.rs") with std::fs::read_to_string — verify this matches the pattern in the existing invariants file (some projects use include_str! macros instead; match whatever the project uses).
  </action>
  <verify>
    <automated>cargo test -p ironhermes-agent -- invariants_33 2>&amp;1 | tail -15</automated>
    <automated>cargo test --workspace 2>&amp;1 | grep -E "^(test result|FAILED)" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p ironhermes-agent -- invariants_33 shows 6 tests, 6 passed
    - grep "register_skill_manage_tool" crates/ironhermes-agent/src/app_runtime_factory.rs returns >= 1 line
    - File crates/ironhermes-agent/tests/invariants_33.rs exists with >= 6 #[test] functions
    - cargo test --workspace exits 0 (full suite green — this is the Phase 33 completion gate)
  </acceptance_criteria>
  <done>6 INV-33-* tests pass; register_skill_manage_tool wired in app_runtime_factory; cargo test --workspace exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config DEFAULT_TOOLSETS -> learning enabled by default | learning enabled on fresh install; no external prereqs — same risk profile as "memory" |
| app_runtime_factory -> registry | conditional registration is compile-time verified; no dynamic loading or reflection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03-A | Information Disclosure | DEFAULT_TOOLSETS enabling learning by default | accept | skill_manage writes only to HERMES_HOME/skills/; no network calls; no credential access; identical risk profile to "memory" toolset |
| T-33-03-B | Elevation of Privilege | Agent autonomously writing SKILL.md after task completion | mitigate | scan_skill_content pre-write gate enforced in Plan 02; SkillSource::SelfCreated in WARN-BUT-LOAD arm enforced in Plan 01; no privilege escalation path through this wiring layer |
| T-33-03-SC | Tampering | cargo workspace dependencies | accept | No new external packages in this plan. |
</threat_model>

<verification>
cargo test -p ironhermes-cli -- toolset
cargo test -p ironhermes-tools -- toolset
cargo test -p ironhermes-agent -- invariants_33
cargo test --workspace 2>&amp;1 | grep -E "^(test result|FAILED|error\[)"
</verification>

<success_criteria>
- KNOWN_TOOLSETS.len() == 8; count assertion passes at 8
- Both toolset_members_maps (CLI and session) contain "learning" -> ["skill_manage"]
- DEFAULT_TOOLSETS includes "learning"
- register_skill_manage_tool called in app_runtime_factory when "learning" toolset active
- All 6 INV-33-* static-grep invariant tests pass
- Full workspace test suite green (cargo test --workspace exits 0)
</success_criteria>

<output>
Create `.planning/phases/33-autonomous-skill-creation/33-03-SUMMARY.md` when done
</output>
