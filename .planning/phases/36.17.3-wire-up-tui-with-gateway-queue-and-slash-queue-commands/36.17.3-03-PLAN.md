---
phase: 36.17.3
plan: 03
type: execute
wave: 2
depends_on:
  - 36.17.3-01
  - 36.17.3-02
files_modified:
  - crates/ironhermes-cli/src/tui_rata/app.rs
  - crates/ironhermes-cli/tests/tui_queue_drain.rs
  - crates/ironhermes-cli/tests/tui_queue_controls.rs
  - crates/ironhermes-cli/tests/status_line_queue_pill.rs
  - crates/ironhermes-cli/tests/README.md
autonomous: true
requirements:
  - D-11
  - D-12
threats: []
objective: |
  Wave 0 test scaffolding. Add `App::new_test_with_queue(queue)` to `app.rs`
  under `#[cfg(feature = "test-support")]` mirroring `App::new_test_empty`,
  create three test-file stubs (`tui_queue_drain.rs`, `tui_queue_controls.rs`,
  `status_line_queue_pill.rs`) with module headers and module-level
  `#[ignore]`d placeholder bodies, and add a `tests/README.md` snippet
  documenting how to run the D-12 negative-control rebase verification.
  Real test bodies land in Plan 06.

must_haves:
  truths:
    - "`App::new_test_with_queue(queue: Arc<dyn MessageQueue<SessionKey>>)` is callable from tests once Plan 04 lands the `queue` field on `App` (this plan ships the constructor signature + initial body using Plan 04's pending field layout — see action notes)."
    - "Three test files compile under `cargo test -p ironhermes-cli --no-run` with the `test-support` feature."
    - "tests/README.md contains the D-12 manual rebase verification protocol."
  artifacts:
    - path: "crates/ironhermes-cli/src/tui_rata/app.rs"
      provides: "App::new_test_with_queue test constructor"
      contains: "new_test_with_queue"
    - path: "crates/ironhermes-cli/tests/tui_queue_drain.rs"
      provides: "D-04/D-05/D-11/D-12 harness skeleton"
      contains: "tui_queue_drain"
    - path: "crates/ironhermes-cli/tests/tui_queue_controls.rs"
      provides: "D-06/D-07/D-08/D-10 harness skeleton"
      contains: "tui_queue_controls"
    - path: "crates/ironhermes-cli/tests/status_line_queue_pill.rs"
      provides: "D-09 status-bar pill test skeleton"
      contains: "status_line_queue_pill"
    - path: "crates/ironhermes-cli/tests/README.md"
      provides: "D-12 negative-control rebase verification protocol"
      contains: "D-12"
  key_links:
    - from: "crates/ironhermes-cli/tests/tui_queue_drain.rs"
      to: "crates/ironhermes-cli/src/tui_rata/app.rs"
      via: "App::new_test_with_queue test constructor"
      pattern: "new_test_with_queue"
---

<objective>
Ship the Wave 0 test scaffolding so Plan 06 can fill in real test bodies
without touching the harness shape. This plan creates the constructor, the
three test files, and the README snippet — every file compiles immediately
under `cargo test -p ironhermes-cli --no-run --features test-support` but the
test bodies are `#[ignore]`d placeholders until Plan 06.

Purpose: D-11 / D-12 require an integration test suite. The VALIDATION.md
Wave 0 list explicitly calls out the missing test files + the missing
`App::new_for_test` constructor. We close those gaps here so Plans 04 / 05
can land production code without a "tests haven't been scaffolded" blocker.

Output: Test constructor on `App`, three skeleton test files, README snippet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-CONTEXT.md
@.planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-RESEARCH.md
@.planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-PATTERNS.md
@.planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-VALIDATION.md
@crates/ironhermes-cli/src/tui_rata/app.rs
@crates/ironhermes-cli/tests/tui_rata_toggles.rs

<interfaces>
<!-- Plan 04 will add `queue` and `queue_key` fields to `App`. -->
<!-- This plan's `new_test_with_queue` must align with that field layout. -->
<!-- Coordination: the test constructor swaps in a caller-provided queue       -->
<!-- after `test_deps()` returns its defaults; queue_key uses the fixed TUI    -->
<!-- key per D-03.                                                             -->

```rust
// Plan 04 will add to App:
pub queue: Arc<dyn MessageQueue<SessionKey>>,
pub queue_key: SessionKey,
pub queue_paused: Arc<AtomicBool>,

// This plan adds (under #[cfg(feature = "test-support")]):
pub fn new_test_with_queue(
    queue: Arc<dyn MessageQueue<SessionKey>>,
) -> Self {
    let mut app = Self::new(test_deps());
    app.queue = queue;
    app.queue_key = SessionKey::new(Platform::Local, "local").with_user("local");
    app
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add App::new_test_with_queue constructor (gated by test-support feature)</name>
  <files>crates/ironhermes-cli/src/tui_rata/app.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/tui_rata/app.rs around lines 895-899 (the existing `App::new_test_empty` constructor — copy gating + style)
    - crates/ironhermes-cli/src/tui_rata/app.rs around lines 1088-1155 (the `test_deps()` function — the minimal AppDeps factory)
    - crates/ironhermes-cli/Cargo.toml (confirm the `test-support` feature is declared; PATTERNS §"`test-support` Feature Gate" cites `#![cfg(feature = "test-support")]` as the established gate)
    - .planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-PATTERNS.md §"7. Integration Test Harness Pattern" (the `new_test_with_queue` signature is given there)
  </read_first>
  <action>
    In `crates/ironhermes-cli/src/tui_rata/app.rs`, immediately AFTER the existing `pub fn new_test_empty()` method (around line 895-899), add a new method gated by the same `#[cfg(feature = "test-support")]` attribute:
    - Method name: `new_test_with_queue`
    - Signature: `pub fn new_test_with_queue(queue: std::sync::Arc<dyn ironhermes_core::queue::MessageQueue<ironhermes_core::session::SessionKey>>) -> Self`
    - Body: build `let mut app = Self::new(test_deps());`, then assign `app.queue = queue;` and `app.queue_key = ironhermes_core::session::SessionKey::new(ironhermes_core::Platform::Local, "local").with_user("local");` (use the exact `SessionKey` constructor signatures present in core — adjust to match the actual API if `with_user` is named differently; refer to the relocated `SessionKey` definition shipped by Plan 01).
    NOTE: this constructor REFERENCES fields (`queue`, `queue_key`) that Plan 04 will add to `App` and `test_deps()`. Plan 04 owns the field additions; this Plan 03 task may need to add temporary defaults to `test_deps()` (e.g. `queue: Arc::new(SessionQueue::new()) as Arc<dyn MessageQueue<SessionKey>>`, `queue_key: SessionKey::new(Platform::Local, "local").with_user("local")`, `queue_paused: Arc::new(AtomicBool::new(false))`) so the file compiles. Coordinate: if Plan 04 runs in the same wave as this plan, the wave runner must order Task 1 to land its field additions before this task's body references them. If Plan 04 hasn't landed yet at execution time, add the fields + defaults as part of THIS task's edit and document in the task summary that Plan 04 will replace the defaults with real production wiring.
    Add the import `use ironhermes_core::queue::MessageQueue;` and `use ironhermes_core::session::SessionKey;` near the top of `app.rs` if not already present.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-cli --features test-support 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - `crates/ironhermes-cli/src/tui_rata/app.rs` contains `fn new_test_with_queue` gated by `#[cfg(feature = "test-support")]`
    - `cargo build -p ironhermes-cli --features test-support` exits 0
    - The constructor signature matches the interfaces block above
  </done>
  <acceptance_criteria>
    - `cargo build -p ironhermes-cli --features test-support` exits 0.
    - `grep -B1 'fn new_test_with_queue' crates/ironhermes-cli/src/tui_rata/app.rs | grep -c 'cfg(feature = "test-support")'` returns ≥ 1.
    - `grep -c 'fn new_test_with_queue' crates/ironhermes-cli/src/tui_rata/app.rs` returns 1.
    - `grep -c 'MessageQueue<' crates/ironhermes-cli/src/tui_rata/app.rs` returns ≥ 1 (the parameter type is referenced).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create three skeleton test files with #[ignore]d placeholder tests</name>
  <files>crates/ironhermes-cli/tests/tui_queue_drain.rs, crates/ironhermes-cli/tests/tui_queue_controls.rs, crates/ironhermes-cli/tests/status_line_queue_pill.rs</files>
  <read_first>
    - crates/ironhermes-cli/tests/tui_rata_toggles.rs (the established TUI-test file structure — the file header `#![cfg(feature = "test-support")]` and import style at lines 1-12; PATTERNS §7 cites this as the analog)
    - .planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-VALIDATION.md §"Per-Task Verification Map" + §"Wave 0 Requirements" (the per-decision test-function names: `tui_queue_drain_fifo`, `tui_queue_pause_unpause`, `tui_queue_new_clears_atomic`, `tui_stop_clears_queue`, `tui_queue_cap_hit`, `status_line_queue_pill`, `tui_queue_regression_d11`, `tui_queue_regression_negative_control`)
    - .planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-PATTERNS.md §"7. Integration Test Harness Pattern" (the `make_queue()` + `make_key()` helpers + file structure)
  </read_first>
  <action>
    Create three test files, each starting with the header `#![cfg(feature = "test-support")]` followed by the relevant imports (`ironhermes_cli::tui_rata::app::App`, `ironhermes_cli::tui_rata::stream_events::StreamEvent`, `ironhermes_core::queue::MessageQueue`, `ironhermes_core::session::SessionKey`, `ironhermes_core::Platform`, `ironhermes_gateway::session_queue::SessionQueue`, `std::sync::Arc`, `std::sync::atomic::Ordering`).
    File 1 — `crates/ironhermes-cli/tests/tui_queue_drain.rs`: top doc-comment "Phase 36.17.3 D-04/D-05/D-11/D-12 — FIFO drain + negative control regression." Add helper fns `fn make_queue() -> Arc<SessionQueue>` returning `Arc::new(SessionQueue::new())` and `fn make_key() -> SessionKey` returning `SessionKey::new(Platform::Local, "local").with_user("local")`. Add stub functions (each gated `#[test]` AND `#[ignore = "Plan 06 fills body"]`): `fn tui_queue_drain_fifo()`, `fn tui_queue_regression_d11()`, `fn tui_queue_regression_negative_control()`. Body of each = `let _ = make_queue(); let _ = make_key();` (just exercising the helpers so dead-code warnings don't fire) plus a `// Plan 06 will fill in the real assertions per D-11 / D-12.` comment. Per D-11 / D-12.
    File 2 — `crates/ironhermes-cli/tests/tui_queue_controls.rs`: top doc-comment "Phase 36.17.3 D-06/D-07/D-08/D-10 — pause/unpause + /new + /stop + cap-hit." Same helpers. Stubs: `fn tui_queue_pause_unpause()`, `fn tui_queue_new_clears_atomic()`, `fn tui_stop_clears_queue()`, `fn tui_queue_cap_hit()`. Same `#[ignore]` gating.
    File 3 — `crates/ironhermes-cli/tests/status_line_queue_pill.rs`: top doc-comment "Phase 36.17.3 D-09 — status-bar `Queue: N` pill rendering + inline transcript line." Stubs: `fn status_line_queue_pill()`, `fn queue_transcript_line()`. Same `#[ignore]` gating.
    All three files MUST compile under `cargo test -p ironhermes-cli --features test-support --no-run` after this task. Use `let _ = ...` patterns to avoid unused-variable warnings; do NOT add `#![allow(...)]` unless the build forces it. Per D-11.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cli --features test-support --no-run 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - All three new test files exist at the paths above
    - Each file starts with `#![cfg(feature = "test-support")]`
    - Each `#[test]` stub is also marked `#[ignore = "Plan 06 fills body"]`
    - `cargo test -p ironhermes-cli --features test-support --no-run` exits 0
  </done>
  <acceptance_criteria>
    - `cargo test -p ironhermes-cli --features test-support --no-run` exits 0.
    - `test -f crates/ironhermes-cli/tests/tui_queue_drain.rs &amp;&amp; test -f crates/ironhermes-cli/tests/tui_queue_controls.rs &amp;&amp; test -f crates/ironhermes-cli/tests/status_line_queue_pill.rs` exits 0.
    - `grep -c 'cfg(feature = "test-support")' crates/ironhermes-cli/tests/tui_queue_drain.rs` returns ≥ 1.
    - `grep -c '#\[ignore' crates/ironhermes-cli/tests/tui_queue_drain.rs` returns ≥ 3 (the three stub fns).
    - `grep -c '#\[ignore' crates/ironhermes-cli/tests/tui_queue_controls.rs` returns ≥ 4.
    - `grep -c '#\[ignore' crates/ironhermes-cli/tests/status_line_queue_pill.rs` returns ≥ 2.
    - `grep -c 'fn tui_queue_drain_fifo' crates/ironhermes-cli/tests/tui_queue_drain.rs` returns 1.
    - `grep -c 'fn tui_queue_regression_d11' crates/ironhermes-cli/tests/tui_queue_drain.rs` returns 1.
    - `grep -c 'fn tui_queue_regression_negative_control' crates/ironhermes-cli/tests/tui_queue_drain.rs` returns 1.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add tests/README.md with D-12 negative-control rebase verification protocol</name>
  <files>crates/ironhermes-cli/tests/README.md</files>
  <read_first>
    - .planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-VALIDATION.md §"Manual-Only Verifications" — the D-12 row specifies the rebase protocol verbatim
    - .planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-CONTEXT.md D-12 row
  </read_first>
  <action>
    Create (or append to) `crates/ironhermes-cli/tests/README.md` with a section heading `## Phase 36.17.3 D-12 negative-control verification`. Body must document the manual rebase verification protocol verbatim from VALIDATION.md §"Manual-Only Verifications" D-12 row:
    1. Before final merge of Phase 36.17.3, run `git stash`.
    2. `git checkout <pre-fix-sha> -- crates/ironhermes-cli/src/tui_rata/commands.rs` to restore the pre-fix textarea-prepopulate `/queue` handler.
    3. Run `cargo test -p ironhermes-cli --features test-support tui_queue_regression_negative_control -- --ignored` and confirm the test FAILS (D-12 contract).
    4. Restore current code via `git checkout HEAD -- crates/ironhermes-cli/src/tui_rata/commands.rs` and `git stash pop`.
    5. Record the result (PASS-against-new / FAIL-against-old) in the phase SUMMARY before merging.
    Add a one-line table of contents at the top of the README pointing to this section. Per D-12.
  </action>
  <verify>
    <automated>grep -c 'D-12' crates/ironhermes-cli/tests/README.md &amp;&amp; grep -c 'negative-control' crates/ironhermes-cli/tests/README.md</automated>
  </verify>
  <done>
    - `crates/ironhermes-cli/tests/README.md` exists
    - Contains the D-12 negative-control protocol with the five numbered steps
  </done>
  <acceptance_criteria>
    - `test -f crates/ironhermes-cli/tests/README.md` exits 0.
    - `grep -c 'D-12' crates/ironhermes-cli/tests/README.md` returns ≥ 1.
    - `grep -c 'negative-control' crates/ironhermes-cli/tests/README.md` returns ≥ 1.
    - `grep -c 'git checkout' crates/ironhermes-cli/tests/README.md` returns ≥ 1.
    - `grep -c 'tui_queue_regression_negative_control' crates/ironhermes-cli/tests/README.md` returns ≥ 1.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-only code ↔ production code | `#[cfg(feature = "test-support")]` gating must prevent test seam leaking into release binaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none direct) | — | — | — | All additions are test-only (gated by `test-support` feature). No production code path changes. No external input trust boundaries crossed. |
</threat_model>

<verification>
After all tasks:
- `cargo build -p ironhermes-cli --features test-support` exits 0
- `cargo test -p ironhermes-cli --features test-support --no-run` exits 0
- Three test files exist with ignored stubs
- tests/README.md documents the D-12 rebase protocol
</verification>

<success_criteria>
- `App::new_test_with_queue` constructor available under `test-support` feature.
- Three test files (`tui_queue_drain.rs`, `tui_queue_controls.rs`, `status_line_queue_pill.rs`) compile with `#[ignore]`d stubs.
- D-12 negative-control rebase protocol documented in `tests/README.md`.
- Plan 06 can fill in test bodies without touching the file structure.
</success_criteria>

<output>
Create `.planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-03-SUMMARY.md` when done.
</output>
</content>
</invoke>