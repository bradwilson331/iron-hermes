---
phase: 36.17.3
plan: 05
type: execute
wave: 3
depends_on:
  - 36.17.3-04
files_modified:
  - crates/ironhermes-cli/src/tui_rata/commands.rs
autonomous: true
requirements:
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
threats:
  - T-01
  - T-02
objective: |
  Wire the slash-command surface in `commands.rs`: rewrite `/queue` to push
  into the live queue (replaces the textarea-prepopulate placeholder at
  lines 978-999), add `/pause` and `/unpause` toggle arms in
  `handle_session_control` BEFORE the `map_core_to_slash_outcome` fallback,
  hook `/new` and `/reset` to clear the queue + reset paused BEFORE the
  session-clear path (D-07), modify `/stop` to clear the queue then fire
  `cancel_child.take().cancel()` (D-08; RESEARCH Pitfall 1 — clear MUST
  precede cancel), and render the inline transcript confirmation /
  cap-hit error per D-09 + D-10. Bell omitted per Resolution 7.

must_haves:
  truths:
    - "/queue arm pushes into `app.queue.try_push(&app.queue_key, message)` and emits inline transcript line `Queued: \"<text>\" (N in queue)` (D-09)."
    - "Cap-hit (`QueueError::CapacityReached`) emits inline error `Queue is full (128/128). /stop or /flush to drain.` (D-10). T-01 mitigated by cap-at-source from Plan 01."
    - "/pause toggles `app.queue_paused` via `fetch_xor(true, Ordering::SeqCst)` (canonical AtomicBool pattern); /unpause uses `swap(false, Ordering::SeqCst)`."
    - "/new and /reset call `app.queue.clear(&app.queue_key)` + `app.queue_paused.store(false, ...)` BEFORE forwarding to session clear (D-07; T-02 mitigation)."
    - "/stop calls `app.queue.clear(&app.queue_key)` BEFORE `app.cancel_child.take().cancel()` (D-08 + RESEARCH Pitfall 1)."
    - "The old textarea-prepopulate logic at commands.rs:978-999 and the deferral marker at commands.rs:1123-1129 are removed/closed (D-12 negative-control precondition)."
  artifacts:
    - path: "crates/ironhermes-cli/src/tui_rata/commands.rs"
      provides: "Full slash-command wiring for /queue, /pause, /unpause, /new, /reset, /stop"
      contains: "try_push"
  key_links:
    - from: "crates/ironhermes-cli/src/tui_rata/commands.rs (/queue arm)"
      to: "crates/ironhermes-core/src/queue.rs"
      via: "MessageQueue::try_push call"
      pattern: "app\\.queue\\.try_push"
    - from: "crates/ironhermes-cli/src/tui_rata/commands.rs (/stop arm)"
      to: "crates/ironhermes-cli/src/tui_rata/app.rs (cancel_child)"
      via: "cancel_child.take().cancel() after queue.clear"
      pattern: "cancel_child\\.take\\(\\)\\.cancel"
---

<objective>
Replace the TUI's placeholder slash handling with the real queue surface. The
`/queue` arm becomes an actual queue push. `/pause` and `/unpause` become
real toggles. `/new`, `/reset`, `/stop` learn to clear the queue at the right
moment in the sequence. The cap-hit error renders inline.

Purpose: D-06 (amended) + D-07 + D-08 + D-09 inline-transcript half + D-10.
This is the user-visible behaviour change of the phase.

Output: All slash commands behave per spec; the deferral marker at
`commands.rs:1123-1129` is closed; the D-12 negative-control precondition
(old textarea-prepopulate path no longer touches `app.queue`) is established.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-CONTEXT.md
@.planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-RESEARCH.md
@.planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-PATTERNS.md
@.planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-04-PLAN.md
@crates/ironhermes-cli/src/tui_rata/commands.rs

<interfaces>
<!-- Plan 04 ships these fields on App that this plan reads/writes -->
<!--   app.queue: Arc<dyn MessageQueue<SessionKey>>                   -->
<!--   app.queue_key: SessionKey                                      -->
<!--   app.queue_paused: Arc<AtomicBool>                              -->
<!--   app.cancel_child: Option<CancellationToken>                    -->

<!-- Existing transcript-push pattern (PATTERNS §"Shared Patterns →     -->
<!--   System Message Transcript Push") used for both confirmations  -->
<!--   and the cap-hit error: push ChatMessage with role = Role::System -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace /queue arm (lines 978-999) with real queue push + close deferral marker (1123-1129)</name>
  <files>crates/ironhermes-cli/src/tui_rata/commands.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/tui_rata/commands.rs lines 978-999 (the current textarea-prepopulate placeholder being REPLACED)
    - crates/ironhermes-cli/src/tui_rata/commands.rs lines 1123-1129 (the deferral marker at `map_core_to_slash_outcome` for `CommandResult::Queued`)
    - crates/ironhermes-cli/src/tui_rata/commands.rs lines 944-960 (the `/background` arm — PATTERNS §5 cites this as the structural analog: arg validation + history push + (in our case) queue push)
    - .planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-RESEARCH.md §"Code Examples → Queue Item Injection (push path)"
    - .planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-PATTERNS.md §"5. /queue Arm Replacement Pattern"
  </read_first>
  <action>
    Replace the existing `"queue" => { ... }` arm in `handle_session_control` (commands.rs lines 978-999) with a new arm:
    - If `args.is_empty()`: return `SlashOutcome::Handled("Usage: /queue <message>".to_string())`.
    - Else: let `message = args.join(" ");`. Call `app.queue.try_push(&app.queue_key, message.clone())`.
      - On `Ok(())`: read `let depth = app.queue.len(&app.queue_key);`. Return `SlashOutcome::Handled(format!("Queued: \"{}\" ({} in queue)", message, depth))` per D-09.
      - On `Err(QueueError::CapacityReached { max, .. })`: return `SlashOutcome::Handled(format!("Queue is full ({max}/{max}). /stop or /flush to drain."))` per D-10. NOTE: `/flush` is OUT of scope this phase but mentioned in the message for forward-compat per CONTEXT D-10. T-01 mitigation: the cap=128 const from Plan 01 enforces the bound at source.
    Bell is omitted per Resolution 7. Document this in the SUMMARY.
    Delete (do NOT keep dead) the old `tui_textarea::TextArea::default()` block + `app.textarea = ta;` mutation from the old arm — these lines are the D-12 negative-control signature. The acceptance criterion grep enforces removal.
    Close the deferral marker at lines 1123-1129: the `CommandResult::Queued { message }` arm in `map_core_to_slash_outcome` MUST still exist (gateway / classic CLI also emit this variant, so the fallback path stays for non-TUI surfaces) — but UPDATE its inline comment to: `// Phase 36.17.3: closed via handle_session_control's "queue" arm above; this fallback remains for non-TUI consumers.` (replacing the "Phase 36.17.1 Plan 03 Task 1" deferral comment). The arm body itself can remain `SlashOutcome::Handled(format!("Queued: {message}"))`.
    Import `ironhermes_core::queue::QueueError;` (or `ironhermes_gateway::session_queue::QueueError;` if the gateway re-export is the working path after Plan 01) at the top of the file if not already present.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-cli 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `commands.rs` `/queue` arm calls `app.queue.try_push(&app.queue_key, message.clone())`
    - `Ok(())` branch emits `Queued: "<message>" (N in queue)` text
    - `Err(CapacityReached)` branch emits `Queue is full (128/128). /stop or /flush to drain.`
    - Old `tui_textarea::TextArea::default()` mutation gone from the `/queue` arm
    - Deferral comment at line 1123 updated to reference Phase 36.17.3 closure
    - `cargo build -p ironhermes-cli` exits 0
  </done>
  <acceptance_criteria>
    - `cargo build -p ironhermes-cli` exits 0.
    - `grep -c 'app.queue.try_push(&app.queue_key' crates/ironhermes-cli/src/tui_rata/commands.rs` returns ≥ 1.
    - `grep -c 'Queue is full' crates/ironhermes-cli/src/tui_rata/commands.rs` returns ≥ 1.
    - `grep -E 'Queued: \\\\\"' crates/ironhermes-cli/src/tui_rata/commands.rs | wc -l` returns ≥ 1 (the `Queued: "..."` confirmation format).
    - In the `/queue` arm specifically (the lines around the old 978-999 block): `awk '/\"queue\" =>/,/^        \}/' crates/ironhermes-cli/src/tui_rata/commands.rs | grep -c 'tui_textarea::TextArea::default'` returns 0 (old textarea mutation removed — this is the D-12 negative-control precondition).
    - `grep -c 'Phase 36.17.1 Plan 03 Task 1' crates/ironhermes-cli/src/tui_rata/commands.rs` returns 0 (deferral marker text replaced with the 36.17.3 closure note).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add /pause and /unpause arms in handle_session_control (D-06 amended)</name>
  <files>crates/ironhermes-cli/src/tui_rata/commands.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/tui_rata/commands.rs around lines 745-795 (the `handle_toggle` analog and the existing `fast_enabled.fetch_xor` pattern — PATTERNS §2 cites the canonical toggle)
    - crates/ironhermes-cli/src/tui_rata/commands.rs around the dispatcher block that maps slash-name strings → arms (locate by searching for the `"queue"` arm position)
    - .planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-PATTERNS.md §"2. AtomicBool Toggle Pattern" + §"4. CommandResult Variant Addition Pattern → Consumer impact table"
  </read_first>
  <action>
    In `handle_session_control` (or the active dispatch site that handles `/queue`, `/new`, `/reset`, `/stop` arms — same match block), add two new arms BEFORE the catch-all `_ => map_core_to_slash_outcome(core_result.clone())`:
    - `"pause" => { ... }`:
      - `let was_paused = app.queue_paused.fetch_xor(true, std::sync::atomic::Ordering::SeqCst);`
      - `let new_state = !was_paused;`
      - `let depth = app.queue.len(&app.queue_key);`
      - Return `SlashOutcome::Handled(format!("Queue drain: {}. ({} queued)", if new_state { "paused" } else { "resumed" }, depth))` per D-06 (amended). Print confirmation per Claude's Discretion item 5 in CONTEXT (the recommendation was to print).
    - `"unpause" => { ... }`:
      - `let was_paused = app.queue_paused.swap(false, std::sync::atomic::Ordering::SeqCst);`
      - `let depth = app.queue.len(&app.queue_key);`
      - If `was_paused`: return `SlashOutcome::Handled(format!("Queue resumed. ({} queued)", depth))`.
      - Else: return `SlashOutcome::Handled("Queue was not paused.".to_string())` (informational no-op per D-06 amended).
    Both arms run BEFORE `map_core_to_slash_outcome` so the defensive Silent fallback arms added in Plan 02 never fire for TUI. Annotate the arms with inline comments referencing "Phase 36.17.3 (D-06 amended)".
  </action>
  <verify>
    <automated>cargo build -p ironhermes-cli 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `commands.rs` contains a `"pause"` arm calling `app.queue_paused.fetch_xor(true`
    - `commands.rs` contains an `"unpause"` arm calling `app.queue_paused.swap(false`
    - Both arms run before the catch-all forwarder
    - `cargo build -p ironhermes-cli` exits 0
  </done>
  <acceptance_criteria>
    - `cargo build -p ironhermes-cli` exits 0.
    - `grep -c 'app.queue_paused.fetch_xor(true' crates/ironhermes-cli/src/tui_rata/commands.rs` returns ≥ 1.
    - `grep -c 'app.queue_paused.swap(false' crates/ironhermes-cli/src/tui_rata/commands.rs` returns ≥ 1.
    - `grep -E '\"pause\" =>' crates/ironhermes-cli/src/tui_rata/commands.rs | wc -l` returns ≥ 1.
    - `grep -E '\"unpause\" =>' crates/ironhermes-cli/src/tui_rata/commands.rs | wc -l` returns ≥ 1.
    - `grep -c 'Queue drain:' crates/ironhermes-cli/src/tui_rata/commands.rs` returns ≥ 1.
    - `grep -c 'Queue was not paused' crates/ironhermes-cli/src/tui_rata/commands.rs` returns ≥ 1.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Hook /new + /reset queue clear + /stop queue clear BEFORE cancel (D-07, D-08, Pitfall 1)</name>
  <files>crates/ironhermes-cli/src/tui_rata/commands.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/tui_rata/commands.rs lines 833-837 (the current `/stop` arm that ONLY calls `map_core_to_slash_outcome`)
    - crates/ironhermes-cli/src/tui_rata/commands.rs — locate `/new` and `/reset` arms by searching for `"new" =>` and `"reset" =>` (PATTERNS §5 cites these as adding `queue.clear` + `queue_paused.store(false)` BEFORE forwarding to session clear)
    - crates/ironhermes-cli/src/tui_rata/app.rs (the `cancel_child: Option<CancellationToken>` field — `cancel_child.take().cancel()` is the established cancel mechanism per RESEARCH §"/stop Cancellation Path")
    - .planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-RESEARCH.md §"/stop Cancellation Path" + §"Common Pitfalls — Pitfall 1"
    - .planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-PATTERNS.md §"5. /queue Arm Replacement Pattern → /stop arm + /new + /reset arms"
  </read_first>
  <action>
    Three coordinated edits in `commands.rs`:
    (a) `/new` and `/reset` arms: at the TOP of each arm body (before any session-clear forwarding to `map_core_to_slash_outcome` or the local clear logic), add:
        - `app.queue.clear(&app.queue_key);`
        - `app.queue_paused.store(false, std::sync::atomic::Ordering::SeqCst);`
        Per D-07 + T-02 mitigation. The clears must happen BEFORE the session-clear path so the user never observes stale queue items firing against a fresh session.
    (b) `/stop` arm (currently lines 833-837): rewrite to perform the cancel+clear in the order Pitfall 1 mandates:
        - FIRST: `app.queue.clear(&app.queue_key);` and `app.queue_paused.store(false, std::sync::atomic::Ordering::SeqCst);` (queue must be empty BEFORE cancel fires so the eventual `StreamEvent::Cancelled` finds an empty queue — though the Cancelled arm already avoids drain per Plan 04 Task 2, this is belt-and-suspenders ordering per Pitfall 1).
        - THEN: `if let Some(tok) = app.cancel_child.take() { tok.cancel(); }` to fire the in-flight turn cancel (D-08).
        - FINALLY: forward `map_core_to_slash_outcome(core_result.clone())` so core's `cmd_stop` continues to drain background processes via `ProcessRegistry`.
    Annotate the modified arms with inline comments citing D-07 / D-08 / "RESEARCH Pitfall 1" so the verifier and any future reader knows the ordering is intentional.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-cli 2>&amp;1 | tail -20 &amp;&amp; cargo build --workspace 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - `/new` and `/reset` arms call `app.queue.clear(&app.queue_key)` AND `app.queue_paused.store(false, ...)` before forwarding
    - `/stop` arm calls `app.queue.clear(&app.queue_key)` BEFORE `app.cancel_child.take().cancel()`
    - All edits cite the relevant decision (D-07/D-08) and Pitfall 1
    - `cargo build -p ironhermes-cli` and `cargo build --workspace` both exit 0
  </done>
  <acceptance_criteria>
    - `cargo build -p ironhermes-cli` exits 0.
    - `cargo build --workspace` exits 0.
    - `grep -c 'app.queue.clear(&app.queue_key)' crates/ironhermes-cli/src/tui_rata/commands.rs` returns ≥ 3 (once each in `/new`, `/reset`, `/stop`).
    - `grep -c 'app.cancel_child.take' crates/ironhermes-cli/src/tui_rata/commands.rs` returns ≥ 1 (the `/stop` cancel fire).
    - Source-pattern assertion for ordering in `/stop`: `awk '/\"stop\" =>/,/^        \}/' crates/ironhermes-cli/src/tui_rata/commands.rs | awk '/queue.clear/{ q=NR } /cancel_child.take/{ c=NR } END { exit !(q &amp;&amp; c &amp;&amp; q < c) }'` exits 0 (queue.clear MUST appear before cancel_child.take per Pitfall 1).
    - `grep -c 'app.queue_paused.store(false' crates/ironhermes-cli/src/tui_rata/commands.rs` returns ≥ 3 (in `/new`, `/reset`, `/stop`).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (slash text) ↔ queue | Cap=128 enforced at queue source; sequence ordering of clear-then-cancel for /stop |
| Session boundary (/new, /reset) ↔ stale queued items | Queue MUST be cleared before session is reset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Denial of Service | `/queue` spam → memory exhaustion | mitigate | Cap=128 is enforced at `SessionQueue::try_push` (Plan 01); this plan's `/queue` arm propagates `QueueError::CapacityReached` to the user as inline error per D-10. T-01 severity is LOW. |
| T-02 | Tampering | Stale queue across session boundary | mitigate | `/new`, `/reset`, `/stop` all call `app.queue.clear(&app.queue_key)` + reset paused before session clear. D-07 + D-08. T-02 severity MEDIUM if missed; Plan 06 D-11 step 7 enforces. |
</threat_model>

<verification>
After all tasks:
- `cargo build --workspace` exits 0
- `/queue text` arm calls `app.queue.try_push` (replacing textarea-prepopulate)
- `/pause` / `/unpause` arms toggle `app.queue_paused`
- `/new`, `/reset`, `/stop` clear queue + reset paused before session/cancel paths
- `/stop` ordering — clear BEFORE cancel — verified by awk source-pattern assertion
</verification>

<success_criteria>
- All six slash commands behave per CONTEXT.md decisions D-06..D-10.
- D-12 negative-control precondition established (old textarea-prepopulate path is gone from `/queue` arm).
- T-01 and T-02 mitigations are observable in source.
- Plan 06 can now exercise the full sequence end-to-end.
</success_criteria>

<output>
Create `.planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-05-SUMMARY.md` when done.
</output>
</content>
</invoke>