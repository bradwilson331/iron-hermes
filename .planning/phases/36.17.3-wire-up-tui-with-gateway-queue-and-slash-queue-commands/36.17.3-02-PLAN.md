---
phase: 36.17.3
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-core/src/commands/mod.rs
  - crates/ironhermes-core/src/commands/registry.rs
  - crates/ironhermes-cli/src/tui_rata/commands.rs
  - crates/ironhermes-gateway/src/handler.rs
  - crates/ironhermes-cli/src/main.rs
autonomous: true
requirements:
  - D-06
threats: []
objective: |
  Add `CommandResult::PauseQueue` and `CommandResult::UnpauseQueue` variants
  to `ironhermes-core::commands`, register `/pause` (alias `/unpause`) in the
  command registry — using `/unpause` per D-06 (amended) to avoid the
  `/resume` collision at `registry.rs:88` — and add defensive no-op match
  arms in every existing `match core_result` site so the workspace still
  compiles. Active toggling of `App::queue_paused` is wired in Plan 05.

must_haves:
  truths:
    - "`CommandResult::PauseQueue` and `CommandResult::UnpauseQueue` exist in ironhermes-core (D-06 amended)."
    - "`/pause` is registered with alias `/unpause` (NOT `/resume`) per D-06 (amended) — the existing `/resume` at registry.rs:88 (session resume) stays untouched."
    - "Every existing exhaustive `match core_result` site handles the two new variants — workspace compiles with `-D warnings` where applied."
    - "CommandRouter does not panic at startup — no duplicate name / alias collision."
  artifacts:
    - path: "crates/ironhermes-core/src/commands/mod.rs"
      provides: "PauseQueue + UnpauseQueue variant definitions"
      contains: "PauseQueue"
    - path: "crates/ironhermes-core/src/commands/registry.rs"
      provides: "/pause command def with /unpause alias"
      contains: "\"pause\""
  key_links:
    - from: "crates/ironhermes-core/src/commands/registry.rs"
      to: "crates/ironhermes-core/src/commands/mod.rs"
      via: "CommandDef::new(\"pause\", ...) referenced by router build"
      pattern: "CommandDef::new\\(\"pause\""
---

<objective>
Land the two new `CommandResult` variants (`PauseQueue`, `UnpauseQueue`) plus
the `/pause` registry entry with `/unpause` as the alias. Add defensive no-op
arms in every consumer that currently does an exhaustive `match` on
`CommandResult` so the workspace compiles cleanly.

Purpose: D-06 (amended). The amendment is explicit — `/resume` would collide
with the existing "Resume a previous session" command at
`crates/ironhermes-cli/src/tui_rata/commands/registry.rs:88` and panic at
startup (RESEARCH §Pitfall 4). User locked `/unpause` as the alias.

Output: Variants + registry entry shipped, workspace builds, no router panic
at startup. Active toggle wiring (the `App::queue_paused` flip) is Plan 05's
job.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-CONTEXT.md
@.planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-RESEARCH.md
@.planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-PATTERNS.md
@crates/ironhermes-core/src/commands/mod.rs
@crates/ironhermes-core/src/commands/registry.rs

<interfaces>
<!-- New variants downstream plans (especially Plan 05) will match against. -->

```
ironhermes_core::commands::CommandResult {
    // ... existing variants ...
    Queued { message: String },

    /// Phase 36.17.3 (D-06 amended): toggle queue drain pause state.
    /// TUI maps to AtomicBool flip; gateway and CLI REPL treat as no-op.
    PauseQueue,

    /// Phase 36.17.3 (D-06 amended): explicit unpause (alias path).
    /// Sets pause=false. Same consumer impact as PauseQueue.
    UnpauseQueue,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PauseQueue / UnpauseQueue variants + register /pause with /unpause alias</name>
  <files>crates/ironhermes-core/src/commands/mod.rs, crates/ironhermes-core/src/commands/registry.rs</files>
  <read_first>
    - crates/ironhermes-core/src/commands/mod.rs (the full `CommandResult` enum — RESEARCH lists every variant at §"Existing `CommandResult` Variants")
    - crates/ironhermes-core/src/commands/registry.rs around line 81-89 (the `/queue` and `/resume` CommandDefs — the new `/pause` entry sits next to `/queue`; `/resume` at line 88 is the COLLISION we are avoiding per D-06 amended)
    - .planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-RESEARCH.md §"`/pause` and `/resume` in the Registry" + §"Common Pitfalls — Pitfall 4"
    - .planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-PATTERNS.md §"4. CommandResult Variant Addition Pattern"
  </read_first>
  <action>
    In `crates/ironhermes-core/src/commands/mod.rs`: append two new variants to the `CommandResult` enum immediately AFTER `Queued { message: String }`. Use these exact identifiers — `PauseQueue` (unit variant, no payload) and `UnpauseQueue` (unit variant, no payload). Mirror the doc-comment style of the existing `Queued { message }` variant (which references its phase + decision). Both variants reference "Phase 36.17.3 (D-06 amended)".
    In `crates/ironhermes-core/src/commands/registry.rs`: in the same Session-scope block where `/queue` is defined, add `CommandDef::new("pause", "Pause or resume queue drain", CommandScope::Session).aliases(&["unpause"]).platform(PlatformFilter::CliOnly)` (use the existing `PlatformFilter` / `CommandScope` enum identifiers from the file — verify against line 81-89 region; copy the casing exactly). The alias MUST be `"unpause"` and NOT `"resume"` per D-06 (amended). DO NOT touch the existing `/resume` entry at line 88 — it stays as "Resume a previous session".
    Do NOT add active dispatch handlers here — the active arms live in Plan 05. This task only adds the enum variants + registry entry so downstream code can reference them.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-core 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `crates/ironhermes-core/src/commands/mod.rs` contains both `PauseQueue` and `UnpauseQueue` variants
    - `crates/ironhermes-core/src/commands/registry.rs` contains `CommandDef::new("pause"` with `.aliases(&["unpause"])`
    - `crates/ironhermes-core/src/commands/registry.rs` does NOT contain `.aliases(&["resume"])` for the pause command (collision avoidance — D-06 amended)
    - `cargo build -p ironhermes-core` exits 0
  </done>
  <acceptance_criteria>
    - `cargo build -p ironhermes-core` exits 0.
    - `grep -c 'PauseQueue' crates/ironhermes-core/src/commands/mod.rs` returns ≥ 1.
    - `grep -c 'UnpauseQueue' crates/ironhermes-core/src/commands/mod.rs` returns ≥ 1.
    - `grep -E 'CommandDef::new\("pause"' crates/ironhermes-core/src/commands/registry.rs | wc -l` returns 1.
    - `grep -E 'aliases\(&\["unpause"\]\)' crates/ironhermes-core/src/commands/registry.rs | wc -l` returns 1.
    - `grep -E 'aliases\(&\["resume"\]\)' crates/ironhermes-core/src/commands/registry.rs | wc -l` returns 0 (no collision with session-resume).
    - Workspace startup sanity: `cargo test -p ironhermes-core --no-run` exits 0 (router builds in test compilation without panic).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add defensive no-op match arms in every consumer of CommandResult</name>
  <files>crates/ironhermes-cli/src/tui_rata/commands.rs, crates/ironhermes-gateway/src/handler.rs, crates/ironhermes-cli/src/main.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/tui_rata/commands.rs around line 1102-1131 — the `map_core_to_slash_outcome` match (existing `CommandResult::Queued` arm at 1127-1129 is the reference shape)
    - crates/ironhermes-gateway/src/handler.rs — search for `match core_result` or `CommandResult::` to locate any exhaustive matches that must be extended
    - crates/ironhermes-cli/src/main.rs — the classic CLI REPL (`run_chat` or equivalent) that matches on `CommandResult`
    - .planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-RESEARCH.md §"Consumer Impact Analysis" (the table of every match site)
    - .planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-PATTERNS.md §"4. CommandResult Variant Addition Pattern" → "Consumer impact" table
  </read_first>
  <action>
    Locate every exhaustive `match` on `CommandResult` in the workspace by running `grep -rn 'CommandResult::' crates/ --include '*.rs' | grep -v test | grep -v 'pub enum'` and inspecting each file's match block. For each exhaustive match, add two new arms:
    - `CommandResult::PauseQueue => SlashOutcome::Silent,` (or the file-local equivalent no-op — e.g. for gateway handler.rs the default may be `CommandResult::Handled`-equivalent return; in classic CLI main.rs map to `CommandResult::Handled` flow or `println!` no-op consistent with the file's style)
    - `CommandResult::UnpauseQueue => SlashOutcome::Silent,` (same treatment as above)
    In `crates/ironhermes-cli/src/tui_rata/commands.rs::map_core_to_slash_outcome` specifically, add the arms IMMEDIATELY AFTER the existing `CommandResult::Queued { message }` arm at lines 1127-1129. Use `SlashOutcome::Silent` — Plan 05 will OVERRIDE this in the active session-control dispatch path BEFORE `map_core_to_slash_outcome` is called, so this arm is the defensive fallback per PATTERNS §4. Per D-06 (amended).
    Add a brief inline comment `// Phase 36.17.3 (D-06 amended): defensive no-op; active toggle lives in handle_session_control (Plan 05).`
    For non-TUI consumers (gateway handler.rs, classic CLI main.rs): use whatever the local no-op idiom is (e.g. `CommandResult::Handled`-equivalent return value or empty arm `{}`).
    Any `_ => ...` catch-all already present in a match block is sufficient and needs no addition — only TOUCH files where the catch-all is absent and the compiler would error on missing patterns. Verify each touched file individually before moving to the next.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    - `cargo build --workspace` exits 0 (no missing-match-arm errors anywhere)
    - `crates/ironhermes-cli/src/tui_rata/commands.rs` contains `CommandResult::PauseQueue` and `CommandResult::UnpauseQueue` arms in `map_core_to_slash_outcome`
    - No file added an arm that does anything other than no-op (the active dispatch is Plan 05's job)
  </done>
  <acceptance_criteria>
    - `cargo build --workspace` exits 0.
    - `grep -E 'CommandResult::(Pause|Unpause)Queue' crates/ironhermes-cli/src/tui_rata/commands.rs | wc -l` returns ≥ 2 (one arm for each variant in `map_core_to_slash_outcome`).
    - No clippy warning about unhandled enum variants on `CommandResult`: `cargo clippy --workspace --no-deps 2>&amp;1 | grep -c 'non-exhaustive patterns.*CommandResult'` returns 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Smoke-test the router build to confirm no duplicate-name panic</name>
  <files>(no production-file changes; this task only RUNS a router-build smoke test)</files>
  <read_first>
    - crates/ironhermes-core/src/commands/registry.rs (the `build_registry()` function — the panic site for duplicates is `panic!("Duplicate command name in registry")` per RESEARCH Pitfall 4)
    - .planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-RESEARCH.md §"Common Pitfalls — Pitfall 4"
  </read_first>
  <action>
    Run `cargo test -p ironhermes-core 2>&amp;1 | tail -30` and confirm all existing `ironhermes-core` tests pass (the router-build tests exercise `build_registry()` and would panic at startup if `/pause` or `/unpause` collided with any existing command name or alias). If any test panics with "Duplicate command name in registry: 'unpause'" or "Duplicate command name in registry: 'pause'" → diagnose and rename. Per D-06 (amended), the alias MUST stay `/unpause`; if `/unpause` somehow already exists, raise a checker-feedback note rather than swapping the alias. If a test panics elsewhere, examine whether Task 2's no-op arms changed behaviour for any existing test path (they should not — `Silent` is a no-op).
  </action>
  <verify>
    <automated>cargo test -p ironhermes-core 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - `cargo test -p ironhermes-core` exits 0
    - Final test summary shows `0 failed` AND no panic mentioning "Duplicate command name"
  </done>
  <acceptance_criteria>
    - `cargo test -p ironhermes-core 2>&amp;1 | grep -E 'test result:.*0 failed' | wc -l` returns ≥ 1.
    - `cargo test -p ironhermes-core 2>&amp;1 | grep -c 'Duplicate command name'` returns 0.
    - `cargo test -p ironhermes-core 2>&amp;1 | grep -c 'panicked at'` returns 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Command registry ↔ runtime startup | Duplicate name/alias causes startup panic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none direct) | — | — | — | This plan adds enum variants + a registry entry only; no I/O, no user input parsing, no trust boundary crossing. Indirect: this plan's `/unpause` alias choice is the lockstep mitigation for the D-06 collision risk identified in RESEARCH Pitfall 4. |
</threat_model>

<verification>
After all tasks:
- `cargo build --workspace` exits 0
- `cargo test -p ironhermes-core` exits 0 with 0 failed
- No panic mentioning "Duplicate command name"
- `/unpause` (NOT `/resume`) is the registered alias for `/pause`
</verification>

<success_criteria>
- `CommandResult::PauseQueue` + `CommandResult::UnpauseQueue` variants exist in `ironhermes-core`.
- `/pause` registered with `/unpause` alias; `/resume` registry entry untouched.
- Every exhaustive match on `CommandResult` in the workspace handles the two new variants (defensive no-op only).
- Workspace builds and `ironhermes-core` tests pass.
- Plan 05 can now wire active behaviour in `handle_session_control`.
</success_criteria>

<output>
Create `.planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-02-SUMMARY.md` when done.
</output>
</content>
</invoke>