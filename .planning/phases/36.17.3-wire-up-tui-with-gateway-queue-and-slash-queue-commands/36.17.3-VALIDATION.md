---
phase: 36.17.3
slug: wire-up-tui-with-gateway-queue-and-slash-queue-commands
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-28
---

# Phase 36.17.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in `#[test]` + `#[tokio::test]` (async, via `tokio-test`) |
| **Config file** | `Cargo.toml` workspace `[dev-dependencies]` (no extra config file) |
| **Quick run command** | `cargo test -p ironhermes-cli -- tui_queue` |
| **Full suite command** | `cargo test -p ironhermes-cli && cargo test -p ironhermes-gateway && cargo test -p ironhermes-core` |
| **Estimated runtime** | ~30-60 seconds (quick), ~3-5 minutes (full) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-cli -- tui_queue`
- **After every plan wave:** Run `cargo test -p ironhermes-cli && cargo test -p ironhermes-gateway` (gateway must stay green — trait migration must not break it)
- **Before `/gsd:verify-work`:** Full workspace suite must be green
- **Max feedback latency:** ~60 seconds for quick-run

---

## Per-Task Verification Map

> Phase requirements are TBD in ROADMAP — verification is keyed by the locked CONTEXT.md decisions (D-01..D-12).

| Decision | Behavior | Test Type | Automated Command | File Exists | Status |
|----------|----------|-----------|-------------------|-------------|--------|
| D-01 / D-02 | `MessageQueue` trait exists in `ironhermes-core`; `SessionQueue` implements it; gateway compiles unchanged | unit (compile-time) | `cargo build -p ironhermes-gateway -p ironhermes-core` | ❌ Wave 0 | ⬜ pending |
| D-03 | `App` owns `Arc<dyn MessageQueue<SessionKey>>` and a fixed `queue_key: SessionKey { platform: "tui", chat_id: "local", user_id: "local" }` | unit (compile) | `cargo build -p ironhermes-cli` | ❌ Wave 0 | ⬜ pending |
| D-04 / D-05 | 5 `/queue` events during in-flight turn → 5 sequential FIFO turns; one item per `run_turn` cycle | integration | `cargo test -p ironhermes-cli tui_queue_drain_fifo` | ❌ Wave 0 | ⬜ pending |
| D-06 | `/pause` halts drain (items still buffer); `/unpause` resumes; double-toggle is no-op with informational message; status bar shows `(paused)` | integration | `cargo test -p ironhermes-cli tui_queue_pause_unpause` | ❌ Wave 0 | ⬜ pending |
| D-07 | `/new` and `/reset` each clear queue AND clear session AND set `queue_paused=false`, atomically | integration | `cargo test -p ironhermes-cli tui_queue_new_clears_atomic` | ❌ Wave 0 | ⬜ pending |
| D-08 | `/stop` clears queue THEN fires cancel_child; `StreamEvent::Cancelled` arm does NOT call `maybe_drain_queue()` | integration | `cargo test -p ironhermes-cli tui_stop_clears_queue` | ❌ Wave 0 | ⬜ pending |
| D-09 | Status bar shows `Queue: N` (when N>0) or `Queue: N (paused)`; absent when N=0; inline transcript renders `Queued: "<text>" (N in queue)` | unit | `cargo test -p ironhermes-cli status_line_queue_pill && cargo test -p ironhermes-cli queue_transcript_line` | ❌ Wave 0 | ⬜ pending |
| D-10 | cap=128; `try_push` on 129th item → `QueueError::CapacityReached`; inline error rendered; bell deferred per research recommendation | unit | `cargo test -p ironhermes-cli tui_queue_cap_hit` | ❌ Wave 0 | ⬜ pending |
| D-11 | Full regression sequence: free-text → 5 `/queue` items → assert FIFO ordering; queue 2 → `/pause` → `/unpause` → assert drain; queue 3 → `/new` → assert empty + session cleared | integration | `cargo test -p ironhermes-cli tui_queue_regression_d11` | ❌ Wave 0 | ⬜ pending |
| D-12 | Negative control: the D-11 test must FAIL when run against the pre-fix `/queue` textarea-prepopulate handler (snapshot prior to wiring) | integration | `cargo test -p ironhermes-cli tui_queue_regression_negative_control` (manual rebase verification before merge) | ❌ Wave 0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/ironhermes-core/src/queue.rs` — `MessageQueue` trait module + re-export
- [ ] Move `SessionKey` from `ironhermes-gateway/src/session.rs` to `ironhermes-core` (re-exported from gateway for back-compat)
- [ ] `impl MessageQueue<SessionKey> for SessionQueue` in `ironhermes-gateway/src/session_queue.rs`
- [ ] `crates/ironhermes-cli/tests/tui_queue_drain.rs` — D-04/D-05/D-11/D-12 harness
- [ ] `crates/ironhermes-cli/tests/tui_queue_controls.rs` — D-06/D-07/D-08/D-10 harness
- [ ] `crates/ironhermes-cli/tests/status_line_queue_pill.rs` — D-09 unit tests
- [ ] `App::new_for_test(...)` constructor in `app.rs` (under `#[cfg(test)]`) — minimal `App` with stub agent runtime and injectable `StreamEvent` channel
- [ ] `RecordingFailingAdapter`-style stub for `AgentRuntime` (parity with gateway test pattern at `crates/ironhermes-gateway/tests/uqm_session_queue_unification.rs`)
- [ ] `CommandResult::PauseQueue` / `CommandResult::UnpauseQueue` variants added in `ironhermes-core::commands::mod.rs`, with default no-op match arms wherever non-TUI consumers route `CommandResult`

*If none of the existing test files cover a decision, that decision lives entirely in Wave 0.*

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| Terminal bell on cap-hit (if planner enables) | D-10 | Bell signal is terminal-emulator dependent; many emulators suppress BEL silently | Run TUI in iTerm/Kitty/Alacritty, push 128 items, attempt 129th, listen for bell — record per-emulator result. Research recommends omitting bell from initial impl. |
| `Queue: 1 (paused)` pill renders with correct dim/red style under each theme | D-09 | Visual style review requires human eye on actual terminal | Run TUI under default theme, dark theme, light theme; `/queue x`; `/pause`; capture screenshot per theme. |
| Negative-control test (D-12) fails against pre-fix code | D-12 | Requires git-rebase to a pre-fix commit before merging Phase 36.17.3 | Before final merge: `git stash`, `git checkout <pre-fix-sha> -- crates/ironhermes-cli/src/tui_rata/commands.rs`, `cargo test tui_queue_regression_negative_control` → MUST fail. Restore, document result. |

*All other phase behaviors have automated verification.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (trait module, SessionKey move, test harness, App::new_for_test)
- [ ] No watch-mode flags (no `cargo watch`)
- [ ] Feedback latency < 60 seconds for per-task quick-run
- [ ] `nyquist_compliant: true` set in frontmatter
- [ ] D-12 negative-control verified at least once before merge (manual rebase step)

**Approval:** pending
