---
phase: 21.6-port-deployment-setup-files-from-hermes-agent
plan: 03
subsystem: infra
tags: [bash, installer, setup, curl-pipe, cargo, deployment]

# Dependency graph
requires:
  - phase: 21.6-01
    provides: ".env.example, cli-config.yaml.example, docker/SOUL.md templates"
provides:
  - "install.sh curl-pipe end-user installer with binary download + cargo fallback"
  - "setup-ironhermes.sh post-clone developer setup with cargo build --release"
affects: [deployment, docker, ci-cd]

# Tech tracking
tech-stack:
  added: []
  patterns: ["curl-pipe installer pattern", "developer setup script pattern", "idempotent PATH patching", "chmod 600 credential protection"]

key-files:
  created:
    - install.sh
    - setup-ironhermes.sh
  modified: []

key-decisions:
  - "install.sh downloads from GitHub Releases first, falls back to cargo install -- prioritizes fast install for end users"
  - "setup-ironhermes.sh uses ln -sf symlink instead of cp -- rebuild-friendly for developer workflow"
  - "Both scripts use tput for colors with terminal detection fallback -- safe for non-interactive pipes"
  - "Templates seeded with [ ! -f ] guards -- never overwrite user customizations"

patterns-established:
  - "curl-pipe safety: IS_INTERACTIVE via [ -t 0 ], no read prompts in pipe mode"
  - "Template seeding: check local repo first, download from GitHub raw as fallback"
  - "PATH patching: append to .bashrc/.zshrc/.profile with grep -qF idempotency"

requirements-completed: [D-15, D-16, D-17, D-18, D-19]

# Metrics
duration: 4min
completed: 2026-04-22
---

# Phase 21.6 Plan 03: Install & Setup Scripts Summary

**Curl-pipe installer (install.sh) with GitHub binary download + cargo fallback, and developer setup script (setup-ironhermes.sh) with cargo build --release and symlink workflow**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-22T00:53:37Z
- **Completed:** 2026-04-22T00:57:45Z
- **Tasks:** 2
- **Files created:** 2

## Accomplishments
- Created install.sh (233 lines): OS/arch detection, GitHub Releases binary download, cargo install fallback, directory scaffolding, template seeding, PATH patching
- Created setup-ironhermes.sh (151 lines): Rust toolchain check, cargo build --release, directory scaffolding, template seeding from repo, symlink to ~/.local/bin, PATH patching
- Both scripts scaffold 7 ~/.ironhermes/ subdirectories and seed 3 config templates with skip-if-exists guards
- Both scripts apply chmod 600 to .env for credential protection (T-21.6-12 mitigation)

## Task Commits

Each task was committed atomically:

1. **Task 1: Create install.sh curl-pipe end-user installer** - `21cf5f2` (feat)
2. **Task 2: Create setup-ironhermes.sh post-clone developer setup** - `c7776e5` (feat)

## Files Created/Modified
- `install.sh` - Curl-pipe end-user installer: OS detection, binary download, cargo fallback, scaffolding, PATH
- `setup-ironhermes.sh` - Post-clone developer setup: Rust check, cargo build, scaffolding, symlink, PATH

## Decisions Made
- install.sh downloads from GitHub Releases first, falls back to cargo install -- prioritizes fast install for end users
- setup-ironhermes.sh uses ln -sf symlink instead of cp -- rebuild-friendly for developer workflow
- Both scripts use tput for colors with terminal detection fallback -- safe for non-interactive pipes
- Templates seeded with [ ! -f ] guards -- never overwrite user customizations

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All 3 plans in Phase 21.6 are complete
- Deployment infrastructure established: .env.example, cli-config.yaml.example, .dockerignore, docker/SOUL.md (Plan 01), Dockerfile + entrypoint.sh (Plan 02), install.sh + setup-ironhermes.sh (Plan 03)
- Ready for CI/CD pipeline setup or next phase

## Self-Check: PASSED

- install.sh: FOUND
- setup-ironhermes.sh: FOUND
- 21.6-03-SUMMARY.md: FOUND
- Commit 21cf5f2: FOUND
- Commit c7776e5: FOUND

---
*Phase: 21.6-port-deployment-setup-files-from-hermes-agent*
*Completed: 2026-04-22*
