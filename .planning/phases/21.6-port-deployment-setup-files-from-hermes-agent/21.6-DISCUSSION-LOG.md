# Phase 21.6: Port Deployment Setup Files from hermes-agent - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-21
**Phase:** 21.6-port-deployment-setup-files-from-hermes-agent
**Areas discussed:** .env.example + config template, Dockerfile + entrypoint, Install/setup scripts, Directory bootstrapping, Sandboxing + workspaces

---

## .env.example + config template

### .env.example comprehensiveness

| Option | Description | Selected |
|--------|-------------|----------|
| Mirror hermes-agent fully (Recommended) | Port the full .env.example — document all env vars IronHermes reads today, plus placeholders for upcoming features | ✓ |
| Current features only | Only document env vars that IronHermes actually reads right now | |
| Minimal — just API keys + Telegram | Bare minimum: OPENROUTER_API_KEY, ANTHROPIC_API_KEY, TELEGRAM_BOT_TOKEN | |

**User's choice:** Mirror hermes-agent fully
**Notes:** Forward-looking — include sections for upcoming features so users see what's coming.

### cli-config.yaml.example

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, full config template (Recommended) | Port cli-config.yaml.example with all config sections IronHermes supports | ✓ |
| Skip — config is self-documenting | Don't create a template. Users create config.yaml via setup wizard | |

**User's choice:** Yes, full config template
**Notes:** None.

---

## Dockerfile + entrypoint

### Base image strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Multi-stage with distroless (Recommended) | Build: rust:latest. Runtime: debian-slim or distroless. ~50-100MB | ✓ |
| Single Debian stage | Match hermes-agent: Debian trixie with all deps. ~500MB+ | |
| Alpine-based | Multi-stage with alpine. Smallest but musl libc issues | |

**User's choice:** Multi-stage with distroless
**Notes:** None.

### Python in Docker image

| Option | Description | Selected |
|--------|-------------|----------|
| Include Python3 (Recommended) | Install python3 in runtime image for execute_code sandbox | ✓ |
| Python optional via volume mount | Pure-Rust image. Users mount Python if needed | |
| No Python — WASM sandbox instead | Skip Python. Plan for WASM-based code execution | |

**User's choice:** Include Python3
**Notes:** None.

### Entrypoint bootstrap strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Port hermes-agent pattern (Recommended) | entrypoint.sh: dirs, .env copy, config.yaml copy, SOUL.md, gosu, exec | ✓ |
| Lean entrypoint — let binary handle it | Minimal entrypoint, binary handles bootstrap | |

**User's choice:** Port hermes-agent pattern
**Notes:** None.

---

## Install/setup scripts

### Script scope

| Option | Description | Selected |
|--------|-------------|----------|
| install.sh + setup-ironhermes.sh (Recommended) | install.sh: curl-pipe user installer. setup-ironhermes.sh: dev setup. Skip Windows | ✓ |
| install.sh only | Single script for both user install and dev setup | |
| Cargo install only — no scripts | Users do cargo install. README documents manual setup | |

**User's choice:** install.sh + setup-ironhermes.sh
**Notes:** None.

### Binary distribution

| Option | Description | Selected |
|--------|-------------|----------|
| GitHub Releases + cargo fallback (Recommended) | Download prebuilt binary first, cargo install as fallback | ✓ |
| Cargo install only | Always build from source | |
| You decide | Claude's discretion | |

**User's choice:** GitHub Releases + cargo fallback
**Notes:** None.

---

## Directory bootstrapping

### Directory structure

| Option | Description | Selected |
|--------|-------------|----------|
| Match hermes-agent dirs (Recommended) | cron/, sessions/, logs/, hooks/, memories/, skills/, workspace/. Skip skins/, plans/ | ✓ |
| Minimal — create on demand | Lazy creation. Empty ~/.ironhermes/ on fresh install | |
| Full mirror | Every dir hermes-agent creates, even unused ones | |

**User's choice:** Match hermes-agent dirs
**Notes:** None.

### First-run scaffolding responsibility

| Option | Description | Selected |
|--------|-------------|----------|
| Both — binary + scripts (Recommended) | Binary creates dirs on first run. Scripts also scaffold. Belt and suspenders | ✓ |
| Scripts only | Scripts handle all scaffolding | |
| Binary only | Binary handles everything | |

**User's choice:** Both — binary + scripts
**Notes:** None.

---

## Sandboxing + workspaces

### Sandbox backend scope

| Option | Description | Selected |
|--------|-------------|----------|
| Document local + Docker in config (Recommended) | Config + .env docs for docker backend. Don't implement Docker backend yet | ✓ |
| Implement Docker sandbox backend | Build Docker sandbox in this phase | |
| Config only — defer all new backends | Just document planned backends | |
| WASM sandbox exploration | Explore wasmtime/wasmer instead of Docker | |

**User's choice:** Document local + Docker in config
**Notes:** User asked about WASM vs Docker sandboxing — captured as deferred idea. This phase sets up config documentation; actual backend implementations are separate phases.

### Workspaces

| Option | Description | Selected |
|--------|-------------|----------|
| Create workspace/ dir, document in config (Recommended) | Create ~/.ironhermes/workspace/, document terminal.cwd. Defer per-session isolation | ✓ |
| Full workspace isolation | Per-session workspace dirs | |
| Defer entirely | Don't address workspaces | |

**User's choice:** Create workspace/ dir, document in config
**Notes:** None.

---

## Claude's Discretion

- .env.example ordering and grouping
- Dockerfile layer caching strategy
- install.sh OS/arch detection logic
- .dockerignore contents
- SOUL.md default template content for Docker

## Deferred Ideas

- Windows install scripts (install.ps1, install.cmd)
- Docker sandbox backend implementation
- WASM sandbox exploration
- Per-session workspace isolation
- Interactive setup wizard (Phase 23)
- Skills sync in Docker entrypoint
- Skin/theme engine (skins/ dir)
