---
phase: 21.6
slug: port-deployment-setup-files-from-hermes-agent
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-21
---

# Phase 21.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | cargo test (Rust) + shell script validation |
| **Config file** | Cargo.toml (workspace) |
| **Quick run command** | `cargo test -p ironhermes-cli --lib` |
| **Full suite command** | `cargo test --workspace` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-cli --lib`
- **After every plan wave:** Run `cargo test --workspace`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | D-01..D-03 | — | N/A | file-check | `test -f .env.example` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-04..D-06 | — | N/A | file-check | `test -f cli-config.yaml.example` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-07..D-11 | — | Non-root runtime | file-check | `grep -q 'USER ironhermes' Dockerfile` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-12..D-14 | — | Privilege drop | file-check | `grep -q 'gosu' docker/entrypoint.sh` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-15..D-18 | — | N/A | file-check | `test -f install.sh && test -f setup-ironhermes.sh` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-20..D-22 | — | N/A | unit | `cargo test -p ironhermes-cli ensure_home_dirs` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] All files are net-new — no existing test infrastructure needed beyond cargo test
- [ ] Shell script validation via `shellcheck` if available

*Existing infrastructure covers Rust unit tests. Shell scripts validated via file-existence and grep checks.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Docker image builds and runs | D-07..D-11 | Requires Docker daemon | `docker build -t ironhermes . && docker run --rm ironhermes --version` |
| install.sh curl-pipe safety | D-15..D-17 | Requires clean env | Test in ephemeral VM or container |
| entrypoint.sh privilege drop | D-12..D-13 | Requires Docker + gosu | `docker run --rm ironhermes id` should show non-root |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
