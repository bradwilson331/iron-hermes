# Phase 37.1: setup script not working on macos — Discussion Log

**Date:** 2026-06-06
*Human-reference record of the discuss-phase session. Not consumed by downstream agents — see 37.1-CONTEXT.md for the decisions that drive research/planning.*

## Scouting findings (Claude)
- 6 setup-related scripts found: `install.sh`, `scripts/deploy/install.sh`, `uninstall.sh`, `quick_setup_script.sh`, `setup-ironhermes.sh`, `install-gitea.sh` (+ `quick_setup_script.ps1` Windows).
- Read `install.sh` end-to-end. Flow after OS detection: `resolve_version()` (GitHub Releases API) → `download_binary()` (`ironhermes-<os>-<arch>.tar.gz`) → fallback `cargo install ironhermes`.
- Root cause triangulated: (1) only `ci.yml` exists — no release pipeline builds prebuilt binaries; (2) `install.sh`/README point at `bradwilson331/ironhermes` but the GitHub remote is `bradwilson331/iron-hermes`; (3) not on crates.io (primary remote is Gitea LAN). → 404 → no binary → cargo fallback fails.
- Todo matcher returned 11 matches, all fuzzy false-positives except two setup-*wizard* todos (tangential, not folded).

## Questions & answers

### Round 1 — discovery
1. **Which script failed?** → `install.sh` (option 3, the README installer). User: "stopped at os detection but they need to be unified and handle new installs, update reinstall and uninstall."
2. **Symptom?** → "failed to run after os detection."
3. **Fix scope?** → **Consolidate to one cross-platform script.**

### Round 2 — acquisition & shape
4. **Binary acquisition?** → **Stand up release pipeline this phase.**
5. **Distribution source?** → **Both / configurable** (env-var override).
6. **Lifecycle UX?** → **Subcommands** (`install|update|reinstall|uninstall`).
7. **Windows .ps1 in scope?** → **Yes — keep .ps1 in parity.**

### Round 3 — pipeline & boundary
8. **Release pipeline CI host?** → **GitHub Actions builds, both can serve** (macOS + Linux runners on GH; mirror/serve from either).
9. **macOS Gatekeeper handling?** → **Full Apple notarization.**
10. **37.1 vs 36.12 boundary?** → **Installer + raw tarball release only** — "but no crates.io yet" (crates.io publish deferred; Homebrew/Nix/Termux stay in 36.12).

### Round 4 — notarization deferral + config completeness (post-CONTEXT revision)
- **Apple notarization → DEFERRED** (account not set up). Ship ad-hoc sign + `xattr` quarantine-strip this phase; full notarization is a future phase. (D-07 revised.)
- **Config gap to fill + wizard:** every section of `cli-config.yaml.example` must be accounted for in the wizard; add `crates/ironhermes-kanban` config.
  - Scouted: wizard covers ~9/20 sections (`apply_{model,provider,api_key,learning_loop,memory_provider,hermes_home,gateway_section,tools_section,auxiliary}`). `KanbanConfig` has 17 fields, absent from the example entirely.
11. **Wizard depth?** → **Essentials prompted, rest documented defaults** — "but must be aware of existing ironhermes and hermes installs, later openclaw installs." (D-10 + D-11)
12. **Config example completeness?** → **Full parity with Config structs** + drift test (D-08/D-09).

## Deferred / redirected
- Homebrew/Nix/Termux/crates.io/native Windows packaging → Phase 36.12.
- Config/setup wizard todos → future tooling phase.

## Claude's discretion (noted in CONTEXT)
- Canonical script filename, exact flag surface, Linux aarch64 build mechanism, tag/trigger convention, uninstall data-preservation default (`--purge` opt-in).
