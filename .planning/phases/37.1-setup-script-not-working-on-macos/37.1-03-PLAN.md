---
phase: 37.1-setup-script-not-working-on-macos
plan: 03
type: execute
wave: 1
depends_on: [37.1-01]
files_modified:
  - cli-config.yaml.example
autonomous: true
requirements: [REQ-37.1-02, REQ-37.1-03]
must_haves:
  truths:
    - "cli-config.yaml.example documents every one of the 27 top-level Config sections"
    - "cli-config.yaml.example has a kanban: section documenting all 17 KanbanConfig fields with defaults + comments"
    - "cli-config.yaml.example still deserializes cleanly into the typed Config struct"
    - "config_parity.rs (from Plan 01) is now GREEN"
  artifacts:
    - path: "cli-config.yaml.example"
      provides: "Full-parity, fully-commented config reference covering all typed Config fields + kanban"
      contains: "kanban:"
      min_lines: 470
  key_links:
    - from: "cli-config.yaml.example"
      to: "crates/ironhermes-core/tests/config_parity.rs"
      via: "drift test reads the example and asserts all 27 sections present"
      pattern: "REQUIRED_SECTIONS"
    - from: "cli-config.yaml.example kanban section"
      to: "crates/ironhermes-kanban/src/config.rs"
      via: "17 KanbanConfig field names + defaults transcribed manually (kanban is serde_yaml::Value in Config)"
      pattern: "goal_inner_max_iterations"
---

<objective>
Bring `cli-config.yaml.example` to full parity with the typed `Config` structs: add the 9 currently-missing top-level sections (mcp_servers, autonomous, browser, extract, prompt_caching, kanban, dashboard, tts, audio_cache) and a new `kanban:` section documenting all 17 `KanbanConfig` fields with their defaults + comments. This turns the Plan 01 `config_parity.rs` drift test green and makes the example authoritative.

Purpose: D-08 (full struct parity + drift test) and D-09 (kanban section). The example becomes the single source of truth power users edit.
Output: extended `cli-config.yaml.example`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-CONTEXT.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-RESEARCH.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-PATTERNS.md

<interfaces>
<!-- Field inventory + defaults. From RESEARCH.md §Q2 and §Q3. -->
9 MISSING top-level sections + their fields/defaults:
- autonomous: yolo=false
- mcp_servers: free-form HashMap (one commented example entry suffices)
- browser: headed=false, no_sandbox=false, allowed_domains=[], allowed_schemes=["http","https"], chromium_path=null, timeout_seconds=30, user_data_dir=null
- extract: max_parallel_summaries=4, summary_chunk_chars=100000, refuse_threshold_chars=2000000, summary_tier2_threshold_chars=5000, summary_tier3_threshold_chars=500000, redact_url_patterns=[]
- prompt_caching: enabled=true, ttl="1h"
- dashboard: kanban.tail_interval_ms=250
- tts: provider="edge", ffmpeg_path=null, edge.voice="en-US-AriaNeural", edge.output_format="mp3", elevenlabs.voice_id="pNInz6obpgDQGcFmaJgB", elevenlabs.model_id="eleven_multilingual_v2", elevenlabs.output_format="mp3"
- audio_cache: max_age_days=7, sweep_interval_secs=86400

17 KanbanConfig fields (D-09; defaults from KanbanConfig::default()):
dispatch_in_gateway=true, dispatch_interval_seconds=60, max_in_progress=8 (null=unlimited),
failure_limit=2, stranded_threshold_seconds=1800, dispatch_stale_timeout_seconds=14400,
default_workdir=null, notification_sources=null, notifier_poll_seconds=3, auto_decompose=false,
auto_decompose_per_tick=3, orchestrator_profile="", default_assignee="", decomposer_model="",
auto_promote_children=true, judge_model="", goal_inner_max_iterations=10

CRITICAL: kanban is `serde_yaml::Value` in Config (circular-dep avoidance). So:
- The 9 NEW sections that ARE typed (autonomous/browser/extract/prompt_caching/dashboard/tts/audio_cache/mcp_servers)
  must deserialize cleanly into Config when uncommented? No — keep them COMMENTED so the example still
  deserializes (commented lines are ignored). config_parity asserts KEY presence on the parsed YAML, so at least
  the section HEADER line must be an ACTIVE (uncommented) `section:` key for the drift test to see it.
  Match the EXISTING example's style: top-level section keys are active; nested fields are commented defaults.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 9 missing top-level sections + kanban (17 fields) to cli-config.yaml.example; make config_parity green</name>
  <read_first>
    - cli-config.yaml.example (the file being modified — study how EXISTING sections render active top-level keys vs commented nested defaults so the drift test sees the key while values stay documented)
    - crates/ironhermes-kanban/src/config.rs (authoritative 17 KanbanConfig field names + KanbanConfig::default values)
    - crates/ironhermes-core/tests/config_parity.rs (the Plan-01 drift test — read REQUIRED_SECTIONS + the deserialize round-trip test to learn exactly what "present" means and which form keeps deserialize clean)
    - .planning/phases/37.1-setup-script-not-working-on-macos/37.1-PATTERNS.md (cli-config.yaml.example pattern section, lines 335-371 — section comment style + the full kanban block)
    - .planning/phases/37.1-setup-script-not-working-on-macos/37.1-RESEARCH.md (§Q2 defaults, §Q3 kanban fields, Pitfall 5)
  </read_first>
  <action>
    Edit cli-config.yaml.example. First, run `config_parity` to confirm which sections it reports missing and to learn whether the round-trip test requires the section key to be an active mapping or tolerates an empty/commented value. Then add the 9 missing sections so that BOTH `example_covers_all_typed_config_sections` (key presence) AND `example_deserializes_to_config_without_errors` (clean deserialize into Config) pass. For each section, render the top-level key in whatever form the existing example uses for sections that satisfy both tests (an active `section:` header with commented nested fields, OR an active mapping of default values — match the established convention and the round-trip requirement). Use these exact field names + defaults:
      - `autonomous:` → yolo: false
      - `mcp_servers:` → empty map with one commented example server entry
      - `browser:` → headed, no_sandbox, allowed_domains, allowed_schemes, chromium_path, timeout_seconds, user_data_dir (defaults above)
      - `extract:` → max_parallel_summaries, summary_chunk_chars, refuse_threshold_chars, summary_tier2_threshold_chars, summary_tier3_threshold_chars, redact_url_patterns
      - `prompt_caching:` → enabled, ttl
      - `dashboard:` → kanban: { tail_interval_ms: 250 }
      - `tts:` → provider, ffmpeg_path, edge.{voice,output_format}, elevenlabs.{voice_id,model_id,output_format}
      - `audio_cache:` → max_age_days, sweep_interval_secs
      - `kanban:` → ALL 17 fields with the defaults + inline comments per PATTERNS.md lines 347-367 (transcribe field names exactly from crates/ironhermes-kanban/src/config.rs; null for max_in_progress is the documented unlimited sentinel; empty strings for orchestrator_profile/default_assignee/decomposer_model/judge_model).
    Preserve all existing sections byte-for-byte except where adding the new ones. Keep the file a valid YAML document that deserializes into Config. Per Open Question #3, the kanban section is documented manually (not generated) and the drift test only checks the `kanban:` KEY — but document all 17 fields for the human reader.

    No fenced code blocks in this action — edit the YAML file directly.
  </action>
  <verify>
    <automated>cargo nextest run -p ironhermes-core --test config_parity 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `cargo nextest run -p ironhermes-core --test config_parity` PASSES all tests (example_covers_all_typed_config_sections + example_deserializes_to_config_without_errors + kanban_key_present)
    - SOURCE: `grep -c '^kanban:' cli-config.yaml.example` returns 1 (active top-level key)
    - SOURCE: each of the 17 kanban field names appears — `grep -cE 'dispatch_in_gateway|goal_inner_max_iterations|auto_promote_children|notifier_poll_seconds' cli-config.yaml.example` returns >= 4
    - SOURCE: the 9 new section headers exist — `grep -cE '^(autonomous|mcp_servers|browser|extract|prompt_caching|kanban|dashboard|tts|audio_cache):' cli-config.yaml.example` returns 9
    - BEHAVIOR: `cargo nextest run -p ironhermes-core` (whole crate) stays green (no regression to config_validate.rs)
  </acceptance_criteria>
  <done>cli-config.yaml.example covers all 27 typed Config sections incl. a 17-field kanban block; deserializes cleanly into Config; Plan-01 config_parity.rs is green; no existing-test regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example config → user config.yaml | The example is copied/merged into the user's runtime config; documented defaults become live behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37.1-03-01 | Information Disclosure | tts/mcp_servers example values | mitigate | Use placeholder/default values only; never embed real API keys or secrets in the example (secrets live in `.env`, documented as such) |
| T-37.1-03-02 | Tampering | drift-test integrity | accept | Field-list drift test is maintained-by-hand (Rust has no reflection); accepted per RESEARCH.md Option A — adding a Config field without updating the example fails the test, which is the intended guardrail |
</threat_model>

<verification>
- `cargo nextest run -p ironhermes-core --test config_parity` all green
- All 27 sections present as active top-level keys; 17 kanban fields documented
- Whole-crate `cargo nextest run -p ironhermes-core` green (no config_validate.rs regression)
</verification>

<success_criteria>
cli-config.yaml.example is at full Config struct parity with a complete kanban section; the drift test passes; the example still deserializes into Config.
</success_criteria>

<output>
Create `.planning/phases/37.1-setup-script-not-working-on-macos/37.1-03-SUMMARY.md` when done. Record the rendering convention chosen (active-header vs active-mapping) and confirm config_parity.rs flipped green.
</output>
