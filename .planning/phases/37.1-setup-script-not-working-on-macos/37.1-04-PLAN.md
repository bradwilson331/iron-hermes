---
phase: 37.1-setup-script-not-working-on-macos
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
autonomous: false
requirements: [REQ-37.1-01, REQ-37.1-07]
user_setup:
  - service: github-actions
    why: "Release workflow needs the default GITHUB_TOKEN (auto-provided) to upload release assets; first real release requires pushing a v* tag"
    env_vars: []
    dashboard_config:
      - task: "Confirm Actions has write permission for releases (Settings → Actions → Workflow permissions → Read and write) on bradwilson331/iron-hermes"
        location: "GitHub repo Settings → Actions → General"
must_haves:
  truths:
    - "D-06: stands up the GitHub Actions release pipeline this phase — tag-triggered, building macOS arm64+Intel and Linux multi-arch tarballs uploaded to GitHub Releases (Gitea has no macOS runner)"
    - "A new .github/workflows/release.yml exists alongside ci.yml and never modifies ci.yml"
    - "The workflow triggers on tag push v* only (orthogonal to ci.yml branch/PR triggers)"
    - "The matrix builds 5 artifacts: macOS arm64, macOS x86_64, Linux x86_64, Linux aarch64, Windows x86_64"
    - "Artifact names exactly match install.sh download_binary() expectation ironhermes-${os}-${arch}.tar.gz (and a Windows artifact install.ps1 will fetch)"
    - "macOS steps ad-hoc codesign (-s -); notarization slot-in is documented in a comment for a future phase"
    - "Release assets upload via softprops/action-gh-release@v2"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Tag-triggered cross-platform release build + GitHub Release upload"
      contains: "softprops/action-gh-release"
      min_lines: 50
  key_links:
    - from: ".github/workflows/release.yml"
      to: "install.sh download_binary()"
      via: "matrix artifact names match ironhermes-${os}-${arch}.tar.gz contract"
      pattern: "ironhermes-macos-aarch64"
    - from: ".github/workflows/release.yml"
      to: "install.ps1 (Plan 06)"
      via: "Windows artifact produced here is fetched by install.ps1"
      pattern: "windows"
---

<objective>
Stand up a NEW GitHub Actions release pipeline (`.github/workflows/release.yml`) that triggers on `v*` tag pushes and builds + uploads platform tarballs to GitHub Releases. Build 5 artifacts (macOS arm64 + Intel, Linux x86_64 + aarch64, Windows x86_64) with names that exactly match the installer's `download_binary()` contract. macOS binaries are ad-hoc signed (`codesign -s -`) with a documented notarization slot-in. The Windows artifact is required by Plan 06's `install.ps1` (D-12).

Purpose: D-06 (release pipeline this phase) + D-07 (ad-hoc sign, notarization deferred but structured for slot-in) + the D-12 Windows artifact prerequisite.
Output: `.github/workflows/release.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-CONTEXT.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-RESEARCH.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-PATTERNS.md

<interfaces>
<!-- From ci.yml (analog) + PATTERNS.md. -->
ci.yml conventions to mirror: env CARGO_TERM_COLOR: always; uses actions/checkout@v4,
dtolnay/rust-toolchain@stable, Swatinem/rust-cache@v2 (all already approved).
ci.yml triggers on push develop/main + PRs — release.yml MUST use a disjoint trigger (tags v*).
NOTE: ci.yml sets RUSTFLAGS "-D warnings"; release.yml SHOULD NOT set -D warnings (a warning must
never fail a release build) — omit RUSTFLAGS or set it empty for the release job.

Runner labels (RESEARCH.md §Q5, verified against GitHub docs):
- macos-15        → aarch64-apple-darwin   → artifact ironhermes-macos-aarch64
- macos-15-intel  → x86_64-apple-darwin    → artifact ironhermes-macos-x86_64   (macos-15 alone = ARM only!)
- ubuntu-24.04    → x86_64-unknown-linux-gnu → artifact ironhermes-linux-x86_64
- ubuntu-24.04-arm→ aarch64-unknown-linux-gnu → artifact ironhermes-linux-aarch64 (native; no cross)
- windows-latest  → x86_64-pc-windows-msvc  → artifact ironhermes-windows-x86_64

Artifact-name contract (install.sh builds): ironhermes-${os}-${arch}.tar.gz with os∈{macos,linux},
arch∈{x86_64,aarch64}. detect_platform normalizes arm64→aarch64 — pipeline MUST use aarch64 NOT arm64.
Windows: produce a tarball ironhermes-windows-x86_64.tar.gz (install.ps1 in Plan 06 fetches this exact name).

Package legitimacy (RESEARCH.md): softprops/action-gh-release@v2 APPROVED (well-known, 50M+ uses) —
pin to @v2. Fallback if needed: `gh release upload`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/release.yml (5-target matrix, ad-hoc macOS sign, GitHub Release upload)</name>
  <read_first>
    - .github/workflows/ci.yml (analog: env block, action versions, job/step structure — DO NOT modify this file)
    - .planning/phases/37.1-setup-script-not-working-on-macos/37.1-PATTERNS.md (release.yml pattern section, lines 237-331: trigger, env, matrix, build+sign+package+upload, notarization slot-in comment)
    - .planning/phases/37.1-setup-script-not-working-on-macos/37.1-RESEARCH.md (§Q5 runners + artifact contract + notarization slot-in; Pitfalls 1 & 2)
    - install.sh (download_binary artifact-name construction — match exactly)
  </read_first>
  <action>
    Create .github/workflows/release.yml. Trigger: `on: push: tags: ["v*"]` only. Top-level `env: { CARGO_TERM_COLOR: always }` (do NOT set RUSTFLAGS "-D warnings" — releases must not fail on warnings). One `build` job with a matrix `include` of 5 entries, each carrying `os` (runner label), `target` (rust triple), and `artifact` (base name WITHOUT .tar.gz):
      1. os=macos-15, target=aarch64-apple-darwin, artifact=ironhermes-macos-aarch64
      2. os=macos-15-intel, target=x86_64-apple-darwin, artifact=ironhermes-macos-x86_64
      3. os=ubuntu-24.04, target=x86_64-unknown-linux-gnu, artifact=ironhermes-linux-x86_64
      4. os=ubuntu-24.04-arm, target=aarch64-unknown-linux-gnu, artifact=ironhermes-linux-aarch64
      5. os=windows-latest, target=x86_64-pc-windows-msvc, artifact=ironhermes-windows-x86_64
    `runs-on: ${{ matrix.os }}`. Steps: checkout@v4 → dtolnay/rust-toolchain@stable → Swatinem/rust-cache@v2 → `cargo build --release --bin ironhermes`. Add a macOS-only step `if: runner.os == 'macOS'` running `codesign -s - target/release/ironhermes`, with a comment block documenting the future notarization slot-in (codesign --sign "Developer ID Application: ..." --options runtime + xcrun notarytool submit --wait + xcrun stapler staple, using future secrets APPLE_ID/APPLE_TEAM_ID/NOTARYTOOL_PASSWORD) so D-07 can be added later without restructuring. Package step: on non-Windows, `cp target/release/ironhermes ironhermes && tar czf ${{ matrix.artifact }}.tar.gz ironhermes`; on Windows (`if: runner.os == 'Windows'`) copy `target/release/ironhermes.exe` then tar czf the same artifact base name `ironhermes-windows-x86_64.tar.gz` (so install.ps1 fetches a predictable name) — use a separate Windows-only packaging step OR a cross-shell `tar` (tar is available on windows-latest). Upload step: `softprops/action-gh-release@v2` with `files: ${{ matrix.artifact }}.tar.gz`. Ensure each tarball name resolves to exactly ironhermes-macos-aarch64.tar.gz / ironhermes-macos-x86_64.tar.gz / ironhermes-linux-x86_64.tar.gz / ironhermes-linux-aarch64.tar.gz / ironhermes-windows-x86_64.tar.gz (aarch64 NOT arm64).

    No fenced code blocks in this action — author the YAML directly (use ci.yml + PATTERNS.md as the structural reference).
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/release.yml')); print('TRIGGER', list(d.get(True, d.get('on')) or {})); jobs=d['jobs']; m=jobs['build']['strategy']['matrix']['include']; arts=sorted(e['artifact'] for e in m); print('ARTIFACTS', arts); assert len(m)==5, m; assert all('arm64' not in a for a in arts), 'arm64 must be aarch64'"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c 'tags:' .github/workflows/release.yml` returns >= 1 AND `grep -c 'branches:' .github/workflows/release.yml` returns 0 (tag-only trigger)
    - SOURCE: `grep -cE 'ironhermes-(macos-aarch64|macos-x86_64|linux-x86_64|linux-aarch64|windows-x86_64)' .github/workflows/release.yml` returns >= 5
    - SOURCE: `grep -c 'arm64' .github/workflows/release.yml` returns 0 (must use aarch64, Pitfall 1)
    - SOURCE: `grep -c 'macos-15-intel' .github/workflows/release.yml` returns 1 (explicit Intel runner, Pitfall 2)
    - SOURCE: `grep -c 'codesign -s -' .github/workflows/release.yml` returns 1; `grep -ci 'notarytool' .github/workflows/release.yml` returns >= 1 (slot-in comment present)
    - SOURCE: `grep -c 'softprops/action-gh-release@v2' .github/workflows/release.yml` returns 1
    - SOURCE: `grep -c 'RUSTFLAGS' .github/workflows/release.yml` returns 0 (no -D warnings on release)
    - BEHAVIOR: the python yaml verify command exits 0 (valid YAML, 5 matrix entries, no arm64)
    - BEHAVIOR: ci.yml is byte-unchanged — `git diff --quiet .github/workflows/ci.yml` exits 0
  </acceptance_criteria>
  <done>release.yml exists, tag-triggered, 5-artifact matrix with installer-matching names (aarch64 not arm64), explicit macos-15-intel, ad-hoc codesign + notarization slot-in comment, softprops upload, ci.yml untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify a real tagged release produces all 5 correctly-named assets (REQ-37.1-07)</name>
  <action>Manual verification only — REQ-37.1-07 requires a real GitHub Release on the actual runner matrix (no CI substitute). The executor pauses here; the human follows the how-to-verify steps and replies on the resume-signal.</action>
  <what-built>A new tag-triggered release workflow at .github/workflows/release.yml that builds 5 platform tarballs and uploads them to a GitHub Release. The artifact names match what install.sh and install.ps1 download. CI (ci.yml) is unchanged.</what-built>
  <how-to-verify>
    REQ-37.1-07 can only be fully proven by an actual tagged release on a real runner set (manual, per VALIDATION.md). After this plan merges:
    1. Confirm Actions write permission: GitHub → Settings → Actions → General → Workflow permissions = "Read and write".
    2. Push a test tag, e.g. `git tag v0.2.0-rc1 && git push origin-github v0.2.0-rc1`.
    3. Watch the Actions run; confirm all 5 matrix legs succeed.
    4. On the resulting GitHub Release, confirm these 5 assets exist with EXACT names: ironhermes-macos-aarch64.tar.gz, ironhermes-macos-x86_64.tar.gz, ironhermes-linux-x86_64.tar.gz, ironhermes-linux-aarch64.tar.gz, ironhermes-windows-x86_64.tar.gz.
    5. On a clean Mac, run the README curl install and confirm it pulls the prebuilt tarball with no Gatekeeper "cannot be verified" dialog (ad-hoc sign + installer quarantine strip).
  </how-to-verify>
  <resume-signal>Type "approved" once a tagged release produced all 5 correctly-named assets, or describe which leg/asset failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → GitHub Release | Workflow uploads build artifacts using GITHUB_TOKEN |
| released tarball → end-user machine | Artifacts are downloaded and executed by install.sh / install.ps1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37.1-04-01 | Tampering | third-party Actions | mitigate | Only approved actions used (checkout@v4, rust-toolchain@stable, rust-cache@v2, action-gh-release@v2 per RESEARCH Package Legitimacy Audit); pin to major version tags |
| T-37.1-04-02 | Spoofing/Integrity | macOS binary authenticity | accept | Ad-hoc codesign provides integrity-after-sign only, NOT Gatekeeper trust; full Developer-ID notarization deferred to a future phase (D-07) — accepted for this phase, slot-in documented |
| T-37.1-04-03 | Elevation of Privilege | GITHUB_TOKEN scope | mitigate | Use the default per-run GITHUB_TOKEN with contents:write only; no long-lived secrets added (notarization secrets are future) |
| T-37.1-04-SC | Tampering | Actions supply chain | mitigate | No new package-manager installs; all actions are from the RESEARCH-approved legitimacy table |
</threat_model>

<verification>
- python yaml parse: 5 matrix entries, tag-only trigger, no arm64 strings
- ci.yml byte-unchanged (`git diff --quiet .github/workflows/ci.yml`)
- Human checkpoint: a real `v*` tag produces all 5 correctly-named release assets (REQ-37.1-07)
</verification>

<success_criteria>
A tag-triggered release.yml builds and uploads 5 platform tarballs with installer-matching names, ad-hoc signs macOS, documents notarization slot-in, and leaves ci.yml untouched. The human checkpoint confirms a real release produces the expected assets.
</success_criteria>

<output>
Create `.planning/phases/37.1-setup-script-not-working-on-macos/37.1-04-SUMMARY.md` when done. Record the Windows packaging approach used (separate step vs cross-shell tar) and the checkpoint status.
</output>
