---
phase: 37.1-setup-script-not-working-on-macos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-core/tests/config_parity.rs
  - crates/ironhermes-core/tests/wizard_coverage.rs
  - tests/installer_integration.sh
autonomous: true
requirements: [REQ-37.1-02, REQ-37.1-04, REQ-37.1-05, REQ-37.1-06, REQ-37.1-08]
must_haves:
  truths:
    - "Running `cargo nextest run -p ironhermes-core --test config_parity` executes and FAILS (red) because the example is not yet complete"
    - "Running `cargo nextest run -p ironhermes-core --test wizard_coverage` executes and FAILS (red) because new wizard fns do not yet exist"
    - "Running `bash tests/installer_integration.sh --verify` exits 0 (harness self-check) and the full run FAILS (red) because installer verbs do not yet exist"
  artifacts:
    - path: "crates/ironhermes-core/tests/config_parity.rs"
      provides: "Field-list drift test scaffold for cli-config.yaml.example (REQ-37.1-02)"
      contains: "REQUIRED_SECTIONS"
      min_lines: 30
    - path: "crates/ironhermes-core/tests/wizard_coverage.rs"
      provides: "Wizard section-coverage test scaffold (REQ-37.1-04)"
      contains: "Config::default"
      min_lines: 25
    - path: "tests/installer_integration.sh"
      provides: "Bash subcommand behavior harness for uninstall/update/hermes-agent detection (REQ-37.1-05/-06/-08)"
      contains: "verify_uninstall"
      min_lines: 60
  key_links:
    - from: "crates/ironhermes-core/tests/config_parity.rs"
      to: "cli-config.yaml.example"
      via: "std::fs::read_to_string at CARGO_MANIFEST_DIR/../../cli-config.yaml.example"
      pattern: "cli-config.yaml.example"
    - from: "tests/installer_integration.sh"
      to: "install.sh"
      via: "bash install.sh <verb> under HOME override"
      pattern: "install.sh"
---

<objective>
Create the Wave 0 test scaffolding so every downstream implementation task in this phase has an automated verify hook. Three test files are produced: a Rust config-parity drift test, a Rust wizard-coverage test, and a bash installer-integration harness. All three MUST compile/run and FAIL (red) at the end of this plan — they encode the target behavior that Waves 1-2 will make green.

Purpose: Nyquist compliance — no implementation task may ship without a fast automated check. These scaffolds are the checks.
Output: `crates/ironhermes-core/tests/config_parity.rs`, `crates/ironhermes-core/tests/wizard_coverage.rs`, `tests/installer_integration.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-CONTEXT.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-RESEARCH.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-PATTERNS.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-VALIDATION.md

<interfaces>
<!-- Analog test files the executor must mirror. Extracted from codebase. -->
- crates/ironhermes-core/tests/config_validate.rs — analog for config_parity.rs (load_example_yaml helper, top-level mapping assertions, deserialize round-trip)
- crates/ironhermes-core/tests/wizard_flow.rs — analog for wizard_coverage.rs (imports from ironhermes_core::wizard, Config::default(), pure-fn calls, no rustyline/async)
- crates/ironhermes-gateway/tests/uat-setup-36.17.2.2.sh — analog for installer_integration.sh (set -euo pipefail, log/pass/fail helpers, mktemp -d fixtures, trap cleanup EXIT, main() flag dispatch)

The 27 REQUIRED top-level Config sections (from RESEARCH.md §Q2):
model, agent, terminal, web, gateway, cron, security, rate_limit, skills, exec,
delegation, batch, memory, compression, providers, custom_providers, mcp_servers,
autonomous, tools, auxiliary, browser, extract, prompt_caching, kanban, dashboard,
tts, audio_cache
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold config_parity.rs + wizard_coverage.rs (Rust drift + coverage tests, intentionally red)</name>
  <read_first>
    - crates/ironhermes-core/tests/config_validate.rs (analog: load_example_yaml helper + assertion style)
    - crates/ironhermes-core/tests/wizard_flow.rs (analog: wizard import style, Config::default, pure-fn tests)
    - .planning/phases/37.1-setup-script-not-working-on-macos/37.1-PATTERNS.md (config_parity.rs + wizard_coverage.rs pattern sections, lines 480-588)
    - .planning/phases/37.1-setup-script-not-working-on-macos/37.1-RESEARCH.md (§Q2 the 27 sections; Open Question #3 — kanban is KEY-presence only)
  </read_first>
  <action>
    Create crates/ironhermes-core/tests/config_parity.rs implementing REQ-37.1-02. Mirror config_validate.rs: add a `load_example_yaml()` helper reading `concat!(env!("CARGO_MANIFEST_DIR"), "/../../cli-config.yaml.example")`. Add test `example_covers_all_typed_config_sections` that declares `const REQUIRED_SECTIONS: &[&str]` with EXACTLY these 27 keys: model, agent, terminal, web, gateway, cron, security, rate_limit, skills, exec, delegation, batch, memory, compression, providers, custom_providers, mcp_servers, autonomous, tools, auxiliary, browser, extract, prompt_caching, kanban, dashboard, tts, audio_cache — and asserts each is present as a top-level key (failure message names the missing section). Add test `example_deserializes_to_config_without_errors` that deserializes the full example file into `ironhermes_core::config::Config` via serde_yaml and expects Ok. Add test `kanban_key_present` asserting only that the `kanban` key exists (per Open Question #3 — field-level kanban coverage is NOT this test's job). These tests MUST FAIL now because cli-config.yaml.example is missing mcp_servers, autonomous, browser, extract, prompt_caching, kanban, dashboard, tts, audio_cache.

    Create crates/ironhermes-core/tests/wizard_coverage.rs implementing REQ-37.1-04. Mirror wizard_flow.rs imports. Import (these do NOT exist yet — that is why the test is red): `ironhermes_core::wizard::{apply_kanban_section_answer, write_defaults_if_absent}`. Add test `apply_kanban_section_answer_emits_all_17_fields` building a `Config::default()`, calling `apply_kanban_section_answer(&mut config, "")`, and asserting the returned `serde_yaml::Mapping` contains all 17 kanban keys: dispatch_in_gateway, dispatch_interval_seconds, max_in_progress, failure_limit, stranded_threshold_seconds, dispatch_stale_timeout_seconds, default_workdir, notification_sources, notifier_poll_seconds, auto_decompose, auto_decompose_per_tick, orchestrator_profile, default_assignee, decomposer_model, auto_promote_children, judge_model, goal_inner_max_iterations. Add test `write_defaults_if_absent_is_idempotent` that calls `write_defaults_if_absent` twice on the same key and asserts the second call does not clobber (assert_eq! before/after the second call). Add test `write_defaults_if_absent_inserts_when_missing` asserting a fresh empty mapping gains the key after one call. These MUST FAIL to compile/run now because the two fns are not yet defined in wizard.rs.

    Do NOT add fenced implementation here — write the test files directly. Keep both files free of `#[ignore]`.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-core --test config_parity --no-run 2>&1 | tail -5; cargo test -p ironhermes-core --test wizard_coverage --no-run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c 'REQUIRED_SECTIONS' crates/ironhermes-core/tests/config_parity.rs` returns 1
    - SOURCE: `grep -oE 'goal_inner_max_iterations' crates/ironhermes-core/tests/wizard_coverage.rs | head -1` returns the field name (all 17 kanban fields are referenced)
    - BEHAVIOR: `config_parity.rs` compiles cleanly (`--no-run` exit 0); when run it FAILS on a missing section (red is the goal)
    - BEHAVIOR: `wizard_coverage.rs` FAILS to compile because `apply_kanban_section_answer`/`write_defaults_if_absent` are unresolved — this is the expected red state and documented in the SUMMARY as intentional
  </acceptance_criteria>
  <done>Both Rust test files exist; config_parity compiles and is red on missing sections; wizard_coverage is red (unresolved-symbol) pending Plan 05. The red state is documented as intentional Wave-0 scaffolding.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold tests/installer_integration.sh (bash harness, intentionally red)</name>
  <read_first>
    - crates/ironhermes-gateway/tests/uat-setup-36.17.2.2.sh (analog: header, log/pass/fail helpers, mktemp fixtures, trap cleanup, main flag dispatch)
    - install.sh (the script under test — current verbs do not exist yet)
    - .planning/phases/37.1-setup-script-not-working-on-macos/37.1-PATTERNS.md (installer_integration.sh pattern section, lines 591-666)
    - .planning/phases/37.1-setup-script-not-working-on-macos/37.1-VALIDATION.md (REQ-37.1-05/-06/-08 commands)
  </read_first>
  <action>
    Create tests/installer_integration.sh implementing REQ-37.1-05, REQ-37.1-06, REQ-37.1-08. Start with `#!/usr/bin/env bash` + `set -euo pipefail` + log/warn/fail/pass helpers (match install.sh style). Build fixtures with `mktemp -d` into FAKE_HOME / FAKE_BIN (`$FAKE_HOME/.local/bin`) / FAKE_IH_HOME (`$FAKE_HOME/.ironhermes`); `trap cleanup EXIT`. Implement these test functions, each invoking the installer with `HOME="$FAKE_HOME" IRONHERMES_HOME="$FAKE_IH_HOME" bash install.sh <verb>`:
      - `verify_uninstall` (REQ-37.1-05): after `uninstall`, assert binary file at FAKE_BIN/ironhermes is absent AND FAKE_IH_HOME dir is preserved.
      - `verify_uninstall_purge` (REQ-37.1-05): after `uninstall --purge`, assert FAKE_IH_HOME is removed.
      - `verify_update_additive` (REQ-37.1-06): seed a FAKE_IH_HOME/config.yaml containing a known user key; run `update`; assert the user key value is byte-unchanged (additive merge never clobbers SET values).
      - `verify_hermes_agent_detection` (REQ-37.1-08): create FAKE_HOME/.hermes/config.yaml; run `install`; assert stdout contains "hermes-agent detected" AND assert FAKE_HOME/.hermes/config.yaml is byte-unchanged (never mutated).
      - `verify_openclaw_detection`: create FAKE_HOME/.openclaw/; run `install`; assert stdout contains "openclaw" notice.
    Add a `main()` with flag dispatch: `--verify` runs only a harness self-check (mktemp works, install.sh path exists) and exits 0; `--clean` removes temp dirs; no-arg runs ALL test functions. Make the file executable (chmod +x). Because install.sh has no verb dispatch yet, the no-arg full run MUST FAIL — that is the intended red Wave-0 state.

    Do NOT add fenced implementation here — write the bash file directly.
  </action>
  <verify>
    <automated>bash -n tests/installer_integration.sh && echo SYNTAX_OK && bash tests/installer_integration.sh --verify; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `bash -n tests/installer_integration.sh` exits 0 (valid bash syntax)
    - BEHAVIOR: `bash tests/installer_integration.sh --verify` exits 0 (harness self-check passes)
    - SOURCE: `grep -c 'verify_uninstall\|verify_hermes_agent_detection\|verify_update_additive' tests/installer_integration.sh` returns >= 3
    - SOURCE: `grep -c 'hermes-agent detected' tests/installer_integration.sh` returns >= 1
    - BEHAVIOR: a full no-arg run FAILS today (install.sh lacks verbs) — documented as intentional red
  </acceptance_criteria>
  <done>tests/installer_integration.sh exists, is executable, passes `bash -n` and `--verify`; full run is red pending Plan 02 installer verbs. Red state documented as intentional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → filesystem | Tests create/remove temp dirs under mktemp; must never touch the real `$HOME/.ironhermes` or real `~/.hermes` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37.1-01-01 | Tampering | installer_integration.sh fixtures | mitigate | All paths under `mktemp -d`; HOME/IRONHERMES_HOME overridden per-invocation; `trap cleanup EXIT` removes the temp root; never reference unscoped `$HOME` for writes |
| T-37.1-01-02 | Information Disclosure | hermes-agent detection test | mitigate | Test asserts the real-equivalent fake `~/.hermes/config.yaml` is byte-unchanged after `install` — locks the "detect, never mutate" contract (REQ-37.1-08) |
</threat_model>

<verification>
- `cargo test -p ironhermes-core --test config_parity --no-run` exit 0 (compiles)
- `bash -n tests/installer_integration.sh && bash tests/installer_integration.sh --verify` exit 0
- All three files committed; their red status documented in the SUMMARY as intentional Wave-0 scaffolding
</verification>

<success_criteria>
Three test scaffolds exist and provide automated verify hooks for Waves 1-2. config_parity compiles and is red on missing sections; wizard_coverage is red on unresolved symbols (Plan 05); installer_integration passes syntax + self-check and is red on missing verbs (Plan 02).
</success_criteria>

<output>
Create `.planning/phases/37.1-setup-script-not-working-on-macos/37.1-01-SUMMARY.md` when done. Explicitly record which tests are intentionally red and which plan turns each green (config_parity → Plan 03; wizard_coverage → Plan 05; installer_integration → Plan 02).
</output>
