---
phase: 37.1
slug: setup-script-not-working-on-macos
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-06
---

# Phase 37.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from RESEARCH.md "## Validation Architecture".

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo nextest` (already in CI) + `cargo test --doc` + bash integration tests |
| **Config file** | `nextest.toml` / `.cargo/nextest.toml` (CI uses `--profile ci`) |
| **Quick run command** | `cargo nextest run -p ironhermes-core --test config_parity` |
| **Full suite command** | `cargo nextest run --workspace --all-features` |
| **Estimated runtime** | ~30s (quick) / multi-minute (full) |

---

## Sampling Rate

- **After every task commit:** Run `cargo nextest run -p ironhermes-core` (< 30s)
- **After every plan wave:** Run `cargo nextest run --workspace --all-features`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Req ID | Behavior | Test Type | Automated Command | File Exists |
|--------|----------|-----------|-------------------|-------------|
| REQ-37.1-01 | Release artifact names match installer's `download_binary()` pattern `ironhermes-${os}-${arch}.tar.gz` | unit | `cargo test -p ironhermes-core -- installer_artifact_naming` | ❌ W0 |
| REQ-37.1-02 | `cli-config.yaml.example` covers all typed `Config` fields (drift test) | unit | `cargo nextest run -p ironhermes-core --test config_parity` | ❌ W0 |
| REQ-37.1-03 | `kanban:` section present with all 17 KanbanConfig fields documented | manual grep | `grep -c 'dispatch_in_gateway\|goal_inner_max_iterations' cli-config.yaml.example` ≥1 each | ❌ W0 |
| REQ-37.1-04 | Wizard writes commented blocks for all ~13 missing config sections | unit | `cargo nextest run -p ironhermes-core -- wizard_coverage` | ❌ W0 |
| REQ-37.1-05 | Installer `uninstall` removes binary + PATH lines, preserves `~/.ironhermes/` | bash test | `bash <installer> uninstall && test -d ~/.ironhermes && ! command -v ironhermes` | ❌ W0 |
| REQ-37.1-06 | Installer `update` re-seeds only missing config keys (additive merge) | bash test | shellcheck + integration test against temp `$IRONHERMES_HOME` | ❌ W0 |
| REQ-37.1-07 | Release workflow produces 4 correctly-named tarballs (macOS arm64/x64, Linux x86_64/aarch64) | CI gate | tag-trigger or release dry-run job | ❌ W0 |
| REQ-37.1-08 | hermes-agent detection prints informational message, never mutates `~/.hermes/` | bash test | bash unit test against fake `~/.hermes/config.yaml` | ❌ W0 |

*Req IDs are provisional (CONTEXT lists decisions D-01..D-11; planner finalizes REQ mapping). Status: ⬜ pending · ✅ green · ❌ red.*

---

## Wave 0 Requirements

- [ ] `crates/ironhermes-core/tests/config_parity.rs` — field-list drift test (REQ-37.1-02; verifies `kanban:` key presence per Open Question #3)
- [ ] `crates/ironhermes-core/tests/wizard_coverage.rs` — wizard section-coverage test (REQ-37.1-04)
- [ ] `tests/installer_integration.sh` — bash subcommand behavior tests (REQ-37.1-05, -06, -08), runnable under shellcheck + temp-dir harness

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| End-to-end `curl … \| bash` install on a clean macOS host pulls the prebuilt tarball | REQ-37.1-07 | Needs a real GitHub Release + real macOS host (Gatekeeper/quarantine first-launch) | After a tagged release, run the README curl command on a clean Mac; confirm `ironhermes doctor` passes and no quarantine prompt |
| Ad-hoc codesign + quarantine-strip lets the binary launch without Gatekeeper block | REQ-37.1-07 | Gatekeeper behavior only observable on real macOS first-launch | Download released tarball, run binary, confirm it launches (no "cannot be opened" dialog) |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
