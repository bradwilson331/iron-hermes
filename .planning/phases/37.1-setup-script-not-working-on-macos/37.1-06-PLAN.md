---
phase: 37.1-setup-script-not-working-on-macos
plan: 06
type: execute
wave: 2
depends_on: [37.1-04]
files_modified:
  - install.ps1
  - README.md
autonomous: true
requirements: [REQ-37.1-01]
must_haves:
  truths:
    - "A new minimal install.ps1 exists implementing the install verb ONLY"
    - "install.ps1 downloads the prebuilt Windows binary ironhermes-windows-x86_64.tar.gz produced by the release pipeline"
    - "install.ps1 places ironhermes.exe under a user bin dir and adds it to the user PATH"
    - "install.ps1 defaults its download source to GitHub bradwilson331/iron-hermes and validates an IRONHERMES_REPO override"
    - "update/reinstall/uninstall for Windows are explicitly out of scope (deferred per D-12)"
  artifacts:
    - path: "install.ps1"
      provides: "Minimal Windows install-only installer"
      contains: "ironhermes-windows-x86_64"
      min_lines: 40
  key_links:
    - from: "install.ps1"
      to: ".github/workflows/release.yml Windows artifact"
      via: "downloads ironhermes-windows-x86_64.tar.gz from GitHub Releases"
      pattern: "ironhermes-windows-x86_64"
    - from: "install.ps1"
      to: "user PATH"
      via: "appends bin dir to the user PATH environment variable"
      pattern: "PATH"
---

<objective>
Create a NEW thin Windows installer `install.ps1` implementing the `install` verb ONLY: download the prebuilt Windows binary (`ironhermes-windows-x86_64.tar.gz` from the Plan-04 release pipeline), extract `ironhermes.exe` to a user bin directory, and add it to the user PATH. The `update | reinstall | uninstall` lifecycle for Windows is explicitly deferred (D-12). Document the Windows install path in README.

Purpose: D-12 (minimal Windows install-only support; full lifecycle deferred).
Output: `install.ps1` + a README Windows section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-CONTEXT.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-RESEARCH.md

<interfaces>
<!-- From RESEARCH.md §Q1 (D-12 resolution) + §Security Domain + Plan 04 contract. -->
D-12: ship a MINIMAL new install.ps1 with the `install` verb ONLY. There is NO existing IronHermes
Windows installer (quick_setup_script.ps1 is unrelated LoRA tooling — leave it alone).

The release pipeline (Plan 04) uploads ironhermes-windows-x86_64.tar.gz to GitHub Releases.
install.ps1 must fetch THAT exact asset name from the latest release.

Default source: GitHub https://github.com/bradwilson331/iron-hermes (correct hyphenated name).
IRONHERMES_REPO override: validate scheme (https:// or http://192.168. for Gitea) + reject metacharacters,
mirroring install.sh's validate_repo_url (Security V5, parity with bash installer).

Suggested install dir: %LOCALAPPDATA%\Programs\ironhermes\ — keep it user-scoped (no admin).
PATH edit: user-scoped via [Environment]::SetEnvironmentVariable(...,'User'), idempotent
(only append if the dir is not already on PATH). tar is available on modern Windows (bsdtar).

OUT OF SCOPE (deferred, D-12): update/reinstall/uninstall verbs for Windows; quarantine/Gatekeeper (macOS-only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create minimal install.ps1 (install verb only) + document Windows install in README</name>
  <read_first>
    - install.sh (parity reference: validate_repo_url logic, REPO_OWNER/REPO_NAME defaults, artifact-name construction, latest-release resolution — mirror the contract in PowerShell)
    - .github/workflows/release.yml (Plan-04 output — confirm the exact Windows artifact name ironhermes-windows-x86_64.tar.gz this script must fetch)
    - .planning/phases/37.1-setup-script-not-working-on-macos/37.1-RESEARCH.md (§Q1 D-12 resolution; §Security Domain IRONHERMES_REPO validation; §Q5 artifact naming)
    - README.md (install section to extend with a Windows subsection)
  </read_first>
  <action>
    Create install.ps1 at the repo root. Implement ONLY the install path: (1) Set defaults RepoOwner="bradwilson331", RepoName="iron-hermes"; derive Repo from $env:IRONHERMES_REPO if set, else "https://github.com/$RepoOwner/$RepoName". (2) Add a Test-RepoUrl validation function mirroring install.sh::validate_repo_url — accept only values matching ^https:// or ^http://192.168. and reject any value containing ; | & $ backtick whitespace ( ) < > ; throw + exit 1 on reject (run it before any download). (3) Resolve the latest release tag via Invoke-RestMethod against https://api.github.com/repos/$RepoOwner/$RepoName/releases/latest, honoring an $env:IRONHERMES_VERSION override. (4) Download ironhermes-windows-x86_64.tar.gz from the release to a temp dir via Invoke-WebRequest; extract with tar -xzf (bsdtar ships on Windows 10+); locate ironhermes.exe. (5) Install dir: $InstallDir = Join-Path $env:LOCALAPPDATA 'Programs\ironhermes' (create if absent); copy ironhermes.exe there. (6) PATH: append $InstallDir to the USER PATH via [Environment]::SetEnvironmentVariable('Path', "$current;$InstallDir", 'User') only if not already present (idempotent check on the existing user PATH). (7) Print a success message plus a note that update/reinstall/uninstall are not yet supported on Windows (deferred). Clean up the temp dir. Use Set-StrictMode -Version Latest and $ErrorActionPreference = 'Stop'. Then edit README.md to add a "Windows (install only)" subsection in the install area documenting the download-then-run flow and that update/reinstall/uninstall are deferred on Windows. Leave quick_setup_script.ps1 untouched.

    Write the PowerShell directly — no fenced code blocks in this action.
  </action>
  <verify>
    <automated>test -f install.ps1 && echo PS1_OK; grep -c 'ironhermes-windows-x86_64' install.ps1; grep -c 'iron-hermes' install.ps1; grep -c 'Test-RepoUrl' install.ps1; grep -ci 'windows' README.md</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c 'ironhermes-windows-x86_64' install.ps1` returns >= 1 (fetches the exact Plan-04 artifact)
    - SOURCE: `grep -c 'iron-hermes' install.ps1` returns >= 1 AND `grep -c 'bradwilson331/ironhermes' install.ps1` returns 0 (correct repo name)
    - SOURCE: `grep -c 'Test-RepoUrl' install.ps1` returns >= 2 (definition + call — IRONHERMES_REPO validated)
    - SOURCE: `grep -c "SetEnvironmentVariable" install.ps1` returns >= 1 (user PATH edit)
    - SOURCE: install.ps1 mentions only the install verb — `grep -ciE 'uninstall|reinstall' install.ps1` is 0 OR only appears in the deferred-note string
    - SOURCE: README documents Windows install — `grep -ci 'windows' README.md` returns >= 1
    - BEHAVIOR: `test -f install.ps1` exits 0; file is >= 40 lines
  </acceptance_criteria>
  <done>install.ps1 exists implementing install-only: validates IRONHERMES_REPO, fetches ironhermes-windows-x86_64.tar.gz from the latest release, installs ironhermes.exe to a user dir, adds it to user PATH idempotently, and notes the deferred lifecycle; README has a Windows section; quick_setup_script.ps1 untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user env (IRONHERMES_REPO) → download URL | Untrusted env var interpolated into a download URL (Windows parity with install.sh) |
| released Windows tarball → user machine | Remote artifact downloaded and extracted on the user machine |
| user PATH → install.ps1 edit | Installer edits the user-scoped PATH environment variable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37.1-06-01 | Tampering/Spoofing | IRONHERMES_REPO → download URL | mitigate | Test-RepoUrl: scheme must be https:// or http://192.168.; reject shell/format metacharacters before any Invoke-WebRequest (parity with install.sh validate_repo_url) |
| T-37.1-06-02 | Tampering | tarball extraction | mitigate | Extract into a temp dir, copy only ironhermes.exe to the install dir; remove the temp dir afterward |
| T-37.1-06-03 | Elevation of Privilege | install scope | mitigate | User-scoped install (LOCALAPPDATA) + user PATH edit only; no admin elevation, no machine-wide PATH write |
</threat_model>

<verification>
- `test -f install.ps1` exit 0; file present and >= 40 lines
- grep gates: artifact name, iron-hermes (not ironhermes), Test-RepoUrl def+call, SetEnvironmentVariable, README windows section
- Full end-to-end Windows install is manual (no Windows host in CI verify) — proven via the Plan-04 release checkpoint's Windows asset + a manual run on a Windows machine if available
</verification>

<success_criteria>
A minimal install-only install.ps1 fetches the Plan-04 Windows tarball, installs to a user dir, edits user PATH idempotently, validates IRONHERMES_REPO, and documents the deferred Windows lifecycle; README covers Windows install.
</success_criteria>

<output>
Create `.planning/phases/37.1-setup-script-not-working-on-macos/37.1-06-SUMMARY.md` when done. Note that full Windows lifecycle (update/reinstall/uninstall) is deferred per D-12 and that quick_setup_script.ps1 was left untouched.
</output>
