---
phase: 37.1-setup-script-not-working-on-macos
plan: 02
type: execute
wave: 1
depends_on: [37.1-01]
files_modified:
  - install.sh
  - README.md
  - setup-ironhermes.sh
  - install-gitea.sh
autonomous: true
requirements: [REQ-37.1-05, REQ-37.1-06, REQ-37.1-08]
must_haves:
  truths:
    - "Running `bash install.sh` (no arg) performs an install (default verb)"
    - "Running `bash install.sh uninstall` removes the binary + installer-added PATH lines and preserves ~/.ironhermes/"
    - "Running `bash install.sh uninstall --purge` also removes ~/.ironhermes/"
    - "Running `bash install.sh update` swaps the binary and never clobbers SET config values (additive merge)"
    - "install.sh and README point at iron-hermes (hyphen), not ironhermes"
    - "IRONHERMES_REPO env var overrides the distribution source after URL validation"
    - "Installer detects legacy hermes-agent at ~/.hermes/ and prints an informational message without mutating it"
    - "macOS install strips the quarantine xattr after extracting the binary"
  artifacts:
    - path: "install.sh"
      provides: "Unified cross-platform installer with install|update|reinstall|uninstall verbs"
      contains: "VERB"
      min_lines: 280
    - path: "README.md"
      provides: "Corrected curl install command + documented verbs"
      contains: "iron-hermes"
  key_links:
    - from: "install.sh"
      to: "GitHub Releases iron-hermes"
      via: "download_binary curl URL built from REPO_NAME=iron-hermes / IRONHERMES_REPO"
      pattern: "iron-hermes"
    - from: "install.sh"
      to: "tests/installer_integration.sh"
      via: "verbs exercised by the Wave-0 bash harness"
      pattern: "cmd_uninstall|cmd_update"
---

<objective>
Consolidate the IronHermes setup scripts into ONE canonical cross-platform bash installer (`install.sh`) with `install|update|reinstall|uninstall` subcommands (default = install, curl-pipe friendly). Fix the `ironhermes` → `iron-hermes` repo-name bug in both `install.sh` and `README.md`. Add the validated `IRONHERMES_REPO` override, the macOS quarantine strip, the `cargo install --git` fallback, existing-install detection (IronHermes / hermes-agent / openclaw), and retire the redundant scripts. Turn the Wave-0 `tests/installer_integration.sh` harness green.

Purpose: Root-fix the reported macOS failure (wrong repo name + dead fallback) and deliver full install lifecycle in one script (D-01..D-05, D-11).
Output: extended `install.sh`, corrected `README.md`, retired `setup-ironhermes.sh` + `install-gitea.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-CONTEXT.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-RESEARCH.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-PATTERNS.md

<interfaces>
<!-- Existing install.sh helpers to REUSE as-is (do not rewrite). From PATTERNS.md. -->
install.sh existing helpers (KEEP): detect_platform() [normalizes arm64→aarch64],
resolve_version(), download_binary() [subshell mktemp + trap cleanup + install -m 755],
scaffold_home(), seed_templates() [skip-if-exists guard], update_shell_path() [grep -qF idempotent].
Constants block at lines 17-21: REPO_OWNER="bradwilson331", REPO_NAME="ironhermes" (BUG),
INSTALL_DIR="$HOME/.local/bin", IRONHERMES_HOME="${IRONHERMES_HOME:-$HOME/.ironhermes}", VERSION.

Git remotes confirm correct names: GitHub = bradwilson331/iron-hermes (public, installer default);
Gitea = http://192.168.7.6:8418/twilson/ironhermes (LAN override via IRONHERMES_REPO).

scripts/deploy/install.sh and scripts/deploy/uninstall.sh are the GATEWAY SERVICE installer/uninstaller —
KEEP AS-IS, do NOT touch. The README layout block at lines ~271-276 documenting them stays.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add subcommand dispatch + verbs + repo-name fix + IRONHERMES_REPO validation + quarantine strip + cargo-git fallback to install.sh</name>
  <read_first>
    - install.sh (the file being modified — all 238 lines; reuse existing helpers verbatim)
    - .planning/phases/37.1-setup-script-not-working-on-macos/37.1-PATTERNS.md (install.sh pattern section, lines 25-233: constants fix, subcommand dispatch, detect_existing_install, cmd_uninstall, cargo_install fallback, quarantine strip)
    - .planning/phases/37.1-setup-script-not-working-on-macos/37.1-RESEARCH.md (§Security Domain — IRONHERMES_REPO validation; §Q6 detection paths; §Architecture Patterns additive-merge)
    - tests/installer_integration.sh (the Wave-0 harness this task must turn green — read its assertions to match contract strings)
  </read_first>
  <action>
    Edit install.sh in place. (1) Fix constants: `REPO_NAME="iron-hermes"`; add `IRONHERMES_REPO="${IRONHERMES_REPO:-https://github.com/${REPO_OWNER}/${REPO_NAME}}"`. (2) Add `validate_repo_url()` (Security V5, T-37.1-02-01): accept IRONHERMES_REPO only if it matches `^https://` OR `^http://192\.168\.` (Gitea LAN); reject any value containing shell metacharacters (`;`, `|`, `&`, `$`, backtick, spaces, `(`, `)`, `<`, `>`); on reject call log_error + exit 1. Call it before any curl that uses IRONHERMES_REPO. (3) Update download_binary URL to build from REPO_OWNER/REPO_NAME (now correct); after the `install -m 755` line add the macOS quarantine strip: `if [ "$OS" = "macos" ]; then xattr -d com.apple.quarantine "$INSTALL_DIR/ironhermes" 2>/dev/null || true; fi`. (4) Replace the dead `cargo install ironhermes` body with `cargo install --git "$IRONHERMES_REPO" ironhermes` behind the existing `command -v cargo` guard, keeping the rustup hint on failure. (5) Add `detect_existing_install()` setting IRONHERMES_EXISTS / HERMES_AGENT_EXISTS / OPENCLAW_EXISTS: IronHermes if `~/.ironhermes/config.yaml` or `.env` present; hermes-agent if `~/.hermes/` dir or `~/.hermes/config.yaml` present → print EXACT string `Legacy hermes-agent detected at ~/.hermes/. IronHermes uses ~/.ironhermes/ — your existing hermes-agent data is untouched.` and NEVER read/write under `~/.hermes/`; openclaw if `~/.openclaw/` present → print a forward-compat notice containing `openclaw`. (6) Extend seed_templates with a `mode` param (install|update|reinstall): install/update only seed when file absent; reinstall additively appends missing top-level sections to an existing config.yaml as commented blocks but NEVER overwrites a SET value (use the `[ ! -f ]` guard + append-if-section-absent via `grep -qE '^section:'`). Keep `chmod 600` on `.env` and ensure it runs on update too. (7) Add verb functions `cmd_install` (current main body + detect_existing_install), `cmd_update` (resolve_version → download_binary/cargo fallback → seed_templates update → re-chmod 600 .env; never re-scaffold over data), `cmd_reinstall` (force re-fetch + scaffold_home + seed_templates reinstall, preserving .env/config.yaml), `cmd_uninstall` (rm binary; strip installer-added PATH lines via the `# Added by IronHermes installer` marker + grep -v; preserve `~/.ironhermes/` unless `--purge`/`IRONHERMES_PURGE=true`). (8) Replace the trailing `main "$@"` with verb dispatch: `VERB="${1:-install}"`; case install|update|reinstall|uninstall → cmd_*; passing remaining args (e.g. `--purge`) through to the verb; unknown verb → usage + exit 1. Keep `set -euo pipefail` and the `IS_INTERACTIVE` TTY guard intact.

    No fenced code blocks in this action — make the edits directly in install.sh.
  </action>
  <verify>
    <automated>bash -n install.sh && shellcheck -S error install.sh 2>/dev/null; bash tests/installer_integration.sh</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c 'REPO_NAME="iron-hermes"' install.sh` returns 1 AND `grep -c 'REPO_NAME="ironhermes"' install.sh` returns 0
    - SOURCE: `grep -c 'cargo install --git' install.sh` returns 1; `grep -c 'cargo install ironhermes$' install.sh` returns 0
    - SOURCE: `grep -c 'com.apple.quarantine' install.sh` returns >= 1
    - SOURCE: `grep -c 'validate_repo_url' install.sh` returns >= 2 (definition + call)
    - SOURCE: `grep -c 'hermes-agent detected' install.sh` returns 1
    - BEHAVIOR: `bash -n install.sh` exits 0
    - BEHAVIOR: `bash tests/installer_integration.sh` exits 0 (Wave-0 harness now GREEN: uninstall preserves/purges, update is additive, hermes-agent detected + unmutated, openclaw notice)
  </acceptance_criteria>
  <done>install.sh has 4 verbs with default=install; repo name fixed; IRONHERMES_REPO validated; quarantine stripped on macOS; cargo-git fallback; existing-install detection prints the exact hermes-agent string and never mutates ~/.hermes/; tests/installer_integration.sh exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Fix README curl URL + document verbs; retire setup-ironhermes.sh and install-gitea.sh</name>
  <read_first>
    - README.md (lines ~16-24 install section with the wrong curl URL; lines ~36-41 git clone; lines ~50-62 wizard/first-launch; lines ~271-286 layout block)
    - setup-ironhermes.sh (being retired — confirm its dev-build use case is covered by the cargo-git fallback note)
    - install-gitea.sh (being retired — confirm its Gitea use case is covered by IRONHERMES_REPO)
    - install.sh (verb surface to document)
  </read_first>
  <action>
    Edit README.md: (1) Fix the install curl command (line ~23) and any other `bradwilson331/ironhermes` occurrence to `bradwilson331/iron-hermes`; the raw URL becomes `https://raw.githubusercontent.com/bradwilson331/iron-hermes/main/install.sh`. (2) Replace the header-comment usage in install.sh (line 5) similarly to use iron-hermes. (3) In the README install section, document the four verbs: `install` (default), `update`, `reinstall`, `uninstall` (+ `--purge`), and the `IRONHERMES_REPO` override (default GitHub `bradwilson331/iron-hermes`; set to the Gitea URL for LAN installs). (4) Add a short "Build from source" note pointing at `cargo install --git` (replacing the retired setup-ironhermes.sh / install-gitea.sh dev paths). (5) Do NOT alter the `scripts/deploy/` layout block (gateway service install.sh/uninstall.sh stay documented). Then `git rm setup-ironhermes.sh install-gitea.sh` (retired per D-01; functions absorbed by cargo-git fallback + IRONHERMES_REPO). Leave quick_setup_script.sh / quick_setup_script.ps1 untouched (they are unrelated LoRA tooling — RESEARCH.md confirmed; note this in the SUMMARY).
  </action>
  <verify>
    <automated>grep -c 'bradwilson331/ironhermes' README.md install.sh; test ! -f setup-ironhermes.sh && test ! -f install-gitea.sh && echo RETIRED_OK; grep -c 'iron-hermes' README.md</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c 'bradwilson331/ironhermes' README.md install.sh` returns 0 (no wrong-name occurrences remain in either file)
    - SOURCE: `grep -c 'iron-hermes' README.md` returns >= 1
    - SOURCE: README documents all four verbs — `grep -c 'uninstall\|reinstall\|update' README.md` returns >= 3
    - SOURCE: `grep -c 'IRONHERMES_REPO' README.md` returns >= 1
    - BEHAVIOR: `test ! -f setup-ironhermes.sh && test ! -f install-gitea.sh` exits 0 (both retired)
  </acceptance_criteria>
  <done>README + install.sh header use iron-hermes; the four verbs + IRONHERMES_REPO documented; setup-ironhermes.sh and install-gitea.sh removed; gateway-service docs untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user env (IRONHERMES_REPO) → curl URL | Untrusted env var interpolated into a download URL |
| user shell RC files → installer edits | Installer appends/removes PATH lines in ~/.bashrc/.zshrc/.profile |
| downloaded tarball → local filesystem | Remote artifact extracted on the user machine |
| legacy ~/.hermes/ → installer | Pre-existing third-party data the installer must not touch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37.1-02-01 | Tampering/Spoofing | IRONHERMES_REPO → curl URL | mitigate | `validate_repo_url()`: scheme must be `https://` or `http://192.168.`; reject shell metacharacters before any curl/cargo use |
| T-37.1-02-02 | Tampering | update_shell_path / cmd_uninstall RC edits | mitigate | Reuse `grep -qF` exact-match add; removal keys on the literal `# Added by IronHermes installer` marker + literal PATH line; no interpolation of user input into RC files |
| T-37.1-02-03 | Tampering | tarball extraction | mitigate | Keep download_binary's `mktemp -d` + `trap cleanup EXIT`; `install` only the named `ironhermes` binary (no blanket extract into INSTALL_DIR) |
| T-37.1-02-04 | Information Disclosure | ~/.ironhermes/.env | mitigate | `chmod 600` on `.env` enforced on install AND update paths |
| T-37.1-02-05 | Information Disclosure / Integrity | legacy ~/.hermes/ | mitigate | Detection is existence-check only; never read contents, never write; asserted byte-unchanged by installer_integration.sh REQ-37.1-08 |
</threat_model>

<verification>
- `bash -n install.sh` exit 0; `shellcheck -S error install.sh` clean of error-level findings
- `bash tests/installer_integration.sh` exit 0 (REQ-37.1-05/-06/-08 green)
- No `bradwilson331/ironhermes` (wrong name) anywhere in install.sh or README.md
- setup-ironhermes.sh + install-gitea.sh removed; scripts/deploy/* untouched
</verification>

<success_criteria>
One canonical installer with install|update|reinstall|uninstall (default install, curl-pipe safe); repo-name bug fixed in both files; validated IRONHERMES_REPO override; macOS quarantine strip; cargo-git fallback; non-destructive existing-install detection; redundant scripts retired; Wave-0 bash harness green.
</success_criteria>

<output>
Create `.planning/phases/37.1-setup-script-not-working-on-macos/37.1-02-SUMMARY.md` when done. Note that quick_setup_script.{sh,ps1} were intentionally left untouched (unrelated LoRA tooling) and that scripts/deploy/{install,uninstall}.sh remain the gateway-service scripts.
</output>
