---
phase: 37.1-setup-script-not-working-on-macos
plan: 05
type: execute
wave: 2
depends_on: [37.1-01, 37.1-03]
files_modified:
  - crates/ironhermes-core/src/wizard.rs
  - crates/ironhermes-cli/src/setup.rs
autonomous: true
requirements: [REQ-37.1-04]
must_haves:
  truths:
    - "The wizard accounts for EVERY config section — interactive essentials plus commented-default blocks for the ~13 currently-missing sections"
    - "apply_kanban_section_answer emits all 17 KanbanConfig fields as a serde_yaml::Mapping"
    - "write_defaults_if_absent inserts a section only when absent and never clobbers a SET user value (additive merge)"
    - "On update/reinstall the wizard never overwrites a value the user already set"
    - "The learning: raw-YAML splice path remains the precedent for the new commented-default writes"
    - "wizard_coverage.rs (from Plan 01) is now GREEN"
  artifacts:
    - path: "crates/ironhermes-core/src/wizard.rs"
      provides: "New apply_*_answer + write_defaults_if_absent + apply_kanban_section_answer covering all config sections"
      contains: "apply_kanban_section_answer"
      min_lines: 235
    - path: "crates/ironhermes-cli/src/setup.rs"
      provides: "Wizard call sites wiring the new section writers into the load-mutate-save flow"
      contains: "write_defaults_if_absent"
  key_links:
    - from: "crates/ironhermes-core/src/wizard.rs"
      to: "crates/ironhermes-core/tests/wizard_coverage.rs"
      via: "new fns referenced by the Plan-01 coverage test"
      pattern: "apply_kanban_section_answer|write_defaults_if_absent"
    - from: "crates/ironhermes-cli/src/setup.rs"
      to: "config.yaml"
      via: "load-mutate-save splicing commented-default blocks for absent sections"
      pattern: "write_defaults_if_absent"
---

<objective>
Extend the setup wizard so EVERY config section is accounted for at first-run and on update/reinstall: interactively prompt the essentials (already covered) and write commented, default-valued blocks for the ~13 currently-missing sections (agent, terminal, web, exec, cron, compression, skills, delegation, rate_limit, batch, security, custom_providers, + advanced kanban). Add `apply_kanban_section_answer` (all 17 fields) and a generic `write_defaults_if_absent` additive-merge helper, then wire them into `setup.rs`. Honor the `learning:` raw-YAML splice precedent and the D-11 never-clobber rule. Turn the Plan-01 `wizard_coverage.rs` test green.

Purpose: D-10 (every section accounted for) + D-11 (additive merge, never clobber SET values).
Output: extended `wizard.rs` (+ inline tests), wired `setup.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-CONTEXT.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-RESEARCH.md
@.planning/phases/37.1-setup-script-not-working-on-macos/37.1-PATTERNS.md

<interfaces>
<!-- From wizard.rs (analog) + RESEARCH.md §Q4 + PATTERNS.md. -->
Existing wizard.rs fns (KEEP): apply_model_answer, apply_provider_answer, apply_api_key_answer,
apply_learning_loop_answer (returns serde_yaml::Mapping for raw-YAML splice — the precedent for new blocks),
apply_memory_provider_answer, apply_hermes_home_answer, apply_gateway_section_answer (stub),
apply_tools_section_answer (stub), apply_auxiliary_answer. Inline #[cfg(test)] tests live in wizard.rs itself.

setup.rs additional fns: apply_tool_prereq_answers, apply_skills_prereq_answers, apply_terminal_answer,
apply_minimum_viable_answers. setup.rs is the I/O driver (rustyline); wizard.rs holds pure-fn helpers.

13 sections needing commented-default blocks (RESEARCH §Q4): agent, terminal, web, exec, cron, compression,
skills, delegation, rate_limit, batch, security, custom_providers, kanban(advanced). Plus re-seed gateway/tools
if missing. learning: is a raw-YAML splice (apply_learning_loop_answer) and is NOT a typed Config field — keep that path.

17 kanban fields (for apply_kanban_section_answer): dispatch_in_gateway, dispatch_interval_seconds,
max_in_progress, failure_limit, stranded_threshold_seconds, dispatch_stale_timeout_seconds, default_workdir,
notification_sources, notifier_poll_seconds, auto_decompose, auto_decompose_per_tick, orchestrator_profile,
default_assignee, decomposer_model, auto_promote_children, judge_model, goal_inner_max_iterations.

New helper contract (PATTERNS.md lines 422-436): write_defaults_if_absent(yaml_value: &mut serde_yaml::Value,
section_key: &str, default_block: &str) — pure; inserts parsed default_block only when section_key is absent
in the mapping; idempotent (second call is a no-op). Caller does load-mutate-save.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add apply_kanban_section_answer + write_defaults_if_absent + per-section default-block constants to wizard.rs</name>
  <read_first>
    - crates/ironhermes-core/src/wizard.rs (the file being modified — copy apply_learning_loop_answer's serde_yaml::Mapping splice + inline test style)
    - crates/ironhermes-core/tests/wizard_coverage.rs (the Plan-01 test defining the EXACT signatures/behaviors these fns must satisfy — read it first; it is the spec)
    - crates/ironhermes-kanban/src/config.rs (authoritative 17 field names + defaults for apply_kanban_section_answer)
    - .planning/phases/37.1-setup-script-not-working-on-macos/37.1-PATTERNS.md (wizard.rs pattern section, lines 373-476: raw-YAML splice, write_defaults_if_absent, inline test patterns)
    - cli-config.yaml.example (Plan-03 output — the documented default values the commented blocks should mirror)
  </read_first>
  <behavior>
    - apply_kanban_section_answer(&mut Config, "") returns a serde_yaml::Mapping containing all 17 kanban keys with their defaults (dispatch_in_gateway=true, dispatch_interval_seconds=60, max_in_progress=8, failure_limit=2, stranded_threshold_seconds=1800, dispatch_stale_timeout_seconds=14400, default_workdir=null, notification_sources=null, notifier_poll_seconds=3, auto_decompose=false, auto_decompose_per_tick=3, orchestrator_profile="", default_assignee="", decomposer_model="", auto_promote_children=true, judge_model="", goal_inner_max_iterations=10)
    - write_defaults_if_absent on an empty mapping + a section key inserts the parsed default block (key now present)
    - write_defaults_if_absent called twice with DIFFERENT default blocks leaves the first value intact (idempotent, never clobbers) — assert_eq! before/after second call
  </behavior>
  <action>
    Edit crates/ironhermes-core/src/wizard.rs. Add `pub fn apply_kanban_section_answer(config: &mut Config, enable: &str) -> serde_yaml::Mapping` mirroring apply_learning_loop_answer's Mapping-building style; insert all 17 KanbanConfig keys with the exact defaults above (use serde_yaml::Value::{Bool,Number,String,Null} appropriately; max_in_progress=Number(8), default_workdir/notification_sources=Null, the four empty-string fields=String("")). Add `pub fn write_defaults_if_absent(yaml_value: &mut serde_yaml::Value, section_key: &str, default_block: &str)` per the PATTERNS contract: if yaml_value is a Mapping and does not contain section_key, parse default_block via serde_yaml::from_str into a Value and insert; otherwise no-op. Add module-level `pub const`/`pub fn` default-block YAML strings (or a single `pub fn default_block_for(section: &str) -> Option<&'static str>`) for the 13 sections (agent, terminal, web, exec, cron, compression, skills, delegation, rate_limit, batch, security, custom_providers, kanban-advanced), with values mirroring cli-config.yaml.example. Add inline `#[cfg(test)]` tests in wizard.rs proving: kanban returns 17 keys, write_defaults_if_absent inserts-when-absent and is-no-op-when-present. Do not break existing apply_* fns or their inline tests.

    Write the Rust directly — no fenced code blocks in this action.
  </action>
  <verify>
    <automated>cargo nextest run -p ironhermes-core --test wizard_coverage 2>&1 | tail -15; cargo nextest run -p ironhermes-core wizard 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `cargo nextest run -p ironhermes-core --test wizard_coverage` PASSES (all 3 Plan-01 tests green)
    - SOURCE: `grep -c 'pub fn apply_kanban_section_answer' crates/ironhermes-core/src/wizard.rs` returns 1
    - SOURCE: `grep -c 'pub fn write_defaults_if_absent' crates/ironhermes-core/src/wizard.rs` returns 1
    - SOURCE: all 17 kanban field names present — `grep -cE 'dispatch_in_gateway|goal_inner_max_iterations|auto_promote_children|notifier_poll_seconds|dispatch_stale_timeout_seconds' crates/ironhermes-core/src/wizard.rs` returns >= 5
    - SOURCE: the 13 default-block sections referenced — `grep -cE 'agent|terminal|web|exec|cron|compression|skills|delegation|rate_limit|batch|security|custom_providers' crates/ironhermes-core/src/wizard.rs` returns >= 12
    - BEHAVIOR: whole-crate `cargo nextest run -p ironhermes-core` green (no wizard_flow.rs regression)
  </acceptance_criteria>
  <done>wizard.rs has apply_kanban_section_answer (17 fields) + write_defaults_if_absent (idempotent) + 13 default-block sources + inline tests; wizard_coverage.rs is green; no existing-test regression.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the section writers into setup.rs load-mutate-save (additive merge, never clobber SET values)</name>
  <read_first>
    - crates/ironhermes-cli/src/setup.rs (the file being modified — find the existing config.yaml load-mutate-save flow that calls apply_learning_loop_answer's Mapping splice; mirror it)
    - crates/ironhermes-core/src/wizard.rs (the new pub fns from Task 1 to call)
    - .planning/phases/37.1-setup-script-not-working-on-macos/37.1-RESEARCH.md (§Q6 additive-merge strategy; Pitfall 6 — never clobber on reinstall/update)
    - .planning/phases/37.1-setup-script-not-working-on-macos/37.1-CONTEXT.md (D-11 detail: preserve user values, only fill missing)
  </read_first>
  <action>
    Edit crates/ironhermes-cli/src/setup.rs. At the point where the wizard finalizes config.yaml (the same place apply_learning_loop_answer's Mapping is spliced), load the existing config.yaml as a serde_yaml::Value (or build fresh on first install), then for each of the 13 sections call `write_defaults_if_absent(&mut yaml, "<section>", default_block_for("<section>"))` so a section is written only when absent — preserving any value the user already set. Call `apply_kanban_section_answer` and splice its Mapping under the `kanban:` key only if `kanban` is absent. Ensure the gateway/tools stub sections also get at-least-default blocks when missing (close the stub gap). Keep the existing essentials-prompt path and the learning: splice unchanged. The merge must be additive on EVERY path (fresh install writes all; update/reinstall fills only gaps) so a SET user value is never overwritten. Save back to config.yaml via the existing write path.

    Write the Rust directly — no fenced code blocks in this action.
  </action>
  <verify>
    <automated>cargo nextest run -p ironhermes-core 2>&1 | tail -8; cargo build -p ironhermes-cli 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c 'write_defaults_if_absent' crates/ironhermes-cli/src/setup.rs` returns >= 1
    - SOURCE: `grep -c 'apply_kanban_section_answer' crates/ironhermes-cli/src/setup.rs` returns 1
    - BEHAVIOR: `cargo build -p ironhermes-cli` exits 0
    - BEHAVIOR: `cargo nextest run -p ironhermes-core` stays green
    - BEHAVIOR (additive-merge proof): the wizard_coverage idempotency test (write_defaults_if_absent no-op on present key) covers the never-clobber contract; setup.rs uses that helper for every section
  </acceptance_criteria>
  <done>setup.rs splices commented-default blocks for all 13 sections + kanban via write_defaults_if_absent, additive-merge only (never clobbers SET values), gateway/tools stub gap closed; ironhermes-cli builds; core tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| existing config.yaml → wizard rewrite | The wizard reads then rewrites user config; a SET value must survive untouched |
| wizard input → config.yaml | User-entered values written into config; the raw-YAML splice path must survive unknown keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37.1-05-01 | Tampering | additive merge over existing config | mitigate | write_defaults_if_absent inserts ONLY when the section key is absent; proven no-op-on-present by the idempotency test — a SET user value is never overwritten (D-11) |
| T-37.1-05-02 | Information Disclosure | secrets in config.yaml | mitigate | Wizard writes only non-secret defaults to config.yaml; API keys continue to go to `.env` (existing apply_api_key path) — no secret values in the commented default blocks |
| T-37.1-05-03 | Tampering | unknown-key survival | mitigate | Use the serde_yaml load-mutate-save (learning: splice) precedent so unrecognized user keys round-trip unharmed |
</threat_model>

<verification>
- `cargo nextest run -p ironhermes-core --test wizard_coverage` green (REQ-37.1-04)
- whole-crate `cargo nextest run -p ironhermes-core` green; `cargo build -p ironhermes-cli` exit 0
- setup.rs calls write_defaults_if_absent + apply_kanban_section_answer in the additive-merge flow
</verification>

<success_criteria>
The wizard accounts for every config section (interactive essentials + commented-default blocks for the 13 missing sections + 17-field kanban), additive-merge never clobbers SET user values, honors the learning: splice precedent, and wizard_coverage.rs is green.
</success_criteria>

<output>
Create `.planning/phases/37.1-setup-script-not-working-on-macos/37.1-05-SUMMARY.md` when done. Record the default-block source mechanism (const strings vs default_block_for fn) and confirm wizard_coverage.rs flipped green.
</output>
