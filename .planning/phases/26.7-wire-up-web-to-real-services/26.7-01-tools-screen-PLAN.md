---
phase: 26.7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/iron_hermes_ui/src/components/hermes_app/screens/tools.rs
autonomous: true
requirements:
  - 26.7-D-01
  - 26.7-D-03
  - 26.7-D-12
  - 26.7-D-13
status: pending
estimated_complexity: small
must_haves:
  truths:
    - "Tools screen renders the live tool catalog from the running runtime (not stub_data)"
    - "Empty-while-loading state renders the existing `.grid` container with zero cards"
    - "Error state renders inline danger-colored copy: 'Could not load tools — check server connection.'"
  artifacts:
    - path: crates/iron_hermes_ui/src/components/hermes_app/screens/tools.rs
      provides: "ScreenTools wired to api::list_tools via use_server_future"
      contains: "use_server_future(crate::server::api::list_tools)"
  key_links:
    - from: "crates/iron_hermes_ui/src/components/hermes_app/screens/tools.rs"
      to: "crates/iron_hermes_ui/src/server/api.rs::list_tools"
      via: "use_server_future suspension"
      pattern: "use_server_future\\(crate::server::api::list_tools\\)"
---

<objective>
Wire the Tools screen to the live `api::list_tools()` server fn — the simplest of the five
wire-ups and the pattern validation for the rest of phase 26.7. Replace `stub_data::tools()`
with `use_server_future(api::list_tools)?`, render `Vec<ToolInfo>` into the existing
`.tool-card` grid, and add the inline error row required by UI-SPEC.

Purpose: Validate the canonical fetch pattern (Sessions screen analog) end-to-end on the
lowest-risk screen before applying it to the other four. Per RESEARCH §"Screen 1": no new
types, no new server fn, no AppState changes — pure stub → live swap.

Output: ScreenTools renders live tool catalog from `state.runtime_bundle.registry`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.7-wire-up-web-to-real-services/26.7-CONTEXT.md
@.planning/phases/26.7-wire-up-web-to-real-services/26.7-UI-SPEC.md
@.planning/phases/26.7-wire-up-web-to-real-services/26.7-RESEARCH.md
@.planning/phases/26.7-wire-up-web-to-real-services/26.7-PATTERNS.md
@crates/iron_hermes_ui/CLAUDE.md
@crates/iron_hermes_ui/src/components/hermes_app/screens/sessions.rs
@crates/iron_hermes_ui/src/components/hermes_app/screens/tools.rs
@crates/iron_hermes_ui/src/server/api.rs

<interfaces>
<!-- ToolInfo already exists in api.rs lines 30-34 — DO NOT redefine. -->

From crates/iron_hermes_ui/src/server/api.rs (lines 30-34, 104-121):
- `pub struct ToolInfo { pub name: String, pub description: String }` (derives Serialize, Deserialize, Clone, Debug, PartialEq)
- `#[get("/api/tools")] pub async fn list_tools() -> Result<Vec<ToolInfo>>` — already wired to `state.runtime_bundle.registry.read().await.get_definitions(None)`.

Canonical fetch pattern from sessions.rs:33,48-51:
- `let resource = use_server_future(crate::server::api::list_sessions)?;`
- `let data: Vec<...> = match resource() { Some(Ok(v)) => v, _ => Vec::new() };`
- `?` on `use_server_future` suspends until first resolution; loading == None arm of match.
</interfaces>
</context>

<reconciliations>
- **R-1 applies:** No AppState changes. ToolInfo already exists. Skip any task that adds fields to AppState.
</reconciliations>

<tasks>

<task type="auto">
  <name>Task 1: Replace stub_data with use_server_future + render ToolInfo</name>
  <files>crates/iron_hermes_ui/src/components/hermes_app/screens/tools.rs</files>
  <action>
Apply the Sessions screen wire-up pattern to ScreenTools per D-12, D-13.

1. Remove the `use crate::mocks::stub_data::{tools, ToolStub};` import. Replace with
   `use crate::server::api;` (or keep using `crate::server::api::ToolInfo` fully-qualified
   inline — match the import style already present in sessions.rs).
2. Inside `fn ScreenTools()`, immediately after `let active = use_context::<Signal<...>>()`
   (or wherever the existing context reads happen), add:
   `let tools_resource = use_server_future(crate::server::api::list_tools)?;`
   The trailing `?` is required — it suspends the component until the first resolution
   per D-03 / Pattern 1 from PATTERNS.md.
3. Right after the resource fetch, extract the data using the canonical loading guard:
   ```
   let tools_list: Vec<crate::server::api::ToolInfo> = match tools_resource() {
       Some(Ok(v)) => v,
       _ => Vec::new(),
   };
   ```
   This satisfies D-13 loading state (empty grid while None).
4. For the error state (D-13 + UI-SPEC Copywriting Contract), compute a flag BEFORE the
   `rsx!` block (do NOT call `tools_resource()` inside RSX — keep signal borrow discipline
   per iron_hermes_ui/clippy.toml):
   ```
   let load_error = matches!(tools_resource(), Some(Err(_)));
   ```
5. In the RSX where the existing `for tool in tools.iter() { ToolCard { ... } }` block lives
   inside `.grid`, render the error row when `load_error` is true and otherwise iterate
   `tools_list`. Exact RSX shape inside the `.grid` div:
   ```
   if load_error {
       div {
           style: "color:var(--danger);font-size:var(--fs-12);",
           "Could not load tools — check server connection."
       }
   } else {
       for tool in tools_list.iter() {
           ToolCard { key: "{tool.name}", tool: tool.clone() }
       }
   }
   ```
   Copy MUST match UI-SPEC exactly: `"Could not load tools — check server connection."`
   (em dash, lowercase after).
6. Update `ToolCard` (currently `fn ToolCard(tool: ToolStub) -> Element`) to take
   `crate::server::api::ToolInfo` instead. Field mapping per RESEARCH §"Type mapping":
   - `tool.name` → unchanged
   - `tool.summary` (stub) → `tool.description` (live) — rename binding inside RSX
   - `tool.status` (stub) → DOES NOT EXIST on `ToolInfo`. Remove any `class: if on {"on"}`
     usage on the `.tgl` element; render it with the static class `"tgl"` (off-state
     default) per UI-SPEC §"Tools Screen" / §"ToolCard".
   - `tool.group` (stub) → DOES NOT EXIST. Remove `data-tool-group` attribute entirely;
     do NOT replace with empty string (omit the attribute).
7. Keep the `ENABLE ALL` / `DISABLE ALL` ghost buttons in `.screen-actions` as static
   affordance per UI-SPEC §"Toggle buttons" — they have no `onclick` this phase.
8. The `ToolCard` component MUST derive props with `PartialEq + Clone` automatically via
   `#[component]` (already satisfied because `ToolInfo` derives both).

Critical Dioxus-0.7 hygiene from iron_hermes_ui/CLAUDE.md and PATTERNS.md "Gotchas":
- The `use_server_future` result handle (`tools_resource`) is callable — `tools_resource()`
  returns `Option<Result<Vec<ToolInfo>, ServerFnError>>`. Do NOT hold any signal `.read()`
  borrow across `.await`; this plan has no `.await` inside the component, so the rule is
  trivially satisfied — but the pattern matters for plans 02–05.
- Do NOT introduce any new CSS. UI-SPEC §"Component Inventory" confirms `.tool-card`,
  `.tgl`, `.grid`, `.screen-actions` all already exist.
  </action>
  <verify>
    <automated>cargo check -p iron_hermes_ui --all-features 2>&amp;1 | tail -20</automated>
    <human-check>Run `dx serve` from `crates/iron_hermes_ui/`, navigate to the Tools screen, confirm: (a) cards render with real tool names + descriptions from the runtime registry, (b) hard-killing the server and reloading shows the danger-colored inline error row with exact UI-SPEC copy, (c) the `.tgl` element renders in off state on every card with no console errors.</human-check>
  </verify>
  <done>
- `cargo check -p iron_hermes_ui --all-features` exits 0 with no warnings about unused imports of `stub_data` or `ToolStub` in tools.rs.
- `ScreenTools` contains exactly one `use_server_future(crate::server::api::list_tools)?` call.
- `ToolCard` signature is `fn ToolCard(tool: crate::server::api::ToolInfo) -> Element` (or equivalent local alias).
- No reference to `tool.status`, `tool.group`, `ToolStub`, or `data-tool-group` remains in tools.rs.
- Error copy exactly matches `"Could not load tools — check server connection."` (em dash, single line).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/WASM → server fn | Browser fetches `GET /api/tools`; same-origin via Dioxus fullstack mount in `main.rs` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.7-01-01 | Information Disclosure | `list_tools()` response | accept | Read-only catalog of tool names + descriptions from `runtime_bundle.registry`. The tool catalog is already exposed to every chat turn via the system prompt; surfacing the same list in the UI introduces no new disclosure vector. Server is same-process, single-tenant per Phase 26.7 D-01 scope. |
| T-26.7-01-02 | Denial of Service | `/api/tools` endpoint | accept | Endpoint is read-only, returns a small bounded list (~10s of tools), and is cached by `use_server_future`. No rate-limit added this phase; consistent with existing `/api/tools` behavior already shipped. |
| T-26.7-01-03 | Tampering | Tool toggle (`.tgl`) | mitigate | Toggle is render-only this phase (D-12 / UI-SPEC §"ToolCard"). No `onclick` handler is wired, so no mutation surface is exposed. Explicit out-of-scope guard prevents accidental write-op wiring. |
</threat_model>

<validation>
## Nyquist Dimensions Covered

- **Dim 1 Copy:** Error string verified verbatim against UI-SPEC §"Copywriting Contract" row "Tools — error state".
- **Dim 2 Visuals:** `.tool-card`, `.tgl`, `.grid` all reused per UI-SPEC §"Component Inventory" — no new visuals introduced.
- **Dim 3 Color:** Error row uses `var(--danger)` per UI-SPEC §"Color"; no hardcoded hex.
- **Dim 4 Typography:** Error row uses `var(--fs-12)` per UI-SPEC §"Typography" row "Error inline text".
- **Dim 5 Spacing:** No spacing changes; existing `.grid` gap honored.
- **Dim 6 Registry Safety:** N/A (custom CSS, no shadcn).
- **Dim 7 Loading/Empty:** Loading == empty `.grid` per D-13 (loading state).
- **Dim 8 Error state:** Single inline row, no modal, exact copy match.

## Acceptance Criteria

1. `cargo check -p iron_hermes_ui --all-features` passes.
2. Browser smoke test: Tools screen renders ≥1 live tool card from runtime registry.
3. Server-down smoke test: reload after killing `dx serve` shows danger inline row.
</validation>

<verification>
- `cargo check -p iron_hermes_ui --all-features` — type-check on web+server features.
- `cargo clippy -p iron_hermes_ui --all-features -- -D warnings` — verify no signal-borrow-across-await violations from the clippy.toml ruleset.
- `grep -n "stub_data\|ToolStub" crates/iron_hermes_ui/src/components/hermes_app/screens/tools.rs` — MUST return empty.
- `grep -n "use_server_future(crate::server::api::list_tools)" crates/iron_hermes_ui/src/components/hermes_app/screens/tools.rs` — MUST return exactly 1 match.
</verification>

<success_criteria>
- Tools screen fetches via `api::list_tools` and renders live data.
- Loading state = empty `.grid`. Error state = single inline danger-colored row with verbatim UI-SPEC copy.
- No new types, no AppState changes, no new CSS, no new server fn.
- All verification commands above pass.
- Pattern is proven and reusable for plans 02–05.
</success_criteria>

<out_of_scope>
- ENABLE ALL / DISABLE ALL buttons remain non-functional (no write endpoint this phase).
- Per-tool toggle on `.tgl` element — no `onclick` handler.
- Adding new fields to `ToolInfo` (e.g. status, group, category).
- Any change to `api::list_tools()` server fn — already correct.
- Any change to `stub_data::tools()` or `ToolStub` — these stay as dead code for the other deferred stubbed screens per D-01.
- Any change to `state.rs` / `AppState` — R-1 confirms no extension needed.
</out_of_scope>

<output>
Create `.planning/phases/26.7-wire-up-web-to-real-services/26.7-01-SUMMARY.md` when done.
</output>
