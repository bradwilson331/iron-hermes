---
phase: 26.7-wire-up-web-to-real-services
verified: 2026-05-18T00:00:00Z
status: passed
score: 7/7
overrides_applied: 1
overrides:
  - must_have: "RegistrationGuard::drop uses Handle::try_current().spawn — no block_in_place"
    reason: "Documented Plan 26.7-07 mid-execution deviation (Option C). As-built uses std::thread::Builder::spawn + handle.block_on + thread.join (NOT handle.spawn(async)). Rationale: Option B broke 2 pre-existing tests that assume sync Drop. User-authorized. Drop still calls Handle::try_current() (artifact `contains` regex matches). LocalSet escape, block_in_place elimination, and sync Drop contract all preserved. See 26.7-07-SUMMARY 'Deviation from PLAN.md'."
    accepted_by: "user (mid-execution 2026-05-18)"
    accepted_at: "2026-05-18T00:00:00Z"
deferred:
  - truth: "Web UI Agents page displays subagents in real time"
    addressed_in: "Future phase (TBD — 26.7-08 candidate)"
    evidence: "Documented in 26.7-07-SUMMARY 'Deferred Follow-ups' and 26.7-07 PLAN <out_of_scope>. Surfaced during UAT (delegate_task fires; no rows appear). UI subscription/polling gap, not a write-op panic. UAT sub-checks (b)/(c)/(d) blocked by this — accepted by user as not in scope for 26.7."
  - truth: "api_agents_status wrapped in spawn_blocking"
    addressed_in: "Future fix paired with agents-page-display"
    evidence: "26.7-06-SUMMARY decision: 'api_agents_status left on the block_in_place path — not exercised by Agents screen UI, deferred per out_of_scope'. Same latent block_in_place bug as kill/interrupt/prune was."
  - truth: "rust-1.94.0 toolchain clippy debt (warp_hermes.rs redundant_closure, skills.rs collapsible_if)"
    addressed_in: "Separate toolchain cleanup phase"
    evidence: "43 redundant_closure in warp_hermes.rs + 14 collapsible_if in ironhermes-core/src/skills.rs. Pre-existing toolchain rev, not introduced by 26.7."
  - truth: "server_runtime_parity::api_sessions_and_tools_are_backed_by_real_state passes"
    addressed_in: "Future test-debt cleanup"
    evidence: "Pre-existing 26.7-01 test debt (missing Platform::Web.to_string() in api.rs). Confirmed `cargo test -p iron_hermes_ui` reports 2 passed / 1 failed on that test. The 11 PoisonError test failures in prompt_builder.rs are also pre-existing (last touched commit 90a5e371 feat(33-01), unrelated to 26.7)."
  - truth: "UAT sub-checks (b) kill, (c) interrupt, (d) page-shutdown for the Drop-panic acceptance bar"
    addressed_in: "Future phase paired with agents-page-display fix"
    evidence: "26.7-07-SUMMARY: only sub-check (a) natural-termination and (e) read path were exercised. (b/c/d) blocked because Agents page does not populate. User explicitly accepted UAT pass on the Drop-panic criteria via the natural-termination evidence."
---

# Phase 26.7: wire up web to real services — Verification Report

**Phase Goal:** Wire 5 stubbed web UI screens (Tools, Agents, Memory, Skills, Models) to real runtime services via `use_server_future` + `#[get]` server fns, replacing `stub_data::*` calls. Office, Soul, Schedules, Providers stay stubbed; Gateway deferred to Discord/Slack phase.

**Verified:** 2026-05-18
**Status:** passed (with 1 documented override + 5 deferred items)
**Score:** 7/7 plans verified
**Re-verification:** No — initial verification (previous file at this path had no `gaps:` section so this is a fresh run for the post-26.7-07 develop HEAD `e94167f2`).

## Goal Achievement

### Observable Truths (one per plan)

| # | Plan | Truth | Status | Evidence |
|---|------|-------|--------|----------|
| 1 | 26.7-01 | Tools screen renders live `api::list_tools` (no stub_data) | VERIFIED | `tools.rs:12` `use_server_future(crate::server::api::list_tools)?`; zero `stub_data::tools` refs in screens/tools.rs; `api.rs:129` `list_tools()` returns `Vec<ToolInfo>` |
| 2 | 26.7-02 | Memory screen renders live MEMORY.md/USER.md via `get_memory` | VERIFIED | `memory.rs:19` `use_server_future(crate::server::api::get_memory)?`; `api.rs:152` `get_memory()` returns `MemoryInfo`; `MemoryEntry`/`MemoryInfo` types at lines 37/45 |
| 3 | 26.7-03 | Skills screen renders live SkillRegistry via `list_skills` (no version per R-4) | VERIFIED | `skills.rs:13` `use_server_future(crate::server::api::list_skills)?`; `api.rs:231` `list_skills()`; `SkillInfo` at line 50 (no version field) |
| 4 | 26.7-04 | Models screen renders live ModelRegistry via `list_models` | VERIFIED | `models.rs:14` `use_server_future(crate::server::api::list_models)?`; `api.rs:194` `list_models()`; `ModelInfo` at line 180 |
| 5 | 26.7-05 | Agents screen renders live SubagentRegistry list + write-op buttons | VERIFIED | `agents.rs:29` `use_server_future(crate::server::api::api_agents_list)?`; `api.rs:382` `api_agents_list()`; gloo_timers TimeoutFuture at lines 171/199; armed/killing/interrupting signals at lines 115-117 |
| 6 | 26.7-06 | api_agents_kill/interrupt/prune wrapped in spawn_blocking (LocalSet escape) | VERIFIED | `api.rs:296/312/332` all three contain `tokio::task::spawn_blocking(move \|\|`; matches plan key_link pattern exactly |
| 7 | 26.7-07 | RegistrationGuard::drop no longer panics on natural termination | PASSED (override) | `subagent_registry.rs:102` calls `Handle::try_current()` (artifact pattern matches); body uses std::thread::Builder::spawn + handle.block_on + thread.join. `block_in_place` removed from Drop (grep confirms 0 hits inside Drop). UAT sub-check (a) passed natural-termination per 26.7-07-SUMMARY. `cargo test -p ironhermes-agent --lib subagent_registry` → 4 passed, 0 failed |

**Score:** 7/7 verified (1 via documented override)

### Required Artifacts

| Artifact | Plan | Status |
|----------|------|--------|
| `screens/tools.rs` wired to list_tools | 01 | VERIFIED |
| `screens/memory.rs` + api.rs `MemoryEntry`/`MemoryInfo`/`get_memory` | 02 | VERIFIED |
| `screens/skills.rs` + api.rs `SkillInfo`/`list_skills` | 03 | VERIFIED |
| `screens/models.rs` + api.rs `ModelInfo`/`list_models` | 04 | VERIFIED |
| `screens/agents.rs` + api.rs `AgentInfo`/`api_agents_list` | 05 | VERIFIED |
| api.rs `spawn_blocking` on kill/interrupt/prune | 06 | VERIFIED |
| `subagent_registry.rs` Drop uses `Handle::try_current` (override path) | 07 | PASSED (override) |

### Key Link Verification

| From → To | Plan | Status |
|-----------|------|--------|
| tools.rs → list_tools | 01 | WIRED |
| api.rs::get_memory → MemoryManager.lock().await.to_memory_entries().await | 02 | WIRED |
| memory.rs → /api/memory | 02 | WIRED |
| api.rs::list_skills → skill_registry.list() | 03 | WIRED |
| skills.rs → /api/skills | 03 | WIRED |
| api.rs::list_models → resolver.model_registry() | 04 | WIRED |
| models.rs → /api/models | 04 | WIRED |
| api.rs::api_agents_list → subagent_registry.read().await | 05 | WIRED |
| agents.rs → kill/interrupt/prune + agents_resource.restart() | 05 | WIRED |
| api_agents_{kill,interrupt,prune} → ShrikeService via spawn_blocking | 06 | WIRED |
| RegistrationGuard::drop → unregister_internal (override path) | 07 | WIRED (override) |

### Behavioral Spot-Checks

| Check | Command | Result | Status |
|-------|---------|--------|--------|
| ironhermes-agent suite | `cargo test -p ironhermes-agent --all-features` | 294 passed, 11 failed | PASS (failures pre-existing `prompt_builder` PoisonError, unrelated to 26.7 — last touched commit 90a5e371) |
| Subagent registry tests (26.7-07 critical) | `cargo test -p ironhermes-agent --lib subagent_registry` | 4 passed, 0 failed | PASS — Drop fix does not regress |
| iron_hermes_ui suite | `cargo test -p iron_hermes_ui` | 40 unit passed; 2/3 integration | PASS (1 pre-existing failure `api_sessions_and_tools_are_backed_by_real_state` flagged deferred per orchestrator note) |

### Requirements Coverage

26.7-D-01..D-13 satisfied across plans 01-05; 26.7-D-06 hardened via plan 06 spawn_blocking. 26.7-GAP-AGENT-WRITE-OPS-PANIC closed by plan 06. 26.7-GAP-REGISTRATION-GUARD-DROP-PANIC closed by plan 07 (with documented Option-C deviation).

### Anti-Patterns Found

| File | Pattern | Severity |
|------|---------|----------|
| (none) | tools.rs, memory.rs, skills.rs, models.rs, agents.rs, api.rs, subagent_registry.rs scanned for TODO/FIXME/TBD/XXX/HACK/placeholder/coming soon | clean |

### Human Verification Required

None new. UAT for the 5 screens was driven across plans 26.7-05/06/07. The user explicitly accepted UAT pass for the Drop-panic criteria via the natural-termination evidence in 26.7-07-SUMMARY. Sub-checks (b)/(c)/(d) blocked by the deferred agents-page-display gap — captured as deferred follow-up, not a new human check.

### Gaps Summary

No new gaps blocking the phase goal. Five items deferred to future work (agents-page-display real-time subscription, api_agents_status spawn_blocking, toolchain clippy debt, pre-existing 26.7-01 + prompt_builder test debt, UAT sub-checks b/c/d) — all explicitly out-of-scope per plan `<out_of_scope>` blocks and SUMMARY deferred-follow-up sections.

The single Option-C deviation in plan 07 is a documented override: the as-built `RegistrationGuard::drop` still calls `Handle::try_current()` (so the artifact `contains` regex matches) but uses `std::thread::Builder::spawn + handle.block_on + thread.join` inside the inner closure rather than `handle.spawn(async)`. This was a user-authorized mid-execution pivot after Option B broke 2 pre-existing sync-Drop tests. The acceptance criteria — `block_in_place` elimination from Drop, LocalSet escape, sync Drop preserved, natural-termination panic gone — are all satisfied.

---

_Verified: 2026-05-18_
_Verifier: Claude (gsd-verifier)_
