---
context: phase
phase: 26.7-wire-up-web-to-real-services
plan: 7
task: 2
total_tasks: 2
status: awaiting_human_uat
last_updated: 2026-05-18T10:46:01.175Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> No `blocking`-severity constraints were discovered through failure this session.
> The advisory items below in **Critical Anti-Patterns** are environmental
> gotchas that wasted time but were resolved — they are advisory, not blocking.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| dx-CLI-version-must-match-Cargo.lock | I read `dioxus = "0.7"` in Cargo.toml and incorrectly upgraded dx to 0.7.9. Cargo.lock actually pins dioxus to 0.7.1; dx must match the lockfile, not the caret range in Cargo.toml. Reverting wasted ~10 minutes of background install. | advisory | Before changing dx version, read `Cargo.lock` (`grep -A1 '^name = "dioxus"' Cargo.lock`) — NOT `Cargo.toml`. Then `cargo install dioxus-cli --version <exact-lock-version> --locked --force`. |
| sccache-wraps-rustc-globally | `~/.cargo/config.toml` sets `rustc-wrapper = sccache`. sccache 0.14.0 feeds a C preprocessor test file to rustc which can't parse it, breaking `dx serve` with `Os NotFound`. | advisory | Every `dx serve` (and any cargo invocation prone to the bug) must run with `RUSTC_WRAPPER= dx serve` — env override takes precedence over the config file. Do NOT edit `~/.cargo/config.toml`. |
| orchestrator-cwd-drift (#3174) | After `Agent(isolation="worktree")` returned, the orchestrator's bash CWD had migrated into the worktree path. `git merge worktree-...` from that CWD reported "Already up to date" because the worktree's HEAD already pointed at the merge target. | advisory | Before any post-worktree git operation, run `cd /Users/twilson/code/ironhermes && pwd` to pin back to the primary checkout. The workflow's #3174 guard is documented in execute-phase.md step 5.5. |
| rust-1.94.0-lint-debt (workspace-wide) | `cargo clippy -p iron_hermes_ui -- -D warnings` fails on 43 `redundant_closure` violations in `warp_hermes.rs` + 14 `collapsible_if` in `ironhermes-core/src/skills.rs`. None caused by 26.7-06/07 — surfaced by the toolchain bump. | advisory | When the plan's `<verify><automated>` invokes `-D warnings`, scope to `--no-deps` AND verify error sites are NOT in the plan's edit range before flagging as a real regression. A workspace cleanup pass is owed in a future phase. |

<current_state>
Phase 26.7 Plan 26.7-07 is parked at Task 2 (checkpoint:human-verify, gate=blocking).
Task 1 (`ec5e4a42`) is merged into develop (`6942f073`). The orchestrator is sitting at:

  HEAD: 6942f073 chore(26.7-07): merge executor worktree (RegistrationGuard::drop std::thread fix)
  branch: develop
  worktree: /Users/twilson/code/ironhermes (PRIMARY)

The unrelated worktree `agent-ab017ba987199d4f0` at `/Users/twilson/code/ironhermes/.claude/worktrees/agent-ab017ba987199d4f0` is from a prior session and untouched. The 26.7-07 worktree was cleaned up.

Background process: `cargo test -p ironhermes-agent --all-features` (id b5uv50t97) was running at pause time as the post-merge gate. The executor already proved tests pass in the worktree (305+ tests, 0 failed). If the background run finishes during the pause it will notify on resume.
</current_state>

<completed_work>

Plan 26.7-06 — closed cleanly this session:
- Task 1 (`f871a54f`): wrap `api_agents_kill/interrupt/prune` in `tokio::task::spawn_blocking`
- Merge (`e99c68ae`): worktree → develop
- Task 2 (human UAT): all 6 sub-checks (a)-(f) passed; kill/interrupt/prune all returned 200; zero shrike.rs panics
- Close-out (`447f25ce`): 26.7-06-SUMMARY.md written, UAT Test 5 flipped issue→pass

Plan 26.7-07 — created and partially executed:
- Plan write (`19f30548`): added 26.7-07-PLAN.md + ROADMAP plan list entry + STATE.md focus update
- Task 1 (`ec5e4a42`): APPROVED DEVIATION (Option C) — replaced `block_in_place + block_on` in `RegistrationGuard::drop` with `std::thread::Builder::spawn` + `handle.block_on` + `thread.join()`. Synchronous Drop preserved; all 305+ ironhermes-agent tests pass without modification.
- Merge (`6942f073`): worktree → develop
</completed_work>

<remaining_work>

1. **Task 2 (human-verify UAT)** — 5 sub-checks against `RUSTC_WRAPPER= dx serve`:
   - (a) Natural termination via `Max iterations reached turns=N`: NO panic at `subagent_registry.rs:86`, no `ws turn task join failed`
   - (b) Kill-triggered termination: NO panic; POST `/api/agents/kill` → 200 (26.7-06 regression)
   - (c) Interrupt-triggered termination: NO panic; POST `/api/agents/interrupt` → 200
   - (d) Session shutdown (page refresh / tab close): no panic during ws teardown
   - (e) Read-path regression: GET `/api/agents/list` → 200 throughout

   User reply `approved` (or paste failing sub-check + server log lines) advances.

2. **Continuation agent** (after approval) — writes `.planning/phases/26.7-wire-up-web-to-real-services/26.7-07-SUMMARY.md`. MUST document the Option C deviation prominently (the SUMMARY's "what was built" section needs to call out that the implementation differs from the PLAN.md `<action>` block by user-approved choice).

3. **Tracking update** — `gsd-sdk query roadmap.update-plan-progress "26.7" "26.7-07" "complete"`. This will flip phase 26.7 to 7/7 plans complete; `[ ]` checkbox for 26.7-07 in ROADMAP.md will need to be `[x]`.

4. **Phase verifier** (after all 7 plans have summaries) — run only after the partial-execution gate (`handle_partial_wave_execution`) is satisfied.
</remaining_work>

<decisions_made>

1. **Approved Plan 26.7-06 despite the Drop panic surfacing.** Plan's explicit scope was met (three write-op endpoints work); Drop panic was the named deferred follow-up. Spawning 26.7-07 was cleaner than reopening 26.7-06.

2. **Use merge-then-test-from-main-checkout pattern (same as 26.7-06).** `dx serve` does not run cleanly from a worktree path (dx looks for files that exist in the main checkout but not in fresh worktrees). Merge first; UAT from `/Users/twilson/code/ironhermes`.

3. **Option C deviation (std::thread + handle.block_on) over plan's Handle::try_current().spawn(async).** The async-spawn pattern introduced eventual consistency that broke 2 existing tests (`register_then_drop_guard_leaves_count_zero`, `list_preserves_all_entries`). Option C preserves sync Drop semantics so tests pass without modification. Executor halted at the plan's "do NOT silently fix tests" tripwire and asked — user authorized C.
</decisions_made>

<blockers>
- Plan 26.7-07 Task 2 awaiting human UAT (5 sub-checks). NOT an automated gate — requires human at a browser running `dx serve`.
</blockers>

## Required Reading (in order)

1. `.planning/phases/26.7-wire-up-web-to-real-services/26.7-07-PLAN.md` — the plan being executed. Note the `<action>` block specifies `Handle::try_current().spawn(async)` but Task 1 deviated to Option C — see executor commit `ec5e4a42` for the as-built code.
2. `.planning/phases/26.7-wire-up-web-to-real-services/26.7-06-SUMMARY.md` — context for why 26.7-07 exists (the independent Drop panic).
3. `crates/ironhermes-agent/src/subagent_registry.rs` (lines ~79-104 of the modified Drop body) — the actual implementation.
4. This file's **Critical Anti-Patterns** table — environmental gotchas that bit this session.

## Critical Anti-Patterns (do NOT repeat these)

- dx-CLI version: read `Cargo.lock` not `Cargo.toml`; mismatch → `dx and dioxus versions are incompatible!`.
- sccache: every `dx serve` needs `RUSTC_WRAPPER=` prefix on user side.
- cwd drift: after worktree spawn, `cd /Users/twilson/code/ironhermes &&` before any git operation.

## Infrastructure State

- **Git:** branch `develop` @ `6942f073`. No active worktrees from this run. Unrelated `worktree-agent-ab017ba987199d4f0` predates this session.
- **dx CLI:** 0.7.1 (matches lockfile). DO NOT upgrade.
- **sccache:** 0.14.0, globally enabled via `~/.cargo/config.toml`. Override per-command with `RUSTC_WRAPPER=`.
- **Background process:** `cargo test -p ironhermes-agent --all-features` (`b5uv50t97`) was running at pause time. Output: `/private/tmp/claude-501/-Users-twilson-code-ironhermes/bec7f37a-973d-4cf7-a990-647354f93357/tasks/b5uv50t97.output`. Executor already confirmed it passes in the worktree.
- **Pre-existing toolchain debt** (NOT from this session): rust-1.94.0 introduces lint failures in `warp_hermes.rs` (43) + `ironhermes-core/src/skills.rs` (14); `server_runtime_parity.rs::api_sessions_and_tools_are_backed_by_real_state` fails on pre-existing missing `Platform::Web.to_string()` in api.rs.

<context>
26.7 entered this session with 5/6 plans done. Goal: close the UAT Test 5 blocker (kill/interrupt/prune panics) via Plan 26.7-06 (`--gaps-only`). Plan 06 worked: server endpoints all 200, but the natural-termination path surfaced a DIFFERENT panic at `subagent_registry.rs:86` — exactly the cascade-vs-independent scenario the plan's `<out_of_scope>` block had anticipated. User chose to approve 06 and create 26.7-07 as a fresh gap-closure plan. Plan 26.7-07 was designed around `Handle::try_current().spawn(async)`, but during execution that pattern broke 2 existing tests and the executor halted (correctly) per the plan's "do NOT silently fix tests" instruction. User authorized Option C deviation: `std::thread::spawn + handle.block_on + thread.join()` preserving sync Drop semantics. That edit landed cleanly and all tests pass. Now parked at human UAT.
</context>

<next_action>
On resume, do NOT immediately rerun gsd-execute-phase. The phase state is mid-checkpoint, not fresh-start.

1. Verify HEAD is `6942f073` on `develop` (`git log --oneline -3`).
2. Check background process `b5uv50t97` (cargo test) if it finished while paused — read the output file or look for the task-notification in history.
3. If the user types `approved`, spawn a continuation gsd-executor (NO worktree — fix is already on develop) instructed to:
   - Write `.planning/phases/26.7-wire-up-web-to-real-services/26.7-07-SUMMARY.md` per the plan's `<output>` block; MUST document Option C deviation (real implementation: `std::thread::Builder::spawn` + `handle.block_on` + `thread.join()`, NOT `Handle::try_current().spawn(async)`).
   - Single atomic commit `docs(26.7-07): close plan — Drop panic UAT passed, summary written (Option C deviation documented)`.
   - Do NOT modify STATE.md or ROADMAP.md (orchestrator owns those).
4. After continuation returns: `gsd-sdk query roadmap.update-plan-progress "26.7" "26.7-07" "complete"`; if the ROADMAP `[ ]` checkbox for 26.7-07 didn't auto-flip to `[x]`, edit it. Commit tracking changes.
5. Phase verifier on phase 26.7 (now 7/7 complete, no incomplete plans left). Per workflow §`handle_partial_wave_execution`, this gate is now satisfied.
6. Final aggregation summary.

If the user reports a failed UAT sub-check instead of `approved`: triage the panic/log lines, decide if 26.7-07 needs a Task 3 revision or if a 26.7-08 is warranted. The Drop fix may have edge cases (e.g. `Handle::try_current` returning Err during runtime shutdown leaving an orphaned guard) that are deferred-acceptable but worth noting in SUMMARY.
</next_action>
