---
status: complete
phase: 26.7-wire-up-web-to-real-services
source:
  - 26.7-01-SUMMARY.md
  - 26.7-02-SUMMARY.md
  - 26.7-03-SUMMARY.md
  - 26.7-04-SUMMARY.md
  - 26.7-05-SUMMARY.md
started: 2026-05-17T22:00:00Z
updated: 2026-05-17T22:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Tools Screen Live Data
expected: |
  Navigate to the Tools screen. Tool cards render with real names + descriptions
  from `state.runtime_bundle.registry` (not stub_data placeholders). The .tgl
  element on each card renders in off state. ENABLE ALL / DISABLE ALL buttons
  render but do nothing on click.
result: pass

### 2. Memory Screen Live Data and Client Filter
expected: |
  Navigate to the Memory screen. Left panel (Entries) shows real MEMORY.md text
  blocks. Right-top panel (User Profile) shows USER.md entries (replacing the old
  static "ALPHA-7" profile). Right-bottom Provider dl unchanged. Each row's
  `mem-ts` cell shows an em dash (—). Typing in the filter input narrows visible
  rows in both panels case-insensitively without a page reload. If MemoryManager
  is disabled, both panels show empty row-lists (not an error).
result: pass

### 3. Skills Screen Live Data and Toggle State
expected: |
  Navigate to the Skills screen. Skill cards render with real names, categories
  (bundled / official / installed / etc.), and enabled state. Cards with
  `enabled=true` show the `tgl on` CSS class (visually on). The screen sub-copy
  shows correct live counts: `{count} loaded · {enabled_count} enabled for default.`
  The ALL tab shows live count `ALL · {count}`. Other tabs show static placeholder
  counts. Clicking .tgl does nothing (no handler this phase).
result: pass

### 4. Models Screen Family Grouping and Default Marker
expected: |
  Navigate to the Models screen. Models are grouped by inferred family (one
  `.model-family-group` per family) with section labels like
  "Anthropic (Claude) · 4 configs". The configured default model (from
  `config.model.default`) shows the `DEFAULT` pill in teal; others show
  `AVAILABLE`. Context windows display as human-readable strings (e.g. "200k",
  "1M"). EDIT button per card renders but does nothing on click.
result: pass

### 5. Agents Screen KILL?/INTERRUPT/PRUNE Interactions
expected: |
  Navigate to the Agents screen with at least one active subagent running.
  (a) Click KILL on an agent card once — label changes to "KILL?".
  (b) Wait 3+ seconds without clicking again — label reverts to "KILL"
      (no kill fires).
  (c) Click KILL twice within 3 seconds — agent is killed and the list refreshes
      (row disappears).
  (d) Click INTERRUPT — button shows "..." for ~500ms then reverts; agent
      receives an interrupt signal.
  (e) Arming one card's KILL does not affect other cards' state.
  (f) Click PRUNE ENDED with ended agents present — they disappear from the list.
  (g) Kill server and reload — danger inline row appears in the grid with copy
      "Could not load agents — check server connection."
result: issue
reported: |
  "click on prune ended did not end the agents"
  Server logs show panics on all three write endpoints — kill, interrupt, prune
  all return HTTP 500. Panic message: "can call blocking only when running on the
  multi-threaded runtime".
  - shrike.rs:123 — /api/agents/kill panic
  - shrike.rs:175 — /api/agents/interrupt panic
  - shrike.rs:214 — /api/agents/prune panic
  - subagent_registry.rs:86 — agent runtime panic from main turn task
  /api/agents/list returns 200 (read path works), so failure is isolated to the
  three ShrikeService write ops.
severity: blocker

## Summary

total: 5
passed: 4
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Agent screen write operations (kill, interrupt, prune) succeed and refresh the live list"
  status: failed
  reason: |
    User reported: "click on prune ended did not end the agents".
    Server logs show panics on all three write endpoints with message
    "can call blocking only when running on the multi-threaded runtime":
      - crates/ironhermes-agent/src/shrike.rs:123 (kill) → HTTP 500
      - crates/ironhermes-agent/src/shrike.rs:175 (interrupt) → HTTP 500
      - crates/ironhermes-agent/src/shrike.rs:214 (prune) → HTTP 500
    Related panic in crates/ironhermes-agent/src/subagent_registry.rs:86
    on main turn task. /api/agents/list returns 200 (read works); failure
    isolated to ShrikeService write paths.
  severity: blocker
  test: 5
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""
