---
phase: 26.7.1
slug: agents-page-live-updates
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-18
---

# Phase 26.7.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `26.7.1-RESEARCH.md` § Validation Architecture.
> Per-task IDs are placeholders — the planner assigns concrete IDs.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` workspace-wide + `cargo test -p iron_hermes_ui --features web` for the UI crate specifically |
| **Config file** | None (cargo defaults). Project uses `cargo clippy` with custom `crates/iron_hermes_ui/clippy.toml` (signal-borrow rules — MUST pass on every commit) |
| **Quick run command** | `cargo test -p iron_hermes_ui --features web` |
| **Full suite command** | `cargo test --workspace --all-features` |
| **Lint gate** | `cargo clippy -p iron_hermes_ui --features web -- -D warnings` |
| **Estimated runtime** | ~45s quick / ~12 min full |
| **WASM-specific tests** | `wasm-bindgen-test` NOT configured in this crate. Component behavior verified via `checkpoint:human-verify` UAT against `dx serve`. |

---

## Sampling Rate

- **After every task commit:** `cargo test -p iron_hermes_ui --features web` + `cargo clippy -p iron_hermes_ui --features web -- -D warnings`
- **After every plan wave:** `cargo test --workspace --all-features`
- **Before `/gsd-verify-phase`:** Full suite + clippy `-D warnings` workspace-wide must be green
- **Max feedback latency:** ~45 seconds (quick) / ~12 min (full)
- **Phase gate:** `checkpoint:human-verify` UAT — see Manual-Only Verifications below

---

## Per-Task Verification Map

> Per-task IDs (`26.7.1-XX-YY`) are placeholders. Planner assigns concrete IDs in `26.7.1-XX-PLAN.md` files. Each task row maps a phase requirement (D-XX from CONTEXT.md) to its verification path.

| Task ID (placeholder) | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---|---|---|---|---|---|---|---|---|---|
| 26.7.1-01-context-providers | 01 | 1 | D-07 / D-08 (setup-only) | — | N/A | static-grep | `grep -c 'subagent_events' crates/iron_hermes_ui/src/components/warp_hermes.rs` ≥ 1; `grep -c 'is_ws_connected' crates/iron_hermes_ui/src/components/warp_hermes.rs` ≥ 1 | ✅ existing files | ⬜ pending |
| 26.7.1-01-recently-terminated | 01 | 1 | D-02 / D-11 / D-13 | — | sync-Drop preserves diff invariant | unit + static-grep | (a) `grep -c 'recently_terminated' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` ≥ 2; (b) extract `fn diff_terminations(old, new) -> ...` as pure fn, unit-test re-running-id wins | ❌ Wave 0 (pure-fn extraction) | ⬜ pending |
| 26.7.1-01-poll-loop | 01 | 1 | D-03 / D-04 / D-05 | — | poll respects visibility + RPC guards | manual-UAT (devtools Network) + static-grep | (a) `grep -c 'TimeoutFuture::new(1500)' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` ≥ 1; (b) `grep -c 'visibilitychange' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` ≥ 1; (c) UAT switches tabs and confirms poll halt | ✅ | ⬜ pending |
| 26.7.1-01-css-modifiers | 01 | 1 | D-09 / D-10 / D-14 | — | N/A | static-grep | (a) `grep -c '\.card\.is-ended' crates/iron_hermes_ui/assets/screens.css` ≥ 1; (b) `grep -c '\.pill\.ended' crates/iron_hermes_ui/assets/screens.css` ≥ 1; (c) `grep -c 'opacity: 0.55' crates/iron_hermes_ui/assets/screens.css` ≥ 1 | ✅ | ⬜ pending |
| 26.7.1-01-prune-extension | 01 | 1 | D-12 | — | local + server cleared atomically | static-grep + UAT | `grep -c 'recently_terminated.write().clear()' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` ≥ 1; UAT confirms ENDED cards gone after PRUNE click | ✅ | ⬜ pending |
| 26.7.1-01-uat | 01 | 1 | D-01 / D-02 / D-04 / D-12 | — | full HOLD-N user flow | manual-UAT | n/a — see Manual-Only Verifications | n/a | ⬜ pending |
| 26.7.1-02-event-variant | 02 | 1 | D-07 | T-26.7.1-V5 (V5 Input Validation) | serde-enum-exhaustive | unit | `cargo test -p iron_hermes_ui test_subagent_event_json_shape` — `serde_json::to_string(&ChatStreamEvent::SubagentEvent {})` produces stable counter-only payload | ❌ Wave 0 | ⬜ pending |
| 26.7.1-02-callback-plumb | 02 | 1 | D-06 | — | per-turn callback lifetime safe | unit (server-feature gated) | `cargo test -p iron_hermes_ui --features server test_subagent_callback_emits_event` — mock `mpsc::unbounded_channel`, build callback, invoke with synthetic `SubagentProgress::Started`, assert receiver sees `ChatStreamEvent::SubagentEvent {}` | ❌ Wave 0 | ⬜ pending |
| 26.7.1-02-client-wire | 02 | 1 | D-07 / D-08 | — | counter increments on event; is_ws_connected drives cadence | manual-UAT + static-grep | (a) `grep -c 'subagent_events.set' crates/iron_hermes_ui/src/components/warp_hermes.rs` ≥ 1; (b) `grep -c 'is_ws_connected.set' crates/iron_hermes_ui/src/components/warp_hermes.rs` ≥ 1; (c) UAT confirms <1s update latency on delegate_task | ✅ existing file | ⬜ pending |
| 26.7.1-02-dynamic-cadence | 02 | 1 | D-08 | — | poll widens to 5s on connect, tightens to 1500ms on disconnect | manual-UAT (DevTools Network + close-ws scenario) | n/a — visual verification | n/a | ⬜ pending |
| 26.7.1-02-uat | 02 | 1 | D-01 / D-02 / D-06 / D-07 / D-08 / 26.7-06 regression | — | full live-update + ws-disconnect fallback flow | manual-UAT | n/a — see Manual-Only Verifications | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Tests that don't exist yet and must be authored before the corresponding tasks can verify.

- [ ] **`test_subagent_event_json_shape`** (covers D-07) — `crates/iron_hermes_ui/src/server/ws.rs` (or sibling test module). 5-LoC unit test asserting `serde_json::to_string(&ChatStreamEvent::SubagentEvent {})` produces the documented counter-only JSON shape.
- [ ] **`test_subagent_callback_emits_event`** (covers D-06) — `crates/iron_hermes_ui/src/server/ws.rs` (server-feature gated). Mock an `mpsc::unbounded_channel`, build the `SubagentProgressCallback` per the Plan 02 pattern, invoke with a synthetic `SubagentProgress::Started`, assert the receiver sees `ChatStreamEvent::SubagentEvent {}` on its end.
- [ ] **Pure-fn extraction of the diff logic** (enables D-13 unit test) — extract `fn diff_terminations(old: &HashMap<String, AgentInfo>, new: &[AgentInfo], now: Instant) -> Vec<(String, AgentInfo, Instant)>` as a top-level pure function in `crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs`. Unit-test re-running-id behavior (D-13).
- [ ] **No WASM browser test framework is configured.** Component-level behavior (visibilitychange listener, opacity transition, button-hide) is verified by UAT only. Configuring `wasm-bindgen-test` is deferred to a separate workspace-tooling phase.

---

## Manual-Only Verifications

> The `checkpoint:human-verify` UAT for each plan exercises these scenarios against `cd crates/iron_hermes_ui && RUSTC_WRAPPER= dx serve`.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| HOLD-N = 5s timing | D-01 | Visual timer requires real browser | Send `delegate_task`; wait for natural termination; click stopwatch; confirm card disappears ~5s after `recently_terminated` capture |
| All terminations show HOLD (kill + interrupt + natural) | D-02 | Three trigger paths exercised in real UI | (a) Natural: `Max iterations` prompt → card moves to ENDED state, fades after 5s. (b) Kill: click KILL, then KILL? within 3s → card moves to ENDED state, fades after 5s. (c) Interrupt-finalize: click INTERRUPT, let agent finalize → card moves to ENDED state, fades after 5s. |
| Poll cadence baseline = 1500ms | D-03 | Network-tab observation | DevTools → Network → filter `/api/agents/list` → measure interval between fetches (~1500ms ±100ms with no ws traffic) |
| Poll pauses on tab hidden | D-04 | Tab focus is a real browser state | Switch to another tab; wait 10s; switch back; confirm Network shows no `/api/agents/list` fetches during hidden period |
| Poll pauses during kill/interrupt RPCs | D-05 | Race window observation | Click KILL? armed → second click; observe Network to confirm no `/api/agents/list` fetch between kill POST and its 200 response |
| Live-spawn visibility <1s post-Plan-02 | D-06 / D-07 | Real ws round-trip timing | Send `delegate_task`; expected card appearance ≤1s after the chat stream shows `delegate_task` tool call |
| Dynamic cadence on ws state | D-08 | DevTools Network + ws lifecycle | While ws connected, confirm `/api/agents/list` polls at ~5s; close the ws (chat reconnect button or DevTools); confirm poll tightens to ~1500ms within 1 cycle |
| PRUNE clears both server + client | D-12 | Visual confirmation | While ≥1 agent in ENDED state, click PRUNE ENDED; expect both that card AND any server-side stale agents to disappear immediately, not after 5s |
| Re-running id wins | D-13 | Edge-case spawn timing | Spawn a subagent, let it terminate (HOLD state), then spawn ANOTHER subagent with the same id (artificial — may not be reproducible without server-side test hook); expect ENDED card to be replaced by RUNNING card, no animation |
| Kill/interrupt regression (26.7-06) | 26.7-06 carryforward | UI smoke vs 26.7-06's `spawn_blocking` wraps | All four `/api/agents/{kill, interrupt, prune, list}` endpoints return 200 throughout the UAT |
| No `subagent_registry.rs:86` panic (26.7-07) | 26.7-07 carryforward | Watch server stderr | Server log must not contain "panicked at crates/ironhermes-agent/src/subagent_registry.rs:86" during any UAT scenario |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify OR Wave 0 dependency OR manual-UAT justification
- [x] Sampling continuity: no 3 consecutive tasks without automated verify or Wave 0 — every UAT-only task has a sibling static-grep or unit task in the same wave
- [x] Wave 0 covers all MISSING references (`test_subagent_event_json_shape`, `test_subagent_callback_emits_event`, `fn diff_terminations` pure-fn extraction)
- [x] No watch-mode flags
- [x] Feedback latency < 45s for the quick gate
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-18 (orchestrator)
