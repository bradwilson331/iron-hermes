---
phase: 26.7.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs
  - crates/iron_hermes_ui/assets/screens.css
  - crates/iron_hermes_ui/src/components/warp_hermes.rs
  - crates/iron_hermes_ui/Cargo.toml
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
  - D-07-slot
  - D-08-slot
user_setup: []

must_haves:
  truths:
    - "Agents page polls /api/agents/list at 1500 ms baseline and the page reflects live registry state within 1.5 s without user action"
    - "When an agent transitions out of the live list, a HOLD card with .card.is-ended + .pill.ended renders for ~5 s with the frozen uptime, then disappears"
    - "Tab-hidden state halts polling; visibility-return triggers one immediate fetch and resumes the interval"
    - "Per-card kill/interrupt RPCs in flight pause polling until both signals are false again"
    - "PRUNE click clears both server-side ended agents and the client recently_terminated map synchronously"
    - "An agent id that reappears in the live list after termination is removed from recently_terminated and rendered live (no double card)"
    - "WarpHermes provides subagent_events: Signal<u64> and is_ws_connected: Signal<bool> via use_context_provider with initial values 0 and false (consumed by Plan 02)"
  artifacts:
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs"
      provides: "Periodic poll loop, recently_terminated signal, decay sweep, Page Visibility listener, busy-set context provider, pure-fn diff_terminations, HOLD card render path"
      contains: "POLL_INTERVAL_BASELINE_MS"
    - path: "crates/iron_hermes_ui/assets/screens.css"
      provides: ".card.is-ended and .pill.ended modifier rules"
      contains: ".card.is-ended"
    - path: "crates/iron_hermes_ui/src/components/warp_hermes.rs"
      provides: "use_context_provider slots for subagent_events: Signal<u64> and is_ws_connected: Signal<bool>"
      contains: "is_ws_connected"
    - path: "crates/iron_hermes_ui/Cargo.toml"
      provides: "web-sys Event feature flag enabled for the visibilitychange Closure type"
      contains: "\"Event\""
  key_links:
    - from: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs"
      to: "crates/iron_hermes_ui/src/server/api.rs::api_agents_list"
      via: "agents_resource.restart() inside the use_future poll loop"
      pattern: "agents_resource\\.restart\\("
    - from: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs"
      to: "crates/iron_hermes_ui/assets/screens.css"
      via: "AgentCard renders class: if is_ended { \"is-ended\" } / class: if is_ended { \"pill ended\" }"
      pattern: "is-ended"
    - from: "crates/iron_hermes_ui/src/components/warp_hermes.rs"
      to: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs"
      via: "use_context_provider(|| Signal::new(0u64)) and use_context_provider(|| Signal::new(false)); consumed via use_context::<Signal<u64>>() and use_context::<Signal<bool>>() in ScreenAgents"
      pattern: "use_context_provider"
---

<objective>
Land the periodic-poll baseline plus the TERMINATED-HOLD-N=5s fade for the web UI Agents screen. Implement client-side diff of disappeared agents into `recently_terminated: Signal<HashMap<String, (AgentInfo, Instant)>>`, render frozen-snapshot HOLD cards with `.card.is-ended` + `.pill.ended` styling for 5 s, then drop. Pause polling under tab-hidden + per-card kill/interrupt. Provide (do not yet increment) the `subagent_events: Signal<u64>` and `is_ws_connected: Signal<bool>` context slots so Plan 02 ships as a minimal increment-and-watch edit.

Purpose: Close the user-visible "Agents page never shows subagents" gap reported in 26.7-07 UAT for any agent that lives >1.5 s, and establish the context plumbing Plan 02 layers onto.
Output: Updated `agents.rs`, `screens.css`, `warp_hermes.rs`, `Cargo.toml` (web-sys feature), plus pure-fn `diff_terminations` with a unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.7.1-agents-page-live-updates/26.7.1-CONTEXT.md
@.planning/phases/26.7.1-agents-page-live-updates/26.7.1-RESEARCH.md
@.planning/phases/26.7.1-agents-page-live-updates/26.7.1-PATTERNS.md
@.planning/phases/26.7.1-agents-page-live-updates/26.7.1-UI-SPEC.md
@.planning/phases/26.7.1-agents-page-live-updates/26.7.1-VALIDATION.md
@crates/iron_hermes_ui/CLAUDE.md
@crates/iron_hermes_ui/clippy.toml
@crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs
@crates/iron_hermes_ui/src/components/warp_hermes.rs
@crates/iron_hermes_ui/src/components/hermes_app/wheel.rs
@crates/iron_hermes_ui/src/server/api.rs
@crates/iron_hermes_ui/assets/screens.css
@crates/iron_hermes_ui/assets/site.css
@crates/iron_hermes_ui/Cargo.toml

<interfaces>
<!-- Key types and idioms the executor needs. Extracted directly from the codebase. -->

`crates/iron_hermes_ui/src/server/api.rs` defines `pub struct AgentInfo { id: String, status: String, uptime_secs: u64, parent_id: Option<String>, ... }`. The exact shape is consumed unchanged — read the file for the full set of fields. AgentInfo derives Clone + PartialEq (required for Dioxus Props on AgentCard).

`crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` exposes `ScreenAgents` and `AgentCard`. `AgentCard` already has Props { agent: AgentInfo, agents_resource: Resource<...> }. This plan ADDS a `is_ended: bool` prop (Plan 02 does not touch it).

`crates/iron_hermes_ui/clippy.toml` blocks holding `GenerationalRef`, `GenerationalRefMut`, `dioxus_signals::WriteLock` across `.await`. Plan-01 use_future bodies MUST drop reads before awaits — use call-syntax `signal()` for Copy types and explicit `{ let copy = sig.read().clone(); }` blocks for HashMap.

`crates/iron_hermes_ui/src/components/hermes_app/wheel.rs:184-280` is the verbatim template for the visibilitychange listener — `Closure::<dyn FnMut(web_sys::Event)>::new(...)`, store in `Signal<Option<Closure<...>>>`, install in `use_effect`, remove in `use_drop`. Do NOT call `cb.forget()`.

`crates/iron_hermes_ui/src/components/warp_hermes.rs:135` declares the chat WebSocket via `dioxus_fullstack::use_websocket(...)`. Plan 01 ADDS `use_context_provider(|| Signal::new(0u64))` and `use_context_provider(|| Signal::new(false))` near this declaration — initial values fixed per RESEARCH Pitfall 3.

`crates/iron_hermes_ui/assets/screens.css` already contains `.card.is-active` (~line 118) and the `.pill.green/.amber/.red` triplet (~line 211). Use those as the placement anchors for the new modifier rules.

`crates/iron_hermes_ui/assets/site.css:30-31` defines `--gray: #6e7681` and `--gray-faint: rgba(110, 118, 129, 0.45)`. These are the locked UI-SPEC token sources for `.pill.ended`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 unit test + pure-fn diff_terminations + Cargo.toml web-sys Event feature</name>
  <files>crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs, crates/iron_hermes_ui/Cargo.toml</files>

  <read_first>
    - crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs (entire file — current ScreenAgents + AgentCard shape and current imports)
    - crates/iron_hermes_ui/src/server/api.rs (AgentInfo struct exact field list, derives)
    - crates/iron_hermes_ui/Cargo.toml (current web-sys feature list lines 19-32)
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-RESEARCH.md (Pitfall 7 web-sys Event; Pattern 5 diff; Open Question 1 std::time::Instant on wasm32)
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-VALIDATION.md (Wave 0 Requirements section — pure-fn extraction for D-13)
  </read_first>

  <behavior>
    - Pure fn `pub fn diff_terminations(prev: &HashMap<String, AgentInfo>, current: &[AgentInfo], now: Instant) -> (Vec<(String, AgentInfo, Instant)>, Vec<String>)` returns (new_terminations_to_insert, ids_to_remove_because_replayed).
    - Test 1 (D-02 + D-11): given prev = {"a": AgentInfo{id:"a", uptime_secs:12, ...}}, current = [], returns insertions = [("a", AgentInfo{uptime_secs:12,...}, now)], removals = []. Asserts uptime_secs is the prev value (frozen, NOT current's).
    - Test 2 (D-13 re-running id wins): given prev = {"a": old_info_with_terminated_state}, current = [AgentInfo{id:"a", status:"running",...}], returns insertions = [], removals = ["a"]. The id present in current must NOT also appear in insertions.
    - Test 3 (no change): given prev = {"a": ...}, current = [AgentInfo{id:"a",...}, AgentInfo{id:"b",...}], returns insertions = [] (no disappearance), removals = ["a"] (any current id removes from prev unconditionally per D-13 simpler invariant) — OR the fn can split the contract so removals only fires when prev contained the id; the implementation MUST pick one and the tests MUST match. Recommended contract: removals = current_ids ∩ prev_keys (only ids that were in prev AND are in current).
    - Test 4 (mixed): prev has "a","b"; current has only "a"; returns insertions = [("b", b_info, now)], removals = ["a"].
    - Tests live in a `#[cfg(test)] mod tests` block at the bottom of agents.rs and run under `cargo test -p iron_hermes_ui --features web`.
  </behavior>

  <action>
    Open `crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs`. Add imports `use std::collections::HashMap; use std::time::Instant;` at the top of the file if not already present. Add `pub fn diff_terminations(...)` as a free function at module scope (NOT inside a component). Signature exactly as specified in behavior — return type `(Vec<(String, crate::server::api::AgentInfo, Instant)>, Vec<String>)`. Contract: for each key in `prev` not present in `current`, push `(id.clone(), info.clone(), now)` into insertions. For each `id` in `current` whose `id` is also a key in `prev`, push `id.clone()` into removals (D-13 re-running id wins).

    Add a `#[cfg(test)] mod tests { ... }` block at the file bottom with the four tests above. Each test constructs `AgentInfo` with the project's existing field set (read `api.rs` for the exact set — do NOT invent fields). Use `Instant::now()` for `now`. Tests assert exact insertion counts, exact removal counts, and that the inserted `AgentInfo.uptime_secs` equals the `prev` value not a re-fetched value.

    Open `crates/iron_hermes_ui/Cargo.toml`. Locate the `web-sys` dependency block (RESEARCH cites lines 19-32). Add `"Event"` to the features list if not already present (RESEARCH Pitfall 7 explicitly notes Event is needed for `Closure::<dyn FnMut(web_sys::Event)>::new(...)` in Task 2's Page Visibility listener). Do NOT add any other features. Do NOT touch any other dependency.

    Per Open Question 1 (RESEARCH Risk #3): the pure fn uses `std::time::Instant` first. If a later task discovers a wasm32 panic at runtime, add `web-time = "1.1"` workspace dep and substitute `web_time::Instant` — but DO NOT pre-emptively add it.

    NEVER place fenced code blocks inside this action. Read `read_first` files for current state.
  </action>

  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p iron_hermes_ui --features web diff_terminations 2>&amp;1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `cargo test -p iron_hermes_ui --features web diff_terminations` exits 0 with all four test cases passing.
    - `grep -c 'pub fn diff_terminations' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` returns ≥ 1.
    - `grep -c '"Event"' crates/iron_hermes_ui/Cargo.toml` returns ≥ 1 (in the web-sys features list).
    - `cargo build -p iron_hermes_ui --target wasm32-unknown-unknown --features web 2>&1 | tail -20` exits 0 (verifies the web-sys feature flag is wired and the wasm target still compiles).
    - `cargo clippy -p iron_hermes_ui --features web -- -D warnings 2>&1 | tail -10` exits 0 for new code (pre-existing 26.7 warp_hermes.rs lints are out of scope per RESEARCH Project Constraints).
    - `diff_terminations` is invoked only from tests so far; the production call-site lands in Task 2.
    - No fenced code blocks were added inside the planner action text.
  </acceptance_criteria>

  <done>
    Pure-fn diff helper compiles, four unit tests pass, web-sys Event feature flag enabled, wasm32 build still green. Production wiring deferred to Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Plan-01 client wiring — poll loop, recently_terminated, decay sweep, Page Visibility, busy-set, PRUNE extension, HOLD render, CSS modifiers, WarpHermes context slots</name>
  <files>crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs, crates/iron_hermes_ui/assets/screens.css, crates/iron_hermes_ui/src/components/warp_hermes.rs</files>

  <read_first>
    - crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs (current full file — needs poll + diff + decay + PV listener + busy-set integration)
    - crates/iron_hermes_ui/src/components/hermes_app/wheel.rs lines 180-280 (RESEARCH Pattern 1 — verbatim visibilitychange template with use_drop cleanup)
    - crates/iron_hermes_ui/src/components/warp_hermes.rs lines 100-180 and 400-560 (ws declaration + ShellSettings use_context_provider for the placement anchor; use_drop pattern at 541-551)
    - crates/iron_hermes_ui/assets/screens.css lines 100-220 (.card and .card.is-active around line 118; .pill.green/.amber/.red triplet at lines 209-211; .card-footer at lines 162-169)
    - crates/iron_hermes_ui/assets/site.css lines 17-34 (--gray, --gray-faint, --teal-dim tokens; UI-SPEC locks 0.55 opacity)
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-CONTEXT.md (D-01..D-14, especially D-05 busy-set, D-12 PRUNE, D-13 replay)
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-RESEARCH.md (Patterns 1-5, Pitfalls 1/3/5/6/7, Common Op 1+3+4, Sequencing Note items 1-3)
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-UI-SPEC.md (locked opacity = 0.55; locked .pill.ended token sources)
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-PATTERNS.md (8-item Specific Pattern Catalog — every analog file:line)
  </read_first>

  <behavior>
    - When `ScreenAgents` is mounted with no ws traffic: a `/api/agents/list` fetch fires every ~1500 ms ±150 ms (D-03), observable in DevTools Network.
    - When `document.hidden` becomes true, no `/api/agents/list` fetches occur until visibility returns (D-04). On visibility-return, exactly one immediate fetch fires and the interval resumes.
    - When any AgentCard's `killing()` or `interrupting()` per-card signal is true (already in agents.rs), the screen-level busy set contains that id and poll loop skips the fetch step (D-05). On both becoming false, polling resumes on next iteration. AgentCard registers/unregisters its id via `use_drop` and on signal transitions.
    - When an `AgentInfo.id` is in the prev list and not in the new list, an entry `(id, AgentInfo_snapshot, Instant::now())` lands in `recently_terminated` (D-02). The snapshot's `uptime_secs` is the prev value (D-11), NOT a re-fetched value.
    - Recently-terminated entries are rendered as HOLD cards with `class: "card is-ended"` and pill `class: "pill ended"` text `"ENDED"` (D-09, D-10, D-14). The `.card-footer` is hidden by CSS (`display: none`).
    - A decay sweep `use_future` runs every 1 s and removes entries whose `Instant::elapsed() >= Duration::from_secs(5)` (D-01). The HashMap shrinks; the card disappears from the render tree.
    - If an id appears in `current` AND is also a key in `recently_terminated`, the entry is removed immediately (D-13 — implemented via `diff_terminations` from Task 1's pure fn).
    - Clicking PRUNE ENDED clears `recently_terminated` synchronously (write().clear()) BEFORE awaiting `api_agents_prune(None).await` (D-12).
    - `WarpHermes` provides `subagent_events: Signal<u64>` (initial 0) and `is_ws_connected: Signal<bool>` (initial false per RESEARCH Pitfall 3) via `use_context_provider`. Both are present BUT unincremented in Plan 01 — Plan 02 wires the increment sites.
    - No signal `.read()` GenerationalRef is held across any `.await` (Pitfall 1 + clippy.toml).
    - `cb.forget()` is NEVER called on the visibilitychange Closure (Pitfall 7/anti-pattern; use_drop removes the listener instead).
  </behavior>

  <action>
    Step A — `crates/iron_hermes_ui/src/components/warp_hermes.rs` context slots (Plan 02 prep per RESEARCH Sequencing Note items 1+2):
    Locate the existing `dioxus_fullstack::use_websocket(...)` declaration around line 135 and the existing `use_context_provider(|| ShellSettings { ... })` around line 433. Immediately AFTER the ws declaration (so the slot is mounted before children read it), insert two new context providers:
    - `let mut subagent_events: Signal&lt;u64&gt; = use_context_provider(|| Signal::new(0u64));`
    - `let mut is_ws_connected: Signal&lt;bool&gt; = use_context_provider(|| Signal::new(false));`
    Plan 01 does NOT touch the ws receiver loop. The bindings may be flagged `#[allow(unused_mut)]` IF rustc warns — preferred path: do not warn (Plan 02 will write through these signals). If unused-warning fires, add `#[allow(unused_variables)]` on each binding with a `// Phase 26.7.1: Plan 02 increments through this slot` comment. Do NOT add `set(true)` here — Pitfall 3 locks initial values.

    Step B — `crates/iron_hermes_ui/assets/screens.css` modifier rules (D-09, D-10, D-14 — UI-SPEC LOCKED, do NOT tune):
    Append the `.card.is-ended` rule immediately AFTER the existing `.card.is-active` block (around line 122). Rule body: `opacity: 0.55; transition: opacity 200ms ease-out; pointer-events: none;`. Append a sibling rule `.card.is-ended .card-footer { display: none; }` so all three buttons (CHAT, INTERRUPT, KILL) are hidden by CSS without an RSX conditional. Append the `.pill.ended` rule immediately AFTER `.pill.red` (around line 211) with `color: var(--gray); border-color: var(--gray-faint); background: rgba(110, 118, 129, 0.06);`. Token sources locked by UI-SPEC §"New CSS Classes". Do NOT introduce any new CSS custom property.

    Step C — `crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` core wiring:

    1. Add module-top constants (RESEARCH Sequencing Note item 4): `const POLL_INTERVAL_BASELINE_MS: u64 = 1500;` and `const HOLD_DURATION: std::time::Duration = std::time::Duration::from_secs(5);`. Plan 02 will sibling-add `POLL_INTERVAL_WS_CONNECTED_MS: u64 = 5000;` — do NOT add it now.

    2. In `ScreenAgents`, declare new signals (read-discipline: all Copy reads via call-syntax):
       - `let mut recently_terminated: Signal<HashMap<String, (crate::server::api::AgentInfo, std::time::Instant)>> = use_signal(HashMap::new);`
       - `let mut prev_agents: Signal<Vec<crate::server::api::AgentInfo>> = use_signal(Vec::new);`
       - `let mut tab_hidden: Signal<bool> = use_signal(|| false);`
       - `let busy_set: Signal<std::collections::HashSet<String>> = use_context_provider(|| Signal::new(std::collections::HashSet::<String>::new()));` (D-05 — busy-set provided to AgentCard children; if RESEARCH Open Q 3 path-of-aggregation differs, planner notes choice in SUMMARY).
       - Consume Plan 02 slots WITHOUT writing: `let is_ws_connected = use_context::<Signal<bool>>();` (Copy bool reads via call-syntax). Plan 01 USES this to decide cadence (currently always false → 1500 ms always; Plan 02 makes it variable).

    3. Insert the diff step BEFORE the rsx! block — after `agents_resource()` resolves and the `agents_list: Vec<AgentInfo>` is materialized (existing pattern at agents.rs:33-37). Call the pure fn from Task 1:
       - `let now = std::time::Instant::now();`
       - `let prev_snapshot: HashMap<String, crate::server::api::AgentInfo> = { let map = recently_terminated.read(); map.iter().map(|(k,(info,_ts))| (k.clone(), info.clone())).collect() };` (read guard dropped at end of block).
       - Plus the prev_agents-vs-current comparison: collect prev IDs as keys, then call `diff_terminations(&prev_keyed_from_prev_agents, &agents_list, now)`. The pure fn signature treats `prev` as a `HashMap<String, AgentInfo>` — build it once from `prev_agents.read().iter()` cloned, drop the read.
       - Apply insertions: `for (id, info, ts) in insertions { let mut map = recently_terminated.write(); map.entry(id).or_insert((info, ts)); }`.
       - Apply removals (D-13): `for id in removals { recently_terminated.write().remove(&id); }`.
       - Update `prev_agents.set(agents_list.clone());` at the end.

    4. Add a `use_future` POLL loop (RESEARCH Pattern 2; PATTERNS.md "Core poll loop pattern") that loops forever:
       - Reads `let hidden = tab_hidden();` (Copy bool — no borrow held)
       - Reads `let busy = !busy_set.read().is_empty();` then drops via block scope `{ let r = busy_set.read(); !r.is_empty() }`
       - Reads `let connected = is_ws_connected();`
       - Computes `let interval_ms: u64 = POLL_INTERVAL_BASELINE_MS;` (Plan 01 — Plan 02 will widen this when connected is true)
       - If `!hidden && !busy { agents_resource.restart(); }` — only when both clear (D-04 + D-05)
       - `gloo_timers::future::TimeoutFuture::new(interval_ms as u32).await;` — no signal borrow held across this point (Pitfall 1 / clippy.toml)

    5. Add a Page Visibility listener (D-04) using the wheel.rs:184-280 template VERBATIM (web_sys::Event closure; `Signal<Option<Closure<dyn FnMut(web_sys::Event)>>>` storage; install in `use_effect`; `add_event_listener_with_callback("visibilitychange", ...)` on `document`; mirror `document.hidden()` into `tab_hidden`; cleanup in `use_drop` with `remove_event_listener_with_callback` + drop the closure). On visibility-return (i.e., the listener fires with `!doc.hidden()`), call `agents_resource.restart()` from inside the listener (Pitfall 5) so there's no up-to-1500 ms wait. NEVER call `cb.forget()`.

    6. Add a decay-sweep `use_future` (RESEARCH Common Op 1; PATTERNS.md "Decay sweep pattern") that loops every 1000 ms: collect expired IDs in a read-block (drop the read), then take a single write to remove them. Use `HOLD_DURATION`.

    7. Extend the existing PRUNE click handler at `agents.rs:61-66` (D-12). BEFORE the spawn that awaits `api_agents_prune(None).await`, add `recently_terminated.write().clear();` so the client-side state is gone synchronously. Keep the existing `agents_resource.restart()` call after the await.

    8. Extend the rsx! body to render HOLD cards (Common Op 4):
       - Existing `for agent in agents_list.iter()` → pass `is_ended: false` to AgentCard.
       - Add a sibling `for (id, (info, _ts)) in recently_terminated.read().iter()` that renders `AgentCard { key: "{id}-ended", agent: info.clone(), agents_resource, is_ended: true }`. The `.read()` GenerationalRef is fine here — no `.await` in the rsx! block scope (verified in RESEARCH Common Op 4 footnote).

    9. Extend `AgentCard` props with a new `is_ended: bool` field. Inside `AgentCard`'s rsx!:
       - On the outer `div.card`, add `class: if is_ended { "is-ended" }` (Dioxus 0.7 conditional-attribute idiom — `.is-active` at agents.rs:41 is the established analog).
       - On the status pill, replace `class: "pill green"` with `class: if is_ended { "pill ended" } else { "pill green" }`. Pill text becomes `if is_ended { "ENDED" } else { "{agent.status.to_uppercase()}" }` (D-09 locked literal).
       - D-11 (frozen uptime): the `.card-meta` rendering already pulls `agent.uptime_secs` — because `agent` is the snapshot passed in, no extra work needed. Confirm via the test in Task 1 that the snapshot field is preserved through the prop boundary.
       - D-10 (button-hide) is pure-CSS via `.card.is-ended .card-footer { display: none; }` — do NOT add an RSX conditional on the card-footer.
       - AgentCard registers its id into `busy_set` from inside its existing `killing/interrupting` signal-flip path: when either becomes true, `busy_set.write().insert(agent.id.clone())`; when both are false, `busy_set.write().remove(&agent.id)`. Add a `use_drop(move || { busy_set.write().remove(&agent.id); })` so unmount cleans up (RESEARCH Pitfall 6).

    Step D — clippy / build sanity:
    Run `cargo clippy -p iron_hermes_ui --features web -- -D warnings`. The new code MUST be warning-free (pre-existing warp_hermes.rs lints are out of scope per RESEARCH Project Constraints). Run `cargo build -p iron_hermes_ui --target wasm32-unknown-unknown --features web`. If `std::time::Instant` panics under `dx serve` later (Risk #3 — discovered during UAT), fall back to `web-time = "1.1"` workspace dep + substitute `web_time::Instant`. Do NOT pre-emptively add the dep.

    NEVER place fenced code blocks in this action. Read the read_first files for current source. Use the `<interfaces>` block at the top of this plan for type signatures.
  </action>

  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p iron_hermes_ui --features web 2>&amp;1 | tail -20 &amp;&amp; cargo clippy -p iron_hermes_ui --features web -- -D warnings 2>&amp;1 | tail -10 &amp;&amp; cargo build -p iron_hermes_ui --target wasm32-unknown-unknown --features web 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `cargo test -p iron_hermes_ui --features web` exits 0 (Task 1 tests still pass; no new unit tests required here).
    - `cargo clippy -p iron_hermes_ui --features web -- -D warnings` exits 0 for files touched in this plan. Pre-existing warp_hermes.rs lints are NOT a regression source.
    - `cargo build -p iron_hermes_ui --target wasm32-unknown-unknown --features web` exits 0 (Risk #3 wasm32 verification — if it fails at runtime, fallback to web-time as described in action).
    - `grep -c 'POLL_INTERVAL_BASELINE_MS' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` ≥ 1 and `grep -c 'POLL_INTERVAL_WS_CONNECTED_MS' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` returns 0 (Plan 02 adds it).
    - `grep -c 'HOLD_DURATION' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` ≥ 1.
    - `grep -c 'recently_terminated' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` ≥ 4 (signal decl + diff + decay + render).
    - `grep -c 'visibilitychange' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` ≥ 1.
    - `grep -c '\.forget()' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` returns 0 (anti-pattern guard — RESEARCH anti-pattern: never `cb.forget()`).
    - `grep -c 'recently_terminated.write().clear()' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` ≥ 1 (D-12 PRUNE).
    - `grep -v '^#' crates/iron_hermes_ui/assets/screens.css | grep -c '\.card\.is-ended'` ≥ 1 and `grep -v '^#' crates/iron_hermes_ui/assets/screens.css | grep -c '\.pill\.ended'` ≥ 1 and `grep -v '^#' crates/iron_hermes_ui/assets/screens.css | grep -c 'opacity: 0.55'` ≥ 1 (D-09/D-10/D-14 — comment-stripped to avoid counting headers; UI-SPEC locks 0.55, no other value acceptable).
    - `grep -c 'subagent_events' crates/iron_hermes_ui/src/components/warp_hermes.rs` ≥ 1 and `grep -c 'is_ws_connected' crates/iron_hermes_ui/src/components/warp_hermes.rs` ≥ 1 (Plan 02 prep — RESEARCH Sequencing Note items 1+2).
    - `grep -c 'use_context_provider' crates/iron_hermes_ui/src/components/warp_hermes.rs` ≥ 3 (existing ShellSettings + two new slots).
    - `grep -c '"Event"' crates/iron_hermes_ui/Cargo.toml` ≥ 1 (Task 1 already added; this confirms it's still present).
    - No fenced code blocks were added inside the planner action text.
  </acceptance_criteria>

  <done>
    Periodic poll + Page Visibility + busy-set pause + recently_terminated diff/decay + HOLD render + PRUNE clear all wired. CSS modifier rules added. WarpHermes context slots present with initial values (0, false). Clippy clean for new code, wasm32 builds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Plan 01 UAT — TERMINATED-HOLD-N + tab-hidden + 26.7-06 regression</name>
  <files>n/a (manual verification against dx serve)</files>

  <read_first>
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-VALIDATION.md (Manual-Only Verifications table — exact scenarios to exercise)
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-UI-SPEC.md (Card State Mockup — expected visual)
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-CONTEXT.md ("Closes with" section)
  </read_first>

  <what-built>
    Periodic 1500 ms poll of `/api/agents/list`. Page Visibility pause + restore. Per-card kill/interrupt pause via busy-set context. Client-side TERMINATED-HOLD-N=5s fade with frozen uptime, ENDED pill, hidden card-footer via `.card.is-ended` + `.pill.ended` CSS. PRUNE clears both server and client state. WarpHermes context slots present but unincremented (Plan 02 wires them).
  </what-built>

  <how-to-verify>
    Spin up `cd crates/iron_hermes_ui && RUSTC_WRAPPER= dx serve`. Open DevTools → Network. Verify these scenarios:

    1. **Baseline poll (D-03):** With no chat ws traffic (or with ws live — both should be 1500 ms in Plan 01 since `is_ws_connected` is never set true yet), filter `/api/agents/list`. Expect a fetch every ~1500 ms ±150 ms.

    2. **Natural-termination HOLD (D-01 + D-02 + D-09 + D-10 + D-11):** Submit a chat message that triggers `delegate_task` with a subagent that takes >2 s (e.g., a real model call). Watch the Agents page — agent appears as RUNNING card with green pill. When agent terminates naturally, expect the card to shift to `.card.is-ended` (opacity 0.55, no buttons, ENDED gray pill). Stopwatch from the visual transition: the card disappears ~5 s later. The uptime value in the ENDED card matches the last-observed live value (frozen, NOT incrementing).

    3. **Kill-path HOLD (D-02):** Click KILL, then KILL? within 3 s. Server returns 200 (26.7-06 regression). Card shifts to ENDED state and fades over 5 s.

    4. **Interrupt-path HOLD (D-02):** Click INTERRUPT, let agent finalize. Card shifts to ENDED state and fades over 5 s.

    5. **Tab-hidden pause (D-04):** Switch tabs for 10 s, return. DevTools Network shows zero `/api/agents/list` fetches during hidden period. One fetch fires within 50 ms of return.

    6. **Kill/interrupt pause (D-05):** Click KILL then KILL? in rapid succession. While the kill RPC is in flight, no `/api/agents/list` fetch interleaves between the kill POST and its 200 response. (May require slowing kill artificially; observation in a fast network is best-effort.)

    7. **PRUNE clears both (D-12):** With at least one ENDED card visible, click PRUNE ENDED. Expect that card and any server-side stale agents to disappear immediately (NOT wait the remaining HOLD duration).

    8. **26.7-06 regression:** All four endpoints `/api/agents/kill`, `/api/agents/interrupt`, `/api/agents/prune`, `/api/agents/list` return 200 throughout. Server log contains no `panicked at crates/ironhermes-agent/src/subagent_registry.rs:86` line (26.7-07 carryforward).

    9. **No console errors:** DevTools Console shows zero red errors related to this phase (in particular no `borrow already taken` runtime panic — Pitfall 1 self-check; no `Instant::now() not supported` panic — Risk #3 self-check).

    If scenario 9 hits an `Instant::now()` panic, fall back to `web-time = "1.1"` per Task 2's escape hatch and re-run.
  </how-to-verify>

  <action>
    Run the UAT scenarios listed in the how-to-verify block above against `cd crates/iron_hermes_ui && RUSTC_WRAPPER= dx serve`. This task does not modify code; it executes the manual checks and reports back. If a scenario fails, do NOT silently fix and re-run — surface the failure on the resume-signal so the planner / developer can decide whether to revise Task 2's output.
  </action>

  <verify>
    <human-check>UAT scenarios 1-9 above all pass. Approval recorded on the resume-signal.</human-check>
    <automated>echo "UAT checkpoint — see resume-signal"</automated>
  </verify>

  <done>
    User has typed "approved" (or scenario-specific approval) on the resume-signal. All 9 UAT scenarios verified visually + via DevTools.
  </done>

  <resume-signal>Type "approved" if all 9 scenarios pass, or describe specific failures (with screenshots/devtools captures preferred). If an `Instant::now()` panic occurred and was patched with web-time, mention it explicitly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ frontend server | Pre-existing chat ws + `/api/agents/*` endpoints (carry from 26.7) — Plan 01 adds no new server surface |
| Tab ↔ Tab (same origin) | Each tab maintains independent `recently_terminated` — server-side buffer explicitly rejected per CONTEXT D-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.7.1-01-DoS-map | Denial of Service | `recently_terminated` HashMap in `agents.rs` ScreenAgents | accept | CONTEXT.md "Deferred Ideas" explicitly accepts no cap; 5 s decay window bounds size to roughly (terminations per second × 5) for human-cadence agents. RESEARCH Pitfall 4 confirms accept. Add bound only if user complaint emerges. |
| T-26.7.1-01-DoS-listener | Denial of Service / Resource Leak | Page Visibility `Closure` storage in `agents.rs` | mitigate | `Signal<Option<Closure<...>>>` slot + `use_drop` removes listener on unmount (RESEARCH Pattern 1; verified at wheel.rs:262-279 and warp_hermes.rs:541-551). Acceptance criterion grep forbids `cb.forget()`. |
| T-26.7.1-01-Race-borrow | Tampering / Reliability | Poll loop `use_future` reading `recently_terminated` across awaits | mitigate | All `.read()` GenerationalRef borrows are dropped before `TimeoutFuture::new().await` via call-syntax for Copy types and explicit read-block scopes for HashMap (RESEARCH Pitfall 1). `crates/iron_hermes_ui/clippy.toml` enforces statically; `cargo clippy -- -D warnings` is an acceptance gate. |
| T-26.7.1-01-Race-RPC | Tampering / Reliability | Concurrent poll-fetch and kill/interrupt RPC | mitigate | D-05 + busy-set context provider pauses poll while any per-card kill/interrupt is in flight. AgentCard cleanup via `use_drop` ensures no stale ids leak after unmount (RESEARCH Pitfall 6). |
| T-26.7.1-01-Spoof-replay | Tampering | Subagent registry replay with same id | mitigate | D-13 + `diff_terminations` pure fn unit-tested (Task 1 Test 2) returns the replayed id in `removals`, removing it from `recently_terminated` before the next render. |
| T-26.7.1-01-V5-shape | Input Validation | New CSS class names rendered into the DOM | accept | `.is-ended` / `.pill ended` are static literals in Rust code — no user input flows into the class attribute. AgentInfo.id flows into the `key` attribute and into busy_set; existing 26.7 sanitization at the server boundary applies. No new validation needed. |
| T-26.7.1-01-26.7-06-regr | Reliability | `/api/agents/{kill, interrupt, prune}` endpoints | accept | Phase 26.7-06 `spawn_blocking` and 26.7-07 `RegistrationGuard::drop` fixes are not modified. UAT Task 3 scenario 8 + 9 are the regression assertion. |
</threat_model>

<verification>
- Task 1 unit tests (`diff_terminations`) verify D-02, D-11, D-13 invariants.
- Task 2 static greps verify D-03 (POLL_INTERVAL_BASELINE_MS), D-04 (visibilitychange), D-09/D-10/D-14 (CSS classes + 0.55 opacity), D-12 (PRUNE write().clear()), and Plan 02 prep (subagent_events, is_ws_connected, "Event" feature).
- Task 2 clippy + wasm32 build gates verify Pitfall 1 (signal-borrow across await) and Risk #3 (Instant on wasm32).
- Task 3 UAT verifies D-01 (HOLD-N=5s), D-02 (all terminations covered), D-03–D-05 (poll cadence + pauses), D-12 (PRUNE atomicity), and 26.7-06/26.7-07 regression.
</verification>

<success_criteria>
- All requirements D-01..D-05 + D-09..D-14 demonstrably satisfied (unit + static + UAT).
- D-07-slot and D-08-slot context providers in `WarpHermes` present with initial values 0u64 and false (Plan 02 entry contract).
- `cargo test -p iron_hermes_ui --features web` passes, `cargo clippy -p iron_hermes_ui --features web -- -D warnings` clean for new code, `cargo build -p iron_hermes_ui --target wasm32-unknown-unknown --features web` succeeds.
- UAT all 9 scenarios pass; the user approves with `approved`.
</success_criteria>

<output>
Create `.planning/phases/26.7.1-agents-page-live-updates/26.7.1-01-SUMMARY.md` when done. Include:
- Decisions made within Claude's Discretion (opacity tuning rejected per UI-SPEC lock; busy-set via context vs prop; Instant source — std vs web-time).
- Whether Risk #3 fired (Instant::now() wasm32 panic) and if so the web-time substitution.
- Plan 02 hand-off line: "Context slots `subagent_events: Signal<u64>` (init 0) and `is_ws_connected: Signal<bool>` (init false) provided in WarpHermes; Plan 02 wires the increment + lifecycle sites."
</output>
