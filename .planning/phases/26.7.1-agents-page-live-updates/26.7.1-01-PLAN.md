---
phase: 26.7.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/iron_hermes_ui/src/components/hermes_app/mod.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/agents_diff.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/mod.rs
  - crates/iron_hermes_ui/assets/screens.css
  - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-VALIDATION.md
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
user_setup: []

must_haves:
  truths:
    - "ScreenAgents polls the /api/agents/list endpoint every ~1500 ms while visible and idle"
    - "Poll halts when document.hidden() returns true (Page Visibility) and resumes on visibility return"
    - "Poll halts while any per-card kill or interrupt RPC is in flight (rpc_in_flight > 0)"
    - "Agents that terminate (any kind — natural, killed, interrupted) appear as an ENDED card for 5 seconds before being removed from the DOM"
    - "The ENDED card body opacity is 0.55 and its card-footer (CHAT / INTERRUPT / KILL) is hidden via CSS"
    - "An agent id that reappears in the live list while present in recently_terminated is dropped from recently_terminated immediately (D-13)"
    - "PRUNE ENDED click clears recently_terminated synchronously in addition to calling api_agents_prune"
    - "HermesApp provides subagent_events: Signal<u64> and is_ws_connected: Signal<bool> via use_context_provider so the SSR-render of ScreenAgents resolves both contexts without panic"
    - "Server-render and dx serve both load the Agents page without 'Could not find context' panic"
  artifacts:
    - path: "crates/iron_hermes_ui/src/components/hermes_app/mod.rs"
      provides: "subagent_events + is_ws_connected signal declarations and context providers"
      contains: "use_context_provider(|| subagent_events)"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/mod.rs"
      provides: "is_ws_connected provider"
      contains: "use_context_provider(|| is_ws_connected)"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents_diff.rs"
      provides: "pure diff_terminations function — unit-testable without proc-macro expansion"
      contains: "pub fn diff_terminations"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs"
      provides: "ScreenAgents poll loop + recently_terminated + decay sweep + AgentCard is-ended rendering"
      contains: "recently_terminated"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs"
      provides: "Page Visibility (D-04) check inside the poll loop"
      contains: "visibilitychange"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs"
      provides: "rpc_in_flight counter consulted by the poll loop (D-05)"
      contains: "rpc_in_flight"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs"
      provides: "PRUNE click clears local recently_terminated synchronously (D-12)"
      contains: "recently_terminated.write().clear()"
    - path: "crates/iron_hermes_ui/assets/screens.css"
      provides: ".card.is-ended modifier (opacity 0.55, hidden card-footer) per D-10/D-14"
      contains: ".card.is-ended"
    - path: "crates/iron_hermes_ui/assets/screens.css"
      provides: ".pill.ended modifier (gray neutral) per D-09/D-14"
      contains: ".pill.ended"
    - path: ".planning/phases/26.7.1-agents-page-live-updates/26.7.1-VALIDATION.md"
      provides: "Two grep targets corrected from warp_hermes.rs to hermes_app/mod.rs"
      contains: "crates/iron_hermes_ui/src/components/hermes_app/mod.rs"
  key_links:
    - from: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs"
      to: "crates/iron_hermes_ui/src/components/hermes_app/mod.rs"
      via: "use_context::<Signal<bool>>() for is_ws_connected and use_context::<Signal<u64>>() for subagent_events"
      pattern: "use_context::<Signal<(bool|u64)>>"
    - from: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs"
      to: "agents_diff::diff_terminations"
      via: "module use of diff_terminations to compute newly-absent agent ids"
      pattern: "diff_terminations\\("
    - from: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs"
      to: "gloo_timers::future::TimeoutFuture"
      via: "poll loop and decay sweep both await TimeoutFuture::new(ms)"
      pattern: "gloo_timers::future::TimeoutFuture::new"
    - from: "crates/iron_hermes_ui/assets/screens.css"
      to: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs"
      via: ".card.is-ended class toggled by ScreenAgents on HOLD-state cards"
      pattern: "class: if is_ended.*\"is-ended\""
---

<objective>
Plan 01 lands the **active-root** Foundation for live Agents updates:

1. Install two new context providers (`subagent_events: Signal<u64>`, `is_ws_connected: Signal<bool>`) on `HermesApp` BEFORE any consumer reads them, so server-side render of `ScreenAgents` resolves both `use_context::<Signal<...>>()` calls without the `Could not find context` panic that reverted the prior attempt.
2. Extract a pure `diff_terminations` function in a new sibling file (`agents_diff.rs`) so D-13 ("re-running id wins") can be unit-tested without `use_server_future` proc-macro expansion.
3. Wrap `ScreenAgents` with a periodic poll loop (1500 ms baseline) that respects the Page Visibility API (D-04) and a `rpc_in_flight: Signal<u32>` counter (D-05). The poll-cadence value reads `is_ws_connected` (consumed via context — wired to a `false` default by HermesApp in this plan; Plan 02 mutates it from the ws lifecycle).
4. Maintain `recently_terminated: Signal<HashMap<String, (AgentInfo, Instant)>>` with a 1-second decay sweep that removes entries whose elapsed time ≥ 5 s (D-01, D-11). Render HOLD cards with `.card.is-ended` + `.pill.ended` and the `card-footer` hidden via CSS (D-09/D-10/D-14).
5. Extend the existing PRUNE ENDED click handler to `recently_terminated.write().clear()` synchronously (D-12).
6. Patch the two stale grep rows in `26.7.1-VALIDATION.md` from `warp_hermes.rs` to `hermes_app/mod.rs` so the verifier sees the active-root targets.

Purpose: ship a polling baseline that makes any subagent living >1.5 s visible to the user, and surface ALL terminations (regardless of cause) as a 5 s HOLD card. After Plan 01 the user can see real-time agent appearance/disappearance even with no WebSocket events flowing — Plan 02 then layers push-driven sub-second latency on top.

Output: HermesApp signal providers, `agents_diff.rs` (new file, pure fn + unit test), modified `agents.rs` (poll + diff + decay + HOLD render + PRUNE clear + rpc_in_flight prop threading), modified `screens.css` (two new modifier rules), modified `VALIDATION.md` (two grep target corrections).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.7.1-agents-page-live-updates/26.7.1-CONTEXT.md
@.planning/phases/26.7.1-agents-page-live-updates/26.7.1-RESEARCH.md
@.planning/phases/26.7.1-agents-page-live-updates/26.7.1-PATTERNS.md
@.planning/phases/26.7.1-agents-page-live-updates/26.7.1-UI-SPEC.md
@.planning/phases/26.7.1-agents-page-live-updates/26.7.1-VALIDATION.md

@crates/iron_hermes_ui/CLAUDE.md
@crates/iron_hermes_ui/clippy.toml
@crates/iron_hermes_ui/Cargo.toml
@crates/iron_hermes_ui/src/components/hermes_app/mod.rs
@crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs
@crates/iron_hermes_ui/src/components/hermes_app/screen_router.rs
@crates/iron_hermes_ui/src/components/hermes_app/wheel.rs
@crates/iron_hermes_ui/src/protocol.rs

<interfaces>
<!-- Key contracts extracted from the codebase so the executor does not need to spelunk -->

From crates/iron_hermes_ui/src/server/api.rs (READ-ONLY — do not edit):
  pub struct AgentInfo { pub id: String, pub status: String, pub uptime_secs: u64, pub task_summary: String, /* + parent_id, started_at_unix_ms */ }
  pub async fn api_agents_list() -> Result<Vec<AgentInfo>>;
  pub async fn api_agents_prune(stale_secs: Option<u64>) -> Result<Vec<String>>;
  pub async fn api_agents_kill(id: String) -> Result<...>;
  pub async fn api_agents_interrupt(id: String) -> Result<...>;

From crates/iron_hermes_ui/src/protocol.rs (READ-ONLY in Plan 01 — Plan 02 extends):
  pub enum ChatStreamEvent { Delta{..}, ToolCallStart{..}, ToolCallEnd{..}, Finished{..}, Error{..} }
  No #[serde(...)] attribute on the enum (external tagging applies).

From crates/iron_hermes_ui/src/components/hermes_app/mod.rs (READ AND EXTEND):
  pub fn HermesApp() -> Element  // active root
  Existing signal cluster at ~lines 120-124 (use_signal block).
  Existing context provider cluster at lines 302-306. ADD two new providers AFTER line 306.
  Existing use_websocket at line 147, recv loop use_future at line 156.
  Receive-loop match block runs ~lines 172-232; outer connect/close arms at ~lines 158-162, ~lines 234-237.

From crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs (READ AND EXTEND):
  #[component] pub fn ScreenAgents(is_active: bool) -> Element  // Plan 01 wraps body
  #[component] fn AgentCard(agent: AgentInfo, agents_resource: Resource<...>) -> Element
    // Plan 01 extends props to (agent, agents_resource, is_ended, rpc_in_flight)
  Existing per-card signals: armed, killing, interrupting (use_signal, component-local).
  Existing gloo_timers::future::TimeoutFuture usage at lines 171, 199.
  Existing PRUNE click at lines 60-68 — extend to clear recently_terminated.

From crates/iron_hermes_ui/src/components/hermes_app/wheel.rs (idiom reference ONLY):
  use dioxus::core::use_drop;            // exact import path — required
  #[cfg(target_arch = "wasm32")] use wasm_bindgen::JsCast;   // exact gating — required
  // The Closure<dyn FnMut> + listener_slot: Signal<Option<Closure>> + use_drop teardown
  // pattern in wheel.rs is the analog for any visibilitychange listener Plan 01 installs.

From crates/iron_hermes_ui/clippy.toml (enforced as -D warnings on every commit):
  await-holding-invalid-types includes:
    generational_box::GenerationalRef
    generational_box::GenerationalRefMut
    dioxus_signals::WriteLock
  // Rule: never hold a .read() / .write() across a `.await`. Materialize into an
  // owned/Copy value, end the statement, THEN await.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install subagent_events + is_ws_connected providers in HermesApp (active root)</name>
  <files>crates/iron_hermes_ui/src/components/hermes_app/mod.rs</files>
  <action>
This is the load-bearing task that fixes the prior phase's runtime panic. Add the two new signal declarations and their context providers to `HermesApp` (the ACTIVE root — never `WarpHermes`) so that any descendant `use_context::<Signal<u64>>()` / `use_context::<Signal<bool>>()` resolves on both server-side render and `dx serve`. Per D-07 / D-08 / per the targeting correction in 26.7.1-RESEARCH.md.

In `crates/iron_hermes_ui/src/components/hermes_app/mod.rs`, inside the `HermesApp` component body:

1. After the existing `use_signal` cluster ending at ~line 124 (right after `let mut tokens = use_signal(|| (0u32, 128_000u32));`), declare two new signals:
   - `let mut subagent_events = use_signal(|| 0u64);`
   - `let mut is_ws_connected = use_signal(|| false);`

   Both must be declared with `let mut` (so Plan 02 can call `.set()` from the recv loop). The `mut` binding on the local does not prevent Plan 02's `use_effect` consumers from observing changes — Dioxus `Signal<T>` is `Copy` and the `mut` is on the binding, not the signal.

2. Immediately AFTER the existing `use_context_provider(|| send_handler);` line (at ~line 306), append two new providers in this order:
   - `use_context_provider(|| subagent_events);`
   - `use_context_provider(|| is_ws_connected);`

   Add a brief doc comment above the two new lines: `// Phase 26.7.1 Plan 01 — context for ScreenAgents (D-07 / D-08). subagent_events drives push-restart in Plan 02; is_ws_connected drives dynamic poll cadence.`

3. Do NOT yet add any match arm or lifecycle `.set()` calls for these signals in the recv loop — Plan 02 owns that work. Plan 01 only installs the providers with their initial values (0 / false). This is sufficient for `ScreenAgents` to consume them via `use_context` without panic.

Compile check (after this task only):
- `cargo check -p iron_hermes_ui --features web` (native target — clippy.toml signal-borrow rule will fire if any read is held over an await; should be clean here since no awaits added).
- `cargo check -p iron_hermes_ui --features web --target wasm32-unknown-unknown`.

Per the critical lesson from the prior attempt: `cargo check --features web` does NOT exercise wasm32 by default. BOTH builds must pass.

DO NOT touch `warp_hermes.rs`. DO NOT add a `use_context_provider` anywhere else in the tree.
  </action>
  <verify>
    <automated>
grep -c 'use_context_provider(|| subagent_events)' crates/iron_hermes_ui/src/components/hermes_app/mod.rs   # must be >= 1
grep -c 'use_context_provider(|| is_ws_connected)' crates/iron_hermes_ui/src/components/hermes_app/mod.rs   # must be >= 1
grep -c 'let mut subagent_events = use_signal(|| 0u64)' crates/iron_hermes_ui/src/components/hermes_app/mod.rs   # must be >= 1
grep -c 'let mut is_ws_connected = use_signal(|| false)' crates/iron_hermes_ui/src/components/hermes_app/mod.rs   # must be >= 1
! grep -q 'use_context_provider(|| subagent_events)' crates/iron_hermes_ui/src/components/warp_hermes.rs   # must NOT appear in dead-code shell
cargo check -p iron_hermes_ui --features web
cargo check -p iron_hermes_ui --features web --target wasm32-unknown-unknown
    </automated>
  </verify>
  <done>
HermesApp declares both new signals and exposes them via use_context_provider, in the same cluster as the existing nine providers. Native + wasm32 builds both succeed. `warp_hermes.rs` is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract pure diff_terminations into agents_diff.rs sibling with unit tests (D-13)</name>
  <files>
crates/iron_hermes_ui/src/components/hermes_app/screens/agents_diff.rs
crates/iron_hermes_ui/src/components/hermes_app/screens/mod.rs
  </files>
  <action>
Create a NEW file `crates/iron_hermes_ui/src/components/hermes_app/screens/agents_diff.rs` containing the pure diff function the polling loop will use. Keeping this function out of `agents.rs` (which is decorated with `#[component]` and the `use_server_future` proc-macro expansion) makes it unit-testable without spinning up a Dioxus VirtualDom.

Required exports:

```
pub fn diff_terminations(
    live_prev: &[crate::server::api::AgentInfo],
    live_next: &[crate::server::api::AgentInfo],
) -> Vec<crate::server::api::AgentInfo>;
```

Semantics: returns every `AgentInfo` whose `id` is present in `live_prev` but absent from `live_next`. The returned `AgentInfo` is cloned from the `live_prev` slot so the caller has the LAST-OBSERVED snapshot (D-11: uptime_secs is frozen at termination time).

Implementation must:
- Build a `HashSet<&str>` of next-ids from `live_next.iter().map(|a| a.id.as_str())`.
- Iterate `live_prev`, filter `!next_ids.contains(a.id.as_str())`, collect cloned matches.
- Be `pub` (callable from `agents.rs`).
- Carry a doc comment naming D-02 (uniform — no per-kind branching) and D-11 (snapshot).

Add unit tests inside the same file under `#[cfg(test)] mod tests`:

- `test_diff_terminations_returns_absent_agents` — three agents in prev, one missing in next, asserts that one is returned with its uptime_secs intact.
- `test_diff_terminations_empty_when_all_present` — same set in both → empty Vec.
- `test_diff_terminations_handles_empty_prev` — empty prev + non-empty next → empty Vec.
- `test_diff_terminations_rerunning_id_is_not_a_termination` (D-13 invariant) — an id present in both prev AND next must NOT appear in the returned Vec; this is the D-13 "re-running id wins" property exercised at the diff level. Critical regression guard.

To construct test fixtures without a live SubagentRegistry, build `AgentInfo` instances directly. Inspect the struct shape in `crates/iron_hermes_ui/src/server/api.rs` (the `pub struct AgentInfo { ... }` around line 361) and instantiate each field with a literal. Use the simplest possible values (empty strings / 0 / "sub_test_id"); the diff function only reads `.id`.

Then, register the new module in `crates/iron_hermes_ui/src/components/hermes_app/screens/mod.rs`:
- Add `pub mod agents_diff;` at the same indentation as the existing `pub mod agents;` line. Order alphabetically if the mod list is alphabetical, otherwise place it immediately after `pub mod agents;`.

No call site is added in `agents.rs` in this task — Task 4 wires the use of `diff_terminations`. Decoupling lets the test pass independently of the heavier integration work.

Build + test gates:
- `cargo test -p iron_hermes_ui --features web agents_diff` runs the four tests.
- `cargo clippy -p iron_hermes_ui --features web -- -D warnings`.
- BOTH cargo check targets (native + wasm32) per Task 1's protocol.
  </action>
  <verify>
    <automated>
test -f crates/iron_hermes_ui/src/components/hermes_app/screens/agents_diff.rs
grep -c 'pub fn diff_terminations' crates/iron_hermes_ui/src/components/hermes_app/screens/agents_diff.rs   # must be >= 1
grep -c 'pub mod agents_diff' crates/iron_hermes_ui/src/components/hermes_app/screens/mod.rs   # must be >= 1
cargo test -p iron_hermes_ui --features web --no-run
cargo test -p iron_hermes_ui --features web agents_diff
cargo clippy -p iron_hermes_ui --features web -- -D warnings
cargo check -p iron_hermes_ui --features web --target wasm32-unknown-unknown
    </automated>
  </verify>
  <done>
agents_diff.rs exists with diff_terminations + 4 unit tests (including the D-13 rerunning-id guard). All tests pass. Workspace clippy clean. Both native and wasm32 builds succeed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add CSS modifiers — .card.is-ended and .pill.ended (D-09 / D-10 / D-14)</name>
  <files>crates/iron_hermes_ui/assets/screens.css</files>
  <action>
Add exactly two new CSS rule blocks to `crates/iron_hermes_ui/assets/screens.css`, following the values locked in `26.7.1-UI-SPEC.md` (which itself sources `--gray = #6e7681` / `--gray-faint = rgba(110, 118, 129, 0.45)` from `site.css:30-31`).

1. **`.card.is-ended` block** — place immediately AFTER the existing `.card.is-active { ... }` rule (currently `screens.css:118-122`). Contents:

```
/* Phase 26.7.1 D-10 / D-14: terminated agent card in HOLD-N state */
.card.is-ended {
  opacity: 0.55;
  transition: opacity 200ms ease-out;
  pointer-events: none;
}
.card.is-ended .card-footer {
  display: none;
}
```

Why these values (do not tune):
- `opacity: 0.55` — matches the existing `-dim` token alpha family (`--teal-dim: rgba(57, 197, 207, 0.55)` at `site.css:23`) and is the locked D-10 value.
- `transition: opacity 200ms ease-out` — aligns with the existing `.card` `transition: ... 0.18s` cadence at the same file.
- `pointer-events: none` — suppresses hover-state shifts on dimmed cards.
- `.card.is-ended .card-footer { display: none; }` is the single source of truth that hides CHAT / INTERRUPT / KILL. Per UI-SPEC, this is preferred over an RSX conditional because it requires no signal reads in the AgentCard render path.

2. **`.pill.ended` block** — place immediately AFTER the existing `.pill.red { ... }` rule (currently `screens.css:211`). Contents:

```
/* Phase 26.7.1 D-09 / D-14: neutral gray pill for terminated agents */
.pill.ended {
  color: var(--gray);
  border-color: var(--gray-faint);
  background: rgba(110, 118, 129, 0.06);
}
```

Why these values (do not tune):
- `color: var(--gray)` for text (solid, not faint) — D-09 / UI-SPEC requires solid color so the ENDED label remains legible against the 0.55 dimmed card.
- `border-color: var(--gray-faint)` (rgba 0.45) — mirrors `.pill.green/.amber/.red` 0.45-alpha border family.
- `background: rgba(110, 118, 129, 0.06)` — mirrors `.pill.green/.amber/.red` 0.06-alpha background family.

Do not introduce a `.pill.gray` alias, do not change `.card.is-active`, do not modify any unrelated rules. The two new rules are additive only.

Verify via static-grep AND via running clippy (CSS isn't lint-checked but the build must still pass).
  </action>
  <verify>
    <automated>
grep -cE '^\.card\.is-ended \{' crates/iron_hermes_ui/assets/screens.css                   # must be >= 1
grep -cE '^\.card\.is-ended \.card-footer \{' crates/iron_hermes_ui/assets/screens.css     # must be >= 1
grep -cE '^\.pill\.ended \{' crates/iron_hermes_ui/assets/screens.css                      # must be >= 1
grep -c 'opacity: 0.55' crates/iron_hermes_ui/assets/screens.css                            # must be >= 1
grep -c 'rgba(110, 118, 129, 0.06)' crates/iron_hermes_ui/assets/screens.css                # must be >= 1
cargo check -p iron_hermes_ui --features web
    </automated>
  </verify>
  <done>
Both new CSS blocks present with the locked values (opacity 0.55, gray pill rgba 0.06 background, card-footer hidden). No unrelated rules changed. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 4: ScreenAgents poll loop + HOLD-N diff + decay + AgentCard is-ended (D-01..D-05, D-09..D-14)</name>
  <files>crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs</files>
  <action>
This is the largest task in Plan 01. It extends `ScreenAgents` and `AgentCard` in `crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs`.

**Required imports at top of file** (add only the ones not already present):
- `use dioxus::core::use_drop;` (exact path — NOT via prelude; matches `wheel.rs:32` and `warp_hermes.rs:2`). Add gated: `#[cfg(target_arch = "wasm32")] use dioxus::core::use_drop;` only if the listener-cleanup closure is used. If you take the simpler "poll-loop-checks-document.hidden() each iteration" approach (no listener at all — recommended per D-04 simplicity), this import is unnecessary.
- `#[cfg(target_arch = "wasm32")] use wasm_bindgen::JsCast as _;` (only if you cast Closure values; the synchronous `Document::hidden()` path does NOT require JsCast).
- The existing `use dioxus::prelude::*;` already covers `use_signal`, `use_future`, `use_effect`, `Signal`, `Resource`, `EventHandler`, `spawn`, `rsx!`.

**Inside `ScreenAgents` body, BEFORE the existing `let agents_list ...` line:**

1. Declare three new screen-level signals:

```
let mut recently_terminated = use_signal(||
    std::collections::HashMap::<String, (crate::server::api::AgentInfo, std::time::Instant)>::new()
);
let mut prev_live = use_signal(|| Vec::<crate::server::api::AgentInfo>::new());
let mut rpc_in_flight = use_signal(|| 0u32);
```

2. Consume the two context signals provided by Task 1's HermesApp providers:

```
let ws_connected_ctx = use_context::<Signal<bool>>();      // is_ws_connected
let _subagent_events_ctx = use_context::<Signal<u64>>();   // subagent_events — touched in Plan 02; bound here so the SSR context lookup happens
```

Do NOT yet add a `use_effect` over `_subagent_events_ctx` — Plan 02 owns that. Binding the value here proves the context is resolvable on first render.

3. After the existing `let agents_list: Vec<...>` materialization (currently lines 33-36), insert the **diff + snapshot block** that runs each render:

```
// D-13: re-running id wins — drop any recently_terminated entry whose id reappeared.
for agent in agents_list.iter() {
    let in_map = recently_terminated.read().contains_key(&agent.id); // bool — borrow ends at ;
    if in_map {
        recently_terminated.write().remove(&agent.id);
    }
}

// D-02 + D-11: ids in prev_live absent from agents_list → snapshot into recently_terminated.
let prev_snapshot: Vec<crate::server::api::AgentInfo> = prev_live.read().clone();
let newly_terminated =
    crate::components::hermes_app::screens::agents_diff::diff_terminations(
        &prev_snapshot,
        &agents_list,
    );
for old in newly_terminated.into_iter() {
    let already = recently_terminated.read().contains_key(&old.id);
    if !already {
        recently_terminated.write().insert(
            old.id.clone(),
            (old, std::time::Instant::now()),
        );
    }
}
prev_live.set(agents_list.clone());
```

4. Materialize the HOLD-card list before `rsx!` (signal-borrow discipline — collect into an owned `Vec` so no `GenerationalRef` is held during the rsx! macro expansion):

```
let ended_cards: Vec<crate::server::api::AgentInfo> = {
    let map = recently_terminated.read();
    map.values().map(|(info, _ts)| info.clone()).collect()
}; // borrow ends at }
```

5. **Poll loop** — install as a `use_future` BEFORE the rsx! block (placement around the existing materialization, but ordering doesn't matter since use_future closures run independently). Read all signal values into Copy locals BEFORE every `.await`:

```
let mut agents_resource_for_poll = agents_resource;   // Resource is Copy
use_future(move || async move {
    loop {
        // D-04: skip while tab is hidden. document.hidden() is synchronous bool — no JsCast needed.
        #[cfg(target_arch = "wasm32")]
        let hidden: bool = web_sys::window()
            .and_then(|w| w.document())
            .map(|d| d.hidden())
            .unwrap_or(false);
        #[cfg(not(target_arch = "wasm32"))]
        let hidden: bool = false;

        if hidden {
            gloo_timers::future::TimeoutFuture::new(500).await;
            continue;
        }

        // D-05: skip while a kill/interrupt RPC is in flight.
        let in_flight: u32 = *rpc_in_flight.read();   // Copy — borrow ends at ;
        if in_flight > 0 {
            gloo_timers::future::TimeoutFuture::new(200).await;
            continue;
        }

        agents_resource_for_poll.restart();

        // D-08: dynamic cadence reads is_ws_connected from context. Plan 01 ships with the
        // initial value (false), giving 1500 ms baseline (D-03). Plan 02 wires the recv-loop
        // .set(true)/.set(false) calls that promote to 5000 ms while connected.
        let interval_ms: u32 = if *ws_connected_ctx.read() { 5_000 } else { 1_500 };
        // ws_connected_ctx borrow ends at ; — interval_ms is Copy.
        gloo_timers::future::TimeoutFuture::new(interval_ms).await;
    }
});
```

6. **Decay sweep** — second `use_future` that runs every 1 s, removes entries older than 5 s:

```
use_future(move || async move {
    loop {
        gloo_timers::future::TimeoutFuture::new(1_000).await;
        // Collect expired ids into owned Vec — read borrow ends at } before any write.
        let expired: Vec<String> = {
            let map = recently_terminated.read();
            map.iter()
                .filter_map(|(id, (_info, ts))| {
                    if ts.elapsed() >= std::time::Duration::from_secs(5) {
                        Some(id.clone())
                    } else {
                        None
                    }
                })
                .collect()
        };
        if !expired.is_empty() {
            let mut map = recently_terminated.write();
            for id in &expired {
                map.remove(id);
            }
        }
    }
});
```

7. **Extend PRUNE click** (currently at agents.rs:60-68) — call `recently_terminated.write().clear()` synchronously BEFORE the server prune call (D-12):

```
onclick: move |_| {
    // D-12: clear client-side HOLD state synchronously, then call server prune.
    recently_terminated.write().clear();
    spawn(async move {
        let _ = crate::server::api::api_agents_prune(None).await;
        agents_resource.restart();
    });
},
```

8. **Render HOLD cards alongside live cards.** Inside the existing `for agent in agents_list.iter() { AgentCard { ... } }` block — keep it for the live agents but pass two new props. Then add a SECOND `for` block immediately after, iterating `ended_cards.iter()` and rendering an `AgentCard` with `is_ended: true`:

```
for agent in agents_list.iter() {
    AgentCard {
        key: "{agent.id}",
        agent: agent.clone(),
        agents_resource: agents_resource,
        is_ended: false,
        rpc_in_flight: rpc_in_flight,
    }
}
for agent in ended_cards.iter() {
    AgentCard {
        key: "ended-{agent.id}",
        agent: agent.clone(),
        agents_resource: agents_resource,
        is_ended: true,
        rpc_in_flight: rpc_in_flight,
    }
}
```

9. **Update the `AgentCard` component** — extend the props block at the top of `AgentCard`:

```
#[component]
fn AgentCard(
    agent: crate::server::api::AgentInfo,
    agents_resource: Resource<Result<Vec<crate::server::api::AgentInfo>, CapturedError>>,
    is_ended: bool,
    rpc_in_flight: Signal<u32>,
) -> Element {
    // ... existing per-card signals ...
}
```

10. **Toggle `.card.is-ended` and `.pill.ended` and re-label the pill in AgentCard rsx!:**

- On the outermost `div { class: "card", ... }`, add a second `class:` attribute (Dioxus 0.7 supports chained `class:` conditionals, same pattern as the section's `class: if is_active { "is-active" }`):
  ```
  div {
      class: "card",
      class: if is_ended { "is-ended" },
      ...
  }
  ```
- The `span { class: "pill green", ... }` becomes:
  ```
  span {
      class: if is_ended { "pill ended" } else { "pill green" },
      if is_ended { "ENDED" } else { "{agent.status.to_uppercase()}" }
  }
  ```
- The `card-footer` div does NOT need any RSX change — the CSS rule `.card.is-ended .card-footer { display: none; }` from Task 3 hides all three buttons.
- The `card-meta` line stays at `"{agent.status} · {agent.uptime_secs}s"` — `uptime_secs` is the frozen value from the snapshot stored in `recently_terminated` (D-11), passed through `agent` prop.

11. **D-05 rpc_in_flight increments inside AgentCard onclick closures.** In the existing KILL and INTERRUPT closures:

- INTERRUPT onclick (currently around line 165): before `interrupting.set(true);` add `rpc_in_flight.set(*rpc_in_flight.read() + 1);` and after `interrupting.set(false);` add `rpc_in_flight.set((*rpc_in_flight.read()).saturating_sub(1));`. Note: read into a Copy u32 BEFORE the set call to avoid signal-borrow-across-mutation issues; for clarity:
  ```
  let cur = *rpc_in_flight.read();
  rpc_in_flight.set(cur + 1);
  ```
  Apply the inverse at the end of the spawned async block. Read+set is the canonical Dioxus 0.7 pattern.
- KILL second-click path (currently around line 183-202): same pattern — increment when the kill RPC is dispatched, decrement after the RPC resolves (after `killing.set(false)`).
- Do NOT increment for the FIRST kill click (the "armed" 3-second timer is not an RPC — increment only when the actual kill POST is dispatched on the second click).

Signal-borrow discipline (clippy.toml — enforced):
- Every `.read()` inside an `async move` block MUST be released before `.await`. Materialize into an owned/Copy local, end the statement, then await.
- No `recently_terminated.read()` held across any `.await` in either use_future.
- The diff loops in step 3 deliberately use `let in_map = ...read().contains_key(...);` (Copy bool) so the read guard is dropped before the `.write()` call on the same signal.

**Build/test gates** (after this task):
- `cargo check -p iron_hermes_ui --features web` (native)
- `cargo check -p iron_hermes_ui --features web --target wasm32-unknown-unknown` (wasm)
- `cargo clippy -p iron_hermes_ui --features web -- -D warnings`
- `cargo test -p iron_hermes_ui --features web` (Task 2's tests still green; no new tests added here — agents.rs has the `#[component]` macro so unit tests are non-trivial; integration coverage happens in the UAT checkpoint)

DO NOT touch warp_hermes.rs. DO NOT create new files (besides those in Task 2).
  </action>
  <verify>
    <automated>
grep -c 'recently_terminated' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs   # must be >= 5
grep -c 'rpc_in_flight' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs        # must be >= 3
grep -c 'gloo_timers::future::TimeoutFuture::new(1_500)' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs   # must be >= 1
grep -c 'gloo_timers::future::TimeoutFuture::new(1_000)' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs   # must be >= 1
grep -c 'document().*hidden\|d\.hidden()' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs   # must be >= 1
grep -c 'use_context::<Signal<bool>>()' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs    # must be >= 1
grep -c 'use_context::<Signal<u64>>()' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs     # must be >= 1
grep -c 'recently_terminated.write().clear()' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs   # must be >= 1
grep -c 'is_ended' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs             # must be >= 3
grep -c 'diff_terminations' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs    # must be >= 1
cargo check -p iron_hermes_ui --features web
cargo check -p iron_hermes_ui --features web --target wasm32-unknown-unknown
cargo clippy -p iron_hermes_ui --features web -- -D warnings
cargo test -p iron_hermes_ui --features web
    </automated>
  </verify>
  <done>
ScreenAgents has a 1500 ms poll loop with D-04 visibility guard and D-05 rpc_in_flight guard; recently_terminated populated by diff_terminations and pruned by the 1 s decay sweep; PRUNE click clears it synchronously; AgentCard takes is_ended + rpc_in_flight props and toggles .card.is-ended / .pill.ended / "ENDED" label; signal borrows never cross awaits. All clippy + cargo checks pass on BOTH native and wasm32 targets.
  </done>
</task>

<task type="auto">
  <name>Task 5: Patch VALIDATION.md grep targets (warp_hermes.rs → hermes_app/mod.rs)</name>
  <files>.planning/phases/26.7.1-agents-page-live-updates/26.7.1-VALIDATION.md</files>
  <action>
The current VALIDATION.md has two rows that target the legacy-shell dead-code file. They must be corrected so the verifier reads the active root.

In `.planning/phases/26.7.1-agents-page-live-updates/26.7.1-VALIDATION.md`, update the table cells whose `Automated Command` columns reference `crates/iron_hermes_ui/src/components/warp_hermes.rs`:

1. Row `26.7.1-01-context-providers` — change both grep paths from:
   `grep -c 'subagent_events' crates/iron_hermes_ui/src/components/warp_hermes.rs`
   `grep -c 'is_ws_connected' crates/iron_hermes_ui/src/components/warp_hermes.rs`
   TO:
   `grep -c 'subagent_events' crates/iron_hermes_ui/src/components/hermes_app/mod.rs`
   `grep -c 'is_ws_connected' crates/iron_hermes_ui/src/components/hermes_app/mod.rs`

2. Row `26.7.1-02-client-wire` — change both grep paths from:
   `grep -c 'subagent_events.set' crates/iron_hermes_ui/src/components/warp_hermes.rs`
   `grep -c 'is_ws_connected.set' crates/iron_hermes_ui/src/components/warp_hermes.rs`
   TO:
   `grep -c 'subagent_events.set' crates/iron_hermes_ui/src/components/hermes_app/mod.rs`
   `grep -c 'is_ws_connected.set' crates/iron_hermes_ui/src/components/hermes_app/mod.rs`

Do NOT change any other rows. Do NOT change wave numbers or requirement IDs. Do NOT touch the frontmatter `approved` flag — leave it as it is.

Add a single-line note at the bottom of VALIDATION.md (just above the `## Validation Sign-Off` section or below it — your choice) recording the patch:
`> 2026-05-18 — Plan 26.7.1-01 Task 5 patched two grep rows from warp_hermes.rs (legacy-shell dead code) to hermes_app/mod.rs (active root) per RESEARCH targeting correction.`
  </action>
  <verify>
    <automated>
! grep -q "warp_hermes.rs" .planning/phases/26.7.1-agents-page-live-updates/26.7.1-VALIDATION.md   # zero occurrences after patch
grep -c "hermes_app/mod.rs" .planning/phases/26.7.1-agents-page-live-updates/26.7.1-VALIDATION.md   # must be >= 4 (two grep pairs)
grep -c "26.7.1-01-context-providers" .planning/phases/26.7.1-agents-page-live-updates/26.7.1-VALIDATION.md   # row preserved
grep -c "26.7.1-02-client-wire" .planning/phases/26.7.1-agents-page-live-updates/26.7.1-VALIDATION.md         # row preserved
    </automated>
  </verify>
  <done>
VALIDATION.md no longer references warp_hermes.rs in any grep command. Both former rows now point at hermes_app/mod.rs. A dated note records the correction.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: UAT — verify Plan 01 against dx serve (D-01 / D-02 / D-03 / D-04 / D-05 / D-09..D-14)</name>
  <what-built>
Periodic poll baseline (1500 ms) on the Agents page, with Page Visibility pause (D-04), per-card RPC pause (D-05), and a TERMINATED-HOLD-N 5 s fade for all terminations (D-01 / D-02). HermesApp now provides the two context signals (subagent_events, is_ws_connected) so Server-Side Render and dx serve both resolve `use_context` from ScreenAgents without panic. ENDED cards render dimmed (.card.is-ended opacity 0.55) with their action buttons hidden via CSS, and PRUNE ENDED clears both client-side and server-side state synchronously (D-12).
  </what-built>
  <how-to-verify>
Run from the absolute worktree path (Pitfall 8 from RESEARCH):

```
cd /Users/twilson/code/ironhermes/crates/iron_hermes_ui
RUSTC_WRAPPER= dx serve
```

Wait for the build to finish, then open the dx-printed URL (typically http://127.0.0.1:8080).

1. **No SSR / context panic.**
   - Click the wheel to navigate to the Agents screen.
   - Expected: page renders WITHOUT a "Could not find context dioxus_signals::signal::Signal<bool>" or `<u64>` panic in browser DevTools or in the dx serve stderr.
   - If a panic appears, Task 1's providers did not land in HermesApp — STOP, do not approve.

2. **Baseline poll = ~1500 ms (D-03).**
   - Open DevTools → Network → filter on `/api/agents/list`.
   - Expected: fetches every ~1500 ms (±100 ms) while no chat ws traffic is happening. (Plan 01 ships with `is_ws_connected = false`; Plan 02 will widen this to 5 s.)

3. **Page Visibility pause (D-04).**
   - Switch to another browser tab; wait ~10 s; switch back.
   - Expected: zero `/api/agents/list` fetches during the hidden period; one fetch fires within ~500 ms of returning to the tab.

4. **All terminations show HOLD (D-01 / D-02 / D-11).**
   - Send a chat message like: `delegate a quick task to a subagent to count to 3`. Confirm the chat stream shows a `delegate_task` tool call.
   - As soon as the subagent self-finishes (natural exit), watch the Agents page. Expected: the card switches to gray ENDED pill + dimmed body (`.card.is-ended` opacity 0.55) + hidden CHAT/INTERRUPT/KILL footer. Time the disappearance with a stopwatch — expected ~5 s ±200 ms after the next poll captures the termination.
   - Repeat for KILL (click KILL → KILL? → wait): expected same HOLD card visual.
   - Repeat for INTERRUPT followed by natural finalization: expected same HOLD card visual.
   - All three paths must show the SAME gray ENDED visual (no per-kind branching, D-02 / D-09).

5. **Frozen uptime (D-11).**
   - On a HOLD card, the `card-meta` line should show the uptime that was last observed at termination. Confirm it does NOT keep counting up during the 5 s hold. (Visual: `running · 12s` → `running · 12s` for 5 s, then card disappears.)

6. **Poll pause during kill/interrupt RPC (D-05).**
   - Spawn a long-running subagent.
   - Click KILL (armed), then KILL? again. Observe Network: there should be NO `/api/agents/list` fetch between the kill POST and its 200 response. After the kill resolves, polling resumes.

7. **PRUNE ENDED clears client + server (D-12).**
   - With ≥ 1 ENDED card visible (HOLD state), click PRUNE ENDED.
   - Expected: the ENDED card disappears IMMEDIATELY (before its 5 s would have elapsed). Any server-side stale agents also disappear after the prune's restart.

8. **Re-running id wins (D-13) — best effort.**
   - Spawn a subagent, let it terminate, observe HOLD card. Within the 5 s window, spawn another subagent. If by chance the registry assigns the same id (today unlikely — id generation is random), the HOLD should be immediately replaced by a live RUNNING card with no fade gap. If id assignment makes this unreproducible without a server-side hook, document as "covered by Task 2 unit test only" in the UAT notes.

9. **Regression — 26.7-06 kill/interrupt/prune still 200 (D-02 covers killed-too).**
   - During UAT, all four `/api/agents/{kill,interrupt,prune,list}` endpoints should continue returning 200.
   - Server stderr must NOT contain `subagent_registry.rs:86` panic strings.

10. **Build hygiene (final).**
    - In a separate terminal, before approving:
      ```
      cargo check -p iron_hermes_ui --features web
      cargo check -p iron_hermes_ui --features web --target wasm32-unknown-unknown
      cargo clippy -p iron_hermes_ui --features web -- -D warnings
      cargo test -p iron_hermes_ui --features web
      ```
    - All four must be green.

If any scenario fails, type a description of the failure rather than `approved`. Otherwise approve.
  </how-to-verify>
  <resume-signal>Type "approved" or describe failures so the planner can iterate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM ↔ HermesApp signals | No untrusted input crosses this boundary in Plan 01 (no payload-bearing events added). Document.hidden() is a synchronous bool from the browser's own Visibility API — not attacker-controlled in any meaningful sense. |
| /api/agents/list response → recently_terminated map | Server-controlled response. SubagentRegistry guarantees id uniqueness via Phase 26.7-07's `RegistrationGuard::drop`. No new server surface added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.7.1-01-01 | Denial of Service | recently_terminated HashMap unbounded growth | accept | 5-second decay window bounds map size to roughly (terminations per second × 5). For human-cadence agents this is fine. Cap deferred per CONTEXT.md "Deferred Ideas". |
| T-26.7.1-01-02 | Tampering | Page Visibility bypass (forged event) | accept | The hidden() guard is a UX optimization, not a security boundary. A bypass at worst makes the tab poll while hidden — same behavior as the always-poll baseline. No data exposure. |
| T-26.7.1-01-03 | Denial of Service | poll loop hammering /api/agents/list when document.hidden() misbehaves on some browsers | mitigate | 500 ms re-check interval inside the hidden-arm of the loop bounds request rate to at most 2/s in degenerate cases — still well within the existing endpoint's headroom. Existing api_agents_list is read-only and cheap. |

No new external dependencies, no new authentication surfaces, no new credentials. Security domain unchanged from Phase 26.7's posture.
</threat_model>

<verification>
- Two new `use_context_provider` lines exist in `hermes_app/mod.rs` for `subagent_events` and `is_ws_connected`.
- `agents_diff.rs` exists, exports `pub fn diff_terminations`, has the four unit tests including the D-13 re-running-id guard, and all pass.
- `agents.rs` has the poll loop (1500 ms baseline), decay sweep (1000 ms), document.hidden() guard, rpc_in_flight guard, PRUNE clear, AgentCard `is_ended` prop + class toggles, and `diff_terminations` call site.
- `screens.css` contains `.card.is-ended { opacity: 0.55; ... }`, `.card.is-ended .card-footer { display: none; }`, and `.pill.ended { color: var(--gray); ... }`.
- VALIDATION.md no longer references `warp_hermes.rs` in any grep command.
- Both `cargo check --features web` (native) AND `cargo check --features web --target wasm32-unknown-unknown` succeed.
- `cargo clippy -p iron_hermes_ui --features web -- -D warnings` clean.
- `cargo test -p iron_hermes_ui --features web` green.
- UAT scenarios 1–9 all pass; build hygiene scenario 10 green.
</verification>

<success_criteria>
Plan 01 is complete when the developer has approved the UAT checkpoint (Task 6) with a literal `approved` reply, and the four automated build gates (native + wasm32 check, clippy, test) are green at the post-Task-5 HEAD. The "no SSR / context panic on Agents screen load" scenario is the absolute go/no-go signal — every other UAT scenario assumes that one passes. The page MUST render without panic in dx serve before approval can be given.
</success_criteria>

<output>
Create `.planning/phases/26.7.1-agents-page-live-updates/26.7.1-01-SUMMARY.md` when Plan 01 is complete, summarizing:
- Files touched (count + bullet list).
- The two new HermesApp providers (D-07 / D-08 setup-only landings).
- The `diff_terminations` extraction + test count.
- Confirmation that VALIDATION.md no longer references `warp_hermes.rs`.
- Build-gate evidence (4 cargo commands green).
- UAT scenario outcomes (any deviations from the 9 scenarios, especially D-13 reproducibility note).
</output>
