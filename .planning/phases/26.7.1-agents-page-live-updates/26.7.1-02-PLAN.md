---
phase: 26.7.1
plan: 02
type: execute
wave: 2
depends_on:
  - 26.7.1-01
files_modified:
  - crates/iron_hermes_ui/src/protocol.rs
  - crates/iron_hermes_ui/src/server/ws.rs
  - crates/iron_hermes_ui/src/server/state.rs
  - crates/iron_hermes_ui/src/components/warp_hermes.rs
  - crates/iron_hermes_ui/src/components/hermes_app/mod.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs
autonomous: false
requirements:
  - D-06
  - D-07
  - D-08
user_setup: []

must_haves:
  truths:
    - "ChatStreamEvent has a payload-free SubagentEvent {} variant that serializes to a stable counter-only JSON shape"
    - "ws.rs constructs a SubagentProgressCallback per turn that captures the turn's tx: UnboundedSender<ChatStreamEvent> and forwards Started/ToolCall/Completed alike as SubagentEvent {} frames"
    - "state.rs::run_web_turn accepts an Option<SubagentProgressCallback> parameter and threads it into DelegateTaskWiring for the duration of the turn (not session)"
    - "warp_hermes.rs increments subagent_events: Signal<u64> on each SubagentEvent {} frame received and toggles is_ws_connected: Signal<bool> on Text/Binary open vs Close/error close"
    - "ScreenAgents use_effect subscribes to subagent_events and calls agents_resource.restart() on every increment"
    - "When is_ws_connected() is true, the Plan 01 poll loop widens to 5000 ms (POLL_INTERVAL_WS_CONNECTED_MS); when false it stays at 1500 ms (POLL_INTERVAL_BASELINE_MS)"
    - "Both ChatStreamEvent parse sites — warp_hermes.rs and hermes_app/mod.rs — match-arm the new SubagentEvent {} variant (no _ catch-all silently dropping it)"
  artifacts:
    - path: "crates/iron_hermes_ui/src/protocol.rs"
      provides: "ChatStreamEvent::SubagentEvent {} variant"
      contains: "SubagentEvent"
    - path: "crates/iron_hermes_ui/src/server/ws.rs"
      provides: "Per-turn SubagentProgressCallback construction + run_web_turn argument plumbing + Wave 0 unit test test_subagent_callback_emits_event"
      contains: "SubagentProgressCallback"
    - path: "crates/iron_hermes_ui/src/server/state.rs"
      provides: "run_web_turn signature extended with Option<SubagentProgressCallback>; per-turn injection into DelegateTaskWiring.progress_callback"
      contains: "subagent_progress_callback"
    - path: "crates/iron_hermes_ui/src/components/warp_hermes.rs"
      provides: "subagent_events increment site on SubagentEvent {} arm; is_ws_connected lifecycle toggling on Text/Binary/Close/error"
      contains: "subagent_events.set"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/mod.rs"
      provides: "ChatStreamEvent::SubagentEvent {} match arm at the second parse site"
      contains: "SubagentEvent"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs"
      provides: "POLL_INTERVAL_WS_CONNECTED_MS constant; use_effect over subagent_events that restarts the resource; dynamic interval selection"
      contains: "POLL_INTERVAL_WS_CONNECTED_MS"
  key_links:
    - from: "crates/ironhermes-tools/src/delegate_task.rs"
      to: "crates/iron_hermes_ui/src/server/ws.rs"
      via: "SubagentProgressCallback Arc<dyn Fn(usize, SubagentProgress) + Send + Sync> fires on Started/ToolCall/Completed sites; ws.rs callback forwards each as tx.send(ChatStreamEvent::SubagentEvent {})"
      pattern: "SubagentProgressCallback"
    - from: "crates/iron_hermes_ui/src/server/ws.rs"
      to: "crates/iron_hermes_ui/src/components/warp_hermes.rs"
      via: "Same per-turn tx as ToolProgressCallback; client parse arm at warp_hermes.rs increments subagent_events"
      pattern: "ChatStreamEvent::SubagentEvent"
    - from: "crates/iron_hermes_ui/src/components/warp_hermes.rs"
      to: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs"
      via: "use_context::<Signal<u64>>() for subagent_events + use_context::<Signal<bool>>() for is_ws_connected; ScreenAgents use_effect over subagent_events triggers agents_resource.restart()"
      pattern: "use_effect"
---

<objective>
Plumb `SubagentProgressCallback` from the per-turn ws send half through `AppState::run_web_turn` into `DelegateTaskWiring`, extend `ChatStreamEvent` with a payload-free `SubagentEvent {}` variant, increment a counter on the client via `WarpHermes`, watch the counter in `ScreenAgents` to call `agents_resource.restart()`, and switch the Plan 01 poll loop to a 5 s cadence while ws is connected. Upgrades the Agents page from poll-only (1.5 s lower bound) to push-driven with a 5 s poll safety net — closing the <1.5 s subagent visibility gap from Plan 01.

Purpose: Make subagent state changes appear on the Agents page within <1 s of the underlying registry event, including for short-lived agents (<1.5 s) that Plan 01's poll cannot catch.
Output: One enum variant, one server-side per-turn callback, one signature change, two client increment/lifecycle sites, two client parse arms, one effect watcher, one new constant + interval selector. Two new Wave 0 unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.7.1-agents-page-live-updates/26.7.1-CONTEXT.md
@.planning/phases/26.7.1-agents-page-live-updates/26.7.1-RESEARCH.md
@.planning/phases/26.7.1-agents-page-live-updates/26.7.1-PATTERNS.md
@.planning/phases/26.7.1-agents-page-live-updates/26.7.1-VALIDATION.md
@.planning/phases/26.7.1-agents-page-live-updates/26.7.1-01-PLAN.md
@crates/iron_hermes_ui/CLAUDE.md
@crates/iron_hermes_ui/src/protocol.rs
@crates/iron_hermes_ui/src/server/ws.rs
@crates/iron_hermes_ui/src/server/state.rs
@crates/iron_hermes_ui/src/components/warp_hermes.rs
@crates/iron_hermes_ui/src/components/hermes_app/mod.rs
@crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs
@crates/ironhermes-tools/src/delegate_task.rs
@crates/ironhermes-agent/src/app_runtime_factory.rs

<interfaces>
<!-- Key types and idioms extracted from the codebase. Executor uses these directly. -->

`crates/iron_hermes_ui/src/protocol.rs` (line 19) defines `pub enum ChatStreamEvent { Delta { text: String }, ToolCallStart { name: String, args: String }, ToolCallEnd { name: String, success: bool }, Finished { total_tokens: u32 }, Error { message: String } }` with `#[derive(Serialize, Deserialize, Debug, Clone)]`. Serde produces externally-tagged JSON `{"VariantName": {...}}`. Plan 02 adds `SubagentEvent {}` as a payload-free struct variant producing `{"SubagentEvent": {}}`.

`crates/ironhermes-tools/src/delegate_task.rs:38` defines `pub type SubagentProgressCallback = Arc<dyn Fn(usize, SubagentProgress) + Send + Sync>;`. Note: Arc not Box (differs from ToolProgressCallback). Emission sites are at lines 292-296 (ToolCall), 368-374 (Started), 446-448 (Completed). Synthetic invocation in Wave 0 test uses whichever SubagentProgress variant is simplest to construct.

`crates/ironhermes-agent/src/app_runtime_factory.rs:18-32` defines `pub struct DelegateTaskWiring { ..., pub progress_callback: Option<SubagentProgressCallback>, ... }` — the slot currently set to `None` at `state.rs:124`.

`crates/iron_hermes_ui/src/server/ws.rs:194-217` is the verbatim template — `let tx = ...mpsc::unbounded_channel::<ChatStreamEvent>(); let tx_tool = tx.clone(); let tool_progress_callback: ToolProgressCallback = Box::new(move |name, args| { let _ = tx_tool.send(...); }); ... app_state.run_web_turn(...Some(tool_progress_callback), ...).await;`. Mirror this for SubagentProgressCallback with `Arc::new` not `Box::new`.

`crates/iron_hermes_ui/src/server/state.rs:169-176` defines `pub async fn run_web_turn(&self, session_id: &str, user_input: &str, stream_callback: StreamCallback, tool_progress_callback: Option<ToolProgressCallback>, tool_result_callback: Option<ToolResultCallback>) -> Result<...>`. Plan 02 adds `subagent_progress_callback: Option<ironhermes_tools::delegate_task::SubagentProgressCallback>` as the trailing parameter.

`crates/iron_hermes_ui/src/server/state.rs:124` is the `progress_callback: None` slot inside the `DelegateTaskWiring { ... }` literal — this is in `AppState::init` (per-session). DO NOT fix it here. The per-turn callback lives in `run_web_turn` body and threads in (RESEARCH Risk #1).

`crates/iron_hermes_ui/src/components/warp_hermes.rs:135` is the `dioxus_fullstack::use_websocket(...)` declaration. Plan 01 added `let mut subagent_events: Signal<u64> = use_context_provider(...)` and `let mut is_ws_connected: Signal<bool> = use_context_provider(...)` after it. Plan 02 makes these `mut`-bindings actually written.

`crates/iron_hermes_ui/src/components/warp_hermes.rs` parse site: the `match` on `crate::protocol::ChatStreamEvent` (cited at warp_hermes.rs:210-355 in PATTERNS.md). Add a `SubagentEvent {}` arm that does `let n = subagent_events(); subagent_events.set(n + 1);`. The ws lifecycle handler at the receiver loop (~lines 149-205) is where `is_ws_connected.set(true)` lands on first Text/Binary and `.set(false)` lands on Close/error.

`crates/iron_hermes_ui/src/components/hermes_app/mod.rs:166-225` is the SECOND ChatStreamEvent parse site — has arms for Delta (line 173), ToolCallStart (190), ToolCallEnd (204), Finished (219), Error (223). It MUST also gain a `SubagentEvent {}` arm (even if just `{ /* no-op at this parse site — counter is at WarpHermes */ }`). Otherwise the rustc non-exhaustive-pattern error blocks compile (the existing arms don't use `_`, so any new variant breaks both sites).

`crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` (Plan 01 output): contains `POLL_INTERVAL_BASELINE_MS = 1500`, `HOLD_DURATION = 5s`, the poll-loop `use_future`, and an unincremented `is_ws_connected` consumed via `use_context::<Signal<bool>>()`. Plan 02 adds `POLL_INTERVAL_WS_CONNECTED_MS = 5000` and the `use_effect` over `subagent_events`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: protocol.rs ChatStreamEvent::SubagentEvent {} variant + Wave 0 JSON-shape unit test</name>
  <files>crates/iron_hermes_ui/src/protocol.rs</files>

  <read_first>
    - crates/iron_hermes_ui/src/protocol.rs (entire file — current ChatStreamEvent enum definition at line 18 onward; verify the serde derives, any existing #[cfg(test)] mod tests block, and the current Cargo features under which the file compiles)
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-CONTEXT.md (D-07 — counter-only event payload)
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-VALIDATION.md (Wave 0 test_subagent_event_json_shape spec)
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-RESEARCH.md (Common Op 2 — verbatim variant comment)
  </read_first>

  <behavior>
    - ChatStreamEvent gains a new SubagentEvent {} struct variant. Payload-free per D-07.
    - serde_json::to_string(&ChatStreamEvent::SubagentEvent {}) produces the externally-tagged JSON {"SubagentEvent":{}} exactly — no fields, stable across re-derives.
    - Round-trip: serde_json::from_str::<ChatStreamEvent>(r#"{"SubagentEvent":{}}"#).unwrap() deserializes to ChatStreamEvent::SubagentEvent {} and matches! confirms the variant.
    - Unit test test_subagent_event_json_shape lives in a #[cfg(test)] mod tests block in protocol.rs. Runs under cargo test -p iron_hermes_ui --features web.
  </behavior>

  <action>
    Open `crates/iron_hermes_ui/src/protocol.rs`. After the existing `Error { message: String }` variant (the last variant in the current enum at line ~25), add `SubagentEvent {}` as a new variant. Keep the existing derives unchanged. Add a doc comment above the variant: "Phase 26.7.1 Plan 02 D-07: subagent registry changed (Started/ToolCall/Completed). Payload-free per D-07 — client increments a counter and triggers agents_resource.restart()."

    Add a `#[cfg(test)] mod tests { use super::*; ... }` block (or extend the existing one if present) with `#[test] fn test_subagent_event_json_shape()`. The test:
    1. Construct `let evt = ChatStreamEvent::SubagentEvent {};`
    2. Serialize: `let s = serde_json::to_string(&evt).unwrap();`
    3. Assert literal byte-for-byte stability: `assert_eq!(s, r#"{"SubagentEvent":{}}"#);`
    4. Round-trip: `let parsed: ChatStreamEvent = serde_json::from_str(&s).unwrap();`
    5. Variant assertion: `assert!(matches!(parsed, ChatStreamEvent::SubagentEvent {}));`

    Do NOT touch any other variant. Do NOT change derives. Do NOT add a `#[serde(rename = ...)]` attribute. The stable JSON shape relies on serde-default externally-tagged behavior.

    Read read_first files for current source. NEVER place fenced code blocks inside this action.
  </action>

  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p iron_hermes_ui --features web test_subagent_event_json_shape 2>&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - cargo test -p iron_hermes_ui --features web test_subagent_event_json_shape exits 0.
    - grep -c 'SubagentEvent' crates/iron_hermes_ui/src/protocol.rs ≥ 2 (variant + test).
    - grep -F '{"SubagentEvent":{}}' crates/iron_hermes_ui/src/protocol.rs returns ≥ 1 hit (literal byte stability assertion present).
    - cargo build -p iron_hermes_ui --features server 2>&1 | tail -10 exits 0 — protocol.rs compiles cleanly under server feature.
    - cargo build -p iron_hermes_ui --features web 2>&1 | tail -10 — EXPECTED to fail with rustc non-exhaustive-match errors at the two client parse sites (warp_hermes.rs and hermes_app/mod.rs); those are fixed in Task 3.
    - No fenced code blocks were added inside the planner action text.
  </acceptance_criteria>

  <done>
    Variant added, JSON shape unit-tested and locked. Compile errors at the two parse sites are EXPECTED and addressed in later tasks.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ws.rs SubagentProgressCallback per-turn construction + state.rs run_web_turn signature + Wave 0 callback unit test</name>
  <files>crates/iron_hermes_ui/src/server/ws.rs, crates/iron_hermes_ui/src/server/state.rs, crates/ironhermes-agent/src/app_runtime_factory.rs</files>

  <read_first>
    - crates/iron_hermes_ui/src/server/ws.rs lines 70-310 (chat ws lifecycle handler; especially the ToolProgressCallback construction at 194-210, the tool_result_callback block immediately after, and the run_web_turn call site at ~213-219)
    - crates/iron_hermes_ui/src/server/state.rs lines 99-250 (AppState init at ~99-145 incl. progress_callback: None at line 124; run_web_turn signature at 169-176; the build_agent_loop call at ~182 and the second-overload signature at line ~235)
    - crates/ironhermes-tools/src/delegate_task.rs lines 30-50 (SubagentProgressCallback type alias + SubagentProgress enum variants)
    - crates/ironhermes-agent/src/app_runtime_factory.rs lines 18-40 (DelegateTaskWiring.progress_callback slot — Plan 02 path (b) modifies this struct)
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-RESEARCH.md (Pattern 3 + Pitfall 2 + Risk #1 — per-turn vs per-session lifetime mandate)
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-VALIDATION.md (Wave 0 test_subagent_callback_emits_event spec)
  </read_first>

  <behavior>
    - AppState::run_web_turn accepts a new trailing parameter subagent_progress_callback: Option<ironhermes_tools::delegate_task::SubagentProgressCallback>.
    - Inside run_web_turn, when Some(cb) is supplied, the callback is threaded into DelegateTaskWiring.progress_callback for the duration of the turn — RESEARCH Risk #1 path (b) (Arc<RwLock<Option<SubagentProgressCallback>>> on the wiring with swap-and-restore) is the recommended low-LoC path. Path (a) (per-turn DelegateTaskWiring clone) is acceptable. Planner does not pre-pick; executor picks the lower-LoC path and documents in SUMMARY.
    - ws.rs constructs the callback per turn (inside the per-message block — NOT at AppState init): clones tx, wraps in Arc::new(move |_index, _event| { let _ = tx_clone.send(crate::protocol::ChatStreamEvent::SubagentEvent {}); }). All three SubagentProgress variants — Started, ToolCall, Completed — produce the same SubagentEvent {} frame (D-07 counter-only; underscore-bind the variant). Passes Some(subagent_progress_callback) as the new last argument to app_state.run_web_turn(...).
    - Wave 0 unit test test_subagent_callback_emits_event (under #[cfg(all(test, feature = "server"))], or otherwise server-feature gated): creates let (tx, mut rx) = tokio::sync::mpsc::unbounded_channel::<ChatStreamEvent>();, builds the callback by hand mirroring the ws.rs construction, invokes cb(0, SubagentProgress::Started { ... }) (or whichever variant is cheapest to construct), then assert!(matches!(rx.try_recv(), Ok(ChatStreamEvent::SubagentEvent {})));. The test confirms the counter-only contract and that the callback type matches the Arc<dyn Fn(usize, SubagentProgress) + Send + Sync> shape.
  </behavior>

  <action>
    Step A — crates/iron_hermes_ui/src/server/state.rs:
    1. Add the import (or qualified path) for `ironhermes_tools::delegate_task::SubagentProgressCallback` near the existing `ToolProgressCallback` import at lines 8-14.
    2. Add `subagent_progress_callback: Option<ironhermes_tools::delegate_task::SubagentProgressCallback>` as the trailing parameter to `pub async fn run_web_turn(...)` at line 169-176. Parameter order: stream_callback, tool_progress_callback, tool_result_callback, subagent_progress_callback. Mirror this trailing-parameter pattern on any internal overload at line ~235 if it forwards the args.
    3. Inside run_web_turn body, BEFORE build_agent_loop is called, thread the callback into DelegateTaskWiring.progress_callback. Choose RESEARCH Risk #1 path (a) or (b):
       - (b) recommended low-LoC: Change `DelegateTaskWiring.progress_callback` (defined in `crates/ironhermes-agent/src/app_runtime_factory.rs:18-32`) from `Option<SubagentProgressCallback>` to `Arc<RwLock<Option<SubagentProgressCallback>>>` (use `std::sync::RwLock` since the lock is held only briefly across sync boundaries; if a tokio RwLock is required because the lock is held across await, document the choice). Update `state.rs:124` `progress_callback: None` → `progress_callback: Arc::new(RwLock::new(None))`. In run_web_turn, on entry: snapshot prev via `let prev = wiring.progress_callback.write().unwrap().take();` then `*wiring.progress_callback.write().unwrap() = subagent_progress_callback.clone();`. On turn-end (success OR error path), restore via `*wiring.progress_callback.write().unwrap() = prev;` — use a RAII guard struct (Drop impl) to make the restore exception-safe.
       - (a) per-turn clone: Clone DelegateTaskWiring per turn with the callback set; rebuild the relevant slice of the tool registry. Higher LoC, cleaner architecturally.
    4. Document the chosen path in the SUMMARY for the plan. If path (b) was chosen, add a comment `// Phase 26.7.1 Plan 02 Risk #1 path (b): per-turn swap with RAII restore` near the swap site.
    5. CRITICAL: do NOT touch state.rs:124's init-time `progress_callback` semantics (it remains None-equivalent — at init time there is no per-turn tx). The fix is the per-turn swap inside run_web_turn, NOT a one-time fix at init.

    Step B — crates/iron_hermes_ui/src/server/ws.rs (mirroring lines 194-210):
    1. Immediately after the existing `tool_result_callback` block (which follows `tool_progress_callback` at 194-210), add a parallel block:
       - `let tx_subagent = tx.clone();`
       - `let subagent_progress_callback: ironhermes_tools::delegate_task::SubagentProgressCallback = std::sync::Arc::new(move |_index: usize, _event: ironhermes_tools::delegate_task::SubagentProgress| { let _ = tx_subagent.send(crate::protocol::ChatStreamEvent::SubagentEvent {}); });`
       - Note `std::sync::Arc::new`, NOT `Box::new` — type contract from delegate_task.rs:38.
    2. Update the `app_state.run_web_turn(...).await` call at ~line 213-219: add `Some(subagent_progress_callback)` as the new trailing argument.
    3. NEVER hoist this construction to AppState::init or module scope — RESEARCH Pitfall 2 + anti-pattern. The tx channel is per-turn.

    Step C — Wave 0 unit test test_subagent_callback_emits_event:
    Add a `#[cfg(all(test, feature = "server"))] mod tests { ... }` block in ws.rs (or extend the existing test module if one exists). Use `tokio::sync::mpsc::unbounded_channel::<crate::protocol::ChatStreamEvent>()` to create a tx/rx pair. Build the callback by hand using the exact construction from Step B. Invoke it with the cheapest-to-construct SubagentProgress variant (Started with the minimum required fields per delegate_task.rs:27-47). Assert `matches!(rx.try_recv(), Ok(ChatStreamEvent::SubagentEvent {}))`. Use `#[tokio::test]` if the test needs an async runtime; otherwise construction is sync. If a test attribute requires async runtime but the test body is sync, prefer a plain `#[test]` and use `rx.try_recv()` (non-blocking).

    Step D — crates/ironhermes-agent/src/app_runtime_factory.rs:
    Only if path (b) was selected — update `DelegateTaskWiring.progress_callback` field type to `Arc<RwLock<Option<SubagentProgressCallback>>>`. Add a Default impl or constructor that returns the wrapped-None. Update all existing call sites that read or write `progress_callback`. Search the workspace: `rg 'progress_callback' --type rust` to confirm all call sites are migrated. Likely sites: app_runtime_factory.rs itself, state.rs:124, and any consumer that today reads `if let Some(cb) = &wiring.progress_callback`.

    Read read_first files for current source. NEVER place fenced code blocks inside this action.
  </action>

  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p iron_hermes_ui --features server test_subagent_callback_emits_event 2>&amp;1 | tail -30 &amp;&amp; cargo build --workspace 2>&amp;1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - cargo test -p iron_hermes_ui --features server test_subagent_callback_emits_event exits 0.
    - cargo build --workspace exits 0 — full workspace builds after path (b) ripples through all DelegateTaskWiring consumers.
    - cargo build -p iron_hermes_ui --features server exits 0 (server-side wiring compiles).
    - grep -c 'SubagentProgressCallback' crates/iron_hermes_ui/src/server/ws.rs ≥ 2 (type + construction).
    - grep -c 'subagent_progress_callback' crates/iron_hermes_ui/src/server/state.rs ≥ 2 (parameter + injection site).
    - grep -c 'ChatStreamEvent::SubagentEvent' crates/iron_hermes_ui/src/server/ws.rs ≥ 1 (the tx.send(...) call inside the Arc closure).
    - grep -c 'std::sync::Arc::new' crates/iron_hermes_ui/src/server/ws.rs ≥ 1 (NOT Box::new — the type contract differs from ToolProgressCallback).
    - state.rs:124 progress_callback slot still equates to None at AppState::init (path (b) wraps in RwLock<None>; path (a) leaves it None) — verify by reading the file after change: no eager Some(callback) at init time.
    - cargo clippy -p iron_hermes_ui --features server -- -D warnings 2>&1 | tail -10 exits 0 for new code.
    - No fenced code blocks were added inside the planner action text.
  </acceptance_criteria>

  <done>
    Per-turn SubagentProgressCallback constructed in ws.rs, threaded through run_web_turn into DelegateTaskWiring, restored after turn. Wave 0 unit test confirms the callback shape and emits SubagentEvent {} on rx. Workspace builds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Client parse arms + WarpHermes counter increment + is_ws_connected lifecycle + ScreenAgents use_effect watcher + dynamic poll cadence</name>
  <files>crates/iron_hermes_ui/src/components/warp_hermes.rs, crates/iron_hermes_ui/src/components/hermes_app/mod.rs, crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs</files>

  <read_first>
    - crates/iron_hermes_ui/src/components/warp_hermes.rs lines 100-360 (ws receiver loop with Text/Binary/Close arms; the ChatStreamEvent match block; the Plan 01-added subagent_events + is_ws_connected use_context_provider declarations near line 135-140)
    - crates/iron_hermes_ui/src/components/hermes_app/mod.rs lines 160-230 (the SECOND ChatStreamEvent parse site with arms for Delta/ToolCallStart/ToolCallEnd/Finished/Error — RESEARCH Open Q 4 + PATTERNS.md Section warp_hermes.rs)
    - crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs (Plan 01 output — verify POLL_INTERVAL_BASELINE_MS, HOLD_DURATION, the poll-loop use_future, and the consumed-but-unused is_ws_connected via use_context::<Signal<bool>>())
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-CONTEXT.md (D-06, D-07, D-08)
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-RESEARCH.md (Pattern 4 use_effect cross-signal reactivity; Pitfall 3 is_ws_connected initial value race; Sequencing Note item 4 — POLL_INTERVAL_WS_CONNECTED_MS naming)
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-PATTERNS.md (Section warp_hermes.rs — both parse sites identified)
  </read_first>

  <behavior>
    - warp_hermes.rs match block on ChatStreamEvent gains a SubagentEvent {} arm that reads `let n = subagent_events();` (Copy u64, no borrow held) then `subagent_events.set(n + 1);`.
    - hermes_app/mod.rs second parse site gains a SubagentEvent {} arm — minimal body. The arm exists primarily to satisfy rustc non-exhaustive match.
    - warp_hermes.rs ws lifecycle: on first successful Text or Binary message (the arms around lines 174-186 per RESEARCH), is_ws_connected.set(true) fires (idempotent — repeated set(true) is fine). On the Message::Close arm (~line 188), on ws.is_err() (~line 150), on recv error (~line 372), is_ws_connected.set(false) fires. Initial value remains false from Plan 01 (Pitfall 3 — assume disconnected → fast poll = safe default).
    - agents.rs adds the constant const POLL_INTERVAL_WS_CONNECTED_MS: u64 = 5000; at module top alongside the existing POLL_INTERVAL_BASELINE_MS. The poll-loop interval selector becomes `let interval_ms = if connected { POLL_INTERVAL_WS_CONNECTED_MS } else { POLL_INTERVAL_BASELINE_MS };` (Plan 01 was unconditional POLL_INTERVAL_BASELINE_MS).
    - agents.rs adds a `use_effect(move || { let _ = subagent_events(); if !is_busy() { agents_resource.restart(); } });` (RESEARCH Pattern 4 + Sequencing Note item 3 — must read the signal inside the closure for Dioxus reactivity to subscribe; respect Plan 01 busy_set to avoid racing kill RPCs). Effect placed alongside the Plan 01 poll-loop use_future declaration.
    - End-to-end UAT (Task 4): submitting a delegate_task triggers delegate_task.rs:368 Started event → ws.rs Arc closure fires → tx sends SubagentEvent {} → warp_hermes.rs arm increments subagent_events → ScreenAgents use_effect → agents_resource.restart() → fresh GET /api/agents/list → diff_terminations adds new card. Round-trip <1 s in normal conditions.
  </behavior>

  <action>
    Step A — crates/iron_hermes_ui/src/components/warp_hermes.rs (Plan 01 context slots already present from Plan 01 Task 2):
    1. Locate the existing match block on `crate::protocol::ChatStreamEvent` cited in PATTERNS.md at warp_hermes.rs:210-355. Add a new arm after `Error { message }`:
       - Pattern: `crate::protocol::ChatStreamEvent::SubagentEvent {}` → body: `let n = subagent_events(); subagent_events.set(n + 1);`
       - subagent_events is already in scope via the Plan 01 `let mut subagent_events = use_context_provider(...)` declaration. If Plan 01 made it non-mut (warning fix), revert to `mut`.
    2. Locate the ws receiver loop arms (~lines 149-205 per RESEARCH). On the first arm that handles a successful Text or Binary message (RESEARCH cites lines 174-187), insert `is_ws_connected.set(true);` BEFORE any further processing of the message. (Idempotent set — every received message asserts connected.)
    3. On the `Message::Close` arm (~line 188), insert `is_ws_connected.set(false);`.
    4. On the `ws.is_err()` guard at the top of the loop (~line 150), insert `is_ws_connected.set(false);` before the continue/break.
    5. On any recv error arm (~line 372 per RESEARCH), insert `is_ws_connected.set(false);` so disconnect is detected via all three signals.
    6. If Plan 01 left is_ws_connected non-mut, revert to mut.

    Step B — crates/iron_hermes_ui/src/components/hermes_app/mod.rs:
    Locate the match block at lines 166-225 (arms for Delta/ToolCallStart/ToolCallEnd/Finished/Error). Add a new arm: `crate::protocol::ChatStreamEvent::SubagentEvent {} => { /* Counter is owned by WarpHermes; this parse site is for chat-stream rendering and does not need to react. */ }`. Body intentionally empty — rustc exhaustiveness is the only reason this arm exists at this site.

    Step C — crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs:
    1. Add module-top constant: `const POLL_INTERVAL_WS_CONNECTED_MS: u64 = 5000;` placed adjacent to the existing POLL_INTERVAL_BASELINE_MS.
    2. Locate the Plan 01 poll-loop use_future. Replace the `let interval_ms = POLL_INTERVAL_BASELINE_MS;` line (or however Plan 01 spelled it) with `let interval_ms: u64 = if connected { POLL_INTERVAL_WS_CONNECTED_MS } else { POLL_INTERVAL_BASELINE_MS };`. The `connected` Copy bool comes from `let connected = is_ws_connected();` already added in Plan 01 (signal-borrow discipline preserved).
    3. Add a use_effect watcher: `use_effect(move || { let _ = subagent_events(); let busy = { let r = busy_set.read(); !r.is_empty() }; if !busy { agents_resource.restart(); } });`. Place it AFTER the poll-loop use_future declaration and BEFORE the rsx! block. `subagent_events` is consumed via `use_context::<Signal<u64>>()` — add the line `let subagent_events = use_context::<Signal<u64>>();` if Plan 01 did not already declare it (Plan 01 declared is_ws_connected consumption; this task adds the parallel subagent_events consumption).
    4. RESEARCH Pattern 4 + anti-pattern guard: the effect's closure MUST read subagent_events() (call-syntax). Without the read, Dioxus does NOT subscribe and the effect runs only at mount. Do NOT call subagent_events.read() inside the rsx! body — that would re-render the entire grid on every event (RESEARCH anti-pattern).
    5. The poll-loop's busy-set check from Plan 01 (D-05) MUST also gate the effect-triggered restart. RESEARCH Sequencing Note item 3: the use_effect body should materialize the busy bool first (block-scoped read) and only call .restart() when not busy. Same signal-borrow discipline as the poll loop.

    Read read_first files for current source. NEVER place fenced code blocks inside this action.
  </action>

  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p iron_hermes_ui --features web 2>&amp;1 | tail -15 &amp;&amp; cargo build -p iron_hermes_ui --target wasm32-unknown-unknown --features web 2>&amp;1 | tail -10 &amp;&amp; cargo clippy -p iron_hermes_ui --features web -- -D warnings 2>&amp;1 | tail -10 &amp;&amp; cargo test -p iron_hermes_ui --features web 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - cargo build -p iron_hermes_ui --features web exits 0 (both client parse sites match-arm the new variant).
    - cargo build -p iron_hermes_ui --target wasm32-unknown-unknown --features web exits 0.
    - cargo clippy -p iron_hermes_ui --features web -- -D warnings exits 0 for files touched (signal-borrow discipline preserved).
    - cargo test -p iron_hermes_ui --features web exits 0 (Plan 01 + Task 1 + Task 2 tests still pass).
    - grep -c 'ChatStreamEvent::SubagentEvent' crates/iron_hermes_ui/src/components/warp_hermes.rs ≥ 1 (parse arm).
    - grep -c 'ChatStreamEvent::SubagentEvent' crates/iron_hermes_ui/src/components/hermes_app/mod.rs ≥ 1 (second parse arm — RESEARCH Open Q 4 requirement).
    - grep -c 'subagent_events.set' crates/iron_hermes_ui/src/components/warp_hermes.rs ≥ 1 (counter write).
    - grep -c 'is_ws_connected.set' crates/iron_hermes_ui/src/components/warp_hermes.rs ≥ 2 (at least one true and one false — Pitfall 3 lifecycle).
    - grep -c 'POLL_INTERVAL_WS_CONNECTED_MS' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs ≥ 1 (constant added).
    - grep -c 'use_effect' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs ≥ 1 (effect watcher).
    - grep -c 'subagent_events' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs ≥ 1 (consumption via use_context).
    - No fenced code blocks were added inside the planner action text.
  </acceptance_criteria>

  <done>
    Both client parse sites match-arm SubagentEvent {}. WarpHermes increments subagent_events on event arrival and toggles is_ws_connected on ws lifecycle. ScreenAgents use_effect triggers agents_resource.restart() per increment. Poll loop widens to 5 s while connected. Workspace + wasm32 builds green, clippy clean for new code.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Plan 02 UAT — live subagent visibility under 1s + dynamic cadence + ws-disconnect fallback</name>
  <files>n/a (manual verification against dx serve)</files>

  <read_first>
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-VALIDATION.md (Manual-Only Verifications — scenarios for D-06/D-07/D-08 + 26.7-06 regression)
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-CONTEXT.md ("Closes with" section — four scenarios)
    - .planning/phases/26.7.1-agents-page-live-updates/26.7.1-RESEARCH.md (Risk #2 — Plan 02 closes Plan 01's <1.5s blind spot)
  </read_first>

  <what-built>
    Per-turn SubagentProgressCallback construction in ws.rs. ChatStreamEvent::SubagentEvent {} variant + JSON shape lock. AppState::run_web_turn extended with Option<SubagentProgressCallback>. DelegateTaskWiring.progress_callback wrapped in Arc<RwLock<Option<...>>> for per-turn swap (or per-turn clone, per executor's choice). Client increments subagent_events on each event. is_ws_connected toggles on ws Text/Binary/Close/error. ScreenAgents use_effect over subagent_events triggers agents_resource.restart(). Poll loop widens to 5s while ws connected, tightens to 1.5s on disconnect.
  </what-built>

  <how-to-verify>
    Spin up `cd crates/iron_hermes_ui && RUSTC_WRAPPER= dx serve`. Open DevTools → Network + Console. Run these scenarios:

    1. **Live spawn <1s after delegate_task (D-06 + D-07):** Send a chat message that triggers delegate_task. Within ≤1 s of seeing the `delegate_task` tool call in the chat block, the Agents page must show the new RUNNING card. (Plan 01 alone could not guarantee <1.5s; Plan 02's event path is what makes this scenario pass.)

    2. **Very-short-lived agent (<1.5s) — Risk #2 closure:** Construct a delegate_task that finishes in well under 1500 ms (e.g., simple read_file in a small bounded scope). Expect: the Agents page DOES show a brief RUNNING-then-ENDED card flicker (or at minimum lands in recently_terminated via the event-driven fetch). Plan 01 alone would have missed this entirely.

    3. **Dynamic cadence widen (D-08):** While ws is connected (verify via DevTools Network → WS tab showing live frames), filter Network for `/api/agents/list`. Expect ~5 s ±500 ms between fetches (POLL_INTERVAL_WS_CONNECTED_MS=5000).

    4. **Dynamic cadence tighten on disconnect (D-08):** Close the ws (DevTools → Network → WS → right-click → close, OR navigate away from chat and back to force a reconnect failure). Within ~5 s of the close, the next `/api/agents/list` poll should fire (still at the old cadence) but subsequent polls should occur every ~1500 ms (POLL_INTERVAL_BASELINE_MS). Confirm in DevTools timeline.

    5. **Counter consistency (D-07):** Spam multiple delegate_task calls in quick succession. Expect: subagent_events counter increments on each registry transition (Started/ToolCall/Completed alike). Verify behaviorally: each subagent transition triggers a fresh `/api/agents/list` request visible in Network.

    6. **HOLD-N continues to work (Plan 01 carryforward):** Same as Plan 01 Task 3 scenarios 2-4. Natural-terminate, kill, and interrupt all produce ENDED cards that fade after 5 s.

    7. **Tab-hidden + busy-set guards remain effective:** Same as Plan 01 Task 3 scenarios 5-6. The Plan 02 use_effect must ALSO respect busy_set per Step C.5 of Task 3 — verify no `/api/agents/list` fetch fires between a KILL POST and its 200 response, even if a SubagentEvent {} arrives during that window.

    8. **PRUNE clears both (D-12 carryforward):** Same as Plan 01 Task 3 scenario 7.

    9. **26.7-06 regression:** All four `/api/agents/*` endpoints return 200 throughout. Server stderr contains no `panicked at crates/ironhermes-agent/src/subagent_registry.rs:86`.

    10. **No client-side runtime panic:** DevTools Console shows zero red errors. Specifically no "borrow already taken" (Pitfall 1), no "channel closed" panics from the per-turn callback (Risk #1 — would indicate the callback fired AFTER turn-end with a freed tx).

    If any of scenarios 1-5 fails, the root cause is likely Risk #1 (per-turn lifetime mismatch). Verify in the SUMMARY which path (a or b) was implemented and re-test.
  </how-to-verify>

  <action>
    Run the UAT scenarios listed in the how-to-verify block above against `cd crates/iron_hermes_ui && RUSTC_WRAPPER= dx serve`. This task does not modify code; it executes the manual checks and reports back. Measure scenario 1 round-trip latency with a stopwatch or DevTools Performance flame; record the concrete number in the SUMMARY. If Risk #1 fires (channel-closed panic or events on wrong turn), do NOT auto-revise — surface the failure on the resume-signal so the planner can revisit path (a) vs (b).
  </action>

  <verify>
    <human-check>UAT scenarios 1-10 above all pass. Approval recorded on the resume-signal.</human-check>
    <automated>echo "UAT checkpoint — see resume-signal"</automated>
  </verify>

  <done>
    User has typed "approved" (or scenario-specific approval) on the resume-signal. All 10 UAT scenarios verified, scenario 1 round-trip latency recorded in SUMMARY.
  </done>

  <resume-signal>Type "approved" if scenarios 1-10 all pass, or describe failures with DevTools captures preferred. If Risk #1 fired (channel-closed panic OR events arriving on wrong turn), say so explicitly so the executor can revisit path (a) vs (b).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ frontend server (chat ws) | New `ChatStreamEvent::SubagentEvent {}` JSON variant crosses this boundary. Same ws connection as 26.7's Delta/Finished/Error — auth/session unchanged. |
| Subagent registry ↔ ws send half (server-side) | New SubagentProgressCallback closure crosses from per-turn `tokio::spawn` body into delegate_task fire sites. Captures `tx: UnboundedSender<ChatStreamEvent>`. |
| Tab ↔ Tab (same origin) | Each tab's subagent_events counter + is_ws_connected signal is per-tab. No cross-tab contamination. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.7.1-02-V5-shape | Input Validation (V5) | ChatStreamEvent JSON parse on client (warp_hermes.rs + hermes_app/mod.rs) | mitigate | serde-default externally-tagged JSON. `serde_json::from_str` returns `Result`; existing client code at warp_hermes.rs:210-214 already skips malformed messages via `continue`. New variant inherits this. Wave 0 `test_subagent_event_json_shape` locks the byte-shape against silent serde-attribute drift. |
| T-26.7.1-02-Tampering-spoof | Tampering | Crafted SubagentEvent {} frames injected via ws (theoretical — same-origin chat ws is server-authoritative) | accept | Counter-only payload (D-07) — there is nothing to spoof. Every event triggers a server-fn round-trip via agents_resource.restart() which fetches authoritative state from /api/agents/list. Spoofing the event causes at worst a wasted GET (no privilege escalation, no state corruption). RESEARCH Security Domain confirms. |
| T-26.7.1-02-DoS-flood | Denial of Service | Runaway subagent flooding tx with SubagentEvent {} | accept | tx is UnboundedSender — same backpressure characteristic as the existing Delta stream. CONTEXT "deferred" lists "Telemetry on ws event drops" as future work. For this phase, unbounded matches existing pattern. Acceptable per RESEARCH. |
| T-26.7.1-02-Race-callback | Reliability / Tampering | Per-turn callback firing AFTER turn-end (Risk #1) — tx half closed, send returns Err | mitigate | RAII guard struct restores `wiring.progress_callback` to its prior value at turn-end (or per-turn clone discards). Per-turn lifetime contract enforced by the swap-and-restore discipline in run_web_turn body. Wave 0 unit test `test_subagent_callback_emits_event` exercises the exact construction; future regression test could exercise the swap. `tx.send(...)` returns `Result<(), SendError>` which is discarded via `let _ = ...` — a closed tx logs nothing visible (acceptable; the next turn rebuilds). |
| T-26.7.1-02-Race-borrow | Reliability | Signal borrow across .await in use_effect + poll loop | mitigate | use_effect body materializes Copy bools (subagent_events()) and block-scoped reads for busy_set before any state mutation. No await in use_effect. Poll loop discipline carries from Plan 01. clippy.toml + `cargo clippy -- -D warnings` is the gate. |
| T-26.7.1-02-26.7-06-regr | Reliability | Existing /api/agents/{kill, interrupt, prune, list} endpoints | accept | 26.7-06 spawn_blocking wraps and 26.7-07 RegistrationGuard::drop fix are not modified. Task 4 scenarios 9-10 are the regression assertion. |
| T-26.7.1-02-PV-leak | Resource Leak | Plan 01's Page Visibility listener (carryforward) | accept | Mitigated in Plan 01 Task 2 via use_drop + Signal<Option<Closure<_>>>. Plan 02 does not touch this surface. |
</threat_model>

<verification>
- Task 1 unit test `test_subagent_event_json_shape` verifies D-07 counter-only contract + byte-stable JSON.
- Task 2 unit test `test_subagent_callback_emits_event` verifies D-06 callback shape + emission. Workspace build verifies path (b) ripple-fixes.
- Task 3 static greps verify D-06 (counter increment site), D-07 (both parse arms — solving RESEARCH Open Q 4), D-08 (POLL_INTERVAL_WS_CONNECTED_MS + use_effect).
- Task 3 clippy + wasm32 + workspace builds confirm signal-borrow discipline preserved and the Plan 02 changes compile across all platforms.
- Task 4 UAT scenarios 1-10 verify the end-to-end live update flow, dynamic cadence, ws-disconnect fallback, 26.7-06/07 regression, and Risk #1 closure.
</verification>

<success_criteria>
- D-06, D-07, D-08 demonstrably satisfied (unit + static + UAT).
- Both ChatStreamEvent parse sites match-arm SubagentEvent {} (warp_hermes.rs + hermes_app/mod.rs).
- Per-turn SubagentProgressCallback construction does NOT leak across turns (Risk #1 closed by swap-and-restore or per-turn clone).
- cargo build --workspace, cargo build -p iron_hermes_ui --target wasm32-unknown-unknown --features web, cargo clippy -p iron_hermes_ui --features web -- -D warnings, and cargo test --workspace all exit 0.
- UAT scenarios 1-10 pass; the user approves with "approved".
</success_criteria>

<output>
Create `.planning/phases/26.7.1-agents-page-live-updates/26.7.1-02-SUMMARY.md` when done. Include:
- Risk #1 path chosen: (a) per-turn clone OR (b) Arc<RwLock<...>> swap. Justify the choice.
- Whether the RAII guard or explicit prev-restore path was used. Cite the file:line of the swap site.
- Any DelegateTaskWiring API changes that rippled into other crates — list every call-site touched.
- UAT round-trip latency observed for scenario 1 (live-spawn <1s) — concrete measurement, not "felt fast".
- Confirmation that scenario 2 (Risk #2 closure) succeeded — i.e., a <1.5s subagent did appear briefly on the Agents page.
- Whether path (b)'s std::sync::RwLock was sufficient or tokio::sync::RwLock was required (document if a sync lock was held across an await and required upgrade).
</output>
