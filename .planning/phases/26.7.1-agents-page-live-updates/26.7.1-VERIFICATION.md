---
phase: 26.7.1-agents-page-live-updates
verified: 2026-05-19T17:00:00Z
status: passed
score: 16/16 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 26.7.1: Agents page live updates — Verification Report

**Phase Goal (ROADMAP):** Make the web UI Agents screen reflect live `SubagentRegistry` state in real time and keep terminated agents on-screen for N=5 s before removal. Three deliverables: (a) periodic-poll baseline (~1.5 s, paused during kill/interrupt RPCs), (b) recently-terminated HOLD-N fade for all terminations (natural, killed, interrupted), (c) event-driven push upgrade via the existing chat ws so live-spawn latency drops to <1 s, with dynamic poll cadence (5 s while ws up, 1.5 s while down).

**Verified:** 2026-05-19
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | ScreenAgents polls `/api/agents/list` every ~1500 ms while visible and idle | VERIFIED | `agents.rs:194-237` — poll `use_future` calls `agents_resource_for_poll.restart()` every iteration; `interval_ms = if *ws_connected_ctx.read() { 5_000 } else { 1_500 }`; literal `1_500` present at line 234 |
| 2  | Poll halts when `document.hidden()` returns true and resumes on visibility return | VERIFIED | `agents.rs:196-200, 218-221` — `web_sys::window().and_then(\|w\| w.document()).map(\|d\| d.hidden())`; if hidden, `TimeoutFuture::new(500).await; continue;` |
| 3  | Poll halts while any per-card kill/interrupt RPC is in flight (`rpc_in_flight > 0`) | VERIFIED | `agents.rs:204-224` — `let in_flight: u32 = *rpc_in_flight.read(); if in_flight > 0 { TimeoutFuture::new(200).await; continue; }`; increments at lines 445-446, 479-480; decrements at 460-461, 497-498 |
| 4  | Agents that terminate (natural, killed, interrupted) appear as ENDED card for 5 s before removal | VERIFIED | `agents_diff.rs:28-38` pure fn returns ids in prev but absent from next (D-02 uniform); `agents.rs:121-138` diff call site inserts into `recently_terminated`; `agents.rs:245-265` 1-second decay sweep removes entries with `ts.elapsed() >= Duration::from_secs(5)`. 4/4 unit tests passing including D-13 invariant. HOLD-N for natural terminations confirmed by UAT (note 1 below). |
| 5  | ENDED card body opacity 0.55, card-footer hidden via CSS | VERIFIED | `screens.css:124-132` — `.card.is-ended { opacity: 0.55; transition: opacity 200ms ease-out; pointer-events: none; }` and `.card.is-ended .card-footer { display: none; }` |
| 6  | An agent id that reappears in the live list while present in `recently_terminated` is dropped immediately (D-13) | VERIFIED | `agents.rs:99-107` — pre-diff loop iterates `agents_list`, removes from `recently_terminated` if `in_map`. Plus pure-fn unit test `test_diff_terminations_rerunning_id_is_not_a_termination` in `agents_diff.rs:103-127` — PASSES |
| 7  | PRUNE ENDED click clears `recently_terminated` synchronously in addition to calling `api_agents_prune` | VERIFIED | `agents.rs:299-310` — `recently_terminated.write().clear();` BEFORE `spawn(async move { let _ = api_agents_prune(None).await; agents_resource.restart(); })` |
| 8  | HermesApp provides `subagent_events: Signal<u64>` and `is_ws_connected: Signal<bool>` via use_context_provider | VERIFIED | `hermes_app/mod.rs:131-132` (signal declarations) and `mod.rs:330-331` (`use_context_provider(\|\| subagent_events);` and `use_context_provider(\|\| is_ws_connected);`). NOT in legacy-shell `warp_hermes.rs` — correctly targets active root. |
| 9  | Server-render + dx serve load the Agents page without "Could not find context" panic | VERIFIED | Context providers in active root `HermesApp` (mod.rs:330-331); consumers `use_context::<Signal<bool>>()` (agents.rs:88) and `use_context::<Signal<u64>>()` (agents.rs:89) resolve at SSR. Confirmed by Plan 01 UAT-2 (no panic). |
| 10 | `ChatStreamEvent::SubagentEvent {}` payload-free variant round-trips as `{"SubagentEvent":{}}` via serde_json | VERIFIED | `protocol.rs:38` — variant declared with no `#[serde(...)]` attributes; `test_subagent_event_json_shape` (protocol.rs:48-59) asserts exact JSON shape and round-trip — PASSES (`cargo test -p iron_hermes_ui --features web test_subagent_event_json_shape` → 1 passed) |
| 11 | Server `SubagentProgress` events emit `ChatStreamEvent::SubagentEvent {}` on the per-turn ws send channel | VERIFIED | `state.rs:127-146` — `subagent_callback_slot` constructed; `progress_callback` Arc closure uses `try_lock` to peek slot and send `SubagentEvent {}` via `tx.send(...)`; wired as `progress_callback: Some(progress_callback)` at `DelegateTaskWiring` (state.rs:161). Test `test_subagent_callback_emits_event` (ws.rs:401) PASSES under `--features web,server` (1 passed). UAT confirmed 3 rapid `/api/agents/list` calls at spawn time. |
| 12 | HermesApp recv loop increments `subagent_events` on each SubagentEvent and ScreenAgents `use_effect` calls `agents_resource.restart()` | VERIFIED | `mod.rs:244-249` — match arm reads cur into Copy u64 then `subagent_events.set(cur + 1);`. `agents.rs:272-276` — `use_effect(move \|\| { let _ = subagent_events(); agents_resource_for_effect.restart(); });`. Live-spawn UAT showed sub-second update latency. |
| 13 | HermesApp sets `is_ws_connected.set(true)` on connect success and `.set(false)` on Close/Err/is_err arms | VERIFIED | `mod.rs:170` (is_err → false), `mod.rs:174` (connect success → true), `mod.rs:254` (Close → false), `mod.rs:258` (Err → false) — 1 set(true) + 3 set(false), matches D-08 contract |
| 14 | ScreenAgents poll cadence widens to 5000 ms while ws connected, tightens to 1500 ms while disconnected | VERIFIED | `agents.rs:233-237` — `let interval_ms: u32 = if *ws_connected_ctx.read() { 5_000 } else { 1_500 }; TimeoutFuture::new(interval_ms).await;` |
| 15 | AppState carries per-turn `UnboundedSender<ChatStreamEvent>` slot cleared via RAII guard so panics never leave a dangling sender | VERIFIED | `ws.rs:22-45` — `SubagentCallbackSlotGuard` struct with `Drop` impl using `try_lock` to clear slot (gated `#[cfg(feature = "server")]`). `ws.rs:241-263` — slot installed via `lock().await` BEFORE `run_web_turn`; `_slot_guard` binding ensures Drop runs at end-of-spawn-block on both success and panic paths |
| 16 | Plan 01 regressions remain green: HOLD-N=5s, PRUNE clears, kill/interrupt/prune still return 200, no `subagent_registry.rs:86` panic | VERIFIED | Plan 02 SUMMARY confirms 6 commits with `cargo test -p iron_hermes_ui --features web` 45 passing and `--features web,server` 54 passing. UAT-4 confirmed `web_time::Instant` wasm32 fix landed. `agents_diff` 4/4 still pass after Plan 02 wiring. PRUNE clear (truth #7) and dynamic cadence (truth #14) reuse same diff/decay pipeline. Kill HOLD un-UAT-able only because of UPSTREAM `shrike.kill` bug (Phase 32.3, pre-existing, documented separately) — frontend machinery verified by unit tests + code reading. |

**Score:** 16/16 truths verified

---

### Required Artifacts

| # | Artifact | Expected | Status | Details |
|---|----------|----------|--------|---------|
| A1 | `crates/iron_hermes_ui/src/components/hermes_app/mod.rs` | `subagent_events` + `is_ws_connected` signal declarations and `use_context_provider` calls | VERIFIED | `let mut subagent_events = use_signal(\|\| 0u64);` at line 131; `let mut is_ws_connected = use_signal(\|\| false);` at 132; `use_context_provider(\|\| subagent_events);` at 330; `use_context_provider(\|\| is_ws_connected);` at 331 |
| A2 | `crates/iron_hermes_ui/src/components/hermes_app/mod.rs` | `ChatStreamEvent::SubagentEvent` match arm + `is_ws_connected.set` lifecycle calls | VERIFIED | SubagentEvent arm at 244-249; `is_ws_connected.set(true)` at 174; `is_ws_connected.set(false)` at 170, 254, 258 |
| A3 | `crates/iron_hermes_ui/src/components/hermes_app/screens/agents_diff.rs` | Pure `diff_terminations` function + unit tests | VERIFIED | `pub fn diff_terminations(live_prev, live_next) -> Vec<AgentInfo>` at line 28; 4 unit tests including `test_diff_terminations_rerunning_id_is_not_a_termination` (D-13 guard) — ALL 4 PASS |
| A4 | `crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` | Poll loop + recently_terminated + decay sweep + AgentCard is-ended rendering | VERIFIED | `recently_terminated` HashMap signal at line 75-76 (`web_time::Instant` per UAT-4 hotfix); poll `use_future` at 192-238; decay `use_future` at 243-265; AgentCard with `is_ended: bool` + `rpc_in_flight: Signal<u32>` at 373-374; `class: if is_ended { "is-ended" }` at 399; `if is_ended { "pill ended" } else { "pill green" }` and ENDED label at 415-416 |
| A5 | `crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` | `document.hidden()` per-iteration check (D-04) | VERIFIED | `agents.rs:196-200, 218-221` — hidden check + 500ms backoff loop |
| A6 | `crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` | `rpc_in_flight` counter consulted by poll loop (D-05) | VERIFIED | `let mut rpc_in_flight = use_signal(\|\| 0u32);` at line 83; consulted at 204-208; incremented/decremented around KILL+INTERRUPT closures at 445-498 |
| A7 | `crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` | PRUNE click clears `recently_terminated` synchronously (D-12) | VERIFIED | `recently_terminated.write().clear();` at line 304 BEFORE the async server prune spawn at 305 |
| A8 | `crates/iron_hermes_ui/assets/screens.css` | `.card.is-ended` modifier (opacity 0.55, hidden card-footer) per D-10/D-14 | VERIFIED | Lines 124-132: `opacity: 0.55; transition: opacity 200ms ease-out; pointer-events: none;` and `.card.is-ended .card-footer { display: none; }` |
| A9 | `crates/iron_hermes_ui/assets/screens.css` | `.pill.ended` neutral-gray modifier per D-09/D-14 | VERIFIED | Lines 222-226: `color: var(--gray); border-color: var(--gray-faint); background: rgba(110, 118, 129, 0.06);` |
| A10 | `crates/iron_hermes_ui/src/protocol.rs` | `ChatStreamEvent::SubagentEvent {}` payload-free variant | VERIFIED | Line 38: `SubagentEvent {},` with doc comment, no serde attributes — external tagging gives `{"SubagentEvent":{}}` |
| A11 | `crates/iron_hermes_ui/src/server/state.rs` | `subagent_callback_slot` field on AppState + `Some(progress_callback)` | VERIFIED | Field at line 60 (`pub subagent_callback_slot: Arc<tokio::sync::Mutex<Option<UnboundedSender<...>>>>`); construction at 127-131; `Arc::new(move \|...\|)` callback at 133-146 (NOT `Box::new`); `progress_callback: Some(progress_callback)` at 161; field stored in returned `Self` at 185 |
| A12 | `crates/iron_hermes_ui/src/server/ws.rs` | Per-turn UnboundedSender installed with RAII guard, bracketed around `run_web_turn` | VERIFIED | `SubagentCallbackSlotGuard` struct at lines 23-29; `Drop` impl at 32-45; install via `.lock().await` at 245-248; `_slot_guard` RAII binding at 249-251; runs immediately before `run_web_turn` call at 255-263 |
| A13 | `.planning/phases/26.7.1-agents-page-live-updates/26.7.1-VALIDATION.md` | Grep targets corrected to `hermes_app/mod.rs` (not `warp_hermes.rs`) | VERIFIED | All grep commands in the per-task table point at `crates/iron_hermes_ui/src/components/hermes_app/mod.rs`; no `warp_hermes.rs` paths remain in any automated command (one HTML-escaped reference in a footnote is intentional) |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `agents.rs` | `hermes_app/mod.rs` (context providers) | `use_context::<Signal<bool>>()` for `is_ws_connected` and `use_context::<Signal<u64>>()` for `subagent_events` | WIRED | `agents.rs:88-89` — both `use_context` calls resolve to providers at `mod.rs:330-331`. SSR proven by Plan 01 UAT-2. |
| `agents.rs` | `agents_diff::diff_terminations` | Module use of `diff_terminations` to compute newly-absent agent ids | WIRED | `agents.rs:121-124` — `crate::components::hermes_app::screens::agents_diff::diff_terminations(&prev_snapshot, &agents_list)`; module registered at `screens/mod.rs:12` |
| `agents.rs` | `gloo_timers::future::TimeoutFuture` | Poll loop and decay sweep both await `TimeoutFuture::new(ms)` | WIRED | Poll: `TimeoutFuture::new(500)`/`TimeoutFuture::new(200)`/`TimeoutFuture::new(interval_ms)` (lines 219, 223, 237). Decay: `TimeoutFuture::new(1_000)` (line 245). |
| `screens.css` | `agents.rs` | `.card.is-ended` class toggled by AgentCard | WIRED | `class: if is_ended { "is-ended" }` at `agents.rs:399`; `class: if is_ended { "pill ended" } else { "pill green" }` at 415; CSS rules at `screens.css:124-132, 222-226` |
| `ws.rs` | `state.rs::AppState::subagent_callback_slot` | Per-turn `tx.clone()` installed via `lock().await` + RAII guard cleanup | WIRED | `ws.rs:244-251` — `let tx_subagent = tx.clone(); { let mut guard = app_state.subagent_callback_slot.lock().await; *guard = Some(tx_subagent); } let _slot_guard = SubagentCallbackSlotGuard { slot: app_state.subagent_callback_slot.clone() };` |
| `state.rs::AppState::init` | `state.rs::subagent_callback_slot` | `DelegateTaskWiring.progress_callback` is `Some(Arc::new(move \|_, _\| ...))` | WIRED | `state.rs:133-146` Arc closure captures `cb_slot` clone, uses `try_lock` to peek + `tx.send(SubagentEvent {})`; passed as `progress_callback: Some(progress_callback)` at line 161 |
| `hermes_app/mod.rs` (recv loop) | `agents.rs` (use_effect) | `subagent_events: Signal<u64>` increment → ScreenAgents `use_effect` subscribes and calls `agents_resource.restart()` | WIRED | `mod.rs:244-249` increments via context-shared signal; `agents.rs:272-276` use_effect subscribes via call-syntax `subagent_events()` (returns Copy u64) and calls `.restart()`. Cross-component path resolves via the providers from `mod.rs:330-331`. |
| `agents.rs` (poll loop) | `hermes_app/mod.rs` (`is_ws_connected` provider) | `ws_connected_ctx.read()` chooses 5000 vs 1500 ms cadence each iteration | WIRED | `agents.rs:88` consumes context; `agents.rs:233-237` — `let interval_ms: u32 = if *ws_connected_ctx.read() { 5_000 } else { 1_500 };` reads the same signal that `mod.rs:170, 174, 254, 258` mutate. |

All 8 key links WIRED.

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `ScreenAgents` agent grid | `agents_list: Vec<AgentInfo>` | `use_resource(\|\| async move { api_agents_list().await })` (agents.rs:59) → `api_agents_list` server fn which queries the live `SubagentRegistry` | Yes — UAT confirmed `prev=0 → curr=3` on real `delegate_task` invocation; not a hardcoded empty array | FLOWING |
| `ScreenAgents` ENDED cards | `ended_cards: Vec<AgentInfo>` | Derived from `recently_terminated.read().values()` (agents.rs:179-186); populated by diff_terminations from real api_agents_list snapshots | Yes — diff fires when `agents_list` shrinks between fetches; UAT-confirmed for natural-termination path | FLOWING |
| `subagent_events` counter | `Signal<u64>` | Incremented in mod.rs:244-249 on each `ChatStreamEvent::SubagentEvent {}` frame; frames originate from real `SubagentProgressCallback` invocation in state.rs:133-146 driven by `SubagentProgress` events from `subagent_runner` | Yes — UAT confirmed 3 rapid restart fetches at spawn time, proving real ws frames drove the counter | FLOWING |
| `is_ws_connected` signal | `Signal<bool>` | mod.rs:170/174/254/258 toggle this on real ws lifecycle transitions in `dioxus_fullstack::use_websocket` recv loop | Yes — Plan 01 baseline value `false` gives 1500 ms cadence; Plan 02 mutates from connect/Close arms | FLOWING |
| `rpc_in_flight` counter | `Signal<u32>` | KILL/INTERRUPT button closures (agents.rs:445-498) increment before spawning the api_agents_kill/interrupt RPC and decrement after the spawn returns | Yes — bracketing matches the documented D-05 contract | FLOWING |

No HOLLOW or DISCONNECTED data flows detected.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `cargo check` native target (web feature) | `cargo check -p iron_hermes_ui --features web` | exit 0 — Finished dev profile | PASS |
| `agents_diff` unit tests (4 tests including D-13 guard) | `cargo test -p iron_hermes_ui --features web agents_diff` | 4 passed; 0 failed | PASS |
| `SubagentEvent` serde round-trip | `cargo test -p iron_hermes_ui --features web test_subagent_event_json_shape` | 1 passed; 0 failed | PASS |
| `SubagentProgressCallback` emits event + slot teardown no-op | `cargo test -p iron_hermes_ui --features web,server test_subagent_callback_emits_event` | 1 passed; 0 failed | PASS |
| Push-driven live-spawn (real ws round-trip) | Manual UAT — recorded in user prompt | 3 rapid `/api/agents/list` calls at spawn time, prev=0 → curr=3 | PASS |
| wasm32 build target | (not re-executed in this verification — would re-trigger full wasm rebuild) | SUMMARY (UAT-4) records `cargo check -p iron_hermes_ui --features web --target wasm32-unknown-unknown` PASS post-`web_time` hotfix; native build re-confirmed green here | SKIP (transitively confirmed by phase SUMMARY UAT-4 evidence) |

### Probe Execution

No phase-declared `scripts/*/tests/probe-*.sh` exist for Phase 26.7.1. The phase's validation contract (`26.7.1-VALIDATION.md`) explicitly designates the runnable-check surface as `cargo test` + `cargo clippy` — those four commands above ARE the probe equivalents and all PASS.

---

### Requirements Coverage

PLAN frontmatter declares D-XX decisions from `26.7.1-CONTEXT.md` as the requirements set. The ROADMAP entry says "Requirements: D-01..D-NN from `26.7.1-CONTEXT.md`".

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| D-01 (N=5s HOLD) | 01 | HOLD card persists 5 s before removal | SATISFIED | `agents.rs:251` — `if ts.elapsed() >= Duration::from_secs(5)` |
| D-02 (uniform terminations) | 01 + 02 | All terminations show ENDED uniformly | SATISFIED | `agents_diff.rs:28-38` pure fn, no per-kind branching; `agents.rs:415-416` single ENDED label |
| D-03 (1500 ms baseline poll) | 01 | Baseline poll interval | SATISFIED | `agents.rs:233` — literal `1_500` in the else branch |
| D-04 (Page Visibility pause) | 01 | Poll pauses while `document.hidden()` true | SATISFIED | `agents.rs:196-221` — synchronous `d.hidden()` check + 500ms backoff |
| D-05 (rpc_in_flight pause) | 01 | Poll pauses during kill/interrupt RPC | SATISFIED | `agents.rs:204-224` |
| D-06 (chat ws reuse for SubagentProgress) | 02 | Path A subagent_callback_slot routing | SATISFIED | `state.rs:127-186` + `ws.rs:22-45, 241-263` |
| D-07 (counter-only payload, restart on increment) | 02 | `SubagentEvent {}` + use_effect | SATISFIED | `protocol.rs:38`, `mod.rs:244-249`, `agents.rs:272-276` |
| D-08 (dynamic 5000/1500 ms cadence) | 02 | Cadence reads is_ws_connected | SATISFIED | `agents.rs:233-237` + `mod.rs:170/174/254/258` |
| D-09 (single ENDED pill) | 01 | No per-kind branching in pill text | SATISFIED | `agents.rs:415-416` — `"ENDED"` literal in is_ended branch |
| D-10 (HOLD body opacity 0.55, action buttons hidden) | 01 | Card-footer hidden via CSS | SATISFIED | `screens.css:124-132` |
| D-11 (uptime_secs frozen at termination) | 01 | Snapshot held at diff time | SATISFIED | `agents_diff.rs` returns cloned `AgentInfo` from `live_prev`; `agents.rs:131-136` stores cloned `old` snapshot |
| D-12 (PRUNE clears both client + server) | 01 | Synchronous client clear before server prune | SATISFIED | `agents.rs:304` |
| D-13 (re-running id wins) | 01 | Drop entry from `recently_terminated` if id reappears | SATISFIED | `agents.rs:99-107` pre-diff loop; pure-fn unit test passes |
| D-14 (CSS modifier classes) | 01 | `.card.is-ended` + `.pill.ended` | SATISFIED | `screens.css:124-132, 222-226` |

All 14 D-XX requirements declared in PLAN frontmatter are SATISFIED.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs` | 165-167, 212-213 | `web_sys::console::log_1` instrumentation lines (`[26.7.1-01 diff]` / `[26.7.1-01 poll]`) | ℹ️ Info | Diagnostic console logging added during UAT iterations; not behaviorally harmful but should be removed in a cleanup pass before milestone close. Wasm-gated (`#[cfg(target_arch = "wasm32")]`), so native builds are not affected. |

No 🛑 BLOCKER anti-patterns (no `TBD`/`FIXME`/`XXX` without issue references in modified files); no ⚠️ WARNING patterns flagged. The console.log instrumentation is informational only.

---

### Human Verification Required

None for the goal-backward verification of this phase. The user-provided UAT evidence (2026-05-19) confirms:

- **Live-spawn push verified** — 3 rapid `/api/agents/list` calls at spawn time with prev=0 → curr=3, proving the end-to-end ws-event → counter → use_effect → restart path works under real load (covers Truths #10, #11, #12, plus key links 5, 6, 7).
- **HOLD-N kill scenario un-UAT-able** — explicitly excluded from this phase's verification scope by the user prompt; the upstream `shrike.kill` bug is documented as a Phase 32.3 pre-existing failure, NOT a Phase 26.7.1 deliverable. Frontend HOLD-N machinery is verified by 4/4 `agents_diff` unit tests (including D-13 guard) plus full code-level inspection of the diff/decay/render path.
- **Natural-termination HOLD-N** — confirmed visible in the UAT-1 → UAT-4 iteration cycle documented in 26.7.1-01-SUMMARY.md (scenarios 4, 5, 6 covered after the four hotfixes landed).

---

### Gaps Summary

No gaps blocking goal achievement. All 16 must-have truths verified, all 13 artifacts present and substantive, all 8 key links wired, all 14 declared requirements satisfied, all 4 cargo test gates green, and the headline UAT scenario (live-spawn push <1 s latency) demonstrably works in real `dx serve`.

The two documented limitations are correctly bounded:

1. **Kill-path HOLD UAT deferred** — upstream `shrike.kill` Phase 32.3 bug, separate from Phase 26.7.1's deliverable. The phase's diff/decay/render machinery is verified by unit tests + code inspection; UAT will close once the upstream bug is fixed.
2. **Single-session broadcast** — documented as accepted in CONTEXT.md (T-26.7.1-02-01); explicitly deferred to multi-session phase. Acceptable for current single-user web UI.

Both fall under "accepted scope boundaries" rather than gaps.

---

## Verdict

**VERIFIED** — Phase 26.7.1 delivers what its goal promised:

- (a) **Periodic-poll baseline (1.5 s)** — implemented end-to-end with Page Visibility (D-04) and RPC-in-flight (D-05) pauses.
- (b) **TERMINATED-HOLD-N=5s fade** — pure-fn diff + decay sweep + CSS modifiers, fully unit-tested at the diff layer including the D-13 re-running-id invariant.
- (c) **Push-driven sub-second updates via chat ws** — Path A `subagent_callback_slot` + RAII guard + `ChatStreamEvent::SubagentEvent {}` variant + recv-loop arm + `use_effect` restart. End-to-end verified by UAT live-spawn observation.

Code, tests, and UAT evidence align. The phase is complete and ready to proceed.

---

_Verified: 2026-05-19T17:00:00Z_
_Verifier: Claude (gsd-verifier, Opus 4.7)_
