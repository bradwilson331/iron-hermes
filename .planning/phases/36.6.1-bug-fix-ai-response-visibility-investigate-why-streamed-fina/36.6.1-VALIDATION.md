---
phase: 36.6.1
slug: bug-fix-ai-response-visibility-investigate-why-streamed-fina
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-26
---

# Phase 36.6.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Derived from RESEARCH.md §7 (Validation Architecture).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in test (`cargo test`) |
| **Feature gate** | `--features test-support` (required for `App::new_test_with_messages`) |
| **Config file** | `crates/ironhermes-cli/Cargo.toml` `[features]` section |
| **Quick run command** | `cargo test -p ironhermes-cli --features test-support word_wrapped` |
| **Full suite command** | `cargo test -p ironhermes-cli --features test-support` |
| **Estimated runtime** | ~15–30 seconds (quick); ~60–90 seconds (full) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-cli --features test-support word_wrapped`
- **After every plan wave:** Run `cargo test -p ironhermes-cli --features test-support`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 36.6.1-01-01 | 01 | 0 | D-01 | — / — | N/A (pure scroll math; no attack surface) | unit | `cargo test -p ironhermes-cli --features test-support word_wrapped_line_count_matches_ratatui` | ❌ W0 | ⬜ pending |
| 36.6.1-01-02 | 01 | 0 | D-01 | — / — | N/A | unit | `cargo test -p ironhermes-cli --features test-support word_wrapped_line_count_short_empty_exact` | ❌ W0 | ⬜ pending |
| 36.6.1-01-03 | 01 | 0 | D-01 | — / — | N/A | unit | `cargo test -p ironhermes-cli --features test-support word_wrapped_line_count_word_that_straddles_boundary` | ❌ W0 | ⬜ pending |
| 36.6.1-01-04 | 01 | 0 | D-01 | — / — | N/A | unit | `cargo test -p ironhermes-cli --features test-support word_wrapped_line_count_leading_whitespace` | ❌ W0 | ⬜ pending |
| 36.6.1-01-05 | 01 | 0 | D-02 | — / — | N/A | unit | `cargo test -p ironhermes-cli --features test-support transcript_line_count_matches_ratatui` | ❌ W0 | ⬜ pending |
| 36.6.1-02-01 | 02 | 1 | D-03 | — / — | N/A | integration | `cargo test -p ironhermes-cli --features test-support auto_scroll_lands_at_true_bottom` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

Task IDs are illustrative; final IDs assigned by gsd-planner. The mapping above asserts that **every** task that ships fix code or test code MUST have an `<automated>` verify command — there are no manual-only behaviors in this phase.

---

## Wave 0 Requirements

- [ ] Add `unicode-width = "0.2"` to `crates/ironhermes-cli/Cargo.toml` (or workspace root) — direct dep promotion (already locked at 0.2.2 transitively)
- [ ] Change `compute_transcript_area` visibility from `fn` to `pub(crate) fn` in `event_loop.rs` (required by D-03 integration test)
- [ ] Add `#[cfg(test)]` module scaffold in `app.rs` for D-02 unit tests (5 test functions)
- [ ] Add `#[cfg(all(test, feature = "test-support"))]` module scaffold in `ui.rs` for D-03 integration test (1 test function)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Send "Hi!" to TUI, verify response appears in viewport | D-01 end-to-end | Reproduces screenshot 1; UAT confirmation that fix solves the user-visible bug | After cargo build: `cargo run -p ironhermes-cli -- tui`, send "Hi!", confirm response is visible without scrolling |
| Run `/usage` with long table, verify last rows visible | D-01 end-to-end | Reproduces screenshot 2 | In TUI, run `/usage` — confirm all table rows render in viewport (or that scroll position rests at true bottom) |

*All automated behaviors covered by D-02 + D-03 above; manual checks are UAT screenshots-reproducer only.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (Cargo.toml dep, pub(crate) visibility, test module scaffolds)
- [ ] No watch-mode flags (`cargo test` runs to completion)
- [ ] Feedback latency < 30s for quick run, < 90s for full suite
- [ ] `nyquist_compliant: true` set in frontmatter once Wave 0 lands

**Approval:** pending
