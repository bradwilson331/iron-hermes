---
phase: 36.6.1-bug-fix-ai-response-visibility-investigate-why-streamed-fina
plan: 02
type: execute
wave: 1
depends_on:
  - 36.6.1-01
files_modified:
  - crates/ironhermes-cli/src/tui_rata/event_loop.rs
  - crates/ironhermes-cli/src/tui_rata/ui.rs
autonomous: true
requirements: [D-03]

must_haves:
  truths:
    - "D-03 — an integration test in ui.rs constructs App::new_test_with_messages with a body long enough that transcript_max_scroll > 0 at 80x24, simulates scroll_to_bottom (the same auto_follow=true; transcript_scroll=0 that handle_stream_event(StreamEvent::Finished) performs), calls reconcile_scroll(compute_transcript_area(Size{80,24})), and asserts app.transcript_scroll == app.transcript_max_scroll(area)."
    - "D-03 — the same test renders the resulting App via ui(f, &app) to an 80x24 TestBackend and asserts that the last in-viewport content row (row index 22 inside the 24-row terminal with 1-char top/bottom borders) is non-blank — proving the fix delivers the user-visible bug closure, not just an arithmetic equality."
    - "D-03 — compute_transcript_area is pub(crate) so the ui.rs test module can call it without inlining the layout duplication; the function body is unchanged (RESEARCH §6 Risk 4 confirms this is a trivial visibility change with no semantic effect)."
  artifacts:
    - path: "crates/ironhermes-cli/src/tui_rata/event_loop.rs"
      provides: "pub(crate) fn compute_transcript_area(size: Size) -> Rect"
      contains: "pub(crate) fn compute_transcript_area"
    - path: "crates/ironhermes-cli/src/tui_rata/ui.rs"
      provides: "End-to-end auto-scroll regression test under existing #[cfg(all(test, feature = test-support))] mod tests block"
      contains: "auto_scroll_lands_at_true_bottom"
  key_links:
    - from: "crates/ironhermes-cli/src/tui_rata/ui.rs::tests::auto_scroll_lands_at_true_bottom_after_stream_finished"
      to: "crate::tui_rata::event_loop::compute_transcript_area"
      via: "function call now permitted by pub(crate) visibility"
      pattern: "compute_transcript_area"
    - from: "crates/ironhermes-cli/src/tui_rata/ui.rs::tests::auto_scroll_lands_at_true_bottom_after_stream_finished"
      to: "App::reconcile_scroll AND App::transcript_max_scroll AND App::scroll_to_bottom"
      via: "exercises the full Finished -> scroll_to_bottom -> reconcile_scroll -> max-scroll equality chain"
      pattern: "reconcile_scroll|transcript_max_scroll|scroll_to_bottom"

user_setup: []
---

<objective>
Add the D-03 end-to-end integration test that exercises the full auto-scroll path: long transcript -> scroll_to_bottom (mirroring StreamEvent::Finished) -> reconcile_scroll(compute_transcript_area(size)) -> assert transcript_scroll == transcript_max_scroll(area) AND assert the last visible row in the rendered TestBackend is non-blank. This catches regressions in the *control flow* (not just the formula — that is what Plan 01 covers), preventing future refactors from re-introducing the bug.

Purpose: Plan 01's unit tests prove the formula is correct in isolation. This test proves the formula is correctly wired into the actual rendering loop — that no future change to reconcile_scroll, transcript_max_scroll, compute_transcript_area, or the ui() layout breaks the end-to-end guarantee that after Finished, the user sees the last line of the response.

Output: One new test in ui.rs (~30 lines) and a one-token visibility change in event_loop.rs (fn compute_transcript_area -> pub(crate) fn compute_transcript_area).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.6.1-bug-fix-ai-response-visibility-investigate-why-streamed-fina/36.6.1-CONTEXT.md
@.planning/phases/36.6.1-bug-fix-ai-response-visibility-investigate-why-streamed-fina/36.6.1-RESEARCH.md
@.planning/phases/36.6.1-bug-fix-ai-response-visibility-investigate-why-streamed-fina/36.6.1-VALIDATION.md
@.planning/phases/36.6.1-bug-fix-ai-response-visibility-investigate-why-streamed-fina/36.6.1-01-SUMMARY.md
@CLAUDE.md

<interfaces>
Targets created/modified by Plan 01 that this plan consumes:

- crates/ironhermes-cli/src/tui_rata/app.rs::word_wrapped_line_count (Plan 01 Task 2) — used transitively via transcript_line_count.
- App::transcript_line_count(width), App::transcript_max_scroll(area), App::reconcile_scroll(area), App::scroll_to_bottom(), App::new_test_with_messages — all existing public methods on App; not modified by this plan.

Targets touched directly by THIS plan:

- crates/ironhermes-cli/src/tui_rata/event_loop.rs:688 — fn compute_transcript_area(size: ratatui::prelude::Size) -> ratatui::layout::Rect (currently a private free function in the module). Change visibility to pub(crate) only — body unchanged.
- crates/ironhermes-cli/src/tui_rata/ui.rs:117-171 — existing #[cfg(all(test, feature = "test-support"))] mod tests block. Add one new #[test] fn auto_scroll_lands_at_true_bottom_after_stream_finished() inside this module, alongside scrollbar_renders_in_right_column_when_content_overflows.

Test plan reference: RESEARCH.md §5 "D-03: Integration Test — End-to-End Auto-Scroll" — provides the complete test body to adapt.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote compute_transcript_area to pub(crate) in event_loop.rs</name>
  <files>crates/ironhermes-cli/src/tui_rata/event_loop.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/tui_rata/event_loop.rs lines 685-706 (the existing fn compute_transcript_area — RESEARCH §6 Risk 4 confirms this is the only call to make the D-03 test possible without duplicating the layout calculation)
    - .planning/phases/36.6.1-.../36.6.1-RESEARCH.md §6 "Risk 4" (visibility change is safe; no external crate imports this symbol)
    - .planning/phases/36.6.1-.../36.6.1-VALIDATION.md "Wave 0 Requirements" (lists this visibility change as a required Wave 0 scaffold for D-03)
  </read_first>
  <behavior>
    - After this change, crate::tui_rata::event_loop::compute_transcript_area(size) is callable from crate::tui_rata::ui::tests.
    - No callers of compute_transcript_area outside the tui_rata module exist (verify via grep), so pub(crate) is the minimum sufficient visibility — do NOT promote to pub.
    - The function body, return type, parameter signature, and doc comment remain unchanged.
  </behavior>
  <action>
    In crates/ironhermes-cli/src/tui_rata/event_loop.rs, locate the function declaration at line 688:

    `fn compute_transcript_area(size: ratatui::prelude::Size) -> ratatui::layout::Rect {`

    Change it to:

    `pub(crate) fn compute_transcript_area(size: ratatui::prelude::Size) -> ratatui::layout::Rect {`

    Do NOT modify the function body (lines 689-705). Do NOT modify the doc comment (lines 685-687). Do NOT modify the closing brace (line 706). Do NOT promote to pub — pub(crate) is correct because the only new consumer (the ui.rs test module in Task 2) is in the same crate.

    Run cargo build -p ironhermes-cli and cargo clippy -p ironhermes-cli -- -D warnings to confirm the visibility change does not trigger any clippy unused/dead-code lints. Run grep -rn 'compute_transcript_area' crates/ to verify the only callers are inside the tui_rata module (event_loop.rs itself around lines 642-706, plus ui.rs tests after Task 2).
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo build -p ironhermes-cli 2&gt;&amp;1 | tail -10 &amp;&amp; cargo clippy -p ironhermes-cli -- -D warnings 2&gt;&amp;1 | tail -10 &amp;&amp; grep -n 'pub(crate) fn compute_transcript_area' crates/ironhermes-cli/src/tui_rata/event_loop.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub(crate) fn compute_transcript_area' crates/ironhermes-cli/src/tui_rata/event_loop.rs` == 1
    - `grep -c '^fn compute_transcript_area' crates/ironhermes-cli/src/tui_rata/event_loop.rs` == 0 (old private decl gone)
    - `cargo build -p ironhermes-cli` exits 0
    - `cargo clippy -p ironhermes-cli -- -D warnings` exits 0
    - `grep -rn 'compute_transcript_area' crates/ironhermes-cli/src/` shows callers only inside the tui_rata module (event_loop.rs + ui.rs after Task 2)
  </acceptance_criteria>
  <done>compute_transcript_area is pub(crate); body untouched; build + clippy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add D-03 integration test auto_scroll_lands_at_true_bottom_after_stream_finished in ui.rs</name>
  <files>crates/ironhermes-cli/src/tui_rata/ui.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/tui_rata/ui.rs lines 117-171 (existing `#[cfg(all(test, feature = "test-support"))] mod tests` block — pattern to mimic for the new test; the existing scrollbar test demonstrates TestBackend::new(80, 24) + Terminal::new + terminal.draw(|f| ui(f, &app)) + terminal.backend().buffer() cell inspection)
    - crates/ironhermes-cli/src/tui_rata/app.rs (Plan 01 outputs: word_wrapped_line_count + corrected transcript_line_count + App::new_test_with_messages still at app.rs:894-906 — unchanged signature)
    - crates/ironhermes-cli/src/tui_rata/event_loop.rs (Task 1 output: pub(crate) fn compute_transcript_area)
    - .planning/phases/36.6.1-.../36.6.1-RESEARCH.md §5 "D-03: Integration Test — End-to-End Auto-Scroll" (complete reference test body to adapt — see the auto_scroll_lands_at_true_bottom_after_stream_finished example)
    - .planning/phases/36.6.1-.../36.6.1-RESEARCH.md §6 "Risk 6" (note: this test does NOT cover ANSI-in-body case, which is documented as a pre-existing limitation — do not try to add coverage for it here)
    - .planning/phases/36.6.1-.../36.6.1-VALIDATION.md row 36.6.1-02-01 (the test name in the VALIDATION map is auto_scroll_lands_at_true_bottom — match that exactly so the validation contract resolves)
  </read_first>
  <behavior>
    - After this task, `cargo test -p ironhermes-cli --features test-support auto_scroll_lands_at_true_bottom` runs and passes 1 test.
    - The test asserts BOTH the arithmetic equality (app.transcript_scroll == app.transcript_max_scroll(area)) AND the visual outcome (the last viewport row is non-blank when content fills the view).
    - The test seeds enough content that transcript_max_scroll(area) > 0 — i.e., the assertion is non-trivial. If max_scroll were 0 the test would pass for any scroll_to_bottom implementation (including the buggy one that pinned scroll to 0). A pre-condition assert proves max > 0.
    - The test uses ratatui::prelude::Size { width: 80, height: 24 } as the canonical fixed terminal size, matching the existing scrollbar test.
    - The test was failing on develop before Plan 01 landed (confirm by reasoning: with the old chars().count() formula, transcript_line_count for the 25-line body returns ~25, viewport ~22, so max ~3 — but ratatui renders ~26 rows due to word-wrap, so the *true* max is ~4; the old formula's max=3 differs from the true visual bottom of 4, and the assertion app.transcript_scroll == transcript_max_scroll(area) would still pass with the old formula because both sides use the same broken count). The asserting power of this test comes from the *visual* check (last_row_non_blank) which uses ratatui's actual render. The combination of both assertions is what guarantees regression coverage.
  </behavior>
  <action>
    In crates/ironhermes-cli/src/tui_rata/ui.rs, locate the existing test module at lines 117-171 (`#[cfg(all(test, feature = "test-support"))] mod tests`). Inside this module, after the closing brace of scrollbar_renders_in_right_column_when_content_overflows (around line 170, before the final `}` of mod tests), add a new test function named exactly `auto_scroll_lands_at_true_bottom_after_stream_finished`.

    The test body (adapt from RESEARCH §5 D-03):

    1. Import additions inside the test function (or at the mod tests top — match local style; the existing scrollbar test uses local imports inside the fn): `use crate::tui_rata::app::App;` and `use crate::tui_rata::event_loop::compute_transcript_area;` (Task 1 made the latter pub(crate)).
    2. Build a 25-line assistant body long enough that transcript_max_scroll > 0 at 80x24:
       `let long_body: &'static str = Box::leak((1..=25).map(|i| format!("Line {:02}: some assistant content here.", i)).collect::<Vec<_>>().join("\n").into_boxed_str());`
    3. `let mut app = App::new_test_with_messages(vec![("assistant", long_body)]);`
    4. `let size = ratatui::prelude::Size { width: 80, height: 24 };`
    5. `let transcript_area = compute_transcript_area(size);`
    6. Call `app.scroll_to_bottom();` — this mirrors what handle_stream_event(StreamEvent::Finished) does (sets auto_follow=true; transcript_scroll=0). Per CONTEXT D-03 and RESEARCH §5, we do NOT need to drive handle_stream_event directly — scroll_to_bottom is the deterministic call site for this behavior.
    7. Call `app.reconcile_scroll(transcript_area);` — clamps transcript_scroll to max when auto_follow is true.
    8. Compute the expected max: `let max = app.transcript_max_scroll(transcript_area);`
    9. Pre-condition assert: `assert!(max > 0, "test setup: expected max_scroll > 0, got 0 — adjust body length or terminal height");` — guards against silently-passing-because-max-is-zero (RESEARCH §5 calls this out).
    10. Main equality assertion: `assert_eq!(app.transcript_scroll, max, "auto-scroll must land at true bottom: scroll={}, max={}", app.transcript_scroll, max);`
    11. Visual assertion: render `ui(f, &app)` to TestBackend::new(80, 24), then inspect column range 1..79 of row 22 (the last in-viewport content row; rows 0 and 23 are top/bottom borders). At least one cell in row 22 cols 1..79 must be non-blank, proving the last content row is rendered (not hidden below the viewport). Use the exact buffer-walking pattern from scrollbar_renders_in_right_column_when_content_overflows (`terminal.backend().buffer()` + `buf.cell((col, row)).map(|c| c.symbol() != " ").unwrap_or(false)`). Assertion: `assert!(last_row_non_blank, "last visible row (row 22) must be non-blank after auto-scroll to bottom — got all blanks, content is below viewport");`.

    Do NOT modify the existing scrollbar test, ui_renders_four_chunks_in_80x24, or ui_idle_knight_rider_chunk_is_blank. Do NOT add new top-level use statements to the file outside the test module — keep imports scoped.

    Run cargo test -p ironhermes-cli --features test-support auto_scroll_lands_at_true_bottom and confirm pass. Then run the full feature-gated test suite to confirm no other tests regressed: cargo test -p ironhermes-cli --features test-support.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes &amp;&amp; cargo test -p ironhermes-cli --features test-support auto_scroll_lands_at_true_bottom 2&gt;&amp;1 | tail -20 &amp;&amp; cargo test -p ironhermes-cli --features test-support 2&gt;&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fn auto_scroll_lands_at_true_bottom_after_stream_finished' crates/ironhermes-cli/src/tui_rata/ui.rs` == 1
    - `grep -c 'compute_transcript_area' crates/ironhermes-cli/src/tui_rata/ui.rs` >= 1 (the test calls it)
    - `cargo test -p ironhermes-cli --features test-support auto_scroll_lands_at_true_bottom` exits 0
    - `cargo test -p ironhermes-cli --features test-support` exits 0 (full suite still green; no regressions in existing scrollbar / ui_renders_four_chunks tests)
    - `cargo clippy -p ironhermes-cli --features test-support --tests -- -D warnings` exits 0
    - Inside the test body, `grep -c 'assert!(max > 0' crates/ironhermes-cli/src/tui_rata/ui.rs` >= 1 (the precondition guard is present so the test cannot silently pass at max=0)
  </acceptance_criteria>
  <done>The D-03 integration test exists in ui.rs, asserts both arithmetic equality and the visual non-blank-last-row outcome, includes the max>0 precondition guard, and passes. The full ironhermes-cli test suite remains green.</done>
</task>

</tasks>

<threat_model>
  <surface>None — pure-function scroll math + dev-dependency promotion + test code. No external inputs, no auth/network/IPC, no untrusted data path touched.</surface>
  <threats>N/A</threats>
  <mitigations>N/A</mitigations>
  <residual_risk>None</residual_risk>
</threat_model>

<verification>
After both tasks land:

1. cargo build -p ironhermes-cli clean
2. cargo build -p ironhermes-cli --features test-support clean
3. cargo clippy -p ironhermes-cli --features test-support --tests -- -D warnings clean
4. cargo test -p ironhermes-cli --features test-support auto_scroll_lands_at_true_bottom — 1 test passes
5. cargo test -p ironhermes-cli --features test-support — full suite still green; specifically Plan 01's word_wrapped_* and transcript_line_count_matches_ratatui tests still pass
6. grep -c 'pub(crate) fn compute_transcript_area' crates/ironhermes-cli/src/tui_rata/event_loop.rs == 1
7. grep -c 'fn auto_scroll_lands_at_true_bottom_after_stream_finished' crates/ironhermes-cli/src/tui_rata/ui.rs == 1
</verification>

<success_criteria>
- D-03 regression test exists and passes, exercising the full auto-scroll control flow end-to-end.
- The combination of D-02 (Plan 01) and D-03 (Plan 02) covers BOTH the formula correctness (unit) AND the wiring of the formula into the render loop (integration), satisfying the must_haves for the phase as a whole.
- compute_transcript_area is now pub(crate); no other call sites needed promotion.
- No existing tests regressed; the full feature-gated suite is green.
- Manual UAT (sending "Hi!" in the TUI and confirming visibility, plus running /usage and confirming the last row is visible) is documented in VALIDATION.md as the post-execution sign-off and is not part of this plan's automated coverage.
</success_criteria>

<output>
Create .planning/phases/36.6.1-bug-fix-ai-response-visibility-investigate-why-streamed-fina/36.6.1-02-SUMMARY.md when done. Include:
- The before/after of the compute_transcript_area declaration (one-line diff).
- The full body of the new auto_scroll_lands_at_true_bottom_after_stream_finished test.
- The full test suite count delta (e.g., "ironhermes-cli: 142 tests pass (+1 from this plan)") and confirmation that no existing test regressed.
- Pointer to VALIDATION.md for the manual UAT screenshots-reproducer steps (sending "Hi!" / running /usage).
</output>
