---
phase: 02-telegram-gateway
task: 2
total_tasks: 2
status: blocked
last_updated: 2026-04-03T01:35:56.026Z
---

<current_state>
Phase 02 (Telegram Gateway) has all 5 plans code-complete (02-01 through 02-05). 26 gateway tests pass. The bot connects to Telegram, receives messages, and dispatches them to the handler correctly. However, the agent loop starts but the LLM response never appears in Telegram — the streaming call to OpenRouter appears to hang.

Two runtime bugs were already fixed this session:
1. dispatch_future was not running concurrently with shutdown signal (bot received messages but never processed them)
2. Handler used config.model.api_key directly instead of config.resolve_api_key() (empty API key since env vars were bypassed)

The current bug needs debug-level logging to diagnose. client.rs now has tracing around the HTTP request/response.
</current_state>

<completed_work>

- Plan 02-01: Async foundation (tokio-util, CancellationToken, config extensions, trait redesign, TelegramAdapter) - Done
- Plan 02-02: StreamConsumer + BackoffState (throttled edits, cursor, overflow, backoff with jitter) - Done
- Plan 02-03: Core wiring (polling, dispatch, user queue, handler, runner, JoinSet, Semaphore) - Done
- Plan 02-04: Slash commands (/start, /new, /clear, /help) + error recovery (D-18/D-19/D-20) - Done
- Plan 02-05 Task 1: Multimodal input (images, PDFs, documents) + gateway CLI subcommand - Done
- Plan 02-05 Task 2: Human smoke test - In progress, blocked on LLM streaming bug
</completed_work>

<remaining_work>

- Diagnose and fix LLM streaming bug (agent.run() never completes)
- Complete human smoke test (bot responds to messages, slash commands work, streaming visible)
- Phase verification (automated + human UAT)
</remaining_work>

<decisions_made>

- dispatch_future must run inside tokio::select! alongside shutdown signal, not after it
- Handler must use config.resolve_api_key() to pick up OPENROUTER_API_KEY from env vars
- Default model updated from anthropic/claude-sonnet-4-20250514 to anthropic/claude-sonnet-4
- Added connect_timeout(30s) to LlmClient's reqwest builder
- Added debug tracing in client.rs around streaming request send/receive
</decisions_made>

<blockers>
- LLM streaming call hangs: agent.run() logs "Starting agent loop" but never completes. Need debug logs to determine if it's a connection issue, auth failure, or SSE parsing problem.
</blockers>

<context>
The debug logging has been added to client.rs but not yet tested. The next step is to run with RUST_LOG=debug and send a message to see exactly where the streaming request stalls. The four uncommitted files contain the bug fixes + debug logging: client.rs, constants.rs, handler.rs, runner.rs.
</context>

<next_action>
1. Commit the uncommitted bug fixes: `git add crates/ironhermes-agent/src/client.rs crates/ironhermes-core/src/constants.rs crates/ironhermes-gateway/src/handler.rs crates/ironhermes-gateway/src/runner.rs`
2. Run: `RUST_LOG=debug cargo run -- gateway`
3. Send a message to the bot in Telegram
4. Check logs for "Sending streaming LLM request" and "LLM response received" to identify the stall point
5. Fix the streaming issue and complete the smoke test
</next_action>
