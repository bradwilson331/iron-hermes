---
phase: 36.3.7.12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-kanban/src/schema.rs
  - crates/ironhermes-kanban/src/types.rs
  - crates/ironhermes-kanban/src/store.rs
  - crates/ironhermes-kanban/src/config.rs
  - crates/ironhermes-kanban/src/judge.rs
  - crates/ironhermes-kanban/src/lib.rs
  - crates/ironhermes-core/src/config.rs
  - crates/ironhermes-kanban/tests/goal_mode_migration.rs
  - crates/ironhermes-kanban/tests/goal_mode_persistence.rs
autonomous: true
requirements:
  - D-01
  - D-05
  - D-06
  - D-08
must_haves:
  truths:
    - "D-01: tasks table on a fresh DB has goal_mode INTEGER NOT NULL DEFAULT 0, goal_max_turns INTEGER NOT NULL DEFAULT 20, goal_turns_used INTEGER NOT NULL DEFAULT 0 (verified via PRAGMA table_info(tasks))"
    - "D-01: tasks table on an existing v1 DB upgrades to v2 idempotently — KanbanStore::new on a v1 file produces a v2 file with the three new columns; re-running is a no-op (no PartialReExecError, no duplicate-column failure)"
    - "D-01: Task struct in types.rs carries goal_mode: bool, goal_max_turns: u32, goal_turns_used: u32 and round-trips through KanbanStore::get_task"
    - "D-01: CreateTaskOptions in store.rs accepts goal_mode + goal_max_turns; KanbanStore::create_task coerces goal_max_turns=0 → 20 (D-03 default-of-20 contract honored at the producer)"
    - "D-05: KanbanConfig.judge_model: String exists, defaults to empty, serializes/deserializes via serde with the same pattern as decomposer_model (config.rs:95,119)"
    - "D-05: ironhermes-core reserved-role list at config.rs:61 includes the literal kanban_judge entry; the role-count guard test bumps by exactly one"
    - "D-06: JudgeFn typedef exists in crates/ironhermes-kanban/src/judge.rs (sibling of decomposer.rs); type signature mirrors DecomposeFn at decomposer.rs:141-142 (Arc<dyn Fn(JudgeRequest) -> BoxFuture<Result<JudgeOutput>> + Send + Sync>)"
    - "D-08: ironhermes-kanban crate continues to have ZERO ironhermes-agent dep (verified by cargo tree -p ironhermes-kanban grep)"
    - "Schema-version constant bumps from 1 → 2 at schema.rs:13; run_migrations gains a `if current < 2 { ... }` branch using `let _ = conn.execute(ALTER TABLE ...)` idempotent pattern; UPDATE schema_version SET version = 2"
  artifacts:
    - path: "crates/ironhermes-kanban/src/schema.rs"
      provides: "SCHEMA_VERSION=2 + v1→v2 migration branch + DDL block updated with 3 new tasks columns"
      contains: "SCHEMA_VERSION: i64 = 2"
    - path: "crates/ironhermes-kanban/src/judge.rs"
      provides: "JudgeFn typedef + JudgeRequest + JudgeOutput + JudgeVerdict"
      contains: "pub type JudgeFn"
    - path: "crates/ironhermes-kanban/src/types.rs"
      provides: "Task struct fields goal_mode, goal_max_turns, goal_turns_used"
      contains: "goal_mode"
    - path: "crates/ironhermes-kanban/src/store.rs"
      provides: "CreateTaskOptions.goal_mode, goal_max_turns + create_task wiring + 0→20 coercion + get_task hydration"
      contains: "goal_mode"
    - path: "crates/ironhermes-kanban/src/config.rs"
      provides: "KanbanConfig.judge_model"
      contains: "judge_model"
    - path: "crates/ironhermes-core/src/config.rs"
      provides: "Reserved role list entry 'kanban_judge'"
      contains: "kanban_judge"
    - path: "crates/ironhermes-kanban/tests/goal_mode_migration.rs"
      provides: "Bilateral test — fresh DB has v2 schema; v1 DB upgrades to v2; re-running migration is idempotent"
      contains: "PRAGMA table_info"
    - path: "crates/ironhermes-kanban/tests/goal_mode_persistence.rs"
      provides: "Bilateral test — CreateTaskOptions{goal_mode=true, goal_max_turns=5} round-trips via get_task"
      contains: "goal_max_turns"
  key_links:
    - from: "crates/ironhermes-kanban/src/lib.rs"
      to: "crates/ironhermes-kanban/src/judge.rs"
      via: "pub mod judge; pub use judge::{JudgeFn, JudgeRequest, JudgeOutput, JudgeVerdict};"
      pattern: "pub mod judge"
    - from: "crates/ironhermes-kanban/src/schema.rs::run_migrations"
      to: "ALTER TABLE tasks ADD COLUMN"
      via: "if current < 2 { ... } block at schema.rs:180-190"
      pattern: "ADD COLUMN goal_mode"
    - from: "crates/ironhermes-kanban/src/store.rs::create_task"
      to: "tasks.goal_mode + tasks.goal_max_turns columns"
      via: "INSERT statement augmentation with two new bindings"
      pattern: "goal_mode"
---

<objective>
Land the schema-and-types foundation for Phase 36.3.7.12 Goal Mode. Bumps SCHEMA_VERSION 1→2, lands three new columns on the `tasks` table (`goal_mode`, `goal_max_turns`, `goal_turns_used`) via BOTH the DDL block (fresh-DB path) AND the migration ladder (existing-DB path) per RESEARCH Finding 5 + Pitfall 1. Adds matching fields to `Task` (types.rs) and `CreateTaskOptions` (store.rs). Adds `judge_model: String` to `KanbanConfig` (config.rs:95) mirroring `decomposer_model`. Adds `kanban_judge` to the reserved auxiliary-role list in `ironhermes-core` (config.rs:61 + role-count test guard at lines 2547-2563). Stands up the `JudgeFn` typedef + `JudgeRequest`/`JudgeOutput` in a new sibling file `crates/ironhermes-kanban/src/judge.rs`, mirroring `decomposer.rs:141-142`'s `DecomposeFn` shape and re-exporting from `lib.rs`. Ships bilateral regression tests for the migration (fresh + upgrade + idempotent re-run) and for `CreateTaskOptions` round-trip via `get_task`.

Purpose: All downstream plans (02 producer surface, 03 CLI + judge LLM call, 04 goal loop wrapper) depend on the columns existing, the Task struct carrying the new fields, the JudgeFn typedef being importable from `ironhermes-kanban`, and the `kanban_judge` role being resolvable in the three-tier provider cascade. This is the contracts-first Wave 1 that unblocks parallel Wave 2 work.

Output:
- `crates/ironhermes-kanban/src/schema.rs` (DDL + migration v1→v2)
- `crates/ironhermes-kanban/src/types.rs` (Task fields)
- `crates/ironhermes-kanban/src/store.rs` (CreateTaskOptions fields + create_task wiring + get_task hydration)
- `crates/ironhermes-kanban/src/config.rs` (KanbanConfig.judge_model)
- `crates/ironhermes-kanban/src/judge.rs` (new file — JudgeFn typedef)
- `crates/ironhermes-kanban/src/lib.rs` (pub mod judge + re-exports)
- `crates/ironhermes-core/src/config.rs` (reserved role + count test bump)
- `crates/ironhermes-kanban/tests/goal_mode_migration.rs` (new bilateral test)
- `crates/ironhermes-kanban/tests/goal_mode_persistence.rs` (new bilateral test)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-CONTEXT.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-RESEARCH.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-VALIDATION.md
@crates/ironhermes-kanban/src/schema.rs
@crates/ironhermes-kanban/src/types.rs
@crates/ironhermes-kanban/src/store.rs
@crates/ironhermes-kanban/src/config.rs
@crates/ironhermes-kanban/src/decomposer.rs
@crates/ironhermes-kanban/src/lib.rs

<interfaces>
<!-- Key types and contracts extracted from the codebase. -->
<!-- Executor uses these directly — no codebase exploration needed. -->

From crates/ironhermes-kanban/src/decomposer.rs:141-142 (the EXACT shape JudgeFn must mirror):
  pub type DecomposeFn =
      Arc<dyn Fn(DecomposeRequest) -> BoxFuture<Result<DecomposeOutput>> + Send + Sync>;

From crates/ironhermes-kanban/src/decomposer.rs:69 (DecomposeRequest struct that JudgeRequest mirrors in spirit):
  pub struct DecomposeRequest { /* task_id, title, body, ... */ }

From crates/ironhermes-kanban/src/decomposer.rs:87 (DecomposeOutput shape):
  pub struct DecomposeOutput { /* new_body, child_specs, ... */ }

From crates/ironhermes-kanban/src/config.rs:75-95 (the EXACT pattern judge_model must mirror):
  pub auto_decompose: bool,
  #[serde(default = "default_auto_decompose_per_tick")]
  pub auto_decompose_per_tick: u32,
  /* ... */
  pub decomposer_model: String,
And the Default impl at config.rs:115-119:
  auto_decompose: false,
  auto_decompose_per_tick: default_auto_decompose_per_tick(),
  decomposer_model: String::new(),

From crates/ironhermes-kanban/src/schema.rs:13 (the constant to bump):
  pub const SCHEMA_VERSION: i64 = 1; // → bump to 2

From crates/ironhermes-kanban/src/schema.rs:31-55 (the CREATE TABLE IF NOT EXISTS tasks block — add 3 columns INSIDE the DDL so fresh DBs get them):
  CREATE TABLE IF NOT EXISTS tasks ( ... )

From crates/ironhermes-kanban/src/schema.rs:180 (the migration ladder landing-place):
  pub fn run_migrations(conn: &mut Connection, current: i64) -> Result<()> {
      // current < 2 branch lands here
  }

From crates/ironhermes-kanban/src/store.rs:34-52 (CreateTaskOptions — add two fields):
  #[derive(Default)]
  pub struct CreateTaskOptions { /* existing fields */ }

From crates/ironhermes-kanban/src/types.rs:28-68 (Task — add three fields):
  pub struct Task { /* existing fields */ }

From crates/ironhermes-core/src/config.rs:61 (reserved role list):
  // "kanban_decomposer" already present; ADD "kanban_judge" alongside it.
And config.rs:2547-2563 (role-count guard test — bump expected count by 1).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump SCHEMA_VERSION + add 3 columns to DDL + migration ladder + bilateral migration test</name>
  <files>crates/ironhermes-kanban/src/schema.rs, crates/ironhermes-kanban/tests/goal_mode_migration.rs</files>
  <behavior>
    - Fresh DB: KanbanStore::new on a never-existed path produces a tasks table with goal_mode, goal_max_turns, goal_turns_used columns at the expected defaults (0, 20, 0). Verified via PRAGMA table_info(tasks).
    - Existing v1 DB upgrade: a hand-crafted v1 DB (no goal_* columns, schema_version row = 1) upgraded by KanbanStore::new emerges with the three new columns and schema_version = 2.
    - Idempotent re-run: calling KanbanStore::new on a v2 DB is a no-op (no PartialReExec, no duplicate column error, schema_version stays 2).
    - Migration must NOT touch any other table; only the tasks ALTERs land.
  </behavior>
  <action>
    Implement schema.rs changes per RESEARCH Finding 5 + Code Example "Pattern D":

    Step 1. At schema.rs:13, change `pub const SCHEMA_VERSION: i64 = 1;` to `pub const SCHEMA_VERSION: i64 = 2;`.

    Step 2. Inside the `CREATE TABLE IF NOT EXISTS tasks (...)` block at schema.rs:31-55, append three new columns BEFORE the trailing `)`:
      - `goal_mode INTEGER NOT NULL DEFAULT 0,`
      - `goal_max_turns INTEGER NOT NULL DEFAULT 20,`
      - `goal_turns_used INTEGER NOT NULL DEFAULT 0`
      (Order: append after the last existing column. Mind comma placement so the final column has no trailing comma.) This is the FRESH-DB path. Existing DBs do NOT pick up these columns from this block (per RESEARCH Pitfall 1) — the migration ladder below is REQUIRED.

    Step 3. In `run_migrations(conn, current)` at schema.rs:180, add the following branch BEFORE the final `Ok(())`:
      ```
      if current < 2 {
          // Phase 36.3.7.12: goal_mode + goal_max_turns + goal_turns_used.
          let _ = conn.execute("ALTER TABLE tasks ADD COLUMN goal_mode INTEGER NOT NULL DEFAULT 0", []);
          let _ = conn.execute("ALTER TABLE tasks ADD COLUMN goal_max_turns INTEGER NOT NULL DEFAULT 20", []);
          let _ = conn.execute("ALTER TABLE tasks ADD COLUMN goal_turns_used INTEGER NOT NULL DEFAULT 0", []);
          conn.execute("UPDATE schema_version SET version = 2", [])?;
      }
      ```
      The `let _ =` suppresses "duplicate column" rusqlite errors on partial re-runs (mirror of the existing migration-ladder idiom). The final `conn.execute("UPDATE schema_version SET version = 2", [])?` MUST propagate via `?` — version-row failure is a hard error.

    Step 4. Create `crates/ironhermes-kanban/tests/goal_mode_migration.rs` with three #[test] fns:
      - `fresh_db_has_v2_columns()`: open tempdir, run KanbanStore::new, then `conn.prepare("PRAGMA table_info(tasks)")` and assert the row set contains all three names with the expected default expressions ("0", "20", "0"). Use the open_store helper pattern from `tests/store_smoke.rs:1-73`.
      - `v1_db_upgrades_to_v2()`: open a connection, manually CREATE the v1 tasks DDL (omit the three new columns), seed schema_version row = 1, close. Re-open via KanbanStore::new and assert PRAGMA shows all three columns plus schema_version = 2.
      - `migration_is_idempotent()`: run KanbanStore::new on the SAME tempdir twice in a row. Second call must succeed with no error and PRAGMA must still show schema_version = 2.

    Step 5. DO NOT weaken the existing `BEGIN`/`COMMIT` wrap in `KanbanStore::init_schema` (schema.rs:168-179 atomicity docstring is load-bearing per RESEARCH Finding 5).
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo test -p ironhermes-kanban --test goal_mode_migration 2>&1 | tail -20 && grep -c "SCHEMA_VERSION: i64 = 2" crates/ironhermes-kanban/src/schema.rs | xargs test 1 -eq && grep -c "ADD COLUMN goal_mode" crates/ironhermes-kanban/src/schema.rs | xargs test 1 -eq && grep -c "ADD COLUMN goal_max_turns" crates/ironhermes-kanban/src/schema.rs | xargs test 1 -eq && grep -c "ADD COLUMN goal_turns_used" crates/ironhermes-kanban/src/schema.rs | xargs test 1 -eq</automated>
  </verify>
  <done>
    Three migration tests green. `SCHEMA_VERSION` is 2. Fresh DBs have the three new columns. Existing v1 DBs upgrade idempotently. Re-running the migration produces no error.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add goal_* fields to Task + CreateTaskOptions + KanbanConfig.judge_model + bilateral persistence test</name>
  <files>crates/ironhermes-kanban/src/types.rs, crates/ironhermes-kanban/src/store.rs, crates/ironhermes-kanban/src/config.rs, crates/ironhermes-kanban/tests/goal_mode_persistence.rs</files>
  <behavior>
    - CreateTaskOptions { goal_mode: true, goal_max_turns: 5, ..Default::default() } persists via create_task; get_task returns a Task with goal_mode=true, goal_max_turns=5, goal_turns_used=0.
    - CreateTaskOptions { goal_mode: true, goal_max_turns: 0, ..Default::default() } (caller didn't override) → create_task coerces to goal_max_turns=20 in the stored row (D-03 default-of-20 contract honored at the producer level even though the column DDL also defaults to 20).
    - Default CreateTaskOptions (goal_mode: false, goal_max_turns: 0) → stored row has goal_mode=false, goal_max_turns=20 (DDL default), goal_turns_used=0.
    - KanbanConfig::default().judge_model == "".
  </behavior>
  <action>
    Step 1 — `crates/ironhermes-kanban/src/types.rs:28-68`. Append three fields to the Task struct (after the last existing field, before the closing `}`):
      ```
      pub goal_mode: bool,
      pub goal_max_turns: u32,
      pub goal_turns_used: u32,
      ```
      If Task derives Serialize/Deserialize, ensure default tags work; if any field needs `#[serde(default)]` to handle old DB rows pre-migration → add it. Mirror the pattern at types.rs for fields like `auto_decompose` or similar booleans if present; otherwise no serde attr is needed because the column DEFAULTs guarantee non-NULL.

    Step 2 — `crates/ironhermes-kanban/src/store.rs:34-52`. Append two fields to CreateTaskOptions (after the last existing field):
      ```
      pub goal_mode: bool,
      pub goal_max_turns: u32,
      ```
      The `#[derive(Default)]` at line 33 gives goal_mode=false, goal_max_turns=0 for free.

    Step 3 — In `KanbanStore::create_task` (the INSERT for tasks): augment the column list and value bindings to include `goal_mode` and `goal_max_turns`. Per D-03 producer-level default: BEFORE binding, coerce `let goal_max_turns = if opts.goal_max_turns == 0 { 20 } else { opts.goal_max_turns };`. `goal_turns_used` is NOT bound — let the DDL DEFAULT 0 apply.

    Step 4 — In `KanbanStore::get_task` (the SELECT that hydrates a Task): augment the column list to include `goal_mode`, `goal_max_turns`, `goal_turns_used` and the row mapper to populate the three new Task fields. Use `row.get::<_, i64>` then cast for u32 columns, mirror existing u32 fields in get_task. Treat `goal_mode INTEGER` as 0/1 → bool via `row.get::<_, i64>(...)? != 0`.

    Step 5 — `crates/ironhermes-kanban/src/config.rs:95`. Add a sibling field to `decomposer_model`:
      ```
      /// Aux model id for the goal-mode judge LLM. Empty = resolve through `auxiliary.kanban_judge`
      /// role then fall back to the main provider. Mirrors `decomposer_model` (D-05).
      #[serde(default)]
      pub judge_model: String,
      ```
      And in the Default impl (config.rs:119): add `judge_model: String::new(),`.

    Step 6 — In the existing test at config.rs:181-185 (the Default test), add `assert!(cfg.judge_model.is_empty());`. (Mirror the line at config.rs:185 for decomposer_model.)

    Step 7 — Create `crates/ironhermes-kanban/tests/goal_mode_persistence.rs` with three #[test] fns following the `tests/store_smoke.rs:1-73` template:
      - `goal_mode_round_trips()`: open_store, create_task with `CreateTaskOptions { assignee: "alice".into(), goal_mode: true, goal_max_turns: 5, ..Default::default() }`, then get_task and assert returned Task has goal_mode=true, goal_max_turns=5, goal_turns_used=0.
      - `goal_max_turns_zero_coerces_to_twenty()`: CreateTaskOptions { goal_mode: true, goal_max_turns: 0, ..Default::default() } → get_task returns goal_max_turns == 20 (D-03 producer-level default).
      - `default_options_have_goal_mode_false_and_default_budget()`: CreateTaskOptions::default() → get_task returns goal_mode=false AND goal_max_turns=20 (DDL DEFAULT 20 honored by the column DEFAULT path).

    DO NOT add any agent-crate dep or any LLM-call code in this task; this is pure data shape.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo test -p ironhermes-kanban --test goal_mode_persistence 2>&1 | tail -20 && cargo test -p ironhermes-kanban --lib config:: 2>&1 | tail -10 && grep -c "pub judge_model: String" crates/ironhermes-kanban/src/config.rs | xargs test 1 -eq && grep -c "pub goal_mode: bool" crates/ironhermes-kanban/src/types.rs | xargs test 1 -eq && grep -c "pub goal_mode: bool" crates/ironhermes-kanban/src/store.rs | xargs test 1 -eq</automated>
  </verify>
  <done>
    Three persistence tests green. config lib tests green. Task / CreateTaskOptions / KanbanConfig all carry the new fields. Producer-level 0→20 coercion verified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stand up JudgeFn typedef in new judge.rs + reserved-role registration in ironhermes-core</name>
  <files>crates/ironhermes-kanban/src/judge.rs, crates/ironhermes-kanban/src/lib.rs, crates/ironhermes-core/src/config.rs</files>
  <behavior>
    - `crates/ironhermes-kanban/src/judge.rs` exists and exports `JudgeFn`, `JudgeRequest`, `JudgeOutput`, `JudgeVerdict`.
    - The JudgeFn type signature mirrors DecomposeFn at decomposer.rs:141-142 exactly: `Arc<dyn Fn(JudgeRequest) -> BoxFuture<Result<JudgeOutput>> + Send + Sync>`.
    - `lib.rs` re-exports the four symbols so downstream crates (ironhermes-cli Plan 03 + 04) can `use ironhermes_kanban::{JudgeFn, JudgeRequest, JudgeOutput, JudgeVerdict};`.
    - `ironhermes-core::config` reserved-role list contains the literal string "kanban_judge" alongside "kanban_decomposer".
    - The reserved-role-list count guard test at lines 2547-2563 of ironhermes-core/src/config.rs has its expected count bumped by exactly 1.
    - `cargo tree -p ironhermes-kanban --depth 1` does NOT list `ironhermes-agent` (crate-isolation fence preserved — D-08).
  </behavior>
  <action>
    Step 1 — Create `crates/ironhermes-kanban/src/judge.rs`:
      - Use the same imports as decomposer.rs (Arc, BoxFuture, Result via `anyhow::Result` or the kanban-crate Result alias — whichever decomposer.rs uses).
      - Define:
        ```
        pub struct JudgeRequest {
            pub task_id: String,
            pub title: String,
            pub body: String,
            pub worker_turn_output: String,
            pub turn: u32,
        }

        #[derive(Clone, Debug, PartialEq, Eq)]
        pub enum JudgeVerdict { Met, NotMet }

        pub struct JudgeOutput {
            pub verdict: JudgeVerdict,
            pub reason: String,
        }

        pub type JudgeFn =
            Arc<dyn Fn(JudgeRequest) -> BoxFuture<'static, anyhow::Result<JudgeOutput>> + Send + Sync>;
        ```
      - Match the lifetime parameter on `BoxFuture` to whatever `decomposer.rs:141-142` uses. If decomposer.rs uses `BoxFuture<'_, Result<...>>`, use the same. Do NOT introduce a different signature shape.
      - Add a unit test at the bottom: `#[test] fn judge_fn_typedef_compiles() { let _: Option<JudgeFn> = None; }` — purely to anchor the typedef in the test surface so refactors that break it fail compilation.

    Step 2 — `crates/ironhermes-kanban/src/lib.rs`:
      - Add `pub mod judge;` next to the existing `pub mod decomposer;` line.
      - Add `pub use judge::{JudgeFn, JudgeRequest, JudgeOutput, JudgeVerdict};` next to the existing decomposer re-export.

    Step 3 — `crates/ironhermes-core/src/config.rs:61`:
      - Locate the reserved-role list (the `const` array or `static` slice that contains the literal `"kanban_decomposer"` per RESEARCH Finding 3 + Finding 5). Add `"kanban_judge"` as a sibling entry. Preserve alphabetical or existing order; if unclear, add immediately after `"kanban_decomposer"`.

    Step 4 — `crates/ironhermes-core/src/config.rs:2547-2563`:
      - Find the role-count guard test. Bump the expected count by 1 (e.g., if it asserts `len() == N`, change to `len() == N+1`). Add an explicit `assert!(roles.contains(&"kanban_judge"))` so the test also encodes WHY the count went up.

    DO NOT add any LLM-call code, ProviderResolver wiring, or `build_runtime_judge_fn`. That lives in Plan 03 (CLI crate). This task's deliverable is the typedef + reserved-role registration only — the contract surface for Plan 03 and Plan 04 to consume.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo build -p ironhermes-kanban 2>&1 | tail -10 && cargo test -p ironhermes-kanban --lib judge:: 2>&1 | tail -10 && cargo test -p ironhermes-core --lib 2>&1 | tail -10 && grep -c "pub type JudgeFn" crates/ironhermes-kanban/src/judge.rs | xargs test 1 -eq && grep -c "pub use judge::" crates/ironhermes-kanban/src/lib.rs | xargs test 1 -eq && grep -c "kanban_judge" crates/ironhermes-core/src/config.rs | xargs -I {} test {} -ge 2 && (cargo tree -p ironhermes-kanban --depth 1 2>/dev/null | grep -c '^ironhermes-agent ' | xargs test 0 -eq)</automated>
  </verify>
  <done>
    JudgeFn typedef compiles. JudgeRequest/JudgeOutput/JudgeVerdict are public. lib.rs re-exports. ironhermes-core reserved-role list contains "kanban_judge" and the role-count guard test bumps. Crate-isolation fence holds — ironhermes-kanban still has no ironhermes-agent dep.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → `KanbanStore::create_task` | Untrusted `CreateTaskOptions.goal_max_turns` could be hostile (overflow, negative-cast). Coerced + bounded at the producer. |
| Migration ladder → existing v1 DBs in the field | Existing tasks rows pre-migration must remain queryable; new columns must default to safe values. |
| ironhermes-kanban ↔ ironhermes-core | Reserved-role list is a typed contract; mismatch silently disables a feature. The role-count guard test is the load-bearing invariant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.12-01-T01 | Tampering | `goal_max_turns` field accepts any u32 from the LLM tool / CLI / future caller | mitigate | Producer-level coercion in `KanbanStore::create_task`: `let goal_max_turns = if opts.goal_max_turns == 0 { 20 } else { opts.goal_max_turns };`. Phase 36.3.7.12 caps tested-by-the-loop-wrapper at 20 default; DEFCON-tier ceilings (D-07) explicitly deferred to a future phase. The u32 type prevents negatives at compile time. |
| T-36.3.7.12-01-T02 | Tampering | Migration ladder applied twice (partial re-run after a crash) | mitigate | `let _ = conn.execute("ALTER TABLE ... ADD COLUMN ...", [])` swallows "duplicate column" rusqlite error idempotently; `idempotent_re_run` test in `goal_mode_migration.rs` proves the contract holds. |
| T-36.3.7.12-01-D01 | Denial of Service | Schema migration locks the tasks table on first open on a large existing DB | accept | ALTER TABLE ADD COLUMN with DEFAULT is fast in SQLite (no row rewrite); existing kanban DBs in this codebase are bounded to hundreds-of-tasks scale. No mitigation beyond the wall-clock budget. |
| T-36.3.7.12-01-I01 | Information Disclosure | `Task.goal_mode` + `goal_turns_used` visible via `kanban_show` LLM tool | accept | Same data exposed via `hermes kanban show` CLI; iron_hermes_ui dashboard auth gate is the existing boundary (out of scope for this plan). No PII or secret material on these columns. |
| T-36.3.7.12-01-E01 | Elevation of Privilege | Reserved-role list mismatch could let a caller resolve `kanban_judge` to an unintended provider | mitigate | `ironhermes-core/src/config.rs:2547-2563` reserved-role guard test asserts the literal entry plus the exact count; a bypass attempt is caught at compile/test time. |
| T-36.3.7.12-01-SC | Tampering | npm/pip/cargo package installs | not-applicable | This plan adds ZERO new external registry packages; only workspace-internal path additions (judge.rs is in-tree). RESEARCH "Don't Hand-Roll" table + "Environment Availability" table confirm no new deps. |
| T-36.3.7.12-01-T03 | Tampering | `let _ = conn.execute(ALTER ...)` could silently mask a NON-duplicate-column rusqlite error (e.g., disk-full mid-migration) | mitigate | The subsequent `conn.execute("UPDATE schema_version SET version = 2", [])?` propagates with `?`; if the ALTER actually failed (not just duplicate-column), the schema_version row write also fails because the columns the next session reads don't exist → caller surfaces error on next open. Plus: the bilateral `v1_db_upgrades_to_v2` test directly asserts PRAGMA table_info shows the new columns, catching silent ALTER failures at test time. |

Block-on severity: high. All `mitigate` threats have a dedicated test in `goal_mode_migration.rs` or `goal_mode_persistence.rs`.
</threat_model>

<verification>
- `cargo build -p ironhermes-kanban` succeeds.
- `cargo build -p ironhermes-core` succeeds.
- `cargo test -p ironhermes-kanban --test goal_mode_migration` — three tests green (fresh, upgrade, idempotent).
- `cargo test -p ironhermes-kanban --test goal_mode_persistence` — three tests green (round-trip, 0→20 coerce, defaults).
- `cargo test -p ironhermes-kanban --lib judge::` — typedef anchor test green.
- `cargo test -p ironhermes-kanban --lib config::` — kanban-config Default test (now asserting empty judge_model) green.
- `cargo test -p ironhermes-core --lib` — reserved-role-count guard test bumped and green.
- `cargo clippy -p ironhermes-kanban -- -D warnings` clean.
- `cargo tree -p ironhermes-kanban --depth 1 | grep -c '^ironhermes-agent ' == 0` — crate-isolation fence holds.
- The two known-failing-at-base end_to_end tests (`full_lifecycle_via_tools_layer` line 61, `duplicate_completion_is_rejected` line 234) MUST NOT be touched or debugged — they are DEFER-1 per nd7 and remain failing.
</verification>

<success_criteria>
- All three tasks complete; verify commands return zero exit code (excluding the documented DEFER-1 failures).
- Decision Coverage: D-01 (DB columns + Task fields + CreateTaskOptions fields), D-05 (judge_model on KanbanConfig + reserved-role registration), D-06 (JudgeFn typedef ready for Plan 03/04 injection), D-08 (worker tool surface unchanged, crate-isolation fence intact).
- `must_haves.truths` all observable in the test surface.
- Plan 02 and Plan 03 (Wave 2) can begin in parallel after this plan lands.
</success_criteria>

<output>
Create `.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-01-SUMMARY.md` when done.
</output>
