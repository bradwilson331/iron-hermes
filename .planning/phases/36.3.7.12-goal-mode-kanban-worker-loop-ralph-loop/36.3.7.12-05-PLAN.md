---
phase: 36.3.7.12
plan: 05
type: execute
wave: 4
depends_on:
  - 36.3.7.12-01
  - 36.3.7.12-02
  - 36.3.7.12-03
  - 36.3.7.12-04
files_modified:
  - crates/ironhermes-kanban/tests/dispatcher_logic.rs
  - crates/ironhermes-kanban/src/worker_spawn.rs
  - crates/ironhermes-kanban/src/store.rs
  - crates/ironhermes-kanban/src/dispatcher.rs
  - skills/kanban-worker/SKILL.md
  - docs/kanban/reference.md
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-06
  - D-08
must_haves:
  truths:
    - "D-06: dispatcher_logic.rs `goal_mode_no_dispatcher_change` test claims a goal_mode=true card via the existing claim_and_spawn path; assertions confirm the dispatcher tick traversal (steps 0..8 per RESEARCH Finding 4) is identical to a goal_mode=false card — only the spawn-env Vec content differs"
    - "D-01/D-03/D-06: extending worker_spawn.rs tests, `goal_mode_env_vars_injected_end_to_end` builds a Task with goal_mode=true via KanbanStore::create_task, calls build_kanban_worker_env, asserts BOTH HERMES_KANBAN_GOAL_MODE=1 AND HERMES_KANBAN_GOAL_MAX_TURNS=<task.goal_max_turns> are present alongside the existing 8 HERMES_KANBAN_* vars (the 8+2=10 env contract)"
    - "Counter reset on reclaim: the existing reclaim path in dispatcher.rs (step 3 — `reclaim_stale_claims`) wires KanbanStore::reset_goal_turn_counter(task_id) so a reclaimed goal_mode card starts the next run at goal_turns_used=0. Bilateral test in dispatcher_logic.rs proves the reset; if a one-line addition to reclaim doesn't fit, the test instead documents the DDL DEFAULT 0 + CAS gate as the safety net (see Plan 02 Task 3 deferral note)"
    - "Handoff coverage — prior-run judge history survives in-place reclaim: `goal_mode_card_reclaim_preserves_judge_history` test asserts (a) `run_id_B != run_id_A` and `claim_lock_B != claim_lock_A` after reclaim+re-claim, (b) the card body (acceptance criteria the new judge will evaluate against) is byte-stable across reclaim, (c) `kanban_show(task_id)` on the second claim still returns the two judge_verdict events emitted under run_id_A. Locks the user's pasted-spec contract: 'when a worker is spawned (or re-spawned), it reads the full comment thread as part of its context'"
    - "Handoff coverage — consecutive-failures cap is honored or gap-documented: `goal_mode_card_consecutive_failures_caps_reclaim` test exercises the reclaim loop N+1 times (N = whatever cap the existing dispatcher enforces — likely `task.max_retries` or a `KanbanConfig` `max_consecutive_failures`). If the cap exists, asserts the (N+1)th reclaim transitions the card to BLOCKED (not infinite re-spawn). If no cap exists in the existing code, the test pivots to assert each reclaim increments `task.consecutive_failures` AND documents the absence-of-cap as pre-existing dispatcher gap DEFER-2 in SUMMARY.md (NOT introduced by goal_mode — goal_mode merely surfaces it because the reset means each respawn has a fresh budget)"
    - "Workspace gate: cargo build --workspace, cargo test --workspace, cargo clippy --workspace --features iron_hermes_ui/server -- -D warnings all exit 0 EXCLUDING the documented DEFER-1 end_to_end failures (full_lifecycle_via_tools_layer line 61, duplicate_completion_is_rejected line 234)"
    - "Skill doc updated: skills/kanban-worker/SKILL.md mentions HERMES_KANBAN_GOAL_MODE + HERMES_KANBAN_GOAL_MAX_TURNS env vars and the budget-exhaustion-as-block contract; does NOT create a new skill (D-08 — `kanban-worker` skill already bundled)"
    - "Reference doc updated: docs/kanban/reference.md describes goal_mode field on tasks + the --goal CLI flag + the judge LLM call site (matching the 36.3.7.7 and 36.3.7.8 phase precedents of updating reference.md alongside surface-changing phases)"
    - "Four manual UAT rows from VALIDATION.md §Manual-Only Verifications are executed by the user; each row's pass/fail recorded in the phase SUMMARY"
  artifacts:
    - path: "crates/ironhermes-kanban/tests/dispatcher_logic.rs"
      provides: "goal_mode_no_dispatcher_change test (dispatcher tick is byte-equivalent regardless of task.goal_mode) + reclaim_resets_goal_counter test + goal_mode_card_reclaim_preserves_judge_history test (prior-run judge_verdict events survive in-place reclaim; body byte-stable) + goal_mode_card_consecutive_failures_caps_reclaim test (or the cap-missing gap-documentation variant goal_mode_card_reclaim_increments_consecutive_failures)"
      contains: "goal_mode_no_dispatcher_change"
    - path: "crates/ironhermes-kanban/src/dispatcher.rs"
      provides: "Optional one-line reclaim_stale_claims addition: store.reset_goal_turn_counter(task_id) on goal_mode card reclaim"
      contains: "reset_goal_turn_counter"
    - path: "skills/kanban-worker/SKILL.md"
      provides: "Documentation of HERMES_KANBAN_GOAL_MODE + HERMES_KANBAN_GOAL_MAX_TURNS env vars + goal-loop semantics"
      contains: "HERMES_KANBAN_GOAL_MODE"
    - path: "docs/kanban/reference.md"
      provides: "Goal mode section describing per-card flag + CLI + tool + judge cascade + budget contract"
      contains: "goal_mode"
  key_links:
    - from: "crates/ironhermes-kanban/src/dispatcher.rs::reclaim_stale_claims"
      to: "crates/ironhermes-kanban/src/store.rs::reset_goal_turn_counter"
      via: "if task.goal_mode { store.reset_goal_turn_counter(task_id)?; }"
      pattern: "reset_goal_turn_counter"
    - from: "skills/kanban-worker/SKILL.md"
      to: "Worker LLM behavior when HERMES_KANBAN_GOAL_MODE is set"
      via: "Documentation prose"
      pattern: "HERMES_KANBAN_GOAL_MODE"
---

<objective>
Close the phase with integration tests, the reclaim-resets-counter wiring deferred from Plan 02, documentation amendments, and the four manual UAT signoffs from VALIDATION.md.

Four threads:

1. **Dispatcher passthrough test (D-06)** — extend `crates/ironhermes-kanban/tests/dispatcher_logic.rs` with `goal_mode_no_dispatcher_change`: drive the dispatcher tick against a synthetic board containing one goal_mode=true card and one goal_mode=false card; assert the tick traversal (8 ordered steps per RESEARCH Finding 4) emits the same event sequence for both cards EXCEPT for the spawn-env content (which Plan 02 covered separately).

2. **Reclaim-resets-counter wiring + handoff coverage (D-04 + Pitfall 4 mitigation)** — in `dispatcher.rs::reclaim_stale_claims` (step 3), add a single-line `if task.goal_mode { store.reset_goal_turn_counter(task_id)?; }` inside the existing transactional reclaim path. Bilateral test in dispatcher_logic.rs proves a reclaimed goal_mode card starts the next run with goal_turns_used=0. If the reclaim site is more invasive than a one-line addition (Plan 02 Task 3 deferral guidance), document the gap and rely on Plan 02's CAS gate + DDL DEFAULT 0 instead.

   **Two additional handoff-coverage tests close the carve-outs surfaced during planning review:**
   - `goal_mode_card_reclaim_preserves_judge_history` — locks that the in-place reclaim does NOT delete prior-run `task_events` (the new worker can read the previous run's `judge_verdict` events via `kanban_show` to understand what was tried), AND that the card body (the literal acceptance criteria the new judge will evaluate against) is byte-stable across reclaim.
   - `goal_mode_card_consecutive_failures_caps_reclaim` (or the gap-documentation variant `goal_mode_card_reclaim_increments_consecutive_failures` if no cap exists in the existing code) — closes the infinite-reclaim risk for goal_mode cards that consistently kill their worker mid-loop. Since `reset_goal_turn_counter` gives each respawn a fresh budget, an existing consecutive-failures cap is the only thing preventing infinite re-spawn. The test exercises N+1 reclaims and asserts either (a) the cap is enforced (card → BLOCKED at N+1) or (b) the cap is missing and the gap is logged as DEFER-2 (NOT introduced by goal_mode — pre-existing dispatcher behavior that goal_mode merely surfaces).

3. **Documentation** — amend `skills/kanban-worker/SKILL.md` (the bundled worker skill per D-08) with a "Goal mode" section describing HERMES_KANBAN_GOAL_MODE / HERMES_KANBAN_GOAL_MAX_TURNS env recognition, the implicit budget contract, and the synthetic-block exit semantics. Amend `docs/kanban/reference.md` with a "Goal mode" subsection mirroring the precedent set by Phases 36.3.7.7 and 36.3.7.8 (which both updated this file as part of phase close).

4. **Manual UAT (VALIDATION.md §Manual-Only Verifications)** — checkpoint task: human runs the four rows ("end-to-end goal-mode card", "budget exhaustion → BLOCKED", "judge errors → 2nd consecutive → BLOCKED", "auto_decompose + goal_mode interaction"). Records pass/fail. Confirms phase close.

Output:
- `crates/ironhermes-kanban/tests/dispatcher_logic.rs` (extended)
- `crates/ironhermes-kanban/src/dispatcher.rs` (one-line reclaim reset)
- `crates/ironhermes-kanban/src/store.rs` (no new code; reset_goal_turn_counter already landed in Plan 02)
- `skills/kanban-worker/SKILL.md` (Goal mode section)
- `docs/kanban/reference.md` (Goal mode subsection)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-CONTEXT.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-RESEARCH.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-VALIDATION.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-01-PLAN.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-02-PLAN.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-03-PLAN.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-04-PLAN.md
@crates/ironhermes-kanban/src/dispatcher.rs
@crates/ironhermes-kanban/tests/dispatcher_logic.rs
@docs/kanban/reference.md

<interfaces>
<!-- Contracts from prior plans + base. -->

From Plan 02 (now landed):
  impl KanbanStore {
      pub fn bump_goal_turn_counter(&self, task_id: &str, claim_lock: &str) -> anyhow::Result<bool>;
      pub fn reset_goal_turn_counter(&self, task_id: &str) -> anyhow::Result<()>;
  }

From crates/ironhermes-kanban/src/dispatcher.rs:229-312 (run_dispatch_tick_for_board, 8 ordered steps):
  - Step 3 is reclaim_stale_claims; the one-line addition lands inside its existing transaction.

From VALIDATION.md §Manual-Only Verifications (4 rows):
  1. End-to-end goal-mode card runs to completion via judge approval (D-02)
  2. Budget exhaustion → BLOCKED (D-03)
  3. Judge errors → 2nd consecutive → BLOCKED (D-04)
  4. auto_decompose + goal_mode interaction (D-01 / D-02)

From RESEARCH §State of the Art:
  - "Drop-and-recreate kanban tables" superseded by migration ladder — already addressed in Plan 01.

From phase 36.3.7.7 + 36.3.7.8 precedent (REQUIREMENTS.md REQ-36.3.7.7-09 + REQ-36.3.7.8-15):
  - Reference doc grep gates: e.g., `grep -c 'deferred to Phase 36.3.7.12' docs/kanban/reference.md` returns 0 if there were any such markers; `grep -c 'goal_mode' docs/kanban/reference.md` returns >= 1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dispatcher passthrough test + reclaim-resets-counter wiring + handoff-coverage bilateral tests</name>
  <files>crates/ironhermes-kanban/tests/dispatcher_logic.rs, crates/ironhermes-kanban/src/dispatcher.rs</files>
  <behavior>
    - `goal_mode_no_dispatcher_change`: drive `run_dispatch_tick_for_board` (or equivalent test entry — match what existing dispatcher_logic.rs tests use) against a board with two ready cards: card A with goal_mode=false, card B with goal_mode=true (same priority, same created_at within a tick). Assert the dispatcher tick traversal claims both cards via the same code path (same event types written, same step ordering), and assert the SpawnFn is called with both Task structs as arguments — the dispatcher does not inspect goal_mode to decide what to do.
    - `reclaim_resets_goal_counter`: simulate a goal_mode card that was claimed, had goal_turns_used bumped to e.g. 3, then had its heartbeat go stale; the reclaim path transitions the card back to READY and sets goal_turns_used=0. After reclaim, `store.get_task(task_id).goal_turns_used == 0`.
    - Alternative-if-deferred: if the one-line addition to reclaim_stale_claims is invasive enough to risk a separate refactor, the test instead validates the safety net — `bump_goal_turn_counter` rejects the stale claim_lock from the OLD run (already tested in Plan 02) and the new run's first bump_goal_turn_counter call sees goal_turns_used=0 because the column DEFAULT is 0 on the NEW row… BUT the row is the same row (reclaim is in-place, not insert-new). So if the one-line addition is deferred, the test instead asserts that the OLD run cannot bump the counter against the NEW run's claim_lock (Plan 02 contract) AND emits a `judge_error` advisory rather than corrupting state. Document the choice in SUMMARY.md.
    - `goal_mode_card_reclaim_preserves_judge_history`: locks the handoff contract for the in-place reclaim path. Create a goal_mode=true card; claim + capture run_id_A; bump goal counter twice; append two `KanbanEventKind::Edited` events with payload `{"subkind":"judge_verdict","verdict":"not_met","reason":"..."}` against run_id_A using the existing event-insert helper; force the claim stale; run reclaim; re-claim the same card and capture run_id_B (run_id_B != run_id_A). Assert `kanban_show(task_id)` on the SECOND claim still returns the two judge_verdict events from run_A in `task_events` (the reclaim does NOT delete the prior run's events). Assert the original `title` and `body` (the acceptance criteria the new judge will evaluate against) are byte-identical between the two claims. Mirrors the user's pasted-spec contract: "when a worker is spawned (or re-spawned), it reads the full comment thread as part of its context."
    - `goal_mode_card_consecutive_failures_caps_reclaim`: closes the infinite-reclaim risk for goal_mode cards that consistently kill their worker mid-loop. Create a goal_mode=true card; loop N+1 times (where N = the existing dispatcher's `max_consecutive_failures` cap — grep `crates/ironhermes-kanban/src/config.rs` and `dispatcher.rs` for `max_consecutive_failures` / `MAX_CONSECUTIVE_FAILURES` to find the canonical name; if it's a per-task limit on the Task row use `task.max_retries` instead): claim → bump counter once → force stale → reclaim. Assert: each reclaim increments `task.consecutive_failures` by 1; the (N+1)th reclaim transitions the card to `status='blocked'` (not back to READY) with a reason citing consecutive-failures cap (mirror whatever the existing dispatcher does for non-goal cards — the goal_mode flag does NOT bypass this cap). If the existing reclaim path does NOT increment consecutive_failures (i.e., this safety net does not exist for non-goal cards either), the test instead asserts the gap explicitly and the SUMMARY documents the carve-out as DEFER-2 (the absence-of-cap is not introduced by goal_mode; it's a pre-existing gap in the dispatcher's reclaim semantics that goal_mode merely surfaces).
  </behavior>
  <action>
    Step 1 — Extend `crates/ironhermes-kanban/tests/dispatcher_logic.rs` with `goal_mode_no_dispatcher_change()`. Pattern off any existing test in this file that calls `run_dispatch_tick_for_board` (or the test surface the dispatcher exposes). Build the board with two cards as described. Capture the SpawnFn invocations (likely the existing tests inject a mock SpawnFn that records calls — REUSE that idiom). Assert:
    - Both cards reach RUNNING state.
    - The event sequence for each card includes the same 'Claimed' / status-transition events (no goal-specific extra events at dispatcher step 6-8 — goal-specific events only land later, inside the worker process, from Plan 04's wrapper).
    - The mock SpawnFn was called twice, once per card; the second arg (Task) for card B has goal_mode=true.

    Step 2 — Look at `crates/ironhermes-kanban/src/dispatcher.rs::reclaim_stale_claims` (step 3 of the tick). The fn iterates stale claims and transitions them back to READY. The one-line addition: BEFORE (or AFTER — match the existing transactional boundary) the row's status update, add:
    ```
    if task.goal_mode {
        store.reset_goal_turn_counter(&task.id)?;
    }
    ```
    If the reclaim path operates on `&Task` ref → no field-access issue. If it operates only on `task_id` → fetch the task fresh inside the reclaim, then check goal_mode. The choice depends on the existing reclaim signature; mirror it.

    If the reclaim_stale_claims fn body is shaped such that injecting this addition requires a larger refactor (e.g., it operates entirely inside a closure that doesn't take &self / no store handle), DEFER per the Plan 02 Task 3 contract: leave the reclaim path unchanged, document the gap in this plan's SUMMARY, and rely on the bilateral test variant from "Behavior" above that validates the CAS gate as the safety net.

    Step 3 — Extend dispatcher_logic.rs with `reclaim_resets_goal_counter()`:
    - Open a store; create a goal_mode=true task with goal_max_turns=10; claim it (capture claim_lock + run_id).
    - Bump the goal counter twice via `store.bump_goal_turn_counter(task_id, &claim_lock)` (sanity-check: get_task shows goal_turns_used=2).
    - Force the claim stale: directly bump the claim heartbeat to e.g. 5 hours ago via a test-only helper, OR use whatever sleep/clock-manipulation idiom existing dispatcher tests use.
    - Run `reclaim_stale_claims` (call the same entry the dispatcher tick uses — if it's not pub, use `run_dispatch_tick_for_board` with the steps gated appropriately, or whatever the existing tests do).
    - Assert get_task(task_id) shows status=READY AND goal_turns_used=0.

    If the wiring was deferred per Step 2's escape hatch, the test asserts the safety-net variant instead.

    Step 4 — Honor `crate::ENV_LOCK` if any of these tests set env vars (likely not).

    Step 5 — Extend `crates/ironhermes-kanban/tests/dispatcher_logic.rs` with `goal_mode_card_reclaim_preserves_judge_history()`:
    - Open store; create goal_mode=true task with goal_max_turns=10 and `body="Acceptance: produce a haiku"` (or any non-trivial body content).
    - Claim → capture `run_id_A` + `claim_lock_A`.
    - `store.bump_goal_turn_counter(task_id, &claim_lock_A)?;` twice.
    - Append two judge_verdict events tied to run_id_A. Use the existing event-write path the rest of the codebase uses — likely `KanbanStore` has a `append_event(...)` helper (grep `crates/ironhermes-kanban/src/store.rs` for `append_event` or the SQL `INSERT INTO task_events`). The payloads are JSON: `{"subkind":"judge_verdict","verdict":"not_met","reason":"missing line 2"}` and `{"subkind":"judge_verdict","verdict":"not_met","reason":"missing line 3"}`. Kind = `KanbanEventKind::Edited` (frozen surface).
    - Force claim stale (same time-warp idiom as `reclaim_resets_goal_counter` — bump heartbeat back 5h).
    - Run reclaim entry (same as the prior test).
    - Re-claim the card → capture `run_id_B` and `claim_lock_B`; assert `run_id_B != run_id_A` AND `claim_lock_B != claim_lock_A`.
    - Snapshot the task body BEFORE reclaim and AFTER second claim → assert byte-equal (`assert_eq!(body_before, body_after)`).
    - Call `kanban_show(task_id)` (or the underlying `store.list_events_for_task(task_id)` — match what `kanban_show` actually calls) → assert the returned events still contain BOTH judge_verdict events from run_id_A. The reclaim does NOT delete prior-run task_events; that is the handoff contract.

    Step 6 — Extend `crates/ironhermes-kanban/tests/dispatcher_logic.rs` with `goal_mode_card_consecutive_failures_caps_reclaim()`:
    - First, locate the cap: grep `crates/ironhermes-kanban/src/config.rs`, `dispatcher.rs`, and `types.rs` for `consecutive_failures`, `max_consecutive_failures`, `max_retries`, `MAX_CONSECUTIVE_FAILURES`. The cap is most likely either a `KanbanConfig` field with a `default_*` helper fn, or it lives on the `Task` row as `max_retries` (already in the schema per `types.rs:28` Task struct fields). Use whichever name the existing reclaim path enforces.
    - If no such cap exists in the existing code (i.e., the dispatcher will infinitely reclaim a card that keeps going stale), pivot to the gap-documentation variant: write the test as `goal_mode_card_reclaim_increments_consecutive_failures` instead, assert that each reclaim DOES increment `task.consecutive_failures` (this much is likely already true — Phase 36.3.7.5 wiring); then explicitly document in SUMMARY.md that the absence of an enforced cap is a pre-existing dispatcher gap (NOT introduced by goal_mode) and file as DEFER-2.
    - Body for the cap-exists branch: create goal_mode=true card with `max_retries=2` (or whatever cap-setting verb the existing code uses). Then loop: claim → bump counter → force stale → reclaim. After each reclaim, assert `task.consecutive_failures` incremented by 1. After the 3rd (= N+1 where N=2) reclaim, assert `task.status == "blocked"` (NOT "ready") and the blocked reason cites the consecutive-failures cap. The goal_mode flag is irrelevant to the cap behavior — that's the point of the test.
    - Body for the cap-missing branch: create the card; loop 5 times (claim → bump → stale → reclaim); after the 5th iteration, assert `task.status == "ready"` (the gap is observable) AND `task.consecutive_failures >= 5`. Then write the SUMMARY.md DEFER-2 note explicitly.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo test -p ironhermes-kanban --test dispatcher_logic -- goal_mode_no_dispatcher_change reclaim_resets_goal_counter goal_mode_card_reclaim_preserves_judge_history goal_mode_card_consecutive_failures_caps_reclaim goal_mode_card_reclaim_increments_consecutive_failures 2>&1 | tail -30 && grep -c 'goal_mode_no_dispatcher_change' crates/ironhermes-kanban/tests/dispatcher_logic.rs | xargs test 1 -eq && grep -c 'reclaim_resets_goal_counter\|reclaim.*goal' crates/ironhermes-kanban/tests/dispatcher_logic.rs | xargs -I {} test {} -ge 1 && grep -cE 'goal_mode_card_reclaim_preserves_judge_history' crates/ironhermes-kanban/tests/dispatcher_logic.rs | xargs test 1 -eq && grep -cE 'goal_mode_card_consecutive_failures_caps_reclaim|goal_mode_card_reclaim_increments_consecutive_failures' crates/ironhermes-kanban/tests/dispatcher_logic.rs | xargs -I {} test {} -ge 1</automated>
  </verify>
  <done>
    All four new tests green (goal_mode_no_dispatcher_change, reclaim_resets_goal_counter, goal_mode_card_reclaim_preserves_judge_history, and one of the consecutive_failures variants — cap-enforced or gap-documented). Dispatcher passthrough verified. Reclaim path either wires reset_goal_turn_counter (preferred) or documents the deferral with the safety-net test variant. Handoff coverage locked: prior-run judge_verdict events survive in-place reclaim; card body (acceptance criteria) is byte-stable; consecutive-failures cap is either enforced (test asserts the cap) or documented as pre-existing dispatcher gap DEFER-2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Documentation — kanban-worker skill amendment + reference.md Goal mode section</name>
  <files>skills/kanban-worker/SKILL.md, docs/kanban/reference.md</files>
  <behavior>
    - `skills/kanban-worker/SKILL.md` gains a "Goal mode" section. Mentions HERMES_KANBAN_GOAL_MODE / HERMES_KANBAN_GOAL_MAX_TURNS env vars; explains that when set, the worker shell wraps AgentLoop::run in a budget-bounded loop with judge LLM evaluation; pins the budget-exhaustion-as-block contract (D-03); does NOT introduce a new skill (the file already exists per D-08).
    - `docs/kanban/reference.md` gains a "Goal mode" subsection. Describes: per-card goal_mode + goal_max_turns columns; --goal + --goal-max-turns CLI flags; goal_mode + goal_max_turns args on the kanban_create LLM tool; judge_model config field; three-tier judge model resolution; goal-loop event subkinds (judge_verdict, judge_error, goal_turn_advanced, goal_budget_exhausted); RAII budget exhaustion → kanban_block contract.
    - reference.md grep gates: `grep -c 'goal_mode' docs/kanban/reference.md` returns >= 3; `grep -c 'HERMES_KANBAN_GOAL_MODE' docs/kanban/reference.md` returns >= 1; `grep -c 'judge_model' docs/kanban/reference.md` returns >= 1.
  </behavior>
  <action>
    Step 1 — Locate `skills/kanban-worker/SKILL.md` (if the exact path differs, grep the skills/ tree for kanban-worker). Append (or insert near a sensible existing section) a "## Goal mode" section with prose along these lines (write the prose verbatim — do not invent capabilities):

    ```
    ## Goal mode

    Cards created with `goal_mode=true` opt into an in-session worker loop with
    automatic judge LLM evaluation. The dispatcher claims goal-mode cards the
    normal way and spawns the worker the normal way. The worker shell detects
    goal mode via two env vars set by the spawner:

    - `HERMES_KANBAN_GOAL_MODE=1` — flag indicating the worker should wrap
      `AgentLoop::run` in a budget-bounded loop.
    - `HERMES_KANBAN_GOAL_MAX_TURNS=<N>` — per-card turn budget. Default 20.

    On each turn the worker:
    1. Runs one `AgentLoop::run` iteration in the same session.
    2. Bumps the per-card turn counter under a CAS gate (reclaimed workers cannot
       bump against a stale run).
    3. Invokes an auxiliary judge LLM that evaluates the worker's output against
       the card's `title + body` (literal acceptance criteria).
    4. On judge-met → loop exits cleanly; the worker LLM is expected to call
       `kanban_complete` next.
    5. On judge-not-met → a synthetic user message carrying the judge's reason
       is appended to the chat and the next turn begins.
    6. On budget exhaustion → the worker shell emits a synthetic
       `kanban_block(reason="goal_max_turns exhausted; needs human review")` so
       the card lands in BLOCKED for human review, never silently dropped.

    Two consecutive judge errors → synthetic `kanban_block(reason="judge unavailable")`.

    Goal-loop events ride on `KanbanEventKind::Edited` with JSON subkind tags:
    `judge_verdict`, `judge_error`, `goal_turn_advanced`, `goal_budget_exhausted`.
    No new event variants are introduced (frozen surface preserved).
    ```

    Step 2 — Locate `docs/kanban/reference.md`. Find a natural anchor — likely after the auto_decompose / decomposer section, or after the tools table. Insert a "## Goal mode" subsection covering:
    - The new tasks columns `goal_mode INTEGER NOT NULL DEFAULT 0`, `goal_max_turns INTEGER NOT NULL DEFAULT 20`, `goal_turns_used INTEGER NOT NULL DEFAULT 0` and the v1→v2 migration ladder note.
    - CLI: `ironhermes kanban create --goal [--goal-max-turns N]`.
    - LLM tool: `kanban_create` accepts `goal_mode: bool` and `goal_max_turns: u32`.
    - Config: `kanban.judge_model: String` mirroring `decomposer_model`. Empty → `auxiliary.kanban_judge` → main provider three-tier cascade.
    - Worker shell env vars `HERMES_KANBAN_GOAL_MODE` + `HERMES_KANBAN_GOAL_MAX_TURNS` (set by `build_kanban_worker_env`).
    - Goal-loop event subkinds (the four CONST tags).
    - Budget-exhaustion-as-BLOCKED contract (D-03).
    - Reference to the canonical-usage example from CONTEXT.md §specifics:
      ```
      ironhermes kanban create "Translate the docs site to French" \
        --body "Acceptance: every page translated, no English left, links intact." \
        --assignee linguist \
        --goal \
        --goal-max-turns 15
      ```

    If reference.md uses a numbered-section convention (e.g., §200 table, §664 reference per phase 36.3.7.7's REQ-36.3.7.7-09), respect it: add a row to the tools table for kanban_create's new args if such a table exists, and update any "all N LLM tools" inline count if it appears.

    Step 3 — Do NOT introduce any "TBD" or "deferred to Phase XX" placeholders; if a sub-topic is truly out of scope (DEFCON-tier ceilings per D-07), note it once with a sentence and move on.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && grep -c 'HERMES_KANBAN_GOAL_MODE' skills/kanban-worker/SKILL.md | xargs -I {} test {} -ge 1 && grep -c 'goal_mode' docs/kanban/reference.md | xargs -I {} test {} -ge 3 && grep -c 'HERMES_KANBAN_GOAL_MODE' docs/kanban/reference.md | xargs -I {} test {} -ge 1 && grep -c 'judge_model' docs/kanban/reference.md | xargs -I {} test {} -ge 1 && grep -c 'Goal mode' skills/kanban-worker/SKILL.md | xargs -I {} test {} -ge 1</automated>
  </verify>
  <done>
    Both documentation files updated. Grep gates green. No "TBD" placeholders. No new skill file. The kanban-worker bundled skill carries the goal-loop semantics.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Workspace gate — full build / test / clippy across the workspace</name>
  <files>none (verification only)</files>
  <behavior>
    - `cargo build --workspace` exits 0.
    - `cargo test --workspace` exits 0 EXCEPT for the two documented DEFER-1 failures in `crates/ironhermes-kanban/tests/end_to_end.rs` (lines 61 + 234).
    - `cargo clippy --workspace --features iron_hermes_ui/server -- -D warnings` exits 0.
    - No NEW workspace-wide test failures introduced by this phase. If a NEW failure appears, executor stops and diagnoses (do NOT proceed to UAT until workspace is green excluding DEFER-1).
  </behavior>
  <action>
    Step 1 — Run `cargo build --workspace` from the repo root. If errors appear, stop and fix (likely a missed import, a clippy lint, or a forgotten re-export from prior plans).

    Step 2 — Run `cargo test --workspace`. Capture the output. Identify:
    - GREEN tests: count tracked.
    - The two DEFER-1 failures at end_to_end.rs:61 and end_to_end.rs:234 — EXPECTED, do not investigate.
    - Any OTHER failures: STOP. These are phase-introduced regressions; diagnose and fix before continuing.

    Step 3 — Run `cargo clippy --workspace --features iron_hermes_ui/server -- -D warnings`. Per `.planning/phases/36.3.7.12-VALIDATION.md` Dimension 7, this is the load-bearing gate. Fix any warnings — even `unused_imports` from new wiring code — before declaring done.

    Step 4 — Record the test count + DEFER-1 acknowledgment in the SUMMARY artifact for this plan.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo build --workspace 2>&1 | tail -10 && cargo test --workspace 2>&1 | tail -40 && cargo clippy --workspace --features iron_hermes_ui/server -- -D warnings 2>&1 | tail -20</automated>
  </verify>
  <done>
    Workspace builds. Workspace tests green except DEFER-1. Workspace clippy clean with -D warnings.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT — four manual verifications from VALIDATION.md §Manual-Only Verifications</name>
  <what-built>
    The full goal-mode pipeline is now landed:
    - DB columns + Task fields (Plan 01)
    - kanban_create LLM tool surface + env-var injection + CAS-gated counter (Plan 02)
    - CLI flags + judge LLM factory (Plan 03)
    - Goal-loop wrapper + RAII BudgetSentinel (Plan 04)
    - Dispatcher passthrough + reclaim reset + docs (Plan 05 Tasks 1-2)
    - Workspace gate green excluding DEFER-1 (Plan 05 Task 3)

    The user (operator) now runs four end-to-end smoke tests against a live LLM
    provider that the user has configured for `kanban.judge_model` (or relies on
    the cascade fallback to the main provider).
  </what-built>
  <how-to-verify>
    Run each row from VALIDATION.md §Manual-Only Verifications IN ORDER. Record
    pass/fail in this plan's SUMMARY:

    **Row 1 — End-to-end goal-mode card runs to completion via judge approval (D-02)**
    1. `cargo run -p ironhermes-cli -- kanban create --goal --goal-max-turns 5 --assignee dev --body "Acceptance: write a haiku about Rust" "Write haiku"`
    2. Wait for the dispatcher tick to claim and spawn the card. (Confirm the dispatcher is actually running for your profile — `ironhermes kanban dispatcher status` or whatever the project's command is.)
    3. `cargo run -p ironhermes-cli -- kanban show <id>` reports `status: done` and `goal_turns_used` is at most 5.
    4. The comment thread / event log for the card contains at least one `judge_verdict` subkind event with `verdict: "met"`.
    PASS criteria: status=done AND goal_turns_used <= 5 AND at least one judge_verdict event present.

    **Row 2 — Budget exhaustion → BLOCKED (D-03)**
    1. `cargo run -p ironhermes-cli -- kanban create --goal --goal-max-turns 2 --assignee dev --body "Acceptance: produce output that contains the literal string ASDFXYZ and only that string, no other text" "Impossible task"`
       (The judge will never agree — by design.)
    2. Wait for the worker to burn 2 turns.
    3. `cargo run -p ironhermes-cli -- kanban show <id>` reports `status: blocked` with reason `goal_max_turns exhausted; needs human review`.
    4. The event log contains a `goal_budget_exhausted` subkind event.
    5. The event log does NOT contain a `protocol_violation` event (silent-exit guard from RESEARCH Pitfall 5).
    PASS criteria: status=blocked AND reason matches AND goal_budget_exhausted event present AND no protocol_violation event.

    **Row 3 — Judge errors → 2nd consecutive → BLOCKED (D-04)**
    1. Set `kanban.judge_model` in your config to a non-existent model ID (e.g., `model-that-definitely-does-not-exist-xyz`).
    2. `cargo run -p ironhermes-cli -- kanban create --goal --goal-max-turns 10 --assignee dev --body "Acceptance: say hello" "Test judge errors"`
    3. Wait. The worker fires turn 0 → judge_error event lands. Turn 1 → second judge_error event lands. Worker calls synthetic kanban_block.
    4. `cargo run -p ironhermes-cli -- kanban show <id>` reports `status: blocked` with reason `judge unavailable`.
    5. Event log has exactly two `judge_error` subkind events for this run.
    6. RESTORE your config (revert the bogus judge_model).
    PASS criteria: status=blocked AND reason matches AND exactly two judge_error events AND no goal_budget_exhausted event for this card.

    **Row 4 — auto_decompose + goal_mode interaction (D-01 / D-02)**
    1. Ensure `auto_decompose: true` is set in the kanban config (Phase 36.3.7.10 contract).
    2. `cargo run -p ironhermes-cli -- kanban create --triage --goal --goal-max-turns 5 --assignee dev --body "Acceptance: complete the decomposed subtask" "Triage candidate"`
    3. The auto_decompose path promotes the card TRIAGE → TODO normally.
    4. `cargo run -p ironhermes-cli -- kanban show <id>` after the promotion shows `goal_mode: true` is preserved on the TODO row (the rewrite did not clobber the flag).
    5. The dispatcher claims the card normally; the worker enters the goal loop on its first run.
    PASS criteria: TRIAGE → TODO promotion happens AND goal_mode survives the rewrite AND goal loop fires on the first run.

    Record each row's outcome (pass / fail / partial / blocked) in the SUMMARY.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all four rows pass. If any row fails, describe the failure mode (which row, what behavior was observed, any DB / event-log evidence). For partial passes (e.g., row 4 happened but goal_mode flag did not survive), capture the specific gap so the executor can open a follow-up phase.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → live LLM provider | UAT rows 1-3 require actual provider calls; provider responses are untrusted but caged by the wrapper's JSON parse + reason truncation. |
| Operator → kanban DB | UAT rows mutate the DB directly via CLI + worker; the existing kanban auth model (D-22 of phase 36.3.7.11, file-permission-bound) is the boundary. |
| Documentation → future readers | Reference doc text drives downstream consumers' (skills authors, integrators) understanding; misdocumentation here propagates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.12-05-D01 | Denial of Service | UAT row 1 burns provider quota on a haiku that never satisfies a judge under-prompted | mitigate | `--goal-max-turns 5` caps the burn. Plan 04's BudgetSentinel guarantees exit at turn 5 regardless of judge behavior. |
| T-36.3.7.12-05-T01 | Tampering | UAT row 2's adversarial body ("produce ASDFXYZ only") could trick the judge into "met" via prompt injection | mitigate | The judge system prompt is static and trusted; even a successful injection in the body would still funnel through the JSON verdict parser. If the row 2 result is "status=done" instead of "status=blocked", that itself is a finding — record in SUMMARY and open a follow-up. |
| T-36.3.7.12-05-T02 | Tampering | UAT row 3 leaves a bogus judge_model in user config if RESTORE step is skipped | mitigate | Step 6 of row 3 explicitly says RESTORE. Checkpoint prompt instructs the user to record completion of step 6. |
| T-36.3.7.12-05-I01 | Information Disclosure | UAT rows reveal provider API key behavior to logs | accept | Standard ironhermes provider config behavior; goal_mode does not change the log surface. RESEARCH §Security V12 confirms env-scrub at worker_spawn.rs:63. |
| T-36.3.7.12-05-T03 | Tampering | docs/kanban/reference.md reference-doc grep gates could silently regress if a future phase removes "goal_mode" | accept | The grep gates encoded in this plan's <verify> automated block catch the case; future phases that touch reference.md would re-run their own gates. |
| T-36.3.7.12-05-SC | Tampering | npm/pip/cargo package installs | not-applicable | Plan adds ZERO new external packages. |

Block-on severity: high. T01 is the highest-risk UAT finding-class; the checkpoint prompt instructs the user to record the row 2 outcome explicitly.
</threat_model>

<verification>
- `cargo test -p ironhermes-kanban --test dispatcher_logic -- goal_mode` — two new tests green.
- `cargo build --workspace` exits 0.
- `cargo test --workspace` exits 0 except for the two DEFER-1 failures (`full_lifecycle_via_tools_layer` line 61, `duplicate_completion_is_rejected` line 234).
- `cargo clippy --workspace --features iron_hermes_ui/server -- -D warnings` exits 0.
- `skills/kanban-worker/SKILL.md` contains "Goal mode" section + HERMES_KANBAN_GOAL_MODE mention.
- `docs/kanban/reference.md` contains goal_mode (≥3 occurrences), HERMES_KANBAN_GOAL_MODE (≥1), judge_model (≥1).
- Four UAT rows from VALIDATION.md executed by the operator; outcomes recorded in SUMMARY.
- No NEW workspace test failures introduced by this phase.
</verification>

<success_criteria>
- All four tasks complete; Tasks 1-3 verify commands return zero exit (excluding DEFER-1); Task 4 captures the operator's UAT signoff.
- Decision coverage: D-01/D-02/D-03/D-04/D-06/D-08 all surfaced in either tests or documentation.
- `must_haves.truths` all observable.
- Phase 36.3.7.12 closes with: 5 plans, ~14 tasks, 3 new files (judge.rs, goal_loop.rs, and the new test files), ~5 small additions to existing surfaces (per RESEARCH "Don't Hand-Roll" key insight).
- The ROADMAP.md Phase 36.3.7.12 entry can flip from "INSERTED — pending plan" to "PLANNED — 5 plans" upon this plan's PLAN.md write (handled by the planner's update_roadmap step, not this plan).
</success_criteria>

<output>
Create `.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-05-SUMMARY.md` when done. The phase SUMMARY (one level up — phase-wide, not plan-wide) is composed by `/gsd:verify-work` after all five plans land.
</output>
