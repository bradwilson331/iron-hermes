---
phase: 36.3.7.12
plan: 02
type: execute
wave: 2
depends_on:
  - 36.3.7.12-01
files_modified:
  - crates/ironhermes-kanban/src/tools/create.rs
  - crates/ironhermes-kanban/src/worker_spawn.rs
  - crates/ironhermes-kanban/src/cas.rs
  - crates/ironhermes-kanban/src/store.rs
  - crates/ironhermes-kanban/src/events.rs
  - crates/ironhermes-kanban/src/lib.rs
  - crates/ironhermes-kanban/tests/goal_judge_event_subkind.rs
  - crates/ironhermes-kanban/tests/cas_concurrency.rs
  - crates/ironhermes-kanban/tests/tools_smoke.rs
autonomous: true
requirements:
  - D-01
  - D-03
  - D-04
  - D-06
  - D-08
must_haves:
  truths:
    - "D-01: KanbanCreateTool::schema() JSON contains goal_mode (boolean, default false) + goal_max_turns (integer, default 20) under properties; existing schema_contains_board_property test (create.rs:334) still passes"
    - "D-01: KanbanCreateTool::execute() with JSON args {goal_mode:true, goal_max_turns:5, ...} persists a row with goal_mode=true and goal_max_turns=5"
    - "D-03/D-06: worker_spawn::build_kanban_worker_env appends HERMES_KANBAN_GOAL_MODE=1 and HERMES_KANBAN_GOAL_MAX_TURNS=<n> ONLY when task.goal_mode==true; for goal_mode=false tasks the env is unchanged (existing 8-var contract preserved per INV-36.3.7-07)"
    - "D-04/D-06: KanbanStore::bump_goal_turn_counter wraps worker_write_gated; a stale claim_lock causes the counter to NOT advance and the helper returns Ok(false) (no panic)"
    - "D-04: subkind tag strings 'judge_verdict' / 'judge_error' / 'goal_turn_advanced' / 'goal_budget_exhausted' are PINNED as pub const &str in events.rs; ZERO new KanbanEventKind variants land (frozen surface preserved)"
    - "D-08: SAFE_SYSTEM_VARS allowlist at worker_spawn.rs:63 unchanged; new env vars ride the existing env_clear+allowlist path; no provider API key can leak through the goal env additions"
  artifacts:
    - path: "crates/ironhermes-kanban/src/tools/create.rs"
      provides: "kanban_create JSON schema fields goal_mode + goal_max_turns + execute wiring through CreateTaskOptions"
      contains: "goal_mode"
    - path: "crates/ironhermes-kanban/src/worker_spawn.rs"
      provides: "HERMES_KANBAN_GOAL_MODE + HERMES_KANBAN_GOAL_MAX_TURNS env append on task.goal_mode"
      contains: "HERMES_KANBAN_GOAL_MODE"
    - path: "crates/ironhermes-kanban/src/store.rs"
      provides: "KanbanStore::bump_goal_turn_counter(task_id, claim_lock) -> Result<bool>"
      contains: "bump_goal_turn_counter"
    - path: "crates/ironhermes-kanban/src/events.rs"
      provides: "Four pub const &str subkind tags + frozen-surface doc comment"
      contains: "GOAL_SUBKIND_JUDGE_VERDICT"
    - path: "crates/ironhermes-kanban/tests/goal_judge_event_subkind.rs"
      provides: "Producer-side test that subkind payload round-trips and frozen-surface variant count holds"
      contains: "judge_verdict"
  key_links:
    - from: "crates/ironhermes-kanban/src/tools/create.rs::execute"
      to: "crates/ironhermes-kanban/src/store.rs::create_task"
      via: "CreateTaskOptions { goal_mode, goal_max_turns, .. }"
      pattern: "goal_mode"
    - from: "crates/ironhermes-kanban/src/worker_spawn.rs::build_kanban_worker_env"
      to: "spawned worker process environment"
      via: "task.goal_mode conditional env.push of HERMES_KANBAN_GOAL_* pair"
      pattern: "HERMES_KANBAN_GOAL_MODE"
    - from: "crates/ironhermes-kanban/src/store.rs::bump_goal_turn_counter"
      to: "crates/ironhermes-kanban/src/cas.rs::worker_write_gated"
      via: "closure UPDATE tasks SET goal_turns_used = goal_turns_used + 1 WHERE id=?1"
      pattern: "worker_write_gated"
---

<objective>
Land the kanban-crate-side producer surface for Goal Mode. Three independent additions, all inside `ironhermes-kanban` (crate-isolation fence holds per D-08):

1. **`kanban_create` LLM tool schema** — adds `goal_mode` (boolean, default false) and `goal_max_turns` (integer, default 20) to the JSON Schema at `tools/create.rs:96-167`. The execute path at `tools/create.rs:240-271` reads the JSON args and threads them through `CreateTaskOptions` (the struct already carries the fields after Plan 01). NO new tool registered.

2. **`build_kanban_worker_env` env-var injection** — when `task.goal_mode == true`, the env list returned by `worker_spawn.rs:108-145` gains `HERMES_KANBAN_GOAL_MODE=1` AND `HERMES_KANBAN_GOAL_MAX_TURNS=<task.goal_max_turns>`. When `task.goal_mode == false` (every existing card), the env list is unchanged.

3. **`bump_goal_turn_counter` CAS-gated store helper + frozen-surface subkind tag CONSTs** — adds `KanbanStore::bump_goal_turn_counter(task_id, claim_lock) -> Result<bool>` to `store.rs` that wraps `cas::worker_write_gated` (cas.rs:241-277) with `UPDATE tasks SET goal_turns_used = goal_turns_used + 1 WHERE id=?1`. Pins four subkind tag strings (`judge_verdict`, `judge_error`, `goal_turn_advanced`, `goal_budget_exhausted`) as `pub const` in `events.rs` so Plan 04's wrapper imports them by symbol instead of string-literal duplication.

Purpose: Plan 03 (CLI surface) and Plan 04 (goal loop wrapper) need (a) the env vars to read, (b) the create-tool schema to accept the flag from the LLM caller, (c) the bump helper to write counter advances under CAS, and (d) the subkind tag CONSTs to avoid string drift. All four are exercised by producer-side tests inside `ironhermes-kanban` with zero agent-crate dep.

Output:
- `crates/ironhermes-kanban/src/tools/create.rs` (schema + execute wiring)
- `crates/ironhermes-kanban/src/worker_spawn.rs` (env conditional append + unit tests)
- `crates/ironhermes-kanban/src/store.rs` (bump_goal_turn_counter)
- `crates/ironhermes-kanban/src/events.rs` (4 pub const subkind tags + doc)
- `crates/ironhermes-kanban/src/lib.rs` (re-exports)
- `crates/ironhermes-kanban/tests/goal_judge_event_subkind.rs` (new bilateral producer test)
- Extend `crates/ironhermes-kanban/tests/cas_concurrency.rs` (stale-claim turn-bump test)
- Extend `crates/ironhermes-kanban/tests/tools_smoke.rs` (create_goal_mode subtest)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-CONTEXT.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-RESEARCH.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-VALIDATION.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-01-PLAN.md
@crates/ironhermes-kanban/src/tools/create.rs
@crates/ironhermes-kanban/src/worker_spawn.rs
@crates/ironhermes-kanban/src/cas.rs
@crates/ironhermes-kanban/src/store.rs
@crates/ironhermes-kanban/src/events.rs

<interfaces>
<!-- Key contracts extracted from the codebase. -->

From crates/ironhermes-kanban/src/tools/create.rs:96-167 (schema fn):
  fn schema(&self) -> ToolSchema { /* JSON-Schema for kanban_create */ }

From crates/ironhermes-kanban/src/tools/create.rs:240-271 (execute — CreateTaskOptions instantiation site):
  let opts = CreateTaskOptions { /* existing fields */ };

From crates/ironhermes-kanban/src/worker_spawn.rs:108-145 (env build site):
  fn build_kanban_worker_env(...) -> Vec<(String, String)> {
      // 8 existing HERMES_KANBAN_* env vars. Append HERMES_KANBAN_GOAL_* when task.goal_mode.
  }
And worker_spawn.rs:63 (SAFE_SYSTEM_VARS allowlist — DO NOT MODIFY).

From crates/ironhermes-kanban/src/cas.rs:241-277 (worker_write_gated):
  pub fn worker_write_gated<F>(conn: &mut Connection, task_id: &str, claim_lock: &str, write: F) -> Result<bool>
  where F: FnOnce(&Transaction) -> Result<()>;

From crates/ironhermes-kanban/src/events.rs (FROZEN ENUM — Phase 36.3.7.6):
  // KanbanEventKind variants are frozen. Goal-loop events ride on
  // KanbanEventKind::Edited + JSON payload { "subkind": "<tag>", ... }.

Tag strings (pinned per RESEARCH "Claude's Discretion"):
  - "judge_verdict"          — judge said met / not_met for one turn
  - "judge_error"            — judge LLM call failed
  - "goal_turn_advanced"     — turn counter bumped
  - "goal_budget_exhausted"  — synthetic kanban_block on budget hit
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: kanban_create JSON schema + execute threads goal_mode/goal_max_turns into CreateTaskOptions</name>
  <files>crates/ironhermes-kanban/src/tools/create.rs, crates/ironhermes-kanban/tests/tools_smoke.rs</files>
  <behavior>
    - KanbanCreateTool::schema() JSON contains under "properties" the two keys "goal_mode" (type boolean, default false) and "goal_max_turns" (type integer, default 20).
    - KanbanCreateTool::execute() with JSON args {"title":..., "assignee":..., "goal_mode":true, "goal_max_turns":5} creates a row with goal_mode=true and goal_max_turns=5.
    - Omitted goal_mode defaults to false. Omitted goal_max_turns or goal_max_turns=0 with goal_mode=true coerces to 20 via the producer-level coercion landed in Plan 01.
    - Existing schema_contains_board_property test at create.rs:334 still passes.
  </behavior>
  <action>
    Step 1 — In `KanbanCreateTool::schema()` at create.rs:96-167, append two entries to the `properties` JSON object (grouped near `triage` per Finding 7 layout):
    `"goal_mode": { "type": "boolean", "description": "Enable goal mode: worker enters in-session loop with judge LLM evaluating against title + body.", "default": false }` and
    `"goal_max_turns": { "type": "integer", "description": "Per-task turn budget when goal_mode=true. Budget exhaustion → kanban_block. Default 20.", "default": 20 }`. Neither field goes in `required` — both have defaults.

    Step 2 — In `execute()` at create.rs:240-271, where `CreateTaskOptions { ... }` is constructed from parsed JSON args, add two field bindings: `goal_mode: args.get("goal_mode").and_then(|v| v.as_bool()).unwrap_or(false)` and `goal_max_turns: args.get("goal_max_turns").and_then(|v| v.as_u64()).map(|n| n as u32).unwrap_or(0)`. The `0` fallback intentionally hits the producer-level coercion in Plan 01 (`if opts.goal_max_turns == 0 { 20 }`) so default-of-20 lands consistently.

    Step 3 — Extend `crates/ironhermes-kanban/tests/tools_smoke.rs` (DO NOT create a new file). Add two tests:
    `create_goal_mode_sets_columns()` — async, mirrors the test scaffolding at create.rs:316-329. Build JSON args `{"title":"Translate docs","assignee":"alice","body":"Acceptance: ...","goal_mode":true,"goal_max_turns":5}`. Call `tool.execute(&args).await`, assert Ok, then `store.get_task(id)` returns Task with goal_mode=true, goal_max_turns=5, goal_turns_used=0.
    `schema_contains_goal_properties()` — sync. Serialize `tool.schema()` to a JSON string; assert it contains `"goal_mode"`, `"goal_max_turns"`, and `"default":20`.

    Step 4 — If either new test mutates `std::env`, hold `crate::ENV_LOCK.lock()` per RESEARCH Finding 10 (heartbeat.rs:169 idiom). The default execute path should not need env mutation.

    Surface guard: the tool count in `tools/mod.rs::register_kanban_tools` stays at the current value. `kanban_create` is amended in place; no new `registry.register(Box::new(...))` call lands.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo test -p ironhermes-kanban --test tools_smoke -- create_goal_mode_sets_columns schema_contains_goal_properties 2>&1 | tail -20 && cargo test -p ironhermes-kanban --lib tools::create 2>&1 | tail -10 && grep -c '"goal_mode"' crates/ironhermes-kanban/src/tools/create.rs | xargs -I {} test {} -ge 1 && grep -c '"goal_max_turns"' crates/ironhermes-kanban/src/tools/create.rs | xargs -I {} test {} -ge 1</automated>
  </verify>
  <done>
    Schema JSON contains goal_mode + goal_max_turns with the documented defaults. execute() threads them into CreateTaskOptions. Two new tests green. schema_contains_board_property still green. No new tool registered.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: worker_spawn::build_kanban_worker_env appends HERMES_KANBAN_GOAL_* when task.goal_mode</name>
  <files>crates/ironhermes-kanban/src/worker_spawn.rs</files>
  <behavior>
    - `build_kanban_worker_env(&task, ...)` returns the existing 8-var env list UNCHANGED when `task.goal_mode == false`. Every existing test that asserts the env list still passes.
    - When `task.goal_mode == true`, the returned env list contains the existing 8 vars PLUS exactly two new entries: `("HERMES_KANBAN_GOAL_MODE", "1")` and `("HERMES_KANBAN_GOAL_MAX_TURNS", task.goal_max_turns.to_string())`.
    - When `task.goal_mode == true && task.goal_max_turns == 0` (defensive), the budget value emitted is "20".
    - `SAFE_SYSTEM_VARS` allowlist at worker_spawn.rs:63 is NOT modified. The new env vars ride the per-task env list path downstream of env_clear+allowlist (verified at worker_spawn.rs:234).
  </behavior>
  <action>
    Step 1 — In `build_kanban_worker_env` at worker_spawn.rs:108-145, AFTER the 8 existing `env.push(...)` calls and BEFORE the final return, add a conditional block:
    `if task.goal_mode { env.push(("HERMES_KANBAN_GOAL_MODE".to_string(), "1".to_string())); let budget = if task.goal_max_turns == 0 { 20 } else { task.goal_max_turns }; env.push(("HERMES_KANBAN_GOAL_MAX_TURNS".to_string(), budget.to_string())); }`. DO NOT mutate `SAFE_SYSTEM_VARS`.

    Step 2 — Extend the existing `#[cfg(test)] mod tests` block at the end of worker_spawn.rs (mirror any existing Task fixture helper inside the test mod by grepping for `Task {` in test scope). Add three #[test] fns:
    `goal_mode_off_env_unchanged()` — Task with goal_mode:false; assert the returned env Vec contains NO entry whose key starts with "HERMES_KANBAN_GOAL_".
    `goal_mode_on_env_has_budget()` — Task with goal_mode:true, goal_max_turns:7; assert exactly one ("HERMES_KANBAN_GOAL_MODE","1") and one ("HERMES_KANBAN_GOAL_MAX_TURNS","7") in the returned env.
    `goal_mode_on_with_zero_budget_falls_back_to_twenty()` — Task with goal_mode:true, goal_max_turns:0; assert ("HERMES_KANBAN_GOAL_MAX_TURNS","20").

    Step 3 — Do NOT change the `SpawnFn` typedef at dispatcher.rs:66 or any dispatcher tick step. D-06 says the dispatcher stays oblivious; only the env build changes.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo test -p ironhermes-kanban --lib worker_spawn::tests::goal_mode 2>&1 | tail -15 && grep -c 'HERMES_KANBAN_GOAL_MODE' crates/ironhermes-kanban/src/worker_spawn.rs | xargs -I {} test {} -ge 2 && grep -c 'HERMES_KANBAN_GOAL_MAX_TURNS' crates/ironhermes-kanban/src/worker_spawn.rs | xargs -I {} test {} -ge 2 && grep -c 'SAFE_SYSTEM_VARS' crates/ironhermes-kanban/src/worker_spawn.rs | xargs -I {} test {} -ge 1</automated>
  </verify>
  <done>
    Three new unit tests green. SAFE_SYSTEM_VARS unchanged. goal_mode=false leaves env unchanged. goal_mode=true appends exactly two new entries. Defensive 0→20 emit verified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: bump_goal_turn_counter (CAS-gated) + subkind tag CONSTs + bilateral CAS + event-subkind tests</name>
  <files>crates/ironhermes-kanban/src/store.rs, crates/ironhermes-kanban/src/events.rs, crates/ironhermes-kanban/src/lib.rs, crates/ironhermes-kanban/tests/goal_judge_event_subkind.rs, crates/ironhermes-kanban/tests/cas_concurrency.rs</files>
  <behavior>
    - `KanbanStore::bump_goal_turn_counter(task_id, claim_lock) -> Result<bool>` is pub. Successful call (fresh claim_lock) returns Ok(true) and `tasks.goal_turns_used` increments by exactly 1.
    - Stale claim_lock returns Ok(false), `tasks.goal_turns_used` UNCHANGED, and the existing claim_expired advisory fires per cas::worker_write_gated semantics. No panic.
    - Four `pub const &str` subkind tag CONSTs exported from events.rs (and re-exported from lib.rs).
    - A frozen-surface test fails to COMPILE if a new KanbanEventKind variant lands (exhaustive-match guard, not runtime).
    - A producer-side test writes an Edited event with payload `{"subkind": GOAL_SUBKIND_JUDGE_VERDICT, "verdict":"not_met", "reason":"...", "turn":7}` via existing append_event API and reads back the exact subkind string.
  </behavior>
  <action>
    Step 1 — In `crates/ironhermes-kanban/src/events.rs`, AFTER the existing `KanbanEventKind` enum definition, add four `pub const &str` declarations with doc comments citing semantics + Plan 04 emit site:
    `pub const GOAL_SUBKIND_JUDGE_VERDICT: &str = "judge_verdict";`
    `pub const GOAL_SUBKIND_JUDGE_ERROR: &str = "judge_error";`
    `pub const GOAL_SUBKIND_TURN_ADVANCED: &str = "goal_turn_advanced";`
    `pub const GOAL_SUBKIND_BUDGET_EXHAUSTED: &str = "goal_budget_exhausted";`
    Doc header above the block: "Phase 36.3.7.12 — Goal-loop event subkind tags. KanbanEventKind enum is FROZEN (Phase 36.3.7.6). Goal-loop events ride on KanbanEventKind::Edited with a JSON `subkind` payload tag, mirroring the nd7 quick-task precedent. Emitted from crates/ironhermes-cli/src/kanban/goal_loop.rs (Plan 04)."

    Step 2 — In `crates/ironhermes-kanban/src/lib.rs`, re-export the four CONSTs alongside any existing events re-exports:
    `pub use events::{GOAL_SUBKIND_JUDGE_VERDICT, GOAL_SUBKIND_JUDGE_ERROR, GOAL_SUBKIND_TURN_ADVANCED, GOAL_SUBKIND_BUDGET_EXHAUSTED};`

    Step 3 — In `crates/ironhermes-kanban/src/store.rs`, near the existing CAS-gated helpers (search for `worker_write_gated` callers to find conventional placement), add a new pub fn that wraps `cas::worker_write_gated` with the closure `|tx| { tx.execute("UPDATE tasks SET goal_turns_used = goal_turns_used + 1 WHERE id=?1", params![task_id])?; Ok(()) }`. Match the surrounding signature pattern exactly (whether `&self` with internal `Arc<Mutex<Connection>>` or `&mut self`); do NOT introduce a new mutex pattern.

    Counter-reset on reclaim per RESEARCH Pitfall 4: add a sibling `pub fn reset_goal_turn_counter(&self, task_id: &str) -> anyhow::Result<()>` that runs `UPDATE tasks SET goal_turns_used = 0 WHERE id=?1` (no CAS gate — reclaim is the writer here, not the worker). DO NOT modify dispatcher.rs in this plan per D-06; the dispatcher integration of `reset_goal_turn_counter` is forward-referenced to Plan 05 integration tests. The safety net during the gap is (a) DDL DEFAULT 0 on fresh rows, (b) the CAS gate rejecting stale-lock bumps.

    Step 4 — Create `crates/ironhermes-kanban/tests/goal_judge_event_subkind.rs`:
    `subkind_consts_pin_exact_strings()` — assert each CONST equals the exact string literal. Guards against typos at the Plan 04 emit site.
    `judge_verdict_subkind_round_trips_through_append_event()` — open_store, create goal_mode task, claim it (capture run_id), call `store.append_event(task_id, Some(run_id), KanbanEventKind::Edited, Some(&serde_json::json!({"subkind": GOAL_SUBKIND_JUDGE_VERDICT, "verdict":"not_met", "reason":"missing criterion 3", "turn":7})))?`. Read events via existing list-events API; assert the last event's payload deserializes to a JSON object whose "subkind" field equals "judge_verdict".
    `judge_error_subkind_round_trips()` — same shape with GOAL_SUBKIND_JUDGE_ERROR.
    `frozen_surface_no_new_variants()` — write an exhaustive `match kind { KanbanEventKind::Created => 1, KanbanEventKind::Edited => 1, /* enumerate every CURRENT variant, omit catch-all */ }` over a representative value. A new variant lands → match becomes non-exhaustive → BUILD BREAKS. This is the load-bearing compile-time guard.

    Step 5 — Extend `crates/ironhermes-kanban/tests/cas_concurrency.rs` (DO NOT create a new file). Add two #[test] fns:
    `goal_turn_bump_fresh_claim_advances_counter()` — create goal_mode task, claim it, capture claim_lock, call `store.bump_goal_turn_counter(task_id, &claim_lock)?`; assert Ok(true) and get_task shows goal_turns_used==1; call again; assert goal_turns_used==2.
    `goal_turn_bump_stale_claim_does_not_advance()` — create goal_mode task, claim it, capture claim_lock, then SIMULATE reclaim (release claim or invalidate run_id via the project's existing test pattern); then call bump_goal_turn_counter with the now-stale claim_lock; assert Ok(false) AND get_task shows goal_turns_used==0.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo test -p ironhermes-kanban --test goal_judge_event_subkind 2>&1 | tail -20 && cargo test -p ironhermes-kanban --test cas_concurrency -- goal_turn_bump 2>&1 | tail -15 && grep -c 'pub const GOAL_SUBKIND_' crates/ironhermes-kanban/src/events.rs | xargs -I {} test {} -ge 4 && grep -c 'pub fn bump_goal_turn_counter' crates/ironhermes-kanban/src/store.rs | xargs test 1 -eq && grep -c 'pub fn reset_goal_turn_counter' crates/ironhermes-kanban/src/store.rs | xargs test 1 -eq && grep -c 'worker_write_gated' crates/ironhermes-kanban/src/store.rs | xargs -I {} test {} -ge 1</automated>
  </verify>
  <done>
    Four CONSTs exported. bump_goal_turn_counter + reset_goal_turn_counter exist. All goal_judge_event_subkind + cas_concurrency new tests green. frozen-surface match guard is exhaustive. No new KanbanEventKind variant landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM caller → kanban_create JSON args | Untrusted goal_max_turns u64 input crosses here; coerced + bounded at the schema and producer layer. |
| Worker subprocess env | Goal env vars cross the env_clear+allowlist scrub at worker_spawn.rs:234. New vars MUST NOT carry secrets. |
| Reclaimed worker → CAS gate | A stale-claim worker attempting to bump the goal counter must be rejected before the DB write. |
| Plan 04 wrapper → events.rs subkind tag strings | Tag-string typos at the emit site would silently break filtering; pinned CONSTs are the contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.12-02-T01 | Tampering | `goal_max_turns` from LLM tool JSON (u64 from JSON, cast to u32) | mitigate | Schema declares `"type":"integer", "default": 20`. Execute path `as u32` cast clamps oversized values; the producer-level `if 0 { 20 }` coercion in Plan 01 lands the default. Future DEFCON-tier ceilings (D-07) plug into the same coercion site. |
| T-36.3.7.12-02-T02 | Tampering | Reclaimed worker bumps goal_turns_used against stale claim_lock | mitigate | `bump_goal_turn_counter` wraps `cas::worker_write_gated`; Ok(false) on stale lock + bilateral test `goal_turn_bump_stale_claim_does_not_advance` proves the contract. |
| T-36.3.7.12-02-I01 | Information Disclosure | New env vars (HERMES_KANBAN_GOAL_MODE/MAX_TURNS) leak into subprocess env | accept | The new vars carry only a feature flag and an integer budget — zero secret material. RESEARCH §Security V12 confirms SAFE_SYSTEM_VARS allowlist at worker_spawn.rs:63 already excludes provider API keys; goal env additions ride the per-task list downstream of the scrub and add no new exfil vector. |
| T-36.3.7.12-02-D01 | Denial of Service | Pathological goal_max_turns = u32::MAX from LLM | mitigate | The 20-default coercion fires only on 0. Phase 36.3.7.12 ships no upper bound this phase per D-07 (DEFCON tier ceilings deferred). Phase-close UAT row 2 explicitly tests budget=2 to confirm the budget IS enforced; future DEFCON-tier phase will clamp the upper end. |
| T-36.3.7.12-02-T03 | Tampering | New KanbanEventKind variant sneaks into events.rs | mitigate | `frozen_surface_no_new_variants` exhaustive-match test in `goal_judge_event_subkind.rs` causes COMPILE-TIME break if a new variant lands. Plus: `pub const GOAL_SUBKIND_*` strings preempt the temptation by giving the Plan 04 wrapper a typed surface. |
| T-36.3.7.12-02-T04 | Tampering | Goal subkind tag drift between emitter (goal_loop.rs Plan 04) and consumer (dashboard / tests) | mitigate | Four `pub const &str` symbols re-exported from `ironhermes-kanban::events`; Plan 04's wrapper imports by symbol, never by string literal. The `subkind_consts_pin_exact_strings` test pins the literals at the producer. |
| T-36.3.7.12-02-SC | Tampering | npm/pip/cargo package installs | not-applicable | Plan adds ZERO new external registry packages; only in-tree symbol additions. |
| T-36.3.7.12-02-E01 | Elevation of Privilege | `reset_goal_turn_counter` is NOT CAS-gated — a stray caller could zero a live worker's counter | accept | Caller is the reclaim path which is already privileged (it transitions claim state); the dispatcher reclaim transaction is the only acceptable caller. Plan 05 integration test asserts no other call site lands. Documented in fn docstring + SUMMARY. |

Block-on severity: high. T01/T02/T03/T04 all carry dedicated tests in this plan.
</threat_model>

<verification>
- `cargo build -p ironhermes-kanban` succeeds.
- `cargo test -p ironhermes-kanban --test tools_smoke -- create_goal_mode_sets_columns schema_contains_goal_properties` — green.
- `cargo test -p ironhermes-kanban --lib worker_spawn::tests::goal_mode` — three tests green.
- `cargo test -p ironhermes-kanban --test cas_concurrency -- goal_turn_bump` — two tests green.
- `cargo test -p ironhermes-kanban --test goal_judge_event_subkind` — four tests green (incl. compile-time frozen-surface guard).
- `cargo clippy -p ironhermes-kanban -- -D warnings` clean.
- Tool count UNCHANGED: `grep -c 'registry.register(Box::new(' crates/ironhermes-kanban/src/tools/mod.rs` returns the same value as before Plan 02.
- `cargo tree -p ironhermes-kanban --depth 1 | grep -c '^ironhermes-agent ' == 0` — crate-isolation fence holds.
- KNOWN-FAILING-AT-BASE: `crates/ironhermes-kanban/tests/end_to_end.rs::full_lifecycle_via_tools_layer` (line 61) and `::duplicate_completion_is_rejected` (line 234) are DEFER-1 per nd7. DO NOT touch or debug.
</verification>

<success_criteria>
- All three tasks complete; verify commands return zero exit (excluding documented DEFER-1).
- Decision coverage: D-01 (kanban_create surface), D-03 (env-var distribution of budget), D-04 (event-subkind tags pinned + frozen surface preserved), D-06 (dispatcher unchanged), D-08 (worker tool surface amended, crate fence intact).
- `must_haves.truths` all observable in the new test surface.
- Plan 04 (goal loop wrapper) can read the env vars, write counter advances via bump_goal_turn_counter, and emit events with the pinned subkind CONSTs.
</success_criteria>

<output>
Create `.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-02-SUMMARY.md` when done.
</output>
