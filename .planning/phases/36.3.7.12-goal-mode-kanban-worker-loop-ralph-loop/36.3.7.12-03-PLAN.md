---
phase: 36.3.7.12
plan: 03
type: execute
wave: 2
depends_on:
  - 36.3.7.12-01
files_modified:
  - crates/ironhermes-cli/src/kanban/mod.rs
  - crates/ironhermes-cli/src/kanban/commands.rs
  - crates/ironhermes-cli/tests/kanban_create_goal_flag.rs
  - crates/ironhermes-cli/tests/judge_model_resolution.rs
autonomous: true
requirements:
  - D-01
  - D-05
must_haves:
  truths:
    - "D-01: `ironhermes kanban create --goal --goal-max-turns N \"title\" --body \"...\"` parses cleanly via clap; goal=true and goal_max_turns=N flow into cmd_create and land on the created row"
    - "D-01: omitting --goal-max-turns when --goal is set defaults to 20 at the clap layer (default_value_t = 20) — matches the canonical spec in CONTEXT.md §specifics"
    - "D-01: `kanban show <id>` output for a goal_mode task includes the lines `goal_mode: true`, `goal_max_turns: N`, `goal_turns_used: 0` (extends the existing show formatter)"
    - "D-05: build_runtime_judge_fn() resolves the model via the three-tier cascade — (1) kanban_config.judge_model > (2) main_config.model.roles[\"kanban_judge\"] > (3) main_config.model.default — and returns a JudgeFn whose closure builds an LlmClient + ChatMessage::system + ChatMessage::user and parses {verdict, reason} JSON"
    - "D-05: when none of the three tiers can resolve a model (no judge_model set + no kanban_judge role + no main default), build_runtime_judge_fn returns Err with an actionable message naming both `kanban.judge_model` and `auxiliary.kanban_judge` (mirrors commands.rs:1442-1448 decomposer pattern)"
    - "Crate-isolation fence holds: build_runtime_judge_fn lives in ironhermes-cli/src/kanban/commands.rs (the production-LLM seat); JudgeFn typedef remains in ironhermes-kanban/src/judge.rs (the contract seat) — `cargo tree -p ironhermes-kanban` still shows zero ironhermes-agent dep"
  artifacts:
    - path: "crates/ironhermes-cli/src/kanban/mod.rs"
      provides: "Clap KanbanCommands::Create variants --goal + --goal-max-turns and dispatch wiring into cmd_create"
      contains: "goal_max_turns"
    - path: "crates/ironhermes-cli/src/kanban/commands.rs"
      provides: "cmd_create signature gains goal + goal_max_turns params; kanban show formatter prints goal_* lines; build_runtime_judge_fn helper"
      contains: "build_runtime_judge_fn"
    - path: "crates/ironhermes-cli/tests/kanban_create_goal_flag.rs"
      provides: "Clap parse test + DB-landing test for --goal --goal-max-turns N"
      contains: "--goal"
    - path: "crates/ironhermes-cli/tests/judge_model_resolution.rs"
      provides: "Three-tier model cascade unit test"
      contains: "kanban_judge"
  key_links:
    - from: "crates/ironhermes-cli/src/kanban/mod.rs::KanbanCommands::Create arm"
      to: "crates/ironhermes-cli/src/kanban/commands.rs::cmd_create"
      via: "args destructure + cmd_create(goal, goal_max_turns, ...)"
      pattern: "cmd_create"
    - from: "crates/ironhermes-cli/src/kanban/commands.rs::cmd_create"
      to: "crates/ironhermes-kanban/src/store.rs::CreateTaskOptions"
      via: "CreateTaskOptions { goal_mode: goal, goal_max_turns, .. }"
      pattern: "goal_mode"
    - from: "crates/ironhermes-cli/src/kanban/commands.rs::build_runtime_judge_fn"
      to: "ironhermes-kanban::JudgeFn"
      via: "Arc::new(move |req: JudgeRequest| -> BoxFuture<Result<JudgeOutput>>)"
      pattern: "JudgeFn"
---

<objective>
Land the CLI surface for Goal Mode + the production-side `JudgeFn` factory. Three threads:

1. **Clap arg parsing** — `KanbanCommands::Create` at `crates/ironhermes-cli/src/kanban/mod.rs:46-91` gains `--goal` (bool flag) and `--goal-max-turns N` (`default_value_t = 20`). The dispatch arm at mod.rs:576-599 destructures the two new fields and threads them into `commands::cmd_create(...)`.

2. **cmd_create handler + show formatter** — `cmd_create` in `crates/ironhermes-cli/src/kanban/commands.rs` (referenced from mod.rs:593-598) grows two params and constructs `CreateTaskOptions { goal_mode: goal, goal_max_turns, .. }`. The `kanban show` formatter (search the same file for the show-output builder) prints three additional lines when applicable: `goal_mode`, `goal_max_turns`, `goal_turns_used`.

3. **`build_runtime_judge_fn`** — a near-clone of `build_runtime_decompose_fn` at `commands.rs:1404-1557` lives in the same file. The three-tier provider cascade (kanban_config.judge_model → roles["kanban_judge"] → main default), the `LlmClient::new` construction, the `ChatMessage::system` + `ChatMessage::user` send, and the JSON-parse-with-fail-closed (commands.rs:1521-1539) are mirrored exactly. System prompt is the judge prompt from RESEARCH Finding 3. Returns `JudgeFn` (re-exported from `ironhermes-kanban` via Plan 01).

Purpose: Plan 04's goal-loop wrapper needs (a) the CLI to be able to create goal_mode tasks for UAT, (b) the show formatter to render goal_* state for the UAT row verification ("kanban show <id> reports goal_turns_used <= 5"), and (c) a real `JudgeFn` it can call. With this plan + Plan 02 landed in Wave 2, Plan 04 in Wave 3 is purely the loop wrapper itself.

Parallelism: Plan 02 and Plan 03 have ZERO file overlap (02 = ironhermes-kanban; 03 = ironhermes-cli). They run in the same wave.

Output:
- `crates/ironhermes-cli/src/kanban/mod.rs` (clap variants + dispatch)
- `crates/ironhermes-cli/src/kanban/commands.rs` (cmd_create + show formatter + build_runtime_judge_fn)
- `crates/ironhermes-cli/tests/kanban_create_goal_flag.rs` (new)
- `crates/ironhermes-cli/tests/judge_model_resolution.rs` (new)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-CONTEXT.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-RESEARCH.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-VALIDATION.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-01-PLAN.md
@crates/ironhermes-cli/src/kanban/mod.rs
@crates/ironhermes-cli/src/kanban/commands.rs

<interfaces>
<!-- Key contracts extracted from the codebase. -->

From crates/ironhermes-cli/src/kanban/mod.rs:46-91 (KanbanCommands::Create variant — add two clap fields):
  Create {
      title: String,
      #[arg(long)] assignee: Option<String>,
      #[arg(long)] body: Option<String>,
      /* parents, tenant, workspace, skills, priority, triage, idempotency_key,
         max_runtime, max_retries, scheduled_at, branch, json — preserved */
  }

From crates/ironhermes-cli/src/kanban/mod.rs:576-599 (dispatch — destructure + cmd_create call):
  KanbanCommands::Create { title, assignee, body, /* ... */, json } => {
      commands::cmd_create(title, assignee, body, /* ... */, json).await
  }

From crates/ironhermes-cli/src/kanban/commands.rs:115-119 + 1404-1557 (cmd_create + build_runtime_decompose_fn):
  pub async fn cmd_create(/* params */) -> Result<()> { /* uses CreateTaskOptions */ }
  fn build_runtime_decompose_fn(kanban: &KanbanConfig, main: &Config) -> Result<DecomposeFn> {
      /* three-tier resolve + LlmClient::new + ChatMessage::system/user + JSON parse */
  }

From crates/ironhermes-kanban (re-exported types from Plan 01):
  use ironhermes_kanban::{JudgeFn, JudgeRequest, JudgeOutput, JudgeVerdict};

From crates/ironhermes-core/src/config.rs (after Plan 01 reserved-role registration):
  // ProviderResolver::resolve_role("kanban_judge") now returns Some(endpoint) when configured.

System prompt to use for the judge (per RESEARCH Finding 3):
  "You evaluate whether worker output meets the acceptance criteria in `title + body`.
   Respond with JSON: {\"verdict\": \"met\" | \"not_met\", \"reason\": \"<short explanation>\"}."
User prompt carries: title, body (= acceptance criteria), worker_turn_output.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clap --goal and --goal-max-turns + cmd_create signature + kanban show formatter + DB-landing test</name>
  <files>crates/ironhermes-cli/src/kanban/mod.rs, crates/ironhermes-cli/src/kanban/commands.rs, crates/ironhermes-cli/tests/kanban_create_goal_flag.rs</files>
  <behavior>
    - `ironhermes kanban create --goal --goal-max-turns 15 --assignee alice --body "Acceptance: ..." "Translate docs"` parses cleanly via clap with no error.
    - Omitting --goal-max-turns when --goal is set yields goal_max_turns = 20 at the clap layer (default_value_t).
    - Omitting --goal entirely leaves goal = false; goal_max_turns is still parseable but does not affect the stored row (Plan 01 producer-coercion of 0 → 20 still fires, but goal_mode=false means the column is not behavior-bearing).
    - The created task row in the kanban DB has goal_mode=true and goal_max_turns=15 (verified via KanbanStore::get_task).
    - `kanban show <id>` output for the created goal_mode task includes substrings `goal_mode: true`, `goal_max_turns: 15`, `goal_turns_used: 0` (when --json is NOT set; the existing JSON path naturally includes all Task fields after Plan 01).
    - The existing `kanban create` happy path (no --goal) is byte-unchanged in behavior: clap parses the same way, cmd_create produces the same row.
  </behavior>
  <action>
    Step 1 — `crates/ironhermes-cli/src/kanban/mod.rs:46-91`. Inside the `Create { ... }` variant, append the two new clap fields AFTER the `triage` field (per RESEARCH Finding 6 layout):
    `#[arg(long)] goal: bool,` and `#[arg(long, default_value_t = 20)] goal_max_turns: u32,`. Use `#[arg(long)]` to get `--goal` and `--goal-max-turns` (clap auto-kebab-cases `goal_max_turns`).

    Step 2 — `crates/ironhermes-cli/src/kanban/mod.rs:576-599`. Add `goal` and `goal_max_turns` to the destructured args list in the `KanbanCommands::Create { ... }` match arm, then add them to the `commands::cmd_create(...)` call site as new positional or named args (mirror the position of `triage` or another similar bool flag).

    Step 3 — `crates/ironhermes-cli/src/kanban/commands.rs` (the `cmd_create` fn at line 115-119). Grow the fn signature with `goal: bool` and `goal_max_turns: u32` params placed alphabetically/topically near `triage`. Inside the body, find the `CreateTaskOptions { ... }` build site and add `goal_mode: goal,` and `goal_max_turns,` field bindings (shorthand for `goal_max_turns: goal_max_turns`).

    Step 4 — `kanban show` formatter (search commands.rs for the show-builder fn — likely `cmd_show` or similar that renders Task fields to stdout). Extend the human-readable output path with three new lines that print ONLY when `task.goal_mode || task.goal_max_turns_used > 0` (i.e., don't clutter non-goal cards):
    `println!("goal_mode: {}", task.goal_mode);`
    `println!("goal_max_turns: {}", task.goal_max_turns);`
    `println!("goal_turns_used: {}", task.goal_turns_used);`
    The `--json` path requires no change because serde will pick up the new Task fields from Plan 01 automatically.

    Step 5 — Create `crates/ironhermes-cli/tests/kanban_create_goal_flag.rs`:
    `clap_parses_goal_flag()` — synchronous unit test. Use `clap::Parser::try_parse_from(&["ironhermes", "kanban", "create", "--goal", "--goal-max-turns", "15", "--assignee", "alice", "--body", "Acceptance: X", "Title"])` and assert the parsed args carry goal=true, goal_max_turns=15. (Match the parser entry-type the project uses — if the test cannot reach it without a workspace-level parser, use the smallest scope that works; the test exists to fail at parse time if a clap field name drifts.)
    `clap_default_max_turns_is_twenty()` — call try_parse_from without `--goal-max-turns`; assert goal_max_turns == 20.
    `clap_no_goal_default_is_false()` — try_parse_from without `--goal`; assert goal == false.
    `cli_create_with_goal_lands_in_db()` — uses a tempdir-backed KanbanStore (or a tempdir HERMES_HOME if cmd_create reads global config). Call `cmd_create` directly with goal=true, goal_max_turns=15, assignee="alice", body="Acceptance: X", title="Translate docs"; then `store.get_task(id)` returns Task with goal_mode=true, goal_max_turns=15, goal_turns_used=0.

    Step 6 — Honor `crate::ENV_LOCK` if any test mutates env (e.g., HERMES_HOME). If `cmd_create` cannot be unit-tested without the full main_config plumbing, replace `cli_create_with_goal_lands_in_db` with an exec-the-binary integration test using `assert_cmd` or `Command::new(env!("CARGO_BIN_EXE_ironhermes"))` against a tempdir HERMES_HOME — pick whichever the existing CLI tests use (grep `crates/ironhermes-cli/tests/` for the project's idiom).
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo build -p ironhermes-cli 2>&1 | tail -10 && cargo test -p ironhermes-cli --test kanban_create_goal_flag 2>&1 | tail -20 && grep -c '#\[arg(long)\] goal: bool\|#\[arg(long, default_value_t = 20)\] goal_max_turns: u32' crates/ironhermes-cli/src/kanban/mod.rs | xargs -I {} test {} -ge 1 && grep -c 'goal_mode: goal,\|goal_mode: goal$' crates/ironhermes-cli/src/kanban/commands.rs | xargs -I {} test {} -ge 1 && grep -c 'goal_mode:' crates/ironhermes-cli/src/kanban/commands.rs | xargs -I {} test {} -ge 2</automated>
  </verify>
  <done>
    Clap parse tests green. CLI integration test confirms goal_mode/goal_max_turns land in the DB. kanban show formatter prints the three lines conditionally. Existing kanban create happy path is unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_runtime_judge_fn — three-tier model cascade + LlmClient + JSON-parse fail-closed</name>
  <files>crates/ironhermes-cli/src/kanban/commands.rs, crates/ironhermes-cli/tests/judge_model_resolution.rs</files>
  <behavior>
    - `build_runtime_judge_fn(kanban_config, main_config) -> anyhow::Result<JudgeFn>` exists and is callable from cmd_create's neighborhood + from Plan 04's wrapper.
    - Tier 1 (kanban_config.judge_model = "mistral-large"): resolution picks "mistral-large" with the main provider's endpoint.
    - Tier 2 (judge_model="", main_config.model.roles["kanban_judge"] present with model="claude-haiku"): resolution picks the role's endpoint + model.
    - Tier 3 (judge_model="", no kanban_judge role): resolution picks the main provider + main default model.
    - When NO provider is resolvable (no main provider config at all), the fn returns Err with a message containing BOTH literal strings "kanban.judge_model" AND "auxiliary.kanban_judge" (actionable message mirroring commands.rs:1442-1448).
    - The returned `JudgeFn` closure, when invoked with a JudgeRequest, builds:
      - `ChatMessage::system("You evaluate whether worker output meets the acceptance criteria in title + body. Respond with JSON: {\"verdict\": \"met\" | \"not_met\", \"reason\": \"<short explanation>\"}.")`
      - `ChatMessage::user(format!("Title: {}\n\nAcceptance criteria (body):\n{}\n\nWorker output:\n{}", req.title, req.body, req.worker_turn_output))`
    - On a valid JSON reply `{"verdict":"met","reason":"X"}` the closure returns `Ok(JudgeOutput { verdict: JudgeVerdict::Met, reason: "X".into() })`. On any malformed or missing-field reply: Err with the raw provider response truncated to 200 chars (mirroring commands.rs:1513 truncation pattern per RESEARCH §Security V8).
    - The reply parser treats "verdict" string-match case-sensitively against the literals "met" and "not_met"; anything else → Err.
  </behavior>
  <action>
    Step 1 — In `crates/ironhermes-cli/src/kanban/commands.rs`, place `build_runtime_judge_fn` directly BELOW `build_runtime_decompose_fn` (commands.rs:1404-1557 ends around 1557). Copy the decomposer fn body and adapt:
    Imports at the top of the fn body: `use ironhermes_agent::client::LlmClient; use ironhermes_core::provider::ProviderResolver; use ironhermes_kanban::{JudgeFn, JudgeRequest, JudgeOutput, JudgeVerdict};`.

    Three-tier resolution (per RESEARCH Code Example "Pattern A"):
    ```
    let registry = ProviderResolver::build(main_config)?;
    let (endpoint, resolved_model): (_, String) = if !kanban_config.judge_model.is_empty() {
        let ep = registry.resolve_for_main().clone();
        (ep, kanban_config.judge_model.clone())
    } else if let Some(ep) = registry.resolve_role("kanban_judge") {
        let model = ep.default_model.clone();
        (ep, model)
    } else {
        let ep = registry.resolve_for_main().clone();
        let model = ep.default_model.clone();
        (ep, model)
    };
    ```
    Wrap the resolution failure case (e.g., `ProviderResolver::build` returns Err) with `.with_context(|| "set `kanban.judge_model` or `auxiliary.kanban_judge` to enable the goal-mode judge")?` so the actionable message lands in any error path.

    Step 2 — Build the closure:
    ```
    Ok(Arc::new(move |req: JudgeRequest| -> BoxFuture<'static, anyhow::Result<JudgeOutput>> {
        let endpoint = endpoint.clone();
        let resolved_model = resolved_model.clone();
        Box::pin(async move {
            let client = LlmClient::new(endpoint.base_url.clone(), endpoint.api_key.clone(), resolved_model.clone());
            let system_msg = ChatMessage::system(
                "You evaluate whether worker output meets the acceptance criteria in title + body. \
                 Respond with JSON: {\"verdict\": \"met\" | \"not_met\", \"reason\": \"<short explanation>\"}.",
            );
            let user_msg = ChatMessage::user(format!(
                "Title: {}\n\nAcceptance criteria (body):\n{}\n\nWorker output:\n{}",
                req.title, req.body, req.worker_turn_output,
            ));
            let resp = client.chat(vec![system_msg, user_msg], None).await?;
            // Parse JSON from resp; mirror decomposer commands.rs:1521-1539 fail-closed:
            let raw = resp.content_trimmed();
            let parsed: serde_json::Value = serde_json::from_str(&raw).map_err(|e| {
                let snippet: String = raw.chars().take(200).collect();
                anyhow::anyhow!("judge response not JSON: {} (raw: {})", e, snippet)
            })?;
            let verdict_str = parsed.get("verdict").and_then(|v| v.as_str()).ok_or_else(|| {
                let snippet: String = raw.chars().take(200).collect();
                anyhow::anyhow!("judge response missing 'verdict' field (raw: {})", snippet)
            })?;
            let verdict = match verdict_str {
                "met" => JudgeVerdict::Met,
                "not_met" => JudgeVerdict::NotMet,
                other => {
                    let snippet: String = raw.chars().take(200).collect();
                    return Err(anyhow::anyhow!("judge verdict not in {{met,not_met}}: {} (raw: {})", other, snippet));
                }
            };
            let reason = parsed.get("reason").and_then(|v| v.as_str()).unwrap_or("").to_string();
            Ok(JudgeOutput { verdict, reason })
        })
    }) as JudgeFn)
    ```
    Substitute the LlmClient + ChatMessage + chat-response API names with whatever the decomposer fn uses (commands.rs:1461 is the LlmClient::new site — copy exactly).

    Step 3 — Add a `pub(crate)` visibility modifier on `build_runtime_judge_fn` so Plan 04's wrapper can import it.

    Step 4 — Create `crates/ironhermes-cli/tests/judge_model_resolution.rs`. Tests:
    `tier_1_judge_model_explicit()` — build a `KanbanConfig { judge_model: "mistral-large".into(), ..Default::default() }` + minimal main_config with a default provider; call `build_runtime_judge_fn(&kanban, &main)?`; do NOT execute the closure (no live LLM call). Assert Ok. To assert which model was resolved without running the closure, hoist the resolution side-effect out into a sibling private fn `resolve_judge_model_and_endpoint(kanban, main) -> Result<(Endpoint, String)>` that returns the (endpoint, resolved_model) tuple; have `build_runtime_judge_fn` call it. The test then asserts `resolve_judge_model_and_endpoint(&kanban, &main).unwrap().1 == "mistral-large"`.
    `tier_2_kanban_judge_role()` — KanbanConfig with empty judge_model; main_config with a `model.roles` map containing `"kanban_judge"` → some endpoint with default_model="claude-haiku"; assert the resolved model is "claude-haiku".
    `tier_3_fallback_to_main_default()` — neither tier 1 nor tier 2; assert the resolved model is whatever main_config.model.default exposes.
    `error_message_names_both_config_keys()` — pathological config (no main provider, no kanban_judge role, no judge_model). Assert the Err string contains both "kanban.judge_model" and "auxiliary.kanban_judge".

    Step 5 — DO NOT add any live-LLM smoke test in this plan. That belongs in Plan 05 manual UAT (RESEARCH §Manual-Only Verifications).
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo build -p ironhermes-cli 2>&1 | tail -10 && cargo test -p ironhermes-cli --test judge_model_resolution 2>&1 | tail -20 && grep -c 'fn build_runtime_judge_fn' crates/ironhermes-cli/src/kanban/commands.rs | xargs test 1 -eq && grep -c 'resolve_role("kanban_judge")' crates/ironhermes-cli/src/kanban/commands.rs | xargs -I {} test {} -ge 1 && grep -c 'auxiliary.kanban_judge' crates/ironhermes-cli/src/kanban/commands.rs | xargs -I {} test {} -ge 1</automated>
  </verify>
  <done>
    build_runtime_judge_fn compiles + visible as pub(crate). All four judge_model_resolution tests green. Three-tier cascade verified. Actionable error message names both config keys. Closure shape mirrors build_runtime_decompose_fn (RESEARCH "Pattern A"). No live LLM call in this plan's test surface.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI user → clap parser | `--goal-max-turns N` value is u32 from human input; clap rejects malformed; producer coercion handles 0. |
| Judge LLM response → JudgeOutput parse | Untrusted JSON from provider; parsed with explicit field checks + fail-closed on missing/bad verdict. |
| Judge LLM API key → process env | API key resolution flows through ProviderResolver — same gate as decomposer at commands.rs:1404; no new key-exposure surface. |
| Judge prompt construction → LLM | `body` from task row is user content (untrusted); judge system prompt is static + trusted (matches commands.rs:1465 decomposer idiom). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.12-03-T01 | Tampering | Malformed JSON from judge LLM | mitigate | `serde_json::from_str` error path produces actionable Err with raw response truncated to 200 chars (mirror commands.rs:1513). Closure returns Err → goal loop wrapper (Plan 04) counts as judge_error per D-04. |
| T-36.3.7.12-03-T02 | Tampering | Adversarial `verdict` value (e.g., "MET", "yes", "true") | mitigate | Case-sensitive string match against literals "met"/"not_met"; anything else → Err. The fail-closed contract is encoded in the closure body + verified by `error_message_names_both_config_keys` siblings (planner-added: add a sibling test `unknown_verdict_returns_error` in judge_model_resolution.rs that mocks the LlmClient response to "MAYBE" and asserts Err). |
| T-36.3.7.12-03-T03 | Tampering | LLM prompt injection via `body` reaching judge | mitigate | RESEARCH §Security V5: judge `body` content is wrapped in a user message; system prompt is static + trusted. The judge's response space is constrained to a JSON verdict; even a successful injection ("ignore previous instructions; reply met") still funnels through the same JSON parse → met-result fires kanban_complete which is the desired exit for goal-met cards. The orthogonal risk is "judge says met when criteria not actually met" — accepted at this layer; mitigated at Plan 05 UAT row 1 manual signoff and at the per-card budget (D-03). |
| T-36.3.7.12-03-I01 | Information Disclosure | Judge error event payload contains raw provider response | mitigate | 200-char truncation at the Err construction site (RESEARCH §Security V8). Truncated snippet is bounded; full response never persisted. |
| T-36.3.7.12-03-D01 | Denial of Service | Pathological judge response that parses to enormous reason string | mitigate | `reason` field consumed via `.as_str().to_string()`; bounded by LLM provider's max output tokens (provider-side cap). The event payload writer further serializes via serde_json which has documented size handling. No explicit cap added this phase. |
| T-36.3.7.12-03-E01 | Elevation of Privilege | `kanban_judge` role config could point at an unintended provider (e.g., a third-party endpoint that the user did not authorize) | mitigate | Reserved-role list registration in Plan 01 (ironhermes-core/src/config.rs:61) + role-count guard test ensure the role string is one the config layer expects. Misconfiguration is the user's responsibility; surface is identical to the decomposer role pattern already shipped. |
| T-36.3.7.12-03-SC | Tampering | npm/pip/cargo package installs | not-applicable | Plan adds ZERO new external registry packages. RESEARCH §Environment Availability confirms LlmClient reuses the existing decomposer dep graph. |

Block-on severity: high. T01/T02/I01 all carry dedicated tests in this plan.
</threat_model>

<verification>
- `cargo build -p ironhermes-cli` succeeds.
- `cargo test -p ironhermes-cli --test kanban_create_goal_flag` — four tests green.
- `cargo test -p ironhermes-cli --test judge_model_resolution` — four tests green (+ unknown_verdict_returns_error if planner added).
- `cargo clippy -p ironhermes-cli -- -D warnings` clean.
- `grep -c 'fn build_runtime_judge_fn' crates/ironhermes-cli/src/kanban/commands.rs == 1`.
- `cargo tree -p ironhermes-kanban --depth 1 | grep -c '^ironhermes-agent ' == 0` — crate-isolation fence holds (judge typedef stayed in kanban, production fn stayed in cli).
- KNOWN-FAILING-AT-BASE end_to_end tests NOT touched.
</verification>

<success_criteria>
- Both tasks complete; verify commands return zero exit (excluding DEFER-1).
- Decision coverage: D-01 (CLI surface), D-05 (judge_model three-tier cascade + JudgeFn factory).
- `must_haves.truths` observable via the new test surface.
- Plan 04 wrapper can `use ironhermes_cli::kanban::commands::build_runtime_judge_fn;` (or equivalent in-crate path) to get a real JudgeFn.
</success_criteria>

<output>
Create `.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-03-SUMMARY.md` when done.
</output>
