---
phase: "21.8.2"
plan: "03"
subsystem: "skills-hot-reload"
tags: ["skills", "hot-reload", "SKILL-13", "tui_rata", "gateway", "cli"]
dependency_graph:
  requires: ["21.8.2-01", "21.8.2-02"]
  provides: ["hermes-skills-reload-cli", "skill-13-fallback-all-surfaces", "gateway-skill-swap"]
  affects: ["ironhermes-cli", "ironhermes-gateway", "ironhermes-agent"]
tech_stack:
  added: []
  patterns:
    - "Arc<Mutex<Option<Arc<T>>>> interior-mutability for lock-free atomic registry swap"
    - "SlashOutcome enum extension for new command results"
    - "Static-grep invariant tests via include_str!()"
    - "TDD RED/GREEN for prompt_builder skill overlay API"
key_files:
  created:
    - "crates/ironhermes-cli/tests/skills_reload_cli.rs"
  modified:
    - "crates/ironhermes-agent/src/prompt_builder.rs"
    - "crates/ironhermes-core/src/skills.rs"
    - "crates/ironhermes-core/src/lib.rs"
    - "crates/ironhermes-cli/src/main.rs"
    - "crates/ironhermes-cli/src/tui/commands.rs"
    - "crates/ironhermes-cli/src/tui_rata/commands.rs"
    - "crates/ironhermes-cli/src/tui_rata/app.rs"
    - "crates/ironhermes-cli/src/tui_rata/event_loop.rs"
    - "crates/ironhermes-cli/src/skills_cmd.rs"
    - "crates/ironhermes-cli/tests/skill_registry_wiring.rs"
    - "crates/ironhermes-gateway/src/handler.rs"
    - "crates/ironhermes-gateway/src/runner.rs"
decisions:
  - "Arc<Mutex<Option<Arc<SkillRegistry>>>> chosen over Arc<RwLock<...>> for D-03 atomic swap — std::sync::Mutex is simpler and hot-reload is infrequent"
  - "SKILL-13 fallback lives in tui/commands.rs dispatch_command and tui_rata/commands.rs dispatch_slash (not main.rs) — main.rs consumes the SkillActivated result from both"
  - "cmd_reload in skills_cmd.rs uses synchronous load_with_config — no async needed for one-shot CLI invocation"
  - "Static-grep invariant tests preferred over integration tests — avoids live filesystem/network deps in CI"
metrics:
  duration: "~90 minutes (across two sessions)"
  completed_date: "2026-05-09"
  tasks_completed: 3
  tasks_total: 3
  files_modified: 12
  files_created: 1
  tests_added: 19
---

# Phase 21.8.2 Plan 03: Skills Hot-Reload Integration Layer Summary

SkillsReload/SkillActivated command results wired end-to-end across CLI REPL, TUI rata, and gateway with SKILL-13 dynamic fallback at all NotFound dispatch sites, plus `hermes skills reload` CLI subcommand.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| RED  | Failing tests for activate_skill/clear_skill_overlays | 98211d57 | skill_registry_wiring.rs |
| 1    | activate_skill helper + SKILL-13 + SkillsReload arms | 3b65e3f3 | prompt_builder.rs, main.rs, tui/commands.rs, skills.rs, lib.rs, skill_registry_wiring.rs |
| 2    | TUI + gateway skill hot-reload integration | bd412928 | tui_rata/commands.rs, tui_rata/app.rs, event_loop.rs, main.rs, handler.rs, runner.rs |
| 3    | `hermes skills reload` CLI subcommand | df89dabc | skills_cmd.rs, skills_reload_cli.rs |

## What Was Built

**Task 1 — Core integration layer:**
- Added `skill_overlays: Vec<(String, String)>` field to `PromptBuilder` with `activate_skill()` / `clear_skill_overlays()` methods
- `build_split()` prepends overlay bodies as ephemeral content before the slot loop (D-07)
- `count_invalid_skipped` helper in `main.rs` walks search paths and reports SKILL.md dirs not loaded (D-05 WARN-BUT-LOAD)
- Replaced both `unreachable!()` placeholders in `run_chat` with real `SkillsReload` and `SkillActivated` arms
- Promoted `build_skill_search_paths` from `pub(crate)` to `pub` and re-exported from `ironhermes-core`
- SKILL-13 fallback at `tui/commands.rs` `ResolveResult::NotFound` — returns `CommandResult::SkillActivated { name, body }`

**Task 2 — TUI rata + gateway:**
- Added `SlashOutcome::SkillsReload(String)` and `SlashOutcome::SkillActivated { name, body }` to `tui_rata/commands.rs`
- Replaced placeholder arms in `map_core_to_slash_outcome` with real dispatch
- SKILL-13 fallback at `dispatch_slash` `ResolveResult::NotFound` branch — checks `app.skill_registry` before typo hint
- Added `apply_slash_outcome` arms in `app.rs` — SkillsReload pushes Role::System message; SkillActivated pushes to `pending_skill_overlays` buffer + System message
- Added `skills_config` and `pending_skill_overlays` to `AppDeps` literal in `event_loop.rs`
- Updated `test_deps()` with all three new fields
- Gateway `Handler.skill_registry` refactored to `Arc<std::sync::Mutex<Option<Arc<SkillRegistry>>>>` for D-03 atomic swap
- Per-session `skill_overlays: Arc<Mutex<HashMap<SessionKey, Vec<(String, String)>>>>` on Handler for D-07
- `set_skills_config` added to `GatewayRunner` and wired in `build_gateway_handler`
- Full SkillsReload/SkillActivated/SKILL-13 arms added to `handle_slash_command` and `run_agent` in handler.rs

**Task 3 — CLI subcommand:**
- Added `SkillsAction::Reload` variant to skills_cmd.rs
- `cmd_reload()` calls `SkillRegistry::load_with_config`, lists loaded skills, reports invalid_skipped count
- Dispatch arm routes `SkillsAction::Reload => cmd_reload(&cfg)`
- 5 static-grep smoke tests in `skills_reload_cli.rs`

## Test Coverage

- 4 `prompt_builder` unit tests (activate_skill/clear_skill_overlays)
- 14 static-grep invariants in `skill_registry_wiring.rs` (all pass)
- 5 static-grep smoke tests in `skills_reload_cli.rs` (all pass)
- **Total: 23 tests added, all passing**

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `send_text` doesn't exist on `PlatformAdapter`**
- **Found during:** Task 1 (gateway handler)
- **Issue:** Plan referenced `adapter.send_text(event, msg).await` but the trait only has `send_message(&chat_id, &text, None)`
- **Fix:** Used `with_rate_limit_retry(|| adapter.send_message(...))` pattern; bound format strings to `let` variables to avoid temporary reference in closure
- **Files modified:** `crates/ironhermes-gateway/src/handler.rs`
- **Commit:** bd412928

**2. [Rule 1 - Bug] `build_skill_search_paths` was `pub(crate)` — not accessible from CLI**
- **Found during:** Task 1 (count_invalid_skipped helper in main.rs)
- **Issue:** `count_invalid_skipped` needs `ironhermes_core::build_skill_search_paths` but it was crate-private
- **Fix:** Changed visibility to `pub` in `skills.rs`, added `pub use skills::build_skill_search_paths;` in `lib.rs`
- **Files modified:** `crates/ironhermes-core/src/skills.rs`, `crates/ironhermes-core/src/lib.rs`
- **Commit:** 3b65e3f3

**3. [Rule 1 - Bug] Static-grep test pointed at wrong file for SKILL-13**
- **Found during:** Task 1 test verification
- **Issue:** `main_rs_skill13_fallback_present` checked `main.rs` for `registry.find(` but the SKILL-13 fallback lives in `tui/commands.rs` (which `main.rs` calls via `dispatch_command`)
- **Fix:** Updated test to check `tui/commands.rs` for `registry.find(`
- **Files modified:** `crates/ironhermes-cli/tests/skill_registry_wiring.rs`
- **Commit:** 3b65e3f3

**4. [Rule 1 - Bug] `config` moved into `GatewayRunner::new` before `set_skills_config` call**
- **Found during:** Task 2 build
- **Issue:** `GatewayRunner::new(config, ...)` moved `config`, then `runner.set_skills_config(config.skills.clone())` tried to borrow moved value
- **Fix:** Changed to `GatewayRunner::new(config.clone(), ...)` 
- **Files modified:** `crates/ironhermes-cli/src/main.rs`
- **Commit:** bd412928

**5. [Rule 1 - Bug] `SkillRegistry::all()` doesn't exist — method is `list()`**
- **Found during:** Task 3 build
- **Issue:** `cmd_reload` used `registry.all()` which doesn't exist
- **Fix:** Changed to `registry.list()`
- **Files modified:** `crates/ironhermes-cli/src/skills_cmd.rs`
- **Commit:** df89dabc

## Self-Check: PASSED

Files exist:
- `.planning/phases/21.8.2-skills-hot-reload-command/21.8.2-03-SUMMARY.md` — this file

Commits verified:
- `98211d57` — RED test commit
- `3b65e3f3` — Task 1 commit
- `bd412928` — Task 2 commit
- `df89dabc` — Task 3 commit
