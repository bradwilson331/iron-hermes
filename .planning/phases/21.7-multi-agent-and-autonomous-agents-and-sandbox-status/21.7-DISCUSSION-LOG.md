# Phase 21.7: Multi-agent and autonomous agents and sandbox status - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-22
**Phase:** 21.7-multi-agent-and-autonomous-agents-and-sandbox-status
**Areas discussed:** Scope boundary, Subagent surface & worktree, Autonomous (--yolo) scope, Status + process registry

---

## Initial scope framing

| Option | Description | Selected |
|--------|-------------|----------|
| Multi-agent / subagent polish | Harden/expose delegate_task subagent system (/agents, status pill, worktree, lifecycle) | ✓ |
| Autonomous mode (--yolo) | Non-interactive mode, bypass approvals, iteration/timeout guards | ✓ |
| `hermes status` CLI | Port `hermes status [--all] [--deep]` component diagnostics | ✓ |
| Background process registry / sandbox status | Port process_registry.py, background process tracking, /stop, session cleanup | ✓ |

**User's choice:** All four.
**Notes from user:** Described hermes multi-agent architecture: delegate_tool.py for subagent spawning; Toolsets (19 named) + 6 terminal backends as the "multi-entity" pattern; prompt_builder + AGENTS.md for role definitions; gateway/mirror.py for cross-session visibility; Plugin System (User/Project/Pip discovery) for team extensibility. Explicitly said: "skills can be enabled/disabled per platform via skills_config.py, effectively allowing you to 're-spec' an agent for a specific team role on the fly."

| Option | Description | Selected |
|--------|-------------|----------|
| Blocked by demo/UAT | Fix specific gap hit during real usage | |
| Parity closeout for v2.0 | Fill known hermes-agent gaps before shipping v2.0 | ✓ |
| Dogfooding pain | Smooth rough edges hit while using IronHermes | |
| Roadmap grooming | Placeholder, scope from scratch | |

**User's choice:** Parity closeout for v2.0.

---

## Scope boundary

### Parity-adjacent pieces: IN or OUT of 21.7?

| Option | Description | Selected |
|--------|-------------|----------|
| Toolsets grouping + per-platform enable | 19 toolsets + `hermes tools` CLI | ✓ |
| Terminal backends (Docker/SSH/Modal/Singularity) | 6 env backends for containerized/remote execution | ✓ |
| Gateway session mirror (cross-session visibility) | Port gateway/mirror.py | ✓ |
| Plugin discovery sources (User/Project/Pip) | 3-source plugin loading | ✓ |

**User's initial choice:** All four (apparent "include everything").

| Option | Description | Selected |
|--------|-------------|----------|
| Minimum viable parity (anchors only) | Only the 4 title anchors; adjacencies become own phases | ✓ (recommended) |
| Anchors + toolsets | Middle ground | |
| Wide — all parity-adjacent | Anchors + all 4 adjacencies; multiple waves | |

**User's initial choice:** Minimum viable parity.

### Conflict reconciliation

Claude flagged the contradiction (Q1 "all four IN" vs Q2 "anchors only").

| Option | Description | Selected |
|--------|-------------|----------|
| Minimum viable — anchors only | Keep 21.7 tight | ✓ |
| Wide — include all four adjacencies | Big multi-wave phase | |
| Middle ground — anchors + toolsets only | Anchors + toolset grouping | |
| Let me describe the cut | Freehand | |

**User's choice:** Minimum viable — anchors only.
**Notes:** Scope LOCKED as anchors-only. Toolsets, terminal backends, gateway mirror, and plugin discovery all deferred to own phases.

---

## Subagent surface & worktree

### Worktree-parallel mode

| Option | Description | Selected |
|--------|-------------|----------|
| Defer to own phase (recommended) | Worktree isolation is substantial; scope it separately | ✓ |
| Include minimal version | `--worktree` flag, no auto-merge | |
| Include full version | Worktree + cleanup + merge prompt + conflict policy | |

**User's choice:** Defer to own phase.

### Visibility surface

| Option | Description | Selected |
|--------|-------------|----------|
| /agents slash command | List active subagents with kill/logs subcommands | ✓ |
| Status-line indicator | TUI pill showing active-subagent count | ✓ |
| Persistent subagent transcripts | JSONL logs at $HERMES_HOME/subagent-transcripts/ | ✓ |
| Gateway subagent echo | Subagent tool progress echoed to parent's gateway thread | ✓ |

**User's choice:** All four.
**Notes:** "Gateway subagent echo" is narrower than full gateway mirror — reuses existing SubagentProgressCallback, not a cross-session mirror port. Consistent with minimum-viable cut.

### Lifecycle gaps

| Option | Description | Selected |
|--------|-------------|----------|
| Cancellation on parent ctrl-c | Verify child_token() cascade; add regression test | ✓ |
| Subagent timeout (wall-clock) | Per-call + config default; hard-cancel on timeout | ✓ |
| Concurrency surface | Semaphore state visible in /agents + status | ✓ |
| Subagent budget inheritance (PROV-10) | Parent/child shared iteration budget | ✓ |

**User's choice:** All four.

---

## Autonomous (--yolo) scope

### Approval bypass scope

| Option | Description | Selected |
|--------|-------------|----------|
| Blanket bypass (recommended) | Skip every approval gate, matches hermes --yolo | ✓ |
| Bypass except destructive | Skip normal, still prompt for hard-destructive ops | |
| Policy file | Config-driven allow/deny regex list | |

**User's choice:** Blanket bypass.

### Iteration budget

| Option | Description | Selected |
|--------|-------------|----------|
| Fold in — full PROV-09 + PROV-10 (recommended) | 2-tier pressure (70/90/100) + parent/child shared | ✓ |
| Fold in — hard cap only | Just iteration cap, no 2-tier pressure | |
| Defer budget | Ship --yolo without budget (risky) | |

**User's choice:** Full PROV-09 + PROV-10.

### Non-interactive error policy

| Option | Description | Selected |
|--------|-------------|----------|
| Continue past tool errors (recommended) | Tool errors feed back to model; agent keeps iterating | ✓ |
| Stop on first error | Any tool error terminates | |
| Configurable retry | Per-tool retry count | |

**User's choice:** Continue past tool errors.

### --yolo surface

| Option | Description | Selected |
|--------|-------------|----------|
| CLI flag + config (recommended) | Both; flag wins; gateway reads config only | ✓ |
| CLI flag only | Ephemeral per-invocation | |
| Config only | No flag | |

**User's choice:** CLI flag + config.

---

## Status + process registry

### `hermes status` components

| Option | Description | Selected |
|--------|-------------|----------|
| Provider + model | Active provider, resolved model, API mode, fallback chain | ✓ |
| Memory + sessions | Memory provider, capacity, session count, state.db size | ✓ |
| Gateway + platforms | Telegram auth, bot token, gateway PID, allowlist | ✓ |
| Subagents + processes + MCP | Active subagents, background processes, MCP servers | ✓ |

**User's choice:** All four sections.

### Output format + depth

| Option | Description | Selected |
|--------|-------------|----------|
| Styled text + --json + --deep (recommended) | Styled default; --json machine-readable; --deep live probes | ✓ |
| Styled text only | Simpler, no flags | |
| JSON first, pretty wrapper | Internal JSON, TTY pretty-prints | |

**User's choice:** Styled text + --json + --deep.

### Process registry design

| Option | Description | Selected |
|--------|-------------|----------|
| In-memory + session-scoped cleanup (recommended) | Simplest; no crash recovery | ✓ |
| In-memory + JSON checkpoint | Match hermes; ~/.ironhermes/processes.json | |
| SQLite-backed | Durable, queryable; overkill | |

**User's choice:** In-memory + session-scoped cleanup.

### Process registry features

| Option | Description | Selected |
|--------|-------------|----------|
| Spawn/poll/wait/kill + output buffer | Core ops + 200KB rolling buffer | ✓ |
| /stop slash — kill all | Non-interactive kill-all in current session | ✓ |
| Watch patterns + notifications | watch_patterns trigger new agent turn; rate-limited 8/10s | ✓ |
| `hermes status` integration | Surfaces under Processes section | ✓ |

**User's choice:** All four.
**Notes:** Claude flagged watch patterns as "probably defer" — user chose to include. Rate limiting (8 per 10s window, 45s sustained overload disables watch) ported from hermes `WATCH_MAX_PER_WINDOW` / `WATCH_WINDOW_SECONDS` / `WATCH_OVERLOAD_KILL_SECONDS`.

---

## Claude's Discretion

Captured as D-XX items with "Claude's Discretion" sub-section in CONTEXT.md §Implementation Decisions:

- `subagent.default_timeout_seconds` default (likely 300)
- `agent.max_iterations` default (likely 50)
- Status output layout details (column widths, color assignments)
- `--json` schema field names (must be stable; document in SUMMARY)
- Subagent transcript JSONL schema (must round-trip losslessly)
- Whether 2-tier pressure advisory text is a PromptBuilder slot or hardcoded inline

## Deferred Ideas

Captured in CONTEXT.md §deferred. Summary:

- Worktree-parallel mode (own phase)
- Toolsets grouping + per-platform enable (TOOL-02, own phase)
- Terminal backends (Docker/SSH/Modal/Singularity — likely 3+ phases)
- Full gateway session mirror (only narrow subagent echo in 21.7)
- Plugin discovery sources (User/Project/Pip)
- Full dangerous-command approval queue (21.7 bypasses but doesn't build)
- Profile system (parity gap #8)
- JSON checkpoint file for process registry
- SQLite-backed process registry
- Per-tool retry policy under --yolo
- Classification-based approval bypass
