---
status: partial
phase: 21.7-multi-agent-and-autonomous-agents-and-sandbox-status
source: [21.7-VERIFICATION.md]
started: "2026-04-23T15:30:00Z"
updated: "2026-04-23T15:30:00Z"
---

## Current Test

[awaiting human testing]

## Tests

### 1. Interactive /agents list|kill|logs smoke test in REPL
expected: Launching `hermes` in chat mode, delegating a subagent task, and running `/agents list` shows a live row; `/agents kill <id>` cancels it; `/agents logs <id>` shows the transcript tail.
why_human: Slash handlers require a live REPL context + a running subagent; programmatic verification confirms handler wiring (crates/ironhermes-core/src/commands/handlers.rs:133-211) but end-to-end UX needs human in the loop.
result: failed
observed: |
  User spawned 3 concurrent delegate_task subagents researching LoRA. While the subagent progress ticker
  showed "[subagent-1] Running: web_read", "[subagent-2] Running: web_read", "[subagent-3] Running: web_read"
  output streaming live, `/agents list` returned "No active subagents."
  `/agents kill` and `/agents logs` without an id returned their expected usage errors.
  CommandContext.subagent_registry IS populated (would return "Subagent registry not wired." otherwise),
  but SubagentRegistry.list() returned empty while delegate_task was actively running children.
  Likely defect: either AgentSubagentRunner::run_child's register() call is not firing on the live path,
  or there's a register/unregister timing/ordering issue that lets the registry appear empty mid-run.

### 2. Double ctrl-c cascade cancellation under live delegate_task
expected: First ctrl-c cancels in-flight parent + every child subagent process within p95 < 500ms; second ctrl-c within 1.5s exits cleanly.
why_human: SIGINT delivery + wall-clock cascade timing is flaky in CI; Plan 10 S-03 is `#[ignore]`'d with explicit VALIDATION.md UAT pointer.
result: passed
observed: User ran 3 concurrent subagents then pressed ctrl-c twice — "turn cancelled" message fired, subagent-2 reported Done, clean prompt returned. "/quit to exit" prompt appeared for the second ctrl-c.

### 3. --yolo banner + approval bypass behavior end-to-end
expected: `hermes chat --yolo` prints the red stderr banner once; dangerous-command approval prompts are bypassed; iteration budget + ctrl-c + fatal-error still halt the agent.
why_human: End-to-end banner emission + approval skip requires a live tool call; `resolve_yolo` and `maybe_print_yolo_banner` unit tests confirm the state machine, but banner-once semantics across CLI + config sources want human validation.
result: [pending]

### 4. Watch-pattern rate limiter under sustained real child-process output
expected: Spawning a noisy child (e.g. `yes "MATCH" | head -n 5000`) and a watch pattern matching `MATCH` produces exactly 8 notifications in first 10s, then `tracing::warn` drops, then AutoDisable after 45s.
why_human: `tokio::time::pause()` tests in crates/ironhermes-exec/tests/process_registry_watch_rate_limit.rs prove the state machine is correct; real-world wall-clock validation is Plan 10 S-11/S-12 scope and was never exercised against a real child.
result: [pending]

### 5. hermes status --deep against live provider/FTS5/MCP endpoints
expected: Provider HEAD probe returns `reachable=true` for configured openrouter model; FTS5 `integrity_check=ok` against the real state.db; MCP HEAD probe reports honest unreachable fallback for offline servers.
why_human: `LiveDeepProbe` was spot-tested via `MockDeepProbe` fault injection (status_cmd_deep_probe.rs) but real provider / real DB / real MCP round-trips were never network-exercised; CLI command runs but `--deep` behavior against live services wants a human.
result: [pending]

## Summary

total: 5
passed: 1
issues: 1
pending: 3
skipped: 0
blocked: 0

## Gaps

### GAP-21.7-01: /agents list returns empty while subagents are live

source: HUMAN-UAT test 1
severity: blocking
status: failed
summary: |
  `/agents list` reports "No active subagents." while delegate_task subagents
  are actively running (ticker output shows them streaming web_read calls).
  CommandContext.subagent_registry is populated (otherwise the handler would
  return "Subagent registry not wired."), but SubagentRegistry.list() is
  empty mid-run. AgentSubagentRunner::run_child does call
  `reg.write().await.register(info)` (subagent_runner.rs:176) — this needs
  runtime investigation to determine whether the register call fails silently,
  fires on a different SubagentRegistry instance than CommandContext reads,
  or is masked by a race condition.
investigation_hints:
  - Verify at runtime that `subagent_registry.clone()` passed to `AgentSubagentRunner` (main.rs:737)
    and `SubagentRegistryHandle::new(subagent_registry.clone())` wrapped into CommandContext
    (main.rs:1030-1032) are in fact the same Arc — same inner pointer.
  - Add a `tracing::info!` at register/unregister sites and reproduce.
  - Add an integration test that spawns a real delegate_task (via a mock runner)
    and calls `/agents list` mid-run — should be deterministic.
  - Consider whether `tokio::task::block_in_place` inside `SubagentRegistryHandle::list_summary`
    can see writes made from a different tokio worker thread's held write-lock.
