---
phase: 26.7.2
slug: sessions-load-session-data
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-19
---

# Phase 26.7.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo build --features web` + `cargo clippy` (compile-time verification) |
| **Config file** | `crates/iron_hermes_ui/Cargo.toml` |
| **Quick run command** | `cargo build -p iron_hermes_ui --features web 2>&1 \| tail -5` |
| **Full suite command** | `cargo clippy -p iron_hermes_ui --features web -- -D warnings && cargo build -p iron_hermes_ui --features web` |
| **Estimated runtime** | ~30–60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo build -p iron_hermes_ui --features web 2>&1 | tail -5`
- **After every plan wave:** Run full clippy + build
- **Before `/gsd:verify-work`:** Full suite must be green + UAT session-load walkthrough
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Secure Behavior | Test Type | Automated Command | Status |
|---------|------|------|-------------|-----------------|-----------|-------------------|--------|
| server-fn | 01 | 1 | D-10 (history cap) | Returns ≤50 user/assistant rows, no system/tool leakage | compile | `cargo build -p iron_hermes_ui --features web` | ⬜ pending |
| sessioninfo-enrichment | 01 | 1 | D-04 (last_message) | last_message truncated ≤80 chars server-side | compile | `cargo build -p iron_hermes_ui --features web` | ⬜ pending |
| divider-variant | 02 | 2 | D-02 (divider row) | ChatBubbleKind::Divider match arms exhaustive | compile | `cargo clippy -p iron_hermes_ui --features web` | ⬜ pending |
| use-effect-wiring | 02 | 2 | D-06 (use_effect on session_id) | No GenerationalRef held across await | compile | `cargo clippy -p iron_hermes_ui --features web -- -D warnings` | ⬜ pending |
| timestamp-format | 02 | 2 | D-05 (relative time) | WASM-gated js_sys call; no-op on non-wasm | compile | `cargo build -p iron_hermes_ui --features web` | ⬜ pending |
| uat-session-load | — | UAT | D-06/D-07/D-08/D-09 | History visible, divider present, input active | manual | `dx serve` + browser walkthrough | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements — no new test framework install needed.

- `cargo build` and `cargo clippy` provide compile-time verification for all structural changes
- Manual UAT via `dx serve` covers behavioral verification (D-06/D-07/D-08/D-09)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| History loads in chat after clicking session row | D-06, D-08, D-09 | Dioxus WASM UI; no headless test harness | Run `dx serve`, open Sessions, click a row with messages, verify bubbles appear in Chat with divider |
| Divider appears between history and live input | D-02 | Visual/layout check | After above, send a new message; verify divider separates loaded history from new bubble |
| Relative timestamp in session row | D-05 | JavaScript Date formatting in WASM | In Sessions screen, verify "2 days ago" / "just now" format replaces raw timestamp |
| `/clear` wipes history bubbles | D-03 | Requires slash command processing | After loading history, type `/clear`; verify all bubbles including historical ones are wiped |
| Session switch clears stale history | D-08 | Multi-session interaction | Load session A, switch to session B; verify chat stream shows B's history, not A's |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify (compile) or manual UAT specification
- [ ] No 3 consecutive tasks without a compile verification step
- [ ] Wave 0: no new framework installs required
- [ ] No watch-mode flags in commands
- [ ] Feedback latency < 60s for compile checks
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
