---
phase: 26.7.2-sessions-load-session-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/iron_hermes_ui/src/server/api.rs
autonomous: true
requirements:
  - D-04
  - D-10
must_haves:
  truths:
    - "GET /api/session-messages?id={sid} returns up to 50 messages for that session, filtered to user+assistant roles, oldest first"
    - "SessionInfo carries a last_message Option<String> field whose value is the most recent assistant (or user fallback) text truncated to 80 chars server-side"
    - "list_sessions populates last_message for every returned session (None when no renderable message exists)"
  artifacts:
    - path: "crates/iron_hermes_ui/src/server/api.rs"
      provides: "ChatMessage wire struct, get_session_messages #[get] server fn, last_message field on SessionInfo, extract_last_message_preview helper, HISTORY_LIMIT constant"
      contains: "pub struct ChatMessage"
  key_links:
    - from: "api.rs::get_session_messages"
      to: "ironhermes_state::StateStore::get_messages"
      via: "state.state_store.lock().unwrap().get_messages(&id)"
      pattern: "state_store"
    - from: "api.rs::list_sessions"
      to: "extract_last_message_preview"
      via: "per-session call inside the existing .map() block"
      pattern: "extract_last_message_preview"
---

<objective>
Wave 1 — pure server-side surface for Phase 26.7.2. Add the wire type, history endpoint, and SessionInfo enrichment that Plan 02 (client wiring) consumes. Zero Dioxus signal code; zero WASM concerns. Everything in this plan lives in api.rs.

Purpose: Plan 02's `use_effect` in ScreenChat cannot be written until the `ChatMessage` type and `get_session_messages` fn it calls exist. SessionRow's `last_message` preview cannot render until `SessionInfo.last_message` is populated.

Output:
- New struct `ChatMessage { role: String, content: String }` in api.rs (Serialize+Deserialize, derives match SessionInfo).
- New `#[get("/api/session-messages")]` server fn `get_session_messages(id: String) -> Result<Vec<ChatMessage>>` with `HISTORY_LIMIT: usize = 50`.
- New field `last_message: Option<String>` on `SessionInfo`.
- New private helper `extract_last_message_preview(&[StoredMessage]) -> Option<String>` (80-char truncation, assistant-preferred fallback to user).
- Wired into `list_sessions()` so every returned `SessionInfo` carries the preview.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.7.2-sessions-load-session-data/26.7.2-CONTEXT.md
@.planning/phases/26.7.2-sessions-load-session-data/26.7.2-RESEARCH.md
@.planning/phases/26.7.2-sessions-load-session-data/26.7.2-PATTERNS.md
@crates/iron_hermes_ui/src/server/api.rs
@crates/iron_hermes_ui/src/server/state.rs
@crates/iron_hermes_ui/CLAUDE.md

<interfaces>
Source-of-truth contracts the executor must respect. Extracted from codebase and RESEARCH.md Q4/Q6/Q7.

From crates/ironhermes-state/src/lib.rs (StoredMessage — server-only type):
- pub struct StoredMessage with fields including: pub id: i64, pub role: String, pub content: Option<String>
- StateStore::get_messages(&self, session_id: &str) returns Result<Vec<StoredMessage>, _>; returns ALL messages id ASC, no internal limit.
- Role string values observed: "system", "user", "assistant", "tool".
- started_at is fractional seconds (f64), already cast `as i64` in existing list_sessions mapping.

From crates/iron_hermes_ui/src/server/api.rs (current shape):
- pub struct SessionInfo with fields: pub id: String, pub title: Option<String>, pub created_at: String, pub message_count: u32.
- pub async fn list_sessions() returning Result<Vec<SessionInfo>>; already filters .filter(|s| s.message_count > 0).
- All existing #[get] routes use QUERY-PARAM style. Example: api_agents_status declared as #[get("/api/agents/status")] with `id: String`. NO path-param routes anywhere in the file.
- Mutex idiom: state.state_store.lock().unwrap().<method>(args).map_err(|e| ServerFnError::new(format!("...: {e}")))

From crates/iron_hermes_ui/src/server/api.rs derive convention (lines 6-12):
- All wire structs derive: #[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add ChatMessage struct and last_message field on SessionInfo</name>
  <files>crates/iron_hermes_ui/src/server/api.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/api.rs (current SessionInfo definition at lines ~6-12; all #[derive] attributes in this file; existing imports at top).
    - crates/iron_hermes_ui/src/server/state.rs (lines ~539-560 — stored_to_chat_message — confirms StoredMessage role/content shape used elsewhere).
    - .planning/phases/26.7.2-sessions-load-session-data/26.7.2-PATTERNS.md sections "New wire-type struct — ChatMessage" and "last_message field on SessionInfo".
  </read_first>
  <action>
    Edit crates/iron_hermes_ui/src/server/api.rs and make two changes implementing D-04 (last_message preview) and the wire type required by D-10 (history endpoint).

    Change A — modify the existing pub struct SessionInfo: add ONE new field at the end of the struct body, after `pub message_count: u32`:
      pub last_message: Option<String>,
    Add an inline comment that references "Phase 26.7.2 D-04: last message preview (~80 chars, server-truncated)". Keep all existing fields (id, title, created_at, message_count) in their current order and types. The struct still derives Serialize, Deserialize, Clone, Debug, PartialEq.

    Change B — add a NEW pub struct ChatMessage immediately after the SessionInfo block (or before the first #[get] fn — colocate with the other wire structs). Use the exact derive line that matches SessionInfo: derive(Serialize, Deserialize, Clone, Debug, PartialEq). Fields in this exact order:
      - pub role: String, with a trailing comment noting tool/system filtered out server-side
      - pub content: String, with a trailing comment noting empty string when StoredMessage.content is None
    Add a doc comment above the struct referencing Phase 26.7.2 D-10 and explaining the role filtering rationale.

    Do NOT define ChatMessage in chat.rs or in the ironhermes-state crate — RESEARCH Q4 and the Anti-Patterns section lock it in api.rs because ironhermes-state is server-only and ChatBubble is client-only. ChatMessage is the wire type that bridges them.

    Do NOT modify any other struct. Do NOT add any new server fn in this task (that is Task 2). Do NOT touch the list_sessions .map() body in this task (that is Task 3).

    After editing, run cargo check to confirm the file still parses. Note: the existing `list_sessions` construction site `SessionInfo { id: ..., title: ..., created_at: ..., message_count: ... }` will now be missing the new `last_message` field. If cargo reports this as an error in Task 1, that is EXPECTED — it is repaired in Task 3 where we wire the helper. To prevent a hard fail mid-task, add a single placeholder field initializer in the existing .map(|session| SessionInfo { ... }) block at the construction site so the struct literal is complete:
      last_message: None,
    Task 3 will replace `None` with the real helper invocation. This keeps every task individually compilable.
  </action>
  <verify>
    <automated>cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features server 2>&1 | tail -40 ; grep -nE 'pub struct (SessionInfo|ChatMessage)' crates/iron_hermes_ui/src/server/api.rs ; grep -nE 'pub last_message: Option' crates/iron_hermes_ui/src/server/api.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub struct ChatMessage' crates/iron_hermes_ui/src/server/api.rs` returns 1.
    - `grep -nE 'pub role: String' crates/iron_hermes_ui/src/server/api.rs` shows a line inside the ChatMessage struct body.
    - `grep -nE 'pub content: String' crates/iron_hermes_ui/src/server/api.rs` shows a line inside the ChatMessage struct body.
    - `grep -c 'pub last_message: Option' crates/iron_hermes_ui/src/server/api.rs` returns exactly 1 (one field declaration on SessionInfo).
    - ChatMessage and SessionInfo BOTH carry the derive line `#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]` immediately above their declarations.
    - The existing `list_sessions` .map() construction site has been updated with `last_message: None,` as a placeholder so the struct literal is complete.
    - `cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features server` exits 0.
  </acceptance_criteria>
  <done>
    api.rs declares ChatMessage with exactly two String fields (role, content) and SessionInfo carries a single new Option<String> field named last_message. list_sessions's struct literal initializes last_message to None as a placeholder (replaced in Task 3). cargo check passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add get_session_messages server fn and HISTORY_LIMIT constant</name>
  <files>crates/iron_hermes_ui/src/server/api.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/api.rs (the entire #[get] route block — examine api_agents_status at lines ~343-349 as the canonical simplest query-param parameterized route; also examine list_sessions lines ~77-113 for the state_store Mutex + map_err idiom).
    - crates/iron_hermes_ui/src/server/state.rs (confirm StateStore::get_messages signature returns Vec<StoredMessage> and how stored_to_chat_message destructures role/content).
    - crates/ironhermes-state/src/lib.rs (StoredMessage struct: confirm content is Option<String>, role is String).
    - .planning/phases/26.7.2-sessions-load-session-data/26.7.2-RESEARCH.md section "Pattern 1: Server fn with query param".
    - .planning/phases/26.7.2-sessions-load-session-data/26.7.2-PATTERNS.md section "New #[get] server fn — get_session_messages".
  </read_first>
  <action>
    Add a new public server fn `get_session_messages` to api.rs. Place it immediately after `create_session`. Do NOT place it inside any impl block — #[get] fns in this file are free functions at module scope.

    Add a module-level constant before the fn:
      const HISTORY_LIMIT: usize = 50;
    Doc-comment it referencing D-10 and noting that this is a UI display cap only — the agent's build_messages_for_turn has independent context-window logic.

    The fn signature MUST be EXACTLY:
      #[get("/api/session-messages")]
      pub async fn get_session_messages(id: String) -> Result<Vec<ChatMessage>>

    Rationale (RESEARCH Q1): every other #[get] in this file uses query-param style with a free `id: String` parameter. No path-parameterized routes exist anywhere. Do NOT write `#[get("/api/sessions/{id}/messages")]` — that pattern is absent from the codebase and would diverge from established convention.

    Fn body — exact algorithm from RESEARCH Pattern 1 plus Q4 filter rule:
      1. Acquire global state: let state = crate::server::state::global_app_state();
      2. Lock state_store and call get_messages, map_err to ServerFnError:
         let all_msgs = state.state_store.lock().unwrap().get_messages(&id).map_err(|e| ServerFnError::new(format!("get_messages failed: {e}")))?;
      3. Build the response: iterate all_msgs by reference, FIRST filter to only `m.role == "user" || m.role == "assistant"` (tool/system not renderable per Q4), THEN .rev().take(HISTORY_LIMIT).rev() so the tail of last 50 visible messages is returned in chronological (oldest-first) order, THEN map each StoredMessage to ChatMessage { role: m.role.clone(), content: m.content.clone().unwrap_or_default() }. Collect into Vec<ChatMessage>.
      4. Return Ok(out).

    CRITICAL ORDERING RULE from Q4: filter MUST run BEFORE the .take(HISTORY_LIMIT) slice. If you reverse the order you will consume the 50-message cap with invisible tool/system rows and lose visible content.

    Do NOT add tool_calls, id, timestamp, or any other field to ChatMessage — RESEARCH Q4 explicitly excludes them. Do NOT add per-request auth — consistent with all other server fns in this file (Security Domain section).

    Do NOT touch list_sessions in this task — Task 3 handles the last_message enrichment.

    Ensure ChatMessage is in scope (defined in the same file from Task 1) and that any required imports (Serialize/Deserialize already present, ServerFnError already used elsewhere) need no additions. If `Result` is aliased in this file, use the same alias as the other #[get] fns — do NOT introduce a new alias.
  </action>
  <verify>
    <automated>cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features server 2>&1 | tail -40 ; grep -n 'const HISTORY_LIMIT' crates/iron_hermes_ui/src/server/api.rs ; grep -n 'api/session-messages' crates/iron_hermes_ui/src/server/api.rs ; grep -n 'pub async fn get_session_messages' crates/iron_hermes_ui/src/server/api.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'const HISTORY_LIMIT: usize = 50' crates/iron_hermes_ui/src/server/api.rs` returns exactly 1.
    - `grep -c 'api/session-messages' crates/iron_hermes_ui/src/server/api.rs` returns at least 1 (the #[get] attribute string literal).
    - `grep -c 'pub async fn get_session_messages' crates/iron_hermes_ui/src/server/api.rs` returns exactly 1.
    - The fn signature accepts a single parameter `id: String` and returns a Vec<ChatMessage> wrapped in the file's Result alias.
    - The fn body contains the literal `state.state_store.lock().unwrap().get_messages(&id)` and `.take(HISTORY_LIMIT)`.
    - The fn body filters by role to only "user" and "assistant" BEFORE the take call. Verify by grepping the fn body: filter line appears at a lower line number than the take line.
    - No path-param syntax introduced: `grep -c 'api/session-messages/{' crates/iron_hermes_ui/src/server/api.rs` returns 0.
    - `cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features server` exits 0.
  </acceptance_criteria>
  <done>
    api.rs declares HISTORY_LIMIT = 50 and a get_session_messages #[get] server fn that returns the tail of the last 50 user-or-assistant messages for a session id in chronological order. cargo check --features server passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire extract_last_message_preview into list_sessions to populate last_message</name>
  <files>crates/iron_hermes_ui/src/server/api.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/api.rs (specifically the list_sessions fn at lines ~77-113 and the existing private helpers section near `format_context_window` at ~line 224 — the new helper goes in that block).
    - crates/iron_hermes_ui/src/server/state.rs (lines ~539-560 stored_to_chat_message — shows the role string set and the Option<String> content handling).
    - .planning/phases/26.7.2-sessions-load-session-data/26.7.2-RESEARCH.md section Q6 (last_message preview — role filtering and content handling) and Pitfall 5 (N+1 acceptable for this phase).
    - .planning/phases/26.7.2-sessions-load-session-data/26.7.2-PATTERNS.md sections "Enrich list_sessions with last_message" and the "extract_last_message_preview" helper template.
  </read_first>
  <action>
    Replace the `last_message: None,` placeholder added in Task 1 with a real per-session derivation, and add a new private helper that does the work.

    Step A — add a private helper fn `extract_last_message_preview` in the private-helpers section of api.rs (near `format_context_window` at ~line 224). Signature:
      fn extract_last_message_preview(msgs: &[ironhermes_state::StoredMessage]) -> Option<String>

    Body (exact algorithm — RESEARCH Q6):
      1. Iterate msgs in reverse. Find the first message where role == "assistant" AND content.is_some(). Call this `preview`.
      2. If no such assistant message exists, fall back: iterate msgs in reverse, find the first message where role == "user" AND content.is_some(). This becomes `preview` instead.
      3. If still no candidate, return None.
      4. Otherwise, take preview's content (an &String), truncate to 80 chars using chars().take(80).collect::<String>() to handle multi-byte characters safely (do NOT use slice indexing like &s[..80] — would panic on non-ASCII boundary), and return Some(truncated).

    Skip "tool" and "system" messages entirely (do not consider them as candidates). Skip any message where content is None (tool-call-only assistant response — pick the previous one instead).

    Strip role prefix (RESEARCH Claude's Discretion bullet): just return the raw content. The SessionRow's sub-text already shows message count and id-tail context; the preview should be content only without "You: " or "Hermes: " prefixes.

    If `ironhermes_state::StoredMessage` is not already in scope at the top of api.rs, either add a `use ironhermes_state::StoredMessage;` import OR use the fully-qualified path `ironhermes_state::StoredMessage` in the helper signature (the path form is preferred — keeps the import block byte-stable and explicit at the call site). Check whether the import already exists; if so, use the short form.

    Step B — modify the existing .map() block inside list_sessions to invoke the helper per session. Before this task, the map looks like:
      .map(|session| SessionInfo {
          id: session.id,
          title: session.title,
          created_at: format!("{}", session.started_at as i64),
          message_count: session.message_count.max(0) as u32,
          last_message: None,   // placeholder from Task 1
      })

    After this task, the map MUST look like this (lock the state_store fresh per-session, run the helper, place into the struct literal):
      .map(|session| {
          let last_message = {
              let store = state.state_store.lock().unwrap();
              store.get_messages(&session.id).ok().and_then(|msgs| {
                  extract_last_message_preview(&msgs)
              })
          };
          SessionInfo {
              id: session.id,
              title: session.title,
              created_at: format!("{}", session.started_at as i64),
              message_count: session.message_count.max(0) as u32,
              last_message,
          }
      })

    Signal-borrow rule for the Mutex: the inner block `{ let store = state.state_store.lock().unwrap(); ... }` scopes the MutexGuard. The guard must drop before the SessionInfo struct literal evaluates `session.id` (which moves the String out of `session`). Confirm by reading: the block returns last_message: Option<String>, the guard is dropped at the closing `}` of the inner block, then the struct literal runs. This ordering is enforced by the structure shown above — do NOT inline the lock+get_messages into the struct literal (would extend the guard lifetime across the SessionInfo construction).

    Performance note from RESEARCH Pitfall 5: this is an N+1 SQLite read pattern (one extra get_messages call per session, up to 100 per page load). Acceptable for this phase per CONTEXT.md and RESEARCH Assumption A3. Do NOT add caching, batching, or a new SQL query in this task — that is a future-phase optimization. The acceptance criteria expressly do not include a performance gate.

    Do NOT touch get_session_messages from Task 2. Do NOT touch the SessionInfo struct definition or the ChatMessage struct definition. Do NOT remove the .filter(|s| s.message_count > 0) call that already exists in list_sessions (Phase 26.2.1 R3 invariant).

    Run cargo check --features server and confirm it exits 0.
  </action>
  <verify>
    <automated>cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features server 2>&1 | tail -40 ; grep -c 'fn extract_last_message_preview' crates/iron_hermes_ui/src/server/api.rs ; grep -c 'extract_last_message_preview(' crates/iron_hermes_ui/src/server/api.rs ; grep -c 'chars().take(80)' crates/iron_hermes_ui/src/server/api.rs ; grep -c 'last_message: None' crates/iron_hermes_ui/src/server/api.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fn extract_last_message_preview' crates/iron_hermes_ui/src/server/api.rs` returns exactly 1 (helper defined).
    - `grep -c 'extract_last_message_preview(' crates/iron_hermes_ui/src/server/api.rs` returns at least 2 (one definition site, at least one call site inside list_sessions).
    - `grep -c 'chars().take(80)' crates/iron_hermes_ui/src/server/api.rs` returns at least 1 (multi-byte-safe truncation in the helper).
    - `grep -c 'last_message: None' crates/iron_hermes_ui/src/server/api.rs` returns 0 (placeholder from Task 1 is gone — replaced with real wiring).
    - `grep -c 'last_message,' crates/iron_hermes_ui/src/server/api.rs` returns at least 1 (shorthand field init from the surrounding let-binding inside the .map closure).
    - The helper body grep shows BOTH `"assistant"` and `"user"` role checks (assistant preferred, user fallback): `grep -A 15 'fn extract_last_message_preview' crates/iron_hermes_ui/src/server/api.rs | grep -c '"assistant"'` returns at least 1 AND `grep -A 15 'fn extract_last_message_preview' crates/iron_hermes_ui/src/server/api.rs | grep -c '"user"'` returns at least 1.
    - The helper does NOT use direct byte-slicing like `&s[..80]` — confirm `grep -c '\[\.\.80\]' crates/iron_hermes_ui/src/server/api.rs` returns 0.
    - The .filter(|s| s.message_count > 0) call (Phase 26.2.1 R3 invariant) is still present: `grep -c 'message_count > 0' crates/iron_hermes_ui/src/server/api.rs` returns at least 1.
    - `cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features server` exits 0.
  </acceptance_criteria>
  <done>
    list_sessions returns SessionInfo records whose last_message field is a Some(80-char-or-less preview) when the session has any user or assistant message with content, or None otherwise. The helper prefers the most recent assistant message and falls back to the most recent user message. cargo check --features server passes. Plan 02 can now consume both the new ChatMessage wire type (Task 2) and the enriched SessionInfo (Tasks 1 + 3) on the client side.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → server fn `get_session_messages` | The `id: String` query param is attacker-controllable in any network-exposed deployment. Crosses into a SQLite parameterized query inside StateStore::get_messages. |
| HTTP client → server fn `list_sessions` | Triggers N+1 get_messages reads — N controlled by the number of sessions in the store, not by request input. |
| StateStore SQLite store → ChatMessage wire payload | Stored message content originates from prior user input or LLM output. Returned as JSON across the WASM boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.7.2-01 | Tampering / Injection | get_session_messages `id: String` parameter into SQL | mitigate | StateStore::get_messages uses rusqlite `params![session_id]` parameterized binding — SQL injection structurally impossible. Same pattern as every other state_store call (Security Domain section of RESEARCH). No change to query construction in this plan. |
| T-26.7.2-02 | Information Disclosure | get_session_messages returns messages for any session id provided, no auth | accept | Consistent with all other server fns in api.rs — no per-request auth, local-only deployment per CLAUDE.md "No external services". Same risk tier as the existing api_agents_status and list_sessions endpoints. |
| T-26.7.2-03 | Denial of Service | Unbounded payload size from extremely long stored messages | mitigate | HISTORY_LIMIT = 50 caps message count. Per-message content size is bounded by whatever upstream wrote to StateStore (no new write path here — content size = existing system bound). The 80-char truncation in extract_last_message_preview also caps the per-session list_sessions payload growth. |
| T-26.7.2-04 | Denial of Service | N+1 SQLite reads in list_sessions can stall server under large session counts | accept | RESEARCH Pitfall 5 + Assumption A3 document this as acceptable for 50-100 sessions on local SQLite. Optimization deferred to a future phase. No mitigation in scope for this plan. |
| T-26.7.2-05 | Tampering | Multi-byte UTF-8 boundary panic in 80-char truncation | mitigate | The helper uses `chars().take(80).collect::<String>()` — character-aware. Direct byte-slicing is explicitly prohibited by an acceptance grep. |
| T-26.7.2-SC | Tampering | npm/pip/cargo installs | n/a | This plan adds ZERO new packages. All required crates (ironhermes-state, serde, dioxus-fullstack) are preexisting deps. RESEARCH "Package Legitimacy Audit" section: "No new packages introduced. Section not applicable." Package-legitimacy gate is inert for this plan. |
</threat_model>

<verification>
After all three tasks complete:

1. cargo check on the server target:
   `cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features server` exits 0.

2. cargo check on the web/wasm target (to confirm the wire type ChatMessage compiles on both sides):
   `cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features web` exits 0.

3. Grep audit (proves the locked decisions are honored):
   - `grep -c 'pub struct ChatMessage' crates/iron_hermes_ui/src/server/api.rs` returns 1 (D-10 wire type defined in api.rs not chat.rs).
   - `grep -c 'const HISTORY_LIMIT: usize = 50' crates/iron_hermes_ui/src/server/api.rs` returns 1 (D-10 cap as a constant).
   - `grep -c 'api/session-messages' crates/iron_hermes_ui/src/server/api.rs` returns at least 1 (query-param route shape, not path-param).
   - `grep -c 'pub last_message: Option' crates/iron_hermes_ui/src/server/api.rs` returns 1 (D-04 field on SessionInfo).
   - `grep -c 'chars().take(80)' crates/iron_hermes_ui/src/server/api.rs` returns at least 1 (D-04 80-char cap, multi-byte-safe).

4. Functional smoke test (optional — Plan 02 wires the UI):
   - With the dev server running, call: `curl 'http://localhost:8080/api/session-messages?id=<known-session-id>'`. Expect HTTP 200 + a JSON array of at most 50 ChatMessage entries, each containing only "role" and "content" string fields, with role in {"user", "assistant"} only.
   - Call: `curl http://localhost:8080/api/sessions` (the underlying list_sessions endpoint — confirm the exact path from api.rs at execution time). Expect every entry to include a `last_message` field (either string ≤80 chars or null).

5. No new dependencies:
   `git diff crates/iron_hermes_ui/Cargo.toml` should produce zero output for this plan.
</verification>

<success_criteria>
Plan 01 succeeds when:
- ChatMessage and SessionInfo (with new last_message field) are defined in api.rs with the standard derive set.
- get_session_messages is callable at GET /api/session-messages?id=… and returns Vec<ChatMessage> capped at 50, filtered to user+assistant, oldest first.
- extract_last_message_preview is a private helper that returns a chars-safe ≤80-char preview, assistant-preferred with user fallback.
- list_sessions populates last_message for every returned SessionInfo via the helper.
- cargo check on both --features server AND --features web exits 0.
- Zero new Cargo dependencies.
</success_criteria>

<output>
On completion, write `.planning/phases/26.7.2-sessions-load-session-data/26.7.2-01-SUMMARY.md` capturing: line ranges of the new ChatMessage struct + HISTORY_LIMIT + get_session_messages + extract_last_message_preview, the exact route string registered, whether StoredMessage required a new use-import or a fully-qualified path, the cargo check output tail for both target features, and any deviations from the RESEARCH/PATTERNS reference impls.
</output>
