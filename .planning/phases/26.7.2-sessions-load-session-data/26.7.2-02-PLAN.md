---
phase: 26.7.2-sessions-load-session-data
plan: 02
type: execute
wave: 2
depends_on:
  - 26.7.2-01
files_modified:
  - crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/sessions.rs
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
must_haves:
  truths:
    - "Clicking a SessionRow loads that session's last 50 visible messages into the Chat screen as ChatBubble items, in chronological order, followed by a divider bubble"
    - "Switching from one session to another clears the previous session's bubbles and any STREAMING indicator before fetching the new history"
    - "Sending a new message after loading history continues the session — the chat-input-pill stays active and the new turn is appended below the divider"
    - "On a brand-new session (zero history) the chat stream is empty with no divider, no loading indicator, no spinner"
    - "SessionRow displays a relative timestamp ('2 days ago', 'just now') instead of the raw Unix-seconds string"
    - "SessionRow's row-sub text shows the last_message preview when present, falling back to the existing 'N msgs · session ...' line when absent"
  artifacts:
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs"
      provides: "ChatBubbleKind::Divider variant + match-site exhaustiveness updates + use_effect on session_id wiring D-06"
      contains: "ChatBubbleKind::Divider"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/sessions.rs"
      provides: "format_relative timestamp helper (wasm-gated) + last_message preview wired into SessionRow sub-text"
      contains: "fn format_relative"
  key_links:
    - from: "chat.rs::ScreenChat::use_effect"
      to: "crate::server::api::get_session_messages"
      via: "spawn(async move { ... get_session_messages(sid).await ... })"
      pattern: "get_session_messages\\("
    - from: "chat.rs::use_effect"
      to: "Signal<Vec<ChatBubble>>"
      via: "bubbles.write().clear() then bubbles.write().extend(history)"
      pattern: "bubbles\\.write\\(\\)"
    - from: "sessions.rs::SessionRow"
      to: "format_relative helper"
      via: "format_relative(&session.created_at)"
      pattern: "format_relative\\("
    - from: "sessions.rs::SessionRow"
      to: "SessionInfo.last_message"
      via: "session.last_message.clone() → row-sub display"
      pattern: "session\\.last_message"
---

<objective>
Wave 2 — client-side wiring for Phase 26.7.2. Now that Plan 01 has shipped `ChatMessage`, `get_session_messages`, and `SessionInfo.last_message`, this plan adds:

1. `ChatBubbleKind::Divider` variant (chat.rs) + exhaustiveness fixes at both `match b.kind` sites (D-02).
2. A `use_effect` on `session_id` inside `ScreenChat` that clears bubbles, resets `streaming_id`, fetches history via the new server fn, and prepopulates `Signal<Vec<ChatBubble>>` plus a trailing divider when history is non-empty (D-01/D-06/D-07/D-08).
3. A `format_relative` helper in sessions.rs (wasm-gated) and SessionRow display swap from raw Unix-seconds string to relative time (D-05).
4. SessionRow sub-text wired to show `session.last_message` preview when present (D-04 display side; Plan 01 produces the data).

Purpose: this is the user-visible deliverable. Without this plan, the server fn from Plan 01 is dead code and the Sessions screen still shows raw timestamps and pre-existing "N msg · session …" sub-text only.

Output:
- chat.rs: Divider variant; both match arms updated (class selector + avatar selector); D-06 use_effect implemented; divider bubble RSX branch.
- sessions.rs: format_relative helper (wasm/non-wasm cfg-split); SessionRow renders relative timestamp; row-sub prefers last_message when Some.
- D-09 continuation semantics: chat-input-pill remains untouched — no code change required (the input is already enabled regardless of bubble content); test by sending a new message after loading history.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.7.2-sessions-load-session-data/26.7.2-CONTEXT.md
@.planning/phases/26.7.2-sessions-load-session-data/26.7.2-RESEARCH.md
@.planning/phases/26.7.2-sessions-load-session-data/26.7.2-PATTERNS.md
@.planning/phases/26.7.2-sessions-load-session-data/26.7.2-01-PLAN.md
@crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs
@crates/iron_hermes_ui/src/components/hermes_app/screens/sessions.rs
@crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs
@crates/iron_hermes_ui/src/components/hermes_app/mod.rs
@crates/iron_hermes_ui/src/server/api.rs
@crates/iron_hermes_ui/CLAUDE.md

<interfaces>
<!-- Contracts from Plan 01 + existing client code. Executor must not re-derive these from the codebase. -->

From crates/iron_hermes_ui/src/server/api.rs (Plan 01 output — already shipped before Plan 02 runs):
- pub struct ChatMessage { pub role: String, pub content: String }   // role is "user" or "assistant" — server pre-filtered
- pub struct SessionInfo { pub id: String, pub title: Option<String>, pub created_at: String, pub message_count: u32, pub last_message: Option<String> }
- #[get("/api/session-messages")] pub async fn get_session_messages(id: String) -> Result<Vec<ChatMessage>>
- created_at format: stringified i64 seconds (existing format: `format!("{}", session.started_at as i64)`)

From crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs (current shape):
- pub enum ChatBubbleKind { User, Assistant, Error }   // becomes 4 variants after Task 1
- pub struct ChatBubble { pub id: u64, pub kind: ChatBubbleKind, pub text: String, pub tool_rows: Vec<...> }
- ChatBubble::user(id: u64, text: String) -> ChatBubble
- ChatBubble::assistant(id: u64, text: String) -> ChatBubble
- Two `match b.kind` sites in chat.rs: line ~212 (class selector) + line ~221 (avatar selector). Both become non-exhaustive once Divider is added.

From crates/iron_hermes_ui/src/components/hermes_app/mod.rs (signals provided via context, consumed by ScreenChat):
- Signal<Vec<ChatBubble>> declared at ~line 123 — already in scope inside ScreenChat as `bubbles`
- Signal<Option<u64>> `streaming_id` for STREAMING indicator
- Signal<u64> `next_id` for monotonic bubble id allocation
- SessionIdContext newtype (B-03) at ~line 326 — call-syntax `session_id_ctx()` returns owned String (subscribes to changes); via `use_context::<SessionIdContext>()` in ScreenChat

Established async-in-use_effect idiom (mod.rs lines 137-150):
- use_effect closure body is SYNCHRONOUS. Async work goes inside spawn(async move { ... }).
- Signal reads MUST be call-syntax `signal()` or `*signal.read()` (Copy types) — never hold `GenerationalRef` across spawn or .await.

Available crates (already in Cargo.toml, no additions):
- js-sys = "0.3" (unconditional — js_sys::Date::now() returns f64 ms since epoch)
- gloo_timers (already used in agents.rs — not needed here but available)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add ChatBubbleKind::Divider variant and update both match sites</name>
  <files>crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs (entire file — focus on: ChatBubbleKind enum definition at lines ~27-31; ChatBubble struct definition at lines ~34-50; ChatBubble::user / ChatBubble::assistant constructors at lines ~55-84; the two match-on-kind sites at ~line 212 class selector and ~line 221 avatar selector; the bubble RSX loop block at lines ~237-278)
    - .planning/phases/26.7.2-sessions-load-session-data/26.7.2-PATTERNS.md section "Add Divider variant to ChatBubbleKind" — read both BEFORE and AFTER blocks for class selector AND avatar selector
    - .planning/phases/26.7.2-sessions-load-session-data/26.7.2-RESEARCH.md Pattern 3 ("Divider bubble — ChatBubble variant vs RSX element") and Assumption A4 (exhaustiveness risk)
  </read_first>
  <action>
    Implement D-02 by adding the new Divider variant and updating ALL match sites so the file still compiles.

    Step 1 — modify the `pub enum ChatBubbleKind` declaration in chat.rs:
    Add a fourth variant after `Error`:
      Divider,
    Add an inline comment: "// Phase 26.7.2 D-02: history/live boundary marker — renders as a section-label rule, no avatar, no bubble body". Keep the existing `#[derive(Clone, PartialEq, Debug)]` line; do NOT add Copy, Hash, or other derives.

    Step 2 — locate the FIRST match site: the class-selector match inside the bubble RSX loop (PATTERNS.md identifies it at lines ~212-216). It currently has three arms:
      ChatBubbleKind::User      => "chat-msg user",
      ChatBubbleKind::Assistant => "chat-msg assistant",
      ChatBubbleKind::Error     => "chat-msg error",
    Add a fourth arm:
      ChatBubbleKind::Divider   => "chat-divider",

    Step 3 — locate the SECOND match site: the avatar-selector match (PATTERNS.md identifies it at lines ~221-233). It currently has three arms returning rsx! blocks. Add a fourth arm that renders NOTHING (an empty rsx! macro):
      ChatBubbleKind::Divider   => rsx! {},

    Step 4 — wrap the existing `div { class: "chat-bubble-wrap", ... }` block (PATTERNS.md lines ~237-278) in a conditional so dividers render only the label rule, not the bubble body. Two acceptable shapes:

    Shape A (preferred — minimal diff):
      if b.kind != ChatBubbleKind::Divider {
          rsx! { div { class: "chat-bubble-wrap", /* existing body unchanged */ } }
      } else {
          rsx! { div { class: "chat-divider-label", span { class: "section-label", "─── Earlier ───" } } }
      }

    Shape B: match on b.kind for the body too, with the Divider arm returning the section-label span and other arms returning the existing chat-bubble-wrap.

    Use Shape A unless the surrounding RSX context makes Shape B simpler.

    Critical: The outer div's `class: match b.kind { ... }` line MUST still return `"chat-divider"` for the Divider arm so the row gets a different CSS class. This is independent of the inner body change.

    Step 5 — grep the entire crate for any OTHER `match b.kind` or `match .*ChatBubbleKind` sites. RESEARCH Open Question 1 says only the two in chat.rs exist, but exhaustively verify:
      grep -nE 'match\s+.*ChatBubbleKind' crates/iron_hermes_ui/src/
      grep -nE 'ChatBubbleKind::' crates/iron_hermes_ui/src/
    If any additional non-exhaustive match site is found outside chat.rs (mod.rs, sessions.rs, agents.rs), add a `ChatBubbleKind::Divider => /* appropriate no-op */` arm. Do NOT use a wildcard `_ =>` arm — exhaustive matches are an invariant that protects future variants.

    Do NOT add CSS for `.chat-divider` or `.chat-divider-label` in this task — the visual styling is owned by the existing design system (CONTEXT D-02 says match the existing `.section-label` / `.pill` conventions). The `.section-label` class is already styled. If the divider needs additional CSS, it is a follow-up; this task ships the structural marker.

    Verify cargo check passes before completing.
  </action>
  <verify>
    <automated>cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features web 2>&1 | tail -40 ; grep -nE 'ChatBubbleKind::Divider' crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs ; grep -cE 'ChatBubbleKind::Divider' crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs ; grep -rnE 'match\s+.*ChatBubbleKind' crates/iron_hermes_ui/src/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Divider,' crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs` returns at least 1 (the enum variant declaration)
    - `grep -c 'ChatBubbleKind::Divider' crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs` returns at least 3 (one each in: class-selector match, avatar-selector match, body-wrap conditional or body-match Divider arm)
    - `grep -c '"chat-divider"' crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs` returns at least 1 (the class-selector arm)
    - `grep -c '"section-label"' crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs` returns at least 1 (the divider body)
    - `grep -rnE 'match\s+.*ChatBubbleKind' crates/iron_hermes_ui/src/ | grep -v test` returns only matches that contain a `Divider =>` arm — i.e. no non-exhaustive match remains
    - `cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features web` exits 0 (this is the wasm target build — the one that actually exercises the RSX rendering)
    - The chat.rs file contains zero wildcard `_ =>` arms newly introduced in match-on-ChatBubbleKind blocks (preserves exhaustive-match invariant)
  </acceptance_criteria>
  <done>
    ChatBubbleKind has four variants. Both match sites in chat.rs (class-selector at ~line 212 and avatar-selector at ~line 221) are exhaustive with explicit `Divider` arms. The bubble RSX loop renders dividers as a label-only row using `.chat-divider` outer class and `.section-label` inner text. cargo check --features web exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add use_effect on session_id in ScreenChat to clear+fetch+populate history</name>
  <files>crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs (entire ScreenChat component lines ~119-319 — focus on: signal/context bindings at the top of the fn body; existing use_effect for auto-scroll near line ~155 to confirm placement; how `bubbles`, `streaming_id`, `next_id`, `session_id` are bound from context)
    - crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs (lines ~316-325 — the canonical use_effect subscribe + restart pattern; lines ~34-58 — UAT-2 comment explaining why use_resource is preferred over use_server_future, relevant for understanding why we use spawn instead of either)
    - crates/iron_hermes_ui/src/components/hermes_app/mod.rs (lines ~123, 137-150, 326 — Signal<Vec<ChatBubble>> declaration, the canonical spawn-inside-use_effect pattern from create_session bootstrap, SessionIdContext provider)
    - crates/iron_hermes_ui/CLAUDE.md (signal-borrow rules — GenerationalRef must not cross await; .read() borrows must drop before spawn)
    - .planning/phases/26.7.2-sessions-load-session-data/26.7.2-RESEARCH.md sections Q2 (use_resource vs use_server_future — answer: use neither, just spawn), Q3 (session_id race — "pending" guard required), Q8 (signal borrow safety in this effect), Pitfall 2 (re-render loop avoidance), Pitfall 3 (divider only when history non-empty), Pitfall 6 (skip "pending"), Open Question 3 (streaming_id.set(None) inside the sync part of the effect)
    - .planning/phases/26.7.2-sessions-load-session-data/26.7.2-PATTERNS.md section "use_effect on session_id — D-06 wiring" — includes the exact reference implementation
  </read_first>
  <action>
    Add a new `use_effect` block inside `ScreenChat` that implements D-06/D-07/D-08 and partially D-01/D-02.

    PLACEMENT: insert this use_effect immediately AFTER the existing auto-scroll use_effect (PATTERNS.md identifies its location at ~line 155). Do not place it BEFORE the auto-scroll effect — hook registration order matters in Dioxus 0.7, and any future code inserting hooks above this point must maintain consistent ordering.

    SIGNAL BINDINGS: confirm these signals are already in scope in ScreenChat (from context or props). They should be:
      - `session_id` (or `session_id_ctx` — whichever name the existing chat.rs uses for the SessionIdContext-derived signal). Check the top of ScreenChat for the existing binding line and use the same identifier.
      - `bubbles: Signal<Vec<ChatBubble>>`
      - `streaming_id: Signal<Option<u64>>`
      - `next_id: Signal<u64>`
    If any of these are not bound at the top of ScreenChat already, add a binding using `use_context` or the existing context-consumption pattern from the file (do NOT introduce a new context type).

    EFFECT BODY (RESEARCH Q8 + Pattern 2 + PATTERNS.md reference impl):

    1. Synchronous block (no .await, no .read() that outlives ;):
       - Call session_id with call-syntax to subscribe and copy out an owned String:
           let sid = session_id.read().clone();
         (call-syntax `session_id()` is also acceptable if the binding is a Copy newtype; match the convention used by the existing auto-scroll effect or by agents.rs. If unsure, `.read().clone()` is safe.)
       - If `sid == "pending"`, return early (RESEARCH Pitfall 6 + Q3 — skip fetch during initial bootstrap before create_session has resolved).
       - Clear bubbles: `bubbles.write().clear();` — WriteLock drops at `;`. (RESEARCH Pitfall 2: this does NOT cause a re-render loop because the effect subscribes to `session_id`, not `bubbles`. Writing to a non-subscribed signal does not re-trigger the effect.)
       - Cancel STREAMING indicator: `streaming_id.set(None);` — RESEARCH Open Question 3 recommendation. This prevents a stale STREAMING badge in the header from previous session if user switched mid-stream.

    2. Spawn the async fetch (RESEARCH Pattern 2):
         spawn(async move {
             match crate::server::api::get_session_messages(sid).await {
                 Ok(msgs) if !msgs.is_empty() => {
                     // Map ChatMessage → ChatBubble. Read next_id as Copy u64 before any further write.
                     let mut id_val = *next_id.read();
                     let mut history: Vec<ChatBubble> = msgs
                         .into_iter()
                         .filter_map(|m| match m.role.as_str() {
                             "user"      => { let b = ChatBubble::user(id_val, m.content); id_val += 1; Some(b) }
                             "assistant" => { let b = ChatBubble::assistant(id_val, m.content); id_val += 1; Some(b) }
                             _           => None,
                         })
                         .collect();
                     // Pitfall 3: divider only when history is non-empty (guarded by the outer `if !msgs.is_empty()` arm).
                     history.push(ChatBubble {
                         id: id_val,
                         kind: ChatBubbleKind::Divider,
                         text: String::new(),
                         tool_rows: vec![],
                     });
                     id_val += 1;
                     bubbles.write().extend(history);
                     next_id.set(id_val);
                 }
                 _ => { /* D-07: empty or error → empty stream, no indicator, no divider */ }
             }
         });

    CRITICAL SIGNAL-BORROW RULES (CLAUDE.md + RESEARCH Pitfall 1):
    - Inside spawn, `*next_id.read()` evaluates to a u64 Copy — the GenerationalRef drops at the `;`. Do NOT bind it to a let-binding that lives across `.await` or across additional reads.
    - `bubbles.write()` and `next_id.set(...)` each acquire and release the lock atomically — no WriteLock spans an .await. Do NOT hoist `bubbles.write()` to a let-binding outside the immediate scope.
    - The closure passed to use_effect is `move ||` (synchronous). It captures the signal handles by move (signals are arena-allocated; cloning a signal handle is cheap and the borrow checker enforces correctness).

    CRITICAL FIELD ASSUMPTIONS:
    - `ChatBubble { id, kind, text, tool_rows }` — if the actual struct has additional fields beyond text and tool_rows (e.g. timestamps, streaming flags), read the struct definition in chat.rs and provide default values for those fields too (use `..Default::default()` only if the struct derives Default; otherwise specify each field explicitly). The PATTERNS.md reference uses exactly these four fields — confirm against the actual definition before writing.
    - `ChatBubble::user(id, content)` and `ChatBubble::assistant(id, content)` — these are the existing constructors at chat.rs lines ~55-84. Use them rather than building the User/Assistant variants by hand to ensure all default fields are populated correctly.

    NO LOADING SPINNER (D-07): do NOT add a "loading" bubble, skeleton, or any visual indicator between the clear and the populate. Empty stream is the correct intermediate state.

    NO use_resource / use_server_future: RESEARCH Q2 explicitly rejects both for this fetch — it is a one-shot fire-on-effect, not a polling resource. Use `spawn` directly.

    NO #[cfg] gating on the use_effect: the effect is identical on web and server targets (server-side rendering will simply skip executing it; this is not a wasm-only feature).

    Do NOT modify `bubbles` initial state (it stays as the existing `use_signal(Vec::new)` or whatever the current declaration is). Do NOT modify `streaming_id` initial state. Do NOT add a new signal.

    Run cargo check --features web to confirm.
  </action>
  <verify>
    <automated>cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features web 2>&1 | tail -40 ; awk '/fn ScreenChat/,/^}/' crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs | grep -nE '(use_effect|get_session_messages|streaming_id\.set\(None\)|bubbles\.write\(\)\.clear|sid == "pending")'</automated>
  </verify>
  <acceptance_criteria>
    - ScreenChat fn body contains the literal substring `get_session_messages` (the server fn from Plan 01 is called)
    - ScreenChat fn body contains the literal substring `if sid == "pending"` (RESEARCH Pitfall 6 / Q3 guard)
    - ScreenChat fn body contains the literal substring `bubbles.write().clear()` (D-08 clear-on-switch)
    - ScreenChat fn body contains the literal substring `streaming_id.set(None)` (RESEARCH Open Question 3 recommendation)
    - ScreenChat fn body contains the literal substring `ChatBubbleKind::Divider` (the divider bubble is created after non-empty history)
    - ScreenChat fn body contains the literal substring `if !msgs.is_empty()` or equivalent guard arm `Ok(msgs) if !msgs.is_empty()` (Pitfall 3 guard)
    - ScreenChat fn body contains `spawn(async move` at least once (the async fetch is dispatched from a sync effect body)
    - `cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features web` exits 0
    - `cargo clippy --manifest-path crates/iron_hermes_ui/Cargo.toml --features web -- -D warnings 2>&1 | grep -E '(temporary_value|borrow_across)' | wc -l` returns 0 (no signal-borrow violations)
    - No `use_resource` or `use_server_future` call was introduced in the new code (RESEARCH Q2 lock): `git diff crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs | grep '^+' | grep -E '(use_resource|use_server_future)' | wc -l` returns 0
  </acceptance_criteria>
  <done>
    ScreenChat's bubble stream populates from `get_session_messages` whenever session_id changes (post-bootstrap), clears prior bubbles+streaming indicator on switch, and appends a divider bubble at the boundary when history is non-empty. The use_effect is signal-borrow safe and does not loop. cargo check + clippy pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add format_relative helper and wire last_message + relative timestamp into SessionRow</name>
  <files>crates/iron_hermes_ui/src/components/hermes_app/screens/sessions.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/components/hermes_app/screens/sessions.rs (entire file — focus on: imports at lines ~1-22; ScreenSessions component; SessionRow component at lines ~128-186 specifically the `sub` string construction at lines ~147-157 and the row-main RSX at lines ~165-168)
    - crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs (lines ~83-98 — js_sys::Date pattern for wasm time display; confirm exact import form for js_sys)
    - crates/iron_hermes_ui/src/state.rs (lines ~292-306 — alternate js_sys::Date pattern reference)
    - .planning/phases/26.7.2-sessions-load-session-data/26.7.2-RESEARCH.md sections Q5 (js_sys::Date availability and pattern), Q7 (started_at units — fractional seconds, multiply by 1000.0 for js Date arithmetic), Pitfall 4 (units error mode)
    - .planning/phases/26.7.2-sessions-load-session-data/26.7.2-PATTERNS.md section "Add last_message to SessionRow sub-text display (D-04)" + "Relative timestamp formatting (D-05)"
  </read_first>
  <action>
    Implement D-04 (display side — Plan 01 already produced the data) and D-05 (relative time formatting) in sessions.rs.

    Step 1 — add a private helper fn `format_relative` near the top of sessions.rs (after the existing `use` block, BEFORE the first component definition). Use the EXACT cfg-split shape from PATTERNS.md "Relative timestamp formatting":

      #[cfg(target_arch = "wasm32")]
      fn format_relative(unix_secs_str: &str) -> String {
          let unix_secs: f64 = unix_secs_str.parse().unwrap_or(0.0);
          let now_ms = js_sys::Date::now();
          let then_ms = unix_secs * 1000.0;
          let diff_secs = ((now_ms - then_ms) / 1000.0) as i64;
          match diff_secs {
              s if s < 5       => "just now".to_string(),
              s if s < 60      => format!("{}s ago", s),
              s if s < 3_600   => format!("{}m ago", s / 60),
              s if s < 86_400  => format!("{}h ago", s / 3_600),
              s if s < 604_800 => format!("{}d ago", s / 86_400),
              _                => format!("{}w ago", diff_secs / 604_800),
          }
      }

      #[cfg(not(target_arch = "wasm32"))]
      fn format_relative(_: &str) -> String {
          "—".to_string()
      }

    Critical RESEARCH constraints (do NOT deviate):
    - js_sys::Date::now() returns milliseconds — multiply unix_secs by 1000.0 (Pitfall 4).
    - started_at is fractional seconds (RESEARCH Q7) — already stringified as integer-seconds by Plan 01's existing list_sessions mapping. Parse as f64 to tolerate either form, falling back to 0.0 on parse failure.
    - The wasm gate is `#[cfg(target_arch = "wasm32")]` exactly (not `feature = "web"`). The non-wasm stub returns `"—"` so server-rendered placeholders show a dash.
    - The fn is module-private (no `pub`).

    Imports: js-sys is already an unconditional Cargo.toml dep (RESEARCH Standard Stack confirms). Use the fully-qualified path `js_sys::Date::now()` to avoid adding a top-of-file `use js_sys;` import — keeps the diff minimal and avoids needing a matching `#[cfg]` on the import.

    Step 2 — modify SessionRow's `sub` string construction (PATTERNS.md identifies it at lines ~147-157). Currently:
      let sub = format!("{msgs} msg{plural} · session {id_tail}", ...);
    Replace with a preference-aware construction (D-04 + Q6 Claude's Discretion bullet — strip role prefix, just show content):
      let preview: Option<String> = session.last_message.clone();
      let sub = match &preview {
          Some(p) if !p.is_empty() => p.clone(),
          _ => format!("{msgs} msg{plural} · session {id_tail}",
              msgs = session.message_count,
              plural = if session.message_count == 1 { "" } else { "s" },
          ),
      };
    Rationale: when the server provides a real preview, show it (more informative); when None or empty (zero-message session or all tool/system messages), fall back to the existing identifier-based sub-text so the row never looks blank.

    Step 3 — modify the SessionRow row-main RSX (PATTERNS.md identifies it at lines ~165-168). Currently:
      div { class: "row-main",
          span { class: "row-title", "{title}" }
          span { class: "row-sub", "{sub}" }
      }
    Add a second sub-text span carrying the relative timestamp. Compute the timestamp string OUTSIDE the rsx! macro (signal-borrow rule — but this is a plain &str → String, no signal involved; still, keeping it as a let-binding above the rsx! is the cleaner pattern matching existing code in this file):
      let ts = format_relative(&session.created_at);
    Then in the rsx!:
      div { class: "row-main",
          span { class: "row-title", "{title}" }
          span { class: "row-sub", "{sub}" }
          span { class: "row-sub", style: "opacity:0.5", "{ts}" }
      }
    The second `span.row-sub` with inline `opacity:0.5` styling gives a visual hierarchy (preview dominant, timestamp secondary). PATTERNS.md offers this exact pattern; use it unless an existing `.row-meta` or `.row-time` class already exists in the design system (grep sessions.rs and the CSS file for these — if present, prefer the class over inline style). If neither exists, use the inline `opacity:0.5` style as shown.

    Step 4 — do NOT remove the existing `{sub}` span or the `{id_tail}` fallback. The fallback path is the right behavior when last_message is None (zero-message session, foreign-format directory, etc. — RESEARCH State of the Art notes Phase 26.2.1 R3 already filters zero-message sessions, so this fallback is rare but non-zero).

    Step 5 — do NOT change the SessionRow component signature. Do NOT change the click handler (`on_select`). Do NOT touch the `+ NEW SESSION` button or `⊞ FILTER` button (CONTEXT Deferred Ideas).

    Run cargo check on both server and web targets:
      cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features server
      cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features web
    Both must exit 0. The server build exercises the `#[cfg(not(target_arch = "wasm32"))]` arm; the web build exercises the wasm arm.
  </action>
  <verify>
    <automated>cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features server 2>&1 | tail -20 ; cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features web 2>&1 | tail -20 ; grep -cE 'fn format_relative' crates/iron_hermes_ui/src/components/hermes_app/screens/sessions.rs ; grep -cE 'session\.last_message' crates/iron_hermes_ui/src/components/hermes_app/screens/sessions.rs ; grep -cE 'js_sys::Date::now\(\)' crates/iron_hermes_ui/src/components/hermes_app/screens/sessions.rs ; grep -cE '\* 1000\.0' crates/iron_hermes_ui/src/components/hermes_app/screens/sessions.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fn format_relative' crates/iron_hermes_ui/src/components/hermes_app/screens/sessions.rs` returns exactly 2 (two cfg-gated definitions — wasm32 + not-wasm32)
    - The file contains both `#[cfg(target_arch = "wasm32")]` and `#[cfg(not(target_arch = "wasm32"))]` attributes immediately above the two format_relative definitions
    - `grep -c 'js_sys::Date::now()' crates/iron_hermes_ui/src/components/hermes_app/screens/sessions.rs` returns exactly 1 (inside the wasm arm only)
    - `grep -c '\* 1000\.0' crates/iron_hermes_ui/src/components/hermes_app/screens/sessions.rs` returns at least 1 (RESEARCH Pitfall 4 — seconds-to-ms conversion present)
    - `grep -c 'session\.last_message' crates/iron_hermes_ui/src/components/hermes_app/screens/sessions.rs` returns at least 1 (the SessionRow sub-text now reads the new field from Plan 01)
    - `grep -c 'format_relative(&session\.created_at)' crates/iron_hermes_ui/src/components/hermes_app/screens/sessions.rs` returns exactly 1 (helper wired into RSX)
    - SessionRow RSX contains two `class: "row-sub"` spans (preview + timestamp) — `grep -c 'class: "row-sub"' crates/iron_hermes_ui/src/components/hermes_app/screens/sessions.rs` returns at least 2
    - `cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features server` exits 0 (non-wasm arm compiles)
    - `cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features web` exits 0 (wasm arm compiles)
  </acceptance_criteria>
  <done>
    sessions.rs has a wasm-gated format_relative helper and SessionRow renders both the last_message preview (with id-tail fallback) and a relative timestamp ("2 days ago" style). Both target builds compile. The raw Unix-seconds string is no longer the user-visible timestamp.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (session row click) → ScreenChat use_effect | session_id flows from user action through SessionIdContext signal into the new use_effect; trusted (same WASM origin) |
| ScreenChat use_effect → server fn `get_session_messages` | sid String crosses the WASM → server fn boundary; security tier is Plan 01's responsibility (T-26.7.2-01) |
| StoredMessage content → ChatBubble text → DOM | server-stored content (possibly historical user-typed input) is rendered as `{m.content}` in RSX; Dioxus 0.7 RSX text interpolation HTML-escapes by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.7.2-06 | Tampering | use_effect signal-borrow violations causing UB or panic | mitigate | RESEARCH Pitfall 1 + Q8 + CLAUDE.md signal-borrow rules: call-syntax for owned String copy, no .read() GenerationalRef across spawn, atomic .write()/.set() inside async block. Acceptance criteria include clippy gate `-D warnings` filtering on temporary_value/borrow_across heuristics. |
| T-26.7.2-07 | Denial of Service | use_effect infinite re-render loop (write triggers re-read triggers re-effect) | mitigate | RESEARCH Pitfall 2 — the effect subscribes to `session_id`, NOT to `bubbles`. Writing bubbles inside the effect does not retrigger because Dioxus effects only re-fire on signals read via call-syntax inside the closure body. The session_id call-syntax `.read().clone()` does subscribe — but session_id is only set externally (by SessionRow on_select), not by this effect, so no self-loop. UAT MUST include rapid session switching to confirm no CPU spike. |
| T-26.7.2-08 | Tampering | XSS via stored message content rendered into ChatBubble | accept | Dioxus 0.7 RSX text interpolation (`"{m.content}"`) HTML-escapes by default (same as React JSX text children). RESEARCH Security Domain notes the historical message store contents are user-authored from a local session — not attacker-controlled in the local-only deployment context. Tool/system messages are filtered server-side (Plan 01 Task 2), so historical XSS surface = same as live chat XSS surface (preexisting risk, not introduced by this plan). |
| T-26.7.2-09 | Information Disclosure | Race: D-06 effect fires on `"pending"` and issues a fetch that errors or returns [] | mitigate | RESEARCH Pitfall 6 / Q3: explicit `if sid == "pending" { return; }` guard as the first statement after the owned-String copy. Acceptance criteria includes grep for the exact literal. |
| T-26.7.2-10 | Denial of Service | format_relative panic on malformed created_at (non-numeric string) | mitigate | The wasm arm uses `unix_secs_str.parse().unwrap_or(0.0)` — parse failure yields 0.0 (will render as "tens of years ago" but will not panic). Acceptable degraded behavior; no panic surface. |
| T-26.7.2-11 | Spoofing | Stale STREAMING indicator from previous session leaks into new session header | mitigate | `streaming_id.set(None)` called in the synchronous part of the use_effect BEFORE spawn (RESEARCH Open Question 3). Acceptance criteria includes the literal grep. |
| T-26.7.2-12 | Information Disclosure | last_message preview rendered server-side or via SSR-prerender before WASM hydration leaks across origins | accept | Local-only fullstack — no cross-origin surface. Same risk tier as the rest of the chat application. |
| T-26.7.2-SC | Tampering | npm/pip/cargo installs | n/a | Plan 02 adds zero new packages — js-sys, dioxus, gloo_timers all preexisting unconditional deps. RESEARCH "Package Legitimacy Audit: No new packages introduced." Package-legitimacy gate inert. |
</threat_model>

<verification>
After all three tasks complete, run the full phase verification:

1. Both target builds clean:
   `cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features server` exits 0
   `cargo check --manifest-path crates/iron_hermes_ui/Cargo.toml --features web` exits 0

2. Clippy gate (signal-borrow violations would fire here):
   `cargo clippy --manifest-path crates/iron_hermes_ui/Cargo.toml --features web -- -D warnings 2>&1 | tail -30`
   Should exit 0 — any borrow-across-await error is a planning bug that must be fixed before completion.

3. Match exhaustiveness invariant (Divider variant covered everywhere it must be):
   `grep -rn 'match\s\+[a-z_]*\.kind' crates/iron_hermes_ui/src/components/ | xargs -I{} echo {}` — every site must have a Divider arm or compilation would already have failed; this is a manual cross-check.

4. UAT (human-verifiable, run after merging):
   - Start the iron_hermes_ui dev server.
   - Open the Sessions screen — confirm at least one row shows a relative timestamp (e.g. "2h ago") and a last_message preview text (when content exists).
   - Click a session row that has prior messages. Confirm: Chat screen loads, the historical messages render as user/assistant bubbles in chronological order, followed by a divider row labeled "─── Earlier ───".
   - Send a new message. Confirm: it appears below the divider; the session continues server-side.
   - Type `/clear` (if the slash command is wired). Confirm: all bubbles including history wipe (D-03).
   - Switch from session A → session B → session A rapidly. Confirm: no stale bubbles, no STREAMING indicator stuck on, no console errors.
   - Open a brand-new session (via `+ NEW SESSION` if wired, or via the bootstrap path). Confirm: empty stream, NO divider visible, no loading indicator (D-07).
</verification>

<success_criteria>
Plan 02 succeeds when:
- ChatBubbleKind has 4 variants and all match-on-kind sites compile without wildcard arms.
- ScreenChat's new use_effect: subscribes to session_id, skips "pending", clears bubbles + streaming_id, spawns the history fetch, populates bubbles + appends divider (only when non-empty).
- sessions.rs has format_relative (wasm + non-wasm arms) and SessionRow renders last_message preview + relative timestamp.
- cargo check --features server AND --features web both exit 0.
- cargo clippy with `-D warnings` exits 0 on the new code.
- No new dependencies introduced.
- The chat-input-pill remains active after history load (D-09 — no code change required; confirm via UAT).
</success_criteria>

<output>
On completion, write `.planning/phases/26.7.2-sessions-load-session-data/26.7.2-02-SUMMARY.md` capturing: line ranges of the new use_effect block, exact placement of format_relative, any deviation from PATTERNS.md (e.g. if existing ChatBubble struct had extra fields requiring additional defaults), and a UAT runlog (which sessions were tested, what the divider looked like, any visual polish suggestions for a follow-up phase).
</output>
