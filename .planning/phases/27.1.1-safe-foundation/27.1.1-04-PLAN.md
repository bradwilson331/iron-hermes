---
phase: 27.1.1-safe-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - "27.1.1-02"
files_modified:
  - crates/ironhermes-tools/src/lib.rs
  - crates/ironhermes-tools/src/registry.rs
autonomous: true
requirements:
  - HXP-TOOL-01
user_setup: []

must_haves:
  truths:
    - "`crates/ironhermes-tools/src/lib.rs` declares `pub mod hexapod_tcp;` in alphabetical position (between `file_tools` and `memory_manager_handle`)"
    - "`ToolRegistry::register_defaults()` registers `HexapodTcpTool` via `self.register(Box::new(HexapodTcpTool));` so the tool is available in every IronHermes invocation path (CLI run_single, CLI run_chat, ratatui TUI) without needing per-call-site wiring"
    - "When HEXAPOD_IP is unset, the default `is_available()` (registry.rs:34-43, which walks prerequisites()) returns false and the tool is hidden from the LLM schema — proving HXP-TOOL-01 'tool registered' in a way that gracefully degrades on machines without a hexapod (parallels web_search's behavior when FIRECRAWL_API_KEY is missing)"
    - "When HEXAPOD_IP is set, calling `ToolRegistry::list_tools()` includes `hexapod_tcp` in the returned slice"
    - "All existing `cargo test -p ironhermes-tools` cases continue to pass — no regressions from the registration"
  artifacts:
    - path: "crates/ironhermes-tools/src/lib.rs"
      provides: "Module declaration for the new tool file"
      contains: "pub mod hexapod_tcp"
    - path: "crates/ironhermes-tools/src/registry.rs"
      provides: "Tool registration in register_defaults"
      contains: "HexapodTcpTool"
  key_links:
    - from: "register_defaults (registry.rs:477)"
      to: "HexapodTcpTool struct (hexapod_tcp.rs, Plan 02)"
      via: "self.register(Box::new(HexapodTcpTool));"
      pattern: "Box::new\\(HexapodTcpTool\\)"
    - from: "lib.rs pub mod declaration"
      to: "hexapod_tcp.rs file body"
      via: "module system — makes the file's items addressable as `crate::hexapod_tcp::HexapodTcpTool` inside the ironhermes-tools crate"
      pattern: "pub mod hexapod_tcp"
---

<objective>
Register the `HexapodTcpTool` (created in Plan 02) in the IronHermes tool registry so it is discoverable by every IronHermes invocation path. Two micro-edits: (1) add `pub mod hexapod_tcp;` to `lib.rs`, (2) add a single `self.register(Box::new(HexapodTcpTool));` line to `ToolRegistry::register_defaults()` in `registry.rs`.

Purpose: Until this plan lands, the `HexapodTcpTool` exists in the codebase but is invisible to the LLM (no schema is exposed). This plan closes HXP-TOOL-01 ("`hexapod_tcp` tool registered in IronHermes ToolRegistry") and is the smallest viable change that turns the Plan 02 deliverable into a tool the agent can actually call.

Output: Two additive edits across two files, totaling ~3 lines of executable code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md
@.planning/phases/27.1.1-safe-foundation/27.1.1-RESEARCH.md
@.planning/phases/27.1.1-safe-foundation/27.1.1-PATTERNS.md
@.planning/phases/27.1.1-safe-foundation/27.1.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `pub mod hexapod_tcp;` to lib.rs in alphabetical position</name>
  <files>crates/ironhermes-tools/src/lib.rs</files>
  <read_first>
    crates/ironhermes-tools/src/lib.rs (lines 1-34 — full module declaration + re-export pattern),
    .planning/phases/27.1.1-safe-foundation/27.1.1-PATTERNS.md (lines 280-345 — lib.rs registration pattern, with the exact alphabetical placement guidance)
  </read_first>
  <action>
Locate the `pub mod` declarations in `crates/ironhermes-tools/src/lib.rs` (lines 1-27 per the live file). The existing ordering is alphabetical:

```
pub mod approval;
pub mod browser_back;
... browser_* ...
pub mod cronjob_tool;
pub mod delegate_task;
pub mod execute_code;
pub mod file_tools;
pub mod memory_manager_handle;
pub mod memory_tool;
pub mod registry;
...
```

Insert `pub mod hexapod_tcp;` in the alphabetical position between `file_tools` and `memory_manager_handle`. The exact line to add (no trailing comment; matches the existing terse style of older entries):

```
pub mod hexapod_tcp;
```

Do NOT add a `pub use` re-export. Per PATTERNS.md §`pub use` Re-export Pattern (lines 432-445), individual tool structs (`TerminalTool`, `CronjobTool`, `ReadFileTool`) are NOT re-exported from lib.rs — they are imported at registration call sites via `use crate::<module>::<Struct>` inside the registering function body. `HexapodTcpTool` follows the same pattern: no `pub use` here.

After the splice:
- `cargo check -p ironhermes-tools` must succeed.
- `grep -c 'pub mod hexapod_tcp' crates/ironhermes-tools/src/lib.rs` must return exactly 1.
- The pre-existing `pub use registry::{...}` block and `pub use web_extract::WebExtractTool;` line must remain untouched.

Commit (combined with Task 2): `feat(27.1.1-04): register HexapodTcpTool in lib.rs and ToolRegistry::register_defaults (D-03)`.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo check -p ironhermes-tools 2>&1 | tail -5 && grep -c 'pub mod hexapod_tcp' crates/ironhermes-tools/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub mod hexapod_tcp' crates/ironhermes-tools/src/lib.rs` returns exactly 1
    - The line `pub mod hexapod_tcp;` appears alphabetically between `pub mod file_tools;` and `pub mod memory_manager_handle;` (verify by inspection or `awk '/pub mod (file_tools|hexapod_tcp|memory_manager_handle)/ {print NR": "$0}'` shows them in that order)
    - `grep -c 'pub use.*HexapodTcpTool' crates/ironhermes-tools/src/lib.rs` returns 0 (no pub-use re-export, per crate convention)
    - `cargo check -p ironhermes-tools` exits 0
  </acceptance_criteria>
  <done>
    `lib.rs` exposes the new module; `cargo check -p ironhermes-tools` is green; no re-export added.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register HexapodTcpTool inside ToolRegistry::register_defaults()</name>
  <files>crates/ironhermes-tools/src/registry.rs</files>
  <read_first>
    crates/ironhermes-tools/src/registry.rs (lines 471-520 — list_tools(), call_session_end_hooks (from Plan 01), and register_defaults body),
    crates/ironhermes-tools/src/registry.rs (lines 477-507 — register_defaults body — note the `use crate::...` block at the top and the existing `self.register(Box::new(...))` calls),
    .planning/phases/27.1.1-safe-foundation/27.1.1-RESEARCH.md (lines 318-365 — Registered Tools Inventory + registration decision: per RESEARCH §Tool Inventory, add to register_defaults() unconditionally because is_available() returns false when HEXAPOD_IP is unset, hiding the tool from the LLM schema; matches the web_search/FIRECRAWL_API_KEY pattern)
  </read_first>
  <action>
Locate `pub fn register_defaults(&mut self)` in `crates/ironhermes-tools/src/registry.rs` (begins around L477). The existing body looks like:

```
pub fn register_defaults(&mut self) {
    use crate::file_tools::{PatchFileTool, ReadFileTool, SearchFilesTool, WriteFileTool};
    use crate::terminal::TerminalTool;
    use crate::web_read::WebReadTool;
    use crate::web_search::WebSearchTool;

    self.register(Box::new(TerminalTool::new()));
    self.register(Box::new(ReadFileTool));
    // ...
}
```

Two splices inside this function:

(a) Add an import line. Insert `use crate::hexapod_tcp::HexapodTcpTool;` in the `use crate::...` block at the top of register_defaults, in alphabetical position (between `file_tools` and `terminal` — i.e. after the `use crate::file_tools::{...}` line and before `use crate::terminal::TerminalTool;`).

(b) Add a registration call. Insert `self.register(Box::new(HexapodTcpTool));` at the END of the `register_defaults` body (after the existing `self.register(...)` calls, but BEFORE the closing `}` of the function). HexapodTcpTool is a unit struct (Plan 02 made it `pub struct HexapodTcpTool;` with no `new()`), so the call uses `Box::new(HexapodTcpTool)` — no `::new()` invocation, no constructor args.

The reason for adding to `register_defaults()` rather than a separate `register_hexapod_tool()` helper (which PATTERNS.md initially suggested, lines 335-346): RESEARCH §Registered Tools Inventory (lines 351-363) revised this — because `is_available()` returns false when HEXAPOD_IP is unset, the tool gracefully hides itself from machines without a robot, exactly like `web_search` hides when FIRECRAWL_API_KEY is absent. This is the lowest-friction approach. Documented as Assumption A2 in RESEARCH §Assumptions Log with "Low" risk.

Add a `// HXP-TOOL-01 (Phase 27.1.1): hexapod TCP tool — is_available() hides this when HEXAPOD_IP is unset.` comment on the line above the registration call so future readers understand the conditional-availability semantics.

After both splices:
- `cargo check -p ironhermes-tools` must succeed.
- `cargo test -p ironhermes-tools` must succeed (the new tool participating in any test that builds a default registry adds one more tool to its list_tools() output — confirm no test asserts on a hard-coded count).
- If a registry-default test fails on a hard-coded tool count, update the assertion to match the new count (the tool is gated by `is_available()` so behavior in production paths is unchanged for users without HEXAPOD_IP).
- `grep -c 'Box::new(HexapodTcpTool)' crates/ironhermes-tools/src/registry.rs` returns exactly 1.
- `grep -c 'use crate::hexapod_tcp::HexapodTcpTool' crates/ironhermes-tools/src/registry.rs` returns exactly 1.

Combine with Task 1 in a single commit: `feat(27.1.1-04): register HexapodTcpTool in lib.rs and ToolRegistry::register_defaults (D-03, HXP-TOOL-01)`.

If you prefer separate commits, Task 1 commits as `feat(27.1.1-04): expose hexapod_tcp module in ironhermes-tools/lib.rs` and Task 2 as `feat(27.1.1-04): register HexapodTcpTool in ToolRegistry::register_defaults (HXP-TOOL-01)`. Either is acceptable.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo test -p ironhermes-tools 2>&1 | tail -10 && grep -c 'Box::new(HexapodTcpTool)' crates/ironhermes-tools/src/registry.rs && cargo check --workspace 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'use crate::hexapod_tcp::HexapodTcpTool' crates/ironhermes-tools/src/registry.rs` returns exactly 1
    - `grep -c 'Box::new(HexapodTcpTool)' crates/ironhermes-tools/src/registry.rs` returns exactly 1
    - Both the import and the register call appear INSIDE the `pub fn register_defaults` function body (verify by inspection: locate `pub fn register_defaults` and confirm both grep hits fall within its `{ ... }` block)
    - `cargo check -p ironhermes-tools` exits 0
    - `cargo test -p ironhermes-tools` exits 0 (if a count-assertion test breaks, update its expected count; do not skip or delete tests)
    - `cargo check --workspace` exits 0
    - With `HEXAPOD_IP` SET: `is_available()` returns true (verify in a unit test or via inspection of the default trait method walking prerequisites())
    - With `HEXAPOD_IP` UNSET: `is_available()` returns false (the tool is silently hidden from the LLM schema)
  </acceptance_criteria>
  <done>
    HexapodTcpTool is registered in `register_defaults()` and visible through `ToolRegistry::list_tools()` (when HEXAPOD_IP is set) and hidden via `is_available()` when it is not. All workspace tests pass. Commit landed on develop.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check --workspace` exits 0
- [ ] `cargo test --workspace` exits 0 (full workspace suite; Plan 02 unit tests still pass and any count-sensitive registry default tests have been updated to match the new count)
- [ ] `cargo build -p ironhermes-cli` exits 0 — verifies the CLI binary links cleanly with the new tool registered
- [ ] `grep -rn 'hexapod_tcp\|HexapodTcpTool' crates/ironhermes-tools/src/ | wc -l` shows the tool referenced in lib.rs (1), registry.rs (use + register call, 2), hexapod_tcp.rs (multiple — its own file), and the inline test module
</verification>

<success_criteria>
- HXP-TOOL-01 acceptance: the tool is registered in IronHermes ToolRegistry; when run with HEXAPOD_IP set, the agent's tool schema includes hexapod_tcp; when run without it, the tool is silently absent (no error)
- HXP-TOOL-06 wiring is now end-to-end live: trait default (Plan 01) → override implementation (Plan 02) → wired shutdown call site (Plan 03) → tool actually in the registry to receive the call (this plan)
- The phase's autonomous code-only test surface (`cargo test --workspace`) is fully green
- Manual UAT (Plan 05) can now exercise the live robot
</success_criteria>

<output>
After completion, create `.planning/phases/27.1.1-safe-foundation/27.1.1-04-SUMMARY.md`
</output>
