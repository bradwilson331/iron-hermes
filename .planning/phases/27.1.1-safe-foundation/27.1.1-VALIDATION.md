---
phase: 27.1.1
slug: safe-foundation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-10
---

# Phase 27.1.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `#[tokio::test]` (already enabled via `tokio = { workspace = true, features = ["full", "test-util"] }` in `crates/ironhermes-tools/Cargo.toml` dev-deps) |
| **Config file** | none — workspace `Cargo.toml` carries the dependency; `#[cfg(test)] mod tests` is inline in each tool file (terminal.rs is the canonical example) |
| **Quick run command** | `cargo test -p ironhermes-tools hexapod` |
| **Full suite command** | `cargo test --workspace` |
| **Estimated runtime** | ~30s for the targeted hexapod tests; ~3-5min for the full workspace suite on a warm cache |

---

## Sampling Rate

- **After every task commit:** Run `cargo check -p ironhermes-tools` (Plans 01, 02, 04) or `cargo check -p ironhermes-cli` (Plan 03) — sub-10s feedback
- **After every plan wave:** Run `cargo test -p ironhermes-tools` (Wave 1, 2 — trait + tool); `cargo build -p ironhermes-cli && cargo test -p ironhermes-cli --lib` (Wave 2 Plan 03); `cargo test --workspace` (Wave 3 Plan 04)
- **Before `/gsd-verify-work`:** `cargo test --workspace` must be green; Plan 05 UAT signed off
- **Max feedback latency:** 30s for unit suite; 5min for full workspace; manual UAT (Plan 05) is on-demand

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 27.1.1-01-01 | 01 | 1 | HXP-TOOL-06 (foundation) | — | Trait default no-op; ToolRegistry helper | unit (compile + existing test reuse) | `cargo check -p ironhermes-tools && cargo test -p ironhermes-tools --lib` | ❌ W0 (Plan 01 creates the splice) | ⬜ pending |
| 27.1.1-02-01 | 02 | 2 | HXP-TOOL-01..05, HXP-LOCO-01..05 (impl) | Allowlist (D-20 match) enforces compile-time access control; speed clamp prevents tampering | Blocked actions return D-16 string Ok-not-Err; speed clamped 2..=10; missing env var returns D-12 string; sensor timeout returns D-18 string | unit | `cargo test -p ironhermes-tools hexapod` | ❌ W0 (Plan 02 creates 12 inline tests) | ⬜ pending |
| 27.1.1-03-01 | 03 | 2 | HXP-TOOL-06 (wiring run_single + run_chat) | — | `registry.read().await` — read-lock only per RESEARCH Pitfall 6 | unit (compile + existing CLI tests) | `cargo check -p ironhermes-cli && cargo test -p ironhermes-cli --lib` | ✅ (touches existing files) | ⬜ pending |
| 27.1.1-03-02 | 03 | 2 | HXP-TOOL-06 (wiring ratatui) | — | Same read-lock discipline | unit (compile + existing TUI tests) | `cargo check -p ironhermes-cli && cargo build -p ironhermes-cli` | ✅ | ⬜ pending |
| 27.1.1-04-01 | 04 | 3 | HXP-TOOL-01 (lib.rs module decl) | — | — | unit (compile) | `cargo check -p ironhermes-tools` | ✅ | ⬜ pending |
| 27.1.1-04-02 | 04 | 3 | HXP-TOOL-01 (register_defaults) | Tool gated by `is_available()` (HEXAPOD_IP env var) so it is silently absent on machines without a hexapod | Default `is_available()` walks `prerequisites()` and checks `std::env::var` — same pattern as web_search/FIRECRAWL_API_KEY | unit (workspace test) | `cargo test --workspace` | ✅ | ⬜ pending |
| 27.1.1-05-01 | 05 | 4 | HXP-LOCO-01..05, HXP-TOOL-06 (UAT artifact) | Manual UAT verifies physical outcomes the unit suite cannot reach | — | manual UAT | `.planning/phases/27.1.1-safe-foundation/27.1.1-UAT.md` — developer runs with powered robot | N/A (manual) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠ flaky*

**Per-task automated coverage:**
- Plan 01 → reuses existing `cargo test -p ironhermes-tools --lib`; the splice itself is verified by `cargo check` (compilation proof) + the existing test module's 25+ test cases compiling unchanged (Tool default no-op proof)
- Plan 02 → 12 inline tests under `hexapod_tcp::tests` (see RESEARCH §Test Surface)
- Plan 03 → compilation (`cargo check -p ironhermes-cli`) + existing CLI/TUI tests; the wiring is structurally verified by grep + the workspace build
- Plan 04 → `cargo test --workspace` (the integration point — the new tool participates in any default-registry test); plus a workspace build (`cargo build -p ironhermes-cli`)
- Plan 05 → manual UAT only (the only path that exercises physical robot behavior)

---

## Wave 0 Requirements

- [ ] `crates/ironhermes-tools/src/hexapod_tcp.rs` — does NOT yet exist; created by Plan 02. Contains the 12 inline unit tests that cover HXP-TOOL-01..05 (Plan 02 RED→GREEN cycle).
- [ ] `crates/ironhermes-tools/src/registry.rs` — needs the `on_session_end` default and `call_session_end_hooks` method (Plan 01). The existing test module continues to exercise these via the 25+ existing Tool impls (zero-breaking-change proof).
- [ ] No new test framework needed — `#[tokio::test]` and `tokio = { features = ["full", "test-util"] }` are already in workspace dev-deps (RESEARCH §Test Surface, lines 670-678)
- [ ] No additional fixtures needed — sensor responses are synthesized in-test as static strings (`"CMD_POWER#7.2#8.1\n"`, etc.); live TCP is NOT exercised in the unit suite

If none of the above gaps are open at the start of Plan execution: existing test infrastructure covers everything.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Robot physically walks forward / backward / left / right | HXP-LOCO-01, ROADMAP #1 | No robot in CI; physical-effect outcome | UAT.md Criterion 1 |
| Robot halts on stop command | HXP-LOCO-02, ROADMAP #2 | Physical effect | UAT.md Criterion 2 |
| Battery voltage string returned with current readings | HXP-LOCO-03, ROADMAP #3 | Requires live TCP + ADC | UAT.md Criterion 3 |
| Ultrasonic distance returned in centimeters | HXP-LOCO-04, ROADMAP #4 | Requires live TCP + sensor | UAT.md Criterion 4 |
| Servos visibly relax on relax_servos | HXP-LOCO-05 | Physical effect (limp legs) | UAT.md Criterion 5 |
| Robot auto-halts and relaxes on REPL session end | HXP-TOOL-06, ROADMAP #5 | End-to-end across trait + override + wiring + registration | UAT.md Criterion 6 |
| Robot auto-halts on run_single session end | HXP-TOOL-06 | End-to-end batch path | UAT.md Criterion 6b |
| Robot auto-halts on ratatui TUI session end | HXP-TOOL-06 | End-to-end TUI path — first time this hook exists for ratatui (RESEARCH §Path 3) | UAT.md Criterion 6c |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or are documented in Manual-Only Verifications
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (verified — every code-producing plan has cargo check/test, only the UAT plan is manual)
- [ ] Wave 0 covers all MISSING references (Plans 01-04 create what the verify commands assert)
- [ ] No watch-mode flags (cargo test runs to completion)
- [ ] Feedback latency < 30s for the targeted tool suite; < 5min for the workspace
- [ ] `nyquist_compliant: true` set in frontmatter (gated until /gsd-validate-phase confirms the per-task sampling actually held during execution)

**Approval:** pending — to be set to `approved YYYY-MM-DD` after `/gsd-validate-phase 27.1.1` confirms the sampling rate held through Plans 01-05 execution.
