---
phase: 27.1.1-safe-foundation
verified: 2026-05-11T12:00:00Z
status: human_needed
score: 11/11 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Run cargo test --workspace and confirm the two pre-existing failures (browser_integration in ironhermes-tools-api; websocket_lifecycle_parity in iron_hermes_ui) have not grown in count"
    expected: "Exactly 2 pre-existing failures, zero new failures"
    why_human: "The worktree cannot run the full workspace build (ironhermes-tools-api missing Cargo dep; iron_hermes_ui stale grep assertions); requires a full local build environment to count failures precisely"
  - test: "Confirm REQUIREMENTS.md traceability table rows for HXP-TOOL-01..06 and HXP-LOCO-01..05 are updated from 'Pending' to 'Satisfied' (the table currently shows all 11 as Pending)"
    expected: "All 11 requirement rows marked Satisfied"
    why_human: "REQUIREMENTS.md traceability table was not updated as part of any plan — the status column still reads 'Pending' for all 11 HXP rows. Requires a human decision on whether to update now or defer to milestone close."
---

# Phase 27.1.1: Safe Foundation — Verification Report

**Phase Goal:** Agent can issue walk, stop, read_battery, read_distance, and relax_servos commands to the Freenove hexapod robot over TCP — with every unsafe command blocked before any bytes reach the hardware.
**Verified:** 2026-05-11
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | Agent can walk the hexapod in all four directions (forward/backward/left/right) at configurable speed | VERIFIED | `build_walk_wire` produces `CMD_MOVE#1#0#25#<s>#0\n`, `CMD_MOVE#1#0#-25#<s>#0\n`, `CMD_MOVE#1#-25#0#<s>#0\n`, `CMD_MOVE#1#25#0#<s>#0\n`; 4 direction tests pass; UAT Criterion 1 all boxes checked |
| 2 | Agent can halt and return hexapod to neutral stance | VERIFIED | `STOP_CMD = "CMD_MOVE#0#0#0#0#0\n"` wired into stop arm; `test_stop_wire_string` passes; UAT Criterion 2 checked |
| 3 | Agent receives human-readable battery voltage string | VERIFIED | `parse_battery_response` returns `"Battery: <v1>V / <v2>V (OK|LOW)"`; LOW thresholds 5.5V / 6.0V verified; `test_battery_parse_ok` and `test_battery_parse_low` pass; UAT Criterion 3 checked (actual: `Battery: 7.94V / 7.47V (OK)`) |
| 4 | Agent receives human-readable distance string | VERIFIED | `parse_distance_response` returns `"Distance: <N> cm"`; `test_distance_parse` passes; UAT Criterion 4 checked |
| 5 | Agent can relax hexapod servos | VERIFIED | `RELAX_CMD = "CMD_RELAX\n"` wired into relax_servos arm; toggle comment per Pitfall 5 present; `test_relax_wire_string` passes; UAT Criterion 5 checked |
| 6 | Every unsafe command is blocked before bytes reach hardware | VERIFIED | Compile-time exhaustive `match action` with catch-all returning `"Action '<x>' is blocked — not permitted via hexapod_tcp. Never send this command."`; `test_unknown_action_returns_blocked`, `test_calibration_action_blocked`, `test_servo_power_action_blocked` all pass; UAT Criterion 7 checked |
| 7 | Session end fires stop + relax automatically on all three shutdown paths | VERIFIED | `on_session_end()` override uses `tokio::spawn(async move { send_stop_and_relax(&addr).await })`; `call_session_end_hooks` wired at `main.rs:923` (run_single), `main.rs:2193` (run_chat), `event_loop.rs:110` (ratatui TUI); UAT Criteria 6/6b/6c all checked |
| 8 | Tool is hidden when HEXAPOD_IP is unset | VERIFIED | `prerequisites()` returns `{kind:"env_var", name:"HEXAPOD_IP", required:true}`; default `is_available()` returns false; `test_missing_env_var_returns_ok_error` passes; UAT Criterion 8 checked |
| 9 | `hexapod_tcp` tool registered in ToolRegistry and visible to all production paths | VERIFIED | `pub mod hexapod_tcp` in `lib.rs`; `register_defaults_except` in `registry.rs` (14 occurrences of the method); `app_runtime_factory.rs` delegates to `register_defaults_except(&["terminal"])`; `event_loop.rs` also delegates; `test_register_defaults_includes_hexapod_tcp` passes |
| 10 | `tools.toolsets` from `config.yaml` is enforced at session start | VERIFIED | `with_default_toolsets_merged()` in `config.rs`; `set_toolset_config(Some(merged_tools.clone()))` in `app_runtime_factory.rs:139`; TUI also calls `set_toolset_config` on locally-built registry; `test_toolset_config_filters_disabled_web`, `test_toolset_config_robotics_default_enabled`, `test_toolset_config_robotics_explicitly_disabled` all pass (ironhermes-agent: 268/268) |
| 11 | `PromptBuilder.active_toolsets` populated so system-prompt catalog matches API schemas | VERIFIED | `set_active_toolsets(merged_tools.enabled_toolset_names())` at run_chat (`main.rs:801`), run_single (`main.rs:1387`), TUI (`event_loop.rs:527`), iron_hermes_ui (`state.rs:220-221`); stale "Phase 20" comments updated to "Phase 27.1.1-gap-02" |

**Score:** 11/11 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/ironhermes-tools/src/hexapod_tcp.rs` | HexapodTcpTool with allowlist, wire translation, sensor parsing, session-end halt, 15 inline tests | VERIFIED | 554 lines; 15 tests pass (plan required ≥13; 15 delivered including `test_read_timeout_branch`, `test_on_session_end_no_panic`) |
| `crates/ironhermes-tools/src/registry.rs` | `Tool::on_session_end` default + `call_session_end_hooks` + `register_defaults_except` | VERIFIED | `fn on_session_end(&self) {}` at L77; `pub fn call_session_end_hooks` at L494; `pub fn register_defaults_except` at L527 |
| `crates/ironhermes-tools/src/lib.rs` | `pub mod hexapod_tcp;` in alphabetical position | VERIFIED | Confirmed present (1 occurrence) |
| `crates/ironhermes-cli/src/main.rs` | `call_session_end_hooks` in run_single and run_chat shutdown blocks | VERIFIED | L923 (run_single) and L2193 (run_chat); both use `registry.read().await.call_session_end_hooks()` |
| `crates/ironhermes-cli/src/tui_rata/event_loop.rs` | `call_session_end_hooks` after `run_app_inner` returns | VERIFIED | L110 confirmed |
| `crates/ironhermes-core/src/config.rs` | `with_default_toolsets_merged()` + `enabled_toolset_names()` | VERIFIED | Both methods present (grep count = 1 each) |
| `crates/ironhermes-agent/src/app_runtime_factory.rs` | `set_toolset_config` call + `merged_tools` field on bundle | VERIFIED | `pub merged_tools: ToolsConfig` at L58; `set_toolset_config(Some(merged_tools.clone()))` at L139 |
| `crates/ironhermes-agent/src/prompt_builder.rs` | `set_active_toolsets` populated at production sites | VERIFIED | Method exists at L126; called at 4 production sites |
| `crates/ironhermes-core/src/constants.rs` | `ALL_TOOLSETS` with "robotics" included | VERIFIED | `pub const ALL_TOOLSETS: &[&str]` at L53 with 8 entries including "robotics" |
| `.planning/phases/27.1.1-safe-foundation/27.1.1-UAT.md` | UAT script with all criteria + sign-off | VERIFIED | Status: PASSED (2026-05-11); all 8+ criteria checked; signed off by Brad Wilson |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `HexapodTcpTool::execute` | `tokio::net::TcpStream` | `TcpStream::connect` per call | WIRED | `grep -c 'TcpStream::connect' hexapod_tcp.rs` = 1+ |
| `HexapodTcpTool::on_session_end` | `send_stop_and_relax` | `tokio::spawn(async move { ... })` | WIRED | `tokio::spawn` count ≥1; `fn on_session_end` (sync, not async) |
| `run_single` shutdown | `ToolRegistry::call_session_end_hooks` | `registry.read().await.call_session_end_hooks()` | WIRED | main.rs:923 |
| `run_chat` shutdown | `ToolRegistry::call_session_end_hooks` | `registry.read().await.call_session_end_hooks()` | WIRED | main.rs:2193 |
| `run_with_deps` (TUI) shutdown | `ToolRegistry::call_session_end_hooks` | `registry.read().await.call_session_end_hooks()` | WIRED | event_loop.rs:110 |
| `build_app_runtime_bundle` | `ToolRegistry::set_toolset_config` | `registry.set_toolset_config(Some(merged_tools.clone()))` | WIRED | app_runtime_factory.rs:139 |
| `PromptBuilder` construction sites (4) | `set_active_toolsets` | `merged_tools.enabled_toolset_names()` | WIRED | main.rs:801, 1387; event_loop.rs:527; state.rs:220 |
| `register_defaults_except` | `HexapodTcpTool` | included in canonical entry point | WIRED | `test_register_defaults_includes_hexapod_tcp` passes |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `hexapod_tcp.rs execute()` walk arm | `wire` string from `build_walk_wire` | `args["action"]`, `args["direction"]`, `args["speed"]` | Yes — format!() with clamped speed | FLOWING |
| `hexapod_tcp.rs execute()` battery arm | `raw` line from TCP | `send_and_read_line` + `timeout(3s)` | Yes — real TCP read via `BufReader::read_line` | FLOWING |
| `hexapod_tcp.rs execute()` distance arm | `raw` line from TCP | `send_and_read_line` + `timeout(3s)` | Yes — real TCP read | FLOWING |
| `ToolRegistry::get_definitions` toolset filter | `toolset_config` | `set_toolset_config(Some(merged_tools))` at startup | Yes — `with_default_toolsets_merged()` from real config | FLOWING |
| `PromptBuilder` catalog text | `active_toolsets` | `merged_tools.enabled_toolset_names()` at construction | Yes — same merged config as registry | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 15 hexapod unit tests pass | `cargo test -p ironhermes-tools --lib hexapod` | 15 passed, 0 failed | PASS |
| Full ironhermes-tools suite (321 tests) | `cargo test -p ironhermes-tools --lib` | 321 passed, 0 failed | PASS |
| ironhermes-agent suite (268 tests) incl. toolset_config tests | `cargo test -p ironhermes-agent --lib` | 268 passed, 0 failed | PASS |
| ironhermes-core suite (pre-existing 1 failure) | `cargo test -p ironhermes-core --lib` | 450 passed, 1 failed (pre-existing `dispatch_all_todo_stubs`) | PASS (pre-existing) |
| ironhermes-cli suite (pre-existing 1 failure) | `cargo test -p ironhermes-cli --lib` | 203 passed, 1 failed (pre-existing `inv_25_1_gap8_app_carries_browser_session_field`) | PASS (pre-existing) |
| Hexapod walk direction strings exact | `test_walk_direction_strings` | forward/backward/left/right all match D-06 byte-for-byte | PASS |
| Battery LOW both thresholds | `test_battery_parse_low` | v1<5.5 → LOW, v2<6.0 → LOW | PASS |
| Timeout error mapping helper | `test_read_timeout_branch` | Elapsed→"Error: read timed out after 3s...", Ok(Err)→"Error: cannot connect to robot at..." | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| HXP-TOOL-01 | Plans 04, gap-01 | `hexapod_tcp` tool registered in IronHermes ToolRegistry | SATISFIED | `pub mod hexapod_tcp` in lib.rs; `register_defaults_except` includes it; `test_register_defaults_includes_hexapod_tcp` passes; UAT Criterion 8 |
| HXP-TOOL-02 | Plan 02 | Allowlist + blocked commands return Ok("Action '...' is blocked") | SATISFIED | Compile-time exhaustive `match action`; catch-all D-16 string; 3 blocked-action tests pass; UAT Criterion 7 |
| HXP-TOOL-03 | Plan 02 | Fresh TCP connection per call (stateless) | SATISFIED | `TcpStream::connect` in each arm; no persistent socket; `D-11` pattern; stateless unit struct |
| HXP-TOOL-04 | Plan 02 | JSON schema → wire protocol translation | SATISFIED | `build_walk_wire`, `STOP_CMD`, `RELAX_CMD`, `BATTERY_CMD`, `DISTANCE_CMD` all correct D-06..D-10 wire strings; test_walk_direction_strings verifies exact byte format |
| HXP-TOOL-05 | Plan 02 | Parsed human-readable response with 3s read timeout | SATISFIED | `parse_battery_response`, `parse_distance_response`; `timeout(Duration::from_secs(3), ...)` in battery/distance arms; `map_read_outcome` helper; D-18 expiry path tested |
| HXP-TOOL-06 | Plans 01, 02, 03 | Send stop + relax on session end via `on_session_end` | SATISFIED | Trait default in registry.rs; override in hexapod_tcp.rs with `tokio::spawn`; wired at all 3 CLI/TUI shutdown paths; UAT Criteria 6/6b/6c all checked |
| HXP-LOCO-01 | Plan 02 | Walk forward/backward/left/right at speed 2–10 | SATISFIED | 4 direction strings correct; speed clamped to `clamp(2, 10)`; UAT Criterion 1 |
| HXP-LOCO-02 | Plan 02 | Stop and return to neutral stance (CMD_MOVE mode=0) | SATISFIED | `STOP_CMD = "CMD_MOVE#0#0#0#0#0\n"`; UAT Criterion 2 |
| HXP-LOCO-03 | Plan 02 | Read battery voltage with low-battery alert | SATISFIED | `parse_battery_response` with 5.5V/6.0V thresholds; UAT Criterion 3 (actual: `Battery: 7.94V / 7.47V (OK)`) |
| HXP-LOCO-04 | Plan 02 | Read ultrasonic distance in centimeters | SATISFIED | `parse_distance_response` returns `"Distance: <N> cm"`; UAT Criterion 4 |
| HXP-LOCO-05 | Plan 02 | Relax servos (CMD_RELAX) | SATISFIED | `RELAX_CMD = "CMD_RELAX\n"`; toggle comment per Pitfall 5; UAT Criterion 5 |

**Note:** REQUIREMENTS.md traceability table rows for all 11 HXP requirements still show "Pending" — they were not updated as part of any plan. This is flagged for human decision in the Human Verification section.

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| None found | — | — | All verified files have substantive implementations with real data flows; no stubs, no hardcoded empty returns, no TODO placeholders in production paths |

### Human Verification Required

#### 1. Full Workspace Test Baseline

**Test:** Run `cargo test --workspace` in a complete build environment and count failures.
**Expected:** Only the 2 pre-existing failures documented in gap-01-SUMMARY.md (`ironhermes-tools-api` compile error; `iron_hermes_ui::websocket_lifecycle_parity` — 2 test cases) remain. Zero new failures from gap-02 changes.
**Why human:** The verification environment cannot build `ironhermes-tools-api` (missing `ironhermes-tools` Cargo dependency) or run `iron_hermes_ui` tests (stale source-grep assertions against `warp_hermes.rs`). Count must be confirmed in the full dev environment.

#### 2. REQUIREMENTS.md Traceability Table Update

**Test:** Inspect `.planning/REQUIREMENTS.md` lines 382–392. The status column for all 11 HXP-TOOL/LOCO rows currently reads "Pending".
**Expected:** Rows updated to "Satisfied" (or equivalent) now that UAT passed with a signed sign-off dated 2026-05-11.
**Why human:** Updating the requirements status table is a policy decision — it may be deferred to milestone close, or done now. The verifier cannot change the table without explicit instruction.

### Gaps Summary

No technical gaps blocking the phase goal. Both human verification items are process/housekeeping concerns, not implementation gaps:

1. The full workspace test count cannot be confirmed in this environment (known pre-existing failures exist; the question is whether gap-02 introduced any new ones — the per-crate results above show zero new failures in the crates touched by gap-02).
2. REQUIREMENTS.md status rows remain "Pending" — the implementation satisfies all 11 requirements but the table was not updated.

---

_Verified: 2026-05-11_
_Verifier: Claude (gsd-verifier)_
