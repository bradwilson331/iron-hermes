---
phase: 27.1.1-safe-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "27.1.1-01"
files_modified:
  - crates/ironhermes-tools/src/hexapod_tcp.rs
autonomous: true
requirements:
  - HXP-TOOL-01
  - HXP-TOOL-02
  - HXP-TOOL-03
  - HXP-TOOL-04
  - HXP-TOOL-05
  - HXP-TOOL-06
  - HXP-LOCO-01
  - HXP-LOCO-02
  - HXP-LOCO-03
  - HXP-LOCO-04
  - HXP-LOCO-05
user_setup: []

must_haves:
  truths:
    - "A new `pub struct HexapodTcpTool;` exists in `crates/ironhermes-tools/src/hexapod_tcp.rs` (unit struct, stateless per D-11)"
    - "`impl Tool for HexapodTcpTool` declares name()='hexapod_tcp', toolset()='robotics', a description() that names the 5 actions AND the blocked commands, schema() with action enum + optional direction/speed, prerequisites() declaring HEXAPOD_IP, execute(), and on_session_end() override"
    - "execute() uses a compile-time exhaustive `match action.as_str()` (D-20) — five known arms (walk/stop/read_battery/read_distance/relax_servos) plus a catch-all that returns the D-16 blocked-command string"
    - "Walk action produces the four wire strings exactly per D-06: forward=`CMD_MOVE#1#0#25#<speed>#0\\n`, backward=`CMD_MOVE#1#0#-25#<speed>#0\\n`, left=`CMD_MOVE#1#-25#0#<speed>#0\\n`, right=`CMD_MOVE#1#25#0#<speed>#0\\n` (HXP-LOCO-01)"
    - "Stop action produces `CMD_MOVE#0#0#0#0#0\\n` (D-07, HXP-LOCO-02). Relax produces `CMD_RELAX\\n` (D-08, HXP-LOCO-05) — code comment notes CMD_RELAX is a toggle per RESEARCH Pitfall 5"
    - "read_battery sends `CMD_POWER\\n`, parses response `CMD_POWER#<v1>#<v2>\\n` split on '#', returns `\"Battery: <v1>V / <v2>V (OK|LOW)\"` — LOW iff v1<5.5 OR v2<6.0 (D-09, HXP-LOCO-03)"
    - "read_distance sends `CMD_SONIC\\n`, parses response `CMD_SONIC#<dist>\\n`, returns `\"Distance: <dist> cm\"` (D-10, HXP-LOCO-04)"
    - "Missing HEXAPOD_IP returns `Ok(\"Error: HEXAPOD_IP env var not set — cannot connect to robot\")` (D-12)"
    - "Connect failure returns `Ok(\"Error: cannot connect to robot at <ip>:5002 — is HEXAPOD_IP set and the robot powered on?\")` (D-17)"
    - "Sensor read timeout (3s) returns `Ok(\"Error: read timed out after 3s waiting for robot response\")` (D-18)"
    - "Fire-and-forget commands (walk, stop, relax_servos) that successfully write return `Ok(\"OK\")` (D-19)"
    - "Speed is clamped to 2..=10 in execute() before building the wire string (RESEARCH Pitfall 4)"
    - "on_session_end() override uses `tokio::spawn(async move { ... })` to fire CMD_MOVE#0#0#0#0#0\\n then CMD_RELAX\\n; the spawned future swallows all errors (D-14)"
    - "prerequisites() returns a single `Prerequisite { kind: 'env_var', name: 'HEXAPOD_IP', required: true, ... }` so the default `is_available()` hides the tool when the env var is unset (RESEARCH §Env Var Read Pattern)"
    - "Inline `#[cfg(test)] mod tests` block contains all 13 unit tests (12 from RESEARCH §Test Surface plus `test_read_timeout_branch` exercising the D-18 expiry path) — runs green via `cargo test -p ironhermes-tools hexapod`"
    - "No live TCP test is in the unit suite (manual UAT lives in Plan 05)"
  artifacts:
    - path: "crates/ironhermes-tools/src/hexapod_tcp.rs"
      provides: "HexapodTcpTool — Phase 27.1.1 hexapod TCP tool with allowlist, wire translation, sensor parsing, session-end halt, inline tests"
      min_lines: 180
      exports: ["HexapodTcpTool"]
      contains: "impl Tool for HexapodTcpTool"
    - path: "crates/ironhermes-tools/src/hexapod_tcp.rs"
      provides: "Compile-time exhaustive match enforcing the allowlist"
      contains: "match action"
    - path: "crates/ironhermes-tools/src/hexapod_tcp.rs"
      provides: "Wire string for forward walk per D-06"
      contains: "CMD_MOVE#1#0#25"
    - path: "crates/ironhermes-tools/src/hexapod_tcp.rs"
      provides: "Stop wire string per D-07"
      contains: "CMD_MOVE#0#0#0#0#0"
    - path: "crates/ironhermes-tools/src/hexapod_tcp.rs"
      provides: "Battery low threshold per D-09 + adc.py"
      contains: "5.5"
    - path: "crates/ironhermes-tools/src/hexapod_tcp.rs"
      provides: "on_session_end override per D-14"
      contains: "fn on_session_end"
    - path: "crates/ironhermes-tools/src/hexapod_tcp.rs"
      provides: "Inline test module per RESEARCH §Test Surface"
      contains: "#[cfg(test)]"
  key_links:
    - from: "HexapodTcpTool::execute (hexapod_tcp.rs)"
      to: "tokio::net::TcpStream"
      via: "fresh connect per call (D-11)"
      pattern: "TcpStream::connect"
    - from: "HexapodTcpTool::execute walk arm"
      to: "wire format CMD_MOVE#1#<x>#<y>#<speed>#0"
      via: "format!() with clamped speed and ±25 displacement (D-06)"
      pattern: "CMD_MOVE#1#"
    - from: "HexapodTcpTool::on_session_end (hexapod_tcp.rs)"
      to: "Tool trait default in registry.rs (Plan 01)"
      via: "method override; trait declares the default — see 27.1.1-01-PLAN.md"
      pattern: "fn on_session_end"
    - from: "HexapodTcpTool::prerequisites"
      to: "Tool trait default is_available() in registry.rs:34-43"
      via: "is_available() walks prerequisites() — auto-hides tool when HEXAPOD_IP unset"
      pattern: "HEXAPOD_IP"
---

<objective>
Implement the `hexapod_tcp` Rust tool — a stateless TCP client that translates LLM-issued action enums into Freenove wire-protocol strings, blocks every command outside the Phase 1 allowlist via compile-time exhaustive `match` (D-20), parses sensor responses with a 3-second read timeout (D-18), and overrides `on_session_end` to fire a stop+relax safety halt when the IronHermes session ends (HXP-TOOL-06, D-14).

Purpose: This is the core Phase 27.1.1 deliverable. Every ROADMAP success criterion (walk, stop, battery, distance, session-end halt) is satisfied by the code in this single file. The tool is registered in Plan 03 and wired into shutdown in Plan 04 — both downstream plans depend on this file's existence.

Output: One new file `crates/ironhermes-tools/src/hexapod_tcp.rs` (~180-220 lines) with the `HexapodTcpTool` struct, full `impl Tool for HexapodTcpTool`, private TCP helpers, and an inline `#[cfg(test)] mod tests` block covering all 13 unit tests (12 from RESEARCH §Test Surface plus `test_read_timeout_branch` for the D-18 expiry path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md
@.planning/phases/27.1.1-safe-foundation/27.1.1-RESEARCH.md
@.planning/phases/27.1.1-safe-foundation/27.1.1-PATTERNS.md
@.planning/phases/27.1.1-safe-foundation/27.1.1-01-SUMMARY.md
@crates/ironhermes-tools/src/terminal.rs
@crates/ironhermes-tools/src/registry.rs
@crates/ironhermes-tools/src/web_search.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create `hexapod_tcp.rs` — full HexapodTcpTool implementation with inline unit tests</name>
  <files>crates/ironhermes-tools/src/hexapod_tcp.rs</files>
  <read_first>
    crates/ironhermes-tools/src/terminal.rs (lines 1-365 — canonical Tool impl pattern; struct shape, schema, execute, timeout, inline tests),
    crates/ironhermes-tools/src/registry.rs (lines 1-105 — Tool trait surface, Prerequisite struct, ToolSchema, is_available default; AFTER Plan 01: lines now also contain `fn on_session_end(&self) {}` default and `pub fn call_session_end_hooks`),
    crates/ironhermes-tools/src/web_search.rs (lines 60-90 — prerequisites() pattern for env_var; this is the closest analog),
    crates/ironhermes-tools/src/cronjob_tool.rs (lines 341-395 — action-enum schema pattern; copy the enum + optional-field shape),
    .planning/phases/27.1.1-safe-foundation/27.1.1-RESEARCH.md (lines 110-250 — terminal.rs reference mapping with schema construction body),
    .planning/phases/27.1.1-safe-foundation/27.1.1-RESEARCH.md (lines 480-535 — TCP/timeout pattern with concrete code snippets),
    .planning/phases/27.1.1-safe-foundation/27.1.1-RESEARCH.md (lines 538-585 — env var read pattern),
    .planning/phases/27.1.1-safe-foundation/27.1.1-RESEARCH.md (lines 587-665 — description survey + blocked-command surface),
    .planning/phases/27.1.1-safe-foundation/27.1.1-RESEARCH.md (lines 668-705 — test surface, 12 base inline tests; this plan adds a 13th `test_read_timeout_branch` to cover the D-18 expiry path that RESEARCH did not enumerate),
    .planning/phases/27.1.1-safe-foundation/27.1.1-RESEARCH.md (lines 744-855 — Pitfalls 1-7),
    .planning/phases/27.1.1-safe-foundation/27.1.1-PATTERNS.md (lines 35-215 — file-level snippets ready to copy),
    .planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md (lines 15-58 — all 20 locked decisions)
  </read_first>
  <behavior>
    These thirteen behaviors define the file's correctness contract. Every test below MUST be written in the inline `#[cfg(test)] mod tests` block in this same file. They are not aspirational — the file does not compile/pass review without all 13 green.

    1. `test_missing_env_var_returns_ok_error`: Given `HEXAPOD_IP` is unset (use `std::env::remove_var` at the start of the test), `tool.execute(json!({"action":"walk","direction":"forward","speed":5})).await` returns `Ok(s)` where `s.starts_with("Error: HEXAPOD_IP env var not set")` (D-12).
    2. `test_unknown_action_returns_blocked`: With HEXAPOD_IP set to a non-routable IP (e.g., "127.0.0.255"), `tool.execute(json!({"action":"chase_lights"})).await` returns `Ok(s)` where `s == "Action 'chase_lights' is blocked — not permitted via hexapod_tcp. Never send this command."` (D-16, D-20). Verify NO TCP connect was attempted (the blocked match arm fires before any I/O).
    3. `test_calibration_action_blocked`: `tool.execute(json!({"action":"calibration"})).await` returns the same D-16 string with `'calibration'` (covers CMD_CALIBRATION mapping).
    4. `test_servo_power_action_blocked`: `tool.execute(json!({"action":"servo_power"})).await` returns the D-16 string with `'servo_power'`.
    5. `test_walk_speed_clamps_low`: A helper or test-visible function (e.g., `pub(crate) fn build_walk_wire(direction: &str, speed: i64) -> String`) when called with `speed=1` produces a wire string containing `#1#0#25#2#0\n` (clamped to 2).
    6. `test_walk_speed_clamps_high`: `build_walk_wire("forward", 99)` produces a wire string containing `#1#0#25#10#0\n` (clamped to 10).
    7. `test_walk_direction_strings`: `build_walk_wire("forward", 5)` == `"CMD_MOVE#1#0#25#5#0\n"`; `build_walk_wire("backward", 5)` == `"CMD_MOVE#1#0#-25#5#0\n"`; `build_walk_wire("left", 5)` == `"CMD_MOVE#1#-25#0#5#0\n"`; `build_walk_wire("right", 5)` == `"CMD_MOVE#1#25#0#5#0\n"` (D-06).
    8. `test_stop_wire_string`: A `pub(crate) const STOP_CMD: &str = "CMD_MOVE#0#0#0#0#0\n";` (or equivalent helper) literally equals that string (D-07).
    9. `test_relax_wire_string`: `const RELAX_CMD: &str = "CMD_RELAX\n";` literally equals that string (D-08); the file MUST contain a comment near this constant noting "CMD_RELAX is a toggle — see RESEARCH Pitfall 5".
    10. `test_battery_parse_ok`: `parse_battery_response("CMD_POWER#7.2#8.1\n")` returns `"Battery: 7.2V / 8.1V (OK)"` (D-09).
    11. `test_battery_parse_low`: `parse_battery_response("CMD_POWER#5.4#7.9\n")` returns a string ending in `"(LOW)"`; `parse_battery_response("CMD_POWER#7.0#5.9\n")` also returns `"(LOW)"` (verifies BOTH thresholds: v1<5.5 OR v2<6.0 per D-09 + adc.py).
    12. `test_distance_parse`: `parse_distance_response("CMD_SONIC#42\n")` returns `"Distance: 42 cm"` (D-10). Also `test_on_session_end_no_panic`: `HexapodTcpTool.on_session_end()` with HEXAPOD_IP unset returns without panic (the tokio::spawn body short-circuits on the env-var miss).
    13. `test_read_timeout_branch`: D-18 expiry path. Extract the timeout-error-mapping logic into a testable `pub(crate)` helper — e.g., `pub(crate) fn map_read_outcome(outcome: Result<Result<String, std::io::Error>, tokio::time::error::Elapsed>, addr: &str) -> Result<String, anyhow::Error>` that returns `Ok("Error: read timed out after 3s waiting for robot response")` on `Err(_)` (D-18), `Ok("Error: cannot connect to robot at <addr> — is HEXAPOD_IP set and the robot powered on?")` on `Ok(Err(_))` (D-17), and `Ok(raw)` on `Ok(Ok(raw))`. The test asserts: `map_read_outcome(Err(tokio::time::error::Elapsed::default()), "127.0.0.255:5002")` returns `Ok(s)` where `s.starts_with("Error: read timed out after 3s")`. Bonus assertion: also test the `Ok(Err(...))` connection-error branch produces a string starting with `"Error: cannot connect to robot at 127.0.0.255:5002"` (D-17). This is the ONLY unit-testable surface for the timeout-error string — no real TCP needed.

    Write these tests FIRST in a `#[cfg(test)] mod tests` block. Run `cargo test -p ironhermes-tools hexapod` — they MUST fail to compile (because the helpers/constants/struct don't exist yet). THEN implement the production code in the same file until every test passes. RED → GREEN.
  </behavior>
  <action>
    Create the new file `crates/ironhermes-tools/src/hexapod_tcp.rs`. Structure (top to bottom):

    **Imports.** Mirror terminal.rs:1-14, swap process-related imports for TCP:
    ```rust
    use std::env;

    use async_trait::async_trait;
    use ironhermes_core::ToolSchema;
    use serde_json::json;
    use tokio::io::{AsyncReadExt, AsyncWriteExt, BufReader};
    use tokio::net::TcpStream;
    use tokio::time::{Duration, timeout};
    use tracing::debug;

    use crate::registry::{Prerequisite, Tool};
    ```
    Per RESEARCH §TCP/Timeout Pattern, prefer `BufReader::read_line` over fixed-size `read()` (Pitfall 3) for sensor responses since the protocol terminates with `\n`.

    **Wire constants (D-07, D-08, D-09, D-10).**
    ```rust
    /// Stop = mode 0 (halts motion + returns to neutral stance). Per D-07.
    pub(crate) const STOP_CMD: &str = "CMD_MOVE#0#0#0#0#0\n";

    /// Relax servos. Per D-08. NOTE: CMD_RELAX is a TOGGLE — each invocation
    /// flips the servo-enabled state. Calling twice re-enables servos. See
    /// RESEARCH Pitfall 5; CMD_SERVOPOWER would be more reliable but is in
    /// the blocked list (D-16).
    pub(crate) const RELAX_CMD: &str = "CMD_RELAX\n";

    pub(crate) const BATTERY_CMD: &str = "CMD_POWER\n";    // D-09
    pub(crate) const DISTANCE_CMD: &str = "CMD_SONIC\n";   // D-10

    /// D-09 battery thresholds verified against Code/Server/adc.py:
    /// channel 0 (load) < 5.5V or channel 4 (Pi) < 6.0V → "LOW".
    pub(crate) const BATTERY_LOW_V1: f32 = 5.5;
    pub(crate) const BATTERY_LOW_V2: f32 = 6.0;
    ```

    **Struct.** Unit struct (no fields — stateless per D-11, IP read at call time per D-12). Per RESEARCH §Struct Shape:
    ```rust
    pub struct HexapodTcpTool;
    ```
    No `new()` or `Default` impl needed; consumers construct via `HexapodTcpTool` directly.

    **Description constant.** Per RESEARCH §LLM Tool Description Survey (lines 587-621), include both the Phase 1 action list and the blocked-command guidance so Gemma 4 behaves without the skill doc:
    ```rust
    const DESCRIPTION: &str = "Control the Freenove hexapod robot over TCP. \
        Phase 1 actions: walk (with direction and speed), stop, read_battery, \
        read_distance, relax_servos. \
        BLOCKED (do not attempt): calibration, servo_power, led_mode 2-5 — \
        these return a block error and are never permitted via this tool.";
    ```

    **Helper functions (testable, no I/O).** Define BEFORE `impl Tool` for clarity:

    - `pub(crate) fn build_walk_wire(direction: &str, speed: i64) -> String` — clamps speed to 2..=10 then matches direction → returns one of the four D-06 strings. Default branch (unknown direction): returns the forward string (or could return empty `""`; pick forward as the safe default since execute() validates direction before calling).
    - `pub(crate) fn parse_battery_response(raw: &str) -> String` — splits raw on `'#'` (Pitfall 7 — TWO voltage fields), trims, parses parts[1] and parts[2] as `f32` with `unwrap_or(0.0)`, returns `format!("Battery: {}V / {}V ({})", v1, v2, if v1 < BATTERY_LOW_V1 || v2 < BATTERY_LOW_V2 { "LOW" } else { "OK" })`.
    - `pub(crate) fn parse_distance_response(raw: &str) -> String` — splits on `'#'`, returns `format!("Distance: {} cm", parts[1].trim())`.

    Keep these `pub(crate)` so the inline test module can call them directly with synthetic inputs — that is how tests 5–12 cover wire/parse behavior, and test 13 covers the D-18 timeout-error-mapping branch, without needing a robot.

    **Private async TCP helpers.** Defined as free async fns inside the module (NOT methods on the struct — keeps the struct unit and lets `on_session_end`'s spawned future avoid `&self` capture per Pitfall 2):

    - `async fn send_fire_and_forget(addr: &str, cmd: &str) -> anyhow::Result<()>` — `TcpStream::connect`, `write_all`, `flush`, return `Ok(())`. No read. Used for walk, stop, relax.
    - `async fn send_and_read_line(addr: &str, cmd: &str) -> anyhow::Result<String>` — connect, write, flush, wrap stream in `BufReader::new`, `read_line(&mut buf)`, return `buf` trimmed. Used for read_battery and read_distance. Per Pitfall 3, `read_line` is the robust choice.
    - `async fn send_stop_and_relax(addr: &str)` — fires `STOP_CMD` then `RELAX_CMD` via two separate connections (D-11 stateless), swallows errors with `let _ = ...`. Used by `on_session_end`.

    **`impl Tool for HexapodTcpTool`** (async_trait). Methods in order: `name`, `toolset`, `description`, `schema`, `prerequisites`, `execute`, `on_session_end`.

    - `name(&self) -> &str { "hexapod_tcp" }` (per D-03).
    - `toolset(&self) -> &str { "robotics" }` — new toolset name per RESEARCH §Open Questions A1 (toolset is used for filtering only; "robotics" cleanly groups Phases 27.1.1/27.1.2/27.1.3 hexapod work).
    - `description(&self) -> &str { DESCRIPTION }`.
    - `schema(&self) -> ToolSchema` — copy verbatim from RESEARCH lines 201-230 (action enum [walk, stop, read_battery, read_distance, relax_servos], optional `direction` enum [forward, backward, left, right], optional integer `speed` with minimum=2, maximum=10, `required: ["action"]`).
    - `prerequisites(&self) -> Vec<Prerequisite>` — return a single Prerequisite per RESEARCH §Prerequisites Hook (lines 731-742): kind="env_var", name="HEXAPOD_IP", description="IP address of the Freenove hexapod robot (e.g., 192.168.1.42). Required for hexapod_tcp to connect to the robot.", required=true. This (combined with the default `is_available()` at registry.rs:34-43) auto-hides the tool from the LLM schema when HEXAPOD_IP is unset.
    - `execute(&self, args: serde_json::Value) -> anyhow::Result<String>`:
        - Read HEXAPOD_IP via `std::env::var("HEXAPOD_IP")`. On Err → `return Ok("Error: HEXAPOD_IP env var not set — cannot connect to robot".to_string())` (D-12).
        - Build `addr = format!("{}:5002", ip)` (D-12).
        - Extract action: `let action = args["action"].as_str().unwrap_or("");`.
        - `match action { "walk" => ..., "stop" => ..., "read_battery" => ..., "read_distance" => ..., "relax_servos" => ..., other => return Ok(format!("Action '{}' is blocked — not permitted via hexapod_tcp. Never send this command.", other)), }` — this is the D-20 compile-time exhaustive allowlist.
        - **walk arm:** Extract direction (`args["direction"].as_str().unwrap_or("forward")`) and speed (`args["speed"].as_i64().unwrap_or(5)`). Call `build_walk_wire(direction, speed)` to produce the wire string. Call `send_fire_and_forget(&addr, &wire).await`. On Err → `return Ok(format!("Error: cannot connect to robot at {} — is HEXAPOD_IP set and the robot powered on?", addr))` (D-17). On Ok → `Ok("OK".to_string())` (D-19).
        - **stop arm:** Same connect pattern with `STOP_CMD`. Ok("OK") on success, D-17 error on failure (D-19).
        - **relax_servos arm:** Same with `RELAX_CMD`. Ok("OK") / D-17 (D-19).
        - **read_battery arm:** Wrap `send_and_read_line(&addr, BATTERY_CMD)` in `timeout(Duration::from_secs(3), ...)`, then call the testable helper `map_read_outcome(outcome, &addr)` (defined alongside the other `pub(crate)` helpers; see test 13) — `Ok(raw)` → `parse_battery_response(&raw)` (D-09). Funneling both error branches through `map_read_outcome` is what makes test 13 a meaningful regression check on D-17 and D-18 error strings without a real TCP.
        - **read_distance arm:** Identical timeout structure as battery, calling `parse_distance_response` on the `Ok(raw)` branch of `map_read_outcome` (D-10, D-18).
    - `on_session_end(&self)` — per D-14 + Pitfall 2 (no `&self` capture):
        ```rust
        fn on_session_end(&self) {
            // D-14 fire-and-forget safety halt: stop motion + relax servos
            // so the robot does not continue walking after the session ends.
            // No &self capture; reads HEXAPOD_IP inside the spawned future.
            tokio::spawn(async move {
                if let Ok(ip) = std::env::var("HEXAPOD_IP") {
                    let addr = format!("{}:5002", ip);
                    send_stop_and_relax(&addr).await;
                }
            });
        }
        ```

    **Inline test module** at the bottom of the file, exactly as RESEARCH §Test Surface specifies. Implement the 13 tests from the `<behavior>` block. Use `#[tokio::test]` for all async tests (per `terminal.rs:251-365`). For env-var manipulation, use `std::env::remove_var` / `std::env::set_var` — set the var to `"127.0.0.255"` (non-routable) for tests that need it set but never connect successfully. NEVER mock or attempt a real TCP connection in the unit suite. Test 13 (`test_read_timeout_branch`) calls the `map_read_outcome` helper directly with a synthetic `tokio::time::error::Elapsed` value — no async runtime gymnastics, just a unit assertion on the error-string mapping.

    Confirm via Pitfall 1: `on_session_end` is `fn`, not `async fn`. Confirm via Pitfall 6: the `on_session_end` body does not hold any lock.

    Run `cargo test -p ironhermes-tools hexapod` from the repo root. All 13 tests MUST pass. Also run `cargo check -p ironhermes-tools` — zero warnings.

    Commit: `feat(27.1.1-02): implement HexapodTcpTool with allowlist, wire translation, sensor parsing, session-end halt`. If you split test creation and implementation into two commits, use `test(27.1.1-02): add failing tests for HexapodTcpTool` for the RED commit followed by the `feat(...)` commit for GREEN.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo test -p ironhermes-tools hexapod 2>&1 | tail -20 && cargo check -p ironhermes-tools 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/ironhermes-tools/src/hexapod_tcp.rs` exists and is at least 180 lines including tests
    - `grep -c 'pub struct HexapodTcpTool' crates/ironhermes-tools/src/hexapod_tcp.rs` returns 1
    - `grep -c '"hexapod_tcp"' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 2 (in `name()` and `schema()`)
    - `grep -c 'HEXAPOD_IP' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 3 (description text in prereq, `env::var` read in execute, env var read in on_session_end spawn)
    - `grep -c 'CMD_MOVE#1#0#25' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 1 (forward walk string per D-06)
    - `grep -c 'CMD_MOVE#1#0#-25' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 1 (backward per D-06)
    - `grep -c 'CMD_MOVE#1#-25#0' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 1 (left per D-06)
    - `grep -c 'CMD_MOVE#1#25#0' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 1 (right per D-06)
    - `grep -c 'CMD_MOVE#0#0#0#0#0' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 1 (stop per D-07)
    - `grep -c 'CMD_RELAX' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 1 (relax per D-08)
    - `grep -c 'CMD_POWER' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 1 (battery per D-09)
    - `grep -c 'CMD_SONIC' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 1 (distance per D-10)
    - `grep -E 'CMD_RELAX.+toggle|toggle.+CMD_RELAX|toggle.*RELAX' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 1 (Pitfall 5 comment requirement)
    - `grep -c 'fn on_session_end' crates/ironhermes-tools/src/hexapod_tcp.rs` returns 1 (the override; NOT async fn — verify with `grep -c 'async fn on_session_end' crates/ironhermes-tools/src/hexapod_tcp.rs` returns 0)
    - `grep -c 'tokio::spawn' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 1 (D-14 fire-and-forget)
    - `grep -c 'TcpStream::connect' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 1 (D-11 stateless connect)
    - `grep -c '5.5' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 1 (battery threshold v1)
    - `grep -c '6.0' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 1 (battery threshold v2)
    - `grep -c '.clamp(2, 10)' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 1 (speed clamp per Pitfall 4)
    - `grep -c '#\\[cfg(test)\\]' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 1 (inline test module)
    - `cargo test -p ironhermes-tools hexapod` exits 0 AND output contains "test result: ok" with at least 13 passed test count (or the message lists each test by name and shows zero failed)
    - `grep -c 'fn map_read_outcome' crates/ironhermes-tools/src/hexapod_tcp.rs` returns 1 (the testable D-17/D-18 error-mapping helper required by test 13)
    - `grep -c 'test_read_timeout_branch' crates/ironhermes-tools/src/hexapod_tcp.rs` returns at least 1 (the D-18 expiry path test)
    - `cargo check -p ironhermes-tools` exits 0 with zero new warnings
  </acceptance_criteria>
  <done>
    `hexapod_tcp.rs` exists with the full HexapodTcpTool implementation; all 13 inline unit tests pass (including `test_read_timeout_branch` covering D-17 connect-error and D-18 timeout-expiry strings via the `map_read_outcome` helper); no live TCP is exercised in the unit suite; the file compiles cleanly; commits land on `develop` with the conventional-commit messages above; downstream Plan 03 can now `use crate::hexapod_tcp::HexapodTcpTool` and Plan 04's wiring will call `tool.on_session_end()` on a real instance.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p ironhermes-tools hexapod` exits 0 (all 13 inline tests pass, including `test_read_timeout_branch`)
- [ ] `cargo test -p ironhermes-tools` exits 0 (no regressions in any other tool's tests)
- [ ] `cargo check --workspace` exits 0 (the new file does not break any downstream crate; lib.rs has NOT been touched yet — Plan 03 wires that)
- [ ] No `async fn on_session_end` anywhere in the codebase (`grep -rE 'async fn on_session_end' crates/ironhermes-tools/src/ | wc -l` returns 0)
- [ ] File line count: `wc -l crates/ironhermes-tools/src/hexapod_tcp.rs` ≥ 180
</verification>

<success_criteria>
- All 11 in-scope requirements (HXP-TOOL-01..06, HXP-LOCO-01..05) have their tool-side code in place. Registration (HXP-TOOL-01 wiring) lands in Plan 03; CLI-side session-end wiring (HXP-TOOL-06 wiring) lands in Plan 04. This plan delivers the implementation, not the integration.
- The compile-time exhaustive `match` (D-20) is the enforcement of HXP-TOOL-02; the catch-all returns the D-16 blocked-command string.
- Every wire string in the file is byte-for-byte equal to its D-06..D-10 specification.
- The inline test suite covers the 12 RESEARCH §Test Surface scenarios plus a 13th `test_read_timeout_branch` test exercising the D-17/D-18 error-string mapping via the `map_read_outcome` helper.
</success_criteria>

<output>
After completion, create `.planning/phases/27.1.1-safe-foundation/27.1.1-02-SUMMARY.md`
</output>
