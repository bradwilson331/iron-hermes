# Phase 21.8.3.1: personality applied doesn't change the llm responses - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-10
**Phase:** 21.8.3.1-personality-applied-doesn-t-chage-the-llm-responses
**Areas discussed:** Scope, Persistence, Clear mechanic, Web UI scope, Gateway state storage

---

## Scope

| Option | Description | Selected |
|--------|-------------|----------|
| TUI only | Fix event_loop.rs spawn_turn; gateway has separate unconfirmed gap — leave for its own phase | |
| TUI + Gateway | Also wire personality overlay into GatewaySession for Telegram | |
| TUI + Gateway + Web UI | Fix all three surfaces | ✓ |

**User's choice:** All three surfaces (TUI + Gateway + Web UI)
**Notes:** User initially wanted all three surfaces. After clarification that the web UI is mock-only with no real LLM backend, the web UI scope was refined to "signal-only — personality pill already works" (no changes needed).

---

## Persistence

| Option | Description | Selected |
|--------|-------------|----------|
| Persist for all turns | Apply once, stays active until user changes or clears. Field name "next_turn" is misleading. | ✓ |
| Consume once (next turn only) | Overlay applies to next LLM call, then clears. Matches literal field name but bad UX. | |

**User's choice:** Persist for all turns (session-wide)
**Notes:** Field rename from `next_turn_personality_overlay` to `active_personality_overlay` flows from this decision.

---

## Clear Mechanic

| Option | Description | Selected |
|--------|-------------|----------|
| /personality clear | Add 'clear' as special arg — shows up in help text | ✓ |
| Defer — no clear mechanic | Fix apply path only; clear added when requested | |
| Restart session to clear | No clear mechanic; user can /reset | |

**User's choice:** `/personality clear`
**Notes:** Checked BEFORE the registry lookup so it doesn't try to find a preset named "clear".

---

## Web UI Scope (clarification round)

| Option | Description | Selected |
|--------|-------------|----------|
| Signal-only — personality pill already works | Dioxus Signal<Personality> drives status bar; no backend connection in v1 | ✓ |
| Prep the backend connection plumbing now | Add WebSocket/API backend connection — significant scope | |
| Add /personality clear to palette only | Add clear entry to command palette; stays mock-only | |

**User's choice:** Signal-only — no changes needed
**Notes:** iron_hermes_ui/CLAUDE.md explicitly states "Zero API keys, zero network calls in v1. Mocks only." The personality Signal already drives the status bar pill correctly (warp_hermes.rs:791). Web UI backend connection is deferred to its own future phase.

---

## Gateway State Storage

| Option | Description | Selected |
|--------|-------------|----------|
| GatewayHandler struct field | Add active_personality_overlay: Option<String> to GatewayHandler; each turn's PromptBuilder calls set_overlay() | ✓ |
| StateStore/session storage | Persist to SQLite so it survives gateway restarts. More durable but heavier. | |

**User's choice:** GatewayHandler struct field
**Notes:** Session-scoped is sufficient for now. SQLite persistence deferred.

---

## Claude's Discretion

- Whether to surface "Active personality: {name}" in the gateway's confirmation reply text (the TUI already does this via the `SlashOutcome::Handled` message)
- Whether "clear" hint appears in `/personality` (no-args) list output
- Exact gateway command dispatch wiring location — researcher should identify the precise method and line where gateway processes `/personality` command results

## Deferred Ideas

- **Web UI backend connection** — wiring `iron_hermes_ui` to real LLM calls so personality affects the agent; separate phase
- **Personality persistence across sessions** — saving overlay to StateStore/SQLite so it survives restarts; deferred, session-only is correct scope now
- **Active personality name in TUI status bar pill** — out of scope for this fix phase
