---
phase: 21.8.3.1
slug: personality-applied-doesn-t-chage-the-llm-responses
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-10
---

# Phase 21.8.3.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in test + cargo test |
| **Config file** | `Cargo.toml` per crate (no separate test config) |
| **Quick run command** | `cargo test -p ironhermes-cli -p ironhermes-core 2>&1 \| tail -20` |
| **Full suite command** | `cargo test --workspace 2>&1 \| tail -30` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-cli -p ironhermes-core 2>&1 | tail -20`
- **After every plan wave:** Run `cargo test --workspace 2>&1 | tail -30`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TUI-rename | TBD | 1 | D-02 | — | N/A | regression | `cargo test -p ironhermes-cli --test invariants_22_4` | ✅ exists | ⬜ pending |
| TUI-inject | TBD | 1 | D-03 | — | N/A | unit | `cargo test -p ironhermes-cli` | ❌ W0 gap | ⬜ pending |
| TUI-clear | TBD | 1 | D-05 | — | N/A | unit | `cargo test -p ironhermes-cli` | ❌ W0 gap | ⬜ pending |
| GW-struct | TBD | 1 | D-07 | — | N/A | structural | `cargo build -p ironhermes-gateway` | compile only | ⬜ pending |
| GW-detect | TBD | 1 | D-08 | — | N/A | unit | `cargo test -p ironhermes-gateway` | ❌ W0 gap | ⬜ pending |
| GW-overlay | TBD | 1 | D-09 | — | N/A | unit | `cargo test -p ironhermes-gateway` | ❌ W0 gap | ⬜ pending |
| core-clear-doc | TBD | 1 | D-05 | — | N/A | unit | `cargo test -p ironhermes-core --test handlers_personality` | ✅ exists (add test) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/ironhermes-cli/tests/invariants_22_4.rs` — verify still compiles after D-02 field rename (no new file; confirm existing test passes)
- [ ] Add test to `crates/ironhermes-core/tests/handlers_personality.rs` — `/personality clear` dispatched to core returns `CommandResult::Error("Unknown personality: clear")` (documents that "clear" is NOT handled at core level)
- [ ] `crates/ironhermes-cli/src/tui_rata/` — unit/invariant test asserting `active_personality_overlay` appears in `event_loop.rs` as a regression guard (grep-style or structural test acceptable given `tokio::spawn` block constraints)

*All Wave 0 items are additions to existing test files — no new test infrastructure needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `/personality pirate` then send a message — LLM response adopts pirate persona | D-03 (TUI), D-09 (gateway) | Integration test requires live LLM call | Start TUI session, `/personality pirate`, send "Hello", verify response style changed |
| `/personality clear` then send — LLM response returns to default | D-05 (TUI), D-08 (gateway) | Integration test requires live LLM call | Follow above with `/personality clear`, send another message, verify default response |

---

## Security Notes

| Concern | Risk | Mitigation |
|---------|------|------------|
| Overlay content injection | Personality preset text originates from `PersonalityRegistry` which runs `scan_context_content` on HERMES_HOME .md files | No new attack surface — overlay text pre-scanned at load time |
| "clear" as preset name collision | "clear" is not among the 14 built-in presets | Pre-dispatch check intercepts before registry lookup; no conflict |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
