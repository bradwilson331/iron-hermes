# Phase 25.2 — Deferred Decisions

> Out-of-scope decisions explicitly NOT covered by any 25.2 plan. Tracked here so the
> phase coverage gate can confirm intentional deferral (vs accidental omission).

---

## D-14 — Cache-awareness flag for `auxiliary.summary` mutation

**CONTEXT.md text (verbatim):**

> Cache-awareness flag. `summarization` role config changes are schema-altering for the model
> context (different aux model = different cost/latency curve). Phase 25 D-04's toolset-cache-break
> banner does NOT cover provider role changes. Flag for **Phase 27 (cache enforcement)** to surface
> a mid-session warning when the operator mutates `auxiliary.summary` while a session is live.
> **Out of scope for 25.2**; documented here so the v2.1 architectural-principle-#2 audit sees the dependency.

**Disposition:** **Deferred to Phase 27 (cache enforcement)** per CONTEXT.md author intent.

**What 25.2 ships instead:**
- Plan 11 emits structured `tracing::info_span!("web_extract.summary.aux_call", ...)` spans on every
  aux LLM invocation. Phase 27's cache-warning subsystem can consume these spans without re-instrumenting
  Phase 25.2 code.
- Plan 02's doc-comment update at `crates/ironhermes-core/src/config.rs:208` adds `summarization` to
  the cascade-role list, making the dependency visible to anyone editing `AuxiliaryConfig`.

**Why this is NOT a coverage gap:**
- CONTEXT.md text explicitly says "Out of scope for 25.2".
- The `architectural-principle-#2` audit (cache-awareness is load-bearing) is satisfied by surfacing
  the dependency in code comments + observability spans, not by implementing the warning itself.

**Acceptance for the deferral:**
- `grep -q 'summarization' crates/ironhermes-core/src/config.rs` exits 0 (RESERVED_ROLE_NAMES + cascade doc comment)
- `grep -q 'web_extract.summary.aux_call' crates/ironhermes-tools/src/web_extract/summary/tiers.rs` exits 0
- Phase 27 backlog item references this file as the prior-art dependency

---

*All other 27 D-decisions (D-01 through D-13, D-15 through D-28) are covered by plans 25.2-00 through 25.2-13.*
