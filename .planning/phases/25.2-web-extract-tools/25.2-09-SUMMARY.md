---
phase: 25.2-web-extract-tools
plan: 09
subsystem: tools/web-extract
tags: [pdf-handler, pdf-extract, ssrf, spawn-blocking, timeout, byte-cap, d-09, d-18, wave-3]

dependency_graph:
  requires:
    - phase: 25.2-00
      provides: "crates/ironhermes-tools/src/web_extract/pdf.rs (Wave 0 stub) + pdf-extract = { workspace = true } (workspace pin 0.10) in ironhermes-tools/Cargo.toml"
    - phase: 25.2-01
      provides: "crates/ironhermes-tools/src/web_local.rs::validate_url_async (D-18 SSRF check shared helper)"
    - phase: 25.2-06
      provides: "crates/ironhermes-tools/src/web_extract/backends/firecrawl.rs::fetch_with_firecrawl (D-09 primary entry that PDF handler delegates to first when FIRECRAWL_API_KEY is set)"
    - phase: 25.2-08
      provides: "Plan 08's LocalFetchOutcome.raw_bytes contract — guarantees mid-fetch PDF detection hands bytes to extract_pdf_bytes without a second GET"
  provides:
    - "pub async fn extract_pdf(url: &str) -> Result<ExtractionResult> in web_extract::pdf — D-09 full pipeline (Firecrawl primary + pdf-extract local fallback)"
    - "pub async fn extract_pdf_bytes(url: &str, bytes: Vec<u8>) -> Result<ExtractionResult> in web_extract::pdf — D-03 mid-fetch reroute entry that skips the GET because Plan 08's local backend already has the bytes"
    - "fn filename_title(url: &str) -> String — case-insensitive .pdf-stripping URL-last-segment fallback (used as canonical title on byte-direct path and as Firecrawl-empty-title backfill)"
    - "Three-layer T-25.2-06 mitigation: 50 MB byte cap (PDF_MAX_BYTES) + tokio::task::spawn_blocking (Pitfall 1) + tokio::time::timeout(30s) (T5)"
    - "T-25.2-03 SSRF mitigation: pre-fetch validate_url_async + post-redirect re-validation when fetching bytes directly (D-18) — mirrors Plan 08 contract"
  affects:
    - "Plan 25.2-13 (WebExtractTool dispatch) — UrlClass::Pdf arm calls extract_pdf(url); UrlClass::Web arm that mid-fetch reroutes (Plan 08 LocalFetchOutcome.content_type == application/pdf) calls extract_pdf_bytes(url, outcome.raw_bytes.unwrap())"
    - "Plan 25.2-14 (integration tests) — wiremock-backed web_extract_pdf_url_routes_to_pdf_backend exercises real PDF parsing end-to-end with a small fixture; redirect-chain SSRF integration test exercises post-redirect re-validation"

tech-stack:
  added: []  # pdf-extract workspace dep was added by Plan 25.2-00
  patterns:
    - "spawn_blocking + outer timeout pattern: `tokio::time::timeout(Duration::from_secs(30), tokio::task::spawn_blocking(move || pdf_extract::extract_text_from_mem(&bytes))).await` — three nested Result variants (timeout / join error / extract error) explicitly destructured for distinct error envelopes (pdf_text_extraction_timeout, pdf extract task panicked, pdf_text_extraction_failed)"
    - "Defense-in-depth byte cap: PDF_MAX_BYTES check enforced TWICE — once in fetch_pdf_bytes() before the bytes leave the network layer, again in extract_pdf_bytes() so the public mid-fetch entry is independently safe regardless of caller"
    - "Filename-fallback title pattern: url::Url::parse + .path_segments().and_then(...next_back()) + case-insensitive .pdf strip — works for both `arxiv.org/abs/2401.12345.pdf` and `example.com/doc.PDF`; empty string for path-less URLs (matches firecrawl.rs title-empty convention)"
    - "Firecrawl-primary-with-graceful-fallback dispatch: env-key gate → match on fetch_with_firecrawl result → empty-content downgrade (debug log) OR error downgrade (warn log) → fall through to local pdf_extract path. Mirrors Plan 25.2-13's broader dispatcher chain at module-internal scope."
    - "Title-backfill pattern: when Firecrawl returns ExtractionResult with empty title, mutate `r.title = filename_title(url)` BEFORE returning — keeps the URL-last-segment heuristic as the universal title fallback regardless of which backend won"

key-files:
  created: []
  modified:
    - "crates/ironhermes-tools/src/web_extract/pdf.rs (Wave 0 2-line stub → 212-line full PDF handler with two public async fns, fetch_pdf_bytes private helper, filename_title private helper, 4 unit tests)"

key-decisions:
  - "Two public entry points (extract_pdf + extract_pdf_bytes) instead of one fn-with-Option<Vec<u8>> — call sites in Plan 13 are syntactically distinct (UrlClass::Pdf arm vs reroute arm), so two named entries are clearer than a single conditional. Matches the must-haves contract verbatim."
  - "PDF_MAX_BYTES = 50 * 1024 * 1024 hard-coded as module const (not a Config field) — RESEARCH.md threat T5 + Assumption Log A3 give a fixed bound; making it config-tunable would invite operators to disable the DoS mitigation. If a future operator needs a different cap, the change is a one-line const edit + a paired test update."
  - "PDF_EXTRACT_TIMEOUT_SECS = 30 hard-coded for the same reason — RESEARCH.md T5 specifies the bound. The reqwest fetch timeout (timeout_secs in Config.web) IS still operator-configurable because that controls network latency tolerance, not parse-stage runaway."
  - "Bytes-cap check duplicated in extract_pdf_bytes (in addition to fetch_pdf_bytes) — defends against Plan 08's mid-fetch reroute path where bytes arrive from outside this module. Belt-and-braces; cost is one O(1) length comparison."
  - "Three-arm Result destructure on the timeout/spawn_blocking/extract chain produces distinct user-facing error envelopes (pdf_too_large / pdf_text_extraction_timeout / pdf_text_extraction_failed / pdf extract task panicked) — gives operators actionable telemetry instead of a single opaque 'extract failed' string"
  - "filename_title() returns String (not Option<String>) with empty-string sentinel — matches the firecrawl.rs convention (`title.unwrap_or_default()`) and Plan 06 D-07 Option B inline-Markdown-header pattern that already special-cases empty title"

requirements-completed: []  # Plan addresses D-09 + D-18 (decisions, no requirement IDs)

# Metrics
duration: ~17 min
task_count: 1
file_count: 1
completed_date: "2026-05-02"
---

# Phase 25.2 Plan 09: D-09 / D-18 PDF Handler Summary

**One-liner:** Replaces the Wave 0 PDF stub with two public async fns — `extract_pdf(url)` (full pipeline: Firecrawl primary + pdf-extract local fallback) and `extract_pdf_bytes(url, bytes)` (D-03 mid-fetch reroute that re-uses bytes already fetched by Plan 08's local backend) — wrapping `pdf_extract::extract_text_from_mem` in three layers of T-25.2-06 defense (50 MB byte cap, `tokio::task::spawn_blocking`, 30-second `tokio::time::timeout`) plus D-18 SSRF pre-fetch + post-redirect re-validation.

## Performance

- **Duration:** ~17 min (1067s wall clock — single-task plan; clippy gate blocked on pre-existing `ironhermes-core` lints, so verification fell back to `cargo check -p ironhermes-tools` + grep + targeted unit tests)
- **Started:** 2026-05-02T05:24:09Z
- **Completed:** 2026-05-02T05:41:56Z
- **Files Touched:** 1 (`crates/ironhermes-tools/src/web_extract/pdf.rs` — 2 lines → 212 lines)
- **Lines:** +212 / -2

## Tasks Completed

| # | Task | Commit | Result |
|---|------|--------|--------|
| 1 | Implement PDF handler with safety guards | `4ba64ec` | 4/4 unit tests pass; all 9 acceptance greps pass; tools crate `cargo check` clean |

### Commit Details

**`4ba64ec` — feat(25.2-09): implement PDF handler with safety guards (D-09/D-18)**

`crates/ironhermes-tools/src/web_extract/pdf.rs` (+212 / -2):
- Two public async fns: `extract_pdf(url)` (full pipeline) and `extract_pdf_bytes(url, bytes)` (mid-fetch reroute).
- Private `fetch_pdf_bytes(url)` — reqwest GET with `Config.web.timeout_secs`, post-redirect SSRF re-validation, status check, byte cap.
- Private `filename_title(url)` — `url::Url::parse` + last-path-segment + case-insensitive `.pdf` strip.
- Three-layer T-25.2-06 mitigation: `PDF_MAX_BYTES = 50 * 1024 * 1024`, `tokio::task::spawn_blocking`, `tokio::time::timeout(Duration::from_secs(30), …)`.
- D-18 SSRF: `validate_url_async(url).await?` at start of `extract_pdf` AND `if final_url != url { validate_url_async(&final_url).await? }` after the GET in `fetch_pdf_bytes`.
- 4 unit tests:
  * `filename_title_strips_pdf_extension_case_insensitive` — covers `.pdf` and `.PDF` and multi-segment paths
  * `filename_title_empty_for_no_path` — covers root-only URLs
  * `pdf_max_bytes_constant_is_50_mb` — locks the const value as a regression test
  * `extract_pdf_bytes_rejects_oversize` — exercises the cap on a `vec![0u8; PDF_MAX_BYTES + 1]` payload

## Decisions Made

See frontmatter `key-decisions` section above. Six decisions, all module-internal — none change cross-crate or workspace contracts.

## Verification

### Acceptance Greps (all pass)

```
OK pub async fn extract_pdf
OK pub async fn extract_pdf_bytes
OK pdf_extract::extract_text_from_mem
OK spawn_blocking
OK tokio::time::timeout
OK PDF_MAX_BYTES
OK validate_url_async
OK final_url != url
OK 50 * 1024 * 1024 (literal)
```

### Unit Tests (4/4 pass)

```
test web_extract::pdf::tests::pdf_max_bytes_constant_is_50_mb ... ok
test web_extract::pdf::tests::filename_title_empty_for_no_path ... ok
test web_extract::pdf::tests::filename_title_strips_pdf_extension_case_insensitive ... ok
test web_extract::pdf::tests::extract_pdf_bytes_rejects_oversize ... ok

test result: ok. 4 passed; 0 failed; 0 ignored; 0 measured; 267 filtered out
```

### Compile

`cargo check -p ironhermes-tools` finishes clean (only the pre-existing `DEFAULT_SAFE_TOOLS dead_code` warning in `delegate_task.rs` remains, which is unrelated to this plan).

### Threat Model Mitigations

| Threat | Component | Mitigation | Verified |
|--------|-----------|------------|----------|
| T-25.2-06 (Tampering / DoS via PDF parser CVEs / pathological inputs) | `pdf_extract::extract_text_from_mem` | Three layers: 50 MB byte cap (`PDF_MAX_BYTES`); `tokio::task::spawn_blocking`; `tokio::time::timeout(30s)` | `extract_pdf_bytes_rejects_oversize` test pass + 3 grep guards (`spawn_blocking`, `tokio::time::timeout`, `PDF_MAX_BYTES`) all pass |
| T-25.2-03 (SSRF via redirect chain) | `fetch_pdf_bytes` | Pre-fetch `validate_url_async(url)` + post-redirect `if final_url != url { validate_url_async(&final_url).await? }` | Source-grep `validate_url_async` + `final_url != url` both pass; Plan 14 redirect-chain integration test will exercise end-to-end |
| T-25.2-10 (DoS via infinite hang) | reqwest + pdf_extract | reqwest `.timeout(Duration::from_secs(timeout_secs))` from Config.web + outer `tokio::time::timeout(30s)` on extract | Source-grep `tokio::time::timeout` passes; `pdf_text_extraction_timeout` error envelope present in extract_pdf_bytes match arm |
| T-25.2-runtime-stall (block tokio worker) | sync `pdf_extract::extract_text_from_mem` | `tokio::task::spawn_blocking` (RESEARCH.md Pitfall 1) | Source-grep `spawn_blocking` passes |

## Deviations from Plan

**None — plan executed exactly as written.**

The plan provided the full module body verbatim, including all 4 unit tests, all safety constants, the filename_title helper, and the public-fn signatures. Implementation was a straight copy with one cosmetic adjustment (rustfmt pass: `match tokio::time::timeout(…)` formatted across multiple lines for the long signature). All acceptance criteria and verification gates passed first try.

### Auth Gates

None — this plan is pure code; no env-keyed services exercised at execution time. (Firecrawl primary path is dormant code in unit tests; only exercised in Plan 14 wiremock integration tests.)

## Deferred Issues

None — single task completed inside one auto-fix budget cycle.

## Out-of-Scope Discoveries

The plan-mandated `cargo clippy -p ironhermes-tools -- -D warnings` gate cannot pass because of pre-existing clippy errors in `crates/ironhermes-core/src/skills.rs` and 9 sibling files (introduced before Phase 25.2; same 10 errors documented in Plans 25.2-01, 25.2-03, 25.2-04 deferred-items entries). Filtering clippy output for `pdf.rs|web_extract/pdf` returns ZERO matches — the new PDF handler itself is clippy-clean. `cargo check -p ironhermes-tools` succeeds. Per the executor scope-boundary rule, these out-of-scope clippy warnings were NOT modified; they are tracked in `.planning/phases/25.2-web-extract-tools/deferred-items.md` (left untouched per sequential_execution noise-list directive).

## TDD Gate Compliance

N/A — plan frontmatter `type: execute` (not `type: tdd`); no RED/GREEN/REFACTOR cycle required.

## Self-Check: PASSED

- File `crates/ironhermes-tools/src/web_extract/pdf.rs` exists at 212 lines
- Commit `4ba64ec` exists in git log: `feat(25.2-09): implement PDF handler with safety guards (D-09/D-18)`
- All 9 acceptance greps pass
- All 4 unit tests pass
- `cargo check -p ironhermes-tools` clean
