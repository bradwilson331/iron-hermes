---
status: complete
phase: 25.2-web-extract-tools
source:
  - 25.2-00-SUMMARY.md
  - 25.2-01-SUMMARY.md
  - 25.2-02-SUMMARY.md
  - 25.2-03-SUMMARY.md
  - 25.2-04-SUMMARY.md
  - 25.2-05-SUMMARY.md
  - 25.2-06-SUMMARY.md
  - 25.2-07-SUMMARY.md
  - 25.2-08-SUMMARY.md
  - 25.2-09-SUMMARY.md
  - 25.2-10-SUMMARY.md
  - 25.2-11-SUMMARY.md
  - 25.2-12-SUMMARY.md
  - 25.2-13-SUMMARY.md
  - 25.2-14-SUMMARY.md
started: 2026-05-02T23:57:39Z
updated: 2026-05-03T00:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running ironhermes process. Run `ironhermes gateway` from a clean shell (env vars from ~/.ironhermes/.env loaded). The gateway boots without errors, picks up the Telegram bot token, and the web_extract tool registration logs appear in trace output (or no error). No "wizard" launches.
result: pass

### 2. web_extract Tool Surfaces in REPL
expected: Run `ironhermes` (REPL). Type `/tools` (or ask the agent "what tools do you have access to?"). The list includes `web_extract` since the `web` toolset was just enabled.
result: issue
reported: "fail on telegram and REPL chat: /toolset: toolset session handle not configured. and when I ask about tools this is the answer: Here are█"
severity: major

### 3. Single URL Extraction (Live Network)
expected: In REPL or via gateway, ask the agent "Extract the content of https://example.com". Agent calls web_extract, returns a Markdown-formatted block containing the page's heading ("Example Domain") and body text. No raw HTML tags in the output.
result: pass
notes: User also confirmed extraction works well on https://news.ycombinator.com — agent returned a coherent categorized summary of stories with point counts. Bonus evidence the pipeline handles real-world content beyond the minimal test case.

### 4. Multi-URL Extraction in One Call
expected: Ask the agent "Extract content from https://example.com and https://example.org". Agent calls web_extract once with both URLs, returns two extraction blocks (one per URL), each with Source/Title headers and content.
result: pass
notes: Both URLs returned distinct extraction blocks (Source: prefix per URL) in a single call. Confirms parallel fan-out via tokio::spawn + Arc<Semaphore> works as designed in Plan 12.

### 5. PDF URL Routes to PDF Backend
expected: Ask the agent "Extract https://www.w3.org/WAI/ER/tests/xhtml/testfiles/resources/pdf/dummy.pdf" (a tiny public 1-page PDF). Agent returns extracted text from the PDF (e.g., "Dummy PDF file"), not raw bytes or HTML.
result: pass

### 6. YouTube URL Dispatches to Skill
expected: Ask the agent "Extract https://www.youtube.com/watch?v=dQw4w9WgXcQ". Agent recognizes YouTube and either (a) returns content via the youtube-content skill, or (b) returns a clear error explaining the youtube-content skill needs setup (e.g., yt-dlp). It does NOT silently try to scrape the YouTube page as HTML.
result: pass
notes: web_extract did the right thing — YouTube was correctly classified and dispatched to the youtube-content skill, which returned an explicit "skill not installed" error (option b). The agent (LLM, not the tool) then chose to fall back to a separate web reader tool to satisfy the user's request — that's agent-level retry behavior, NOT web_extract silently HTML-scraping YouTube. Tool contract upheld. Follow-up: install the youtube-content skill (`hermes skills add youtube-content`) to get clean dispatch in option (a).

### 7. SSRF Protection Blocks Internal IPs
expected: Ask the agent "Extract content from http://192.168.1.1/admin". Agent receives a clear error stating the URL was blocked (private/internal IP), and does NOT actually attempt to fetch.
result: pass
notes: SSRF guard fired with a clear "private/internal IP address" error and explicitly listed the blocked ranges (192.168.x.x, 10.x.x.x, 172.16-31.x.x). Agent did NOT attempt the fetch. Confirms validate_url_async() pre-fetch SSRF check from Plan 01 + threat T-25.2-03 mitigation across all backends.

### 8. Long Content Gets Summarized
expected: Ask the agent to extract a long article (e.g., a Wikipedia article >5K chars). Output is a tiered summary (Tier 2 single-pass or Tier 3 chunked depending on length), not the raw 50K-char dump. Summary is coherent and references the source.
result: pass
notes: 665 KB Wikipedia article ("2025 in American Television") successfully tier-3 chunked → categorized summary across Politics/Mergers/Sports/Programming/Viewership/Rebrandings sections. No raw HTML, no truncated mid-sentence dumps, content is coherent and referenced to the source. Confirms Plan 11's `route_tiers` + `summarize_chunked` pipeline (semaphore-bounded fan-out + final synthesis) works end-to-end on real-world content well above the Tier 3 threshold (500K chars).

### 9. Secret in URL is Redacted in Output/Errors
expected: Ask the agent to extract from a URL containing what looks like a secret, e.g., "https://example.com/?api_key=sk-or-v1-fakekeyabc123". The output (whether success or error) does NOT echo the literal `sk-or-v1-fakekeyabc123` token — it should be redacted to `***` or similar.
result: issue
reported: "The agent's safety warning literally repeated the API key token: '⚠️ Hold on! That URL contains what appears to be an API key (sk-or-v1-fakekeyabc123) in the query string.'"
severity: major

### 10. Telegram Gateway End-to-End
expected: From your Telegram chat with the bot (chat ID 7018949547 in your whitelist), send the message "Extract https://example.com and tell me what it says". The bot responds with the extracted content within a reasonable time (no timeout, no crash). The full pipeline (Telegram → gateway → agent → web_extract → response) works.
result: pass
notes: Full Telegram → gateway → agent → web_extract → reply pipeline works end-to-end. Confirms Plan 14's CLI wireup of register_web_extract_tool in run_gateway is functional, and the AnyClientSummarizationHandle cascade resolves correctly under live network + LLM load.

## Summary

total: 10
passed: 8
issues: 2
pending: 0
skipped: 0

## Gaps

- truth: "REPL `/tools` (or `/toolset`) and agent self-introspection (\"what tools do you have access to?\") should list `web_extract` after the `web` toolset is enabled — verifies the tool is registered AND discoverable to the user."
  status: failed
  reason: "User reported (REPL + Telegram, both channels): `/toolset` slash command errors with 'toolset session handle not configured'. Asking the agent 'what tools do you have' truncates mid-response at 'Here are█'."
  severity: major
  test: 2
  artifacts: []
  missing: []

- truth: "Secrets embedded in user-provided URLs (api_key, token, secret, etc.) must NEVER appear in cleartext in the agent's response — the sanitizer (Plan 04 `SECRET_URL_PATTERNS` + `contains_secret`) was supposed to detect such URLs before they propagated. Whether the tool succeeds, fails, or the agent pre-emptively refuses, the literal secret token must be redacted to `***`."
  status: failed
  reason: "User reported the agent's safety warning literally echoed the API key token: '⚠️ Hold on! That URL contains what appears to be an API key (sk-or-v1-fakekeyabc123) in the query string.' The sanitizer's intent is defeated when the agent's wrapper text leaks the very secret it's warning about. Likely root cause: web_extract was never called — the agent (LLM) intercepted the URL prompt-side and refused, so Plan 04's sanitize pass never ran. The fix lives at agent-prompt level (system prompt or tool-input pre-filter), not in the web_extract tool itself."
  severity: major
  test: 9
  artifacts: []
  missing: []
