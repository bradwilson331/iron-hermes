---
phase: 25.2-web-extract-tools
plan: 10
subsystem: tools/web-extract
tags: [youtube, skill-dispatch, shell-out, tokio-process, d-10, wave-3]

dependency_graph:
  requires:
    - phase: 25.2-00
      provides: "crates/ironhermes-tools/src/web_extract/youtube.rs (Wave 0 stub) + ExtractionResult struct exported from web_extract crate root"
    - phase: 25.2-06
      provides: "ExtractionResult { url, title, content, error: Option<String> } envelope shape used as the return type"
  provides:
    - "pub async fn extract_youtube(url: &str, registry: &Arc<SkillRegistry>) -> Result<ExtractionResult> in web_extract::youtube — D-10 entry point"
    - "fn first_h1(markdown: &str) -> String — pulls the first `# ` heading as a plain-text title; empty string when none found"
    - "Distinct error envelopes per failure mode: youtube_skill_not_installed, youtube_skill_invalid_path, youtube_skill_helper_missing, youtube_skill_helper_spawn_failed, youtube_skill_helper_failed (with last stderr line), youtube_skill_output_not_utf8, youtube_skill_returned_empty_output"
  affects:
    - "Plan 25.2-13 (WebExtractTool dispatch) — UrlClass::YouTube arm calls extract_youtube(url, &skill_registry); returned Err is mapped into ExtractionResult.error per D-02"
    - "Plan 25.2-14 (integration tests) — wiremock-style web_extract_youtube_url_dispatches_to_skill builds a mock SkillRegistry pointing at a stub script returning canned Markdown"

tech-stack:
  added: []  # tokio (full feature) + tracing + anyhow already workspace deps in ironhermes-tools
  patterns:
    - "Skill dispatch by shell-out: registry.find(name) → record.path.parent().join('scripts/...') → tokio::process::Command — Phase 19 skills runtime exposes NO programmatic execute API (RESEARCH.md target #4 + Open Q §2), so the canonical extension point is the on-disk skill helper script. Operators can replace scripts/fetch_transcript.py without rebuilding the binary."
    - "Argv (not shell) command construction for T-25.2-shell-injection mitigation: tokio::process::Command::new('python3').arg(&script_path).arg(url).arg('--text-only') — args go to execve directly; no shell interpretation, no format!(...url) interpolation."
    - "Last-stderr-line failure envelope: stderr.lines().last().unwrap_or('(no stderr output)') — typical Python traceback's last line is the exception message (actionable telemetry) without leaking full stack traces."
    - "Const-as-single-source-of-truth dispatch key: YOUTUBE_SKILL_NAME constant + assert_eq!/assert_ne! pair test that locks both the canonical hyphenated form AND blocks regression to the underscored PRD typo (T-25.2-skill-name-mismatch mitigation)."
    - "Plain-line Markdown title parser: lines().find(|l| l.starts_with('# ')).map(|l| l.trim_start_matches('# ').trim().to_string()).unwrap_or_default() — skips ## sub-headings, returns empty for no-heading input. 3 unit tests cover all branches."

key-files:
  created: []
  modified:
    - "crates/ironhermes-tools/src/web_extract/youtube.rs (Wave 0 2-line stub → 152-line full YouTube dispatcher with extract_youtube async fn, first_h1 helper, 5 unit tests)"
    - ".planning/phases/25.2-web-extract-tools/deferred-items.md (appended Plan 25.2-10 entry documenting the same 10 pre-existing ironhermes-core clippy errors that Plans 25.2-01/03/04 already documented)"

key-decisions:
  - "Shell-out via tokio::process::Command — the documented CONTEXT.md D-10 wording 'via the Phase 19 skills framework' is satisfied by treating the skill's on-disk layout (scripts/fetch_transcript.py) as the framework's canonical extension point, since the actual Phase 19 SkillRegistry API exposes only find/read_content/list/load (content-only). RESEARCH.md target #4 + Open Question §2 + the planner explicitly tagged this plan 'CRITICAL DIVERGENCE FROM CONTEXT.md per planner spec'; trust the planner over CONTEXT.md when they conflict."
  - "Skill dispatch key HYPHENATED ('youtube-content', not 'youtube_content') — verified against skills/media/youtube-content/SKILL.md frontmatter `name: youtube-content`. The PRD's underscore form ('youtube_content') is a copy-paste error; the directory name is hyphenated, the frontmatter is hyphenated, and SkillRegistry::find() does case-insensitive but NOT hyphen/underscore-normalising lookup. Locked by youtube_skill_name_is_hyphenated test (assert_eq + assert_ne pair)."
  - "Helper script invoked with --text-only (not --timestamps) — keeps stdout as a single Markdown body suitable for the first_h1 title extraction. The skill supports --timestamps but adds [HH:MM:SS] prefixes that would clutter the LLM-facing content; the LLM can re-fetch with timestamps via the SkillsTool if it needs them."
  - "Distinct error envelopes per failure mode (skill missing / invalid path / script missing / spawn failed / non-zero exit / non-utf8 output / empty output) — not a single opaque 'youtube_failed'. Plan 13 dispatcher pipes Err.to_string() into ExtractionResult.error so the LLM sees actionable telemetry per failure surface."
  - "first_h1 returns String (not Option<String>) with empty-string sentinel — matches Plan 25.2-09's filename_title pattern and Plan 25.2-06's firecrawl.rs title-empty convention; the ExtractionResult struct already carries title as String (D-07 inline-Markdown-header pattern)."
  - "Real skill-dispatch behavior is exercised by Plan 25.2-14 wiremock test — the unit tests in this plan only cover pure helpers (const-name guard + first_h1 parser). Integration coverage is Plan 14's responsibility per the plan's own action note."

requirements-completed: []  # Plan addresses D-10 only (decisions, no requirement IDs)

# Metrics
duration: ~22 min
task_count: 1
file_count: 1
completed_date: "2026-05-02"
---

# Phase 25.2 Plan 10: D-10 YouTube Dispatch Summary

**One-liner:** Replaces the Wave 0 YouTube stub with a single-file dispatcher that looks up the in-tree `youtube-content` skill via `SkillRegistry::find` (HYPHENATED name verified against SKILL.md frontmatter), resolves `<skill_dir>/scripts/fetch_transcript.py`, and shells out via `tokio::process::Command` (URL passed as a separate argv element — no shell interpretation, no format-string interpolation, T-25.2-shell-injection mitigated by construction) — because the Phase 19 skills runtime has no programmatic execute API, the on-disk helper script IS the framework's canonical extension point.

## Performance

- **Duration:** ~22 min wall-clock
- **Started:** 2026-05-02T05:46:12Z
- **Completed:** 2026-05-02T06:08:32Z
- **Files Touched:** 1 source (`crates/ironhermes-tools/src/web_extract/youtube.rs` — 2 lines → 152 lines) + 1 deferred-items doc append
- **Lines:** +150 / -2 (source); +28 (deferred-items append)

## Tasks Completed

| # | Task | Commit | Result |
|---|------|--------|--------|
| 1 | Implement YouTube dispatch via skill helper script shell-out | `162fd24` | 5/5 unit tests pass; all substantive acceptance greps pass; tools crate clippy clean for the new file (pre-existing `ironhermes-core` lints out-of-scope, deferred per established phase pattern) |

### Commit Details

**`162fd24` — feat(25.2-10): implement YouTube dispatch via skill helper script shell-out**

`crates/ironhermes-tools/src/web_extract/youtube.rs` (+150 / -2):
- `pub async fn extract_youtube(url: &str, registry: &Arc<SkillRegistry>) -> Result<ExtractionResult>` — the D-10 entry point.
- `const YOUTUBE_SKILL_NAME: &str = "youtube-content"` — hyphenated single-source-of-truth dispatch key.
- `const HELPER_SCRIPT_RELPATH: &str = "scripts/fetch_transcript.py"` — relative path inside the skill dir.
- Private `fn first_h1(markdown: &str) -> String` — first-`# `-heading title parser.
- Five-step pipeline: skill lookup → skill_dir parent resolve → script existence check → `tokio::process::Command::new("python3").arg(&script_path).arg(url).arg("--text-only").output().await` → utf8 + empty-output guard → first_h1 title extraction → `Ok(ExtractionResult)`.
- 5 unit tests (`cargo test -p ironhermes-tools --lib web_extract::youtube` — 5/5 pass in 0.00s):
  * `youtube_skill_name_is_hyphenated` — locks `assert_eq!(YOUTUBE_SKILL_NAME, "youtube-content")` AND `assert_ne!(YOUTUBE_SKILL_NAME, "youtube_content")` together; T-25.2-skill-name-mismatch mitigation.
  * `helper_script_relpath_correct` — locks the helper-script relative path const.
  * `first_h1_pulls_title` — Markdown with `# Title\nDescription...\n## Transcript\n[00:00] ...` → `"My Video Title"`.
  * `first_h1_returns_empty_when_no_heading` — plain-text input returns empty string.
  * `first_h1_skips_subheadings` — `## Subhead\n# Title` returns `"Title"` (NOT `"Subhead"`).

## Decisions Made

See frontmatter `key-decisions` section above. Six decisions:
1. Shell-out is the canonical Phase 19 extension point (no programmatic API exists).
2. Dispatch key HYPHENATED — guarded by an `assert_eq!` + `assert_ne!` test pair.
3. `--text-only` flag chosen over `--timestamps` for clean Markdown output.
4. Distinct error envelopes per failure mode for actionable LLM telemetry.
5. `first_h1` returns `String` with empty sentinel (matches sibling helpers).
6. Real skill-dispatch coverage deferred to Plan 14 wiremock integration test.

## Verification

### Acceptance Greps

| Grep | Result |
|------|--------|
| `pub async fn extract_youtube` | PASS |
| `"youtube-content"` (HYPHENATED const) | PASS (3 matches: const + 2 doc lines) |
| `tokio::process::Command` | PASS |
| `scripts/fetch_transcript.py` | PASS |
| `use ironhermes_core::SkillRegistry` | PASS |
| `! grep -q '"youtube_content"'` (no underscored variant in dispatch path) | **Substantively PASS** — see Deviations section below |

### Unit Tests (5/5 pass)

```
test web_extract::youtube::tests::youtube_skill_name_is_hyphenated ... ok
test web_extract::youtube::tests::helper_script_relpath_correct ... ok
test web_extract::youtube::tests::first_h1_pulls_title ... ok
test web_extract::youtube::tests::first_h1_skips_subheadings ... ok
test web_extract::youtube::tests::first_h1_returns_empty_when_no_heading ... ok

test result: ok. 5 passed; 0 failed; 0 ignored; 0 measured; 271 filtered out
```

### Compile / Clippy

- `cargo test -p ironhermes-tools --lib web_extract::youtube` — clean compile, all 5 tests pass.
- `cargo clippy -p ironhermes-tools -- -D warnings` — fails with **10 errors all in `crates/ironhermes-core/`** (handlers.rs, typo.rs, config.rs, memory_store.rs, skills.rs); ZERO clippy errors in `youtube.rs` or any `web_extract` path. These 10 errors pre-exist Plan 25.2-10 (last touched in commit `82d9cf7` = Plan 25.2-02 which only modified `config.rs` to add `ExtractConfig`); they are documented in `deferred-items.md` under prior Plan 25.2-01/03/04 entries and now also under a Plan 25.2-10 entry. Per the executor `<scope_boundary>` rule, out-of-scope pre-existing lints in unrelated crates are NOT fixed; they are logged for a future workspace clippy hygiene pass.

### Threat Model Mitigations

| Threat | Component | Mitigation | Verified |
|--------|-----------|------------|----------|
| T-25.2-08 (RCE via tampered helper script) | `scripts/fetch_transcript.py` | **accept (delegated)** — operator-controlled in-tree script; future Phase 19 trust-tier system (SKILL-09 pending) is the canonical mitigation surface | Documented in plan threat register; tool runs whatever script ships at the resolved path |
| T-25.2-shell-injection (URL injection into command) | `tokio::process::Command` arg construction | URL passed as a SEPARATE `arg(url)`, NOT interpolated into a shell string. `tokio::process::Command` does NOT invoke a shell — args go directly to execve | Source-grep: zero `sh -c` occurrences; no `format!(...url)` into `arg()`; URL appears only as `.arg(url)` literal |
| T-25.2-skill-name-mismatch (PRD typo lets attacker substitute their own skill) | `YOUTUBE_SKILL_NAME` const | `youtube_skill_name_is_hyphenated` test asserts `YOUTUBE_SKILL_NAME == "youtube-content"` AND `!= "youtube_content"`. Const is single source of truth | Test passes; verified via `cargo test web_extract::youtube::tests::youtube_skill_name_is_hyphenated` |
| T-25.2-stderr-leak (sensitive data in stderr surfaced to LLM) | last stderr line in error message | **accept** — only the LAST line of stderr included; typical Python traceback's last line is the exception message, not credentials. Helper script is operator-controlled | Source-grep: `stderr.lines().last().unwrap_or("(no stderr output)")` is the only stderr surface |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Plan acceptance grep is internally inconsistent with prescribed test body**
- **Found during:** Task 1 verification step
- **Issue:** Plan line 241 specifies the acceptance grep `! grep -q '"youtube_content"' crates/ironhermes-tools/src/web_extract/youtube.rs` (must NOT contain underscored name). However plan line 204 prescribes the test body `assert_ne!(YOUTUBE_SKILL_NAME, "youtube_content");` which deliberately includes the underscored literal as a regression guard. The plan's own grep would fail against its own prescribed source code.
- **Fix:** Honored the substantive intent (production const HYPHENATED; dispatch path HYPHENATED; underscored variant guarded against by an explicit `assert_ne!` test). The only occurrence of `"youtube_content"` in the file is the negative assertion at line 122; every other reference uses the hyphenated form. This is documented inline in `deferred-items.md` as a plan-text-vs-plan-test inconsistency rather than an implementation deviation.
- **Files modified:** None (the implementation matches the plan's prescribed body verbatim)
- **Commit:** N/A

### Out of Scope (deferred)

- 10 pre-existing clippy errors in `crates/ironhermes-core/{commands/handlers.rs, commands/typo.rs, config.rs, memory_store.rs, skills.rs}`. Logged to `.planning/phases/25.2-web-extract-tools/deferred-items.md` (4th time — same set as Plans 01, 03, 04). Per `<scope_boundary>` rule, these are NOT modified by this plan.

### Auth Gates

None — pure code; no env-keyed services exercised at execution time. The skill helper script (when actually invoked at runtime) reads `YOUTUBE_DATA_API_KEY` if set but the Plan 14 wiremock test will mock the helper, so no real auth gate fires during phase-25.2 execution.

## Deferred Issues

None — single task completed inside one auto-fix budget cycle (zero auto-fix attempts on the implementation itself).

## Out-of-Scope Discoveries

The plan-mandated `cargo clippy -p ironhermes-tools -- -D warnings` gate cannot pass workspace-wide because of the same 10 pre-existing clippy errors in `crates/ironhermes-core/` that have blocked the same gate in Plans 25.2-01, 25.2-03, 25.2-04, 25.2-09. Filtering clippy output for `youtube|web_extract` returns ZERO matches — the new YouTube dispatch code itself is clippy-clean. `cargo test -p ironhermes-tools --lib web_extract::youtube` succeeds with 5/5 tests passing in 0.00s. Per the executor scope-boundary rule, these out-of-scope clippy errors were NOT modified; they are tracked in `deferred-items.md` under the new Plan 25.2-10 entry.

## TDD Gate Compliance

N/A — plan frontmatter `type: execute` (not `type: tdd`); no RED/GREEN/REFACTOR cycle required.

## Self-Check: PASSED

- File `crates/ironhermes-tools/src/web_extract/youtube.rs` exists at 152 lines (verified via `wc -l`).
- Commit `162fd24` exists in git log: `feat(25.2-10): implement YouTube dispatch via skill helper script shell-out`.
- All 5 unit tests pass (`cargo test -p ironhermes-tools --lib web_extract::youtube`).
- All substantive acceptance greps pass; the one inconsistent grep (no underscored name) is satisfied substantively (production const + dispatch path are HYPHENATED; only occurrence is the test guard `assert_ne!`).
- `deferred-items.md` updated with Plan 25.2-10 entry following the established phase pattern.
