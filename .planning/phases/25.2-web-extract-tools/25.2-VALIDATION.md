---
phase: 25.2
slug: web-extract-tools
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-01
---

# Phase 25.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (Rust 2021, workspace) |
| **Config file** | `Cargo.toml` (workspace) + per-crate `Cargo.toml` |
| **Quick run command** | `cargo test -p ironhermes-tools web_extract --lib --no-fail-fast` |
| **Full suite command** | `cargo test --workspace --no-fail-fast && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --all -- --check` |
| **Estimated runtime** | ~120 seconds (cold) / ~15 seconds (warm) |

---

## Sampling Rate

- **After every task commit:** Run `cargo check -p ironhermes-tools` + module-scoped `cargo test -p ironhermes-tools <task_module> --lib`
- **After every plan wave:** Run `cargo test -p ironhermes-tools --no-fail-fast` (covers all 8 wiremock integration tests + schema-availability test)
- **Before `/gsd-verify-work`:** Full suite must be green (workspace tests + clippy + fmt)
- **Max feedback latency:** 30 seconds (warm) / 120 seconds (cold)

---

## Per-Task Verification Map

> The planner will fill this table — one row per task it generates. The waves and plan IDs below are placeholders pending plan-phase output. The 8 wiremock integration tests from CONTEXT.md D-26 plus the D-27 schema-availability test MUST appear here.

| Task ID | Plan | Wave | Decision Ref | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|--------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 25.2-01-01 | 01-web-local-refactor | 1 | D-04 | T-25.2-01 (cache exhaustion via giant pages) | `extract_content_local` continues to truncate at config.web.max_content_chars after move | unit | `cargo test -p ironhermes-tools web_local::tests` | ❌ W0 | ⬜ pending |
| 25.2-02-01 | 02-config-extract-block | 1 | D-22 | — | `ExtractConfig` defaults load when block omitted | unit | `cargo test -p ironhermes-core config::extract_config_defaults` | ❌ W0 | ⬜ pending |
| 25.2-03-01 | 03-summarization-handle | 1 | D-13 | T-25.2-04 (aux-LLM cost amplification) | `SummarizationClientHandle` returns None when no aux configured (cascade fall-through) | unit | `cargo test -p ironhermes-core provider::summarization_handle` | ❌ W0 | ⬜ pending |
| 25.2-04-01 | 04-sanitize | 2 | D-08 / D-19 | T-25.2-02 (secret leak via URL) / T-25.2-05 (Markdown injection) | base64 stripped, secret URLs rejected with `error: "url_contains_secret"` before network | unit | `cargo test -p ironhermes-tools web_extract::sanitize::tests` | ❌ W0 | ⬜ pending |
| 25.2-05-01 | 05-dispatch-classifier | 2 | D-03 | T-25.2-01 | URL classification routes YouTube/PDF/default before any network call; mid-fetch reroute on `Content-Type: application/pdf` | unit | `cargo test -p ironhermes-tools web_extract::dispatch::tests` | ❌ W0 | ⬜ pending |
| 25.2-06-01 | 06-backends-firecrawl | 2 | D-04 / D-05 / D-06 | T-25.2-01 / T-25.2-03 (SSRF) | Firecrawl-first; on failure returns truncated 5K content NOT generic error; SSRF validated pre-fetch | integration | `cargo test -p ironhermes-tools --test web_extract_integration firecrawl_path` | ❌ W0 | ⬜ pending |
| 25.2-06-02 | 06-backends-exa | 2 | D-06 | T-25.2-03 | Exa hand-rolled `reqwest` client; response normalized to Markdown; auth header `x-api-key` | integration | `cargo test -p ironhermes-tools --test web_extract_integration exa_path` | ❌ W0 | ⬜ pending |
| 25.2-06-03 | 06-backends-tavily | 2 | D-06 | T-25.2-03 | Tavily `/extract` hand-rolled `reqwest`; auth via JSON body `api_key`; Markdown normalization | integration | `cargo test -p ironhermes-tools --test web_extract_integration tavily_path` | ❌ W0 | ⬜ pending |
| 25.2-07-01 | 07-pdf-handler | 3 | D-09 | T-25.2-06 (PDF parser CVEs) | `pdf-extract` 0.10 (workspace pin, NOT 0.7) byte→text path; SSRF validated; filename → title fallback | integration | `cargo test -p ironhermes-tools --test web_extract_integration pdf_routes_to_pdf_backend` | ❌ W0 | ⬜ pending |
| 25.2-08-01 | 08-youtube-dispatch | 3 | D-10 | — | YouTube URL dispatched to `youtube-content` (HYPHENATED) skill via shell-out helper (skills runtime has no execute API); first `# ` heading → title | integration | `cargo test -p ironhermes-tools --test web_extract_integration youtube_dispatches_to_skill` | ❌ W0 | ⬜ pending |
| 25.2-09-01 | 09-summary-tiers | 3 | D-11 / D-12 | T-25.2-04 | Tier 1 raw <5K; tier 2 single aux; tier 3 chunked+synthesis; tier 4 refuse w/ `error: "content_too_large"`; `use_llm_processing: false` short-circuits all aux tiers | integration | `cargo test -p ironhermes-tools --test web_extract_integration summary_tier_thresholds use_llm_processing_false` | ❌ W0 | ⬜ pending |
| 25.2-10-01 | 10-summary-chunked | 3 | D-15 / D-16 / D-17 | T-25.2-04 | Single Semaphore (default 4 permits) covers BOTH multi-URL AND chunk-summary parallelism; one synthesis call over all chunks with `---\n[Chunk N/M]\n` markers | integration | `cargo test -p ironhermes-tools --test web_extract_integration semaphore_bounds_concurrency` | ❌ W0 | ⬜ pending |
| 25.2-11-01 | 11-tool-impl | 4 | D-01 / D-02 / D-20 | — | `Tool::toolset()` returns `"web"`; `is_available()` returns `true` unconditionally (D-27); per-URL errors populate `ExtractionResult.error` (partial-success) | integration | `cargo test -p ironhermes-tools --test web_extract_integration multi_url_partial_failure_returns_per_url_errors` | ❌ W0 | ⬜ pending |
| 25.2-12-01 | 12-cascade-second-consumer | 4 | D-13 | T-25.2-04 | `auxiliary.summary: { provider, model }` routes summary calls to aux server (Phase 26 PROV-06 second-consumer validation) | integration | `cargo test -p ironhermes-tools --test web_extract_integration summarization_role_resolves_via_phase26_cascade` | ❌ W0 | ⬜ pending |
| 25.2-13-01 | 13-registry | 4 | D-20 / D-27 | — | `register_defaults()` adds `WebExtractTool` in `web` toolset block; schema present in `get_definitions()` regardless of env-var state | unit | `cargo test -p ironhermes-tools registry::web_extract_excluded_when_no_backend_available` | ❌ W0 | ⬜ pending |
| 25.2-14-01 | 14-secret-redact | 4 | D-19 | T-25.2-02 | `?token=`, `api_key=`, `bearer ` etc. → `error: "url_contains_secret"` BEFORE any network call | integration | `cargo test -p ironhermes-tools --test web_extract_integration secret_in_url_redacted` | ❌ W0 | ⬜ pending |
| 25.2-15-01 | 15-roadmap-update | 4 | D-28 | — | ROADMAP.md `### Phase 25.2` goal text replaces `[Urgent work - to be planned]` with the new goal | unit | `grep -q 'A single \\\`web_extract\\\` tool' .planning/ROADMAP.md` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Threat IDs (T-25.2-NN) are defined in the planner's `<threat_model>` block per step 5.55 ASVS-L1 contract.*

---

## Wave 0 Requirements

- [ ] `crates/ironhermes-tools/src/web_extract.rs` — top-level module file (does not exist yet)
- [ ] `crates/ironhermes-tools/src/web_local.rs` — extracted shared HTML→Markdown helpers (D-04 refactor target)
- [ ] `crates/ironhermes-tools/tests/web_extract_integration.rs` — wiremock integration test file (8 tests per D-26 + D-27)
- [ ] `pdf-extract` — confirm workspace pin is `0.10` (NOT `0.7` as CONTEXT.md states); update CONTEXT.md D-09/D-24 if planner finds a divergence
- [ ] `crates/ironhermes-core/src/config.rs` — `ExtractConfig` struct (D-22, all fields `#[serde(default)]`)
- [ ] `crates/ironhermes-core/src/provider.rs` — add `SummarizationClientHandle` trait (mirrors Phase 25.1 `VisionClientHandle` to avoid `tools→agent` circular dep — see RESEARCH.md target #5)
- [ ] `crates/ironhermes-tools/src/lib.rs` — `pub mod web_extract; pub mod web_local;`
- [ ] `crates/ironhermes-tools/src/registry.rs` — `register_defaults()` registers `WebExtractTool` in `web` toolset block

*The planner MUST emit a Wave 0 task that creates each missing file BEFORE any later wave references it (Nyquist Wave-0 prerequisite).*

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| Live Exa.ai backend returns Markdown when `EXA_API_KEY` is set | D-06 | Real network call; Exa free tier rate-limited; CI does not have keys | `EXA_API_KEY=$(security find-generic-password ...) cargo test --ignored exa_live_smoke -- --nocapture` |
| Live Tavily `/extract` returns Markdown when `TAVILY_API_KEY` is set | D-06 | Real network call; Tavily free tier rate-limited; CI does not have keys | `TAVILY_API_KEY=... cargo test --ignored tavily_live_smoke -- --nocapture` |
| Live Firecrawl PDF extraction returns Markdown for an arXiv URL | D-09 | Real network call to Firecrawl + arXiv | `FIRECRAWL_API_KEY=... cargo test --ignored firecrawl_pdf_live_smoke -- --nocapture` |
| YouTube `youtube-content` skill end-to-end with a real video transcript | D-10 | Skill shells out to a Python helper that hits YouTube; flaky offline | `cargo test --ignored youtube_skill_live_smoke -- --nocapture` (requires network + python deps) |
| Tier 4 refuse banner surfaces correctly to the LLM in a real `/run` session | D-11 | Requires LLM-in-loop UAT to observe `error: "content_too_large"` rendering | UAT script: load a >2M-char URL via `web_extract`, confirm the LLM sees the structured refuse envelope and abandons the URL |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (file creation tasks gate later waves)
- [ ] No watch-mode flags (no `cargo watch` / `--watch` in any test command)
- [ ] Feedback latency < 30 seconds (warm)
- [ ] `nyquist_compliant: true` set in frontmatter
- [ ] All 8 D-26 wiremock integration tests present in the per-task table
- [ ] D-27 schema-availability test present
- [ ] `pdf-extract` version drift (CONTEXT says 0.7, workspace has 0.10) flagged for plan-phase resolution

**Approval:** pending
