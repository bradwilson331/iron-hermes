---
phase: 25.2-web-extract-tools
verified: 2026-05-02T23:55:00Z
status: passed
score: 16/16 must-haves verified
overrides_applied: 0
re_verification: true
previous_status: gaps_found
previous_score: 14/16
gaps_closed:
  - "RegistryToolsetSession production impl exists in crates/ironhermes-tools/src/toolset_session.rs and is wired into build_cmd_ctx so /toolset works in REPL + Telegram (UAT Issue 2 / Symptom 1 closure)"
  - "redact_secrets_in_url primitive exists, ExtractionResult.url echoes the redacted URL, and DEFAULT_AGENT_IDENTITY contains a Secret handling rule (UAT Issue 9 closure)"
gaps_remaining: []
regressions: []
deferred:
  - truth: "UAT Issue 2 / Symptom 2: LLM truncation at 'Here are█' fully diagnosed end-to-end"
    addressed_in: "Future /gsd-debug session"
    evidence: "25.2-15-PLAN.md and 25.2-UAT.md root_cause analysis explicitly classify Symptom 2 as INDEPENDENT of the handle wireup. Confirming requires runtime trace of finish_reason + delta.tool_calls in a separate diagnose-only session. Plan 15 SUMMARY 'Next Phase Readiness' logs this as the follow-up."
  - truth: "youtube.rs:81 stderr-leak surface (yt-dlp may echo URL in its own stderr) is scrubbed"
    addressed_in: "Future /gsd-debug session"
    evidence: "25.2-16-PLAN.md B-1 audit explicitly DROPPED youtube.rs from files_modified — different fix shape (free-text stderr scrub vs URL transform). 25.2-16-SUMMARY.md 'Next Phase Readiness' logs the follow-up: '/gsd-debug audit yt-dlp helper stderr for URL/secret leakage at youtube.rs:81'."
human_verification:
  - test: "Manual UAT replay of Test 2: /toolset in REPL + Telegram"
    expected: "Populated 3-member web entry (web_search, web_read, web_extract); NO 'toolset session handle not configured' fallback string"
    why_human: "End-to-end UI verification across two transports (REPL + Telegram bot). Programmatic checks confirm the structural fix is in code; live replay confirms the user-visible behavior."
  - test: "Manual UAT replay of Test 9: agent presented with `https://example.com/?api_key=sk-or-v1-fakekeyabc123`"
    expected: "The literal token `sk-or-v1-fakekeyabc123` does NOT appear in any agent response, refusal, error envelope, or tracing log line. Either the agent calls web_extract (which returns a redacted error envelope) OR refuses with paraphrased prose per the new system-prompt rule."
    why_human: "LLM behavior under the new 'Secret handling' system-prompt rule is probabilistic and must be observed in a real conversation. Defense-in-depth via tool-side redaction is structurally verified; prompt-side compliance requires live observation."
---

# Phase 25.2: web-extract-tools — Re-Verification Report

**Phase Goal:** A single `web_extract` tool that unifies multi-URL HTML/PDF/YouTube extraction behind one dispatcher, normalizes everything to Markdown, runs a tiered LLM summarization pipeline (5K direct / 5K-500K single-pass / 500K-2M chunked synthesis / >2M refuse) with aux-LLM routing via Phase 26 `resolve_role("summarization")` cascade, and ships in the existing `web` toolset.

**Verified:** 2026-05-02T23:55:00Z
**Status:** passed (with human verification deferred to UAT replay)
**Re-verification:** Yes — after gap closure (wave-5 merge into develop)

---

## Re-Verification Outcome (read this first)

The previous verification report (timestamp `2026-05-02T23:00:00Z`) flagged **2 gaps**: the wave-5 implementation commits for plans 15 + 16 had been authored but were sitting on worktree branches and were not on `develop`. The mechanical claims in that report were correct AT THE TIME.

**Develop has since been updated:**

```
$ git rev-parse HEAD develop
2c72380d34cb4e4221650d936f588a3894e09782   # HEAD
2c72380d34cb4e4221650d936f588a3894e09782   # develop (HEAD == develop)

$ git log develop --oneline | head -12
2c72380 chore(25.2): merge wave-5 gap-closure (plans 15 + 16) into develop
1483770 docs(25.2): mark phase 25.2 complete in STATE.md after gap-closure wave 5
e363985 chore: merge executor worktree (worktree-agent-a23479864b608285c) — plan 25.2-15 ToolsetSessionHandle wireup
66b1e21 docs(25.2-16): complete UAT Issue 9 gap-closure plan
8224549 docs(25.2-15): complete toolset session handle wireup plan summary
b64c067 fix(25.2-16): add Secret handling rule to DEFAULT_AGENT_IDENTITY
3dda29d fix(25.2-15): add web_extract to CLI toolset_members_map web entry
22b8098 feat(25.2-15): wire RegistryToolsetSession into all three CLI entry points
9e237fb fix(25.2-16): redact secrets in ExtractionResult.url echo and warn! log sites
f116e77 feat(25.2-16): implement redact_secrets_in_url primitive
37afb6e feat(25.2-15): add RegistryToolsetSession production ToolsetSessionHandle impl
8513d45 test(25.2-16): add failing tests for redact_secrets_in_url
```

All 10 wave-5 commits (8513d45, f116e77, 9e237fb, b64c067, 66b1e21 — Plan 16; 37afb6e, 22b8098, 3dda29d, 8224549, e363985 — Plan 15) are now in develop's first-parent ancestry through merge commit `2c72380`. Both gaps from the prior report are CLOSED.

---

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                                                                                       | Status     | Evidence                                                                                                                                                                                                                                                                                                                                |
| -- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1  | A single `web_extract` tool unifies multi-URL HTML/PDF/YouTube extraction behind one dispatcher                                                              | VERIFIED   | `crates/ironhermes-tools/src/web_extract.rs` defines `pub struct WebExtractTool` + `impl Tool`. Per-URL pipeline: D-19 secret check → classify_url → backend chain → sanitize → tier route. UAT Tests 3-4 passed.                                                                                                                                |
| 2  | Multi-URL fan-out is parallel and bounded                                                                                                                   | VERIFIED   | UAT Test 4 passed. Plan 11 SUMMARY confirms `tokio::spawn + Arc<Semaphore>` fan-out.                                                                                                                                                                                                                                                            |
| 3  | Everything normalizes to Markdown                                                                                                                           | VERIFIED   | UAT Test 3 passed (Markdown returned, no HTML tags). UAT Test 5 PDF returned text. UAT Test 8 65 KB Wikipedia → categorized Markdown summary.                                                                                                                                                                                                   |
| 4  | Tiered LLM summarization pipeline (5K / 5K-500K / 500K-2M / >2M refuse)                                                                                       | VERIFIED   | `summary/tiers.rs` and `summary/chunked.rs` exist. `route_tiers` invoked at web_extract.rs:269. UAT Test 8 confirmed Tier-3 chunked path on a 665 KB article.                                                                                                                                                                                   |
| 5  | Aux-LLM routing via Phase 26 `resolve_role("summarization")` cascade                                                                                         | VERIFIED   | `crates/ironhermes-agent/src/any_client.rs:256` defines `AnyClientSummarizationHandle`. Plan 12 integration test `web_extract_summarization_role_resolves_via_phase26_cascade` exists.                                                                                                                                                          |
| 6  | Ships in existing `web` toolset                                                                                                                             | VERIFIED   | `WebExtractTool::toolset()` returns `"web"`. `register_defaults` registers it. Both the slash UI map (toolset_session.rs `members_map`) AND the CLI subcommand map (toolset_cmd.rs:242) now list `web_extract` as the 3rd member. Discoverability via `/toolset` is now wired (truth 12 closure).                                                |
| 7  | Extends `web_read` patterns                                                                                                                                 | VERIFIED   | Plan 01 SUMMARY confirms `web_local` refactor extracted shared HTML→Markdown helpers from web_read.rs into `crates/ironhermes-tools/src/web_local.rs`.                                                                                                                                                                                          |
| 8  | Adds Exa + Tavily provider backends                                                                                                                         | VERIFIED   | `web_extract/backends/{exa,tavily}.rs` exist. `dispatch.rs` enumerates `Backend::Firecrawl/Exa/Tavily`. Per-backend integration tests in `web_extract_integration.rs`.                                                                                                                                                                            |
| 9  | PDF support                                                                                                                                                 | VERIFIED   | `web_extract/pdf.rs` exists; root Cargo.toml line 74 pins `pdf-extract = "0.10"`. UAT Test 5 confirmed PDF routing end-to-end.                                                                                                                                                                                                                  |
| 10 | YouTube dispatched via Phase 19 `youtube-content` skill                                                                                                     | VERIFIED   | `web_extract/youtube.rs:22` `const YOUTUBE_SKILL_NAME: &str = "youtube-content"`. Hyphenation locked by test. UAT Test 6 confirmed correct dispatch.                                                                                                                                                                                            |
| 11 | SSRF protection blocks internal IPs across all backends                                                                                                     | VERIFIED   | UAT Test 7 passed.                                                                                                                                                                                                                                                                                                                              |
| 12 | UAT Issue 2 / Symptom 1 closed: `/toolset` returns populated table in REPL + Telegram listing web_extract as a member                                       | **VERIFIED**   | Plan 15 wave-5 commits NOW on develop (verified `git branch --contains 37afb6e` lists `develop`). Code spot-checks: `RegistryToolsetSession::new` appears 6 times in main.rs (≥3 required); `with_toolset_session` appears 4 times (≥1 required); `build_cmd_ctx` signature at line 937 has `toolset_session: Option<Arc<dyn ToolsetSessionHandle>>`; `toolset_cmd.rs:242` lists 3 members `["web_search", "web_read", "web_extract"]`. Tests pass: 5/5 in `toolset_session::tests`, 1/1 `with_toolset_session_wired_in_all_three_sites`, 1/1 `toolset_members_map_web_includes_web_extract`. |
| 13 | UAT Issue 9 closed: literal API-key tokens (`sk-or-v1-fakekeyabc123`) MUST NOT appear in any agent response, error envelope, or log line                    | **VERIFIED**   | Plan 16 wave-5 commits NOW on develop. Code spot-checks: `pub fn redact_secrets_in_url` at sanitize.rs:90; web_extract.rs:239 uses it on the secret-rejection error envelope; web_extract.rs:313 defensively redacts on success path; web_extract.rs has `url_for_log` idiom across 5 sites; pdf.rs:56 uses redactor with `&[]`; prompt_builder.rs:24 has the `Secret handling:` rule. Negative grep gate: zero raw-`url` warn! sites in web_extract.rs OR pdf.rs. Tests pass: 19/19 sanitize tests (11 pre-existing + 8 new), 1/1 prompt lock test. |
| 14 | All 17 plans documented with SUMMARY.md                                                                                                                     | VERIFIED   | `git ls-tree develop -- .planning/phases/25.2-web-extract-tools/ \| grep SUMMARY` lists 17 SUMMARYs (00-16) on develop. Plans 15-SUMMARY.md (blob ac7ac4c2) and 16-SUMMARY.md (blob 22fb2565) are present. STATE.md `completed_plans: 103` matches the 17-plan total.                                                                                |
| 15 | Workspace builds clean                                                                                                                                      | VERIFIED   | `cargo build --workspace` exits 0. (Pre-existing baseline warnings carried; not introduced by phase 25.2.)                                                                                                                                                                                                                                       |
| 16 | Symptom 2 (LLM truncation at "Here are█") logged as out-of-scope follow-up                                                                                  | VERIFIED   | UAT.md Test 2 root_cause classifies Symptom 2 as INDEPENDENT. Plan 15 PLAN.md explicitly excludes it. Plan 15 SUMMARY logs the `/gsd-debug` follow-up command. Same for the residual youtube.rs:81 stderr-leak surface in Plan 16's "Next Phase Readiness". Both items deferred (see `deferred` frontmatter).                                  |

**Score:** 16/16 truths verified. (Previous: 14/16 — both wave-5 closures now flipped to VERIFIED.)

---

### Required Artifacts (Levels 1-3)

| Artifact                                                                              | Expected                                                          | Status   | Details                                                                                                                                       |
| ------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `crates/ironhermes-tools/src/web_extract.rs`                                          | WebExtractTool struct + Tool impl + per-URL dispatch              | VERIFIED | `pub struct WebExtractTool`, `impl Tool`, `route_tiers` invocation present                                                                   |
| `crates/ironhermes-tools/src/web_extract/dispatch.rs`                                 | URL classifier + Backend enum                                     | VERIFIED | Backend variants present, classifier tests in same file                                                                                       |
| `crates/ironhermes-tools/src/web_extract/sanitize.rs`                                 | base64 strip + secret URL detection (Plan 04)                     | VERIFIED | `contains_secret` predicate + `SECRET_URL_PATTERNS` constant present                                                                          |
| `crates/ironhermes-tools/src/web_extract/sanitize.rs`                                 | `redact_secrets_in_url` primitive (Plan 16)                       | **VERIFIED** | `pub fn redact_secrets_in_url(url: &str, extras: &[String]) -> String` at line 90; 8 unit tests pass; SECRET_URL_PATTERNS const unchanged (count=9) |
| `crates/ironhermes-tools/src/web_extract/backends/{firecrawl,exa,tavily,local,mod}.rs` | All four provider backends + module file                         | VERIFIED | All five files present                                                                                                                        |
| `crates/ironhermes-tools/src/web_extract/pdf.rs`                                      | PDF backend using pdf-extract 0.10                                | VERIFIED | 0.10 pin matches Cargo.toml line 74. NEW (Plan 16): pdf.rs:56 uses `redact_secrets_in_url`                                                  |
| `crates/ironhermes-tools/src/web_extract/youtube.rs`                                  | YouTube dispatch via skill helper                                 | VERIFIED | Hyphenated `youtube-content` skill name locked by test                                                                                        |
| `crates/ironhermes-tools/src/web_extract/summary/{mod,tiers,chunked}.rs`              | Tier router + chunked synthesis                                   | VERIFIED | All three files present; `route_tiers` wired                                                                                                  |
| `crates/ironhermes-core/src/config.rs::ExtractConfig`                                 | Config block per D-22                                             | VERIFIED | Defined at line 554, RoamingExtractConfig at line 295                                                                                          |
| `crates/ironhermes-core/src/SummarizationClientHandle`                                | Trait per D-13                                                    | VERIFIED | Re-exported at crate root; impl in `any_client.rs:267`                                                                                         |
| `crates/ironhermes-agent/src/any_client.rs::AnyClientSummarizationHandle`             | Production summarization handle                                   | VERIFIED | Defined at :256; dyn-compatibility test at :454                                                                                               |
| `crates/ironhermes-cli/src/main.rs::register_web_extract_tool` (3 wireup sites)       | Tool registered in run_chat / run_single / run_gateway            | VERIFIED | Parity-guard test `register_web_extract_tool_wired_in_all_three_sites` PASSES                                                                |
| `crates/ironhermes-tools/src/toolset_session.rs`                                      | RegistryToolsetSession production impl (Plan 15 Task 1)           | **VERIFIED** | File exists (13,465 bytes); `pub struct RegistryToolsetSession` + `impl ToolsetSessionHandle`; 5/5 unit tests pass                          |
| `crates/ironhermes-cli/src/main.rs::with_toolset_session` wireup (3 CLI sites)        | Plan 15 Task 2                                                    | **VERIFIED** | `RegistryToolsetSession::new(` count=6 in main.rs (≥3 required); `with_toolset_session(` count=4; `build_cmd_ctx` signature widened; parity test passes |
| `crates/ironhermes-cli/src/toolset_cmd.rs::toolset_members_map web entry`             | 3-member list including web_extract (Plan 15 Task 3)              | **VERIFIED** | Line 242: `m.insert("web", &["web_search", "web_read", "web_extract"]);` (3 members); test `toolset_members_map_web_includes_web_extract` passes |
| `crates/ironhermes-agent/src/prompt_builder.rs::DEFAULT_AGENT_IDENTITY`               | Augmented with Secret handling rule (Plan 16 Task 3)              | **VERIFIED** | Line 24: `Secret handling:` section with three rules (NEVER echo, call-tools-anyway, paraphrase). Lock test passes.                          |
| `crates/ironhermes-tools/src/web_extract.rs::ExtractionResult.url echo`               | Uses redacted URL on both error AND success paths (Plan 16 Task 4) | **VERIFIED** | Line 239 (error path): `let redacted = redact_secrets_in_url(...); ExtractionResult::error(&redacted, ...)`. Line 313 (success path defensive): `extraction.url = redact_secrets_in_url(&extraction.url, ...)`. |
| `.planning/phases/25.2-web-extract-tools/25.2-15-SUMMARY.md`                          | Plan 15 SUMMARY                                                   | **VERIFIED** | `git ls-tree develop` lists blob ac7ac4c2; file present in working tree                                                                        |
| `.planning/phases/25.2-web-extract-tools/25.2-16-SUMMARY.md`                          | Plan 16 SUMMARY                                                   | **VERIFIED** | `git ls-tree develop` lists blob 22fb2565; file present in working tree                                                                        |

---

### Key Link Verification

| From                                                       | To                                                       | Via                                                                | Status   | Details                                                                                                                                                 |
| ---------------------------------------------------------- | -------------------------------------------------------- | ------------------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `WebExtractTool`                                           | `web` toolset registry                                   | `toolset()` method + register_defaults                             | WIRED    | Confirmed via Plan 12 SUMMARY                                                                                                                            |
| `register_web_extract_tool` (CLI)                          | run_chat / run_single / run_gateway                      | 3 call sites in main.rs                                            | WIRED    | Parity test `register_web_extract_tool_wired_in_all_three_sites` PASSES                                                                                  |
| `AnyClientSummarizationHandle`                             | `resolve_role("summarization")`                          | Phase 26 cascade                                                   | WIRED    | `any_client.rs:267` impl + integration test                                                                                                              |
| `WebExtractTool` summarization pipeline                    | aux-LLM client                                           | `summary/tiers.rs route_tiers`                                     | WIRED    | web_extract.rs:269                                                                                                                                       |
| `RegistryToolsetSession`                                   | `CommandContext.toolset_session`                         | `with_toolset_session` (build_cmd_ctx)                             | **WIRED** | The struct exists and is constructed at 3 production sites (run_chat, run_single, run_gateway). `build_cmd_ctx` signature includes `toolset_session: Option<Arc<dyn ToolsetSessionHandle>>` at line 937. cmd_toolset's `Some(handle)` branch is now reachable in production. |
| `redact_secrets_in_url`                                    | `ExtractionResult.url` echo + warn! sites                | `sanitize::redact_secrets_in_url`                                  | **WIRED** | Function exists; called at web_extract.rs:239 (error envelope), 313 (success defense), 330 (`url_for_log`); pdf.rs:56                                  |
| `DEFAULT_AGENT_IDENTITY`                                   | LLM system prompt                                        | prompt_builder slot 1                                              | **WIRED + content complete** | Slot wiring intact; Secret handling clause present (`Secret handling:` at line 24 with 3 rules)                                                    |

---

### Data-Flow Trace (Level 4)

| Artifact                          | Data Variable           | Source                                           | Produces Real Data                                            | Status                                                                  |
| --------------------------------- | ----------------------- | ------------------------------------------------ | ------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `WebExtractTool::execute`         | ExtractionResult        | per-URL pipeline (D-19→D-03→backends→D-08→D-11)  | Yes (UAT Tests 3,4,5,8 confirmed real Markdown content)       | FLOWING                                                                 |
| `ToolRegistry::get_definitions`   | tool schema             | registry.rs::register_defaults → WebExtractTool  | Yes (UAT Test 1 boot logs confirmed registration)             | FLOWING                                                                 |
| `/toolset` command (REPL/Telegram) | `ctx.toolset_session`   | `CommandContext` field — populated by build_cmd_ctx | **Yes — `Arc<RegistryToolsetSession>` populated in run_chat/run_single/run_gateway** | **FLOWING** (was DISCONNECTED in pre-merge state; now WIRED via the 3-site construction) |
| `web_extract` error envelope      | `ExtractionResult.url`  | web_extract.rs:239 (error) and :313 (success defense) | **Redacted URL emitted on both paths** (uses `redact_secrets_in_url`) | **FLOWING** (was HOLLOW_PROP in pre-merge state; now structurally redacted) |

---

### Behavioral Spot-Checks

| Behavior                                              | Command                                                                  | Result                                          | Status |
| ----------------------------------------------------- | ------------------------------------------------------------------------ | ----------------------------------------------- | ------ |
| Workspace compiles                                    | `cargo build --workspace`                                                | Exit 0 (per pre-condition + spot-check)         | PASS   |
| `toolset_session` module exists                       | `ls crates/ironhermes-tools/src/toolset_session.rs`                      | 13,465 bytes                                    | PASS   |
| `RegistryToolsetSession` is referenceable             | `grep -rl RegistryToolsetSession crates/`                                | 4 files (toolset_session.rs, lib.rs, toolset_cmd.rs, main.rs) | PASS   |
| `with_toolset_session` count in main.rs               | `grep -c with_toolset_session crates/ironhermes-cli/src/main.rs`         | 4                                               | PASS   |
| `RegistryToolsetSession::new` count in main.rs        | `grep -c RegistryToolsetSession::new crates/ironhermes-cli/src/main.rs`  | 6 (≥3 required)                                 | PASS   |
| `redact_secrets_in_url` exists                        | `grep -rl redact_secrets_in_url crates/`                                 | 3 files (sanitize.rs, web_extract.rs, pdf.rs)   | PASS   |
| Web toolset members map includes web_extract          | `grep -nE 'm.insert\("web",' crates/ironhermes-cli/src/toolset_cmd.rs`   | Line 242: 3-member list                         | PASS   |
| `DEFAULT_AGENT_IDENTITY` has Secret handling          | `grep -n 'Secret handling' crates/ironhermes-agent/src/prompt_builder.rs` | Line 24                                         | PASS   |
| Wave-5 SUMMARY files on develop                       | `git ls-tree develop -- .planning/phases/25.2-web-extract-tools/`        | 25.2-15 + 25.2-16 SUMMARYs present (17 total)  | PASS   |
| Negative grep — no raw `url`/`original_url` in warn! | `grep -nE 'warn!\(.*\b(url\|original_url)\b' web_extract.rs pdf.rs`     | Empty (only `url_for_log` and `redact_secrets_in_url` references) | PASS   |
| `toolset_session::tests`                              | `cargo test -p ironhermes-tools --lib toolset_session`                   | 5 passed; 0 failed                              | PASS   |
| `web_extract::sanitize::tests` (11 + 8 = 19)          | `cargo test -p ironhermes-tools --lib web_extract::sanitize`             | 19 passed; 0 failed                             | PASS   |
| Plan 15 parity guard                                  | `cargo test ironhermes-cli --bin ironhermes with_toolset_session_wired_in_all_three_sites` | 1 passed                              | PASS   |
| Plan 14 parity guard (no regression)                  | `cargo test ironhermes-cli --bin ironhermes register_web_extract_tool_wired_in_all_three_sites` | 1 passed                          | PASS   |
| Plan 16 prompt lock                                   | `cargo test ironhermes-agent default_agent_identity_includes_secret_handling_rule` | 1 passed                                | PASS   |
| Plan 15 members-map test                              | `cargo test ironhermes-cli --bin ironhermes toolset_members_map_web_includes_web_extract` | 1 passed                            | PASS   |

All 16 spot-checks PASS.

---

### Requirements Coverage

Phase 25.2 declared `Requirements: D-01 through D-28` (CONTEXT-locked decisions; no REQ-IDs in REQUIREMENTS.md). Coverage assessed against the decision register:

| Decision     | Description                                          | Status    | Evidence                                                                                                                |
| ------------ | ---------------------------------------------------- | --------- | ----------------------------------------------------------------------------------------------------------------------- |
| D-01..D-04   | Tool surface, toolset, multi-URL, web_local refactor | SATISFIED | All wave-1 plans landed; UAT 3+4 passed                                                                                 |
| D-05..D-09   | Backend cascade, providers, PDF                      | SATISFIED | UAT 5 passed; pdf-extract 0.10                                                                                          |
| D-10         | YouTube via youtube-content skill                    | SATISFIED | UAT 6 passed                                                                                                            |
| D-11..D-17   | Tiered summarization                                 | SATISFIED | UAT 8 confirmed Tier-3 path                                                                                             |
| D-18..D-19   | sanitize / SSRF                                      | SATISFIED | base64 strip + contains_secret predicate (Plan 04) + **redact_secrets_in_url transform (Plan 16)**. UAT 7 passed; UAT 9 structural fix landed. |
| D-20         | Agent CLI wireup                                     | SATISFIED | register_web_extract_tool present at 3 sites + parity guard passes                                                     |
| D-21..D-22   | ExtractConfig, RESERVED_ROLE_NAMES                   | SATISFIED | config.rs:554                                                                                                           |
| D-23..D-24   | Test infrastructure                                  | SATISFIED | web_extract_integration.rs has 9 D-26 + D-27 tests                                                                      |
| D-25..D-27   | Validation strategy + ROADMAP update                 | SATISFIED | VALIDATION.md flipped; ROADMAP placeholder replaced                                                                     |
| D-28         | ROADMAP goal text replaces placeholder               | SATISFIED | Confirmed in ROADMAP.md `Phase 25.2` entry                                                                              |

D-19 was PARTIAL pre-merge; now FULLY SATISFIED with the redactor primitive landed.

---

### Anti-Patterns Found

| File                                                       | Line          | Pattern                                                                                                                                                  | Severity | Impact                                                                                                                                                                                                                |
| ---------------------------------------------------------- | ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none in scope of this phase)                              | —             | All previously flagged BLOCKER patterns from the pre-merge report are RESOLVED:                                                                          | —        | Pre-merge BLOCKER 1 (handlers.rs `None` short-circuit unreachable in production): RESOLVED — wireup at run_chat/run_single/run_gateway populates the handle. Pre-merge BLOCKER 2 (web_extract.rs:236-237 raw URL echo): RESOLVED — `redact_secrets_in_url` applied at line 239. |
| `crates/ironhermes-cli/src/main.rs`                        | (cli build)   | 26+ pre-existing warnings emitted during cold build                                                                                                       | INFO     | Pre-existing baseline carried from earlier phases; not introduced by phase 25.2. Logged for a future workspace-wide clippy cleanup.                                                                                  |
| `crates/ironhermes-tools/src/web_extract/youtube.rs`       | 81            | warn! logs `output.status` + `stderr` from yt-dlp helper. If yt-dlp echoes the URL on failure, the URL leaks here.                                       | INFO (deferred) | Recorded as residual risk in Plan 16 SUMMARY's "Next Phase Readiness". Different fix shape (free-text stderr scrub vs URL transform). Not in scope of phase 25.2 closure.                                |
| `crates/ironhermes-cli/src/main.rs` UAT Test 2 Symptom 2   | (LLM stream)  | LLM truncation at "Here are█" — visible glyph is the activity scanner, NOT a streaming caret                                                              | INFO (deferred) | Independent root cause; requires runtime trace of `finish_reason` + `delta.tool_calls`. Logged in Plan 15 SUMMARY's "Next Phase Readiness" as a `/gsd-debug` follow-up.                                  |

No active anti-patterns blocking the phase goal.

---

### Human Verification Required

Two items are appropriate for live UAT replay (NOT failures — these are end-to-end confirmations of structurally-verified behavior):

#### 1. UAT Test 2 replay — `/toolset` works in REPL + Telegram

**Test:** From a clean shell, run `ironhermes` (REPL). Type `/toolset`. Then `/toolset show web`. Repeat from the whitelisted Telegram chat.

**Expected:**
- REPL `/toolset` returns a populated multi-line table (NOT the deferred fallback string `toolset session handle not configured`)
- The `web` row shows 3 members: `web_search`, `web_read`, `web_extract`
- `/toolset show web` lists the same 3 members with their availability status
- Telegram exhibits the identical behavior

**Why human:** End-to-end UI verification across two transports. Programmatic checks (file presence + parity tests) confirm the structural fix is in code. Live replay confirms the user-visible behavior matches.

#### 2. UAT Test 9 replay — secret in URL is redacted in agent response

**Test:** Ask the agent to extract content from `https://example.com/?api_key=sk-or-v1-fakekeyabc123`. Repeat in REPL and Telegram.

**Expected:** The literal token `sk-or-v1-fakekeyabc123` does NOT appear in:
- The agent's response (refusal, warning, OR success)
- Any error envelope (`ExtractionResult.url`)
- Any tracing log line emitted during the request

**Why human:** LLM compliance with the new `Secret handling:` system-prompt rule is probabilistic and must be observed in a real conversation. Tool-side redaction is structurally verified (defense-in-depth via `redact_secrets_in_url` at the error-envelope and warn! sites). Prompt-side compliance requires live observation.

---

### Gaps Summary

**No gaps remain.** Both pre-merge gaps (Plan 15 wireup absent on develop; Plan 16 redactor + system-prompt rule absent on develop) are CLOSED via merge commit `2c72380`.

**Deferred items** (NOT gaps — explicitly out of scope per plan + SUMMARY decisions):

1. **UAT Test 2 / Symptom 2** — LLM truncation at "Here are█". Independent root cause (model emits tool_call mid-sentence; visible glyph is the Knight-Rider activity scanner from `tui/render.rs:917`, not a streaming caret). Requires runtime trace of `finish_reason` + `delta.tool_calls` in a separate `/gsd-debug` session. Logged in Plan 15 SUMMARY "Next Phase Readiness".
2. **youtube.rs:81 stderr-leak** — yt-dlp helper's stderr may echo the URL on failure. Different fix shape (free-text stderr scrub vs URL field transform). Plan 16 B-1 audit explicitly dropped this from `files_modified`. Logged in Plan 16 SUMMARY "Next Phase Readiness" as a follow-up `/gsd-debug` task.

---

### Recommendation

**status: passed** with two human-verification items deferred to UAT replay.

Phase 25.2 is structurally complete. All 17 plans have SUMMARYs on develop. All wave-5 commits are in develop's first-parent ancestry. All four levels of artifact verification (exists / substantive / wired / data flowing) pass. All 16 behavioral spot-checks pass. Both pre-merge gaps are closed by structural fixes that the test suite locks against future regression.

The user owns the live UAT replay of Tests 2 and 9 to confirm end-to-end user-visible behavior. Per the prompt's instructions, I have NOT manually run UAT — those replays are the user's job. The verification is **PASS-WITH-FOLLOWUPS**: the two follow-ups are the documented out-of-scope debug tasks (Symptom 2 LLM truncation; youtube.rs:81 stderr leak), neither of which blocks the phase goal.

---

_Re-Verified: 2026-05-02T23:55:00Z_
_Verifier: Claude (gsd-verifier, post-merge re-run)_
