---
phase: 25.2
plan: 00
subsystem: tools/web-extract
tags: [scaffolding, wave-0, web-extract, pdf-extract, dependencies]
dependency_graph:
  requires:
    - "Phase 25.2 PLAN/CONTEXT/PATTERNS/RESEARCH/VALIDATION docs"
    - "Workspace pdf-extract = 0.10 (already pinned at root Cargo.toml line 74)"
  provides:
    - "crates/ironhermes-tools/src/web_extract.rs (Wave 0 dispatcher stub)"
    - "crates/ironhermes-tools/src/web_extract/{dispatch,sanitize,pdf,youtube}.rs"
    - "crates/ironhermes-tools/src/web_extract/backends/{firecrawl,exa,tavily,local}.rs"
    - "crates/ironhermes-tools/src/web_extract/summary/{tiers,chunked}.rs"
    - "crates/ironhermes-tools/src/web_local.rs (D-04 refactor target stub)"
    - "crates/ironhermes-tools/tests/web_extract_integration.rs (env_lock + EnvGuard)"
    - "pdf-extract dep wired into ironhermes-tools (workspace inheritance)"
    - "lib.rs declares pub mod web_extract; pub mod web_local;"
  affects:
    - "Plans 25.2-01..14 — every later wave's file-existence verification gate"
tech_stack:
  added:
    - "pdf-extract 0.10 (workspace inheritance, NOT 0.7 as CONTEXT D-24 suggested)"
  patterns:
    - "Per-tool flat-file layout (Phase 25.1 D-17 / web_read.rs)"
    - "env_lock + EnvGuard RAII test helper (browser_integration.rs)"
key_files:
  created:
    - "crates/ironhermes-tools/src/web_extract.rs"
    - "crates/ironhermes-tools/src/web_extract/dispatch.rs"
    - "crates/ironhermes-tools/src/web_extract/sanitize.rs"
    - "crates/ironhermes-tools/src/web_extract/pdf.rs"
    - "crates/ironhermes-tools/src/web_extract/youtube.rs"
    - "crates/ironhermes-tools/src/web_extract/backends/mod.rs"
    - "crates/ironhermes-tools/src/web_extract/backends/firecrawl.rs"
    - "crates/ironhermes-tools/src/web_extract/backends/exa.rs"
    - "crates/ironhermes-tools/src/web_extract/backends/tavily.rs"
    - "crates/ironhermes-tools/src/web_extract/backends/local.rs"
    - "crates/ironhermes-tools/src/web_extract/summary/mod.rs"
    - "crates/ironhermes-tools/src/web_extract/summary/tiers.rs"
    - "crates/ironhermes-tools/src/web_extract/summary/chunked.rs"
    - "crates/ironhermes-tools/src/web_local.rs"
    - "crates/ironhermes-tools/tests/web_extract_integration.rs"
  modified:
    - "crates/ironhermes-tools/Cargo.toml"
    - "crates/ironhermes-tools/src/lib.rs"
decisions:
  - "Used `pdf-extract = { workspace = true }` (workspace pin = 0.10) — overrode CONTEXT D-24 reference to 0.7, matched RESEARCH.md verified versions."
  - "Placed `pub mod web_extract;` and `pub mod web_local;` after `pub mod terminal;` to keep web_* modules adjacent and ahead of web_read/web_search."
  - "Deferred `pub use web_extract::WebExtractTool;` re-export to plan 25.2-13 (struct does not exist in Wave 0)."
  - "Used `pub(crate)` visibility on env_lock/EnvGuard so they can be referenced by sibling test modules added in plan 25.2-14."
metrics:
  duration_minutes: ~5
  task_count: 4
  files_created: 15
  files_modified: 2
  completed_date: "2026-05-02"
---

# Phase 25.2 Plan 00: Wave 0 File Scaffolding Summary

**One-liner:** Wave 0 file scaffolding lays down 15 empty stub files (14 src + 1 test) plus a single `pdf-extract = { workspace = true }` line so every later wave's file-existence gate passes; workspace and test compile cleanly with zero new logic.

## What Was Built

This plan delivered the load-bearing skeleton for the entire 25.2 phase without
introducing any executable behavior:

1. **Module tree under `crates/ironhermes-tools/src/web_extract/`** — 13 stub
   files matching the D-23 layout (top-level dispatcher + dispatch / sanitize /
   pdf / youtube + backends/{firecrawl,exa,tavily,local} + summary/{tiers,chunked}).
   Each file carries a `//! Phase 25.2` header doc-comment and `mod.rs` files
   declare their `pub mod` children so rustc resolves the tree.
2. **Refactor target `crates/ironhermes-tools/src/web_local.rs`** — empty stub
   that plan 25.2-01 will populate by moving `extract_content_local`,
   `truncate_content`, and `validate_url_async` out of `web_read.rs` (D-04 DRY).
3. **Cargo.toml dep addition** — single line
   `pdf-extract = { workspace = true }` added to the
   `crates/ironhermes-tools/Cargo.toml` `[dependencies]` block. Workspace root
   already pinned `pdf-extract = "0.10"` (line 74) via existing
   `ironhermes-gateway` consumer; this plan only adds the new consumer edge.
4. **lib.rs wiring** — `pub mod web_extract;` + `pub mod web_local;` declared
   alongside the existing `pub mod web_read;` / `pub mod web_search;` lines.
5. **Integration test shell** — `tests/web_extract_integration.rs` with the
   9-test docstring (D-26 #1-#8 + D-27 #9), `env_lock()` helper, and `EnvGuard`
   RAII struct lifted verbatim (with cited source lines) from
   `tests/browser_integration.rs`. `#![allow(dead_code)]` keeps the helpers
   compile-clean until plan 14 references them.

## Tasks Completed

| Task | Name                                                    | Commit  | Files                                                 |
| ---- | ------------------------------------------------------- | ------- | ----------------------------------------------------- |
| 1    | Create web_extract.rs + submodule directory tree        | 11ccc4d | 14 stub files under src/web_extract/ + src/web_local.rs |
| 2    | Add pdf-extract workspace dep to ironhermes-tools       | ef71180 | crates/ironhermes-tools/Cargo.toml                    |
| 3    | Wire new modules into ironhermes-tools/src/lib.rs       | 82babfc | crates/ironhermes-tools/src/lib.rs                    |
| 4    | Create empty integration test file shell                | 699f2aa | crates/ironhermes-tools/tests/web_extract_integration.rs |

## Verification

- All 14 src stub files exist (`test -f` per path)
- `crates/ironhermes-tools/src/web_extract.rs` contains 6 `^pub mod` lines
  (backends, dispatch, pdf, sanitize, summary, youtube)
- `web_extract/backends/mod.rs` contains 4 `^pub mod` lines (exa, firecrawl,
  local, tavily)
- `web_extract/summary/mod.rs` contains 2 `^pub mod` lines (chunked, tiers)
- `cargo check -p ironhermes-tools` exits 0 (1m 07s first build, 4s incremental
  after lib.rs wiring)
- `cargo check --tests -p ironhermes-tools` exits 0 (2m 51s first build)
- `grep -c 'pdf-extract' Cargo.toml` returns 1 (only the pre-existing
  `[workspace.dependencies]` line at root)
- `grep -c 'pdf-extract' crates/ironhermes-tools/Cargo.toml` returns 1 (only
  the new `workspace = true` consumer line)
- No `pdf-extract = "0.7"` anywhere in `crates/` or `Cargo.toml` (the wrong
  version from CONTEXT D-24 is absent)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Used pdf-extract 0.10 (workspace inheritance) instead of 0.7**
- **Found during:** Task 2 (read_first step on workspace `Cargo.toml`)
- **Issue:** CONTEXT D-24 said `pdf-extract = "0.7"`, but workspace
  `Cargo.toml` line 74 already pins `pdf-extract = "0.10"` and `ironhermes-gateway`
  already consumes that version. Adding 0.7 to the tools crate would have either
  conflicted with the workspace pin or duplicated the dep.
- **Fix:** Used `{ workspace = true }` per RESEARCH.md "Standard Stack §verified
  versions" guidance, matching the plan's own `<interfaces>` block which had
  pre-flagged the override.
- **Files modified:** `crates/ironhermes-tools/Cargo.toml`
- **Commit:** ef71180

The plan itself anticipated this in the `<interfaces>` HTML comments and the
Task 2 action ("Per RESEARCH.md... use `{ workspace = true }`; do NOT specify
`version = "0.7"` anywhere"), so this was less a deviation and more an
already-decided override that the SUMMARY records explicitly for the audit
trail.

### Out of Scope (deferred)

- Pre-existing `DEFAULT_SAFE_TOOLS` dead-code warning in
  `crates/ironhermes-tools/src/delegate_task.rs:45` — present before this plan,
  not caused by Wave 0 changes, no action taken.
- Pre-existing unused `result_str` warning in
  `crates/ironhermes-tools/tests/hub_search_test.rs:167` — present before this
  plan, not caused by Wave 0 changes, no action taken.

## Authentication Gates

None — Wave 0 is files-only, no network or credentialed calls.

## Threat Surface Scan

Wave 0 introduces no executable code paths. Per PLAN's `<threat_model>`,
T-25.2-supply-chain (mitigate disposition) is fully covered by using
`{ workspace = true }` instead of a fresh version pin — the existing rustsec
audit / Cargo.lock entry covers the new consumer. Verified via
`grep -c 'pdf-extract' Cargo.toml` returning 1.

No new security-relevant surface introduced beyond what the threat model
already enumerates.

## Self-Check: PASSED

All 15 created files verified present on disk. All 4 commits exist in git log:

- `11ccc4d` feat(25.2-00): scaffold web_extract module tree as Wave 0 stubs
- `ef71180` feat(25.2-00): add pdf-extract workspace dep to ironhermes-tools
- `82babfc` feat(25.2-00): wire web_extract + web_local modules into ironhermes-tools lib.rs
- `699f2aa` test(25.2-00): scaffold web_extract integration test shell with env_lock + EnvGuard
