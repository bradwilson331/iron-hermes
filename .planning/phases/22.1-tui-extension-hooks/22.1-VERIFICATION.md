---
phase: 22.1-tui-extension-hooks
verified: 2026-04-17T00:00:00Z
status: passed
score: 13/13 must-haves verified
overrides_applied: 0
re_verification: null
gaps: []
deferred: []
human_verification: []
---

# Phase 22.1: TUI Extension Hooks Verification Report

**Phase Goal:** Create a Rust extension mechanism for the CLI TUI so that external code (plugins, custom builds, future crates) can add widgets, keybindings, layout sections, command handlers, and style overrides — the Rust equivalent of hermes-agent's subclassable CliManager. Implements a hybrid three-layer architecture: TuiExtension trait (static contract), mpsc message bus (dynamic updates), and command registry (extension-first dispatch). Slot-based layout with dynamic DECSTBM scroll region adjustment. No new dependencies.
**Verified:** 2026-04-17T00:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | TuiExtension trait exists with default no-op methods for widgets, keybindings, process_command, and style_overrides | VERIFIED | `extension.rs:166` — `pub trait TuiExtension: Send + Sync` with all four default no-op impls; 18 unit tests pass |
| 2 | Widget struct, LayoutSlot enum, and TuiEvent enum are defined and exported | VERIFIED | `extension.rs:21,36,140`; all three exported from `tui/mod.rs:32` |
| 3 | KeybindingRegistry can register and match keybindings by KeyContext | VERIFIED | `keybindings.rs:14-56`; 16 tests covering Idle/InFlight/Always context matching all pass |
| 4 | CommandRegistry dispatches to extensions first then falls back to core stub | VERIFIED | `commands.rs:30-75`; extension-first chain with `catch_unwind`; 12 dispatch tests pass |
| 5 | All types are Send + Sync for use with tokio tasks | VERIFIED | `TuiExtension: Send + Sync` bound; `Widget`, `Keybinding`, `KeyContext`, `LayoutSlot`, `TuiEvent`, `CommandResult` all implement `Send + Sync` via `#[derive(Debug, Clone)]` and primitive fields |
| 6 | TuiHandle accepts optional extensions at construction time | VERIFIED | `render.rs:126` — `pub fn new_with_extensions(initial_status, extensions: Vec<Box<dyn TuiExtension>>) -> Self`; `new()` delegates to it with `Vec::new()` |
| 7 | Render loop composites extension widgets into named layout slots (AboveStatus, BelowScanner, StatusRight) | VERIFIED | `render.rs:509-632` — `redraw_with_extensions` renders BelowScanner, Scanner, AboveStatus, StatusRight inline |
| 8 | DECSTBM scroll region adjusts dynamically based on 3 + extension widget rows | VERIFIED | `render.rs:43-50` — `reserved_rows()` replaces all hardcoded `saturating_sub(3)`; no hardcoded 3-row calls remain in active paths; 3 unit tests pass |
| 9 | TuiEvent messages received via mpsc channel update widgets in real-time | VERIFIED | `render.rs:398-436` — `try_recv` drain loop handles `UpdateWidget`, `RemoveWidget`, `FlashHint` each tick; mpsc channel created in `new_with_extensions` at `render.rs:183` |
| 10 | Pre-readline keybinding check dispatches Idle/Always bindings before rl.readline() | VERIFIED | `main.rs:699-710` — `crossterm::event::poll(Duration::ZERO)` + `keybinding_registry.match_key(&key_event, &KeyContext::Idle)` before `prepare_prompt_with_reserve`; INV-22.1-05 static-grep test passes |
| 11 | REPL command dispatch uses extension-first chain from commands.rs instead of inline match | VERIFIED | `main.rs:728-759` — `dispatch_command(tui.extensions(), cmd, args)` with `CommandResult::Handled/Silent/Error` arms; old inline `"/quit" \| "/exit"` match replaced; INV-22.1-02 static-grep test passes |
| 12 | Zero-extension case renders identically to Phase 21 (no visible change) | VERIFIED | `TuiHandle::new()` delegates to `new_with_extensions(_, Vec::new())`; empty extensions produce `reserved=3`, empty `StyleOverrides`; `build_scanner_frame` delegates to `knight_rider::frame()` when colors match defaults |
| 13 | StyleOverrides from all registered extensions are merged at TuiHandle construction and applied in redraw_with_extensions for scanner and status style slots | VERIFIED | `render.rs:59-84` — `merge_style_overrides()` with last-wins and `catch_unwind`; `render.rs:490-506` applies `scanner.lit`, `scanner.trail`, `scanner.bg`, `status.separator`, `status.hint` slots; 4 unit tests pass |

**Score:** 13/13 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/ironhermes-cli/src/tui/extension.rs` | TuiExtension trait, Widget, LayoutSlot, TuiEvent, StyleOverrides, Keybinding, KeyContext | VERIFIED | 409 lines, all 7 types present, 18 unit tests |
| `crates/ironhermes-cli/src/tui/keybindings.rs` | KeybindingRegistry with register/match/collect_help operations | VERIFIED | 218 lines, `register_from_extensions`, `match_key`, `help_entries`, `format_key_display`; 16 tests |
| `crates/ironhermes-cli/src/tui/commands.rs` | CommandResult enum, dispatch_command function, format_help function | VERIFIED | 290 lines, all three present, `catch_unwind` panic containment; 12 tests |
| `crates/ironhermes-cli/src/tui/mod.rs` | Module declarations and re-exports for new submodules | VERIFIED | `pub mod extension`, `pub mod keybindings`, `pub mod commands`; all types re-exported |
| `crates/ironhermes-cli/src/tui/render.rs` | TuiHandle with extension support, render_loop with widget slot compositing, dynamic DECSTBM, merged style overrides | VERIFIED | `new_with_extensions`, `reserved_rows`, `merge_style_overrides`, `event_sender`, `reserved_row_count`, `extensions`, `prepare_prompt_with_reserve`, `finish_prompt_with_reserve`; 14 tests |
| `crates/ironhermes-cli/src/main.rs` | Pre-readline keybinding check, extension-first command dispatch, TuiHandle construction with extensions | VERIFIED | `new_with_extensions`, `KeybindingRegistry::register_from_extensions`, `dispatch_command`, `prepare_prompt_with_reserve`, `finish_prompt_with_reserve`, `match_key` + `KeyContext::Idle`; 5 static-grep invariant tests |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `keybindings.rs` | `extension.rs` | `use crate::tui::extension::{KeyContext, Keybinding, TuiExtension}` | WIRED | `keybindings.rs:5` — imports confirmed |
| `commands.rs` | `extension.rs` | `use crate::tui::extension::{CommandResult, TuiExtension}` | WIRED | `commands.rs:8` — imports confirmed |
| `render.rs` | `extension.rs` | `use crate::tui::extension::{LayoutSlot, StyleOverrides, TuiEvent, TuiExtension, Widget, MAX_WIDGET_HEIGHT}` | WIRED | `render.rs:15` — imports confirmed |
| `main.rs` | `commands.rs` | `use crate::tui::{dispatch_command, ...}` | WIRED | `main.rs:15` — `dispatch_command` imported and called at `main.rs:730` |
| `main.rs` | `keybindings.rs` | `use crate::tui::{..., KeybindingRegistry, ...}` | WIRED | `main.rs:15` — `KeybindingRegistry` imported; `register_from_extensions` called at `main.rs:493` |
| `render.rs` | `tokio::sync::mpsc` | `UnboundedReceiver<TuiEvent>` in render_loop | WIRED | `render.rs:29` — `use tokio::sync::{mpsc, watch}`; `event_rx: mpsc::UnboundedReceiver<TuiEvent>` at `render.rs:363` |
| `render.rs` | `extension.rs` | `merge_style_overrides` reads StyleOverrides from extensions | WIRED | `render.rs:167` — `let merged_styles = merge_style_overrides(&extensions)` called in `new_with_extensions` |

---

### Data-Flow Trace (Level 4)

Not applicable — this phase produces trait contracts, registries, and render integration code (not data-fetching components). The artifacts are infrastructure that enable data flow, not end-user data renderers backed by a database.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| extension.rs unit tests (18) | `cargo test "tui::extension::tests"` | 18 passed, 0 failed | PASS |
| keybindings.rs unit tests (16) | `cargo test "tui::keybindings::tests"` | 16 passed, 0 failed | PASS |
| commands.rs unit tests (12) | `cargo test "tui::commands::tests"` | 12 passed, 0 failed | PASS |
| render.rs unit tests (14) | `cargo test "tui::render::tests"` | 14 passed, 0 failed | PASS |
| Static-grep wiring invariants (5) | `cargo test "tui_extension_wiring_tests"` | 5 passed, 0 failed | PASS |
| Full combined run (65 tests) | `cargo test` filter on all above groups | 65 passed, 0 failed | PASS |
| Compilation | `cargo check -p ironhermes-cli` | 0 errors; pre-existing unused-import warnings only (unrelated to phase) | PASS |
| No hardcoded saturating_sub(3) in active paths | grep `saturating_sub(3)` in render.rs | No matches | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| CLI-02 | 22.1-01-PLAN.md, 22.1-02-PLAN.md | CLI extension hooks: extra TUI widgets, keybindings, layout children, process_command, style dict | SATISFIED | `TuiExtension` trait provides all five hook surfaces; wired into render.rs render loop and main.rs REPL; all 65 tests pass |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | — | — | — |

Scan results: No TODO/FIXME/PLACEHOLDER/stub comments in any phase 22.1 source files. No empty `return null` / `return []` patterns in active code paths. The `_scanner_bg_color` and `_status_separator_color` variables are prefixed with `_` (accepted Rust convention for intentionally unused variables — the plan documentation notes T-22.1-05 accepted: colored crate handles unknown color strings gracefully). These are not stubs; the override values are read and stored; applying them to terminal output requires the colored crate's runtime dispatch which is wired for `scanner.lit` and `scanner.trail` in `build_scanner_frame`.

---

### Human Verification Required

None. All observable truths are programmatically verifiable through code inspection and test execution.

---

### Gaps Summary

No gaps. All 13 must-have truths verified. All 6 required artifacts exist, are substantive, and are wired. All 7 key links confirmed. CLI-02 requirement satisfied. 65 tests pass with 0 failures. `cargo check` exits clean (0 errors).

---

_Verified: 2026-04-17T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
