---
phase: 36.14-sse-stream-error-fallback-gap
verified: 2026-05-26T14:30:00Z
status: passed
score: 17/17 must-haves verified
overrides_applied: 0
re_verification: false
---

# Phase 36.14: SSE Stream Error Fallback Gap Verification Report

**Phase Goal:** When an LLM provider returns HTTP 200 with an SSE-body error envelope (OpenRouter/Qwen pattern: `data: {"error":{"message":"...","code":400}}`), the stream consumer must surface it as `StreamEvent::ProviderError`, `call_llm_streaming` must return `Err`, and the existing `run()` fallback/retry block must activate configured `fallback_providers`.
**Verified:** 2026-05-26T14:30:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-01: SSE error detection activates only when ChatStreamChunk deserialization fails AND the data parses as a JSON object with a top-level 'error' key | ✓ VERIFIED | `client.rs` line 420–431: `Err(_parse_err)` arm does second-pass `serde_json::from_str::<serde_json::Value>(data)` then checks `val.get("error").is_some()` before emitting ProviderError |
| 2 | D-02: StreamEvent::ProviderError(String) is added; stream consumer sends it and returns; call_llm_streaming converts to Err | ✓ VERIFIED | `client.rs` line 32: variant declared. Line 425: `tx.send(StreamEvent::ProviderError(msg)).await; return;`. `agent_loop.rs` line 1827–1832: match arm `return Err(anyhow::anyhow!(body))` |
| 3 | D-03: Error string encodes 'code' in '(NNN Reason)' format; unknown/missing codes fall back to '(SSE error)' | ✓ VERIFIED | `sse_error_to_bail_string` lines 61–72: full code→reason match table; `_ => ("SSE error", "SSE error")` for unknown/None codes |
| 4 | D-04: AnthropicClient streaming path is not changed | ✓ VERIFIED | `anthropic_client.rs` not in the commit `6f97e5db` or `75f8c37d` file lists; workspace build exits 0 |
| 5 | D-05: Existing debug! log for unrecognized parse failures is preserved | ✓ VERIFIED | `client.rs` line 430: `debug!("Failed to parse stream chunk: {} — data: {}", _parse_err, data)` retained in the non-error branch |
| 6 | D-06: invariants_36_14.rs static-grep test locks StreamEvent::ProviderError existence in client.rs and match arm in agent_loop.rs | ✓ VERIFIED | `tests/invariants_36_14.rs` exists with 4 `#[test]` fns; all 4 pass |
| 7 | An OpenRouter '(400 Bad Request)' SSE error triggers should_fallback=true | ✓ VERIFIED | Unit test `test_sse_provider_error_400_triggers_fallback` asserts `(false, true)`; integration test `sse_error_400_produces_provider_error_event` asserts msg contains `(400 Bad Request)` |
| 8 | A missing/unknown SSE error code falls back to '(SSE error)' and lands as ProviderError::Unknown (should_fallback=false) | ✓ VERIFIED | Unit test `test_sse_provider_error_unknown_code_no_fallback` asserts `(true, false)`; integration test `sse_error_unknown_code_produces_provider_error_event` asserts msg contains `(SSE error)` |
| 9 | call_llm_streaming returns Err on receiving StreamEvent::ProviderError, not Ok | ✓ VERIFIED | `agent_loop.rs` line 1832: `return Err(anyhow::anyhow!(body))` — not break, not continue |
| 10 | The run() fallback/retry block in agent_loop.rs is NOT modified | ✓ VERIFIED | Commit `6f97e5db --stat` shows only 9 lines added to agent_loop.rs (the match arm); run() block unchanged |
| 11 | Task 1 lands atomically — variant and match arm in the same commit | ✓ VERIFIED | `git log -S "ProviderError(String)"` and `git log -S "StreamEvent::ProviderError(body)"` both point to the same commit `6f97e5db`; `--stat` shows exactly 2 files |
| 12 | extract_sse_error_code handles both numeric (as_u64) and string (as_str().parse::<u16>()) shapes | ✓ VERIFIED | `client.rs` lines 44–48: `as_u64().and_then(|n| u16::try_from(n).ok())` tried first; `as_str().and_then(|s| s.parse::<u16>().ok())` as fallback |
| 13 | A string-code SSE error is classified as fallback exactly like a numeric-code one | ✓ VERIFIED | Unit test `test_sse_provider_error_string_code_400_triggers_fallback` (with comment "Codex MEDIUM #2") asserts `(false, true)`; integration test `sse_error_string_code_400_produces_provider_error_event` passes |
| 14 | Real stream-path integration test exists: HTTP 200 + SSE error data line produces StreamEvent::ProviderError with (NNN Reason) token | ✓ VERIFIED | `tests/sse_provider_error_stream.rs` with 7 wiremock `#[tokio::test]` fns; all 7 pass |
| 15 | invariants_36_14.rs asserts co-occurrence of StreamEvent::ProviderError AND return Err(anyhow in agent_loop.rs | ✓ VERIFIED | `agent_loop_provider_error_returns_err_anyhow_prov07` checks `return Err(anyhow` within 400-byte window of `StreamEvent::ProviderError(body)`; passes |
| 16 | Log hygiene: error string extracts 'message' field and caps raw JSON tail to 512 chars with '... [truncated]' marker | ✓ VERIFIED | `client.rs` lines 73–80: `val.pointer("/error/message").and_then(|m| m.as_str())` extracted; `if raw.len() <= 512` guard with `format!("{}... [truncated]", &raw[..512])` on the else branch |
| 17 | Partial-content-then-error trade-off is documented in a code comment and deferred entry | ✓ VERIFIED | `agent_loop.rs` lines 1828–1831: inline comment "Partial deltas already forwarded to on_stream are observed by the UI; in-function accumulated text is dropped. See phase 36.14 deferred." |

**Score:** 17/17 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/ironhermes-agent/src/client.rs` | StreamEvent::ProviderError(String) variant; extract_sse_error_code; sse_error_to_bail_string; D-01 detection loop | ✓ VERIFIED | All 8 acceptance-criteria substrings confirmed present |
| `crates/ironhermes-agent/src/agent_loop.rs` | StreamEvent::ProviderError(body) match arm returning Err(anyhow); 7 unit tests | ✓ VERIFIED | Match arm at line 1827–1833; 7 unit tests at lines 3384–3453 |
| `crates/ironhermes-agent/tests/invariants_36_14.rs` | 4 static-grep tests with include_str! pattern | ✓ VERIFIED | 63-line file; 4 `#[test]` fns; all pass |
| `crates/ironhermes-agent/tests/sse_provider_error_stream.rs` | 7 wiremock integration tests covering 400/429/500/502/503/unknown/string-code | ✓ VERIFIED | 184-line file; 7 `#[tokio::test]` fns; all pass |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `client.rs` SSE loop `Err` arm | `StreamEvent::ProviderError` | `tx.send(StreamEvent::ProviderError(msg)).await` | ✓ WIRED | Line 425; also returns immediately (D-02) |
| `agent_loop.rs` call_llm_streaming match arm | `run()` Err branch | `return Err(anyhow::anyhow!(body))` | ✓ WIRED | Line 1832; existing run() Err branch unchanged |
| `extract_sse_error_code` | `sse_error_to_bail_string` | called at line 60 | ✓ WIRED | Tolerant numeric+string extraction flows into format fn |
| `sse_error_to_bail_string` | `(NNN Reason)` token in error string | `format!("Streaming chat completion failed ({}):", status_token, ...)` | ✓ WIRED | Token parseable by existing `extract_http_status` in classifier |

---

## Compile Gates

| Gate | Command | Result | Status |
|------|---------|--------|--------|
| Crate build | `cargo build -p ironhermes-agent` | exit 0, 20 warnings (pre-existing), Finished in 20.84s | ✓ PASS |
| Workspace build | `cargo build --workspace` | exit 0, warnings only, Finished in 0.23s (cached) | ✓ PASS |

---

## Behavioral Spot-Checks (Test Gates)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 4 invariant static-grep tests | `cargo test -p ironhermes-agent --test invariants_36_14` | 4 passed; 0 failed | ✓ PASS |
| 7 wiremock integration tests | `cargo test -p ironhermes-agent --test sse_provider_error_stream` | 7 passed; 0 failed | ✓ PASS |
| 7 unit tests in lib | `cargo test -p ironhermes-agent --lib test_sse_provider_error` | 7 passed; 0 filtered out = 387 | ✓ PASS |
| Full crate test suite | `cargo test -p ironhermes-agent` | All binaries exit 0; 0 failed across all test binaries | ✓ PASS |

Full suite breakdown (0 regressions confirmed): 394 + 2 + 6 + 2 + 1 + 2 + 6 + 2 + 1 + 2 + 8 + 7 + 4 + 8 + 8 + 12 + 12 + 34 + 16 + 5 = no failures in any binary.

---

## Atomicity Gate (Codex HIGH #1)

| Check | Result | Status |
|-------|--------|--------|
| `git log -S "ProviderError(String)"` — develop commit | `6f97e5db feat(36.14): wire StreamEvent::ProviderError end-to-end for SSE-body error fallback (PROV-07)` | ✓ SAME COMMIT |
| `git log -S "StreamEvent::ProviderError(body)"` — develop commit | `6f97e5db feat(36.14): wire StreamEvent::ProviderError end-to-end for SSE-body error fallback (PROV-07)` | ✓ SAME COMMIT |
| Task 1 commit file count | `--stat` shows exactly 2 files: `client.rs` (+70 lines), `agent_loop.rs` (+9 lines) | ✓ EXACTLY 2 |
| Task 2 commit file count | `--stat` shows exactly 3 files: `agent_loop.rs` (+83), `invariants_36_14.rs` (+63), `sse_provider_error_stream.rs` (+184) | ✓ EXACTLY 3 |

Note: Commit hashes on develop (`6f97e5db`, `75f8c37d`) differ from SUMMARY.md hashes (`b2db9fce`, `b8ce21fd`) — expected per the operational note; these are the cherry-picked equivalents with identical messages and content.

---

## Documentation Gate

| Item | Status |
|------|--------|
| `.planning/phases/36.14-sse-stream-error-fallback-gap/36.14-01-SUMMARY.md` exists | ✓ EXISTS |

---

## Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| None found | — | — | Zero `TBD`, `FIXME`, or `XXX` markers in any of the 4 phase files | 

---

## Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| PROV-07 | Fallback model switching on primary model failure — extended to SSE-body errors at HTTP 200 | ✓ SATISFIED | End-to-end wiring from SSE error detection → StreamEvent::ProviderError → call_llm_streaming Err → existing run() fallback block; 18 tests (7 unit + 7 integration + 4 invariants) lock the surface |

---

## Human Verification Required

None. All success criteria are verifiable programmatically. The manual smoke test described in the plan (real OpenRouter + misspelled model) is an optional operator confirmation, not a required gate. All automated gates pass.

---

## Gaps Summary

No gaps. All 17 must-have truths are VERIFIED. All 4 artifacts exist, are substantive, and are wired end-to-end. Both compile gates and all 18 test assertions pass with zero regressions.

---

_Verified: 2026-05-26T14:30:00Z_
_Verifier: Claude (gsd-verifier)_
