---
context: phase
phase: 35-cron-subagent-budget-isolation-give-cron-its-own-delegate-ta
task: null
total_tasks: 0
status: context_gathered_ready_to_plan
last_updated: 2026-05-21T14:05:55.950Z
---

<current_state>
Phase 35 discussion is COMPLETE and the roadmap goal has been broadened. No
source code has changed and no plans exist yet. We paused right before
`/gsd-plan-phase 35`.

The phase pivoted during discussion: it started as cron-specific budget
isolation (T-28.1-16) and became a **global delegation-budget-model change**.
All decisions are locked in `35-CONTEXT.md` — read it before doing anything.
</current_state>

<completed_work>

- discuss-phase 35 → `35-CONTEXT.md` + `35-DISCUSSION-LOG.md` (commit 55a04f31)
- Broadened ROADMAP Phase 35 title/goal + STATE roadmap-evolution (commit 86262782)
</completed_work>

<remaining_work>

- Plan Phase 35: `/gsd-plan-phase 35` (not started)
- Then research/execute/verify as normal.
</remaining_work>

<decisions_made>

- **Global per-subagent independent budgets.** Every subagent (interactive + cron)
  gets a fresh `BudgetHandle::new(delegation.max_iterations)` instead of cloning
  the parent's Arc. Resolves T-28.1-16 as a side effect.
- **Retire PROV-10** shared parent↔child counter. Runaway now bounded by
  `max_spawn_depth × max_concurrent_children × delegation.max_iterations`
  (reference-style; no tree-wide ceiling).
- **No new config field** — `delegation.max_iterations` already exists
  (`config.rs:982`, default 50). The change is the wiring at
  `subagent_runner.rs:282-284`.
- **Handoff deferred** to a future phase; **budget refresh dropped** entirely.
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/35-cron-subagent-budget-isolation-give-cron-its-own-delegate-ta/35-CONTEXT.md` — all locked decisions; the source of truth (supersedes the roadmap's earlier framing).
2. `docs/AGENT-RUNTIME-DESIGN.md` §6.4 / §8 — the T-28.1-16 gap description. NOTE: §8's cron-specific "own AgentRuntime" fix is **SUPERSEDED** by the global per-subagent model — read for the gap, not the chosen approach.
3. `/Users/twilson/code/hermes-agent/tools/delegate_tool.py` (:507, :997-1000, :1968-1979) — the reference model being matched (per-subagent own budget, default 50, authoritative).

## Infrastructure State
- No running services tied to this work. All work committed; clean tree (except unrelated `.DS_Store` / `.gitkeep`).
- HEAD at pause: 86262782 on `develop`.

<context>
The key implementation change is small and localized: `subagent_runner.rs:282-284`
currently does `agent = agent.with_budget(self.budget.clone())` (PROV-10 shared
Arc). It must instead give each child a fresh `BudgetHandle::new(config.delegation.max_iterations)`.
The doc-comment at `subagent_runner.rs:34-39` describing the SHARED budget becomes
false and must be rewritten, and the PROV-10 shared-counter regression test(s)
must be inverted to assert independence. Threat model must document the new
runaway bound. Verification needs: (1) child drains own budget → parent unchanged;
(2) cron job delegates to exhaustion → interactive budget full; (3) model-supplied
max_iterations cannot override config.
</context>

<next_action>
Start with: `/gsd-plan-phase 35` (after reading `35-CONTEXT.md`).
</next_action>
