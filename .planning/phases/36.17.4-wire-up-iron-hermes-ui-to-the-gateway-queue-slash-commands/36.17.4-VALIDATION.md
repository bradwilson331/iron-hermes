---
phase: 36.17.4
slug: wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-28
---

# Phase 36.17.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Sourced from RESEARCH.md `## Validation Architecture` (D-01..D-12 + FIFO/cap/pause unit assertions).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in test (`#[test]`, `cargo test`) |
| **Config file** | None — standard Cargo workspace test runner |
| **Quick run command** | `cargo test -p iron_hermes_ui` |
| **Full suite command** | `cargo test -p iron_hermes_ui && cargo test -p ironhermes-gateway && cargo test -p ironhermes-cli --features test-support && cargo check --workspace --all-features` |
| **Estimated runtime** | ~90 seconds (workspace test) |

---

## Sampling Rate

- **After every task commit:** Run `cargo check -p iron_hermes_ui`
- **After every plan wave:** Run `cargo test -p iron_hermes_ui`
- **Before `/gsd:verify-work`:** Full D-12 suite must be green
- **Max feedback latency:** ~30 seconds (per-crate test)

---

## Per-Task Verification Map

| Req ID | Behavior | Test Type | Automated Command | File |
|--------|----------|-----------|-------------------|------|
| D-01 | `AppState` has `queue: Arc<dyn MessageQueue<SessionKey>>` field | Source-text grep | `grep -c 'queue: Arc<dyn.*MessageQueue' crates/iron_hermes_ui/src/server/state.rs` | `web_queue_drain.rs` |
| D-01a | `AppState` has `queue_paused` map + `get_or_create_paused_flag` method | Source-text grep | `grep -c 'get_or_create_paused_flag' crates/iron_hermes_ui/src/server/state.rs` | `web_queue_drain.rs` |
| D-01a | `get_or_create_paused_flag` returns same Arc on second call | Unit | `cargo test -p iron_hermes_ui paused_flag_idempotent` | `web_queue_controls.rs` |
| D-02 | Drain fires in `None =>` arm after turn completes | Source-text grep | `grep -c 'queue.pop' crates/iron_hermes_ui/src/server/ws.rs` | `web_queue_drain.rs` |
| D-02 | `QueueUpdated` emitted before next spawn (ordering) | Source-text position grep | offset of `QueueUpdated` < offset of `tokio::spawn` in drain block | `web_queue_drain.rs` |
| D-03 | `ChatStreamEvent::QueueUpdated { depth, paused }` variant exists | Serde shape test | `cargo test -p iron_hermes_ui test_queue_updated_json_shape` | `protocol.rs` (inline) |
| D-03 | JSON shape locked: `{"QueueUpdated":{"depth":3,"paused":false}}` | Serde round-trip | same | `protocol.rs` (inline) |
| D-03a | `hermes_app/mod.rs` has `QueueUpdated` match arm + `queue_state.set(...)` | Source-text grep | `grep -c 'QueueUpdated' crates/iron_hermes_ui/src/components/hermes_app/mod.rs` | `web_queue_protocol.rs` |
| D-04 | `/new` calls `queue.clear` BEFORE `reset_web_session` | Source-text ordering | offset of `queue.clear` < offset of `reset_web_session` in NewSession arm | `web_queue_controls.rs` |
| D-04a | `/stop` arm sequence: clear → paused.store → `QueueUpdated` → Delta → Finished | Source-text ordering | sequential grep of these strings in stop arm | `web_queue_controls.rs` |
| D-05 | `/stop` does NOT call `handle.abort()` or `cancel_token.cancel()` | Source-text negative | `grep -c 'abort\|cancel_token.cancel' stop_arm` == 0 | `web_queue_controls.rs` |
| D-06 | Cap-hit emits `"Queue is full (128/128)..."` | Source-text grep | `grep -c 'Queue is full' crates/iron_hermes_ui/src/server/ws.rs` | `web_queue_controls.rs` |
| D-07 | `/unpause` is NOT `/resume` alias | Registry test | `cargo test -p ironhermes-core registry_no_duplicate_aliases` | existing core tests |
| D-08 | `is_bypass("pause")` returns true | Unit | `cargo test -p ironhermes-core is_bypass_locked_list` | `running_agent.rs` tests |
| D-08 | `is_bypass("unpause")` returns true | Unit | `cargo test -p ironhermes-core is_bypass_locked_list` | `running_agent.rs` tests |
| D-11 | `QueueUpdated` serde shape locked | Serde test | `cargo test -p iron_hermes_ui test_queue_updated_json_shape` | `protocol.rs` (inline) |
| D-12 | No regression in gateway tests | Full suite | `cargo test -p ironhermes-gateway` | CI |
| D-12 | No regression in CLI/TUI tests | Full suite | `cargo test -p ironhermes-cli --features test-support` | CI |
| FIFO | FIFO ordering: push A, B, C → pop returns A, B, C | Unit | `cargo test -p iron_hermes_ui queue_fifo_ordering` | `web_queue_drain.rs` |
| PAUSE | Pause blocks drain, unpause resumes FIFO | Unit | `cargo test -p iron_hermes_ui queue_pause_drain_semantics` | `web_queue_drain.rs` |
| CAP | 128th push succeeds, 129th returns `CapacityReached` | Unit | `cargo test -p iron_hermes_ui queue_cap_at_128` | `web_queue_drain.rs` |
| MULTI | Multi-tab depth drift accepted (CONTEXT D-02a) | Manual / comment | — | PLAN.md note |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky (filled during execution)*

---

## Wave 0 Requirements

- [ ] `crates/iron_hermes_ui/tests/web_queue_drain.rs` — covers D-01, D-01a, D-02, D-02 ordering, FIFO/PAUSE/CAP units
- [ ] `crates/iron_hermes_ui/tests/web_queue_controls.rs` — covers D-04, D-04a, D-05, D-06, D-08 (per-session paused-flag unit)
- [ ] `crates/iron_hermes_ui/tests/web_queue_protocol.rs` — covers D-03a (QueueUpdated arm in `hermes_app/mod.rs`)
- [ ] `crates/iron_hermes_ui/src/protocol.rs` `#[cfg(test)]` module — add `test_queue_updated_json_shape` alongside existing `test_subagent_event_json_shape`
- [ ] `crates/ironhermes-core/src/commands/running_agent.rs` test for `is_bypass("pause")` + `is_bypass("unpause")` (Pitfall 1 fix — see RESEARCH Finding 4)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Pill renders `Queue: N` then disappears when N=0 | D-03a hide-when-zero | Visual / wasm runtime; no headless wasm test in iron_hermes_ui | `dx serve --platform web` + open two tabs on same session_id, push `/queue foo` `/queue bar` `/queue baz` mid-turn, observe pill increment + decrement |
| Multi-tab queue sharing (same session_id) | D-01 | Visual / wasm runtime | Two tabs on same session_id — confirm `/queue` in tab A is drained by either tab; tab A's pill updates, tab B's drifts (accepted divergence per D-02a) |
| `/stop` clears queue without aborting in-flight turn | D-05 documented divergence | Visual / timing | While turn in flight, send `/stop`; confirm current Delta stream continues, queue cleared, Delta `"Queue cleared. Current turn finishing.\n"` appears |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify command or Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING test files (web_queue_drain, web_queue_controls, web_queue_protocol, protocol.rs inline shape test, is_bypass unit test)
- [ ] No watch-mode flags (`cargo test` is one-shot)
- [ ] Feedback latency < 90s (workspace test)
- [ ] `nyquist_compliant: true` set in frontmatter (flip after `/gsd:execute-phase` completes Wave 0)

**Approval:** pending
