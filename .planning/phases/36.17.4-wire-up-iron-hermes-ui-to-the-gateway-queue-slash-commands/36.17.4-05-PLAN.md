---
phase: 36.17.4
plan: 05
type: execute
wave: 2
depends_on:
  - 36.17.4-02
files_modified:
  - crates/iron_hermes_ui/src/components/hermes_app/mod.rs
  - crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs
autonomous: true
requirements:
  - D-03a
  - D-03b
threats: []
must_haves:
  truths:
    - "`HermesApp` declares `let mut queue_state = use_signal(|| (0u32, false));` alongside the existing `subagent_events` signal (line 131)."
    - "The WS recv loop in `HermesApp`'s `use_future` body has a new match arm `ChatStreamEvent::QueueUpdated { depth, paused } => { queue_state.set((depth, paused)); }`."
    - "`HermesApp` calls `use_context_provider(|| queue_state);` alongside the existing `subagent_events` provider (line 334)."
    - "`AppFooter` consumes the signal via `let queue_state = use_context::<Signal<(u32, bool)>>();` and reads it into Copy locals BEFORE the RSX tree via `let (queue_depth, queue_paused) = *queue_state.read();` (signal borrow does not span RSX, satisfies clippy.toml rule)."
    - "`AppFooter` renders a `QUEUE <span class=v>N</span>` (or `QUEUE <span class=v>N (paused)</span>`) pill conditionally on `queue_depth > 0` (hide-when-zero discipline per D-09 carry-forward from 36.17.3)."
    - "Pill placement is inside the existing `div.app-footer` left-side run, after the `AGENT ...` span and its `span.sep` separator (planner discretion per D-03b — chosen for visual parity with the TUI status_line.rs which puts the queue pill in the left run)."
    - "Existing `subagent_events`, `is_ws_connected`, `active_screen`, `clock_str` wiring is unchanged."
  artifacts:
    - path: "crates/iron_hermes_ui/src/components/hermes_app/mod.rs"
      provides: "queue_state Signal<(u32, bool)> + QueueUpdated match arm + context provider"
      contains: "queue_state.set"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs"
      provides: "queue_state context consumer + Queue: N pill with hide-when-zero"
      contains: "queue_depth > 0"
  key_links:
    - from: "crates/iron_hermes_ui/src/components/hermes_app/mod.rs (WS recv loop)"
      to: "crates/iron_hermes_ui/src/protocol.rs::ChatStreamEvent::QueueUpdated"
      via: "deserialize + signal.set"
      pattern: "ChatStreamEvent::QueueUpdated \\{ depth, paused \\}"
    - from: "crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs"
      to: "crates/iron_hermes_ui/src/components/hermes_app/mod.rs::queue_state"
      via: "Signal<(u32, bool)> via use_context"
      pattern: "use_context::<Signal<\\(u32, bool\\)>>"
---

<objective>
Land the wasm-side queue UI: a `Signal<(u32, bool)>` in `HermesApp` updated
from `QueueUpdated` events shipped by Plan 02, exposed via context to
`AppFooter`, and rendered as a `QUEUE N` / `QUEUE N (paused)` pill with the
hide-when-zero discipline that mirrors the TUI 36.17.3 status_line.rs
behavior.

Purpose: D-03a (status pill + Delta lines render through existing paths),
D-03b (planner picks render site — left run of app-footer immediately after
`AGENT DEFAULT`).

Output: Two file edits — `hermes_app/mod.rs` gains a signal, a match arm,
and a context provider; `hermes_app/app_footer.rs` consumes the signal and
adds the conditional pill render. Parallel-safe with Plan 04 (different
files entirely). Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-CONTEXT.md
@.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-RESEARCH.md
@.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-PATTERNS.md
@crates/iron_hermes_ui/src/components/hermes_app/mod.rs
@crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs
@crates/iron_hermes_ui/CLAUDE.md

<interfaces>
<!-- Existing precedent in mod.rs (the signal pattern to mirror):
       line 131: let mut subagent_events = use_signal(|| 0u64);
       lines 244-249 (SubagentEvent arm in WS recv loop):
         crate::protocol::ChatStreamEvent::SubagentEvent {} => {
             let cur = *subagent_events.read();
             subagent_events.set(cur + 1);
         }
       line 334: use_context_provider(|| subagent_events);

     Existing AppFooter shape (app_footer.rs:19-61):
       line 19: let active_screen = use_context::<Signal<Screen>>();
       line 20: let mut clock = use_signal(now_time_utc);
       line 40: let label = screen_label(*active_screen.read());
       line 41: let clock_str = clock.read().clone();
       lines 43-60: rsx! { div { class: "app-footer",
         span { "NODE " ... } span { class: "sep" }
         span { "SCREEN " ... } span { class: "sep" }
         span { "AGENT " span { class: "v", "DEFAULT" } }
         div { class: "app-footer-right", ... }
       }}

     New surfaces this plan ships:
       mod.rs: let mut queue_state = use_signal(|| (0u32, false));
       mod.rs: QueueUpdated arm: queue_state.set((depth, paused));
       mod.rs: use_context_provider(|| queue_state);
       app_footer.rs: let queue_state = use_context::<Signal<(u32, bool)>>();
       app_footer.rs: let (queue_depth, queue_paused) = *queue_state.read();
       app_footer.rs: conditional `if queue_depth > 0 { rsx!{...} }` pill

     Clippy.toml constraint (per iron_hermes_ui CLAUDE.md):
       Signal `.read()` borrows must NOT span `.await` or RSX. Copy-deref
       into locals first (the `(u32, bool)` tuple is Copy). The `set` call
       in the match arm is synchronous — constraint trivially satisfied.
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HermesApp — declare queue_state signal, add QueueUpdated match arm, provide via context</name>
  <files>crates/iron_hermes_ui/src/components/hermes_app/mod.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/components/hermes_app/mod.rs (read full file — confirm precise locations of: subagent_events declaration at line 131; the use_future WS recv loop with the inner match on `ChatStreamEvent` between lines 176-264; the SubagentEvent arm at 244-249; the use_context_provider block around 324-335)
    - crates/iron_hermes_ui/src/protocol.rs (confirm `ChatStreamEvent::QueueUpdated { depth: u32, paused: bool }` is shipped from Plan 02)
    - .planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-CONTEXT.md (D-03a — signal pattern; D-03b — render-location discretion)
    - .planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-PATTERNS.md (`hermes_app/mod.rs` section — exact insertions)
    - crates/iron_hermes_ui/CLAUDE.md (Dioxus 0.7 conventions; signal-borrow-across-await rule)
  </read_first>
  <behavior>
    - Compiles for the `web` feature (wasm target).
    - `queue_state` starts at `(0, false)` on `HermesApp` mount.
    - A WS frame `{"QueueUpdated":{"depth":3,"paused":false}}` deserializes into `ChatStreamEvent::QueueUpdated { depth: 3, paused: false }` and triggers `queue_state.set((3, false))`.
    - The signal is reachable from descendants of `HermesApp` via `use_context::<Signal<(u32, bool)>>()` (consumed by `AppFooter` in Task 2 of this plan).
    - All other existing arms (Delta / ToolCallStart / ToolCallEnd / Finished / Error / SubagentEvent) remain untouched.
  </behavior>
  <action>
    Edit `crates/iron_hermes_ui/src/components/hermes_app/mod.rs` in three places, all alongside the existing `subagent_events` precedent.

    Step A — Signal declaration: immediately after line 131 `let mut subagent_events = use_signal(|| 0u64);` add `let mut queue_state = use_signal(|| (0u32, false));` with a doc-style line comment above it: `// Phase 36.17.4 (D-03a): queue pill state — (depth, paused). Updated on every QueueUpdated event from the server (Plan 02 protocol variant). Consumed by AppFooter via use_context.`

    Step B — WS recv-loop match arm: locate the inner `match event { ... }` block whose existing arms include `ChatStreamEvent::SubagentEvent {}` at lines 244-249. Immediately AFTER that arm (after the closing brace of the SubagentEvent arm), add a new arm: `crate::protocol::ChatStreamEvent::QueueUpdated { depth, paused } => { queue_state.set((depth, paused)); }`. Above the arm, add a one-line comment: `// Phase 36.17.4 (D-03a): (u32, bool) is Copy — direct .set call satisfies signal-borrow-across-await clippy rule (no .await in this arm).`

    Step C — Context provider: locate the `use_context_provider(|| subagent_events);` line at 334 and add immediately after it: `use_context_provider(|| queue_state);` with a comment: `// Phase 36.17.4 (D-03a): expose queue_state to AppFooter via context.`

    Do NOT modify any other arm, signal, or use_context_provider. Do NOT change the signature of `HermesApp`. Do NOT add any new imports — `crate::protocol::ChatStreamEvent` is already in scope from the existing arms.
  </action>
  <verify>
    <automated>cargo check -p iron_hermes_ui --features web --target wasm32-unknown-unknown 2>&1 | tail -20 || cargo check -p iron_hermes_ui --features server</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'let mut queue_state = use_signal' crates/iron_hermes_ui/src/components/hermes_app/mod.rs` returns 1.
    - `grep -c 'ChatStreamEvent::QueueUpdated' crates/iron_hermes_ui/src/components/hermes_app/mod.rs` returns 1.
    - `grep -c 'queue_state.set((depth, paused))' crates/iron_hermes_ui/src/components/hermes_app/mod.rs` returns 1.
    - `grep -c 'use_context_provider(|| queue_state)' crates/iron_hermes_ui/src/components/hermes_app/mod.rs` returns 1.
    - Ordering: position of `let mut queue_state = use_signal` < position of `ChatStreamEvent::QueueUpdated` < position of `use_context_provider(|| queue_state)` in the source file (declaration, then update arm, then provider — the natural top-to-bottom flow of HermesApp).
    - `cargo check -p iron_hermes_ui` exits 0 with any feature combination the project supports for non-wasm checking.
  </acceptance_criteria>
  <done>
    HermesApp declares the queue_state signal, updates it from QueueUpdated events, and provides it via context. No other behavior changes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AppFooter — consume queue_state via context and render the conditional Queue: N pill</name>
  <files>crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs (read full file — confirm structure of lines 19-61: imports/use_context/clock setup at 19-41, the rsx! tree at 43-60)
    - .planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-PATTERNS.md (`app_footer.rs` section — exact pill render shape)
    - crates/iron_hermes_ui/CLAUDE.md (Dioxus 0.7 conventions; conditional rendering with `if condition { ... }` directly in RSX)
    - crates/ironhermes-cli/src/tui_rata/status_line.rs (parity reference — TUI's `Queue: N` / `Queue: N (paused)` pill format for visual consistency)
  </read_first>
  <behavior>
    - Compiles.
    - When `queue_depth == 0` the pill is absent from the rendered DOM (no separator either, no `QUEUE` span).
    - When `queue_depth > 0` and `queue_paused == false`, the rendered fragment is `<span class="sep"></span><span>QUEUE <span class="v">N</span></span>` where N is the depth.
    - When `queue_depth > 0` and `queue_paused == true`, the value span text is `N (paused)`.
    - All existing footer content (NODE / SCREEN / AGENT / MEM / SKILLS / P50 / clock) is unchanged.
    - No signal borrow spans `.await` or the RSX tree (the tuple is Copy-deref'd into locals at the top, mirroring the existing `*active_screen.read()` and `clock.read().clone()` pattern).
  </behavior>
  <action>
    Edit `crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs` in two places:

    Step A — Context + local read: immediately after line 19 `let active_screen = use_context::<Signal<Screen>>();` add `let queue_state = use_context::<Signal<(u32, bool)>>();` with comment `// Phase 36.17.4 (D-03a): queue pill state provided by HermesApp.`. Then, immediately after line 41 `let clock_str = clock.read().clone();`, add `let (queue_depth, queue_paused) = *queue_state.read();` with comment `// Phase 36.17.4: read into Copy locals before RSX (clippy.toml: signal borrow must not span RSX).`.

    Step B — Pill render: inside the existing `rsx! { div { class: "app-footer", ... } }` block at lines 43-60, locate the line `span { "AGENT " span { class: "v", "DEFAULT" } }` (this is the last span before the `div { class: "app-footer-right", ... }` group). Immediately AFTER that AGENT span, insert a conditional block: `if queue_depth > 0 { span { class: "sep" } span { "QUEUE " span { class: "v", if queue_paused { format!("{} (paused)", queue_depth) } else { format!("{}", queue_depth) } } } }`. The `if queue_depth > 0` wraps BOTH the separator AND the QUEUE span so the separator is also hidden when the pill is absent. The inner `if queue_paused { ... } else { ... }` is Dioxus 0.7 conditional-attribute syntax — verify it parses correctly inside the `span { class: "v", ... }` content position; if it does not, fall back to `if queue_paused { rsx!{ span { class: "v", "{queue_depth} (paused)" } } } else { rsx!{ span { class: "v", "{queue_depth}" } } }` (two-branch rsx! arms) and put both inside the outer `if queue_depth > 0` block.

    Use the EXACT identifiers `queue_depth` and `queue_paused` so Plan 06's source-grep can locate them.

    Do NOT modify any other span, the right-side div, the clock, the active_screen wiring, or the `now_time_utc` use_future. Do NOT add new imports — `Signal`, `use_context`, `rsx!` are already in scope.
  </action>
  <verify>
    <automated>cargo check -p iron_hermes_ui --features server 2>&1 | tail -30 ; cargo check -p iron_hermes_ui 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'use_context::<Signal<(u32, bool)>>()' crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs` returns 1.
    - `grep -c 'let (queue_depth, queue_paused) = \*queue_state.read()' crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs` returns 1.
    - `grep -c 'queue_depth > 0' crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs` returns 1 (hide-when-zero discipline).
    - `grep -c 'QUEUE ' crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs` returns 1 (the pill label).
    - `grep -c '(paused)' crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs` returns 1 (paused-state branch).
    - `cargo check -p iron_hermes_ui` exits 0.
    - The diff to app_footer.rs touches only the lines between (a) the use_context block at 19 and the let bindings at 41, and (b) the rsx! tree between the AGENT span and the `div.app-footer-right` group — no edits outside these regions.
  </acceptance_criteria>
  <done>
    AppFooter consumes the queue_state signal, reads it into Copy locals before RSX, and renders the conditional QUEUE pill with hide-when-zero and paused-aware formatting.
  </done>
</task>

</tasks>

<verification>
- `cargo check -p iron_hermes_ui` green for any non-wasm feature build (verifies type wiring across both files).
- `cargo check -p iron_hermes_ui --features web --target wasm32-unknown-unknown` green if the workspace supports the wasm target locally (the executor should attempt this check but fall back to non-wasm check if the toolchain target is not installed; document either outcome in SUMMARY).
- Manual visual verification (deferred to phase-level UAT per VALIDATION.md "Manual-Only Verifications"): pill renders + hides correctly + reflects paused state.
</verification>

<success_criteria>
- D-03a: `queue_state: Signal<(u32, bool)>` lives on HermesApp, updates from `QueueUpdated`, and is read by `AppFooter` to drive the pill.
- D-03b discretion: pill is placed in the left run of `div.app-footer` immediately after the `AGENT DEFAULT` span, before the `div.app-footer-right` block.
- Hide-when-zero discipline preserved (no DOM noise when queue is empty).
- No clippy.toml signal-borrow rule violation introduced (the `.read()` is dereferenced into Copy locals before the RSX tree, mirroring existing `*active_screen.read()` at line 40).
</success_criteria>

<output>
Create `.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-05-SUMMARY.md` when done.
</output>
