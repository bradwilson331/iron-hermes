---
phase: 36.17.4
plan: 04
type: execute
wave: 2
depends_on:
  - 36.17.4-01
  - 36.17.4-02
  - 36.17.4-03
files_modified:
  - crates/iron_hermes_ui/src/server/ws.rs
autonomous: true
requirements:
  - D-01
  - D-02
  - D-02a
  - D-02b
  - D-03
  - D-04
  - D-04a
  - D-05
  - D-05a
  - D-06
  - D-07
  - D-08
threats:
  - T-36.17.4-02
  - T-36.17.4-03
must_haves:
  truths:
    - "When a turn is in flight and a free-text (non-slash) message arrives, `ws.rs` calls `app_state.queue.try_push(&key, message)` instead of emitting the hard `Another request is already in progress` error (D-01 / D-03 — replaces the reject at lines 204-214)."
    - "Successful `try_push` emits Delta `Queued: \"<text>\" (N in queue)\\n` + `QueueUpdated { depth: N, paused }` + `Finished { total_tokens: 0 }` in that order, all written to the socket before `continue`."
    - "Cap-hit (`QueueError::CapacityReached`) emits Delta `Queue is full (128/128). /stop or /flush to drain.\\n` + `Finished` (D-06 — uses the `max` field from the error, not a hardcoded literal)."
    - "Slash dispatch handles `CommandResult::Queued { message }`: try_push + Delta + `QueueUpdated` + Finished (same as the in-flight path) with the same cap-hit fork."
    - "Slash dispatch handles `CommandResult::PauseQueue`: `fetch_xor(true, SeqCst)` toggle on the per-session paused flag, emit `QueueUpdated { depth, paused: new_paused }` + Delta `Queue paused. (N queued)\\n` or `Queue resumed. (N queued)\\n` + Finished."
    - "Slash dispatch handles `CommandResult::UnpauseQueue`: `swap(false, SeqCst)` on the paused flag, emit `QueueUpdated { depth, paused: false }` + Delta `Queue resumed.\\n` (if was paused) or `Queue was not paused.\\n` (if was not) + Finished."
    - "`CommandResult::NewSession` arm (D-04): `app_state.queue.clear(&key)` AND `get_or_create_paused_flag.store(false, SeqCst)` AND emit `QueueUpdated { depth: 0, paused: false }` ALL BEFORE the existing `app_state.reset_web_session(&session_id)` call."
    - "Slash interception block adds an early `if def.name == \"stop\"` branch (D-04a / D-05): `queue.clear` → `paused.store(false, SeqCst)` → emit `QueueUpdated { depth: 0, paused: false }` → emit Delta `Queue cleared. Current turn finishing.\\n` → emit Finished → `continue`. NO `JoinHandle::abort()`, NO `cancel_token.cancel()` — the in-flight turn completes naturally (D-05)."
    - "WS recv-loop self-drain (D-02): the `None =>` branch in arm 2 (lines 486-498), AFTER the existing `turn.handle.await`, constructs the SessionKey, reads the paused flag, and IF `!paused` AND `queue.pop(&key)` returns `Some(text)`, emits `QueueUpdated { depth: depth_after, paused: false }` to the socket and spawns a new `run_web_turn` with the popped text (mirrors the primary spawn block in arm 1), assigning the result to `in_flight_turn`."
    - "All queue events use `SessionKey { platform: Platform::Web, chat_id: session_id.clone(), user_id: Some(\"web\".into()) }` per D-01."
    - "`is_bypass(&def.name)` at line 242 is left untouched — Plan 01 already extended the core list to include `pause` and `unpause` (D-08)."
  artifacts:
    - path: "crates/iron_hermes_ui/src/server/ws.rs"
      provides: "Queue push (replacing reject), drain (None arm), slash arms for Queued/PauseQueue/UnpauseQueue, NewSession ordering, /stop early-intercept"
      contains: "app_state.queue.try_push"
  key_links:
    - from: "crates/iron_hermes_ui/src/server/ws.rs (lines ~204-214 replacement)"
      to: "crates/iron_hermes_ui/src/server/state.rs::AppState::queue"
      via: "FIFO push on in-flight reception"
      pattern: "queue\\.try_push\\(&key"
    - from: "crates/iron_hermes_ui/src/server/ws.rs (None => arm, lines ~486)"
      to: "crates/iron_hermes_ui/src/server/state.rs::AppState::queue"
      via: "FIFO pop + immediate re-spawn"
      pattern: "queue\\.pop\\(&key\\)"
    - from: "crates/iron_hermes_ui/src/server/ws.rs (CommandResult arms)"
      to: "crates/iron_hermes_ui/src/protocol.rs::ChatStreamEvent::QueueUpdated"
      via: "typed event emission ordering: state-change → QueueUpdated → Delta → Finished"
      pattern: "ChatStreamEvent::QueueUpdated"
---

<objective>
Wire the WS recv-loop in `crates/iron_hermes_ui/src/server/ws.rs` to use the
queue + paused-flag infrastructure shipped in Plan 03 and the typed event
shipped in Plan 02. This is the behavior-change plan of the phase: after
this lands, free-text messages received during an in-flight turn enqueue
instead of rejecting, slash commands route through the queue, and turn
completion automatically drains the next queued item.

Purpose: D-01/D-02/D-02a/D-02b (FIFO queue + WS recv-loop self-drain),
D-03 (typed event surface), D-04/D-04a (slash ordering invariants),
D-05/D-05a (documented `/stop` divergence — clear-queue-only, no abort),
D-06 (cap-hit Delta string), D-07 (`/unpause` not `/resume`), D-08 (uses
the `is_bypass` Plan 01 already extended).

Output: One large file modification covering six insertion sites. All
follow the canonical slash result delivery shape at lines 318-330 (Delta +
Finished, drop tx, drain rx → socket, `continue`). No `JoinHandle::abort`,
no new `tokio::sync::Notify`, no fourth `tokio::select!` arm — the drain
trigger reuses the existing `None =>` branch in arm 2 per RESEARCH Finding
1 recommendation (simplest Option A variant).

This plan depends on Plans 01, 02, 03 — all Wave 1, all parallel-safe with
each other. Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-CONTEXT.md
@.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-RESEARCH.md
@.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-PATTERNS.md
@crates/iron_hermes_ui/src/server/ws.rs
@crates/iron_hermes_ui/src/server/state.rs
@crates/iron_hermes_ui/src/protocol.rs

<interfaces>
<!-- Plan 03 has shipped on AppState (state.rs):
       pub queue: Arc<dyn MessageQueue<SessionKey>>,
       pub queue_paused: Arc<std::sync::Mutex<HashMap<String, Arc<AtomicBool>>>>,
       pub fn get_or_create_paused_flag(&self, session_id: &str) -> Arc<AtomicBool>

     Plan 02 has shipped on protocol.rs:
       ChatStreamEvent::QueueUpdated { depth: u32, paused: bool }

     Plan 01 has shipped in ironhermes-core::commands::running_agent:
       is_bypass now accepts "pause" and "unpause" — the line-242 gate at
       `if running_flag.load(SeqCst) && !is_bypass(&def.name)` will let
       /pause and /unpause through unchanged.

     Existing ws.rs landmarks (verified via grep):
       line 107:  let mut in_flight_turn: Option<InFlightTurn> = None;
       lines 125+: arm 1 socket.recv_raw()
       lines 198-202: WR-02 is_finished() opportunistic clear
       lines 204-214: in-flight HARD REJECT (the block being replaced)
       line 233:  if message.starts_with('/') { ... slash interception }
       line 242:  if running_flag.load(SeqCst) && !is_bypass(&def.name) { ... }
       lines 302-317: existing CommandResult match arms (Output/Error/NewSession/Handled/Quit)
       line 308:  app_state.reset_web_session(&session_id); -- D-04 ordering pivot
       lines 318-330: canonical slash result delivery
       line 452:  arm 2 turn.rx.recv()
       lines 486-498: arm 2 None => branch (the drain trigger)
       line 502:  arm 3 keepalive.tick()

     SessionKey helper (used in 6+ call sites — extract as inline local fn):
       fn web_key(session_id: &str) -> SessionKey {
           SessionKey { platform: Platform::Web,
                        chat_id: session_id.to_string(),
                        user_id: Some("web".into()) }
       }
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire queue push + slash command arms (replace in-flight reject, add Queued/PauseQueue/UnpauseQueue arms, fix /new ordering, intercept /stop early)</name>
  <files>crates/iron_hermes_ui/src/server/ws.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/ws.rs (read full file — at minimum offset 1 limit 600 then offset 600 limit 200 to cover the recv-loop + slash block + None branch)
    - crates/iron_hermes_ui/src/server/state.rs (offset 25, limit 250 — AppState fields, init, get_or_create_running_flag, get_or_create_paused_flag, run_web_turn signature)
    - crates/iron_hermes_ui/src/protocol.rs (full file — confirm `QueueUpdated { depth: u32, paused: bool }` is shipped from Plan 02)
    - crates/ironhermes-core/src/queue.rs (`MessageQueue` trait method signatures: `try_push`, `pop`, `len`, `clear`; `QueueError::CapacityReached { session_key, max }`)
    - crates/ironhermes-core/src/session.rs (`SessionKey` struct shape — confirm `user_id: Option<String>`)
    - crates/ironhermes-core/src/types.rs (or wherever `Platform::Web` lives — confirm enum location)
    - crates/ironhermes-core/src/commands/mod.rs (`CommandResult::Queued { message: String }`, `CommandResult::PauseQueue`, `CommandResult::UnpauseQueue` — confirm variant shapes shipped by 36.17.3 Plan 02)
    - .planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-CONTEXT.md (D-01..D-08 — all decisions implemented by this plan)
    - .planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-RESEARCH.md (Research Finding 1 — the recommended simplest Option A drain trigger pattern; Finding 5 — slash interception block shape; Finding 4 — confirms Plan 01 has extended is_bypass)
    - .planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-PATTERNS.md (`ws.rs` section — exact insertable code shapes for in-flight push, all three CommandResult arms, NewSession ordering, /stop early intercept)
    - .planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-05-PLAN.md (parity precedent — the TUI slash arms for /queue /pause /unpause /new /reset /stop ordering invariants)
  </read_first>
  <behavior>
    - Free-text message during in-flight turn enqueues; cap-hit emits the locked Delta string; no `Another request is already in progress` Error event is emitted on this path anymore.
    - `/queue <text>` while idle OR while in-flight queues via `CommandResult::Queued`, never spawns a new turn directly.
    - `/pause` toggles the per-session paused flag via `fetch_xor(true, SeqCst)`, emits the depth-aware Delta `Queue paused. (N queued)\n` (if newly paused) or `Queue resumed. (N queued)\n` (if previously paused — fetch_xor's toggle semantics).
    - `/unpause` does explicit `swap(false, SeqCst)`. If was paused → `Queue resumed.\n`; if was already false → `Queue was not paused.\n`.
    - `/new` and `/reset` (both routed through `CommandResult::NewSession`) clear queue + reset paused + emit `QueueUpdated { depth: 0, paused: false }` BEFORE `app_state.reset_web_session(&session_id)`.
    - `/stop` intercepted at `def.name == "stop"` BEFORE the existing dispatch (mirrors RESEARCH Finding 5 pattern). Performs the D-04a sequence: queue.clear → paused.store(false) → QueueUpdated → Delta `Queue cleared. Current turn finishing.\n` → Finished → drain rx → continue. Does NOT call `JoinHandle::abort` (D-05). Does NOT call any cancel_token (no CancellationToken exists on AppState — D-05 documented divergence).
    - WS recv-loop self-drain (D-02): in arm 2's `None =>` branch (after the existing `turn.handle.await`), if the per-session paused flag is false AND `queue.pop(&key)` returns `Some(text)`, send a `QueueUpdated` socket frame, then construct a new `(tx, rx) = mpsc::unbounded_channel()`, spawn `run_web_turn` mirroring the primary spawn block in arm 1, and assign `in_flight_turn = Some(InFlightTurn { session_id, rx, handle })`. If paused OR queue empty, leave `in_flight_turn = None` and the loop continues as it does today (next inbound frame or keepalive tick).
  </behavior>
  <action>
    Apply six edits to `crates/iron_hermes_ui/src/server/ws.rs`, all bound by the existing canonical patterns (slash result delivery shape lines 318-330; SessionKey construction; mpsc tx/rx + drop(tx) + drain rx → socket idiom).

    Edit A — Add a private local helper at the top of the `ws_chat` function (above the recv-loop, just after the in_flight_turn / keepalive declarations around line 109) to avoid SessionKey duplication: `fn web_key(session_id: &str) -> ironhermes_core::session::SessionKey { ironhermes_core::session::SessionKey { platform: ironhermes_core::types::Platform::Web, chat_id: session_id.to_string(), user_id: Some("web".into()) } }`. Confirm the exact `Platform` import path by reading the existing ws.rs imports — adjust if the file already re-exports under a different alias. Use this helper in every queue call site below.

    Edit B — Replace the in-flight HARD REJECT block at lines 204-214. The current block emits `ChatStreamEvent::Error { message: "Another request is already in progress".to_string() }` whenever `in_flight_turn.is_some()`. Replace it with a guard `if in_flight_turn.is_some() && !message.starts_with('/')` (the slash path falls through to line 233 unchanged so slash commands still get intercepted). Inside the new guard: build `let key = web_key(&session_id);`, build `let paused_flag = app_state.get_or_create_paused_flag(&session_id);` and `let paused_snapshot = paused_flag.load(std::sync::atomic::Ordering::SeqCst);`, then match on `app_state.queue.try_push(&key, message.clone())`. On `Ok(())`: `let depth = app_state.queue.len(&key) as u32;`. Build an unbounded mpsc, push the three events `Delta { text: format!("Queued: \"{}\" ({} in queue)\n", message, depth) }`, `QueueUpdated { depth, paused: paused_snapshot }`, `Finished { total_tokens: 0 }`, drop tx, then drain rx into the socket via `serde_json::to_string` + `socket.send_raw(Message::Text(...)).await`. On `Err(QueueError::CapacityReached { max, .. })`: same mpsc shape but the Delta is `format!("Queue is full ({max}/{max}). /stop or /flush to drain.\n")` followed by Finished only (no QueueUpdated — depth did not change). End with `continue;`. After the new block, the existing `in_flight_turn.is_some()` branch at 204-214 is fully replaced; do not leave the old Error path.

    Edit C — Inside the existing slash interception block (lines 233-337), in the `match result` block at lines 302-317, insert THREE new `CommandResult` arms BEFORE the `CommandResult::Handled | CommandResult::Quit` catch (which currently lives around line 311). Each arm follows the canonical slash result delivery shape (lines 318-330 — Delta + Finished, drop tx, drain rx → socket, continue) but they do NOT compute a `text` and fall through to the shared delivery at line 318; instead they perform the full per-arm event sequence inline and `continue` directly. This mirrors how PATTERNS.md shows the arms.
       Arm C1 — `CommandResult::Queued { message: queued_msg }`: `let key = web_key(&session_id);` and `let paused_flag = app_state.get_or_create_paused_flag(&session_id);` and `let paused_snapshot = paused_flag.load(SeqCst);`. Match on `app_state.queue.try_push(&key, queued_msg.clone())`. Ok → push `Delta { text: format!("Queued: \"{}\" ({} in queue)\n", queued_msg, depth) }` and `QueueUpdated { depth, paused: paused_snapshot }` into tx. Err(CapacityReached { max, .. }) → push `Delta { text: format!("Queue is full ({max}/{max}). /stop or /flush to drain.\n") }` only. Then push `Finished { total_tokens: 0 }`, `drop(tx)`, drain rx → socket, `continue`.
       Arm C2 — `CommandResult::PauseQueue`: `let paused_flag = app_state.get_or_create_paused_flag(&session_id);` then `let was_paused = paused_flag.fetch_xor(true, SeqCst);` and `let new_paused = !was_paused;`. `let key = web_key(&session_id);` and `let depth = app_state.queue.len(&key) as u32;`. Push `QueueUpdated { depth, paused: new_paused }` and Delta `if new_paused { format!("Queue paused. ({} queued)\n", depth) } else { format!("Queue resumed. ({} queued)\n", depth) }` and Finished. Drop tx, drain rx → socket, continue.
       Arm C3 — `CommandResult::UnpauseQueue`: `let paused_flag = app_state.get_or_create_paused_flag(&session_id);` then `let was_paused = paused_flag.swap(false, SeqCst);`. `let key = web_key(&session_id);` and `let depth = app_state.queue.len(&key) as u32;`. Push `QueueUpdated { depth, paused: false }` and Delta `if was_paused { "Queue resumed.\n".to_string() } else { "Queue was not paused.\n".to_string() }` and Finished. Drop tx, drain rx → socket, continue.

    Edit D — Modify the existing `CommandResult::NewSession { message: m }` arm at lines 307-310 to perform D-04 ordering. Inside the arm, BEFORE the existing `app_state.reset_web_session(&session_id);` call: build `let key = web_key(&session_id);`, call `app_state.queue.clear(&key);`, call `app_state.get_or_create_paused_flag(&session_id).store(false, SeqCst);`, and push `ChatStreamEvent::QueueUpdated { depth: 0, paused: false }` into the existing `tx` channel. Then keep the existing `app_state.reset_web_session(&session_id); m` lines as the trailing expression of the arm. The shared delivery at lines 318-330 will then emit the original message text + Finished after the QueueUpdated. The QueueUpdated must arrive BEFORE the message Delta — pushing it into tx before the shared delivery's `tx.send(ChatStreamEvent::Delta { text })` is sufficient because mpsc is FIFO.

    Edit E — Add an early `/stop` intercept inside the slash block, BEFORE the existing `let result = ironhermes_core::commands::handlers::dispatch(...)` call (locate the dispatch call by greping for `handlers::dispatch` inside lines 233-300). The check is `if def.name == "stop"` (post-resolution canonical name — Pitfall 4 compliant). Body (per D-04a / D-05): `let key = web_key(&session_id);`, `app_state.queue.clear(&key);`, `app_state.get_or_create_paused_flag(&session_id).store(false, SeqCst);`, push into tx in this exact order: `QueueUpdated { depth: 0, paused: false }`, `Delta { text: "Queue cleared. Current turn finishing.\n".to_string() }`, `Finished { total_tokens: 0 }`. Drop tx, drain rx → socket, `continue`. NO `JoinHandle::abort()`. NO `cancel_token.cancel()`. NO reference to `in_flight_turn` at all — the in-flight turn (if any) finishes naturally; its eventual `None =>` completion in arm 2 will see an empty queue (we just cleared it) and not re-drain.

    Edit F — Extend the existing `None =>` branch in arm 2 (lines 486-498) with the drain trigger per D-02. The current branch already does `if let Some(turn) = in_flight_turn.take() { if let Err(err) = turn.handle.await { warn!(...) } }`. AFTER the `.await` on `turn.handle` (still inside the `if let Some(turn) = in_flight_turn.take()` body), capture `let session_id_done = turn.session_id.clone();` BEFORE the await (move the clone up so it survives the `turn` consumption — or capture before the inner `if let Err`). Then immediately after the await: `let key = web_key(&session_id_done);` and `let paused_flag = app_state.get_or_create_paused_flag(&session_id_done);` and `let paused_now = paused_flag.load(SeqCst);`. If `!paused_now`, call `let next_text = app_state.queue.pop(&key);`. If `Some(text) = next_text`, compute `let depth_after = app_state.queue.len(&key) as u32;`, then emit `QueueUpdated { depth: depth_after, paused: false }` directly to the socket via `socket.send_raw(Message::Text(serde_json::to_string(&ev).unwrap_or_default())).await` BEFORE spawning the next turn (D-03 emission ordering — pill updates before the next Delta). Then construct a new `(tx_drain, rx_drain) = mpsc::unbounded_channel::<ChatStreamEvent>();` and spawn the next `run_web_turn` mirroring the primary spawn block in arm 1 (the spawn block lives between approximately lines 363 and 443; the exact pattern is `app_state.clone()`, `session_id_done.clone()`, `text`, `tx_drain`, and any scrubber/callback setup the primary spawn uses). Assign `in_flight_turn = Some(InFlightTurn { session_id: session_id_done.clone(), rx: rx_drain, handle: drain_handle });`. If paused OR pop returned None, leave `in_flight_turn = None` and fall through — the loop's existing arm 1 or arm 3 will handle the next iteration.

    Cleanliness: all new SessionKey constructions use the `web_key` helper from Edit A. Every new `tx.send(...)` is `let _ = tx.send(...)` (the existing pattern at lines 318-322). Every new `socket.send_raw(...).await` is `let _ = socket.send_raw(...).await`. Every new branch ends in `continue;` (matches existing slash arms). Do NOT introduce any new `tokio::sync::Notify`, no fourth `tokio::select!` arm, no `JoinHandle::abort`, no `CancellationToken`.
  </action>
  <verify>
    <automated>cargo check -p iron_hermes_ui --features server && cargo test -p iron_hermes_ui --lib server::ws</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'app_state.queue.try_push' crates/iron_hermes_ui/src/server/ws.rs` ≥ 2 (in-flight push branch + CommandResult::Queued arm).
    - `grep -c 'app_state.queue.pop' crates/iron_hermes_ui/src/server/ws.rs` ≥ 1 (drain in None branch).
    - `grep -c 'app_state.queue.clear' crates/iron_hermes_ui/src/server/ws.rs` ≥ 2 (NewSession arm + /stop intercept).
    - `grep -c 'ChatStreamEvent::QueueUpdated' crates/iron_hermes_ui/src/server/ws.rs` ≥ 6 (in-flight push, Queued arm, PauseQueue arm, UnpauseQueue arm, NewSession arm, /stop intercept, drain).
    - `grep -c 'CommandResult::PauseQueue' crates/iron_hermes_ui/src/server/ws.rs` returns 1.
    - `grep -c 'CommandResult::UnpauseQueue' crates/iron_hermes_ui/src/server/ws.rs` returns 1.
    - `grep -c 'CommandResult::Queued' crates/iron_hermes_ui/src/server/ws.rs` returns 1.
    - `grep -c 'fetch_xor(true' crates/iron_hermes_ui/src/server/ws.rs` returns 1 (PauseQueue arm).
    - `grep -c 'swap(false' crates/iron_hermes_ui/src/server/ws.rs` ≥ 2 (UnpauseQueue arm + NewSession arm flag-reset + /stop intercept flag-reset, depending on `.store(false` usage). Use `grep -c 'paused.*\.store(false\|paused_flag.*\.store(false\|swap(false' crates/iron_hermes_ui/src/server/ws.rs` ≥ 3.
    - `grep -c 'Queue is full' crates/iron_hermes_ui/src/server/ws.rs` ≥ 1 (D-06 cap-hit message).
    - `grep -c 'Queue cleared. Current turn finishing.' crates/iron_hermes_ui/src/server/ws.rs` returns 1 (D-04a /stop Delta).
    - `grep -c 'handle.abort\|cancel_token.cancel' crates/iron_hermes_ui/src/server/ws.rs` returns 0 inside the /stop branch (D-05 negative invariant — open file to confirm the `0` count is global; if any usage exists elsewhere, it must be unrelated to /stop).
    - Ordering: position of `app_state.queue.clear` inside the NewSession arm < position of `app_state.reset_web_session` inside that same arm. Use ranged grep: `awk '/CommandResult::NewSession/,/^[[:space:]]*}[[:space:]]*$/' crates/iron_hermes_ui/src/server/ws.rs | grep -n 'queue.clear\|reset_web_session'` and confirm `queue.clear` appears on a smaller line number than `reset_web_session`.
    - Ordering: position of `Another request is already in progress` string in ws.rs returns 0 occurrences (the hard reject text is fully gone). `grep -c 'Another request is already in progress' crates/iron_hermes_ui/src/server/ws.rs` returns 0.
    - `cargo check -p iron_hermes_ui --features server` exits 0.
    - `cargo test -p iron_hermes_ui --lib` exits 0 (no regression in existing unit tests).
    - `cargo build -p iron_hermes_ui --features server` exits 0.
  </acceptance_criteria>
  <done>
    The WS recv-loop pushes free-text into the queue when in-flight, dispatches the three new CommandResult arms with the depth-aware Delta + typed QueueUpdated pattern, performs the D-04 ordering on /new/reset, intercepts /stop early with the D-04a sequence and zero abort calls, and drains the next queued item from the existing `None =>` completion branch in arm 2 with QueueUpdated emitted to the socket before respawn.
  </done>
</task>

</tasks>

<verification>
- `cargo check -p iron_hermes_ui --features server` green.
- `cargo test -p iron_hermes_ui --lib` green (no regression in server::ws unit tests).
- `cargo build -p iron_hermes_ui --features server` green (catches any wasm-vs-server feature gating issues introduced by the new imports).
- Manual end-to-end behavior is verified by Plan 06's integration tests (`web_queue_drain.rs`, `web_queue_controls.rs`) which assert the source-text shape AND the runtime drain semantics, and by the manual UAT step listed in VALIDATION.md.
</verification>

<success_criteria>
- D-01 / D-03 in-flight push replaces the hard reject; D-06 cap-hit string is exact.
- D-04 NewSession ordering: clear → paused.store(false) → QueueUpdated → reset_web_session.
- D-04a /stop sequence: clear → paused.store(false) → QueueUpdated → Delta → Finished, with zero abort calls.
- D-02 drain: WS recv-loop self-drain fires from the `None =>` branch in arm 2, gated on `!paused`, emits QueueUpdated before spawning the next run_web_turn.
- D-05 documented divergence: no `JoinHandle::abort`, no `cancel_token.cancel()` anywhere in the new /stop path.
- D-08 honored: the existing `is_bypass(&def.name)` gate at line 242 is untouched and now correctly lets /pause and /unpause through (because Plan 01 extended the core list).
</success_criteria>

<output>
Create `.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-04-SUMMARY.md` when done.
</output>
