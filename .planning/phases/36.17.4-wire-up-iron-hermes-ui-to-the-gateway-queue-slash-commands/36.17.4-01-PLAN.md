---
phase: 36.17.4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-core/src/commands/running_agent.rs
autonomous: true
requirements:
  - D-08
threats:
  - T-36.17.4-01
must_haves:
  truths:
    - "`is_bypass(\"pause\")` returns `true`."
    - "`is_bypass(\"unpause\")` returns `true`."
    - "`is_bypass(\"stop\")`, `is_bypass(\"new\")`, `is_bypass(\"status\")`, `is_bypass(\"queue\")` continue to return `true` (no regression)."
    - "All non-member checks (e.g. `is_bypass(\"model\")`, `is_bypass(\"approve\")`, `is_bypass(\"deny\")`, `is_bypass(\"\")`, `is_bypass(\"STOP\")`) continue to return `false`."
    - "Gateway + TUI test suites remain green — `pause`/`unpause` now bypassing the running-agent guard does not change observable TUI behavior because the TUI dispatches them via `handle_session_control` independent of the guard."
  artifacts:
    - path: "crates/ironhermes-core/src/commands/running_agent.rs"
      provides: "Extended bypass list including `pause` and `unpause` + extended unit test"
      contains: "\"pause\" | \"unpause\""
  key_links:
    - from: "crates/ironhermes-core/src/commands/running_agent.rs::is_bypass"
      to: "crates/iron_hermes_ui/src/server/ws.rs:242 `is_bypass(&def.name)` gate"
      via: "shared canonical bypass check"
      pattern: "is_bypass\\(&def\\.name\\)"
---

<objective>
Close the critical gap surfaced by RESEARCH Finding 4: CONTEXT D-08 assumes
`is_bypass("pause") == true` and `is_bypass("unpause") == true`, but the
shipped `is_bypass` in `crates/ironhermes-core/src/commands/running_agent.rs`
only matches `stop | new | status | queue`. Without this fix, the web `ws.rs`
slash gate at line 242 would reject `/pause` and `/unpause` whenever a turn
is in flight — exactly when the user needs to pause the drain.

Purpose: Implement Option B from RESEARCH Finding 4 (cleaner than ws.rs
interception) — extend the locked bypass list at the core level so every
surface (gateway, web, TUI) gets correct behavior from a single source.

Output: One-line change to `is_bypass` + extended `is_bypass_locked_list`
unit test. Wave 1, no dependencies, unblocks Plan 04's `/pause` `/unpause`
arms (D-08 invariant).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-CONTEXT.md
@.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-RESEARCH.md
@.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-PATTERNS.md
@crates/ironhermes-core/src/commands/running_agent.rs

<interfaces>
<!-- Existing shape of the function being modified (lines 44-47): -->
<!--   pub fn is_bypass(name: &str) -> bool {                                         -->
<!--       // TODO(D-01): add "approve" | "deny" when approval queue lands           -->
<!--       matches!(name, "stop" | "new" | "status" | "queue")                       -->
<!--   }                                                                              -->
<!--                                                                                  -->
<!-- Consumers (do not modify in this plan, just note for reachability):              -->
<!-- - crates/ironhermes-gateway/src/handler.rs (slash dispatch + free-text guard)    -->
<!-- - crates/iron_hermes_ui/src/server/ws.rs:242 `is_bypass(&def.name)` slash gate  -->
<!-- - crates/ironhermes-cli/src/tui_rata/commands.rs:370 TUI slash gate              -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `pause` and `unpause` to `is_bypass` locked list and extend unit test</name>
  <files>crates/ironhermes-core/src/commands/running_agent.rs</files>
  <read_first>
    - crates/ironhermes-core/src/commands/running_agent.rs (lines 1-148: read full file — current `is_bypass` at lines 44-47, current `is_bypass_locked_list` test at lines 57-72, and the `is_bypass_keeps_todo_for_approve_deny` test at lines 132-146)
    - .planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-CONTEXT.md (D-08 — locked assumption that pause/unpause already bypass)
    - .planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-RESEARCH.md (Research Finding 4 — gap discovery + Option B recommendation)
    - .planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-PATTERNS.md (file 9 section — exact diff shape)
  </read_first>
  <behavior>
    - `is_bypass("pause")` returns `true`.
    - `is_bypass("unpause")` returns `true`.
    - `is_bypass("stop")`, `is_bypass("new")`, `is_bypass("status")`, `is_bypass("queue")` still return `true`.
    - `is_bypass("model")`, `is_bypass("retry")`, `is_bypass("approve")`, `is_bypass("deny")`, `is_bypass("")`, `is_bypass("STOP")` still return `false`.
    - `is_bypass("PAUSE")` returns `false` (case-sensitivity preserved).
  </behavior>
  <action>
    Edit the `matches!` literal in `pub fn is_bypass(name: &str) -> bool` at lines 44-47 of `crates/ironhermes-core/src/commands/running_agent.rs` so the locked list reads `"stop" | "new" | "status" | "queue" | "pause" | "unpause"`. Keep the existing `// TODO(D-01): add "approve" | "deny" when approval queue lands` comment intact and append a second comment line `// Phase 36.17.4 (D-08): pause/unpause must bypass — they affect drain behavior, not the in-flight turn.`. Then extend the existing `#[test] fn is_bypass_locked_list()` at lines 57-72 by appending two assertions inside the "Members" block: `assert!(is_bypass("pause"), "pause must bypass (Phase 36.17.4 D-08)");` and `assert!(is_bypass("unpause"), "unpause must bypass (Phase 36.17.4 D-08)");`. Also append one negative assertion `assert!(!is_bypass("PAUSE"), "PAUSE (uppercase) must not bypass — case-sensitive (D-01)");` to lock case-sensitivity for the new members. Do NOT touch `is_bypass_keeps_todo_for_approve_deny`, `RunningAgentGuard`, `AGENT_RUNNING_REJECT_MSG`, or any other test in the module.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-core --lib commands::running_agent::tests::is_bypass_locked_list -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"stop" | "new" | "status" | "queue" | "pause" | "unpause"' crates/ironhermes-core/src/commands/running_agent.rs` returns 1.
    - `cargo test -p ironhermes-core --lib commands::running_agent::tests::is_bypass_locked_list -- --exact` exits 0.
    - `cargo test -p ironhermes-core --lib commands::running_agent::tests` exits 0 (no regression in the other 5 tests).
    - `cargo test -p ironhermes-gateway` exits 0 (gateway suite must stay green — T-03 / D-12 invariant).
    - `cargo test -p ironhermes-cli --features test-support` exits 0 (TUI suite must stay green — D-12 invariant).
  </acceptance_criteria>
  <done>
    `is_bypass` accepts `pause` and `unpause`, the locked-list test asserts both members + case-sensitivity, and the workspace test gates for gateway and TUI remain green.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p ironhermes-core --lib` green.
- `cargo test -p ironhermes-gateway` green (no regression in the production consumer of `is_bypass`).
- `cargo test -p ironhermes-cli --features test-support` green (no regression in the TUI consumer of `is_bypass`).
- `cargo check --workspace --all-features` green.
</verification>

<success_criteria>
- `is_bypass("pause") == true && is_bypass("unpause") == true` (D-08 invariant restored).
- D-12 phase gate intact: gateway + TUI suites green.
- No new warnings introduced under `-D warnings`.
</success_criteria>

<output>
Create `.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-01-SUMMARY.md` when done.
</output>
