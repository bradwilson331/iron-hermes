---
phase: 36.17.4
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/iron_hermes_ui/src/server/state.rs
autonomous: true
requirements:
  - D-01
  - D-01a
threats: []
must_haves:
  truths:
    - "`AppState` carries a `queue: Arc<dyn MessageQueue<SessionKey>>` field initialized to a single `SessionQueue` instance at `AppState::init`."
    - "`AppState` carries a `queue_paused: Arc<std::sync::Mutex<HashMap<String, Arc<AtomicBool>>>>` field initialized to an empty map at `AppState::init`."
    - "`AppState::get_or_create_paused_flag(&self, session_id: &str) -> Arc<AtomicBool>` exists and mirrors `get_or_create_running_flag` byte-for-byte (entry-or-insert under std::sync::Mutex, std mutex never held across `.await`)."
    - "Second call to `get_or_create_paused_flag` with the same `session_id` returns the SAME `Arc<AtomicBool>` (idempotent — atomic state is preserved across lookups)."
    - "Two different `session_id` values produce two independent `Arc<AtomicBool>` instances with independent state (session isolation)."
    - "Existing `AppState` consumers (run_web_turn, reset_web_session, nudge_turns wiring, running_agents wiring) compile and behave unchanged — no field renamed, no method signature altered."
  artifacts:
    - path: "crates/iron_hermes_ui/src/server/state.rs"
      provides: "queue + queue_paused fields + get_or_create_paused_flag + inline unit tests"
      contains: "fn get_or_create_paused_flag"
  key_links:
    - from: "crates/iron_hermes_ui/src/server/state.rs::AppState"
      to: "crates/ironhermes-gateway/src/session_queue.rs::SessionQueue"
      via: "concrete MessageQueue<SessionKey> implementor instantiated in AppState::init"
      pattern: "SessionQueue::new\\(\\)"
    - from: "crates/iron_hermes_ui/src/server/state.rs::AppState"
      to: "crates/ironhermes-core/src/queue.rs::MessageQueue"
      via: "trait-object field type"
      pattern: "Arc<dyn .*MessageQueue<.*SessionKey>>"
---

<objective>
Add the queue + paused-flag state to `AppState` so Plan 04 (ws.rs recv-loop)
has a place to push, pop, clear, and read pause state from. Mirror the
existing `running_agents` + `get_or_create_running_flag` pattern at
`state.rs:49` and `state.rs:230-238` byte-for-byte (D-01a says explicitly
"mirroring `get_or_create_running_flag` byte-for-byte").

Purpose: D-01 (per-session_id queue ownership on AppState, single
`SessionQueue` shared across all WS connections), D-01a (per-session
`Arc<AtomicBool>` paused-flag map mirroring `running_agents`).

Output: Two new fields + one new method + an inline test module mirroring
`phase_36_1_02_state_tests` (lines 591-685) for paused-flag idempotency and
session isolation. Parallel-safe with Plans 01 and 02 (zero `files_modified`
overlap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-CONTEXT.md
@.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-RESEARCH.md
@.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-PATTERNS.md
@crates/iron_hermes_ui/src/server/state.rs

<interfaces>
<!-- Existing AppState shape (state.rs:25-71) — the byte-for-byte analog:
       pub running_agents: Arc<std::sync::Mutex<HashMap<String, Arc<AtomicBool>>>>,
       pub nudge_turns:    Arc<std::sync::Mutex<HashMap<String, u32>>>,

     AppState::init (state.rs:88-186) currently initializes:
       running_agents: Arc::new(std::sync::Mutex::new(HashMap::new())),
       nudge_turns:    Arc::new(std::sync::Mutex::new(HashMap::new())),
       subagent_cancel_token: None,

     get_or_create_running_flag (state.rs:230-238) — the method to clone:
       pub fn get_or_create_running_flag(&self, session_id: &str) -> Arc<AtomicBool> {
           let mut map = self.running_agents.lock().unwrap_or_else(|e| e.into_inner());
           map.entry(session_id.to_string())
               .or_insert_with(|| Arc::new(AtomicBool::new(false)))
               .clone()
       }

     Inline test module phase_36_1_02_state_tests at lines 591-685 provides:
       fn make_running_agents() -> Arc<...>
       fn get_or_create(map, id) -> Arc<AtomicBool>   (free-fn equivalent of method)
       #[test] fn new_session_flag_starts_false
       #[test] fn flag_persists_across_lookups
       #[test] fn session_isolation

     New surfaces this plan ships (consumed by Plan 04, Plan 06):
       pub queue: Arc<dyn ironhermes_core::queue::MessageQueue<ironhermes_core::session::SessionKey>>,
       pub queue_paused: Arc<std::sync::Mutex<HashMap<String, Arc<AtomicBool>>>>,
       pub fn get_or_create_paused_flag(&self, session_id: &str) -> Arc<AtomicBool> { ... }
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `queue` + `queue_paused` fields to AppState + initialize in AppState::init + add `get_or_create_paused_flag` method + extend inline test module</name>
  <files>crates/iron_hermes_ui/src/server/state.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/state.rs (offset 1, limit 250 — the struct + init + the get_or_create_running_flag method)
    - crates/iron_hermes_ui/src/server/state.rs (offset 590, limit 100 — the existing phase_36_1_02_state_tests module to mirror)
    - .planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-CONTEXT.md (D-01, D-01a — locked field shapes + ownership model)
    - .planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-PATTERNS.md (`state.rs` section — exact field placement, init wiring, method body)
    - crates/ironhermes-core/src/queue.rs (`MessageQueue<K>` trait surface — needed for the `Arc<dyn ...>` type)
    - crates/ironhermes-core/src/session.rs (`SessionKey` type used as `K`)
    - crates/ironhermes-gateway/src/session_queue.rs (concrete `SessionQueue` implementor — the type instantiated in init)
  </read_first>
  <behavior>
    - The struct compiles with two new fields whose types are exactly `Arc<dyn ironhermes_core::queue::MessageQueue<ironhermes_core::session::SessionKey>>` and `Arc<std::sync::Mutex<HashMap<String, Arc<std::sync::atomic::AtomicBool>>>>`.
    - `AppState::init(...)` initializes both fields (queue via `Arc::new(ironhermes_gateway::session_queue::SessionQueue::new())`, queue_paused via `Arc::new(std::sync::Mutex::new(HashMap::new()))`).
    - `get_or_create_paused_flag(session_id)` returns an `Arc<AtomicBool>` that is the same `Arc` (pointer-equal under `Arc::ptr_eq`) on the second call with the same session_id.
    - `get_or_create_paused_flag` for two different session_ids returns two distinct `Arc<AtomicBool>` (not pointer-equal) and storing `true` in one does not affect the other (session isolation).
    - All existing `AppState` consumers continue to compile (no field renamed, no method removed).
  </behavior>
  <action>
    Edit `crates/iron_hermes_ui/src/server/state.rs` in four places: (a) struct field block — immediately after the `running_agents` field at line 49, add two `pub` fields. First: `pub queue_paused: Arc<std::sync::Mutex<HashMap<String, Arc<std::sync::atomic::AtomicBool>>>>,` with doc comment `/// Phase 36.17.4 (D-01a): per-session queue-drain paused flags. Arc<Mutex<HashMap>> mirrors running_agents above. std::sync::Mutex — never held across .await. Map grows unbounded (accepted tradeoff, matches running_agents + nudge_turns precedent).`. Second: `pub queue: Arc<dyn ironhermes_core::queue::MessageQueue<ironhermes_core::session::SessionKey>>,` with doc comment `/// Phase 36.17.4 (D-01): shared FIFO queue for all sessions, keyed by SessionKey. Single SessionQueue instance; each WS connection constructs its own SessionKey { platform: Web, chat_id, user_id: Some("web") } at call sites. Queue survives WS disconnect/reconnect.`. (b) `AppState::init` body — find the `running_agents: Arc::new(std::sync::Mutex::new(HashMap::new())),` line at 175 and immediately after it add two initialization lines: `queue_paused: Arc::new(std::sync::Mutex::new(HashMap::new())),` and `queue: Arc::new(ironhermes_gateway::session_queue::SessionQueue::new()) as Arc<dyn ironhermes_core::queue::MessageQueue<ironhermes_core::session::SessionKey>>,`. The explicit `as Arc<dyn ...>` cast is necessary so the concrete `Arc<SessionQueue>` widens to the trait-object type required by the field. (c) After the existing `pub fn get_or_create_running_flag(...)` ends at line 238, add `pub fn get_or_create_paused_flag(&self, session_id: &str) -> Arc<std::sync::atomic::AtomicBool>` whose body is byte-for-byte the same shape: `let mut map = self.queue_paused.lock().unwrap_or_else(|e| e.into_inner()); map.entry(session_id.to_string()).or_insert_with(|| Arc::new(std::sync::atomic::AtomicBool::new(false))).clone()`. Add doc comment `/// Phase 36.17.4 (D-01a): mirror of get_or_create_running_flag above. Entry-or-insert under std::sync::Mutex — Mutex must NEVER be held across .await.` (d) In the existing `#[cfg(test)] mod phase_36_1_02_state_tests` block at lines 591-685, append a sibling module `mod queue_paused_tests` (or simply append three free-fn-based tests directly after the existing `session_isolation` test, mirroring its body verbatim) covering three behaviors: `paused_flag_starts_false` (lookup, assert `.load(SeqCst) == false`), `paused_flag_persists_across_lookups` (lookup → `.store(true, SeqCst)` → second lookup → assert `.load(SeqCst) == true` AND assert `Arc::ptr_eq(&first, &second)`), `paused_flag_session_isolation` (lookup id_a, store true; lookup id_b, assert load id_b == false, assert load id_a still true, assert `!Arc::ptr_eq(&a, &b)`). Use the same `make_running_agents`/`get_or_create` helper pattern as the existing tests but renamed `make_queue_paused`/`get_or_create_paused`. Tag every new test name with `paused_flag_` prefix so they namespace clearly.
  </action>
  <verify>
    <automated>cargo test -p iron_hermes_ui --lib server::state -- --skip phase_36_1_02_state_tests::guard</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub queue: Arc<dyn' crates/iron_hermes_ui/src/server/state.rs` returns 1 (D-01 field declaration).
    - `grep -c 'pub queue_paused:' crates/iron_hermes_ui/src/server/state.rs` returns 1 (D-01a field declaration).
    - `grep -c 'fn get_or_create_paused_flag' crates/iron_hermes_ui/src/server/state.rs` returns 1 (method declaration).
    - `grep -c 'SessionQueue::new()' crates/iron_hermes_ui/src/server/state.rs` returns 1 (init wiring).
    - `cargo test -p iron_hermes_ui --lib server::state::phase_36_1_02_state_tests::paused_flag_starts_false` exits 0.
    - `cargo test -p iron_hermes_ui --lib server::state::phase_36_1_02_state_tests::paused_flag_persists_across_lookups` exits 0.
    - `cargo test -p iron_hermes_ui --lib server::state::phase_36_1_02_state_tests::paused_flag_session_isolation` exits 0.
    - `cargo check -p iron_hermes_ui --features server` exits 0 (no consumer of `AppState` breaks).
  </acceptance_criteria>
  <done>
    AppState owns the queue and the per-session paused-flag map, both are initialized in `AppState::init`, `get_or_create_paused_flag` mirrors `get_or_create_running_flag`, and three new unit tests verify start-false, idempotency, and session isolation.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p iron_hermes_ui --lib server::state` green (all existing + new paused-flag tests).
- `cargo check -p iron_hermes_ui --features server` green.
- The std::sync::Mutex discipline (never held across `.await`) is preserved — verify by inspection that no `.await` appears between `.lock()` and the end-of-block drop in `get_or_create_paused_flag`.
</verification>

<success_criteria>
- AppState carries `queue` and `queue_paused` fields exactly as D-01 / D-01a specify.
- `get_or_create_paused_flag` returns the same `Arc<AtomicBool>` on repeat lookups and distinct `Arc`s for distinct session_ids.
- No existing test fails; no AppState consumer fails to compile.
</success_criteria>

<output>
Create `.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-03-SUMMARY.md` when done.
</output>
