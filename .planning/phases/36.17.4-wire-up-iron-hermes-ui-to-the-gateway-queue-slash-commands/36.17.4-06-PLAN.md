---
phase: 36.17.4
plan: 06
type: execute
wave: 3
depends_on:
  - 36.17.4-01
  - 36.17.4-02
  - 36.17.4-03
  - 36.17.4-04
  - 36.17.4-05
files_modified:
  - crates/iron_hermes_ui/tests/web_queue_drain.rs
  - crates/iron_hermes_ui/tests/web_queue_controls.rs
  - crates/iron_hermes_ui/tests/web_queue_protocol.rs
autonomous: true
requirements:
  - D-02
  - D-04
  - D-04a
  - D-05
  - D-06
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
threats: []
must_haves:
  truths:
    - "`crates/iron_hermes_ui/tests/web_queue_drain.rs` exists and asserts source-text invariants on state.rs + ws.rs (queue field, queue_paused field, get_or_create_paused_flag, queue.try_push, queue.pop, QueueUpdated emission) AND ships direct unit tests against the production `SessionQueue` + per-session paused-flag map covering FIFO ordering, the 128-message cap, paused-blocks-drain, and `Arc<AtomicBool>` idempotency."
    - "`crates/iron_hermes_ui/tests/web_queue_controls.rs` exists and locks D-04 NewSession ordering (queue.clear < reset_web_session), D-04a /stop sequence (clear → paused.store → QueueUpdated → Delta → Finished), D-05 negative invariant (zero `handle.abort` or `cancel_token.cancel` in the /stop arm), D-06 cap-hit message text, D-08 `is_bypass(\"pause\")` + `is_bypass(\"unpause\")` (mirrors Plan 01 test from a downstream consumer's perspective), and the pause/unpause AtomicBool state-machine semantics."
    - "`crates/iron_hermes_ui/tests/web_queue_protocol.rs` exists and asserts source-text invariants on protocol.rs (QueueUpdated variant + inline shape test exists + exact JSON string locked), mod.rs (queue_state signal + QueueUpdated arm + context provider), and app_footer.rs (queue_depth read + hide-when-zero + paused branch)."
    - "All three test files compile cleanly under `cargo test -p iron_hermes_ui` and exit 0."
    - "No test depends on a running WebSocket server — all assertions are source-text grep + direct unit tests on the production types (no Axum spawn, no real WS handshake)."
    - "D-10 (highest-leverage seam) honored: tests follow the `include_str! + assert_contains` pattern from existing `running_agent_guard_web_tests.rs`, plus direct unit assertions on `ironhermes_gateway::session_queue::SessionQueue` and the `Arc<Mutex<HashMap<String, Arc<AtomicBool>>>>` idiom."
  artifacts:
    - path: "crates/iron_hermes_ui/tests/web_queue_drain.rs"
      provides: "FIFO + cap + paused-blocks-drain unit tests + state.rs/ws.rs source-text anchors"
      contains: "queue_fifo_ordering"
    - path: "crates/iron_hermes_ui/tests/web_queue_controls.rs"
      provides: "Source-text ordering + negative invariants + pause/unpause state-machine + is_bypass downstream check"
      contains: "ws_rs_new_clears_queue_before_session_reset"
    - path: "crates/iron_hermes_ui/tests/web_queue_protocol.rs"
      provides: "Protocol variant + mod.rs signal/arm + app_footer.rs pill anchors"
      contains: "protocol_rs_queue_updated_shape_test_exists"
  key_links:
    - from: "crates/iron_hermes_ui/tests/web_queue_drain.rs"
      to: "crates/iron_hermes_ui/src/server/state.rs + ws.rs"
      via: "include_str! source anchors + direct SessionQueue/AtomicBool unit tests"
      pattern: "include_str!"
    - from: "crates/iron_hermes_ui/tests/web_queue_controls.rs"
      to: "crates/iron_hermes_ui/src/server/ws.rs + ironhermes-core::commands::running_agent"
      via: "ordering grep + is_bypass downstream contract test"
      pattern: "is_bypass"
    - from: "crates/iron_hermes_ui/tests/web_queue_protocol.rs"
      to: "crates/iron_hermes_ui/src/protocol.rs + components/hermes_app/{mod.rs,app_footer.rs}"
      via: "source-text anchors for the wasm wiring"
      pattern: "include_str!"
---

<objective>
Ship the regression test triplet that closes the D-09 / D-10 / D-11 / D-12
test surface for Phase 36.17.4. The test seam follows D-10 (highest-leverage
seam) per CONTEXT — source-text invariants via `include_str!` + assert-contains
(mirrors the existing `running_agent_guard_web_tests.rs:448-496` pattern) PLUS
direct unit tests against the production `SessionQueue` and `Arc<AtomicBool>`
types so FIFO / cap / paused-blocks-drain behavior is exercised end-to-end
without spinning up a real WebSocket server.

Purpose: D-09 split (3 test files mirroring the 36.17.3 triplet), D-10
(highest-leverage seam — source-text + direct unit tests, not full
integration), D-11 (downstream consumer of the inline shape test from
Plan 02), D-12 (phase gate: full cargo test green across iron_hermes_ui +
ironhermes-gateway + ironhermes-cli).

Output: Three new test files. Plan depends on every prior plan in this
phase so all source-text anchors actually exist in the codebase by Wave 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-CONTEXT.md
@.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-RESEARCH.md
@.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-PATTERNS.md
@.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-VALIDATION.md
@crates/iron_hermes_ui/tests/running_agent_guard_web_tests.rs
@crates/iron_hermes_ui/src/server/state.rs
@crates/iron_hermes_ui/src/server/ws.rs
@crates/iron_hermes_ui/src/protocol.rs
@crates/iron_hermes_ui/src/components/hermes_app/mod.rs
@crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs

<interfaces>
<!-- The pattern to follow exactly (from running_agent_guard_web_tests.rs:448+):
       const WS_SOURCE: &str = include_str!("../src/server/ws.rs");
       const STATE_SOURCE: &str = include_str!("../src/server/state.rs");
       #[test] fn name() {
           assert!(SOURCE.contains("token"), "human-readable reason");
       }

     Direct unit tests use the production types:
       use ironhermes_gateway::session_queue::SessionQueue;
       use ironhermes_core::queue::{MessageQueue, QueueError, MAX_QUEUE_DEPTH};
       use ironhermes_core::session::SessionKey;
       use ironhermes_core::types::Platform;
       use std::sync::Arc;
       use std::collections::HashMap;
       use std::sync::atomic::{AtomicBool, Ordering};

     SessionKey constructor (confirm by reading session.rs):
       SessionKey { platform: Platform::Web, chat_id: "test".to_string(), user_id: Some("web".into()) }
       OR SessionKey::new(Platform::Web, "test").with_user("web")  -- pick whichever the existing tests use
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `tests/web_queue_drain.rs` — FIFO + cap + paused-blocks-drain unit tests + state.rs/ws.rs source anchors</name>
  <files>crates/iron_hermes_ui/tests/web_queue_drain.rs</files>
  <read_first>
    - crates/iron_hermes_ui/tests/running_agent_guard_web_tests.rs (read full file — confirm the exact `include_str!` header pattern at the top + the `#[test] fn ... { assert!(SOURCE.contains(...), ...); }` shape)
    - crates/iron_hermes_ui/src/server/state.rs (offset 1, limit 250 — confirm field names exactly as Plan 03 shipped them so the source-text grep matches)
    - crates/iron_hermes_ui/src/server/ws.rs (offset 100, limit 50 + offset 200, limit 60 + offset 480, limit 30 — confirm tokens `app_state.queue.try_push`, `app_state.queue.pop`, `QueueUpdated`, `web_key` exist as Plan 04 shipped them)
    - crates/ironhermes-core/src/queue.rs (confirm `MAX_QUEUE_DEPTH` constant + `try_push` / `pop` / `len` / `clear` method signatures)
    - crates/ironhermes-gateway/src/session_queue.rs (confirm `SessionQueue::new()` ctor + `MessageQueue<SessionKey>` impl)
    - crates/ironhermes-core/src/session.rs (confirm `SessionKey` constructor shape used by existing tests)
    - .planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-PATTERNS.md (`web_queue_drain.rs` section — exact test bodies)
    - .planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-VALIDATION.md (per-task verification map for D-01, D-01a, D-02, FIFO, PAUSE, CAP)
    - .planning/phases/36.17.3-wire-up-tui-with-gateway-queue-and-slash-queue-commands/36.17.3-06-PLAN.md (parity precedent — the TUI `tui_queue_drain.rs` tests)
  </read_first>
  <action>
    Create new file `crates/iron_hermes_ui/tests/web_queue_drain.rs`. Top of file: a header doc comment `//! Phase 36.17.4 regression tests — queue drain + FIFO + cap + paused semantics. //! Source-text anchors (include_str!) mirror the running_agent_guard_web_tests.rs pattern. //! Direct unit tests exercise the production SessionQueue + Arc<AtomicBool> idiom.`. Then declare two source anchors: `const WS_SOURCE: &str = include_str!("../src/server/ws.rs");` and `const STATE_SOURCE: &str = include_str!("../src/server/state.rs");`.

    Imports: `ironhermes_core::queue::{MessageQueue, MAX_QUEUE_DEPTH}`, `ironhermes_core::queue::QueueError`, `ironhermes_core::session::SessionKey`, `ironhermes_core::types::Platform`, `ironhermes_gateway::session_queue::SessionQueue`, `std::sync::Arc`, `std::sync::Mutex`, `std::collections::HashMap`, `std::sync::atomic::{AtomicBool, Ordering}`.

    Helpers (free fns):
      - `fn make_queue() -> Arc<SessionQueue> { Arc::new(SessionQueue::new()) }`
      - `fn make_key(chat: &str) -> SessionKey` — construct using whichever ctor the existing codebase uses (read `session.rs` to confirm; e.g. `SessionKey { platform: Platform::Web, chat_id: chat.to_string(), user_id: Some("web".into()) }`).
      - `fn make_paused_map() -> Arc<Mutex<HashMap<String, Arc<AtomicBool>>>> { Arc::new(Mutex::new(HashMap::new())) }`
      - `fn get_or_create_paused(map: &Arc<Mutex<HashMap<String, Arc<AtomicBool>>>>, id: &str) -> Arc<AtomicBool>` — entry-or-insert returning a clone (mirrors AppState::get_or_create_paused_flag exactly).

    Source-text anchor tests:
      - `#[test] fn state_rs_declares_queue_fields()` — assert `STATE_SOURCE.contains("pub queue:")`, `STATE_SOURCE.contains("pub queue_paused:")`, `STATE_SOURCE.contains("fn get_or_create_paused_flag")` with human-readable assert messages tying each to a D-XX decision.
      - `#[test] fn ws_rs_contains_drain_mechanism()` — assert `WS_SOURCE.contains("queue.try_push")` (D-01 / D-03 push site), `WS_SOURCE.contains("queue.pop")` (D-02 drain), `WS_SOURCE.contains("QueueUpdated")` (D-03 typed event).
      - `#[test] fn ws_rs_drain_emits_queue_updated_before_spawn()` — find the LAST occurrence of `queue.pop` in WS_SOURCE (the drain branch lives in arm 2's None => branch, which is near the end of the file). Verify that within a 2000-character window AFTER that index, the string `QueueUpdated` appears (ensures the drain branch DOES emit the typed event) AND `tokio::spawn` also appears after the QueueUpdated (D-03 emission ordering: pill update before respawn). If `tokio::spawn` is rare elsewhere this can be a simple offset comparison.

    Direct unit tests (exercise the production types, no AppState mock needed):
      - `#[test] fn queue_fifo_ordering()` — `let q = make_queue(); let k = make_key("sess-a");` push "A", "B", "C". Assert `q.pop(&k) == Some("A".to_string())`, then "B", then "C", then None (4th pop). Assert `q.len(&k) == 0`.
      - `#[test] fn queue_cap_at_128()` — `let q = make_queue(); let k = make_key("sess-cap");` push `MAX_QUEUE_DEPTH` (128) distinct strings expecting `Ok(())` for each. The 129th push must return `Err(QueueError::CapacityReached { max: MAX_QUEUE_DEPTH, .. })`. Use `matches!(...)` for the error variant check.
      - `#[test] fn queue_pause_drain_semantics()` — `let q = make_queue(); let k = make_key("sess-p"); let paused_map = make_paused_map(); let flag = get_or_create_paused(&paused_map, "sess-p");` push 3 items. Set `flag.store(true, Ordering::SeqCst)`. Simulate the drain guard: `let paused_now = flag.load(Ordering::SeqCst); if !paused_now { /* would pop */ } else { /* skip */ }` — assert paused_now is true and assert `q.len(&k) == 3` (queue unchanged). Then `flag.store(false, Ordering::SeqCst);` simulate drain pulling one item: `assert_eq!(q.pop(&k), Some("...".to_string()));` and `assert_eq!(q.len(&k), 2);`.
      - `#[test] fn paused_flag_idempotent()` — Two `get_or_create_paused` calls with the same id return Arcs that compare equal under `Arc::ptr_eq` and storing into one is observable from the other (just confirm: store true on first, load from second returns true).

    Use literal D-XX references in assert! messages for traceability.
  </action>
  <verify>
    <automated>cargo test -p iron_hermes_ui --test web_queue_drain</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/iron_hermes_ui/tests/web_queue_drain.rs` exists.
    - `grep -c 'include_str!' crates/iron_hermes_ui/tests/web_queue_drain.rs` returns 2 (ws.rs + state.rs anchors).
    - `grep -c 'queue_fifo_ordering\|queue_cap_at_128\|queue_pause_drain_semantics\|paused_flag_idempotent' crates/iron_hermes_ui/tests/web_queue_drain.rs` returns 4 (all four unit tests defined).
    - `cargo test -p iron_hermes_ui --test web_queue_drain` exits 0.
    - `cargo test -p iron_hermes_ui --test web_queue_drain queue_fifo_ordering` exits 0.
    - `cargo test -p iron_hermes_ui --test web_queue_drain queue_cap_at_128` exits 0.
  </acceptance_criteria>
  <done>
    web_queue_drain.rs exists, locks the queue/queue_paused/get_or_create_paused_flag source anchors and the drain emission ordering, and exercises FIFO, cap-at-128, paused-blocks-drain, and Arc<AtomicBool> idempotency on the production types.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create `tests/web_queue_controls.rs` — D-04/D-04a ordering, D-05 negative, D-06 cap text, D-08 is_bypass downstream, pause/unpause AtomicBool state-machine</name>
  <files>crates/iron_hermes_ui/tests/web_queue_controls.rs</files>
  <read_first>
    - crates/iron_hermes_ui/tests/running_agent_guard_web_tests.rs (confirm `include_str!` + assert-contains pattern + ranged ordering grep style)
    - crates/iron_hermes_ui/src/server/ws.rs (offset 200, limit 200 — confirm tokens for /stop branch, NewSession arm, PauseQueue arm, UnpauseQueue arm exactly as Plan 04 shipped them so the substring grep matches; specifically the `Queue is full` literal, the `Queue cleared. Current turn finishing.` literal, `fetch_xor(true`, `swap(false`)
    - crates/ironhermes-core/src/commands/running_agent.rs (confirm Plan 01 extended `is_bypass` to include `pause` and `unpause`)
    - .planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-PATTERNS.md (`web_queue_controls.rs` section — exact tests)
    - .planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-VALIDATION.md (D-04, D-04a, D-05, D-06, D-08 verification map)
  </read_first>
  <action>
    Create `crates/iron_hermes_ui/tests/web_queue_controls.rs`. Top: header doc comment + `const WS_SOURCE: &str = include_str!("../src/server/ws.rs");`.

    Imports: `ironhermes_core::commands::running_agent::is_bypass`, `std::sync::Arc`, `std::sync::Mutex`, `std::collections::HashMap`, `std::sync::atomic::{AtomicBool, Ordering}`.

    Reuse the same `make_paused_map` / `get_or_create_paused` helpers (paste them in — these tests don't need a queue, but the pause/unpause state-machine tests do need the flag map).

    Tests:
      - `#[test] fn is_bypass_includes_pause_and_unpause()` — assert `is_bypass("pause")` and `is_bypass("unpause")`, plus `is_bypass("stop")`, `is_bypass("new")`, `is_bypass("status")`, `is_bypass("queue")` (D-08 downstream contract; this test failing means Plan 01 regressed).
      - `#[test] fn ws_rs_new_clears_queue_before_session_reset()` — D-04 ordering: locate the substring `CommandResult::NewSession` in WS_SOURCE, then within the bytes from that index to the next `CommandResult::` token (or end-of-arm closing brace), assert the substring `queue.clear` appears at a position less than the position of `reset_web_session`. Easiest implementation: use `WS_SOURCE.split("CommandResult::NewSession").nth(1).expect(...)`, then within that slice locate both substrings and compare offsets.
      - `#[test] fn ws_rs_stop_arm_no_abort_or_cancel()` — D-05 negative invariant: locate the substring `def.name == "stop"` (or `def.name=="stop"`) in WS_SOURCE which marks the Plan 04 Edit E intercept, then within a 1500-byte window after that index, assert the substrings `handle.abort` and `cancel_token.cancel` are ABSENT. Use `.find(...)` and verify it returns None for both. Assert message `"D-05: /stop must NOT abort the in-flight JoinHandle (clear-queue-only divergence from TUI 36.17.3 D-08)"`.
      - `#[test] fn ws_rs_stop_arm_ordering()` — D-04a: within the same 1500-byte /stop window, find positions of `queue.clear`, `.store(false`, `QueueUpdated`, `Queue cleared. Current turn finishing.`, `Finished` and assert they appear in that order (each `.find` index strictly less than the next).
      - `#[test] fn ws_rs_cap_hit_message_locked()` — D-06: assert `WS_SOURCE.contains("Queue is full ({max}/{max}). /stop or /flush to drain.")` — the exact format string the in-flight push branch uses (Plan 04 Edit B emits this via `format!`). Tolerate either the literal `({max}/{max})` Rust format-string OR the runtime-evaluated `(128/128)` shape — pick the format-string form since the source is what we're asserting on.
      - `#[test] fn ws_rs_contains_pause_unpause_arms()` — assert `WS_SOURCE.contains("CommandResult::PauseQueue")`, `WS_SOURCE.contains("CommandResult::UnpauseQueue")`, `WS_SOURCE.contains("fetch_xor(true")` (D-01a toggle for /pause), `WS_SOURCE.contains("swap(false")` (D-07 explicit-set for /unpause).
      - `#[test] fn pause_toggle_and_double_unpause()` — direct unit test on the paused-flag atomic semantics (mirrors Plan 04's runtime behavior without needing to drive ws.rs): `let map = make_paused_map(); let flag = get_or_create_paused(&map, "sess-1");`. `/pause` toggle: `let was = flag.fetch_xor(true, Ordering::SeqCst); assert!(!was, "was_paused=false before first pause"); assert!(flag.load(Ordering::SeqCst), "now paused");`. `/unpause` swap: `let was2 = flag.swap(false, Ordering::SeqCst); assert!(was2, "swap returns true (was paused)"); assert!(!flag.load(Ordering::SeqCst), "now unpaused");`. Double-unpause: `let was3 = flag.swap(false, Ordering::SeqCst); assert!(!was3, "second swap returns false (was-not-paused branch)");`.
  </action>
  <verify>
    <automated>cargo test -p iron_hermes_ui --test web_queue_controls</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/iron_hermes_ui/tests/web_queue_controls.rs` exists.
    - `grep -c 'fn is_bypass_includes_pause_and_unpause\|fn ws_rs_new_clears_queue_before_session_reset\|fn ws_rs_stop_arm_no_abort_or_cancel\|fn ws_rs_stop_arm_ordering\|fn ws_rs_cap_hit_message_locked\|fn ws_rs_contains_pause_unpause_arms\|fn pause_toggle_and_double_unpause' crates/iron_hermes_ui/tests/web_queue_controls.rs` returns 7.
    - `cargo test -p iron_hermes_ui --test web_queue_controls` exits 0.
    - `cargo test -p iron_hermes_ui --test web_queue_controls ws_rs_stop_arm_no_abort_or_cancel` exits 0 (D-05 negative invariant compiles and passes).
    - `cargo test -p iron_hermes_ui --test web_queue_controls ws_rs_new_clears_queue_before_session_reset` exits 0 (D-04 ordering compiles and passes).
  </acceptance_criteria>
  <done>
    web_queue_controls.rs locks the D-04 / D-04a ordering invariants, D-05 negative invariant, D-06 cap text, D-08 downstream is_bypass contract, and the pause/unpause AtomicBool state-machine via direct unit assertions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create `tests/web_queue_protocol.rs` — protocol.rs variant + inline shape test anchor + mod.rs signal/arm + app_footer.rs pill anchors</name>
  <files>crates/iron_hermes_ui/tests/web_queue_protocol.rs</files>
  <read_first>
    - crates/iron_hermes_ui/tests/running_agent_guard_web_tests.rs (confirm `include_str!` pattern)
    - crates/iron_hermes_ui/src/protocol.rs (confirm Plan 02's `QueueUpdated { depth: u32, paused: bool }` variant and `test_queue_updated_json_shape` inline test name + the exact `{"QueueUpdated":{"depth":3,"paused":false}}` literal)
    - crates/iron_hermes_ui/src/components/hermes_app/mod.rs (confirm Plan 05's `queue_state` identifier, `ChatStreamEvent::QueueUpdated` arm, `queue_state.set((depth, paused))`)
    - crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs (confirm Plan 05's `queue_depth`, `queue_paused`, `queue_depth > 0`, `(paused)` tokens)
    - .planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-PATTERNS.md (`web_queue_protocol.rs` section)
    - .planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-VALIDATION.md (D-03, D-03a, D-11 verification map)
  </read_first>
  <action>
    Create `crates/iron_hermes_ui/tests/web_queue_protocol.rs`. Top: header doc comment.

    Source anchors:
      - `const PROTOCOL_SOURCE: &str = include_str!("../src/protocol.rs");`
      - `const MOD_RS_SOURCE: &str = include_str!("../src/components/hermes_app/mod.rs");`
      - `const APP_FOOTER_SOURCE: &str = include_str!("../src/components/hermes_app/app_footer.rs");`

    Tests:
      - `#[test] fn protocol_rs_declares_queue_updated_variant()` — assert `PROTOCOL_SOURCE.contains("QueueUpdated")`, `PROTOCOL_SOURCE.contains("depth: u32")`, `PROTOCOL_SOURCE.contains("paused: bool")`. Each assertion ties to D-03.
      - `#[test] fn protocol_rs_queue_updated_shape_test_exists()` — D-11: assert `PROTOCOL_SOURCE.contains("test_queue_updated_json_shape")` AND `PROTOCOL_SOURCE.contains(r#"{"QueueUpdated":{"depth":3,"paused":false}}"#)`. The raw-string literal locks the exact JSON wire format.
      - `#[test] fn mod_rs_handles_queue_updated_in_recv_loop()` — assert `MOD_RS_SOURCE.contains("QueueUpdated")`, `MOD_RS_SOURCE.contains("queue_state")`, `MOD_RS_SOURCE.contains("queue_state.set(")`, `MOD_RS_SOURCE.contains("use_context_provider(|| queue_state)")` (D-03a signal + provider).
      - `#[test] fn mod_rs_queue_state_initial_value_is_zero_unpaused()` — assert `MOD_RS_SOURCE.contains("(0u32, false)")` (the signal initializer Plan 05 ships).
      - `#[test] fn app_footer_rs_renders_queue_pill()` — assert `APP_FOOTER_SOURCE.contains("queue_depth")`, `APP_FOOTER_SOURCE.contains("queue_depth > 0")` (hide-when-zero), `APP_FOOTER_SOURCE.contains("(paused)")` (paused branch), `APP_FOOTER_SOURCE.contains("use_context::<Signal<(u32, bool)>>()")` (context consumer).
      - `#[test] fn app_footer_rs_pill_after_agent_span()` — locate index of `AGENT ` and index of `queue_depth > 0` in APP_FOOTER_SOURCE; assert the latter is greater than the former (D-03b discretion: pill placed after AGENT in the left run).
      - `#[test] fn queue_updated_confirmation_strings_frozen()` — stability guard on the user-visible format strings emitted by Plan 04's ws.rs. Build the expected strings inline and assert they match the documented contract: `assert_eq!(format!("Queued: \"{}\" ({} in queue)\n", "hello", 1u32), "Queued: \"hello\" (1 in queue)\n");`, `assert_eq!(format!("Queue is full ({}/{}). /stop or /flush to drain.\n", 128, 128), "Queue is full (128/128). /stop or /flush to drain.\n", "D-06: cap-hit message text frozen");`, `assert_eq!("Queue cleared. Current turn finishing.\n", "Queue cleared. Current turn finishing.\n", "D-05: /stop Delta message frozen");`. These act as fail-loud canaries if any future edit silently changes the user-visible strings.
  </action>
  <verify>
    <automated>cargo test -p iron_hermes_ui --test web_queue_protocol</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/iron_hermes_ui/tests/web_queue_protocol.rs` exists.
    - `grep -c 'include_str!' crates/iron_hermes_ui/tests/web_queue_protocol.rs` returns 3 (protocol.rs + mod.rs + app_footer.rs).
    - `grep -c 'fn protocol_rs_declares_queue_updated_variant\|fn protocol_rs_queue_updated_shape_test_exists\|fn mod_rs_handles_queue_updated_in_recv_loop\|fn mod_rs_queue_state_initial_value_is_zero_unpaused\|fn app_footer_rs_renders_queue_pill\|fn app_footer_rs_pill_after_agent_span\|fn queue_updated_confirmation_strings_frozen' crates/iron_hermes_ui/tests/web_queue_protocol.rs` returns 7.
    - `cargo test -p iron_hermes_ui --test web_queue_protocol` exits 0.
    - `cargo test -p iron_hermes_ui --test web_queue_protocol protocol_rs_queue_updated_shape_test_exists` exits 0 (D-11 wire-format lock).
  </acceptance_criteria>
  <done>
    web_queue_protocol.rs locks the typed-event variant, the inline JSON shape test name + literal string, the HermesApp signal + arm + provider, the AppFooter context consumer + pill render + paused branch + placement-after-AGENT discretion, and the user-visible string canaries.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p iron_hermes_ui` green (full crate suite — every existing test + the three new files).
- `cargo test -p ironhermes-gateway` green (D-12 invariant — no regression in the production trait implementor).
- `cargo test -p ironhermes-cli --features test-support` green (D-12 invariant — no regression in the prior trait consumer).
- `cargo check --workspace --all-features` green (D-12 final gate).
</verification>

<success_criteria>
- D-09 test split shipped (three files, mirrors 36.17.3 triplet).
- D-10 highest-leverage seam honored: source-text anchors + direct unit tests on production types, no Axum spin-up.
- D-11 wire-format lock asserted from a downstream consumer's perspective (in addition to Plan 02's inline test).
- D-12 phase gate intact: full workspace test + check green.
</success_criteria>

<output>
Create `.planning/phases/36.17.4-wire-up-iron-hermes-ui-to-the-gateway-queue-slash-commands/36.17.4-06-SUMMARY.md` when done.
</output>
