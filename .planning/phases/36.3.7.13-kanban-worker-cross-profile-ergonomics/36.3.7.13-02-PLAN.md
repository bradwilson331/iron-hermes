---
phase: 36.3.7.13
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-kanban/src/worker_spawn.rs
  - crates/ironhermes-kanban/tests/worker_bin_resolution.rs
autonomous: true
requirements: [F-02]
must_haves:
  truths:
    - "IRONHERMES_WORKER_BIN env var, when set, overrides the hardcoded `ironhermes` PATH lookup at worker_spawn.rs:254"
    - "When IRONHERMES_WORKER_BIN is unset, worker_spawn falls back to bare `ironhermes` (pre-this-phase behavior preserved)"
    - "IRONHERMES_WORKER_BIN appears in SAFE_SYSTEM_VARS so recursive worker spawn forward-carries the override"
    - "build_kanban_worker_env propagates IRONHERMES_WORKER_BIN from parent process env to child env when set"
  artifacts:
    - path: "crates/ironhermes-kanban/src/worker_spawn.rs"
      provides: "resolve_worker_bin() helper + SAFE_SYSTEM_VARS extension + Command::new() swap"
      contains: "resolve_worker_bin"
    - path: "crates/ironhermes-kanban/tests/worker_bin_resolution.rs"
      provides: "4 bilateral tests covering F-02 (env wins, fallback, SAFE_SYSTEM_VARS membership, forward-carry)"
      contains: "IRONHERMES_WORKER_BIN"
  key_links:
    - from: "crates/ironhermes-kanban/src/worker_spawn.rs:254"
      to: "resolve_worker_bin()"
      via: "Command::new() spawn site"
      pattern: "Command::new\\(resolve_worker_bin\\(\\)\\)"
    - from: "crates/ironhermes-kanban/src/worker_spawn.rs:63"
      to: "SAFE_SYSTEM_VARS"
      via: "appended `\"IRONHERMES_WORKER_BIN\"`"
      pattern: "\"IRONHERMES_WORKER_BIN\""
---

<objective>
Close F-02 (worker-bin override). Add `resolve_worker_bin()` helper to `crates/ironhermes-kanban/src/worker_spawn.rs` reading `IRONHERMES_WORKER_BIN` env first (falling back to bare `"ironhermes"` PATH lookup). Replace `Command::new("ironhermes")` at line 254 with `Command::new(resolve_worker_bin())`. Append `"IRONHERMES_WORKER_BIN"` to `SAFE_SYSTEM_VARS` at line 63 so recursive worker spawn carries the override forward.

Purpose: Operator hit silent stale-code execution during Phase 36.3.7.12 live UAT (`~/.local/bin/ironhermes` symlink ran May-31 binary with no goal loop). `cargo run` from worktree now pins worker subprocesses to the same code base via the env override — no more symlink dance.

Output: One new helper + 2-line edit to SAFE_SYSTEM_VARS + 1 new test file with 4 tests. Operator validation depth: as-is per CONTEXT.md (no file-exists or executable-bit check; deferred to future hardening phase).

This plan is **file-disjoint from Plan 01** (no overlap in files_modified). Both Wave 1 plans can run in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-CONTEXT.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-RESEARCH.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-PATTERNS.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Key signatures the executor needs. Extracted from worker_spawn.rs. -->

From crates/ironhermes-kanban/src/worker_spawn.rs:63-71 (current state — 7 entries):
  pub const SAFE_SYSTEM_VARS: &[&str] = &[
      "PATH", "HOME", "USER", "LANG", "TERM", "RUST_LOG", "IRONHERMES_HOME",
  ];

After this plan (8 entries, append-only):
  pub const SAFE_SYSTEM_VARS: &[&str] = &[
      "PATH", "HOME", "USER", "LANG", "TERM", "RUST_LOG", "IRONHERMES_HOME",
      "IRONHERMES_WORKER_BIN",  // Phase 36.3.7.13 D-02
  ];

From crates/ironhermes-kanban/src/worker_spawn.rs:254 (current):
  let child = Command::new("ironhermes")

After:
  let child = Command::new(resolve_worker_bin())

New fn (PATTERNS.md § worker_spawn.rs anchor):
  fn resolve_worker_bin() -> String {
      std::env::var("IRONHERMES_WORKER_BIN").unwrap_or_else(|_| "ironhermes".to_string())
  }

build_kanban_worker_env env-passthrough mechanism: existing code at line 111+ iterates SAFE_SYSTEM_VARS and forwards any that are set in parent env. Adding "IRONHERMES_WORKER_BIN" to that array enables forward-carry — no other wiring needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write 4 bilateral tests for resolve_worker_bin + SAFE_SYSTEM_VARS forward-carry (Wave 0 scaffold)</name>
  <files>crates/ironhermes-kanban/tests/worker_bin_resolution.rs</files>
  <read_first>
    - crates/ironhermes-kanban/src/worker_spawn.rs lines 60-75 (SAFE_SYSTEM_VARS constant)
    - crates/ironhermes-kanban/src/worker_spawn.rs lines 100-140 (build_kanban_worker_env — env passthrough loop iterating SAFE_SYSTEM_VARS)
    - crates/ironhermes-kanban/src/worker_spawn.rs lines 250-260 (Command::new("ironhermes") site)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-PATTERNS.md (§ worker_spawn.rs section — exact code shapes)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-VALIDATION.md (lines 52-55 — 4 test rows for F-02)
    - crates/ironhermes-kanban/tests/goal_mode_migration.rs (analog scaffold — tempfile + ENV_LOCK guard)
  </read_first>
  <behavior>
    - Test 1 (F-02 env wins): with HERMES_KANBAN_TASK + IRONHERMES_WORKER_BIN set, resolve_worker_bin() returns the env value verbatim
    - Test 2 (F-02 fallback): with IRONHERMES_WORKER_BIN unset, resolve_worker_bin() returns "ironhermes" literal
    - Test 3 (F-02 SAFE_SYSTEM_VARS membership): SAFE_SYSTEM_VARS contains "IRONHERMES_WORKER_BIN" (assert via `.contains(&"IRONHERMES_WORKER_BIN")`)
    - Test 4 (F-02 forward-carry): with IRONHERMES_WORKER_BIN set in parent process env, build_kanban_worker_env produces a child env that includes that entry. (Drive the test against build_kanban_worker_env directly with a synthesized Task fixture; assert the returned Vec<(OsString, OsString)> contains the IRONHERMES_WORKER_BIN entry.)
  </behavior>
  <action>
    Create NEW file `crates/ironhermes-kanban/tests/worker_bin_resolution.rs`. Mirror the scaffold from `tests/goal_mode_migration.rs`: `use tempfile::TempDir`, `use ironhermes_kanban::worker_spawn::{SAFE_SYSTEM_VARS, build_kanban_worker_env}` (or whatever the module path is — verify via grep on lib.rs re-exports). NOTE: `resolve_worker_bin` is a private free fn (per PATTERNS.md anchor) — Test 1 + Test 2 cannot call it directly. INSTEAD, Test 1 + Test 2 set `IRONHERMES_WORKER_BIN` (or don't) and assert via `build_kanban_worker_env` output OR add an inline `#[cfg(test)]` `pub(crate)` re-export. Recommended: make `resolve_worker_bin` `pub(crate)` to enable direct unit testing. If the executor prefers to keep it private, Test 1 and Test 2 can drive it indirectly by inspecting the child env emitted by build_kanban_worker_env when IRONHERMES_WORKER_BIN is in SAFE_SYSTEM_VARS. ENV_LOCK guard mandatory (per STATE.md side-quest 4208bc28): `static ENV_LOCK: std::sync::Mutex<()>` at file top; `let _g = ENV_LOCK.lock().unwrap_or_else(|e| e.into_inner())` at start of each test; teardown via `std::env::remove_var("IRONHERMES_WORKER_BIN")` in each test's exit path.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-kanban --test worker_bin_resolution -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f crates/ironhermes-kanban/tests/worker_bin_resolution.rs`
    - `grep -c 'IRONHERMES_WORKER_BIN' crates/ironhermes-kanban/tests/worker_bin_resolution.rs` >= 4 (one per test)
    - `grep -c 'static ENV_LOCK: std::sync::Mutex' crates/ironhermes-kanban/tests/worker_bin_resolution.rs` >= 1 (race-isolation guard)
    - Initial state: `cargo test -p ironhermes-kanban --test worker_bin_resolution -- --test-threads=1` reports 4 tests FAILING (tests reference `resolve_worker_bin` / SAFE_SYSTEM_VARS entry that don't yet exist or expectations not yet met). This is the RED phase per TDD discipline.
  </acceptance_criteria>
  <done>4 tests scaffolded with ENV_LOCK guards and IRONHERMES_WORKER_BIN setup/teardown. Tests fail (RED) — producer wiring in Task 2 makes them GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add resolve_worker_bin() + swap Command::new + extend SAFE_SYSTEM_VARS → make 4 tests pass</name>
  <files>crates/ironhermes-kanban/src/worker_spawn.rs</files>
  <read_first>
    - crates/ironhermes-kanban/src/worker_spawn.rs lines 60-71 (SAFE_SYSTEM_VARS to extend)
    - crates/ironhermes-kanban/src/worker_spawn.rs lines 250-260 (Command::new("ironhermes") swap site)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-PATTERNS.md (§ worker_spawn.rs SAFE_SYSTEM_VARS append + § resolve_worker_bin() helper + § Swap site at line 254 — three exact code blocks)
    - crates/ironhermes-kanban/tests/worker_bin_resolution.rs (the failing tests from Task 1 — make them pass)
  </read_first>
  <behavior>
    - Producer wiring that satisfies the 4 RED tests from Task 1:
      - resolve_worker_bin() fn exists and behaves per env-or-fallback contract
      - SAFE_SYSTEM_VARS includes "IRONHERMES_WORKER_BIN" as the 8th entry
      - Command::new at line 254 invokes resolve_worker_bin() instead of literal "ironhermes"
      - build_kanban_worker_env, by virtue of the SAFE_SYSTEM_VARS extension, now forwards parent-process IRONHERMES_WORKER_BIN to child env
  </behavior>
  <action>
    Three edits in `crates/ironhermes-kanban/src/worker_spawn.rs`:

    1. Line 63-71 — append `"IRONHERMES_WORKER_BIN",  // Phase 36.3.7.13 D-02: forward-compat for recursive worker spawn` as the 8th entry of `SAFE_SYSTEM_VARS`. Append-only — preserve existing 7 entries verbatim. This single edit enables forward-carry (build_kanban_worker_env iterates this array; nothing else to wire).

    2. New helper `resolve_worker_bin` added near the top of the file (next to SAFE_SYSTEM_VARS or just above `build_kanban_worker_env`). Visibility: `pub(crate)` if Task 1's tests call it directly; `fn` (private) if Task 1 drives it indirectly. Pick based on what Task 1's test file imports. Body per PATTERNS.md § resolve_worker_bin helper anchor: `std::env::var("IRONHERMES_WORKER_BIN").unwrap_or_else(|_| "ironhermes".to_string())`. Doc comment cites Phase 36.3.7.13 D-02 and `~/.local/bin/ironhermes` symlink dance context per RESEARCH.md.

    3. Line 254 — replace `Command::new("ironhermes")` with `Command::new(resolve_worker_bin())`. One-token swap. Add inline `// Phase 36.3.7.13 D-02` comment above the call.

    After all 3 edits, run `cargo test -p ironhermes-kanban --test worker_bin_resolution -- --test-threads=1` — tests from Task 1 must transition RED → GREEN. Also re-run the full ironhermes-kanban suite (`cargo test -p ironhermes-kanban --no-fail-fast`) to confirm zero regressions on the existing 120-test baseline (especially the existing `worker_spawn` inline tests).
  </action>
  <verify>
    <automated>cargo test -p ironhermes-kanban --test worker_bin_resolution -- --test-threads=1 && cargo test -p ironhermes-kanban --no-fail-fast 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"IRONHERMES_WORKER_BIN"' crates/ironhermes-kanban/src/worker_spawn.rs` >= 1 (SAFE_SYSTEM_VARS entry)
    - `grep -cE 'fn resolve_worker_bin\(\)' crates/ironhermes-kanban/src/worker_spawn.rs` == 1
    - `grep -c 'Command::new(resolve_worker_bin())' crates/ironhermes-kanban/src/worker_spawn.rs` == 1
    - `grep -c 'Command::new("ironhermes")' crates/ironhermes-kanban/src/worker_spawn.rs` == 0 (the literal must be fully replaced)
    - `cargo test -p ironhermes-kanban --test worker_bin_resolution -- --test-threads=1` reports `4 passed; 0 failed` (transitioned RED → GREEN)
    - `cargo test -p ironhermes-kanban --no-fail-fast` reports >= 124 passed (120 baseline + 4 new from Task 1); 0 NEW failures
    - `cargo build -p ironhermes-kanban` exit 0
    - Crate-isolation fence: `grep -cE '^ironhermes-gateway\s*=' crates/ironhermes-kanban/Cargo.toml` == 0 (unchanged)
  </acceptance_criteria>
  <done>resolve_worker_bin() defined, SAFE_SYSTEM_VARS at 8 entries with IRONHERMES_WORKER_BIN included, Command::new uses helper. 4 tests GREEN. No regressions on the 120-test baseline. F-02 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → process env | IRONHERMES_WORKER_BIN path string is operator-supplied at parent process invocation |
| Parent process → worker subprocess | env passthrough for SAFE_SYSTEM_VARS (recursive worker spawn) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.13-02-T01 | Tampering | IRONHERMES_WORKER_BIN binary substitution | accept | Operator owns env. Locked by SCOPE.md D-02. Command::new errors surface at spawn time. No file-exists or executable-bit check (deferred to future hardening). Severity LOW — env is not a network surface and is functionally equivalent to PATH lookup substitution risk that already exists. |
| T-36.3.7.13-02-SC | Tampering | crate-isolation fence | mitigate | Task 2 acceptance gate: `grep -cE '^ironhermes-gateway\s*=' crates/ironhermes-kanban/Cargo.toml` == 0. No new deps; pure additive change to existing worker_spawn.rs file. |
</threat_model>

<verification>
- `cargo test -p ironhermes-kanban --test worker_bin_resolution -- --test-threads=1` reports 4 passed
- `cargo test -p ironhermes-kanban --no-fail-fast` reports >= 124 passed, 0 NEW failures
- `cargo build -p ironhermes-kanban` exit 0
- File-modified set is disjoint from Plan 01 (verified at planning time; both Wave 1 plans can run in parallel)
- IRONHERMES_WORKER_BIN forward-carries in build_kanban_worker_env (Test 4)
</verification>

<success_criteria>
- F-02 materially observable: `resolve_worker_bin()` exists in worker_spawn.rs and is invoked at Command::new (line 254 area)
- `IRONHERMES_WORKER_BIN` appears as the 8th entry of SAFE_SYSTEM_VARS at line 63
- 4 bilateral tests in tests/worker_bin_resolution.rs all pass
- Operator can now run `IRONHERMES_WORKER_BIN=$(pwd)/target/debug/ironhermes ironhermes kanban dispatch` from a worktree and have worker subprocesses use the worktree binary
</success_criteria>

<output>
Create `.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-02-SUMMARY.md` when done. Include: 3-edit summary (SAFE_SYSTEM_VARS, resolve_worker_bin fn, Command::new swap); 4-test pass record from worker_bin_resolution.rs; bilateral-tracing table (producer = worker_spawn.rs 3 edits; consumer = 4 tests); crate-isolation fence audit; F-02 anchor at worker_spawn.rs:254.
</output>
