---
phase: 36.3.7.13
plan: 04
type: execute
wave: 3
depends_on: ["36.3.7.13-03"]
files_modified:
  - docs/configuration/profiles.md
  - docs/kanban/cli-reference.md
  - docs/kanban/profile-discipline.md
  - docs/kanban/goal-mode-uat-prompts.md
  - skills/kanban-worker/SKILL.md
  - docs/kanban/reference.md
autonomous: false
requirements: [D-D1, D-C1]
must_haves:
  truths:
    - "docs/configuration/ directory exists (must mkdir -p before creating profiles.md per RESEARCH § DOC-A)"
    - "All 6 documentation surfaces exist and pass markdown-link-check (§7 gate)"
    - "DOC-A profiles.md documents HERMES_HOME resolution table, profile inheritance, --profile flag effect, mixed-profile pitfall (cites Phase 36.3.7.12 F-01)"
    - "DOC-B cli-reference.md covers 15 verbs: create/dispatch/show/unblock/list/link/heartbeat/comment/swarm/mention/decompose/specify + notify-subscribe/notify-list/notify-unsubscribe — each with summary, args, examples, env reads, env writes, side effects"
    - "DOC-C profile-discipline.md contains the canonical §5 LIVE UAT script for cross-profile DB resolution (per D-C1) + card-reset SQL recipe"
    - "DOC-D goal-mode-uat-prompts.md contains the §6 dual-rail UAT pattern (live + unit-cite branches per D-C1) + 4-row reference UAT script"
    - "DOC-E SKILL.md amends Goal mode section with HERMES_KANBAN_GOAL_TOOLSET + clamp note (NOT a rewrite — targeted amendment)"
    - "DOC-F reference.md amended in 4 locations: Schema table v3 row, columns table goal_toolset row, config table kanban.goal_inner_max_iterations row, new 'Goal mode operational model' section linking to DOC-A..DOC-D"
  artifacts:
    - path: "docs/configuration/profiles.md"
      provides: "DOC-A — Profile resolution reference"
      min_lines: 250
    - path: "docs/kanban/cli-reference.md"
      provides: "DOC-B — Per-verb CLI cheatsheet"
      min_lines: 350
    - path: "docs/kanban/profile-discipline.md"
      provides: "DOC-C — Operator runbook + §5 LIVE UAT script"
      min_lines: 180
    - path: "docs/kanban/goal-mode-uat-prompts.md"
      provides: "DOC-D — UAT prompt patterns + §6 dual-rail script"
      min_lines: 180
    - path: "skills/kanban-worker/SKILL.md"
      provides: "DOC-E — UPDATED with new env var + clamp note"
      contains: "HERMES_KANBAN_GOAL_TOOLSET"
    - path: "docs/kanban/reference.md"
      provides: "DOC-F — UPDATED with 4 targeted amendments"
      contains: "goal_toolset"
  key_links:
    - from: "docs/kanban/reference.md (DOC-F 'Goal mode operational model' section)"
      to: "docs/configuration/profiles.md + docs/kanban/cli-reference.md + docs/kanban/profile-discipline.md + docs/kanban/goal-mode-uat-prompts.md"
      via: "markdown link block (DOC-A..DOC-D)"
      pattern: "configuration/profiles.md|cli-reference.md|profile-discipline.md|goal-mode-uat-prompts.md"
    - from: "DOC-C profile-discipline.md (§5 LIVE UAT script)"
      to: "RESEARCH.md § UAT Scripts §5"
      via: "verbatim copy of the cross-profile UAT script"
      pattern: "HERMES_KANBAN_DB override"
---

<objective>
Ship all 6 documentation surfaces per CONTEXT D-D1 and lock the §5 LIVE UAT + §6 dual-rail evidence per D-C1.

- DOC-A `docs/configuration/profiles.md` (NEW) — profile resolution + HERMES_HOME table + mixed-profile pitfall
- DOC-B `docs/kanban/cli-reference.md` (NEW) — 15-verb cheatsheet (the canonical post-this-phase CLI surface)
- DOC-C `docs/kanban/profile-discipline.md` (NEW) — operator runbook + §5 LIVE UAT script (cross-profile DB resolution per D-C1)
- DOC-D `docs/kanban/goal-mode-uat-prompts.md` (NEW) — UAT prompt patterns + §6 dual-rail (live + unit-cite branches per D-C1) + 4-row reference UAT
- DOC-E `skills/kanban-worker/SKILL.md` (UPDATE — targeted amendment, NOT rewrite) — add `HERMES_KANBAN_GOAL_TOOLSET` + clamp note
- DOC-F `docs/kanban/reference.md` (UPDATE — 4 targeted amendments) — Schema v3 row, columns table goal_toolset, config table kanban.goal_inner_max_iterations, new "Goal mode operational model" section linking to DOC-A..DOC-D

§7 verification gate: all 6 docs pass `markdown-link-check` (or grep-based cross-link audit fallback).

§5 LIVE UAT is REQUIRED for phase close per D-C1. §6 is dual-rail (live preferred OR unit-cite from Plan 03's goal_toolset_filter.rs).

This plan has a `checkpoint:human-verify` task at the end — the operator runs the §5 LIVE UAT script from DOC-C, captures output, and confirms PASS or describes failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-CONTEXT.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-RESEARCH.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-PATTERNS.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-VALIDATION.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-01-SUMMARY.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-02-SUMMARY.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-03-SUMMARY.md
@docs/kanban/reference.md
@skills/kanban-worker/SKILL.md
@CLAUDE.md

<interfaces>
<!-- Content specs sourced from CONTEXT D-D1 + RESEARCH § Documentation Surface Inventory + RESEARCH § UAT Scripts. -->
<!-- DOC-A through DOC-D have no codebase analog — use the content specs verbatim. -->

DOC-A docs/configuration/profiles.md (NEW, ~250-500 lines):
Sections required:
1. HERMES_HOME resolution table (3 rows: interactive shell, CLI dispatcher, worker subprocess)
2. ~/.ironhermes/config.yaml — global config (canonical example, every field documented)
3. ~/.ironhermes/profiles/<name>/config.yaml — profile override (canonical example)
4. Profile inheritance rules (profile overlays global)
5. `--profile <name>` flag effect on every subprocess
6. Mixed-profile pitfall (cite Phase 36.3.7.12 F-01 specifically: "dispatcher under profile B, worker booted profile A, card invisible")
7. HERMES_KANBAN_DB bridge explanation (the F-01 fix this phase shipped — Plan 01)

DOC-B docs/kanban/cli-reference.md (NEW, ~400-600 lines):
15 verbs, each gets: one-line summary; required vs optional args; examples (happy + edge); env-var reads; env-var writes; DB/log side effects.
Verbs: create, dispatch, show, unblock, list, link, heartbeat, comment, swarm, mention, decompose, specify, notify-subscribe, notify-list, notify-unsubscribe.
`create` MUST document the new `--goal-toolset {restricted|extended|full}` flag (shipped Plan 03).

DOC-C docs/kanban/profile-discipline.md (NEW, ~200-300 lines):
1. "All kanban verbs in a UAT chain MUST agree on `--profile`" (lede)
2. Step-by-step: setting up a `dev` profile from scratch (template config.yaml provided)
3. ~/.local/bin/ironhermes symlink workflow (legacy)
4. IRONHERMES_WORKER_BIN shortcut (post-Plan-02 — the modern path)
5. **§5 LIVE UAT SCRIPT** — copy verbatim from RESEARCH.md § UAT Scripts §5 (this is the canonical landing site per D-C1)
6. Card-reset SQL recipe for breaker recovery (verbatim from Phase 36.3.7.12 UAT notes):
   ```sql
   UPDATE tasks SET status='ready', claim_pid=NULL, claim_lock=NULL,
     consecutive_failures=0, goal_turns_used=0
   WHERE id='t_<your_task_id>';
   ```

DOC-D docs/kanban/goal-mode-uat-prompts.md (NEW, ~200-300 lines):
1. FINDING-03 lessons:
   - Avoid filesystem-temptation prompts ("Find the string X" → LLM greps filesystem)
   - Prefer logical-impossibility over tool-evadability ("summary='banana' AND summary='apple' simultaneously")
   - Prefer constrained-output over open-ended ("reply in exactly 0 tokens")
   - Use `--goal-toolset restricted` (default post this phase)
2. 4-row reference UAT script: constrained-output haiku (Row 1 retest), logical-impossibility (Row 2), judge-error cascade (Row 3), auto_decompose interaction (Row 4)
3. **§6 DUAL-RAIL UAT** — copy from RESEARCH.md § UAT Scripts §6 Rail A (live) + Rail B (unit-cite — points at Plan 03's goal_toolset_filter.rs tests)

DOC-E skills/kanban-worker/SKILL.md (UPDATE — targeted amendment per CONTEXT D-D1):
Insertion point: after the existing `## Goal mode` section (SKILL.md line 193), inside the env-vars list at lines 201-205. Append:
- `HERMES_KANBAN_GOAL_TOOLSET=<preset>` — bullet describing the new env var
- After the env list, new paragraph: "The inner agent loop is clamped to `kanban.goal_inner_max_iterations` (default 10)..."
PRESERVE existing content. DO NOT rewrite the Phase 36.3.7.12 Goal mode section.

DOC-F docs/kanban/reference.md (UPDATE — 4 targeted amendments per RESEARCH § DOC-F):
1. Schema table — after the goal_turns_used row in the v2 columns table: insert v3 row for goal_toolset
2. Worker-shell env contract table — after HERMES_KANBAN_GOAL_MAX_TURNS row: insert HERMES_KANBAN_GOAL_TOOLSET row
3. Config table — after kanban.judge_model row: insert kanban.goal_inner_max_iterations row
4. NEW "Goal mode operational model" section — insert before `#### Scope boundary` (or after `#### Budget-exhaustion-as-BLOCKED contract`); contains links to DOC-A, DOC-B, DOC-C, DOC-D + summary of v2→v3 migration

§5 LIVE UAT SCRIPT (canonical — sourced from RESEARCH.md § UAT Scripts §5, lines 606-663):
```bash
#!/usr/bin/env bash
set -euo pipefail
PROFILE_A="dev"; PROFILE_B="default"
IRONHERMES_BIN="${IRONHERMES_WORKER_BIN:-$(which ironhermes)}"
TASK_ID=$("$IRONHERMES_BIN" --profile "$PROFILE_A" kanban create --goal --goal-toolset restricted --goal-max-turns 3 --assignee "$PROFILE_A" --body "Acceptance: produce the word PARITY in uppercase with no other output." "§5 cross-profile DB resolution UAT" --json 2>/dev/null | jq -r '.task_id')
PROFILE_A_DB="$HOME/.ironhermes/profiles/$PROFILE_A/kanban.db"
export HERMES_KANBAN_DB="$PROFILE_A_DB"; export IRONHERMES_WORKER_BIN="$IRONHERMES_BIN"
"$IRONHERMES_BIN" --profile "$PROFILE_B" kanban dispatch 2>&1 | tee /tmp/dispatch-uatlog.txt
grep -q "HERMES_KANBAN_DB override" /tmp/dispatch-uatlog.txt
# Poll up to 120s for status=done
```
Expected: `grep -c 'HERMES_KANBAN_DB override' /tmp/dispatch-uatlog.txt` >= 1 AND final status = "done" under profile A.

Cross-link integrity (§7 markdown-link-check fallback if tool unavailable):
For each of the 6 docs, every `](relative_or_absolute_path.md)` must resolve. Use a grep-based audit:
  `for f in docs/configuration/profiles.md docs/kanban/cli-reference.md docs/kanban/profile-discipline.md docs/kanban/goal-mode-uat-prompts.md docs/kanban/reference.md skills/kanban-worker/SKILL.md; do echo "$f"; grep -oE '\]\([^)]+\.md\)' "$f"; done`
Then verify each cited file exists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create directory + DOC-A profiles.md + DOC-B cli-reference.md (the two largest NEW surfaces)</name>
  <files>docs/configuration/profiles.md, docs/kanban/cli-reference.md</files>
  <read_first>
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-CONTEXT.md (§ Documentation surfaces — DOC-A and DOC-B specs)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-RESEARCH.md (§ Documentation Surface Inventory § DOC-A, DOC-B — content anchors)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-SCOPE.md (lines 89-105 — DOC-A, DOC-B preliminary content)
    - .planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-05-SUMMARY.md (F-01 description verbatim — cite in DOC-A's mixed-profile pitfall section)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-01-SUMMARY.md (Plan 01's swap inventory — DOC-A's HERMES_KANBAN_DB bridge section references this)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-02-SUMMARY.md (Plan 02 IRONHERMES_WORKER_BIN — DOC-A + DOC-B cite this)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-03-SUMMARY.md (Plan 03 --goal-toolset flag — DOC-B's `create` verb section cites this)
    - docs/kanban/reference.md (read first 100 lines to understand markdown style + table conventions used in this repo)
  </read_first>
  <action>
    Step 1: `mkdir -p docs/configuration/` — per RESEARCH § DOC-A "Current state: MISSING — `docs/configuration/` directory does not exist". Bash invocation.

    Step 2: Create `docs/configuration/profiles.md` (DOC-A) using the Write tool. Required sections per CONTEXT D-D1 + RESEARCH § DOC-A:
    - Title + 1-paragraph lede explaining what a profile is (HERMES_HOME-scoped install + config + DB layout)
    - HERMES_HOME resolution table (3 rows: interactive shell, CLI dispatcher, worker subprocess) — each row explains how HERMES_HOME is resolved in that context
    - Section: Global config file `~/.ironhermes/config.yaml` with annotated example (every field documented with one-line purpose)
    - Section: Profile override `~/.ironhermes/profiles/<name>/config.yaml` with annotated example
    - Section: Profile inheritance rules (profile overlays global; explicit field set in profile wins)
    - Section: `--profile <name>` flag — table of subprocesses that respect it
    - **Mixed-profile pitfall** — citing Phase 36.3.7.12 F-01 verbatim from 36.3.7.12-05-SUMMARY.md ("dispatcher under profile B, worker booted profile A, card invisible")
    - Section: HERMES_KANBAN_DB bridge — explains the F-01 fix from Plan 01 (open_from_env() + 25 swap sites)
    - Cross-link block: links to DOC-B (cli-reference.md), DOC-C (profile-discipline.md), DOC-F (reference.md)

    Step 3: Create `docs/kanban/cli-reference.md` (DOC-B). Required 15-verb cheatsheet per CONTEXT D-D1:
    - Lede explaining the global `--profile P` flag applies to all 15 verbs
    - One section per verb. Verbs (15): `create`, `dispatch`, `show`, `unblock`, `list`, `link`, `heartbeat`, `comment`, `swarm`, `mention`, `decompose`, `specify`, `notify-subscribe`, `notify-list`, `notify-unsubscribe`
    - Each verb section uses a consistent structure:
      ```
      ### kanban <verb>
      **Summary:** one-line description
      **Synopsis:** `ironhermes [--profile P] kanban <verb> [args]`
      **Required args:** ...
      **Optional args:** ...
      **Env vars read:** HERMES_KANBAN_DB, ...
      **Env vars written:** (only relevant for `dispatch`)
      **Side effects:** writes to ~/.ironhermes/profiles/<P>/kanban.db (or $HERMES_KANBAN_DB), appends to worker logs at ...
      **Examples:**
      - Happy path: `ironhermes --profile dev kanban <verb> ...`
      - Edge case: ...
      ```
    - For `create`, document the new `--goal-toolset {restricted|extended|full}` flag (shipped Plan 03) AND the existing `--goal` / `--goal-max-turns` (shipped Phase 36.3.7.12) AND `--assignee` / `--body` / TITLE positional
    - For `dispatch`, document that it reads HERMES_KANBAN_DB (Plan 01) and IRONHERMES_WORKER_BIN (Plan 02)
    - Cross-link block at top: links to DOC-A (profiles.md), DOC-C (profile-discipline.md), DOC-F (reference.md)

    Both files use the same markdown style as `docs/kanban/reference.md` (tables for tabular data; fenced bash blocks for examples; H2/H3 hierarchy).
  </action>
  <verify>
    <automated>test -f docs/configuration/profiles.md && test -f docs/kanban/cli-reference.md && wc -l docs/configuration/profiles.md docs/kanban/cli-reference.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -d docs/configuration` exits 0 (directory created)
    - `test -f docs/configuration/profiles.md` exits 0
    - `wc -l docs/configuration/profiles.md` shows >= 200 lines (target ~250-500 per RESEARCH)
    - `grep -c 'HERMES_HOME' docs/configuration/profiles.md` >= 3 (resolution table + bridge section)
    - `grep -c 'HERMES_KANBAN_DB' docs/configuration/profiles.md` >= 2 (bridge section + F-01 pitfall citation)
    - `grep -c 'F-01' docs/configuration/profiles.md` >= 1 (Phase 36.3.7.12 citation per RESEARCH § DOC-A content anchor)
    - `test -f docs/kanban/cli-reference.md` exits 0
    - `wc -l docs/kanban/cli-reference.md` shows >= 300 lines
    - DOC-B contains all 15 verb sections: `for v in create dispatch show unblock list link heartbeat comment swarm mention decompose specify notify-subscribe notify-list notify-unsubscribe; do grep -c "kanban $v" docs/kanban/cli-reference.md; done` — every count >= 1
    - `grep -c 'goal-toolset' docs/kanban/cli-reference.md` >= 1 (new flag documented under create)
    - `grep -c 'IRONHERMES_WORKER_BIN' docs/kanban/cli-reference.md` >= 1 (documented under dispatch)
    - Cross-link integrity for these 2 files (grep audit): every `](relative.md)` link resolves to an existing file or to one of the other 4 docs being created in Tasks 2-3
  </acceptance_criteria>
  <done>DOC-A (profiles.md) + DOC-B (cli-reference.md) shipped with all required content sections. Directory created. Cross-links coherent (other docs created in later tasks).</done>
</task>

<task type="auto">
  <name>Task 2: DOC-C profile-discipline.md + DOC-D goal-mode-uat-prompts.md (operator runbooks with §5 LIVE + §6 dual-rail UAT scripts)</name>
  <files>docs/kanban/profile-discipline.md, docs/kanban/goal-mode-uat-prompts.md</files>
  <read_first>
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-CONTEXT.md (§ Documentation surfaces — DOC-C and DOC-D specs)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-RESEARCH.md (§ DOC-C + § DOC-D content anchors; § UAT Scripts §5 + §6 — VERBATIM scripts to embed)
    - .planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-05-SUMMARY.md (FINDING-03 lessons — verbatim source for DOC-D)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-03-SUMMARY.md (Plan 03 §6 unit-cite test names — referenced by DOC-D Rail B)
  </read_first>
  <action>
    Create `docs/kanban/profile-discipline.md` (DOC-C). Required sections per CONTEXT D-D1:
    1. Lede: "All kanban verbs in a UAT chain MUST agree on --profile" — explain why (HERMES_HOME divergence → cards invisible across profiles)
    2. Section "Setting up a dev profile from scratch": step-by-step (mkdir ~/.ironhermes/profiles/dev/; copy template config.yaml; explain HERMES_HOME override); include template config.yaml
    3. Section "The ~/.local/bin/ironhermes symlink workflow" (legacy approach): instructions + caveat that it can run stale code (the F-02 footgun)
    4. Section "IRONHERMES_WORKER_BIN shortcut" (modern, post-Plan-02): example `IRONHERMES_WORKER_BIN=$(pwd)/target/debug/ironhermes ironhermes --profile dev kanban dispatch` + explain why this beats the symlink
    5. **Section "§5 LIVE UAT — Cross-Profile DB Resolution"** — copy the script VERBATIM from RESEARCH.md § UAT Scripts §5 (RESEARCH.md lines 606-663). Include the expected log grep assertions. THIS IS THE LIVE-REQUIRED gate per CONTEXT D-C1.
    6. Section "Card-reset SQL recipe for breaker recovery" — embed the canonical UPDATE statement from RESEARCH § DOC-C:
       ```sql
       UPDATE tasks SET status='ready', claim_pid=NULL, claim_lock=NULL,
         consecutive_failures=0, goal_turns_used=0
       WHERE id='t_<your_task_id>';
       ```
       Plus explanation of WHEN to use it (after circuit-breaker tripped from worker crashes)
    Cross-link block: DOC-A, DOC-B, DOC-D, DOC-F.

    Create `docs/kanban/goal-mode-uat-prompts.md` (DOC-D). Required sections per CONTEXT D-D1:
    1. Lede: "Lessons from Phase 36.3.7.12 UAT thrash" — explain FINDING-03 root cause (unrestricted toolset under filesystem-temptation prompts)
    2. Section "Filesystem-temptation prompts to AVOID": examples and what goes wrong (LLM greps fs, 10-15 min/turn)
    3. Section "Logical-impossibility prompts to PREFER": "summary='banana' AND summary='apple' simultaneously" — forces judge path, no thrash possible
    4. Section "Constrained-output prompts": "reply in exactly 0 tokens" — exhausts budget fast, deterministic
    5. Section "Use --goal-toolset restricted (default)": one paragraph explaining the new default + link to DOC-B `create` verb
    6. Section "4-row reference UAT script": Row 1 = constrained-output haiku (Row 1 retest from 36.3.7.12), Row 2 = logical-impossibility (Row 2 from 36.3.7.12 — now tractable with restricted), Row 3 = judge-error cascade (Row 3), Row 4 = auto_decompose interaction (Row 4). Each row: prompt text, expected event sequence (e.g., kanban_complete on Row 1; budget_exhaustion+kanban_block on Row 2).
    7. **Section "§6 DUAL-RAIL UAT — Restricted-Toolset Smoke"** — copy BOTH Rail A (live) and Rail B (unit-cite) from RESEARCH.md § UAT Scripts §6 (RESEARCH.md lines 669-731). Rail B explicitly cites `cargo test -p ironhermes-cli --test goal_toolset_filter -- restricted_preset_registers_exactly_15_tools extended_preset_registers_approximately_18_tools full_preset_no_tools_removed filter_only_runs_in_goal_worker_mode` (4 of the 6 tests from Plan 03 goal_toolset_filter.rs).

    Cross-link block: DOC-A, DOC-B, DOC-C, DOC-F.
  </action>
  <verify>
    <automated>test -f docs/kanban/profile-discipline.md && test -f docs/kanban/goal-mode-uat-prompts.md && wc -l docs/kanban/profile-discipline.md docs/kanban/goal-mode-uat-prompts.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/kanban/profile-discipline.md` exits 0
    - `wc -l docs/kanban/profile-discipline.md` shows >= 150 lines
    - `grep -c 'HERMES_KANBAN_DB override' docs/kanban/profile-discipline.md` >= 1 (verbatim from §5 UAT script — assertion line)
    - `grep -c 'UPDATE tasks SET status=' docs/kanban/profile-discipline.md` >= 1 (card-reset SQL)
    - `grep -c 'IRONHERMES_WORKER_BIN' docs/kanban/profile-discipline.md` >= 2 (modern-workflow section + §5 UAT script)
    - `grep -c '§5' docs/kanban/profile-discipline.md` >= 1 OR `grep -c 'Cross-Profile DB Resolution' docs/kanban/profile-discipline.md` >= 1 (the §5 section heading)
    - `test -f docs/kanban/goal-mode-uat-prompts.md` exits 0
    - `wc -l docs/kanban/goal-mode-uat-prompts.md` shows >= 150 lines
    - `grep -c 'goal_toolset_filter' docs/kanban/goal-mode-uat-prompts.md` >= 1 (Rail B citation per D-C1 dual-rail)
    - `grep -c '§6\|Restricted-Toolset Smoke' docs/kanban/goal-mode-uat-prompts.md` >= 1
    - `grep -c 'restricted_preset_registers_exactly_15_tools' docs/kanban/goal-mode-uat-prompts.md` >= 1 (specific test name from Plan 03)
    - 4-row UAT script present: `grep -c 'Row 1\|Row 2\|Row 3\|Row 4' docs/kanban/goal-mode-uat-prompts.md` >= 4
  </acceptance_criteria>
  <done>DOC-C + DOC-D shipped with §5 LIVE UAT script verbatim and §6 dual-rail (Rail A live + Rail B unit-cite). Both documents are operator-ready for the phase-close checkpoint.</done>
</task>

<task type="auto">
  <name>Task 3: DOC-E SKILL.md amendment + DOC-F reference.md 4 targeted edits + §7 link-check audit</name>
  <files>skills/kanban-worker/SKILL.md, docs/kanban/reference.md</files>
  <read_first>
    - skills/kanban-worker/SKILL.md (read FULL file — confirm "## Goal mode" section exists at ~line 193 per RESEARCH § DOC-E; identify exact env-var list lines 201-205)
    - docs/kanban/reference.md (read 4 areas: schema table around line 484, env contract table around line 537, config table around line 543, anchor point for the new "Goal mode operational model" section)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-CONTEXT.md (§ Documentation surfaces — DOC-E and DOC-F specs)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-RESEARCH.md (§ DOC-E + § DOC-F — 4 insertion points)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-PATTERNS.md (§ skills/kanban-worker/SKILL.md UPDATE + § docs/kanban/reference.md UPDATE — exact insertion text)
  </read_first>
  <action>
    DOC-E SKILL.md targeted amendment (NOT rewrite per CONTEXT D-D1):
    - Locate the existing `## Goal mode` section (line ~193 per RESEARCH § DOC-E) and the env-vars bullet list within it (lines ~201-205).
    - First, VERIFY the existing two env bullets (HERMES_KANBAN_GOAL_MODE, HERMES_KANBAN_GOAL_MAX_TURNS) are still accurate against `crates/ironhermes-kanban/src/worker_spawn.rs:164,170` (per CONTEXT § DOC-E "verify still accurate"). If wording drifted during Phase 36.3.7.12, fix in passing — otherwise leave alone.
    - APPEND a third bullet for HERMES_KANBAN_GOAL_TOOLSET per PATTERNS.md § skills/kanban-worker/SKILL.md UPDATE anchor — exact text:
      ```
      - `HERMES_KANBAN_GOAL_TOOLSET=<preset>` — toolset preset for this goal-mode
        worker. One of `restricted` (default), `extended`, or `full`. Set by the
        dispatcher at spawn; worker reads via `filter_for_goal_mode_if_applicable`
        at startup. NULL column → dispatcher resolves to `"restricted"` before
        emitting the var.
      ```
    - APPEND a new paragraph below the env list per PATTERNS.md anchor:
      ```
      The inner agent loop is clamped to `kanban.goal_inner_max_iterations` (default 10)
      before the goal loop begins. Operators tune this up for large-goal workspaces or
      down for tight-budget ones via `~/.ironhermes/config.yaml`.
      ```
    - DO NOT touch anything outside the "## Goal mode" section. PRESERVE all other content verbatim.

    DOC-F reference.md 4 targeted amendments per RESEARCH § DOC-F + PATTERNS.md § docs/kanban/reference.md UPDATE:

    Amendment 1 — Schema table v3 row (after the existing v2 columns table around line 484-493):
    Locate the v2 schema migration row in docs/kanban/reference.md. Below it (or as a new row in the columns table for tasks), insert per PATTERNS.md exact text:
    `| `goal_toolset` | TEXT | NULL (`"restricted"`) | Per-card toolset preset for goal-mode workers. One of `restricted`, `extended`, `full`. NULL resolves to `restricted` at dispatch time (D-E1). Non-goal cards: always NULL, ignored. Added in v3 migration (Phase 36.3.7.13). |`

    Amendment 2 — Worker-shell env contract table (after HERMES_KANBAN_GOAL_MAX_TURNS row around line 537):
    `| `HERMES_KANBAN_GOAL_TOOLSET` | `"restricted"\|"extended"\|"full"` | Toolset preset resolved by dispatcher from `goal_toolset` column (NULL → `"restricted"`). Worker reads at startup to filter registry before LLM sees tool schemas. |`

    Amendment 3 — Config table (after kanban.judge_model row around line 543):
    `| `kanban.goal_inner_max_iterations` | Per-worker clamp on `config.agent.max_iterations` during a goal-mode turn. Default 10. Tune up for long-running goals; tune down for budget-constrained setups. |`

    Amendment 4 — NEW "Goal mode operational model" section (insert before `#### Scope boundary` OR after `#### Budget-exhaustion-as-BLOCKED contract`):
    ```
    #### Goal mode operational model

    See companion documents for operational detail:

    - [Profile resolution and cross-profile UAT](../configuration/profiles.md) — DOC-A
    - [CLI verb cheatsheet](cli-reference.md) — DOC-B
    - [Profile discipline runbook](profile-discipline.md) — DOC-C
    - [Goal-mode UAT prompt patterns](goal-mode-uat-prompts.md) — DOC-D

    The v2→v3 schema migration adds `goal_toolset TEXT NULL` to `tasks`. Upgrade is
    automatic on first open (same idempotent ladder as v1→v2). Existing goal-mode
    cards with `NULL` resolve to the `restricted` preset at worker spawn.
    ```

    §7 link-check audit (RESEARCH § Environment Availability assumption A1: markdown-link-check via npx, with grep-fallback if Node absent):
    Run the link audit for all 6 docs:
    ```
    for f in docs/configuration/profiles.md docs/kanban/cli-reference.md docs/kanban/profile-discipline.md docs/kanban/goal-mode-uat-prompts.md docs/kanban/reference.md skills/kanban-worker/SKILL.md; do
      echo "=== $f ==="
      npx --yes markdown-link-check "$f" 2>&1 || grep -oE '\]\([^)]+\.md\)' "$f"
    done
    ```
    If `markdown-link-check` not present, fall back to grep-based: extract every `](*.md)` link and `test -f` each resolved path. The §7 gate is "all 6 docs pass link check OR grep-audit shows zero broken intra-doc links".
  </action>
  <verify>
    <automated>grep -c "HERMES_KANBAN_GOAL_TOOLSET" skills/kanban-worker/SKILL.md && grep -c "goal_toolset" docs/kanban/reference.md && grep -c "Goal mode operational model" docs/kanban/reference.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md: `grep -c 'HERMES_KANBAN_GOAL_TOOLSET' skills/kanban-worker/SKILL.md` >= 1
    - SKILL.md: `grep -c 'goal_inner_max_iterations' skills/kanban-worker/SKILL.md` >= 1 (new paragraph)
    - SKILL.md amendment is SCOPED: `wc -l skills/kanban-worker/SKILL.md` shows delta of ~10-20 lines added (not a wholesale rewrite); compare against initial ~280-line baseline
    - SKILL.md: `grep -c 'HERMES_KANBAN_GOAL_MODE\|HERMES_KANBAN_GOAL_MAX_TURNS' skills/kanban-worker/SKILL.md` >= 2 (existing Phase 36.3.7.12 bullets preserved)
    - reference.md: `grep -c 'goal_toolset' docs/kanban/reference.md` >= 2 (schema table row + env contract row)
    - reference.md: `grep -c 'kanban.goal_inner_max_iterations' docs/kanban/reference.md` >= 1 (config table row)
    - reference.md: `grep -c 'Goal mode operational model' docs/kanban/reference.md` >= 1 (new section heading)
    - reference.md cross-links: `grep -cE 'configuration/profiles.md|cli-reference.md|profile-discipline.md|goal-mode-uat-prompts.md' docs/kanban/reference.md` >= 4 (one link per DOC-A..DOC-D)
    - §7 audit: every `](*.md)` link in the 6 docs resolves to an existing file (grep-audit fallback acceptable if markdown-link-check unavailable)
  </acceptance_criteria>
  <done>DOC-E targeted amendment landed (env bullet + clamp paragraph). DOC-F has all 4 targeted amendments. §7 link-check passes (or grep-fallback confirms cross-link integrity). All 6 doc surfaces complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: §5 LIVE UAT checkpoint — operator runs cross-profile DB resolution script (live-required per D-C1)</name>
  <files>docs/kanban/profile-discipline.md (operator extracts the §5 LIVE UAT script from this DOC-C file)</files>
  <action>
    This is a human-verify checkpoint, not an automated edit. The operator runs the §5 LIVE UAT script defined in DOC-C (docs/kanban/profile-discipline.md) and reports the outcome via the resume-signal. See &lt;what-built&gt;, &lt;how-to-verify&gt;, and &lt;resume-signal&gt; below for the full instructions.
  </action>
  <what-built>
    - Plan 01: open_from_env() + 25 swap sites + 5 cross_profile_db_resolution tests
    - Plan 02: IRONHERMES_WORKER_BIN override + 4 worker_bin_resolution tests
    - Plan 03: goal_toolset column + restricted preset filter + clamp + 13 bilateral tests (4 migration + 3 dispatch + 6 filter)
    - Plan 04 Tasks 1-3: all 6 documentation surfaces shipped
  </what-built>
  <how-to-verify>
    Open `docs/kanban/profile-discipline.md` and run the §5 LIVE UAT script (cross-profile DB resolution). This is the LIVE-REQUIRED gate per CONTEXT D-C1.

    Prerequisites:
    1. Two profiles exist: `dev` and `default` (or whichever your gateway uses). If `dev` doesn't exist, follow DOC-C "Setting up a dev profile from scratch" section first.
    2. `jq` installed (for JSON parsing in the script).
    3. `cargo build -p ironhermes-cli --release` completed (or set `IRONHERMES_WORKER_BIN=$(pwd)/target/debug/ironhermes`).

    Run the script verbatim from `docs/kanban/profile-discipline.md` § "§5 LIVE UAT — Cross-Profile DB Resolution":
    ```bash
    bash docs/kanban/profile-discipline.md  # NO — extract the script block; profile-discipline.md is not directly executable
    ```
    Instead, copy the fenced bash block from DOC-C's §5 section into a temp script and run it.

    Expected outcomes:
    - The dispatch run logs `INFO kanban.db: kanban store: using HERMES_KANBAN_DB override path="..."` AT LEAST ONCE (`grep -c 'HERMES_KANBAN_DB override' /tmp/dispatch-uatlog.txt` >= 1)
    - The card transitions to `status: done` within ~120s as observed from the SOURCE profile (`ironhermes --profile dev kanban show "$TASK_ID" --json | jq -r '.status'` returns `"done"`)
    - If the card hits `status: blocked` instead of `done`, examine the block event payload for the reason — this is acceptable as long as the worker DID read profile A's DB (the HERMES_KANBAN_DB override log line is the canonical gate per D-C1; status=done is the secondary gate).

    PASS criteria (one of):
    - PASS-CLEAN: log grep == "HERMES_KANBAN_DB override" found AND final status == "done"
    - PASS-WITH-NOTES: log grep == "HERMES_KANBAN_DB override" found AND final status == "blocked" (worker read correct DB but acceptance criteria failed for unrelated LLM reasons) — describe block reason in resume signal

    FAIL criteria:
    - log grep fails (HERMES_KANBAN_DB override NOT in worker log) → worker did not read env override; F-01 not closed in this commit set; investigate which swap site is missing
    - worker errored on store open (path resolution broken) → file an issue against Plan 01 commit
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved §5 PASS-CLEAN — log shows HERMES_KANBAN_DB override + status=done`
    - `approved §5 PASS-WITH-NOTES — log shows HERMES_KANBAN_DB override, status=blocked with reason: <describe>`
    - `failed §5 — <describe failure>`
  </resume-signal>
  <verify>
    <human-check>Operator runs the §5 LIVE UAT script from docs/kanban/profile-discipline.md, captures /tmp/dispatch-uatlog.txt, runs `grep -c 'HERMES_KANBAN_DB override' /tmp/dispatch-uatlog.txt` (expect >= 1), and runs `ironhermes --profile dev kanban show "$TASK_ID" --json | jq -r '.status'` (expect "done" for PASS-CLEAN or "blocked" with documented reason for PASS-WITH-NOTES). Reply via resume-signal.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator captures `/tmp/dispatch-uatlog.txt` (or equivalent) and runs `grep -c 'HERMES_KANBAN_DB override' /tmp/dispatch-uatlog.txt` — result >= 1 for PASS
    - Operator captures `ironhermes --profile dev kanban show "$TASK_ID" --json | jq -r '.status'` — result == "done" for PASS-CLEAN OR "blocked" with documented reason for PASS-WITH-NOTES
    - Resume signal explicitly states PASS-CLEAN, PASS-WITH-NOTES, or FAIL with description
  </acceptance_criteria>
  <done>Operator confirms §5 LIVE UAT PASS (clean or with-notes per D-C1). Live evidence captured in resume signal. §5 gate locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc → operator | Documentation describes a security-positive feature (restricted preset removes terminal/delegate_task from goal-mode LLM) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.13-04-I | Information Disclosure | Docs document HERMES_KANBAN_DB + IRONHERMES_WORKER_BIN env-var contract | accept | These env vars are operator-controlled and intentionally part of the public CLI contract. Severity LOW — not secrets. |
| T-36.3.7.13-04-T | Tampering | Operator runs §5 LIVE UAT script | accept | Script runs locally with operator's profiles, no network surface beyond LLM API the operator already controls. The script's `jq -r '.task_id'` extraction is operator-bounded input. Severity LOW. |
</threat_model>

<verification>
- All 4 NEW files exist and meet min_lines targets
- DOC-E SKILL.md amendment is SCOPED (10-20 line delta, not rewrite)
- DOC-F reference.md has all 4 targeted amendments (Schema row, env contract row, config row, new section)
- §7 link-check audit: every `](*.md)` link in the 6 docs resolves
- §5 LIVE UAT PASS-CLEAN or PASS-WITH-NOTES per D-C1 (operator-attested via checkpoint resume signal)
- §6 dual-rail satisfied via DOC-D citing Plan 03's goal_toolset_filter.rs tests (Rail B unit-cite is shipped; Rail A live-run is optional operator activity per D-C1)
</verification>

<success_criteria>
- D-D1 materially observable: all 6 doc surfaces exist (DOC-A NEW, DOC-B NEW, DOC-C NEW, DOC-D NEW, DOC-E UPDATE, DOC-F UPDATE)
- D-C1 materially observable: §5 LIVE UAT script lives in DOC-C; §6 dual-rail (live + unit-cite) lives in DOC-D
- All cross-links resolve (§7 gate)
- F-04 closed: profile-context documentation gap from Phase 36.3.7.12 UAT is filled
- Phase 36.3.7.13 ready for `/gsd-verify-work` after this plan ships
</success_criteria>

<output>
Create `.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-04-SUMMARY.md` when done. Include: file listing with line counts for all 6 docs; SKILL.md delta line count (confirm targeted amendment, not rewrite); reference.md 4-amendment audit (grep results for each amendment site); §7 link-check pass/fail per doc; §5 LIVE UAT operator-attested PASS/PASS-WITH-NOTES verbatim from resume signal; cross-reference to RESEARCH.md § UAT Scripts §5+§6 source.
</output>
