---
phase: 36.3.7.13
plan: 03
type: execute
wave: 2
depends_on: ["36.3.7.13-01"]
files_modified:
  - crates/ironhermes-kanban/src/schema.rs
  - crates/ironhermes-kanban/src/store.rs
  - crates/ironhermes-kanban/src/types.rs
  - crates/ironhermes-kanban/src/config.rs
  - crates/ironhermes-kanban/src/worker_spawn.rs
  - crates/ironhermes-kanban/src/tools/create.rs
  - crates/ironhermes-tools/src/registry.rs
  - crates/ironhermes-cli/src/main.rs
  - crates/ironhermes-cli/src/kanban/mod.rs
  - crates/ironhermes-cli/src/kanban/commands.rs
  - crates/ironhermes-kanban/tests/goal_toolset_migration.rs
  - crates/ironhermes-cli/tests/goal_toolset_filter.rs
  - crates/ironhermes-cli/tests/goal_toolset_dispatch.rs
autonomous: true
requirements: [D-B1, D-B2, D-B3, D-E1, D-E2, D-F1, D-G1, Schema-v3]
must_haves:
  truths:
    - "tasks table at schema v3 has a goal_toolset TEXT NULL column"
    - "v2→v3 migration ladder is idempotent (re-run safe)"
    - "Task struct gains goal_toolset: Option<String>; all 6 row_to_task SELECTs include the column"
    - "KanbanConfig gains goal_inner_max_iterations: u32 default 10, with serde-default helper"
    - "worker_spawn emits HERMES_KANBAN_GOAL_TOOLSET=<resolved> when goal_mode=true; NULL coerces to 'restricted'"
    - "ToolRegistry exposes retain_by_name(allowed: &[&str]) sibling to unregister_by_prefix"
    - "filter_for_goal_mode_if_applicable runs after register_kanban_tools_if_applicable at BOTH main.rs:873 and :1629 call sites"
    - "Restricted preset registers exactly 15 tools (13 kanban_* + file_read + file_write); LLM never sees terminal/delegate_task schemas"
    - "Inner agent loop clamped to min(config.agent.max_iterations, kanban_cfg.goal_inner_max_iterations) when worker_mode && goal_mode"
    - "CLI flag --goal-toolset uses Option<String> + requires=\"goal\" (per RESEARCH Risk 1 — no default_value to avoid clap silent-pass)"
    - "KanbanCreateTool JSON schema gains optional goal_toolset enum field"
  artifacts:
    - path: "crates/ironhermes-kanban/src/schema.rs"
      provides: "SCHEMA_VERSION=3 + v2→v3 migration block adding goal_toolset TEXT"
      contains: "SCHEMA_VERSION: i64 = 3"
    - path: "crates/ironhermes-kanban/src/types.rs"
      provides: "Task.goal_toolset: Option<String> field"
      contains: "goal_toolset: Option<String>"
    - path: "crates/ironhermes-kanban/src/config.rs"
      provides: "KanbanConfig.goal_inner_max_iterations: u32"
      contains: "goal_inner_max_iterations"
    - path: "crates/ironhermes-tools/src/registry.rs"
      provides: "pub fn retain_by_name(&mut self, allowed: &[&str])"
      contains: "retain_by_name"
    - path: "crates/ironhermes-cli/src/main.rs"
      provides: "filter_for_goal_mode_if_applicable + inner-loop clamp wiring at 2 call sites"
      contains: "filter_for_goal_mode_if_applicable"
    - path: "crates/ironhermes-kanban/tests/goal_toolset_migration.rs"
      provides: "4 bilateral tests covering Schema v3"
      contains: "goal_toolset"
    - path: "crates/ironhermes-cli/tests/goal_toolset_filter.rs"
      provides: "6 bilateral tests covering D-B1/D-B2/D-F1 clamp"
      contains: "restricted"
    - path: "crates/ironhermes-cli/tests/goal_toolset_dispatch.rs"
      provides: "3-4 bilateral tests covering D-B3 clap surface + D-G1 JSON schema"
      contains: "goal_toolset"
  key_links:
    - from: "crates/ironhermes-kanban/src/worker_spawn.rs (goal_mode block)"
      to: "HERMES_KANBAN_GOAL_TOOLSET env var emission"
      via: "task.goal_toolset.as_deref().unwrap_or(\"restricted\")"
      pattern: "HERMES_KANBAN_GOAL_TOOLSET"
    - from: "crates/ironhermes-cli/src/main.rs (worker entry, two call sites :873 and :1629)"
      to: "filter_for_goal_mode_if_applicable + clamp"
      via: "called right after register_kanban_tools_if_applicable"
      pattern: "filter_for_goal_mode_if_applicable"
    - from: "crates/ironhermes-tools/src/registry.rs"
      to: "retain_by_name"
      via: "self.tools.retain(...)"
      pattern: "pub fn retain_by_name"
---

<objective>
Close F-03 (goal-mode toolset thrash) on both axes:

1. **Toolset axis (D-B1/D-B2/D-E1/D-E2/D-G1)** — Add `goal_toolset TEXT NULL` column via schema v2→v3 migration; extend Task struct + 6 row_to_task SELECTs; emit `HERMES_KANBAN_GOAL_TOOLSET=<resolved>` env at spawn (NULL→"restricted" at dispatcher side); add `ToolRegistry::retain_by_name` method; add `filter_for_goal_mode_if_applicable` worker-startup pass calling retain_by_name; ship CLI `--goal-toolset {restricted|extended|full}` flag (Option<String> + requires="goal" per RESEARCH Risk 1); extend KanbanCreateTool JSON schema.

2. **Inner-loop axis (D-F1)** — Add `kanban.goal_inner_max_iterations: u32` config (default 10); clamp `config.agent.max_iterations` at worker-mode entry when goal_mode is active.

Purpose: Phase 36.3.7.12 UAT Rows 2-4 thrashed at 10-15 min per turn because goal-mode workers spawned delegate_task subagents and ran filesystem-tempted searches under unrestricted toolsets. This plan eliminates both pathologies: (a) LLM literally never sees terminal/delegate_task schemas under `restricted` (default for goal_mode), (b) inner agent loop bounded to 10 iterations max.

Output: ~12 files modified + 3 new test files (4+6+3 = 13 bilateral tests) + extended inline tests in worker_spawn.rs/config.rs/tools/create.rs. **BLOCKING dependency on Plan 01**: per RESEARCH Risk 6, the store-Arc creation site at main.rs:307 (inside register_kanban_tools_if_applicable) MUST use open_from_env() before this plan's filter pass lands — otherwise decompose/specify (constructor-injected store) would use stale DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-CONTEXT.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-RESEARCH.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-PATTERNS.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-VALIDATION.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-01-SUMMARY.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Key signatures the executor needs. Embedded so executor doesn't hunt. -->

From crates/ironhermes-kanban/src/schema.rs:13-16:
  /// Current schema version. Increment when run_migrations gains a new step.
  pub const SCHEMA_VERSION: i64 = 2;  // → bump to 3

From crates/ironhermes-kanban/src/schema.rs:190-213 (v1→v2 ladder pattern — template for v2→v3):
  pub fn run_migrations(conn: &mut Connection, current: i64) -> Result<()> {
      if current < 2 {
          let _ = conn.execute("ALTER TABLE tasks ADD COLUMN goal_mode INTEGER NOT NULL DEFAULT 0", []);
          // ... more goal_* columns ...
          conn.execute("UPDATE schema_version SET version = 2", [])?;
      }
      // APPEND v2→v3 block here
      Ok(())
  }

From crates/ironhermes-kanban/src/types.rs:68-88 (goal-mode block to extend):
  #[serde(default)] pub goal_mode: bool,
  #[serde(default)] pub goal_max_turns: u32,
  #[serde(default)] pub goal_turns_used: u32,
  // APPEND: #[serde(default)] pub goal_toolset: Option<String>,

From crates/ironhermes-kanban/src/store.rs row_to_task (line 360) — 6 SELECT sites at:
  lines 360, 524, 542, 906, 1873, 1915, 2072  (per RESEARCH Implementation Approach §6)
  EACH SELECT column list gains `, goal_toolset` after `goal_turns_used`
  row_to_task body: let goal_toolset: Option<String> = row.get(26)?;  // column index 26

From crates/ironhermes-kanban/src/config.rs:95-130 (judge_model template):
  /// Aux model id for the goal-mode judge LLM (Phase 36.3.7.12 D-05).
  #[serde(default)] pub judge_model: String,
  // APPEND:
  /// Inner agent loop iteration ceiling for goal-mode workers (Phase 36.3.7.13 D-F1).
  #[serde(default = "default_goal_inner_max_iterations")] pub goal_inner_max_iterations: u32,

  fn default_goal_inner_max_iterations() -> u32 { 10 }
  // Default impl line: goal_inner_max_iterations: default_goal_inner_max_iterations(),

From crates/ironhermes-kanban/src/worker_spawn.rs:163-171 (HERMES_KANBAN_GOAL_MODE emission to extend):
  if task.goal_mode {
      env.push(("HERMES_KANBAN_GOAL_MODE".into(), "1".into()));
      let budget = if task.goal_max_turns == 0 { 20 } else { task.goal_max_turns };
      env.push(("HERMES_KANBAN_GOAL_MAX_TURNS".into(), budget.to_string()));
      // APPEND (Phase 36.3.7.13 D-E2):
      let preset = task.goal_toolset.as_deref().unwrap_or("restricted");
      env.push(("HERMES_KANBAN_GOAL_TOOLSET".into(), preset.to_string()));
  }

From crates/ironhermes-tools/src/registry.rs:272-277 (unregister_by_prefix template):
  pub fn unregister_by_prefix(&mut self, server_name: &str) -> usize {
      let prefix = format!("{server_name}__");
      let before = self.tools.len();
      self.tools.retain(|name, _| !name.starts_with(&prefix));
      before - self.tools.len()
  }

NEW method (PATTERNS.md anchor):
  /// Phase 36.3.7.13 D-B2: remove all tools NOT in `allowed`.
  pub fn retain_by_name(&mut self, allowed: &[&str]) -> usize {
      let before = self.tools.len();
      self.tools.retain(|name, _| allowed.contains(&name.as_str()));
      before - self.tools.len()
  }

From crates/ironhermes-cli/src/main.rs:299-324 (register_kanban_tools_if_applicable signature):
  async fn register_kanban_tools_if_applicable(
      registry: &Arc<RwLock<ToolRegistry>>,
      toolsets: &std::collections::HashSet<String>,
  ) -> anyhow::Result<()>

NEW sibling fn (PATTERNS.md anchor lines 515-575) — `filter_for_goal_mode_if_applicable(registry: &Arc<RwLock<ToolRegistry>>) -> anyhow::Result<()>`:
  - reads HERMES_KANBAN_TASK + HERMES_KANBAN_GOAL_MODE (no-op if not worker_mode + goal_mode)
  - reads HERMES_KANBAN_GOAL_TOOLSET (defaults "restricted")
  - For "full" preset: no-op
  - For "extended": retain_by_name(EXTENDED) — 18 tools
  - For "restricted" (or anything unrecognized): retain_by_name(RESTRICTED) — 15 tools
  - Logs `target: "kanban.goal_mode"` with preset + removed count

RESTRICTED preset (15 tools, per CONTEXT D-B1):
  ["kanban_show", "kanban_list", "kanban_comment", "kanban_complete",
   "kanban_block", "kanban_create", "kanban_heartbeat", "kanban_link",
   "kanban_unblock", "kanban_swarm", "kanban_mention", "kanban_decompose",
   "kanban_specify",      // 13 kanban_*
   "file_read", "file_write"]  // +2 file tools — RESEARCH Risk 3 cautions: verify exact registry names

EXTENDED preset (18 tools):
  RESTRICTED + ["memory_tool", "memory_manager_handle", "skills_tool"]

NOTE on tool names (RESEARCH Risk 3): file_read/file_write may register as "read_file"/"write_file" instead. The executor MUST verify exact name strings by running `grep -rE "fn name\(.*\) -> .*\"file_read\"|\"read_file\"" crates/ironhermes-tools/src/` BEFORE encoding the constants. Test 1 of goal_toolset_filter.rs asserts exact count == 15 — naming drift surfaces immediately.

Call sites in main.rs where filter_for_goal_mode_if_applicable + clamp insert (per CONTEXT.md):
  main.rs:873   right after `register_kanban_tools_if_applicable(...).await?;`
  main.rs:1629  right after `register_kanban_tools_if_applicable(...).await?;`

CLI clap surface (per RESEARCH Risk 1 — Option<String>, NO default_value):
  #[arg(long, requires = "goal",
        value_parser = ["restricted", "extended", "full"])]
  pub goal_toolset: Option<String>,

  // Handler resolution: let preset = args.goal_toolset.clone().unwrap_or_else(|| "restricted".to_string());

KanbanCreateTool JSON schema extension (tools/create.rs lines 161-170, after goal_max_turns):
  "goal_toolset": {
      "type": "string",
      "enum": ["restricted", "extended", "full"],
      "description": "Toolset preset for goal-mode workers (Phase 36.3.7.13 D-B1). ..."
  }
  // NO "default" key — NULL semantics owned by dispatcher per D-E1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema v2→v3 migration + Task struct + 6 row_to_task SELECTs + migration tests</name>
  <files>crates/ironhermes-kanban/src/schema.rs, crates/ironhermes-kanban/src/types.rs, crates/ironhermes-kanban/src/store.rs, crates/ironhermes-kanban/tests/goal_toolset_migration.rs</files>
  <read_first>
    - crates/ironhermes-kanban/src/schema.rs lines 1-220 (SCHEMA_VERSION constant + DDL + run_migrations)
    - crates/ironhermes-kanban/src/types.rs lines 60-90 (Task struct goal_* block)
    - crates/ironhermes-kanban/src/store.rs lines 355-400 (row_to_task fn)
    - crates/ironhermes-kanban/src/store.rs lines 510-560 (representative SELECT)
    - crates/ironhermes-kanban/tests/goal_mode_migration.rs (analog file — full scaffold to mirror)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-PATTERNS.md (§ schema.rs + § types.rs + § store.rs row_to_task — exact code blocks)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-VALIDATION.md (lines 66-69 — 4 migration test rows)
  </read_first>
  <behavior>
    - Test 1 (Schema v3 fresh): A new DB opened by KanbanStore::new() reports SCHEMA_VERSION=3 and PRAGMA table_info(tasks) includes goal_toolset
    - Test 2 (v2→v3 upgrade): Seed a hand-crafted v2 DB (Connection::open + manual schema_version=2 + tasks rows without goal_toolset), close, reopen via KanbanStore::new() — assert version becomes 3, existing rows preserved, goal_toolset column NULL on prior rows
    - Test 3 (idempotent re-run): Open at v3, close, reopen — run_migrations does not error; version stays 3
    - Test 4 (round-trip via row_to_task): Insert a task with goal_toolset = Some("extended"), retrieve via get_task, assert returned Task.goal_toolset == Some("extended")
  </behavior>
  <action>
    Four-file producer + 4-test consumer in same commit (bilateral discipline per LEARNINGS 2026-05-30):

    schema.rs:
    - Line 13-16: bump `pub const SCHEMA_VERSION: i64 = 2` → `= 3`. Update the doc comment to add the v3 row per PATTERNS.md.
    - DDL `SCHEMA_SQL` const (locate via grep `CREATE TABLE.*tasks`): add `goal_toolset TEXT,` line after `goal_turns_used` column.
    - run_migrations: APPEND a `if current < 3 {` block after the existing `if current < 2 {` block, using the exact `let _ = conn.execute("ALTER TABLE tasks ADD COLUMN goal_toolset TEXT", [])` idempotency pattern from v1→v2 (per PATTERNS.md schema.rs anchor). Then `conn.execute("UPDATE schema_version SET version = 3", [])?;`

    types.rs:
    - Lines 68-88 goal-mode block: APPEND `#[serde(default)] pub goal_toolset: Option<String>,` after `goal_turns_used`. Doc comment per PATTERNS.md anchor cites Phase 36.3.7.13 D-B1 and D-E1.

    store.rs (6 SELECT sites + row_to_task — verified line numbers per RESEARCH Implementation Approach §6):
    - Sites: lines 360 (row_to_task body), 524, 542, 906, 1873, 1915, 2072 (the SELECT column lists)
    - Each SELECT column list: append `, goal_toolset` after `goal_turns_used` (keep relative ordering of all columns — goal_toolset is column index 26 in the SELECT result; row.get index reflects this)
    - row_to_task fn body: add `let goal_toolset: Option<String> = row.get(26)?;` after the `goal_turns_used_i: i64 = row.get(25)?;` line; add `goal_toolset,` in the Task struct literal.
    - INSERT statements that write tasks: locate via grep `INSERT INTO tasks` — extend column list + params binding to include goal_toolset where the existing INSERTs already include goal_mode/goal_max_turns/goal_turns_used (avoid F-01 sibling-miss pattern from 36.3.7.12-01 per CONTEXT note).

    Create NEW file `crates/ironhermes-kanban/tests/goal_toolset_migration.rs` with the 4 tests above. ENV_LOCK not needed (this test only opens tempdir-scoped DBs, no env mutation). Use `Connection::open` for the v2-DB seed step (Test 2) — same scaffold as `goal_mode_migration.rs`.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-kanban --test goal_toolset_migration -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub const SCHEMA_VERSION: i64 = 3' crates/ironhermes-kanban/src/schema.rs` == 1 (and == 0 occurrences of `= 2` on that constant line)
    - `grep -c 'if current < 3' crates/ironhermes-kanban/src/schema.rs` == 1 (new migration arm)
    - `grep -c 'ALTER TABLE tasks ADD COLUMN goal_toolset' crates/ironhermes-kanban/src/schema.rs` == 1
    - `grep -c 'goal_toolset: Option<String>' crates/ironhermes-kanban/src/types.rs` == 1
    - `grep -cE 'goal_turns_used,?\s+goal_toolset' crates/ironhermes-kanban/src/store.rs` >= 6 (the 6 SELECT column lists + the row_to_task struct literal — confirm via raw count >= 6, comments stripped)
    - `grep -c 'row.get(26)' crates/ironhermes-kanban/src/store.rs` >= 1 (row_to_task)
    - `cargo test -p ironhermes-kanban --test goal_toolset_migration -- --test-threads=1` reports `4 passed; 0 failed`
    - `cargo test -p ironhermes-kanban --no-fail-fast` reports >= 124 passed (120 baseline + 4 new; carry Plan 01/02 deltas separately); 0 NEW failures on the workspace gate
    - `cargo build -p ironhermes-kanban` exit 0
  </acceptance_criteria>
  <done>Schema bumped to v3 with idempotent ladder. Task struct + 6 SELECTs + row_to_task carry goal_toolset. 4 migration tests pass. No regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KanbanConfig field + worker_spawn env emission + KanbanCreateTool JSON schema + CLI clap surface + inline tests</name>
  <files>crates/ironhermes-kanban/src/config.rs, crates/ironhermes-kanban/src/worker_spawn.rs, crates/ironhermes-kanban/src/tools/create.rs, crates/ironhermes-cli/src/kanban/mod.rs, crates/ironhermes-cli/src/kanban/commands.rs, crates/ironhermes-cli/tests/goal_toolset_dispatch.rs</files>
  <read_first>
    - crates/ironhermes-kanban/src/config.rs lines 1-150 (full file — find judge_model field + Default impl + serde-default helpers)
    - crates/ironhermes-kanban/src/worker_spawn.rs lines 160-180 (HERMES_KANBAN_GOAL_MODE emission block to extend)
    - crates/ironhermes-kanban/src/tools/create.rs lines 150-180 (JSON schema goal_max_turns block to extend)
    - crates/ironhermes-cli/src/kanban/mod.rs (clap definition for KanbanCommands::Create — locate `--goal` flag and add sibling `--goal-toolset`)
    - crates/ironhermes-cli/src/kanban/commands.rs (cmd_create handler — locate where args.goal is read; add goal_toolset propagation)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-PATTERNS.md (§ config.rs + § worker_spawn.rs HERMES_KANBAN_GOAL_TOOLSET + § tools/create.rs + § kanban/mod.rs + commands.rs)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-RESEARCH.md (§ Risks Risk 1 — CRITICAL: Option<String>, no default_value)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-VALIDATION.md (lines 63-72 — D-E1/D-E2/D-F1/D-B3/D-G1 test rows)
  </read_first>
  <behavior>
    - Inline config.rs test: KanbanConfig::default().goal_inner_max_iterations == 10
    - Inline config.rs test: serde_yaml::from_str("{}") roundtrip resolves goal_inner_max_iterations to 10 (serde-default helper fires)
    - Inline worker_spawn.rs test (extend existing): build_kanban_worker_env with task.goal_mode=true + task.goal_toolset=None emits HERMES_KANBAN_GOAL_TOOLSET="restricted" (D-E1 NULL-coercion)
    - Inline worker_spawn.rs test (extend existing): task.goal_toolset=Some("extended") emits HERMES_KANBAN_GOAL_TOOLSET="extended" verbatim
    - Inline worker_spawn.rs test (extend existing): task.goal_mode=false does NOT emit HERMES_KANBAN_GOAL_TOOLSET (no key in env vec)
    - Inline tools/create.rs test (extend existing JSON schema assertion): KanbanCreateTool::schema()["parameters"]["properties"]["goal_toolset"] exists and ["enum"] == ["restricted","extended","full"]
    - NEW goal_toolset_dispatch.rs test 1 (D-B3 negative): `kanban create --goal-toolset restricted "title"` (without `--goal`) FAILS clap parse
    - NEW goal_toolset_dispatch.rs test 2 (D-B3 positive): `kanban create --goal --goal-toolset extended "title"` parses successfully; args.goal_toolset == Some("extended"); args.goal == true
    - NEW goal_toolset_dispatch.rs test 3 (D-B3 default-via-handler): `kanban create --goal "title"` parses; args.goal_toolset == None; handler resolves to "restricted"
  </behavior>
  <action>
    config.rs:
    - Insert `goal_inner_max_iterations: u32` field after `judge_model` (line ~107) with `#[serde(default = "default_goal_inner_max_iterations")]`. Doc comment per PATTERNS.md config.rs anchor.
    - Add free fn `fn default_goal_inner_max_iterations() -> u32 { 10 }` near the other default_* helpers.
    - Extend `impl Default for KanbanConfig { fn default() { Self { ..., goal_inner_max_iterations: default_goal_inner_max_iterations() } } }` — add the field initializer.
    - Extend the existing `default_matches_context_md` inline test (or whichever inline test verifies Default values) to assert `KanbanConfig::default().goal_inner_max_iterations == 10`.
    - Add inline test for YAML roundtrip with empty map (verifies serde-default helper).

    worker_spawn.rs (lines 163-171 area):
    - Inside the existing `if task.goal_mode { ... }` block, AFTER the HERMES_KANBAN_GOAL_MAX_TURNS push, append the HERMES_KANBAN_GOAL_TOOLSET emission per PATTERNS.md § HERMES_KANBAN_GOAL_TOOLSET emission anchor: `let preset = task.goal_toolset.as_deref().unwrap_or("restricted"); env.push(("HERMES_KANBAN_GOAL_TOOLSET".into(), preset.to_string()));`
    - Extend existing inline tests with 3 new cases: NULL→"restricted" coercion (D-E1); Some("extended") passthrough; goal_mode=false skip-emission.

    tools/create.rs (lines 161-170):
    - APPEND the `goal_toolset` property to the JSON schema after `goal_max_turns` per PATTERNS.md § tools/create.rs anchor. NO `"default"` key. Use enum `["restricted", "extended", "full"]`.
    - Extend whichever inline test exercises the schema (locate via grep `goal_max_turns` in tools/create.rs) to assert the new field.
    - Also propagate `goal_toolset` from tool args → CreateTaskOptions → store.create_task: the tool already plumbs goal_mode/goal_max_turns through this path; mirror that wiring for goal_toolset (per D-G1).

    kanban/mod.rs (CLI clap):
    - Find the `KanbanCreate` struct or `Create { ... }` enum variant with the `--goal` and `--goal-max-turns` fields. Add `goal_toolset: Option<String>` field with `#[arg(long, requires = "goal", value_parser = ["restricted", "extended", "full"])]` — per RESEARCH Risk 1: NO `default_value`, NO `default_value_t`. Resolve default in handler.

    kanban/commands.rs (handler):
    - In `cmd_create` (or wherever `--goal` is processed), add `let goal_toolset = if args.goal { Some(args.goal_toolset.clone().unwrap_or_else(|| "restricted".to_string())) } else { None };` per PATTERNS.md § commands.rs anchor. Pass through to CreateTaskOptions (matching the field added to types.rs in Task 1).

    Create NEW file `crates/ironhermes-cli/tests/goal_toolset_dispatch.rs` with the 3 clap parse tests above. Use `clap::Parser::try_parse_from(args)` against the KanbanCreate type — see test patterns in existing CLI tests like `crates/ironhermes-cli/tests/heartbeat_cli.rs` for the parser-shape harness (per STATE.md Phase 36.3.7.6 close-out note: this analog dropped `Debug` derives to avoid KanbanCommands trait issue — mirror that workaround if encountered).
  </action>
  <verify>
    <automated>cargo test -p ironhermes-kanban --no-fail-fast 2>&1 | tail -15 && cargo test -p ironhermes-cli --test goal_toolset_dispatch -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'goal_inner_max_iterations' crates/ironhermes-kanban/src/config.rs` >= 3 (field decl + default fn + Default impl)
    - `grep -c 'default_goal_inner_max_iterations' crates/ironhermes-kanban/src/config.rs` >= 2 (fn + serde attribute)
    - `grep -c 'HERMES_KANBAN_GOAL_TOOLSET' crates/ironhermes-kanban/src/worker_spawn.rs` >= 2 (emission site + extended inline test)
    - `grep -c '"goal_toolset"' crates/ironhermes-kanban/src/tools/create.rs` >= 1 (JSON schema property name as string literal)
    - `grep -cE 'requires\s*=\s*"goal"' crates/ironhermes-cli/src/kanban/mod.rs` >= 1 (clap requires attr)
    - `grep -c 'default_value' crates/ironhermes-cli/src/kanban/mod.rs` near the `goal_toolset` field = 0 (per RESEARCH Risk 1 — must not use default_value here; spot-check by reading the relevant block — naked grep can false-positive on other fields)
    - `grep -c 'goal_toolset: Option<String>' crates/ironhermes-cli/src/kanban/mod.rs` >= 1
    - `cargo test -p ironhermes-cli --test goal_toolset_dispatch -- --test-threads=1` reports 3 (or 4) passed
    - `cargo test -p ironhermes-kanban --no-fail-fast` reports >= 128 passed (124 from Task 1 + 4 extended-inline deltas roughly); 0 NEW failures
    - `cargo build -p ironhermes-cli -p ironhermes-kanban` exit 0
  </acceptance_criteria>
  <done>Config knob shipped with default 10. worker_spawn emits HERMES_KANBAN_GOAL_TOOLSET with NULL-coercion. KanbanCreateTool schema gains optional enum. CLI flag is Option<String>+requires="goal" (no default_value). 3 dispatch tests pass; extended inline tests in worker_spawn.rs + config.rs + tools/create.rs all pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ToolRegistry::retain_by_name + filter_for_goal_mode_if_applicable + inner-loop clamp + filter tests</name>
  <files>crates/ironhermes-tools/src/registry.rs, crates/ironhermes-cli/src/main.rs, crates/ironhermes-cli/tests/goal_toolset_filter.rs</files>
  <read_first>
    - crates/ironhermes-tools/src/registry.rs lines 265-285 (unregister_by_prefix — template for retain_by_name)
    - crates/ironhermes-tools/src/registry.rs (top — find existing inline tests near unregister_by_prefix; mirror the test scaffold)
    - crates/ironhermes-cli/src/main.rs lines 295-330 (register_kanban_tools_if_applicable — Task 1/Plan-01 will have swapped line 307 to open_from_env(); confirm before adding sibling fn)
    - crates/ironhermes-cli/src/main.rs lines 870-885 (FIRST register_kanban_tools_if_applicable call site)
    - crates/ironhermes-cli/src/main.rs lines 1625-1640 (SECOND register_kanban_tools_if_applicable call site)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-PATTERNS.md (§ ironhermes-tools/src/registry.rs retain_by_name + § ironhermes-cli/src/main.rs filter_for_goal_mode_if_applicable + § Inner loop clamp — three exact code blocks)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-RESEARCH.md (§ Risks Risk 3 — file_read/write naming + Risk 4 — retain_by_name addition justification)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-VALIDATION.md (lines 56-60 — 6 filter test rows)
    - Verify exact registry tool name strings: `grep -rE 'fn name\(.*->.*"(read_file|write_file|file_read|file_write|terminal|delegate_task|memory_tool|memory_manager_handle|skills_tool)' crates/ironhermes-tools/src/ crates/ironhermes-cli/src/` (per RESEARCH Risk 3)
  </read_first>
  <behavior>
    - retain_by_name inline test (registry.rs): register N tools, call retain_by_name(&["a","b"]), assert only "a" and "b" remain
    - retain_by_name inline test: returns count of REMOVED tools (mirror unregister_by_prefix return contract)
    - goal_toolset_filter.rs Test 1 (D-B1/D-B2): restricted preset removes terminal + delegate_task + web_fetch + any non-allowed tool; final tool count == 15 exactly
    - Test 2 (D-B1): extended preset registers ~18 tools (kanban_*=13 + file_read+file_write=2 + memory_tool+memory_manager_handle+skills_tool=3). Exact count assertion — may need to be ==18 or stated range.
    - Test 3 (D-B1): full preset registers ALL default tools + kanban_* (no filtering — retain_by_name not called when preset=="full")
    - Test 4 (D-B2): filter no-ops when HERMES_KANBAN_GOAL_MODE is unset (worker_mode but not goal_mode)
    - Test 5 (D-B2): filter no-ops when HERMES_KANBAN_TASK is unset (not worker_mode)
    - Test 6 (D-F1): when HERMES_KANBAN_GOAL_MODE=1, config.agent.max_iterations is clamped to min(original, kanban_cfg.goal_inner_max_iterations=10). Drive via constructing a config with max_iterations=50, run the clamp logic, assert == 10.
  </behavior>
  <action>
    registry.rs:
    - Insert `pub fn retain_by_name(&mut self, allowed: &[&str]) -> usize` immediately after `unregister_by_prefix` per PATTERNS.md anchor. Body: `let before = self.tools.len(); self.tools.retain(|name, _| allowed.contains(&name.as_str())); before - self.tools.len()`. Doc comment cites Phase 36.3.7.13 D-B2.
    - Add 2 inline tests near the existing unregister_by_prefix tests (find via grep `#\[test\]` in registry.rs): test_retain_by_name_removes_non_allowed + test_retain_by_name_returns_removed_count.

    main.rs:
    - VERIFY first: Plan 01 has landed the open_from_env() swap at line 307 (per its depends_on entry). If not, this plan is blocked.
    - Insert new async fn `filter_for_goal_mode_if_applicable(registry: &Arc<RwLock<ToolRegistry>>) -> anyhow::Result<()>` immediately after `register_kanban_tools_if_applicable`. Body per PATTERNS.md § filter_for_goal_mode_if_applicable anchor (lines 515-575).
    - The RESTRICTED and EXTENDED constants MUST use the verified tool name strings from the pre-task grep step (per RESEARCH Risk 3). If `read_file`/`write_file` vs `file_read`/`file_write` differs from PATTERNS.md, use what the registry actually registers. Test 1's exact-count assertion (==15) will surface drift.
    - At BOTH call sites (lines 873 and 1629), append immediately after `register_kanban_tools_if_applicable(...).await?;`:
      `filter_for_goal_mode_if_applicable(runtime.registry()).await?;`
      And then, also immediately after the filter, append the inner-loop clamp block per PATTERNS.md § Inner loop clamp anchor:
      `if std::env::var("HERMES_KANBAN_GOAL_MODE").map(|v| v == "1").unwrap_or(false) { if let Ok(kanban_cfg) = ironhermes_kanban::config::KanbanConfig::load_or_default() { config.agent.max_iterations = std::cmp::min(config.agent.max_iterations, kanban_cfg.goal_inner_max_iterations as usize); tracing::info!(target: "kanban.goal_mode", clamped_to = kanban_cfg.goal_inner_max_iterations, "goal-mode: inner agent loop clamped"); } }`
      NOTE: the exact API for loading KanbanConfig in this main.rs scope may already exist locally — locate via grep `KanbanConfig::` in main.rs and reuse the established loader (don't introduce `load_or_default` if a sibling like `load_from_default_path` already exists).
    - Verify config.agent.max_iterations field type (usize vs u32) — if u32, drop the `as usize` cast; if usize, keep it. Type-mismatch surfaces at build time.

    Create NEW file `crates/ironhermes-cli/tests/goal_toolset_filter.rs` with the 6 tests above. The tests drive `filter_for_goal_mode_if_applicable` directly with a synthesized ToolRegistry. For Tests 4 and 5, the filter must early-return when env conditions are not met — those tests set/unset HERMES_KANBAN_TASK + HERMES_KANBAN_GOAL_MODE with ENV_LOCK guards and assert tool count unchanged. For Test 6, the clamp is an inline expression in main.rs — extract it into a helper fn `fn clamp_max_iterations(config_iter: usize, kanban_iter: u32) -> usize` (or similar) at module scope, then test that helper directly. This avoids needing to spawn a worker process.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-tools -- retain_by_name && cargo test -p ironhermes-cli --test goal_toolset_filter -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub fn retain_by_name' crates/ironhermes-tools/src/registry.rs` == 1
    - `grep -c 'fn filter_for_goal_mode_if_applicable' crates/ironhermes-cli/src/main.rs` == 1
    - `grep -c 'filter_for_goal_mode_if_applicable' crates/ironhermes-cli/src/main.rs` >= 3 (1 fn def + 2 call sites at :873 + :1629)
    - `grep -cE 'goal_inner_max_iterations|clamped_to' crates/ironhermes-cli/src/main.rs` >= 2 (clamp block present in both worker-mode entries)
    - `cargo test -p ironhermes-tools -- retain_by_name` reports >= 2 passed
    - `cargo test -p ironhermes-cli --test goal_toolset_filter -- --test-threads=1` reports `6 passed; 0 failed`
    - Restricted preset exact-count gate: Test 1 of goal_toolset_filter.rs asserts `registry.len() == 15` after filter
    - `cargo build -p ironhermes-cli -p ironhermes-tools` exit 0
    - `cargo test --workspace --no-fail-fast` shows no NEW failures (carry-forward Phase 36.3.7.12 failures expected: 2 DEFER-1 + 5 protocol_violation)
    - Crate-isolation fence still held: `grep -cE '^ironhermes-gateway\s*=' crates/ironhermes-kanban/Cargo.toml` == 0
    - §6 dual-rail unit-citation gate satisfied (per CONTEXT D-C1): the 6 tests in goal_toolset_filter.rs are the citation rail referenced by VALIDATION.md
  </acceptance_criteria>
  <done>Filter pass wired at both worker-mode entry points. ToolRegistry has retain_by_name. Restricted preset enforces 15 tools exactly. Inner-loop clamp applies. 6 filter tests + 2 retain_by_name tests pass. F-03 closed on both axes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → tasks.goal_toolset | Enum-typed at CLI/JSON-schema entry; SQLite stores via parameterized binding |
| Dispatcher → worker env (HERMES_KANBAN_GOAL_TOOLSET) | Operator-controlled via tasks.goal_toolset; resolved at spawn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.13-03-T01 | Tampering | goal_toolset column (SQLite TEXT NULL accepting enum strings) | mitigate | Enum values are validated at TWO entry points: clap `value_parser = ["restricted","extended","full"]` and JSON schema `enum: [...]` on KanbanCreateTool. SQLite binds via parameterized rusqlite calls (existing pattern). Unrecognized values at worker side fall back to "restricted" (the safest preset) per filter_for_goal_mode_if_applicable. Severity LOW. |
| T-36.3.7.13-03-EoP | Elevation of Privilege | full preset escape hatch | accept | `full` preset removes all filtering — operator-known escape hatch per SCOPE.md D-07. Future DEFCON-tiered phase will tier-cap this. Severity NEGATIVE in net (this phase REMOVES tools from goal-mode workers; full is the documented opt-out). |
| T-36.3.7.13-03-I | Information Disclosure | LLM no longer sees terminal/delegate_task schemas | mitigate (positive) | This phase reduces blast radius. Restricted preset removes terminal (shell), delegate_task (subagent spawn). NEGATIVE-severity threat: this is a security improvement. |
| T-36.3.7.13-03-SC | Tampering | crate-isolation fence | mitigate | Plan adds NO new ironhermes-kanban → ironhermes-gateway deps. Task 3 acceptance gate verifies `grep -cE '^ironhermes-gateway\s*=' crates/ironhermes-kanban/Cargo.toml` == 0. |
</threat_model>

<verification>
- `cargo test -p ironhermes-kanban --test goal_toolset_migration -- --test-threads=1` reports 4 passed
- `cargo test -p ironhermes-cli --test goal_toolset_dispatch -- --test-threads=1` reports 3-4 passed
- `cargo test -p ironhermes-cli --test goal_toolset_filter -- --test-threads=1` reports 6 passed
- `cargo test -p ironhermes-tools -- retain_by_name` reports 2 passed
- `cargo test -p ironhermes-kanban --no-fail-fast` reports >= 128 passed, 0 NEW failures
- `cargo build --workspace --features iron_hermes_ui/server` exit 0
- `cargo test --workspace --no-fail-fast` shows only Phase 36.3.7.12 carry-forward failures (2 DEFER-1 + 5 protocol_violation)
- Crate-isolation fence held
- §6 dual-rail unit-citation gate satisfied (per CONTEXT D-C1)
</verification>

<success_criteria>
- Schema v3 materially observable: `pub const SCHEMA_VERSION: i64 = 3` + `goal_toolset TEXT` column in tasks (verified via PRAGMA table_info assertion in Test 1 of goal_toolset_migration.rs)
- D-B1 materially observable: RESTRICTED and EXTENDED constants in main.rs + Test 1 in goal_toolset_filter.rs asserts registry.len() == 15 after restricted filter
- D-B2 materially observable: filter_for_goal_mode_if_applicable called at BOTH main.rs:873 + main.rs:1629; LLM never sees removed schemas
- D-B3 materially observable: `--goal-toolset` clap flag with Option<String> + requires="goal" + no default_value
- D-E1 + D-E2 materially observable: NULL→"restricted" coercion at worker_spawn.rs; HERMES_KANBAN_GOAL_TOOLSET emitted on every goal_mode=true spawn
- D-F1 materially observable: `kanban.goal_inner_max_iterations: u32 = 10` in config.rs; clamp wired at both worker-mode entries
- D-G1 materially observable: KanbanCreateTool JSON schema has optional goal_toolset enum
- F-03 closed on BOTH axes: restricted toolset prevents tool-spawn thrash; inner-loop clamp bounds wall-clock per turn
</success_criteria>

<output>
Create `.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-03-SUMMARY.md` when done. Include: schema bump anchor + 6-SELECT-extension audit; worker_spawn HERMES_KANBAN_GOAL_TOOLSET anchor; retain_by_name + filter_for_goal_mode_if_applicable anchors with exact line numbers; restricted-preset 15-tool count gate result from goal_toolset_filter.rs Test 1; clamp gate result from goal_toolset_filter.rs Test 6; bilateral-tracing table per requirement D-B1, D-B2, D-B3, D-E1, D-E2, D-F1, D-G1, Schema-v3; crate-isolation fence audit; §6 dual-rail unit-citation pointer.
</output>
