---
phase: 36.3.7.13
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-kanban/src/store.rs
  - crates/ironhermes-cli/src/main.rs
  - crates/ironhermes-gateway/src/runner.rs
  - crates/iron_hermes_ui/src/server/kanban_api.rs
  - crates/iron_hermes_ui/src/server/kanban_ws.rs
  - crates/ironhermes-kanban/src/tools/show.rs
  - crates/ironhermes-kanban/src/tools/list.rs
  - crates/ironhermes-kanban/src/tools/comment.rs
  - crates/ironhermes-kanban/src/tools/complete.rs
  - crates/ironhermes-kanban/src/tools/block.rs
  - crates/ironhermes-kanban/src/tools/create.rs
  - crates/ironhermes-kanban/src/tools/heartbeat.rs
  - crates/ironhermes-kanban/src/tools/link.rs
  - crates/ironhermes-kanban/src/tools/unblock.rs
  - crates/ironhermes-kanban/src/tools/swarm.rs
  - crates/ironhermes-kanban/src/tools/mention.rs
  - crates/ironhermes-kanban/src/store_writer_impl.rs
  - crates/ironhermes-kanban/tests/cross_profile_db_resolution.rs
autonomous: true
requirements: [D-A1, D-A2, D-H1]
must_haves:
  truths:
    - "HERMES_KANBAN_DB env var, when set, overrides every dispatcher-bridged KanbanStore open call"
    - "Each open_from_env() call emits a `tracing::info!(target = \"kanban.db\", ...)` log line"
    - "Slug-aware dashboard handlers collapse to a single open_from_env_or_board(slug) call"
    - "Workers spawned under profile B with HERMES_KANBAN_DB pointing at profile A's DB read profile A's cards"
    - "Interactive shell sites at main.rs:1646 stay on open_default() (NOT swapped)"
    - "PRODUCER-SIDE (per WARNING 6 bilateral pairing): build_kanban_worker_env emits HERMES_KANBAN_DB into the child env Vec when a non-default DB path is configured; asserted by Test 6 in cross_profile_db_resolution.rs"
  artifacts:
    - path: "crates/ironhermes-kanban/src/store.rs"
      provides: "open_from_env() + open_from_env_or_board(slug) constructors"
      contains: "pub fn open_from_env"
    - path: "crates/ironhermes-kanban/tests/cross_profile_db_resolution.rs"
      provides: "6 bilateral tests covering D-A1/D-A2/D-H1 (5 consumer + 1 producer-side per WARNING 6)"
      contains: "HERMES_KANBAN_DB"
  key_links:
    - from: "crates/ironhermes-cli/src/main.rs:935"
      to: "KanbanStore::open_from_env"
      via: "run_single dispatcher wrapper"
      pattern: "KanbanStore::open_from_env\\(\\)"
    - from: "crates/ironhermes-gateway/src/runner.rs:1281"
      to: "KanbanStore::open_from_env"
      via: "background dispatcher loop"
      pattern: "KanbanStore::open_from_env\\(\\)"
    - from: "crates/ironhermes-kanban/src/tools/{show,list,comment,complete,block,create,heartbeat,link,unblock,swarm,mention}.rs"
      to: "KanbanStore::open_from_env_or_board"
      via: "execute() bodies (11 sites)"
      pattern: "open_from_env_or_board"
---

<objective>
Close F-01 (cross-profile DB resolution). Add `KanbanStore::open_from_env()` and `open_from_env_or_board(slug)` constructors in `crates/ironhermes-kanban/src/store.rs`, then swap every dispatcher-bridged call site to the new helpers. The dispatcher already emits `HERMES_KANBAN_DB=<path>` to worker subprocess env (`worker_spawn.rs:111-115`); this plan makes the worker SIDE consume it.

Purpose: Operator hit silent cross-profile DB mismatch during Phase 36.3.7.12 live UAT (Row 1 dispatcher ran profile B, worker booted profile A, card invisible to worker). HERMES_KANBAN_DB env var was set-but-never-read. This phase closes the loop.

Output: 13 swapped call sites + 1 new test file with 5 tests + tracing visibility on every env-bridge open. Interactive-shell sites at `main.rs:1646` STAY on `open_default()` (per CONTEXT.md decision audit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-CONTEXT.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-RESEARCH.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-PATTERNS.md
@.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-VALIDATION.md
@.planning/phases/36.3.7.12-goal-mode-kanban-worker-loop-ralph-loop/36.3.7.12-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Key signatures the executor needs. Extracted from store.rs lines 223-234. -->
<!-- Existing sibling constructors that the two new helpers land beside. -->

From crates/ironhermes-kanban/src/store.rs:223-234:
  pub fn open_default() -> Result<Self>
  pub fn open_for_board(slug: &str) -> Result<Self>
  pub fn open(path: PathBuf) -> Result<Self>
  pub fn open_labeled(path: PathBuf, label: &str) -> Result<Self>

From crates/ironhermes-kanban/src/paths.rs:
  pub fn kanban_db_path() -> PathBuf
  pub fn board_db_path_for_slug(slug: &str) -> PathBuf

Two new public fns to add (D-A1, D-A2) — full bodies in PATTERNS.md "store.rs — new constructor helpers" anchor (lines 65-101):
  pub fn open_from_env() -> Result<Self>
  pub fn open_from_env_or_board(slug: Option<&str>) -> Result<Self>

Tracing target invariant (D-H1, locked):
  tracing::info!(target = "kanban.db", path = %path, "kanban store: using HERMES_KANBAN_DB override")

Swap site coordinates (per RESEARCH.md § Swap Site Summary Table):
  crates/ironhermes-cli/src/main.rs:935                    → open_from_env()
  crates/ironhermes-gateway/src/runner.rs:1281             → open_from_env()
  crates/iron_hermes_ui/src/server/kanban_ws.rs:239        → open_from_env()
  crates/iron_hermes_ui/src/server/kanban_api.rs:55,121,284,341,402,504,707,759 → open_from_env_or_board(slug.as_deref())
  crates/ironhermes-kanban/src/tools/show.rs:118           → open_from_env_or_board(Some(&board_ctx.slug))
  crates/ironhermes-kanban/src/tools/list.rs:132           → open_from_env_or_board(Some(&board_ctx.slug))
  crates/ironhermes-kanban/src/tools/comment.rs:149,187    → open_from_env_or_board(Some(&board_ctx.slug)) (two sites)
  crates/ironhermes-kanban/src/tools/complete.rs:163       → open_from_env_or_board(Some(&board_ctx.slug))
  crates/ironhermes-kanban/src/tools/block.rs:137          → open_from_env_or_board(Some(&board_ctx.slug))
  crates/ironhermes-kanban/src/tools/create.rs:299         → open_from_env_or_board(Some(&board_ctx.slug))
  crates/ironhermes-kanban/src/tools/heartbeat.rs:130      → open_from_env_or_board(Some(&board_ctx.slug))
  crates/ironhermes-kanban/src/tools/link.rs:126           → open_from_env_or_board(Some(&board_ctx.slug))
  crates/ironhermes-kanban/src/tools/unblock.rs:121        → open_from_env_or_board(Some(&board_ctx.slug))
  crates/ironhermes-kanban/src/tools/swarm.rs:376          → open_from_env_or_board(Some(&board_ctx.slug))
  crates/ironhermes-kanban/src/tools/mention.rs:199        → open_from_env_or_board(Some(&board_ctx.slug))
  crates/ironhermes-kanban/src/store_writer_impl.rs:33     → open_from_env()  (per RESEARCH.md Risk 2: tentative SWAP)
  crates/ironhermes-cli/src/main.rs:307                    → open_from_env()  (so register_kanban_tools_if_applicable store-Arc honors env; per RESEARCH.md Risk 6)

DO NOT swap (interactive shell, per CONTEXT.md):
  crates/ironhermes-cli/src/main.rs:1646                   → STAYS on open_default()
  crates/ironhermes-cli/src/kanban/store_reader_impl.rs:41 → STAYS (wraps 1646 path)
  crates/ironhermes-kanban/src/dispatcher.rs:187           → STAYS (already slug-aware open_for_board)
  crates/ironhermes-kanban/src/notifier.rs:262             → STAYS (already slug-aware open_for_board)
  tools/decompose.rs + tools/specify.rs                    → No direct call site; store injected at registration via main.rs:307 swap
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add open_from_env() + open_from_env_or_board() constructors + 5 bilateral tests (Wave 0 scaffold)</name>
  <files>crates/ironhermes-kanban/src/store.rs, crates/ironhermes-kanban/tests/cross_profile_db_resolution.rs</files>
  <read_first>
    - crates/ironhermes-kanban/src/store.rs (read lines 220-240 to see open_default/open_for_board siblings)
    - crates/ironhermes-kanban/src/paths.rs (confirm kanban_db_path signature)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-PATTERNS.md (section "store.rs — new constructor helpers" lines 43-101 — exact code to land)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-VALIDATION.md (lines 47-51 — 5 test rows for D-A1/D-A2)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-RESEARCH.md (§ Risks Risk 5 — env-isolation discipline)
    - crates/ironhermes-kanban/tests/goal_mode_migration.rs (analog test scaffold — tempfile + env::set_var/remove_var guards)
  </read_first>
  <behavior>
    - Test 1 (D-A1): open_from_env() reads HERMES_KANBAN_DB and opens that path verbatim
    - Test 2 (D-A1): open_from_env() falls back to kanban_db_path() when env unset
    - Test 3 (D-A1): open_from_env() returns Err on a path under a non-existent unwritable directory
    - Test 4 (D-A2): open_from_env_or_board(Some("foo")) with HERMES_KANBAN_DB set → env wins, slug ignored
    - Test 5 (D-A2): open_from_env_or_board(None) with HERMES_KANBAN_DB unset → falls back to kanban_db_path()
    - Test 6 (D-A1 PRODUCER SIDE — bilateral pairing per LEARNINGS 2026-05-30): build_kanban_worker_env emits HERMES_KANBAN_DB into the returned child env Vec when the dispatcher resolves a non-default DB path. Mirrors Plan 02 Test 4's forward-carry pattern, applied to HERMES_KANBAN_DB. Drive against build_kanban_worker_env with a synthesized Task fixture; assert the returned Vec<(OsString, OsString)> contains a ("HERMES_KANBAN_DB", "<expected_path>") entry. Closes WARNING 6 from checker iteration 0 (producer + consumer paired in same commit set per crate-isolation bilateral discipline). Note: build_kanban_worker_env is in ironhermes-kanban — Test 6 can live in this same file OR inline in worker_spawn.rs (executor picks the lower-friction site; the test file is the default).
  </behavior>
  <action>
    Add two new public fns to `crates/ironhermes-kanban/src/store.rs` immediately after `open_for_board` (around line 234), copying the exact bodies from PATTERNS.md "store.rs — new constructor helpers" anchor lines 65-101. Both fns emit `tracing::info!(target = "kanban.db", path = %path, "kanban store: using HERMES_KANBAN_DB override")` per D-H1 — the `target = "kanban.db"` string is the LOCKED invariant, do not rename. `open_from_env_or_board` also includes `slug = ?slug` in the log fields when env wins (per PATTERNS.md). NULL/None semantics: env wins over slug; slug wins over default. Create NEW file `crates/ironhermes-kanban/tests/cross_profile_db_resolution.rs` with 5 tests above. Use `static ENV_LOCK: std::sync::Mutex<()>` guard (see STATE.md side-quest 4208bc28) and `let _g = ENV_LOCK.lock().unwrap_or_else(|e| e.into_inner())` at top of each test that touches `HERMES_KANBAN_DB` — process-global env mutation race. After each test, `std::env::remove_var("HERMES_KANBAN_DB")` in a teardown block (or with a `Drop`-based guard).
  </action>
  <verify>
    <automated>cargo test -p ironhermes-kanban --test cross_profile_db_resolution -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub fn open_from_env' crates/ironhermes-kanban/src/store.rs` == 2 (open_from_env + open_from_env_or_board)
    - `grep -c 'target = "kanban.db"' crates/ironhermes-kanban/src/store.rs` >= 2 (D-H1 invariant on both helpers)
    - `grep -c 'HERMES_KANBAN_DB' crates/ironhermes-kanban/src/store.rs` >= 2 (env::var reads in both helpers)
    - `cargo test -p ironhermes-kanban --test cross_profile_db_resolution -- --test-threads=1` reports `6 passed; 0 failed` (5 D-A1/D-A2 tests + 1 producer-side Test 6 per WARNING 6)
    - `cargo build -p ironhermes-kanban` exit 0
    - Test file uses `std::sync::Mutex` ENV_LOCK guard (grep: `static ENV_LOCK: std::sync::Mutex` >= 1)
  </acceptance_criteria>
  <done>Two new constructors exist with locked tracing target. 5 bilateral tests pass. Worker code path can now read HERMES_KANBAN_DB; consumer swap in Tasks 2-3 will exercise it.</done>
</task>

<task type="auto">
  <name>Task 2: Swap 4 no-slug dispatcher-bridged sites + 8 dashboard slug-aware sites</name>
  <files>crates/ironhermes-cli/src/main.rs, crates/ironhermes-gateway/src/runner.rs, crates/iron_hermes_ui/src/server/kanban_ws.rs, crates/iron_hermes_ui/src/server/kanban_api.rs, crates/ironhermes-kanban/src/store_writer_impl.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/main.rs lines 295-320, 925-945, 1640-1655 (the three open_default call sites; 307 + 935 SWAP, 1646 STAYS)
    - crates/ironhermes-gateway/src/runner.rs lines 1275-1290 (background dispatcher spawn)
    - crates/iron_hermes_ui/src/server/kanban_ws.rs lines 235-245 (run_kanban_tail_loop)
    - crates/iron_hermes_ui/src/server/kanban_api.rs (read ALL 8 sites: lines 50-60, 116-125, 280-290, 336-345, 397-407, 500-509, 703-712, 754-763 — to see the Some(slug)/None pattern)
    - crates/ironhermes-kanban/src/store_writer_impl.rs lines 25-45 (line 33 swap; per RESEARCH.md Risk 2)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-RESEARCH.md (§ Parity Audit, § Swap Site Summary Table, § Risks 2, 5, 6)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-PATTERNS.md (§ Swap Site Summary Table at the bottom)
  </read_first>
  <action>
    13 swap edits across 5 files. Each is a single-line replacement (no behavior changes beyond the env-bridge):

    1. `crates/ironhermes-cli/src/main.rs:307` — inside `register_kanban_tools_if_applicable`, replace `KanbanStore::open_default()` → `KanbanStore::open_from_env()`. This is the store-Arc creation site that flows to ALL 13 tools (including decompose/specify which receive the store via constructor injection per RESEARCH.md Risk 6 — single swap covers them).
    2. `crates/ironhermes-cli/src/main.rs:935` — inside `run_single`, replace `KanbanStore::open_default()` → `KanbanStore::open_from_env()`.
    3. `crates/ironhermes-cli/src/main.rs:1646` — DO NOT TOUCH. This is the interactive `/kanban dispatch` slash command path. CONTEXT.md explicitly locks this as STAYS-on-open_default. Audit-grep: confirm it still reads `open_default` after edits.
    4. `crates/ironhermes-gateway/src/runner.rs:1281` — gateway background dispatcher loop, replace `KanbanStore::open_default()` → `KanbanStore::open_from_env()`.
    5. `crates/iron_hermes_ui/src/server/kanban_ws.rs:239` — `run_kanban_tail_loop`, replace `KanbanStore::open_default()` → `KanbanStore::open_from_env()`.
    6-13. `crates/iron_hermes_ui/src/server/kanban_api.rs` — all 8 `#[server]` fns share the `Some(slug) => open_for_board(slug)` / `None => open_default()` pattern. Collapse each to a single `KanbanStore::open_from_env_or_board(slug.as_deref())` call. Watch out for variations: some sites bind to `slug: String`, others `slug: Option<String>`. Use `.as_deref()` to coerce to `Option<&str>` per the helper signature.
    14. `crates/ironhermes-kanban/src/store_writer_impl.rs:33` — replace `KanbanStore::open_default()` → `KanbanStore::open_from_env()` (per RESEARCH.md Risk 2 tentative-swap recommendation; this is dashboard write paths which run in the same dispatcher-bridged context as the SPA reads).

    Imports: `kanban_api.rs` and `store_writer_impl.rs` may need no import change since they already import `KanbanStore`. Audit each file's `use` block after edits.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-cli -p ironhermes-gateway -p iron_hermes_ui -p ironhermes-kanban --features iron_hermes_ui/server 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'open_default' crates/ironhermes-cli/src/main.rs` exactly 1 (only line 1646 remains; lines 307 and 935 swapped). Comments stripped: `grep -v '^[[:space:]]*//' crates/ironhermes-cli/src/main.rs | grep -c 'KanbanStore::open_default'` == 1
    - `grep -cE 'KanbanStore::open_default\(\)' crates/ironhermes-gateway/src/runner.rs` == 0
    - `grep -cE 'KanbanStore::open_default\(\)' crates/iron_hermes_ui/src/server/kanban_ws.rs` == 0
    - `grep -cE 'KanbanStore::open_default\(\)' crates/iron_hermes_ui/src/server/kanban_api.rs` == 0
    - `grep -cE 'KanbanStore::open_for_board' crates/iron_hermes_ui/src/server/kanban_api.rs` == 0 (collapsed into open_from_env_or_board)
    - `grep -cE 'open_from_env_or_board' crates/iron_hermes_ui/src/server/kanban_api.rs` == 8
    - `grep -cE 'KanbanStore::open_default\(\)' crates/ironhermes-kanban/src/store_writer_impl.rs` == 0
    - `cargo build -p ironhermes-cli -p ironhermes-gateway -p iron_hermes_ui -p ironhermes-kanban --features iron_hermes_ui/server` exit 0
    - `cargo test -p ironhermes-gateway --no-fail-fast` no NEW regressions vs Phase 36.3.7.12 baseline (165 passed pre-this-phase)
    - `cargo test -p ironhermes-kanban --no-fail-fast` shows the 5 new tests from Task 1 + no NEW regressions on existing 120-test baseline
  </acceptance_criteria>
  <done>13 sites swapped (1 in main.rs at 307, 1 in main.rs at 935, 1 in runner.rs:1281, 1 in kanban_ws.rs:239, 8 in kanban_api.rs, 1 in store_writer_impl.rs). Interactive main.rs:1646 untouched. Workspace builds, no test regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Swap 12 LLM tool sites + verify crate-isolation fence + tracing audit</name>
  <files>crates/ironhermes-kanban/src/tools/show.rs, crates/ironhermes-kanban/src/tools/list.rs, crates/ironhermes-kanban/src/tools/comment.rs, crates/ironhermes-kanban/src/tools/complete.rs, crates/ironhermes-kanban/src/tools/block.rs, crates/ironhermes-kanban/src/tools/create.rs, crates/ironhermes-kanban/src/tools/heartbeat.rs, crates/ironhermes-kanban/src/tools/link.rs, crates/ironhermes-kanban/src/tools/unblock.rs, crates/ironhermes-kanban/src/tools/swarm.rs, crates/ironhermes-kanban/src/tools/mention.rs</files>
  <read_first>
    - crates/ironhermes-kanban/src/tools/show.rs lines 110-125 (sample tool — `open_for_board(&board_ctx.slug)` pattern shared by 11 tools)
    - crates/ironhermes-kanban/src/tools/comment.rs lines 140-200 (two-site tool — confirm both line 149 AND line 187 are swap sites; both call open_for_board(&board_ctx.slug))
    - crates/ironhermes-kanban/src/tools/decompose.rs (read entirely — confirm NO open_for_board call site exists; store is injected via Arc<TokioMutex<KanbanStore>> in constructor per RESEARCH.md Parity Audit "Total tool sites" note)
    - crates/ironhermes-kanban/src/tools/specify.rs (read entirely — same as decompose; no swap)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-PATTERNS.md (§ Swap Site Summary Table)
    - .planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-RESEARCH.md (§ Risk 6 — decompose/specify store injection timing)
    - crates/ironhermes-kanban/Cargo.toml (verify crate-isolation fence: no ironhermes-gateway dep)
  </read_first>
  <action>
    11 swap edits across 11 tool files (12 call sites — comment.rs has two). Each `open_for_board(&board_ctx.slug)` becomes `open_from_env_or_board(Some(&board_ctx.slug))`:

    Files + line numbers (per RESEARCH.md Parity Audit § VERIFIED):
    - show.rs:118
    - list.rs:132
    - comment.rs:149 AND comment.rs:187 (two sites in same fn — both swap)
    - complete.rs:163
    - block.rs:137
    - create.rs:299
    - heartbeat.rs:130
    - link.rs:126
    - unblock.rs:121
    - swarm.rs:376
    - mention.rs:199

    DO NOT TOUCH:
    - tools/decompose.rs (store is constructor-injected; covered by main.rs:307 swap from Task 2)
    - tools/specify.rs (same — constructor injection)

    After all 12 swaps land, perform the crate-isolation fence audit + tracing visibility audit:

    Fence: `grep -cE '^ironhermes-gateway\s*=' crates/ironhermes-kanban/Cargo.toml` == 0. The swap MUST NOT introduce any new dep on `ironhermes-gateway` to `ironhermes-kanban`.

    Tracing audit: run `cargo test -p ironhermes-kanban --no-fail-fast` with `RUST_LOG=kanban.db=info` and confirm at least one `kanban.db` info line emits during the new cross_profile_db_resolution tests (this asserts D-H1 loud-every-call is wired through the new helpers).

    Workspace gate: `cargo test --workspace --no-fail-fast` should show only pre-existing Phase 36.3.7.12 failures (2 DEFER-1 + 5 protocol_violation per SCOPE.md §2). NO new failures introduced by the 25 swaps.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-kanban --no-fail-fast 2>&1 | tail -20 && ! grep -lE 'open_for_board\(&board_ctx\.slug\)' crates/ironhermes-kanban/src/tools/*.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -lE 'open_for_board\(&board_ctx\.slug\)' crates/ironhermes-kanban/src/tools/*.rs` lists ONLY decompose.rs/specify.rs IF those use the pattern at all (verify they do NOT — both inject store) — expect 0 files listed
    - `grep -c 'open_from_env_or_board' crates/ironhermes-kanban/src/tools/show.rs` == 1
    - `grep -c 'open_from_env_or_board' crates/ironhermes-kanban/src/tools/list.rs` == 1
    - `grep -c 'open_from_env_or_board' crates/ironhermes-kanban/src/tools/comment.rs` == 2 (two sites)
    - `grep -c 'open_from_env_or_board' crates/ironhermes-kanban/src/tools/complete.rs` == 1
    - `grep -c 'open_from_env_or_board' crates/ironhermes-kanban/src/tools/block.rs` == 1
    - `grep -c 'open_from_env_or_board' crates/ironhermes-kanban/src/tools/create.rs` == 1
    - `grep -c 'open_from_env_or_board' crates/ironhermes-kanban/src/tools/heartbeat.rs` == 1
    - `grep -c 'open_from_env_or_board' crates/ironhermes-kanban/src/tools/link.rs` == 1
    - `grep -c 'open_from_env_or_board' crates/ironhermes-kanban/src/tools/unblock.rs` == 1
    - `grep -c 'open_from_env_or_board' crates/ironhermes-kanban/src/tools/swarm.rs` == 1
    - `grep -c 'open_from_env_or_board' crates/ironhermes-kanban/src/tools/mention.rs` == 1
    - `grep -c 'open_for_board' crates/ironhermes-kanban/src/tools/decompose.rs` == 0 AND `grep -c 'open_for_board' crates/ironhermes-kanban/src/tools/specify.rs` == 0 (confirms RESEARCH Parity Audit — they inject store, no direct open call)
    - Crate-isolation fence: `grep -cE '^ironhermes-gateway\s*=' crates/ironhermes-kanban/Cargo.toml` == 0
    - `cargo test -p ironhermes-kanban --no-fail-fast` reports >= 125 passed (120 baseline + 5 new from Task 1); 0 NEW failures
    - `cargo test --workspace --no-fail-fast 2>&1 | grep -cE '^(test result:|FAILED)' | tail` shows only known Phase 36.3.7.12 carry-forward failures (2 DEFER-1 + 5 protocol_violation)
  </acceptance_criteria>
  <done>All 25 dispatcher-bridged sites swap to env-bridge helpers (12 in tools/* this task; 13 from Task 2). Decompose/specify covered transitively via Task 2's main.rs:307 swap. Crate-isolation fence held. Workspace tests show no new regressions. F-01 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → process env | HERMES_KANBAN_DB string is operator-supplied (CLI invocation or build_kanban_worker_env emission) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.13-01-T01 | Tampering | HERMES_KANBAN_DB path string | accept | Operator owns env. SQLite errors surface cleanly on open. No file-exists check (per CONTEXT.md deferred scope). Severity LOW — env is not a network surface. |
| T-36.3.7.13-01-T02 | Information Disclosure | tracing::info!(target = "kanban.db", path = %path) | accept | The DB path itself is operator-known (they set the env). Logging the path is the foot-gun-visibility mitigation per D-H1. RUST_LOG=kanban.db=warn silences. Severity LOW. |
| T-36.3.7.13-01-SC | Tampering | crate-isolation fence | mitigate | Task 3 acceptance gate: `grep -cE '^ironhermes-gateway\s*=' crates/ironhermes-kanban/Cargo.toml` == 0. Fence-by-construction; swaps add no new deps. |
</threat_model>

<verification>
- `cargo build -p ironhermes-cli -p ironhermes-gateway -p iron_hermes_ui -p ironhermes-kanban --features iron_hermes_ui/server` exit 0
- `cargo test -p ironhermes-kanban --test cross_profile_db_resolution -- --test-threads=1` reports 6 passed (producer + consumer paired bilaterally per WARNING 6)
- `cargo test -p ironhermes-kanban --no-fail-fast` reports >= 125 passed, 0 NEW failures
- `cargo test --workspace --no-fail-fast` no NEW failures (known Phase 36.3.7.12 failures allowed)
- All 25 swap sites verified via grep (per Task 2/3 acceptance criteria)
- Crate-isolation fence held: `grep -cE '^ironhermes-gateway\s*=' crates/ironhermes-kanban/Cargo.toml` == 0
</verification>

<success_criteria>
- D-A1 materially observable: `pub fn open_from_env()` exists at crates/ironhermes-kanban/src/store.rs with `target = "kanban.db"` tracing
- D-A2 materially observable: `pub fn open_from_env_or_board(slug: Option<&str>)` exists; 8 dashboard sites collapsed; 11 LLM tool sites swapped
- D-H1 materially observable: at least one `tracing::info!(target = "kanban.db", ...)` fires per env-override open (confirmed via test 4 in cross_profile_db_resolution.rs)
- Interactive shell sites preserved: main.rs:1646 still reads open_default (audit-grep confirms)
- F-01 closed: workers spawned under profile B with HERMES_KANBAN_DB pointing at profile A's DB now resolve profile A's cards
</success_criteria>

<output>
Create `.planning/phases/36.3.7.13-kanban-worker-cross-profile-ergonomics/36.3.7.13-01-SUMMARY.md` when done. Include: all 25 swap sites by file:line with before/after; 5-test pass record from cross_profile_db_resolution.rs; bilateral-tracing table (producer = store.rs new helpers + 25 swap sites; consumer = 5 tests + workspace suite); crate-isolation fence audit (grep result); D-A1/D-A2/D-H1 anchor lines in the new code.
</output>
