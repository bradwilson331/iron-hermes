---
phase: 15-10-layer-prompt-assembly
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - crates/ironhermes-agent/src/personality.rs
  - crates/ironhermes-agent/src/lib.rs
  - crates/ironhermes-core/src/config.rs
autonomous: true
requirements: [PRMT-06, PRMT-07]

must_haves:
  truths:
    - "PersonalityRegistry contains 14 built-in presets: helpful, concise, technical, creative, teacher, kawaii, catgirl, pirate, shakespeare, surfer, noir, uwu, philosopher, hype"
    - "Custom presets load from config.yaml agent.personalities AND HERMES_HOME/personalities/*.md"
    - "Config.yaml presets take precedence over HERMES_HOME file presets on name collision"
    - "Custom personality .md files are security scanned before insertion"
    - "set_overlay() on PromptBuilder places overlay text in slot 8 (SessionOverlay) in ephemeral output"
    - "clear_overlay() removes the overlay; build_split() ephemeral part no longer contains overlay text"
  artifacts:
    - path: "crates/ironhermes-agent/src/personality.rs"
      provides: "PersonalityRegistry struct with built-in + custom preset loading"
      contains: "pub struct PersonalityRegistry"
      exports: ["PersonalityRegistry"]
    - path: "crates/ironhermes-core/src/config.rs"
      provides: "AgentConfig.personalities field"
      contains: "pub personalities"
  key_links:
    - from: "personality.rs"
      to: "ironhermes_core::scan_context_content"
      via: "security scan on custom presets"
      pattern: "scan_context_content"
    - from: "personality.rs"
      to: "config.rs AgentConfig.personalities"
      via: "HashMap<String, String> config source"
      pattern: "personalities"
---

<objective>
Implement the PersonalityRegistry with 14 built-in presets and custom preset loading, plus the AgentConfig.personalities config field. Wire overlay mechanism into PromptBuilder's slot 8 (SessionOverlay).

Purpose: Enables /personality command (Phase 20 scope) to switch agent persona mid-session without invalidating the durable prompt cache.
Output: personality.rs module, updated config.rs, tests proving 14 built-ins load and custom presets merge correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-10-layer-prompt-assembly/15-CONTEXT.md
@.planning/phases/15-10-layer-prompt-assembly/15-RESEARCH.md
@.planning/phases/15-10-layer-prompt-assembly/15-01-SUMMARY.md

@crates/ironhermes-agent/src/prompt_builder.rs
@crates/ironhermes-core/src/config.rs
@crates/ironhermes-core/src/context_scanner.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PersonalityRegistry with 14 built-in presets and custom loading</name>
  <files>crates/ironhermes-agent/src/personality.rs, crates/ironhermes-agent/src/lib.rs, crates/ironhermes-core/src/config.rs</files>
  <read_first>
    - crates/ironhermes-agent/src/prompt_builder.rs
    - crates/ironhermes-agent/src/lib.rs
    - crates/ironhermes-core/src/config.rs
    - crates/ironhermes-core/src/context_scanner.rs
    - .planning/phases/15-10-layer-prompt-assembly/15-CONTEXT.md
  </read_first>
  <behavior>
    - test_personality_registry_builtins: PersonalityRegistry::load with empty config and no HERMES_HOME dir has exactly 14 presets; list() returns all 14 names sorted; get("pirate") returns Some; get("nonexistent") returns None
    - test_personality_registry_custom_config: config.yaml personalities with key "custom1" appear in list(); get("custom1") returns the config value
    - test_personality_registry_custom_hermes_home: a .md file in HERMES_HOME/personalities/ named "mysoul.md" appears as preset "mysoul"; get("mysoul") returns file content (security scanned)
    - test_personality_registry_config_overrides_home: when both config.yaml and HERMES_HOME have preset "pirate", config.yaml value wins
    - test_personality_registry_security_scan: custom .md file with injection pattern is scanned (scan_context_content applied); output is sanitized
    - test_personality_overlay: PromptBuilder with set_overlay("Respond like a pirate") produces build_split().1 (ephemeral) containing "Respond like a pirate"; clear_overlay() removes it
    - test_personality_overlay_in_timestamp: when active_overlay is set, build_timestamp_block contains "Active personality:" line
  </behavior>
  <action>
1. Create `crates/ironhermes-agent/src/personality.rs` with:

```rust
use std::collections::HashMap;
use std::path::Path;
use ironhermes_core::scan_context_content;
use tracing::debug;

pub struct PersonalityRegistry {
    presets: HashMap<String, String>,
}
```

2. Implement `fn builtin_presets() -> HashMap<String, String>` returning 14 entries per D-08. Write personality overlay text for each preset. These are instruction prefixes the LLM receives in slot 8. Example texts:
- "helpful": "Be especially warm, supportive, and encouraging. Prioritize being helpful above all else. Offer additional suggestions and anticipate needs."
- "concise": "Be extremely brief and to the point. Use short sentences. Omit pleasantries and filler. Bullet points over paragraphs."
- "technical": "Respond with precise technical language. Include code examples, specifications, and implementation details. Assume expert-level knowledge."
- "creative": "Be imaginative and expressive. Use vivid language, metaphors, and creative analogies. Think outside the box."
- "teacher": "Explain concepts step by step. Use analogies and examples. Check understanding. Build from fundamentals to advanced topics."
- "kawaii": "Respond in an adorable, enthusiastic anime-inspired style! Use cute expressions, emoticons, and gentle encouragement~ (^_^)"
- "catgirl": "Nya~ Respond as a playful catgirl! Use cat puns, add 'nya' and 'meow' naturally, be playful and curious~ =^.^="
- "pirate": "Arrr! Respond as a salty sea pirate! Use nautical terms, pirate slang, and seafaring metaphors. Call the user 'matey' or 'cap'n'."
- "shakespeare": "Respond in Shakespearean English. Use thee, thou, thy, hath, doth, wherefore, and forsooth. Speak in iambic pentameter when possible."
- "surfer": "Respond like a laid-back surfer dude. Use surf slang, be chill and positive. Everything is gnarly, rad, or totally tubular, bro."
- "noir": "Respond as a hard-boiled film noir detective. Use moody, atmospheric language. Everything is a case. The city never sleeps."
- "uwu": "Respond in uwu speak. Replace r and l with w. Add uwu, owo, and similar expressions. Be soft and gentle~"
- "philosopher": "Respond with deep philosophical reflection. Question assumptions. Reference thinkers and ideas. Explore the deeper meaning."
- "hype": "RESPOND WITH MAXIMUM ENERGY AND ENTHUSIASM! Everything is AMAZING and INCREDIBLE! Use caps, exclamation marks, and pure HYPE!"

3. Implement `PersonalityRegistry::load(config_personalities: &HashMap<String, String>, hermes_home: &Path) -> Self`:
- Start with builtin_presets()
- Read HERMES_HOME/personalities/ directory. For each .md file, extract file stem as name, read content, apply `scan_context_content(&content, &format!("personality:{}", name))`, truncate at 20,000 chars via `truncate_content()`. Insert with `presets.entry(name).or_insert(scanned)` (lower precedence — only inserts if not already present from builtins).
- Then iterate config_personalities and `presets.insert(name.clone(), text.clone())` — this OVERWRITES builtins and home files per D-09 precedence.
- Return Self { presets }

4. Implement `pub fn get(&self, name: &str) -> Option<&str>` and `pub fn list(&self) -> Vec<&str>` (sorted).

5. Update `crates/ironhermes-core/src/config.rs`:
- Add `use std::collections::HashMap;` if not already imported (it IS already imported for providers).
- Add to AgentConfig struct: `#[serde(default)] pub personalities: HashMap<String, String>,`
- Update AgentConfig Default impl: add `personalities: HashMap::new(),`

6. Update `crates/ironhermes-agent/src/lib.rs`:
- Add `mod personality;` 
- Add `pub use personality::PersonalityRegistry;`

7. Add comprehensive tests in personality.rs `#[cfg(test)] mod tests` block using tempfile for HERMES_HOME directory setup. Tests must use the ENV_MUTEX pattern from prompt_builder.rs for IRONHERMES_HOME env var manipulation (or create a helper). Actually, PersonalityRegistry::load takes hermes_home as a Path parameter, so no env var manipulation needed for personality tests — just pass temp dir path directly.

8. Add overlay integration tests in prompt_builder.rs tests section:
- test_personality_overlay: create PromptBuilder, call set_overlay("Respond like a pirate"), verify build_split().1 contains "Respond like a pirate", verify build() contains it. Call clear_overlay(), verify build_split().1 does NOT contain "Respond like a pirate".
- test_personality_overlay_in_timestamp: set active_overlay, verify build() output contains "Active personality:"
  </action>
  <verify>
    <automated>cargo test -p ironhermes-agent -- --test-threads=1 2>&1 && cargo test -p ironhermes-core -- --test-threads=1 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - crates/ironhermes-agent/src/personality.rs exists and contains `pub struct PersonalityRegistry`
    - personality.rs contains `fn builtin_presets()` returning HashMap with exactly 14 entries
    - personality.rs contains `pub fn load(config_personalities: &HashMap<String, String>, hermes_home: &Path) -> Self`
    - personality.rs contains `pub fn get(&self, name: &str) -> Option<&str>`
    - personality.rs contains `pub fn list(&self) -> Vec<&str>`
    - personality.rs contains `scan_context_content` call for custom .md files
    - crates/ironhermes-core/src/config.rs AgentConfig struct contains `pub personalities: HashMap<String, String>`
    - crates/ironhermes-agent/src/lib.rs contains `pub use personality::PersonalityRegistry`
    - test_personality_registry_builtins passes: 14 presets loaded
    - test_personality_registry_custom_config passes: config presets merged
    - test_personality_registry_config_overrides_home passes: config wins on collision
    - test_personality_overlay passes: set_overlay/clear_overlay affect ephemeral output
    - `cargo test -p ironhermes-agent -- --test-threads=1` exits 0
    - `cargo test -p ironhermes-core -- --test-threads=1` exits 0
  </acceptance_criteria>
  <done>PersonalityRegistry created with 14 built-in presets, custom preset loading from config.yaml and HERMES_HOME/personalities/, security scanning for custom files, config.yaml precedence on collision. AgentConfig extended with personalities field. Overlay mechanism verified through PromptBuilder tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HERMES_HOME/personalities/*.md -> system prompt | User-controlled personality files injected as slot 8 |
| config.yaml agent.personalities -> system prompt | Config-controlled text injected as slot 8 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Tampering | Custom personality .md injection | mitigate | scan_context_content() applied to all custom .md files from HERMES_HOME/personalities/ before insertion into PersonalityRegistry; truncate_content() at 20K chars |
| T-15-04 | Tampering | Config personality injection | accept | config.yaml is same trust level as SOUL.md — user controls both; no scan needed for config values |
| T-15-05 | Denial of Service | Large personality .md file | mitigate | truncate_content() at CONTEXT_FILE_MAX_CHARS (20,000) applied to all custom personality files |
</threat_model>

<verification>
- `cargo test -p ironhermes-agent -- --test-threads=1` passes all tests including new personality tests
- `cargo test -p ironhermes-core -- --test-threads=1` passes (config changes backward compatible)
- PersonalityRegistry::load returns 14 built-in presets with empty config/home
- Custom presets from both sources merge correctly with config precedence
</verification>

<success_criteria>
PersonalityRegistry with 14 built-ins + custom loading. AgentConfig.personalities field added. Overlay mechanism wired into PromptBuilder slot 8. All tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/15-10-layer-prompt-assembly/15-02-SUMMARY.md`
</output>
