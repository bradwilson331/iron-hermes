# Phase 26.1: fix-websocket-error-for-chat - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-04
**Phase:** 26.1-fix-websocket-error-for-chat
**Areas discussed:** Server turn concurrency model, Disconnect teardown semantics, Client reconnect lifecycle strategy, UI interruption behavior

---

## Server turn concurrency model

| Option | Description | Selected |
|--------|-------------|----------|
| Spawn one turn task per request + forward events concurrently | Live event forwarding while turn runs | ✓ |
| Keep turn inline and interleave sends | Lower task complexity, weaker lifecycle isolation | |
| Per-connection worker queue | Stronger structure, higher complexity | |

**User's choice:** Spawn one turn task per request + forward concurrently.
**Notes:** User also chose reject-on-concurrent-request, local in-flight gate in `ws_chat`, and terminal event ownership inside turn task.

---

## Disconnect teardown semantics

| Option | Description | Selected |
|--------|-------------|----------|
| Abort turn immediately on send failure, then exit | Fast leak prevention | ✓ |
| Graceful bounded wait then abort | Cleaner completion chance, more complexity | |
| Detach and let turn finish | Simpler but risks orphan work | |

**User's choice:** Immediate abort and clean loop exit.
**Notes:** User selected no explicit close-frame write on broken path, warn-level disconnect telemetry, and malformed-frame recovery (`Error` + continue loop).

---

## Client reconnect lifecycle strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Outer reconnect loop + state reset + re-enter receive | Persistent receive lifecycle | ✓ |
| Exit on first receive error and rely on remount | Can permanently stop stream handling | |
| Full page-level reinit on disconnect | Heavy-handed | |

**User's choice:** Outer reconnect lifecycle.
**Notes:** User selected built-in automatic reconnect only (no custom backoff), reset only `scanner_active` + `streaming_block_id`, and no auto-resubmit after reconnect.

---

## UI interruption behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Keep partial text visible and end stream state | Preserves user-visible progress | ✓ |
| Remove partial block on disconnect | Hides partial output | |
| Add new interruption metadata/badge UX | Out-of-scope UX expansion | |

**User's choice:** Keep partial output as history and start new AI block on next submit.
**Notes:** User requested transcript-visible disconnect error block with generic message, while technical details should be logged only.

---

## Claude's Discretion

None. User made explicit selections for all questions.

## Deferred Ideas

- Reviewed todo matches were intentionally not folded into this phase context; all were deferred as out-of-scope for websocket lifecycle stabilization.
