---
context: phase
phase: 26.1-fix-websocket-error-for-chat
task: 3
total_tasks: 3
status: blocked
last_updated: 2026-05-04T15:24:28.702Z
---

# BLOCKING CONSTRAINTS - Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Live UAT over static confidence - static checks/parity tests can pass while browser/proxy runtime still fails with websocket reset-without-close-handshake loop - always run and pass live Task 3 UAT before marking plan/phase complete.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| False completion from static-only evidence | Phase 26.1 repeatedly looked healthy in automated checks but failed in real browser/proxy runs with `Connection reset without closing handshake`. | blocking | Gate completion on Task 3 human-verify checkpoint approval with fresh live logs from `dx serve --package iron_hermes_ui`. |
| Blind staging in dirty monorepo | Repository contains hundreds of unrelated modified/untracked files; broad staging risks accidental commits. | advisory | Stage only explicit handoff/phase files with path-scoped commit commands. |

<current_state>
Paused in Phase 26.1, Plan 03, Task 3 (`checkpoint:human-verify`).

Task 1 and Task 2 are complete, plus a follow-up fix after failed checkpoint feedback.
Automated checks pass, but latest manual UAT still reports websocket reset without closing handshake during live chat.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Normalize server disconnect teardown for live proxy stability - Done (`e3c6f20`, `9e1fed8`)
- Task 2: Harden client reconnect receive lifecycle against repeated reset loops - Done (`017392e`, `7081711`)
- Task 2 follow-up (after failed UAT): Close-frame reconnect hardening - Done (`e830039`)
- Task 3: Human verification checkpoint - In progress (manual UAT still failing)
</completed_work>

<remaining_work>

- Task 3: Re-run live browser UAT and confirm all checkpoint criteria
- If still failing: capture logs/observations and continue another checkpoint continuation pass
- If approved: complete plan summary, then continue phase verification/completion flow
</remaining_work>

<decisions_made>

- Treated websocket close frames as reconnect boundaries so receive loop re-enters reconnect path instead of skipping close events.
- Deduplicated disconnect notices/log churn per disconnect window while preserving generic user-facing transcript copy.
- Kept D-01..D-15 lifecycle constraints locked; avoided protocol or route redesign.
</decisions_made>

<blockers>
- Blocking human checkpoint: Task 3 needs manual approval.
- Latest run still shows `Error proxying websocket connection: ... Connection reset without closing handshake` with reconnect churn.
- Placeholder/TBD scan found many legacy phase docs with placeholder strings; treat historical artifacts cautiously and do not infer completion from template text.
</blockers>

## Required Reading (in order)
1. `.planning/phases/26.1-fix-websocket-error-for-chat/26.1-03-PLAN.md` - current task contract and checkpoint criteria.
2. `.planning/phases/26.1-fix-websocket-error-for-chat/26.1-HUMAN-UAT.md` - latest manual failure evidence.
3. `.planning/phases/26.1-fix-websocket-error-for-chat/26.1-VERIFICATION.md` - verifier status (`human_needed`) and must-have traceability.
4. `.planning/phases/26.1-fix-websocket-error-for-chat/26.1-REVIEW.md` - remaining review warnings.
5. `crates/iron_hermes_ui/src/server/ws.rs` - disconnect/teardown logic under active iteration.
6. `crates/iron_hermes_ui/src/components/warp_hermes.rs` - reconnect/receive lifecycle and notice handling.
7. `.planning/METHODOLOGY.md` (if it exists) - project analytical lenses; apply before any assumption analysis.

## Critical Anti-Patterns (do NOT repeat these)
- STATIC-PARITY-ONLY-SIGNOFF: Treating passing checks as sufficient without live browser/proxy run -> require checkpoint approval with fresh manual evidence.
- WIDE-STAGE-IN-DIRTY-WORKTREE: Using broad `git add` in this repo state -> stage only explicit target files.

## Infrastructure State
- `dx serve --package iron_hermes_ui` is required for Task 3 UAT; no active long-running process is assumed at handoff time.
- Worktree is very dirty (`git status --short` shows 307 entries, many unrelated to Phase 26.1).

## Pre-Execution Critique Required
- Not applicable: this pause occurs mid-execution at a blocking checkpoint, not between design and execution.

<context>
Checkpoint continuation already consumed one failed UAT cycle and delivered a close-frame reconnect fix (`e830039`).
The next best signal is fresh live UAT evidence after this fix. Do not plan new scope until Task 3 is decisively approved or fails again with new logs.
</context>

<next_action>
Start with: run `dx serve --package iron_hermes_ui`, execute Task 3 manual steps from `26.1-03-PLAN.md`, then continue `/gsd-execute-phase 26.1 --gaps-only` with either "approved" or fresh failing logs.
</next_action>
