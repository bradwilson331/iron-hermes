---
phase: 26.1-fix-websocket-error-for-chat
plan: 02
type: execute
wave: 2
depends_on: [26.1-01]
files_modified:
  - crates/iron_hermes_ui/src/server/ws.rs
  - crates/iron_hermes_ui/src/components/warp_hermes.rs
  - crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs
autonomous: true
gap_closure: true
requirements: []
must_haves:
  truths:
    - "Default web build compiles without pulling server-only websocket dependencies (closes UAT Gap 1)."
    - "Server-feature websocket route still compiles and lifecycle parity tests keep passing after boundary hardening."
    - "Client websocket send/receive paths continue using shared protocol types while reconnect behavior from D-09..D-15 remains intact."
  artifacts:
    - path: "crates/iron_hermes_ui/src/server/ws.rs"
      provides: "Feature-safe websocket route module that isolates server-only runtime usage"
      exports: ["ws_chat"]
    - path: "crates/iron_hermes_ui/src/components/warp_hermes.rs"
      provides: "Client chat flow referencing shared protocol types independent of server runtime internals"
    - path: "crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs"
      provides: "Regression locks for feature boundary + lifecycle invariants"
  key_links:
    - from: "crates/iron_hermes_ui/src/components/warp_hermes.rs"
      to: "crates/iron_hermes_ui/src/protocol.rs"
      via: "ChatRequest/ChatStreamEvent serialization for ws.send_raw/recv_raw"
      pattern: "crate::protocol::(ChatRequest|ChatStreamEvent)"
    - from: "crates/iron_hermes_ui/src/server/ws.rs"
      to: "crates/iron_hermes_ui/src/server/state.rs::run_web_turn"
      via: "server-feature-gated turn execution path"
      pattern: "run_web_turn\(|#\[cfg\(feature = \"server\"\)\]"
---

<objective>
Close the Phase 26.1 manual-UAT blocking gap by making the websocket module compile cleanly in default web builds while preserving the server-feature chat lifecycle behavior delivered in Plan 01.

Purpose: Unblock `dx serve --package iron_hermes_ui` so manual reconnect/race verification can run.
Output: Feature-boundary-safe websocket code, protocol imports decoupled from server runtime, and parity tests guarding against regressions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.1-fix-websocket-error-for-chat/26.1-CONTEXT.md
@.planning/phases/26.1-fix-websocket-error-for-chat/26.1-VERIFICATION.md
@.planning/phases/26.1-fix-websocket-error-for-chat/26.1-HUMAN-UAT.md
@.planning/phases/26.1-fix-websocket-error-for-chat/26.1-01-SUMMARY.md
@crates/iron_hermes_ui/src/server/ws.rs
@crates/iron_hermes_ui/src/components/warp_hermes.rs
@crates/iron_hermes_ui/src/protocol.rs

<interfaces>
From crates/iron_hermes_ui/src/protocol.rs:
```rust
pub struct ChatRequest {
    pub session_id: String,
    pub message: String,
}

pub enum ChatStreamEvent {
    Delta { text: String },
    ToolCallStart { name: String, args: String },
    ToolCallEnd { name: String, success: bool },
    Finished { total_tokens: u32 },
    Error { message: String },
}
```

From crates/iron_hermes_ui/src/server/ws.rs:
```rust
#[get("/api/ws/chat")]
pub async fn ws_chat(ws: WebSocketOptions) -> Result<Websocket<String, String>>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Isolate server-only websocket runtime from default web build</name>
  <files>crates/iron_hermes_ui/src/server/ws.rs, crates/iron_hermes_ui/src/components/warp_hermes.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/ws.rs (current tokio/tracing imports and ws_chat implementation)
    - crates/iron_hermes_ui/src/components/warp_hermes.rs (current `crate::server::ws::ChatRequest/ChatStreamEvent` usage)
    - crates/iron_hermes_ui/src/protocol.rs (shared protocol source-of-truth)
    - crates/iron_hermes_ui/Cargo.toml (feature model: default web vs `server`)
    - .planning/phases/26.1-fix-websocket-error-for-chat/26.1-CONTEXT.md (D-01..D-15 lock)
    - .planning/phases/26.1-fix-websocket-error-for-chat/26.1-HUMAN-UAT.md (Gap 1 failure details)
  </read_first>
  <behavior>
    - Test 1: Default `cargo check -p iron_hermes_ui` succeeds without `E0432`/`E0603` from websocket module imports.
    - Test 2: `--features server` build still compiles and exposes `/api/ws/chat` behavior from D-01..D-08.
    - Test 3: Client send/receive paths serialize/parse `crate::protocol::{ChatRequest, ChatStreamEvent}` rather than importing protocol types from `server::ws`.
  </behavior>
  <action>
    Implement a strict feature boundary to close UAT Gap 1 while preserving all locked lifecycle decisions:

    1. In `crates/iron_hermes_ui/src/server/ws.rs`, gate server-only imports and server-only execution path behind `#[cfg(feature = "server")]` so `tokio::sync::mpsc`, `tokio::task::JoinHandle`, and `tracing::warn` are never required in default web builds (per D-01..D-08).
    2. Keep `ws_chat` available for both build modes, but ensure non-server compilation path does not reference server-only runtime symbols.
    3. In `crates/iron_hermes_ui/src/components/warp_hermes.rs`, replace `crate::server::ws::ChatRequest` and `crate::server::ws::ChatStreamEvent` references with `crate::protocol::ChatRequest` and `crate::protocol::ChatStreamEvent` so protocol usage is decoupled from server module internals (supports D-09..D-15 without cross-feature leaks).
    4. Preserve reconnect and interruption semantics already implemented in Plan 01 (D-09..D-15): no auto-resubmit, reset only transient state, keep partial AI blocks visible, generic disconnect copy.

    Do NOT redesign protocol shapes, endpoint path, or lifecycle policy (D-01..D-15 are locked).
  </action>
  <acceptance_criteria>
    - `crates/iron_hermes_ui/src/server/ws.rs` contains `#[cfg(feature = "server")]` guards around server-only runtime usage (tokio mpsc/join handle + warn telemetry path)
    - `crates/iron_hermes_ui/src/components/warp_hermes.rs` contains `crate::protocol::ChatRequest` and `crate::protocol::ChatStreamEvent`
    - `crates/iron_hermes_ui/src/components/warp_hermes.rs` no longer contains `crate::server::ws::ChatRequest` or `crate::server::ws::ChatStreamEvent`
    - `cargo check -p iron_hermes_ui` exits 0 (web/default build path)
    - `cargo check -p iron_hermes_ui --features server` exits 0 (server path)
  </acceptance_criteria>
  <verify>
    <automated>cargo check -p iron_hermes_ui &amp;&amp; cargo check -p iron_hermes_ui --features server</automated>
  </verify>
  <done>Web/default build no longer fails on websocket module imports, and server websocket code remains intact for runtime behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock feature-boundary fix and lifecycle invariants with parity tests</name>
  <files>crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs</files>
  <read_first>
    - crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs (existing lifecycle anchors)
    - crates/iron_hermes_ui/src/server/ws.rs (post-Task-1 feature-gated shape)
    - crates/iron_hermes_ui/src/components/warp_hermes.rs (post-Task-1 protocol import path)
    - crates/iron_hermes_ui/tests/server_runtime_parity.rs (parity-test style)
    - .planning/phases/26.1-fix-websocket-error-for-chat/26.1-HUMAN-UAT.md (Gap 1 closure target)
  </read_first>
  <behavior>
    - Test 1: Source parity test fails if websocket server code loses required feature-gating anchors.
    - Test 2: Source parity test fails if client code regresses back to `crate::server::ws::*` protocol references.
    - Test 3: Existing lifecycle checks for concurrent forwarding/reconnect reset continue to pass after boundary hardening.
  </behavior>
  <action>
    Extend `crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs` with explicit static-source assertions that:

    1. Verify server-only runtime usage in `ws.rs` is guarded by `#[cfg(feature = "server")]` anchors so default web builds remain clean.
    2. Verify client websocket component uses `crate::protocol` type paths for chat request/event serialization.
    3. Keep (or strengthen) branch-local lifecycle anchors from Plan 01 so D-01..D-15 behavior remains covered while closing review concern WR-03 false-positive risk.

    Maintain deterministic source-inspection tests only (no network sleeps, no browser automation).
  </action>
  <acceptance_criteria>
    - `crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs` includes explicit assertions for `#[cfg(feature = "server")]` websocket boundary anchors
    - test file includes explicit assertion for `crate::protocol::ChatRequest` and `crate::protocol::ChatStreamEvent` usage in `warp_hermes.rs`
    - lifecycle parity assertions remain present for concurrent forwarding and reconnect reset behavior
    - `cargo test -p iron_hermes_ui --features server --test websocket_lifecycle_parity -- --nocapture` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p iron_hermes_ui --features server --test websocket_lifecycle_parity -- --nocapture &amp;&amp; cargo test -p iron_hermes_ui --features server --test server_runtime_parity -- --nocapture</automated>
  </verify>
  <done>Gap closure is regression-locked: web build compatibility and websocket lifecycle semantics are both enforced by automated checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client -> `/api/ws/chat` | Untrusted websocket payloads cross into route parsing and turn execution |
| feature boundary (web/default vs server) | Build-target differences can silently disable protections or break runtime paths |
| websocket stream -> UI transcript state | Transport faults can create inconsistent UI state if reset behavior regresses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.1-gap-01 | D | `src/server/ws.rs` feature boundary | mitigate | Guard server-only tokio/tracing runtime usage with `#[cfg(feature = "server")]` so web builds do not fail and operators can run UAT checks. |
| T-26.1-gap-02 | T | `warp_hermes.rs` protocol linkage | mitigate | Bind client serialization/parsing to `crate::protocol` as single source-of-truth; remove coupling to server module re-exports. |
| T-26.1-gap-03 | D | lifecycle regression test coverage | mitigate | Add branch-local static parity assertions for boundary anchors and keep lifecycle assertions for reconnect/reset behavior. |
</threat_model>

<verification>
- `cargo check -p iron_hermes_ui`
- `cargo check -p iron_hermes_ui --features server`
- `cargo test -p iron_hermes_ui --features server --test websocket_lifecycle_parity -- --nocapture`
- `cargo test -p iron_hermes_ui --features server --test server_runtime_parity -- --nocapture`
</verification>

<success_criteria>
- The exact HUMAN-UAT Gap 1 compile error condition is removed for default web builds.
- Server websocket route behavior remains buildable and regression-tested under `--features server`.
- Protocol and lifecycle boundaries are explicit, test-locked, and ready for rerunning manual websocket UAT steps.
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-fix-websocket-error-for-chat/26.1-02-SUMMARY.md`
</output>
