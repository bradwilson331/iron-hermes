---
phase: 26.1-fix-websocket-error-for-chat
verified: 2026-05-04T15:04:06Z
status: human_needed
score: 18/18 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 15/15
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Disconnect mid-stream in browser chat"
    expected: "Partial AI text remains visible, generic disconnect block appears, and next submit resumes on a new AI block"
    why_human: "Requires real websocket disruption timing and visual transcript continuity validation"
  - test: "Two rapid submits on same socket while first turn is active"
    expected: "Second submit surfaces in-transcript protocol error and first stream continues/terminates cleanly"
    why_human: "Needs interactive race timing and UI behavior verification beyond static/source tests"
---

# Phase 26.1: Fix websocket error for chat Verification Report

**Phase Goal:** Restore reliable Dioxus chat-to-LLM streaming by fixing the WebSocket server route and connection lifecycle so `/api/ws/chat` no longer resets without a closing handshake.
**Verified:** 2026-05-04T15:04:06Z
**Status:** human_needed
**Re-verification:** Yes — refresh after gaps-only execution

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | D-01: Per request, run the agent turn in a spawned task and forward websocket events concurrently while the turn is running. | ✓ VERIFIED | `tokio::spawn` turn task + concurrent `tokio::select!` forwarding loop (`crates/iron_hermes_ui/src/server/ws.rs:35`, `:80`, `:138-143`). |
| 2 | D-02: Enforce one in-flight turn per socket; if a second request arrives while one is active, return `ChatStreamEvent::Error` and do not queue/cancel-switch. | ✓ VERIFIED | Busy guard returns `ChatStreamEvent::Error` and `continue` while first turn remains in flight (`ws.rs:65-73`). |
| 3 | D-03: Keep the in-flight gate as explicit local lifecycle state in `ws_chat` rather than inside `run_web_turn`. | ✓ VERIFIED | Local `InFlightTurn` + `Option<InFlightTurn>` in `ws_chat`; `run_web_turn` remains a plain execution API (`ws.rs:26-33`, `state.rs:135-142`). |
| 4 | D-04: Terminal event ownership remains in the spawned turn task, emitting exactly one terminal event (`Finished` or `Error`) before sender close. | ✓ VERIFIED | Spawned task sends exactly one terminal event in `match result` and then exits (sender dropped) (`ws.rs:117-129`). |
| 5 | D-05: On websocket send failure during forwarding, abort the in-flight turn task immediately and exit the connection loop. | ✓ VERIFIED | Send failure path aborts handle and breaks outer loop (`ws.rs:147-153`). |
| 6 | D-06: Do not attempt explicit close-frame writes on broken send paths; end handler cleanly. | ✓ VERIFIED | Broken-send branch performs abort + break only; no close-frame write call present (`ws.rs:147-153`). |
| 7 | D-07: Emit warn-level telemetry for disconnect aborts including session id, reason, and in-flight state. | ✓ VERIFIED | `warn!` includes `session_id`, `reason`, `in_flight` on recv and send failure paths (`ws.rs:44`, `:149`). |
| 8 | D-08: Malformed request payloads remain recoverable per-frame errors by sending `ChatStreamEvent::Error` and continuing to receive. | ✓ VERIFIED | JSON parse failure sends error event then `continue` to next frame (`ws.rs:52-62`). |
| 9 | D-09: Replace one-shot receive behavior with a reconnect lifecycle that re-enters connect/receive after transient failures. | ✓ VERIFIED | Outer reconnect loop (`loop { ws.connect().await ... }`) with inner recv loop that breaks back to reconnect (`warp_hermes.rs:83-85`, `:117-118`, `:279-309`). |
| 10 | D-10: Use `with_automatic_reconnect()` only for reconnect behavior; no custom app-level backoff loop. | ✓ VERIFIED | WebSocket initialized with `with_automatic_reconnect()`; no sleep/backoff loop introduced (`warp_hermes.rs:70-74`). |
| 11 | D-11: On disconnect boundaries, reset only transient stream state: `scanner_active = false` and `streaming_block_id = None`. | ✓ VERIFIED | Both connect-failure and recv-failure branches reset exactly those transient fields (`warp_hermes.rs:93-94`, `:287-288`). |
| 12 | D-12: Do not auto-resubmit the last request after reconnect. | ✓ VERIFIED | Sends occur only in user-triggered submit/rerun handlers (`warp_hermes.rs:458-488`, `:492-576`); reconnect loop has no resend queue. |
| 13 | D-13: Preserve already-rendered partial AI text when disconnect happens mid-stream. | ✓ VERIFIED | Disconnect handling resets stream pointer and scanner, but does not clear existing AI blocks (`warp_hermes.rs:287-303`). |
| 14 | D-14: After reconnect, new submits must start a new AI block and never append into interrupted partial blocks. | ✓ VERIFIED | `submit` and `on_rerun` both set `streaming_block_id.set(None)` before send (`warp_hermes.rs:477`, `:504`). |
| 15 | D-15: Show a transcript-visible, generic disconnect error block while keeping technical details in logs only. | ✓ VERIFIED | Logs include technical details; transcript gets generic user-facing block message (`warp_hermes.rs:87-91`, `:102-109`, `:281-285`, `:296-303`). |
| 16 | Default web build compiles without pulling server-only websocket dependencies (closes UAT Gap 1). | ✓ VERIFIED | `cargo check -p iron_hermes_ui` passes; server-only imports are cfg-gated and non-server branch avoids tokio/tracing symbols (`ws.rs:5-10`, `:168-177`). |
| 17 | Server-feature websocket route still compiles and lifecycle parity tests keep passing after boundary hardening. | ✓ VERIFIED | `cargo check -p iron_hermes_ui --features server` and both parity suites pass (websocket + server runtime, 3/3 each). |
| 18 | Client websocket send/receive paths continue using shared protocol types while reconnect behavior from D-09..D-15 remains intact. | ✓ VERIFIED | Client parses/sends `crate::protocol::{ChatRequest, ChatStreamEvent}` while reconnect/reset logic remains (`warp_hermes.rs:132`, `:139`, `:480-484`, `:568-571`). |

**Score:** 18/18 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `crates/iron_hermes_ui/src/server/ws.rs` | Feature-safe websocket route module with concurrent forwarding + clean disconnect handling | ✓ VERIFIED | Exists, substantive (181 lines), and wired to route + `run_web_turn` path with cfg-boundary branch. |
| `crates/iron_hermes_ui/src/components/warp_hermes.rs` | Reconnect-resilient websocket client using shared protocol types | ✓ VERIFIED | Exists, substantive (708 lines), wired as root UI via `App` and actively drives send/recv lifecycle. |
| `crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs` | Regression tests for lifecycle + feature-boundary invariants | ✓ VERIFIED | Exists, substantive (90 lines), and executed/passing in spot checks. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `crates/iron_hermes_ui/src/components/warp_hermes.rs` | `crates/iron_hermes_ui/src/server/ws.rs` | ChatRequest JSON over `/api/ws/chat` | WIRED | `use_websocket` calls `ws_chat`, and submit/rerun paths send serialized chat requests (`warp_hermes.rs:70-74`, `:480-487`, `:568-575`). |
| `crates/iron_hermes_ui/src/server/ws.rs` | `crates/iron_hermes_ui/src/server/state.rs::run_web_turn` | stream/tool callbacks forwarding ChatStreamEvent | WIRED | `run_web_turn` invoked from spawned task and callback events forwarded via channel (`ws.rs:80-115`, `:138-146`). |
| `crates/iron_hermes_ui/src/components/warp_hermes.rs` | `crates/iron_hermes_ui/src/protocol.rs` | ChatRequest/ChatStreamEvent serialization for ws.send_raw/recv_raw | WIRED | Explicit `crate::protocol::*` paths for receive parsing and send payload creation (`warp_hermes.rs:132`, `:139`, `:480`, `:568`). |
| `crates/iron_hermes_ui/src/server/ws.rs` | `crates/iron_hermes_ui/src/server/state.rs::run_web_turn` | server-feature-gated turn execution path | WIRED | `#[cfg(feature = "server")]` wraps runtime path and includes `run_web_turn` call (`ws.rs:24-25`, `:107-115`). |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `src/server/ws.rs` | `maybe_event` (`ChatStreamEvent`) | `run_web_turn` stream/tool callbacks + terminal mapping | Yes — upstream `run_web_turn` executes `agent.run(messages).await` and callbacks emit live deltas/tool events (`state.rs:143-149`, `ws.rs:82-105`) | ✓ FLOWING |
| `src/components/warp_hermes.rs` | `blocks/messages/tokens` | `ws.recv_raw()` parsed into `ChatStreamEvent` | Yes — event arms mutate rendered transcript, tool panel, and token budget (`warp_hermes.rs:138-277`) | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Default web build compiles (gap closure target) | `cargo check -p iron_hermes_ui` | Exit 0 | ✓ PASS |
| Server-feature websocket route compiles | `cargo check -p iron_hermes_ui --features server` | Exit 0 | ✓ PASS |
| Lifecycle + boundary parity checks | `cargo test -p iron_hermes_ui --features server --test websocket_lifecycle_parity -- --nocapture` | `3 passed; 0 failed` | ✓ PASS |
| Existing server runtime parity still holds | `cargo test -p iron_hermes_ui --features server --test server_runtime_parity -- --nocapture` | `3 passed; 0 failed` | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| *(none declared)* | `26.1-01-PLAN.md` | Plan frontmatter `requirements: []` | ✓ SATISFIED | No IDs to map against `REQUIREMENTS.md`. |
| *(none declared)* | `26.1-02-PLAN.md` | Plan frontmatter `requirements: []` | ✓ SATISFIED | No IDs to map against `REQUIREMENTS.md`. |

Orphaned requirements check: no `REQUIREMENTS.md` traceability entries map to Phase 26.1.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `crates/iron_hermes_ui/Cargo.toml` | 56 | `demo = []` (empty-array regex hit) | ℹ️ Info | Legitimate Cargo feature declaration; not a stub. No blocker/warning anti-patterns found in phase files. |

### Human Verification Required

### 1. Mid-stream disconnect UX continuity

**Test:** Start a long streaming response in web chat, interrupt websocket/network mid-response, reconnect, then submit a new prompt.
**Expected:** Partial AI text remains visible, one generic disconnect block appears, and next prompt starts a fresh AI block.
**Why human:** Requires live transport interruption timing and visual transcript continuity checks.

### 2. In-flight double-submit race behavior

**Test:** While one websocket turn is actively streaming, submit a second message quickly on the same socket.
**Expected:** Second request surfaces protocol error (busy/in-progress), with no queued shadow execution and no crash.
**Why human:** Race timing and UX outcome need interactive validation beyond static/source tests.

### Gaps Summary

No code-level gaps found against merged phase must-haves (Plans 01 + 02). Phase remains `human_needed` until live reconnect/race UX checks are completed.

---

_Verified: 2026-05-04T15:04:06Z_
_Verifier: the agent (gsd-verifier)_
