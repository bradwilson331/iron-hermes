---
phase: 36.3.7.2-tool-schema-compatibility-drop-top-level-oneof-from-delegate
verified: 2026-05-29
status: passed
verdict: PASS-WITH-NOTES
score: 5/5 specific gates verified
must_haves_total: 10
must_haves_passed: 10
verifier_methodology: goal-backward + bilateral-tracing (LEARNINGS 2026-05-29)
notes:
  - "Pre-existing failure in crates/ironhermes-cli/tests/invariants_26_4_1_cfg_03.rs::phase_amendment_doc_comment_present caused by Phase 36.3.7.0 commit c453411f (rewrote a comment block this CFG-03 invariant test was checking). Phase 36.3.7.2 did NOT touch ironhermes-cli — this is a pre-existing regression that must be addressed in 36.3.7.0 follow-up (likely 36.3.7.1)."
  - "crates/ironagent-tools-api/ is a stale byte-for-byte duplicate of crates/ironhermes-tools/ and is excluded from the workspace (per Cargo.toml comment lines 'crates/ironagent-tools-api is a stale byte-for-byte duplicate'). It is NOT a real consumer of delegate_task.rs. Confirmed by reading the workspace exclude list."
forward_notes_for_36_3_7_1:
  - "ironhermes-cli test failure: phase_amendment_doc_comment_present — root cause is commit c453411f which rewrote the comment block in crates/ironhermes-cli/src/main.rs preceding the run_preflight gate. The CFG-03 invariant test scans the 1500 chars BEFORE the gate for 'Phase 26.4.1' or 'CFG-03'; commit c453411f's new comment block buries those tokens further back. Fix EITHER by updating the invariant test's grace window or by adding a CFG-03 amendment doc comment immediately above the new run_preflight match. Out of scope for 36.3.7.2; surface as 36.3.7.1 candidate."
gates_checked:
  gate_1_no_top_level_combinator_in_producer:
    truth: "delegate_task::schema() returns parameters Value with NO top-level oneOf/allOf/anyOf"
    status: verified
    evidence:
      - "grep -c '\"oneOf\":' crates/ironhermes-tools/src/delegate_task.rs → 0"
      - "grep -c '\"allOf\":' crates/ironhermes-tools/src/delegate_task.rs → 0"
      - "grep -c '\"anyOf\":' crates/ironhermes-tools/src/delegate_task.rs → 0"
      - "Source read: lines 670-732 show flat schema — properties block followed directly by 'required: []' at line 731; no top-level combinator key"
      - "Comment block at lines 733-737 explicitly documents BUG-IRONHERMES-TOOLS-SCHEMA-COMPAT-01 and explains the runtime+prose mutex fallback"
  gate_2_mutex_prose_in_task_and_tasks_descriptions:
    truth: "task and tasks property descriptions both contain 'Mutually exclusive'"
    status: verified
    evidence:
      - "grep -c 'Mutually exclusive' crates/ironhermes-tools/src/delegate_task.rs → 4 (≥4 expected per planner note)"
      - "Line 675 (task property description): '...Mutually exclusive with `tasks` — provide exactly one of `task` (single mode) or `tasks` (batch mode).'"
      - "Line 702 (tasks property description): '...Mutually exclusive with `task` — provide exactly one of `tasks` (batch mode) or `task` (single mode).'"
      - "Two more occurrences in the unit-test body's assertion messages — does not undercut the must_have because Anthropic sees only the rendered schema, where the prose is at lines 675 and 702"
  gate_3_receiver_end_test_exists_and_iterates_registry:
    truth: "tests/no_top_level_schema_combinators.rs exists, iterates ToolRegistry::register_defaults() + get_definitions(None), asserts no top-level combinator on every registered tool, names offending tool in failure message"
    status: verified
    evidence:
      - "File exists: crates/ironhermes-tools/tests/no_top_level_schema_combinators.rs (56 lines, 2170 bytes)"
      - "Uses ToolRegistry::register_defaults() at line 25 — registry walk, not hardcoded list (auto-covers future tools)"
      - "Uses get_definitions(None) at line 27"
      - "Iterates `for schema in &definitions` and checks all 3 forbidden_keys (lines 34, 37, 40)"
      - "Failure message formatted as 'tool `{tool_name}` has top-level `{key}` in input_schema' (lines 42-44) — names offending tool exactly as CONTEXT.md requires"
      - "Precondition guard at line 28-32 asserts !definitions.is_empty() (threat T-36.3.7.2-02-02 — prevents silent pass via empty registry)"
      - "cargo test -p ironhermes-tools --test no_top_level_schema_combinators → 1 passed, 0 failed (exit 0)"
  gate_4_inverted_unit_test_in_delegate_task_rs:
    truth: "delegate_task::tests::test_delegate_task_schema_has_no_top_level_boolean_combinator asserts absence of oneOf/allOf/anyOf + mutex prose present in both property descriptions"
    status: verified
    evidence:
      - "Source read: crates/ironhermes-tools/src/delegate_task.rs lines 1217-1250"
      - "Test renamed from test_delegate_task_schema_task_and_tasks_are_mutually_exclusive_optional → test_delegate_task_schema_has_no_top_level_boolean_combinator (matches plan)"
      - "Line 1228: params.get('oneOf').is_none() assertion"
      - "Line 1232: params.get('allOf').is_none() assertion"
      - "Line 1236: params.get('anyOf').is_none() assertion"
      - "Lines 1241-1243: properties existence assertions for task and tasks"
      - "Lines 1245-1249: prose-signal assertions — task_desc and tasks_desc must contain 'Mutually exclusive'"
      - "Test ran as part of cargo test -p ironhermes-tools --lib (357 passed, 0 failed)"
  gate_5_runtime_mutex_check_preserved:
    truth: "execute() still rejects calls with neither task nor tasks"
    status: verified
    evidence:
      - "Source read: crates/ironhermes-tools/src/delegate_task.rs lines 741-745 — runtime check unchanged, bails with 'Either task (single mode) or tasks (batch mode) is required'"
      - "Test test_delegate_task_execute_rejects_when_neither_task_nor_tasks_provided at lines 1252-1261 unchanged and passing"
      - "Test ran as part of cargo test -p ironhermes-tools --lib (357 passed, 0 failed)"
bilateral_tracing:
  producer:
    file: "crates/ironhermes-tools/src/delegate_task.rs"
    lines_produced:
      - "schema() at lines 661-739 — emits flat input_schema with no top-level boolean combinator"
      - "execute() at lines 741-745 — runtime mutex enforcement (load-bearing correctness layer)"
  consumers_in_tree:
    - file: "crates/ironhermes-tools/src/delegate_task.rs"
      lines: [1226, 1267, 1541, 2022, 2088]
      accesses:
        - "Line 1226: &schema.function.parameters (the new inverted test — reads top-level keys, mutex-prose-aware)"
        - "Line 1267: &schema.function.parameters['properties'] (test_delegate_task_schema_has_allowed_tools — accesses properties only)"
        - "Lines 1541, 2022, 2088: &schema.function.parameters['properties'] (other tests — properties access only, do not touch top-level keys)"
      compatibility: "All compatible with new shape — none asserted on oneOf/allOf/anyOf, none unwrap 'required'"
    - file: "crates/ironhermes-tools/tests/delegate_task_timeout_cancel.rs"
      lines: [213, 224]
      accesses:
        - "Line 213: &schema.function.parameters['properties'] (schema_exposes_timeout_seconds_field — properties read only)"
        - "Line 224: schema.function.parameters['required'].as_array().unwrap() — THE PREVIOUSLY-BROKEN CONSUMER"
      regression_history: "Plan 01's initial implementation dropped the entire required key from the schema. Commit 1448c441 ('fix(36.3.7.2): restore required: [] in delegate_task schema') restored 'required': [] at delegate_task.rs:731. Verified at line 224 the consumer still calls .as_array().unwrap() which now succeeds against an empty array."
      compatibility: "Restored to compatibility by commit 1448c441; cargo test -p ironhermes-tools --test delegate_task_timeout_cancel → 3 passed, 0 failed"
    - file: "crates/ironhermes-tools/tests/no_top_level_schema_combinators.rs"
      lines: [39]
      accesses: ["&schema.function.parameters — reads top-level keys to assert absence of combinators"]
      compatibility: "By construction compatible with the new flat shape"
    - file: "crates/ironhermes-tools/src/registry.rs"
      lines: [2013, 2045, 2429]
      accesses:
        - "Line 2013: serde_json::to_value(&schema.function.parameters).unwrap() — todo_write_schema test"
        - "Line 2045: serde_json::to_value(&schema.function.parameters).unwrap() — todo_read_schema test"
        - "Line 2429: &close.function.parameters['type'/'properties'/'required'] — browser_close test, uses .is_array() (not .as_array().unwrap()), tolerates empty required"
      compatibility: "All three are tests of OTHER tools (todo_write, todo_read, browser_close), not delegate_task. They are decoupled from delegate_task schema shape and remained green (full lib suite: 357 passed, 0 failed)."
    - file: "crates/ironhermes-tools/src/browser_back.rs"
      lines: [128]
      accesses: ["s.function.parameters — moves the value, does not key into it"]
      compatibility: "Decoupled from delegate_task; no top-level key access"
  consumers_cross_crate:
    - file: "crates/ironhermes-agent/src/anthropic_client.rs"
      lines: [574]
      accesses: ["t.function.parameters.clone() — passes the full Value to Anthropic as input_schema"]
      compatibility: "THIS IS THE BILATERAL-RECEIVER — Anthropic itself is the consumer that rejected the old shape. Its acceptance criteria is encoded by Plan 02's no_top_level_schema_combinators integration test (and ultimately by re-running UAT-09-A, deferred to a later phase per CONTEXT.md verification_targets)."
  consumers_excluded_from_workspace:
    - file: "crates/ironagent-tools-api/src/delegate_task.rs"
      lines: [1077, 1109, 1381, 1856, 1922]
      accesses: ["schema.function.parameters and parameters['properties'] in delegate_task duplicate"]
      compatibility: "NOT IN WORKSPACE — Cargo.toml exclude list explicitly marks this crate as 'stale byte-for-byte duplicate' of ironhermes-tools, with test files that import the sibling crate. Not built, not tested, not shipped. Confirmed by grep -B2 -A8 'ironagent-tools-api' Cargo.toml. No action required for this phase."
    - file: "crates/ironagent-tools-api/tests/delegate_task_timeout_cancel.rs"
      lines: [213, 224]
      accesses: ["parameters['properties'] and parameters['required'].as_array().unwrap()"]
      compatibility: "NOT IN WORKSPACE — same as above. Note: this duplicate's delegate_task.rs:614 ALSO has 'required': [] (confirmed by grep), so even if the crate were re-enabled, this consumer would still succeed."
build_test_gates:
  cargo_build_workspace: "asserted PASS by user (exit 0) — re-verification not run because the user already confirmed"
  cargo_test_ironhermes_tools: "PASS — 395 tests total across 13 test binaries (357 lib + 38 integration), 0 failed. See /private/tmp/.../b5giwxr7k.output for all 'test result: ok' lines."
  cargo_test_delegate_task_timeout_cancel: "PASS — 3 passed, 0 failed (exit 0). All three tests including schema_exposes_timeout_seconds_field that calls parameters['required'].as_array().unwrap()."
  cargo_test_no_top_level_schema_combinators: "PASS — 1 passed, 0 failed (exit 0). The receiver-end regression lock from Plan 02."
  cargo_test_ironhermes_cli_ironhermes_core: "PASS-WITH-PRE-EXISTING-FAIL — most binaries green, but ironhermes-cli/tests/invariants_26_4_1_cfg_03.rs::phase_amendment_doc_comment_present FAILS. Root cause: commit c453411f (Phase 36.3.7.0 Plan 05) rewrote the comment block in main.rs preceding the run_preflight gate, and the CFG-03 invariant test scans the 1500 chars before the gate for 'Phase 26.4.1' / 'CFG-03' tokens. The new comment block buried them further back than 1500 chars. Phase 36.3.7.2 did NOT touch crates/ironhermes-cli/ — confirmed by git log --oneline 36.3.7.2 -- crates/ironhermes-cli/tests/. This is a pre-existing regression from 36.3.7.0 and is OUT OF SCOPE for 36.3.7.2."
---

# Phase 36.3.7.2 Verification Report — Tool-schema compatibility (drop top-level oneOf from delegate_task)

**Verdict: PASS-WITH-NOTES**
**Verified:** 2026-05-29
**Methodology:** Goal-backward verification + bilateral-tracing (per LEARNINGS 2026-05-29 meta-rule)

## Phase Goal (verbatim from CONTEXT)

> Drop the top-level `oneOf` block in `delegate_task`'s `input_schema` so Anthropic accepts the tool definition. Preserve runtime mutual-exclusion of `task` vs `tasks` in `execute()`. Move the mutex signal to the LLM via property descriptions. Add a system-level receiver-end test that asserts no top-level `oneOf`/`allOf`/`anyOf` for EVERY tool in the registry — lock the rule for future tools. Invert the pre-existing `delegate_task::tests` unit test so it asserts the absence of the combinator (not its presence).

## Per-Gate Checklist

| Gate | Truth | Status | Evidence |
|------|-------|--------|----------|
| 1 | `delegate_task::schema()` returns parameters with NO top-level `oneOf`/`allOf`/`anyOf` | PASS | `grep -c '"oneOf":' crates/ironhermes-tools/src/delegate_task.rs` → `0`; same for `allOf` (0) and `anyOf` (0); source read at lines 670-732 confirms flat shape |
| 2 | `task` and `tasks` descriptions BOTH contain "Mutually exclusive" | PASS | `grep -c 'Mutually exclusive' crates/ironhermes-tools/src/delegate_task.rs` → `4` (≥2 required, 4 observed: 2 in descriptions, 2 in test assertion messages); lines 675 + 702 confirmed |
| 3 | `tests/no_top_level_schema_combinators.rs` exists, iterates registry, names offending tool | PASS | File exists (56 lines); uses `register_defaults()` + `get_definitions(None)`; failure format string includes `tool \`{tool_name}\``; `cargo test --test no_top_level_schema_combinators` → 1 passed |
| 4 | Existing test inverted to assert absence + mutex prose | PASS | Test renamed to `test_delegate_task_schema_has_no_top_level_boolean_combinator` (lines 1217-1250); three `is_none()` assertions on `oneOf`/`allOf`/`anyOf`; two `contains("Mutually exclusive")` assertions on descriptions |
| 5 | `execute()` still rejects neither-`task`-nor-`tasks` calls | PASS | Lines 741-745 unchanged; `test_delegate_task_execute_rejects_when_neither_task_nor_tasks_provided` at line 1252 still passes |

## Bilateral-Tracing Section

Per the meta-rule from `.planning/LEARNINGS.md` 2026-05-29: for every wire-up the phase touches, cite BOTH ends — PRODUCER and CONSUMER.

### Producer

- **File:** `crates/ironhermes-tools/src/delegate_task.rs`
- **schema() at lines 661-739:** emits a flat `input_schema` — top-level keys are `type`, `properties`, `required` (empty array). No `oneOf`/`allOf`/`anyOf`. Mutex prose embedded in `task` (line 675) and `tasks` (line 702) descriptions.
- **execute() at lines 741-745:** load-bearing runtime mutex check; preserved verbatim from pre-phase state.

### Consumers in `crates/ironhermes-tools/` (all compatible)

| Consumer | Lines | Access pattern | Compatibility |
|----------|-------|----------------|---------------|
| `src/delegate_task.rs` (inverted unit test) | 1226 | `&schema.function.parameters` — top-level key reads | Compatible — asserts absence of combinators |
| `src/delegate_task.rs` (other in-file tests) | 1267, 1541, 2022, 2088 | `&schema.function.parameters["properties"]` | Compatible — only reads properties, never `required` |
| `tests/delegate_task_timeout_cancel.rs::schema_exposes_timeout_seconds_field` | 213, 224 | Line 213: `parameters["properties"]`; Line 224: `parameters["required"].as_array().unwrap()` | **PREVIOUSLY BROKEN** — Plan 01's initial implementation dropped the `required` key entirely, causing this consumer's `.as_array().unwrap()` to panic. **Fixed by commit `1448c441`** which restored `"required": []` at delegate_task.rs:731. Currently passing (3 passed, 0 failed) |
| `tests/no_top_level_schema_combinators.rs` (new Plan 02 lock) | 39 | `&schema.function.parameters` — top-level key reads | Compatible by construction |
| `src/registry.rs` (todo_write/todo_read tests) | 2013, 2045 | `serde_json::to_value(&schema.function.parameters)` then `params["required"].as_array().unwrap()` (for todo_write only) | Compatible — these test OTHER tools (todo_write, todo_read), not delegate_task; their schemas remain unchanged; full lib suite green |
| `src/registry.rs` (browser_close test) | 2429 | `&close.function.parameters["type"/"properties"/"required"]` with `.is_array()` (not `.as_array().unwrap()`) | Compatible — tolerates empty required; tests browser_close, not delegate_task |
| `src/browser_back.rs` | 128 | `s.function.parameters` (move, no key access) | Decoupled |

### Cross-crate consumer (the actual bilateral receiver)

| Consumer | Lines | Access pattern | Compatibility |
|----------|-------|----------------|---------------|
| `crates/ironhermes-agent/src/anthropic_client.rs::adapt_tools` | 574 | `t.function.parameters.clone()` — sent as Anthropic `input_schema` | This is the BILATERAL RECEIVER. Anthropic's tool API rejected the old shape with `400: input_schema does not support oneOf, allOf, or anyOf at the top level`. The new flat shape is what Anthropic expects. End-to-end UAT re-validation deferred to a later phase per CONTEXT.md verification_targets ("UAT re-validation (deferred to a separate phase): re-run UAT-09-A from 36.3.7.0 against this fix's binary; expect stage 7 to PASS"). The Plan 02 integration test is the receiver-end regression lock that proves the producer side. |

### Consumers excluded from workspace (informational only)

`crates/ironagent-tools-api/` is a stale byte-for-byte duplicate of `crates/ironhermes-tools/` and is **excluded from the workspace** (Cargo.toml `exclude = ["crates/ironagent-tools-api"]` with comment: "stale byte-for-byte duplicate of `crates/ironhermes-tools`...Nothing in the workspace depends on it. Excluded pending removal."). Its `delegate_task.rs` consumers (lines 1077, 1109, 1381, 1856, 1922) and its `tests/delegate_task_timeout_cancel.rs:224` (which uses the same `as_array().unwrap()` pattern) are not built and not tested. For the record: the duplicate's `src/delegate_task.rs:614` already has `"required": []`, so even if the crate were re-enabled, the consumer pattern would not regress.

### Audit of all `"oneOf"`/`"allOf"`/`"anyOf"` hits in tree (post-fix)

```
$ grep -rnE '"oneOf"|"allOf"|"anyOf"' crates/ironhermes-tools/src/
crates/ironhermes-tools/src/delegate_task.rs:1229:            params.get("oneOf").is_none(),
crates/ironhermes-tools/src/delegate_task.rs:1233:            params.get("allOf").is_none(),
crates/ironhermes-tools/src/delegate_task.rs:1237:            params.get("anyOf").is_none(),
```

All three remaining hits are **negative assertions** in the inverted unit test. No producer-side hits remain.

## Build / Test Gates

| Gate | Status | Evidence |
|------|--------|----------|
| `cargo build --workspace` | PASS (asserted by user, not re-run) | User confirmed in verification request |
| `cargo test -p ironhermes-tools --no-fail-fast` | PASS | 395 tests across 13 binaries, 0 failed. Tail of output: `test result: ok. 357 passed; 0 failed` (lib); `3 passed` (delegate_task_timeout_cancel); `1 passed` (no_top_level_schema_combinators); `9 passed` (web_extract_integration); etc. |
| `cargo test -p ironhermes-tools --test delegate_task_timeout_cancel` | PASS | `running 3 tests / test schema_exposes_timeout_seconds_field ... ok / test timeout_seconds_per_call_overrides_config_default ... ok / test timeout_arm_hard_cancels_child_token_within_grace ... ok / test result: ok. 3 passed; 0 failed` |
| `cargo test -p ironhermes-tools --test no_top_level_schema_combinators` | PASS | `running 1 test / test no_default_tool_has_top_level_schema_combinator ... ok / test result: ok. 1 passed; 0 failed` |
| `cargo test -p ironhermes-cli -p ironhermes-core --no-fail-fast` | PASS-WITH-PRE-EXISTING-FAIL — see Forward Notes | Most binaries green; one binary fails: `ironhermes-cli/tests/invariants_26_4_1_cfg_03.rs::phase_amendment_doc_comment_present`. This failure is **NOT introduced by Phase 36.3.7.2** — it was introduced by Phase 36.3.7.0 commit `c453411f` which rewrote a comment block in `main.rs` that the CFG-03 invariant test was scanning. Phase 36.3.7.2 did not touch `crates/ironhermes-cli/` (`git log --oneline 36.3.7.2 -- crates/ironhermes-cli/tests/invariants_26_4_1_cfg_03.rs` returns empty). |

## Forward Notes for Phase 36.3.7.1 (or other follow-up)

**Pre-existing failure to address (not 36.3.7.2's responsibility):**

- **Test:** `crates/ironhermes-cli/tests/invariants_26_4_1_cfg_03.rs::phase_amendment_doc_comment_present`
- **Failure:** "Phase 26.4.1 CFG-03: expected an amendment doc comment referencing 'Phase 26.4.1' or 'CFG-03' within the 1500 chars preceding the widened gate."
- **Root cause:** Phase 36.3.7.0 Plan 05 commit `c453411f` ("fix(36.3.7.0-05): preflight gate skips chat -q non-interactive path (closes BUG-36.3.7-04)") added a long comment block to `crates/ironhermes-cli/src/main.rs` immediately above the `run_preflight` gate that documents the BUG-36.3.7-04 amendment. The CFG-03 invariant test scans the 1500 chars BEFORE the gate for the tokens `Phase 26.4.1` or `CFG-03`; the new block pushed the original CFG-03 amendment doc-comment out of the 1500-char window.
- **Fix options for the follow-up phase:**
  1. Add a one-line reference to `CFG-03` or `Phase 26.4.1` inside the new BUG-36.3.7-04 comment block (cheapest).
  2. Widen the invariant test's 1500-char grace window to ~3000 chars.
  3. Restructure the comment block above `run_preflight` so the original CFG-03 amendment doc-comment is preserved.
- **Out of scope here:** Phase 36.3.7.2 modified zero files outside `crates/ironhermes-tools/`. Do NOT expand 36.3.7.2 scope to fix this — it predates the phase.

## Summary

Phase 36.3.7.2 achieves its stated goal. All 5 specific gates from the CONTEXT verification targets PASS. The bilateral-tracing rule was applied to every consumer of `delegate_task` schema shape:

- One previously-broken consumer (`tests/delegate_task_timeout_cancel.rs:224`) was identified, fixed by post-merge commit `1448c441`, and re-verified green.
- All other in-tree consumers were traced and found compatible.
- The cross-crate consumer (`anthropic_client.rs::adapt_tools`) is the bilateral RECEIVER that motivated this phase; Plan 02's `no_top_level_schema_combinators.rs` is the receiver-end regression lock that ensures no future tool re-introduces the bug.
- The `crates/ironagent-tools-api/` duplicate is excluded from the workspace and is not a real consumer.

**Verdict: PASS-WITH-NOTES** — the "WITH-NOTES" is about the pre-existing `phase_amendment_doc_comment_present` failure in `ironhermes-cli` which was inherited from Phase 36.3.7.0 and is out of scope for 36.3.7.2.

---

_Verified: 2026-05-29_
_Verifier: Claude (gsd-verifier)_
