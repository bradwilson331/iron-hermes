---
phase: 26.7.3
slug: skills-page-enable-tab-search-and-toggle-on-off-features
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-19
revised: 2026-05-19
---

# Phase 26.7.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | cargo test (Rust workspace) |
| **Config file** | Cargo.toml (workspace standard — no extra config) |
| **Quick run command** | `cargo test -p ironhermes-core 2>&1 \| tail -5` |
| **Full suite command** | `cargo test --workspace 2>&1 \| tail -20` |
| **Estimated runtime** | ~30–60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-core 2>&1 | tail -5`
- **After every plan wave:** Run `cargo test --workspace 2>&1 | tail -20`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 26.7.3-01-01 | 01 | 1 | D-06 (disabled field) | T-26.7.3-01-01 | `serde(default)` prevents parse failure on existing YAML without `disabled` key | unit | `cargo test -p ironhermes-core --lib skills_config 2>&1 \| tail -5` | ✅ | ⬜ pending |
| 26.7.3-01-02 | 01 | 1 | D-04/D-05 (search filter) | T-26.7.3-01-03 | `search_matches` — case-insensitive substring on name+description; empty query passes everything; category not searched | unit | `cargo test -p ironhermes-core --lib skills_filter 2>&1 \| tail -10` | ✅ (Plan 01 Task 2) | ⬜ pending |
| 26.7.3-01-03 | 01 | 1 | D-01/D-03 (tab filter, live counts) | T-26.7.3-01-04 | `tab_predicate` — all/bundled/installed/enabled produce correct subsets; ENABLED uses server-sourced bool | unit | `cargo test -p ironhermes-core --lib tab_filter 2>&1 \| tail -10` | ✅ (Plan 01 Task 2) | ⬜ pending |
| 26.7.3-02-01 | 02 | 2 | D-07 (toggle server fn config write) | T-26.7.3-02-01 | `disabled` field toggled in config YAML on disk; no path traversal via skill name; round-trip semantics | unit | `cargo test -p iron_hermes_ui --lib toggle_skill 2>&1 \| tail -10` | ✅ (Plan 02) | ⬜ pending |
| 26.7.3-03-01 | 03 | 3 | D-01/D-04/D-05/D-09 (filter use_memo delegates to core) | T-26.7.3-03-02 | Filter logic delegates to `tab_predicate` + `search_matches` (covered by 26.7.3-01-02 and 26.7.3-01-03); UI wrapper compiles | compile + delegation | `cargo build -p iron_hermes_ui --features web 2>&1 \| tail -5 && cargo test -p ironhermes-core --lib tab_filter 2>&1 \| tail -3` | ✅ (Plan 03 Task 1) | ⬜ pending |
| 26.7.3-03-02 | 03 | 3 | D-07/D-08 (optimistic toggle, revert on error) | T-26.7.3-03-01, T-26.7.3-03-04 | SkillCard prop shape correct; `Signal<bool>` anti-pattern guarded by grep; spawn+revert pattern present | compile + structural grep | `cargo build -p iron_hermes_ui --features web 2>&1 \| tail -5 && cargo clippy -p iron_hermes_ui --features web 2>&1 \| grep -E 'generational_ref_held_across_await\|write_lock_held_across_await' \| wc -l` (must be 0) | ✅ (Plan 03 Task 2) | ⬜ pending |
| 26.7.3-04-01 | 04 | 4 | D-08 (optimistic UI + revert) | — | Error state reverts toggle and displays inline error in real browser | manual | Browser DevTools: disable network, click toggle, verify revert | N/A | ⬜ pending |

**Sampling continuity check:** No three consecutive implementation tasks are compile-only. The chain runs:
- 26.7.3-01-01 (behavioral unit) → 26.7.3-01-02 (behavioral unit) → 26.7.3-01-03 (behavioral unit) — Wave 1, all behavioral.
- 26.7.3-02-01 (behavioral unit) — Wave 2.
- 26.7.3-03-01 (compile + delegation to behavioral tests in 01-02 / 01-03) → 26.7.3-03-02 (compile + structural grep + clippy lint gate) — Wave 3. Plan 03 Task 1's `<verify>` re-runs the Plan 01 Task 2 behavioral tests after the UI build, so the wire-up is exercised against the same authoritative coverage on every Task 1 commit. The two compile-only verifies in Plan 03 are bracketed by behavioral coverage upstream (Plan 01 Task 2) and downstream UAT (Plan 04 Task 2) — no 3-in-a-row violation.
- 26.7.3-04-01 (manual UAT) — Wave 4.

---

## Wave 0 Requirements

All Wave 0 requirements are satisfied by Wave 1 Plan 01 (Tasks 1 + 2) — Wave 1 starts with test-creation tasks rather than separating into a distinct Wave 0:

- [x] `crates/ironhermes-core/src/config.rs` — `SkillsConfig` deserialization tests with and without `disabled` key (covers `serde(default)` invariant) → Plan 01 Task 1 creates `skills_config_default_has_empty_disabled`, `skills_config_disabled_field_round_trip`, `skills_config_missing_disabled_key_defaults_empty`.
- [x] `crates/ironhermes-core/src/skills.rs` — pure-function filter predicates (`tab_predicate`, `search_matches`) with full behavioral unit test coverage (tab predicates: all/bundled/installed/enabled/unknown; search: empty/name/description/case-insensitive/no-match/category-excluded) → Plan 01 Task 2 creates `mod tab_filter` (≥5 tests) and `mod skills_filter` (≥6 tests).
- [x] `crates/iron_hermes_ui/src/server/api.rs` — `toggle_skill` config round-trip via the `apply_disable_toggle` pure helper: add name to disabled, remove name from disabled, round-trip → Plan 02 Task 1 creates `mod toggle_skill_tests` (≥2 tests).

The pure helpers from Plan 01 Task 2 (`tab_predicate`, `search_matches`) are imported by Plan 03 Task 1 inside the `use_memo` closure (`use ironhermes_core::skills::{tab_predicate, search_matches};`), so the UI thin-wraps a tested core implementation. There is no separate `cargo test -p ironhermes-core skills_filter` or `tab_filter` row that lacks an implementing plan — both rows now point to Plan 01 Task 2.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Optimistic toggle revert on server error | D-08 | Requires network interception to simulate server failure | Open DevTools → Network → block `/api/skills/*/toggle` → click toggle → verify revert + inline error |
| UPDATES tab is visually disabled and non-interactive | D-02 | CSS/pointer-events state requires browser rendering | Open Skills page → verify UPDATES tab is grayed, count is 0, click does nothing |
| Tab + search AND composition (visual confirmation) | D-09 | Pure-function predicates are unit-tested in 26.7.3-01-02 / 26.7.3-01-03, but live filtering in browser requires reactive state validation | Set tab to BUNDLED, type search query, verify only bundled skills matching query appear |
| Sub-copy `{count} loaded · {enabled_count} enabled` updates on toggle | Copywriting Contract | Requires reactive state validation in running app | Toggle a skill, verify the sub-copy counts update accordingly |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — confirmed: every row in the Per-Task Verification Map has either a behavioral or compile+grep automated command; manual row 26.7.3-04-01 is bracketed by upstream automated coverage.
- [x] Sampling continuity: no 3 consecutive tasks without automated verify — confirmed via the "Sampling continuity check" annotation above. Plan 03's two compile-bracketed tasks delegate filter coverage to Plan 01 Task 2 behavioral tests, which are re-run by Plan 03 Task 1's verify command.
- [x] Wave 0 covers all MISSING references — confirmed: the previously MISSING `skills_filter` and `tab_filter` rows are now mapped to Plan 01 Task 2, which creates `pub fn tab_predicate` + `pub fn search_matches` in `ironhermes-core::skills` and their test modules.
- [x] No watch-mode flags — confirmed (all commands are single-run).
- [x] Feedback latency < 60s — confirmed (the heaviest command `cargo test --workspace` budgets ~30-60s).
- [x] `nyquist_compliant: true` set in frontmatter — confirmed.

**Approval:** revised — ready for execution.
</content>
</invoke>