---
phase: 26.7.3
plan: 03
type: execute
wave: 3
depends_on: ["26.7.3-02"]
files_modified:
  - crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs
autonomous: true
requirements: []
tags: [ui, dioxus, skills, screen]
must_haves:
  truths:
    - "Clicking ALL tab shows all skills (no filter); clicking BUNDLED shows only skills where category == \"bundled\"; clicking INSTALLED shows only skills where category != \"bundled\"; clicking ENABLED shows only skills where the server-sourced enabled is true"
    - "Each tab label includes a live count in the format \"TAB · {N}\" where N is computed from skills_list (the unfiltered server response)"
    - "The UPDATES tab renders with grayed appearance, count of 0, and is not clickable (disabled attribute or pointer-events:none)"
    - "Typing in the search input narrows the displayed cards to those whose name OR description contains the query as a case-insensitive substring"
    - "Tab filter and search query are AND-combined; switching tabs does not clear search; typing does not reset tab"
    - "When the filtered+searched result is empty AND skills_list is non-empty, the grid renders the inline text \"No skills match.\" (matching the inline error pattern)"
    - "Clicking the toggle on a card immediately flips its visual state (optimistic UI), spawns toggle_skill in the background, and on Err reverts the visual state AND shows the inline text \"Toggle failed — try again.\" inside the card"
    - "The filter predicate inside the use_memo closure is implemented by calling ironhermes_core::skills::tab_predicate and ironhermes_core::skills::search_matches (both added in Plan 01 Task 2) — the UI is a thin wrapper, not a re-implementation"
  artifacts:
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs"
      provides: "ScreenSkills with active_tab + search_query + toggle_states + toggle_errors signals, filtered_skills memo, and live tab counts"
      contains: "use_signal(|| \"all\")"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs"
      provides: "SkillCard with optimistic enabled prop (plain bool, NOT Signal<bool>), error_msg prop, on_toggle EventHandler"
      contains: "on_toggle: EventHandler<()>"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs"
      provides: "use_effect that seeds toggle_states from skills_list on first non-empty load"
      contains: "use_effect"
  key_links:
    - from: "SkillCard onclick on .tgl div"
      to: "on_toggle EventHandler"
      via: "EventHandler<()> call"
      pattern: "onclick:\\s*move\\s*\\|_\\|\\s*on_toggle\\.call"
    - from: "ScreenSkills on_toggle closure"
      to: "crate::server::api::toggle_skill(name).await"
      via: "spawn(async move { ... }) after optimistic flip"
      pattern: "spawn\\(async move"
    - from: "search input oninput"
      to: "search_query signal"
      via: "search_query.set(e.value())"
      pattern: "search_query\\.set"
    - from: "tab buttons onclick"
      to: "active_tab signal"
      via: "active_tab.set(\"...\")"
      pattern: "active_tab\\.set"
    - from: "filtered_skills use_memo"
      to: "ironhermes_core::skills::{tab_predicate, search_matches}"
      via: "AND-combined predicate inside use_memo closure that delegates to the core-crate helpers (added in Plan 01 Task 2)"
      pattern: "tab_predicate\\("
---

<objective>
Rewrite `ScreenSkills` and `SkillCard` in `crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` to wire tab filtering, search, and the optimistic per-skill toggle. All three features land in one file because they share the same signal-owning parent (`ScreenSkills`) and child render seam (`SkillCard`); splitting them would force duplicate prop-threading and risk mis-aligned signal lifetimes.

Purpose: Phase 26.7.3 D-01 through D-10. Activates currently-inert UI elements (tabs hardcoded to 87/55/57/3, search input with no binding, .tgl div with no onclick). The Plan 02 `toggle_skill` server fn is the single write seam; the rest is reactive client state. The filter logic itself is delegated to the pure `ironhermes_core::skills::{tab_predicate, search_matches}` helpers added in Plan 01 Task 2 — this plan wires them into a Dioxus `use_memo` rather than re-implementing the same match arms in component code.

Output: Updated `skills.rs` (component file only — no new files), confirmed by `cargo build -p iron_hermes_ui --features web` clean + the Plan 01 Task 2 unit tests covering the underlying filter behavior + browser UAT in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.7.3-skills-page-enable-tab-search-and-toggle-on-off-features/26.7.3-CONTEXT.md
@.planning/phases/26.7.3-skills-page-enable-tab-search-and-toggle-on-off-features/26.7.3-RESEARCH.md
@.planning/phases/26.7.3-skills-page-enable-tab-search-and-toggle-on-off-features/26.7.3-PATTERNS.md
@.planning/phases/26.7.3-skills-page-enable-tab-search-and-toggle-on-off-features/26.7.3-UI-SPEC.md
@.planning/phases/26.7.3-skills-page-enable-tab-search-and-toggle-on-off-features/26.7.3-01-SUMMARY.md
@.planning/phases/26.7.3-skills-page-enable-tab-search-and-toggle-on-off-features/26.7.3-02-SUMMARY.md
@crates/iron_hermes_ui/CLAUDE.md
@crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs
@crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs

<interfaces>
<!-- The full current skills.rs (101 lines) is in <read_first>; here are the
     external contracts the new code depends on. -->

From crates/iron_hermes_ui/src/server/api.rs lines 62-71:

```rust
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub struct SkillInfo {
    pub name: String,
    pub description: String,
    /// Derived from SkillSource: Builtin→"bundled", Official→"official",
    /// Trusted→"trusted", Community→"installed", SelfCreated→"self-created".
    pub category: String,
    /// True if skill.name appears in runtime_bundle.active_skills.
    pub enabled: bool,
}
```

From crates/iron_hermes_ui/src/server/api.rs (after Plan 02):

```rust
#[server]
pub async fn toggle_skill(name: String) -> Result<(), ServerFnError>;
```

From crates/ironhermes-core/src/skills.rs (after Plan 01 Task 2 — import these verbatim):

```rust
pub fn tab_predicate(category: &str, enabled: bool, tab: &str) -> bool;
pub fn search_matches(name: &str, description: &str, query: &str) -> bool;
```

From crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs lines 498-554 (optimistic spawn pattern reference):

```rust
spawn(async move {
    let _ = crate::server::api::api_agents_interrupt(id).await;
    gloo_timers::future::TimeoutFuture::new(500).await;
    interrupting.set(false);
    agents_resource.restart();
    // ...
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tabs (live counts + filter), search (oninput binding + memo filter), and empty state — no toggle yet</name>
  <files>crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs (current 101 lines — exact current state with hardcoded tab counts and unbound search input)
    - crates/iron_hermes_ui/CLAUDE.md (Dioxus 0.7 Specific Conventions §: use_signal / use_memo rules; signal borrow discipline § — no GenerationalRef across rsx!)
    - crates/iron_hermes_ui/clippy.toml (signal borrow rules — informs which patterns will fail at compile time)
    - .planning/phases/26.7.3-skills-page-enable-tab-search-and-toggle-on-off-features/26.7.3-PATTERNS.md (§"Tab button rendering pattern", §"Search input binding pattern", §"Grid / empty-state pattern", §"use_memo for derived filter state")
    - .planning/phases/26.7.3-skills-page-enable-tab-search-and-toggle-on-off-features/26.7.3-UI-SPEC.md (§"Interaction Contracts" — D-09 AND composition; §"Empty States" — D-10 copy; §"Color" — UPDATES disabled visual; §"Copywriting Contract" — exact tab label format `"ALL · {n}"`)
    - .planning/phases/26.7.3-skills-page-enable-tab-search-and-toggle-on-off-features/26.7.3-RESEARCH.md (Pitfall 5 — clone skills_list before use_memo; Pitfall 6 — ENABLED tab uses server-sourced `enabled`, not optimistic state)
    - .planning/phases/26.7.3-skills-page-enable-tab-search-and-toggle-on-off-features/26.7.3-01-SUMMARY.md (Plan 01 Task 2 — exact signatures of `tab_predicate` and `search_matches` for the use_memo body)
  </read_first>
  <action>
    Edit `crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` to:

    (a) Add `use std::collections::HashMap;` to the imports block at the top of the file (after `use dioxus::prelude::*;`). Add a second import line `use ironhermes_core::skills::{tab_predicate, search_matches};` immediately below — these are the pure helpers added in Plan 01 Task 2.

    (b) Inside `ScreenSkills`, BEFORE the existing `rsx!` macro and AFTER the existing `skills_list` / `load_error` / `count` / `enabled_count` extractions (current lines 17-23), declare three new signals using `use_signal`: `active_tab` initialized to the string literal `"all"` (`&'static str` — Copy), `search_query` initialized to `String::new()`. These two signals MUST be `let mut` so the event handlers can `.set()` on them. Extract owned values to local bindings BEFORE the rsx! macro is entered: `let tab_val = active_tab();` (Copy `&'static str`) and `let query_val = search_query();` (Clone `String`). Do not introduce any `toggle_*` signals in this task — Task 2 owns those.

    (c) Compute live tab counts as 4 new `let` bindings before rsx!: `count_all = skills_list.len()`, `count_bundled = skills_list.iter().filter(|s| s.category == "bundled").count()`, `count_installed = skills_list.iter().filter(|s| s.category != "bundled").count()`, `count_enabled = skills_list.iter().filter(|s| s.enabled).count()` (use server-sourced `s.enabled` per RESEARCH Pitfall 6). Remove the existing `let count = skills_list.len();` (now redundant — replaced by `count_all`) and the existing `let enabled_count = skills_list.iter().filter(|s| s.enabled).count();` (now redundant — replaced by `count_enabled`); update the screen-sub copy interpolation to use `count_all` and `count_enabled`.

    (d) Build the `filtered_skills` derived state with `use_memo` per PATTERNS §"use_memo for derived filter state": clone `skills_list` into a binding named `skills_for_memo` BEFORE the `use_memo` call (Pitfall 5 — owned capture), then `let filtered_skills = use_memo(move || { ... });`. Inside the closure: read `let tab = active_tab();` (Copy &'static str), `let query = search_query();` (Clone String — single capture from outer signal), then `skills_for_memo.iter().filter(|s| tab_predicate(&s.category, s.enabled, tab) && search_matches(&s.name, &s.description, &query)).cloned().collect::<Vec<SkillInfo>>()`. The two filter calls are the Plan 01 Task 2 pure helpers imported in step (a). DO NOT re-implement the match arms in the closure body — the canonical implementation lives in `ironhermes-core` and is unit-tested there. This delegation is what gives Plan 03's filter behavior automated coverage (under `cargo test -p ironhermes-core tab_filter / skills_filter`) — the UI is a thin call site.

    (e) Replace the current `div { class: "tabs", ... }` block (lines 53-59) with the 5-button block from PATTERNS §"Tab button rendering pattern": four interactive buttons (ALL / BUNDLED / INSTALLED / ENABLED), each with `class: if tab_val == "<key>" { "tab is-active" } else { "tab" }` and `onclick: move |_| active_tab.set("<key>")`, label format `"<KEY> · {count_<key>}"`. The fifth button is UPDATES rendered per UI-SPEC §Color: `button { class: "tab", style: "opacity:0.5; pointer-events:none;", disabled: true, "UPDATES · 0" }`. The literal label strings MUST be uppercase with a middle-dot separator and a single space on each side: `"ALL · {count_all}"`, `"BUNDLED · {count_bundled}"`, `"INSTALLED · {count_installed}"`, `"ENABLED · {count_enabled}"`, `"UPDATES · 0"`.

    (f) Replace the current `div { class: "search", ... }` block (lines 48-51) with the bound version from PATTERNS §"Search input binding pattern": same outer div + glyph, but the `input` element gains `oninput: move |e| search_query.set(e.value()),`. The placeholder string MUST remain literally `"Search skills, tags, capabilities…"` (unchanged from existing markup — UI-SPEC §Copywriting Contract).

    (g) Replace the body of the `div { class: "grid", ... }` block (lines 61-72) per PATTERNS §"Grid / empty-state pattern": keep the existing `if load_error { ... }` branch unchanged. Add a new `else if filtered_skills.read().is_empty() && !skills_list.is_empty() { ... }` branch rendering `div { style: "color:var(--gray);font-size:var(--fs-12);", "No skills match." }`. Replace the existing `else { for skill in skills_list.iter() { ... } }` with `else { for skill in filtered_skills.read().iter() { SkillCard { key: "{skill.name}", skill: skill.clone() } } }`. In Task 1 the `SkillCard` props are still the original single-prop signature `skill: SkillInfo` — Task 2 adds the toggle props.

    (h) Do NOT edit `SkillCard` in this task. Leave its signature and body identical to the current file.

    Maintain strict signal-borrow discipline: every `.read()` call MUST be inside a let-binding that closes (`;`) before the next statement OR be inlined into an interpolation expression that is fully evaluated before crossing the `rsx!` boundary. Match the existing pattern used by `let skills_list = ... ; let load_error = ... ;` at lines 17-21.
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --features web 2>&1 | tail -15 && cargo test -p ironhermes-core --lib tab_filter 2>&1 | tail -5 && cargo test -p ironhermes-core --lib skills_filter 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'use ironhermes_core::skills::\{tab_predicate,\s*search_matches\}' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1 (delegation import in place)
    - `grep -cE 'tab_predicate\(' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns at least 1 (called inside use_memo body)
    - `grep -cE 'search_matches\(' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns at least 1 (called inside use_memo body)
    - `grep -cE 'use_signal\(\|\|\s*"all"\)' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1
    - `grep -cE 'use_signal\(\|\|\s*String::new\(\)\)' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1
    - `grep -cE 'use_memo\(move\s*\|\|' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1 (exactly one memo introduced)
    - `grep -cE 'oninput:\s*move\s*\|e\|\s*search_query\.set\(e\.value\(\)\)' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1
    - `grep -cE 'active_tab\.set\("(all|bundled|installed|enabled)"\)' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 4 (one onclick per interactive tab)
    - `grep -cE '"UPDATES · 0"' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1
    - `grep -cE 'pointer-events:\s*none' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1 (UPDATES disabled style)
    - `grep -cE 'disabled:\s*true' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1 (UPDATES disabled attribute)
    - `grep -cF '"No skills match."' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1
    - `grep -cE '"ALL · \{count_all\}"' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1
    - `grep -cE '"BUNDLED · \{count_bundled\}"' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1
    - `grep -cE '"INSTALLED · \{count_installed\}"' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1
    - `grep -cE '"ENABLED · \{count_enabled\}"' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1
    - `grep -cE '"BUNDLED · 87"|"INSTALLED · 55"|"ENABLED · 57"|"UPDATES · 3"' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 0 (hardcoded placeholders removed)
    - `cargo build -p iron_hermes_ui --features web 2>&1 | tail -5` exits 0 with no new warnings on skills.rs
    - `cargo test -p ironhermes-core --lib tab_filter 2>&1 | grep -E 'test result: ok\. [5-9]+ passed'` matches (Plan 01 Task 2 behavioral tests still pass — these are the underlying coverage for the filter logic this task invokes)
    - `cargo test -p ironhermes-core --lib skills_filter 2>&1 | grep -E 'test result: ok\. [6-9]+ passed'` matches (same — Plan 01 Task 2 search tests still pass)
  </acceptance_criteria>
  <done>The Skills screen displays four interactive tabs with live counts plus one grayed-disabled UPDATES tab, the search input updates `search_query` on every keystroke, the grid renders `filtered_skills` (tab AND search — computed by delegating to `ironhermes_core::skills::tab_predicate` and `search_matches`), and "No skills match." appears when the filter yields zero results. Behavioral coverage of the filter logic lives in `ironhermes-core` unit tests (Plan 01 Task 2) so this task's verify is not compile-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire optimistic toggle — toggle_states + toggle_errors signals, use_effect seeding, SkillCard prop rewrite, on_toggle spawn handler</name>
  <files>crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs (post-Task 1 state — confirm tabs + search + memo are in place; identify exact insertion points for new signals and use_effect)
    - crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs (lines 491-565 — full optimistic kill/interrupt block, the canonical reference pattern for spawn + capture + flip + revert)
    - .planning/phases/26.7.3-skills-page-enable-tab-search-and-toggle-on-off-features/26.7.3-PATTERNS.md (§"use_effect seeding pattern", §"Optimistic toggle pattern", §"SkillCard component signature")
    - .planning/phases/26.7.3-skills-page-enable-tab-search-and-toggle-on-off-features/26.7.3-RESEARCH.md (Pitfall 2 — no signal read across rsx!; Pitfall 3 — toggle_states seeding race; Pitfall 6 — ENABLED tab uses server-sourced enabled, not optimistic state — DO NOT regress the Task 1 memo)
    - .planning/phases/26.7.3-skills-page-enable-tab-search-and-toggle-on-off-features/26.7.3-UI-SPEC.md (§"Interaction Contracts" — toggle sequence steps 1-7; §"Component Inventory" — SkillCard prop list; §"Copywriting Contract" — `"Toggle failed — try again."`; §"Accessibility" — `role="switch"` + `aria-checked`)
    - .planning/phases/26.7.3-skills-page-enable-tab-search-and-toggle-on-off-features/26.7.3-02-SUMMARY.md (toggle_skill server fn signature confirmation)
  </read_first>
  <preconditions>
    **MANDATORY: Do NOT start this task if Task 1's `cargo build -p iron_hermes_ui --features web` verify did not exit 0.** This task reads the post-Task-1 file as its starting state — every let-binding insertion and grid-loop body modification described below assumes Task 1's structural edits (the `active_tab` / `search_query` signals, the `count_*` bindings, the `filtered_skills` use_memo, the bound search input, the 5-button tab block, the empty-state branch) are already present and the file compiles. If Task 1 left the file in a non-compiling state, fix Task 1 first (or open a gap-closure plan) — DO NOT layer Task 2 edits on broken structure, because the failures will compound and the executor will not be able to distinguish Task 1 regressions from Task 2 errors. The intermediate `cargo build` at the end of Task 1's verify is the gate; rerun it before opening this file if any time has passed.
  </preconditions>
  <behavior>
    - Toggle click on a card whose current optimistic state is `enabled=true` immediately flips the visual to OFF (`.tgl` class without `.on`, `is-active` class removed from `.card`).
    - Toggle click on a card whose current optimistic state is `enabled=false` immediately flips the visual to ON (`.tgl on`, `.card.is-active`).
    - If `toggle_skill(name).await` returns `Ok(_)`, no further visual change happens (optimistic state is already correct).
    - If `toggle_skill(name).await` returns `Err(_)`, the card's visual reverts to its pre-click state AND an inline `div { style: "color:var(--danger);font-size:var(--fs-12);", "Toggle failed — try again." }` appears inside the card BELOW the `.card-body`.
    - A second click after a failure clears the error message (toggle_errors entry is removed before the new optimistic flip).
    - `toggle_states` is seeded from `skills_list` on first non-empty load via `use_effect`; the seeding is guarded by `is_empty()` so optimistic flips after initial seeding are never overwritten.
    - The screen-sub `enabled_count` value shown in the header now reads from the live `toggle_states` HashMap (count of `true` values) so the count updates on optimistic toggle.
  </behavior>
  <action>
    **NOTE ON PROP TYPE — READ FIRST:** RESEARCH.md Pattern 5 shows `enabled: Signal<bool>` as a SkillCard prop type. **That is NOT the prop type this plan uses.** The correct prop type is plain `enabled: bool`. The reason: the underlying optimistic state lives in the parent (`toggle_states: Signal<HashMap<String, bool>>` on `ScreenSkills`), and the child receives a snapshotted plain bool per render. Passing `Signal<bool>` per card would require allocating one signal per card name (per-card signal proliferation — the exact anti-pattern called out in RESEARCH §Pitfalls under "per-card signal proliferation"), and would also create cross-component signal ownership that is hard to reason about. If the executor follows RESEARCH Pattern 5 literally (`Signal<bool>`), the acceptance grep `grep -cE 'enabled:\s*bool' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` will return 0 and the task will fail verification. Use the prop type stated in this action block — RESEARCH's Pattern 5 is superseded for this plan.

    Extend the file edited in Task 1 to wire the toggle:

    (a) Inside `ScreenSkills`, AFTER the `active_tab` / `search_query` signal declarations from Task 1 and BEFORE the `filtered_skills` memo, declare two new signals: `let mut toggle_states: Signal<HashMap<String, bool>> = use_signal(|| HashMap::new());` and `let mut toggle_errors: Signal<HashMap<String, String>> = use_signal(|| HashMap::new());`. Both MUST be `let mut`.

    (b) Add the `use_effect` seeding block from PATTERNS §"use_effect seeding pattern" immediately after the two new signal declarations. The closure clones `skills_list` to `sl`, then on each re-run checks `if !sl.is_empty() && toggle_states.read().is_empty() { let mut map = toggle_states.write(); for s in &sl { map.insert(s.name.clone(), s.enabled); } }`. The guard `toggle_states.read().is_empty()` is mandatory — without it, the effect would overwrite optimistic flips on every re-render after the initial seed.

    (c) Update the screen-sub copy: REPLACE the Task 1 `count_enabled` interpolation in the header sub-copy with a NEW binding computed BEFORE rsx!: `let enabled_count_live = toggle_states.read().values().filter(|&&v| v).count();` (borrow ends at `;`). The header sub-copy interpolates `enabled_count_live` so the count tracks optimistic state. The tab label `"ENABLED · {count_enabled}"` continues to use the server-sourced `count_enabled` (RESEARCH Pitfall 6 — DO NOT switch this to optimistic state).

    (d) Define the `on_toggle` closure BEFORE rsx! per PATTERNS §"Optimistic toggle pattern". The closure signature MUST be `move |name: String| { ... }` so it can be invoked from the SkillCard's EventHandler. Body (verbatim from PATTERNS): capture `let current = *toggle_states.read().get(&name).unwrap_or(&false);` (Copy bool, borrow ends at `;`), then `toggle_states.write().insert(name.clone(), !current);`, then `toggle_errors.write().remove(&name);`, then `spawn(async move { match crate::server::api::toggle_skill(name.clone()).await { Ok(_) => {} Err(_) => { toggle_states.write().insert(name.clone(), current); toggle_errors.write().insert(name.clone(), "Toggle failed — try again.".to_string()); } } });`. The on_toggle binding type is inferred — DO NOT annotate; Rust + Dioxus EventHandler derive the right shape from usage.

    (e) Inside the grid `for skill in filtered_skills.read().iter() { ... }` loop body, extract per-card values BEFORE the SkillCard call: `let is_enabled = *toggle_states.read().get(&skill.name).unwrap_or(&skill.enabled);` (borrow ends at `;`; the `unwrap_or(&skill.enabled)` fallback handles the pre-seeding render window per Pitfall 3); `let err_msg = toggle_errors.read().get(&skill.name).cloned();` (borrow ends at `;`); `let skill_name_for_toggle = skill.name.clone();` (cloned to satisfy 'static EventHandler capture). Then invoke `SkillCard { key: "{skill.name}", skill: skill.clone(), enabled: is_enabled, error_msg: err_msg, on_toggle: move |_| on_toggle(skill_name_for_toggle.clone()) }`.

    (f) Rewrite `SkillCard` per PATTERNS §"SkillCard component signature". The new prop list MUST be exactly: `skill: crate::server::api::SkillInfo, enabled: bool, error_msg: Option<String>, on_toggle: EventHandler<()>`. The `enabled` prop is a PLAIN bool (NOT `Signal<bool>` — see the note above). Body must: render `class: if enabled { "is-active" }` on the outer `.card`, render `class: if enabled { "card-icon" } else { "card-icon gray" }` on the icon div, render the `.tgl` div with `class: if enabled { "tgl on" } else { "tgl" }` AND `role: "switch"`, `aria_checked: "{enabled}"`, `onclick: move |_| on_toggle.call(())`. After the `.card-body` div, conditionally render `if let Some(ref err) = error_msg { div { style: "color:var(--danger);font-size:var(--fs-12);", "{err}" } }`. Remove the existing `"data-tgl": "true"` attribute and the existing `// No onclick — write ops out of scope` comment.

    (g) Do NOT add per-card signals. The `enabled: bool` and `error_msg: Option<String>` props are owned by `ScreenSkills` via `toggle_states` and `toggle_errors` — RESEARCH Pattern 5's `Signal<bool>` prop shape is superseded by the parent-owned snapshot shape described above (avoids per-card signal proliferation).
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui --features web 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'toggle_states:\s*Signal<HashMap<String,\s*bool>>' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1
    - `grep -cE 'toggle_errors:\s*Signal<HashMap<String,\s*String>>' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1
    - `grep -cE 'use_effect\(move\s*\|\|' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1 (the seeding effect)
    - `grep -cE 'toggle_states\.read\(\)\.is_empty\(\)' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1 (Pitfall 3 seeding guard)
    - `grep -cE 'spawn\(async move' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1 (the toggle spawn)
    - `grep -cE 'crate::server::api::toggle_skill\(' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1
    - `grep -cF '"Toggle failed — try again."' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1
    - `grep -cE 'role:\s*"switch"' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1 (toggle accessibility)
    - `grep -cE 'aria_checked:\s*"\{enabled\}"' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1
    - `grep -cE 'on_toggle:\s*EventHandler<\(\)>' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1 (SkillCard prop signature)
    - `grep -cE 'enabled:\s*bool' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns at least 1 (SkillCard prop — plain bool, NOT Signal<bool>; this grep would return 0 if the executor followed RESEARCH Pattern 5 literally)
    - `grep -cE 'enabled:\s*Signal<bool>' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 0 (anti-pattern guard — confirms the executor did NOT regress to RESEARCH Pattern 5's prop shape)
    - `grep -cE 'error_msg:\s*Option<String>' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1 (SkillCard prop)
    - `grep -cE '"data-tgl":\s*"true"' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 0 (old inert marker removed)
    - `grep -cE 'on_toggle\.call\(\(\)\)' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns 1 (SkillCard fires it on .tgl click)
    - `grep -cE 'enabled_count_live' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs` returns at least 2 (one let-binding, one interpolation in header sub-copy)
    - `awk '/^#\[component\]$/{p=1} p' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs | grep -c '\.await'` returns at most 1 (only the `toggle_skill(name).await` inside the spawn closure; never on a held signal borrow)
    - `cargo build -p iron_hermes_ui --features web 2>&1 | tail -5` exits 0 with zero new errors
    - `cargo clippy -p iron_hermes_ui --features web 2>&1 | grep -E 'generational_ref_held_across_await|write_lock_held_across_await' | wc -l` returns 0 (signal borrow discipline holds)
  </acceptance_criteria>
  <done>Each `SkillCard` toggle click flips the card's visual state immediately, fires `toggle_skill` in the background, reverts + shows inline error on failure; the header sub-copy "{count} loaded · {enabled_count_live} enabled" updates on optimistic flip; the ENABLED tab count remains server-sourced per Pitfall 6; the workspace compiles clean and clippy reports no signal-borrow violations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard → WASM | search query and tab clicks arrive as DOM events |
| WASM → server fn | toggle_skill is called over HTTP (handled in Plan 02 threat model) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.7.3-03-01 | Information Disclosure | inline error string visible in DOM | accept | The error copy is the literal "Toggle failed — try again." — no underlying server error text leaks into the DOM. The verbose server error is dropped (`Err(_)` pattern discards the inner) before being converted to the user-facing string. |
| T-26.7.3-03-02 | Tampering | search query injected into RSX | mitigate | `search_query.set(e.value())` stores the raw String; the filter uses `.contains()` for substring match — never `eval`, never `innerHTML`. Dioxus auto-escapes all text interpolations into RSX. |
| T-26.7.3-03-03 | Denial of Service | extremely large skills_list slows filter | accept | `use_memo` recomputes only when `active_tab`/`search_query` change; the filter is O(N) over a bounded local registry. Single-operator deployment posture; >10K skills is implausible. |
| T-26.7.3-03-04 | Spoofing | optimistic state diverging from server truth | mitigate | On `Err`, the optimistic flip is reverted to the captured pre-click value AND an inline error appears so the user observes the divergence. On `Ok`, the optimistic state matches the server commit. The next page reload re-reads ground truth from `list_skills()`. |
| T-26.7.3-03-SC | Tampering | npm/pip/cargo installs | n/a | No new dependencies added in this plan. |
</threat_model>

<verification>
- `cargo build -p iron_hermes_ui --features web 2>&1 | tail -5` exits 0
- `cargo clippy -p iron_hermes_ui --features web 2>&1 | tail -10` reports zero generational_ref / write_lock violations
- `grep -nE 'use_signal\(\|\|\s*"all"\)|use_signal\(\|\|\s*String::new\(\)\)|use_signal\(\|\|\s*HashMap::new\(\)\)' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs | wc -l` returns 3 or 4 (active_tab + search_query + 1-2 HashMap signals — accepts either count)
- `grep -A 60 '#\[component\]\s*$' crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs | grep -nE 'rsx! \{' | head -2` shows the rsx! macro is entered AFTER all signal extractions
</verification>

<success_criteria>
The Skills page in `dx serve` renders five tabs with live counts (UPDATES disabled), filters by tab AND search in real time, shows "No skills match." when filter yields zero, and each card's toggle button flips the card state immediately + persists to `toggle_skill` server fn + reverts + shows inline error on Err. Plan 04 will UAT these flows in the browser.
</success_criteria>

<output>
Create `.planning/phases/26.7.3-skills-page-enable-tab-search-and-toggle-on-off-features/26.7.3-03-SUMMARY.md` when done. Include: post-edit line count of `skills.rs`, the four signal names + types, confirmation that the `enabled_count_live` is computed from `toggle_states` while the `count_enabled` tab label is computed from `skills_list`, the literal text strings introduced (`"No skills match."`, `"Toggle failed — try again."`, all five tab labels), and confirmation that the `SkillCard` `enabled` prop is plain `bool` (not `Signal<bool>`).
</output>
</content>
</invoke>