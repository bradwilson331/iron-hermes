---
phase: 26.7.3-skills-page-enable-tab-search-and-toggle-on-off-features
plan: "01"
subsystem: config, skills
tags: [config, skills, serde, filters, rust, unit-tests]

requires:
  - phase: 19-skills-config
    provides: SkillsConfig struct and HubConfig in crates/ironhermes-core/src/config.rs

provides:
  - "SkillsConfig.disabled: Vec<String> field with #[serde(default)] for backward-compatible config YAML parsing"
  - "tab_predicate(category, enabled, tab) -> bool pure helper in ironhermes_core::skills"
  - "search_matches(name, description, query) -> bool pure helper in ironhermes_core::skills"
  - "11 unit tests covering serde round-trip, tab semantics, and search behavior"

affects:
  - "26.7.3-02 (toggle server fn reads/writes config.skills.disabled)"
  - "26.7.3-03 (ScreenSkills use_memo imports tab_predicate and search_matches)"

tech-stack:
  added: []
  patterns:
    - "#[serde(default)] field addition to existing serde struct for backward-compatible YAML parsing"
    - "Pure pure-function extraction: tab/search predicates live in ironhermes-core so behavioral tests run under cargo test without WASM runtime"
    - "TDD RED-GREEN on each task: tests written before implementation, confirmed failing, then implementation added"

key-files:
  created: []
  modified:
    - crates/ironhermes-core/src/config.rs
    - crates/ironhermes-core/src/skills.rs

key-decisions:
  - "opt-out model: all skills enabled by default; only explicitly disabled names appear in Vec<String>"
  - "pure fn placement: tab_predicate and search_matches placed at top-level module scope (above existing tests) so Plan 03 can use ironhermes_core::skills::{tab_predicate, search_matches}"
  - "separate test modules (mod tab_filter, mod skills_filter) rather than nesting inside existing mod tests, so cargo test --lib tab_filter and --lib skills_filter produce isolated filtered output"

patterns-established:
  - "Pure-function extraction pattern: filter logic lives in ironhermes-core with full unit test coverage before UI wires it up"
  - "#[serde(default)] on Vec field: Vec::new() is the natural serde default for Vec<String>; no custom default fn needed"

requirements-completed: []

duration: 8min
completed: 2026-05-19
---

# Phase 26.7.3 Plan 01: SkillsConfig disabled field and pure filter helpers Summary

**`disabled: Vec<String>` opt-out field added to `SkillsConfig` with serde round-trip coverage; `tab_predicate` and `search_matches` pure fns exported from `ironhermes_core::skills` with 11 behavioral unit tests**

## Performance

- **Duration:** 8 min
- **Started:** 2026-05-19T04:25:51Z
- **Completed:** 2026-05-19T04:33:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added `#[serde(default)] pub disabled: Vec<String>` to `SkillsConfig` (after `hub` field, line 874) with `disabled: Vec::new()` in the `Default` impl — all skills on by default, opt-out model
- Verified backward compatibility: existing `config.yaml` files without `disabled:` key parse cleanly via `#[serde(default)]`
- Added two pure public helpers to `ironhermes_core::skills` (`tab_predicate` and `search_matches`) that Plan 03's `use_memo` closure can import verbatim, keeping behavioral logic tested at the core layer without a WASM runtime
- 11 new unit tests across 3 test modules all pass under `cargo test -p ironhermes-core`

## Task Commits

Each task was committed atomically:

1. **Task 1: Add `disabled: Vec<String>` to SkillsConfig** - `c570a745` (feat)
2. **Task 2: Add `tab_predicate` and `search_matches` pure helpers** - `640ea82b` (feat)

**Plan metadata:** (docs commit — see below)

_Note: TDD tasks had inline RED-GREEN on each task; no separate refactor commit needed — code was clean as written._

## Files Created/Modified

- `crates/ironhermes-core/src/config.rs` — Added `disabled: Vec<String>` field (lines 873–880) and `Default` initializer (line 886); added 3 unit tests (lines 1681–1726)
- `crates/ironhermes-core/src/skills.rs` — Added `tab_predicate` (lines 1043–1055) and `search_matches` (lines 1057–1065) pure fns; added `mod tab_filter` (5 tests, lines 1072–1125) and `mod skills_filter` (6 tests, lines 1127–1187)

## Rust Signatures (for Plan 03 import)

```rust
pub fn tab_predicate(category: &str, enabled: bool, tab: &str) -> bool

pub fn search_matches(name: &str, description: &str, query: &str) -> bool
```

Import path for Plan 03:
```rust
use ironhermes_core::skills::{tab_predicate, search_matches};
```

## New YAML Field Shape

```yaml
skills:
  enabled: true
  extra_paths: []
  disabled:
    - some-skill-name
    - another-skill
```

When `disabled:` key is absent (all pre-phase config.yaml files), `serde_yaml` applies `#[serde(default)]` and produces `Vec::new()` — no parse error.

## Test Inventory

### config.rs — skills_config tests (3 new, 4 pre-existing = 7 total)

| Test Name | Status |
|-----------|--------|
| `skills_config_default_has_empty_disabled` | PASS |
| `skills_config_disabled_field_round_trip` | PASS |
| `skills_config_missing_disabled_key_defaults_empty` | PASS |
| `test_skills_config_default` (pre-existing) | PASS |
| `test_skills_config_round_trip_with_config_map` (pre-existing) | PASS |
| `test_skills_config_empty_config_defaults_to_empty_map` (pre-existing) | PASS |
| `test_skills_config_backward_compat_no_hub` (pre-existing) | PASS |

### skills.rs — mod tab_filter (5 tests)

| Test Name | Status |
|-----------|--------|
| `all_tab_matches_every_skill` | PASS |
| `bundled_tab_matches_only_bundled` | PASS |
| `installed_tab_excludes_bundled` | PASS |
| `enabled_tab_requires_server_enabled` | PASS |
| `unknown_tab_defaults_to_all` | PASS |

### skills.rs — mod skills_filter (6 tests)

| Test Name | Status |
|-----------|--------|
| `empty_query_matches_everything` | PASS |
| `name_substring_matches` | PASS |
| `description_substring_matches` | PASS |
| `case_insensitive_match` | PASS |
| `no_match_returns_false` | PASS |
| `category_not_searched` | PASS |

## `#[serde(default)]` Attribute Count

- **Pre-edit count in config.rs:** 42
- **Post-edit count in config.rs:** 43 (increased by exactly 1 — the new `disabled` field)

## Decisions Made

- Used opt-out model (`disabled: Vec<String>`) not opt-in, consistent with existing `enabled: bool` master switch on `SkillsConfig` — all skills on by default, explicit names opt out
- Placed `tab_predicate` / `search_matches` above the existing `#[cfg(test)] mod tests` block so they sit at crate top-level module scope
- Created two separate `#[cfg(test)] mod` blocks (`tab_filter`, `skills_filter`) rather than nesting inside the existing `mod tests` — this allows `cargo test -p ironhermes-core --lib tab_filter` to produce clean filtered output for Plan 03 authors to verify

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 02 (toggle server fn) can now write `config.skills.disabled.push(name)` / `config.skills.disabled.retain(...)` without further struct changes
- Plan 03 (ScreenSkills tab/search/filter) can `use ironhermes_core::skills::{tab_predicate, search_matches};` and call them from the Dioxus `use_memo` closure directly
- All plan-level verification commands pass: `cargo test -p ironhermes-core --lib skills_config` (7 passed), `--lib tab_filter` (5 passed), `--lib skills_filter` (6 passed), `cargo build -p ironhermes-core` (clean)

## Self-Check: PASSED

- `crates/ironhermes-core/src/config.rs` — file exists and contains `pub disabled: Vec<String>` in `SkillsConfig`
- `crates/ironhermes-core/src/skills.rs` — file exists and contains `pub fn tab_predicate` and `pub fn search_matches`
- Commit `c570a745` — verified in git log
- Commit `640ea82b` — verified in git log
- All 3 acceptance criteria test commands match expected `test result: ok. 1 passed` / `ok. 5 passed` / `ok. 6 passed`

---
*Phase: 26.7.3-skills-page-enable-tab-search-and-toggle-on-off-features*
*Completed: 2026-05-19*
