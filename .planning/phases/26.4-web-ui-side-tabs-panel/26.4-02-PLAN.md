---
phase: 26.4
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/iron_hermes_ui/assets/warp-ih.css
autonomous: true
requirements: [D-05, D-06, D-08, D-10]
tags: [dioxus, css, side-panel, design-tokens]

must_haves:
  truths:
    - "Five new CSS class blocks exist in warp-ih.css under the .wh-side-* family"
    - "Active tab indicator uses ::after with bottom: 0 (NOT ::before with top: 0)"
    - "All new CSS values reference existing design tokens (var(--…)) — no raw hex literals introduced"
  artifacts:
    - path: "crates/iron_hermes_ui/assets/warp-ih.css"
      provides: ".wh-side-tabs, .wh-side-tab, .wh-side-info, .wh-side-info-card, .wh-side-info-heading, .wh-side-info-row CSS rules"
      contains: ".wh-side-tab.is-active::after"
  key_links:
    - from: ".wh-side-tab.is-active"
      to: "var(--accent-primary)"
      via: "::after pseudo-element bottom: 0; height: 2px; background"
      pattern: "wh-side-tab\\.is-active::after"
---

<objective>
Add the five CSS class families for the new side panel tab strip and INFO card layout, inserted after the existing `.wh-side-scroll` rule and before `.wh-msg`. The classes are fully specified in `26.4-UI-SPEC.md` and `26.4-RESEARCH.md` — values are authoritative; do NOT invent new ones.

Purpose: D-05 forbids reusing `.wh-tab` / `.wh-tabs` classes. D-06 mandates a 2px bottom-border active indicator using `var(--accent-primary)`. D-08 requires INFO content rendered as styled cards with section heading + key-value rows. D-10 mandates design-token vocabulary only.

Output: Six new rule blocks (`.wh-side-tabs`, `.wh-side-tab` + `:hover` + `.is-active` + `.is-active::after`, `.wh-side-info`, `.wh-side-info-card`, `.wh-side-info-heading`, `.wh-side-info-row` + `.wh-side-info-key` + `.wh-side-info-val`) inserted as a contiguous block in `warp-ih.css`. No other rules modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26.4-web-ui-side-tabs-panel/26.4-CONTEXT.md
@.planning/phases/26.4-web-ui-side-tabs-panel/26.4-RESEARCH.md
@.planning/phases/26.4-web-ui-side-tabs-panel/26.4-PATTERNS.md
@.planning/phases/26.4-web-ui-side-tabs-panel/26.4-UI-SPEC.md
@crates/iron_hermes_ui/assets/warp-ih.css
@crates/iron_hermes_ui/CLAUDE.md

<interfaces>
<!-- Insertion anchor in warp-ih.css — current state (lines 369-376): -->
```css
.wh-side-scroll {
  flex: 1; min-height: 0;
  overflow-y: auto;
  padding: 12px;
  display: flex; flex-direction: column; gap: 10px;
  scrollbar-width: thin;
}
                       /* ← INSERT new blocks immediately after this closing brace */
.wh-msg {              /* ← BEFORE this rule */
  display: flex; flex-direction: column; gap: 4px;
}
```

<!-- Existing TitleBar tab pattern at warp-ih.css:118-122 — DO NOT copy class names; CONTRAST reference for ::after vs ::before -->
```css
.wh-tab.is-active::before {
  content: ""; position: absolute; top: 0; left: 0; right: 0; height: 2px;
  background: var(--accent-primary);
}
```
The new side tab is the MIRROR: `::after` + `bottom: 0` (not `::before` + `top: 0`). This is intentional per UI-SPEC and Pitfall 3.

<!-- Available design tokens (verified in design-tokens.css + warp-ih.css:16-38) -->
- var(--accent-primary)   — cyan, active indicator + headings accent
- var(--w-border)         — divider lines
- var(--w-bg-1)           — panel background
- var(--w-bg-2)           — card background
- var(--w-bg-3)           — tab hover background
- var(--fg)               — body foreground
- var(--fg-strong)        — active tab label, card values, headings
- var(--fg-dim)           — inactive tab label, info keys
- var(--font-mono)        — Ioskeley Mono
- var(--w-radius-block)   — 6px (card border-radius)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert side-tabs + info-card CSS blocks after .wh-side-scroll</name>
  <files>crates/iron_hermes_ui/assets/warp-ih.css</files>
  <read_first>
    - crates/iron_hermes_ui/assets/warp-ih.css (lines 320-400 — confirm insertion anchor and existing .wh-side-scroll / .wh-msg surrounding context)
    - .planning/phases/26.4-web-ui-side-tabs-panel/26.4-UI-SPEC.md (Component Inventory — authoritative CSS values)
    - .planning/phases/26.4-web-ui-side-tabs-panel/26.4-CONTEXT.md (D-05, D-06, D-08, D-10)
  </read_first>
  <behavior>
    - .wh-side-tabs renders a 32px-tall flex-row strip with a 1px bottom border
    - .wh-side-tab buttons fill the strip equally (flex: 1), 12px monospace bold uppercase, dim color when inactive
    - On hover, an inactive tab gains var(--w-bg-3) background and var(--fg) color
    - On .is-active, label becomes var(--fg-strong) AND a 2px cyan bar appears at the BOTTOM of the tab via ::after
    - .wh-side-info wraps the INFO tab content with overflow-y auto and 12px padding, stacking cards with 8px gap
    - .wh-side-info-card has var(--w-bg-2) background, 6px border-radius, 8px 12px padding, 4px gap between rows
    - .wh-side-info-heading is 12px/700 var(--fg-strong) with 4px bottom margin and 0.04em letter-spacing
    - .wh-side-info-row is a flex-row with 12px font, 1.4 line-height; .wh-side-info-key is dim with min-width 80px; .wh-side-info-val is var(--fg) with word-break: break-all
  </behavior>
  <action>
    Edit `crates/iron_hermes_ui/assets/warp-ih.css`:

    Locate the `.wh-side-scroll { ... }` rule (currently at lines 369-375). Immediately AFTER its closing `}` (line 375) and BEFORE the `.wh-msg {` rule (currently line 377), insert these EXACT blocks (do NOT modify the values; copy verbatim from UI-SPEC):

    ```css

    /* ------- side panel tab strip + info cards (Phase 26.4) -------- */
    .wh-side-tabs {
      display: flex;
      height: 32px;
      border-bottom: 1px solid var(--w-border);
      flex-shrink: 0;
    }

    .wh-side-tab {
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: var(--font-mono);
      font-size: 12px;
      font-weight: 700;
      letter-spacing: .04em;
      color: var(--fg-dim);
      cursor: default;
      position: relative;
      background: transparent;
      border: none;
    }
    .wh-side-tab:hover { background: var(--w-bg-3); color: var(--fg); }
    .wh-side-tab.is-active { color: var(--fg-strong); }
    .wh-side-tab.is-active::after {
      content: "";
      position: absolute;
      bottom: 0; left: 0; right: 0;
      height: 2px;
      background: var(--accent-primary);
    }

    .wh-side-info {
      flex: 1;
      min-height: 0;
      overflow-y: auto;
      padding: 12px;
      display: flex;
      flex-direction: column;
      gap: 8px;
      scrollbar-width: thin;
    }

    .wh-side-info-card {
      background: var(--w-bg-2);
      border-radius: var(--w-radius-block);
      padding: 8px 12px;
      display: flex;
      flex-direction: column;
      gap: 4px;
    }

    .wh-side-info-heading {
      font-size: 12px;
      font-weight: 700;
      color: var(--fg-strong);
      letter-spacing: .04em;
      margin-bottom: 4px;
    }

    .wh-side-info-row {
      display: flex;
      gap: 8px;
      font-size: 12px;
      line-height: 1.4;
    }
    .wh-side-info-row .wh-side-info-key {
      color: var(--fg-dim);
      flex-shrink: 0;
      min-width: 80px;
    }
    .wh-side-info-row .wh-side-info-val {
      color: var(--fg);
      word-break: break-all;
    }
    ```

    Critical invariants per Pitfall 3: the active indicator MUST use `::after` with `bottom: 0` — NOT `::before` with `top: 0` (that's TitleBar's pattern). Per Pitfall 5: `.wh-side-tabs` MUST have `flex-shrink: 0` and `height: 32px` so the existing `.wh-side-scroll { flex: 1 }` does not collapse it.

    Per D-10 + Don't-Hand-Roll table: do NOT introduce raw hex literals. Every color/size value above is either a `var(--…)` token, a CSS keyword (`transparent`, `none`), or a layout numeric (px / em / unitless flex/line-height). No new hex.

    Do NOT modify `.wh-side-scroll`, `.wh-side-head`, `.wh-side`, `.wh-msg`, `.wh-tab`, `.wh-tabs`, or any other existing rule. The change is purely additive at one insertion point.
  </action>
  <verify>
    <automated>grep -n "\.wh-side-tabs\|\.wh-side-tab\b\|\.wh-side-info" crates/iron_hermes_ui/assets/warp-ih.css | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\.wh-side-tabs {" crates/iron_hermes_ui/assets/warp-ih.css` returns 1
    - `grep -c "^\.wh-side-tab {" crates/iron_hermes_ui/assets/warp-ih.css` returns 1
    - `grep -c "^\.wh-side-tab\.is-active {" crates/iron_hermes_ui/assets/warp-ih.css` returns 1
    - `grep -c "\.wh-side-tab\.is-active::after" crates/iron_hermes_ui/assets/warp-ih.css` returns 1
    - `grep -c "^\.wh-side-info {" crates/iron_hermes_ui/assets/warp-ih.css` returns 1
    - `grep -c "^\.wh-side-info-card {" crates/iron_hermes_ui/assets/warp-ih.css` returns 1
    - `grep -c "^\.wh-side-info-heading {" crates/iron_hermes_ui/assets/warp-ih.css` returns 1
    - `grep -c "^\.wh-side-info-row {" crates/iron_hermes_ui/assets/warp-ih.css` returns 1
    - `grep -c "::before" crates/iron_hermes_ui/assets/warp-ih.css` returns the SAME count as before edit (active indicator uses ::after; no new ::before added)
    - `grep -c "var(--accent-primary)" crates/iron_hermes_ui/assets/warp-ih.css` increases by exactly 1 (the new active-bar background)
    - `grep -B 0 -A 1 "^\.wh-side-tabs {" crates/iron_hermes_ui/assets/warp-ih.css | grep -c "display: flex"` returns 1
    - `grep -A 5 "\.wh-side-tab\.is-active::after" crates/iron_hermes_ui/assets/warp-ih.css | grep -c "bottom: 0"` returns 1
    - The block sits between `.wh-side-scroll` and `.wh-msg`: `grep -n "\.wh-side-scroll {\|\.wh-side-tabs {\|\.wh-msg {" crates/iron_hermes_ui/assets/warp-ih.css | head -3 | awk -F: '{print $1}'` produces 3 line numbers in strictly increasing order
    - No new raw hex colors introduced: `grep -E "background:\s*#[0-9a-fA-F]" crates/iron_hermes_ui/assets/warp-ih.css | grep -v "var(--"` returns the SAME line count as before edit
  </acceptance_criteria>
  <done>
    Six rule families (.wh-side-tabs, .wh-side-tab + :hover + .is-active + .is-active::after, .wh-side-info, .wh-side-info-card, .wh-side-info-heading, .wh-side-info-row) added between .wh-side-scroll and .wh-msg; tokens-only; no other rules touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static asset → browser | warp-ih.css is served as a static asset and rendered by the browser CSS engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.4-04 | Tampering | Static CSS asset | accept | CSS is bundled at build time via Dioxus `asset!()` macro and content-hashed by `dx`; no runtime mutation; no user input is reflected into the stylesheet |
| T-26.4-05 | Information Disclosure | CSS class names exposed in DOM | accept | Class names like `.wh-side-info-card` are non-sensitive UI hints; reveal nothing about server state, secrets, or user data |
| T-26.4-06 | Denial of Service | CSS performance | accept | Six small rule blocks, no animations, no expensive selectors (no descendant combinators on universal selectors); no perf risk |
</threat_model>

<verification>
- All 8 grep acceptance criteria above return expected counts
- File compiles as valid CSS (browser-loadable)
- `dx serve --features web` (manual) renders the side panel without layout regression in the AGENT view (which is unaffected — only adjacent rules added)
</verification>

<success_criteria>
The six CSS rule families exist in `warp-ih.css`, sit between `.wh-side-scroll` and `.wh-msg`, use only design tokens, and the `.is-active::after` indicator uses `bottom: 0` (not `top: 0`). Plan 03's RSX (which references these class names) will style correctly when rendered.
</success_criteria>

<output>
After completion, create `.planning/phases/26.4-web-ui-side-tabs-panel/26.4-02-SUMMARY.md`
</output>
