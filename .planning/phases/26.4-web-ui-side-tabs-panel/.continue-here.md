---
context: phase
phase: 26.4-web-ui-side-tabs-panel
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-05-06T10:35:06.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: UAT-HUMAN-GATE — Task 2 is a blocking human-verify checkpoint. The resuming agent cannot self-approve. Completion requires the user to run the browser UAT and type "approved".

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Self-approval of UAT gate | Marking Task 2 done based on code inspection alone without live browser verification | blocking | Task 2 type is `checkpoint:human-verify gate="blocking"` — only user input "approved" closes it |
| Stale WASM in browser | Browser cache serving old WASM shows old UI even after successful rebuild | advisory | Always instruct user to hard-refresh (Cmd+Shift+R) or use incognito before reporting UAT failure |

<current_state>
Phase 26.4, plan 04, task 2 (visual UAT). All code is in place and compiled. Task 1 committed at ff00f7f. UAT attempted but tab strip not visible — root cause is suspected browser cache. dx serve is running at http://127.0.0.1:8080.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Add active_side_tab signal + on_side_tab_click + widen config_summary + 9-prop AgentPanel call site — Done (commit ff00f7f)
  - Signal at warp_hermes.rs:61
  - 4-tuple destructure at warp_hermes.rs:388
  - on_side_tab_click at warp_hermes.rs:599
  - 9-prop AgentPanel call at warp_hermes.rs:817
- Task 2: Visual UAT — In progress
  - dx serve built successfully (2904ms)
  - Browser opened but showed no changes (suspected stale cache)
  - Full investigation completed: code ✓, CSS ✓, build ✓, layout ✓
</completed_work>

<remaining_work>

- Task 2: Re-run UAT after hard browser refresh
  - Open http://127.0.0.1:8080 in incognito OR do Cmd+Shift+R
  - Walk through all 11 verification steps in 26.4-04-PLAN.md Task 2
  - Type "approved" if all pass; describe failure if any step fails
- Post-UAT (if approved):
  - Create .planning/phases/26.4-web-ui-side-tabs-panel/26.4-04-SUMMARY.md
  - Run /gsd-verify-phase 26.4
  - Update ROADMAP.md, STATE.md
  - Commit watermark files (warp-ih.css + app.rs) or stash them
  - Advance to next phase
</remaining_work>

<decisions_made>

- All 4 plan-04 edits verified correct in warp_hermes.rs (code inspection + cargo check)
- CSS for .wh-side-tabs, .wh-side-tab, .wh-side-info-* all present in warp-ih.css
- Uncommitted changes to warp-ih.css + app.rs are a shield watermark feature — unrelated to plan 26.4 tab strip; should be committed or stashed separately
- Browser cache identified as suspected root cause for UAT failure (not a code/CSS bug)
</decisions_made>

<blockers>

- **UAT gate**: Browser showed old UI (no tab strip). Likely stale WASM cache. Fix: Cmd+Shift+R or incognito window.
- **If tab strip still missing after hard refresh**: Open DevTools > Elements and check whether `div.wh-side-tabs` is present in the DOM. Present = CSS issue. Absent = WASM issue (rebuild or cargo clean).
</blockers>

## Required Reading (in order)

1. `.planning/phases/26.4-web-ui-side-tabs-panel/26.4-04-PLAN.md` — Task 2 lists the 11 UAT verification steps
2. `crates/iron_hermes_ui/src/components/warp_hermes.rs` (lines 61, 388, 599, 817) — verify all 4 edits are in place before re-running UAT

## Infrastructure State

- `dx serve`: Running at http://127.0.0.1:8080 (may need restart if session ended)
- Command to restart: `dx serve --package iron_hermes_ui` from workspace root

## Pre-Execution Critique Required

_Not applicable — no design artifact pending critique. This is a UAT gate only._

<context>
Plan 26.4-04 is the final plan of the phase. Tasks 1-4 (code edits) are done and committed. Only the visual UAT remains. All investigation in this session confirmed the code is correct — the tab strip, INFO cards, signal wiring, and CSS are all in place. The only open question is whether the browser will render it correctly after cache is cleared.

The uncommitted watermark changes (warp-ih.css + app.rs) were from a previous session and are unrelated to 26.4. They add a faint shield watermark behind the side panel. These should be addressed (commit or stash) before the phase is closed.
</context>

<next_action>
Start with: Hard-refresh the browser (Cmd+Shift+R) or open http://127.0.0.1:8080 in an incognito window. Then walk through the 11 UAT steps in 26.4-04-PLAN.md Task 2. If the tab strip is visible and all 11 steps pass, type "approved".
</next_action>
