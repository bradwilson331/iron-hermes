---
phase: 26.4
plan: 03
subsystem: iron_hermes_ui
tags: [dioxus, component, agent-panel, rsx, tab-strip]
dependency_graph:
  requires: [26.4-02]
  provides: [AgentPanel-tab-strip, AgentPanel-INFO-view]
  affects: [warp_hermes.rs, agent_panel.rs]
tech_stack:
  added: []
  patterns: [ReadSignal-prop-coercion, EventHandler-usize-callback, RSX-conditional-branch]
key_files:
  created: []
  modified:
    - crates/iron_hermes_ui/src/components/shell/agent_panel.rs
    - crates/iron_hermes_ui/src/components/warp_hermes.rs
decisions:
  - "active_side_tab signal declared in WarpHermes alongside active_tab; on_side_tab_click callback is a plain closure (not mut) since it only calls signal.set()"
  - "memory_enabled: false hardcoded at AgentPanel call site — ConfigSummary does not yet have this field; Plan 04 will wire the real value"
  - "context_length extracted from config_summary destructure (was previously 2-tuple, now 3-tuple) — avoids needing a separate signal"
  - "warp_hermes.rs call-site update is a Rule 3 auto-fix (blocking compile error) not a separate plan task"
metrics:
  duration: 2 min
  completed: "2026-05-06T03:58:58Z"
  tasks: 1
  files: 2
---

# Phase 26.4 Plan 03: AgentPanel Tab Strip + INFO View Summary

AgentPanel extended to 9 props with `.wh-side-tabs` strip (AGENT/INFO buttons), conditional AGENT/INFO content branch, and SESSION + CONFIG info cards using ReadSignal prop coercion pattern.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Extend AgentPanel signature, add tab strip, and add INFO view | a2d139a | agent_panel.rs, warp_hermes.rs |

## What Was Built

### `agent_panel.rs` — Full Rewrite

Extended `AgentPanel` from 1 prop (`messages`) to 9 props:
- `active_side_tab: ReadSignal<usize>` — tab index signal from parent
- `on_side_tab_click: EventHandler<usize>` — click callback to parent
- `session_id: ReadSignal<String>` — for INFO SESSION card
- `token_budget: ReadSignal<TokenBudget>` — for INFO tokens row
- `model_label: String`, `provider_label: String`, `context_length: u32`, `memory_enabled: bool` — for INFO CONFIG card

RSX structure:
1. `.wh-side-head` — unchanged (Sigil + HERMES + personality pill)
2. `.wh-side-tabs` — new strip with two hardcoded `button.wh-side-tab` elements; `is-active` toggled via `class: if 0 == active_side_tab() { "is-active" }` (Pitfall 4: no loop, literal indices)
3. Conditional branch:
   - `active_side_tab() == 0` → `.wh-side-scroll` (existing message iteration, byte-identical to old implementation)
   - `else` → `.wh-side-info` with two `.wh-side-info-card` sections (SESSION: id/messages/tokens; CONFIG: model/provider/context/memory)

Empty session id renders `—` (em dash U+2014); memory renders `yes`/`no` lowercase.

### `warp_hermes.rs` — Call-site wiring (Rule 3 auto-fix)

- Added `active_side_tab` signal declaration after `active_tab`
- Added `on_side_tab_click` closure (calls `active_side_tab.set(idx)`)
- Extended `config_summary` destructure from 2-tuple to 3-tuple to extract `context_length`
- Updated `AgentPanel` call site with all 9 props

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Updated warp_hermes.rs AgentPanel call site**
- **Found during:** Task 1 cargo check
- **Issue:** `warp_hermes.rs:809` still called `AgentPanel { messages: messages }` with the old 1-prop signature, causing E0061 compile error
- **Fix:** Added `active_side_tab` signal + `on_side_tab_click` callback to WarpHermes; extended config_summary destructure to 3-tuple; updated AgentPanel call site with all 9 props
- **Files modified:** `crates/iron_hermes_ui/src/components/warp_hermes.rs`
- **Commit:** a2d139a

## Known Stubs

| File | Location | Value | Reason |
|------|----------|-------|--------|
| `crates/iron_hermes_ui/src/components/warp_hermes.rs` | line 824 | `memory_enabled: false` | `ConfigSummary` does not yet have a `memory_enabled` field; Plan 04 (WarpHermes wiring) will add the field to `ConfigSummary` and wire the real value |

The stub does not prevent Plan 03's goal — AgentPanel renders correctly with `memory_enabled: false`; "no" will display in the INFO CONFIG card until Plan 04 wires the real value.

## Threat Flags

No new threat surface introduced. All RSX text interpolation uses Dioxus 0.7 auto-escaping (no `dangerous_inner_html`). T-26.4-07 and T-26.4-10 mitigations implemented: `session_id().is_empty()` guard renders em dash instead of empty value.

## Self-Check: PASSED

| Item | Result |
|------|--------|
| `agent_panel.rs` exists | FOUND |
| `warp_hermes.rs` exists | FOUND |
| `26.4-03-SUMMARY.md` exists | FOUND |
| Commit a2d139a exists | FOUND |
| `cargo check -p iron_hermes_ui --features web` | Finished (0 errors, pre-existing warnings only) |
