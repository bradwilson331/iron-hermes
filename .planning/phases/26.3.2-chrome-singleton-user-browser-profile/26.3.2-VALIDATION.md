---
phase: 26.3.2
slug: chrome-singleton-user-browser-profile
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-12
---

# Phase 26.3.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `26.3.2-RESEARCH.md` § Validation Architecture — the planner refines the Per-Task map once plan/task IDs exist.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in `#[test]` (sync) |
| **Config file** | none — workspace-level `cargo test` |
| **Quick run command** | `cargo test -p ironhermes-core --lib -- browser_profile` |
| **Full suite command** | `cargo test -p ironhermes-core -p ironhermes-tools -p ironagent-tools-api` |
| **Estimated runtime** | ~10 seconds (quick) / ~60–120 s (full) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-core --lib -- browser_profile`
- **After every plan wave:** Run `cargo test -p ironhermes-core -p ironhermes-tools -p ironagent-tools-api`
- **Before `/gsd-verify-work`:** `cargo test --workspace` must be green
- **Max feedback latency:** ~10 seconds

---

## Per-Task Verification Map

> Planner fills Task IDs / Plan / Wave once plans exist. Requirement IDs are the D-NN decisions from `26.3.2-CONTEXT.md` (no REQUIREMENTS.md entries this phase). Suggested test IDs SL-01..SL-08 below.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | D-01 (SL-01) | — | no lock present → `UseProfile`, nothing deleted | unit | `cargo test -p ironhermes-core --lib -- browser_profile::tests::no_lock_returns_use_profile` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-01 (SL-02) | T-26.3.2-02 | unparseable lock (regular file) → `UseProfile`, NOT deleted | unit | `cargo test -p ironhermes-core --lib -- browser_profile::tests::unparseable_regular_file_returns_use_profile_without_deletion` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-01 (SL-03) | T-26.3.2-02 | unparseable symlink target (no `-<digits>`) → `UseProfile`, NOT deleted | unit | `cargo test -p ironhermes-core --lib -- browser_profile::tests::unparseable_bad_target_returns_use_profile_without_deletion` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-02 (SL-04) | T-26.3.2-01 | stale lock (dead pid `i32::MAX as u32`) → `UseProfile` + `SingletonLock` removed | unit | `cargo test -p ironhermes-core --lib -- browser_profile::tests::stale_lock_returns_use_profile_and_removes_sentinel` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-04 (SL-05) | — | stale lock → all 3 sentinels (`SingletonLock`/`SingletonSocket`/`SingletonCookie`) removed | unit | `cargo test -p ironhermes-core --lib -- browser_profile::tests::stale_lock_also_removes_socket_and_cookie` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-05 (SL-06) | T-26.3.2-03 | live lock (pid = `std::process::id()`) → `UseEphemeral` + `SingletonLock` NOT deleted | unit | `cargo test -p ironhermes-core --lib -- browser_profile::tests::live_lock_returns_use_ephemeral_and_preserves_sentinel` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-01/D-03 (SL-07) | — | target parser splits on LAST `-` (regression: `Brads-MacBook-Pro.local-20704`) | unit | `cargo test -p ironhermes-core --lib -- browser_profile::tests::parse_singleton_target_handles_hostname_with_dashes` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-07 (SL-08) | — | both `browser_session.rs` `spawn()` call sites compile against the shared helper (zero drift) | compile | `cargo build -p ironhermes-tools -p ironagent-tools-api` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/ironhermes-core/src/browser_profile.rs` — new file: the `reconcile_singleton_lock(&Path) -> SingletonOutcome` helper + `#[cfg(unix)]` pid-liveness/hostname helpers + the `#[cfg(test)] mod tests` with fixtures (stale / live / unparseable symlinks via `std::os::unix::fs::symlink` in a `tempfile::TempDir`) and tests SL-01..SL-07
- [ ] `crates/ironhermes-core/src/lib.rs` — add `pub mod browser_profile;`
- [ ] Both `crates/ironhermes-tools/src/browser_session.rs` and `crates/ironagent-tools-api/src/browser_session.rs` — call the helper before the `.user_data_dir(...)` decision; branch on `UseProfile` vs `UseEphemeral` (SL-08 = these compile)

*No existing test files are modified; no new test files are created (all tests live in `browser_profile.rs`).*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| End-to-end: with a real stale `SingletonLock` left in `$HERMES_HOME/browser-profile`, a `browser_navigate` tool call no longer aborts with `Failed to create a ProcessSingleton` | D-01/D-02/D-04 | Requires a real Chromium binary + a real `$HERMES_HOME`; not exercised in CI | 1. `kill -9` a running hermes browser session (or `ln -s "$(hostname)-99999999" ~/.ironhermes/browser-profile/SingletonLock`). 2. Run an agent turn that calls `browser_navigate`. 3. Confirm it succeeds and a `warn!` line reports the stale-lock cleanup. |
| Live-lock ephemeral fallback: with the operator's own Chrome running on the profile dir, `browser_navigate` still works (degraded, no persistence) and logs a `warn!` | D-05 | Requires two real Chromium processes contending for one profile dir | 1. Launch Chrome with `--user-data-dir=~/.ironhermes/browser-profile`. 2. Run an agent turn that calls `browser_navigate`. 3. Confirm it succeeds and a `warn!` line says the session is ephemeral / not persisted. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (`browser_profile.rs` + lib.rs registration + both call sites)
- [ ] No watch-mode flags
- [ ] Feedback latency < 10 s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
