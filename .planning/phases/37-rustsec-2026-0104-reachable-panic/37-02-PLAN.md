---
phase: 37-rustsec-2026-0104-reachable-panic
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - Cargo.toml
  - crates/iron_hermes_ui/Cargo.toml
  - crates/ironhermes-exec/Cargo.toml
  - Cargo.lock
autonomous: true
requirements: [VER-01, VER-02, VER-03, VER-04]
must_haves:
  truths:
    - "Root [workspace.package].version is 0.2.0"
    - "crates/iron_hermes_ui/Cargo.toml version is 0.2.0 (hardcoded, not workspace-inherited)"
    - "crates/ironhermes-exec/Cargo.toml version is 0.2.0 (hardcoded, not workspace-inherited)"
    - "The CLI --version output reports 0.2.0"
  artifacts:
    - path: "Cargo.toml"
      provides: "[workspace.package] version = 0.2.0"
      contains: "version = \"0.2.0\""
    - path: "crates/iron_hermes_ui/Cargo.toml"
      provides: "hardcoded package version = 0.2.0"
      contains: "version = \"0.2.0\""
    - path: "crates/ironhermes-exec/Cargo.toml"
      provides: "hardcoded package version = 0.2.0"
      contains: "version = \"0.2.0\""
  key_links:
    - from: "[workspace.package].version = 0.2.0"
      to: "CARGO_PKG_VERSION at compile time"
      via: "ironhermes-cli main.rs env!(\"CARGO_PKG_VERSION\") (no source edit)"
      pattern: "0\\.2\\.0"
---

<objective>
Bump the IronHermes workspace version from `0.1.0` to `0.2.0`.

Purpose: Mark the release boundary that includes the RUSTSEC-2026-0104 security
remediation (Plan 01). Most crates inherit the version via
`version.workspace = true` and pick up the bump automatically; exactly two crates
hardcode their own `version` and need explicit edits.

Output: `version = "0.2.0"` in three files — the root `[workspace.package]` and
the two hardcoded-version crates (`iron_hermes_ui`, `ironhermes-exec`) — plus a
confirmation that the CLI `--version` reports 0.2.0 (no source change needed; it
reads `env!("CARGO_PKG_VERSION")`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-rustsec-2026-0104-reachable-panic/37-RESEARCH.md
@.planning/phases/37-rustsec-2026-0104-reachable-panic/37-VALIDATION.md

<interfaces>
<!-- Exactly three Cargo.toml version edits. Verified line locations: -->
- Root Cargo.toml: `[workspace.package]` at line 50, `version = "0.1.0"` at line 51 → change to "0.2.0".
- crates/iron_hermes_ui/Cargo.toml: `[package]` at line 1, `version = "0.1.0"` at line 3 (hardcoded — does NOT use version.workspace=true) → change to "0.2.0".
- crates/ironhermes-exec/Cargo.toml: `[package]` at line 1, `version = "0.1.0"` at line 3 (hardcoded) → change to "0.2.0".

<!-- All ~15 other workspace member crates use `version.workspace = true` and
     inherit automatically — DO NOT edit them. -->
<!-- ironhermes-cli/src/main.rs uses env!("CARGO_PKG_VERSION") at lines 947 and
     3290 — picks up the workspace bump at compile time. NO source edit needed. -->
<!-- This plan depends on Plan 01: Plan 01 added [patch.crates-io] to root
     Cargo.toml and regenerated Cargo.lock. This plan edits the same root
     Cargo.toml (version line only) — hence wave 2, after Plan 01. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 0.2.0 in the three version-bearing Cargo.toml files</name>
  <files>Cargo.toml, crates/iron_hermes_ui/Cargo.toml, crates/ironhermes-exec/Cargo.toml</files>
  <read_first>Cargo.toml (line 50-51 [workspace.package].version), crates/iron_hermes_ui/Cargo.toml (line 1-3), crates/ironhermes-exec/Cargo.toml (line 1-3) — confirm each shows version = "0.1.0" and that Plan 01's [patch.crates-io] block is present in root Cargo.toml before editing</read_first>
  <action>
    Change `version = "0.1.0"` to `version = "0.2.0"` in exactly three places:
    (1) root `Cargo.toml` under `[workspace.package]` (line 51) — VER-01;
    (2) `crates/iron_hermes_ui/Cargo.toml` under `[package]` (line 3, hardcoded,
    NOT `version.workspace = true`) — VER-02;
    (3) `crates/ironhermes-exec/Cargo.toml` under `[package]` (line 3, hardcoded)
    — VER-03. Do NOT touch any of the ~15 crates that use
    `version.workspace = true` — they inherit the root bump automatically. Do NOT
    edit any source file for the CLI `--version` string — it resolves
    `env!("CARGO_PKG_VERSION")` at compile time (VER-04 is satisfied by the
    workspace bump). Preserve the Plan 01 `[patch.crates-io]` block in root
    Cargo.toml — only the version line changes.
  </action>
  <verify>
    <automated>grep -q '^version = "0.2.0"' Cargo.toml && grep -q '^version = "0.2.0"' crates/iron_hermes_ui/Cargo.toml && grep -q '^version = "0.2.0"' crates/ironhermes-exec/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - VER-01: `grep -q '^version = "0.2.0"' Cargo.toml` exits 0
    - VER-02: `grep -q '^version = "0.2.0"' crates/iron_hermes_ui/Cargo.toml` exits 0
    - VER-03: `grep -q '^version = "0.2.0"' crates/ironhermes-exec/Cargo.toml` exits 0
    - Plan 01 patch preserved: `grep -q 'rustls-webpki = { version = "=0.103.13" }' Cargo.toml` exits 0
    - No stray 0.1.0 workspace.package line remains: `grep -c '^version = "0.1.0"' Cargo.toml` returns 0
  </acceptance_criteria>
  <done>All three version-bearing Cargo.toml files read version = "0.2.0"; the Plan 01 [patch.crates-io] block is intact; no version.workspace=true crate was edited.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate lockfile and confirm CLI --version reports 0.2.0</name>
  <files>Cargo.lock</files>
  <read_first>.planning/phases/37-rustsec-2026-0104-reachable-panic/37-VALIDATION.md (VER-04 command: cargo run -p ironhermes-cli -- --version | grep -q 0.2.0)</read_first>
  <action>
    Run `cargo build --workspace` (or `cargo update --workspace`) to refresh
    `Cargo.lock` package version entries for the bumped crates and confirm the
    workspace still compiles after the version change. Then run
    `cargo run -p ironhermes-cli -- --version` and confirm the output contains
    `0.2.0` (VER-04) — this proves the `env!("CARGO_PKG_VERSION")` path picked up
    the workspace bump with no source edit. Do NOT modify the
    `[patch.crates-io]` block or revert the rustls-webpki 0.103.13 pin from
    Plan 01.
  </action>
  <verify>
    <automated>cargo run -p ironhermes-cli -- --version | grep -q 0.2.0</automated>
  </verify>
  <acceptance_criteria>
    - VER-04: `cargo run -p ironhermes-cli -- --version | grep -q 0.2.0` exits 0
    - Workspace still builds: `cargo build --workspace` exits 0
    - Plan 01 security patch intact: `cargo tree -i rustls-webpki | grep -q 0.103.13` exits 0
  </acceptance_criteria>
  <done>Cargo.lock reflects the 0.2.0 crate versions, the workspace builds, the CLI --version outputs 0.2.0, and the Plan 01 rustls-webpki 0.103.13 patch is still in effect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits version metadata only — no new untrusted-input path, no new dependency, no new network surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02-01 | Tampering | Accidental revert of Plan 01's [patch.crates-io] / rustls-webpki pin while editing root Cargo.toml version | mitigate | Task acceptance criteria re-assert `rustls-webpki = { version = "=0.103.13" }` presence and `cargo tree -i rustls-webpki | grep -q 0.103.13` after the version edit, so a regression of the security patch is caught immediately |
| T-37-02-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs occur in this plan — version-string edits only; no new dependency added |
</threat_model>

<verification>
- VER-01: `grep -q '^version = "0.2.0"' Cargo.toml`
- VER-02: `grep -q '^version = "0.2.0"' crates/iron_hermes_ui/Cargo.toml`
- VER-03: `grep -q '^version = "0.2.0"' crates/ironhermes-exec/Cargo.toml`
- VER-04: `cargo run -p ironhermes-cli -- --version | grep -q 0.2.0`
- Regression guard: `cargo tree -i rustls-webpki | grep -q 0.103.13` (Plan 01 patch still in effect)
</verification>

<success_criteria>
- All three version-bearing Cargo.toml files read `version = "0.2.0"`.
- `cargo build --workspace` exits 0.
- `cargo run -p ironhermes-cli -- --version` outputs 0.2.0.
- The Plan 01 [patch.crates-io] block and rustls-webpki 0.103.13 resolution are unchanged.
</success_criteria>

<output>
Create `.planning/phases/37-rustsec-2026-0104-reachable-panic/37-02-SUMMARY.md` when done.
</output>
