---
phase: 22.4.2.3
plan: "01"
subsystem: invariants / static-grep regression tests
tags: [test-only, invariant, banner-bleed, tui-init, static-grep]
dependency_graph:
  requires: []
  provides:
    - "INV-22.3-02 regression gate: relaxed-count + every-site-before-TUI-init form"
  affects:
    - "crates/ironhermes-cli/tests/invariants_22_3.rs (INV-22.3-02 only)"
tech_stack:
  added: []
  patterns:
    - "match_indices on &str for all-positions iteration (no new imports)"
key_files:
  created: []
  modified:
    - "crates/ironhermes-cli/tests/invariants_22_3.rs"
decisions:
  - "D-01: assert!(count >= 1) replaces assert_eq!(count, 1) — accepts 3 legitimate Phase 22.4 call sites"
  - "D-02: match_indices loop asserts every banner_pos < tui_pos instead of only the first"
  - "D-03: anchored on qualified TuiHandle::new_with_extensions to avoid INV-22.1-01 false positive"
  - "D-04: test renamed to invariant_22_3_02_banner_called_at_least_once_strictly_before_tui_init"
  - "D-05: doc-comment and assertion messages cite Phase 22.4 CONTEXT D-03 / Plan 22.4-11 + Plans 22.3-05 / Banner-Bleed Probe"
  - "D-06: match literal print_banner(); (with semicolon) preserved throughout — avoids fn definition + doc-comment false positives"
metrics:
  duration: "~5 min"
  completed: "2026-04-27"
---

# Phase 22.4.2.3 Plan 01: INV-22.3-02 Banner-Bleed Invariant Relaxation Summary

**One-liner:** Replaced `assert_eq!(count, 1)` with `assert!(count >= 1)` + `match_indices` all-positions-before-TUI-init loop in `invariant_22_3_02_banner_called_at_least_once_strictly_before_tui_init` (renamed), unblocking the `develop` milestone branch.

## Diff Range

**File:** `crates/ironhermes-cli/tests/invariants_22_3.rs`

- **Lines replaced (before):** Lines 35–66 (old INV-22.3-02 doc-comment block + function body, 32 lines)
- **Lines after:** Lines 35–108 (new doc-comment block + function body, 74 lines)
- **Net change:** +46 lines (73 insertions, 27 deletions per git)
- **Commit:** `190ea48`

## Observed Byte Offsets

All three `print_banner();` call sites appear strictly before `TuiHandle::new_with_extensions` in `main.rs` source:

| Call site | Source line | Byte offset | < tui_pos? |
|-----------|-------------|-------------|------------|
| ratatui dispatch arm 1 | main.rs:257 | 9946 | YES (delta: -25948) |
| ratatui dispatch arm 2 | main.rs:321 | 13263 | YES (delta: -22631) |
| run_chat original site | main.rs:770 | 31489 | YES (delta: -4405) |
| `TuiHandle::new_with_extensions` | main.rs:851 | 35894 | (anchor) |

All `banner_pos < tui_pos (35894)` — invariant passes correctly.

## Test Results

```
running 6 tests
test invariant_22_3_01_reset_terminal_arm_exists_in_main ... ok
test invariant_22_3_02_banner_called_at_least_once_strictly_before_tui_init ... ok
test invariant_22_3_03_cmd_clear_returns_reset_terminal_and_cmd_new_unchanged ... ok
test invariant_22_3_04_slash_commands_added_to_history_at_prompt_time ... ok
test invariant_22_3_05_rustyline_correct_api_used_in_repl_input ... ok
test invariant_22_3_06_mid_turn_dispatch_has_no_add_history ... ok

test result: ok. 6 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
```

`cargo test -p ironhermes-cli --test invariants_22_3 invariant_22_3_02_banner_called_at_least_once_strictly_before_tui_init` — 1 passed.

## Clippy Results

`cargo clippy -p ironhermes-cli --tests --no-deps -- -D warnings` exits with errors on pre-existing issues in other files (see Deferred Issues below). Zero errors reference `invariants_22_3.rs` — confirmed via `grep "invariants_22_3"` returning no output.

## Scope-Boundary Confirmation

- `crates/ironhermes-cli/src/main.rs`: byte-identical to pre-task state — `git diff --quiet -- crates/ironhermes-cli/src/main.rs` exits 0.
- `git status --porcelain crates/ironhermes-cli/` shows exactly one modified file: `M crates/ironhermes-cli/tests/invariants_22_3.rs`.
- No other invariants touched (INV-22.3-01, 03, 04, 05, 06 unchanged — all 5 still present).
- No new dev-deps, no new use-statements, no new test files.

## D-01..D-06 Evidence

| Decision | Evidence in commit |
|----------|--------------------|
| D-01: `count >= 1` replaces `count == 1` | `assert!(count >= 1, ...)` in function body; `assert_eq!(count, 1` absent (AC6 = 0) |
| D-02: every site before tui_pos | `for (banner_pos, _) in MAIN_RS.match_indices("print_banner();")` loop with `banner_pos < tui_pos` assertion |
| D-03: qualified `TuiHandle::new_with_extensions` anchor | `MAIN_RS.find("TuiHandle::new_with_extensions")` — bare `new_with_extensions` not used |
| D-04: test renamed | `fn invariant_22_3_02_banner_called_at_least_once_strictly_before_tui_init` — old name absent (AC2 = 0) |
| D-05: doc-comment + messages cite Phase 22.4 CONTEXT D-03 / Plan 22.4-11 + Plans 22.3-05 / Banner-Bleed Probe | 3 occurrences each of "Phase 22.4 CONTEXT D-03" and "Plan 22.4-11"; 2 occurrences of "Banner-Bleed Probe" |
| D-06: semicolon-form `print_banner();` throughout | 2 occurrences of `"print_banner();"` (count + match_indices); 0 occurrences of bare `"print_banner()"` without semicolon |

## Deviations from Plan

### Pre-existing Out-of-Scope Issues

**cargo fmt side-effect (Rule 3 - contained):** Running `cargo fmt -p ironhermes-cli` to check the target file reformatted many other source files including `main.rs`. All non-target files were restored via `git checkout -- .` then the saved `invariants_22_3.rs` was reapplied. Final state: only `invariants_22_3.rs` is modified.

**Pre-existing clippy failures (logged to deferred):** `cargo clippy -p ironhermes-cli --tests --no-deps -- -D warnings` fails with 25 errors in files outside this task's scope (`tui_rata/status_line.rs`, `tui_rata/app.rs`, `tui/keybindings.rs`, etc.). These are pre-existing issues predating this task — zero errors reference `invariants_22_3.rs`. Logged to deferred-items per scope-boundary rule. AC-13 from the plan (clippy exits 0) cannot be satisfied without fixing pre-existing clippy debt; this is documented here.

## Known Stubs

None. The test is fully wired — `MAIN_RS` is loaded via `include_str!` and the assertions run against live source content.

## Threat Flags

None. This is a test-only change with no new network endpoints, auth paths, or schema changes.

## Self-Check: PASSED

| Check | Result |
|-------|--------|
| SUMMARY.md exists at .planning/phases/22.4.2.3-.../22.4.2.3-01-SUMMARY.md | FOUND |
| invariants_22_3.rs exists | FOUND |
| Task commit 190ea48 exists in git log | FOUND |
| main.rs byte-identical (git diff --quiet exits 0) | UNCHANGED |
| Only invariants_22_3.rs modified under crates/ironhermes-cli/ | CONFIRMED |
