---
phase: 22.4.2.3-fix-the-pre-existing-inv-22-3-02-banner-bleed-before-milesto
verified: 2026-04-27T00:00:00Z
status: passed
score: 10/10 must-haves verified
overrides_applied: 0
re_verification: false
---

# Phase 22.4.2.3: INV-22.3-02 Banner-Bleed Invariant Relaxation — Verification Report

**Phase Goal:** Fix the pre-existing INV-22.3-02 banner-bleed regression test failure on `develop` by relaxing `assert_eq!(count, 1)` to `assert!(count >= 1)` and strengthening ordering to "every call site strictly before `TuiHandle::new_with_extensions`". Test-only edit; no production-code changes.
**Verified:** 2026-04-27
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | Test renamed to `invariant_22_3_02_banner_called_at_least_once_strictly_before_tui_init`; old name absent | VERIFIED | `grep -c "fn invariant_22_3_02_banner_called_at_least_once_strictly_before_tui_init"` = 1; old name grep = 0 |
| 2  | `assert!(count >= 1, ...)` replaces `assert_eq!(count, 1, ...)` | VERIFIED | `grep -c "count >= 1"` = 1; `grep -c "assert_eq!(count, 1"` = 0 |
| 3  | Iteration uses `match_indices("print_banner();")` — every call site checked | VERIFIED | `grep -c 'match_indices("print_banner();")'` = 1 |
| 4  | TUI-init anchor is `TuiHandle::new_with_extensions` (qualified) | VERIFIED | `grep -c 'find("TuiHandle::new_with_extensions")'` = 1 |
| 5  | Doc-comment + assertion messages cite Phase 22.4 CONTEXT D-03, Plan 22.4-11, and Plans 22.3-05 / Banner-Bleed Probe | VERIFIED | "Phase 22.4 CONTEXT D-03" = 3 occurrences; "Plan 22.4-11" = 3 occurrences; "Banner-Bleed Probe" = 2 occurrences |
| 6  | Match literal is `"print_banner();"` (with semicolon) at both match sites; bare `"print_banner()"` without semicolon absent | VERIFIED | `grep -c '"print_banner();"'` = 2; `grep -cE '"print_banner\(\)"[^;]'` = 0 |
| 7  | `cargo test -p ironhermes-cli --test invariants_22_3` reports 6 passed; 0 failed | VERIFIED | `test result: ok. 6 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` |
| 8  | `cargo test ... invariant_22_3_02_banner_called_at_least_once_strictly_before_tui_init` reports 1 passed | VERIFIED | `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 5 filtered out` |
| 9  | `git diff --quiet -- crates/ironhermes-cli/src/main.rs` exits 0 — main.rs untouched | VERIFIED | exit code 0 — no modifications to production code |
| 10 | Other 5 invariants (01, 03, 04, 05, 06) still present | VERIFIED | `grep -cE "fn invariant_22_3_(01|03|04|05|06)_"` = 5 |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/ironhermes-cli/tests/invariants_22_3.rs` | Updated INV-22.3-02 with relaxed-count + all-positions-before-TUI-init form | VERIFIED | Contains `fn invariant_22_3_02_banner_called_at_least_once_strictly_before_tui_init`, `assert!(count >= 1)`, `match_indices("print_banner();")`, and `find("TuiHandle::new_with_extensions")` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `crates/ironhermes-cli/tests/invariants_22_3.rs` | `crates/ironhermes-cli/src/main.rs` (read via `include_str!`) | `MAIN_RS.match_indices("print_banner();")` / `MAIN_RS.find("TuiHandle::new_with_extensions")` | WIRED | `MAIN_RS` const loaded via `include_str!("../src/main.rs")` at line 9; all string searches operate on live source content |

### Data-Flow Trace (Level 4)

Not applicable — this is a static-grep test, not a dynamic rendering component. The `MAIN_RS` constant is populated by `include_str!` at compile time and contains the full `main.rs` source. All assertions operate on that real source string with no runtime fetch or state management.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| INV-22.3 full suite passes (6/6) | `cargo test -p ironhermes-cli --test invariants_22_3` | `6 passed; 0 failed` | PASS |
| INV-22.3-02 renamed test passes in isolation | `cargo test -p ironhermes-cli --test invariants_22_3 invariant_22_3_02_banner_called_at_least_once_strictly_before_tui_init` | `1 passed; 0 failed` | PASS |
| main.rs production code untouched | `git diff --quiet -- crates/ironhermes-cli/src/main.rs` | exit 0 | PASS |

### Requirements Coverage

No requirement IDs are declared for this phase (internal regression-test fix). No entries in REQUIREMENTS.md are linked to this phase. Coverage: N/A.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (advisory) multiple files under `crates/ironhermes-cli/` | various | Pre-existing clippy `-D warnings` failures in `tui_rata/status_line.rs`, `tui_rata/app.rs`, `tui/keybindings.rs` and others | Advisory only | These failures predate this phase and are outside its scope. Zero clippy errors reference `invariants_22_3.rs`. The plan's clippy gate (AC-13) cannot be green until pre-existing debt is addressed separately. Not a gap for this phase. |

### Human Verification Required

None. This phase's deliverable is entirely structural: a test-file edit verified by `cargo test`. The automated test pass IS the user-facing outcome. No visual, real-time, or external-service behavior is involved.

### Gaps Summary

No gaps. All 10 must-have truths are independently verified against the actual codebase and confirmed by live `cargo test` output.

---

_Verified: 2026-04-27_
_Verifier: Claude (gsd-verifier)_
