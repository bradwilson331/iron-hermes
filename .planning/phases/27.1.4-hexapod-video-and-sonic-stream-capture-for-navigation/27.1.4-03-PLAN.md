---
phase: 27.1.4-hexapod-video-and-sonic-stream-capture-for-navigation
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - skills/hexapod/SKILL.md
autonomous: true
requirements: [D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-01, D-02, D-03, D-04, D-05, D-06, D-07]
must_haves:
  truths:
    - "SKILL.md documents the stream_distance, camera_pan, and camera_tilt actions with their wire formats, parameter ranges, and clamping"
    - "SKILL.md documents the new hexapod_video tool — its connection (port 8002, 4-byte LE prefix), the capture_frame action, error strings, and the camera-then-capture usage pattern"
    - "An agent can invoke every Phase 27.1.4 action using the skill doc alone (no inline prompt guidance needed)"
  artifacts:
    - path: "skills/hexapod/SKILL.md"
      provides: "Updated protocol reference covering all 15 hexapod_tcp actions + the hexapod_video tool"
      contains: "hexapod_video"
  key_links:
    - from: "SKILL.md actions table"
      to: "stream_distance / camera_pan / camera_tilt rows + hexapod_video section"
      via: "documentation rows matching the wire formats implemented in Plans 01 and 02"
      pattern: "CMD_CAMERA#"
---

<objective>
Update `skills/hexapod/SKILL.md` so the agent has full protocol knowledge of the three new
`hexapod_tcp` actions (stream_distance, camera_pan, camera_tilt) and the new `hexapod_video`
tool — without needing any inline prompt guidance.

Purpose: Phase 27.1.x's design principle is "the agent invokes any protocol action using the
skill doc alone." This plan extends that coverage to the navigation-sensor additions.
Output: Modified `skills/hexapod/SKILL.md` — three new rows in the actions and parameter-range
tables, an updated frontmatter description, and a new `## hexapod_video Tool` section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27.1.4-hexapod-video-and-sonic-stream-capture-for-navigation/27.1.4-CONTEXT.md
@.planning/phases/27.1.4-hexapod-video-and-sonic-stream-capture-for-navigation/27.1.4-PATTERNS.md
@skills/hexapod/SKILL.md
@crates/ironhermes-tools/src/hexapod_tcp.rs
@.planning/phases/27.1.4-hexapod-video-and-sonic-stream-capture-for-navigation/27.1.4-01-SUMMARY.md
@.planning/phases/27.1.4-hexapod-video-and-sonic-stream-capture-for-navigation/27.1.4-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stream_distance / camera_pan / camera_tilt + hexapod_video to SKILL.md</name>
  <files>skills/hexapod/SKILL.md</files>
  <read_first>
    - skills/hexapod/SKILL.md (entire file — line 3 frontmatter description, lines 28–43 actions table, lines 45–60 walk/rotate/led detail subsections, lines 62–73 parameter-ranges table, lines 85–94 error-strings table, line 98 session-end note)
    - .planning/phases/27.1.4-hexapod-video-and-sonic-stream-capture-for-navigation/27.1.4-PATTERNS.md (section "skills/hexapod/SKILL.md (modify — add rows + new tool section)" — exact rows and the full hexapod_video section template)
    - .planning/phases/27.1.4-hexapod-video-and-sonic-stream-capture-for-navigation/27.1.4-01-SUMMARY.md (confirm the final constant names/values and the exact stream_distance return string as implemented)
    - .planning/phases/27.1.4-hexapod-video-and-sonic-stream-capture-for-navigation/27.1.4-02-SUMMARY.md (confirm the final hexapod_video error strings and read-timeout value as implemented)
    - .planning/phases/27.1.4-hexapod-video-and-sonic-stream-capture-for-navigation/27.1.4-CONTEXT.md (D-08..D-15 for stream_distance/camera; D-01..D-07 for hexapod_video)
  </read_first>
  <action>
    Update `skills/hexapod/SKILL.md`:

    1. Frontmatter `description` (line 3): change the count from "12 hexapod_tcp actions" to "15 hexapod_tcp actions" and append a clause mentioning the companion `hexapod_video` tool (capture_frame on video port 8002). Add `video` to the `tags` list if appropriate.

    2. Actions table (lines 28–43): append three rows in the existing column format `| Action | Parameters | Wire Command | Returns | Notes |`:
       - `| stream_distance | samples | \`CMD_SONIC\n\` × N with ~200ms sleep | \`"Distances: [d1, d2, ...] cm \| min=M max=X avg=A.B"\` | samples clamped to [1, 20]; 20 × 200ms = 4s max |`
       - `| camera_pan | x | \`CMD_CAMERA#{x}#90\n\` | \`"OK"\` | x clamped to 50–180; y defaults to midpoint 90 (CMD_CAMERA sets both axes) |`
       - `| camera_tilt | y | \`CMD_CAMERA#115#{y}\n\` | \`"OK"\` | y clamped to 0–180; x defaults to midpoint 115 (CMD_CAMERA sets both axes) |`
       (Use the actual return-string format and constant values from `27.1.4-01-SUMMARY.md` if they differ from the above.)

    3. Add a short subsection after the existing detail subsections (after the "LED Off vs LED Color-Zero" block) explaining: `CMD_CAMERA#x#y\n` always sets BOTH the camera's pan and tilt servos; because `camera_pan` and `camera_tilt` are exposed as separate single-axis actions, the unused axis is sent at its protocol midpoint (`camera_pan` sends `y=90`, `camera_tilt` sends `x=115`); the camera gimbal is independent of the head servos (`CMD_HEAD` controls the head, `CMD_CAMERA` controls the camera mount).

    4. Parameter-ranges table (lines 62–73): append three rows in the `| Parameter | Action | Range | Clamping |` format:
       - `| samples | stream_distance | 1–20 | clamped to [1, 20]; 20 × 200ms = 4s max |`
       - `| x | camera_pan | 50–180 | clamped to [50, 180]; CAMERA_PAN_MIN / CAMERA_PAN_MAX constants |`
       - `| y | camera_tilt | 0–180 | clamped to [0, 180]; CAMERA_TILT_MIN / CAMERA_TILT_MAX constants |`

    5. Append a new top-level section after the "Session End Behavior" section (the hexapod_video tool is not part of hexapod_tcp, so it gets its own section). Use the template from PATTERNS.md "New `hexapod_video` tool section": a 1–2 sentence intro; a Connection table (Env var `HEXAPOD_IP` shared with hexapod_tcp; Port `8002`; Protocol "4-byte little-endian frame length + JPEG bytes"; Pattern "one connection per call, no persistent socket"); an Actions table with the single `capture_frame` row (`| capture_frame | — | "data:image/jpeg;base64,<data>" | Connects to port 8002, reads one JPEG frame, disconnects |`); an Error Strings table (HEXAPOD_IP not set → `"Error: HEXAPOD_IP env var not set — cannot connect to robot"`; port 8002 busy → `"Error: video port 8002 is busy — another client is connected. Disconnect the other client and retry."`; read timeout → `"Error: read timed out after 5s waiting for video frame — camera may not be streaming"`; frame read error → `"Error: cannot read video frame from robot at {addr} — camera may not be running"` — use the actual strings from `27.1.4-02-SUMMARY.md`); and a "Usage Pattern" block showing `camera_pan(x=115)` → `camera_tilt(y=90)` → `capture_frame()` and a note that the desktop PyQt5 client (`ui_client.py`) occupies port 8002 when running and must be disconnected first.

    Preserve all existing content — only add rows/sections, do not remove or restructure the existing 12-action documentation.
  </action>
  <verify>
    <automated>grep -c -e 'stream_distance' -e 'camera_pan' -e 'camera_tilt' -e 'hexapod_video' -e 'capture_frame' skills/hexapod/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `skills/hexapod/SKILL.md` contains a `| stream_distance |` row in the actions table referencing `CMD_SONIC` and the `"Distances: [...] cm | min=... max=... avg=..."` return format
    - `skills/hexapod/SKILL.md` contains a `| camera_pan |` row with wire `CMD_CAMERA#{x}#90` and a `| camera_tilt |` row with wire `CMD_CAMERA#115#{y}`
    - `skills/hexapod/SKILL.md` contains a `## hexapod_video` section header
    - The hexapod_video section documents port `8002`, the `capture_frame` action, the `data:image/jpeg;base64,` return prefix, and the busy-port error string `"Error: video port 8002 is busy"`
    - `skills/hexapod/SKILL.md` parameter-ranges table contains rows for `samples`, `x` (camera_pan), and `y` (camera_tilt)
    - `skills/hexapod/SKILL.md` frontmatter `description` says "15 hexapod_tcp actions" (not "12")
    - `grep -c -e 'stream_distance' -e 'camera_pan' -e 'camera_tilt' -e 'hexapod_video' -e 'capture_frame' skills/hexapod/SKILL.md` returns ≥ 7
    - No existing actions table row (walk, stop, read_battery, read_distance, relax_servos, rotate, head_pan, head_tilt, buzzer_on, buzzer_off, led, led_off) was removed
  </acceptance_criteria>
  <done>SKILL.md updated with the three new hexapod_tcp action rows, the camera joint-axis explanation, the three new parameter-range rows, the updated frontmatter description, and a new hexapod_video tool section; existing content preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill doc → LLM system prompt | SKILL.md content is injected into the agent's context — it is already security-scanned by the Phase 19 skills runtime before injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.1.4-10 | Tampering / prompt injection | text added to SKILL.md | accept | this plan only adds plain protocol-reference tables and prose written by the executor (no external/user input); the Phase 19 skills runtime security-scans skill content on load regardless; no new attack surface |
| T-27.1.4-11 | Information Disclosure | SKILL.md documents the robot's wire protocol | accept | the Freenove protocol is public (open-source kit); documenting it in a local skill file discloses nothing not already in the vendor repo |
</threat_model>

<verification>
- `grep -c -e 'stream_distance' -e 'camera_pan' -e 'camera_tilt' -e 'hexapod_video' -e 'capture_frame' skills/hexapod/SKILL.md` returns ≥ 7.
- `skills/hexapod/SKILL.md` still contains all 12 original action rows (walk … led_off) — `grep -c '| walk \| stop' skills/hexapod/SKILL.md` style spot-check, or visually confirm the original table is intact.
- The new `## hexapod_video` section is present and references port 8002 and `capture_frame`.
</verification>

<success_criteria>
- SKILL.md documents all 15 hexapod_tcp actions (12 prior + stream_distance + camera_pan + camera_tilt) with wire formats, parameter ranges, and clamping.
- SKILL.md has a `## hexapod_video Tool` section covering the connection, the `capture_frame` action, error strings, and the camera-then-capture usage pattern.
- Frontmatter description updated to "15 hexapod_tcp actions" and mentions the hexapod_video companion tool.
- All pre-existing SKILL.md content is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/27.1.4-hexapod-video-and-sonic-stream-capture-for-navigation/27.1.4-03-SUMMARY.md`
</output>
