# Phase 22.4.2.1 Discussion Log

**Date:** 2026-04-26
**Mode:** discuss (default)
**Outcome:** CONTEXT.md written; user fired `/gsd-next` to advance after providing rich context in a single multiSelect answer.

---

## Q1: Which gray areas to discuss?

**Multi-select options presented:**
- ☐ Cron surface scope — Which cron call site(s) are broken (CLI / tool / `/cron` slash / runner)?
- ☐ Telegram delivery scope — Which TG path (cron→TG outbound, real-time send, streaming edit, multimodal)?
- ☐ Symptoms & reproduction — Failure modes, known reproducer, when last worked
- ☐ Fix depth + regression coverage — Band-aid vs full audit, regression tests

**User selection:** All four areas, plus rich freeform notes.

**User's freeform notes (verbatim):**
> UI says cron not avaliable, no cron management UI. cron list and other command have been available. In .ironhermes cron shows successful but it was not sent to telegram. Also ctrl+c does not work on the ironhermes in gateway mode. All of these have worked and been fixed before, since phase 1.1.

**Decisions extracted from this single answer:**

| User statement | Decision captured |
|---|---|
| "UI says cron not avaliable, no cron management UI" | D-04, D-05, D-06: wire `/cron` slash to read-side cron operations via `CommandContext.cron_store`. The literal `"No cron management UI"` is the test anchor for INV-22.4.2.1-01. |
| "cron list and other command have been available" | D-01 / Phase Boundary: CLI subcommands (`hermes cron list/...`) are out of scope — they work. |
| "In .ironhermes cron shows successful but it was not sent to telegram" | D-07, D-08, D-09: restore the missing platform-dispatch step in `execute_cron_job`. Cross-checked with code — runner.rs:781-938 confirms the function ends after `complete_job_run` with no dispatch to `job.deliver`. |
| "ctrl+c does not work on the ironhermes in gateway mode" | D-10, D-11: restore SIGINT graceful shutdown for `hermes gateway`. Fix pattern reuses Phase 21.2 Plan 11's CancellationToken approach. |
| "All of these have worked and been fixed before, since phase 1.1" | D-01: framed as three regressions of v1.1-validated capabilities, NOT new features. D-03: each gets a regression test so they don't break again. |

---

## Code findings during the discussion

While building gray areas I read the following files and confirmed the user's reports against the source:

- **`/cron` slash stub confirmed:** `crates/ironhermes-core/src/commands/handlers.rs:1102` — `"cron" => "No cron management UI"` in the `todo_stub` match. User's UI message matches this code path.
- **Cron→TG delivery gap confirmed:** `crates/ironhermes-gateway/src/runner.rs:781-938` (`execute_cron_job`) — agent runs (line 883), output captured (line 889), `ResponseSent` hook fires (lines 894-903), `complete_job_run` records success (line 906), function returns `Ok(())` (line 910). **No dispatch to `job.deliver` exists in this function.** The `deliver` field is set at job creation time (`cron.rs:36-37`) but never consumed in the runner.
- **Cron CLI rendering helpers identified:** `crates/ironhermes-cli/src/cron.rs` — `cmd_list` (127), `render_job_details` (270), `cmd_status` (513). These are the helpers the slash handler should reuse (D-06).
- **`JobStore` API surface:** `crates/ironhermes-cron/src/store.rs:95-425` — all read-side methods needed by `/cron` (`list_jobs`, `get_job`, `find_job`) are already public.
- **Phase 21.2 Plan 11 GAP-8 fix** (CancellationToken + `kill_on_drop(true)` + 2s `JoinHandle` timeout) is the precedent for D-10/D-11 (Ctrl+C gateway shutdown).

---

## Scope creep redirected

None. The user kept the discussion tight and only flagged the three regressions. The `cron list / get / status / pause / resume / run / remove` slash UI was scoped to read-side only (D-04) — `create` / `edit` interactive flows deferred because the slash framework doesn't standardize multi-`--flag` parsing.

---

## Deferred ideas captured

12 items. See CONTEXT.md `<deferred>` section. Highlights:
- Interactive `/cron create` and `/cron edit` from slash (multi-flag parsing limitation)
- Webhook delivery dispatch fix (researcher checks if same gap; if yes, follow-up phase)
- `/cron run` interactive output streaming
- Cron delivery retry on TG send failure
- Other-platform delivery (Discord, Slack — PROJECT.md out-of-scope)
- Pagination for `/cron list`
- Refactor of `execute_cron_job` to a trait-based dispatcher
- Unified gateway `tokio::select!` shape
- Removing `todo_stub` entirely (14 other commands still stubbed)
- Multi-target `Vec<DeliveryTarget>` fan-out

## Reviewed todos (not folded)

`gsd-sdk query todo.match-phase 22.4.2.1` returned 3 matches, all weak (max score 0.4), all unrelated (config wizard / Phase 18 UAT). None folded.

---

## How the discussion ended

User fired `/gsd-next` mid-flow. Per `/gsd-next` routing: Phase 22.4.2.1 has no CONTEXT.md → Route 2 → `/gsd-discuss-phase 22.4.2.1` (the workflow already in flight). The user's single-message answer carried enough decision content to close out the discuss phase without further turns. CONTEXT.md was written based on the user's input + on-demand code reads. Next-step routing after CONTEXT.md commit will resolve to `/gsd-plan-phase 22.4.2.1` (Route 3: phase has CONTEXT.md but no plans).
