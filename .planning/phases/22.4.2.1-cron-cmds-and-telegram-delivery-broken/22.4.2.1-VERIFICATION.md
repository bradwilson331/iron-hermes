---
phase: 22.4.2.1-cron-cmds-and-telegram-delivery-broken
verified: 2026-04-26T00:00:00Z
status: passed
score: 21/21 must-haves verified
overrides_applied: 0
---

# Phase 22.4.2.1: Cron cmds and Telegram delivery broken — Verification Report

**Phase Goal:** Restore three v1.1-validated regressions: (1) `/cron` slash command UI, (2) cron->Telegram delivery, (3) Ctrl+C graceful shutdown for `hermes gateway`.
**Verified:** 2026-04-26
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `/cron` slash command no longer returns stub "No cron management UI" | VERIFIED | `grep -v '^[[:space:]]*//' handlers.rs \| grep -c "No cron management UI"` → 0 |
| 2 | `fn cmd_cron` exists and is routed via `"cron" => cmd_cron(args, ctx)` dispatch arm | VERIFIED | handlers.rs:697 (fn), handlers.rs:69 (dispatch arm) |
| 3 | `/cron` sub-dispatches all 7 read-side subcommands (list/status/get/pause/resume/run/remove) | VERIFIED | handlers.rs:703-757 — all 7 arms present with None\|Some("list") default |
| 4 | `CronJobReader` trait defined in `ironhermes-core::commands::context` (no circular dep) | VERIFIED | context.rs:93; `ironhermes-cron` NOT in ironhermes-core/Cargo.toml |
| 5 | `cron_store: Option<Arc<dyn CronJobReader>>` field + `with_cron_store` builder on `CommandContext` | VERIFIED | context.rs:245, 276, 385-387 |
| 6 | `ironhermes-cron::display` module exists with 3 pure-text formatters | VERIFIED | display.rs (9201 bytes, 4 unit tests pass) |
| 7 | CLI `cron.rs` delegates to `ironhermes_cron::display::format_*` shared renderers | VERIFIED | cron.rs:8 (import), :132 (format_job_list), :209 (format_job_detail), :389 (format_cron_status) |
| 8 | `CronJobReaderImpl` adapter wired in `tui_rata/commands.rs` via `with_cron_store` | VERIFIED | commands.rs:375 (struct), :377 (impl), :518 (wiring) |
| 9 | 6 behavioral tests in `handlers_cron.rs` pass | VERIFIED | `cargo test -p ironhermes-core --test handlers_cron` → 6 passed |
| 10 | `execute_cron_job` broken `if let Err(e) = ironhermes_cron::complete_job_run` pattern is ABSENT | VERIFIED | grep returns exit 1 (0 matches) |
| 11 | `TgSendApi` trait + `impl TgSendApi for TelegramAdapter` exist in `gateway::telegram` | VERIFIED | telegram.rs:20 (trait), :30 (impl) |
| 12 | `execute_cron_job` has 8th param `tg_client: Option<Arc<dyn TgSendApi>>` | VERIFIED | runner.rs:887 |
| 13 | `dispatch_delivery` helper called after `complete_job_run` for `Ok(Some(target))` | VERIFIED | runner.rs:1006 (complete_job_run), :1015 (dispatch_delivery) — correct ordering |
| 14 | D-09 non-fatal: TG send failure logs via tracing::error, does not propagate | VERIFIED | runner.rs:840 (doc), :862 (D-09 comment), :1047 (error arm) |
| 15 | D-08 hook ordering: ResponseSent (line ~996) before complete_job_run (line ~1006) before dispatch (line ~1015) | VERIFIED | Line numbers confirmed by grep |
| 16 | 3 behavioral tests in `cron_delivery.rs` pass (telegram fires, local no-op, send failure non-fatal) | VERIFIED | `cargo test -p ironhermes-gateway --test cron_delivery` → 3 passed |
| 17 | `worker_join_set: Arc<TokioMutex<JoinSet<()>>>` declared and used to track per-chat workers | VERIFIED | runner.rs:316 (declaration), :476 (spawn), :699 (drain) — 7 occurrences total |
| 18 | 5-second drain timeout inserted AFTER `self.cancel.cancel()` (line 689) and BEFORE `drop(msg_tx)` (line 725) | VERIFIED | runner.rs:697-721 (drain block at line 698-701), ordering confirmed |
| 19 | `pub(crate) fn cancel_token()` accessor exists on `GatewayRunner` | VERIFIED | runner.rs:159 |
| 20 | Existing `tokio::signal::ctrl_c()` select arm preserved | VERIFIED | runner.rs:665 |
| 21 | 2 behavioral tests in `gateway_shutdown.rs` pass within bounded runtime | VERIFIED | `cargo test -p ironhermes-gateway --test gateway_shutdown` → 2 passed |

**Score:** 21/21 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/ironhermes-core/src/commands/context.rs` | CronJobReader trait + cron_store field + with_cron_store builder | VERIFIED | trait at :93, field at :245, builder at :385 |
| `crates/ironhermes-core/src/commands/handlers.rs` | cmd_cron sub-dispatch + dispatch arm | VERIFIED | fn at :697, arm at :69 |
| `crates/ironhermes-cron/src/display.rs` | format_job_list / format_job_detail / format_cron_status | VERIFIED | 9201 bytes, 4 unit tests pass |
| `crates/ironhermes-cron/src/lib.rs` | `pub mod display;` | VERIFIED | lib.rs:13 |
| `crates/ironhermes-cli/src/cron.rs` | CLI renderers delegate to ironhermes_cron::display | VERIFIED | import at :8, calls at :132, :209, :389 |
| `crates/ironhermes-cli/src/tui_rata/commands.rs` | with_cron_store (11th with_* call) | VERIFIED | :518 |
| `crates/ironhermes-core/tests/handlers_cron.rs` | FakeCronJobReader + make_test_ctx_with_cron_store + 6 tests | VERIFIED | 6931 bytes, 6 tests pass |
| `crates/ironhermes-gateway/src/telegram.rs` | TgSendApi trait + impl TgSendApi for TelegramAdapter | VERIFIED | trait at :20, impl at :30 |
| `crates/ironhermes-gateway/src/runner.rs` | dispatch_delivery + tg_client param + worker_join_set + cancel_token | VERIFIED | all present, ordering correct |
| `crates/ironhermes-gateway/tests/cron_delivery.rs` | FakeTgClient + 3 tests | VERIFIED | 3473 bytes, 3 tests pass |
| `crates/ironhermes-gateway/tests/gateway_shutdown.rs` | gateway_drains_workers_within_timeout + worker_join_set_drains_on_cancel | VERIFIED | 5054 bytes, 2 tests pass |
| `crates/ironhermes-cli/tests/invariants_22_4.rs` | 62 total INVs (3 new: _01_no_cron_stub, _02_delivery_call_site_present, _03_worker_drain_present) | VERIFIED | 62 passed |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `dispatch` match in handlers.rs | `fn cmd_cron` | `"cron" => cmd_cron(args, ctx)` | WIRED | handlers.rs:69 |
| `todo_stub` "cron" arm | (removed) | Absence of "No cron management UI" | WIRED | 0 non-comment matches |
| `build_command_context` | `CommandContext.cron_store` | `.with_cron_store(...)` | WIRED | commands.rs:518 |
| `crates/ironhermes-cli/src/cron.rs` | `ironhermes_cron::display::format_*` | delegation from CLI renderers | WIRED | cron.rs:8, :132, :209, :389 |
| `execute_cron_job` | `TgSendApi::send_message` | `dispatch_delivery(Some(target), ...)` | WIRED | runner.rs:1006-1015 |
| `complete_job_run` return value | delivery dispatch | `match` consuming `Result<Option<DeliveryTarget>>` | WIRED | `Ok(Some(target))` arm at runner.rs:1014, :1046 |
| cron tick closure | `execute_cron_job` (8th arg) | `Some(adapter_tick.clone() as Arc<dyn TgSendApi>)` | WIRED | runner.rs — adapter_tick clone wired |
| `worker_join_set_dispatch` | per-chat worker tasks | `worker_join_set_dispatch.lock().await.spawn(...)` | WIRED | runner.rs:476 |
| `self.cancel.cancel()` (line 689) | `worker_join_set` drain (line 698) | `wjs.join_next()` in timeout_at loop | WIRED | Ordering confirmed: 689 < 698 < 725 |

---

### Data-Flow Trace (Level 4)

Not applicable for this phase — no rendering components or DB-queried dashboards. Fixes are call-site wiring (dispatch_delivery, worker_join_set) and trait definitions. The `FakeCronJobReader` and `FakeTgClient` in tests prove the data paths carry real data through the trait seams.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| handlers_cron — 6 tests | `cargo test -p ironhermes-core --test handlers_cron` | 6 passed, 0 failed | PASS |
| display unit tests | `cargo test -p ironhermes-cron --lib display::` | 4 passed, 0 failed | PASS |
| cron_delivery — 3 tests | `cargo test -p ironhermes-gateway --test cron_delivery` | 3 passed, 0 failed | PASS |
| gateway_shutdown — 2 tests | `cargo test -p ironhermes-gateway --test gateway_shutdown` | 2 passed, 0 failed | PASS |
| invariants_22_4 — 62 INVs | `cargo test -p ironhermes-cli --test invariants_22_4` | 62 passed, 0 failed | PASS |
| Workspace build | `cargo build --workspace` | 0 errors | PASS |
| Workspace compile sanity | `cargo test --workspace --no-run` | 0 errors | PASS |

---

### Requirements Coverage

No new REQ-IDs for this phase (regression fix sweep of already-validated v1.1 capabilities per CONTEXT.md). All three regressions were pre-existing validated behaviors restored.

---

### Decision Coverage (D-01..D-14)

| Decision | Check | Status |
|----------|-------|--------|
| D-01: Three regressions, one phase | 3 SUMMARY.md files present, 3 PLAN files present | VERIFIED |
| D-02: No opportunistic refactors | git diff --stat shows only in-scope files; `crates/ironhermes-cli/src/cron.rs` changes are renderer delegation only | VERIFIED |
| D-03: Each regression has a regression test | handlers_cron.rs (6), cron_delivery.rs (3), gateway_shutdown.rs (2) — all exist and pass | VERIFIED |
| D-04: 7 read-side subcommands in cmd_cron | list/status/get/pause/resume/run/remove all present at handlers.rs:703-757 | VERIFIED |
| D-05: cron_store: Option<Arc<dyn CronJobReader>> | context.rs:245 | VERIFIED |
| D-06: Shared renderer in ironhermes-cron::display | CLI cron.rs delegates at :132, :209, :389 | VERIFIED |
| D-07: Delivery dispatch step restored | `if let Err` replaced by match; `Ok(Some(target))` dispatches via dispatch_delivery | VERIFIED |
| D-08: Hook ordering preserved | ResponseSent (line ~996) < complete_job_run (line ~1006) < dispatch_delivery (line ~1015) | VERIFIED |
| D-09: Delivery failure non-fatal | runner.rs:840 doc + :862 comment + non-propagating error handling | VERIFIED |
| D-10: SIGINT graceful shutdown via worker_join_set drain | worker_join_set.spawn replaces bare tokio::spawn; drain inserted post-cancel | VERIFIED |
| D-11: 5-second timeout on drain | runner.rs:698 `Duration::from_secs(5)` | VERIFIED |
| D-12: Test files in owning crates | handlers_cron.rs in core/tests, cron_delivery.rs + gateway_shutdown.rs in gateway/tests | VERIFIED |
| D-13: 3 new INVs, 62 total | `grep -c "^fn invariant_"` → 62; _01, _02, _03 all present at lines 1494, 1508, 1528 | VERIFIED |
| D-14: 3 plans wave-1 parallel-safe | All 3 have PLAN.md + SUMMARY.md + commits; source files disjoint | VERIFIED |

**Known WARNING (acknowledged, non-blocking per plan):** `App.cron_store` defaults to `None` in `tui_rata`; `/cron` in a live `hermes chat` session returns "cron store not configured." until a future plan loads the runtime JobStore. This is by-design per D-02 (gateway is the primary cron host). Not a blocker.

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| runner.rs | `cancel_token()` accessor has `dead_code` warning (used only in tests, `pub(crate)`) | INFO | Non-blocking — warning only, not an error. Expected: the accessor is `pub(crate)` for integration tests only. |

No stub implementations, no empty returns in production paths, no placeholder comments in delivered code.

---

### Human Verification Required

**Manual UAT (cannot verify programmatically — requires live Telegram bot and gateway):**

1. **Ctrl+C graceful shutdown**
   - Test: `cargo run -p ironhermes-cli -- gateway`, wait for "ready" log, press Ctrl+C
   - Expected: process exits cleanly within 5 seconds
   - Why human: requires live Telegram bot token; test uses Path B (synthetic JoinSet) per RESEARCH §6 Open Question 2

2. **Cron->Telegram live delivery**
   - Test: create a cron job with `deliver=telegram:<your_chat_id>`, wait for it to fire
   - Expected: Telegram message received in target chat
   - Why human: requires live bot token, live chat_id, real cron schedule firing

3. **`/cron list` in tui_rata REPL with wired JobStore**
   - Test: run `hermes chat` with a gateway that has cron jobs; type `/cron list`
   - Expected: formatted job list displayed (currently returns "cron store not configured" per the known gap above until App.cron_store is initialized at runtime)
   - Why human: App.cron_store defaults to None per D-02 scope limit; the gateway path works, the tui_rata chat path needs future wiring

---

### Gaps Summary

No automated gaps found. All must-haves verified. Three human verification items identified for live environment testing (standard for gateway/delivery features that require external services).

---

_Verified: 2026-04-26_
_Verifier: Claude (gsd-verifier)_
