---
phase: 22.4.2.1
slug: cron-cmds-and-telegram-delivery-broken
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-26
---

# Phase 22.4.2.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `22.4.2.1-RESEARCH.md` § 6 Validation Architecture (researcher confirmed all seams).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (Rust 2024 / tokio multi-thread) |
| **Config file** | per-crate `Cargo.toml` `[dev-dependencies]` |
| **Quick run command** | `cargo test --workspace --no-run` (compile-only sanity, ~20s) |
| **Full suite command** | `cargo test --workspace -- --include-ignored` |
| **Per-plan focused command** | `cargo test -p ironhermes-core --test handlers_cron` (Plan 01), `cargo test -p ironhermes-gateway --test cron_delivery` (Plan 02), `cargo test -p ironhermes-gateway --test gateway_shutdown` (Plan 03) |
| **INV ledger command** | `cargo test -p ironhermes-cli --test invariants_22_4` |
| **Estimated runtime** | ~90 seconds full suite; ~5 seconds per focused test file |

---

## Sampling Rate

- **After every task commit:** Run the per-plan focused command for the plan being executed
- **After every plan wave:** Run `cargo test --workspace`
- **Before `/gsd-verify-work`:** Full suite must be green AND `invariants_22_4` must pass with all 62 INVs
- **Max feedback latency:** ~5 seconds (per-plan), ~90 seconds (full suite)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-01 | 01 | 1 | D-04, D-05 | — | N/A (read-only slash UI) | unit/seam | `cargo test -p ironhermes-core --test handlers_cron -- dispatch_cron_returns_jobs_when_store_populated` | ❌ W0 | ⬜ pending |
| 01-02 | 01 | 1 | D-04 | — | None-guard returns informational | unit | `cargo test -p ironhermes-core --test handlers_cron -- dispatch_cron_with_no_store_returns_not_configured` | ❌ W0 | ⬜ pending |
| 01-03 | 01 | 1 | D-06 | — | Renderer parity with CLI | unit | `cargo test -p ironhermes-cron --lib display::tests` | ❌ W0 | ⬜ pending |
| 01-04 | 01 | 1 | D-13 | — | Stub literal absent | static-grep | `cargo test -p ironhermes-cli --test invariants_22_4 -- invariant_22_4_2_1_01_no_cron_stub` | ❌ W0 | ⬜ pending |
| 02-01 | 02 | 1 | D-07 | — | Telegram dispatch fires after success | integration | `cargo test -p ironhermes-gateway --test cron_delivery -- telegram_target_invokes_send_message` | ❌ W0 | ⬜ pending |
| 02-02 | 02 | 1 | D-07 | — | Local target is no-op | integration | `cargo test -p ironhermes-gateway --test cron_delivery -- local_target_does_not_call_tg` | ❌ W0 | ⬜ pending |
| 02-03 | 02 | 1 | D-09 | — | TG send failure is non-fatal | integration | `cargo test -p ironhermes-gateway --test cron_delivery -- tg_send_failure_is_non_fatal` | ❌ W0 | ⬜ pending |
| 02-04 | 02 | 1 | D-13 | — | Dispatch call site present | static-grep | `cargo test -p ironhermes-cli --test invariants_22_4 -- invariant_22_4_2_1_02_delivery_call_site_present` | ❌ W0 | ⬜ pending |
| 03-01 | 03 | 1 | D-10 | — | Gateway shuts down within 5s | smoke | `cargo test -p ironhermes-gateway --test gateway_shutdown -- gateway_drains_workers_within_timeout` | ❌ W0 | ⬜ pending |
| 03-02 | 03 | 1 | D-11 | — | Worker JoinSet drained on cancel | smoke | `cargo test -p ironhermes-gateway --test gateway_shutdown -- worker_join_set_drains_on_cancel` | ❌ W0 | ⬜ pending |
| 03-03 | 03 | 1 | D-13 | — | Worker drain present in source | static-grep | `cargo test -p ironhermes-cli --test invariants_22_4 -- invariant_22_4_2_1_03_worker_drain_present` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/ironhermes-core/tests/handlers_cron.rs` — new test file for Plan 01 (covers dispatch + None guard)
- [ ] `crates/ironhermes-core/tests/handlers_cron.rs` — `make_test_ctx_with_cron_store` fixture using `Arc<dyn CronJobReader>`
- [ ] `crates/ironhermes-cron/src/display.rs` — new module exposing pure-text renderers (lifted from CLI per D-06)
- [ ] `crates/ironhermes-cron/src/lib.rs` — `pub mod display;` export
- [ ] `crates/ironhermes-gateway/tests/cron_delivery.rs` — new test file for Plan 02 (covers TG dispatch path + non-fatal failure)
- [ ] `crates/ironhermes-gateway/src/telegram.rs` — `pub trait TgSendApi` (one-method seam) + `impl TgSendApi for TelegramAdapter`
- [ ] `crates/ironhermes-gateway/tests/gateway_shutdown.rs` — new test file for Plan 03 (CancellationToken-based timeout)
- [ ] `crates/ironhermes-gateway/src/runner.rs` — `pub(crate) fn cancel_token(&self) -> CancellationToken` accessor (test exposure)
- [ ] `crates/ironhermes-cli/tests/invariants_22_4.rs` — append 3 new INV functions (60, 61, 62 — per RESEARCH §6)

*Existing infrastructure (cargo test, tokio test runtime, workspace dev-dependencies) covers everything else — no new framework install needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `hermes gateway` Ctrl+C cleanly exits in real terminal | D-10 | OS signal layer requires real PTY; CI-only test uses `cancel_token.cancel()` | 1) `cargo run -p ironhermes-cli -- gateway`. 2) Wait until "ready" log. 3) Press Ctrl+C. 4) Process exits within 5s with `tracing::info!("gateway shutdown complete")` line. |
| `/cron list` rendered output matches `hermes cron list` | D-06 | Visual diff of two terminal outputs is hard to automate but trivial manually | 1) `hermes cron list` (CLI). 2) In REPL: `/cron`. 3) Diff side-by-side — text columns and ordering must match. |
| Cron job with `deliver=telegram:<chat>` actually delivers to Telegram in production | D-07 | Requires real TG bot token + chat ID; integration test uses `FakeTgClient` | 1) Create cron job with `--deliver telegram:<your_chat_id>`. 2) Wait for next tick. 3) Verify message arrives in Telegram. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies (per table above)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (✓ — every task has a `cargo test` command)
- [ ] Wave 0 covers all MISSING references (3 new test files + display module + TgSendApi trait + cancel_token accessor)
- [ ] No watch-mode flags (cargo test runs once and exits)
- [ ] Feedback latency < 90s (full suite); < 5s (per-plan focused)
- [ ] `nyquist_compliant: true` set in frontmatter (after planner approval)

**Approval:** pending
