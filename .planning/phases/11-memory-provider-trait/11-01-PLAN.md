---
phase: 11-memory-provider-trait
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-core/src/memory_provider.rs
  - crates/ironhermes-core/src/memory_store.rs
  - crates/ironhermes-core/src/config.rs
  - crates/ironhermes-core/src/lib.rs
autonomous: true
requirements: [MEM-07, MEM-08, MEM-12]
must_haves:
  truths:
    - "MemoryProvider trait compiles with Send + Sync + 'static bounds, all five lifecycle hooks, and operational methods (add, replace, remove, format_for_system_prompt, load_from_disk, to_memory_entries)"
    - "MemoryEntries and MemoryProviderConfig types exist and are usable"
    - "MemoryStore implements MemoryProvider — lifecycle hooks and operational methods"
    - "Config accepts memory.provider key defaulting to 'file'"
    - "Unknown provider name causes hard error with clear message"
    - "Existing config.yaml files without memory section parse cleanly"
    - "build_memory_provider factory returns correct provider for 'file'"
    - "All existing MemoryStore tests pass unchanged"
  artifacts:
    - path: "crates/ironhermes-core/src/memory_provider.rs"
      provides: "MemoryProvider trait, MemoryEntries, MemoryProviderConfig, build_memory_provider factory, format_entries_for_prompt"
      exports: ["MemoryProvider", "MemoryEntries", "MemoryProviderConfig", "build_memory_provider", "format_entries_for_prompt"]
    - path: "crates/ironhermes-core/src/config.rs"
      provides: "MemoryConfig struct with provider field"
      contains: "pub struct MemoryConfig"
    - path: "crates/ironhermes-core/src/lib.rs"
      provides: "Re-exports of new memory provider types"
      contains: "pub mod memory_provider"
  key_links:
    - from: "crates/ironhermes-core/src/memory_provider.rs"
      to: "crates/ironhermes-core/src/memory_store.rs"
      via: "imports MemoryTarget, MemoryResult; MemoryStore implements MemoryProvider"
      pattern: "use crate::memory_store::"
    - from: "crates/ironhermes-core/src/memory_provider.rs"
      to: "crates/ironhermes-core/src/config.rs"
      via: "MemoryConfig used by factory"
      pattern: "MemoryConfig"
---

<objective>
Define the MemoryProvider trait with lifecycle hooks AND operational methods, implement it for MemoryStore, add config extension, and build the provider factory.

Purpose: Establishes the complete pluggable memory backend abstraction. The trait includes both lifecycle hooks (D-01 through D-07) and operational methods (add/replace/remove/format_for_system_prompt/load_from_disk/to_memory_entries) so that `dyn MemoryProvider` is a full replacement for concrete `MemoryStore` at all call sites. This enables Plan 02 to migrate every `Arc<Mutex<MemoryStore>>` to `Arc<Mutex<dyn MemoryProvider + Send>>`.

Output: `memory_provider.rs` with trait + types + factory, `memory_store.rs` with MemoryProvider impl, `config.rs` extended with MemoryConfig, lib.rs re-exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-memory-provider-trait/11-CONTEXT.md
@.planning/phases/11-memory-provider-trait/11-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From crates/ironhermes-core/src/memory_store.rs:
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum MemoryTarget {
    Memory,
    User,
}

impl MemoryTarget {
    pub fn filename(&self) -> &'static str;
    pub fn char_limit(&self) -> usize;
    pub fn label(&self) -> &'static str;
}

/// Ok contains JSON success message, Err contains JSON error message.
pub type MemoryResult = std::result::Result<String, String>;

pub struct MemoryStore {
    entries: HashMap<MemoryTarget, Vec<String>>,  // private
    snapshot: HashMap<MemoryTarget, String>,       // private
    memory_dir: PathBuf,                           // private
}

impl MemoryStore {
    pub fn new(memory_dir: PathBuf) -> Self;
    pub fn load_from_disk(&mut self) -> anyhow::Result<()>;
    pub fn add(&mut self, target: MemoryTarget, content: &str) -> MemoryResult;
    pub fn replace(&mut self, target: MemoryTarget, old_text: &str, new_content: &str) -> MemoryResult;
    pub fn remove(&mut self, target: MemoryTarget, old_text: &str) -> MemoryResult;
    pub fn format_for_system_prompt(&self, target: MemoryTarget) -> Option<String>;
}
```

From crates/ironhermes-core/src/config.rs:
```rust
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
#[serde(default)]
pub struct Config {
    pub model: ModelConfig,
    pub agent: AgentConfig,
    pub terminal: TerminalConfig,
    pub web: WebConfig,
    pub gateway: GatewayConfig,
    pub cron: CronConfig,
    pub security: SecurityConfig,
    pub rate_limit: RateLimitConfig,
    pub skills: SkillsConfig,
    pub exec: ExecConfig,
    pub subagent: SubagentConfig,
    pub batch: BatchConfig,
}
```

From crates/ironhermes-core/src/constants.rs (relevant):
```rust
pub const MEMORY_FILENAME: &str = "MEMORY.md";
pub const USER_FILENAME: &str = "USER.md";
pub const MEMORY_CHAR_LIMIT: usize = 2200;
pub const USER_CHAR_LIMIT: usize = 1375;
pub const ENTRY_DELIMITER: &str = "\n\u{00a7}\n";
pub fn get_hermes_home() -> PathBuf;
```

From crates/ironhermes-core/src/lib.rs:
```rust
pub mod config;
pub mod constants;
pub mod context_scanner;
pub mod error;
pub mod memory_store;
pub mod skills;
pub mod ssrf;
pub mod types;

pub use memory_store::{MemoryStore, MemoryTarget};
```

From crates/ironhermes-tools/src/registry.rs (Tool trait pattern):
```rust
#[async_trait]
pub trait Tool: Send + Sync {
    fn name(&self) -> &str;
    fn toolset(&self) -> &str;
    fn description(&self) -> &str;
    fn schema(&self) -> ToolSchema;
    async fn execute(&self, args: serde_json::Value) -> anyhow::Result<String>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define MemoryProvider trait with lifecycle hooks and operational methods</name>
  <files>crates/ironhermes-core/src/memory_provider.rs, crates/ironhermes-core/src/lib.rs</files>
  <read_first>
    crates/ironhermes-core/src/memory_store.rs
    crates/ironhermes-core/src/lib.rs
    crates/ironhermes-core/src/constants.rs
    crates/ironhermes-tools/src/registry.rs
  </read_first>
  <behavior>
    - Test 1: MemoryEntries::default() returns empty entries HashMap
    - Test 2: MemoryEntries can insert and retrieve entries for MemoryTarget::Memory and MemoryTarget::User
    - Test 3: MemoryProviderConfig serializes and deserializes via serde_json round-trip
    - Test 4: MemoryProviderConfig extra field defaults to empty HashMap
    - Test 5: format_entries_for_prompt returns None for empty entries
    - Test 6: format_entries_for_prompt returns "## Memory\n\nfact one\nfact two" for Memory target with two entries
    - Test 7: format_entries_for_prompt returns "## User Profile\n\nuser info" for User target
    - Test 8: A mock struct implementing MemoryProvider compiles and can be stored as Box&lt;dyn MemoryProvider + Send&gt; (proves Send + Sync + 'static + dyn-compatible)
    - Test 9: The mock struct's operational methods (add, replace, remove) can be called through the trait object
  </behavior>
  <action>
Create `crates/ironhermes-core/src/memory_provider.rs` with:

1. **MemoryEntries wrapper type:**
```rust
#[derive(Debug, Clone, Default)]
pub struct MemoryEntries {
    pub entries: HashMap<MemoryTarget, Vec<String>>,
}
```

2. **MemoryProviderConfig struct** (per D-03):
```rust
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct MemoryProviderConfig {
    pub provider: String,
    pub memory_dir: PathBuf,
    pub memory_char_limit: usize,
    pub user_char_limit: usize,
    #[serde(default)]
    pub extra: HashMap<String, serde_json::Value>,
}
```

3. **MemoryProvider trait** (per D-01, D-02, D-03 through D-07, plus operational methods):

The trait has TWO sections: lifecycle hooks (async, per user decisions D-01 through D-07) and operational methods (sync, matching MemoryStore's existing public API). The operational methods are needed so that `dyn MemoryProvider` is a complete replacement for concrete `MemoryStore` at all call sites — this is what enables "memory backends can be swapped without changing agent code."

```rust
#[async_trait]
pub trait MemoryProvider: Send + Sync + 'static {
    // === Lifecycle hooks (D-01 through D-07) ===

    /// Set up provider resources. Fatal on error (D-11).
    async fn initialize(&mut self, config: &MemoryProviderConfig) -> anyhow::Result<()>;
    /// Load provider state for session. On error, log warning and return empty (D-12).
    async fn prefetch(&self, session_id: &str) -> anyhow::Result<MemoryEntries>;
    /// Sync current entries after mutation. On error, log warning and skip (D-12).
    async fn sync_turn(&self, session_id: &str, entries: &MemoryEntries) -> anyhow::Result<()>;
    /// Persist/flush on session end. On error, log warning and skip (D-12).
    async fn on_session_end(&self, session_id: &str, entries: &MemoryEntries) -> anyhow::Result<()>;
    /// Clean teardown. Fatal on error (D-11).
    async fn shutdown(&mut self) -> anyhow::Result<()>;

    // === Operational methods (sync — used by MemoryTool and PromptBuilder) ===

    /// Load entries from backing store. Called at startup before wrapping in Arc<Mutex<>>.
    fn load_from_disk(&mut self) -> anyhow::Result<()>;
    /// Add a new memory entry for the given target.
    fn add(&mut self, target: MemoryTarget, content: &str) -> MemoryResult;
    /// Replace an entry identified by substring match.
    fn replace(&mut self, target: MemoryTarget, old_text: &str, new_content: &str) -> MemoryResult;
    /// Remove an entry identified by substring match.
    fn remove(&mut self, target: MemoryTarget, old_text: &str) -> MemoryResult;
    /// Return formatted snapshot for system prompt injection (D-12: frozen snapshot).
    fn format_for_system_prompt(&self, target: MemoryTarget) -> Option<String>;
    /// Return current entries as a MemoryEntries snapshot.
    fn to_memory_entries(&self) -> MemoryEntries;
}
```

Note: `#[async_trait]` only transforms `async fn` methods. The sync `fn` methods are unaffected and work normally. `MemoryResult` is imported from `crate::memory_store::MemoryResult`.

4. **format_entries_for_prompt standalone function** (replaces MemoryStore::format_for_system_prompt for trait-based usage where MemoryEntries is available):
```rust
pub fn format_entries_for_prompt(entries: &MemoryEntries, target: MemoryTarget) -> Option<String> {
    let entry_list = entries.entries.get(&target)?;
    if entry_list.is_empty() {
        return None;
    }
    let header = match target {
        MemoryTarget::Memory => "## Memory",
        MemoryTarget::User => "## User Profile",
    };
    Some(format!("{}\n\n{}", header, entry_list.join("\n")))
}
```

5. Update `crates/ironhermes-core/src/lib.rs`:
   - Add `pub mod memory_provider;` after `pub mod memory_store;`
   - Add to re-exports: `pub use memory_provider::{MemoryProvider, MemoryEntries, MemoryProviderConfig, format_entries_for_prompt};`
   - Add to re-exports: `pub use memory_store::MemoryResult;` (needed by downstream crates referencing the trait)

All imports needed: `use async_trait::async_trait;`, `use std::collections::HashMap;`, `use std::path::PathBuf;`, `use anyhow::Result;`, `use crate::memory_store::{MemoryTarget, MemoryResult};`.

The mock struct in tests should implement all methods (lifecycle hooks return Ok(()), operational methods return simple Ok/Err values) and verify it compiles as `Box<dyn MemoryProvider + Send>`.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo test --package ironhermes-core memory_provider -- --nocapture</automated>
  </verify>
  <done>MemoryProvider trait compiles with Send+Sync+'static bounds, all 5 lifecycle hooks, and 6 operational methods. MemoryEntries, MemoryProviderConfig, and format_entries_for_prompt are defined and tested. Mock impl proves dyn-compatibility. All existing ironhermes-core tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MemoryStore implements MemoryProvider, add MemoryConfig and factory</name>
  <files>crates/ironhermes-core/src/memory_store.rs, crates/ironhermes-core/src/memory_provider.rs, crates/ironhermes-core/src/config.rs, crates/ironhermes-core/src/lib.rs</files>
  <read_first>
    crates/ironhermes-core/src/config.rs
    crates/ironhermes-core/src/memory_provider.rs
    crates/ironhermes-core/src/memory_store.rs
    crates/ironhermes-core/src/constants.rs
    crates/ironhermes-core/src/lib.rs
  </read_first>
  <behavior>
    - Test 1: MemoryConfig::default().provider == "file"
    - Test 2: Config::default().memory.provider == "file"
    - Test 3: Config parses from YAML without `memory:` section (backward compat) — memory.provider defaults to "file"
    - Test 4: Config parses from YAML with `memory: { provider: "sqlite" }` — memory.provider == "sqlite"
    - Test 5: build_memory_provider with provider="file" returns Ok (Box of dyn MemoryProvider)
    - Test 6: build_memory_provider with provider="unknown" returns Err containing "Unknown memory provider 'unknown'" and "Available providers: file"
    - Test 7: build_memory_provider with provider="sqlite" returns Err mentioning feature flag (not compiled in yet)
    - Test 8: MemoryStore as MemoryProvider: initialize loads from disk, prefetch returns entries, add/replace/remove work through trait object
    - Test 9: Arc&lt;Mutex&lt;MemoryStore&gt;&gt; coerces to Arc&lt;Mutex&lt;dyn MemoryProvider + Send&gt;&gt; at construction and add/replace/remove still work through the trait object
  </behavior>
  <action>
1. **Implement MemoryProvider for MemoryStore** in `memory_store.rs` (not memory_provider.rs, to keep implementation with the struct):

Add at the bottom of memory_store.rs, before `#[cfg(test)]`:

```rust
use crate::memory_provider::{MemoryProvider, MemoryEntries, MemoryProviderConfig};
use async_trait::async_trait;

#[async_trait]
impl MemoryProvider for MemoryStore {
    // Lifecycle hooks — file provider is trivially sync
    async fn initialize(&mut self, _config: &MemoryProviderConfig) -> anyhow::Result<()> {
        self.load_from_disk()
    }

    async fn prefetch(&self, _session_id: &str) -> anyhow::Result<MemoryEntries> {
        Ok(self.to_memory_entries())
    }

    async fn sync_turn(&self, _session_id: &str, _entries: &MemoryEntries) -> anyhow::Result<()> {
        Ok(())  // File provider: disk is authoritative, mutations write directly
    }

    async fn on_session_end(&self, _session_id: &str, _entries: &MemoryEntries) -> anyhow::Result<()> {
        Ok(())  // File provider: writes happen in-place via add/replace/remove
    }

    async fn shutdown(&mut self) -> anyhow::Result<()> {
        Ok(())  // File provider: no resources to release
    }

    // Operational methods — delegate to existing MemoryStore methods
    fn load_from_disk(&mut self) -> anyhow::Result<()> {
        MemoryStore::load_from_disk(self)
    }

    fn add(&mut self, target: MemoryTarget, content: &str) -> MemoryResult {
        MemoryStore::add(self, target, content)
    }

    fn replace(&mut self, target: MemoryTarget, old_text: &str, new_content: &str) -> MemoryResult {
        MemoryStore::replace(self, target, old_text, new_content)
    }

    fn remove(&mut self, target: MemoryTarget, old_text: &str) -> MemoryResult {
        MemoryStore::remove(self, target, old_text)
    }

    fn format_for_system_prompt(&self, target: MemoryTarget) -> Option<String> {
        MemoryStore::format_for_system_prompt(self, target)
    }

    fn to_memory_entries(&self) -> MemoryEntries {
        let mut map = HashMap::new();
        for target in &[MemoryTarget::Memory, MemoryTarget::User] {
            if let Some(entries) = self.entries.get(target) {
                if !entries.is_empty() {
                    map.insert(*target, entries.clone());
                }
            }
        }
        MemoryEntries { entries: map }
    }
}
```

IMPORTANT: The trait impl's `load_from_disk()` calls `MemoryStore::load_from_disk(self)` using fully qualified syntax to disambiguate from the trait method. Same for `add`, `replace`, `remove`, `format_for_system_prompt`. The existing `pub fn` methods on `MemoryStore` stay exactly as they are — the trait impl delegates to them.

NOTE: The `entries` field on MemoryStore is private. The `to_memory_entries()` implementation accesses `self.entries` which works because this impl is in the same file as the struct. If the impl were in a different file, you'd need a getter. Keep the impl in `memory_store.rs` for this reason.

2. **Add MemoryConfig to config.rs** (per D-08, D-10):

After the `BatchConfig` section, add:
```rust
/// Memory provider configuration (D-08, D-10).
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(default)]
pub struct MemoryConfig {
    /// Provider name. Default: "file". Future: "sqlite", "grafeo", "duckdb".
    pub provider: String,
}

impl Default for MemoryConfig {
    fn default() -> Self {
        Self {
            provider: "file".to_string(),
        }
    }
}
```

Add `pub memory: MemoryConfig,` field to the `Config` struct.

3. **Add build_memory_provider factory to memory_provider.rs** (per D-09, D-10):

```rust
use crate::config::MemoryConfig;
use crate::constants::get_hermes_home;
use crate::memory_store::MemoryStore;

const MEMORIES_DIR: &str = "memories";

pub fn build_memory_provider(config: &MemoryConfig) -> anyhow::Result<Box<dyn MemoryProvider + Send>> {
    match config.provider.as_str() {
        "file" => {
            let memory_dir = get_hermes_home().join(MEMORIES_DIR);
            Ok(Box::new(MemoryStore::new(memory_dir)))
        }
        "sqlite" | "grafeo" | "duckdb" => {
            anyhow::bail!(
                "Memory provider '{}' is not available in this build. \
                 It requires the '{}' feature flag. Available providers: file.",
                config.provider,
                config.provider
            )
        }
        other => {
            anyhow::bail!(
                "Unknown memory provider '{}'. Available providers: file. \
                 Future providers (sqlite, grafeo, duckdb) require feature flags.",
                other
            )
        }
    }
}
```

4. **Update lib.rs re-exports:**
   - Add `MemoryConfig` to the `pub use config::{...}` line
   - Add `build_memory_provider` to the `pub use memory_provider::{...}` line
   - Add `pub use memory_store::MemoryResult;`

5. **Add tests** to both config.rs and memory_provider.rs covering the behavior list above. For Test 9, verify that `Arc::new(Mutex::new(store)) as Arc<Mutex<dyn MemoryProvider + Send>>` works and that calling `.lock().unwrap().add(...)` through the trait object succeeds.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo test --package ironhermes-core -- --nocapture</automated>
  </verify>
  <done>MemoryConfig added to Config with serde(default) backward compat. build_memory_provider factory selects provider from config, returns hard error for unknown/uncompiled providers. MemoryStore implements full MemoryProvider trait (lifecycle hooks + operational methods). Arc coercion to trait object works. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml -> provider factory | User-supplied provider string parsed at startup |
| MemoryEntries data flow | Entries pass through trait hooks; file provider inherits existing security scanning |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | config.yaml memory.provider | mitigate | Exhaustive match in build_memory_provider rejects unknown provider strings with hard error (D-09); no arbitrary code execution path |
| T-11-02 | Information Disclosure | MemoryProviderConfig.extra | accept | Extra settings are HashMap<String, Value>; no secrets stored here — API keys handled by provider resolution (Phase 12) |
| T-11-03 | Spoofing | MemoryProvider trait | accept | Trait is internal to the binary; providers are compiled-in, not dynamically loaded; no plugin attack surface |
| T-11-04 | Tampering | MemoryEntries passed to hooks | accept | Entries are already security-scanned by MemoryStore::add/replace before reaching sync_turn/on_session_end; scanning remains in the mutation path, not the trait hooks |
</threat_model>

<verification>
1. `cargo test --package ironhermes-core` — all existing + new tests pass
2. `cargo build --package ironhermes-core` — compiles cleanly
3. Grep confirms: trait has 5 async lifecycle hooks + 6 sync operational methods, Send + Sync + 'static bounds
4. Grep confirms: MemoryConfig in Config struct with #[serde(default)]
5. Grep confirms: build_memory_provider handles "file", "sqlite", "grafeo", "duckdb", unknown
6. Grep confirms: `impl MemoryProvider for MemoryStore` exists in memory_store.rs
</verification>

<success_criteria>
- MemoryProvider trait compiles with Send + Sync + 'static, 5 lifecycle hooks, and 6 operational methods (MEM-07)
- MemoryStore implements MemoryProvider with all methods delegating to existing implementations (MEM-08)
- Config accepts memory.provider key defaulting to "file" (MEM-12)
- Unknown/uncompiled provider causes hard error at startup (MEM-12, D-09)
- Arc<Mutex<MemoryStore>> coerces to Arc<Mutex<dyn MemoryProvider + Send>> at construction
- Zero new Cargo dependencies added
- All existing ironhermes-core tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/11-memory-provider-trait/11-01-SUMMARY.md`
</output>
