---
phase: 36.17.6-toolset-cli-tts-wiring
plan: "02"
status: complete
requirements:
  - A3
  - A4
  - A5
key_files:
  created:
    - /tmp/36.17.6-02-01-list.log
    - /tmp/36.17.6-02-01-show.log
    - /tmp/36.17.6-02-02-uat.log
    - /tmp/36.17.6-02-03-gate4.log
    - /tmp/36.17.6-02-03-synth.log
    - /tmp/36.17.6-02-03-gate5.log
  modified: []
tests_added: 0
---

# Plan 36.17.6-02 SUMMARY — UAT re-run against Plan 01 release binary

## Outcome

All 5 UAT gates from phase 36.17.5 are GREEN against the release binary built from the Plan 01 HEAD. Operator confirmed `approved` for Gates 4 and 5.

## Task 1 — Release build + preflight (auto)

`cargo build --release -p ironhermes-cli` exit 0 (1m 30s, finished `release` profile, 31 warnings — pre-existing, not introduced by Plan 01). Binary at `target/release/ironhermes`, 72,860,000 bytes.

Preflight against `IRONHERMES_HOME=/tmp/uat-36.17.6`:

```
$ ./target/release/ironhermes toolset list
TOOLSET    STATUS     TOOLS   AVAILABLE
memory     enabled    1       1/1 (memory ✓)
session    enabled    1       1/1 (session_search ✓)
agent      enabled    2       2/2 (delegate_task ✓, cronjob ✓)
skills     enabled    1       1/1 (skills ✓)
robotics   disabled   0       0/0
learning   disabled   1       1/1 (skill_manage ✓)
web        disabled   3       2/3 (web_search ✗ FIRECRAWL_API_KEY, web_read ✓, web_extract ✓)
code       disabled   6       6/6 (execute_code ✓, terminal ✓, read_file ✓, write_file ✓, list_dir ✓, grep_files ✓)
browser    disabled   11      11/11 (...)
voice      disabled   2       2/2 (text_to_speech ✓, send_audio ✓)
```

A3 satisfied: `voice` row visible with `TOOLS=2`, `AVAILABLE=2/2`.

```
$ ./target/release/ironhermes toolset show voice
Toolset: voice
Status:  disabled
Tools:   2/2 available

Members:
  ✓ text_to_speech
  ✓ send_audio
```

A4 satisfied: exit 0, both members listed with `✓`.

## Task 2 — Full UAT script re-run (Gates 1-3, operator-verified)

```
$ bash scripts/uat/phase-36.17.5-tts.sh
==> Phase 36.17.5 TTS UAT — IRONHERMES_HOME=/var/folders/_d/.../ironhermes-36.17.5-XXXXXX.bqdGANp1q2

==> Gate 1 (D-03 / D-16): hermes tts test writes MP3 to audio_cache/
    OK (synth wrote /var/folders/.../audio_cache/39198534-db1c-46ec-9c98-cb2baf68f23f.mp3)

==> Gate 2 (D-04 / D-05): synthesized file is non-empty
    OK (27504 bytes, magic fff364)

==> Gate 3 (D-10 / D-03): edge provider requires no API key
    OK

==> Gates 4 and 5 require human-verify checkpoints (see script header).
    Gate 4: run 'hermes tools list' and confirm text_to_speech + send_audio appear.
    Gate 5: run 'hermes tts play --path /var/folders/.../audio_cache/39198534-db1c-46ec-9c98-cb2baf68f23f.mp3' and confirm audio or clean error.

==> Phase 36.17.5 TTS UAT: all green
```

Gates 1-3 GREEN; final marker `==> Phase 36.17.5 TTS UAT: all green` printed. Operator confirmed: **approved**.

D-04 satisfied: full UAT re-run confirmed no regression on the standalone `tts test` path despite the Plan 01 wiring change in `cmd_toolset_list` / `cmd_toolset_show`.

## Task 3 — Manual Gates 4 + 5 (operator-verified)

**Gate 4 (toolset inspection):** transcript captured to `/tmp/36.17.6-02-03-gate4.log` — same content as the Task 1 preflight; both `text_to_speech` and `send_audio` present with `✓`. Operator confirmed: **approved**.

**Gate 5 (Platform::Local audible playback):**

The UAT script's `mktemp` scratch dir was cleaned up by its trap on exit, so the Gate 1 MP3 path was no longer valid. Re-synthesized a fresh MP3 into the persistent UAT home and replayed:

```
$ IRONHERMES_HOME=/tmp/uat-36.17.6 ./target/release/ironhermes tts test --provider edge
/tmp/uat-36.17.6/audio_cache/228ee9b9-9fde-4bc5-bd74-6dadec0ad363.mp3

$ ./target/release/ironhermes tts play --path /tmp/uat-36.17.6/audio_cache/228ee9b9-9fde-4bc5-bd74-6dadec0ad363.mp3
Dropping DeviceSink, audio playing through this sink will stop, to prevent this message from appearing use tracing or call `.log_on_drop(false)` on this DeviceSink
played: /tmp/uat-36.17.6/audio_cache/228ee9b9-9fde-4bc5-bd74-6dadec0ad363.mp3
```

Binary printed `played:` (success path) with no panic, no stack trace, no `RUST_BACKTRACE` reference. The "Dropping DeviceSink" line is informational tracing, not an error. Operator confirmed audible playback: **approved**.

## Forward note

Gate 5 surfaced a minor UX paper-cut: the UAT script's instruction at the end points at its own `mktemp` MP3 path, but that path is cleaned up when the script exits — so a literal copy-paste of the printed path will fail. The script header comments (lines 26-54) tell the operator how to re-synthesize, but a one-line tweak to keep that MP3 alive (or to print a survival path) would smooth the manual UAT. Out of scope for this phase; capture as a polish item for a future TTS UX cleanup.

## Acceptance mapping

| Requirement | Status | Evidence |
|-------------|--------|----------|
| A3 | GREEN | Task 1 preflight `toolset list` transcript above |
| A4 | GREEN | Task 1 preflight `toolset show voice` transcript above |
| A5 | GREEN | Task 2 + Task 3 transcripts above; operator typed `approved` for Gates 4 and 5 |
| D-04 (full UAT re-run, not just blocked gates) | satisfied | Script run produced final `all green` marker |
| D-05 (Plan 03 can close 36.17.5) | unblocked | Plan 03 may now flip 36.17.5-VALIDATION.md and ROADMAP counters |

## Self-Check: PASSED
