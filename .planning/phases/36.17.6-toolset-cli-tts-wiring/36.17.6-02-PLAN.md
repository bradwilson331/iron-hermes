---
phase: 36.17.6-toolset-cli-tts-wiring
plan: "02"
type: execute
wave: 2
depends_on:
  - 36.17.6-01
files_modified: []
autonomous: false
requirements:
  - A3
  - A4
  - A5
tags:
  - tts
  - cli
  - uat
  - operator-verify

must_haves:
  truths:
    - "Release binary built from the Plan 01 commit invokes `register_tts_for_inspection` and prints a `voice` row"
    - "`ironhermes toolset list` shows a `voice` row with `TOOLS=2` and `AVAILABLE=2/2` (both `text_to_speech` and `send_audio` marked `✓`)"
    - "`ironhermes toolset show voice` exits 0 and lists both `text_to_speech` and `send_audio` with prerequisite labels"
    - "`bash scripts/uat/phase-36.17.5-tts.sh` exits 0 with Gates 1-3 GREEN automated, and operator types `approved` for Gates 4 and 5"
    - "All 5 UAT gates from phase 36.17.5 are GREEN by the end of this plan"
  artifacts:
    - path: "target/release/ironhermes"
      provides: "Release-build binary used for the UAT run; rebuilt from the Plan 01 commit"
    - path: "/tmp/uat-36.17.6"
      provides: "Operator-side `IRONHERMES_HOME` scratch dir for the UAT run (isolates config + audio_cache)"
  key_links:
    - from: "Operator terminal"
      to: "target/release/ironhermes toolset list"
      via: "`IRONHERMES_HOME=/tmp/uat-36.17.6` env var redirects config/audio paths"
      pattern: "IRONHERMES_HOME=/tmp/uat-36.17.6"
    - from: "scripts/uat/phase-36.17.5-tts.sh"
      to: "Operator approval prompt"
      via: "Gates 4-5 manual checkpoints (script body prints instructions; operator confirms via `approved` keyword)"
      pattern: "approved"
---

<objective>
Re-run phase 36.17.5's UAT script end-to-end against the Plan 01 release binary
to (a) regression-verify Gates 1-3 still pass with the new TTS wiring in the
inspection path, and (b) secure operator approval for Gates 4 and 5 — the two
manual gates that were BLOCKED in 36.17.5 Plan 04.

Purpose: Per CONTEXT.md D-04, re-running the full 5-gate script (not just the
two blocked gates) catches any subtle CLI-startup regressions the wiring change
might introduce while delivering operator-confirmed acceptance for the two
previously-blocked gates. Per D-05, phase 36.17.5 stays open until this plan's
UAT comes back 5/5 GREEN.

Output: No file modifications. This plan's deliverable is operator-confirmed
UAT evidence — a transcript showing Gates 1-3 automated GREEN, Gates 4 and 5
GREEN via operator `approved` keyword. The transcript is captured in the
plan SUMMARY so phase 36.17.5's closeout (Plan 03 in this phase) can cite it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/36.17.6-toolset-cli-tts-wiring/36.17.6-CONTEXT.md
@.planning/phases/36.17.6-toolset-cli-tts-wiring/36.17.6-RESEARCH.md
@.planning/phases/36.17.6-toolset-cli-tts-wiring/36.17.6-PATTERNS.md
@.planning/phases/36.17.6-toolset-cli-tts-wiring/36.17.6-VALIDATION.md
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-04-SUMMARY.md
@scripts/uat/phase-36.17.5-tts.sh
@CLAUDE.md

<interfaces>
<!-- The UAT script is the contract. Operator sees these gates in order. -->

From `scripts/uat/phase-36.17.5-tts.sh` (per RESEARCH.md Q6 analysis):
  - Gate 1 (lines 74-88): `hermes tts test --provider edge` writes MP3 to `${IRONHERMES_HOME}/audio_cache/`. Automated.
  - Gate 2 (lines 94-105): file size ≥ 1000 bytes + MP3 magic bytes (`fffb*|fff3*|fff2*|494433*`). Automated.
  - Gate 3 (lines 113-120): same `tts test` exits 0 with `ELEVENLABS_API_KEY` + `OPENAI_API_KEY` unset. Automated.
  - Gate 4 (lines 125-128 header instructions): operator runs `ironhermes toolset show voice` and confirms `text_to_speech` + `send_audio` both appear. Manual checkpoint — this is the gate Plan 01 unblocks.
  - Gate 5 (lines 129-131 header instructions): operator runs `ironhermes tts play --path <Gate-1 mp3>` and confirms audible playback OR a clean no-device error (no panic). Manual checkpoint.
  - Final line: `==> Phase 36.17.5 TTS UAT: all green` prints after Gates 1-3 pass; operator must still confirm 4+5 to call the phase complete.

Per CONTEXT.md Q1 / RESEARCH.md Q6 — the script body is unchanged for 36.17.6. The script's Gate 4 header comment says `hermes tools list`; per RESEARCH.md Open Question 2, the correct command after Plan 01 is `ironhermes toolset show voice` (operator follows the CONTEXT.md verification block, not the stale header comment).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build release binary from Plan 01 HEAD and pre-flight that the new wiring is in the artifact</name>
  <files>target/release/ironhermes (build artifact, not source)</files>
  <read_first>
    - .planning/phases/36.17.6-toolset-cli-tts-wiring/36.17.6-CONTEXT.md (D-04 — re-run full UAT; D-05 — close criteria)
    - .planning/phases/36.17.6-toolset-cli-tts-wiring/36.17.6-RESEARCH.md (Q7 — `--release` flag is needed for the UAT binary; `IRONHERMES_HOME` env behavior)
    - .planning/phases/36.17.6-toolset-cli-tts-wiring/36.17.6-VALIDATION.md (per-task verification map, rows for A3 + A4)
    - scripts/uat/phase-36.17.5-tts.sh
  </read_first>
  <action>
    1. Confirm Plan 01 HEAD is the working-tree state (`git log -1 --oneline` shows the Plan 01 commit; `git status` is clean for `crates/ironhermes-cli/src/toolset_cmd.rs`).
    2. Build the release binary: `cargo build --release -p ironhermes-cli` (per RESEARCH.md Q7 — `--release` flag is needed; CONTEXT.md elides it but the UAT script uses `target/release/ironhermes`).
    3. Pre-flight sanity: with `IRONHERMES_HOME=/tmp/uat-36.17.6` (create the dir if needed: `mkdir -p /tmp/uat-36.17.6`), run `./target/release/ironhermes toolset list` and confirm the `voice` row appears with `AVAILABLE=2/2 (text_to_speech ✓, send_audio ✓)`. Capture the stdout into the SUMMARY transcript. Then run `./target/release/ironhermes toolset show voice` and confirm exit 0 + both tool entries print. These two captures cover A3 + A4 deterministically before the full UAT script reruns Gates 1-3.

    Per RESEARCH.md Q7: both `text_to_speech.is_available()` (registry-driven, edge provider always registered) and `send_audio.is_available()` (no prerequisites, defaults to true) return true regardless of `ELEVENLABS_API_KEY`, so the expected output is `AVAILABLE=2/2` even without the API key set.
  </action>
  <verify>
    <automated>cargo build --release -p ironhermes-cli 2>&amp;1 | tail -3 &amp;&amp; mkdir -p /tmp/uat-36.17.6 &amp;&amp; IRONHERMES_HOME=/tmp/uat-36.17.6 ./target/release/ironhermes toolset list 2>&amp;1 | tee /tmp/36.17.6-02-01-list.log | grep -E "^voice\\s" &amp;&amp; IRONHERMES_HOME=/tmp/uat-36.17.6 ./target/release/ironhermes toolset show voice 2>&amp;1 | tee /tmp/36.17.6-02-01-show.log | grep -E "text_to_speech|send_audio"</automated>
  </verify>
  <done>
    - `target/release/ironhermes` exists and is executable.
    - `/tmp/36.17.6-02-01-list.log` contains a `voice` row with both tools marked available (or at minimum the slug `voice` and the count `2/2`).
    - `/tmp/36.17.6-02-01-show.log` lists both `text_to_speech` and `send_audio` and the command exited 0.
    - Operator (or executor) captures both stdout transcripts into the plan SUMMARY for A3 + A4 evidence.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the full 36.17.5 UAT script (Gates 1-3 automated) and confirm GREEN on operator machine</name>
  <files>scripts/uat/phase-36.17.5-tts.sh (script invoked — not modified)</files>
  <read_first>
    - scripts/uat/phase-36.17.5-tts.sh (full script — operator should re-read before running)
    - .planning/phases/36.17.6-toolset-cli-tts-wiring/36.17.6-CONTEXT.md (D-04)
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-04-SUMMARY.md (UAT Evidence section — historical Gates 1-3 GREEN transcript for reference)
  </read_first>
  <what-built>
    Phase 36.17.5 Plan 04 shipped `scripts/uat/phase-36.17.5-tts.sh` with Gates 1-3 automated (write MP3 via Edge TTS, size + magic-byte check, no-API-key exit-0 check). Plan 01 of this phase added the missing TTS wiring to the CLI inspection path. Per CONTEXT.md D-04, the operator re-runs the full script — Gates 1-3 prove there is no regression in the standalone `hermes tts test` path, and Gates 4+5 (handled separately in Task 3) cover the inspection path that Plan 01 wired.
  </what-built>
  <action>
    From the repo root:

    1. Ensure network reachability to the Microsoft Edge TTS WebSocket endpoint (Gate 1 requires it).
    2. Run `bash scripts/uat/phase-36.17.5-tts.sh`. The script uses its own `mktemp` scratch dir; the Task 1 `/tmp/uat-36.17.6` directory is NOT reused here.
    3. Observe the gate-by-gate output. Expected behaviour:
       - `Gate 1` prints `OK (synth wrote /<tmp>/audio_cache/<uuid>.mp3)`.
       - `Gate 2` prints `OK (<bytes> bytes, magic <hex>)` where bytes ≥ 1000 and magic matches one of `fffb*|fff3*|fff2*|494433*`.
       - `Gate 3` prints `OK` after running the same `tts test` invocation with `ELEVENLABS_API_KEY` and `OPENAI_API_KEY` unset.
       - Final line: `==> Phase 36.17.5 TTS UAT: all green`.
    4. Capture the full stdout transcript and paste it into the plan SUMMARY (this becomes part of the Plan 03 closeout evidence per CONTEXT.md D-05).

    Expected outcome: Gates 1-3 PASS as a regression check on the wiring change. If any of Gates 1-3 regress, STOP — return to Plan 01 to diagnose (Plan 01's changes should be inert to the standalone `tts test` path; a regression here means an unintended side effect).
  </action>
  <verify>
    <automated>bash scripts/uat/phase-36.17.5-tts.sh 2>&amp;1 | tee /tmp/36.17.6-02-02-uat.log; grep -F "Phase 36.17.5 TTS UAT: all green" /tmp/36.17.6-02-02-uat.log</automated>
    <human-check>Operator confirms the captured transcript shows `Gate 1`, `Gate 2`, `Gate 3` each printed `OK (...)` and the final `==> Phase 36.17.5 TTS UAT: all green` line is present. Operator types `approved` to advance.</human-check>
  </verify>
  <done>
    - Script `bash scripts/uat/phase-36.17.5-tts.sh` exits 0.
    - `/tmp/36.17.6-02-02-uat.log` contains the literal string `==> Phase 36.17.5 TTS UAT: all green`.
    - All three of `Gate 1`, `Gate 2`, `Gate 3` lines in the transcript show `OK (...)`.
    - Operator pastes the full transcript into the SUMMARY artifact.
    - Operator types `approved`.
  </done>
  <resume-signal>Type `approved` after Gates 1-3 show GREEN and the transcript is captured; or describe any failure (which line printed FAIL, plus the full stderr) so Plan 01 can be revisited.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator manually verifies Gates 4 (toolset inspection shows voice) and 5 (Platform::Local audio playback)</name>
  <files>target/release/ironhermes (binary invoked — not modified)</files>
  <read_first>
    - scripts/uat/phase-36.17.5-tts.sh (header comments lines 26-54 describe the manual procedure for Gates 4 + 5)
    - .planning/phases/36.17.6-toolset-cli-tts-wiring/36.17.6-CONTEXT.md (verification block — exact commands for Gates 4 + 5)
    - .planning/phases/36.17.6-toolset-cli-tts-wiring/36.17.6-RESEARCH.md (Q6 — UAT script body is unchanged; Open Question 2 — correct Gate 4 command is `ironhermes toolset show voice`, not `hermes tools list`)
  </read_first>
  <what-built>
    Plan 01 of this phase wired `register_tts_for_inspection` into both `cmd_toolset_list` and `cmd_toolset_show`. Phase 36.17.5 Plan 04 already shipped `hermes tts play` (folded forward from the original Task 5 scope per the 36.17.5 Plan 04 SUMMARY). Together these enable the two manual gates that the UAT script header (lines 26-54) documents as operator-only.
  </what-built>
  <action>
    With `IRONHERMES_HOME=/tmp/uat-36.17.6` and the release binary built in Task 1:

    **Gate 4 — tool registry exposure:**
    1. Run `IRONHERMES_HOME=/tmp/uat-36.17.6 ./target/release/ironhermes toolset show voice`.
    2. Confirm exit 0 and the output lists BOTH `text_to_speech` and `send_audio` with sensible prerequisite labels.
    3. As a cross-check, also run `IRONHERMES_HOME=/tmp/uat-36.17.6 ./target/release/ironhermes toolset list` and confirm the `voice` row appears with `AVAILABLE=2/2`.
    4. NOTE per RESEARCH.md Open Question 2: the UAT script header comment incorrectly says `hermes tools list` — use `toolset show voice` (singular) per CONTEXT.md `<verification>`.

    **Gate 5 — Platform::Local audible playback:**
    1. Pick the MP3 file from Gate 1's audio_cache (the script printed the path); alternatively, use any pre-existing `.mp3` under `~/.ironhermes/audio_cache/` from prior runs.
    2. Run `./target/release/ironhermes tts play --path <path-to-mp3>`.
    3. EITHER hear audio play through the default output device (PASS — audible playback), OR see a clean error message about no audio device available (also PASS — clean failure, no panic). A panic, stack trace, or `RUST_BACKTRACE` reference is a FAIL.

    Capture both Gate 4 stdout transcripts and the Gate 5 outcome (audible / clean-error) into the plan SUMMARY.
  </action>
  <verify>
    <automated>IRONHERMES_HOME=/tmp/uat-36.17.6 ./target/release/ironhermes toolset show voice 2>&amp;1 | tee /tmp/36.17.6-02-03-gate4.log | grep -E "text_to_speech" &amp;&amp; grep -E "send_audio" /tmp/36.17.6-02-03-gate4.log</automated>
    <human-check>Operator runs `./target/release/ironhermes tts play --path &lt;mp3&gt;` and confirms either audible playback OR a clean no-device error (no panic, no stack trace). Operator types `approved` for Gate 5 if either outcome is observed; types `failed: &lt;reason&gt;` if a panic occurs.</human-check>
  </verify>
  <done>
    - Gate 4: `/tmp/36.17.6-02-03-gate4.log` contains both `text_to_speech` and `send_audio`; `ironhermes toolset show voice` exited 0. Operator confirms with `approved`.
    - Gate 5: `ironhermes tts play --path <mp3>` either plays audible audio OR returns a clean no-device error (no panic / no stack-trace). Operator confirms with `approved`.
    - SUMMARY artifact contains the Gate 4 transcript and a one-line Gate 5 outcome description.
    - Combined with Task 2, all 5 UAT gates are now GREEN.
  </done>
  <resume-signal>Type `approved` for each gate as you confirm; type `failed: <reason>` if either gate fails (which returns control to Plan 01 for diagnosis or to a future fix-plan).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → CLI | Operator-typed env var `IRONHERMES_HOME=/tmp/uat-36.17.6` redirects config/audio paths |
| Operator machine → Edge TTS network | TLS WebSocket handshake for Gate 1 synthesis (Plan 04 already shipped the `rustls 0.23` CryptoProvider install) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.17.6-04 | Tampering | `/tmp/uat-36.17.6` scratch dir on multi-user host | accept | Single-developer UAT context; scratch dir is `/tmp` per CONTEXT.md verification block; no secrets written there |
| T-36.17.6-05 | Information Disclosure | UAT transcript pasted into SUMMARY | accept | Transcript contains MP3 paths + tool names; no API keys (per Gate 3 the keys are unset for the synthesis run) |
| T-36.17.6-SC | Tampering | No new package installs in this plan | accept | UAT script is unchanged from Plan 04; no new dependencies pulled at UAT time |
</threat_model>

<verification>
- Task 1 automated check: `voice` row visible in release binary's `toolset list` output AND `toolset show voice` exit 0.
- Task 2 operator gate: `bash scripts/uat/phase-36.17.5-tts.sh` prints `==> Phase 36.17.5 TTS UAT: all green` and the operator types `approved`.
- Task 3 operator gate: Gates 4 + 5 both confirmed `approved` by the operator with captured transcripts.
- Plan SUMMARY documents the full UAT transcript (script output + Gate 4 outputs + Gate 5 outcome) so Plan 03 can flip 36.17.5-VALIDATION.md with citable evidence.
</verification>

<success_criteria>
- A3 (CONTEXT.md acceptance item 3): `ironhermes toolset list` shows a `voice` row.
- A4 (CONTEXT.md acceptance item 4): `ironhermes toolset show voice` exits 0 and lists both tools.
- A5 (CONTEXT.md acceptance item 5): All 5 UAT gates GREEN; Gates 4 + 5 confirmed by operator `approved`.
- D-04 satisfied: Full UAT re-run (not just 4-5) — Gates 1-3 confirm no regression on `tts test` path.
- D-05 partially satisfied: Plan 03 may now proceed to flip 36.17.5-VALIDATION.md frontmatter.
</success_criteria>

<output>
Create `.planning/phases/36.17.6-toolset-cli-tts-wiring/36.17.6-02-SUMMARY.md` when done, embedding the full UAT transcript and the operator's `approved` confirmations for Gates 4 + 5.
</output>
