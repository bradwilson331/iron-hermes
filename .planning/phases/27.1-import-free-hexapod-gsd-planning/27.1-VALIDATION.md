---
phase: 27.1
slug: import-free-hexapod-gsd-planning
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-10
---

# Phase 27.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | bash + grep (markdown-only meta-phase — no compiled tests required for this phase) |
| **Config file** | none — phase modifies markdown planning artifacts only |
| **Quick run command** | `grep -c "HXP-" .planning/REQUIREMENTS.md` and similar static-grep checks (planner fills exact commands) |
| **Full suite command** | bash test script wrapping all static-grep invariants (planner fills exact path) |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run the static-grep invariants for the artifact just written (REQUIREMENTS / ROADMAP / 27.1.1-CONTEXT / FROZEN)
- **After every plan wave:** Run the full static-grep invariant suite for Phase 27.1
- **Before `/gsd-verify-work`:** Full suite must be green AND `cargo build` from the IronHermes root must still succeed (sanity check that no markdown-vs-tooling drift was introduced)
- **Max feedback latency:** 5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| _to be filled by gsd-planner from PLAN.md tasks_ | | | | | | | | | |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] _to be filled by gsd-planner — likely "create test bash script(s) under tests/ or .planning/checks/ that lock the import contract"_

*If none: "Existing infrastructure covers all phase requirements."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| FROZEN.md committed inside the source Hexapod repo | D-04 | Touches a separate git repo (`~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/`) outside this repo's CI surface | Operator runs `cd ~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi && git log --oneline -1 .planning/FROZEN.md` and confirms a commit exists |

*If none: "All phase behaviors have automated verification."*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
