---
phase: 27.1
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - .planning/phases/27.1-import-free-hexapod-gsd-planning/validate-import.sh
  - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "RESEARCH §Validation Architecture: A static-grep regression test script `.planning/phases/27.1-import-free-hexapod-gsd-planning/validate-import.sh` exists, executable (`chmod +x`), and exits 0 against the post-import state of `.planning/`."
    - "RESEARCH §Phase Requirements → Validation Map: The script enforces 9+ invariants covering D-01 (numbering), D-02 (3 artifact classes), D-03 (HXP- prefix on every imported ID), D-03b (Phase 25 TOOL-01..05 unchanged), D-04 (FROZEN.md presence)."
    - "VALIDATION.md sampling rate: Running the script must complete in ≤ 5 seconds (RESEARCH §Test Framework `Estimated runtime`)."
    - "VALIDATION.md `## Wave 0 Requirements`: This script IS the Wave 0 deliverable — it is what locks the import contract into a future-drift-detection regression test referenced by every subsequent /gsd-verify-work invocation for Phase 27.1."
    - "Source-audit closure: The script exercises every D-NN decision from CONTEXT.md (D-01..D-04 plus sub-decisions D-01a/b, D-02a/b, D-03a/b, D-04a) so the Decision Coverage Gate has machine-verifiable evidence each decision was honored."
    - "D-01b + D-04a: IronHermes' own .planning/PROJECT.md is not modified by this import — INV-27.1-14 confirms no Hexapod content was added."
    - "D-04 source-byte stability (INV-27.1-15): The Hexapod source repo's `.planning/` working tree shows only `.planning/FROZEN.md` as the modified/untracked file — no other source-repo file under `.planning/` has been added, modified, or deleted by this import."
    - "VALIDATION.md is no longer a draft: Wave 0 Requirements and Per-Task Verification Map are populated, and the frontmatter records `wave_0_complete: true` and `nyquist_compliant: true`."
  artifacts:
    - path: ".planning/phases/27.1-import-free-hexapod-gsd-planning/validate-import.sh"
      provides: "Self-contained bash script that grep-asserts the import contract; runs in seconds; non-zero exit on any drift"
      contains: "INV-27.1-"
      min_lines: 80
    - path: ".planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md"
      provides: "Filled-in Wave 0 Requirements + Per-Task Verification Map; frontmatter flips to wave_0_complete: true + nyquist_compliant: true"
      contains: "wave_0_complete: true"
  key_links:
    - from: ".planning/phases/27.1-import-free-hexapod-gsd-planning/validate-import.sh"
      to: ".planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md, .planning/STATE.md, ~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/FROZEN.md, ~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/ (source-byte-stability)"
      via: "grep / test / find against expected post-import contents; git status on source repo .planning/"
      pattern: "INV-27.1-[0-9]{2}"
    - from: ".planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md"
      to: ".planning/phases/27.1-import-free-hexapod-gsd-planning/validate-import.sh"
      via: "Wave 0 Requirements bullet names the script path; Per-Task Verification Map rows reference the script and each plan's auto tasks"
      pattern: "validate-import\\.sh"
---

<objective>
Author the Wave 0 static-grep regression test script that locks the entire Phase 27.1 import contract, and finalize VALIDATION.md so it leaves draft state.

**Purpose:** Per VALIDATION.md `## Wave 0 Requirements` and RESEARCH.md `## Validation Architecture`, this phase has no compiled tests — its acceptance is markdown-grep based. The script consolidates every decision check into one runnable artifact so:
1. Future edits to REQUIREMENTS.md / ROADMAP.md / STATE.md don't silently regress the HXP- contract
2. `/gsd-verify-work 27.1` has a deterministic pass/fail signal
3. Each invariant is named (INV-27.1-NN) so a failure points at the exact contract clause
4. The source Hexapod repo's `.planning/` byte-stability (D-04) is regression-checked, not just one-shot verified in Plan 03

VALIDATION.md is finalized as part of this plan so it exits its `_to be filled by gsd-planner_` draft state.

**Output:**
- One bash script under the phase directory.
- VALIDATION.md `## Wave 0 Requirements` + `## Per-Task Verification Map` populated; frontmatter `wave_0_complete: true` + `nyquist_compliant: true`.
- No new IronHermes-side .md edits beyond these.

**Wave assignment:** Wave 3 alongside Plan 03 — they touch disjoint files (validate-import.sh + VALIDATION.md in IronHermes vs FROZEN.md in source repo). Both depend on Plans 01 and 02 having written the post-import state the script asserts against.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-CONTEXT.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-RESEARCH.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-PATTERNS.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-01-SUMMARY.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `validate-import.sh` static-grep regression test script (executable, ≤5s runtime, 15 named invariants)</name>
  <files>.planning/phases/27.1-import-free-hexapod-gsd-planning/validate-import.sh</files>
  <read_first>
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-RESEARCH.md (lines 376–410 — Validation Architecture section + Phase Requirements → Validation Map; lines 90–149 — Insertion Point Precision)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md (the validation skeleton this plan fills in)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-CONTEXT.md `<decisions>` (D-01..D-04 + sub-decisions — every decision must be locked by at least one INV)
    - .planning/REQUIREMENTS.md (post-Plan-01 state — confirm what the script will assert against)
    - .planning/ROADMAP.md (post-Plan-01 state — confirm 4 INSERTED Phase 27.1 family entries exist)
    - .planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md (post-Plan-02 state — confirm the file the script will scan for un-prefixed IDs)
    - .planning/STATE.md (post-Plan-02 state — confirm the 3 new Roadmap Evolution lines)
  </read_first>
  <action>
**Step A — Create the script.** Use the Write tool at `.planning/phases/27.1-import-free-hexapod-gsd-planning/validate-import.sh` with EXACTLY this content (every line load-bearing — invariant IDs, expected counts, and grep patterns are the contract):

```bash
#!/usr/bin/env bash
# Phase 27.1 — Hexapod GSD planning import contract regression test.
# Runs in ≤5s. Exits 0 if the import contract is intact; exits non-zero with the failing INV ID otherwise.
# Authored 2026-05-10 by Phase 27.1 Plan 04 (Wave 3, Wave 0 validation deliverable).
#
# Total invariants: 15 (INV-27.1-01..15).
# Each INV-27.1-NN locks one or more CONTEXT.md decisions (D-01..D-04 + sub-decisions).

set -uo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)"
HEXAPOD_REPO="${HEXAPOD_REPO:-$HOME/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi}"

REQ="$REPO_ROOT/.planning/REQUIREMENTS.md"
ROADMAP="$REPO_ROOT/.planning/ROADMAP.md"
STATE="$REPO_ROOT/.planning/STATE.md"
CTX="$REPO_ROOT/.planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md"
FROZEN="$HEXAPOD_REPO/.planning/FROZEN.md"

fail() { echo "FAIL [$1]: $2" >&2; exit 1; }
pass() { echo "  pass [$1]"; }

echo "== Phase 27.1 Import Contract Regression =="

# ---------------------------------------------------------------------------
# INV-27.1-01 (D-02 REQUIREMENTS class): `### Hexapod Integration` section exists exactly once
# ---------------------------------------------------------------------------
test -f "$REQ" || fail "INV-27.1-01" "REQUIREMENTS.md missing at $REQ"
n=$(grep -c "^### Hexapod Integration$" "$REQ")
[ "$n" = "1" ] || fail "INV-27.1-01" "expected exactly 1 '### Hexapod Integration' heading in REQUIREMENTS.md, found $n"
pass "INV-27.1-01"

# ---------------------------------------------------------------------------
# INV-27.1-02 (D-03 prefix contract, REQ surface): exactly 16 HXP- definitions
# ---------------------------------------------------------------------------
n=$(grep -cE "^- \[ \] \*\*HXP-(TOOL|LOCO|NAV|DOC)-[0-9]{2}\*\*:" "$REQ")
[ "$n" = "16" ] || fail "INV-27.1-02" "expected 16 HXP- definitions in REQUIREMENTS.md, found $n"
pass "INV-27.1-02"

# ---------------------------------------------------------------------------
# INV-27.1-03 (D-03 prefix contract, traceability surface): exactly 16 HXP- traceability rows
# ---------------------------------------------------------------------------
n=$(grep -cE "^\| HXP-(TOOL|LOCO|NAV|DOC)-[0-9]{2} \| Phase 27\.1\.[123] \| Pending \|$" "$REQ")
[ "$n" = "16" ] || fail "INV-27.1-03" "expected 16 HXP- traceability rows in REQUIREMENTS.md, found $n"
pass "INV-27.1-03"

# ---------------------------------------------------------------------------
# INV-27.1-04 (D-03b non-pollution): existing Phase 25 TOOL-01..05 definitions byte-stable
# ---------------------------------------------------------------------------
n=$(grep -cE "^- \[ \] \*\*TOOL-0[1-5]\*\*:" "$REQ")
[ "$n" = "5" ] || fail "INV-27.1-04" "Phase 25 toolset TOOL-01..05 definition count drifted: expected 5, found $n"
n=$(grep -cE "^\| TOOL-0[1-5] \| Phase 25 \| Pending \|$" "$REQ")
[ "$n" = "5" ] || fail "INV-27.1-04" "Phase 25 toolset TOOL-01..05 traceability count drifted: expected 5, found $n"
pass "INV-27.1-04"

# ---------------------------------------------------------------------------
# INV-27.1-05 (D-02 ROADMAP class): exactly 4 Phase 27.1 family (INSERTED) entries
# ---------------------------------------------------------------------------
test -f "$ROADMAP" || fail "INV-27.1-05" "ROADMAP.md missing"
n=$(grep -cE "^### Phase 27\.1(\.[123])?: .* \(INSERTED\)$" "$ROADMAP")
[ "$n" = "4" ] || fail "INV-27.1-05" "expected 4 Phase 27.1 family INSERTED entries, found $n"
pass "INV-27.1-05"

# ---------------------------------------------------------------------------
# INV-27.1-06 (D-01 numbering, dependency chain): correct Depends-on for each sub-phase
# ---------------------------------------------------------------------------
grep -A4 "^### Phase 27\.1\.1: " "$ROADMAP" | grep -qF "**Depends on:** Phase 27.1" \
  || fail "INV-27.1-06" "Phase 27.1.1 must Depends on: Phase 27.1"
grep -A4 "^### Phase 27\.1\.2: " "$ROADMAP" | grep -qF "**Depends on:** Phase 27.1.1" \
  || fail "INV-27.1-06" "Phase 27.1.2 must Depends on: Phase 27.1.1"
grep -A4 "^### Phase 27\.1\.3: " "$ROADMAP" | grep -qF "**Depends on:** Phase 27.1.2" \
  || fail "INV-27.1-06" "Phase 27.1.3 must Depends on: Phase 27.1.2"
pass "INV-27.1-06"

# ---------------------------------------------------------------------------
# INV-27.1-07 (D-03 prefix contract, ROADMAP surface): no bare TOOL/LOCO/NAV/DOC IDs
#   inside any Phase 27.1.x **Requirements:** line. (Bare = not preceded by HXP-.)
# ---------------------------------------------------------------------------
for phase in 27.1.1 27.1.2 27.1.3; do
  line=$(grep -A2 "^### Phase ${phase}: " "$ROADMAP" | grep "^\*\*Requirements:\*\*" || true)
  [ -n "$line" ] || fail "INV-27.1-07" "missing Requirements line for $phase"
  echo "$line" | grep -qP "(?<!HXP-)\b(TOOL|LOCO|NAV|DOC)-[0-9]+" \
    && fail "INV-27.1-07" "bare ID in Requirements line for $phase: $line"
done
pass "INV-27.1-07"

# ---------------------------------------------------------------------------
# INV-27.1-08 (D-01a Phase 27 byte-stable, D-01a/D-02b Phase 28 byte-stable):
#   adjacent phases unchanged (heading-level sentinel)
# ---------------------------------------------------------------------------
grep -qF "### Phase 27: Prompt Caching" "$ROADMAP" \
  || fail "INV-27.1-08" "Phase 27 heading missing or renamed"
grep -qF "### Phase 28: Skills Trust Tiers" "$ROADMAP" \
  || fail "INV-27.1-08" "Phase 28 heading missing or renamed"
# Pre-import stub MUST be gone
grep -qF "[Urgent work - to be planned]" "$ROADMAP" \
  && fail "INV-27.1-08" "Phase 27.1 placeholder Goal '[Urgent work - to be planned]' still present"
pass "INV-27.1-08"

# ---------------------------------------------------------------------------
# INV-27.1-09 (D-02 CONTEXT class + D-04a phase dir): 27.1.1-CONTEXT.md exists
# ---------------------------------------------------------------------------
test -f "$CTX" || fail "INV-27.1-09" "27.1.1-CONTEXT.md missing at $CTX"
test -d "$(dirname "$CTX")" || fail "INV-27.1-09" "phase directory missing"
pass "INV-27.1-09"

# ---------------------------------------------------------------------------
# INV-27.1-10 (D-03a surface 3): zero un-prefixed (TOOL|LOCO|NAV|DOC)-NN in 27.1.1-CONTEXT.md
# ---------------------------------------------------------------------------
matches=$(grep -nP "(?<!HXP-)\b(TOOL|LOCO|NAV|DOC)-[0-9]+" "$CTX" || true)
if [ -n "$matches" ]; then
  echo "FAIL [INV-27.1-10]: un-prefixed IDs in 27.1.1-CONTEXT.md:" >&2
  echo "$matches" >&2
  exit 1
fi
pass "INV-27.1-10"

# ---------------------------------------------------------------------------
# INV-27.1-11 (D-02 CONTEXT class, fidelity): D-01..D-20 present in copied file
# ---------------------------------------------------------------------------
n=$(grep -cE "^- \*\*D-[0-9]{2}:\*\*" "$CTX")
[ "$n" = "20" ] || fail "INV-27.1-11" "expected 20 decisions D-01..D-20 in 27.1.1-CONTEXT.md, found $n"
# Title + footer cosmetic rewrites
grep -q "^# Phase 27.1.1: Safe Foundation - Context$" "$CTX" \
  || fail "INV-27.1-11" "title line not rewritten to '# Phase 27.1.1: Safe Foundation - Context'"
grep -qF "*Phase: 27.1.1-safe-foundation*" "$CTX" \
  || fail "INV-27.1-11" "footer slug not rewritten to '*Phase: 27.1.1-safe-foundation*'"
pass "INV-27.1-11"

# ---------------------------------------------------------------------------
# INV-27.1-12 (Claude's-Discretion log update): 3 new STATE.md Roadmap Evolution lines
# ---------------------------------------------------------------------------
test -f "$STATE" || fail "INV-27.1-12" "STATE.md missing"
n=$(grep -cE "^- Phase 27\.1\.[123] inserted after Phase 27\.1(\.[12])?: " "$STATE")
[ "$n" = "3" ] || fail "INV-27.1-12" "expected 3 sub-phase Roadmap Evolution lines in STATE.md, found $n"
# Original Phase 27.1 line still exactly once
n=$(grep -cF "- Phase 27.1 inserted after Phase 27: Import Free_Hexapod gsd planning (URGENT)" "$STATE")
[ "$n" = "1" ] || fail "INV-27.1-12" "Phase 27.1 parent log line count drifted: expected 1, found $n"
pass "INV-27.1-12"

# ---------------------------------------------------------------------------
# INV-27.1-13 (D-04 + D-04a): FROZEN.md exists in source repo with required content elements
# ---------------------------------------------------------------------------
if [ -f "$FROZEN" ]; then
  grep -qF "**Import date:** 2026-05-10" "$FROZEN" \
    || fail "INV-27.1-13" "FROZEN.md missing import date"
  grep -qF "**Target repository:** \`~/code/ironhermes\`" "$FROZEN" \
    || fail "INV-27.1-13" "FROZEN.md missing target repository line"
  grep -qF "Phase 27.1.x" "$FROZEN" \
    || fail "INV-27.1-13" "FROZEN.md missing Phase 27.1.x reference"
  grep -qF "**Do not create or modify any planning files in this directory.**" "$FROZEN" \
    || fail "INV-27.1-13" "FROZEN.md missing do-not-edit warning"
  grep -qF "27.1.1-safe-foundation/27.1.1-CONTEXT.md" "$FROZEN" \
    || fail "INV-27.1-13" "FROZEN.md missing pointer to IronHermes Phase 27.1.1 CONTEXT"
  pass "INV-27.1-13"
else
  echo "  skip [INV-27.1-13] FROZEN.md not present at $FROZEN (Plan 03 may not have run yet — non-fatal)"
fi

# ---------------------------------------------------------------------------
# INV-27.1-14 (D-01b + D-04a non-import): IronHermes PROJECT.md not extended with hexapod content
# ---------------------------------------------------------------------------
if [ -f "$REPO_ROOT/.planning/PROJECT.md" ]; then
  grep -q "Hexapod" "$REPO_ROOT/.planning/PROJECT.md" \
    && fail "INV-27.1-14" "PROJECT.md must NOT mention Hexapod (D-01b + D-04a — IronHermes PROJECT.md untouched by import)"
  pass "INV-27.1-14"
else
  echo "  skip [INV-27.1-14] PROJECT.md absent — cannot verify D-01b/D-04a"
fi

# ---------------------------------------------------------------------------
# INV-27.1-15 (D-04 source-byte stability, D-03b source untouched):
#   Hexapod source repo's .planning/ working tree contains ONLY FROZEN.md as a change.
#   Tolerant of both `?? .planning/FROZEN.md` (untracked) and `A  .planning/FROZEN.md`
#   (staged) statuses — committing FROZEN.md is recommended but not required (D-04).
#   Skip gracefully if the source repo is absent (same pattern as INV-27.1-13).
# ---------------------------------------------------------------------------
if [ -d "$HEXAPOD_REPO/.git" ]; then
  # `git status --porcelain` is two-column: XY <space> path. Strip the FROZEN.md
  # row regardless of its status code (?? or A or M etc.), and verify nothing else remains.
  MODS=$(git -C "$HEXAPOD_REPO" status --porcelain -- .planning/ 2>/dev/null \
           | grep -v -E '^\?\? \.planning/FROZEN\.md$' \
           | grep -v -E '^A  \.planning/FROZEN\.md$' \
           | grep -v -E '^M  \.planning/FROZEN\.md$' \
           | grep -v -E '^ M \.planning/FROZEN\.md$' \
           || true)
  if [ -n "$MODS" ]; then
    echo "FAIL [INV-27.1-15]: source repo .planning/ has unexpected modifications beyond FROZEN.md:" >&2
    echo "$MODS" >&2
    exit 1
  fi
  pass "INV-27.1-15"
else
  echo "  skip [INV-27.1-15] Hexapod source repo absent at $HEXAPOD_REPO — cannot verify D-04 source-byte stability"
fi

echo ""
echo "== All 15 Phase 27.1 import invariants passed (or skipped where source/optional files absent) =="
exit 0
```

**Step B — Make the script executable:**

```bash
chmod +x /Users/twilson/code/ironhermes/.planning/phases/27.1-import-free-hexapod-gsd-planning/validate-import.sh
```

**Decision-coverage map (the script's 15 INVs cover every D-NN):**

| Decision | Covered by |
|----------|------------|
| D-01 (sub-phase numbering 27.1.1/.2/.3) | INV-27.1-05, INV-27.1-06 |
| D-01a (Phase 27 unmodified) | INV-27.1-08 |
| D-01b (PROJECT.md untouched) | INV-27.1-14 |
| D-02 (3 artifact classes imported) | INV-27.1-01 (REQ), INV-27.1-05 (ROADMAP), INV-27.1-09 (CONTEXT) |
| D-02a (PROJECT.md / research / codebase / HANDOFF / STATE NOT imported) | INV-27.1-14 (PROJECT specifically); INV-27.1-15 (no other source-side change implies no spurious imports back-fed) |
| D-02b (paths already correct, no rewriting) | INV-27.1-10 (zero un-prefixed IDs ⇒ rewriting limited to ID strings) |
| D-03 (HXP- prefix on every imported ID) | INV-27.1-02, INV-27.1-03, INV-27.1-07, INV-27.1-10 |
| D-03a (atomic propagation across 3 surfaces) | INV-27.1-02 (REQ), INV-27.1-07 (ROADMAP), INV-27.1-10 (CONTEXT) |
| D-03b (existing TOOL-01..05 untouched in IronHermes; source repo IDs left un-prefixed and untouched) | INV-27.1-04 (IronHermes side); INV-27.1-15 (source repo side — no source IDs were renamed by this phase) |
| D-04 (FROZEN.md in source repo; source byte-stability) | INV-27.1-13 (presence + content); INV-27.1-15 (no other source-repo change) |
| D-04a (IronHermes PROJECT.md untouched) | INV-27.1-14 |
| Claude's Discretion (STATE.md log update) | INV-27.1-12 |
| Claude's Discretion (CONTEXT.md fidelity) | INV-27.1-11 |

**HARD CONSTRAINT:** All 15 named INVs must be present in the script body verbatim (each as `# INV-27.1-NN` comment + a fail/pass branch). If a future maintainer removes an INV, the regression test stops covering that decision.
  </action>
  <verify>
    <automated>bash -c '
set -e
cd /Users/twilson/code/ironhermes
S=.planning/phases/27.1-import-free-hexapod-gsd-planning/validate-import.sh
# 1) Script exists and is executable
test -x "$S"
# 2) All 15 INV markers present (each marker appears as a comment header in the script body)
for i in 01 02 03 04 05 06 07 08 09 10 11 12 13 14 15; do
  grep -qF "# INV-27.1-$i " "$S" || grep -qF "# INV-27.1-$i:" "$S" || grep -qF "# INV-27.1-$i (" "$S" || {
    echo "FAIL: missing INV-27.1-$i marker in script"; exit 1;
  }
done
# 3) Script invokes pass for each non-conditional INV at least once (heuristic — full pass count check)
n=$(grep -cE "^pass \"INV-27\\.1-[0-9]{2}\"" "$S")
[ "$n" -ge 12 ] || { echo "FAIL: expected ≥12 pass statements, got $n"; exit 1; }
# 4) Script header announces 15 invariants
grep -qF "Total invariants: 15" "$S"
# 5) Script fails on any drift — sanity test by running it; expect exit 0 against current state
"$S"
echo "validate-import.sh OK (executable, 15 INVs, runs green)"
'</automated>
  </verify>
  <acceptance_criteria>
    - `test -x .planning/phases/27.1-import-free-hexapod-gsd-planning/validate-import.sh` exits 0 (file exists and is executable).
    - All 15 INV markers (`INV-27.1-01` through `INV-27.1-15`) are present as comments inside the script body.
    - At least 12 `pass "INV-27.1-NN"` invocations are emitted at runtime (the three conditional skips for INV-13 / INV-14 / INV-15 depend on FROZEN.md / PROJECT.md / Hexapod-repo presence; the rest must always pass).
    - The header comment block lists `Total invariants: 15` and the summary line names "15 Phase 27.1 import invariants".
    - Running the script against the post-Wave-2 state (REQUIREMENTS.md + ROADMAP.md + 27.1.1-CONTEXT.md + STATE.md all written) exits 0.
    - Running the script after FROZEN.md is also written (post-Plan-03) exits 0 with INV-27.1-13 + INV-27.1-15 both passing instead of skipping (assuming the Hexapod source repo is present on this machine).
    - Total runtime ≤ 5 seconds (per VALIDATION.md `Estimated runtime`).
  </acceptance_criteria>
  <done>The script is on disk, executable, contains all 15 named invariants, and exits 0 against the post-import state. Future drift in REQUIREMENTS.md / ROADMAP.md / STATE.md / 27.1.1-CONTEXT.md / FROZEN.md / source-repo `.planning/` will surface as a precise INV-27.1-NN failure.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize VALIDATION.md — populate Wave 0 Requirements + Per-Task Verification Map; flip frontmatter to wave_0_complete: true + nyquist_compliant: true</name>
  <files>.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md (current draft state — confirm the three `_to be filled by gsd-planner_` markers and the two `false` frontmatter flags)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-01-PLAN.md (Tasks 1 + 2 — REQUIREMENTS.md and ROADMAP.md writes)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-02-PLAN.md (Tasks 1 + 2 — 27.1.1-CONTEXT.md and STATE.md writes)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-03-PLAN.md (Task 1 auto write of FROZEN.md; Task 2 is a checkpoint with no `<automated>` block — excluded from the Per-Task Verification Map per VALIDATION.md sign-off rule "All tasks have `<automated>` verify or Wave 0 dependencies")
  </read_first>
  <action>
Apply three Edit operations to `.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md`. Each Edit uses the precise `old_string` / `new_string` blocks below. Do NOT use sed or any global rewrite — use the Edit tool with exact string targeting.

**EDIT 1 — Frontmatter flip** (both flags change in one Edit because the lines are adjacent):

`old_string`:
```
status: draft
nyquist_compliant: false
wave_0_complete: false
```

`new_string`:
```
status: final
nyquist_compliant: true
wave_0_complete: true
```

**EDIT 2 — Per-Task Verification Map.** Replace the single placeholder row with 7 concrete rows (one per auto task across Plans 01..04; Plan 03 Task 2 is a `checkpoint:human-verify` and is excluded since it has no `<automated>` block — see VALIDATION.md Manual-Only Verifications section).

`old_string`:
```
| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| _to be filled by gsd-planner from PLAN.md tasks_ | | | | | | | | | |
```

`new_string`:
```
| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-T1 | 27.1-01 | 1 | D-02 + D-03 + D-03a (REQ surface) + D-03b | T-27.1-01 | No global find-replace; HXP- prefix on 16 new IDs; Phase 25 TOOL-01..05 byte-stable | static-grep | bash inline (see Plan 01 Task 1 `<automated>`) | .planning/REQUIREMENTS.md | ⬜ pending |
| 01-T2 | 27.1-01 | 1 | D-01 + D-02 + D-02b + D-03 + D-03a (ROADMAP surface) | T-27.1-02 | Stub replaced; 4 INSERTED entries; dependency chain anchored | static-grep | bash inline (see Plan 01 Task 2 `<automated>`) | .planning/ROADMAP.md | ⬜ pending |
| 02-T1 | 27.1-02 | 2 | D-02 (CONTEXT class) + D-02a + D-02b + D-03a (CONTEXT surface) | T-27.1-01, T-27.1-03 | Verbatim copy with 5 ID rewrites + 2 cosmetic rewrites; zero un-prefixed (TOOL\|LOCO\|NAV\|DOC)-NN | static-grep + Perl negative-lookbehind | bash inline (see Plan 02 Task 1 `<automated>`) | .planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md | ⬜ pending |
| 02-T2 | 27.1-02 | 2 | Claude's Discretion (Roadmap Evolution log) | T-27.1-02 | 3 new log lines appended in dependency order; existing 27.1 line preserved | static-grep | bash inline (see Plan 02 Task 2 `<automated>`) | .planning/STATE.md | ⬜ pending |
| 03-T1 | 27.1-03 | 3 | D-04 + D-04a | T-27.1-01 | FROZEN.md written with 5 required elements; no other source-repo file touched | static-grep + git-status | bash inline (see Plan 03 Task 1 `<automated>`) | ~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/FROZEN.md | ⬜ pending |
| 04-T1 | 27.1-04 | 3 | Wave 0 deliverable (all D-NN coverage) | T-27.1-01, T-27.1-02 | 15 named invariants run in ≤5s; non-zero exit on any drift; INV-15 confirms source-byte stability | bash regression script | `.planning/phases/27.1-import-free-hexapod-gsd-planning/validate-import.sh` | .planning/phases/27.1-import-free-hexapod-gsd-planning/validate-import.sh | ⬜ pending |
| 04-T2 | 27.1-04 | 3 | VALIDATION.md finalization (this row) | — | Placeholders replaced; frontmatter flipped to wave_0_complete: true + nyquist_compliant: true | static-grep | `! grep -q "_to be filled by gsd-planner" 27.1-VALIDATION.md && grep -q "wave_0_complete: true" 27.1-VALIDATION.md` | .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md | ⬜ pending |
```

**EDIT 3 — Wave 0 Requirements.** Replace the placeholder bullet with the actual deliverable description naming the script + INVs.

`old_string`:
```
- [ ] _to be filled by gsd-planner — likely "create test bash script(s) under tests/ or .planning/checks/ that lock the import contract"_

*If none: "Existing infrastructure covers all phase requirements."*
```

`new_string`:
```
- [ ] **Wave 0 deliverable:** `.planning/phases/27.1-import-free-hexapod-gsd-planning/validate-import.sh` — bash regression script implementing 15 named invariants (INV-27.1-01 through INV-27.1-15) that lock the entire import contract.
  - **INV-27.1-01..03:** REQUIREMENTS.md surface (Hexapod Integration section, 16 HXP- definitions, 16 traceability rows)
  - **INV-27.1-04:** Phase 25 TOOL-01..05 byte-stability (D-03b)
  - **INV-27.1-05..08:** ROADMAP.md surface (4 INSERTED entries, dependency chain, no bare IDs, Phase 27/28 byte-stable)
  - **INV-27.1-09..11:** 27.1.1-CONTEXT.md surface (file exists, zero un-prefixed IDs, D-01..D-20 verbatim, title/footer rewrites)
  - **INV-27.1-12:** STATE.md Roadmap Evolution surface (3 new lines + parent preserved)
  - **INV-27.1-13:** FROZEN.md surface (5 required content elements + IronHermes pointer)
  - **INV-27.1-14:** IronHermes PROJECT.md untouched (D-01b + D-04a)
  - **INV-27.1-15:** Hexapod source repo `.planning/` byte-stability — only FROZEN.md changed (D-04 + D-03b)

  Authored by Plan 04 Task 1. Runtime ≤ 5s. Exits non-zero on any drift, naming the failing INV.

*If none: "Existing infrastructure covers all phase requirements."*
```

**HARD CONSTRAINTS:**
- All three Edit anchors must match exactly (whitespace + blank lines included). If the executor sees a drift between the `old_string` blocks here and the live VALIDATION.md content, re-read VALIDATION.md first and adjust the anchors before writing — DO NOT proceed with a partial edit.
- The frontmatter `created: 2026-05-10` line is preserved (not in any of the edit blocks).
- The `## Manual-Only Verifications`, `## Sampling Rate`, `## Test Infrastructure`, and `## Validation Sign-Off` sections are NOT touched.
- After all three edits, the file must have ZERO occurrences of the string `_to be filled by gsd-planner`.
  </action>
  <verify>
    <automated>bash -c '
set -e
cd /Users/twilson/code/ironhermes
V=.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md
# 1) File exists
test -f "$V"
# 2) Frontmatter flipped — both flags true
grep -q "^wave_0_complete: true$" "$V"
grep -q "^nyquist_compliant: true$" "$V"
grep -q "^status: final$" "$V"
# 3) No remaining placeholder markers
! grep -q "_to be filled by gsd-planner" "$V"
# 4) Wave 0 Requirements names the script
grep -qF "validate-import.sh" "$V"
grep -qF "INV-27.1-01 through INV-27.1-15" "$V"
# 5) Per-Task Verification Map has all 7 expected rows (auto tasks only — Plan 03 Task 2 checkpoint excluded)
test "$(grep -cE "^\| 0[1-4]-T[12] \| 27\.1-0[1-4] \|" "$V")" = "7"
# 6) Each plan represented
for row in "01-T1" "01-T2" "02-T1" "02-T2" "03-T1" "04-T1" "04-T2"; do
  grep -qF "| $row |" "$V" || { echo "FAIL: missing row $row in Per-Task Verification Map"; exit 1; }
done
# 7) INV-27.1-15 specifically mentioned in Wave 0 Requirements block (D-04 source-byte stability bullet)
grep -qF "INV-27.1-15" "$V"
echo "VALIDATION.md finalized OK"
'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^wave_0_complete: true$" .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md` exits 0.
    - `grep -q "^nyquist_compliant: true$" .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md` exits 0.
    - `grep -q "^status: final$" .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md` exits 0.
    - `! grep -q "_to be filled by gsd-planner" .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md` exits 0 (zero remaining placeholders).
    - `grep -qF "validate-import.sh" .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md` exits 0.
    - `grep -qF "INV-27.1-01 through INV-27.1-15" .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md` exits 0.
    - `grep -cE "^\| 0[1-4]-T[12] \| 27\.1-0[1-4] \|" .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md` returns exactly 7 (the 7 auto-task rows).
    - All seven Task IDs (`01-T1`, `01-T2`, `02-T1`, `02-T2`, `03-T1`, `04-T1`, `04-T2`) are present in the Per-Task Verification Map.
    - `grep -qF "INV-27.1-15" .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md` exits 0 (Wave 0 Requirements names the new INV).
  </acceptance_criteria>
  <done>VALIDATION.md is no longer a draft: the three placeholder regions are populated, both frontmatter flags are flipped to `true`, the document references `validate-import.sh` as the Wave 0 deliverable, and the Per-Task Verification Map lists every auto task across Plans 01–04 (7 rows, excluding Plan 03's checkpoint-only Task 2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script execution → host filesystem | Read-only access to IronHermes `.planning/` and the Hexapod source repo's `.planning/`; no writes performed by the script |
| Script invocation → CI / verifier | Exit code 0/non-zero is the trust signal consumed by `/gsd-verify-work 27.1` |
| Planning artifact → drift detection | VALIDATION.md finalization moves the validation contract out of `draft` so the GSD verifier reads `wave_0_complete: true` as the canonical signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.1-01 | Tampering (self-invalidating grep gate) | Script's own grep patterns | mitigate | Each invariant uses anchored, fixed-string or PCRE patterns rather than counts on raw files. Comment-style INV markers embed the marker as a literal `# INV-27.1-NN` so a `grep -c "INV-27.1-"` against the script itself will correctly find them in comments AND in code (the `pass`/`fail` strings echo the marker). The script does not grep its own body to derive thresholds. |
| T-27.1-02 | Repudiation | Drift slipping past the gate | mitigate | INVs are named (INV-27.1-01..15) so any failure points at a specific contract clause. The decision-coverage map in the action body records which D-NN each INV locks. INV-27.1-15 specifically closes the prior gap where source-repo byte-stability was only a one-shot Plan 03 check. |
| T-27.1-03 | Tampering | VALIDATION.md draft state | mitigate | Task 2 grep-asserts zero occurrences of `_to be filled by gsd-planner` and explicit `wave_0_complete: true` after the edit. Edit anchors use multi-line `old_string` blocks to prevent ambiguous matches. |
| T-27.1-04 | (none) | Network / auth | accept | Script does no I/O beyond local filesystem reads; VALIDATION.md edit is local markdown. Severity: none. |
</threat_model>

<verification>
After Task 1 completes:

```bash
cd /Users/twilson/code/ironhermes
.planning/phases/27.1-import-free-hexapod-gsd-planning/validate-import.sh
# Expected: every INV-27.1-NN reports "pass" (or "skip" for INV-13/14/15 if those files/repos absent), final line "All 15 Phase 27.1 import invariants passed", exit 0.
```

After Task 2 completes:

```bash
V=.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-VALIDATION.md
grep "^wave_0_complete:" "$V"        # wave_0_complete: true
grep "^nyquist_compliant:" "$V"      # nyquist_compliant: true
grep "_to be filled by gsd-planner" "$V" || echo "OK — no placeholders remain"
```

After Plan 03 also completes (cross-plan combined run):

```bash
cd /Users/twilson/code/ironhermes
.planning/phases/27.1-import-free-hexapod-gsd-planning/validate-import.sh
# Expected: INV-27.1-13 + INV-27.1-15 both pass (FROZEN.md present; Hexapod source repo .planning/ contains only FROZEN.md as a change).
```
</verification>

<success_criteria>
1. `validate-import.sh` exists, is executable, and contains 15 named invariants (INV-27.1-01..15).
2. The script exits 0 against the post-import state.
3. Each INV maps to at least one CONTEXT.md decision (decision-coverage map in action body).
4. Runtime ≤ 5 seconds.
5. Future drift in any of the five import surfaces (REQUIREMENTS.md / ROADMAP.md / 27.1.1-CONTEXT.md / STATE.md / FROZEN.md) OR in the Hexapod source repo's `.planning/` byte-stability (INV-15) produces a non-zero exit pointing at a specific INV-27.1-NN.
6. VALIDATION.md exits draft state: Wave 0 Requirements names the script + its 15 INVs; Per-Task Verification Map lists 7 auto-task rows (one per `<automated>` task across Plans 01–04); frontmatter records `wave_0_complete: true` and `nyquist_compliant: true`.
</success_criteria>

<output>
After completion, create `.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-04-SUMMARY.md` describing:
- The script path and runtime
- The 15 named invariants and which decisions each covers (highlight INV-27.1-15 as the new source-byte-stability check)
- Confirmation the script passes against the current post-import state
- Notes on which INVs skip when a file (FROZEN.md, PROJECT.md, Hexapod source repo) is absent
- Confirmation that VALIDATION.md is no longer a draft (`wave_0_complete: true`, `nyquist_compliant: true`, no `_to be filled` markers)
</output>
