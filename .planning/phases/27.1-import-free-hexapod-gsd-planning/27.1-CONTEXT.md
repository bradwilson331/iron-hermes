# Phase 27.1: Import Free_Hexapod gsd planning - Context

**Gathered:** 2026-05-10
**Status:** Ready for planning

<domain>
## Phase Boundary

Import the GSD planning artifacts authored in `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/` (a read-only reference repo) into IronHermes' canonical `.planning/` so the IronHermes × Hexapod Robot Integration work can be planned and executed under this project's GSD tracking.

Source-repo `HANDOFF.json` already declares: *"All Phase 1 code in ~/code/ironhermes, not this repo"*. This phase is the bridge that makes that real — it lands the source repo's roadmap, requirements, and locked phase-1 context into IronHermes so `/gsd-plan-phase 27.1.1` (and later .2/.3) can execute against the canonical IronHermes planning surface.

**This phase is meta:** it imports artifacts, it does NOT implement the hexapod TCP tool. The actual implementation lives in the imported sub-phases:
- **Phase 27.1.1** — Safe Foundation (HXP-TOOL-01..06, HXP-LOCO-01..05)
- **Phase 27.1.2** — Navigation (HXP-NAV-01, HXP-NAV-03, HXP-NAV-04)
- **Phase 27.1.3** — Expression + Skill Doc (HXP-NAV-02, HXP-DOC-01)

</domain>

<decisions>
## Implementation Decisions

### Phase numbering (D-01)
- **D-01:** Map Hexapod's 3 ROADMAP phases to IronHermes sub-phases under 27.1: **27.1.1 Safe Foundation / 27.1.2 Navigation / 27.1.3 Expression + Skill Doc**. Pattern matches the existing `21.8 / 21.8.1 / 21.8.2 / 21.8.3 / 21.8.3.1` precedent. Phase 27.1 itself is the import meta-phase; .1/.2/.3 carry the implementation work.
- **D-01a:** Phase 27 (Prompt Caching, PRMT-08/09) is unrelated and unmodified by this import — only the `.1` sub-tree is touched.
- **D-01b:** Hexapod sub-phases inherit IronHermes' v2.1 milestone framing. **PROJECT.md is not modified by this import** (see D-04). v2.1 milestone framing in PROJECT.md remains "Carry-Overs + Learning Loop"; the hexapod work is additive within the milestone.

### Import scope (D-02)
- **D-02:** Three artifact classes are imported:
  1. **ROADMAP entries** — the 3 Hexapod phase entries land in `.planning/ROADMAP.md` as `### Phase 27.1.1` / `### Phase 27.1.2` / `### Phase 27.1.3` (Goal, Depends-on, Requirements list, Success Criteria preserved).
  2. **REQUIREMENTS** — the 16 v1 requirements land in `.planning/REQUIREMENTS.md` under a new `### Hexapod Integration` section, **renamed per D-03**.
  3. **Phase 1 CONTEXT.md** — `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/phases/01-safe-foundation/01-CONTEXT.md` is copied **verbatim** to `.planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md`, with the rename per D-03 applied throughout. The verbatim copy preserves the locked Hexapod decisions (canonical refs, code-context, claude's-discretion notes from the source repo's discuss-phase) without re-discussion.
- **D-02a:** **NOT imported** — `Hexapod/.planning/PROJECT.md` (D-04 keeps IronHermes PROJECT.md untouched), `Hexapod/.planning/research/`, `Hexapod/.planning/codebase/`, `Hexapod/.planning/HANDOFF.json`, `Hexapod/.planning/STATE.md`. These are reference-only / re-derivable.
- **D-02b:** ROADMAP path-paths — when the Hexapod ROADMAP entries reference paths like `crates/ironhermes-tools/src/hexapod_tcp.rs`, those paths are already correct for IronHermes (the hexapod project authored its planning targeting this codebase). No path rewriting needed beyond ID renames.

### ID collision rename (D-03)
- **D-03:** Apply `HXP-` prefix to **all 16 imported requirement IDs** (not just the colliders) for consistency:
  - `TOOL-01..06` → `HXP-TOOL-01..06` (resolves the Phase 25 "Toolset management" collision on TOOL-01..05)
  - `LOCO-01..05` → `HXP-LOCO-01..05`
  - `NAV-01..04` → `HXP-NAV-01..04`
  - `DOC-01` → `HXP-DOC-01`
- **D-03a:** Rename **must propagate to three surfaces** atomically:
  1. `.planning/REQUIREMENTS.md` — new entries written with HXP- IDs.
  2. `.planning/ROADMAP.md` — Phase 27.1.1/.2/.3 `**Requirements:**` lines reference HXP- IDs.
  3. `.planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md` (verbatim copy) — every `TOOL-NN` / `LOCO-NN` / `NAV-NN` / `DOC-NN` reference inside the copied body is rewritten to its HXP- form before write.
- **D-03b:** The Hexapod source repo retains its un-prefixed IDs (the FROZEN flag in D-04 means it is no longer maintained, so no drift risk). IronHermes is the canonical home post-import.

### Source repo treatment (D-04)
- **D-04:** Mark `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/` as **FROZEN** with a redirect note. Concrete shape:
  - Add `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/FROZEN.md` containing: import date, target repo path (`~/code/ironhermes`), target phase (`Phase 27.1.x`), and a "do not edit" warning.
  - **Do NOT delete** any source files — historical record stays intact for traceability.
  - **Do NOT modify** the source's PROJECT.md / ROADMAP.md / REQUIREMENTS.md / CONTEXT.md content beyond adding FROZEN.md (avoids polluting the source's git history with import-related diffs).
- **D-04a:** IronHermes' own `.planning/PROJECT.md` is **untouched** by this import (decided in Area 2 — PROJECT.md merge was explicitly rejected). The hexapod integration shows up only in ROADMAP.md / REQUIREMENTS.md / phase dirs.

### Claude's Discretion
- Exact wording of `FROZEN.md` redirect note (one paragraph, must include the IronHermes phase reference and the import date).
- Whether the `FROZEN.md` file is committed inside the Hexapod repo or just dropped on disk — recommend **commit** so the freeze is git-recorded in that repo's history.
- Order of operations during execution: REQUIREMENTS rename first (locks IDs), then ROADMAP entries (consumes IDs), then verbatim CONTEXT.md copy (rewrites IDs in-flight), then FROZEN.md (closes the source). This ordering minimizes the window where REQUIREMENTS.md references undefined IDs.
- Whether the imported REQUIREMENTS section sits at the top of `### Active` or in a new `### Hexapod Integration` subsection (recommend the latter for visual grouping; matches PROJECT.md's existing Active-list style).
- Whether to update STATE.md's `### Roadmap Evolution` log with three new "inserted after Phase 27.1" lines for 27.1.1/.2/.3 (recommend yes — STATE.md already tracks every phase insertion in that list).

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Source repo (Hexapod) — read for content to copy
- `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/ROADMAP.md` — 3 phases (Safe Foundation / Navigation / Expression + Skill Doc) with full Goal / Depends-on / Requirements / Success Criteria — this IS the content to copy into IronHermes ROADMAP.md as 27.1.1/.2/.3 (with HXP- ID rewrites)
- `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/REQUIREMENTS.md` — 16 v1 requirements (TOOL-01..06, LOCO-01..05, NAV-01..04, DOC-01) — copy under a new `### Hexapod Integration` section in IronHermes REQUIREMENTS.md, with HXP- prefix applied to all 16 IDs
- `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/phases/01-safe-foundation/01-CONTEXT.md` — Phase 1 locked context — verbatim copy to `.planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md` with HXP- ID rewrites
- `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/PROJECT.md` — read for **understanding only** — provides project framing ("IronHermes × Hexapod Robot Integration", challenge submission, hexapod TCP protocol details) but is **not imported** per D-04a
- `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/HANDOFF.json` — read for **understanding only** — confirms locked decisions for Phase 1 (single hexapod_tcp tool, fresh TCP per call, on_session_end, blocked-cmd-as-Ok). These decisions live in 01-CONTEXT.md (which IS imported); HANDOFF.json itself is reference-only.
- `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/STATE.md` — read for **understanding only** — provides last-activity context. IronHermes' own STATE.md takes over post-import.

### Target repo (IronHermes) — write targets
- `.planning/ROADMAP.md` — receives 3 new phase entries (27.1.1 / 27.1.2 / 27.1.3) inserted after the existing 27.1 entry
- `.planning/REQUIREMENTS.md` — receives 16 new HXP-prefixed requirement entries under a new `### Hexapod Integration` section
- `.planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md` — receives the verbatim-copied + HXP-rewritten Phase 1 context from the source repo
- `.planning/STATE.md` — `### Roadmap Evolution` list gains three new "Phase 27.1.x inserted" entries (per Claude's-Discretion note above)

### Hexapod TCP protocol (downstream — for 27.1.1+ planning, NOT this import phase)
- `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/Code/Server/command.py` — full command set (CMD_MOVE, CMD_ATTITUDE, CMD_BALANCE, CMD_HEAD, CMD_LED, CMD_POWER, CMD_SONIC, CMD_BUZZER, CMD_POSITION, CMD_CALIBRATION). Wire format: UTF-8 text, `#`-separated fields, `\n`-terminated. Only some commands respond.
- `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/CLAUDE.md` — read by 27.1.1+ planning for hexapod-side context (server fragilities: TCP recv splitting, command_queue race, etc.)

### IronHermes tool / agent surface (downstream — for 27.1.1+ planning)
- `crates/ironhermes-tools/src/terminal.rs` — pattern to model `hexapod_tcp.rs` on (single-tool struct, async execute, ToolResult return). Locked in the source repo's 01-CONTEXT.md as the canonical model.
- `crates/ironhermes-tools/src/registry.rs` — `ToolRegistry` registration site for the new `hexapod_tcp` tool
- `crates/ironhermes-tools/src/lib.rs` — pub-use surface; new tool registered here
- `crates/ironhermes-core/src/agent_loop.rs` — `AgentLoop` session lifecycle; `on_session_end` Tool-trait extension hooks here (per source repo D-03 in 01-CONTEXT.md)

### Project conventions (carry-forward, applies to import + downstream sub-phases)
- `.planning/PROJECT.md` — IronHermes "Key Decisions" table, especially:
  - "Cross-crate transport types use plain Strings (no embedded downstream types)" (Phase 22.4.2.2 D-18) — relevant if any new cross-crate type is introduced for hexapod work
  - Architectural Principle #2 — Cache-awareness is load-bearing (no model/memory/context churn mid-session) — applies if hexapod tool changes any of those layers
  - "Out of Scope" list — confirms single-operator deployment + no multi-user auth (matches Hexapod project's "no auth on TCP server" assumption)
- `.planning/REQUIREMENTS.md` — current requirement-ID conventions: prefix-NN format, hyphen-separated. HXP- prefix follows this convention.
- `.planning/phases/26.4.1-config-fix/26.4.1-CONTEXT.md` — recent CONTEXT.md example for IronHermes phase format / canonical_refs style — used as the structural template for this CONTEXT.md

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `crates/ironhermes-tools/src/terminal.rs` — single-tool struct with `Tool` trait impl + async `execute()` returning `ToolResult` — direct model for `hexapod_tcp.rs`. (Reusable in 27.1.1, not this phase.)
- `crates/ironhermes-tools/src/registry.rs` — `ToolRegistry::register()` API pattern — the registration call site for the new tool. (Reusable in 27.1.1.)
- 18 existing tools in `crates/ironhermes-tools/src/` (terminal, browser_*, cronjob_tool, delegate_task, execute_code, file_tools, memory_*, etc.) confirm the established tool-per-file convention.

### Established Patterns
- **Phase-import convention:** This is the first explicit "import external GSD planning" phase. Closest analog is the existing `### Roadmap Evolution` list in STATE.md, which records every "Phase X.Y inserted after Phase X" event — this import will add three such entries (27.1.1/.2/.3).
- **Sub-phase pattern:** `21.8 / 21.8.1 / 21.8.2 / 21.8.3 / 21.8.3.1` — five-deep nesting confirms this project supports sub-phases as first-class, no roadmap restructuring needed for 27.1.x.
- **Verbatim-context-copy convention:** No precedent. New convention introduced by this phase: when an external CONTEXT.md is copied in, it lands as-is except for ID rewrites; the import phase's own CONTEXT.md (this file) records that the copy happened. Downstream agents read the copied CONTEXT.md, not the import phase's CONTEXT.md, when planning the imported sub-phases.
- **Plain-String cross-crate transport** (PROJECT.md Key Decisions) — applies if 27.1.1's hexapod_tcp tool ever needs to surface protocol-status enums to other crates. The source repo's 01-CONTEXT.md likely already accounts for this.

### Integration Points
- `.planning/ROADMAP.md` — current Phase 27 / 27.1 / 28 entries; 27.1.1/.2/.3 must insert between 27.1 and 28 without breaking the existing dependency chain (Phase 28 depends on Phase 21.8 + 25, not on 27.x — verified via grep).
- `.planning/REQUIREMENTS.md` — 139-line file; the new `### Hexapod Integration` section is additive (no edits to existing TOOL-01..05 / Toolset management entries).
- IronHermes binary entry points (`crates/ironhermes-cli/src/main.rs` `run_chat`, `run_single`, `run_gateway`) — registration sites for the new tool. **Out of scope for 27.1**, in scope for 27.1.1.

</code_context>

<specifics>
## Specific Ideas

- **Source repo path** is hardcoded by the user's filesystem layout: `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/`. Tilde-expanded once at planning time; absolute paths in all canonical_refs.
- **Verbatim copy means verbatim** — no editorial cleanup of the source 01-CONTEXT.md beyond ID rewrites. If the source has typos / formatting drift, they come over. Future edits happen in IronHermes only (per D-04 freeze).
- **HXP- prefix is project-canonical for this integration** — future hexapod work in IronHermes (e.g., later milestones adding video-stream, vision, or face-rec) reuses HXP- naming so the namespace stays consistent.
- **FROZEN.md content** should include: (1) date imported, (2) target repo path, (3) target phase reference (27.1.x), (4) explicit "do not edit — see IronHermes" warning, (5) brief explanation of why both repos exist (Freenove repo is the robot reference + Pi server code, IronHermes is the agent).

</specifics>

<deferred>
## Deferred Ideas

- **PROJECT.md merge** — The Hexapod project's PROJECT.md framing as a "challenge submission" (IronHermes × Hexapod, Gemma 4 gets a body) was explicitly rejected for this import. If the integration becomes prominent, a future phase can promote it into IronHermes' PROJECT.md milestone framing.
- **research/ + codebase/ artifact import** — Hexapod's `.planning/research/` and `.planning/codebase/` directories were not imported. If a future hexapod sub-phase needs that material, re-derive via `/gsd-map-codebase` or fetch from the FROZEN source.
- **Video stream + vision support** — Hexapod's port 8002 video stream is explicitly deferred in the source PROJECT.md ("Out of Scope"). Belongs in a future hexapod milestone, not 27.1.x.
- **Migrating un-prefixed IDs in the source repo** — D-03b chose to leave the source repo's IDs un-prefixed (FROZEN). If anyone ever revives the Hexapod repo's planning, they can rename then; not this phase's concern.
- **Cross-repo CI / link checking** — verifying that IronHermes' canonical_refs into the Hexapod repo (e.g., to `Code/Server/command.py`) stay valid as the Hexapod repo evolves. Out of scope; manual responsibility for now.

</deferred>

---

*Phase: 27.1-import-free-hexapod-gsd-planning*
*Context gathered: 2026-05-10*
