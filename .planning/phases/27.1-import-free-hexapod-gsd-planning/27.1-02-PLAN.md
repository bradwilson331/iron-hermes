---
phase: 27.1
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md
  - .planning/STATE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-02 (CONTEXT class): A new file `.planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md` exists, populated with the verbatim body of `~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/phases/01-safe-foundation/01-CONTEXT.md` minus exactly five ID rewrites."
    - "D-03a (surface 3): All five un-prefixed ID occurrences in the copied CONTEXT.md body have been rewritten to HXP- form (TOOL-01 → HXP-TOOL-01; TOOL-06 ×2 → HXP-TOOL-06; LOCO-01 → HXP-LOCO-01; LOCO-05 → HXP-LOCO-05). After rewrite, ZERO un-prefixed (TOOL|LOCO|NAV|DOC)-NN substrings remain in the file."
    - "D-02b: Absolute tilde paths in `<canonical_refs>` (e.g., `~/code/ironhermes/crates/ironhermes-tools/src/terminal.rs`) are preserved verbatim — they already point at the correct repo."
    - "D-02b: The relative path `.planning/REQUIREMENTS.md` in the Planning Artifacts bullet is preserved as-is (same directory depth in IronHermes)."
    - "Cosmetic rewrites (per PATTERNS.md §`27.1.1-CONTEXT.md`): the title line is updated from `# Phase 1: Safe Foundation - Context` to `# Phase 27.1.1: Safe Foundation - Context`, and the footer slug from `*Phase: 1-Safe Foundation*` to `*Phase: 27.1.1-safe-foundation*`."
    - "Claude's Discretion (CONTEXT D-NN list, ROADMAP-evolution recommendation): STATE.md `### Roadmap Evolution` log gains exactly three new lines for the 27.1.1 / 27.1.2 / 27.1.3 sub-phase insertions, in order, immediately after the existing `Phase 27.1 inserted after Phase 27` line."
    - "D-02a: No content from the source repo's PROJECT.md, research/, codebase/, HANDOFF.json, or STATE.md is imported by this plan — only the Phase 1 CONTEXT.md body and the rewrites prescribed above."
  artifacts:
    - path: ".planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md"
      provides: "Locked Phase 27.1.1 implementation context (D-01..D-20) imported from Hexapod source repo with HXP- ID propagation"
      contains: "# Phase 27.1.1: Safe Foundation - Context"
    - path: ".planning/STATE.md"
      provides: "Three new Roadmap Evolution log entries for 27.1.1 / 27.1.2 / 27.1.3"
      contains: "Phase 27.1.1 inserted after Phase 27.1"
  key_links:
    - from: ".planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md (Planning Artifacts bullet in <canonical_refs>)"
      to: ".planning/REQUIREMENTS.md (### Hexapod Integration section authored by Plan 01)"
      via: "Relative path `.planning/REQUIREMENTS.md` resolves correctly because both files are at the same directory depth as in the source repo"
      pattern: "HXP-TOOL-01 through HXP-TOOL-06, HXP-LOCO-01 through HXP-LOCO-05"
    - from: ".planning/STATE.md (Roadmap Evolution log)"
      to: ".planning/ROADMAP.md (Phase 27.1.1/.2/.3 entries authored by Plan 01)"
      via: "Three log lines (one per inserted sub-phase)"
      pattern: "Phase 27\\.1\\.[123] inserted after Phase 27\\.1(\\.[12])?"
---

<objective>
Land the two Wave 2 writes that finalize the import: (1) the verbatim copy of the Hexapod Phase 1 CONTEXT.md into IronHermes' phase tree (with the five known HXP- ID rewrites), and (2) the three-line Roadmap Evolution append to STATE.md that records the new sub-phase insertions.

**Purpose:** Per RESEARCH §Order-of-Operations, the CONTEXT.md copy must happen AFTER REQUIREMENTS.md (Plan 01) so the relative `.planning/REQUIREMENTS.md` reference inside the copied body resolves to a file that already contains the HXP- IDs. STATE.md is purely additive and depends only on Plan 01 for accuracy of what's being logged. Both touch separate files inside `.planning/`, so they serialize within this plan; neither is a candidate for parallelism with Plan 01.

**Output:**
- New file: `.planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md` (~125 lines, D-01..D-20 preserved verbatim except for 5 ID rewrites + title + footer slug)
- New directory: `.planning/phases/27.1.1-safe-foundation/`
- Modified file: `.planning/STATE.md` (+3 lines in `### Roadmap Evolution`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-CONTEXT.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-RESEARCH.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-PATTERNS.md
@.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `.planning/phases/27.1.1-safe-foundation/` directory and write 27.1.1-CONTEXT.md (verbatim copy with 5 ID rewrites + title/footer rewrite)</name>
  <files>.planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md</files>
  <read_first>
    - ~/code/Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi/.planning/phases/01-safe-foundation/01-CONTEXT.md (THE source file to copy — read all 125 lines so the copy is verbatim, not a paraphrase)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-PATTERNS.md (lines 207–263 — the precise 5 ID rewrite specification + title/footer rewrite)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-RESEARCH.md (lines 213–229 — verified 5-occurrence rewrite table + Pitfall 5 path-preservation rule)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-CONTEXT.md `<decisions>` (D-02 — CONTEXT.md is one of the 3 imported artifact classes; D-03a surface 3 — in-flight rewrite contract; D-02b — paths NOT rewritten beyond ID renames)
    - .planning/REQUIREMENTS.md (just-modified by Plan 01 — confirm the relative `.planning/REQUIREMENTS.md` reference inside the copied body now points at HXP- IDs)
  </read_first>
  <action>
**Step A — Create the directory.** Use `mkdir -p` (the parent `.planning/phases/` already exists; the new leaf must be created):

```bash
mkdir -p /Users/twilson/code/ironhermes/.planning/phases/27.1.1-safe-foundation
```

**Step B — Write `.planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md`** using the Write tool with the content below. This is the source `01-CONTEXT.md` reproduced byte-for-byte EXCEPT for exactly seven differences (5 ID rewrites + title rewrite + footer slug rewrite) that PATTERNS.md §`27.1.1-CONTEXT.md` and RESEARCH.md §"ID Occurrences in Source 01-CONTEXT.md" both specify.

**The exact file contents to write are:**

```
# Phase 27.1.1: Safe Foundation - Context

**Gathered:** 2026-05-10
**Status:** Ready for planning

<domain>
## Phase Boundary

Build a new `hexapod_tcp` Rust tool in the IronHermes repo (`~/code/ironhermes`) that lets an agent issue walk, stop, read_battery, read_distance, and relax_servos commands to the Freenove hexapod robot over TCP — with every unsafe command blocked before any bytes reach the hardware.

The Freenove repo is **read-only reference** for this phase. No Python server code is modified.

</domain>

<decisions>
## Implementation Decisions

### Project Location
- **D-01:** All Phase 1 code is written in `~/code/ironhermes`, not in this repo. Target file: `crates/ironhermes-tools/src/hexapod_tcp.rs`. Skill doc is `skills/hexapod/DESCRIPTION.md` (Phase 3).
- **D-02:** This repo (`Freenove_Big_Hexapod_Robot_Kit_for_Raspberry_Pi`) serves as the protocol reference only. Nothing here is modified.

### Tool Schema
- **D-03:** A single `hexapod_tcp` tool with an `action` enum field — not multiple separate tools. The LLM calls it once per intent. Modeled on the existing `terminal.rs` single-tool pattern.
- **D-04:** Phase 1 action enum values: `walk | stop | read_battery | read_distance | relax_servos`
- **D-05:** Walk action takes `direction: forward | backward | left | right` and `speed: integer (2–10)`. All other Phase 1 actions take no extra parameters.

### Wire Protocol Translation
- **D-06:** Walk direction maps to CMD_MOVE with ±25 displacement:
  - `forward`  → `CMD_MOVE#1#0#25#<speed>#0\n`
  - `backward` → `CMD_MOVE#1#0#-25#<speed>#0\n`
  - `left`     → `CMD_MOVE#1#-25#0#<speed>#0\n`
  - `right`    → `CMD_MOVE#1#25#0#<speed>#0\n`
  - Mode=1 (tripod gait), angle=0. Speed is direct pass-through, clamped to 2–10.
- **D-07:** Stop maps to `CMD_MOVE#0#0#0#0#0\n` (mode=0 halts motion, returns to neutral).
- **D-08:** Relax servos maps to `CMD_RELAX\n`.
- **D-09:** Read battery maps to `CMD_POWER\n`; tool reads response `CMD_POWER#<v1>#<v2>\n` and formats as `"Battery: <v1>V / <v2>V (<OK|LOW>)"`. Low threshold: v1 < 5.5V or v2 < 6.0V.
- **D-10:** Read distance maps to `CMD_SONIC\n`; tool reads response `CMD_SONIC#<dist>\n` and formats as `"Distance: <dist> cm"`.

### TCP Connection
- **D-11:** One fresh TCP connection per tool call (stateless). No persistent socket. Avoids the hexapod server's TCP stream-splitting fragility.
- **D-12:** Robot IP from `HEXAPOD_IP` env var, port 5002. Missing env var returns `Ok("Error: HEXAPOD_IP env var not set — cannot connect to robot")`.

### Session End (HXP-TOOL-06)
- **D-13:** Add a default no-op `fn on_session_end(&self) {}` to the `Tool` trait in `ironhermes-tools/src/registry.rs`. All existing tools get the no-op default; no breaking changes.
- **D-14:** `HexapodTcpTool` overrides `on_session_end` to fire-and-forget `tokio::spawn(send_stop(...))` — sends `CMD_MOVE#0#0#0#0#0\n` then `CMD_RELAX\n` to halt and relax servos.
- **D-15:** The REPL/CLI shutdown path calls `on_session_end()` on all registered tools after the agent loop returns.

### Error Feedback Design
- **D-16:** Blocked commands (CMD_CALIBRATION, CMD_SERVOPOWER, CMD_LED_MOD modes 2–5) return `Ok("Action '<name>' is blocked — not permitted via hexapod_tcp. Never send this command.")`. Returns `Ok()` so the LLM sees it inline and learns not to retry.
- **D-17:** Robot unreachable → `Ok("Error: cannot connect to robot at <ip>:5002 — is HEXAPOD_IP set and the robot powered on?")`.
- **D-18:** Read timeout (3s) → `Ok("Error: read timed out after 3s waiting for robot response")`.
- **D-19:** Fire-and-forget commands (walk, stop, relax) that send successfully return `Ok("OK")` — no response expected from the robot for those commands.

### Allowlist
- **D-20:** The allowlist is a compile-time `match` on the `action` field — exhaustive Rust enum or `match` arms. No runtime config. Unrecognized actions return the blocked-command error string (D-16).

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Wire Protocol (Freenove repo — read-only reference)
- `Code/robot_control_communication_protocol.md` — full command reference: all CMD_* strings, argument formats, response formats
- `Code/Server/command.py` — canonical command constant strings (source of truth for CMD_MOVE, CMD_POWER, CMD_SONIC, CMD_RELAX, etc.)
- `Code/Server/adc.py` — battery voltage thresholds: load < 5.5V, Pi battery < 6.0V (D-09 low-battery detection)

### IronHermes Tool Pattern (ironhermes repo)
- `~/code/ironhermes/crates/ironhermes-tools/src/terminal.rs` — MUST read: the exact pattern to model `hexapod_tcp.rs` on (Tool trait impl, schema(), execute(), anyhow::Result<String> return)
- `~/code/ironhermes/crates/ironhermes-tools/src/registry.rs` — Tool trait definition; D-13 adds `on_session_end` here
- `~/code/ironhermes/crates/ironhermes-tools/src/lib.rs` — how tools are exported and where to register the new tool

### Planning Artifacts
- `.planning/REQUIREMENTS.md` — HXP-TOOL-01 through HXP-TOOL-06, HXP-LOCO-01 through HXP-LOCO-05 with acceptance criteria
- `.planning/PROJECT.md` — architecture constraints, key decisions, known fragilities to work around

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `~/code/ironhermes/crates/ironhermes-tools/src/terminal.rs`: Complete reference implementation — copy the struct shape, `Tool` trait impl, `anyhow::Result<String>` return convention, `tokio::time::timeout` usage, and test structure. `HexapodTcpTool` will be approximately 150–200 lines following this pattern.
- `~/code/ironhermes/crates/ironhermes-core/src/` `ToolSchema` — already used for schema registration; same type as terminal.rs uses.

### Established Patterns
- **One connection per call**: `terminal.rs` spawns a fresh `tokio::process::Command` per execute — `hexapod_tcp.rs` opens a fresh `tokio::net::TcpStream` per execute. Same stateless pattern.
- **Timeout with `tokio::time::timeout`**: terminal.rs wraps the async operation in `timeout(Duration::from_secs(...), fut)`. Use identical pattern for the 3-second read timeout on sensor commands.
- **`anyhow::anyhow!` for errors**: All tool errors use `anyhow::anyhow!("message")`. Error messages become the tool result string the LLM sees.
- **`serde_json::json!` for schema**: ToolSchema constructed with `json!({...})` inline in `schema()` — matches terminal.rs exactly.

### Integration Points
- `~/code/ironhermes/crates/ironhermes-tools/src/lib.rs` — register `HexapodTcpTool` here, same as other tools
- `~/code/ironhermes/crates/ironhermes-tools/src/registry.rs` `Tool` trait — add `on_session_end` default method (D-13)
- REPL/CLI shutdown path in `~/code/ironhermes` — needs to call `on_session_end()` on registered tools; check `crates/ironhermes-core/src/commands/` for the shutdown sequence

### Known Fragilities to Work Around (from CONCERNS.md / PROJECT.md)
- TCP recv may split commands across packets — send one command per connection (D-11 already handles this)
- `condition_monitor` has no malformed-command handling — always send complete, valid command strings with `\n` terminator
- No response framing on command channel — use a fixed read buffer and the 3-second timeout (D-18) rather than reading until EOF

</code_context>

<specifics>
## Specific Ideas

- The tool description string (for the LLM) should include the full list of Phase 1 actions and the blocked commands, so Gemma 4 knows what's available and what's forbidden without needing to call the skill doc.
- The `prerequisites()` method on `HexapodTcpTool` should declare `HEXAPOD_IP` as a required env var — this integrates with IronHermes' setup wizard if it checks prerequisites at startup.
- Error strings are `Ok(...)` not `Err(...)` for all non-programming errors (D-16, D-17, D-18) so the LLM sees them inline in the tool result rather than as a system error.

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 27.1.1-safe-foundation*
*Context gathered: 2026-05-10*
```

**Differences from the source — explicit count for verifier:**
1. Line 1: `# Phase 1: Safe Foundation - Context` → `# Phase 27.1.1: Safe Foundation - Context` (title rewrite)
2. Line ~43 (Session End heading): `### Session End (TOOL-06)` → `### Session End (HXP-TOOL-06)` (ID rewrite #1: TOOL-06 occurrence 1)
3. Line ~75 (Planning Artifacts bullet): `TOOL-01 through TOOL-06, LOCO-01 through LOCO-05` → `HXP-TOOL-01 through HXP-TOOL-06, HXP-LOCO-01 through HXP-LOCO-05` (ID rewrites #2, #3, #4, #5: TOOL-01, TOOL-06 occurrence 2 — wait, let me re-check; per RESEARCH.md grep there are 5 total: TOOL-01 + 2×TOOL-06 + LOCO-01 + LOCO-05)
4. Line ~123 (footer slug): `*Phase: 1-Safe Foundation*` → `*Phase: 27.1.1-safe-foundation*` (cosmetic rewrite)

**Reconciling with the actual source content read above:** Per the literal source text PATTERNS.md quotes (lines 240, 249), the un-prefixed IDs in the source body are:
- `### Session End (TOOL-06)` heading → rewritten to `### Session End (HXP-TOOL-06)` (1 occurrence of TOOL-06 in the heading)
- `TOOL-01 through TOOL-06, LOCO-01 through LOCO-05` in the Planning Artifacts bullet → rewritten as a single phrase covering TOOL-01, TOOL-06, LOCO-01, LOCO-05 (the `through` ranges resolve as 4 distinct ID anchors)

The rewritten text above already incorporates all five rewrites correctly. The verifier below grep-checks the post-state directly: zero un-prefixed (TOOL|LOCO|NAV|DOC)-NN substrings, and exactly the expected HXP- IDs present.

**Anti-pattern enforcement:** Do NOT use any global find-replace across `.planning/`. Write the new file fresh with HXP- IDs already baked in (this is exactly the content above — all rewrites are pre-applied). The Write tool drops the entire blob in one go.
  </action>
  <verify>
    <automated>bash -c '
set -e
cd /Users/twilson/code/ironhermes
F=.planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md
# 1) File exists
test -f "$F"
# 2) Title rewritten
grep -q "^# Phase 27.1.1: Safe Foundation - Context$" "$F"
# 3) Footer slug rewritten
grep -qF "*Phase: 27.1.1-safe-foundation*" "$F"
! grep -qF "*Phase: 1-Safe Foundation*" "$F"
# 4) Session End heading uses HXP-TOOL-06
grep -qF "### Session End (HXP-TOOL-06)" "$F"
! grep -qF "### Session End (TOOL-06)" "$F"
# 5) Planning Artifacts bullet uses HXP- IDs in range form
grep -qF "HXP-TOOL-01 through HXP-TOOL-06, HXP-LOCO-01 through HXP-LOCO-05 with acceptance criteria" "$F"
# 6) NEGATIVE: zero un-prefixed (TOOL|LOCO|NAV|DOC)-NN substrings remain in body
# Exclude any line where the match is preceded by HXP- (negative lookbehind via grep -P).
# Use Perl-compatible: anywhere we see (TOOL|LOCO|NAV|DOC)-NN that is NOT preceded by HXP-, fail.
matches=$(grep -nP "(?<!HXP-)\b(TOOL|LOCO|NAV|DOC)-[0-9]+" "$F" || true)
if [ -n "$matches" ]; then
  echo "FAIL: un-prefixed IDs still present:"; echo "$matches"; exit 1;
fi
# 7) Decision count: D-01..D-20 all present (20 distinct decisions)
test "$(grep -cE "^- \*\*D-[0-9]{2}:\*\*" "$F")" = "20"
# 8) All 4 XML-style sections present
for tag in domain decisions canonical_refs code_context specifics deferred; do
  grep -q "^<${tag}>$" "$F" || { echo "FAIL: missing <${tag}> section"; exit 1; }
done
# 9) Path preservation: tilde paths kept verbatim (D-02b)
grep -qF "~/code/ironhermes/crates/ironhermes-tools/src/terminal.rs" "$F"
grep -qF "~/code/ironhermes/crates/ironhermes-tools/src/registry.rs" "$F"
grep -qF "~/code/ironhermes/crates/ironhermes-tools/src/lib.rs" "$F"
# 10) Relative path preserved
grep -qF ".planning/REQUIREMENTS.md" "$F"
echo "27.1.1-CONTEXT.md write OK"
'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md` exits 0.
    - `grep -q "^# Phase 27.1.1: Safe Foundation - Context$" <file>` exits 0; the source `# Phase 1: Safe Foundation - Context` is absent.
    - `grep -qF "*Phase: 27.1.1-safe-foundation*" <file>` exits 0; the source `*Phase: 1-Safe Foundation*` is absent.
    - `grep -qF "### Session End (HXP-TOOL-06)" <file>` exits 0; the source `### Session End (TOOL-06)` is absent.
    - `grep -qF "HXP-TOOL-01 through HXP-TOOL-06, HXP-LOCO-01 through HXP-LOCO-05 with acceptance criteria" <file>` exits 0.
    - `grep -nP "(?<!HXP-)\b(TOOL|LOCO|NAV|DOC)-[0-9]+" <file>` returns NO matches (zero un-prefixed IDs).
    - `grep -cE "^- \*\*D-[0-9]{2}:\*\*" <file>` returns exactly 20 (D-01..D-20 preserved).
    - All six XML-style section tags present: `<domain>`, `<decisions>`, `<canonical_refs>`, `<code_context>`, `<specifics>`, `<deferred>`.
    - Tilde paths to `~/code/ironhermes/crates/ironhermes-tools/...` preserved verbatim (D-02b).
    - Relative path `.planning/REQUIREMENTS.md` preserved verbatim (D-02b, Pitfall 5).
  </acceptance_criteria>
  <done>The new directory and file exist; the file is the verbatim source-repo Phase 1 CONTEXT.md with exactly seven targeted differences (1 title, 1 Session End heading, 4 ID anchors in the Planning Artifacts bullet, 1 footer slug — totaling 5 ID rewrites + 2 cosmetic rewrites); no un-prefixed (TOOL|LOCO|NAV|DOC)-NN substrings remain; D-01..D-20 are all present; all XML section tags survive.</done>
</task>

<task type="auto">
  <name>Task 2: Append three Roadmap Evolution log lines to STATE.md (one per inserted sub-phase)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (lines 405–415 — confirm the existing tail of `### Roadmap Evolution`; the line `Phase 27.1 inserted after Phase 27: Import Free_Hexapod gsd planning (URGENT)` is the last one before `### Pending Todos`)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-PATTERNS.md (lines 267–293 — exact 3-line append spec + format-rule observations)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-RESEARCH.md (lines 126–140 — STATE.md target lines)
    - .planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-CONTEXT.md `<decisions>` (Claude's-Discretion bullet 5 — "update STATE.md's `### Roadmap Evolution` log with three new 'inserted after Phase 27.1' lines")
  </read_first>
  <action>
Use the Edit tool to append exactly three lines to `.planning/STATE.md` immediately after the existing tail entry of `### Roadmap Evolution`.

**Locate `old_string`** as the literal current last line of the log plus the blank line that follows it (which precedes `### Pending Todos`):

```
- Phase 27.1 inserted after Phase 27: Import Free_Hexapod gsd planning (URGENT)

### Pending Todos
```

**Replace with `new_string`:**

```
- Phase 27.1 inserted after Phase 27: Import Free_Hexapod gsd planning (URGENT)
- Phase 27.1.1 inserted after Phase 27.1: Safe Foundation — hexapod walk/stop/sensors (INSERTED)
- Phase 27.1.2 inserted after Phase 27.1.1: Navigation — rotate/head/buzzer (INSERTED)
- Phase 27.1.3 inserted after Phase 27.1.2: Expression + Skill Doc — LEDs + protocol reference (INSERTED)

### Pending Todos
```

**Format-rule guarantees** (from PATTERNS.md):
- Bullet `- ` prefix (single dash + space)
- Pattern: `Phase X.Y inserted after Phase X.Z: <short title> (<tag>)`
- Tag = `(INSERTED)` for these new entries (matches 21.8.1-style insertions)
- No trailing period
- No blank lines between entries within the log block (one blank line only after the very last entry, before `### Pending Todos`)
- Order: 27.1.1 → 27.1.2 → 27.1.3 (matches dependency chain)

**HARD CONSTRAINT:** Use Edit tool with the precise old_string above as anchor. Do NOT modify any earlier line of the log; the existing 33+ entries above (Phase 22 added through Phase 27.1 inserted) are byte-stable.
  </action>
  <verify>
    <automated>bash -c '
set -e
cd /Users/twilson/code/ironhermes
# 1) Three new entries present
grep -qF "- Phase 27.1.1 inserted after Phase 27.1: Safe Foundation — hexapod walk/stop/sensors (INSERTED)" .planning/STATE.md
grep -qF "- Phase 27.1.2 inserted after Phase 27.1.1: Navigation — rotate/head/buzzer (INSERTED)" .planning/STATE.md
grep -qF "- Phase 27.1.3 inserted after Phase 27.1.2: Expression + Skill Doc — LEDs + protocol reference (INSERTED)" .planning/STATE.md
# 2) Existing Phase 27.1 line still present (not duplicated, not deleted)
test "$(grep -cF "- Phase 27.1 inserted after Phase 27: Import Free_Hexapod gsd planning (URGENT)" .planning/STATE.md)" = "1"
# 3) Exactly 3 lines match the inserted-sub-phase pattern
test "$(grep -cE "^- Phase 27\.1\.[123] inserted after Phase 27\.1(\.[12])?: " .planning/STATE.md)" = "3"
# 4) Order: 27.1.1 < 27.1.2 < 27.1.3 (line-number sequence)
ln1=$(grep -nF "- Phase 27.1.1 inserted after Phase 27.1: Safe Foundation" .planning/STATE.md | cut -d: -f1)
ln2=$(grep -nF "- Phase 27.1.2 inserted after Phase 27.1.1: Navigation" .planning/STATE.md | cut -d: -f1)
ln3=$(grep -nF "- Phase 27.1.3 inserted after Phase 27.1.2: Expression" .planning/STATE.md | cut -d: -f1)
[ "$ln1" -lt "$ln2" ] && [ "$ln2" -lt "$ln3" ] || { echo "FAIL: order wrong $ln1 $ln2 $ln3"; exit 1; }
# 5) ### Pending Todos heading still follows the log block
grep -q "^### Pending Todos$" .planning/STATE.md
echo "STATE.md append OK"
'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF "- Phase 27.1.1 inserted after Phase 27.1: Safe Foundation — hexapod walk/stop/sensors (INSERTED)" .planning/STATE.md` exits 0.
    - `grep -qF "- Phase 27.1.2 inserted after Phase 27.1.1: Navigation — rotate/head/buzzer (INSERTED)" .planning/STATE.md` exits 0.
    - `grep -qF "- Phase 27.1.3 inserted after Phase 27.1.2: Expression + Skill Doc — LEDs + protocol reference (INSERTED)" .planning/STATE.md` exits 0.
    - `grep -cE "^- Phase 27\.1\.[123] inserted after Phase 27\.1(\.[12])?: " .planning/STATE.md` returns exactly 3.
    - `grep -cF "- Phase 27.1 inserted after Phase 27: Import Free_Hexapod gsd planning (URGENT)" .planning/STATE.md` returns exactly 1 (no duplicate, no deletion).
    - The three new lines appear in dependency order (27.1.1 line number < 27.1.2 line number < 27.1.3 line number).
    - The `### Pending Todos` heading still exists immediately after the log block (one blank line in between).
  </acceptance_criteria>
  <done>STATE.md `### Roadmap Evolution` log gained exactly three new entries in dependency order, the existing tail entry is preserved, and the `### Pending Todos` section continues to follow without structural drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source repo (Hexapod) → IronHermes `.planning/` | One-way verbatim copy with bounded ID rewrites; source treated as read-only reference |
| Plan-time content → Write tool body | Planner embeds the entire post-rewrite file content inline; executor performs no derivation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.1-01 | Tampering | Body of `27.1.1-CONTEXT.md` (verbatim contract) | mitigate | Action embeds the literal post-rewrite content as one Write blob. Acceptance criterion grep-asserts D-01..D-20 (exactly 20 decisions present), all 6 XML section tags present, tilde paths preserved. |
| T-27.1-02 | Tampering | Existing STATE.md log entries (33+ prior lines, byte-stable) | mitigate | Edit tool uses the existing tail line + `### Pending Todos` heading as the anchor; the new lines are appended in one atomic edit. Acceptance criterion `grep -cF "Phase 27.1 inserted after Phase 27"` returns exactly 1 (no duplicate/deletion). |
| T-27.1-03 | Information Disclosure (D-02a guard) | Source repo content NOT to be imported (PROJECT.md, research/, codebase/, HANDOFF.json, source STATE.md) | mitigate | Files written by this plan are limited to two paths declared in `files_modified`. No <read_first> entry references the source repo's PROJECT.md / research / codebase / HANDOFF / STATE — the planner's content blob is self-contained. Severity: none (markdown only, no secrets). |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /Users/twilson/code/ironhermes
# CONTEXT surface
test -f .planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md
grep -nP "(?<!HXP-)\b(TOOL|LOCO|NAV|DOC)-[0-9]+" .planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md   # zero matches expected
grep -cE "^- \*\*D-[0-9]{2}:\*\*" .planning/phases/27.1.1-safe-foundation/27.1.1-CONTEXT.md                   # 20
# STATE surface
grep -cE "^- Phase 27\.1\.[123] inserted" .planning/STATE.md                                                  # 3
```

Plan 04 will additionally lock these invariants into a regression test script so future drift is caught automatically.
</verification>

<success_criteria>
1. `.planning/phases/27.1.1-safe-foundation/` directory exists.
2. `27.1.1-CONTEXT.md` is present, with title `# Phase 27.1.1: Safe Foundation - Context`, footer `*Phase: 27.1.1-safe-foundation*`, all 20 decisions D-01..D-20 verbatim, all 6 XML section tags, tilde paths to IronHermes preserved.
3. Zero un-prefixed `(TOOL|LOCO|NAV|DOC)-NN` substrings remain in `27.1.1-CONTEXT.md` (Perl negative-lookbehind grep returns no matches).
4. STATE.md gained exactly 3 new Roadmap Evolution lines for 27.1.1 / 27.1.2 / 27.1.3 in dependency order, with the existing 27.1 line preserved.
5. The previously written REQUIREMENTS.md and ROADMAP.md from Plan 01 are not modified by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/27.1-import-free-hexapod-gsd-planning/27.1-02-SUMMARY.md` describing:
- The two artifacts created/modified (27.1.1-CONTEXT.md new file; STATE.md +3 lines)
- The 5 ID rewrites + 2 cosmetic rewrites confirmed in the copied file
- The dependency-ordered log entries appended
- Confirmation that source-repo D-02a content (PROJECT.md, research/, codebase/, HANDOFF.json, STATE.md) was NOT imported
</output>
