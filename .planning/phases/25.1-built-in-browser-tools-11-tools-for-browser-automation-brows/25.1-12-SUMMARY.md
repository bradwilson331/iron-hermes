---
phase: 25.1-built-in-browser-tools-11-tools-for-browser-automation-brows
plan: "12"
subsystem: cli
tags: [gap-closure, toolset, browser, regression-test]
dependency_graph:
  requires: [25.1-09]
  provides: [GAP-1-fix]
  affects: [toolset_cmd.rs]
tech_stack:
  added: []
  patterns: [KNOWN_TOOLSETS slice extension, static members map, regression tests]
key_files:
  created: []
  modified:
    - crates/ironhermes-cli/src/toolset_cmd.rs
decisions:
  - Add "browser" as 7th entry in KNOWN_TOOLSETS; map all 11 browser_* tools in toolset_members_map()
metrics:
  duration: "~5 minutes"
  completed: "2026-04-30T20:25:00Z"
  tasks_completed: 1
  files_changed: 1
---

# Phase 25.1 Plan 12: Browser Toolset Gap Closure (GAP-1) Summary

Extend `KNOWN_TOOLSETS` in `crates/ironhermes-cli/src/toolset_cmd.rs` from 6 to 7 entries by adding `"browser"`, wire the 11 browser_* tool names into `toolset_members_map()`, and add 2 regression tests that pin the fix.

## Tasks Completed

| Task | Description | Commit | Files |
|------|-------------|--------|-------|
| 1 | Extend KNOWN_TOOLSETS + members map + regression tests | 8af9261 | crates/ironhermes-cli/src/toolset_cmd.rs |

## Changes Made

### `KNOWN_TOOLSETS` extended (line 15)

Before: 6-entry slice `["web", "code", "memory", "agent", "skills", "session"]`

After: 7-entry slice `["web", "code", "memory", "agent", "skills", "session", "browser"]`

### `toolset_members_map()` extended

Added `"browser"` key mapping to all 11 browser_* tool names:
`browser_back`, `browser_click`, `browser_close`, `browser_console`, `browser_get_images`,
`browser_navigate`, `browser_press`, `browser_scroll`, `browser_snapshot`, `browser_type`, `browser_vision`

### Regression tests added

- `browser_in_known_set` — asserts `KNOWN_TOOLSETS.contains(&"browser")` and `len() == 7`
- `cmd_toolset_enable_accepts_browser` — calls `cmd_toolset_enable(tmp.path(), "browser")` and asserts `Ok`

## Verification

```
cargo check -p ironhermes-cli       → Finished (0 errors, pre-existing warnings only)
cargo test -p ironhermes-cli --lib toolset_cmd::tests
  running 7 tests
  test toolset_cmd::tests::browser_in_known_set ... ok
  test toolset_cmd::tests::validate_toolset_name_accepts_known_d01_names ... ok
  test toolset_cmd::tests::validate_toolset_name_rejects_empty ... ok
  test toolset_cmd::tests::validate_toolset_name_rejects_path_traversal ... ok
  test toolset_cmd::tests::validate_toolset_name_rejects_uppercase ... ok
  test toolset_cmd::tests::cmd_toolset_enable_rejects_unknown_name ... ok
  test toolset_cmd::tests::cmd_toolset_enable_accepts_browser ... ok
  test result: ok. 7 passed; 0 failed
```

## Scope Discipline

- `DEFAULT_TOOLSETS` in `crates/ironhermes-core/src/constants.rs:35` was NOT modified (default-enabled list is distinct from the known list and out of scope for this gap closure).

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes introduced.

## Self-Check: PASSED

- File exists: `crates/ironhermes-cli/src/toolset_cmd.rs` — FOUND
- Commit exists: `8af9261` — FOUND
- grep `"browser"` count: 4 (≥ 2 required) — PASSED
- DEFAULT_TOOLSETS unchanged — PASSED
- All 7 toolset_cmd tests pass — PASSED
