---
status: partial
phase: 25.1-built-in-browser-tools-11-tools-for-browser-automation-brows
source: [25.1-01-SUMMARY.md, 25.1-02-SUMMARY.md, 25.1-03-SUMMARY.md, 25.1-04-SUMMARY.md, 25.1-05-SUMMARY.md, 25.1-06-SUMMARY.md, 25.1-07-SUMMARY.md, 25.1-08-SUMMARY.md, 25.1-09-SUMMARY.md, 25.1-10-SUMMARY.md, 25.1-11-SUMMARY.md]
started: 2026-04-30T20:05:00Z
updated: 2026-04-30T20:14:00Z
---

## Current Test

[testing paused — 8 items blocked by gap GAP-1 (browser toolset not enumerated in CLI). Resume after fix lands.]

## Tests

### 1. Cold Start Smoke Test
expected: Build hermes; run with --help; binary starts cleanly, prints usage, exits 0. No panics or stack traces.
result: pass

### 2. Browser toolset is default-disabled (D-04)
expected: Run `hermes toolset list` (or equivalent toolset-status command). The `browser` toolset appears in the list and shows enabled=false. The 11 browser_* tools do NOT appear in the active tool schema (no LLM call needed — this is the registry view).
result: issue
reported: |
  TOOLSET    STATUS     TOOLS   AVAILABLE
  memory     enabled    1       1/1 (memory ✓)
  session    enabled    1       1/1 (session_search ✓)
  agent      enabled    2       2/2 (delegate_task ✓, cronjob ✓)
  skills     enabled    1       1/1 (skills ✓)
  web        disabled   2       1/2 (web_search ✗ FIRECRAWL_API_KEY, web_read ✓)
  code       disabled   6       6/6 (execute_code ✓, terminal ✓, read_file ✓, write_file ✓, list_dir ✓, grep_files ✓)
severity: major

### 3. Cache-break banner on enable (D-19)
expected: Run `hermes toolset enable browser`. Output displays the cache-break banner — same format as Phase 25 toolsets — indicating the schema cache will rebuild on the next LLM call. After enable, `hermes toolset list` shows browser as enabled=true.
result: blocked
blocked_by: prior-phase
reason: |
  User reported (verbatim): `ironhermes toolset enable browser` → `Error: unknown toolset 'browser' — known toolsets: agent, code, memory, session, skills, web`. Same root cause as test 2 — the CLI's known-toolset list does not include `browser`. Tracked in GAP-1.

### 4. Chromium binary discovery (D-05)
expected: With chromium installed (or BROWSER_PATH set to a chromium binary), all 11 browser_* tools are available in a session. Without chromium and BROWSER_PATH unset (or set to /dev/null/x), no browser_* tools appear in get_definitions output. Test by running a session with and without chromium present.
result: blocked
blocked_by: prior-phase
reason: Cannot enable the browser toolset (GAP-1) — tools are unreachable in any session.

### 5. browser_navigate + browser_snapshot end-to-end
expected: Start a hermes chat session with browser toolset enabled. Tell the agent to "navigate to <some URL with a button> and take a snapshot." Agent's tool calls succeed; the snapshot output contains a numeric `[N] button "..."` line for the page's interactive button. Refs are stable for that one snapshot call.
result: blocked
blocked_by: prior-phase
reason: Depends on the browser toolset being enable-able (GAP-1).

### 6. browser_click consumes ref + stale-ref handling (D-10/D-11)
expected: After a snapshot, ask the agent to "click the button with ref N." The click executes (page state changes) without error. Then ask the agent to navigate to a different page, and click the SAME ref (stale). The response contains a structured `element_stale` error envelope rather than a Rust panic; the agent recovers by re-snapshotting.
result: blocked
blocked_by: prior-phase
reason: Depends on the browser toolset being enable-able (GAP-1).

### 7. URL allowlist enforcement (D-15, T-25.1-01)
expected: Set `browser.allowed_domains: ["example.com"]` in config. Ask the agent to navigate to `https://wikipedia.org/`. The tool returns a structured `domain_blocked` error envelope (not a successful nav, no exception). Then nav to `https://example.com/` — succeeds.
result: blocked
blocked_by: prior-phase
reason: Depends on the browser toolset being enable-able (GAP-1).

### 8. browser_console eval gating (D-13, T-25.1-02)
expected: With yolo OFF, ask the agent to evaluate JS via `browser_console mode:"eval" expression:"document.title"`. The tool returns an `approval_needed` envelope showing the JS expression — does NOT execute. With yolo ON, the same call evaluates and returns the result. browser_console mode:"log" is NEVER gated.
result: blocked
blocked_by: prior-phase
reason: Depends on the browser toolset being enable-able (GAP-1).

### 9. browser_vision routes through auxiliary cascade (D-07)
expected: With `auxiliary.vision: { provider: openai, model: gpt-4o-mini }` (or any multimodal model) configured, ask the agent to "use browser_vision to describe what's on the page." The vision call hits the configured aux provider, NOT the main provider. (Verify by inspecting provider logs or by seeing the model name in the response.) Without auxiliary.vision configured AND with a vision-capable main provider, falls back to main. Without either, browser_vision is excluded from get_definitions.
result: blocked
blocked_by: prior-phase
reason: Depends on the browser toolset being enable-able (GAP-1).

### 10. browser_close idempotency + session re-spawn (D-03)
expected: Ask the agent to navigate, then close the browser. Then ask it to navigate again. The second navigate spawns a fresh BrowserSession (lazy re-init). Calling close twice doesn't error.
result: blocked
blocked_by: prior-phase
reason: Depends on the browser toolset being enable-able (GAP-1).

## Summary

total: 10
passed: 1
issues: 1
pending: 0
skipped: 0
blocked: 8

## Gaps

# GAP-1
- truth: "browser toolset appears in `hermes toolset list` and is enable-able via `hermes toolset enable browser` (D-04 toolset enumeration extended from 6 to 7)"
  status: failed
  reason: |
    User reported two consistent failures:
    (1) Test 2 — `hermes toolset list` output shows only 6 toolsets: memory, session, agent, skills, web, code. `browser` is absent.
    (2) Test 3 — `hermes toolset enable browser` returns: `Error: unknown toolset 'browser' — known toolsets: agent, code, memory, session, skills, web`.
    The browser toolset is registered in `ToolsConfig::default()` (Plan 02) and the 11 browser_* tools are wired in the registry (Plan 09), but the CLI command's known-toolset enumeration was not extended. Likely a hardcoded list/match arm in `crates/ironhermes-cli/src/main.rs` (or wherever the toolset CLI subcommand parses arguments) covering only the original 6 enum variants.
  severity: major
  test: 2
  cascading_blocked_tests: [3, 4, 5, 6, 7, 8, 9, 10]
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""
