---
phase: 25.1
slug: built-in-browser-tools-11-tools-for-browser-automation-brows
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-30
---

# Phase 25.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | cargo test (Rust 1.x — workspace) |
| **Config file** | `Cargo.toml` (workspace root) + `crates/ironhermes-tools/Cargo.toml` |
| **Quick run command** | `cargo test -p ironhermes-tools` |
| **Full suite command** | `cargo test --workspace` |
| **Estimated runtime** | ~60s quick / ~300s full (CI may skip integration tests when no Chromium binary is available — D-22) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-tools` (scoped to changed crate)
- **After every plan wave:** Run `cargo test --workspace`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds (quick) / 300 seconds (full)

---

## Per-Task Verification Map

> The planner fills this table during plan-phase. Each row maps a task ID to its automated verify command and threat ref. The integration tests below (D-20/D-21) are pre-allocated; per-tool unit tests will be enumerated by the planner per file.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 25.1-XX-XX | XX | N | TBD | T-25.1-XX / — | TBD | integration | `cargo test -p ironhermes-tools --test browser_integration -- browser_navigate_then_snapshot_returns_refs` | ❌ W0 | ⬜ pending |
| 25.1-XX-XX | XX | N | TBD | T-25.1-XX / — | element_stale envelope returned (D-11) | integration | `cargo test -p ironhermes-tools --test browser_integration -- browser_click_with_stale_ref_returns_structured_error` | ❌ W0 | ⬜ pending |
| 25.1-XX-XX | XX | N | TBD | T-25.1-XX / — | vision request hits aux server, not main (D-07) | integration | `cargo test -p ironhermes-tools --test browser_integration -- browser_vision_routes_to_auxiliary_vision_role` | ❌ W0 | ⬜ pending |
| 25.1-XX-XX | XX | N | TBD | T-25.1-XX / — | schemas absent without chromium (D-21) | integration | `cargo test -p ironhermes-tools --test browser_prereq -- browser_tools_excluded_when_chromium_missing` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/ironhermes-tools/Cargo.toml` — add `chromiumoxide = "0.9"` (RESEARCH OQ-1 confirms 0.9.1 is the current crates.io release; pin "0.9" for SemVer compat)
- [ ] `crates/ironhermes-tools/tests/browser_integration.rs` — stubs for D-20 three integration tests
- [ ] `crates/ironhermes-tools/tests/browser_prereq.rs` — stub for D-21 schema-exclusion test
- [ ] Verify wiremock 0.6 + reqwest 0.12 are already workspace deps (RESEARCH confirms — no install needed)
- [ ] Confirm `AnthropicClient` handles `ContentPart::ImageUrl` with `data:image/png;base64,...` URLs (RESEARCH OQ-2 — must inspect `anthropic_client.rs` before implementing `browser_vision`); if missing, add Wave-0 task to extend the client

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Headed-mode debugging works in dev | D-02 | Visible window cannot be asserted in CI (no display) | Set `browser.headed: true` in dev config; run a session with `browser_navigate` + `browser_snapshot`; visually confirm Chromium window opens and renders the target URL |
| Toolset-cache-break banner emits on enable | D-19 | UX confirmation; banner exists from Phase 25 — Phase 25.1 inherits | Run `hermes toolset enable browser`; visually confirm `[toolset: browser] enabled — schema cache will rebuild on next LLM call` appears |
| End-to-end vision call against a real provider | D-07 | Requires real provider keys + multimodal model; not run in CI | With `auxiliary.vision: { provider: openai, model: gpt-4o }` and `OPENAI_API_KEY` set, run a session that calls `browser_vision` on a known page; confirm the model description matches the page content |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags (cargo test runs once-and-exit)
- [ ] Feedback latency < 60s (quick) / < 300s (full)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
