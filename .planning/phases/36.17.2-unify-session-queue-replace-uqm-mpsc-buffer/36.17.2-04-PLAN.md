---
phase: 36.17.2
plan: 04
type: execute
wave: 4
depends_on:
  - 36.17.2-01
  - 36.17.2-02
  - 36.17.2-03
files_modified:
  - crates/ironhermes-gateway/tests/session_queue_telegram_uat.md
autonomous: false
requirements:
  - D-22  # Live Telegram UAT runbook with 4 scenarios + Scenario 6 multimodal burst (checker M1 follow-up)
  - D-12  # HTTP 429 deferred — webhook/REST adapter not yet in scope; documented as orphan-decision note (checker minor nit)
  - T-36.17.2-01  # Worker-exit/dispatch race scenario
  - T-36.17.2-02  # Notify lost-wakeup (documented as accepted)
  - T-36.17.2-03  # Spurious 👁 during cancellation (documented as accepted)
  - T-36.17.2-04  # Multimodal sidecar drift — live evidence via Scenario 6

must_haves:
  truths:
    - "session_queue_telegram_uat.md Scenario 1 expects 👁 emission AT POP TIME (worker), not at dispatch time"
    - "A new scenario validates the worker-exit/dispatch race (T-36.17.2-01) live"
    - "Cap-hit Scenario reads identically to 36.17.1-05 (the ❌ + chat reply UX is preserved verbatim)"
    - "/new clears queue Scenario reads identically to 36.17.1-05 (Pitfall 5 ordering preserved)"
    - "Drain-mode + cross-process out-of-scope notes are preserved from 36.17.1-05 (no behavior change in 36.17.2)"
    - "Blocking-human checkpoint task gates phase sign-off on user typing 'approved' or describing issues"
    - "Scenario 6 verifies multimodal photo/document burst preserves payload in FIFO lockstep with text-message order (M1 sidecar live evidence; T-36.17.2-04)"
    - "D-12 (HTTP 429 deferred) is called out as an orphan-decision footnote so the runbook doesn't read like a coverage gap"
  artifacts:
    - path: "crates/ironhermes-gateway/tests/session_queue_telegram_uat.md"
      provides: "Updated UAT runbook for the unified architecture (replaces 36.17.1-05 version)"
      contains: "Phase 36.17.2"
      contains_2: "👁"
      contains_3: "pop time"
      contains_4: "T-36.17.2-01"
  key_links:
    - from: "Scenario 1 (busy burst)"
      to: "Worker pop-loop architecture"
      via: "Expected: 👁 reaches Telegram for each message at the moment of processing"
      pattern: "pop time|moment of (?:pop|processing)"
    - from: "Scenario 5 (NEW — worker-exit race)"
      to: "T-36.17.2-01 mitigation"
      via: "Send message → wait drain → kill worker via /new → send another → verify processing"
      pattern: "T-36.17.2-01"
---

<objective>
Update the existing `crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` (created in 36.17.1-05) to match the unified architecture from 36.17.2. The critical edit is Scenario 1's expected behavior: under the old (36.17.1) architecture, 👁 reactions fired at dispatch time (UQM emitted them when landing on the mpsc queue); under the new (36.17.2) architecture, 👁 reactions fire at pop time (the worker emits them immediately before each `handle_with_multimodal` turn). The runbook MUST instruct the verifier to watch for 👁 reactions appearing SEQUENTIALLY as each message is processed — not as a burst at dispatch.

Add a new Scenario 5 validating T-36.17.2-01 (worker-exit/dispatch race): the verifier sends one message, waits for the agent to finish (worker exits because queue is empty), then sends another message after a deliberate gap. The new message must be processed (proving the post-exit dispatch correctly spawns a fresh worker per D-15/D-16 + Plan 01's mutex serialization).

Preserve all existing scenarios from 36.17.1-05 unchanged in their expected outcomes (cap-hit UX, /new clear-queue, drain-mode + cross-process out-of-scope). The wording for those is identical because 36.17.2 doesn't alter their behavior — only Scenario 1's reaction-emission timing changes.

The plan ships `autonomous: false` (D-22 mandates a `checkpoint:human-verify` task gating phase sign-off on live Telegram verification).

Output: rewritten `session_queue_telegram_uat.md` updated for the unified architecture; one human-verify checkpoint task instructing the user to execute the runbook against their live bot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-CONTEXT.md
@.planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-01-SUMMARY.md
@.planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-02-SUMMARY.md
@.planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-03-SUMMARY.md
@.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-05-SUMMARY.md
@./CLAUDE.md

<interfaces>
<!-- The UAT runbook from 36.17.1-05 is the starting template. Plan 04 surgically edits it. -->

From crates/ironhermes-gateway/tests/session_queue_telegram_uat.md (existing, 408 lines per 36.17.1-05 SUMMARY):
- 6 scenarios already documented (Scenario 1 busy-enqueue, 2 /queue command, 3 cap-hit, 4a /new clear, 4b is_draining drain-mode, plus an Out-of-Scope cross-process section)
- Sign-off checklist + verifier name + date + notes block

From this phase's CONTEXT.md decisions:
- D-08: 👁 emission moves from dispatch to worker (this is the only USER-VISIBLE change in 36.17.2)
- D-22 scenario list: (a) burst 5 → all 5 process FIFO with 👁 firing on each at moment of pop; (b) cap-hit at 128 → ❌ + chat reply from dispatch (unchanged); (c) /new clears queue mid-burst (unchanged); (d) shutdown + restart drain-mode (unchanged, sanity check)
- T-36.17.2-01: NEW scenario for worker-exit/dispatch race
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update session_queue_telegram_uat.md for the unified architecture + add T-36.17.2-01 scenario</name>
  <files>crates/ironhermes-gateway/tests/session_queue_telegram_uat.md</files>
  <read_first>
    - crates/ironhermes-gateway/tests/session_queue_telegram_uat.md (full file — current state from 36.17.1-05)
    - .planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-CONTEXT.md (D-08, D-22; T-36.17.2-01..03)
    - .planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-05-SUMMARY.md (the runbook's authoring conventions — scenario structure, sign-off template)
    - .planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-02-SUMMARY.md (final worker structure — informs the "what to watch for" wording in Scenario 1)
  </read_first>
  <action>
Edit `crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` in place. Concrete edits:

**Edit 1 — Document header / phase reference (top of file):**

The current header reads `Phase 36.17.1`. Update to:
- Title: keep `Session Queue — Telegram Live UAT`.
- Replace `Phase 36.17.1` phase callout with `Phase 36.17.2 (supersedes 36.17.1-05)`.
- Add a new bullet: `**Architecture (locked):** UnifiedQueueing (Option C, D-01..D-22). UserQueueManager's per-chat mpsc buffer has been removed; SessionQueue is the single source of truth for buffered messages. 👁 transport reactions now fire AT POP TIME (the moment the worker begins processing each message), NOT at dispatch time (when the message lands on the queue). This is the only user-visible change from 36.17.1.`
- Update the locked-decision-callouts bullet list to reference D-01, D-04, D-08, D-11, D-13, D-22 from 36.17.2-CONTEXT.md (in addition to the inherited 36.17.1 callouts already listed).

**Edit 2 — Scenario 1 (busy-enqueue):**

This is the critical edit. Locate Scenario 1's `Expected` section in the existing runbook. Current expected wording (paraphrasing from 36.17.1-05's busy-enqueue scenario): the verifier sends N messages rapidly while the agent is busy on the first one, and the expected outcome notes that 👁 reactions appear on messages 2..N "as they are queued by UserQueueManager" or similar dispatch-time wording.

Replace the Expected section with the unified-architecture expectation:

```
**Expected (Phase 36.17.2 unified architecture):**

1. Message 1 starts processing immediately. You may see a 👁 reaction appear on message 1 BEFORE the agent begins (this is the worker emitting 👁 at the moment of pop — D-06 step 3, D-08). Note: the legacy 36.17.1 behavior emitted 👁 only on messages 2..N. Under 36.17.2, every message gets 👁 at the moment its turn begins, INCLUDING the first one.
2. Messages 2..5 sit silently in the SessionQueue while message 1 is processed. NO 👁 reactions appear on messages 2..5 yet — they have not been popped.
3. When message 1's agent turn completes, the worker pops message 2. A 👁 reaction appears on message 2 at THIS moment (not earlier). The agent begins processing message 2.
4. Repeat for messages 3, 4, 5 — each gets a 👁 reaction at the moment of pop, sequentially.
5. Final state: all 5 messages have been processed in FIFO order; each has a 👁 reaction; the SessionQueue depth for this session is 0.

**Regression signal:** If 👁 reactions appear on messages 2..5 BEFORE message 1's turn completes, the architecture has regressed — 👁 emission has moved back to dispatch (violating D-08). File a bug citing T-36.17.2-03 regression and DO NOT sign off on this scenario.

**Architectural justification:** Under 36.17.1, 👁 emission was a dispatch-time signal ("UserQueueManager has accepted your message into its buffer"). Under 36.17.2, 👁 is a processing-start signal ("the agent has begun working on this specific message"). The new semantics align with user mental models: when 👁 appears, the agent is actually looking at the message.
```

The step-by-step verifier instructions at the top of Scenario 1 (the numbered "Send 5 messages in quick succession" steps) do NOT change — only the Expected section.

**Edit 3 — Scenario 2 (/queue command):**

No expected-behavior change. The /queue slash command from 36.17.1-03 is unchanged in 36.17.2. Leave the scenario verbatim.

Add a one-line clarification at the top of Scenario 2: `**Note (36.17.2):** /queue continues to write directly to SessionQueue via the gateway's handle_slash_command intercept. UQM::dispatch is bypassed for /queue (slash commands are not whitelist/dispatch-path messages). This scenario verifies the slash-command path is unaffected by the dispatch-path refactor.`

**Edit 4 — Scenario 3 (cap-hit):**

The cap-hit UX is unchanged in expected outcome (D-11 preserves the ❌ + "⏳ Queue is full (128 messages)..." chat reply verbatim). BUT the **layer** that fires the UX moved: in 36.17.1 it was `handler.rs:991-1011` inside `handle_with_multimodal`; in 36.17.2 it's `user_queue.rs::dispatch` (Plan 01 D-11). The user-visible behavior is identical, so the scenario's verifier steps and expected output don't change.

Add a one-line note at the top of Scenario 3: `**Note (36.17.2 internal migration):** The cap-hit UX literal (❌ reaction + "⏳ Queue is full (128 messages). Wait for the agent to drain before sending more." chat reply) now fires from inside UserQueueManager::dispatch (D-11). User-visible behavior is identical to 36.17.1. If you see the cap-hit reply for messages that should NOT have hit cap (e.g., depth was <128), file a bug — the migration may have moved the emission to the wrong layer.`

The Pitfall 6 (offset-advance) note in Scenario 3 remains unchanged — runner.rs:723-736 still does the unconditional offset advance before dispatch, exactly as in 36.17.1.

**Edit 5 — Scenario 4a (/new clears queue):**

No expected-behavior change. /new continues to call `session_queue.clear(&session_key)` BEFORE `session_store.remove(&session_key)` (Pitfall 5, 36.17.1-03 D-13). Leave verbatim.

Add one-line note: `**Note (36.17.2):** /new behavior is unchanged. The clear-queue-before-remove ordering is preserved from 36.17.1-03 (handler.rs NewSession arm).`

**Edit 6 — Scenario 4b (is_draining drain-mode):**

No change. 36.17.1-04's drain-mode flag is untouched in 36.17.2 (D-17 explicitly defers cross-restart preservation). Leave verbatim.

**Edit 7 — NEW Scenario 5 (T-36.17.2-01 worker-exit/dispatch race):**

Insert immediately after Scenario 4b, before the "Out of Scope" cross-process section:

```
## Scenario 5 — Worker-exit/dispatch race (T-36.17.2-01)

**Goal:** Verify that after a per-chat worker exits (queue drained to empty, worker calls UQM::remove and dies), a subsequent dispatch correctly spawns a fresh worker. This validates the mutex serialization in UserQueueManager between `dispatch` and `remove` (D-19 + T-36.17.2-01 mitigation).

**Verifier steps:**

1. Send a single message: `"first message after race test"`. Wait for the agent to fully complete its turn — confirm the agent has fully responded AND the 👁 reaction is visible AND no further activity is happening (look for the "agent finished" log line `tail -f` on the gateway logs, or just wait ~30 seconds after the agent's last visible reply).

2. At this point, the per-chat worker for your SessionKey has exited (it popped the queue, found it empty, hit the cancel/notify select arm with no pending notification, and dropped through to the `queue_task.remove(&session_key_task).await` call at the bottom of the worker spawn closure).

3. Wait at least 5 seconds AFTER the agent's final reply. This guarantees the worker has fully exited and released its Notify clone.

4. Send a second message: `"second message after race test"`.

5. Confirm: the second message is processed normally. A 👁 reaction appears on it. The agent responds.

**Expected (Phase 36.17.2):**

- The second message MUST be processed. If it sits in the queue indefinitely with no 👁 reaction, the dispatch-after-worker-exit path is broken (T-36.17.2-01 mitigation has regressed).
- gateway logs MUST show TWO instances of "Per-chat worker exited" or equivalent debug line — one after each message's turn.
- Each message MUST receive exactly one 👁 reaction (no duplicates from the race).

**Regression signal:** If the second message is silently dropped, file T-36.17.2-01 regression. Likely causes: UQM::dispatch's lock re-check (after acquiring `workers.lock().await`) is failing to re-evaluate the map state correctly, OR the Arc<Notify> dropped before `dispatch`'s notify_one call.
```

**Edit 7b — D-12 orphan-decision footnote (checker minor nit):**

Immediately after Scenario 4b's drain-mode section (and before the new Scenario 5 from Edit 7), add a short subsection:

```
### Note on D-12 — HTTP 429 (deferred)

CONTEXT.md D-12 specifies that HTTP-arrival platforms (future webhook/REST adapter, Discord/Slack when they flow through `UserQueueManager`) inherit the `Err(QueueError::CapacityReached)` signal at the dispatch boundary and respond with **HTTP 429 + `Retry-After: 5`** at the network edge.

**Status in 36.17.2:** DEFERRED. Phase 36.17.2 only ships the Telegram path; the webhook/REST adapter (Phase 36.7.1+) does not yet flow through `UserQueueManager::dispatch`. The 429 path is wired ARCHITECTURALLY (the `Err(QueueError::CapacityReached)` return shape is in place) but no UAT scenario can exercise it without the adapter.

**Verifier action:** None. This note exists so D-12 is not an orphan decision in the UAT sign-off — it is captured here as a deliberate future-phase deferral.
```

**Edit 8 — NEW Scenario 6 (multimodal photo-burst — M1 sidecar live evidence, T-36.17.2-04):**

Insert immediately after Scenario 5, before the "Out of Scope" cross-process section:

```
## Scenario 6 — Multimodal payload preservation (M1 sidecar live evidence, T-36.17.2-04)

**Goal:** Verify that the multimodal sidecar (`pending_multimodal` introduced in Plan 01) preserves `(text_prefix, image_data_uri)` payloads in FIFO lockstep with `SessionQueue`, so a burst of mixed photo/document/text messages reaches `handle_with_multimodal` with the correct attachments per message.

**Background:** 36.17.1 stored multimodal payload inside `QueuedMessage` on the mpsc channel. 36.17.2 removes the mpsc and stores `MessageEvent` only in `SessionQueue`; multimodal payload moves to a separate `pending_multimodal` map indexed by `SessionKey` with `VecDeque` FIFO semantics. If the sidecar drifts out of lockstep with `SessionQueue` (e.g., a push to the queue with no matching `push_multimodal`, or two takes per pop), photos arrive misaligned to their captions and the agent sees the wrong attachment for each turn. This scenario is the live evidence that the lockstep holds.

**Verifier steps:**

1. Wait until the agent is idle for the test chat (no pending replies, queue depth 0).

2. Send the following 5 messages in **rapid succession** (< 2 seconds between each — fast enough to enqueue messages 2..5 while the agent is still processing message 1):
   - Message 1: text only, content `"first — text only"`.
   - Message 2: a photo attachment (any JPEG/PNG, ≤ 1MB), with caption `"second — photo"`.
   - Message 3: a document attachment (any PDF/txt, ≤ 1MB), with caption `"third — document"`.
   - Message 4: a photo with caption `"fourth — photo with caption B"`.
   - Message 5: text only, content `"fifth — text only"`.

3. Wait for the agent to process all 5 messages (~30-90 seconds depending on response length). Observe the 👁 reactions appear sequentially at pop time (Scenario 1 behavior).

4. Inspect each agent response in Telegram. For each, verify the agent's response references the CORRECT attachment for that turn — message 2's response talks about the photo, message 3's response talks about the document, etc.

**Expected (Phase 36.17.2):**

- 5 messages processed in FIFO order.
- 5 👁 reactions at pop time (one per message, Scenario 1 behavior).
- Each agent response correctly references its message's attachment (or lack thereof for text-only messages).
- Message 1 and Message 5 (text-only) receive responses that do NOT reference any image/document.
- Message 2 and Message 4 (photo) receive responses that reference the image content (e.g., describe what the photo shows).
- Message 3 (document) receives a response that references the document content.

**Regression signal:** If message 3's response talks about the photo (instead of the document), the sidecar has drifted by one position. If message 1 (text-only) receives a response describing a phantom image, the sidecar's pop returned a stale payload from a previous turn. Either case indicates `take_multimodal`/`push_multimodal` lockstep with `SessionQueue::try_push`/`SessionQueue::pop` has broken — file T-36.17.2-04 regression and DO NOT sign off on this scenario.

**Note:** This scenario does NOT require the cap to be hit. It exercises sidecar ordering across the busy-enqueue path, which is the dispatch path the architectural shift made reachable. If Plan 03's `test_multimodal_payload_roundtrips_through_sidecar_to_handler` integration test passes, this live scenario should also pass — Scenario 6 is the human-side confirmation that automation + production behave consistently.
```

**Edit 8 (continued) — Sign-off checklist:**

Append a new line to the existing sign-off checklist:
- `[ ] Scenario 5 — Worker-exit/dispatch race (T-36.17.2-01): second-message-after-worker-exit processed normally`
- `[ ] Scenario 6 — Multimodal payload preservation (T-36.17.2-04): photo/document burst preserves payload→message alignment FIFO`

Update the existing Scenario 1 checklist line to:
- `[ ] Scenario 1 — Busy-enqueue silent path (36.17.2 unified): 👁 reactions appear sequentially at pop time, not at dispatch time`

(Other checklist items unchanged.)

**Edit 9 — Architecture-history footer:**

At the bottom of the file (below the sign-off + notes block), add a short "Document History" section:

```
## Document History

- **2026-05-27 (Phase 36.17.2-04):** Updated for unified architecture (D-01..D-22). Scenario 1's expected behavior rewrote — 👁 reactions now emit at pop time per D-08. Added Scenario 5 covering T-36.17.2-01 worker-exit/dispatch race. Added Scenario 6 covering T-36.17.2-04 multimodal sidecar lockstep (M1 sidecar live evidence). Added D-12 (HTTP 429) deferred-decision footnote. Other scenarios unchanged.
- **2026-05-27 (Phase 36.17.1-05):** Initial runbook for the mpsc-buffer architecture.
```

**Verification:**

- `grep -c "Phase 36.17.2" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 3 (phase reference + scenario headers + document history)
- `grep -c "pop time\\|moment of pop\\|moment of processing" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 2 (D-08 reaction-timing wording)
- `grep -c "T-36.17.2-01" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 2 (Scenario 5 reference + threat-model citation)
- `grep -c "Scenario 5" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 2 (header + checklist line)
- `grep -c "Queue is full (128 messages)" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 1 (D-11 literal preserved)
- `grep -c "is_draining" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 1 (Scenario 4b preserved)
- `git diff --stat crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` shows changes scoped to this file only.
  </action>
  <verify>
    <automated>test -f crates/ironhermes-gateway/tests/session_queue_telegram_uat.md && grep -c "pop time\|moment of pop" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md && grep -c "T-36.17.2-01" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md && grep -c "Scenario 5" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md</automated>
  </verify>
  <acceptance_criteria>
    - `crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` exists.
    - `grep -c "Phase 36.17.2" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 3.
    - `grep -c "pop time\\|moment of pop\\|moment of processing" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 2 (D-08 wording present).
    - `grep -c "T-36.17.2-01" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 2 (new Scenario 5 + threat citation).
    - `grep -c "## Scenario 5" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 1 (Scenario 5 section header exists).
    - `grep -c "Queue is full (128 messages)" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 1 (D-11 cap-hit literal preserved from 36.17.1-05).
    - `grep -c "is_draining" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 1 (Scenario 4b drain-mode preserved).
    - `grep -c "👁" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 5 (👁 emoji referenced throughout, especially in Scenario 1 + 5).
    - `grep -c "## Document History\\|Document History" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 1 (changelog at bottom).
    - The `Regression signal:` sub-section in Scenario 1 instructs the verifier to NOT sign off if 👁 fires on messages 2..N before message 1's turn completes. `grep -c "Regression signal" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 3 (one each in Scenarios 1, 5, 6).
    - `grep -c "## Scenario 6\|Multimodal payload preservation" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 1 (M1 multimodal scenario section header exists).
    - `grep -c "T-36.17.2-04" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 2 (Scenario 6 reference + threat-model citation).
    - `grep -c "D-12\|HTTP 429" crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` >= 1 (orphan-decision footnote present).
  </acceptance_criteria>
  <done>UAT runbook updated to reflect 36.17.2 unified architecture: Scenario 1 expects 👁 at pop time, Scenario 5 covers T-36.17.2-01 race, other scenarios unchanged. Document history records the architectural shift. Ready for human verification (Task 2).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify checkpoint — execute UAT runbook against live Telegram bot</name>
  <what-built>
    Phase 36.17.2 has refactored `UserQueueManager` to back its per-chat queueing with `SessionQueue` instead of a per-chat `mpsc::channel`. The architecturally visible change is that 👁 transport reactions now fire AT POP TIME (when the worker begins processing each message) instead of AT DISPATCH TIME (when the message lands on the buffer). All other behavior — cap-hit UX, `/queue` slash command, `/new` clear-queue, `is_draining` drain-mode preservation — is preserved verbatim from 36.17.1.

    Plans 01-03 shipped:
    - Plan 01: `UserQueueManager` internals rewritten; `dispatch` returns `Result<DispatchOutcome, QueueError>`; cap-hit UX moved into dispatch.
    - Plan 02: Per-chat worker in `runner.rs` rewritten to pop-loop on `SessionQueue` + `Notify`; 👁 emission moved into the worker; post-turn `drain_pending` call removed.
    - Plan 03: New integration test `uqm_session_queue_unification.rs` proves 5 same-chat messages → 5 👁 reactions in FIFO order; 9 existing tests in `session_queue_integration.rs` still pass (D-20 contract).

    Plan 04 has updated the live-Telegram UAT runbook at `crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` to reflect the unified architecture: Scenario 1's expected behavior rewrote for pop-time 👁 emission, new Scenario 5 covers the worker-exit/dispatch race (T-36.17.2-01), and new Scenario 6 covers the multimodal sidecar lockstep (T-36.17.2-04 — M1 live evidence). D-12 (HTTP 429 for webhook adapter) is captured as a deliberate deferred-decision footnote.
  </what-built>
  <how-to-verify>
    Execute the live UAT runbook against your Telegram bot:

    1. **Prerequisites checklist:**
       - `TELEGRAM_BOT_TOKEN` set in your environment matches a bot you can DM.
       - The chat the bot operates in is in the gateway whitelist.
       - The gateway is built from the 36.17.2 branch tip: `cargo build --release -p ironhermes-cli` succeeds.
       - You can `tail -f` the gateway logs in a separate terminal (helpful but not strictly required — UAT is primarily user-visible behavior).

    2. **Boot the gateway:** Start `hermes` (or whatever the CLI entry point is for `run_gateway`). Confirm it logs "Gateway started" or equivalent and that it polls Telegram successfully.

    3. **Execute each scenario in the runbook IN ORDER:** Open `crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` and work through Scenarios 1 → 2 → 3 → 4a → 4b → 5 → 6. Tick each checkbox in the sign-off section as you complete each one.

    4. **CRITICAL — Scenario 1's regression check:** Watch carefully whether 👁 reactions appear on messages 2..N BEFORE message 1's turn completes. They should NOT. If they do, the architecture has regressed (D-08 violation, T-36.17.2-03). Do NOT sign off.

    5. **CRITICAL — Scenario 3's cap-hit UX literal:** The chat reply must contain the EXACT string `"Queue is full (128 messages)"`. Any deviation indicates the cap-hit migration to `UQM::dispatch` (D-11) corrupted the literal.

    6. **CRITICAL — Scenario 5's race test:** After the first message's turn completes AND you've waited 5+ seconds, the second message must still be processed normally. If it sits silently, T-36.17.2-01 has regressed.

    7. **Final sign-off:** Fill in the verifier name, date, and "approved" status in the runbook's sign-off section. If any scenario failed, describe what you saw in the Notes block.

    Expected duration: 15-25 minutes for a thorough run including waiting on agent responses.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 36.17.2 complete, OR describe the failure scenario (which scenario failed, what you saw vs expected) to open a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a documentation + human-checkpoint plan. No new in-process trust boundaries. The UAT exercises real Telegram traffic so the boundaries are:
- Telegram API → gateway (existing; unchanged)
- User typing in Telegram chat → gateway dispatch (existing; unchanged)
- Verifier's observations → sign-off decision (this plan's checkpoint)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.17.1-01 (inherited) | DoS | Cap-hit at 128 | mitigate (verified live) | UAT Scenario 3 floods the queue to 128, sends a 129th. Verifier confirms ❌ reaction + chat reply fire AND no further messages are accepted until the queue drains. Same as 36.17.1-05 Scenario 3. |
| T-36.17.1-02 (inherited) | Tampering / ID disclosure | SessionKey cross-user collision | accept (single-user UAT) | UAT runs with the verifier's single user_id; cross-user isolation is proven by unit tests (Plan 01's `test_session_key_uses_full_triple`). Same as 36.17.1-05. |
| T-36.17.1-03 (inherited) | Lost-update during drain | is_draining + preserve-AND-accept | accept | Scenario 4b verifies the drain-mode log ordering; cross-process preservation explicitly OUT OF SCOPE. Same as 36.17.1-05. |
| **T-36.17.2-01** | Race | Worker-exit/dispatch | mitigate (verified live) | Scenario 5 (new in this plan) is the live verification. The verifier explicitly waits for worker exit then dispatches and confirms processing. |
| **T-36.17.2-02** | Notify lost-wakeup | tokio::sync::Notify | accept | No direct UAT — the lost-wakeup window is microseconds and not human-observable. Tokio runtime guarantee. Documented in CONTEXT.md, mentioned in Scenario 1's architectural justification footnote. |
| **T-36.17.2-03** | Spurious 👁 during cancellation | Worker pops + emits 👁 + cancel fires | accept (documented) | Not directly verified in UAT (window is microseconds). Scenario 1's "Regression signal" sub-section is the regression-detection vector: if 👁 fires on messages 2..N before message 1's turn completes, the architecture has regressed. |

</threat_model>

<verification>
- Task 1 verification: grep gates on the rewritten runbook (see acceptance_criteria).
- Task 2 verification: human verifier executes the runbook and types "approved" OR describes a failure scenario. The orchestrator is responsible for halting on this checkpoint.
- Phase-level verification (post-Task 2 approval): all D-01..D-22 closure points have evidence (Plans 01/02/03 unit + integration tests, Plan 04 live UAT).
- Goal-backward must_haves for this plan are all about the runbook content and the checkpoint gate.
</verification>

<success_criteria>
1. `session_queue_telegram_uat.md` updated with Phase 36.17.2 header references (architectural shift documented in document history).
2. Scenario 1's expected behavior rewritten for pop-time 👁 emission (D-08).
3. New Scenario 5 added covering T-36.17.2-01 worker-exit/dispatch race.
4. Cap-hit UX literals preserved verbatim from 36.17.1 (D-11 — `"Queue is full (128 messages)"`).
5. Scenarios 2, 4a, 4b unchanged in expected behavior (notes added for context).
6. Sign-off checklist includes Scenario 5 line; existing scenario lines refreshed for 36.17.2 wording.
7. Task 2 checkpoint blocks phase sign-off on live Telegram verification (`autonomous: false`).
8. Verifier explicitly tests the regression signals in Scenario 1 (👁-at-pop) and Scenario 5 (post-worker-exit dispatch).
</success_criteria>

<output>
Create `.planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-04-SUMMARY.md` when done.
</output>
