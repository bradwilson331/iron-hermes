---
phase: 36.17.2
plan: 02
type: execute
wave: 2
depends_on:
  - 36.17.2-01
files_modified:
  - crates/ironhermes-gateway/src/runner.rs
autonomous: true
requirements:
  - D-04  # Per-chat worker reads from SessionQueue::pop
  - D-05  # Worker shutdown via Notify wake + cancel_token
  - D-06  # Per-iteration pop -> 👁 -> handle_with_multimodal
  - D-07  # Post-turn drain_pending call REMOVED (worker's natural pop loop drains)
  - D-08  # 👁 reaction emission moves from dispatch to worker
  - D-09  # 👁 emission failure is warn-and-ignore
  - D-15  # Dispatch loop matches on DispatchOutcome
  - D-16  # Worker calls UQM::remove(&session_key) on exit
  - D-19  # Worker holds Arc<Notify> clone obtained from UQM::notify_for

must_haves:
  truths:
    - "Per-chat worker loop reads from session_queue.pop, not chat_rx.recv (D-04)"
    - "👁 reaction is emitted by the worker at pop time, NOT by dispatch (D-08)"
    - "Post-turn drain_pending call at runner.rs:999-1013 is removed (D-07)"
    - "Worker exit is gated on (cancel_token OR queue-empty + no-pending-pushes-via-Notify) (D-05/D-16)"
    - "Dispatch loop matches on DispatchOutcome and spawns worker only on WorkerSpawned (D-15)"
    - "Multimodal payload reconstruction wired (text_prefix + image_data_uri preserved across the pop-loop boundary)"
    - "GatewayRunner::drain_pending function body is retained as pub API (D-07 — backward compat for the integration test)"
  artifacts:
    - path: "crates/ironhermes-gateway/src/runner.rs"
      provides: "Pop-loop worker + DispatchOutcome match in dispatch loop"
      contains: "session_queue.pop"
      contains_2: "notify.notified()"
      contains_3: "add_reaction"
      contains_4: "DispatchOutcome::WorkerSpawned"
      not_contains: "chat_rx.recv"
      not_contains_2: "runner_task.drain_pending"
  key_links:
    - from: "runner.rs dispatch loop"
      to: "UserQueueManager::dispatch"
      via: "Result<DispatchOutcome, QueueError> match"
      pattern: "DispatchOutcome::WorkerSpawned"
    - from: "per-chat worker spawn closure"
      to: "SessionQueue::pop"
      via: "session_queue clone captured into worker"
      pattern: "session_queue\\.pop\\(&session_key_task\\)"
    - from: "per-chat worker (post-pop)"
      to: "PlatformAdapter::add_reaction"
      via: "👁 emission inline before handle_with_multimodal (D-06 step 3)"
      pattern: "add_reaction.*👁"
---

<objective>
Rewrite the per-chat worker loop in `runner.rs` (lines 943-1024) to consume from `SessionQueue::pop` + `Notify::notified()` instead of `mpsc::Receiver::recv`. Move the 👁 transport reaction emission from `UserQueueManager::dispatch` (now removed in Plan 01) into the worker, where it fires immediately before each `handle_with_multimodal` call (D-06 step 3, D-08). Remove the post-turn `runner.drain_pending(...)` call at runner.rs:999-1013 — the worker's natural pop loop now drains anything that arrived during the turn (D-07). Update the dispatch loop at runner.rs:942 to match on `Result<DispatchOutcome, QueueError>` from Plan 01's new `dispatch` signature (D-15).

Purpose: completes the architectural shift. After this plan ships, real Telegram messages flow through `dispatch → SessionQueue::try_push → Notify wake → worker pops → 👁 emitted → handle_with_multimodal called`. The 👁 reaction now correctly reflects "agent is starting this message's turn" instead of "buffer occupancy". The 36.17.1 unreachability bug is closed because the production path now exercises `SessionQueue` end-to-end. `handle_with_multimodal`'s busy-branch enqueue (handler.rs:980-1020) becomes defensive fallback (D-20) — kept for test-driver paths but no longer the production hot path.

Output: rewritten dispatch-loop match-on-`DispatchOutcome` arm at runner.rs:942-1025; rewritten per-chat worker spawn closure that pop-loops on `SessionQueue` + `Notify`; preserved `runner.try_enqueue`/`pop`/`queue_len`/`clear_queue`/`drain_pending` public API (no signature changes); removed `runner.drain_pending(...)` invocation site inside the worker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-CONTEXT.md
@.planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-02-SUMMARY.md
@.planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-01-SUMMARY.md
@./CLAUDE.md

<interfaces>
<!-- Extracted from codebase. Executor uses these directly — no exploration. -->

From crates/ironhermes-gateway/src/user_queue.rs (after Plan 01 lands):
- `pub enum DispatchOutcome { Accepted, WorkerSpawned }`
- `pub async fn UserQueueManager::dispatch(&self, event, text_prefix, image_data_uri) -> Result<DispatchOutcome, QueueError>`
- `pub fn UserQueueManager::notify_for(&self, key: &SessionKey) -> Option<Arc<Notify>>`
- `pub async fn UserQueueManager::remove(&self, key: &SessionKey)`

From crates/ironhermes-gateway/src/runner.rs (current call sites being rewritten):
- Line 942: `let maybe_rx = user_queue_dispatch.dispatch(event, text_prefix, image_data_uri).await;` — return type changes to `Result<DispatchOutcome, QueueError>` per Plan 01.
- Line 943: `if let Some(mut chat_rx) = maybe_rx { ... }` — match-on-Result-of-DispatchOutcome.
- Lines 958-1024: worker spawn closure — `while let Some(queued_msg) = chat_rx.recv().await { ... }` becomes pop-loop.
- Lines 999-1013: post-turn `runner_task.drain_pending(...)` call — REMOVED (D-07).
- Line 1023: `queue_task.remove(&chat_id_task).await;` — argument changes from `&chat_id_task: String` to `&session_key_task: SessionKey` (D-13).

From crates/ironhermes-gateway/src/handler.rs (NOT modified in this plan):
- `pub(crate) async fn run_agent(...)` — unchanged
- `pub async fn handle_with_multimodal(...)` — unchanged; busy-branch at lines 974-1020 remains as defensive fallback (D-20)

From ironhermes-core/src/lib.rs:
- `pub struct MessageEvent { pub platform: Platform, pub message_id: String, pub chat_id: String, pub sender_id: String, ... }`

From crates/ironhermes-gateway/src/adapter.rs:
- `async fn add_reaction(&self, chat_id: &str, message_id: &str, emoji: &str) -> Result<()>` on `PlatformAdapter` trait
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite dispatch loop + per-chat worker to pop-loop on SessionQueue + Notify</name>
  <files>crates/ironhermes-gateway/src/runner.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/runner.rs (lines 685-1030 — dispatch loop + worker spawn site)
    - crates/ironhermes-gateway/src/runner.rs (lines 380-500 — drain_pending impl, retained as pub API)
    - crates/ironhermes-gateway/src/user_queue.rs (full file — after Plan 01 lands; this is the API being called)
    - crates/ironhermes-gateway/src/session_queue.rs (lines 92-220 — SessionQueue::pop is what the worker calls)
    - .planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-CONTEXT.md (D-04..D-09, D-15, D-16)
    - .planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-01-SUMMARY.md (Plan 01 outcome — what DispatchOutcome looks like; what notify_for returns; whether multimodal sidecar landed in UQM)
    - .planning/phases/36.17.1-in-mem-fifo-queuing-parity-of-python-deque-for-chat-sessions/36.17.1-02-SUMMARY.md (existing GatewayRunner::drain_pending shape — staying as pub API; informs whether to keep the runner-clone-into-worker pattern)
  </read_first>
  <action>
Rewrite the dispatch loop's worker-spawn block at `runner.rs:942-1025`. Concrete changes:

**Step 1 — Dispatch loop match (D-15):**

Replace:
```
let maybe_rx = user_queue_dispatch.dispatch(event, text_prefix, image_data_uri).await;
if let Some(mut chat_rx) = maybe_rx {
    ...
}
```

with:
```
let dispatch_result = user_queue_dispatch.dispatch(event.clone(), text_prefix, image_data_uri).await;
match dispatch_result {
    Ok(DispatchOutcome::Accepted) => {
        // Existing worker picked up the message via Notify wake. Nothing to do here.
        // The 👁 reaction will fire when the worker pops the message (D-08).
    }
    Ok(DispatchOutcome::WorkerSpawned) => {
        // Caller responsibility: spawn the per-chat worker for this SessionKey.
        // (worker spawn block below)
    }
    Err(QueueError::CapacityReached { .. }) => {
        // Cap-hit UX (❌ + chat reply) already fired inside UserQueueManager::dispatch (D-11).
        // Dispatch loop's only job here is to log + continue. The dropped event is gone;
        // Telegram offset has already advanced (Pitfall 6) so no re-delivery risk.
        tracing::warn!(chat_id = %event.chat_id, "Dispatch: queue full, message dropped (Phase 36.17.2 D-11)");
    }
}
```

Note: `event.clone()` before dispatch because the cap-hit log line below references `event.chat_id`. Alternative: capture `event.chat_id.clone()` into a local before the dispatch call and use it in the warn — saves a `MessageEvent` clone on the hot path. Implementer's choice.

Add the imports needed at the top of runner.rs:
- `use crate::user_queue::{DispatchOutcome};` (UserQueueManager already imported)
- `use crate::session_queue::{QueueError};` (already imported)

**Step 2 — Worker spawn closure (D-04, D-05, D-06, D-08, D-09, D-16):**

Inside the `Ok(DispatchOutcome::WorkerSpawned)` arm, set up the worker spawn. Variables captured (clones for the `async move`):
- `let handler_task = handler_dispatch.clone();`
- `let adapter_task = adapter_dispatch.clone();`
- `let sem_task = semaphore_dispatch.clone();`
- `let cancel_task = cancel_dispatch.clone();`
- `let queue_task = user_queue_dispatch.clone();` (clone of `Arc<UserQueueManager>`)
- `let session_queue_task = runner_dispatch.session_queue();` (already a public `pub(crate)` method per runner.rs:371) — gives the worker an `Arc<SessionQueue>` to pop from. If `session_queue` accessor visibility is `pub(crate)`, that's fine — the spawn block is inside the same crate.
- `let session_key_task = SessionKey::new(event.platform.clone(), &event.chat_id).with_user(&event.sender_id);` (D-14 — note: this is the SessionKey of the event we just dispatched, which is the SAME key the worker will pop on; matches what UQM stored).
- `let notify_task: Arc<Notify> = queue_task.notify_for(&session_key_task).await.expect("notify_for must return Some immediately after WorkerSpawned (Plan 01 invariant)");` — D-19 + Plan 01 invariant. If `notify_for` is non-async per Plan 01 design (it acquires a sync `tokio::sync::Mutex` so it IS async, but Plan 01 may have made the workers map use `std::sync::Mutex` instead — verify against Plan 01 SUMMARY before writing this line; **adjust based on actual Plan 01 outcome**).

Rewrite the spawn body (replacing lines 958-1024):

```
worker_join_set_dispatch.lock().await.spawn(async move {
    loop {
        // D-05/D-06 step 1+2: pop or wait
        let next_event = match session_queue_task.pop(&session_key_task) {
            Some(ev) => ev,
            None => {
                // Queue empty — wait for a Notify wake OR cancellation
                tokio::select! {
                    _ = cancel_task.cancelled() => break,
                    _ = notify_task.notified() => continue, // re-poll the queue
                }
            }
        };

        // Cancellation check after pop (in case cancel fired between pop and the next step)
        if cancel_task.is_cancelled() { break; }

        // Acquire semaphore permit (TG-06 — bounded concurrency)
        let permit = match sem_task.acquire().await {
            Ok(p) => p,
            Err(_) => break, // semaphore closed
        };

        // D-06 step 3 + D-08: emit 👁 reaction at pop time (inline await — see CONTEXT.md
        // Claude's Discretion last bullet. Inline await means "👁 reaches Telegram before
        // the placeholder █ send"; we accept the small added latency in exchange for
        // strict ordering. D-09: warn-and-ignore on reaction failure.)
        if let Err(e) = adapter_task.add_reaction(&next_event.chat_id, &next_event.message_id, "👁").await {
            tracing::warn!(
                chat_id = %next_event.chat_id,
                message_id = %next_event.message_id,
                error = %e,
                "Worker: 👁 reaction emission failed; continuing (Phase 36.17.2 D-09)"
            );
        }

        // Reconstruct multimodal payload. Plan 01 either stored (text_prefix, image_data_uri)
        // in a UQM sidecar map per SessionKey OR dropped them (TODO in Plan 01).
        // If Plan 01 added the sidecar, call `queue_task.take_multimodal(&session_key_task)`
        // here to retrieve the per-event payload. If Plan 01 dropped it, set both to None
        // and document the regression in this plan's SUMMARY for follow-up.
        // **The executor must determine which path Plan 01 took** by reading
        // .planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-01-SUMMARY.md
        // before writing this code. The recommended path is the sidecar map.
        let (text_prefix, image_data_uri) = queue_task.take_multimodal(&session_key_task).await
            .unwrap_or((None, None));
        let processed = crate::multimodal::ProcessedAttachments { text_prefix, image_data_uri };

        let result = handler_task
            .handle_with_multimodal(
                &next_event,
                adapter_task.clone(),
                cancel_task.child_token(),
                processed,
            )
            .await;

        if let Err(e) = result {
            error!(
                chat_id = %next_event.chat_id,
                error = %e,
                "Handler error for message (Phase 36.17.2 worker pop-loop)"
            );
        }

        // D-07: the post-turn `runner_task.drain_pending(...)` call (current lines 999-1013) is REMOVED.
        // The next iteration of this loop pops the next event from session_queue if any arrived
        // during the turn — that's the natural drain.

        drop(permit);

        // D-05: check cancellation between iterations
        if cancel_task.is_cancelled() { break; }
    }

    // D-16: worker exits — clean up UQM map entry
    queue_task.remove(&session_key_task).await;
});
```

**Step 3 — Remove the `runner_dispatch: Arc<Self>` clone if no longer needed:**

Lines 864-868 capture `runner_dispatch: Arc<Self>` solely to call `runner_task.drain_pending(...)` inside the worker. Since Plan 02 removes that call (D-07), check if `runner_dispatch` has any other use in the dispatch_future. If not, remove it. The `Arc<Self>` plumbing was introduced in Plan 02 of 36.17.1 — see SUMMARY for justification. Removing it returns `start()`'s signature to `&self`? **No** — the public `start(self: Arc<Self>)` signature was widened in 36.17.1-02 and changing it back would break `crates/ironhermes-cli/src/main.rs::run_gateway`. Keep the signature, just stop using the `runner_dispatch` capture inside the dispatch_future. Mark it with `let _runner_dispatch = self.clone();` if needed for forward-compat documentation, or remove entirely. The `Arc<Self>` is still useful for accessing `session_queue()`, but the worker's `session_queue_task = runner_dispatch.session_queue()` line above already covers that — so the runner_dispatch clone IS still needed for that access. Keep it for the session_queue accessor.

Actually re-read: `session_queue_task = runner_dispatch.session_queue();` — this is the runner's `pub(crate) fn session_queue(&self) -> Arc<SessionQueue>`. So `runner_dispatch` is still needed. Net: rename the local intent comment from "for drain_pending" to "for session_queue accessor + future runner ops". The drain_pending invocation lines 999-1013 are deleted; the `runner_task = runner_dispatch.clone()` capture at line 953 is DELETED (no longer needed inside the spawn — the worker captures `session_queue_task` directly from `runner_dispatch.session_queue()` BEFORE the spawn, not inside it).

**Step 4 — Verify `GatewayRunner::drain_pending` body is untouched (D-07):**

`runner.rs:381-435` defines `pub async fn drain_pending(&self, key, handler, adapter, cancel) -> Result<()>`. This function is retained per D-07 (the integration test `test_drain_after_turn` from 36.17.1-02 still calls it). The plan does NOT touch its body or visibility. Only the in-worker invocation at lines 999-1013 is removed.

**Step 5 — Verify dispatch loop still handles multimodal processing upstream:**

Lines 926-939 do multimodal processing BEFORE `user_queue_dispatch.dispatch(...)`. The processed `(text_prefix, image_data_uri)` is passed into dispatch. Plan 01's UQM either stores them in a sidecar map or drops them. The worker code above retrieves them via `queue_task.take_multimodal(&session_key_task)`. **If Plan 01 did NOT add the sidecar map**, the worker's call to `take_multimodal` will fail to compile. In that case, the executor's options are:
- (a) Add the sidecar map in Plan 02 (out-of-scope per file ownership — Plan 01 owns user_queue.rs) — **DO NOT do this**.
- (b) Document the regression and ship multimodal-as-None in Plan 02; flag it as a deferred issue and update Plan 03's integration test to exclude multimodal payload assertions until follow-up.

**The executor's decision tree:** read Plan 01 SUMMARY first. If sidecar exists → use it. If not → wrap in `if let Some(...)` fallback to None and add a `tracing::warn!` at worker startup documenting the regression, then leave a deferred-issues entry. Either way, ship Plan 02 — multimodal is a non-blocking gap for the unification milestone.

**Verification commands:**
- `cargo build -p ironhermes-gateway` — clean
- `cargo build -p ironhermes-cli` — clean (call-site at run_gateway compiles)
- `cargo build --workspace` — clean
- `cargo test -p ironhermes-gateway --lib` — all gateway lib tests pass
- `cargo test -p ironhermes-gateway --test session_queue_integration` — 9/9 still pass (D-20 — handler.rs busy-branch is unchanged, so test_busy_agent_enqueues_event still passes; test_drain_after_turn still passes because drain_pending is still callable)
- `cargo clippy -p ironhermes-gateway -- -D warnings` — no NEW warnings beyond pre-existing dead-code

**Manual smoke verification (optional, executor may skip if cargo tests pass):**
- Read the diff for runner.rs lines 940-1025 and confirm:
  1. No `chat_rx.recv()` token remains in the file (`grep -c "chat_rx.recv" runner.rs` == 0).
  2. No `runner_task.drain_pending` token remains (`grep -c "runner_task.drain_pending" runner.rs` == 0).
  3. `session_queue.pop(&session_key_task)` appears at least once.
  4. `add_reaction(...&next_event.chat_id, &next_event.message_id, "👁")` appears at least once.
  5. `queue_task.remove(&session_key_task)` appears at least once (D-16).
  6. `DispatchOutcome::WorkerSpawned` appears at least once in the match arm.
  7. `DispatchOutcome::Accepted` appears at least once in the match arm.
  8. `notify_task.notified()` appears at least once (D-05).
  </action>
  <verify>
    <automated>cargo build -p ironhermes-gateway 2>&1 | tail -5 && cargo test -p ironhermes-gateway --test session_queue_integration --no-fail-fast 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "chat_rx.recv" crates/ironhermes-gateway/src/runner.rs` equals 0 (mpsc::Receiver consumption gone).
    - `grep -c "runner_task.drain_pending" crates/ironhermes-gateway/src/runner.rs` equals 0 (post-turn drain call gone — D-07).
    - `grep -v '^//' crates/ironhermes-gateway/src/runner.rs | grep -v '^\\s*//' | grep -c "session_queue_task.pop\\|session_queue.pop" >= 1` (worker pops from SessionQueue).
    - `grep -c "DispatchOutcome::WorkerSpawned" crates/ironhermes-gateway/src/runner.rs` >= 1 (dispatch loop matches the variant).
    - `grep -c "DispatchOutcome::Accepted" crates/ironhermes-gateway/src/runner.rs` >= 1 (dispatch loop matches the variant).
    - `grep -c "QueueError::CapacityReached" crates/ironhermes-gateway/src/runner.rs` >= 1 (dispatch loop matches the error arm).
    - `grep -c '"👁"' crates/ironhermes-gateway/src/runner.rs` >= 1 (👁 emission in worker per D-08). If the source uses unicode escape `\\u{1f440}`, that token must appear instead.
    - `grep -c "notify_task.notified\\|notify\\.notified" crates/ironhermes-gateway/src/runner.rs` >= 1 (D-05 Notify wake).
    - `grep -c "queue_task.remove\\|queue\\.remove" crates/ironhermes-gateway/src/runner.rs` >= 1 (D-16 worker exit cleanup).
    - `grep -n "pub async fn drain_pending" crates/ironhermes-gateway/src/runner.rs` returns at least one line (function body retained per D-07 / D-20).
    - `cargo build -p ironhermes-gateway` exits 0.
    - `cargo build --workspace` exits 0 (CLI call-site to run_gateway still compiles — Plan 01 already updated `UserQueueManager::new` constructor signature).
    - `cargo test -p ironhermes-gateway --test session_queue_integration` reports 9/9 passing (D-20 contract — busy-branch + drain_pending tests untouched).
    - `cargo test -p ironhermes-gateway --lib` passes; runner.rs internal tests including `runner::tests::drain_mode_tests` (7 tests from 36.17.1-04) all green.
  </acceptance_criteria>
  <done>Per-chat worker now pop-loops on SessionQueue + Notify; 👁 reaction fires at pop time; post-turn drain_pending call is removed (worker's natural pop loop drains); dispatch loop matches Result<DispatchOutcome, QueueError>; workspace builds and existing tests pass. Plan 03 will add an integration test that fails if 👁 emission moves back to dispatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `runner.rs` dispatch loop → `UserQueueManager::dispatch` | In-process; the boundary moved to Plan 01. This plan only changes the consumer side of that boundary. |
| Per-chat worker → `SessionQueue::pop` | In-process; FIFO order is enforced by VecDeque::pop_front (verified by Plan 01 of 36.17.1's proptest). |
| Per-chat worker → `PlatformAdapter::add_reaction` ("👁") | Outbound to Telegram API; failure is warn-and-ignore (D-09). |
| Per-chat worker → `handler.handle_with_multimodal` | In-process; handler's `agent_running` AtomicBool is the existing concurrency gate. RAII guard pattern preserved. |
| Per-chat worker → `Notify::notified()` | In-process synchronization; lost-wakeup risk addressed by tokio::sync::Notify's permit semantics (T-36.17.2-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.17.1-01 | Denial of Service | `SessionQueue::try_push` cap-check | mitigate (inherited) | Plan 01 already migrated cap-hit UX into `UserQueueManager::dispatch`. The Plan 02 worker is only on the consume side — it cannot trigger or bypass the cap. No new mitigation needed. |
| T-36.17.1-02 | Tampering / Information Disclosure | SessionKey full-triple keying | mitigate (inherited) | Plan 01 keys UQM map by SessionKey. The worker's `session_key_task = SessionKey::new(...).with_user(...)` matches exactly what UQM stored under (D-14). Cross-user collision impossible by construction. |
| T-36.17.1-03 | Lost-update during drain | `is_draining` preserve-AND-accept | accept (no change) | 36.17.1-04's `is_draining` flag is consulted nowhere in this plan. Worker continues to pop until queue empty + cancel fires, which is the same in drain mode as in normal operation (D-17). |
| **T-36.17.2-01** | Race | `dispatch` arriving mid-`remove` | mitigate (Plan 01 handles primary; Plan 02 ordering invariant) | Plan 02's worker calls `queue_task.remove(&session_key_task).await` ONLY after exiting the pop loop. By that point, the worker's local Arc<Notify> clone is the only living reference besides UQM's map entry. UQM serializes dispatch+remove via its mutex. If a dispatch lands between the worker's last pop-returns-None and the remove call, Plan 01's dispatch will see `Some(workers.get(&session_key))` because the map entry hasn't been removed yet, return `Accepted`, and call `notify_one()` on the still-living Arc<Notify>. The worker IS dead by that point. **Mitigation gap**: the dispatched event sits on the queue with no consumer until a fresh dispatch arrives for the same SessionKey, which spawns a new worker that picks up both the orphan and the new event. The orphan event's latency is bounded by the next dispatch. Acceptable for Phase 36.17.2; if tighter latency is needed, future work: add a "check queue depth after notify_one in Plan 01 dispatch path; if depth > 0 and workers map lacks entry, spawn worker from dispatch loop" — out of scope here. **Document this in SUMMARY.md.** |
| **T-36.17.2-02** | Notify lost-wakeup | `notify_one` racing `notified().await` | accept | tokio::sync::Notify retains a permit when no one is waiting; the next `notified()` call receives it. The Plan 02 worker structure (`pop → if None, notified().await`) is the standard tokio pattern. No mitigation needed; behavior is enforced by tokio runtime guarantees. |
| **T-36.17.2-03** | Spurious 👁 during cancellation | Worker pops + emits 👁 + cancel fires before handle_with_multimodal | accept | User sees a stale 👁 on a message that never gets processed. Window is microseconds; the user-visible artifact is identical to the existing "agent crashed mid-turn" case. The Plan 02 cancellation check between iterations limits the window. Documented in CONTEXT.md. No code change. |
| T-36.17.2-SC | Tampering (supply chain) | No new `cargo` deps in this plan | n/a | Plan 02 reuses existing tokio + tracing + adapter trait. No `Cargo.toml` changes. |

</threat_model>

<verification>
- `cargo build --workspace` — clean.
- `cargo test -p ironhermes-gateway --lib` — all gateway lib tests pass (Plan 01's user_queue tests + 36.17.1-04's drain_mode tests + any other runner tests).
- `cargo test -p ironhermes-gateway --test session_queue_integration` — 9/9 still pass; this is the D-20 contract.
- Grep gates listed in acceptance_criteria above.
- Goal-backward must_haves: each truth is grep-checkable on runner.rs OR enforced by a passing test.
</verification>

<success_criteria>
1. Per-chat worker reads from `session_queue.pop(&session_key)` instead of `chat_rx.recv().await` (D-04).
2. 👁 reaction emission is INSIDE the worker, IMMEDIATELY before `handle_with_multimodal` (D-06 step 3, D-08). 👁 emission is NOT inside `UserQueueManager::dispatch` (verified by Plan 01's acceptance test).
3. Post-turn `runner_task.drain_pending(...)` call at runner.rs:999-1013 is removed (D-07).
4. Worker exit is gated on `cancel_token.is_cancelled()` OR `session_queue.pop` returning `None` + `notify.notified()` waiting + cancel firing on the select! arm (D-05).
5. Dispatch loop at runner.rs:942 matches on `Result<DispatchOutcome, QueueError>` and only spawns a worker on `Ok(DispatchOutcome::WorkerSpawned)` (D-15).
6. On worker exit, `queue_task.remove(&session_key_task).await` is called with the full SessionKey, not chat_id (D-13, D-16).
7. `GatewayRunner::drain_pending` body and visibility are unchanged (D-07 retains it as pub API for the integration test).
8. `handle_with_multimodal` and its busy-branch in handler.rs are untouched (D-20 — defensive fallback preserved).
9. Workspace builds; all 9 existing integration tests and all lib tests pass.
</success_criteria>

<output>
Create `.planning/phases/36.17.2-unify-session-queue-replace-uqm-mpsc-buffer/36.17.2-02-SUMMARY.md` when done.
</output>
