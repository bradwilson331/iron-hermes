---
phase: 36.17.2
plan: "04"
subsystem: ironhermes-gateway
tags: [uat, runbook, session-queue, user-queue-manager, reaction-timing, pop-time, multimodal, sidecar, race-condition]
dependency_graph:
  requires:
    - 36.17.2-01  # UserQueueManager with SessionQueue + Notify map + multimodal sidecar
    - 36.17.2-02  # Notify-based per-chat worker pop-loop, 👁 at pop time (D-08)
    - 36.17.2-03  # Integration tests: 5-message FIFO + sequential 👁 emission
    - 36.17.1-05  # Original UAT runbook (this plan supersedes it)
  provides:
    - Updated live-Telegram UAT runbook for the unified architecture
    - Scenario 1 expected behavior rewritten for pop-time 👁 emission (D-08)
    - Scenario 5: worker-exit/dispatch race (T-36.17.2-01 live verification)
    - Scenario 6: multimodal sidecar lockstep (T-36.17.2-04 / M1 live evidence)
    - D-12 (HTTP 429) orphan-decision footnote
    - Document History recording 36.17.1-05 → 36.17.2-04 architectural shift
  affects:
    - crates/ironhermes-gateway/tests/session_queue_telegram_uat.md
tech_stack:
  added: []
  patterns:
    - UAT runbook with per-scenario regression signals
    - Scenario-to-threat-register cross-references (T-36.17.2-01, T-36.17.2-04)
    - Orphan-decision footnote pattern for deferred architectural items (D-12)
key_files:
  created: []
  modified:
    - crates/ironhermes-gateway/tests/session_queue_telegram_uat.md
decisions:
  - "Scenario 1 expected behavior fully rewritten — not just annotated — because the 36.17.1 wording was architecturally incorrect under 36.17.2 (👁 at dispatch vs pop time is a user-visible semantic difference, not a minor clarification)"
  - "D-12 (HTTP 429) footnote placed between Scenario 4b and Scenario 5 so it appears near the cap-hit context without interrupting scenario flow"
  - "Scenario 5 step 3 specifies a 5-second wait after agent reply to guarantee worker-exit before the next dispatch — this makes the race condition reliably exercisable without gateway log inspection"
  - "Scenario 6 uses 5 messages with alternating text/photo/document/photo/text ordering to maximally stress sidecar FIFO alignment"
metrics:
  duration_minutes: 3
  completed_date: "2026-05-28"
  tasks_completed: 1
  files_modified: 1
---

# Phase 36.17.2 Plan 04: UAT Runbook Update — Unified Architecture Summary

UAT runbook updated from 36.17.1-05 mpsc-buffer architecture to 36.17.2 unified architecture: Scenario 1 rewritten for sequential pop-time 👁 emission (D-08), new Scenario 5 covers T-36.17.2-01 worker-exit/dispatch race, new Scenario 6 covers T-36.17.2-04 multimodal sidecar lockstep, D-12 HTTP 429 captured as deliberate deferral footnote.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Update session_queue_telegram_uat.md for unified architecture + add T-36.17.2-01 scenario | `f0bae4b9` | session_queue_telegram_uat.md |

## Task 2 — Checkpoint (Pending)

Task 2 is a `checkpoint:human-verify` gate. The runbook has been updated; human verification against a live Telegram bot is required before phase sign-off.

## What Was Built

**Header update:**
- Title retained as "Session Queue — Telegram Live UAT"
- Phase callout changed from `Phase 36.17.1` to `Phase 36.17.2 (supersedes 36.17.1-05)`
- Added Architecture (locked) bullet documenting UQM mpsc removal and pop-time 👁 semantics
- Added D-04, D-08, D-11, D-22 to locked-decision callouts (in addition to inherited 36.17.1 callouts)
- Extended "Why this runbook exists" list with item 5 (sequential 👁 D-08 regression detection)
- Log path references updated from `uat-36.17.1-gateway.log` to `uat-36.17.2-gateway.log`
- Prerequisites updated to reference Phase 36.17.2 plans 01–04

**Scenario 1 (Busy-enqueue) — critical rewrite (D-08):**
- Step block rewritten: 5 messages in rapid succession (vs. 2 in 36.17.1-05) to better exercise FIFO
- Expected section fully replaced: describes sequential pop-time 👁 emission (messages 2..5 remain silent until each turn begins)
- Added `Regression signal` sub-section: instructs verifier to NOT sign off if 👁 fires on messages 2..5 before message 1's turn completes (D-08 violation / T-36.17.2-03)
- Added `Architectural justification` paragraph explaining dispatch-time vs. pop-time semantic difference

**Scenario 2 (/queue command) — unchanged behavior, note added:**
- Added one-line Note (36.17.2): /queue bypasses UQM::dispatch; unaffected by dispatch-path refactor

**Scenario 3 (Cap-hit UX) — unchanged behavior, note added:**
- Added Note (36.17.2 internal migration): cap-hit UX literal now fires from UQM::dispatch (D-11); user-visible behavior identical; regression signal for cap-hit on wrong messages
- Log path reference updated

**Scenario 4a (/new clears queue) — unchanged behavior, note added:**
- Added Note (36.17.2): /new behavior unchanged; clear-queue-before-remove ordering preserved

**Scenario 4b (is_draining) — unchanged:**
- No edits to scenario content; log path references updated

**D-12 orphan-decision footnote (new, between 4b and Scenario 5):**
- Documents HTTP 429 deferral status: DEFERRED pending webhook/REST adapter (Phase 36.7.1+)
- States architectural wiring is in place (QueueError::CapacityReached return shape) but no UAT scenario can exercise it yet
- Verifier action: none required

**Scenario 5 (NEW — T-36.17.2-01 worker-exit/dispatch race):**
- Goal: verify post-exit dispatch spawns a fresh worker (mutex serialization in UQM between dispatch and remove)
- 5-step verifier flow: send one message → wait for full turn completion → wait 5s → send second message → confirm processing
- Expected: second message processed, two worker-exit log lines, no duplicated 👁 reactions
- Regression signal: silently dropped second message → file T-36.17.2-01 regression

**Scenario 6 (NEW — T-36.17.2-04 multimodal sidecar lockstep / M1 live evidence):**
- Goal: verify pending_multimodal sidecar preserves (text_prefix, image_data_uri) in FIFO lockstep with SessionQueue
- Background explains 36.17.1 vs 36.17.2 multimodal storage architecture
- 4-step verifier flow: idle state → 5-message burst (text/photo/doc/photo/text) → wait all processed → inspect each response for correct attachment reference
- Expected: FIFO order, 5 sequential 👁 at pop time, each response references correct attachment
- Regression signal: message 3 response talks about photo (sidecar drift by 1 position) or text-only message describes phantom image (stale payload from prior turn) → file T-36.17.2-04 regression

**Sign-off checklist updates:**
- Scenario 1 line refreshed: "36.17.2 unified: 👁 reactions appear sequentially at pop time, not at dispatch time"
- Scenario 5 line added: "Worker-exit/dispatch race (T-36.17.2-01): second-message-after-worker-exit processed normally"
- Scenario 6 line added: "Multimodal payload preservation (T-36.17.2-04): photo/document burst preserves payload→message alignment FIFO"

**Document History footer (new):**
- Records 36.17.2-04 update (2026-05-27): architectural changes, new scenarios, D-12 footnote
- Records 36.17.1-05 original (2026-05-27): initial mpsc-buffer runbook

## Deviations from Plan

None — plan executed exactly as written. All edits specified in the plan's nine Edit blocks were applied. The Scenario 2 note was placed at the top of the scenario (before the "What this verifies" line) as specified. The D-12 footnote was placed as its own `### Note on D-12` subsection between Scenario 4b and Scenario 5 as specified.

## Verification Results

All 13 acceptance criteria passed:

| Check | Threshold | Actual |
|-------|-----------|--------|
| File exists | present | PASS |
| `grep -c "Phase 36.17.2"` | >= 3 | 9 |
| `grep -cE "pop time\|moment of pop\|moment of processing"` | >= 2 | 10 |
| `grep -c "T-36.17.2-01"` | >= 2 | 6 |
| `grep -c "## Scenario 5"` | >= 1 | 1 |
| `grep -c "Queue is full (128 messages)"` | >= 1 | 4 |
| `grep -c "is_draining"` | >= 1 | 11 |
| `grep -c "👁"` | >= 5 | 22 |
| `grep -c "Document History"` | >= 1 | 1 |
| `grep -c "Regression signal"` | >= 3 | 3 |
| `grep -cE "## Scenario 6\|Multimodal payload preservation"` | >= 1 | 2 |
| `grep -c "T-36.17.2-04"` | >= 2 | 4 |
| `grep -cE "D-12\|HTTP 429"` | >= 1 | 4 |

## Known Stubs

None — this is a documentation plan. The runbook is complete and ready for human execution against a live Telegram bot.

## Threat Flags

None — this plan modifies documentation only. No new network endpoints, auth paths, file access patterns, or schema changes introduced.

## Self-Check: PASSED

- [x] `crates/ironhermes-gateway/tests/session_queue_telegram_uat.md` exists and contains all required content
- [x] Commit `f0bae4b9` exists in git log
- [x] All 13 acceptance criteria pass (verified via grep)
- [x] No modifications to STATE.md or ROADMAP.md
