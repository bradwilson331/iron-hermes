---
phase: 22.4.2.2
slug: cron-create-defaults-to-tg-origin-when-gateway-active
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-26
---

# Phase 22.4.2.2 — Validation Strategy

> Source: `22.4.2.2-RESEARCH.md` § Validation Architecture (researcher confirmed seams + IRONHERMES_HOME env-var test pattern).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (Rust 2024 / tokio multi-thread where needed) |
| **Config file** | per-crate `Cargo.toml` `[dev-dependencies]` (no new dev-deps required — `tempfile` already in workspace) |
| **Quick run command** | `cargo test -p ironhermes-cli --test cron_default_deliver` (focused, ~3s) |
| **Full suite command** | `cargo test --workspace` |
| **Per-task focused commands** | (1) `cargo test -p ironhermes-core config::tests::telegram_default_origin` for unit tests; (2) `cargo test -p ironhermes-cli --test cron_default_deliver` for integration; (3) `cargo test -p ironhermes-cli --test invariants_22_4 -- invariant_22_4_2_2_01` for INV |
| **INV ledger command** | `cargo test -p ironhermes-cli --test invariants_22_4` (must pass with **63** invariants total) |
| **Estimated runtime** | ~3 s focused; ~90 s full suite |

---

## Sampling Rate

- **After every task commit:** Run the per-plan focused command for the plan being executed.
- **After plan complete:** `cargo test --workspace`.
- **Before `/gsd-verify-work`:** Full suite must be green AND `invariants_22_4` must pass with all 63 INVs.
- **Max feedback latency:** ~3 seconds (focused), ~90 seconds (full suite).

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Decision | Test Type | Automated Command | File Exists | Status |
|---------|------|------|----------|-----------|-------------------|-------------|--------|
| 01-01 | 01 | 0 | D-07 helper API | unit | `cargo test -p ironhermes-core config::tests::telegram_default_origin_disabled_returns_none` | ❌ W0 | ⬜ pending |
| 01-02 | 01 | 0 | D-07 helper API | unit | `cargo test -p ironhermes-core config::tests::telegram_default_origin_single_returns_single` | ❌ W0 | ⬜ pending |
| 01-03 | 01 | 0 | D-07 helper API | unit | `cargo test -p ironhermes-core config::tests::telegram_default_origin_multi_returns_multi` | ❌ W0 | ⬜ pending |
| 01-04 | 01 | 0 | D-08 fixture | integration | `cargo build -p ironhermes-cli --tests` (Wave 0 stub compiles) | ❌ W0 | ⬜ pending |
| 02-01 | 01 | 1 | D-01 single-chat | integration | `cargo test -p ironhermes-cli --test cron_default_deliver tg_enabled_single_chat_routes_to_origin` | ❌ W0 | ⬜ pending |
| 02-02 | 01 | 1 | D-03 multi-chat | integration | `cargo test -p ironhermes-cli --test cron_default_deliver tg_enabled_multi_chat_falls_back_to_local` | ❌ W0 | ⬜ pending |
| 02-03 | 01 | 1 | D-05 disabled | integration | `cargo test -p ironhermes-cli --test cron_default_deliver tg_disabled_falls_back_to_local` | ❌ W0 | ⬜ pending |
| 02-04 | 01 | 1 | D-05 missing | integration | `cargo test -p ironhermes-cli --test cron_default_deliver tg_section_missing_falls_back_to_local` | ❌ W0 | ⬜ pending |
| 02-05 | 01 | 1 | D-04 explicit | integration | `cargo test -p ironhermes-cli --test cron_default_deliver explicit_deliver_flag_skips_helper` | ❌ W0 | ⬜ pending |
| 03-01 | 01 | 1 | D-09 INV | static-grep | `cargo test -p ironhermes-cli --test invariants_22_4 -- invariant_22_4_2_2_01` | ❌ W0 | ⬜ pending |
| 03-02 | 01 | 1 | workspace integrity | compile | `cargo build --workspace` | ✓ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/ironhermes-core/src/config.rs` — append unit tests for `telegram_default_origin()` in `mod tests` at ~line 634 (3 tests: None/Single/Multi)
- [ ] `crates/ironhermes-core/src/config.rs` — define `OriginDecision` enum AND `pub fn telegram_default_origin(&self) -> OriginDecision` (or move enum to `ironhermes-cron` if circular dep — researcher Q-OQ1)
- [ ] `crates/ironhermes-cron/src/job.rs` (or wherever `JobOrigin` lives) — confirm exported via `pub use job::*` in lib.rs (already true per RESEARCH §Q1)
- [ ] `crates/ironhermes-cli/tests/cron_default_deliver.rs` — new integration test file with 5 behavioral tests + `make_test_config(tg_enabled, whitelist)` fixture using `IRONHERMES_HOME` env override + process-global `Mutex` (pattern from `chat_memory_persistence.rs`)
- [ ] `crates/ironhermes-cli/src/cron.rs` — extract `pub(crate) fn resolve_cron_deliver(deliver_flag: Option<String>, config: &Config) -> (String, Option<JobOrigin>)` for direct test invocation
- [ ] `crates/ironhermes-cli/tests/invariants_22_4.rs` — append `invariant_22_4_2_2_01_cmd_create_consults_origin_helper` (function 63)

*Existing infrastructure (cargo test, tempfile, tokio test runtime) covers everything else — no new framework install needed.*

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| Newly-enabled TG config auto-routes future cron create only | D-05 / D-12 | Touches user's home dir + config edit | (1) Edit `~/.ironhermes/config.yaml` to set `gateway.platforms.telegram.enabled: true` and `whitelist: [<your_chat_id>]`. (2) Run `hermes cron create --name 'doc-test' --schedule '*/5 * * * *' --prompt 'echo hi'` (no `--deliver`). (3) Inspect `~/.ironhermes/cron/jobs.json` — created job MUST have `"deliver": "origin"`, `"origin": {"platform": "telegram", "chat_id": "<your_chat_id>", ...}`. (4) Confirm pre-existing jobs in the same file are UNCHANGED (their `deliver` is still whatever it was before). |
| Multi-chat fallback hint visible | D-03 | OS stderr in real terminal | (1) Add a second entry to whitelist. (2) Run the same `cron create`. (3) Stderr MUST contain `"multiple authorized chats"` and the hint listing whitelist values. (4) Created job has `"deliver": "local"`, `"origin": null`. |
| Cron job with auto-routed origin actually delivers via TG at next tick | D-01 (cross-phase, Phase 22.4.2.1 Plan 02) | Requires real TG bot token + tick wait | (1) After step 1 above, restart `hermes gateway`. (2) Wait for the 5-min tick. (3) Verify the message arrives in your Telegram chat. |

---

## Validation Sign-Off

- [ ] All tasks have automated `cargo test` verify or Wave 0 dependency (per table above)
- [ ] Sampling continuity: every task has a `cargo test` command (✓)
- [ ] Wave 0 covers all MISSING references (1 new test file + helper extraction + INV append + 3 unit tests in config.rs)
- [ ] No watch-mode flags
- [ ] Feedback latency < 90 s full suite; < 3 s focused
- [ ] `nyquist_compliant: true` set in frontmatter (after planner approval)

**Approval:** pending
