---
context: phase
phase: 22.4.2.2-cron-create-defaults-to-tg-origin-when-gateway-active
task: null
total_tasks: null
status: paused
stage: plans-written-not-verified-not-committed
last_updated: 2026-04-26T13:46:12.426Z
---

# Resume Phase 22.4.2.2 — Cron create defaults to TG origin when gateway active

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Wrong-level phase insert | First `gsd-sdk query phase.insert "22.4.2.1" ...` produced `22.4.2.1.1` (a child of the just-completed phase) instead of `22.4.2.2` (a sibling). Root cause: passing the most-recent phase number rather than its parent. | advisory | When inserting urgent work as a sibling of an existing decimal phase (e.g., 22.4.2.X+1), pass the PARENT phase number to `phase.insert` (here: `22.4.2`). The SDK then detects existing decimals and increments. Don't just bump the prior phase number. |
| Decision-coverage gate looks for `D-NN:` (with colon) | Phase 22.4.2.1 plan-checker initially reported 12/14 uncovered because the `decisions: [D-01, D-02, ...]` frontmatter array doesn't satisfy the gate; it parses inline `D-NN:` citations. Plan 22.4.2.2 plans were written with `decisions_addressed:` blocks for this reason. | advisory | Both 22.4.2.2 plans MUST keep their `decisions_addressed:` block in frontmatter with `"D-NN: <text>"` strings. Re-running `gsd-sdk query check.decision-coverage-plan` post-write should report 12/12 (or more) covered. |
| OriginDecision in ironhermes-core cannot embed JobOrigin | RESEARCH §Q-OQ1 confirmed ironhermes-core has zero dep on ironhermes-cron — embedding `JobOrigin` would create a circular dependency. The enum carries plain `String` fields instead. | blocking | Both plans construct `JobOrigin { platform: <decision.platform>, chat_id: <decision.chat_id>, chat_name: None, thread_id: None }` from the enum's String fields at the consumer site (cron.rs::cmd_create AND cronjob_tool.rs::handle_create). Verify the planner's enum definition in Plan 01 is `OriginDecision::Single { platform: String, chat_id: String }` (or equivalent), NOT `OriginDecision::Single(JobOrigin)`. |

<current_state>
Mid-flight in `/gsd-plan-phase 22.4.2.2 --chain`. Both PLAN.md files were just written by gsd-planner. About to spawn gsd-plan-checker when user invoked `/gsd-pause-work`. Two plans (01 wave 1 / 02 wave 2 depends_on 01) exist on disk but are NOT committed. ROADMAP.md was updated by the planner (Phase 22.4.2.2 goal + plans listing) but is also NOT committed.

Phase 22.4.2.1 (predecessor) FULLY SHIPPED this session — 10 atomic commits `f796674..e7d5450`, 62 INVs green, all three regressions resolved (cron slash UI, TG delivery dispatch, Ctrl+C graceful shutdown). User confirmed Ctrl+C works live. User then spotted that pre-existing weather cron jobs aren't reaching TG — root cause: their `deliver` field is `"local"` (the v2.0 CLI default). That's the regression Phase 22.4.2.2 fixes.

The "dispatch_gateway in scope" user constraint expanded the phase from 1 plan to 2 plans:
- Plan 01: CLI default-routing (`hermes cron create` consults `Config::telegram_default_origin()`)
- Plan 02: LLM tool default-routing (`cronjob_tool::handle_create` consults the same helper — pure addition, no surgery to `CronjobTool::new` or `register_cronjob_tool`)
</current_state>

<completed_work>

Completed Tasks:
- Phase 22.4.2.1 — DONE this session (10 commits, 62 INVs green, goal-backward verified)
- 22.4.2.2 phase insert (`4bcdca9` after correcting wrong-level insert) — Done
- 22.4.2.2 CONTEXT.md (extended to 2 plans after user constraint) — Done, committed
- 22.4.2.2 RESEARCH.md (HIGH confidence; 7 questions resolved + 2 open Q-OQ1/Q-OQ2) — Done, committed
- 22.4.2.2 VALIDATION.md (per-task automated commands for both plans) — Done, committed
- 22.4.2.2 PATTERNS.md (analog files for cmd_create + cronjob_tool seams) — Done, committed
- 22.4.2.2-01-PLAN.md written (CLI path) — Done, **UNCOMMITTED**
- 22.4.2.2-02-PLAN.md written (LLM tool / dispatch_gateway path) — Done, **UNCOMMITTED**
- ROADMAP.md updated by planner — **UNCOMMITTED**
</completed_work>

<remaining_work>

1. **Verify plans** — Spawn `gsd-plan-checker` (model: sonnet) with both PLAN.md paths. The verification context was drafted in the conversation but not sent — see `<next_action>` for the prompt skeleton.
2. **Address any BLOCKER findings** from the checker (revision loop, max 3 iterations).
3. **Decision-coverage gate** — `gsd-sdk query check.decision-coverage-plan ".planning/phases/22.4.2.2-cron-create-defaults-to-tg-origin-when-gateway-active" "<context_path>"` should pass 12/12 (or 14/14 if D-13/D-14 are added). The plans already have `decisions_addressed:` blocks designed to satisfy the gate.
4. **Record planning** — `gsd-sdk query state.planned-phase --phase 22.4.2.2 --name cron-create-defaults-to-tg-origin-when-gateway-active --plans 2`
5. **Annotate roadmap** — `gsd-sdk query roadmap.annotate-dependencies "22.4.2.2"` (this hit a TypeError bug for Phase 22.4.2.1 — non-blocking; skip if it errors).
6. **Commit plans** — `gsd-sdk query commit "docs(22.4.2.2): create phase plan" --files 22.4.2.2-01-PLAN.md 22.4.2.2-02-PLAN.md .planning/STATE.md .planning/ROADMAP.md`
7. **Execute Plan 01** — Sequential, no worktree (file overlap on `invariants_22_4.rs`). Spawn gsd-executor (sonnet).
8. **Execute Plan 02** — Sequential after Plan 01 (depends_on). Spawn gsd-executor (sonnet).
9. **Verify phase** — Spawn gsd-verifier (sonnet) for goal-backward verification.
</remaining_work>

<decisions_made>

- **Split into 2 plans** instead of folding both entry points into one — enables independent review and lets Plan 01 ship if Plan 02 reveals an edge case.
- **`OriginDecision` carries `String` chat_id** (not `JobOrigin`) because `ironhermes-core` cannot import `ironhermes-cron` (circular dep — RESEARCH Q-OQ1).
- **Plan 02 reuses Plan 01's `Config::telegram_default_origin()`** instead of plumbing runtime Origin through `CronjobTool::new` — zero plumbing surgery, consistent CLI/tool behavior. Multi-chat per-message-Origin precision is lost but user's whitelist is single-chat.
- **Plan 02 uses `tracing::warn!`** for multi-chat hint (NOT `eprintln!`) — stderr from inside an LLM tool would pollute the JSON response the LLM reads back.
- **`cmd_edit` is unchanged** (D-06) — researcher Q5 confirmed it already has `Option<String>` for deliver.
- **INV ledger final count: 64** (62 from Phase 22.4.2.1 + 1 per plan).
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)

1. `.planning/phases/22.4.2.2-cron-create-defaults-to-tg-origin-when-gateway-active/22.4.2.2-CONTEXT.md` — D-01..D-12 LOCKED. Note D-10 was UPDATED in-flight to reflect 2-plan structure.
2. `.planning/phases/22.4.2.2-cron-create-defaults-to-tg-origin-when-gateway-active/22.4.2.2-RESEARCH.md` — § Locked APIs, § Q-OQ1 (circular dep), § Q-OQ2 (helper extraction for testability).
3. `.planning/phases/22.4.2.2-cron-create-defaults-to-tg-origin-when-gateway-active/22.4.2.2-VALIDATION.md` — per-task automated commands; mirror these into executor `<acceptance_criteria>`.
4. `.planning/phases/22.4.2.2-cron-create-defaults-to-tg-origin-when-gateway-active/22.4.2.2-PATTERNS.md` — analog files for cmd_create + cronjob_tool.
5. `.planning/phases/22.4.2.2-cron-create-defaults-to-tg-origin-when-gateway-active/22.4.2.2-01-PLAN.md` and `22.4.2.2-02-PLAN.md` — the actual plans.
6. `.planning/phases/22.4.2.1-cron-cmds-and-telegram-delivery-broken/22.4.2.1-02-SUMMARY.md` — confirms `dispatch_delivery` already handles `deliver="origin"` (the cross-phase contract this phase relies on).

## Infrastructure State

- Working tree: clean except for the uncommitted plan files + ROADMAP.md edit.
- HEAD: `e7d5450` (Phase 22.4.2.1 final SUMMARY).
- `~/.ironhermes/config.yaml`: `gateway.platforms.telegram.enabled=true`, `whitelist=[7018949547]` — single-chat case (the simple/common path).
- `~/.ironhermes/cron/jobs.json`: 3 weather jobs with `deliver=local`, `origin=null` — these will NOT be auto-migrated by Phase 22.4.2.2; user must `hermes cron edit` after this phase ships.
- `_auto_chain_active`: true (set during the chain). If resuming manually without `--auto`/`--chain`, clear it via `gsd-sdk query config-set workflow._auto_chain_active false` (or edit `.planning/config.json` directly — config-set rejects underscore-prefixed keys).
- INV ledger currently at 62 (after Phase 22.4.2.1). This phase appends to 64.

## Pre-Execution Critique Required

Not applicable — this is a code-implementation phase, not a design phase. Plans were already written by gsd-planner; the next step is gsd-plan-checker verification.

<context>
Plans look solid from spot-check (frontmatter has wave/depends_on/decisions_addressed correctly; truths are user-observable; key_links cite specific symbols). The pattern-mapper and researcher caught the JobOrigin-vs-Origin naming and the circular-dep risk before planning started, so the plans should encode those constraints correctly.

The only meaningful risk left is whether the plan-checker flags something around D-12 (UAT note in SUMMARY.md). The verification_context was drafted in the conversation transcript and is reproducible.
</context>

<next_action>
**Resume with: spawn gsd-plan-checker.** Use this prompt skeleton (model: sonnet, subagent_type: gsd-plan-checker):

```
<verification_context>
**Phase:** 22.4.2.2 — Cron create defaults to TG origin when gateway active
**Phase Goal:** Restore v1.x ergonomic where `cron create` (without explicit `--deliver`) auto-routes to TG when the gateway is configured. Two plans: Plan 01 (CLI path), Plan 02 (LLM tool / dispatch_gateway path).

<files_to_read>
- .planning/phases/22.4.2.2-.../22.4.2.2-01-PLAN.md
- .planning/phases/22.4.2.2-.../22.4.2.2-02-PLAN.md
- .planning/ROADMAP.md
- .planning/REQUIREMENTS.md (no new REQ-IDs)
- .planning/phases/22.4.2.2-.../22.4.2.2-CONTEXT.md (D-01..D-12 LOCKED)
- .planning/phases/22.4.2.2-.../22.4.2.2-RESEARCH.md (Q-OQ1 circular dep, Q-OQ2 helper extraction)
- .planning/phases/22.4.2.2-.../22.4.2.2-PATTERNS.md
- .planning/phases/22.4.2.2-.../22.4.2.2-VALIDATION.md
- .planning/phases/22.4.2.1-.../22.4.2.1-02-SUMMARY.md
</files_to_read>

Verification focus:
1. Decision coverage (D-01..D-12; gate looks for "D-NN:" inline citations — verify decisions_addressed: blocks satisfy this)
2. Wave-2 dependency correctness (Plan 02 has wave: 2, depends_on: ["22.4.2.2-01"]; Plan 02 must NOT redefine OriginDecision)
3. Anti-shallow rules (every task has read_first + acceptance_criteria + concrete actions)
4. Cross-plan consistency (OriginDecision in core::config with String chat_id; both consumers construct JobOrigin from String fields; Plan 02 uses tracing::warn! NOT eprintln!)
5. Validation traceability (VALIDATION.md commands appear in plan task acceptance_criteria)
6. Goal-backward (CLI + LLM tool both default to TG origin when single-chat whitelist)
7. No opportunistic refactors (D-02 — Plan 02 must NOT modify CronjobTool::new, register_cronjob_tool, main.rs callers)
8. INV ledger correctness (Plan 01 INV = function 63; Plan 02 INV = function 64; final count 64)

End with `## VERIFICATION PASSED` or `## ISSUES FOUND` + YAML.
```

After plan-checker returns:
- If PASSED → run decision-coverage gate → record planning → commit → execute Plan 01 → execute Plan 02 → verify phase.
- If ISSUES → revision loop (max 3); for each BLOCKER, spawn gsd-planner with revision_context.
</next_action>
