---
phase: 22.4.2.2-cron-create-defaults-to-tg-origin-when-gateway-active
verified: 2026-04-26T00:00:00Z
status: human_needed
score: 17/17 must-haves verified
overrides_applied: 0
re_verification: false
human_verification:
  - test: "With config.yaml having gateway.platforms.telegram.enabled=true and a single whitelist entry, run `hermes cron create --name 'tg-test' --schedule '*/5 * * * *' --prompt 'echo hi'` (no --deliver flag), then inspect ~/.ironhermes/cron/jobs.json and confirm the created job has deliver=origin, origin.platform=telegram, origin.chat_id matching the whitelist entry."
    expected: "Job file shows deliver=origin and origin is a non-null object with platform=telegram and the correct chat_id."
    why_human: "Requires a live config.yaml with real TG gateway credentials and writing to the local filesystem at the real IRONHERMES_HOME path; cannot be exercised without real operator credentials and a configured gateway."
  - test: "With config.yaml having gateway.platforms.telegram.enabled=true and two whitelist entries, run `hermes cron create --name 'multi-test' --schedule '*/5 * * * *' --prompt 'echo hi'` (no --deliver flag), observe stderr output, then inspect the created job."
    expected: "stderr contains 'multiple authorized chats' and the actual whitelist values. Created job has deliver=local and origin=null."
    why_human: "Requires a live multi-entry whitelist config; stderr capture in automated tests is not wired for cmd_create at the process level."
  - test: "With a single-whitelist TG config, run `hermes cron create --name 'explicit-test' --schedule '*/5 * * * *' --prompt 'echo hi' --deliver local` and inspect the created job."
    expected: "Created job has deliver=local and origin=null — explicit flag wins despite eligible config."
    why_human: "End-to-end CLI binary smoke test; complements the unit-seam behavioral tests."
  - test: "Start hermes chat with a single-whitelist TG config active, ask the agent to create a cron job without specifying a deliver target, then inspect ~/.ironhermes/cron/jobs.json."
    expected: "The LLM-created job has deliver=origin, origin.platform=telegram, origin.chat_id matching the whitelist entry. The tracing::warn for multi-chat is only observable in the gateway log, not in the chat transcript."
    why_human: "Requires a live hermes chat session with TG gateway running; cannot be exercised programmatically."
---

# Phase 22.4.2.2: cron-create-defaults-to-tg-origin-when-gateway-active Verification Report

**Phase Goal:** Restore the v1.x ergonomic where `hermes cron create` (without explicit `--deliver`) auto-routes to TG when the gateway is configured. Two plans: Plan 01 covers the CLI path, Plan 02 covers the LLM tool / dispatch_gateway path. After this phase: cron jobs created via either entry point auto-deliver back to Telegram by default when the gateway whitelist has exactly one authorized chat.
**Verified:** 2026-04-26
**Status:** human_needed (all automated checks pass; 4 live UAT items require human)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `Config::telegram_default_origin()` exists in ironhermes-core::config with correct OriginDecision return type | VERIFIED | config.rs lines 650-667; enum at lines 593-600 |
| 2 | OriginDecision carries plain String fields (no JobOrigin embedding); ironhermes-core has no ironhermes-cron dep | VERIFIED | Cargo.toml has no ironhermes-cron entry; enum fields are `platform: String, chat_id: String` |
| 3 | whitelist len==1 → Single{platform:"telegram", chat_id: whitelist[0].to_string()} | VERIFIED | 4 unit tests pass: test_telegram_default_origin_single confirmed ok |
| 4 | whitelist len>1 → Multi{whitelist: Vec<String>}; enabled:false or missing → None | VERIFIED | test_telegram_default_origin_multi, test_telegram_default_origin_disabled, test_telegram_default_origin_no_section all pass |
| 5 | cmd_create consults resolve_cron_deliver helper; helper calls Config::telegram_default_origin() | VERIFIED | cron.rs lines 204-205; fn resolve_cron_deliver lines 149-179; INV-22.4.2.2-01 passes |
| 6 | Single case: cmd_create stores deliver="origin" and origin=Some(JobOrigin{chat_name:None,thread_id:None}) | VERIFIED | cron.rs lines 157-164; 5 behavioral tests pass (cron_default_deliver test suite) |
| 7 | Multi case: cmd_create falls back to deliver="local", origin=None, eprintln hint with "multiple authorized chats" | VERIFIED | cron.rs lines 166-174; eprintln present; grep count=1 for "multiple authorized chats" |
| 8 | None case (disabled/missing TG): deliver="local", origin=None, no eprintln | VERIFIED | cron.rs OriginDecision::None arm line 176; test tg_disabled_falls_back_to_local passes |
| 9 | Explicit --deliver flag bypasses helper entirely (D-04) | VERIFIED | resolve_cron_deliver Some(d) arm line 155; test explicit_deliver_flag_skips_helper passes |
| 10 | Create.deliver is Option<String> with no default_value attribute (D-11) | VERIFIED | grep returns 0 for `default_value.*"local"` in cron.rs; 4 occurrences of `deliver: Option<String>` |
| 11 | cmd_edit is UNCHANGED — helper NOT invoked from cmd_edit (D-06) | VERIFIED | cmd_edit body (lines 259-319) contains no call to resolve_cron_deliver or telegram_default_origin; git diff shows 0 cmd_edit changes |
| 12 | handle_create in cronjob_tool.rs consults Config::telegram_default_origin() when deliver arg absent | VERIFIED | cronjob_tool.rs lines 119-148; telegram_default_origin|OriginDecision count=4 (>=2) |
| 13 | Tool Multi case uses tracing::warn! NOT eprintln! (D-03 LLM-tool discipline) | VERIFIED | tracing::warn count=1; eprintln count=0 (non-comment) in cronjob_tool.rs |
| 14 | Tool stores deliver="origin"+origin=Some(JobOrigin) for Single; deliver="local"+origin=None for other cases | VERIFIED | 5 behavioral tests in cronjob_tool_default_deliver all pass; store.add_job gets origin_opt |
| 15 | CronjobTool::new signature unchanged (D-02) | VERIFIED | grep -c "fn new(store: Arc<Mutex<JobStore>>) -> Self" = 1 |
| 16 | INV ledger at exactly 64; INV-22.4.2.2-01 and INV-22.4.2.2-02 both present and passing | VERIFIED | grep -c "^fn invariant_" = 64; full ledger test result: 64 passed; both new INVs individually confirmed ok |
| 17 | D-12 UAT note appears verbatim in both 22.4.2.2-01-SUMMARY.md and 22.4.2.2-02-SUMMARY.md | VERIFIED | grep -c "Newly-enabled TG configs auto-route" = 1 in each file |

**Score:** 17/17 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/ironhermes-core/src/config.rs` | OriginDecision enum + telegram_default_origin method + 4 unit tests | VERIFIED | Enum at lines 593-600; method at lines 650-667; 4 tests pass |
| `crates/ironhermes-cli/src/cron.rs` | Create.deliver as Option<String>; fn resolve_cron_deliver; helper consultation in cmd_create; updated help text | VERIFIED | All grep checks pass; fn resolve_cron_deliver present |
| `crates/ironhermes-cli/tests/cron_default_deliver.rs` | 5 behavioral tests + env_lock + make_test_config | VERIFIED | File exists; 2 helpers confirmed; 5 tests pass |
| `crates/ironhermes-cli/tests/invariants_22_4.rs` | INV-22.4.2.2-01 (fn 63) + INV-22.4.2.2-02 (fn 64); total count = 64 | VERIFIED | Both INVs at lines 1548 and 1567; count = 64 |
| `crates/ironhermes-tools/src/cronjob_tool.rs` | handle_create consults telegram_default_origin; tracing::warn for Multi; no eprintln; store.add_job gets origin_opt | VERIFIED | All grep checks pass |
| `crates/ironhermes-tools/tests/cronjob_tool_default_deliver.rs` | 5 behavioral tests + env_lock + make_test_config + make_tool | VERIFIED | File exists; 3 helpers confirmed; 5 tokio tests pass |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| cmd_create body (cron.rs) | Config::telegram_default_origin | resolve_cron_deliver(deliver, &config) at line 205 | WIRED | Pattern "telegram_default_origin" found 8 times in cron.rs |
| OriginDecision::Single {platform, chat_id} | JobOrigin{platform, chat_id, chat_name: None, thread_id: None} | match arm in resolve_cron_deliver lines 157-164 | WIRED | JobOrigin construction verified; cross-phase contract: chat_name=None, thread_id=None |
| store.add_job in cmd_create | persisted job with deliver="origin" and origin=Some(JobOrigin) | deliver_str + origin_opt args at lines 208-216 | WIRED | old None arg gone (grep=0); origin_opt arg present (grep=1) |
| handle_create body (cronjob_tool.rs) | Config::telegram_default_origin | Config::load().unwrap_or_default().telegram_default_origin() at line 128 | WIRED | Pattern found; 5 behavioral tests confirm routing |
| OriginDecision::Multi | tracing::warn! | match arm in handle_create | WIRED | tracing::warn count=1; eprintln count=0 |
| store.add_job in handle_create | persisted job with origin_opt | 7th arg is origin_opt (line 160) | WIRED | old None arg gone; origin_opt present |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| cron.rs cmd_create | deliver_str, origin_opt | resolve_cron_deliver → Config::telegram_default_origin → whitelist[0].to_string() | Yes — real whitelist data from config.yaml or OriginDecision::None fallback | FLOWING |
| cronjob_tool.rs handle_create | deliver, origin_opt | Config::load().unwrap_or_default().telegram_default_origin() → whitelist[0].to_string() | Yes — same source, same parity | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 4 core unit tests (disabled/single/multi/no-section) | `cargo test -p ironhermes-core -- test_telegram_default_origin` | 4 passed, 0 failed | PASS |
| 5 Plan 01 CLI behavioral tests | `cargo test -p ironhermes-cli --test cron_default_deliver` | 5 passed, 0 failed | PASS |
| INV-22.4.2.2-01 + INV-22.4.2.2-02 | `cargo test -p ironhermes-cli --test invariants_22_4 -- invariant_22_4_2_2_01 invariant_22_4_2_2_02` | 2 passed, 0 failed | PASS |
| 5 Plan 02 LLM-tool behavioral tests | `cargo test -p ironhermes-tools --test cronjob_tool_default_deliver` | 5 passed, 0 failed | PASS |
| Full INV ledger | `cargo test -p ironhermes-cli --test invariants_22_4` | 64 passed, 0 failed | PASS |

### Requirements Coverage

No requirement IDs were declared for this phase (both plans have `requirements: []`). This is a polish/regression-fix decimal phase with no new REQ-IDs per REQUIREMENTS.md.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | No stubs, placeholder comments, hardcoded empty returns, or orphaned code found in phase-modified files | — | — |

Specific checks run:
- `grep -c "default_value.*local" crates/ironhermes-cli/src/cron.rs` = 0 (old attribute removed)
- `grep -c "store.add_job(..., None)" crates/ironhermes-tools/src/cronjob_tool.rs` = 0 (stub arg gone)
- `grep -c "eprintln!" crates/ironhermes-tools/src/cronjob_tool.rs` (non-comment) = 0 (D-03 discipline clean)

### Decision Compliance (D-01..D-12)

| Decision | Description | Status |
|----------|-------------|--------|
| D-01 | job.origin=Some(JobOrigin) + job.deliver="origin" for single-whitelist TG | VERIFIED |
| D-02 | Smallest change — CronjobTool::new signature bit-identical; cmd_edit body unchanged | VERIFIED |
| D-03 | Multi-chat: eprintln in CLI (cron.rs); tracing::warn in LLM tool (cronjob_tool.rs); no eprintln in tool | VERIFIED |
| D-04 | Explicit --deliver / deliver JSON arg always bypasses helper (Some(d) arm) | VERIFIED |
| D-05 | Missing TG / disabled TG → OriginDecision::None → deliver=local silently, no warn | VERIFIED |
| D-06 | cmd_edit body has 0 calls to resolve_cron_deliver or telegram_default_origin | VERIFIED |
| D-07 | OriginDecision defined in ironhermes-core with plain String fields; no circular dep | VERIFIED |
| D-08 | 5 behavioral tests in cron_default_deliver.rs; 5 in cronjob_tool_default_deliver.rs | VERIFIED |
| D-09 | Final INV ledger = 64; INV-22.4.2.2-01 at fn 63; INV-22.4.2.2-02 at fn 64 | VERIFIED |
| D-10 | Plan 01 wave 1, Plan 02 wave 2 (depends_on Plan 01) | VERIFIED (structural) |
| D-11 | Create.deliver is Option<String> with no default_value; help text updated | VERIFIED |
| D-12 | UAT note "Newly-enabled TG configs auto-route..." present in both SUMMARY files | VERIFIED |

### Cross-Phase Contract Verification

Phase 22.4.2.1 Plan 02 wired `dispatch_delivery` to consume `deliver="origin"` jobs by reading `job.origin.platform` and `job.origin.chat_id`. This phase produces such jobs from both entry points.

JobOrigin shape produced by this phase:
- `platform`: `"telegram"` (String literal)
- `chat_id`: `whitelist[0].to_string()` (i64 stringified)
- `chat_name`: `None` — matches RESEARCH §Q1 requirement
- `thread_id`: `None` — matches RESEARCH §Q1 requirement

This matches the shape `dispatch_delivery` expects to find in `job.origin`. Cross-phase contract: SATISFIED.

### Human Verification Required

#### 1. CLI single-whitelist end-to-end smoke test

**Test:** With `~/.ironhermes/config.yaml` having `gateway.platforms.telegram.enabled: true` and a single-entry `whitelist`, run `hermes cron create --name 'tg-test' --schedule '*/5 * * * *' --prompt 'echo hi'` (no `--deliver` flag). Inspect the created entry in `~/.ironhermes/cron/jobs.json`.
**Expected:** Job has `"deliver": "origin"`, `"origin": {"platform": "telegram", "chat_id": "<whitelist_entry>", "chat_name": null, "thread_id": null}`.
**Why human:** Requires real operator `config.yaml` with live IRONHERMES_HOME; cannot be exercised in the automated test harness without real credentials.

#### 2. CLI multi-whitelist stderr hint and fallback

**Test:** Temporarily add a second entry to the whitelist, run the same `cron create` command, observe stderr and the created job.
**Expected:** stderr contains `"multiple authorized chats"` and the actual whitelist values. Created job has `"deliver": "local"`, `"origin": null`.
**Why human:** Live multi-entry whitelist config required; cmd_create stderr capture at the process level is not wired in automated tests.

#### 3. CLI explicit --deliver bypass with eligible config

**Test:** With a single-whitelist TG config (eligible for auto-routing), run `hermes cron create --name 'explicit-test' --schedule '*/5 * * * *' --prompt 'echo hi' --deliver local`. Inspect the created job.
**Expected:** Job has `"deliver": "local"`, `"origin": null` — explicit flag wins despite eligible config.
**Why human:** End-to-end CLI binary smoke test complementing the unit-seam behavioral tests.

#### 4. LLM tool path end-to-end (hermes chat)

**Test:** Start `hermes chat` with a single-whitelist TG config active. Ask the agent to schedule a cron job without specifying a deliver target. Inspect `~/.ironhermes/cron/jobs.json`.
**Expected:** The LLM-created job has `"deliver": "origin"`, `"origin": {"platform": "telegram", "chat_id": "<whitelist_entry>", ...}`. The `tracing::warn` for multi-chat is only visible in gateway logs, not in the chat transcript.
**Why human:** Requires a live `hermes chat` session with TG gateway running; cannot be exercised programmatically.

### Gaps Summary

No gaps. All 17 must-haves verified across both plans. All targeted tests pass. The 4 human verification items are live UAT checks requiring a configured TG gateway — they cannot be automated without real credentials.

---

_Verified: 2026-04-26_
_Verifier: Claude (gsd-verifier)_
