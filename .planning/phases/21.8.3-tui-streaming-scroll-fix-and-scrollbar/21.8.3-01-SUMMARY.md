---
phase: 21.8.3
plan: "01"
subsystem: tui-rata
tags:
  - tui
  - ratatui
  - scroll
  - bug-fix
dependency_graph:
  requires:
    - "21.8.2-04: scroll_to_bottom() helper introduced"
  provides:
    - "correct transcript_line_count with role-prefix accounting"
    - "snap-to-bottom on StreamEvent::Finished"
    - "submit() uses scroll_to_bottom() helper"
    - "End/Ctrl+End key binding for jump-to-bottom"
    - "paused indicator in scroll_indicator()"
  affects:
    - "crates/ironhermes-cli/src/tui_rata/app.rs"
tech_stack:
  added: []
  patterns:
    - "TDD RED/GREEN cycle for scroll-state bug-fix"
    - "let-Some structural guard mirroring transcript_text() semantics"
    - "effective_width = width.saturating_sub(prefix_len).max(1) for line 0"
key_files:
  created: []
  modified:
    - "crates/ironhermes-cli/src/tui_rata/app.rs"
decisions:
  - "D-06/D-07: role_style() lookup + prefix subtraction makes transcript_line_count mirror transcript_text() exactly — no role currently returns None so the let-Some guard is structural only"
  - "D-08: auto_follow guard on Finished snap prevents resetting scroll when user deliberately scrolled up then stream ended"
  - "D-09: scroll_to_bottom() in submit() vs bare auto_follow=true — single source of truth, reconcile_scroll corrects transcript_scroll=0 on next render tick"
  - "D-11: Option B (derived from transcript_max_scroll - transcript_scroll) chosen for paused indicator — no new App field, resets on resize which is acceptable"
  - "Pre-existing inv_25_1_gap8_app_carries_browser_session_field failure confirmed pre-existing (fails on base commit 5a89bd4a); out of scope per deviation rule scope boundary"
metrics:
  duration: "4 minutes"
  completed: "2026-05-09"
  tasks_completed: 2
  files_modified: 1
---

# Phase 21.8.3 Plan 01: streaming-scroll fix + End key + paused indicator Summary

Fixed the rata-TUI streaming-scroll bug by repairing `transcript_line_count` / `transcript_text` render parity, then layered in defense-in-depth (Finished snap, submit helper unification, End/Ctrl+End jump-to-bottom, paused indicator) — all in `crates/ironhermes-cli/src/tui_rata/app.rs`.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | TDD RED — 6 failing tests for line-count parity, snap-on-Finished, submit helper, End key | 843fd557 | app.rs (+144 lines) |
| 2 | TDD GREEN — implement D-06..D-11 production fixes | a4f3d29c | app.rs (+43/-6 lines) |

## Five Edits Applied

**Edit 1 — D-06/D-07: `transcript_line_count` body replacement (lines 426-440 → 426-461)**

- Uses `role_style(msg)` to get `(role_label, color)` for each history message
- `let Some(_color) = color else { continue }` mirrors `transcript_text()` line 785 structural guard
- `prefix_len = role_label.len() + 2` accounts for `": "` separator
- Line 0: `effective_width = width.saturating_sub(prefix_len).max(1)` — narrows width by prefix
- Continuation lines: `effective_width = width` — full terminal width
- `assistant_buffer` block: hardcoded `prefix_len = "Hermes".len() + 2 = 8`
- Pitfall 1 resolved: System now INCLUDED (role_style returns `Some(DarkGray)` post-22.4-17; D-07's "skip" was written against old behavior)

**Edit 2 — D-08: `StreamEvent::Finished` snap-to-bottom (line 677)**

- Added `if self.auto_follow { self.scroll_to_bottom(); }` after `commit_assistant_buffer()`
- Guard ensures it only snaps when user hasn't manually scrolled away mid-stream

**Edit 3 — D-09: `submit()` helper unification (line 742)**

- Replaced `self.auto_follow = true;` with `self.scroll_to_bottom();`
- `reconcile_scroll` corrects the temporary `transcript_scroll=0` on the next render tick
- Pre-existing test `non_slash_submit_creates_pending_rx_and_pending_tx` does not assert on `transcript_scroll` (verified per RESEARCH Pitfall 3)

**Edit 4 — D-10: End key binding (after line 494)**

- Added `(KeyCode::End, _) => self.scroll_to_bottom()` arm after `PageDown` arm
- Single arm catches both plain End and Ctrl+End via wildcard modifiers
- Placed before `Esc` arm per Pitfall 4 (must precede catch-all)

**Edit 5 — D-11: paused indicator in `scroll_indicator()` (lines 398-405)**

- Hoisted `let max = self.transcript_max_scroll(area)` before the if/else chain
- Added `else if self.pending_rx.is_some() || self.assistant_buffer.is_some()` branch
- `format!("paused ({n} new lines below)")` where `n = max.saturating_sub(self.transcript_scroll)`
- Option B chosen: derived from existing state, no new App field

## Six New Tests Passing

All in `mod scroll_tests` (gated on `#[cfg(all(test, feature = "test-support"))]`):

| Test | Requirement | Status |
|------|-------------|--------|
| `transcript_line_count_accounts_for_role_prefix` | D-06 | GREEN |
| `system_message_counted_in_line_count` | D-07 | GREEN |
| `stream_finished_snaps_to_bottom` | D-08 | GREEN |
| `submit_calls_scroll_to_bottom` | D-09 | GREEN |
| `end_key_calls_scroll_to_bottom` | D-10 | GREEN |
| `auto_follow_tracks_buffer_growth` | D-13c | GREEN (was also GREEN at RED stage — acceptable per plan done criteria) |

Pre-existing tests: all 20 pre-existing scroll_tests pass. 26/26 total.

## Deviations from Plan

### Pre-existing Out-of-Scope Failure

**Found during:** Overall verification (full `cargo test -p ironhermes-cli --features test-support`)

**Issue:** `tui_rata::app::inv_tests::inv_25_1_gap8_app_carries_browser_session_field` FAILS — 231 pass, 1 fails.

**Scope determination:** Confirmed pre-existing by running the test against the base commit `5a89bd4a` (before any plan changes) — same failure. Unrelated to this plan's `app.rs` changes (invariant test about `browser_session` field propagation from Phase 25.1 GAP-8).

**Action:** Logged to deferred-items. Not fixed per scope boundary rule (only fix issues directly caused by current task's changes).

### `auto_follow_tracks_buffer_growth` Incidental Pass in RED Phase

Per plan done criteria: "The 6th may incidentally pass at the RED stage if pre-fix and post-fix happen to agree." With `"x\n".repeat(200)` (200 lines, each 1 char + newline, no prefix mismatch at that scale), the buggy impl returned the same `max` as the fixed one for this specific input. 5/6 tests were RED as required.

## Known Stubs

None. All production code is wired. The Atlanta-weather UAT scenario (D-16) requires manual verification post-Plan 02 (scrollbar widget in `ui.rs`).

## Threat Flags

None. All changes are pure local `&mut self` state mutations. No new external surface, no I/O, no auth.

## TDD Gate Compliance

- RED gate commit: `843fd557` (`test(21.8.3-01): ...`) — 5/6 tests FAILED as required
- GREEN gate commit: `a4f3d29c` (`fix(21.8.3-01): ...`) — all 26 tests PASSED
- TDD cycle complete and correctly ordered.

## Self-Check: PASSED

- FOUND: `.planning/phases/21.8.3-tui-streaming-scroll-fix-and-scrollbar/21.8.3-01-SUMMARY.md`
- FOUND: `crates/ironhermes-cli/src/tui_rata/app.rs`
- FOUND: commit `843fd557` (RED test commit)
- FOUND: commit `a4f3d29c` (GREEN fix commit)
- All 6 test function names verified present in app.rs (grep count: 1 each)
