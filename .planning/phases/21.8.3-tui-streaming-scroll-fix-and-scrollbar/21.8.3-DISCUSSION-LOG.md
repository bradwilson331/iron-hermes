# Phase 21.8.3: tui-streaming-scroll-fix-and-scrollbar — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-09
**Phase:** 21.8.3-tui-streaming-scroll-fix-and-scrollbar
**Areas discussed:** Scrollbar widget design, Streaming-follow fix scope, User scroll-up-during-stream UX, Test strategy

---

## Scrollbar Widget Design

### Q1: Where should the scrollbar render?

| Option | Description | Selected |
|--------|-------------|----------|
| Right edge, inside the transcript border | `ScrollbarOrientation::VerticalRight`, render after Paragraph in same area. Border absorbs the column; no content reflow. | ✓ |
| Right edge, outside the transcript border | Render in a sliver area next to the Paragraph. Slightly more visible, eats one column. | |
| You decide | Claude picks idiomatic ratatui placement during planning. | |

**User's choice:** Inside-border-right (Recommended) → D-01

### Q2: When should the scrollbar be visible?

| Option | Description | Selected |
|--------|-------------|----------|
| Always visible | Empty track when content fits, full track when scrolling. Predictable layout. | ✓ |
| Only when scrollable | Hide when `transcript_max_scroll == 0`; can cause 1-column reflow. | |
| On-scroll fade (auto-hide) | Show during scroll input, fade after N idle ticks. ratatui Scrollbar lacks native fade; would need custom state machine. | |

**User's choice:** Always visible (Recommended) → D-02

### Q3: What scrollbar style/character set?

| Option | Description | Selected |
|--------|-------------|----------|
| Default ratatui Scrollbar style | `Scrollbar::default()`, thin track + filled thumb, terminal-theme colors. | ✓ |
| Custom thumb/track chars | Override symbols (e.g., `┃` thumb, `│` track). Slight differentiation, risks looking off in non-Unicode envs. | |
| You decide | Claude picks something matching Hermes-green theme. | |

**User's choice:** Default (Recommended) → D-03

### Q4: Scrollbar vs existing 'live' / 'scroll N/M' indicator?

| Option | Description | Selected |
|--------|-------------|----------|
| Keep title indicator + add scrollbar | Both reinforce each other. Textual readout for accessibility, visual bar for at-a-glance position. | ✓ |
| Drop title indicator, keep scrollbar only | Cleaner border title, loses 'live' textual cue. | |
| Repurpose title to show only auto-follow state | Title becomes 'live' / 'paused'; scrollbar carries position. | |

**User's choice:** Keep both (Recommended) → D-04

---

## Streaming-Follow Fix Scope

### Q1: How broad should the transcript_line_count fix be?

| Option | Description | Selected |
|--------|-------------|----------|
| Narrow: role-prefix accounting only | Fix only the under-count from `"You: "` / `"Hermes: "` prefix not subtracted from width. Single-cause fix. | ✓ |
| Broad: full audit | Audit every divergence: role prefix, blank lines, System suppression, assistant_buffer trailing newline, etc. Bigger blast radius. | |
| Narrow + parity test | Fix role prefix, add a parity test asserting `transcript_line_count(width) == rendered Lines count`. | |

**User's choice:** Narrow (Recommended) → D-06

### Q2: Snap-to-bottom safety net on StreamEvent::Finished?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — explicit snap on Finished if auto_follow=true | Defense-in-depth. Cheap, low risk. | ✓ |
| No — fix the root cause only | Trust transcript_line_count post-fix. Cleaner architecture. | |
| Yes, on every StreamEvent::Delta too | Aggressive re-snap. Probably overkill — reconcile_scroll already runs every render. | |

**User's choice:** Yes on Finished (Recommended) → D-08

### Q3: Reuse `scroll_to_bottom()` helper on the agent-turn path?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — replace submit() line 742's `auto_follow = true` with self.scroll_to_bottom() | Single source of truth. Helper sets auto_follow + zeros scroll; reconcile_scroll wins next tick. | ✓ |
| No — leave submit() as-is | Helper sets transcript_scroll=0; submit() doesn't. Avoids any chance of behavior change. | |
| You decide during planning | Claude verifies whether transcript_scroll value matters between submit return and next render. | |

**User's choice:** Yes, unify (Recommended) → D-09

### Q4: Also fix System message handling in transcript_line_count?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — skip System messages in line count | transcript_text() skips them (line 785, role_style returns None); transcript_line_count counts them. Symmetric bug to D-06. | ✓ |
| No — not in scope for this phase | Defer; same kind of bug. | |
| Only if the parity test catches it | Falls out from the parity test option in Q1 (which wasn't picked). | |

**User's choice:** Yes (Recommended) → D-07

---

## User Scroll-up-During-Stream UX

### Q1: Add a key to jump to bottom and re-engage auto-follow?

| Option | Description | Selected |
|--------|-------------|----------|
| End / Ctrl+End — snap to bottom + re-engage auto_follow | Standard terminal/IDE mental model. Sits next to existing PageUp/PageDown. | ✓ |
| G (vim shift+g) | Vim convention; rata-TUI is not modal so single-letter risks future input-mode conflict. | |
| Ctrl+E | Emacs convention. | |
| No new key | reconcile_scroll re-engages auto_follow when user scrolls back to bottom. Simpler but less convenient. | |

**User's choice:** End / Ctrl+End (Recommended) → D-10

### Q2: 'Paused' indicator when user scrolls up mid-stream?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — title shows `paused (N new lines below)` when auto_follow=false during active stream | Cue that streaming continues; explains static viewport. | ✓ |
| Yes, simpler: just `paused` (no line count) | Easier — no new-line delta tracking. | |
| No — keep existing 'scroll N/M' | Less work; user can deduce streaming-still-going from status hint. | |

**User's choice:** Yes with N count (Recommended) → D-11

### Q3: Scrollbar thumb tracks assistant_buffer growth during streaming?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — thumb reflects current scroll position vs growing total each render | Natural: thumb stays at bottom when auto_follow=true; moves up when user scrolls up while content grows. | ✓ |
| No — freeze thumb during streaming, update on Finished | Less work but loses live cue exactly when most useful. | |

**User's choice:** Yes, live thumb (Recommended) → D-05

### Q4: Auto-follow re-engage on submit (mid-pause)?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — keep submit() re-engaging auto_follow | Existing behavior at app.rs:742. New prompt = explicit user action. | ✓ |
| Make it a setting | Configurable. Probably overkill — Ctrl+End is the manual override. | |

**User's choice:** Keep submit re-engage (Recommended) → D-12

---

## Test Strategy

### Q1: Coverage level for transcript_line_count fixes?

| Option | Description | Selected |
|--------|-------------|----------|
| Targeted unit tests in mod scroll_tests | (a) Long User msg with 'You: ' prefix at narrow widths, (b) System msg = 0 contribution, (c) regression test for the bug-as-experienced. | ✓ |
| Targeted + parity test | Same plus an assertion `transcript_line_count == rendered Lines count`. | |
| Property-based testing (proptest) | New dep + harness. Strongest guarantee, overkill for one phase. | |

**User's choice:** Targeted unit tests (Recommended) → D-13

### Q2: Scrollbar widget render test?

| Option | Description | Selected |
|--------|-------------|----------|
| Snapshot test via TestBackend | Render at fixed size, snapshot buffer, assert against expected ASCII grid. Standard ratatui pattern. | ✓ |
| Stateful unit test only | Test ScrollbarState transitions without rendering. Faster but doesn't catch render-time issues. | |
| Manual UAT only | No automated test. Render regression could land silently. | |

**User's choice:** Snapshot test (Recommended) → D-14

### Q3: End / Ctrl+End binding test?

| Option | Description | Selected |
|--------|-------------|----------|
| Unit test on handle_key | Existing PageUp/PageDown coverage at handle_key (line 462). Add KeyEvent for End / Ctrl+End. | ✓ |
| Unit test + manual UAT | Same plus log in UAT.md for live verification. | |

**User's choice:** Unit test (Recommended) → D-15

### Q4: Bare-minimum manual UAT?

| Option | Description | Selected |
|--------|-------------|----------|
| Long weather-style response stays visible to bottom | Reproduce today's screenshot scenario. Atlanta weather → 'Heads up' line visible without manual scroll. | ✓ |
| Multi-turn conversation (5+ exchanges, mixed lengths) | Catches interaction edge cases. | |
| Both | Belt-and-suspenders. | |

**User's choice:** Atlanta weather repro (Recommended) → D-16

---

## Claude's Discretion

- Exact subtraction logic for the role prefix in `transcript_line_count` (compute from `role_style` label vs hard-code length).
- ScrollbarState lifecycle (per-render recompute vs cached field on `App`).
- "N new lines below" counter mechanics (separate field vs derived from `max - scroll`; reset semantics on resize / user-initiated `scroll_down`).
- Whether to add symmetric `Home` / `Ctrl+Home` for jump-to-top (low-friction tag-along; not locked).

## Deferred Ideas

- Property-based testing of transcript_line_count vs transcript_text parity — future phase if `proptest` / `quickcheck` adopted.
- Full audit of all `transcript_line_count` vs `transcript_text` divergences beyond role prefix + System skip — narrow fix locked for this phase.
- Mouse wheel sensitivity tuning (current `scroll_up(3)` / `scroll_down(3)` unchanged).
- Horizontal scrolling — out of scope; long lines wrap.
- Accessibility / screen-reader cues — future cross-cutting phase if needed.
- Configurable "snap on submit" — overkill; submit always re-engages.
