---
phase: 36.1-running-agent-guard-parity-web-ui-tui-inserted
plan: 02
type: execute
wave: 2
depends_on:
  - 36.1-01
files_modified:
  - crates/iron_hermes_ui/src/server/state.rs
  - crates/iron_hermes_ui/src/server/ws.rs
  - crates/iron_hermes_ui/tests/running_agent_guard_web_tests.rs
autonomous: true
requirements:
  - GW-05-WEB
tags:
  - rust
  - web-ui
  - websocket
  - slash-commands
  - per-session-state
  - atomic-bool
  - raii

must_haves:
  truths:
    - "Each web UI session_id maps to its own Arc<AtomicBool> on AppState; setting session A's flag does not affect session B's flag (D-03)"
    - "Slash commands typed in the web UI are intercepted in ws.rs BEFORE run_web_turn — resolved via app_state.command_router and dispatched directly, never reaching run_web_turn (D-04)"
    - "Slash dispatch output is delivered to the WebSocket client as ChatStreamEvent::Delta { text } followed by ChatStreamEvent::Finished { total_tokens: 0 } — no new protocol variant added (D-05)"
    - "When agent_running.load(SeqCst) is true: non-bypass slash commands AND plain-text messages are rejected with the exact AGENT_RUNNING_REJECT_MSG via Delta+Finished; bypass-listed slash commands (stop/new/status/queue) dispatch normally (D-06, D-07)"
    - "RunningAgentGuard is constructed at the top of run_web_turn (inside the async future, not before tokio::spawn returns) — Pitfall 1 mitigation; flag clears via RAII Drop on Ok, Err, ?, panic, and future cancel"
    - "Bypass-list check uses ironhermes_core::commands::running_agent::is_bypass(&def.name) on the POST-resolution canonical name from CommandRouter::resolve — never the raw user input (Pitfall 4 — /reset resolves to 'new' and correctly bypasses)"
    - "running_agents field uses Arc<std::sync::Mutex<HashMap<String, Arc<AtomicBool>>>> — same shape and Mutex flavor as the existing nudge_turns field at state.rs:38 (D-03 + Claude's Discretion lock)"
  artifacts:
    - path: "crates/iron_hermes_ui/src/server/state.rs"
      provides: "AppState.running_agents field + get_or_create_running_flag helper + RunningAgentGuard bind at top of run_web_turn"
      contains: "pub running_agents: Arc<std::sync::Mutex<HashMap<String, Arc<AtomicBool>>>>"
    - path: "crates/iron_hermes_ui/src/server/ws.rs"
      provides: "Slash interception block + plain-text guard check inserted into the WebSocket select! loop"
      contains: "if message.starts_with('/')"
    - path: "crates/iron_hermes_ui/tests/running_agent_guard_web_tests.rs"
      provides: "Integration test suite — 6 behaviors: session isolation, /model rejected, /stop bypasses, RAII clears on error, /reset alias bypasses, plain-text rejected"
      contains: "fn test_session_isolation"
  key_links:
    - from: "crates/iron_hermes_ui/src/server/ws.rs"
      to: "ironhermes_core::commands::running_agent"
      via: "use import + AGENT_RUNNING_REJECT_MSG + is_bypass + RunningAgentGuard (constructed inside run_web_turn)"
      pattern: "use ironhermes_core::commands::running_agent"
    - from: "crates/iron_hermes_ui/src/server/state.rs"
      to: "ironhermes_core::commands::running_agent::RunningAgentGuard"
      via: "RunningAgentGuard::new(running_flag) at top of run_web_turn body"
      pattern: "RunningAgentGuard::new"
    - from: "crates/iron_hermes_ui/src/server/ws.rs"
      to: "app_state.command_router.resolve"
      via: "slash-command resolution before guard check + dispatch"
      pattern: "command_router.resolve"
---

<objective>
Wire the Phase 36.1 running-agent guard into the web UI: per-session `Arc<AtomicBool>` map on `AppState`, slash-command interception in `ws.rs` before `run_web_turn`, D-02 rejection for non-bypass slash commands and plain-text messages when a turn is in flight, and `RunningAgentGuard` RAII at `run_web_turn` entry.

Purpose: Implements D-03 through D-07 — closes BACKLOG item 1 (web UI has no `/stop`, `/model` etc. interception today; messages typed during a turn race or are silently dropped). After this plan, the web UI has feature parity with the gateway for running-state guard behavior.

Output: `AppState` gains `running_agents` field + `get_or_create_running_flag` helper; `ws.rs` gains a slash-interception block + plain-text guard check; `run_web_turn` body opens with a `RunningAgentGuard`; new integration test file covers the 6 GW-05-WEB behaviors mirroring Phase 36's gateway suite pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-CONTEXT.md
@.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-RESEARCH.md
@.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-PATTERNS.md
@.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-01-PLAN.md

@crates/iron_hermes_ui/src/server/state.rs
@crates/iron_hermes_ui/src/server/ws.rs
@crates/iron_hermes_ui/src/protocol.rs
@crates/ironhermes-gateway/src/handler.rs
@crates/ironhermes-gateway/src/session.rs
@crates/ironhermes-gateway/tests/running_agent_guard_tests.rs

<interfaces>
<!-- Shared running-agent machinery from Plan 01 (now live in ironhermes-core). -->

From crates/ironhermes-core/src/commands/running_agent.rs (created by Plan 01):
- `pub const AGENT_RUNNING_REJECT_MSG: &str;` — exact D-02 string
- `pub struct RunningAgentGuard(Arc<AtomicBool>);` — RAII guard
- `pub fn is_bypass(name: &str) -> bool;` — bypass list: stop, new, status, queue

From crates/iron_hermes_ui/src/server/state.rs (current — lines 25-60):
- `pub struct AppState { pub command_router: Arc<CommandRouter>, pub state_store: Arc<std::sync::Mutex<StateStore>>, ..., pub nudge_turns: Arc<std::sync::Mutex<HashMap<String, u32>>>, ... }`
- `impl AppState { pub async fn init() -> Result<Self>; pub fn reset_web_session(&self, session_id: &str); pub async fn run_web_turn(&self, ...) -> ...; }`

From crates/iron_hermes_ui/src/protocol.rs:
- `pub enum ChatStreamEvent { Delta { text: String }, Finished { total_tokens: u32 }, Error { ... }, ToolCallStart { ... }, ToolCallEnd { ... }, SubagentEvent { ... } }`
- `pub struct ChatRequest { pub message: String, pub session_id: String, ... }`

From crates/iron_hermes_ui/src/server/ws.rs (current structure — relevant lines):
- Line ~95: `rx: mpsc::UnboundedReceiver<ChatStreamEvent>` inside InFlightTurn
- Line ~100: `let mut in_flight_turn: Option<InFlightTurn> = None;`
- Line ~108: `tokio::select!` loop
- Line ~197: `if in_flight_turn.is_some()` busy-guard (sends `ChatStreamEvent::Error`)
- Line ~209: `let (tx, rx) = mpsc::unbounded_channel::<ChatStreamEvent>();`
- Line ~213: `let message = req.message;`
- Line ~214: `let handle = tokio::spawn(async move { ... app_state.run_web_turn(...).await ... });`
- Line ~294: `in_flight_turn = Some(InFlightTurn { ... });`

From ironhermes_core::types::Platform (read crate types — likely `Platform::Local` or `Platform::Web`; planner must confirm at implementation time which variant the web UI supplies):
- Verify: `grep -n "platform\|Platform::" crates/iron_hermes_ui/src/server/ws.rs crates/iron_hermes_ui/src/server/state.rs` to find the existing convention (if any) for the platform passed to CommandContext.

From CommandRouter (ironhermes-core::commands::registry):
- `pub fn resolve(&self, input: &str, platform: &Platform) -> ResolveResult;`
- `pub enum ResolveResult { Exact(CommandDef), PrefixMatch(CommandDef), Ambiguous(Vec<String>), NotFound }`

Dispatch path note: the canonical dispatch helper is in `ironhermes_core::commands::handlers::dispatch` (used by the gateway's `handle_slash_command` around handler.rs:499-600). Read that path during implementation to mirror the dispatch call signature.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add running_agents field + get_or_create_running_flag helper + run_web_turn guard bind</name>
  <files>crates/iron_hermes_ui/src/server/state.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/state.rs (lines 1-220 — AppState struct, init, run_web_turn body)
    - crates/iron_hermes_ui/src/server/state.rs (line 38 — `nudge_turns` field — exact precedent for the new field's type shape)
    - crates/iron_hermes_ui/src/server/state.rs (line 161 — `nudge_turns` init in `Ok(Self { ... })`)
    - crates/ironhermes-gateway/src/session.rs (lines 44-90 — `GatewaySession.running` field) and (lines 256-261 — `get_running_flag` helper)
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-PATTERNS.md (section "state.rs (MODIFY)")
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-RESEARCH.md (Pattern 2 "Web UI — AppState running_agents field" — including the Mutex selection rationale: std::sync::Mutex, NOT tokio::sync::Mutex)
    - Plan 36.1-01 PLAN — confirms the import path `ironhermes_core::commands::running_agent::RunningAgentGuard`
  </read_first>
  <behavior>
    - Test 1 (`new_session_flag_starts_false`): `app_state.get_or_create_running_flag("session-A").load(SeqCst)` returns false for a fresh session_id.
    - Test 2 (`flag_persists_across_lookups`): calling `get_or_create_running_flag("session-A")` twice returns Arc handles pointing to the SAME AtomicBool (verified by setting via the first and observing via the second). Proves the get-OR-create semantics — second call must NOT create a fresh false-initialized flag.
    - Test 3 (`session_isolation`): set session A's flag to true via `get_or_create_running_flag("session-A").store(true, SeqCst)`; assert session B's flag (`get_or_create_running_flag("session-B")`) loads false — proves the HashMap keys sessions independently (T-36.1-07 mitigation: no cross-session bleed).
    - Test 4 (`run_web_turn_sets_flag_during_execution`): smoke test that `RunningAgentGuard::new(flag)` inside `run_web_turn` sets the flag to true (callable via the helper + manual guard construction in a unit test scope; full run_web_turn execution may require additional infrastructure — at minimum unit-test the helper retrieves the live Arc).
  </behavior>
  <action>
    Add a new field to the `AppState` struct in `crates/iron_hermes_ui/src/server/state.rs` IMMEDIATELY AFTER the `nudge_turns` field (line 38, per RESEARCH.md Pattern 2): `pub running_agents: Arc<std::sync::Mutex<HashMap<String, Arc<std::sync::atomic::AtomicBool>>>>,` with doc comment "Phase 36.1 (GW-05-WEB, D-03): per-session running-agent flag map. Each session_id maps to its own Arc<AtomicBool>. Mutex choice: std::sync::Mutex (non-async brief access), matching the nudge_turns field at line 38." In `AppState::init()` `Ok(Self { ... })` block (around line 156-168), add `running_agents: Arc::new(std::sync::Mutex::new(HashMap::new())),` directly after the `nudge_turns:` init line. Add a new public method on `impl AppState` (place AFTER `reset_web_session` near line 201, BEFORE `run_web_turn` at line 208): `pub fn get_or_create_running_flag(&self, session_id: &str) -> Arc<std::sync::atomic::AtomicBool>` — body: `let mut map = self.running_agents.lock().unwrap_or_else(|e| e.into_inner()); map.entry(session_id.to_string()).or_insert_with(|| Arc::new(std::sync::atomic::AtomicBool::new(false))).clone()`. (This mirrors `SessionStore::get_running_flag` in `ironhermes-gateway/src/session.rs:256-261`.) Inside `run_web_turn` (line 208+), add at the TOP of the function body — BEFORE any `?`-returning code — the lines: `let running_flag = self.get_or_create_running_flag(session_id);` then `let _agent_guard = ironhermes_core::commands::running_agent::RunningAgentGuard::new(running_flag);` (Pitfall 1: this is INSIDE the async fn body, so Drop fires when the future completes — not when a sync caller returns). Add any missing imports (`std::sync::atomic::AtomicBool` may already be imported via `std::sync::*`; if needed add `use ironhermes_core::commands::running_agent::RunningAgentGuard;` at the top of state.rs). Append a `#[cfg(test)] mod tests { ... }` (or add tests to the existing test module if present) covering the four `<behavior>` items. Do NOT remove or alter the existing busy-guard or `in_flight_turn` machinery in ws.rs (Task 2 territory). Mutex flavor MUST be `std::sync::Mutex` (Pitfall 5 awareness: map grows unbounded per RESEARCH §Pitfall 5 — accepted tradeoff matching the nudge_turns precedent; do NOT add cleanup complexity in this phase).
  </action>
  <verify>
    <automated>cargo test -p iron_hermes_ui --lib server::state 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'pub running_agents: Arc<std::sync::Mutex<HashMap<String, Arc<std::sync::atomic::AtomicBool>>>>' crates/iron_hermes_ui/src/server/state.rs` returns 1
    - Source assertion: `grep -c 'fn get_or_create_running_flag' crates/iron_hermes_ui/src/server/state.rs` returns 1
    - Source assertion: `grep -c 'running_agents: Arc::new(std::sync::Mutex::new(HashMap::new()))' crates/iron_hermes_ui/src/server/state.rs` returns 1
    - Source assertion: `grep -c 'RunningAgentGuard::new' crates/iron_hermes_ui/src/server/state.rs` returns at least 1 (bound inside run_web_turn body)
    - Source assertion: `grep -v '^\s*//' crates/iron_hermes_ui/src/server/state.rs | grep -c 'tokio::sync::Mutex' | grep -E '^0$|^1$'` — count of `tokio::sync::Mutex` references in the new field type MUST be zero (Pattern 2 rationale: std::sync::Mutex chosen)
    - Test command: `cargo test -p iron_hermes_ui --lib server::state` exits 0 with at least 4 new passing tests (`new_session_flag_starts_false`, `flag_persists_across_lookups`, `session_isolation`, plus the RAII smoke).
    - Build command: `cargo build -p iron_hermes_ui` exits 0
    - Lint command: `cargo clippy -p iron_hermes_ui --lib -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    `AppState` has the `running_agents` field initialized in `init()`. The helper `get_or_create_running_flag` returns the live Arc (not a fresh one). `run_web_turn` body opens with `let _agent_guard = RunningAgentGuard::new(self.get_or_create_running_flag(session_id))`. Unit tests prove session isolation and Arc persistence across lookups.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ws.rs — slash interception, plain-text guard check, channel restructure</name>
  <files>crates/iron_hermes_ui/src/server/ws.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/ws.rs (lines 1-400 — full WebSocket handler structure, select! arms, in_flight_turn lifecycle, channel creation site at line ~209, tokio::spawn at line ~214)
    - crates/ironhermes-gateway/src/handler.rs (lines 486-498 — slash guard check pattern to MIRROR for ws.rs slash interception)
    - crates/ironhermes-gateway/src/handler.rs (lines 854-866 — free-text guard check pattern to MIRROR for ws.rs plain-text path)
    - crates/ironhermes-gateway/src/handler.rs (lines 499-600 — handle_slash_command dispatch wiring for `ironhermes_core::commands::handlers::dispatch` call signature)
    - crates/iron_hermes_ui/src/server/state.rs (final after Task 1 — `get_or_create_running_flag` helper available)
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-PATTERNS.md (section "ws.rs (MODIFY)")
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-RESEARCH.md (Pitfall 4: post-resolution canonical-name check — `is_bypass(&def.name)`, NEVER on raw input; Pitfall 6 + 7: keep slash dispatch out of `in_flight_turn`)
    - crates/ironhermes-core/src/commands/handlers.rs (to confirm the `dispatch` helper signature used by the gateway path)
  </read_first>
  <behavior>
    - Test 1 (manual UAT only — automated coverage in Task 3): `/model gpt-4` typed in the web UI client while a turn is in flight produces ONLY a Delta with AGENT_RUNNING_REJECT_MSG followed by Finished — does NOT mutate any AnyClient or AgentManager state.
    - Test 2 (manual UAT only): `/stop` typed in the web UI client while a turn is in flight DISPATCHES (does not produce the rejection message — bypass path).
    - Test 3 (compile + grep gates): the source contains the literal `if message.starts_with('/')` (slash interception entry) AND `is_bypass(&def.name)` (Pitfall 4 — never raw input) AND `AGENT_RUNNING_REJECT_MSG` (D-02 string never inlined).
  </behavior>
  <action>
    In `crates/iron_hermes_ui/src/server/ws.rs`, locate the WebSocket select! loop arm that currently builds `let (tx, rx) = mpsc::unbounded_channel::<ChatStreamEvent>();` at line ~209 and `let message = req.message;` at line ~213 and `let handle = tokio::spawn(async move { ... run_web_turn ... });` at line ~214. **Restructure** so the slash interception runs BEFORE the spawn. Steps: (1) Move the channel creation `let (tx, rx) = mpsc::unbounded_channel::<ChatStreamEvent>();` to BEFORE `let message = req.message;` so `tx` is in scope for the slash arm. (2) After `let message = req.message;` and the per-session-flag lookup `let running_flag = app_state.get_or_create_running_flag(&session_id);`, INSERT the slash interception block: `if message.starts_with('/') { match app_state.command_router.resolve(&message, &<platform>) { ResolveResult::Exact(def) | ResolveResult::PrefixMatch(def) => { if running_flag.load(std::sync::atomic::Ordering::SeqCst) && !ironhermes_core::commands::running_agent::is_bypass(&def.name) { let _ = tx.send(ChatStreamEvent::Delta { text: ironhermes_core::commands::running_agent::AGENT_RUNNING_REJECT_MSG.to_string() }); let _ = tx.send(ChatStreamEvent::Finished { total_tokens: 0 }); /* drain rx into the websocket sink using the existing forwarder; do NOT set in_flight_turn — Pitfall 7 */ continue; } /* dispatch via ironhermes_core::commands::handlers::dispatch with a properly-built CommandContext that supplies running_flag.clone() to ctx.agent_running; send dispatch output as Delta + Finished on tx; do NOT set in_flight_turn */ continue; }, ResolveResult::Ambiguous(_) | ResolveResult::NotFound => { /* fall through to plain-text path */ } } }`. The exact `<platform>` value must match the platform variant used elsewhere in this file (audit existing CommandContext construction in ws.rs/state.rs to find the convention; default to `ironhermes_core::types::Platform::Local` if no other web-specific variant exists — confirm by grep before committing). (3) AFTER the slash arm (in the plain-text path, BEFORE the existing `tokio::spawn` for `run_web_turn`), INSERT the D-06 plain-text guard check: `if running_flag.load(std::sync::atomic::Ordering::SeqCst) { let _ = tx.send(ChatStreamEvent::Delta { text: ironhermes_core::commands::running_agent::AGENT_RUNNING_REJECT_MSG.to_string() }); let _ = tx.send(ChatStreamEvent::Finished { total_tokens: 0 }); /* drain rx; do NOT set in_flight_turn */ continue; }`. (4) Leave the existing `let handle = tokio::spawn(async move { ... run_web_turn ... });` UNCHANGED — the RAII guard binding is inside `run_web_turn` per Task 1 (Pitfall 1: guard MUST live inside the async future, not the sync caller). (5) Add `use ironhermes_core::commands::{ResolveResult};` and `use ironhermes_core::commands::running_agent::{is_bypass, AGENT_RUNNING_REJECT_MSG};` to the top of ws.rs. CRITICAL: never inline the D-02 string (T-36.1-02); never call `is_bypass` on raw user input (Pitfall 4 / T-36.1-08); never set `in_flight_turn = Some(...)` from the slash arm (Pitfall 7). The slash rx must still be drained to the WebSocket — examine how the existing in_flight_turn rx is drained and apply the same forwarder for the slash rx (likely a short in-arm loop reading from rx and sending to the WebSocket sink, since slash output is bounded and immediate). If channel-restructure conflicts arise with the existing `in_flight_turn` rx ownership, prefer minimal restructure: create a SECOND short-lived (tx, rx) pair specifically for slash output, drain it inline before `continue`, and leave the original channel creation in place for the agent-turn path. Document the choice with a `// Phase 36.1 D-04/D-05:` comment marker.
  </action>
  <verify>
    <automated>cargo build -p iron_hermes_ui 2>&1 | tail -20 && cargo clippy -p iron_hermes_ui --lib -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "starts_with('/')" crates/iron_hermes_ui/src/server/ws.rs` returns at least 1 (slash interception present)
    - Source assertion: `grep -c 'is_bypass(&def.name)' crates/iron_hermes_ui/src/server/ws.rs` returns at least 1 (Pitfall 4: post-resolution canonical-name check, never raw input)
    - Source assertion: `grep -F 'AGENT_RUNNING_REJECT_MSG' crates/iron_hermes_ui/src/server/ws.rs` returns at least 2 hits (slash-reject + plain-text-reject) — D-02 string never inlined
    - Source assertion: `grep -c 'in_flight_turn = Some' crates/iron_hermes_ui/src/server/ws.rs` returns the SAME count as before this task (Pitfall 7: slash arm does NOT add new `in_flight_turn = Some(...)` sites). Capture pre-edit baseline with a one-line `git show HEAD:crates/iron_hermes_ui/src/server/ws.rs | grep -c 'in_flight_turn = Some'` and assert post-edit count is equal.
    - Source assertion: `grep -F 'use ironhermes_core::commands::running_agent::' crates/iron_hermes_ui/src/server/ws.rs` returns at least 1
    - Source assertion: `grep -F 'AGENT_RUNNING_REJECT_MSG: &str = "Agent is running.' crates/iron_hermes_ui/src/server/ws.rs` returns 0 (no inline-literal redefinition)
    - Build command: `cargo build -p iron_hermes_ui` exits 0
    - Lint command: `cargo clippy -p iron_hermes_ui --lib -- -D warnings` exits 0
    - Behavior assertion (manual UAT, documented for executor — automated coverage lives in Task 3): full Phase 36 gateway integration tests still pass after this task: `cargo test -p ironhermes-gateway --test running_agent_guard_tests` exits 0 (proves the extraction + web UI changes do not regress the gateway path).
  </acceptance_criteria>
  <done>
    ws.rs intercepts slash commands BEFORE `run_web_turn`. Slash dispatch result is delivered as `Delta + Finished` over the same WebSocket without setting `in_flight_turn`. Non-bypass slash commands and plain-text messages typed while a turn is in flight produce the D-02 rejection. Bypass-listed slash commands (`stop`, `new`, `status`, `queue`) dispatch normally. The bypass check uses `is_bypass(&def.name)` on the canonical resolved name (Pitfall 4). No inline literal of the D-02 string. Build and clippy clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration test suite — 6 GW-05-WEB behaviors mirroring Phase 36 gateway suite</name>
  <files>crates/iron_hermes_ui/tests/running_agent_guard_web_tests.rs</files>
  <read_first>
    - crates/ironhermes-gateway/tests/running_agent_guard_tests.rs (full file, ~614 lines — exact template to mirror, especially `mod helpers`, `build_test_session_store`, `RecordingPlatformAdapter`, `d02_error_message`, and the 11 `#[tokio::test]` functions)
    - crates/iron_hermes_ui/src/server/state.rs (final after Task 1 — `AppState`, `get_or_create_running_flag`, `run_web_turn` signature)
    - crates/iron_hermes_ui/src/server/ws.rs (final after Task 2 — to understand the slash dispatch path under test)
    - crates/iron_hermes_ui/src/protocol.rs (ChatStreamEvent enum variants for assertion)
    - crates/iron_hermes_ui/tests/ (list existing integration tests via `ls crates/iron_hermes_ui/tests/` to confirm directory exists and to find any existing helper patterns to reuse)
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-PATTERNS.md (section "tests/running_agent_guard_web_tests.rs (NEW)")
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-RESEARCH.md (Validation Architecture §Phase Requirements → Test Map — exact 6 test names: test_session_isolation, test_model_rejected_when_running, test_stop_bypasses_guard, test_guard_clears_on_error, test_alias_bypasses_guard, test_freetext_rejected_when_running)
  </read_first>
  <behavior>
    - `test_session_isolation` (GW-05-WEB-1): set session A's flag true; assert session B's flag remains false; assert the same Arc handle is returned by repeated `get_or_create_running_flag("A")` calls.
    - `test_model_rejected_when_running` (GW-05-WEB-2): with `app_state.get_or_create_running_flag("S").store(true, SeqCst)`, drive the slash dispatch path with `message = "/model gpt-4"`; assert collected events contain a `ChatStreamEvent::Delta { text: AGENT_RUNNING_REJECT_MSG }` AND no `AnyClient::rebuild` (or equivalent provider mutation) was observed.
    - `test_stop_bypasses_guard` (GW-05-WEB-3): with the flag true, drive `message = "/stop"`; assert the D-02 string does NOT appear in collected Delta events (the bypass path dispatches normally; the stop output may differ but must not be the rejection).
    - `test_guard_clears_on_error` (GW-05-WEB-4): construct a `RunningAgentGuard::new(flag.clone())` inside an inner scope that returns Err; assert `flag.load(SeqCst) == false` after the scope. Proves RAII Drop fires on error / `?` paths.
    - `test_alias_bypasses_guard` (GW-05-WEB-5): with the flag true, drive `message = "/reset"`; resolve to canonical `"new"` via `CommandRouter`; assert `is_bypass(&def.name) == true`; assert the D-02 string does NOT appear (Pitfall 4 mitigation verified end-to-end).
    - `test_freetext_rejected_when_running` (GW-05-WEB-6): with the flag true, drive `message = "hello world"` (no leading `/`); assert collected events contain `ChatStreamEvent::Delta { text: AGENT_RUNNING_REJECT_MSG }` AND `run_web_turn` was NOT invoked (verified by checking the agent runtime received no `run_turn` call, or by structural means — e.g. asserting `in_flight_turn` remained `None`).
  </behavior>
  <action>
    Create `crates/iron_hermes_ui/tests/running_agent_guard_web_tests.rs`. Open with a `mod helpers { ... }` module mirroring `crates/ironhermes-gateway/tests/running_agent_guard_tests.rs:20-154`: include `pub async fn build_test_app_state() -> AppState` (calls `AppState::init().await.expect("test AppState")` per RESEARCH §Test Pattern — Web UI, or constructs the minimal subset needed); include `pub fn set_running(app_state: &AppState, session_id: &str)` that calls `app_state.get_or_create_running_flag(session_id).store(true, std::sync::atomic::Ordering::SeqCst)`; include `pub fn collect_delta_texts(events: Vec<ChatStreamEvent>) -> Vec<String>` that filters `ChatStreamEvent::Delta { text } => Some(text)`; include `pub fn d02_error_message() -> &'static str { "Agent is running. Use /stop to interrupt or /queue to send after this turn." }` (byte-identical Phase 36 lock — assert against this and also import the public const from `ironhermes_core` to cross-check). Implement six `#[tokio::test]` functions covering the behaviors above. For tests 2/3/5/6 that drive the ws.rs slash dispatch path: the cleanest approach is to invoke the ws.rs slash-resolution + guard logic directly via a helper function — if ws.rs's slash interception is currently inline in the WebSocket select! arm, EXTRACT the slash-dispatch body into a `pub(crate) async fn handle_slash_web(app_state: &AppState, session_id: &str, message: &str, running_flag: Arc<AtomicBool>) -> Vec<ChatStreamEvent>` (or similar testable seam) and call it from both the select! arm and the tests. If extraction is impractical in this plan, fall back to a minimal end-to-end harness: open a WebSocket via `axum::Server` test infrastructure (check existing `crates/iron_hermes_ui/tests/websocket_lifecycle_parity.rs` for an established pattern), send a `ChatRequest` JSON frame, collect emitted `ChatStreamEvent` frames into a Vec, assert membership. Test 1 (`test_session_isolation`) and Test 4 (`test_guard_clears_on_error`) need no WebSocket harness — they operate directly on `AppState` + `Arc<AtomicBool>`. Test 4 uses the public `ironhermes_core::commands::running_agent::RunningAgentGuard`. Use `assert_eq!(helpers::d02_error_message(), ironhermes_core::commands::running_agent::AGENT_RUNNING_REJECT_MSG)` once at the top of the file to lock the helper against the canonical constant (drift-detection guard).
  </action>
  <verify>
    <automated>cargo test -p iron_hermes_ui --test running_agent_guard_web_tests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '^async fn test_session_isolation\|^fn test_session_isolation' crates/iron_hermes_ui/tests/running_agent_guard_web_tests.rs` returns 1
    - Source assertion: `grep -c '^async fn test_model_rejected_when_running\|^fn test_model_rejected_when_running' crates/iron_hermes_ui/tests/running_agent_guard_web_tests.rs` returns 1
    - Source assertion: `grep -c '^async fn test_stop_bypasses_guard\|^fn test_stop_bypasses_guard' crates/iron_hermes_ui/tests/running_agent_guard_web_tests.rs` returns 1
    - Source assertion: `grep -c '^async fn test_guard_clears_on_error\|^fn test_guard_clears_on_error' crates/iron_hermes_ui/tests/running_agent_guard_web_tests.rs` returns 1
    - Source assertion: `grep -c '^async fn test_alias_bypasses_guard\|^fn test_alias_bypasses_guard' crates/iron_hermes_ui/tests/running_agent_guard_web_tests.rs` returns 1
    - Source assertion: `grep -c '^async fn test_freetext_rejected_when_running\|^fn test_freetext_rejected_when_running' crates/iron_hermes_ui/tests/running_agent_guard_web_tests.rs` returns 1
    - Source assertion: `grep -c 'fn d02_error_message' crates/iron_hermes_ui/tests/running_agent_guard_web_tests.rs` returns 1 (helper present, mirrors gateway pattern)
    - Source assertion: `grep -F 'ironhermes_core::commands::running_agent::AGENT_RUNNING_REJECT_MSG' crates/iron_hermes_ui/tests/running_agent_guard_web_tests.rs` returns at least 1 hit (canonical-constant cross-check assertion present)
    - Test command: `cargo test -p iron_hermes_ui --test running_agent_guard_web_tests` exits 0 with all 6 tests passing
    - Behavior assertion: `cargo test -p ironhermes-gateway --test running_agent_guard_tests` ALSO still exits 0 (no cross-surface regression).
  </acceptance_criteria>
  <done>
    `tests/running_agent_guard_web_tests.rs` exists with 6 `#[tokio::test]` functions covering GW-05-WEB-1 through GW-05-WEB-6. All 6 pass. `helpers::d02_error_message()` is byte-identical to and cross-asserts against `ironhermes_core::commands::running_agent::AGENT_RUNNING_REJECT_MSG`. The Phase 36 gateway suite still passes (no cross-surface regression).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket client → server (`ws.rs`) | Untrusted user input (slash commands and free-text) crosses here; the running-agent guard is the first authorization gate after deserialization |
| Per-session state isolation | `running_agents` map keyed by `session_id`; cross-session mutation would constitute Information Disclosure |
| `run_web_turn` future lifetime | RAII guard MUST be tied to the future's lifetime, not the sync caller's stack frame (Pitfall 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.1-05 | Tampering | Concurrent slash command races plain message on same session, both mutate AgentManager state | mitigate | `RunningAgentGuard` with SeqCst stores at run_web_turn entry blocks second turn; ws.rs plain-text guard rejects second message with D-02 BEFORE reaching run_web_turn |
| T-36.1-06 | Information Disclosure | Wrong session's `Arc<AtomicBool>` is reused — session A's running flag blocks session B's typing | mitigate | Per-session `HashMap<String, Arc<AtomicBool>>` keyed by `session_id`; `test_session_isolation` integration test enumerates the invariant |
| T-36.1-07 | EoP | Bypass-check bypass via raw input — `/RESET` or `//new` reaches `is_bypass` and bypasses the guard despite resolving to a non-bypass command | mitigate | `is_bypass(&def.name)` ALWAYS called on the post-resolution canonical name from `CommandRouter::resolve`; `test_alias_bypasses_guard` covers `/reset → "new"` end-to-end. Pitfall 4 cited by name in Task 2 read_first. |
| T-36.1-08 | DoS | RunningAgentGuard not bound INSIDE the async block — sync caller returns, Drop fires, flag clears immediately, no protection | mitigate | Pitfall 1 cited by name in Task 1 and Task 2 read_first; guard is constructed inside `run_web_turn` body (an `async fn`) so Drop fires on future completion; integration test `test_guard_clears_on_error` exercises the Err path |
| T-36.1-09 | Information Disclosure | D-02 rejection message echoes user-controlled content (e.g. mirrors `/model gpt-4` back into the WebSocket) | mitigate | D-02 message is a CONSTANT `AGENT_RUNNING_REJECT_MSG` from `ironhermes-core` — never interpolates `message`, `def.name`, or any user-supplied string; grep gate in acceptance criteria enforces zero inline-literal redefinitions |
| T-36.1-10 | DoS | `running_agents` map grows unbounded as sessions are created and never cleaned up | accept | Tradeoff documented in RESEARCH §Pitfall 5; matches the `nudge_turns` precedent at state.rs:38; single-user web UI deployment model bounds session count |
| T-36.1-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies in this plan — RESEARCH "Standard Stack" §confirms all types are `std::*` and `ironhermes-core` (existing workspace dep) |
</threat_model>

<verification>
1. `cargo test -p iron_hermes_ui --lib server::state` — Task 1 unit tests pass.
2. `cargo test -p iron_hermes_ui --test running_agent_guard_web_tests` — Task 3 integration suite passes (6 tests).
3. `cargo test -p ironhermes-gateway --test running_agent_guard_tests` — Phase 36 suite STILL passes (no regression after Plan 01 extraction + this plan).
4. `cargo build -p iron_hermes_ui` — clean build.
5. `cargo clippy -p iron_hermes_ui --lib -- -D warnings` — no violations.
6. Grep regression gates (enforced by acceptance criteria):
   - `running_agents` field present with exact type signature.
   - `is_bypass(&def.name)` present in ws.rs (Pitfall 4 enforcement).
   - Zero inline literal of D-02 string in ws.rs.
   - `in_flight_turn = Some` count unchanged (Pitfall 7 — slash arm does NOT add new sites).
</verification>

<success_criteria>
- Web UI sessions each have an independent `Arc<AtomicBool>` running flag — `test_session_isolation` passes.
- Slash commands intercepted in ws.rs before reaching `run_web_turn` (D-04).
- Slash dispatch output delivered as `ChatStreamEvent::Delta + Finished` without new protocol variant (D-05).
- Non-bypass slash commands and plain-text messages are rejected with the exact D-02 string when the agent is running; bypass list (stop/new/status/queue) dispatches normally (D-06, D-07).
- `RunningAgentGuard` RAII binds at the top of `run_web_turn` body (inside the async fn — Pitfall 1).
- All 6 GW-05-WEB integration tests pass; Phase 36 gateway suite continues to pass unchanged.
</success_criteria>

<output>
Create `.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-02-SUMMARY.md` when done.
</output>
