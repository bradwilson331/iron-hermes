---
phase: 36.1-running-agent-guard-parity-web-ui-tui-inserted
plan: 03
type: execute
wave: 2
depends_on:
  - 36.1-01
files_modified:
  - crates/ironhermes-cli/src/tui_rata/app.rs
  - crates/ironhermes-cli/src/tui_rata/commands.rs
  - crates/ironhermes-cli/src/tui_rata/event_loop.rs
  - crates/ironhermes-cli/tests/running_agent_guard_tui_tests.rs
autonomous: true
requirements:
  - GW-05-TUI
tags:
  - rust
  - tui
  - tui-rata
  - slash-commands
  - atomic-bool
  - raii

must_haves:
  truths:
    - "App struct has a persistent agent_running: Arc<AtomicBool> field that survives multiple build_command_context invocations on the same App instance (D-08)"
    - "build_command_context at commands.rs line ~537 uses `app.agent_running.clone()` — the previous `Arc::new(AtomicBool::new(app.pending_rx.is_some()))` snapshot is COMPLETELY REMOVED (D-08, Pitfall 4 from RESEARCH)"
    - "After CommandRouter::resolve() returns ResolveResult::Exact|PrefixMatch(def), commands.rs checks `app.agent_running.load(SeqCst) && !is_bypass(&def.name)` BEFORE invoke_handler dispatch; on rejection returns SlashOutcome::Handled(AGENT_RUNNING_REJECT_MSG.to_string()) (D-10, Pitfall 4)"
    - "RunningAgentGuard::new(app.agent_running.clone()) is constructed INSIDE the `tokio::spawn(async move { ... })` block of spawn_turn at event_loop.rs ~722-742, NOT in the sync wrapper function body (Pitfall 1 — guard MUST live inside the async future)"
    - "/reset typed in the TUI resolves to canonical 'new' via CommandRouter and bypasses the guard via is_bypass(&def.name); typing the raw string `new` against is_bypass would also bypass — the resolution layer is what makes alias→canonical mapping work"
    - "/model typed in the TUI while agent_running is true returns SlashOutcome::Handled with the exact AGENT_RUNNING_REJECT_MSG and does NOT call handle_subsystem_mutator or rebuild AnyClient"
    - "Both store sites in the guard remain Ordering::SeqCst — the TUI inherits this from Plan 01's extracted module; no relaxed ordering anywhere"
  artifacts:
    - path: "crates/ironhermes-cli/src/tui_rata/app.rs"
      provides: "App.agent_running: Arc<AtomicBool> field + initializer"
      contains: "pub agent_running: Arc<AtomicBool>"
    - path: "crates/ironhermes-cli/src/tui_rata/commands.rs"
      provides: "build_command_context snapshot replacement + bypass-list check after CommandRouter::resolve"
      contains: "let agent_running = app.agent_running.clone();"
    - path: "crates/ironhermes-cli/src/tui_rata/event_loop.rs"
      provides: "RunningAgentGuard binding inside spawn_turn's tokio::spawn(async move { ... }) block"
      contains: "RunningAgentGuard::new"
    - path: "crates/ironhermes-cli/tests/running_agent_guard_tui_tests.rs"
      provides: "TUI integration test suite — 5 behaviors: live Arc invariant, RAII clears on turn end, /model rejected, /stop bypasses, /reset alias bypasses"
      contains: "fn test_agent_running_is_live_arc"
  key_links:
    - from: "crates/ironhermes-cli/src/tui_rata/commands.rs (line ~537)"
      to: "crates/ironhermes-cli/src/tui_rata/app.rs (agent_running field)"
      via: "app.agent_running.clone() in build_command_context"
      pattern: "app\\.agent_running\\.clone\\(\\)"
    - from: "crates/ironhermes-cli/src/tui_rata/event_loop.rs (spawn_turn at line ~722)"
      to: "ironhermes_core::commands::running_agent::RunningAgentGuard"
      via: "RunningAgentGuard::new(agent_running) inside tokio::spawn async block (Pitfall 1)"
      pattern: "RunningAgentGuard::new"
    - from: "crates/ironhermes-cli/src/tui_rata/commands.rs (after CommandRouter::resolve)"
      to: "ironhermes_core::commands::running_agent::is_bypass"
      via: "is_bypass(&def.name) — Pitfall 4 mitigation"
      pattern: "is_bypass\\(&def\\.name\\)"
---

<objective>
Wire the Phase 36.1 running-agent guard into the tui_rata TUI: add a persistent `agent_running: Arc<AtomicBool>` field to `App`, replace the `Arc::new(AtomicBool::new(app.pending_rx.is_some()))` snapshot at `commands.rs:537` with the live handle, add the bypass-list check after `CommandRouter::resolve` returns, and bind `RunningAgentGuard` INSIDE the `tokio::spawn(async move { ... })` block of `spawn_turn` (Pitfall 1 — never in the sync wrapper).

Purpose: Implements D-08 through D-10 — replaces the `pending_rx.is_some()` snapshot anti-pattern (Pitfall 4) and brings the TUI to feature parity with the gateway's guard discipline. After this plan, `cmd_stop` invoked in the TUI reads the live flag controlled by the RAII guard, `/model` is correctly rejected mid-turn, and `/reset` (alias → `new`) correctly bypasses.

Output: `App.agent_running` field added; commands.rs build_command_context line replaced; commands.rs gains a bypass check after `CommandRouter::resolve()` returning `SlashOutcome::Handled(AGENT_RUNNING_REJECT_MSG.to_string())`; event_loop.rs `spawn_turn` clones `app.agent_running` before `tokio::spawn` and binds `RunningAgentGuard::new(agent_running)` as the first statement inside the spawned async block; new integration test file covers the 5 GW-05-TUI behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-CONTEXT.md
@.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-RESEARCH.md
@.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-PATTERNS.md
@.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-01-PLAN.md

@crates/ironhermes-cli/src/tui_rata/app.rs
@crates/ironhermes-cli/src/tui_rata/commands.rs
@crates/ironhermes-cli/src/tui_rata/event_loop.rs
@crates/ironhermes-gateway/src/handler.rs
@crates/ironhermes-gateway/tests/running_agent_guard_tests.rs

<interfaces>
<!-- Shared running-agent machinery from Plan 01. -->

From crates/ironhermes-core/src/commands/running_agent.rs (created by Plan 01):
- `pub const AGENT_RUNNING_REJECT_MSG: &str;`
- `pub struct RunningAgentGuard(Arc<AtomicBool>);` — RAII guard
- `pub fn is_bypass(name: &str) -> bool;` — bypass list: stop, new, status, queue

From crates/ironhermes-cli/src/tui_rata/app.rs (current — lines 200-210, the existing AtomicBool field cluster):
- `pub yolo_enabled: Arc<AtomicBool>`
- `pub verbose_enabled: Arc<AtomicBool>`
- `pub statusbar_enabled: Arc<AtomicBool>`
- `pub debug_enabled: Arc<AtomicBool>`
- `pub fast_enabled: Arc<AtomicBool>`
- `pub pending_rx: Option<UnboundedReceiver<StreamEvent>>` (line 171 region — KEEP this field, just stop using `.is_some()` as a running proxy)

From crates/ironhermes-cli/src/tui_rata/commands.rs (current):
- Line 272-301: `pub enum SlashOutcome { Handled(String), Silent, Quit, ResetTerminal, McpReload, ClearSession(String), SkillsReload(String), SkillActivated { name, body }, Unknown { input, hint }, Error(String) }`
- Line 317: `pub async fn dispatch_slash(app: &mut App, input: &str) -> SlashOutcome`
- Line 319: `match app.command_router.resolve(input, &platform)`
- Line 320: `ResolveResult::Exact(def) | ResolveResult::PrefixMatch(def) => { ... build_command_context(app); invoke_handler(def.name, &ctx, &app.command_router, &args_vec).await; ... }`
- Line 536-540: `fn build_command_context(app: &App) -> CommandContext { ... let agent_running = Arc::new(AtomicBool::new(app.pending_rx.is_some())); ... }`

From crates/ironhermes-cli/src/tui_rata/event_loop.rs (current — lines 722-797):
- Line 722: `fn spawn_turn(app: &App, tx: UnboundedSender<StreamEvent>, cancel: CancellationToken) {`
- Line 723: `let runtime = app.agent_runtime.clone();`
- Line 742: `tokio::spawn(async move {`
- Line 743: `let _ = tx.send(StreamEvent::Started);`
- Line 797: `let result = runtime.run_turn(request).await;` (the turn body — guard MUST be live across this await)
- Line 853: existing test asserts `non_comment.contains("run_turn(")` — invariant must not regress.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: App.agent_running field + build_command_context snapshot replacement + bypass-list check after resolve</name>
  <files>crates/ironhermes-cli/src/tui_rata/app.rs, crates/ironhermes-cli/src/tui_rata/commands.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/tui_rata/app.rs (lines 160-240 — App struct, the AtomicBool field cluster around lines 202-209, the initializer blocks in `App::new` / `App::from_deps`)
    - crates/ironhermes-cli/src/tui_rata/commands.rs (lines 1-50 for imports, lines 272-401 for SlashOutcome + dispatch_slash structure, lines 530-560 for build_command_context — the exact line 537 to be replaced)
    - crates/ironhermes-gateway/src/handler.rs (lines 486-498 — gateway's slash guard check; structurally mirrored in commands.rs)
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-PATTERNS.md (sections "app.rs (MODIFY)" + "commands.rs (MODIFY)")
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-RESEARCH.md (Pitfall 2: post-resolution canonical-name bypass check; Pitfall 4: fresh-Arc snapshot is a correctness bug — the snapshot Arc disconnects from any future guard)
    - Plan 36.1-01 PLAN — confirms `ironhermes_core::commands::running_agent::{RunningAgentGuard, is_bypass, AGENT_RUNNING_REJECT_MSG}` is publicly available
  </read_first>
  <behavior>
    - Test 1 (`agent_running_field_initialized_false`): `App::new(...)` (or whatever the constructor is) produces an instance where `app.agent_running.load(SeqCst) == false`.
    - Test 2 (`agent_running_field_is_persistent`): two consecutive `build_command_context(&app)` calls return contexts whose `agent_running` Arc handles point to the SAME underlying AtomicBool (verified by setting via the first context's handle and observing via the second).
    - Test 3 (`build_command_context_uses_live_handle_not_snapshot`): set `app.agent_running.store(true, SeqCst)`; call `build_command_context(&app)`; assert `ctx.agent_running.load(SeqCst) == true`. (The OLD `Arc::new(AtomicBool::new(app.pending_rx.is_some()))` snapshot would return false when `pending_rx` is None — this test proves the replacement.)
    - Test 4 (`model_rejected_when_running`): with `app.agent_running.store(true, SeqCst)`, invoke `dispatch_slash(&mut app, "/model gpt-4").await`; assert the result is `SlashOutcome::Handled(s)` where `s == AGENT_RUNNING_REJECT_MSG` AND `handle_subsystem_mutator` was NOT called (verified structurally — e.g. AnyClient model not changed, or by adding a counter in a test-only wrapper). (GW-05-TUI-3)
    - Test 5 (`stop_bypasses_when_running`): with `app.agent_running.store(true, SeqCst)`, invoke `dispatch_slash(&mut app, "/stop").await`; assert the result is NOT `SlashOutcome::Handled(AGENT_RUNNING_REJECT_MSG.to_string())` — the bypass path proceeds. (GW-05-TUI-4)
    - Test 6 (`reset_alias_bypasses_when_running`): with `app.agent_running.store(true, SeqCst)`, invoke `dispatch_slash(&mut app, "/reset").await`; the router resolves `/reset` → canonical `"new"`; `is_bypass("new")` returns true; assert the result is NOT the rejection. (GW-05-TUI-5 + Pitfall 2/4 mitigation verified end-to-end.)
  </behavior>
  <action>
    Step A — app.rs: add `pub agent_running: Arc<AtomicBool>,` to the `App` struct in the SAME field cluster as `yolo_enabled`/`verbose_enabled`/`statusbar_enabled`/`debug_enabled`/`fast_enabled` (lines 202-209). Doc-comment: "Phase 36.1 (GW-05-TUI, D-08): persistent per-session running flag. Set to true by `RunningAgentGuard::new()` at spawn_turn entry; cleared by Drop on all turn exit paths (Ok/Err/?/panic/cancel). Replaces the `pending_rx.is_some()` snapshot anti-pattern at commands.rs:537 (Pitfall 4). DO NOT remove or repurpose pending_rx — it still owns the streaming receive role." In `App::new` / `App::from_deps` (whichever constructor(s) initialize the existing AtomicBool fields), add `agent_running: Arc::new(AtomicBool::new(false)),` in the same `Self { ... }` block. The existing `use std::sync::Arc;` and `use std::sync::atomic::AtomicBool;` imports already cover the new field — no new imports needed (confirm by grep).

    Step B — commands.rs build_command_context replacement: locate line ~537 currently reading `let agent_running = Arc::new(AtomicBool::new(app.pending_rx.is_some()));`. REPLACE with `let agent_running = app.agent_running.clone();`. Leave the surrounding `CommandContext::new(...)` construction and the `ctx.with_*(...)` chain intact. Add a `// Phase 36.1 (D-08, Pitfall 4): live handle replaces the pending_rx.is_some() snapshot.` comment marker immediately above the replacement line.

    Step C — commands.rs bypass check: inside `dispatch_slash` (line 317+), in the `ResolveResult::Exact(def) | ResolveResult::PrefixMatch(def)` arm at line 320, INSERT the bypass-list guard check AFTER the existing `let ctx = build_command_context(app);` (line 332) and BEFORE the existing `invoke_handler(def.name, &ctx, ...)` call (line 333). The inserted block: `if app.agent_running.load(std::sync::atomic::Ordering::SeqCst) && !ironhermes_core::commands::running_agent::is_bypass(def.name) { return ironhermes_core::commands::running_agent::AGENT_RUNNING_REJECT_MSG.to_string().into(); }` — where `.into()` produces `SlashOutcome::Handled(String)` via a `From<String> for SlashOutcome` impl if it exists, OR use the explicit form `return SlashOutcome::Handled(ironhermes_core::commands::running_agent::AGENT_RUNNING_REJECT_MSG.to_string());`. Use the explicit form unless a `From<String>` impl already exists (verify by grep — `grep "impl From<String> for SlashOutcome" crates/ironhermes-cli/src/tui_rata/commands.rs`). Add a `// Phase 36.1 (D-10, Pitfall 4): bypass check on POST-resolution canonical def.name — never raw user input. /reset resolves to "new" via the CommandRouter and correctly bypasses.` comment marker. Add `use ironhermes_core::commands::running_agent::{is_bypass, AGENT_RUNNING_REJECT_MSG};` to the imports at the top of commands.rs IF the explicit-form usage is preferred over fully-qualified paths.

    Step D — `#[cfg(test)] mod tests`: add an inline test module to commands.rs OR add the tests to the new test file in Task 3 (Task 3 is the chosen location — see Task 3's `<behavior>`). Step A through C remain pure-implementation in this task; Task 3 owns ALL test code.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-cli 2>&1 | tail -20 && cargo clippy -p ironhermes-cli --lib -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '^\s*pub agent_running: Arc<AtomicBool>' crates/ironhermes-cli/src/tui_rata/app.rs` returns 1
    - Source assertion: `grep -c 'agent_running: Arc::new(AtomicBool::new(false))' crates/ironhermes-cli/src/tui_rata/app.rs` returns at least 1 (initializer in App::new / App::from_deps)
    - Source assertion (Pitfall 4 enforcement — snapshot removed): `grep -c 'AtomicBool::new(app.pending_rx.is_some())' crates/ironhermes-cli/src/tui_rata/commands.rs` returns 0
    - Source assertion: `grep -c 'let agent_running = app.agent_running.clone();' crates/ironhermes-cli/src/tui_rata/commands.rs` returns 1
    - Source assertion (Pitfall 4 enforcement — canonical-name check): `grep -c 'is_bypass(def.name)\|is_bypass(&def.name)' crates/ironhermes-cli/src/tui_rata/commands.rs` returns at least 1
    - Source assertion: `grep -F 'AGENT_RUNNING_REJECT_MSG' crates/ironhermes-cli/src/tui_rata/commands.rs` returns at least 1 (rejection delivered via const, no inline literal)
    - Source assertion: `grep -F 'Agent is running. Use /stop to interrupt' crates/ironhermes-cli/src/tui_rata/commands.rs` returns 0 (no inline-literal redefinition)
    - Source assertion (pending_rx preservation per D-08): `grep -c 'pub pending_rx:' crates/ironhermes-cli/src/tui_rata/app.rs` returns 1 (field still present — only its `.is_some()` proxy use is gone)
    - Build command: `cargo build -p ironhermes-cli` exits 0
    - Lint command: `cargo clippy -p ironhermes-cli --lib -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    `App.agent_running` is a persistent Arc<AtomicBool> initialized to false. `build_command_context` line ~537 reads `let agent_running = app.agent_running.clone();` — the snapshot is GONE. `dispatch_slash` rejects non-bypass slash commands with `SlashOutcome::Handled(AGENT_RUNNING_REJECT_MSG.to_string())` when `app.agent_running` is true, using `is_bypass(def.name)` on the canonical resolved name (Pitfall 4). `pending_rx` field is preserved. Build + clippy clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: event_loop.rs — bind RunningAgentGuard INSIDE the tokio::spawn async block of spawn_turn (Pitfall 1)</name>
  <files>crates/ironhermes-cli/src/tui_rata/event_loop.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/tui_rata/event_loop.rs (lines 700-810 — the `spawn_turn` function definition at line 722, the existing clone block lines 723-741, the `tokio::spawn(async move { ... })` at line 742, the `runtime.run_turn(request).await` at line 797)
    - crates/ironhermes-cli/src/tui_rata/event_loop.rs (lines 840-870 — the existing test `inv_25_1_gap8_browser_tools_wired_in_rata_chat` and the source-assertion invariant `non_comment.contains("run_turn(")` that must continue to pass)
    - crates/ironhermes-gateway/src/handler.rs (lines 884-889 — the gateway's RAII guard bind inside `run_agent` — exact analog pattern; note that gateway's bind is at the top of an `async fn`, NOT before a `tokio::spawn` — the TUI's structural difference is that `spawn_turn` is SYNC and creates the spawn — hence Pitfall 1 risk)
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-PATTERNS.md (section "event_loop.rs (MODIFY)" — the explicit warning that binding the guard outside `tokio::spawn` is Pitfall 1)
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-RESEARCH.md (Pitfall 1: full description and warning signs — cite by name in commit message and code comment)
  </read_first>
  <behavior>
    - Test 1 (`spawn_turn_clones_agent_running_before_spawn`): structural source assertion — `let agent_running = app.agent_running.clone();` appears in `spawn_turn` BEFORE the `tokio::spawn(async move { ... })` line (clone must happen on the sync side to be moved into the future).
    - Test 2 (`guard_bound_inside_async_block`): structural source assertion — the FIRST non-comment, non-empty statement inside the `tokio::spawn(async move { ... })` block is `let _agent_guard = ironhermes_core::commands::running_agent::RunningAgentGuard::new(agent_running);` (or equivalent — assert via grep that `RunningAgentGuard::new` appears inside the async block, NOT in spawn_turn's sync body before the spawn call). (Pitfall 1 mitigation.)
    - Test 3 (`guard_runtime_RAII_clears_on_turn_end`): standalone unit test in the new test file (Task 3) — creates an Arc<AtomicBool>, spawns a tokio task that binds `RunningAgentGuard::new(flag.clone())` then awaits a short delay then returns Ok; joins the spawned task; asserts the flag is false after join. Proves Drop fires when the future completes.
    - Test 4 (`spawn_turn_invariant_run_turn_still_present`): existing test `inv_25_1_gap8_browser_tools_wired_in_rata_chat` at line ~840 must continue to pass — its `non_comment.contains("run_turn(")` invariant is not regressed by inserting the guard binding.
  </behavior>
  <action>
    In `crates/ironhermes-cli/src/tui_rata/event_loop.rs`, modify `fn spawn_turn` (line 722). Step 1: in the sync body (after `let runtime = app.agent_runtime.clone();` at line 723 and alongside the other existing clones lines 724-741), ADD `let agent_running = app.agent_running.clone();` — this MUST be in the sync body so the Arc can be moved into the async block. Step 2: inside the `tokio::spawn(async move {` block at line 742, make the FIRST statement (before `let _ = tx.send(StreamEvent::Started);` at line 743): `let _agent_guard = ironhermes_core::commands::running_agent::RunningAgentGuard::new(agent_running);`. Add the comment: `// Phase 36.1 (GW-05-TUI, D-09, Pitfall 1): RAII guard MUST be bound inside the async block so Drop fires when the future completes, NOT when spawn_turn returns synchronously. Binding this outside the tokio::spawn would set the flag true then immediately clear it before the turn runs.` Step 3: confirm `let result = runtime.run_turn(request).await;` at line 797 remains unchanged — the guard MUST cover this `.await`. Step 4: verify the existing `inv_25_1_gap8_browser_tools_wired_in_rata_chat` test (line ~840) still passes — it greps for `run_turn(` which the guard insertion does not affect. Step 5: if `ironhermes_core::commands::running_agent::RunningAgentGuard` import would be more readable as a top-of-file use, add `use ironhermes_core::commands::running_agent::RunningAgentGuard;` to imports and shorten the bind to `let _agent_guard = RunningAgentGuard::new(agent_running);` — this is preferred for readability and helps the grep gate match the canonical pattern.

    Do NOT bind the guard in the sync wrapper. Do NOT use `Ordering::Relaxed` anywhere. Do NOT move `let runtime = app.agent_runtime.clone();` — its position in the sync body is the existing pattern; just ADD the `agent_running` clone alongside it.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-cli 2>&1 | tail -20 && cargo clippy -p ironhermes-cli --lib -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `awk '/fn spawn_turn/,/^}$/' crates/ironhermes-cli/src/tui_rata/event_loop.rs | grep -c 'let agent_running = app.agent_running.clone();'` returns 1 (clone present in spawn_turn body)
    - Source assertion: `awk '/fn spawn_turn/,/^}$/' crates/ironhermes-cli/src/tui_rata/event_loop.rs | grep -c 'RunningAgentGuard::new(agent_running)'` returns 1 (guard binding present)
    - Source assertion (Pitfall 1 enforcement — guard appears AFTER `tokio::spawn`): `awk '/fn spawn_turn/,/^}$/' crates/ironhermes-cli/src/tui_rata/event_loop.rs | awk '/tokio::spawn/{found=1} found && /RunningAgentGuard::new/{print "OK"; exit}'` outputs "OK" (the RunningAgentGuard::new line is encountered AFTER the tokio::spawn line in source order — proves the binding is inside the async block, not before it)
    - Source assertion (Pitfall 1 negative — guard does NOT appear BEFORE `tokio::spawn` in spawn_turn): `awk '/fn spawn_turn/,/tokio::spawn/' crates/ironhermes-cli/src/tui_rata/event_loop.rs | grep -v '^\s*//' | grep -c 'RunningAgentGuard::new'` returns 0
    - Source assertion: `grep -c 'Ordering::Relaxed\|Ordering::Acquire\|Ordering::Release' crates/ironhermes-cli/src/tui_rata/event_loop.rs` returns 0 in the spawn_turn region (the file may have other uses elsewhere; if so, restrict the grep range to spawn_turn)
    - Test command: `cargo test -p ironhermes-cli --lib tui_rata::event_loop` passes (existing tests including `inv_25_1_gap8_browser_tools_wired_in_rata_chat` still green)
    - Build command: `cargo build -p ironhermes-cli` exits 0
    - Lint command: `cargo clippy -p ironhermes-cli --lib -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    `spawn_turn` clones `app.agent_running` in its sync body and binds `RunningAgentGuard::new(agent_running)` as the first statement inside the `tokio::spawn(async move { ... })` block. The guard covers the `runtime.run_turn(request).await` call. The existing run_turn invariant test still passes. Build + clippy clean. Pitfall 1 mitigation enforced by structural grep gate.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration test suite — 5 GW-05-TUI behaviors mirroring the gateway pattern</name>
  <files>crates/ironhermes-cli/tests/running_agent_guard_tui_tests.rs</files>
  <read_first>
    - crates/ironhermes-gateway/tests/running_agent_guard_tests.rs (full file — the pattern to mirror with TUI adaptations)
    - crates/ironhermes-cli/src/tui_rata/app.rs (final after Task 1 — App struct + agent_running field + constructors)
    - crates/ironhermes-cli/src/tui_rata/commands.rs (final after Task 1 — dispatch_slash signature + SlashOutcome variants)
    - crates/ironhermes-cli/src/tui_rata/event_loop.rs (final after Task 2 — spawn_turn structure)
    - crates/ironhermes-cli/tests/ (list existing test files — confirm directory exists; look for shared helpers)
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-PATTERNS.md (section "tests/running_agent_guard_tui_tests.rs (NEW)" — the 5 test templates)
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-RESEARCH.md (Validation Architecture §Test Pattern — TUI: unit tests, no full TUI runtime)
  </read_first>
  <behavior>
    - `test_agent_running_is_live_arc` (GW-05-TUI-1): builds an `App` (via test helper or minimal constructor), captures `app.agent_running.clone()`, then `app.agent_running.store(true, SeqCst)`; assert the captured Arc handle loads true (proves they share storage).
    - `test_guard_clears_on_turn_end` (GW-05-TUI-2): create `Arc<AtomicBool>` (false); spawn a `tokio::task` that binds `RunningAgentGuard::new(flag.clone())` and awaits a short sleep; join the task; assert the flag loads false after join. Proves Drop fires on future completion.
    - `test_model_rejected_when_running` (GW-05-TUI-3): with `app.agent_running.store(true, SeqCst)`, invoke `dispatch_slash(&mut app, "/model gpt-4").await`; assert `matches!(result, SlashOutcome::Handled(ref s) if s == AGENT_RUNNING_REJECT_MSG)`.
    - `test_stop_bypasses_guard` (GW-05-TUI-4): with `app.agent_running.store(true, SeqCst)`, invoke `dispatch_slash(&mut app, "/stop").await`; assert the result is NOT `SlashOutcome::Handled(s)` where s == AGENT_RUNNING_REJECT_MSG (proves bypass path took over).
    - `test_alias_reset_bypasses_guard` (GW-05-TUI-5): direct router test — build the registry, call `router.resolve("/reset", &Platform::Local)`, assert it returns `ResolveResult::Exact(def)` or `PrefixMatch(def)` with `def.name == "new"` AND `is_bypass(&def.name) == true`. Then end-to-end: with `app.agent_running.store(true, SeqCst)`, `dispatch_slash(&mut app, "/reset").await` returns a result that is NOT the AGENT_RUNNING_REJECT_MSG rejection (proves Pitfall 4 mitigation: raw `"reset"` would not match `is_bypass`, but the resolver hands us `"new"` which does).
  </behavior>
  <action>
    Create `crates/ironhermes-cli/tests/running_agent_guard_tui_tests.rs`. Start with a `mod helpers` module that builds a minimal `App` instance for testing — if `App::new`/`App::from_deps` requires heavy dependencies (agent_runtime, command_router, etc.) that make construction in test contexts difficult, expose a `pub(crate) fn build_test_app() -> App` in `tui_rata/app.rs` behind `#[cfg(test)]` or `#[cfg(any(test, feature = "testing"))]` that fabricates a stub `App` with only the fields needed: `agent_running`, `command_router` (built via `ironhermes_core::commands::registry::build_registry()`), `pending_rx`, and any other field touched by `dispatch_slash`. Add `pub fn d02_error_message() -> &'static str { "Agent is running. Use /stop to interrupt or /queue to send after this turn." }` and `assert_eq!(d02_error_message(), ironhermes_core::commands::running_agent::AGENT_RUNNING_REJECT_MSG)` once in a `#[test] fn d02_constant_matches_helper()` to lock the helper against the canonical const. Implement the five `#[tokio::test]` (or `#[test]` for the pure-router test) functions above. For tests 3, 4, and 5 driving `dispatch_slash`: if `App` construction is impractical, fall back to testing the guard logic in isolation — directly exercise `app.command_router.resolve(input, &Platform::Local)` then manually replicate the `if app.agent_running.load(SeqCst) && !is_bypass(&def.name)` check and assert the result. This is acceptable because the structural source-grep gates in Task 1 already prove the same check is wired into `dispatch_slash`. Prefer end-to-end via `dispatch_slash` if test-App construction is feasible — note feasibility in the test file with a `// Test approach: ...` comment.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cli --test running_agent_guard_tui_tests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '^fn test_agent_running_is_live_arc\|^async fn test_agent_running_is_live_arc' crates/ironhermes-cli/tests/running_agent_guard_tui_tests.rs` returns 1
    - Source assertion: `grep -c '^fn test_guard_clears_on_turn_end\|^async fn test_guard_clears_on_turn_end' crates/ironhermes-cli/tests/running_agent_guard_tui_tests.rs` returns 1
    - Source assertion: `grep -c '^fn test_model_rejected_when_running\|^async fn test_model_rejected_when_running' crates/ironhermes-cli/tests/running_agent_guard_tui_tests.rs` returns 1
    - Source assertion: `grep -c '^fn test_stop_bypasses_guard\|^async fn test_stop_bypasses_guard' crates/ironhermes-cli/tests/running_agent_guard_tui_tests.rs` returns 1
    - Source assertion: `grep -c '^fn test_alias_reset_bypasses_guard\|^async fn test_alias_reset_bypasses_guard' crates/ironhermes-cli/tests/running_agent_guard_tui_tests.rs` returns 1
    - Source assertion: `grep -F 'ironhermes_core::commands::running_agent::AGENT_RUNNING_REJECT_MSG' crates/ironhermes-cli/tests/running_agent_guard_tui_tests.rs` returns at least 1 (canonical-constant cross-check assertion present)
    - Source assertion: `grep -F 'def.name == "new"' crates/ironhermes-cli/tests/running_agent_guard_tui_tests.rs` returns at least 1 (Pitfall 4 verification — alias→canonical resolution asserted)
    - Test command: `cargo test -p ironhermes-cli --test running_agent_guard_tui_tests` exits 0 with all 5 tests passing (plus the `d02_constant_matches_helper` sanity test)
    - Behavior assertion (cross-surface): `cargo test -p ironhermes-gateway --test running_agent_guard_tests` ALSO still exits 0 (Phase 36 suite not regressed) AND `cargo test -p iron_hermes_ui --test running_agent_guard_web_tests` exits 0 (Plan 02's suite still green if it has already been merged).
  </acceptance_criteria>
  <done>
    `tests/running_agent_guard_tui_tests.rs` exists with 5 GW-05-TUI behavior tests + the d02-constant sanity test. All pass. `helpers::d02_error_message()` is byte-identical to and cross-asserts against `ironhermes_core::commands::running_agent::AGENT_RUNNING_REJECT_MSG`. The `/reset → "new"` alias resolution is explicitly asserted (Pitfall 4 evidence).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI keyboard input → command dispatch | Untrusted user input crosses `dispatch_slash`; the running-agent guard is the first authorization gate after `CommandRouter::resolve` |
| `spawn_turn` sync→async transition | RAII guard binding MUST cross this boundary INSIDE the async block (Pitfall 1) — binding in the sync body fires Drop immediately |
| `pending_rx.is_some()` legacy snapshot | Pre-Phase-36.1 code used the channel state as a running-state proxy; this is a correctness bug now removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.1-11 | Tampering | `pending_rx.is_some()` snapshot causes `cmd_stop` to read a disconnected Arc — `/stop` cannot observe a running turn | mitigate | Task 1 Step B removes the snapshot; replaces with `app.agent_running.clone()` — live handle. `test_agent_running_is_live_arc` enforces. |
| T-36.1-12 | EoP | `/RESET` or any alias bypasses the guard check because raw input is matched instead of canonical name | mitigate | Task 1 Step C inserts `is_bypass(&def.name)` AFTER `CommandRouter::resolve` returns `def`; `test_alias_reset_bypasses_guard` proves `/reset → "new"` end-to-end. Pitfall 4 cited by name in Task 1 read_first. |
| T-36.1-13 | DoS | RunningAgentGuard bound in the sync body of `spawn_turn` — Drop fires when `spawn_turn` returns (immediately), before the spawned future runs; flag is never live during the turn | mitigate | Task 2 inserts `let _agent_guard = RunningAgentGuard::new(agent_running)` INSIDE `tokio::spawn(async move { ... })` as the first statement. Acceptance gate uses `awk` to assert the binding appears AFTER `tokio::spawn` in source order. Pitfall 1 cited by name. |
| T-36.1-14 | Information Disclosure | D-02 rejection echoes user input (e.g. mirrors `/model gpt-4` back into the transcript) | mitigate | Task 1 Step C returns `SlashOutcome::Handled(AGENT_RUNNING_REJECT_MSG.to_string())` — the constant from `ironhermes-core`; never interpolates `input`, `args`, or `def.name`. Grep gate enforces zero inline-literal redefinitions. |
| T-36.1-15 | DoS | Guard not cleared on panic — flag stuck true, all subsequent turns rejected forever | mitigate | RAII `Drop` impl in Plan 01 fires on panic per Rust unwind semantics; `test_guard_clears_on_turn_end` covers the normal-exit case; Plan 01's `guard_clears_flag_on_panic` unit test covers the panic case. |
| T-36.1-16 | EoP | Guard ordering downgraded to Relaxed during integration | mitigate | Plan 01 locks SeqCst inside `running_agent.rs`; Plan 03 inherits and adds an acceptance grep against `Ordering::Relaxed` in the spawn_turn region. Pitfall 3. |
| T-36.1-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies in this plan — only `ironhermes-core` (existing workspace dep) added as an import |
</threat_model>

<verification>
1. `cargo test -p ironhermes-cli --test running_agent_guard_tui_tests` — Task 3 integration suite passes (5 tests + d02 sanity).
2. `cargo test -p ironhermes-cli --lib tui_rata` — existing TUI tests including `inv_25_1_gap8_browser_tools_wired_in_rata_chat` still pass.
3. `cargo test -p ironhermes-gateway --test running_agent_guard_tests` — Phase 36 gateway suite still green (no cross-surface regression).
4. `cargo build -p ironhermes-cli` — clean build.
5. `cargo clippy -p ironhermes-cli --lib -- -D warnings` — no violations.
6. Grep regression gates (enforced by acceptance criteria):
   - `pending_rx.is_some()` snapshot REMOVED from commands.rs (count 0).
   - `is_bypass(def.name)` / `is_bypass(&def.name)` present in commands.rs (Pitfall 4).
   - `RunningAgentGuard::new` in event_loop.rs appears AFTER `tokio::spawn` in source order (Pitfall 1, asserted via awk).
   - Zero `Ordering::Relaxed` in spawn_turn region.
   - Zero inline D-02 literal in commands.rs or tests.
</verification>

<success_criteria>
- `App` has a persistent `agent_running: Arc<AtomicBool>` field initialized to false.
- `build_command_context` reads `app.agent_running.clone()` — snapshot is gone (D-08, Pitfall 4).
- `dispatch_slash` rejects non-bypass slash commands with `SlashOutcome::Handled(AGENT_RUNNING_REJECT_MSG)` when running, using `is_bypass(def.name)` on the canonical resolved name (D-10, Pitfall 4).
- `RunningAgentGuard::new(app.agent_running.clone())` bound INSIDE the `tokio::spawn(async move { ... })` block of `spawn_turn` (D-09, Pitfall 1).
- `/reset` resolves to canonical `"new"` and bypasses the guard end-to-end.
- All 5 GW-05-TUI integration tests pass; Phase 36 gateway suite and (if present) Plan 02 web suite remain green.
</success_criteria>

<output>
Create `.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-03-SUMMARY.md` when done.
</output>
