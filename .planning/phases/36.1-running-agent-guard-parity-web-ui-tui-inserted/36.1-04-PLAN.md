---
phase: 36.1-running-agent-guard-parity-web-ui-tui-inserted
plan: 04
type: execute
wave: 3
depends_on:
  - 36.1-01
  - 36.1-02
  - 36.1-03
files_modified:
  - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-VALIDATION.md
autonomous: true
requirements:
  - GW-05-WEB
  - GW-05-TUI
tags:
  - validation
  - cross-surface
  - sign-off
  - nyquist

must_haves:
  truths:
    - "Full workspace `cargo test -p ironhermes-core -p iron_hermes_ui -p ironhermes-cli -p ironhermes-gateway` passes with zero failures across all four crates touched by Phase 36.1"
    - "Phase 36 gateway suite (`running_agent_guard_tests`) passes UNCHANGED — Plans 01-03 introduced zero regressions"
    - "GW-05-WEB integration tests (6 functions) all pass on iron_hermes_ui"
    - "GW-05-TUI integration tests (5 functions + d02 sanity) all pass on ironhermes-cli"
    - "`workspace clippy -- -D warnings` is clean across all four crates"
    - "36.1-VALIDATION.md per-task verification map is populated; nyquist_compliant flipped to true; status flipped to approved"
  artifacts:
    - path: ".planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-VALIDATION.md"
      provides: "Per-task verification map filled with concrete test commands per task, sign-off frontmatter (status: approved, nyquist_compliant: true)"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-VALIDATION.md"
      to: "36.1-01/02/03 plan tasks"
      via: "Per-task verification map row per task with the automated cargo test command"
      pattern: "36.1-01-01\\|36.1-02-01\\|36.1-03-01"
---

<objective>
Run the full workspace verification, fill in `36.1-VALIDATION.md`'s per-task verification map, and sign off `nyquist_compliant: true` once every Plan 01/02/03 task has a working `<automated>` command and the full cross-surface test suite is green.

Purpose: Closes the Nyquist validation contract for Phase 36.1 — every requirement (GW-05-WEB + GW-05-TUI + EXTRACTION) has at least one automated test, no consecutive 3 tasks without coverage, and the cross-surface invariant ("Phase 36 suite still passes after extraction + web UI + TUI changes") is exercised one final time.

Output: Updated `36.1-VALIDATION.md` with the per-task verification map fully populated, frontmatter `status: approved`, `nyquist_compliant: true`, `wave_0_complete: true`, and a sign-off line citing the executed cargo commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-CONTEXT.md
@.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-VALIDATION.md
@.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-01-PLAN.md
@.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-02-PLAN.md
@.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-03-PLAN.md
@.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full cross-surface test suite + clippy and capture results</name>
  <files>(no files modified — observation only)</files>
  <read_first>
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-01-PLAN.md (success criteria block)
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-02-PLAN.md (success criteria block)
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-03-PLAN.md (success criteria block)
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-RESEARCH.md (Validation Architecture section — full suite command)
  </read_first>
  <action>
    Run these commands in sequence and capture the exit code + tail of each output for Task 2's sign-off section. (1) `cargo test -p ironhermes-core --lib commands::running_agent` — must exit 0 with 6+ tests passing. (2) `cargo test -p ironhermes-gateway --test running_agent_guard_tests` — must exit 0 with 11 tests passing (the unmodified Phase 36 suite). (3) `cargo test -p iron_hermes_ui --test running_agent_guard_web_tests` — must exit 0 with 6 tests passing. (4) `cargo test -p iron_hermes_ui --lib server::state` — must exit 0 with the Plan 02 Task 1 unit tests passing. (5) `cargo test -p ironhermes-cli --test running_agent_guard_tui_tests` — must exit 0 with 5+ tests passing. (6) `cargo test -p ironhermes-cli --lib tui_rata` — existing TUI tests including `inv_25_1_gap8_browser_tools_wired_in_rata_chat` must still pass. (7) `cargo clippy -p ironhermes-core -p iron_hermes_ui -p ironhermes-cli -p ironhermes-gateway --lib -- -D warnings` — must exit 0. If ANY command fails: stop and surface the failure to the orchestrator; do NOT proceed to Task 2. If all pass: record the exact commands and tail outputs (last 5 lines of each) in a scratch buffer for Task 2 to embed in the VALIDATION.md sign-off.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-core --lib commands::running_agent && cargo test -p ironhermes-gateway --test running_agent_guard_tests && cargo test -p iron_hermes_ui --test running_agent_guard_web_tests && cargo test -p iron_hermes_ui --lib server::state && cargo test -p ironhermes-cli --test running_agent_guard_tui_tests && cargo test -p ironhermes-cli --lib tui_rata && cargo clippy -p ironhermes-core -p iron_hermes_ui -p ironhermes-cli -p ironhermes-gateway --lib -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - All 7 commands above exit 0.
    - Total passing test count >= 28 (6 core + 11 gateway + 6 web + ≥1 web-state unit + 5+ tui + existing tui-lib tests).
    - Zero clippy warnings across the four affected crates.
  </acceptance_criteria>
  <done>
    Full cross-surface test suite green; clippy clean; results recorded for Task 2 to commit into VALIDATION.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate VALIDATION.md per-task verification map and sign off nyquist_compliant</name>
  <files>.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-VALIDATION.md (current scaffold — frontmatter + Per-Task Verification Map placeholder row)
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-01-PLAN.md (Task 1 + Task 2 — pull the verify <automated> commands)
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-02-PLAN.md (Tasks 1, 2, 3 — pull the verify <automated> commands)
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-03-PLAN.md (Tasks 1, 2, 3 — pull the verify <automated> commands)
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-RESEARCH.md (Threat Patterns section — pull T-36.1-NN IDs for the Threat Ref column)
  </read_first>
  <action>
    Update `36.1-VALIDATION.md` in place. (1) Frontmatter: flip `status: draft` → `status: approved`; flip `nyquist_compliant: false` → `nyquist_compliant: true`; flip `wave_0_complete: false` → `wave_0_complete: true`. (2) Test Infrastructure table: fill `Estimated runtime` with the observed total (sum of the 7 commands in Task 1 — replace `~{N} seconds` with the actual value). (3) Sampling Rate section: replace `{N} seconds` placeholders with the observed full-suite runtime (the cargo test commands from Task 1 sum). (4) Per-Task Verification Map table: replace the placeholder row with one row PER TASK from Plans 01, 02, 03 — eight rows total. Columns: Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status. Use the following 8 rows (one per implementation task across the three plans):
      - `36.1-01-01 | 01 | 1 | GW-05-WEB, GW-05-TUI | T-36.1-01/02/03 | RunningAgentGuard + is_bypass + AGENT_RUNNING_REJECT_MSG verbatim move; SeqCst on both store sites; locked bypass list | unit | cargo test -p ironhermes-core --lib commands::running_agent | ✅ | ✅ green`
      - `36.1-01-02 | 01 | 1 | GW-05-WEB, GW-05-TUI | T-36.1-04 | Gateway imports from core; re-export preserved; Phase 36 suite passes unchanged | integration | cargo test -p ironhermes-gateway --test running_agent_guard_tests | ✅ | ✅ green`
      - `36.1-02-01 | 02 | 2 | GW-05-WEB | T-36.1-05/06 | AppState.running_agents per-session map + get_or_create_running_flag helper + RunningAgentGuard at top of run_web_turn (Pitfall 1) | unit | cargo test -p iron_hermes_ui --lib server::state | ✅ | ✅ green`
      - `36.1-02-02 | 02 | 2 | GW-05-WEB | T-36.1-07/09 | ws.rs slash interception + plain-text guard; D-02 const never inlined; in_flight_turn never set from slash arm | structural | cargo build -p iron_hermes_ui && cargo clippy -p iron_hermes_ui --lib -- -D warnings | ✅ | ✅ green`
      - `36.1-02-03 | 02 | 2 | GW-05-WEB | T-36.1-05/06/07/08/09 | 6 GW-05-WEB behaviors: session isolation, /model rejected, /stop bypasses, RAII clears on error, /reset alias bypasses, plain text rejected | integration | cargo test -p iron_hermes_ui --test running_agent_guard_web_tests | ✅ | ✅ green`
      - `36.1-03-01 | 03 | 2 | GW-05-TUI | T-36.1-11/12/14 | App.agent_running persistent field; build_command_context uses live handle (Pitfall 4); is_bypass(def.name) after resolve | structural | cargo build -p ironhermes-cli && cargo clippy -p ironhermes-cli --lib -- -D warnings | ✅ | ✅ green`
      - `36.1-03-02 | 03 | 2 | GW-05-TUI | T-36.1-13/16 | RunningAgentGuard bound INSIDE tokio::spawn async block of spawn_turn (Pitfall 1); SeqCst preserved | structural | cargo build -p ironhermes-cli && cargo test -p ironhermes-cli --lib tui_rata::event_loop | ✅ | ✅ green`
      - `36.1-03-03 | 03 | 2 | GW-05-TUI | T-36.1-11/12/13/14/15 | 5 GW-05-TUI behaviors: live Arc, RAII clears on turn end, /model rejected, /stop bypasses, /reset alias bypasses | integration | cargo test -p ironhermes-cli --test running_agent_guard_tui_tests | ✅ | ✅ green`
    (5) Wave 0 Requirements section: replace the placeholder bullet with: "All Wave 0 test scaffolds were created inline with their respective implementation plans (running_agent.rs unit tests in Plan 01 Task 1; running_agent_guard_web_tests.rs in Plan 02 Task 3; running_agent_guard_tui_tests.rs in Plan 03 Task 3). No separate Wave 0 phase needed." (6) Manual-Only Verifications section: replace the placeholder with: "All phase behaviors have automated verification — no manual-only steps for Phase 36.1." (7) Validation Sign-Off checklist: tick every box, set Approval to: "approved 2026-05-24 by gsd-planner — full cross-surface test suite green per Task 1 cargo invocations".
  </action>
  <verify>
    <automated>grep -c 'nyquist_compliant: true' .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-VALIDATION.md && grep -c '36.1-01-01\|36.1-01-02\|36.1-02-01\|36.1-02-02\|36.1-02-03\|36.1-03-01\|36.1-03-02\|36.1-03-03' .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'nyquist_compliant: true' .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-VALIDATION.md` returns 1
    - Source assertion: `grep -c 'wave_0_complete: true' .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-VALIDATION.md` returns 1
    - Source assertion: `grep -c 'status: approved' .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-VALIDATION.md` returns 1
    - Source assertion: every plan task ID (36.1-01-01, 36.1-01-02, 36.1-02-01, 36.1-02-02, 36.1-02-03, 36.1-03-01, 36.1-03-02, 36.1-03-03) appears in the per-task map — `grep -c '36.1-0[123]-0[0-9]' .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-VALIDATION.md` returns at least 8
    - Source assertion: no row in the per-task map has the `❌ W0` File Exists marker after Wave 0 — every row reads `✅`
  </acceptance_criteria>
  <done>
    `36.1-VALIDATION.md` per-task map populated for all 8 implementation tasks (2 in Plan 01, 3 in Plan 02, 3 in Plan 03); nyquist_compliant + wave_0_complete + status flipped to true/true/approved; full sign-off recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan boundary → integration | Plans 01/02/03 are independent at file level; this plan exercises the cross-surface invariant that they compose correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.1-17 | DoS | Cross-surface integration test regression slips through because each plan ran its own tests but no full-workspace sweep | mitigate | Task 1 runs all 7 cargo commands in sequence covering all four crates touched by Phase 36.1; failure stops the plan |
| T-36.1-18 | Information Disclosure | VALIDATION.md sign-off backdated or falsified (rows marked green without actual test execution) | mitigate | Task 1 produces the cargo exit codes as the gating evidence; Task 2 can only proceed if Task 1's `<automated>` command succeeds — orchestrator enforces task ordering |
| T-36.1-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies in this plan — verification only |
</threat_model>

<verification>
1. Task 1's compound `<automated>` exits 0 — all 7 cargo commands succeed.
2. Task 2's verify exits 0 — VALIDATION.md frontmatter and per-task map populated.
3. Manual reread of VALIDATION.md confirms 8 task rows, all ✅, and the sign-off line is present.
</verification>

<success_criteria>
- Cross-surface workspace test suite green: core + gateway + iron_hermes_ui + ironhermes-cli all pass.
- Phase 36 gateway suite unchanged and passing (no regression from Plan 01 extraction).
- GW-05-WEB: 6 integration tests + ≥1 unit test passing.
- GW-05-TUI: 5+ integration tests passing; existing TUI lib tests not regressed.
- Clippy clean (-D warnings) across all four touched crates.
- VALIDATION.md signed off as `nyquist_compliant: true`, `wave_0_complete: true`, `status: approved`, with 8 task rows populated.
</success_criteria>

<output>
Create `.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-04-SUMMARY.md` when done.
</output>
