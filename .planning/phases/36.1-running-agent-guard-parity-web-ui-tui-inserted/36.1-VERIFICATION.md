---
phase: 36.1
slug: running-agent-guard-parity-web-ui-tui-inserted
status: passed
verified: 2026-05-25T00:00:00Z
score: 10/10 must-haves verified
overrides_applied: 0
---

# Phase 36.1: Running-Agent Guard Parity — Web UI + TUI — Verification Report

**Phase Goal:** Extend the Phase 36 per-session running-agent guard (RunningAgentGuard RAII, is_bypass, D-02 rejection message) to the web UI and TUI surfaces, closing the parity gap from 36-BACKLOG.md items 1 and 3. Extract shared guard to ironhermes-core so all three surfaces (gateway, web, TUI) use one canonical implementation.
**Verified:** 2026-05-25
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `ironhermes-core::commands::running_agent::{RunningAgentGuard, is_bypass, AGENT_RUNNING_REJECT_MSG}` exist and are pub; gateway has no local defs | ✓ VERIFIED | `running_agent.rs` fully implements all three; gateway imports from core at line 5; `pub mod running_agent` at `commands/mod.rs:9` |
| 2 | `is_bypass` matches exactly `{stop, new, status, queue}` — no others | ✓ VERIFIED | `running_agent.rs:46` uses `matches!(name, "stop" | "new" | "status" | "queue")`; unit test `is_bypass_locked_list` asserts approve/deny/model/retry all return false |
| 3 | `AppState.running_agents: Arc<std::sync::Mutex<HashMap<String, Arc<AtomicBool>>>>` exists; `get_or_create_running_flag` helper present | ✓ VERIFIED | `state.rs:49` declares exact type; `get_or_create_running_flag` at lines 230–238 |
| 4 | Slash detection in `ws.rs` BEFORE `run_web_turn` call | ✓ VERIFIED | `ws.rs:233` slash interception block precedes the `tokio::spawn` that calls `run_web_turn` at line 350+ |
| 5 | Slash dispatch emits `ChatStreamEvent::Delta { text } + Finished` on tx; plain-message rejection same | ✓ VERIFIED | `ws.rs:248-265` (rejection path); `ws.rs:306-317` (dispatch path); `ws.rs:334-347` (plain-text guard) all emit Delta+Finished |
| 6 | Plain-message rejection path exists in `ws.rs` with D-02 message | ✓ VERIFIED | `ws.rs:326-348` explicit `running_flag.load(SeqCst)` block with `AGENT_RUNNING_REJECT_MSG` reference |
| 7 | Web UI bypass list uses shared `is_bypass`; bypass check on canonical `def.name` (Pitfall 4) | ✓ VERIFIED | `ws.rs:23` imports `is_bypass`; `ws.rs:242` calls `!is_bypass(&def.name)` on post-resolution name |
| 8 | `App.agent_running: Arc<AtomicBool>` field exists; `pending_rx.is_some()` snapshot REMOVED from `build_command_context` | ✓ VERIFIED | `app.rs:218` declares field; `commands.rs:545` reads `app.agent_running.clone()` with comment "live handle replaces the pending_rx.is_some() snapshot" |
| 9 | `RunningAgentGuard::new(...)` bound INSIDE `tokio::spawn(async move { ... })` in `spawn_turn` (Pitfall 1) | ✓ VERIFIED | `event_loop.rs:747` starts `tokio::spawn(async move {`; `event_loop.rs:752` binds `let _agent_guard = RunningAgentGuard::new(agent_running)` inside the block |
| 10 | Bypass check in TUI `commands.rs` after resolve on canonical `def.name` (Pitfall 4) | ✓ VERIFIED | `commands.rs:337` `if app.agent_running.load(Ordering::SeqCst) && !is_bypass(def.name)` on post-resolution `def.name` |

**Score:** 10/10 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/ironhermes-core/src/commands/running_agent.rs` | RunningAgentGuard, is_bypass, AGENT_RUNNING_REJECT_MSG all pub | ✓ VERIFIED | 147-line file; all three exported; 6 unit tests embedded |
| `crates/ironhermes-gateway/src/handler.rs` | Imports from core, no local defs | ✓ VERIFIED | Line 5 imports all three from `ironhermes_core::commands::running_agent`; no struct/fn defs for guard in this file |
| `crates/iron_hermes_ui/src/server/state.rs` | `running_agents` field + `get_or_create_running_flag` + RunningAgentGuard at `run_web_turn` entry | ✓ VERIFIED | Field at line 49; helper at lines 230–238; guard at lines 253–255 inside `run_web_turn` |
| `crates/iron_hermes_ui/src/server/ws.rs` | Slash interception + guard before `run_web_turn` | ✓ VERIFIED | Slash block at line 233; plain-text guard at lines 326–348; both use `AGENT_RUNNING_REJECT_MSG` constant |
| `crates/ironhermes-cli/src/tui_rata/app.rs` | `agent_running: Arc<AtomicBool>` field | ✓ VERIFIED | Field at line 218; initialized to `Arc::new(AtomicBool::new(false))` at line 334 |
| `crates/ironhermes-cli/src/tui_rata/commands.rs` | Bypass check on `def.name` after resolve | ✓ VERIFIED | Line 337 uses `is_bypass(def.name)` post-resolution |
| `crates/ironhermes-cli/src/tui_rata/event_loop.rs` | Guard inside `tokio::spawn` async block of `spawn_turn` | ✓ VERIFIED | Lines 747–752: guard bound at top of `tokio::spawn(async move { ... })` block |
| `crates/iron_hermes_ui/tests/running_agent_guard_web_tests.rs` | 9 GW-05-WEB integration tests | ✓ VERIFIED | File exists; 9 tests pass |
| `crates/ironhermes-cli/tests/running_agent_guard_tui_tests.rs` | 6 GW-05-TUI integration tests | ✓ VERIFIED | File exists; 6 tests pass |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `ironhermes-gateway/handler.rs` | `ironhermes-core::commands::running_agent` | `use` import line 5 | ✓ WIRED | All three symbols imported; no local fallback |
| `iron_hermes_ui/server/ws.rs` | `ironhermes-core::commands::running_agent` | `use` import line 23 | ✓ WIRED | `is_bypass` + `AGENT_RUNNING_REJECT_MSG` imported |
| `iron_hermes_ui/server/state.rs` | `ironhermes-core::commands::running_agent::RunningAgentGuard` | path call at `run_web_turn:253` | ✓ WIRED | Guard constructed with `get_or_create_running_flag(session_id)` |
| `ironhermes-cli/tui_rata/commands.rs` | `ironhermes-core::commands::running_agent` | `use` import line 26 | ✓ WIRED | `is_bypass` + `AGENT_RUNNING_REJECT_MSG` imported |
| `ironhermes-cli/tui_rata/event_loop.rs` | `ironhermes-core::commands::running_agent::RunningAgentGuard` | `use` inside `spawn_turn` at line 723 | ✓ WIRED | Guard bound inside `tokio::spawn` async block |
| `app.rs` `agent_running` → `build_command_context` | Live Arc clone | `commands.rs:545` `app.agent_running.clone()` | ✓ WIRED | Replaces dead `pending_rx.is_some()` snapshot |

---

## Pitfall Mitigation Verification

| Pitfall | Requirement | Evidence | Status |
|---------|-------------|----------|--------|
| Pitfall 1: Guard in sync wrapper vs async block | Guard must be bound INSIDE `tokio::spawn(async move { ... })` | `event_loop.rs:747` spawns; line 752 binds guard as first statement inside async block | ✓ MITIGATED |
| Pitfall 3: Relaxed/Acquire/Release orderings | Both store sites must use `Ordering::SeqCst` | `running_agent.rs:26` (new) and line 33 (drop) both use `SeqCst`; grep for Relaxed/Acquire/Release returns zero matches in the file | ✓ MITIGATED |
| Pitfall 4: Bypass on raw input vs canonical name | `is_bypass` called on `def.name` (post-resolution), never raw string | `ws.rs:242` `!is_bypass(&def.name)`; `commands.rs:337` `!is_bypass(def.name)` | ✓ MITIGATED |

---

## Behavioral Spot-Checks (Test Suite Execution)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Core: 6 unit tests (is_bypass locked list, guard sets/clears/panic, constant, approve/deny exclusion) | `cargo test -p ironhermes-core --lib commands::running_agent` | 6 passed, 0 failed | ✓ PASS |
| Gateway: 11 integration tests (Phase 36 regression) | `cargo test -p ironhermes-gateway --test running_agent_guard_tests` | 11 passed, 0 failed | ✓ PASS |
| Web UI: 9 integration tests (GW-05-WEB) | `cargo test -p iron_hermes_ui --test running_agent_guard_web_tests` | 9 passed, 0 failed | ✓ PASS |
| TUI: 6 integration tests (GW-05-TUI) | `cargo test -p ironhermes-cli --test running_agent_guard_tui_tests` | 6 passed, 0 failed | ✓ PASS |
| Workspace build | `cargo build --workspace` | Finished dev profile, exit 0 | ✓ PASS |

**Total tests executed: 32 across 4 suites. All pass.**

---

## Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| GW-05-WEB | Web UI integration tests exist and pass; slash guard + plain-text guard wired; per-session flag map on AppState | ✓ SATISFIED | `running_agent_guard_web_tests.rs` — 9 tests pass; `state.rs` D-03 field + helper; `ws.rs` D-04/D-05/D-06 |
| GW-05-TUI | TUI integration tests exist and pass; `agent_running` Arc on App; RunningAgentGuard inside `tokio::spawn`; bypass check on canonical name | ✓ SATISFIED | `running_agent_guard_tui_tests.rs` — 6 tests pass; `app.rs:218`; `event_loop.rs:747-752`; `commands.rs:337` |

---

## Out-of-Scope Discipline

| Item | Expected | Actual | Status |
|------|----------|--------|--------|
| CLI `main.rs` manual `agent_running.store(true/false)` at lines 1707/2024 | UNTOUCHED (deferred per CONTEXT.md) | Lines 1707 and 2024 remain unchanged with manual store pattern | ✓ CONFIRMED |

---

## Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `running_agent.rs:45` | `// TODO(D-01): add "approve" | "deny" when approval queue lands` | Info | Intentional, formally cross-referenced to decision D-01 and documented in CONTEXT.md deferred section. Not a blocker. |

No TBD, FIXME, or XXX markers found in any phase-touched file.

Pre-existing warnings in `ironhermes-agent`, `ironhermes-state`, `ironhermes-gateway`, and `ironhermes-cli` binaries are unchanged from before Phase 36.1 — none appear in the six files modified by this phase (`running_agent.rs`, `state.rs`, `ws.rs`, `app.rs`, `commands.rs`, `event_loop.rs`).

---

## Human Verification Required

None. All phase behaviors have automated test coverage. No visual, real-time, or external-service behavior was introduced by this phase.

---

## Gaps Summary

No gaps. All 10 must-have truths are VERIFIED. All 4 test suites pass. Workspace builds clean. Pitfall mitigations confirmed in code. Out-of-scope CLI lines untouched.

---

_Verified: 2026-05-25_
_Verifier: Claude (gsd-verifier)_
