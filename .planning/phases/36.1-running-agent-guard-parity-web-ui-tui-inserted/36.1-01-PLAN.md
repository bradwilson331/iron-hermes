---
phase: 36.1-running-agent-guard-parity-web-ui-tui-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-core/src/commands/running_agent.rs
  - crates/ironhermes-core/src/commands/mod.rs
  - crates/ironhermes-core/src/lib.rs
  - crates/ironhermes-gateway/src/handler.rs
  - crates/ironhermes-gateway/src/lib.rs
autonomous: true
requirements:
  - GW-05-WEB
  - GW-05-TUI
tags:
  - rust
  - refactor
  - extraction
  - atomic-bool
  - raii

must_haves:
  truths:
    - "RunningAgentGuard, is_bypass, and AGENT_RUNNING_REJECT_MSG are publicly importable from ironhermes_core::commands::running_agent (D-01)"
    - "ironhermes-core::commands::running_agent::is_bypass returns true for exactly {stop, new, status, queue} and false for everything else (D-02)"
    - "ironhermes_gateway::RunningAgentGuard re-exports the SAME type from ironhermes-core (no duplicate definition; gateway integration tests continue to pass without modification)"
    - "Both store sites in RunningAgentGuard (new + Drop) use Ordering::SeqCst — never Relaxed (RESEARCH Pitfall 3)"
    - "AGENT_RUNNING_REJECT_MSG is the exact locked D-02 string byte-for-byte (no paraphrasing)"
  artifacts:
    - path: "crates/ironhermes-core/src/commands/running_agent.rs"
      provides: "RunningAgentGuard, is_bypass, AGENT_RUNNING_REJECT_MSG, plus #[cfg(test)] unit tests"
      contains: "pub struct RunningAgentGuard"
    - path: "crates/ironhermes-core/src/commands/mod.rs"
      provides: "pub mod running_agent; submodule declaration"
      contains: "pub mod running_agent"
    - path: "crates/ironhermes-core/src/lib.rs"
      provides: "Crate-root re-export of RunningAgentGuard, is_bypass, AGENT_RUNNING_REJECT_MSG"
      contains: "pub use commands::running_agent::"
    - path: "crates/ironhermes-gateway/src/handler.rs"
      provides: "Local guard definitions removed; import from ironhermes_core added"
      contains: "use ironhermes_core::commands::running_agent::"
    - path: "crates/ironhermes-gateway/src/lib.rs"
      provides: "Re-export path updated to source from ironhermes-core"
      contains: "pub use ironhermes_core::commands::running_agent::RunningAgentGuard"
  key_links:
    - from: "crates/ironhermes-gateway/src/handler.rs"
      to: "ironhermes_core::commands::running_agent"
      via: "use statement (replaces local defs)"
      pattern: "use ironhermes_core::commands::running_agent"
    - from: "crates/ironhermes-gateway/tests/running_agent_guard_tests.rs"
      to: "ironhermes_gateway::RunningAgentGuard"
      via: "re-export pass-through"
      pattern: "ironhermes_gateway::RunningAgentGuard"
---

<objective>
Extract `RunningAgentGuard`, `is_bypass`, and `AGENT_RUNNING_REJECT_MSG` from `ironhermes-gateway/src/handler.rs` to `ironhermes-core::commands::running_agent`, with re-export through `ironhermes-gateway::lib.rs` so existing call sites and integration tests keep working without modification.

Purpose: Implements D-01 + D-02 — the shared-machinery foundation that GW-05-WEB (Plan 02) and GW-05-TUI (Plan 03) both depend on. Closes BACKLOG item 3 (three-way maintenance hazard) by giving gateway/web/TUI one canonical implementation.

Output: New module `ironhermes-core::commands::running_agent` (verbatim port of the gateway code) + gateway updated to import from core. Phase 36's `running_agent_guard_tests.rs` integration suite continues to pass via the re-export at `ironhermes-gateway/src/lib.rs:20`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-CONTEXT.md
@.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-RESEARCH.md
@.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-PATTERNS.md
@.planning/phases/36-gateway-running-agent-guard-wiring-completes-gw-05/36-CONTEXT.md

@crates/ironhermes-gateway/src/handler.rs
@crates/ironhermes-gateway/src/lib.rs
@crates/ironhermes-core/src/commands/mod.rs
@crates/ironhermes-core/src/lib.rs
@crates/ironhermes-gateway/tests/running_agent_guard_tests.rs

<interfaces>
<!-- Source identifiers to be MOVED from gateway to core, kept byte-stable. -->

From crates/ironhermes-gateway/src/handler.rs (lines 62-103, current):
- `pub const AGENT_RUNNING_REJECT_MSG: &str = "Agent is running. Use /stop to interrupt or /queue to send after this turn.";`
- `pub struct RunningAgentGuard(Arc<AtomicBool>);`
- `impl RunningAgentGuard { pub fn new(flag: Arc<AtomicBool>) -> Self { flag.store(true, Ordering::SeqCst); Self(flag) } }`
- `impl Drop for RunningAgentGuard { fn drop(&mut self) { self.0.store(false, Ordering::SeqCst); } }`
- `pub fn is_bypass(name: &str) -> bool { matches!(name, "stop" | "new" | "status" | "queue") }`

From crates/ironhermes-gateway/src/lib.rs (line 20, current):
- `pub use handler::RunningAgentGuard;` — must remain as the public symbol so `ironhermes_gateway::RunningAgentGuard` continues to resolve.

Consumed-without-modification by:
- `crates/ironhermes-gateway/tests/running_agent_guard_tests.rs` — uses `ironhermes_gateway::RunningAgentGuard`
- `crates/ironhermes-gateway/src/handler.rs` use sites at lines 486-498 (slash guard check), 854-866 (free-text guard check), 884-889 (RAII bind in run_agent)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ironhermes-core::commands::running_agent module with unit tests</name>
  <files>crates/ironhermes-core/src/commands/running_agent.rs, crates/ironhermes-core/src/commands/mod.rs, crates/ironhermes-core/src/lib.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/handler.rs (lines 62-103 — verbatim source to move; do NOT paraphrase)
    - crates/ironhermes-core/src/commands/mod.rs (current pub mod declarations — running_agent must be appended)
    - crates/ironhermes-core/src/lib.rs (existing pub use commands::* block — running_agent re-export goes here)
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-PATTERNS.md (sections: "running_agent.rs (NEW)", "commands/mod.rs (MODIFY)", "core/src/lib.rs (MODIFY)")
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-RESEARCH.md "Pitfall 3" (SeqCst ordering — cite by name in commit message)
  </read_first>
  <behavior>
    - Test 1 (`is_bypass_locked_list`): `is_bypass("stop")`, `is_bypass("new")`, `is_bypass("status")`, `is_bypass("queue")` ALL return true; `is_bypass("model")`, `is_bypass("retry")`, `is_bypass("approve")`, `is_bypass("deny")`, `is_bypass("")`, `is_bypass("STOP")` (case-sensitive) ALL return false. (D-02)
    - Test 2 (`guard_sets_flag_on_construction`): construct `RunningAgentGuard::new(flag)` on an `Arc<AtomicBool>` initialized to false; immediately load the flag with `Ordering::SeqCst` and assert it is true while the guard is in scope.
    - Test 3 (`guard_clears_flag_on_drop`): inner scope creates the guard, asserts flag is true, then drops; outer scope asserts flag is false after drop.
    - Test 4 (`guard_clears_flag_on_panic`): use `std::panic::catch_unwind(AssertUnwindSafe(|| { let _g = RunningAgentGuard::new(flag.clone()); panic!("simulated"); }))` and assert the flag is false after the panic is caught. Proves Drop fires on panic exit paths (D-06 / RAII discipline).
    - Test 5 (`reject_msg_constant_value`): `assert_eq!(AGENT_RUNNING_REJECT_MSG, "Agent is running. Use /stop to interrupt or /queue to send after this turn.")` — byte-for-byte lock against silent paraphrasing of the D-02 contract string.
    - Test 6 (`is_bypass_keeps_todo_for_approve_deny`): `assert!(!is_bypass("approve"))` and `assert!(!is_bypass("deny"))` — proves the locked bypass list does NOT include approve/deny; the TODO comment is informational only and the bypass list stays narrow until the approval queue lands.
  </behavior>
  <action>
    Create `crates/ironhermes-core/src/commands/running_agent.rs` with the EXACT items moved verbatim from `ironhermes-gateway/src/handler.rs:62-103` (per Pitfall 3, RESEARCH.md): `pub const AGENT_RUNNING_REJECT_MSG`, `pub struct RunningAgentGuard(Arc<AtomicBool>)`, `impl RunningAgentGuard::new` (calls `flag.store(true, Ordering::SeqCst)`), `impl Drop for RunningAgentGuard` (calls `self.0.store(false, Ordering::SeqCst)`), and `pub fn is_bypass(name: &str) -> bool` matching exactly `"stop" | "new" | "status" | "queue"`. Preserve the `// TODO(D-01): add "approve" | "deny" when approval queue lands` comment inside `is_bypass`. Use `std::sync::Arc` and `std::sync::atomic::{AtomicBool, Ordering}` imports — no other dependencies. Doc-comment the module header with: "Phase 36.1 (D-01): extracted from ironhermes-gateway/src/handler.rs:62-103 — single canonical impl for gateway, web UI (Plan 02), and TUI (Plan 03). SeqCst is mandatory on both store sites per RESEARCH.md Pitfall 3 — do NOT downgrade." Append `#[cfg(test)] mod tests { ... }` containing the six tests from `<behavior>`. Then append `pub mod running_agent;` to `crates/ironhermes-core/src/commands/mod.rs` after the existing `pub mod` block. Then add `pub use commands::running_agent::{RunningAgentGuard, is_bypass, AGENT_RUNNING_REJECT_MSG};` to `crates/ironhermes-core/src/lib.rs` after the existing `pub use commands::...` block. No new dependencies in any Cargo.toml.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-core --lib commands::running_agent 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "pub struct RunningAgentGuard" crates/ironhermes-core/src/commands/running_agent.rs` returns 1
    - Source assertion: `grep -c 'Ordering::SeqCst' crates/ironhermes-core/src/commands/running_agent.rs` returns at least 2 (new + Drop)
    - Source assertion: `grep -c 'Ordering::Relaxed\|Ordering::Acquire\|Ordering::Release' crates/ironhermes-core/src/commands/running_agent.rs` returns 0 (Pitfall 3 enforcement)
    - Source assertion: `grep -F 'matches!(name, "stop" | "new" | "status" | "queue")' crates/ironhermes-core/src/commands/running_agent.rs` returns a match (bypass list locked verbatim)
    - Source assertion: `grep -F 'Agent is running. Use /stop to interrupt or /queue to send after this turn.' crates/ironhermes-core/src/commands/running_agent.rs` returns a match (D-02 string locked verbatim)
    - Source assertion: `grep -c '^pub mod running_agent;' crates/ironhermes-core/src/commands/mod.rs` returns 1
    - Source assertion: `grep -F 'pub use commands::running_agent::' crates/ironhermes-core/src/lib.rs` returns a match
    - Test command: `cargo test -p ironhermes-core --lib commands::running_agent` exits 0 with at least 6 passing tests (`is_bypass_locked_list`, `guard_sets_flag_on_construction`, `guard_clears_flag_on_drop`, `guard_clears_flag_on_panic`, `reject_msg_constant_value`, `is_bypass_keeps_todo_for_approve_deny`)
    - Lint command: `cargo clippy -p ironhermes-core --lib -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    `ironhermes_core::commands::running_agent` module exists with the verbatim guard + bypass + D-02 const, six unit tests pass under `cargo test -p ironhermes-core --lib commands::running_agent`, both SeqCst store sites preserved (Pitfall 3), and the crate-root re-export `pub use commands::running_agent::*` makes `RunningAgentGuard`, `is_bypass`, `AGENT_RUNNING_REJECT_MSG` importable as `ironhermes_core::RunningAgentGuard` etc.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove gateway local defs, import from core, preserve re-export</name>
  <files>crates/ironhermes-gateway/src/handler.rs, crates/ironhermes-gateway/src/lib.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/handler.rs (lines 1-110 for imports + lines 62-103 to be removed + lines 486-498, 854-866, 884-889 to confirm use sites remain valid)
    - crates/ironhermes-gateway/src/lib.rs (entire file — `pub use handler::RunningAgentGuard;` at line 20 must be re-pathed)
    - crates/ironhermes-gateway/tests/running_agent_guard_tests.rs (lines 1-60 — confirm test imports `ironhermes_gateway::RunningAgentGuard` and `helpers::d02_error_message()` returns the exact constant string; these MUST keep working without modification)
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-PATTERNS.md (sections: "handler.rs (MODIFY)", "lib.rs (MODIFY)")
    - .planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-RESEARCH.md (Summary §Track A — re-export preservation rationale)
  </read_first>
  <behavior>
    - Test 1: full Phase 36 integration suite `cargo test -p ironhermes-gateway --test running_agent_guard_tests` passes (~11 tests) UNCHANGED — including `test_session_isolation`, `test_model_rejected_when_running`, `test_stop_bypasses_guard`, `test_new_bypasses_guard`, `test_status_bypasses_guard`, `test_queue_bypasses_guard`, `test_guard_clears_on_success`, `test_guard_clears_on_error`, `test_alias_bypasses_guard`, `test_freetext_rejected_when_running`, `test_stop_reads_real_flag`. No test file edits required.
    - Test 2 (compile-only): `ironhermes_gateway::RunningAgentGuard` remains a valid path — re-export goes through ironhermes-core but the public name on the gateway crate is preserved.
    - Test 3 (compile-only): handler.rs sites at lines 486-498, 854-866, 884-889 continue to call `is_bypass(&def.name)`, `AGENT_RUNNING_REJECT_MSG`, and `RunningAgentGuard::new(...)` — must resolve via the new `use` import without local definitions present.
  </behavior>
  <action>
    Delete lines 62-103 of `crates/ironhermes-gateway/src/handler.rs` (the `AGENT_RUNNING_REJECT_MSG` const, `RunningAgentGuard` struct + `impl RunningAgentGuard::new` + `impl Drop` + `pub fn is_bypass`). Replace with a single line at the top of the file alongside other `use` statements: `use ironhermes_core::commands::running_agent::{RunningAgentGuard, is_bypass, AGENT_RUNNING_REJECT_MSG};`. Audit the file for `use std::sync::atomic::{AtomicBool, Ordering}` — keep `Ordering` (still used at the guard-check sites lines 486-498 / 854-866) but remove `AtomicBool` from the import list ONLY IF it has no other use site after the delete (run `grep -n 'AtomicBool' crates/ironhermes-gateway/src/handler.rs` after the delete to confirm; if other sites remain, leave the import). In `crates/ironhermes-gateway/src/lib.rs` replace `pub use handler::RunningAgentGuard;` with `pub use ironhermes_core::commands::running_agent::RunningAgentGuard;` — the public symbol `ironhermes_gateway::RunningAgentGuard` MUST remain importable so `crates/ironhermes-gateway/tests/running_agent_guard_tests.rs` keeps compiling without edits. Do NOT modify any handler.rs use sites at lines 486-498 (slash guard check), 854-866 (free-text guard check), or 884-889 (RAII bind) — they continue to call `is_bypass(&def.name)`, `AGENT_RUNNING_REJECT_MSG`, and `RunningAgentGuard::new` against the imported symbols. Do NOT modify the integration test file. (Cites Pitfalls 3 + 4: ordering preserved, post-resolution canonical-name guard preserved.)
  </action>
  <verify>
    <automated>cargo test -p ironhermes-gateway --test running_agent_guard_tests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'pub struct RunningAgentGuard' crates/ironhermes-gateway/src/handler.rs` returns 0 (local def removed)
    - Source assertion: `grep -c 'pub fn is_bypass' crates/ironhermes-gateway/src/handler.rs` returns 0 (local def removed)
    - Source assertion: `grep -c 'pub const AGENT_RUNNING_REJECT_MSG' crates/ironhermes-gateway/src/handler.rs` returns 0 (local def removed)
    - Source assertion: `grep -c 'use ironhermes_core::commands::running_agent::' crates/ironhermes-gateway/src/handler.rs` returns 1 (import added)
    - Source assertion: `grep -c 'pub use ironhermes_core::commands::running_agent::RunningAgentGuard' crates/ironhermes-gateway/src/lib.rs` returns 1
    - Source assertion: `grep -c 'pub use handler::RunningAgentGuard' crates/ironhermes-gateway/src/lib.rs` returns 0 (old path removed)
    - Source assertion: `grep -c 'is_bypass(&def.name)' crates/ironhermes-gateway/src/handler.rs` returns at least 1 (Pitfall 4 — canonical-name check preserved at line 486-498 region)
    - Test command: `cargo test -p ironhermes-gateway --test running_agent_guard_tests` exits 0 with 11+ passing tests (unmodified Phase 36 suite)
    - Build command: `cargo build -p ironhermes-gateway` exits 0 with no warnings about unused imports
    - Lint command: `cargo clippy -p ironhermes-gateway --lib -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    Gateway's `handler.rs` no longer defines the guard locally — it imports from `ironhermes_core::commands::running_agent`. The public symbol `ironhermes_gateway::RunningAgentGuard` still resolves (via the updated re-export at lib.rs:20). All 11 Phase 36 integration tests in `tests/running_agent_guard_tests.rs` pass unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crate-to-crate | `ironhermes-gateway` now imports the guard from `ironhermes-core`; the type identity must be preserved (single canonical `RunningAgentGuard`) so cross-crate `Drop` semantics remain correct |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.1-01 | Tampering | `RunningAgentGuard` ordering downgrade during extraction | mitigate | Verbatim copy of `Ordering::SeqCst` on both store sites; grep gate in acceptance criteria asserts zero `Relaxed/Acquire/Release` occurrences (Pitfall 3) |
| T-36.1-02 | Tampering | `AGENT_RUNNING_REJECT_MSG` silently paraphrased | mitigate | byte-for-byte grep gate in acceptance + `reject_msg_constant_value` unit test |
| T-36.1-03 | EoP | `is_bypass` list silently widened (e.g. accidentally includes "model" or "approve") | mitigate | `is_bypass_locked_list` test enumerates exact membership; `is_bypass_keeps_todo_for_approve_deny` test enumerates exact non-membership |
| T-36.1-04 | DoS | Gateway tests stop passing after extraction (regression) | mitigate | Task 2 acceptance gate runs the unmodified Phase 36 test suite; re-export at lib.rs preserves the public symbol path |
| T-36.1-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies in this plan — verified by RESEARCH.md "Standard Stack" (all types are `std::*`) |
</threat_model>

<verification>
1. `cargo test -p ironhermes-core --lib commands::running_agent` — 6 new unit tests pass.
2. `cargo test -p ironhermes-gateway --test running_agent_guard_tests` — all 11 Phase 36 integration tests pass UNCHANGED.
3. `cargo build -p ironhermes-core -p ironhermes-gateway` — clean build, no unused-import warnings.
4. `cargo clippy -p ironhermes-core -p ironhermes-gateway --lib -- -D warnings` — no clippy violations.
5. Grep regression gates (all enforced by acceptance criteria above):
   - No `pub struct RunningAgentGuard` in handler.rs.
   - Exactly one `pub mod running_agent;` in commands/mod.rs.
   - Zero `Ordering::Relaxed` (or Acquire/Release) in running_agent.rs.
   - `ironhermes_gateway::RunningAgentGuard` still resolves via re-export.
</verification>

<success_criteria>
- `ironhermes-core::commands::running_agent` module compiles standalone with 6 passing unit tests.
- `ironhermes-gateway` compiles with the import substitution and ZERO modifications to its integration test file.
- Phase 36's `running_agent_guard_tests.rs` (11 tests) passes unchanged.
- Both store sites in `RunningAgentGuard` still use `Ordering::SeqCst`.
- D-02 rejection message string is byte-identical to the Phase 36 lock.
- No new dependencies added to any Cargo.toml.
- Plans 02 (web UI) and 03 (TUI) can now import `ironhermes_core::commands::running_agent::{RunningAgentGuard, is_bypass, AGENT_RUNNING_REJECT_MSG}`.
</success_criteria>

<output>
Create `.planning/phases/36.1-running-agent-guard-parity-web-ui-tui-inserted/36.1-01-SUMMARY.md` when done.
</output>
