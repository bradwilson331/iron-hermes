---
status: complete
phase: 21.8-skill-remote-download-and-install-from-skills-sh
source:
  - 21.8-01-SUMMARY.md
  - 21.8-02-SUMMARY.md
  - 21.8-03-SUMMARY.md
  - 21.8-04-SUMMARY.md
  - 21.8-05-SUMMARY.md
  - 21.8-06-SUMMARY.md
started: 2026-04-22T17:45:00Z
updated: 2026-04-22T19:05:00Z
---

## Current Test

[testing complete]

## Tests

### 1. skills --help shows new verb set
expected: Running `ironhermes skills --help` lists install, search, update, remove, list, trust. The `remove` help mentions `uninstall` as an alias (D-04).
result: pass

### 2. skills install --help shows --skip-audit
expected: Running `ironhermes skills install --help` documents a `--skip-audit` option describing it as skipping the pre-install audit endpoint call (D-19).
result: pass

### 3. Install emits D-21 5-line progress shape
expected: Running `ironhermes skills install <anything>` prints in order (even when the install fails partway): `Resolving skills.sh/...`, `Discovering skills in ...`, `Downloading N bytes...`, `Scanning for threats...`. On success a 5th line `Installed '<name>' [<trust>] — hash: <12-char>` appears.
result: pass
note: Verified (initial run) the 4 progress lines emit in correct order for `skills install excalidraw` and `skills install ascii-art`. After plan 21.8-06 landed the advisory-hash fix, re-ran `skills install ascii-art` and observed the full 5-line shape including `Installed 'ascii-art' [community] — hash: 4fe9ab1c00f3`.

### 4. D-23 restart message on successful install
expected: After a successful install, a separate stdout line reads `Installed. Restart the agent or start a new session to use <name>.` This ONLY shows on clean install — if every attempted install errors out (e.g., pre-existing ShaMismatch against live skills.sh), mark this test as blocked.
result: pass
note: Unblocked by plan 21.8-06 (commits `0ac50f9` + `cf14199`). Re-verified with `skills install ascii-art` — final stdout line reads `Installed. Restart the agent or start a new session to use ascii-art.` exactly as specified.

### 5. skills list reads from skills-lock.json
expected: Running `ironhermes skills list` runs without error regardless of whether any skills are installed. `ironhermes skills list --format json` outputs valid JSON. The data source is `~/.ironhermes/skills-lock.json` (NOT the old `~/.ironhermes/skills/.hub/lock.json`).
result: pass
note: `skills list --format json` → `[]` (empty but valid JSON); `skills list` → `No skills installed.` Both run clean with no installed skills (expected given ShaMismatch blocker on fresh installs).

### 6. skills uninstall alias works
expected: Running `ironhermes skills uninstall <some-name>` does NOT error with "unknown subcommand". Clap routes it to the same handler as `ironhermes skills remove <some-name>` (D-04 — one release cycle compatibility alias). If no skill is installed, an error like "skill not installed" is fine — the test is about the verb alias working, not the removal outcome.
result: pass
note: `skills uninstall excalidraw` and `skills remove excalidraw` produce identical `hub error (NotFound): skill 'excalidraw' is not installed` — proving the clap alias routes both verbs to the same handler.

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Installing a skill from live skills.sh completes successfully and reaches the D-21 5th line + D-23 restart message."
  status: resolved
  resolved_by: plan 21.8-06 (commits 0ac50f9 + cf14199 + e2929d9)
  resolution: "Applied option (b) — post-install server/client hash compare is ADVISORY (tracing::warn log-only, install proceeds). D-14 opaque contract preserved (snapshot_hash round-trips verbatim); D-13 drift detection still runs on update() against SkillLockEntry.computed_hash. Re-verified end-to-end with `skills install ascii-art` → full 5-line D-21 shape + D-23 restart message emit as specified."
  original_reason: "Every live install fails with `ShaMismatch: computed_hash ... != server snapshot_hash ...`. Observed on `excalidraw`, `ascii-art`, `brainstorming` (three independent repos). The installer's post-install verify compared client D-13 folder hash byte-for-byte against opaque server snapshotHash — contradicted D-14."
  severity: blocker
  test: 3, 4
