---
phase: 21.8
slug: skill-remote-download-and-install-from-skills-sh
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-22
---

# Phase 21.8 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (Rust 2024 + `wiremock` dev-dep for HTTP mocks + `tempfile` for fs fixtures) |
| **Config file** | `crates/ironhermes-hub/Cargo.toml` (deps already present); no extra config needed |
| **Quick run command** | `cargo test -p ironhermes-hub --lib` |
| **Full suite command** | `cargo test --workspace` |
| **Estimated runtime** | ~90 seconds full workspace; ~12 seconds ironhermes-hub lib tests |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-hub --lib` (fast path for pure-function changes)
- **After every plan wave:** Run `cargo test --workspace` (cross-crate integration)
- **Before `/gsd-verify-work`:** Full suite must be green + `cargo clippy --workspace -- -D warnings`
- **Max feedback latency:** ~15 seconds for unit tests, ~90 seconds for full suite

---

## Per-Task Verification Map

> Populated by the planner during `/gsd-plan-phase`. Each task gets a row with its automated verify command.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD — planner to fill | TBD | TBD | SKILL-08 / MEM-06 | T-21.8-XX | TBD | unit/integration | `cargo test ...` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/ironhermes-hub/src/sanitize.rs` — pure-function test module with golden vectors for `strip_terminal_escapes`, `sanitize_name`, `sanitize_subpath`, `is_path_safe`, `is_contained_in`
- [ ] `crates/ironhermes-hub/src/lock.rs` — test module with golden lock-file fixtures (timestamp-free hashed region)
- [ ] `crates/ironhermes-hub/tests/to_skill_slug.rs` — golden-vector test (20+ cases from reference TS)
- [ ] `crates/ironhermes-hub/tests/skills_sh_blob_adapter.rs` — wiremock fixtures for Trees API + raw.githubusercontent + `/api/download`
- [ ] `crates/ironhermes-cli/tests/skills_cmd_integration.rs` — CLI-level end-to-end (tempfile HERMES_HOME + mock server)

*All Wave 0 infra uses workspace-resident deps (`wiremock`, `tempfile`, `sha2`). No new framework installs required.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Progress line shape (5-line default per D-21) | SKILL-08 UX | stdout formatting — automated snapshot is brittle | Run `hermes skills install <any-test-skill>`; visually confirm the 5-line shape matches D-21 |
| Pre-install audit output (D-19) | SKILL-08 security | Third-party endpoint (`add-skill.vercel.sh/audit`); mocked in integration tests but live behavior needs human confirmation for copy/flow | Run `hermes skills install <audited-skill>` against live audit endpoint; confirm risk summary prints and `--skip-audit` bypasses correctly |
| Next-session activation (D-23) | MEM-06 cache-safety | Requires agent restart; not exercisable inside a single test run | Install a skill, start a new `hermes` session, confirm the skill appears in `SkillRegistry::load_with_paths` output |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (sanitize golden vectors, lock fixtures, slug vectors, wiremock blob fixtures, CLI e2e)
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s for full workspace
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
