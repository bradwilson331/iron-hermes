---
phase: 36.17.5
plan: "01"
subsystem: ironhermes-core / ironhermes-tools
tags:
  - tts
  - trait
  - registry
  - config
  - wave-0
dependency_graph:
  requires: []
  provides:
    - TtsProvider trait (ironhermes-core::tts)
    - TtsRegistry struct (ironhermes-core::tts)
    - BUILTIN_TTS_NAMES constant (ironhermes-core::tts)
    - TtsConfig + EdgeTtsConfig + ElevenLabsConfig (ironhermes-core::config)
    - "voice" in ALL_TOOLSETS (ironhermes-core::constants)
    - msedge-tts 0.4 workspace dep
    - rodio 0.22.2 workspace dep
  affects:
    - crates/ironhermes-core/src/lib.rs
    - crates/ironhermes-core/src/config.rs
    - crates/ironhermes-core/src/constants.rs
    - Cargo.toml
    - crates/ironhermes-tools/Cargo.toml
tech_stack:
  added:
    - msedge-tts = "0.4" (features = ["tokio-runtime"]) — operator-approved
    - rodio = "0.22.2" (features = ["mp3"]) — operator-approved
  patterns:
    - TtsProvider trait mirrors Tool trait shape from registry.rs (async-trait, Arc<dyn>, Send+Sync)
    - TtsRegistry mirrors ToolRegistry pattern (HashMap<String, Arc<dyn TtsProvider>>)
    - TtsConfig follows PromptCachingConfig explicit-Default pattern from config.rs
    - Wave-0 test stub pattern from cronjob_tool_default_deliver.rs (#[ignore = "..."], OnceLock)
    - UAT skeleton mirrors phase-36.17-web-logging.sh (set -euo pipefail, mktemp, trap cleanup)
key_files:
  created:
    - crates/ironhermes-core/src/tts.rs
    - crates/ironhermes-core/tests/tts_registry.rs
    - crates/ironhermes-tools/tests/tts_tools.rs
    - scripts/uat/phase-36.17.5-tts.sh
  modified:
    - crates/ironhermes-core/src/lib.rs
    - crates/ironhermes-core/src/config.rs
    - crates/ironhermes-core/src/constants.rs
    - Cargo.toml
    - crates/ironhermes-tools/Cargo.toml
decisions:
  - "msedge-tts = \"0.4\" (operator-approved, supersedes plan's 0.3)"
  - "rodio = \"0.22.2\" (operator-approved, supersedes plan's 0.20)"
  - "tokio-runtime feature confirmed valid in msedge-tts 0.4 (verified via published Cargo.toml)"
  - "YAML round-trip test uses empty doc {} instead of model: gpt-4 (ModelConfig expects struct not string)"
metrics:
  duration: "~25 minutes"
  completed: "2026-06-04"
  tasks_completed: 7
  tasks_total: 7
  files_created: 4
  files_modified: 5
---

# Phase 36.17.5 Plan 01: TTS Foundation — Trait, Registry, Config, Wave-0 Tests Summary

TtsProvider trait + TtsRegistry + BUILTIN_TTS_NAMES in ironhermes-core, strongly-typed TtsConfig (D-12), "voice" toolset entry, msedge-tts 0.4 + rodio 0.22.2 workspace deps (operator-approved), and 5 passing Wave-0 registry tests + 6 compiling tool stubs.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Record operator approval for msedge-tts 0.4 | 6866aee1 | (doc-only) |
| 2 | Record operator approval for rodio 0.22.2 | c5b6b8a0 | (doc-only) |
| 3 | Add msedge-tts + rodio workspace deps | 460210b9 | Cargo.toml, crates/ironhermes-tools/Cargo.toml |
| 4 | Create ironhermes-core::tts module | b9d5b018 | crates/ironhermes-core/src/tts.rs, lib.rs |
| 5 | Add TtsConfig + sub-structs; add "voice" to ALL_TOOLSETS | cb4ebea6 | config.rs, constants.rs |
| 6 | Wave-0 registry unit tests (TTS-01, TTS-02, TTS-07, TTS-08) | d379de65 | crates/ironhermes-core/tests/tts_registry.rs |
| 7 | Wave-0 tools test stubs + UAT script skeleton | a2ee60f7 | tts_tools.rs, phase-36.17.5-tts.sh |

## Package Versions Installed

- `msedge-tts = "0.4"` (operator-approved, supersedes plan's `0.3`)
  - Feature `tokio-runtime` confirmed valid in v0.4 (verified via published Cargo.toml)
  - Publisher: `hs-CN` (github.com/hs-CN/msedge-tts)
- `rodio = "0.22.2"` (operator-approved, supersedes plan's `0.20`)
  - Feature `mp3` confirmed valid (symphonia-bundle-mp3 pulled transitively)
  - Publisher: `RustAudio` GitHub org (github.com/RustAudio/rodio)

## Operator Approvals

- **Task 1 (msedge-tts):** APPROVED with version override `0.4` — recorded as commit `6866aee1`
- **Task 2 (rodio):** APPROVED with version override `0.22.2` — recorded as commit `c5b6b8a0`

## Test Results

```
cargo test -p ironhermes-core --test tts_registry
running 5 tests
test test_builtin_names_set ... ok
test test_tts_registry_get_edge ... ok
test test_tts_registry_builtin_wins ... ok
test test_tts_config_defaults ... ok
test test_tts_config_yaml_omitted_block_uses_defaults ... ok
test result: ok. 5 passed; 0 failed; 0 ignored
```

```
cargo test -p ironhermes-tools --test tts_tools
running 6 tests
test test_edge_provider_available ... ignored, TTS-03
test test_edge_synth_writes_file ... ignored, TTS-10
test test_elevenlabs_unavailable_no_key ... ignored, TTS-04
test test_ffmpeg_probe_no_panic ... ignored, TTS-09
test test_tts_tool_creates_audio_cache_dir ... ignored, TTS-06
test test_tts_tool_metadata ... ignored, TTS-05
test result: ok. 0 passed; 0 failed; 6 ignored
```

```
bash scripts/uat/phase-36.17.5-tts.sh
==> Phase 36.17.5 TTS UAT — IRONHERMES_HOME=...
==> Gate 0: banner echo only (PLAN 01 skeleton — gates 1-5 wired in PLAN 04)
    OK

==> Phase 36.17.5 TTS UAT: skeleton green (gates 1-5 pending PLAN 04 wiring)
exit 0
```

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] YAML round-trip test used invalid model field**
- **Found during:** Task 6
- **Issue:** Test used `"model: gpt-4\n"` as YAML — the `model` field on `Config` expects `ModelConfig` struct, not a string, causing `serde_yaml` to fail with "invalid type: string"
- **Fix:** Changed test YAML to `"{}\n"` (empty document) — still proves `tts:` block omission fills defaults via `#[serde(default)]`, which is the actual D-12 invariant being tested
- **Files modified:** `crates/ironhermes-core/tests/tts_registry.rs`
- **Commit:** d379de65

### Feature Flag Verification

**msedge-tts 0.4 `tokio-runtime` feature:**
The plan specified `features = ["tokio-runtime"]` as "implementer's best guess". After downloading msedge-tts 0.4, verified via `~/.cargo/registry/src/*/msedge-tts-0.4.*/Cargo.toml` that `tokio-runtime` is a valid feature in v0.4 (it activates `reqwest`, `async-tungstenite/tokio-runtime`, `tokio`, `futures-util`, `tokio-rustls`, and `rustls-platform-verifier`). No deviation — feature flag matches plan.

## D-08 / D-09 / D-10 / D-11 / D-12 Invariants

All negative grep checks pass:

- `grep -rc 'use ironhermes_tools' crates/ironhermes-core/src/` → 0 (core MUST NOT import tools)
- `grep -c 'msedge-tts\|rodio' crates/ironhermes-core/Cargo.toml` → 0 (core MUST NOT depend on provider crates)
- `grep -c 'fn stream\|fn list_voices\|fn list_models\|fn get_setup_schema\|fn default_voice\|fn default_model' crates/ironhermes-core/src/tts.rs` → 0 (D-09 DEFERRED items absent)
- `grep -c 'pub enum TtsError' crates/ironhermes-core/src/tts.rs` → 0 (D-11 — anyhow only)
- `cargo check --workspace` → exit 0

## Known Stubs

6 test stubs in `crates/ironhermes-tools/tests/tts_tools.rs` are intentionally `#[ignore]`d with explicit hand-off contracts:
- TTS-03, TTS-04, TTS-09 → PLAN 02 will un-ignore
- TTS-05, TTS-06 → PLAN 03 will un-ignore
- TTS-10 → PLAN 04 (live network, opt-in)

UAT gates 1-5 in `scripts/uat/phase-36.17.5-tts.sh` are wired stubs — PLAN 04 wires the real gates.

## Threat Surface Scan

No new LLM-callable attack surfaces in this plan. Files created:
- `tts.rs` — trait definition only; no network calls, no file I/O, no LLM input paths
- `tts_registry.rs` — test file only
- `tts_tools.rs` — test stubs only (all `#[ignore]`d, `unimplemented!()`)
- `phase-36.17.5-tts.sh` — UAT shell skeleton; no real synthesis calls yet

PLAN 03 (`TextToSpeechTool`) is where T-output-path (LLM `output_path` traversal) and T-text-length threats apply per the plan's threat model.

## Self-Check: PASSED

All created files exist and all commits verified:

| File | Status |
|------|--------|
| `crates/ironhermes-core/src/tts.rs` | FOUND |
| `crates/ironhermes-core/tests/tts_registry.rs` | FOUND |
| `crates/ironhermes-tools/tests/tts_tools.rs` | FOUND |
| `scripts/uat/phase-36.17.5-tts.sh` | FOUND |
| `.planning/phases/36.17.5-.../36.17.5-01-SUMMARY.md` | FOUND |

Commits:
- `6866aee1` chore(36.17.5-01): record operator approval for msedge-tts 0.4
- `c5b6b8a0` chore(36.17.5-01): record operator approval for rodio 0.22.2
- `460210b9` chore(36.17.5-01): add msedge-tts 0.4 + rodio 0.22.2 workspace deps
- `b9d5b018` feat(36.17.5-01): add TtsProvider trait + TtsRegistry + BUILTIN_TTS_NAMES
- `cb4ebea6` feat(36.17.5-01): add TtsConfig + sub-structs; add voice to ALL_TOOLSETS
- `d379de65` test(36.17.5-01): add Wave 0 registry tests TTS-01 TTS-02 TTS-07 TTS-08
- `a2ee60f7` feat(36.17.5-01): add Wave 0 tool test stubs + UAT script skeleton
