---
phase: 36.17.5
plan: 03
type: execute
wave: 3
depends_on:
  - 36.17.5-02
files_modified:
  - crates/ironhermes-tools/src/tts_tool.rs
  - crates/ironhermes-tools/src/send_audio_tool.rs
  - crates/ironhermes-tools/src/lib.rs
  - crates/ironhermes-tools/src/registry.rs
  - crates/ironhermes-tools/Cargo.toml
  - crates/ironhermes-agent/src/app_runtime_factory.rs
  - crates/ironhermes-tools/tests/tts_tools.rs
autonomous: true
requirements:
  - D-05
  - D-06
  - D-07
  - D-13
  - D-14
  - D-15
  - D-16
threats:
  - T-output-path
  - T-platform-unsupported

must_haves:
  truths:
    - "text_to_speech LLM tool exists, returns the file path as a string (D-06), reads provider from config.tts.provider (D-07)"
    - "send_audio LLM tool exists, dispatches via constructor-injected SessionKey.platform (D-14, D-15)"
    - "LLM-supplied output_path values are CLAMPED to $IRONHERMES_HOME/audio_cache/ via canonicalize+starts_with — T-output-path BLOCKING mitigation"
    - "register_tts_tools method on ToolRegistry takes SessionKey + Option<Arc<TelegramAdapter>> + Arc<Config> and registers BOTH tools"
    - "build_app_runtime_bundle wires register_tts_tools per-session via two new optional fields on AppRuntimeFactoryInput"
    - "Telegram dispatch routes .ogg/.opus to send_voice, .mp3 (ffmpeg present) → convert+send_voice, .mp3 (ffmpeg absent) → send_audio (D-04, D-13)"
    - "Audio cache dir auto-created on first synthesis call via create_dir_all (D-16, idempotent)"
  artifacts:
    - path: "crates/ironhermes-tools/src/tts_tool.rs"
      provides: "TextToSpeechTool LLM tool implementing Tool trait, toolset='voice'"
      contains: "impl Tool for TextToSpeechTool"
    - path: "crates/ironhermes-tools/src/send_audio_tool.rs"
      provides: "SendAudioTool LLM tool with constructor-injected SessionKey + TelegramAdapter"
      contains: "impl Tool for SendAudioTool"
    - path: "crates/ironhermes-tools/src/registry.rs"
      provides: "register_tts_tools(SessionKey, Option<Arc<TelegramAdapter>>, Arc<Config>) method"
      contains: "pub fn register_tts_tools"
    - path: "crates/ironhermes-agent/src/app_runtime_factory.rs"
      provides: "AppRuntimeFactoryInput extension fields + register_tts_tools call"
      contains: "pub session_key: Option<SessionKey>"
  key_links:
    - from: "crates/ironhermes-tools/src/registry.rs::register_tts_tools"
      to: "crates/ironhermes-tools/src/tts/mod.rs::build_tts_registry"
      via: "function call wiring config.tts into the registry"
      pattern: "build_tts_registry"
    - from: "crates/ironhermes-tools/src/send_audio_tool.rs"
      to: "crates/ironhermes-gateway/src/telegram.rs::TelegramAdapter::{send_voice, send_audio}"
      via: "constructor-injected Arc<TelegramAdapter>"
      pattern: "TelegramAdapter"
    - from: "crates/ironhermes-agent/src/app_runtime_factory.rs"
      to: "crates/ironhermes-tools/src/registry.rs::register_tts_tools"
      via: "method call inside build_app_runtime_bundle (line ~89 analog)"
      pattern: "register_tts_tools"
    - from: "crates/ironhermes-tools/src/tts_tool.rs::execute"
      to: "get_hermes_home().join('audio_cache')"
      via: "canonicalize + starts_with clamp for LLM-supplied output_path"
      pattern: "canonicalize"
---

<objective>
Land the two LLM-callable tools that consume PLAN 02's providers: TextToSpeechTool (D-05,
D-06, D-07 — single tool, file-out + path-return, args = text + optional output_path) and
SendAudioTool (D-14, D-15 — companion dispatch tool with constructor-injected SessionKey
and Option<TelegramAdapter>). BLOCKING security gate: clamp LLM-supplied output_path to
$IRONHERMES_HOME/audio_cache/ via canonicalize + starts_with to mitigate T-output-path
(HIGH severity per the security_threat_model_required section of the orchestrator brief).
Also extend AppRuntimeFactoryInput with two additive optional fields (session_key,
telegram_adapter) so build_app_runtime_bundle can wire register_tts_tools per-session;
add a sibling Cargo dep on ironhermes-gateway from ironhermes-tools (NOT a circular dep
because gateway already depends on tools — wait, verify; if circular, refactor SendAudioTool
to receive an Arc<dyn AudioDispatcher> trait object that gateway implements). Update the
RESEARCH-cited app_runtime_factory.rs invocation at line ~89 to call register_tts_tools.

Purpose: This is the final wiring plan. After PLAN 03 ships, the agent will see two new
tools in its catalog (text_to_speech + send_audio), the LLM can synthesize speech on Edge
or ElevenLabs by name, write to audio_cache, and dispatch to either rodio (CLI) or
Telegram (gateway). PLAN 04 then runs the operator UAT.

Output: A merged develop HEAD where:
- cargo build --workspace exits 0
- cargo test -p ironhermes-tools --test tts_tools shows 5 passed + 1 ignored (TTS-10 live network is owned by PLAN 04)
- An invariant test asserts text_to_speech AND send_audio appear in ToolRegistry::get_definitions output after register_tts_tools fires
- The output_path traversal mitigation is locked by a unit test that passes /etc/passwd as output_path and asserts the tool returns Err with a message about audio_cache containment
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-CONTEXT.md
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-RESEARCH.md
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-PATTERNS.md
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-VALIDATION.md
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-01-SUMMARY.md
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-02-SUMMARY.md

<interfaces>
PLAN 01 / 02 froze these. PLAN 03 consumes them verbatim.

Tool trait (from registry.rs lines 24–80):

  #[async_trait]
  pub trait Tool: Send + Sync {
      fn name(this) -> str_ref;
      fn toolset(this) -> str_ref;
      fn description(this) -> str_ref;
      fn schema(this) -> ToolSchema;
      fn is_available(this) -> bool { /* walks prerequisites by default */ }
      fn prerequisites(this) -> Vec<Prerequisite> { vec![] }
      fn redact_args(this, raw: ref serde_json::Value) -> serde_json::Value { raw.clone() }
      fn on_session_end(this) {}
      async fn execute(this, args: serde_json::Value) -> anyhow::Result<String>;
  }

(Note: above uses `this` and `str_ref` as stand-ins for `&self` and `&str` to avoid
literal ampersand-encoding issues; executor reads the real signatures from registry.rs.)

TtsProvider + TtsRegistry + build_tts_registry from PLAN 01 / 02.

ToolSchema constructor pattern (web_search.rs lines 45–65):

  ToolSchema::new(
      "tool_name",
      "Description sentence.",
      serde_json::json!({
          "type": "object",
          "properties": {
              "field": { "type": "string", "description": "..." }
          },
          "required": ["field"]
      }),
  )

register_cronjob_tool pattern from registry.rs (lines 631–636 — closest analog for
register_tts_tools constructor-injection shape):

  pub fn register_cronjob_tool(this_mut, store: Arc<Mutex<JobStore>>) {
      use crate::cronjob_tool::CronjobTool;
      self.register(Box::new(CronjobTool::new(store)));
  }

build_app_runtime_bundle injection site (app_runtime_factory.rs line 89):

  let cron_dir = get_hermes_home().join("cron");
  let job_store = Arc::new(Mutex::new(JobStore::open(cron_dir)?));
  registry.register_cronjob_tool(job_store.clone());
  // ← register_tts_tools wires in immediately after this block

AppRuntimeFactoryInput current shape (app_runtime_factory.rs lines 35–44 — verbatim):

  pub struct AppRuntimeFactoryInput {
      pub config: Arc<Config>,
      pub resolver: Arc<ProviderResolver>,
      pub cwd: PathBuf,
      pub process_registry: Arc<RwLock<ProcessRegistry>>,
      pub memory_manager: Option<SharedMemoryManager>,
      pub delegate_task: Option<DelegateTaskWiring>,
      pub hooks_config: HooksConfig,
      pub emit_mcp_startup_logs: bool,
  }

Two ADDITIVE optional fields to land:

  pub session_key: Option<ironhermes_core::SessionKey>,        // None for global / startup;
                                                                // Some(...) for per-session
  pub telegram_adapter: Option<Arc<TtsDispatchAdapter>>,        // Option-typed Arc; None for
                                                                // CLI/Local-only sessions

(See Task 1 below on the AudioDispatcher trait choice: ironhermes-gateway already depends
on ironhermes-tools, so we CANNOT make tools depend on gateway directly. Solution: define
an `AudioDispatcher` trait in ironhermes-tools::send_audio_tool, implement it for
TelegramAdapter inside ironhermes-gateway, and have AppRuntimeFactoryInput carry
Option<Arc<dyn AudioDispatcher>>. The gateway runner constructs the Arc with its existing
TelegramAdapter and passes it through.)

Telegram dispatch surface (already in place from Phase 36.17.2.2 — verified at
telegram.rs:26 and telegram.rs:258):

  // TelegramAdapter inherent / trait methods:
  async fn send_voice(this, chat_id: str_ref, path: path_ref, thread_id: Option<str_ref>)
      -> anyhow::Result<()>
  pub async fn send_audio(this, chat_id: str_ref, path: path_ref, thread_id: Option<str_ref>)
      -> anyhow::Result<()>

D-15 dispatch decision tree per SessionKey.platform:

  Platform::Local      → rodio playback (loaded MP3 file → default audio sink)
  Platform::Telegram   → if path ends with .ogg | .opus → adapter.send_voice
                         else if .mp3 AND ffmpeg_available() → ffmpeg convert → send_voice
                         else if .mp3 AND !ffmpeg_available()  → adapter.send_audio
  any other variant    → anyhow::bail!("send_audio not yet wired for {platform}")

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AudioDispatcher trait + crate-dep verification + extend AppRuntimeFactoryInput</name>
  <files>crates/ironhermes-tools/src/send_audio_tool.rs, crates/ironhermes-tools/src/lib.rs, crates/ironhermes-agent/src/app_runtime_factory.rs</files>
  <read_first>
    - crates/ironhermes-agent/src/app_runtime_factory.rs lines 35–110 (AppRuntimeFactoryInput shape + build_app_runtime_bundle body up to and past the register_cronjob_tool line at 89)
    - crates/ironhermes-tools/Cargo.toml (verify whether ironhermes-gateway is currently a dep — it must NOT be; gateway depends on tools, not the reverse)
    - crates/ironhermes-gateway/Cargo.toml (confirm `ironhermes-tools = { workspace = true }` or similar — verifies the direction is gateway → tools)
    - crates/ironhermes-gateway/src/telegram.rs lines 26–41 (send_voice signature) and 258–264 (send_audio signature) — both already exist from Phase 36.17.2.2
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-RESEARCH.md § "Pitfall 4: SendAudioTool Receiving Wrong SessionKey" (constructor-injection per-session contract)
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-CONTEXT.md D-15 (single platform-agnostic tool, SessionKey-driven dispatch)
  </read_first>
  <action>
    Step 1: Run `cargo tree -p ironhermes-tools 2>and 1 | head -30` to confirm
    ironhermes-tools does NOT depend on ironhermes-gateway. If it does, abort with a note
    in the SUMMARY and request guidance — the rest of this plan assumes the standard
    direction (gateway → tools). If it does NOT depend on gateway (expected case), proceed.

    Step 2: Create `crates/ironhermes-tools/src/send_audio_tool.rs` with:

    1. An `AudioDispatcher` trait (Send + Sync) declared near the top of the file:

         #[async_trait::async_trait]
         pub trait AudioDispatcher: Send + Sync {
             async fn send_voice_file(this, chat_id: str_ref, path: PathBuf_ref,
                                       thread_id: Option<str_ref>) -> anyhow::Result<()>;
             async fn send_audio_file(this, chat_id: str_ref, path: PathBuf_ref,
                                       thread_id: Option<str_ref>) -> anyhow::Result<()>;
         }

       Method names are deliberately distinct from TelegramAdapter's so the impl block in
       gateway is unambiguous. The trait abstracts "deliver audio to a chat" without
       importing TelegramAdapter into ironhermes-tools (which would require a dep cycle).

    2. A `SendAudioTool` struct:

         pub struct SendAudioTool {
             session_key: ironhermes_core::SessionKey,
             dispatcher: Option<Arc<dyn AudioDispatcher>>,
             config: Arc<ironhermes_core::Config>,
         }

         impl SendAudioTool {
             pub fn new(session_key: ironhermes_core::SessionKey,
                        dispatcher: Option<Arc<dyn AudioDispatcher>>,
                        config: Arc<ironhermes_core::Config>) -> Self {
                 Self { session_key, dispatcher, config }
             }
         }

    3. Tool trait impl (filled out in Task 3 — at the end of Task 1 the impl block exists
       with `unimplemented!()` bodies on schema / execute, but name / toolset / description
       return the correct strings):

         #[async_trait::async_trait]
         impl crate::registry::Tool for SendAudioTool {
             fn name(this) -> str_ref { "send_audio" }
             fn toolset(this) -> str_ref { "voice" }
             fn description(this) -> str_ref {
                 "Send a previously-synthesized audio file to the current platform's reply path."
             }
             fn schema(this) -> ironhermes_core::ToolSchema { unimplemented!("Task 3") }
             async fn execute(this, _args: serde_json::Value)
                 -> anyhow::Result<String> { unimplemented!("Task 3") }
         }

    Step 3: In `crates/ironhermes-tools/src/lib.rs`, add `pub mod send_audio_tool;`
    alphabetically (between `pub mod registry;` and `pub mod skill_manage;`) and
    `pub use send_audio_tool::{SendAudioTool, AudioDispatcher};` near the existing
    `pub use registry::{...}` block.

    Step 4: Extend `AppRuntimeFactoryInput` in `crates/ironhermes-agent/src/app_runtime_factory.rs`
    with two additive optional fields at the END of the struct:

         /// Phase 36.17.5 D-15: per-session SessionKey for SendAudioTool dispatch.
         /// None at startup / global construction; Some when build_app_runtime_bundle is
         /// called per-session (gateway handler, CLI run_chat init).
         pub session_key: Option<ironhermes_core::SessionKey>,
         /// Phase 36.17.5 D-13 / D-15: gateway-supplied audio dispatcher for the Telegram arm.
         /// None for CLI/Local-only paths.
         pub telegram_adapter: Option<Arc<dyn ironhermes_tools::AudioDispatcher>>,

    Step 5: Update the THREE existing `AppRuntimeFactoryInput {...}` literal blocks in
    app_runtime_factory.rs (lines 317, 513, 573, 624 per the grep at line 13) — each
    test's input construction needs the two new fields filled with `None`. The
    `default_input()` function at line 311 sets both to None; the other three sites
    follow the same default. No production caller (agent_runtime.rs:198) is touched in
    Task 1 — Task 4 of this plan threads the value through.

    Do NOT yet add the register_tts_tools method call inside build_app_runtime_bundle —
    Task 4 does that after register_tts_tools exists in Task 2. Do NOT yet implement the
    execute body of SendAudioTool — Task 3 does that.
  </action>
  <verify>
    <automated>cargo tree -p ironhermes-tools 2>and 1 | tee /tmp/36.17.5-03-t1-tree.log; ! grep -q '^ironhermes-gateway' /tmp/36.17.5-03-t1-tree.log; cargo build -p ironhermes-tools -p ironhermes-agent 2>and 1 | tee /tmp/36.17.5-03-t1-build.log; ! grep -E '^error' /tmp/36.17.5-03-t1-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `cargo tree -p ironhermes-tools | grep -c '^ironhermes-gateway'` returns 0 (no circular dep)
    - `grep -c 'pub trait AudioDispatcher' crates/ironhermes-tools/src/send_audio_tool.rs` returns 1
    - `grep -c 'pub struct SendAudioTool' crates/ironhermes-tools/src/send_audio_tool.rs` returns 1
    - `grep -c '^pub mod send_audio_tool;' crates/ironhermes-tools/src/lib.rs` returns 1
    - `grep -c 'pub use send_audio_tool::{SendAudioTool, AudioDispatcher}' crates/ironhermes-tools/src/lib.rs` returns 1
    - `grep -c 'pub session_key: Option<ironhermes_core::SessionKey>' crates/ironhermes-agent/src/app_runtime_factory.rs` returns 1
    - `grep -c 'pub telegram_adapter: Option<Arc<dyn ironhermes_tools::AudioDispatcher>>' crates/ironhermes-agent/src/app_runtime_factory.rs` returns 1
    - `cargo build -p ironhermes-tools -p ironhermes-agent` exits 0 (all test literal-init sites updated)
  </acceptance_criteria>
  <done>AudioDispatcher trait + SendAudioTool struct + lib.rs re-exports land; AppRuntimeFactoryInput has the two additive optional fields; no circular dep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TextToSpeechTool impl + T-output-path BLOCKING clamp + register_tts_tools registry method</name>
  <files>crates/ironhermes-tools/src/tts_tool.rs, crates/ironhermes-tools/src/lib.rs, crates/ironhermes-tools/src/registry.rs, crates/ironhermes-tools/tests/tts_tools.rs</files>
  <read_first>
    - crates/ironhermes-tools/src/web_search.rs (full file — Tool trait impl shape verbatim)
    - crates/ironhermes-core/src/constants.rs lines 63–70 (get_hermes_home() — used to resolve audio_cache base)
    - crates/ironhermes-tools/src/registry.rs lines 631–636 (register_cronjob_tool analog for the new register_tts_tools method)
    - crates/ironhermes-tools/src/tts/mod.rs (build_tts_registry factory from PLAN 02 Task 1)
    - crates/ironhermes-tools/tests/tts_tools.rs (TTS-05, TTS-06 stubs to un-ignore)
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-RESEARCH.md § "Pattern 8: TextToSpeechTool execute()" + § "Security Domain" (T-output-path mitigation)
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-PATTERNS.md § crates/ironhermes-tools/src/tts_tool.rs
  </read_first>
  <behavior>
    - TTS-05 `test_tts_tool_metadata`: construct TextToSpeechTool with a default Config; assert `tool.name() == "text_to_speech"`, `tool.toolset() == "voice"`, and the schema has a `text` required field plus an `output_path` optional field. Locks D-05 / D-06 / D-07.
    - TTS-06 `test_tts_tool_creates_audio_cache_dir`: lock env_lock(), set IRONHERMES_HOME to a TempDir, construct the tool, call execute({"text": "x"}) — assert `audio_cache/` exists under the TempDir afterward. Since Edge synth requires live network this test ALSO un-ignores TTS-10's contract requirement that "the tool returns Ok in the synth path" — for TTS-06 the actual network call may fail; the test asserts only the directory side-effect (create_dir_all is idempotent and runs BEFORE the synth call). Either: (a) substitute the TtsRegistry under test with a FakeProvider that returns Ok(output_path.clone()) without doing network I/O, or (b) accept Err on the network call and grep the error message for "Edge TTS" rather than asserting Ok. Choose (a) — construct TextToSpeechTool::new_with_registry(config, fake_registry) where the test-only `new_with_registry` constructor accepts a pre-built TtsRegistry containing one FakeProvider named "edge".
    - new test `test_output_path_traversal_blocked`: lock env_lock, set IRONHERMES_HOME to TempDir, construct tool, call execute({"text": "x", "output_path": "/etc/passwd"}) — assert Result is Err and the error message contains "audio_cache". Locks T-output-path BLOCKING mitigation per the orchestrator's security_threat_model_required brief.
    - new test `test_register_tts_tools_registers_both`: construct empty ToolRegistry, call register_tts_tools with a default Config, SessionKey::new(Platform::Local, "test"), None for dispatcher; then assert the get_definitions output contains both "text_to_speech" and "send_audio". Locks D-08 wiring.
  </behavior>
  <action>
    Step 1: Create `crates/ironhermes-tools/src/tts_tool.rs` with:

    1. Imports per PATTERNS.md (use async_trait::async_trait; use ironhermes_core::ToolSchema;
       use crate::registry::Tool; use std::path::PathBuf; use std::sync::Arc;
       use serde_json::json;).

    2. The struct:

         pub struct TextToSpeechTool {
             config: Arc<ironhermes_core::Config>,
             registry: Arc<ironhermes_core::TtsRegistry>,
         }

         impl TextToSpeechTool {
             pub fn new(config: Arc<ironhermes_core::Config>,
                        registry: Arc<ironhermes_core::TtsRegistry>) -> Self {
                 Self { config, registry }
             }
         }

    3. `#[async_trait]` impl Tool for TextToSpeechTool with:
       - name → "text_to_speech"
       - toolset → "voice"
       - description → "Synthesize text to an audio file via the configured TTS provider. Returns the path to the audio file."
       - schema → ToolSchema::new with the EXACT JSON shape:
           {
               "type": "object",
               "properties": {
                   "text": { "type": "string",
                             "description": "Text to synthesize (Edge caps at 5000 chars, ElevenLabs at 10000)." },
                   "output_path": { "type": "string",
                                     "description": "Optional output file path. Defaults to $IRONHERMES_HOME/audio_cache/<uuid>.<ext>. If provided, MUST be inside the audio_cache directory." }
               },
               "required": ["text"]
           }
         (D-07 — text required + output_path optional, no other args; voice / model / speed
         / output_format come from config and are DEFERRED from the LLM surface.)
       - is_available → return true if registry.get(config.tts.provider).is_some() (the
         configured provider is registered AND available); else false.

    4. execute body:
       a. Extract `text` from args (error if missing).
       b. Look up the provider by name from `self.config.tts.provider` via `self.registry.get(...)`.
          Error if absent.
       c. Resolve `audio_dir = ironhermes_core::get_hermes_home().join("audio_cache");`
          and `std::fs::create_dir_all(and audio_dir)?;` BEFORE the output_path branch
          (D-16 — idempotent dir creation on first call; create_dir_all is the safe variant
          per RESEARCH Pitfall 6).
       d. If args["output_path"] is Some(p_str):
          - Let `p = PathBuf::from(p_str)`. If parent doesn't exist, create it under
            audio_dir.
          - Compute `canonical_parent = std::fs::canonicalize(p.parent().unwrap_or(and audio_dir)).unwrap_or_else(|_| audio_dir.clone());`
          - Compute `audio_canonical = std::fs::canonicalize(and audio_dir).unwrap_or(audio_dir.clone());`
          - **T-output-path BLOCKING CHECK**: `if !canonical_parent.starts_with(and audio_canonical) { anyhow::bail!("output_path must be inside $IRONHERMES_HOME/audio_cache/"); }`
          - Use `p` as the output path.
          Else:
          - Determine ext: if provider name == "elevenlabs" and config.tts.elevenlabs.output_format == "opus" → "opus"; else "mp3".
          - `output_path = audio_dir.join(format!("{}.{}", uuid::Uuid::new_v4(), ext));`
       e. Call `provider.synthesize(text, and output_path).await?`.
       f. Return `Ok(output_path.to_string_lossy().to_string())` (D-06 — path string,
          NO base64, NO event side-channel).

    5. Add a `#[cfg(test)] impl TextToSpeechTool` block with `pub fn new_with_registry`
       constructor for the unit tests (accepts a TtsRegistry by value, wraps it in Arc).

    Step 2: In `crates/ironhermes-tools/src/lib.rs`, add `pub mod tts_tool;` alphabetically
    (between `pub mod terminal;` and `pub mod toolset_session;` — same neighborhood as the
    Task 1 `send_audio_tool` insertion). Add `pub use tts_tool::TextToSpeechTool;` re-export.

    Step 3: Add a new method to ToolRegistry impl in `crates/ironhermes-tools/src/registry.rs`,
    immediately after `register_cronjob_tool` (around line 636):

         /// Phase 36.17.5: register text_to_speech + send_audio LLM tools.
         ///
         /// Called per-session because SendAudioTool's SessionKey + Telegram dispatcher
         /// are injected at construction time (D-14 / D-15). The TextToSpeechTool itself
         /// is session-independent but registered alongside for symmetry.
         ///
         /// `dispatcher: None` is correct for Platform::Local / CLI paths; the Local arm
         /// in SendAudioTool::execute plays locally via rodio without needing a dispatcher.
         pub fn register_tts_tools(
             this_mut,
             session_key: ironhermes_core::SessionKey,
             dispatcher: Option<Arc<dyn crate::AudioDispatcher>>,
             config: Arc<ironhermes_core::Config>,
         ) {
             use crate::tts::build_tts_registry;
             use crate::tts_tool::TextToSpeechTool;
             use crate::send_audio_tool::SendAudioTool;

             let tts_registry = Arc::new(build_tts_registry(and config.tts));
             self.register(Box::new(TextToSpeechTool::new(config.clone(), tts_registry)));
             self.register(Box::new(SendAudioTool::new(session_key, dispatcher, config)));
         }

    Step 4: Un-ignore TTS-05 + TTS-06 in `crates/ironhermes-tools/tests/tts_tools.rs` and
    fill in the test bodies per `<behavior>`. Add the two NEW tests
    (`test_output_path_traversal_blocked` and `test_register_tts_tools_registers_both`).

    Do NOT add a `voice` / `model` / `speed` / `output_format` arg to the tool schema (D-07
    DEFERRED). Do NOT base64-encode the audio in the return value (D-06 — path string only).
    Do NOT emit a side-channel event from execute (D-14 — explicit two-tool sequence).
  </action>
  <verify>
    <automated>cargo build -p ironhermes-tools 2>and 1 | tee /tmp/36.17.5-03-t2-build.log; cargo test -p ironhermes-tools --test tts_tools test_tts_tool_metadata test_tts_tool_creates_audio_cache_dir test_output_path_traversal_blocked test_register_tts_tools_registers_both 2>and 1 | tee /tmp/36.17.5-03-t2-tests.log; ! grep -E '^error' /tmp/36.17.5-03-t2-build.log and and  grep -E 'test result: ok' /tmp/36.17.5-03-t2-tests.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub struct TextToSpeechTool' crates/ironhermes-tools/src/tts_tool.rs` returns 1
    - `grep -c 'impl Tool for TextToSpeechTool' crates/ironhermes-tools/src/tts_tool.rs` returns 1
    - `grep -c 'audio_cache' crates/ironhermes-tools/src/tts_tool.rs` returns >= 2 (dir create + clamp message)
    - `grep -c 'canonicalize' crates/ironhermes-tools/src/tts_tool.rs` returns >= 2 (parent + base canonicalization for T-output-path)
    - `grep -c 'starts_with' crates/ironhermes-tools/src/tts_tool.rs` returns >= 1 (clamp predicate)
    - `grep -c 'anyhow::bail!' crates/ironhermes-tools/src/tts_tool.rs` returns >= 1 (T-output-path BLOCKING fail-closed)
    - `grep -c '"voice"' crates/ironhermes-tools/src/tts_tool.rs` returns >= 1 (toolset)
    - `grep -c '"text_to_speech"' crates/ironhermes-tools/src/tts_tool.rs` returns >= 1
    - `grep -c '^pub mod tts_tool;' crates/ironhermes-tools/src/lib.rs` returns 1
    - `grep -c 'pub fn register_tts_tools' crates/ironhermes-tools/src/registry.rs` returns 1
    - `cargo test -p ironhermes-tools --test tts_tools test_tts_tool_metadata` exits 0
    - `cargo test -p ironhermes-tools --test tts_tools test_tts_tool_creates_audio_cache_dir` exits 0
    - `cargo test -p ironhermes-tools --test tts_tools test_output_path_traversal_blocked` exits 0
    - `cargo test -p ironhermes-tools --test tts_tools test_register_tts_tools_registers_both` exits 0
    - `grep -c '#\[ignore' crates/ironhermes-tools/tests/tts_tools.rs` returns 1 (only TTS-10 live network remains)
    - `cargo build -p ironhermes-tools` exits 0 with no warnings
  </acceptance_criteria>
  <done>TextToSpeechTool implements the D-05/D-06/D-07 surface; T-output-path traversal is BLOCKED via canonicalize+starts_with and locked by unit test; register_tts_tools method exists and wires both tools; TTS-05 + TTS-06 are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SendAudioTool execute body + D-15 dispatch tree + ffmpeg MP3-to-Opus subprocess</name>
  <files>crates/ironhermes-tools/src/send_audio_tool.rs, crates/ironhermes-tools/Cargo.toml, crates/ironhermes-tools/tests/tts_tools.rs</files>
  <read_first>
    - crates/ironhermes-tools/src/send_audio_tool.rs (Task 1 skeleton)
    - crates/ironhermes-tools/src/tts/mod.rs (ffmpeg_available + ffmpeg_available_for_test from PLAN 02)
    - crates/ironhermes-core/src/types.rs (Platform enum — Local + Telegram variants used by D-15 dispatch)
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-RESEARCH.md § "Pattern 7: ffmpeg MP3 → Opus Conversion" + § "Pattern 5: SendAudioTool Constructor Injection" + § "Pitfall 1: Telegram sendVoice Codec/Container Confusion"
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-CONTEXT.md D-13 / D-14 / D-15
  </read_first>
  <behavior>
    - new test `test_send_audio_tool_metadata`: assert name=="send_audio", toolset=="voice", schema has a `path` required string field. Locks D-14 single-tool shape.
    - new test `test_send_audio_local_arm_plays_or_errors`: construct SendAudioTool with SessionKey::new(Platform::Local, "test") + None dispatcher. Pass a tempfile MP3 path that doesn't actually exist. Assert Result is Err (rodio cannot decode), but the error MUST be an anyhow error message NOT a panic. Locks D-15 Platform::Local arm "never panic" property.
    - new test `test_send_audio_telegram_no_adapter_errors`: SendAudioTool with SessionKey::new(Platform::Telegram, "chat123") + None dispatcher. Pass any path. Assert Err with message containing "adapter" or "dispatcher" — never panic. Locks the Telegram-arm-needs-dispatcher invariant.
    - new test `test_send_audio_unsupported_platform_bails`: SendAudioTool with SessionKey::new(Platform::Discord, "chat123") + Some(mock_dispatcher). Assert Err with message containing "not yet wired" or "discord" (D-15 default branch).
  </behavior>
  <action>
    Step 1: Add `rodio = { workspace = true }` to `crates/ironhermes-tools/Cargo.toml`
    [dependencies] block IF not already there from PLAN 01 (PLAN 01 added it at the
    workspace level and the crate level — re-verify; if absent at crate level add it).

    Step 2: Flesh out `crates/ironhermes-tools/src/send_audio_tool.rs` execute body:

    1. Update the Tool trait impl's schema and execute methods:

         fn schema(this) -> ToolSchema {
             ToolSchema::new(
                 "send_audio",
                 "Send a previously-synthesized audio file to the current platform.",
                 serde_json::json!({
                     "type": "object",
                     "properties": {
                         "path": { "type": "string",
                                   "description": "Absolute path to the audio file (typically returned by text_to_speech)." }
                     },
                     "required": ["path"]
                 }),
             )
         }

    2. execute:

         async fn execute(this, args: serde_json::Value) -> anyhow::Result<String> {
             let path_str = args["path"].as_str()
                 .ok_or_else(|| anyhow::anyhow!("Missing required parameter: path"))?;
             let path = std::path::PathBuf::from(path_str);
             if !path.exists() {
                 anyhow::bail!("Audio file does not exist: {}", path.display());
             }

             let ext = path.extension().and_then(|e| e.to_str())
                 .unwrap_or("").to_lowercase();

             match self.session_key.platform {
                 ironhermes_core::types::Platform::Local => {
                     // D-15 Local arm: rodio playback. ffmpeg not required — rodio
                     // handles MP3 + OGG via the `mp3` feature.
                     let file = std::fs::File::open(and path)
                         .map_err(|e| anyhow::anyhow!("Failed to open audio file: {}", e))?;
                     let buf = std::io::BufReader::new(file);
                     let (_stream, handle) = rodio::OutputStream::try_default()
                         .map_err(|e| anyhow::anyhow!("No audio device: {}", e))?;
                     let sink = rodio::Sink::try_new(and handle)
                         .map_err(|e| anyhow::anyhow!("Failed to open sink: {}", e))?;
                     let source = rodio::Decoder::new(buf)
                         .map_err(|e| anyhow::anyhow!("Failed to decode audio: {}", e))?;
                     sink.append(source);
                     sink.sleep_until_end();
                     Ok(format!("played: {}", path.display()))
                 }
                 ironhermes_core::types::Platform::Telegram => {
                     let dispatcher = self.dispatcher.as_ref().ok_or_else(|| anyhow::anyhow!(
                         "send_audio on Telegram requires an AudioDispatcher (none attached this session)"
                     ))?;
                     match ext.as_str() {
                         "ogg" | "opus" => {
                             dispatcher.send_voice_file(and self.session_key.chat_id,
                                                         and path, None).await?;
                             Ok(format!("sent voice: {}", path.display()))
                         }
                         "mp3" if crate::tts::ffmpeg_available() => {
                             // D-04 + Pitfall 1: MP3 → OGG/opus via ffmpeg subprocess
                             let opus_path = path.with_extension("ogg");
                             let status = tokio::process::Command::new(
                                 self.config.tts.ffmpeg_path.as_deref().unwrap_or("ffmpeg"))
                                 .args(["-y", "-i"]).arg(and path)
                                 .args(["-c:a", "libopus", "-b:a", "64k",
                                        "-ar", "48000", "-ac", "1"])
                                 .arg(and opus_path)
                                 .status().await
                                 .map_err(|e| anyhow::anyhow!("ffmpeg spawn failed: {}", e))?;
                             if !status.success() {
                                 anyhow::bail!("ffmpeg conversion failed with status {}", status);
                             }
                             dispatcher.send_voice_file(and self.session_key.chat_id,
                                                         and opus_path, None).await?;
                             Ok(format!("converted+sent voice: {}", opus_path.display()))
                         }
                         "mp3" => {
                             dispatcher.send_audio_file(and self.session_key.chat_id,
                                                         and path, None).await?;
                             Ok(format!("sent audio (no ffmpeg): {}", path.display()))
                         }
                         other => anyhow::bail!(
                             "send_audio Telegram arm: unsupported extension '{}' (need .mp3, .ogg, .opus)",
                             other),
                     }
                 }
                 other => anyhow::bail!(
                     "send_audio not yet wired for platform: {:?}", other),
             }
         }

    3. Also implement a `#[cfg(test)]` mock AudioDispatcher inside the test module of
       tts_tools.rs (or in a new `#[cfg(test)] pub mod test_support` block inside
       send_audio_tool.rs) — RecordingDispatcher with `calls: Arc<Mutex<Vec<(String, String, PathBuf)>>>`
       so the Telegram-arm tests can assert which method was called without spinning up
       a real adapter.

    Step 3: Implement `AudioDispatcher for ironhermes_gateway::TelegramAdapter` — wait,
    this CANNOT live in ironhermes-tools because TelegramAdapter is defined in
    ironhermes-gateway. The impl block must live in ironhermes-gateway because of the
    orphan rule (and to keep tools from depending on gateway). Add this impl to
    `crates/ironhermes-gateway/src/telegram.rs` at the bottom:

         #[async_trait::async_trait]
         impl ironhermes_tools::AudioDispatcher for TelegramAdapter {
             async fn send_voice_file(this, chat_id: str_ref, path: PathBuf_ref,
                                       thread_id: Option<str_ref>) -> anyhow::Result<()> {
                 self.send_voice(chat_id, path.as_path(), thread_id).await
             }
             async fn send_audio_file(this, chat_id: str_ref, path: PathBuf_ref,
                                       thread_id: Option<str_ref>) -> anyhow::Result<()> {
                 self.send_audio(chat_id, path.as_path(), thread_id).await
             }
         }

       Add this impl block at the END of telegram.rs (after the existing impls). It does
       NOT modify TelegramAdapter's existing API; it adds a NEW trait impl. The single-tool
       gateway crate test suite stays green (verify with `cargo test -p ironhermes-gateway`).

    Step 4: Un-ignore the SendAudio tests in tts_tools.rs and add the four NEW tests per
    `<behavior>`. The Telegram-arm tests use `RecordingDispatcher` constructed inline.

    Do NOT call `JoinHandle::abort` from any path. Do NOT spawn a separate thread for
    rodio playback (the test paths use small files; production agent turns block on
    playback intentionally per D-15). Do NOT log the file CONTENTS (only the path). Do NOT
    add a Discord arm (D-15 — Discord deferred).
  </action>
  <verify>
    <automated>cargo build -p ironhermes-tools -p ironhermes-gateway 2>and 1 | tee /tmp/36.17.5-03-t3-build.log; cargo test -p ironhermes-tools --test tts_tools test_send_audio 2>and 1 | tee /tmp/36.17.5-03-t3-test.log; ! grep -E '^error' /tmp/36.17.5-03-t3-build.log and and  grep -E 'test result: ok' /tmp/36.17.5-03-t3-test.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'impl Tool for SendAudioTool' crates/ironhermes-tools/src/send_audio_tool.rs` returns 1
    - `grep -c '"send_audio"' crates/ironhermes-tools/src/send_audio_tool.rs` returns >= 1
    - `grep -c '"voice"' crates/ironhermes-tools/src/send_audio_tool.rs` returns >= 1 (toolset)
    - `grep -c 'Platform::Local' crates/ironhermes-tools/src/send_audio_tool.rs` returns >= 1
    - `grep -c 'Platform::Telegram' crates/ironhermes-tools/src/send_audio_tool.rs` returns >= 1
    - `grep -c 'rodio::' crates/ironhermes-tools/src/send_audio_tool.rs` returns >= 2 (OutputStream + Decoder)
    - `grep -c 'libopus' crates/ironhermes-tools/src/send_audio_tool.rs` returns 1 (D-04 ffmpeg flags)
    - `grep -c 'send_voice_file\|send_audio_file' crates/ironhermes-tools/src/send_audio_tool.rs` returns >= 4 (dispatch tree)
    - `grep -c 'impl ironhermes_tools::AudioDispatcher for TelegramAdapter' crates/ironhermes-gateway/src/telegram.rs` returns 1
    - `cargo test -p ironhermes-tools --test tts_tools test_send_audio_tool_metadata` exits 0
    - `cargo test -p ironhermes-tools --test tts_tools test_send_audio_local_arm_plays_or_errors` exits 0
    - `cargo test -p ironhermes-tools --test tts_tools test_send_audio_telegram_no_adapter_errors` exits 0
    - `cargo test -p ironhermes-tools --test tts_tools test_send_audio_unsupported_platform_bails` exits 0
    - `cargo test -p ironhermes-gateway` exits 0 (no regression from the AudioDispatcher impl in telegram.rs)
    - `cargo build -p ironhermes-tools -p ironhermes-gateway` exits 0
  </acceptance_criteria>
  <done>SendAudioTool::execute fully implements the D-15 dispatch tree; Local arm uses rodio; Telegram arm uses the injected AudioDispatcher with the .ogg/.opus | .mp3+ffmpeg | .mp3 no-ffmpeg branches; TelegramAdapter implements AudioDispatcher (orphan-rule-safe).</done>
</task>

<task type="auto">
  <name>Task 4: Wire register_tts_tools call inside build_app_runtime_bundle</name>
  <files>crates/ironhermes-agent/src/app_runtime_factory.rs</files>
  <read_first>
    - crates/ironhermes-agent/src/app_runtime_factory.rs lines 61–110 (build_app_runtime_bundle body — register_cronjob_tool at line 89 is the insertion neighbor)
    - crates/ironhermes-tools/src/registry.rs (the register_tts_tools method from Task 2)
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-RESEARCH.md § "Pattern 5: SendAudioTool Constructor Injection" (per-session injection contract)
  </read_first>
  <action>
    Inside `build_app_runtime_bundle` in `crates/ironhermes-agent/src/app_runtime_factory.rs`,
    immediately after the existing `registry.register_cronjob_tool(job_store.clone());` line
    (~line 89), add:

         // Phase 36.17.5 D-15: TTS tools — registered only when this bundle is built
         // for a concrete session. session_key is None at startup / global construction,
         // in which case the tools are not exposed to the agent that turn (D-14 — tools
         // are tied to the agent loop, not the runtime).
         if let Some(ref session_key) = input.session_key {
             registry.register_tts_tools(
                 session_key.clone(),
                 input.telegram_adapter.clone(),
                 input.config.clone(),
             );
         }

    Update `default_input()` (line ~311) to set both new fields to None so the existing
    tests stay green without modification. The four existing test-site literal-init
    blocks (lines 317, 513, 573, 624 per the Task 1 grep) need both fields set to None
    — Task 1 already did this; verify with a re-grep.

    Update agent_runtime.rs's call site (line ~198) to pass `session_key: None` and
    `telegram_adapter: None` for the v1 wiring. This is correct because `AgentRuntime`
    is per-AgentRuntime not per-turn — the TTS-tools-per-session pattern requires a
    follow-up wiring phase to thread SessionKey down from `run_turn(TurnRequest)`. For v1
    PLAN 03 ships the INFRASTRUCTURE (additive fields + register_tts_tools), and PLAN 04
    UAT plus a per-turn wiring follow-up phase will land later.

    Add an invariant test in `crates/ironhermes-agent/tests/invariants_36_17_5.rs` (new
    file):

         //! Phase 36.17.5 — locks the build_app_runtime_bundle TTS wiring.

         #[test]
         fn build_app_runtime_bundle_source_calls_register_tts_tools() {
             const SRC: and str = include_str!("../src/app_runtime_factory.rs");
             assert!(
                 SRC.contains("registry.register_tts_tools("),
                 "Phase 36.17.5: build_app_runtime_bundle must invoke register_tts_tools \
                  (look near the register_cronjob_tool call site at line ~89)"
             );
             assert!(
                 SRC.contains("session_key: Option<ironhermes_core::SessionKey>"),
                 "Phase 36.17.5: AppRuntimeFactoryInput must carry session_key field"
             );
             assert!(
                 SRC.contains("telegram_adapter: Option<Arc<dyn ironhermes_tools::AudioDispatcher>>"),
                 "Phase 36.17.5: AppRuntimeFactoryInput must carry telegram_adapter field"
             );
         }

         #[test]
         fn agent_runtime_passes_session_key_none_for_v1() {
             const SRC: and str = include_str!("../src/agent_runtime.rs");
             assert!(
                 SRC.contains("session_key: None") || SRC.contains("session_key:None"),
                 "Phase 36.17.5: AgentRuntime::from_config must default session_key to None \
                  for v1 — per-turn threading lands in a follow-up phase"
             );
         }

    (The `and` in the const string above stands in for the Rust reference operator —
    executor writes the real signature.)

    Do NOT wire SessionKey down from `run_turn(TurnRequest)` in this phase. Do NOT modify
    the test-only `default_input()` to require session_key (back-compat: None is the
    default). Do NOT add a third additive field for "skip TTS tools" — the `Option`
    semantics already cover that case.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-agent 2>and 1 | tee /tmp/36.17.5-03-t4-build.log; cargo test -p ironhermes-agent --test invariants_36_17_5 2>and 1 | tee /tmp/36.17.5-03-t4-inv.log; cargo test --workspace 2>and 1 | tee /tmp/36.17.5-03-t4-ws.log | tail -3; ! grep -E '^error' /tmp/36.17.5-03-t4-build.log and and  grep -E 'test result: ok' /tmp/36.17.5-03-t4-inv.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'registry.register_tts_tools(' crates/ironhermes-agent/src/app_runtime_factory.rs` returns 1
    - `grep -c 'if let Some(ref session_key) = input.session_key' crates/ironhermes-agent/src/app_runtime_factory.rs` returns 1
    - `grep -c 'session_key: None' crates/ironhermes-agent/src/agent_runtime.rs` returns >= 1
    - `crates/ironhermes-agent/tests/invariants_36_17_5.rs` exists
    - `cargo test -p ironhermes-agent --test invariants_36_17_5` exits 0
    - `cargo test --workspace` shows zero NEW failures vs. the pre-merge baseline (pre-existing kanban end_to_end FAILures from STATE are acceptable; the count must not increase)
    - `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>build_app_runtime_bundle conditionally calls register_tts_tools when AppRuntimeFactoryInput.session_key is Some; agent_runtime.rs passes session_key: None for v1; invariant test locks both contracts; workspace test count baseline preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM `output_path` arg | Untrusted LLM-supplied path crossing into local disk via std::fs::write/canonicalize |
| LLM `text` arg | Untrusted LLM-supplied text crossing into provider HTTP/WSS body (mitigated upstream in PLAN 02) |
| LLM `path` arg to send_audio | Untrusted path crossing into rodio decode OR ffmpeg subprocess OR Telegram multipart upload |
| ffmpeg subprocess | Operator-configurable binary running on the host with the file path as an argument |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| **T-output-path** | **Tampering** | **TextToSpeechTool::execute output_path arg** | **mitigate (BLOCKING)** | **Task 2 implements `canonicalize(parent).starts_with(audio_canonical)` clamp BEFORE the synthesize call. If the LLM passes `/etc/passwd`, `~/.ssh/authorized_keys`, or `../../tmp/x`, the canonicalize result will NOT live under `$IRONHERMES_HOME/audio_cache/` and the tool returns `anyhow::bail!("output_path must be inside $IRONHERMES_HOME/audio_cache/")`. Locked by `test_output_path_traversal_blocked` unit test (Task 2 behavior block). This is HIGH severity per the orchestrator's `<security_threat_model_required>` brief and BLOCKS the phase if absent at PLAN 03 close.** |
| T-text-length | EoP / DoS | EdgeProvider / ElevenLabsProvider synthesize | mitigate (inherited) | Truncation lands in PLAN 02 at provider boundary. The tool layer here forwards `text` verbatim — providers truncate. Locked by PLAN 02 unit tests. |
| T-api-key-leak | Information Disclosure | TextToSpeechTool::redact_args | mitigate | `TextToSpeechTool` inherits the default no-op `redact_args` from the Tool trait. Tool args contain ONLY `text` + `output_path` — no API keys. The `ELEVENLABS_API_KEY` is read inside ElevenLabsProvider from env, not from the LLM tool args, so there is nothing for redact_args to scrub. Verified by manual inspection. |
| T-ffmpeg-arg-injection | Tampering | tokio::process::Command ffmpeg call in send_audio Telegram arm | mitigate | The ffmpeg subprocess uses `.arg()` per-argument (NOT a shell string), so `path.with_extension("ogg")` cannot inject extra flags. ffmpeg_path defaults to `"ffmpeg"` (PATH lookup); operator can override via config but the value passes only as the binary name to Command::new — no `bash -c` style. |
| T-platform-unsupported | DoS | SendAudioTool default branch (Platform::Discord / Web / etc.) | mitigate | The default arm `anyhow::bail!("send_audio not yet wired for platform: {:?}", other)` returns Err with the platform name. Locked by `test_send_audio_unsupported_platform_bails`. The LLM sees a clear error and can adapt its plan; no panic. |
| T-rodio-no-device | DoS | SendAudioTool Local arm on a host without an audio device | accept | `rodio::OutputStream::try_default()` returns Err with a clear message; the tool surfaces it via `anyhow::anyhow!("No audio device: {}", e)`. The agent learns and continues; no panic. Operator running headless will see the error string. |
| T-disk-exhaust | DoS | audio_cache/ unbounded growth | accept | Deferred per CONTEXT D-16 — no cleanup logic this phase. Operator can manually `rm -rf $IRONHERMES_HOME/audio_cache/`. Future phase adds size cap or age eviction when disk usage shows up as a real problem. |
| T-ffmpeg-binary-tamper | Tampering | ffmpeg binary on PATH | accept (inherited) | PATH-resolution attacks deferred per PLAN 02's threat model. The operator can pin `tts.ffmpeg_path` to an absolute path in config; Task 3 honors that field. |
</threat_model>

<verification>
- `cargo build --workspace` exits 0 with no new warnings
- `cargo test -p ironhermes-tools --test tts_tools` shows 9 passed / 1 ignored (5 tools tests + TTS-03/04/05/06/09 + 4 new threat-model tests; TTS-10 ignored for PLAN 04)
- `cargo test -p ironhermes-agent --test invariants_36_17_5` exits 0 (locks build_app_runtime_bundle wiring + per-session contract)
- `cargo test -p ironhermes-gateway` exits 0 (no regression from the new AudioDispatcher impl on TelegramAdapter)
- `cargo test --workspace` shows zero NEW test failures vs. pre-merge baseline (pre-existing kanban end_to_end failures noted in STATE are accepted)
- Phase-wide grep: `grep -rc 'use ironhermes_gateway' crates/ironhermes-tools/src/` returns 0 (no circular dep introduced)
- The audio_cache path-traversal mitigation is locked by a passing unit test (T-output-path BLOCKING gate)
</verification>

<success_criteria>
- TextToSpeechTool exposes the D-05 / D-06 / D-07 surface (single tool, file-out + path-return, args = text + optional output_path); toolset = "voice"
- SendAudioTool implements the D-15 dispatch tree: Local→rodio, Telegram .ogg/.opus→send_voice, Telegram .mp3+ffmpeg→ffmpeg convert+send_voice, Telegram .mp3 no-ffmpeg→send_audio, other platforms→bail
- LLM-supplied output_path is CLAMPED to $IRONHERMES_HOME/audio_cache/ via canonicalize+starts_with; T-output-path BLOCKING mitigation locked by passing unit test
- register_tts_tools method on ToolRegistry takes (SessionKey, Option<Arc<dyn AudioDispatcher>>, Arc<Config>) and registers BOTH tools by Box<dyn Tool>
- AppRuntimeFactoryInput has two new additive optional fields (session_key, telegram_adapter); build_app_runtime_bundle conditionally fires register_tts_tools when session_key is Some
- AudioDispatcher trait lives in ironhermes-tools; TelegramAdapter (in gateway) implements it — orphan-rule-safe, no circular dep
- agent_runtime.rs passes session_key: None and telegram_adapter: None for v1 (per-turn threading deferred to a follow-up phase)
- 5 Wave 0 TTS-NN stubs at GREEN (TTS-03 / TTS-04 / TTS-05 / TTS-06 / TTS-09); only TTS-10 remains ignored for PLAN 04 live-network UAT
- Zero D-09 / D-11 violations; zero side-channel events; zero Discord arm in v1
</success_criteria>

<output>
Create `.planning/phases/36.17.5-integrate-tts-functions/36.17.5-03-SUMMARY.md` when done.
Include in the summary:
- Confirmation that `cargo tree -p ironhermes-tools | grep '^ironhermes-gateway'` returned no matches (no circular dep)
- Exact ffmpeg argv used for MP3→OGG conversion (`-c:a libopus -b:a 64k -ar 48000 -ac 1 -y`)
- The unit test command + output proving `test_output_path_traversal_blocked` passes (T-output-path BLOCKING gate evidence)
- Final ignored-test count in tts_tools.rs (should be exactly 1: TTS-10 live network)
- Any deviation from D-15 dispatch tree (should be NONE)
- Any new workspace dep added beyond what PLAN 01 declared (should be NONE — rodio + msedge-tts already at workspace level)
</output>
