---
phase: 36.17.5
plan: 02
type: execute
wave: 2
depends_on:
  - 36.17.5-01
files_modified:
  - crates/ironhermes-tools/src/lib.rs
  - crates/ironhermes-tools/src/tts/mod.rs
  - crates/ironhermes-tools/src/tts/edge.rs
  - crates/ironhermes-tools/src/tts/elevenlabs.rs
  - crates/ironhermes-tools/tests/tts_tools.rs
autonomous: true
requirements:
  - D-03
  - D-04
tests:
  - TTS-03
  - TTS-04
  - TTS-09
threats:
  - T-text-length
  - T-api-key-leak

must_haves:
  truths:
    - "EdgeProvider always reports is_available() == true (no API key required) per D-03"
    - "ElevenLabsProvider reports is_available() == false when ELEVENLABS_API_KEY is unset and true when set"
    - "Both providers truncate text BEFORE the network call (Edge 5000 chars, ElevenLabs 10000 chars) — mitigates T-text-length"
    - "ffmpeg_available() returns a bool on every platform without panicking; result cached in OnceLock"
    - "Both providers write audio bytes to output_path and return PathBuf on success (D-09 / D-11 signature)"
  artifacts:
    - path: "crates/ironhermes-tools/src/tts/mod.rs"
      provides: "module declarations + ffmpeg_available() probe + build_tts_registry() factory"
      contains: "static FFMPEG_AVAILABLE: OnceLock<bool>"
    - path: "crates/ironhermes-tools/src/tts/edge.rs"
      provides: "EdgeProvider impl wrapping msedge-tts"
      contains: "impl TtsProvider for EdgeProvider"
    - path: "crates/ironhermes-tools/src/tts/elevenlabs.rs"
      provides: "ElevenLabsProvider impl wrapping reqwest"
      contains: "impl TtsProvider for ElevenLabsProvider"
  key_links:
    - from: "crates/ironhermes-tools/src/tts/edge.rs"
      to: "msedge_tts::MSEdgeTTSClient"
      via: "tokio-runtime feature on workspace dep"
      pattern: "msedge_tts::"
    - from: "crates/ironhermes-tools/src/tts/elevenlabs.rs"
      to: "https://api.elevenlabs.io/v1/text-to-speech/{voice_id}"
      via: "reqwest::Client::new() per call"
      pattern: "api.elevenlabs.io"
    - from: "crates/ironhermes-tools/src/lib.rs"
      to: "crates/ironhermes-tools/src/tts/"
      via: "pub mod tts;"
      pattern: "pub mod tts;"
---

<objective>
Land the two built-in TTS provider implementations behind the trait that PLAN 01 froze:
EdgeProvider (D-03 — free default, no API key, MS Edge TTS WebSocket via msedge-tts) and
ElevenLabsProvider (D-03 — premium, ELEVENLABS_API_KEY env var, REST via reqwest, mirrors
web_search.rs). Also ship the ffmpeg detection probe (D-04 — lazy OnceLock boolean used by
PLAN 03's send_audio Telegram arm) and a build_tts_registry(and TtsConfig) helper that
PLAN 03 will call from register_tts_tools.

Purpose: PLAN 01 declared the trait, config, and workspace deps. PLAN 03 will create the
LLM-callable tools that dispatch through TtsRegistry::get. The providers themselves have
to land between those two plans so PLAN 03 has something concrete to dispatch to.

Output: A merged develop HEAD where cargo build -p ironhermes-tools succeeds; the
tts/edge.rs and tts/elevenlabs.rs files exist with TtsProvider impls; the Wave 0 stubs
TTS-03, TTS-04, TTS-09 are un-ignored and pass; zero changes to ironhermes-core,
registry.rs, or app_runtime_factory.rs (PLAN 03 owns those files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-CONTEXT.md
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-RESEARCH.md
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-PATTERNS.md
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-VALIDATION.md
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-01-SUMMARY.md

<interfaces>
PLAN 01 froze these. PLAN 02 consumes them verbatim — no exploration needed.

From ironhermes-core (re-exported at crate root):

  #[async_trait::async_trait]
  pub trait TtsProvider: Send + Sync {
      fn name(and self) -> and str;
      fn display_name(and self) -> and str;          // defaults to self.name()
      fn is_available(and self) -> bool;
      fn voice_compatible(and self) -> bool;          // defaults to false
      async fn synthesize(and self, text: and str, output_path: and PathBuf)
          -> anyhow::Result<PathBuf>;
  }

  pub struct TtsRegistry { /* HashMap<String, Arc<dyn TtsProvider>> */ }
  impl TtsRegistry {
      pub fn new() -> Self;
      pub fn register(and mut self, provider: Arc<dyn TtsProvider>);
      pub fn get(and self, name: and str) -> Option<Arc<dyn TtsProvider>>;
  }

  pub const BUILTIN_TTS_NAMES: and [and str] = and ["edge", "elevenlabs"];

Config (from ironhermes-core::config):

  pub struct EdgeTtsConfig    { pub voice: String, pub output_format: String }
  pub struct ElevenLabsConfig { pub voice_id: String, pub model_id: String, pub output_format: String }
  pub struct TtsConfig        { pub provider: String, pub ffmpeg_path: Option<String>,
                                pub edge: EdgeTtsConfig, pub elevenlabs: ElevenLabsConfig }

(NOTE: "and" above stands in for the Rust reference operator; the actual signatures use
the reference operator. The executor reads tts.rs directly in PLAN 01's output.)

From crates/ironhermes-tools/src/web_search.rs (closest analog per PATTERNS.md):

  fn is_available(and self) -> bool { std::env::var("FIRECRAWL_API_KEY").is_ok() }

  // execute() pattern (lines 81–112):
  let api_key = std::env::var("FIRECRAWL_API_KEY")
      .map_err(|_| anyhow::anyhow!("FIRECRAWL_API_KEY environment variable not set"))?;
  let client = reqwest::Client::new();                  // per-call, NOT shared
  let response = client.post(URL).bearer_auth(and api_key).json(and body).send().await
      .map_err(|e| anyhow::anyhow!("HTTP request failed: {}", e))?;
  let status = response.status();
  if !status.is_success() {
      let body = response.text().await.unwrap_or_default();
      return Err(anyhow::anyhow!("Firecrawl API returned {}: {}", status, body));
  }

From crates/ironhermes-core/src/token_estimator.rs (OnceLock pattern):

  static GLOBAL_ESTIMATOR: OnceLock<TokenEstimator> = OnceLock::new();

ElevenLabs request shape (RESEARCH § Code Examples, citing elevenlabs.io docs):

  POST https://api.elevenlabs.io/v1/text-to-speech/{voice_id}
  Header: xi-api-key: <ELEVENLABS_API_KEY>
  Body  : { "text": "...", "model_id": "eleven_multilingual_v2",
            "output_format": "mp3_44100_128" }
  Resp 200: Content-Type: application/octet-stream  (raw MP3 audio bytes at v1)
  Resp 4xx/5xx: {"detail": {"type": "...", "code": "...", "message": "...",
                            "request_id": "..."}}

Text length caps (T-text-length mitigation, RESEARCH § Security Domain):

  - Edge: 5000 chars (server-side limit, truncate client-side to avoid noisy 4xx)
  - ElevenLabs (eleven_multilingual_v2): 10000 chars

build_tts_registry helper (RESEARCH Pattern 5 — single source of truth for built-in wiring):

  // crates/ironhermes-tools/src/tts/mod.rs
  pub fn build_tts_registry(
      config: and ironhermes_core::config::TtsConfig,
  ) -> ironhermes_core::TtsRegistry {
      use std::sync::Arc;
      let mut registry = ironhermes_core::TtsRegistry::new();
      registry.register(Arc::new(
          crate::tts::edge::EdgeProvider::new(config.edge.clone()),
      ));
      registry.register(Arc::new(
          crate::tts::elevenlabs::ElevenLabsProvider::new(config.elevenlabs.clone()),
      ));
      registry
  }

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tts module skeleton + ffmpeg probe + build_tts_registry factory</name>
  <files>crates/ironhermes-tools/src/tts/mod.rs, crates/ironhermes-tools/src/lib.rs, crates/ironhermes-tools/src/tts/edge.rs, crates/ironhermes-tools/src/tts/elevenlabs.rs</files>
  <read_first>
    - crates/ironhermes-tools/src/lib.rs lines 1–31 (current pub mod declarations; the new `pub mod tts;` slots alphabetically after `pub mod terminal;` and before `pub mod toolset_session;`)
    - crates/ironhermes-core/src/token_estimator.rs lines 57–63 (OnceLock pattern verbatim per PATTERNS.md)
    - crates/ironhermes-core/src/tts.rs (the trait + registry created in PLAN 01 Task 4)
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-RESEARCH.md § "Pattern 6: ffmpeg Detection"
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-CONTEXT.md D-04
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-PATTERNS.md § `crates/ironhermes-tools/src/tts/mod.rs`
  </read_first>
  <action>
    Create the directory crates/ironhermes-tools/src/tts/ and inside it create mod.rs containing:

    1. Module declarations: `pub mod edge;` and `pub mod elevenlabs;` (each gets its own file
       in Tasks 2 and 3 of this plan).
    2. The ffmpeg_available function:
       - `static FFMPEG_AVAILABLE: OnceLock<bool> = OnceLock::new();` at module scope.
       - `pub fn ffmpeg_available() -> bool` that returns
         `*FFMPEG_AVAILABLE.get_or_init(|| std::process::Command::new("ffmpeg").arg("-version")
           .output().map(|o| o.status.success()).unwrap_or(false))`.
       - MUST be sync (not async — probe runs once at first call; OnceLock is thread-safe).
       - MUST NOT panic on any platform — `unwrap_or(false)` handles Windows-without-ffmpeg.
    3. A test-only helper `pub fn ffmpeg_available_for_test(force: Option<bool>) -> bool`
       behind `#[cfg(test)]` — when `Some(b)`, returns b directly without touching the
       OnceLock; when None, returns ffmpeg_available(). PLAN 03's tool tests use this so
       they don't race on the process-global OnceLock.
    4. The build_tts_registry factory shown in `<interfaces>` — pub fn, single source of
       truth for built-in provider wiring. Per D-10 the registry only ever holds the two
       built-ins; this factory is the production call site.
    5. `pub use edge::EdgeProvider; pub use elevenlabs::ElevenLabsProvider;` so PLAN 03
       imports are short.

    Create crates/ironhermes-tools/src/tts/edge.rs as a placeholder:
       `pub struct EdgeProvider { pub config: ironhermes_core::config::EdgeTtsConfig }`
       plus an `impl EdgeProvider { pub fn new(config: ironhermes_core::config::EdgeTtsConfig)
       -> Self { Self { config } } }`. Task 2 fills in the TtsProvider impl.

    Create crates/ironhermes-tools/src/tts/elevenlabs.rs as a placeholder:
       `pub struct ElevenLabsProvider { pub config: ironhermes_core::config::ElevenLabsConfig }`
       plus `impl ElevenLabsProvider { pub fn new(config: ironhermes_core::config::ElevenLabsConfig)
       -> Self { Self { config } } }`. Task 3 fills in the TtsProvider impl.

    In crates/ironhermes-tools/src/lib.rs:
    - Add `pub mod tts;` alphabetically between `pub mod terminal;` and
      `pub mod toolset_session;` with the trailing comment
      `// Phase 36.17.5 — TTS provider impls (edge, elevenlabs)`.
    - Do NOT add re-exports at crate root for EdgeProvider / ElevenLabsProvider —
      ironhermes_tools::tts::EdgeProvider is short enough; the bare crate-root re-exports
      stay narrow.

    Do NOT yet implement TtsProvider for either provider (Tasks 2 + 3). The placeholder
    structs ensure `pub mod edge; pub mod elevenlabs;` compile so the next two tasks can
    drop in the impl blocks in isolation.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-tools 2>and 1 | tee /tmp/36.17.5-02-t1.log; ! grep -E '^error' /tmp/36.17.5-02-t1.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^pub mod tts;' crates/ironhermes-tools/src/lib.rs` returns 1
    - `grep -c 'static FFMPEG_AVAILABLE: OnceLock<bool>' crates/ironhermes-tools/src/tts/mod.rs` returns 1
    - `grep -c 'pub fn ffmpeg_available' crates/ironhermes-tools/src/tts/mod.rs` returns >= 1
    - `grep -c 'pub fn build_tts_registry' crates/ironhermes-tools/src/tts/mod.rs` returns 1
    - `grep -c 'pub struct EdgeProvider' crates/ironhermes-tools/src/tts/edge.rs` returns 1
    - `grep -c 'pub struct ElevenLabsProvider' crates/ironhermes-tools/src/tts/elevenlabs.rs` returns 1
    - `cargo build -p ironhermes-tools` exits 0 with no new warnings
  </acceptance_criteria>
  <done>Module skeleton compiles; ffmpeg probe + build_tts_registry factory are reachable; placeholder provider structs exist for Tasks 2 + 3 to extend.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EdgeProvider impl + un-ignore TTS-03 + TTS-09 + ffmpeg-probe test</name>
  <files>crates/ironhermes-tools/src/tts/edge.rs, crates/ironhermes-tools/tests/tts_tools.rs</files>
  <read_first>
    - crates/ironhermes-tools/src/tts/edge.rs (Task 1 placeholder)
    - crates/ironhermes-tools/src/tts/mod.rs (Task 1 ffmpeg + factory)
    - crates/ironhermes-tools/src/web_search.rs (full file — analog for env-var-keyed Tool surface; EdgeProvider mirrors the error-mapping style but uses WSS instead of HTTP)
    - crates/ironhermes-core/src/tts.rs (TtsProvider trait surface)
    - crates/ironhermes-tools/tests/tts_tools.rs (Wave 0 stubs from PLAN 01 Task 7 — un-ignore TTS-03 and TTS-09)
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-RESEARCH.md § "Pitfall 5: msedge-tts Audio Format Is Not MP3 by Default" (Assumption A3 — verify the exact `SpeechConfig` MP3 constant from docs.rs/msedge-tts at implementation time)
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-CONTEXT.md D-03 (Edge as the free default)
  </read_first>
  <behavior>
    - TTS-03 `test_edge_provider_available`: construct EdgeProvider::new(EdgeTtsConfig::default()); assert provider.is_available() == true (no API key required — D-03). No tokio runtime needed; the test is sync.
    - TTS-09 `test_ffmpeg_probe_no_panic`: call `ironhermes_tools::tts::ffmpeg_available()` from a `#[test]` (sync) and assert the call returns without panicking. Outcome (true / false) MUST NOT be asserted — the host machine may or may not have ffmpeg. Result type is bool. Locks D-04 "lazy probe never panics".
    - new test `test_edge_text_truncated_at_5000`: hand-craft a 6000-char string; call `EdgeProvider::truncate_text(and s)` (a `pub(crate) fn` exposed for testing); assert the returned `and str` has length exactly 5000. Locks T-text-length mitigation. Does NOT invoke the network.
    - new test `test_edge_provider_name_is_edge`: assert `provider.name() == "edge"` (matches BUILTIN_TTS_NAMES[0]).
  </behavior>
  <action>
    Replace the placeholder body of `crates/ironhermes-tools/src/tts/edge.rs` with a full
    TtsProvider implementation:

    1. Imports: `use async_trait::async_trait;` + `use ironhermes_core::tts::TtsProvider;` +
       `use ironhermes_core::config::EdgeTtsConfig;` + `use std::path::PathBuf;`.

    2. Keep the struct + `new` from Task 1.

    3. Add a `pub(crate) fn truncate_text(text: and str) -> and str` helper:
       `if text.len() > 5000 { and text[..5000] } else { text }`. Edge enforces a 5000-char
       cap server-side; truncating client-side avoids noisy 4xx and bounds LLM cost on
       prompt injection (T-text-length).

    4. `#[async_trait]` impl TtsProvider for EdgeProvider with:
       - `fn name(and self) -> and str { "edge" }`
       - `fn display_name(and self) -> and str { "Microsoft Edge TTS" }`
       - `fn is_available(and self) -> bool { true }` (D-03 — no API key)
       - `fn voice_compatible(and self) -> bool { false }` (Edge outputs MP3 by default;
         Telegram voice-bubble path needs ffmpeg conversion — falls back to send_audio
         when ffmpeg absent per D-04)
       - `async fn synthesize(and self, text: and str, output_path: and PathBuf)
                              -> anyhow::Result<PathBuf>`:
         - Apply `Self::truncate_text(text)` to bound length.
         - Resolve voice from `self.config.voice` (default `"en-US-AriaNeural"`).
         - Open the msedge-tts client per call:
             `let client = msedge_tts::tts::client::connect().await
                 .map_err(|e| anyhow::anyhow!("Edge TTS connect failed: {}", e))?;`
           (The exact module path and method name MUST be verified against docs.rs/msedge-tts
            at implementation time per RESEARCH Assumptions A2/A7. The CONTRACT for the
            executor: call the crate's documented "open WebSocket" API, then call its
            documented "synthesize" API with the voice and an MP3 output format constant.
            If `connect` is named `MSEdgeTTSClient::connect` or `Client::new` in the actual
            0.3 release, use that — the goal is "WebSocket client to Edge TTS endpoint", not
            a specific identifier.)
         - Build a `SpeechConfig` (or whatever the crate calls it) wiring the voice + an
           MP3 output format constant (per RESEARCH Pitfall 5, the format MUST be MP3 so
           that the `.mp3` extension in audio_cache matches the bytes written; the constant
           is something like `AudioFormat::Audio24khz48kbitrateMonoMp3` — verify at impl
           time and prefer the highest-quality MP3 constant available).
         - Call `client.synthesize(text, and speech_config).await
                 .map_err(|e| anyhow::anyhow!("Edge TTS synthesis failed: {}", e))?;`
         - `tokio::fs::write(output_path, and audio.audio_bytes).await
                 .map_err(|e| anyhow::anyhow!("Failed to write Edge audio file: {}", e))?;`
           (The field name `audio_bytes` is from RESEARCH A7; verify at impl time. If the
           crate exposes the bytes under a different name, use it but DO NOT add a serde
           translation layer.)
         - `Ok(output_path.clone())`

    5. `#[cfg(test)] mod tests` block at the bottom of edge.rs covering the Task 2 behaviors
       that don't need an integration test file:
       - `fn test_truncate_short()` — 100-char input returns the same str.
       - `fn test_truncate_long()` — 6000-char input returns exactly 5000 chars.

    6. In `crates/ironhermes-tools/tests/tts_tools.rs`:
       - Un-ignore `test_edge_provider_available` and replace `unimplemented!()` with the
         Task 2 behavior body (assert is_available() == true on a default EdgeProvider).
       - Un-ignore `test_ffmpeg_probe_no_panic` and call
         `ironhermes_tools::tts::ffmpeg_available()` in a `let _ = ...` to drop the bool;
         assert the call returns. NO assertion on the bool value (host-machine dependent).
       - Add `test_edge_provider_name_is_edge` as a new `#[test]` (not `#[tokio::test]` —
         the call is sync since `name()` is sync trait method). Assert `provider.name() == "edge"`.

    Do NOT implement streaming or `stream()` (D-09 DEFERRED). Do NOT add `list_voices` /
    `list_models` (D-09 DEFERRED). Do NOT add a typed `EdgeError` enum (D-11 — anyhow only).
  </action>
  <verify>
    <automated>cargo test -p ironhermes-tools --test tts_tools test_edge 2>and 1 | tee /tmp/36.17.5-02-t2-edge.log; cargo test -p ironhermes-tools --test tts_tools test_ffmpeg 2>and 1 | tee /tmp/36.17.5-02-t2-ff.log; cargo test -p ironhermes-tools --lib tts::edge 2>and 1 | tee /tmp/36.17.5-02-t2-inline.log; grep -E 'test result: ok' /tmp/36.17.5-02-t2-edge.log and and  grep -E 'test result: ok' /tmp/36.17.5-02-t2-ff.log and and  grep -E 'test result: ok' /tmp/36.17.5-02-t2-inline.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'impl TtsProvider for EdgeProvider' crates/ironhermes-tools/src/tts/edge.rs` returns 1
    - `grep -c 'fn name(and self) -> and str { "edge" }' crates/ironhermes-tools/src/tts/edge.rs` (with the reference op) returns 1
    - `grep -c 'fn is_available(and self) -> bool { true }' crates/ironhermes-tools/src/tts/edge.rs` returns 1
    - `grep -c 'pub(crate) fn truncate_text\|fn truncate_text' crates/ironhermes-tools/src/tts/edge.rs` returns >= 1
    - `grep -c '5000' crates/ironhermes-tools/src/tts/edge.rs` returns >= 1 (T-text-length cap)
    - `grep -c 'msedge_tts' crates/ironhermes-tools/src/tts/edge.rs` returns >= 2 (connect + synthesize)
    - `grep -c 'tokio::fs::write' crates/ironhermes-tools/src/tts/edge.rs` returns 1
    - `grep -c '#\[ignore' crates/ironhermes-tools/tests/tts_tools.rs` returns 4 (was 6; TTS-03 and TTS-09 unignored)
    - `cargo test -p ironhermes-tools --test tts_tools test_edge_provider_available` exits 0 (was ignored, now green)
    - `cargo test -p ironhermes-tools --test tts_tools test_ffmpeg_probe_no_panic` exits 0 (was ignored, now green)
    - `cargo test -p ironhermes-tools --test tts_tools test_edge_provider_name_is_edge` exits 0
    - `cargo test -p ironhermes-tools --lib tts::edge::tests::test_truncate_long` exits 0
    - `cargo build -p ironhermes-tools` exits 0 with no warnings
  </acceptance_criteria>
  <done>EdgeProvider implements TtsProvider with the D-09 surface; truncation caps text at 5000 chars; TTS-03 and TTS-09 are green; ffmpeg probe is sync, cached, and never panics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ElevenLabsProvider impl + un-ignore TTS-04 + truncation test</name>
  <files>crates/ironhermes-tools/src/tts/elevenlabs.rs, crates/ironhermes-tools/tests/tts_tools.rs</files>
  <read_first>
    - crates/ironhermes-tools/src/tts/elevenlabs.rs (Task 1 placeholder)
    - crates/ironhermes-tools/src/web_search.rs lines 31–164 (the closest analog — env-var key + per-call reqwest::Client + status check + body surface in error)
    - crates/ironhermes-core/src/tts.rs (TtsProvider trait surface)
    - crates/ironhermes-tools/tests/tts_tools.rs (TTS-04 stub from PLAN 01)
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-RESEARCH.md § "Pattern 4: ElevenLabs Provider" and § "Pitfall 2: ElevenLabs 429 vs Quota vs Voice-Not-Found"
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-CONTEXT.md D-03 (premium opt-in)
  </read_first>
  <behavior>
    - TTS-04 `test_elevenlabs_unavailable_no_key`: lock the env_lock(), `std::env::remove_var("ELEVENLABS_API_KEY")`, construct ElevenLabsProvider::new(ElevenLabsConfig::default()), assert `provider.is_available() == false`. Then `std::env::set_var("ELEVENLABS_API_KEY", "test-key")` and assert `provider.is_available() == true`. Final remove_var to clean up. Locks D-03 (premium opt-in via env var) and the contract that the provider matches web_search.rs is_available semantics exactly.
    - new test `test_elevenlabs_text_truncated_at_10000`: 12000-char input → `ElevenLabsProvider::truncate_text(and s)` returns exactly 10000 chars. Locks T-text-length for eleven_multilingual_v2's documented cap.
    - new test `test_elevenlabs_provider_name_is_elevenlabs`: assert `provider.name() == "elevenlabs"` (matches BUILTIN_TTS_NAMES[1]).
  </behavior>
  <action>
    Replace the placeholder body of `crates/ironhermes-tools/src/tts/elevenlabs.rs` with a
    full TtsProvider implementation mirroring web_search.rs's HTTP shape:

    1. Imports: `use async_trait::async_trait;` + `use ironhermes_core::tts::TtsProvider;` +
       `use ironhermes_core::config::ElevenLabsConfig;` + `use std::path::PathBuf;`.

    2. Keep the struct + `new` from Task 1.

    3. Add `pub(crate) fn truncate_text(text: and str) -> and str`:
       `if text.len() > 10000 { and text[..10000] } else { text }`. ElevenLabs cap for
       eleven_multilingual_v2 is 10000 chars per RESEARCH; truncating bounds LLM cost
       (T-text-length).

    4. `#[async_trait]` impl TtsProvider for ElevenLabsProvider:
       - `fn name(and self) -> and str { "elevenlabs" }`
       - `fn display_name(and self) -> and str { "ElevenLabs" }`
       - `fn is_available(and self) -> bool { std::env::var("ELEVENLABS_API_KEY").is_ok() }`
         (verbatim mirror of web_search.rs line 67–69 with the new var name).
       - `fn voice_compatible(and self) -> bool { false }` for v1. (RESEARCH Open Q #2:
         ElevenLabs Opus voice-bubble support requires OGG container handling — deferred.
         The CONTEXT comment in PLAN 01 EdgeTtsConfig output_format explicitly notes this.
         When config.output_format is set to opus, voice_compatible could return true in a
         future plan; v1 returns false uniformly.)
       - `async fn synthesize(and self, text: and str, output_path: and PathBuf)
                              -> anyhow::Result<PathBuf>`:
         - Apply Self::truncate_text(text).
         - Read `let api_key = std::env::var("ELEVENLABS_API_KEY")
                 .map_err(|_| anyhow::anyhow!("ELEVENLABS_API_KEY environment variable not set"))?;`
         - `let client = reqwest::Client::new();`  (per-call, mirrors web_search.rs:92).
         - `let url = format!("https://api.elevenlabs.io/v1/text-to-speech/{}",
                              self.config.voice_id);`
         - POST with header `xi-api-key: api_key` and JSON body
           `{ "text": text, "model_id": self.config.model_id,
              "output_format": "mp3_44100_128" }`. (output_format is hardcoded to MP3 for
           v1 per the RESEARCH Open Q #2 deferral — the config.output_format String is
           reserved for a future Opus-opt-in phase.)
         - `.send().await.map_err(|e| anyhow::anyhow!("ElevenLabs HTTP request failed: {}", e))?;`
         - Check `if !response.status().is_success()`: read body, return
           `anyhow::anyhow!("ElevenLabs API returned {}: {}", status, body)`.
           (Mirrors web_search.rs lines 104–112. Per Pitfall 2 the body contains
           `detail.code` which lets the operator distinguish quota_exceeded vs
           rate_limit_exceeded vs invalid_voice_id.)
         - `let bytes = response.bytes().await
                 .map_err(|e| anyhow::anyhow!("Failed to read ElevenLabs response body: {}", e))?;`
         - `tokio::fs::write(output_path, and bytes).await
                 .map_err(|e| anyhow::anyhow!("Failed to write audio file: {}", e))?;`
         - `Ok(output_path.clone())`.

    5. `#[cfg(test)] mod tests` block at the bottom of elevenlabs.rs covering:
       - `fn test_truncate_short()` — 100-char input returns the same str.
       - `fn test_truncate_long()` — 12000-char input returns exactly 10000 chars.

    6. In `crates/ironhermes-tools/tests/tts_tools.rs`:
       - Un-ignore `test_elevenlabs_unavailable_no_key`. Replace `unimplemented!()` with the
         Task 3 behavior body. Hold `env_lock().lock()` for the duration so parallel tests
         don't race on ELEVENLABS_API_KEY. Pattern verbatim from cronjob_tool_default_deliver.rs.
       - Add `test_elevenlabs_provider_name_is_elevenlabs` as a new `#[test]`.

    Do NOT add per-call retry / rate-limit handling (D-11 — anyhow lossy errors are fine;
    typed errors deferred). Do NOT add streaming (D-09 DEFERRED). The `xi-api-key` header
    contains a secret but is NEVER logged — `redact_args` is irrelevant here because the
    provider receives NO args; it reads the env var and the config directly.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-tools --test tts_tools test_elevenlabs 2>and 1 | tee /tmp/36.17.5-02-t3.log; cargo test -p ironhermes-tools --lib tts::elevenlabs 2>and 1 | tee /tmp/36.17.5-02-t3-inline.log; grep -E 'test result: ok' /tmp/36.17.5-02-t3.log and and  grep -E 'test result: ok' /tmp/36.17.5-02-t3-inline.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'impl TtsProvider for ElevenLabsProvider' crates/ironhermes-tools/src/tts/elevenlabs.rs` returns 1
    - `grep -c 'fn name(and self) -> and str { "elevenlabs" }' crates/ironhermes-tools/src/tts/elevenlabs.rs` (with the reference op) returns 1
    - `grep -c 'ELEVENLABS_API_KEY' crates/ironhermes-tools/src/tts/elevenlabs.rs` returns >= 2 (is_available + execute env var read)
    - `grep -c 'api.elevenlabs.io/v1/text-to-speech' crates/ironhermes-tools/src/tts/elevenlabs.rs` returns 1
    - `grep -c 'xi-api-key' crates/ironhermes-tools/src/tts/elevenlabs.rs` returns 1
    - `grep -c 'reqwest::Client::new' crates/ironhermes-tools/src/tts/elevenlabs.rs` returns 1 (per-call client, NOT a static)
    - `grep -c '10000' crates/ironhermes-tools/src/tts/elevenlabs.rs` returns >= 1 (T-text-length cap)
    - `grep -c '#\[ignore' crates/ironhermes-tools/tests/tts_tools.rs` returns 3 (was 4 after Task 2; TTS-04 unignored)
    - `cargo test -p ironhermes-tools --test tts_tools test_elevenlabs_unavailable_no_key` exits 0
    - `cargo test -p ironhermes-tools --test tts_tools test_elevenlabs_provider_name_is_elevenlabs` exits 0
    - `cargo test -p ironhermes-tools --lib tts::elevenlabs::tests::test_truncate_long` exits 0
    - `cargo build -p ironhermes-tools` exits 0 with no warnings
  </acceptance_criteria>
  <done>ElevenLabsProvider mirrors web_search.rs shape exactly with the new env var; truncation caps text at 10000 chars; TTS-04 is green; per-call reqwest::Client matches the project convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM `text` arg | Untrusted LLM-controlled string crossing into the Edge WSS / ElevenLabs HTTP request body |
| `std::env::var("ELEVENLABS_API_KEY")` | Operator-supplied secret crossing into the `xi-api-key` HTTP header |
| `msedge-tts` WSS endpoint | Third-party WebSocket service (Microsoft Edge TTS) crossing into local disk via `tokio::fs::write` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-text-length | EoP / DoS | EdgeProvider::synthesize, ElevenLabsProvider::synthesize | mitigate | Task 2 and Task 3 add `truncate_text` helpers (`pub(crate) fn`) with hard caps of 5000 (Edge) and 10000 (ElevenLabs) chars BEFORE the network call. `test_edge_text_truncated_at_5000` and `test_elevenlabs_text_truncated_at_10000` lock the behavior at unit-test level. Bounds LLM cost-blowup attacks (a runaway agent loop generating long text). |
| T-api-key-leak | Information Disclosure | ElevenLabsProvider::synthesize | mitigate | The `xi-api-key` header value is read from env at call time, passed to reqwest's HeaderMap directly, and never written to logs or trajectory output. `redact_args` (Tool trait) is NOT relevant here because TtsProvider has no args struct that the LLM populates — the API key is provider-internal state. PLAN 03's TextToSpeechTool::redact_args (the LLM-facing layer) takes the default no-op because tool args contain only `text` + optional `output_path` (no secrets). |
| T-ffmpeg-binary | Tampering | `ffmpeg_available()` probe via `Command::new("ffmpeg")` | accept | The probe runs `ffmpeg -version` and tolerates non-zero exit / missing binary (`.unwrap_or(false)`). PATH-resolution attacks on the ffmpeg binary are accepted risk for v1 — the same attacker who could plant a malicious `ffmpeg` could plant any number of other binaries, so the additional gain is marginal. Operator can set `tts.ffmpeg_path` explicitly in config (Task 1 included the field on TtsConfig — PLAN 03 consumes it when wiring the Telegram arm). |
| T-wss-mitm | Information Disclosure | EdgeProvider WSS connection | accept | msedge-tts handles TLS for the WebSocket connection. We rely on the crate's transport security; no certificate pinning or alternative verification this phase. Telegram and ElevenLabs traffic share the same trust posture. |

Note: T-output-path (LLM path-traversal on the audio file destination) and T-SC-msedge-tts /
T-SC-rodio (package-legitimacy supply-chain) are owned by PLAN 03 and PLAN 01 respectively;
they do not re-enter the model here because PLAN 02 owns no LLM-facing surface (the
providers are invoked by the tool in PLAN 03, not directly by the LLM).
</threat_model>

<verification>
- `cargo build -p ironhermes-tools` exits 0 with no warnings
- `cargo test -p ironhermes-tools --test tts_tools` shows 3 passed (TTS-03 + TTS-04 + TTS-09) and 3 ignored (TTS-05 / TTS-06 / TTS-10 — handed off to PLAN 03 and PLAN 04)
- `cargo test -p ironhermes-tools --lib tts::edge::tests` and `cargo test -p ironhermes-tools --lib tts::elevenlabs::tests` both pass
- `grep -rc 'use ironhermes_tools' crates/ironhermes-core/src/` returns 0 (D-08 — core MUST NOT import tools)
- Phase-wide check: `cargo check --workspace` exits 0
- `grep -c '#\[ignore' crates/ironhermes-tools/tests/tts_tools.rs` returns exactly 3 (down from 6 after PLAN 01 and Tasks 2–3 of PLAN 02)
</verification>

<success_criteria>
- EdgeProvider implements the full D-09 TtsProvider surface; is_available is always true (D-03)
- ElevenLabsProvider implements the full D-09 TtsProvider surface; is_available reads `ELEVENLABS_API_KEY` exactly as web_search.rs reads `FIRECRAWL_API_KEY`
- Both providers truncate `text` to their documented cap BEFORE the network call (T-text-length mitigated and locked by unit tests)
- `ffmpeg_available()` lazy-probes ffmpeg via Command, caches the result in OnceLock, and never panics on any platform (D-04, TTS-09)
- `build_tts_registry(and TtsConfig)` is the single source of truth for built-in provider registration (D-10)
- 3 Wave 0 TTS-NN stubs flipped to GREEN (TTS-03, TTS-04, TTS-09); 3 remain ignored for PLAN 03 (TTS-05, TTS-06) and PLAN 04 (TTS-10)
- Zero D-09 DEFERRED items appear in either provider (no `stream`, `list_voices`, `list_models`, etc.)
- Zero D-11 violations: no typed error enums
- Zero modifications to ironhermes-core, registry.rs, or app_runtime_factory.rs (PLAN 03 owns those)
</success_criteria>

<output>
Create `.planning/phases/36.17.5-integrate-tts-functions/36.17.5-02-SUMMARY.md` when done.
Include in the summary:
- Exact crate API used: msedge-tts `connect` / synthesize method names actually invoked
  (RESEARCH A2 / A3 / A7 — confirm or correct the assumed `audio_bytes` field name and the
  MP3 format constant name based on what cargo doc / docs.rs/msedge-tts shows at impl time)
- Exact ElevenLabs `output_format` string sent on the wire (`mp3_44100_128` per RESEARCH;
  if rejected by the live API, document the corrected value)
- Post-task ignored count in tts_tools.rs (should be exactly 3: TTS-05, TTS-06, TTS-10)
- Confirmation that `ffmpeg_available()` was called from a test on the merge runner — record
  whether the result was true or false (does not gate merge; informational only)
- Any deviation from the truncation caps (5000 Edge / 10000 ElevenLabs) — should be NONE
</output>
