---
phase: 36.17.5
plan: 04
type: execute
wave: 4
depends_on:
  - 36.17.5-03
files_modified:
  - scripts/uat/phase-36.17.5-tts.sh
  - crates/ironhermes-tools/tests/tts_tools.rs
  - crates/ironhermes-cli/src/main.rs
autonomous: false
requirements:
  - D-01
  - D-02
tests:
  - TTS-10
threats: []

must_haves:
  truths:
    - "scripts/uat/phase-36.17.5-tts.sh exits 0 when all 5 operator gates pass on a real machine — proves the parity-gap catch-up (D-01) is closed with a faithful concept-level port"
    - "The UAT covers exactly the phase-width deliverables — Edge TTS output + TtsProvider trait + registry + Telegram delivery — and nothing else (D-02 scope fence: STT, push-to-talk, Discord VC remain out of scope and untouched)"
    - "Gate 1 proves $IRONHERMES_HOME/audio_cache/ contains a UUID-named .mp3 after a CLI text_to_speech invocation (D-03, D-16)"
    - "Gate 2 proves the .mp3 file is non-empty (D-04, D-05)"
    - "Gate 3 proves the ironhermes binary exits 0 with tts.provider='edge' — no API key panic (D-10)"
    - "Gate 4 proves text_to_speech AND send_audio appear in `hermes tools list` (D-08)"
    - "Gate 5 proves send_audio Platform::Local either plays audibly or returns clean error (no panic, D-15)"
    - "TTS-10 live network integration test un-ignored and passes against real Edge TTS endpoint"
  artifacts:
    - path: "scripts/uat/phase-36.17.5-tts.sh"
      provides: "5-gate operator UAT exercising real Edge TTS over the network"
      contains: "Gate 5"
    - path: "crates/ironhermes-tools/tests/tts_tools.rs"
      provides: "TTS-10 test_edge_synth_writes_file unignored"
      contains: "test_edge_synth_writes_file"
    - path: "crates/ironhermes-cli/src/main.rs"
      provides: "`hermes tts test` subcommand for Gate 1+2 of the UAT script"
      contains: "TtsTest"
  key_links:
    - from: "scripts/uat/phase-36.17.5-tts.sh"
      to: "hermes tts test"
      via: "cargo run -p ironhermes-cli -- tts test"
      pattern: "tts test"
    - from: "crates/ironhermes-cli/src/main.rs::TtsTest subcommand"
      to: "crates/ironhermes-tools/src/tts/mod.rs::build_tts_registry + EdgeProvider::synthesize"
      via: "in-process call (no LLM, no agent)"
      pattern: "build_tts_registry"
---

<objective>
Close the phase with operator-verifiable proof that the full TTS stack works end-to-end:
ship the 5-gate UAT shell script (mirroring scripts/uat/phase-36.17-web-logging.sh), wire
a thin `hermes tts test` CLI subcommand that the UAT calls in-process (no LLM round-trip
needed — the subcommand uses build_tts_registry directly to invoke EdgeProvider::synthesize),
un-ignore TTS-10 (live network test that runs only with `--ignored` since CI machines may
lack network), and run the operator through the 5 gates on a real machine. Gates 1–3 are
fully automated by the script; Gates 4 and 5 require human-verify checkpoints because
they involve listing the tool registry and audibly playing audio (or accepting an audio-
device-absent error message).

Purpose: Operator UAT is the only way to prove the synth actually works against Microsoft's
production Edge TTS endpoint. It also confirms that the tool catalog exposes the two new
tools — which is the receiver-end half of PLAN 03's register_tts_tools wiring. Without
this UAT, PLAN 03 could ship with a register_tts_tools method that silently never gets
called from any production path.

Output: A merged develop HEAD where `bash scripts/uat/phase-36.17.5-tts.sh` exits 0 with
5 GREEN gates; `cargo test -p ironhermes-tools --test tts_tools -- --ignored test_edge_synth_writes_file`
exits 0 against real Edge TTS; `hermes tts test --provider edge` writes an MP3 to
$IRONHERMES_HOME/audio_cache/ and prints the path; the operator has signed off via the
final checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-CONTEXT.md
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-RESEARCH.md
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-VALIDATION.md
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-01-SUMMARY.md
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-02-SUMMARY.md
@.planning/phases/36.17.5-integrate-tts-functions/36.17.5-03-SUMMARY.md
@scripts/uat/phase-36.17-web-logging.sh

<interfaces>
PLAN 01–03 froze these. PLAN 04 consumes them verbatim.

UAT script skeleton from PLAN 01 Task 7 (at scripts/uat/phase-36.17.5-tts.sh):

  - Has #!/usr/bin/env bash + set -euo pipefail
  - mktemp -d temp IRONHERMES_HOME with trap cleanup
  - cd "${WORKSPACE_ROOT}"
  - Gate 0 echo + exit 0

PLAN 04 fills in real Gate 1–5 logic between Gate 0 and the final echo.

CLI shape (from `crates/ironhermes-cli/src/main.rs` — confirm by grep for `Subcommand` /
`enum Command` / `enum Subcommands`):

The CLI uses clap derive. The new `Tts` subcommand mirrors existing top-level subcommands
(e.g., `Doctor`, `Cron`). New invocation: `hermes tts test [--provider edge|elevenlabs]
[--text "Hello"]` — emits the synthesized file path on stdout and exits 0; on error
prints to stderr and exits 1.

TTS-10 stub (from PLAN 01 Task 7, still ignored after PLAN 03):

  #[tokio::test]
  #[ignore = "TTS-10 — live network; opt-in via cargo test -- --ignored"]
  async fn test_edge_synth_writes_file() {
      unimplemented!("PLAN 04 will un-ignore once live UAT path is wired");
  }

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement `hermes tts test` CLI subcommand</name>
  <files>crates/ironhermes-cli/src/main.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/main.rs (the top-level Subcommand enum + the dispatch match; the new `Tts(TtsCommands)` arm slots alongside existing subcommands; find an existing simple subcommand like `Doctor` as the template)
    - crates/ironhermes-tools/src/tts/mod.rs (build_tts_registry factory from PLAN 02)
    - crates/ironhermes-core/src/constants.rs (get_hermes_home for path resolution)
    - crates/ironhermes-tools/src/tts_tool.rs (TextToSpeechTool — NOT used here; the CLI subcommand calls the registry directly to avoid pulling in the Tool trait runtime)
  </read_first>
  <action>
    Add a `Tts(TtsCommands)` arm to the top-level CLI Subcommands enum. Inside the
    `TtsCommands` enum add a single `Test` variant carrying:

      Test {
          /// Provider name (matches BUILTIN_TTS_NAMES). Default "edge".
          #[arg(long, default_value = "edge")]
          provider: String,

          /// Text to synthesize. Default "Hello from IronHermes Phase 36.17.5."
          #[arg(long, default_value = "Hello from IronHermes Phase 36.17.5.")]
          text: String,

          /// Optional output path. Defaults to $IRONHERMES_HOME/audio_cache/<uuid>.mp3.
          #[arg(long)]
          output_path: Option<String>,
      }

    Dispatch implementation in the match arm calls a new sibling helper
    `async fn cmd_tts_test(provider: String, text: String, output_path: Option<String>)
    -> anyhow::Result<()>`:

      1. Load config: `let config = ironhermes_core::Config::load()?;` (or whatever the
         existing CLI helper uses; mirror the Doctor subcommand's config-load path).
      2. Build registry: `let registry = ironhermes_tools::tts::build_tts_registry(and config.tts);`
      3. Resolve provider:
           let provider_arc = registry.get(and provider).ok_or_else(|| anyhow::anyhow!(
               "Provider '{}' not found in TtsRegistry (BUILTIN_TTS_NAMES: {:?})",
               provider, ironhermes_core::BUILTIN_TTS_NAMES))?;
      4. Check is_available:
           if !provider_arc.is_available() {
               anyhow::bail!("Provider '{}' is not available (missing env var?)", provider);
           }
      5. Resolve output path:
           let audio_dir = ironhermes_core::get_hermes_home().join("audio_cache");
           std::fs::create_dir_all(and audio_dir)?;
           let path = output_path.map(std::path::PathBuf::from).unwrap_or_else(||
               audio_dir.join(format!("{}.mp3", uuid::Uuid::new_v4())));
      6. Synthesize: `let written = provider_arc.synthesize(and text, and path).await?;`
      7. Print the path: `println!("{}", written.display());`
      8. `Ok(())`.

    This subcommand bypasses the Tool trait + LLM machinery — it's a thin shell over
    `build_tts_registry` + `TtsProvider::synthesize`. The UAT script (Task 2) calls this
    subcommand to drive Gates 1+2 without spinning up a full agent.

    Do NOT add this subcommand to `register_defaults_except` or any Tool-trait surface —
    it's a CLI verb, not an LLM tool (D-05: tool-only entry point is `text_to_speech`).
    Do NOT add `hermes tts setup` or `hermes tts list-voices` (D-09 / D-07 DEFERRED).
  </action>
  <verify>
    <automated>cargo build -p ironhermes-cli 2>and 1 | tee /tmp/36.17.5-04-t1.log; ! grep -E '^error' /tmp/36.17.5-04-t1.log; cargo run -p ironhermes-cli -- tts test --help 2>and 1 | tee /tmp/36.17.5-04-t1-help.log; grep -c 'provider' /tmp/36.17.5-04-t1-help.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Tts(TtsCommands)' crates/ironhermes-cli/src/main.rs` returns 1
    - `grep -c 'enum TtsCommands' crates/ironhermes-cli/src/main.rs` returns 1
    - `grep -c 'fn cmd_tts_test' crates/ironhermes-cli/src/main.rs` returns 1
    - `cargo build -p ironhermes-cli` exits 0
    - `cargo run -p ironhermes-cli -- tts test --help` prints help including `--provider`, `--text`, `--output_path`
    - `cargo run -p ironhermes-cli -- tts test --help` exits 0
    - No new `Tool` trait impl appears anywhere (CLI verb only, not LLM tool)
  </acceptance_criteria>
  <done>`hermes tts test` subcommand exists, wires to build_tts_registry directly, parses three flags, writes an mp3 to audio_cache and prints the path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Un-ignore TTS-10 + UAT script Gates 1–3 (automated)</name>
  <files>crates/ironhermes-tools/tests/tts_tools.rs, scripts/uat/phase-36.17.5-tts.sh</files>
  <read_first>
    - scripts/uat/phase-36.17-web-logging.sh (full file — verbatim model for the gate-echo pattern, GATE FAIL diagnostic shape, trap cleanup)
    - crates/ironhermes-tools/tests/tts_tools.rs (TTS-10 stub from PLAN 01 Task 7)
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-VALIDATION.md § UAT Script Specification (Gates 1–5)
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-RESEARCH.md § UAT Script Specification
  </read_first>
  <behavior>
    - TTS-10 `test_edge_synth_writes_file` (un-ignored, but still tagged with `#[ignore]` AND `#[ignore = "TTS-10 — live network"]` so it runs ONLY with `cargo test -- --ignored`): lock env_lock, set IRONHERMES_HOME to TempDir, construct EdgeProvider with default EdgeTtsConfig, call `synthesize("Hello from IronHermes Phase 36.17.5 TTS test.", and tmp_path)` with `tmp_path = TempDir.join(format!("{}.mp3", uuid))`; assert returned PathBuf == tmp_path; assert file exists; assert file size > 0; assert first 3 bytes are MP3 frame sync (0xFF 0xFB / 0xFF 0xF3 / 0xFF 0xF2 or ID3 tag `0x49 0x44 0x33`).
  </behavior>
  <action>
    Step 1: Un-ignore TTS-10 in `crates/ironhermes-tools/tests/tts_tools.rs`. Replace
    `unimplemented!()` with the Task 2 behavior body. Keep the `#[ignore]` attribute on
    the test (network is opt-in) but update the literal to
    `#[ignore = "TTS-10 — live network; runs with cargo test -- --ignored"]`.

    Step 2: Replace the Gate-0-only body of `scripts/uat/phase-36.17.5-tts.sh` with the
    real Gates 1–3 (automated). Preserve the header / set -euo pipefail / mktemp /
    trap cleanup / cd "${WORKSPACE_ROOT}" prefix from PLAN 01.

    Insert between the prefix and the final exit 0:

      # ----------------------------------------------------------------------
      # Gate 1 (D-03 / D-16): synth via `hermes tts test --provider edge`
      # creates audio_cache/ and writes exactly one UUID-named .mp3 file.
      # ----------------------------------------------------------------------
      echo "==> Gate 1 (D-03 / D-16): hermes tts test writes MP3 to audio_cache/"
      OUTPUT_PATH="$(cargo run --quiet -p ironhermes-cli -- tts test --provider edge --text 'Phase 36.17.5 gate 1 sample.' 2>and 1 | tail -1)"
      if [ -z "${OUTPUT_PATH}" ]; then
          echo "    GATE FAIL (D-03): hermes tts test produced no path on stdout"
          exit 1
      fi
      if [ ! -f "${OUTPUT_PATH}" ]; then
          echo "    GATE FAIL (D-16): output path '${OUTPUT_PATH}' does not exist"
          exit 1
      fi
      case "${OUTPUT_PATH}" in
          *"${IRONHERMES_HOME}/audio_cache/"*.mp3) : ;;
          *) echo "    GATE FAIL (D-16): output '${OUTPUT_PATH}' is not under audio_cache/"; exit 1 ;;
      esac
      echo "    OK (synth wrote ${OUTPUT_PATH})"
      echo

      # ----------------------------------------------------------------------
      # Gate 2 (D-04 / D-05): synthesized file is non-empty (real audio bytes).
      # ----------------------------------------------------------------------
      echo "==> Gate 2 (D-04 / D-05): synthesized file is non-empty"
      BYTES="$(wc -c < "${OUTPUT_PATH}" | tr -d ' ')"
      if [ "${BYTES}" -lt 1000 ]; then
          echo "    GATE FAIL (D-04 / D-05): file size ${BYTES} bytes is suspiciously small"
          exit 1
      fi
      # MP3 frame sync OR ID3 header
      FIRST_BYTES="$(head -c 3 "${OUTPUT_PATH}" | od -An -tx1 | tr -d ' ')"
      case "${FIRST_BYTES}" in
          fffb*|fff3*|fff2*|494433*) : ;;
          *) echo "    GATE FAIL (D-04 / D-05): unexpected file magic ${FIRST_BYTES} (need MP3 frame sync or ID3)"; exit 1 ;;
      esac
      echo "    OK (${BYTES} bytes, magic ${FIRST_BYTES})"
      echo

      # ----------------------------------------------------------------------
      # Gate 3 (D-10 / D-03): provider 'edge' is the BUILTIN_TTS_NAMES default
      # and the binary exits 0 without an API key (Edge is keyless).
      # ----------------------------------------------------------------------
      echo "==> Gate 3 (D-10 / D-03): edge provider requires no API key"
      # Unset any API key environment vars that might mask a real failure.
      unset ELEVENLABS_API_KEY OPENAI_API_KEY
      if ! cargo run --quiet -p ironhermes-cli -- tts test --provider edge --text 'Gate 3.' > /dev/null 2>and 1; then
          echo "    GATE FAIL (D-03): edge provider failed without an API key (expected to succeed)"
          exit 1
      fi
      echo "    OK"
      echo

    Gates 4 and 5 are added in Task 3 (require human-verify checkpoints). The final
    `echo "==> Phase 36.17.5 TTS UAT: all green"` stays at the very bottom.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-tools --test tts_tools -- --ignored test_edge_synth_writes_file 2>and 1 | tee /tmp/36.17.5-04-t2-net.log; bash scripts/uat/phase-36.17.5-tts.sh 2>and 1 | tee /tmp/36.17.5-04-t2-uat.log; grep -E 'test result: ok' /tmp/36.17.5-04-t2-net.log and and  grep -c 'Gate 3' /tmp/36.17.5-04-t2-uat.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '#\[ignore = "TTS-10' crates/ironhermes-tools/tests/tts_tools.rs` returns 1
    - `grep -c 'unimplemented!()' crates/ironhermes-tools/tests/tts_tools.rs` returns 0 (all stubs replaced)
    - `cargo test -p ironhermes-tools --test tts_tools -- --ignored test_edge_synth_writes_file` exits 0 (requires network; if running offline mark this acceptance as deferred and note in summary)
    - `grep -c 'Gate 1' scripts/uat/phase-36.17.5-tts.sh` returns >= 1
    - `grep -c 'Gate 2' scripts/uat/phase-36.17.5-tts.sh` returns >= 1
    - `grep -c 'Gate 3' scripts/uat/phase-36.17.5-tts.sh` returns >= 1
    - `grep -c 'hermes tts test\|cargo run.*tts test' scripts/uat/phase-36.17.5-tts.sh` returns >= 1
    - `grep -c 'GATE FAIL' scripts/uat/phase-36.17.5-tts.sh` returns >= 3 (each gate has a diagnostic)
    - `bash scripts/uat/phase-36.17.5-tts.sh` exits 0 when run with network access
  </acceptance_criteria>
  <done>TTS-10 un-ignored and passes with --ignored against live Edge TTS; UAT script Gates 1–3 are automated and pass on a development machine with network access.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BLOCKING Gate 4 — operator verifies tool registry exposes both tools</name>
  <what-built>
    PLAN 03 wired register_tts_tools into build_app_runtime_bundle conditionally on
    `session_key: Some(...)`. Task 3 of PLAN 04 verifies that wiring actually surfaces
    BOTH tools (`text_to_speech` AND `send_audio`) to the operator-facing tool list — the
    receiver-end half of the register_tts_tools contract.
  </what-built>
  <how-to-verify>
    1. Run `cargo build --release -p ironhermes-cli` (cold-build acceptable, ~3-5 minutes).
    2. Invoke `IRONHERMES_HOME=/tmp/uat-36.17.5-home target/release/hermes tools list` (or
       the equivalent existing tools-listing subcommand — verify the exact verb via
       `hermes --help`).
    3. Confirm that BOTH tool names appear in the output:
       - `text_to_speech`  (under toolset `voice`)
       - `send_audio`      (under toolset `voice`)
    4. Optionally: invoke `hermes tools list --toolset voice` if that flag exists — both
       names must appear there too.
    5. If only one or zero appear, abort: PLAN 03's register_tts_tools is not actually
       being invoked from the CLI startup path. The fix is a follow-up phase that wires
       session_key=Some(SessionKey::new(Platform::Local, "local")) into the CLI's
       AppRuntimeFactoryInput construction site.

    Add the gate to scripts/uat/phase-36.17.5-tts.sh as a documentation block — the script
    cannot run this gate automatically because PLAN 03 makes session_key optional and
    the CLI's startup path may pass None. Document the manual step in the script header.
  </how-to-verify>
  <resume-signal>Type `approved` if both tools appear; type `text_to_speech missing` or `send_audio missing` to identify the gap; type `abort` to stop and queue a follow-up wiring phase</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: BLOCKING Gate 5 — operator verifies Platform::Local arm plays audibly OR fails cleanly</name>
  <what-built>
    PLAN 03 Task 3 implemented the SendAudioTool Platform::Local arm via rodio. Gate 5
    requires a human ear (or a clean error in the absence of an audio device).
  </what-built>
  <how-to-verify>
    1. Take the OUTPUT_PATH from Gate 1 (the MP3 written to audio_cache/).
    2. Invoke a CLI command (or unit-test driver) that constructs a SendAudioTool with
       SessionKey::new(Platform::Local, "uat") + None dispatcher, then calls
       execute({"path": "<OUTPUT_PATH>"}).

       The simplest path: add ONE additional CLI subcommand `hermes tts play --path <P>`
       in this task — small change to main.rs that constructs SendAudioTool with
       Platform::Local and invokes execute.

    3. On a machine WITH an audio device:
       - Headphones / speakers required.
       - Run `cargo run -p ironhermes-cli -- tts play --path ${OUTPUT_PATH}`.
       - Confirm audible speech of "Phase 36.17.5 gate 1 sample." (or equivalent text).
    4. On a machine WITHOUT an audio device (CI, headless server):
       - Run the same command.
       - Confirm a clean Err with message containing "No audio device" or similar.
       - The binary MUST exit non-zero but MUST NOT panic.
    5. Verify the OUTPUT_PATH file was NOT deleted by playback (D-16 — no cleanup).
  </how-to-verify>
  <resume-signal>Type `approved (heard audio)` if speech played audibly; `approved (no device, clean error)` if running headless and the tool returned a clean Err; `panic` or describe failure mode otherwise</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Add `hermes tts play` subcommand + finalize VALIDATION.md frontmatter</name>
  <files>crates/ironhermes-cli/src/main.rs, .planning/phases/36.17.5-integrate-tts-functions/36.17.5-VALIDATION.md</files>
  <read_first>
    - crates/ironhermes-cli/src/main.rs (the TtsCommands enum from Task 1 — add a Play variant)
    - crates/ironhermes-tools/src/send_audio_tool.rs (SendAudioTool from PLAN 03)
    - crates/ironhermes-core/src/types.rs (Platform enum)
    - crates/ironhermes-core/src/session.rs (SessionKey constructor)
    - .planning/phases/36.17.5-integrate-tts-functions/36.17.5-VALIDATION.md (Per-Task Verification Map — flip ⬜ to ✅ on each row; flip `nyquist_compliant` to true; flip `wave_0_complete` to true)
  </read_first>
  <action>
    Step 1: Add `Play { #[arg(long)] path: String }` variant to the `TtsCommands` enum
    introduced in Task 1.

    Step 2: Implement `async fn cmd_tts_play(path: String) -> anyhow::Result<()>`:
      1. Construct SessionKey: `let key = ironhermes_core::SessionKey::new(
             ironhermes_core::types::Platform::Local, "uat");`
      2. Construct SendAudioTool: `let tool = ironhermes_tools::SendAudioTool::new(
             key, None, std::sync::Arc::new(ironhermes_core::Config::load()?));`
      3. Invoke via the Tool trait: `let result = tool.execute(
             serde_json::json!({ "path": path })).await?;`
      4. `println!("{}", result);`
      5. `Ok(())`.

    Step 3: Update `.planning/phases/36.17.5-integrate-tts-functions/36.17.5-VALIDATION.md`:
      - Frontmatter: set `nyquist_compliant: true` and `wave_0_complete: true`.
      - Per-Task Verification Map: replace each `TBD` Plan ID with the actual owner plan
        (TTS-01/02/07/08 → PLAN 01; TTS-03/04/09 → PLAN 02; TTS-05/06 → PLAN 03; TTS-10 → PLAN 04).
      - Status column: flip ⬜ to ✅ for every row whose `cargo test` command exited 0 in
        the executor's CI run.
      - Append a `## Phase Close-Out` section summarizing UAT outcomes:
        - Gate 1 PASS/FAIL with the OUTPUT_PATH from the run
        - Gate 2 PASS/FAIL with the BYTES count
        - Gate 3 PASS/FAIL
        - Gate 4 PASS/FAIL with the operator-checkpoint timestamp
        - Gate 5 PASS/FAIL with the audio-device disposition (heard / no-device clean error)
        - TTS-10 PASS (live network) — date / runner / network status
  </action>
  <verify>
    <automated>cargo build -p ironhermes-cli 2>and 1 | tee /tmp/36.17.5-04-t5-build.log; ! grep -E '^error' /tmp/36.17.5-04-t5-build.log; cargo run -p ironhermes-cli -- tts play --help 2>and 1 | tee /tmp/36.17.5-04-t5-help.log; grep -c '\-\-path' /tmp/36.17.5-04-t5-help.log; grep -c '^nyquist_compliant: true' .planning/phases/36.17.5-integrate-tts-functions/36.17.5-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Play { ' crates/ironhermes-cli/src/main.rs` returns >= 1 (TtsCommands::Play variant)
    - `grep -c 'fn cmd_tts_play' crates/ironhermes-cli/src/main.rs` returns 1
    - `cargo run -p ironhermes-cli -- tts play --help` exits 0 and shows `--path`
    - `grep -c '^nyquist_compliant: true' .planning/phases/36.17.5-integrate-tts-functions/36.17.5-VALIDATION.md` returns 1
    - `grep -c '^wave_0_complete: true' .planning/phases/36.17.5-integrate-tts-functions/36.17.5-VALIDATION.md` returns 1
    - `grep -c '## Phase Close-Out' .planning/phases/36.17.5-integrate-tts-functions/36.17.5-VALIDATION.md` returns 1
    - `grep -c 'TBD' .planning/phases/36.17.5-integrate-tts-functions/36.17.5-VALIDATION.md` returns 0 (every test row has its real owner plan)
    - `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>`hermes tts play` subcommand exists and enables Gate 5; VALIDATION.md is sealed with nyquist_compliant=true, wave_0_complete=true, every TTS-NN row mapped to its owner plan, and the Phase Close-Out section records the 5-gate outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator's PATH | The UAT script invokes `cargo run` which trusts the operator's installed toolchain |
| Network → Microsoft Edge TTS WSS | Live network call during Gate 1 + 2 + 3 and TTS-10 — first crossing of a third-party endpoint in this phase's automated path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-uat-no-network | DoS | UAT Gates 1–3 + TTS-10 | accept | The UAT script aborts cleanly with `GATE FAIL` if `cargo run -- tts test` returns non-zero. Operator running offline should not run the UAT. TTS-10 is gated behind `cargo test -- --ignored` so it does not run by default. |
| T-msedge-flaky | DoS | Edge TTS endpoint flakiness | accept | The Edge TTS WSS endpoint can return transient errors. UAT script does not implement retries — operator re-runs on failure. Adding retry logic is out of scope (D-09 / D-11 — no typed retry surface this phase). |
| T-platform-detection | EoP | `hermes tts play` defaults to Platform::Local | mitigate | The Play subcommand HARDCODES Platform::Local + chat_id="uat". The operator cannot accidentally invoke Telegram dispatch from the CLI (no `--platform` flag on tts play — it's a diagnostic command, not a delivery path). The agent path retains the SessionKey-aware dispatch from PLAN 03. |

Note: T-output-path BLOCKING mitigation is locked by PLAN 03's unit test. PLAN 04's UAT
re-exercises the happy path through `hermes tts test` (default output_path under
audio_cache/) but does NOT re-test the traversal-blocked path — that test already runs as
a unit test in every CI run.
</threat_model>

<verification>
- `cargo build --workspace` exits 0
- `bash scripts/uat/phase-36.17.5-tts.sh` exits 0 with Gates 1–3 PASS on a development machine with network
- Gate 4 BLOCKING checkpoint approved by operator with both tool names present
- Gate 5 BLOCKING checkpoint approved with either audible playback or clean no-device error
- `cargo test -p ironhermes-tools --test tts_tools -- --ignored test_edge_synth_writes_file` exits 0 (run at least once during PLAN 04 close)
- `cargo test --workspace` shows zero NEW failures vs. pre-merge baseline
- VALIDATION.md frontmatter: `nyquist_compliant: true`, `wave_0_complete: true`, zero `TBD` rows
</verification>

<success_criteria>
- `hermes tts test [--provider <name>] [--text <s>] [--output_path <p>]` subcommand exists, writes mp3 to audio_cache/, prints the path
- `hermes tts play --path <p>` subcommand exists, plays via rodio on Platform::Local, errors cleanly without an audio device
- scripts/uat/phase-36.17.5-tts.sh implements all 5 gates (1–3 automated, 4–5 human-verify in the executor's checkpoint flow)
- TTS-10 live-network integration test passes when invoked with --ignored
- 10/10 TTS-NN rows in VALIDATION.md sealed with their owner plan ID and ✅ status
- VALIDATION.md frontmatter flipped to nyquist_compliant=true + wave_0_complete=true
- Phase Close-Out section in VALIDATION.md records the 5-gate outcomes for posterity
- Zero D-09 / D-11 violations (no `hermes tts list-voices`, no `hermes tts setup`, no typed errors)
- Operator has explicitly approved both human-verify checkpoints
</success_criteria>

<output>
Create `.planning/phases/36.17.5-integrate-tts-functions/36.17.5-04-SUMMARY.md` when done.
Include in the summary:
- The actual OUTPUT_PATH from Gate 1 (proves the synth wrote a real file to a real path)
- The BYTES count from Gate 2 (proves it's non-trivial audio, not a stub)
- Gate 4 operator verdict + the exact `hermes tools list` output line(s) confirming both names
- Gate 5 operator verdict + audio-device disposition
- TTS-10 run timestamp + outcome
- Any deviation from the 5-gate plan (should be NONE)
- The final state of VALIDATION.md (nyquist_compliant=true, wave_0_complete=true, all rows ✅)
- Recommended follow-up phase to thread SessionKey from `run_turn(TurnRequest)` so the
  per-session register_tts_tools actually fires for in-agent text_to_speech calls (PLAN 03
  shipped the optional fields but the CLI/agent path passes None for v1 — this is a known
  forward-compat gap, not a phase failure)
</output>
