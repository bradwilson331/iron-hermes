# Phase 36.17.5: integrate-tts-functions - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-03
**Phase:** 36.17.5-integrate-tts-functions
**Areas discussed:** Phase scope boundary, Integration model, Provider abstraction, Delivery surfaces

---

## Phase scope boundary

### Q1 — Urgency driver

| Option | Description | Selected |
|--------|-------------|----------|
| Need TTS in production NOW | Telegram/Discord/CLI users want spoken replies today; ship smallest slice fastest | |
| Demo / showcase deadline | Specific demoable event soon — optimize for wow-factor | |
| Catch up on parity gap | Python hermes-agent has TTS; IronHermes doesn't — gap is embarrassing or blocking | ✓ |
| Personal-use itch / unblocking myself | Personal daily use — UX-driven, no external deadline | |

**User's choice:** Catch up on parity gap
**Notes:** No hard deadline. Port the concept faithfully; resist the pressure to over-ship.

### Q2 — Phase width

| Option | Description | Selected |
|--------|-------------|----------|
| TTS output only | Defer STT + Voice Mode to own phases | |
| TTS + STT | Voice in & out on messaging | |
| TTS + STT + full Voice Mode | Full Python parity incl. push-to-talk + Discord VC | |
| TTS-output AND port the Provider trait/registry | Working Edge TTS + the TtsProvider trait + registry abstraction | ✓ |

**User's choice:** TTS-output AND port the Provider trait/registry
**Notes:** Matches the dropped `tts_provider.py` + `tts_registry.py` seed files — confirms the trait/registry pattern is the deliverable, not a single hardcoded provider.

### Q3 — Provider lineup

| Option | Description | Selected |
|--------|-------------|----------|
| Edge TTS only | One provider, zero API keys | |
| Edge TTS + OpenAI TTS | Free default + paid-but-trivial second to prove the abstraction | |
| Edge TTS + ElevenLabs | Free default + premium provider — mirrors Python's most-used pairing | ✓ |
| Edge + OpenAI + ElevenLabs | Top 3 of Python's lineup; larger phase | |

**User's choice:** Edge TTS + ElevenLabs

### Q4 — ffmpeg policy

| Option | Description | Selected |
|--------|-------------|----------|
| Hard-require ffmpeg in PATH | Like Python — doctor warns, tokio::process for MP3→Opus | |
| Pure-Rust opus encoding | `opus` / `audiopus` crate — no system dep | |
| Defer Opus conversion entirely | MP3 ships as audio attachment; only ElevenLabs voice-bubbles | |
| ffmpeg optional, opt-in via config | Detect at startup; present → bubbles for both; absent → fallbacks | ✓ |

**User's choice:** ffmpeg optional, opt-in via config (detected at startup) — matches Python's actual behavior in tts.md.

---

## Integration model

### Q1 — Hook vs Tool

| Option | Description | Selected |
|--------|-------------|----------|
| Tool only (Path B) | LLM-callable `text_to_speech` only — smallest blast radius, matches existing Tool trait | ✓ |
| Hook only (Path A) | Auto-speak every reply via ChatStreamEvent interception | |
| Both (Python parity) | Tool + auto-speak hook + /voice on/off/tts commands | |
| Tool + thin auto-reply hook (Telegram only) | Middle path — Telegram-only voice_mode | |

**User's choice:** Tool only (Path B)

### Q2 — Tool I/O shape

| Option | Description | Selected |
|--------|-------------|----------|
| Write to file + return path | Path string return, smallest scope | ✓ |
| Write + auto-play locally | rodio inline — couples synthesis to playback | |
| Write + return base64/bytes for gateway | Bytes in return value | |
| Write + return path + side-channel hint | Quietly registers (turn_id → path) for gateway pickup | |

**User's choice:** Write to file + return path

### Q3 — Tool arg schema

| Option | Description | Selected |
|--------|-------------|----------|
| `text` only | All voice/model/speed from config | |
| `text` + optional `output_path` | Plus optional output path | ✓ (this phase) |
| `text` + `output_path` + optional voice/speed | Per-call voice override | |
| Full Python parity (text/voice/model/speed/output_format/output_path) | All 6 fields | ✓ (next phase, per user) |

**User's choice (verbatim):** "option 2 this phase with the plans to expand to option 4 next phase"
**Notes:** v1 = `text` + optional `output_path`. Follow-up phase expands to full Python-parity arg surface. Captured in CONTEXT.md D-07 as a forward-looking note so the deferred expansion is on the roadmap.

### Q4 — Crate placement

| Option | Description | Selected |
|--------|-------------|----------|
| All in ironhermes-tools | Trait + registry + impls + tool in one crate | |
| New crate: ironhermes-voice | Dedicated voice crate ready for STT and Voice Mode | |
| Trait/registry in ironhermes-core, impls in ironhermes-tools | Matches SessionKey-in-core pattern from 36.17.3 | ✓ |
| Trait in core, impls split per-provider | Per-provider feature gating; premature for 2 providers | |

**User's choice:** Trait/registry in ironhermes-core, impls in ironhermes-tools

---

## Provider abstraction

### Q1 — Trait surface

| Option | Description | Selected |
|--------|-------------|----------|
| Minimal: name + synthesize | YAGNI-strong; later expansion is breaking | |
| Core: name + display_name + is_available + synthesize | Defensible minimum for two providers | |
| Core + voice_compatible flag | Same as Core plus the gateway-delivery hint | ✓ |
| Full Python parity minus stream | All 9 ABC methods except `stream()` | |

**User's choice:** Core + voice_compatible flag
**Notes:** `list_voices`, `list_models`, `default_voice`, `default_model`, `get_setup_schema`, `stream` are DEFERRED to future phases.

### Q2 — Built-ins-always-win invariant

| Option | Description | Selected |
|--------|-------------|----------|
| Drop the invariant entirely | Plugin loader is rejected; collisions are programmer errors | |
| Keep as a registration-time assert | Panic or warn-ignore on collision | |
| Keep + reserve names in a frozen set | Mirror Python's `_BUILTIN_NAMES` frozenset | ✓ |
| Defer the invariant entirely | Smallest scope this phase | |

**User's choice:** Keep + reserve built-in names in a frozen set
**Notes:** Sets up the invariant for a hypothetical future user-extension reversal even though the plugin loader is rejected today.

### Q3 — Error propagation

| Option | Description | Selected |
|--------|-------------|----------|
| `anyhow::Result<PathBuf>` | Matches Tool trait pattern + project anyhow convention | ✓ |
| Typed `TtsError` enum | NotConfigured/HttpFailed/RateLimited/AudioFormatUnsupported variants | |
| thiserror enum re-exported from ironhermes-core::error | Single project-wide error type | |
| anyhow now, refactor later | Equivalent to option 1 with explicit YAGNI note | |

**User's choice:** anyhow::Result<PathBuf>

### Q4 — Config shape

| Option | Description | Selected |
|--------|-------------|----------|
| `TtsConfig` with provider + per-provider sub-blocks | Mirrors Python's tts.* nesting | |
| Flat TtsConfig with provider-prefixed fields | Single struct, smaller serde surface | |
| TtsConfig provider + extra: serde_json::Value | Untyped JSON blob per provider | |
| TtsConfig with provider + strongly-typed per-provider sub-blocks (recommended) | Like option 1 but explicitly strongly typed | ✓ |

**User's choice:** TtsConfig provider + per-provider strongly-typed sub-blocks (recommended)
**Notes:** Matches the project's existing `ModelConfig` style. Adding a new provider in a future phase = adding a new strongly-typed field; no `serde_json::Value` escape hatches.

---

## Delivery surfaces

### Q1 — Which surfaces this phase

| Option | Description | Selected |
|--------|-------------|----------|
| None — file path only | Smallest scope; agent uses terminal tool to play | |
| Telegram voice/audio attach | Gateway hook + ffmpeg fallback | ✓ |
| Telegram + Discord attach | Both messaging platforms | |
| Telegram + Discord + iron_hermes_ui web | All three messaging surfaces | |

**User's choice:** Telegram voice/audio attach
**Notes:** Discord and web auto-delivery deferred to future phases. CONTEXT.md captures both as "Mid-term" deferred ideas.

### Q2 — Detection mechanism

| Option | Description | Selected |
|--------|-------------|----------|
| Agent must call a dedicated `send_audio` tool | Explicit, two-tool sequence | ✓ |
| Reconsider 'no side-channel' decision | Add side-channel hint to text_to_speech | |
| Gateway scans tool call results for audio paths | Heuristic trajectory walk | |
| Defer auto-attach entirely | Drop the Telegram wiring this phase | |

**User's choice:** Agent must call a dedicated send_audio tool
**Notes:** Two-tool sequence is intentional — explicit > implicit. Matches the Python Path B pattern from `tts-idea.txt`.

### Q3 — send_audio shape

| Option | Description | Selected |
|--------|-------------|----------|
| Single `send_audio(path)` tool, platform from session context | Forward-compatible, future phases add Discord/web arms | ✓ |
| `send_audio(platform, chat_id, path)` | Explicit destination args | |
| Platform-specific tools | One tool per platform | |
| `send_audio` with `as_voice: bool` flag | Voice-bubble vs file hint | |

**User's choice:** Single `send_audio(path)` tool, platform from session context
**Notes:** CLI Local arm plays via rodio; Telegram arm dispatches to `TelegramAdapter::send_voice` / `send_audio`. Future Discord / web phases add new arms without a new tool.

### Q4 — Audio cache + cleanup

| Option | Description | Selected |
|--------|-------------|----------|
| `$HERMES_HOME/audio_cache/` — no cleanup this phase | Smallest scope | |
| Size-cap eviction | 500 MB rolling cap | |
| 7-day age eviction | Simpler than size-cap | |
| `tempfile::NamedTempFile` — OS-managed | OS-cleans on reboot, ephemeral | |

**User's choice (verbatim):** "Option 1 for $IRONHERMES_HOME/audio_cache/ not $HERMES_HOME"
**Notes:** **Important correction** — the env var is `$IRONHERMES_HOME`, NOT `$HERMES_HOME`. The project uses `~/.ironhermes/`. Any reference to `HERMES_HOME` from the seed docs (which are from the Python project) must be translated. Captured in CONTEXT.md D-16 + Specifics section.

---

## Claude's Discretion

- Exact `toolset()` grouping name for `TextToSpeechTool` + `SendAudioTool` (suggested `"voice"`).
- `edge-tts-rust` crate currency at planning time — researcher verifies; falls back to alternative if abandoned.
- Default voice IDs for `EdgeTtsConfig` and `ElevenLabsConfig` (mirror Python defaults unless researcher finds a stronger Rust-side choice).
- `is_available()` for ElevenLabs: env var direct read vs Config look-up — pick whichever matches `WebSearchTool`.
- UAT script shape — model on `scripts/uat/phase-36.17-web-logging.sh`.
- `Platform::Local` arm of `send_audio` — gate on `voice_compatible` or always play? Recommend always-play; extension is source of truth.

## Deferred Ideas

**Next-phase candidates (close successors):**
- Expand `text_to_speech` arg schema to full Python parity (`voice`, `model`, `speed`, `output_format`).
- STT (voice-message transcription) — mirrors the trait/registry pattern landed here.
- Auto-speak hook (Path A) — requires the `stream()` trait method this phase deferred.

**Mid-term:**
- More providers behind the trait: OpenAI TTS, MiniMax, Mistral, Gemini, xAI, NeuTTS, KittenTTS, Piper.
- Discord delivery — extend `SendAudioTool` with `Platform::Discord` arm.
- `iron_hermes_ui` web delivery — extend with web arm; HTML5 audio over WS.

**Long-term:**
- Full Voice Mode (Ctrl+B CLI + Discord voice channels + per-platform mode toggling).
- Audio cache cleanup (size-cap or age-based eviction).
- Pure-Rust Opus encoding (`audiopus` / `opus`) to drop the ffmpeg optional dep.
- Voice cloning support (xAI custom voices, ElevenLabs voice library) — requires `list_voices()`.
- `hermes tts test` / `hermes tts setup` CLI subcommands.

**REJECTED (do not re-litigate):**
- Command-type providers / Python plugin providers — project-level plugin-loader rejection (Phase 36.13 ADR) forecloses this entirely.
