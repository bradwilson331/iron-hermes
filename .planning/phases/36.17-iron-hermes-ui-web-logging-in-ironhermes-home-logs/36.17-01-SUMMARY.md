---
phase: 36.17-iron-hermes-ui-web-logging-in-ironhermes-home-logs
plan: 01
subsystem: infra
tags: [rust, cargo, tracing, tracing-appender, tower-http, axum, workspace-deps]

# Dependency graph
requires:
  - phase: 36.17 (eedb49e1 — earlier in this phase's lineage)
    provides: tracing-appender = "0.2" declared in workspace [workspace.dependencies]
provides:
  - workspace-level tower-http = "0.6" with the "trace" feature
  - iron_hermes_ui (non-wasm32) direct dependency on tracing-appender
  - iron_hermes_ui (non-wasm32) direct dependency on tower-http with "trace" feature
  - foundational Cargo manifest gate that unblocks Wave 2 logging-helper and main.rs-wiring plans
affects:
  - 36.17-02 (logging helper / install_web_logger_subscriber)
  - 36.17-03 (main.rs wiring + TraceLayer mount)
  - any future plan that mounts tower_http::trace::TraceLayer in iron_hermes_ui

# Tech tracking
tech-stack:
  added:
    - "tower-http 0.6 (workspace dep, features=[trace]) — TraceLayer HTTP access middleware for Axum"
  patterns:
    - "Crate-level feature override at call site: features = [\"trace\"] repeated on the workspace-resolved tower-http line in iron_hermes_ui/Cargo.toml, mirroring the existing dioxus = { version = \"=0.7.1\", features = [\"fullstack\"] } convention in the same manifest"
    - "Pin direct workspace deps to the line already resolved in Cargo.lock as a transitive (tower-http 0.6.8 via dioxus-cli-config / dioxus-devtools / reqwest / ironhermes-cli) to keep version churn zero-cost"

key-files:
  created: []
  modified:
    - Cargo.toml
    - crates/iron_hermes_ui/Cargo.toml
    - Cargo.lock

key-decisions:
  - "Pinned tower-http to 0.6 (resolves to 0.6.8 already in Cargo.lock) — compatible with axum = \"=0.8.4\" and avoids introducing a new transitive resolution"
  - "Explicit features = [\"trace\"] at the iron_hermes_ui call site (in addition to the workspace declaration) for grep-ability and to mirror existing per-crate feature-list style"
  - "Committed Cargo.lock graph-edge update (tower-http + tracing-appender added to iron_hermes_ui's deps array) in a separate commit alongside the manifest edits to keep the lockfile in sync with workspace intent"

patterns-established:
  - "Foundational Cargo wave-1 plan structure: workspace dep declaration commit, then crate-level commit that also runs cargo build as its own verification gate, then a lockfile commit when the build induces a graph-edge change"

requirements-completed:
  - D-13
  - D-14

# Metrics
duration: 2m 43s
completed: 2026-05-27
---

# Phase 36.17 Plan 01: Cargo manifest wiring for iron_hermes_ui web logging — Summary

**Workspace + crate Cargo.toml now declare tower-http 0.6 (features=[trace]) and tracing-appender at the iron_hermes_ui non-wasm32 dependency block; `cargo build -p iron_hermes_ui --features server` exits 0, unblocking the Wave 2 logging-helper and TraceLayer-mount work.**

## Performance

- **Duration:** 2m 43s
- **Started:** 2026-05-27T11:39:22Z
- **Completed:** 2026-05-27T11:42:05Z
- **Tasks:** 2 (+ 1 lockfile sync commit)
- **Files modified:** 3 (Cargo.toml workspace, crates/iron_hermes_ui/Cargo.toml, Cargo.lock)

## Accomplishments

- Workspace `[workspace.dependencies]` now declares `tower-http = { version = "0.6", features = ["trace"] }` in the `# Logging` group, immediately after the existing `tracing-appender = "0.2"` line — preserving the comment-grouped four-line layout established by `eedb49e1`.
- `crates/iron_hermes_ui/Cargo.toml` non-wasm32 dependency block now wires `tracing-appender = { workspace = true }` and `tower-http = { workspace = true, features = ["trace"] }` immediately after `tracing-subscriber = { workspace = true }`, inside `[target.'cfg(not(target_arch = "wasm32"))'.dependencies]`. The wasm32 client build is untouched.
- `cargo build -p iron_hermes_ui --features server` exits 0 (finished in 47.93s on a clean incremental build), confirming both new deps resolve and that the `trace` feature on `tower-http` is correctly enabled. The 17 emitted warnings are all pre-existing dead-code warnings in `crates/iron_hermes_ui/src/state.rs` unrelated to this plan — left out of scope.

## Task Commits

Each task was committed atomically:

1. **Task 1: Declare tower-http in workspace Cargo.toml** — `7ae31ee6` (chore)
2. **Task 2: Add tracing-appender + tower-http to iron_hermes_ui non-wasm32 deps and verify build** — `ead2a267` (chore)
3. **Lockfile sync (Cargo.lock graph-edge update from Task 2's verification build)** — `0f5c3261` (chore)

_Note: The lockfile commit is a follow-on to Task 2, not a separate planned task. Cargo.lock gained two graph-edges (`tower-http`, `tracing-appender`) under the `iron_hermes_ui` dependencies array; no version changes._

## Files Created/Modified

- `Cargo.toml` (workspace root) — added one line: `tower-http = { version = "0.6", features = ["trace"] }` in the `# Logging` group.
- `crates/iron_hermes_ui/Cargo.toml` — added two lines inside `[target.'cfg(not(target_arch = "wasm32"))'.dependencies]`: `tracing-appender = { workspace = true }` and `tower-http = { workspace = true, features = ["trace"] }`.
- `Cargo.lock` — under `[[package]] name = "iron_hermes_ui"`, added `"tower-http"` and `"tracing-appender"` to the `dependencies` array. No new package entries created (both already present at 0.6.8 and 0.2.x respectively).

## Decisions Made

- **tower-http version pinned to 0.6 (resolves to 0.6.8).** Cargo.lock already had `tower-http 0.6.8` resolved as a transitive of `dioxus-cli-config`, `dioxus-devtools`, `reqwest`, and `ironhermes-cli`. Pinning to `"0.6"` reuses the existing resolution rather than introducing a new minor line, and remains compatible with the workspace's `axum = "=0.8.4"` constraint. Source: RESEARCH.md Q1.
- **Feature `["trace"]` repeated at the crate call site.** Both the workspace declaration and the iron_hermes_ui crate declaration carry `features = ["trace"]`. The crate-level repetition is grep-friendly and mirrors the existing `dioxus = { version = "=0.7.1", features = ["fullstack"] }` convention in the same manifest.
- **Separate lockfile commit instead of amending Task 2.** Per the executor's git safety protocol ("create NEW commits rather than amending"), the Cargo.lock graph-edge update was committed as `0f5c3261` rather than folded into Task 2 — preserving the per-task atomicity of the manifest edit and the lockfile sync.

## Deviations from Plan

None — plan executed exactly as written. Task 1 and Task 2 acceptance criteria all passed on first attempt. The additional Cargo.lock commit is a mechanical consequence of Task 2's build-verification step and is not a deviation; the plan's `<verification>` block explicitly required `cargo build -p iron_hermes_ui --features server` to succeed, which is what induced the lockfile graph-edge update.

## Issues Encountered

None.

## Threat Surface

No new threat surface introduced beyond what the plan's `<threat_model>` already covered:

- T-36.17-01 (Tampering — tower-http crate from crates.io): Mitigation in place — pinned to `0.6`, the same line already resolved in `Cargo.lock` at `0.6.8` as a transitive; publisher is `tower-rs` (canonical), hash check enforced by `Cargo.lock`.
- T-36.17-SC (Tampering — package install): Both `tower-http` and `tracing-appender` are `[VERIFIED]` in RESEARCH.md's `## Package Legitimacy Audit` — no `[ASSUMED]`/`[SUS]` packages introduced; no blocking-human checkpoint was required.

## User Setup Required

None — this plan only modifies build-time Cargo manifests. No environment variables, no service configuration.

## Next Phase Readiness

- **Wave 2 unblocked.** `iron_hermes_ui` can now `use tracing_appender::rolling::daily` and `use tower_http::trace::TraceLayer` without any further manifest edits.
- **Wave 2 reminder (from RESEARCH.md Q2):** `tower_http::trace::DefaultOnRequest` defaults to `Level::DEBUG`, not `Level::INFO`. The Wave 2 plan that mounts `TraceLayer::new_for_http()` MUST explicitly call `.on_request(DefaultOnRequest::new().level(Level::INFO))` and `.on_response(DefaultOnResponse::new().level(Level::INFO))` or `web-access.log` will exist but be empty (filtered out by the locked per-layer `tower_http::trace=info` filter from D-05).
- **No blockers.** Wasm32 client build is unaffected. Pre-existing dead-code warnings in `state.rs` are out of scope for this phase.

## Self-Check: PASSED

- File `Cargo.toml` contains exactly one `tower-http = { version = "0.6", features = ["trace"] }` line: FOUND.
- File `crates/iron_hermes_ui/Cargo.toml` contains exactly one `tracing-appender = { workspace = true }` line and one `tower-http = { workspace = true, features = ["trace"] }` line: FOUND.
- Commit `7ae31ee6` (Task 1) exists on this branch: FOUND.
- Commit `ead2a267` (Task 2) exists on this branch: FOUND.
- Commit `0f5c3261` (Lockfile sync) exists on this branch: FOUND.
- `cargo build -p iron_hermes_ui --features server` exits 0: VERIFIED (47.93s, exit code 0).

---
*Phase: 36.17-iron-hermes-ui-web-logging-in-ironhermes-home-logs*
*Completed: 2026-05-27*
