---
phase: 36.17
plan: 02
type: execute
wave: 2
depends_on:
  - 36.17-01
files_modified:
  - crates/iron_hermes_ui/src/server/mod.rs
  - crates/iron_hermes_ui/src/server/logging.rs
autonomous: true
requirements:
  - D-02
  - D-03
  - D-04
  - D-05
  - D-15
  - D-16
  - D-17
  - D-18
must_haves:
  truths:
    - "install_web_logger_subscriber() is callable from main.rs as server::logging::install_web_logger_subscriber()"
    - "The function returns (Option<WorkerGuard>, Option<WorkerGuard>) per D-17"
    - "The function creates $IRONHERMES_HOME/logs before installing file layers (D-16)"
    - "On create_dir_all failure the function still installs the console layer and returns (None, None) (TUI parity)"
    - "Both file layers are constructed with .with_ansi(false) (D-03)"
    - "The web.log layer's filter falls back to EnvFilter::new(\"ironhermes=info,iron_hermes_ui=info\") when RUST_LOG is unset (D-04)"
    - "The web-access.log layer uses fixed EnvFilter::new(\"tower_http::trace=info\") independent of RUST_LOG (D-05)"
    - "Registry is composed with .with(console_layer).with(web_file_layer).with(access_file_layer) and installed via try_init (D-18)"
  artifacts:
    - path: "crates/iron_hermes_ui/src/server/logging.rs"
      provides: "install_web_logger_subscriber() helper"
      contains: "pub fn install_web_logger_subscriber"
      min_lines: 50
    - path: "crates/iron_hermes_ui/src/server/mod.rs"
      provides: "Module declaration for logging (server-only)"
      contains: "pub mod logging"
  key_links:
    - from: "crates/iron_hermes_ui/src/server/logging.rs"
      to: "ironhermes_core::constants::get_hermes_home"
      via: "join('logs')"
      pattern: "get_hermes_home\\(\\)\\.join\\(\"logs\"\\)"
    - from: "crates/iron_hermes_ui/src/server/logging.rs"
      to: "tracing_appender::rolling::daily"
      via: "two daily appenders (web.log + web-access.log)"
      pattern: "rolling::daily\\(&log_dir, \"web(-access)?\\.log\"\\)"
---

<objective>
Create a new server-only module `crates/iron_hermes_ui/src/server/logging.rs` that defines `install_web_logger_subscriber()` — the helper that composes the three-layer tracing subscriber (stdout console + `web.log` file + `web-access.log` file) and returns the two `WorkerGuard`s for the non-blocking file appenders. Wire the new module into `crates/iron_hermes_ui/src/server/mod.rs` behind the existing `#[cfg(feature = "server")]` guard pattern used for `state`.

Purpose: Encapsulate every locked logging decision (D-02..D-05, D-15..D-18) in a single helper so `main.rs` (Plan 03) only has to call it. Pattern parity with `crates/ironhermes-cli/src/tui_rata/event_loop.rs:85-117` (`install_tui_logger_subscriber`).

Output: One new file and one one-line module-declaration edit; `cargo build -p iron_hermes_ui --features server` succeeds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.17-iron-hermes-ui-web-logging-in-ironhermes-home-logs/36.17-CONTEXT.md
@.planning/phases/36.17-iron-hermes-ui-web-logging-in-ironhermes-home-logs/36.17-RESEARCH.md
@.planning/phases/36.17-iron-hermes-ui-web-logging-in-ironhermes-home-logs/36.17-PATTERNS.md
@crates/ironhermes-cli/src/tui_rata/event_loop.rs
@crates/iron_hermes_ui/src/server/mod.rs

<interfaces>
<!-- Key contracts the executor needs. Match these signatures exactly. -->

Public signature to create (in `src/server/logging.rs`):

```
pub fn install_web_logger_subscriber() -> (
    Option<tracing_appender::non_blocking::WorkerGuard>,
    Option<tracing_appender::non_blocking::WorkerGuard>,
)
```

The complete recommended implementation is supplied verbatim in RESEARCH.md
section "## Code Examples" → "### The complete install function (recommended shape)".
The executor should use that body as the implementation — it already
encodes all of D-02..D-05, D-15..D-18 correctly.

Existing helper from `ironhermes-core` (already a dep of `iron_hermes_ui`):

```
ironhermes_core::constants::get_hermes_home() -> PathBuf
```

Existing module structure (`crates/iron_hermes_ui/src/server/mod.rs`):

```
pub mod api;
#[cfg(feature = "server")]
pub mod state;
pub mod ws;
```

The new `pub mod logging;` line MUST mirror the `state` pattern: gated by
`#[cfg(feature = "server")]` because `tracing-appender` and `tower-http`
are non-wasm32 deps and the helper uses them unconditionally.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write install_web_logger_subscriber helper in src/server/logging.rs</name>
  <files>crates/iron_hermes_ui/src/server/logging.rs</files>
  <read_first>
    - crates/ironhermes-cli/src/tui_rata/event_loop.rs (lines 51–117 — the canonical install pattern, especially `install_tui_logger_subscriber` at lines 85–117)
    - .planning/phases/36.17-iron-hermes-ui-web-logging-in-ironhermes-home-logs/36.17-RESEARCH.md (section "## Code Examples" → "### The complete install function (recommended shape)" supplies the exact body to use)
    - .planning/phases/36.17-iron-hermes-ui-web-logging-in-ironhermes-home-logs/36.17-PATTERNS.md (section "`crates/iron_hermes_ui/src/server/logging.rs` (NEW — utility, log fan-out)")
    - crates/ironhermes-core/src/constants.rs (around line 61 — `get_hermes_home` signature)
  </read_first>
  <action>
    Create new file `crates/iron_hermes_ui/src/server/logging.rs` defining a single public function `install_web_logger_subscriber` with the signature shown in `<interfaces>`. The function body is the exact code in RESEARCH.md "## Code Examples" → "### The complete install function (recommended shape)" — copy that body verbatim. Required structural elements (each MUST appear in the source):
    - Top-level imports: `tracing::Level`, `tracing_appender::non_blocking::WorkerGuard`, `tracing_subscriber::{fmt, prelude::*, registry, EnvFilter}`.
    - A doc comment block above the fn naming all three layers (console, web file, access file), citing D-04/D-05 default filters, and stating the `WorkerGuard`-lifetime requirement.
    - `log_dir` resolved via `ironhermes_core::constants::get_hermes_home().join("logs")` (D-15).
    - A closure `app_filter` that builds `EnvFilter::try_from_default_env().unwrap_or_else(|_| EnvFilter::new("ironhermes=info,iron_hermes_ui=info"))` (D-04 / D-06).
    - A `console_layer` built with `fmt::layer().with_writer(std::io::stdout).with_target(true).with_filter(app_filter())` — note no `.with_ansi(false)` (D-08).
    - A `match std::fs::create_dir_all(&log_dir)` (D-16) with two arms:
      - `Ok(_)` → build `web_appender = tracing_appender::rolling::daily(&log_dir, "web.log")`, wrap in `non_blocking` to get `(web_nb, web_guard)`; same for `access_appender` with filename `"web-access.log"` and `(access_nb, access_guard)`. Build `web_file_layer = fmt::layer().with_writer(web_nb).with_ansi(false).with_target(true).with_filter(app_filter())` (D-03 + D-04). Build `access_file_layer = fmt::layer().with_writer(access_nb).with_ansi(false).with_target(true).with_filter(EnvFilter::new("tower_http::trace=info"))` (D-03 + D-05). Compose `registry().with(console_layer).with(web_file_layer).with(access_file_layer).try_init()` (D-18); discard the result with `let _ =`. Return `(Some(web_guard), Some(access_guard))`.
      - `Err(_)` → install only the console layer via `registry().with(console_layer).try_init()` and return `(None, None)` (TUI fallback parity).
    Do not panic. Do not log to stderr on `create_dir_all` failure (matches TUI silent fallback). Do not add a `#[cfg(test)]` test block — RESEARCH.md Q7 recommends skipping; UAT (Plan 04) covers behavior.
  </action>
  <verify>
    <automated>grep -n "pub fn install_web_logger_subscriber" crates/iron_hermes_ui/src/server/logging.rs && grep -n "get_hermes_home().join(\"logs\")" crates/iron_hermes_ui/src/server/logging.rs && grep -n 'rolling::daily(&log_dir, "web.log")' crates/iron_hermes_ui/src/server/logging.rs && grep -n 'rolling::daily(&log_dir, "web-access.log")' crates/iron_hermes_ui/src/server/logging.rs && grep -n 'EnvFilter::new("tower_http::trace=info")' crates/iron_hermes_ui/src/server/logging.rs && grep -n '\.with_ansi(false)' crates/iron_hermes_ui/src/server/logging.rs</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/iron_hermes_ui/src/server/logging.rs` exists.
    - Source contains the literal substring `pub fn install_web_logger_subscriber` exactly once.
    - Source contains the literal substring `(Option<WorkerGuard>, Option<WorkerGuard>)` (the return type per D-17).
    - Source contains the literal substring `get_hermes_home().join("logs")` (D-15).
    - Source contains the literal substring `rolling::daily(&log_dir, "web.log")` (D-01 stream 1).
    - Source contains the literal substring `rolling::daily(&log_dir, "web-access.log")` (D-01 stream 2).
    - Source contains the literal substring `EnvFilter::new("ironhermes=info,iron_hermes_ui=info")` (D-04).
    - Source contains the literal substring `EnvFilter::new("tower_http::trace=info")` (D-05).
    - `grep -c '\.with_ansi(false)' crates/iron_hermes_ui/src/server/logging.rs` prints at least `2` (both file layers strip ANSI per D-03).
    - Source contains the literal substring `try_init()` (D-18, not bare `init()`).
    - Source contains the literal substring `create_dir_all(&log_dir)` (D-16).
    - `grep -c '^pub fn install_web_logger_subscriber' crates/iron_hermes_ui/src/server/logging.rs` prints `1`.
  </acceptance_criteria>
  <done>New file defines the helper with the exact signature, both file appenders, both per-layer filters, ANSI-strip on file layers, `try_init` install, and `(None, None)` fallback on dir-creation failure.</done>
</task>

<task type="auto">
  <name>Task 2: Declare the logging module in src/server/mod.rs and verify build</name>
  <files>crates/iron_hermes_ui/src/server/mod.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/mod.rs (full file — only 12 lines; note the `#[cfg(feature = "server")]` guard on `state`)
    - .planning/phases/36.17-iron-hermes-ui-web-logging-in-ironhermes-home-logs/36.17-PATTERNS.md (section "`crates/iron_hermes_ui/src/server/mod.rs` (MODIFIED — module declaration)")
  </read_first>
  <action>
    In `crates/iron_hermes_ui/src/server/mod.rs`, append exactly two new lines at the end of the file (after `pub mod ws;`):
    - First line: `#[cfg(feature = "server")]`
    - Second line: `pub mod logging;`
    Do not modify any existing line. Do not reorder. After the edit, run `cargo build -p iron_hermes_ui --features server` and ensure it exits 0 — this proves both Task 1's new file and Task 2's wiring compile together. Also run `cargo build -p iron_hermes_ui` (default features, no `server`) to confirm the `#[cfg(feature = "server")]` guard properly excludes the file from wasm/client builds (the wasm32 build would fail if `tracing-appender` / `tower-http` were pulled in unconditionally).
  </action>
  <verify>
    <automated>tail -2 crates/iron_hermes_ui/src/server/mod.rs | grep -E '^pub mod logging;$' && grep -B1 '^pub mod logging;$' crates/iron_hermes_ui/src/server/mod.rs | grep -E '^#\[cfg\(feature = "server"\)\]$' && cargo build -p iron_hermes_ui --features server && cargo build -p iron_hermes_ui</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^pub mod logging;$' crates/iron_hermes_ui/src/server/mod.rs` prints `1`.
    - The line immediately preceding `pub mod logging;` is exactly `#[cfg(feature = "server")]`.
    - `cargo build -p iron_hermes_ui --features server` exits 0.
    - `cargo build -p iron_hermes_ui` exits 0 (default-feature build does not break; the cfg-gate keeps `logging` out of client builds).
    - `cargo clippy -p iron_hermes_ui --features server -- -D warnings` exits 0 (no warnings from the new file under the server feature).
  </acceptance_criteria>
  <done>Module declaration added behind `#[cfg(feature = "server")]`; both server and default builds succeed; no clippy warnings introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process env → filesystem | `$IRONHERMES_HOME` is resolved from the env (via `get_hermes_home`) and joined with `"logs"` to form a write path. |
| App code → log file | Log events (potentially containing request paths or other strings) are written to disk by the non-blocking appender. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.17-02 | Tampering | `$IRONHERMES_HOME` env var → `log_dir` path | mitigate | Reuse `ironhermes_core::constants::get_hermes_home()` (already used by the TUI for the same logs directory — validation lives in that helper). No new validation layer added; path traversal mitigation is inherited from the canonical helper. |
| T-36.17-03 | Denial of Service | Unbounded `logs/` growth | accept (deferred) | CONTEXT.md `## Deferred Ideas` explicitly marks "Log retention beyond daily-rolling (gzip + N-day cleanup)" as out of scope. Daily rotation prevents single-file unboundedness; deletion policy is a separate phase. Risk documented. |
| T-36.17-04 | Information Disclosure | Sensitive strings in log lines | mitigate (scope-limited) | Per D-11 (no detailed request body/header capture), the access log only carries the standard `tower_http::trace` events (method, path, status, latency). Query-string secrets are still possible but mitigation is upstream tooling configuration, not in scope for this phase. |
| T-36.17-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced by this plan; Plan 01 covered the audit. |
</threat_model>

<verification>
- After Task 2: `cargo build -p iron_hermes_ui --features server` exits 0.
- After Task 2: `cargo build -p iron_hermes_ui` exits 0 (default features).
- After Task 2: `cargo clippy -p iron_hermes_ui --features server -- -D warnings` exits 0.
- All `grep` literal-substring checks under each task's `<acceptance_criteria>` pass.
- Behavioral verification (server actually writes to the files) is deferred to Plan 04's UAT script — this plan only proves compile-time correctness and source structure.
</verification>

<success_criteria>
- `crates/iron_hermes_ui/src/server/logging.rs` exists and contains a `pub fn install_web_logger_subscriber` returning `(Option<WorkerGuard>, Option<WorkerGuard>)` with both daily appenders, both per-layer filters, ANSI-stripped file layers, `try_init` install, and the `(None, None)` dir-failure fallback.
- `crates/iron_hermes_ui/src/server/mod.rs` declares `pub mod logging;` behind `#[cfg(feature = "server")]`.
- Both `--features server` and default-feature builds succeed; no new clippy warnings.
</success_criteria>

<output>
Create `.planning/phases/36.17-iron-hermes-ui-web-logging-in-ironhermes-home-logs/36.17-02-SUMMARY.md` when done.
</output>
