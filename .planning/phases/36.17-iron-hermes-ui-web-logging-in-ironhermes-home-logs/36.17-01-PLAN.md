---
phase: 36.17
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/iron_hermes_ui/Cargo.toml
autonomous: true
requirements:
  - D-13
  - D-14
must_haves:
  truths:
    - "Workspace declares tower-http with trace feature at version 0.6"
    - "iron_hermes_ui crate (non-wasm32 target) depends on tracing-appender via workspace"
    - "iron_hermes_ui crate (non-wasm32 target) depends on tower-http with trace feature via workspace"
    - "iron_hermes_ui builds successfully with --features server after the manifest edits"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace-level tower-http dependency"
      contains: "tower-http = { version = \"0.6\", features = [\"trace\"] }"
    - path: "crates/iron_hermes_ui/Cargo.toml"
      provides: "Crate-level tracing-appender + tower-http declarations in non-wasm32 block"
      contains: "tracing-appender = { workspace = true }"
  key_links:
    - from: "crates/iron_hermes_ui/Cargo.toml"
      to: "Cargo.toml (workspace root)"
      via: "workspace = true reference"
      pattern: "tower-http = \\{ workspace = true"
---

<objective>
Add the `tower-http` workspace dependency (with `trace` feature) and wire both `tracing-appender` and `tower-http` into `crates/iron_hermes_ui/Cargo.toml`'s non-wasm32 dependency block.

Purpose: Wave 2 plans (logging helper, main.rs wiring) cannot reference `tower_http::trace::TraceLayer` or `tracing_appender::rolling::daily` from `iron_hermes_ui` without these manifest declarations. This is the foundational gate for the whole phase.

Output: Two modified `Cargo.toml` files; `cargo build -p iron_hermes_ui --features server` succeeds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.17-iron-hermes-ui-web-logging-in-ironhermes-home-logs/36.17-CONTEXT.md
@.planning/phases/36.17-iron-hermes-ui-web-logging-in-ironhermes-home-logs/36.17-RESEARCH.md
@.planning/phases/36.17-iron-hermes-ui-web-logging-in-ironhermes-home-logs/36.17-PATTERNS.md

<interfaces>
<!-- Existing relevant content of files being modified. -->
<!-- Executor should match this style exactly. -->

Workspace `Cargo.toml` lines 70–73 (current, post-eedb49e1):
```
# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
tracing-appender = "0.2"
```

`crates/iron_hermes_ui/Cargo.toml` non-wasm32 block currently ends with:
```
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
ironhermes-core = { path = "../ironhermes-core" }
...
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
dotenvy = { workspace = true }
```

Reference style from `crates/ironhermes-cli/Cargo.toml` line 51:
```
tracing-appender = { workspace = true }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare tower-http in workspace Cargo.toml</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (current workspace file — read lines 65–90 to see the `# Logging` group and surrounding `[workspace.dependencies]` layout)
    - .planning/phases/36.17-iron-hermes-ui-web-logging-in-ironhermes-home-logs/36.17-PATTERNS.md (section "`Cargo.toml` (workspace root, MODIFIED — workspace dependency block)")
    - .planning/phases/36.17-iron-hermes-ui-web-logging-in-ironhermes-home-logs/36.17-RESEARCH.md (Q1 — version evidence from Cargo.lock)
  </read_first>
  <action>
    In `Cargo.toml` (workspace root), within the `[workspace.dependencies]` table, append a new line immediately after the existing `tracing-appender = "0.2"` line so the four-line `# Logging` group reads `tracing`, `tracing-subscriber`, `tracing-appender`, then the new entry. The new entry is the exact toml line `tower-http = { version = "0.6", features = ["trace"] }`. Do not reorder existing lines; do not change versions of `tracing`, `tracing-subscriber`, or `tracing-appender`. Per D-14 and Q1: pin to `0.6` because `tower-http 0.6.8` is already resolved in `Cargo.lock` as a transitive of `dioxus-cli-config`, `dioxus-devtools`, `reqwest`, and `ironhermes-cli`, and is compatible with `axum = "=0.8.4"`.
  </action>
  <verify>
    <automated>grep -E '^tower-http = \{ version = "0\.6", features = \["trace"\] \}$' Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '^tower-http = \{ version = "0\.6", features = \["trace"\] \}$' Cargo.toml` exits 0 and prints exactly one matching line.
    - `grep -c '^tracing-appender = "0.2"$' Cargo.toml` prints `1` (existing line preserved).
    - `cargo metadata --format-version 1 --no-deps --manifest-path Cargo.toml > /dev/null 2>&1` exits 0 (manifest is still valid toml).
  </acceptance_criteria>
  <done>Workspace toml has the new `tower-http` line in the `# Logging` group; manifest still parses.</done>
</task>

<task type="auto">
  <name>Task 2: Add tracing-appender + tower-http to iron_hermes_ui non-wasm32 deps and verify build</name>
  <files>crates/iron_hermes_ui/Cargo.toml</files>
  <read_first>
    - crates/iron_hermes_ui/Cargo.toml (full file — the non-wasm32 block currently spans roughly lines 45–57)
    - crates/ironhermes-cli/Cargo.toml (look for the existing `tracing-appender = { workspace = true }` line for exact style — referenced in PATTERNS.md)
    - .planning/phases/36.17-iron-hermes-ui-web-logging-in-ironhermes-home-logs/36.17-PATTERNS.md (section "`crates/iron_hermes_ui/Cargo.toml` (MODIFIED — crate dependency block)")
  </read_first>
  <action>
    In `crates/iron_hermes_ui/Cargo.toml`, locate the `[target.'cfg(not(target_arch = "wasm32"))'.dependencies]` block. Immediately after the existing `tracing-subscriber = { workspace = true }` line, insert two new lines in this order: first `tracing-appender = { workspace = true }`, then `tower-http = { workspace = true, features = ["trace"] }`. Do not touch the wasm32 block, the `[features]` table, or any other dependency. The explicit `features = ["trace"]` repetition at the call site mirrors the existing style of `dioxus = { version = "=0.7.1", features = ["fullstack"] }` in the same crate. After editing, run `cargo build -p iron_hermes_ui --features server` and ensure it exits 0 (this also exercises the workspace edit from Task 1).
  </action>
  <verify>
    <automated>grep -E '^tracing-appender = \{ workspace = true \}$' crates/iron_hermes_ui/Cargo.toml && grep -E '^tower-http = \{ workspace = true, features = \["trace"\] \}$' crates/iron_hermes_ui/Cargo.toml && cargo build -p iron_hermes_ui --features server</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^tracing-appender = { workspace = true }$' crates/iron_hermes_ui/Cargo.toml` prints `1`.
    - `grep -c '^tower-http = { workspace = true, features = \["trace"\] }$' crates/iron_hermes_ui/Cargo.toml` prints `1`.
    - Both new lines sit inside the `[target.'cfg(not(target_arch = "wasm32"))'.dependencies]` block (verified by checking that no `[` table-header line appears between `tracing-subscriber = { workspace = true }` and the new entries).
    - `cargo build -p iron_hermes_ui --features server` exits 0.
    - `cargo metadata --format-version 1 --no-deps --manifest-path crates/iron_hermes_ui/Cargo.toml > /dev/null 2>&1` exits 0.
  </acceptance_criteria>
  <done>Both new dependency lines present in the non-wasm32 block; crate builds with `--features server`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io → workspace | External crate metadata enters the build graph via `Cargo.toml`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.17-01 | Tampering | tower-http crate from crates.io | mitigate | Pin to `0.6` line; rely on `Cargo.lock` hash check; package legitimacy audit in RESEARCH.md `## Package Legitimacy Audit` records tower-rs as the canonical publisher (50M downloads/month, github.com/tower-rs/tower-http). |
| T-36.17-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH.md `## Package Legitimacy Audit` covers `tower-http` (Approved, already in `Cargo.lock` at 0.6.8 as transitive) and `tracing-appender` (Approved, already in workspace per eedb49e1). No `[ASSUMED]` / `[SUS]` packages introduced — both are `[VERIFIED]`; no blocking-human checkpoint required. |
</threat_model>

<verification>
- `cargo build -p iron_hermes_ui --features server` exits 0 after Task 2.
- `cargo metadata --no-deps` exits 0 (workspace toml still parses).
- No changes to `Cargo.lock` semantics expected — `tower-http 0.6.8` is already resolved.
</verification>

<success_criteria>
- `Cargo.toml` workspace declares `tower-http = { version = "0.6", features = ["trace"] }` in the `# Logging` group.
- `crates/iron_hermes_ui/Cargo.toml` declares both `tracing-appender = { workspace = true }` and `tower-http = { workspace = true, features = ["trace"] }` in the non-wasm32 dependency block.
- `cargo build -p iron_hermes_ui --features server` succeeds.
</success_criteria>

<output>
Create `.planning/phases/36.17-iron-hermes-ui-web-logging-in-ironhermes-home-logs/36.17-01-SUMMARY.md` when done.
</output>
