---
phase: 26.2.1
plan: 14
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - crates/iron_hermes_ui/assets/site.css
  - crates/iron_hermes_ui/src/components/hermes_app/hud_chrome.rs
  - crates/iron_hermes_ui/src/server/api.rs
autonomous: true
requirements:
  - GAP-26.2.1-07-R3-scanlines-toggle-does-not-engage
  - GAP-26.2.1-09-R3-list-sessions-non-loadable-formats

dependency_graph:
  - Task 1 (GAP-07-R3 live DOM diagnostic) — runs first, BLOCKS Task 2 until Diagnostic Result block is filled
  - Task 2 (GAP-07-R3 surgical fix) — branches on Task 1 diagnostic outcome (3 mutually exclusive decision branches)
  - Task 3 (GAP-09-R3 message_count post-filter in api.rs) — independent of Tasks 1/2
  - Task 4 (regression tests) — static-grep for the fixes in Tasks 2 and 3 (depends on those landing)
  - Task 5 (re-verify gate) — depends on Tasks 1-4 all landing

tech_stack:
  - Rust 2021 + Dioxus 0.7.1 (signals, EventHandler, RSX)
  - CSS3 (selector specificity, animation/keyframe override, body-class cascade)
  - cargo check / cargo test gates per Phase 26.2.1 D-02 build contract
  - Static-grep regression tests via include_str! (Plan 11 / Plan 13 pattern)
  - SQLite-backed Session catalog (ironhermes-state) — message_count column on `sessions` table

key_files:
  - crates/iron_hermes_ui/assets/site.css                                       # GAP-07-R3 likely fix-site (CSS specificity)
  - crates/iron_hermes_ui/src/components/hermes_app/hud_chrome.rs               # GAP-07-R3 possible fix-site (DOM class adjustment)
  - crates/iron_hermes_ui/src/server/api.rs                                     # GAP-09-R3 fix-site (post-filter by message_count > 0)
  - crates/iron_hermes_ui/src/components/hermes_app/theme_effects.rs            # GAP-07-R3 reference (effect path is exonerated; do not modify)
  - crates/ironhermes-state/src/lib.rs                                          # GAP-09-R3 reference (Session.message_count column — read-only, NOT modified)

decisions:
  - id: D-26.2.1-14-A
    title: "D-02 narrow gate — INHERITED from D-26.2.1-13-A"
    decision: |
      api.rs is the only file in `crates/iron_hermes_ui/src/server/` that may
      be modified in this gap-closure cycle. `ws.rs`, `state.rs`, and
      `protocol.rs` remain byte-for-byte unchanged. The `crates/ironhermes-state`
      crate is NOT touched in this plan — option (i) post-filter in api.rs
      was selected over option (ii) state-crate refactor specifically to
      avoid expanding scope.
    rationale: |
      Plan 13 established the api.rs surgical-lift exemption under
      D-26.2.1-13-A. Plan 14 inherits that scope verbatim. The state crate
      (`crates/ironhermes-state/src/lib.rs`) is architecturally a separate
      concern from the iron_hermes_ui server boundary that D-02 governs,
      but the smaller-blast-radius choice is to filter in api.rs after
      the `state_store.list_sessions(...)` call returns. This keeps the
      diff to ONE file and ONE function (api.rs::list_sessions).

  - id: D-26.2.1-14-B
    title: "GAP-09-R3 — option (i) post-filter by message_count > 0"
    decision: |
      Filter the `Vec<Session>` returned by `state_store.list_sessions(None, 100)`
      to retain ONLY sessions where `session.message_count > 0`. The filter
      runs in the `.iter().filter(...).map(...).collect()` pipeline currently
      at `api.rs:74-82` (the `out = sessions.into_iter().map(...).collect();`
      block). The condition becomes `.filter(|s| s.message_count > 0)` inserted
      before the existing `.map(...)`.

      EXPLICITLY REJECTED:
      - Option (ii) adding `list_loadable_sessions(limit)` to the state crate —
        rejected because it expands the diff into a crate that D-02 was
        designed to insulate (even though D-02 technically only covers the
        ui server boundary). One-file scope is the priority.
      - Option (iii) JOIN-based existence check `WHERE EXISTS (SELECT 1 FROM
        messages WHERE session_id = sessions.id)` — rejected because it
        requires changes to the state crate's SQL query in
        `list_sessions_filtered` at `crates/ironhermes-state/src/lib.rs:538`,
        which is out of scope per D-26.2.1-14-A.
    rationale: |
      The `Session` struct returned by `list_sessions` already carries the
      `message_count: i64` column directly from SQLite (see
      `crates/ironhermes-state/src/lib.rs:121-132` and the SELECT statement
      at `lib.rs:546-559`). Filtering `message_count > 0` in api.rs is the
      cheapest possible diff (one `.filter(|s| s.message_count > 0)` call
      inserted into an existing iterator chain). Sessions whose on-disk
      directory contains only `trajectories.jsonl` (foreign agent format,
      no SQLite messages row) will have `message_count == 0` and will be
      excluded from the SESSIONS wedge listing.

      The minor risk is that this could exclude a legitimately-empty
      iron_hermes session (e.g., a session created via `start_session()`
      where the user never sent a message). For the SESSIONS wedge use case,
      that is acceptable behavior — an empty session row is useless to
      click anyway. If a future phase needs to surface session shells,
      that's a separate UI affordance, not this gap closure.

  - id: D-26.2.1-14-C
    title: "GAP-07-R3 — diagnostic-first, then branch (no blind fix)"
    decision: |
      Plan 13's `!important` addition did not engage. Plan 14 will NOT
      apply another speculative CSS edit. Task 1 is a LIVE DOM diagnostic
      run by the executor in DevTools that captures three data points
      before any code change:

        1. `document.body.classList.contains('no-scanlines')` while toggle is OFF
           (expected true)
        2. `getComputedStyle(document.querySelector('.scanlines')).display`
           while toggle is OFF (expected `none`)
        3. If display != none: identify the matching CSS rules via the
           Computed panel or `window.getMatchedCSSRules` (Chrome-only) and
           record the winning rule (selector + declaration).

      The Diagnostic Result block in Task 1 is COMMITTED to the plan body
      as a checkpoint before Task 2 runs. Task 2 then branches on the
      Diagnostic Result:

        - Branch (a) body class missing → fix `theme_effects.rs:80` toggle
          (re-verify signal subscription for `prefs.scanlines`).
        - Branch (b) body class present, `.scanlines` rendered with
          different/additional classes OR outside `<body>` → fix in
          `hud_chrome.rs:20` (class-name correction) OR adjust CSS selector
          in `site.css:291`.
        - Branch (c) body class present, `.scanlines` matched, but a
          more-specific or `!important`-bearing rule wins → bump our
          rule's specificity (e.g., `html body.no-scanlines .scanlines`)
          OR delete the offending rule. Note that `.scanlines` at
          `site.css:85-96` declares `position: fixed; inset: 0;
          z-index: 9000` — `display: none !important` SHOULD win over
          any `display` declaration there (specificity 0,2,1 vs 0,1,0),
          so the most likely culprit is an animation/transition that
          re-renders `display` or an inline-style write somewhere.
    rationale: |
      Plan 13's speculative `!important` did not work. Continuing to
      guess wastes user cycles. A live DOM diagnostic in DevTools takes
      ~2 minutes and definitively identifies the cause. The branch tree
      makes the fix mechanical once the cause is known.

metrics:
  - cargo_check_host: must exit 0
  - cargo_check_legacy_shell: must exit 0
  - cargo_check_wasm32: must exit 0
  - cargo_test_bin: must exit 0 (>= 37 tests pass — Plan 13 baseline; this plan adds 2 new static-grep regression tests so expect >= 39)
  - cargo_test_wave0_smoke: must exit 0 (1 test passes)
  - d_02_narrow_gate: git diff is empty for crates/iron_hermes_ui/src/server/state.rs, ws.rs, protocol.rs (api.rs EXEMPTED per D-26.2.1-13-A inherited by D-26.2.1-14-A)
  - d_02_state_crate_untouched: git diff is empty for crates/ironhermes-state/src/lib.rs

must_haves:
  truths:
    - "GAP-07-R3: Toggling SCANLINES off in the Tweaks panel hides the .scanlines overlay; toggling on restores it. Live DOM diagnostic recorded in the SUMMARY explains WHICH cause was identified and WHICH branch's fix was applied."
    - "GAP-07-R3: Regression-clean — BREADCRUMB / FOOTER / RAIL / DENSITY toggles still engage exactly as they did after Plan 13"
    - "GAP-09-R3: SESSIONS wedge lists ONLY sessions with message_count > 0 (excludes foreign-format directories with only trajectories.jsonl)"
    - "GAP-09-R3: Clicking a row from the filtered list loads the chat thread successfully (chat-mini header SID badge updates AND the bubble list populates from messages or starts empty for fresh-but-message-bearing sessions)"
    - "D-02 narrow gate (inherited from D-26.2.1-13-A): state.rs, ws.rs, protocol.rs byte-for-byte unchanged"
    - "state crate untouched: crates/ironhermes-state/src/lib.rs git diff is empty"
    - "All five cargo gates exit 0 (host check, legacy-shell check, wasm32 check, bin tests, wave0_smoke)"
  artifacts:
    - path: "crates/iron_hermes_ui/assets/site.css"
      provides: "GAP-07-R3 likely fix-site (specificity bump OR rule delete based on diagnostic)"
      contains_or: "no-scanlines|!important|html body"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/hud_chrome.rs"
      provides: "GAP-07-R3 possible fix-site (DOM class correction) — modified ONLY IF Branch (b) of Task 2 applies"
      contains: "class: \"scanlines\""
    - path: "crates/iron_hermes_ui/src/server/api.rs"
      provides: "GAP-09-R3 fix-site — post-filter by message_count > 0 in list_sessions"
      contains: ".filter(|s| s.message_count > 0)"
      contains_2: "GAP-26.2.1-09-R3"
  key_links:
    - from: "Tweaks panel SCANLINES toggle"
      to: ".scanlines overlay div (hud_chrome.rs:20)"
      via: "body.no-scanlines class toggled by theme_effects.rs:80, cascading via site.css selector — DIAGNOSTIC will identify where this chain breaks"
      pattern: "body\\.no-scanlines"
    - from: "list_sessions server fn (api.rs:54-83)"
      to: "Vec<SessionInfo> emitted to the SESSIONS wedge UI"
      via: "iterator chain with .filter(|s| s.message_count > 0) inserted between .into_iter() and .map(...)"
      pattern: "\\.filter\\(\\|s\\| s\\.message_count > 0\\)"
    - from: "SESSIONS wedge row click (screens/sessions.rs)"
      to: "Chat screen rehydrated for the clicked session (only loadable sessions reach this code path)"
      via: "list_sessions API now returns ONLY rows with message_count > 0"
      pattern: "message_count > 0"

reverification:
  closes_gaps:
    - "GAP-26.2.1-07-R3 (UAT Test 6): SCANLINES toggle still didn't engage after Plan 13's !important — fixed by diagnostic-driven surgical edit (specific branch determined by Task 1 Diagnostic Result block)"
    - "GAP-26.2.1-09-R3 (UAT Test 9): list_sessions surfaced non-loadable session formats — fixed by message_count > 0 post-filter in api.rs"
  rerun:
    - "UAT-26.2.1.md §6 (Themes & Tweaks Panel) — SCANLINES toggle subsection + regression on BREADCRUMB/FOOTER/RAIL/DENSITY"
    - "UAT-26.2.1.md §9 (Sessions — Switch & Delete) — clicking a row from filtered list loads chat thread"
---

<objective>
Close the two round-3 UAT gaps surfaced after Plan 13 landed for phase 26.2.1:

  1. **GAP-26.2.1-07-R3** (minor) — SCANLINES toggle STILL does not engage. Plan 13's `!important` was insufficient. Plan 14 runs a live DOM diagnostic FIRST to identify the actual cause, then applies a branch-specific surgical fix.
  2. **GAP-26.2.1-09-R3** (major) — Plan 13's D-02 surgical lift (api.rs `Some(Web)` → `None`) broadened the SESSIONS wedge listing to ~74 entries, but some rows are foreign-format directories (only `trajectories.jsonl`, no SQLite messages). Clicking those rows does not load a discussion. **User-chosen scope option A** (2026-05-14): filter `list_sessions` to **loadable** sessions only. Plan 14 implements option (i): post-filter by `message_count > 0` in api.rs.

Purpose: Phase 26.2.1 cannot ship at 13/13 until both gaps close. GAP-07-R3 is minor (cosmetic chrome) but documented in the acceptance criteria for Test 6. GAP-09-R3 is major because the SESSIONS wedge currently surfaces unusable rows.

Output: Up to two source files modified (`assets/site.css` and/or `hud_chrome.rs` per the GAP-07 diagnostic outcome; `server/api.rs` for GAP-09). Two new static-grep regression tests added in `mod.rs` to assert the fixes hold. One re-verify gate task at the end. Atomic commits per task. D-02 narrow gate inherited from D-26.2.1-13-A: state.rs, ws.rs, protocol.rs remain byte-for-byte unchanged. State crate untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-CONTEXT.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-UAT.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-VERIFICATION.md

# Predecessor plan (inherits D-26.2.1-13-A/B/C/D)
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-13-PLAN.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-13-SUMMARY.md

# Fix-sites
@crates/iron_hermes_ui/assets/site.css
@crates/iron_hermes_ui/src/server/api.rs

# Reference (read; modify ONLY if Task 2 Branch (b) applies)
@crates/iron_hermes_ui/src/components/hermes_app/hud_chrome.rs

# Reference (read but DO NOT modify — exonerated)
@crates/iron_hermes_ui/src/components/hermes_app/theme_effects.rs
@crates/iron_hermes_ui/src/components/hermes_app/tweaks_panel.rs
@crates/iron_hermes_ui/src/components/hermes_app/mod.rs

# State crate reference (read for Session struct shape; DO NOT modify)
@crates/ironhermes-state/src/lib.rs

<interfaces>
The current `.scanlines` element emission in `hud_chrome.rs:20`:

  div { class: "scanlines", "aria-hidden": "true" }

The current site.css scanlines rules:

  /* site.css:85-96 — base definition */
  .scanlines {
    position: fixed; inset: 0;
    background: repeating-linear-gradient(
      0deg,
      transparent 0px,
      transparent 2px,
      rgba(0, 0, 0, 0.10) 2px,
      rgba(0, 0, 0, 0.10) 3px
    );
    pointer-events: none;
    z-index: 9000;
    ...
  }

  /* site.css:288-291 — Plan 13's fix that didn't engage */
  /* Hide HUD scanlines slightly so screen content stays legible */
  .scanlines { opacity: 0.35; }
  .hud-grid { opacity: 0.35; }
  body.no-scanlines .scanlines { display: none !important; }

Specificity calculus:
  - `.scanlines { ... }` (lines 85 and 289) — (0,1,0)
  - `body.no-scanlines .scanlines { display: none !important; }` (line 291) — (0,2,1) + !important
  - The `!important` should defeat ALL non-!important rules of equal/lower specificity.
  - Two paths remain to explain the failure:
    (1) The body class is not being added (DevTools `document.body.classList` will show)
    (2) Some other inline style or higher-specificity rule writes `display: block`
        with `!important` (DevTools Computed panel will show the winning rule)

The body-class effect at `theme_effects.rs:70-95`:

  use_effect(move || {
      let p = prefs.read().clone();
      let screen = *active_screen.read();
      #[cfg(target_arch = "wasm32")]
      {
          if let Some(body) = web_sys::window()
              .and_then(|w| w.document())
              .and_then(|d| d.body())
          {
              let list = body.class_list();
              let _ = list.toggle_with_force("no-scanlines", !p.scanlines);
              let _ = list.toggle_with_force("no-breadcrumb", !p.breadcrumb);
              let _ = list.toggle_with_force("no-footer", !p.footer);
              let _ = list.toggle_with_force("has-rail", p.rail);
              let _ = list.toggle_with_force(
                  "density-dense",
                  p.density == crate::ui_prefs::Density::Dense,
              );
              let _ = list
                  .toggle_with_force("on-chat", screen == crate::state::Screen::Chat);
          }
      }
      ...
  });

Note: the SAME effect block toggles `no-breadcrumb` / `no-footer` / `has-rail` /
`density-dense`, and the UAT round-3 notes confirm those DO work. That STRONGLY
suggests the body class IS being added for `no-scanlines` too (it's the same
`prefs.read()` read followed by sequential `toggle_with_force` calls — there's
no plausible mechanism for one toggle in this block to fire while another
silently fails). The Diagnostic Result will likely show a CSS-side cause.

The current `list_sessions` body in `api.rs:53-83` (after Plan 13's surgical lift):

  #[get("/api/sessions")]
  pub async fn list_sessions() -> Result<Vec<SessionInfo>> {
      let state = crate::server::state::global_app_state();
      let sessions = state
          .state_store
          .lock()
          .unwrap()
          // GAP-26.2.1-09 / D-26.2.1-13-A (USER-APPROVED 2026-05-14):
          // Pass None instead of Some(Platform::Web) so the SESSIONS wedge sources
          // the full cross-platform on-disk session catalog (~/.ironhermes/sessions/).
          // See .planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-UAT.md
          // §"Round-2 D-02 Decision" for the user authorization quote.
          .list_sessions(
              None,
              100,
          )
          .map_err(|e| ServerFnError::new(format!("StateStore list sessions failed: {e}")))?;

      let out = sessions
          .into_iter()
          .map(|session| SessionInfo {
              id: session.id,
              title: session.title,
              created_at: format!("{}", session.started_at as i64),
              message_count: session.message_count.max(0) as u32,
          })
          .collect();
      Ok(out)
  }

The `Session` struct from `crates/ironhermes-state/src/lib.rs:121-132`:

  pub struct Session {
      pub id: String,
      pub source: String,
      pub workspace_root: Option<String>,
      pub agent_logical_path: Option<String>,
      pub started_at: f64,
      pub ended_at: Option<f64>,
      pub end_reason: Option<String>,
      pub message_count: i64,    // <- the column we filter on
      pub tool_call_count: i64,
      pub title: Option<String>,
  }

The SELECT clause at `crates/ironhermes-state/src/lib.rs:546-559` returns
`message_count` directly from the SQLite `sessions` table — no further
data round-trip needed.

For the GAP-09-R3 fix, insert `.filter(|s| s.message_count > 0)` between
`.into_iter()` and `.map(...)`:

  let out = sessions
      .into_iter()
      // GAP-26.2.1-09-R3 / D-26.2.1-14-B: drop non-loadable sessions
      // (foreign-format directories with only trajectories.jsonl and no
      // SQLite messages row produce message_count == 0).
      .filter(|s| s.message_count > 0)
      .map(|session| SessionInfo {
          id: session.id,
          ...
      })
      .collect();
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GAP-26.2.1-07-R3 — Live DOM diagnostic (NO code change yet)</name>
  <files>.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-14-PLAN.md</files>
  <action>
This task does NOT modify any source file. It runs a live DOM diagnostic in the browser, captures the result, and commits the result inline to THIS plan body so Task 2 can branch on it.

Step A — Start the dev server (kill any running instance first):

  pkill -f "dx serve" 2>/dev/null || true
  dx serve --package iron_hermes_ui

Wait for "Server listening on http://localhost:8080" or equivalent.

Step B — Open http://localhost:8080/ in a fresh browser tab with DevTools open (Chrome strongly preferred — Firefox lacks `getMatchedCSSRules`). Open the Tweaks panel (gear FAB at bottom-right). Click the SCANLINES toggle to OFF. Confirm the bug visually reproduces (scanlines overlay still painting).

Step C — In the DevTools Console, run these three diagnostic commands IN ORDER and record the EXACT output:

  // Diagnostic D1: body class state
  document.body.classList.contains('no-scanlines')
  // Expected if effect fires correctly: true

  // Diagnostic D2: computed display on .scanlines
  getComputedStyle(document.querySelector('.scanlines')).display
  // Expected if CSS rule wins: "none"
  // If "block" or anything else: a more-specific rule or animation wins

  // Diagnostic D3: matched CSS rules (Chrome only)
  // If D2 returned a non-"none" value, capture the winning rule(s):
  // Either inspect the .scanlines element in the Elements panel,
  // open the Computed sub-panel, expand the `display` property,
  // and screenshot/copy the cascade list.
  // Alternatively in Chrome only:
  Array.from(document.styleSheets).flatMap(ss => {
      try { return Array.from(ss.cssRules || []) } catch (e) { return [] }
  }).filter(r => r.selectorText && (r.selectorText.includes('scanlines') || r.selectorText.includes('no-scanlines'))).map(r => ({
      selector: r.selectorText,
      display: r.style.display,
      important: r.style.getPropertyPriority('display'),
      cssText: r.cssText
  }))

Also capture the .scanlines element's INLINE style attribute (if any):

  document.querySelector('.scanlines').getAttribute('style')

Step D — Capture the answers in a "Diagnostic Result" block by EDITING this PLAN file. Insert a fenced markdown block immediately AFTER this Task 1 closing tag and BEFORE Task 2's opening tag, in this exact shape:

  ## Diagnostic Result (Task 1 output — captured 2026-05-14)

  ```
  D1 (document.body.classList.contains('no-scanlines')) = <PASTE BOOLEAN>
  D2 (getComputedStyle(.scanlines).display)             = "<PASTE STRING>"
  D3 (matched scanlines rules):
    <PASTE JSON ARRAY OR SCREENSHOT-EQUIVALENT TEXT>
  Inline style on .scanlines:
    <PASTE STRING OR "null">
  ```

  **Conclusion:** Branch (a/b/c) per D-26.2.1-14-C decision tree:
    - Branch (a) if D1 == false → fix theme_effects.rs (effect not firing)
    - Branch (b) if D1 == true AND D2 != "none" AND D3 shows the rule
                  doesn't match `.scanlines` (e.g. different class name or
                  no rule applies) → fix the DOM class in hud_chrome.rs OR
                  the CSS selector in site.css
    - Branch (c) if D1 == true AND D2 != "none" AND D3 shows a competing
                  rule winning → bump specificity OR delete the offending
                  rule in site.css

  Selected branch for Task 2: <a|b|c>

Step E — Stop the dev server. This Task 1 commits ONLY the plan-file edit:

  git add .planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-14-PLAN.md
  git commit -m "test(26.2.1-14): GAP-07-R3 live DOM diagnostic — branch <a|b|c> selected"

DO NOT modify any source file in this task. The next task implements the branch fix.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && grep -q "## Diagnostic Result (Task 1 output" .planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-14-PLAN.md && grep -qE "Selected branch for Task 2: [abc]" .planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-14-PLAN.md && test -z "$(git diff -- crates/iron_hermes_ui/)" || (echo "ERROR: Task 1 must edit ONLY the plan file; expected zero diff under crates/iron_hermes_ui/" && exit 1)</automated>
  </verify>
  <rollback>
If the dev server fails to start (e.g. port 8080 already in use), kill the conflicting process (`lsof -ti:8080 | xargs kill`) and retry. If `dx serve` itself is broken, fall back to capturing the diagnostic from a previously-built dev session — the result is what matters, not which build artifact served it.
  </rollback>
  <done>
The Diagnostic Result block exists in this PLAN file with D1/D2/D3 captured and a specific branch (a, b, or c) selected. No source files under `crates/iron_hermes_ui/` were modified by this task. Atomic commit: `test(26.2.1-14): GAP-07-R3 live DOM diagnostic — branch <a|b|c> selected`.
  </done>
</task>

<task type="auto">
  <name>Task 2: GAP-26.2.1-07-R3 — Surgical fix per Task 1 Diagnostic Result branch</name>
  <files>crates/iron_hermes_ui/assets/site.css, crates/iron_hermes_ui/src/components/hermes_app/hud_chrome.rs</files>
  <action>
Read the "Diagnostic Result" block written by Task 1 in this plan file. Apply ONLY the fix for the selected branch.

---

**Branch (a) — D1 == false (body class not applied)**

If Task 1 concluded the `no-scanlines` body class is not landing on `<body>`, the bug is in the effect path. This contradicts the round-3 UAT note that other toggles (BREADCRUMB / FOOTER / RAIL / DENSITY) work via the SAME effect block, so before applying any fix, double-check by toggling one of those other classes and confirming D1 for that class. If branch (a) really applies:

  1. Open `crates/iron_hermes_ui/src/components/hermes_app/theme_effects.rs` (lines 70-95).
  2. Inspect the `prefs.read().clone()` reader and the `toggle_with_force("no-scanlines", !p.scanlines)` write. Verify `p.scanlines` matches the actual signal name on `UiPrefs`.
  3. Compare with the lines for `no-breadcrumb`/`no-footer`/etc. — find any structural difference (typo in the field name, wrong negation, missing subscribe).
  4. Apply the minimal correction (likely a one-line typo fix).

Files modified under Branch (a): `crates/iron_hermes_ui/src/components/hermes_app/theme_effects.rs` — `site.css` and `hud_chrome.rs` are NOT modified.

Commit: `fix(26.2.1-14): GAP-07-R3 branch (a) — re-wire no-scanlines body class toggle`

---

**Branch (b) — D1 == true AND D2 != "none" AND D3 shows the selector doesn't match**

If the body class IS applied but the CSS selector `body.no-scanlines .scanlines` does not match the actual scanlines element (e.g., the rendered element has additional classes, or is nested in a way that breaks descendant selection):

  1. Capture the actual class attribute of the visible scanlines element via DevTools Elements panel.
  2. Decide between:
     - (b1) Adjust the CSS selector in `site.css:291` to match the actual class chain (e.g., `body.no-scanlines .scanlines.scanlines--active` or `body.no-scanlines .ih-scanlines`).
     - (b2) Adjust the DOM class in `hud_chrome.rs:20` to match the existing CSS selector (revert to canonical `class: "scanlines"`).
  3. Pick whichever is the smaller diff. Document the choice in the SUMMARY.

Files modified under Branch (b): `crates/iron_hermes_ui/assets/site.css` OR `crates/iron_hermes_ui/src/components/hermes_app/hud_chrome.rs` — NOT both unless absolutely required.

Commit: `fix(26.2.1-14): GAP-07-R3 branch (b) — reconcile scanlines DOM class with CSS selector`

---

**Branch (c) — D1 == true AND D2 != "none" AND a competing rule wins**

If the body class IS applied AND a competing rule overrides our `!important display: none`, identify the winning selector from D3 and:

  1. Either DELETE the offending rule (preferred if it's a stale or accidentally-introduced rule).
  2. Or BUMP the specificity of our hide rule. The cheapest specificity bump is adding `html` prefix:

       /* site.css line 291 — was: body.no-scanlines .scanlines { display: none !important; } */
       html body.no-scanlines .scanlines { display: none !important; }

     New specificity: (0,2,2) vs the body-class form (0,2,1). Adding `html` is sufficient if the winning rule was at (0,2,1) without `!important`.

  3. If the winning rule ALSO uses `!important`, escalate to attribute-selector chaining:

       html body.no-scanlines .scanlines[aria-hidden="true"] { display: none !important; }

     Specificity: (0,3,2) + !important — beats almost anything sensible.

  4. If the winning rule is an animation-driven `display: block`, consider replacing `display: none` with the combination:

       html body.no-scanlines .scanlines {
           display: none !important;
           visibility: hidden !important;
           opacity: 0 !important;
       }

     The animation paints background-position, but if the element itself is `visibility: hidden; opacity: 0`, it's invisible regardless of `display` state.

Files modified under Branch (c): `crates/iron_hermes_ui/assets/site.css` only (1-3 lines edited).

Commit: `fix(26.2.1-14): GAP-07-R3 branch (c) — <specificity-bump|delete-rule|visibility-fallback>`

---

**Universal verification for any branch:**

After applying the fix:

  cargo check -p iron_hermes_ui                                  # must exit 0
  cargo check -p iron_hermes_ui --features legacy-shell          # must exit 0

Then manually re-test in the browser:
  - Toggle SCANLINES OFF → overlay disappears
  - Toggle SCANLINES ON → overlay returns
  - Toggle BREADCRUMB / FOOTER / RAIL / DENSITY each → each engages (regression check)

DO NOT modify any file outside the branch's expected set. DO NOT touch `theme_effects.rs` under branches (b)/(c). DO NOT touch `hud_chrome.rs` under branches (a)/(c). DO NOT modify any server file (D-02 narrow gate).
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && BRANCH=$(grep -oE "Selected branch for Task 2: [abc]" .planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-14-PLAN.md | tail -1 | awk '{print $NF}') && echo "Branch: $BRANCH" && case "$BRANCH" in a) git diff --name-only HEAD~1 HEAD -- crates/iron_hermes_ui/src/components/hermes_app/theme_effects.rs | grep -q theme_effects.rs ;; b) (git diff --name-only HEAD~1 HEAD -- crates/iron_hermes_ui/assets/site.css crates/iron_hermes_ui/src/components/hermes_app/hud_chrome.rs | grep -qE "site\\.css|hud_chrome\\.rs") ;; c) git diff --name-only HEAD~1 HEAD -- crates/iron_hermes_ui/assets/site.css | grep -q site.css ;; *) echo "ERROR: branch not in {a,b,c}: '$BRANCH'" && exit 1 ;; esac && test -z "$(git diff -- crates/iron_hermes_ui/src/server/state.rs crates/iron_hermes_ui/src/server/ws.rs crates/iron_hermes_ui/src/protocol.rs)" && cargo check -p iron_hermes_ui 2>&1 | tail -3 | grep -q "Finished"</automated>
  </verify>
  <rollback>
If the chosen branch fix produces a regression in another toggle (e.g. branch (c) specificity bump cascades into hud-grid disappearing), revert with `git checkout -- <files>` and re-read the Diagnostic Result. If the diagnostic itself was inconclusive, re-run Task 1 with additional probes (e.g. `console.log(document.body.className)` over a 5-second toggle cycle to confirm the class lands).
  </rollback>
  <done>
The branch fix is applied to the expected file set and to NO OTHER file. `cargo check -p iron_hermes_ui` exits 0. `cargo check -p iron_hermes_ui --features legacy-shell` exits 0. D-02 narrow gate confirmed by empty diff under `crates/iron_hermes_ui/src/server/state.rs`, `ws.rs`, `protocol.rs`. Atomic commit per the branch (one of three commit subjects above).
  </done>
</task>

<task type="auto">
  <name>Task 3: GAP-26.2.1-09-R3 — Post-filter list_sessions by message_count > 0 in api.rs</name>
  <files>crates/iron_hermes_ui/src/server/api.rs</files>
  <action>
Implement option (i) per D-26.2.1-14-B: insert a `.filter(|s| s.message_count > 0)` step into the existing iterator chain in `list_sessions` at `api.rs:54-83`.

Step A — Locate the iterator pipeline in `crates/iron_hermes_ui/src/server/api.rs` (currently at approximately lines 73-83):

  let out = sessions
      .into_iter()
      .map(|session| SessionInfo {
          id: session.id,
          title: session.title,
          created_at: format!("{}", session.started_at as i64),
          message_count: session.message_count.max(0) as u32,
      })
      .collect();

Step B — Insert the filter between `.into_iter()` and `.map(...)`, with an explanatory comment block citing the round-3 user choice:

  let out = sessions
      .into_iter()
      // GAP-26.2.1-09-R3 / D-26.2.1-14-B (USER CHOSE OPTION A, 2026-05-14):
      // Drop non-loadable sessions. The D-26.2.1-13-A surgical lift broadened
      // the listing to ~74 entries from ~/.ironhermes/sessions/, but some
      // are foreign-format directories (only trajectories.jsonl, no SQLite
      // messages row → message_count == 0) that the chat-load path cannot
      // populate. Filtering message_count > 0 yields ONLY sessions the
      // SESSIONS wedge can actually open. See 26.2.1-UAT.md round-3 notes.
      .filter(|s| s.message_count > 0)
      .map(|session| SessionInfo {
          id: session.id,
          title: session.title,
          created_at: format!("{}", session.started_at as i64),
          message_count: session.message_count.max(0) as u32,
      })
      .collect();

Step C — Verify scope: the ONLY change to api.rs in this task is the insertion of the comment block + the `.filter(|s| s.message_count > 0)` line. Inherited D-26.2.1-13-A scope (api.rs exempted from "no diff" gate) covers this.

Step D — Run the cargo gates:

  cargo check -p iron_hermes_ui                                  # must exit 0
  cargo check -p iron_hermes_ui --features legacy-shell          # must exit 0
  cargo check -p iron_hermes_ui --target wasm32-unknown-unknown  # must exit 0

Step E — Verify the state crate is NOT touched:

  git diff -- crates/ironhermes-state/src/lib.rs
  # Expected: empty

Step F — Verify other server boundary files are NOT touched:

  git diff -- crates/iron_hermes_ui/src/server/state.rs crates/iron_hermes_ui/src/server/ws.rs crates/iron_hermes_ui/src/protocol.rs
  # Expected: empty

DO NOT modify the `state_store.list_sessions(None, 100)` call argument (it remains `None` per Plan 13). DO NOT add a new state-crate method. DO NOT touch any test file in this task (Task 4 handles the regression tests).
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && grep -q ".filter(|s| s.message_count > 0)" crates/iron_hermes_ui/src/server/api.rs && grep -q "GAP-26.2.1-09-R3" crates/iron_hermes_ui/src/server/api.rs && grep -q "D-26.2.1-14-B" crates/iron_hermes_ui/src/server/api.rs && test -z "$(git diff -- crates/ironhermes-state/src/lib.rs)" && test -z "$(git diff -- crates/iron_hermes_ui/src/server/state.rs crates/iron_hermes_ui/src/server/ws.rs crates/iron_hermes_ui/src/protocol.rs)" && cargo check -p iron_hermes_ui 2>&1 | tail -3 | grep -q "Finished" && cargo check -p iron_hermes_ui --features legacy-shell 2>&1 | tail -3 | grep -q "Finished"</automated>
  </verify>
  <rollback>
If `cargo check` errors (e.g. the `s.message_count` field type mismatch or borrow conflict), revert with `git checkout -- crates/iron_hermes_ui/src/server/api.rs` and re-read the Session struct in `crates/ironhermes-state/src/lib.rs:121-132` to confirm `message_count: i64`. The filter is `|s| s.message_count > 0` — comparing `i64` against integer literal `0` should always typecheck. If the issue is mutability/move semantics, an alternative form is `.filter(|s| s.message_count > 0_i64)`.
  </rollback>
  <done>
api.rs `list_sessions` iterator chain has `.filter(|s| s.message_count > 0)` between `.into_iter()` and `.map(...)`, with an explanatory comment block citing GAP-26.2.1-09-R3 and D-26.2.1-14-B. State crate (`crates/ironhermes-state/src/lib.rs`) and other server boundary files (`state.rs`/`ws.rs`/`protocol.rs`) git diffs are empty. All three cargo check invocations exit 0. Atomic commit: `fix(26.2.1-14): GAP-09-R3 filter list_sessions to loadable (message_count > 0)`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Static-grep regression tests for GAP-07-R3 and GAP-09-R3</name>
  <files>crates/iron_hermes_ui/src/components/hermes_app/mod.rs</files>
  <action>
Add two new static-grep regression tests in the `#[cfg(test)] mod tests` block at the bottom of `crates/iron_hermes_ui/src/components/hermes_app/mod.rs` (joining the existing Plan 11 / Plan 13 test family that uses `include_str!`).

Step A — Locate the existing `#[cfg(test)] mod tests` block. Identify the helper that provides `MOD_RS` and add equivalents for site.css and api.rs:

  #[cfg(test)]
  const SITE_CSS: &str = include_str!("../../../assets/site.css");

  #[cfg(test)]
  const API_RS: &str = include_str!("../../server/api.rs");

The relative paths above are based on `mod.rs` being at `crates/iron_hermes_ui/src/components/hermes_app/mod.rs`:
  - `../../../assets/site.css` → `crates/iron_hermes_ui/assets/site.css`
  - `../../server/api.rs` → `crates/iron_hermes_ui/src/server/api.rs`

If `SITE_CSS` already exists from a prior plan, reuse it. If not, declare it once.

Step B — Add the GAP-07-R3 regression test. This test asserts that the no-scanlines hide rule is present in site.css (regardless of which branch's fix was applied, the rule MUST exist because all three branches leave the rule in place):

  #[test]
  fn scanlines_toggle_hide_rule_exists_in_site_css() {
      // GAP-26.2.1-07-R3 regression: the no-scanlines body-class hide rule
      // must remain in site.css. Plan 14 may have bumped specificity (Branch c)
      // or adjusted the selector (Branch b) — but a rule matching the
      // pattern `no-scanlines.*display.*none` MUST be present.
      let has_rule = SITE_CSS.lines().any(|line| {
          let l = line.trim_start();
          l.contains("no-scanlines") && l.contains("display") && l.contains("none")
      });
      assert!(
          has_rule,
          "GAP-26.2.1-07-R3: site.css must contain a rule matching `no-scanlines ... display ... none` (any specificity)",
      );
  }

Step C — Add the GAP-09-R3 regression test. This test asserts the message_count > 0 filter is present in api.rs:

  #[test]
  fn list_sessions_filters_by_message_count_in_api_rs() {
      // GAP-26.2.1-09-R3 regression: list_sessions must filter the Vec<Session>
      // by message_count > 0 before mapping to SessionInfo, otherwise foreign-
      // format directories (only trajectories.jsonl) leak into the SESSIONS
      // wedge and produce dead row-clicks.
      assert!(
          API_RS.contains(".filter(|s| s.message_count > 0)"),
          "GAP-26.2.1-09-R3: api.rs list_sessions must include `.filter(|s| s.message_count > 0)` per D-26.2.1-14-B",
      );
      assert!(
          API_RS.contains("GAP-26.2.1-09-R3"),
          "GAP-26.2.1-09-R3: api.rs must cite the gap ID in the filter's explanatory comment",
      );
  }

Step D — Verify all tests still pass and the new tests are present:

  cargo test -p iron_hermes_ui --bin iron_hermes_ui -- --list 2>&1 | grep -E "scanlines_toggle_hide_rule_exists_in_site_css|list_sessions_filters_by_message_count_in_api_rs"
  # Expected: both names appear in the list

  cargo test -p iron_hermes_ui --bin iron_hermes_ui
  # Expected: >= 39 tests passed (Plan 13 baseline 37 + 2 new)

Step E — Verify wave0_smoke still passes:

  cargo test -p iron_hermes_ui --test wave0_smoke
  # Expected: 1 test passed

DO NOT modify any existing test. DO NOT touch any source file outside the test module additions in mod.rs. DO NOT touch any server file (D-02 narrow gate — api.rs is INCLUDED via `include_str!` for test-only static-grep, which is read-only and does not modify api.rs).
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && grep -q "scanlines_toggle_hide_rule_exists_in_site_css" crates/iron_hermes_ui/src/components/hermes_app/mod.rs && grep -q "list_sessions_filters_by_message_count_in_api_rs" crates/iron_hermes_ui/src/components/hermes_app/mod.rs && grep -q "SITE_CSS" crates/iron_hermes_ui/src/components/hermes_app/mod.rs && grep -q "API_RS" crates/iron_hermes_ui/src/components/hermes_app/mod.rs && cargo check -p iron_hermes_ui 2>&1 | tail -3 | grep -q "Finished" && cargo test -p iron_hermes_ui --bin iron_hermes_ui 2>&1 | grep -E "test result.*passed" | head -1 && cargo test -p iron_hermes_ui --test wave0_smoke 2>&1 | grep -E "test result.*1 passed"</automated>
  </verify>
  <rollback>
If `include_str!` path errors (file not found at compile time), the relative path may differ — adjust based on the actual location of `mod.rs`. Verify with `find crates/iron_hermes_ui -name "mod.rs" -path "*/hermes_app/*"`. The relative paths in this plan assume `crates/iron_hermes_ui/src/components/hermes_app/mod.rs`; adjust the `../../../` and `../../` prefixes accordingly. If a test fails (the production code under test doesn't match the assertion), revisit Tasks 2/3 — do NOT loosen the test.
  </rollback>
  <done>
Two new tests `scanlines_toggle_hide_rule_exists_in_site_css` and `list_sessions_filters_by_message_count_in_api_rs` exist in mod.rs. `SITE_CSS` and `API_RS` const declarations are in place. `cargo test -p iron_hermes_ui --bin iron_hermes_ui` exits 0 with >= 39 tests passing. `cargo test -p iron_hermes_ui --test wave0_smoke` exits 0 with 1 test passing. Atomic commit: `test(26.2.1-14): add regression tests for GAP-07-R3 and GAP-09-R3`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Re-verify gate — re-run UAT Tests 6 and 9</name>
  <files>.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-UAT.md</files>
  <what-built>
GAP-07-R3 (scanlines toggle) closed via Task 1 diagnostic + Task 2 branch-specific surgical fix. GAP-09-R3 (list_sessions non-loadable filter) closed via Task 3 `.filter(|s| s.message_count > 0)` post-filter in api.rs. Two new static-grep regression tests added in Task 4. The remaining step is human re-verification of UAT Tests 6 and 9 (the two round-3 tests that previously failed).
  </what-built>
  <how-to-verify>
1. Confirm all five build gates exit 0 (run from repo root):

     cargo check -p iron_hermes_ui                                         # expect 0
     cargo check -p iron_hermes_ui --features legacy-shell                 # expect 0
     cargo check -p iron_hermes_ui --target wasm32-unknown-unknown         # expect 0
     cargo test -p iron_hermes_ui --bin iron_hermes_ui                     # expect 0, >= 39 tests pass
     cargo test -p iron_hermes_ui --test wave0_smoke                       # expect 0, 1 test passes

2. Confirm the D-02 narrow gate (inherited from D-26.2.1-13-A):

     git diff -- crates/iron_hermes_ui/src/server/state.rs crates/iron_hermes_ui/src/server/ws.rs crates/iron_hermes_ui/src/protocol.rs
     # Expected: empty output

   Confirm the state crate is untouched:

     git diff -- crates/ironhermes-state/src/lib.rs
     # Expected: empty output

   And the api.rs round-3 diff is exactly the filter insertion plus its comment:

     git diff -- crates/iron_hermes_ui/src/server/api.rs | head -40
     # Expected: 1 line added (.filter(|s| s.message_count > 0)),
     #           plus the GAP-26.2.1-09-R3 / D-26.2.1-14-B comment block

3. Kill any running dev server, then start fresh:

     pkill -f "dx serve" 2>/dev/null || true
     dx serve --package iron_hermes_ui

   Open http://localhost:8080/ in a fresh browser tab with DevTools open.

4. **Test 6 (GAP-07-R3 re-verify)** — Themes & Tweaks Panel SCANLINES toggle:
   - Open the Tweaks panel (gear FAB at bottom-right).
   - Click the SCANLINES toggle to OFF.
   - PASS criteria: the scanlines overlay visibly DISAPPEARS.
   - Click SCANLINES back to ON.
   - PASS criteria: the scanlines overlay visibly RETURNS.
   - Sanity-check the other body-class toggles (BREADCRUMB / FOOTER / RAIL / DENSITY) still work — regression-clean from Plan 13.

5. **Test 9 (GAP-09-R3 re-verify)** — Sessions wedge:
   - Click the SESSIONS wedge.
   - PASS criteria: the list shows ONLY sessions with message_count > 0 (no foreign-format directories whose on-disk content is only trajectories.jsonl).
   - Click any row in the filtered list.
   - PASS criteria: the chat-mini header SID badge updates AND the chat bubble list populates with the historical messages for that session. The discussion is loadable.
   - If the list ends up being only web-platform sessions (because nothing else has SQLite messages), that is still PASS — empty rows are useless to click anyway.

6. After both tests pass, update `.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-UAT.md`:
   - Test 6 round_3_notes: change `result: issue` → `result: pass` and add a closure note for GAP-07-R3 citing the branch fix applied.
   - Test 9 round_3_notes: change `result: issue` → `result: pass` and add a closure note for GAP-09-R3 citing the message_count > 0 filter.
   - Update §Summary block: `passed: 13, issues: 0, round_3_closed: 4 (GAP-06, GAP-07-R3, GAP-08, GAP-09-R3); round_3_open: 0`.

7. If Test 6 OR Test 9 fails, describe the failure mode in the resume signal. A follow-up plan (Plan 15) will be authored.
  </how-to-verify>
  <resume-signal>Type "approved" once both Test 6 and Test 9 visually pass and UAT-26.2.1.md is updated. If a test fails, describe the failure mode and I will spin up a follow-up plan.</resume-signal>
</task>

</tasks>

<verification>
- `cargo check -p iron_hermes_ui` exits 0
- `cargo check -p iron_hermes_ui --features legacy-shell` exits 0
- `cargo check -p iron_hermes_ui --target wasm32-unknown-unknown` exits 0
- `cargo test -p iron_hermes_ui --bin iron_hermes_ui` exits 0 (>= 39 tests pass: Plan 13 baseline 37 + 2 new from Task 4)
- `cargo test -p iron_hermes_ui --test wave0_smoke` exits 0 (1 test passes)
- `git diff -- crates/iron_hermes_ui/src/server/state.rs crates/iron_hermes_ui/src/server/ws.rs crates/iron_hermes_ui/src/protocol.rs` is empty (D-02 narrow gate, inherited from D-26.2.1-13-A)
- `git diff -- crates/ironhermes-state/src/lib.rs` is empty (state crate untouched per D-26.2.1-14-A)
- `git diff -- crates/iron_hermes_ui/src/server/api.rs` shows exactly: 1 line added (`.filter(|s| s.message_count > 0)`), plus the GAP-26.2.1-09-R3 / D-26.2.1-14-B comment block
- `grep -q ".filter(|s| s.message_count > 0)" crates/iron_hermes_ui/src/server/api.rs` succeeds
- `grep -q "scanlines_toggle_hide_rule_exists_in_site_css" crates/iron_hermes_ui/src/components/hermes_app/mod.rs` succeeds
- `grep -q "list_sessions_filters_by_message_count_in_api_rs" crates/iron_hermes_ui/src/components/hermes_app/mod.rs` succeeds
- The Diagnostic Result block exists in this PLAN with D1/D2/D3 captured and a specific branch selected
- The Task 2 branch fix is applied to ONLY the files associated with the selected branch
- GAP-07-R3 visual re-verify passes (toggling SCANLINES off hides the overlay; toggling on returns it)
- GAP-09-R3 visual re-verify passes (SESSIONS wedge shows ONLY loadable rows; clicking populates the chat thread)

Manual re-run gates (developer Task 5):
- UAT Test 6: SCANLINES toggle visibly engages on/off (GAP-07-R3 closed)
- UAT Test 9: SESSIONS wedge shows only loadable sessions; click loads discussion (GAP-09-R3 closed)
</verification>

<success_criteria>
- Both round-3 gaps (GAP-07-R3, GAP-09-R3) close
- 5 build gates exit 0 (host check, legacy-shell check, wasm32 check, bin tests, wave0_smoke)
- D-02 narrow gate (inherited from D-26.2.1-13-A): state.rs, ws.rs, protocol.rs unchanged; api.rs diff is exactly the filter insertion + comment
- State crate (`crates/ironhermes-state/src/lib.rs`) byte-for-byte unchanged per D-26.2.1-14-A
- 4 atomic commits land (Task 1 plan-diagnostic commit, Task 2 branch fix commit, Task 3 api.rs filter commit, Task 4 regression-test commit); Task 5 is the verification gate (no commit)
- UAT-26.2.1.md round-3 entries updated to reflect closure
- Phase 26.2.1 ships at 13/13 pending final `/gsd-verify-work` re-run
</success_criteria>

<output>
After completion, create `.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-14-SUMMARY.md` per the standard summary template. Include:

- The verbatim Task 1 Diagnostic Result block (D1/D2/D3 captures + selected branch)
- The Task 2 branch fix applied (a/b/c) with the verbatim diff
- The Task 3 api.rs diff (verbatim — should be exactly the comment block + 1 filter line)
- The Task 4 new test names + final test count delta (37 → 39+)
- Confirmation that state.rs, ws.rs, protocol.rs are byte-for-byte unchanged (D-02 narrow gate)
- Confirmation that crates/ironhermes-state/src/lib.rs is byte-for-byte unchanged (D-26.2.1-14-A scope contract)
- Re-quote of the user choice for option A from `26.2.1-UAT.md` round-3 notes (verbatim) so future readers can audit the GAP-09-R3 filter scope authorization
- Net round count: round 3 closed at 4/4 (GAP-06 + GAP-08 in Plan 13; GAP-07-R3 + GAP-09-R3 in Plan 14)
</output>
