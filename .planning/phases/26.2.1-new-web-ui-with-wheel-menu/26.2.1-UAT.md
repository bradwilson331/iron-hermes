---
status: diagnosed
phase: 26.2.1-new-web-ui-with-wheel-menu
source:
  - 26.2.1-01-SUMMARY.md
  - 26.2.1-02-SUMMARY.md
  - 26.2.1-03-SUMMARY.md
  - 26.2.1-04-SUMMARY.md
  - 26.2.1-05-SUMMARY.md
  - 26.2.1-06-SUMMARY.md
  - 26.2.1-07-SUMMARY.md
  - 26.2.1-08-SUMMARY.md
  - 26.2.1-09-SUMMARY.md
  - 26.2.1-10-SUMMARY.md
  - 26.2.1-11-SUMMARY.md
  - 26.2.1-12-SUMMARY.md
started: 2026-05-13T22:30:00Z
updated: 2026-05-14T19:30:00Z
reverify_round: 5
reverify_scope: tests-2-6 (scan-bar synonym closure)
---

## Current Test

[round 3 re-verify complete — 2 pass, 2 issues, diagnosed inline]

## Round-2 D-02 Decision

User decision (2026-05-14): **Lift D-02 surgically for GAP-26.2.1-09** — change `crates/iron_hermes_ui/src/server/api.rs:60-61` from `Some(&ironhermes_core::types::Platform::Web.to_string())` to `None` so the SESSIONS wedge sources the full on-disk session catalog (currently 74 entries in `~/.ironhermes/sessions/`).

Rationale: the 1-line server diff is the smallest possible D-02 deviation; deferring to 26.2.12 would ship 26.2.1 with a SESSIONS wedge that is functionally empty for any real user. Acceptable surgical lift of the 26.2.1 D-02 contract.

Implication for the gap-closure plan: a single follow-up plan (Plan 26.2.1-13) closes all 4 round-2 gaps — GAP-06 (CSS layering), GAP-07 (CSS selector), GAP-08 (dispatch_slash refactor to allowlist+fallthrough), GAP-09 (one-line filter relax in api.rs).

## Round-3 Re-verify Outcome (2026-05-14)

Manual UAT after Plan 13 landed (commits `0b02b440`, `61930b11`, `d6a8eae1`, `3efe0868`):

- Test 2 (GAP-06 footer pills cover HUD corners) — **PASS**. Plan 13 raised `.hud-corner--bl/.hud-corner--br` z-index above `.app-footer`. Round-2 GAP-06 closed.
- Test 6 (GAP-07 scanlines toggle) — **FAIL still**. User confirmed scanlines remain on after toggle OFF. Plan 13's `display: none !important` on `body.no-scanlines .scanlines` did not engage. Other body-class toggles still work, exonerating the signal path. Re-opened as **GAP-26.2.1-07-R3**: live DOM diagnostic needed (likely body class not landing on `<body>` or a higher-specificity rule still painting the overlay).
- Test 8 (GAP-08 dispatch_slash) — **PASS**. Plan 13's `dispatch_slash → bool` refactor with fall-through to `ws.send_raw` works for `/research` etc. Round-2 GAP-08 closed.
- Test 9 (GAP-09 list_sessions catalog) — **PARTIAL**. Plan 13's D-02 surgical lift (Some(Web)→None) now lists ~74 sessions from `~/.ironhermes/sessions/`, but clicking a non-web row does not load the discussion. User shared a directory listing showing only `trajectories.jsonl` for one of the surfaced sessions — that on-disk content is from a different agent format and is not loadable by the chat path. The listing is broader than the loadable set. Re-opened as **GAP-26.2.1-09-R3** with **scope option A** (chosen by user 2026-05-14): filter `list_sessions` to return only **loadable** sessions (e.g., `message_count > 0` AND a verified SQLite messages row, OR a known-loadable source list). Keeps D-02 narrow-gate spirit.

Net round-3: 2 closed (GAP-06, GAP-08), 2 reopened (GAP-07-R3, GAP-09-R3). Plan 14 will close the remaining two before phase 26.2.1 can ship at 13/13.

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running iron_hermes_ui dev server. From the repo root, run `dx serve --package iron_hermes_ui` (this is Dioxus 0.7's unified fullstack launcher — it builds the WASM client and runs the server in one process; the separate `cargo run --features server` path from UAT-26.2.1.md §How-to-run is incorrect and panics on a missing `target/debug/public` dir). Open http://localhost:8080/ in a fresh browser tab (DevTools open). Page loads with no red console errors, the HermesApp shell mounts (not the Plan 01 stub placeholder text), and no WASM panic appears in the console.
result: pass
notes: User reported that `dx serve --package iron_hermes_ui` works; the inherited `cargo run … --features server` step from UAT-26.2.1.md §How-to-run is broken (panics on missing `target/debug/public` dir). Doc bug, not a code defect — logged in Gaps as a minor follow-up.

### 2. Page Load & HUD Chrome
expected: HUD corners render in all four corners (TL/TR/BL/BR). Scanlines overlay animates, hud-grid + hud-vignette visible, scan-bar sweeps horizontally. Top-left breadcrumb reads "NODE HERMES-7 › BRIDGE › CHAT" (rightmost crumb tracks active screen). Top-right SysMeta shows BUILD/UPTIME/OP. Bottom-left footer shows NODE/SCREEN/AGENT; bottom-right shows MEM/SKILLS/P50 + UTC clock. After ~2s the footer clock ticks at least once.
result: pass
prior_round_1: issue — "I don't see the bottom left or bottom right bread crumbs, but everything else is there" (severity: major). Closed by Plan 10 (commit 80452931 — appended .app-footer + chrome positioning CSS to site.css).
round_2_notes: User confirmed the bottom-left and bottom-right footer pills now render. New cosmetic side-effect: the bottom-left and bottom-right HUD corner brackets are now covered by the footer pills (z-order/positioning collision). Logged as new gap GAP-26.2.1-06 for follow-up — does not block round-2 closure of GAP-26.2.1-02 since the requested chrome IS present.
round_3_notes: PASS. Plan 13 (commit 0b02b440) raised `.hud-corner--bl/.hud-corner--br` above `.app-footer` z-stack. Footer pills AND HUD corner brackets render simultaneously. GAP-26.2.1-06 closed.
round_5_notes: HUD chrome `expected:` mentioned "scan-bar sweeps horizontally" — that overlay has been REMOVED in round-5 (commits fba5f103, 74fe99c6). HudChrome now renders two full-viewport overlays (hud-grid + hud-vignette) plus the four corner brackets. The scan-bar removal was part of GAP-26.2.1-07-R3-FEATURE-REMOVAL round-5 (scrolling-line synonym closure); see Test 6 round_5_notes for full context. Other Test 2 elements (breadcrumb, sys-meta, HUD corners, footer pills) are regression-clean per round-5 build gates.

### 3. Wheel — Rendering & Hover
expected: A 380px SVG wheel renders in the upper-left of the viewport with exactly 10 wedges. Wedge labels read in D-10 index order: CHAT, AGENTS, MODELS, TOOLS, SKILLS, MEMORY, SESSIONS, PROVIDER, GATEWAY, SYSTEM. Glyphs render: ▓ ◆ ◇ ◈ ✦ ⬢ ▣ ◉ ⌬ ⚙. Hub center shows the active wedge's label (CHAT default), sub-label INTELLIGENCE CONSOLE, and "▸ LAUNCH" CTA. Hovering each wedge toggles `is-active` highlight and updates the hub label.
result: pass

### 4. Wheel — Click & Navigate
expected: Clicking each of the 10 wedges switches the active screen — the matching `<section id="screen-XXX">` gets `class="screen is-active"`, the footer SCREEN field updates, and the breadcrumb rightmost crumb updates. Clicking the hub launches the currently-hovered wedge. Return to Chat works.
result: pass
notes: Screen switching works correctly. Footer SCREEN field update can't be observed because of the AppFooter regression already logged in Test 2 — not a new issue.

### 5. Wheel — Drag & Resize
expected: Pointerdown on the rim annulus and drag — the wheel follows the cursor smoothly to each of the four viewport corners without snap-back. Dragging near the right edge, DRAG_MARGIN (33px) keeps the wheel + resize ring inside the viewport. Pointerdown on the orange south-east resize ring and drag outward → `--wheel-size` CSS var increases up to 640; drag inward → decreases to 240 (MIN_SIZE). After moving + resizing, hard-reload — position and size persist.
result: pass

### 6. Themes & Tweaks Panel
expected: Clicking the gear FAB at bottom-right slides up the tweaks panel. Cycling all 5 themes (slate-dark, slate-light, iron-dark, terminal-dark, parchment-light) updates `<html data-theme="…">` and flips the palette. Picking each AccentColor (TEAL/ORANGE/GREEN/VIOLET/AMBER) changes the `--teal` / `--teal-bright` CSS vars on `<html>`. The WHEEL slider scales the wheel SVG live. Toggling SCANLINES/BREADCRUMB/FOOTER/RAIL adds/removes the matching `body.no-*` / `body.has-rail` class; density DENSE adds `body.density-dense`.
result: removed
reported: "fail scanlines still on after off selected - all other items pass"
severity: minor
prior_round_1: issue — "Cycling all 5 themes does not work. AccentColors work. Wheel slider scale works. Toggling SCANLINES/BREADCRUMB/FOOTER/RAIL do not work." (severity: major). Closed by Plan 10 (commit dc7e5122 — appended 5 [data-theme] blocks reassigning legacy var family + body-class toggle rules; theme_effects.rs untouched per D-02).
round_2_notes: Round 2 closes most of GAP-03 — theme cycling works, BREADCRUMB / FOOTER / RAIL / DENSITY toggles work, AccentColor + WHEEL slider regression-clean. Only the SCANLINES toggle fails. Logged as new round-2 gap GAP-26.2.1-07. Likely cause: Plan 10 added `body.no-scanlines .scanlines { display: none }` per the verbatim app.html copy, but the scanlines element either uses a different class name in the new component tree, or a more-specific rule (animation/opacity) is overriding `display: none`. Other body-class toggles work, which exonerates the body_class_effect signal/effect path entirely — so this is a CSS-selector-mismatch bug in site.css, not a Rust bug.
round_3_notes: FAIL still. Plan 13 (commit 61930b11) added `!important` to `body.no-scanlines .scanlines { display: none !important; }` but the overlay still paints when toggle is OFF. Reopened as GAP-26.2.1-07-R3. The CSS rule now exists with `!important`, the body-class toggle path is proven sound by all other working chrome toggles, so the remaining failure is either (a) the body class isn't actually being added (effect not firing for this specific signal — unlikely, but verify in DevTools), (b) the rendered scanlines element has a different class or is nested such that the descendant selector misses it, or (c) a more-specific rule (e.g. `.scanlines { display: block; … }` with higher specificity inside a media query or pseudo-element rule) wins despite `!important`. Plan 14 must do a live DevTools diagnostic on `<body>`'s classList when toggle is OFF and `getComputedStyle(.scanlines)` to identify the actual winning rule.
round_4_notes: CLOSED BY REMOVAL. Plan 15 (commits d6c940b1, cbdfba43, 463b48f3, bb7e96c0, 16edb4ec, f0987647) removed the scanlines feature entirely from all 7 in-scope source files (site.css, hud_chrome.rs, ui_prefs.rs, theme_effects.rs, tweaks_panel.rs, screens/settings.rs, mod.rs). The Plan 14 Branch (c) triple-guard CSS (specificity 0,2,2 + display:none + visibility:hidden + opacity:0 all !important) did NOT engage in round-3 UAT despite being unbeatable in principle. After 4 failed fix attempts (Plans 10, 13, 14a/b/c) the user decision 2026-05-14 was to remove the feature rather than continue iterating. GAP-26.2.1-07-R3 disposition: closed_by_removal. New gap entry GAP-26.2.1-07-R3-FEATURE-REMOVAL records the 4-attempt history.
round_5_notes: FULLY CLOSED. After round-4 shipped (commit a8a29544), the user reported the scrolling-line animation was STILL VISIBLE on the rendered webpage: "scanlines animation is scrolling on the webpage even though the removal has been 'completed'. Only the controls have been removed from the webpages, which did not affect the animation." Root-cause investigation found the surviving source was `.scan-bar` / `@keyframes scan-bar-move` in site.css (lines 85–97) — a synonymous 2px teal horizontal line animating translateY(0vh → 100vh) over 7s, mounted unconditionally by HudChrome at hud_chrome.rs:20. Introduced by Plan 26.2.1-03 as one of three "decorative full-viewport overlays"; never linked in code, comments, or commit history to the word "scanlines". The round-4 `scanlines_feature_is_fully_removed` test was a textual include_str! grep for `.scanlines` / `no-scanlines` / `pub scanlines: bool` strings — by construction unable to catch a synonym. Round-5 fix (4 commits): fba5f103 (remove .scan-bar CSS), 74fe99c6 (remove scan-bar DOM mount + module doc update), 17e43f2e (extend removal-guard test with 3 new asserts: .scan-bar / scan-bar-move / class:"scan-bar"), plus this docs commit. All 5 build gates re-run green; D-02 narrow gate EMPTY since a8a29544. User-visible regression closed: no horizontal line scrolls on the rendered page after round-5.
reported_round_3: "fail scanlines still on after off selected"
severity_round_3: minor

### 7. Persistence (localStorage)
expected: DevTools → Application → localStorage shows three keys under http://localhost:8080: `ih.ui.tweaks`, `ih.ui.theme`, `ih.ui.wheel`. After hard-reload, every tweak from §6 persists. Manually editing `ih.ui.theme` to `iron-dark` and reloading picks up the manual edit. Setting `ih.ui.tweaks` to invalid JSON like `{not-json}` and reloading falls back to `UiPrefs::default()` without panic (T-26.2.1-05 mitigation).
result: pass
notes: User confirms the three keys appear and persist across reload; manual-edit pickup and tamper-safe fallback work. Side note (separate from this test): some Tweaks/Settings UI controls don't visibly engage their side-effects (see Test 6 issue) — but the persistence layer itself round-trips correctly.

### 8. Chat — Round Trip
expected: On the Chat screen, the chat-mini header shows the 8-char session id prefix and status READY. Typing "hello, what model are you?" + Enter renders a user bubble immediately and an assistant bubble below streaming deltas character-by-character; status flips to STREAMING then back to READY. Shift+Enter inserts a literal newline in the user bubble. `/clear` empties the chat buffer. `/research weather` streams an assistant bubble with tool-progress rows underneath during tool execution (is-running → is-done is-success). Toggling DevTools "Offline" then back online triggers automatic WS reconnect (Phase 26.1 `with_automatic_reconnect`).
result: issue
reported: "fail on research command result is unknown command: /research"
severity: major
prior_round_1: issue — "no - it takes a while for the system to respond and this does not appear to be a builtin command *Context cleared.*…" (severity: major). Closed by Plan 11 (commit 1d3f8980 — added dispatch_slash + early-return slash-prefix branch in hermes_app/mod.rs send EventHandler; client-side, D-02 preserved). `/clear` should now empty the bubble list locally with ZERO WS frames; `/notarealcommand` should produce a single error-styled "unknown command" bubble locally with zero WS frames. Plain messages still round-trip through ws.send_raw.
round_2_notes: GAP-04 (`/clear` empties buffer) IS closed — user did not flag /clear failing. NEW issue: Plan 11's `dispatch_slash` helper intercepts ALL slash-prefix input and falls into the `_ => unknown command` arm for anything not equal to `"/clear"`. This breaks `/research weather` (a SERVER-side tool-execution command) because the early-return branch swallows it before it reaches `ws.send_raw(ChatRequest)`. Plan 11's "Known Stubs" section anticipated this for `/help`/`/status`/`/personality` (UI-only commands) but did not contemplate that `/research` is a server-routed command. The fix is to make `dispatch_slash` use an ALLOWLIST of UI-only commands (return-true-when-handled) instead of a blanket interception — unrecognized slashes should fall through to ws.send_raw so the server (or LLM) can handle them. Logged as new major round-2 gap GAP-26.2.1-08.
round_3_notes: PASS. Plan 13 (commit d6a8eae1) refactored `dispatch_slash` to `fn → bool`; `/clear` returns true after clearing bubbles, `_` arm returns false. Call-site now `if trimmed.starts_with('/') && dispatch_slash(...) { return; }` so unhandled slashes fall through to `ws.send_raw(ChatRequest)`. User confirmed `/clear` empties bubbles (no WS frame), `/research weather` streams from server, `hello` round-trips. GAP-26.2.1-08 closed.

### 9. Sessions — Switch & Delete
expected: SESSIONS wedge renders the sessions list. Clicking a non-current row switches to Chat and the chat-mini header shows the new session id short-form. Clicking a row's "×" close button removes the row from the list via optimistic local delete; `evt.stop_propagation()` prevents accidental row-select on close. NOTE: server-side delete is deferred to 26.2.12 — after reload the closed session reappears (documented in VERIFICATION.md deferred items).
result: issue
reported: "sessions still load current sessions. Sessions must load from /Users/twilson/.ironhermes or the defined IRONHERMES_HOME defined path"
severity: major
prior_round_1: issue — "shows different sessions but all reference the current session." (severity: major). Closed by Plan 12 (commit 3061e10a — chat.rs SID badge truncation now takes LAST 8 chars of `agent:main:web:dm:{uuid}` instead of leading 8 which was constant `agent:ma`).
round_2_notes: Plan 12's SID-badge truncation IS correct (each row's badge would now show its own tail), BUT the round-1 root-cause analysis was incomplete: the assumption "Sessions list renders multiple distinct rows (good — list_sessions server fn is firing)" was wrong. The user clarifies the actual contract: the sessions list MUST source from the on-disk session store at `$IRONHERMES_HOME` (default `/Users/twilson/.ironhermes`), not from in-memory current-process sessions. Whatever `list_sessions` is currently returning is NOT reading the home-dir session catalog. This re-categorizes the bug from a UI-truncation issue (Plan 12's fix-site) to a SERVER data-layer issue at `crates/iron_hermes_ui/src/server/api.rs::list_sessions` — which D-02 forbids touching in 26.2.1. The original GAP-05 wasn't actually closed by Plan 12; it was masked. New gap GAP-26.2.1-09 logged with the corrected scope. Likely needs a follow-up phase that lifts D-02 OR an intermediate phase that adds a new server fn for the home-dir scan.
round_3_notes: PARTIAL. Plan 13 (commit 3efe0868) USER-APPROVED D-02 surgical lift — api.rs:60-61 changed `Some(&Platform::Web.to_string())` → `None`. SESSIONS wedge now lists ~74 cross-platform sessions from the SQLite catalog. BUT clicking a non-web row does not load the discussion. User shared `~/.ironhermes/sessions/03057b90-2e1b-4944-b75c-7914ea6a60a1/` directory listing — only `trajectories.jsonl` present (foreign agent format, not iron_hermes message history). The list now includes sessions whose content the chat-load path cannot read. Reopened as GAP-26.2.1-09-R3 with chosen scope option A: filter `list_sessions` to only return **loadable** sessions (criteria: e.g. `message_count > 0` AND verified SQLite messages row, OR known-loadable source allowlist). Keeps D-02 narrow-gate spirit (api.rs is already exempted; this is a refinement of the same line).
round_4_notes: PARTIALLY CLOSED — user-approved ship with residual deferred. Plan 14 (commit 5f1a9f2a) implemented Option (i) per D-26.2.1-14-B: `.filter(|s| s.message_count > 0)` post-filter in api.rs `list_sessions`. However, the round-3 live DOM diagnostic the user performed for Test 6 also surfaced GAP-09 residual evidence — the foreign-format session `03057b90-2e1b-4944-b75c-7914ea6a60a1/` (with only `trajectories.jsonl` on disk, no SQLite messages of iron_hermes shape) was found to have a **non-zero `message_count`** (15 msgs reported by the SQLite sessions row). The `message_count > 0` filter therefore does NOT exclude this row from the listing, and clicking it still fails to load. User decision (2026-05-14): ship Plan 14 with the `message_count > 0` filter as the round-3 ship state; defer the deeper fix to phase **26.2.12** (the same phase that owns deferred server-side session delete). New residual gap **GAP-26.2.1-09-R3-RESIDUAL** logged below — disposition: deferred to 26.2.12.
reported_round_3: "sessions still load current sessions" → updated 2026-05-14: "test 9 shows .ironhermes/sessions/ but the session dir is not loaded to continue the disscussion" (loadable-subset issue)
severity_round_3: major

### 10. Settings — Read & Write
expected: SYSTEM wedge routes to Screen::Settings. Runtime block renders four read-only rows (MODEL/PROVIDER/CONTEXT/MEMORY) from ConfigSummary with disabled affordance + "(coming soon)" copy. UI Preferences block exposes 5 theme buttons, 5 accent buttons, wheel-size slider, 4 boolean toggles, density two-state — same state as the Tweaks panel. Under "Other Screens", clicking SOUL / SCHEDULES / OFFICE switches `Signal<Screen>` and activates the corresponding off-wheel screen (D-10 reachability).
result: pass

### 11. Visual Stubs (10 screens)
expected: Clicking each non-live wedge — AGENTS (4 agent cards), SKILLS (6 skill cards), MODELS (5 cards in 2 families), MEMORY (5 entries + profile + provider), TOOLS (12 tool cards), GATEWAY (6 platform cards, mix of connected + disconnected), PROVIDERS (5 provider cards) — renders pixel-faithful content from `stub_data::*`, NOT the Plan 03 placeholder. Via Settings → Other Screens, SOUL (4 persona picker + SOUL.md editor), SCHEDULES (5 cron rows), OFFICE (claw-stage + 4 devices) also render mock content. No console errors on any screen; navigation back to Chat works.
result: pass

### 12. D-02 — Backend Byte-for-Byte Untouched
expected: From the repo root, run `git diff 5afe6175..HEAD -- crates/iron_hermes_ui/src/server/ crates/iron_hermes_ui/src/protocol.rs`. Output is empty (zero lines, zero files changed) across the entire phase. (Baseline commit 5afe6175 is "chore: insert phase 26.2.1".)
result: pass
notes: Verified live — `git diff 5afe6175..HEAD --stat -- crates/iron_hermes_ui/src/server/ crates/iron_hermes_ui/src/protocol.rs` returns 0 files / 0 lines.

### 13. Build & Test Gates
expected: All four commands exit 0: `cargo check -p iron_hermes_ui`, `cargo check -p iron_hermes_ui --features legacy-shell`, `cargo check -p iron_hermes_ui --target wasm32-unknown-unknown`, `cargo test -p iron_hermes_ui --bin iron_hermes_ui` (32 tests pass) and `cargo test -p iron_hermes_ui --test wave0_smoke` (1 test passes).
result: pass
notes: All 5 commands verified live — host check, legacy-shell check, wasm32 check, bin tests (32 passed / 0 failed), wave0_smoke (1 passed / 0 failed). 15 dead-code warnings on the bin tests are pre-existing per VERIFICATION.md.

## Round-4 Outcome (2026-05-14)

Manual UAT after Plan 15 landed (scanlines feature removal):

- Test 6 (GAP-07-R3 scanlines toggle) — **CLOSED BY REMOVAL**. The Tweaks panel and Settings screen no longer expose a SCANLINES toggle row (the rows are deleted, not just disabled). No `.scanlines` element renders. The toggle removal was verified; however, the user's subsequent re-test surfaced a residual regression (see Round-5 Outcome below).
- Test 9 (GAP-09-R3 list_sessions residual) — **UNCHANGED**. Plan 14's `message_count > 0` filter remains. GAP-26.2.1-09-R3-RESIDUAL stays deferred to phase 26.2.12 per the round-3 user decision.

Net round 4: 1 partial closure (GAP-07-R3 — toggle controls removed but visible scrolling-line effect persisted under synonym; see round-5 below), 1 deferred (GAP-09-R3-RESIDUAL unchanged).

## Round-5 Outcome (2026-05-14)

Re-verify triggered after round-4 ship — user reported: *"scanlines animation is scrolling on the webpage even though the removal has been 'completed'. Only the controls have been removed from the webpages, which did not affect the animation. This is a major issue and must be resolved. Expand the scope and research this issue."*

**Investigation:** Surveyed every `@keyframes` across all 10 CSS asset files. Found exactly one viewport-scrolling animation in the entire UI: `.scan-bar` / `@keyframes scan-bar-move` in `site.css:85–97` — a 2px teal horizontal line that animates `translateY(0vh → 100vh)` over 7s on infinite loop. The element is mounted unconditionally by `HudChrome` at `hud_chrome.rs:20` (with `aria-hidden="true"` — pure decoration). Introduced by Plan **26.2.1-03** as one of three "decorative full-viewport overlays" alongside `.hud-grid` and `.hud-vignette`. The class was never called `scanlines` in code, comments, or commit history. Visually identical effect; synonymous implementation.

**Why round-4 missed it:** the `scanlines_feature_is_fully_removed` removal-guard was a textual `include_str!` grep for the literal strings `.scanlines` / `no-scanlines` / `pub scanlines: bool`. By construction, it could not catch a synonym. All 5 build gates passed; the user-visible regression persisted nonetheless.

**Fix (4 commits, in-place round-5 amendment per D-26.2.1-15-D — no new plan file):**

- `fba5f103` fix(26.2.1-15): remove `.scan-bar` CSS rule + `@keyframes scan-bar-move` from site.css
- `74fe99c6` fix(26.2.1-15): remove `<div class="scan-bar">` from HudChrome; module doc "three overlays" → "two"
- `17e43f2e` test(26.2.1-15): extend `scanlines_feature_is_fully_removed` with 3 new asserts (`.scan-bar`, `scan-bar-move`, `class: "scan-bar"`)
- *(this commit)* docs(26.2.1-15): round-5 SUMMARY + UAT update

**Build/D-02 attestation (round-5 re-run):**

| Gate | Result |
|------|--------|
| Host check | PASS |
| Legacy-shell check | PASS |
| Wasm32 check | PASS |
| Bin tests | 40 pass / 0 fail (count unchanged) |
| Wave0 smoke | 1 pass / 0 fail |
| D-02 narrow gate | EMPTY diff for server/state.rs, ws.rs, protocol.rs, ironhermes-state/src/lib.rs, server/api.rs since a8a29544 |

**User-visible verification: PASS** (user-confirmed 2026-05-14 after round-5 commits landed): *"pass - the scan-bar is gone and other features work."* No horizontal line scrolls on the rendered page. HUD chrome (corner brackets, breadcrumb, sys-meta, footer pills, hud-grid, hud-vignette) regression-clean.

Net round 5: GAP-26.2.1-07-R3-FEATURE-REMOVAL fully closed (round-4 removed controls + .scanlines surfaces; round-5 removed the synonymous .scan-bar visual). Phase 26.2.1 ships at 12/12 directly verified + 1 closed-by-removal-via-rounds-4+5 + 1 deferred residual.

## Summary

total: 13
passed: 12
issues: 0
pending: 0
skipped: 0
removed: 1 (Test 6 — SCANLINES + scan-bar synonym both removed per GAP-26.2.1-07-R3-FEATURE-REMOVAL)
round_3_closed: 3 (GAP-26.2.1-06 footer/HUD layering; GAP-26.2.1-08 dispatch_slash fall-through; GAP-26.2.1-09-R3 partially closed via message_count > 0 filter — see RESIDUAL)
round_4_closed: 1 (GAP-26.2.1-07-R3-FEATURE-REMOVAL initial pass — toggle controls + `.scanlines` removed in Plan 15; user decision 2026-05-14)
round_5_closed: 1 (GAP-26.2.1-07-R3-FEATURE-REMOVAL synonym closure — `.scan-bar` removed; same plan 15 round-5 amendment; user-visible scrolling-line effect eliminated)
round_3_open: 0
round_3_deferred: 1 (GAP-26.2.1-09-R3-RESIDUAL — foreign-format sessions with non-zero message_count still leak through filter; user-approved ship; deferred to phase 26.2.12)
notes: Round 3 re-verify closed by Plans 13 + 14. Plan 13 closed GAP-06 and GAP-08 fully (with the user-approved D-02 surgical lift for GAP-09 broadening the listing). Plan 14 partially closed GAP-09-R3 via `message_count > 0` filter in api.rs. The Plan 14 GAP-09 fix excludes foreign-format directories whose SQLite row has `message_count == 0`, but the user's live DOM evidence showed at least one foreign-format directory has a non-zero message_count (15 msgs) and therefore still appears in the filtered list and remains unloadable. User decision 2026-05-14: ship Plan 14 with this residual; defer the session-key-format filter / chat-load path for non-web sources to phase 26.2.12 (paired with the deferred server-side session delete). Plan 15 closed GAP-26.2.1-07-R3 in two rounds: round-4 removed the literal `.scanlines` feature across 7 source files; round-5 (same day) closed the synonym `.scan-bar` after user reported the scrolling-line effect was still visible. Final phase state: 12/12 passed + 1 fully removed (rounds 4+5) + 1 deferred residual. Lesson: textual-grep removal guards cannot catch synonyms — future feature-removal plans should consider structural CSS pattern matching or wasm-bindgen-test runtime assertions for visible effects.

## Gaps

- truth: "UAT-26.2.1.md §How-to-run documents the working dev command"
  status: doc-bug
  reason: "The §How-to-run block tells the tester to start both `cargo run -p iron_hermes_ui --bin iron_hermes_ui --features server` AND `dx serve --package iron_hermes_ui`. The cargo run path panics on a missing `target/debug/public` directory. `dx serve` alone is the supported launcher for the Dioxus 0.7 fullstack target."
  severity: cosmetic
  test: 1
  root_cause: "UAT-26.2.1.md §How-to-run (Plan 09 artifact) inherited from an earlier draft that assumed a split client/server build. Dioxus 0.7's `dx serve` is the unified fullstack launcher — running a separate `cargo --features server` binary creates a process that expects `target/debug/public` to exist as a static-assets dir, which only `dx serve` populates."
  artifacts:
    - path: crates/iron_hermes_ui/UAT-26.2.1.md
      issue: "Step 1 of §How-to-run prescribes a broken `cargo run … --features server` command alongside `dx serve`"
  missing:
    - "Remove step 1 (the cargo-run command) from §How-to-run; renumber the remaining steps; keep `dx serve --package iron_hermes_ui` as the sole launcher"

# ============================================================================
# CONVERGENT BUG — Gaps 2 and the body-class half of Gap 3 collapse into ONE
# missing-CSS fix in Plan 01's site.css copy.
# ============================================================================

- truth: "AppFooter chrome renders bottom-left (NODE/SCREEN/AGENT) and bottom-right (MEM/SKILLS/P50/UTC-clock) fields on page load"
  status: failed
  reason: "User reported: I don't see the bottom left or bottom right bread crumbs, but everything else is there. HUD corners, scanlines, hud-grid, hud-vignette, scan-bar, top-left breadcrumb, and top-right SysMeta all render correctly — footer-only failure."
  severity: major
  test: 2
  root_cause: "Plan 01's `site.css` copy was INCOMPLETE. ~70 lines of chrome CSS at `filestoimport/ironhermes-design-system/project/app.html` lines 42-112 were silently dropped during the Task 1 asset copy — including the full `.app-footer { position: fixed; left:0; right:0; bottom:0; height:30px; … }` rule plus `.app-footer-right`, `.app-footer .v`, `.app-footer .sep`, all `.breadcrumb` / `.sys-meta` positioning rules, AND every `body.no-scanlines / .no-breadcrumb / .no-footer / .has-rail / .density-dense / .on-chat` toggle rule. `app_footer.rs` emits correct DOM (`<div class='app-footer'>…</div>`) and is correctly mounted at `mod.rs:301`, but `.app-footer` has ZERO CSS rules anywhere in the 5 loaded stylesheets (site.css, tokens.css, wheel.css, screens.css, components.css), so the element renders as a zero-sized unstyled inline-flow div hidden under `body { overflow: hidden }`. Plan 03 SUMMARY line 114 explicitly claimed `CSS is supplied by assets/site.css (copied verbatim from the design bundle by Plan 01)` — that claim is false. Verified via `grep 'app-footer|no-footer|breadcrumb|sys-meta' assets/*.css` → zero matches in all 10 CSS files."
  artifacts:
    - path: crates/iron_hermes_ui/assets/site.css
      issue: "Missing ~70 lines at end of file: `.breadcrumb`/`.sys-meta`/`.app-footer` positioning blocks + 6 body-class toggle rules"
    - path: crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/app.html
      issue: "Lines 42-112 are the canonical source; copy verbatim"
  missing:
    - "Append the dropped chrome rules from app.html lines 42-112 to assets/site.css verbatim: `.breadcrumb`, `.sys-meta`, `.app-footer`, `.app-footer-right`, `.app-footer .v`, `.app-footer .sep`, `body.no-scanlines .scanlines { display: none }`, `body.no-breadcrumb .breadcrumb { display: none }`, `body.no-breadcrumb .sys-meta { display: none }`, `body.no-footer .app-footer { display: none }`, `body.has-rail #wheel-rail { display: flex !important }`, `body:not(.has-rail) #wheel-rail { display: none }`, `body.density-dense .screen { padding-top: 70px; gap: 16px }`, `body.density-dense .screen-title { font-size: 28px }`, `body.density-dense .card { padding: 12px 14px }`, `body.density-dense .panel { padding: 14px 16px }`, `body.has-rail.on-chat #screen-chat { padding-left: 360px }`, `body.has-rail.on-chat #screen-chat .screen-header { padding-left: 0 }`, plus the `@media` narrow override for chat-rail offset"
    - "Verify post-fix: `grep app-footer assets/site.css` returns matches; AppFooter renders; toggling FOOTER off in Tweaks panel hides the footer (validates Gap 3's body-class effect)"
  debug_session: .planning/debug/app-footer-chrome-missing.md

- truth: "Cycling 5 themes in the TweaksPanel updates <html data-theme=…> AND body class toggles (scanlines/breadcrumb/footer/rail/density) flip the matching body.no-* / body.has-rail / body.density-dense classes"
  status: failed
  reason: "User reported: Cycling all 5 themes does not work. AccentColors work. Wheel slider scale works. Toggling SCANLINES/BREADCRUMB/FOOTER/RAIL do not work."
  severity: major
  test: 6
  root_cause: "TWO INDEPENDENT missing-CSS bugs — both CSS-only, no Rust changes needed. (1) THEME PICKER SILENT: `tokens.css` has `[data-theme=…]` blocks for all 5 themes (lines 79/114/149/184/219), but they ONLY override the NEW design-system tokens (`--bg-canvas`, `--fg`, `--accent`, `--bg-elevated`) consumed by `components.css`'s `ih-btn`/`ih-badge` — and NONE of those components are rendered anywhere in `hermes_app/*`. The visible chrome (`.scanlines`, `.wheel-shell`, `.tweaks-fab`, `.tweaks`, `.screen`, `.panel`, `.statbar`, corner brackets, etc.) is styled via the LEGACY `--teal/--teal-bright/--bg/--bg-card/--text/--gray/--border` vars declared once on `site.css :root` (lines 7-40). ZERO `[data-theme=…]` selector anywhere reassigns those legacy vars — so `<html data-theme=iron-dark>` is set correctly but cascades into nothing. (2) BODY-CLASS TOGGLES SILENT: same root cause as Gap 2 — the `body.no-scanlines/.no-breadcrumb/.no-footer/.has-rail/.density-dense/.on-chat` CSS rules were never ported into production CSS (they live only in `app.html` lines 102-112, 315-316, 320). Effect 2 in `theme_effects.rs` correctly toggles the classes; nothing reacts to them. (3) WHY EFFECT 3 (ACCENT) WORKS: it writes `--teal`/`--teal-bright` directly as INLINE STYLE on `<html>` — the SAME legacy vars consumed by 30+ rules across site.css/wheel.css/screens.css — so the cascade re-colors instantly. The Rust code in `theme_effects.rs` is CORRECT AS WRITTEN; do not modify."
  artifacts:
    - path: crates/iron_hermes_ui/assets/site.css
      issue: "Missing 5 `[data-theme='<slug>']` blocks at top of file that should reassign the legacy `--teal/--teal-bright/--teal-dim/--teal-faint/--teal-glow/--bg/--bg-deep/--bg-card/--bg-card-hover/--bg-input/--border/--border-faint/--border-strong/--text/--text-dim/--gray/--gray-faint` vars per theme (slate-dark = current `:root` values, plus slate-light, iron-dark, terminal-dark, parchment-light)"
    - path: crates/iron_hermes_ui/assets/site.css
      issue: "Missing body-class rules (overlap with Gap 2 fix — apply once, it closes both)"
    - path: crates/iron_hermes_ui/src/components/hermes_app/theme_effects.rs
      issue: "CORRECT AS WRITTEN — do not modify; all three use_effects subscribe and fire correctly"
  missing:
    - "Append 5 `[data-theme='<slug>'] { … }` blocks to site.css that reassign the legacy vars per theme (slate-dark = current defaults; the other four take new per-theme hex values aligned with tokens.css palettes). Alternative refactor: change `site.css :root` to alias legacy vars to the new tokens.css vars (`--teal: var(--accent);`, `--bg: var(--bg-canvas);`, etc.) so tokens.css's `[data-theme]` rules automatically flip everything — but that's higher-risk and more invasive than the additive approach"
    - "Port body-class rules from app.html lines 102-112 + 315-316 + 320 into site.css (or components.css). This is the SAME edit as Gap 2's body-class portion — apply once and both gaps close"
    - "Verify post-fix: clicking each of the 5 theme buttons visibly flips the palette; toggling SCANLINES/BREADCRUMB/FOOTER/RAIL/DENSITY visibly engages the corresponding chrome change"
  debug_session: .planning/debug/theme-and-body-class-effects-silent.md

- truth: "/clear slash command empties the chat buffer when typed into the chat input pill and submitted with Enter"
  status: failed
  reason: "User reported the assistant returned a natural-language reply (`*Context cleared.* I have reset the conversation state…`) — that's the LLM's verbal interpretation of the literal string `/clear`, meaning the slash command flowed through to inference."
  severity: major
  test: 8
  root_cause: "Neither the client nor the server intercepts the `/` prefix on the message path. The CommandRouter IS built in `server/state.rs:53` (`Arc::new(CommandRouter::new(build_command_registry()))`) but is NEVER dispatched — `run_web_turn` (state.rs:135-156) routes straight to `AgentLoop` without consulting it. The legacy shell's `warp_hermes.rs:800` has a `/clear → blocks.set(Vec::new())` handler, but it lives in the palette-pick callback ONLY (not the Enter-submit path), AND `warp_hermes.rs` is the legacy shell — the new HermesApp's chat send path is in `hermes_app/mod.rs` (where `use_websocket` is hoisted) + `hermes_app/screens/chat.rs` (Enter handler), and that path has NO slash branch. The canonical interception pattern (`if event.content.starts_with('/') → command_router.dispatch`) exists in `ironhermes-gateway/src/handler.rs:686-687,334-338` for Telegram and in `ironhermes-cli` — never ported to `iron_hermes_ui/src/server/ws.rs`. Plan 06's D-20 contract ('slash commands route server-side via existing CommandRouter') was an inherited assumption that was never actually implemented for the WebSocket path. NOTE: D-02 forbids server edits in 26.2.1 — the fix MUST live client-side. All currently-known web slash commands (`/clear`, `/help`, `/status`, `/personality`, `/doctor`, `/quit`) are pure UI-state operations with no server-side effect requirement, so client-side dispatch is architecturally correct, not a workaround."
  artifacts:
    - path: crates/iron_hermes_ui/src/components/hermes_app/mod.rs
      issue: "The `ChatSendHandler` send fn at the HermesApp root forwards every message via `ws.send_raw(ChatRequest)` with no slash-prefix branch"
    - path: crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs
      issue: "The Enter-submit handler calls `send.0.call(text)` without pre-checking for `/`; alternative fix-site is here if upstream send fn cannot be modified"
    - path: crates/iron_hermes_ui/src/server/state.rs
      issue: "CommandRouter is built (line 53) but never dispatched from run_web_turn (lines 135-156) — D-02 forbids changing this in 26.2.1; this is informational for a future phase"
    - path: crates/iron_hermes_ui/src/server/ws.rs
      issue: "ws_chat handler has no slash interception — D-02 forbids touching this in 26.2.1; informational"
    - path: crates/iron_hermes_ui/src/components/warp_hermes.rs
      issue: "Legacy shell has a /clear handler at line 800 in the palette-pick callback only — does NOT apply to the new HermesApp; do not modify (legacy-shell feature-gated)"
  missing:
    - "Add a client-side `dispatch_slash(cmd: &str)` helper that handles at minimum `/clear` (clear the bubbles signal). Other commands (`/help`, `/status`, `/personality`, etc.) can be deferred or skeleton-stubbed for follow-up"
    - "In the ChatSendHandler send fn at `hermes_app/mod.rs` (or, if that's harder to reach, in the Enter handler in `hermes_app/screens/chat.rs`): at the top of the function, after trimming, check `if text.starts_with('/') { dispatch_slash(&text); return; }` BEFORE `ws.send_raw(ChatRequest)`"
    - "Document in the SUMMARY for the fix plan that D-20's 'server-side CommandRouter' contract is amended to 'client-side dispatch for purely-visual commands; server-side CommandRouter dispatch is reserved for future server-effecting commands and requires a follow-up phase that lifts D-02'"
    - "Verify post-fix: typing `/clear` + Enter clears bubbles without an LLM round-trip; the bubble list goes empty and chat-mini status stays READY"
  debug_session: .planning/debug/slash-command-clear-not-intercepted.md

- truth: "Clicking a non-current session row in ScreenSessions switches Screen to Chat AND the chat-mini header shows the new session id"
  status: failed
  reason: "User reported: shows different sessions but all reference the current session."
  severity: major
  test: 9
  root_cause: "The bug is NOT in `screens/sessions.rs` — the row-click write path is correct. `sessions.rs:110-117` iterates `for s in visible.iter().cloned()` and passes `session: s.clone()` to `SessionRow`; line 135 captures `sid_for_select = session.id.clone()` per row; line 164 calls `on_select.call(sid_for_select.clone())`; the on_select closure writes the new id into `SessionIdContext.0` and flips `Screen::Chat`. SessionIdContext is correctly newtyped and consumed by ScreenChat at `screens/chat.rs`. THE BUG is in `screens/chat.rs:163-164` — the chat-mini header's SID badge: `let sid_short = sid_full.chars().take(8).collect();`. But EVERY session id created by the web path is formatted as `format!('agent:main:web:dm:{session_uuid}')` (server/api.rs:114), so the first 8 chars of every session key are the constant string `'agent:ma'`. When the user clicks a different row, `SessionIdContext` IS updated to the new id (the data layer is correct), but the header always re-renders as `agent:ma`, giving the visual impression that all rows reference the current session. The data layer is sound; the projection into the header is a constant. This is a one-line truncation bug."
  artifacts:
    - path: crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs
      issue: "Line 164 (or thereabouts): `let sid_short: String = sid_full.chars().take(8).collect();` truncates from the front, but all web session keys share the 17-char prefix `agent:main:web:dm:`. Need to take the LAST 8 chars (or strip the prefix first)"
    - path: crates/iron_hermes_ui/src/server/api.rs
      issue: "Line 114 establishes the constant prefix `agent:main:web:dm:<uuid>` — informational, do not modify (D-02)"
    - path: crates/iron_hermes_ui/src/components/hermes_app/screens/sessions.rs
      issue: "INVESTIGATED AND EXONERATED — the row-click write path is correct; do not modify"
  missing:
    - "Change `screens/chat.rs:164` to take the LAST 8 chars of the id (the uuid suffix tail): `let sid_short: String = if sid_full.len() <= 8 { sid_full.clone() } else { sid_full[sid_full.len() - 8..].to_string() };`"
    - "Verify post-fix: clicking different session rows changes the chat-mini header value (each session now shows a distinct 8-char tail). Also run Test 9b (originally not exercised) — close-button optimistic delete + stop_propagation"
  debug_session: .planning/debug/session-row-click-no-switch.md

# ============================================================================
# ROUND 2 GAPS (surfaced during re-verify after Plans 10/11/12 landed)
# ============================================================================

- truth: "Bottom-left and bottom-right HUD corner brackets remain visible alongside the AppFooter pills (no z-order or layout collision)"
  status: failed
  reason: "User reported during round-2 re-verify of Test 2: 'Looks good - bottom pills cover the 2 right and left bottom HUD corners.' The Plan 10 chrome restoration did make the footer render (closing GAP-02), but the new `.app-footer` rule positions on top of the pre-existing BL/BR HUD corner-bracket elements."
  severity: cosmetic
  test: 2
  round: 2
  root_cause: "Likely one of: (a) `.app-footer { position: fixed; bottom: 0; left: 0; right: 0; height: 30px; }` shares the same z-index layer as `.hud-corner` BL/BR and visually occludes them — fix by lowering footer z-index or raising HUD corners; OR (b) the BL/BR HUD corner brackets are positioned with bottom < 30px and the new 30px-tall footer simply covers them — fix by either moving footer above the corners (e.g., `bottom: 8px` with rounded corners) or shifting BL/BR brackets up by `--app-footer-height`. Verbatim copy from app.html may have been pixel-faithful in isolation but the HUD-corners + footer composition was not co-rendered in the design source."
  artifacts:
    - path: crates/iron_hermes_ui/assets/site.css
      issue: "Investigate computed z-index and bottom-offset of `.app-footer` vs `.hud-corner.hud-corner--bl` / `.hud-corner.hud-corner--br`"
  missing:
    - "Pick a layering strategy (raise corners above footer OR shrink/inset footer above corners) — both are CSS-only, single-rule changes"
    - "Re-verify Test 2 after fix: BL/BR HUD corners visible AND footer pills visible simultaneously"
  follow_up_phase: deferred — non-blocking cosmetic regression; safe to ship 26.2.1 and address in a touch-up phase

- truth: "Toggling SCANLINES off in the Tweaks panel hides the scanline overlay (body.no-scanlines .scanlines { display: none } engages)"
  status: failed
  reason: "User reported during round-2 re-verify of Test 6: 'fail scanlines still on after off selected - all other items pass'. Theme cycling works; BREADCRUMB / FOOTER / RAIL / DENSITY toggles all work — only the SCANLINES toggle does not visually engage. AccentColor + WHEEL slider regression-clean."
  severity: minor
  test: 6
  round: 2
  root_cause: "Plan 10 appended `body.no-scanlines .scanlines { display: none }` per a verbatim copy from app.html lines 102-112. The `body_class_effect` in theme_effects.rs is confirmed firing (because BREADCRUMB / FOOTER / RAIL / DENSITY all flip via the same effect), so the signal path is sound. The remaining failure must be a CSS selector mismatch: either (a) the scanlines DOM element is rendered with a class other than `.scanlines` in hermes_app — e.g. `.scanline` (singular), `.scanlines-overlay`, or nested under a wrapper that breaks the descendant selector — OR (b) a more-specific rule (an `@keyframes`-driven animation or an `opacity`/`transform` rule with higher specificity) overrides `display: none` such that the element still paints. Inspect: (1) the DOM element class in `components/hermes_app/*` (likely `app_layout.rs` or `mod.rs` — wherever the scanlines overlay div is rendered); (2) cross-reference against the `body.no-scanlines …` selector in site.css to confirm the descendant matches. Likely fix is a single one-line CSS edit (selector spelling) — no Rust change."
  artifacts:
    - path: crates/iron_hermes_ui/assets/site.css
      issue: "Verify the appended `body.no-scanlines .scanlines { display: none }` rule actually targets the scanlines element's real class"
    - path: crates/iron_hermes_ui/src/components/hermes_app/
      issue: "Find the rsx! that renders the scanlines overlay; confirm its class attribute matches the CSS selector exactly"
  missing:
    - "Audit the scanlines element class in the hermes_app component tree vs. the `body.no-scanlines` rule's selector in site.css; reconcile via a one-line selector or class-name fix"
    - "Re-verify Test 6 SCANLINES toggle — overlay should hide on toggle-off and reappear on toggle-on"
  follow_up_phase: candidate for inclusion in the same touch-up phase as GAP-26.2.1-06 (both are localized CSS edits)

- truth: "Typing `/research weather` + Enter sends the slash command to the server (or LLM) so the research tool executes and streams tool-progress rows beneath an assistant bubble"
  status: failed
  reason: "User reported during round-2 re-verify of Test 8: 'fail on research command result is unknown command: /research'. Plan 11's `dispatch_slash` helper intercepts EVERY slash-prefix input — `_ => ChatBubble::error(id, format!('unknown command: {head}'))` — and the early-return swallows the message before it reaches `ws.send_raw(ChatRequest)`. So `/research` (and any future server-routed slash command) cannot reach the backend. `/clear` itself works correctly (user did not flag it failing — GAP-04 IS closed)."
  severity: major
  test: 8
  round: 2
  root_cause: "Plan 11's design over-corrected GAP-04 by making slash interception unconditional. The intended split is: (a) UI-state commands handled CLIENT-SIDE (`/clear` clears the bubble buffer locally), (b) server-effecting commands fall through to `ws.send_raw` for the server CommandRouter (or, until D-02 lifts, the LLM/agent loop) to handle. Plan 11's `dispatch_slash` collapsed (b) into a hardcoded error response. The 'Known Stubs' note in Plan 11's SUMMARY anticipated this for purely-visual commands (/help, /status, /personality) but did NOT account for `/research` being a server-side tool-execution command. The matrix is: `/clear` (UI-state) — handle client-side; `/help`, `/status`, `/personality` (would-be UI-state) — stubbed-as-unknown for now is fine; `/research` and any future tool-routed slash command — MUST flow through to server. Fix: change `dispatch_slash` to return `bool` (handled vs. not), and the call-site early-return only when handled; the `_ =>` arm returns `false` instead of appending an error bubble, letting the message fall through to `ws.send_raw`."
  artifacts:
    - path: crates/iron_hermes_ui/src/components/hermes_app/mod.rs
      issue: "`dispatch_slash` should be `fn dispatch_slash(...) -> bool` — return true on `/clear`, false on `_`. The send EventHandler should `if trimmed.starts_with('/') && dispatch_slash(...) { return; }` so unhandled slashes fall through to ws.send_raw"
    - path: crates/iron_hermes_ui/src/components/hermes_app/mod.rs
      issue: "Update the 5 static-grep regression tests (`unknown_command_arm_appends_error_bubble_locally` test will need to be replaced with `unknown_command_arm_falls_through_to_websocket`)"
  missing:
    - "Refactor `dispatch_slash` to return a handled-bool; remove the unknown-command error bubble; let unhandled slashes fall through to `ws.send_raw(ChatRequest)`"
    - "Re-verify Test 8: `/clear` still empties the buffer locally with zero WS frames; `/research weather` now reaches the server and streams tool-progress rows; plain messages still round-trip"
    - "Update Plan 11's static-grep tests to reflect the new fall-through behavior (the 'unknown_command_arm_appends_error_bubble_locally' test needs to be inverted)"
  follow_up_phase: needed before phase 26.2.1 can ship — `/research` is a documented Test-8 acceptance criterion. Recommend a Plan 26.2.1-13 gap-closure with both this and GAP-07 (scanlines toggle); GAP-06 (footer/HUD overlap) can ride along since it's a CSS one-liner

- truth: "ScreenSessions lists ALL sessions persisted under $IRONHERMES_HOME (default ~/.ironhermes/sessions/), not just sessions originated by the current web process"
  status: failed
  reason: "User reported during round-2 re-verify of Test 9: 'sessions still load current sessions. Sessions must load from /Users/twilson/.ironhermes or the defined IRONHERMES_HOME defined path'. Plan 12's SID-badge truncation fix is correct in isolation but masked the deeper problem — there is plenty of session history on disk (74 entries in ~/.ironhermes/sessions/) but the user only sees current-process web sessions in the UI."
  severity: major
  test: 9
  round: 2
  root_cause: "The `list_sessions` server fn at `crates/iron_hermes_ui/src/server/api.rs:54-76` calls `state_store.list_sessions(Some(&Platform::Web.to_string()), 100)` — the `Some(Platform::Web)` argument restricts results to sessions whose platform tag is exactly `web`. All historical sessions in `~/.ironhermes/sessions/` were originated by other platforms (CLI/REPL/Telegram/etc.) and are filtered out. The state_store IS reading from the on-disk catalog correctly — it's the platform filter at the API boundary that hides the bulk of the sessions. Verified: `ls ~/.ironhermes/sessions/ | wc -l` → 74 entries on disk; `list_sessions(Some(Web), 100)` returns only the freshly-created `agent:main:web:dm:{uuid}` keys for the current process. Plan 12's exoneration of `screens/sessions.rs` was correct (the row-click write path works), and Plan 12's SID-badge tail fix was correct. The miss was upstream: round-1 root-cause analysis assumed the visible rows represented the full session catalog. They don't — they're a single-platform slice."
  artifacts:
    - path: crates/iron_hermes_ui/src/server/api.rs
      issue: "Line 60-61: `.list_sessions(Some(&Platform::Web.to_string()), 100)` — the `Some(Web)` filter excludes the on-disk session catalog"
    - path: ~/.ironhermes/sessions/
      issue: "74 historical sessions present on disk; user expects to see them all in the SESSIONS wedge"
  missing:
    - "Decide the contract: (a) drop the platform filter (`None` instead of `Some(Web)`) so the SESSIONS wedge shows the full cross-platform catalog, OR (b) add a UI affordance (filter chip / toggle) to switch between 'Web sessions only' and 'All sessions', OR (c) keep the web filter but aggregate other platform-keyed adjacent sessions where the conversation thread is shared"
    - "Implement the chosen contract in `list_sessions` — this REQUIRES touching `crates/iron_hermes_ui/src/server/api.rs`, which D-02 currently forbids in 26.2.1. Decision needed: lift D-02 for this single line, OR defer to a follow-up phase that opens the server boundary"
    - "Re-verify Test 9 after fix: SESSIONS wedge shows the historical sessions from disk; clicking one switches to that session's chat thread; SID badge shows that session's tail"
  follow_up_phase: needed before 26.2.1 ships if shipping requires a useful sessions list — currently the SESSIONS wedge is functionally empty for a real user. Decision required: (1) lift D-02 surgically for a one-line filter change in api.rs, OR (2) defer to 26.2.12 (the same phase that owns deferred server-side delete) and ship 26.2.1 with the SESSIONS wedge documented as web-process-only

# ============================================================================
# ROUND 3 GAPS (surfaced during re-verify after Plan 13 landed)
# ============================================================================

- truth: "Toggling SCANLINES off in the Tweaks panel hides the scanline overlay (the rule `body.no-scanlines .scanlines { display: none !important; }` actually engages)"
  status: closed
  closed_by: "Plan 26.2.1-14 Task 2 (commit 6c334fe4) — Branch (c) escalation: specificity (0,2,1)→(0,2,2) via `html` prefix + visibility+opacity triple-guard. Live DOM diagnostic in Task 1 (commit ac87831a) confirmed Branch (c) after ruling out branches (a) and (b)."
  reason: "User reported during round-3 re-verify of Test 6 (post Plan 13 commit 61930b11): 'fail scanlines still on after off selected'. Plan 13 added `!important` to the rule per the GAP-07 diagnosis, but the overlay still paints when toggle is OFF. All other body-class toggles (BREADCRUMB / FOOTER / RAIL / DENSITY) continue to work — the signal/effect path is sound."
  severity: minor
  test: 6
  round: 3
  root_cause: "Unknown post-Plan-13 — needs LIVE DevTools diagnostic. Plan 13 fix added `!important` to `body.no-scanlines .scanlines { display: none !important; }` at site.css:291, and `theme_effects.rs:80` calls `body.class_list().toggle_with_force('no-scanlines', !p.scanlines)`. Hud chrome renders `<div class='scanlines' aria-hidden='true'>` at hud_chrome.rs:20. Other toggles using the same effect work, so either: (a) the toggle for the scanlines signal specifically isn't propagating (unlikely — all body classes are written in the same `use_effect` block), (b) `<body>` actually has `no-scanlines` but `.scanlines` element is rendered outside `<body>` (e.g., portal'd) so the descendant selector misses, (c) a higher-specificity rule wins despite `!important` (e.g., a `.scanlines.is-on { display: block !important; }` somewhere, OR an inline style attribute set by some other effect), (d) the `.scanlines` element has additional classes like `.scanlines.scanlines--active` and a more-specific rule overrides. Plan 14 must: open DevTools → toggle SCANLINES off → inspect `document.body.classList` (must contain `no-scanlines`); inspect `getComputedStyle($('.scanlines')).display` (must be `none`); if not, run `$('.scanlines').matches('body.no-scanlines .scanlines')` and `getMatchedCSSRules` (or use 'Computed' panel) to identify the winning rule."
  artifacts:
    - path: crates/iron_hermes_ui/assets/site.css
      issue: "Lines 288-291: `.scanlines { opacity: 0.35; }` (line 289), `body.no-scanlines .scanlines { display: none !important; }` (line 291) — verify nothing else with higher specificity reasserts display"
    - path: crates/iron_hermes_ui/assets/site.css
      issue: "Line 85: original `.scanlines { … }` rule — verify whether `display` is asserted there with `!important`"
    - path: crates/iron_hermes_ui/src/components/hermes_app/hud_chrome.rs
      issue: "Line 20: `<div class='scanlines' aria-hidden='true'>` — verify no additional classes are added at runtime"
    - path: crates/iron_hermes_ui/src/components/hermes_app/theme_effects.rs
      issue: "Lines 70-95: confirmed correct — same effect handles all body class toggles, others work"
  missing:
    - "Live DevTools diagnostic in Plan 14: capture body.classList state when toggle is OFF, capture getComputedStyle('.scanlines').display, identify the actual winning CSS rule"
    - "Apply the right surgical fix once root cause is known (likely a one-line CSS edit at higher specificity, OR a Rust class-name correction)"
    - "Re-verify Test 6: SCANLINES toggle off → overlay disappears; toggle on → returns"
  follow_up_phase: Plan 26.2.1-14 (gap closure round 3)

- truth: "Sessions wedge lists ONLY loadable iron_hermes sessions (clicking a row loads the discussion successfully)"
  status: partially-closed
  closed_by: "Plan 26.2.1-14 Task 3 (commit 5f1a9f2a) — `.filter(|s| s.message_count > 0)` inserted into the api.rs list_sessions iterator chain per D-26.2.1-14-B option (i). The filter excludes foreign-format directories whose SQLite sessions row has `message_count == 0`. RESIDUAL: at least one foreign-format directory (e.g. `03057b90-2e1b-4944-b75c-7914ea6a60a1/`, on-disk content only `trajectories.jsonl`) has a non-zero message_count (15 msgs reported) so it bypasses the filter and remains unloadable. User-approved ship 2026-05-14; deeper fix deferred to phase 26.2.12. See GAP-26.2.1-09-R3-RESIDUAL below."
  reason: "User reported during round-3 re-verify of Test 9 (post Plan 13 commit 3efe0868): 'test 9 shows .ironhermes/sessions/ but the session dir is not loaded to continue the disscussion'. User pasted `ls` of `~/.ironhermes/sessions/03057b90-2e1b-4944-b75c-7914ea6a60a1/` showing only `trajectories.jsonl` (foreign agent format — not iron_hermes message history). The D-02 surgical lift correctly broadened the listing, but listability != loadability."
  severity: major
  test: 9
  round: 3
  user_choice: "(A) Filter to loadable sessions (Recommended) — keeps D-02 narrow-gate spirit."
  root_cause: "Plan 13's D-02 surgical lift (api.rs:60-61, `Some(&Platform::Web)` → `None`) was correct for broadening visibility, but the SQLite `sessions` table contains rows from any source/platform that ever wrote to it, including agent runs whose actual message history was stored as flat `trajectories.jsonl` files (foreign agent format, possibly an earlier orchestrator or a different tool sharing `~/.ironhermes/`). When the user clicks one of those rows, the chat-resume path queries SQLite `messages` for that session_id and gets zero rows (or fails to load because the foreign format never wrote SQLite messages at all), so the chat does not populate. Three filtering candidates: (i) `message_count > 0` — cheapest, uses an existing column on the sessions table; rejects sessions that never wrote SQLite messages. (ii) Allowlist by `source` column (e.g., `'cli' | 'web' | 'telegram'`) — explicit but requires knowing every loadable source. (iii) JOIN-based — require at least one row in `messages` for the session_id; safest but more SQL. Recommend (i) `message_count > 0` AND optionally `(source = 'web' OR exists(select 1 from messages where session_id = sessions.id limit 1))` to be doubly safe."
  artifacts:
    - path: crates/iron_hermes_ui/src/server/api.rs
      issue: "Lines 60-67: list_sessions currently passes `None` for source filter; need to refine to a loadability filter (option (i) message_count > 0, or option (iii) JOIN-based existence check). Stays within the api.rs-exempted D-02 narrow gate."
    - path: crates/ironhermes-state/src/lib.rs
      issue: "Line 523-560: existing `list_sessions_filtered` does NOT filter by message_count — Plan 14 either adds a new state-store method `list_loadable_sessions(limit)` OR filters client-side in api.rs (cheaper, no state crate change)"
    - path: ~/.ironhermes/sessions/
      issue: "74 entries on disk; subset are foreign-format `trajectories.jsonl` only — those must be excluded from the SESSIONS wedge"
  missing:
    - "Decide filter implementation: (a) post-filter in api.rs by `s.message_count > 0` (simplest), (b) add `list_loadable_sessions(limit)` to state crate that filters in SQL (cleaner), or (c) JOIN-based existence check on messages table"
    - "Implement the chosen filter in `crates/iron_hermes_ui/src/server/api.rs::list_sessions` — D-02 narrow gate already exempts api.rs; this refines the same line family"
    - "Re-verify Test 9 after fix: SESSIONS wedge shows only loadable sessions; clicking a row populates the chat thread; non-iron_hermes-format directories are absent from the list"
  follow_up_phase: Plan 26.2.1-14 (gap closure round 3) — paired with GAP-07-R3

# ============================================================================
# ROUND 4 RESIDUAL (surfaced during Plan 14 live DOM diagnostic — user-deferred)
# ============================================================================

- truth: "SESSIONS wedge excludes ALL non-loadable foreign-format session directories, including those with non-zero SQLite message_count"
  id: GAP-26.2.1-09-R3-RESIDUAL
  status: deferred
  deferred_to: phase 26.2.12
  reason: "Plan 14's `message_count > 0` filter (commit 5f1a9f2a) excludes the easy case (foreign-format directories whose SQLite sessions row reports message_count == 0). But the live DOM diagnostic the user performed for Test 6 also surfaced that at least one foreign-format directory (`~/.ironhermes/sessions/03057b90-2e1b-4944-b75c-7914ea6a60a1/`, on-disk content: only `trajectories.jsonl`) has a NON-ZERO message_count (15 msgs reported by the SQLite row). The `message_count > 0` filter therefore does NOT catch this row, and clicking it still fails to load (the chat-resume path cannot read the `trajectories.jsonl` foreign format). User decision 2026-05-14: keep the Plan 14 filter (it does close the easy cases), ship phase 26.2.1, and defer the proper session-key-format filter (or a source allowlist, or a chat-load-path adapter for `trajectories.jsonl`) to phase **26.2.12** — the same phase that owns the deferred server-side session delete."
  severity: major
  test: 9
  round: 4
  user_choice: "Ship Plan 14 with this residual; defer the deeper fix to 26.2.12 (verbatim user instruction, 2026-05-14)."
  root_cause: "The SQLite `sessions` table has a foreign-format row whose `message_count` is populated to a non-zero value (likely by an earlier agent or a different orchestrator sharing `~/.ironhermes/sessions/`). The Plan 14 filter is a coarse heuristic (assumes message_count == 0 iff foreign-format); the heuristic fails when the foreign-format directory also wrote `message_count` to SQLite without writing matching `messages` rows of iron_hermes shape. Three candidate fixes for 26.2.12: (a) filter by `source` column allowlist (`'web' | 'cli' | 'telegram' | …`); (b) JOIN-based existence check requiring at least one row in the `messages` table with matching session_id AND the iron_hermes message shape (role/content/etc.); (c) make the chat-load path adapt `trajectories.jsonl` so foreign-format sessions become loadable (broadest scope — likely belongs to a different phase)."
  artifacts:
    - path: crates/iron_hermes_ui/src/server/api.rs
      note: "Plan 14 filter `.filter(|s| s.message_count > 0)` remains in place — it closes the easy case. 26.2.12 will tighten further."
    - path: ~/.ironhermes/sessions/03057b90-2e1b-4944-b75c-7914ea6a60a1/
      note: "Reference case — foreign-format directory (only `trajectories.jsonl`) with non-zero SQLite message_count that bypasses the Plan 14 filter and remains unloadable in the SESSIONS wedge"
  follow_up_phase: 26.2.12 (paired with deferred server-side session delete per VERIFICATION.md)

# ============================================================================
# ROUND 4 GAP (Plan 15 feature removal — 2026-05-14)
# ============================================================================

- truth: "SCANLINES toggle visually engages on/off"
  status: closed_by_removal
  id: GAP-26.2.1-07-R3-FEATURE-REMOVAL
  disposition: GAP-26.2.1-07-R3-FEATURE-REMOVAL
  reason: "After 4 failed fix attempts (Plan 10 added `body.no-scanlines .scanlines { display: none }`; Plan 13 added `!important`; Plan 14 Branch (c) escalated to `html body.no-scanlines .scanlines { display: none !important; visibility: hidden !important; opacity: 0 !important; }` at specificity (0,2,2) with triple-guard — none engaged in UAT despite the Plan 14 live DOM diagnostic confirming `body.classList.contains('no-scanlines')` was true and the `.scanlines` element existed as a direct body child), the user decision 2026-05-14 was to remove the scanlines feature entirely rather than continue iterating."
  severity: minor
  test: 6
  closure_path: "Plan 15 (this plan) deletes the scanlines surface across 7 source files: site.css (3 rule blocks deleted), hud_chrome.rs (1 RSX element deleted), ui_prefs.rs (1 field deleted + serde migration via D-26.2.1-15-C), theme_effects.rs (1 toggle_with_force call deleted), tweaks_panel.rs (1 toggle row deleted), screens/settings.rs (1 field-row + 2 tuple slots deleted), mod.rs (regression test replaced with removal-stays-removed guard)."
  user_decision: "2026-05-14 — Plan 15 is the removal plan. Stop trying to fix the toggle."
  artifacts:
    - path: crates/iron_hermes_ui/assets/site.css
      issue: "REMOVED — no `.scanlines` selectors remain"
    - path: crates/iron_hermes_ui/src/components/hermes_app/hud_chrome.rs
      issue: "REMOVED — no `<div class='scanlines'>` element remains"
    - path: crates/iron_hermes_ui/src/ui_prefs.rs
      issue: "REMOVED — no `pub scanlines: bool` field; legacy localStorage blob migration via D-26.2.1-15-C (serde default `deny_unknown_fields = false` posture silently ignores legacy `scanlines` key)"
  missing: []
