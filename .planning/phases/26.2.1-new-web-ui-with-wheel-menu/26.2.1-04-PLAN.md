---
phase: 26.2.1
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - crates/iron_hermes_ui/src/components/hermes_app/wheel.rs
  - crates/iron_hermes_ui/src/components/hermes_app/wheel_rail.rs
  - crates/iron_hermes_ui/src/components/hermes_app/mod.rs
autonomous: true
decisions: [D-10, D-11, D-12, D-17, D-26]
tags: [dioxus, rust, wasm, svg, wheel, pointer-events, drag, resize]

must_haves:
  truths:
    - "The wheel SVG renders 10 wedges + 10 separators + 60 ticks + hub + resize ring inside HermesApp (10 wedges per CONTEXT D-10 / wheel-v2.js DEFAULT_SECTIONS)"
    - "Wheel geometry constants exactly match wheel-v2.js: SIZE=380.0, R_OUTER=190.0, R_INNER=78.0, R_LABEL=148.0, R_GLYPH=118.0, RING_GAP=14.0, PAD=22.0; MIN_SIZE=240.0, MAX_SIZE=640.0"
    - "Wedges are emitted in CONTEXT D-10 order: chat, agents, models, tools, skills, memory, sessions, providers, gateway, settings"
    - "Hover on a wedge sets WheelState.active_wedge; click on a wedge OR click on the hub sets active_screen to the corresponding Screen"
    - "Drag-rim translates the wheel-shell via WheelState.position, clamped to viewport with margin = 12 + RING_GAP + 7 (per Pitfall 3 conservative margin)"
    - "Drag-resize-ring rescales the wheel via WheelState.size, clamped between MIN_SIZE and MAX_SIZE"
    - "Window-level pointermove + pointerup listeners are installed via a Signal<Option<Closure>> slot + use_drop teardown (PATTERNS warp_hermes:497-552 idiom — NOT Box::leak)"
    - "WheelRail renders a docked panel beneath the wheel; visible only when `body.has-rail` is set (which Plan 05 toggles)"
  artifacts:
    - path: "crates/iron_hermes_ui/src/components/hermes_app/wheel.rs"
      provides: "Wheel SVG primitive + module-level geometry consts + helper fns + pointer handlers"
      contains: "pub const SIZE: f64"
      min_lines: 250
    - path: "crates/iron_hermes_ui/src/components/hermes_app/wheel_rail.rs"
      provides: "Docked wheel rail panel"
      contains: "pub fn WheelRail"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/mod.rs"
      provides: "Declares `pub mod wheel; pub mod wheel_rail;` and mounts `wheel::Wheel {} wheel_rail::WheelRail {}` at the Plan 04 insertion point"
  key_links:
    - from: "crates/iron_hermes_ui/src/components/hermes_app/wheel.rs"
      to: "crates/iron_hermes_ui/src/state.rs"
      via: "use_context::<Signal<WheelState>>() reads/writes position + size + active_wedge"
      pattern: "Signal<WheelState>"
    - from: "crates/iron_hermes_ui/src/components/hermes_app/wheel.rs"
      to: "web_sys::PointerEvent"
      via: "wasm_bindgen::Closure registering window pointermove/pointerup listeners via use_effect"
      pattern: "pointermove"
    - from: "crates/iron_hermes_ui/src/components/hermes_app/wheel.rs"
      to: "crates/iron_hermes_ui/src/state.rs"
      via: "Wedge click sets Signal<Screen> via WheelWedge::to_screen()"
      pattern: "active_screen.set"

user_setup: []
---

<objective>
Port `wheel-v2.js` into Rust/Dioxus 0.7 as a self-contained `wheel.rs` module. This is the highest-risk piece in the phase because no existing pattern in the crate handles SVG generation + window-scoped pointer events. The plan locks down RESEARCH Pattern 2 (SVG-in-RSX), Pattern 3 (drag/resize), and the PATTERNS analog `warp_hermes.rs:497-552` (`listener_slot: Signal<Option<Closure>>` + `use_drop` cleanup — NOT `Box::leak`).

Output is two new modules plus a 2-line mod.rs insertion at the Plan 04 marker:

1. `wheel.rs` — module-level geometry consts (SIZE, R_OUTER, etc.), helper fns (`polar`, `wedge_path`), `DragState` / `ResizeState` types, and `#[component] pub fn Wheel() -> Element` rendering the full SVG (rim + ticks + wedges + separators + glyph/label text + inner ring + hub + arrow + resize ring + tooltip).

2. `wheel_rail.rs` — `#[component] pub fn WheelRail() -> Element` rendering the docked variant beneath the wheel (D-17). Position is computed from `WheelState` (RESEARCH Pitfall 9) — declaratively, no observers needed.

3. Edit `hermes_app/mod.rs` at the Plan 04 insertion marker to: (a) add `pub mod wheel; pub mod wheel_rail;` to the submodule declarations, (b) replace the `// Plan 04 inserts: ...` comment with the actual `wheel::Wheel {} wheel_rail::WheelRail {}` rsx siblings.

Purpose: This delivers the visual centerpiece of the new UI. Clicking a wedge or the hub mutates `Signal<Screen>`, which Plan 03's ScreenRouter consumes to toggle the `is-active` class. Drag and resize mutate `Signal<WheelState>`, which Plan 03's hydration effect persists to `ih.ui.wheel` localStorage. After this plan ships, the new shell becomes interactive: you can navigate by clicking, move the wheel by dragging, and resize via the orange ring — all persisted across reloads.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-CONTEXT.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-RESEARCH.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-PATTERNS.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-UI-SPEC.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-VALIDATION.md

@crates/iron_hermes_ui/CLAUDE.md
@crates/iron_hermes_ui/AGENTS.md
@crates/iron_hermes_ui/clippy.toml
@crates/iron_hermes_ui/Cargo.toml

<!-- The canonical source for geometry + interaction -->
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/wheel-v2.js
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/wheel.css
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/app.html

<!-- The closest analog in tree for window-scoped event listeners -->
@crates/iron_hermes_ui/src/components/warp_hermes.rs

<!-- Plan 02 outputs that this plan consumes -->
@crates/iron_hermes_ui/src/state.rs
@crates/iron_hermes_ui/src/ui_prefs.rs

<interfaces>
After Plan 02:
```rust
crate::state::{Screen, WheelWedge, WheelState}
crate::state::WheelWedge::{index, from_index, label, sub, glyph, to_screen}
```

After Plan 03, `hermes_app::mod.rs` provides `Signal<WheelState>` and `Signal<Screen>` via `use_context_provider`. This plan's Wheel reads/writes both.

Window-listener idiom (PATTERNS warp_hermes.rs:497-552 — the in-tree blessed pattern, NOT RESEARCH Pattern 3's Box::leak example):

```rust
#[cfg(target_arch = "wasm32")]
{
    use web_sys::PointerEvent as WebPointerEvent;
    use wasm_bindgen::closure::Closure;
    use wasm_bindgen::JsCast;

    let mut move_slot: Signal<Option<Closure<dyn FnMut(WebPointerEvent)>>> = use_signal(|| None);
    let mut up_slot:   Signal<Option<Closure<dyn FnMut(WebPointerEvent)>>> = use_signal(|| None);

    use_effect(move || {
        let Some(window) = web_sys::window() else { return; };
        let move_cb = Closure::<dyn FnMut(WebPointerEvent)>::new(move |e: WebPointerEvent| {
            // pointermove handler (see Pattern 3 body)
        });
        let _ = window.add_event_listener_with_callback("pointermove", move_cb.as_ref().unchecked_ref());
        move_slot.set(Some(move_cb));

        let up_cb = Closure::<dyn FnMut(WebPointerEvent)>::new(move |_e: WebPointerEvent| {
            dragging.set(None);
            resizing.set(None);
        });
        let _ = window.add_event_listener_with_callback("pointerup", up_cb.as_ref().unchecked_ref());
        up_slot.set(Some(up_cb));
    });

    use_drop(move || {
        if let Some(cb) = move_slot.write().take() {
            if let Some(window) = web_sys::window() {
                let _ = window.remove_event_listener_with_callback("pointermove", cb.as_ref().unchecked_ref());
            }
        }
        if let Some(cb) = up_slot.write().take() {
            if let Some(window) = web_sys::window() {
                let _ = window.remove_event_listener_with_callback("pointerup", cb.as_ref().unchecked_ref());
            }
        }
    });
}
```

Geometry table (port verbatim from wheel-v2.js lines 26-37):

| const | value | source |
|-------|-------|--------|
| SIZE | 380.0 | wheel-v2.js |
| R_OUTER | 190.0 | wheel-v2.js (= SIZE/2) |
| R_INNER | 78.0 | wheel-v2.js |
| R_LABEL | 148.0 | wheel-v2.js |
| R_GLYPH | 118.0 | wheel-v2.js |
| RING_GAP | 14.0 | wheel-v2.js |
| RING_W | 4.0 | wheel-v2.js |
| PAD | 22.0 | wheel-v2.js |
| VB | SIZE + PAD*2 = 424.0 | derived |
| N | 10 | per CONTEXT D-10 / wheel-v2.js DEFAULT_SECTIONS (10 wedges) |
| STEP | 360.0 / N = 36.0 | derived |
| MIN_SIZE | 240.0 | wheel-v2.js line 426 + Pitfall 4 |
| MAX_SIZE | 640.0 | wheel-v2.js |
| DRAG_MARGIN | 33.0 (= 12 + RING_GAP + 7, RESEARCH Pitfall 3 conservative) | derived |

WheelRail position derivation (RESEARCH Pitfall 9):
```rust
let left  = wheel_state.read().position.0;
let top   = wheel_state.read().position.1 + wheel_state.read().size + 18.0;
let width = wheel_state.read().size.max(280.0);
let style = format!("left: {left}px; top: {top}px; width: {width}px;");
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement Wheel SVG + module-level geometry + pointer drag/resize listeners</name>
  <files>
    crates/iron_hermes_ui/src/components/hermes_app/wheel.rs
  </files>
  <read_first>
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/wheel-v2.js,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/wheel.css,
    crates/iron_hermes_ui/src/state.rs,
    crates/iron_hermes_ui/src/components/warp_hermes.rs,
    crates/iron_hermes_ui/CLAUDE.md,
    crates/iron_hermes_ui/AGENTS.md,
    crates/iron_hermes_ui/clippy.toml,
    .planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-RESEARCH.md,
    .planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-PATTERNS.md
  </read_first>
  <action>
    Create `crates/iron_hermes_ui/src/components/hermes_app/wheel.rs`.

    Module-level constants (port from wheel-v2.js):

      pub const SIZE: f64 = 380.0;
      pub const R_OUTER: f64 = 190.0;
      pub const R_INNER: f64 = 78.0;
      pub const R_LABEL: f64 = 148.0;
      pub const R_GLYPH: f64 = 118.0;
      pub const RING_GAP: f64 = 14.0;
      pub const RING_W: f64 = 4.0;
      pub const PAD: f64 = 22.0;
      pub const VB: f64 = SIZE + PAD * 2.0;
      pub const N: usize = 10; // CONTEXT D-10: 10 wedges
      pub const STEP: f64 = 360.0 / N as f64; // = 36.0
      pub const MIN_SIZE: f64 = 240.0;
      pub const MAX_SIZE: f64 = 640.0;
      pub const DRAG_MARGIN: f64 = 33.0;

    Module-level helper fns:

      pub fn polar(ang_deg: f64, r: f64) -> (f64, f64) {
          let a = (ang_deg - 90.0).to_radians();
          (a.cos() * r, a.sin() * r)
      }

      pub fn wedge_path(ang_a: f64, ang_b: f64, r_inner: f64, r_outer: f64) -> String { /* see RESEARCH Pattern 2 */ }

    Module-level types:

      #[derive(Clone, Copy)]
      struct DragState { start_client: (f64, f64), start_pos: (f64, f64), pointer_id: i32, dist: f64 }
      #[derive(Clone, Copy)]
      struct ResizeState { start_client: (f64, f64), start_size: f64, pointer_id: i32 }

    Component:

      #[component] pub fn Wheel() -> Element

    Inside Wheel:
    1. Read context signals: `let mut wheel = use_context::<Signal<crate::state::WheelState>>(); let mut active_screen = use_context::<Signal<crate::state::Screen>>();`.
    2. Define local signals: `let mut dragging = use_signal(|| Option::<DragState>::None); let mut resizing = use_signal(|| Option::<ResizeState>::None); let mut tooltip_open = use_signal(|| false); let mut tooltip_resize = use_signal(|| false); let mut tooltip_xy = use_signal(|| (0.0_f64, 0.0_f64));`.
    3. Read WheelState into locals BEFORE the rsx block to release the signal borrow (clippy.toml signal-borrow rules):
       ```
       let (px, py) = { let s = wheel.read(); s.position };
       let size = wheel.read().size;
       let active = wheel.read().active_wedge.index();
       let viewbox_min = -(R_OUTER + PAD);
       let viewbox = format!("{viewbox_min} {viewbox_min} {VB} {VB}");
       ```
    4. Install window-level pointermove/pointerup listeners inside a `#[cfg(target_arch = "wasm32")]` block using the `listener_slot: Signal<Option<Closure>>` idiom from PATTERNS warp_hermes.rs:497-552. The pointermove handler must:
       - If `*dragging.read()` is Some, compute new position from `e.client_x()` / `e.client_y()` minus drag start, clamp x to `[DRAG_MARGIN, window.inner_width - shell_size - DRAG_MARGIN]` and y similarly, then `wheel.with_mut(|s| s.position = (new_x, new_y));`.
       - If `*resizing.read()` is Some, compute `delta = (dx + dy) / 2.0`, new_size = clamp(start_size + delta, MIN_SIZE, MAX_SIZE), `wheel.with_mut(|s| s.size = new_size);`.
       - Track `d.dist` = max distance for click-suppression (wheel-v2.js parity).
       Window inner_width/inner_height fall back to 1920.0 / 1080.0 if unavailable.
    5. `use_drop` cleanup removes both listeners using each slot's stored `Closure` reference (PATTERNS analog).
    6. RSX tree per RESEARCH Pattern 2 — this is the heavy lift, ~200 lines of SVG. Render:
       - Outer `div { class: "wheel-shell", id: "wheel-shell", style: "--wheel-size: {size}px; left: {px}px; top: {py}px;", ... }`. The `--wheel-size` CSS var drives `wheel.css` sizing rules.
       - Inner `svg { class: "wheel-svg", id: "wheel-svg", "viewBox": "{viewbox}", ... }`.
       - `defs` with two `radialGradient` blocks (`hub-grad`, `hub-grad-hover`) — values verbatim from RESEARCH Pattern 2 / wheel-v2.js.
       - Rim decoration rings (2 `circle` elements, static).
       - 60 tick marks (`g { id: "wheel-ticks", "pointer-events": "none", for i in 0..60 { line { ... } } }`).
       - `path { id: "wheel-rim", class: "wheel-rim", ... }` — the invisible annulus hit-area for drag. The `d` attribute is the two-ring path from RESEARCH Pattern 2. Wire `onpointerdown` to start a drag (push DragState into the signal). Wire `onmouseenter` / `onmouseleave` / `onmousemove` to drive the tooltip signals.
       - 10 wedges via `for i in 0..N { ... }` (N = 10 per D-10). Each wedge is a `path` with `class: "wheel-wedge", class: if i == active { "is-active" }, "data-i": "{i}", d: "{d}", onmouseenter: ..., onclick: ...`. The onmouseenter writes `wheel.active_wedge = WheelWedge::from_index(i)`. The onclick writes the wedge AND sets `active_screen` to `WheelWedge::from_index(i).to_screen()` (Pitfall 6 ordering: write wedge first, compute target, set screen).
       - 10 separator lines (`g { id: "wheel-seps", "pointer-events": "none", for i in 0..N { ... } }`).
       - 20 glyph + label `text` nodes (`g { id: "wheel-text", "pointer-events": "none", for i in 0..N { text { ... glyph } text { ... label } } }`) — N=10 wedges × 2 text nodes per wedge = 20. Use `WheelWedge::from_index(i).glyph()` and `.label()` for content.
       - Inner divider circle (static).
       - Hub group `g { id: "wheel-hub", class: "wheel-hub", onclick: move |_| { let target = wheel.read().active_wedge.to_screen(); active_screen.set(target); }, ... }` containing hub-bg circle + 2 decoration circles + 4 text nodes (hub-glyph, hub-label, hub-sub, hub-cta) — content is `WheelWedge::from_index(active).label() / .sub()`.
       - Hub arrow polygon.
       - Resize ring (3 nested circles `resize-glow`, `resize-orbit`, `resize-ring`). The `resize-ring` circle has `onpointerdown: move |e| { let c = e.client_coordinates(); let start_size = wheel.read().size; resizing.set(Some(ResizeState { start_client: (c.x, c.y), start_size, pointer_id: e.pointer_id() })); e.stop_propagation(); }`.
       - Floating tooltip div (per RESEARCH Pattern 2's tooltip block).
    7. ALL hyphenated SVG attribute names (`stroke-width`, `text-anchor`, `pointer-events`, `data-i`, `font-size`, `letter-spacing`, `font-family`, `font-weight`, `stroke-dasharray`, `fill-rule`, `viewBox`, `stop-color`, `aria-hidden`) MUST be QUOTED as `"stroke-width": "1"` (RESEARCH Pitfall 1).
    8. Signal-borrow safety: in every onclick / onmouseenter / pointer handler, read into locals first, drop the borrow, then `.set()` or `.with_mut()`. Do NOT hold `wheel.read()` across `active_screen.set()`. Patterns in warp_hermes.rs lines 585-614 show the discipline.

    Anti-patterns forbidden in this task:
    - `Box::leak` for closure lifetime (RESEARCH Pattern 3 suggests it but PATTERNS says no — use `listener_slot: Signal<Option<Closure>>` + `use_drop`).
    - `use_state`, `cx`, `Scope`.
    - Computing wedge_path / polar inside the `rsx!` body (they must be helper fns at module scope — RESEARCH "Compute wheel geometry inside `rsx!` body" anti-pattern).
    - Unquoted hyphenated SVG attributes.
    - Holding `wheel.read()` borrow across `.set()` on `active_screen` (clippy will reject).
    - Calling `e.target.set_pointer_capture(...)` — Pitfall 2 notes the cast is awkward in Dioxus event handlers; window-level binding already solves the problem.
    - Touching `src/server/*.rs` or `src/protocol.rs` (D-02).

    Also add a `#[cfg(test)] mod tests` block with:
    - `#[test] fn geometry_matches_v2_constants()` asserting SIZE == 380.0, R_OUTER == 190.0, R_INNER == 78.0, R_LABEL == 148.0, R_GLYPH == 118.0, RING_GAP == 14.0, PAD == 22.0, N == 10 (per D-10), STEP == 36.0, MIN_SIZE == 240.0, MAX_SIZE == 640.0.
    - `#[test] fn polar_at_zero_is_top()` — `polar(0.0, 100.0)` returns approximately `(0.0, -100.0)` (within 1e-9). The -90° rotation in polar puts angle 0 at the top.
    - `#[test] fn wedge_path_starts_with_m()` — `wedge_path(0.0, 36.0, 78.0, 190.0)` starts with `"M "` (one full wedge sweep at STEP=36.0).
  </action>
  <verify>
    <automated>cargo test -p iron_hermes_ui --lib wheel:: 2>&amp;1 | tail -20 &amp;&amp; cargo clippy -p iron_hermes_ui --all-features -- -D warnings 2>&amp;1 | tail -30 &amp;&amp; cargo clippy -p iron_hermes_ui --target wasm32-unknown-unknown --no-default-features --features web -- -D warnings 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `crates/iron_hermes_ui/src/components/hermes_app/wheel.rs` with at least 250 source lines (the SVG is large; an undersized file means the wedge/tick/label loops are missing)
    - Source contains all geometry constants byte-for-byte: `pub const SIZE: f64 = 380.0;`, `pub const R_OUTER: f64 = 190.0;`, `pub const R_INNER: f64 = 78.0;`, `pub const R_LABEL: f64 = 148.0;`, `pub const R_GLYPH: f64 = 118.0;`, `pub const RING_GAP: f64 = 14.0;`, `pub const PAD: f64 = 22.0;`, `pub const N: usize = 10;` (CONTEXT D-10 / wheel-v2.js DEFAULT_SECTIONS — 10 wedges), `pub const MIN_SIZE: f64 = 240.0;`, `pub const MAX_SIZE: f64 = 640.0;`, `pub const DRAG_MARGIN: f64 = 33.0;`
    - Source contains literal `pub fn polar(ang_deg: f64, r: f64) -> (f64, f64)` and literal `pub fn wedge_path` (module-scope helpers, NOT inside rsx)
    - Source contains literal `Signal<Option<Closure<dyn FnMut(WebPointerEvent)>>>` (the listener slot idiom from PATTERNS — NOT Box::leak)
    - Source contains literal `use_drop` (cleanup of window listeners is mandatory)
    - Source contains literal `add_event_listener_with_callback("pointermove"` and `add_event_listener_with_callback("pointerup"` (window-level binding per Pitfall 2)
    - Source contains literal `WheelWedge::from_index` (used in onclick / onmouseenter handlers and text loops)
    - Source contains literal `to_screen()` (wheel click → screen change)
    - Source contains a doc-comment or const annotation with the canonical 10-wedge string `chat, agents, models, tools, skills, memory, sessions, providers, gateway, settings` so grep can lock D-10 source-of-truth in a single byte-anchor
    - Source contains all 12 hyphenated SVG attrs quoted: `"stroke-width"`, `"text-anchor"`, `"pointer-events"`, `"data-i"`, `"font-size"`, `"letter-spacing"`, `"font-family"`, `"font-weight"`, `"stroke-dasharray"`, `"fill-rule"`, `"stop-color"`, `"viewBox"` — each MUST appear as a quoted attribute name in the source (Pitfall 1 enforcement)
    - Source negative assertion: does NOT contain `Box::leak` (PATTERNS forbids this for production closures)
    - Source negative assertion: does NOT contain `use_state\|\bcx:\|\bScope\b` (Dioxus 0.6 — removed in 0.7)
    - Source negative assertion: does NOT contain `dangerous_inner_html`
    - Behavior: `cargo test -p iron_hermes_ui --lib wheel::tests::geometry_matches_v2_constants` passes
    - Behavior: `cargo test -p iron_hermes_ui --lib wheel::tests::polar_at_zero_is_top` passes
    - Behavior: `cargo clippy -p iron_hermes_ui --all-features -- -D warnings` exits 0 (no signal-borrow-across-await violations, no unused warnings)
    - Behavior: `cargo clippy -p iron_hermes_ui --target wasm32-unknown-unknown --no-default-features --features web -- -D warnings` exits 0 (B-06: WASM-target clippy lints the `#[cfg(target_arch = "wasm32")]` block including the signal-borrow rules inside the pointer handlers; host clippy alone cannot see this code)
  </acceptance_criteria>
  <done>
    Wheel SVG + window-level pointer handlers + tooltip + resize ring all land in `wheel.rs`. The geometry constants match wheel-v2.js exactly. Pointer event listeners use the in-tree blessed `listener_slot + use_drop` idiom (not Box::leak). All tests pass; clippy passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement WheelRail docked variant and wire wheel + wheel_rail into hermes_app/mod.rs</name>
  <files>
    crates/iron_hermes_ui/src/components/hermes_app/wheel_rail.rs,
    crates/iron_hermes_ui/src/components/hermes_app/mod.rs
  </files>
  <read_first>
    crates/iron_hermes_ui/src/components/hermes_app/mod.rs,
    crates/iron_hermes_ui/src/components/hermes_app/wheel.rs,
    crates/iron_hermes_ui/src/state.rs,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/app.html,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/wheel.css,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens.css,
    .planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-RESEARCH.md
  </read_first>
  <action>
    Create `crates/iron_hermes_ui/src/components/hermes_app/wheel_rail.rs`:

    1. `#[component] pub fn WheelRail() -> Element`. Read `let wheel_state = use_context::<Signal<crate::state::WheelState>>();`.
    2. Compute position style declaratively (RESEARCH Pitfall 9). Read into locals to drop the borrow before format:
       ```
       let (px, py, size) = { let s = wheel_state.read(); (s.position.0, s.position.1, s.size) };
       let left = px;
       let top = py + size + 18.0;
       let width = size.max(280.0);
       let style = format!("left: {left}px; top: {top}px; width: {width}px;");
       ```
    3. Emit:
       ```rust
       rsx! {
           aside {
               class: "wheel-rail",
               id: "wheel-rail",
               style: "{style}",
               "aria-hidden": "true",
               div { class: "rail-row",
                   span { class: "rail-pill", "RAIL" }
                   span { class: "rail-status", "DOCKED" }
               }
           }
       }
       ```
       Visibility is owned by `body.has-rail #wheel-rail { display: flex !important; }` (already in `wheel.css` from Plan 01 — Plan 05 toggles the body class via the UiPrefs `rail` field). The rail RSX is always mounted; CSS controls show/hide.
    4. The rail content (`rail-row` etc.) is intentionally minimal — D-17 only requires the layout primitive + reachability, NOT a wired chat sidebar. A future phase may add tabs/buttons to it.

    Edit `crates/iron_hermes_ui/src/components/hermes_app/mod.rs`:

    5. Add `pub mod wheel;` and `pub mod wheel_rail;` to the submodule declaration block (alongside `hud_chrome`, `breadcrumb`, etc. from Plan 03).
    6. Replace the `// Plan 04 inserts: wheel::Wheel {} wheel_rail::WheelRail {}` marker comment in the HermesApp rsx tree with the actual children — placement should be AFTER `app_footer::AppFooter {}` so the wheel renders on top of the chrome (CSS z-index handles the actual stack ordering, but later RSX siblings still take precedence on equal-z elements):
       ```rust
       app_footer::AppFooter {}
       wheel_rail::WheelRail {}
       wheel::Wheel {}
       // Plan 05 inserts: tweaks_panel::TweaksPanel {}
       ```
    7. Do NOT remove the `// Plan 05 inserts: ...` marker — Plan 05 needs it.
    8. Do NOT add any new context provider here — Plan 03 already established all four root signals.

    Anti-patterns forbidden in this task:
    - Re-providing any signal already provided by Plan 03's HermesApp body (use_context_provider for WheelState lives in mod.rs alone).
    - Mounting Wheel BEFORE HudChrome in the rsx tree (the wheel must render on top — same-z elements paint in source order, so wheel goes last).
    - Adding ResizeObserver / MutationObserver in WheelRail (RESEARCH Pitfall 9: signal-driven style replaces observers).
    - Touching `src/server/*.rs` or `src/protocol.rs` (D-02).
  </action>
  <verify>
    <automated>cargo check -p iron_hermes_ui 2>&amp;1 | tail -30 &amp;&amp; cargo clippy -p iron_hermes_ui --all-features -- -D warnings 2>&amp;1 | tail -30 &amp;&amp; cargo clippy -p iron_hermes_ui --target wasm32-unknown-unknown --no-default-features --features web -- -D warnings 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `crates/iron_hermes_ui/src/components/hermes_app/wheel_rail.rs`
    - Source contains literal `class: "wheel-rail"` and literal `id: "wheel-rail"`
    - Source contains literal `use_context::<Signal<crate::state::WheelState>>()`
    - Source contains literal `+ 18.0` (the rail-top offset per Pitfall 9 formula)
    - `crates/iron_hermes_ui/src/components/hermes_app/mod.rs` source contains literal `pub mod wheel;` AND literal `pub mod wheel_rail;`
    - `mod.rs` source contains literal `wheel::Wheel {}` AND literal `wheel_rail::WheelRail {}` inside the HermesApp rsx tree
    - `mod.rs` source still contains literal `Plan 05 inserts` marker (Plan 05 depends on it)
    - `mod.rs` source no longer contains `Plan 04 inserts` marker (replaced by the actual rsx children)
    - Behavior: `cargo check -p iron_hermes_ui` exits 0 (default build mounts the wheel)
    - Behavior: `cargo check -p iron_hermes_ui --features legacy-shell` exits 0
    - Behavior: `cargo clippy -p iron_hermes_ui --all-features -- -D warnings` exits 0
    - Behavior: `cargo clippy -p iron_hermes_ui --target wasm32-unknown-unknown --no-default-features --features web -- -D warnings` exits 0 (B-06: WASM-target clippy)
  </acceptance_criteria>
  <done>
    WheelRail renders beneath the wheel with declarative position derived from WheelState. HermesApp mounts both Wheel and WheelRail at the Plan 04 marker. Both feature builds compile clean; clippy passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Window event listeners ↔ Wheel component | wasm_bindgen closures registered against the global `window` object; the Wheel must release them on unmount to avoid leaks (HermesApp never unmounts, so this is a hygiene concern not a real exposure). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.2.1-11 | Denial of Service | Closure leak via Box::leak instead of listener_slot | mitigate | PATTERNS analog `warp_hermes.rs:497-552` mandates the `Signal<Option<Closure>>` + `use_drop` idiom. The action explicitly forbids `Box::leak`. Acceptance grep checks. |
| T-26.2.1-12 | Tampering | User-controlled `data-i` index in wedge click handler | accept | The `data-i` attribute is set by Rust (`"{i}"` from `for i in 0..N`); the click handler closes over `i` directly via the for-loop body. No user-controlled string ever flows into `WheelWedge::from_index` — only the loop index. |
| T-26.2.1-13 | DoS | Drag handler firing thousands of pointermove events per second | accept | Each pointermove writes once to `wheel` via `with_mut`; Dioxus coalesces consecutive signal writes within a single frame. No backpressure issue. |
| T-26.2.1-14 | Tampering | Resize ring orbit clipped by `body { overflow: hidden }` near viewport edges | mitigate | DRAG_MARGIN = 33.0 (= 12 + RING_GAP + 7) per RESEARCH Pitfall 3 conservative — the clamp keeps the entire resize ring inside the viewport. |
</threat_model>

<verification>
Per VALIDATION.md Sampling Rate:
- Per task: `cargo check -p iron_hermes_ui` (~30s, automated).
- Per wave merge: `cargo test -p iron_hermes_ui --lib` + `cargo clippy -p iron_hermes_ui --all-features -- -D warnings`.

Per VALIDATION.md Per-Task Verification Map TBD-03 / TBD-04: this plan satisfies D-10 (10 wedges labeled per wheel-v2.js DEFAULT_SECTIONS), D-11 (drag/resize via pointer events), and D-17 (wheel-rail variant).

Manual UAT additions for this plan (added to UAT-26.2.1.md in Plan 09):
- Load `/`; confirm the wheel SVG renders in the upper-left with **10 wedges** labeled CHAT, AGENTS, MODELS, TOOLS, SKILLS, MEMORY, SESSIONS, PROVIDER, GATEWAY, SYSTEM (per CONTEXT D-10) and a hub showing CHAT / INTELLIGENCE CONSOLE / ▸ LAUNCH.
- Hover each wedge; confirm the `is-active` class toggles (visual highlight) and the hub label updates.
- Click each wedge; confirm `Signal<Screen>` updates and the matching `<section id="screen-XXX">` gets `class:"is-active"` via Plan 03's ScreenRouter.
- Click the hub; confirm it launches the currently-hovered wedge.
- Drag the rim (the invisible annulus); confirm the wheel-shell translates. Drag fast across the screen — confirm the drag does NOT freeze (Pitfall 2 mitigation: window-level listeners).
- Drag the orange resize ring; confirm `--wheel-size` changes between 240 and 640.
- Refresh; confirm position + size persist (Plan 03's hydration gate restoring `ih.ui.wheel`).
</verification>

<success_criteria>
- `cargo check -p iron_hermes_ui` and `cargo check -p iron_hermes_ui --features legacy-shell` both exit 0
- `cargo clippy -p iron_hermes_ui --all-features -- -D warnings` exits 0
- `cargo clippy -p iron_hermes_ui --target wasm32-unknown-unknown --no-default-features --features web -- -D warnings` exits 0 (B-06: WASM clippy lints the pointer-event handlers and the rest of the `#[cfg(target_arch = "wasm32")]` block that host clippy never sees)
- Geometry constants in `wheel.rs` match `wheel-v2.js` lines 26-37 byte-for-byte
- 10 wedges render with the correct labels and glyphs (per D-10 / wheel-v2.js DEFAULT_SECTIONS — sourced from `WheelWedge::label` and `WheelWedge::glyph`)
- Wheel click and hub click both write to `Signal<Screen>` through `WheelWedge::to_screen()`
- Drag + resize listeners are window-bound via the `listener_slot` idiom with `use_drop` cleanup (NOT Box::leak)
- WheelRail position derives from `Signal<WheelState>` declaratively (no observers)
- `hermes_app/mod.rs` mounts Wheel and WheelRail at the Plan 04 marker location
</success_criteria>

<output>
After completion, create `.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-04-SUMMARY.md` capturing: exact geometry-const values shipped, list of all hyphenated SVG attribute quotes (Pitfall 1 audit), confirmation that listener cleanup uses use_drop (not Box::leak), drag clamp margin value used, and confirmation both feature builds + clippy pass.
</output>
