---
phase: 26.2.1
plan: 10
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - crates/iron_hermes_ui/assets/site.css
autonomous: true
requirements:
  - GAP-26.2.1-02-app-footer-chrome-missing
  - GAP-26.2.1-03-theme-and-body-class-effects-silent
user_setup: []

must_haves:
  truths:
    - "AppFooter chrome renders at page load: bottom-left shows NODE/SCREEN/AGENT, bottom-right shows MEM/SKILLS/P50 + UTC clock"
    - "Cycling all 5 themes (slate-dark, slate-light, iron-dark, terminal-dark, parchment-light) visibly flips the palette of the legacy-var-styled chrome (wheel, screens, panels, FAB, breadcrumb, footer, scanlines)"
    - "Toggling SCANLINES off in the Tweaks panel hides the .scanlines overlay"
    - "Toggling BREADCRUMB off in the Tweaks panel hides .breadcrumb AND .sys-meta"
    - "Toggling FOOTER off in the Tweaks panel hides .app-footer"
    - "Toggling RAIL on activates #wheel-rail display only when on the Chat screen (body.has-rail.on-chat)"
    - "Toggling DENSE adjusts .screen padding-top to 70px, .screen-title font-size to 28px, .card padding to 12px 14px, .panel padding to 14px 16px"
    - "After toggling, hard-reload preserves all visible chrome state through the existing localStorage round-trip (no Rust changes needed — already wired)"
  artifacts:
    - path: "crates/iron_hermes_ui/assets/site.css"
      provides: "Restored chrome positioning rules (.breadcrumb, .sys-meta, .app-footer, .app-footer-right, .app-footer .v, .app-footer .sep) AND 5 per-theme [data-theme] blocks reassigning the legacy --teal/--bg/--text/--border/--gray var family AND body-class display/layout toggles for no-scanlines, no-breadcrumb, no-footer, has-rail, density-dense, on-chat"
      contains: ".app-footer { position: fixed"
      contains_2: "[data-theme=\"iron-dark\"]"
      contains_3: "body.no-footer .app-footer"
  key_links:
    - from: "app_footer.rs DOM (<div class='app-footer'>…)"
      to: ".app-footer CSS rule in site.css"
      via: "matching class selector"
      pattern: "\\.app-footer\\s*\\{"
    - from: "theme_effects.rs Effect 2 (body.class_list toggle)"
      to: "body.no-scanlines/.no-breadcrumb/.no-footer/.has-rail/.density-dense rules in site.css"
      via: "CSS body-class selector cascade"
      pattern: "body\\.no-(scanlines|breadcrumb|footer)"
    - from: "theme_effects.rs Effect 1 (<html data-theme=…> attribute writer)"
      to: "[data-theme='<slug>'] { --teal: …; --bg: …; … } blocks in site.css"
      via: "[data-theme] attribute selector cascade onto legacy vars"
      pattern: "\\[data-theme=\""

reverification:
  closes_gaps:
    - "Gap 2 (UAT Test 2): AppFooter bottom-left + bottom-right chrome missing"
    - "Gap 3 (UAT Test 6): Theme cycling silent + SCANLINES/BREADCRUMB/FOOTER/RAIL/DENSE toggles silent"
  rerun:
    - "UAT-26.2.1.md §2 (Page Load & HUD Chrome) — items 1.9 through 1.13"
    - "UAT-26.2.1.md §6 (Themes & Tweaks Panel) — theme cycling + body-class toggle subsections"
---

<objective>
Restore the dropped chrome CSS (~70 lines from `filestoimport/ironhermes-design-system/project/app.html` lines 42-112) AND add 5 per-theme `[data-theme]` blocks that reassign the legacy `--teal/--bg/--text/--border/--gray` var family, so:

  1. AppFooter renders at page load (closes Gap 2 / UAT Test 2)
  2. The 5 themes visibly flip the palette of the chrome that is styled via legacy vars in `site.css` (closes the theme half of Gap 3)
  3. The body-class toggles in the Tweaks panel (SCANLINES / BREADCRUMB / FOOTER / RAIL / DENSE) visibly engage (closes the body-class half of Gap 3)

Purpose: Plan 01 silently dropped ~70 lines of chrome CSS during the asset copy, and Plan 03's claim that "CSS is supplied by assets/site.css (copied verbatim from the design bundle)" is false. Plan 03's `theme_effects.rs` writes `<html data-theme=…>` and `body.no-*` classes correctly, but the corresponding CSS selectors don't exist in any of the 10 loaded stylesheets, so the writes cascade into nothing.

Output: One edited file (`crates/iron_hermes_ui/assets/site.css`) with three new CSS chunks appended. Zero Rust changes. D-02 untouched. Plan 03's `theme_effects.rs` is **CORRECT AS WRITTEN — DO NOT MODIFY** (per UAT-26.2.1.md root_cause for Gap 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-CONTEXT.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-UAT.md

# Source-of-truth for the body-class + .app-footer chrome rules — copy verbatim
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/app.html

# Reference for per-theme palettes (token blocks lines 79, 114, 149, 184, 219)
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/tokens.css

# The destination file — currently missing the ~70 lines plus the 5 [data-theme] blocks
@crates/iron_hermes_ui/assets/site.css

# Rust code — DO NOT MODIFY, both files are correct as written
@crates/iron_hermes_ui/src/components/hermes_app/theme_effects.rs
@crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs

<interfaces>
The legacy var family declared in `site.css :root` (lines 7-40) — these are the vars that must be reassigned per-theme:

```css
:root {
  --bg:            #0d1117;
  --bg-deep:       #070a0e;
  --bg-card:       #161b22;
  --bg-card-hover: #1c2129;
  --bg-input:      #11161d;
  --border:        #30363d;
  --border-faint:  #1f242c;
  --border-strong: #404750;
  --teal:          #39c5cf;
  --teal-bright:   #56d4dd;
  --teal-dim:      rgba(57, 197, 207, 0.55);
  --teal-faint:    rgba(57, 197, 207, 0.15);
  --teal-glow:     rgba(57, 197, 207, 0.32);
  --text:          #e6edf3;
  --text-dim:      #9aa4ad;
  --gray:          #6e7681;
  --gray-faint:    rgba(110, 118, 129, 0.45);
  --green:         #3fb950;
  --red:           #f85149;
  --amber:         #d29922;
}
```

The slate-dark theme = the current `:root` values (no override needed, but a `[data-theme="slate-dark"]` block can be added for symmetry / explicitness).

Per-theme palette hints (synthesize hex equivalents aligned with tokens.css oklch palettes):
- slate-light: warm paper bg `#f5f3ee` / dark ink fg `#1d2228` / cyan accent kept (`#39c5cf` / `#56d4dd`); borders go light
- iron-dark: near-black bg `#0a0807` / molten-orange accent `#f0883e` / `#ff9a4a`; warm-tinted borders
- terminal-dark: pure black bg `#000000` / ANSI-green accent `#3fb950` / `#56e070`; green-tinted borders
- parchment-light: cream bg `#f4ecdc` / brass accent `#a67c2c` / `#c5984a`; warm sepia borders

The chrome rules from app.html lines 42-112 are the canonical body-class + position-fixed rules — copy verbatim. The CSS rules referenced by Plan 03's `theme_effects.rs::on_chat_class_effect` (the `body.on-chat #wheel-rail` interaction) are included in this same block — they were dropped together.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append chrome positioning rules and body-class toggle rules to site.css</name>
  <files>crates/iron_hermes_ui/assets/site.css</files>
  <action>
Open `crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/app.html` and copy the inline `<style>` block content from lines 42 through 112 (the `.breadcrumb`, `.sys-meta`, `.app-footer`, `.app-footer-right`, `.app-footer .v`, `.app-footer .sep`, `.scanlines { opacity: 0.35 }`, `.hud-grid { opacity: 0.35 }`, `body.no-scanlines .scanlines`, `body.no-breadcrumb .breadcrumb,.sys-meta`, `body.no-footer .app-footer`, `body.has-rail #wheel-rail`, `body:not(.has-rail) #wheel-rail, body:not(.on-chat) #wheel-rail`, `body.density-dense .screen`, `body.density-dense .screen-title`, `body.density-dense .card`, `body.density-dense .panel` rules).

Also copy lines 315-316 (`body.has-rail.on-chat #screen-chat { padding-left: 360px; }` and `body.has-rail.on-chat #screen-chat .screen-header { padding-left: 0; }`) and line 320 (`@media (max-width: 1100px) { #screen-chat { padding-left: 36px; padding-right: 20px; } body.has-rail.on-chat #screen-chat { padding-left: 300px; } }`).

Append all of the above VERBATIM to the end of `crates/iron_hermes_ui/assets/site.css` (preserve the original CSS — do not modify any existing rule). Add a single section-header comment line above the new block: `/* ─── Chrome positioning + body-class toggles (ported from app.html lines 42-112, 315-316, 320) ─── */`.

NO edits to the body-class names, no reorderings, no merging of selectors. The grep gates below verify byte-for-byte presence.

DO NOT modify any existing line in site.css. DO NOT touch any other CSS file. DO NOT touch any Rust file.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && grep -c "^\.app-footer\s*{" crates/iron_hermes_ui/assets/site.css | awk '{ if ($1 < 1) exit 1 }' && grep -q "body\.no-footer .app-footer" crates/iron_hermes_ui/assets/site.css && grep -q "body\.no-scanlines .scanlines" crates/iron_hermes_ui/assets/site.css && grep -q "body\.no-breadcrumb" crates/iron_hermes_ui/assets/site.css && grep -q "body\.has-rail" crates/iron_hermes_ui/assets/site.css && grep -q "body\.density-dense .screen" crates/iron_hermes_ui/assets/site.css && grep -q "body\.has-rail\.on-chat #screen-chat" crates/iron_hermes_ui/assets/site.css && grep -q "^\.breadcrumb\s*{" crates/iron_hermes_ui/assets/site.css && grep -q "^\.sys-meta\s*{" crates/iron_hermes_ui/assets/site.css && grep -q "\.app-footer-right" crates/iron_hermes_ui/assets/site.css</automated>
  </verify>
  <done>
All 10 grep assertions return matches. `.app-footer`, `.breadcrumb`, `.sys-meta`, `.app-footer-right`, and the six body-class rules (`no-scanlines`, `no-breadcrumb`, `no-footer`, `has-rail`, `density-dense`, `has-rail.on-chat`) are now present in site.css.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append 5 per-theme [data-theme] blocks reassigning the legacy var family</name>
  <files>crates/iron_hermes_ui/assets/site.css</files>
  <action>
Append a second new section to `crates/iron_hermes_ui/assets/site.css` (after the Task 1 block) with a section header `/* ─── Per-theme legacy-var overrides (aligns site.css legacy vars with tokens.css [data-theme] palettes) ─── */` followed by 5 `[data-theme="<slug>"] { … }` blocks.

For each theme block, reassign **every legacy var** that `site.css :root` declares (lines 7-40) — namely `--bg`, `--bg-deep`, `--bg-card`, `--bg-card-hover`, `--bg-input`, `--border`, `--border-faint`, `--border-strong`, `--teal`, `--teal-bright`, `--teal-dim`, `--teal-faint`, `--teal-glow`, `--text`, `--text-dim`, `--gray`, `--gray-faint`, `--green`, `--red`, `--amber` — to per-theme hex equivalents.

The 5 blocks (with anchor values aligned to `tokens.css` palettes; use these exact hex values as the synthesized per-theme legacy-var palette, NOT oklch — site.css consumers expect hex/rgba):

1. `[data-theme="slate-dark"]` — duplicate of `:root` values byte-for-byte (so the theme is symmetric / explicit). Use the identical hex values from the `:root` block (`--bg: #0d1117;` etc.).

2. `[data-theme="slate-light"]` — light paper palette. Suggested:
   - `--bg: #f5f3ee; --bg-deep: #e8e6e0; --bg-card: #ffffff; --bg-card-hover: #f0eee8; --bg-input: #faf8f3;`
   - `--border: #d4d0c4; --border-faint: #e4e1d6; --border-strong: #aaa595;`
   - `--teal: #1f9ba4; --teal-bright: #2db4be; --teal-dim: rgba(31,155,164,.55); --teal-faint: rgba(31,155,164,.15); --teal-glow: rgba(31,155,164,.32);`
   - `--text: #1d2228; --text-dim: #555c66; --gray: #7a8088; --gray-faint: rgba(122,128,136,.45);`
   - `--green: #2a9942; --red: #d83e3a; --amber: #b07810;`

3. `[data-theme="iron-dark"]` — near-black bg + molten-orange accent. Suggested:
   - `--bg: #0a0807; --bg-deep: #050404; --bg-card: #14110f; --bg-card-hover: #1a1614; --bg-input: #0f0c0a;`
   - `--border: #3a2e26; --border-faint: #251d18; --border-strong: #4c3d33;`
   - `--teal: #f0883e; --teal-bright: #ff9a4a; --teal-dim: rgba(240,136,62,.55); --teal-faint: rgba(240,136,62,.15); --teal-glow: rgba(240,136,62,.32);`
   - `--text: #f0e5db; --text-dim: #b59e8c; --gray: #7d6d61; --gray-faint: rgba(125,109,97,.45);`
   - `--green: #6abf4b; --red: #ff5a4a; --amber: #f0883e;`

4. `[data-theme="terminal-dark"]` — pure black + ANSI green. Suggested:
   - `--bg: #000000; --bg-deep: #000000; --bg-card: #0a0d0a; --bg-card-hover: #121712; --bg-input: #040604;`
   - `--border: #1f3a1f; --border-faint: #132113; --border-strong: #2e5a2e;`
   - `--teal: #3fb950; --teal-bright: #56e070; --teal-dim: rgba(63,185,80,.55); --teal-faint: rgba(63,185,80,.15); --teal-glow: rgba(63,185,80,.32);`
   - `--text: #b5f5b5; --text-dim: #7ac57a; --gray: #4a8a4a; --gray-faint: rgba(74,138,74,.45);`
   - `--green: #56e070; --red: #ff5a4a; --amber: #f0c14a;`

5. `[data-theme="parchment-light"]` — cream + brass ink. Suggested:
   - `--bg: #f4ecdc; --bg-deep: #e6dcc6; --bg-card: #fcf6e8; --bg-card-hover: #f2eadb; --bg-input: #fbf3df;`
   - `--border: #c8b896; --border-faint: #ddd0b3; --border-strong: #a89366;`
   - `--teal: #a67c2c; --teal-bright: #c5984a; --teal-dim: rgba(166,124,44,.55); --teal-faint: rgba(166,124,44,.15); --teal-glow: rgba(166,124,44,.32);`
   - `--text: #2a2218; --text-dim: #645540; --gray: #8a785a; --gray-faint: rgba(138,120,90,.45);`
   - `--green: #3a8a3a; --red: #c43a35; --amber: #a67c2c;`

Use 2-space indentation inside each block, one declaration per line, ending each block with `}`.

DO NOT modify the existing `site.css :root` rule. DO NOT touch tokens.css. DO NOT touch any Rust file.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && grep -c "^\[data-theme=" crates/iron_hermes_ui/assets/site.css | awk '{ if ($1 < 5) exit 1 }' && grep -q '\[data-theme="slate-dark"\]' crates/iron_hermes_ui/assets/site.css && grep -q '\[data-theme="slate-light"\]' crates/iron_hermes_ui/assets/site.css && grep -q '\[data-theme="iron-dark"\]' crates/iron_hermes_ui/assets/site.css && grep -q '\[data-theme="terminal-dark"\]' crates/iron_hermes_ui/assets/site.css && grep -q '\[data-theme="parchment-light"\]' crates/iron_hermes_ui/assets/site.css && for theme in slate-dark slate-light iron-dark terminal-dark parchment-light; do awk "/\\[data-theme=\"$theme\"\\]/,/^}/" crates/iron_hermes_ui/assets/site.css | grep -q -- '--teal:' || exit 1; done</automated>
  </verify>
  <done>
File contains exactly 5 `[data-theme="..."]` blocks (slate-dark, slate-light, iron-dark, terminal-dark, parchment-light) AND every block reassigns at minimum `--teal` (proxy for the full legacy-var family).
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify build gates + D-02 backend immutability gate</name>
  <files>crates/iron_hermes_ui/assets/site.css</files>
  <action>
Run the standard build + test gates that every plan in this phase must satisfy, then explicitly verify D-02 (no backend file touched).

Run from repo root:

  cargo check -p iron_hermes_ui                                         # expect exit 0
  cargo test -p iron_hermes_ui --bin iron_hermes_ui                     # expect exit 0, 32 tests pass
  cargo test -p iron_hermes_ui --test wave0_smoke                       # expect exit 0, 1 test pass
  git diff -- crates/iron_hermes_ui/src/server/ crates/iron_hermes_ui/src/protocol.rs  # expect empty
  git diff -- crates/iron_hermes_ui/src/components/hermes_app/theme_effects.rs         # expect empty (do-not-modify)
  git diff -- crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs            # expect empty (do-not-modify)

CSS-only change ⇒ `cargo check` should be a no-op recompile; the asset is `include_str!` / fingerprinted at next `dx serve`. If `cargo check` exits non-zero, STOP and report.

This task does NOT do manual UAT — the live re-walk of UAT Test 2 + Test 6 lives in the phase-level gap-closure verification step after all gap plans land.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && cargo check -p iron_hermes_ui 2>&1 | tail -3 && cargo test -p iron_hermes_ui --bin iron_hermes_ui 2>&1 | grep -E "test result.*passed" && cargo test -p iron_hermes_ui --test wave0_smoke 2>&1 | grep -E "test result.*passed" && test -z "$(git diff -- crates/iron_hermes_ui/src/server/ crates/iron_hermes_ui/src/protocol.rs)" && test -z "$(git diff -- crates/iron_hermes_ui/src/components/hermes_app/theme_effects.rs crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs)"</automated>
  </verify>
  <done>
`cargo check -p iron_hermes_ui` exit 0. Binary tests 32/32 pass. wave0_smoke 1/1 pass. D-02 git diff is empty across server/, protocol.rs. theme_effects.rs and app_footer.rs are unchanged.
  </done>
</task>

</tasks>

<verification>
- `cargo check -p iron_hermes_ui` exits 0
- `cargo test -p iron_hermes_ui --bin iron_hermes_ui` exits 0 (32 tests pass)
- `cargo test -p iron_hermes_ui --test wave0_smoke` exits 0 (1 test passes)
- `git diff -- crates/iron_hermes_ui/src/server/ crates/iron_hermes_ui/src/protocol.rs` is empty (D-02)
- `git diff -- crates/iron_hermes_ui/src/components/hermes_app/theme_effects.rs` is empty
- `git diff -- crates/iron_hermes_ui/src/components/hermes_app/app_footer.rs` is empty
- `grep -c '^\[data-theme=' crates/iron_hermes_ui/assets/site.css` ≥ 5
- `grep -q '\.app-footer\s*{' crates/iron_hermes_ui/assets/site.css` succeeds
- `grep -q 'body\.no-footer' crates/iron_hermes_ui/assets/site.css` succeeds
- `grep -q 'body\.has-rail\.on-chat' crates/iron_hermes_ui/assets/site.css` succeeds

Manual re-run gates (logged in UAT-26.2.1.md re-run; performed by developer after all gap plans land):
- UAT Test 2: AppFooter bottom-left + bottom-right chrome both visible at page load
- UAT Test 6: All 5 themes flip the wheel/screen/footer/breadcrumb palette; all four chrome toggles (SCANLINES/BREADCRUMB/FOOTER/RAIL) visibly engage; DENSE toggle adjusts spacing
</verification>

<success_criteria>
- site.css contains 5 `[data-theme]` blocks AND the chrome positioning rules AND all 6 body-class toggle rules
- All three cargo gates exit 0
- D-02 byte-for-byte backend untouched (zero diff in server/ and protocol.rs)
- `theme_effects.rs` and `app_footer.rs` are byte-for-byte unchanged from the pre-plan-10 commit
- Plan 03's claim ("CSS is supplied by assets/site.css") is now factually true
</success_criteria>

<output>
After completion, create `.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-10-SUMMARY.md` per the standard summary template. Include:
- Hex values chosen for each of the 5 themes' legacy-var family (full table)
- Exact line range copied from app.html (e.g., "lines 42-112, 315-316, 320 — verbatim")
- Confirmation that `theme_effects.rs` was not modified (per UAT-26.2.1.md root_cause directive)
- Re-verification check: grep counts for `[data-theme=` and `app-footer` after the change
</output>
