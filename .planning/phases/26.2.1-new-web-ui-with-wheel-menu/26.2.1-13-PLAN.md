---
phase: 26.2.1
plan: 13
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - crates/iron_hermes_ui/assets/site.css
  - crates/iron_hermes_ui/src/components/hermes_app/mod.rs
  - crates/iron_hermes_ui/src/server/api.rs
autonomous: true
requirements:
  - GAP-26.2.1-06-footer-pills-cover-hud-corners
  - GAP-26.2.1-07-scanlines-toggle-does-not-engage
  - GAP-26.2.1-08-dispatch-slash-intercepts-research
  - GAP-26.2.1-09-list-sessions-platform-web-filter

dependency_graph:
  - GAP-06 (CSS layering, site.css) — independent of all other tasks
  - GAP-07 (CSS selector + Rust class audit, site.css and/or hud_chrome.rs) — independent of all other tasks
  - GAP-08 (Rust refactor, mod.rs) — independent of all other tasks
  - GAP-09 (D-02 surgical lift, server/api.rs) — independent of all other tasks
  - Task 5 (re-verify gate) — depends on Tasks 1-4 all landing

tech_stack:
  - Rust 2021 + Dioxus 0.7.1 (signals, EventHandler, RSX)
  - CSS3 (z-index layering, attribute selectors, body-class cascade)
  - cargo check / cargo test gates per Phase 26.2.1 D-02 build contract
  - Static-grep regression tests via include_str! (Plan 11 pattern)

key_files:
  - crates/iron_hermes_ui/assets/site.css                                       # GAP-06 + GAP-07 fix-site
  - crates/iron_hermes_ui/src/components/hermes_app/mod.rs                      # GAP-08 fix-site (dispatch_slash refactor + tests)
  - crates/iron_hermes_ui/src/server/api.rs                                     # GAP-09 fix-site (D-02 surgical lift, line 60-61)
  - crates/iron_hermes_ui/src/components/hermes_app/hud_chrome.rs               # GAP-07 reference (scanlines DOM, line 20)
  - crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs             # GAP-08 reference (ChatBubble constructors)
  - crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/app.html  # GAP-06 / GAP-07 design source-of-truth

decisions:
  - id: D-26.2.1-13-A
    title: "GAP-09 — D-02 surgical lift for list_sessions platform filter (USER-APPROVED)"
    decision: |
      Replace `Some(&ironhermes_core::types::Platform::Web.to_string())` with
      `None` on line 60-61 of `crates/iron_hermes_ui/src/server/api.rs` so the
      SESSIONS wedge sources the full cross-platform on-disk session catalog
      (currently 74 entries in ~/.ironhermes/sessions/).
    rationale: |
      The user explicitly approved this surgical lift in
      `26.2.1-UAT.md` §"Round-2 D-02 Decision" (2026-05-14):

        "Lift D-02 surgically for GAP-26.2.1-09 — change
         crates/iron_hermes_ui/src/server/api.rs:60-61 from
         Some(&ironhermes_core::types::Platform::Web.to_string())
         to None so the SESSIONS wedge sources the full on-disk
         session catalog (currently 74 entries in
         ~/.ironhermes/sessions/).

         Rationale: the 1-line server diff is the smallest
         possible D-02 deviation; deferring to 26.2.12 would
         ship 26.2.1 with a SESSIONS wedge that is functionally
         empty for any real user. Acceptable surgical lift of
         the 26.2.1 D-02 contract."

      Scope: ONE file (api.rs), ONE function (list_sessions), TWO lines
      (60-61). All other server boundary files (ws.rs, state.rs,
      protocol.rs) remain byte-for-byte unchanged. The D-02 gate in the
      verify task is narrowed accordingly: api.rs is exempted from the
      "no diff" assertion, all other server/* files are still asserted
      unchanged.
  - id: D-26.2.1-13-B
    title: "GAP-08 — dispatch_slash returns bool (allowlist + fallthrough)"
    decision: |
      Refactor `dispatch_slash` from `fn(...) -> ()` to `fn(...) -> bool`.
      `"/clear"` arm returns `true` after clearing bubbles. `_` arm returns
      `false` and does NOT append an error bubble. The send EventHandler
      call site becomes:
          `if trimmed.starts_with('/') && dispatch_slash(...) { return; }`
      so unhandled slashes fall through to `ws.send_raw(ChatRequest)` and
      reach the server (or LLM) for routing.
    rationale: |
      Plan 11's blanket interception broke `/research weather` (a
      server-routed tool-execution command) by emitting "unknown command:
      /research" locally instead of forwarding to the WS. The split is:
      UI-state commands (e.g. `/clear`) handled CLIENT-SIDE; server-effecting
      commands (`/research`, future tool-routed commands) flow through to
      the backend. Plan 11's `unknown_command_arm_appends_error_bubble_locally`
      static-grep test must be inverted to assert the new fall-through
      behavior (renamed to `unknown_command_arm_falls_through_to_websocket`).
  - id: D-26.2.1-13-C
    title: "GAP-06 — Strategy preference: lift HUD corners above footer (smaller diff)"
    decision: |
      Investigate computed z-index and bottom-offset of `.app-footer` (z-index
      50, bottom 0, height 30px per site.css:264-280) vs `.hud-corner.hud-corner--bl`
      / `.hud-corner.hud-corner--br`. Pick whichever single-rule change
      produces the smaller diff:
      (a) raise BL/BR HUD corners above the footer (z-index > 50 OR shift
          their bottom offset above the 30px footer); OR
      (b) shrink/inset the footer above the corners (e.g. bottom: 8px with
          rounded corners + smaller width).
    rationale: |
      Both are CSS-only single-rule changes. Executor should grep the
      existing `.hud-corner.hud-corner--bl` / `.hud-corner.hud-corner--br`
      rules in site.css (or wherever HUD corners are styled), measure
      computed values, and choose the smaller diff. Document the choice
      in the SUMMARY.
  - id: D-26.2.1-13-D
    title: "GAP-07 — Reconcile scanlines DOM class vs site.css selector"
    decision: |
      Both the DOM and CSS use `.scanlines` (verified — `hud_chrome.rs:20` emits
      `div { class: "scanlines", ... }` and `site.css:288` has
      `body.no-scanlines .scanlines { display: none; }`). The body-class
      effect IS firing (other body classes work). The remaining cause must
      be a specificity / animation override: site.css:286 declares
      `.scanlines { opacity: 0.35; }` and other rules may animate the
      element via background-position cycling. Even though `display: none`
      should win on specificity grounds for `body.no-scanlines .scanlines`
      (specificity 0,2,1) vs `.scanlines` (specificity 0,1,0), the
      executor must inspect for: (a) any `.scanlines` rule using
      `display: block !important` or animation that bypasses display, or
      (b) the actual rendered DOM in browser DevTools showing whether the
      `body.no-scanlines` class is applied at all when the toggle fires.
      Apply the smallest diff that produces the desired effect — most
      likely adding `!important` to the `display: none` rule OR adding a
      `visibility: hidden; opacity: 0;` companion rule for animation-driven
      cases.
    rationale: |
      The investigation order matters: confirm class-name parity first
      (already verified: `.scanlines` matches), THEN investigate
      specificity/animation override. The smallest diff is the goal.

metrics:
  - cargo_check_host: must exit 0
  - cargo_check_legacy_shell: must exit 0
  - cargo_check_wasm32: must exit 0
  - cargo_test_bin: must exit 0 (≥ 32 tests pass; one test renamed but count unchanged)
  - cargo_test_wave0_smoke: must exit 0 (1 test passes)
  - d_02_narrow_gate: git diff is empty for crates/iron_hermes_ui/src/server/state.rs, ws.rs, protocol.rs (api.rs EXEMPTED per D-26.2.1-13-A)
  - d_02_api_rs_diff_size: api.rs diff is exactly 1 logical change (Some(&...) → None), narrowly verifiable

must_haves:
  truths:
    - "GAP-06: Bottom-left and bottom-right HUD corner brackets are visible alongside the AppFooter pills (no occlusion)"
    - "GAP-07: Toggling SCANLINES off in the Tweaks panel hides the .scanlines overlay; toggling on restores it"
    - "GAP-08: Typing `/clear` + Enter clears the bubble list locally with zero WS frames sent"
    - "GAP-08: Typing `/research weather` + Enter forwards the command to the server (an assistant bubble streams back, NOT a local 'unknown command' bubble)"
    - "GAP-08: Plain (non-slash) messages still flow through ws.send_raw(ChatRequest) — no regression"
    - "GAP-09: SESSIONS wedge lists ALL sessions persisted under $IRONHERMES_HOME (~/.ironhermes/sessions/), not just current-process web sessions"
    - "GAP-09: Clicking a row from the historical catalog switches to that session's chat thread; SID badge shows that session's tail"
    - "D-02 narrow gate: state.rs, ws.rs, protocol.rs are byte-for-byte unchanged. api.rs has exactly one logical diff (Some→None) per user-approved D-26.2.1-13-A lift."
    - "All four cargo gates exit 0 (host check, legacy-shell check, wasm32 check, bin tests, wave0_smoke)"
  artifacts:
    - path: "crates/iron_hermes_ui/assets/site.css"
      provides: "GAP-06 layering fix (HUD corners visible + footer visible) AND GAP-07 SCANLINES toggle effective"
      contains_or: "z-index|hud-corner--bl|hud-corner--br|!important"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/mod.rs"
      provides: "GAP-08 dispatch_slash refactor returning bool with fall-through for unknown slashes; static-grep regression tests updated"
      contains: "fn dispatch_slash"
      contains_2: ") -> bool"
      contains_3: "&& dispatch_slash"
    - path: "crates/iron_hermes_ui/src/server/api.rs"
      provides: "GAP-09 D-02 surgical lift — list_sessions platform filter relaxed from Some(Web) to None"
      contains: ".list_sessions(\n            None,\n            100,"
      excludes: "Some(&ironhermes_core::types::Platform::Web.to_string())"
  key_links:
    - from: "Tweaks panel SCANLINES toggle (tweaks_panel.rs:185-189)"
      to: ".scanlines overlay div (hud_chrome.rs:20)"
      via: "body.no-scanlines class toggled by theme_effects.rs:80, cascading via site.css selector"
      pattern: "body\\.no-scanlines"
    - from: "ChatSendHandler send EventHandler (mod.rs ~line 247)"
      to: "ws.send_raw(ChatRequest) for non-slash AND unhandled-slash inputs"
      via: "early-return only when dispatch_slash returns true"
      pattern: "starts_with\\('/'\\) && dispatch_slash"
    - from: "list_sessions server fn (api.rs:54-76)"
      to: "state_store.list_sessions on-disk catalog at ~/.ironhermes/sessions/"
      via: "platform filter argument changed from Some(Web) to None"
      pattern: "\\.list_sessions\\(\\s*None"
    - from: ".app-footer (site.css:264-280, z-index 50, bottom 0)"
      to: ".hud-corner.hud-corner--bl / .hud-corner.hud-corner--br"
      via: "z-index OR bottom-offset adjustment so both render simultaneously"
      pattern: "hud-corner--b[lr]"

reverification:
  closes_gaps:
    - "GAP-26.2.1-06 (UAT Test 2): footer pills covered BL/BR HUD corners — fixed by CSS layering"
    - "GAP-26.2.1-07 (UAT Test 6): SCANLINES toggle did not engage — fixed by CSS selector/specificity reconcile"
    - "GAP-26.2.1-08 (UAT Test 8): /research returned unknown command — fixed by dispatch_slash bool refactor + fall-through"
    - "GAP-26.2.1-09 (UAT Test 9): SESSIONS wedge filtered to Platform::Web only — fixed by D-02 surgical lift to None"
  rerun:
    - "UAT-26.2.1.md §2 (Page Load & HUD Chrome) — confirm BL/BR corners + footer both visible"
    - "UAT-26.2.1.md §6 (Themes & Tweaks Panel) — SCANLINES toggle subsection"
    - "UAT-26.2.1.md §8 (Chat — Round Trip) — /clear local, /research server-routed, plain message regression-clean"
    - "UAT-26.2.1.md §9 (Sessions — Switch & Delete) — list shows ~/.ironhermes/sessions/ catalog"
---

<objective>
Close all four round-2 UAT gaps surfaced after Plans 10/11/12 landed for phase 26.2.1:

  1. **GAP-26.2.1-06** (cosmetic) — Footer pills cover BL/BR HUD corner brackets. CSS-only single-rule layering fix.
  2. **GAP-26.2.1-07** (minor) — SCANLINES toggle does not engage. CSS selector / specificity reconcile (one-line edit).
  3. **GAP-26.2.1-08** (major) — Plan 11 over-corrected: `dispatch_slash` intercepts ALL slash-prefix inputs and `/research weather` returns "unknown command" instead of forwarding to the WS. Refactor to return `bool` (allowlist + fallthrough).
  4. **GAP-26.2.1-09** (major, **D-02 surgical lift APPROVED BY USER 2026-05-14**) — `list_sessions` filters to `Platform::Web`, hiding the 74 historical on-disk sessions in `~/.ironhermes/sessions/`. Replace `Some(&Platform::Web.to_string())` with `None` on line 60-61 of `crates/iron_hermes_ui/src/server/api.rs`.

Purpose: 26.2.1 cannot ship until all four gaps close — three are blocking acceptance criteria (Tests 6/8/9) and one is a documented cosmetic regression (Test 2). The user has approved the D-02 surgical lift for GAP-09 specifically (see decision D-26.2.1-13-A in this plan's frontmatter and `26.2.1-UAT.md` §"Round-2 D-02 Decision").

Output: Three edited source files (`assets/site.css`, `src/components/hermes_app/mod.rs`, `src/server/api.rs`) plus updated regression tests in `mod.rs`. One re-verify gate task at the end. Atomic commits per task. D-02 narrow gate: state.rs, ws.rs, protocol.rs remain byte-for-byte unchanged; only api.rs has exactly one logical diff per the approved lift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-CONTEXT.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-UAT.md

# Prior gap-closure plans (pattern reference)
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-10-PLAN.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-11-PLAN.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-12-PLAN.md

# Fix-sites
@crates/iron_hermes_ui/assets/site.css
@crates/iron_hermes_ui/src/components/hermes_app/mod.rs
@crates/iron_hermes_ui/src/server/api.rs

# Reference (read but DO NOT modify)
@crates/iron_hermes_ui/src/components/hermes_app/hud_chrome.rs
@crates/iron_hermes_ui/src/components/hermes_app/screens/chat.rs
@crates/iron_hermes_ui/src/components/hermes_app/theme_effects.rs
@crates/iron_hermes_ui/src/components/hermes_app/tweaks_panel.rs

<interfaces>
The current dispatch_slash signature in `mod.rs` (lines 339-365):

```rust
fn dispatch_slash(
    cmd: &str,
    bubbles: &mut Signal<Vec<ChatBubble>>,
    next_id: &mut Signal<u64>,
) {
    let mut parts = cmd.trim().splitn(2, char::is_whitespace);
    let head = parts.next().unwrap_or("");
    match head {
        "/clear" => {
            bubbles.write().clear();
        }
        _ => {
            let id = { let n = *next_id.read(); next_id.set(n + 1); n };
            let msg = format!("unknown command: {head}");
            bubbles.write().push(ChatBubble::error(id, msg));
        }
    }
}
```

The current call site (mod.rs:264-265):

```rust
if trimmed.starts_with('/') {
    dispatch_slash(&trimmed, &mut bubbles, &mut next_id);
    // (early-return follows on the next line)
}
```

The current `list_sessions` body in `api.rs:54-64`:

```rust
#[get("/api/sessions")]
pub async fn list_sessions() -> Result<Vec<SessionInfo>> {
    let state = crate::server::state::global_app_state();
    let sessions = state
        .state_store
        .lock()
        .unwrap()
        .list_sessions(
            Some(&ironhermes_core::types::Platform::Web.to_string()),
            100,
        )
```

The scanlines DOM (hud_chrome.rs:20):

```rust
div { class: "scanlines", "aria-hidden": "true" }
```

Existing site.css scanlines rules (lines 285-288):

```css
/* Hide HUD scanlines slightly so screen content stays legible */
.scanlines { opacity: 0.35; }
.hud-grid { opacity: 0.35; }
body.no-scanlines .scanlines { display: none; }
```

Specificity check:
- `.scanlines { opacity: 0.35; }` — specificity (0,1,0)
- `body.no-scanlines .scanlines { display: none; }` — specificity (0,2,1)

The body-class rule SHOULD win on specificity grounds. If the visible behavior is "scanlines still on after off selected," investigate whether: (a) the `body.no-scanlines` class is actually present on `<body>` when the toggle fires (DevTools), (b) there's an animation-driven rule that bypasses `display: none`, or (c) the cached browser is serving a stale CSS. Likely smallest diff: `body.no-scanlines .scanlines { display: none !important; }` to defeat any animation/transform/opacity override the CSS author may add later.

Existing static-grep tests in mod.rs (lines 415-421) that need inversion:

```rust
#[test]
fn unknown_command_arm_appends_error_bubble_locally() {
    assert!(
        MOD_RS.contains("ChatBubble::error(id, msg)"),
        "unknown slash commands must append a ChatBubble::error locally",
    );
}
```

This test must be RENAMED to `unknown_command_arm_falls_through_to_websocket` and assert the new behavior (no error bubble, returns false).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GAP-26.2.1-06 — Layer HUD BL/BR corners above the AppFooter (CSS-only)</name>
  <files>crates/iron_hermes_ui/assets/site.css</files>
  <action>
Step A — Investigate the current `.hud-corner.hud-corner--bl` and `.hud-corner.hud-corner--br` rules in `crates/iron_hermes_ui/assets/site.css`. Run:

  grep -n "hud-corner" crates/iron_hermes_ui/assets/site.css

Identify the existing z-index and bottom-offset values for the BL and BR corners. The footer is at z-index 50 / bottom 0 / height 30px (site.css:264-280).

Step B — Pick the smaller-diff strategy per D-26.2.1-13-C:

  - Option A (preferred if BL/BR already have a z-index declaration nearby): RAISE the BL and BR HUD corners above the footer by setting `z-index: 60` (or higher than 50) on the existing `.hud-corner--bl` and `.hud-corner--br` selectors.
  - Option B (preferred if BL/BR are at bottom < 30px and have no z-index): SHIFT the BL/BR corners up by ~30-34px so they sit above the footer band (e.g. `bottom: 34px;` instead of `bottom: 0` or whatever they currently use).

If neither selector exists yet (i.e. HUD corners are styled by class-less rules or via a different selector entirely), ADD a new minimal rule block to site.css at the bottom of the chrome section:

  .hud-corner.hud-corner--bl,
  .hud-corner.hud-corner--br {
      z-index: 60;
  }

Choose whichever produces the smaller diff (1-3 lines max). Do NOT redesign the corners — just unblock the visual collision.

Step C — Verify post-edit: the BL/BR HUD corners and the footer pills can coexist in the rendered output. This is a CSS-only change; `cargo check` should be a no-op.

DO NOT touch any other CSS rule, any Rust file, or any other asset.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && grep -E "hud-corner--b[lr]" crates/iron_hermes_ui/assets/site.css | grep -v "^#" && (grep -E "hud-corner--b[lr]" crates/iron_hermes_ui/assets/site.css | grep -E "z-index|bottom" | head -3 || (echo "ERROR: GAP-06 fix not detected — expected z-index or bottom adjustment on hud-corner--bl/br" && exit 1)) && cargo check -p iron_hermes_ui 2>&1 | tail -3 | grep -q "Finished"</automated>
  </verify>
  <rollback>
If the chosen layering strategy causes other regressions (e.g. corner brackets now overlap the FAB or wheel), revert via `git checkout -- crates/iron_hermes_ui/assets/site.css` and try the alternate strategy (Option A vs B from Step B).
  </rollback>
  <done>
The `.hud-corner--bl` and `.hud-corner--br` selectors in site.css have either a higher z-index than 50 OR a bottom offset ≥ 30px (above the footer band). `cargo check -p iron_hermes_ui` exits 0. Atomic commit: `fix(26.2.1-13): GAP-06 layer HUD BL/BR corners above app-footer`.
  </done>
</task>

<task type="auto">
  <name>Task 2: GAP-26.2.1-07 — Reconcile SCANLINES toggle (CSS specificity / !important)</name>
  <files>crates/iron_hermes_ui/assets/site.css</files>
  <action>
Step A — Verify class-name parity between DOM and CSS (per D-26.2.1-13-D, already pre-verified by the planner):

  grep -n "class: \"scanlines\"" crates/iron_hermes_ui/src/components/hermes_app/hud_chrome.rs   # expect: line 20
  grep -n "body\\.no-scanlines" crates/iron_hermes_ui/assets/site.css                            # expect: line 288

Both should match. The DOM emits `<div class="scanlines" aria-hidden="true">` (hud_chrome.rs:20) and CSS targets `body.no-scanlines .scanlines { display: none; }` (site.css:288). Class-name parity is confirmed.

Step B — The body-class effect IS firing (other body classes work — see UAT round-2 notes for Test 6: "BREADCRUMB / FOOTER / RAIL / DENSITY toggles work — only the SCANLINES toggle fails"). The remaining cause must be CSS specificity OR animation override. The smallest reliable diff is to add `!important` to the `display: none` rule, which defeats any current OR future animation/transform/opacity-driven override.

Edit `crates/iron_hermes_ui/assets/site.css` line 288 from:

  body.no-scanlines .scanlines { display: none; }

To:

  body.no-scanlines .scanlines { display: none !important; }

That is the entire fix — one word added (`!important`).

Step C — If the executor (during Step B investigation) discovers a more-specific CSS rule that needs adjusting (e.g. `.scanlines` has a `display: block` declaration somewhere that should be removed instead), prefer the smaller-diff fix that preserves the original rule shape. Document in the SUMMARY.

DO NOT modify `hud_chrome.rs` (DOM is correct). DO NOT modify `theme_effects.rs` (signal/effect path is exonerated). DO NOT touch any other CSS rule.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && grep -E "body\\.no-scanlines.*\\.scanlines.*display.*none.*!important" crates/iron_hermes_ui/assets/site.css || (echo "ERROR: GAP-07 fix not detected — expected !important on body.no-scanlines .scanlines display:none rule" && exit 1) && grep -q 'class: "scanlines"' crates/iron_hermes_ui/src/components/hermes_app/hud_chrome.rs && cargo check -p iron_hermes_ui 2>&1 | tail -3 | grep -q "Finished"</automated>
  </verify>
  <rollback>
If !important produces visual side-effects (e.g. cascading into a parent rule), revert with `git checkout -- crates/iron_hermes_ui/assets/site.css` and apply the alternate-strategy: locate any `display: block` on `.scanlines` and remove it instead.
  </rollback>
  <done>
The `body.no-scanlines .scanlines { display: none !important; }` rule is in place. `hud_chrome.rs` is unchanged. `theme_effects.rs` is unchanged. `cargo check -p iron_hermes_ui` exits 0. Atomic commit: `fix(26.2.1-13): GAP-07 force scanlines display:none with !important`.
  </done>
</task>

<task type="auto">
  <name>Task 3: GAP-26.2.1-08 — Refactor dispatch_slash to return bool with fall-through for unknown slashes</name>
  <files>crates/iron_hermes_ui/src/components/hermes_app/mod.rs</files>
  <action>
Step A — Refactor the `dispatch_slash` function in `crates/iron_hermes_ui/src/components/hermes_app/mod.rs` (currently at lines 339-365). Change:

  - Signature from `fn dispatch_slash(...) -> ()` to `fn dispatch_slash(...) -> bool`
  - `"/clear"` arm: clear bubbles, then `return true;`
  - `_` (default) arm: REMOVE the unknown-command bubble construction entirely, return `false`. No `let id = ...`, no `format!`, no `bubbles.write().push(ChatBubble::error(...))`.

The new function body should be:

  fn dispatch_slash(
      cmd: &str,
      bubbles: &mut Signal<Vec<ChatBubble>>,
      _next_id: &mut Signal<u64>,
  ) -> bool {
      let mut parts = cmd.trim().splitn(2, char::is_whitespace);
      let head = parts.next().unwrap_or("");
      match head {
          "/clear" => {
              bubbles.write().clear();
              true
          }
          // Per D-26.2.1-13-B: unknown slashes are NOT handled
          // client-side. Fall through to ws.send_raw so the server
          // (or LLM) routes them. /research weather, /help (future),
          // /status (future) all reach the backend via this path.
          _ => false,
      }
  }

Note: `_next_id: &mut Signal<u64>` keeps the parameter name + underscore prefix to avoid the unused-variable warning while preserving the call-site signature unchanged. The doc-comment above the fn should be updated to reflect the new contract (allowlist + fallthrough).

Step B — Update the call site in the send EventHandler (currently around mod.rs:264-265). Change from:

  if trimmed.starts_with('/') {
      dispatch_slash(&trimmed, &mut bubbles, &mut next_id);
      return;
  }

To:

  if trimmed.starts_with('/') && dispatch_slash(&trimmed, &mut bubbles, &mut next_id) {
      return;
  }

(combine the conditional and call into a single `&&` expression; the `return` only fires when dispatch_slash returns true). Unhandled slashes (false) fall through to the existing `let sid = ...; ... ws.send_raw(...)` path that follows.

Step C — Update the static-grep regression tests in the `#[cfg(test)] mod tests` block at the bottom of mod.rs (currently around lines 367-439). Specifically:

  1. Update `dispatch_slash_helper_exists` to also assert the new bool return type:

     #[test]
     fn dispatch_slash_helper_exists() {
         assert!(
             MOD_RS.contains("fn dispatch_slash"),
             "dispatch_slash helper must exist in mod.rs (GAP-26.2.1-04 regression)",
         );
         assert!(
             MOD_RS.contains(") -> bool"),
             "dispatch_slash must return bool per GAP-26.2.1-08 (D-26.2.1-13-B)",
         );
     }

  2. Update `send_eventhandler_intercepts_slash_prefix_before_websocket` to assert the new combined-conditional call shape:

     assert!(
         MOD_RS.contains("trimmed.starts_with('/') && dispatch_slash(&trimmed, &mut bubbles, &mut next_id)"),
         "send EventHandler must combine starts_with('/') with dispatch_slash bool return per GAP-26.2.1-08",
     );

  3. RENAME `unknown_command_arm_appends_error_bubble_locally` to `unknown_command_arm_falls_through_to_websocket` and INVERT its assertion:

     #[test]
     fn unknown_command_arm_falls_through_to_websocket() {
         // Per GAP-26.2.1-08 (D-26.2.1-13-B): unknown slashes must NOT
         // be handled client-side. The `_` arm returns false so the
         // call site falls through to ws.send_raw(ChatRequest).
         assert!(
             !MOD_RS.contains("ChatBubble::error(id, msg)"),
             "GAP-26.2.1-08: unknown-slash arm must NOT append a local error bubble",
         );
         assert!(
             !MOD_RS.contains("format!(\"unknown command:"),
             "GAP-26.2.1-08: unknown-slash arm must NOT format an 'unknown command' message",
         );
         // Positive assertion: the `_` arm must yield `false` so the
         // call site short-circuits and falls through.
         assert!(
             MOD_RS.contains("_ => false"),
             "GAP-26.2.1-08: unknown-slash arm must return false to enable fall-through to ws.send_raw",
         );
     }

  4. The `slash_branch_precedes_websocket_send_path` test at lines 423-438 still asserts the right invariant (slash branch comes before `let sid = session_id.read().clone();`) — leave it unchanged. The `&&` short-circuit combined-conditional preserves source-text ordering.

  5. The `clear_arm_empties_bubble_list_locally` test at lines 403-413 still asserts the right invariant (`/clear` empties bubbles) — leave it unchanged.

Step D — Verify the test count is preserved (5 tests before → 5 tests after; one renamed, none added, none removed).

DO NOT touch `screens/chat.rs`, `screens/sessions.rs`, `warp_hermes.rs`, or any server file. DO NOT modify any ChatBubble constructor.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && grep -q "fn dispatch_slash" crates/iron_hermes_ui/src/components/hermes_app/mod.rs && grep -q ") -> bool" crates/iron_hermes_ui/src/components/hermes_app/mod.rs && grep -q "trimmed.starts_with('/') && dispatch_slash(&trimmed, &mut bubbles, &mut next_id)" crates/iron_hermes_ui/src/components/hermes_app/mod.rs && grep -q "_ => false" crates/iron_hermes_ui/src/components/hermes_app/mod.rs && grep -q "unknown_command_arm_falls_through_to_websocket" crates/iron_hermes_ui/src/components/hermes_app/mod.rs && ! grep -q "unknown_command_arm_appends_error_bubble_locally" crates/iron_hermes_ui/src/components/hermes_app/mod.rs && ! grep -q "ChatBubble::error(id, msg)" crates/iron_hermes_ui/src/components/hermes_app/mod.rs && cargo check -p iron_hermes_ui 2>&1 | tail -3 | grep -q "Finished" && cargo test -p iron_hermes_ui --bin iron_hermes_ui 2>&1 | grep -E "test result.*passed" | head -1</automated>
  </verify>
  <rollback>
If the bool refactor breaks the borrow-checker (e.g. a `&mut bubbles` borrow conflict appears at the new call-site `&&` short-circuit), revert with `git checkout -- crates/iron_hermes_ui/src/components/hermes_app/mod.rs` and try the alternate form: split the conditional into two lines using a `let handled = if trimmed.starts_with('/') { dispatch_slash(...) } else { false }; if handled { return; }` pattern.
  </rollback>
  <done>
`dispatch_slash` returns `bool`, the `_` arm returns `false` with no error-bubble construction, the call site uses the combined-conditional `&&` short-circuit, the regression tests are inverted/renamed, `cargo check -p iron_hermes_ui` exits 0, `cargo test -p iron_hermes_ui --bin iron_hermes_ui` passes (test count unchanged at ≥ 32). Atomic commit: `fix(26.2.1-13): GAP-08 dispatch_slash returns bool with fall-through for unknown slashes`.
  </done>
</task>

<task type="auto">
  <name>Task 4: GAP-26.2.1-09 — D-02 surgical lift: relax list_sessions platform filter (USER-APPROVED)</name>
  <files>crates/iron_hermes_ui/src/server/api.rs</files>
  <action>
Per the user-approved D-26.2.1-13-A decision (`26.2.1-UAT.md` §"Round-2 D-02 Decision"), perform a one-line surgical lift of the D-02 contract. ONLY this single line in api.rs may change.

Step A — Open `crates/iron_hermes_ui/src/server/api.rs` and locate the `list_sessions` function (lines 53-76). The current body (lines 60-63):

  .list_sessions(
      Some(&ironhermes_core::types::Platform::Web.to_string()),
      100,
  )

Step B — Replace the `Some(&ironhermes_core::types::Platform::Web.to_string())` argument with `None`. The new shape (lines 60-63):

  .list_sessions(
      None,
      100,
  )

That is the entire change. ONE line. ONE argument. No new imports needed (the `ironhermes_core::types::Platform` reference simply vanishes from this call site; if it was the only use of that type in this file, the rustc unused-import lint will surface — verify with `cargo check` and remove the `use ...Platform...` line at the top of api.rs ONLY IF rustc warns).

Step C — Add a brief inline comment immediately above the modified line documenting the user-approved D-02 lift, so future readers don't re-introduce the filter:

  // GAP-26.2.1-09 / D-26.2.1-13-A (USER-APPROVED 2026-05-14):
  // Pass None instead of Some(Platform::Web) so the SESSIONS wedge sources
  // the full cross-platform on-disk session catalog (~/.ironhermes/sessions/).
  // See .planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-UAT.md
  // §"Round-2 D-02 Decision" for the user authorization quote.
  .list_sessions(
      None,
      100,
  )

Step D — Confirm scope: this is the ONLY change to api.rs in this task. `git diff crates/iron_hermes_ui/src/server/api.rs` should show:
  - 1 line removed (`Some(&ironhermes_core::types::Platform::Web.to_string()),`)
  - 1 line added (`None,`)
  - Plus the explanatory comment block (4-6 lines added)

DO NOT touch `state.rs`, `ws.rs`, or `protocol.rs`. DO NOT touch any other function in api.rs. DO NOT touch any test in api.rs.

Step E — Verify D-02 narrow gate. Run:

  git diff -- crates/iron_hermes_ui/src/server/state.rs crates/iron_hermes_ui/src/server/ws.rs crates/iron_hermes_ui/src/protocol.rs

Expect: empty (these files are still byte-for-byte unchanged). The api.rs diff is exempted per D-26.2.1-13-A.

Step F — If `cargo check` warns about an unused import for `ironhermes_core::types::Platform` (because this was the sole consumer in api.rs), remove the offending `use` statement at the top of api.rs. Verify by re-running `cargo check`.
  </action>
  <verify>
    <automated>cd /Users/twilson/code/ironhermes && grep -A 4 "list_sessions" crates/iron_hermes_ui/src/server/api.rs | grep -E "^\s*None,\s*$" && ! grep -q "Some(&ironhermes_core::types::Platform::Web.to_string())" crates/iron_hermes_ui/src/server/api.rs && grep -q "GAP-26.2.1-09" crates/iron_hermes_ui/src/server/api.rs && grep -q "D-26.2.1-13-A" crates/iron_hermes_ui/src/server/api.rs && test -z "$(git diff -- crates/iron_hermes_ui/src/server/state.rs crates/iron_hermes_ui/src/server/ws.rs crates/iron_hermes_ui/src/protocol.rs)" && cargo check -p iron_hermes_ui 2>&1 | tail -3 | grep -q "Finished" && cargo check -p iron_hermes_ui --features legacy-shell 2>&1 | tail -3 | grep -q "Finished"</automated>
  </verify>
  <rollback>
If `cargo check` errors (the `list_sessions` signature on `state_store` may have changed in some future version such that `None` is not a valid argument), revert with `git checkout -- crates/iron_hermes_ui/src/server/api.rs` and consult the actual `list_sessions` signature on `StateStore` — adjust the argument shape to whatever the trait expects for "no platform filter" (could be an explicit `Option::<&str>::None` or `&None`). The semantic intent is "drop the platform filter so all on-disk sessions are returned."
  </rollback>
  <done>
api.rs `list_sessions` calls `.list_sessions(None, 100)`. The user-approval comment block is in place. state.rs, ws.rs, protocol.rs git diffs are empty (D-02 narrow gate passes). Both `cargo check` invocations (host + legacy-shell) exit 0. Atomic commit: `fix(26.2.1-13): GAP-09 D-02 surgical lift — list_sessions returns full on-disk catalog (USER-APPROVED)`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Re-verify gate — re-run UAT Tests 2, 6, 8, 9</name>
  <files>.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-UAT.md</files>
  <what-built>
All four round-2 gaps (GAP-06 / GAP-07 / GAP-08 / GAP-09) closed via Tasks 1-4. Atomic commits landed. Build gates green. The remaining step is human re-verification of the four UAT tests that previously failed.
  </what-built>
  <how-to-verify>
1. Confirm all five build gates exit 0 (run from repo root):

     cargo check -p iron_hermes_ui                                         # expect 0
     cargo check -p iron_hermes_ui --features legacy-shell                 # expect 0
     cargo check -p iron_hermes_ui --target wasm32-unknown-unknown         # expect 0
     cargo test -p iron_hermes_ui --bin iron_hermes_ui                     # expect 0, ≥ 32 tests pass
     cargo test -p iron_hermes_ui --test wave0_smoke                       # expect 0, 1 test passes

2. Confirm the D-02 narrow gate:

     git diff -- crates/iron_hermes_ui/src/server/state.rs crates/iron_hermes_ui/src/server/ws.rs crates/iron_hermes_ui/src/protocol.rs
     # Expected: empty output

   And the api.rs diff is exactly the user-approved Some→None change plus the explanatory comment:

     git diff -- crates/iron_hermes_ui/src/server/api.rs | head -30
     # Expected: 1 line removed (Some(&Platform::Web.to_string())),
     #           1 line added (None,),
     #           plus the GAP-26.2.1-09 / D-26.2.1-13-A comment block

3. Kill any running dev server, then start fresh:

     dx serve --package iron_hermes_ui

   Open http://localhost:8080/ in a fresh browser tab with DevTools open.

4. **Test 2 (GAP-06 re-verify)** — Page Load & HUD Chrome:
   - Bottom-left HUD corner bracket IS visible alongside the bottom-left footer pill (NODE/SCREEN/AGENT)
   - Bottom-right HUD corner bracket IS visible alongside the bottom-right footer pill (MEM/SKILLS/P50 + clock)
   - PASS criteria: BOTH the corners AND the footer pills render simultaneously without occlusion

5. **Test 6 (GAP-07 re-verify)** — Themes & Tweaks Panel SCANLINES toggle:
   - Open the Tweaks panel (gear FAB at bottom-right)
   - Click the SCANLINES toggle to OFF
   - PASS criteria: the scanlines overlay visibly disappears
   - Click SCANLINES back to ON
   - PASS criteria: the scanlines overlay visibly returns
   - Sanity-check the other body-class toggles (BREADCRUMB / FOOTER / RAIL / DENSITY) still work — should be regression-clean

6. **Test 8 (GAP-08 re-verify)** — Chat round trip:
   - 8a. Type `/clear` + Enter — bubble list empties, status stays READY, DevTools Network shows ZERO new WebSocket frames sent for that input. PASS criteria: local-only handling.
   - 8b. Type `/research weather` + Enter — an assistant bubble streams back from the server (NOT a local "unknown command" bubble); tool-progress rows render underneath. PASS criteria: command reaches the backend and tool execution proceeds.
   - 8c. Type `hello, what model are you?` + Enter — plain message round-trips through the WS exactly as before. PASS criteria: no regression for non-slash messages.

7. **Test 9 (GAP-09 re-verify)** — Sessions wedge:
   - Click the SESSIONS wedge
   - PASS criteria: the list shows the historical sessions from `~/.ironhermes/sessions/` (74 entries on disk per the UAT round-2 notes), NOT just current-process web sessions
   - Click any non-current row from the historical catalog
   - PASS criteria: the chat-mini header SID badge updates to that session's distinct 8-char tail (Plan 12's truncation fix should still hold)

8. After all four tests pass, update `.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-UAT.md` round-2 entries:
   - Test 2 round_2_notes: change "result: pass / cosmetic side-effect" closing line to confirm GAP-06 closed
   - Test 6 result: change from "issue" to "pass" with closure note for GAP-07
   - Test 8 result: change from "issue" to "pass" with closure note for GAP-08
   - Test 9 result: change from "issue" to "pass" with closure note for GAP-09
   - Update the §Summary block: passed: 13, issues: 0, new_round_2_gaps: 0 (closed: GAP-06, GAP-07, GAP-08, GAP-09)
  </how-to-verify>
  <resume-signal>Type "approved" once all four tests visually pass and UAT-26.2.1.md is updated. If any test fails, describe the failure mode and I will spin up a follow-up plan.</resume-signal>
</task>

</tasks>

<verification>
- `cargo check -p iron_hermes_ui` exits 0
- `cargo check -p iron_hermes_ui --features legacy-shell` exits 0
- `cargo check -p iron_hermes_ui --target wasm32-unknown-unknown` exits 0
- `cargo test -p iron_hermes_ui --bin iron_hermes_ui` exits 0 (≥ 32 tests pass; one test renamed)
- `cargo test -p iron_hermes_ui --test wave0_smoke` exits 0 (1 test passes)
- `git diff -- crates/iron_hermes_ui/src/server/state.rs crates/iron_hermes_ui/src/server/ws.rs crates/iron_hermes_ui/src/protocol.rs` is empty (D-02 narrow gate)
- `git diff -- crates/iron_hermes_ui/src/server/api.rs` shows exactly: 1 line removed (Some(&Platform::Web)), 1 line added (None,), plus the GAP-26.2.1-09 / D-26.2.1-13-A comment (user-approved scope)
- `grep -E "hud-corner--b[lr]" crates/iron_hermes_ui/assets/site.css` shows z-index ≥ 60 OR bottom ≥ 30px
- `grep -E "body\.no-scanlines.*display.*none.*!important" crates/iron_hermes_ui/assets/site.css` succeeds
- `grep -q ") -> bool" crates/iron_hermes_ui/src/components/hermes_app/mod.rs` succeeds
- `grep -q "starts_with('/') && dispatch_slash" crates/iron_hermes_ui/src/components/hermes_app/mod.rs` succeeds
- `grep -q "_ => false" crates/iron_hermes_ui/src/components/hermes_app/mod.rs` succeeds
- `grep -q "unknown_command_arm_falls_through_to_websocket" crates/iron_hermes_ui/src/components/hermes_app/mod.rs` succeeds
- `! grep -q "unknown_command_arm_appends_error_bubble_locally" crates/iron_hermes_ui/src/components/hermes_app/mod.rs` succeeds (test renamed)
- `grep -q "None,\s*$" crates/iron_hermes_ui/src/server/api.rs` succeeds (in list_sessions context)
- `! grep -q "Some(&ironhermes_core::types::Platform::Web.to_string())" crates/iron_hermes_ui/src/server/api.rs` succeeds

Manual re-run gates (developer Task 5):
- UAT Test 2: BL/BR HUD corners + footer pills both visible (GAP-06 closed)
- UAT Test 6: SCANLINES toggle visibly engages on/off (GAP-07 closed)
- UAT Test 8: /clear local, /research server-routed, plain message regression-clean (GAP-08 closed)
- UAT Test 9: SESSIONS wedge shows ~/.ironhermes/sessions/ catalog (GAP-09 closed)
</verification>

<success_criteria>
- All four round-2 gaps (GAP-06, GAP-07, GAP-08, GAP-09) close
- 5 build gates exit 0 (host check, legacy-shell check, wasm32 check, bin tests, wave0_smoke)
- D-02 narrow gate: state.rs, ws.rs, protocol.rs unchanged; api.rs diff is exactly the user-approved Some→None lift plus comment
- 4 atomic commits land (one per Task 1-4); Task 5 is the verification gate (no commit)
- UAT-26.2.1.md round-2 entries updated to reflect closure
- Phase 26.2.1 ready to ship pending final `/gsd-verify-work` re-run
</success_criteria>

<output>
After completion, create `.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-13-SUMMARY.md` per the standard summary template. Include:
- Strategy chosen for GAP-06 (Option A z-index OR Option B bottom-offset, with the actual selector + value)
- Strategy chosen for GAP-07 (`!important` per default, OR a deeper specificity fix if discovered during Step B)
- The verbatim diff of the dispatch_slash function (before/after) and the call-site one-liner change
- The verbatim diff of api.rs lines 60-63 (Some→None) plus the user-approval comment block
- Confirmation that state.rs, ws.rs, protocol.rs are byte-for-byte unchanged
- Re-quote of the user-approval text from `26.2.1-UAT.md` §"Round-2 D-02 Decision" (verbatim) so future readers can audit the D-02 lift authorization without re-discovering the source
- Test count delta (5 mod.rs tests before → 5 after; one renamed: `unknown_command_arm_appends_error_bubble_locally` → `unknown_command_arm_falls_through_to_websocket`)
</output>
