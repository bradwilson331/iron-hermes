---
phase: 26.2.1
plan: 08
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/models.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/memory.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/soul.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/tools.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/schedules.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/gateway.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/office.rs
  - crates/iron_hermes_ui/src/components/hermes_app/screens/providers.rs
autonomous: true
decisions: [D-04, D-05, D-06]
tags: [dioxus, rust, wasm, stub-screens, mocks, visual-port]

must_haves:
  truths:
    - "All 10 visual stub screens (agents, skills, models, memory, soul, tools, schedules, gateway, office, providers) replace their Plan 03 placeholders with the pixel-faithful layout from the corresponding `<section id=\"screen-XXX\">` in app.html (or for providers, from `project/screens/Providers.tsx` per D-05)"
    - "Each stub screen sources its data from one of the 10 factory fns in `crates/iron_hermes_ui/src/mocks/stub_data.rs` (Plan 02 output) — no server function calls, no `use_resource`, no `use_websocket`"
    - "Each stub screen takes a single `is_active: bool` prop and preserves Plan 03's `class: if is_active { \"is-active\" }` toggle"
    - "src/server/ws.rs, src/server/api.rs, src/server/state.rs, src/protocol.rs are byte-for-byte unchanged (D-02)"
    - "No new server functions are added (D-04 visual stubs only)"
  artifacts:
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs"
      provides: "Pixel-faithful Agents screen rendering `stub_data::agents()` rows from app.html line 501"
      contains: "pub fn ScreenAgents"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs"
      provides: "Pixel-faithful Skills screen rendering `stub_data::skills()` rows from app.html line 594"
      contains: "pub fn ScreenSkills"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/models.rs"
      provides: "Pixel-faithful Models screen rendering `stub_data::models()` rows from app.html line 691"
      contains: "pub fn ScreenModels"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/memory.rs"
      provides: "Pixel-faithful Memory screen rendering `stub_data::memory_entries()` rows from app.html line 797"
      contains: "pub fn ScreenMemory"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/soul.rs"
      provides: "Pixel-faithful Soul screen rendering `stub_data::soul_personas()` rows from app.html line 885"
      contains: "pub fn ScreenSoul"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/tools.rs"
      provides: "Pixel-faithful Tools screen rendering `stub_data::tools()` rows from app.html line 960"
      contains: "pub fn ScreenTools"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/schedules.rs"
      provides: "Pixel-faithful Schedules screen rendering `stub_data::schedules()` rows from app.html line 1074"
      contains: "pub fn ScreenSchedules"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/gateway.rs"
      provides: "Pixel-faithful Gateway screen rendering `stub_data::gateway_platforms()` rows from app.html line 1150"
      contains: "pub fn ScreenGateway"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/office.rs"
      provides: "Pixel-faithful Office screen rendering `stub_data::office_workspaces()` rows from app.html line 1266"
      contains: "pub fn ScreenOffice"
    - path: "crates/iron_hermes_ui/src/components/hermes_app/screens/providers.rs"
      provides: "Pixel-faithful Providers screen rendering `stub_data::providers()` rows from project/screens/Providers.tsx (D-05)"
      contains: "pub fn ScreenProviders"
  key_links:
    - from: "crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs"
      to: "crates/iron_hermes_ui/src/mocks/stub_data.rs"
      via: "use crate::mocks::stub_data::agents;"
      pattern: "stub_data::agents"
    - from: "crates/iron_hermes_ui/src/components/hermes_app/screens/providers.rs"
      to: "crates/iron_hermes_ui/src/mocks/stub_data.rs"
      via: "use crate::mocks::stub_data::providers;"
      pattern: "stub_data::providers"

user_setup: []
---

<objective>
Replace the 10 Plan 03 visual-stub placeholders with pixel-faithful ports of the corresponding `<section>` panels from `app.html` (or, for Providers, from `project/screens/Providers.tsx` per D-05 — `app.html` has no `#screen-providers`).

Per D-04: these screens are PIXEL-FAITHFUL VISUAL STUBS — they port the prototype HTML/JSX verbatim into RSX, source their data from `crate::mocks::stub_data::*` (the 10 factory functions delivered by Plan 02), and call ZERO server functions. They are the production path for the 10 non-live screens in 26.2.1; follow-up phases 26.2.2 through 26.2.11 progressively wire each one to a real backend. The screens are intentionally simple — pure render from a typed Vec.

Per D-06: when `app.html` and the React/TSX reference disagree on layout, **app.html wins**. The TSX references in `project/screens/*.tsx` are read for component-internal logic only.

The screens covered by this plan, in the order they appear under `screens/mod.rs` (defined by Plan 03 Task 2):

| File | Component | app.html section | stub fn | source notes |
|------|-----------|------------------|---------|--------------|
| agents.rs | ScreenAgents | line 501 | `stub_data::agents()` returning `Vec<AgentStub>` | rich card grid with avatar letter + status pill |
| skills.rs | ScreenSkills | line 594 | `stub_data::skills()` returning `Vec<SkillStub>` | category-grouped list with status badge |
| models.rs | ScreenModels | line 691 | `stub_data::models()` returning `Vec<ModelStub>` | family-grouped list with context window column |
| memory.rs | ScreenMemory | line 797 | `stub_data::memory_entries()` returning `Vec<MemoryEntryStub>` | scope/key/value-preview rows |
| soul.rs | ScreenSoul | line 885 | `stub_data::soul_personas()` returning `Vec<SoulPersonaStub>` | persona cards with "active" highlight |
| tools.rs | ScreenTools | line 960 | `stub_data::tools()` returning `Vec<ToolStub>` | group-then-tool nested layout |
| schedules.rs | ScreenSchedules | line 1074 | `stub_data::schedules()` returning `Vec<ScheduleStub>` | cron rows with last-run timestamp |
| gateway.rs | ScreenGateway | line 1150 | `stub_data::gateway_platforms()` returning `Vec<GatewayPlatformStub>` | platform status grid |
| office.rs | ScreenOffice | line 1266 | `stub_data::office_workspaces()` returning `Vec<OfficeWorkspaceStub>` | workspace cards |
| providers.rs | ScreenProviders | (no app.html section — port `project/screens/Providers.tsx`) | `stub_data::providers()` returning `Vec<ProviderStub>` | per D-05 |

Purpose: Closes D-04 (9 visual stubs from app.html) and D-05 (Providers from TSX). After this plan, every wheel wedge AND every Soul/Schedules/Office reachability link from Plan 07 lands on a screen that renders meaningful content — the phase is now visually complete. Future phases swap stub data sources for live server functions one screen at a time.

Output: A repository state where `cargo check -p iron_hermes_ui` passes, navigating to any non-live wedge renders the pixel-faithful prototype layout with hardcoded stub data, and zero bytes change under `src/server/` or `src/protocol.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-CONTEXT.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-RESEARCH.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-PATTERNS.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-UI-SPEC.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-VALIDATION.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-02-PLAN.md
@.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-03-PLAN.md

@crates/iron_hermes_ui/CLAUDE.md
@crates/iron_hermes_ui/AGENTS.md
@crates/iron_hermes_ui/clippy.toml

<!-- Plan 02 stub data factories — the data source for every screen here -->
@crates/iron_hermes_ui/src/mocks/stub_data.rs
@crates/iron_hermes_ui/src/mocks/mod.rs

<!-- Plan 03 placeholders being replaced -->
@crates/iron_hermes_ui/src/components/hermes_app/screens/mod.rs

<!-- Pattern analog for typed-Vec card rendering -->
@crates/iron_hermes_ui/src/components/shell_legacy/agent_panel.rs

<!-- Bundle structural spec — primary -->
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/app.html
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens.css
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/components.css

<!-- TSX references — secondary (component-internal logic only per D-06) -->
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Agents.tsx
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Skills.tsx
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Models.tsx
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Memory.tsx
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Soul.tsx
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Tools.tsx
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Schedules.tsx
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Gateway.tsx
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Office.tsx
@crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Providers.tsx

<interfaces>
After Plan 02 lands, `crate::mocks::stub_data::*` exposes:

```rust
// Each factory returns Vec<…Stub> with hardcoded rows sourced from app.html / Providers.tsx.
pub fn agents()            -> Vec<AgentStub>;
pub fn skills()            -> Vec<SkillStub>;
pub fn models()            -> Vec<ModelStub>;
pub fn memory_entries()    -> Vec<MemoryEntryStub>;
pub fn soul_personas()     -> Vec<SoulPersonaStub>;
pub fn tools()             -> Vec<ToolStub>;
pub fn schedules()         -> Vec<ScheduleStub>;
pub fn gateway_platforms() -> Vec<GatewayPlatformStub>;
pub fn office_workspaces() -> Vec<OfficeWorkspaceStub>;
pub fn providers()         -> Vec<ProviderStub>;

pub struct AgentStub { pub name: &'static str, pub avatar_letter: char, pub avatar_color: &'static str, pub status: &'static str, pub model: &'static str, pub skills_count: u32, pub summary: &'static str }
pub struct SkillStub { pub name: &'static str, pub category: &'static str, pub status: &'static str, pub summary: &'static str, pub version: &'static str }
pub struct ModelStub { pub id: &'static str, pub family: &'static str, pub context_window: u32, pub status: &'static str }
pub struct MemoryEntryStub { pub scope: &'static str, pub key: &'static str, pub value_preview: &'static str, pub updated: &'static str }
pub struct SoulPersonaStub { pub id: &'static str, pub label: &'static str, pub blurb: &'static str, pub active: bool }
pub struct ToolStub { pub name: &'static str, pub group: &'static str, pub status: &'static str, pub summary: &'static str }
pub struct ScheduleStub { pub id: &'static str, pub cron: &'static str, pub target: &'static str, pub status: &'static str, pub last_run: &'static str }
pub struct GatewayPlatformStub { pub name: &'static str, pub status: &'static str, pub chats_connected: u32 }
pub struct OfficeWorkspaceStub { pub name: &'static str, pub kind: &'static str, pub last_active: &'static str }
pub struct ProviderStub { pub id: &'static str, pub label: &'static str, pub status: &'static str, pub model_count: u32, pub latency_p50_ms: u32 }
```

After Plan 03, every screen file already exists as a placeholder with the shape:

```rust
use dioxus::prelude::*;

#[component]
pub fn ScreenXxx(is_active: bool) -> Element {
    rsx! {
        section {
            class: "screen",
            class: if is_active { "is-active" },
            id: "screen-xxx",
            "data-screen-label": "NN Xxx",
            div { class: "screen-placeholder",
                "Xxx screen — stubbed by Plan 08"
            }
        }
    }
}
```

This plan REPLACES the `div { class: "screen-placeholder", ... }` body with the real layout for each screen. The outer `section { ... class: if is_active ... id: ... data-screen-label: ... }` envelope MUST be preserved byte-for-identical (only the inner children change). Plan 03's ScreenRouter wiring depends on the outer attributes existing.

Per CONTEXT D-04 + RESEARCH §Notes-for-Planner #5, stub data factories are NOT `#[cfg(any(test, feature = "demo"))]`-gated — Plan 02 explicitly drops the demo gate for these. The factories are unconditional `pub fn`s.

Per PATTERNS Cross-cutting: stable RSX keys — every `for` loop must use a stable content-derived key:
- agents: `key: "{a.name}"`
- skills: `key: "{s.name}"`
- models: `key: "{m.id}"`
- memory_entries: `key: "{e.scope}::{e.key}"`
- soul_personas: `key: "{p.id}"`
- tools: `key: "{t.name}"`
- schedules: `key: "{sc.id}"`
- gateway_platforms: `key: "{g.name}"`
- office_workspaces: `key: "{o.name}"`
- providers: `key: "{p.id}"`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Port agents.rs, skills.rs, models.rs, memory.rs, soul.rs — 5 stub screens (app.html sections 501, 594, 691, 797, 885)</name>
  <files>
    crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs,
    crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs,
    crates/iron_hermes_ui/src/components/hermes_app/screens/models.rs,
    crates/iron_hermes_ui/src/components/hermes_app/screens/memory.rs,
    crates/iron_hermes_ui/src/components/hermes_app/screens/soul.rs
  </files>
  <read_first>
    crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs,
    crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs,
    crates/iron_hermes_ui/src/components/hermes_app/screens/models.rs,
    crates/iron_hermes_ui/src/components/hermes_app/screens/memory.rs,
    crates/iron_hermes_ui/src/components/hermes_app/screens/soul.rs,
    crates/iron_hermes_ui/src/mocks/stub_data.rs,
    crates/iron_hermes_ui/src/components/shell_legacy/agent_panel.rs,
    crates/iron_hermes_ui/CLAUDE.md,
    crates/iron_hermes_ui/AGENTS.md,
    crates/iron_hermes_ui/clippy.toml,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/app.html,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens.css,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/components.css,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Agents.tsx,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Skills.tsx,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Models.tsx,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Memory.tsx,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Soul.tsx,
    .planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-PATTERNS.md,
    .planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-RESEARCH.md
  </read_first>
  <action>
    For EACH of the five files (`agents.rs`, `skills.rs`, `models.rs`, `memory.rs`, `soul.rs`), perform the same replacement procedure. The outer `section { class: "screen", class: if is_active ..., id: "screen-XXX", "data-screen-label": "NN Xxx" }` envelope from the Plan 03 placeholder MUST be preserved byte-for-byte; only the inner placeholder div is replaced with the real layout.

    Common procedure for each screen:

    1. Read the corresponding `<section id="screen-XXX">` block from `app.html` (line numbers in the per-screen table below) — extract the inner HTML structure verbatim.
    2. Read the corresponding `project/screens/XXX.tsx` for component-internal logic hints (per D-06 — TSX informs structure but app.html wins on layout).
    3. Read the corresponding stub factory from `crate::mocks::stub_data` to confirm the data shape.
    4. Port the inner HTML to RSX preserving every `class:` attribute byte-for-byte (these are CSS selectors in the bundle's `screens.css` / `components.css` — invented class names will not style).
    5. Replace static rows in the prototype with a `for x in agents.iter() { ... }`-style loop binding each `XxxStub` field to the corresponding text node.
    6. Each iteration in the `for` loop emits a stable `key: "..."` per the table in `<interfaces>`.
    7. Imports needed at the top of each file: `use dioxus::prelude::*;` (Plan 03 already has this). Add `use crate::mocks::stub_data::{agents, AgentStub};` (or the matching factory + struct for that screen).

    Per-screen specifics:

    **agents.rs — Screen 03 Agents (app.html line 501):**
    - Read `app.html` lines 501..~593 for the section markup.
    - Read `project/screens/Agents.tsx` for the row-card internal structure (avatar block, name + status row, skills-count + model row, summary).
    - Bind `AgentStub` fields: name → name span, avatar_letter → avatar circle text, avatar_color → avatar circle inline style or class hook, status → status pill, model → model row value, skills_count → "{n} skills" hint, summary → bottom row.
    - Optional private sub-component `fn AgentCard(a: AgentStub) -> Element` (PATTERNS analog `agent_panel.rs:153-199` MemoryCard) — only if it makes the file noticeably more readable. The minimum-viable form is an inline `for` loop body.

    **skills.rs — Screen 04 Skills (app.html line 594):**
    - Read `app.html` lines 594..~690.
    - Read `project/screens/Skills.tsx` for the category-grouping logic. If the prototype groups skills by category (e.g. nested `for category in unique_categories { for skill in skills.iter().filter(...) { ... } }`), do the same; otherwise emit a flat list.
    - Bind `SkillStub`: name, category (group header or row label), status (badge), summary, version.

    **models.rs — Screen 05 Models (app.html line 691):**
    - Read `app.html` lines 691..~796.
    - Bind `ModelStub`: id (primary identifier text), family (group header or label column), context_window (numeric column — render as `"{m.context_window}"` or `"{m.context_window / 1000}K"` if the prototype uses K-suffix), status.

    **memory.rs — Screen 06 Memory (app.html line 797):**
    - Read `app.html` lines 797..~884.
    - Bind `MemoryEntryStub`: scope (left column label), key (mid column), value_preview (right column truncated text), updated (timestamp footer).
    - Key strategy: `key: "{e.scope}::{e.key}"` is stable assuming the stub data is unique per (scope, key) pair — Plan 02's factory must already enforce this; if not, fall back to enumerated index keys here only.

    **soul.rs — Screen 07 Soul (app.html line 885):**
    - Read `app.html` lines 885..~959.
    - Bind `SoulPersonaStub`: id (data-id attr), label (card title), blurb (card body), active (toggle `class: if p.active { "is-active" }` on the card).

    For each screen, the action consists of overwriting the existing Plan 03 file's body. The new file shape for `agents.rs` (example structure — repeat similar shape for the other 4):

    ```rust
    use dioxus::prelude::*;
    use crate::mocks::stub_data::{agents, AgentStub};

    #[component]
    pub fn ScreenAgents(is_active: bool) -> Element {
        let agents_list = agents();
        rsx! {
            section {
                class: "screen",
                class: if is_active { "is-active" },
                id: "screen-agents",
                "data-screen-label": "03 Agents",
                // INNER MARKUP PORTED FROM app.html lines 501..593 verbatim
                div { class: "agents-head",
                    span { class: "agents-title", "AGENTS" }
                    span { class: "agents-count", "{agents_list.len()}" }
                }
                div { class: "agents-grid",
                    for a in agents_list.iter() {
                        AgentCard { key: "{a.name}", agent: a.clone() }
                    }
                }
            }
        }
    }

    #[component]
    fn AgentCard(agent: AgentStub) -> Element {
        // Per-card layout ported from app.html agent row + Agents.tsx hints.
        rsx! {
            div { class: "agent-card",
                div { class: "agent-card-avatar",
                    style: "background: {agent.avatar_color};",
                    "{agent.avatar_letter}"
                }
                div { class: "agent-card-main",
                    div { class: "agent-card-head",
                        span { class: "agent-card-name", "{agent.name}" }
                        span { class: "agent-card-status", "{agent.status}" }
                    }
                    div { class: "agent-card-meta",
                        span { class: "agent-card-model", "{agent.model}" }
                        span { class: "agent-card-skills", "{agent.skills_count} skills" }
                    }
                    div { class: "agent-card-summary", "{agent.summary}" }
                }
            }
        }
    }
    ```

    The exact class names (`agents-head`, `agents-title`, `agents-count`, `agents-grid`, `agent-card`, `agent-card-avatar`, etc.) MUST come from `app.html` — if they differ in the prototype, use the prototype's names. The executor reads `app.html` line 501..593 first and copies the class names verbatim.

    Anti-patterns forbidden in this task:
    - Calling any server function or `use_resource` / `use_server_future` (D-04 — stub-only).
    - Calling `use_websocket` (D-04).
    - Mutating any signal — these screens are pure render (`use_context::<Signal<…>>()` is allowed only for `Signal<Screen>` if a screen wants to surface navigation, but none of the 5 in this task need it).
    - Using `key: "{i}"` index keys (PATTERNS Cross-cutting: stable RSX keys).
    - Inventing class names that aren't in `app.html` / `screens.css` / `components.css`.
    - Changing the outer `section { class: "screen", class: if is_active ..., id: ..., "data-screen-label": ... }` envelope from the Plan 03 placeholder (ScreenRouter depends on it).
    - Modifying any file under `src/server/` or `src/protocol.rs` (D-02).
  </action>
  <verify>
    <automated>cargo check -p iron_hermes_ui 2>&amp;1 | tail -30 &amp;&amp; cargo clippy -p iron_hermes_ui --all-features -- -D warnings 2>&amp;1 | tail -30 &amp;&amp; git diff HEAD -- crates/iron_hermes_ui/src/server/ crates/iron_hermes_ui/src/protocol.rs</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist with full layouts (Plan 03 placeholders replaced)
    - `screens/agents.rs` contains literal `pub fn ScreenAgents(is_active: bool)` AND `use crate::mocks::stub_data::agents` AND literal `id: "screen-agents"` AND literal `"data-screen-label": "03 Agents"`
    - `screens/skills.rs` contains literal `pub fn ScreenSkills(is_active: bool)` AND `use crate::mocks::stub_data::skills` AND literal `id: "screen-skills"` AND literal `"data-screen-label": "04 Skills"`
    - `screens/models.rs` contains literal `pub fn ScreenModels(is_active: bool)` AND `use crate::mocks::stub_data::models` AND literal `id: "screen-models"` AND literal `"data-screen-label": "05 Models"`
    - `screens/memory.rs` contains literal `pub fn ScreenMemory(is_active: bool)` AND `use crate::mocks::stub_data::memory_entries` AND literal `id: "screen-memory"` AND literal `"data-screen-label": "06 Memory"`
    - `screens/soul.rs` contains literal `pub fn ScreenSoul(is_active: bool)` AND `use crate::mocks::stub_data::soul_personas` AND literal `id: "screen-soul"` AND literal `"data-screen-label": "07 Soul"`
    - Each of the 5 files contains literal `class: if is_active { "is-active" }` (Plan 03 envelope preserved)
    - Each of the 5 files contains at least one `for ` loop iterating the corresponding stub_data Vec
    - Each `for` loop uses a stable content-derived key per the `<interfaces>` table (e.g. `key: "{a.name}"`, `key: "{s.name}"`, `key: "{m.id}"`, `key: "{e.scope}::{e.key}"`, `key: "{p.id}"`)
    - Source negative assertion across all 5 files: `grep -rn 'use_resource\|use_server_future\|use_websocket\|crate::server::' crates/iron_hermes_ui/src/components/hermes_app/screens/agents.rs crates/iron_hermes_ui/src/components/hermes_app/screens/skills.rs crates/iron_hermes_ui/src/components/hermes_app/screens/models.rs crates/iron_hermes_ui/src/components/hermes_app/screens/memory.rs crates/iron_hermes_ui/src/components/hermes_app/screens/soul.rs` returns no matches
    - Source negative assertion across all 5 files: no `key: "{i}"` index-based keys
    - Behavior: `git diff HEAD -- crates/iron_hermes_ui/src/server/ crates/iron_hermes_ui/src/protocol.rs` exits with empty output (D-02 byte-for-byte preservation)
    - Behavior: `cargo check -p iron_hermes_ui` exits 0
    - Behavior: `cargo check -p iron_hermes_ui --features legacy-shell` exits 0
    - Behavior: `cargo clippy -p iron_hermes_ui --all-features -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    Five visual stub screens (agents, skills, models, memory, soul) render their full pixel-faithful layouts from `app.html` with data sourced from `crate::mocks::stub_data::*`. Zero server function calls; zero server-side modifications. Both feature builds compile clean; clippy passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Port tools.rs, schedules.rs, gateway.rs, office.rs, providers.rs — 5 stub screens (app.html sections 960, 1074, 1150, 1266 + Providers.tsx)</name>
  <files>
    crates/iron_hermes_ui/src/components/hermes_app/screens/tools.rs,
    crates/iron_hermes_ui/src/components/hermes_app/screens/schedules.rs,
    crates/iron_hermes_ui/src/components/hermes_app/screens/gateway.rs,
    crates/iron_hermes_ui/src/components/hermes_app/screens/office.rs,
    crates/iron_hermes_ui/src/components/hermes_app/screens/providers.rs
  </files>
  <read_first>
    crates/iron_hermes_ui/src/components/hermes_app/screens/tools.rs,
    crates/iron_hermes_ui/src/components/hermes_app/screens/schedules.rs,
    crates/iron_hermes_ui/src/components/hermes_app/screens/gateway.rs,
    crates/iron_hermes_ui/src/components/hermes_app/screens/office.rs,
    crates/iron_hermes_ui/src/components/hermes_app/screens/providers.rs,
    crates/iron_hermes_ui/src/mocks/stub_data.rs,
    crates/iron_hermes_ui/src/components/shell_legacy/agent_panel.rs,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/app.html,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens.css,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/components.css,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Tools.tsx,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Schedules.tsx,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Gateway.tsx,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Office.tsx,
    crates/iron_hermes_ui/filestoimport/ironhermes-design-system/project/screens/Providers.tsx,
    .planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-PATTERNS.md,
    .planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-RESEARCH.md
  </read_first>
  <action>
    Same replacement procedure as Task 1, applied to the remaining 5 screens. The outer `section { ... }` envelope from the Plan 03 placeholder MUST be preserved byte-for-byte; only inner children change.

    Per-screen specifics:

    **tools.rs — Screen 08 Tools (app.html line 960):**
    - Read `app.html` lines 960..1073 for the section markup.
    - Read `project/screens/Tools.tsx` for the group-then-tool nested layout.
    - Bind `ToolStub`: name, group (header for grouped section), status, summary.
    - Grouping strategy: if the prototype groups tools by `group` field, emit a two-level `for` — outer iterates unique groups, inner iterates `tools_list.iter().filter(|t| t.group == g)`. Otherwise emit a flat list.
    - `data-screen-label`: "08 Tools".

    **schedules.rs — Screen 09 Schedules (app.html line 1074):**
    - Read `app.html` lines 1074..1149.
    - Bind `ScheduleStub`: id, cron (cron expression — monospace), target (action name), status, last_run (timestamp).
    - `data-screen-label`: "09 Schedules".

    **gateway.rs — Screen 10 Gateway (app.html line 1150):**
    - Read `app.html` lines 1150..1265.
    - Bind `GatewayPlatformStub`: name, status, chats_connected.
    - `data-screen-label`: "10 Gateway".

    **office.rs — Screen 11 Office (app.html line 1266):**
    - Read `app.html` lines 1266..1355.
    - Bind `OfficeWorkspaceStub`: name, kind, last_active.
    - `data-screen-label`: "11 Office".

    **providers.rs — Screen 13 Providers (no app.html section — port from `project/screens/Providers.tsx` per D-05):**
    - `app.html` has no `<section id="screen-providers">`. Per D-05, port the entire TSX file's render tree into RSX.
    - Read `project/screens/Providers.tsx` end-to-end. Treat the `return ( ... )` JSX block as the structural spec.
    - Bind `ProviderStub`: id, label, status, model_count, latency_p50_ms.
    - Outer envelope (already in the Plan 03 placeholder — preserve):
      ```rust
      section { class: "screen", class: if is_active { "is-active" }, id: "screen-providers", "data-screen-label": "13 Providers", /* inner from Providers.tsx */ }
      ```
    - Class names in Providers.tsx are JSX `className` — copy them as RSX `class:` attributes verbatim. If Providers.tsx uses CSS modules or Tailwind utility classes, fall back to the same flat `class:` strings — the bundle's `screens.css` / `components.css` style by class name regardless of source.
    - `data-screen-label`: "13 Providers".

    Each of the 5 files follows the same shape as the agents.rs example in Task 1's action. The skeleton (illustrated for tools.rs):

    ```rust
    use dioxus::prelude::*;
    use crate::mocks::stub_data::{tools, ToolStub};

    #[component]
    pub fn ScreenTools(is_active: bool) -> Element {
        let tools_list = tools();
        rsx! {
            section {
                class: "screen",
                class: if is_active { "is-active" },
                id: "screen-tools",
                "data-screen-label": "08 Tools",
                // INNER MARKUP PORTED FROM app.html lines 960..1073 verbatim
                div { class: "tools-head",
                    span { class: "tools-title", "TOOLS" }
                }
                div { class: "tools-grid",
                    for t in tools_list.iter() {
                        ToolRow { key: "{t.name}", tool: t.clone() }
                    }
                }
            }
        }
    }

    #[component]
    fn ToolRow(tool: ToolStub) -> Element {
        rsx! {
            div { class: "tool-row",
                span { class: "tool-row-group", "{tool.group}" }
                span { class: "tool-row-name", "{tool.name}" }
                span { class: "tool-row-status", "{tool.status}" }
                span { class: "tool-row-summary", "{tool.summary}" }
            }
        }
    }
    ```

    The exact class names must come from `app.html` (or Providers.tsx for providers.rs); the executor reads the source file before writing.

    Anti-patterns forbidden in this task (identical to Task 1):
    - No server function or `use_resource` / `use_server_future` / `use_websocket` calls.
    - No signal mutation.
    - No `key: "{i}"` index keys.
    - No inventing class names not in the bundle CSS.
    - No changes to the Plan 03 outer envelope.
    - No edits to `src/server/` or `src/protocol.rs` (D-02).
    - For providers.rs only: if Providers.tsx references React-specific helpers (e.g. `useMemo`, `useState`), DO NOT port them — they are component-internal logic that violates the visual-stub contract. Replace with plain Rust expressions (e.g. compute filtered Vec at the top of `ScreenProviders`).
  </action>
  <verify>
    <automated>cargo check -p iron_hermes_ui 2>&amp;1 | tail -30 &amp;&amp; cargo clippy -p iron_hermes_ui --all-features -- -D warnings 2>&amp;1 | tail -30 &amp;&amp; git diff HEAD -- crates/iron_hermes_ui/src/server/ crates/iron_hermes_ui/src/protocol.rs</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist with full layouts (Plan 03 placeholders replaced)
    - `screens/tools.rs` contains literal `pub fn ScreenTools(is_active: bool)` AND `use crate::mocks::stub_data::tools` AND literal `id: "screen-tools"` AND literal `"data-screen-label": "08 Tools"`
    - `screens/schedules.rs` contains literal `pub fn ScreenSchedules(is_active: bool)` AND `use crate::mocks::stub_data::schedules` AND literal `id: "screen-schedules"` AND literal `"data-screen-label": "09 Schedules"`
    - `screens/gateway.rs` contains literal `pub fn ScreenGateway(is_active: bool)` AND `use crate::mocks::stub_data::gateway_platforms` AND literal `id: "screen-gateway"` AND literal `"data-screen-label": "10 Gateway"`
    - `screens/office.rs` contains literal `pub fn ScreenOffice(is_active: bool)` AND `use crate::mocks::stub_data::office_workspaces` AND literal `id: "screen-office"` AND literal `"data-screen-label": "11 Office"`
    - `screens/providers.rs` contains literal `pub fn ScreenProviders(is_active: bool)` AND `use crate::mocks::stub_data::providers` AND literal `id: "screen-providers"` AND literal `"data-screen-label": "13 Providers"`
    - Each of the 5 files contains literal `class: if is_active { "is-active" }` (Plan 03 envelope preserved)
    - Each of the 5 files contains at least one `for ` loop iterating the corresponding stub_data Vec
    - Each `for` loop uses a stable content-derived key per the `<interfaces>` table (e.g. `key: "{t.name}"`, `key: "{sc.id}"`, `key: "{g.name}"`, `key: "{o.name}"`, `key: "{p.id}"`)
    - Source negative assertion across all 5 files: `grep -rn 'use_resource\|use_server_future\|use_websocket\|crate::server::' crates/iron_hermes_ui/src/components/hermes_app/screens/tools.rs crates/iron_hermes_ui/src/components/hermes_app/screens/schedules.rs crates/iron_hermes_ui/src/components/hermes_app/screens/gateway.rs crates/iron_hermes_ui/src/components/hermes_app/screens/office.rs crates/iron_hermes_ui/src/components/hermes_app/screens/providers.rs` returns no matches
    - Source negative assertion across all 5 files: no `key: "{i}"` index-based keys
    - Source negative assertion across `providers.rs`: does NOT contain `useMemo\|useState\|useEffect\|useRef` (React hooks must not be ported per D-04 visual-stub rule)
    - Behavior: `git diff HEAD -- crates/iron_hermes_ui/src/server/ crates/iron_hermes_ui/src/protocol.rs` exits with empty output (D-02 byte-for-byte preservation)
    - Behavior: `cargo check -p iron_hermes_ui` exits 0
    - Behavior: `cargo check -p iron_hermes_ui --features legacy-shell` exits 0
    - Behavior: `cargo clippy -p iron_hermes_ui --all-features -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    Five more visual stub screens (tools, schedules, gateway, office, providers) render their full pixel-faithful layouts from `app.html` (or Providers.tsx for providers.rs) with data sourced from `crate::mocks::stub_data::*`. Combined with Task 1, all 10 stubs are live. Zero server function calls; zero server-side modifications. Both feature builds compile clean; clippy passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Stub screens render hardcoded `&'static str` values from `stub_data.rs` — no user input, no network calls, no DOM-side input fields. There are no trust boundaries beyond what Plan 02 already accounts for. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.2.1-29 | Tampering | XSS via stub data containing HTML/script literals | accept | Stub data is hand-authored static text in `stub_data.rs` — values are `&'static str` controlled by the developer at compile time, not user input. Dioxus rsx! also auto-escapes text inside `"{field}"` expressions, providing a second layer (T-XSS-CSS from RESEARCH §Security). No `dangerous_inner_html` is used (acceptance grep enforces). |
| T-26.2.1-30 | Information Disclosure | Stub screens leaking that "this is fake data" to a curious user | accept | The screens are intentionally pixel-faithful visual stubs per D-04 — the user knows they are scaffolding because the data is identical across reloads and the screens have no interactive write paths. Future phases 26.2.2..26.2.11 progressively wire each screen to a real backend. |
| T-26.2.1-31 | Tampering | A stub screen accidentally calling a server function (would violate D-04) | mitigate | Per-file negative grep `use_resource\|use_server_future\|use_websocket\|crate::server::` enforces the visual-stub contract structurally in every acceptance run. |
| T-26.2.1-32 | Privilege Escalation | A stub screen mutating any backend state via implicit side effects | mitigate | The forbidden imports above include the entire `crate::server::` path tree; any future drift toward live wiring requires removing the negative grep from acceptance criteria — visible in diff review. Plan 26.2.x will explicitly relax this gate for the specific screen it wires. |
</threat_model>

<verification>
Per VALIDATION.md Sampling Rate: `cargo check -p iron_hermes_ui` + `cargo clippy -p iron_hermes_ui --all-features -- -D warnings` after each task. The git-diff guard on `src/server/` + `src/protocol.rs` enforces D-02 byte-for-byte preservation at every verify pass.

Per VALIDATION.md Per-Task Verification Map TBD-10: this plan satisfies D-04 (9 visual stubs from app.html — Agents/Skills/Models/Memory/Soul/Tools/Schedules/Gateway/Office) + D-05 (Providers from TSX). Combined with Plan 07 (Sessions, Settings) and Plan 06 (Chat), all 13 screens are live or stubbed.

Manual UAT additions for this plan (added to UAT-26.2.1.md in Plan 09):
- Click each of the 10 non-live wedges in turn (agents, skills, models, memory, tools, gateway) plus the three non-wheel screens (soul, schedules, office) via Settings → Other Screens, plus the wheel-reachable Providers wedge.
- For each, confirm: the screen renders content (not the Plan 03 placeholder div), the data shape matches the prototype (cards / rows / grids visually distinguishable per screen), and no console errors appear.
- For Memory: confirm memory entries render with scope + key + value preview.
- For Schedules: confirm cron expressions render in monospace alignment with timestamps.
- For Providers: confirm the screen renders despite app.html having no providers section (D-05 — sourced from Providers.tsx).
</verification>

<success_criteria>
- `cargo check -p iron_hermes_ui` and `cargo check -p iron_hermes_ui --features legacy-shell` both exit 0
- `cargo clippy -p iron_hermes_ui --all-features -- -D warnings` exits 0
- `git diff HEAD -- crates/iron_hermes_ui/src/server/ crates/iron_hermes_ui/src/protocol.rs` is empty (D-02 enforcement)
- All 10 stub screen files contain full layouts ported verbatim from `app.html` (or Providers.tsx for providers.rs)
- Every screen imports its corresponding `crate::mocks::stub_data::*` factory and binds the stub data Vec into the RSX via a `for` loop with stable content-derived keys
- No screen calls any server function, `use_resource`, `use_server_future`, or `use_websocket` (acceptance grep enforces)
- Every screen preserves Plan 03's outer envelope (class:"screen" + is_active class-toggle + id + data-screen-label)
</success_criteria>

<output>
After completion, create `.planning/phases/26.2.1-new-web-ui-with-wheel-menu/26.2.1-08-SUMMARY.md` capturing: per-screen line range copied from app.html (or TSX for providers), any class names actually used per screen (with rationale if they diverged from the prototype), row counts rendered per screen (sourced from stub_data factories), confirmation D-02 holds (empty git diff for server/ tree), confirmation no React hooks were ported into providers.rs, and confirmation both feature builds + clippy pass.
</output>
