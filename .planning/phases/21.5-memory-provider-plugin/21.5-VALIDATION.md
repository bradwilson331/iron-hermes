---
phase: 21.5
slug: memory-provider-plugin
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-21
---

# Phase 21.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | cargo test (Rust built-in) |
| **Config file** | Cargo.toml (workspace) |
| **Quick run command** | `cargo test -p ironhermes-agent -p memory-sqlite -p memory-grafeo -p memory-duckdb --lib` |
| **Full suite command** | `cargo test --workspace` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-agent -p memory-sqlite -p memory-grafeo -p memory-duckdb --lib`
- **After every plan wave:** Run `cargo test --workspace`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | D-01..D-13 | — | N/A | unit/integration | TBD | ⬜ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Populated after PLAN.md files are created.*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements — cargo test framework and provider crate test modules already exist.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| memory_recall returns ranked results with relevance | D-05 | Requires LLM interaction to validate result quality | Call memory_recall via CLI, inspect JSON result format |
| system_prompt_block visible in LLM context | D-10 | Requires LLM session to see injected prompt block | Start session with each provider, check system prompt |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
