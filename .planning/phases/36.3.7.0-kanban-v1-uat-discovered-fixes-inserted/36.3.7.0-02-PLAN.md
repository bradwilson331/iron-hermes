---
phase: 36.3.7.0
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-core/src/commands/context.rs
  - crates/ironhermes-core/src/commands/handlers.rs
  - crates/ironhermes-cli/src/main.rs
  - crates/ironhermes-cli/src/kanban/store_reader_impl.rs
  - crates/ironhermes-cli/src/kanban/mod.rs
  - crates/ironhermes-core/tests/handlers_kanban.rs
autonomous: true
requirements:
  - BUG-36.3.7-02
deferred_subverbs:
  - claim
  - complete
  - block
  - unblock
  - comment
  - archive
  - reclaim
  - reassign
  - assign
  - link
  - unlink
  - create
  - init
  - tail
  - watch
  - runs
  - assignees
  - dispatch
  - stats
  - log
  - context
  - gc
  - daemon
  - diagnostics
user_setup: []

must_haves:
  truths:
    - "Typing `/kanban list` in an interactive `ironhermes chat` REPL returns the CLI table-format output (column headers `STATUS` and `ASSIGNEE` visible) directly, without an LLM round-trip and without the `todo_stub` 'not yet available' message."
    - "Typing `/kanban show <task_id>` returns the detail view (the `format_task_detail` output shape with `STATUS:`, `ASSIGNEE:`, `RUNS:` blocks)."
    - "Typing `/kanban tip` returns the scratch-workspace discoverability tip text."
    - "Typing `/kanban claim t_fake` (or any other deferred subverb) returns a message of the form `/kanban claim: use 'ironhermes kanban claim' from the CLI for v1.` — NOT the catch-all `todo_stub` 'is not yet available' shape."
    - "`CommandContext.kanban_store` field exists as `Option<Arc<dyn KanbanStoreReader>>`, mirroring `cron_store: Option<Arc<dyn CronJobReader>>`; `with_kanban_store` builder method exists."
    - "`build_cmd_ctx` in `crates/ironhermes-cli/src/main.rs` opens `KanbanStore::open_default()` once and attaches it to every `CommandContext` it returns (best-effort: on open failure the field stays None and the handler returns the standard 'not configured' message, matching `cmd_cron`'s line 1064 fallback shape)."
  artifacts:
    - path: "crates/ironhermes-core/src/commands/context.rs"
      provides: "KanbanStoreReader trait + CommandContext.kanban_store field + with_kanban_store builder (mirrors CronJobReader cycle-break pattern)"
      contains: "pub trait KanbanStoreReader"
    - path: "crates/ironhermes-core/src/commands/handlers.rs"
      provides: "cmd_kanban handler + dispatch arm; list/show/tip active subverbs + 24 deferred subverbs routed to 'use the CLI' message"
      contains: "fn cmd_kanban"
    - path: "crates/ironhermes-cli/src/main.rs"
      provides: "build_cmd_ctx populated with kanban_store via KanbanStore::open_default() best-effort"
      contains: ".with_kanban_store"
    - path: "crates/ironhermes-cli/src/kanban/store_reader_impl.rs"
      provides: "KanbanStoreReaderImpl — Arc<Mutex<KanbanStore>> wrapper implementing the KanbanStoreReader trait"
      exports: ["KanbanStoreReaderImpl"]
    - path: "crates/ironhermes-core/tests/handlers_kanban.rs"
      provides: "Receiver-end dispatch-chain test: dispatch_command + FakeKanbanStoreReader returns table-format output (NOT 'not yet available')"
      exports: ["dispatch_kanban_list_returns_table_format", "dispatch_kanban_show_returns_detail", "dispatch_kanban_tip_returns_tip_text", "dispatch_kanban_deferred_subverb_routes_to_cli_message", "dispatch_kanban_unknown_subverb_typo_suggests"]
  key_links:
    - from: "crates/ironhermes-core/src/commands/handlers.rs::dispatch (the giant match at line 18)"
      to: "crates/ironhermes-core/src/commands/handlers.rs::cmd_kanban (new function)"
      via: "new dispatch arm: '\"kanban\" => cmd_kanban(args, ctx),'"
      pattern: "\"kanban\" => cmd_kanban"
    - from: "crates/ironhermes-core/src/commands/handlers.rs::cmd_kanban"
      to: "crates/ironhermes-core/src/commands/context.rs::KanbanStoreReader"
      via: "ctx.kanban_store.as_ref()"
      pattern: "ctx\\.kanban_store"
    - from: "crates/ironhermes-cli/src/main.rs::build_cmd_ctx"
      to: "crates/ironhermes-cli/src/kanban/store_reader_impl.rs::KanbanStoreReaderImpl"
      via: ".with_kanban_store(Arc::new(KanbanStoreReaderImpl::new(...)))"
      pattern: "with_kanban_store"
---

<objective>
Close BUG-36.3.7-02 (per UAT-09-B FAIL at step 4, captured in `.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-09-SUMMARY.md` Addendum). The `/kanban` slash command is registered as a `CommandDef` (registry.rs:215) AND in the bypass list (running_agent.rs:48), but `handlers.rs::dispatch` has no `"kanban" =>` arm. The `name => todo_stub(name)` catch-all (handlers.rs:86) intercepts and returns `"/kanban is not yet available. (Not implemented)"`.

Decision (CONTEXT.md BUG-36.3.7-02 locked): add `fn cmd_kanban(args, ctx)` mirroring `cmd_cron` (handlers.rs:1062), route subverbs to in-process `KanbanStore` calls via a new trait-object handle `Arc<dyn KanbanStoreReader>` declared in `ironhermes-core::commands::context` (mirrors `Arc<dyn CronJobReader>` for cycle-break). CLI provides `KanbanStoreReaderImpl` wrapping `Arc<Mutex<KanbanStore>>` and reusing the existing `format_task_table` / `format_task_detail` formatters. `build_cmd_ctx` opens the store once at session start.

**Subverb coverage (CONTEXT.md MINIMUM):** `list`, `show <id>`, `tip` are the active subverbs for UAT-09-B PASS. ALL other CLI verbs (24 of them — frontmatter `deferred_subverbs`) MUST route to a dedicated deferred-subverb message: `/kanban <verb>: use 'ironhermes kanban <verb>' from the CLI for v1.` NOT to `todo_stub` (the catch-all is what hid this bug from grep-based verifiers — Meta-finding for Plan 04 SUMMARY). A typo (e.g. `/kanban garbage-verb`) goes through `suggest_typo` mirroring `cmd_cron` line 1120.

Purpose: unblock UAT-09-B's interactive `/kanban list` path so the slash command surface matches the registered CommandDef surface.
Output: KanbanStoreReader trait + field + builder + impl + cmd_kanban + dispatch arm + receiver test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/36.3.7.0-kanban-v1-uat-discovered-fixes-inserted/36.3.7.0-CONTEXT.md
@.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-09-SUMMARY.md
@crates/ironhermes-kanban/src/lib.rs
@crates/ironhermes-cli/src/kanban/format.rs
@crates/ironhermes-cli/src/kanban/mod.rs

<interfaces>
<!-- Existing code surface — extracted from source. Executor uses these directly, no exploration. -->

From `crates/ironhermes-core/src/commands/context.rs:193` (CronJobReader precedent — mirror exactly):
```rust
pub trait CronJobReader: Send + Sync {
    fn list_jobs_text(&self) -> String;
    fn get_job_text(&self, id_or_name: &str) -> Option<String>;
    fn status_text(&self) -> String;
    fn pause_job(&self, id_or_name: &str) -> Result<String, String>;
    fn resume_job(&self, id_or_name: &str) -> Result<String, String>;
    fn remove_job(&self, id_or_name: &str) -> Result<String, String>;
    fn queue_run(&self, id_or_name: &str) -> Result<String, String>;
}
```

From `crates/ironhermes-core/src/commands/context.rs:395-606`:
- Line 448: `pub cron_store: Option<Arc<dyn CronJobReader>>`
- Line 496: `cron_store: None,` (in `::new`)
- Line 603: `pub fn with_cron_store(mut self, store: Arc<dyn CronJobReader>) -> Self { self.cron_store = Some(store); self }`

From `crates/ironhermes-core/src/commands/handlers.rs:1062-1129` — `cmd_cron` (the structural mirror):
```rust
fn cmd_cron(args: &[&str], ctx: &CommandContext) -> CommandResult {
    let store = match &ctx.cron_store {
        Some(s) => s.clone(),
        None => return CommandResult::Output("/cron: cron store not configured.".to_string()),
    };
    match args.first().copied() {
        None | Some("list") => CommandResult::Output(store.list_jobs_text()),
        // ...subverb arms...
        Some(other) => {
            let candidates: &[&str] = &["list", "status", "get", "pause", "resume", "run", "remove"];
            let suffix = suggest_typo(other, candidates).map(|s| format!(" {}", s)).unwrap_or_default();
            CommandResult::Error(format!("Unknown /cron subcommand: {}{}", other, suffix))
        }
    }
}
```

From `crates/ironhermes-core/src/commands/handlers.rs:71`:
```text
"cron" => cmd_cron(args, ctx),
```

From `crates/ironhermes-kanban/src/lib.rs` re-exports + `crates/ironhermes-kanban/src/store.rs`:
```rust
pub use store::{CreateTaskOptions, KanbanStore, ListFilters};
pub use types::{Task, TaskComment, TaskLink, TaskRun};
// KanbanStore:
//   pub fn open_default() -> Result<Self>;
//   pub fn list_tasks(&self, filters: ListFilters) -> Result<Vec<Task>>;     // store.rs:305
//   pub fn get_task(&self, id: &str) -> Result<Task>;                         // store.rs:287
//   pub fn get_events(&self, task_id: &str) -> Result<Vec<KanbanEvent>>;      // store.rs:754
```

From `crates/ironhermes-cli/src/kanban/format.rs` (public — REUSE; do NOT reimplement):
```rust
pub fn format_task_table(tasks: &[Task]) -> String;
// Headers: ID | STATUS | ASSIGNEE | PRIORITY | TITLE (per source line 42-43)
pub fn format_task_detail(task: &Task, runs: &[TaskRun], comments: &[TaskComment]) -> String;
// Output includes "STATUS:" and "ASSIGNEE:" labeled lines (source line 154-155)
```

From `crates/ironhermes-cli/src/main.rs:1010-1076` — `build_cmd_ctx` signature + body (add `kanban_store` param + conditional `.with_kanban_store(...)` block mirroring lines 1064-1068).

From `crates/ironhermes-cli/src/main.rs:1624,2020` — the two `build_cmd_ctx` call sites in `run_chat` (prompt-time + mid-turn). Plan 02 threads `Some(kanban_store_handle.clone())` to both.

From `crates/ironhermes-cli/src/main.rs:875` (run_single comment): "no user can trigger a slash command mid-batch. Dropping it is intentional." Plan 02 does NOT change `run_single` per CONTEXT.md scope_fences.

From `crates/ironhermes-cli/src/tui_rata/commands.rs:536` — `impl CronJobReader for CronJobReaderImpl` — the structural precedent for `KanbanStoreReaderImpl`.

From `crates/ironhermes-core/tests/handlers_cron.rs:33` — `impl CronJobReader for FakeCronJobReader` — the test fixture pattern Task 4 mirrors for FakeKanbanStoreReader.
</interfaces>

<existing_pattern>
Two precedents drive every shape in this plan:

1. **CronJobReader (Phase 22.4.2.1 Plan 01)** — trait declared in `ironhermes-core::commands::context` (cycle-break), impl in `ironhermes-cli/src/tui_rata/commands.rs`, field on `CommandContext`, builder method, handler routing. Plan 02 mirrors this verbatim with `Kanban*` substituted for `Cron*`.

2. **Deferred-subverb routing (new in Plan 02)** — distinct from `cmd_cron`'s typo-suggest fallback. KNOWN deferred names route to a deterministic CLI-redirect message; UNKNOWN names go through typo-suggest like `cmd_cron`. The split matters per CONTEXT.md Meta-finding: the catch-all `todo_stub` hid BUG-36.3.7-02 from grep-based verifiers — Plan 02 routes known-deferred names to a distinct dispatch arm so future grep-based verifiers see them.
</existing_pattern>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare KanbanStoreReader trait + CommandContext.kanban_store field + with_kanban_store builder (ironhermes-core)</name>
  <read_first>
    - crates/ironhermes-core/src/commands/context.rs (full file: trait shapes at lines 185-232, struct + ::new + builders at 390-610)
    - crates/ironhermes-core/tests/handlers_cron.rs (FakeCronJobReader pattern — template for Task 4)
  </read_first>
  <files>crates/ironhermes-core/src/commands/context.rs</files>
  <behavior>
    No tests in this task — trait + field + builder additions only. Compilation success on `cargo check -p ironhermes-core` is the gate. Test coverage lands in Task 4.

    Acceptance:
    - `pub trait KanbanStoreReader: Send + Sync` declared in `crates/ironhermes-core/src/commands/context.rs`, with four methods:
      - `fn list_text(&self) -> String;`
      - `fn show_text(&self, id: &str) -> Option<String>;`
      - `fn tip_text(&self) -> String;`
      - `fn deferred_subverb_message(&self, name: &str) -> String;`
    - `pub kanban_store: Option<Arc<dyn KanbanStoreReader>>` field on `CommandContext`.
    - `kanban_store: None,` in `CommandContext::new`.
    - `pub fn with_kanban_store(mut self, store: Arc<dyn KanbanStoreReader>) -> Self { self.kanban_store = Some(store); self }`.
    - All three placed adjacent to their `Cron*` analogs (trait after CronJobReader at ~201; field after cron_store at ~448; builder after with_cron_store at ~606).
    - Doc comment names "Phase 36.3.7.0 Plan 02 (BUG-36.3.7-02)" and "Mirrors CronJobReader (Phase 22.4.2.1 Plan 01) for cycle-break."
  </behavior>
  <action>
    Edit `crates/ironhermes-core/src/commands/context.rs`:
    1. After the closing `}` of the `CronJobReader` trait (~line 201), insert a section header `// ============================================================================ // KanbanStoreReader trait — Phase 36.3.7.0 Plan 02 (BUG-36.3.7-02) // ============================================================================` followed by the trait declaration with the four methods listed in `<behavior>`. Doc comment: "Mirrors CronJobReader (Phase 22.4.2.1 Plan 01) for cycle-break — declared here in ironhermes-core so CommandContext can hold a kanban store handle without core depending on ironhermes-kanban."
    2. After `pub cron_store: Option<Arc<dyn CronJobReader>>` (line 448), insert `pub kanban_store: Option<Arc<dyn KanbanStoreReader>>,` with doc comment "Phase 36.3.7.0 Plan 02: KanbanStoreReader handle for `/kanban` slash UI. Option<Arc<dyn>> to avoid circular dep with ironhermes-kanban."
    3. In `CommandContext::new` after `cron_store: None,` (line 496), insert `kanban_store: None,`.
    4. After `with_cron_store` (line 606), insert `pub fn with_kanban_store(mut self, store: Arc<dyn KanbanStoreReader>) -> Self { self.kanban_store = Some(store); self }` with doc comment naming Phase 36.3.7.0 Plan 02.
    5. Run `cargo check -p ironhermes-core` — must succeed.
    6. Run `cargo test -p ironhermes-core --lib` — must pass with no regression.
    Commit: `feat(36.3.7.0-02-task1): add KanbanStoreReader trait + CommandContext.kanban_store field + with_kanban_store builder`.
  </action>
  <verify>
    <automated>cargo check -p ironhermes-core 2>&amp;1 | tail -5 &amp;&amp; grep -nE "pub trait KanbanStoreReader" crates/ironhermes-core/src/commands/context.rs &amp;&amp; grep -nE "pub kanban_store:" crates/ironhermes-core/src/commands/context.rs &amp;&amp; grep -nE "pub fn with_kanban_store" crates/ironhermes-core/src/commands/context.rs</automated>
  </verify>
  <done>
    - Trait, field, builder all present adjacent to Cron* analogs.
    - `cargo check -p ironhermes-core` succeeds.
    - `cargo test -p ironhermes-core --lib` passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cmd_kanban handler + add dispatch arm</name>
  <read_first>
    - crates/ironhermes-core/src/commands/context.rs (verify Task 1 trait + builder landed)
    - crates/ironhermes-core/src/commands/handlers.rs (lines 1-95 for the dispatch match; lines 1058-1129 for cmd_cron; lines 1630-1656 for todo_stub)
    - crates/ironhermes-core/src/commands/typo.rs (verify suggest_typo signature)
  </read_first>
  <files>crates/ironhermes-core/src/commands/handlers.rs</files>
  <behavior>
    Tests live in Task 4 (the receiver-end test exercises the full dispatch chain via FakeKanbanStoreReader).

    Acceptance:
    - `fn cmd_kanban(args: &[&str], ctx: &CommandContext) -> CommandResult` after `cmd_cron` (around line 1130).
    - Dispatch arm `"kanban" => cmd_kanban(args, ctx),` added at line 72 (right after the `"cron"` arm at 71).
    - Subverb routing:
      - `None | Some("list")` → `CommandResult::Output(store.list_text())`
      - `Some("show")` → require `args.get(1)`; on missing → `CommandResult::Error("/kanban show <id>: missing id")`; on present route to `store.show_text(id)` → `Some(text)` to Output, `None` to `CommandResult::Error(format!("No kanban task found: {}", id))`.
      - `Some("tip")` → `CommandResult::Output(store.tip_text())`
      - `Some(other)` where `other` is in `DEFERRED_KANBAN_SUBVERBS` → `CommandResult::Output(store.deferred_subverb_message(other))`
      - `Some(other)` otherwise → typo-suggest with candidates `["list", "show", "tip"]` (the ACTIVE subverbs only — deferred list is consulted before typo-suggest). Error format `"Unknown /kanban subcommand: {}{}"`.
    - Fallback when `ctx.kanban_store` is None: `CommandResult::Output("/kanban: kanban store not configured.")` mirroring `cmd_cron` line 1064.
  </behavior>
  <action>
    Edit `crates/ironhermes-core/src/commands/handlers.rs`:
    1. After line 71 (`"cron" => cmd_cron(args, ctx),`), insert `"kanban" => cmd_kanban(args, ctx),` with trailing comment `// Phase 36.3.7.0 Plan 02 (BUG-36.3.7-02) — /kanban slash command dispatch.`
    2. After `cmd_cron`'s closing brace (~line 1129), insert `fn cmd_kanban(args: &[&str], ctx: &CommandContext) -> CommandResult { ... }`. Body mirrors `cmd_cron` exactly: store fetch (with None fallback) → `match args.first().copied()` with the arms above.
    3. Place `const DEFERRED_KANBAN_SUBVERBS: &[&str] = &["claim", "complete", "block", "unblock", "comment", "archive", "reclaim", "reassign", "assign", "link", "unlink", "create", "init", "tail", "watch", "runs", "assignees", "dispatch", "stats", "log", "context", "gc", "daemon", "diagnostics"];` at the top of the `cmd_kanban` function body (or as a file-scope const directly above the function). 24 entries — exact match with frontmatter `deferred_subverbs`.
    4. In the `Some(other)` arm, check `DEFERRED_KANBAN_SUBVERBS.contains(&other)` FIRST; on hit return `CommandResult::Output(store.deferred_subverb_message(other))`. On miss fall through to typo-suggest with `&["list", "show", "tip"]`.
    5. Doc comment above `cmd_kanban`: "Phase 36.3.7.0 Plan 02 (BUG-36.3.7-02) — /kanban slash command sub-dispatch. Mirrors `cmd_cron` (handlers.rs:1062). v1 active subverbs: list / show / tip. Deferred operator-recovery subverbs route to a 'use the CLI verb directly' message — NOT to `todo_stub` (which would hide receiver-end gaps per the 36.3.7.0 Meta-finding)."
    6. `cargo check -p ironhermes-core` — must succeed.
    Commit: `feat(36.3.7.0-02-task2): add cmd_kanban + dispatch arm — list/show/tip + 24 deferred-subverb routing`.
  </action>
  <verify>
    <automated>cargo check -p ironhermes-core 2>&amp;1 | tail -5 &amp;&amp; grep -nE '"kanban" =&gt; cmd_kanban' crates/ironhermes-core/src/commands/handlers.rs &amp;&amp; grep -nE "fn cmd_kanban" crates/ironhermes-core/src/commands/handlers.rs &amp;&amp; grep -nE "DEFERRED_KANBAN_SUBVERBS" crates/ironhermes-core/src/commands/handlers.rs</automated>
  </verify>
  <done>
    - Dispatch arm routes `"kanban"` to `cmd_kanban`.
    - `cmd_kanban` exists with four arms (None/list, show, tip, deferred-or-typo).
    - `DEFERRED_KANBAN_SUBVERBS` const has exactly 24 entries.
    - `cargo check -p ironhermes-core` succeeds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement KanbanStoreReaderImpl + wire kanban_store through build_cmd_ctx</name>
  <read_first>
    - crates/ironhermes-cli/src/main.rs (lines 1004-1076 build_cmd_ctx; lines 1620-1640 first call site; lines 2010-2035 second call site; lines 14-15 imports; vicinity of line 1456 `let command_router = CommandRouter::new(...)` for kanban_store_handle construction site)
    - crates/ironhermes-cli/src/kanban/mod.rs (confirm module layout: `pub mod commands; pub mod format;`)
    - crates/ironhermes-cli/src/kanban/format.rs (lines 15-50 `format_task_table` signature + headers; lines 144-200 `format_task_detail` signature)
    - crates/ironhermes-cli/src/tui_rata/commands.rs (lines 530-600 CronJobReaderImpl pattern)
    - crates/ironhermes-kanban/src/lib.rs (lines 38-62 public surface)
    - crates/ironhermes-kanban/src/store.rs (lines 70-310 — confirm KanbanStore::open_default + list_tasks + get_task signatures)
  </read_first>
  <files>
    crates/ironhermes-cli/src/kanban/store_reader_impl.rs
    crates/ironhermes-cli/src/kanban/mod.rs
    crates/ironhermes-cli/src/main.rs
  </files>
  <behavior>
    Tests for the impl land in Task 4 (the receiver test uses FakeKanbanStoreReader; the real impl is exercised live by Plan 04 UAT).

    Acceptance:
    - New file `crates/ironhermes-cli/src/kanban/store_reader_impl.rs`:
      - `pub struct KanbanStoreReaderImpl { store: Arc<Mutex<KanbanStore>> }`.
      - `pub fn new(store: Arc<Mutex<KanbanStore>>) -> Self`.
      - `pub fn open_default() -> ironhermes_kanban::Result<Self>` calling `KanbanStore::open_default()`, wrapping in `Arc::new(Mutex::new(...))`.
      - `impl KanbanStoreReader for KanbanStoreReaderImpl`:
        - `list_text()` → lock → `list_tasks(ListFilters::default())` → on Ok pass to `format_task_table` and return; on Err return `format!("Failed to read kanban tasks: {}", e)`.
        - `show_text(id)` → lock → `get_task(id)` → on Err return None; on Ok fetch runs via `get_runs(id)` (if available; else `&[]`) and comments via the existing API (if not exposed, pass `&[]`); call `format_task_detail`; wrap in Some.
        - `tip_text()` → static string (≤5 lines) referencing D-31 scratch-workspace semantics and the workspace creation flag options (`--workspace dir:/abs/path`, `--workspace worktree`).
        - `deferred_subverb_message(name)` → `format!("/kanban {}: use 'ironhermes kanban {}' from the CLI for v1.", name, name)`.
    - `crates/ironhermes-cli/src/kanban/mod.rs` gains `pub mod store_reader_impl;` and `pub use store_reader_impl::KanbanStoreReaderImpl;`.
    - `crates/ironhermes-cli/src/main.rs`:
      - `build_cmd_ctx` gains a `kanban_store: Option<Arc<dyn ironhermes_core::commands::context::KanbanStoreReader>>` parameter (after `skill_registry`).
      - At end of `build_cmd_ctx` body (before final return), add `let ctx = if let Some(handle) = kanban_store { ctx.with_kanban_store(handle) } else { ctx };` mirroring lines 1064-1068.
      - In `run_chat` near `let command_router = CommandRouter::new(build_command_registry());` (line 1456), construct `kanban_store_handle` ONCE:
        ```rust
        let kanban_store_handle: Option<Arc<dyn ironhermes_core::commands::context::KanbanStoreReader>> =
            match ironhermes_cli::kanban::KanbanStoreReaderImpl::open_default() {
                Ok(imp) => Some(Arc::new(imp) as Arc<dyn _>),
                Err(e) => { tracing::warn!(error = %e, "kanban store unavailable for /kanban slash dispatch"); None }
            };
        ```
      - At BOTH `build_cmd_ctx` call sites (line 1624 + line 2020), pass `kanban_store_handle.clone()` as the new argument.
    - `run_single` (if it calls `build_cmd_ctx`) passes `None` per CONTEXT.md scope_fence preserving main.rs:875 non-bypass intent. If `run_single` does NOT call `build_cmd_ctx`, no change needed.
    - `cargo check -p ironhermes-cli` succeeds.
    - `cargo test -p ironhermes-cli --bin ironhermes -- --test-threads=1` succeeds (target ≥243 per CONTEXT.md verification_targets).
  </behavior>
  <action>
    1. Create `crates/ironhermes-cli/src/kanban/store_reader_impl.rs` with the struct + two constructors + trait impl per `<behavior>`. Imports: `std::sync::{Arc, Mutex}`, `ironhermes_kanban::{KanbanStore, ListFilters}`, `ironhermes_core::commands::context::KanbanStoreReader`, `super::format::{format_task_table, format_task_detail}`.
       Inside `show_text`, if `KanbanStore::get_runs` is not exposed via the public re-exports (verify by grepping `crates/ironhermes-kanban/src/lib.rs` for `get_runs`), call only `get_task(id)` and pass `&[]` for runs and comments to `format_task_detail` — the detail formatter still renders the labeled fields (`STATUS:`, `ASSIGNEE:`) which satisfies the test contract. If `get_runs` IS exposed, use it and pass the result.
       The `tip_text` content: a single string literal containing roughly: `"Scratch workspaces live at ~/.ironhermes/kanban/workspaces/<task_id>/ and are wiped on kanban_complete.\nTo preserve outputs, use `--workspace dir:/abs/path` or `--workspace worktree` on `ironhermes kanban create`.\nSee `docs/kanban/worker-lanes.md` for the full workspace contract."`
    2. Edit `crates/ironhermes-cli/src/kanban/mod.rs`: append the two `pub mod` / `pub use` lines.
    3. Edit `crates/ironhermes-cli/src/main.rs::build_cmd_ctx`: add the parameter; add the `.with_kanban_store` conditional block near the end of the function body.
    4. Edit `crates/ironhermes-cli/src/main.rs::run_chat`: construct `kanban_store_handle` near line 1456; thread `kanban_store_handle.clone()` through BOTH `build_cmd_ctx` call sites at lines 1624 + 2020.
    5. If `run_single` (lines 690-1000) calls `build_cmd_ctx`, thread `None` (preserve scope_fence). Otherwise no change.
    6. `cargo check -p ironhermes-cli` — must succeed.
    7. `cargo test -p ironhermes-cli --bin ironhermes -- --test-threads=1` — must succeed with no regression.
    Commit: `feat(36.3.7.0-02-task3): KanbanStoreReaderImpl + build_cmd_ctx wiring — open_default best-effort lifecycle`.
  </action>
  <verify>
    <automated>cargo check -p ironhermes-cli 2>&amp;1 | tail -5 &amp;&amp; test -f crates/ironhermes-cli/src/kanban/store_reader_impl.rs &amp;&amp; grep -nE "impl KanbanStoreReader for KanbanStoreReaderImpl" crates/ironhermes-cli/src/kanban/store_reader_impl.rs &amp;&amp; grep -nE "with_kanban_store" crates/ironhermes-cli/src/main.rs &amp;&amp; grep -nE "KanbanStoreReaderImpl::open_default" crates/ironhermes-cli/src/main.rs</automated>
  </verify>
  <done>
    - `store_reader_impl.rs` exists with the impl and constructors.
    - `crates/ironhermes-cli/src/kanban/mod.rs` re-exports `KanbanStoreReaderImpl`.
    - `build_cmd_ctx` accepts and threads the new `kanban_store` parameter.
    - Both call sites at main.rs:1624 + 2020 pass `kanban_store_handle.clone()`.
    - `cargo check -p ironhermes-cli` and `cargo test -p ironhermes-cli --bin ironhermes -- --test-threads=1` both PASS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Receiver-end dispatch-chain test (full chain via FakeKanbanStoreReader)</name>
  <read_first>
    - crates/ironhermes-core/tests/handlers_cron.rs (FakeCronJobReader pattern — verbatim mirror with Kanban method names)
    - crates/ironhermes-core/src/commands/context.rs (KanbanStoreReader trait from Task 1)
    - crates/ironhermes-core/src/commands/handlers.rs (cmd_kanban from Task 2; existing `dispatch` function signature at line 12)
    - crates/ironhermes-core/src/commands/running_agent.rs (verify `is_bypass("kanban")` PASSES — INV-36.3.7-03)
  </read_first>
  <files>crates/ironhermes-core/tests/handlers_kanban.rs</files>
  <behavior>
    New integration test file `crates/ironhermes-core/tests/handlers_kanban.rs`. Five tests covering the full dispatch chain via a FakeKanbanStoreReader fixture.

    Acceptance:
    - `FakeKanbanStoreReader` struct + `impl KanbanStoreReader for FakeKanbanStoreReader` returns canned strings:
      - `list_text()` → `"ID                STATUS    ASSIGNEE         PRIORITY  TITLE\nt_test01          ready     alice            0         test task\n"` (must include `STATUS` and `ASSIGNEE` substrings so the test assertion can match the production format_task_table contract).
      - `show_text("t_known")` → `Some("STATUS:   ready\nASSIGNEE: alice\n...".to_string())`. For any other id → `None`.
      - `tip_text()` → `"Scratch workspaces live at ~/.ironhermes/kanban/workspaces/...".to_string()` (any non-empty string referencing scratch is fine).
      - `deferred_subverb_message("claim")` → `"/kanban claim: use 'ironhermes kanban claim' from the CLI for v1.".to_string()`.
    - Test 1 `dispatch_kanban_list_returns_table_format`: build a CommandContext with `.with_kanban_store(Arc::new(FakeKanbanStoreReader::new()))`; build router via `CommandRouter::new(build_registry())`; resolve `"kanban"`; call `dispatch(&def, &["list"], &ctx, &router)`; assert `CommandResult::Output(s)` where `s.contains("STATUS")` AND `s.contains("ASSIGNEE")` AND NOT `s.contains("not yet available")`.
    - Test 2 `dispatch_kanban_show_returns_detail`: same setup; call `dispatch(&def, &["show", "t_known"], &ctx, &router)`; assert `CommandResult::Output(s)` where `s.contains("STATUS:")` AND `s.contains("ASSIGNEE:")`.
    - Test 3 `dispatch_kanban_show_missing_id_returns_error`: call `dispatch(&def, &["show"], &ctx, &router)`; assert `CommandResult::Error(msg)` where `msg.contains("missing id")`.
    - Test 4 `dispatch_kanban_tip_returns_tip_text`: call `dispatch(&def, &["tip"], &ctx, &router)`; assert `CommandResult::Output(s)` where `s.contains("Scratch")`.
    - Test 5 `dispatch_kanban_deferred_subverb_routes_to_cli_message`: call `dispatch(&def, &["claim"], &ctx, &router)`; assert `CommandResult::Output(s)` where `s.contains("use 'ironhermes kanban claim'")` AND NOT `s.contains("not yet available")`.
    - Test 6 `dispatch_kanban_unknown_subverb_typo_suggests`: call `dispatch(&def, &["lst"], &ctx, &router)`; assert `CommandResult::Error(msg)` where `msg.contains("Unknown /kanban subcommand: lst")` AND (optionally) `msg.contains("list")` (typo-suggest).
    - Test 7 `dispatch_kanban_without_store_returns_not_configured`: build ctx WITHOUT `.with_kanban_store(...)`; call `dispatch(&def, &["list"], &ctx, &router)`; assert `CommandResult::Output(s)` where `s.contains("kanban store not configured")`.
  </behavior>
  <action>
    1. Create `crates/ironhermes-core/tests/handlers_kanban.rs`. Imports: `std::sync::Arc`, `std::sync::atomic::AtomicBool`, `ironhermes_core::commands::context::{CommandContext, KanbanStoreReader}`, `ironhermes_core::commands::handlers::dispatch`, `ironhermes_core::commands::registry::build_registry`, `ironhermes_core::commands::{CommandResult, CommandRouter}`, `ironhermes_core::types::Platform`.
    2. Define `struct FakeKanbanStoreReader;` + `impl KanbanStoreReader for FakeKanbanStoreReader` returning the canned strings from `<behavior>`.
    3. Helper `fn make_ctx_with_store() -> CommandContext { ... }` that builds a `CommandContext` with `.with_kanban_store(Arc::new(FakeKanbanStoreReader))`.
    4. Helper `fn make_ctx_without_store() -> CommandContext { ... }`.
    5. Helper `fn router_and_def() -> (CommandRouter, CommandDef) { let router = CommandRouter::new(build_registry()); let def = build_registry().into_iter().find(|c| c.name == "kanban").expect("kanban registered"); (router, def) }`.
    6. The seven tests from `<behavior>` — assertions verbatim.
    7. Run `cargo test -p ironhermes-core --test handlers_kanban -- --test-threads=1`. All seven must PASS.
    8. Run `cargo test -p ironhermes-core -- --test-threads=1` end-to-end — no regression of existing tests.
    Commit: `test(36.3.7.0-02-task4): receiver-end dispatch chain test for /kanban — 7 tests through FakeKanbanStoreReader`.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-core --test handlers_kanban -- --test-threads=1 2>&amp;1 | grep -E "test result: ok\\. [7-9]|test result: ok\\. [0-9]+ passed"</automated>
  </verify>
  <done>
    - `handlers_kanban.rs` test file exists with `FakeKanbanStoreReader` + the seven tests.
    - All seven tests PASS via `cargo test -p ironhermes-core --test handlers_kanban -- --test-threads=1`.
    - `cargo test -p ironhermes-core -- --test-threads=1` has no regression.
    - The receiver-end gap that hid BUG-36.3.7-02 is closed: the dispatch chain is exercised end-to-end through the trait-object boundary.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slash-command dispatch (in-process) | `cmd_kanban` reads `kanban.db` via the trait-object handle — no LLM round-trip, no IPC; same in-process boundary as `cmd_cron` |
| `Arc<Mutex<KanbanStore>>` in `KanbanStoreReaderImpl` | Shared between session-level handle and the gateway-embedded dispatcher (if both exist in the same process); the existing `KanbanStore` already supports concurrent reads via SQLite WAL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.0-04 | Denial of Service | Long-running `list_tasks` query (huge task table) | accept | Same risk surface as the CLI verb `ironhermes kanban list`. v1 has no row-cap mitigation; deferred to follow-up phase per CONTEXT.md scope_fences. |
| T-36.3.7.0-05 | Information Disclosure | `/kanban show <id>` could leak task body containing secrets | accept | Same surface as `ironhermes kanban show <id>`; trusted-local-user threat model from Phase 36.3.7 D-31 (single-host). |
| T-36.3.7.0-06 | Elevation of Privilege | `Arc<dyn KanbanStoreReader>` trait-object boundary | mitigate | Trait declared in `ironhermes-core` (no impl in core). Impl lives in `ironhermes-cli`. Same cycle-break + leaf-crate guarantee as CronJobReader. Test 7 (`without_store_returns_not_configured`) proves the None-fallback path. |

</threat_model>

<verification>
- `cargo build --workspace` exits 0.
- `cargo test -p ironhermes-core -- --test-threads=1` PASSES with no regression; the new `handlers_kanban` test file adds 7 PASSING tests.
- `cargo test -p ironhermes-cli --bin ironhermes -- --test-threads=1` PASSES with no regression (target: ≥243).
- Static-grep: `"kanban" => cmd_kanban` appears at handlers.rs:72.
- Static-grep: `fn cmd_kanban` appears at handlers.rs:~1130.
- Static-grep: `DEFERRED_KANBAN_SUBVERBS` const has exactly 24 entries (count words between `[` and `]`).
- Static-grep: `pub trait KanbanStoreReader` appears in context.rs.
- Static-grep: `with_kanban_store` appears at least once in main.rs (build_cmd_ctx call sites).
- The receiver-end gap is closed: Test 1 + Test 5 prove the dispatch chain reaches the trait method (NOT `todo_stub`).
</verification>

<success_criteria>
- BUG-36.3.7-02 closed at the dispatch chain: `/kanban list` in the interactive REPL routes through `cmd_kanban` → `KanbanStoreReader::list_text` → `format_task_table` and prints the CLI table.
- Deferred-subverb routing is DISTINCT from `todo_stub` — a future grep-based verifier scanning for the receiver-end pattern can find the deferred routing.
- `CommandContext.kanban_store` field + `with_kanban_store` builder match the CronJobReader precedent for the cycle-break.
- Plan 04 UAT-09-B PASS criteria 1, 2, 5, 6 are testable by this plan; criteria 3, 4, 7, 8 are unblocked by this plan (full UAT runs against a real binary).
</success_criteria>

<output>
Create `.planning/phases/36.3.7.0-kanban-v1-uat-discovered-fixes-inserted/36.3.7.0-02-SUMMARY.md` documenting:
- Files modified + lines added/removed counts.
- Trait + field + builder additions.
- The cmd_kanban dispatch shape (active subverbs + deferred-subverb routing + typo-suggest).
- The KanbanStoreReaderImpl wiring lifecycle (open_default best-effort at session start).
- Test PASS counts (7 new tests in handlers_kanban.rs + baseline regression check).
- Explicit list of the 24 deferred subverbs (verbatim from frontmatter) for downstream Plan 04 UAT scope.
- Meta-finding forward-reference: "the catch-all `todo_stub` hid this bug — Plan 04 SUMMARY's Meta-learning section captures the rule."
</output>
