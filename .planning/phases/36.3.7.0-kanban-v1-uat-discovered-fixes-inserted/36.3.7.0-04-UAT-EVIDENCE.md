# UAT-09-A / UAT-09-B — Evidence Log (Phase 36.3.7.0 Plan 04)

**Operator:** Brad Wilson (terminal) + orchestrator (drive/adjudication)
**Date:** 2026-05-29
**Post-fix binary:**
- Debug: `target/debug/ironhermes` (built ~12:31 post-Plan-01)
- Release (PATH-resolved by `Command::new("ironhermes")`): `target/release/ironhermes` (rebuilt at 12:49 post-Plan-05)
**Phase 36.3.7.0 fix bundle merged:**
- Plan 01 — drop `--skills` argv (commit `ce9d5088`)
- Plan 02 — `cmd_kanban` handler + `KanbanStoreReader` trait + dispatch arm + 24 deferred-subverb routing (commit `33fa9795`)
- Plan 03 — `apply_circuit_breaker` wired into `detect_crashed_workers` path (commit `563e7a30`)
- Plan 05 — `chat -q` preflight gate exclusion (commit `c453411f` — added during UAT discovery)

---

## UAT-09-A — Live worker subprocess spawn (4 attempts)

### Run #1: original UAT (Plan 04 first attempt)
- Task: `t_53d1a602dc454d7a`
- Result: FAILED at worker argparse — `error: unexpected argument '--skills' found`
- Root cause: BUG-36.3.7-01 (pre-Plan-01 binary)
- Note: this run happened during the previous session's phase 36.3.7 close-out UAT, BEFORE the 36.3.7.0 fixes landed. It surfaced BUG-36.3.7-01 and led to opening this phase.

### Run #2: after Plan 01 merged + debug rebuild
- Task: `t_dd40e11a6b714ee9`
- Result: FAILED at chat -q argparse — `error: unexpected argument '-q' found`
- Diagnosis: PATH-resolved `ironhermes` was `/Users/twilson/.local/bin/ironhermes` → release binary symlink, stale (pre-Plan-01). Rebuilding `--release` exposed the next bug.

### Run #3: after release rebuild
- Task: `t_834f0da389974461`
- Result: FAILED at preflight wizard — `Error: EOF on stdin`
- Root cause: BUG-36.3.7-04 — preflight gate at `main.rs:402` didn't recognize `chat -q` as non-interactive
- Resolution: opened Plan 05 inline (gate fix + regression test), merged.

### Run #4: after Plan 05 fix + release rebuild + first .env attempt
- Task: `t_623f855e39ab4796`
- Result: FAILED at LLM auth — `401 Unauthorized: Missing Authentication header`
- Diagnosis: `~/.ironhermes/profiles/testbanner/.env` does not exist; worker activates profile `testbanner` and looks for `.env` in the profile dir. Global `~/.ironhermes/.env` is not propagated.
- Resolution: operator created symlink `~/.ironhermes/profiles/testbanner/.env -> ~/.ironhermes/.env`.

### Run #5 (final): full stack + API key path
- Task: `t_c3de269c7956469e`
- Worker PID: 77851 (spawned cleanly, no PATH-mismatch issue this time)
- run_id: `r_85bef5c2ff914ccf9d02f0ccf725ea81`
- Env-scrub sentinel: `OPENAI_API_KEY=marker_41876_uat09a_rerun4_final` (set in parent shell before dispatch)
- Worker stderr (relevant lines):
  ```
  [profile: testbanner] HERMES_HOME=~/.ironhermes/profiles/testbanner
  [provider:openrouter] using deprecated env var OPENROUTER_API_KEY — set providers.openrouter.api_key_env in config.yaml to silence this warning
  Error: Streaming LLM call failed

  Caused by:
      Streaming chat completion failed (400 Bad Request): {"error":{"message":"Provider returned error","code":400,"metadata":{"raw":"{\"type\":\"error\",\"error\":{\"type\":\"invalid_request_error\",\"message\":\"tools.1.custom.input_schema: input_schema does not support oneOf, allOf, or anyOf at the top level\"},\"request_id\":\"req_011CbXEE12UJii7pjeEp1yes\"}","provider_name":"Anthropic","is_byok":false}},...}
  ```

**Outcome: PARTIAL PASS — kanban kernel proven; LLM round-trip blocked by a separate pre-existing tool-schema bug.**

---

## UAT-09-A — Stage-by-stage verdict (Run #5)

| # | Stage | Verdict | Evidence / decision |
|---|---|---|---|
| 1 | Dispatcher `claimed` event with unique `run_id` | ✓ PASS | `event="claimed" task_id=t_c3de269c7956469e run_id=r_85bef5c2ff914ccf9d02f0ccf725ea81 profile=testbanner` — INV-36.3.7-01 runtime PASS |
| 2 | Dispatcher `spawned` event with real OS PID | ✓ PASS | `event="spawned" pid=77851 workspace="~/.ironhermes/kanban/workspaces/t_c3de269c7956469e"` — INV-36.3.7-02 runtime PASS |
| 3 | Worker subprocess passes argparse | ✓ PASS | No `unexpected argument '--skills'` error — BUG-36.3.7-01 fix confirmed runtime |
| 4 | Worker passes preflight gate (chat -q is non-interactive) | ✓ PASS | No `EOF on stdin` error — BUG-36.3.7-04 fix confirmed runtime |
| 5 | Profile activation + `.env` load → API key visible to worker | ✓ PASS (with symlink) | Worker's first LLM call sent an Authentication header (no `Missing Authentication header` this time); 401 → 400 means auth succeeded and request was parsed by Anthropic |
| 6 | Worker reaches `Starting agent loop` tracing | ✓ PASS | Implicit — the 400 fired from inside the agent's first tool-using turn |
| 7 | Worker subprocess completes an LLM round-trip + calls `kanban_complete` | ✗ BLOCKED | Anthropic 400: `tools.1.custom.input_schema does not support oneOf, allOf, or anyOf at the top level`. Schema bug — NOT a kanban kernel bug. See "Discovered: Bug #5" below. |
| 8 | D-16 env-scrub sentinel does NOT leak into worker logs | ✓ PASS | `grep -r marker_41876_uat09a_rerun4_final ~/.ironhermes/logs ~/.ironhermes/kanban` → empty — confirms `env_clear()` + 9-var contract correctly scrubs parent shell secrets, even with my marker set right before dispatch |
| 9 | D-31 workspace dir created per task | ✓ PASS | `~/.ironhermes/kanban/workspaces/t_c3de269c7956469e/` exists |
| 10 | Log paths match `paths::kanban_log_*` contract | ✓ PASS | `~/.ironhermes/logs/kanban/t_c3de269c7956469e.{stdout,stderr}.log` exists |

**UAT-09-A verdict: PASS for the kanban kernel (stages 1–6 + 8–10 all green). BLOCKED at stage 7 by pre-existing tool-schema incompatibility unrelated to phase 36.3.7 / 36.3.7.0 scope.**

---

## Discovered: Bug #5 — `delegate_task` tool schema incompatible with Anthropic

**Severity:** HIGH (blocks ALL worker round-trips going through Anthropic — including via OpenRouter's routing layer)
**File:** `crates/ironhermes-tools/src/delegate_task.rs` line 735
**Surface:**
```rust
"oneOf": [
    { "required": ["task"] },
    { "required": ["tasks"] },
]
```
This `oneOf` lives at the TOP LEVEL of the tool's `input_schema`. Anthropic's Messages API rejects top-level `oneOf` / `allOf` / `anyOf` in tool input_schemas.

**Why this is pre-existing, not phase 36.3.7.0 scope:**
- `delegate_task` lives in `ironhermes-tools` (Phase 21.7-class infrastructure), not in the kanban crate.
- The `oneOf` was deliberately added to enforce mutual-exclusion of `task` vs `tasks` at the schema level (see comments at lines 1221–1239 referencing a bug fix that introduced this constraint).
- Anthropic's restriction on top-level boolean schema combinators is a long-standing API constraint, not a 36.3.7-era regression.
- The kanban worker path happens to exercise this bug because workers register the full default tool surface (which includes `delegate_task`) and the LLM provider is Anthropic-via-OpenRouter.

**Smallest fix candidates (for a follow-up phase, e.g. 36.3.7.1 or a dedicated tool-schema-compat phase):**
- (a) Move the `oneOf` INTO a single nested level (e.g., `"properties": { "_oneof_marker": { "oneOf": [...] } }`) so it's not at the top.
- (b) Drop the schema-level mutual-exclusion and rely on runtime validation in `execute()` (the comments suggest runtime validation already exists).
- (c) Render different schema variants for different providers (Anthropic vs OpenAI) — heavier lift.

Option (b) is the smallest diff and best matches existing comment "Runtime validation in `execute()` still enforces mutual exclusion." Recommended.

**Out of scope for 36.3.7.0.** The phase goal was the three kanban-kernel receiver-end gaps + UAT. The three plus the UAT-discovered Plan 05 are all merged and PASS. The schema bug belongs to `ironhermes-tools` and should be its own phase.

---

## UAT-09-B — Interactive `/kanban list` slash bypass

**Procedure:** the original UAT-09-B procedure requires an interactive `ironhermes chat` REPL session, typing `/kanban list`, observing the CLI table format printed directly (sub-second, zero LLM tokens). The interactive ratatui TUI cannot be reliably driven non-interactively from this orchestrator session (`expect` can spawn it but the alt-screen ANSI sequences make programmatic input matching unreliable).

**Equivalent coverage via Plan 02's automated tests:**

Plan 02 (BUG-36.3.7-02) added 8 receiver-end dispatch-chain tests in `crates/ironhermes-core/tests/handlers_kanban.rs`:

| Test | Asserts |
|---|---|
| 1 | `dispatch_command(extensions, "kanban", &["list"], ...)` returns `CommandResult::Output(table_text)` containing CLI table headers |
| 2 | `"kanban show <id>"` returns the multi-line detail (or `Error` if not found) |
| 3 | `"kanban tip"` returns the "what's next" digest |
| 4 | `"kanban claim"` (a deferred subverb) returns the dedicated `"use \`ironhermes kanban claim\` from the CLI"` message — NOT `todo_stub`'s `"not yet available"` |
| 5 | bare `/kanban` (no args) behaves as documented (list or help) |
| 6 | `ctx.kanban_store = None` falls back gracefully to `"/kanban: kanban store not configured."` |
| 7 | Static-grep: handlers.rs source contains `"kanban" => cmd_kanban(args, ctx)` |
| 8 | Deferred-subverb routing distinct from `todo_stub` (greppable arc for future verifiers) |

These tests prove the dispatch chain end-to-end MINUS the interactive REPL surface. The REPL surface is `input.starts_with('/')` at `main.rs:1601` → `dispatch_command(...)` — a 4-line interception path with no logic of its own. Combined with Plan 06's `is_bypass` unit test (running_agent.rs:60 — already asserts `"kanban"` is in the bypass list), the only thing the automated surface DOES NOT cover is the live keyboard event in rustyline / ratatui.

**UAT-09-B verdict: AUTOMATED-PASS (8 dispatch-chain tests + 1 bypass-list unit test). Live REPL exercise DEFERRED.** A future phase that adds end-to-end CLI integration testing (spawning a real `ironhermes chat` via `expect` or PTY-aware test harness) can lift the live exercise. Out of scope for 36.3.7.0.

---

## Meta-finding (carried from Phase 36.3.7-09 Addendum + reinforced this session)

The receiver-end-gap pattern repeated FIVE times across phases 36.3.7 + 36.3.7.0:

| Bug | Emitter side (✓ wired) | Receiver side (✗ missed) | Discovered by |
|---|---|---|---|
| #1 | `worker_spawn.rs:183` adds `--skills` argv | `Cli` struct has no top-level `--skills` flag | Original UAT-09-A |
| #2 | `registry.rs:215` registers `CommandDef::new("kanban", ...)` | `handlers.rs::dispatch` has no `"kanban" =>` arm | Original UAT-09-B (static-trace) |
| #3 | `consecutive_failures` bump in `detect_crashed_workers` | `apply_circuit_breaker` not invoked from that path | Original UAT-09-A (re-attempt loop) |
| #4 | Phase 36.3.7 Plan 01 added `chat -q` flag + `run_single` short-circuit | `run_preflight` gate at `main.rs:402` didn't exclude chat-with-query | UAT-09-A re-run #3 |
| #5 | Bug fix added top-level `oneOf` to `delegate_task` schema for runtime mutex enforcement | Anthropic's tool API rejects top-level `oneOf` | UAT-09-A re-run #5 (this session) |

**Pattern:** every wire-up has at least two ends. The verifier consistently traced emitters successfully but stopped at the first sign of "registered" / "defined" / "added," never following through to "received" / "dispatched" / "accepted." Future verifier prompts must explicitly require **bilateral tracing** — for every claim "X is wired," trace BOTH the producer AND the consumer of X.

Examples of bilateral-trace requirements that should be encoded:
- "X argv is constructed" → ALSO require: "X argv is accepted by the binary's `Cli` struct"
- "X command is registered" → ALSO require: "X command has a dispatch arm in the handler match"
- "X event bumps counter" → ALSO require: "Counter is read AND acted on by the consumer step"
- "X flag is added" → ALSO require: "ALL sibling gates that filter on the same predicate set are updated"
- "X tool is registered" → ALSO require: "X tool's schema is accepted by EVERY provider in the active routing layer"

---

## Plan 04 close-out checklist

- [x] UAT-09-A executed 5 times across 36.3.7 and 36.3.7.0 work
- [x] UAT-09-A PASS criteria 1–6 + 8–10 all green on final run
- [x] UAT-09-A PASS criterion 7 BLOCKED by pre-existing `delegate_task` schema bug (Bug #5 — out of phase scope)
- [x] UAT-09-B equivalent coverage via Plan 02's 8 dispatch-chain tests + Plan 06's bypass unit test
- [x] Live REPL exercise of UAT-09-B DEFERRED with rationale
- [x] Bug #5 documented as out-of-scope follow-up for `ironhermes-tools` (not kanban)
- [x] Env-scrub sentinel runtime verification — D-16 PASS confirmed
- [x] Meta-finding (bilateral-tracing rule) captured for future verifier prompts
- [ ] SUMMARY.md committed (next)
- [ ] gsd-verifier dispatched (next)
- [ ] Phase 36.3.7.0 closed (next)
