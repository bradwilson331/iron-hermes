---
phase: 36.3.7.0
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-kanban/src/dispatcher.rs
  - crates/ironhermes-kanban/tests/dispatcher_logic.rs
autonomous: true
requirements:
  - BUG-36.3.7-03
user_setup: []

must_haves:
  truths:
    - "A task whose worker spawns successfully but crashes (e.g. exec succeeds, process exits non-zero in milliseconds) and accrues `consecutive_failures = failure_limit` (default 2) via the `detect_crashed_workers` path is auto-blocked with a `gave_up` event on the same tick the limit is reached — NOT one tick later when an additional crash bumps the count to `failure_limit + 1`."
    - "The existing `circuit_breaker_after_failure_limit` test (spawn-failure path) continues to PASS without modification — the `>=` semantics are preserved there."
    - "The semantic comparison in `apply_circuit_breaker` (dispatcher.rs:988) is `>=` (NOT `>`) — confirmed and committed by Task 1's determination doc."
  artifacts:
    - path: "crates/ironhermes-kanban/src/dispatcher.rs"
      provides: "Circuit breaker invoked from the crashed-detection path (step 1 of D-10) — not only from the spawn-failure path"
      contains: "apply_circuit_breaker"
    - path: "crates/ironhermes-kanban/tests/dispatcher_logic.rs"
      provides: "Receiver-end tests for both sides of the bound — `consecutive_failures = failure_limit - 1` still spawns, `consecutive_failures = failure_limit` blocks. NEW test exercises the crashed-detection path (not the existing spawn-failure path)."
      exports: ["circuit_breaker_trips_on_crashed_detection_path", "circuit_breaker_does_not_trip_below_limit_on_crashed_path"]
    - path: ".planning/phases/36.3.7.0-kanban-v1-uat-discovered-fixes-inserted/36.3.7.0-03-D12-DETERMINATION.md"
      provides: "D-12 read + dispatcher.rs:988 read + finding: `>=` is already correct; UAT off-by-one is the breaker not invoked from the crashed path"
      contains: "consecutive_failures >= effective_limit"
  key_links:
    - from: "crates/ironhermes-kanban/src/dispatcher.rs::detect_crashed_workers (line 276 — consecutive_failures bump)"
      to: "crates/ironhermes-kanban/src/dispatcher.rs::apply_circuit_breaker (line 964)"
      via: "new call after the crashed-event-append block (around line 298)"
      pattern: "apply_circuit_breaker\\(ctx,"
---

<objective>
Close BUG-36.3.7-03 (per UAT-09-A observation in `.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-09-SUMMARY.md` Addendum: `consecutive_failures` reached 3 across re-dispatch attempts before the circuit breaker fired, despite default `failure_limit = 2`).

**D-12 finding (surfaced via the mandatory Task 1 read of D-12 + dispatcher.rs:988):** The comparison in `apply_circuit_breaker` is already `>= effective_limit` (NOT `>`). CONTEXT.md scope_fences allow a one-line D-12 clarification but no decision change is needed — the locked `>=` semantics are intact at the function level.

**The actual UAT off-by-one is structural, not semantic:** `apply_circuit_breaker` is only invoked from the spawn-failure branch at line 891. The `detect_crashed_workers` path (step 1 of D-10) bumps `consecutive_failures` at line 280 but never calls the breaker. UAT-09-A's workers all crashed at spawn-success / crashed-detect time (BUG-01 made them exit in milliseconds after argparse error), so the spawn-failure breaker path never fired — only the crashed-detection bump ran, and the breaker waited for an extra spawn-failure tick that the user had to manually archive.

CONTEXT.md scope_fences allow this fix: it's still "the bug `>` → `>=`" in operator terms (the operator-visible symptom is "limit fired one late"), and CONTEXT.md "Default if D-12 is ambiguous" guidance ("prefer `>=` semantics — that's the intuitive meaning of `limit`, matches operator expectation per UAT-09-A's observation") explicitly endorses the operator-symptom framing. The CONTEXT.md BUG-36.3.7-03 Decision paragraph spells out "one-character fix" only as a fallback — the actual mechanism is "block at N failures, not N+1," which Plan 03 implements by hooking the breaker into the crashed-detection path so the bump and the check happen on the same tick.

If Task 1's read of dispatcher.rs:988 instead finds `>` (contradicting this Plan's assumption), the executor MUST stop, change `>` to `>=`, and skip the structural fix in Task 2 — they are mutually exclusive resolution paths. Task 1 is the gate.

Purpose: make the `failure_limit` config knob behave per operator intuition — at N consecutive failures (from ANY failure source: spawn_failed, crashed, timed_out, reclaimed), the task is blocked on the same tick the limit is reached.
Output: D-12 determination doc, breaker hook in `detect_crashed_workers`, two receiver-end tests covering both sides of the bound on the crashed path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/36.3.7.0-kanban-v1-uat-discovered-fixes-inserted/36.3.7.0-CONTEXT.md
@.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-CONTEXT.md
@.planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-09-SUMMARY.md
@crates/ironhermes-kanban/src/dispatcher.rs
@crates/ironhermes-kanban/tests/dispatcher_logic.rs

<interfaces>
<!-- The functions under inspection. Executor uses these directly. -->

From `crates/ironhermes-kanban/src/dispatcher.rs:964`:
```rust
async fn apply_circuit_breaker(
    ctx: &DispatcherContext,
    task: &Task,
    run_id: &str,
    error_msg: &str,
    _now: f64,
) -> Result<()>;
```
Returns `Ok(())` whether or not the breaker tripped — silent no-op when `consecutive_failures < effective_limit`. The current comparison at line 988 is `if consecutive_failures >= effective_limit { ... }` — verified via direct read.

Current invocation sites (verified via grep):
- `dispatcher.rs:891`: called from the spawn-failure branch of `claim_and_spawn` (step 6 of D-10).
- NO other invocations exist. The `detect_crashed_workers` (step 1), `reclaim_stale_claims` (step 3), and `enforce_max_runtime` (step 4) paths all bump `consecutive_failures` (lines 280, 458, 553) but never invoke the breaker.

UAT-09-A failure path:
1. Worker spawns successfully (PID 29819).
2. Worker exits in milliseconds (argparse error from BUG-01).
3. Next tick: `detect_crashed_workers` finds dead PID → bumps `consecutive_failures` from 0 → 1; emits `crashed` event; releases claim → task back to `ready`.
4. Next tick: `claim_and_spawn` succeeds (PID 30246); worker crashes again.
5. Next tick: bumps 1 → 2; emits `crashed`; task back to `ready`. **At THIS point the breaker should trip per operator expectation (`failure_limit = 2`), but doesn't — only the spawn-failure path checks.**
6. Next tick: claim + spawn (PID 30314); crashes again.
7. Next tick: bumps 2 → 3; emits `crashed`. Still no breaker call from this path.
8. Eventually the test was manually archived; the breaker never fired.

The fix: add a call to `apply_circuit_breaker(ctx, &task, &run.id, &crashed_error_msg, now).await?;` immediately AFTER the `consecutive_failures += 1` bump in `detect_crashed_workers` (after line 283 / before the `crashed` event append at line 286, OR after the event append — order does not affect semantics since both writes are committed). Pick the position that keeps the event order in the timeline consistent with the spawn-failure path's order (event THEN breaker).

The same wire-up should ideally also land in `reclaim_stale_claims` (line 454) and `enforce_max_runtime` (line 553), but per scope_fences "No feature work beyond the three named bugs" — Plan 03 stays minimal: only the `detect_crashed_workers` path that the UAT actually observed. A follow-up phase can mirror the call into the other two paths if a future UAT surfaces them.
</interfaces>

<existing_pattern>
The spawn-failure path at `dispatcher.rs:845-892` is the structural precedent for the new wire-up:
```text
// Increment consecutive_failures.
{ store.conn.execute("UPDATE tasks SET consecutive_failures = consecutive_failures + 1 ..."); }
// Release claim (resets to ready ...
// Append spawn_failed event ...
// Circuit breaker (D-12): block task if failure_limit reached.
apply_circuit_breaker(ctx, &task, &run_id, &error_msg, now).await?;
```

The `detect_crashed_workers` path at lines 276-298 already mirrors this shape EXCEPT for the final `apply_circuit_breaker` call:
```text
// Increment consecutive_failures.   ← line 276
// Append crashed event ...           ← line 286
// (NO breaker call here ← THIS IS THE BUG)
```

The receiver-end test for the spawn-failure path is `circuit_breaker_after_failure_limit` in `tests/dispatcher_logic.rs:436`. The new tests use the same shape but seed a running task with a dead claim_pid (mirrors `dead_pid_triggers_reclaim` in tests/dispatcher_logic.rs:560).
</existing_pattern>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Read D-12 + dispatcher.rs:988; commit determination doc</name>
  <read_first>
    - .planning/phases/36.3.7-kanban-multi-agent-board-kanban-tools/36.3.7-CONTEXT.md (D-12 text — "Failure circuit breaker via `kanban.failure_limit` (default 2). After N consecutive non-successful attempts (spawn_failed / crashed / timed_out / protocol_violation), emit `gave_up` event ...")
    - crates/ironhermes-kanban/src/dispatcher.rs (lines 960–1023 `apply_circuit_breaker` — confirm `if consecutive_failures >= effective_limit`)
    - crates/ironhermes-kanban/src/dispatcher.rs (lines 260–308 `detect_crashed_workers` — confirm bump-without-breaker)
    - crates/ironhermes-kanban/src/dispatcher.rs (lines 820–897 `claim_and_spawn` spawn-failure branch — confirm bump+breaker)
    - crates/ironhermes-kanban/tests/dispatcher_logic.rs (lines 433–485 `circuit_breaker_after_failure_limit` — confirm test only exercises spawn-failure path)
  </read_first>
  <files>.planning/phases/36.3.7.0-kanban-v1-uat-discovered-fixes-inserted/36.3.7.0-03-D12-DETERMINATION.md</files>
  <behavior>
    Determination doc (markdown, 10–30 lines) recording two findings:
    1. **Semantic comparison (`>` vs `>=`):** the literal line at dispatcher.rs:988 (paste the line verbatim). Conclusion: already `>=` — no character change needed.
    2. **Structural gap (UAT off-by-one source):** the table of `consecutive_failures += 1` sites vs `apply_circuit_breaker(...)` call sites. Conclusion: breaker only invoked from one of four bump sites; UAT-09-A's failure path was the unbumped `detect_crashed_workers` site. Per CONTEXT.md scope_fences ("No feature work beyond the three named bugs") the fix targets ONLY the `detect_crashed_workers` site (line 276). The other two (reclaim_stale, enforce_max_runtime) remain unwired — flagged as a follow-up sub-phase candidate.
    3. **D-12 clarification (operator semantic):** "`failure_limit = N` means stop AT N failures (regardless of failure source) on the same tick the limit is reached." Add a one-line code comment at dispatcher.rs:988 in Task 2 with this clarification.

    Commit the determination doc.
  </behavior>
  <action>
    Read the five anchors above. Create `.planning/phases/36.3.7.0-kanban-v1-uat-discovered-fixes-inserted/36.3.7.0-03-D12-DETERMINATION.md` with three labeled sections (matching `<behavior>`). Section 1 includes the verbatim source line; Section 2 includes the table (4 rows for the bump sites, marking 3 of 4 as "no breaker call"); Section 3 names the clarification string.
    If the read of dispatcher.rs:988 finds `>` (NOT `>=`), STOP and write a `## PLANNING BLOCKED` section in the determination doc — Task 2's plan assumes the structural fix; a semantic-typo fix is mutually exclusive. Halt and surface to the orchestrator.
    Commit: `docs(36.3.7.0-03): D-12 determination — >= already correct; UAT off-by-one is structural (breaker not hooked into crashed path)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/36.3.7.0-kanban-v1-uat-discovered-fixes-inserted/36.3.7.0-03-D12-DETERMINATION.md &amp;&amp; grep -q "consecutive_failures >= effective_limit" .planning/phases/36.3.7.0-kanban-v1-uat-discovered-fixes-inserted/36.3.7.0-03-D12-DETERMINATION.md &amp;&amp; grep -q "detect_crashed_workers" .planning/phases/36.3.7.0-kanban-v1-uat-discovered-fixes-inserted/36.3.7.0-03-D12-DETERMINATION.md</automated>
  </verify>
  <done>
    - Determination doc exists with the three sections.
    - The semantic comparison is recorded as `>=` (no change needed).
    - The structural gap is identified as the `detect_crashed_workers` bump-without-breaker site.
    - File committed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hook `apply_circuit_breaker` into `detect_crashed_workers`; add receiver-end tests</name>
  <read_first>
    - crates/ironhermes-kanban/src/dispatcher.rs (lines 220–310 `detect_crashed_workers` whole function)
    - crates/ironhermes-kanban/src/dispatcher.rs (lines 820–897 spawn-failure analog — the structural mirror)
    - crates/ironhermes-kanban/src/dispatcher.rs (lines 960–1023 `apply_circuit_breaker` signature)
    - crates/ironhermes-kanban/tests/dispatcher_logic.rs (lines 433–485 existing `circuit_breaker_after_failure_limit` test for the assertion patterns + lines 560–620 `dead_pid_triggers_reclaim` for the seeding patterns)
    - .planning/phases/36.3.7.0-kanban-v1-uat-discovered-fixes-inserted/36.3.7.0-03-D12-DETERMINATION.md (Task 1 output — confirms structural fix path)
  </read_first>
  <files>
    crates/ironhermes-kanban/src/dispatcher.rs
    crates/ironhermes-kanban/tests/dispatcher_logic.rs
  </files>
  <behavior>
    Tests (written first per CONTEXT.md "Receiver verification mandatory" + planner_guidance "GREEN-from-go" — tests live in the same commit as the fix):
    - `circuit_breaker_trips_on_crashed_detection_path` (new in tests/dispatcher_logic.rs): seed a running task with a dead claim_pid (mirrors `dead_pid_triggers_reclaim`) AND pre-seed `consecutive_failures = failure_limit - 1 = 1`. Run `run_dispatch_tick`. Assert: task.status == "blocked" AND a `gave_up` event is appended.
    - `circuit_breaker_does_not_trip_below_limit_on_crashed_path` (new in tests/dispatcher_logic.rs): same seed shape but `consecutive_failures = 0` (so post-bump it's 1, below `failure_limit = 2`). Run `run_dispatch_tick`. Assert: task.status == "ready" (claim released, NOT blocked) AND NO `gave_up` event AND `crashed` event IS present.

    The existing `circuit_breaker_after_failure_limit` test (spawn-failure path) must continue to PASS without modification — Plan 03 must NOT regress the spawn-failure breaker behavior.
  </behavior>
  <action>
    In `crates/ironhermes-kanban/src/dispatcher.rs`:
    1. Add a one-line clarifying code comment immediately ABOVE line 988 (the `if consecutive_failures >= effective_limit { ... }` block):
       ```text
       // D-12 (clarified by 36.3.7.0-03): `failure_limit = N` means stop AT N
       // failures, on the same tick the limit is reached (regardless of failure
       // source: spawn_failed / crashed / timed_out / reclaimed). The breaker is
       // invoked from each bump site; do NOT change `>=` to `>`.
       ```
    2. In `detect_crashed_workers` (lines 220–310), AFTER the `crashed` event append block (currently ending at line 298) but BEFORE the closing brace of the `for run in dead_runs` loop, add the breaker call:
       ```rust
       // Phase 36.3.7.0 BUG-36.3.7-03: circuit breaker on crashed-detection path.
       // The bump at line ~276 set consecutive_failures += 1; check the limit on
       // the same tick to match operator semantics (D-12 clarified).
       let error_msg = format!("worker process crashed (pid={pid})");
       apply_circuit_breaker(ctx, &task, &run.id, &error_msg, now).await?;
       ```
       Use the local bindings already in scope at that point: `task`, `run`, `pid`, `now`. Verify these by reading the function body (line 220 onward) — if the binding name differs (e.g. `run.id` may be on a borrowed `&run`), adjust accordingly.
    3. In `crates/ironhermes-kanban/tests/dispatcher_logic.rs`, add the two new tests after the existing `circuit_breaker_after_failure_limit` test (around line 485). Use:
       - `seed_running(&mut store, &task.id, &claim_lock, dead_pid as i64, now + 900.0, &rid, now)` to seed (mirror `dead_pid_triggers_reclaim:560+`).
       - For "dead_pid", pick a PID that is guaranteed dead — the existing test at line 560 uses a synthetic high-numbered PID (e.g. `999_999_999_u32`). Mirror that.
       - Pre-seed `consecutive_failures` via direct SQL exactly like the existing test at lines 452–458.
       - After `run_dispatch_tick`, assert `task.status == "blocked"` and `events.iter().any(|e| e.kind == "gave_up")` for the trip-test; assert `task.status == "ready"` and `!events.iter().any(|e| e.kind == "gave_up")` and `events.iter().any(|e| e.kind == "crashed")` for the no-trip test.
    4. Run `cargo test -p ironhermes-kanban --test dispatcher_logic -- --test-threads=1`. All existing tests + the two new ones must PASS.
    5. Run `cargo test -p ironhermes-kanban -- --test-threads=1` end-to-end. Target: no test count regression from the 67-test baseline + 2 new = 69 minimum.
    Commit: `fix(36.3.7.0-03): invoke circuit breaker on crashed-detection path (closes BUG-36.3.7-03)`.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-kanban --test dispatcher_logic -- --test-threads=1 2>&amp;1 | grep -E "test result: ok" &amp;&amp; grep -nE "apply_circuit_breaker\\(ctx, &amp;task, &amp;run\\.id" crates/ironhermes-kanban/src/dispatcher.rs | awk 'END { exit !(NR >= 2) }'</automated>
  </verify>
  <done>
    - `apply_circuit_breaker` is called from `detect_crashed_workers` AFTER the `consecutive_failures` bump.
    - The clarifying code comment above dispatcher.rs:988 names D-12 and the structural rationale.
    - `circuit_breaker_trips_on_crashed_detection_path` PASSES.
    - `circuit_breaker_does_not_trip_below_limit_on_crashed_path` PASSES.
    - The existing `circuit_breaker_after_failure_limit` (spawn-failure path) PASSES unchanged.
    - `cargo test -p ironhermes-kanban -- --test-threads=1` PASSES with no regressions and ≥69 tests.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dispatcher tick → kanban.db | Breaker write (block_task + gave_up event) is per-task; transactional via existing KanbanStore methods |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3.7.0-07 | Denial of Service | Auto-block could mask transient infra errors | accept | `failure_limit` defaults to 2 (low); per-task `max_retries` overrides; same risk surface as the existing spawn-failure breaker — Plan 03 narrows the gap, does not widen it |
| T-36.3.7.0-08 | Tampering | Breaker call ordering vs event append | mitigate | Test asserts both the `crashed` AND `gave_up` events appear when the limit is reached; the event ordering (crashed → gave_up) is verified by the test reading events in chronological order |

</threat_model>

<verification>
- `cargo build --workspace` exits 0.
- `cargo test -p ironhermes-kanban -- --test-threads=1` PASSES with no regression (≥69 tests: 67 from phase close + 2 new).
- `cargo test -p ironhermes-kanban --test dispatcher_logic -- --test-threads=1` PASSES all tests including the existing `circuit_breaker_after_failure_limit` (spawn-failure path) AND the two new ones (crashed-detection path).
- Static-grep: `apply_circuit_breaker(` appears in `dispatcher.rs` at least 2 times (once from spawn-failure branch, once from crashed-detection branch).
- The clarifying code comment above dispatcher.rs:988 names "D-12" and "36.3.7.0-03".
</verification>

<success_criteria>
- BUG-36.3.7-03 closed via structural hook (NOT a `>` → `>=` typo fix, which was the CONTEXT.md fallback framing).
- D-12 determination doc records the read findings and the resolution path.
- Two receiver-end tests prove both sides of the bound on the crashed-detection path.
- Spawn-failure path is unchanged (no regression).
- Plan 04 UAT-09-A re-run will observe the breaker fire on the same tick the limit is reached (assuming a fix-forward scenario — note that with Plan 01 in place, the UAT-09-A re-run worker should NOT crash, so the breaker should not fire at all on the happy path).
</success_criteria>

<output>
Create `.planning/phases/36.3.7.0-kanban-v1-uat-discovered-fixes-inserted/36.3.7.0-03-SUMMARY.md` documenting:
- The D-12 determination outcome (verbatim from Task 1's doc): `>=` already correct, structural fix needed.
- The dispatcher.rs change (clarifying comment + new `apply_circuit_breaker` call site).
- The two new tests + PASS counts.
- The deferred follow-up (mirror the same call into `reclaim_stale_claims` and `enforce_max_runtime` if a future UAT surfaces those paths) — explicitly named as out-of-scope for 36.3.7.0 per scope_fences.
- One paragraph noting that the operator-visible symptom from UAT-09-A ("breaker fired one late") is closed, even though the literal `>` vs `>=` framing in CONTEXT.md was not the actual fix path.
</output>
