---
phase: 14-context-files-soul-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-agent/src/context_loader.rs
  - crates/ironhermes-agent/src/prompt_builder.rs
  - crates/ironhermes-agent/src/lib.rs
autonomous: true
requirements:
  - CTX-01
  - CTX-02
  - CTX-05
  - CTX-06
  - CTX-07

must_haves:
  truths:
    - "Priority chain loads .hermes.md > AGENTS.md > CLAUDE.md > .cursorrules with case-sensitive matching (no lowercase variants)"
    - ".hermes.md walks upward from CWD to git root (or $HOME if no git root), first match wins"
    - "YAML frontmatter is stripped from .hermes.md before scan and injection"
    - "All loaded context files pass through scan_context_content and truncate_content"
    - "When skip_context_files is true, SOUL.md/project context/AGENTS.md are all skipped and DEFAULT_AGENT_IDENTITY is used"
  artifacts:
    - path: "crates/ironhermes-agent/src/context_loader.rs"
      provides: "find_git_root, strip_yaml_frontmatter, CONTEXT_CANDIDATES constant"
      exports: ["find_git_root", "strip_yaml_frontmatter", "CONTEXT_CANDIDATES"]
    - path: "crates/ironhermes-agent/src/prompt_builder.rs"
      provides: "Updated PromptBuilder with skip_context_files, git-root walk, frontmatter stripping"
      contains: "skip_context_files"
  key_links:
    - from: "crates/ironhermes-agent/src/prompt_builder.rs"
      to: "crates/ironhermes-agent/src/context_loader.rs"
      via: "use crate::context_loader"
      pattern: "context_loader::(find_git_root|strip_yaml_frontmatter)"
    - from: "crates/ironhermes-agent/src/prompt_builder.rs"
      to: "ironhermes-core context_scanner"
      via: "scan_context_content + truncate_content"
      pattern: "scan_context_content|truncate_content"
---

<objective>
Create the ContextLoader module and update PromptBuilder to implement the full context file priority chain with git-root walking for .hermes.md, YAML frontmatter stripping, case-sensitive candidate matching, and the skip_context_files flag for subagents.

Purpose: The agent needs to discover its project context from the filesystem using hermes-agent's priority chain pattern. This is the foundation for the identity and context system.
Output: context_loader.rs with utility functions, updated prompt_builder.rs with correct priority chain behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-context-files-soul-md/14-CONTEXT.md
@.planning/phases/14-context-files-soul-md/14-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From crates/ironhermes-core/src/lib.rs:
```rust
pub fn scan_context_content(content: &str, source: &str) -> String;
pub fn truncate_content(content: &str, source: &str, max_chars: usize) -> String;
pub const CONTEXT_FILE_MAX_CHARS: usize; // 20_000
pub fn get_hermes_home() -> PathBuf;
```

From crates/ironhermes-agent/src/prompt_builder.rs (current):
```rust
pub struct PromptBuilder {
    model: String,
    platform: String,
    soul_content: Option<String>,
    project_context: Option<String>,
    agents_md_content: Option<String>,
    memory_store: Option<Arc<Mutex<dyn MemoryProvider + Send>>>,
    skill_registry: Option<Arc<SkillRegistry>>,
}

impl PromptBuilder {
    pub fn new(model: impl Into<String>, platform: impl Into<String>) -> Self;
    pub fn load_context(mut self, cwd: &Path) -> Self;
    pub fn build(&self) -> String;
}
```

Current load_project_context candidates (BROKEN — includes lowercase variants):
```rust
let candidates: &[&[&str]] = &[
    &[".hermes.md", "HERMES.md"],
    &["AGENTS.md", "agents.md"],
    &["CLAUDE.md", "claude.md"],
    &[".cursorrules"],
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create context_loader.rs with git-root walk, frontmatter stripping, and priority chain utilities</name>
  <files>crates/ironhermes-agent/src/context_loader.rs, crates/ironhermes-agent/src/lib.rs</files>
  <read_first>
    - crates/ironhermes-agent/src/prompt_builder.rs (current implementation to understand what to extract)
    - crates/ironhermes-core/src/context_scanner.rs (scan_context_content and truncate_content signatures)
    - crates/ironhermes-agent/src/lib.rs (module declarations)
  </read_first>
  <behavior>
    - test_find_git_root_found: directory with .git returns that directory
    - test_find_git_root_walks_up: nested directory finds .git in parent
    - test_find_git_root_stops_at_home: walk stops at $HOME, returns None if no .git found below
    - test_find_git_root_no_git: returns None when no .git exists anywhere up to $HOME
    - test_strip_frontmatter_basic: "---\nkey: val\n---\nbody" returns "body"
    - test_strip_frontmatter_none: "no frontmatter here" returns input unchanged
    - test_strip_frontmatter_malformed: "---\nno closing" returns input unchanged
    - test_strip_frontmatter_empty_body: "---\nkey: val\n---\n" returns empty string (trimmed)
    - test_context_candidates_case_sensitive: CONTEXT_CANDIDATES contains exactly [".hermes.md", "AGENTS.md", "CLAUDE.md", ".cursorrules"] — no lowercase variants, no HERMES.md
  </behavior>
  <action>
Create `crates/ironhermes-agent/src/context_loader.rs` with these exports:

1. `pub const CONTEXT_CANDIDATES: &[&str] = &[".hermes.md", "AGENTS.md", "CLAUDE.md", ".cursorrules"];`
   - Case-sensitive exact names per D-08. No groups, no lowercase variants, no HERMES.md.

2. `pub fn find_git_root(start: &Path) -> Option<PathBuf>`:
   - Walk upward from `start` checking for `.git` (file or directory — supports worktrees).
   - Stop at $HOME using `std::env::var("HOME").ok().map(PathBuf::from)` as sentinel.
   - If `start` itself has `.git`, return `start`.
   - Return None if no `.git` found before reaching $HOME or filesystem root.
   - Per D-01 and D-03.

3. `pub fn strip_yaml_frontmatter(content: &str) -> &str`:
   - If content (after trim_start) starts with "---", find the next line that is exactly "---" (trimmed).
   - Return everything after the closing "---" marker, trimmed of leading newline.
   - If no closing marker found, return input unchanged (malformed frontmatter = no-op).
   - Per D-02 and CTX-07.

Add `pub mod context_loader;` to `crates/ironhermes-agent/src/lib.rs` after the existing module declarations.

Tests go in a `#[cfg(test)] mod tests` block inside context_loader.rs. Use `tempfile::tempdir()` for filesystem tests. For find_git_root tests that manipulate HOME, use the same `ENV_MUTEX` pattern from prompt_builder.rs tests (static Mutex to prevent env var races).
  </action>
  <verify>
    <automated>cargo test -p ironhermes-agent -- context_loader --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - crates/ironhermes-agent/src/context_loader.rs exists
    - context_loader.rs contains `pub const CONTEXT_CANDIDATES: &[&str]`
    - context_loader.rs contains `pub fn find_git_root(start: &Path) -> Option<PathBuf>`
    - context_loader.rs contains `pub fn strip_yaml_frontmatter(content: &str) -> &str`
    - CONTEXT_CANDIDATES has exactly 4 entries: .hermes.md, AGENTS.md, CLAUDE.md, .cursorrules
    - CONTEXT_CANDIDATES does NOT contain "agents.md", "claude.md", or "HERMES.md"
    - lib.rs contains `pub mod context_loader;`
    - All context_loader tests pass with `cargo test -p ironhermes-agent -- context_loader`
  </acceptance_criteria>
  <done>context_loader.rs compiles, exports all three items, all unit tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update PromptBuilder with skip_context_files flag, git-root walk, and frontmatter stripping</name>
  <files>crates/ironhermes-agent/src/prompt_builder.rs</files>
  <read_first>
    - crates/ironhermes-agent/src/context_loader.rs (just created in Task 1 — read for function signatures)
    - crates/ironhermes-agent/src/prompt_builder.rs (current implementation)
    - crates/ironhermes-agent/src/subagent_runner.rs (understand how subagents construct their prompt)
    - crates/ironhermes-cli/src/main.rs (call sites for PromptBuilder::new().load_context())
    - crates/ironhermes-gateway/src/handler.rs (call sites)
  </read_first>
  <behavior>
    - test_skip_context_files_default_identity: with skip_context_files(true), build() output contains DEFAULT_AGENT_IDENTITY text ("IronHermes, an AI assistant") even when SOUL.md exists on disk, and does NOT contain project context or AGENTS.md from HERMES_HOME
    - test_hermes_md_git_root_walk: .hermes.md in parent directory (with .git) is found when CWD is a child directory
    - test_hermes_md_frontmatter_stripped: .hermes.md with YAML frontmatter has frontmatter removed in output but body content present
    - test_case_sensitive_candidates: `agents.md` (lowercase) in CWD is NOT loaded; `AGENTS.md` (uppercase) IS loaded
    - test_hermes_home_agents_md_separate: HERMES_HOME/AGENTS.md loads even when CWD has .hermes.md (they serve separate roles per D-09)
  </behavior>
  <action>
Modify `crates/ironhermes-agent/src/prompt_builder.rs`:

1. Add `use crate::context_loader::{find_git_root, strip_yaml_frontmatter, CONTEXT_CANDIDATES};` at top.

2. Add field `skip_context_files: bool` to `PromptBuilder` struct, initialized to `false` in `new()`.

3. Add builder method:
   ```rust
   pub fn skip_context_files(mut self) -> Self {
       self.skip_context_files = true;
       self
   }
   ```

4. Update `load_context()`:
   ```rust
   pub fn load_context(mut self, cwd: &Path) -> Self {
       if self.skip_context_files {
           return self; // D-10: subagents get clean identity, no SOUL.md, no project context, no AGENTS.md
       }
       self.load_soul_md();
       self.load_project_context(cwd);
       self.load_agents_md();
       self
   }
   ```

5. Update `build()` identity selection to respect skip_context_files:
   ```rust
   let identity = if self.skip_context_files {
       DEFAULT_AGENT_IDENTITY // D-10: always use default for subagents
   } else {
       self.soul_content.as_deref().unwrap_or(DEFAULT_AGENT_IDENTITY)
   };
   ```

6. Replace `load_project_context()` entirely:
   - Step 1: Walk upward from CWD looking for `.hermes.md` only. Use `find_git_root(cwd)` to determine walk-stop (if Some, stop at git root; if None, use $HOME via `std::env::var("HOME")`). On finding `.hermes.md`, call `strip_yaml_frontmatter(&content)` FIRST, then `scan_context_content`, then `truncate_content`. Set `self.project_context = Some(truncated)` and return. Per D-01, D-02, D-03.
   - Step 2: If no `.hermes.md` found, check CWD only for remaining candidates: `AGENTS.md`, `CLAUDE.md`, `.cursorrules` (skip index 0 which is `.hermes.md`). First match wins. Apply `scan_context_content` + `truncate_content`. No frontmatter stripping for non-.hermes.md files. Per CTX-01, D-08.
   - Note: The walk-stop for .hermes.md is the git root directory itself (inclusive — check that dir too). If no git root, stop at $HOME (do NOT check $HOME itself for .hermes.md, just stop there). Per D-01, D-03.

7. Update existing tests to remove any reliance on lowercase candidate names (agents.md, claude.md). The existing tests use `.hermes.md` and `CLAUDE.md` which are correct, so they should still pass.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-agent -- prompt_builder --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - prompt_builder.rs contains `skip_context_files: bool` field in PromptBuilder struct
    - prompt_builder.rs contains `pub fn skip_context_files(mut self) -> Self`
    - prompt_builder.rs contains `use crate::context_loader::{find_git_root, strip_yaml_frontmatter`
    - load_project_context does NOT contain "agents.md" or "claude.md" (lowercase) in candidate list
    - load_project_context does NOT contain "HERMES.md" in candidate list
    - load_context() returns early when skip_context_files is true
    - build() uses DEFAULT_AGENT_IDENTITY when skip_context_files is true
    - strip_yaml_frontmatter is called on .hermes.md content before scan_context_content
    - find_git_root is called in load_project_context for .hermes.md walk
    - All prompt_builder tests pass including new ones
    - `cargo check -p ironhermes-agent` compiles clean
  </acceptance_criteria>
  <done>PromptBuilder has skip_context_files, git-root walk for .hermes.md, frontmatter stripping, case-sensitive candidates; all tests pass; cargo check clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> prompt | Untrusted .hermes.md/AGENTS.md/CLAUDE.md/.cursorrules content crosses into system prompt |
| YAML frontmatter -> strip | Frontmatter parsing must not leak config into prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | .hermes.md content | mitigate | All context files pass through `scan_context_content()` which blocks 10 injection patterns + invisible unicode |
| T-14-02 | Information Disclosure | YAML frontmatter | mitigate | `strip_yaml_frontmatter()` removes frontmatter BEFORE scan+truncate; frontmatter never reaches system prompt |
| T-14-03 | Elevation of Privilege | git-root walk past $HOME | mitigate | `find_git_root()` stops at $HOME per D-03; prevents loading system-level context files |
| T-14-04 | Tampering | Oversized context file | mitigate | `truncate_content()` enforces 20K char cap on every load path |
| T-14-05 | Tampering | Case-insensitive filename trick | mitigate | CONTEXT_CANDIDATES uses exact case-sensitive names per D-08; no lowercase variants |
</threat_model>

<verification>
- `cargo test -p ironhermes-agent -- context_loader --test-threads=1` — all context_loader unit tests pass
- `cargo test -p ironhermes-agent -- prompt_builder --test-threads=1` — all prompt_builder tests pass (existing + new)
- `cargo check -p ironhermes-agent` — no compile errors
- `cargo check --workspace` — no workspace-level breakage
</verification>

<success_criteria>
- context_loader.rs exists with find_git_root, strip_yaml_frontmatter, CONTEXT_CANDIDATES
- PromptBuilder loads .hermes.md via git-root walk with frontmatter stripping
- Priority chain is case-sensitive (.hermes.md > AGENTS.md > CLAUDE.md > .cursorrules)
- skip_context_files flag works: subagents use DEFAULT_AGENT_IDENTITY
- All context files are scanned (scan_context_content) and truncated (truncate_content)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-context-files-soul-md/14-01-SUMMARY.md`
</output>
