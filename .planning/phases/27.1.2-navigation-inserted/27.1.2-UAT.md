---
status: complete
phase: 27.1.2-navigation-inserted
source: [27.1.2-01-SUMMARY.md]
started: 2026-05-11T00:00:00Z
updated: 2026-05-11T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Unit test suite passes
expected: Run `cargo test -p ironhermes-tools hexapod_tcp -- --test-threads=1`. All 22 hexapod_tcp tests pass (tests 1–20 + 2 additional). Full crate suite: 328 passed, 0 failed.
result: pass

### 2. New actions pass outer allowlist
expected: All 5 new actions (rotate, head_pan, head_tilt, buzzer_on, buzzer_off) are in the outer allowlist. Tool is hidden from LLM when HEXAPOD_IP is unset (is_available() gate — by design). With HEXAPOD_IP set, actions are visible and return env-var error rather than "blocked" string.
result: pass
note: Initial expectation was wrong — tool being invisible without HEXAPOD_IP is intentional design, not a bug.

### 3. Rotate 0 degrees is a no-op
expected: Calling rotate with degrees=0 returns OK immediately without sending any motion command (no wire bytes sent, no sleep). This is the CR-03 fix applied in the code review.
result: pass

### 4. Head angle clamping
expected: head_pan with angle=180 clamps to 90 in the wire output (CMD_HEAD#0#90). head_tilt with angle=-180 clamps to -90 (CMD_HEAD#1#-90). Values within [-90, 90] are sent as-is.
result: pass

### 5. Physical robot — rotate
expected: With HEXAPOD_IP set and robot connected, calling rotate(90) makes the robot rotate clockwise, then stop after ~1.8 seconds. rotate(-90) rotates counter-clockwise then stops.
result: blocked
blocked_by: physical-device
reason: Requires physical Freenove hexapod robot connected over Wi-Fi

### 6. Physical robot — head pan and tilt
expected: head_pan(45) moves the robot's head horizontally. head_tilt(30) moves it vertically. head_pan(0) centers horizontally, head_tilt(0) centers vertically.
result: blocked
blocked_by: physical-device
reason: Requires physical Freenove hexapod robot connected over Wi-Fi

### 7. Physical robot — buzzer
expected: buzzer_on makes the robot emit a tone. buzzer_off silences it. Both commands return "OK" over TCP.
result: blocked
blocked_by: physical-device
reason: Requires physical Freenove hexapod robot connected over Wi-Fi

## Summary

total: 7
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 3

## Gaps

[none]
