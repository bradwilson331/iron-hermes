---
phase: 27.1.2
slug: navigation-inserted
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-11
---

# Phase 27.1.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `#[tokio::test]` (enabled via `tokio = { workspace = true, features = ["full", "test-util"] }` in `crates/ironhermes-tools/Cargo.toml` dev-deps) |
| **Config file** | none — workspace `Cargo.toml` carries the dependency; `#[cfg(test)] mod tests` is inline in `hexapod_tcp.rs` |
| **Quick run command** | `cargo test -p ironhermes-tools hexapod_tcp -- --test-threads=1` |
| **Full suite command** | `cargo test -p ironhermes-tools -- --test-threads=1` |
| **Note** | `--test-threads=1` is mandatory — the `ENV_LOCK` mutex serialises env-var-mutating tests; parallel threads will race on `HEXAPOD_IP` |
| **Estimated runtime** | ~15s for the targeted hexapod tests; ~3-5min for the full workspace suite on a warm cache |

---

## Sampling Rate

- **After every task commit:** `cargo test -p ironhermes-tools hexapod_tcp -- --test-threads=1` — sub-30s feedback on the full inline test suite
- **After plan wave:** `cargo test -p ironhermes-tools -- --test-threads=1` — full tools crate suite
- **Before `/gsd-verify-work`:** `cargo test --workspace -- --test-threads=1` must be green; manual UAT on powered robot for physical confirmation
- **Max feedback latency:** 30s for targeted suite; 5min for full workspace

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 27.1.2-01-T1 | 01 | 1 | HXP-NAV-01, HXP-NAV-03, HXP-NAV-04 | Compile-time allowlist (D-20) — 5 new actions added to outer arm; blocked catch-all unchanged | Blocked actions still return D-16 string; new actions reach inner dispatch; angle clamped before wire format | unit + compile | `cargo test -p ironhermes-tools hexapod_tcp -- --test-threads=1` | ❌ W0 (Plan 01 Task 1 extends hexapod_tcp.rs) | ⬜ pending |
| 27.1.2-01-T2 | 01 | 1 | HXP-NAV-01, HXP-NAV-03, HXP-NAV-04 | Test 20 regression guard — any future allowlist omission causes test 20 to fail | New actions return env-var error (not blocked string) without HEXAPOD_IP set | unit (tests 14–20) | `cargo test -p ironhermes-tools hexapod_tcp -- --test-threads=1` | ❌ W0 (Plan 01 Task 2 adds tests 14–20) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠ flaky*

**Per-task automated coverage:**
- Plan 01 Task 1 → implementation + compile proof + all 13 pre-existing tests remain green (zero-regression proof)
- Plan 01 Task 2 → 7 new inline tests (14–20) under `hexapod_tcp::tests`; test 20 is the critical allowlist regression guard

---

## Phase Requirements → Test Map

| Req ID | Behavior | Test # | Test Name | Test Type | Automated Command | File Exists |
|--------|----------|--------|-----------|-----------|-------------------|-------------|
| HXP-NAV-01 | Rotate wire builder — positive degrees | 14 | `test_rotate_wire_positive_degrees` | unit (pure fn) | `cargo test -p ironhermes-tools test_rotate_wire_positive_degrees -- --test-threads=1` | ❌ Wave 0 |
| HXP-NAV-01 | Rotate wire builder — negative degrees | 15 | `test_rotate_wire_negative_degrees` | unit (pure fn) | `cargo test -p ironhermes-tools test_rotate_wire_negative_degrees -- --test-threads=1` | ❌ Wave 0 |
| HXP-NAV-01 | Rotate wire builder — zero degrees | 16 | `test_rotate_wire_zero_degrees` | unit (pure fn) | `cargo test -p ironhermes-tools test_rotate_wire_zero_degrees -- --test-threads=1` | ❌ Wave 0 |
| HXP-NAV-03 | head_pan angle clamping — high | 17 | `test_head_pan_clamps_high` | unit | `cargo test -p ironhermes-tools test_head_pan_clamps_high -- --test-threads=1` | ❌ Wave 0 |
| HXP-NAV-03 | head_tilt angle clamping — low | 18 | `test_head_tilt_clamps_low` | unit | `cargo test -p ironhermes-tools test_head_tilt_clamps_low -- --test-threads=1` | ❌ Wave 0 |
| HXP-NAV-04 | Buzzer wire string constants | 19 | `test_buzzer_wire_strings` | unit (constant check) | `cargo test -p ironhermes-tools test_buzzer_wire_strings -- --test-threads=1` | ❌ Wave 0 |
| HXP-NAV-01,03,04 | All 5 new actions pass outer allowlist arm | 20 | `test_new_actions_not_blocked` | async unit + ENV_LOCK | `cargo test -p ironhermes-tools test_new_actions_not_blocked -- --test-threads=1` | ❌ Wave 0 |

---

## Wave 0 Requirements

- [ ] `crates/ironhermes-tools/src/hexapod_tcp.rs` — already exists (Phase 27.1.1); Plan 01 Task 1 extends it with 5 new action arms, 5 new constants, 1 new builder function, updated schema, updated DESCRIPTION
- [ ] Tests 14–20 added inline in `hexapod_tcp.rs` `#[cfg(test)] mod tests { ... }` — no new test files; extend the existing inline test module
- [ ] No new test framework needed — `#[tokio::test]` and `tokio = { features = ["full", "test-util"] }` already in workspace dev-deps
- [ ] No new fixtures needed — all wire strings are pure-function outputs or constants; no live TCP in the unit suite

If none of the above gaps are open at the start of Plan execution: existing test infrastructure covers everything.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Robot rotates clockwise for positive degrees | HXP-NAV-01, ROADMAP #1 | Physical effect — requires powered robot | Invoke `hexapod_tcp {"action":"rotate","degrees":90}` via REPL; observe clockwise rotation |
| Robot rotates counterclockwise for negative degrees | HXP-NAV-01, ROADMAP #1 | Physical effect | Invoke `hexapod_tcp {"action":"rotate","degrees":-90}`; observe counterclockwise rotation |
| Head pans left and right within safe range | HXP-NAV-03, ROADMAP #2 | Servo movement — physical effect | Invoke `head_pan` with angle=-45, 0, 45; observe servo range |
| Head tilts up and down within safe range | HXP-NAV-03, ROADMAP #2 | Servo movement — physical effect | Invoke `head_tilt` with angle=-30, 0, 30; observe tilt range |
| Buzzer sounds on buzzer_on command | HXP-NAV-04, ROADMAP #3 | Audible output — not automatable | Invoke `buzzer_on`; audible confirmation; invoke `buzzer_off`; silence confirmed |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or are documented in Manual-Only Verifications
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (verified — Plan 01 has 2 tasks, both automated)
- [ ] Wave 0 covers all MISSING file references (Plan 01 creates/extends the one file with all new tests)
- [ ] No watch-mode flags (`cargo test` runs to completion)
- [ ] Feedback latency < 30s for the targeted tool suite; < 5min for the workspace
- [ ] `nyquist_compliant: true` set in frontmatter (gated until `/gsd-validate-phase` confirms the per-task sampling actually held during execution)

**Approval:** pending — to be set to `approved YYYY-MM-DD` after `/gsd-validate-phase 27.1.2` confirms the sampling rate held through Plan 01 execution.
