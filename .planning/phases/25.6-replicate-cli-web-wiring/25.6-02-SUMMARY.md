---
phase: 25.6-replicate-cli-web-wiring
plan: 02
status: completed
requirements_completed: [D-03, D-05, D-08, D-09, D-10, D-14]
files_modified:
  - crates/ironhermes-cli/src/main.rs
  - crates/ironhermes-cli/src/tui_rata/event_loop.rs
  - crates/ironhermes-cli/tests/cli_web_runtime_parity.rs
verification:
  - cargo test -p ironhermes-cli --test cli_web_runtime_parity -- --nocapture
  - cargo test -p ironhermes-cli --test cli_tool_parity -- --nocapture
completed: 2026-05-04
---

# Phase 25.6 Plan 02 Summary

Migrated CLI runtime wiring toward the shared app runtime factory and added regression locks for parity.

- `run_single`, `run_chat`, and `run_gateway` call `build_app_runtime_bundle(AppRuntimeFactoryInput { ... })` and consume the bundle handles.
- Hook and context-engine wiring invariants remain explicit (`with_hook_registry(...)` and `attach_context_engine(..., Some(hook_registry), ...)`).
- Ratatui `build_app_deps` now references the shared runtime-factory API and delegate-task wiring contract.
- Added `crates/ironhermes-cli/tests/cli_web_runtime_parity.rs` with static parity checks for factory callsites and hook-context anchors.
- Existing `cli_tool_parity` tests continue to pass, preserving tool-surface and runtime contract checks.

Verification passed with targeted CLI parity test suites.
