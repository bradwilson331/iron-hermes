---
phase: 25.6-replicate-cli-web-wiring
plan: 01
status: completed
requirements_completed: [D-01, D-02, D-03, D-04, D-05, D-08, D-09, D-10, D-14]
files_modified:
  - crates/ironhermes-agent/src/app_runtime_factory.rs
  - crates/ironhermes-agent/src/lib.rs
  - crates/ironhermes-agent/Cargo.toml
verification:
  - cargo test -p ironhermes-agent app_runtime_factory -- --nocapture
completed: 2026-05-04
---

# Phase 25.6 Plan 01 Summary

Implemented a shared app-scoped runtime factory in `ironhermes-agent` via `build_app_runtime_bundle` and `AppRuntimeBundle`.

- Added a single construction path for registry, hook registry, MCP manager, skills runtime, browser session, and cron store.
- Centralized HookRegistry listener wiring (`create_jsonl_listener`, `create_webhook_listener`) plus `drain_retry_queue`.
- Applied tool blocklist guardrails and error detail from `HooksConfig` in the factory path.
- Preserved CLI registration-order invariants (memory -> delegate_task -> cronjob -> browser -> skills -> web_extract -> execute_code).
- Re-exported runtime-factory symbols from `crates/ironhermes-agent/src/lib.rs` for downstream CLI/web consumers.

Verification passed with focused factory tests, including marker-order checks and parity assertions.
