---
phase: 25.6-replicate-cli-web-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ironhermes-agent/Cargo.toml
  - crates/ironhermes-agent/src/lib.rs
  - crates/ironhermes-agent/src/app_runtime_factory.rs
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-08, D-09, D-10, D-14]
must_haves:
  truths:
    - "A single in-process factory builds CLI-parity app-scoped runtime primitives for any surface."
    - "Factory output preserves existing CLI wiring order semantics so downstream surfaces do not diverge."
    - "Guardrail + hook listener + retry-drain wiring is built from HooksConfig in one place."
  artifacts:
    - path: "crates/ironhermes-agent/src/app_runtime_factory.rs"
      provides: "Shared app-scoped runtime factory and runtime bundle types"
      exports: ["AppRuntimeBundle", "build_app_runtime_bundle"]
    - path: "crates/ironhermes-agent/src/lib.rs"
      provides: "Public export surface for shared runtime factory"
    - path: "crates/ironhermes-agent/Cargo.toml"
      provides: "Any additional crate deps needed by factory implementation"
  key_links:
    - from: "build_app_runtime_bundle"
      to: "ToolRegistry registration APIs"
      via: "ordered registration pipeline"
      pattern: "register_.*tool|register_browser_tools_with_vision|register_web_extract_tool"
    - from: "build_app_runtime_bundle"
      to: "ironhermes_hooks::HookRegistry"
      via: "new + listener wiring + drain_retry_queue"
      pattern: "HookRegistry::new|create_jsonl_listener|create_webhook_listener|drain_retry_queue"
---

<objective>
Create a shared runtime factory in `ironhermes-agent` that encapsulates CLI-equivalent app-scoped wiring.

Purpose: enforce D-01/D-02/D-03 by making parity wiring reusable instead of copy-pasted.
Output: reusable runtime bundle + tests that lock registration and hook/guardrail behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25.6-replicate-cli-web-wiring/25.6-CONTEXT.md
@.planning/phases/25.6-replicate-cli-web-wiring/25.6-RESEARCH.md
@crates/ironhermes-cli/src/main.rs
@crates/ironhermes-cli/src/tui_rata/event_loop.rs
@crates/ironhermes-tools/src/registry.rs

<interfaces>
From `crates/ironhermes-tools/src/registry.rs`:
```rust
pub struct ToolRegistry { ... }
pub fn register_memory_tool(&mut self, manager: SharedMemoryManager)
pub fn register_cronjob_tool(&mut self, store: Arc<Mutex<JobStore>>)
pub fn register_browser_tools_with_vision(...)
pub fn register_skills_tool(...)
pub fn register_web_extract_tool(...)
pub fn register_execute_code_tool_with_process_registry(...)
```

From `crates/ironhermes-cli/src/main.rs`:
```rust
fn build_registry_with_process_registry(...) -> ToolRegistry
let mut hook_registry = ironhermes_hooks::HookRegistry::new(hooks_config.clone());
hook_registry.add_listener(ironhermes_hooks::create_jsonl_listener(...));
hook_registry.add_listener(ironhermes_hooks::create_webhook_listener(...));
ironhermes_hooks::drain_retry_queue(...).await;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared runtime bundle contracts in ironhermes-agent</name>
  <files>crates/ironhermes-agent/src/app_runtime_factory.rs, crates/ironhermes-agent/src/lib.rs</files>
  <read_first>crates/ironhermes-agent/src/lib.rs, crates/ironhermes-cli/src/main.rs, crates/ironhermes-cli/src/tui_rata/event_loop.rs, crates/ironhermes-tools/src/registry.rs</read_first>
  <behavior>
    - Test 1: Factory output type exposes app-scoped artifacts needed by both CLI and web (tool registry, hook registry, mcp manager handle, skill registry, browser session) per D-05.
    - Test 2: Factory API separates app-scoped input from per-session input so per-turn concerns are not embedded in the app bundle (D-04/D-06).
  </behavior>
  <action>Create `app_runtime_factory.rs` in `ironhermes-agent` and define explicit input/output contracts for shared app-scoped construction per D-01/D-02/D-04/D-05. Export the public API from `lib.rs`. Do not include per-session state (session_id, per-turn cancellation, message persistence) in the app-scoped bundle; those stay consumer-owned per D-06.</action>
  <acceptance_criteria>
    - `crates/ironhermes-agent/src/app_runtime_factory.rs` exports `AppRuntimeBundle` and `build_app_runtime_bundle`.
    - `AppRuntimeBundle` fields include app-scoped parity primitives (registry/hook/mcp/skills/browser) and exclude per-session primitives.
    - `crates/ironhermes-agent/src/lib.rs` publicly re-exports factory symbols.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p ironhermes-agent app_runtime_factory -- --nocapture</automated>
  </verify>
  <done>Factory contract compiles and exposes a stable app-scoped API for downstream CLI/web migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CLI-parity registration and hook/guardrail setup in the factory</name>
  <files>crates/ironhermes-agent/src/app_runtime_factory.rs, crates/ironhermes-agent/Cargo.toml</files>
  <read_first>crates/ironhermes-agent/src/app_runtime_factory.rs, crates/ironhermes-cli/src/main.rs, crates/ironhermes-cli/src/tui_rata/event_loop.rs, crates/ironhermes-tools/src/registry.rs</read_first>
  <behavior>
    - Test 1: Factory applies guardrail + error_detail from HooksConfig exactly once when blocked_tools is non-empty (D-08/D-09).
    - Test 2: Factory creates HookRegistry listeners + retry queue drain path from HooksConfig (D-10).
    - Test 3: Factory wiring order for tool registration matches the CLI load-bearing sequence (D-03/D-14).
  </behavior>
  <action>Implement `build_app_runtime_bundle` to construct app-scoped runtime with the same high-level tool surface as CLI (file/web/browser/memory/skills/delegate_task/cronjob/execute_code/MCP) per D-14, and mirror hook/guardrail wiring per D-08/D-09/D-10. Keep the construction in-process (no proxy service, no new crate) per deferred constraints. If additional dependencies are required in `ironhermes-agent/Cargo.toml`, add only crates already present in workspace and justify each in comments.</action>
  <acceptance_criteria>
    - Factory includes registration calls for browser + web_extract + execute_code + delegate_task + cronjob + skills + memory paths.
    - HooksConfig-driven BlocklistGuardrail + error_detail are applied in factory logic.
    - HookRegistry setup includes JSONL listener, webhook listeners, and retry queue drain.
    - No localhost proxy or new `ironagent-tools-api` crate appears in changed files.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p ironhermes-agent app_runtime_factory::tests:: -- --nocapture</automated>
  </verify>
  <done>Factory produces a CLI-parity app-scoped runtime bundle with guardrail/hook behavior centralized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add parity lock tests for tool surface and wiring order</name>
  <files>crates/ironhermes-agent/src/app_runtime_factory.rs</files>
  <read_first>crates/ironhermes-agent/src/app_runtime_factory.rs, crates/ironhermes-cli/src/main.rs</read_first>
  <behavior>
    - Test 1: Factory-generated schema includes expected tool names (including browser + web_extract) at least once.
    - Test 2: Ordering assertions catch accidental drift against the intended registration sequence.
    - Test 3: Hook wiring assertions verify listener configuration path is present and callable.
  </behavior>
  <action>Add focused unit/static-grep-style tests in `app_runtime_factory.rs` that enforce D-03/D-14 parity invariants and D-10 hook setup invariants. Anchor assertions to explicit function/method call presence and ordering markers so future refactors cannot silently drop wiring.</action>
  <acceptance_criteria>
    - At least one test validates browser + web_extract inclusion in factory-produced schemas.
    - At least one test validates ordering-sensitive registration markers.
    - At least one test validates HookRegistry listener/drain setup path.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p ironhermes-agent app_runtime_factory -- --nocapture</automated>
  </verify>
  <done>Factory parity is enforced by automated tests that fail on wiring drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → runtime factory | Untrusted/malformed config values influence tool + hook setup |
| webhook endpoint config → listener registration | External endpoint definitions could cause unsafe dispatch behavior |
| tool registry surface → LLM tool calls | Incorrect registration exposes unexpected capabilities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25.6-01-01 | T | app_runtime_factory tool registration | mitigate | Add ordering + presence tests that lock expected registration sequence and tool names. |
| T-25.6-01-02 | E | guardrail setup | mitigate | Apply BlocklistGuardrail + error_detail from HooksConfig in centralized factory path; test blocked-tools branch. |
| T-25.6-01-03 | I | webhook listener setup | mitigate | Reuse existing `create_webhook_listener` + retry queue path and require explicit config-driven registration only. |
| T-25.6-01-04 | D | startup initialization | accept | Keep existing async/background startup semantics for MCP and retry-drain; no new network boot blockers introduced. |
</threat_model>

<verification>
- `cargo test -p ironhermes-agent app_runtime_factory -- --nocapture`
- `cargo check -p ironhermes-agent`
</verification>

<success_criteria>
- Shared factory exists in `ironhermes-agent` and is exported.
- Factory centralizes CLI-equivalent app-scoped wiring including hooks/guardrails.
- Automated parity tests fail if tool surface or ordering drifts.
</success_criteria>

<output>
After completion, create `.planning/phases/25.6-replicate-cli-web-wiring/25.6-01-SUMMARY.md`
</output>
