---
phase: 34-webchat-and-multi-platform-gateway
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/ironhermes-agent/tests/invariants_33.rs
  - crates/ironhermes-gateway/tests/invariants_34.rs
  - crates/iron_hermes_ui/tests/session_store_shared_with_gateway.rs
autonomous: true
requirements: [LEARN-01, LEARN-02, LEARN-03, LEARN-04, LEARN-05]

must_haves:
  truths:
    - "Every agent-execution surface (CLI, Telegram, Discord, Slack, Web UI) is locked to wire skill_manage via build_app_runtime_bundle"
    - "Discord/Slack adapters are forced to route through GatewayMessageHandler.handle_with_multimodal (Learning Loop inherited)"
    - "Web UI sessions are forced to be keyed by Platform::Web in StateStore"
  artifacts:
    - path: "crates/ironhermes-agent/tests/invariants_33.rs"
      provides: "INV-33-07: AppState calls build_app_runtime_bundle"
      contains: "inv_33_07_appstate_calls_build_app_runtime_bundle"
    - path: "crates/ironhermes-gateway/tests/invariants_34.rs"
      provides: "INV-34-01/02: Discord/Slack route through handle_with_multimodal"
      contains: "inv_34_01_discord_routes_through_handle_with_multimodal"
    - path: "crates/iron_hermes_ui/tests/session_store_shared_with_gateway.rs"
      provides: "Web sessions are keyed by Platform::Web"
      contains: "web_session_keyed_by_platform_web"
  key_links:
    - from: "invariants_33.rs"
      to: "iron_hermes_ui/src/server/state.rs"
      via: "include_str!(\"../../iron_hermes_ui/src/server/state.rs\")"
      pattern: "build_app_runtime_bundle"
    - from: "invariants_34.rs"
      to: "ironhermes-gateway/src/discord.rs, slack.rs"
      via: "include_str!(\"../src/discord.rs\"), include_str!(\"../src/slack.rs\")"
      pattern: "handle_with_multimodal"
---

<objective>
Wave 0 gate plan: create the static-grep invariant scaffolds that lock every Phase 34 wiring contract before any production code is written. Tests will initially FAIL for Discord/Slack files (they don't exist yet); they go green in Waves 2-3 when the source files land.

Purpose: Per D-05 and Phase 34 quality gate, no plan can claim "done" without an invariant-grep test that survives future refactors. INV-33-07 closes the only remaining gap in the Phase 33 invariant set (AppState → build_app_runtime_bundle). INV-34-01/02 lock D-10/D-11 (Discord/Slack route through handle_with_multimodal). The web-session test locks D-07/D-08 (Platform::Web keying).

Output: 3 test files (1 modified, 2 new) containing 4 new #[test] functions. Wave 0 deliverable per VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-webchat-and-multi-platform-gateway/34-CONTEXT.md
@.planning/phases/34-webchat-and-multi-platform-gateway/34-PATTERNS.md
@.planning/phases/34-webchat-and-multi-platform-gateway/34-VALIDATION.md

<interfaces>
<!-- Existing include_str! invariant pattern from invariants_33.rs lines 42-135 — reuse verbatim -->

From crates/ironhermes-agent/tests/invariants_33.rs (existing):
```rust
const APP_RUNTIME_FACTORY_SOURCE: &str = include_str!("../src/app_runtime_factory.rs");

#[test]
fn inv_33_01_register_skill_manage_tool_in_app_runtime_factory() {
    let count = APP_RUNTIME_FACTORY_SOURCE.matches("register_skill_manage_tool").count();
    assert!(count >= 1, "INV-33-01: ... Found {count} occurrences (expected >= 1). See Phase 33 Plan 03.");
}
```

From crates/iron_hermes_ui/src/server/state.rs (line 111, verified):
```
let runtime_bundle = build_app_runtime_bundle(AppRuntimeFactoryInput { ... });
```

From crates/iron_hermes_ui/tests/server_runtime_parity.rs (existing failing test):
```rust
// list_sessions must query StateStore for Platform::Web sessions
// (Already asserts `api.contains("Platform::Web.to_string()")` against api.rs)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add INV-33-07 to invariants_33.rs (locks web path → build_app_runtime_bundle)</name>
  <files>crates/ironhermes-agent/tests/invariants_33.rs</files>
  <read_first>
    - crates/ironhermes-agent/tests/invariants_33.rs (existing 6 tests, lines 42-135 — exact include_str! pattern to mirror)
    - crates/iron_hermes_ui/src/server/state.rs (line 111 — confirms `build_app_runtime_bundle(` appears in source)
    - .planning/phases/34-webchat-and-multi-platform-gateway/34-PATTERNS.md (lines 468-499 — analog excerpt + path verification note)
  </read_first>
  <behavior>
    - Test fn `inv_33_07_appstate_calls_build_app_runtime_bundle` exists in invariants_33.rs.
    - The test compiles (the relative include_str! path resolves at compile time).
    - The test passes today (`build_app_runtime_bundle` already appears in state.rs:111 per existing wiring) — locking the chain against future regression.
    - Failure message references "Phase 33 Plan 03 / Phase 34 D-05" so future readers find the canonical decision.
  </behavior>
  <action>
    Append two items to crates/ironhermes-agent/tests/invariants_33.rs AFTER the existing INV-33-06 block:
    (1) `const WEB_STATE_SOURCE: &str = include_str!("../../iron_hermes_ui/src/server/state.rs");`
    (2) `#[test] fn inv_33_07_appstate_calls_build_app_runtime_bundle()` that asserts `WEB_STATE_SOURCE.matches("build_app_runtime_bundle").count() >= 1` with a failure message naming `INV-33-07`, the source file path, and `Phase 33 Plan 03 / Phase 34 D-05`.

    Also extend the file-header doc comment (`//!`) with one new bullet describing INV-33-07's purpose (analogous to the existing INV-33-01..06 bullets at the top of the file).

    Use the exact `let count = SOURCE.matches("...").count(); assert!(count >= N, "...Found {count}...")` form already used by INV-33-01..06. Per D-05, this is the lock that confirms skill_manage is registered for web turns via the shared runtime bundle.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-agent --test invariants_33 inv_33_07_appstate_calls_build_app_runtime_bundle</automated>
  </verify>
  <acceptance_criteria>
    - crates/ironhermes-agent/tests/invariants_33.rs contains literal string `inv_33_07_appstate_calls_build_app_runtime_bundle`
    - crates/ironhermes-agent/tests/invariants_33.rs contains literal string `include_str!("../../iron_hermes_ui/src/server/state.rs")`
    - `cargo test -p ironhermes-agent --test invariants_33 inv_33_07_appstate_calls_build_app_runtime_bundle` exits 0
    - `cargo test -p ironhermes-agent --test invariants_33` (all 7 tests) exits 0
  </acceptance_criteria>
  <done>INV-33-07 added, passing, and discoverable from the file header docs. Workspace compiles clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create invariants_34.rs with INV-34-01/02 routing gates (initially RED for Discord/Slack)</name>
  <files>crates/ironhermes-gateway/tests/invariants_34.rs</files>
  <read_first>
    - crates/ironhermes-agent/tests/invariants_33.rs (canonical include_str! + assert structure, including the file-header `//!` docs explaining why each invariant exists)
    - .planning/phases/34-webchat-and-multi-platform-gateway/34-PATTERNS.md (lines 343-383 — invariants_34.rs sketch)
    - .planning/phases/34-webchat-and-multi-platform-gateway/34-CONTEXT.md (D-10, D-11)
  </read_first>
  <behavior>
    - The file declares two test fns: `inv_34_01_discord_routes_through_handle_with_multimodal` and `inv_34_02_slack_routes_through_handle_with_multimodal`.
    - Both tests assert `SOURCE.matches("handle_with_multimodal").count() >= 1` against `include_str!("../src/discord.rs")` and `include_str!("../src/slack.rs")` respectively.
    - Until Wave 2/3 lands `discord.rs` and `slack.rs`, the file FAILS TO COMPILE (include_str! on a missing file is a compile error). This is the intended Wave 0 RED gate — execute-phase must NOT proceed past Wave 0 without verifying the gate exists, and Wave 2/3 must satisfy it.
    - Once Wave 2/3 produces the source files with the `handle_with_multimodal` reference, both tests go GREEN.
  </behavior>
  <action>
    Create crates/ironhermes-gateway/tests/invariants_34.rs as a NEW file containing:
    (1) A `//!` file-header block in the same style as invariants_33.rs lines 1-44, naming INV-34-01 and INV-34-02 and citing Phase 34 D-10/D-11.
    (2) `const DISCORD_SOURCE: &str = include_str!("../src/discord.rs");` and `const SLACK_SOURCE: &str = include_str!("../src/slack.rs");`
    (3) `#[test] fn inv_34_01_discord_routes_through_handle_with_multimodal()` asserting `DISCORD_SOURCE.matches("handle_with_multimodal").count() >= 1` with a failure message naming INV-34-01 and Phase 34 D-10.
    (4) `#[test] fn inv_34_02_slack_routes_through_handle_with_multimodal()` asserting `SLACK_SOURCE.matches("handle_with_multimodal").count() >= 1` with a failure message naming INV-34-02 and Phase 34 D-11.

    The Wave-0-RED status is intentional: per the planner contract this file MUST exist as a scaffold gating Waves 2 and 3, even though it will not compile until those waves land. Execute-phase must accept "compile failure: missing discord.rs/slack.rs" as the Wave 0 RED state and require it to flip to GREEN at the end of Wave 3. Do NOT use a `#[cfg(...)]` guard to suppress the failure — the failure IS the gate.
  </action>
  <verify>
    <automated>test -f crates/ironhermes-gateway/tests/invariants_34.rs &amp;&amp; grep -q "inv_34_01_discord_routes_through_handle_with_multimodal" crates/ironhermes-gateway/tests/invariants_34.rs &amp;&amp; grep -q "inv_34_02_slack_routes_through_handle_with_multimodal" crates/ironhermes-gateway/tests/invariants_34.rs</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/ironhermes-gateway/tests/invariants_34.rs` exists
    - File contains literal `include_str!("../src/discord.rs")` and `include_str!("../src/slack.rs")`
    - File contains literal `inv_34_01_discord_routes_through_handle_with_multimodal` and `inv_34_02_slack_routes_through_handle_with_multimodal`
    - File contains literal `handle_with_multimodal` (in assertion arg, twice — once per test)
    - `cargo test -p ironhermes-gateway --test invariants_34` is EXPECTED to fail at this point (compile error: missing discord.rs/slack.rs); compile-failure is the documented Wave 0 RED gate. Wave 3 must flip it to GREEN.
  </acceptance_criteria>
  <done>File created with both tests; Wave 0 RED gate established. Execute-phase log records "INV-34-01/02 scaffold landed; expected RED until Wave 3."</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create session_store_shared_with_gateway.rs (locks Platform::Web keying)</name>
  <files>crates/iron_hermes_ui/tests/session_store_shared_with_gateway.rs</files>
  <read_first>
    - crates/iron_hermes_ui/tests/server_runtime_parity.rs (existing test `api_sessions_and_tools_are_backed_by_real_state` — same include-via-read pattern; note its current failure mode is irrelevant here since that test reads api.rs while this one reads state.rs)
    - crates/iron_hermes_ui/src/server/state.rs (line 158 — confirms `&Platform::Web.to_string()` already appears in ensure_web_session)
    - .planning/phases/34-webchat-and-multi-platform-gateway/34-PATTERNS.md (lines 448-463 — analog excerpt)
  </read_first>
  <behavior>
    - New test `web_session_keyed_by_platform_web` exists and PASSES today (because state.rs:158 already passes `&Platform::Web.to_string()` to create_session).
    - Failure message names Phase 34 D-07/D-08 so future readers locate the locked decision.
    - The test locks the invariant against a future refactor that might change the source string (e.g. to a bare `"web"` literal or a different platform enum).
  </behavior>
  <action>
    Create crates/iron_hermes_ui/tests/session_store_shared_with_gateway.rs as a NEW file containing:
    (1) `const STATE_SOURCE: &str = include_str!("../src/server/state.rs");`
    (2) `#[test] fn web_session_keyed_by_platform_web()` asserting `STATE_SOURCE.matches("Platform::Web").count() >= 1` with a failure message: `"D-07/D-08: iron_hermes_ui/src/server/state.rs must use Platform::Web as the SessionStore key for web chat sessions. Found {count}. See Phase 34 Plan 02."`

    Do NOT use the existing `server_runtime_parity.rs::api_sessions_and_tools_are_backed_by_real_state` — that test asserts against api.rs and is a separate failing test Plan 02 will fix. This test is a state.rs-side lock that complements it.
  </action>
  <verify>
    <automated>cargo test -p iron_hermes_ui --test session_store_shared_with_gateway web_session_keyed_by_platform_web</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/iron_hermes_ui/tests/session_store_shared_with_gateway.rs` exists
    - File contains literal `include_str!("../src/server/state.rs")`
    - File contains literal `web_session_keyed_by_platform_web`
    - `cargo test -p iron_hermes_ui --test session_store_shared_with_gateway web_session_keyed_by_platform_web` exits 0
  </acceptance_criteria>
  <done>Web-session keying lock in place and green. Plan 02's api.rs fix will additionally flip the pre-existing `api_sessions_and_tools_are_backed_by_real_state` test from red to green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test source ↔ production source | include_str! reads production .rs files at compile time; tests cannot modify them — read-only structural invariants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-PHASE-SC | Tampering | Future refactor silently removes build_app_runtime_bundle, handle_with_multimodal routing, or Platform::Web keying | mitigate | Three static-grep invariants in this plan (INV-33-07, INV-34-01/02, web_session_keyed_by_platform_web) fail loudly on accidental removal — every CI run gates the wiring contract |
</threat_model>

<verification>
After all three tasks land:
- `cargo test -p ironhermes-agent --test invariants_33` — all 7 tests pass (INV-33-01..07)
- `cargo test -p iron_hermes_ui --test session_store_shared_with_gateway` — passes
- `cargo test -p ironhermes-gateway --test invariants_34` — EXPECTED RED until Wave 3 lands discord.rs/slack.rs (compile error: `include_str!("../src/discord.rs")` fails). Execute-phase logs this expected red and proceeds.
</verification>

<success_criteria>
- INV-33-07 lives in invariants_33.rs and passes; file header `//!` doc explains its purpose
- invariants_34.rs scaffold exists with two `inv_34_*` tests asserting against `handle_with_multimodal`
- session_store_shared_with_gateway.rs exists with `web_session_keyed_by_platform_web` test passing
- Plan 01 SUMMARY.md records that Wave 0 RED gate is established for Wave 3
</success_criteria>

<output>
Create `.planning/phases/34-webchat-and-multi-platform-gateway/34-01-SUMMARY.md` when done.
</output>
