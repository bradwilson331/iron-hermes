---
phase: 34-webchat-and-multi-platform-gateway
plan: 02
type: execute
wave: 1
depends_on: [34-01]
files_modified:
  - crates/iron_hermes_ui/src/server/api.rs
  - crates/ironhermes-gateway/Cargo.toml
  - crates/ironhermes-core/src/config.rs
autonomous: false
requirements: [LEARN-01, LEARN-02, LEARN-03, LEARN-04, LEARN-05]
user_setup:
  - service: discord
    why: "Phase 34 D-10 Discord adapter requires bot token"
    env_vars:
      - name: DISCORD_BOT_TOKEN
        source: "Discord Developer Portal -> Applications -> [App] -> Bot -> Token"
    dashboard_config:
      - task: "Enable 'Message Content Intent' under Bot -> Privileged Gateway Intents"
        location: "https://discord.com/developers/applications"
  - service: slack
    why: "Phase 34 D-11 Slack adapter requires app + bot tokens (Socket Mode)"
    env_vars:
      - name: SLACK_APP_TOKEN
        source: "Slack API Dashboard -> [App] -> Basic Information -> App-Level Tokens (scope: connections:write); starts with xapp-"
      - name: SLACK_BOT_TOKEN
        source: "Slack API Dashboard -> [App] -> OAuth & Permissions -> Bot User OAuth Token; starts with xoxb-"
    dashboard_config:
      - task: "Enable Socket Mode, generate app-level token with connections:write scope"
        location: "https://api.slack.com/apps -> [App] -> Socket Mode"
      - task: "Add bot token scopes: chat:write, channels:history, im:history, im:read, im:write"
        location: "https://api.slack.com/apps -> [App] -> OAuth & Permissions"

must_haves:
  truths:
    - "Web UI list_sessions queries StateStore filtered by Platform::Web (pre-existing failing test goes green)"
    - "PlatformGatewayConfig supports the Slack two-token shape (app_token + bot_token) without breaking existing telegram config parsing"
    - "serenity 0.12.5 and slack-morphism 2.22.0 dependencies are added to ironhermes-gateway/Cargo.toml AFTER package-legitimacy human verification"
  artifacts:
    - path: "crates/iron_hermes_ui/src/server/api.rs"
      provides: "list_sessions delegates to StateStore with Platform::Web filter"
      contains: "Platform::Web.to_string()"
    - path: "crates/ironhermes-gateway/Cargo.toml"
      provides: "serenity + slack-morphism dependencies"
      contains: "serenity = "
    - path: "crates/ironhermes-core/src/config.rs"
      provides: "PlatformGatewayConfig.app_token field (Slack)"
      contains: "app_token"
  key_links:
    - from: "iron_hermes_ui/src/server/api.rs"
      to: "iron_hermes_ui/src/server/state.rs (AppState.state_store)"
      via: "list_sessions reads StateStore via AppState"
      pattern: "Platform::Web.to_string\\(\\)"
    - from: "ironhermes-gateway/Cargo.toml"
      to: "ironhermes-gateway/src/discord.rs (Wave 2)"
      via: "use serenity::*"
      pattern: "^serenity\\s*="
---

<objective>
Wave 1 preparation: fix the pre-existing `api_sessions_and_tools_are_backed_by_real_state` test (D-07/D-08 SessionStore unification), add `app_token` to PlatformGatewayConfig (Slack needs two tokens per Pitfall 2), and land `serenity` + `slack-morphism` Cargo dependencies behind a blocking human-verify gate (both packages are [ASSUMED] per RESEARCH.md Package Legitimacy Audit).

Purpose: D-07/D-08 require web sessions to share the gateway's SQLite-backed StateStore keyed by Platform::Web. RESEARCH.md §SessionStore Migration Detail confirms `state.rs:158` already passes `&Platform::Web.to_string()` to create_session — the only outstanding gap is `list_sessions` in api.rs not filtering by `Platform::Web.to_string()` (the failing test pins this). Cargo deps are prep work for Waves 2 & 3.

Output: api.rs filters sessions by Platform::Web; PlatformGatewayConfig has Slack-compatible token shape; serenity + slack-morphism are dependencies of ironhermes-gateway (verified legitimate before install).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-webchat-and-multi-platform-gateway/34-CONTEXT.md
@.planning/phases/34-webchat-and-multi-platform-gateway/34-RESEARCH.md
@.planning/phases/34-webchat-and-multi-platform-gateway/34-PATTERNS.md

<interfaces>
<!-- Existing PlatformGatewayConfig (config.rs:723-740) — must remain backward compatible -->

From crates/ironhermes-core/src/config.rs:
```rust
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
#[serde(default)]
pub struct PlatformGatewayConfig {
    pub enabled: bool,
    pub token: Option<String>,           // Telegram bot token / Discord bot token / Slack BOT token (xoxb-)
    pub api_key: Option<String>,
    #[serde(default)]
    pub whitelist: Vec<i64>,             // sender allowlist (T-34-02 mitigation)
    #[serde(default = "default_session_timeout_hours")]
    pub session_timeout_hours: u64,
    #[serde(default = "default_max_concurrent_runs")]
    pub max_concurrent_runs: usize,
    #[serde(flatten)]
    pub extra: HashMap<String, serde_json::Value>,
}
```

From crates/iron_hermes_ui/tests/server_runtime_parity.rs (the failing test pinning D-07):
```rust
assert!(
    api.contains("list_sessions(") && api.contains("Platform::Web.to_string()"),
    "list_sessions must query StateStore for Platform::Web sessions"
);
```

From crates/iron_hermes_ui/src/server/state.rs (line 158, ALREADY correct):
```rust
store.create_session(session_id, &Platform::Web.to_string(), ...)
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: HUMAN VERIFY — serenity 0.12.5 + slack-morphism 2.22.0 package legitimacy</name>
  <what-built>
    Per RESEARCH.md Package Legitimacy Audit, both packages are [ASSUMED] (cargo search confirmed names but slopcheck was unavailable). Before adding to Cargo.toml, a human must confirm:

    1. https://crates.io/crates/serenity — version 0.12.5 — github.com/serenity-rs/serenity — published by the serenity-rs team
    2. https://crates.io/crates/slack-morphism — version 2.22.0 — github.com/abdolence/slack-morphism-rust — published by abdolence
  </what-built>
  <how-to-verify>
    1. Open https://crates.io/crates/serenity in a browser. Confirm:
       - Crate name `serenity` (not a typosquat like `seranity` / `serenity-rs-bot`)
       - Repository links to github.com/serenity-rs/serenity (canonical Discord Rust SDK)
       - Latest 0.12.x version available
       - Download count is in the millions (de facto standard library)
    2. Open https://crates.io/crates/slack-morphism. Confirm:
       - Crate name exactly `slack-morphism`
       - Repository links to github.com/abdolence/slack-morphism-rust
       - Version 2.22.0 (or close) exists
       - Has Socket Mode support documented
    3. Confirm both feature names referenced in research:
       - serenity 0.12 features `client`, `gateway`, `http`, `model`, `cache`, `rustls_backend` (check the crate's published Cargo.toml on crates.io / docs.rs)
       - slack-morphism features `socket-mode` (in 2.22.0; if renamed, capture the actual name)
    4. Per project CLAUDE.md "no backwards-compatibility hacks" — confirm there are no known supply-chain incidents on either crate.
  </how-to-verify>
  <action>
    Pause execution until a human inspects both crates.io URLs above and types "approved" (or supplies corrections). On approval, Task 2 proceeds with the exact crate names + versions + feature flags shown in <what-built>. If the human reports corrections (renamed feature, different version, alternate crate), Task 2 must apply the corrected values rather than the defaults. If the human types "abort", planner reconvenes with the alternative (twilight-* for Discord, Events API HTTP for Slack) — do NOT proceed to Cargo additions.
  </action>
  <verify>
    <human-check>Human confirms via "approved" or supplies corrections; planner records resolution in 34-02-SUMMARY.md before Task 2 runs.</human-check>
  </verify>
  <done>Human approval logged; corrections (if any) captured for Task 2 to apply.</done>
  <resume-signal>Type "approved" to proceed with Task 2 (Cargo additions), OR provide corrections (e.g. "slack-morphism socket-mode feature is now named X" / "abort, use twilight-* instead").</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add serenity + slack-morphism deps and Slack two-token field to config</name>
  <files>crates/ironhermes-gateway/Cargo.toml, crates/ironhermes-core/src/config.rs</files>
  <read_first>
    - crates/ironhermes-gateway/Cargo.toml (current dependency block, lines 9-38)
    - crates/ironhermes-core/src/config.rs lines 700-740 (GatewayConfig + PlatformGatewayConfig)
    - .planning/phases/34-webchat-and-multi-platform-gateway/34-RESEARCH.md (Standard Stack section + Pitfall 2 — Slack needs TWO tokens)
    - .planning/phases/34-webchat-and-multi-platform-gateway/34-PATTERNS.md (lines 285-310 — Cargo.toml additions with TLS-backend caveat)
  </read_first>
  <action>
    (a) In crates/ironhermes-gateway/Cargo.toml, after the existing dependency block (currently ends with `tempfile = "3"` at line 38) add:
    ```
    # Phase 34 D-10: Discord adapter (per D-01..D-15 user-locked decisions; rustls_backend matches workspace reqwest stack)
    serenity = { version = "0.12.5", default-features = false, features = ["client", "gateway", "http", "model", "cache", "rustls_backend"] }
    # Phase 34 D-11: Slack Socket Mode adapter
    slack-morphism = { version = "2.22.0", features = ["socket-mode"] }
    ```
    If Task 1 surfaced a different `socket-mode` feature name or TLS backend constraint, use the corrected value.

    (b) In crates/ironhermes-core/src/config.rs, extend `PlatformGatewayConfig` (line 723) with:
    ```
    /// Slack Socket Mode app-level token (xapp-). Telegram/Discord leave this None.
    #[serde(default)]
    pub app_token: Option<String>,
    ```
    Place the new field directly after the existing `pub token: Option<String>,` line. The `#[serde(default)]` keeps backward compatibility with existing telegram-only configs (existing config.yaml files without `app_token` parse cleanly per Phase 33 D-15 backward-compat-via-default convention). Update the doc comment on `token` to read: `// Bot token: Telegram (TELEGRAM_BOT_TOKEN), Discord (DISCORD_BOT_TOKEN), or Slack bot token (xoxb-, SLACK_BOT_TOKEN)`.

    Do NOT add a separate per-platform struct. The single PlatformGatewayConfig with optional fields keeps the `HashMap<String, PlatformGatewayConfig>` keyed by platform name ("telegram", "discord", "slack") shape unchanged.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-gateway --no-default-features 2>&amp;1 | tail -20 &amp;&amp; cargo build -p ironhermes-core 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p ironhermes-core` exits 0
    - `cargo build -p ironhermes-gateway` exits 0 (compiles with new deps; Discord/Slack source files arrive in Waves 2/3)
    - `grep -c '^serenity\s*=' crates/ironhermes-gateway/Cargo.toml` returns >= 1
    - `grep -c '^slack-morphism\s*=' crates/ironhermes-gateway/Cargo.toml` returns >= 1
    - `grep -n 'pub app_token: Option<String>' crates/ironhermes-core/src/config.rs` returns exactly 1 match
    - All existing tests still pass: `cargo test -p ironhermes-core --lib` exits 0
  </acceptance_criteria>
  <done>Cargo deps added (legitimacy gate cleared in Task 1); PlatformGatewayConfig supports Slack's two-token shape; existing tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix list_sessions in api.rs to query StateStore filtered by Platform::Web</name>
  <files>crates/iron_hermes_ui/src/server/api.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/server/api.rs (entire file — find the existing `list_sessions` function and any current stub/TODO)
    - crates/iron_hermes_ui/src/server/state.rs lines 30, 148-167 (state_store + ensure_web_session — see how StateStore is accessed)
    - crates/iron_hermes_ui/tests/server_runtime_parity.rs lines 26-40 (the pre-existing failing test naming the contract)
    - crates/ironhermes-state source (look for the StateStore method that lists sessions by source/platform; mirror gateway pattern)
    - .planning/phases/34-webchat-and-multi-platform-gateway/34-RESEARCH.md (lines 514-535 — D-07 interpretation)
  </read_first>
  <behavior>
    - `list_sessions` in api.rs calls the StateStore via `AppState.state_store` and passes `Platform::Web.to_string()` as the source filter.
    - `list_sessions` does NOT contain a `TODO` marker or `Ok(vec![])` empty stub.
    - The pre-existing test `api_sessions_and_tools_are_backed_by_real_state` in server_runtime_parity.rs FLIPS from RED to GREEN.
    - Today's fresh test `web_session_keyed_by_platform_web` (Plan 01 Task 3) continues to pass.
  </behavior>
  <action>
    In crates/iron_hermes_ui/src/server/api.rs, replace the current `list_sessions` implementation (currently returns an empty stub or TODO per the failing test) with a real query against `AppState.state_store`:

    1. Take `&AppState` (or the existing handler-owned reference) and lock `state_store`.
    2. Call the existing StateStore session-listing method, passing `Platform::Web.to_string()` as the source/platform filter argument.
    3. Map the resulting Vec<SessionRecord> (or equivalent) into the API response type already used by api.rs (mirror existing response shapes — do NOT introduce a new response struct).
    4. Drop the std::sync::MutexGuard before any `.await` (mirror state.rs:188-193 pattern: `let result = { let store = self.state_store.lock().unwrap(); store.list_sessions_by_source(&Platform::Web.to_string()) }?;`).

    If the StateStore listing method signature differs (e.g. takes `&str` for source vs `Option<&str>`), match the existing signature exactly; do NOT add a new method to StateStore in this task. Use the literal `Platform::Web.to_string()` string in the call site so the regression test's `api.contains("Platform::Web.to_string()")` assertion passes.

    Remove any `// TODO` or `Ok(vec![])` placeholder. Per project CLAUDE.md "no backwards-compatibility hacks" — there is no `_old_list_sessions` to preserve. Delete the stub entirely.
  </action>
  <verify>
    <automated>cargo test -p iron_hermes_ui --test server_runtime_parity api_sessions_and_tools_are_backed_by_real_state</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Platform::Web.to_string()" crates/iron_hermes_ui/src/server/api.rs` returns >= 1
    - `grep -c "TODO" crates/iron_hermes_ui/src/server/api.rs` (within list_sessions / list_tools) returns 0
    - `cargo test -p iron_hermes_ui --test server_runtime_parity api_sessions_and_tools_are_backed_by_real_state` exits 0
    - `cargo test -p iron_hermes_ui --test session_store_shared_with_gateway web_session_keyed_by_platform_web` (from Plan 01) still exits 0
    - `cargo build -p iron_hermes_ui` exits 0
  </acceptance_criteria>
  <done>list_sessions returns real Platform::Web sessions from StateStore; pre-existing failing test goes green; D-07 unification verified end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io ↔ developer machine | Cargo install pulls source from registry; supply-chain risk if name is typosquatted |
| config.yaml ↔ gateway process | Operator-provided tokens; injected via Option<String> fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-SC | Tampering | npm/crate installs of serenity + slack-morphism | mitigate | Task 1 is a blocking human-verify checkpoint per RESEARCH.md Package Legitimacy Audit — both packages are [ASSUMED]; install only after human confirms canonical crate names + repos |
| T-34-01 | Information Disclosure | Bot tokens stored in PlatformGatewayConfig | mitigate | Tokens remain Option<String>; never logged. Existing TELEGRAM_BOT_TOKEN handling pattern in runner.rs:421-430 (`resolve_token`) inherited automatically. Slack's xapp- token (app_token field) mirrors this — `resolve_token(&cfg.app_token)` lookup with env-var fallback in Plan 05 |
| T-34-05 | Spoofing | Session key collision across platforms | mitigate | api.rs `list_sessions` filters by `Platform::Web.to_string()` exclusively; gateway already filters by `Platform::Telegram` etc. SessionKey(platform, chat_id) discriminant prevents cross-surface bleeds. Locked by Plan 01 Task 3 grep test. |
</threat_model>

<verification>
- `cargo build -p ironhermes-gateway` compiles with new serenity + slack-morphism deps
- `cargo test -p iron_hermes_ui --test server_runtime_parity` — all 3 tests green (including the previously failing one)
- `cargo test -p iron_hermes_ui --test session_store_shared_with_gateway` — still green (no regression from Plan 01)
- `cargo build --workspace` — clean
</verification>

<success_criteria>
- Both crates.io packages human-verified before install (Task 1 checkpoint)
- serenity + slack-morphism added to ironhermes-gateway/Cargo.toml; build is clean
- PlatformGatewayConfig.app_token field added (Slack two-token shape) without breaking existing telegram configs
- list_sessions queries StateStore filtered by Platform::Web; pre-existing failing test goes green
- Plan 02 SUMMARY.md notes config schema change (add to STATE.md key decisions)
</success_criteria>

<output>
Create `.planning/phases/34-webchat-and-multi-platform-gateway/34-02-SUMMARY.md` when done.
</output>
