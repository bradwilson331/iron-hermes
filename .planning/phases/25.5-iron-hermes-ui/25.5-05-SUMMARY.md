---
phase: 25.5-iron-hermes-ui
plan: 05
subsystem: ui
tags: [dioxus, mocks, cfg-gate, integration-verification]

# Dependency graph
requires:
  - phase: 25.5-03
    provides: "Server functions (list_slash_commands, get_config_summary, list_sessions)"
  - phase: 25.5-04
    provides: "WebSocket streaming chat, AgentLoop wiring"
provides:
  - "Mock module gated behind cfg(any(test, feature = 'demo'))"
  - "Zero mock/demo references in production component code"
  - "Production build contains no mock data"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "cfg(any(test, feature = 'demo')) gating for demo/mock data"
    - "Dynamic status text from server function data instead of static mock constant"

key-files:
  created: []
  modified:
    - "crates/iron_hermes_ui/src/main.rs"
    - "crates/iron_hermes_ui/src/state.rs"
    - "crates/iron_hermes_ui/src/components/warp_hermes.rs"

key-decisions:
  - "Replaced mock STATUS_TEXT with dynamic format from config_summary server data — maintains /status functionality with real data"
  - "cargo check -p iron_hermes_ui (without --features server) pre-existing failure documented but not fixed — out of scope (ws module references in warp_hermes.rs not feature-gated, predates Plan 05)"

patterns-established:
  - "cfg gate pattern: #[cfg(any(test, feature = 'demo'))] for mock/demo data throughout iron_hermes_ui"

requirements-completed: []

# Metrics
duration: 2min
completed: 2026-05-04
---

# Phase 25.5 Plan 05: Integration Verification + Mock Retirement Summary

**Mock module cfg-gated behind test/demo flags; zero mock references in production components; /status handler wired to real config data**

## Performance

- **Duration:** 2 min
- **Started:** 2026-05-04T01:39:54Z
- **Completed:** 2026-05-04T01:42:27Z
- **Tasks:** 1 of 2 (Task 2 is checkpoint:human-verify — awaiting human verification)
- **Files modified:** 3

## Accomplishments
- Gated `mod mocks` behind `#[cfg(any(test, feature = "demo"))]` in main.rs — mock module not compiled in production builds
- Gated `demo_block_entries()` and `demo_messages()` behind cfg flags in state.rs (joining already-gated `demo_palette_items()` and `demo_tabs()`)
- Replaced last production mock reference (`crate::mocks::shell_outputs::STATUS_TEXT`) with dynamically formatted status text from `config_summary` server function
- Verified zero `crate::mocks` and zero `demo_*` references in `src/components/` (production path)
- All compilation targets pass: `cargo check -p iron_hermes_ui --features server` ✅, `cargo check` (workspace) ✅

## Task Commits

Each task was committed atomically:

1. **Task 1: Gate mocks module and retire remaining demo data** - `19334dc` (feat)

**Task 2:** checkpoint:human-verify — automated `cargo check` verification passed; human smoke test (dx serve + end-to-end chat) pending.

## Files Created/Modified
- `crates/iron_hermes_ui/src/main.rs` — Added `#[cfg(any(test, feature = "demo"))]` gate on `mod mocks`
- `crates/iron_hermes_ui/src/state.rs` — Added cfg gates on `demo_block_entries()` and `demo_messages()`
- `crates/iron_hermes_ui/src/components/warp_hermes.rs` — Replaced mock `STATUS_TEXT` with dynamic config_summary-based status text

## Decisions Made
- Replaced mock `STATUS_TEXT` constant with dynamic format string using already-fetched `config_summary` data — real model/provider/context_length displayed in `/status` output
- `cargo check -p iron_hermes_ui` (without `--features server`) failure is pre-existing — `warp_hermes.rs` references `crate::server::ws` which is cfg-gated behind `server` feature, but the component code is not. This predates Plan 05 and is out of scope.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Replaced mock STATUS_TEXT with real config data**
- **Found during:** Task 1 (warp_hermes.rs mock audit)
- **Issue:** `/status` palette handler used `crate::mocks::shell_outputs::STATUS_TEXT` — would break when mocks module is cfg-gated
- **Fix:** Replaced with dynamic format string using already-fetched `config_summary` server function data (model, provider, context_length)
- **Files modified:** crates/iron_hermes_ui/src/components/warp_hermes.rs
- **Verification:** `cargo check -p iron_hermes_ui --features server` passes; grep confirms zero mock references
- **Committed in:** 19334dc (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug fix — mock reference would break production build)
**Impact on plan:** Essential fix — without it, gating mocks module would cause compilation failure. No scope creep.

## Issues Encountered
- `cargo check -p iron_hermes_ui` (no features) fails with 9 errors — pre-existing issue where `warp_hermes.rs` references `crate::server::ws` module which is behind `#[cfg(feature = "server")]`. Documented but not fixed (out of scope).

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| T-25.5-14 mitigated | crates/iron_hermes_ui/src/main.rs | Mock data module now cfg-gated — never compiled into production binaries |

## Known Stubs

None — all mock/demo references replaced with real data sources or properly cfg-gated.

## Next Phase Readiness
- Mock retirement complete — production builds have zero mock code
- Human verification smoke test needed: `dx serve` → browser → chat loop → all 13 components with real data
- Phase 25.5 completion gate: awaiting Task 2 human-verify checkpoint

## Self-Check: PASSED

- [x] main.rs exists with cfg-gated mocks
- [x] state.rs exists with cfg-gated demo functions
- [x] warp_hermes.rs exists with mock reference replaced
- [x] Commit 19334dc found in git log
- [x] SUMMARY.md created

---
*Phase: 25.5-iron-hermes-ui*
*Completed: 2026-05-04 (Task 1 only; Task 2 awaiting human verification)*
