---
phase: 25.5-iron-hermes-ui
plan: 05
type: execute
wave: 3
depends_on: ["25.5-03", "25.5-04"]
files_modified:
  - crates/iron_hermes_ui/src/mocks/mod.rs
  - crates/iron_hermes_ui/src/mocks/agent_steps.rs
  - crates/iron_hermes_ui/src/mocks/personalities.rs
  - crates/iron_hermes_ui/src/mocks/shell_outputs.rs
  - crates/iron_hermes_ui/src/state.rs
  - crates/iron_hermes_ui/src/main.rs
  - crates/iron_hermes_ui/src/components/warp_hermes.rs
autonomous: false
requirements: []
user_setup:
  - service: ironhermes
    why: "LLM provider must be configured for agent chat to work"
    env_vars:
      - name: ANTHROPIC_API_KEY or OPENAI_API_KEY
        source: "Provider dashboard (Anthropic/OpenAI/OpenRouter)"
    dashboard_config:
      - task: "Ensure config.yaml has model + provider set"
        location: "~/.ironhermes/config.yaml"
must_haves:
  truths:
    - "dx serve launches a working web UI connected to real backend"
    - "Typing a message and pressing Enter sends it to the LLM and streams back a response"
    - "All 13 shell components display real data (no mock data in production path)"
    - "Mock module is gated behind cfg(test) or demo feature"
    - "cargo build at workspace root still does not build the UI"
  artifacts:
    - path: "crates/iron_hermes_ui/src/mocks/mod.rs"
      provides: "Mock module gated behind cfg flags"
      contains: "cfg(any(test"
    - path: "crates/iron_hermes_ui/src/components/warp_hermes.rs"
      provides: "Fully wired WarpHermes with zero mock dependencies"
  key_links:
    - from: "crates/iron_hermes_ui/src/components/warp_hermes.rs"
      to: "crates/iron_hermes_ui/src/server/api.rs"
      via: "Server function calls for static data"
      pattern: "list_slash_commands\\|get_config_summary"
    - from: "crates/iron_hermes_ui/src/components/warp_hermes.rs"
      to: "crates/iron_hermes_ui/src/server/ws.rs"
      via: "WebSocket for streaming chat"
      pattern: "ws_chat\\|ChatStreamEvent"
---

<objective>
Integration verification + mock retirement + end-to-end smoke test

Purpose: Final integration plan — gate the entire mocks/ module behind `#[cfg(any(test, feature = "demo"))]` so production builds have zero mock code, verify all 13 components are wired to real data (D-02 completion), run `dx serve` and perform a human verification smoke test confirming the full chat loop works end-to-end. This is the phase completion gate.

Output: Clean production build with all mocks retired; human-verified working UI
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25.5-iron-hermes-ui/25.5-CONTEXT.md
@crates/iron_hermes_ui/CLAUDE.md
@crates/iron_hermes_ui/src/main.rs
@crates/iron_hermes_ui/src/mocks/mod.rs
@crates/iron_hermes_ui/src/components/warp_hermes.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate mocks module and retire remaining demo data</name>
  <files>crates/iron_hermes_ui/src/main.rs, crates/iron_hermes_ui/src/mocks/mod.rs, crates/iron_hermes_ui/src/state.rs, crates/iron_hermes_ui/src/components/warp_hermes.rs</files>
  <read_first>
    - crates/iron_hermes_ui/src/main.rs (mod mocks declaration)
    - crates/iron_hermes_ui/src/mocks/mod.rs (module structure)
    - crates/iron_hermes_ui/src/state.rs (remaining demo functions)
    - crates/iron_hermes_ui/src/components/warp_hermes.rs (any remaining mock references)
  </read_first>
  <action>
1. **Gate the mocks module** in `main.rs`:
   Change `mod mocks;` to `#[cfg(any(test, feature = "demo"))] mod mocks;`
   This ensures the mock module is not compiled in production builds.

2. **Gate remaining demo functions** in `state.rs`:
   - `demo_block_entries()` → `#[cfg(any(test, feature = "demo"))]`
   - `demo_messages()` → `#[cfg(any(test, feature = "demo"))]`
   - `demo_palette_items()` → `#[cfg(any(test, feature = "demo"))]` (if not already done in Plan 03)
   - `demo_tabs()` → `#[cfg(any(test, feature = "demo"))]` (if not already done in Plan 03)

3. **Verify zero mock references in production warp_hermes.rs**:
   Scan `warp_hermes.rs` for any remaining `crate::mocks::*` or `demo_*` calls. If any exist as fallbacks, replace with:
   - Empty vec for block/message initialization
   - Static "loading..." text for string fields
   - The `use_server_future` / `use_resource` pattern with proper loading states

4. **Final audit**: grep the entire `src/components/` directory for `demo_` and `crate::mocks` — count must be zero in non-test code.

5. **Verify compilation**:
   - `cargo check -p iron_hermes_ui --features server` → exits 0
   - `cargo check -p iron_hermes_ui` → exits 0
   - `cargo check` (workspace root) → exits 0
  </action>
  <acceptance_criteria>
    - `grep 'cfg.*test.*demo.*mod mocks' crates/iron_hermes_ui/src/main.rs` returns a match (mocks gated)
    - `grep -rn 'crate::mocks' crates/iron_hermes_ui/src/components/` returns 0 matches
    - `grep -rn 'demo_block_entries\|demo_messages\|demo_palette_items\|demo_tabs' crates/iron_hermes_ui/src/components/` returns 0 matches
    - `cargo check -p iron_hermes_ui --features server` exits 0
    - `cargo check -p iron_hermes_ui` exits 0
    - `cargo check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -rn 'crate::mocks\|demo_block_entries\|demo_messages\|demo_palette_items\|demo_tabs' crates/iron_hermes_ui/src/components/ 2>/dev/null | wc -l && echo "---" && cargo check -p iron_hermes_ui --features server 2>&1 | tail -3</automated>
  </verify>
  <done>All mock/demo code gated behind cfg flags; zero mock references in production components; all three compilation targets pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — end-to-end smoke test of web UI</name>
  <files>crates/iron_hermes_ui/</files>
  <action>
    Launch the UI with `dx serve`, open in browser, and verify all 13 components display real data.
    Submit a chat message and confirm streaming response appears. Verify command palette shows real commands.
    This is a human verification checkpoint — the operator must confirm the UI works end-to-end.
  </action>
  <what-built>
    Complete Phase 25.5 integration:
    - Dioxus fullstack UI wired to real IronHermes backend
    - 13 shell components receiving real data (commands, config, sessions, agent output, tool calls)
    - WebSocket streaming chat from AgentLoop
    - Platform::Web session keying
    - Mock data retired from production path
    - Separate binary (not part of default cargo build)
  </what-built>
  <how-to-verify>
    1. Ensure IronHermes is configured (config.yaml has model + provider + API key)
    2. From workspace root: `cd crates/iron_hermes_ui && dx serve`
    3. Open http://localhost:8080 (or whatever port dx reports)
    4. Verify the UI loads with:
       - Title bar showing "New Session" tab (or real sessions if any exist)
       - Status bar showing real model name and provider
       - Command palette (⌘K) showing real 49 slash commands
       - Empty block stream (ready for input)
    5. Type a message in the input box and press Enter
    6. Verify:
       - Scanner animates (agent working)
       - Block stream updates with streaming agent response
       - Agent panel shows tool calls (if agent uses tools)
       - Token count updates in status bar
    7. Try ⌘K → select /help → verify output appears
    8. Try typing another message → verify conversation continues
    9. Verify: `cargo build` at workspace root does NOT compile the UI (default-members exclusion)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues encountered</resume-signal>
  <verify>
    <automated>cargo check -p iron_hermes_ui --features server 2>&1 | tail -3</automated>
  </verify>
  <done>Human verified: dx serve launches working UI, chat loop works end-to-end, all 13 components display real data</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (same as Plan 04 — no new boundaries) | |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25.5-14 | I (Info Disclosure) | mock data in production | mitigate | cfg-gated mocks/demo module ensures mock data never ships in production binaries |
</threat_model>

<verification>
- Zero `crate::mocks` references in production components
- Zero `demo_*` calls in production components
- mocks module gated behind cfg(test|demo)
- All three cargo check targets pass
- Human verification: dx serve launches, chat works end-to-end
</verification>

<success_criteria>
- All mock/demo code gated — not compiled in production builds
- dx serve launches the UI successfully
- User can chat with the agent through the web UI
- All 13 components display real data
- cargo build at workspace root does not build UI
- Human verification passed
</success_criteria>

<output>
After completion, create `.planning/phases/25.5-iron-hermes-ui/25.5-05-SUMMARY.md`
</output>
