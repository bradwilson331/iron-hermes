---
phase: 07.5-skills-housekeeping
plan: 02
subsystem: gateway/cron-enforcement, documentation
tags: [skills, cron, enforcement, traceability]
dependency_graph:
  requires: [07.5-01]
  provides: [cron-active-skills-wiring, roadmap-traceability-fix]
  affects: [ironhermes-gateway]
tech_stack:
  added: []
  patterns: [active-skills-pre-population, builder-pattern-chaining]
key_files:
  created: []
  modified:
    - crates/ironhermes-gateway/src/runner.rs
    - .planning/ROADMAP.md
    - .planning/STATE.md
decisions:
  - "D-11/D-12: Cron jobs resolve attached skill names to SkillRecords and pre-populate active_skills before AgentLoop starts"
  - "D-13: ROADMAP/STATE references corrected from SKILL-05..09 to SKILL-05..08; directory name kept as-is to avoid breaking references"
  - "D-14: REQUIREMENTS.md already correct - no changes needed"
metrics:
  duration: "3 minutes"
  completed: "2026-04-10"
  tasks: 2
  files_modified: 3
---

# Phase 07.5 Plan 02: Cron Enforcement Wiring + ROADMAP/REQUIREMENTS Traceability Cleanup Summary

Cron-triggered agent runs now pre-populate active_skills from attached skill SkillRecords before AgentLoop starts, giving cron jobs the same allowed_tools enforcement as conversation mode. ROADMAP and STATE.md traceability corrected from SKILL-05..09 to SKILL-05..08.

## Task Results

### Task 1: Wire active_skills pre-population in execute_cron_job
**Commit:** 0a0812a

In `execute_cron_job`, added active_skills pre-population loop that resolves each attached skill name via `skill_reg.find()` and pushes the `SkillRecord` clone into a shared `Arc<Mutex<Vec<SkillRecord>>>`. This is then wired to the AgentLoop via `.with_active_skills(active_skills)` before hook registry wiring. The same enforcement check added in Plan 01's `execute_tool_call` now applies to cron-triggered runs.

Added `test_cron_job_prepopulates_active_skills` test verifying:
- Skill with `allowed-tools: [web_read]` is loaded correctly
- Pre-population logic resolves and clones the SkillRecord
- The active_skills vector contains the skill with its allowed_tools

### Task 2: Fix ROADMAP Phase 07.2 title + verify REQUIREMENTS.md state
**Commit:** bd2fd21

- Updated STATE.md Roadmap Evolution entry from "SKILL-05..09" to "SKILL-05..08" with note about SKILL-09 deferral to v2
- Updated ROADMAP.md Phase 07.5 gap closure description to reflect completed fixes
- Verified REQUIREMENTS.md is already correct per D-14:
  - SKILL-09 at line 120 in v2 section with "moved from v1.1" note
  - SKILL-13 at line 83 in v1.1 section with "(backlog)" annotation
  - Traceability table has SKILL-09 mapped to v2 and SKILL-13 to Backlog
- Full workspace tests pass with zero regressions (335+ tests, 0 failures)

## Deviations from Plan

None - plan executed exactly as written.

## Verification Results

1. `cargo test -p ironhermes-gateway test_cron_job_prepopulates` - PASSED (1/1)
2. `cargo build` - PASSED (zero errors)
3. `cargo test --workspace` - PASSED (all tests, 0 failures)
4. STATE.md contains "SKILL-05..08" - CONFIRMED
5. REQUIREMENTS.md SKILL-09 in v2, SKILL-13 in Backlog - CONFIRMED (no changes needed)

## Self-Check: PASSED

All files exist, all commits verified.
