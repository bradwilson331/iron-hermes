---
phase: 21.3-model-metadata-models-dev-context-lengths-token-estimation
plan: 01
subsystem: core
tags: [model-metadata, tiktoken, bpe, tokenization, model-registry]

# Dependency graph
requires: []
provides:
  - ModelMetadata, ModelCapabilities, ModelRegistry structs with 37-model static table
  - TokenEstimator wrapping tiktoken-rs BPE singletons for accurate token counting
  - TiktokenEncoding enum with cl100k_base/o200k_base selection
  - Global token estimator with OnceLock and heuristic fallback
  - warm_tiktoken_singletons() for eager startup initialization
  - Alias map resolving versioned, legacy, and provider-prefixed model names
affects: [21.3-02, 21.3-03, 21.3-04, context-compression, agent-loop, status-line]

# Tech tracking
tech-stack:
  added: [tiktoken-rs 0.11.0]
  patterns: [static lookup table with alias map, singleton BPE encoding, OnceLock global estimator]

key-files:
  created:
    - crates/ironhermes-core/src/model_metadata.rs
    - crates/ironhermes-core/src/token_estimator.rs
  modified:
    - crates/ironhermes-core/Cargo.toml
    - crates/ironhermes-core/src/lib.rs

key-decisions:
  - "tiktoken-rs 0.11.0 singletons return &'static CoreBPE (not Arc<RwLock<CoreBPE>> as research assumed) -- no .read() needed"
  - "37 models in static table across 7 families: Claude, GPT, Llama, Gemini, Mistral/Mixtral, DeepSeek, Qwen"
  - "Helper functions cl100k() and o200k() keep static table DRY and readable"

patterns-established:
  - "ModelRegistry::lookup() resolution chain: cache -> static table -> alias map -> strip provider prefix -> None"
  - "TiktokenEncoding::from_name() defaults to Cl100kBase for unknown encodings (D-08 fallback)"
  - "OnceLock<TokenEstimator> global with heuristic fallback when uninitialized"

requirements-completed: [D-01, D-05, D-06, D-07, D-08, D-10, D-12, D-13, D-15]

# Metrics
duration: 5min
completed: 2026-04-19
---

# Phase 21.3 Plan 01: Core Model Metadata & Token Estimator Summary

**ModelMetadata registry with 37-model static table and tiktoken-rs BPE token estimation replacing text.len()/4 heuristic**

## Performance

- **Duration:** 5 min
- **Started:** 2026-04-19T21:31:04Z
- **Completed:** 2026-04-19T21:36:06Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- ModelMetadata, ModelCapabilities, ModelRegistry types with 37-model static lookup table covering Claude, GPT, Llama, Gemini, Mistral/Mixtral, DeepSeek, and Qwen families
- Alias map resolving versioned names (claude-sonnet-4-20250514), legacy names (claude-3-5-sonnet-latest), and provider-prefixed names (anthropic/claude-sonnet-4) to canonical IDs
- TokenEstimator wrapping tiktoken-rs 0.11.0 cl100k_base and o200k_base singletons for accurate BPE token counting
- Global estimator with OnceLock pattern and graceful heuristic fallback, plus warm_tiktoken_singletons() for startup preloading
- 20 new unit tests (12 model_metadata + 8 token_estimator), all passing with 0 regressions (238 total)

## Task Commits

Each task was committed atomically:

1. **Task 1: ModelMetadata, ModelCapabilities, ModelRegistry with static table and alias map** - `9982a63` (feat)
2. **Task 2: TokenEstimator wrapping tiktoken-rs singletons and wire into lib.rs** - `d1cde6a` (feat)

_Both tasks followed TDD: RED (failing tests) then GREEN (implementation)_

## Files Created/Modified
- `crates/ironhermes-core/src/model_metadata.rs` - ModelMetadata, ModelCapabilities, ModelRegistry structs, 37-model static table, alias map, lookup with cache override
- `crates/ironhermes-core/src/token_estimator.rs` - TokenEstimator with BPE counting, TiktokenEncoding enum, global estimator with OnceLock, warm function
- `crates/ironhermes-core/Cargo.toml` - Added tiktoken-rs = "0.11.0" dependency
- `crates/ironhermes-core/src/lib.rs` - Added pub mod declarations and re-exports for both new modules

## Decisions Made
- tiktoken-rs 0.11.0 singleton API returns `&'static CoreBPE` directly (lazy_static), not `Arc<RwLock<CoreBPE>>` as the research document assumed -- calls go directly to `bpe.encode_with_special_tokens(text)` without `.read()`
- Static table includes 37 models (exceeding the 30 minimum) with claude-opus-4.6 at 1M context
- Helper functions cl100k() and o200k() reduce boilerplate in the static table builder

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed tiktoken-rs singleton API usage**
- **Found during:** Task 2 (TokenEstimator implementation)
- **Issue:** Plan and research specified `.read()` on singletons assuming `Arc<RwLock<CoreBPE>>` return type, but tiktoken-rs 0.11.0 singletons use lazy_static and return `&'static CoreBPE` directly
- **Fix:** Removed `.read()` calls; call `encode_with_special_tokens` directly on the `&CoreBPE` reference
- **Files modified:** crates/ironhermes-core/src/token_estimator.rs
- **Verification:** All 8 token_estimator tests pass
- **Committed in:** d1cde6a (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** API correction necessary for compilation. No scope change.

## Issues Encountered
None beyond the singleton API deviation noted above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- ModelMetadata and TokenEstimator types are compiled, exported, and tested in ironhermes-core
- Plan 02 can wire metadata through ProviderResolver, AgentLoop, ContextCompressor, and StatusLine
- Plan 03 can implement disk cache and API fetch
- Plan 04 can add CLI subcommand and slash command

## Self-Check: PASSED

- FOUND: crates/ironhermes-core/src/model_metadata.rs
- FOUND: crates/ironhermes-core/src/token_estimator.rs
- FOUND: 21.3-01-SUMMARY.md
- FOUND: commit 9982a63
- FOUND: commit d1cde6a
- 238 tests passed, 0 failed, 3 ignored

---
*Phase: 21.3-model-metadata-models-dev-context-lengths-token-estimation*
*Completed: 2026-04-19*
