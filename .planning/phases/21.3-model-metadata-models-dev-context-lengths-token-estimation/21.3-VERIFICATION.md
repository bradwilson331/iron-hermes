---
phase: 21.3-model-metadata-models-dev-context-lengths-token-estimation
verified: 2026-04-19T23:45:00Z
status: human_needed
score: 14/14 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 13/14
  gaps_closed:
    - "Disk cache is auto-loaded at startup and merged into ModelRegistry used for context_length resolution"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Run `hermes models list` and verify colored table output"
    expected: "Colored table with 37+ models, context lengths in K/M format, capabilities in green, dimmed footer with count"
    why_human: "Visual formatting, ANSI color rendering, column alignment cannot be verified programmatically"
  - test: "Run `hermes models fetch` with network access"
    expected: "Fetches from models.dev and OpenRouter, reports counts, saves cache to ~/.ironhermes/models-cache.json"
    why_human: "Requires live network access to external APIs"
  - test: "Start `hermes chat`, type `/models refresh`"
    expected: "Plain text output without ANSI codes showing fetch results"
    why_human: "Requires running interactive REPL session with active tokio runtime"
  - test: "Start `hermes chat`, type `/models info claude-sonnet-4`"
    expected: "6-line plain text with comma-formatted numbers (200,000 not 200.0K)"
    why_human: "Requires running interactive REPL session"
---

# Phase 21.3: Model Metadata & models.dev Verification Report

**Phase Goal:** Replace hardcoded DEFAULT_CONTEXT_LENGTH and text.len()/4 heuristic with model-aware metadata and accurate BPE tokenization. Provide CLI and slash command surfaces for model browsing and cache management.
**Verified:** 2026-04-19T23:45:00Z
**Status:** human_needed
**Re-verification:** Yes -- after gap closure (Plan 05)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | ModelRegistry.lookup('claude-sonnet-4') returns ModelMetadata with context_length=200000 | VERIFIED | model_metadata.rs line 197: insert("claude-sonnet-4", cl100k(200_000, ...)). Test `lookup_claude_sonnet_4` passes. |
| 2 | ModelRegistry.lookup('anthropic/claude-sonnet-4') resolves via prefix stripping | VERIFIED | model_metadata.rs lines 72-88: strip provider/ prefix path. Test `lookup_provider_prefix_stripping` passes. |
| 3 | ModelRegistry.lookup('claude-sonnet-4-20250514') resolves via alias map | VERIFIED | model_metadata.rs line 258: alias insert. Test `lookup_versioned_alias` passes. |
| 4 | ModelRegistry.lookup('nonexistent-model') returns None | VERIFIED | model_metadata.rs lines 54-91: returns None at end. Test `lookup_nonexistent_returns_none` passes. |
| 5 | TokenEstimator::new(Cl100kBase).count('hello world') returns nonzero BPE count | VERIFIED | token_estimator.rs lines 37-49: BPE counting. Tests `count_cl100k_hello_world` and `count_differs_from_heuristic` pass. |
| 6 | TokenEstimator encoding selection uses model metadata tokenizer field | VERIFIED | TiktokenEncoding::from_name maps tokenizer string to enum. main.rs uses model_metadata.tokenizer for init_global_estimator. |
| 7 | Resolution precedence: cache entry overrides static table entry for same key | VERIFIED | model_metadata.rs lookup() checks cache before static table (lines 56-59). Test `merge_cache_overrides_static_table` passes. **Reinforced by gap closure:** provider.rs test `provider_resolver_cache_overrides_static_for_same_model` proves 999_999 overrides 200_000 through full ProviderResolver path. |
| 8 | ResolvedEndpoint.context_length() checks config first (D-06) | VERIFIED | provider.rs lines 34-40: config_context_length checked first, then model_metadata, then DEFAULT. Test `test_user_config_context_length_overrides_metadata` passes. |
| 9 | estimate_tokens in context_compressor uses tiktoken via global_estimate_tokens | VERIFIED | context_compressor.rs line 1: `use ironhermes_core::global_estimate_tokens;` line 7: delegates to it. No text.len()/4 in function body. |
| 10 | All hardcoded 128_000 eliminated from main.rs, handler.rs, runner.rs, agent_wiring.rs | VERIFIED | grep 128_000 returns zero matches in all four files. |
| 11 | hermes models list/fetch/info CLI subcommands exist and compile | VERIFIED | models_cmd.rs exists, ModelsSubcommand enum with List/Fetch/Info, wired into Commands::Models in main.rs. CLI builds clean. |
| 12 | /models refresh and /models info slash commands registered and dispatch | VERIFIED | registry.rs line 120: CommandDef::new("models"...). handlers.rs line 48: dispatch to cmd_models. cmd_models_refresh uses block_in_place. 7 handler tests pass. |
| 13 | Static table contains 30+ models across 7+ families | VERIFIED | 37 models across Claude (7), GPT (8), Llama (6), Gemini (3), Mistral/Mixtral (6), DeepSeek (2), Qwen (4). Test `static_table_has_at_least_30_models` passes. |
| 14 | Disk cache auto-loaded at startup and merged into resolution chain | VERIFIED | **Gap closed by Plan 05.** provider.rs line 107: `let disk_cache = ModelsCache::load();` line 108: `model_registry.merge_cache(disk_cache.into_metadata_map());` in ProviderResolver::build(). Two regression tests pass: `provider_resolver_loads_disk_cache_at_build` (cache-only model resolves with 500_000 context) and `provider_resolver_cache_overrides_static_for_same_model` (cache overrides static 200K with 999_999). |

**Score:** 14/14 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/ironhermes-core/src/model_metadata.rs` | ModelMetadata, ModelCapabilities, ModelRegistry structs and static lookup table | VERIFIED | All structs present. 37-model static table. 12 unit tests. |
| `crates/ironhermes-core/src/token_estimator.rs` | TokenEstimator wrapping tiktoken-rs singletons | VERIFIED | TokenEstimator, TiktokenEncoding, global estimator with OnceLock. 8 unit tests. |
| `crates/ironhermes-core/src/models_cache.rs` | ModelsCache, ModelsCacheEntry, fetch functions | VERIFIED | Full disk I/O, parse functions, fetch_all with fallback. 14 unit tests. |
| `crates/ironhermes-cli/src/models_cmd.rs` | hermes models list/fetch/info CLI subcommands | VERIFIED | ModelsSubcommand enum, handle_models_command, pure render functions. 5 unit tests. |
| `crates/ironhermes-core/src/commands/handlers.rs` | /models slash command handler | VERIFIED | cmd_models dispatcher, cmd_models_refresh (block_in_place), cmd_models_info (plain text). 7 tests. |
| `crates/ironhermes-core/src/commands/registry.rs` | models command definition | VERIFIED | Line 120: CommandDef::new("models"...) with args_hint and platform(Universal). |
| `crates/ironhermes-core/Cargo.toml` | tiktoken-rs dependency | VERIFIED | tiktoken-rs = "0.11.0" and reqwest = { workspace = true } present. |
| `crates/ironhermes-core/src/lib.rs` | pub mod and re-exports | VERIFIED | pub mod model_metadata, models_cache, token_estimator. All re-exports present. |
| `crates/ironhermes-core/src/provider.rs` | ResolvedEndpoint with model_metadata, disk cache auto-load | VERIFIED | model_metadata and config_context_length fields. context_length() with D-06 precedence. ModelsCache::load() + merge_cache() in build(). 2 regression tests. |
| `crates/ironhermes-agent/src/context_compressor.rs` | tiktoken-backed token estimation | VERIFIED | imports and delegates to global_estimate_tokens. |
| `crates/ironhermes-agent/src/agent_wiring.rs` | Parameterized attach_context_engine | VERIFIED | context_length: usize parameter on line 50. No DEFAULT_CONTEXT_LENGTH constant. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| model_metadata.rs | token_estimator.rs | TiktokenEncoding::from_name | WIRED | main.rs uses model_metadata.tokenizer for from_name() call |
| lib.rs | model_metadata.rs | pub mod and re-exports | WIRED | pub mod + pub use present |
| lib.rs | token_estimator.rs | pub mod and re-exports | WIRED | pub mod + pub use present |
| lib.rs | models_cache.rs | pub mod and re-exports | WIRED | pub mod + pub use present |
| provider.rs | model_metadata.rs | ModelRegistry::lookup in build() | WIRED | Line 243: endpoint.model_metadata = model_registry.lookup(...).cloned() |
| provider.rs | models_cache.rs | ModelsCache::load() in build() | WIRED | **Gap closed.** Line 107-108: load + merge_cache in build() |
| context_compressor.rs | token_estimator.rs | global_estimate_tokens | WIRED | Line 1: import, line 7: delegates |
| main.rs | provider.rs | context_length from resolver | WIRED | resolver.resolve_for_main().context_length() used for with_compression and StatusLineState |
| models_cmd.rs | models_cache.rs | fetch_all, ModelsCache | WIRED | imports and calls fetch_all, ModelsCache::load/save |
| models_cmd.rs | model_metadata.rs | ModelRegistry for listing | WIRED | builds registry and calls all_models() |
| handlers.rs | models_cache.rs | fetch_all for /models refresh | WIRED | block_in_place wrapping fetch_all().await |
| handler.rs (gateway) | provider.rs | context_length from resolver | WIRED | resolver.resolve_for_main().context_length() in constructor and attach call |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| model_metadata.rs | ModelRegistry.table | build_static_table() | Yes -- 37 entries | FLOWING |
| model_metadata.rs | ModelRegistry.cache | ModelsCache::load() via merge_cache | Yes -- disk cache entries | FLOWING |
| token_estimator.rs | GLOBAL_ESTIMATOR | init_global_estimator() | Yes -- tiktoken BPE | FLOWING |
| provider.rs | endpoint.model_metadata | ModelRegistry::lookup() | Yes -- static + cache | FLOWING |
| context_compressor.rs | estimate_tokens() | global_estimate_tokens() | Yes -- tiktoken BPE | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Core tests pass | cargo test -p ironhermes-core --lib | 265 passed, 0 failed, 3 ignored | PASS |
| Agent tests pass | cargo test -p ironhermes-agent --lib | 195 passed, 0 failed | PASS |
| CLI builds | cargo build -p ironhermes-cli | Compiles (warnings only) | PASS |
| Gap closure tests pass | cargo test -- provider_resolver_loads_disk_cache provider_resolver_cache_overrides_static --test-threads=1 | 2 passed, 0 failed | PASS |
| No 128_000 in main.rs | grep 128_000 main.rs | 0 matches | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| D-01 | Plan 01 | Static lookup table with 30+ known models | SATISFIED | 37 models in build_static_table() across 7 families |
| D-02 | Plan 03, 05 | Disk cache auto-loaded at startup, merge over static | SATISFIED | **Gap closed.** ModelsCache::load() + merge_cache() in ProviderResolver::build(). Regression tests prove it. |
| D-03 | Plan 03 | API fetch fallback: models.dev first, OpenRouter fallback | SATISFIED | fetch_all() tries both, FetchResult reports per-source |
| D-04 | Plan 04 | hermes models CLI + /models slash command | SATISFIED | models_cmd.rs (List/Fetch/Info), handlers.rs (cmd_models) |
| D-05 | Plan 01 | Canonical ID + alias map normalization | SATISFIED | build_alias_map() with versioned/legacy/provider-prefixed aliases |
| D-06 | Plan 01, 02, 05 | Precedence: config > cache > static > default | SATISFIED | **Gap closed.** context_length() checks config first, then metadata (now includes cache via merge), then DEFAULT |
| D-07 | Plan 01 | tiktoken-rs BPE replacing text.len()/4 | SATISFIED | tiktoken-rs 0.11.0. TokenEstimator wraps singletons. |
| D-08 | Plan 01 | Encoding from metadata, fallback cl100k | SATISFIED | TiktokenEncoding::from_name defaults to Cl100kBase |
| D-09 | Plan 02 | tiktoken used in context_compressor | SATISFIED | estimate_tokens delegates to global_estimate_tokens |
| D-10 | Plan 01 | ModelMetadata struct fields | SATISFIED | context_length, max_output_tokens, tokenizer, capabilities |
| D-11 | N/A | Pricing deferred | SATISFIED | By design -- no pricing fields |
| D-12 | Plan 01 | ModelMetadata in ironhermes-core | SATISFIED | model_metadata.rs in crates/ironhermes-core/src/ |
| D-13 | Plan 01 | tiktoken-rs as core dependency | SATISFIED | tiktoken-rs = "0.11.0" in Cargo.toml. TokenEstimator in core. |
| D-14 | Plan 02 | ProviderResolver carries metadata | SATISFIED | model_metadata field on ResolvedEndpoint, populated in build() |
| D-15 | Plan 01 | tracing::warn for unknown models | SATISFIED | tracing::warn! in context_length_or_default |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none found in phase 21.3 files) | -- | -- | -- | -- |

### Human Verification Required

### 1. hermes models list output formatting

**Test:** Run `hermes models list` and verify the colored table is readable, aligned, and includes all expected model families.
**Expected:** Colored table with 37+ models, context lengths in K/M format, capabilities in green, dimmed footer with count.
**Why human:** Visual formatting, ANSI color rendering, column alignment cannot be verified programmatically.

### 2. hermes models fetch network behavior

**Test:** Run `hermes models fetch` (with network access and optionally OPENROUTER_API_KEY set).
**Expected:** Fetches from models.dev and OpenRouter, reports counts, saves cache to ~/.ironhermes/models-cache.json.
**Why human:** Requires live network access to external APIs. Cannot test without running servers.

### 3. /models refresh in active REPL session

**Test:** Start `hermes chat`, type `/models refresh`, verify plain text output without ANSI codes.
**Expected:** Fetching message, per-source counts, completion message. All in plain text.
**Why human:** Requires running interactive REPL session with active tokio runtime for block_in_place.

### 4. /models info in active REPL session

**Test:** Start `hermes chat`, type `/models info claude-sonnet-4`, verify compact detail output.
**Expected:** 6-line plain text with comma-formatted numbers (200,000 not 200.0K).
**Why human:** Requires running interactive REPL session.

### Gaps Summary

No gaps remaining. The single gap from the initial verification (disk cache not auto-loaded at startup) has been closed by Plan 05. Two regression tests confirm the fix:
- `provider_resolver_loads_disk_cache_at_build`: proves cache-only models resolve through ProviderResolver
- `provider_resolver_cache_overrides_static_for_same_model`: proves cache entries override static table entries

All 14 observable truths verified. All 15 requirements (D-01 through D-15) satisfied. All artifacts exist, are substantive, wired, and data flows through them. No anti-patterns found.

4 items require human verification (visual output formatting and network/interactive behavior).

---

_Verified: 2026-04-19T23:45:00Z_
_Verifier: Claude (gsd-verifier)_
