---
phase: 07.1
plan: 02
type: execute
wave: 2
depends_on: [07.1-01]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []
requirements_addressed: []
# Note: phase 07.1 CREATES new requirement IDs as its deliverable.
# This plan mints SKILL-05..SKILL-NN (and potentially SELF-11/SELF-12 extensions)
# and appends them to REQUIREMENTS.md. The new IDs become addressable
# by downstream phases via /gsd-plan-milestone-gaps.

must_haves:
  truths:
    - "REQUIREMENTS.md contains one new SKILL-NN entry per gap dossier marked for v1.1 in 07.1-GAP-REPORT.md"
    - "REQUIREMENTS.md contains self-improvement extensions (e.g. SELF-11, SELF-12) for any gap marked for v2 Self-Improvement parity"
    - "Every new REQ-ID appears in both the relevant section body AND the Traceability table at the bottom of REQUIREMENTS.md"
    - "Every new REQ-ID also appears in 07.1-GAP-REPORT.md's Summary Appendix so the audit and requirements stay in sync"
    - "New REQ-IDs follow the same bullet/schema pattern as existing SKILL-01..SKILL-04 (checkbox, bold ID, single-sentence description)"
    - "No existing SKILL-01..SKILL-04 or SELF-01..SELF-10 entries are modified — append-only"
    - "ROADMAP.md is NOT modified in this plan (D-16 defers roadmap expansion)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "New SKILL-05+ entries plus any SELF-* extensions"
      contains: "SKILL-05"
  key_links:
    - from: ".planning/REQUIREMENTS.md Skills System section"
      to: ".planning/REQUIREMENTS.md Traceability table"
      via: "Each new REQ-ID must appear in both"
      pattern: "SKILL-0[5-9]|SKILL-[1-9][0-9]"
    - from: ".planning/REQUIREMENTS.md"
      to: "07.1-GAP-REPORT.md Summary Appendix"
      via: "Same REQ-ID set"
      pattern: "SKILL-0[5-9]"
---

<objective>
Mint new `SKILL-05..SKILL-NN` requirement IDs (plus any `SELF-*` extensions for SELF-08/SELF-10 parity) from the gaps identified in Plan 01's `07.1-GAP-REPORT.md`, and append them to `.planning/REQUIREMENTS.md` per D-15.

Purpose: Make the gaps addressable by future planning runs. Downstream phases pick up these REQ-IDs via `/gsd-plan-milestone-gaps` or `/gsd-insert-phase`. Until this plan lands, the gaps exist only as a report — requirements are the source of truth for the roadmap.

Output: Updated `.planning/REQUIREMENTS.md` with new entries in the v1.1 Skills System section, any v2 Self-Improvement extensions, and corresponding rows in the Traceability table. Also a final consistency cross-check between the report and the requirements file.

**Phase character:** AUDIT-ONLY. This plan only writes to `.planning/REQUIREMENTS.md`. No Rust source files are modified. No ROADMAP.md mutation (deferred per D-16).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-CONTEXT.md
@.planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md
@.planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint SKILL-05+ REQ-IDs and append to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md (entire file — the source of gaps and proposed REQ-IDs)
    - .planning/REQUIREMENTS.md (entire file — to see existing SKILL-01..SKILL-04 schema and the traceability table layout)
    - .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-CONTEXT.md (D-15 for mutation rules)
  </read_first>
  <action>
**Objective:** Append new REQ-ID entries to `.planning/REQUIREMENTS.md` for every gap identified in `07.1-GAP-REPORT.md`'s summary appendix.

**Step 1 — Extract the draft REQ-ID list from the report.**

Open `07.1-GAP-REPORT.md` and locate the `<summary_appendix>` section. Find the "v1.1 Additions", "v2 Additions", and "Backlog" tables. Build an in-memory list of tuples:

```
[
  (draft_id, description, severity, milestone, source_gap_anchor),
  ...
]
```

The draft IDs in the report are tentative (e.g., SKILL-05, SKILL-06, ...). You will assign FINAL sequential IDs in the order they appear in the report, starting from SKILL-05 (the next available ID after SKILL-04 which is the last existing SKILL-NN in REQUIREMENTS.md).

For SELF extensions: if any gap is filed under v2 Self-Improvement (SELF-08 / SELF-10 parity extensions), mint SELF-11, SELF-12, ... starting from the next available SELF-NN after SELF-10.

**Step 2 — Read the existing SKILL-01..SKILL-04 schema from REQUIREMENTS.md.**

The existing entries use this exact pattern (copied verbatim from REQUIREMENTS.md lines 74-78):

```markdown
### Skills System

- [ ] **SKILL-01**: Agent discovers skill documents from skills directories (~/.ironhermes/skills/, ~/.agents/skills/, project-level)
- [ ] **SKILL-02**: Skills use progressive disclosure — catalog (name+description) loaded at session start, full content loaded only on activation
- [ ] **SKILL-03**: Skill documents follow the agentskills.io open standard (SKILL.md with name/description frontmatter, Markdown body)
- [ ] **SKILL-04**: Agent can list, view, and activate skills via a dedicated skills tool during conversation
```

New entries MUST follow this pattern exactly:
- `- [ ] **SKILL-NN**: <single-sentence description>`
- Description format: "<what the agent/system does>, <constraint or behavior>"
- No multi-line descriptions. No sub-bullets. One line each.

The existing SELF-* schema (lines 104-109) is identical in form:

```markdown
### Self-Improvement (Advanced)

- **SELF-07**: Version history for context files with rollback capability
- **SELF-08**: Skills subsystem: procedural knowledge stored in SKILL.md directories
- **SELF-09**: Session-end reflection: agent evaluates performance and optionally updates context
- **SELF-10**: Skill auto-creation: agent saves multi-step procedures as reusable skills after 5+ tool calls
```

Note the v2 section does NOT use the `- [ ]` checkbox — it uses `- **ID**: desc`. Follow that pattern for any new SELF-NN entries.

**Step 3 — Append new SKILL-NN entries to the v1.1 Skills System section.**

Per D-15:
- BLOCKER / HIGH gaps → append to `v1.1 Requirements` → `Skills System` section, right after SKILL-04.
- MEDIUM / nice-to-have → append to the same section with a brief note or at the end of the section.
- Self-improvement parity → append to `v2 Requirements` → `Self-Improvement (Advanced)` section, right after SELF-10.

Use the Edit tool (not Write) to insert lines in-place. Preserve all existing content exactly. The edit point for v1.1 Skills System is the line immediately after `- [ ] **SKILL-04**: ...`. The edit point for v2 Self-Improvement is the line immediately after `- **SELF-10**: ...`.

For EACH new entry:
1. Assign the next sequential ID.
2. Write a single-sentence description derived from the report's gap dossier (Delta + Recommended scope).
3. Keep it under 200 chars.
4. Match the existing tone (imperative verb, clear capability).

**Example derivations** (illustrative only — actual entries come from the report):

If the report has:
```
### Gap: Skills Hub git source adapter [C1]
Delta: No code for cloning git repos as skill sources. Python skills_hub.py:245-380 implements GitSourceAdapter.
Recommended REQ-ID: SKILL-05
```

Then the REQUIREMENTS.md entry is:
```
- [ ] **SKILL-05**: Skills Hub supports git repositories as a skill source, allowing users to install skills from remote git URLs
```

**Step 4 — Update the Traceability table at the bottom of REQUIREMENTS.md.**

The table currently ends around line 201 with `BATCH-04 | Phase 10 | Pending`. For each new REQ-ID, append a new row in the same format:

```
| SKILL-05 | TBD (post-07.1) | Pending |
```

Use `TBD (post-07.1)` as the Phase column because per D-16, roadmap expansion is deferred to a human-reviewed step. The phase column will be filled in by `/gsd-plan-milestone-gaps` or `/gsd-insert-phase` after this plan lands.

**Step 5 — Update the Coverage summary at the very end of REQUIREMENTS.md.**

Currently:
```
- v1.1 requirements: 23 total
- Mapped to phases: 29 (v1.0) + 23 (v1.1)
- Unmapped: 0
```

Update to:
```
- v1.1 requirements: <23 + N_new_v1.1> total
- Mapped to phases: 29 (v1.0) + 23 (original v1.1) + 0 (post-07.1 gap entries, pending phase assignment)
- Unmapped post-07.1: <N_new_total>
```

Also update the `Last updated:` line at the very bottom from `2026-04-08 after v1.1 Automation roadmap created (Phases 5-10)` to `2026-04-09 after Phase 07.1 skill system gap analysis (SKILL-05..SKILL-NN added)`.

**Rules:**
- Append-only. Do NOT modify any existing SKILL-01..SKILL-04 or SELF-01..SELF-10 entry text.
- Do NOT touch any Rust source files.
- Do NOT touch `.planning/ROADMAP.md` (D-16).
- Use the Edit tool for surgical inserts. If you must rewrite the file, ensure the diff shows only additions, no deletions of existing entries.
- Every new REQ-ID must appear in BOTH the section body AND the Traceability table. Missing either is a verification failure.
  </action>
  <verify>
    <automated>bash -c 'R=.planning/REQUIREMENTS.md; grep -q "SKILL-05" "$R" && grep -c "^\- \[ \] \*\*SKILL-" "$R" | awk "{exit (\$1 < 5)}" && grep -q "| SKILL-05 " "$R" && grep -q "^\- \[x\] \*\*SKILL-01\*\*" "$R" -o -q "^\- \[ \] \*\*SKILL-01\*\*" "$R" && grep -q "^\- \[ \] \*\*SKILL-04\*\*" "$R" && grep -q "2026-04-09" "$R"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep '^- \[ \] \*\*SKILL-05\*\*' .planning/REQUIREMENTS.md` returns >= 1 (SKILL-05 appended in v1.1 Skills System section)
    - `grep -cE '^- \[ \] \*\*SKILL-0[5-9]\*\*|^- \[ \] \*\*SKILL-[1-9][0-9]\*\*' .planning/REQUIREMENTS.md` returns >= 1 (at least one new entry added — the exact count depends on how many gaps the report yielded)
    - Existing SKILL-01..SKILL-04 entries are unmodified (`grep '^- \[ \] \*\*SKILL-01\*\*' .planning/REQUIREMENTS.md` returns 1 and the line text matches the original)
    - `grep '^- \[ \] \*\*SKILL-04\*\*' .planning/REQUIREMENTS.md` returns 1
    - `grep '| SKILL-05 ' .planning/REQUIREMENTS.md` returns >= 1 (traceability row exists)
    - Every new `**SKILL-NN**` entry in the Skills System section has a corresponding `| SKILL-NN |` row in the Traceability table (count match)
    - `grep '2026-04-09' .planning/REQUIREMENTS.md` returns >= 1 (Last updated line refreshed)
    - `git status --porcelain .planning/REQUIREMENTS.md` shows the file as modified
    - `git status --porcelain crates/` shows no modified Rust files
    - `git status --porcelain .planning/ROADMAP.md` shows ROADMAP.md is NOT modified (D-16)
  </acceptance_criteria>
  <done>All gaps from 07.1-GAP-REPORT.md's summary appendix now have corresponding REQ-IDs in REQUIREMENTS.md, with both section-body entries and traceability table rows. Existing entries untouched. Last updated line bumped. No Rust files or ROADMAP.md modified.</done>
</task>

<task type="auto">
  <name>Task 2: Self-consistency check and cross-reference GAP-REPORT.md ↔ REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the freshly mutated file from Task 1)
    - .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md (the source of truth for which IDs should exist)
    - .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-CONTEXT.md (D-15 for the mutation rules being verified)
  </read_first>
  <action>
**Objective:** Verify that every REQ-ID in `07.1-GAP-REPORT.md`'s summary appendix exists in `.planning/REQUIREMENTS.md`, and that every new REQ-ID in REQUIREMENTS.md is traceable back to a gap dossier. Also back-fill the REPORT with the FINAL REQ-IDs if Task 1 had to renumber.

**Step 1 — Extract REQ-ID sets from both files.**

```bash
# REQ-IDs in the report's summary appendix
grep -oE 'SKILL-[0-9]+|SELF-[0-9]+' .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md | sort -u > /tmp/07.1-report-ids.txt

# REQ-IDs in REQUIREMENTS.md (new — SKILL-05+ and SELF-11+)
grep -oE 'SKILL-[0-9]+|SELF-[0-9]+' .planning/REQUIREMENTS.md | sort -u > /tmp/07.1-req-ids.txt

# Diff
diff /tmp/07.1-report-ids.txt /tmp/07.1-req-ids.txt
```

Every SKILL-NN (N>=5) or SELF-NN (N>=11) in the report MUST appear in REQUIREMENTS.md. If anything is missing, add it. If anything in REQUIREMENTS.md is NOT in the report, either (a) add it to the report's appendix or (b) remove it from REQUIREMENTS.md — they must be in sync.

**Step 2 — Back-fill finalized REQ-IDs into GAP-REPORT.md.**

If Task 1 had to renumber (e.g., because the report draft had SKILL-05..SKILL-20 but the final assignment differs), update the report's dossiers and summary appendix tables to use the FINAL IDs. Use Edit tool for in-place updates. The report's `Recommended REQ-ID:` lines and `Proposed REQ-ID:` table columns must match what is now in REQUIREMENTS.md exactly.

**Step 3 — Verify the traceability table is complete.**

For every new REQ-ID, confirm there is a row in the Traceability table at the bottom of REQUIREMENTS.md:

```bash
for id in $(grep -oE '^\- \[ \] \*\*SKILL-[0-9]+\*\*|^\- \*\*SELF-[0-9]+\*\*' .planning/REQUIREMENTS.md | grep -oE 'SKILL-[0-9]+|SELF-[0-9]+' | awk -F'-' '$2 >= 5 || ($1 == "SELF" && $2 >= 11)'); do
  grep -q "| $id " .planning/REQUIREMENTS.md || echo "MISSING TRACEABILITY ROW: $id"
done
```

If any ID is missing a traceability row, add it in the same format used by existing rows: `| SKILL-NN | TBD (post-07.1) | Pending |`.

**Step 4 — Verify count math in the Coverage block.**

The Coverage block should reflect:
```
- v1.1 requirements: (23 original) + (N new from 07.1) = 23+N total
- v2 requirements: original + any SELF-NN extensions
- Mapped to phases: 29 (v1.0) + 23 (original v1.1)
- Unmapped post-07.1: N (pending phase assignment per D-16)
```

Verify N matches the actual count of new REQ-IDs added. If not, fix the Coverage block.

**Step 5 — Final audit-phase hygiene check.**

Run these assertions:

```bash
# No Rust files modified
git status --porcelain crates/ 2>/dev/null | grep -v '^??' && echo 'FAIL: Rust files modified' || echo 'PASS: no Rust mods'

# ROADMAP.md untouched
git status --porcelain .planning/ROADMAP.md 2>/dev/null | grep -q . && echo 'FAIL: ROADMAP.md modified (violates D-16)' || echo 'PASS: ROADMAP.md clean'

# Only expected files mutated
git status --porcelain .planning/REQUIREMENTS.md .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/ | awk '{print $2}' | sort
```

The last command should show ONLY:
- `.planning/REQUIREMENTS.md`
- `.planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-01-PLAN.md`
- `.planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-02-PLAN.md`
- `.planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md`
- (and corresponding SUMMARY files)

Any other file in the diff is a violation of D-01.

**Do NOT touch any Rust source files. Do NOT modify ROADMAP.md.**
  </action>
  <verify>
    <automated>bash -c 'comm -23 <(grep -oE "SKILL-[0-9]+|SELF-[0-9]+" .planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-GAP-REPORT.md | sort -u | awk -F- "/SKILL/ && \$2+0 >= 5 {print} /SELF/ && \$2+0 >= 11 {print}") <(grep -oE "SKILL-[0-9]+|SELF-[0-9]+" .planning/REQUIREMENTS.md | sort -u) | wc -l | awk "{exit (\$1 != 0)}" && [ -z "$(git status --porcelain crates/ 2>/dev/null | grep -v \"^??\")" ] && [ -z "$(git status --porcelain .planning/ROADMAP.md 2>/dev/null)" ]'</automated>
  </verify>
  <acceptance_criteria>
    - Every REQ-ID (SKILL-NN where N>=5, or SELF-NN where N>=11) that appears in `07.1-GAP-REPORT.md` also appears in `.planning/REQUIREMENTS.md`
    - Every new REQ-ID in REQUIREMENTS.md has a matching row in the Traceability table
    - The Coverage block's count reflects the total new REQ-IDs added
    - `git status --porcelain crates/` is empty except for untracked files (no modifications to Rust source)
    - `git status --porcelain .planning/ROADMAP.md` is empty (ROADMAP.md untouched per D-16)
    - `git diff .planning/REQUIREMENTS.md` shows ONLY additions (no deletions of existing entries)
    - The report's `Recommended REQ-ID:` lines match the final IDs in REQUIREMENTS.md
  </acceptance_criteria>
  <done>GAP-REPORT.md and REQUIREMENTS.md are in full sync on REQ-IDs. Traceability table is complete. Coverage block is updated. No Rust files, no ROADMAP.md modifications. The phase is ready for handoff to future phases that will implement the new requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — this is an audit-only phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-02 | N/A | Requirements document mutation | accept | This plan appends entries to `.planning/REQUIREMENTS.md` (a markdown planning document) and back-fills REQ-IDs into `07.1-GAP-REPORT.md`. No code, services, endpoints, or data handling are introduced. No new attack surface. Per D-01, no Rust source files are modified. Per D-16, no roadmap modification. The security gate is satisfied by the phase's nature as pure documentation. |
</threat_model>

<verification>
**Per-task verification is encoded in each `<verify>` block.**

**Phase-level verification:**
- Every gap dossier in `07.1-GAP-REPORT.md` has a matching REQ-ID entry in `.planning/REQUIREMENTS.md`
- Every new REQ-ID in REQUIREMENTS.md is present in both the section body and the Traceability table
- Existing SKILL-01..SKILL-04 and SELF-01..SELF-10 entries are unchanged (append-only)
- No Rust source files modified (D-01)
- No changes to `.planning/ROADMAP.md` (D-16)
- Coverage count in REQUIREMENTS.md reflects the new totals
</verification>

<success_criteria>
1. `.planning/REQUIREMENTS.md` has new `SKILL-05..SKILL-NN` entries (and any SELF-NN extensions) corresponding to every v1.1 / v2 gap identified in Plan 01's report
2. The Traceability table at the bottom has a row for each new REQ-ID with status `Pending` and phase `TBD (post-07.1)`
3. `07.1-GAP-REPORT.md` and `.planning/REQUIREMENTS.md` use identical REQ-IDs for the same gaps (cross-referenceable in both directions)
4. Phase 07.1's entire output (GAP-REPORT.md + REQUIREMENTS.md diff) forms a self-contained handoff package for `/gsd-plan-milestone-gaps` and future phase planning
5. No Rust source files modified, no ROADMAP.md modified — the phase's documentation-only character is preserved
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-skill-system-gap-analysis-does-current-code-cover-hermes-ski/07.1-02-SUMMARY.md` following the standard summary template:
- What was appended (list of new SKILL-NN and SELF-NN IDs)
- Totals: v1.1 additions, v2 additions, backlog
- How downstream planning picks these up (`/gsd-plan-milestone-gaps` / `/gsd-insert-phase`)
- Cross-reference confirmation: report <-> requirements in sync
</output>
