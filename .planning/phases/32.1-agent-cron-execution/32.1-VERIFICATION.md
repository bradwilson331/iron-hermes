---
phase: 32.1-agent-cron-execution
verified: 2026-05-15T00:00:00Z
status: human_needed
score: 44/44
overrides_applied: 0
human_verification:
  - test: "Run `hermes cron tick` against a real HERMES_HOME with at least one due job and confirm the job fires, output is saved, and delivery is attempted"
    expected: "Tick completes, job last_status updated, output file written to ~/.hermes/cron/output/<id>/"
    why_human: "Requires a live HERMES_HOME + agent environment; cannot be run in a static grep pass"
  - test: "Run `hermes cron daemon` in a terminal, verify it ticks every ~60s, then press Ctrl-C and confirm clean exit"
    expected: "Daemon runs, ticks fire, Ctrl-C produces 'Cron daemon stopped.' and process exits cleanly"
    why_human: "Long-running interactive process; cannot be verified statically"
  - test: "Run `hermes cron trigger <name>` for an existing job, then run `hermes cron tick` and confirm the triggered job runs immediately"
    expected: "next_run_at is set to ~now by trigger, tick picks it up within the grace window"
    why_human: "Requires real job store state on disk"
  - test: "Craft a jobs.json with origin: \"telegram\" (string, not object) and open JobStore — confirm the job loads with origin=None and a tracing::warn! is logged"
    expected: "No serde error; origin field is None; warn log contains 'origin'"
    why_human: "Requires a running process with tracing subscriber to observe the warn! output"
  - test: "Set TELEGRAM_HOME_CHANNEL and run a cron job with deliver='telegram' to verify actual Telegram message delivery"
    expected: "Message arrives in the configured home channel"
    why_human: "Requires live Telegram bot credentials and network"
---

# Phase 32.1: Agent Cron Execution — Verification Report

**Phase Goal:** Port the missing Python cron scheduler behaviors into the Rust `ironhermes-cron` crate (PARITY.md §10 + §11), then extract agent-execution + tick-orchestration into a new `ironhermes-cron-runner` crate, then cut over both the gateway and a new CLI surface to the new crate.
**Verified:** 2026-05-15
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | PARITY.md §10 renamed from "out of scope" to "pulled into Phase 32.1" | VERIFIED | `grep -n "pulled into Phase 32.1" PARITY.md` → line 223; §10 heading reads "Agent execution — pulled into Phase 32.1" |
| 2 | PARITY.md §11 lists Phase 32.1 in-scope items and defers only JobState::Error | VERIFIED | Lines 261, 298 confirm the in-scope preface and closing sentence; JobState::Error item #2 explicitly deferred |
| 3 | PARITY.md has >= 3 "Phase 32.1" references | VERIFIED | `grep -c "Phase 32.1" PARITY.md` → 4 |
| 4 | §11 still has 22 numbered incidental-gap bullets | VERIFIED | `grep -n "^22\." PARITY.md` → line 296 matches item 22 |
| 5 | CronJob carries 9 new fields with #[serde(default)] | VERIFIED | `grep -cE "^\s*pub (model|provider|...):" job.rs` → 9; all new fields present |
| 6 | Defensive JobOrigin deserializer: non-object origin yields None | VERIFIED | `grep -n "deserialize_origin_lenient" job.rs` → 2 lines (fn def + attribute); defensive pattern present |
| 7 | AgentLoop exposes activity_summary(), interrupt(), is_cancelled() | VERIFIED | All three found at lines 598, 615, 626 in agent_loop.rs |
| 8 | ActivityKind enum and ActivitySummary struct declared | VERIFIED | Lines 3102, 3117 in agent_loop.rs |
| 9 | mark_activity wired into run() at 3 event sites (ApiCall, ToolCall, StreamToken) | VERIFIED | 8 total self.mark_activity() calls; ApiCall×11, ToolCall×10, StreamToken×4 references |
| 10 | JobStore::save calls f.sync_all() after f.flush() | VERIFIED | store.rs lines 151, 211, 578 all show f.sync_all() |
| 11 | Unix chmod 0700 on cron dir in JobStore::open | VERIFIED | store.rs line 116: set_permissions from_mode(0o700) |
| 12 | Unix chmod 0600 on jobs.json in JobStore::save | VERIFIED | store.rs lines 157, 219, 592: from_mode(0o600) |
| 13 | Control-char auto-repair with tracing::warn! | VERIFIED | raw_has_bare_ctrl_chars() at line 617; strip_ctrl_chars() at line 622; warn triggered on repair |
| 14 | JobStore::trigger_job(id_or_name) exists | VERIFIED | store.rs line 474 |
| 15 | JobStore::jobs_mut() -> &mut Vec<CronJob> exists | VERIFIED | store.rs line 380 |
| 16 | compute_grace_seconds and compute_next_run_from called in get_due_jobs | VERIFIED | store.rs line 12 imports both; lines 411, 425 use them |
| 17 | ONESHOT_GRACE_SECONDS = 120 in parser.rs | VERIFIED | parser.rs line 16 |
| 18 | compute_grace_seconds in parser.rs | VERIFIED | parser.rs line 133 |
| 19 | compute_next_run_from in parser.rs | VERIFIED | parser.rs line 156 |
| 20 | compute_next_run (old wrapper) preserved | VERIFIED | parser.rs line 196 |
| 21 | Grace_seconds fixed field removed from JobStore | VERIFIED | `grep -n "grace_seconds.*3600" store.rs` → 0 matches |
| 22 | KNOWN_DELIVERY_PLATFORMS const in delivery.rs | VERIFIED | delivery.rs line 30 |
| 23 | resolve_delivery_targets (plural) in delivery.rs | VERIFIED | delivery.rs line 190 |
| 24 | resolve_delivery_target (singular, deprecated) preserved | VERIFIED | delivery.rs line 225 |
| 25 | QQBOT_HOME_CHANNEL legacy fallback | VERIFIED | delivery.rs lines 52, 607, 661 |
| 26 | save_job_output calls f.sync_all() | VERIFIED | delivery.rs line 276 |
| 27 | New crate ironhermes-cron-runner in workspace | VERIFIED | Cargo.toml lines 11, 37 (members + default-members); all 7 src files present |
| 28 | Three tokio::task_local! statics: CRON_AUTO_DELIVER_PLATFORM, CRON_AUTO_DELIVER_CHAT_ID, CRON_AUTO_DELIVER_THREAD_ID | VERIFIED | lib.rs lines 41-43 |
| 29 | run_job_script sandboxed under ${HERMES_HOME}/scripts/ via canonicalize + starts_with | VERIFIED | script_runner.rs lines 94, 116, 122, 136 — escapes scripts_dir bail! present |
| 30 | parse_wake_gate returns false only for {"wakeAgent": false} on last non-empty line | VERIFIED | script_runner.rs line 61 |
| 31 | IRONHERMES_CRON_SCRIPT_TIMEOUT env var enforced | VERIFIED | script_runner.rs lines 91, 140 |
| 32 | build_job_prompt 5-step assembly with UUID guard, 8000-char cap, post-assembly rescan | VERIFIED | prompt_builder.rs lines 175, 204; CONTEXT_FROM_MAX_BYTES=8000 at line 18; scan_cron_prompt call at line 204 |
| 33 | run_with_wall_clock (wall_secs=0 → unlimited) | VERIFIED | timeout.rs line 20; "wall-clock timeout" message at line 29 |
| 34 | run_with_inactivity_timeout (inactivity_secs=0 → unlimited) | VERIFIED | timeout.rs line 47; "inactivity timeout" message at line 71 |
| 35 | CronRunnerContext struct with tg_client: Option<Arc<dyn TgSendApi>> | VERIFIED | runner.rs lines 26-37; tg_client field present |
| 36 | run_cron_job handles no_agent, wake-gate, empty soft-fail, workdir TERMINAL_CWD, enabled_toolsets scoping, BLOCKED on rescan | VERIFIED | runner.rs lines 199, 223, 245-246, 69-74, 279-282, 351-354 all confirmed |
| 37 | run_tick_loop ticks every 60s, MissedTickBehavior::Skip, partitions workdir vs parallel, JoinSet | VERIFIED | tick_loop.rs lines 23-28, 41, 99-113 confirmed |
| 38 | prepare_mcp_for_tick no-op wrapper exposed | VERIFIED | tick_loop.rs line 70 |
| 39 | wrap_response in delivery.rs with header/footer | VERIFIED | delivery.rs lines 14, 23, 430, 455, 491 |
| 40 | dispatch_all_targets with MEDIA routing via classify_media_path + route_media_payload | VERIFIED | delivery.rs lines 76, 96, 185-188; Plan 07 TODO comment stale but routing implemented |
| 41 | TgSendApi trait relocated to ironhermes-cron::adapter (cycle prevention) | VERIFIED | adapter.rs line 16; gateway telegram.rs uses `pub use ironhermes_cron::TgSendApi` |
| 42 | Gateway execute_cron_job + dispatch_delivery DELETED | VERIFIED | `grep -n "fn execute_cron_job" gateway/runner.rs` → 0; regression test at line 1753 |
| 43 | Gateway tick task calls ironhermes_cron_runner::run_tick_loop | VERIFIED | gateway/runner.rs lines 775, 788 |
| 44 | CLI has Trigger + Daemon subcommands wired to run_cron_job + run_tick_loop | VERIFIED | cron.rs lines 94, 99, 131-132, 471, 526 |

**Score:** 44/44 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/ironhermes-cron/PARITY.md` | §10 scope shift + §11 Phase 32.1 | VERIFIED | 4 "Phase 32.1" references; 22 §11 bullets intact |
| `crates/ironhermes-cron/src/job.rs` | 9 new CronJob fields + defensive origin deserializer | VERIFIED | 9 fields confirmed; deserialize_origin_lenient on 2 lines |
| `crates/ironhermes-agent/src/agent_loop.rs` | ActivityKind, ActivitySummary, activity_summary(), interrupt(), mark_activity | VERIFIED | All present at confirmed line numbers |
| `crates/ironhermes-cron/src/store.rs` | fsync, chmod, trigger_job, jobs_mut, dynamic grace, due-recovery, control-char repair | VERIFIED | All 7 behaviors confirmed |
| `crates/ironhermes-cron/src/parser.rs` | ONESHOT_GRACE_SECONDS, compute_grace_seconds, compute_next_run_from | VERIFIED | All 3 present |
| `crates/ironhermes-cron/src/delivery.rs` | KNOWN_DELIVERY_PLATFORMS, resolve_delivery_targets, fsync/chmod in save_job_output | VERIFIED | All present |
| `crates/ironhermes-cron-runner/Cargo.toml` | New crate manifest | VERIFIED | In workspace members + default-members |
| `crates/ironhermes-cron-runner/src/lib.rs` | tokio::task_local! × 3, pub mod declarations, re-exports | VERIFIED | Lines 40-43, re-exports 19-21 |
| `crates/ironhermes-cron-runner/src/script_runner.rs` | Sandboxed run_job_script, parse_wake_gate | VERIFIED | All confirmed |
| `crates/ironhermes-cron-runner/src/prompt_builder.rs` | 5-step build_job_prompt, UUID guard, rescan | VERIFIED | All confirmed |
| `crates/ironhermes-cron-runner/src/timeout.rs` | run_with_wall_clock, run_with_inactivity_timeout | VERIFIED | All confirmed |
| `crates/ironhermes-cron-runner/src/runner.rs` | CronRunnerContext, run_cron_job | VERIFIED | All confirmed |
| `crates/ironhermes-cron-runner/src/delivery.rs` | dispatch_all_targets, wrap_response, MEDIA routing | VERIFIED | All confirmed; stale comment noted below |
| `crates/ironhermes-cron-runner/src/tick_loop.rs` | run_tick_loop, prepare_mcp_for_tick, workdir partition | VERIFIED | All confirmed |
| `crates/ironhermes-cron/src/adapter.rs` | pub trait TgSendApi (relocated from gateway) | VERIFIED | Line 16 |
| `crates/ironhermes-cli/src/cron.rs` | Trigger, Daemon subcommands + cmd_trigger, cmd_daemon | VERIFIED | Lines 94, 99, 489, 504, 523 |
| `crates/ironhermes-cli/Cargo.toml` | ironhermes-cron-runner dep | VERIFIED | Line 32 |
| `crates/ironhermes-gateway/src/runner.rs` | execute_cron_job deleted, run_tick_loop delegated | VERIFIED | Deletion confirmed; regression test at line 1753 |
| `crates/ironhermes-gateway/Cargo.toml` | ironhermes-cron-runner dep | VERIFIED | Line 17 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| job.rs::CronJob new fields | serde defaults | #[serde(default)] on 9 new fields | WIRED | grep returns 9 matches |
| job.rs::CronJob.origin | malformed jobs.json | deserialize_origin_lenient | WIRED | fn + attribute both confirmed |
| agent_loop.rs::AgentLoop | cron-runner timeout.rs | activity_summary() polling | WIRED | activity_summary() public; timeout.rs uses Fn() -> f64 |
| agent_loop.rs::interrupt | CancellationToken | cancellation_token.cancel() | WIRED | interrupt() calls cancel() |
| store.rs::get_due_jobs | parser.rs::compute_next_run_from | dynamic grace + recovery | WIRED | store.rs line 411 |
| store.rs::trigger_job | store.rs::save | after next_run_at mutation | WIRED | trigger_job calls self.save() |
| delivery.rs::resolve_delivery_targets | process env vars | allowlist-gated std::env::var | WIRED | allowlist check before any env read |
| cron-runner/delivery.rs::dispatch_all_targets | TgSendApi | send_message + route_media_payload | WIRED | lines 179-200 |
| cron-runner/runner.rs::run_cron_job | prompt_builder::build_job_prompt | 5-step assembly | WIRED | runner.rs line 243 |
| cron-runner/runner.rs::run_cron_job | timeout::run_with_inactivity_timeout | 5s polling + wall-clock wrap | WIRED | runner.rs lines 327, 340 |
| cron-runner/tick_loop.rs::run_tick_loop | runner.rs::run_cron_job | per-job dispatch | WIRED | tick_loop runs run_cron_job for each due job |
| CLI cron.rs::cmd_tick | cron_runner::run_cron_job | for each due job | WIRED | cron.rs line 471 |
| CLI cron.rs::cmd_daemon | cron_runner::run_tick_loop | via cancel token | WIRED | cron.rs line 526 |
| gateway/runner.rs tick task | cron_runner::run_tick_loop | Arc<CronRunnerContext> + cancel | WIRED | runner.rs line 788 |
| ironhermes-cron/adapter.rs::TgSendApi | gateway/telegram.rs impl | pub use + impl TgSendApi | WIRED | telegram.rs line 6: pub use ironhermes_cron::TgSendApi |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| runner.rs::run_cron_job | final_text | AgentLoop::run() | Yes — live LLM call | FLOWING |
| delivery.rs::dispatch_all_targets | targets | resolve_delivery_targets(job) | Yes — reads job.deliver + env vars | FLOWING |
| tick_loop.rs::run_tick_loop | due_jobs | ironhermes_cron::run_tick_check | Yes — reads JobStore from disk | FLOWING |
| prompt_builder.rs::build_job_prompt | assembled | 5 real content sources | Yes — skills/script/context_from/prompt | FLOWING |
| store.rs::get_due_jobs | Vec<CronJob> | serde_json from jobs.json | Yes — reads disk | FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED — verification of gateway/CLI execution paths requires a live agent environment (LLM credentials, HERMES_HOME). The provided context confirms all crate tests pass: ironhermes-cron 125/125, ironhermes-cron-runner 54/54, ironhermes-agent 285/285, ironhermes-gateway 70/70.

### Probe Execution

No probe-*.sh files declared for this phase.

### Requirements Coverage

The CRON-* requirement IDs are phase-local tags defined in plan frontmatter. They do not appear in the global `.planning/REQUIREMENTS.md` (which uses milestone-scoped IDs like MEM-*, SESS-*, etc.). All 43 CRON-* requirement IDs are covered by the 8 plan files with confirmed must_haves satisfied.

| Category | Requirement IDs | Status |
|----------|----------------|--------|
| Parity scope | CRON-PARITY-SCOPE | SATISFIED — PARITY.md §10/§11 updated |
| Job model | CRON-JOB-FIELDS, CRON-ORIGIN-DEFENSIVE | SATISFIED — 9 fields + lenient deserializer |
| Agent tracker | CRON-ACTIVITY-TRACKER, CRON-AGENT-INTERRUPT | SATISFIED — ActivitySummary + interrupt() |
| Store hardening | CRON-STORE-FSYNC, CRON-STORE-CHMOD, CRON-STORE-CTRLCHAR-REPAIR, CRON-STORE-TRIGGER-JOB, CRON-STORE-ONESHOT-GRACE, CRON-STORE-DYNAMIC-GRACE, CRON-STORE-DUE-RECOVERY, CRON-STORE-JOBS-MUT | SATISFIED — all confirmed in store.rs |
| Parser | CRON-PARSER-ANCHORING | SATISFIED — compute_next_run_from confirmed |
| Delivery routing | CRON-DELIVERY-MULTI-TARGET, CRON-DELIVERY-ALLOWLIST, CRON-DELIVERY-HOME-CHANNEL, CRON-DELIVERY-LEGACY-ENV, CRON-DELIVERY-THREAD-ENV, CRON-DELIVERY-ORIGIN-FALLBACK, CRON-DELIVERY-FSYNC-CHMOD | SATISFIED — all confirmed in delivery.rs |
| Runner crate | CRON-RUNNER-CRATE, CRON-RUNNER-TASKLOCALS, CRON-RUNNER-SCRIPT-SANDBOX, CRON-RUNNER-WAKE-GATE, CRON-RUNNER-PROMPT-ASSEMBLY, CRON-RUNNER-PROMPT-RESCAN, CRON-RUNNER-INACTIVITY-TIMEOUT, CRON-RUNNER-CONTEXT, CRON-RUNNER-RUN-JOB, CRON-RUNNER-NO-AGENT, CRON-RUNNER-WAKE-SKIP, CRON-RUNNER-EMPTY-SOFT-FAIL, CRON-RUNNER-MULTI-DELIVERY, CRON-RUNNER-WRAP-RESPONSE, CRON-RUNNER-MEDIA-EXTRACTION, CRON-RUNNER-LAST-DELIVERY-ERROR, CRON-RUNNER-TICK-LOOP, CRON-RUNNER-WORKDIR-PARTITION | SATISFIED — all confirmed |
| CLI | CRON-CLI-TRIGGER, CRON-CLI-TICK-ONCE, CRON-CLI-DAEMON | SATISFIED — Trigger+Daemon enum variants, cmd_trigger, cmd_daemon confirmed |
| Gateway | CRON-GATEWAY-MIGRATION, CRON-GATEWAY-CALL-NEW-CRATE | SATISFIED — execute_cron_job deleted, run_tick_loop wired |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `crates/ironhermes-cron-runner/src/delivery.rs` | 163-165 | Stale `TODO Plan 07: route by extension via send_voice/send_image_file/send_video` comment — the routing is implemented at lines 185-188 but the TODO comment was not removed | Warning | No behavioral impact — code is correct; comment is misleading |
| `crates/ironhermes-cron-runner/src/tick_loop.rs` | 138 | `TODO Plan 32.x: MCP orphan reaper after tick` | Info | Known deferred item per CONTEXT.md; no formal issue reference but the deferred nature is documented in CONTEXT.md |
| `crates/ironhermes-cron-runner/src/script_runner.rs` | ~380 | `redact_passthrough` — TODO comment for future workspace secret-redaction wiring | Info | Documented deferral per RESEARCH.md Open Question 1; pass-through is the explicit design choice for this phase |

No `TBD`, `FIXME`, or `XXX` markers found in any phase-modified file.

### Human Verification Required

#### 1. Live Tick Execution

**Test:** Run `hermes cron tick` against a real HERMES_HOME with at least one due job configured (e.g. `deliver=local`, `prompt="say hello"`, schedule set to fire now).
**Expected:** Tick acquires lock, runs the agent, writes output to `~/.hermes/cron/output/<id>/`, updates `last_status=ok`, prints "Tick complete."
**Why human:** Requires live LLM credentials + HERMES_HOME on disk.

#### 2. Daemon Lifecycle

**Test:** Run `hermes cron daemon` in a terminal. Observe it starts, ticks every ~60s, then press Ctrl-C.
**Expected:** Ticks fire at 60s intervals; Ctrl-C triggers clean shutdown printing "Cron daemon stopped."
**Why human:** Long-running interactive process; cannot be verified statically.

#### 3. Trigger + Immediate Tick Integration

**Test:** `hermes cron trigger <name>`, then immediately `hermes cron tick`. Confirm the triggered job ran.
**Expected:** `next_run_at` set to now by trigger; tick picks it up within the 120s grace window.
**Why human:** Requires real job store on disk + timing coordination.

#### 4. Malformed Origin Loading (tracing::warn! observation)

**Test:** Manually inject `"origin": "telegram"` (string) into a record in `~/.hermes/cron/jobs.json`, restart the gateway or CLI, confirm the job loads with `origin=None` and a warn! log appears.
**Expected:** No crash; warn! log containing "origin" visible in tracing output.
**Why human:** Requires tracing subscriber at runtime to observe warn! output.

#### 5. Live Telegram Delivery

**Test:** Create a cron job with `deliver=telegram` (using `TELEGRAM_HOME_CHANNEL`), fire it via `hermes cron tick`.
**Expected:** Message appears in the configured Telegram channel with Cronjob Response header (when wrap_response=true).
**Why human:** Requires live Telegram bot credentials and network.

### Gaps Summary

No gaps. All 44 must-have truths are VERIFIED against the actual codebase. The stale `TODO Plan 07` comment in `delivery.rs` line 163-165 is a WARNING (misleading comment, behavior correct). The two Info-level TODO items in tick_loop.rs and script_runner.rs are documented deferred items within scope of the phase design.

The only reason status is `human_needed` rather than `passed` is the 5 end-to-end behavioral items above that require a live environment.

---

_Verified: 2026-05-15_
_Verifier: Claude (gsd-verifier)_
