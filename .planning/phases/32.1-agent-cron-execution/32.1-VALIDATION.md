---
phase: 32.1
slug: agent-cron-execution
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-15
---

# Phase 32.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in `#[test]` + `#[tokio::test]`, `tempfile` for FS isolation |
| **Config file** | `Cargo.toml` per crate (`[dev-dependencies]`) |
| **Quick run command** | `cargo test -p ironhermes-cron && cargo test -p ironhermes-cron-runner` |
| **Full suite command** | `cargo test --workspace` |
| **Estimated runtime** | ~60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ironhermes-cron && cargo test -p ironhermes-cron-runner`
- **After every plan wave:** Run `cargo test --workspace`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 32.1-01 | 01 | 1 | CRON-01 | — | `no_agent` short-circuit: script stdout delivered verbatim | unit | `cargo test -p ironhermes-cron-runner script_runner` | ❌ Wave 0 | ⬜ pending |
| 32.1-02 | 01 | 1 | CRON-02 | — | Wake-gate `{"wakeAgent": false}` → silent, no agent run | unit | `cargo test -p ironhermes-cron-runner wake_gate` | ❌ Wave 0 | ⬜ pending |
| 32.1-03 | 01 | 1 | CRON-08 | T-path-traversal | Script path traversal guard rejects `../` and absolute paths | unit | `cargo test -p ironhermes-cron-runner script_sandbox` | ❌ Wave 0 | ⬜ pending |
| 32.1-04 | 02 | 1 | CRON-04 | T-env-enum | Multi-target delivery: comma-split, `all`, platform-without-colon | unit | `cargo test -p ironhermes-cron resolve_delivery_targets` | ❌ Wave 0 | ⬜ pending |
| 32.1-05 | 02 | 1 | CRON-14 | — | `last_delivery_error` accumulates per-target failures | unit | `cargo test -p ironhermes-cron-runner last_delivery_error` | ❌ Wave 0 | ⬜ pending |
| 32.1-06 | 03 | 2 | CRON-05 | — | `context_from` chain injection with 8K truncation | unit | `cargo test -p ironhermes-cron-runner build_job_prompt` | ❌ Wave 0 | ⬜ pending |
| 32.1-07 | 03 | 2 | CRON-06 | T-prompt-injection | Assembled-prompt threat rescan blocks injection | unit | `cargo test -p ironhermes-cron-runner prompt_injection` | ❌ Wave 0 | ⬜ pending |
| 32.1-08 | 03 | 2 | CRON-07 | — | `wrap_response` header/footer added when `config.cron.wrap_response = true` | unit | `cargo test -p ironhermes-cron-runner wrap_response` | ❌ Wave 0 | ⬜ pending |
| 32.1-09 | 04 | 2 | CRON-03 | — | Empty `final_response` → soft failure | unit | `cargo test -p ironhermes-cron-runner empty_response` | ❌ Wave 0 | ⬜ pending |
| 32.1-10 | 04 | 2 | CRON-09 | — | Dynamic grace: `compute_grace_seconds` clamps to `[120s, 7200s]` | unit | `cargo test -p ironhermes-cron grace_seconds` | ❌ Wave 0 | ⬜ pending |
| 32.1-11 | 04 | 2 | CRON-10 | — | `trigger_job(id)` sets `next_run_at = now`, fires on next tick | unit | `cargo test -p ironhermes-cron trigger_job` | ❌ Wave 0 | ⬜ pending |
| 32.1-12 | 04 | 2 | CRON-11 | — | `fsync` called after flush in `save_job_output` | static grep | `grep -n "sync_all" crates/ironhermes-cron/src/delivery.rs` | ❌ Wave 0 | ⬜ pending |
| 32.1-13 | 04 | 2 | CRON-12 | — | Unix chmod 0600/0700 on cron dir and output files | unit (cfg unix) | `cargo test -p ironhermes-cron chmod` | ❌ Wave 0 | ⬜ pending |
| 32.1-14 | 05 | 3 | CRON-13 | — | `hermes cron tick` exits after one tick cycle | integration | `cargo test -p ironhermes-cli cron_tick_exits` | ❌ Wave 0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/ironhermes-cron-runner/Cargo.toml` — new crate manifest
- [ ] `crates/ironhermes-cron-runner/src/lib.rs` — new crate root with test module stubs
- [ ] Test module stubs in each `ironhermes-cron-runner` source file (`runner`, `script_runner`, `prompt_builder`, `delivery`)
- [ ] Updated tests in `crates/ironhermes-cron/src/delivery.rs` covering `resolve_delivery_targets` (plural)
- [ ] Updated tests in `crates/ironhermes-cron/src/store.rs` covering `trigger_job`, dynamic grace, `compute_next_run` anchoring

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Inactivity timeout fires and interrupts agent | CRON (timeout) | Requires real AgentLoop run hanging > 600s | Start a job with a long-running tool; wait for interrupt log line in output |
| Wall-clock ceiling catches runaway job | CRON (wall-clock) | Requires 4h clock or mock | Set `IRONHERMES_CRON_WALL_TIMEOUT_SECS=5`; run agent that never completes |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
