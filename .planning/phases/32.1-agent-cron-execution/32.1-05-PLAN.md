---
phase: 32.1-agent-cron-execution
plan: 05
type: execute
wave: 3
depends_on: [32.1-01, 32.1-02, 32.1-03, 32.1-04]
files_modified:
  - Cargo.toml
  - crates/ironhermes-cron-runner/Cargo.toml
  - crates/ironhermes-cron-runner/src/lib.rs
  - crates/ironhermes-cron-runner/src/script_runner.rs
  - crates/ironhermes-cron-runner/src/prompt_builder.rs
  - crates/ironhermes-cron-runner/src/timeout.rs
autonomous: true
requirements:
  - CRON-RUNNER-CRATE
  - CRON-RUNNER-TASKLOCALS
  - CRON-RUNNER-SCRIPT-SANDBOX
  - CRON-RUNNER-WAKE-GATE
  - CRON-RUNNER-PROMPT-ASSEMBLY
  - CRON-RUNNER-PROMPT-RESCAN
  - CRON-RUNNER-INACTIVITY-TIMEOUT
tags: [cron, new-crate, script-runner, prompt-assembly, timeout]

must_haves:
  truths:
    - "Workspace gains a new member crate `ironhermes-cron-runner` that builds with `cargo build -p ironhermes-cron-runner`"
    - "The crate root `lib.rs` declares three `tokio::task_local!` statics: `CRON_AUTO_DELIVER_PLATFORM`, `CRON_AUTO_DELIVER_CHAT_ID`, `CRON_AUTO_DELIVER_THREAD_ID`, each `pub static ... : String`"
    - "`script_runner::run_job_script(script_name) -> Result<ScriptOutcome>` resolves the script under `${HERMES_HOME}/scripts/` via `canonicalize() + starts_with(scripts_dir)` and refuses any path that escapes (returns `Err` with the literal substring `escapes scripts_dir`)"
    - "`script_runner::run_job_script` selects interpreter by extension: `.sh`/`.bash` → bash, anything else → python3 — picked up from `BASH_PATH` / `PYTHON_PATH` env overrides when set, else hardcoded fallbacks `/bin/bash` and `python3`"
    - "`script_runner::run_job_script` enforces per-script timeout `IRONHERMES_CRON_SCRIPT_TIMEOUT` (default 120s) via `tokio::time::timeout`; timeout returns `ScriptOutcome { ok: false, ... }` rather than an `Err`"
    - "`script_runner::parse_wake_gate(stdout: &str) -> bool` returns `false` only when the LAST non-empty line of stdout parses as JSON and contains `{\"wakeAgent\": false}` (or `false`-equivalent boolean); every other shape returns `true` (default-wake)"
    - "`prompt_builder::build_job_prompt(job, script_output, skill_registry, hermes_home)` assembles in the locked five-step order: (1) cron-hint banner, (2) skill content with skip-missing prefix, (3) `## Script Output` block, (4) `## Output from job '...'` blocks for `context_from` entries truncated at 8000 chars, (5) the user prompt"
    - "`prompt_builder::build_job_prompt` validates each `context_from` job id as a UUID before any filesystem read; non-UUID ids are silently skipped with a `tracing::warn!`"
    - "`prompt_builder::scan_assembled(assembled)` calls `ironhermes_cron::scan_cron_prompt(assembled)` and returns the scanner's verdict; the rescan runs AFTER the five-step assembly, not before"
    - "`timeout::run_with_inactivity_timeout(future, activity_summary_fn, inactivity_secs) -> Result<T>` returns `Err(...)` with the literal substring `inactivity timeout` when `activity_summary_fn()` reports `seconds_since > inactivity_secs` for 5 consecutive seconds; `inactivity_secs == 0` disables the inactivity gate"
    - "`timeout::run_with_wall_clock(future, wall_secs) -> Result<T>` wraps a future in `tokio::time::timeout` returning `Err` with the substring `wall-clock timeout` after the deadline; `wall_secs == 0` disables the wall gate"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace `members` + `default-members` include `crates/ironhermes-cron-runner`"
      contains: "ironhermes-cron-runner"
    - path: "crates/ironhermes-cron-runner/Cargo.toml"
      provides: "New crate manifest"
      contains: "ironhermes-cron-runner"
    - path: "crates/ironhermes-cron-runner/src/lib.rs"
      provides: "Crate root, module declarations, task_local! statics"
      contains: "tokio::task_local!"
    - path: "crates/ironhermes-cron-runner/src/script_runner.rs"
      provides: "Sandboxed script execution + wake-gate"
      contains: "pub async fn run_job_script"
    - path: "crates/ironhermes-cron-runner/src/prompt_builder.rs"
      provides: "Five-step prompt assembly + rescan"
      contains: "pub async fn build_job_prompt"
    - path: "crates/ironhermes-cron-runner/src/timeout.rs"
      provides: "Inactivity + wall-clock timeout primitives"
      contains: "pub async fn run_with_inactivity_timeout"
  key_links:
    - from: "crates/ironhermes-cron-runner/src/script_runner.rs"
      to: "filesystem sandbox under ${HERMES_HOME}/scripts/"
      via: "canonicalize() + starts_with(scripts_dir)"
      pattern: "starts_with\\(.*scripts_dir"
    - from: "crates/ironhermes-cron-runner/src/prompt_builder.rs"
      to: "crates/ironhermes-cron/src/scanner.rs::scan_cron_prompt"
      via: "post-assembly rescan call"
      pattern: "scan_cron_prompt"
    - from: "crates/ironhermes-cron-runner/src/timeout.rs"
      to: "crates/ironhermes-agent/src/agent_loop.rs::activity_summary"
      via: "5s polling loop reads seconds_since"
      pattern: "seconds_since"
---

<objective>
Create the new `ironhermes-cron-runner` workspace crate skeleton plus its
three leaf modules — `script_runner.rs`, `prompt_builder.rs`, `timeout.rs`
— and the crate root declarations (modules + tokio task-locals). These
are the dependency-free building blocks the orchestration modules in
Plan 06 (`runner.rs`, `delivery.rs`, `tick_loop.rs`) call into.

Purpose: separating the leaf modules into their own plan caps Plan 06's
scope to orchestration code only. The leaves are also independently
testable — script execution, prompt assembly, and timeout primitives all
have clean inputs and outputs that don't require an in-flight `AgentLoop`.

Output: a new workspace member crate that builds and tests on its own,
exposing five public entry points the orchestration layer (Plan 06) will
call. No `run_cron_job` yet — that's Plan 06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md
@.planning/phases/32.1-agent-cron-execution/32.1-RESEARCH.md
@.planning/phases/32.1-agent-cron-execution/32.1-PATTERNS.md
@.planning/phases/32.1-agent-cron-execution/32.1-01-SUMMARY.md
@.planning/phases/32.1-agent-cron-execution/32.1-02-SUMMARY.md
@.planning/phases/32.1-agent-cron-execution/32.1-03-SUMMARY.md
@.planning/phases/32.1-agent-cron-execution/32.1-04-SUMMARY.md
@Cargo.toml
@crates/ironhermes-cron/Cargo.toml
@crates/ironhermes-cron/src/scanner.rs
@crates/ironhermes-tools/src/terminal.rs

<interfaces>
<!-- Public API this plan creates -->

pub mod script_runner;
pub mod prompt_builder;
pub mod timeout;

tokio::task_local! {
    pub static CRON_AUTO_DELIVER_PLATFORM: String;
    pub static CRON_AUTO_DELIVER_CHAT_ID: String;
    pub static CRON_AUTO_DELIVER_THREAD_ID: String;
}

// script_runner.rs
pub struct ScriptOutcome { pub ok: bool, pub stdout: String, pub stderr: String, pub wake_agent: bool }
pub async fn run_job_script(script_name: &str) -> anyhow::Result<ScriptOutcome>;
pub fn parse_wake_gate(stdout: &str) -> bool;

// prompt_builder.rs
pub struct AssembledPrompt { pub system_addendum: String, pub user_prompt: String, pub blocked_reason: Option<String> }
pub async fn build_job_prompt(
    job: &ironhermes_cron::CronJob,
    script_output: Option<&str>,
    skill_registry: Option<&ironhermes_core::SkillRegistry>,
) -> anyhow::Result<AssembledPrompt>;

// timeout.rs
pub async fn run_with_inactivity_timeout<F, T>(
    fut: F,
    activity_summary_fn: impl Fn() -> f64 + Send + 'static,
    inactivity_secs: u64,
) -> anyhow::Result<T>
where F: std::future::Future<Output = anyhow::Result<T>> + Send;

pub async fn run_with_wall_clock<F, T>(
    fut: F,
    wall_secs: u64,
) -> anyhow::Result<T>
where F: std::future::Future<Output = anyhow::Result<T>>;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the new crate — Cargo.toml + lib.rs (module decls + task_locals) + workspace registration</name>
  <files>Cargo.toml, crates/ironhermes-cron-runner/Cargo.toml, crates/ironhermes-cron-runner/src/lib.rs</files>
  <read_first>
    - Cargo.toml (root — find the `members = [...]` and `default-members = [...]` arrays; verify the existing `crates/ironhermes-cron` entry as the insertion-order analog)
    - crates/ironhermes-cron/Cargo.toml (analog manifest — the new crate copies the `[package]` block style verbatim with `version.workspace = true` etc.)
    - .planning/phases/32.1-agent-cron-execution/32.1-PATTERNS.md (§Cargo.toml lines 35–66 — locked manifest template)
    - .planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md (§Crate layout — locks the dependency list)
  </read_first>
  <action>
    (a) **Create** `crates/ironhermes-cron-runner/Cargo.toml`:

        [package]
        name = "ironhermes-cron-runner"
        version.workspace = true
        edition.workspace = true
        authors.workspace = true
        license.workspace = true
        description = "Agent execution runner for IronHermes cron jobs"

        [dependencies]
        ironhermes-core   = { path = "../ironhermes-core" }
        ironhermes-agent  = { path = "../ironhermes-agent" }
        ironhermes-tools  = { path = "../ironhermes-tools" }
        ironhermes-hooks  = { path = "../ironhermes-hooks" }
        ironhermes-cron   = { path = "../ironhermes-cron" }
        ironhermes-mcp    = { path = "../ironhermes-mcp" }
        serde       = { workspace = true }
        serde_json  = { workspace = true }
        anyhow      = { workspace = true }
        tokio       = { workspace = true }
        tokio-util  = { workspace = true }
        tracing     = { workspace = true }
        uuid        = { workspace = true }
        chrono      = { workspace = true }

        [dev-dependencies]
        tempfile = "3"

    NOTE: per 32.1-PATTERNS.md the `which` crate is NOT in the workspace.
    This plan uses env-var fallback (`BASH_PATH`, `PYTHON_PATH`) + hardcoded
    paths. Do NOT add `which` as a dependency.

    NOTE: if `tempfile` is already a workspace dev-dep, prefer
    `tempfile = { workspace = true }` for version pinning consistency.
    Inspect root `Cargo.toml` `[workspace.dependencies]` block first.

    (b) **Register** the new crate in the root `/Users/twilson/code/ironhermes/Cargo.toml`:
    locate the `members = [...]` array and `default-members = [...]` array and
    add `"crates/ironhermes-cron-runner"` to BOTH, preserving alphabetical
    order. Verify by `grep -n "ironhermes-cron-runner" Cargo.toml` returning
    exactly 2 lines.

    (c) **Create** `crates/ironhermes-cron-runner/src/lib.rs`:

        //! Agent execution runner for IronHermes cron jobs.
        //!
        //! This crate owns end-to-end execution of a due `CronJob`: prompt
        //! assembly, sandboxed script execution, agent loop invocation,
        //! timeout enforcement, and per-target delivery dispatch.
        //!
        //! Plan 32.1-05 lands the crate skeleton + leaf modules (script
        //! sandbox, prompt assembly, timeout primitives). Plan 32.1-06
        //! lands the orchestration modules (`runner`, `delivery`,
        //! `tick_loop`) and the `run_cron_job` public entry point.

        pub mod prompt_builder;
        pub mod script_runner;
        pub mod timeout;

        // Per-job delivery context. Propagated to the agent's tool layer
        // (e.g. `send_message`) without env-var mutation. Survives `.await`
        // within the same tokio task; explicit `scope()` wrapping is required
        // for any `tokio::spawn` children.
        tokio::task_local! {
            pub static CRON_AUTO_DELIVER_PLATFORM: String;
            pub static CRON_AUTO_DELIVER_CHAT_ID: String;
            pub static CRON_AUTO_DELIVER_THREAD_ID: String;
        }

    (d) Create empty stub files so `cargo build` succeeds at the end of this
    task:

        crates/ironhermes-cron-runner/src/script_runner.rs:
            //! Sandboxed script execution + wake-gate parsing.
            //! Implemented in Task 2 of plan 32.1-05.

        crates/ironhermes-cron-runner/src/prompt_builder.rs:
            //! Five-step prompt assembly + assembled-prompt rescan.
            //! Implemented in Task 3 of plan 32.1-05.

        crates/ironhermes-cron-runner/src/timeout.rs:
            //! Inactivity + wall-clock timeout primitives.
            //! Implemented in Task 4 of plan 32.1-05.

    (e) Verify the crate compiles:
        `cargo build -p ironhermes-cron-runner`

    Should succeed with zero warnings (no use-statements yet means nothing
    to flag). Three empty modules + three task-local statics are
    syntactically complete on their own.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-cron-runner && grep -c "ironhermes-cron-runner" Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `cargo build -p ironhermes-cron-runner` exits 0
    - source assertion: `grep -c "ironhermes-cron-runner" Cargo.toml` returns at least 2 (members + default-members)
    - source assertion: `ls crates/ironhermes-cron-runner/Cargo.toml` is a regular file
    - source assertion: `ls crates/ironhermes-cron-runner/src/lib.rs crates/ironhermes-cron-runner/src/script_runner.rs crates/ironhermes-cron-runner/src/prompt_builder.rs crates/ironhermes-cron-runner/src/timeout.rs` all four are regular files
    - source assertion: `grep -n "tokio::task_local!" crates/ironhermes-cron-runner/src/lib.rs` matches exactly 1 line
    - source assertion: `grep -nE "pub static (CRON_AUTO_DELIVER_PLATFORM|CRON_AUTO_DELIVER_CHAT_ID|CRON_AUTO_DELIVER_THREAD_ID)" crates/ironhermes-cron-runner/src/lib.rs` returns 3 lines
    - source assertion: `grep -nE "pub mod (script_runner|prompt_builder|timeout)" crates/ironhermes-cron-runner/src/lib.rs` returns 3 lines
    - test-command: `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>The new workspace crate exists, builds standalone and as part of the workspace, declares its three modules, and exports the three cron-delivery task-locals from its root.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: script_runner.rs — sandboxed script execution + wake-gate parsing</name>
  <files>crates/ironhermes-cron-runner/src/script_runner.rs</files>
  <read_first>
    - crates/ironhermes-cron-runner/src/script_runner.rs (the stub from Task 1)
    - crates/ironhermes-tools/src/terminal.rs lines 1–55, 196–220 (subprocess + tokio::time::timeout pattern, `Command::new + stdout/stderr piped + wait_with_output`)
    - .planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md (§Script execution — locks the sandbox, interpreter selection, timeout env var, secret redaction, `no_agent` short-circuit, wake-gate JSON parsing)
    - .planning/phases/32.1-agent-cron-execution/32.1-RESEARCH.md (Code Examples §Script sandbox; Common Pitfalls Pitfall — `which` crate not in workspace; Open Question 1 — secret redactor location)
    - .planning/phases/32.1-agent-cron-execution/32.1-PATTERNS.md (§script_runner.rs lines 234–318)
    - hermes-agent/cron/scheduler.py (`_run_job_script` and `_parse_wake_gate` for parity reference)
  </read_first>
  <behavior>
    - Test 1 (happy path bash): With `HERMES_HOME` set to a tempdir, write a `.sh` script in `<HERMES_HOME>/scripts/` that prints `hello\n`. Call `run_job_script("test.sh").await`. Returns `Ok(ScriptOutcome { ok: true, stdout: "hello\n", stderr: "", wake_agent: true })`.
    - Test 2 (happy path python): With a `.py` script that prints `world\n`. Returns `ok: true, stdout == "world\n"`.
    - Test 3 (path traversal rejection): Call `run_job_script("../etc/passwd")`. Returns `Err` whose message contains `escapes scripts_dir`. No subprocess is spawned.
    - Test 4 (absolute path rejection): Call `run_job_script("/etc/passwd")`. Same as Test 3.
    - Test 5 (non-existent script): Call `run_job_script("nope.sh")` on an empty scripts dir. Returns `Err` whose message indicates the script was not found / unreadable (the canonicalize step naturally produces this — surface it via `with_context`).
    - Test 6 (non-zero exit): Script `exit 1` returns `Ok(ScriptOutcome { ok: false, stdout: "", stderr: ..., wake_agent: true })`.
    - Test 7 (script timeout): Set `IRONHERMES_CRON_SCRIPT_TIMEOUT=1` and run a script that sleeps 5s. Returns `Ok(ScriptOutcome { ok: false, ... })` with `stderr` containing the literal substring `script timed out`. Total elapsed wall-clock time is bounded under 3s (proves timeout fired).
    - Test 8 (wake gate true, default): `parse_wake_gate("any non-json line\nanother line\n")` returns `true`.
    - Test 9 (wake gate true, valid JSON without wakeAgent): `parse_wake_gate(r#"{"foo":"bar"}"#)` returns `true`.
    - Test 10 (wake gate false): `parse_wake_gate(r#"some preamble\n{"wakeAgent": false}\n"#)` returns `false`.
    - Test 11 (wake gate true when wakeAgent=true): `parse_wake_gate(r#"{"wakeAgent": true}"#)` returns `true`.
    - Test 12 (wake gate: trailing blank lines ignored): `parse_wake_gate("{\"wakeAgent\":false}\n\n\n")` returns `false` (the helper finds the LAST non-empty line).
    - Test 13 (scripts dir auto-create + chmod 0700 Unix): The first call to `run_job_script` after pointing `HERMES_HOME` at a fresh tempdir creates `<HERMES_HOME>/scripts/` with mode `0o700` on Unix. Gated `#[cfg(unix)]`.
    - Test 14 (BASH_PATH override): Set `BASH_PATH=/bin/bash` (or just confirm fallback path used when env unset). Script runs.
    - Test 15 (ScriptOutcome.wake_agent populated): For a script whose LAST line is `{"wakeAgent": false}`, `run_job_script` sets `outcome.wake_agent = false` automatically (parse_wake_gate is called inside).
  </behavior>
  <action>
    Replace the stub `script_runner.rs` with the full implementation:

    (a) Imports:

        use anyhow::{anyhow, bail, Context, Result};
        use std::path::Path;
        use std::process::Stdio;
        use std::time::Duration;
        use tokio::process::Command;
        use tokio::time::timeout;
        use ironhermes_core::get_hermes_home;

    (b) Public types:

        #[derive(Debug, Clone)]
        pub struct ScriptOutcome {
            pub ok: bool,
            pub stdout: String,
            pub stderr: String,
            pub wake_agent: bool,
        }

    (c) Helper `select_interpreter`:

        fn select_interpreter(script_path: &Path) -> String {
            match script_path.extension().and_then(|e| e.to_str()) {
                Some("sh") | Some("bash") => {
                    std::env::var("BASH_PATH").unwrap_or_else(|_| "/bin/bash".to_string())
                }
                _ => {
                    std::env::var("PYTHON_PATH").unwrap_or_else(|_| "python3".to_string())
                }
            }
        }

    (d) Helper `parse_env_u64`:

        fn parse_env_u64(name: &str, default: u64) -> u64 {
            std::env::var(name).ok().and_then(|s| s.parse().ok()).unwrap_or(default)
        }

    (e) Public `parse_wake_gate`:

        pub fn parse_wake_gate(stdout: &str) -> bool {
            let last_non_empty = stdout
                .lines()
                .rev()
                .find(|l| !l.trim().is_empty())
                .unwrap_or("");
            if let Ok(v) = serde_json::from_str::<serde_json::Value>(last_non_empty) {
                if matches!(v.get("wakeAgent").and_then(|v| v.as_bool()), Some(false)) {
                    return false;
                }
            }
            true
        }

    (f) Public `run_job_script`:

        pub async fn run_job_script(script_name: &str) -> Result<ScriptOutcome> {
            let scripts_dir = get_hermes_home().join("scripts");
            tokio::fs::create_dir_all(&scripts_dir).await
                .with_context(|| format!("create scripts dir: {:?}", scripts_dir))?;
            #[cfg(unix)] {
                use std::os::unix::fs::PermissionsExt;
                let _ = std::fs::set_permissions(
                    &scripts_dir,
                    std::fs::Permissions::from_mode(0o700)
                );
            }

            let candidate = scripts_dir.join(script_name);
            let canonical = candidate.canonicalize()
                .with_context(|| format!("script not found or unreadable: {:?}", candidate))?;
            let scripts_dir_canonical = scripts_dir.canonicalize()
                .with_context(|| format!("canonicalize scripts dir: {:?}", scripts_dir))?;
            if !canonical.starts_with(&scripts_dir_canonical) {
                bail!("script path escapes scripts_dir: {:?}", canonical);
            }

            let interpreter = select_interpreter(&canonical);
            let timeout_secs = parse_env_u64("IRONHERMES_CRON_SCRIPT_TIMEOUT", 120);

            let child = Command::new(&interpreter)
                .arg(&canonical)
                .stdout(Stdio::piped())
                .stderr(Stdio::piped())
                .kill_on_drop(true)
                .spawn()
                .with_context(|| format!("spawn script {:?} with {}", canonical, interpreter))?;

            let outcome = match timeout(Duration::from_secs(timeout_secs), child.wait_with_output()).await {
                Ok(Ok(out)) => {
                    let stdout = redact_passthrough(String::from_utf8_lossy(&out.stdout).into_owned());
                    let stderr = redact_passthrough(String::from_utf8_lossy(&out.stderr).into_owned());
                    let wake_agent = parse_wake_gate(&stdout);
                    ScriptOutcome { ok: out.status.success(), stdout, stderr, wake_agent }
                }
                Ok(Err(e)) => return Err(anyhow!("script subprocess failure: {}", e)),
                Err(_) => ScriptOutcome {
                    ok: false,
                    stdout: String::new(),
                    stderr: format!("script timed out after {}s", timeout_secs),
                    wake_agent: true,
                },
            };
            Ok(outcome)
        }

    (g) `redact_passthrough` — local function placeholder since no Rust
    `redact_sensitive_text` was found in the workspace (per RESEARCH.md
    Open Question 1):

        // TODO(Phase 32.x): wire to a workspace secret-redaction primitive once
        // one exists. For now, pass-through preserves the API shape so future
        // callers don't need to change.
        fn redact_passthrough(s: String) -> String { s }

    Capture this as a deferred item in the SUMMARY — Plan 06's runner.rs
    and Plan 07's gateway migration must NOT block on this.

    (h) Tests: `#[cfg(test)] mod tests` covering Tests 1–15. Use
    `tempfile::TempDir` for `HERMES_HOME` isolation. Acquire a single
    `OnceLock<Mutex<()>>` env lock for the env-mutating cases. Some tests
    (path traversal, wake gate parse) need no env mutation.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cron-runner --lib script_runner -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `cargo test -p ironhermes-cron-runner --lib script_runner -- --test-threads=1` exits 0
    - source assertion: `grep -n "pub async fn run_job_script" crates/ironhermes-cron-runner/src/script_runner.rs` matches exactly 1 line
    - source assertion: `grep -n "pub fn parse_wake_gate" crates/ironhermes-cron-runner/src/script_runner.rs` matches exactly 1 line
    - source assertion: `awk '/pub async fn run_job_script/,/^}/' crates/ironhermes-cron-runner/src/script_runner.rs | grep -c "starts_with"` returns at least 1 (path-traversal guard call)
    - source assertion: `awk '/pub async fn run_job_script/,/^}/' crates/ironhermes-cron-runner/src/script_runner.rs | grep -c "canonicalize"` returns at least 2 (candidate + dir)
    - source assertion: `grep -n "escapes scripts_dir" crates/ironhermes-cron-runner/src/script_runner.rs` matches at least 1 line (the bail! message)
    - source assertion: `grep -n "IRONHERMES_CRON_SCRIPT_TIMEOUT" crates/ironhermes-cron-runner/src/script_runner.rs` matches at least 1 line
    - source assertion: `grep -nE "kill_on_drop\\(true\\)" crates/ironhermes-cron-runner/src/script_runner.rs` matches at least 1 line (timeout cleanup)
    - source assertion: `grep -n "from_mode(0o700)" crates/ironhermes-cron-runner/src/script_runner.rs` matches at least 1 line (scripts dir hardening)
    - test-command: `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>`script_runner` enforces the scripts-dir sandbox, selects the right interpreter, respects `IRONHERMES_CRON_SCRIPT_TIMEOUT`, and `parse_wake_gate` correctly identifies `{"wakeAgent": false}` on the last non-empty stdout line.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: prompt_builder.rs — five-step assembly + UUID context_from guard + assembled-prompt rescan</name>
  <files>crates/ironhermes-cron-runner/src/prompt_builder.rs</files>
  <read_first>
    - crates/ironhermes-cron-runner/src/prompt_builder.rs (the stub from Task 1)
    - .planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md (§Prompt assembly — locks the five-step order, the 8000-char context_from cap, and the assembled-prompt rescan placement)
    - .planning/phases/32.1-agent-cron-execution/32.1-PATTERNS.md (§prompt_builder.rs lines 322–404 — full assembly + skip-missing-skill + threat rescan)
    - crates/ironhermes-gateway/src/runner.rs lines 909–925 (existing `resolve_skill_context` — the skill-loading pattern this module mirrors)
    - crates/ironhermes-cron/src/scanner.rs (`scan_cron_prompt` signature — verify it accepts `&str` and returns `Result<(), ScanReason>` or similar)
    - crates/ironhermes-cron/src/lib.rs (confirm `pub use scanner::scan_cron_prompt` is exported)
    - hermes-agent/cron/scheduler.py `_build_job_prompt` for parity reference (cron-hint banner verbatim text from lines 921–932)
  </read_first>
  <behavior>
    - Test 1 (banner present, single-line user prompt): A `CronJob` with `prompt = "summarize the day"`, no skills, no script, no context_from. `build_job_prompt(&job, None, None).await` returns an `AssembledPrompt` whose `user_prompt` STARTS with the cron-hint banner literal and ENDS with `summarize the day`.
    - Test 2 (script output block): With `script_output = Some("hello world")`, the assembled string contains `## Script Output` exactly once, with `hello world` on the lines that follow.
    - Test 3 (skill content + skip-missing prefix): With a skill registry whose `read_content("greeter")` returns `Some("you are friendly".into())` and `read_content("missing-skill")` returns `None`, and `job.skills = vec!["greeter".into(), "missing-skill".into()]`, the assembled string contains BOTH the literal substring `you are friendly` AND a skip-missing prefix containing `missing-skill`.
    - Test 4 (context_from happy path): A job whose `context_from = Some(vec!["<valid-uuid-1>".into()])` with a corresponding output file at `<HERMES_HOME>/cron/output/<uuid-1>/<YYYYMMDD_HHMMSS>.md` (set up by the test). The assembled prompt contains `## Output from job '<valid-uuid-1>'` exactly once and the file's content.
    - Test 5 (context_from 8000-char truncation): Same as Test 4 but the output file is 10000 chars. The assembled prompt contains exactly the first 8000 chars of the file content followed by the literal suffix `[... output truncated ...]`.
    - Test 6 (context_from UUID guard): A job whose `context_from = Some(vec!["../etc/passwd".into(), "not-a-uuid".into()])`. The assembled prompt contains NO `## Output from job` block (both ids rejected silently). A `tracing::warn!` fires for each rejected id.
    - Test 7 (assembly order): When all five steps are populated, they appear in the exact order: banner → skill content → `## Script Output` → `## Output from job 'X'` → user prompt. Verified by recording the byte offset of each section and asserting strictly ascending.
    - Test 8 (assembled-prompt rescan blocks): Inject a prompt injection token from the `CRON_THREAT_PATTERNS` list (e.g. `ignore previous instructions`) into a SKILL'S content (not the user prompt — proves the rescan sees the assembled view). `build_job_prompt` returns `AssembledPrompt { blocked_reason: Some(...), ... }` whose reason contains the scanner's verdict text. Callers (Plan 06) decide whether to emit a BLOCKED delivery doc.
    - Test 9 (assembled-prompt rescan clean): A benign assembled prompt returns `blocked_reason: None`.
  </behavior>
  <action>
    Replace the stub `prompt_builder.rs` with the full implementation:

    (a) Imports:

        use anyhow::Result;
        use ironhermes_core::{get_hermes_home, SkillRegistry};
        use ironhermes_cron::{scan_cron_prompt, CronJob};
        use uuid::Uuid;

    (b) Public type:

        #[derive(Debug, Clone)]
        pub struct AssembledPrompt {
            pub system_addendum: String,  // unused for now; future-proofing
            pub user_prompt: String,
            pub blocked_reason: Option<String>,
        }

    (c) Constants:

        const CRON_HINT_BANNER: &str =
            "[IMPORTANT: You are running as a scheduled cron job. \
             Your response will be delivered automatically without user \
             interaction. Be concise and actionable.]\n\n";
        const CONTEXT_FROM_MAX_BYTES: usize = 8000;
        const CONTEXT_FROM_TRUNC_SUFFIX: &str = "\n[... output truncated ...]";

    (d) Helper — skill content + skip-missing-prefix:

        fn resolve_skill_content(
            registry: Option<&SkillRegistry>,
            skill_names: &[String],
        ) -> String {
            if skill_names.is_empty() { return String::new(); }
            let registry = match registry {
                Some(r) => r,
                None => {
                    tracing::warn!("skills requested but no SkillRegistry — skipping all");
                    return format!(
                        "[IMPORTANT: The following skill(s) were listed for this job but \
                         could not be found and were skipped: {}.]\n\n",
                        skill_names.join(", ")
                    );
                }
            };
            let mut parts: Vec<String> = Vec::new();
            let mut skipped: Vec<String> = Vec::new();
            for name in skill_names {
                match registry.read_content(name) {
                    Some(content) => parts.push(format!(
                        "[IMPORTANT: The user has invoked the \"{}\" skill...]\n\n{}",
                        name, content
                    )),
                    None => {
                        tracing::warn!(skill=%name, "skill not found at tick time — skipping");
                        skipped.push(name.clone());
                    }
                }
            }
            let mut out = String::new();
            if !skipped.is_empty() {
                out.push_str(&format!(
                    "[IMPORTANT: The following skill(s) were listed for this job but \
                     could not be found and were skipped: {}.]\n\n",
                    skipped.join(", ")
                ));
            }
            out.push_str(&parts.join("\n\n---\n\n"));
            if !out.is_empty() { out.push_str("\n\n"); }
            out
        }

    (e) Helper — context_from blocks with UUID guard + 8K cap:

        async fn resolve_context_from(
            job: &CronJob,
        ) -> String {
            let Some(source_ids) = &job.context_from else { return String::new(); };
            if source_ids.is_empty() { return String::new(); }
            let mut blocks: Vec<String> = Vec::new();
            for source_id in source_ids {
                // UUID guard: reject anything that's not a valid UUID.
                if Uuid::parse_str(source_id).is_err() {
                    tracing::warn!(source_id=%source_id, "context_from id is not a UUID — skipping");
                    continue;
                }
                let output_dir = get_hermes_home()
                    .join("cron").join("output").join(source_id);
                let mut entries: Vec<_> = match std::fs::read_dir(&output_dir) {
                    Ok(rd) => rd.filter_map(|e| e.ok()).collect(),
                    Err(_) => continue,
                };
                entries.sort_by_key(|e| e.file_name());
                let latest = match entries.last() {
                    Some(e) => e,
                    None => continue,
                };
                let content = match std::fs::read_to_string(latest.path()) {
                    Ok(c) => c,
                    Err(_) => continue,
                };
                let truncated = if content.len() > CONTEXT_FROM_MAX_BYTES {
                    let cap = content
                        .char_indices()
                        .rev()
                        .find(|(i, _)| *i <= CONTEXT_FROM_MAX_BYTES)
                        .map(|(i, _)| i)
                        .unwrap_or(CONTEXT_FROM_MAX_BYTES);
                    format!("{}{}", &content[..cap], CONTEXT_FROM_TRUNC_SUFFIX)
                } else {
                    content
                };
                blocks.push(format!("## Output from job '{}'\n\n{}\n\n", source_id, truncated));
            }
            blocks.join("")
        }

    (f) Public `build_job_prompt`:

        pub async fn build_job_prompt(
            job: &CronJob,
            script_output: Option<&str>,
            skill_registry: Option<&SkillRegistry>,
        ) -> Result<AssembledPrompt> {
            let mut assembled = String::new();

            // 1. Cron-hint banner
            assembled.push_str(CRON_HINT_BANNER);

            // 2. Skill content (with skip-missing prefix)
            assembled.push_str(&resolve_skill_content(skill_registry, &job.skills));

            // 3. ## Script Output block (when applicable)
            if let Some(stdout) = script_output {
                let stdout = stdout.trim();
                if !stdout.is_empty() {
                    assembled.push_str(&format!("## Script Output\n\n{}\n\n", stdout));
                }
            }

            // 4. context_from blocks (8K cap each, UUID-guarded)
            assembled.push_str(&resolve_context_from(job).await);

            // 5. User prompt
            assembled.push_str(&job.prompt);

            // Post-assembly threat rescan
            let blocked_reason = match scan_cron_prompt(&assembled) {
                Ok(()) => None,
                Err(reason) => Some(reason.to_string()),
            };

            Ok(AssembledPrompt {
                system_addendum: String::new(),
                user_prompt: assembled,
                blocked_reason,
            })
        }

    NOTE: `scan_cron_prompt`'s actual return type may be `Result<(),
    SomeError>` or `Result<(), &'static str>` — verify by `grep -nA3 "pub fn
    scan_cron_prompt" crates/ironhermes-cron/src/scanner.rs` before
    finalizing. Adjust the `match` arm accordingly. If the function returns
    `bool` (true = clean, false = blocked) instead, adjust to that shape.

    (g) Tests covering behaviors 1–9. For Test 4/5, set `HERMES_HOME` to a
    tempdir and write fixture files at the expected paths. Use a generated
    UUID via `Uuid::new_v4()` to keep tests deterministic per-run.
    `SkillRegistry` is constructed via whatever pub constructor the
    `ironhermes-core` crate exposes today — verify via
    `grep -nE "impl SkillRegistry|pub fn new" crates/ironhermes-core/src/skill_registry.rs`.

    For Test 8, the prompt-injection token MUST come from the scanner's own
    `CRON_THREAT_PATTERNS` regex list — embed it in a SKILL's content (not
    the user prompt) so the test proves the rescan operates on the
    POST-assembly view. Hard-coded examples from
    crates/ironhermes-cron/src/scanner.rs (e.g. `"ignore previous instructions"`).
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cron-runner --lib prompt_builder -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `cargo test -p ironhermes-cron-runner --lib prompt_builder -- --test-threads=1` exits 0
    - source assertion: `grep -n "pub async fn build_job_prompt" crates/ironhermes-cron-runner/src/prompt_builder.rs` matches exactly 1 line
    - source assertion: `grep -nE "(scan_cron_prompt|Uuid::parse_str|## Script Output|## Output from job|\\[\\.\\.\\. output truncated \\.\\.\\.\\])" crates/ironhermes-cron-runner/src/prompt_builder.rs | wc -l` is at least 5 (every locked anchor visible in source)
    - source assertion: `grep -n "CONTEXT_FROM_MAX_BYTES: usize = 8000" crates/ironhermes-cron-runner/src/prompt_builder.rs` matches exactly 1 line
    - source assertion: `grep -nE "blocked_reason: Option<String>" crates/ironhermes-cron-runner/src/prompt_builder.rs` matches at least 1 line
    - test-command: `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>`prompt_builder::build_job_prompt` produces a five-section assembled prompt in the locked order, validates each context_from id as a UUID, caps each context_from block at 8000 bytes, and runs the scanner over the full assembled view, returning a `blocked_reason` for callers to consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: timeout.rs — inactivity poll + wall-clock primitives</name>
  <files>crates/ironhermes-cron-runner/src/timeout.rs</files>
  <read_first>
    - crates/ironhermes-cron-runner/src/timeout.rs (the stub from Task 1)
    - .planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md (§Timeout & resilience — locks both inactivity (default 600s) and wall-clock (default 4h) gates, with `0` ⇒ unlimited)
    - .planning/phases/32.1-agent-cron-execution/32.1-RESEARCH.md (§Architecture Patterns Pattern 3 — the tokio::select! shape)
    - .planning/phases/32.1-agent-cron-execution/32.1-PATTERNS.md (§timeout.rs lines 551–579 — no exact analog, this module is new)
    - 32.1-02-SUMMARY.md (the `activity_summary()` signature on `AgentLoop` Plan 02 landed)
  </read_first>
  <behavior>
    - Test 1 (wall-clock fires): `run_with_wall_clock(async { tokio::time::sleep(Duration::from_secs(5)).await; Ok(()) }, 1).await` returns `Err` with message containing `wall-clock timeout`. Elapsed wall time bounded under 2s.
    - Test 2 (wall-clock disabled): `run_with_wall_clock(async { Ok::<_, anyhow::Error>(42i32) }, 0).await` returns `Ok(42)` — `wall_secs == 0` means unlimited.
    - Test 3 (wall-clock succeeds when fast): `run_with_wall_clock(async { Ok::<_, anyhow::Error>("done") }, 60).await` returns `Ok("done")`.
    - Test 4 (inactivity fires): An `activity_summary_fn` that always returns `1000.0` (1000 seconds since last activity) paired with `inactivity_secs = 5` triggers the inactivity branch. `run_with_inactivity_timeout(async { tokio::time::sleep(Duration::from_secs(60)).await; Ok(()) }, fn_, 5).await` returns `Err` with message containing `inactivity timeout`. Elapsed wall time bounded under 7s (5s polling check + 5s slack).
    - Test 5 (inactivity disabled): `inactivity_secs == 0` means unlimited — same future as Test 4 runs to completion.
    - Test 6 (inactivity succeeds when active): `activity_summary_fn` always returns `0.1` (recent activity). Future sleeps 2s, returns Ok. Wrapper returns Ok.
    - Test 7 (inactivity gating: brief idleness does not trigger): `activity_summary_fn` returns `2.0` (2s since activity) but `inactivity_secs = 10`. Future sleeps 3s, returns Ok. Wrapper returns Ok.
  </behavior>
  <action>
    Replace the stub `timeout.rs` with:

    (a) Imports:

        use anyhow::{anyhow, Result};
        use std::future::Future;
        use std::time::Duration;
        use tokio::time::{sleep, timeout};

    (b) `run_with_wall_clock`:

        pub async fn run_with_wall_clock<F, T>(fut: F, wall_secs: u64) -> Result<T>
        where
            F: Future<Output = Result<T>>,
        {
            if wall_secs == 0 {
                return fut.await;
            }
            match timeout(Duration::from_secs(wall_secs), fut).await {
                Ok(inner) => inner,
                Err(_) => Err(anyhow!("wall-clock timeout after {}s", wall_secs)),
            }
        }

    (c) `run_with_inactivity_timeout`:

        pub async fn run_with_inactivity_timeout<F, T, S>(
            fut: F,
            activity_summary_fn: S,
            inactivity_secs: u64,
        ) -> Result<T>
        where
            F: Future<Output = Result<T>> + Send,
            S: Fn() -> f64 + Send + 'static,
        {
            if inactivity_secs == 0 {
                return fut.await;
            }
            tokio::select! {
                result = fut => result,
                _ = async {
                    loop {
                        sleep(Duration::from_secs(5)).await;
                        let idle_secs = activity_summary_fn();
                        if idle_secs > inactivity_secs as f64 {
                            break;
                        }
                    }
                } => Err(anyhow!("inactivity timeout after {}s of no activity", inactivity_secs))
            }
        }

    NOTE: the trait bound `S: Fn() -> f64 + Send + 'static` is sufficient
    for the `tokio::select!` arm; the closure is owned by the polling task.
    `Sync` is NOT needed because we never share the closure across awaits.

    (d) Tests covering behaviors 1–7. Use `tokio::time::Instant::now()` to
    bound wall-clock assertions. For the activity callback in Tests 4–7,
    use a simple `move || 1000.0` closure or `Arc<AtomicU64>` for dynamic
    cases. Tests use `#[tokio::test(flavor = "current_thread", start_paused = false)]`.

    NOTE: do NOT use `start_paused = true` — the timeout primitives rely on
    real `tokio::time::sleep` advancement. Real wall-clock waits are
    acceptable here since each test's max duration is under 10 seconds.
    If CI flakes on this, switch to `tokio::time::pause()` + `advance`
    inside the test body.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cron-runner --lib timeout</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `cargo test -p ironhermes-cron-runner --lib timeout` exits 0
    - source assertion: `grep -n "pub async fn run_with_wall_clock" crates/ironhermes-cron-runner/src/timeout.rs` matches exactly 1 line
    - source assertion: `grep -n "pub async fn run_with_inactivity_timeout" crates/ironhermes-cron-runner/src/timeout.rs` matches exactly 1 line
    - source assertion: `grep -nE "wall-clock timeout|inactivity timeout" crates/ironhermes-cron-runner/src/timeout.rs` matches at least 2 lines
    - source assertion: `awk '/pub async fn run_with_wall_clock/,/^}/' crates/ironhermes-cron-runner/src/timeout.rs | grep -c "wall_secs == 0"` returns at least 1 (unlimited branch)
    - source assertion: `awk '/pub async fn run_with_inactivity_timeout/,/^}/' crates/ironhermes-cron-runner/src/timeout.rs | grep -c "inactivity_secs == 0"` returns at least 1 (unlimited branch)
    - test-command: `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>The two timeout primitives compile, return the locked error messages, and respect the `0 ⇒ unlimited` convention from CONTEXT.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `${HERMES_HOME}/scripts/<name>` resolved from `job.script` on disk | The `script` field is operator-controlled but the resolution MUST stay inside `${HERMES_HOME}/scripts/`; absolute paths and `..` traversal are the primary attack |
| `context_from` source ids on disk | Each id is an operator-controlled string; a non-UUID id MUST NOT translate into a filesystem lookup |
| Assembled prompt → LLM | A malicious skill, script output, or context_from block could carry prompt-injection tokens; the post-assembly rescan is the input-validation gate |
| Subprocess stdout/stderr → callers | Stdout may contain secrets if the script reads env or files; `redact_passthrough` is a TODO seam for a future workspace redactor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32.1.05-01 | Elevation of Privilege | Path traversal via `script` field | mitigate | `canonicalize() + starts_with(scripts_dir)` in `run_job_script`. Tests 3 + 4 prove `../etc/passwd` and absolute paths are rejected before any subprocess spawn. |
| T-32.1.05-02 | Elevation of Privilege | Path traversal via `context_from` id | mitigate | `uuid::Uuid::parse_str` guard before the filesystem read in `resolve_context_from`. Test 6 proves `../etc/passwd` and `not-a-uuid` are rejected silently. |
| T-32.1.05-03 | Tampering | Prompt injection via skill content or `context_from` output | mitigate | `scan_cron_prompt(assembled)` AFTER the five-step assembly, not before. Caller (Plan 06) translates a non-`None` `blocked_reason` into a BLOCKED delivery doc per CONTEXT.md. |
| T-32.1.05-04 | Denial of Service | Runaway script | mitigate | `IRONHERMES_CRON_SCRIPT_TIMEOUT` (default 120s) + `kill_on_drop(true)` on the subprocess child. Test 7 proves timeout fires within deadline. |
| T-32.1.05-05 | Denial of Service | Runaway agent (no script) | mitigate | `run_with_wall_clock` + `run_with_inactivity_timeout` primitives. Defaults are 4h wall and 600s inactivity per CONTEXT.md. Tests 1 + 4 prove both fire. |
| T-32.1.05-06 | Information Disclosure | Secret leakage via script stdout | accept-with-TODO | `redact_passthrough` is a pass-through placeholder; no Rust redactor exists in the workspace today (RESEARCH.md Open Question 1). Deferred to a future phase. Risk profile: scripts are operator-controlled, run only when scheduled, and outputs are stored in `0600` files (Plan 04 chmod). |
| T-32.1.05-07 | Information Disclosure | Cron task-locals exposed across spawn boundaries | mitigate | The `tokio::task_local!` statics propagate across `.await` but not `tokio::spawn`. Callers (Plan 06) wrap any `spawn` with explicit `scope()` wrapping. Documented in the `lib.rs` doc-comment. |
| T-32.1.05-SC | Tampering | npm/pip/cargo installs | mitigate | Single new dependency on `tempfile` (dev-only). `tempfile` is already a workspace dev-dep, verified by `grep -n "tempfile" Cargo.toml`. No production crates added; all six dependency paths are sibling workspace crates already vetted. |
</threat_model>

<verification>
- `cargo build -p ironhermes-cron-runner` is green
- `cargo build --workspace` is green
- `cargo test -p ironhermes-cron-runner -- --test-threads=1` is green
- The crate appears in `Cargo.toml` `members` and `default-members`
- All five public entry points (`run_job_script`, `parse_wake_gate`, `build_job_prompt`, `run_with_wall_clock`, `run_with_inactivity_timeout`) are exported and tested
</verification>

<success_criteria>
- New workspace crate `ironhermes-cron-runner` builds standalone and inside the workspace
- Three `tokio::task_local!` statics for cron delivery context
- `script_runner` enforces sandbox, interpreter selection, timeout, wake-gate parsing
- `prompt_builder` produces the five-step assembled prompt, UUID-guards `context_from`, runs the scanner post-assembly
- `timeout` provides `run_with_wall_clock` and `run_with_inactivity_timeout` primitives with `0 ⇒ unlimited` convention
- No new production workspace dependency (only `tempfile` dev-dep)
</success_criteria>

<output>
Create `.planning/phases/32.1-agent-cron-execution/32.1-05-SUMMARY.md` when done. Record:
- Confirmed signature of `scan_cron_prompt` (the executor verified during Task 3) — Plan 06 uses the same shape
- Whether `SkillRegistry::read_content` returned `Option<String>` or `Option<&str>` — affects Plan 06's wiring
- The exact `redact_passthrough` deferral entry (a TODO comment line + planned follow-up phase number)
- Whether `tempfile` was wired as `workspace = true` or a literal `"3"` pin
- Any deferred items for Plan 06 (e.g. "scan_cron_prompt returns &'static str — Plan 06 must call `.to_string()` before storing the reason")
</output>
