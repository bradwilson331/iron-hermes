---
phase: 32.1-agent-cron-execution
plan: 07
type: execute
wave: 5
depends_on: [32.1-03, 32.1-04, 32.1-06]
files_modified:
  - crates/ironhermes-gateway/src/runner.rs
  - crates/ironhermes-cli/src/cron.rs
  - crates/ironhermes-cli/Cargo.toml
autonomous: true
requirements:
  - CRON-CLI-TRIGGER
  - CRON-CLI-TICK-ONCE
  - CRON-CLI-DAEMON
  - CRON-GATEWAY-MIGRATION
  - CRON-GATEWAY-CALL-NEW-CRATE
tags: [cron, cli, gateway, migration]

must_haves:
  truths:
    - "`hermes cron trigger <id-or-name>` exists; on success prints `Triggered job <id>` and the job's `next_run_at` is set to `Utc::now()` via `JobStore::trigger_job` (Plan 03)"
    - "`hermes cron tick` (replacing the existing placeholder) runs ONE full tick cycle — acquires tick lock, scans due jobs, calls `ironhermes_cron_runner::run_cron_job` for each, then exits"
    - "`hermes cron daemon` runs `ironhermes_cron_runner::run_tick_loop` until interrupted by Ctrl-C (SIGINT)"
    - "`hermes cron daemon` honors `IRONHERMES_CRON_MAX_PARALLEL`, `IRONHERMES_CRON_TIMEOUT`, `IRONHERMES_CRON_WALL_TIMEOUT_SECS` exactly as the gateway does — because both call `run_tick_loop`"
    - "Gateway `runner.rs::execute_cron_job` is DELETED; gateway's cron tick task at ~line 738 calls `ironhermes_cron_runner::run_tick_loop(ctx, cancel)` instead of its prior inline tick orchestration"
    - "Gateway's `dispatch_delivery` (single-target, lines 1014–1048) is DELETED; the runner crate's `dispatch_all_targets` (Plan 06) is the single dispatch path"
    - "Gateway's `fast_forward_backlog` (line 935) remains (it's a gateway-specific boot guard) but no longer references the deleted `execute_cron_job`"
    - "The workspace still builds and ALL pre-existing gateway/cli/cron tests still pass — proven by `cargo test --workspace` exiting 0"
  artifacts:
    - path: "crates/ironhermes-gateway/src/runner.rs"
      provides: "Gateway tick task delegates to ironhermes-cron-runner::run_tick_loop; execute_cron_job + dispatch_delivery deleted"
      contains: "ironhermes_cron_runner::run_tick_loop"
    - path: "crates/ironhermes-cli/src/cron.rs"
      provides: "Trigger/TickOnce/Daemon subcommands wired to runner crate"
      contains: "CronCommands::Daemon"
    - path: "crates/ironhermes-cli/Cargo.toml"
      provides: "Adds ironhermes-cron-runner dependency"
      contains: "ironhermes-cron-runner"
  key_links:
    - from: "crates/ironhermes-cli/src/cron.rs::cmd_tick"
      to: "crates/ironhermes-cron-runner/src/runner.rs::run_cron_job"
      via: "for each due job, call run_cron_job(job, ctx).await"
      pattern: "ironhermes_cron_runner::run_cron_job"
    - from: "crates/ironhermes-cli/src/cron.rs::cmd_daemon"
      to: "crates/ironhermes-cron-runner/src/tick_loop.rs::run_tick_loop"
      via: "CLI constructs CronRunnerContext + cancel token then calls run_tick_loop"
      pattern: "ironhermes_cron_runner::run_tick_loop"
    - from: "crates/ironhermes-gateway/src/runner.rs (tick task)"
      to: "crates/ironhermes-cron-runner/src/tick_loop.rs::run_tick_loop"
      via: "gateway calls run_tick_loop with its own cancel token"
      pattern: "ironhermes_cron_runner::run_tick_loop"
---

<objective>
Cut over from the gateway's inline `execute_cron_job` / `dispatch_delivery`
implementation (the seed that PARITY.md §10 documented as out-of-scope for
the cron crate) to the new `ironhermes-cron-runner` crate's public API.
Simultaneously add three CLI subcommands: `hermes cron trigger
<id-or-name>`, `hermes cron tick` (replacing the placeholder), and
`hermes cron daemon` (long-running tick loop).

After this plan lands, the workspace has ONE cron-execution code path —
the new runner crate — exercised by gateway, CLI tick, and CLI daemon.
PARITY.md §10 + §11 are fully closed for everything except the
intentionally-deferred `JobState::Error`.

Purpose: Plan 06 produced a working runner crate but no caller was wired
to it yet. Wave 5 is the migration wave: gateway delegates, CLI gains the
new subcommands, the legacy code is removed.

Output: a one-call-path workspace where every cron run flows through
`ironhermes_cron_runner::run_cron_job` regardless of caller.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md
@.planning/phases/32.1-agent-cron-execution/32.1-RESEARCH.md
@.planning/phases/32.1-agent-cron-execution/32.1-PATTERNS.md
@.planning/phases/32.1-agent-cron-execution/32.1-03-SUMMARY.md
@.planning/phases/32.1-agent-cron-execution/32.1-04-SUMMARY.md
@.planning/phases/32.1-agent-cron-execution/32.1-06-SUMMARY.md
@crates/ironhermes-gateway/src/runner.rs
@crates/ironhermes-cli/src/cron.rs
@crates/ironhermes-cli/Cargo.toml
@crates/ironhermes-cron-runner/src/lib.rs
@crates/ironhermes-cron-runner/src/runner.rs
@crates/ironhermes-cron-runner/src/tick_loop.rs

<interfaces>
<!-- New CLI subcommands in CronCommands enum -->
- Trigger { job_id: String }
- TickOnce          (replaces existing Tick variant? OR adds beside it — see Action)
- Daemon

<!-- Gateway migration shape -->
- Delete: pub(crate) async fn execute_cron_job(...) at line 1051
- Delete: pub async fn dispatch_delivery(...) at line 1014
- Edit:   the cron-tick task block at ~line 738 — replace inline orchestration with `ironhermes_cron_runner::run_tick_loop(ctx, cancel_token).await;`
- Keep:   pub async fn fast_forward_backlog(store) at line 935 — gateway-specific boot guard
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI subcommands — Trigger + TickOnce + Daemon wired to ironhermes-cron-runner</name>
  <files>crates/ironhermes-cli/src/cron.rs, crates/ironhermes-cli/Cargo.toml</files>
  <read_first>
    - crates/ironhermes-cli/src/cron.rs (entire file: CronCommands enum at line 17, handle_cron_command at line 98, existing cmd_tick at line 446)
    - crates/ironhermes-cli/Cargo.toml (verify ironhermes-cron-runner is NOT already a dep)
    - crates/ironhermes-cron-runner/src/lib.rs (the public re-exports landed in Plan 05–06)
    - crates/ironhermes-cron-runner/src/runner.rs (CronRunnerContext fields — every Arc the CLI needs to construct)
    - crates/ironhermes-cron/src/lib.rs (`acquire_tick_lock`, `run_tick_check` — CLI tick reuses them via the runner)
    - .planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md (§CLI standalone execution — locks `tick` (single-shot) + `daemon` (long-running) semantics)
    - .planning/phases/32.1-agent-cron-execution/32.1-PATTERNS.md (§ironhermes-cli/src/cron.rs lines 750–779)
  </read_first>
  <behavior>
    - Test 1 (Trigger by id): On a tempdir-backed `HERMES_HOME`, add a job programmatically, then invoke `handle_cron_command(CronCommands::Trigger { job_id: <id> })`. After return, reload the store and assert `next_run_at` is within 5s of `Utc::now()`. Stdout contains `Triggered job <id>`.
    - Test 2 (Trigger by name): Same as Test 1 but pass the job's name. Assert same store state.
    - Test 3 (Trigger nonexistent): Invoke with `"nope"`. Returns `Err` containing `job not found`.
    - Test 4 (TickOnce exits): Invoke `handle_cron_command(CronCommands::Tick)`. Function returns within a few seconds (never spawns a long-running loop). Stdout contains `Tick complete` (or similar one-line summary).
    - Test 5 (Daemon: cancel exits): Spawn `handle_cron_command(CronCommands::Daemon)` in a tokio task. Send SIGINT (or invoke `cancel.cancel()` via test hook). The future resolves within 2s.
    - Test 6 (CronRunnerContext construction): Both `cmd_tick_once` and `cmd_daemon` construct a `CronRunnerContext` with `tg_client = None` (CLI never has a live adapter). Verified via source assertion.
  </behavior>
  <action>
    (a) **Cargo.toml** — add the runner crate dependency:

        ironhermes-cron-runner = { path = "../ironhermes-cron-runner" }

    (b) **cron.rs — extend `CronCommands` enum** (after the existing `Tick`
    variant — keep `Tick` as the single-shot one-tick command per Test 4):

        /// Trigger a job immediately (sets next_run_at = now)
        Trigger {
            /// Job ID or name (case-insensitive)
            job_id: String,
        },
        /// Run as a long-lived cron daemon (ticks every 60s without gateway)
        Daemon,

    NOTE: keep the existing `Tick` variant. CONTEXT.md describes the
    user-facing CLI as `hermes cron tick` (single-shot) and `hermes cron
    daemon` (long-running). The existing `cmd_tick` placeholder is the
    natural single-shot home — we just need to wire it to the runner.

    (c) **handle_cron_command match arms** — add three new arms (or rewire
    existing `Tick` arm):

        CronCommands::Tick      => cmd_tick().await,        // now does real work
        CronCommands::Trigger { job_id }
                                => cmd_trigger(job_id),
        CronCommands::Daemon    => cmd_daemon().await,

    (d) **`cmd_trigger`** — synchronous, mirrors `cmd_pause` style:

        fn cmd_trigger(job_id: String) -> Result<()> {
            let mut store = ironhermes_cron::JobStore::new()?;
            store.trigger_job(&job_id)?;
            let resolved = store.find_job(&job_id)
                .map(|j| j.id.clone())
                .unwrap_or(job_id);
            println!("Triggered job {}", resolved);
            Ok(())
        }

    (e) **`cmd_tick`** — the existing async fn at line 446 currently is a
    placeholder; rewrite to do real work:

        async fn cmd_tick() -> Result<()> {
            let config = Config::load().unwrap_or_default();
            let ctx = build_cron_runner_ctx(&config).await?;

            let Some(_lock) = ironhermes_cron::acquire_tick_lock()? else {
                println!("Another tick is already running. Exiting.");
                return Ok(());
            };

            let (due_jobs, tick_result, _inner_lock) =
                ironhermes_cron::run_tick_check(&ctx.job_store).await?;

            for job in &due_jobs {
                if let Err(e) = ironhermes_cron_runner::run_cron_job(job, &ctx).await {
                    error!(job_id=%job.id, "tick: job failed: {}", e);
                }
            }

            println!("Tick complete. {} due, {} ran, {} errors.",
                     due_jobs.len(),
                     tick_result.jobs_run,
                     tick_result.jobs_skipped);
            Ok(())
        }

    NOTE: the exact fields on `TickResult` may differ from `jobs_run` /
    `jobs_skipped`. Verify via `grep -nA5 "pub struct TickResult"
    crates/ironhermes-cron/src/tick.rs` and adjust the format string.

    (f) **`cmd_daemon`** — long-running tick loop:

        async fn cmd_daemon() -> Result<()> {
            use std::sync::Arc;
            use tokio_util::sync::CancellationToken;

            let config = Config::load().unwrap_or_default();
            let ctx = Arc::new(build_cron_runner_ctx(&config).await?);
            let cancel = CancellationToken::new();
            let cancel_for_signal = cancel.clone();

            // Spawn ctrl-c watcher
            tokio::spawn(async move {
                let _ = tokio::signal::ctrl_c().await;
                tracing::info!("Received Ctrl+C, cancelling cron daemon...");
                cancel_for_signal.cancel();
            });

            println!("Cron daemon running. Press Ctrl+C to stop.");
            ironhermes_cron_runner::run_tick_loop(ctx, cancel).await;
            println!("Cron daemon stopped.");
            Ok(())
        }

    (g) **`build_cron_runner_ctx`** — shared helper used by both
    `cmd_tick` and `cmd_daemon`:

        async fn build_cron_runner_ctx(
            config: &ironhermes_core::Config,
        ) -> Result<ironhermes_cron_runner::CronRunnerContext> {
            use std::sync::{Arc, Mutex as StdMutex};
            use tokio::sync::RwLock;

            let job_store = Arc::new(StdMutex::new(ironhermes_cron::JobStore::new()?));

            // ToolRegistry — build the same registry the gateway uses for cron
            // context. Reuse whatever public constructor / `build_tool_registry`
            // helper the project exposes. If a dedicated cron-mode helper
            // exists (e.g. `build_cron_tool_registry`), prefer it.
            let tool_registry = Arc::new(RwLock::new(
                ironhermes_tools::ToolRegistry::default()
            ));

            // skill_registry — load if config.skills is enabled
            let skill_registry: Option<Arc<ironhermes_core::SkillRegistry>> =
                /* whatever the existing CLI does for skill registry loading;
                   if no analog, None is acceptable for v1 */
                None;

            // memory_manager / hook_registry — best-effort load; None acceptable
            let memory_manager = None;
            let hook_registry = None;
            let mcp_manager = None;

            Ok(ironhermes_cron_runner::CronRunnerContext {
                job_store,
                skill_registry,
                tool_registry,
                memory_manager,
                hook_registry,
                config: config.clone(),
                mcp_manager,
                tg_client: None,  // CLI path is always standalone
            })
        }

    NOTE: the `build_cron_runner_ctx` helper is the primary integration
    point. The placeholder bodies for `skill_registry`, `memory_manager`,
    `hook_registry`, `mcp_manager` reflect that the CLI path doesn't
    require all of these for a minimum viable tick. If the existing CLI
    has its own skill/memory wiring (search via `grep -n
    "SkillRegistry\\|MemoryManager" crates/ironhermes-cli/src/`), use it.
    Otherwise, ship with `None` and add a `// TODO: wire skills/memory
    for CLI cron path` comment + record in SUMMARY.

    (h) **Imports** — at the top of cron.rs, add:

        use anyhow::Result;
        use tracing::error;
        // (Config, ironhermes_cron_runner, ironhermes_cron may already be imported)

    (i) Tests: `#[cfg(test)] mod tests_phase_32_1`. For tests requiring
    process-wide state (Test 4 / Test 5), use the project's env-lock and
    a `tempfile::TempDir` for `HERMES_HOME`. Tests 1–3 (`cmd_trigger`) are
    straightforward — set up the store, call, assert. Test 5 (daemon) uses
    `tokio::time::timeout(Duration::from_secs(3), handle_cron_command(...))`
    + manually cancelling — or sends a SIGINT-equivalent via the cancel
    token (the daemon's task-spawned ctrl-c watcher does not exist in the
    test environment because there's no terminal).

    Easiest path for Test 5: factor out a `cmd_daemon_with_cancel(cancel:
    CancellationToken) -> Result<()>` helper. `cmd_daemon` calls it after
    setting up the ctrl-c watcher. The test calls `cmd_daemon_with_cancel`
    directly with a pre-cancelled token, asserting it returns within ~1s.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cli --lib tests_phase_32_1 -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `cargo test -p ironhermes-cli --lib tests_phase_32_1 -- --test-threads=1` exits 0
    - source assertion: `grep -nE "CronCommands::(Trigger|Daemon)" crates/ironhermes-cli/src/cron.rs` returns at least 4 lines (enum variant declarations + match arms)
    - source assertion: `grep -n "fn cmd_trigger" crates/ironhermes-cli/src/cron.rs` matches exactly 1 line
    - source assertion: `grep -n "async fn cmd_daemon" crates/ironhermes-cli/src/cron.rs` matches exactly 1 line
    - source assertion: `grep -n "ironhermes_cron_runner::run_cron_job" crates/ironhermes-cli/src/cron.rs` matches at least 1 line (cmd_tick wiring)
    - source assertion: `grep -n "ironhermes_cron_runner::run_tick_loop" crates/ironhermes-cli/src/cron.rs` matches at least 1 line (cmd_daemon wiring)
    - source assertion: `grep -n "ironhermes-cron-runner" crates/ironhermes-cli/Cargo.toml` matches exactly 1 line (dep)
    - source assertion: `grep -n "tg_client: None" crates/ironhermes-cli/src/cron.rs` matches at least 1 line (CLI is always standalone)
    - test-command: `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>CLI gains three new commands wired to the runner crate. `hermes cron trigger` works for both id and name lookups. `hermes cron tick` runs ONE tick cycle and exits. `hermes cron daemon` runs a long-lived tick loop terminable by Ctrl+C.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gateway migration — delete inline execute_cron_job + dispatch_delivery, call run_tick_loop instead</name>
  <files>crates/ironhermes-gateway/src/runner.rs</files>
  <read_first>
    - crates/ironhermes-gateway/src/runner.rs (focus on lines 738–832 [tick task], 909–925 [resolve_skill_context — KEEP, gateway-specific], 935 [fast_forward_backlog — KEEP], 1014–1048 [dispatch_delivery — DELETE], 1051–1229 [execute_cron_job — DELETE]; lines 1349, 1408–1420, 1446, 1459, 1669, 1892–1989 contain test references to execute_cron_job that need updating)
    - crates/ironhermes-gateway/Cargo.toml (verify ironhermes-cron-runner needs to be added; Plan 06 already had ironhermes-gateway depend INTO the runner from runner side — verify no cycle; the dependency direction here is `gateway → cron-runner`, which is the same direction the runner reads `TgSendApi` from gateway. **WAIT: this is a CYCLE risk** — see Action below)
    - crates/ironhermes-cron-runner/Cargo.toml (Plan 06 made it depend on `ironhermes-gateway` — verify; if so, the dependency from gateway → cron-runner would CYCLE)
    - crates/ironhermes-cron-runner/src/runner.rs (`CronRunnerContext` field shape — the gateway builds one of these to call run_tick_loop)
    - 32.1-06-SUMMARY.md (cycle resolution choices, if Plan 06 hit this)
    - .planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md (§Crate layout — locks the cron-runner-owns-execution architecture; the cycle decision is in §Crate layout bullet 4: "gateway calls the new crate's public run_cron_job(...). Gateway retains the tick-task plumbing (interval, cancel token, fast-forward-on-first-tick guard, MCP orphan reaper).")
  </read_first>
  <behavior>
    - Test 1 (workspace builds): `cargo build --workspace` exits 0 with no cycle errors.
    - Test 2 (gateway tests pass): `cargo test -p ironhermes-gateway` exits 0. Existing tests at lines 1349, 1408–1420, 1446, 1459, 1669, 1892–1989 that reference `execute_cron_job` are updated to call `ironhermes_cron_runner::run_cron_job` (with a constructed `CronRunnerContext`) or are removed / relocated to the runner crate if they specifically tested behavior now owned by the runner.
    - Test 3 (cron job actually runs through new path): An integration test that adds a due job, ticks the gateway, and asserts the job is processed through `ironhermes_cron_runner::run_cron_job`. The simplest variant: patch the gateway test that currently calls `execute_cron_job` (line 1409) to call `ironhermes_cron_runner::run_cron_job` instead and assert the same outcome.
    - Test 4 (no dead code): `grep -n "fn execute_cron_job\\|fn dispatch_delivery" crates/ironhermes-gateway/src/runner.rs` returns 0 lines. Both functions are deleted.
    - Test 5 (tick task delegates): The gateway runner's cron tick task body is reduced to a call to `ironhermes_cron_runner::run_tick_loop(ctx, cancel).await` after constructing the `CronRunnerContext`. Verifiable by `grep -n run_tick_loop crates/ironhermes-gateway/src/runner.rs`.
  </behavior>
  <action>
    **CRITICAL: Resolve the gateway ↔ cron-runner cycle before any code change.**

    Plan 06 made `ironhermes-cron-runner` depend on `ironhermes-gateway` to
    pull in `TgSendApi`. If this task adds `ironhermes-cron-runner` to
    `ironhermes-gateway`'s `[dependencies]`, that creates a CYCLE.
    `cargo build` will refuse with a "cyclic package dependency" error.

    The fix (matches the Phase 21.2 `McpReloader` trait pattern documented in
    STATE.md): **MOVE `TgSendApi` to a neutral crate** so neither gateway
    nor cron-runner is the trait's home. The cron crate's
    `delivery.rs` (or a new `ironhermes-core::delivery` trait module) is the
    natural location since the trait is just `async fn send_message(...)`.

    Two acceptable options — choose ONE before editing source. Record the
    decision in the SUMMARY:

    **Option A** (preferred): move `pub trait TgSendApi` into
    `crates/ironhermes-cron/src/delivery.rs` (or a new
    `crates/ironhermes-cron/src/adapter.rs`). Re-export from
    `ironhermes-cron::lib.rs`. Update Plan 06's runner.rs + delivery.rs
    imports to `use ironhermes_cron::TgSendApi`. Gateway keeps the
    `TelegramAdapter: TgSendApi` impl but pulls the trait from
    `ironhermes-cron`. Remove `ironhermes-gateway` from
    `ironhermes-cron-runner/Cargo.toml`. Add
    `ironhermes-cron-runner = { path = ... }` to `ironhermes-gateway/Cargo.toml`.

    **Option B**: move `TgSendApi` to `ironhermes-core`. Same logic but
    `ironhermes-core::TgSendApi` is the trait home. Slightly broader
    semantic placement.

    Both options solve the cycle. Option A is closer to the existing
    project pattern (the trait is cron-delivery-specific).

    NOTE: this trait-relocation is a 30-line edit (move the trait
    declaration, update 3–5 imports, leave the impl with `TelegramAdapter`
    in gateway/telegram.rs). It is in-scope for this task because Plan 06
    structurally requires it.

    With the cycle resolved, proceed:

    (a) **Add `ironhermes-cron-runner` dependency to
    `crates/ironhermes-gateway/Cargo.toml`** (under `[dependencies]`,
    alphabetized):

        ironhermes-cron-runner = { path = "../ironhermes-cron-runner" }

    (b) **Delete `pub(crate) async fn execute_cron_job`** at line 1051 and
    its entire body up to the closing `}`. Also delete `pub async fn
    dispatch_delivery` at line 1014–1048. Use the Edit tool, replacing
    the full block with NOTHING (or with a comment line
    `// Plan 32.1-07: execute_cron_job + dispatch_delivery moved to
    crates/ironhermes-cron-runner` — operator preference).

    (c) **Edit the cron tick task** (block starting at ~line 738 — locate
    via `grep -nE "let.*tick_cancel|cron.*tick|interval\\.tick" crates/
    ironhermes-gateway/src/runner.rs`). The current body interleaves
    `acquire_tick_lock` → `run_tick_check` → `execute_cron_job` per job.

    Replace the body with construction of `CronRunnerContext` (using the
    Arcs the gateway already has in scope — `job_store`, `skill_registry`,
    `tool_registry`, `memory_manager`, `hook_registry`, `config`,
    `mcp_manager`, `tg_client`) and a call to
    `ironhermes_cron_runner::run_tick_loop(ctx, tick_cancel).await;`.
    The `tick_cancel: CancellationToken` is already in scope per existing
    gateway code.

    If the existing `fast_forward_backlog` runs on first-tick boot, keep
    that call BEFORE invoking `run_tick_loop` — the cron-runner's tick
    loop has its own first-tick suppression (Plan 06 Task 3) but the
    gateway-specific backlog forward-advance is a different mechanism
    (advancing missed runs without dispatching). Keep both: gateway runs
    `fast_forward_backlog` on the JobStore, then enters
    `run_tick_loop`.

    (d) **Update gateway tests** at lines 1349 / 1408–1420 / 1446 / 1459 /
    1669 / 1892–1989 that reference `execute_cron_job`. Three options
    per test:
       (i) **Migrate** — rewrite to call
           `ironhermes_cron_runner::run_cron_job` with a constructed
           `CronRunnerContext` (most tests should take this path because
           the behavior being tested — hook fires, error propagation —
           is identical).
       (ii) **Move** — relocate the test to
           `crates/ironhermes-cron-runner/tests/` if it specifically
           tests behavior the runner crate now owns. Don't delete tests
           unless they were testing the gateway's `execute_cron_job`
           specifically (e.g. the gateway-version-of-the-test was a
           stub that's no longer needed).
       (iii) **Delete** — if the test was a TODO/placeholder marker for
           "Phase 07.3 Task 4 execute_cron_job extraction" (line 1349
           hints at this), it can be removed.

    For each updated test, the test name and assertions are preserved as
    much as possible — only the call site changes. The fixture-building
    helpers (`make_test_store`, etc.) stay in the gateway test module.

    (e) **`fast_forward_backlog`** at line 935 — keep its body as-is. It
    only touches `JobStore` (cron crate primitives) and does not call
    `execute_cron_job`. Verify post-edit that this function still exists
    and compiles.

    (f) **Static-grep regression test** — add a test in the gateway's
    test module asserting the migration didn't regress:

        #[test]
        fn execute_cron_job_no_longer_exists_in_gateway() {
            let src = include_str!("runner.rs");
            assert!(
                !src.contains("fn execute_cron_job"),
                "execute_cron_job must be deleted from gateway/src/runner.rs \
                 (moved to crates/ironhermes-cron-runner per Phase 32.1)"
            );
            assert!(
                !src.contains("fn dispatch_delivery"),
                "dispatch_delivery must be deleted from gateway/src/runner.rs"
            );
            assert!(
                src.contains("ironhermes_cron_runner::run_tick_loop"),
                "gateway must call ironhermes_cron_runner::run_tick_loop"
            );
        }

    This regression test guards against any future refactor accidentally
    re-introducing the deleted functions.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-gateway && cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `cargo build --workspace` exits 0 (no dependency cycle)
    - test-command: `cargo test -p ironhermes-gateway` exits 0
    - source assertion: `grep -n "fn execute_cron_job" crates/ironhermes-gateway/src/runner.rs` returns 0 lines (function deleted)
    - source assertion: `grep -n "fn dispatch_delivery" crates/ironhermes-gateway/src/runner.rs` returns 0 lines (function deleted)
    - source assertion: `grep -n "ironhermes_cron_runner::run_tick_loop" crates/ironhermes-gateway/src/runner.rs` matches at least 1 line
    - source assertion: `grep -n "fn fast_forward_backlog" crates/ironhermes-gateway/src/runner.rs` matches exactly 1 line (preserved)
    - source assertion: `grep -n "ironhermes-cron-runner" crates/ironhermes-gateway/Cargo.toml` matches exactly 1 line (dep added)
    - source assertion: `grep -nE "trait TgSendApi|impl TgSendApi" crates/ironhermes-cron/src/` returns at least 1 line (trait moved; OR equivalent for ironhermes-core under Option B)
    - source assertion: the regression test from Action (f) is present — `grep -n "execute_cron_job_no_longer_exists_in_gateway" crates/ironhermes-gateway/src/runner.rs` matches at least 1 line
    - test-command: `cargo test --workspace` exits 0 (all crates green — proves no migration regression)
  </acceptance_criteria>
  <done>Gateway delegates cron tick to `ironhermes_cron_runner::run_tick_loop`; the inline `execute_cron_job` + `dispatch_delivery` are deleted; the dependency cycle was resolved via trait relocation (Option A or B documented); the full workspace builds and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args (`hermes cron trigger <id>`) → JobStore | The job id/name string is operator-controlled; `JobStore::find_job` does no filesystem lookup with it (string equality only — no traversal vector) |
| Gateway tick task ↔ cron-runner crate | After migration, the only data crossing is the `Arc`-shared CronRunnerContext; no shared mutable state outside the crate's contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32.1.07-01 | Tampering | CLI `cmd_trigger` id arg | mitigate | `JobStore::find_job` does only in-memory string equality / case-insensitive lookup; no filesystem path is constructed from the id. |
| T-32.1.07-02 | Denial of Service | Daemon ctrl-c handler races with tick loop | mitigate | `CancellationToken` is the documented graceful-shutdown primitive; tested explicitly (Test 5). |
| T-32.1.07-03 | Code Reintroduction | A future refactor re-creates `execute_cron_job` inside gateway | mitigate | Regression test `execute_cron_job_no_longer_exists_in_gateway` asserts the deleted symbols stay deleted. |
| T-32.1.07-04 | Dependency cycle | gateway ↔ cron-runner edge | mitigate | `TgSendApi` trait moved to `ironhermes-cron` (Option A) or `ironhermes-core` (Option B). Resolution documented in SUMMARY. |
| T-32.1.07-SC | Tampering | npm/pip/cargo installs | mitigate | One workspace-internal dep added to two crate manifests (`ironhermes-cli` and `ironhermes-gateway`); no external crates. |
</threat_model>

<verification>
- `cargo build --workspace` is green
- `cargo test --workspace` is green
- `hermes cron trigger`, `hermes cron tick`, `hermes cron daemon` all defined and routable
- Gateway has no inline `execute_cron_job` or `dispatch_delivery`
- The cron-runner ↔ gateway dependency edge runs one-way (gateway → cron-runner) after trait relocation
- PARITY.md §10 + §11 (excluding intentionally-deferred `JobState::Error`) are fully closed
</verification>

<success_criteria>
- Three new CLI subcommands present and tested
- Gateway tick task delegates to `run_tick_loop`
- `execute_cron_job` + `dispatch_delivery` deleted from gateway
- `TgSendApi` trait relocated to neutral crate; dependency cycle resolved
- Regression test prevents future re-introduction of deleted functions
- Full workspace test suite green
</success_criteria>

<output>
Create `.planning/phases/32.1-agent-cron-execution/32.1-07-SUMMARY.md` when done. Record:
- Which Option (A or B) was used for `TgSendApi` relocation, and the exact destination crate + module
- A list of every gateway test that was migrated/moved/deleted and the disposition for each
- Whether `Config::load()` was a viable construction path in the CLI helper or whether the executor used a different idiom
- The list of `CronRunnerContext` field bodies the CLI helper constructed as `None` (informational — future plans can fill them in)
- Final PARITY.md status — every closed gap labeled and confirmation that only `JobState::Error` (item #2) remains deferred

Final phase outcome (to be summarized by the orchestrator at the end of the
phase): the workspace has a unified cron execution path via
`ironhermes-cron-runner::run_cron_job`, reachable from gateway tick,
`hermes cron tick`, and `hermes cron daemon`. Phase 32.1 closes PARITY.md
§10 (full agent execution port) and 21 of 22 §11 incidental gaps.
</output>
</content>
</invoke>