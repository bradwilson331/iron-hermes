---
phase: 32.1-agent-cron-execution
plan: 03
type: execute
wave: 2
depends_on: [32.1-01]
files_modified:
  - crates/ironhermes-cron/src/store.rs
  - crates/ironhermes-cron/src/parser.rs
autonomous: true
requirements:
  - CRON-STORE-FSYNC
  - CRON-STORE-CHMOD
  - CRON-STORE-CTRLCHAR-REPAIR
  - CRON-STORE-TRIGGER-JOB
  - CRON-STORE-ONESHOT-GRACE
  - CRON-STORE-DYNAMIC-GRACE
  - CRON-STORE-DUE-RECOVERY
  - CRON-PARSER-ANCHORING
  - CRON-STORE-JOBS-MUT
tags: [cron, store, parser, parity]

must_haves:
  truths:
    - "`JobStore::save` calls `f.sync_all()?` after `f.flush()?` so atomic rename guarantees durability of the new inode's bytes, not just its path"
    - "On Unix, `JobStore` enforces `chmod 0700` on the cron directory and `chmod 0600` on `jobs.json` after every create/save"
    - "A corrupted `jobs.json` with bare control characters in string values triggers an auto-repair: serde_json fails → strip 0x00–0x1F (preserving \\n/\\r/\\t) → re-parse → rewrite + `tracing::warn!`"
    - "`JobStore::trigger_job(id_or_name)` exists and sets `next_run_at = Utc::now()` on the matching job, persisting via `save()`"
    - "`JobStore::jobs_mut(&mut self) -> &mut Vec<CronJob>` exists as a `pub(crate)` accessor so Plan 06's runner can in-place set `last_delivery_error` without going through `update_job` + `JobUpdate` plumbing"
    - "`ONESHOT_GRACE_SECONDS = 120` exists as a public `pub const` in `ironhermes-cron::job` and `get_due_jobs` uses it: a `Once` job whose `run_at` was up to 120s ago and that has never run is still returned as due"
    - "`compute_grace_seconds(&ScheduleParsed) -> i64` returns `(interval_minutes * 60) / 2` for Interval (clamped to [120, 7200]), `3600` for Cron, `120` for Once — replacing the fixed `grace_seconds = 3600` field on JobStore"
    - "`get_due_jobs` recovers a missing `next_run_at`: for recurring schedules it recomputes from now and persists the value; for `Once` schedules it falls back to the 120s grace window"
    - "`compute_next_run_from(schedule, after, last_run_at)` exists with the same signature as today's `compute_next_run` plus an `Option<DateTime<Utc>>` last-run anchor; when `last_run_at` is Some, Interval anchors at `last_run_at + period` instead of `after + period`; existing `compute_next_run(schedule, after)` continues to compile via thin wrapper forwarding `None`"
  artifacts:
    - path: "crates/ironhermes-cron/src/store.rs"
      provides: "Hardened JobStore — fsync, Unix chmod, control-char repair, trigger_job, jobs_mut accessor, dynamic grace, due-job recovery"
      contains: "pub fn trigger_job"
    - path: "crates/ironhermes-cron/src/parser.rs"
      provides: "compute_next_run_from with last_run_at anchoring"
      contains: "pub fn compute_next_run_from"
  key_links:
    - from: "crates/ironhermes-cron/src/store.rs::JobStore::save"
      to: "POSIX durability"
      via: "sync_all after flush, before rename"
      pattern: "sync_all"
    - from: "crates/ironhermes-cron/src/store.rs::JobStore::get_due_jobs"
      to: "crates/ironhermes-cron/src/parser.rs::compute_next_run_from"
      via: "recomputes next_run_at when missing for recurring jobs"
      pattern: "compute_next_run_from"
    - from: "crates/ironhermes-cron/src/store.rs::JobStore::trigger_job"
      to: "crates/ironhermes-cron/src/store.rs::JobStore::save"
      via: "after mutating next_run_at"
      pattern: "self\\.save\\(\\)"
    - from: "crates/ironhermes-cron/src/store.rs::JobStore::jobs_mut"
      to: "crates/ironhermes-cron-runner/src/runner.rs::complete_and_dispatch"
      via: "in-place mutation of last_delivery_error after dispatch"
      pattern: "pub fn jobs_mut"
---

<objective>
Close eight of the PARITY.md §11 incidental gaps in the cron crate's storage
and schedule-parsing modules:

- §11.4 dynamic per-schedule grace
- §11.5 compute_next_run anchoring at `last_run_at`
- §11.6 missing-next_run_at recovery in get_due_jobs
- §11.7 `trigger_job`
- §11.3 one-shot grace window (`ONESHOT_GRACE_SECONDS = 120`)
- §11.15 Unix 0700/0600 chmod
- §11.17 control-char auto-repair
- §11.18 fsync on save

Additionally, expose a `pub fn jobs_mut(&mut self) -> &mut Vec<CronJob>` accessor
on `JobStore` so Plan 06's runner can in-place set `last_delivery_error`
without expanding `JobUpdate`. This is a small, explicit Plan 03 deliverable
called out by the checker to remove ambiguity at execution time (see also
Plan 06 Task 2 action item (f) `complete_and_dispatch`).

These are all in the `ironhermes-cron` crate (no new crates, no agent-loop
touches) and are independent of the new runner crate landing in Plans 05–06.
Landing them in Wave 2 means Plans 05–06 build against an already-hardened
store and a parser with the anchoring signature they need.

Purpose: the new `ironhermes-cron-runner` crate (Plan 05–06) relies on
`trigger_job` (CLI `hermes cron trigger`), `ONESHOT_GRACE_SECONDS`,
`compute_grace_seconds`, `compute_next_run_from` for tick semantics, and
`jobs_mut` for `last_delivery_error` accumulation. Production durability
(fsync) and Unix permission hardening close ASVS L1 gaps independent of
the runner work.

Output: an extended `JobStore` and `parser` with full PARITY.md storage-tier
parity. The bare-`[...]` jobs.json envelope decision (PARITY.md §11.16) is
LOCKED to "keep the bare array" per CONTEXT.md and is not modified here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md
@.planning/phases/32.1-agent-cron-execution/32.1-RESEARCH.md
@.planning/phases/32.1-agent-cron-execution/32.1-PATTERNS.md
@.planning/phases/32.1-agent-cron-execution/32.1-01-SUMMARY.md
@crates/ironhermes-cron/PARITY.md
@crates/ironhermes-cron/src/store.rs
@crates/ironhermes-cron/src/parser.rs
@crates/ironhermes-cron/src/job.rs

<interfaces>
<!-- Existing JobStore API (from grep) -->
- pub struct JobStore at line 87
- pub fn new() at line 95
- pub fn open(dir: PathBuf) at line 100
- pub fn reload(&mut self) at line 158
- pub fn add_job at line 205
- pub fn update_job at line 256
- pub fn remove_job at line 297
- pub fn get_job at line 308
- pub fn find_job at line 313
- pub fn list_jobs at line 321
- pub fn get_due_jobs(&mut self) -> Vec<&CronJob> at line 327
- pub fn toggle_job(&mut self, id: &str, enabled: bool) at line 363
- pub fn mark_job_run at line 387
- pub fn save(&self) at line 434

<!-- Existing parser API (from PATTERNS.md and PARITY.md) -->
- pub fn compute_next_run(schedule: &ScheduleParsed, after: DateTime<Utc>) -> Result<Option<DateTime<Utc>>>
- pub enum ScheduleParsed { Once { run_at, raw_input }, Interval { minutes, ... }, Cron { ... } }

<!-- New functions/fields this plan adds -->
- pub const ONESHOT_GRACE_SECONDS: i64 = 120; (in job.rs OR parser.rs — choose one and re-export from lib.rs)
- pub fn compute_grace_seconds(schedule: &ScheduleParsed) -> i64 (in parser.rs)
- pub fn compute_next_run_from(schedule, after, last_run_at: Option<DateTime<Utc>>) -> Result<Option<DateTime<Utc>>> (in parser.rs)
- pub fn trigger_job(&mut self, id_or_name: &str) -> Result<()> (in store.rs)
- pub fn jobs_mut(&mut self) -> &mut Vec<CronJob> (in store.rs)  <-- NEW: explicit Plan 03 deliverable for Plan 06's runner
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parser additions — compute_next_run_from + compute_grace_seconds + ONESHOT_GRACE_SECONDS</name>
  <files>crates/ironhermes-cron/src/parser.rs</files>
  <read_first>
    - crates/ironhermes-cron/src/parser.rs (entire file — needs full read to find `compute_next_run`, `ScheduleParsed`, and the existing tests)
    - crates/ironhermes-cron/src/job.rs (find existing `ONESHOT_GRACE_SECONDS` constants — Plan 01 SUMMARY notes any constants it added; if absent here, Plan 03 introduces it)
    - crates/ironhermes-cron/src/lib.rs (the re-exports — anything new added here must be re-exported)
    - .planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md (§Storage parity — locks 120s grace, dynamic half-period clamped [120, 7200], anchoring semantics)
    - .planning/phases/32.1-agent-cron-execution/32.1-PATTERNS.md (§parser.rs anchored compute_next_run lines 727–742; §store.rs Dynamic grace seconds lines 667–676)
    - hermes-agent/cron/jobs.py (reference for `compute_next_run(schedule, after, last_run_at)` and `_compute_grace_seconds`)
  </read_first>
  <behavior>
    - Test 1: `ONESHOT_GRACE_SECONDS` is `120` and is publicly accessible from `ironhermes_cron::ONESHOT_GRACE_SECONDS` (re-export in lib.rs).
    - Test 2: `compute_grace_seconds(&ScheduleParsed::Interval { minutes: 10, .. })` returns `300` (half of 600 = 300, within clamp).
    - Test 3: `compute_grace_seconds(&ScheduleParsed::Interval { minutes: 1, .. })` returns `120` (half of 60 = 30, clamped UP to 120).
    - Test 4: `compute_grace_seconds(&ScheduleParsed::Interval { minutes: 600, .. })` returns `7200` (half of 36000 = 18000, clamped DOWN to 7200).
    - Test 5: `compute_grace_seconds(&ScheduleParsed::Cron { .. })` returns `3600`.
    - Test 6: `compute_grace_seconds(&ScheduleParsed::Once { .. })` returns `120` (== `ONESHOT_GRACE_SECONDS`).
    - Test 7: `compute_next_run_from(&Interval{ minutes: 10 }, t0, None)` equals today's `compute_next_run(&Interval{minutes:10}, t0)` (backward-compat anchor).
    - Test 8: `compute_next_run_from(&Interval{ minutes: 10 }, t_now, Some(t_last))` returns `t_last + 10min` regardless of how `t_now` differs from `t_last` (anchoring at last_run prevents drift across crashes).
    - Test 9: `compute_next_run_from(&Cron{...}, t_now, Some(t_last))` returns the FIRST cron tick strictly after `t_last` (not after `t_now`) — anchoring matches Python behavior.
    - Test 10: `compute_next_run(schedule, after)` (the existing signature) still compiles and returns the same value as `compute_next_run_from(schedule, after, None)` — proven by a regression test calling both and asserting equality on a representative Interval and Cron schedule.
  </behavior>
  <action>
    In `crates/ironhermes-cron/src/parser.rs`:

    (a) Add `pub const ONESHOT_GRACE_SECONDS: i64 = 120;` near the top of the
    file (just below the existing `use` block).

    (b) Add `pub fn compute_grace_seconds(schedule: &ScheduleParsed) -> i64`.
    Body: match on schedule; for `Interval { minutes, .. }` return
    `((*minutes as i64) * 60 / 2).clamp(120, 7200)`; for `Cron { .. }` return
    `3600`; for `Once { .. }` return `ONESHOT_GRACE_SECONDS`.

    (c) Rename the existing `pub fn compute_next_run(schedule, after) -> ...`
    body into a new `pub fn compute_next_run_from(schedule: &ScheduleParsed,
    after: DateTime<Utc>, last_run_at: Option<DateTime<Utc>>) -> Result<...>`
    function. The new function's logic:
      - For `ScheduleParsed::Interval { minutes, .. }`:
          let anchor = last_run_at.unwrap_or(after);
          Ok(Some(anchor + Duration::minutes(*minutes as i64)))
      - For `ScheduleParsed::Cron { .. }`:
          use the existing cron-crate iteration logic but start the iterator
          at `last_run_at.unwrap_or(after)` rather than hardcoded `after`.
      - For `ScheduleParsed::Once { .. }`: unchanged from today — returns
          `Some(run_at)` only if `run_at > after`; otherwise `None`.

    Keep `pub fn compute_next_run(schedule, after)` as a thin wrapper:
      `compute_next_run_from(schedule, after, None)`. This preserves every
    existing call site in the crate (`store.rs::mark_job_run`,
    `tick.rs::complete_job_run`, etc.).

    (d) In `crates/ironhermes-cron/src/lib.rs`, ensure `pub use parser::*;`
    already re-exports `compute_next_run`, `compute_next_run_from`,
    `compute_grace_seconds`, and `ONESHOT_GRACE_SECONDS` via the existing
    glob re-export. Verify the names are exported by `grep -n "pub use
    parser" crates/ironhermes-cron/src/lib.rs`. If a `pub use` is more
    restrictive (e.g. explicit symbol list), add the new symbols to the
    list.

    (e) Append a `#[cfg(test)] mod parser_tests_phase_32_1` (or extend the
    existing `tests` module) covering Tests 1–10 from the behavior block.
    Use deterministic timestamps (`Utc.with_ymd_and_hms(2026, 1, 1, ...)`)
    and `chrono::Duration`.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cron --lib parser_tests_phase_32_1</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `cargo test -p ironhermes-cron --lib parser_tests_phase_32_1` exits 0
    - source assertion: `grep -n "pub const ONESHOT_GRACE_SECONDS: i64 = 120" crates/ironhermes-cron/src/parser.rs` matches exactly 1 line
    - source assertion: `grep -n "pub fn compute_grace_seconds" crates/ironhermes-cron/src/parser.rs` matches exactly 1 line
    - source assertion: `grep -n "pub fn compute_next_run_from" crates/ironhermes-cron/src/parser.rs` matches exactly 1 line
    - source assertion: `grep -n "pub fn compute_next_run\\(" crates/ironhermes-cron/src/parser.rs` matches exactly 1 line (the wrapper preserves the old signature)
    - source assertion: `grep -n "compute_next_run_from(schedule, after, None)" crates/ironhermes-cron/src/parser.rs` matches at least 1 line (proves wrapper delegates)
    - test-command: `cargo test -p ironhermes-cron` exits 0 (no existing test broken)
    - test-command: `cargo build --workspace` exits 0 (every call site of `compute_next_run` still compiles via the wrapper)
  </acceptance_criteria>
  <done>`parser.rs` exposes `ONESHOT_GRACE_SECONDS`, `compute_grace_seconds`, and `compute_next_run_from`; the old `compute_next_run` continues to work; ten new tests cover the matrix; the workspace still builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: JobStore hardening — fsync, chmod, control-char repair, trigger_job, jobs_mut accessor, dynamic grace, due-job recovery</name>
  <files>crates/ironhermes-cron/src/store.rs</files>
  <read_first>
    - crates/ironhermes-cron/src/store.rs (entire file: `JobStore`, `JobUpdate`, `LegacyCronJob` migration, `open`, `reload`, `add_job`, `update_job`, `remove_job`, `get_due_jobs` at line 327, `mark_job_run` at line 387, `save` at line 434)
    - crates/ironhermes-cron/src/parser.rs (Task 1's `compute_grace_seconds` + `compute_next_run_from` + `ONESHOT_GRACE_SECONDS` are required)
    - crates/ironhermes-cron/src/delivery.rs lines 144–168 (`make_job` test fixture pattern — `trigger_job` tests will reuse it)
    - crates/ironhermes-cron/src/lib.rs lines 139–145 (`#[cfg(unix)]` pattern reference for `is_process_alive` — use the same pattern for `set_permissions(0o700/0o600)`)
    - 32.1-01-SUMMARY.md (records any fixture-builder fixups Plan 01 already shipped — avoid duplicating)
    - .planning/phases/32.1-agent-cron-execution/32.1-PATTERNS.md (§store.rs lines 632–693)
    - .planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md (§Storage parity — locks the eight behaviors)
  </read_first>
  <behavior>
    - Test 1 (fsync): After calling `store.save()` on a tempdir-backed store, the saved `jobs.json` exists at the expected path and contains valid JSON. (Static-grep the `sync_all` call in source — directly testing fsync semantics requires hardware-level instrumentation, not in scope.)
    - Test 2 (chmod, Unix only): After `JobStore::open(tempdir.path().join("cron"))`, on Unix, `std::fs::metadata(<cron_dir>).permissions().mode() & 0o777` equals `0o700`. After `store.save()`, `<jobs.json>` permissions mode is `0o600`. Test gated by `#[cfg(unix)]`.
    - Test 3 (control-char repair): Manually write a `jobs.json` file containing `[{"id":"x","name":"<0x07 bell>bad","prompt":"...",...}]` (every required field present, with a bare ASCII 0x07 inside the `name` string). Call `JobStore::open(...)`. The store loads ONE job, its `name` is `" bad"` (bell replaced by space), and a `tracing::warn!` was emitted with the substring `control characters`. The repaired version is rewritten to disk (verified by reading the file again and confirming no bare 0x07 byte remains).
    - Test 4 (trigger_job by id): Add a job, then call `store.trigger_job(&job.id)`. Reload the store. The job's `next_run_at` equals `Utc::now()` within a 5-second window.
    - Test 5 (trigger_job by name): Add a job with name `"daily-sync"`, then call `store.trigger_job("daily-sync")`. Same assertion as Test 4.
    - Test 6 (trigger_job nonexistent): Call `store.trigger_job("nope")`. Returns `Err` with message containing `job not found`.
    - Test 7 (dynamic grace): Add an Interval(minutes=10) job whose `next_run_at` is 200s in the past. Call `get_due_jobs()`. Job IS returned (200s < clamped grace of 300s for 10-min interval).
    - Test 8 (oneshot grace): Add a `Once` job whose `run_at` is 60s in the past. Call `get_due_jobs()`. Job IS returned (within 120s oneshot grace).
    - Test 9 (oneshot beyond grace): Add a `Once` job whose `run_at` is 200s in the past. Call `get_due_jobs()`. Job is NOT returned (200s > 120s oneshot grace).
    - Test 10 (due recovery, recurring): Add a recurring Interval job whose `next_run_at` is `None`. Call `get_due_jobs()`. `next_run_at` is recomputed (`compute_next_run_from(schedule, now, last_run_at)`) and persisted; if the recomputed value is `<= now + grace`, the job IS returned.
    - Test 11 (due recovery, once): Add a `Once` job whose `next_run_at` is `None` but `schedule.run_at` is 30s in the past. Call `get_due_jobs()`. Job IS returned (uses 120s grace).
    - Test 12 (jobs_mut accessor): Build a store with two jobs. Call `store.jobs_mut()` to obtain `&mut Vec<CronJob>`. Mutate the second job's `last_delivery_error = Some("test-err".into())` in place. Call `store.save()`. Reload from disk. The second job's `last_delivery_error` equals `Some("test-err".into())`. The first job's `last_delivery_error` is unchanged (None).
  </behavior>
  <action>
    In `crates/ironhermes-cron/src/store.rs`:

    (a) **fsync** — locate `pub fn save(&self) -> Result<()>` at line 434. After
    the existing `f.flush()?` line, add `f.sync_all()?;`. Same change anywhere
    else `JobStore` opens-writes-renames a file (look for additional
    `fs::File::create` / `flush` pairs; only `save()` is expected in this
    file).

    (b) **Unix chmod 0700 on cron dir** — locate `JobStore::open(dir: PathBuf)`
    at line 100. After the existing `fs::create_dir_all(&dir)?` (or
    equivalent) call, add a `#[cfg(unix)] { ... }` block that runs
    `fs::set_permissions(&dir, fs::Permissions::from_mode(0o700))` and
    silently ignores errors via `let _ = ...`. Mirror the existing
    `#[cfg(unix)]` style from lib.rs lines 139–145.

    (c) **Unix chmod 0600 on jobs.json** — at the end of `save()` (after the
    `fs::rename(&tmp, &path)?`), add a `#[cfg(unix)] { ... }` block setting
    permissions on `path` to `0o600`. Silent on error.

    (d) **Control-char auto-repair** — in `JobStore::open` (and `reload` if it
    independently parses the file), wrap the existing `serde_json::from_str::
    <Vec<CronJob>>(&raw)` call with a fallback:

        match serde_json::from_str::<Vec<CronJob>>(&raw) {
            Ok(jobs) => jobs,
            Err(e) if e.is_data() || raw_has_bare_ctrl_chars(&raw) => {
                let cleaned: String = raw.chars()
                    .map(|c| if (c as u32) < 0x20 && !matches!(c, '\n'|'\r'|'\t')
                        { ' ' } else { c }).collect();
                match serde_json::from_str::<Vec<CronJob>>(&cleaned) {
                    Ok(jobs) => {
                        tracing::warn!(
                            "jobs.json had bare control characters — repaired and rewriting"
                        );
                        // Best-effort rewrite — do not propagate write errors here,
                        // they'll surface on next save().
                        let _ = std::fs::write(&path, &cleaned);
                        jobs
                    }
                    Err(_) => return Err(e.into()),  // unrecoverable
                }
            }
            Err(e) => return Err(e.into()),
        }

    Where `raw_has_bare_ctrl_chars(&raw)` is a private helper:
    `raw.bytes().any(|b| b < 0x20 && !matches!(b, b'\n'|b'\r'|b'\t'))`. The
    second arm (control-char detected directly) gives a robustness net since
    not all serde_json error categories cleanly classify as `is_data()`.

    Existing legacy-migration paths (`LegacyCronJob`) must remain reachable —
    apply the same control-char repair to the legacy-format branch by
    re-using `cleaned` inside that match arm.

    (e) **`trigger_job(id_or_name)`** — add a new public method anywhere
    among the other mutating methods (after `toggle_job` at line 363 is fine).
    Signature: `pub fn trigger_job(&mut self, id_or_name: &str) -> Result<()>`.
    Body: use existing `self.find_job(id_or_name)` to locate; if `None`,
    `anyhow::bail!("job not found: {id_or_name}")`. Else extract the id,
    locate the `&mut CronJob` in `self.jobs`, set `job.next_run_at =
    Some(Utc::now())`, `tracing::info!("Job id={id} manually triggered")`,
    then `self.save()`.

    (f) **`jobs_mut(&mut self) -> &mut Vec<CronJob>`** — add a new public
    method on `JobStore` (adjacent to the existing `list_jobs` reader at
    line 321 is the natural location). Signature and body:

        /// Mutable accessor over the in-memory job list. Used by the
        /// `ironhermes-cron-runner` crate to set `last_delivery_error`
        /// in place after dispatching delivery (Plan 32.1-06 Task 2).
        ///
        /// Callers MUST follow up with `self.save()` to persist mutations.
        /// Prefer `update_job` + `JobUpdate` for canonical partial updates;
        /// this accessor exists specifically because `last_delivery_error`
        /// accumulation in the cron runner is a write-only side effect
        /// that doesn't model cleanly as a `JobUpdate` variant.
        pub fn jobs_mut(&mut self) -> &mut Vec<CronJob> {
            &mut self.jobs
        }

    NOTE: the visibility is `pub` (not `pub(crate)`) because the consumer
    (`ironhermes-cron-runner`) lives in a separate workspace crate. The
    doc comment explicitly names Plan 32.1-06 as the consumer so future
    maintainers know the intentional API surface.

    (g) **Dynamic grace** — remove the `grace_seconds: i64 = 3600` field on
    `JobStore` (if present). Update `get_due_jobs(&mut self)` to use
    `parser::compute_grace_seconds(&job.schedule)` per job instead of the
    fixed value. Specifically, the existing "skip & fast-forward stale
    recurring jobs" branch (PARITY.md §4 row 117) becomes per-job:
        let grace_secs = parser::compute_grace_seconds(&job.schedule);
        let is_stale = next - now < -Duration::seconds(grace_secs);
    For `Once` schedules, treat the grace as `ONESHOT_GRACE_SECONDS` (which
    `compute_grace_seconds` already returns).

    (h) **Missing-next_run_at recovery** — at the top of the per-job loop in
    `get_due_jobs`, BEFORE the disabled/paused/state checks, handle
    `job.next_run_at = None`:
      - For `ScheduleParsed::Once { run_at, .. }`: treat the job as due if
        `now <= run_at + ONESHOT_GRACE_SECONDS` AND the job has never run
        (`last_run_at.is_none()`).
      - For `ScheduleParsed::Interval` or `Cron`: recompute next via
        `parser::compute_next_run_from(&job.schedule, now,
        job.last_run_at)`. Set `job.next_run_at = Some(recomputed)` in
        place. After the loop, persist via `self.save()` (best-effort).
    The persistence is best-effort to match Python parity — if `save`
    fails, log a warning and continue (the next tick will retry).

    (i) **Tests** — add `#[cfg(test)] mod store_phase_32_1_tests` at the
    bottom of store.rs covering Tests 1–12. Use `tempfile::TempDir` (already
    a dev-dep) for FS isolation. The `make_job` style fixture from
    delivery.rs is the model — Plan 01 should have already updated it for
    the new `CronJob` fields; mirror it here if needed.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cron --lib store_phase_32_1_tests</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `cargo test -p ironhermes-cron --lib store_phase_32_1_tests` exits 0
    - source assertion: `grep -nE "f\\.sync_all\\(\\)" crates/ironhermes-cron/src/store.rs` matches at least 1 line
    - source assertion: `grep -nE "from_mode\\(0o700\\)" crates/ironhermes-cron/src/store.rs` matches at least 1 line
    - source assertion: `grep -nE "from_mode\\(0o600\\)" crates/ironhermes-cron/src/store.rs` matches at least 1 line
    - source assertion: `grep -nE "control characters" crates/ironhermes-cron/src/store.rs` matches at least 1 line (the warn! message)
    - source assertion: `grep -n "pub fn trigger_job" crates/ironhermes-cron/src/store.rs` matches exactly 1 line
    - source assertion: `grep -n "pub fn jobs_mut" crates/ironhermes-cron/src/store.rs` matches exactly 1 line (Plan 06 dependency)
    - source assertion: `grep -nE "compute_grace_seconds|compute_next_run_from" crates/ironhermes-cron/src/store.rs` matches at least 2 lines (used in `get_due_jobs`)
    - source assertion: the fixed `grace_seconds: i64 = 3600` field is removed — `grep -n "grace_seconds.*3600" crates/ironhermes-cron/src/store.rs` returns 0 matches (only the dynamic helper remains)
    - test-command: `cargo test -p ironhermes-cron` exits 0 (full crate test pass)
    - test-command: `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>`JobStore` enforces fsync + Unix chmod 0700/0600; auto-repairs bare-control-char jobs.json; exposes `trigger_job` and the new `jobs_mut` accessor; uses dynamic per-schedule grace; recovers missing `next_run_at` for both recurring and `Once` schedules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `jobs.json` on disk → in-process `JobStore` | Attacker with FS write can craft malformed JSON; the control-char repair is a defense against accidental corruption, not against adversarial input — adversarial scenarios fall outside Phase 32.1 |
| Filesystem permissions of `~/.hermes/cron/` | Unix chmod 0700/0600 prevents other users on the same host from reading job records and outputs |
| Tick recovery vs replay | Recovered `next_run_at` values must not produce repeated runs of past jobs — anchoring at `last_run_at` is the correctness guard |
| `jobs_mut` exposed across crate boundary | A bug in the consumer crate could mutate fields beyond `last_delivery_error`; mitigated by doc-comment policy + the consumer-crate is in-tree, not third-party |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32.1.03-01 | Tampering | Corrupted `jobs.json` | mitigate | Control-char repair degrades garbage to spaces and rewrites, preserving job continuity. Adversarial JSON (e.g. crafted to bypass the cleaner) is out of scope — would require full schema validation. |
| T-32.1.03-02 | Information Disclosure | `~/.hermes/cron/` readable by other Unix users | mitigate | `chmod 0700` on cron dir, `0600` on `jobs.json` and each output file. Best-effort (`let _ = ...`); on Windows there is no equivalent. Plan 04 covers `save_job_output` chmod 0600 in delivery.rs. |
| T-32.1.03-03 | Tampering | Path traversal via `trigger_job(id_or_name)` if a name contains slashes | accept | `find_job` only does string equality / case-insensitive name match against in-memory records; it does not perform any filesystem lookup using the provided name. No path-traversal vector. |
| T-32.1.03-04 | Denial of Service | `get_due_jobs` recovery + `save()` per tick | mitigate | Best-effort save when recovering missing `next_run_at` — failure logs a warn and the next tick retries. No retry storm. |
| T-32.1.03-05 | Denial of Service | Crash window between `flush` and `sync_all` | mitigate | Adding `sync_all` is the mitigation; it costs ~1 syscall per save, which fires only on CRUD. Cost acceptable. |
| T-32.1.03-06 | Tampering | `jobs_mut` accessor allows arbitrary in-place CronJob mutation | accept | Consumer is the in-tree `ironhermes-cron-runner` crate. Doc-comment policy and code review are the controls. A future tightening could replace this with a typed `set_last_delivery_error(id, value)` API. |
| T-32.1.03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new crates introduced; the work uses `tempfile` (already a dev-dep), `chrono`, `serde_json`, `tracing`, `std::os::unix::fs` — all in workspace today. |
</threat_model>

<verification>
- `cargo test -p ironhermes-cron` is green
- `cargo build --workspace` is green
- PARITY.md §11 incidental gaps #3, #4, #5, #6, #7, #15, #17, #18 are closed (the SUMMARY for this plan documents which items it closed)
- `pub fn jobs_mut(&mut self) -> &mut Vec<CronJob>` is callable from `ironhermes-cron-runner` (verified at build time by Plan 06's compile)
- No new workspace dependency added
</verification>

<success_criteria>
- All eight PARITY.md §11 storage-tier gaps closed
- `JobStore::trigger_job(id_or_name)` works for both id and case-insensitive name lookups
- `JobStore::jobs_mut(&mut self) -> &mut Vec<CronJob>` exposed for Plan 06's runner
- Unix permission tests gated `#[cfg(unix)]` pass on macOS and Linux developer machines
- Workspace still compiles; no consumer's call site of `compute_next_run` is broken (wrapper preserves signature)
</success_criteria>

<output>
Create `.planning/phases/32.1-agent-cron-execution/32.1-03-SUMMARY.md` when done. Record:
- Whether the `grace_seconds` field on `JobStore` was actually present (some forks may have already removed it)
- The exact match arm used to detect control-char repair triggers (so Plan 04 can mirror it for `save_job_output` recovery if needed)
- Whether `compute_next_run` (no `_from`) is still called by any consumer outside `parser.rs` (informational — those call sites will gradually migrate to `compute_next_run_from`)
- Confirmation that `pub fn jobs_mut(&mut self) -> &mut Vec<CronJob>` was shipped (Plan 06 Task 2 reads this)
- Any deferred items
</output>
