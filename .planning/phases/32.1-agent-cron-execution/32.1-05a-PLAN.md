---
phase: 32.1-agent-cron-execution
plan: 05a
type: execute
wave: 3
depends_on: [32.1-01, 32.1-02, 32.1-03, 32.1-04]
files_modified:
  - Cargo.toml
  - crates/ironhermes-cron-runner/Cargo.toml
  - crates/ironhermes-cron-runner/src/lib.rs
  - crates/ironhermes-cron-runner/src/script_runner.rs
  - crates/ironhermes-cron-runner/src/prompt_builder.rs
  - crates/ironhermes-cron-runner/src/timeout.rs
autonomous: true
requirements:
  - CRON-RUNNER-CRATE
  - CRON-RUNNER-TASKLOCALS
  - CRON-RUNNER-SCRIPT-SANDBOX
  - CRON-RUNNER-WAKE-GATE
tags: [cron, new-crate, script-runner]

must_haves:
  truths:
    - "Workspace gains a new member crate `ironhermes-cron-runner` that builds with `cargo build -p ironhermes-cron-runner`"
    - "The crate root `lib.rs` declares three `tokio::task_local!` statics: `CRON_AUTO_DELIVER_PLATFORM`, `CRON_AUTO_DELIVER_CHAT_ID`, `CRON_AUTO_DELIVER_THREAD_ID`, each `pub static ... : String`"
    - "`script_runner::run_job_script(script_name) -> Result<ScriptOutcome>` resolves the script under `${HERMES_HOME}/scripts/` via `canonicalize() + starts_with(scripts_dir)` and refuses any path that escapes (returns `Err` with the literal substring `escapes scripts_dir`)"
    - "`script_runner::run_job_script` selects interpreter by extension: `.sh`/`.bash` → bash, anything else → python3 — picked up from `BASH_PATH` / `PYTHON_PATH` env overrides when set, else hardcoded fallbacks `/bin/bash` and `python3`"
    - "`script_runner::run_job_script` enforces per-script timeout `IRONHERMES_CRON_SCRIPT_TIMEOUT` (default 120s) via `tokio::time::timeout`; timeout returns `ScriptOutcome { ok: false, ... }` rather than an `Err`"
    - "`script_runner::parse_wake_gate(stdout: &str) -> bool` returns `false` only when the LAST non-empty line of stdout parses as JSON and contains `{\"wakeAgent\": false}` (or `false`-equivalent boolean); every other shape returns `true` (default-wake)"
    - "Stub modules `prompt_builder` and `timeout` exist as empty files declared in `lib.rs` — they compile but are NOT implemented in this plan; Plan 05b fills them in"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace `members` + `default-members` include `crates/ironhermes-cron-runner`"
      contains: "ironhermes-cron-runner"
    - path: "crates/ironhermes-cron-runner/Cargo.toml"
      provides: "New crate manifest"
      contains: "ironhermes-cron-runner"
    - path: "crates/ironhermes-cron-runner/src/lib.rs"
      provides: "Crate root, module declarations, task_local! statics"
      contains: "tokio::task_local!"
    - path: "crates/ironhermes-cron-runner/src/script_runner.rs"
      provides: "Sandboxed script execution + wake-gate"
      contains: "pub async fn run_job_script"
    - path: "crates/ironhermes-cron-runner/src/prompt_builder.rs"
      provides: "Stub file — implemented in Plan 05b"
      contains: "Implemented in Task 1 of plan 32.1-05b"
    - path: "crates/ironhermes-cron-runner/src/timeout.rs"
      provides: "Stub file — implemented in Plan 05b"
      contains: "Implemented in Task 2 of plan 32.1-05b"
  key_links:
    - from: "crates/ironhermes-cron-runner/src/script_runner.rs"
      to: "filesystem sandbox under ${HERMES_HOME}/scripts/"
      via: "canonicalize() + starts_with(scripts_dir)"
      pattern: "starts_with\\(.*scripts_dir"
---

<objective>
Create the new `ironhermes-cron-runner` workspace crate skeleton (Cargo.toml,
lib.rs, module stubs) plus the first leaf module `script_runner.rs`. This
plan deliberately stops short of the prompt_builder and timeout modules —
those live in Plan 05b — so per-plan context budget stays healthy.

Purpose: this is the foundation the rest of the cron-runner work builds on.
Splitting from Plan 05b lets the crate scaffold + script_runner ship in one
context-budget-safe pass (~35%) before the heavier prompt_builder land in
05b (~50%).

Output: the new crate compiles, has its three module declarations and
task-local statics, and `script_runner` is fully implemented and tested.
`prompt_builder` and `timeout` are stub files declared in `lib.rs` but
empty — Plan 05b implements them.

**Context budget for this plan:** Task 1 ≈ 5% (scaffold + workspace
registration), Task 2 ≈ 30% (script_runner sandbox + wake-gate). Total
≈ 35%. Comfortably under the 50% target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md
@.planning/phases/32.1-agent-cron-execution/32.1-RESEARCH.md
@.planning/phases/32.1-agent-cron-execution/32.1-PATTERNS.md
@.planning/phases/32.1-agent-cron-execution/32.1-01-SUMMARY.md
@.planning/phases/32.1-agent-cron-execution/32.1-02-SUMMARY.md
@.planning/phases/32.1-agent-cron-execution/32.1-03-SUMMARY.md
@.planning/phases/32.1-agent-cron-execution/32.1-04-SUMMARY.md
@Cargo.toml
@crates/ironhermes-cron/Cargo.toml
@crates/ironhermes-tools/src/terminal.rs

<interfaces>
<!-- Public API this plan creates -->

pub mod script_runner;
pub mod prompt_builder;  // stub only — implemented in Plan 05b
pub mod timeout;         // stub only — implemented in Plan 05b

tokio::task_local! {
    pub static CRON_AUTO_DELIVER_PLATFORM: String;
    pub static CRON_AUTO_DELIVER_CHAT_ID: String;
    pub static CRON_AUTO_DELIVER_THREAD_ID: String;
}

// script_runner.rs
pub struct ScriptOutcome { pub ok: bool, pub stdout: String, pub stderr: String, pub wake_agent: bool }
pub async fn run_job_script(script_name: &str) -> anyhow::Result<ScriptOutcome>;
pub fn parse_wake_gate(stdout: &str) -> bool;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the new crate — Cargo.toml + lib.rs (module decls + task_locals) + workspace registration</name>
  <files>Cargo.toml, crates/ironhermes-cron-runner/Cargo.toml, crates/ironhermes-cron-runner/src/lib.rs, crates/ironhermes-cron-runner/src/script_runner.rs, crates/ironhermes-cron-runner/src/prompt_builder.rs, crates/ironhermes-cron-runner/src/timeout.rs</files>
  <read_first>
    - Cargo.toml (root — find the `members = [...]` and `default-members = [...]` arrays; verify the existing `crates/ironhermes-cron` entry as the insertion-order analog)
    - crates/ironhermes-cron/Cargo.toml (analog manifest — the new crate copies the `[package]` block style verbatim with `version.workspace = true` etc.)
    - .planning/phases/32.1-agent-cron-execution/32.1-PATTERNS.md (§Cargo.toml lines 35–66 — locked manifest template)
    - .planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md (§Crate layout — locks the dependency list)
  </read_first>
  <action>
    (a) **Create** `crates/ironhermes-cron-runner/Cargo.toml`:

        [package]
        name = "ironhermes-cron-runner"
        version.workspace = true
        edition.workspace = true
        authors.workspace = true
        license.workspace = true
        description = "Agent execution runner for IronHermes cron jobs"

        [dependencies]
        ironhermes-core   = { path = "../ironhermes-core" }
        ironhermes-agent  = { path = "../ironhermes-agent" }
        ironhermes-tools  = { path = "../ironhermes-tools" }
        ironhermes-hooks  = { path = "../ironhermes-hooks" }
        ironhermes-cron   = { path = "../ironhermes-cron" }
        ironhermes-mcp    = { path = "../ironhermes-mcp" }
        serde       = { workspace = true }
        serde_json  = { workspace = true }
        anyhow      = { workspace = true }
        tokio       = { workspace = true }
        tokio-util  = { workspace = true }
        tracing     = { workspace = true }
        uuid        = { workspace = true }
        chrono      = { workspace = true }

        [dev-dependencies]
        tempfile = "3"

    NOTE: per 32.1-PATTERNS.md the `which` crate is NOT in the workspace.
    Plan 05b's script_runner uses env-var fallback (`BASH_PATH`,
    `PYTHON_PATH`) + hardcoded paths. Do NOT add `which` as a dependency.

    NOTE: if `tempfile` is already a workspace dev-dep, prefer
    `tempfile = { workspace = true }` for version pinning consistency.
    Inspect root `Cargo.toml` `[workspace.dependencies]` block first.

    (b) **Register** the new crate in the root `/Users/twilson/code/ironhermes/Cargo.toml`:
    locate the `members = [...]` array and `default-members = [...]` array and
    add `"crates/ironhermes-cron-runner"` to BOTH, preserving alphabetical
    order. Verify by `grep -n "ironhermes-cron-runner" Cargo.toml` returning
    exactly 2 lines.

    (c) **Create** `crates/ironhermes-cron-runner/src/lib.rs`:

        //! Agent execution runner for IronHermes cron jobs.
        //!
        //! This crate owns end-to-end execution of a due `CronJob`: prompt
        //! assembly, sandboxed script execution, agent loop invocation,
        //! timeout enforcement, and per-target delivery dispatch.
        //!
        //! Plan 32.1-05a lands the crate skeleton + `script_runner`.
        //! Plan 32.1-05b lands `prompt_builder` and `timeout`.
        //! Plan 32.1-06 lands the orchestration modules (`runner`,
        //! `delivery`, `tick_loop`) and the `run_cron_job` public entry point.

        pub mod prompt_builder;
        pub mod script_runner;
        pub mod timeout;

        // Per-job delivery context. Propagated to the agent's tool layer
        // (e.g. `send_message`) without env-var mutation. Survives `.await`
        // within the same tokio task; explicit `scope()` wrapping is required
        // for any `tokio::spawn` children.
        tokio::task_local! {
            pub static CRON_AUTO_DELIVER_PLATFORM: String;
            pub static CRON_AUTO_DELIVER_CHAT_ID: String;
            pub static CRON_AUTO_DELIVER_THREAD_ID: String;
        }

    (d) Create empty stub files so `cargo build` succeeds at the end of this
    task:

        crates/ironhermes-cron-runner/src/script_runner.rs:
            //! Sandboxed script execution + wake-gate parsing.
            //! Implemented in Task 2 of plan 32.1-05a.

        crates/ironhermes-cron-runner/src/prompt_builder.rs:
            //! Five-step prompt assembly + assembled-prompt rescan.
            //! Implemented in Task 1 of plan 32.1-05b.

        crates/ironhermes-cron-runner/src/timeout.rs:
            //! Inactivity + wall-clock timeout primitives.
            //! Implemented in Task 2 of plan 32.1-05b.

    (e) Verify the crate compiles:
        `cargo build -p ironhermes-cron-runner`

    Should succeed with zero warnings (no use-statements yet means nothing
    to flag). Three empty modules + three task-local statics are
    syntactically complete on their own.
  </action>
  <verify>
    <automated>cargo build -p ironhermes-cron-runner && grep -c "ironhermes-cron-runner" Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `cargo build -p ironhermes-cron-runner` exits 0
    - source assertion: `grep -c "ironhermes-cron-runner" Cargo.toml` returns at least 2 (members + default-members)
    - source assertion: `ls crates/ironhermes-cron-runner/Cargo.toml` is a regular file
    - source assertion: `ls crates/ironhermes-cron-runner/src/lib.rs crates/ironhermes-cron-runner/src/script_runner.rs crates/ironhermes-cron-runner/src/prompt_builder.rs crates/ironhermes-cron-runner/src/timeout.rs` all four are regular files
    - source assertion: `grep -n "tokio::task_local!" crates/ironhermes-cron-runner/src/lib.rs` matches exactly 1 line
    - source assertion: `grep -nE "pub static (CRON_AUTO_DELIVER_PLATFORM|CRON_AUTO_DELIVER_CHAT_ID|CRON_AUTO_DELIVER_THREAD_ID)" crates/ironhermes-cron-runner/src/lib.rs` returns 3 lines
    - source assertion: `grep -nE "pub mod (script_runner|prompt_builder|timeout)" crates/ironhermes-cron-runner/src/lib.rs` returns 3 lines
    - test-command: `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>The new workspace crate exists, builds standalone and as part of the workspace, declares its three modules, and exports the three cron-delivery task-locals from its root.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: script_runner.rs — sandboxed script execution + wake-gate parsing</name>
  <files>crates/ironhermes-cron-runner/src/script_runner.rs</files>
  <read_first>
    - crates/ironhermes-cron-runner/src/script_runner.rs (the stub from Task 1)
    - crates/ironhermes-tools/src/terminal.rs lines 1–55, 196–220 (subprocess + tokio::time::timeout pattern, `Command::new + stdout/stderr piped + wait_with_output`)
    - .planning/phases/32.1-agent-cron-execution/32.1-CONTEXT.md (§Script execution — locks the sandbox, interpreter selection, timeout env var, secret redaction, `no_agent` short-circuit, wake-gate JSON parsing)
    - .planning/phases/32.1-agent-cron-execution/32.1-RESEARCH.md (Code Examples §Script sandbox; Common Pitfalls Pitfall — `which` crate not in workspace; Open Question 1 — secret redactor location)
    - .planning/phases/32.1-agent-cron-execution/32.1-PATTERNS.md (§script_runner.rs lines 234–318)
    - hermes-agent/cron/scheduler.py (`_run_job_script` and `_parse_wake_gate` for parity reference)
  </read_first>
  <behavior>
    - Test 1 (happy path bash): With `HERMES_HOME` set to a tempdir, write a `.sh` script in `<HERMES_HOME>/scripts/` that prints `hello\n`. Call `run_job_script("test.sh").await`. Returns `Ok(ScriptOutcome { ok: true, stdout: "hello\n", stderr: "", wake_agent: true })`.
    - Test 2 (happy path python): With a `.py` script that prints `world\n`. Returns `ok: true, stdout == "world\n"`.
    - Test 3 (path traversal rejection): Call `run_job_script("../etc/passwd")`. Returns `Err` whose message contains `escapes scripts_dir`. No subprocess is spawned.
    - Test 4 (absolute path rejection): Call `run_job_script("/etc/passwd")`. Same as Test 3.
    - Test 5 (non-existent script): Call `run_job_script("nope.sh")` on an empty scripts dir. Returns `Err` whose message indicates the script was not found / unreadable (the canonicalize step naturally produces this — surface it via `with_context`).
    - Test 6 (non-zero exit): Script `exit 1` returns `Ok(ScriptOutcome { ok: false, stdout: "", stderr: ..., wake_agent: true })`.
    - Test 7 (script timeout): Set `IRONHERMES_CRON_SCRIPT_TIMEOUT=1` and run a script that sleeps 5s. Returns `Ok(ScriptOutcome { ok: false, ... })` with `stderr` containing the literal substring `script timed out`. Total elapsed wall-clock time is bounded under 3s (proves timeout fired).
    - Test 8 (wake gate true, default): `parse_wake_gate("any non-json line\nanother line\n")` returns `true`.
    - Test 9 (wake gate true, valid JSON without wakeAgent): `parse_wake_gate(r#"{"foo":"bar"}"#)` returns `true`.
    - Test 10 (wake gate false): `parse_wake_gate(r#"some preamble\n{"wakeAgent": false}\n"#)` returns `false`.
    - Test 11 (wake gate true when wakeAgent=true): `parse_wake_gate(r#"{"wakeAgent": true}"#)` returns `true`.
    - Test 12 (wake gate: trailing blank lines ignored): `parse_wake_gate("{\"wakeAgent\":false}\n\n\n")` returns `false` (the helper finds the LAST non-empty line).
    - Test 13 (scripts dir auto-create + chmod 0700 Unix): The first call to `run_job_script` after pointing `HERMES_HOME` at a fresh tempdir creates `<HERMES_HOME>/scripts/` with mode `0o700` on Unix. Gated `#[cfg(unix)]`.
    - Test 14 (BASH_PATH override): Set `BASH_PATH=/bin/bash` (or just confirm fallback path used when env unset). Script runs.
    - Test 15 (ScriptOutcome.wake_agent populated): For a script whose LAST line is `{"wakeAgent": false}`, `run_job_script` sets `outcome.wake_agent = false` automatically (parse_wake_gate is called inside).
  </behavior>
  <action>
    Replace the stub `script_runner.rs` with the full implementation:

    (a) Imports:

        use anyhow::{anyhow, bail, Context, Result};
        use std::path::Path;
        use std::process::Stdio;
        use std::time::Duration;
        use tokio::process::Command;
        use tokio::time::timeout;
        use ironhermes_core::get_hermes_home;

    (b) Public types:

        #[derive(Debug, Clone)]
        pub struct ScriptOutcome {
            pub ok: bool,
            pub stdout: String,
            pub stderr: String,
            pub wake_agent: bool,
        }

    (c) Helper `select_interpreter`:

        fn select_interpreter(script_path: &Path) -> String {
            match script_path.extension().and_then(|e| e.to_str()) {
                Some("sh") | Some("bash") => {
                    std::env::var("BASH_PATH").unwrap_or_else(|_| "/bin/bash".to_string())
                }
                _ => {
                    std::env::var("PYTHON_PATH").unwrap_or_else(|_| "python3".to_string())
                }
            }
        }

    (d) Helper `parse_env_u64`:

        fn parse_env_u64(name: &str, default: u64) -> u64 {
            std::env::var(name).ok().and_then(|s| s.parse().ok()).unwrap_or(default)
        }

    (e) Public `parse_wake_gate`:

        pub fn parse_wake_gate(stdout: &str) -> bool {
            let last_non_empty = stdout
                .lines()
                .rev()
                .find(|l| !l.trim().is_empty())
                .unwrap_or("");
            if let Ok(v) = serde_json::from_str::<serde_json::Value>(last_non_empty) {
                if matches!(v.get("wakeAgent").and_then(|v| v.as_bool()), Some(false)) {
                    return false;
                }
            }
            true
        }

    (f) Public `run_job_script`:

        pub async fn run_job_script(script_name: &str) -> Result<ScriptOutcome> {
            let scripts_dir = get_hermes_home().join("scripts");
            tokio::fs::create_dir_all(&scripts_dir).await
                .with_context(|| format!("create scripts dir: {:?}", scripts_dir))?;
            #[cfg(unix)] {
                use std::os::unix::fs::PermissionsExt;
                let _ = std::fs::set_permissions(
                    &scripts_dir,
                    std::fs::Permissions::from_mode(0o700)
                );
            }

            let candidate = scripts_dir.join(script_name);
            let canonical = candidate.canonicalize()
                .with_context(|| format!("script not found or unreadable: {:?}", candidate))?;
            let scripts_dir_canonical = scripts_dir.canonicalize()
                .with_context(|| format!("canonicalize scripts dir: {:?}", scripts_dir))?;
            if !canonical.starts_with(&scripts_dir_canonical) {
                bail!("script path escapes scripts_dir: {:?}", canonical);
            }

            let interpreter = select_interpreter(&canonical);
            let timeout_secs = parse_env_u64("IRONHERMES_CRON_SCRIPT_TIMEOUT", 120);

            let child = Command::new(&interpreter)
                .arg(&canonical)
                .stdout(Stdio::piped())
                .stderr(Stdio::piped())
                .kill_on_drop(true)
                .spawn()
                .with_context(|| format!("spawn script {:?} with {}", canonical, interpreter))?;

            let outcome = match timeout(Duration::from_secs(timeout_secs), child.wait_with_output()).await {
                Ok(Ok(out)) => {
                    let stdout = redact_passthrough(String::from_utf8_lossy(&out.stdout).into_owned());
                    let stderr = redact_passthrough(String::from_utf8_lossy(&out.stderr).into_owned());
                    let wake_agent = parse_wake_gate(&stdout);
                    ScriptOutcome { ok: out.status.success(), stdout, stderr, wake_agent }
                }
                Ok(Err(e)) => return Err(anyhow!("script subprocess failure: {}", e)),
                Err(_) => ScriptOutcome {
                    ok: false,
                    stdout: String::new(),
                    stderr: format!("script timed out after {}s", timeout_secs),
                    wake_agent: true,
                },
            };
            Ok(outcome)
        }

    (g) `redact_passthrough` — local function placeholder since no Rust
    `redact_sensitive_text` was found in the workspace (per RESEARCH.md
    Open Question 1):

        // TODO(Phase 32.x): wire to a workspace secret-redaction primitive once
        // one exists. For now, pass-through preserves the API shape so future
        // callers don't need to change.
        fn redact_passthrough(s: String) -> String { s }

    Capture this as a deferred item in the SUMMARY — Plan 06's runner.rs
    and Plan 07's gateway migration must NOT block on this.

    (h) Tests: `#[cfg(test)] mod tests` covering Tests 1–15. Use
    `tempfile::TempDir` for `HERMES_HOME` isolation. Acquire a single
    `OnceLock<Mutex<()>>` env lock for the env-mutating cases. Some tests
    (path traversal, wake gate parse) need no env mutation.
  </action>
  <verify>
    <automated>cargo test -p ironhermes-cron-runner --lib script_runner -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `cargo test -p ironhermes-cron-runner --lib script_runner -- --test-threads=1` exits 0
    - source assertion: `grep -n "pub async fn run_job_script" crates/ironhermes-cron-runner/src/script_runner.rs` matches exactly 1 line
    - source assertion: `grep -n "pub fn parse_wake_gate" crates/ironhermes-cron-runner/src/script_runner.rs` matches exactly 1 line
    - source assertion: `awk '/pub async fn run_job_script/,/^}/' crates/ironhermes-cron-runner/src/script_runner.rs | grep -c "starts_with"` returns at least 1 (path-traversal guard call)
    - source assertion: `awk '/pub async fn run_job_script/,/^}/' crates/ironhermes-cron-runner/src/script_runner.rs | grep -c "canonicalize"` returns at least 2 (candidate + dir)
    - source assertion: `grep -n "escapes scripts_dir" crates/ironhermes-cron-runner/src/script_runner.rs` matches at least 1 line (the bail! message)
    - source assertion: `grep -n "IRONHERMES_CRON_SCRIPT_TIMEOUT" crates/ironhermes-cron-runner/src/script_runner.rs` matches at least 1 line
    - source assertion: `grep -nE "kill_on_drop\\(true\\)" crates/ironhermes-cron-runner/src/script_runner.rs` matches at least 1 line (timeout cleanup)
    - source assertion: `grep -n "from_mode(0o700)" crates/ironhermes-cron-runner/src/script_runner.rs` matches at least 1 line (scripts dir hardening)
    - test-command: `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>`script_runner` enforces the scripts-dir sandbox, selects the right interpreter, respects `IRONHERMES_CRON_SCRIPT_TIMEOUT`, and `parse_wake_gate` correctly identifies `{"wakeAgent": false}` on the last non-empty stdout line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `${HERMES_HOME}/scripts/<name>` resolved from `job.script` on disk | The `script` field is operator-controlled but the resolution MUST stay inside `${HERMES_HOME}/scripts/`; absolute paths and `..` traversal are the primary attack |
| Subprocess stdout/stderr → callers | Stdout may contain secrets if the script reads env or files; `redact_passthrough` is a TODO seam for a future workspace redactor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32.1.05a-01 | Elevation of Privilege | Path traversal via `script` field | mitigate | `canonicalize() + starts_with(scripts_dir)` in `run_job_script`. Tests 3 + 4 prove `../etc/passwd` and absolute paths are rejected before any subprocess spawn. |
| T-32.1.05a-02 | Denial of Service | Runaway script | mitigate | `IRONHERMES_CRON_SCRIPT_TIMEOUT` (default 120s) + `kill_on_drop(true)` on the subprocess child. Test 7 proves timeout fires within deadline. |
| T-32.1.05a-03 | Information Disclosure | Secret leakage via script stdout | accept-with-TODO | `redact_passthrough` is a pass-through placeholder; no Rust redactor exists in the workspace today (RESEARCH.md Open Question 1). Deferred to a future phase. Risk profile: scripts are operator-controlled, run only when scheduled, and outputs are stored in `0600` files (Plan 04 chmod). |
| T-32.1.05a-04 | Information Disclosure | Cron task-locals exposed across spawn boundaries | mitigate | The `tokio::task_local!` statics propagate across `.await` but not `tokio::spawn`. Callers (Plan 06) wrap any `spawn` with explicit `scope()` wrapping. Documented in the `lib.rs` doc-comment. |
| T-32.1.05a-SC | Tampering | npm/pip/cargo installs | mitigate | Single new dependency on `tempfile` (dev-only). `tempfile` is already a workspace dev-dep, verified by `grep -n "tempfile" Cargo.toml`. No production crates added; all six dependency paths are sibling workspace crates already vetted. |
</threat_model>

<verification>
- `cargo build -p ironhermes-cron-runner` is green
- `cargo build --workspace` is green
- `cargo test -p ironhermes-cron-runner -- --test-threads=1` is green (only script_runner tests in this plan)
- The crate appears in `Cargo.toml` `members` and `default-members`
- `run_job_script` and `parse_wake_gate` are exported and tested
- `prompt_builder` and `timeout` stub files exist (empty doc-comment only) so the crate compiles
</verification>

<success_criteria>
- New workspace crate `ironhermes-cron-runner` builds standalone and inside the workspace
- Three `tokio::task_local!` statics for cron delivery context
- `script_runner` enforces sandbox, interpreter selection, timeout, wake-gate parsing
- `prompt_builder.rs` and `timeout.rs` exist as empty stub modules (Plan 05b implements them)
- No new production workspace dependency (only `tempfile` dev-dep)
</success_criteria>

<output>
Create `.planning/phases/32.1-agent-cron-execution/32.1-05a-SUMMARY.md` when done. Record:
- Whether `tempfile` was wired as `workspace = true` or a literal `"3"` pin
- The exact `redact_passthrough` deferral entry (a TODO comment line + planned follow-up phase number)
- Any deferred items for Plan 05b / Plan 06
</output>
